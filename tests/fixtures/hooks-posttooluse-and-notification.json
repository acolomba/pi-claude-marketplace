{
  "description": "Synthetic fixture: a supported PostToolUse group plus a dropped Notification event (event-level partition, D-71-01)",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [{ "type": "command", "command": "echo posttooluse" }]
      }
    ],
    "Notification": [
      {
        "hooks": [{ "type": "command", "command": "echo notification" }]
      }
    ]
  }
}
