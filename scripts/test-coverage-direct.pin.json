{
  "version": 1,
  "rows": [
    {
      "sourcePath": "extensions/pi-claude-marketplace/bridges/agents/convert.ts",
      "reading": "branches 125/126, lines 751/755",
      "findingIds": ["AG-11"],
      "reasons": [
        "668-671 (branch 668): the `first === undefined` throw in toolsFields' explicit arm. assertMappedToolsNonEmpty already rejected an empty explicit tools list with the user-facing AG-11 message before toolsFields runs, so the arm is unreachable through convertAgent; it exists because destructuring is the only cast-free way to build the non-empty tuple GeneratedToolsFields requires, and the compiler cannot see the earlier assert."
      ]
    },
    {
      "sourcePath": "extensions/pi-claude-marketplace/bridges/commands/discover.ts",
      "reading": "branches 54/55, lines 395/397",
      "findingIds": ["BC-019"],
      "reasons": [
        "277-279 (branch 277): the `if (!(err instanceof CommandNameError)) { throw err; }` narrowing arm around `nameCommandInDir`. BC-019 records the measured disposition: the arm is unreachable -- only a `Symbol.hasInstance` patch reaches its throw -- but the check still narrows the `unknown` catch binding for `badNameWarning(err)`, so deleting it without a restructure breaks typing.",
        "The overlapping-command deduplication fix removes duplicateFileWarning and replaces its warn-and-install block with an early Set-based skip. The merged source has 17 fewer lines and one fewer covered branch. The uncovered deficit remains the same BC-019 arm: one branch and two lines; no new exception is introduced."
      ]
    },
    {
      "sourcePath": "extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts",
      "reading": "branches 120/122, lines 1122/1128",
      "findingIds": ["D-08-A14"],
      "reasons": [
        "The LOAD-02 clear-on-lift note adds a fourteen-line comment inside statePhase's record literal, recording that the omission of `dependencyDisabled` is load-bearing. It introduces no statement and no branch: the reading grows by fourteen lines on both sides and the deficit is unchanged -- the same two arms below, one branch and six lines. No new exception is introduced.",
        "The D-04-01 provenance field adds an optional `provenance` member to InstallLedgerOptions and the statePhase write `existing?.provenance ?? opts.provenance ?? \"explicit\"`. Every arm of that chain is covered (the enable branch, the cascade, and a caller naming neither), so the reading grows by three branches and fourteen lines on both sides and the deficit is unchanged: the same two arms below, one branch and six lines. No new exception is introduced.",
        "The RESV-03 source pin adds one conditional in the git-source resolve callback and its doc comment. Both of its arms are covered, so the reading grows by two branches and eighteen lines on both sides and the deficit is unchanged: the same two arms below, one branch and six lines. No new exception is introduced.",
        "The D-07-06 path-source pin (07-marketplace-repo-tag-resolution) adds the `resolvePathPluginRoot` callback wired beside the existing git-source `resolveGitPluginRoot` one: the `opts.sourcePinOverride !== undefined` conditional spread, the `opts.pathPinProbe ?? materializeMarketplaceTagClone` default, and the `result.kind === \"materialized\"` resolvedSha stamp. Every one of those decision points is covered by its own paired test (pinned vs. unpinned; the default real probe vs. an injected one; materialized vs. missing-subdir), so the reading grows by six branches and thirty lines on both sides and the deficit is unchanged: the same two arms below, one branch and six lines each. No new exception is introduced.",
        "The path-source resolvedSha doc comment (07-marketplace-repo-tag-resolution WR-01, iteration 1) expands the one-line `Path / github-name installs omit it.` note beside the git-source resolvedSha write into a four-line comment recording that a path source ALSO carries a resolvedSha once its constraint pins a marketplace tag. It introduces no statement and no branch: the reading grows by three lines on both sides and the deficit is unchanged -- the same two arms below, one branch and six lines. This row was not refreshed alongside that comment-only change; reconciled here so the bare `test:coverage:direct` gate reads the tree as it stands, pre-dating and unrelated to this phase's iteration-2 findings.",
        "422-426 (branch 422): re-validation of a manifest entry that the identical compiled schema already accepted in the same pass. MARKETPLACE_SCHEMA.plugins is Type.Array(PLUGIN_ENTRY_SCHEMA) and PLUGIN_ENTRY_VALIDATOR is Compile(PLUGIN_ENTRY_SCHEMA), so an entry reaching the re-check was already validated by that exact schema, in this process, from this parse. ARCHITECTURE.md records the re-check as deliberate defense-in-depth.",
        "827-829 (branch 827): the hooks re-parse guard, over bytes on which the resolver already ran both of parseHooksConfig's {ok:false} arms at install-entry under D-57-04. The only difference between the two calls is skipIfMap, and D-61-02 records that every compileIfPredicate failure collapses to MATCH_ALL_IF, so no if-field issue can produce a refusal. The guard fires only on a mutation between the resolve and the phase."
      ]
    }
  ]
}
