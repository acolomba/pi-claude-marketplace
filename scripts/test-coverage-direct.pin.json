{
  "version": 1,
  "rows": [
    {
      "sourcePath": "extensions/pi-claude-marketplace/bridges/agents/convert.ts",
      "reading": "branches 125/126, lines 751/755",
      "findingIds": ["AG-11"],
      "reasons": [
        "668-671 (branch 668): the `first === undefined` throw in toolsFields' explicit arm. assertMappedToolsNonEmpty already rejected an empty explicit tools list with the user-facing AG-11 message before toolsFields runs, so the arm is unreachable through convertAgent; it exists because destructuring is the only cast-free way to build the non-empty tuple GeneratedToolsFields requires, and the compiler cannot see the earlier assert."
      ]
    },
    {
      "sourcePath": "extensions/pi-claude-marketplace/bridges/commands/discover.ts",
      "reading": "branches 55/56, lines 412/414",
      "findingIds": ["BC-019"],
      "reasons": [
        "288-290 (branch 288): the `if (!(err instanceof CommandNameError)) { throw err; }` narrowing arm around `nameCommandInDir`. BC-019 records the measured disposition: the arm is unreachable -- only a `Symbol.hasInstance` patch reaches its throw -- but the check still narrows the `unknown` catch binding for `badNameWarning(err)`, so deleting it without a restructure breaks typing."
      ]
    },
    {
      "sourcePath": "extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts",
      "reading": "branches 114/119, functions 30/31, lines 1168/1183",
      "findingIds": ["D-08-A14", "WMERGE-03"],
      "reasons": [
        "461-464 (branch 461, was 422-426 before the workflows phase was inserted earlier in the file): re-validation of a manifest entry that the identical compiled schema already accepted in the same pass. MARKETPLACE_SCHEMA.plugins is Type.Array(PLUGIN_ENTRY_SCHEMA) and PLUGIN_ENTRY_VALIDATOR is Compile(PLUGIN_ENTRY_SCHEMA), so an entry reaching the re-check was already validated by that exact schema, in this process, from this parse. ARCHITECTURE.md records the re-check as deliberate defense-in-depth.",
        "868-869 (branch 868, was 827-829): the hooks re-parse guard, over bytes on which the resolver already ran both of parseHooksConfig's {ok:false} arms at install-entry under D-57-04. The only difference between the two calls is skipIfMap, and D-61-02 records that every compileIfPredicate failure collapses to MATCH_ALL_IF, so no if-field issue can produce a refusal. The guard fires only on a mutation between the resolve and the phase.",
        "990-991 (branch 990): workflowsPhase's own undo guard `if (c.workflowsPrep === undefined) { return; }`. workflowsPhase is the last content phase in the D-01 literal array, immediately before statePhase, and its do() assigns c.workflowsPrep unconditionally the instant prepareStageWorkflows resolves -- before anything else in the phase body runs. A do() that throws before that assignment never has its own undo invoked (runPhases only unwinds phases that already committed), and any later phase (only statePhase) failing implies workflowsPhase.do() already succeeded and already set the field. The guard cannot observe undefined through any real ledger path.",
        "985-986 (branch 985) and 1006-1010 (branch 1006): the two deep fault-injection arms of the workflows commit/rollback pair -- a leak string returned by a SUCCESSFUL commitPreparedWorkflows (985-986), and unstagePluginWorkflows itself failing during the undo rollback (1006-1010, throws WorkflowsUnstageFailureError). Both require a fault one layer beneath this module's own test doubles (a filesystem failure mid-bridge-commit, or mid-bridge-unstage) rather than anything this module's ledger orchestration controls; the install-side counterpart of WMERGE-01/WMERGE-02, pinned for the same reason in reinstall-replace.ts's rollback pair."
      ]
    },
    {
      "sourcePath": "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-replace.ts",
      "reading": "branches 51/53, functions 18/19, lines 661/675",
      "findingIds": ["WMERGE-01", "WMERGE-02"],
      "reasons": [
        "297-308 (unplaceWorkflows, whole function): the rollback-side catch around `unstagePluginWorkflows` during a reinstall abort. Reachable only when the workflows bridge's own containment/rename cleanup throws mid-rollback -- a fault one layer beneath this module's own test doubles, which stub the bridge call rather than its internal `assertPathInside`/rename failure paths. The function's own header comment records why the catch swallows rather than propagates: a second throw here would replace the real reinstall failure and discard every leak string already collected.",
        "475-476: `commitPreparedWorkflows` returning a defined leak string on its SUCCESS path (the commit placed every envelope but a non-fatal staging-cleanup step failed). Exercised on the abort path elsewhere in this suite; the success-path leak is a narrower combination -- commit succeeds AND reports a soft leak -- that the current fixtures do not drive."
      ]
    }
  ]
}
