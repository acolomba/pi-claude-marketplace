---
milestone: v1.18
audited: 2026-08-10T09:30:00Z
status: tech_debt
scores:
  requirements: 22/22
  phases: 4/4
  integration: 4/4
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: cross-phase (integration audit)
    items:
      - "ManifestLookup is not exported: list.ts, info.ts, and update.ts each derive manifest-absence independently; the three agree today but a fourth surface copying the lookup idiom without the read-success guard would reintroduce the BOUND-03 defect ungated. Priority fix: export the discriminant so consumers take the value, not the rule."
      - "The ENBL-05 drift gate's regexes miss three twin spellings (destructured `{ enabled }`, bracket access, Boolean() comparison); the destructured form is the likeliest to be written naturally."
      - "reinstall.ts declares fields literally named stagedAgents/stagedMcpServers as string arrays on its own outcome type beside the shared LedgerDegradationSignals booleans of the same names — a future enableRowDependencies(reinstallOutcome) call would silently read a truthy array as true."
  - phase: 98-lifecycle-regression-and-contract-documentation
    items:
      - "Carried (todo): update verb drops degradation signals — a malformed SKILL.md pulled by update renders a clean (updated) row (2026-08-10-update-verb-drops-degradation-signals.md, WR-12)."
      - "Deferred (doc-only, 98-06-SUMMARY.md): autoupdate cascade skip row has no catalog state; catalog's description-bearing variant count stale in an untouched section; RLD-04/D-08 anchors survive at six source sites outside the swept scope."
  - phase: 97-disabled-state-classification-repair
    items:
      - "Carried (todo): stale resolvedSource on unchanged version — preflightUpdate returns unchanged before the disabled branch, so refreshDisabledRecord never runs when the source moved but the version pin did not (2026-08-09-disabled-record-stale-resolvedsource-on-unchanged-version.md)."
  - phase: backlog (pre-existing)
    items:
      - "2026-06-12 coverage sweep: rare failure arms in update/reinstall/install."
nyquist:
  compliant_phases: [95, 96, 97, 98]
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: compliant
---

# Milestone v1.18 — Audit Report

**Milestone:** Manifest-Independent Installed Plugin Info
**Audited:** 2026-08-10
**Status:** tech_debt (no blockers; deferred items enumerated for review)

## Requirements Coverage — 22/22 satisfied

Three-source cross-reference (VERIFICATION.md status × SUMMARY frontmatter ×
REQUIREMENTS.md traceability): every REQ-ID resolves to **satisfied** — all four
phase VERIFICATIONs are `passed`, every ID appears in at least one SUMMARY's
`requirements-completed`, and every traceability row is checked `[x]` /
`Complete`. No orphans: every ID appears in its phase's verification report.

| Phase | Requirements | Verification | Nyquist | threats_open |
|-------|--------------|--------------|---------|--------------|
| 95 — manifest-independent installed inventory | INV-01..05, BOUND-03 | passed (21/21 with operator UAT) | compliant | 0 |
| 96 — installation-record-backed plugin info | INFO-09..12, BOUND-01/02 | passed (15/15) | compliant | 0 |
| 97 — disabled-state classification repair | ENBL-05..09 | passed (6/6 criteria, 29/29 must-haves) | compliant | 0 |
| 98 — lifecycle regression and contract documentation | LIFE-04..06, COMPAT-01, DOC-08 (+ carriers IN-07/WR-06/WR-02/WR-04) | passed (5/5 criteria, 9/9 IDs) | compliant | 0 |

## Integration — 4/4 seams wired (0 blockers, 4 warnings)

1. **Manifest-absence authority** — list/info/update all license an absence claim
   only on a successful manifest read plus an exact-identity miss; the LLM tool
   payload consumes list's rows rather than re-deriving. WIRED. *Warning:* three
   independent implementations of one rule; nothing gates a fourth surface.
2. **`isRecordedButDisabled`** — single definition, nine consumers spanning
   every classification surface; the whole-tree drift gate passes. WIRED.
   *Warning:* three twin spellings escape the gate's regexes.
3. **`LedgerDegradationSignals`** — genuinely single-sourced and inherited (not
   redeclared) by both reconcile projections; `enableRowDependencies` shared by
   standalone and orchestrated enable rows. WIRED. *Warning:* reinstall's
   same-named string-array fields invite a type confusion.
4. **COMPAT-01 closed-set pins** — the gate imports the live constants and
   asserts exact membership and order; `not in manifest` sits inside the pinned
   set. WIRED.

## E2E Flows — 3/3 complete

- **(a) install → manifest removal → list → info → update → uninstall:** every
  hop assertion-backed, including info's zero-network guarantee (injected-mock
  call counters) and per-kind uninstall cleanup.
- **(b) disabled partial lifecycle:** disable → parity rows → `update
  --partial` refresh (consent-preserving) → enable through the partial arm with
  markers. *Known carried residual:* stale resolvedSource on unchanged version.
- **(c) load-time reconcile:** backfill fixed point proven with an
  enabled-record positive control; cascade rows inherit the signal shape.

## Tech Debt Summary

Six tracked items (three pending backlog todos with mechanism + repro, three
doc-only deferrals recorded in 98-06-SUMMARY.md) plus three newly observed
fragility warnings from the integration audit (frontmatter `tech_debt`). None
blocks the milestone; the highest-leverage follow-up is exporting the
`ManifestLookup` discriminant so absence is consumed as a value, not re-derived
as a rule.
