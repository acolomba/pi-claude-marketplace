---
context: default
phase: 117-measured-agent-failure-evidence (milestone workflows-replay, archived)
task: null
total_tasks: null
status: paused
last_updated: 2026-09-23T10:40:14.991Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Prove both installed Pi versions before claiming a cross-version test. The first combined smoke used Pi 0.85.1 in both the host and engine checkout, so it did not recreate the original mismatch. Read the host version and the engine node_modules package version before launching the test.
- [ ] CONSTRAINT: Never report a gate result from the exit status of a compound shell command. Capture the target command status directly in its log and confirm it with a second signal.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Inferring the engine SDK version | The source branch and prior install history did not prove which Pi version node_modules contained. The first combined smoke therefore tested matching versions. | blocking | Print the host Pi version and read the engine's installed pi-coding-agent package.json before every compatibility claim. |
| Reading the last compound-command status as the gate status | A trailing echo or grep can hide a failing gate or make a passing gate look red. | blocking | Write the target command's exit status into its log immediately, then inspect both that value and the failure markers. |
| Diagnosing a network-denied run as a workflow defect | The first interactive smoke failed with fetch failed because it ran in the restricted sandbox. | advisory | If the provider request reports fetch failed, repeat the same disposable test with approved network access before investigating the workflow runtime. |
| Treating the transient pending row as the final delivery state | The panel shows pending delivery while the parent model processes the injected result. The durable record cleared pendingDelivery after acknowledgment. | advisory | Wait for the parent turn and inspect the final persisted delta before classifying delivery as stuck. |

<current_state>
The workflows-replay milestone is archived and has no active phase. This session
continued post-milestone UAT and upstream defect work on features/workflow.

The repository branch is seven commits ahead of origin/features/workflow. Commit
a0d8f9ee records the resolved OpenAI child-tool investigation. No production
code changed in this repository during the upstream fix work.

Three pi-dynamic-workflows pull requests are open:

- PR 232: fix(delivery): capture explicitly loaded host sessions
- PR 233: fix(runtime): create children with the host Pi SDK
- PR 234: fix(ui): label completed workflow status correctly

GitHub reports all three OPEN and BLOCKED with no checks. The upstream guide says
a maintainer must approve the first CI run from a fork.

The combined plugin smoke test passed under the original hard boundary:

- Current pi-claude-marketplace extension from features/workflow
- Pi host 0.85.1
- pi-dynamic-workflows combined local build with Pi dependency 0.87.0
- OpenAI gpt-5.6-terra
- Explicit --no-extensions plus -e launch shape
- Run read-fixture-mudxlglt-nz0e5t

The child made native read and structured_output calls with zero tool errors. It
returned workflow-tool-probe, delivered the result into the parent conversation,
removed pendingDelivery, and printed Workflow completed. It used 2,619 tokens,
cost about $0.0059, and completed in 3.6 seconds.
</current_state>

<completed_work>

Completed Tasks:

- Diagnosed the OpenAI child-tool failure as a Pi 0.85.1 host runtime being injected into a child created by Pi 0.87.0.
- Confirmed that pi-dynamic-workflows 3.13.0 was both installed and current upstream.
- Prepared and fully tested the delivery fix. Upstream branch fix/explicit-extension-delivery, commit 8c62327, PR 232.
- Prepared and fully tested the SDK compatibility fix. Upstream branch fix/host-session-runtime-compat, commit d3ef63a, PR 233.
- Prepared and fully tested the completed-status heading fix. Upstream branch fix/completed-status-heading, commit abb61e7, PR 234.
- Ran the upstream full test gates. The delivery and UI branches passed 1,642 tests; the runtime branch passed 1,641 tests. Each release gate reported zero warnings.
- Created local integration branch test/combined-plugin-smoke from upstream main and cherry-picked the three PR commits.
- Built a disposable combined engine with Pi dependency 0.87.0 and ran the plugin through Pi host 0.85.1 using OpenAI only.
- Removed copied auth.json files from /tmp/pi-plugin-combined.QM4AZW and /tmp/openai-workflow-tools.b6WgdN. The normal tmp/pi-uat authentication was not removed.
</completed_work>

<remaining_work>

- Monitor upstream PRs 232, 233, and 234. Respond to CI and maintainer review.
- Rotate or re-authenticate the OpenAI credential exposed during the earlier audit investigation.
- Decide whether features/workflow needs a version bump. Main released 0.19.0 while this branch already used 0.19.0.
- Open the pi-claude-marketplace pull request for features/workflow.
- Decide whether the code-modernization workflow's line-sensitive credential-finding dedup deserves a separate fix. It can give conflicting verdicts for the same ignored credential at adjacent lines.
</remaining_work>

<decisions_made>

- Used OpenAI credentials only. Anthropic credentials are not a requirement and were not used.
- Split the upstream work into three PRs because pi-dynamic-workflows requires one concern per PR.
- Kept the all-fixes integration branch local. The GitHub fork carries the three isolated PR branches, while the local integration branch proves that the fixes work together.
- Left pi-claude-marketplace production code unchanged because all three fix sites are in pi-dynamic-workflows.
- Did not repeat the expensive 12-agent scan. The small combined workflow exercised child tools, structured output, result delivery, and status rendering for about $0.006.
</decisions_made>

<blockers>

- Upstream CI is waiting for maintainer approval on PRs 232-234. No checks are visible yet.
- OpenAI credential rotation requires the account owner. Disposable copies are gone, but rotation cannot be completed by the agent.
- There is no remaining technical blocker for the combined OpenAI plugin workflow path.
</blockers>

## Required Reading (in order)

1. .planning/HANDOFF.json — machine-readable current position and pending actions.
2. .planning/workstreams/workflows/debug/resolved/openai-workflow-child-tools.md — root cause, eliminated hypotheses, and canonical UAT evidence.
3. AGENTS.md — repository rules, commit gates, versioning, and PR policy.
4. .planning/workstreams/workflows/STATE.md — archived milestone state and why the next repository action is a PR.

## Critical Anti-Patterns (do NOT repeat these)

- CROSS-VERSION CLAIM WITHOUT VERSION PROOF: read both installed package versions before running or reporting the test.
- PROVIDER FAILURE FROM A RESTRICTED PROCESS: a fetch failed result is not workflow evidence; use approved network access.
- EARLY DELIVERY VERDICT: wait for the delivery acknowledgment delta before deciding that pending delivery is stuck.
- COMPOUND EXIT STATUS: capture the target gate status before any echo, grep, or formatting command.

## Infrastructure State

- No Pi process, server, watcher, or workflow remains running.
- GitHub CLI is authenticated as acolomba.
- /tmp/pi-dynamic-workflows-upstream contains the three PR branches and local test/combined-plugin-smoke. Its origin is QuintinShaw/pi-dynamic-workflows and its fork remote is acolomba/pi-dynamic-workflows.
- /tmp/pi-dw-combined-87.XOlCsE contains the combined compiled engine over a Pi 0.87.0 dependency tree.
- /tmp/pi-plugin-combined.QM4AZW contains the successful run and session evidence. Its copied auth.json was removed.
- Successful run record: /tmp/pi-plugin-combined.QM4AZW/home/.pi/workflows/projects/work-2a5f4acd3de5/runs/read-fixture-mudxlglt-nz0e5t.json
- Successful event history: /tmp/pi-plugin-combined.QM4AZW/home/.pi/workflows/projects/work-2a5f4acd3de5/runs/read-fixture-mudxlglt-nz0e5t.json.events.jsonl
- /tmp/openai-workflow-tools.b6WgdN remains as older diagnostic evidence, but its copied auth.json was removed.
- tmp/pi-uat remains the repository UAT home and still owns its configured OpenAI authentication.
- Per-machine files remain intentionally unstaged: .claude/settings.json, .codex/config.toml, and .mcp.json.
- .planning/workstreams/workflows/.verification-ledger.json is also untracked and was not folded into this WIP commit.

<context>
The workflow path is now proven end to end with an OpenAI model and with the
exact host-to-engine SDK skew that caused the original failure. The visible
pending-delivery row during the successful run was temporary: the parent session
received the result, produced its acknowledgment, and sequence 5 removed
pendingDelivery from the durable run record.

The three GitHub PRs contain the same patches used in the combined smoke, but no
single remote branch combines them. The local integration branch cherry-picks
all three so upstream can still review and merge one concern at a time.

The next repository-level work is release and PR preparation, not another GSD
phase. Before creating that PR, follow the project rule to offer a version bump,
update package.json, sonar-project.properties, package-lock.json, and CHANGELOG.md
if approved, then run the required gates.
</context>

<next_action>
Start with: check PRs 232-234 for approved CI or review feedback. Then decide the
features/workflow version bump and prepare the pi-claude-marketplace PR.
</next_action>
