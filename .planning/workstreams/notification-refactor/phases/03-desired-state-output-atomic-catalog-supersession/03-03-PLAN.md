---
phase: 03-desired-state-output-atomic-catalog-supersession
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - docs/output-catalog.md
autonomous: true
requirements: [OUT-08, GATE-02]
must_haves:
  truths:
    - "OUT-08/D-06: the catalog `present`→`installed` grammar collapse lands HERE — the `present` discriminator references in docs/output-catalog.md PROSE and the status-token reference TABLE are collapsed/merged into `installed`; the runtime collapse already shipped in Phase 2."
    - "no fenced ```text block emits the literal `(present)` token (the render was already byte-identical to `(installed)`) — so this is a prose/table-only edit with ZERO fixture-byte change (RESEARCH blast-radius (d) / Assumption A4)."
    - "OUT-08: the `reasons` set stays CLOSED; per-row grammar (icons, status tokens) preserved except this `present`→`installed` collapse."
    - "GATE-02: catalog-uat stays GREEN — the two-direction walk is unaffected because no catalog-state annotation or fence body changes (only prose/table text)."
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "prose + status-token reference table with `present` collapsed into `installed`"
  key_links:
    - from: "status-token reference table (catalog L131-132)"
      to: "single `(installed)` row"
      via: "merge of the `(installed) (via present discriminator)` row"
      pattern: "installed"
---

<objective>
Land the catalog `present`→`installed` grammar collapse (OUT-08 / D-06). The runtime
status collapse shipped in Phase 2 (`present` plugin status folded into `installed` +
`needsReload: false`); the catalog markdown PROSE and the status-token reference TABLE
still reference the `present` discriminator. Collapse those references into `installed`.

This is a prose/table-only edit — RESEARCH confirmed no fenced ```text block emits the
literal `(present)` token (the render was already `(installed)`), so there is ZERO
fixture-byte change and the catalog-uat two-direction walk is unaffected. Runs last so
it does not contend with Plans 01/02's large catalog-block rewrites on the same file.

Purpose: Finish the catalog supersession by removing the stale `present` grammar so the
hand-authored catalog matches the post-Phase-2 runtime (RLD-04 / D-08).
Output: docs/output-catalog.md with `present` collapsed into `installed` in prose +
the status-token reference table.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse `present`→`installed` in catalog prose + status-token reference table (prose/table-only, no fixture byte change)</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - docs/output-catalog.md — the `present` references: L73 (reload-hint trailer prose describing `present` as the list-only inventory token excluded from the trigger set), L131-132 (the status-token reference TABLE rows — `(installed)` and `(installed) (via present discriminator)` — to merge), L247 (the failed-marketplace prose mentioning the `present` plugin row), L289 (the list-row version prose `The present inventory discriminator carries no /reload trailer`), L318 (the PL-4 description prose listing `present` among the four list-surface variants).
    - 03-PATTERNS.md §"docs/output-catalog.md" (`present`→`installed` collapse is PROSE/TABLE-only, no fenced-block byte change; edit the status-token reference table row and the `present` prose; the byte-compared fences are untouched).
    - 03-RESEARCH.md §"The Catalog / Fixture Byte Contract" blast-radius row (d) (`present` collapse = prose + status-token-reference-table edit, NOT a fixture-byte change), Assumption A4 (grep found no `(present)` in fenced blocks — verify with the grammar-invariant test), §"State of the Art" (`present` collapsed into `installed` + `needsReload:false` in Phase 2 runtime; Phase 3 catalog grammar).
    - 03-CONTEXT.md D-06 (catalog grammar `present`→`installed` collapse lands here; reasons set stays closed; no generation seam), RLD-04/D-08 (the Phase-2 runtime collapse this catches up to).
  </read_first>
  <action>
Edit docs/output-catalog.md prose + the status-token reference table ONLY. In the
status-token reference table (catalog L131-132), merge the
`(installed) (via present discriminator)` row into the single `(installed)` row —
the list-surface steady-state inventory is now just `(installed)` with `needsReload:
false` (no separate `present` discriminator). Update the surrounding prose that names
the `present` discriminator: L73 (reload-hint trailer prose — drop the `present`
inventory-token straddle language now that the token is `installed`), L247 (the
failed-marketplace prose — the other marketplace's list row is `installed`, not
`present`), L289 (the list-row version prose — `installed` inventory row carries no
`/reload` trailer), L318 (the PL-4 description prose — the list-surface variants are
`installed`/`upgradable`/`available`/`unavailable`, dropping `present`). Do NOT touch
any ```text fenced block — none emits `(present)` (the render was already `(installed)`),
so there is no fixture-byte change and catalog-uat's two-direction walk is unaffected.
Do NOT touch the `reasons` set or any catalog-state annotation. First verify the
assumption by confirming no fenced block emits the literal `(present)` token
(`grep -n '(present)' docs/output-catalog.md` should match only prose/table lines, never
inside a ```text fence). If any fence DOES emit `(present)`, STOP and treat it as a
fixture-bearing change (pair it with its catalog-uat fixture in the same commit) — but
RESEARCH/A4 expect none.
  </action>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts" "tests/architecture/notify-grammar-invariant.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/architecture/catalog-uat.test.ts" "tests/architecture/notify-grammar-invariant.test.ts"` exits 0 (no fixture-byte change; grammar-invariant test green — no `present`-token assumption left).
    - `grep -n 'present discriminator\|via present' docs/output-catalog.md` returns no matches (the discriminator references are collapsed).
    - The status-token reference table has a single `(installed)` row (no separate `(via present discriminator)` row).
    - No ```text fenced block was edited: `git diff docs/output-catalog.md` shows changes only to prose/table lines, never inside a fence body.
    - `npm run check` green.
  </acceptance_criteria>
  <done>The catalog `present` discriminator is collapsed into `installed` in prose + the status-token reference table with zero fixture-byte change; catalog-uat, grammar-invariant, and npm run check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only edit (catalog prose + reference table). No code, no user-visible runtime string, no input/path/secret crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | docs/output-catalog.md prose/table | accept | Documentation-only edit; no runtime behavior, no path/secret/PII. No new cause-bearing row introduced. No leak surface. |
| T-03-06 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (markdown-only edit). No legitimacy gate required. |
</threat_model>

<verification>
- `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 (GATE-02 — green; no fixture change).
- `node --test "tests/architecture/notify-grammar-invariant.test.ts"` exits 0 (no lingering `present`-token assumption).
- `npm run check` green (GATE-03).
</verification>

<success_criteria>
- OUT-08/D-06 catalog `present`→`installed` grammar collapse complete (prose + status-token table); reasons set stays closed.
- GATE-02 catalog-uat green with zero fixture-byte change (prose/table-only).
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-03-SUMMARY.md` when done.
</output>
