---
phase: 03-desired-state-output-atomic-catalog-supersession
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/architecture/notify-producer-wire-coverage.test.ts
autonomous: true
requirements: [OUT-01, OUT-02, OUT-05, OUT-08, GATE-02]
must_haves:
  truths:
    - "OUT-02/D-02: every error/warning emission leads with `[A|Some] <subject> operation[s] has/have failed | needs/need attention.` (subject = plugin|marketplace; A=1 row, Some>1; `has/have failed` for error, `needs/need attention` for warning; terminal period); info has no leading sentence."
    - "OUT-06/D-03: a cascade whose rows span both plugin and marketplace subjects drops the subject noun (`[A|Some] operation[s] ...`), detected at RENDER TIME from the live counts in buildSummaryLineForCascade (the existing `counts.plugins > 0 && counts.marketplaces > 0` branch)."
    - "OUT-01: error/warning emit via `ctx.ui.notify(msg, severity)` retaining the host label; info omits the 2nd arg — the single emitWithSummary seam is preserved (exactly one ctx.ui.notify call)."
    - "D-01 absent-target: `reinstall` not-installed and `update` not-installed stamp `severity: \"error\"` (was warning); the `(skipped) {not installed}` per-row token is preserved (severity-only flip, option A)."
    - "D-01 PU-5: the STANDALONE uninstall already-gone path emits an `error` row (was literal silence); the ORCHESTRATED `{status: \"converged\"}` arm stays silent (race-safety WR-06/NFR-2)."
    - "OUT-05/D-05: a plugin row never renders without its marketplace header (renderMpHeader runs once per block — audited, no plugin-row-without-header path remains)."
    - "OUT-08/GATE-02/D-06: docs/output-catalog.md summary-bearing fenced blocks + catalog-uat fixtures are rewritten IN LOCKSTEP; catalog-uat is GREEN at every commit boundary."
    - "notify-producer-wire-coverage.test.ts gains absent-target wire fixtures (reinstall/update not-installed, uninstall PU-5) asserting producer→wire `error`/no-trailer, updated in the SAME commit as the producer flips."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "D-02 leading-sentence grammar in buildSummaryLine + buildSummaryLineForCascade with D-03 render-time mixed-subject detection"
      contains: "has/have failed"
    - path: "docs/output-catalog.md"
      provides: "rewritten summary-bearing fenced blocks + new uninstall PU-5 state"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "fixtures matching the rewritten catalog blocks + the new PU-5 fixture"
    - path: "tests/architecture/notify-producer-wire-coverage.test.ts"
      provides: "3 absent-target wire fixtures (error/no-trailer)"
  key_links:
    - from: "buildSummaryLineForCascade (notify.ts:2268)"
      to: "buildSummaryLine cascade tail (notify.ts:2321) + reconcile-applied arm"
      via: "shared D-02 grammar"
      pattern: "has/have failed|needs/need attention"
    - from: "reinstall.ts:863 / update.ts:1564 / uninstall.ts:542 absent-target arms"
      to: "stamped severity \"error\""
      via: "producer emit-site stamp (not skipSeverity)"
      pattern: "severity: \"error\""
---

<objective>
Land the D-02 leading severity sentence (OUT-02) with D-03 render-time mixed-subject
detection (OUT-06), and the D-01 absent-target severity flips (reinstall/update
not-installed → error) plus the PU-5 standalone uninstall → error row — all with
atomic catalog supersession (OUT-08 / GATE-02 / D-06).

This is the dominant rendered-bytes commit of the phase. Per RESEARCH Pitfall 3, the
moment buildSummaryLine changes grammar, every summary-bearing fixture goes red until
its catalog block is rewritten — so the grammar rewrite and ALL summary-bearing
catalog blocks/fixtures land in ONE atomic commit (Task 1). The absent-target producer
flips + their catalog states then land using the already-rewritten D-02 grammar
(Task 2), avoiding a double-touch of any catalog block.

Purpose: First phase that changes rendered output; establish the desired-state leading
sentence and the absent-target error severity, keeping catalog-uat green at every
commit boundary.
Output: Rewritten summary grammar in notify.ts; flipped absent-target producers;
new PU-5 error row; superseded catalog blocks + fixtures; new wire-coverage fixtures.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-PATTERNS.md
@.planning/workstreams/notification-refactor/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-02 leading-sentence grammar rewrite + D-03 mixed-subject detection + all summary-bearing catalog blocks/fixtures (atomic)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts — `buildSummaryLineForCascade` (notify.ts:2268-2288, the OLD grammar to rewrite), `buildSummaryLine` (notify.ts:2321, cascade tail + reconcile-applied arm), `operationPhrase` (notify.ts:2294, the `count === 1 ? singular : plural` ternary idiom to mirror), `emitWithSummary` (notify.ts:2938, the single emission seam — DO NOT add a second ctx.ui.notify), `cascadeSeverity`/`computeSeverity` (notify.ts:2121/2144, KEEP unchanged — pure MAX), `renderMpHeader` (notify.ts:1466, per-block header for the D-05 audit).
    - 03-PATTERNS.md §"shared/notify.ts — D-02 leading-sentence rewrite" (the three-step verb-select → count → stitch skeleton to preserve; the mixed-subject branch `counts.plugins > 0 && counts.marketplaces > 0` is the D-03 detection point) and §"shared/notify.ts — thread Messaging.label" (the single emission seam to preserve).
    - 03-RESEARCH.md §"The Summary Composition Surface" + §"How D-03 mixed-subject detection reads rows" + Pitfall 3 (the ~40-50-fixture simultaneous-red hazard → one atomic commit) + §"Common Pitfalls".
    - 03-CONTEXT.md D-02 (exact sentence format), D-03 (render-time mixed-subject), D-05 (always-header), D-06 (atomic supersession).
    - docs/output-catalog.md — the "Summary line (error / warning)" prose section (≈L112-123) and the per-command summary-bearing fenced blocks across the plugin/marketplace/import/reconcile-applied sections.
    - tests/architecture/catalog-uat.test.ts — the `FIXTURES` map shape (`{ message, pi, expectedSeverity? }`), the forward+inverse walk driver (≈L3056-3120) that byte-compares `ctx.ui.notify.mock.calls[0].arguments[0]` to the catalog fence.
  </read_first>
  <behavior>
    - Error, 1 plugin row: leading sentence `A plugin operation has failed.`
    - Warning, >1 plugin rows: leading sentence `Some plugin operations need attention.`
    - Error, 1 marketplace row: `A marketplace operation has failed.`
    - Mixed subjects (plugin rows AND marketplace rows present), error: `Some operations have failed.` (subject noun dropped, D-03).
    - Info severity: no leading sentence emitted (emitWithSummary info arm unchanged — `ctx.ui.notify(body)`).
    - byte-equality: every summary-bearing catalog fenced block matches the new grammar; catalog-uat forward + inverse walks both pass.
  </behavior>
  <action>
Rewrite `buildSummaryLineForCascade` (notify.ts:2268) to the D-02 grammar, keeping its
existing three-step skeleton (verb-select → count plugin-vs-mp → stitch). Replace the
OLD literals (`${count} ${kind} operation[s] failed|skipped.`) with the D-02 form:
article = `count === 1 ? "A" : "Some"`; subject word = `plugin`|`marketplace`;
operationWord = `count === 1 ? "operation" : "operations"`; verb phrase =
`severity === "error" ? (count === 1 ? "has failed" : "have failed") : (count === 1 ? "needs attention" : "needs need attention")` — correct verb agreement: error → `has failed`/`have failed`, warning → `needs attention`/`need attention`; terminal period kept.
Mirror the `operationPhrase` (notify.ts:2294) ternary idiom; keep a small local helper
that maps count → {article, operationWord, verbPhrase} rather than inlining. For D-03,
the EXISTING `counts.plugins > 0 && counts.marketplaces > 0` branch (the first `if` in
buildSummaryLineForCascade) drops the subject noun and renders `[A|Some] operation[s]
<verbPhrase>.` counting all rows uniformly (article/operationWord/verb keyed off the
total row count across both subjects). This is render-time detection by construction
(reads live counts, satisfying D-03). Apply the SAME D-02 grammar to the cascade tail
and reconcile-applied arm inside `buildSummaryLine` (notify.ts:2321) so the two call
paths stay byte-parallel. Do NOT touch `cascadeSeverity`/`computeSeverity` (pure MAX,
SEV-02/04) and do NOT add a second `ctx.ui.notify` (IL-2 — the leading sentence stays
the `buildSummaryLine` prefix in emitWithSummary). Audit D-05: confirm `renderMpHeader`
runs once per marketplace block in every cascade seam (notify.ts:3104/3150) — no
plugin-row-without-header path; this is verify-only, expect no code change.
IN THE SAME COMMIT rewrite the leading summary line of EVERY summary-bearing fenced
block in docs/output-catalog.md to the D-02 grammar (the OLD `N plugin operation(s)
failed|skipped.` examples, including the prose at the "Summary line" section ≈L112-123),
and update the matching `catalog-uat.test.ts` fixtures so the forward + inverse walks
stay green. This is unavoidably one large atomic commit (≈40-50 blocks) — do NOT split
the grammar change from the catalog blocks (catalog-uat would be red between commits,
violating GATE-02). The byte-compared fenced blocks are the contract; catalog prose
wording is discretion but must not contradict the fences.
  </action>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 (forward + inverse walks green; byte-equality holds for every rewritten block).
    - `grep -n 'has failed\|have failed\|needs attention\|need attention' extensions/pi-claude-marketplace/shared/notify.ts` shows the D-02 verb phrases present in buildSummaryLineForCascade.
    - A 1-plugin error cascade renders the leading sentence `A plugin operation has failed.` (verified via its catalog fixture byte-block).
    - A mixed plugin+marketplace error cascade renders `Some operations have failed.` (subject noun dropped — the D-03 reconcile-applied / import fixture).
    - No second `ctx.ui.notify` call added: `grep -c 'ctx.ui.notify' extensions/pi-claude-marketplace/shared/notify.ts` is unchanged from baseline for the emitWithSummary seam.
    - `npm run check` green.
  </acceptance_criteria>
  <done>D-02 leading sentence + D-03 mixed-subject grammar live in notify.ts; all summary-bearing catalog blocks + fixtures rewritten in lockstep; catalog-uat and npm run check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-01 absent-target producer flips (reinstall/update not-installed → error) + PU-5 standalone uninstall → error row + catalog states + wire-coverage fixtures (atomic)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, docs/output-catalog.md, tests/architecture/catalog-uat.test.ts, tests/architecture/notify-producer-wire-coverage.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts — the `case "skipped"` arm (reinstall.ts:854-866) that currently stamps `severity: skipSeverity(reasons)`.
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts — the `case "unchanged"` info hard-stamp idiom (update.ts:1537-1548) to mirror, and the `case "skipped"` arm (update.ts:1549-1567, `severity: skipSeverity(reasons)`) to flip.
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts — `emitCascadeFailure` template (uninstall.ts:200-237, the `PluginFailedMessage` + one-marketplace `notifyWithContext` shape to copy) and the `alreadyGone` arm (uninstall.ts:542, currently `return undefined` standalone / `{status: "converged"}` orchestrated).
    - extensions/pi-claude-marketplace/shared/notify-reasons.ts — `skipSeverity` (notify-reasons.ts:51, info-vs-warning only — do NOT route `not installed` through it; do NOT add `not installed` to any error set) + `IDEMPOTENT_REASONS` (:30). `not installed`/`not found` already exist as command-private reasons (set stays CLOSED, OUT-08).
    - extensions/pi-claude-marketplace/shared/notify.ts — `redactAbsolutePaths` (notify.ts:223) — the path-redaction seam any new cause-bearing error row MUST route through.
    - 03-PATTERNS.md §"reinstall.ts", §"update.ts", §"uninstall.ts" (the exact arms + the error-stamp shape `severity: "error", needsReload: false`), §"notify-producer-wire-coverage.test.ts" (the FAILURE WireFixture analog at test:202-216), §"catalog-uat.test.ts" (the `enable-not-installed` fixture analog at :2569-2592), §"docs/output-catalog.md" (the `enable-not-installed` block analog at catalog L1602-1609).
    - 03-RESEARCH.md §"Absent-target ... THE REAL CODE DELTAS" (option A: severity-only flip, keep `skipped` token), §"The PU-5 silent-converge conversion", §"The Wire-Coverage Test", §"Security Domain" (preserve redactAbsolutePaths basename-only).
    - 03-CONTEXT.md D-01 (absent-target → error; PU-5 standalone → error, orchestrated stays silent per the standalone reading), D-06.
    - docs/output-catalog.md — the uninstall section (L435), reinstall section (L493), update section (L632) for the absent-target / PU-5 catalog states.
  </read_first>
  <behavior>
    - reinstall of a not-installed plugin → row `(skipped) {not installed}` stamped `severity: "error"`, leading sentence `A plugin operation has failed.`
    - update of a not-installed plugin → row `(skipped) {not installed}` stamped `severity: "error"`.
    - STANDALONE uninstall of an already-gone plugin → emits an `error` row (was silent), reasons `["not installed"]`, `needsReload: false`, leading sentence present.
    - ORCHESTRATED uninstall already-gone (`{status: "converged"}`, reconcile apply) → stays silent (no row).
    - wire-coverage: each of the 3 absent-target rows asserts `args[1] === "error"` and no `/reload` trailer.
  </behavior>
  <action>
Flip the absent-target arms to a producer-stamped `severity: "error"` (option A —
severity-only, preserve the `status: "skipped"` token so the `(skipped) {not installed}`
per-row grammar is unchanged, D-06). In reinstall.ts:854-866 and update.ts:1549-1567,
replace `severity: skipSeverity(reasons)` with `severity: "error"` for the
not-installed/not-found reason arm; keep `needsReload: false`. Do NOT route `not
installed` through `skipSeverity` (it only knows info-vs-warning) and do NOT add it to
any error-reason set — keep severity a producer-stamped fact (SEV-04/05). Use the
comment-anchor convention with decision IDs (`D-01`), never `Phase N`
(.claude/rules/typescript-comments.md). For PU-5 (uninstall.ts:542), replace the
STANDALONE `return undefined` with an `emitCascadeFailure`-shaped block (copy the
uninstall.ts:220-237 template): build a `skipped` row stamped `severity: "error"`,
`reasons: ["not installed"]`, `needsReload: false`, wrapped in a one-marketplace
`notifyWithContext(ctx, pi, UNINSTALL_CONTEXT, [{ name: marketplace, scope, plugins:
[row] }])`. KEEP the ORCHESTRATED `{status: "converged"}` arm silent (do NOT touch
apply.ts) — the orchestrated converge exists for reconcile race-safety (WR-06/NFR-2);
D-01's "error across the board" applies to the standalone user command only (CONTEXT
names "uninstall of a not-installed plugin"). If the new row carries a `cause`, route
its diagnostic through `redactAbsolutePaths` (basename-only) — do NOT introduce a raw
absolute path (V7 info-disclosure). IN THE SAME COMMIT: rewrite the
reinstall/update not-installed catalog states' `expectedSeverity` to `error` (their
summary line already uses the D-02 grammar from Task 1, so no double-touch), ADD a NEW
catalog state for the uninstall PU-5 already-gone error row (new
`<!-- catalog-state: ... -->` annotation + its `text` fence under the uninstall section
key) with its `catalog-uat.test.ts` fixture (`expectedSeverity: "error"`, mirroring the
`enable-not-installed` fixture shape), and ADD 3 sibling `WireFixture` entries to
notify-producer-wire-coverage.test.ts (`reinstall not-installed`/REINSTALL_CONTEXT,
`update not-installed`/UPDATE_CONTEXT, `uninstall PU-5`/UNINSTALL_CONTEXT — each
`expectedSeverity: "error"`, `expectTrailer: false`). The two-direction catalog gate
requires the new state's fixture in the SAME commit. Update the wire-coverage
top-of-file producer line-number doc-comment if the flipped arms shift lines.
  </action>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts" "tests/architecture/notify-producer-wire-coverage.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/architecture/catalog-uat.test.ts" "tests/architecture/notify-producer-wire-coverage.test.ts"` exits 0.
    - `grep -n 'severity: "error"' extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` shows the flipped absent-target arms.
    - The uninstall standalone alreadyGone arm no longer returns `undefined` silently: `grep -n 'notifyWithContext\|severity: "error"' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` shows the new error-row emission in the standalone branch.
    - The orchestrated `{status: "converged"}` arm is unchanged (apply.ts untouched): `git diff --name-only` does NOT list any reconcile apply file.
    - notify-producer-wire-coverage.test.ts contains 3 new absent-target WireFixtures with `expectedSeverity: "error"` and `expectTrailer: false`.
    - `not installed` is NOT added to skipSeverity or any error-reason set: `grep -n 'not installed' extensions/pi-claude-marketplace/shared/notify-reasons.ts` is unchanged from baseline.
    - `npm run check` green.
  </acceptance_criteria>
  <done>reinstall/update not-installed stamp error; PU-5 standalone emits an error row while orchestrated stays silent; new PU-5 catalog state + fixture + 3 wire fixtures land in the same commit; catalog-uat, wire-coverage, and npm run check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator error row → user-visible notification | A new cause-bearing error row (PU-5, absent-target) may carry a diagnostic string that could embed an absolute filesystem path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | new PU-5 / absent-target error rows carrying a `cause` | mitigate | Route any cause diagnostic through the existing `redactAbsolutePaths` basename-only seam (notify.ts:223). The absent-target reinstall/update rows carry no `cause` (reason-only). Verify the PU-5 row, if it carries a cause, is basename-redacted — do NOT introduce a raw absolute path (ASVS V7). |
| T-03-02 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (in-repo TypeScript/markdown only — RESEARCH §Standard Stack). No legitimacy gate required. |
</threat_model>

<verification>
- `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 after each task (GATE-02 atomic supersession — green at every commit boundary).
- `node --test "tests/architecture/notify-producer-wire-coverage.test.ts"` exits 0 (producer→wire severity gate covers the new absent-target rows).
- `npm run check` green at the plan/wave boundary (GATE-03).
- D-05 always-header audit confirms no plugin-row-without-header path.
</verification>

<success_criteria>
- OUT-02/D-02 leading sentence + OUT-06/D-03 mixed-subject grammar live and byte-fixed in the catalog.
- OUT-01 single emitWithSummary seam preserved (one ctx.ui.notify).
- D-01 absent-target (reinstall/update not-installed → error) + PU-5 standalone → error row landed; orchestrated converge silent.
- OUT-08/GATE-02 catalog + fixtures rewritten in lockstep; catalog-uat green at every commit.
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-01-SUMMARY.md` when done.
</output>
