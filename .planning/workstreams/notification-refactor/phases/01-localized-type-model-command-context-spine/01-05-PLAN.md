---
phase: 01-localized-type-model-command-context-spine
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - tests/architecture/notify-types.test.ts
autonomous: true
requirements: [MOD-02, MOD-03, OUT-08]
must_haves:
  truths:
    - "tests/architecture/notify-types.test.ts is deleted (D-03) — the central tuples its bidirectional set-equality proofs guarded no longer drive any command's rendering"
    - "The legacy notify(ctx, pi, message) entry point and its adapter are removed once all 18 commands + reconcile/import route through notifyWithContext (D-02)"
    - "The now-orphaned central renderPluginRow/renderMpHeader switches and central status tuples that only existed to serve the removed legacy path are removed or reduced to the shared vocabulary that stays central (D-11)"
    - "No test imports the deleted file's _V*/_Assert_* aliases (verified before deletion)"
    - "Output stays byte-identical: catalog-uat + notify-v2 + notify-grammar-invariant green; npm run check green"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "Slimmed monolith: shared vocabulary + envelope + notifyWithContext dispatcher; legacy notify() and dead central switches removed"
      contains: "notifyWithContext"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "all 18 command messaging modules"
      via: "notifyWithContext dispatches to the threaded per-command render maps (no central switch fallback remains)"
      pattern: "notifyWithContext"
---

<objective>
Final cleanup of the cutover: delete the bidirectional `notify-types.test.ts` proofs
(D-03), remove the legacy `notify(ctx, pi, message)` entry point + the adapter now
that all 18 commands + reconcile/import route through `notifyWithContext`, and remove
the central `renderPluginRow`/`renderMpHeader` switches and central status tuples that
only existed to serve the removed legacy path. Confirm `npm run check` green and all
byte gates byte-identical.

Purpose: Land the D-03 deletion and complete the D-02 reshape (no central registry /
no central render switch fallback), giving Phase 4's ≤3-central-files / 0-notify-edits
open-closed proof a clean baseline. The shared presentation vocabulary
(`ICON_*`/composers, D-11) and the `notify()` envelope/dispatcher STAY central.

Output: `notify-types.test.ts` removed; `notify.ts` slimmed to the shared vocabulary +
envelope + `notifyWithContext` dispatcher. Implements the MOD-02 proof deletion, the
MOD-03 central-switch removal, and preserves OUT-08 (closed reasons set, byte-identical
output). Output-neutral.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-PATTERNS.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-01-SUMMARY.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-02-SUMMARY.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-03-SUMMARY.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-04-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete notify-types.test.ts (D-03), relocating any field-presence guards first</name>
  <files>tests/architecture/notify-types.test.ts</files>
  <read_first>
    - tests/architecture/notify-types.test.ts lines 1–48 (header), 127–189 (bidirectional set-equality proofs, tuple length-locks, exact-membership, per-variant field-presence @ts-expect-error blocks)
    - .planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-RESEARCH.md "notify-types.test.ts deletion — what coverage is removed (D-03)"
    - .planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-PATTERNS.md "DELETE tests/architecture/notify-types.test.ts"
    - the four 01-0{2,3,4}-SUMMARY.md files (to confirm whether any command kept relocated field-presence @ts-expect-error guards)
  </read_first>
  <action>
    Confirm nothing imports the file's `_V*`/`_Assert_*` aliases (it is self-contained
    and runtime-inert — verify with a grep across the repo before deleting). Items 1–3
    of the proofs (PluginStatus⇄message set-equality, tuple length-locks for
    PLUGIN_STATUSES/MARKETPLACE_STATUSES/STATUS_TOKENS/DEPENDENCIES/REASONS, exact
    literal-set membership) guard the CENTRAL tuples that no longer drive any command's
    rendering after Wave 2 — they are safe to delete (D-03). Item 4 (per-variant
    field-presence `@ts-expect-error` blocks, e.g. `failed` has `cause?`, `installed`
    does not) travels with the message interface to the owning command's `.messaging.ts`
    module — if any of these guards were NOT already relocated during Wave 2, relocate
    them as co-located `@ts-expect-error` blocks in the owning command module BEFORE
    deleting (so the field-presence coverage is not lost). Then delete
    `tests/architecture/notify-types.test.ts`.

    Use only D-NN comment anchors in any relocated guard.
  </action>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && test ! -f tests/architecture/notify-types.test.ts && npm run typecheck && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/architecture/notify-types.test.ts` does not exist (`test ! -f` succeeds)
    - `grep -rn "_Assert_PluginStatus\|notify-types" tests/ extensions/` returns no live imports of the deleted aliases
    - `npm run typecheck` exits 0
    - `npm test` green (no test references the deleted file)
    - Any relocated field-presence guards live in the owning command's `.messaging.ts` module
  </acceptance_criteria>
  <done>notify-types.test.ts is deleted (D-03); any still-needed field-presence guards relocated to the owning command modules; typecheck + test green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy notify() + adapter + dead central switches/tuples; slim notify.ts</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts lines 2987–3048 (legacy notify() body), 1798–1977 (renderPluginRow switch), 1462–1566 (renderMpHeader switch), 450–489 (PLUGIN_STATUSES/MARKETPLACE_STATUSES tuples), 844–860/975–985 (the central message unions)
    - extensions/pi-claude-marketplace/shared/notify-context.ts (notifyWithContext — the surviving dispatcher)
    - extensions/pi-claude-marketplace/shared/notify.ts the STAYS-central vocabulary list (RESEARCH "What stays central"): ICON_*, joinTokens, renderScopeBracket, renderVersion, composeVersionArrow, composeReasons, pluginRow, redactAbsolutePaths, renderIndentedCauseChain, composeRollbackPartialLines, collectManualRecoveryLeaks, isInfoKind/dispatchInfoMessage/emitWithSummary, the standalone info shapes
    - the four Wave-2 SUMMARY files (to confirm which standalone/envelope routing the commands left central — those paths and their helpers STAY)
    - .claude/rules/typescript-comments.md
  </read_first>
  <action>
    Now that every command + reconcile/import threads `notifyWithContext`, remove the
    legacy `notify(ctx, pi, message)` entry point and any adapter shim from 01-01 that
    only existed to bridge un-migrated callers (D-02 — no central registry / no legacy
    fallback). Remove the central `renderPluginRow` and `renderMpHeader` switches and
    the central status tuples (`PLUGIN_STATUSES`, `MARKETPLACE_STATUSES`) and the
    central message-union declarations that ONLY existed to serve the removed legacy
    render path — but ONLY if nothing surviving still references them. KEEP everything
    on the STAYS-central list (D-11): the shared presentation vocabulary
    (`ICON_*`/`joinTokens`/`renderScopeBracket`/`renderVersion`/`composeVersionArrow`/
    `composeReasons`/`pluginRow`), the path-redaction seam (`redactAbsolutePaths`,
    `renderIndentedCauseChain`, NFR-9), the rollback/manual-recovery composers, the
    `notify()` envelope/dispatcher pieces the standalone info-kind routing still uses
    (`isInfoKind`/`dispatchInfoMessage`/`emitWithSummary`), and the closed `REASONS`
    set (OUT-08). If a standalone info-kind path the Wave-2 commands left central still
    needs the legacy envelope, KEEP exactly that envelope surface and remove only the
    cascade-render switch that is now dead. Be conservative: remove a symbol only when
    the typecheck/lint confirms it is unreferenced (eslint no-unused / tsc).

    Use only D-NN/MOD-NN/NFR-N/OUT-NN comment anchors. Do NOT edit docs/output-catalog.md.
  </action>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && npm run check && git diff --quiet docs/output-catalog.md && echo CATALOG_UNCHANGED</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` green (typecheck + lint + format:check + test + test:integration)
    - `node --test tests/architecture/catalog-uat.test.ts tests/shared/notify-v2.test.ts tests/architecture/notify-grammar-invariant.test.ts` green and byte-identical
    - `git diff docs/output-catalog.md` is empty (CATALOG_UNCHANGED printed)
    - The legacy `export function notify(ctx, pi, message)` is removed (`grep -nE "export function notify\(" extensions/pi-claude-marketplace/shared/notify.ts` returns no match for the 3-arg message form; notifyWithContext remains the entry point)
    - The dead central renderPluginRow/renderMpHeader cascade switches are removed (the per-command render maps own that rendering); the STAYS-central vocabulary + redaction seam + closed REASONS set remain
    - No eslint no-unused or tsc dead-code error
  </acceptance_criteria>
  <done>Legacy notify() + adapter + dead central cascade switches/tuples removed; shared vocabulary + envelope + redaction seam + closed REASONS preserved; notifyWithContext is the sole state-change dispatcher; npm run check green; output-catalog.md untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal cleanup — deletion of a runtime-inert type-proof test + removal of now-dead central code. No new input, I/O, network, or dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | path-redaction seam during dead-code removal (NFR-9) | mitigate | redactAbsolutePaths / renderIndentedCauseChain are on the STAYS-central list and MUST NOT be removed; no-credential-leak.test.ts + npm run check gate this. |
| T-01-10 | Tampering | Accidental byte drift while removing the legacy render switch | mitigate | Symbols removed only when typecheck/lint prove them unreferenced; catalog-uat (114 fixtures) is the final byte-equality proof; output-catalog.md must remain unchanged. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. |
</threat_model>

<verification>
Phase gate: `npm run check` green; `node --test tests/architecture/catalog-uat.test.ts
tests/shared/notify-v2.test.ts tests/architecture/notify-grammar-invariant.test.ts`
byte-identical/green; `git diff docs/output-catalog.md` empty; notify-types.test.ts
deleted; notifyWithContext is the sole state-change dispatcher; shared vocabulary +
closed REASONS set preserved. All 18 commands own their notification vocabulary
locally (clean baseline for Phase 4).
</verification>

<success_criteria>
- tests/architecture/notify-types.test.ts deleted (D-03); field-presence guards relocated if still needed (MOD-02)
- Legacy notify() + adapter removed; notifyWithContext is the sole dispatcher (D-02)
- Dead central cascade switches/tuples removed; STAYS-central vocabulary + redaction seam + closed REASONS preserved (D-11/NFR-9/OUT-08)
- `npm run check` green; byte gates byte-identical; output-catalog.md untouched
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-05-SUMMARY.md` when done. Record: which central symbols were removed vs kept (and why), confirmation the redaction seam + closed REASONS set survived, and the final notify.ts line-count delta (informational baseline for Phase 4's ≤3-files proof).
</output>
