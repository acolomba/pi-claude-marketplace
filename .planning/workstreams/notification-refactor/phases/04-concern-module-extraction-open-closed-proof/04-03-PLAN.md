---
phase: 04-concern-module-extraction-open-closed-proof
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - docs/open-closed-proof.md
autonomous: true
requirements: [MOD-05, MOD-06, GATE-03]

must_haves:
  truths:
    - "A durable, discoverable doc enumerates that adding a command touches <=3 central files (edge/router.ts registration, edge/register.ts wiring line, one docs/output-catalog.md section) and 0 notify.ts edits"
    - "The doc measures that end state against the MESSAGING-COUPLING.md baseline (5 no-grammar / 9-11 new-grammar; 6 of those previously inside notify.ts)"
    - "The doc documents the MOD-06 catalog floor: hand-authored, one central section per new rendered state, no generation/aggregation seam (deferred)"
    - "The notify.ts before(3431)/after line count is recorded as evidence of the 'slims to' claim"
    - "npm run check is green at milestone close (typecheck + ESLint + Prettier + tests) and catalog-uat byte-equality holds"
  artifacts:
    - path: "docs/open-closed-proof.md"
      provides: "D-02 open-closed measurement + D-03 catalog floor (the durable proof artifact)"
      contains: "open-closed"
      min_lines: 40
  key_links:
    - from: "docs/open-closed-proof.md"
      to: ".planning/workstreams/notification-refactor/research/MESSAGING-COUPLING.md"
      via: "cites the 5 / 9-11 baseline it measures against"
      pattern: "MESSAGING-COUPLING"
---

<objective>
Author the durable open-closed proof doc `docs/open-closed-proof.md` (MOD-05 + MOD-06,
per D-02 documented-measurement and D-03 catalog-floor) and run the GATE-03
milestone-close gate (`npm run check` green + `catalog-uat` byte-equality). Per D-02 this
is a WRITTEN measurement, NOT an architecture test — NO `notify.ts`-purity test is added
(explicitly forbidden by D-02).

The doc enumerates the 3 central files a new command touches AFTER the Phase-4 extraction
(`edge/router.ts` registration, `edge/register.ts` one wiring line, one
`docs/output-catalog.md` section) and 0 `notify.ts` edits, measured against the
`research/MESSAGING-COUPLING.md` baseline (5 no-grammar / 9-11 new-grammar; 6 of those
previously inside `notify.ts`). It honestly notes the `completions/provider.ts` /
catalog-uat `FIXTURES` caveat (partially irreducible per the audit's Part D) rather than
claiming absolute zero-touch. It documents the MOD-06 catalog floor as the deliberate
milestone boundary (hand-authored, one section per rendered state, no generation seam —
deferred), captured in the SAME doc as the explicit accepted 3rd central file.

Depends on 04-01 and 04-02 because the doc must measure the TRUE end state — both concerns
must already be extracted before the proof is written and before the notify.ts after-line
-count is recorded.

Purpose: Closes MOD-05 (the open-closed proof), MOD-06 (the documented catalog floor), and
GATE-03 (the milestone-close green gate) — completing the milestone.
Output: `docs/open-closed-proof.md`; a green `npm run check`; recorded notify.ts line-count
delta.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/STATE.md
@.planning/workstreams/notification-refactor/ROADMAP.md
@.planning/workstreams/notification-refactor/REQUIREMENTS.md
@.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-PATTERNS.md
@.planning/workstreams/notification-refactor/research/MESSAGING-COUPLING.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author docs/open-closed-proof.md (D-02 measurement + D-03 floor)</name>
  <files>docs/open-closed-proof.md</files>
  <read_first>
    - .planning/.../04-RESEARCH.md §5 (D-02 PROOF BASELINE: the 5 / 9-11 figures, the 3-central-files touch matrix with concrete current lines, the post-Phase-1 correction, the honest provider/FIXTURES caveat A4) + §6 (neutrality recipe)
    - .planning/.../04-PATTERNS.md "docs/open-closed-proof.md" section (structure pattern from messaging-style-guide.md; content the doc must carry; the hard constraint NOT to append to output-catalog.md or be a code comment)
    - .planning/workstreams/notification-refactor/research/MESSAGING-COUPLING.md §Part A.3 (the 5 / 9-11 baseline prose — cite prose, NOT its stale 3119-line numbers) + §Part D (the irreducible provider/catalog caveats)
    - docs/messaging-style-guide.md (lines 1-40: the normative-doc tone/structure analog — bolded Status/Audience front-matter, ## Overview, decision-ID anchoring, "reference don't duplicate")
    - extensions/pi-claude-marketplace/edge/router.ts (the registration touch-points: interface :26-49, tuples :55-85, switch :143-213, usage :87-110 — cite current lines)
    - extensions/pi-claude-marketplace/edge/register.ts (lines 78-97: the one wiring line per command)
    - .claude/rules/typescript-comments.md (D-NN/MOD-NN anchors allowed in prose; NO GSD phase/plan references)
  </read_first>
  <action>
    Create `docs/open-closed-proof.md` (per D-02 Claude's-discretion home; RESEARCH §5
    recommends this path — it sits beside `output-catalog.md`/`messaging-style-guide.md`
    so reviewers find it, it is not a code comment, and it is NOT the byte-frozen
    `output-catalog.md`). Follow the `messaging-style-guide.md` structure: a bolded
    `**Status:** Normative ... **Audience:** ...` front-matter line, a `## Overview`
    paragraph, decision-ID anchoring in prose (`per D-02`, `per D-03`, `MOD-05`,
    `MOD-06`), and a reference-don't-duplicate stance (point at `edge/router.ts`,
    `edge/register.ts`, `docs/output-catalog.md`, and `research/MESSAGING-COUPLING.md`
    rather than re-pasting spans). NO GSD phase/plan references.

    The doc MUST carry (RESEARCH §5, verbatim-citable):
    (1) The 3-central-files post-extraction touch matrix for adding a new command:
        `edge/router.ts` (interface field + tuple + switch + usage, :26-49 / :55-85 /
        :143-213 / :87-110), `edge/register.ts` (one wiring line, :78-97), one
        `docs/output-catalog.md` H2 section per new rendered state = 3 central files,
        0 `notify.ts` edits.
    (2) The baseline it measures against — cite MESSAGING-COUPLING.md §A.3 PROSE (not its
        stale line numbers): 5 central files (no new grammar) / 9-11 (new status+reason),
        6 of those previously inside `notify.ts`. Note the post-Phase-1 correction
        (RESEARCH §5): Phases 1-2 already collapsed the 6 central notify.ts edit sites
        (command-local status sets + render maps, caller-stamped severity/reload, deleted
        notify-types.test length-locks); Phase 4 closes the last two cross-cutting concerns
        (hooks + soft-dep), after which adding a command needs 0 notify.ts edits EVEN for
        new grammar.
    (3) The HONEST caveat (RESEARCH A4 / audit Part D): `completions/provider.ts` may still
        need a declarative descriptor for novel positional/flag shapes, and the catalog-uat
        `FIXTURES` map gains one entry per state — state this as "partially irreducible"
        rather than claiming absolute zero-touch. The locked D-02 target is the 3
        grammar/registration files, which the doc proves.
    (4) The MOD-06 / D-03 catalog floor: the catalog stays hand-authored, one central
        section per new rendered state, no generation/aggregation seam (deferred to a
        future milestone) — this IS the explicit accepted 3rd central file.
    (5) Evidence for the "notify.ts slims to envelope + reducer + shared vocabulary" claim:
        record the BEFORE line count (3431, pre-Phase-4) and the measured AFTER line count
        (run `wc -l extensions/pi-claude-marketplace/shared/notify.ts` against the
        post-extraction file) so the slimming is evidenced, not asserted. Name what STAYS
        central (envelope, reducer spine, ICON_*/scope/version/reason composers,
        composeReasons, pluginRow, joinTokens, isInfoKind, RELOAD_HINT_TRAILER,
        redactAbsolutePaths) and what LEFT (soft-dep + hooks concerns under shared/concerns/).

    Do NOT modify any code, the catalog, or any test in this task — the doc is the only
    artifact written here. Do NOT append the proof to `output-catalog.md` (byte-frozen).
    Run `prettier --write docs/open-closed-proof.md` (markdown formatting is in
    `npm run format:check`) before committing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/open-closed-proof.md && grep -q "MESSAGING-COUPLING" docs/open-closed-proof.md && grep -q "output-catalog.md" docs/open-closed-proof.md && git diff --exit-code docs/output-catalog.md && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `docs/open-closed-proof.md` exists and is >= 40 lines
    - `grep -c "MESSAGING-COUPLING" docs/open-closed-proof.md` >= 1 (cites the baseline it measures against)
    - The doc names all 3 central touch-points (router.ts, register.ts, output-catalog.md) and states "0 notify.ts edits": `grep -c "router.ts\|register.ts\|output-catalog.md" docs/open-closed-proof.md` >= 3
    - The doc records the notify.ts before(3431)/after line count: `grep -c "3431" docs/open-closed-proof.md` >= 1
    - The doc documents the MOD-06 catalog floor (hand-authored, no generation seam, deferred): `grep -ci "floor\|hand-authored\|generation" docs/open-closed-proof.md` >= 1
    - The doc notes the honest provider.ts / FIXTURES caveat: `grep -ci "provider\|fixtures\|irreducible" docs/open-closed-proof.md` >= 1
    - `git diff --exit-code docs/output-catalog.md` produces no output (the proof did NOT touch the byte-frozen catalog)
    - `npm run format:check` exits 0 (the new markdown is prettier-clean)
  </acceptance_criteria>
  <done>
    `docs/open-closed-proof.md` is a durable, discoverable artifact that enumerates the
    3-central-files / 0-notify.ts-edits open-closed target, measures it against the
    MESSAGING-COUPLING baseline, records the notify.ts line-count delta as evidence,
    honestly notes the provider/FIXTURES caveat, and documents the MOD-06 catalog floor.
    The byte-frozen catalog is untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GATE-03 milestone-close gate — npm run check green + catalog byte-equality</name>
  <files>(verification-only — runs npm run check + catalog byte-equality over the full post-extraction tree; modifies no files)</files>
  <read_first>
    - .planning/.../04-RESEARCH.md §6 (the exact neutrality verification recipe: `npm run check` = typecheck && lint && format:check && test && test:integration; the catalog-uat byte mechanism)
    - .planning/.../04-VALIDATION.md (the sampling contract: per-task byte-freeze, per-wave/gate `npm run check`)
    - package.json (the `check` script composition)
  </read_first>
  <action>
    Run the GATE-03 milestone-close gate over the full post-extraction tree (after 04-01,
    04-02, and this plan's proof-doc task). Execute `npm run check`
    (= `npm run typecheck && npm run lint && npm run format:check && npm test &&
    npm run test:integration`) and confirm it exits 0. Separately confirm catalog
    byte-equality: `git diff --exit-code docs/output-catalog.md` (empty) and
    `node --test tests/architecture/catalog-uat.test.ts` (exit 0). This is the only
    automated gate at milestone close (per D-02, no architecture test was added). If any
    sub-check fails, this gate task surfaces the failure for fixing in the owning
    extraction/doc task — do NOT relax the gate or edit the catalog to make it pass.
    Record the green result + the measured notify.ts line count in the plan SUMMARY.
    Follow project commit hygiene before any commit (`pre-commit run --files <changed>`;
    conventional-commit title 5-72 chars; never `--no-verify`; never commit to main —
    work is on `features/notification-refactor`; `SKIP=trufflehog` prefix only if
    committing from a worktree).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run check && git diff --exit-code docs/output-catalog.md && node --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck + ESLint + Prettier + tests + integration all green) — GATE-03 satisfied
    - `git diff --exit-code docs/output-catalog.md` produces no output (output-neutral across the whole milestone)
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 (catalog byte-equality holds — GATE-02 still green)
    - NO `notify.ts`-purity architecture test was added (D-02): `grep -rL "notify.*purity" tests/architecture/ >/dev/null 2>&1 || true` — confirm no new purity test file exists
  </acceptance_criteria>
  <done>
    `npm run check` is green, `docs/output-catalog.md` is byte-frozen (diff empty), and
    `catalog-uat` byte-equality holds at the milestone close — GATE-03 satisfied with the
    documented-measurement path (no architecture test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Writes one documentation file and runs the test/lint gate; no new input, auth, crypto, network, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | byte-frozen docs/output-catalog.md | mitigate | The proof lives in a separate file (`open-closed-proof.md`); `git diff --exit-code docs/output-catalog.md` confirms the catalog was not touched. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No packages added/removed/upgraded this phase (RESEARCH Package Legitimacy Audit: N/A). |

No new threats — documentation + gate run. `redactAbsolutePaths` (notify.ts:223) is the
cross-cutting path-redaction security primitive and MUST stay in notify.ts (NFR-9); it is
NOT a concern to extract.
</threat_model>

<verification>
- `docs/open-closed-proof.md` exists, cites the baseline, records the line-count delta, documents the floor + caveat
- `npm run check` green (GATE-03)
- `git diff --exit-code docs/output-catalog.md` empty + `catalog-uat` green (GATE-02 holds)
</verification>

<success_criteria>
- The open-closed proof doc enumerates <=3 central files / 0 notify.ts edits, measured vs the MESSAGING-COUPLING baseline, with the notify.ts line-count evidence and the honest provider/FIXTURES caveat
- The MOD-06 catalog floor is documented in the same doc
- `npm run check` green; catalog byte-equality holds; no architecture test added (D-02)
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-03-SUMMARY.md` when done
</output>
