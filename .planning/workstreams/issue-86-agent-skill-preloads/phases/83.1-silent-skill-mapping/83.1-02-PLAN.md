---
phase: 83.1-silent-skill-mapping
plan: 02
type: execute
wave: 2
depends_on: ["83.1-01"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
  - tests/bridges/agents/frontmatter.test.ts
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGSK-04]

must_haves:
  truths:
    - "D-83.1-03: renderSkillLegend annotates every non-preloaded entry '(available on demand)' unconditionally -- the string 'not available in this session' appears nowhere in extensions/ or tests/ (grep -rn -a sweep returns zero lines); the empirical basis is that extension-contributed skills survive --no-skills in child sessions, so the catalog is present regardless of inheritSkills"
    - "AGSK-04 unified state proven in both directions: the frontmatter.test.ts two-entry legend pin under inheritSkills: false renders '(available on demand)' for its non-preloaded entry, and the pre-existing inheritSkills-true legend pin stays green with zero edits"
    - "D-83.1-04: agents without Skill declarations and without body skill tokens stay byte-identical to Phase 83 output -- all seven convert-byte-identity.test.ts corpus classes green with a zero-line diff to that file, and the NO_LEGEND_EXPECTED pins stay green with zero edits"
    - "Phase gate: npm run check green (NFR-6) and both retired-string sweeps (warning strings from plan 83.1-01, legend string from this plan) return zero over extensions/ and tests/"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      provides: "single-parameter renderSkillLegend with the unconditional two-state annotation"
      contains: "available on demand"
    - path: "tests/bridges/agents/frontmatter.test.ts"
      provides: "two-entry legend pin proving the unified state under inheritSkills: false"
      contains: "(available on demand)"
    - path: "tests/bridges/agents/convert.test.ts"
      provides: "two-member LegendAnnotation union + inverted unified-state legend pins (SC-3)"
      contains: "LegendAnnotation"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (~L318 call site)"
      to: "renderSkillLegend"
      via: "single-argument call after the inheritSkills parameter removal"
      pattern: "renderSkillLegend\\(legend\\)"
    - from: "tests/bridges/agents/convert.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      via: "convertSpecTreeWithBody + legendEntryLine helpers pinning rendered legend bytes"
      pattern: "legendEntryLine"
---

<objective>
Unify the legend's non-preloaded state (amended AGSK-04, D-83.1-03): the annotation is "(available on demand)" for ALL agents regardless of `inheritSkills`, and "(not available in this session)" is removed from the codebase. Empirical basis (phase 83 UAT, 2026-07-19): pi's `--no-skills` keeps extension-contributed skill paths, so a child session lists every marketplace-converted skill either way -- the three-state distinction was false. The change collapses a single ternary in `renderSkillLegend`, removes the function's now-dead `inheritSkills` parameter (private, single call site), and re-pins the affected legend tests. This plan also runs the phase-wide closing sweep (both retired-string greps at zero, byte-identity corpus zero-diff, `npm run check` green).

`inheritSkills` frontmatter EMISSION does not change -- after this plan the legend is simply no longer a consumer of the flag; emission remains its purpose. The provenance behavior was already silenced in plan 83.1-01.

Purpose: closes the amended AGSK-04 and completes the phase's success criteria (SC-3 plus the sweep half of SC-4).
Output: two-state `frontmatter.ts` legend, re-pinned legend tests, phase gate green.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` -- the orchestrator commits. Leave changes staged-ready.
- ASCII-only source literals; never a literal em-dash (U+2014). The legend arrow stays the `→` escape in .ts source (real arrow only in output bytes).
- Comment/test-title anchors: `AGSK-03`, `AGSK-04`, `AGSK-05`, `D-83.1-01`..`D-83.1-04`, still-valid `D-82-01`..`D-82-07`, `D-83-01`, `D-83-07`, `#86` only -- never `Phase 83.1`/plan/wave references. RETIRE `D-83-05` (three-state legend, superseded) from every edited region.
- New/rewritten comments must NOT quote the retired legend string verbatim -- the sweep grep must reach zero.
- All verification greps use `grep -a` (macOS BSD grep silently skips glyph-bearing files as binary; frontmatter.test.ts and the legend tests carry U+2192 output bytes).
- convert.test.ts line numbers have SHIFTED since plan 83.1-01's edits -- locate every region by the quoted strings/test titles, never by raw line number. frontmatter.ts / frontmatter.test.ts numbers are still HEAD-accurate (untouched by plan 01).
- Do NOT edit any file under `.planning/` -- historical occurrences of the retired string there are records, and `.planning/PROJECT.md:22` is updated by the milestone-completion flow, not this phase.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-CONTEXT.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@tests/bridges/agents/frontmatter.test.ts
@tests/bridges/agents/convert.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the legend to two states and re-pin the legend tests</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts, tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (doc comment ~L323-332; renderSkillLegend signature + ternary + entry template ~L334-348; the single call site ~L318; the FENCED inheritSkills emission ~L274-278 and formatOptionalProvenanceList ~L358-360)
    - tests/bridges/agents/frontmatter.test.ts (two-entry legend pin ~L389-435 -- its expected constant carries `inheritSkills: false` at ~L412 and the retired annotation at ~L430; the byte-identical target string is in the already-green inheritSkills-true pin at ~L532; FENCED regions: NO_LEGEND_EXPECTED ~L379-387, emission pins ~L446-487, true-legend pin ~L489-537)
    - tests/bridges/agents/convert.test.ts (locate by string, lines have shifted: the `type LegendAnnotation` union; the annotation tests titled around "known-but-not-emitted skill token", "on-demand annotation", the dedupe/order legend test; the `legendEntryLine` helper)
    - .planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-PATTERNS.md (frontmatter.ts section for the exact target shape; "Shared Patterns" for anchors and string hygiene)
  </read_first>
  <action>
    frontmatter.ts -- two edits, nothing else changes:

    (1) In `renderSkillLegend` (~L334-348): delete the `notPreloadedAnnotation` ternary (`inheritSkills ? "available on demand" : "not available in this session"`, ~L342-344) and make the per-entry ternary read `entry.preloaded ? "preloaded in your context" : "available on demand"`. Remove the `inheritSkills: boolean` parameter from the signature (private function, single call site -- leaving it unused trips typescript-eslint no-unused-vars and turns `npm run check` red) AND change the call site (~L318) from `renderSkillLegend(legend, frontmatter.inheritSkills ?? false)` to `renderSkillLegend(legend)` in the same edit. The entry-line template (backtick tokens, `→` arrow escape) is byte-unchanged.

    (2) Doc comment (~L323-332): keep the first paragraph (the D-82-05 heading/spacing contract) untouched; replace the second paragraph (the "AGSK-05 / D-83-05 ... third state gated on skill inheritance" text) with the unified two-state contract anchored `AGSK-04 / D-83.1-03`: the non-preloaded annotation is unconditionally "available on demand" because extension-contributed skills survive `--no-skills` in child sessions, so the catalog is present regardless of inheritSkills.

    FENCED in frontmatter.ts -- must not change: the `inheritSkills` frontmatter emission (~L274-278) and `formatOptionalProvenanceList` (~L358-360).

    frontmatter.test.ts -- one line changes: in the two-entry legend pin's expected constant (~L430), the entry `- \`spec-tree:other-skill\` → skill \`spec-tree-other-skill\` (not available in this session)` becomes `(available on demand)` -- copy the annotation bytes exactly from the already-true pin at ~L532. The constant's `inheritSkills: false` line (~L412) stays: that is what makes this pin the proof the unified state holds with inheritance OFF. If the surrounding doc comment or title describes the retired state, update the wording (anchors `AGSK-04 / D-83.1-03`; the existing D-82-04 / D-82-05 anchors are still valid -- keep them). FENCED -- zero edits: NO_LEGEND_EXPECTED pins (~L379-387), INHERIT_TRUE_EXPECTED + emission tests (~L446-487), the inheritSkills-true legend pin (~L489-537).

    convert.test.ts -- locate by string (lines shifted after plan 83.1-01):

    (1) `type LegendAnnotation` union: DELETE the `"not available in this session"` member entirely (do not string-replace it -- that would duplicate the `"available on demand"` member); the two-member union remains and the compiler then flags any missed annotation call site. If the comment above the union cites `D-83-05`, replace the anchor with `AGSK-04 / D-83.1-03`. The `legendEntryLine` helper is unchanged.

    (2) Test "known-but-not-emitted skill token annotates not available" (no Skill declared, previously asserting the retired string): annotation argument becomes `"available on demand"`; retitle to the on-demand claim (e.g. "AGSK-04 / D-83.1-03 known-but-not-emitted skill token annotates available on demand").

    (3) Test asserting the Skill-declaring on-demand annotation (already `"available on demand"`): assertions unchanged; if its title cites `D-83-05`, swap that anchor for `D-83.1-03`.

    (4) Test "the on-demand annotation requires Skill declared AND allowed" -- this test INVERTS and becomes the SC-3 pin: both branches (no-Skill input AND Skill-disallowed input) now expect `"available on demand"`; replace both retired-string annotation arguments and retitle to the unified-state claim (e.g. "AGSK-04 / D-83.1-03 the on-demand annotation applies regardless of inheritSkills").

    (5) Dedupe/order legend test: its two retired-string annotation arguments become `"available on demand"`; the ordering/count assertions are unchanged.

    Sanity check before finishing: at task start `grep -c -a "not available in this session" tests/bridges/agents/convert.test.ts` reports 6 occurrences (1 union member + 5 annotation arguments) and frontmatter.test.ts reports 1 -- after this task all 7 are gone (the union member deleted, the other 6 replaced). Do NOT touch convert-byte-identity.test.ts or any provenance-side region re-pinned by plan 83.1-01.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts` all green
    - `grep -rn -a "not available in this session" extensions/ tests/` outputs zero lines
    - `grep -n -a "renderSkillLegend(legend)" extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` matches the single-argument call site, and `grep -n -a "inheritSkills" extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` no longer matches inside renderSkillLegend (emission at ~L274-278 still matches -- fenced)
    - `grep -c -a '"available on demand"' tests/bridges/agents/convert.test.ts` counts at least 5 (the replaced annotation arguments plus the surviving union member) and the LegendAnnotation union has exactly two members
    - `grep -n -a "D-83-05" extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts tests/bridges/agents/convert.test.ts` outputs zero lines (superseded anchor retired from edited files)
    - frontmatter.test.ts: the two-entry pin's expected constant still contains `inheritSkills: false` and now contains `(available on demand)` for the non-preloaded entry; NO_LEGEND_EXPECTED and the ~L446-537 regions have zero edits
    - `git status --porcelain` for this plan's changes lists exactly: `extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts`, `tests/bridges/agents/frontmatter.test.ts`, `tests/bridges/agents/convert.test.ts` (plus plan-01 files only if the orchestrator has not yet committed them)
  </acceptance_criteria>
  <done>Legend renders exactly two states -- "(preloaded in your context)" / "(available on demand)" -- for all agents; the retired string is gone from extensions/ and tests/; fenced emission and no-legend pins green with zero edits.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-gate sweep (retired strings zero, corpus zero-diff, full check)</name>
  <files>(none -- verification only, no repo files modified)</files>
  <read_first>
    - .planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-RESEARCH.md (item 7 "Verification commands" and the Validation Architecture SC map -- the closing gate this task executes)
  </read_first>
  <action>
    Run the phase-closing verification suite from the worktree root and record each result in the SUMMARY. No file edits are permitted in this task; if any gate fails, fix the failure by amending Task 1's surface (or flag a blocker), never by editing `convert-byte-identity.test.ts` or any `.planning/` file.

    Gates, in order:
    (1) Retired legend string sweep (D-83.1-03): `grep -rn -a "not available in this session" extensions/ tests/` -- must output zero lines.
    (2) Retired warning strings sweep (D-83.1-01, confirms plan 83.1-01's edits survived): `grep -rn -a "skills discovery disabled\|mapped to Pi skill discovery" extensions/ tests/` -- must output zero lines.
    (3) Corpus guard (D-83.1-04): `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts` -- must print nothing (the file has zero working-tree modifications this phase), AND `node --test tests/bridges/agents/convert-byte-identity.test.ts` green.
    (4) Fenced-behavior spot checks green as part of the suite: inheritSkills emission pins (frontmatter.test.ts), inheritSkills computation branches and AG-11 throws (convert.test.ts), install notify formatting (stage.test.ts -- runs inside npm test).
    (5) Phase gate (NFR-6): `npm run check` -- typecheck + ESLint + Prettier + full unit + integration, all green.
  </action>
  <verify>
    <automated>git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts | wc -l | grep -q "^ *0$" && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn -a "not available in this session" extensions/ tests/` outputs zero lines
    - `grep -rn -a "skills discovery disabled\|mapped to Pi skill discovery" extensions/ tests/` outputs zero lines
    - `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts` prints nothing
    - `npm run check` exits 0
    - `git status --porcelain` contains no `.planning/` entries (except plan-01 planning artifacts, e.g. `83.1-01-SUMMARY.md`, if the orchestrator has not yet committed them) and no `tests/bridges/agents/convert-byte-identity.test.ts` entry
  </acceptance_criteria>
  <done>All four phase success criteria verifiable: silent Skill provenance (SC-1/SC-2, pinned in plan 83.1-01), unified legend state with zero retired-string occurrences (SC-3), unmapped-tool provenance and byte-identity untouched (SC-4); `npm run check` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent body/frontmatter -> generated agent bytes | The legend is converter-authored prose derived from already-sanitized skill tokens; this plan changes one annotation string, not the tokenization or sanitization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83.1-03 | Repudiation | legend annotation (retired "not available in this session" state) | accept | Deliberate, UAT-validated reclassification: the removed state asserted an isolation that real child sessions do not have (extension-contributed skills survive --no-skills); the unified "(available on demand)" state is the accurate claim. Worst case a user loses a (false) provenance signal -- the intended outcome |
| T-83.1-04 | Tampering | Byte drift of legend-free agent output during the frontmatter.ts edit | mitigate | NO_LEGEND_EXPECTED pins and the seven-class byte-identity corpus must stay green with zero edits; corpus zero-diff gate in Task 2 |

No packages installed; no network; no user-input parsing changes; no privilege boundaries. No high-severity threats identified.
</threat_model>

<verification>
- `node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts` green
- `grep -rn -a "not available in this session" extensions/ tests/` returns zero lines
- `grep -rn -a "skills discovery disabled\|mapped to Pi skill discovery" extensions/ tests/` returns zero lines
- `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts` empty
- `npm run check` green (NFR-6)
- No `.planning/` file modified
</verification>

<success_criteria>
- SC-3: the legend's non-preloaded annotation reads "(available on demand)" for all agents regardless of inheritSkills; the retired string appears nowhere in generated output, source, or tests
- SC-4 (byte-identity half): Skill-free/token-free agents byte-identical to Phase 83 output -- corpus green with zero edits, NO_LEGEND_EXPECTED pins unchanged-green
- Phase gate: `npm run check` green; both retired-string sweeps at zero
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-02-SUMMARY.md` when done.
</output>
