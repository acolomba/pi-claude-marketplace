---
phase: 83.1-silent-skill-mapping
plan: 01
subsystem: agents-bridge
tags: [agents-bridge, inheritSkills, tool-mapping, provenance, silent-conversion]

# Dependency graph
requires:
  - phase: 83-skill-tool-inherit-mapping
    provides: "inheritSkills computed in mapTools and threaded to the emitter; SKILL_INHERIT_WARNING / SKILL_DROP_WARNING_NO_INHERIT wordings; DISALLOWED_SKILL_EXPECTED and #86 canonical whole-file pins"
  - phase: 82-agent-skill-preload-fidelity
    provides: "convertSpecTree* test drivers, byte-identity corpus, legend renderer"
provides:
  - "mapToolTokens Skill exclusion (Seam A): Skill is neither mapped nor dropped, silencing provenance comment, ConvertedAgent.droppedTools, agents-index.json, and install notify from one classification point"
  - "Warning-free mapTools: both Skill provenance warning strings removed; warnings deepEqual [] in every Skill branch"
  - "Re-pinned silent contract: DISALLOWED_SKILL_EXPECTED and #86 canonical constants carry droppedTools: (none) / warnings: (none)"
affects: [silent-skill-mapping]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Single upstream classification seam: excluding a token inside mapToolTokens silences all four downstream droppedTools consumers with zero further edits"

key-files:
  created: []
  modified:
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts
    - tests/bridges/agents/convert.test.ts

key-decisions:
  - "Seam A adopted for the D-83.1-02 discretion call: exact-match skip inside mapToolTokens (RESEARCH recommendation), keeping the drop-classification decision in the function whose job is classification"
  - "Tasks 1 and 2 landed as one commit: a Task-1-only commit leaves convert.test.ts red and would break the NFR-6 green-at-every-commit bar (same rationale and shape as the 4a62c64c precedent)"
  - "Retired D-82-08 stripped from two otherwise-unchanged test titles: the plan marked those regions zero-edit but its acceptance criteria require file-wide zero retired anchors; title-only edits reconcile both with assertions byte-unchanged"

patterns-established:
  - "Deliberate re-pin mechanics: edit only the contract-changed lines inside a raw whole-file template literal, rewrite the guard doc comment to name the new contract, keep the never-edit-to-pass clause"

requirements-completed: [AGSK-03, AGSK-05]

# Metrics
duration: 9min
completed: 2026-07-19
---

# Phase 83.1 Plan 01: Silent Skill Conversion Summary

**Skill-declaring agents now convert with zero warnings and zero droppedTools
entries in both the allowed and disallowed branches -- a one-line exclusion at
the mapToolTokens classification seam plus deletion of the warning block, with
the whole-file pins deliberately re-pinned to droppedTools: (none) /
warnings: (none) and the byte-identity corpus untouched**

## Performance

- **Duration:** ~9 min
- **Started:** 2026-07-19T19:30:24Z
- **Completed:** 2026-07-19T19:39:30Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `mapToolTokens` skips `Skill` before the TOOL_MAP lookup (exact,
  case-sensitive match, consistent with the existing raw-token check in
  `mapTools`): Skill is neither mapped nor dropped, so the provenance HTML
  comment, `ConvertedAgent.droppedTools`, the persisted `agents-index.json`
  entry, and the install-time notify line all go silent from the single
  upstream seam with zero downstream edits (AGSK-03, D-83.1-02)
- The `mapTools` warning block and both Skill warning strings (the phase-82
  "skills discovery disabled" wording and the phase-83 "mapped to Pi skill
  discovery" wording) are deleted; `skillDeclared` survives as the input to
  the untouched `inheritSkills` computation (D-83.1-01)
- The `mapToolTokens` doc comment now states the silent-translation
  contract; superseded anchors D-82-08 / D-82-09 / D-83-04 retired from
  convert.ts, still-valid D-83-01 kept
- convert.test.ts re-pinned to the silent contract: allowed and disallowed
  Skill branches assert `droppedTools` and `warnings` deepEqual `[]` on the
  RESULT record; the warnings-order test now proves Skill contributes zero
  warnings even alongside a cross-plugin skill warning
- `DISALLOWED_SKILL_EXPECTED` and the `#86 canonical` whole-file constants
  deliberately re-pinned per the D-83.1-04 mechanics: only the
  `droppedTools: (none)` and `warnings: (none)` lines changed (the canonical
  constant also lost its `${SKILL_INHERIT_WARNING}` interpolation with the
  deleted constant); `inheritSkills: false` / `inheritSkills: true` and the
  preloaded legend entry line stayed byte-identical
- Fenced behavior proven unchanged: inheritSkills computation-branch tests,
  frontmatter emission pins, the WebFetch genuinely-unmapped guard, the
  omitted-tools default warning, and all seven byte-identity corpus classes
  green with zero edits

## Task Commits

1. **Tasks 1+2: Seam A exclusion + warning removal + silent re-pin** -
   `0e634e6f` (feat) -- single commit for both tasks (see Deviations)

## Files Created/Modified

- `extensions/pi-claude-marketplace/bridges/agents/convert.ts` -
  mapToolTokens Skill skip + doc-comment rewrite; mapTools warning block
  deleted; inheritSkills computation/threading byte-untouched
- `tests/bridges/agents/convert.test.ts` - both warning constants deleted;
  section header and eight tests rewritten/retitled to the silent contract;
  two whole-file constants re-pinned; retired anchors removed

## Decisions Made

- Seam A (mapToolTokens skip) chosen over filtering at mapTools
  destructuring or provenance assembly -- single classification point covers
  all four droppedTools consumers (CONTEXT discretion grant, RESEARCH
  recommendation)
- Two test titles outside the plan's edit inventory ("non-Skill drops stay
  silent", "omitted tools: keeps only the implicit-default warning") had
  " / D-82-08" stripped from their titles only, assertions untouched: the
  plan listed them UNCHANGED but its acceptance criterion requires zero
  file-wide occurrences of retired anchors; the criterion wins and the
  title-only edit is minimal

## Deviations from Plan

### Process deviations

- The plan's execution note said "Do NOT run git commit -- the orchestrator
  commits"; the orchestrator instead directed per-task commits with the
  mandated SKIP=trufflehog prefix (same deviation as phases 82/83)
- Tasks 1 and 2 landed as ONE commit instead of two: the plan itself
  declares convert.test.ts intentionally red after Task 1, so a Task-1-only
  commit can never satisfy the NFR-6 "npm run check green before each
  source commit" bar. Single source+tests commit matches the 4a62c64c
  precedent for contract-change re-pins

### Auto-fixed Issues

**1. [Rule 3 - Plan inconsistency] Retired D-82-08 in zero-edit regions**
- **Found during:** Task 2
- **Issue:** Plan item (d) marks two tests UNCHANGED, but the acceptance
  criterion requires `grep D-82-08` over the whole file to return zero;
  both titles carried the retired anchor
- **Fix:** Removed " / D-82-08" from the two titles; bodies byte-unchanged
- **Files modified:** tests/bridges/agents/convert.test.ts
- **Commit:** 0e634e6f

## Issues Encountered

- `pre-commit run trufflehog --all-files` cannot scan inside the worktree
  (`.git` is a file pointer, not a directory: "failed to read index file
  ... not a directory"). Known environmental limitation; every other hook
  ran and passed, and the commit used the sanctioned `SKIP=trufflehog`
  prefix only

## User Setup Required

None - no external service configuration required.

## Verification Results

- 98/98 tests green across convert.test.ts, frontmatter.test.ts, and
  convert-byte-identity.test.ts; `npm run check` green end to end
  (typecheck + ESLint + Prettier + 2974 unit + 16 integration, 0 fail)
- `grep -rn -a "skills discovery disabled|mapped to Pi skill discovery"
  extensions/ tests/` returns zero lines (both retired strings gone from
  code AND tests)
- `grep -c "SKILL_INHERIT_WARNING|SKILL_DROP_WARNING_NO_INHERIT"` over
  convert.test.ts returns 0 (constants and all references gone)
- `DISALLOWED_SKILL_EXPECTED` contains `droppedTools: (none)`,
  `warnings: (none)`, still `inheritSkills: false`, zero `${` interpolation;
  the #86 canonical constant contains `droppedTools: (none)`,
  `warnings: (none)`, `inheritSkills: true`
- `grep "D-82-08|D-82-09|D-83-02|D-83-04|D-83-06|pre-AGSK-05"` over
  convert.test.ts returns zero lines; convert.ts likewise clean of
  D-82-08/D-82-09/D-83-04 with D-83-01 kept
- `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts`
  empty across the whole plan (D-83.1-04 corpus untouched)
- Fenced guards green with zero edits: convert.test.ts inheritSkills
  branches and duplication pin, frontmatter.test.ts emission pins

## Known Stubs

None - no placeholder values or unwired data paths introduced.

## Next Phase Readiness

- AGSK-03 and AGSK-05 (amended, silence halves) complete; legend-state
  unification (AGSK-04, D-83.1-03) remains for plan 83.1-02, which touches
  frontmatter.ts and the legend regions of both test files left untouched
  here

## Self-Check: PASSED

- FOUND: extensions/pi-claude-marketplace/bridges/agents/convert.ts
  (`token === "Skill"` skip inside mapToolTokens; no warning strings)
- FOUND: tests/bridges/agents/convert.test.ts (`droppedTools: (none)` in
  both re-pinned constants)
- FOUND: commit 0e634e6f
- FOUND: 83.1-01-SUMMARY.md

---

*Phase: 83.1-silent-skill-mapping*
*Completed: 2026-07-19*
