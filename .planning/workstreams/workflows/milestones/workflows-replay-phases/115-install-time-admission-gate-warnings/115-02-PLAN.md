---
phase: 115-install-time-admission-gate-warnings
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - tests/bridges/workflows/discover.test.ts
  - tests/architecture/workflows-single-parse.test.ts
  - extensions/pi-claude-marketplace/domain/workflow-script.ts
  - extensions/pi-claude-marketplace/bridges/workflows/discover.ts
autonomous: true
requirements: [WGATE-01, WGATE-02, WGATE-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each warned script earns EXACTLY ONE warning line, in both the install and the preview tense, and a sibling script in the same directory that trips no gate earns none (WGATE-01)."
    - "A `stem-fallback` script carrying a gate earns one line, not two: the gate name rides the existing unrunnable caveat rather than adding a second line for the same file."
    - "The gate reader decides only on acorn node `type` strings and the exact identifier name `meta`, compared with `===` and with no case folding or Unicode normalization; no `GATE_REASONS` value interpolates any script-derived text, so the gate line needs no `forMessage` escaping of its own (WGATE-02)."
    - "`extensions/pi-claude-marketplace/domain/workflow-script.ts` contains exactly one `parse(` call site once comments are stripped, and neither it nor the workflows bridge carries an evaluator, function-constructor, `vm` or dynamic-import surface (WGATE-02)."
    - "The throw containment inside `readEngineGate` is pinned PERMANENTLY, not once: a source gate reddens when the `try`/`catch` around the gate walk is removed. The phase's absolute — no gate reading can ever block an install — therefore survives a later editor deleting the construct, rather than resting on a one-time transcript in a SUMMARY (WGATE-03)."
    - "No comment in `domain/workflow-script.ts` or `bridges/workflows/discover.ts` states a claim this phase falsified: that install-time gate warnings are untracked future work, that the determinism blocklist is the only replicated gate, that the host engine is a 0.x package, or that the `named` arm alone earns no warning."
  prohibitions: []
  artifacts:
    - tests/architecture/workflows-single-parse.test.ts
    - tests/bridges/workflows/discover.test.ts
  key_links:
    - "`tests/architecture/source-scan.ts` (`REPO_ROOT`, `stripComments`, `assertNoForbiddenSurface`) -> the new single-parse gate"
    - "`scanWorkflowsDirectory` -> `verdictWarning` -> `warnings[]`: exactly one push per warned file"
---

<objective>
Prove the bridge-side contract of the gate line — one line per file, in both tenses,
with siblings untouched — pin WGATE-02 as a source gate rather than a claim, and
retire the four comments this phase falsified.

Purpose: WGATE-02 is a statement about the SHAPE of the code, so it needs a gate that
reads the code, not a unit test that reads its output. And a comment that survives the
change it describes is the quiet half of a broken contract.

Output: a new architecture gate asserting one `parse(` call site and no evaluator
surface, exhaustive bridge-layer warning-shape cases, and four corrected comments.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-RESEARCH.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-01-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
| Artifact | Kind | Purpose |
|---|---|---|
| `tests/architecture/workflows-single-parse.test.ts` | new test file | the WGATE-02 source gate: exactly one `parse(` call site in `domain/workflow-script.ts`, and no evaluator / function-constructor / `vm` / dynamic-import surface in that module or in `bridges/workflows/`. Also the WGATE-03 containment pin: `readEngineGate`'s `try`/`catch` is present in the same comment-stripped source |

No new production symbol. This plan adds test cases and corrects comments.

`tests/architecture/` is exempt from `scripts/check-corresponding-tests.mjs`
(`nonCorrespondingRoots = {architecture, e2e, integration}`), so the new file needs no
production pair. It goes in an EXISTING `tests/` subdirectory: a new top-level `tests/`
directory would redden `tests/architecture/unit-suite-glob-completeness.test.ts`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One warning line per file, both tenses, siblings untouched</name>
  <files>tests/bridges/workflows/discover.test.ts</files>
  <read_first>
    - `tests/bridges/workflows/discover.test.ts` end to end (890 lines) — how existing cases seed a temporary workflows directory, drive `discoverPluginWorkflows` in each tense, and assert over the returned `warnings` array.
    - `extensions/pi-claude-marketplace/bridges/workflows/discover.ts` as left by plan 115-01 — `softFailWarning` (`:98-105`), `INSTALL_OUTCOMES` / `PREVIEW_OUTCOMES` / `outcomePhrase` (`:107-141`), `unrunnableWarning` (`:209-232`), `verdictWarning` (`:234-270`), `scanWorkflowsDirectory`'s single warning push (`:396-404`).
    - `extensions/pi-claude-marketplace/bridges/workflows/types.ts` — `WorkflowOutcomeTense`, `WorkflowOutcomeSite` as left by plan 115-01.
    - `115-RESEARCH.md` "Interaction with the existing `stem-fallback` warning" and Common Pitfalls, "two warning lines for one file".
  </read_first>
  <behavior>
    - A directory holding one gate-tripping script and two well-formed siblings returns a `warnings` array of length 1, and neither sibling name appears in it.
    - The same directory in the preview tense returns one warning whose phrase is the future-tense pair of the install one, and whose reason text is identical.
    - A `stem-fallback` script whose `meta.name` is an interpolated template returns exactly ONE warning for that file, and that warning names the engine check.
    - A `stem-fallback` script that trips a structural gate earlier in the engine's order returns one warning naming that earlier check.
    - A directory holding one gate-tripping script, one skipped helper module and one determinism-refused script returns exactly three warnings — one per affected file, none doubled.
    - Warning order follows the directory scan order, which the module already sorts by entry name.
    - Warnings are returned frozen, and re-running discovery over the same directory returns an array of the same length (no accumulation across calls).
  </behavior>
  <action>
Extend `tests/bridges/workflows/discover.test.ts` with the bridge-layer contract cases.

The sibling case is WGATE-01's second clause and must assert the array LENGTH, not
merely that a sibling is absent: an assertion that a name does not appear is green when
the array is empty for the wrong reason.

The one-line-per-file cases are the pin for the overlap rule plan 115-01 stated in a
comment. Assert the count per file, so the rule is enforced by behavior rather than by
the comment that describes it. Also assert the gate is named in the surviving line for
the interpolated-template case, so "one line" cannot be satisfied by dropping the gate
name.

The mixed-directory case is the non-vacuity anchor: three affected files, three
warnings, and each warning names its own file. A discovery test whose expected warnings
array is longer than the number of affected files is the warning sign RESEARCH names.

Assert both tenses over the SAME fixture, comparing the two strings, so a tense that
drifts is a diff rather than two independently-passing cases.

The re-run case answers idempotency at the bridge layer: `discoverPluginWorkflows`
builds a fresh array per call, so a second call over an unchanged directory must return
the same length. Assert it rather than assume it.

Follow the existing fixture helpers in the file; do not add a new seeding mechanism.
Cite WGATE-01 and WGATE-03 in the titles.
  </action>
  <acceptance_criteria>
    - A case seeds one gate-tripping script plus two well-formed siblings and asserts `warnings.length === 1`.
    - A case asserts the install-tense and preview-tense strings for one fixture differ only in their outcome phrase.
    - A case asserts a `stem-fallback` script with an interpolated `meta.name` produces exactly one warning that names an engine check.
    - A case seeds a gate-tripping, a skipped and a determinism-refused script and asserts `warnings.length === 3`.
    - A case calls `discoverPluginWorkflows` twice over one unchanged directory and asserts the two arrays have equal length.
    - `node --test tests/bridges/workflows/discover.test.ts` reports no failing tests.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridges/workflows/discover.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/bridges/workflows/discover.ts</automated>
    <fails_when>non-zero exit — the script's own criterion is complete coverage of the module, so it exits non-zero when the focused run fails or when `assertCompleteCoverage` finds an uncovered line or branch. No baseline is captured anywhere in this phase and the script cannot compute a delta, so its own status is the whole pass condition</fails_when>
  </verify>
  <done>The bridge emits exactly one warning line per affected file in both tenses, siblings are untouched, and the stem-fallback overlap rule is pinned by a count rather than by a comment.</done>
</task>

<task type="auto">
  <name>Task 2: The WGATE-02 source gate — one parse call site, no evaluator surface, and the containment pinned permanently</name>
  <files>tests/architecture/workflows-single-parse.test.ts</files>
  <read_first>
    - `tests/architecture/source-scan.ts` end to end — `REPO_ROOT` (`:34`), `stripComments` (`:47-51`) and why stripping is mandatory, `assertNoForbiddenSurface` (`:71-105`) including the WR-06 must-exist rule and `opts.allowMissing`, and `filesMatching` (`:121`) including the non-global-regex assertion.
    - `tests/architecture/no-orchestrator-network.test.ts` — the house shape for a source-grep architecture gate: what it asserts, how it words its failure message, and its header note on why it is not replaceable by a fallow boundary rule.
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts:20-24` (the module header's SECURITY paragraph, the claim this gate makes machine-checkable) and `:26` (the acorn import) and `:528-555` (`parseScript`, the one call site).
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts` as left by plan 115-01 — `readEngineGate` and the `try`/`catch` wrapping its `GATE_ORDER` walk. Read `parseScript` (`:528-552`) beside it and note the DISANALOGY, because it is the reason case three exists: `parseScript`'s `catch` is pinned by ordinary unit tests because an unparseable script is NATURAL input, so any suite that feeds one exercises the catch. `readEngineGate`'s `catch` is not reachable by natural input at all — every predicate is a property test over an already-parsed tree — so no behavioral test can reach it without a planted defect. "It contains its throws the way `parseScript` already does" is true of the CODE and false of the EVIDENCE, and it is not a reason to leave the construct unpinned. A source-level assertion is what closes that gap, and it needs no export and no test-only seam: this task already reads this module's comment-stripped source from `REPO_ROOT` for case one.
    - `.planning/codebase/CONVENTIONS.md`, the paragraph beginning "A gate wants a test that plants the violation, not one that reads the config."
    - `115-RESEARCH.md` Security Domain, "Known threat patterns for this stack".
  </read_first>
  <action>
Create `tests/architecture/workflows-single-parse.test.ts`.

Case one — the count. Read `extensions/pi-claude-marketplace/domain/workflow-script.ts`
through `readFile` from `REPO_ROOT`, pass it through `stripComments`, and assert the
comment-stripped source contains EXACTLY ONE match of a bare `parse` call. Stripping is
mandatory: the module header legally names the call in prose, and an unstripped scan
would count its own subject's documentation. The count today is one, measured; the gate
therefore fails on a second parse being ADDED, which is WGATE-02's whole content.
Use `readFile`, never a `grep` subprocess (D-98-10: a subprocess that classifies a file
as binary reports nothing and greens a gate over zero inspected bytes).

Word the failure message so it states WGATE-02 and says what a second call site means:
the gate reader would be deciding on a tree the verdict was not built from.

Case two — the surface. Use `assertNoForbiddenSurface` over
`extensions/pi-claude-marketplace/domain/workflow-script.ts` plus every module under
`extensions/pi-claude-marketplace/bridges/workflows/`, forbidding the evaluator
surfaces the module header rules out: a direct `eval` call, a `Function` constructor
call, any import of `node:vm` or `vm`, and a dynamic `import(` call. This is the
machine-checkable half of the module's absolute security claim, and it is the
mitigation for the elevation-of-privilege threat: a gate that could be decided by
running the script would be wrong by construction (D-115-03 — nothing in the seven
needs evaluation, so nothing may reach for it).

Enumerate the bridge's module paths with `filesMatching` if a roster is wanted, or name
them explicitly; either way `assertNoForbiddenSurface` fails on a target that does not
exist, so a renamed file uncovers the gate loudly rather than silently.

Case three — the containment, pinned permanently. Reuse the SAME comment-stripped source
string case one already holds, slice out `readEngineGate`'s body, and assert the slice
carries both a `try` and a `catch` token. Today the ONLY evidence that the containment
exists is the one-time planted-throw transcript plan 115-01 writes into its SUMMARY, so
an editor who deletes the `try` reddens nothing and the phase's absolute — no gate
reading can ever block an install — reverts to a hope. This case makes it a property.
Title the case so it cites WGATE-03 and give NO other case in this file that anchor: the
focused verify below selects this case out of the TAP stream by that string, so the anchor
is load-bearing rather than decorative.

Keep the assertion ROBUST, not brittle, on three counts:

  - Slice by DECLARATION BOUNDARY, not by a formatting-shaped regex. Take the index of
    `function readEngineGate` in the stripped source, then the index of the next
    line-initial `function ` or `const ` after it, and assert on the text between. Every
    module-level declaration in this file starts at column zero, so the boundary is a
    structural fact rather than an indentation guess. If no declaration follows — the
    function is last in the file — slice to end of file rather than failing; only the
    START index is mandatory. Assert that start index was found, with a message saying so:
    a rename of `readEngineGate` must fail this case loudly, not green it over an empty
    slice — the same WR-06 failure mode `assertNoForbiddenSurface` exists to prevent,
    arriving through a slice instead of through a missing file. Assert the slice is
    non-empty too, so a zero-length window cannot satisfy the tokens vacuously.
  - Match WORD TOKENS (`try`, `catch`) with word boundaries, so brace style, line
    breaks, indentation, a bound error parameter, an optional-catch-binding `catch {`,
    and any rename of a local all pass unchanged. The case must fail when the construct
    is gone, not when the code is reformatted.
  - Do NOT assert the `catch` body's contents, and do not assert a `return undefined`.
    That is behavior, and behavior is already pinned twice in plan 115-01 — by the
    hostile-script install case and by the planted-throw transcript. Duplicating it here
    would buy nothing and would redden on a legal refactor.

State the one residual limitation in the case's own comment rather than overclaiming:
`stripComments` removes block comments and LINE-INITIAL `//` comments only, so a trailing
`// ...` comment inside this function survives the strip and could in principle carry the
matched words. The negative control below is what establishes the case fires on the real
removal; the case is a presence pin, not a proof of absence of a mention.

Run the negative control before believing any of the three cases. Back the module up,
then plant into the real file so the gate actually reads the plant: a second bare `parse`
call, confirm case one goes red, restore; a `Function` constructor call, confirm case two
goes red, restore; and remove the whole `try`/`catch` wrapper from `readEngineGate`,
leaving the walk unwrapped, confirm case three goes red, restore. Remove the WRAPPER
rather than the `catch` clause alone: a bare `try` with no `catch` and no `finally` is a
`SyntaxError`, and the regression this case guards is an unwrapped walk, not an unparseable
file. Confirm `git status --short` over
`extensions/pi-claude-marketplace/domain/workflow-script.ts` is empty afterwards. Record
all three transcripts in the SUMMARY. This milestone has shipped a gate that printed a
pass having checked nothing.

The file registers cases at its top level and must not import any `*.test.ts` module
(D-98-09: importing one under `node:test` registers its cases a second time).
  </action>
  <acceptance_criteria>
    - `tests/architecture/workflows-single-parse.test.ts` exists and imports `REPO_ROOT`, `stripComments` and `assertNoForbiddenSurface` from `./source-scan.ts`.
    - It asserts an exact count of one for the bare parse call in the comment-stripped module source, with a failure message naming WGATE-02.
    - It forbids `eval`, the `Function` constructor, `vm` and dynamic `import(` across `domain/workflow-script.ts` and the `bridges/workflows/` modules.
    - A third case, whose title cites WGATE-03 and is the only case in the file carrying that anchor, slices `readEngineGate`'s body out of the comment-stripped source by declaration boundary and asserts the slice carries both a `try` and a `catch` word token; it asserts the start boundary was found and the slice is non-empty, so a rename of `readEngineGate` fails rather than greens over an empty slice.
    - The SUMMARY carries all three planted-violation transcripts, pasted, not summarized — including the removal of `readEngineGate`'s `try`/`catch` wrapper, with the case-three failure output and the restored `git status --short`.
    - `node --test tests/architecture/workflows-single-parse.test.ts` reports no failing tests.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/workflows-single-parse.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test. Note the case-that-was-never-written shape is NOT catchable here: on this Node the default reporter counts the FILE as one test and prints a passing count for a file whose cases all went unreported. The tap-reporter command below is what catches it</fails_when>
    <automated>bash -c 'set -e; out=$(node --test --test-reporter=tap tests/architecture/workflows-single-parse.test.ts); printf "%s\n" "$out"; printf "%s\n" "$out" | grep -E "^ok [0-9]+ - .*WGATE-03"'</automated>
    <fails_when>non-zero exit. Three ways this reddens, all of them the point: `set -e` makes the capture fail when `node` exits non-zero; a containment case that FAILS prints `not ok` and so matches no `^ok` line; and a containment case that was never written, or whose title lost its WGATE-03 anchor, matches nothing either — which is the case-that-does-not-exist shape this whole phase is guarding against. `--test-reporter=tap` is REQUIRED and measured, not stylistic: the default reporter counts the FILE as one test and prints `pass 1` for a file whose cases all went unreported, so a summary-count clause over the default reporter would green on an empty run. `--test-name-pattern` was measured and REJECTED for this: on the Node in use a non-matching pattern still reports `pass 1` and exit 0, so it cannot select. `grep -q` is also wrong here — it closes the pipe early, and the resulting SIGPIPE surfaces as exit 141 even on a match. The whole transcript prints, for the SUMMARY</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output names any rule under `tests/architecture/workflows-single-parse.test.ts`</fails_when>
    <automated>node --test "tests/{architecture,bridges,domain}/**/*.test.ts"</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
  </verify>
  <done>WGATE-02 is enforced by a gate that reads the source and was seen to go red against a planted second parse call, not by a claim in a comment. And `readEngineGate`'s throw containment has a permanent check that was seen to go red against its removal, so the phase's absolute outlives the SUMMARY that first proved it.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the four comments this phase falsified</name>
  <files>extensions/pi-claude-marketplace/domain/workflow-script.ts, extensions/pi-claude-marketplace/bridges/workflows/discover.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts:264-299` — `stemFallbackVerdict`'s doc comment, whose closing sentence tracks the warning as an unlanded roadmap criterion.
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts:370-390` — the `DETERMINISM_BLOCKLIST` doc comment, whose closing paragraph makes two false claims: that this is the only replicated engine gate, and that the engine is a 0.x package.
    - `extensions/pi-claude-marketplace/bridges/workflows/discover.ts:234-250` — `verdictWarning`'s doc comment, which says the `named` arm alone earns no warning.
    - `docs/workflows-compatibility.md:52` (the bridge replicates two checks: the determinism screen AND the parse) and `:196` (the engine is well past 1.0 — 57 versions across three majors).
    - `.claude/rules/typescript-comments.md` in full — especially the "Narration of code that no longer exists" clause and the allowed traceability anchors.
    - `115-RESEARCH.md` "The sentence that must be retracted, and everything that leans on it" — the table naming each of these sites and why it moves.
  </read_first>
  <action>
Correct four comments, each to a present-tense fact about the code as it now stands.

In `stemFallbackVerdict`'s doc comment, the closing sentence tracks the user-facing
warning as an unlanded roadmap criterion. It ships in this phase. Replace it with a
statement of where the warning is now composed and what carries it, keeping the
requirement-ID anchors. Do not append a note and do not narrate the sentence being
replaced.

In the `DETERMINISM_BLOCKLIST` doc comment, correct both defects in the closing
paragraph. The determinism screen is one of TWO replicated gates — the parse is the
other, and the published table has always said so. And the host engine is at 3.10.1, not
a leading-zero major; the compatibility document already carries the correct
characterisation. Write the corrected clause as `3.10.1`, or as a phrase like "well past
1.0" — and NEVER as the literal `0.x`, not even to name what the sentence used to claim.
This task's own verify below negative-greps `0\.x` in this very file, so spelling the
retired characterisation into the correction reddens the task that makes it. That is also
what `.claude/rules/typescript-comments.md` already asks for: state the fact, do not
narrate the claim being replaced. Then
restate the reason clause for what the code now does: the remaining structural rules
are unexported internals with no exported contract, which is why this bridge WARNS on
the ones it can name rather than refusing on them — a spurious warning a reader can
ignore, rather than a blocked install only an extension release can clear (WGATE-03).
Keep the standing instruction that the vendored literal is re-checked on an engine
upgrade.

In `verdictWarning`'s doc comment, the claim that the `named` arm alone earns no
warning is now false. Restate the arms as they stand, and state the one-line-per-file
rule and the stem-fallback overlap resolution explicitly here — RESEARCH names silent
suppression of a coinciding line as an anti-pattern precisely because an unstated rule
reads as an accident of ordering.

Every edited comment keeps its requirement and decision IDs and gains none of the
forbidden planning references. Do not touch any comment this phase did not falsify.
  </action>
  <acceptance_criteria>
    - `stemFallbackVerdict`'s doc comment no longer describes the warning as tracked future work and names the composition site instead.
    - The `DETERMINISM_BLOCKLIST` doc comment no longer claims to be the only replicated gate and no longer characterises the host engine as 0.x.
    - `verdictWarning`'s doc comment no longer says the `named` arm alone earns no warning, and states the one-line-per-file rule.
    - No comment in `extensions/pi-claude-marketplace/domain/workflow-script.ts` characterises the host engine by a leading-zero major version; the verify command below is the check, and it exits zero only on a count of zero.
    - No edited comment contains a phase, plan, wave or task reference.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "0\.x" extensions/pi-claude-marketplace/domain/workflow-script.ts || true)" = "0"'</automated>
    <fails_when>non-zero exit, meaning the count was not zero — or grep could not read the file at all, in which case it prints nothing and the comparison against `0` fails rather than passing. A bare `grep -c` cannot be used here: it exits 1 on the very no-match this task is trying to reach, so its status would contradict its own pass condition</fails_when>
    <automated>bash -c 'test "$(grep -c -iE "phase [0-9]|plan [0-9]|wave [0-9]|task [0-9]" extensions/pi-claude-marketplace/domain/workflow-script.ts || true)" = "0"'</automated>
    <fails_when>non-zero exit, meaning a planning reference reached a comment in that module, or the file could not be read</fails_when>
    <automated>bash -c 'test "$(grep -c -iE "phase [0-9]|plan [0-9]|wave [0-9]|task [0-9]" extensions/pi-claude-marketplace/bridges/workflows/discover.ts || true)" = "0"'</automated>
    <fails_when>non-zero exit, meaning a planning reference reached a comment in that module, or the file could not be read. One command per file: `grep -c` over two files prints `path:count` pairs, which no single scalar comparison can decide</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit — `tsc`'s own status distinguishes a clean run from a type error AND from a crash, a missing compiler or a `tsconfig` load failure, none of which print a `TS` diagnostic line</fails_when>
    <automated>node --test tests/domain/workflow-script.test.ts tests/bridges/workflows/discover.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
  </verify>
  <done>No comment in the two changed modules states a claim this phase falsified, and each correction is a present-tense fact rather than a note appended to a stale sentence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin tree -> `domain/workflow-script.ts` | untrusted third-party JavaScript is parsed here; this plan gates the shape of that handling |
| repository source -> architecture gate | the gate reads source text and must not be greened by a file it never inspected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-08 | Elevation of privilege | `domain/workflow-script.ts`, `bridges/workflows/**` | critical | mitigate | `tests/architecture/workflows-single-parse.test.ts` forbids `eval`, the `Function` constructor, `vm` and dynamic `import(` across both, and is proven fail-first by a planted violation whose transcript goes in the SUMMARY. |
| T-115-09 | Tampering | the gate itself | high | mitigate | The gate reads with `readFile` rather than a `grep` subprocess (a subprocess greens over a file it classifies as binary), strips comments before matching (the module's own prose names the forbidden surfaces), and fails on a target that does not exist (`assertNoForbiddenSurface` WR-06). |
| T-115-10 | Spoofing | the gate reader's inputs | medium | mitigate | Task 1 pins that the reader decides on node `type` strings and the exact `meta` identifier only, so no attacker-chosen text steers a gate verdict, and that no `GATE_REASONS` value interpolates script-derived text. |
| T-115-11 | Repudiation | the four corrected comments | low | accept | A comment cannot be machine-verified for truthfulness. The two greps pin the falsifiable literals; the rest is reviewer-read, which is the honest ceiling. |
| T-115-12 | Tampering | dependency manifests | high | mitigate | No package is installed. The manifest-diff guard in `<verification>` proves `package.json` and `package-lock.json` are untouched. |
</threat_model>

<verification>
```
npm test && npm run typecheck && npm run fallow
```

Fails when: non-zero exit from any of the three, or the unit TAP summary reports a
non-zero `# fail` count.

```
npm run test:corresponding
```

Fails when: non-zero exit, meaning a production module lost its paired test file — the
gate that would have fired had the gate reader landed in a new module.

```
bash -c 'test -z "$(git diff HEAD --name-only -- package.json package-lock.json)"'
```

Fails when: non-zero exit.

Reviewer-read, not automated: that each corrected comment reads as a present-tense fact
and narrates nothing that no longer exists.
</verification>

<success_criteria>
- One warning line per affected file, in both tenses, with siblings untouched, pinned by counts.
- WGATE-02 is a source gate seen to go red against a planted second parse call.
- The four falsified comments are gone, and the two falsifiable literals grep to zero.
- `npm test`, `npm run typecheck`, `npm run fallow` and `npm run test:corresponding` are green.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-02-SUMMARY.md` when done.
</output>
