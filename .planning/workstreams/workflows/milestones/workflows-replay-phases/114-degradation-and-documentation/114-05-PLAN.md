---
phase: 114-degradation-and-documentation
plan: 05
type: execute
wave: 3
depends_on: ["114-02", "114-03", "114-04"]
autonomous: true
requirements: [WDOC-01, WDOC-03]

files_modified:
  - docs/workflows-compatibility.md
  - README.md
  - README.es.md
  - tests/domain/resolver.test.ts
  - .planning/spikes/027-workflow-engine-3-10-1-recheck/README.md

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/workflows-compatibility.md` states plainly, near the top and unhedged, that this is the FIRST bridge to install executable code rather than data, names `@quintinshaw/pi-dynamic-workflows` as the host engine, and gives the trust grounds it was chosen on -- including the sandbox comparison against the engine that was rejected."
    - "The document carries an admit-versus-run divergence table: the engine refuses at NINE distinct checks at 3.10.1, this bridge replicates TWO of them (determinism and parse), determinism runs FIRST, and six named script shapes install here and then refuse to run there (CONTEXT D-114-03)."
    - "The document states NO single refusal-message total. Check 2's message is the parser's own text and is not enumerable, so any total would be a number with no honest counting rule. It enumerates the six distinct `validateMeta` message shapes instead."
    - "Every engine claim carries its evidence grade explicitly -- runtime-measured, source-read at the pinned version, or documented upstream -- and no source read is presented as a runtime measurement. The host's `agent()` failure semantics keep their source-read grade; the measurement that would upgrade them is a later phase's work and must not be pre-stated here."
    - "Engine claims cite `@quintinshaw/pi-dynamic-workflows` 3.10.1 and Spike 027; no figure inherited from the 3.5.1-era records survives anywhere in the document."
    - "The engine's own peer floor `pi-coding-agent >=0.80.8` and this project's `>=0.80.5` are both stated, labelled as belonging to different packages, so a reader cannot infer that this project raised its own."
    - "The document states what a plugin shipping an EMPTY `workflows/` directory does: it installs, declares no host-engine dependency, and its row carries no marker (WDOC-01 empty edge)."
    - "The document's naming section states the string-identity rule as exact bytes -- the generated colon form, the engine's own trim and length limits, no case folding and no Unicode normalization -- and names the template-literal case where the engine resolves a name DIFFERENT from the one installed (WDOC-01 encoding edge)."
    - "The upstream `workflows` manifest field is confirmed path-bearing with a `string | array` shape, cited to Claude Code 2.1.251's own schema (graded HIGH) and to the published plugins reference (graded MEDIUM), and both grades are stated as such. `SUPPORTED_COMPONENT_PATH_KINDS` keeps `workflows`; no behavior changes (CONTEXT D-114-01: criterion 6 is CONFIRMED, so the change is prose only)."
    - "`tests/domain/resolver.test.ts`'s 'the premise has lineage but no upstream citation' paragraph is REPLACED by that citation -- the consequence stays pinned and the test stays green; only its prose moves."
    - "`acorn` remains declared exactly once at `^8.16.0` in `package.json` `dependencies`, and this phase's diff touches neither `package.json` nor `package-lock.json` (WDOC-03 concurrency edge: the requirement is verified, not re-implemented)."
    - "Both README files name workflows in their Features list, name the host engine in their Prerequisites list, and link the new compatibility document; the Spanish file stays line-for-line parallel with the English one."
    - "Every install instruction in the document and in both READMEs names the SCOPED package form. `pi-workflows` and `pi-dynamic-workflows` are two real, different packages on npm."
    - "Every `tests/...` path the document cites resolves on disk, so `tests/architecture/no-stale-test-citations.test.ts` stays green."
  artifacts:
    - docs/workflows-compatibility.md (new)
    - README.md (Features bullet, Prerequisites entry, doc link)
    - README.es.md (the same three, translated, line-for-line parallel)
    - tests/domain/resolver.test.ts (the premise paragraph replaced by the citation)
    - .planning/spikes/027-workflow-engine-3-10-1-recheck/README.md (the refusal-message-count sentence corrected against the source)
  key_links:
    - "the README Features bullet is the ONLY discoverability path to the compatibility document: there is no doc gate and no test enumerating `docs/*.md`, so an unlinked file is invisible"
    - "`tests/architecture/no-stale-test-citations.test.ts` policies `docs/**` prose -- `docs/adr/`, `docs/research/` and `docs/plans/` are excluded but this new file is NOT -- so the document must land AFTER the suites it names, which is why this plan is the last wave"
    - "the divergence table's row count is fixed by the engine's own check count; a table with fewer rows understates the divergence"
  prohibitions:
    - "No claim may be graded stronger than its evidence. A source read presented as a runtime measurement is the exact failure this document exists to avoid, because a plugin author decides whether to ship executable code on the strength of the sandbox claim."
    - "The executable-code risk must not be softened. The sandbox comparison is the load-bearing content; a reader who skims it and ships a script is trusting it."
    - "No figure from the 3.5.1-era records may be repeated. Every inherited count in the archived phase's documents was re-derived and several moved; a stale count in a published contract is worse than no count."
    - "A spike record left saying something its own cited source contradicts is how a wrong figure gets cited again. The correction lands in the same change that publishes the document, not later."
    - "No `package.json`, `package-lock.json`, `sonar-project.properties`, `CHANGELOG.md` or extension-version edit is in scope. The version bump is milestone-close work; any such change here is a review finding."
  flagged_assumptions:
    - "Edge-probe predicates authored in this plan: 3 of the phase's 14 (WDOC-01/empty, WDOC-01/encoding, WDOC-03/concurrency). Plan 01 authored 5, plan 02 authored 2, plan 03 authored 2, plan 04 authored 1 truth plus the WDOC-02 flagged assumption -- 13 truths plus 1 flagged assumption, 14 in all."
    - "Confirmed at plan time, for the install-time disposition section: `bridges/workflows/discover.ts` reaches the filesystem through `shared/fs-utils.ts::readDirEntriesTolerant`, which returns an EMPTY array on both `ENOENT` and `ENOTDIR`. So an upstream-legal `workflows` component path pointing at a `.js` FILE is silently dropped -- zero discovered workflows, no throw, no warning. That is a documented divergence, not a bug, and no Broken Windows entry is owed for it. Verify the read before publishing the sentence."
    - "The operator has an open question on the Spanish Features bullet: `Workflows (flujos de trabajo).` (parallel to the existing `Hooks (ganchos).`) versus `Flujos de trabajo (workflows).`. Pick the first for parallelism, and surface the choice in the SUMMARY so the operator can flip it -- the link text changes with it."
---

<objective>
Write down the contract of the one bridge that installs executable code: which
engine runs third-party JavaScript, how it sandboxes it, which script shapes
install and then refuse to run, and which claims were measured versus read.
Settle the path-bearing premise in the two places that carry it, and make the
document findable from both READMEs.

Purpose: a per-kind contract buried in a general README is a contract nobody
finds, and a plugin author deciding whether to ship a `workflows/` directory
needs the sandbox comparison, not just the API.

Output: one new compatibility document, two README updates, one test-comment
citation, and one corrected spike record.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-CONTEXT.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md
@.planning/spikes/027-workflow-engine-3-10-1-recheck/README.md
</context>

## Artifacts this phase produces

The full phase-wide list is in `114-01-PLAN.md`. This plan owns:

| Symbol / artifact | Home |
|---|---|
| `docs/workflows-compatibility.md` | new document |
| the Workflows Features bullet + host-engine Prerequisites entry | `README.md`, `README.es.md` |
| the upstream citation replacing the open-premise paragraph | `tests/domain/resolver.test.ts` |
| the corrected refusal-message sentence | `.planning/spikes/027-workflow-engine-3-10-1-recheck/README.md` |

<tasks>

<task type="auto">
  <name>Task 1: The executable-code contract</name>

  <files>docs/workflows-compatibility.md</files>

  <read_first>
- `docs/hooks-compatibility.md` -- the WHOLE file, 256 lines. It is the structural template. Copy its skeleton exactly: H1 + one-sentence purpose naming implemented/deferred/unsupportable + legend line + provenance paragraph (:1-7); the big feature table (:9); the one prose subsection admitting an irreducible divergence under a supported row (:39-43) -- that is the register and the closing "why we still mark this supported" sentence to imitate; the three-bucket classification section (:45); the per-facet four-column tables (:74-224); `## Install-time disposition` (:225-252), whose shape is an intro sentence stating how many dispositions there are, then one bold-lead paragraph per disposition followed by a bulleted applies-to list; and `## Further reading` (:253-256)
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 319-410 (criterion 6: the verbatim manifest schema, the verbatim loader, the decoy to avoid citing, and the two divergences the confirmation exposes) and lines 872-1026 (every engine fact re-verified at 3.10.1: the moved figures, the probe targets, the check list, the sandbox table with its PER-ROW grades, the failure-semantics grades, the recovery mechanism, and the upstream-stability warning)
- `.planning/spikes/027-workflow-engine-3-10-1-recheck/README.md` -- the cited re-measurement, its negative control, the engine peer floor, the `meta.description` gap, and the one sentence Task 3 corrects
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md` section 3 -- the section-by-section mapping from the hooks document to this one, the four-column table shape, the `--` dash rule, and the gate constraints on this file
- `extensions/pi-claude-marketplace/bridges/workflows/discover.ts` -- the whole file, to confirm the install-time dispositions from the source rather than from a summary
- `extensions/pi-claude-marketplace/domain/workflow-script.ts` -- the verdict arms (admitted, skipped/no-meta, stem fallback, refusals) that the disposition section enumerates
- `extensions/pi-claude-marketplace/domain/name.ts` and the saved-name assertion it wraps -- the engine's own validator clauses, for the naming section
- `.agents/skills/simple-english/SKILL.md` and `.agents/skills/humanizer/SKILL.md` -- apply them, but match the sibling document's register, which is denser than the plain default
  </read_first>

  <action>
Create `docs/workflows-compatibility.md`, mirroring `docs/hooks-compatibility.md`
section for section.

**Opening.** H1, a one-sentence purpose naming what was implemented, deferred and
declared unsupportable, the same legend line the sibling uses, then a provenance
paragraph. The provenance paragraph is where the evidence-grade labelling starts:
name `@quintinshaw/pi-dynamic-workflows` at 3.10.1, Spike 027, and Claude Code
2.1.251, and say which claims are runtime-measured and which are source-read.
Immediately after, state plainly and unhedged that this is the FIRST bridge to
install executable code rather than data, name the host engine, and give the
trust grounds: it runs scripts inside a `vm.createContext` realm; the alternative
engine runs them with the real host `process`, its environment variables, real
filesystem internals and a Function-constructor escape into the host realm.

**`## Manifest and discovery`.** The feature table plus the criterion-6
confirmation. Upstream declares `workflows` as a union of a path string and an
array of path strings, described in the shipped 2.1.251 binary's own schema as a
path to a workflows directory or `.js` file relative to the plugin root, in a
body written identically to the `themes` and `outputStyles` definitions, and
normalized by the loader through the same array-or-single idiom it uses for
`agents`. Grade the binary read HIGH and the published reference page MEDIUM, and
say which is which. Do not cite the second `workflows` field that appears in the
same binary: it belongs to the SKILL frontmatter schema, not the plugin manifest.
Then state the two divergences the confirmation exposes (CONTEXT D-114-02), as
divergences and not as bugs:
- Upstream REPLACES the convention directory when the field is declared and
  raises a folder-shadowed diagnostic; this project UNIONs declared paths with
  the convention directory (D-07), the same as `commands` and `agents`.
- Upstream admits a `.js` FILE path, not only a directory. Confirm against
  `bridges/workflows/discover.ts` how a file target behaves here before writing
  the sentence -- the discovery walk reaches the filesystem through a tolerant
  directory read that returns an empty list on both a missing path and a
  not-a-directory error, so the expected finding is a silent drop with no throw
  and no warning. Document that as an install-time disposition.

**`### Admit-versus-run divergence`.** The mandatory table (CONTEXT D-114-03), and the
one section with no in-tree analog -- the columns are yours. It must carry: that the engine
refuses at NINE distinct checks at 3.10.1; that this bridge replicates TWO of
them, the determinism screen and the parse; that the determinism screen runs
BEFORE the parse, so a script that is both nondeterministic and unparseable is
reported as nondeterministic; and one row per admitted-but-refused shape. The six
shapes are: `meta` missing a non-empty description; `meta` not the first
statement; a `let` rather than `const` meta export; a non-exported `const meta`;
a meta export declaring a second binding alongside it; and both stem-fallback
arms (no `name` property, and a non-literal `name`). Add the template-literal
case explicitly: such a script is admitted under a DIFFERENT name from the one
the engine resolves. Close the section the way the sibling document closes its
own divergence subsection -- but the other way round, because this divergence IS
invocation-observable: the artifact installs, the command registers, and the
engine reports the failure at first invocation. Cite Spike 027, not a
`docs/research/` path.

**`### Refusal-check classification`.** Enumerate the nine checks and mark which
two are replicated. Enumerate the six distinct message shapes the engine's meta
validator throws. State NO single refusal-message total: the parse check's message
is the parser's own text and is not enumerable, so a total would be a number with
no honest counting rule. Say that in one sentence, so a later reader does not add
one back.

**Per-facet four-column tables** in the sibling's shape
(`| <Subject> | Claude Code | Pi | Notes |`, glyphs, lowercase Notes carrying the
reason, no trailing period): script semantics, the sandbox comparison, and
naming.
- The sandbox table's rows carry DIFFERENT grades and must be labelled per row:
  the frozen `process` stub is source-read at 3.10.1; the absent environment is
  runtime-measured at 3.10.1; the filesystem-internals and Function-constructor
  rows carry the earlier 3.5.1 measurement; and the clock/RNG row is a
  refinement at 3.10.1 -- there are TWO layers there now, a pre-parse rejection
  AND an in-realm replacement of the clock and RNG, not one.
- The naming section states the string-identity rule as exact bytes: the
  generated colon form joining the plugin name and the script stem, the engine's
  own trim and length limits, no case folding and no Unicode normalization, and
  the template-literal case where the resolved name differs from the installed
  one.
- State the host's `agent()` failure semantics at the grade they hold TODAY:
  runtime-measured on the rejected engine (it throws) and source-read only on the
  host engine (every failure branch in its agent implementation throws, never
  driven at runtime because that needs real spawn machinery). Contrast with
  Claude's own documented resolve-to-null behavior. Do NOT pre-state the
  measurement a later phase owns.

**Peer floors.** State the engine's own floor and this project's floor as TWO
floors belonging to two packages, so a reader cannot infer that this project
raised its own.

**Upstream stability.** A stated risk, not a footnote: the envelope shape, the
working-directory key derivation, the saved-directory layout and the name
validator are private internals of a 0.x package with 57 published versions and
no exported contract. There is no environment-variable override and no settings
knob to relocate storage.

**`## Install-time disposition`.** Same heading as the sibling, same shape: an
intro sentence stating how many dispositions there are, then one bold-lead
paragraph per disposition with a bulleted applies-to list. Five dispositions.
Four of them are behavior from the three preceding phases that is currently
documented nowhere a plugin author would find; read
`bridges/workflows/discover.ts` and `domain/workflow-script.ts` to state each
one from the source. Include the EMPTY `workflows/` directory case explicitly:
the plugin installs, declares no host-engine dependency, and its row carries no
marker.

**`## Further reading`.** Two bullets in the sibling's shape: the upstream
authoritative reference URL with a sentence on what it covers, and the host
package with a sentence on what it contracts.

**Gate constraints.** Every install instruction names the SCOPED package form --
the two engines are real, different npm packages and the unscoped name is the
one this milestone rejected. Every `tests/...` path the document cites must
resolve on disk; cite only suites that exist now (this plan runs after the plans
that create them). Never repeat a gate count or a release count inherited from
the 3.5.1-era records. The file is formatted by `mdformat` through pre-commit --
never `prettier --write`; use `--` for dashes and let the texthooks fixer
normalize them.
  </action>

  <verify>
    <automated>node --test tests/architecture/no-stale-test-citations.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- the new document cites a `tests/...` path that does not resolve on disk</fails_when>
    <automated>grep -c '3.10.1' docs/workflows-compatibility.md</automated>
    <fails_when>prints `0`</fails_when>
    <automated>grep -c 'Spike 027' docs/workflows-compatibility.md</automated>
    <fails_when>prints `0`</fails_when>
    <automated>grep -c '@quintinshaw/pi-dynamic-workflows' docs/workflows-compatibility.md</automated>
    <fails_when>prints `0`</fails_when>
    <automated>grep -cE '0\.80\.8' docs/workflows-compatibility.md</automated>
    <fails_when>prints `0` -- the engine's own peer floor is not stated</fails_when>
    <automated>grep -cE '0\.80\.5' docs/workflows-compatibility.md</automated>
    <fails_when>prints `0` -- this project's peer floor is not stated beside it</fails_when>
    <automated>grep -inE 'seven gates|seven pre-execution|3\.5\.1 figures' docs/workflows-compatibility.md ; test $? -eq 1</automated>
    <fails_when>the command exits non-zero, meaning grep matched -- a stale gate-count phrase from the 3.5.1-era records survived into the published document</fails_when>
    <automated>grep -n 'pi install npm:' docs/workflows-compatibility.md | grep -vc '@quintinshaw/'</automated>
    <fails_when>prints anything other than `0` -- an install instruction names an unscoped package</fails_when>
    <automated>pre-commit run --files docs/workflows-compatibility.md</automated>
    <fails_when>non-zero exit, or the output reports that `mdformat`, `markdownlint-cli2` or a texthooks fixer modified the file</fails_when>
  </verify>

  <acceptance_criteria>
- The document contains a sentence stating, unhedged and without a qualifier,
  that this is the first bridge to install executable code rather than data.
- The refusal-check section names nine checks and marks exactly two as
  replicated, and states that the determinism screen runs before the parse.
- The document contains no single refusal-MESSAGE total, and contains one
  sentence explaining why it declines to state one.
- The admit-versus-run table has one row per admitted-but-refused shape, at least
  six rows, and the template-literal case is stated as resolving a DIFFERENT
  name.
- Every row of the sandbox table carries an explicit grade word, and at least two
  distinct grades appear (a source read and a runtime measurement).
- The `agent()` rows state two different grades for the two engines.
- Both peer floors appear and each is attributed to its own package by name.
- An `## Install-time disposition` section exists, its intro states five
  dispositions, and one of the five is the empty-`workflows/`-directory case.
- The naming section states the generated colon form, the absence of case folding
  and of Unicode normalization, and the template-literal divergence.
- `node --test tests/architecture/no-stale-test-citations.test.ts` is green.
  </acceptance_criteria>

  <done>
`docs/workflows-compatibility.md` exists, mirrors the hooks document's structure,
carries the admit-versus-run divergence table and the graded sandbox comparison
at 3.10.1, states both peer floors as two, and cites only paths that resolve.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the contract findable from both READMEs</name>

  <files>
README.md,
README.es.md
  </files>

  <read_first>
- `README.md` lines 20-45 -- the Features list and the Prerequisites list. The ANALOG for the new Features bullet is the Hooks bullet: it is the one that both flags reduced support AND links its compatibility document. The ANALOG for the new Prerequisites entry is the `pi-subagents` / `pi-mcp-adapter` pair
- `README.es.md` lines 20-45 -- the line-for-line twin. Identical bullet order, identical URLs, translated prose only. Register notes: `Requiere` for the companion requirement, `Para más información, consulta` for the doc link, `(opcional pero recomendado, ...)` for the prerequisite
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md` section 10 -- both files' current text quoted, and the measured Spanish delta (2 bullet lines plus 1 prerequisite line)
- `docs/workflows-compatibility.md` (as written in Task 1) -- so the link text matches the document's own H1
  </read_first>

  <action>
Add three things to each README, keeping the two files line-for-line parallel.

**Features list.** One bullet naming workflows as a component kind, flagging that
the host engine is required, and linking `docs/workflows-compatibility.md`. Model
it on the Hooks bullet, which is the only existing bullet that both flags reduced
support and links its compatibility document. In the Spanish file the link TARGET
stays the English filename -- that is what the Hooks bullet already does.

**Prerequisites list.** One entry naming the host engine, marked optional but
recommended, with its install command, in the same shape the two existing
companion entries use. The command names the SCOPED package. The unscoped name
belongs to a different, real npm package -- the one this milestone rejected on
trust grounds -- so an install instruction carrying it would send the operator to
the wrong engine.

**Spanish wording.** For the Features bullet use the parallel form the existing
Hooks bullet takes, keeping the English component word first with the Spanish
gloss in parentheses. The operator has an open preference question on that
choice; make it, and surface it in the SUMMARY rather than silently deciding it.
If any other wording is uncertain, add it and say so in the SUMMARY -- leaving
the Spanish file divergent is worse than a modest translation.

Both files are formatted by `mdformat` through pre-commit; never
`prettier --write` them.
  </action>

  <verify>
    <automated>grep -c 'docs/workflows-compatibility.md' README.md README.es.md</automated>
    <fails_when>either file prints `0`</fails_when>
    <automated>grep -c '@quintinshaw/pi-dynamic-workflows' README.md README.es.md</automated>
    <fails_when>either file prints `0`</fails_when>
    <automated>grep -n 'pi install npm:' README.md README.es.md | grep -c 'npm:pi-workflows'</automated>
    <fails_when>prints anything other than `0` -- an install instruction names the rejected engine</fails_when>
    <automated>bash -c 'diff <(grep -c "^- " README.md) <(grep -c "^- " README.es.md)'</automated>
    <fails_when>the command prints any differing line -- the two files no longer carry the same number of top-level bullets, so they are not line-for-line parallel</fails_when>
    <automated>node --test tests/architecture/no-stale-test-citations.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>pre-commit run --files README.md README.es.md</automated>
    <fails_when>non-zero exit, or the output reports that `mdformat`, `markdownlint-cli2` or a texthooks fixer modified either file</fails_when>
  </verify>

  <acceptance_criteria>
- Each README's Features list has a workflows bullet that links
  `docs/workflows-compatibility.md`.
- Each README's Prerequisites list has an entry naming the scoped host-engine
  package with an install command.
- The two files have the same number of Features bullets and the same number of
  Prerequisites entries, in the same order.
- No install command in either file names the unscoped package.
- The SUMMARY records the Spanish Features-bullet wording chosen and states that
  the operator may flip it.
  </acceptance_criteria>

  <done>
Both READMEs name workflows as a component kind, name the host engine as an
optional-but-recommended prerequisite with a scoped install command, and link
the compatibility document; the Spanish twin stayed parallel.
  </done>
</task>

<task type="auto">
  <name>Task 3: Settle the premise in the test, correct the spike, verify the dependency</name>

  <files>
tests/domain/resolver.test.ts,
.planning/spikes/027-workflow-engine-3-10-1-recheck/README.md
  </files>

  <read_first>
- `tests/domain/resolver.test.ts` lines 1790-1830 -- the `WINV-01` strict case and the paragraph above it. Lines 1804-1808 are the paragraph being replaced; note that it also carries a bare planning-artifact reference that the project's comment policy forbids, so the replacement removes that too
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 319-394 -- the verbatim schema definition, the verbatim loader, the version and file path of the binary that was read, the published reference URL, and the decoy field NOT to cite
- `.planning/spikes/027-workflow-engine-3-10-1-recheck/README.md` line 56 -- the message-count sentence, and line 70's check-9 row for context
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 941-970 -- the verbatim 3.10.1 meta validator and the reconciliation the correction must state
- `package.json` -- the `dependencies` block, for the WDOC-03 verification
- `.claude/rules/typescript-comments.md`
  </read_first>

  <action>
**The test comment (CONTEXT D-114-01).** Replace the "premise has lineage but no upstream citation"
paragraph in `tests/domain/resolver.test.ts` with the citation itself. State that
upstream's plugin manifest declares `workflows` as a path string or an array of
path strings; cite Claude Code 2.1.251's own schema, whose wording is a path to a
workflows directory or `.js` file relative to the plugin root, and the published
plugins reference page; and state that the harsher `unavailable` verdict this
case pins therefore rests on a confirmed premise rather than an open one. Keep
the case's assertions byte-identical -- nothing changes but the prose. Remove the
planning-artifact reference the old paragraph carried; the project's comment
policy allows requirement and decision IDs as anchors but not phase or plan
numbers. Do NOT narrate the replacement; write the citation as a present-tense
fact.

**The spike record (CONTEXT D-114-03).** Correct the sentence in
`.planning/spikes/027-workflow-engine-3-10-1-recheck/README.md` that states a
total count of the engine's refusal messages. Read verbatim at 3.10.1 the meta
validator throws SIX distinct messages, not the number the spike recorded, and no
honest single total exists because the parse check's message is the parser's own
text and is not enumerable. Correct the sentence to the verbatim reading and say
why no total is stated. A spike left saying something its own cited source
contradicts is how a wrong figure gets cited again, which is why the correction
lands in the same change that publishes the document.

**WDOC-03.** Verify only. Confirm `acorn` is declared exactly once at `^8.16.0`
in `package.json` `dependencies`, and confirm this phase's whole diff touches
neither `package.json` nor `package-lock.json`. Record the verification in the
SUMMARY. A re-declaration, a version bump, or any manifest edit is a review
finding, not work.
  </action>

  <verify>
    <automated>node --test tests/domain/resolver.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- the pinned `WINV-01` strict case must stay green because no behavior changed</fails_when>
    <automated>grep -c 'no upstream citation' tests/domain/resolver.test.ts</automated>
    <fails_when>prints anything other than `0`</fails_when>
    <automated>grep -c '2.1.251' tests/domain/resolver.test.ts</automated>
    <fails_when>prints `0` -- the citation names no upstream version</fails_when>
    <automated>grep -cEi 'phase [0-9]+|plan [0-9]+|wave [0-9]+' tests/domain/resolver.test.ts</automated>
    <fails_when>prints anything other than `0` -- a planning-artifact reference survives in a test comment</fails_when>
    <automated>node -e 'const p=require("./package.json");if(p.dependencies.acorn!=="^8.16.0")process.exit(1);console.log("acorn",p.dependencies.acorn)'</automated>
    <fails_when>non-zero exit, or the printed range is not `^8.16.0`</fails_when>
    <automated>bash -c 'test -z "$(git diff HEAD --name-only -- package.json package-lock.json sonar-project.properties CHANGELOG.md)"'</automated>
    <fails_when>non-zero exit -- a manifest, version or changelog edit is sitting in the working tree, and none is in scope</fails_when>
    <automated>node -e 'const p=require("./package.json");const v=require("fs").readFileSync("extensions/pi-claude-marketplace/shared/extension-version.ts","utf8");if(p.version!=="0.19.0")process.exit(1);if(!v.includes("\"0.19.0\""))process.exit(1);console.log("version pinned at",p.version)'</automated>
    <fails_when>non-zero exit -- the version moved during this phase. It was `0.19.0` in `package.json` and in `EXTENSION_VERSION` when this phase was planned, and the bump is milestone-close work</fails_when>
    <automated>grep -c 'workflow_control' extensions/pi-claude-marketplace/domain/resolver.ts</automated>
    <fails_when>prints anything other than `0` -- the resolver must not have acquired a probe; criterion 6 confirmed the premise, so no behavior changes</fails_when>
  </verify>

  <acceptance_criteria>
- `tests/domain/resolver.test.ts`'s `WINV-01` strict case has byte-identical
  assertions; only the comment above it changed.
- The replacement comment cites the upstream version it was read from and states
  the `string | array` shape as a confirmed fact.
- No `Phase`/`Plan`/`Wave` followed by a number remains anywhere in
  `tests/domain/resolver.test.ts`.
- `extensions/pi-claude-marketplace/domain/resolver.ts` is absent from this
  phase's diff: `SUPPORTED_COMPONENT_PATH_KINDS` keeps `workflows` unchanged.
- The spike record's message-count sentence names six distinct meta-validator
  messages and states why no total is given.
- `package.json` `dependencies.acorn` is `^8.16.0` and neither manifest file
  appears in the phase diff.
  </acceptance_criteria>

  <done>
The path-bearing premise is settled and cited in both places it lives; the spike
record no longer contradicts its own source; `acorn` is verified declared and no
dependency manifest moved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the compatibility document -> a plugin author's decision to ship executable code | The sandbox comparison is the basis on which a third party decides to distribute JavaScript that will run on other people's machines. |
| a README install instruction -> the operator's package manager | A named package is an implicit instruction to install it. |
| an evidence grade -> a reader's confidence | A source read presented as a measurement transfers unearned confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-18 | Spoofing | install instructions in the document and both READMEs | high | mitigate | `pi-workflows` and `pi-dynamic-workflows` are two real, different npm packages, and the unscoped one is the engine rejected on trust grounds -- it runs third-party scripts with the real host `process`, its environment variables, real filesystem internals and a Function-constructor escape. Every install instruction names the scoped form; Task 1 and Task 2 each carry a grep gate that fails on an unscoped one. |
| T-114-19 | Repudiation | evidence grades in the sandbox and failure-semantics tables | high | mitigate | Each row carries an explicit grade word, at least two distinct grades appear, and the two engines' `agent()` rows state two different grades. The measurement a later phase owns must not be pre-stated. Reviewer read is the backstop, per the manual-only row in `114-VALIDATION.md`. |
| T-114-20 | Information disclosure | the document understating the executable-code risk | high | mitigate | The document itself is the control. State the executable-code fact unhedged, keep the sandbox comparison intact, and state the upstream-stability risk as a risk rather than a footnote. |
| T-114-21 | Tampering | dependency manifest | high | mitigate | No `package.json` / `package-lock.json` / `sonar-project.properties` / `CHANGELOG.md` change is in scope; `acorn ^8.16.0` is already declared and WDOC-03 is verification-only (CONTEXT D-114-07). Task 3's verify asserts an empty phase diff on all four files. |
| T-114-22 | Repudiation | `.planning/spikes/027-.../README.md` | medium | mitigate | A spike record that contradicts its own cited source is a future wrong citation. The correction lands in the same change that publishes the document, and the SUMMARY records both the old and the corrected reading. |
| T-114-SC | Tampering | npm dependency manifest | high | mitigate | Same control as T-114-21. This phase installs nothing, so no package-legitimacy checkpoint is required; the audit table's absence is correct rather than missing. |
</threat_model>

<verification>
- `node --test tests/architecture/no-stale-test-citations.test.ts` green -- every
  `tests/...` path the new document cites resolves.
- `node --test tests/domain/resolver.test.ts` green with byte-identical
  assertions.
- `node --test tests/architecture/peer-floor.test.ts` green -- this project's
  floor is unchanged at `>=0.80.5` while the document names the engine's
  `>=0.80.8`.
- `pre-commit run --files docs/workflows-compatibility.md README.md README.es.md`
  clean and modified nothing.
- `git diff HEAD --name-only -- package.json package-lock.json sonar-project.properties CHANGELOG.md`
  is empty, and `package.json` `version` plus `EXTENSION_VERSION` are still `0.19.0`.
- `npm run check` green (the phase gate).

**Reviewer reads (manual, per `114-VALIDATION.md`):**
- Every sandbox and failure-semantics row's grade matches its cited source.
- Both peer floors appear and are labelled as belonging to different packages.
- `README.md` and `README.es.md` diff line-for-line parallel.
</verification>

<success_criteria>
- Criterion 4: the document states which engine runs third-party JavaScript, how
  it is sandboxed, which script shapes install and then refuse to run, and which
  claims were measured versus read; engine claims cite 3.10.1 and Spike 027.
- Criterion 5: the engine's peer floor is documented as distinct from this
  project's.
- Criterion 6: the path-bearing premise is confirmed against Claude Code's own
  artifacts, cited in the document and in the test, and no behavior changes.
- WDOC-03 verified with no manifest edit.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-05-SUMMARY.md` when done.

It MUST record: the Spanish Features-bullet wording chosen and any other Spanish
wording the executor was unsure of; the install-time disposition finally
documented for an upstream-legal `.js` FILE component path, read from
`bridges/workflows/discover.ts` rather than assumed; the spike sentence as it
read before and after the correction; and the WDOC-03 verification result.
</output>
