---
phase: 116-load-time-workflow-convergence
plan: 02
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - tests/orchestrators/reconcile/backfill.test.ts
  - tests/integration/workflow-kind-inversion.test.ts
autonomous: true
requirements: [WCONV-01, WCONV-02, WCONV-03]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A record whose supported set did not grow is scanned and not re-materialized: no envelope is written, no row is pushed, and the scope's state file is left alone (WCONV-02, criterion 3)."
    - "A record the user disabled is NEVER scanned, and that is a MEASURED ZERO — a positive observation downstream of the filter, not an absence inferred from a missing row (D-116-03, criterion 3)."
    - "The self-heal is one-time: a second load over an already-converged scope leaves `state.json` identical in bytes, inode AND mtime, so an atomic rewrite of identical content cannot pass for having written nothing (WCONV-02, D-116-03)."
    - "A git-source record with a grown supported set is skipped and no clone is attempted, so a reload the user did not initiate cannot reach the network (WCONV-01 bound, NFR-5)."
    - "A record `applyPlan` disabled or uninstalled earlier in the same load is not re-materialized by the widened scan behind it (RECON-04)."
  prohibitions:
    - "No module-global counter, no `_setXForTest`-style seam, and no `t.mock.method` on a production module — CONVENTIONS.md forbids test-only module seams."
    - "No `times(0)` expectation anywhere: strong-mock reads it as no limit, so a zero written that way proves nothing. State zero by omitting the member."
    - "No new file under `tests/orchestrators/` — `scripts/check-corresponding-tests.mjs` pairs every unit test file 1:1 with a production module, and no new top-level `tests/` directory."
    - "No change to any file under `extensions/` in this plan, unless the measured-zero case genuinely cannot be built without the injected-seam fallback."
  artifacts:
    - tests/orchestrators/reconcile/backfill.test.ts
    - tests/integration/workflow-kind-inversion.test.ts
  key_links:
    - "`isRecordedButDisabled` (`backfill.ts:281`) -> the record never reaches `maybeBackfillPlugin` -> `loadMarketplaceManifest` is never called — the observable the measured zero reads"
    - "`resolveRecordedPluginOffline` passing no `resolveGitPluginRoot` -> `resolveStrict` answers `unavailable` for every git source -> `reinstallPlugin` is unreachable — the structural NFR-5 bound"
    - "`alreadyTouched` (`backfill.ts:213-218`, `:287`) -> the pre-`applyPlan` snapshot -> the record the widened scan would otherwise re-materialize over"
    - "`lastReconciledExtensionVersion` stamp (`backfill.ts:76`) + `supportedSetGrew` (`:454`) -> the two bounds WCONV-02 reuses unchanged"
---

<objective>
Pin what the widened scan must NOT do, and prove the self-heal fires once.

Purpose: the deletion in 116-01 opened the scan to every recorded plugin in both scopes.
Three things that were incidental under the narrow filter are now load-bearing: the
disabled-record refusal, the already-touched dedupe, and the structural absence of a
clone-cache resolver. Each is one line of production code with nothing standing behind
it. And the one-time bound is currently proved on bytes alone, which an atomic rewrite
of identical content satisfies.

Output: four cases that fail for four different reasons, each with a control run before
it is believed.
</objective>

<execution_ordering>
Wave 2, beside 116-03. The two plans share no file: this one owns
`tests/orchestrators/reconcile/backfill.test.ts` and
`tests/integration/workflow-kind-inversion.test.ts`; 116-03 owns the catalog pair, the
two `shared/notify*` modules and the closed-set lock test.

KNOWINGLY ACCEPTED: `workflow.use_worktrees` is false, so executors share one checkout.
A `npm test` verify here can redden with a diagnostic naming a file 116-03 is mid-editing.
That is the collision, not a finding — re-run at the wave boundary before treating it as
one. The per-task `node --test` verifies below name only this plan's own files for that
reason.
</execution_ordering>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-RESEARCH.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-PATTERNS.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-01-SUMMARY.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
</context>

<decisions_implemented>
| ID | What this plan does with it |
|---|---|
| D-116-03 | Splits criterion 3 into two cases that fail differently; asserts the disabled half as a measured zero; proves "wrote nothing" on bytes AND mtime; runs a control on every gate before believing it |
| D-116-07 | Extends `tests/integration/workflow-kind-inversion.test.ts` rather than opening a new integration file — it already carries the mtime harness and an existing negative control |
</decisions_implemented>

<tasks>

<task type="auto">
  <name>Task 1: The two boundaries the widening newly makes load-bearing</name>
  <files>tests/orchestrators/reconcile/backfill.test.ts</files>
  <read_first>
    - `tests/orchestrators/reconcile/backfill.test.ts:60-345` — the harness. In particular `createOfflineGitOps` and its `clonedUrls` accessor (`:65-70`), `writeMarketplaceSource` (`:208-234`) and the fact that it hard-codes a relative path source for every plugin entry, `pluginRecord`/`RecordSeed` (`:236-268`), `marketplaceRecord` (`:270-286`).
    - `tests/orchestrators/reconcile/backfill.test.ts:485-528` — the existing equal-set case, whose title names the filter 116-01 deleted.
    - `:1266-1381` — the two `RECON-04` cases, including the generic already-touched skip.
    - `:1486-1568` — the enabled-record promotion case, the shape both new cases mirror.
    - `:309` — an orphaned doc comment. Its subject (`seededScopeTree`, `:324`) was separated from it when `savedWorkflowEntries` was inserted between them. STATE.md records an inherited comment defect in this file and says to clean it here; re-measure what is actually there before fixing it, and record the correction if STATE's description does not match.
    - `extensions/pi-claude-marketplace/domain/resolver.ts:806-822` — the git-source arm and the note it produces when no clone-cache resolver was passed.
    - `116-RESEARCH.md` Q7/P1 and P2, and Measurement 4.
  </read_first>
  <behavior>
    - A record whose marketplace manifest entry names a git source (a `owner/repo` shorthand or an `https://…` URL) and whose supported set WOULD have grown is skipped: no outcome row, no failure reported, no envelope placed, and the counting git fake reports an empty clone-URL list.
    - A record that already appears in this scope's accumulated outcomes — the shape `applyPlan` leaves behind when it disabled that plugin earlier in the same load — is skipped by the widened scan even though the pre-`applyPlan` snapshot still records it enabled with a grown set. No second row is pushed and no envelope is placed.
    - The existing equal-set case still asserts what it asserted: a scanned record whose set did not grow is stamped and not re-materialized.
  </behavior>
  <action>
Add the git-source boundary case. Nothing in this suite covers a git-source record today,
and after the widening it is the only thing keeping a reload the user did not initiate off
the network. `writeMarketplaceSource` hard-codes a relative path source for every entry,
so this case needs a manifest whose entry carries a git-shaped source instead — either
add an optional per-plugin source override to that helper, or write the one manifest
inline in the case. Prefer the helper override: it is a parameter on a shared fixture
builder, which is the same dependency-injection shape the suite already uses for its git
and boundary fakes, and it keeps the manifest's other fields in one place.

Seed the record so the set WOULD grow if the record were reached — otherwise the case
passes for a record skipped by the growth test and pins nothing about the git bound.
Assert three things: no outcome was pushed, the scan reported no failure (a git source is
a benign skip, not a failure that keeps the gate open), and `clonedUrls()` is deeply
equal to an empty array. The clone assertion is the NFR-5 half and the one the suite has
never had.

Add the disable-shaped already-touched case. Build it as the promotion case plus one
pre-seeded outcome in the accumulated array standing for a disable `applyPlan` performed
earlier in the same load, with the snapshot still recording the plugin enabled and its set
grown. Assert no second row and no envelope. The existing generic already-touched case
covers the mechanism; this one covers the consequence the widening introduces — that
without the dedupe the scan would re-materialize a plugin the user just disabled, and
reinstall writes `enabled: true` unconditionally.

Retitle the existing equal-set case. Its body is right and stays byte-unchanged; only its
title names the deleted filter. Give it a title stating what it actually proves — that a
gate-open scope with nothing to promote is stamped — and keep its requirement-ID citation.

Fix the inherited comment defect this file carries. Measure it first: read the lines
around the orphaned doc comment and compare against what STATE.md describes. If the
defect is the orphan, move the comment back onto the function it describes. If STATE's
description turns out to name something else, fix what is actually there and record the
correction in the SUMMARY — a stale inherited claim gets corrected, not carried.
  </action>
  <acceptance_criteria>
    - A case seeds a git-source manifest entry with a would-grow set and asserts: zero outcomes, no failure reported, and an empty clone-URL list.
    - A case seeds a pre-`applyPlan` disable outcome against an enabled, grown-set snapshot and asserts no second row and no placed envelope.
    - The equal-set case's title no longer names the deleted filter, and its body is byte-unchanged.
    - The orphaned doc comment sits directly above the function it describes, and the SUMMARY records what was actually measured at that site versus what STATE.md claimed.
    - `node --test tests/orchestrators/reconcile/backfill.test.ts` reports no failing tests.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/backfill.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit — `tsc`'s own status separates a clean run from a type error and from a compiler or tsconfig load failure</fails_when>
  </verify>
  <done>A git-source plugin cannot pull the network into a reload nobody asked for, and the scan cannot undo a disable `applyPlan` just applied — and both facts are now assertions rather than properties.</done>
</task>

<task type="auto">
  <name>Task 2: "Never scanned" as a measured zero, with the control that proves the counter is wired</name>
  <files>tests/orchestrators/reconcile/backfill.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts:275-300` — the disabled filter's position relative to the try block, and the per-plugin catch that turns a manifest throw into a failure row that keeps the gate open.
    - `extensions/pi-claude-marketplace/domain/manifest-cache.ts:1-20` — why a `stat` failure is a pure cache miss, so the cache cannot mask a deleted manifest.
    - `tests/orchestrators/reconcile/backfill.test.ts:1382-1424` and `:1425-1485` — the two existing disabled-record cases, which infer "not scanned" from a missing row.
    - `:1486-1568` — the enabled twin.
    - `:1773-1823` — the manifest-parse failure case, which proves the poison IS visible when the record is scanned.
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts:250-270` — `ApplyReconcileOptions.gitOps`, the house dependency-injection shape, for the fallback below.
    - `116-RESEARCH.md` Q6 (both options, and why neither is a module seam).
  </read_first>
  <behavior>
    - A disabled record whose supported set would grow, seeded against a marketplace whose cached manifest has been made unreadable, produces zero outcomes AND the scope's version stamp still lands — a two-sided observation, because a record that WAS scanned would throw on the manifest read, surface a failure row and keep the gate open.
    - Its enabled twin, identical in every respect but the disabled flag, surfaces the failure row and leaves the gate open — so the poison is proven visible.
  </behavior>
  <action>
Build the measured zero with the manifest-absence probe, which needs no production change.
Seed a disabled record whose set would grow, then remove or corrupt the cached
marketplace manifest so that any read of it throws. Assert BOTH halves: the outcome array
is empty, and the version stamp landed. The stamp is what makes this a positive fact
rather than an absence — a scanned record would have thrown, the per-plugin catch would
have pushed a failure row, and the caller would have skipped the stamp to keep the gate
open.

Pair it with the enabled twin in the same shape, so the poison's visibility is proved
rather than assumed. Name the pair for the requirement and for the decision that asks for
a measured zero.

State the limit honestly in the case's own comment and in the SUMMARY: this measures
that the marketplace manifest was never read, which on this code path is the first
statement of the resolve. It is one step downstream of "the resolver was never called".

If — and only if — the manifest probe cannot be made to discriminate, fall back to
injecting a counting collaborator through `ApplyReconcileOptions`, mirroring the existing
optional git-ops field: one optional readonly field with a documented production default,
threaded through the options object that is already the first parameter of every backfill
function. Update the `satisfies ApplyReconcileOptions` fixtures in
`tests/orchestrators/reconcile/types.test.ts` if you take that route, and say in the
SUMMARY why the probe was insufficient. Do not reach for a module-global counter or a
mock patched onto a production module: CONVENTIONS.md names dependency injection as the
house pattern precisely so a test cannot punch a hole in a module's internals.

Run the control before believing the case. Remove the disabled-record filter from
`backfillOnePluginIsolated`, run the new case alone, and confirm the observation moves off
zero — the record is now scanned, the poisoned manifest throws, a failure row appears and
the stamp does not land. Capture the transcript, restore the filter, confirm green. A case
that stays green with that filter removed is not wired to the scan, and this milestone has
shipped three guards that were green because they checked nothing.
  </action>
  <acceptance_criteria>
    - A case asserts, for a disabled grown-set record against an unreadable cached manifest: zero outcomes AND a landed version stamp.
    - Its enabled twin asserts the failure row and the un-landed stamp, proving the poison visible.
    - No module-global counter, no mock on a production module, and no `times(0)` anywhere in the new cases.
    - The control was run: with the disabled filter removed the observation moves off zero and the case is RED; the failing transcript is in the SUMMARY.
    - The case comment and the SUMMARY both state that the observable is the manifest read, one step downstream of the resolver call.
    - `node --test tests/orchestrators/reconcile/backfill.test.ts` reports no failing tests.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/backfill.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit — the fallback seam was added without updating the options fixtures, or a type drifted</fails_when>
  </verify>
  <done>"A disabled record is never scanned" is a number the suite measured, and removing the filter that makes it true moves that number.</done>
</task>

<task type="auto">
  <name>Task 3: One-time, proved on bytes, inode and mtime, across two real loads</name>
  <files>tests/orchestrators/reconcile/backfill.test.ts, tests/integration/workflow-kind-inversion.test.ts</files>
  <read_first>
    - `tests/orchestrators/reconcile/backfill.test.ts:443-484` — the existing byte-only "state.json unchanged" case. This is the one to strengthen.
    - `tests/orchestrators/marketplace/autoupdate.test.ts:118-136` — the triple-snapshot helper shape (bytes, inode, mtime in nanoseconds, compared with one deep equality).
    - `tests/integration/workflow-kind-inversion.test.ts:215-241` — the comment explaining why the mtime harness backdates, and the two helpers.
    - `:243-289` — the two-consecutive-reconciles case to mirror.
    - `:290-361` — its existing negative control, the forced-open gate that DOES rewrite.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:1530-1570` — what a promotion preserves and what it rewrites. The updated-at field moves on the FIRST load, legitimately.
    - `116-RESEARCH.md` Q5 and P3.
  </read_first>
  <behavior>
    - Over an already-converged scope, a second load re-materializes nothing: `state.json` is identical in bytes, inode and mtime to its state immediately after the first load.
    - The first load is expected to change `state.json` — the promotion rewrites the compatibility block, the resources and the updated-at stamp — so the snapshot that matters is taken AFTER the first load, not before it.
    - The integration twin drives the real reconcile path twice over a record seeded `installable: true, supported: ["skills"], unsupported: []` — the population whose kind was invisible rather than unsupported — and its workflow envelope's mtime is unchanged by the second load.
  </behavior>
  <action>
Strengthen the existing "state.json unchanged" case in the unit suite from byte equality
to the triple snapshot. Byte equality alone passes an implementation that atomically
rewrites identical content, which is a green run that checked nothing; the inode catches
exactly that rewrite. Borrow the helper shape already used in three suites rather than
inventing a fourth — read bytes and a bigint stat together, return one frozen object, and
compare with a single deep equality so a drift on any axis prints all three.

Then extend the integration file with the WCONV-01/02 twin, per D-116-07. Do not open a
new integration file: the existing one already carries the backdate helpers, the two-load
driver and a negative control that proves the "changed" side of the same invariant. Seed
the record on the population this phase exists for — installable, skills recorded, nothing
unsupported — against a source tree that also carries a workflow. Drive the reconcile
twice. Snapshot AFTER the first load and assert equality after the second. Assert the
first load DID converge, too: a case that only asserts the second load changed nothing
would pass over a scan that never ran at all.

Use the file's own backdate helpers if the two loads land in the same tick; millisecond
timer resolution between two operations one line apart is the reason those helpers exist.

Run the control before believing it. Force the version stamp stale before the second load
and confirm the snapshot differs — that is the "changed" direction, and without it the
equality assertion is satisfied by an implementation that never scans. Capture the
transcript, restore, confirm green.
  </action>
  <acceptance_criteria>
    - The unit case compares a bytes/inode/mtime triple with one deep equality, and no longer relies on byte equality alone.
    - The integration file carries a case that drives the reconcile twice over an installable, grown-set, workflow-bearing record, asserts the first load converged, and asserts the post-first-load snapshot survives the second load.
    - No new integration file was created.
    - The control was run: forcing the stamp stale before the second load makes the snapshot differ; the transcript is in the SUMMARY.
    - `node --test tests/orchestrators/reconcile/backfill.test.ts` and `npm run test:integration` both report no failing tests.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/backfill.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
    <automated>npm run test:integration</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts</automated>
    <fails_when>non-zero exit, or the pair reports under 100% on function, line or branch coverage — the widened scan added reachable arms and the owner suite is the only thing that covers them</fails_when>
  </verify>
  <done>A second load over a converged scope is provably a no-op on every axis a rewrite could move, and a run with a stale stamp proves the assertion can fail.</done>
</task>

</tasks>

<house_gates>
- Comments and test titles cite durable spec IDs only — `WCONV-NN`, `D-116-NN`, `RECON-04`, `ENBL-08`, `NFR-5`. Never a phase, plan, wave or pitfall number.
- Test doubles are injected as arguments, never patched onto a module. Shared doubles live beside the layer they double.
- fallow applies its own complexity ceilings independently of ESLint's and the two disagree; a green lint run is not evidence fallow will pass. 116-04 runs the full chain.
- **Commit discipline in this worktree.** `.git` is a file, so trufflehog's git-mode hook aborts structurally. Run `trufflehog filesystem <paths being committed> --results=verified,unknown --fail` first and only then commit with `SKIP=trufflehog`. Never `--no-verify`, never amend, never `git add -A`. Run `pre-commit run --files <paths>` clean BEFORE committing, and check `git status` afterwards because the prettier hook writes.
</house_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted `state.json` snapshot -> the widened scan | a record written earlier, possibly by another process, decides what gets re-materialized with no user present |
| marketplace manifest entry -> `resolveStrict` | the entry's source field decides whether a clone is attempted |
| the scan -> `~/.pi/workflows/` and the scope roots | executable envelopes are written outside any command the user typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-01 | Elevation of privilege | `isRecordedButDisabled` filter (`backfill.ts:281`) | high | mitigate | Task 2 asserts the filter's effect as a measured zero and runs the control that proves the observation moves when the filter is removed; reinstall's own refusal stays the second layer |
| T-116-03 | Information disclosure | `resolveRecordedPluginOffline` -> `reinstallPlugin` | high | mitigate | Task 1 adds the first case in this suite covering a git-source record: zero outcomes, no failure, empty clone-URL list. The absence of a clone-cache resolver becomes a gate rather than an accident |
| T-116-08 | Tampering | `alreadyTouched` dedupe (`backfill.ts:213-218`, `:287`) | high | mitigate | Task 1 adds the disable-shaped case: a record `applyPlan` disabled in the same load is not re-materialized behind it by the widened scan, which would otherwise restore `enabled: true` unconditionally |
| T-116-04 | Tampering | executable envelopes under `~/.pi/workflows/` | medium | mitigate | Task 3 proves the strict-superset test and the version stamp bound re-materialization to one load, on bytes, inode and mtime, with a stale-stamp control proving the assertion can fail |
| T-116-05 | Denial of service | per-plugin fault isolation | low | accept | Pre-existing and already covered by the healthy-sibling case in this suite; Task 2's poison exercises the same isolation from the other side |
| T-116-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds, upgrades and removes no package; there is no install task for a legitimacy gate to precede. Any package addition would invalidate this acceptance |
</threat_model>

<verification>
- `node --test tests/orchestrators/reconcile/backfill.test.ts` — no failing tests
- `npm run test:integration` — no failing tests
- `npm run typecheck` — exit 0
- `node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts` — 100% function, line and branch, run alone
- Two negative-control transcripts in the SUMMARY: the disabled filter removed, and the stamp forced stale before the second load
</verification>

<success_criteria>
- A git-source record with a grown set is skipped with an empty clone-URL list, and no other case in this suite covered that before.
- A record `applyPlan` disabled in the same load is not re-materialized behind it.
- "A disabled record is never scanned" is a measured zero whose control moves it off zero.
- A second load over a converged scope leaves `state.json` identical in bytes, inode and mtime, and a stale-stamp run proves the assertion can fail.
- No production file changed, unless the injected-seam fallback was needed and the SUMMARY says why.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-02-SUMMARY.md` when done.

The SUMMARY must carry the two failing transcripts verbatim, the honest statement of what
the measured zero actually observes and how far downstream of the resolver call that is,
and the result of re-measuring STATE.md's inherited comment-defect claim in this file.
</output>
