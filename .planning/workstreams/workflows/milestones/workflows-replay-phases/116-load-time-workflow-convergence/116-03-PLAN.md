---
phase: 116-load-time-workflow-convergence
plan: 03
type: execute
wave: 2
depends_on: ["116-01"]
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - tests/architecture/notify-closed-set-locks.test.ts
autonomous: true
requirements: [WCONV-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The fully-promoted arm of a load-time backfill has a published catalog state for the first time, byte-paired against the renderer, so criterion 4's pairing covers the arm the phase exists to make visible (WCONV-03)."
    - "A second published state pins the composed order when the host workflow engine is absent: the convergence token first, the soft-dep marker last, in one brace — the real population's likely first render."
    - "The corpus-count lock, its explanatory comment and its failure message all move together, once, to the new count (D-116-06)."
    - "Changing the token's rendered bytes without touching the closed set turns the catalog test red, so membership and rendering are two separate gates and not one."
    - "The three amendment sites that stay silently green — the lock test's own title and the two prose counts — are corrected by inspection, and the inspection is recorded."
  prohibitions:
    - "No edit to the two pre-existing backfill catalog states or their fixtures: 116-01 already re-byted them, and a second edit here would make one row's truth depend on two plans."
    - "No `docs/` markdown formatted with prettier. `mdformat` through pre-commit is the project's markdown formatter."
    - "No new numbered table row anywhere in `docs/output-catalog.md`."
  artifacts:
    - docs/output-catalog.md
    - tests/architecture/catalog-uat.test.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - extensions/pi-claude-marketplace/shared/notify-reasons.ts
    - tests/architecture/notify-closed-set-locks.test.ts
  key_links:
    - "`<!-- catalog-state: NAME -->` annotation + the next fenced block in `docs/output-catalog.md` <-> `FIXTURES[\"reconcile-applied-cascade\"][NAME]` in `tests/architecture/catalog-uat.test.ts` — forward walk and inverse walk, both directions gated"
    - "the corpus-count assertion in `catalog-uat.test.ts` -> runs BEFORE any byte comparison, so a new state fails there first"
    - "`composeReasons` (`shared/notify.ts`) -> caller reasons in order, soft-dep markers appended last -> the brace order the second new state pins"
---

<objective>
Publish the bytes a converged plugin's row actually renders, and retire the counts that
were left lying because nothing reads them.

Purpose: a reason token can be a legal member of the closed set and never reach a
rendered row. Criterion 4 asks for the byte pairing precisely because membership alone is
half a gate. And three sites carrying the old count stay green whatever happens to the
tuple — the lock test's own title and two prose paragraphs — so they rot silently unless
someone looks.

Output: two new published states for the arm that had none, one corpus-count move, and
three corrected counts with the inspection recorded.
</objective>

<execution_ordering>
Wave 2, beside 116-02. The two plans share no file: 116-02 owns the reconcile backfill
unit suite and the integration file; this plan owns the catalog pair, the two
`shared/notify*` modules and the closed-set lock test.

Both of this plan's `shared/notify*` edits are COMMENT-ONLY. The tuple and the typed
partition were settled in 116-01 and are not touched again here.

KNOWINGLY ACCEPTED: `workflow.use_worktrees` is false, so a broad verify can redden with
a diagnostic naming a file 116-02 is mid-editing. Re-run at the wave boundary before
treating that as a finding. The verifies below name this plan's own files.
</execution_ordering>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-RESEARCH.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-PATTERNS.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-01-SUMMARY.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-04-SUMMARY.md
</context>

<decisions_implemented>
| ID | What this plan does with it |
|---|---|
| D-116-06 | Adds exactly two new catalog states and moves the corpus lock by two, in one edit that carries the literal, its comment and its failure message together |
| D-116-02 | Publishes the `installed` arm the token was added for — the arm with no published state at all today |
</decisions_implemented>

<artifacts_produced>
| Artifact | Kind | Purpose |
|---|---|---|
| a catalog state for a fully-promoted backfill row | published output contract | the bytes of the arm this phase exists to make visible |
| a catalog state for the same row in a session with no host workflow engine | published output contract | pins the composed brace order: convergence token first, soft-dep marker last |
| two matching `FIXTURES` entries under the reconcile cascade section key | test fixtures | the message data the byte gate drives the renderer with |

State names are Claude's Discretion; follow the naming shape of the two states already in
that section. No new production symbol.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Publish the fully-promoted arm, twice, and move the corpus lock once</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - `docs/output-catalog.md:2336` — the reconcile cascade H2 that scopes the section key, and the states already published under it.
    - `docs/output-catalog.md:2425-2455` as left by 116-01 — the two backfill states, their H3 headings, their prose paragraphs, their annotations and their fenced blocks. These are the shape to mirror and they must NOT be edited again here.
    - `docs/output-catalog.md:69` — the paragraph stating that soft-dep markers live inside the same brace as the typed reasons, are emitted by the renderer from the plugin's dependencies field and the host probe, and are appended rather than interleaved. This is what the second new state pins.
    - `docs/output-catalog.md:525-550` — two existing states rendering the host-engine marker; read their fixtures to see which dependencies field value makes that marker fire.
    - `tests/architecture/catalog-uat.test.ts:1-60` — the header: the scope gate, the fixture shape, and the binding user-contract claim.
    - `:200-250` — the four host-probe factories, including the one reporting the workflow engine absent.
    - `:5351-5407` — the two existing backfill fixtures and their comments.
    - `:5490-5530` — `checkCatalogExample`: the missing-fixture failure, the exactly-one-notify assertion, the byte comparison and the severity check.
    - `:5598-5615` — the corpus-count assertion, its explanatory comment and its failure message. All three carry the number.
    - `:5740-5760` — the inverse walk that catches an orphan fixture.
    - `115-04-SUMMARY.md` — the worked example of adding a state and moving this lock, including the fact that the lock was not in that plan's reading list and was discovered by failing on it.
    - `CLAUDE.md`: markdown is formatted by `mdformat` through pre-commit, never by prettier.
  </read_first>
  <behavior>
    - A load-time backfill that fully promoted a record renders a bare marketplace header, an installed plugin row carrying the convergence token in its brace, and a one-success tally — and those bytes are published.
    - The same row in a session where the host workflow engine is not loaded renders both tokens in one brace, the convergence token first and the engine marker last, because the renderer appends soft-dep markers after the caller's reasons.
    - Severity stays info on both: a benign promotion is not a degradation.
    - The corpus-count assertion reports the new count, and its comment and failure message name the same number.
  </behavior>
  <action>
Add two catalog states under the reconcile cascade H2, beside the two backfill states
already there. Each is four things in the doc — an H3 heading in the same form as its
neighbours, one prose paragraph, the annotation comment, then the fenced expected block —
plus one `FIXTURES` entry keyed by the same section-and-state tuple in the test.

The first state is the fully-promoted arm: a bare marketplace header, an installed plugin
row whose brace holds the convergence token alone, and the one-success tally the
neighbours show. Drive it with the probe factory that reports every companion loaded, so
no soft-dep marker fires and the state isolates the token.

The second state is the same row in a session with no host workflow engine, driven by the
probe factory that reports exactly that. Its fixture's dependencies field must be the
value that makes the engine marker fire — read it off one of the existing states that
renders that marker rather than guessing. This state is not decoration: it is the real
population's likely first render, and it is the only place the composed order of a
caller-placed reason against a renderer-appended marker is pinned.

Write each prose paragraph to say what a reader needs and nothing more: what happened
(the extension's supported set moved, so a recorded plugin was re-materialized at load
time without the user running anything), why the row carries the token (so new commands
after an uninitiated reload can be attributed), and, for the second, why the two tokens
sit in that order.

The fenced bytes must equal what the renderer produces. Derive them by running the gate,
not by transcription — a paraphrase is a red run, not a soft mismatch.

Move the corpus-count lock by two, in one edit that carries all three of its parts: the
literal in the assertion, the explanatory comment above it, and the failure message
string. D-116-06 fixed the count at two new states precisely so this moves once. Note the
count assertion runs BEFORE any byte comparison, so a forgotten count fails there first
with a message about the corpus size rather than about your new state.

Do not touch the two pre-existing backfill states or their fixtures. 116-01 re-byted
them; editing them again here would split one row's truth across two plans.

Format the doc through `pre-commit run --files docs/output-catalog.md` — never prettier —
then re-run the byte gate, because a reflow inside a fenced block is a red run.

Prove each new state in THREE directions, not two, which is 115-04's own finding: remove
the annotation and confirm the corpus count and the inverse walk fail; remove the fixture
and confirm the forward walk reports a missing fixture; change one byte in the fenced
block and confirm a byte mismatch. Restore after each. This third direction is the
control criterion 4 specifically asks for — if changing the rendered bytes does NOT turn
this test red, the pairing checks membership only and must be strengthened before the
plan closes. Paste all three transcripts into the SUMMARY.
  </action>
  <acceptance_criteria>
    - Two new states exist under the reconcile cascade H2, each with H3, prose, annotation, fenced block and a matching fixture.
    - One state renders the convergence token alone; the other renders it first with the host-engine marker last, in one brace.
    - The corpus-count literal, its comment and its failure message all read the new count.
    - The two pre-existing backfill states and fixtures are byte-unchanged by this plan.
    - Three control transcripts are in the SUMMARY: annotation removed, fixture removed, one byte changed. The byte-change control turned the test RED.
    - `node --test tests/architecture/catalog-uat.test.ts` reports no failing tests.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test — including the corpus-count assertion, which fires before any byte comparison</fails_when>
    <automated>pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or a hook reports it modified a file — a formatter that reflowed a fenced block invalidates the byte pairing, so re-run the catalog test after any rewrite</fails_when>
  </verify>
  <done>The arm this phase exists to make visible has published bytes for the first time, and changing those bytes is a red run.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the three counts that no gate reads, and record that they were checked</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/notify-reasons.ts, tests/architecture/notify-closed-set-locks.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/shared/notify.ts:82-84` — the sentence stating the tuple's membership count and order as catalog-stable.
    - `extensions/pi-claude-marketplace/shared/notify-reasons.ts:5-14` — two sentences carrying the same count, one in the module's opening paragraph and one describing the flat set a command module would otherwise reference.
    - `extensions/pi-claude-marketplace/shared/notify-reasons.ts:15-30` — the paragraph claiming those sentences cannot drift from the tuple again without a red test. Read it carefully: the claim is true of the tuple's length and enumeration, and false of the prose, which is what makes this task necessary.
    - `tests/architecture/notify-closed-set-locks.test.ts:29` — the test's own title string, which states a count and is read by nothing.
    - `116-RESEARCH.md` Q3, the "stays silently green" table.
    - `116-VALIDATION.md`, the paragraph naming the three sites and requiring the inspection be recorded.
  </read_first>
  <action>
Fix the three counts that no gate reads. Each is a number in prose that was correct before
116-01 appended a member and is wrong now, and none of them will ever go red on its own.

- The lock test's own title states a count. Correct it. The assertion beside it is already
  right; only the name lies about what it locks.
- The tuple's own doc paragraph states the count as part of its catalog-stability claim.
  Correct it.
- The typed-view module states the same count twice. Correct both.

While you are in the typed-view module's header, look at the paragraph asserting that
those sentences cannot drift from the tuple again without a red test. That claim is about
the tuple's length and its enumeration, not about the prose — which is exactly why this
task exists. Amend it to say what is actually true: the length and the enumeration are
gated; a count written in a sentence is not, and moving one is part of the same change
that grows the set. Do not narrate the correction or the old number — the comment rules
forbid describing code and text that no longer exist.

These are comment-only edits. Nothing in this task changes a tuple, a type or an
assertion, so nothing here can redden a test on its own; that is the whole problem and the
reason the check is by inspection.

Record the inspection in the SUMMARY as a checklist naming each of the three sites, the
value found and the value written. "They were checked" is not the record; the values are.

Then confirm the whole set once, deliberately: search the repository for the retired count
appearing next to the words that make it a claim about this tuple, and confirm the only
remaining occurrences are historical ledger entries describing past transitions, which are
correct as written and must not be rewritten. Report that count in the SUMMARY alongside
the three corrections — the milestone's recurring defect is an enumeration shorter than
the set it names, and three is the number the research measured, not a number anyone
counted by reading.
  </action>
  <acceptance_criteria>
    - The lock test's title, the tuple's doc paragraph and both sentences in the typed-view module state the current count.
    - The typed-view module's drift claim is amended to distinguish what is gated from what is not, without narrating the old text.
    - The SUMMARY carries a per-site checklist with the value found and the value written for each.
    - The SUMMARY reports the repository-wide search for other occurrences and states which surviving ones are historical ledger entries left correct on purpose.
    - No tuple, type or assertion changed in this task.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit — a comment-only edit broke a block comment or a doc-comment attachment</fails_when>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts</automated>
    <fails_when>non-zero exit, or the run reports any failing test — the locks must still pass, since nothing they assert was touched</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit — the shared notify modules are the two largest in the extension and carry per-block rule exemptions that a careless comment edit can dislodge</fails_when>
  </verify>
  <done>Every sentence in the codebase that states how large the closed reason set is agrees with the tuple, and the SUMMARY records the value found at each of the three ungated sites.</done>
</task>

</tasks>

<house_gates>
- Comments cite durable spec IDs only — `WCONV-NN`, `D-116-NN`, `OUT-08`, `COMPAT-01`. Never a phase, plan, wave or pitfall number.
- Markdown under `docs/` is formatted by `mdformat` through pre-commit. `prettier --write` on a markdown file is always wrong here and the byte gate is what it breaks.
- fallow's complexity ceilings are computed independently of ESLint's and the two disagree. This plan is comment- and fixture-only, so it has no complexity exposure; 116-04 runs the full chain.
- **Commit discipline in this worktree.** `.git` is a file, so trufflehog's git-mode hook aborts structurally. Run `trufflehog filesystem <paths being committed> --results=verified,unknown --fail` first and only then commit with `SKIP=trufflehog`. Never `--no-verify`, never amend, never `git add -A`. Run `pre-commit run --files <paths>` clean BEFORE committing, and check `git status` afterwards because the prettier and mdformat hooks write.
</house_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered notification bytes -> the user's terminal | the only channel telling a user what a reload did to their machine |
| `docs/output-catalog.md` -> the byte gate | the published contract and the test that enforces it are two artifacts that can disagree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-07 | Repudiation | the fully-promoted backfill row | medium | mitigate | Task 1 publishes the arm's bytes for the first time and pairs them against the renderer in both directions, so a convergence can no longer render byte-identically to a fresh install without a red test |
| T-116-09 | Tampering | the doc-to-fixture byte pairing | low | mitigate | Both walks are exercised by control: annotation removed reddens the corpus count and the inverse walk; fixture removed reddens the forward walk; one byte changed reddens the comparison |
| T-116-11 | Repudiation | ungated prose counts in the notification modules | low | mitigate | Task 2 corrects all three by inspection and records the value found at each, because no gate reads them and none ever goes red |
| T-116-SC | Tampering | npm/pip/cargo installs | high | accept | This plan adds, upgrades and removes no package; there is no install task for a legitimacy gate to precede. Any package addition would invalidate this acceptance |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts` — no failing tests
- `npm run typecheck` and `npm run lint` — exit 0
- `pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts` — clean, with the byte gate re-run afterwards
- Three catalog control transcripts in the SUMMARY, one of them the byte-change control criterion 4 requires
- A per-site checklist in the SUMMARY for the three ungated counts

</verification>

<success_criteria>
- The fully-promoted backfill arm has a published catalog state, and a second state pins the composed brace order against an absent host engine.
- Changing the token's rendered bytes without touching the closed set turns the catalog test red, and the transcript proves it.
- The corpus-count literal, comment and failure message moved together, once.
- All three ungated counts agree with the tuple, and the SUMMARY records the value found at each.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-03-SUMMARY.md` when done.

The SUMMARY must carry the three control transcripts verbatim, the per-site checklist for
the ungated counts, and the result of the repository-wide search for other occurrences of
the retired count.
</output>
