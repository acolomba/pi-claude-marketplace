---
phase: 116-load-time-workflow-convergence
plan: 04
type: execute
wave: 3
depends_on: ["116-02", "116-03"]
files_modified:
  - .planning/workstreams/workflows/REQUIREMENTS.md
  - .planning/workstreams/workflows/ROADMAP.md
  - .planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-CONTEXT.md
autonomous: true
requirements: [WCONV-01, WCONV-02, WCONV-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The population sentence WCONV-01 carries states only what was measured. The widened scan can promote path-source records and nothing else, so an unqualified claim about which side two named upstream plugins land on is either measured or replaced with the bound that is true regardless."
    - "The correction lands at every site the claim appears — the requirement, the roadmap phase description, and the phase context — not just the one that is easiest to reach."
    - "The full project gate is green: typecheck, lint, fallow, formatting, the two pairing meta-gates, the unit suite and the integration suite."
    - "The owner module's per-pair coverage is 100% on function, line and branch when run alone, because the widened scan added reachable arms."
    - "The four negative controls this phase owes are all recorded with their failing transcripts, and the three ungated amendment sites are recorded with the value found at each."
  prohibitions:
    - "No guess about the two named plugins' source kind. An unmeasured answer is replaced with the bound that holds either way, never with a plausible one."
    - "No requirement checkbox flipped and no roadmap phase marked complete here — verification owns that."
    - "No re-run of `npm run check` as a spot-check. It runs once, in this plan, as the phase gate."
  artifacts:
    - .planning/workstreams/workflows/REQUIREMENTS.md
    - .planning/workstreams/workflows/ROADMAP.md
    - .planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-CONTEXT.md
  key_links:
    - "`resolveRecordedPluginOffline` passing no clone-cache resolver -> every git source resolves unavailable -> the scan's population is path-source records only. This is the fact the corrected prose must carry"
    - "`npm run check` -> typecheck, lint, fallow, format:check, both pairing meta-gates, the unit suite, the integration suite — fallow is a member of that chain, not an extra"
---

<objective>
Stop carrying an inherited claim on inheritance, and take the phase through its gate.

Purpose: the phase's own requirement text names a concrete population — two
Anthropic-authored workflow plugins — and asserts which side of the deleted filter they
land on. The research then measured that the widened scan can only ever promote
path-source records, because the offline resolve is handed no clone-cache resolver. If
those two plugins are declared as git sources, the population sentence is wrong even
though every success criterion of this phase stays satisfiable. That is this milestone's
"assume the enumeration is short until measured" rule applied to a population.

Output: a corrected or confirmed population claim at all three sites, a green full gate,
and one place where the phase's controls, risks and out-of-scope inheritances are all
written down.
</objective>

<execution_ordering>
Wave 3. Both wave-2 plans are complete before this runs, which is what makes the single
`npm run check` in Task 2 meaningful: it is the first run over the whole phase's tree.

The two tasks are ordered. Task 1 edits planning prose only and can be committed on its
own; Task 2's gate covers the source tree the earlier plans left.
</execution_ordering>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-RESEARCH.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-VALIDATION.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-01-SUMMARY.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-02-SUMMARY.md
@.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the population claim and correct it wherever it is stated</name>
  <files>.planning/workstreams/workflows/REQUIREMENTS.md, .planning/workstreams/workflows/ROADMAP.md, .planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-CONTEXT.md</files>
  <read_first>
    - `.planning/workstreams/workflows/REQUIREMENTS.md:55` — the WCONV-01 text and its trailing clause about the two named plugins.
    - `.planning/workstreams/workflows/ROADMAP.md:70-80` — the phase-group description carrying the same clause.
    - `.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-CONTEXT.md:222-230` — the specifics section stating the population as concrete, and `:158-166` — the paragraph that already flags this claim as unmeasured and instructs the plan to re-measure it.
    - `116-RESEARCH.md` Q7/P1, Measurement 4, and the Assumptions Log row rating this claim MEDIUM because it was inherited rather than re-measured.
    - `extensions/pi-claude-marketplace/domain/source.ts` — the source kinds a marketplace manifest entry can declare, so "path source" is stated in the vocabulary the code uses.
  </read_first>
  <behavior>
    - Every site stating which side of the retired filter the two named plugins land on states either a measured answer with its evidence, or the bound that holds regardless of the answer.
    - The bound is stated in the code's own vocabulary: the offline resolve is handed no clone-cache resolver, so a git-source record resolves unavailable and only path-source records converge through this scan.
    - No site claims more than was measured, and no site was left on the old wording because it was harder to reach.
  </behavior>
  <action>
Measure first. Locate the marketplace manifest that declares the two Anthropic-authored
workflow plugins and read the `source` field of each entry. A relative directory path
under the marketplace clone is a path source and converges through this scan; a repository
shorthand or a URL is a git source and does not. Record the manifest you read, the two
values, and the date.

If the manifest cannot be reached — no network, or the marketplace cannot be located from
this tree — do NOT guess and do NOT carry the inherited wording. Replace the unqualified
claim with the bound that is true either way: this scan converges path-source records
only, because the offline resolve passes no clone-cache resolver, and whether those two
plugins are declared as path or git sources was not measured. State the reason it was not
measured. A sentence that says less and is true beats a sentence that says more and might
not be.

Then correct all three sites. The requirement text, the roadmap phase description and the
phase context each carry the claim, and the context's own paragraph already instructs
that it be re-measured rather than inherited — so leaving any one of them is leaving the
claim alive.

In the requirement and the roadmap, write the corrected sentence outright. Do not quote
the retired wording there for contrast: those two documents are the current statement of
what is required, not a record of how it changed, and a quoted retired claim reads as a
live one to the next person who greps.

In the phase context, the paragraph that flagged this as unmeasured is the right place to
record the outcome — append the measurement, its evidence and its date beneath it. That
file is a record of the discussion, so it may carry the history the other two must not.

Say plainly, at whichever site names the requirement's scope, that every success
criterion of this phase stays satisfiable regardless of the answer. The correction is
about the truth of a sentence, not about the phase's reach.
  </action>
  <acceptance_criteria>
    - The two source-kind values were measured and recorded with the manifest read and the date, OR the SUMMARY states why the measurement could not be made and the prose carries the path-source bound instead.
    - The requirement text and the roadmap phase description state the corrected claim and do not quote the retired one.
    - The phase context records the measurement beneath the paragraph that flagged it.
    - Every site states the bound in the code's vocabulary: no clone-cache resolver on the offline path, therefore path-source records only.
    - Neither document flips a requirement checkbox or marks a phase complete.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -v '^>' .planning/workstreams/workflows/REQUIREMENTS.md .planning/workstreams/workflows/ROADMAP.md | grep -c 'land on that side')" = "0"</automated>
    <fails_when>the retired unqualified clause survives in either document. Blockquote lines are filtered so a legitimately quoted historical line in a callout cannot invalidate the gate; a live sentence outside a quote is exactly what must be gone. The phase context is deliberately NOT gated here — it is the one file allowed to record the history</fails_when>
    <automated>test "$(grep -c 'clone-cache resolver' .planning/workstreams/workflows/REQUIREMENTS.md)" != "0"</automated>
    <fails_when>the requirement no longer states the scan's real bound. A correction that only deletes the false clause leaves the reader with less than the measurement produced</fails_when>
    <automated>pre-commit run --files .planning/workstreams/workflows/REQUIREMENTS.md .planning/workstreams/workflows/ROADMAP.md .planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-CONTEXT.md</automated>
    <fails_when>non-zero exit, or a hook reports it modified a file — markdown here is formatted by mdformat, and an unformatted planning doc fails the repository-wide lint job later</fails_when>
  </verify>
  <done>Nobody reading WCONV-01 next month inherits a claim about a live upstream marketplace that this phase never checked.</done>
</task>

<task type="auto">
  <name>Task 2: Take the phase through the full gate and write down what it leaves open</name>
  <files>.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-04-SUMMARY.md</files>
  <precondition>The worktree has its own installed `node_modules` — a freshly created worktree has none and cannot run `npm run check` at all.</precondition>
  <read_first>
    - `package.json` scripts — what `check` actually chains, in order, and that fallow is a member of it.
    - `116-VALIDATION.md` — the four negative controls, the three silently-green amendment sites, and the sign-off checklist.
    - The three earlier SUMMARYs in this phase directory — to confirm every control transcript is actually present rather than described.
    - `116-RESEARCH.md` Q7/P4, P5 and P7 — the three pre-existing behaviours the widening makes newly likely, and the reasoning for leaving each out of scope.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:40-52` — the note that the backfill caller renders neither half of the discovery warnings, and the backlog item that tracks it.
  </read_first>
  <action>
Run the full gate once. `npm run check` chains typecheck, lint, fallow, the formatting
check, both pairing meta-gates, the unit suite and the integration suite. Fallow applies
its own complexity ceilings, computed differently from ESLint's and disagreeing with them,
so a green lint run earlier in the phase is not evidence this passes. Run it clean.

Then run the owner module's per-pair coverage alone —
`node scripts/test-coverage-direct.mjs` against the reconcile backfill module — and
require 100% on function, line and branch. Running it alone is the point: coverage
accumulated across the whole suite would credit this module for arms other suites reach.
The widened scan made previously unreachable arms reachable, so a gap here is a real one.

Confirm the phase's own record is complete before declaring it done. Four negative
controls are owed and each must appear in a SUMMARY as a failing transcript, not as a
description of one: the deleted filter restored, the token removed from the closed set,
the token's rendered bytes changed without touching the set, and the disabled-record
filter removed. Confirm the third one actually turned the catalog test red — if it stayed
green, the pairing checks membership only and the phase is not done. Confirm the three
ungated amendment sites are recorded with the value found at each. If anything is
missing, say so plainly rather than closing over it.

Write down what the phase leaves open, so the next reader finds it rather than
rediscovering it as a defect:

- The per-script admission-gate warnings a re-materialization produces are dropped on this
  path — the backfill caller renders neither half, and the outcome shape carries no
  warnings field. The convergence token partly compensates, because the row is no longer
  silent, but the per-script detail still goes nowhere. Pre-existing, tracked on the
  backlog, deliberately out of scope.
- The scan is now proportional to every recorded plugin in both scopes, once per extension
  version change, as offline disk work. Bounded and real.
- One permanently failing record now keeps the version gate open for its whole scope on
  every load, where before only the narrow population could do that. Pre-existing
  mechanism, newly likely.
- Growth is not specific to one component kind: any path-source record whose on-disk
  source gained a component directory re-materializes on the next version bump. The token
  is worded for that population, not for a narrower one.

Record each of the phase's high-severity threats and where it is now gated, so the
security audit that follows reads evidence rather than intent.
  </action>
  <acceptance_criteria>
    - `npm run check` completed with exit 0 and the SUMMARY names the chain it ran.
    - The per-pair coverage run for the reconcile backfill module reports 100% function, line and branch, run alone.
    - All four owed negative controls are present as verbatim failing transcripts across the phase's SUMMARYs, and the byte-change control is confirmed to have turned the catalog test red.
    - The three ungated amendment sites are recorded with the value found at each.
    - The four out-of-scope inheritances are written down, each with the reason it is out of scope.
    - Each high-severity threat is named with the case or filter that now gates it.
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit at any link in the chain. Capture WHICH link failed before reacting — a fallow health failure and a unit-test failure need opposite responses, and the chain reports only the first</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts</automated>
    <fails_when>non-zero exit, or under 100% on function, line or branch for the pair</fails_when>
    <automated>npm run test:integration</automated>
    <fails_when>non-zero exit, or the run reports any failing test</fails_when>
  </verify>
  <done>The phase is green on the gate that actually gates it, and everything it chose not to fix is written down where the next reader will look.</done>
</task>

</tasks>

<house_gates>
- Planning markdown is formatted by `mdformat` through pre-commit, never by prettier.
- `npm run check` is the gate. It runs once, here. Do not re-run it as a spot-check elsewhere in the phase.
- **Commit discipline in this worktree.** `.git` is a file, so trufflehog's git-mode hook aborts structurally. Run `trufflehog filesystem <paths being committed> --results=verified,unknown --fail` first and only then commit with `SKIP=trufflehog`. Never `--no-verify`, never amend, never `git add -A` — the operator has concurrent uncommitted edits outside this phase's scope. Stage explicit paths.
</house_gates>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream marketplace manifest -> this project's stated requirements | a claim about a live external repository, restated in a document nobody re-checks |
| the phase's own record -> the security and verification passes that read it | a control described rather than transcribed is indistinguishable from one never run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-116-10 | Repudiation | the WCONV-01 population sentence | low | mitigate | Task 1 measures the two source kinds or replaces the claim with the bound that holds either way, at all three sites, with the evidence and date recorded |
| T-116-12 | Repudiation | the phase's negative-control record | medium | mitigate | Task 2 confirms each of the four owed controls is present as a verbatim failing transcript, and specifically that the byte-change control turned the catalog test red rather than staying green |
| T-116-13 | Denial of service | one permanently failing record holding the version gate open for its scope | low | accept | Pre-existing mechanism made newly likely by the widening; the cost is a repeated offline scan per load, no data loss and no unbounded growth. Named in the record rather than fixed, because a fix changes the retry contract the whole scan rests on |
| T-116-14 | Repudiation | dropped per-script admission-gate warnings on the backfill path | medium | accept | Pre-existing, tracked on the backlog, and partly compensated by the convergence token now making the row non-silent. Named explicitly in the record rather than silently inherited |
| T-116-SC | Tampering | npm/pip/cargo installs | high | accept | This phase adds, upgrades and removes no package; there is no install task for a legitimacy gate to precede. Any package addition would invalidate this acceptance and require the gate |
</threat_model>

<verification>
- `npm run check` — exit 0
- `node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts` — 100% function, line and branch, run alone
- `npm run test:integration` — no failing tests
- The retired population clause is absent from the requirement and the roadmap; the corrected bound is present in the requirement
- All four owed negative-control transcripts are verbatim in the phase's SUMMARYs, and the three ungated amendment sites are recorded with their values

</verification>

<success_criteria>
- The population claim is measured or bounded, at all three sites, with its evidence and date.
- The full project gate is green, and the owner module's per-pair coverage is complete when run alone.
- The phase's four owed negative controls are present as transcripts, and the byte-change control is confirmed to have gone red.
- The four out-of-scope inheritances and every high-severity threat's gate are written down.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/116-load-time-workflow-convergence/116-04-SUMMARY.md` when done.

The SUMMARY is the phase's record of account. It must carry: the measured source kinds
with the manifest read and the date (or the honest statement that the measurement could
not be made and what was written instead), the `npm run check` result naming the chain,
the per-pair coverage numbers, a roll-call of the four negative controls with where each
transcript lives, the three ungated amendment sites with the value found at each, and the
four out-of-scope inheritances with the reason each is out of scope.
</output>
