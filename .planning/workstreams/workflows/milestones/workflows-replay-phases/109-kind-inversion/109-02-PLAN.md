---
phase: 109-kind-inversion
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - extensions/pi-claude-marketplace/domain/resolver.ts
  - extensions/pi-claude-marketplace/domain/components/plugin.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - extensions/pi-claude-marketplace/shared/probe-classifiers.ts
autonomous: true
requirements: [WINV-01, WINV-02, WINV-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WINV-01: the workflows kind is absent from `UNSUPPORTED_COMPONENT_KINDS` and from `UNSUPPORTED_COMPONENT_CONVENTIONS`, and present in both `SUPPORTED_COMPONENT_KINDS` and `SUPPORTED_COMPONENT_PATH_KINDS`; the resolver exposes `componentPaths.workflows` as a REQUIRED member on all three of its spellings"
    - "WINV-01: the convention probe is byte-identical before and after — `collectStrictComponentKind` derives `<pluginRoot>/<kind>` from the kind name itself, so no new probe, no `WORKFLOW_CONVENTION` entry and no special case is introduced; only which tuple reads it changes"
    - "WINV-02: a plugin whose only unsupported component was the workflows kind resolves `installable` with the kind in `supported`, and a plugin carrying BOTH a workflows directory and a still-unsupported kind resolves `partially-available` with only the other kind in `unsupported`"
    - "WINV-03: all four deletion sites move together — the `REASONS` tail entry, the `UnsupportedReason` topic-group member in `notify-reasons.ts`, the separately-declared `UnsupportedReason` alias member in `probe-classifiers.ts`, and the `kindToReason` arm; the `_ReasonsCoverageProof` in `notify-reasons.ts` fails at TS2344 if either half of the first pair is left behind"
    - "WINV-03: every count narrative agrees with the tuple after the change — the `notify.ts` catalog-stability comment, the two `notify-reasons.ts` header sentences, and the running derivation in the same header all read 43"
    - "T-02-25: no commit produced by this plan leaves the workflows kind in NEITHER closed set. The removal from the unsupported tuple and the additions to both supported tuples are one atomic change."
    - "Pitfall 2 (recorded, not fixed): a plugin whose manifest declares the workflows field while the marketplace entry stays silent now resolves `unavailable` under `resolveLoose` with a component-declarations-conflict note, because the kind became path-bearing and MM-6 applies to it exactly as it already applies to skills, commands and agents. `resolveLoose` has no production consumer, so the blast radius is test-side only and no carve-out is added."
    - "No `SupportedKind` type is exported. Nothing in this phase consumes it, and `fallow dead-code` flags an unused export."
  prohibitions:
    - "No release may be cut and no `EXTENSION_VERSION` bump made while the resolver reports a capability the tree cannot materialize. The load-time backfill scan is gated on the recorded extension version matching the current one, so a bump inside this window would fire a convergence that reinstalls workflow-bearing records while no bridge exists to materialize anything for them."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/resolver.ts"
      provides: "the workflows kind in both supported tuples, out of the unsupported tuple and its conventions map, and the required `componentPaths.workflows` member on the schema, the accumulator and its initializer"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/domain/components/plugin.ts"
      provides: "the workflows manifest field relocated into `SUPPORTED_COMPONENT_PATH_FIELDS`"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "the 43-member `REASONS` tuple and the corrected catalog-stability narrative"
      contains: "43-entry"
    - path: "extensions/pi-claude-marketplace/shared/notify-reasons.ts"
      provides: "the narrowed `UnsupportedReason` topic group and the corrected running count derivation"
      contains: "WINV-03"
    - path: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      provides: "the four-member `UnsupportedReason` alias and the two-carve-out `kindToReason`"
      contains: "unsupported component"
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/resolver.ts"
      to: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      via: "`resolved.unsupported[]` no longer carries the workflows kind, so `narrowUnsupportedKinds` never reaches a workflows arm"
      pattern: "narrowUnsupportedKinds"
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "extensions/pi-claude-marketplace/shared/notify-reasons.ts"
      via: "`REASONS` is the byte-source; the topic groups are typed views over it and carry the structural completeness proof that both halves moved"
      pattern: "_ReasonsCoverageProof"
---

<objective>
Move the workflows kind across the resolver's closed sets and the manifest schema's field groups,
and retire the dedicated reason token that PR #154 wrote to mean the opposite.

Purpose: this is the whole behavioral change. A plugin carrying `<pluginRoot>/workflows/` stops
resolving `partially-available` and starts resolving `installable`, and `componentPaths.workflows`
comes into existence — the single field that is this phase's entire forward contract to Phase 111.

Output: five production files edited, in two atomic commits. The first is constrained by T-02-25
to carry the removal and both additions together; the second is constrained by the
`_ReasonsCoverageProof` to carry both halves of the reason retirement together. Zero new
dependencies, zero new modules, zero production typecheck errors.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-CONTEXT.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-RESEARCH.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-PATTERNS.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The worktree `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`.
`node_modules` is a real directory here. Commit with the `SKIP=trufflehog` prefix only after the
filesystem-mode scan over the changed paths comes back clean (`CLAUDE.md` §Git). Never
`--no-verify`.
</working_directory>

<why_this_plan_reads_109_01_summary>
`109-01-SUMMARY.md` carries the captured RED output for the five locking gates. This plan's job is
to turn each of them green, and the executor needs the recorded failure text to confirm that the
green it produces closes the same failure that was observed — not a different one that happened to
be sitting there. Read the `Red observations (Success Criterion 4)` section before starting.
</why_this_plan_reads_109_01_summary>

<artifacts_this_phase_produces>
The complete phase-109 ledger. This phase mostly REMOVES and MOVES symbols; the removals are
listed explicitly so a later drift check does not read their absence as unexplained.

| Change | Symbol | File | Owner plan |
|--------|--------|------|-----------|
| ADD | `tests/integration/workflow-kind-inversion.test.ts` (new file) | — | 109-05 |
| ADD | `workflows: Type.Array(Type.String())` on `ComponentPathsSchema` | `domain/resolver.ts` | 109-02 |
| ADD | `workflows: string[]` on `PartialResolution.componentPaths` + `workflows: []` in `emptyResolution()` | `domain/resolver.ts` | 109-02 |
| ADD | `workflows` as the 5th member of `SUPPORTED_COMPONENT_KINDS` | `domain/resolver.ts` | 109-02 |
| ADD | `workflows` as the 4th member of `SUPPORTED_COMPONENT_PATH_KINDS` | `domain/resolver.ts` | 109-02 |
| ADD | the `WINV-01` negative mirror test on `UNSUPPORTED_COMPONENT_KINDS` | `tests/architecture/hooks-foundation.test.ts` | 109-01 |
| ADD | the strict and loose `WINV-01` convention tests | `tests/domain/resolver.test.ts` | 109-01 |
| ADD | the `WINV-03` arithmetic ledger lines | `tests/architecture/notify-closed-set-locks.test.ts`, `shared/notify-reasons.ts` | 109-01, 109-02 |
| MOVE | `workflows: Type.Optional(Type.Unknown())` from `UNSUPPORTED_COMPONENT_FIELDS` to `SUPPORTED_COMPONENT_PATH_FIELDS` | `domain/components/plugin.ts` | 109-02 |
| RENAME | the three `catalog-state` ids, in both homes | `docs/output-catalog.md` + `tests/architecture/catalog-uat.test.ts` | 109-01 |
| REMOVE | the tail member of the `REASONS` tuple and its three-line doc comment | `shared/notify.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` topic group | `shared/notify-reasons.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` alias (a DIFFERENT union) | `shared/probe-classifiers.ts` | 109-02 |
| REMOVE | the workflows arm of `kindToReason` | `shared/probe-classifiers.ts` | 109-02 |
| REMOVE | the workflows member of `UNSUPPORTED_COMPONENT_KINDS` | `domain/resolver.ts` | 109-02 |
| REMOVE | the `UNSUPPORTED_COMPONENT_CONVENTIONS` workflows entry | `domain/resolver.ts` | 109-02 |
| REMOVE | the workflows row of `unsupportedConventionScenarios` and its two generated tests | `tests/domain/resolver.test.ts` | 109-01 |
| REMOVE | the tail entry of the ordered `expected` REASONS enumeration | `tests/architecture/compat-01-no-expansion.test.ts` | 109-01 |
| TURN | the dedicated-reason classifier test into the fall-through assertion | `tests/shared/probe-classifiers.test.ts` | 109-04 |
| WIDEN | 76 `componentPaths` construction and assertion sites across 11 test files | `tests/**` | 109-03 (68) + 109-04 (8) |

**Nothing else is created.** No `SupportedKind` export, no `workflowsSavedDir` locations getter
(Phase 110), no bridge (Phase 111).
</artifacts_this_phase_produces>

<sites_that_look_broken_and_are_not>
Two production sites appear to require an edit and do not. Confirmed by reading them and by a
measured typecheck under the applied change — do NOT edit either.

- `orchestrators/plugin/info.ts` around lines 646-656: `composeResolvedComponents` declares its
  parameter STRUCTURALLY, naming only the three readonly arrays. A wider argument satisfies it, so
  `info` compiles and simply does not enumerate the new kind. That is the correct behavior for
  this phase and the seam Phase 111 widens.
- `orchestrators/plugin/info.ts` around line 1901: constructs a three-key `componentPaths` literal
  for that same narrow parameter. No error.

Two adjacent comments the CONTEXT flagged for a read also need no edit:
`orchestrators/plugin/install.messaging.ts` around lines 410-425 names the LSP field as the sole
manifest-field carve-out and the hooks kind as the supported-kind exception — it does not name
workflows. `shared/errors.ts` around lines 520-531 discusses hooks only.
</sites_that_look_broken_and_are_not>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin source tree -> resolver | plugin-declared component paths and on-disk convention directories cross here as untrusted input |
| resolver verdict -> renderer | typed kind strings become user-visible reason tokens through `narrowUnsupportedKinds` |
| stored `state.json` -> renderer | a pre-existing record's `compatibility.unsupported` array can still carry the retired kind string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-01 | Tampering / Repudiation | `UNSUPPORTED_COMPONENT_KINDS` + both supported tuples in `domain/resolver.ts` | high | mitigate | **Task 1 lands the removal and both additions in ONE commit.** A commit where the kind sits in neither closed set reintroduces the exact silently-ignored-kind defect the `SECURITY (T-02-25)` note above `UNSUPPORTED_COMPONENT_KINDS` warns about. Enforced by the single-commit acceptance criterion and proved by the two `hooks-foundation` assertions, which together admit exactly one membership. |
| T-109-02 | Tampering | `validateComponentPath` / `assertPathInside` for a declared workflows path | medium | mitigate | The kind becomes path-bearing, so a declared path such as `../../etc` now routes through the same NFR-10 containment chokepoint as skills. This is a strict IMPROVEMENT over the pre-inversion state, where the path was never validated because the kind was opaque. No new code — the existing chokepoint covers it. |
| T-109-07 | Repudiation | a stale `compatibility.unsupported` entry in an existing `state.json` | low | accept | A pre-existing record can still hold the retired kind string. After the retirement it falls through `kindToReason` to the generic component marker — truthful enough — and self-corrects on the next `update` or `reinstall`, both of which rewrite `compatibility` from a fresh resolve. No data migration. |
| T-109-04 | Information disclosure | none | low | accept | No workflow script is read, parsed or evaluated. The only disk operation the resolver performs against a plugin source is the existing `statKind` probe. |
| T-109-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package. 109-RESEARCH §Package Legitimacy Audit records "not applicable", so no legitimacy checkpoint is required. |
</threat_model>

<!-- planner-discipline-allow: "workflows" -->

<tasks>

<task type="auto">
  <name>Task 1: Move the kind across the closed sets and the schema field groups — one commit</name>
  <files>extensions/pi-claude-marketplace/domain/resolver.ts, extensions/pi-claude-marketplace/domain/components/plugin.ts</files>
  <read_first>
    - `109-RESEARCH.md` §*Pattern 1: The five production edits* (lines 338-360) — the file:line table with the current and target form of every site
    - `109-RESEARCH.md` §*Pattern 2: The convention probe needs no new code* (lines 361-399) — the measured before/after resolver verdict table
    - `109-RESEARCH.md` §*Prior art on `features/workflows-spike`* (lines 691-736) — what transfers byte-identically and, critically, what does NOT: the spike's `UNSUPPORTED_COMPONENT_KINDS` doc comment must NOT be copied, because main rewrote it
    - `109-RESEARCH.md` §*Pitfall 2* (lines 978-996) — the loose manifest-only consequence to record
    - `extensions/pi-claude-marketplace/domain/resolver.ts` lines 60-75 (`ComponentPathsSchema` and its D-07 rationale), 335-405 (both supported tuples with their doc comments, `UNSUPPORTED_COMPONENT_KINDS` with its `SECURITY (T-02-25)` and `D-90-06` paragraphs, `UNSUPPORTED_COMPONENT_CONVENTIONS`, `PartialResolution`), 428-442 (`emptyResolution`), 1045-1060 (`collectStrictComponentKind` — read for confirmation only, it needs NO body change), 1406-1430 (`collectLooseComponentKind` and the MM-6 conflict arm)
    - `extensions/pi-claude-marketplace/domain/components/plugin.ts` lines 26-48 (both field bags)
    - `.claude/rules/typescript-comments.md`
  </read_first>
  <action>
Land every edit below in ONE commit. The removal from the unsupported tuple and the additions to
both supported tuples must not be separated: a commit where the kind sits in neither closed set is
the silently-ignored-kind defect the `SECURITY (T-02-25)` note directly above
`UNSUPPORTED_COMPONENT_KINDS` warns about, and D-109-06 rejected the split-tuple alternative for
exactly this reason.

1. `domain/resolver.ts`, `ComponentPathsSchema` (opens at line 67). Add a fourth key after the
   agents key: an array-of-string typebox member named for the workflows kind. The D-07 rationale
   comment above it describes array-per-kind semantics and stays true unchanged.

2. `domain/resolver.ts`, `SUPPORTED_COMPONENT_KINDS` (line 351). Append the workflows kind as the
   fifth and last member. Amend the doc comment above it (lines ~340-350) so the hooks carve-out
   sentence stays accurate now that a SECOND kind is in this tuple: hooks remains the only member
   excluded from the narrower path tuple, because its discovery path is a convention FILE; the
   workflows kind sits in both tuples because its manifest field is path-bearing and its convention
   directory name equals the kind name. Keep the `HOOK-01` anchor and add a `WINV-01` anchor.

3. `domain/resolver.ts`, `SUPPORTED_COMPONENT_PATH_KINDS` (line 360). Append the workflows kind as
   the fourth and last member. The `SupportedPathKind` alias on the next line derives from it and
   widens for free.

4. `domain/resolver.ts`, `UNSUPPORTED_COMPONENT_KINDS` (opens at line 380). Delete the workflows
   member — it is the last entry of the tuple, at line 388. Delete NOTHING else from the
   surrounding doc comment: keep main's PR-3 partially-available framing, keep the
   `SECURITY (T-02-25)` paragraph verbatim, and keep the `D-90-06` paragraph about the bin
   directory. The prior art on the spike branch carries an older, narrower version of this comment
   — do not import it.

5. `domain/resolver.ts`, `UNSUPPORTED_COMPONENT_CONVENTIONS` (opens at line 392). Delete the
   workflows entry at line 403. Nothing replaces it: `collectStrictComponentKind` derives the
   probed directory from the kind name itself, so `<pluginRoot>/workflows` is probed by exactly the
   same join it was probed by before. Do not add a convention constant, a helper, or a special
   case; the probe is genuinely identical and WINV-01's "only which tuple reads it changes" is
   literally true.

6. `domain/resolver.ts`, `PartialResolution.componentPaths` (line 410) and the `emptyResolution()`
   literal (line 438). Add the workflows key to both — a required `string[]` on the interface and
   an empty array in the initializer. These two plus the schema in step 1 are three spellings of
   the same field and MUST move together: `ComponentPathsSchema` types the public
   `ResolvedPluginInstallable["componentPaths"]`, `PartialResolution` is the internal accumulator,
   and widening only the schema leaves the `materializableFields` assignment at line ~473
   unassignable.

7. `domain/components/plugin.ts`. Move the workflows field line — an optional unknown typebox
   member — out of `UNSUPPORTED_COMPONENT_FIELDS` and into `SUPPORTED_COMPONENT_PATH_FIELDS`,
   placed after the agents key. Both bags are optional-unknown, so this changes no validation
   outcome; it is documentary at the schema layer and semantic at the resolver. Leave the hooks
   key where it is. If an inline anchor comment is added, tag it `WINV-01`.

Change no function body. `collectStrictComponentKind`, `collectLooseComponentKind`,
`validateComponentPath`, `addComponentPath`, `readPathOrArray` and `materializableFields` are all
`SupportedPathKind`-typed or index through `partial.componentPaths[kind]`, so they widen without
an edit. Do not export a `SupportedKind` type: nothing in this phase consumes it and
`fallow dead-code` flags an unused export.

Then run `npm run typecheck` and let it enumerate the test-side widening. Expect zero errors under
`extensions/` and a large enumerated set under `tests/` — that enumeration is the input to plan
`109-03` and belongs in this plan's SUMMARY, not in a fix here.

Record in the SUMMARY: a plugin whose manifest declares the workflows field while the marketplace
entry stays silent now resolves `unavailable` under `resolveLoose`, with a
component-declarations-conflict note. That is MM-6 applying to a newly path-bearing kind exactly
as it already applies to skills, commands and agents. `resolveLoose` has no production consumer —
the only importers are an architecture test — so this is an intended consequence, not a defect, and
no carve-out is added.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '^extensions/[^(]*\([0-9]+,[0-9]+\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — any production typecheck error means the three spellings of the widened field did not move together</fails_when>
    <automated>node --test tests/architecture/hooks-foundation.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — both the 5-tuple pin and the negative mirror must now pass, and together they are the T-02-25 gate: exactly one closed set may claim the kind</fails_when>
    <automated>node --test --test-name-pattern="WINV-01" tests/domain/resolver.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count, or `ℹ tests` reports fewer than 2 — the strict test must now see a populated component path and the loose test must see the installable arm with no contains-note</fails_when>
    <automated>git show HEAD --stat -- extensions/pi-claude-marketplace/domain/resolver.ts extensions/pi-claude-marketplace/domain/components/plugin.ts</automated>
    <fails_when>the output is empty, or it names only one of the two files — both files must land in the single HEAD commit</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show HEAD -- extensions/pi-claude-marketplace/domain/resolver.ts | grep -cE '^-.*workflows'` prints at least `2` (the unsupported tuple member and the conventions entry) AND `git show HEAD -- extensions/pi-claude-marketplace/domain/resolver.ts | grep -cE '^\+.*workflows'` prints at least `4` — both directions of the move are in the SAME commit, so no intermediate state leaves the kind in neither closed set
    - `SUPPORTED_COMPONENT_KINDS` has exactly 5 members and its last is the workflows kind
    - `SUPPORTED_COMPONENT_PATH_KINDS` has exactly 4 members and its last is the workflows kind
    - `UNSUPPORTED_COMPONENT_KINDS` has exactly 7 members and none of them is the workflows kind
    - `UNSUPPORTED_COMPONENT_CONVENTIONS` has exactly 5 entries and none of them is keyed on the workflows kind
    - `grep -c 'SECURITY (T-02-25)' extensions/pi-claude-marketplace/domain/resolver.ts` prints `1` — the security note survives the member deletion
    - `grep -c 'D-90-06' extensions/pi-claude-marketplace/domain/resolver.ts` prints at least `1` — the bin paragraph survives
    - `grep -c 'workflows' extensions/pi-claude-marketplace/domain/components/plugin.ts` prints `1` — the field exists in exactly one bag
    - `grep -c 'SupportedKind' extensions/pi-claude-marketplace/domain/resolver.ts` prints `2` — only the pre-existing `SupportedPathKind` declaration and its use; no new exported alias
    - no comment added by this task contains a `Phase`, `Plan`, `Wave` or `Pitfall` token
  </acceptance_criteria>
  <done>The workflows kind is in both supported tuples and in neither unsupported structure, the manifest field sits in the supported path-field bag, `componentPaths.workflows` exists on all three of its spellings, production typechecks clean, and one commit carries the whole move.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the dedicated reason and correct every count narrative it appears in</name>
  <reversibility rating="costly">D-109-01: re-adding the token means a new tail append plus a
  catalog row, a renderer arm and a fixture in the same change, per the append-only discipline
  `compat-01` enforces.</reversibility>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/notify-reasons.ts, extensions/pi-claude-marketplace/shared/probe-classifiers.ts</files>
  <read_first>
    - `109-CONTEXT.md` D-109-01 — the retirement, its four deletion sites, and the recorded rationale for why no phase in this milestone produces a plugin-level workflows reason
    - `109-RESEARCH.md` §*Pattern 3: The reason retirement is compiler-enforced to be all-or-nothing* (lines 400-427) — why the two `UnsupportedReason` unions are DIFFERENT declarations and why only one pair is compiler-checked
    - `109-RESEARCH.md` §*Complete blast radius of retiring the `{workflows}` reason* (lines 583-628) — the four deletion sites, the seven count sites, and the five `probe-classifiers.ts` doc comments with line numbers
    - `109-RESEARCH.md` §*Pitfall 6* (lines 1035-1049) — the comment-policy constraint on the arithmetic ledger
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 78-90 (the catalog-stability narrative), 228-242 (the `REASONS` tail and its doc comment), 925-935 and 1773-1783 (the two flag-example doc comments)
    - `extensions/pi-claude-marketplace/shared/notify-reasons.ts` lines 1-30 (the header, both count sentences and the running derivation), 95-112 (the `UnsupportedReason` topic group), and the file's final `_ReasonsCoverageProof` block
    - `extensions/pi-claude-marketplace/shared/probe-classifiers.ts` lines 66-105 (the shared-vocabulary doc block, the alias, and the `narrowResolverNotes` doc comment), 160-225 (the `narrowUnsupportedKinds` doc comment, the TD-3 note and `kindToReason`)
    - `.claude/rules/typescript-comments.md`
  </read_first>
  <action>
Land every edit below in ONE commit. The `notify.ts` tuple entry and the `notify-reasons.ts` topic
group member are joined by a structural completeness proof at the end of `notify-reasons.ts`:
removing either half alone resolves one of the proof's `Exclude` types to the surviving literal
and fails `_AssertNever` at TS2344. Splitting them produces a red typecheck for no benefit.

1. `shared/notify.ts`, the `REASONS` tuple. Delete the tail entry at line 237 — the workflows
   string literal — together with the three-line doc comment above it at 234-236 that anchors it to
   `WDET-04 / D-106-04`. Delete nothing else and reorder nothing: `compat-01` pins order as well as
   membership with `deepEqual`, and the tail is the only position whose removal leaves every
   surviving index unmoved. Do not narrate the removal in a replacement comment.

2. `shared/notify.ts` line 83. The catalog-stability sentence in the file header states a 44-entry
   membership; change the count to 43. The rest of the sentence — that membership AND order are
   catalog-stable and that new tokens append at the tail — stays.

3. `shared/notify.ts` lines 930 and 1778. Both doc comments name the workflows kind as one example
   of an unsupported kind that makes the partial flag useful. Both become false. Drop the workflows
   alternative from each list; wording is at the executor's discretion per the Claude's-Discretion
   list, but the sentence must not narrate what was dropped.

4. `shared/notify-reasons.ts` line 107. Delete the workflows member of the `UnsupportedReason`
   topic-group union. Leave the D-90-05 comment above the generic component member alone.

5. `shared/notify-reasons.ts` header, lines 7 and 14. Both state a 44-entry set; change both to 43.

6. `shared/notify-reasons.ts` header, the running derivation ending at lines 24-26. Keep the
   existing sentence recording the prior append and its `43 to 44` arithmetic — D-109-02 records
   the removal by appending, not by rewriting — and add one sentence after it, anchored on
   `WINV-03`, recording minus one for the retired member and the `44 to 43` transition. Keep it
   strictly arithmetic: the derivation is a running count, one term per spec anchor, and that is
   why it survives the comment policy's ban on narrating removed code. A sentence describing the
   token's former meaning would not.

7. `shared/probe-classifiers.ts` line 77. Delete the workflows member of the `UnsupportedReason`
   alias, taking it from five members to four. This is a SEPARATE declaration from the
   `notify-reasons.ts` group that happens to share a name, and the completeness proof does NOT
   span it — nothing catches a miss here except the owner test at
   `tests/shared/probe-classifiers.test.ts:266`, which plan `109-04` turns.

8. `shared/probe-classifiers.ts`, lines 216-218. Delete the `kindToReason` arm that returns the
   dedicated marker for the workflows kind. The function keeps two carve-outs and its fall-through.
   The parameter stays typed as a plain string per the TD-3 note above it — the resolver's
   unsupported array legitimately carries the hooks kind alongside the typed literals, so no closed
   union spans the real input and this deletion changes no type, only a branch.

9. `shared/probe-classifiers.ts`, five doc comments. Line 73 lists the reasons the typed-kind helper
   maps to — remove the workflows item. Line 82 states that the shared alias also contains a
   typed-kind-only member this helper never emits — remove that clause; the sentence's real point
   is that the alias widens with the malformed-MCP token and that the sibling helpers never emit
   it. Lines 100-101 state that workflows do not use the notes axis so the helper never emits that
   marker — delete both lines outright, they are now vacuous. Line 170 states that a typed
   workflows kind renders a dedicated marker through `kindToReason` — delete that sentence. Line
   204 states that three kinds have dedicated mappings and enumerates them — restate it as two,
   naming only the LSP field and the hooks kind, and retag the anchor from `D-90-05 / WDET-04` to
   `D-90-05 / WINV-03`.

`fallow health` and the two cognitive-complexity gates are not engaged: this task removes a branch
and adds none.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '^extensions/[^(]*\([0-9]+,[0-9]+\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`; a `TS2344` on `_ReasonsCoverageProof` in `notify-reasons.ts` specifically means one half of the reason retirement was left behind</fails_when>
    <automated>node --test tests/architecture/compat-01-no-expansion.test.ts tests/architecture/notify-closed-set-locks.test.ts tests/shared/notify.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — all three gates turned in plan 109-01 must now be GREEN, closing the same failures its SUMMARY recorded</fails_when>
    <automated>node --test tests/architecture/partial-vocabulary-guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — a failure means a reworded doc comment reintroduced retired D-75-01 vocabulary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*|/\*)' extensions/pi-claude-marketplace/shared/notify.ts | grep -o '"workflows"' | wc -l` prints `0`
    - `grep -o '44-entry' extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts | wc -l` prints `0`
    - `grep -c '43-entry' extensions/pi-claude-marketplace/shared/notify.ts` prints `1` and `grep -c '43-entry' extensions/pi-claude-marketplace/shared/notify-reasons.ts` prints `2`
    - `grep -c 'WDET-04 / D-106-04' extensions/pi-claude-marketplace/shared/notify-reasons.ts` prints `1` — the prior derivation term survives the append
    - `grep -c 'WINV-03' extensions/pi-claude-marketplace/shared/notify-reasons.ts` prints `1` — exactly one new derivation term
    - `grep -c -i 'workflow' extensions/pi-claude-marketplace/shared/probe-classifiers.ts` prints `0`
    - `grep -c -i 'workflow' extensions/pi-claude-marketplace/shared/notify-reasons.ts` prints `1` — only the retained prior derivation term names the kind
    - `kindToReason` contains exactly two `if` statements and one trailing return
    - the `UnsupportedReason` alias in `probe-classifiers.ts` has exactly 4 members
    - `git show HEAD --name-only` lists all three files — the retirement is one commit
    - no comment written by this task uses the phrasings the comment policy bans for removed code, and none contains a `Phase`, `Plan`, `Wave` or `Pitfall` token
  </acceptance_criteria>
  <done>The dedicated reason is gone from all four declaration sites, every count narrative reads 43, the running derivation records the reversal as arithmetic beneath the retained prior term, the five stale doc comments state the post-inversion mapping, and the three gates plan 109-01 turned are green.</done>
</task>

</tasks>

<verification>
```bash
npm run typecheck 2>&1 | grep -E '^extensions/[^(]*\([0-9]+,[0-9]+\): error TS' | wc -l
node --test tests/architecture/compat-01-no-expansion.test.ts
node --test tests/architecture/notify-closed-set-locks.test.ts
node --test tests/architecture/hooks-foundation.test.ts
node --test tests/shared/notify.test.ts
node --test tests/architecture/partial-vocabulary-guard.test.ts
node --test --test-name-pattern="WINV-01" tests/domain/resolver.test.ts
```

Zero production typecheck errors; all six test runs green. `tests/domain/resolver.test.ts` run
WHOLE is still red at this point — five whole-arm equality assertions gained a key the compiler
cannot see — and `tests/shared/probe-classifiers.test.ts` and `tests/architecture/catalog-uat.test.ts`
are still red. All three are plan `109-04`'s work; do not fix them here.

Capture the full `npm run typecheck` output. Its enumeration of test-side errors is the input to
plan `109-03` and must be pasted into this plan's SUMMARY.
</verification>

<success_criteria>
- The workflows kind is absent from `UNSUPPORTED_COMPONENT_KINDS` and `UNSUPPORTED_COMPONENT_CONVENTIONS`, present in both supported tuples, and the resolver exposes `componentPaths.workflows`
- The removal and both additions are in ONE commit (T-02-25)
- The manifest field sits in `SUPPORTED_COMPONENT_PATH_FIELDS` and nowhere else
- The dedicated reason is deleted from all four sites in ONE commit, and every count narrative reads 43
- `npm run typecheck` reports zero errors under `extensions/`
- The five gates plan 109-01 turned red are green, and the RED-then-GREEN pair is recorded
- No convention constant, helper, special case, or `SupportedKind` export was added
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/109-kind-inversion/109-02-SUMMARY.md` when done.

The SUMMARY must carry, verbatim:

1. The full `npm run typecheck` output after Task 1 — plan `109-03` consumes its enumeration.
2. A `Green observations (Success Criterion 4)` section pairing each gate turned red in
   `109-01-SUMMARY.md` with its now-green run.
3. The loose manifest-only consequence (Pitfall 2), recorded as an intended effect of the kind
   becoming path-bearing, with the note that `resolveLoose` has no production consumer.
4. The D-109-06 window statement: between this phase and Phase 111 a workflow-bearing plugin
   resolves `installable`, renders a clean installed row, and materializes zero workflow commands.
   Cut no release from this branch before Phase 111 lands.
5. The A-03 corollary: do NOT bump `EXTENSION_VERSION`, `package.json` version or
   `sonar-project.properties` inside this window. The load-time backfill scan is gated on the
   recorded extension version matching the current one, so a bump would fire a convergence that
   reinstalls pre-inversion workflow-bearing records while no bridge exists to materialize
   anything for them.
6. The Pitfall 7 note: `CHANGELOG.md` lines 5-7 sit under a RELEASED version heading and describe
   what that version shipped. They are left intact deliberately; the inversion belongs in the next
   version's entry. `CHANGELOG.md` is not under `docs/`, so WINV-05 does not reach it.
</output>
