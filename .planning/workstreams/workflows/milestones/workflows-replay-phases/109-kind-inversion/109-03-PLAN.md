---
phase: 109-kind-inversion
plan: 03
type: execute
wave: 3
depends_on: [109-02]
files_modified:
  - tests/bridges/agents/stage.test.ts
  - tests/bridges/skills/stage.test.ts
  - tests/bridges/commands/stage.test.ts
  - tests/bridges/commands/discover.test.ts
  - tests/bridges/skills/discover.test.ts
  - tests/orchestrators/plugin/plugin-state-classifier.test.ts
  - tests/orchestrators/plugin/discover-names.test.ts
  - tests/orchestrators/plugin/shared.test.ts
autonomous: true
requirements: [WINV-01]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WINV-01: every `componentPaths` object literal the compiler flags carries the new required key, so `npm run typecheck` reports zero errors under `tests/bridges/` and `tests/orchestrators/`"
    - "WINV-01: the enumeration is the compiler's, not a grep's — `tsc` reports each site as a TS2741 plus TS2322 pair with file, line and column, while a grep over-reports comments and cannot see whole-arm assertion payloads"
    - "The widening is additive only: no existing key, value, assertion form or test title changes, and the new key takes the empty array in every construction site unless a sibling key in the same literal is non-empty for a reason the test states"
    - "`tests/orchestrators/plugin/discover-names.test.ts` carries TWO sites, not one — a readonly PARAMETER TYPE as well as the spread construction beneath it. Widening only the spread leaves the parameter type red; widening only the parameter type leaves the construction incomplete."
  prohibitions:
    - "A red assertion must never be repaired by weakening it. Do not loosen a strict deep-equality assertion to a loose one, do not delete the widened key from an expected payload, and do not switch a whole-arm comparison to a partial match. The expected VALUE gains a key; the assertion form does not change."
  artifacts:
    - path: "tests/bridges/agents/stage.test.ts"
      provides: "31 widened `componentPaths` literals"
      contains: "workflows"
    - path: "tests/bridges/skills/stage.test.ts"
      provides: "29 widened `componentPaths` literals"
      contains: "workflows"
    - path: "tests/orchestrators/plugin/discover-names.test.ts"
      provides: "the widened readonly parameter type AND the widened spread construction"
      contains: "workflows"
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/resolver.ts"
      to: "tests/bridges/agents/stage.test.ts"
      via: "`MaterializablePlugin` / `ResolvedPluginInstallable` require the new `componentPaths` key, so every fixture literal is compile-forced"
      pattern: "componentPaths"
---

<objective>
Close every `componentPaths` construction site the compiler can see.

Purpose: `componentPaths` gained a required fourth key in plan `109-02`. TypeScript enumerates
every object literal that must grow, exactly and exhaustively, with file, line and column. This
plan works that list — it invents nothing and decides nothing.

Output: eight test files whose fixture literals carry the fourth key, and a typecheck that is
clean everywhere except the two files plan `109-04` owns.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/109-kind-inversion/109-RESEARCH.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-PATTERNS.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-02-SUMMARY.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The worktree `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`.
Commit with the `SKIP=trufflehog` prefix only after the filesystem-mode scan over the changed
paths comes back clean (`CLAUDE.md` §Git). Never `--no-verify`.
</working_directory>

<site_inventory>
Measured this planning session with `grep -rc 'componentPaths: {' tests/ --include=*.test.ts`.
**These are EDIT sites, not `tsc` error counts.** Each literal produces a TS2741 plus TS2322 pair,
so the error count is roughly double — a planner sizing off the error count over-scopes by 2x.

| File | Sites | Owner |
|------|-------|-------|
| `tests/bridges/agents/stage.test.ts` | 31 | this plan, Task 1 |
| `tests/bridges/skills/stage.test.ts` | 29 | this plan, Task 1 |
| `tests/orchestrators/plugin/plugin-state-classifier.test.ts` | 2 (lines 58, 73) | this plan, Task 2 |
| `tests/orchestrators/plugin/discover-names.test.ts` | 2 (a param type at 20-24, a spread at 27-31) | this plan, Task 2 |
| `tests/orchestrators/plugin/shared.test.ts` | 1 (line 133) | this plan, Task 2 |
| `tests/bridges/skills/discover.test.ts` | 1 (line 26) | this plan, Task 2 |
| `tests/bridges/commands/stage.test.ts` | 1 (line 50) | this plan, Task 2 |
| `tests/bridges/commands/discover.test.ts` | 1 (line 28) | this plan, Task 2 |
| `tests/domain/resolver.test.ts` | 5 | **109-04** — invisible to `tsc` |
| `tests/orchestrators/plugin/install.test.ts` | 1 | **109-04** — invisible to `tsc` |
| `tests/orchestrators/plugin/git-source-probe.test.ts` | 2 | **109-04** — invisible to `tsc` |
| **total** | **76** | 68 here, 8 in `109-04` |

Work from the compiler's output, not from this table. The table exists so a MISSING error is
recognized as suspicious rather than as done.
</site_inventory>

<artifacts_this_phase_produces>
The complete phase-109 ledger; this plan owns the `WIDEN` row's larger half. Removals are listed
so a later drift check does not read their absence as unexplained.

| Change | Symbol | File | Owner plan |
|--------|--------|------|-----------|
| ADD | `tests/integration/workflow-kind-inversion.test.ts` (new file) | — | 109-05 |
| ADD | `componentPaths.workflows` on the schema, the accumulator and its initializer | `domain/resolver.ts` | 109-02 |
| ADD | the workflows kind in `SUPPORTED_COMPONENT_KINDS` and `SUPPORTED_COMPONENT_PATH_KINDS` | `domain/resolver.ts` | 109-02 |
| ADD | the `WINV-01` negative mirror and the two `WINV-01` resolver convention tests | `tests/architecture/hooks-foundation.test.ts`, `tests/domain/resolver.test.ts` | 109-01 |
| ADD | the `WINV-03` arithmetic ledger lines | `tests/architecture/notify-closed-set-locks.test.ts`, `shared/notify-reasons.ts` | 109-01, 109-02 |
| MOVE | the workflows manifest field into `SUPPORTED_COMPONENT_PATH_FIELDS` | `domain/components/plugin.ts` | 109-02 |
| RENAME | the three `catalog-state` ids, in both homes | `docs/output-catalog.md` + `tests/architecture/catalog-uat.test.ts` | 109-01 |
| REMOVE | the `REASONS` tail member and its doc comment | `shared/notify.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` topic group | `shared/notify-reasons.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` alias and the `kindToReason` arm | `shared/probe-classifiers.ts` | 109-02 |
| REMOVE | the workflows member of `UNSUPPORTED_COMPONENT_KINDS` and its conventions entry | `domain/resolver.ts` | 109-02 |
| REMOVE | the workflows scenario row and its two generated tests | `tests/domain/resolver.test.ts` | 109-01 |
| REMOVE | the tail entry of the ordered `expected` REASONS enumeration | `tests/architecture/compat-01-no-expansion.test.ts` | 109-01 |
| TURN | the dedicated-reason classifier test into the fall-through assertion | `tests/shared/probe-classifiers.test.ts` | 109-04 |
| WIDEN | 68 compile-forced `componentPaths` sites across 8 files | `tests/bridges/**`, `tests/orchestrators/plugin/**` | **109-03** |
| WIDEN | 8 compiler-invisible `componentPaths` payloads across 3 files | `tests/domain/**`, `tests/orchestrators/plugin/**` | 109-04 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> production type | fixture literals are the only place a widened required field can be silently satisfied by a wrong value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-01 | Tampering / Repudiation | `domain/resolver.ts` closed sets (`T-02-25`) | high | mitigate | Closed by `109-02` in one commit. This plan changes no production byte and cannot reopen it. |
| T-109-08 | Repudiation | widened fixture literals | medium | mitigate | Every widened site takes the empty array, so no fixture silently starts claiming a component path it does not have. The prohibition above forbids repairing a red assertion by weakening it. |
| T-109-04 | Information disclosure | none | low | accept | Test fixtures only; no new data path, no plugin script read, nothing logged. |
| T-109-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package. 109-RESEARCH §Package Legitimacy Audit records "not applicable". |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Widen the two bulk stage-test fixture files</name>
  <files>tests/bridges/agents/stage.test.ts, tests/bridges/skills/stage.test.ts</files>
  <read_first>
    - `109-02-SUMMARY.md` — the captured `npm run typecheck` output; it is the authoritative site list
    - `109-PATTERNS.md` §*Archetype A* — the verbatim before-and-after form of the one-line change and the per-file key ordering
    - `109-RESEARCH.md` §*What `SUPPORTED_COMPONENT_PATH_KINDS` gaining a member costs* (lines 629-690) — the per-file error table and the TS2741 / TS2322 pairing
    - `tests/bridges/agents/stage.test.ts` around line 106 — one representative literal, to copy the shape
    - `tests/bridges/skills/stage.test.ts` around its first flagged literal
  </read_first>
  <action>
Run `npm run typecheck` and work its output for these two files only. Each flagged literal is the
same one-line fix: the `componentPaths` object gains a fourth key named for the workflows kind,
holding an empty array, appended LAST. Both files order their keys skills, commands, agents, so
the new key lands after the agents key and the order stays consistent within the file.

31 sites in `tests/bridges/agents/stage.test.ts` and 29 in `tests/bridges/skills/stage.test.ts`.
Never a spread, never a partial, never a helper extracted to avoid repetition — these are fixture
literals and the repetition is deliberate, matching every sibling fixture in the repository.

Change nothing else. No key value moves, no assertion form changes, no test title changes, no
comment is added. If a literal appears inside an `assert.deepStrictEqual` expected payload rather
than a fixture construction, it is still the same one-line fix, but note it in the SUMMARY —
`109-04` owns the payloads the compiler cannot see and the two sets must not overlap.

Re-run `npm run typecheck` and confirm both files have dropped off the error list entirely. Errors
in other files are expected at this point and belong to Task 2 and to `109-04`.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '^tests/bridges/(agents|skills)/stage\.test\.ts' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a remaining error in either file means a flagged literal was skipped</fails_when>
    <automated>node --test tests/bridges/agents/stage.test.ts tests/bridges/skills/stage.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — a runtime failure here means a widened literal changed a value rather than adding a key</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflows: \[\]' tests/bridges/agents/stage.test.ts` prints `31`
    - `grep -c 'workflows: \[\]' tests/bridges/skills/stage.test.ts` prints `29`
    - `grep -c 'componentPaths: {' tests/bridges/agents/stage.test.ts` still prints `31` and the same command for the skills file still prints `29` — no literal was merged, extracted or deleted
    - `git diff --stat tests/bridges/agents/stage.test.ts tests/bridges/skills/stage.test.ts` shows insertions and deletions on the same lines only (each changed line is one widened literal), with no net line-count change
    - `grep -c 'deepEqual' tests/bridges/agents/stage.test.ts` is unchanged from `git show HEAD:tests/bridges/agents/stage.test.ts | grep -c 'deepEqual'` — no assertion was loosened
  </acceptance_criteria>
  <done>Both bulk fixture files carry the fourth key on every `componentPaths` literal, both drop off the typecheck error list, and both test files run green.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the six single-site files, including the readonly parameter type</name>
  <files>tests/bridges/commands/stage.test.ts, tests/bridges/commands/discover.test.ts, tests/bridges/skills/discover.test.ts, tests/orchestrators/plugin/plugin-state-classifier.test.ts, tests/orchestrators/plugin/discover-names.test.ts, tests/orchestrators/plugin/shared.test.ts</files>
  <read_first>
    - `109-02-SUMMARY.md` — the captured typecheck output for the remaining flagged files
    - `109-PATTERNS.md` §*Archetype A′* — the six files, their line numbers, and the verbatim two-part shape of the odd one
    - `109-PATTERNS.md` §*Drift found* item D-3 — the readonly parameter type is the site `tsc` reports; the spread is the site that makes it compile, and both must widen
    - `tests/orchestrators/plugin/discover-names.test.ts` lines 15-35 — the parameter type and the spread construction together
    - `tests/orchestrators/plugin/plugin-state-classifier.test.ts` lines 50-80
    - `tests/orchestrators/plugin/shared.test.ts` around line 133
    - `tests/bridges/skills/discover.test.ts` around line 26, `tests/bridges/commands/stage.test.ts` around line 50, `tests/bridges/commands/discover.test.ts` around line 28
  </read_first>
  <action>
Work the remaining flagged files from the compiler's output.

1. `tests/bridges/commands/stage.test.ts` (1 site, around line 50),
   `tests/bridges/commands/discover.test.ts` (1, around line 28),
   `tests/bridges/skills/discover.test.ts` (1, around line 26),
   `tests/orchestrators/plugin/shared.test.ts` (1, around line 133), and
   `tests/orchestrators/plugin/plugin-state-classifier.test.ts` (2, around lines 58 and 73).
   Each is the identical one-line fix: append a fourth `componentPaths` key named for the
   workflows kind, holding an empty array, in whatever key order the surrounding literal already
   uses.

2. `tests/orchestrators/plugin/discover-names.test.ts` is the odd file and carries TWO sites. The
   `resolvedPlugin` helper takes a `componentPaths` argument whose type is spelled out as three
   readonly string arrays (around lines 20-24) and then rebuilds the object by spreading each of
   them (around lines 27-31). Widen BOTH: add a fourth readonly member to the parameter type and a
   fourth spread line to the construction. This file orders its keys alphabetically, so the new
   member still lands last in both places. The parameter type is what `tsc` reports; the spread is
   what makes the widened type actually satisfied, and fixing only one leaves the file red.

Change nothing else — no key value moves, no assertion form changes, no test title changes.

Re-run `npm run typecheck`. The only errors that may remain are in
`tests/architecture/catalog-uat.test.ts` and `tests/shared/probe-classifiers.test.ts`, both owned
by plan `109-04`. Anything else is a missed site.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '^tests/(bridges|orchestrators)/[^(]*\([0-9]+,[0-9]+\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — every compile-forced site under those two roots must be closed by the end of this task</fails_when>
    <automated>node --test tests/bridges/commands/stage.test.ts tests/bridges/commands/discover.test.ts tests/bridges/skills/discover.test.ts tests/orchestrators/plugin/plugin-state-classifier.test.ts tests/orchestrators/plugin/discover-names.test.ts tests/orchestrators/plugin/shared.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | grep -vE '^tests/(architecture/catalog-uat|shared/probe-classifiers)\.test\.ts' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a remaining error outside the two files plan 109-04 owns is a site this plan missed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflows' tests/orchestrators/plugin/discover-names.test.ts` prints `2` — the readonly parameter member and the spread line
    - `grep -c 'readonly workflows' tests/orchestrators/plugin/discover-names.test.ts` prints `1`
    - `grep -c 'workflows' tests/orchestrators/plugin/plugin-state-classifier.test.ts` prints `2`
    - each of `tests/bridges/commands/stage.test.ts`, `tests/bridges/commands/discover.test.ts`, `tests/bridges/skills/discover.test.ts` and `tests/orchestrators/plugin/shared.test.ts` contains exactly `1` occurrence of the widened key
    - after this task the only files still reported by `npm run typecheck` are `tests/architecture/catalog-uat.test.ts` and `tests/shared/probe-classifiers.test.ts`
    - `git diff` for these six files shows no change to any assertion function, test title, or existing key value
  </acceptance_criteria>
  <done>All 68 compile-forced sites are closed, the six single-site files run green, and the typecheck error list has narrowed to exactly the two files plan 109-04 owns.</done>
</task>

</tasks>

<verification>
```bash
npm run typecheck 2>&1 | grep -E '\): error TS' | grep -vE '^tests/(architecture/catalog-uat|shared/probe-classifiers)\.test\.ts' | wc -l
node --test tests/bridges/agents/stage.test.ts tests/bridges/skills/stage.test.ts tests/bridges/commands/stage.test.ts tests/bridges/commands/discover.test.ts tests/bridges/skills/discover.test.ts tests/orchestrators/plugin/plugin-state-classifier.test.ts tests/orchestrators/plugin/discover-names.test.ts tests/orchestrators/plugin/shared.test.ts
```

The count must be `0` and the test run must be green.

**A green typecheck is not the finish line.** Eight further sites are `assert.deepStrictEqual`
payloads whose second argument the compiler types as `unknown`; they compile clean and fail only
under `npm test`, each reporting a diff whose sole content is the added empty key. They belong to
plan `109-04`, which runs in this same wave. Do not chase them here and do not report the phase's
widening as complete on a green typecheck alone.
</verification>

<success_criteria>
- All 68 compile-forced `componentPaths` sites carry the fourth key with an empty array
- `tests/orchestrators/plugin/discover-names.test.ts` widened BOTH its readonly parameter type and its spread construction
- `npm run typecheck` reports errors only in the two files plan `109-04` owns
- All eight touched test files run green
- No assertion was loosened, no fixture literal merged or extracted, no test title changed
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/109-kind-inversion/109-03-SUMMARY.md` when done.

Record the final per-file site counts actually edited against the `<site_inventory>` table, and
flag any divergence — a file that needed fewer edits than the table predicts means a literal was
already widened by another plan, and a file that needed more means the inventory was stale.
</output>
