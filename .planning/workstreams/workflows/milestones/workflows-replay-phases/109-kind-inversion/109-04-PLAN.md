---
phase: 109-kind-inversion
plan: 04
type: execute
wave: 3
depends_on: [109-02]
files_modified:
  - tests/domain/resolver.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/git-source-probe.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/probe-classifiers.test.ts
autonomous: true
requirements: [WINV-01, WINV-03, WINV-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WINV-01: the eight `assert.deepStrictEqual` whole-arm payloads the compiler cannot see carry the widened key — `tsc` types the second argument as `unknown`, so these compile clean and fail only under `npm test`, each reporting a diff whose sole content is the added empty key"
    - "WINV-04: `catalog-uat` is GREEN, closing the exact byte-mismatch failure captured in `109-01-SUMMARY.md`; the three states now pair byte-for-byte with live renderer output and `examples.length === 182` is unchanged"
    - "WINV-04: the three turned catalog states render what their ids say — a clean not-installed inventory row, a clean installed success row with the reload trailer, and a rejection whose brace names the OTHER component kind, which is what proves the workflows kind stopped contributing a token"
    - "WINV-03: a stray workflows kind string surviving in a pre-inversion record's `compatibility.unsupported` array now classifies to the generic component reason through the `kindToReason` fall-through — real behavior, asserted by a turned test rather than proved by a deleted one"
    - "After this plan and `109-03` both land, `npm test` and `npm run typecheck` are both green across the whole tree"
  prohibitions:
    - "A published byte contract must never be edited to match whatever the renderer happens to emit. The fixture payload is fitted to the expected bytes in `docs/output-catalog.md`, never the reverse. If the rejection state's second component kind does not produce the partially-available arm plus the hint trailer, choose a different kind rather than editing the expected bytes."
    - "A locking gate must never be weakened, retitled into vacuity, or deleted to make room. Do not loosen a strict deep-equality assertion, do not delete a key from an expected payload to make a diff go away, and do not delete the classifier test instead of turning it — deletion is the proof-by-absence WINV-04 forbids."
  artifacts:
    - path: "tests/domain/resolver.test.ts"
      provides: "five widened whole-arm resolution payloads"
      contains: "workflows"
    - path: "tests/orchestrators/plugin/git-source-probe.test.ts"
      provides: "two widened warm-candidate payloads"
      contains: "workflows"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "the three post-inversion fixture payloads paired byte-for-byte with the turned annotations"
      contains: "workflow-plus-unsupported-rejection"
    - path: "tests/shared/probe-classifiers.test.ts"
      provides: "the turned fall-through assertion for a stray workflows kind"
      contains: "WINV-03"
  key_links:
    - from: "docs/output-catalog.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "the byte-pairing test renders each fixture through the real `notify()` and compares it to the fenced block under the matching id"
      pattern: "workflow-install-success"
    - from: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      to: "tests/shared/probe-classifiers.test.ts"
      via: "`narrowUnsupportedKinds` is the owner module; this is its only test, and the only thing that would catch the classifier arm being left behind"
      pattern: "narrowUnsupportedKinds"
---

<objective>
Close the three failures the compiler cannot see, and turn the byte gate green.

Purpose: a green typecheck is not a green suite. Eight whole-arm equality payloads take an
untyped second argument and fail only at runtime; the three catalog fixtures still hand the
renderer the retired reason token; and the classifier owner test still asserts a mapping that no
longer exists. This plan closes all three classes and produces the GREEN half of the red-then-green
pair for `catalog-uat`.

Output: `npm test` green across the whole tree once this plan and `109-03` have both landed.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/109-kind-inversion/109-RESEARCH.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-PATTERNS.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-01-SUMMARY.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-02-SUMMARY.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The worktree `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`.
Commit with the `SKIP=trufflehog` prefix only after the filesystem-mode scan over the changed
paths comes back clean (`CLAUDE.md` §Git). Never `--no-verify`.

This plan runs in the same wave as `109-03` and touches a disjoint file set. If both executors run
concurrently in this one worktree, expect a spurious "files were modified by this hook" report
from `pre-commit` when a sibling's write lands mid-run; re-stage the paths this plan owns and
re-run, and never `git add -A`.
</working_directory>

<post_inversion_fixture_payloads>
The three turned `FIXTURES` payloads. Measured in 109-RESEARCH by driving the real `notify()`, and
paired with the bytes plan `109-01` wrote into `docs/output-catalog.md`. Do not recompute them.

**State 1 — `workflow-available-inventory`.** Replaces the plugin entry in the fixture currently at
`catalog-uat.test.ts:907`. Note it carries NO `reasons` field:

```ts
{ status: "available", name: "helper", version: "1.0.0" }
```

Keep `pi: piWithBothLoaded()`, keep the marketplace wrapper (`name: "official"`, `scope: "user"`),
add no `expectedSeverity` — the render is info.

**State 2 — `workflow-install-success`.** Replaces the plugin entry currently at
`catalog-uat.test.ts:1211`. It is a verbatim copy of the existing `success` state's plugin entry:

```ts
{
  status: "installed",
  name: "helper",
  version: "1.0.0",
  dependencies: [],
  severity: "info",
  needsReload: true,
}
```

Add no `expectedSeverity` — the render is info.

**State 3 — `workflow-plus-unsupported-rejection`.** Replaces the plugin entry currently at
`catalog-uat.test.ts:1316`. The state keeps its `expectedSeverity: "error"`:

```ts
{
  status: "partially-available",
  name: "helper",
  reasons: ["unsupported component"],
  partialHint: true,
  severity: "error",
}
```

**Why the brace names the generic component marker and not the second kind by name.** Measured on
two axes. `narrowUnsupportedKinds` has exactly two carve-outs after the retirement — the LSP
manifest field and the hooks kind — so any other kind collapses to the generic component marker.
And post-inversion, a plugin carrying BOTH a workflows directory and a themes directory resolves
`partially-available` with only themes in `unsupported` and the workflows kind in `supported`. So
the brace names only the other kind, which is exactly the proof D-109-04 item 3 asks for.
</post_inversion_fixture_payloads>

<artifacts_this_phase_produces>
The complete phase-109 ledger; this plan owns the `TURN` row and the invisible half of `WIDEN`.
Removals are listed so a later drift check does not read their absence as unexplained.

| Change | Symbol | File | Owner plan |
|--------|--------|------|-----------|
| ADD | `tests/integration/workflow-kind-inversion.test.ts` (new file) | — | 109-05 |
| ADD | `componentPaths.workflows` on the schema, the accumulator and its initializer | `domain/resolver.ts` | 109-02 |
| ADD | the workflows kind in `SUPPORTED_COMPONENT_KINDS` and `SUPPORTED_COMPONENT_PATH_KINDS` | `domain/resolver.ts` | 109-02 |
| ADD | the `WINV-01` negative mirror and the two `WINV-01` resolver convention tests | `tests/architecture/hooks-foundation.test.ts`, `tests/domain/resolver.test.ts` | 109-01 |
| ADD | the `WINV-03` arithmetic ledger lines | `tests/architecture/notify-closed-set-locks.test.ts`, `shared/notify-reasons.ts` | 109-01, 109-02 |
| MOVE | the workflows manifest field into `SUPPORTED_COMPONENT_PATH_FIELDS` | `domain/components/plugin.ts` | 109-02 |
| RENAME | the three `catalog-state` ids, in both homes | `docs/output-catalog.md` + `tests/architecture/catalog-uat.test.ts` | 109-01 |
| REMOVE | the `REASONS` tail member and its doc comment | `shared/notify.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` topic group | `shared/notify-reasons.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` alias and the `kindToReason` arm | `shared/probe-classifiers.ts` | 109-02 |
| REMOVE | the workflows member of `UNSUPPORTED_COMPONENT_KINDS` and its conventions entry | `domain/resolver.ts` | 109-02 |
| REMOVE | the workflows scenario row and its two generated tests | `tests/domain/resolver.test.ts` | 109-01 |
| REMOVE | the tail entry of the ordered `expected` REASONS enumeration | `tests/architecture/compat-01-no-expansion.test.ts` | 109-01 |
| TURN | the three `FIXTURES` payloads to their post-inversion shapes | `tests/architecture/catalog-uat.test.ts` | **109-04** |
| TURN | the dedicated-reason classifier test into the fall-through assertion | `tests/shared/probe-classifiers.test.ts` | **109-04** |
| WIDEN | 68 compile-forced `componentPaths` sites across 8 files | `tests/bridges/**`, `tests/orchestrators/plugin/**` | 109-03 |
| WIDEN | 8 compiler-invisible `componentPaths` payloads across 3 files | `tests/domain/**`, `tests/orchestrators/plugin/**` | **109-04** |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published contract | `docs/output-catalog.md` is the contract users read; the fixture is what proves the renderer honours it |
| stored `state.json` -> renderer | a pre-existing record's `compatibility.unsupported` array can still carry the retired kind string |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-01 | Tampering / Repudiation | `domain/resolver.ts` closed sets (`T-02-25`) | high | mitigate | Closed by `109-02` in one commit. This plan changes no production byte and cannot reopen it. |
| T-109-05 | Repudiation | `docs/output-catalog.md` <-> `catalog-uat` pairing | medium | mitigate | Turning the fixture payloads restores the byte pairing, so the published contract is proved against live renderer output rather than asserted. The prohibition above forbids fitting the contract to the code. |
| T-109-07 | Repudiation | a stale `compatibility.unsupported` entry in an existing `state.json` | low | mitigate | The turned classifier test pins the fall-through explicitly, so the behavior a legacy record hits is asserted rather than incidental. |
| T-109-04 | Information disclosure | none | low | accept | Test fixtures only; no new data path, no plugin script read, nothing logged. |
| T-109-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package. 109-RESEARCH §Package Legitimacy Audit records "not applicable". |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Widen the eight whole-arm equality payloads the compiler cannot see</name>
  <files>tests/domain/resolver.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/git-source-probe.test.ts</files>
  <read_first>
    - `109-RESEARCH.md` §*Pitfall 4* (lines 1011-1022) — why these are invisible and how to find them
    - `109-PATTERNS.md` §*Archetype B* — the exact measured line numbers and the verbatim before-form of a payload
    - `tests/domain/resolver.test.ts` lines 3336-3380 (the two complete-arm payloads that sit adjacent), and each of lines 3421, 3617, 3762
    - `tests/orchestrators/plugin/install.test.ts` lines 7355-7375 — note this file orders its keys alphabetically
    - `tests/orchestrators/plugin/git-source-probe.test.ts` lines 485-525 — the two warm-candidate payloads
  </read_first>
  <action>
These eight sites are `assert.deepStrictEqual` expected payloads. TypeScript types the second
argument as `unknown`, so a missing key is not a compile error: they compile clean and fail only
under `npm test`, each reporting a diff whose sole content is the added empty key.

Add the fourth `componentPaths` key, holding an empty array, to each. Match the key ORDER the
surrounding literal already uses — `tests/domain/resolver.test.ts` and
`tests/orchestrators/plugin/git-source-probe.test.ts` write skills, commands, agents;
`tests/orchestrators/plugin/install.test.ts` writes agents, commands, skills. In every case the
new key lands last.

Measured sites: `tests/domain/resolver.test.ts` at lines 3346, 3372, 3421, 3617 and 3762;
`tests/orchestrators/plugin/install.test.ts` at line 7363;
`tests/orchestrators/plugin/git-source-probe.test.ts` at lines 492 and 521. That is exactly 8 —
the measured figure, not the approximation an earlier draft carried.

Find them by running `npm test` and reading the failures, then cross-check with
`grep -rn 'componentPaths: {' tests/domain tests/orchestrators`: the sites that survive a green
typecheck are exactly these. Do NOT repair a diff by deleting the key from the expected payload or
by loosening the assertion — the assertion is a whole-arm contract and the point of it is that it
is complete.
  </action>
  <verify>
    <automated>node --test tests/domain/resolver.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/git-source-probe.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count; a failure whose diff reads as a single added empty array key means a site was missed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'componentPaths: {' tests/domain/resolver.test.ts` still prints `5` and every one of the five literals now names four keys
    - `grep -c 'componentPaths: {' tests/orchestrators/plugin/install.test.ts` still prints `1`, and `tests/orchestrators/plugin/git-source-probe.test.ts` still prints `2`
    - `grep -rn 'componentPaths: {' tests/ --include=*.test.ts | wc -l` prints `76` — the phase-wide site count is unchanged; nothing was merged, extracted or deleted
    - `grep -c 'deepStrictEqual' tests/domain/resolver.test.ts` equals `git show HEAD:tests/domain/resolver.test.ts | grep -c 'deepStrictEqual'` — no strict assertion was loosened
    - `node --test tests/domain/resolver.test.ts` reports `ℹ tests 157` — the pre-phase baseline; two generated scenario tests were removed in `109-01` and two positive tests replaced them
  </acceptance_criteria>
  <done>All eight compiler-invisible payloads carry the fourth key, the three files run green, and no assertion form or site count changed.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the three catalog fixture payloads and close the byte gate</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - `109-01-SUMMARY.md` `Red observations` section — the captured `catalog UAT failures` diff this task must close, state by state
    - `<post_inversion_fixture_payloads>` in this plan — the three payloads verbatim
    - `109-RESEARCH.md` §*The three catalog states — exact post-inversion bytes* (lines 737-855) — the measured render for each, its severity argument, and the two measured axes behind the rejection state's brace
    - `109-PATTERNS.md` §*Archetype D* — the verbatim before-form of a fixture entry, including the comment block above the key
    - `docs/output-catalog.md` — the three turned blocks as `109-01` left them; the fixture is fitted to THESE bytes
    - `tests/architecture/catalog-uat.test.ts` lines 900-930, 1200-1235, 1300-1340, 5228-5255, 5374-5410
    - `tests/architecture/partial-vocabulary-guard.test.ts` lines 1-60 — this file is inside its scan scope
  </read_first>
  <action>
Replace the plugin entry inside each of the three renamed `FIXTURES` states with the corresponding
payload from `<post_inversion_fixture_payloads>` above. Keep every wrapper untouched: the
`pi: piWithBothLoaded()` line, the `marketplaces` array, the marketplace name and scope, and the
`expectedSeverity` key where one exists.

1. `workflow-available-inventory` — the plugin entry loses its `reasons` array entirely and its
   status becomes the not-installed inventory status. No `expectedSeverity` is added; the render
   is info.

2. `workflow-install-success` — the plugin entry becomes the clean installed row with an empty
   dependencies array, explicit info severity and the reload flag set. This payload is a verbatim
   copy of the existing `success` state's plugin entry, and the state renders byte-identical
   output. That is permitted: `catalog-uat` walks annotations and checks each against its own
   fixture, keyed on section and state, with no cross-state uniqueness rule anywhere. Do not add a
   distinguishing token to make the two differ.

3. `workflow-plus-unsupported-rejection` — the plugin entry keeps the partially-available status,
   the hint flag and the error severity, and its `reasons` array carries the generic component
   marker in place of the retired token. The state keeps `expectedSeverity: "error"`.

Update the comment block above each key so it describes the state the fixture now produces, still
anchored on `WINV-04`. The rejection state's comment should say the brace names the OTHER
component kind the plugin carries — that is the whole point of the repointed state.

Leave `examples.length === 182` exactly as it is. Three renames plus three payload turns add and
remove no annotation.

Every comment written here is read by `partial-vocabulary-guard`, which forbids the retired
D-75-01 vocabulary: the parenthesised render form of the retired verdict token and the standalone
backticked form. The live reason token naming the component axis is fine, and so is the bare word
in ordinary prose.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count, or `ℹ tests` reports fewer than 6 — this run must close the exact `catalog UAT failures` diff captured in `109-01-SUMMARY.md`</fails_when>
    <automated>node --test tests/architecture/partial-vocabulary-guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — a failure means a reworded fixture comment reintroduced retired D-75-01 vocabulary</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '^tests/architecture/catalog-uat\.test\.ts' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a surviving type error means a payload still hands the renderer the retired reason token</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/catalog-uat.test.ts` reports `ℹ tests 6` and `ℹ fail 0`
    - the captured output contains neither `catalog UAT failures` nor `catalog UAT inverse-walk failures`
    - the captured output does not contain `Expected exactly 182 annotated catalog examples`
    - `grep -c 'workflow-available-inventory' tests/architecture/catalog-uat.test.ts` prints `1`, and the same for `workflow-install-success` and `workflow-plus-unsupported-rejection`
    - the `workflow-available-inventory` fixture's plugin entry has no `reasons` key
    - `grep -c 'WDET-04' tests/architecture/catalog-uat.test.ts` prints `0` for the three turned states' comment blocks — each is retagged to `WINV-04`
    - `git diff tests/architecture/catalog-uat.test.ts` shows no change to `piWithBothLoaded`, to any marketplace wrapper, or to the `examples.length` assertion
  </acceptance_criteria>
  <done>The three fixtures render the bytes their annotations promise, the byte gate is green, and the red captured in `109-01-SUMMARY.md` is closed by a matching green.</done>
</task>

<task type="auto">
  <name>Task 3: Turn the classifier owner test to assert the fall-through</name>
  <files>tests/shared/probe-classifiers.test.ts</files>
  <read_first>
    - `109-RESEARCH.md` §*Open Questions* item 2 (lines 1332-1341) — the recommendation to turn rather than delete, and why
    - `109-RESEARCH.md` §*Runtime State Inventory* — the stale-record path that makes the fall-through a real behavior rather than a hypothetical
    - `109-PATTERNS.md` §*Archetype C*, classifier-test analog — the verbatim before-form and the `satisfies` clause that makes it a compile error after the retirement
    - `tests/shared/probe-classifiers.test.ts` lines 240-285 — the sibling classifier tests for the assertion idiom
    - `extensions/pi-claude-marketplace/shared/probe-classifiers.ts` lines 195-225 as `109-02` left them — the two surviving carve-outs and the fall-through
  </read_first>
  <action>
The test at line 266 currently asserts that the typed workflows kind maps to a dedicated marker.
Turn it; do not delete it. Deleting it would be exactly the proof-by-absence WINV-04 forbids, and
the behavior it becomes is real: a record written before the inversion can still carry the
workflows kind string in its `compatibility.unsupported` array, and that string now falls through
`kindToReason` to the generic component marker.

Keep the input array as it is — it still holds the workflows kind string, because that is the
legacy-record case under test. Change the expected reasons array to hold the generic component
marker instead, keeping its `satisfies readonly UnsupportedReason[]` clause: that clause is what
makes a stale expectation a compile error rather than a silent runtime surprise, and it is the
only thing in the tree that would have caught the classifier arm being left behind.

Retitle the test so it names what it now proves — a stray workflows kind in a legacy record
falling through to the generic component reason — anchored on `WINV-03`. Keep the arrange / act /
assert markers.
  </action>
  <verify>
    <automated>node --test tests/shared/probe-classifiers.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count, or `ℹ tests` reports fewer than 41 — 41 is the pre-phase baseline and a lower count means the test was deleted rather than turned</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — with `109-03` landed this is the whole-tree typecheck and it must be clean</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/shared/probe-classifiers.test.ts` reports `ℹ tests 41` and `ℹ fail 0`
    - `grep -c 'satisfies readonly UnsupportedReason\[\]' tests/shared/probe-classifiers.test.ts` is unchanged from `git show HEAD:tests/shared/probe-classifiers.test.ts | grep -c 'satisfies readonly UnsupportedReason\[\]'` — the compile-time clause survives the turn
    - `grep -c 'WINV-03' tests/shared/probe-classifiers.test.ts` prints `1`
    - `grep -c 'dedicated workflows reason' tests/shared/probe-classifiers.test.ts` prints `0`
    - the turned test still contains all three of the `// arrange`, `// act` and `// assert` markers
    - the test title contains no `Phase`, `Plan`, `Wave` or `Pitfall` token
  </acceptance_criteria>
  <done>The classifier owner test asserts the fall-through a legacy record hits, the file keeps its full test count, and the whole-tree typecheck is clean.</done>
</task>

</tasks>

<verification>
```bash
npm run typecheck
npm test
node --test tests/architecture/catalog-uat.test.ts
node --test tests/architecture/partial-vocabulary-guard.test.ts
```

With `109-03` landed in the same wave, `npm run typecheck` must exit 0 with no output lines
matching `error TS`, and `npm test` must report `ℹ fail 0`.

Pair each green in this plan against the red captured in `109-01-SUMMARY.md`. The `catalog-uat`
pair is the load-bearing one for Success Criterion 4 — it is the gate whose red required the
documentation half to be turned first, and its green here is what closes that sequence.
</verification>

<success_criteria>
- All eight compiler-invisible `componentPaths` payloads carry the fourth key
- `catalog-uat` is green with 6 tests and the `examples.length` assertion untouched
- The three turned states render a clean not-installed row, a clean installed row, and a rejection whose brace names the other component kind
- The classifier owner test is turned, not deleted, and keeps its `satisfies` clause
- `npm run typecheck` and `npm test` are both green across the whole tree
- No assertion was loosened and no expected payload lost a key to make a diff go away
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/109-kind-inversion/109-04-SUMMARY.md` when done.

Record a `Red-then-green pairs (Success Criterion 4)` table with one row per locking gate, citing
the captured red from `109-01-SUMMARY.md` and the green produced here or in `109-02-SUMMARY.md`.
That table is what `/gsd-verify-work` reads to certify criterion 4; it is the phase's primary
evidence artifact.
</output>
