---
phase: 109-kind-inversion
plan: 05
type: execute
wave: 4
depends_on: [109-03, 109-04]
files_modified:
  - tests/integration/workflow-kind-inversion.test.ts
autonomous: true
requirements: [WINV-02, WINV-05]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WINV-02: a real install of a workflow-bearing plugin succeeds with NO partial flag — the rendered cascade carries the clean installed row and carries neither the partially-available token nor the partial-flag hint"
    - "D-109-06: the same install writes NO workflow artifact — the host engine's storage root under the hermetic home does not exist at all, because no bridge materializes anything yet. This is the half Phase 111 deliberately inverts."
    - "The new integration file needs no production pair: `scripts/check-corresponding-tests.mjs` exempts the `integration` root, and the behavior under test spans resolver plus orchestrator plus a filesystem root no single module owns"
    - "WINV-05: no `docs/` prose states that a workflow-bearing plugin degrades. `docs/output-catalog.md` is the whole surface — every other tracked non-planning markdown mention of the word is a CI badge URL, a cited blog title, a generic English usage, or a released CHANGELOG record."
    - "Success Criterion 6: `npm run check` is green — typecheck, ESLint, all three fallow sub-gates, Prettier, both corresponding-test gates, the direct-coverage negative gate, the unit suite and the integration suite"
    - "The hermetic home isolates the workflows storage root by construction: `os.homedir()` honours the HOME environment variable on this platform, and the helper overrides and restores it around a single test"
  prohibitions:
    - "The inversion must never become a silent widening. A workflow-bearing plugin must never render an install row that implies workflow commands were materialized while none were. The window this phase opens is acceptable ONLY because it is pinned by an assertion, recorded in the SUMMARY and the VERIFICATION, and closed by Phase 111 before anything ships."
  artifacts:
    - path: "tests/integration/workflow-kind-inversion.test.ts"
      provides: "the install-level proof of both halves — a plain install succeeds, and nothing is materialized"
      contains: "WINV-02"
  key_links:
    - from: "tests/integration/workflow-kind-inversion.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      via: "drives a real `installPlugin` call against a seeded path-source marketplace, with no partial flag"
      pattern: "installPlugin"
    - from: "tests/integration/workflow-kind-inversion.test.ts"
      to: "extensions/pi-claude-marketplace/domain/resolver.ts"
      via: "the install succeeds only because the resolver now returns the installable arm for a workflow-bearing plugin"
      pattern: "installPlugin"
---

<objective>
Pin both halves of the 109-111 window at install level, and close the phase gate.

Purpose: D-109-07 asks the proof to reach past the resolver. A resolver unit test shows the arm
flipped; only a real install shows that a user gets a clean success with no flag, and that nothing
was written. Asserting the second half turns D-109-06's accepted window from an undocumented gap
into a pinned fact — and it is exactly the assertion Phase 111 must invert once the bridge lands.

Output: one new integration test file, a documentation sweep confirmed, and `npm run check` green.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-CONTEXT.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-RESEARCH.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-PATTERNS.md
@.planning/workstreams/workflows/phases/109-kind-inversion/109-04-SUMMARY.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The worktree `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`.
`node_modules` here is a REAL directory, not a symlink — that matters for this plan, because the
integration suite cannot resolve the Pi host types without it and a freshly created agent worktree
has none.

Commit with the `SKIP=trufflehog` prefix only after the filesystem-mode scan over the changed paths
comes back clean (`CLAUDE.md` §Git). Never `--no-verify`.
</working_directory>

<flagged_planner_assumptions>
One probe row came back `unclassified` and is surfaced here rather than auto-resolved. It is NOT a
verification criterion; it is an assumption a human should confirm.

1. **WINV-02 (unclassified).** WINV-02 reads: *"A plugin that resolved `partially-available` with
   the workflows brace before the inversion resolves `installable` after it, and installs its
   workflow envelopes on a normal install rather than needing `--partial`."* The planner assumption
   is that only the FIRST clause is deliverable in this phase, and that the second clause —
   installing workflow envelopes — is Phase 111's, because no bridge exists here to write one.
   Phase 109 therefore delivers the resolver flip plus the flag-free plain install, and pins the
   envelope clause's ABSENCE as the D-109-06 window rather than satisfying it.

   Two things support the reading. The ROADMAP's Success Criterion 2 for this phase states only the
   first clause ("resolves `installable` and installs on a plain `install`, with no `--partial`").
   And `109-CONTEXT.md` §Phase Boundary is explicit that no bridge, discovery, staging or envelope
   writing belongs here.

   If WINV-02 was intended to be fully closed inside Phase 109, it is NOT, and the requirement
   should be re-scoped or split rather than marked satisfied. The carry-forward is already recorded
   in `109-CONTEXT.md` §Deferred Ideas: Phase 111 must INVERT the no-artifact assertion this plan
   writes, because an assertion that quietly stays green while meaning the opposite is worse than
   no assertion.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
The complete phase-109 ledger; this plan produces the phase's only new file. Removals are listed so
a later drift check does not read their absence as unexplained.

| Change | Symbol | File | Owner plan |
|--------|--------|------|-----------|
| ADD | `tests/integration/workflow-kind-inversion.test.ts` (new file) — the sole new file in the phase | — | **109-05** |
| ADD | `componentPaths.workflows` on the schema, the accumulator and its initializer | `domain/resolver.ts` | 109-02 |
| ADD | the workflows kind in `SUPPORTED_COMPONENT_KINDS` and `SUPPORTED_COMPONENT_PATH_KINDS` | `domain/resolver.ts` | 109-02 |
| ADD | the `WINV-01` negative mirror and the two `WINV-01` resolver convention tests | `tests/architecture/hooks-foundation.test.ts`, `tests/domain/resolver.test.ts` | 109-01 |
| ADD | the `WINV-03` arithmetic ledger lines | `tests/architecture/notify-closed-set-locks.test.ts`, `shared/notify-reasons.ts` | 109-01, 109-02 |
| MOVE | the workflows manifest field into `SUPPORTED_COMPONENT_PATH_FIELDS` | `domain/components/plugin.ts` | 109-02 |
| RENAME | the three `catalog-state` ids, in both homes | `docs/output-catalog.md` + `tests/architecture/catalog-uat.test.ts` | 109-01 |
| REMOVE | the `REASONS` tail member and its doc comment | `shared/notify.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` topic group | `shared/notify-reasons.ts` | 109-02 |
| REMOVE | the workflows member of the `UnsupportedReason` alias and the `kindToReason` arm | `shared/probe-classifiers.ts` | 109-02 |
| REMOVE | the workflows member of `UNSUPPORTED_COMPONENT_KINDS` and its conventions entry | `domain/resolver.ts` | 109-02 |
| REMOVE | the workflows scenario row and its two generated tests | `tests/domain/resolver.test.ts` | 109-01 |
| REMOVE | the tail entry of the ordered `expected` REASONS enumeration | `tests/architecture/compat-01-no-expansion.test.ts` | 109-01 |
| TURN | the three `FIXTURES` payloads and the classifier owner test | `tests/architecture/catalog-uat.test.ts`, `tests/shared/probe-classifiers.test.ts` | 109-04 |
| WIDEN | 76 `componentPaths` sites across 11 test files | `tests/**` | 109-03 (68) + 109-04 (8) |

**Nothing else is created.** No `SupportedKind` export, no `workflowsSavedDir` locations getter
(Phase 110), no `bridges/workflows/` (Phase 111), no `docs/workflows-compatibility.md` (Phase 114),
no new closed-set reason for a future phase.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin source tree -> install orchestrator | a seeded fixture plugin crosses the same path the real install path takes |
| install verdict -> user | the rendered cascade is what a user reads to learn what happened |
| test process -> host filesystem | `HOME` is overridden process-wide for the duration of one test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-109-01 | Tampering / Repudiation | `domain/resolver.ts` closed sets (`T-02-25`) | high | mitigate | Closed by `109-02` in one commit; re-proved here end to end, because an install that succeeds cleanly is only reachable when exactly one closed set claims the kind. |
| T-109-03 | Repudiation | the install row for a workflow-bearing plugin during the 109-111 window | medium | mitigate | The window is pinned by an install-level test asserting BOTH halves — the clean flag-free success AND the absent artifact — and recorded in this phase's SUMMARY and VERIFICATION. Phase 111 inverts the second half. |
| T-109-09 | Tampering | `HOME` override in the test process | low | mitigate | One `test(...)` per hermetic-home block, following the analog. `HOME` is set and restored in a `finally`, and no sibling test in this file reads `HOME` outside its own block. |
| T-109-02 | Tampering | `assertPathInside` for a declared workflows path | medium | mitigate | Unchanged from `109-02`: the newly path-bearing kind routes through the existing NFR-10 containment chokepoint. The fixture declares no component path, so it exercises the convention arm. |
| T-109-04 | Information disclosure | none | low | accept | No workflow script is read, parsed or evaluated. The fixture writes a script file to disk; nothing in the tree opens it. |
| T-109-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package. 109-RESEARCH §Package Legitimacy Audit records "not applicable". |
</threat_model>

<!-- planner-discipline-allow: {workflows} -->

<tasks>

<task type="auto">
  <name>Task 1: Write the install-level window test — clean install, zero artifacts</name>
  <precondition>`node_modules/` exists as a real directory in this worktree. `npm run test:integration` resolves the Pi host types from it and cannot run without it; a freshly created agent worktree has none.</precondition>
  <files>tests/integration/workflow-kind-inversion.test.ts</files>
  <read_first>
    - `109-CONTEXT.md` D-109-07 half 2, and §Deferred Ideas — the carry-forward obliging Phase 111 to invert the no-artifact half
    - `109-RESEARCH.md` §*Pattern 4: The install-level window test* (lines 428-512) — where the file goes, the corresponding-test exemption, the analog's helper inventory, and the measured storage-root path
    - `109-RESEARCH.md` §*The install-level window test skeleton* (lines 1156-1197) — the assertion set
    - `109-RESEARCH.md` §*Pitfall 8* (lines 1063-1073) — one test per hermetic-home block
    - `109-PATTERNS.md` §*Pattern Assignment — the one new file* — every helper quoted verbatim from the analog, plus which imports to drop and which to add
    - `109-PATTERNS.md` §*Drift found* item D-1 — the analog's `node:fs/promises` import line has no `stat`; copying it verbatim produces a typecheck error on the window assertion
    - `tests/integration/transaction-lifecycle-cascade.test.ts` lines 1-135 (imports, `makeCtx`, `withHermeticHome`, the seeder, the state seeding) and 136-195 (the test-body wrapper and the install call with its assertion-message idiom)
    - `scripts/check-corresponding-tests.mjs` lines 1-30 — the exempt-roots set
    - `.claude/rules/typescript-comments.md`
  </read_first>
  <action>
Create `tests/integration/workflow-kind-inversion.test.ts`, modelled on
`tests/integration/transaction-lifecycle-cascade.test.ts`. `tests/helpers/` does not exist on this
branch and the unit-suite glob excludes it, so the analog declares every helper locally and the new
file must do the same — declare, do not import.

1. Imports. Take `assert` from `node:assert/strict`; `mkdir`, `mkdtemp`, `rm`, `stat` and
   `writeFile` from `node:fs/promises`; `tmpdir` from `node:os`; `path`; `test`. Then `pathSource`
   from the domain source module, `installPlugin` from the plugin install orchestrator, and
   `locationsFor` from the persistence locations module. Then the two Pi host types as a type-only
   import, grouped last per the import-order rule. Add `stat` — the analog does not import it and
   the window assertion needs it. Do NOT import `readFile`, `reinstallPlugin`, `updatePlugins` or
   `uninstallPlugin`: this file uses none of them, and `noUnusedLocals` plus the unused-vars rule
   both make an unused import an error.

2. `makeCtx`. Copy the analog's helper as-is, including the `NotifyRecord` interface. It captures
   every `ctx.ui.notify` call with its optional severity argument so the test can assert on rendered
   bytes.

3. `withHermeticHome`. Copy the analog's helper as-is and change only the `mkdtemp` prefix to one
   naming this file. It creates a temp directory, overrides `process.env.HOME`, and restores or
   deletes the previous value in a `finally`. This is what isolates the host engine's storage root:
   `os.homedir()` honours `HOME` on this platform, measured. Because the override is process-wide,
   declare exactly ONE `test(...)` inside the hermetic block, as the analog does.

4. `seedWorkflowPlugin`. Copy the analog's seeder and make two changes: drop the required hooks
   configuration field from its options object, and replace the hooks write with a write of one
   JavaScript file under a workflows directory at the plugin root. Keep everything else, in
   particular the skill seed with its comment about staging something visible — a plugin that
   stages nothing makes the success assertion much weaker. Keep the plugin manifest write, the
   marketplace manifest write, and the whole state-seeding block including its deliberate dynamic
   import of the state-io module. The fixture's plugin name, marketplace name and script name are at
   the executor's discretion; the analog's `hello` / `mp` pair is a reasonable default.

5. The test. One `test(...)` inside `withHermeticHome`, titled with both `WINV-02` and `D-109-06`
   and naming what it proves: a workflow-bearing plugin installs with no partial flag and writes no
   workflow artifact. Create a working directory with `mkdtemp`, wrap the body in `try` / `finally`
   with an `rm` cleanup, seed the fixture, build a context, then call the install orchestrator for
   project scope with the seeded marketplace and plugin — and NO partial flag anywhere in the call.
   Do NOT copy the analog's hooks routing-state reset: no hooks routing state is engaged here.
   Do NOT copy its `fileExists` helper: the window assertion uses a rejected `stat` instead.

6. Assertions, in two labelled groups.

   WINV-02: join the captured notification messages and assert the joined summary contains the
   clean installed status token; and that it does NOT contain the failed token, the
   partially-available token, the partial-flag string, or the retired reason brace. Every
   assertion carries a third-argument message interpolating the joined summary, in the analog's
   `expected X; got: ${summary}` form — never a bare assertion.

   D-109-06: assert that a `stat` of the host engine's storage root REJECTS with the
   file-not-found error code. The root is the `.pi/workflows` directory under the hermetic home;
   compose it from the overridden HOME value with `path.join`, because no locations getter for it
   exists on this branch — that getter is Phase 110's deliverable and adding it here would be an
   unused export and a boundary widening. Comment this assertion to say the kind resolves supported
   while no bridge materializes it, and anchor the comment on `D-109-06`. Do not write that the
   assertion will be inverted later — the carry-forward belongs in the SUMMARY and in Phase 111's
   CONTEXT, not in a source comment.

Use the project's `// arrange`, `// act`, `// assert` markers. Test titles and comments cite
requirement and decision ids only.
  </action>
  <verify>
    <automated>node --test tests/integration/workflow-kind-inversion.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count, or `ℹ tests` reports `0` — a zero-test run means the file was written but its `test(...)` never registered</fails_when>
    <automated>npm run test:integration</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — a sibling integration test failing here means the hermetic HOME override leaked outside its block</fails_when>
    <automated>npm run test:corresponding && npm run test:corresponding:negative</automated>
    <fails_when>non-zero exit — a failure means the new file was placed outside the exempt `integration` root and is being asked for a production pair it cannot have</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`; an error naming `stat` means the analog's import line was copied verbatim without adding it</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch tests/integration/workflow-kind-inversion.test.ts` exits 0 after staging — the file is tracked at the intended path
    - the file declares `makeCtx`, `withHermeticHome` and a workflow-plugin seeder locally and imports nothing from `tests/helpers`
    - `grep -c 'stat' tests/integration/workflow-kind-inversion.test.ts` prints at least `2` — the import and the window assertion
    - `grep -c 'reinstallPlugin\|updatePlugins\|uninstallPlugin\|readFile\|resetRoutingState\|fileExists' tests/integration/workflow-kind-inversion.test.ts` prints `0`
    - `grep -c 'test(' tests/integration/workflow-kind-inversion.test.ts` prints `1` — exactly one test inside the hermetic-home block
    - `grep -c 'partial' tests/integration/workflow-kind-inversion.test.ts` counts only occurrences inside assertions and titles; the `installPlugin` call passes no partial option
    - `grep -c 'WINV-02' tests/integration/workflow-kind-inversion.test.ts` prints at least `1` and `grep -c 'D-109-06'` prints at least `1`
    - the file contains all three of the `// arrange`, `// act` and `// assert` markers
    - no comment or test title contains a `Phase`, `Plan`, `Wave` or `Pitfall` token
  </acceptance_criteria>
  <done>A real install of a workflow-bearing plugin succeeds with no partial flag and renders the clean installed row, the host engine's storage root does not exist afterwards, and the integration suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the documentation sweep and take the phase through the full gate</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - `109-RESEARCH.md` §*Complete documentation sweep (WINV-05)* (lines 869-891) — every tracked non-planning markdown file, counted and classified, with the verdict that `docs/output-catalog.md` is the whole surface
    - `109-RESEARCH.md` §*Pitfall 7* (lines 1050-1062) — why `CHANGELOG.md` is left intact
    - `109-RESEARCH.md` §*Pitfall 9* (lines 1074-1085) — markdown is normalized by mdformat, never prettier, and the byte gate is re-run AFTER the hook pass
    - `109-RESEARCH.md` §*Assumptions Log* items A1, A2 and A3 — the pre-commit hook assumption, the mdformat reflow risk, and the SonarCloud duplication risk that sits outside the local gate
    - `109-04-SUMMARY.md` — the red-then-green pairs table this task reviews
    - `docs/output-catalog.md` — the surviving workflow mentions after `109-01`
    - `CLAUDE.md` §Git — the worktree trufflehog path and the `pre-commit run --all-files` rule
  </read_first>
  <action>
No code change is expected here. This task confirms the WINV-05 sweep landed and takes the phase
through Success Criterion 6.

1. Re-read every surviving mention of the word in `docs/output-catalog.md` and confirm each states
   the post-inversion meaning. `109-01` corrected five prose lines and turned three blocks; nothing
   else under `docs/` claims a workflow-bearing plugin degrades, and 109-RESEARCH counted and read
   every tracked non-planning markdown file to establish that. Correct anything that survived. If
   nothing needs correcting, change no byte and say so in the SUMMARY — a no-op is the expected
   outcome and is not a skipped step.

2. Leave `CHANGELOG.md` alone. Its two workflow bullets sit under a RELEASED version heading whose
   number matches both the manifest version and the extension version constant, so they are a
   record of what that version shipped, not live prose. The inversion belongs in the NEXT version's
   entry, which is release work the workstream STATE already carries. `CHANGELOG.md` is not under
   `docs/`, so WINV-05 does not reach it. Record the choice in the SUMMARY so a later reader does
   not read the untouched entry as an oversight.

3. Run `pre-commit run --all-files`, not a scoped run: CI runs the whole pipeline over the whole
   tree, and a scoped run hides pre-existing violations until the Lint job fails. Expect the
   markdown hooks to touch `docs/output-catalog.md`; a hook that rewrites a file leaves the commit
   NOT made, so re-stage and re-run rather than amending. Then re-run the byte gate AFTER the hook
   pass — a reflow that touched a fenced block shows up there and nowhere else.

4. Run the gate chain. `npm run check` chains nine gates and the type-aware ESLint pass alone has
   been measured to exceed a ten-minute foreground budget, so run the fast members as one chain and
   ESLint separately with an extended budget or in the background. Read the fallow output by exit
   code, not by glyph: its health sub-gate prints a cross glyph beside a zero-above-threshold line
   and still exits zero.
  </action>
  <verify>
    <automated>grep -o '{workflows}' docs/output-catalog.md | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a surviving reason brace means a prose line still promises the retired token</fails_when>
    <automated>grep -oE '44-member|44-entry' docs/output-catalog.md | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a surviving count narrative contradicts the tuple</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/architecture/partial-vocabulary-guard.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports a nonzero `ℹ fail` count — run this AFTER `pre-commit run --all-files`, so a markdown reflow that disturbed a fenced block is caught</fails_when>
    <automated>npm run typecheck && npm run fallow && npm run format:check && npm run test:corresponding && npm run test:corresponding:negative && npm run test:coverage:direct:negative && npm test && npm run test:integration</automated>
    <fails_when>non-zero exit — the chain short-circuits at the first failing gate, and the last npm script echoed before the failure names it. A `fallow health` line reading zero above threshold is informational and exits zero; judge by the exit code, not the glyph.</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output contains any line matching `error` — ESLint prints nothing on a clean run. This pass is type-aware and slow; run it with an extended budget or in the background rather than assuming a timeout is a failure.</fails_when>
    <human-check>
      **Two verifications no automated run can attest to. Both are recorded as manual-only in
      `109-VALIDATION.md`.**

      1. **Success Criterion 4 — the observation sequence.** Open the
         `Red-then-green pairs (Success Criterion 4)` table in `109-04-SUMMARY.md` and confirm it
         has one row per locking gate — the byte gate, the ordered reason enumeration, both length
         pins, the supported-kind tuple pin, and the resolver owner tests — and that each row cites
         a captured RED from `109-01-SUMMARY.md` and a matching GREEN. The obligation is about the
         sequence, not the end state: a suite that is green at the end of the phase is exactly what
         the criterion says is insufficient. Confirm in particular that the byte gate's red was
         observed with the documentation half turned and production still untouched — that is the
         one gate whose red is unobtainable in any other order.

      2. **WINV-05 — the prose read.** Run `grep -n -i workflow docs/output-catalog.md` and read
         every hit. Each surviving mention must state the post-inversion meaning: a workflow-bearing
         plugin installs cleanly, and the rejection state's brace names a different component kind.
         No gate covers this — the vocabulary guard scans that file for retired force and
         unsupported vocabulary, not for workflows prose, and the byte gate covers the fenced blocks
         but not the paragraphs around them.

      **Also confirm, and record the answers in the phase VERIFICATION:** no release was cut from
      this branch, and no version number was bumped in the manifest, the Sonar properties or the
      extension version constant. Both are D-109-06 and A-03 obligations that stay open until Phase
      111 lands.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -o '{workflows}' docs/output-catalog.md | wc -l` prints `0`
    - `grep -oE '44-member|44-entry' docs/output-catalog.md | wc -l` prints `0`
    - `git diff --stat CHANGELOG.md` is empty — the released entry is untouched
    - `git diff --stat package.json sonar-project.properties extensions/pi-claude-marketplace/shared/extension-version.ts` is empty — no version was bumped inside the window
    - `pre-commit run --all-files` exits 0 on a re-run after any hook rewrite, with no file left modified
    - `node --test tests/architecture/catalog-uat.test.ts` is green when run AFTER the `pre-commit` pass
    - the gate chain exits 0 and `npm run lint` exits 0
    - `git ls-files --error-unmatch tests/integration/workflow-kind-inversion.test.ts` exits 0
  </acceptance_criteria>
  <done>Every surviving workflows mention under `docs/` states the post-inversion meaning, the released changelog entry and every version number are untouched, and the whole gate chain including ESLint is green.</done>
</task>

</tasks>

<verification>
```bash
pre-commit run --all-files
node --test tests/architecture/catalog-uat.test.ts
node --test tests/architecture/partial-vocabulary-guard.test.ts
node --test tests/integration/workflow-kind-inversion.test.ts
npm run typecheck && npm run fallow && npm run format:check && npm run test:corresponding && npm run test:corresponding:negative && npm run test:coverage:direct:negative && npm test && npm run test:integration
npm run lint
```

All green. Order matters at one point only: run the byte gate AFTER `pre-commit`, so a markdown
reflow that disturbed a fenced block is caught rather than committed.

**Known risk outside this gate chain (109-RESEARCH assumption A3).** The turned install-success
fixture is byte-identical to the existing `success` state's fixture. `fallow dupes` does not flag
it — measured at the same duplicated-line count before and after — but SonarCloud uses a different
duplication engine, is not part of `npm run check`, and `tests/architecture/catalog-uat.test.ts` is
not among the seven files already listed in the Sonar duplication exclusions. If a Sonar PR quality
gate later reports it, the fix is an exclusion entry with a justification, never a distinguishing
token added to the fixture to make the two differ.
</verification>

<success_criteria>
- A real install of a workflow-bearing plugin succeeds with no partial flag and renders the clean installed row
- The host engine's storage root does not exist after that install, pinning the D-109-06 window as a fact rather than a gap
- The new integration test is tracked at `tests/integration/workflow-kind-inversion.test.ts` and needs no production pair
- No `docs/` prose states that a workflow-bearing plugin degrades
- The released changelog entry and every version number are untouched
- `npm run check`, run as a split chain, is green end to end
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/109-kind-inversion/109-05-SUMMARY.md` when done.

The SUMMARY must carry, verbatim:

1. The D-109-06 window statement — between this phase and Phase 111 a workflow-bearing plugin
   resolves installable, renders a clean installed row, and materializes zero workflow commands,
   which is less honest than the pre-inversion behavior. Cut no release from this branch before
   Phase 111 lands. The window has no external surface: this is a feature branch, CI carries no
   push trigger for it, and the milestone runs end to end in one session.
2. The A-03 corollary — do not bump the manifest version, the Sonar project version or the
   extension version constant inside the window, because the load-time backfill scan is gated on
   the recorded extension version matching the current one and a bump would fire a convergence with
   no bridge behind it.
3. The Phase 111 carry-forward — the no-artifact half of the new integration test MUST be inverted
   when the bridge lands. Add it to Phase 111's CONTEXT. An assertion that quietly stays green while
   meaning the opposite is worse than no assertion.
4. The Pitfall 7 note — the released changelog entry is left intact deliberately; the inversion
   belongs in the next version's entry.
5. The Sonar duplication risk (assumption A3) and the rule that it is resolved by an exclusion with
   a justification, never by editing the fixture.
6. The confirmed answers to the two `<human-check>` items.
</output>
