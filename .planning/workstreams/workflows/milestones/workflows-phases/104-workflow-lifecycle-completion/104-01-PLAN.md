---
phase: 104-workflow-lifecycle-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/workflow-home.ts
  - tests/orchestrators/plugin/install-workflows.test.ts
  - tests/orchestrators/plugin/update.test.ts
autonomous: true
requirements: [WLIF-02, WLIF-03]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A suite other than install-workflows.test.ts can obtain a hermetic HOME plus a relocated workflow storage root by importing one exported helper, without redeclaring the save/restore body."
    - "update.test.ts's marketplace fixture builder can seed a plugin carrying two or more named workflow scripts with distinct bodies, addressed by file name."
    - "makePluginRecord seeds resources.workflows from its caller's override instead of always writing an empty array."
    - "npm run check is green after this plan with no production source file changed."
  artifacts:
    - "tests/helpers/workflow-home.ts — new exported hermetic-workflow-home helper"
    - "tests/orchestrators/plugin/update.test.ts — array-shaped workflows fixture option and a workflows-honoring makePluginRecord"
  key_links:
    - "tests/helpers/workflow-home.ts -> setWorkflowHomeDirForTesting (the injectable seam the helper drives)"
    - "update.test.ts seedPathMarketplace -> the workflows directory the update triad reads"
  prohibitions:
    - "The extracted helper must not leave the process HOME or the workflow-home seam mutated when the callback throws — a leaked seam silently redirects a later suite's writes at the developer's real ~/.pi/workflows."
---

<objective>
Wave 0 enabling work. Three of this phase's five requirements cannot express their own
test cases against the fixtures that exist today: the update fixture builder writes one
fixed file behind a boolean, `makePluginRecord` ignores a `workflows` override, and the
hermetic workflow-home helper lives inside a single suite that four more suites need.

This plan changes **no production source**. It exists because `sonarjs/no-identical-functions`
is an ESLint **error** in this repo, so the four copies the rest of the phase would
otherwise make will fail the build — extraction is required, not stylistic.

The phase's tracer is plan 104-02 Task 1; this plan is the enabling slice that lets the
tracer's verification be a real automated assertion rather than a promise.

Purpose: make the phase able to test itself.
Output: one new test helper module and two amended fixture builders.

**Phase-wide probe-edge tally (recorded once, here):** 22 applicable edges, all resolved
auto-`resolved` (verification: explicit); 0 flagged assumptions; 0 silent drops. They are
authored into the `must_haves.truths` of plans 104-02 through 104-06, allocated by the
requirement that owns them (WLIF-02 -> 104-04, WLIF-03 -> 104-02, WLIF-04 -> 104-05,
WLIF-05 -> 104-03, WLIF-06 -> 104-06). This plan carries none: it implements no requirement
behavior.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-PATTERNS.md
@.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-VALIDATION.md
@tests/helpers/git-mock.ts
@tests/orchestrators/plugin/install-workflows.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the hermetic workflow-home helper to tests/helpers/</name>
  <files>tests/helpers/workflow-home.ts, tests/orchestrators/plugin/install-workflows.test.ts</files>
  <read_first>
    - `tests/orchestrators/plugin/install-workflows.test.ts` lines 40-100 — the `withHermeticHome`
      body to lift verbatim, including its `finally` save/restore discipline
    - `tests/helpers/git-mock.ts` lines 1-25 — the block-header convention for a helper module
      (what the double is, why it exists, the strategy)
    - `tests/helpers/credential-mock.ts` header — why helper modules use type-only imports of
      production types
    - `.planning/codebase/CONVENTIONS.md` "Import Organization" and "Function Design" —
      `import-x/order` groups, `explicit-module-boundary-types` is an error
  </read_first>
  <action>
    Create `tests/helpers/workflow-home.ts` exporting a single named async function
    `withHermeticWorkflowHome`. Lift the body of `withHermeticHome` from
    `install-workflows.test.ts` unchanged: `mkdtemp` under `tmpdir()`, save `process.env.HOME`,
    set it to the temp dir, derive `workflowHome` as `.pi/workflows` under it, call
    `setWorkflowHomeDirForTesting(workflowHome)`, run the callback inside a `try`, and in the
    `finally` call `setWorkflowHomeDirForTesting(undefined)`, restore or `delete process.env.HOME`,
    and `rm` the temp dir with `{ recursive: true, force: true }`.

    Parameterize the `mkdtemp` prefix rather than hard-coding `install-wf-home-`: take a first
    parameter `prefix: string` and pass the callback second, so each suite names its own temp
    directories. Give the module an explicit generic return type `Promise<T>` and a block header
    stating what the double is (a relocated engine storage root plus a hermetic HOME), why it
    exists (the assertions must name a path the test owns outright), and that the `finally`
    restore is the load-bearing half.

    Repoint `install-workflows.test.ts` at the extracted helper: delete its local
    `withHermeticHome`, import `withHermeticWorkflowHome` from `../../helpers/workflow-home.ts`,
    and pass `"install-wf-home-"` as the prefix at every call site so the existing temp-directory
    naming is preserved. Remove any import that the deletion orphans (`mkdtemp`, `tmpdir`,
    `setWorkflowHomeDirForTesting` if no longer referenced) — `noUnusedLocals` is on.

    Cite `WLIF-03` and `NFR-3` in the header comment as the traceability anchors. Never cite a
    GSD process artifact (see `.claude/rules/typescript-comments.md`).
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install-workflows.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/helpers/workflow-home.ts tests/orchestrators/plugin/install-workflows.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/orchestrators/plugin/install-workflows.test.ts` passes with the same test
      count as before the change.
    - `npm run typecheck` exits 0.
    - `npx eslint tests/helpers/workflow-home.ts tests/orchestrators/plugin/install-workflows.test.ts`
      reports zero errors (specifically no `sonarjs/no-identical-functions`, no
      `@typescript-eslint/no-unused-vars`).
    - `grep -c 'async function withHermeticHome' tests/orchestrators/plugin/install-workflows.test.ts`
      returns 0 — the local copy is gone, not shadowed.
    - `grep -v '^\s*\(//\|\*\)' tests/helpers/workflow-home.ts | grep -c 'setWorkflowHomeDirForTesting(undefined)'`
      returns at least 1 — the seam is reset in the `finally`, not only on the success path.
  </acceptance_criteria>
  <done>
    `tests/helpers/workflow-home.ts` exists, exports `withHermeticWorkflowHome` with an explicit
    return type and a caller-supplied temp-directory prefix, and `install-workflows.test.ts`
    consumes it with no local duplicate remaining.
  </done>
</task>

<task type="auto">
  <name>Task 2: Teach update.test.ts's fixtures to express the workflow triad</name>
  <files>tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
    - `tests/orchestrators/plugin/update.test.ts` lines 110-135 (`makePluginRecord`) and lines
      185-260 (`seedPathMarketplace`, its `hasWorkflows` boolean and the fixed-body write)
    - `tests/orchestrators/plugin/install-workflows.test.ts` lines 85-140 — `seedWorkflowPlugin`'s
      `workflows?: { fileName: string; source: string }[]` option, the exact shape to copy, and
      `NAMED_SCRIPT` for the `meta.name` body form
    - `.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-PATTERNS.md`
      section "tests/helpers/workflow-home.ts (NEW)" — the "Also in the fixture layer" paragraph
  </read_first>
  <action>
    In `tests/orchestrators/plugin/update.test.ts`:

    Replace `seedPathMarketplace`'s boolean `hasWorkflows?: boolean` spec member with an array
    member `workflows?: { fileName: string; source: string }[]`, copying the option shape from
    `seedWorkflowPlugin` in `install-workflows.test.ts`. When the array is present, create the
    plugin's `workflows/` directory and write each entry's `source` to its `fileName`; when it is
    absent, write no directory at all. Update every existing `hasWorkflows: true` call site to pass
    a one-element array whose body is the current fixed body, so the existing assertions keep their
    meaning. Delete the now-false comment claiming the directory existing is the whole signal and
    that nothing reads the body — the triad reads `meta.name` out of the body.

    Change `makePluginRecord`'s `workflows: []` to `workflows: resources.workflows ?? []`, matching
    the `?? [...]` form its four sibling fields already use, so a caller can seed a record naming
    a prior version's workflows.

    Do not add any new test case in this task — the triad case lands with the update wiring in
    plan 104-04. This task only removes the fixture blockers.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/orchestrators/plugin/update.test.ts` passes with the same test count as
      before the change.
    - `npm run typecheck` exits 0.
    - `grep -c 'hasWorkflows' tests/orchestrators/plugin/update.test.ts` returns 0.
    - `grep -v '^\s*\(//\|\*\)' tests/orchestrators/plugin/update.test.ts | grep -c 'workflows: resources.workflows ?? \[\]'`
      returns 1.
    - A throwaway node check (not committed) constructing `makePluginRecord` with
      `{ workflows: ["acme:ship"] }` yields a record whose `resources.workflows` deep-equals
      `["acme:ship"]`.
  </acceptance_criteria>
  <done>
    `seedPathMarketplace` accepts an array of named workflow scripts with distinct bodies,
    `makePluginRecord` honors a `workflows` override, and the existing update suite is green.
  </done>
</task>

</tasks>

## Artifacts this phase produces

This plan's contribution, followed by the phase-wide roll-up every later plan adds to.

**This plan creates:**

| Symbol | Location | Kind |
|---|---|---|
| `withHermeticWorkflowHome(prefix, fn)` | `tests/helpers/workflow-home.ts` | new exported test helper (new module) |
| `seedPathMarketplace` spec member `workflows?: { fileName, source }[]` | `tests/orchestrators/plugin/update.test.ts` | changed fixture signature (replaces `hasWorkflows?: boolean`) |
| `makePluginRecord` honoring `resources.workflows` | `tests/orchestrators/plugin/update.test.ts` | changed fixture behavior |

**Phase-wide roll-up (every symbol this phase creates or changes):**

| Symbol | Location | Plan | Kind |
|---|---|---|---|
| `withHermeticWorkflowHome` | `tests/helpers/workflow-home.ts` | 104-01 | new test helper module |
| `UnstageOutcome.dropped.workflows` | `orchestrators/marketplace/shared.ts` | 104-02 | new required interface key (6th axis) |
| `WorkflowsUnstageFailureError` | `orchestrators/marketplace/shared.ts` | 104-02 | new typed error class |
| `UnstageWorkflowFailure` | `bridges/workflows/types.ts` | 104-02 | new exported type |
| `UnstageWorkflowsResult.failed` | `bridges/workflows/types.ts` | 104-02 | new required result member |
| `applyPartialCascadeFold` workflows axis | `orchestrators/plugin/shared.ts` | 104-02 | changed parameter shape (both literals) |
| install ledger `previousWorkflowNames` wiring | `orchestrators/plugin/install.ts` | 104-03 | changed call site |
| WR-06 ownership refusal | `bridges/workflows/stage.ts` | 104-03 | new guard in the commit rename loop |
| `PrepHandles.workflows` | `orchestrators/plugin/update.ts` | 104-04 | new required interface member |
| `PHASE3_FAILURE_PHASES` gains `"workflows"` | `orchestrators/plugin/update.ts` | 104-04 | widened closed tuple (5 -> 6) |
| `Phase3Failure["phase"]` gains `"workflows"` | `shared/errors.ts` | 104-04 | widened closed union (5 -> 6) |
| `PreparedHandles.workflows` / `PartialPreparedHandles.workflows` | `orchestrators/plugin/reinstall.ts` | 104-05 | new interface members |
| `resourcesFromHandles` LOSES `previousWorkflows` | `orchestrators/plugin/reinstall.ts` | 104-05 | **removed** parameter (compile-forces both call sites) |
| `"stale workflow command"` | `shared/notify.ts` `REASONS` | 104-06 | new closed-set reason token (tail, 38 -> 39) |
| `CommandPrivateReason` gains `"stale workflow command"` | `shared/notify-reasons.ts` | 104-06 | widened topic union |
| `PluginUninstalledMessage.reasons?` | `shared/notify.ts` | 104-06 | new optional field on a previously reasons-less variant |
| catalog state `stale-workflow-command` | `docs/output-catalog.md` uninstall H2 | 104-06 | new byte-gated catalog state + fixture |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> developer `$HOME` | The helper relocates `process.env.HOME` and the engine's workflow-home seam; a leaked restore points a later suite's writes at the developer's real `~/.pi/workflows/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-01-01 | Tampering | `tests/helpers/workflow-home.ts` | medium | mitigate | The seam reset and the `HOME` restore live in a `finally`, lifted verbatim from the proven original; the acceptance criterion greps for the reset outside comments |
| T-104-01-02 | Denial of Service | `tests/helpers/workflow-home.ts` | low | accept | A `mkdtemp` directory leaked by a hard process kill accumulates under `tmpdir()`; the OS reclaims it and no assertion depends on its absence |
| T-104-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this plan or this phase; `package.json` and `package-lock.json` are unchanged, so the package-legitimacy gate has no input to audit |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `node --test tests/orchestrators/plugin/install-workflows.test.ts tests/orchestrators/plugin/update.test.ts` passes.
- `npx eslint tests/helpers/workflow-home.ts tests/orchestrators/plugin/install-workflows.test.ts tests/orchestrators/plugin/update.test.ts` reports zero errors.
- `git diff --name-only` lists no file under `extensions/` — this plan changes no production source.
</verification>

<success_criteria>
Four more suites can obtain a hermetic workflow home from one import, the update fixture
builder can express two named scripts with distinct bodies, `makePluginRecord` honors a
`workflows` override, and nothing under `extensions/` changed.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-01-SUMMARY.md` when done
</output>
