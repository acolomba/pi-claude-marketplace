---
phase: 104-workflow-lifecycle-completion
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/bridges/workflows/unstage.ts
  - extensions/pi-claude-marketplace/bridges/workflows/types.ts
  - extensions/pi-claude-marketplace/bridges/workflows/index.ts
  - extensions/pi-claude-marketplace/persistence/state-io.ts
  - tests/orchestrators/marketplace/cascade.test.ts
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/live-uat/workflow-storage-canary.mjs
  - tests/live-uat/README.md
autonomous: true
requirements: [WLIF-03, WLIF-05]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After `uninstall` of a workflow-bearing plugin in user scope, the engine's saved directory holds no file whose name starts with that plugin's prefix, and the plugin's state record is gone."
    - "After `uninstall` of a workflow-bearing plugin in project scope, the derived `projects/<key>/saved/` directory holds no file whose name starts with that plugin's prefix."
    - "`disable` physically removes the plugin's workflow envelopes — the one component kind that executes code stops being registered and runnable — while the disabled record preserves `resources.workflows` verbatim."
    - "`marketplace remove --cascade` removes workflow envelopes for every plugin it uninstalls, with no call site of its own."
    - "A non-ENOENT unlink failure is reported through a typed error carrying the failing names and reasons, and the envelopes removed before it are still reported in `dropped.workflows`."
    - "A workflows unstage failure takes the partial-fold path: the shrunken inventory is persisted so a retry knows which envelopes are still on disk, rather than aborting the save."
    - "The live canary asserts an empty per-plugin saved set in BOTH scopes between the uninstall and the sandbox teardown, so an envelope the uninstall failed to remove fails the run."
    - "WLIF-03/adjacency: removal is strictly by recorded name and never enumerates the shared saved directory, so uninstalling plugin `acme` leaves a neighbouring `acme-tools:` envelope and a user's own hand-saved workflow untouched even when the names share a prefix."
    - "WLIF-03/empty: a plugin whose recorded `resources.workflows` is empty (including a record predating the workflows bridge) runs the sixth cascade slot, reports `dropped.workflows` as empty, raises no error, and takes no special-case branch."
    - "WLIF-03/ordering: the workflows slot runs after `mcp` and its key is declared last in `dropped`, so cascade order and declaration order still match; the reported `dropped.workflows` preserves the recorded inventory order for the names actually unlinked."
    - "WLIF-03/idempotency: `uninstall` re-run after a partial failure is a no-op per already-absent envelope — ENOENT is skipped without entering `removed`, so a retry neither errors nor double-reports (NFR-3)."
    - "WLIF-03/concurrency: two processes uninstalling the same plugin in the same scope serialize on the `proper-lockfile` state guard (`retries: 0`); the loser raises `StateLockHeldError` before any unlink, so exactly one cascade removes the envelopes."
  artifacts:
    - "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts — sixth cascade slot, `dropped.workflows`, `WorkflowsUnstageFailureError`"
    - "extensions/pi-claude-marketplace/bridges/workflows/types.ts — `UnstageWorkflowFailure` and `UnstageWorkflowsResult.failed`"
    - "extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts — the workflows axis on `applyPartialCascadeFold`"
    - "tests/live-uat/workflow-storage-canary.mjs — the removal assertion section inside `main`'s `try`"
  key_links:
    - "cascadeUnstagePlugin -> unstagePluginWorkflows (supplied from `installedPlugin.resources.workflows`, the only inventory naming files outside every scope root)"
    - "cascadeUnstagePlugin -> applyPartialCascadeFold (the sixth `dropped` key must have a matching filter line or a partial failure leaves a ghost record)"
    - "uninstall.ts cascade-failure branch -> WorkflowsUnstageFailureError (falls through to the fold, NOT the AG-5 abort-save carve-out)"
  prohibitions:
    - "Removal must never report success it did not achieve: a record, an outcome or a rendered row must not claim a workflow envelope is gone while the file is still on disk and runnable by the host engine."
    - "The removal path must never enumerate, read or unlink anything in the shared saved directory that is not named in this plugin's own recorded inventory — that directory holds the user's own hand-saved workflows and every other plugin's."
    - "Breadcrumb (canon security, dropped per referral): path traversal via a crafted recorded name in a hand-edited state.json — already owned by `assertSafeName` + `assertPathInside` inside `locations.workflowArtifactPath`."
    - "Breadcrumb (canon security, dropped per referral): symlink-following on the target path — already owned by `pathExists`'s lstat-based, non-following predicate (PS-1)."
---

<objective>
The phase's tracer and its expansion. Today `cascadeUnstagePlugin` calls five bridges, not
six: `uninstall` orphans workflow envelopes permanently (they sit outside every scope root
and `removePluginRecord` deletes the only inventory naming them), and `disable` does not
disable the one component kind that executes code.

Task 1 is the **tracer**: the thinnest end-to-end path through every layer this phase
touches on the removal side — bridge, cascade primitive, partial-cascade fold, verb, state
record — proven by a real user-scope `uninstall` that leaves the engine's saved directory
empty. Tasks 2 and 3 expand horizontally from that proven slice: the structured failure
report and the second scope, then the live-engine proof.

The message layer (WLIF-06) is deliberately **not** in the tracer. It carries no
architectural risk — `"malformed command"` is an exact, recent, in-file analog for adding a
closed-set token — and folding it in would double the slice without proving anything the
analog has not already proven. It lands whole in plan 104-06.

Purpose: stop leaking executable code.
Output: a six-bridge cascade, a structured unstage failure, and a live canary that can see
the orphan it is best placed to catch.

**Probe edges authored here:** 5 of 22 (all WLIF-03), all auto-`resolved`
(verification: explicit). 0 flagged assumptions, 0 silent drops.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-PATTERNS.md
@.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-RESEARCH.md
@.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-CONTEXT.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The sixth cascade slot — uninstall actually removes the envelope, end to end</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, extensions/pi-claude-marketplace/persistence/state-io.ts, tests/orchestrators/marketplace/cascade.test.ts, tests/orchestrators/marketplace/remove.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/live-uat/README.md</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` lines 290-425 — the
      whole primitive: the `UnstageOutcome` interface, the mutable `dropped` accumulator, the five
      bridge calls in cascade order, and the TWO identical frozen return literals (success and
      catch)
    - The `hooks` slot and the `mcp` slot in that same function — the sibling shapes to copy, and
      the two supply styles already present (`skills`/`commands` read the recorded inventory;
      `agents`/`mcp` re-derive from marketplace + plugin name)
    - `extensions/pi-claude-marketplace/bridges/workflows/unstage.ts` — the whole file; the
      idempotent by-recorded-name removal contract this slot calls
    - `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` lines 845-890 —
      `applyPartialCascadeFold`, both parameter literals and the `hooks` filter line added for
      exactly this reason
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 1153-1200 — the
      workflows ledger phase, so the test can seed a real install
    - `tests/orchestrators/marketplace/cascade.test.ts` — the whole-object `deepEqual` of
      `outcome.dropped`
    - `tests/orchestrators/plugin/uninstall.test.ts` and `tests/orchestrators/marketplace/remove.test.ts`
      — every `typeof cascadeUnstagePlugin` stub literal (roughly a dozen across the three suites)
    - `tests/helpers/workflow-home.ts` (created in plan 104-01) — the hermetic-home helper the new
      integration test drives
    - `104-PATTERNS.md` section "orchestrators/marketplace/shared.ts — the sixth cascade slot",
      including its stale-count table
  </read_first>
  <behavior>
    - After a real `installPlugin` of a workflow-bearing plugin in user scope followed by a real
      `uninstallPlugin`, the hermetic workflow saved directory contains no entry whose name starts
      with `<plugin>:`.
    - The same uninstall removes the plugin's state record.
    - `cascadeUnstagePlugin` returns `dropped.workflows` naming exactly the envelopes it unlinked.
    - A plugin whose recorded `resources.workflows` is empty cascades cleanly and reports an empty
      `dropped.workflows` — no error, no special-case branch.
    - `applyPartialCascadeFold` subtracts `dropped.workflows` from `installed.resources.workflows`.
  </behavior>
  <action>
    Add `workflows` as the sixth axis of the cascade primitive in
    `orchestrators/marketplace/shared.ts`, appended after `mcp` at every one of the five places the
    axis is spelled: the `UnstageOutcome.dropped` interface, the mutable `dropped` accumulator
    literal, the bridge call sequence, and BOTH frozen return literals (the `ok: true` return and
    the `catch` return carry byte-identical literals; adding the key to one and not the other is
    the exact silent gap this phase exists to close).

    The slot calls `unstagePluginWorkflows({ locations, previousWorkflowNames:
    installedPlugin.resources.workflows })` and assigns `dropped.workflows =
    [...workflowsResult.removedNames]`. Take the recorded-inventory supply style, not the
    re-derive-from-names style: `installedPlugin.resources.workflows` is the only thing that names
    files living outside every scope root. Place the call after the `mcp` call, mirroring the
    install ledger's stated append-after-mcp rule, so the five proven cascade orderings ahead of it
    stay byte-unchanged.

    Add the matching axis to `applyPartialCascadeFold` in `orchestrators/plugin/shared.ts`: both
    parameter literals (`installed.resources` and `dropped`) gain a `workflows` member, and the
    body gains the filter line. The workflows names are identical on both sides, so this line
    copies the `hooks` line with the noun swapped — unlike the `dropped.commands` ->
    `resources.prompts` asymmetry the existing doc comment calls out.

    Correct every doc comment the change falsifies, in this same commit — a stale count is how the
    next reader re-introduces the gap. The sites, all named with their current wording in
    `104-RESEARCH.md` Pattern 1's table: two bridge-count sentences on `UnstageOutcome`, the
    already-stale bridge count above it, the already-stale cascade-order list on
    `cascadeUnstagePlugin`, the already-stale ordering note in `uninstall.ts`, the symmetry claim
    in `enable-disable.ts`'s disable branch, the symmetry claim in `persistence/state-io.ts`, the
    symmetry claim in `tests/orchestrators/plugin/enable-disable.test.ts`, and the inventory claim
    in `tests/live-uat/README.md`. Extend `uninstall.ts`'s field-name mapping note to name the
    workflows axis as name-identical. Leave `install.ts`'s "intersecting all five" alone — it
    counts degradation signals on the outcome type, not bridges. Cite `WLIF-03` and `NFR-3` in new
    comments; never cite a GSD process artifact.

    Add the sixth key to every `typeof cascadeUnstagePlugin` stub literal across
    `tests/orchestrators/plugin/uninstall.test.ts`, `tests/orchestrators/marketplace/remove.test.ts`
    and `tests/orchestrators/marketplace/cascade.test.ts`, and to the whole-object `deepEqual` of
    `outcome.dropped` in `cascade.test.ts`. Each is a compile error, so the compiler enumerates
    them for you — that is the mechanism working, not collateral damage.

    Write the tracer's proof as a new case in `tests/orchestrators/plugin/uninstall.test.ts`,
    driven by `withHermeticWorkflowHome` from `tests/helpers/workflow-home.ts`: seed a path-source
    marketplace whose plugin ships one named workflow script, run the real `installPlugin` in user
    scope (no cascade stub), assert the envelope exists at the engine path, run the real
    `uninstallPlugin`, then assert the saved directory holds no entry starting with the plugin
    prefix and the state record is gone. Add a second case seeding a plugin with an empty
    `resources.workflows` and asserting the cascade completes with an empty `dropped.workflows`.
    Title both with the requirement ID, never with a planning reference.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/marketplace/cascade.test.ts tests/orchestrators/marketplace/remove.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 (the sixth `dropped` key is a required member, so every stub
      literal and the `deepEqual` compile-fail until updated).
    - `node --test tests/orchestrators/plugin/uninstall.test.ts` passes, including a new case that
      performs a real install then a real uninstall and asserts an empty per-plugin saved set.
    - `grep -v '^\s*\(//\|\*\)' extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts | grep -c 'unstagePluginWorkflows'`
      returns 1 — exactly one call site, in the primitive.
    - `grep -v '^\s*\(//\|\*\)' extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts | grep -c 'workflows: Object.freeze'`
      returns 2 — both frozen return literals carry the axis.
    - `grep -v '^\s*\(//\|\*\)' extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts | grep -c 'dropped.workflows'`
      returns at least 1 — the fold subtracts the axis.
    - `grep -rniE 'all (five|FIVE) bridges' extensions/pi-claude-marketplace/ | wc -l` returns 0.
    - `grep -rniE 'symmetric across all five kinds' extensions/ tests/ | wc -l` returns 0.
    - `node --test tests/architecture/import-boundaries.test.ts tests/architecture/no-orchestrator-network.test.ts`
      passes — the new import edges did not breach a boundary gate.
  </acceptance_criteria>
  <done>
    A real user-scope `uninstall` of a workflow-bearing plugin leaves the engine's saved directory
    free of that plugin's envelopes, the cascade reports them in `dropped.workflows`, the partial
    fold subtracts them, and no bridge-count comment in the tree still claims five.
  </done>
  <reversibility rating="reversible">
    The sixth slot is an append at a documented insertion point with an in-file sibling shape;
    reverting is a delete of the same six spellings.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured unstage failure, the partial-fold path, and the second scope</name>
  <files>extensions/pi-claude-marketplace/bridges/workflows/types.ts, extensions/pi-claude-marketplace/bridges/workflows/unstage.ts, extensions/pi-claude-marketplace/bridges/workflows/index.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/orchestrators/marketplace/cascade.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/marketplace/remove.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` lines 50-64 —
      `AgentsUnstageFailureError`, the class shape to copy exactly (extends Error, `this.name`
      assignment, readonly typed public fields, doc comment citing the requirement)
    - The same file's agents raise site — how a non-empty `failed[]` becomes the typed throw
    - `extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` around the cascade
      failure branch — the `instanceof AgentsUnstageFailureError` carve-out that aborts the save,
      and the fold path everything else falls through to
    - `extensions/pi-claude-marketplace/bridges/workflows/unstage.ts` — the current non-ENOENT
      `throw err`, which becomes an accumulation
    - `extensions/pi-claude-marketplace/bridges/workflows/types.ts` — `UnstageWorkflowsResult`
    - `extensions/pi-claude-marketplace/bridges/workflows/index.ts` — the barrel's type re-exports
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` around the workflows
      phase's `undo` — the second caller of `unstagePluginWorkflows`, which must decide what a
      non-empty `failed[]` means
    - `extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` line 55 — the sanctioned
      plugin-ledger import edge onto `../marketplace/shared.ts`
    - `.planning/codebase/CONVENTIONS.md` "Error Handling"
  </read_first>
  <behavior>
    - A non-ENOENT unlink failure no longer escapes the bridge as a raw throw: it lands in a
      `failed[]` entry naming the workflow and the reason, and the loop continues.
    - `cascadeUnstagePlugin` converts a non-empty `failed[]` into `WorkflowsUnstageFailureError`
      after assigning `dropped.workflows`, so envelopes removed before the failure are still
      reported.
    - `uninstallPlugin` treats that error as a partial-cascade failure: it folds the shrunken
      inventory and saves, rather than taking the abort-save carve-out.
    - An already-absent envelope stays a silent no-op and does not enter `failed[]` or
      `removedNames`.
    - A project-scope uninstall leaves the derived `projects/<key>/saved/` directory free of the
      plugin's envelopes.
    - `marketplace remove --cascade` removes workflow envelopes with no call site of its own.
  </behavior>
  <action>
    Widen the bridge return: add an exported `UnstageWorkflowFailure` type to
    `bridges/workflows/types.ts` with readonly `name` and `reason` string members, add a required
    readonly `failed: readonly UnstageWorkflowFailure[]` member to `UnstageWorkflowsResult`, and
    re-export the new type from `bridges/workflows/index.ts`. In `unstage.ts`, replace the
    non-ENOENT `throw err` with a push onto a local `failed` accumulator carrying the name and
    `errorMessage(err)`, and continue the loop; keep the ENOENT arm exactly as it is, including its
    comment explaining why an already-absent envelope is not counted as removed. Freeze `failed`
    on the way out, matching how `removedNames` and `warnings` are frozen.

    Add `WorkflowsUnstageFailureError` to `orchestrators/marketplace/shared.ts` beside its
    precedent, with a readonly `failedWorkflows: readonly UnstageWorkflowFailure[]` field, the
    `this.name` assignment, and a doc comment citing `WLIF-03` and stating why the structured field
    exists (the leftover here is executable code; a consumer must be able to read per-name reasons
    without re-parsing prose). Raise it from the sixth cascade slot only when `failed.length > 0`,
    and only AFTER `dropped.workflows` has been assigned — the partial report is the whole point.

    Do not add a `WorkflowsUnstageFailureError` arm to `uninstall.ts`'s abort-save carve-out. The
    carve-out narrows on the agents error specifically, so the new error falls through to the
    partial-fold branch by construction, which is the decided behavior: persisting the shrunken
    inventory tells a retry which envelopes are still out there, whereas aborting the save would
    leave the record naming files it already removed. Add a short comment at the carve-out stating
    that the workflows failure deliberately takes the fold and why.

    In `install.ts`'s workflows-phase `undo`, read the result's `failed` and, when non-empty, throw
    `WorkflowsUnstageFailureError` so the ledger captures it as a rollback partial instead of the
    undo silently succeeding. Import the class from `../marketplace/shared.ts` — the same sanctioned
    edge `uninstall.ts` already uses; confirm `tests/architecture/import-boundaries.test.ts` and
    `tests/architecture/no-orchestrator-network.test.ts` stay green.

    Add these test cases:
    in `tests/orchestrators/marketplace/cascade.test.ts`, a unit case asserting `dropped.workflows`
    names the unlinked envelopes and a second asserting that a failing unlink yields
    `ok: false` with a `WorkflowsUnstageFailureError` cause whose `failedWorkflows` names the
    offender, while `dropped.workflows` still reports the earlier successes;
    in `tests/orchestrators/plugin/uninstall.test.ts`, a project-scope end-to-end case mirroring
    Task 1's user-scope case but asserting the derived `projects/<key>/saved/` path, plus a case
    asserting a retry after a partial failure is a clean no-op;
    in `tests/orchestrators/marketplace/remove.test.ts`, a case proving `--cascade` removes workflow
    envelopes for the plugins it uninstalls.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/cascade.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/marketplace/remove.test.ts tests/bridges/workflows/unstage.test.ts tests/architecture/import-boundaries.test.ts tests/architecture/no-orchestrator-network.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 with `failed` a REQUIRED member of `UnstageWorkflowsResult` (so
      every construction site is compile-forced).
    - `node --test tests/orchestrators/marketplace/cascade.test.ts` passes, including a case that
      asserts `outcome.ok === false`, `outcome.cause instanceof WorkflowsUnstageFailureError`, and a
      non-empty `outcome.dropped.workflows` in the SAME outcome.
    - `node --test tests/orchestrators/plugin/uninstall.test.ts` passes, including a project-scope
      case whose assertion path contains a `projects/` segment.
    - `grep -v '^\s*\(//\|\*\)' extensions/pi-claude-marketplace/bridges/workflows/unstage.ts | grep -c 'throw err'`
      returns 0 — the non-ENOENT throw became an accumulation.
    - `grep -v '^\s*\(//\|\*\)' extensions/pi-claude-marketplace/bridges/workflows/unstage.ts | grep -c 'ENOENT'`
      returns at least 1 — the idempotent arm survived.
    - `grep -v '^\s*\(//\|\*\)' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts | grep -c 'WorkflowsUnstageFailureError'`
      returns 0 — the workflows error is deliberately NOT added to the abort-save carve-out.
    - `node --test tests/architecture/import-boundaries.test.ts tests/architecture/no-orchestrator-network.test.ts` passes.
  </acceptance_criteria>
  <done>
    A non-ENOENT unlink failure is reported through a typed error carrying per-name reasons, the
    cascade still reports what it removed before failing, uninstall folds and saves rather than
    aborting, and both scopes are proven free of the plugin's envelopes.
  </done>
  <reversibility rating="costly">
    Widening `UnstageWorkflowsResult` with a required member changes the bridge contract; reverting
    means re-narrowing it and restoring the throw at every consumer. Chosen over wrapping the
    existing throw because only this shape lets `dropped.workflows` report partial progress, which
    is exactly what `applyPartialCascadeFold` consumes.
  </reversibility>
</task>

<task type="auto">
  <name>Task 3: The live canary asserts removal before its sandbox disappears</name>
  <files>tests/live-uat/workflow-storage-canary.mjs</files>
  <read_first>
    - `tests/live-uat/workflow-storage-canary.mjs` — the whole file, in particular the `UatExit`
      class with `fail` and `pass`, the `teardown` function (its uninstalls followed by the
      `rm -rf` of the sandbox HOME), `main`'s `try`/`finally`, the `makeStorage` helper, and the
      `proveScope` call sites with their `readCwd` arguments for the user and project halves
    - `104-PATTERNS.md` section "tests/live-uat/workflow-storage-canary.mjs — the WR-10 assertion
      window"
    - `tests/live-uat/README.md` — the inventory sentence corrected in Task 1
  </read_first>
  <action>
    Add a new numbered Removal section at the tail of `main`'s `try`, after the existing project-key
    assertion block and before the `finally`. Print the same banner shape the other sections use,
    run `ext.run` uninstall for the user-scope plugin and the project-scope plugin, then for each
    scope list the engine's storage from the cwd that scope's proof already uses — the user half
    from the same directory `proveScope` reads for user, the project half from the working
    directory — and filter for entries whose name starts with that plugin's prefix. Call `fail`
    with a distinct assertion id per scope when the filtered list is non-empty, including the
    surviving names and their paths in the detail argument; call `pass` per scope otherwise.

    Do NOT put the assertion in `teardown`. `fail` throws, `teardown` runs from a `finally`, and a
    throw there replaces any in-flight primary error — the operator would then debug the wrong
    failure. Leave `teardown` untouched: its own uninstalls become idempotent no-ops once the
    section above has run, and `ext.quiet` already swallows the not-installed row.

    Add a short comment stating why the assertion sits where it does: the teardown deletes the
    engine's storage root, so an assertion after it can only ever pass. Cite `WLIF-03`; never cite
    a GSD process artifact. Note that `tests/live-uat/` is outside the typed ESLint tree, so this
    file follows the surrounding `.mjs` style rather than the TypeScript conventions.
  </action>
  <verify>
    <automated>node --check tests/live-uat/workflow-storage-canary.mjs &amp;&amp; node -e "const s=require('node:fs').readFileSync('tests/live-uat/workflow-storage-canary.mjs','utf8');const t=s.slice(s.indexOf('async function teardown'));const b=t.slice(0,t.indexOf('\nasync function',1)>0?t.indexOf('\nasync function',1):t.length);if(/\bfail\(/.test(b)){console.error('teardown can reach fail()');process.exit(1)}console.log('ok')"</automated>
    <human-check>Run `node tests/live-uat/workflow-storage-canary.mjs` with the host engine installed. Expect the new Removal section to print one PASS line per scope. Then hand-edit the canary to skip one uninstall and confirm the run FAILS — a canary that cannot fail proves nothing.</human-check>
  </verify>
  <acceptance_criteria>
    - `node --check tests/live-uat/workflow-storage-canary.mjs` exits 0.
    - The `teardown` function body contains no `fail(` call (asserted by the automated command
      above, which fails the task if one appears).
    - `grep -c 'Removal' tests/live-uat/workflow-storage-canary.mjs` returns at least 1.
    - The new section appears before the `finally` that calls `teardown`: `node -e` extracting the
      source index of the Removal banner and of `} finally {` shows the banner index is smaller.
    - Two distinct assertion ids are used, one per scope, and both appear in the file.
  </acceptance_criteria>
  <done>
    The canary asserts an empty per-plugin saved set in both scopes between the uninstall and the
    sandbox teardown, `teardown` is unchanged, and no `fail` call is reachable from the `finally`.
  </done>
</task>

</tasks>

## Artifacts this phase produces

This plan's contribution to the phase-wide roll-up recorded in `104-01-PLAN.md`:

| Symbol | Location | Kind |
|---|---|---|
| `UnstageOutcome.dropped.workflows` | `orchestrators/marketplace/shared.ts` | new required interface key (6th axis, appended after `mcpServers`) |
| `WorkflowsUnstageFailureError` | `orchestrators/marketplace/shared.ts` | new exported typed error class, field `failedWorkflows: readonly UnstageWorkflowFailure[]` |
| `UnstageWorkflowFailure` | `bridges/workflows/types.ts` (re-exported from `index.ts`) | new exported type `{ name: string; reason: string }` |
| `UnstageWorkflowsResult.failed` | `bridges/workflows/types.ts` | new **required** result member (compile-forces every construction site) |
| `applyPartialCascadeFold` workflows axis | `orchestrators/plugin/shared.ts` | changed parameter shape on both literals + a new filter line |
| Removal assertion section | `tests/live-uat/workflow-storage-canary.mjs` | new section in `main`'s `try`, two scope-keyed assertion ids |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin artifacts -> host engine storage root | `~/.pi/workflows/saved/` is OUTSIDE every scope root; the state record is the only inventory naming what we wrote there |
| this extension -> the shared saved directory | The same directory holds the user's own hand-saved workflows and every other plugin's; the `<plugin>:` prefix is namespacing, not enforcement |
| process -> process (same scope) | Two Pi instances mutating one scope's `state.json` and its artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-104-02-01 | Elevation of Privilege | `uninstall` / `disable` leaving executable envelopes registered | high | mitigate | The sixth cascade slot removes them for all three verbs behind the primitive; the live canary asserts an empty per-plugin saved set in both scopes before its sandbox is deleted |
| T-104-02-02 | Repudiation | a swallowed unstage failure | high | mitigate | `UnstageWorkflowsResult.failed` plus `WorkflowsUnstageFailureError` carrying per-name reasons; `dropped.workflows` still reports partial progress so the record shrinks to the truth |
| T-104-02-03 | Tampering | removal touching a file this extension did not write | high | mitigate | Removal iterates ONLY the recorded inventory and never enumerates the shared directory; `locations.workflowArtifactPath` is the sole composer and runs `assertSafeName` then `assertPathInside` |
| T-104-02-04 | Tampering | a ghost record after a partial cascade — state naming files that are gone, hiding the inconsistency permanently behind ENOENT tolerance | medium | mitigate | `applyPartialCascadeFold` gains the workflows axis in the same task as the sixth `dropped` key |
| T-104-02-05 | Tampering | path traversal via a crafted recorded name in a hand-edited `state.json` | medium | mitigate | Existing control, unchanged: `assertSafeName` rejects separators, dots and control characters before the join; `assertPathInside` re-checks (V5, V12) |
| T-104-02-06 | Denial of Service | concurrent uninstall double-unlinking | low | accept | `proper-lockfile` (`retries: 0`) serializes the load-mutate-save section per scope; the loser raises `StateLockHeldError` before any unlink |
| T-104-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task; `package.json` and `package-lock.json` unchanged, so the legitimacy gate has no input to audit |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `node --test tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/marketplace/cascade.test.ts tests/orchestrators/marketplace/remove.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/bridges/workflows/*.test.ts` passes.
- `node --test tests/architecture/import-boundaries.test.ts tests/architecture/no-orchestrator-network.test.ts` passes.
- `grep -rniE 'all (five|FIVE) bridges|symmetric across all five kinds' extensions/ tests/ | wc -l` returns 0.
- `npm run check` is green.
- Manual, before UAT: `ls ~/.pi/workflows/saved/` on the developer's real machine — envelopes written by an earlier run of this branch are orphaned by construction (the record naming them was deleted). Remove any stray so a leftover cannot be mistaken for a live regression.
</verification>

<success_criteria>
`uninstall`, `disable` and `marketplace remove --cascade` all remove workflow envelopes,
in both scopes; a failure to remove one is reported through a typed error rather than
swallowed; a partial failure shrinks the record to what is actually still on disk; and the
live canary can now see the orphan it was previously blind to.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-02-SUMMARY.md` when done
</output>
