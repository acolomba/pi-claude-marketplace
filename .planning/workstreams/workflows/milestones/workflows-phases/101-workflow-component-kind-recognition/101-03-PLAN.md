---
phase: 101-workflow-component-kind-recognition
plan: 03
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/orchestrators/plugin/info-manifest-absent.test.ts
  - tests/shared/notify-v2.test.ts
  - extensions/pi-claude-marketplace/shared/concerns/hooks.ts
autonomous: true
requirements: [WFLW-03]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WFLW-03: `docs/output-catalog.md` carries a `<!-- catalog-state: ... -->` block under the `info` section whose fenced body includes a `workflows:` line, and `tests/architecture/catalog-uat.test.ts` carries the paired `FIXTURES` entry — both halves in the same change, because the gate walks in both directions and either half alone fails one walk"
    - "WFLW-03 (probe: ordering): the catalog block pins the rendered order byte-for-byte — component lines appear in the six-kind `COMPONENT_KINDS` order with `workflows` LAST, and names within the kind are pre-sorted by the orchestrator's `localeCompare` base-sensitivity comparator, the renderer never sorting defensively"
    - "Every surviving prose statement of the rendered component-line order names all six kinds. The four sites that named a four-kind order were already stale before this phase, omitting `hooks`; none is left describing a shorter list"
    - "`node --test tests/architecture/catalog-uat.test.ts` passes in both directions: no catalog annotation lacks a fixture, and no fixture lacks an annotation"
  prohibitions:
    - "A rendered example must never be the sole evidence for behavior the code does not produce. The catalog fixture is hand-written `NotificationMessage` data by design — the byte-equality gate drives the renderer only — so the `workflows:` line it pins must ALSO be produced end to end by the real enumeration path, proven independently by the `info` orchestrator test in plan `101-01`. Never mint a reason token, status token or glyph, and never hand-tune a fixture, to make a success criterion read green."
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "the catalog state showing the rendered `workflows:` line"
      contains: "workflows:"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "the paired FIXTURES entry for that state"
      contains: "workflows"
  key_links:
    - from: "docs/output-catalog.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "the bidirectional catalog-state annotation to FIXTURES pairing, asserted byte-equal against the renderer"
      pattern: "catalog-state"
---

<objective>
Land the catalog half of the `info` signal: a rendered example carrying the `workflows:` line, its
paired fixture, and the prose sweep that leaves no statement of the render order naming a shorter
list than the renderer actually walks.

Purpose: the byte-equality gate is the project's binding user-contract check on rendered output.
A new rendered line that is not in the catalog is a user-visible change with no contract behind it.

Output: the paired catalog state, and four stale prose sites corrected.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-CONTEXT.md
@.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-PATTERNS.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
All work happens in the git worktree
`/home/acolomba/pi-claude-marketplace/.worktrees/workflows-spike` (branch
`features/workflows-spike`). Never read from or write into the primary checkout.
</working_directory>

<artifacts_this_phase_produces>
Phase 101 creates or widens the symbols below. This plan produces the last two rows.

| Symbol | File | Produced by |
|--------|------|-------------|
| `"workflows"` in `SUPPORTED_COMPONENT_KINDS` and `SUPPORTED_COMPONENT_PATH_KINDS` | `domain/resolver.ts` | 101-01 |
| `componentPaths.workflows` on the schema, `PartialResolution` and `emptyResolution()` | `domain/resolver.ts` | 101-01 |
| `workflows` in `SUPPORTED_COMPONENT_PATH_FIELDS` | `domain/components/plugin.ts` | 101-01 |
| `PluginInfoComponentsResolved.components.workflows` and the six-slot `COMPONENT_KINDS` | `shared/notify.ts` | 101-01 |
| the `"workflows"` arm of `nameFromEntry` / `discoverComponentNames` — the later-phase name seam | `orchestrators/plugin/info.ts` | 101-01 |
| **the `<!-- catalog-state: installed-single-scope-with-workflows -->` block** | `docs/output-catalog.md` | **101-03 Task 1** |
| **its paired `FIXTURES` entry** | `tests/architecture/catalog-uat.test.ts` | **101-03 Task 1** |
</artifacts_this_phase_produces>

<probe_rows_carried_by_this_plan>
| Requirement | Category | Authored as |
|---|---|---|
| WFLW-03 | ordering | `must_haves.truths` — plain string (explicit) |

One of the twelve probe-surfaced rows. The phase arithmetic and the two flagged assumptions live in
plan `101-01`. One of the four kept prohibitions is authored here.
</probe_rows_carried_by_this_plan>

<tasks>

<task type="auto">
  <name>Task 1: The catalog state and its paired fixture, in one change</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - `docs/output-catalog.md` lines 1554-1600 — the `info` H2 section preamble (which states the
      component-line order and is already stale), the `installed-single-scope` H3 with its
      annotation and fenced block, and the `installed-single-scope-with-dependencies` H3 that shows
      how a following line is documented
    - `tests/architecture/catalog-uat.test.ts` lines 1-80 — the header contract: the walker pairs a
      `<!-- catalog-state: STATE -->` comment with the NEXT fenced block inside a per-command H2
      section, the fixtures are pure `NotificationMessage` data and are deliberately NOT synthesized
      from domain helpers, and an `expectedSeverity` field is present only for warning/error states
    - `tests/architecture/catalog-uat.test.ts` lines 2925-2960 — the
      `"/claude:plugin info <plugin>@<marketplace>"` section key and its `installed-single-scope`
      entry, including the `piWithBothLoaded()` factory
    - the inverse-walk block at the tail of `tests/architecture/catalog-uat.test.ts` — the direction
      that fails on a fixture with no annotation
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 3298-3350 — `COMPONENT_KINDS` and
      `appendResolvedComponentLines`, to derive the expected bytes rather than guess them
  </read_first>
  <action>
Add ONE new catalog state, both halves in this single change. The gate walks forward (every
annotation needs a fixture) and inverse (every fixture needs an annotation), so a half-landing fails
one direction.

Doc half, in `docs/output-catalog.md` under the `info` H2, placed immediately after the
`installed-single-scope-with-dependencies` H3 so the component-line states stay together. Write an
H3 headed as a success state, a prose paragraph in the voice of its siblings stating what triggers
it and what the body shows, then the annotation comment `<!-- catalog-state: installed-single-scope-with-workflows -->`
and a fenced `text` block. Model the block on `installed-single-scope`: marketplace header at column
0 with the autoupdate marker, plugin row at 2-space indent with glyph, name, version and status,
description at 4-space indent, then the component lines at 4-space indent. Give the plugin two
workflow scripts so the comma-join is exercised, and keep at least one other component kind present
so the ordering is observable. The `workflows:` line goes LAST among the component lines — the
renderer walks a fixed six-kind order in which `workflows` sorts after `skills`. Derive every byte
from `appendResolvedComponentLines`: four leading spaces, the kind name, a colon, one space, then the
names joined by a comma and a space. Do not invent a status token, a reason brace or a glyph; this is
an ordinary installed row.

Also correct the `info` section preamble at line 1556, which states the component-line order as a
four-kind list. It was already stale before this phase — it omits `hooks` — so state the full
six-kind order rather than merely appending one name. Apply the same correction to the
`installed-single-scope` H3 prose one paragraph below, which repeats the same stale four-kind order.

Test half, in `tests/architecture/catalog-uat.test.ts`: add the matching entry to
`FIXTURES["/claude:plugin info <plugin>@<marketplace>"]` keyed by the same state string. Copy the
`installed-single-scope` entry's shape exactly — the `piWithBothLoaded()` factory, the `plugin-info`
message kind, the marketplace name and scope, the autoupdate detail, and the plugin block with
`componentsResolved: true`. Its `components` object gains a `workflows` array whose members are the
stems shown in the fenced block, listed in the same order. Omit `expectedSeverity`: every `info`
success state renders at info severity with no second argument, and the fixture must not claim
otherwise. Keep the `satisfies NotificationMessage` annotation so a shape drift is a typecheck error.

The names in the fenced block must be names the real enumeration path can produce — file stems of
`.js` entries under a resolved workflows directory. Do not choose names that the stem rule could not
yield. The independent end-to-end proof that the real path produces such a line lives in the `info`
orchestrator test from plan `101-01`; this gate pins the bytes, not the plumbing.

Markdown in this repository is formatted by `mdformat` through pre-commit, never by prettier — the
format check covers only js, json and ts. Run `pre-commit run --files docs/output-catalog.md` before
committing and never run a prettier write against a markdown file.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/catalog-uat.test.ts` passes. This single command covers both
      walk directions, so it is the whole gate.
    - `grep -c 'catalog-state: installed-single-scope-with-workflows' docs/output-catalog.md` is exactly 1.
    - `grep -c 'installed-single-scope-with-workflows' tests/architecture/catalog-uat.test.ts` is exactly 1.
    - `grep -cE '^    workflows: ' docs/output-catalog.md` is at least 1, and in that fenced block the
      `workflows:` line is the last line beginning with four spaces followed by a kind name and a colon.
    - The new fixture entry contains no `expectedSeverity` key.
    - `npm run typecheck` exits 0 — the fixture satisfies `NotificationMessage`, which is only possible
      once the notify interface carries the `workflows` member.
    - `pre-commit run --files docs/output-catalog.md` passes.
  </acceptance_criteria>
  <done>
    A rendered `workflows:` line is under the byte-equality contract, with both halves of the
    bidirectional gate landed together.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct every prose statement of the rendered component-line order</name>
  <files>tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/info-manifest-absent.test.ts, tests/shared/notify-v2.test.ts, extensions/pi-claude-marketplace/shared/concerns/hooks.ts</files>
  <read_first>
    - `tests/orchestrators/plugin/info.test.ts` around line 1682 — a comment naming the renderer tuple by arity
    - `tests/shared/notify-v2.test.ts` around lines 3609 and 4243 — two comments naming a four-kind render order
    - `tests/orchestrators/plugin/info-manifest-absent.test.ts` around line 370 — the same four-kind order
    - `extensions/pi-claude-marketplace/shared/concerns/hooks.ts` line 6 — a reference to the renderer tuple
    - `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` around line 365 — READ IT
      and leave it alone; see the action for why
    - `.claude/rules/typescript-comments.md`
  </read_first>
  <action>
Sweep the surviving prose that describes the rendered component-line order or the renderer tuple's
arity, so nothing is left describing a shorter list than the renderer walks. Each site is a one-line
comment edit.

Correct the four-kind order statements in `tests/shared/notify-v2.test.ts` (two sites),
`tests/orchestrators/plugin/info-manifest-absent.test.ts` and — if it names an order rather than
merely the tuple's existence — `extensions/pi-claude-marketplace/shared/concerns/hooks.ts`. All of
them were already stale before this phase because they omit `hooks`, so state the full six-kind order
rather than appending one name to a wrong list. Correct the arity phrase in
`tests/orchestrators/plugin/info.test.ts` so it no longer names a stale count; prefer describing the
tuple by what it enforces rather than by how many slots it has, since a count-bearing comment goes
stale on every future admission.

Leave `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` untouched. Its
similar-looking comment counts the five artifact BRIDGES, not the `info` renderer tuple. That count
changes when a bridge is added, which is a later phase, and editing it here would make a correct
comment wrong.

The comment policy is enforced: keep and add requirement and decision anchors such as `WFLW-03`,
`SURF-01`, `INFO-02` and `HOOK-01`; never write a GSD phase, plan, wave, task or milestone reference,
and never a bare numbered-hazard token. Confine each edit to the comment line — do not reflow
adjacent code, do not rename anything, and do not touch assertions.
  </action>
  <verify>
    <automated>npm run test && npm run lint && npm run format:check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 — no assertion was disturbed by a comment edit.
    - `npm run lint` and `npm run format:check` exit 0.
    - `git diff --stat extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` is
      empty — that file was deliberately not touched.
    - `git diff -U0 tests/shared/notify-v2.test.ts tests/orchestrators/plugin/info-manifest-absent.test.ts tests/orchestrators/plugin/info.test.ts`
      shows only comment lines changed: every added and removed line begins with a comment marker.
    - Each corrected order statement names six kinds, in the renderer's order, with `workflows` last.
  </acceptance_criteria>
  <done>
    No surviving comment describes the rendered component-line order as a shorter list than the
    renderer walks, and the bridge-count comment that only looks similar is intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer output → user | The catalog is the binding contract on the bytes a user sees; this plan extends that contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-08 | Repudiation | a rendered line with no catalog contract behind it | medium | mitigate | Task 1 lands the annotation and its fixture together; the bidirectional walk makes a half-landing a hard failure rather than a silent gap. |
| T-101-09 | Spoofing | a hand-written fixture asserting output the code cannot produce | medium | mitigate | The fixture is hand-written by design (the gate drives the renderer only), so the plan requires an independent end-to-end proof of the same line through the real enumeration path, held in plan `101-01`. Authored as this plan's prohibition. |
| T-101-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package; it edits one document, one architecture test, three test comments and one source comment. |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` green (both walk directions).
- `npm run check` green.
- `pre-commit run --files docs/output-catalog.md <changed files>` passes; markdown formatting goes
  through `mdformat`, never prettier.
- From this worktree, confirm trufflehog cleanliness with a filesystem-mode scan of the changed paths,
  then prefix the commit with `SKIP=trufflehog`, that hook only. Never `--no-verify`.
</verification>

<success_criteria>
- One new catalog state pins a rendered `workflows:` line, placed last among the component lines.
- Its fixture exists, carries no severity claim, and typechecks as a `NotificationMessage`.
- No prose site is left stating a component-line order shorter than six kinds.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-03-SUMMARY.md` when done
</output>
