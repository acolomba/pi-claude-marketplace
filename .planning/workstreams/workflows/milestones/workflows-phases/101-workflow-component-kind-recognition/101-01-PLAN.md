---
phase: 101-workflow-component-kind-recognition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/resolver.ts
  - extensions/pi-claude-marketplace/domain/components/plugin.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - tests/bridges/integration.test.ts
  - tests/bridges/integration-foreign-content.test.ts
  - tests/bridges/integration-materialization-gate.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/bridges/commands/discover.test.ts
  - tests/bridges/commands/stage.test.ts
  - tests/bridges/skills/discover.test.ts
  - tests/bridges/skills/stage.test.ts
  - tests/orchestrators/plugin/plugin-state-classifier.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/domain/resolver-strict.test.ts
  - tests/architecture/hooks-foundation.test.ts
autonomous: true
requirements: [WFLW-01, WFLW-02, WFLW-03, WFLW-04]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WFLW-01: a plugin whose only component evidence is `<pluginRoot>/workflows/`, whose manifest declares no `workflows` field, resolves `state: \"installable\"` with `workflows` in `supported` and `componentPaths.workflows` equal to `[\"workflows\"]`"
    - "WFLW-03: the same plugin no longer resolves with an empty `supported` array — the zero-signal outcome is closed by construction"
    - "WFLW-04: `componentPaths.workflows` is a REQUIRED member on `ComponentPathsSchema`, `PartialResolution` and `emptyResolution()`; `npm run typecheck` is green across the whole tree with no consumer using `?? []`"
    - "WFLW-04: `SUPPORTED_COMPONENT_KINDS` is the ordered 5-tuple `[skills, commands, agents, hooks, workflows]` and `SUPPORTED_COMPONENT_PATH_KINDS` the ordered 4-tuple `[skills, commands, agents, workflows]`; `tests/architecture/compat-01-no-expansion.test.ts` passes with zero edits"
    - "WFLW-03: `/claude:plugin info` on a plugin containing `workflows/deploy.js` renders the line `    workflows: deploy`, positioned LAST among the per-kind component lines and before any `dependencies:` line"
    - "WFLW-03 (probe: precision/tie-breaking): `nameFromEntry` strips the suffix by its own length rather than a hard-coded count, so a longer suffix stays correct; two stems differing only in case are BOTH retained, and because the `localeCompare` base-sensitivity comparator reports them equal their relative order is readdir insertion order — identical to the existing `commands`/`agents` behavior"
    - statement: "WFLW-02 (probe: concurrency): `resolveStrict` and `resolveLoose` stay pure, write-free and network-free with `workflows` admitted — two concurrent resolutions of the same entry produce identical output and mutate no shared state"
      verification: backstop
    - "All four path-bearing kinds flow through the SAME `collectStrictComponentKind` body — no `workflows`-specific discovery function, convention probe, or path validator is introduced"
  prohibitions:
    - "Recognition must never read, parse, or evaluate the contents of a workflow script. The only disk operations this phase performs against a plugin source are the resolver's `statKind` probe and the `info` orchestrator's directory listing."
    - "This phase must never write a workflow artifact, create the host engine's storage root, add a bridge directory, or stage anything. Materialization is a later phase; an executable file written here would land outside every scope root with no install record tracking it."
    - "A closed-set pin must never be weakened to make the widening compile or a red gate green. The expected value of a pin is what changes; the assertion form, the rationale message, and every written-out tuple length annotation stay. Deleting the length annotation would silently restore the renderer's ability to omit a kind."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/resolver.ts"
      provides: "`workflows` in both supported-kind tuples plus the required `componentPaths.workflows` member"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/domain/components/plugin.ts"
      provides: "the `workflows` field in `SUPPORTED_COMPONENT_PATH_FIELDS`"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "the `.js` arm of `nameFromEntry`, the widened `discoverComponentNames` union, and the `workflows` enumeration in `composeResolvedComponents`"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "the `workflows` member of `PluginInfoComponentsResolved.components` and the six-slot `COMPONENT_KINDS` tuple"
      contains: "workflows"
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/resolver.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      via: "`componentPaths.workflows` read by `composeResolvedComponents` and passed to `discoverComponentNames`"
      pattern: "componentPaths\\.workflows"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "`components.workflows` rendered by `appendResolvedComponentLines` iterating `COMPONENT_KINDS`"
      pattern: "workflows"
---

<objective>
Admit `workflows` as a supported component kind on both axes — declared-in-manifest OR
convention-on-disk — and carry the recognition all the way to the rendered `info` surface.

Purpose: a Claude plugin shipping `workflows/` installs today with zero signal. The kind sits
in neither the supported nor the unsupported closed set, so its scripts are dropped and no
surface says so. Joining the supported list closes that by construction (WFLW-03).

Output: `workflows` in both resolver tuples, a required `componentPaths.workflows` member,
every compile-forced construction site closed, and a `workflows:` line on `/claude:plugin info`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-CONTEXT.md
@.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-RESEARCH.md
@.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-PATTERNS.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
All work happens in the git worktree
`/home/acolomba/pi-claude-marketplace/.worktrees/workflows-spike` (branch
`features/workflows-spike`). Never read from or write into the primary checkout at
`/home/acolomba/pi-claude-marketplace` — another session holds it on `main`.
`node_modules` in the worktree is a symlink, so `npm run check` and `node --test` work normally.
</working_directory>

<phase_scope_note>
**No `list` bytes change in this phase, and that is correct.** Success Criterion 3 names both
`list` and `info`. `list` renders no per-kind component enumeration in any of its 18 catalog
states, and every token on a `list` row derives from `compatibility.unsupported` —
`narrowUnsupportedKinds(record.compatibility.unsupported)` for `partially-installed`, the same
for `partially-upgradable`. Nothing on `list` reads `supported`. SC 3's `list` half is therefore
satisfied at the STATE level: a workflow-bearing plugin's `compatibility.supported` records
`"workflows"` (pinned in plan `101-04`). SC 3's `info` half IS satisfied at the render level,
by the `workflows:` line this plan adds. Do NOT invent a reason token, status token, or glyph to
force a rendered `list` signal — the rendered `list` signal is the host-engine degradation token
that arrives with the soft-dependency probe in a later phase, and minting one here would break
the closed `REASONS` set that `tests/architecture/notify-closed-set-locks.test.ts` and
`tests/architecture/compat-01-no-expansion.test.ts` pin.
</phase_scope_note>

<artifacts_this_phase_produces>
Every symbol phase 101 creates or widens. This plan produces all of them except the catalog pair.

| Symbol | File | Produced by |
|--------|------|-------------|
| `"workflows"` as the 5th member of `SUPPORTED_COMPONENT_KINDS` | `domain/resolver.ts` | 101-01 Task 1 |
| `"workflows"` as the 4th member of `SUPPORTED_COMPONENT_PATH_KINDS` | `domain/resolver.ts` | 101-01 Task 1 |
| `workflows: Type.Array(Type.String())` on `ComponentPathsSchema` | `domain/resolver.ts` | 101-01 Task 1 |
| `workflows: string[]` on `PartialResolution.componentPaths` + `workflows: []` in `emptyResolution()` | `domain/resolver.ts` | 101-01 Task 1 |
| `workflows: Type.Optional(Type.Unknown())` in `SUPPORTED_COMPONENT_PATH_FIELDS` | `domain/components/plugin.ts` | 101-01 Task 1 |
| `readonly workflows?: readonly string[]` on `PluginInfoComponentsResolved.components` | `shared/notify.ts` | 101-01 Task 1 |
| `COMPONENT_KINDS` grown from a five-slot to a six-slot tuple, `"workflows"` last | `shared/notify.ts` | 101-01 Task 1 |
| the `"workflows"` arm of `nameFromEntry` (`.js` suffix) — the Phase-102 name-source seam | `orchestrators/plugin/info.ts` | 101-01 Task 1 |
| the widened `discoverComponentNames` `kind` union | `orchestrators/plugin/info.ts` | 101-01 Task 1 |
| the `workflows` param member, return member, discovery call and conditional spread in `composeResolvedComponents` | `orchestrators/plugin/info.ts` | 101-01 Task 1 |
| `workflows: ["workflows"]` in `deriveLenientComponentPaths` and the warm-git `unavailable` literal | `orchestrators/plugin/info.ts` | 101-01 Task 1 |
| the `<!-- catalog-state: installed-single-scope-with-workflows -->` block | `docs/output-catalog.md` | 101-03 Task 1 |
| its paired `FIXTURES` entry | `tests/architecture/catalog-uat.test.ts` | 101-03 Task 1 |
</artifacts_this_phase_produces>

<probe_rows_carried_by_this_plan>
The deterministic edge probe surfaced 12 rows across WFLW-01..04. This plan carries three of them.

| Requirement | Category | Authored as |
|---|---|---|
| WFLW-02 | concurrency | `must_haves.truths` — structured `{ statement, verification: backstop }` marker |
| WFLW-03 | precision | `must_haves.truths` — plain string (explicit) |
| WFLW-01 | unclassified | **FLAGGED ASSUMPTION**, unresolved — see below |
| WFLW-04 | unclassified | **FLAGGED ASSUMPTION**, unresolved — see below |

The remaining eight rows are carried by plans `101-02` (five), `101-03` (one) and `101-04` (two).

**Phase arithmetic (no silent drops):** 12 probe-surfaced edges == 10 authored into
`must_haves.truths` across the four plans + 2 surfaced as flagged assumptions below.

**Prohibition arithmetic:** the in-prompt adversarial recall produced 10 raw candidates; the
precision pass dropped 5 as routine engineering already owned by the edge probe or by code review;
the canon-referral rule dropped 1 (a declared `workflows` path escaping the plugin root is
path-traversal canon — it is already refused by `assertPathInside` inside `validateComponentPath`,
so a breadcrumb replaces a minted prohibition). 4 survive and are authored descriptor-less: 3 in
this plan, 1 in `101-03`, none in `101-02` or `101-04`. 4 kept == 4 authored.
</probe_rows_carried_by_this_plan>

<flagged_planner_assumptions>
Two probe rows came back `unclassified` and are surfaced here rather than auto-resolved. They are
NOT verification criteria; they are assumptions a human should confirm.

1. **WFLW-01 (unclassified).** Planner assumption: the convention axis is `statKind(<pluginRoot>/workflows) === "dir"`
   and nothing more — a `workflows` FILE at the plugin root (rather than a directory) does not
   admit the kind, and a `workflows` directory reachable only through a symlink is not probed for
   escape here because the join is unconditionally under `pluginRoot`. Both follow from reusing
   `collectStrictComponentKind` unchanged. If the intended convention is broader, this phase
   under-detects.
2. **WFLW-04 (unclassified).** Planner assumption: "consumers can enumerate a resolved plugin's
   workflow sources" is satisfied by the required `componentPaths.workflows` member alone — no
   accessor, no helper, and no `MaterializablePlugin` field beyond the schema propagation. If a
   consumer-facing enumeration API was intended, it is not in this phase.
</flagged_planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — a convention-only workflows plugin renders `workflows: deploy` on `info`</name>
  <files>extensions/pi-claude-marketplace/domain/resolver.ts, extensions/pi-claude-marketplace/domain/components/plugin.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, extensions/pi-claude-marketplace/shared/notify.ts, tests/orchestrators/plugin/info.test.ts, tests/bridges/integration.test.ts, tests/bridges/integration-foreign-content.test.ts, tests/bridges/integration-materialization-gate.test.ts, tests/bridges/agents/stage.test.ts, tests/bridges/commands/discover.test.ts, tests/bridges/commands/stage.test.ts, tests/bridges/skills/discover.test.ts, tests/bridges/skills/stage.test.ts, tests/orchestrators/plugin/plugin-state-classifier.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/resolver.ts` lines 20-70 (header rationale + `ComponentPathsSchema`), 315-345 (both tuples + their doc comments), 375-415 (`PartialResolution` + `emptyResolution`), 875-910 (`collectStrictComponentKind`, for confirmation only — it needs NO body change)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` lines 250-330 (`nameFromEntry` doc block, `nameFromEntry`, `readEntriesOrEmpty`, `discoverComponentNames`), 650-725 (`composeResolvedComponents` + its SURF-01 doc comment), 1215-1245 (`deriveLenientComponentPaths`), 1855-1875 (the warm-git `unavailable` component-path literal)
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 1375-1402 (`PluginInfoComponentsResolved` + its doc comment), 3298-3350 (`COMPONENT_KINDS` + `appendResolvedComponentLines`)
    - `extensions/pi-claude-marketplace/domain/components/plugin.ts` lines 18-40 (`SUPPORTED_COMPONENT_PATH_FIELDS` / `UNSUPPORTED_COMPONENT_FIELDS`)
    - `tests/orchestrators/plugin/info.test.ts` lines 120-200 (`SeedPathMarketplaceOpts` + `seedPathMarketplace`, in particular the `componentFiles` map that writes arbitrary relative files) and one existing rendered-components assertion (e.g. around line 566) for the notification-shape idiom
    - `tests/bridges/skills/discover.test.ts` lines 18-40 — the representative compile-forced `componentPaths` literal, to copy the one-line fix shape
    - `.claude/rules/typescript-comments.md` — binding on every comment and test title edited here
  </read_first>
  <behavior>
    Write the failing test FIRST, in `tests/orchestrators/plugin/info.test.ts`, before touching any
    production file. It is the end-to-end proof and the guard against the phase's highest-risk
    silent failure.

    - Seed a path-source marketplace with one plugin whose manifest entry declares NO component
      fields, and use the existing `componentFiles` map to write `workflows/deploy.js` and
      `workflows/release.js` under that plugin's root. Use `installablePluginDirs` / `componentDirs`
      as the sibling tests do so the plugin root itself resolves.
    - Run the `info` orchestrator for that plugin and assert the emitted notification message
      contains the exact line `    workflows: deploy, release` (four leading spaces, comma-space
      separator, names sorted).
    - Assert positionally that the `workflows:` line is the LAST per-kind component line: seed the
      same plugin with a `skills/` directory too and assert the `skills:` line index is lower than
      the `workflows:` line index in the split message.
    - Before the production change this test fails twice over: first because
      `componentPaths.workflows` does not exist, then — after the tuples land but before the
      `nameFromEntry` arm — because every `.js` entry is filtered out and the line never renders.
      Both red states must be observed, in that order.
  </behavior>
  <action>
Land the whole widening in one commit; the required schema member makes it impossible to split
without a red typecheck. Work in this order.

1. `extensions/pi-claude-marketplace/domain/resolver.ts`. Add `workflows: Type.Array(Type.String()),`
   as the fourth member of `ComponentPathsSchema` (after `agents`, ~line 65). Append `"workflows"`
   to `SUPPORTED_COMPONENT_KINDS` (line 325) giving `["skills", "commands", "agents", "hooks", "workflows"] as const`,
   and to `SUPPORTED_COMPONENT_PATH_KINDS` (line 336) giving `["skills", "commands", "agents", "workflows"] as const`.
   Add `workflows: string[]` to `PartialResolution.componentPaths` (line 382) and `workflows: []` to
   the `emptyResolution()` literal (line 410). Amend the two tuple doc comments (317-337) and the
   header rationale (24-30) so they still describe the split correctly now that a SECOND kind is
   both path-bearing and conventional: `hooks` remains the sole public-only member because its
   discovery is a convention FILE; `workflows` sits in both because its manifest field is
   path-bearing and its convention directory name equals the kind name. Keep the `HOOK-01` anchor,
   add a `WFLW-01` anchor. Change NO function body — `collectStrictComponentKind`,
   `collectLooseComponentKind`, `validateComponentPath`, `addComponentPath`,
   `addValidatedComponentPath`, `readPathOrArray` and `materializableFields` are all
   `SupportedPathKind`-typed or index through `partial.componentPaths[kind]` and widen for free.
   Also update the loop comments at 1366-1369 and 1444 that name which tuple is iterated.

2. `extensions/pi-claude-marketplace/domain/components/plugin.ts`. Add
   `workflows: Type.Optional(Type.Unknown()),` to `SUPPORTED_COMPONENT_PATH_FIELDS`. Both validators
   already accept an undeclared `workflows` field because the TypeBox objects are open, so this is
   documentation of the supported set, not new validation. Leave `hooks` where it is in
   `UNSUPPORTED_COMPONENT_FIELDS` — that grouping is stale from an earlier admission and correcting
   it is out of scope here.

3. `extensions/pi-claude-marketplace/shared/notify.ts`. Add `readonly workflows?: readonly string[];`
   as the LAST member of `PluginInfoComponentsResolved.components` (alphabetical, after `skills`).
   Grow `COMPONENT_KINDS` from five `ComponentKind` slots to six and set the value to
   `["agents", "commands", "hooks", "mcp", "skills", "workflows"]`. Update the tuple's own doc
   comment so its arithmetic is right again — it currently reasons about five entries and a sixth
   key. Update the two doc comments that state the render order as agents/commands/mcp/skills
   (lines ~1381 and ~3316); both are already stale because they omit `hooks`, so state the full
   six-kind order. `appendResolvedComponentLines` needs NO body change: `workflows` takes the same
   default single-line comma-join arm `skills` takes.

4. `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts`. Widen the `kind` union on both
   `nameFromEntry` (line 274) and `discoverComponentNames` (line 311) to include `"workflows"`.
   In `nameFromEntry`, replace the unguarded tail so the suffix is a local variable chosen by kind
   — `.js` for `workflows`, `.md` otherwise — and the strip uses the suffix's own length rather
   than the hard-coded count that is there now. This is the single genuinely new line of behavior in
   the phase and the one place a compiling change produces a SILENT empty render: adding the union
   member without the arm makes every script fail the markdown-suffix test, so
   `discoverComponentNames` returns an empty array, so the conditional spread is skipped, so the
   line never appears. Add a `workflows` bullet to the per-kind doc block at 253-271.
   In `composeResolvedComponents` (669): add `readonly workflows: readonly string[];` to the
   `componentPaths` param type (REQUIRED, matching the resolver), `readonly workflows?: readonly string[];`
   to the return type (optional, matching the notify interface), one
   `discoverComponentNames(pluginRoot, resolved.componentPaths.workflows, "workflows")` call, and
   `...(workflows.length > 0 && { workflows })` LAST in the returned spread. Update the SURF-01
   doc comment at 654-668 that names the enforced ordering.
   In `deriveLenientComponentPaths` (1223): add `workflows: string[]` to the return type,
   `workflows: ["workflows"]` to the `out` literal, and `"workflows"` to the
   `["skills", "commands", "agents"] as const` loop tuple at 1233. At the warm-git `unavailable`
   literal (1866) add `workflows: ["workflows"]`. All three are compile-forced — there is no
   leave-it-alone option that typechecks — and seeding the convention is the consistent choice:
   these sites exist so a structurally-broken plugin still enumerates its on-disk components, and
   omitting `workflows` would make the `unavailable` arm the one surface where a workflows
   directory reads as absent. No test fixture anywhere under `tests/` contains a workflows
   directory, so no existing assertion moves.
   `buildNotInstallablePathRowFields` (1172) and `buildAvailableRow` (2046) type their `resolved`
   parameter as `Parameters<typeof composeResolvedComponents>[1]` and widen automatically — do not
   edit them. The state-only info arms (~1090-1146) read `record.resources.*`, which gains no
   `workflows` member in this phase — do not edit them either.

5. Run `npm run typecheck` and let it enumerate the remaining construction sites. There are
   sixteen, each inside a function whose return type or variable annotation is
   `ResolvedPluginInstallable` or `ResolvedPlugin`, spread over nine test files:
   `tests/bridges/integration.test.ts` (76), `tests/bridges/integration-foreign-content.test.ts` (55),
   `tests/bridges/integration-materialization-gate.test.ts` (107), `tests/bridges/agents/stage.test.ts` (44),
   `tests/bridges/commands/discover.test.ts` (26, 221, 257), `tests/bridges/commands/stage.test.ts` (62),
   `tests/bridges/skills/discover.test.ts` (31, 187, 222, 251, 291), `tests/bridges/skills/stage.test.ts` (41),
   `tests/orchestrators/plugin/plugin-state-classifier.test.ts` (52, 68). The fix is the identical
   single line `workflows: [],` appended after `agents: [],`. Never a spread, never a partial — the
   grep for these literals is exhaustive and there are no spread or `Object.assign` constructions
   anywhere in the tree. Work from the compiler's output, not from this list; the list exists so a
   missing error is recognized as suspicious rather than as done.

Comment discipline is enforced: comments and test titles may cite `WFLW-01`, `HOOK-01`, `D-07`,
`SURF-01`, `NFR-7`, `SC-3` and similar spec anchors; they must not cite GSD phase, plan, wave or
task numbers, milestone versions, or bare numbered-hazard references. ESLint style applies to every
new line: braces always, a blank line after each block-like statement, explicit return types on
module boundaries.
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/orchestrators/plugin/info.test.ts tests/domain/resolver-strict.test.ts tests/domain/resolver-loose.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0. This is the proof that all twenty-two construction sites are closed.
    - `node --test tests/orchestrators/plugin/info.test.ts` passes, including the new case, and its
      asserted message contains the exact substring `    workflows: deploy, release`.
    - In that same new case, the index of the line beginning `    skills:` is strictly lower than the
      index of the line beginning `    workflows:` in the split message — the six-kind order puts
      `workflows` last.
    - `grep -c 'suffix' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` is at least 1,
      and the strip in `nameFromEntry` is expressed in terms of that variable's length rather than a
      numeric literal.
    - `node -e 'import("./extensions/pi-claude-marketplace/domain/resolver.ts").then(m => { const a = [...m.SUPPORTED_COMPONENT_KINDS].join(","); if (a !== "skills,commands,agents,hooks,workflows") { throw new Error(a); } })'`
      exits 0.
    - `node --test tests/architecture/compat-01-no-expansion.test.ts tests/architecture/notify-closed-set-locks.test.ts`
      passes with those two files unedited — no reason token, status token, glyph, persisted field or
      schema version was added.
    - `git status --porcelain` lists no file under `extensions/pi-claude-marketplace/bridges/` and no
      newly created directory anywhere.
  </acceptance_criteria>
  <done>
    A plugin with `<pluginRoot>/workflows/*.js` and no manifest field resolves `installable` with
    `workflows` in `supported`, and `/claude:plugin info` renders its scripts on a `workflows:` line
    placed last among the component lines. The whole tree typechecks with the member required.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the headline requirement proofs — convention-only admission and the closed-set tuple</name>
  <files>tests/domain/resolver-strict.test.ts, tests/architecture/hooks-foundation.test.ts</files>
  <read_first>
    - `tests/domain/resolver-strict.test.ts` lines 20-90 (the `mockCtx` helper, `MP`, `ROOT`, `basicEntry`) and 1060-1100 (the two convention-directory templates to copy)
    - `tests/architecture/hooks-foundation.test.ts` lines 190-215 (the closed-set pin and the block comment above it)
    - `.claude/rules/typescript-comments.md`
  </read_first>
  <behavior>
    - Convention-only (PRIMARY, WFLW-01): an entry declaring nothing, with `<pluginRoot>` and
      `<pluginRoot>/workflows` both present in the in-memory context and NOTHING else, resolves
      `state: "installable"` with `componentPaths.workflows` deep-equal to `["workflows"]` and
      `supported` including `"workflows"`. This is the shape 16 of 16 sampled real plugins ship and
      the only one a field-only implementation would miss, so it is written first and named as the
      primary case.
    - Zero-signal closure (WFLW-03): the same fixture also asserts `supported` is NON-EMPTY and that
      `unsupported` and `notes` are both empty — the plugin is recognized without being demoted.
    - Closed-set pin (WFLW-04): `SUPPORTED_COMPONENT_KINDS` deep-equals
      `["skills", "commands", "agents", "hooks", "workflows"]`.
  </behavior>
  <action>
Extend `tests/domain/resolver-strict.test.ts` with the two behavior cases above, copying the
existing implicit-by-convention template at lines 1066-1078 and swapping the kind. Three things the
template carries that must be preserved: the `if (r.state === "installable")` narrowing guard is
mandatory before reading `componentPaths` because the resolved union is discriminated (NFR-7) and
omitting it is a typecheck error; `assert.equal(r.state, "installable", ...)` carries the resolved
notes in its failure message so a red run is self-diagnosing; and the context is purely in-memory —
no tmpdir, no memfs, absolute synthetic path keys only.

Then update the closed-set pin in `tests/architecture/hooks-foundation.test.ts` IN PLACE at line 199:
change the expected array to the five-element form, drop the tuple-arity phrase from the test title
so the title stops naming a stale count, and leave the assertion's rationale message unchanged —
it states that shape and order are a locked public contract, which is still exactly true. Do not add
a second parallel pin; one pin, edited, is the contract. Adjust the block comment above it only if
it names the old arity.

Do not weaken the pin to make it pass: the expected array is updated, never the assertion form, and
the length annotation on any tuple type stays written out.
  </action>
  <verify>
    <automated>node --test tests/domain/resolver-strict.test.ts tests/architecture/hooks-foundation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/domain/resolver-strict.test.ts tests/architecture/hooks-foundation.test.ts` passes.
    - `grep -c 'WFLW-01' tests/domain/resolver-strict.test.ts` is at least 1 — the primary case cites
      its requirement anchor in the test title.
    - The convention-only test's fixture map contains exactly two keys: the plugin root and
      `<pluginRoot>/workflows`. Any additional component directory would make it a different case.
    - `grep -c 'shape and order are locked' tests/architecture/hooks-foundation.test.ts` is at least 1
      — the rationale message survived the edit.
    - `git diff --stat tests/architecture/hooks-foundation.test.ts` shows a single test's worth of
      change, not a new test block.
  </acceptance_criteria>
  <done>
    The convention-only plugin — the load-bearing detection axis — is pinned as the primary case, the
    zero-signal outcome is asserted closed, and the public closed-set contract names five kinds in
    locked order.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace manifest / plugin entry → resolver | A third-party-authored `workflows` field crosses here as untrusted input |
| plugin source directory → `info` orchestrator | A third-party-authored directory tree is listed here; contents are never read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-101-01 | Tampering | `validateComponentPath` (`domain/resolver.ts:804`) — a declared `workflows` path escaping the plugin root | high | mitigate | No new validator. `workflows` flows through the existing `readPathOrArray` → `validateComponentPath` chain: nested-array reject, non-string reject, absolute-path reject, `assertPathInside(pluginRoot, candidate)` containment. Pinned by the escaping-path case in plan `101-02`. |
| T-101-02 | Tampering | the convention probe `statKind(path.join(pluginRoot, "workflows"))` | low | accept | The join is unconditionally under `pluginRoot`, so no escape is expressible at this site. The all-symlink refusal that matters for a script file belongs to the materialization phase, not to recognition. |
| T-101-03 | Elevation of Privilege | `discoverComponentNames` reading a workflow script | high | mitigate | Not reachable: this phase performs `statKind` and `readdir` only. No file content is read, parsed, or evaluated. Enforced as a plan prohibition and by the absence of any read call on `.js` paths. |
| T-101-04 | Repudiation | a new upstream component kind silently ignored by the closed list | medium | mitigate | This phase IS the mitigation for the `workflows` instance. The closed-list warning in `domain/resolver.ts` predicted exactly this failure; joining the supported set closes it by construction (WFLW-03). |
| T-101-05 | Information Disclosure | the `unavailable`-arm lenient re-seed newly enumerating a `workflows` directory on `info` | low | accept | The enumeration lists file stems from a directory the user already installed; it discloses nothing the plugin source does not. Omitting it would reintroduce the zero-signal outcome on that one arm. |
| T-101-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package. Every symbol it touches is in-repo or an already-declared dependency (`typebox`). The milestone's one new runtime dependency is audited in the phase that introduces it, not here, so no package-legitimacy checkpoint is required. |

**Canon referral (prohibition probe):** path traversal through a declared component path is canon
security territory and is already owned by the `assertPathInside` chokepoint (NFR-10). A breadcrumb,
not a minted prohibition — see T-101-01 for the live control.
</threat_model>

<verification>
- `npm run typecheck` green (the twenty-two compile-forced sites).
- `node --test tests/orchestrators/plugin/info.test.ts tests/domain/resolver-strict.test.ts tests/architecture/hooks-foundation.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/architecture/notify-closed-set-locks.test.ts` green.
- `npm run test` green before handing off to wave 2.
- Before committing: `pre-commit run --files <changed files>`. From this worktree the trufflehog
  git-mode hook fails structurally, so confirm cleanliness with a filesystem scan of the changed
  paths first and then prefix the commit with `SKIP=trufflehog`, that hook only. Never `--no-verify`,
  never `--amend` to recover from a hook failure.
</verification>

<success_criteria>
- A plugin shipping `<pluginRoot>/workflows/` with no manifest field resolves with `workflows` in
  `supported` and `componentPaths.workflows` equal to `["workflows"]`.
- `componentPaths.workflows` is required, and the whole tree typechecks without a single `?? []`.
- `/claude:plugin info` renders the `workflows:` line last among per-kind component lines.
- The public closed set is the ordered five-tuple; the no-expansion architecture gates pass unedited.
</success_criteria>

<forward_dependencies>
Not this phase; recorded so they are not lost.

- The `info` line shows the FILE STEM here. A later phase swaps the name source to the script's
  extracted `meta.name`; the `"workflows"` arm of `nameFromEntry` is the seam it edits.
- `docs/prd/pi-claude-marketplace-prd.md` lines 77, 92 and 118 enumerate the supported and
  unsupported kinds and are deliberately NOT updated here. A half-updated PRD — kind admitted,
  bridge absent, degradation unstated — is worse than a consistently-behind one. The documentation
  phase owns them.
- The comment at `orchestrators/plugin/enable-disable.ts:365` naming a five-member set refers to the
  five artifact BRIDGES, not the `info` renderer tuple this plan grows. Leave it alone; it grows when
  a bridge is added.
</forward_dependencies>

<output>
Create `.planning/workstreams/workflows/phases/101-workflow-component-kind-recognition/101-01-SUMMARY.md` when done
</output>
