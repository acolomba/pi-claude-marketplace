---
phase: 105-workflow-degradation-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WDEP-01, WDEP-02, WDEP-04, WDOC-02]

files_modified:
  - extensions/pi-claude-marketplace/platform/pi-api.ts
  - extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.messaging.ts
  - docs/output-catalog.md
  - tests/platform/pi-api.test.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/compat-01-no-expansion.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A session whose `pi.getAllTools()` returns a tool named `workflow` and no tool named `workflow_control` reads the host engine as ABSENT — the false positive a bare-name probe produces on the engine this bridge does not target."
    - "A session whose `pi.getAllTools()` returns BOTH `workflow` and `workflow_control` reads the host engine as PRESENT — the discriminator selects on the distinctive name while the decoy is present, so the probe is not merely rejecting an unknown name."
    - "`hasLoadedWorkflowEngine` returns `false` for an empty tool list and `false` when `getAllTools()` throws, and the throwing case surfaces nothing about the caught error to any output channel."
    - "Tool-name matching is exact string identity: no prefix match, no substring match, no case folding, no Unicode normalization — a tool named `workflow` never satisfies the `workflow_control` predicate and a tool named `Workflow_Control` never satisfies it either."
    - "Installing a workflow-bearing plugin while the engine is absent still writes every envelope, still succeeds, and renders the degradation marker on its row at `warning` severity."
    - "A row whose `dependencies` list is empty renders with NO brace at all — not an empty brace — and a row declaring `workflows` while the engine IS loaded renders byte-for-byte the row it rendered before this plan."
    - "Two soft-dep markers in one brace SEPARATE with a comma-space join in `DEPENDENCIES` tuple order and never merge or dedupe: a row declaring both `agents` and `workflows` renders the pi-subagents marker first and the host-engine marker last."
    - "`softDepMarkers` emits in `DEPENDENCIES` tuple order regardless of the order in which the caller's `Dependency[]` was assembled, so no caller's push order can move the rendered bytes."
    - "`DEPENDENCIES` is exactly three members, in the order agents, mcp, workflows, pinned by an order-and-length lock that did not exist before this plan."
    - "`REASONS` is exactly 40 members with the new token at the tail and no existing member moved."
  artifacts:
    - extensions/pi-claude-marketplace/platform/pi-api.ts (hasLoadedWorkflowEngine, SoftDepStatus.workflowEngineLoaded)
    - extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts (third DEPENDENCIES member, SOFT_DEP_MARKER_WORKFLOWS, third softDepMarkers arm)
    - extensions/pi-claude-marketplace/shared/notify.ts (the 40th REASONS member, the widened composeReasons)
    - docs/output-catalog.md (two new catalog states)
    - tests/architecture/notify-closed-set-locks.test.ts (the DEPENDENCIES order-and-length lock)
  key_links:
    - "install.ts's staged-workflow count -> the row's `dependencies` array -> composeReasons -> softDepMarkers -> the rendered brace"
    - "SOFT_DEP_MARKER_WORKFLOWS's literal value must BE a REASONS member, or `shared/concerns/soft-dep.ts` does not typecheck"
    - "the new token needs a home in `UNSUPPORTED_REASONS`, or `_ReasonsCoverageProof` raises TS2344"
    - "each new catalog state needs its paired `CatalogFixture`, or the bidirectional walk in catalog-uat.test.ts flags an orphan"
  prohibitions:
    - "The probe must never report `@nicknisi/pi-workflows` as the host engine, and the degradation token must never name it. Both mistakes tell a user their untrusted third-party scripts are sandboxed in a `vm.createContext` realm when that engine actually hands them the real `process`, all environment variables and `process.binding('fs')` — a trust claim the user cannot check and would act on."
  flagged_assumptions:
    - "WDEP-02 edge probe returned `unclassified` — no automatic edge category applies to `with the engine absent the artifacts are still written and the install still succeeds`. Reviewed manually: the observable behaviour is covered by the install-row pair in Task 1 and by the probe-independence pair in 105-03. Recorded as an assumption rather than auto-dismissed."
    - "WDOC-02 edge probe returned `unclassified` — no automatic edge category applies to `the catalog carries the new token and the new rendered states under the byte gate`. Reviewed manually: the byte gate itself is the check, and Task 3 pairs each state with a fixture in both directions. Recorded as an assumption rather than auto-dismissed."
  canon_breadcrumbs:
    - "Information disclosure through a rendered reason token — canon security, dropped per the canon-referral rule. The token is a fixed closed-set literal with zero interpolation, the same reasoning that made the equivalent Phase-104 threat inapplicable."
    - "Dependency-manifest tampering smuggled in alongside a docs change — canon supply chain, dropped per the canon-referral rule. Covered by T-105-01-SC in the threat model below."
---

<objective>
Probe for the host workflow engine, grow the closed dependency set from two members to
three, and carry one end-to-end path from `pi.getAllTools()` to the rendered brace: a
workflow-bearing plugin installed in a session with no engine writes its envelopes, succeeds,
and says on its own row that nothing will run them until the engine is installed and the
session reloads.

Purpose: today that install is a silent success. The artifacts are written and correct,
nothing runs, and nothing says why (WDEP-02). Making the fact renderable requires the probe
(WDEP-01), the third `DEPENDENCIES` member with its marker and reason token (WDEP-04), and
the byte-gated catalog states that pin what the two-marker brace looks like (WDOC-02).

Output: `hasLoadedWorkflowEngine`, `SoftDepStatus.workflowEngineLoaded`, the third
`DEPENDENCIES` member, `SOFT_DEP_MARKER_WORKFLOWS`, the 40th `REASONS` member, a required
`declaresWorkflows` third flag on `softDepMarkers` / `composeReasons` / `companionSeverity`,
the fact travelling on the install / update / reinstall / enable outcomes, and two new
byte-gated catalog states.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/REQUIREMENTS.md
@.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-CONTEXT.md
@.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-RESEARCH.md
@.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-VALIDATION.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Artifact | Where |
|------|----------|-------|
| function | `hasLoadedWorkflowEngine(pi: ExtensionAPI): boolean` | `platform/pi-api.ts` |
| interface member | `SoftDepStatus.workflowEngineLoaded: boolean` (REQUIRED) | `platform/pi-api.ts` |
| tuple member | `DEPENDENCIES` gains `"workflows"` as the THIRD and LAST member | `shared/concerns/soft-dep.ts` |
| constant | `SOFT_DEP_MARKER_WORKFLOWS: Reason = "requires pi-dynamic-workflows"` | `shared/concerns/soft-dep.ts` |
| reason token | `"requires pi-dynamic-workflows"` — the 40th `REASONS` member, at the tail | `shared/notify.ts` |
| topic-group member | the same literal appended to `UNSUPPORTED_REASONS` after `"requires pi-mcp"` | `shared/notify-reasons.ts` |
| changed signature | `softDepMarkers(declaresAgents, declaresMcp, declaresWorkflows, probe)` | `shared/concerns/soft-dep.ts` |
| changed signature | `composeReasons(reasons, declaresAgents, declaresMcp, declaresWorkflows, probe)` | `shared/notify.ts` |
| changed signature | `companionSeverity({ declaresAgents, declaresMcp, declaresWorkflows }, probe)` | `shared/notify-reasons.ts` |
| outcome field | `declaresWorkflows: boolean` (REQUIRED) on the install and update outcome bases | `orchestrators/types.ts` |
| signal field | `LedgerDegradationSignals.stagedWorkflows?: boolean` | `orchestrators/plugin/shared.ts` |
| catalog state | `success-with-workflow-engine-absent` | `docs/output-catalog.md` |
| catalog state | `success-with-soft-dep-and-workflow-engine-absent` | `docs/output-catalog.md` |
| test lock | the `DEPENDENCIES` order-and-length lock (new — the tuple was unpinned) | `tests/architecture/notify-closed-set-locks.test.ts` |
| renamed helper | `piWithBothLoaded` -> `piWithAllLoaded` (now three tools) plus a new `piWithoutWorkflowEngine` | `tests/architecture/catalog-uat.test.ts`, `tests/shared/notify-v2.test.ts` |
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One install, engine absent, says so — probe to rendered brace</name>

  <files>extensions/pi-claude-marketplace/platform/pi-api.ts, extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts, extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/notify-reasons.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts, extensions/pi-claude-marketplace/orchestrators/plugin/fetch.messaging.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.messaging.ts, extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.messaging.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.messaging.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/remove.messaging.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.messaging.ts, tests/platform/pi-api.test.ts, tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/compat-01-no-expansion.test.ts, tests/architecture/catalog-uat.test.ts, tests/shared/notify-v2.test.ts, tests/orchestrators/plugin/install.test.ts</files>

  <read_first>
    - `extensions/pi-claude-marketplace/platform/pi-api.ts` (whole file, 167 lines) — the header names it the sole `pi.getAllTools()` import site; `hasLoadedPiSubagents` at :133 is the RH-3 shape to copy, `hasLoadedPiMcpAdapter` at :146 is the RH-4 shape to NOT copy, `SoftDepStatus` at :124, `softDepStatus` at :162
    - `extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts` (whole file, 65 lines) — `DEPENDENCIES` at :29, `Dependency` at :35, the two marker constants at :38-39, `softDepMarkers` at :49
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 76-200 (the `REASONS` header block and the tuple through its tail `"stale workflow command"` at :188) and lines 2077-2100 (`composeReasons`) and lines 2140-2270 (`pluginRow`, `partiallyInstalledRow`, `installedLikeRow`) and lines 2280-2330 (the `installed` / `updated` / `reinstalled` switch arms)
    - `extensions/pi-claude-marketplace/shared/notify-reasons.ts` lines 1-110 (the two header count sentences at :7-22, `companionSeverity` at :79-86, `UNSUPPORTED_REASONS` at :93-103) and lines 226-238 (`_ReasonsCoverageProof`)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 1875-1985 (the `stagedAny` reduce at :1882 which already reads `stagedWorkflowNames`, the `Dependency[]` derivation at :1899-1906, the `companionSeverity` call at :1967-1974)
    - `tests/platform/pi-api.test.ts` lines 1-60 — the `makePi(tools)` factory and `makeThrowingPi()` helper at :27-37
    - `tests/architecture/catalog-uat.test.ts` lines 160-200 — `MockPi`, `piWithBothLoaded` at :178, `piWithMcpLoaded` at :185, `piWithNothingLoaded` at :192
    - `tests/shared/notify-v2.test.ts` lines 150-200 — the independent copy of the same four helpers
    - `tests/architecture/notify-closed-set-locks.test.ts` (whole file, 65 lines) — the comment-ladder convention on the `REASONS` length pin
    - `tests/architecture/compat-01-no-expansion.test.ts` lines 130-175 — the hand-written `REASONS` order array
    - `105-RESEARCH.md` sections "Probe Design", "The Four Artifacts of a Soft Dependency", "Token Artifact Trail", "Marker Plumbing: the full blast radius"
  </read_first>

  <behavior>
    Write these assertions BEFORE any production edit and observe each one red.

    In `tests/platform/pi-api.test.ts`, using the existing `makePi` / `makeThrowingPi`:
    - a tool list containing only a tool named `workflow_control` -> `hasLoadedWorkflowEngine` is true
    - a tool list containing only a tool named `workflow` -> false. This is the assertion the
      requirement exists for: it is the shape a session running the non-target engine exposes,
      and a bare-name predicate returns true here
    - a tool list containing BOTH names -> true. Proves the predicate selects on the
      discriminator rather than merely rejecting an unfamiliar name
    - a tool list containing a tool named `Workflow_Control` and one named `workflow_controller`
      -> false. Pins exact string identity: no case folding, no prefix or substring match
    - a tool list whose single entry carries a `sourceInfo.source` string containing the host
      engine's package name but whose `name` is `workflow` -> false. Pins that the predicate
      reads only the tool name, so a source-string arm cannot be added later without reddening
      this case
    - an empty tool list -> false
    - `makeThrowingPi()` -> false, and the case asserts nothing was written to any output channel
    - `softDepStatus` returns an object carrying all three flags, each independently settable

    In `tests/architecture/notify-closed-set-locks.test.ts`, a NEW clause asserting
    `DEPENDENCIES` deep-equals the three-member array in order, and that its length is 3. Also
    bump the `REASONS` length assertion and its test title from 39 to 40.

    In `tests/architecture/compat-01-no-expansion.test.ts`, append the new token to the tail of
    the hand-written order array.

    In `tests/orchestrators/plugin/install.test.ts`, a positive/negative pair over one
    workflow-bearing plugin install:
    - engine ABSENT: the rendered block byte-asserts a row carrying the degradation marker
      inside the brace, and the `ctx.ui.notify` severity argument is `warning`
    - engine PRESENT: the WHOLE rendered block is byte-asserted and carries no marker and no
      `needs attention` summary line. Assert the whole block, never the token's absence — an
      absence-only assertion passes on a stray summary line
    - a plugin that staged ZERO workflows, engine absent: the whole block is byte-asserted and
      carries no brace at all
  </behavior>

  <action>
Add `hasLoadedWorkflowEngine(pi: ExtensionAPI): boolean` to `platform/pi-api.ts`, placed
immediately after `hasLoadedPiMcpAdapter`. It returns
`pi.getAllTools().some((tool) => tool.name === "workflow_control")` inside a `try`, with a
bare `catch { return false; }` that neither logs nor rethrows nor reads the caught value —
byte-symmetric with `hasLoadedPiSubagents`. Take the RH-3 name-predicate shape, NOT the RH-4
shape: do NOT add a `sourceInfo.source` arm, because a substring match on a source string
reopens the exact false-positive surface WDEP-01 exists to close. Its doc comment cites
`WDEP-01`, names both candidate engines and states that the host registers both `workflow`
and `workflow_control` while the other registers only the bare name. Cite no phase, plan or
wave — `.claude/rules/typescript-comments.md` forbids it.

Add `workflowEngineLoaded: boolean` as a REQUIRED member of `SoftDepStatus` and compose it in
`softDepStatus`. Required, not optional: every literal construction in the tree must fail to
compile until it is visited.

In `shared/concerns/soft-dep.ts`, append `"workflows"` as the third and LAST `DEPENDENCIES`
member; add `const SOFT_DEP_MARKER_WORKFLOWS: Reason = "requires pi-dynamic-workflows";`
beside the other two marker constants; and give `softDepMarkers` a REQUIRED third boolean
parameter `declaresWorkflows` placed after `declaresMcp` and before `probe`, with a third `if`
block appended AFTER the mcp block that pushes the new marker when
`declaresWorkflows && !probe.workflowEngineLoaded`. The push must be last, because the brace
join is byte-critical and the tuple is the order authority. The marker constant's doc comment
states why the scope is spelled out rather than abbreviated to the shorter form: the shorter
spelling is the npm name of the engine this bridge does not target, so a degradation message
using it would send the operator to install the wrong and less trustworthy package.

In `shared/notify.ts`, append the same literal as the 40th and last `REASONS` member, after
`"stale workflow command"`, with a comment stating that it is a soft-dependency marker
emitted by the renderer from a row's `dependencies` field and never placed by a caller, and
that severity is `warning` because the envelopes ARE written but nothing runs them until the
engine is present. Update the `39-entry` count sentence in the tuple's header block to 40.
Give `composeReasons` a REQUIRED fourth parameter `declaresWorkflows: boolean` between
`declaresMcp` and `probe`, and forward it to `softDepMarkers`.

Sweep every `composeReasons` call site. There are 33 across `extensions/`; the compiler
enumerates them. Five of them read a row's `dependencies` field and gain
`p.dependencies.includes("workflows")` (or the `?? false` optional form): `partiallyInstalledRow`,
`installedLikeRow`, and the `installed` / `updated` / `reinstalled` arms of `renderPluginRow`.
Every remaining site passes `false` — that is their permanent correct value, not a placeholder:
those variants carry no `dependencies` field by construction. Also update the `pluginRow` doc
comment that says both declares-flags are false, and the `renderPluginRow` header block that
enumerates which arms declare `dependencies`.

In `shared/notify-reasons.ts`, append the same literal to `UNSUPPORTED_REASONS` immediately
after `"requires pi-mcp"` — that group is where both existing soft-dep markers live, its order
is not byte-critical, and without a topic-group home `_ReasonsCoverageProof` raises TS2344.
Bump both `39-entry` header count sentences to 40 and extend the bump narration sentence with
the `WDEP-04` clause. Give `companionSeverity`'s destructured object parameter a REQUIRED
`declaresWorkflows: boolean` and a third disjunct
`declaresWorkflows && !probe.workflowEngineLoaded`. If the three-way disjunction over a
destructured triple trips `sonarjs/cognitive-complexity` or reads badly, extract a named local
predicate rather than adding a disable directive.

In `orchestrators/plugin/install.ts`, append a third arm to the `Dependency[]` derivation at
:1899 that pushes `"workflows"` when `installCtx.stagedWorkflowNames.length > 0`, LAST, after
the mcp push. Pass the same expression as `declaresWorkflows` in the `companionSeverity` call
at :1967. The derivation's new arm carries a `WDEP-02` comment stating that the envelope is
written whether or not the engine is loaded, and that the engine's own load-time storage scan
picks it up on the next reload with no reinstall.

In the two test files that own a `piWithBothLoaded` helper — `tests/architecture/catalog-uat.test.ts`
at :178 and `tests/shared/notify-v2.test.ts` at :169 — rename it to `piWithAllLoaded`, give it
a third tool named `workflow_control`, and add a sibling `piWithoutWorkflowEngine()` whose body
is exactly today's two-tool list, with a doc comment saying what it means. Point every existing
call site at `piWithAllLoaded`. This must land in this task: after the probe exists, the old
name becomes a lie and its "no soft-dep markers fire" claim becomes conditional.

Do NOT touch `package.json`, `package-lock.json` or `sonar-project.properties`. This plan
installs nothing.
  </action>

  <verify>
    <automated>node --test tests/platform/pi-api.test.ts tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/orchestrators/plugin/install.test.ts && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/platform/pi-api.test.ts` passes, and the suite contains a case whose
      tool list is exactly one tool named `workflow` asserting the probe is `false`.
    - `node -e` against the live module asserts `DEPENDENCIES` deep-equals
      `["agents","mcp","workflows"]` and `REASONS.length === 40` and
      `REASONS.at(-1) === "requires pi-dynamic-workflows"`, asserted directly rather than only
      through a gate this task edits.
    - `npm run typecheck` exits 0 — which is the coverage proof for the required third
      parameter, since an unvisited `composeReasons` / `companionSeverity` / `SoftDepStatus`
      construction cannot compile.
    - `grep -rnE 'requires pi-workflows' extensions/ tests/ docs/` returns nothing (the
      scope-less spelling names the wrong package).
    - The suite contains the source-string decoy case, and it asserts `false` — the behavioral
      proof that the workflows probe reads only the tool name.
    - `grep -c 'piWithBothLoaded' tests/architecture/catalog-uat.test.ts tests/shared/notify-v2.test.ts`
      returns 0 for both files.
    - `node --test tests/architecture/catalog-uat.test.ts` passes with no catalog state edited
      yet — the empirical check that the third probe moves no existing byte.
    - `git diff --name-only -- package.json package-lock.json sonar-project.properties` is empty.
    - RED was observed before GREEN on the probe cases, the two closed-set locks and the
      install-row pair; the observed failure text is recorded in the summary.
  </acceptance_criteria>

  <reversibility rating="costly">
    The token's spelling and the tuple's append position are cheap to change now and expensive
    once the catalog byte gate and the COMPAT-01 order array carry them. Both are settled by
    locked decisions; this task is where they are cast.
  </reversibility>

  <done>
The host engine is probed by its distinctive tool name, a bare-name session reads as absent,
`DEPENDENCIES` has three members with the new one last, `REASONS` has 40 with the new one at
the tail, and a workflow-bearing install with no engine loaded renders the degradation marker
at `warning` while an install with the engine loaded renders exactly the bytes it rendered
before.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The fact travels on every outcome the phase's later verbs read</name>

  <files>extensions/pi-claude-marketplace/orchestrators/types.ts, extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/enable-disable.test.ts</files>

  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/types.ts` lines 30-70 (the install outcome base carrying `declaresAgents` / `declaresMcp` at :61-62), lines 150-220 (the update base at :161-162 and the `stagedAgents?: never` refusal block at :210-216), lines 320-340
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 240-275 (the `Omit<LedgerDegradationSignals, ...>` shape) and lines 2005-2030 (the returned outcome literal, `declaresAgents` at :2017)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` lines 45-125 — `LedgerDegradationSignals` with `stagedAgents` at :83 and `stagedMcpServers` at :89, and `enableRowDependencies` at :108-123 including the `partition?: never` refusal at :110-111 and its WR-01 rationale
    - `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` lines 1410-1425 and 1835-1850 (`handles.workflows.result.stagedNames`, the source of update's staged-workflow fact) and lines 2495-2515 (`outcomeToCascadePluginMessage`, the `companionSeverity` call at :2510)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` lines 1260-1275 (`placedWorkflowNames` off `replaceAll`) and lines 930-960 (the outcome-to-row composer and the severity stamp at :956)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` lines 315-330 (`ledgerCtx.stagedAgentNames` -> `stagedAgents` at :323-324), lines 945-960 (the spread at :953-954), lines 1068-1082 (the `companionSeverity` call at :1075-1078)
    - `105-RESEARCH.md` section "Marker Plumbing: the full blast radius", tier 3 table
  </read_first>

  <behavior>
    Write these first and observe them red.
    - `update` of a plugin whose new version stages a workflow, engine absent: the update row's
      `ctx.ui.notify` severity argument is `warning`. Engine present: the WHOLE block is
      byte-asserted unchanged.
    - `reinstall` of a workflow-bearing plugin, engine absent: severity stays whatever the
      reasons alone produce — the reinstall row does NOT raise on an unloaded companion, which
      is the existing deliberate asymmetry the catalog already pins for the other two markers.
    - `enable` of a disabled workflow-bearing plugin, engine absent: severity is `warning`.
      Engine present: the whole block byte-asserted unchanged.
    - a plugin that staged zero workflows on each of those three verbs: whole block asserted,
      no severity move.
  </behavior>

  <action>
Add `declaresWorkflows: boolean` as a REQUIRED member beside `declaresAgents` / `declaresMcp`
on both outcome bases in `orchestrators/types.ts` — the install outcome base at :61 and the
update base at :161. Required, so every producer fails to compile until it supplies the fact.
Extend the doc comments that explain the pair to describe the triple, and extend the
`stagedAgents?: never` / `stagedMcpServers?: never` refusal block to include the new signal
name so the update and reinstall partitions keep excluding the enable-shaped signals.

Add `stagedWorkflows?: boolean` to `LedgerDegradationSignals` in `orchestrators/plugin/shared.ts`,
directly after `stagedMcpServers`, with a doc comment mirroring its two siblings: it is a COUNT
verdict only, the staged workflow names never reach a rendered row, and it drives both the
marker and the info-to-warning raise on the enable row.

Supply the fact at each producer:
- `install.ts`'s returned outcome literal at :2017 gains
  `declaresWorkflows: installCtx.stagedWorkflowNames.length > 0`.
- `enable-disable.ts` at :323-324 gains a spread arm setting `stagedWorkflows: true` when
  `ledgerCtx.stagedWorkflowNames.length > 0`, and the carry-forward spread at :953-954 gains
  the matching `outcome.stagedWorkflows === true` arm. Both use the omitted-when-false spread
  form the two siblings use, so an unaffected outcome renders byte-identically.
- `update.ts` sets `declaresWorkflows` on its updated-partition outcome from
  `handles.workflows.result.stagedNames.length > 0` — the same array the record write at
  :1420-1421 and :1846 already consumes.
- `reinstall.ts` sets `declaresWorkflows` on its reinstalled outcome from
  `placedWorkflowNames.length > 0`, the array `replaceAll` already returns at :1268.

Read the fact at the three `companionSeverity` sites:
- `install.ts:1967` already did this in the previous task.
- `update.ts:2510` passes `declaresWorkflows: outcome.declaresWorkflows`.
- `enable-disable.ts:1075` passes `declaresWorkflows: outcome.stagedWorkflows === true`,
  matching how the two siblings are read there.

Leave `reinstall.ts:956` alone. It stamps severity from `reasons.length` and never calls
`companionSeverity`; the catalog's reinstall soft-dep block carries no summary line, which
byte-pins that asymmetry as deliberate. Copying it keeps the new marker behaving like its two
siblings on every verb rather than making reinstall the one kind that behaves differently.
Record that decision in a comment at the reinstall severity site so a later reader does not
read it as an oversight.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/enable-disable.test.ts && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` exits 0 with `declaresWorkflows` declared REQUIRED on both outcome
      bases — verifiable by `grep -n 'declaresWorkflows?' extensions/pi-claude-marketplace/orchestrators/types.ts`
      returning nothing (no optional form anywhere).
    - `grep -rn 'companionSeverity(' extensions/pi-claude-marketplace/orchestrators/` returns
      exactly three call sites, and each of the three is followed within five lines by a
      `declaresWorkflows` argument.
    - `grep -n 'companionSeverity' extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts`
      returns nothing — the no-raise asymmetry is preserved, not "fixed".
    - The three per-verb positive cases assert `warning` on the `ctx.ui.notify` severity
      argument itself, not only on the presence of a summary line.
    - Each negative case byte-asserts the WHOLE rendered block.
    - RED observed before GREEN on all three verbs.
  </acceptance_criteria>

  <done>
Every outcome a later verb reads carries whether the run staged a workflow, and the three
surfaces that raise severity on an unloaded companion raise on this one too, while reinstall
keeps its existing deliberate non-raise.
  </done>
</task>

<task type="auto">
  <name>Task 3: Two catalog states pin the brace, and the catalog's stale counts are corrected</name>

  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>

  <read_first>
    - `docs/output-catalog.md` lines 55-75 (the reasons-rendering prose, the stale
      `38-member` sentence at :63, the soft-dep paragraph naming both markers and "the 4
      dep-bearing variants" at :67) and lines 455-500 (the install section: the `success` state,
      the `success-with-soft-dep` state at :470-482 with its `A plugin operation needs attention.`
      summary line at :475, and the `success-with-orphan-rewake-and-soft-dep` state that is the
      precedent for pinning a composed brace with its own state)
    - `tests/architecture/catalog-uat.test.ts` lines 1-160 (the bidirectional walk: a state with
      no fixture is an orphan and a fixture with no state is too) and lines 280-520 (the install
      section's fixture entries, including the `success-with-soft-dep` fixture at ~:504 which
      uses `piWithMcpLoaded`)
    - `105-RESEARCH.md` section "Catalog Amendment (WDOC-02)"
    - `.planning/workstreams/workflows/phases/104-workflow-lifecycle-completion/104-06-SUMMARY.md`
      "Notes for the next phase" — the additive-amendability note
  </read_first>

  <action>
Add exactly two new catalog states under the existing
`## \`/claude:plugin install <plugin>@<marketplace>\`` H2, each as a sibling inserted after
`success-with-soft-dep` and removing no existing line.

The first, `success-with-workflow-engine-absent`, shows a plugin that staged workflows and
nothing else companion-bearing, in a session with no host engine: one marker alone inside the
brace. Because the row stamps `warning`, `notify()` prepends the summary line, so the block
opens with that line exactly as the sibling state does. Its prose paragraph states that the
envelopes were written and are correct, that nothing runs them until the engine is present,
and that installing the engine and reloading makes them run with no reinstall.

The second, `success-with-soft-dep-and-workflow-engine-absent`, shows a plugin declaring BOTH
`agents` and `workflows` with neither companion loaded, so two markers share one brace. This
state exists to byte-pin the ORDER inside the brace — the pi-subagents marker first, the host
engine marker last, matching the `DEPENDENCIES` tuple. A single-marker state cannot pin that,
which is the same reasoning that produced the existing composed-brace state in this section.

Pair each state with a `CatalogFixture` in `tests/architecture/catalog-uat.test.ts`. Render each
message before writing its fenced block and copy the emitted bytes — do not hand-write the
block from an analogue, because a field the verb cannot emit produces a line the renderer never
prints. Both fixtures carry `expectedSeverity: "warning"`. The first uses a probe helper
exposing `subagent` and `mcp` but not the engine tool; the second uses the helper exposing
nothing.

Correct two prose sentences in the same file, both outside the byte gate:
- the reasons-rendering paragraph at :63 names a member count that was already one behind
  before this phase started and is now two behind. Set it to the current tuple length.
- the soft-dep paragraph at :67 names the two existing markers and must name the third, and it
  must keep its statement that the renderer emits them from the row's `dependencies` field
  rather than callers placing them in `reasons`.

Do not restructure any other region of the file. The uninstall region was deliberately shaped
by the previous phase to admit a second additive amendment; a restructure moves bytes on states
this plan did not touch and fails the gate for unrelated reasons.
  </action>

  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'catalog-state: success-with-workflow-engine-absent' docs/output-catalog.md`
      returns 1, and the same for `success-with-soft-dep-and-workflow-engine-absent`.
    - `git diff docs/output-catalog.md | grep -c '^-'` returns 3 or fewer. One of those is the
      diff's own header line; the rest are the corrected prose sentences. No fenced-block line
      is removed.
    - `node --test tests/architecture/catalog-uat.test.ts` passes, which exercises the walk in
      BOTH directions, so neither new state is an orphan and neither new fixture is.
    - The two-marker state's fenced block shows the pi-subagents marker before the host-engine
      marker; reversing the two arms of `softDepMarkers` makes this test fail.
    - `grep -cE '\b3[0-9]-member\b' docs/output-catalog.md` returns 0 — no surviving sentence
      names a member count below the current `REASONS` length.
  </acceptance_criteria>

  <done>
`docs/output-catalog.md` carries the new token in two byte-gated rendered states, one of which
pins the two-marker brace order, both fixtures pair in both directions, and the file's two
stale prose sentences are correct.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi host -> extension | `pi.getAllTools()` returns host-controlled objects; the probe reads `tool.name` off them |
| extension -> operator | the rendered reason token is the operator's only signal about which engine is or is not present |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-105-01-01 | Spoofing | `hasLoadedWorkflowEngine` in `platform/pi-api.ts` | high | mitigate | An engine that is NOT the sandboxed host must never satisfy the predicate. Match the distinctive tool name by exact string identity, add no `sourceInfo.source` arm, and pin the bare-name session as ABSENT in a dedicated test case. A false positive would tell the operator their untrusted scripts run in a `vm.createContext` realm when they do not. |
| T-105-01-02 | Information disclosure | the probe's `catch` block | medium | mitigate | The `catch` returns `false` and neither logs, rethrows, nor reads the caught value — byte-symmetric with both existing probes. A host-thrown error can carry paths or configuration detail; surfacing it through the one sanctioned output channel would leak it into the operator's transcript. |
| T-105-01-03 | Information disclosure | the new reason token | low | accept | The token is a fixed closed-set literal with zero interpolation. No plugin, workflow or path name can reach it, which is the same reasoning that made the equivalent threat inapplicable in the previous phase. Accepted with that rationale recorded. |
| T-105-01-04 | Tampering | `DEPENDENCIES` / `REASONS` order | medium | mitigate | Both tuples are byte-critical. The new member appends LAST in both; the new order-and-length lock and the byte-gated two-marker catalog state make a reorder a red test rather than a silent rendering change. |
| T-105-01-SC | Tampering | npm dependency manifest | high | mitigate | This plan installs nothing and no package-legitimacy audit is required. A diff touching `package.json`, `package-lock.json` or `sonar-project.properties` is itself a review finding; the acceptance criteria assert those paths are untouched. |
</threat_model>

<verification>
- `npm run check` exits 0 — typecheck, ESLint, Prettier, the unit suite and the integration suite.
- `node -e` against the live modules asserts, independently of any gate this plan edits:
  `DEPENDENCIES` deep-equals the three-member tuple in order; `REASONS.length` is 40; the tail
  member is the new token.
- `npx eslint` on every changed source file reports zero errors, including
  `sonarjs/cognitive-complexity` and `sonarjs/no-identical-functions`, with no new disable
  directive.
- No line added by this plan cites a GSD process artifact: a diff grep for phase, plan, wave,
  task, milestone or bare pitfall/pattern references over the added lines returns nothing. The
  anchors used are `WDEP-01`, `WDEP-02`, `WDEP-04`, `WDOC-02`, `RH-3`, `RH-4`, `SNM-06`,
  `D-16-15`, `MSG-GR-4`, `MSG-SD-3`, `OUT-08`, `SEV-01`, `NREG-01`, `NFR-2`.
- `git diff --name-only` names no dependency manifest.
</verification>

<success_criteria>
1. A session exposing only a tool named `workflow` reads the host engine as absent; a session
   exposing both names reads it as present; an empty list and a throwing call both read absent.
2. Installing a workflow-bearing plugin with no engine loaded writes every envelope, succeeds,
   and renders the degradation marker at `warning` severity.
3. Installing the same plugin with the engine loaded renders byte-for-byte the block it
   rendered before this plan.
4. `DEPENDENCIES` is three members in order under a lock that did not exist before; `REASONS`
   is 40 with the new token at the tail and nothing moved.
5. Two markers in one brace render in tuple order, byte-pinned by a catalog state.
6. `npm run check` is green and no dependency manifest changed.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-01-SUMMARY.md` when done.
</output>
