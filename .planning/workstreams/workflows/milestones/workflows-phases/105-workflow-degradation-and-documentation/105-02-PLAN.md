---
phase: 105-workflow-degradation-and-documentation
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
autonomous: true
requirements: [WDEP-02, WDEP-04]

files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - tests/orchestrators/plugin/list.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/orchestrators/reconcile/notify.test.ts
  - tests/orchestrators/import/execute.test.ts
  - tests/architecture/notify-stamp-coverage.test.ts

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every surface that can render `{requires pi-subagents}` today can also render the host-engine marker: install, update, reinstall, enable, the list inventory row, the import cascade and the reconcile projections."
    - "The three surfaces that structurally cannot carry a soft-dep marker still cannot: the uninstalled row, the disabled row, and the marketplace-update child rows each keep passing `false` for all three declares-flags, and each keeps its recorded rationale in place."
    - "Every `Dependency[]` derivation pushes `workflows` LAST, after `mcp`, so no caller's assembly order can move the rendered brace bytes."
    - "The list inventory row derives the workflows declaration from the persisted `record.resources.workflows` array, below the disabled early return, so a disabled record's retained inventory never reaches a rendered row."
    - "`enableRowDependencies`'s refusal of the update and reinstall outcome shapes survives the widening — an outcome discriminated by its partition field still fails to satisfy the parameter."
  artifacts:
    - tests/architecture/notify-stamp-coverage.test.ts (a clause asserting every dep-bearing surface can produce the host-engine marker)
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts (enableRowDependencies third arm)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (dependenciesFromDeclares third arm)
  key_links:
    - "a surface's outcome field or record array -> its `Dependency[]` derivation -> the row's `dependencies` -> composeReasons -> the brace"
    - "a derivation that silently omits its third arm is NOT a compile error, because it returns a valid `Dependency[]` either way — the stamp-coverage gate is the only thing that catches it"
  prohibitions: []
  flagged_assumptions:
    - "The set of seven `Dependency[]` derivations was enumerated by grepping the `Dependency[]` type and the `declaresAgents` identifier, not by a compile error. A missed derivation renders a row without the marker silently. Task 3's stamp-coverage gate exists precisely because the grep is not a proof; if it surfaces an eighth site, add it rather than treating the list as closed."
---

<objective>
Give every surface that already renders a soft-dependency marker the third one, and prove it
with a gate rather than with a grep.

Purpose: the previous plan made the fact exist and travel. Nothing renders it yet outside the
standalone install row. Widening a `Dependency[]` derivation is NOT compile-forced — a
derivation that forgets its third arm still returns a valid array — so the seven derivations
are the one part of this phase where an incomplete sweep ships green.

Output: the six remaining `Dependency[]` derivations, the list inventory row's record read,
and a stamp-coverage gate that asserts each dep-bearing surface can produce the marker.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-CONTEXT.md
@.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-RESEARCH.md
@.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-01-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Artifact | Where |
|------|----------|-------|
| changed function | `outcomeDependencies(declaresAgents, declaresMcp, declaresWorkflows)` | `orchestrators/plugin/update-row.ts` |
| changed function | `dependenciesFromOutcome` gains a third push | `orchestrators/plugin/reinstall.ts` |
| changed function | `dependenciesFromInstalled` gains a third push | `orchestrators/import/execute.ts` |
| changed function | `dependenciesFromInstall` gains a third push and a third parameter field | `orchestrators/reconcile/apply.ts` |
| changed function | `dependenciesFromDeclares(declaresAgents, declaresMcp, declaresWorkflows)` | `orchestrators/plugin/list.ts` |
| changed function | `enableRowDependencies` picks `stagedWorkflows` and gains a third push | `orchestrators/plugin/shared.ts` |
| new test file | `tests/architecture/notify-stamp-coverage.test.ts` gains a marker-coverage clause | tests |
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four outcome-bearing derivations render the third marker</name>

  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/orchestrators/reconcile/notify.test.ts, tests/orchestrators/import/execute.test.ts</files>

  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts` lines 105-180 — the row composer at ~:112 and `outcomeDependencies` at :167-175
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` lines 910-960 (the outcome-to-row composer and the deliberate non-raise at :956) and lines 1060-1085 (`dependenciesFromOutcome` at :1073-1082, note its `Object.freeze` return)
    - `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` lines 70-90 and lines 310-330 (`dependenciesFromInstalled` at :317-325, also `Object.freeze`) and lines 720-735
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts` lines 315-335 (`dependenciesFromInstall` at :319-332 — its parameter is a structural object literal type, so the third field must be added to the type as well as the body)
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts` lines 88-100 and 128-140 (the two `dependencies: readonly Dependency[]` carriers)
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts` around lines 505-520 and 605-630 (the two projection row composers and the recorded non-stamp note)
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` lines 565-585 (the child rows that hard-code the declares-flags to false, and why)
    - `105-RESEARCH.md` sections "Pattern 3: the `Dependency` -> row derivation, five copies" and "Marker Plumbing", tier 3
  </read_first>

  <behavior>
    Write first, observe red:
    - the update row for a plugin whose outcome declares workflows, engine absent, renders the
      host-engine marker inside the brace, LAST after any pi-subagents marker
    - the reinstall row for the same outcome renders the marker and does NOT gain a summary line
    - the import cascade row and both reconcile projection rows render the marker
    - the negative half of each pair byte-asserts the WHOLE rendered block over an outcome that
      declares no workflows
  </behavior>

  <action>
Widen the four outcome-driven derivations, each pushing the new member LAST:

- `update-row.ts::outcomeDependencies` takes a third boolean parameter `declaresWorkflows` and
  pushes `"workflows"` after the mcp push; its caller at ~:112 forwards
  `outcome.declaresWorkflows`.
- `reinstall.ts::dependenciesFromOutcome` reads `outcome.declaresWorkflows` and pushes after
  the mcp push, keeping the `Object.freeze` return.
- `import/execute.ts::dependenciesFromInstalled` reads `o.declaresWorkflows` and pushes after
  the mcp push, keeping the `Object.freeze` return and its defense-in-depth comment.
- `reconcile/apply.ts::dependenciesFromInstall` gains `readonly declaresWorkflows: boolean` on
  its structural parameter type and pushes after the mcp push.

Confirm by reading, and record in the summary, that the two reconcile projection carriers in
`apply-outcomes.ts` already type their field as `readonly Dependency[]` and therefore need no
change beyond what the derivation supplies, and that the reconcile projections keep their
existing `info` severity — the reconcile cascade is not visible in the host interface, which is
why the previous phase deliberately did not raise there either.

Leave `marketplace/update.ts`'s child rows hard-coding all three declares-flags to `false`.
Those rows report a marketplace-wide operation spanning many plugins; a per-plugin companion
advisory on them would attribute a fact to the wrong subject. Extend the existing rationale
comment there to name the third flag so the omission stays recorded rather than looking like a
miss.

Do not touch `orchestrators/plugin/info.ts`. It uses the identifier `dependencies` for the
Claude plugin manifest's own dependency declaration, an unrelated concept; a sweep driven by
the bare word edits it wrongly. Sweep by the `Dependency` TYPE and the `declaresAgents`
identifier instead.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/reconcile/notify.test.ts tests/orchestrators/import/execute.test.ts && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - Every function whose return type mentions `Dependency[]` in
      `orchestrators/plugin/update-row.ts`, `orchestrators/plugin/reinstall.ts`,
      `orchestrators/import/execute.ts` and `orchestrators/reconcile/apply.ts` contains exactly
      three `push` calls, and in each the workflows push is textually last.
    - `git diff --name-only` does NOT list `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts`.
    - `grep -n 'declaresWorkflows' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts`
      returns a line whose value is `false`, alongside the existing rationale comment.
    - `npm run typecheck` exits 0 and `npx eslint` on the changed files reports zero errors,
      including `sonarjs/no-identical-functions` — if the widened copies trip it, extract a
      shared helper rather than adding a disable directive.
    - RED observed before GREEN on each of the four surfaces.
  </acceptance_criteria>

  <done>
Update, reinstall, the import cascade and both reconcile projections each render the
host-engine marker last inside the brace when their outcome declares a workflow and the engine
is absent, and each renders its previous bytes exactly when it does not.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The list inventory row and the enable row derive it from their own signals</name>

  <files>extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts, tests/orchestrators/plugin/list.test.ts, tests/orchestrators/plugin/enable-disable.test.ts</files>

  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` lines 290-320
      (`dependenciesFromDeclares` at :300-310), lines 435-460 (the disabled early return above
      it and the `record.resources` reads at :450-451, with the comment stating why the
      derivation sits BELOW the return), and around line 573 (the row that consumes it)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` lines 45-125 — the
      `stagedWorkflows` signal the previous plan added, `enableRowDependencies` at :108-123, and
      the `partition?: never` refusal at :110-111 with its WR-01 rationale
    - `extensions/pi-claude-marketplace/persistence/state-io.ts` around line 132 — `workflows`
      is a REQUIRED `string[]` on the record's `resources`, so the list read needs no optional
      handling and no schema change
    - `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` lines 1060-1085
      (the enable row composer consuming `enableRowDependencies`)
  </read_first>

  <behavior>
    Write first, observe red:
    - a `list` inventory row for an installed record whose `resources.workflows` is non-empty,
      engine absent, renders the host-engine marker
    - a `list` inventory row for a DISABLED record whose `resources.workflows` is non-empty
      renders no marker at all — the disabled row's whole block is byte-asserted
    - an `enable` row for a ledger run that staged a workflow, engine absent, renders the marker
    - a compile-level check that an update or reinstall outcome shape still does NOT satisfy
      `enableRowDependencies`'s parameter after the widening
  </behavior>

  <action>
In `list.ts`, give `dependenciesFromDeclares` a third boolean parameter and a third push after
the mcp push, and derive its argument at the existing declares site from
`record.resources.workflows.length > 0`. That derivation must stay BELOW the disabled early
return, exactly where its two siblings sit: a disabled record retains its inventory arrays, and
a soft-dependency marker states a runtime concern that is suspended while the plugin is
disabled. Extend the comment that already explains this so it names all three inventories.

In `shared.ts`, widen `enableRowDependencies`'s `Pick<LedgerDegradationSignals, ...>` to include
`stagedWorkflows`, and push `"workflows"` when `signals.stagedWorkflows === true`, after the mcp
push. The `partition?: never` refusal must survive the widening unchanged: it is what excludes
the update and reinstall outcome shapes, which spell the same facts under different field names
and would otherwise satisfy this parameter structurally and silently return an empty array for
every update. Extend the function's doc comment to say that the refusal now guards three
optional fields rather than two.

Nothing changes in `persistence/state-io.ts`. The record already carries `resources.workflows`
as a required array, so there is no schema version to bump and no migration.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/enable-disable.test.ts && npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'partition?: never' extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts`
      still returns a line, and the widened `Pick<...>` names three members.
    - The disabled-record case byte-asserts the WHOLE rendered block and that block contains no
      brace.
    - `grep -n 'record.resources.workflows' extensions/pi-claude-marketplace/orchestrators/plugin/list.ts`
      returns a line whose line number is greater than the line number of the disabled early
      return.
    - `npm run typecheck` exits 0; `git diff --name-only -- extensions/pi-claude-marketplace/persistence/`
      is empty.
    - RED observed before GREEN on the list positive and the enable positive.
  </acceptance_criteria>

  <done>
The list inventory row declares the host engine from the persisted record, the enable row
declares it from the ledger's staged-count signal, a disabled record declares nothing, and the
refusal that keeps update and reinstall outcomes out of the enable derivation still holds.
</done>
</task>

<task type="auto">
  <name>Task 3: A gate proves every dep-bearing surface can produce the marker</name>

  <files>tests/architecture/notify-stamp-coverage.test.ts</files>

  <read_first>
    - `tests/architecture/notify-stamp-coverage.test.ts` (whole file) — the existing shape,
      including the previous phase's non-vacuity convention: a projection guard asserts the
      sample carries rows that WOULD stamp on their standalone counterparts
    - `105-RESEARCH.md` "Assumptions Log" entry A4 — the derivation set was enumerated by grep,
      and a missed derivation is silent rather than a compile error, which is what this gate
      answers
    - `105-RESEARCH.md` "Marker Plumbing", tier 3 table — the authoritative stamp / non-stamp
      partition
  </read_first>

  <action>
Add a clause to `tests/architecture/notify-stamp-coverage.test.ts` that walks the dep-bearing
surfaces and asserts each one, given a row whose declares-workflows fact is true and a probe
reporting the engine absent, renders the host-engine marker. Cover install, update, reinstall,
enable, the list inventory row, the import cascade row and both reconcile projection rows. Drive
each through its own row composer rather than through a single shared renderer call, because a
derivation that forgot its third arm is exactly what would otherwise pass.

Add the mirror clause for the three structural non-stamps: the uninstalled row, the disabled
row and the marketplace-update child rows each render no marker even when the subject plugin's
record names workflows. Each of these cases must seed a record that WOULD stamp if the
derivation were reachable, so the assertion is non-vacuous — the same shape the previous phase
used for its projection guard.

Give the clause a comment stating why it exists: growing a `Dependency[]` derivation is not
compile-forced, so the derivation set is the one part of this change that a partial sweep ships
green.
  </action>

  <verify>
    <automated>node --test tests/architecture/notify-stamp-coverage.test.ts && npm run check</automated>
  </verify>

  <acceptance_criteria>
    - `node --test tests/architecture/notify-stamp-coverage.test.ts` passes with at least eight
      positive surface cases and three non-stamp cases.
    - Reverting any ONE of the six derivations widened in tasks 1 and 2 makes this gate red;
      the summary records which derivation was reverted to demonstrate that, and that the revert
      was undone.
    - Each non-stamp case seeds a record naming at least one workflow, so the assertion cannot
      pass by the subject having nothing to declare.
    - `npm run check` exits 0.
  </acceptance_criteria>

  <done>
A red test, not a grep, is what says every surface that can render a soft-dependency marker
renders this one, and the three surfaces that structurally cannot are asserted non-vacuously.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted record -> rendered row | `record.resources.workflows` is read from disk and drives whether a row declares the host engine |
| extension -> operator | the rendered brace is the operator's signal about which companion is missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-105-02-01 | Repudiation | the six `Dependency[]` derivations | medium | mitigate | A derivation that omits its third arm renders a row that silently under-reports the plugin's state — the operator is told the install is clean when a component of it cannot run. The stamp-coverage gate in Task 3 turns that omission into a red test, since the compiler cannot. |
| T-105-02-02 | Information disclosure | the list inventory row for a DISABLED record | low | mitigate | A disabled record retains its inventory arrays. Deriving the declaration above the disabled early return would surface a runtime advisory about a plugin that is not running. The derivation stays below the return and a byte-asserted disabled case pins it. |
| T-105-02-03 | Tampering | `orchestrators/plugin/info.ts` | low | mitigate | That file uses the identifier `dependencies` for the plugin manifest's own dependency declaration. A grep-driven sweep would edit it and change an unrelated user-facing surface. The sweep is specified by TYPE and by the `declaresAgents` identifier, and an acceptance criterion asserts the file is unchanged. |
| T-105-02-SC | Tampering | npm dependency manifest | high | mitigate | No package install is in scope; the plan's diff must name no dependency manifest. |
</threat_model>

<verification>
- `npm run check` exits 0.
- `grep -rn 'requires pi-dynamic-workflows' extensions/pi-claude-marketplace/orchestrators/`
  returns nothing: the marker is never caller-placed, only renderer-emitted from the row's
  `dependencies` field.
- `npx eslint` on every changed source file reports zero errors and no new disable directive.
- No added line cites a GSD process artifact; the anchors used are `WDEP-02`, `WDEP-04`,
  `SNM-06`, `SEV-01`, `MSG-SD-3`, `ENBL-15`, `WR-01`, `D-98-02`, `D-100-06`, `NREG-01`.
</verification>

<success_criteria>
1. Each of the seven `Dependency[]` derivations pushes the new member last.
2. The list inventory row declares it from the persisted record, below the disabled return.
3. The enable row declares it from the ledger signal, and the refusal excluding update and
   reinstall outcome shapes still holds.
4. The three structural non-stamps still stamp nothing, asserted non-vacuously.
5. A gate — not a grep — proves the coverage, and reverting any one derivation reddens it.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/105-workflow-degradation-and-documentation/105-02-SUMMARY.md` when done.
</output>
