---
phase: 260923-t30
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [NFR-11]
---

<objective>
Record the three upstream workflow engine issues in the changelog and PR #205,
then push the branch. Keep the package version unchanged and state that the PR
can merge while the release waits for upstream fixes.
</objective>

<tasks>

<task>
<name>Add short upstream PR links to the unreleased changelog entry</name>
<action>
Link pi-dynamic-workflows PRs #232, #233, and #234 under the workflow entry.
Say that release waits for those fixes. Keep the existing version files intact.
</action>
<verify>Review the diff and run the documentation hooks.</verify>
</task>

<task>
<name>Push the branch and update the existing PR description</name>
<action>
Push features/workflow to origin and add a short release-status paragraph to
PR #205. Preserve its existing description and footer.
</action>
<verify>Read the remote branch head and PR body after the update.</verify>
</task>

</tasks>
