---
phase: 115-install-time-admission-gate-warnings
plan: 05
type: execute
wave: 3
depends_on: ["115-01", "115-02"]
files_modified:
  - docs/workflows-compatibility.md
  - tests/architecture/workflows-doc-pins.test.ts
autonomous: true
requirements: [WGATE-05, WGATE-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The classification table in `docs/workflows-compatibility.md` states one of exactly three values — replicate, warn, neither — for every one of its nine rows, and every row agrees with what the bridge does (WGATE-05)."
    - "The set of gate names on the doc's `warn` rows, read in table order, equals the closed union in `domain/workflow-script.ts`, read out of the source text. A gate added to one and not the other reddens (WGATE-05)."
    - "Check 7's value is `neither` and its stated reason is UNREACHABILITY, not undetectability: `export const meta;` is a `SyntaxError` acorn rejects at the engine's check 2, and every non-`const` form fails check 4 first (D-115-02)."
    - "The document does not claim that any of the seven unreplicated checks needs the script to be evaluated. Nothing in the seven does; the substituted-template case is a one-property AST test the bridge decides the same way the engine does (D-115-03)."
    - "The document no longer states that install-time warnings for these shapes are not implemented; the paragraph is rewritten to say what is warned, what is not, and why warning was chosen over refusing — not annotated with a note (WGATE-05)."
    - "The three enumerations the existing doc-pin gate already holds still agree: nine consecutively numbered table rows, six message bullets, and the two bolded count phrases verbatim."
    - "The four-direction negative control on the union-to-doc totality construct was run, and all four transcripts are pasted into the SUMMARY — including direction 3, which is the one that catches a construct checking gate names but not column values."
  prohibitions:
    - statement: "MUST NOT understate what installs here and then refuses to run there. This bridge installs executable third-party code; a published contract that reads as though the divergence is narrower than it is costs a reader the trust the document exists to earn."
      category: transparency
  artifacts:
    - docs/workflows-compatibility.md
    - tests/architecture/workflows-doc-pins.test.ts
  key_links:
    - "`GATE_ORDER` in `domain/workflow-script.ts` (read as source text) <-> the `warn` rows' gate names in the doc table — the totality construct's second half"
    - "`Record<WorkflowGate, ...>` annotations in `workflow-script.ts` and `discover.ts` — the totality construct's first half, enforced by `tsc`"
---

<objective>
Restate the published admit-versus-run contract as replicate / warn / neither, retract
the sentence that says the opposite of this phase, and bind the doc's rows to the
source's closed union with a gate proven in four directions.

Purpose: a table that describes intent rather than behavior is the failure WGATE-05
exists to prevent. And this milestone has already shipped two guards that were green
because they checked nothing.

Output: the rewritten document, an extended doc-pin gate, and four pasted transcripts.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-RESEARCH.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-CONTEXT.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-01-SUMMARY.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-02-SUMMARY.md
</context>

<artifacts_produced>
| Artifact | Kind | Purpose |
|---|---|---|
| the classification table in `docs/workflows-compatibility.md`, restated with six columns | published contract | `# \| Check \| Engine's refusal message \| This bridge \| Gate name \| Notes` |
| a fourth case in `tests/architecture/workflows-doc-pins.test.ts` | architecture gate | binds the doc's column values AND gate names to the source's closed union |

No new production symbol. No new file.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Restate the admit-versus-run contract and retract the sentence that contradicts it</name>
  <files>docs/workflows-compatibility.md</files>
  <read_first>
    - `docs/workflows-compatibility.md` end to end (243 lines). The edit touches: `:50-54` (the divergence section's opening and its bounded-failure paragraph), `:56-67` (the script-shape divergence table and the template-literal paragraph), `:69` (the paragraph that must be retracted), `:71-89` (the refusal-check classification table and the sentence beneath it), `:91-100` (the six-bullet message list and the no-total-count rule), `:104-114` (the script-semantics table rows on first-statement, name-and-description, and pure-literal), `:205-238` (the five install-time dispositions, especially the caveat response).
    - `tests/architecture/workflows-doc-pins.test.ts` end to end (161 lines) — every assertion the edit must not redden. Lines 86-101 (the nine-row deepEqual, whose row regex is document-wide), 91-93 and 102-106 (the six-bullet count, taken as the SECOND blank-line-delimited chunk after its introducing marker), 107-116 (the two bolded count phrases, matched verbatim), and 117-123, whose bar is the trap: read the exact phrase it forbids there and do not write it into the document in any casing.
    - `115-RESEARCH.md` "The gate table, measured", "Therefore: the replicate / warn / neither column" (the row-by-row source for every cell), "The doc-pin gate" (the assertion-by-assertion table of what can redden), "The sentence that must be retracted, and everything that leans on it" (the seven sites that move with it), and Common Pitfalls "the doc edit reddens" and "the doc-pin count regexes are document-wide".
    - `115-CONTEXT.md` `### Settled after research` — D-115-01, D-115-02, D-115-03, D-115-07, D-115-09.
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts` as left by plans 115-01 and 115-02 — `GATE_ORDER` and the six member spellings the table must reproduce exactly.
    - `CLAUDE.md`: markdown is formatted by `mdformat` and `markdownlint-cli2` in pre-commit, never by prettier.
  </read_first>
  <action>
Restate the classification table and move every sentence that leans on the old claim.

THE TABLE. Give it six columns: the existing number, check and engine-message columns,
then `This bridge`, then `Gate name`, then `Notes`. Keep the nine rows numbered
consecutively, keep every row opening with its number so the existing row regex still
lands, and put no escaped pipe inside any cell — the new gate parses cells by splitting
on the delimiter.

`This bridge` holds exactly one of three literal values per row. Rows 1 and 2 replicate.
Rows 3, 4, 5, 6, 8 and 9 warn — those are the six warnable checks the research measured
(D-115-01), and note that this differs from the ROADMAP's criterion-1 list, which names
check 3 twice, names check 7, and omits check 6. Build the table from the measurement.
Row 7 is the only `neither`.

`Gate name` holds the union member for each `warn` row, spelled exactly as
`GATE_ORDER` spells it, and the two-hyphen placeholder for every other row. Reading the
spellings out of the source rather than out of this plan is what keeps them in step.

`Notes` carries the nuance, per D-115-09: the column stays three-valued and the
distinction rides prose. Row 7's note states UNREACHABILITY and gives the evidence —
`export const meta;` is a syntax error acorn rejects at the engine's check 2, and every
non-`const` form fails check 4 first, so no reachable script arrives there. Do not
describe it as undetectable (D-115-02); those are different facts and conflating them
would leave a reader believing the bridge cannot see something it can. Rows 6, 8 and 9
carry notes naming the sub-arms that are pre-empted by an existing skip or refusal
rather than warned: the destructuring form of check 6, the top-level spread and
computed-key arms of check 8, and the not-an-object and empty-name arms of check 9.
Row 8's note also records that the engine's reserved-key set is an unexported internal
of which only one key is measured, so an unmeasured reserved key is a missed warning
rather than a false one.

THE RETRACTED PARAGRAPH. Rewrite the paragraph at line 69 whole. It currently says
install-time warnings for these shapes are not implemented, and gives as its reason the
risk of refusing scripts a future engine release may relax. Half of that is now wrong
and the other half is the argument FOR what shipped. State what is warned, what is not,
and why warning was chosen over refusing: a spurious warning is one line a reader can
ignore, while a spurious refusal is a blocked install only an extension release can
clear, and the strict direction does not self-correct across engine upgrades. Do not
leave the old sentence standing with a note appended.

DO NOT repeat the old reason for a `neither` value. The research falsified it:
NOTHING in the seven unreplicated checks needs the script to be evaluated (D-115-03). A
substituted template in `meta.name` is a one-property AST test the bridge decides the
same way the engine's own literal evaluator does; what the bridge cannot do is RESOLVE
that name to a value, which is a different sentence. The document must not claim
otherwise.

THE SIX SITES THAT LEAN ON IT. Move each one, keeping every existing true statement:
the divergence section's opening sentence gains a clause saying six of the other seven
checks now produce an install-time warning; the bounded-failure paragraph gains one
sentence saying the signal now arrives earlier; the sentence beneath the classification
table that groups rows by the retired column value is rewritten or removed with that
column; the script-semantics row on `meta` as the first statement changes from
not-enforced to warned-and-not-enforced; the script-semantics row claiming the bridge
never checks the description is directly false after this phase and must be rewritten;
and the install-time disposition for a plugin installed with a caveat gains the
gate-warned population, widened rather than answered with a sixth response, because
nothing new happens on disk.

WHAT MUST NOT MOVE. Keep the two bolded count phrases byte-identical — the gate matches
them verbatim. Keep the six-bullet message list as the second blank-line-delimited chunk
after its introducing marker; inserting a paragraph between the marker sentence and the
bullets breaks the gate's slicing. Keep the standing rule that this document states no
total count of the engine's refusal messages, and add no such total. Add NO second
numbered table anywhere in the document: the row regex is document-wide, so a second one
makes the nine-row deepEqual fail with a doubled list. And do not write the two-word
retired figure the gate bars case-insensitively at
`tests/architecture/workflows-doc-pins.test.ts:117-123` — read it there and choose other
wording; the document already uses `checks` as its noun throughout, so phrasings built
on "the other seven checks" or "six of the seven" are available and true.

Format with `pre-commit run --files docs/workflows-compatibility.md`, never with
prettier, and re-run the doc-pin gate after formatting: `mdformat` re-pads table columns,
and the row regex depends on a row opening with its number followed by whitespace and
the delimiter.
  </action>
  <acceptance_criteria>
    - The classification table has six columns and nine consecutively numbered rows, and no cell contains an escaped pipe.
    - Every row's `This bridge` cell holds exactly one of the three literal values; rows 1 and 2 replicate, row 7 is the sole `neither`, and rows 3, 4, 5, 6, 8, 9 warn.
    - The six `warn` rows' `Gate name` cells reproduce `GATE_ORDER`'s six member spellings in the same order; every other row's `Gate name` cell is the two-hyphen placeholder.
    - Row 7's `Notes` cell states unreachability and cites the syntax error acorn raises.
    - The paragraph at the old line 69 no longer asserts that install-time warnings are not implemented, and the document contains no claim that any unreplicated check requires evaluating the script.
    - The script-semantics row asserting the bridge never checks the description is rewritten.
    - The caveat disposition's bullet list names the gate-warned population.
    - `node --test tests/architecture/workflows-doc-pins.test.ts` reports `# fail 0` after the formatter has run.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/workflows-doc-pins.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`; a message quoting a retired figure means the barred two-word phrase reached the document</fails_when>
    <automated>pre-commit run --files docs/workflows-compatibility.md</automated>
    <fails_when>non-zero exit, or any hook reports `Failed`</fails_when>
    <automated>node --test tests/architecture/workflows-doc-pins.test.ts</automated>
    <fails_when>non-zero exit after the formatter ran, meaning `mdformat` reflowed a row past the gate's row regex</fails_when>
    <automated>grep -c '^| [0-9]' docs/workflows-compatibility.md</automated>
    <fails_when>the printed count is anything other than `9`, meaning a second numbered table entered the document</fails_when>
  </verify>
  <done>The published contract states replicate / warn / neither per gate with the union member named on every warned row, the retracted sentence is gone rather than annotated, and every sentence that leaned on it moved with it.</done>
  <reversibility rating="costly">`docs/workflows-compatibility.md` is the published contract for the one bridge that installs executable code; a second restatement of the same column churns a document users are asked to trust.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Bind the doc's rows to the source's closed union</name>
  <files>tests/architecture/workflows-doc-pins.test.ts</files>
  <read_first>
    - `tests/architecture/workflows-doc-pins.test.ts` end to end — the module header stating what the gate deliberately does NOT claim, the three existing cases, and the `readRepoFile` helper.
    - `tests/architecture/source-scan.ts:34` (`REPO_ROOT`) and `:47-51` (`stripComments`) — the mechanic for reading a source file's text without matching its own prose.
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts` as left by plans 115-01 and 115-02 — the literal form of `GATE_ORDER`, which this gate parses out of the source text.
    - `docs/workflows-compatibility.md` as left by Task 1 — the six-column table shape.
    - `115-RESEARCH.md` "The doc-pin gate", especially the paragraph headed "The honest update to this gate".
    - `115-VALIDATION.md` "The Negative Control for the Gate-Name Totality Construct".
    - `.planning/codebase/CONVENTIONS.md`, the paragraph beginning "A gate wants a test that plants the violation, not one that reads the config."
  </read_first>
  <action>
Add a fourth case to the doc-pin gate: the doc-to-union binding.

Parse every row of the classification table into its six cells. Collect three
projections: the row numbers whose `This bridge` value is each of the three literals,
and the ordered list of gate names on the rows valued `warn`.

Assert four things, each with its own requirement-anchored failure message:

1. Column totality — every one of the nine rows carries exactly one of the three literal
   values. A blank cell or a fourth value fails. This is what makes the column a closed
   set rather than free text.
2. The partition — the replicating rows, the warning rows and the single `neither` row
   are each exactly the set the bridge implements. The `warn` set is derived from the
   source in the next assertion; the other two are pinned literally, with a failure
   message saying that a change here means the bridge's behavior changed and the gate
   must be re-derived from a measurement rather than edited to match the document.
3. The union binding — read `GATE_ORDER`'s members out of
   `extensions/pi-claude-marketplace/domain/workflow-script.ts` by reading the file,
   stripping its comments, and extracting the tuple's string literals; then `deepEqual`
   them, in order, against the `warn` rows' gate names in table order. This is the half
   that no single edit can satisfy on its own: the union lives in TypeScript and the
   rows live in markdown.
4. Placeholder discipline — every non-`warn` row's `Gate name` cell is the placeholder,
   so a gate name cannot hide on a row the document says is not warned.

Read both files through `readFile`, never a `grep` subprocess (D-98-10). Strip comments
before extracting the tuple, or the module's own prose about the gates would be parsed as
members. Do not import any `*.test.ts` module (D-98-09).

Leave the existing module header's disclaimer in place. It says the gate does not claim
the nine and the six are the engine's real figures, and that is still true: the re-read
against 3.10.1 is not machine-checkable and `WPIN-01` remains deferred. Record the
re-read date in the document's own evidence-graded prose instead, where dated claims
already live, rather than in this header.

Do not weaken any existing assertion in this file to get green. If the retired-figure bar
fires, the fix is in the document.
  </action>
  <acceptance_criteria>
    - The file contains a fourth `test(` case whose title cites WGATE-05.
    - It asserts a three-valued column over all nine rows, the three row-number partitions, an ordered `deepEqual` between the source's `GATE_ORDER` members and the doc's `warn` gate names, and the placeholder on every non-`warn` row.
    - It reads `extensions/pi-claude-marketplace/domain/workflow-script.ts` through `readFile` and passes it through `stripComments` before extracting members.
    - No existing assertion in the file is removed or loosened.
    - `node --test tests/architecture/workflows-doc-pins.test.ts` reports `# fail 0` and a non-zero `# pass` count.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/workflows-doc-pins.test.ts</automated>
    <fails_when>non-zero exit, the TAP summary does not read `# fail 0`, or it reads `# pass 0`</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output names any rule under `tests/architecture/workflows-doc-pins.test.ts`</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
  </verify>
  <done>The doc's column values and gate names are bound to the source's closed union by a gate that reads both artifacts, and the existing three cases are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Run the four-direction negative control on the totality construct</name>
  <files>tests/architecture/workflows-doc-pins.test.ts</files>
  <read_first>
    - `115-VALIDATION.md` "The Negative Control for the Gate-Name Totality Construct" — the four directions, verbatim, and the instruction to paste all four transcripts rather than a summary of them.
    - `115-CONTEXT.md` `<decisions>`, "The gate name is a closed set" — the mandate and the reason: this milestone has shipped two guards that were green because they checked nothing, one of them a forcing construct that was unconditionally `never`.
    - `.planning/workstreams/workflows/STATE.md`, "And its companion: a guard can be green because it checks nothing."
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts` — `GATE_ORDER` and the `Record<WorkflowGate, ...>` annotation on `GATE_PREDICATES`.
    - `extensions/pi-claude-marketplace/bridges/workflows/discover.ts` — the `Record<WorkflowGate, string>` annotation on `GATE_REASONS`.
  </read_first>
  <action>
Run the control in all four directions, on the working tree, restoring after each. This
is a task, not a note: the construct binds two artifacts a single edit cannot change
together, and a construct can be one-way green.

Direction 1 — the union grows and the document does not. Add a fabricated seventh member
to `GATE_ORDER`. Expect `npm run typecheck` red, because the two `Record` annotations are
no longer total, AND the doc-pin gate red, because the ordered comparison no longer
matches. Capture both outputs. Restore.

Direction 2 — a document row loses its gate name and the union does not change. Delete
one warned row's gate name from the table. Expect the doc-pin gate red. Capture. Restore.

Direction 3 — a document row's column VALUE is falsified, with no code change. Change one
row from the warning value to the third value. Expect the doc-pin gate red. Capture.
Restore. THIS IS THE ONE THAT MATTERS: if it stays green, the gate compares gate names
and not column values, which is half of what criterion 4 asks, and the gate must be
strengthened before this plan is done. Do not record a green here as a pass.

Direction 4 — the construct's own liveness. With the fabricated seventh member still in
place, remove the explicit `Record<WorkflowGate, ...>` annotation from the predicate map
and re-run `npm run typecheck`. The purpose is to attribute direction 1's redness: if the
typecheck error that named the predicate map disappears, the annotation was what fired,
which is the proof. Record exactly what was observed in each configuration rather than
asserting an expected outcome — a construct that is red for an unrelated reason is the
failure this direction exists to detect. Restore both edits and confirm the tree is clean.

Paste all four transcripts into the SUMMARY verbatim. A summary of a transcript is not a
transcript, and the whole point of the control is that the evidence is readable by
someone who did not run it.

Confirm the working tree is restored before finishing: `git status --short` over the two
source modules and the document must be empty of unintended changes.
  </action>
  <acceptance_criteria>
    - The SUMMARY carries four labelled transcripts, pasted verbatim, one per direction.
    - Direction 1's transcript shows both a typecheck error naming the predicate map and a red doc-pin gate.
    - Direction 2's transcript shows a red doc-pin gate.
    - Direction 3's transcript shows a red doc-pin gate. If it showed green, the gate was strengthened and the direction re-run, and both runs are in the SUMMARY.
    - Direction 4's transcript records what was observed with and without the annotation, attributing direction 1's typecheck redness.
    - `git status --short -- extensions/pi-claude-marketplace/domain/workflow-script.ts extensions/pi-claude-marketplace/bridges/workflows/discover.ts docs/workflows-compatibility.md` prints nothing after restoration.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -z "$(git status --short -- extensions/pi-claude-marketplace/domain/workflow-script.ts extensions/pi-claude-marketplace/bridges/workflows/discover.ts docs/workflows-compatibility.md)"'</automated>
    <fails_when>non-zero exit, meaning a control edit was left in the tree</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node --test tests/architecture/workflows-doc-pins.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
  </verify>
  <done>All four directions were run and their transcripts are in the SUMMARY, so the union-to-document construct is believed on evidence rather than on its shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published document -> a plugin author's trust | this is the contract for the one bridge that installs executable third-party code; an understatement here is a trust failure, not a typo |
| source union <-> document table | two artifacts a single edit cannot change together; the gate is the only thing holding them in step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-22 | Repudiation | `docs/workflows-compatibility.md` | high | mitigate | The document is rewritten so that every row states what the bridge does, not what it intends. The new gate binds column values and gate names to the source, and the four-direction control proves the gate fires. The prose half — honest evidence grading, the unreachability explanation — stays reviewer-read, which is the stated ceiling. |
| T-115-23 | Tampering | the new doc-pin gate | high | mitigate | Direction 3 of the control exists specifically to detect a gate that compares gate names but not column values, which is the "green because it checked nothing" failure this milestone has already shipped twice. A green direction 3 is recorded as a defect, not a pass. |
| T-115-24 | Information disclosure | the document's engine citations | low | accept | Every `src/...` citation is pinned to 3.10.1 and the document already says so; nothing in this edit adds a new unpinned citation. |
| T-115-25 | Spoofing | the retired-figure bar | medium | mitigate | The bar is not weakened to accommodate the rewrite. The rewrite avoids the phrase, and the gate's failure message is the signal if it does not. |
| T-115-26 | Tampering | dependency manifests, Sonar properties, changelog | high | mitigate | The manifest-diff guard in `<verification>` proves none was touched; the version bump and the changelog entry are milestone-close work, out of this phase by CONTEXT. |
</threat_model>

<verification>
```
npm run check
```

Fails when: non-zero exit at any stage of the chain (`typecheck`, `lint`, `fallow`,
`format:check`, `test:corresponding`, `test:corresponding:negative`,
`test:coverage:direct:negative`, `test`, `test:integration`).

```
pre-commit run --all-files
```

Fails when: non-zero exit, or any hook reports `Failed`. Run `--all-files`, not a scoped
`--files` run: CI runs `--all-files` and a scoped run hides pre-existing violations.

```
bash -c 'test -z "$(git diff HEAD --name-only -- package.json package-lock.json sonar-project.properties CHANGELOG.md)"'
```

Fails when: non-zero exit.

Reviewer-read, not automated (115-VALIDATION.md "Manual-Only Verifications"):

- the document's replicate / warn / neither prose reads honestly, and check 7's value is
  explained as unreachability rather than undetectability. The reviewer reads the Notes
  cells against the research's measured gate table.
</verification>

<success_criteria>
- Every row of the classification table states one of three values and agrees with what the bridge does.
- The doc's warned gate names equal the source's closed union, enforced by a gate.
- The retracted sentence is gone and every site that leaned on it moved with it.
- All four negative-control transcripts are in the SUMMARY.
- `npm run check` and `pre-commit run --all-files` are green.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-05-SUMMARY.md` when done.
</output>
