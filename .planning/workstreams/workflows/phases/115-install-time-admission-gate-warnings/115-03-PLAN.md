---
phase: 115-install-time-admission-gate-warnings
plan: 03
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/install.test.ts
autonomous: true
requirements: [WGATE-01, WGATE-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A standalone `/claude:plugin reinstall` of a plugin whose workflow script trips a gate renders the gate line through `surfaceDiscoveryWarnings`, exactly as a standalone install and a standalone update already do (WGATE-01, D-115-05)."
    - "The workflow discovery warnings that `install` and `reinstall` dropped standalone before this phase — the per-file skip, refusal and unrunnable lines — are now rendered on both verbs, and each newly-visible family is pinned by a test rather than left to appear untested (D-115-05)."
    - "The first `ctx.ui.notify` call's message argument and severity argument are BYTE-IDENTICAL between an install of a plugin whose workflow script trips a gate and an install of the same plugin whose script does not (D-115-06, WGATE-03)."
    - "Installing the same gate-warned plugin twice, each into its own hermetic home, produces the same number of gate lines both times; the warning does not accumulate."
    - "`splitStagingWarnings` still has exactly four members: the reclassification happens at the call site, the way `update.ts` already does it."
  prohibitions: []
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - tests/orchestrators/plugin/reinstall.test.ts
    - tests/orchestrators/plugin/install.test.ts
  key_links:
    - "`reinstall.ts` staged handles -> `discoveryWarnings` -> the `render: \"none\"` outcome field -> `surfaceDiscoveryWarnings` -> `notifyDiagnostic`"
    - "`composeInstalledRow` (first notify) is composed from state the gate never touches; `notifyDiagnostic` (second notify) is where the gate line lands"
---

<objective>
Finish D-115-05: make `reinstall` surface the workflow discovery channel the way
`install` now does and `update` already did, and pin criterion 2 as a byte comparison
rather than as an inference from the channel being separate.

Purpose: a gate warning added to a channel only one of four verbs reads would satisfy
WGATE-01's letter and fail on the verb the phase is named after. And "no plugin-level
status, glyph or disposition changes" is only checkable against recorded bytes.

Output: the reinstall call-site reclassification, the newly-visible warning families
pinned on both verbs, and the criterion-2 byte assertion.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-RESEARCH.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-01-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
No new symbol and no new file. This plan changes one call site and adds test cases.

| Change | Site |
|---|---|
| `handles.workflows.result.warnings` moves from the `bridgeWarnings` array to the `discoveryWarnings` field | `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:1054-1069` |

`splitStagingWarnings` is NOT touched. Both `update.ts` and `reinstall.ts` state in
comments that the shared four-member classifier stays at four because a fifth member
would drag every other consumer of it into this change.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reinstall surfaces the workflow discovery channel standalone</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:1040-1075` — `splitHandleWarnings`, the `bridgeWarnings` array, the comment explaining why the workflow prepare's warnings ride the bridge half, and the returned `discoveryWarnings: staging.discovery`.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts:28-40` (the module header's note on the `render: "none"` arm and `discoveryWarnings`), `:300-315` (the outcome shape declaring both arrays), `:375-400` (the `notes` fold and the `discoveryWarnings.length > 0` spread), `:595-618` (the `surfaceDiscoveryWarnings` call and its `partition === "reinstalled"` guard).
    - `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts:1415-1433` — `collectUpdateWarnings`, the WLIF-02 comment giving the argument for the discovery half, and the explicit note that the shared classifier stays at four members.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts:1438-1456` — `surfaceDiscoveryWarnings`, its two header forms, and its `redactAbsolutePaths` pass.
    - `tests/orchestrators/plugin/reinstall.test.ts` — the existing standalone-reinstall cases, how they build a ctx that records `ctx.ui.notify` calls, and any existing case asserting on the second notification.
    - `115-RESEARCH.md` "The warning channel, end to end" (the four-verb table) and Common Pitfalls, "the install warning lands where nobody reads it".
  </read_first>
  <behavior>
    - A standalone reinstall of a plugin whose single workflow script trips a gate makes a second `ctx.ui.notify` call whose message contains the script's file name and the engine check.
    - A standalone reinstall of a plugin carrying a helper module with no `meta` renders that per-file skip line — a family the verb dropped before this change.
    - A standalone reinstall of a plugin carrying a determinism-matching script renders that refusal line.
    - A standalone reinstall of a plugin carrying a `stem-fallback` script renders the unrunnable caveat line.
    - The reinstall row itself (the first notification) carries no gate text and no new reasons-brace token.
    - A standalone reinstall of a plugin whose workflow scripts are all well-formed makes exactly one `ctx.ui.notify` call.
  </behavior>
  <action>
Move `handles.workflows.result.warnings` out of the `bridgeWarnings` array and into the
returned `discoveryWarnings` field, alongside `staging.discovery` (D-115-05). Do the
move AT THE CALL SITE, not by widening `splitStagingWarnings` — the shared classifier
stays at four members, for the reason both this file and `update.ts` already record.

Rewrite the comment at the site to state the current fact and its argument: these
strings describe the plugin's declared scripts, which a standalone user needs, and not
hygiene only the cascade forwards. That is the argument `update.ts` records at its own
site and the argument the phase goal restates. Cite D-141-03 and WGATE-01. Do not
narrate the classification being replaced.

Confirm at the consumer that the move actually reaches a standalone user before writing
the tests: the outcome's `discoveryWarnings` field is spread only when non-empty, and
`surfaceDiscoveryWarnings` is called only for the `reinstalled` partition. If either
guard would swallow the new strings, that is a finding to report rather than a licence
to add a second channel.

Then pin every family this change makes newly visible, not only the gate one. Before
this change the verb dropped the whole array standalone, so the per-file skip, the
per-file refusal and the unrunnable caveat all arrive on this verb for the first time.
D-115-05 is explicit that the newly-visible output is pinned rather than left to appear
untested. One case per family, each asserting the second notification's message
contains that file's name.

Also add the negative case: a plugin whose workflow scripts are all well-formed makes
exactly ONE notify call. Without it, "the second call contains X" is satisfiable by a
second call that always exists.

While reading `surfaceDiscoveryWarnings`, note that its header states the affected
components were skipped, which is not true of an installed-with-caveat line. That
mismatch is PRE-EXISTING — `update` has rendered stem-fallback lines under that header
since the workflows bridge landed — and no requirement or decision in this phase covers
the header's wording, so do NOT change it here. Record it instead with
`gsd-tools windows append`, prefixing the description with `[workflows-replay]` per the
project's ledger convention, and name it in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` no longer includes `handles.workflows.result.warnings` in the `bridgeWarnings` array, and includes it in the returned `discoveryWarnings`.
    - `grep -c 'splitStagingWarnings' extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` prints the same value as before the change.
    - Four cases exist, one per newly-visible family (gate, skip, refusal, unrunnable), each asserting the second notification names the affected file.
    - A case asserts a standalone reinstall of an all-well-formed plugin makes exactly one notify call.
    - A case asserts the reinstall row (the first notification) contains no gate reason text.
    - A Broken Windows entry exists for the `surfaceDiscoveryWarnings` header mismatch, with a `[workflows-replay]` description prefix.
    - `node --test tests/orchestrators/plugin/reinstall.test.ts` reports `# fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node --test tests/orchestrators/plugin/update.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` — update reads the same channel and must be unaffected</fails_when>
  </verify>
  <done>All four verbs now agree: `install`, `update` and `reinstall` render the workflow discovery channel standalone, `info` renders it in preview tense, and every family the reclassification made newly visible on `reinstall` is pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criterion 2 as a byte assertion, plus the install-side newly-visible families and idempotency</name>
  <files>tests/orchestrators/plugin/install.test.ts</files>
  <read_first>
    - `tests/orchestrators/plugin/install.test.ts:275-311` — `NotifyRecord` and `makeCtx`, which records `{message, severity?}` per `ctx.ui.notify` call and omits `severity` entirely for an info row.
    - `tests/orchestrators/plugin/install.test.ts:9670-9800` — `WorkflowInstallRun`, `installWorkflowBearingPlugin`, `workflowEnvelopeBytes` and the two `WDEP-02` / `WDEP-03` cases. This is the two-run comparison shape, including its non-vacuity anchor: each run's own bytes are pinned BEFORE the two runs are compared, because two runs agreeing proves nothing when neither wrote anything.
    - `tests/orchestrators/plugin/install.test.ts:565-600` — `seedPathMarketplaceWithPlugin` and its `workflows` option.
    - The end-to-end case plan 115-01 added to this file.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts:2543-2560` — the row's own `notifyWithContext` call and the `surfaceDiscoveryWarnings` call that follows it.
    - `115-RESEARCH.md` "What criterion 2 forbids, mechanically" and "Criterion 2 as a byte assertion, not a hope".
    - `115-VALIDATION.md`, the WGATE-03 row of the requirements map.
  </read_first>
  <behavior>
    - Two hermetic installs of the SAME plugin fixture, differing ONLY in whether its single workflow script declares a `meta.description`, produce first notifications whose `message` and `severity` are byte-identical.
    - Non-vacuity, both directions: the warned run's SECOND notification contains the gate line, and the unwarned run makes only ONE notify call.
    - Non-vacuity, envelope side: each run's envelope bytes are pinned literally before the two rows are compared.
    - A standalone install of a plugin carrying a helper module with no `meta` renders that skip line; the same for a determinism-matching script and for a `stem-fallback` script — the families the standalone install dropped before this phase.
    - Installing the same gate-warned plugin twice, each into its own hermetic home, yields the same second-notification message both times, and the same number of notify calls.
  </behavior>
  <action>
Extend the two-run comparison fixture already in this file so the two runs differ in the
SCRIPT rather than in the session's tool list, and add the criterion-2 assertion.

The assertion is on BYTES (D-115-06). Compare the first `ctx.ui.notify` call's message
argument and its severity argument between the two runs with strict equality. Do not
compare a parsed or normalized form, and do not assert "the row does not contain the
gate text" as a substitute: an absence check greens over a row that changed in some
other way, and criterion 2 forbids ANY plugin-level status, glyph or disposition change,
not only the appearance of gate text. The install channel is a second `ctx.ui.notify`
call carrying no catalog state, so `catalog-uat` cannot redden from it — which is
exactly why this comparison has to be written by hand rather than assumed.

Pin non-vacuity in both directions before comparing, the way the existing envelope-byte
case does. The warned run MUST have produced a gate line and the unwarned run MUST NOT
have produced a second notification at all; otherwise two identical rows would be
satisfied by neither run warning.

Both runs take their own hermetic home and their own cwd. Two runs sharing one home
would let the first run's envelope satisfy the second run's read.

Then add the install-side newly-visible families. The reclassification plan 115-01 made
to `install.ts` surfaces the per-file skip, the per-file refusal and the unrunnable
caveat on a standalone install for the first time (D-115-05). One case per family,
asserting the second notification names the affected file.

Then add the idempotency case. Install the same gate-warned fixture twice, each into its
own hermetic home, and assert both runs produce the same second-notification message and
the same notify-call count. The warning must not accumulate: `discoverPluginWorkflows`
builds a fresh array per call and `InstallCtx` is constructed per install, so the
expected answer is stability, and asserting it is what makes a future shared accumulator
fail here.

Cite WGATE-01, WGATE-03, D-115-05 and D-115-06 in the case titles. Never a phase, plan,
wave or task number.
  </action>
  <acceptance_criteria>
    - A case named for WGATE-03 and D-115-06 asserts `assert.equal` between the two runs' first-notification `message` values and between their `severity` values.
    - The same case asserts the warned run's second notification contains the gate line and that the unwarned run made exactly one notify call.
    - The same case pins at least one run's envelope bytes literally before the row comparison.
    - Three cases exist for the install-side newly-visible families (skip, refusal, unrunnable), each asserting the second notification names the affected file.
    - A case installs the same gate-warned fixture twice into separate hermetic homes and asserts equal second-notification messages and equal notify-call counts.
    - `node --test tests/orchestrators/plugin/install.test.ts` reports `# fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` — the install-side gate line must not move any catalogued row's bytes</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output names any rule under `tests/orchestrators/plugin/install.test.ts`</fails_when>
  </verify>
  <done>Criterion 2 is a recorded byte comparison with non-vacuity anchors in both directions, every warning family the install reclassification made newly visible is pinned, and re-installing a warned plugin does not accumulate warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge warning array -> orchestrator classification | a policy decision about which warnings a standalone user sees; a misclassification silently hides or over-exposes third-party text |
| orchestrator -> `ctx.ui.notify` | rendered terminal output; `redactAbsolutePaths` runs at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-13 | Information disclosure | `surfaceDiscoveryWarnings` on `install` and `reinstall` | high | mitigate | The reclassification routes MORE third-party-derived text to a rendered surface. `surfaceDiscoveryWarnings` applies `redactAbsolutePaths` to every line before `notifyDiagnostic` (NFR-9), and this plan adds no new render site that could bypass it. The reinstall cases assert on the rendered message, so a line reaching the user unredacted would show as a home path in a test fixture. |
| T-115-14 | Denial of service (against the plugin author) | the plugin row | high | mitigate | The criterion-2 byte assertion is the mitigation: the row's message and severity are proven byte-identical between a warned and an unwarned install, so a gate reading cannot change a status, a glyph or a disposition. |
| T-115-15 | Spoofing | third-party file names inside a rendered warning | medium | accept | The outer `softFailWarning` interpolation of the discovered file name is PRE-EXISTING and unchanged by this phase; every gate REASON is a literal with no interpolation. Widening the render to two more verbs does not change the escaping posture. Recorded rather than silently absorbed. |
| T-115-16 | Repudiation | `surfaceDiscoveryWarnings`'s header claiming the components were skipped | medium | accept | Pre-existing on `update` since the bridge landed; no requirement or decision in this phase covers the header wording, and changing it moves user-visible bytes on three verbs with nothing asking for it. Recorded in the Broken Windows ledger with a `[workflows-replay]` prefix rather than fixed here. |
| T-115-17 | Tampering | dependency manifests | high | mitigate | No package is installed. The manifest-diff guard in `<verification>` proves it. |
</threat_model>

<verification>
```
npm test && npm run typecheck && npm run fallow
```

Fails when: non-zero exit from any of the three, or the unit TAP summary reports a
non-zero `# fail` count.

```
bash -c 'test -z "$(git diff HEAD --name-only -- package.json package-lock.json)"'
```

Fails when: non-zero exit.

Reviewer-read, not automated: that the reclassification comment at the reinstall call
site states the current argument rather than annotating the previous one.
</verification>

<success_criteria>
- `install`, `update` and `reinstall` all render the workflow discovery channel to a standalone user.
- Every warning family the two reclassifications made newly visible is pinned on the verb that gained it.
- Criterion 2 holds as a byte comparison with non-vacuity anchors in both directions.
- Re-installing a warned plugin produces the same warning, once.
- `npm test`, `npm run typecheck` and `npm run fallow` are green.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-03-SUMMARY.md` when done.
</output>
