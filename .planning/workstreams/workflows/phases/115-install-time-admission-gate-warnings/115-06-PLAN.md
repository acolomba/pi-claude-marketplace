---
phase: 115-install-time-admission-gate-warnings
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/BACKLOG.md
autonomous: true
requirements: [WDOCS-01]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.planning/BACKLOG.md` contains no `## WFLW-01` heading and exactly one pruned-footer block naming `WFLW-01`, so no partially-applied state — both present, or neither — can pass (WDOCS-01)."
    - "The pruned footer follows the file's ONE existing pruned-footer instance: an HTML comment block, a `Pruned <ISO date>:` first line naming the milestone, then one bullet per pruned entry in the `\"subject\" -> closed by <requirement IDs> (<seam or test>)` shape."
    - "The footer sits in the body of the file, where the WFLW-01 entry stood, not appended to the end — the existing instance sits immediately after the entry that superseded it, and the file is what the convention is read from, not the requirement's wording."
    - "The footer names `workflows-replay` as the milestone that re-landed the bridge (D-115-08), and the requirement IDs it cites are read out of the workstream's own requirement files rather than reconstructed from memory."
    - "Every surviving `WFLW-01` mention elsewhere in `.planning/` still resolves after the prune: none of them depends on the entry existing as an open `##` heading."
  prohibitions: []
  artifacts:
    - .planning/BACKLOG.md
  key_links:
    - "`.planning/BACKLOG.md` pruned-footer instance (the file's only one) -> the shape of the new block"
    - "`.planning/workstreams/workflows/REQUIREMENTS.md` + `milestones/workflows-REQUIREMENTS.md` -> the requirement IDs the footer cites"
---

<objective>
Prune the stale `WFLW-01` entry from the backlog under the file's own existing
convention, so the backlog stops advertising shipped work as open.

Purpose: `WFLW-01` asks for a `workflows` bridge. The bridge exists — its modules,
its tests and its published contract are all in this repository's history. An entry
that still reads as open is a false statement about what is left to do.

Output: one pruned-footer block where the entry stood, and a cross-reference audit
proving nothing was orphaned.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows/ROADMAP.md
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-RESEARCH.md
</context>

<artifacts_produced>
| Artifact | Kind | Purpose |
|---|---|---|
| a pruned-footer HTML comment block in `.planning/BACKLOG.md`, where the `## WFLW-01` entry stood | planning-artifact record | records what closed the entry and where a reader can find it |

No new file. No code change. This plan touches exactly one file.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Replace the WFLW-01 entry with a pruned footer in the file's own convention</name>
  <files>.planning/BACKLOG.md</files>
  <read_first>
    - `.planning/BACKLOG.md:2544-2552` — the file's ONE existing pruned-footer block, verbatim. This is the convention: an HTML comment with the opening and closing markers each alone on a line; a first line reading `Pruned <ISO date>: <one-sentence reason naming the milestone>.`; then one bullet per pruned entry shaped `- "<the entry's subject>" -> closed by <requirement IDs> (<parenthetical naming the code seam or the test that proves it>).`
    - `.planning/BACKLOG.md:2535-2556` — where that block SITS: in the body of the file, immediately after the entry that superseded it, and before the next `##` heading. It is not a trailing footer at the end of the file. Follow the file, not the requirement's wording.
    - `.planning/BACKLOG.md:891-936` — the whole `WFLW-01` entry to be replaced, from its `##` heading through its closing `Code seams:` paragraph, and the `##` heading that follows it.
    - `.planning/workstreams/workflows/REQUIREMENTS.md` — the "Milestone: workflows-replay" section, which names the archived requirement families the replay re-landed and points at `milestones/workflows-REQUIREMENTS.md`.
    - `.planning/workstreams/workflows/milestones/workflows-REQUIREMENTS.md` — the actual requirement IDs. Read them; do not reconstruct the list from memory or from this plan.
    - `.planning/MILESTONES.md`, the `workflows-detection` entry — the mechanical half of `WFLW-01`, shipped 2026-08-29 as PR #154.
    - `.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-CONTEXT.md`, D-115-08.
    - `115-RESEARCH.md` "The BACKLOG pruned-footer convention" — the convention read off the one instance, and the three-way milestone-name disagreement D-115-08 settles.
    - `CLAUDE.md`: markdown is formatted by `mdformat` and `markdownlint-cli2` in pre-commit, never by prettier.
  </read_first>
  <action>
Delete the `WFLW-01` entry and put a pruned-footer block where it stood.

Match the existing instance's shape exactly — the marker lines, the `Pruned <ISO date>:`
opening, and the bullet shape. Do not invent a new footer format and do not delete the
entry outright with no record: the entry's own history is why the footer exists.

The first line names the milestone. `WFLW-01` has two halves and they closed under two
milestones, so name both truthfully in one sentence: the mechanical fix under
`workflows-detection` (PR #154, 2026-08-29) and the bridge itself under
`workflows-replay` (D-115-08 — the milestone that re-landed the bridge on this branch,
which is the work a reader can find in this repository's own history).

The bullet cites the requirement IDs that actually closed it, read out of the workstream
requirement files listed in `read_first`. The parenthetical names what a reader can open
right now to see it done: the bridge modules under `bridges/workflows/`, the admission
module `domain/workflow-script.ts`, and the published contract
`docs/workflows-compatibility.md`.

Place the block where the entry stood, between the entry that precedes it and the `##`
heading that follows, so the file's ordering is preserved and the second instance of the
convention reads like the first.

Format with `pre-commit run --files .planning/BACKLOG.md`, never with prettier. Note that
`.planning/` is outside the ESLint tree, so lint has nothing to say here; the markdown
hooks are the gate.

Change nothing else in this file. `.planning/BACKLOG.md` is appended to rather than
reconciled, and a sweep of neighbouring entries is not what this requirement asks for.
  </action>
  <acceptance_criteria>
    - `grep -c '^## WFLW-01' .planning/BACKLOG.md` prints `0`.
    - `grep -c 'WFLW-01' .planning/BACKLOG.md` prints `1` — the footer mentions it exactly once.
    - `grep -c '^<!--' .planning/BACKLOG.md` prints `2` and `grep -c '^-->' .planning/BACKLOG.md` prints `2` — two pruned-footer blocks, the existing one and the new one.
    - The new block's first line begins `Pruned ` followed by an ISO date and names both `workflows-detection` and `workflows-replay`.
    - The new block sits between the entry that preceded `WFLW-01` and the `##` heading that followed it, not at the end of the file.
    - The requirement IDs the bullet cites appear in `.planning/workstreams/workflows/milestones/workflows-REQUIREMENTS.md`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test "$(grep -c "^## WFLW-01" .planning/BACKLOG.md)" = "0" && test "$(grep -c "WFLW-01" .planning/BACKLOG.md)" = "1"'</automated>
    <fails_when>non-zero exit, meaning the open heading survived or the footer mentions the ID a number of times other than once</fails_when>
    <automated>bash -c 'test "$(grep -c "^<!--" .planning/BACKLOG.md)" = "2" && test "$(grep -c "^-->" .planning/BACKLOG.md)" = "2"'</automated>
    <fails_when>non-zero exit, meaning the comment markers are unbalanced or the new block was not added in the file's convention</fails_when>
    <automated>grep -c 'workflows-replay' .planning/BACKLOG.md</automated>
    <fails_when>the printed count is `0`, meaning D-115-08's milestone name is absent from the footer</fails_when>
    <automated>pre-commit run --files .planning/BACKLOG.md</automated>
    <fails_when>non-zero exit, or any hook reports `Failed`</fails_when>
  </verify>
  <done>The backlog no longer carries `WFLW-01` as an open entry, and the record of what closed it sits where the entry stood, in the file's own convention.</done>
</task>

<task type="auto">
  <name>Task 2: Audit every surviving WFLW-01 cross-reference</name>
  <files>.planning/BACKLOG.md</files>
  <read_first>
    - `.planning/BACKLOG.md:2494-2502` — the entry referring to the workflow milestone's never-turn-a-reading-into-a-refusal anchor. Read it before finishing: it is prose about this phase's posture rather than a reference to the `WFLW-01` heading, and the audit must confirm that rather than assume it.
    - `115-RESEARCH.md`, the paragraph beginning "Note also" under "The BACKLOG pruned-footer convention".
    - `.planning/workstreams/workflows/ROADMAP.md:123-128` and `:588-591` — the two places the roadmap names `WFLW-01`, both of which describe the prune itself and must survive.
    - `.planning/workstreams/workflows/REQUIREMENTS.md:70` — WDOCS-01's own text, which names `WFLW-01` and must survive.
  </read_first>
  <action>
Enumerate every surviving `WFLW-01` mention under `.planning/` and confirm each still
resolves after the prune. Report the enumeration in the SUMMARY as a table of file, line
and disposition.

Three classes are expected and all three are FINE:

- The roadmap and the requirement file name `WFLW-01` while describing the prune itself.
  A reference to a pruned entry inside the requirement that prunes it is self-consistent.
- The spike READMEs under `.planning/spikes/` record what `WFLW-01` said at the time they
  were written. They are dated historical artifacts and are not reconciled.
- The new pruned footer names it once, which is the record.

The class that would be a defect is a mention that depends on the entry existing as an
open `##` heading — a cross-reference telling a reader to go read it. Confirm there is
none. If one exists, report it rather than editing it: a fix outside `.planning/BACKLOG.md`
is outside this requirement's file scope and belongs to the operator.

Do not edit the spike READMEs. Do not edit the ROADMAP or REQUIREMENTS files. This task
reads and reports.
  </action>
  <acceptance_criteria>
    - The SUMMARY carries a table of every `WFLW-01` mention remaining under `.planning/`, with file, line and disposition.
    - The table shows no mention that instructs a reader to go read an open `WFLW-01` entry, or names any that does as a reported finding rather than an edit.
    - No file other than `.planning/BACKLOG.md` was modified by this plan.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -rn "WFLW-01" .planning/ --include=*.md | tee /dev/stderr | wc -l'</automated>
    <fails_when>the printed count is `0` — every mention vanishing means the prune deleted the record instead of replacing it</fails_when>
    <automated>bash -c 'test "$(git status --short -- .planning/ | grep -v "115-.*SUMMARY" | grep -cv "BACKLOG.md")" = "0"'</automated>
    <fails_when>non-zero exit, meaning a planning file other than the backlog and this plan's own summary was modified</fails_when>
  </verify>
  <done>Every surviving mention of the pruned entry is enumerated and classified, and nothing outside `.planning/BACKLOG.md` was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project record -> a reader deciding what is left to do | the backlog is the artifact a triage pass reads; an entry that misstates its own status misdirects that pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-115-27 | Repudiation | the pruned footer's milestone attribution | medium | mitigate | `WFLW-01` has two halves that closed under two different milestones, and three artifacts disagree on which to name. The footer names both truthfully in one sentence, per D-115-08, and cites requirement IDs read out of the workstream's own requirement files rather than reconstructed. |
| T-115-28 | Tampering | `.planning/BACKLOG.md` | medium | mitigate | The file is appended to rather than reconciled and carries entries this phase has no mandate over. The plan touches exactly one entry, and Task 2's `git status` guard proves no other planning file moved. |
| T-115-29 | Information disclosure | the footer's content | low | accept | The footer names public repository paths and public requirement IDs. No secret, credential or user path is involved. |
| T-115-30 | Tampering | dependency manifests | high | mitigate | This plan touches no source file and no manifest. `git status` over `.planning/` is the guard; the manifests are outside its scope entirely. |
</threat_model>

<verification>
```
pre-commit run --files .planning/BACKLOG.md
```

Fails when: non-zero exit, or any hook reports `Failed`.

```
bash -c 'test -z "$(git diff HEAD --name-only -- package.json package-lock.json extensions tests)"'
```

Fails when: non-zero exit, meaning this planning-artifact plan touched source.

Reviewer-read, not automated (115-VALIDATION.md "Manual-Only Verifications"): that the
new pruned footer follows the file's existing convention. The reviewer diffs it against
the one other pruned block in the same file.
</verification>

<success_criteria>
- `.planning/BACKLOG.md` carries no open `WFLW-01` entry and exactly one footer recording what closed it.
- The footer matches the file's one existing instance in shape and placement.
- Every surviving mention of the ID is enumerated and classified in the SUMMARY.
- No file other than `.planning/BACKLOG.md` changed.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/115-install-time-admission-gate-warnings/115-06-SUMMARY.md` when done.
</output>
