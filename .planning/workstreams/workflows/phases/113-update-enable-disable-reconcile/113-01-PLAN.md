---
phase: 113-update-enable-disable-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/workflows/types.ts
  - extensions/pi-claude-marketplace/bridges/workflows/discover.ts
  - extensions/pi-claude-marketplace/bridges/workflows/index.ts
  - extensions/pi-claude-marketplace/bridges/workflows/stage.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - docs/output-catalog.md
  - tests/bridges/workflows/discover.test.ts
  - tests/bridges/workflows/stage.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [WFLW-04]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WFLW-04: `info` renders a `workflows:` line for a workflow-bearing plugin, sourced from `componentPaths.workflows` — the resolver member Phase 109 exposed and that nothing has consumed until now."
    - "The `workflows:` line lists BOTH admitted verdict arms (`named` and `stem-fallback`) as their generated `<plugin>:<name>`. An envelope is written for both, so listing only `named` would make `info` disagree with what install puts on disk."
    - "The line renders LAST among the per-kind component lines, before `dependencies:`, following the existing alphabetical per-kind order the renderer's tuple enforces."
    - "The renderer's component-kind set carries a compile-forcing proof that FIRES on the widening direction. The exact-length tuple that claimed to guard it does not: five elements each drawn from a six-member union are assignable, and the construct was compiled under the project's strict flags and exited 0. The false claim is corrected in the same commit as the guard."
    - "The proof is demonstrated non-vacuous: reverting the new tuple entry while leaving the interface key produces a typecheck error naming the uncovered kind, and that observation is recorded."
    - "WR-09: the discovery outcome phrase is a PARAMETER of the discovery call, not a module constant. `discoverPluginWorkflows` takes a REQUIRED `tense` discriminant, so the staging surface and the read-only `info` surface each state their own tense and no caller can inherit the wrong one by omission."
    - "The five install-tense phrases stay byte-identical apart from the deliberate `lstat`-site split. `was not installed`, `was refused` and `was installed but will not run` are pinned by existing fixtures and are not retuned."
    - "The read-failure phrase is split BY CALL SITE in both tenses: the inspection arm says the file could not be inspected, the body-read arm says it could not be read. The old shared phrase claimed a read at a site where nothing was read."
    - "One shared warning template composes every phrase. No second template, no `outcomes` record parameter, and no separate preview wrapper function."
    - "The two phrase tables are total records keyed by the site union, so a sixth site cannot be added without an entry in BOTH tenses."
    - "`composeResolvedComponents` takes `componentPaths.workflows` as a REQUIRED member, so both lenient component-path producers — the derived map and the inline literal on the warm git arm — fail to compile until they supply it. That is the compile-forcing behaviour the tuple length does not give."
    - "The state-only `info` arm renders its `workflows:` line from the persisted record inventory, sorted by the same comparator the other four name-list kinds use. It performs no discovery and reads no plugin source."
    - "`list` gains no per-kind workflow count. No existing kind has one; the `list` half of the criterion is a byte-equality regression fixture pinning a workflow-bearing plugin's row, not new rendering."
    - "A declared `workflows` path that escapes the plugin root still refuses loudly. On the lenient `info` arm the refusal folds through the existing catch to the not-resolved marker with a probe reason, rather than crashing the command or silently listing nothing."
    - "`info` RENDERS the preview-tense warnings. Building the tense parameter and rendering nothing would ship five strings with no reader, which is the same defect this phase exists to clean up one criterion over."
    - "NFR-9: a discovery warning embeds the absolute component directory it walked, so it is reduced at the composition site before it reaches a row — the same handling the existing discovery-warning surface already applies. That is also what makes the rendered bytes machine-independent and therefore pinnable by a catalog fixture."
    - "Every new rendered byte is pinned by a paired `(section, state)` catalog fixture. Fixtures are pure notification data and are never synthesized from domain helpers."
  prohibitions:
    - "No second warning template, no `outcomes` record parameter on the discovery call, and no separate preview discovery wrapper."
    - "No optional `tense` with an install default. The parameter is required."
    - "No retuning of the three install-tense phrases outside the deliberate inspection-site split."
    - "No defensive sorting inside the renderer. The composer pre-sorts; the renderer assumes sorted input."
    - "No per-kind component count on the `list` row."
    - "No directory walk to enumerate a plugin's workflows. The names live inside the scripts and the discovery pass owns reading them."
    - "No second composer for the generated workflow name."
    - "No rendered bytes composed outside `shared/notify.ts`."
    - "No comment narrating what the corrected component-kind comment used to claim."
    - "No GSD phase, plan, wave or milestone number in any comment or commit subject."
    - "No `--no-verify`, and no `--amend` after a failed hook run."
    - "No `git add -A` and no `git add -u .`."
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/workflows/discover.ts"
      provides: "the required `tense` discriminant and the two total phrase tables"
      contains: "tense"
    - path: "extensions/pi-claude-marketplace/bridges/workflows/types.ts"
      provides: "the exported outcome-tense union"
      contains: "WorkflowOutcomeTense"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "the sixth component-kind member, the widened tuple and the compile-forcing coverage proof"
      contains: "_ComponentKindsCoverageProof"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "the `workflows:` composition on the resolved, lenient and state-only arms"
      contains: "discoverPluginWorkflows"
    - path: "docs/output-catalog.md"
      provides: "the resolved-arm, state-only-arm, preview-warning and list-regression catalog states"
      contains: "workflows:"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "the paired fixtures for those states"
      contains: "workflows"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      to: "extensions/pi-claude-marketplace/bridges/workflows/discover.ts"
      via: "the resolved arm calls the discovery pass with the preview tense to obtain admitted generated names and preview-tense warnings"
      pattern: "discoverPluginWorkflows"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "the composed components map is the renderer's input; the widened kind tuple is what emits the line"
      pattern: "componentsResolved"
    - from: "docs/output-catalog.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "byte-equality between each new catalog state and the notification the paired fixture renders"
      pattern: "catalog-state"
---

<objective>
Make the `info` surface show a plugin's workflows, and make the discovery pass state the right tense
while doing it.

Purpose: `componentPaths.workflows` has existed since the kind inversion and nothing has ever read it.
The renderer's component-kind set is guarded by a comment that was measured false, so widening it
silently omits the new kind from every row — the recorded repeat failure mode of this codebase. And the
four discovery outcome phrases all assert an install outcome, which on a read-only surface for a plugin
that is not installed is a false statement about the user's disk.

Output: a required tense discriminant with two total phrase tables, a real compile-forcing proof for the
component-kind set with a demonstrated firing, the `workflows:` line on all three `info` component arms,
and four paired catalog fixtures.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/113-update-enable-disable-reconcile/113-CONTEXT.md
@.planning/workstreams/workflows/phases/113-update-enable-disable-reconcile/113-RESEARCH.md
@.planning/workstreams/workflows/phases/113-update-enable-disable-reconcile/113-PATTERNS.md
@.planning/workstreams/workflows/phases/113-update-enable-disable-reconcile/113-VALIDATION.md
@.planning/workstreams/workflows/phases/111-workflows-bridge/111-02-SUMMARY.md
@CLAUDE.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The checkout `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`, sequential
execution, no agent worktree.

Run `pre-commit run --files <changed paths>` before every commit and fix until clean; a failed hook means
the commit did NOT happen, so re-stage and re-run rather than amending. Never `--no-verify`. The prettier
hook rewrites files mid-run and the commit still succeeds — check `git status` after every commit and land
any rewrite as a follow-up commit. Markdown is formatted by the markdown hook, never by the code
formatter, so never run the code formatter over `docs/output-catalog.md`. Stage explicit paths; never
`-A`, never `-u .`. Conventional Commits subjects, 5 to 72 characters, with no milestone or phase number.
</working_directory>

<assumption_delta_decision>
**Primary noun:** the discovery outcome phrase.

**Transition:** a module CONSTANT becomes a PARAMETER of the discovery call — the `chosen` family
("has a configuration decision become a modeling decision?").

**Decision: `promote`.** `tense` is a REQUIRED parameter of `discoverPluginWorkflows`, not an optional one
defaulting to the install tense.

**Rationale:** an optional parameter with an install default is `add-alongside` — every existing call site
keeps the old behaviour implicitly, and any future caller that forgets the parameter silently gets the
wrong tense on a read-only surface, which is precisely the defect this criterion exists to remove.
Requiring it makes the compiler enumerate all call sites: one production site in the staging bridge and
23 in the discovery owner test, measured this session.

**Not re-opened:** the sweep gaining a retained-tree channel was already decided as `add-alongside` (a
separate read-only scan, sweep signature untouched). That decision is recorded in `113-CONTEXT.md` and is
implemented in plan 05.
</assumption_delta_decision>

<flagged_planner_assumptions>
Two edge-coverage probe rows over this phase's requirement IDs came back `unclassified` and are surfaced
here rather than auto-resolved with a backstop:

1. **`WFLW-04` — unclassified.** The requirement states only that the resolver EXPOSES
   `componentPaths.workflows`. Research measured that as already satisfied at `domain/resolver.ts:417`.
   The planner's assumption is that this phase's obligation is to CONSUME it on the `info` surface, and
   that consumption is what the truths above verify. If the intended obligation was something else, the
   requirement is already complete and this plan over-delivers rather than under-delivers.
2. **`WLIF-04` — unclassified.** Research found reinstall already imports the workflows bridge and
   composes workflow names into its outcome. The planner's assumption is that the obligation is
   verify-then-correct, not new code. That is planned as an explicit task in plan 04, which writes the
   case first and corrects the traceability row in the same commit as the evidence — so the assumption is
   tested rather than trusted.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
Phase-wide table. This is the authoritative list of every symbol the phase creates; the other four plans
reference it. A newly-created symbol is excluded from downstream drift verification only if it appears
here.

| Change | Symbol / member | File | Plan |
|--------|-----------------|------|------|
| ADD | `WorkflowOutcomeTense` (exported union `"install" \| "preview"`) | `bridges/workflows/types.ts` | 01 |
| ADD | `WorkflowOutcomeSite` (exported union: the five phrase sites) | `bridges/workflows/types.ts` | 01 |
| ADD | required `tense` member on the `discoverPluginWorkflows` input | `bridges/workflows/discover.ts` | 01 |
| ADD | the two total phrase tables keyed by site | `bridges/workflows/discover.ts` | 01 |
| ADD | `workflows?: readonly string[]` on `PluginInfoComponentsResolved["components"]` | `shared/notify.ts` | 01 |
| ADD | `"workflows"` as the sixth `COMPONENT_KINDS` member | `shared/notify.ts` | 01 |
| ADD | `_ComponentKindsCoverageProof` (exported compile-forcing proof) | `shared/notify.ts` | 01 |
| ADD | `notes?: readonly string[]` on `PluginInfoRow` (the preview-warning channel) | `shared/notify.ts` | 01 |
| ADD | required `componentPaths.workflows` on `composeResolvedComponents`'s parameter | `orchestrators/plugin/info.ts` | 01 |
| ADD | `pluginName` third positional on `composeResolvedComponents` and on `buildNotInstallablePathRowFields` / `buildNonInstallableRowFields` | `orchestrators/plugin/info.ts` | 01 |
| ADD | `workflows` key on `deriveLenientComponentPaths` and on the warm-git inline literal | `orchestrators/plugin/info.ts` | 01 |
| ADD | four catalog states: resolved-arm line, state-only-arm line, preview warnings, list regression | `docs/output-catalog.md` | 01 |
| ADD | `workflows` member of `PrepHandles` | `orchestrators/plugin/update.ts` | 02 |
| ADD | the workflows prepare / abort / phase-3a commit arms | `orchestrators/plugin/update.ts` | 02 |
| ADD | `placedWorkflowNames` on the phase-3a commit's return | `orchestrators/plugin/update.ts` | 02 |
| ADD | `handles` parameter on the intent-mark window | `orchestrators/plugin/update.ts` | 02 |
| ADD | `placedWorkflowNames` parameter on the per-bridge record write | `orchestrators/plugin/update.ts` | 02 |
| ADD | `stagedWorkflowNames: readonly string[]` on `InstallLedgerSummary` and its producer | `orchestrators/plugin/install.ts` | 03 |
| ADD | `"stale workflow command"` as the 44th `REASONS` member and `Reason` union term | `shared/notify.ts` | 04 |
| ADD | that literal to `CommandPrivateReason` | `shared/notify-reasons.ts` | 04 |
| ADD | `staleWorkflowCommand?: boolean` on the update updated-outcome | `orchestrators/types.ts` | 04 |
| ADD | `staleWorkflowCommand?: boolean` on the module-private enable/disable sentinel arms | `orchestrators/plugin/enable-disable.ts` | 04 |
| ADD | the stale-command stamp on the four retiring verbs | `uninstall.ts`, `enable-disable.ts`, `reinstall.ts`, `update.ts`, `update-row.ts` | 04 |
| ADD | four catalog states, one per stamping verb | `docs/output-catalog.md` | 04 |
| ADD | `scanRetainedWorkflowsStaging` (exported) | `orchestrators/plugin/workflows-staging-gc.ts` | 05 |
| ADD | `readDisplacedEnvelopes` (module-private shared reader) | `orchestrators/plugin/workflows-staging-gc.ts` | 05 |
| ADD | `advisories?: readonly string[]` on `CascadeNotificationMessage` and on `ReconcilePendingEmptyMessage` | `shared/notify.ts` | 05 |
| ADD | optional trailing `advisories` argument on `notifyWithContext` | `shared/notify-context.ts` | 05 |
| ADD | two catalog states, one per `pending` arm | `docs/output-catalog.md` | 05 |
| EDIT | the corrected component-kind guard comment | `shared/notify.ts` | 01 |
| EDIT | the reason-count sentence in the module header | `shared/notify-reasons.ts` | 04 |
| EDIT | the reason-count length pin and its test title | `tests/architecture/notify-closed-set-locks.test.ts` | 04 |
| EDIT | the reason enumeration list | `tests/architecture/compat-01-no-expansion.test.ts` | 04 |
| EDIT | the `WLIF-04..06, WFLW-04` traceability row | `.planning/workstreams/workflows/REQUIREMENTS.md` | 04 |

No new production FILE is created anywhere in the phase, so the corresponding-test pairing gate stays
satisfied without new test files.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "info shows a plugin's workflows" — one path only</name>
  <files>extensions/pi-claude-marketplace/bridges/workflows/types.ts, extensions/pi-claude-marketplace/bridges/workflows/discover.ts, extensions/pi-claude-marketplace/bridges/workflows/index.ts, extensions/pi-claude-marketplace/bridges/workflows/stage.ts, extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, docs/output-catalog.md, tests/bridges/workflows/discover.test.ts, tests/orchestrators/plugin/info.test.ts, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/bridges/workflows/discover.ts` in full (344 lines) — the module header's symlink and containment discipline, `softFailWarning` and its three call-site helpers around lines 96 to 185, `verdictWarning` at 203, `discoverPluginWorkflows` at 251 and `scanWorkflowsDirectory` at 290 with its two read-failure pushes
    - `extensions/pi-claude-marketplace/bridges/workflows/types.ts` — `WorkflowDiscoveryTarget` and `DiscoverPluginWorkflowsResult`
    - `extensions/pi-claude-marketplace/bridges/workflows/stage.ts` lines 107 to 200 — the single production discovery call and the admitted-arm filter that yields `generatedName`
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 1495 to 1525 (`PluginInfoRow` tail and `PluginInfoComponentsResolved` with its PRECONDITION paragraph), lines 3513 to 3562 (the component-kind type, the tuple and `appendResolvedComponentLines`), and lines 3640 to 3682 (`renderPluginInfo`)
    - `extensions/pi-claude-marketplace/shared/notify-reasons.ts` lines 255 to 270 — the `_AssertNever` coverage-proof idiom to reuse verbatim in shape, including the `export` and the inline fallow marker on the same line
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` lines 640 to 705 (`composeResolvedComponents`), 1240 to 1300 (`buildNotInstallablePathRowFields`), 1300 to 1380 (`deriveLenientComponentPaths` and `buildNonInstallableRowFields`), 1540 to 1560, 1636 to 1680, 1890 to 1915, 2075 to 2100 — every call site the required-parameter change forces
    - `113-RESEARCH.md` §"Common Pitfalls" Pitfalls 1, 6 and 7 in full, and §"Code Examples" the tense-threading table
    - `113-PATTERNS.md` §"`shared/notify.ts` — `COMPONENT_KINDS` and the `workflows:` info line" and §"`bridges/workflows/discover.ts` — the `tense` discriminant"
    - `113-CONTEXT.md` §"Criterion 5" in full — the phrase pairing table is shipped text
    - `docs/output-catalog.md` lines 1737 to 1790 — the `info` section's per-state convention, and lines 2062 to 2066 for the marker-plus-fence shape
    - `tests/architecture/catalog-uat.test.ts` lines 1 to 120 and the `/claude:plugin info` fixture block — the `(section, state)` key shape and the SCOPE GATE that fixtures are pure notification data
  </read_first>
  <behavior>
    - Discovery with the install tense produces the phrase `was not installed` for a skipped verdict, `was refused` for a refused verdict, and `was installed but will not run` for a stem-fallback verdict — byte-identical to today.
    - Discovery with the install tense produces `could not be inspected and was skipped` when the per-entry inspection fails, and `could not be read and was skipped` when the body read fails. Two distinct phrases for two distinct sites.
    - Discovery with the preview tense produces `will not be installed`, `will be refused`, `would be installed but will not run`, `could not be inspected` and `could not be read` at the same five sites.
    - Every phrase is composed by the one shared template, so each warning still names the file and the containing directory before its outcome and reason.
    - Omitting the tense at any call site is a compile error, not a default.
    - `info` for an installed path-source plugin whose manifest declares a workflows directory renders a `workflows:` line listing every admitted script's generated name, sorted, after `skills:` and before `dependencies:`.
    - A plugin whose workflows directory holds only skipped or refused scripts renders no `workflows:` line at all.
    - Widening the renderer's component-kind interface without extending its tuple is a typecheck error naming the uncovered kind.
  </behavior>
  <action>
This is the phase's tracer: one user-visible path wired through the bridge, the orchestrator, the renderer
and the published byte contract, proved end to end before anything expands out from it. Write the tests
first at each layer.

**Bridge — the tense discriminant.** In `bridges/workflows/types.ts` export two unions:
`WorkflowOutcomeTense` with the members `"install"` and `"preview"`, and `WorkflowOutcomeSite` with the
five members `"skipped"`, `"refused"`, `"stem-fallback"`, `"read"` and `"inspect"`. Re-export both through
`bridges/workflows/index.ts`.

In `discover.ts` declare two module-level constants, one per tense, each typed as a TOTAL
`Record&lt;WorkflowOutcomeSite, string&gt;`. Totality is the forcing construct here: a sixth site cannot be
added without an entry in both tables. Populate them with exactly the phrases named in the behavior block
above, which are the shipped wording locked by `113-CONTEXT.md` and will be quoted back. Keep the three
existing install phrases byte-identical.

Thread `tense` as a REQUIRED member of the `discoverPluginWorkflows` input object, then down through
`scanWorkflowsDirectory`, `verdictWarning` and both read-failure pushes. Replace the single
`readFailureWarning` helper with one that also takes the site, so the inspection arm inside
`isWorkflowScriptFile`'s caller resolves the `"inspect"` entry and the body-read arm resolves the `"read"`
entry. Keep `softFailWarning` as the sole template — do not add a second one, do not accept an outcomes
record, and do not add a preview wrapper function. Delete `skippedWarning`, `refusedWarning` and
`unrunnableWarning` only if the table lookup makes them redundant; if you keep them, they take the tense.
Preserve `unrunnableWarning`'s reason text verbatim in both tenses: it names the missing name and states
the description as the OTHER requirement the engine imposes, and that distinction is load-bearing.

Update the one production caller at `stage.ts` to pass `tense: "install"`, and every one of the 23
discovery call sites in `tests/bridges/workflows/discover.test.ts` likewise. Add owner cases for the
preview table at all five sites and for the inspection-versus-read split in both tenses.

**Renderer — the component-kind set and its real guard.** In `shared/notify.ts` add
`workflows?: readonly string[]` to `PluginInfoComponentsResolved["components"]`, after `skills`, and
extend that interface's doc comment, which today enumerates only four kinds, to enumerate all six and to
keep its PRECONDITION paragraph: arrays are pre-sorted at construction time and the renderer does not sort
defensively.

Extend `COMPONENT_KINDS` to six type slots and append the literal `"workflows"` LAST, matching the
alphabetical order the renderer documents. Then add the real guard, modelled on the coverage proof in
`notify-reasons.ts`: a local never-assertion alias, an `Exclude` of the component-kind union over the
tuple's element type, and an EXPORTED proof type carrying the assertion. The export is what keeps the
unused-locals rule quiet, and the proof needs a `fallow-ignore-next-line private-type-leak` marker with
its justification on the same line, exactly as the reasons proof carries one.

Correct the comment above the tuple. State what the new construct does; do not narrate what the old one
claimed, and do not leave a sentence asserting that the tuple's length is what forces the widening. The
measured fact is that the tuple's element type is the union, so five elements drawn from a six-member union
are assignable and the widening compiles clean — the length only guards extra elements.

`appendResolvedComponentLines` needs no edit: `workflows` falls into its generic single-line arm.

**Demonstrate the proof fires.** Before committing, temporarily remove the `"workflows"` literal from the
tuple while leaving the interface key, run the typechecker, and record the exact error text and code in
the summary. Then restore it. A guard whose firing was never observed is the failure mode this repository
already has on record.

**Orchestrator — the resolved arm.** In `info.ts` make `composeResolvedComponents` take
`componentPaths.workflows` as a REQUIRED readonly string array on its `resolved` parameter, and add
`pluginName` as a THIRD positional parameter rather than a member of the `resolved` bag — the two
`Parameters&lt;typeof composeResolvedComponents&gt;[1]` type references at 1259 and 2075 stay valid untouched
that way. Thread `pluginName` into `buildNotInstallablePathRowFields` and `buildNonInstallableRowFields`
as a new leading parameter; both of the latter's callers already have it in scope.

Inside `composeResolvedComponents`, call `discoverPluginWorkflows` with the plugin root, the workflows
component paths and `tense: "preview"`. Map the ADMITTED arms only — the named arm and the stem-fallback
arm — to their `generatedName`, sort with the same comparator the other name-list kinds use, and spread
the key only when non-empty. Do not filter out the stem-fallback arm: an envelope is written for it, so
omitting it would make this surface disagree with what install puts on disk. Do not walk the workflows
directory yourself and do not re-derive the generated name.

Give `deriveLenientComponentPaths` a `workflows` key seeded with the conventional directory and widened
with any declared strings, exactly as it does for the other three kinds, and add the same key to the
inline literal in the warm-git non-installable row builder. Both are compile-forced by the required
parameter, which is the point of making it required.

Leave the existing catch arms alone. A declared workflows path that escapes the plugin root raises from
the containment assertion inside discovery and folds through the existing catch to the not-resolved marker
with a probe reason, which is truthful on a lenient arm re-derived from raw manifest strings.

**Byte contract.** Add ONE new state to the `info` section of `docs/output-catalog.md` — an installed
path-source plugin whose row carries a `workflows:` line among its other component lines — with the prose
paragraph, the state marker comment and the fenced block, following the section's existing convention.
Pair it with a fixture in `tests/architecture/catalog-uat.test.ts` keyed by the same section and state.
The fixture is pure notification data: hand-write the components map, never build it from `info.ts`.

Add owner cases in `tests/orchestrators/plugin/info.test.ts` for the composition itself: both admitted
arms present, sorted; a skipped-only plugin producing no key; and the containment refusal folding to the
not-resolved marker.
  </action>
  <reversibility rating="reversible">A required parameter on an internal bridge call plus one additional rendered line. Reverting is mechanical at both layers and no persisted artifact changes shape.</reversibility>
  <verify>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node --test tests/bridges/workflows/discover.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports a nonzero `fail` count</fails_when>
    <automated>grep -c 'tense' extensions/pi-claude-marketplace/bridges/workflows/stage.ts</automated>
    <fails_when>the printed count is `0` — the one production discovery caller does not state its tense, which means the parameter was left optional</fails_when>
    <automated>grep -cE 'was not installed|was refused|was installed but will not run' extensions/pi-claude-marketplace/bridges/workflows/discover.ts</automated>
    <fails_when>the printed count is lower than `3` — an install-tense phrase pinned by existing fixtures was retuned</fails_when>
    <automated>grep -cE 'will not be installed|will be refused|would be installed but will not run|could not be inspected' extensions/pi-claude-marketplace/bridges/workflows/discover.ts</automated>
    <fails_when>the printed count is lower than `4` — a preview-tense phrase or the inspection-site split is missing</fails_when>
    <automated>grep -c '_ComponentKindsCoverageProof' extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <fails_when>the printed count is `0` — the widening direction is still unguarded</fails_when>
    <automated>grep -c 'private-type-leak' extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <fails_when>the printed count is `0` — the proof will trip the dead-code gate without its inline marker</fails_when>
    <automated>node --test tests/orchestrators/plugin/info.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports a nonzero `fail` count</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports a nonzero `fail` count, or the run reports an unpaired catalog state — a new fenced block landed in the document with no fixture behind it</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit — a dead-code finding names the new proof type or the new tense unions, or a dupes finding names the two phrase tables against each other</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'WorkflowOutcomeTense' extensions/pi-claude-marketplace/bridges/workflows/types.ts` prints at least `1`.
    - `grep -c 'tense' extensions/pi-claude-marketplace/bridges/workflows/stage.ts` prints at least `1`.
    - `grep -c 'tense' tests/bridges/workflows/discover.test.ts` prints at least `23`.
    - Both phrase tables are declared as total records keyed by the site union, so an added site is a typecheck error in both.
    - `grep -c 'could not be read and was skipped' extensions/pi-claude-marketplace/bridges/workflows/discover.ts` prints exactly `1` — the phrase survives at the body-read site only.
    - `grep -c '_ComponentKindsCoverageProof' extensions/pi-claude-marketplace/shared/notify.ts` prints at least `1`, and the declaration carries a `fallow-ignore-next-line private-type-leak` marker with an inline justification.
    - `grep -c 'breaks the typecheck here' extensions/pi-claude-marketplace/shared/notify.ts` prints `0`.
    - The summary records the verbatim typecheck error observed when the tuple entry was temporarily removed, including its TS error code.
    - `grep -c 'workflows' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` prints at least `4` — the required parameter, the discovery call, the derived lenient map and the warm-git inline literal.
    - `docs/output-catalog.md` contains a new `catalog-state` marker in the `/claude:plugin info` section whose fenced block includes a line matching `    workflows: `.
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 with the new state paired.
    - A case asserts a plugin whose scripts are all skipped or refused renders no `workflows:` key.
    - A case asserts an escaping declared workflows path folds to the not-resolved marker rather than throwing out of the command.
  </acceptance_criteria>
  <done>Running `info` on a workflow-bearing plugin prints a `workflows:` line whose bytes are pinned by the catalog; the discovery call cannot be made without stating its tense; and the component-kind guard was seen to fire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The state-only arm, the lenient arm and the list regression fixture</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, docs/output-catalog.md, tests/orchestrators/plugin/info.test.ts, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` lines 1103 to 1235 — `buildStateOnlyInstalledRow`, `composeStateOnlyComponents` and `sortComponentNames`, including the note that the record's names render verbatim as the generated installed names and the hooks read ladder
    - `extensions/pi-claude-marketplace/persistence/state-io.ts` around the plugin record's `resources` block — the `workflows` member landed as a required string array with a migrate default fill
    - `docs/output-catalog.md` lines 1790 to 1830 — the two state-only `info` states, whose shape the new one mirrors
    - `docs/output-catalog.md` lines 172 to 260 — the `list` section's states, to find whether one already covers a plugin that would carry workflows or whether a new state block is needed
    - `tests/architecture/catalog-uat.test.ts` — the `/claude:plugin list` fixture block and the list command context it drives
    - `113-CONTEXT.md` §"Update, enable/disable, and the `info` surface" — the admitted-arms rule, the alphabetical placement, and the decision that `list` gains no per-kind count
    - `113-RESEARCH.md` §"Assumptions Log" rows A3 and A6
  </read_first>
  <behavior>
    - An installation record whose marketplace manifest no longer declares the plugin renders a `workflows:` line from the persisted inventory, sorted by the same comparator the other four name-list kinds use.
    - A record whose workflow inventory is empty renders no `workflows:` line.
    - The state-only arm performs no discovery: it reads no plugin source and opens no script file.
    - A workflow-bearing plugin's `list` row is byte-identical to a plugin without workflows — the kind changes no list bytes.
    - The lenient component-path map for the structurally unavailable arm includes the conventional workflows directory plus any declared workflows strings.
  </behavior>
  <action>
Add the workflows axis to `composeStateOnlyComponents`, mirroring the `agents` line exactly: read
`record.resources.workflows`, pass it through the existing name-list sort helper, and spread the key only
when non-empty. Extend that function's doc comment, which today says the inventory comes from FOUR
name-list arrays — it is now five. Keep the hooks read ladder untouched.

Add a state-only catalog state to `docs/output-catalog.md` showing an installed record whose row carries
both a component line from an existing kind and a `workflows:` line, with the `{not in manifest}` brace the
arm always emits. Pair it with a fixture. Mirror the existing state-only states' prose in shape: state that
the names are the generated installed names read from the record, and that no discovery runs.

For the `list` half, first read the `list` catalog section and its fixtures to see whether an existing
state already pins a row for a plugin that would carry workflows. If one does, add a case in
`tests/orchestrators/plugin/list.test.ts` asserting the row is unchanged when the record carries a
non-empty workflow inventory. If none does, add one new `list` catalog state plus its paired fixture whose
prose states explicitly that the row carries no per-kind workflow count, and why: no existing kind has
one, so adding it for this kind alone would be an inconsistency rather than a feature.

Add owner cases in `tests/orchestrators/plugin/info.test.ts` for the state-only composition — populated
and empty — and one asserting the lenient derived map carries the conventional workflows directory
alongside a declared one.

Do not add a per-kind count anywhere on the `list` row.
  </action>
  <reversibility rating="costly">Two new published catalog states. Removing a state later means removing shipped documented output, which is a contract change even before release.</reversibility>
  <verify>
    <automated>node --test tests/orchestrators/plugin/info.test.ts tests/orchestrators/plugin/list.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports a nonzero `fail` count</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the run reports a catalog state with no paired fixture</fails_when>
    <automated>grep -c 'resources.workflows' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</automated>
    <fails_when>the printed count is `0` — the state-only arm still omits the kind the record names</fails_when>
    <automated>grep -c 'catalog-state' docs/output-catalog.md</automated>
    <fails_when>the printed count did not grow by at least `1` relative to the value after task 1</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</automated>
    <fails_when>non-zero exit, or the report is short of `hit === found` on branches, functions or lines</fails_when>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'resources.workflows' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` prints at least `1`.
    - The state-only composer sorts the workflow names through the same helper the other four name-list kinds use, not through a second comparator.
    - `grep -rn 'workflows' extensions/pi-claude-marketplace/orchestrators/plugin/list.ts | wc -l` prints `0` — the list row gained no per-kind handling.
    - A `list` case or fixture pins a workflow-bearing plugin's row as byte-unchanged.
    - `docs/output-catalog.md` carries a state-only `info` state whose fenced block includes a `    workflows: ` line.
    - The direct-coverage report for `info.ts` prints `hit === found` on branches, functions and lines.
  </acceptance_criteria>
  <done>Both `info` component arms show workflows, the lenient map carries the kind, and `list` is pinned as unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the preview-tense discovery warnings on `info`</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, docs/output-catalog.md, tests/orchestrators/plugin/info.test.ts, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 1470 to 1525 — `PluginInfoRow`'s declared fields, including that `reasons` is a closed set and `description` is the only free-text member today
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 3640 to 3682 — `renderPluginInfo`, where the description block and the component block are appended
    - `extensions/pi-claude-marketplace/shared/notify.ts` around 4131 to 4137 — the leak trailer, the house shape for N advisory body lines at four-space indent appended after a row
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` — `composeResolvedComponents` as written in task 1, and the three row builders that consume its result
    - `113-CONTEXT.md` §"Criterion 5", the paragraph deciding that `info` RENDERS the preview warnings and the reason `pending` was rejected as their home
    - `113-RESEARCH.md` §"Common Pitfalls" Pitfall 5 in full
    - `tests/architecture/notify-grammar-invariant.test.ts` header — why info-severity emissions are exempt from the summary invariant
  </read_first>
  <behavior>
    - An `info` row for a plugin whose workflows directory holds a refused script carries one advisory line stating the preview-tense outcome for that file.
    - The advisory lines render after the component lines and after any `dependencies:` line, at four-space indent, one line per warning, in discovery order.
    - A plugin whose scripts are all cleanly admitted carries no advisory lines and its row bytes are unchanged.
    - The row's severity is unchanged: a preview warning is a statement about a file, not a failure of the read.
    - The state-only arm carries no advisory lines, because it runs no discovery.
  </behavior>
  <action>
The tense parameter without a reader would ship five strings nothing produces an audience for — the same
defect the phase exists to clean up one criterion over. Give it a channel.

Add an optional readonly string array member to `PluginInfoRow` for free-text advisory lines. Name it for
what it carries rather than for its producer, document it as free text that the composer supplies already
ordered, and state in its doc comment that it is NOT a closed-set reason and must never carry one. In
`renderPluginInfo`, append one four-space-indented labelled line per entry AFTER the component block,
copying the leak trailer's shape. Choose one label token and keep it fixed; the byte form is pinned by the
catalog from this commit on.

In `info.ts`, return the discovery warnings alongside the components from `composeResolvedComponents` —
its return shape becomes a small object carrying both, or the warnings ride out through a sibling field;
either is fine as long as no second discovery pass runs. Thread them onto the three row builders that
consume the resolved components, spreading the key only when non-empty so an unaffected row's bytes are
unchanged. Do not thread them onto the state-only arm.

Add one catalog state to the `info` section showing a row whose workflows directory holds one refused
script, with the preview-tense phrase visible in the fenced block, and pair it with a fixture. The fixture
is pure notification data: hand-write the advisory strings, do not build them from the bridge.

**Redact before rendering.** A discovery warning embeds the ABSOLUTE component directory it walked. The
established handling for exactly this string family is `surfaceDiscoveryWarnings` in
`orchestrators/plugin/shared.ts`, which maps every warning through `redactAbsolutePaths` before it reaches
the user under NFR-9. Do the same at the composition site in `info.ts` — not in the renderer, and not
nowhere. Two consequences follow and both are wanted: the row stops disclosing the resolved home path, and
the rendered bytes stop varying by machine, which is what makes the catalog fixture pinnable at all.

Add owner cases: warnings present and rendered in order; warnings absent leaving the row byte-identical;
the state-only arm carrying none; and one asserting that a warning naming an absolute directory renders
with that path reduced, so the redaction is proved by an assertion rather than by reading the call.
  </action>
  <reversibility rating="costly">A new rendered line family on a published read surface with a paired catalog state. Removing it later is a documented-output contract change.</reversibility>
  <verify>
    <automated>node --test tests/orchestrators/plugin/info.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports a nonzero `fail` count</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/architecture/notify-grammar-invariant.test.ts tests/architecture/notify-stamp-coverage.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports a nonzero `fail` count, or a catalog state is reported unpaired</fails_when>
    <automated>grep -cE 'will not be installed|will be refused|would be installed but will not run' docs/output-catalog.md</automated>
    <fails_when>the printed count is `0` — the preview wording reached no published byte and the tense parameter still has no reader</fails_when>
    <automated>grep -rn 'ctx.ui.notify' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a byte was composed outside the notification module</fails_when>
    <automated>grep -c 'redactAbsolutePaths' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</automated>
    <fails_when>the printed count is `0` — the advisory lines carry the resolved absolute directory, which discloses the home path and makes the catalog fixture machine-dependent</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</automated>
    <fails_when>non-zero exit, or the report is short of `hit === found` on branches, functions or lines</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit — most likely the cognitive-complexity ceiling on the widened row builder</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit — most likely the independently-computed cognitive ceiling on the widened composer, which does not agree with the linter's</fails_when>
  </verify>
  <acceptance_criteria>
    - `PluginInfoRow` carries exactly one new optional free-text array member, documented as never carrying a closed-set reason.
    - The advisory lines render after the component lines and after `dependencies:`.
    - A row with no warnings renders byte-identically to the same row before this task.
    - `grep -cE 'will not be installed|will be refused|would be installed but will not run' docs/output-catalog.md` prints at least `1`.
    - `grep -rn 'ctx.ui.notify' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts | wc -l` prints `0`.
    - `grep -c 'redactAbsolutePaths' extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` prints at least `1`, and a case asserts an absolute directory in a warning renders reduced.
    - No fenced block in `docs/output-catalog.md` added by this task contains a leading-slash absolute path.
    - Exactly one discovery pass runs per resolved `info` row; a case asserts the component names and the warnings come from the same call.
    - The direct-coverage report for `info.ts` prints `hit === found` on branches, functions and lines.
  </acceptance_criteria>
  <done>A user running `info` on a plugin with a defective workflow script is told what WILL happen, in wording paired one-for-one with what the install surface says happened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-declared component path -> the discovery walk | a manifest string becomes a directory the extension reads script bodies out of |
| third-party script body -> a rendered warning string | text taken out of an untrusted file reaches the user's terminal |
| marketplace manifest strings -> the lenient component-path map | the read-only arm re-derives paths from raw manifest values the resolver refused |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-113-01 | Information disclosure | the discovery pass reached from a read-only surface | high | mitigate | The containment assertion on every declared workflows directory is unchanged and still runs before any read. A case asserts an escaping declared path refuses and folds to the not-resolved marker rather than listing contents. |
| T-113-02 | Information disclosure | the rendered soft-fail warning | medium | mitigate | The single template names the file, the directory and the handed reason — never the body. The composition site maps every warning through `redactAbsolutePaths` before it reaches a row, the same NFR-9 handling `surfaceDiscoveryWarnings` already applies to this exact string family. A case asserts the reduction. |
| T-113-03 | Tampering | symlinked script entries reached during a preview discovery | high | mitigate | The two independent symlink refusals in discovery — the directory-entry filter and the unfollowed inspection under it — are untouched by the tense threading. A case asserts a symlinked entry is still refused on the preview path. |
| T-113-04 | Spoofing | the generated workflow name shown on `info` | medium | mitigate | The name comes from the admitted verdict's own generated name, produced by the single composer that owns the engine-parity clauses. No second composer is added and no directory listing is used as a fallback. |
| T-113-SC | Tampering | package-manager installs | low | accept | This plan runs no install. Every module is in-repo and every tool is a committed development dependency. |
</threat_model>

<verification>
- The typechecker is clean at every task boundary.
- The catalog byte-equality runner passes with every new state paired.
- The direct-coverage report prints `hit === found` on branches, functions and lines for `info.ts`.
- The component-kind proof was observed to fire, and the observed error text is recorded in the summary.
- `git status` is clean after each commit, or a formatter rewrite is landed as a follow-up commit.
</verification>

<success_criteria>
- `info` renders a `workflows:` line on the resolved arm and on the state-only arm, listing both admitted verdict arms as generated names, last among the per-kind lines.
- `list` renders unchanged and is pinned by a fixture.
- The discovery call cannot be made without stating its tense, and both tense tables are total.
- The three install-tense phrases are byte-identical; the read-failure phrase is split by call site in both tenses.
- The component-kind set carries a proof that was demonstrated to fire.
- `info` renders the preview-tense warnings, pinned by a paired catalog fixture.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/113-update-enable-disable-reconcile/113-01-SUMMARY.md` when done.
Record: the verbatim typecheck error observed when the component-kind tuple entry was removed, including
its error code; the final count of discovery call sites updated in the owner test; whether the `list`
section already had a usable state or needed a new one; and the label token chosen for the advisory lines,
since every later plan's fixtures must match it.
</output>
