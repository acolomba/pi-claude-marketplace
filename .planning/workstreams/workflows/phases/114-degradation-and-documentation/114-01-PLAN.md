---
phase: 114-degradation-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WDEP-01, WDEP-02, WDEP-04]

files_modified:
  - extensions/pi-claude-marketplace/platform/pi-api.ts
  - extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - tests/platform/pi-api.test.ts
  - tests/shared/concerns/soft-dep.test.ts
  - tests/shared/notify-reasons.test.ts
  - tests/shared/notify.test.ts
  - tests/shared/notify-context.test.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/compat-01-no-expansion.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/install.messaging.test.ts
  - tests/orchestrators/plugin/list.messaging.test.ts
  - tests/orchestrators/plugin/enable-disable.messaging.test.ts
  - tests/orchestrators/plugin/update.messaging.test.ts
  - tests/orchestrators/plugin/fetch.messaging.test.ts
  - tests/orchestrators/plugin/info.messaging.test.ts
  - tests/orchestrators/plugin/uninstall.messaging.test.ts
  - tests/orchestrators/import/execute.messaging.test.ts
  - tests/orchestrators/marketplace/update.messaging.test.ts
  - tests/orchestrators/marketplace/remove.messaging.test.ts
  - tests/orchestrators/marketplace/autoupdate.messaging.test.ts
  - tests/orchestrators/reconcile/reconcile.messaging.test.ts

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`hasLoadedWorkflowEngine(pi)` returns true iff `pi.getAllTools()` contains a tool whose `name` is exactly `workflow_control`; a session exposing only a tool named `workflow` reads as NOT loaded (WDEP-01, the discriminating case)."
    - "`hasLoadedWorkflowEngine(pi)` returns false when `pi.getAllTools()` returns an empty array and when it throws, and the caught error is neither logged nor surfaced (WDEP-01 empty edge, ASVS V7)."
    - "The probe compares `tool.name` to a closed string literal by exact equality -- no case folding, no Unicode normalization, no substring match, no `sourceInfo` fallback arm (WDEP-01 encoding edge)."
    - "`Dependency` is a three-member literal union and `\"workflows\" satisfies Dependency` compiles, while an out-of-set literal remains a compile error. No runtime `DEPENDENCIES` tuple is reintroduced (CONTEXT: Dependency gains a third union member)."
    - "`REASONS` has 45 members, the 45th is appended at the TAIL after `stale workflow command`, and no existing member's index moves (WDEP-04 ordering edge)."
    - "The host-engine marker is a distinct closed-set member: it never merges with, replaces, or suppresses `requires pi-subagents` or `requires pi-mcp` on a row that declares more than one companion (WDEP-04 adjacency edge)."
    - "`composeReasons` with an empty/undefined reasons list and no unloaded declared companion returns the empty string, so a clean row renders no brace at all (WDEP-04 empty edge)."
    - "`softDepMarkers` appends the host-engine marker LAST, after the agents and mcp arms, so every existing two-marker brace form is byte-unchanged."
    - "Installing a workflow-bearing plugin in a session that exposes no `workflow_control` tool renders the host-engine marker inside the install row's brace, and the same install in a session that exposes it renders no such marker."
    - "The declares-workflows flag on `composeReasons`, `softDepMarkers`, `companionSeverity` and the three outcome types is REQUIRED and positional/named-required -- never optional with a default, so every call site is visited by a compile error rather than by a sweep."
    - "Adding the third probe field changes ZERO existing rendered bytes in `docs/output-catalog.md`: `tests/architecture/catalog-uat.test.ts` is green with no fixture edit (CONTEXT R4, RESEARCH assumption A1, verified empirically)."
  artifacts:
    - extensions/pi-claude-marketplace/platform/pi-api.ts (hasLoadedWorkflowEngine + SoftDepStatus.workflowEngineLoaded)
    - extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts (third union member, SOFT_DEP_MARKER_WORKFLOWS, third softDepMarkers arm)
    - extensions/pi-claude-marketplace/shared/notify.ts (REASONS 45th member, composeReasons fourth required boolean)
    - extensions/pi-claude-marketplace/shared/notify-reasons.ts (UnsupportedReason member, companionSeverity third disjunct, header counts)
    - extensions/pi-claude-marketplace/orchestrators/types.ts (declaresWorkflows REQUIRED on three outcome shapes)
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts (LedgerDegradationSignals.stagedWorkflows)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (composeInstalledRow derivation + severity raise)
    - tests/platform/pi-api.test.ts (the four WDEP-01 probe cases)
    - tests/shared/concerns/soft-dep.test.ts (union pin + workflows truth-table axis)
  key_links:
    - "platform/pi-api.ts::softDepStatus -> shared/concerns/soft-dep.ts::softDepMarkers -> shared/notify.ts::composeReasons -> the rendered `{...}` brace: the probe reaches the user ONLY through this chain."
    - "orchestrators/plugin/install.ts::composeInstalledRow reads `installCtx.stagedWorkflowNames.length > 0` -- the same ledger field the workflows phase already writes at install.ts:1235."
    - "REASONS (notify.ts) and UnsupportedReason (notify-reasons.ts) must gain the member together: `_ReasonsCoverageProof` is a TS2344 on a member with no topic-group home."
  prohibitions:
    - "The marker literal must never be spelled with the unscoped npm name of the engine this milestone rejected on trust grounds. A degradation message that points the operator at the wrong package sends them to install the less-sandboxed engine."
    - "The declares-workflows parameter must never be given a default value. An optional default compiles at every call site and visits none of them, which is how a partial sweep ships looking complete."
    - "No new `REASONS` member may be inserted next to its two soft-dep siblings. Reading better is not a reason to reorder a tuple whose declared order is a published byte contract."
    - "No `package.json` or `package-lock.json` change is in scope for this phase; a dependency-manifest diff in this commit is a review finding, not a convenience."
  flagged_assumptions:
    - "Edge-probe predicates authored in this plan: 5 of the phase's 14 (WDEP-01/empty, WDEP-01/encoding, WDEP-04/adjacency, WDEP-04/empty, WDEP-04/ordering). The remaining 9 are authored in plans 02-05."
    - "RESEARCH assumption A2 (unmeasured): `sonarjs/no-identical-functions` may fire on the widened three-arm derivations. If it does, extract a named helper -- never a disable directive."
---

<objective>
Make the host workflow engine the third soft dependency and prove it end to end
on one path: `pi.getAllTools()` -> probe -> `Dependency` union -> marker ->
`composeReasons` -> the rendered install row.

Purpose: a soft dependency in this codebase is four things at once -- a probe in
`platform/pi-api.ts`, a `Dependency` member, a marker constant, and a member of
the closed `REASONS` set. A partial set is a latent bug, so all four land in one
slice, wired through a real install, before any other surface is touched.

Output: the shared vocabulary, the install row's derivation and severity, and
the tests that prove the probe discriminates the host engine from the engine
this milestone rejected.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-CONTEXT.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

## Artifacts this phase produces

Every symbol Phase 114 creates, across all five plans. This plan owns the ones
marked **[01]**.

| Symbol / artifact | Home | Owner |
|---|---|---|
| `hasLoadedWorkflowEngine(pi: ExtensionAPI): boolean` | `platform/pi-api.ts` | **[01]** |
| `SoftDepStatus.workflowEngineLoaded: boolean` (REQUIRED) | `platform/pi-api.ts` | **[01]** |
| `Dependency` union member `"workflows"` | `shared/concerns/soft-dep.ts` | **[01]** |
| `SOFT_DEP_MARKER_WORKFLOWS` (module-private `Reason` const) | `shared/concerns/soft-dep.ts` | **[01]** |
| `softDepMarkers` 3rd param `declaresWorkflows: boolean` (probe moves to 4) | `shared/concerns/soft-dep.ts` | **[01]** |
| `REASONS` 45th member (tail) | `shared/notify.ts` | **[01]** |
| `composeReasons` 4th param `declaresWorkflows: boolean` (probe moves to 5) | `shared/notify.ts` | **[01]** |
| `UnsupportedReason` union member | `shared/notify-reasons.ts` | **[01]** |
| `companionSeverity` param member `declaresWorkflows: boolean` | `shared/notify-reasons.ts` | **[01]** |
| `declaresWorkflows: boolean` REQUIRED on 3 outcome shapes | `orchestrators/types.ts` | **[01]** |
| `LedgerDegradationSignals.stagedWorkflows?: boolean` | `orchestrators/plugin/shared.ts` | **[01]** |
| `declaresWorkflows` on the import row interface | `orchestrators/import/execute.ts` | 02 |
| `tests/architecture/workflows-marker-coverage.test.ts` (new suite) | `tests/architecture/` | 02 |
| `tests/architecture/no-probe-in-workflows-bridge.test.ts` (new suite) | `tests/architecture/` | 03 |
| `piWithAllLoaded()` (renamed from `piWithBothLoaded()`, TWO independent definitions) | `tests/architecture/catalog-uat.test.ts`, `tests/shared/notify.test.ts` | 04 |
| catalog state `success-with-workflow-engine-absent` + fixture | `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts` | 04 |
| catalog state `success-with-agents-and-workflows-soft-dep` + fixture | `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts` | 04 |
| `docs/workflows-compatibility.md` (new document) | `docs/` | 05 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "an engine-absent install says so" -- one path only</name>

  <files>
extensions/pi-claude-marketplace/platform/pi-api.ts,
extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts,
extensions/pi-claude-marketplace/shared/notify.ts,
extensions/pi-claude-marketplace/shared/notify-reasons.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts,
extensions/pi-claude-marketplace/orchestrators/import/execute.messaging.ts,
extensions/pi-claude-marketplace/orchestrators/marketplace/update.messaging.ts,
tests/architecture/notify-closed-set-locks.test.ts,
tests/architecture/compat-01-no-expansion.test.ts,
tests/shared/notify.test.ts,
tests/shared/notify-reasons.test.ts,
tests/shared/notify-context.test.ts,
tests/shared/concerns/soft-dep.test.ts,
tests/platform/pi-api.test.ts,
tests/orchestrators/plugin/install.test.ts,
tests/orchestrators/plugin/install.messaging.test.ts,
tests/orchestrators/plugin/list.messaging.test.ts,
tests/orchestrators/plugin/enable-disable.messaging.test.ts,
tests/orchestrators/plugin/update.messaging.test.ts,
tests/orchestrators/plugin/fetch.messaging.test.ts,
tests/orchestrators/plugin/info.messaging.test.ts,
tests/orchestrators/plugin/uninstall.messaging.test.ts,
tests/orchestrators/import/execute.messaging.test.ts,
tests/orchestrators/marketplace/update.messaging.test.ts,
tests/orchestrators/marketplace/remove.messaging.test.ts,
tests/orchestrators/marketplace/autoupdate.messaging.test.ts,
tests/orchestrators/reconcile/reconcile.messaging.test.ts
  </files>

  <read_first>
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 1121-1240 (the finished probe, marker and install-arm code, verbatim -- use as written) and lines 591-720 (the measured blast radius and the eight-site closed-set trail)
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md` sections 5, 6, 7, 8 (the RH-3 probe shape, the `mcp` marker arm, `composeReasons`, the closed-set bump ritual)
- `extensions/pi-claude-marketplace/platform/pi-api.ts` -- the whole file; the ANALOG is `hasLoadedPiSubagents` at :125-135 (RH-3), NOT `hasLoadedPiMcpAdapter` at :142-156 (its `sourceInfo` fallback re-opens the false-positive surface this probe exists to close)
- `extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts` -- the whole file; the ANALOG is its own `mcp` arm
- `extensions/pi-claude-marketplace/shared/notify.ts` lines 78-205 (the `REASONS` tuple and its "new tokens append at the tail" header) and 2250-2300 (`softDepMarkers` doc mirror, `composeReasons`, `pluginRow`) and 2320-2540 and 2660-2740 (the five composers that translate `dependencies` into declares-flags)
- `extensions/pi-claude-marketplace/shared/notify-reasons.ts` lines 1-115 (module header counts, `companionSeverity`, `UnsupportedReason`)
- `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 1807-1850 (`composeInstalledRow`) and 1200-1245 (the ledger's workflows phase, which writes `stagedWorkflowNames`)
- `tests/architecture/notify-closed-set-locks.test.ts` lines 25-56 (the bump-comment ledger; the ANALOG for how a count bump is recorded)
- `tests/architecture/compat-01-no-expansion.test.ts` lines 127-180 (the order-significant enumeration; the tail member is `stale workflow command`)
- `tests/orchestrators/plugin/install.test.ts` lines 280-440 (`toolInfo`, `makeCtx({ toolNames })`, `withHermeticHome`, `writePluginComponents`, `writeWorkflowScripts`) and lines 9600-9660 (the `WLIF-01` envelope case -- the drive shape to copy)
- `.claude/rules/typescript-comments.md`
  </read_first>

  <behavior>
Write these expectations down before implementing; they are the tracer's red
start and its acceptance.

- A session whose tool list is exactly `["workflow_control"]` reads as engine-loaded.
- A session whose tool list is exactly `["workflow"]` reads as engine ABSENT.
  This is the case that proves the requirement -- a bare-name probe passes the
  first case too.
- A session whose tool list is `["workflow", "workflow_control"]` reads as
  engine-loaded: the probe SELECTS on the discriminator while the decoy is
  present, rather than merely rejecting an absent name.
- An empty tool list, and a `getAllTools()` that throws, both read as absent.
- Installing a plugin carrying one well-formed workflow script, in a session
  whose tool list is `["workflow"]`, renders an `(installed)` row whose brace
  carries `requires pi-dynamic-workflows`.
- The same install in a session whose tool list is `["workflow_control"]`
  renders a row with no such marker.
- `REASONS.length` is 45 and the 45th entry is the new member.
  </behavior>

  <action>
Land the whole vertical slice in ONE commit. A required field and a required
parameter make the tree non-compiling until every forced site is fixed, so an
intermediate split publishes a red tree; the compile errors ARE the coverage
proof and must all be resolved here.

**1. The probe (`platform/pi-api.ts`).** Add `workflowEngineLoaded: boolean` as
a third REQUIRED field on `SoftDepStatus`, after `piMcpAdapterLoaded`. Add
`export function hasLoadedWorkflowEngine(pi: ExtensionAPI): boolean` immediately
after `hasLoadedPiSubagents`, returning
`pi.getAllTools().some((tool) => tool.name === "workflow_control")` inside a
`try` whose `catch` returns `false` with an empty body -- do not log, do not
re-throw, do not name the caught error (ASVS V7; this matches both existing
probes). Its doc comment leads with `WDEP-01`, states the predicate as an `iff`,
states that the host engine registers BOTH `workflow` and `workflow_control`
while the rejected engine registers only `workflow`, and closes with the literal
sentence `Probe failures degrade to unloaded.` Compose the new field in
`softDepStatus(pi)` as a third property, no branching. RESEARCH.md:1125-1147
carries this function finished -- use it as written.

**2. The marker (`shared/concerns/soft-dep.ts`).** Add `"workflows"` as the
third member of the `Dependency` union. Do NOT reintroduce a runtime
`DEPENDENCIES` tuple and do NOT add an order-and-length lock for it: main
removed it deliberately and the module header states why in present tense --
leave that paragraph byte-untouched and update only its member-count sentence
from two to three. Add a module-private `SOFT_DEP_MARKER_WORKFLOWS` typed
`Reason` and valued `requires pi-dynamic-workflows`; its doc comment leads with
`WDEP-04`, names `@quintinshaw/pi-dynamic-workflows` as the package, and records
why the short unscoped form is wrong -- that form is the npm name of a
DIFFERENT, rejected engine, so the short spelling would point the operator at
the wrong package. Change the sibling comment reading "both are REASONS members"
to cover three. Give `softDepMarkers` a third REQUIRED positional boolean
`declaresWorkflows`, placed BEFORE `probe` (probe becomes parameter 4), and a
third `if` arm appended LAST after the mcp arm, pushing the new marker when
`declaresWorkflows && !probe.workflowEngineLoaded`. Keep one blank line between
each `if` block (`@stylistic/padding-line-between-statements`). Extend the
doc comment with a third `Appends X iff Y` bullet and keep its statement that
the order is byte-critical for the brace join.

**3. The reason token (`shared/notify.ts`).** Append the new literal at the TAIL
of `REASONS`, after `stale workflow command` -- never next to its two soft-dep
siblings. Bump the tuple header's count sentence from 44 to 45. Give
`composeReasons` a fourth REQUIRED positional boolean `declaresWorkflows`,
placed BEFORE `probe` (probe becomes parameter 5), and forward it to
`softDepMarkers`. Then fix every call site the compiler names: there are 21
`composeReasons(` occurrences across `shared/notify.ts`,
`orchestrators/marketplace/update.messaging.ts`,
`orchestrators/import/execute.messaging.ts` and
`orchestrators/reconcile/reconcile.messaging.ts`. Exactly FIVE of them translate
a `dependencies` array into declares-flags and get
`p.dependencies?.includes("workflows") ?? false` (or the local equivalent):
`partiallyInstalledRow`, `installedLikeRow`, and the `renderPluginRow` arms for
`installed`, `updated` and `reinstalled`. Every other call site passes a literal
`false` in the new position and keeps its existing rationale comment; update the
`pluginRow`, `installedLikeRow` and `partiallyInstalledRow` doc comments that
enumerate the two markers so they enumerate three.

**4. The topic group (`shared/notify-reasons.ts`).** Append the new literal to
the `UnsupportedReason` union after the mcp member -- `_ReasonsCoverageProof` is
a TS2344 on a member with no topic-group home, so this cannot be deferred. Bump
both module-header count sentences from 44 to 45 and extend the header's bump
narration with one clause naming `WDEP-04` and the arithmetic `44 to 45`.

**5. The install row (`orchestrators/plugin/install.ts`).** In
`composeInstalledRow`, derive `const declaresWorkflows = installCtx.stagedWorkflowNames.length > 0;`
beside the existing two declares locals, and push `"workflows"` onto
`dependencies` LAST, after the mcp arm. RESEARCH.md:1229-1240 carries this arm
finished including its `WDEP-02` / `WDEP-03` comment -- use that comment model:
requirement IDs lead, present tense, no planning-artifact references. Leave the
`companionSeverity(...)` call in this function UNCHANGED in this task; Task 2
widens it.

**6. The forced sweeps.** Run `npm run typecheck` and work the error list to
zero. Two families dominate:
- ~113 `SoftDepStatus` object literals across 18 files under `tests/` (find them
  with `grep -rn "piSubagentsLoaded" tests/ --include=*.ts`). Each gains
  `workflowEngineLoaded: <boolean>`. Default the value to `true` (engine loaded)
  wherever the case says nothing about workflows, so no existing case acquires a
  new marker; use `false` only where the case is deliberately about an absent
  companion AND declares `workflows`.
- `tests/shared/notify.test.ts:4923` extracts the probe type BY POSITION with
  `Parameters<typeof composeReasons>[3]`. It MUST become `[4]`. A silent
  off-by-one here types the alias as `boolean` and makes every probe literal in
  that suite check against the wrong thing. Confirm with
  `grep -rn "Parameters<typeof composeReasons>" tests/` that this is still the
  only such site before you change the signature.

**7. The closed-set bumps (the same commit, or `npm test` is red).** Bump
`tests/architecture/notify-closed-set-locks.test.ts` -- the count appears in the
TEST TITLE and in `assert.equal(REASONS.length, 44)`; both move to 45, and the
bump ledger gains exactly ONE appended comment line naming `WDEP-04` and the
arithmetic `44 -> 45`. Do not rewrite the existing ledger lines. Bump
`tests/shared/notify.test.ts:5008`'s second `assert.equal(REASONS.length, 44)`
to 45. Append the new literal at the END of the hand-written enumeration in
`tests/architecture/compat-01-no-expansion.test.ts` (order-significant, tail
member is currently `stale workflow command`).

**8. The tracer's own end-to-end case.** In
`tests/orchestrators/plugin/install.test.ts`, add ONE case near the existing
`WLIF-01` envelope case that drives `installPlugin` under `withHermeticHome`
against a `seedPathMarketplaceWithPlugin` fixture carrying one workflow script
with a NAMED `meta` export (a default-export body classifies as skipped and
writes zero envelopes, so the case would pass for the wrong reason), once with
`makeCtx({ toolNames: ["workflow"] })` and once with
`makeCtx({ toolNames: ["workflow_control"] })`, and asserts the rendered
`notifications[0].message` carries the marker in the first run and does not in
the second. Title it with `WDEP-02` (no phase or plan references). Use the
literal `// arrange` / `// act` / `// assert` markers the file already uses.

**9. Before committing, verify the zero-byte claim (CONTEXT R4, mandatory).**
Run `tests/architecture/catalog-uat.test.ts` BEFORE any fixture edit. No
existing catalog fixture declares `workflows`, so the added probe field must
change zero rendered bytes. If it does not, stop: the byte gate is reporting a
state this phase did not intend to touch, and that is a finding, not a fixture
to update. Do NOT rename `piWithBothLoaded` here -- plan 04 owns that.

Do not touch `orchestrators/plugin/info.ts`. Its `dependencies` identifier is
the Claude plugin manifest's own dependencies declaration (backlog `PDEP-01`), an
unrelated concept; it appears in no `Dependency[]` derivation. Sweep for the
`Dependency` TYPE and the `declaresAgents` identifier, never the bare word.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- a failure here means the third probe field moved catalog bytes this phase did not intend to touch (RESEARCH assumption A1)</fails_when>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/shared/notify.test.ts tests/shared/notify-reasons.test.ts tests/shared/concerns/soft-dep.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>node --test tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`, or the new WDEP-02 case is absent from the TAP output</fails_when>
    <automated>grep -c 'requires pi-dynamic-workflows' extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts</automated>
    <fails_when>any of the three named files prints `0`</fails_when>
    <automated>grep -rn 'requires pi-workflows' extensions/ tests/ ; test $? -eq 1</automated>
    <fails_when>the command exits non-zero, meaning grep matched -- the marker was spelled with the unscoped name of the rejected engine</fails_when>
    <automated>bash -c 'test -z "$(git diff HEAD --name-only -- package.json package-lock.json)"'</automated>
    <fails_when>non-zero exit -- a dependency-manifest file is in the working diff, and no such change is in scope (WDOC-03 / CONTEXT R7)</fails_when>
  </verify>

  <acceptance_criteria>
- `extensions/pi-claude-marketplace/platform/pi-api.ts` exports
  `hasLoadedWorkflowEngine`, its body contains the literal `"workflow_control"`
  compared with `===`, and its `catch` block body is exactly `return false;`
  with no logging statement.
- `grep -c 'sourceInfo' extensions/pi-claude-marketplace/platform/pi-api.ts`
  is unchanged from before this task: the new probe adds no `sourceInfo`
  fallback arm.
- `SoftDepStatus` has exactly three fields, all `boolean`, none optional.
- `type Dependency` is a three-member literal union; no `DEPENDENCIES` identifier
  is reintroduced (`grep -c 'DEPENDENCIES' extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts`
  prints `0`).
- `REASONS.length` is 45 and the LAST element is the new marker literal;
  `tests/architecture/compat-01-no-expansion.test.ts`'s enumeration has the new
  literal as its final entry.
- `softDepMarkers` and `composeReasons` each take the declares-workflows boolean
  as a REQUIRED positional parameter with no `= false` default, placed
  immediately before `probe`.
- `grep -rn 'Parameters<typeof composeReasons>\[3\]' tests/` returns no match.
- `composeInstalledRow` pushes `"workflows"` after the `mcp` arm, never before.
- `node --test tests/architecture/catalog-uat.test.ts` is green with no edit to
  any fixture or to `docs/output-catalog.md` in this task's diff.
- The install suite's new case asserts BOTH directions (marker present under the
  decoy-only tool list, absent under the discriminator tool list).
  </acceptance_criteria>

  <reversibility rating="costly">
Growing a closed set that is pinned by a published byte contract is reversible
but not cheap: this milestone already reversed one `REASONS` member
(`WINV-03` / `D-109-01`, 44 to 43) and the reversal moved catalog bytes,
enumeration order and three count assertions together. The marker's spelling is
the costly half -- it becomes a published operator-facing string in plans 04
and 05.
  </reversibility>

  <done>
`npm run typecheck` reports zero errors; `REASONS` has 45 members with the new
one at the tail; a workflow-bearing install in a session exposing only a tool
named `workflow` renders the host-engine marker and the same install in a
session exposing `workflow_control` does not; `catalog-uat.test.ts` is green
with zero fixture edits.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The severity raise and the outcome vocabulary</name>

  <files>
extensions/pi-claude-marketplace/shared/notify-reasons.ts,
extensions/pi-claude-marketplace/orchestrators/types.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/update.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts,
extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts,
tests/orchestrators/plugin/install.test.ts
  </files>

  <read_first>
- `extensions/pi-claude-marketplace/shared/notify-reasons.ts` lines 78-100 (`companionSeverity` -- the ANALOG is its own two-disjunct body)
- `extensions/pi-claude-marketplace/orchestrators/types.ts` lines 50-80 (the reinstalled arm's `declaresAgents` / `declaresMcp` and its `CMC-13` required-boolean rationale), 165-182 (`PluginUpdateBase`), 440-470 (`InstallPluginOutcome`'s `installed` arm and the COMPAT-01 key-set pin note)
- `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` lines 60-140 (`LedgerDegradationSignals` -- the ANALOG is its own `stagedAgents` / `stagedMcpServers` members and the `WR-01` `partition?: never` rationale)
- `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 1830-1850 and 1895-1915 (the `companionSeverity` call and the outcome producer)
- `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` lines 2820-2845 (the `companionSeverity` call) and 2640-2655 (the one producer that derives rather than literal-`false`s)
- `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` lines 345-370 (the signals producer), 1080-1095 (the re-emit), 1260-1280 (the `companionSeverity` call)
- `extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts` lines 525-545 (the signals producer)
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 654-683 (Tier 3: which surfaces stamp, and the `reinstall` no-raise asymmetry)
  </read_first>

  <behavior>
- A clean install that staged at least one workflow, in a session with no
  `workflow_control` tool, stamps `warning` -- the operation WAS carried out
  (envelopes are written and correct) but the desired state is not reached,
  because nothing runs them yet.
- The same install in a session that exposes `workflow_control` stamps `info`.
- The `reinstall` row stamps the marker but does NOT raise severity: that
  asymmetry is an existing deliberate precedent and is byte-pinned by the
  catalog's reinstall soft-dep block. Copy it; do not "fix" it.
- `uninstalled` and `disabled` rows still cannot carry the marker: their
  composers hard-code the declares-flags false and their variants carry no
  `dependencies` field.
  </behavior>

  <action>
Widen `companionSeverity`'s destructured object parameter with a REQUIRED
`declaresWorkflows: boolean` and add a THIRD disjunct
`(declaresWorkflows && !probe.workflowEngineLoaded)` to its returned ternary
condition. A third disjunct keeps the function well under both the
`sonarjs/cognitive-complexity: 15` ceiling and fallow's independently computed
`maxCognitive: 15`; do not restructure it into a `Record<Dependency, keyof SoftDepStatus>`
loop here -- that is a larger change in a phase whose acceptance is a byte gate,
and RESEARCH records it as a follow-up. Update its doc comment so the
declared-companion enumeration names three companions, not two.

Fix the three call sites the compiler names:
- `orchestrators/plugin/install.ts` (in `composeInstalledRow`): pass the
  `declaresWorkflows` local Task 1 already derived.
- `orchestrators/plugin/update.ts`: pass `outcome.declaresWorkflows`.
- `orchestrators/plugin/enable-disable.ts`: pass `outcome.stagedWorkflows === true`,
  matching the `=== true` idiom the two sibling flags already use there (the
  signals are optional booleans; do not normalize the two idioms).

To make those two reads exist, widen two vocabularies:

**`orchestrators/types.ts`** gains `readonly declaresWorkflows: boolean` as a
REQUIRED sibling of `declaresMcp` at all THREE declaration sites -- the
reinstalled arm, `PluginUpdateBase`, and `InstallPluginOutcome`'s `installed`
arm. Required, never optional: the existing `CMC-13` comment at the reinstalled
arm already states the reason (every producer populates the predicate
explicitly rather than relying on `undefined ~= false`) and the same reason
governs here. Then fix every producer the compiler names -- there are roughly 18
across `install.ts`, `update.ts` (about 12, most of them literal `false` on
non-`updated` partitions), `reinstall.ts` and `marketplace/update.ts`. Derive
the real predicate where the ledger has one: `install.ts` uses
`installCtx.stagedWorkflowNames.length > 0`, `update.ts`'s `updated` producer
uses its staged-workflow-names length beside the existing
`stagedAgentNames.length > 0`, and `reinstall.ts` uses
`resources.workflows.length > 0` beside its existing `resources.agents.length > 0`.
Every other producer passes literal `false` for the same structural reason its
sibling flags already do, and keeps that reason in the surrounding comment.

**`orchestrators/plugin/shared.ts`** gains `readonly stagedWorkflows?: boolean`
on `LedgerDegradationSignals`, mirroring `stagedMcpServers` including its
doc-comment shape (`SEV-01` lead, "carries a COUNT verdict only"). Every field
on that interface is optional and omitted when empty, so an unaffected outcome
still renders byte-identically -- keep that discipline. Wire the two producers:
`enable-disable.ts`'s signals builder (the `...(x && { … })` spread beside
`stagedAgents`) reading `summary.stagedWorkflowNames.length > 0`, its outcome
re-emit, and `reconcile/apply.ts`'s equivalent spread. Leave
`enableRowDependencies` in this file UNTOUCHED -- plan 02 owns it.

Extend the install suite case Task 1 added so it also asserts the severity of
the recorded notification: `warning` on the engine-absent run, `info` on the
engine-present run.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node --test tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/architecture/compat-01-no-expansion.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- COMPAT-01 pins the install outcome's key set bidirectionally, so it reddens on both a widening and a narrowing</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output names `sonarjs/cognitive-complexity` or `sonarjs/no-identical-functions`</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit, or the output reports a `health` threshold breach on `companionSeverity`</fails_when>
  </verify>

  <acceptance_criteria>
- `companionSeverity`'s parameter object declares three REQUIRED booleans and its
  return expression contains exactly three disjuncts.
- `grep -c 'declaresWorkflows' extensions/pi-claude-marketplace/orchestrators/types.ts`
  prints `3`.
- Every `declaresWorkflows` declaration in `orchestrators/types.ts` is
  `readonly ... : boolean` -- no `?:` anywhere.
- `LedgerDegradationSignals` declares `stagedWorkflows?: boolean` and the two
  producers (`enable-disable.ts`, `reconcile/apply.ts`) set it through the same
  conditional-spread idiom their `stagedAgents` sibling uses, so an outcome that
  staged no workflow omits the key entirely.
- The install suite's engine-absent run asserts severity `warning`; the
  engine-present run asserts `info`.
- `orchestrators/plugin/reinstall.messaging.ts` is NOT in this task's diff: the
  reinstall row's no-severity-raise asymmetry is preserved by leaving it alone.
- `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts`'s
  `enableRowDependencies` body is byte-unchanged in this task's diff.
  </acceptance_criteria>

  <done>
`companionSeverity` raises to `warning` for an unloaded host engine on a row
that declares workflows; all three outcome shapes carry the required predicate
and every producer sets it explicitly; `npm run typecheck`, `npm run lint` and
`npm run fallow` are all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The probe's discriminating cases and the union pin</name>

  <files>
tests/platform/pi-api.test.ts,
tests/shared/concerns/soft-dep.test.ts,
tests/shared/notify-reasons.test.ts
  </files>

  <read_first>
- `tests/platform/pi-api.test.ts` -- the whole file; the ANALOG is the existing `extensionApiWithTools(tools)` helper at :27-29 and the `hasLoadedPiSubagents` cases beside it
- `tests/shared/concerns/soft-dep.test.ts` -- the whole file; the ANALOG is its own `satisfies Dependency` block at :9-12 and its 16-row probe/declares truth table
- `tests/shared/notify-reasons.test.ts` lines 40-60 (the `_ReasonsCoverageProof` type-level pin -- it needs NO edit, it is a proof and not a count) and 140-260 (the `companionSeverity` truth table)
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 1149-1225 (the four probe assertions and the union pin, written out)
- `.agents/skills/typescript-unit-testing-review/SKILL.md`
  </read_first>

  <behavior>
Four probe cases, and the second and third are the ones that carry the
requirement:

1. tool list `["workflow_control"]` -> `true`
2. tool list `["workflow"]` -> `false` (the rejected engine's session shape)
3. tool list `["workflow", "workflow_control"]` -> `true` (the host engine's real
   session shape: the probe SELECTS on the discriminator while the decoy is
   present)
4. empty tool list -> `false`; `getAllTools()` that throws -> `false`

Cases 1 and 4 alone pass under a bare-name probe and therefore prove nothing
about WDEP-01. Cases 2 and 3 are the acceptance.
  </behavior>

  <action>
In `tests/platform/pi-api.test.ts`, add the four `hasLoadedWorkflowEngine` cases
above using the file's existing `extensionApiWithTools` helper, plus one case
asserting that `softDepStatus(pi)` composes `workflowEngineLoaded` from the same
tool list. Title each with `WDEP-01`. The negative case's title must name the
decoy explicitly, so a reader can see at a glance which assertion is the
discriminator; never use a phase or plan reference in a title.

In `tests/shared/concerns/soft-dep.test.ts`, add
`void ("workflows" satisfies Dependency);` to the existing `satisfies` block,
keeping the trailing `@ts-expect-error` line that proves the union still
excludes an unknown companion target. Then extend the truth table along the
workflows axis. Do NOT expand it to the full 64-row cross product: add the rows
that carry a claim -- declares-workflows with the engine absent (marker fires),
declares-workflows with the engine present (no marker), declares-nothing with
the engine absent (no marker), and at least one three-marker row asserting the
brace order `agents`, `mcp`, `workflows` as an exact array. The shape of the case
records is yours; keep the file's existing `probe: { ... }` literal style.

In `tests/shared/notify-reasons.test.ts`, extend the `companionSeverity` table
with the workflows axis: declares-workflows + engine absent is `warning`;
declares-workflows + engine present is `info`; and one row proving the third
disjunct composes with the other two rather than replacing them (two unloaded
companions still `warning`). Leave `_ReasonsCoverageProof` alone -- it is a
compile-time proof, not a count, and it already went green in Task 1.
  </action>

  <verify>
    <automated>node --test tests/platform/pi-api.test.ts tests/shared/concerns/soft-dep.test.ts tests/shared/notify-reasons.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>grep -c 'workflow_control' tests/platform/pi-api.test.ts</automated>
    <fails_when>prints a number less than `4`</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/platform/pi-api.ts</automated>
    <fails_when>non-zero exit, or the report shows an uncovered branch inside `hasLoadedWorkflowEngine`</fails_when>
  </verify>

  <acceptance_criteria>
- `tests/platform/pi-api.test.ts` contains an assertion whose input tool list is
  exactly one tool named `workflow` and whose expected result is `false`.
- `tests/platform/pi-api.test.ts` contains an assertion whose input tool list
  contains BOTH `workflow` and `workflow_control` and whose expected result is
  `true`.
- `tests/platform/pi-api.test.ts` contains an assertion for a throwing
  `getAllTools()` returning `false`.
- `tests/shared/concerns/soft-dep.test.ts` contains
  `void ("workflows" satisfies Dependency);` and retains its
  `@ts-expect-error` out-of-set line.
- At least one `softDepMarkers` case asserts a THREE-element result whose order
  is agents, then mcp, then the host-engine marker -- asserted as an exact array,
  not as a membership check.
- No test title in the three files contains `Phase`, `Plan`, `Wave` or `Task`
  followed by a number.
  </acceptance_criteria>

  <done>
All four WDEP-01 probe cases pass, including the two that a bare-name probe
would fail; the `Dependency` union pin covers three members; the marker-order
case pins agents-mcp-workflows as an exact array.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi host session -> `platform/pi-api.ts` | The tool list returned by `pi.getAllTools()` is host-controlled data: any loaded extension can register a tool with any name. |
| `shared/notify.ts` -> `ctx.ui.notify` | Every rendered reason token crosses to the operator's terminal. |
| The repository -> the operator's package manager | A rendered token that names a package is an implicit install instruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-01 | Spoofing | `hasLoadedWorkflowEngine` (`platform/pi-api.ts`) | medium | mitigate | A different extension registering a tool named `workflow` must NOT read as the host engine. Probe the quintinshaw-specific `workflow_control` name by exact `===` equality, with no `sourceInfo` fallback arm. Task 3's decoy-only case is the acceptance. A false positive misreports rather than mis-writes -- the probe never gates writing (WDEP-02). |
| T-114-02 | Information disclosure | the rendered reason brace | low | mitigate | The marker is a fixed closed-set literal with zero interpolation. Never interpolate a plugin, workflow, path or tool name into it. Enforced structurally: `Reason` is a closed union, so a template literal is a compile error. |
| T-114-03 | Information disclosure | the probe's `catch` block | low | mitigate | The `catch` body is exactly `return false;`. Do not log, re-throw or name the caught error -- a host-thrown error could carry environment detail into the notify channel. Matches both existing probes (ASVS V7). |
| T-114-04 | Tampering | the marker literal | high | mitigate | The token must name the scoped host engine, not the unscoped npm name of the engine this milestone rejected on trust grounds -- the rejected engine runs third-party scripts with the real host `process`, real fs internals and a Function-constructor escape. A misspelled token is an instruction to install the less-sandboxed package. Task 1's verify greps for the wrong spelling and fails on a match; the constant's doc comment records the rationale so a later rename cannot silently undo it. |
| T-114-05 | Denial of service | `pi.getAllTools()` | low | accept | A host that throws or hangs on `getAllTools()` degrades the marker to absent. Accepted: the probe is render-time only, the artifacts are written regardless, and both existing probes make the same trade. |
| T-114-SC | Tampering | npm dependency manifest | high | mitigate | This phase installs nothing. No `package.json` / `package-lock.json` edit is in scope; `acorn ^8.16.0` is already declared (CONTEXT R7). Task 1's verify asserts an empty diff on both manifest files; any manifest change in this phase's commits is a review finding. No package-legitimacy checkpoint is required because no install task exists. |
</threat_model>

<verification>
- `npm run typecheck` reports zero errors after every task.
- `node --test tests/architecture/catalog-uat.test.ts` is green with zero
  fixture or catalog edits in this plan's diff (RESEARCH assumption A1 verified
  empirically, CONTEXT R4).
- `node --test tests/platform/pi-api.test.ts tests/shared/concerns/soft-dep.test.ts tests/shared/notify-reasons.test.ts tests/shared/notify.test.ts`
  is green.
- `node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts`
  is green at 45 members.
- `npm run lint` and `npm run fallow` are green.
- `git diff HEAD --name-only -- package.json package-lock.json` is empty.
</verification>

<success_criteria>
- The host engine is probed by its discriminating tool name, and a session
  exposing only the rejected engine's tool reads as absent.
- `Dependency` carries a third member; `REASONS` carries a 45th at the tail.
- The declares-workflows flag is REQUIRED at every declaration site.
- One real install renders the marker when the engine is absent and does not
  when it is present, and stamps `warning` / `info` accordingly.
- Zero existing catalog bytes moved.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-01-SUMMARY.md` when done.

Record in it: the exact count `npm run typecheck` reported after the first
compile attempt (the size of the forced sweep is the coverage evidence), the
`catalog-uat.test.ts` result from step 9 of Task 1 (the zero-byte verification),
and any site where the `false` default chosen for an existing `SoftDepStatus`
literal was NOT obvious.
</output>
