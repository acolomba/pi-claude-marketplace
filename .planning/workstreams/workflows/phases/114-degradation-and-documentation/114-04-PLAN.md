---
phase: 114-degradation-and-documentation
plan: 04
type: execute
wave: 2
depends_on: ["114-01"]
autonomous: true
requirements: [WDOC-02, WDEP-04]

files_modified:
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify.test.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/output-catalog.md` carries two NEW annotated states under the install verb, each paired with a fixture in `tests/architecture/catalog-uat.test.ts`, and the byte-equality gate passes in BOTH directions (an unmatched catalog annotation fails; an orphan fixture entry fails)."
    - "The second state renders `{requires pi-subagents, requires pi-dynamic-workflows}` inside ONE brace, comma-space separated, with the host-engine marker SECOND -- the two markers neither merge nor open a second brace (WDEP-02 adjacency edge). This byte-pins the marker order that the deleted `DEPENDENCIES` tuple index used to imply."
    - "The exact-count assertion moves 192 -> 194 in all THREE places it appears in the same block: the explanatory comment, the `assert.equal` argument, and the template-literal failure message."
    - "`piWithBothLoaded()` is renamed `piWithAllLoaded()` in BOTH of its two INDEPENDENT definitions -- `tests/architecture/catalog-uat.test.ts` and `tests/shared/notify.test.ts` -- with `{ name: \"workflow_control\" }` added to each body, so its name and its \"no soft-dep markers fire\" comment stay true after the third probe field exists."
    - "The rename changes zero rendered catalog bytes: no pre-existing fixture declares `workflows`, so adding a tool to the probe helper cannot move any state that was already there."
    - "`docs/output-catalog.md`'s prose member count for the `REASONS` tuple reads 45. It currently reads 43 against a 44-member tuple -- a staleness that predates this phase, sits outside the byte gate, and is corrected here so a reviewer does not read the delta as this phase's bug."
    - "`docs/messaging-style-guide.md` no longer describes a runtime `DEPENDENCIES` tuple as the `Dependency` declaration site, no longer says the probe-target set has two members, and no longer names `present` among the dependency-bearing variants."
  artifacts:
    - docs/output-catalog.md (2 new annotated states + 3 corrected prose sentences)
    - tests/architecture/catalog-uat.test.ts (2 new fixtures, 1 new probe helper, the rename, the exact-count bump)
    - tests/shared/notify.test.ts (the second, independent rename)
    - docs/messaging-style-guide.md (the stale-prose sweep)
  key_links:
    - "the catalog state and its fixture are ONE change: `catalog-uat.test.ts` walks the document, pairs each `<!-- catalog-state: NAME -->` annotation with the next fenced block, and fails on an unmatched annotation AND on an orphan fixture -- so a doc-only or a fixture-only edit is red either way"
    - "the two catalog states are now the order authority for the brace: main deleted the runtime tuple whose index used to carry it, and byte equality against a rendered example is what replaces it"
  prohibitions:
    - "The catalog region must not be restructured. It is additively amendable; a restructure moves rendered bytes on states this phase never intended to touch, and the gate cannot tell an intentional move from a regression."
    - "The rename must not be done with a text substitution across the two files. A `sed` sweep over two large fixture files is exactly the change that silently rewrites a string INSIDE a fixture, and a rewritten fixture string is a moved catalog byte that the gate will attribute to this phase."
    - "The exact-count assertion must not be turned into a floor or a range to avoid maintaining it. It exists to stop a parser refactor from silently reading a fraction of the corpus, and a floor cannot do that."
  flagged_assumptions:
    - "Edge-probe predicates authored in this plan: 1 truth (WDEP-02/adjacency) plus 1 flagged assumption (WDOC-02/unclassified, below). Plan 01 authored 5, plan 02 authored 2, plan 03 authored 2, plan 05 authors 3 -- 14 in all."
    - "WDOC-02's edge probe returned `unclassified`: no automatic edge category applies to `docs/output-catalog.md carries the new reason token and the new rendered states, under the existing byte-equality gate`. It is surfaced here rather than auto-resolved. Reviewed manually: the checkable claim is bidirectional byte equality between the two new annotated states and their two fixtures, owned by `tests/architecture/catalog-uat.test.ts`, plus the exact-count assertion moving with them. That is the gate; there is no edge beyond it."
    - "RESEARCH assumption A1: adding the third probe field changes ZERO existing catalog bytes. Plan 01 verified this empirically before any fixture edit. If Task 1's rename moves a byte anyway, stop -- the gate is reporting a state this phase did not intend to touch."
---

<objective>
Amend the byte-gated output catalog: rename the probe helper that becomes a lie
once a third companion exists, add the two rendered states that pin the new
marker and its position inside the brace, and correct the prose counts that sit
outside the gate and have already gone stale.

Purpose: main deleted the runtime `DEPENDENCIES` tuple whose index used to imply
the brace order. Byte equality against two rendered examples is what replaces it,
which is why the second state is not decoration.

Output: two catalog states with their fixtures, one renamed helper in two
independent definitions, and four corrected prose sentences.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-CONTEXT.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-01-SUMMARY.md
</context>

## Artifacts this phase produces

The full phase-wide list is in `114-01-PLAN.md`. This plan owns:

| Symbol / artifact | Home |
|---|---|
| `piWithAllLoaded()` (renamed, TWO independent definitions) | `tests/architecture/catalog-uat.test.ts`, `tests/shared/notify.test.ts` |
| `piWithoutWorkflowEngine()` (new probe helper) | `tests/architecture/catalog-uat.test.ts` |
| catalog state `success-with-workflow-engine-absent` + fixture | `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts` |
| catalog state `success-with-agents-and-workflows-soft-dep` + fixture | `docs/output-catalog.md`, `tests/architecture/catalog-uat.test.ts` |

<tasks>

<task type="auto">
  <name>Task 1: Rename the probe helper in both of its definitions</name>

  <files>
tests/architecture/catalog-uat.test.ts,
tests/shared/notify.test.ts
  </files>

  <read_first>
- `tests/architecture/catalog-uat.test.ts` lines 205-230 -- the probe-helper block. `piWithBothLoaded()` returns two tool literals and its doc comment claims no soft-dep markers fire; `piWithMcpLoaded()` beside it is the shape for a partial-probe helper
- `tests/shared/notify.test.ts` lines 60-80 -- the SECOND, INDEPENDENT definition of the same helper. An editor symbol rename in one file does not reach the other; this is two renames, not one
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-CONTEXT.md` -- decision R4 in full
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md` section 9, the probe-helper analog
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-01-SUMMARY.md` -- the recorded zero-byte verification, which this task must not invalidate
  </read_first>

  <action>
Do BOTH renames with an editor-scoped symbol rename, never a text substitution.
A `sed` across two files of this size is the change that silently rewrites a
string inside a fixture, and a rewritten fixture string is a moved catalog byte
the gate will attribute to this phase.

Measured occurrence counts before the change, for a post-change sanity check:
188 in `tests/architecture/catalog-uat.test.ts` and 116 in
`tests/shared/notify.test.ts`, each count including that file's own definition
and doc comment.

In each file:
1. Rename `piWithBothLoaded` to `piWithAllLoaded` at every occurrence.
2. Add `{ name: "workflow_control" }` to the returned tool array, after the two
   existing entries.
3. Rewrite the helper's doc comment as a present-tense fact naming all three
   companions and stating that no soft-dep marker fires under it. Do not narrate
   the rename or the former name.

In `tests/architecture/catalog-uat.test.ts` only, add a sibling helper
`piWithoutWorkflowEngine()` returning the two tool literals the old body had --
the subagent and mcp names, without the engine's discriminator. Its doc comment
states exactly which marker fires under it: the host-engine marker, on
dep-bearing rows that declare `workflows`. Task 2 uses it.

Run the catalog gate immediately after and confirm zero rendered bytes moved.
The suite passing is necessary but not sufficient here: also confirm the diff
touches no fenced ` ```text ` block in `docs/output-catalog.md` (this task must
not touch that file at all) and no string literal inside a fixture's `message`
object.
  </action>

  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/shared/notify.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>grep -rn 'piWithBothLoaded' tests/ ; test $? -eq 1</automated>
    <fails_when>the command exits non-zero, meaning grep matched -- the old name survives somewhere</fails_when>
    <automated>grep -c 'piWithAllLoaded' tests/architecture/catalog-uat.test.ts tests/shared/notify.test.ts</automated>
    <fails_when>either file prints `0`, or `tests/architecture/catalog-uat.test.ts` prints fewer than `188` or `tests/shared/notify.test.ts` fewer than `116`</fails_when>
    <automated>bash -c 'test -z "$(git diff HEAD --name-only -- docs/output-catalog.md)"'</automated>
    <fails_when>non-zero exit -- this task must not touch the catalog document</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
  </verify>

  <acceptance_criteria>
- `grep -rn 'piWithBothLoaded' tests/` returns no match.
- Both `piWithAllLoaded` bodies return three tool literals, the third named
  `workflow_control`.
- Both doc comments name three companions and neither mentions the former name.
- `tests/architecture/catalog-uat.test.ts` declares `piWithoutWorkflowEngine`
  returning exactly two tool literals.
- `docs/output-catalog.md` is absent from this task's diff.
- The catalog gate is green and no pre-existing fixture entry changed: the
  `expectedSeverity` and `dependencies` values of every fixture that existed
  before this task are byte-identical afterwards, and this task adds no fixture
  entry at all.
  </acceptance_criteria>

  <done>
Both independent definitions carry the true name and the third tool, a
partial-probe sibling exists for the new states, and the byte gate is green with
zero rendered bytes moved.
  </done>
</task>

<task type="auto">
  <name>Task 2: The two byte-pinned catalog states</name>

  <files>
docs/output-catalog.md,
tests/architecture/catalog-uat.test.ts
  </files>

  <read_first>
- `docs/output-catalog.md` lines 500-560 -- `### Success with soft-dep markers` (:509-520) and `### Success with orphan-rewake AND a soft-dep marker in the same brace` (:537-550). The second is the closer analog: it is itself a brace-composition state. Copy the exact additive shape -- H3 title, blank line, the `<!-- catalog-state: NAME -->` annotation, blank line, a ` ```text ` fence with the rendered bytes, blank line, then ONE explanatory paragraph naming the decision IDs
- `docs/output-catalog.md` lines 870-890 -- the reinstall soft-dep block, which carries NO attention summary line. That is the byte-pinned no-severity-raise asymmetry; do not disturb it and do not copy it into the new install states, which DO raise
- `tests/architecture/catalog-uat.test.ts` lines 1150-1185 -- the `success-with-orphan-rewake-and-soft-dep` fixture, the exact fixture shape to copy (annotation-name key, `pi` from a probe helper, explicit `expectedSeverity`, a message literal whose `dependencies` array is the derivation under test)
- `tests/architecture/catalog-uat.test.ts` lines 90-110 and 355-380 -- `CATALOG_STATE_RE` and the `FIXTURES` map, so the pairing rule is clear
- `tests/architecture/catalog-uat.test.ts` lines 5485-5510 -- the exact-count block. The number appears THREE times in it: an explanatory comment, the `assert.equal` argument, and the template-literal failure message
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 725-800 -- how the gate works mechanically and which two states to add
  </read_first>

  <behavior>
State 1, annotated `success-with-workflow-engine-absent`: a plugin installed
under the install verb, declaring only `workflows`, probed with
`piWithoutWorkflowEngine()`. Its row carries exactly one marker inside the brace
-- the host-engine one. `expectedSeverity` is `warning`, so the rendered block
opens with the attention summary line and closes with the reload trailer.

State 2, annotated `success-with-agents-and-workflows-soft-dep`: the same verb,
a plugin declaring `agents` AND `workflows`, probed with the existing
`piWithMcpLoaded()` (subagents unloaded, mcp loaded, engine absent). Its row
renders BOTH markers inside ONE brace, comma-space separated, with the
host-engine marker SECOND. `expectedSeverity` is `warning`.

State 2 is the byte-order guarantee. It is what pins the position the deleted
`DEPENDENCIES` tuple index used to imply, so its rendered bytes are the contract,
not an illustration.
  </behavior>

  <action>
Add the two states to `docs/output-catalog.md` as SIBLINGS immediately after
`### Success with soft-dep markers`, under the same install-verb H2. Append
inside the region; do not restructure, because a restructure moves rendered bytes
on states this phase never intended to touch.

Each state gets: an H3 title in the region's sentence style, the
`<!-- catalog-state: ... -->` annotation, a ` ```text ` fence holding the exact
rendered block, and ONE explanatory paragraph. The paragraph for state 1 names
`WDEP-02` and the tri-state severity rule -- the envelopes ARE written and
correct, but the desired state is not reached because nothing runs them yet, so
the row stamps `warning` rather than `info`. The paragraph for state 2 names
`WDEP-04` and `D-16-15`, states that the marker order inside the brace is
`agents`, `mcp`, `workflows`, and states plainly that this rendered block is the
order authority now that no runtime tuple carries an index.

Add the two matching fixtures to the `FIXTURES` map in
`tests/architecture/catalog-uat.test.ts`, keyed by the annotation names, each
with its `pi` probe helper, its explicit `expectedSeverity: "warning"`, and a
message literal whose `dependencies` array is the derivation under test
(`["workflows"]` and `["agents", "workflows"]` respectively).

Bump the exact-count assertion from 192 to 194 in ALL THREE places in that one
block -- the comment, the `assert.equal` argument, and the template-literal
message. All three move together or the failure message lies about the number it
just checked.

`docs/output-catalog.md` is formatted by `mdformat` through pre-commit. Never run
`prettier --write` on it; `npm run format:check` covers only js/json/ts.

Write the rendered blocks by RUNNING the renderer, not by hand-composing them.
Compose the fixture first, run the gate, and take the exact bytes the gate
reports as expected-versus-actual; hand-typed indentation is how a byte gate
learns to accept a wrong ladder.
  </action>

  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`. An unmatched catalog annotation, an orphan fixture entry, or a rendered-byte mismatch each redden it</fails_when>
    <automated>grep -c 'catalog-state: success-with-workflow-engine-absent' docs/output-catalog.md</automated>
    <fails_when>prints anything other than `1`</fails_when>
    <automated>grep -c 'catalog-state: success-with-agents-and-workflows-soft-dep' docs/output-catalog.md</automated>
    <fails_when>prints anything other than `1`</fails_when>
    <automated>grep -c 'requires pi-subagents, requires pi-dynamic-workflows' docs/output-catalog.md</automated>
    <fails_when>prints `0` -- the two-marker brace order is not byte-pinned</fails_when>
    <automated>grep -c '192' tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>prints anything other than `0`</fails_when>
    <automated>grep -c '194' tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>prints a number less than `3` -- the count did not move in all three places</fails_when>
    <automated>git diff HEAD --numstat -- docs/output-catalog.md</automated>
    <fails_when>the deletions column (the second field) is anything other than `0` -- this task is purely additive, so a removed line means the catalog region was restructured rather than appended to (the prose corrections belong to Task 3)</fails_when>
  </verify>

  <acceptance_criteria>
- Both new states sit under the install-verb H2, immediately after
  `### Success with soft-dep markers`, and no other H3 in the file moved.
- State 2's fenced block contains the two markers in one brace with the
  host-engine marker second, comma-space separated.
- Both fixtures declare `expectedSeverity: "warning"` and both rendered blocks
  open with the attention summary line.
- State 1's fixture uses `piWithoutWorkflowEngine()`; state 2's uses the existing
  `piWithMcpLoaded()`.
- The exact-count block reads 194 in the comment, the assertion and the message.
- The reinstall soft-dep block at `docs/output-catalog.md` is byte-unchanged.
  </acceptance_criteria>

  <done>
Two paired states are in the catalog and in the fixture map, the gate is green in
both directions, the two-marker brace order is byte-pinned, and the exact count
reads 194 in all three of its places.
  </done>
</task>

<task type="auto">
  <name>Task 3: The stale prose outside the gate</name>

  <files>
docs/output-catalog.md,
docs/messaging-style-guide.md
  </files>

  <read_first>
- `docs/output-catalog.md` line 63 (the `REASONS` tuple member count, currently reading 43 against a tuple that has been 44 since the previous phase and is 45 after plan 01) and line 69 (the soft-dep marker enumeration and the dep-bearing-variant sentence)
- `docs/messaging-style-guide.md` lines 26-30 (the `Dependency` type comment naming a deleted tuple), 59-62 (the `DEPENDENCIES` closed-set bullet), 65-70 (the dependency-field discipline sentence naming a retired status and a stale variant count), 82-86 and 164-169 (the two-probe / two-marker enumerations)
- `extensions/pi-claude-marketplace/shared/notify.ts` -- the `PLUGIN_STATUSES` tuple and the four variants that declare a `dependencies` field, so the corrected numbers are read from the source rather than inferred
- `extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts` lines 20-35 -- the union's own header, which states the no-runtime-tuple rationale as a present-tense fact. That is the correct FORM for the style guide's replacement sentences
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 684-724 -- the eight-site closed-set trail and the "also stale, outside every gate" table with the exact line references
- `.agents/skills/simple-english/SKILL.md`
  </read_first>

  <action>
Six prose corrections. None is covered by a test, which is why they went stale;
correct them here and name the pre-existing staleness in the commit body so a
reviewer does not read the delta as this phase's bug.

`docs/output-catalog.md`:
- The `REASONS` tuple member-count sentence reads 43. The tuple has been 44 since
  the previous phase and is 45 after plan 01. Correct it to 45.
- The soft-dep marker sentence names two markers. Add the third, and keep the
  sentence's existing claim that markers are emitted by the renderer at render
  time from the `dependencies` field and the host probe, never placed by callers.

`docs/messaging-style-guide.md`:
- The `Dependency` type comment describes the type as derived from a
  `DEPENDENCIES` tuple. That tuple was deleted from main. Rewrite it as a
  three-member literal union whose sole declaration site is the union itself, in
  the same present-tense form `shared/concerns/soft-dep.ts`'s own header uses.
  Do NOT narrate the deletion; a comment about code that no longer exists is
  worse than none.
- The `DEPENDENCIES` closed-set bullet describes a runtime tuple with two
  members. Rewrite it for the union, with three probe targets and their three
  companion mappings.
- The dependency-field discipline sentence names `present` among the
  dependency-bearing variants and gives a stale count of the remaining variants.
  `present` is a retired status. Reconcile the sentence against
  `shared/notify.ts` and against `docs/output-catalog.md`'s own statement: the
  dep-bearing variants are `installed | updated | reinstalled |
  partially-installed`, `partially-installed`'s field is the one that is
  OPTIONAL, and `PLUGIN_STATUSES` has 19 members. Take the numbers from the
  source, not from either document.
- The two remaining enumerations of the probes and the markers each grow by one.

Do NOT touch `docs/open-closed-proof.md`: its statement already reflects the
tuple's deletion correctly.

Both files are formatted by `mdformat` through pre-commit. Never run
`prettier --write` on them. Use `--` where a dash is wanted and let the texthooks
fixer normalize it.
  </action>

  <verify>
    <automated>grep -c '43-member' docs/output-catalog.md</automated>
    <fails_when>prints anything other than `0`</fails_when>
    <automated>grep -c '45-member' docs/output-catalog.md</automated>
    <fails_when>prints `0`</fails_when>
    <automated>grep -c 'requires pi-dynamic-workflows' docs/messaging-style-guide.md</automated>
    <fails_when>prints `0`</fails_when>
    <automated>grep -c 'DEPENDENCIES' docs/messaging-style-guide.md</automated>
    <fails_when>prints anything other than `0` -- the deleted runtime tuple is still described as the declaration site</fails_when>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- a prose edit that strayed into a fenced block reddens the byte gate</fails_when>
    <automated>pre-commit run --files docs/output-catalog.md docs/messaging-style-guide.md</automated>
    <fails_when>non-zero exit, or the output reports that `mdformat`, `markdownlint-cli2` or a texthooks fixer modified either file</fails_when>
    <automated>bash -c 'test -z "$(git diff HEAD --name-only -- docs/open-closed-proof.md)"'</automated>
    <fails_when>non-zero exit -- `docs/open-closed-proof.md` already reflects the tuple deletion correctly and must stay out of the diff</fails_when>
  </verify>

  <acceptance_criteria>
- `docs/output-catalog.md`'s `REASONS` prose count reads 45 and its marker
  sentence names three markers.
- `docs/messaging-style-guide.md` contains no `DEPENDENCIES` identifier and
  describes `Dependency` as a three-member literal union that is its own
  declaration site.
- The style guide's dependency-field sentence names four dep-bearing variants,
  does not name `present`, and its variant arithmetic sums to 19.
- Both marker/probe enumerations in the style guide name three.
- No fenced ` ```text ` block in `docs/output-catalog.md` changed
  (`node --test tests/architecture/catalog-uat.test.ts` green).
- `docs/open-closed-proof.md` is absent from the diff.
  </acceptance_criteria>

  <done>
Four documents' worth of stale soft-dependency prose now matches the tree: no
deleted tuple is described as a declaration site, no count is short by one or
two, and no enumeration stops at two markers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/output-catalog.md` -> `ctx.ui.notify` bytes | The document is the published contract for what the operator sees; the byte gate makes it binding rather than descriptive. |
| the rendered marker -> the operator's package manager | A marker that names a package is an implicit install instruction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-14 | Tampering | the published marker spelling | high | mitigate | The catalog is where the marker becomes an operator-facing published string. Both new states must render the scoped host-engine name, never the unscoped npm name of the engine this milestone rejected on trust grounds -- that engine runs third-party scripts with the real host `process`, real fs internals and a Function-constructor escape. Plan 01's grep gate covers `extensions/` and `tests/`; Task 2's rendered blocks are the doc-side check. |
| T-114-15 | Repudiation | `tests/architecture/catalog-uat.test.ts` | medium | mitigate | A helper named for two companions while probing three reports the engine ABSENT under a name claiming no marker fires, and the suite stays green because no fixture declares workflows yet -- invisible drift until the first workflows fixture mysteriously fires a marker. Task 1's rename plus the added tool removes the lie in both definitions. |
| T-114-16 | Tampering | the exact-count assertion | medium | mitigate | The count exists to stop a `loadCatalogExamples` refactor from silently parsing a fraction of the corpus. It moves 192 -> 194 in all three places and is never relaxed into a floor. |
| T-114-17 | Tampering | fixture string literals | high | mitigate | A blind text substitution across two large fixture files can rewrite a string inside a rendered-bytes expectation, moving a catalog byte under cover of a rename. Task 1 mandates an editor-scoped symbol rename and a post-change diff check that no fixture `message` literal moved. |
| T-114-SC | Tampering | npm dependency manifest | high | mitigate | No package-manager install task exists in this plan; `package.json` and `package-lock.json` stay out of the diff (CONTEXT R7). No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts tests/shared/notify.test.ts` green.
- `grep -rn 'piWithBothLoaded' tests/` returns no match.
- Both new annotations appear exactly once in `docs/output-catalog.md` and both
  have fixtures.
- The exact count reads 194 in three places and 192 nowhere.
- `grep -c 'DEPENDENCIES' docs/messaging-style-guide.md` prints 0.
- `pre-commit run --files docs/output-catalog.md docs/messaging-style-guide.md`
  is clean and modified nothing.
</verification>

<success_criteria>
WDOC-02 is met: the catalog carries the new token and two new rendered states
under its existing byte-equality gate, the marker's position inside the brace is
pinned by bytes rather than by convention, and the prose counts that sit outside
the gate are correct.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-04-SUMMARY.md` when done.

It MUST record: the `piWithAllLoaded` occurrence counts before and after the
rename in each of the two files; confirmation that no fixture `message` literal
changed during the rename; and a note that the `REASONS` prose count in
`docs/output-catalog.md` was stale BEFORE this phase (43 against a 44-member
tuple) so a reviewer reads the two-step correction as a repair, not a defect.
</output>
