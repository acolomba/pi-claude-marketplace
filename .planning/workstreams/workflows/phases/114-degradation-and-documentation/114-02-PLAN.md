---
phase: 114-degradation-and-documentation
plan: 02
type: execute
wave: 2
depends_on: ["114-01"]
autonomous: true
requirements: [WDEP-02, WDEP-04]

files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - tests/architecture/workflows-marker-coverage.test.ts
  - tests/orchestrators/plugin/list.test.ts
  - tests/orchestrators/plugin/shared.test.ts
  - tests/orchestrators/plugin/update-row.test.ts
  - tests/orchestrators/plugin/reinstall.messaging.test.ts
  - tests/orchestrators/import/execute.test.ts
  - tests/orchestrators/reconcile/apply-outcomes.test.ts
  - tests/orchestrators/reconcile/notify.test.ts

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All SEVEN `Dependency[]` derivation sites push `\"workflows\"`: `orchestrators/plugin/install.ts` (landed in plan 01), `plugin/list.ts`, `plugin/shared.ts`, `plugin/reinstall.messaging.ts`, `plugin/update-row.ts`, `import/execute.ts`, `reconcile/apply-outcomes.ts`. The enumeration was re-read against the tree at plan time and is complete: no other file under `orchestrators/` constructs a `Dependency[]`."
    - "Every derivation pushes `\"workflows\"` LAST, after the agents and mcp arms, so every existing two-marker rendered brace is byte-unchanged (WDEP-02 ordering edge)."
    - "A plugin that stages zero workflow scripts -- including one shipping an EMPTY `workflows/` directory -- derives an empty-of-`workflows` `Dependency[]` and its row carries no host-engine marker (WDEP-02 empty edge)."
    - "One architecture suite owns the cross-site coverage claim: it drives a PUBLIC surface per site and asserts on the RENDERED ROW, so all seven cases prove the same end-to-end claim rather than seven different intermediate ones (CONTEXT R5)."
    - "The gate reddens on EXACTLY ONE case when a single derivation arm is reverted, and the failure message names that site."
    - "No derivation is reached through a new export, a widened visibility, or any other test-only seam: five of the seven stay module-private and are driven through their exported outcome-to-row composers or their full orchestrator entry point."
    - "`enableRowDependencies`'s `partition?: never` refusal survives the `Pick` widening: the update and reinstall outcome shapes still fail to compile against it rather than structurally matching and silently returning an empty array (WR-01)."
  artifacts:
    - tests/architecture/workflows-marker-coverage.test.ts (the criterion-3 gate, new)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (dependenciesFromDeclares third flag)
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts (enableRowDependencies third arm, widened Pick)
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.messaging.ts (dependenciesFromOutcome third arm)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts (outcomeDependencies third flag)
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts (declaresWorkflows on the row interface + dependenciesFromInstalled third arm)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts (dependenciesFromInstall third arm)
  key_links:
    - "list.ts derives its declaration from the PERSISTED `record.resources.workflows` array, beside the two existing `record.resources.*` reads -- the inventory row's only source of truth."
    - "shared.ts::enableRowDependencies is called from BOTH enable-disable.ts and reconcile/notify.ts, so one widening serves the standalone enable row and the reconcile enable projection and the two cannot drift."
    - "The gate's three hard-to-reach cases drive `installPlugin` / `loadPluginListPayload` / `importClaudeSettings`; the harness shape (makeCtx / withHermeticHome / seedPathMarketplaceWithPlugin) is re-created in the new suite, never imported from a `*.test.ts` (importing a node:test module re-registers its cases)."
  prohibitions:
    - "No derivation may be exported, widened, or given a test-only accessor so a test can call it. If a site cannot be reached from a public surface, that is a finding about the surface to record, not a licence to widen it."
    - "The gate must never assert that a rule or a configuration exists. It drives real composers and reads real rendered rows; a suite that reads its own expectations back out of production code proves nothing."
    - "The negative control must target one of the three sites reachable only by driving a full orchestrator. Those are the cases a coverage gate can most easily satisfy vacuously, and controlling an easy site instead would leave exactly the risk the control exists to remove."
    - "`orchestrators/plugin/info.ts` must not be edited. Its `dependencies` identifier is the Claude plugin manifest's own dependencies declaration, an unrelated concept, and it constructs no `Dependency[]`."
  flagged_assumptions:
    - "Edge-probe predicates authored in this plan: 2 of the phase's 14 (WDEP-02/empty, WDEP-02/ordering). Plan 01 authored 5, plan 03 authors 2, plan 04 authors 1 truth plus 1 flagged assumption, plan 05 authors 3."
    - "RESEARCH assumption A6 (MEDIUM-HIGH, undriven): all seven sites are reachable from a public surface. Each caller chain was read, but no prototype gate was driven. If a Tier C drive turns out to need a seam, record it as a surface finding in the SUMMARY -- do not export the helper."
    - "RESEARCH assumption A2 (unmeasured): `sonarjs/no-identical-functions` may fire once the seven derivations become three-arm copies of each other. If it does, extract one named helper the sites share; never a disable directive."
---

<objective>
Make every `Dependency[]` derivation site stamp `workflows`, and prove the
coverage with a gate rather than a grep: one architecture suite, one case per
site, each asserting on the rendered row, with a negative control that turns
exactly one case red.

Purpose: criterion 3 is a cross-site claim. Seven scattered per-site assertions
can each be deleted without anything noticing; one owner cannot. This milestone
has already shipped two guards that were green because they checked nothing, and
in both cases only the negative control caught it.

Output: six widened derivations, one coverage gate, and a recorded negative
control run.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-CONTEXT.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-VALIDATION.md
@.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-01-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

## Artifacts this phase produces

The full phase-wide list is in `114-01-PLAN.md`. This plan owns:

| Symbol / artifact | Home |
|---|---|
| `dependenciesFromDeclares` 3rd param `declaresWorkflows` | `orchestrators/plugin/list.ts` |
| `enableRowDependencies` widened `Pick` + third arm | `orchestrators/plugin/shared.ts` |
| `dependenciesFromOutcome` third arm | `orchestrators/plugin/reinstall.messaging.ts` |
| `outcomeDependencies` 3rd param `declaresWorkflows` | `orchestrators/plugin/update-row.ts` |
| `declaresWorkflows` on the import row interface + `dependenciesFromInstalled` third arm | `orchestrators/import/execute.ts` |
| `dependenciesFromInstall` third arm | `orchestrators/reconcile/apply-outcomes.ts` |
| `tests/architecture/workflows-marker-coverage.test.ts` | new suite |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The six remaining derivation sites</name>

  <files>
extensions/pi-claude-marketplace/orchestrators/plugin/list.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.messaging.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts,
extensions/pi-claude-marketplace/orchestrators/import/execute.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts,
tests/orchestrators/plugin/list.test.ts,
tests/orchestrators/plugin/shared.test.ts,
tests/orchestrators/plugin/update-row.test.ts,
tests/orchestrators/plugin/reinstall.messaging.test.ts,
tests/orchestrators/import/execute.test.ts,
tests/orchestrators/reconcile/apply-outcomes.test.ts,
tests/orchestrators/reconcile/notify.test.ts
  </files>

  <read_first>
- `extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts` lines 430-455 -- `dependenciesFromInstall`, the SMALLEST and CLEAREST instance of the shape and the ANALOG for all six (one `const deps: Dependency[] = []`, one `if` per flag, blank line between, `return deps`)
- `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` lines 108-140 -- `enableRowDependencies`, the ONE site with a structural wrinkle: read its `WR-01` doc comment in full before touching the signature
- `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` lines 296-320 (`dependenciesFromDeclares`), 490-510 (where the two declares locals are derived from `record.resources`), 600-620 (the row composition)
- `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.messaging.ts` lines 215-240 (`reinstalledRowFromOutcome`) and 350-375 (`dependenciesFromOutcome`)
- `extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts` lines 95-115 (`updatedRowFromOutcome`) and 150-175 (`outcomeDependencies`)
- `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` lines 70-90 (the local row interface carrying `declaresAgents`), 355-375 (`dependenciesFromInstalled`), 730-750 (the producer that fills the row interface from the install outcome)
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md` section 11 (all six analogs, plus the `=== true` vs truthiness per-site idiom rule)
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-RESEARCH.md` lines 545-590 (the seven sites with their reachability tiers) and 654-683 (which surfaces stamp and which structurally cannot)
- `.claude/rules/typescript-comments.md`
  </read_first>

  <behavior>
Per site, two claims:

- With the host engine absent and the plugin declaring workflows, the site's
  rendered row carries the marker inside the brace, positioned AFTER any
  `requires pi-subagents` / `requires pi-mcp` already there.
- With the plugin declaring no workflows -- including the empty-directory case,
  where discovery yields zero scripts -- the row carries no host-engine marker
  at all, and its bytes are identical to today's.

Two surfaces must still be UNABLE to carry the marker after this task: the
`uninstalled` row and the `disabled` row. Their variants declare no
`dependencies` field and their composers hard-code the declares-flags false;
that is structural (MSG-SD-3 / ENBL-15 / D-100-06), not an omission.
  </behavior>

  <action>
Widen six derivations. Match the LOCAL idiom at each site -- do not normalize
one into another. Sites reading required booleans test truthiness; sites reading
optional booleans compare `=== true`.

**`plugin/list.ts`.** Give `dependenciesFromDeclares` a third parameter
`declaresWorkflows: boolean` and a third `if` appended LAST. Derive the argument
beside the two existing `record.resources.*` reads as
`record.resources.workflows.length > 0` -- that array is already a REQUIRED
`string[]` on the state schema, so no schema change is needed. Extend the
function's doc comment so its marker enumeration names three markers.

**`plugin/shared.ts`.** Widen `enableRowDependencies`'s `Pick<LedgerDegradationSignals, ...>`
to include `"stagedWorkflows"` (plan 01 added that optional member) and append a
third arm testing `signals.stagedWorkflows === true`. The
`& { readonly partition?: never }` refusal MUST survive verbatim: it is what
excludes the update and reinstall outcome shapes that would otherwise
structurally match and silently return an empty array. Extend the `WR-01` doc
comment's enumeration rather than rewriting it.

**`plugin/reinstall.messaging.ts`.** Append a third arm to
`dependenciesFromOutcome` reading `outcome.declaresWorkflows` (plan 01 made it a
REQUIRED member of the reinstalled arm). Do NOT add a severity raise here: the
reinstall row stamps the marker but never calls `companionSeverity`, and the
catalog's reinstall soft-dep block carries no attention summary line. That
asymmetry is deliberate and byte-pinned; record it in the comment as deliberate
so a later reader does not repair it as an oversight.

**`plugin/update-row.ts`.** Give `outcomeDependencies` a third parameter and a
third appended arm; pass `outcome.declaresWorkflows` from
`updatedRowFromOutcome`.

**`import/execute.ts`.** Add `readonly declaresWorkflows: boolean` to the local
row interface beside `declaresAgents`, fill it from `outcome.declaresWorkflows`
at the producer, and append the third arm to `dependenciesFromInstalled`.

**`reconcile/apply-outcomes.ts`.** Add `readonly declaresWorkflows: boolean` to
`dependenciesFromInstall`'s inline parameter shape and append the third arm.
Check whether `reconcile/notify.ts`'s enable projection needs the
`stagedWorkflows` signal threaded to reach `enableRowDependencies`; if it does,
thread it through the same conditional-spread idiom the sibling signals use.

Every new arm's comment leads with `WDEP-02` / `WDEP-04`, is present tense, and
states the fact rather than narrating the change. No `Phase NN` / `Plan NN` /
`Wave N` / bare `Pitfall N` anywhere.

Then extend each site's OWNER test with a workflows case: `list.test.ts`,
`shared.test.ts`, `update-row.test.ts`, `reinstall.messaging.test.ts`,
`execute.test.ts`, `apply-outcomes.test.ts`, `notify.test.ts`. Each owner test
proves its own row; the cross-site claim belongs to Task 2's gate, so keep these
narrow -- one positive and one empty-declaration case per site is enough.

If `sonarjs/no-identical-functions` fires on the now-three-arm copies, extract
ONE named helper the sites share and give it a doc comment naming why it exists.
Do not add a disable directive.

Leave `orchestrators/plugin/info.ts` alone.
  </action>

  <verify>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/shared.test.ts tests/orchestrators/plugin/update-row.test.ts tests/orchestrators/plugin/reinstall.messaging.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>node --test tests/orchestrators/import/execute.test.ts tests/orchestrators/reconcile/apply-outcomes.test.ts tests/orchestrators/reconcile/notify.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>grep -c 'workflows' extensions/pi-claude-marketplace/orchestrators/plugin/list.ts extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.messaging.ts extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts extensions/pi-claude-marketplace/orchestrators/import/execute.ts extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts</automated>
    <fails_when>any of the six named files prints `0`</fails_when>
    <automated>bash -c 'test -z "$(git diff HEAD --name-only -- extensions/pi-claude-marketplace/orchestrators/plugin/info.ts)"'</automated>
    <fails_when>non-zero exit -- `info.ts` is in the working diff, and its `dependencies` identifier is an unrelated concept that must not be swept</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output names `sonarjs/no-identical-functions` (that finding is a signal to extract a shared helper, never to suppress)</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit, or the output reports a `dupes` clone or a boundary violation on any of the six edited orchestrator files</fails_when>
  </verify>

  <acceptance_criteria>
- Each of the six derivation functions contains exactly three `if` blocks and the
  `workflows` push is the LAST of the three.
- `enableRowDependencies`'s signature still contains `partition?: never`
  (`grep -c 'partition?: never' extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts`
  prints at least `1`).
- `list.ts` derives its flag from `record.resources.workflows.length > 0`, not
  from a manifest read or a filesystem probe.
- `reinstall.messaging.ts`'s diff contains no `companionSeverity` call.
- Each of the seven owner suites named above gains at least one case whose
  expectation includes the host-engine marker, and at least one whose
  expectation excludes it because the plugin declares no workflows.
- `orchestrators/plugin/info.ts` is absent from this task's diff.
  </acceptance_criteria>

  <done>
All six remaining derivations push `workflows` last; each owner suite proves its
own row both ways; `npm run typecheck`, `npm run lint` and `npm run fallow` are
green and `info.ts` is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The marker-coverage gate</name>

  <files>tests/architecture/workflows-marker-coverage.test.ts</files>

  <read_first>
- `tests/architecture/notify-stamp-coverage.test.ts` -- the whole file. It is the ONLY architecture suite in the tree that proves a cross-site coverage claim by DRIVING production composers, with no source grep and no private export. Copy its header docstring framing, its import ordering, its `as const satisfies readonly T[]` closed list, its single `assert.deepEqual` over a projected array, its `<IDs>: <claim in present tense>` test titles, and its literal `// arrange` / `// act` / `// assert` markers
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-PATTERNS.md` section 1 -- the recommended `SiteCase` record, the one-assertion projection shape, and the table naming the existing end-to-end driver for each of the three hard-to-reach sites
- `tests/orchestrators/plugin/install.test.ts` lines 280-440 -- `toolInfo`, `makeCtx({ toolNames })`, `withHermeticHome`, `seedPathMarketplaceWithPlugin`, `writeWorkflowScripts`. These are PRIVATE to that file and must be re-created here, not imported
- `tests/orchestrators/plugin/list.test.ts` lines 35-50 and 330-380 -- how `listPlugins` and `loadPluginListPayload` are driven
- `tests/orchestrators/import/execute.test.ts` lines 30-45 and 415-430 -- how `importClaudeSettings` is driven
- `tests/architecture/source-scan.ts` lines 1-30 -- the convention for a non-`.test.ts` sibling under `tests/architecture/` that "registers no case of its own", in case the drive helpers are worth lifting out
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-VALIDATION.md` -- "The Negative Control for Criterion 3's Gate"
- `.agents/skills/typescript-unit-testing-review/SKILL.md`
  </read_first>

  <behavior>
One suite, seven cases, one per `Dependency[]` derivation site. Each case drives
a PUBLIC surface twice -- once with a session tool list that omits
`workflow_control` and once with a list that includes it -- and returns the
RENDERED row or block as a string.

The suite asserts ONCE, with a single `assert.deepEqual` over a projected array
of seven records shaped `{ site, marked, clean }`, against a hand-written
expected array of seven `{ site, marked: true, clean: true }`. `marked` is
"the engine-absent render contains the host-engine marker"; `clean` is "the
engine-present render does not".

That shape is what makes the mandatory negative control informative: deleting
one derivation arm must produce EXACTLY ONE differing row in the diff, and that
row names its site.

The seven sites and their public surfaces:

| Site | Public surface the case drives |
|---|---|
| `orchestrators/plugin/install.ts` | `installPlugin(...)`, read `notifications[0].message` |
| `orchestrators/plugin/list.ts` | `loadPluginListPayload(...)` / `listPlugins(...)` |
| `orchestrators/plugin/shared.ts` | the exported `enableRowDependencies(...)`, rendered through `notify` |
| `orchestrators/plugin/reinstall.messaging.ts` | the exported `reinstalledRowFromOutcome(...)` / `outcomeToPluginMessage(...)` |
| `orchestrators/plugin/update-row.ts` | the exported `updatedRowFromOutcome(...)` |
| `orchestrators/import/execute.ts` | `importClaudeSettings(...)` |
| `orchestrators/reconcile/apply-outcomes.ts` | the exported `dependenciesFromInstall(...)`, rendered through `notify` |
  </behavior>

  <action>
Create `tests/architecture/workflows-marker-coverage.test.ts`. Do NOT create a
new top-level directory under `tests/` -- `unit-suite-glob-completeness.test.ts`
cross-checks the npm scripts' globs against a recursive walk and reddens on one.
`tests/architecture/` is exempt from the file-pairing gate, so this suite needs
no paired production module.

Open with the file-path-first header docstring the sibling architecture suites
use: name the file, name `WDEP-04` and criterion 3, state that the per-site owner
suites prove their own rows, and state that THIS suite's distinct architectural
claim is that EVERY `Dependency[]` derivation site renders the marker. Say why
that claim needs one owner: seven scattered assertions can each be deleted
without anything noticing.

Define the case records as a module-level list. Each record carries a `site`
string spelling the repo-relative path of the derivation (so a failing row
identifies the file to open) and a `drive(engineLoaded: boolean)` function
returning the rendered string. Build the fake `pi` from a tool-name list --
`["workflow_control"]` for loaded, `["workflow"]` for absent. Use the decoy
spelling rather than an empty list for the absent case: it re-proves the WDEP-01
discriminator through every surface for free.

For the two Tier A sites, call the exported derivation, put its result into a
`dependencies` field on a plugin message literal, and render it through the same
`notify` entry point `catalog-uat.test.ts` uses. For the two Tier B sites, build
the outcome object and call the exported outcome-to-row composer. For the three
Tier C sites, re-create the harness (`makeCtx`, `withHermeticHome`, the
marketplace/plugin seeding, and a workflow script with a NAMED `meta` export --
a default-export body classifies as skipped and writes zero envelopes, so a case
using one would pass for the wrong reason).

Reach nothing through a new export. Five of the seven derivations are
module-private and stay that way. If one turns out to be unreachable from any
public surface, stop and record it in the SUMMARY as a finding about the
surface; do not widen visibility to close the gate.

Assert exactly once, with `assert.deepEqual` over the projection. Title the test
`WDEP-04 / SNM-06: ...` in present tense.
  </action>

  <verify>
    <automated>node --test tests/architecture/workflows-marker-coverage.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0`</fails_when>
    <automated>grep -c 'orchestrators/' tests/architecture/workflows-marker-coverage.test.ts</automated>
    <fails_when>prints a number less than `7` -- fewer than seven site paths named means the enumeration is short</fails_when>
    <automated>grep -c 'assert.deepEqual' tests/architecture/workflows-marker-coverage.test.ts</automated>
    <fails_when>prints `0`, or prints a number greater than `2` (the gate asserts once over the projection, not per site)</fails_when>
    <automated>node --test tests/architecture/unit-suite-glob-completeness.test.ts tests/architecture/no-stale-test-citations.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- a new suite outside the npm glob, or a citation to a path that does not resolve, reddens these</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>

  <acceptance_criteria>
- The suite names all seven derivation-site paths as literal strings, and the
  expected array has exactly seven records.
- Every case's `drive` returns a rendered string, not a `Dependency[]` and not a
  message object: all seven prove the same end-to-end claim.
- The suite imports nothing from any `*.test.ts` file:
  `grep -cE '^\s*(import|} from) .*\.test\.ts' tests/architecture/workflows-marker-coverage.test.ts`
  prints `0` (importing a `node:test` module re-registers its cases).
- No production module gained an export to serve this suite: the diff for this
  task touches only `tests/architecture/workflows-marker-coverage.test.ts`.
- Each Tier C case seeds a workflow script with a named `meta` export, so the
  install genuinely stages a workflow rather than silently staging none.
- The test title contains no `Phase`, `Plan`, `Wave` or `Task` followed by a
  number.
  </acceptance_criteria>

  <done>
`tests/architecture/workflows-marker-coverage.test.ts` is green with seven cases
driven through public surfaces, asserting once over a projected array, with no
new production export.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the negative control and record it</name>

  <files>
extensions/pi-claude-marketplace/orchestrators/import/execute.ts,
tests/architecture/workflows-marker-coverage.test.ts
  </files>

  <read_first>
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-VALIDATION.md` -- "The Negative Control for Criterion 3's Gate", the five numbered steps and the two failure modes it exists to catch
- `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-CONTEXT.md` -- decision R5 (target a hard-to-reach site, never an easy one) and the mandatory-negative-control decision under "The marker-coverage gate"
- `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` lines 355-375 (`dependenciesFromInstalled`, the arm to revert)
- `tests/architecture/workflows-marker-coverage.test.ts` (as written in Task 2)
  </read_first>

  <precondition>Task 2's gate is green on an unmodified tree before the control is run; a control started from a red gate proves nothing about which case reddens.</precondition>

  <action>
Run the control against `orchestrators/import/execute.ts` -- one of the three
sites reachable only by driving a full orchestrator, and therefore one of the
three a coverage gate can most easily satisfy vacuously. Do not pick one of the
two already-exported sites.

1. Confirm the gate is green.
2. Delete the `workflows` push arm from `dependenciesFromInstalled` and leave
   everything else byte-identical.
3. Run the gate. Capture the FULL failing output verbatim -- the assertion diff
   and the case row that differs.
4. Confirm EXACTLY ONE row differs and that its `site` value names
   `orchestrators/import/execute.ts`. If more than one reddens, the gate is
   asserting on a shared composer rather than on each site's own derivation and
   Task 2 must be reshaped. If none reddens, that case's drive never staged a
   workflow and the assertion is vacuous.
5. Restore the arm exactly. Re-run the gate and confirm green.
6. Confirm `git diff` is empty for `orchestrators/import/execute.ts` after the
   restore -- a control that leaves a residue is a control that shipped a bug.

If budget allows, repeat steps 2-5 for the other two hard-to-reach sites
(`orchestrators/plugin/install.ts`, `orchestrators/plugin/list.ts`). Whatever is
done, the SUMMARY must paste the failing output and state explicitly which sites
were NOT individually controlled. An unstated partial control is how this
milestone already shipped two guards that checked nothing.
  </action>

  <verify>
    <automated>node --test tests/architecture/workflows-marker-coverage.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary does not read `# fail 0` -- this is the post-restore run, so a failure means the reverted arm was not restored byte-identically</fails_when>
    <automated>bash -c 'test -z "$(git diff HEAD --stat -- extensions/pi-claude-marketplace/orchestrators/import/execute.ts)"'</automated>
    <fails_when>non-zero exit -- the reverted arm was not restored byte-identically, so the control left a residue</fails_when>
    <automated>npm run typecheck 2>&1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
  </verify>

  <acceptance_criteria>
- The SUMMARY contains the verbatim failing output of the gate under the deleted
  arm, including the assertion diff.
- That output shows exactly one differing case row, and the row's `site` value
  is `orchestrators/import/execute.ts`.
- The SUMMARY states which of the seven sites were individually controlled and
  which were not.
- `git status` shows no modification to `orchestrators/import/execute.ts` after
  the control.
  </acceptance_criteria>

  <done>
The gate has been shown to be a gate: one deleted derivation arm at a
full-orchestrator site turned exactly one case red, that case named the site,
the arm was restored, the gate is green again, and the failing transcript is in
the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin state record -> the `list` inventory row | `record.resources.workflows` is persisted data that a hand-edited `state.json` controls. |
| install / import outcome -> the rendered cascade | Outcome flags cross from the ledger into an operator-visible row. |
| the coverage gate -> production module visibility | A gate that cannot reach a site is under pressure to widen the production surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-114-06 | Tampering | `orchestrators/plugin/list.ts` inventory row | low | mitigate | The declaration is derived from the array length only; no element value is read, rendered or interpolated. A hostile `state.json` can flip a marker on or off, which is the same exposure the two existing companions already carry. |
| T-114-07 | Elevation of privilege | production module visibility | high | mitigate | Exporting a module-private derivation so the gate can call it widens the production API for a test's convenience and is forbidden by this project's conventions. All five private sites are driven through exported composers or full orchestrator entry points; a site that cannot be reached is recorded as a surface finding instead. |
| T-114-08 | Repudiation | the coverage gate | high | mitigate | A gate that is green because it checked nothing is indistinguishable from a gate that passed. Task 3's negative control targets a full-orchestrator site, requires exactly one red case naming its site, and pastes the transcript into the SUMMARY. |
| T-114-09 | Information disclosure | the rendered brace | low | accept | The marker is a closed-set literal with no interpolation (see T-114-02 in plan 01). Accepted here with no additional control. |
| T-114-SC | Tampering | npm dependency manifest | high | mitigate | No package-manager install task exists in this plan. `git diff --name-only -- package.json package-lock.json` must stay empty across the phase (CONTEXT R7); any manifest change is a review finding. No package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- All seven `Dependency[]` derivations push `workflows` last.
- `node --test tests/architecture/workflows-marker-coverage.test.ts` is green
  with seven cases.
- The negative control turned exactly one case red at a full-orchestrator site,
  and its transcript is in the SUMMARY.
- `npm run typecheck`, `npm run lint`, `npm run fallow` green.
- `orchestrators/plugin/info.ts` is untouched.
- No production export was added to serve a test.
</verification>

<success_criteria>
Criterion 3 is met: a gate, not a grep, proves the marker coverage, and
reverting any one `Dependency[]` derivation turns exactly one case red -- shown,
not asserted.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/114-degradation-and-documentation/114-02-SUMMARY.md` when done.

It MUST contain: the verbatim failing output from Task 3's negative control; the
list of which sites were individually controlled and which were not; and any
site where reaching the derivation from a public surface turned out to be harder
than RESEARCH assumption A6 predicted.
</output>
