---
phase: 110-domain-and-platform-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/platform/workflow-home.ts
  - tests/platform/workflow-home.test.ts
  - extensions/pi-claude-marketplace/domain/workflow-project-key.ts
  - tests/domain/workflow-project-key.test.ts
autonomous: true
requirements: [WPTH-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WPTH-02: `workflowHomeDir()` derives the host engine's storage root from the home directory alone. It reads no `cwd` and no environment override of its own, so the deprecated `<cwd>/.pi/workflows/saved/` legacy project path is not a value the function can produce."
    - "Criterion 4: no test-only relocation seam survives in `platform/workflow-home.ts`. The owner test relocates the root by setting `process.env.HOME`, which `os.homedir()` re-reads on every call, and restores the previous value through a `t.after()` registered BEFORE the mutation."
    - "The seam-free module reaches 100% direct coverage from its own owner test alone — `FN 1/1 · BR 2/2 · LN 8/8` was measured on the exact post-deletion shape."
    - "Criterion 6: the project-key derivation reproduces the engine's across the Spike 025 case set, and every expected key is a transcribed literal rather than a value the test recomputes — so changing the 12-hex hash width fails all thirteen literal rows at once."
    - "The dash strip runs BEFORE the 48-character slice. The `\"a\".repeat(47) + \"-b\"` row is the only row that catches an inversion of those two steps, and its doubled dash is the whole signal."
    - "`path.resolve()` runs BEFORE `path.basename()`. Two spellings of one project — `/home/acolomba/some-project` and `/home/acolomba/../acolomba/some-project` — collapse onto one identical key, and a relative spelling resolved from a pinned cwd produces that same key."
    - "V12: `.`, `..` and every path separator are unreachable outputs of the sanitizer, which is what lets Phase 111 join the key under the workflow home without an async containment check. The `/` row and the `..` row are the standing proof of that argument."
    - "Both new modules are consumed by nothing in production until Phase 111, and `fallow dead-code` is nonetheless clean, because `.fallowrc.json` sets `production: false` and the owner test importing the export is the consumer."
    - "The checkout is path-scoped. Phase 109's five inverted files — `domain/resolver.ts`, `domain/components/plugin.ts`, `shared/notify.ts`, `shared/notify-reasons.ts`, `shared/probe-classifiers.ts` — are untouched by anything this plan does."
  prohibitions:
    - "No tree-wide `git checkout features/workflow-port-wip -- extensions/`. The port branch predates the kind inversion and carries pre-109 copies of five files; a tree-wide checkout reverts Phase 109 with no conflict marker to warn anyone."
    - "No test-only export of any kind is reintroduced or left in place — no module-global relocation setter, no double-underscore test re-export."
    - "No `EXTENSION_VERSION` bump. A-03 prohibits it for the whole 109-111 window; the bump is Phase 111's obligation."
    - "No dead-code suppression marker is added to silence an export that has no production consumer yet. The owner test importing the symbol is the sanctioned fix."
    - "No rewrite of the ported module bodies. The only production edit this plan authors is the seam deletion and the header sentence that defended it."
    - "No hardening of the project-key sha256. It is a namespacing device that must match the engine byte-for-byte, so it takes no salt and no HMAC."
    - "No `--no-verify`, and no `--amend` after a failed hook run."
    - "No content from `bridges/workflows/*`, `persistence/locations.ts` or `shared/errors-bridges.ts` is pulled forward from the port branch. Those are Phases 111-112."
  artifacts:
    - path: "extensions/pi-claude-marketplace/platform/workflow-home.ts"
      provides: "`workflowHomeDir()` — the sole import site of the host engine's storage root, seam-free"
      contains: "workflowHomeDir"
    - path: "tests/platform/workflow-home.test.ts"
      provides: "the WPTH-02 and criterion-4 owner test; relocates the root through `process.env.HOME`"
      contains: "WPTH-02"
    - path: "extensions/pi-claude-marketplace/domain/workflow-project-key.ts"
      provides: "`workflowProjectKey(cwd)` — the engine's per-project storage key as `<slug>-<12 hex>`"
      contains: "workflowProjectKey"
    - path: "tests/domain/workflow-project-key.test.ts"
      provides: "the criterion-6 parity table; thirteen transcribed literal keys plus a cwd-pinned relative case"
      contains: "some-project-e4c31526a114"
  key_links:
    - from: "tests/platform/workflow-home.test.ts"
      to: "extensions/pi-claude-marketplace/platform/workflow-home.ts"
      via: "direct relative import of the module under test — the corresponding-test gate parses the import declarations and rejects a module reached only through a barrel"
      pattern: "workflowHomeDir"
    - from: "tests/domain/workflow-project-key.test.ts"
      to: "extensions/pi-claude-marketplace/domain/workflow-project-key.ts"
      via: "direct relative import; the owner test is also the only consumer keeping the export off the dead-code report"
      pattern: "workflowProjectKey"
    - from: "tests/platform/workflow-home.test.ts"
      to: "process.env.HOME"
      via: "the relocation seam that replaces the deleted setter; `os.homedir()` re-reads the variable on every call and caches nothing"
      pattern: "process.env.HOME"
---

<!-- planner-discipline-allow: setWorkflowHomeDirForTesting -->
<!-- planner-discipline-allow: ForTesting -->
<!-- planner-discipline-allow: __test_ -->

<objective>
Land the two independent leaf modules — the engine storage root and the engine project key — with
their owner tests, with the one test-only seam deleted rather than relocated, and with the
path-scoped checkout proved not to have touched Phase 109.

Purpose: this plan is the phase's tracer. Every mechanism the remaining two plans repeat — a
path-scoped checkout with a blast-radius proof, an owner test that is simultaneously the module's
only consumer, a 100% direct-coverage pair, and a green whole-tree gate at the commit boundary —
runs end to end here on the smallest module in the phase. If any of those mechanisms is wrong, it is
wrong after one commit rather than after four.

Output: two ported production modules, two new owner test files, two green commits.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/110-domain-and-platform-modules/110-CONTEXT.md
@.planning/workstreams/workflows/phases/110-domain-and-platform-modules/110-RESEARCH.md
@.planning/workstreams/workflows/phases/110-domain-and-platform-modules/110-PATTERNS.md
@.planning/workstreams/workflows/port/README.md
@CLAUDE.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The worktree `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`.
`node_modules` here is a REAL directory, not a symlink.

`.git` here is a FILE, not a directory, so the `trufflehog` pre-commit hook aborts structurally with
`failed to read index file`. Per `CLAUDE.md` §Git: run `pre-commit run --files <changed paths>`,
then confirm cleanliness with a filesystem-mode trufflehog scan over the same paths
(`--results=verified,unknown --fail`), then commit with the `SKIP=trufflehog` prefix — and `SKIP=`
names that hook only. Never `--no-verify`. A failed hook means the commit did NOT happen, so
re-stage and re-run rather than amending.

No pre-commit hook is installed in this checkout, so `pre-commit` must be invoked by hand.
</working_directory>

<why_this_is_not_tdd>
The three production modules are a verbatim port from `features/workflow-port-wip`. Writing a
failing test before code that already exists on another branch is not available, so the tracer does
not open red on behavior. It DOES open red on the one thing this plan actually authors: the seam
deletion. Task 1 writes the criterion-4 source assertion first and observes it fail against the
ported module before the setter is removed. That red is the tracer's red, and it is recorded.
</why_this_is_not_tdd>

<flagged_planner_assumptions>
One probe row came back `unclassified` and is surfaced here rather than auto-resolved. It is NOT a
verification criterion; it is an assumption a human should confirm.

1. **WPTH-02 (unclassified).** WPTH-02 reads: *"The deprecated `<cwd>/.pi/workflows/saved/` legacy
   project path is never written."* The deterministic edge probe could not classify an edge for it,
   because the requirement states a whole-system negative and this phase writes nothing at all.

   **The assumption this plan is written under:** Phase 110 can only prove the *negative property*
   that `platform/workflow-home.ts` — the sole import site of the engine storage root — derives that
   root from `os.homedir()` alone, reads no `cwd`, and reads no environment override. The actual
   "never written" guarantee is a Phase 111 property of `persistence/locations.ts` and
   `bridges/workflows/stage.ts`, which are the modules that write.

   The owner test therefore pins the strongest fact available here: with `HOME` relocated to one
   temp directory and the process cwd pointed at a *different* temp directory, `workflowHomeDir()`
   returns a path under the relocated home and shares no prefix with the cwd. That makes the
   home-derivation observable rather than incidentally right.

   If WPTH-02 was meant to close inside Phase 110, it does NOT, and the requirement should be
   re-scoped or split rather than marked satisfied. Record the carry-forward in the SUMMARY.
</flagged_planner_assumptions>

<artifacts_this_phase_produces>
Every symbol Phase 110 creates, across all three plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly-created symbols from drift verification.

| Change | Symbol | File | Owner plan |
|--------|--------|------|-----------|
| ADD | `workflowHomeDir()` | `platform/workflow-home.ts` (new file) | **110-01** |
| ADD | `workflowProjectKey(cwd)` | `domain/workflow-project-key.ts` (new file) | **110-01** |
| ADD | `tests/platform/workflow-home.test.ts` (new file) | — | **110-01** |
| ADD | `tests/domain/workflow-project-key.test.ts` (new file) | — | **110-01** |
| REMOVE | the module-global relocation setter and the header sentence defending it | `platform/workflow-home.ts` | **110-01** |
| ADD | `generatedWorkflowName(plugin, source)` | `domain/name.ts` | 110-02 |
| ADD | `assertSafeSavedWorkflowName(name)` (module-private) | `domain/name.ts` | 110-02 |
| ADD | `WorkflowNameCollision` (interface) | `shared/errors.ts` | 110-02 |
| ADD | `WorkflowNameCollisionError` (class) | `shared/errors.ts` | 110-02 |
| EXTEND | `describe("generatedWorkflowName")` | `tests/domain/name.test.ts` | 110-02 |
| EXTEND | `describe("WorkflowNameCollisionError")` | `tests/shared/errors.test.ts` | 110-02 |
| ADD | `admitWorkflowScript(pluginName, fileName, source)` | `domain/workflow-script.ts` (new file) | 110-03 |
| ADD | `assertNoWorkflowNameCollisions(verdicts)` | `domain/workflow-script.ts` | 110-03 |
| ADD | `WORKFLOW_SCRIPT_EXTENSIONS` | `domain/workflow-script.ts` | 110-03 |
| ADD | `fileStem(fileName)` | `domain/workflow-script.ts` | 110-03 |
| ADD | `NamedWorkflow`, `StemFallbackWorkflow`, `SkippedWorkflow`, `RefusedWorkflow`, `WorkflowVerdict`, `AdmittedWorkflow`, `SkippedCause`, `RefusedCause` (types) | `domain/workflow-script.ts` | 110-03 |
| ADD | `tests/domain/workflow-script.test.ts` (new file) | — | 110-03 |
| ADD | `acorn` at `^8.16.0` under `dependencies`, and the lockfile record it rewrites | `package.json`, `package-lock.json` | 110-03 |

**Nothing else is created.** No `bridges/workflows/` (Phase 111), no `persistence/locations.ts`
workflow members (Phase 111), no `WorkflowTargetOccupiedError` in `shared/errors-bridges.ts`
(Phase 111), no `"workflows"` member of the ledger `phase` union (Phase 112), no
`EXTENSION_VERSION` bump (Phase 111, A-03).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host environment -> extension | `$HOME` is read indirectly through `os.homedir()`; a hostile value would relocate every workflow artifact |
| project directory name -> storage path segment | an attacker-influenced directory basename becomes a path segment under the engine's storage root in Phase 111 |
| test process -> host filesystem | `HOME` and the process cwd are both mutated process-wide for the duration of one test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-110-01 | Tampering | `workflowProjectKey` slug becoming a path segment | high | mitigate | The sanitizer collapses every run outside `[a-z0-9._-]` to one dash, strips leading and trailing dashes, slices to 48, and falls back to the literal `project` when empty. `.`, `..` and both separators are therefore unreachable outputs. The `/` row and the `..` row in the parity table are the standing proof, and they must not be dropped. |
| T-110-02 | Tampering | `workflowHomeDir` honouring a relocation input | medium | mitigate | The module reads no environment variable of its own and takes no parameter. The seam deletion removes the only writable relocation input it ever had, so the root is a pure function of `os.homedir()`. |
| T-110-03 | Tampering | `HOME` and cwd mutation leaking out of the owner test | low | mitigate | Save the previous value, register restoration with `t.after()`, and only then mutate — the ordering the unit-testing rule names. Test files are separate OS processes and top-level cases within a file run sequentially, both measured, so no sibling file can observe the mutation. |
| T-110-04 | Information Disclosure | the key leaking the absolute project path | low | accept | The key carries a 12-hex prefix of a `sha256` of the resolved path plus a lowercased basename slug. The basename is already visible to anyone who can list the storage root. Matching the engine byte-for-byte is the requirement; salting would break parity. |
| T-110-05 | Spoofing | two distinct projects colliding onto one storage key | low | accept | 48 bits of hash over the resolved absolute path. This is the engine's own collision posture and cannot be widened without breaking parity. |
| T-110-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package. The one new runtime dependency (`acorn`) lands in 110-03, where 110-RESEARCH §Package Legitimacy Audit records verdict `OK` with no `[ASSUMED]`/`[SUS]` entry. |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a ported leaf module lands green" — the storage root, one path only</name>
  <precondition>Branch `features/workflow-port-wip` exists in this repository and carries `extensions/pi-claude-marketplace/platform/workflow-home.ts`. Verify with `git cat-file -e features/workflow-port-wip:extensions/pi-claude-marketplace/platform/workflow-home.ts` before the checkout; a non-zero exit means the port branch is missing and the phase cannot start.</precondition>
  <files>extensions/pi-claude-marketplace/platform/workflow-home.ts, tests/platform/workflow-home.test.ts</files>
  <read_first>
    - `git show features/workflow-port-wip:extensions/pi-claude-marketplace/platform/workflow-home.ts` — the module as ported, including the header paragraph that defends the seam on concurrency grounds
    - `110-RESEARCH.md` §Q1 — the falsification probe, the three directly-observed facts about the `node:test` process model, and the measured post-deletion coverage
    - `110-RESEARCH.md` §"The seam-free `platform/workflow-home.ts`" and §"The engine's own rooting rule, for the header's citation" — the exact eight-line target shape and the engine source to cite
    - `110-PATTERNS.md` §`tests/platform/workflow-home.test.ts` — the composite idiom: `tests/index.test.ts` ordering with `tests/bridges/hooks/event-router.test.ts` scale
    - `tests/index.test.ts` lines 176-227 — `restoreEnv` plus `createHermeticScope`; the CORRECT ordering (save, register `t.after()`, then mutate) and the `previousCwd` save/restore
    - `tests/bridges/hooks/event-router.test.ts` lines 1-10 and 94-120 — the small-scale idiom and its import block. NOTE: this file mutates `process.env` BEFORE registering `t.after()`. Do NOT copy that ordering; it is the bug the unit-testing rule forbids.
    - `tests/integration/workflow-kind-inversion.test.ts` lines 47-64 — the callback signature that hands the temp home to the body rather than making it re-read the global, and the comment explaining why the `?? ""` fallback the other shape needs is a hazard
    - `scripts/check-corresponding-tests.mjs` — the mirror rule, and the distinction between `missing-test`, `proxy-owned` and `wrong-import`
    - `.claude/rules/typescript-unit-testing.md` §"Pairing and coverage", §"Case structure", §Assertions, §Patterns/Environment
    - `.planning/codebase/CONVENTIONS.md` §"Dependency injection over test-only seams"
  </read_first>
  <action>
This is the tracer slice. It carries the whole phase mechanism on the smallest module, and it is
production-quality work that ships, not a probe.

**Step 1 — observe the criterion-4 red before touching production.** Before the checkout, or
immediately after it and before any edit, run the source assertion the acceptance criteria pin:
filter comment lines out of the ported module and count matches for the test-only export tokens.
It must report a non-zero count while the ported setter is still present. Record that observed
value in the SUMMARY. This is the tracer's red; without it the deletion is unwitnessed.

**Step 2 — path-scoped checkout, one file.** Run `git checkout features/workflow-port-wip --`
naming exactly `extensions/pi-claude-marketplace/platform/workflow-home.ts` and nothing else. Do
NOT name a directory. Do NOT name `extensions/`. The port branch predates the kind inversion and
still carries pre-109 copies of `domain/resolver.ts`, `domain/components/plugin.ts`,
`shared/notify.ts`, `shared/notify-reasons.ts` and `shared/probe-classifiers.ts`; a directory-scoped
checkout would silently revert Phase 109's whole deliverable with no conflict marker. The checkout
both writes and stages the path, which is expected.

Immediately after the checkout, run the blast-radius check in the acceptance criteria and confirm it
prints `0`.

**Step 3 — delete the seam.** Remove the module-global `override` binding, the exported relocation
setter, and the `override ??` operand from `workflowHomeDir`'s return expression. The function
becomes a single `return path.join(os.homedir(), ".pi", "workflows");`. Keep the `node:os` and
`node:path` imports; both are still used.

**Step 4 — rewrite the header sentence that defended the seam.** The ported header contains a
sentence asserting that a test relocates storage by calling the setter rather than by mutating
process-global environment state that concurrently-running suites also read. That sentence argues
for a mechanism that no longer exists, and it argues from a premise measured false: under
`node --test`, test files are separate OS processes and top-level cases within one file run
sequentially, and `os.homedir()` re-reads the environment on every call and caches nothing. Replace
it with a sentence stating what is now true — that the root is a pure function of `os.homedir()`,
that relocation for a test is done by setting `HOME` with `t.after()` restoration, and that the
module holds no mutable module-level state. Leaving a comment that argues for a seam you just
deleted is worse than no comment.

Keep everything else in the header verbatim: the WPTH-04 paragraph explaining why the module reads
no environment override of its own, the sole-import-site claim mirroring `getAgentDir`'s position in
`pi-api.ts`, and the note that no Pi package is imported so the peer-import chokepoint is not
engaged. Cite the engine's rooting rule at `@quintinshaw/pi-dynamic-workflows@3.10.1
dist/workflow-paths.js` — `WORKFLOW_HOME_RELATIVE_DIR = ".pi/workflows"` joined onto `homedir()`.
Comments cite requirement and decision ids only; no planning-artifact tokens.

**Step 5 — write `tests/platform/workflow-home.test.ts`.** The module has ONE exported entrypoint,
so the unit-testing rule says cases stay at the top level with no `describe()` wrapper.

Declare a small local `hermeticHome(t)` helper following `110-RESEARCH.md` §Pattern 1: `mkdtemp`
under `tmpdir()` with a prefix naming this file, save `process.env.HOME`, register a `t.after()`
that restores or deletes the saved value and then `rm`s the temp directory, and only then assign
`process.env.HOME`. Return the temp home so the caller never re-reads the global. The restore branch
is an `if`/`else` on `undefined`, and the required blank line after that block before the `rm` is
what `@stylistic/padding-line-between-statements` enforces. `tests/helpers/` does not exist on this
branch, so declare the helper locally — that is the established pattern, not a smell.

Write these cases:

- **The happy path.** With `HOME` relocated, `workflowHomeDir()` strictly equals
  `path.join(<the temp home>, ".pi", "workflows")`. Compare against a value the test composes from
  the temp home it was handed, not against a second call to the function.
- **WPTH-02, the home-derivation negative.** Relocate `HOME` to one temp directory AND point the
  process cwd at a *different* temp directory, saving `process.cwd()` and restoring it first inside
  the same `t.after()`, exactly as `tests/index.test.ts` does. Then assert the returned root starts
  with the temp home, and assert it strictly does not equal
  `path.join(<the temp cwd>, ".pi", "workflows", "saved")` and does not start with the temp cwd at
  all. Two distinct roots are what make the home-derivation observable instead of incidentally
  right; with one directory serving as both, the assertion passes for a cwd-derived implementation
  too. Title the case as the WPTH-02 behaviour and cite `WPTH-02` in the title.
- **The re-read case.** Two `workflowHomeDir()` calls, with `HOME` reassigned to a second temp
  directory between them, return two different roots. This is the case that closes the module's
  remaining branch and proves no value is cached — which is exactly what makes `HOME` a valid
  replacement for the deleted setter.

Assertions are `strictEqual` / `deepStrictEqual` / `ok` only; no `assert.equal`. Every case carries
`// arrange`, `// act`, `// assert` markers. Import the module by a direct relative specifier with
an explicit `.ts` extension — the corresponding-test gate parses the import declarations and rejects
a module reached through a barrel with the distinct verdict `proxy-owned`. Import the ONE runtime
export by name. Import order is builtin then external then internal, blank line between groups,
alphabetized, type-only members last inside the same braces (`TestContext` from `node:test` rides
inline).

**Step 6 — gate and commit.** Run the sampling commands, then the coverage pair, then the whole
chain. Then `pre-commit run --files` over both changed paths, fix and re-stage until clean, run the
filesystem-mode trufflehog scan, and commit both paths together with a Conventional Commits subject
of at most 72 characters that mentions no milestone or phase. Stage explicit paths; never `-A` and
never `-u .`.
  </action>
  <reversibility rating="reversible">The seam deletion is a five-line removal in a new-to-this-branch file; `git checkout features/workflow-port-wip -- <path>` restores the ported shape verbatim at any time.</reversibility>
  <verify>
    <automated>git cat-file -e features/workflow-port-wip:extensions/pi-claude-marketplace/platform/workflow-home.ts</automated>
    <fails_when>non-zero exit — the port branch or the file within it is missing, and no checkout in this phase can proceed</fails_when>
    <automated>git status --porcelain -- extensions/pi-claude-marketplace/domain/resolver.ts extensions/pi-claude-marketplace/domain/components/plugin.ts extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts extensions/pi-claude-marketplace/shared/probe-classifiers.ts | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — a non-zero count means the checkout reached one of Phase 109's five inverted files and the inversion has been silently reverted; discard those paths with `git checkout HEAD -- &lt;path&gt;` before doing anything else</fails_when>
    <automated>grep -v '^\s*[/*]' extensions/pi-claude-marketplace/platform/workflow-home.ts | grep -cE 'ForTesting|__test_|let override'</automated>
    <fails_when>the printed count is anything other than `0` after the deletion. Before the deletion the same command MUST print a non-zero count — that is the tracer's recorded red, and a `0` there means the assertion is scanning the wrong file. The non-zero exit status of a `grep -c` that found nothing is expected and is not itself the signal.</fails_when>
    <automated>node --test tests/platform/workflow-home.test.ts</automated>
    <fails_when>non-zero exit, or the summary block reports a nonzero `fail` count, or `tests` reports `0` — a zero-test run means the file was written but no `test(...)` ever registered</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/platform/workflow-home.ts</automated>
    <fails_when>non-zero exit, or the reported pair is short of `hit === found` on any of branches, functions or lines. The measured target for the seam-free module is `FN 1/1 · BR 2/2 · LN 8/8`; anything less becomes an accepted-shortfall entry this phase is not permitted to create.</fails_when>
    <automated>npm run test:corresponding && npm run test:corresponding:negative</automated>
    <fails_when>non-zero exit. `missing-test` means the owner test is at the wrong mirrored path; `proxy-owned` or `wrong-import` means it reaches the module through a re-export instead of a direct relative import; `unexpected-test` means a second test file was created for one module.</fails_when>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit. An `Unused exports` section naming `workflowHomeDir` means the owner test does not import it by name; an `Unlisted dependencies` or `Unused dependencies` section here means something outside this plan's scope changed `package.json`. Judge by the exit code: `fallow health` prints a cross glyph beside a zero-above-threshold line and still exits zero.</fails_when>
    <automated>npm run format:check &amp;&amp; npm test</automated>
    <fails_when>non-zero exit, or the unit-suite summary reports a nonzero `fail` count — a sibling test failing here means the `HOME` or cwd mutation leaked outside its own case</fails_when>
    <automated>git show --pretty=format: --name-only HEAD | sed '/^$/d' | sort</automated>
    <fails_when>the printed list is anything other than exactly the two lines `extensions/pi-claude-marketplace/platform/workflow-home.ts` and `tests/platform/workflow-home.test.ts` — any third path means the commit's blast radius exceeded the plan</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch extensions/pi-claude-marketplace/platform/workflow-home.ts tests/platform/workflow-home.test.ts` exits 0 after staging — both files are tracked at the intended paths
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/platform/workflow-home.ts | grep -cE 'ForTesting|__test_|let override'` prints `0`
    - `grep -cE '^export ' extensions/pi-claude-marketplace/platform/workflow-home.ts` prints `1` — exactly one export survives
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/platform/workflow-home.ts | grep -cE 'process\.env|PI_CODING_AGENT_DIR'` prints `0` — the module reads no environment variable directly, which is the WPTH-02 negative property
    - the module header no longer contains the sentence asserting that a test relocates storage by calling a setter rather than by mutating process-global environment state, and the replacement sentence states the `HOME` relocation and the absence of module-level mutable state
    - `grep -c 'WPTH-02' tests/platform/workflow-home.test.ts` prints at least `1`
    - `grep -c 'process\.env\.HOME' tests/platform/workflow-home.test.ts` prints at least `3` — the save, the assignment, and the restore
    - `grep -c 't\.after(' tests/platform/workflow-home.test.ts` prints at least `1`, and in the source order of every case the `t.after(` registration appears BEFORE the first `process.env.HOME =` assignment of that case
    - `grep -c 'process\.chdir(' tests/platform/workflow-home.test.ts` prints at least `2` — the relocation and the restore inside `t.after()`
    - `grep -c 'assert\.equal(' tests/platform/workflow-home.test.ts` prints `0` — strict variants only
    - the file contains all three of the `// arrange`, `// act` and `// assert` markers
    - `grep -cE 'describe\(' tests/platform/workflow-home.test.ts` prints `0` — a single-entrypoint module keeps its cases at the top level
    - no comment or test title contains a `Phase`, `Plan`, `Wave` or `Pitfall` planning token
    - the commit subject follows Conventional Commits, is between 5 and 72 characters, and names no milestone or phase
  </acceptance_criteria>
  <done>`workflowHomeDir()` is on this branch as a pure function of `os.homedir()` with no relocation seam of any kind; its owner test relocates the root through `HOME` with restoration registered before mutation, pins the WPTH-02 home-derivation negative against a distinct cwd, and reaches 100% direct coverage; Phase 109's five inverted files are untouched; and the commit contains exactly the two intended paths.</done>
</task>

<task type="auto">
  <name>Task 2: Land the project key with a mutation-sensitive parity table of transcribed literals</name>
  <precondition>Task 1 is committed and `npm run check`'s fast members are green from that commit, so any red observed in this task is attributable to this task's own changes.</precondition>
  <files>extensions/pi-claude-marketplace/domain/workflow-project-key.ts, tests/domain/workflow-project-key.test.ts</files>
  <read_first>
    - `git show features/workflow-port-wip:extensions/pi-claude-marketplace/domain/workflow-project-key.ts` — the module as ported, including the transcribed engine source in its header and the two load-bearing orderings it names
    - `110-RESEARCH.md` §Q6a — the engine's real derivation at 3.10.1, the thirteen re-derived literal rows with what each discriminates, and the relative-path trap with both dispositions
    - `110-RESEARCH.md` §Pitfall 3 and §Pitfall 4 — why a hard-coded key for the relative case is machine-specific, and why inverting the dash strip and the 48-char slice passes every simple case
    - `110-RESEARCH.md` §Pattern 2 — the measured-literal parity table, and why the long inputs are built with `repeat()` rather than pasted
    - `110-PATTERNS.md` §`tests/domain/workflow-project-key.test.ts` — the exact analog
    - `tests/domain/clone-key.test.ts` lines 1-45 — the structural twin: same 12-hex derived key, literal-only expectations, and the adjacent-input pair asserted as two literals in ONE `deepStrictEqual` object
    - `tests/domain/source.test.ts` lines 15-33 — the named-interface case-table shape to use instead of an anonymous inline array
    - `tests/index.test.ts` lines 198-226 — the `previousCwd` save and the restore-first ordering inside `t.after()`
    - `.planning/spikes/025-canonical-path-mechanics/keyparity.mjs` — the 14-case array and the normalization-collision check
    - `git show features/workflows-spike:tests/domain/workflow-project-key.test.ts` — the spike's 92-line draft; treat it as a proven case set, not as finished structure
  </read_first>
  <action>
**Step 1 — path-scoped checkout, one file.** `git checkout features/workflow-port-wip --` naming
exactly `extensions/pi-claude-marketplace/domain/workflow-project-key.ts`. Same discipline as Task
1: never a directory, never `extensions/`. Re-run the Phase-109 blast-radius check immediately
after and confirm `0`.

Do not edit the module. Its header transcribes the engine's source so a future reader can diff it
against an upgraded release, and it states that the hash is a namespacing device rather than a
security boundary — it takes no salt and no HMAC deliberately, because it must match the engine
byte-for-byte. Do not "harden" it.

**Step 2 — write `tests/domain/workflow-project-key.test.ts`.** One exported entrypoint, so cases
stay at the top level with no `describe()` wrapper.

Declare a named case interface in the shape of `tests/domain/source.test.ts` — a `readonly name`
describing what the row discriminates, a `readonly cwd`, and a `readonly expectedKey` — and a
`readonly KEY_CASES: readonly KeyCase[]` module-level constant. A module-level stateless data table
is permitted; mutable shared setup is not.

The thirteen rows, with every expected key transcribed as a literal. None is recomputed by the test.

1. `/home/acolomba/some-project` -> `some-project-e4c31526a114` — baseline and hash width
2. `/home/acolomba/UPPER-Case-Name` -> `upper-case-name-68af6a471604` — the lowercasing
3. `/home/acolomba/name with spaces & symbols!` -> `name-with-spaces-symbols-0dd0a90eb124` — run
   collapse to one dash plus the trailing-dash strip
4. `/home/acolomba/проект` -> `project-cd4339b8af82` — non-ASCII sanitizes to the empty-slug fallback
5. `/home/acolomba/日本語プロジェクト` -> `project-3cc7d84ca611` — same slug, different hash, which is
   what proves the hash covers the path and not the slug
6. `/home/acolomba/---leading-and-trailing---` -> `leading-and-trailing-adab370c9067` — the dash strip
7. `/home/acolomba/` joined with `"a".repeat(60)` -> `"a".repeat(48) + "-243d40bfeca2"` — the 48-char
   slice width
8. `/home/acolomba/` joined with `"a".repeat(47) + "-b"` -> `"a".repeat(47) + "--79e41bdb3cef"` — the
   strip-before-slice ordering. This is the ONLY row that catches an inversion of those two steps,
   and the doubled dash is the whole signal. Build both the input and the expectation with
   `repeat()`, never as a pasted wall of characters: a miscount fails as a hash mismatch, which
   reads exactly like upstream drift.
9. `/home/acolomba/.hidden` -> `.hidden-c67d01a972fa` — a dot is inside the allowed class and there
   is no leading-dot strip
10. `/home/acolomba/dots.in.name` -> `dots.in.name-a7042d00116c` — interior dots survive
11. `/` -> `project-8a5edab28263` — the OTHER fallback, the empty-basename one. Keep this row: it is
    half the standing proof that a path separator is an unreachable output.
12. `/home/acolomba/trailing/` -> `trailing-0a1fd85765a9` — `resolve()` drops the trailing separator
13. `/home/acolomba/../acolomba/some-project` -> `some-project-e4c31526a114` — `..` normalization,
    identical to row 1. Keep this row: it is the other half of that proof.

Drive the table with a `for` loop over `KEY_CASES` and one `test()` per row, titled from the row's
`name` field so each title states the behaviour rather than restating the function name. Assert with
`strictEqual` against the row's literal.

Add one explicit normalization-collision case outside the table, mirroring
`tests/domain/clone-key.test.ts`'s adjacent-input shape: call the function for the canonical
spelling and for the `..` spelling, collect both into one object, and assert that object against a
single `deepStrictEqual` literal in which both values are the same expected key. Asserting the two
against each other would be an equivalence, not a pin.

**Step 3 — the relative-path case, with the cwd pinned.** A bare literal for a relative input is
machine-specific: `resolve()` runs against the process cwd, so the Spike 025 record and a run from a
different directory produce different hashes for the same input. Take the deterministic route rather
than the split pin, because the split pin computes half its expectation with production code, which
the assertion rule discourages.

In its own top-level case: save `process.cwd()`, register a `t.after()` that restores it, then
`process.chdir("/")`. Call the function with the relative spelling `home/acolomba/some-project` and
assert `strictEqual` against the literal `some-project-e4c31526a114` — the same key row 1 carries,
because `resolve("/", "home/acolomba/some-project")` is row 1's absolute path. That single
assertion pins the hash width, proves `resolve()` runs before `basename()`, and needs no
machine-specific value.

Add a short comment on that case explaining why the cwd is pinned, so a later reader does not
"fix" the `chdir` away. `tests/index.test.ts` already calls `process.chdir()` in a unit test, and
top-level cases within one file do not interleave, so the precedent and the safety are both
established. Cite no planning artifact in the comment.

**Step 4 — gate and commit.** Same ritual as Task 1: the sampling commands, the coverage pair, the
chain, `pre-commit run --files` over both paths until clean, the filesystem-mode trufflehog scan,
then a Conventional Commits commit of both paths.
  </action>
  <reversibility rating="reversible">A ported module plus a new test file, both removable with a path-scoped `git revert` of the single commit; nothing outside this phase consumes either symbol yet.</reversibility>
  <verify>
    <automated>git status --porcelain -- extensions/pi-claude-marketplace/domain/resolver.ts extensions/pi-claude-marketplace/domain/components/plugin.ts extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts extensions/pi-claude-marketplace/shared/probe-classifiers.ts | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — the checkout reached a Phase 109 file</fails_when>
    <automated>node --test tests/domain/workflow-project-key.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count, or `tests` reports fewer than `15` — thirteen table rows plus the normalization pair plus the cwd-pinned relative case</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/workflow-project-key.ts</automated>
    <fails_when>non-zero exit, or the reported pair is short of `hit === found` on branches, functions or lines. The measured target is `FN 2/2 · BR 5/5 · LN 71/71`.</fails_when>
    <automated>grep -c 'e4c31526a114' tests/domain/workflow-project-key.test.ts</automated>
    <fails_when>the printed count is fewer than `3` — the baseline row, the normalization pair's expectation, and the cwd-pinned relative case must all carry the same transcribed literal; fewer means one of them is recomputing its expectation instead of pinning it</fails_when>
    <automated>npm run test:corresponding &amp;&amp; npm run test:corresponding:negative</automated>
    <fails_when>non-zero exit — see Task 1's verdict glossary</fails_when>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>npm run fallow &amp;&amp; npm run format:check &amp;&amp; npm test</automated>
    <fails_when>non-zero exit, or the unit-suite summary reports a nonzero `fail` count. An `Unused exports` section naming `workflowProjectKey` means the owner test does not import it by name. A sibling test failing here means the `chdir` leaked outside its own case.</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or the output contains any line matching `error` — ESLint prints nothing on a clean run. This pass is type-aware and slow; give it an extended budget rather than reading a timeout as a failure.</fails_when>
    <automated>git show --pretty=format: --name-only HEAD | sed '/^$/d' | sort</automated>
    <fails_when>the printed list is anything other than exactly the two lines `extensions/pi-claude-marketplace/domain/workflow-project-key.ts` and `tests/domain/workflow-project-key.test.ts`</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~1 HEAD -- extensions/pi-claude-marketplace/domain/workflow-project-key.ts | grep -c '^-[^-]'` prints `0` — the ported module body was added, never edited
    - `grep -c 'a".repeat(47)' tests/domain/workflow-project-key.test.ts` prints at least `2` — the strip-before-slice input and its doubled-dash expectation are both built with `repeat()`
    - `grep -c '\-\-79e41bdb3cef' tests/domain/workflow-project-key.test.ts` prints at least `1` — the doubled dash survives into the expectation
    - `grep -c 'project-8a5edab28263' tests/domain/workflow-project-key.test.ts` prints at least `1` and `grep -c 'project-cd4339b8af82'` prints at least `1` — both empty-slug fallback rows are present
    - `grep -c 'process\.chdir(' tests/domain/workflow-project-key.test.ts` prints at least `2` — the relocation and the restore
    - `grep -c 'workflowProjectKey(' tests/domain/workflow-project-key.test.ts` counts only call sites inside `// act` phases; no expected value anywhere in the file is produced by calling the function under test
    - `grep -c 'assert\.equal(' tests/domain/workflow-project-key.test.ts` prints `0`
    - `grep -cE 'describe\(' tests/domain/workflow-project-key.test.ts` prints `0` — single entrypoint, cases at the top level
    - the file contains all three of the `// arrange`, `// act` and `// assert` markers
    - the case table is a named `readonly` interface plus a `readonly` module-level array, not an anonymous inline literal
    - no comment or test title contains a `Phase`, `Plan`, `Wave` or `Pitfall` planning token
  </acceptance_criteria>
  <done>`workflowProjectKey` is on this branch unedited, its owner test pins thirteen transcribed literal keys plus a normalization pair plus a cwd-pinned relative case, the pair reaches 100% direct coverage, and changing the 12-hex hash width would fail every literal row at once.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` reports zero errors, `npm run fallow` exits zero with no Unused or Dependencies
  section, `npm run format:check` and `npm run lint` are clean, both corresponding-test gates pass,
  and `npm test` is green.
- Both new pairs report `hit === found` on branches, functions and lines under
  `npm run test:coverage:direct`.
- `git status --porcelain` over Phase 109's five inverted files prints nothing at every commit
  boundary in this plan.
- Each of the two commits contains exactly its own two paths and nothing else.
</verification>

<success_criteria>
- `platform/workflow-home.ts` and `domain/workflow-project-key.ts` are present on `features/workflow`
  with mirrored owner tests at `tests/platform/workflow-home.test.ts` and
  `tests/domain/workflow-project-key.test.ts`.
- No test-only export exists anywhere in either module (ROADMAP criterion 4, for the phase's only
  target).
- The project-key parity table is mutation-sensitive to the hash width by construction (ROADMAP
  criterion 6).
- WPTH-02's provable half is pinned: the storage root is home-derived, reads no cwd, and reads no
  environment override.
- Phase 109 is intact.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/110-domain-and-platform-modules/110-01-SUMMARY.md`
when done. Record in it: the observed pre-deletion count from the tracer's red step; the exact
header sentence that replaced the seam's defence; the WPTH-02 carry-forward from
`<flagged_planner_assumptions>`; and the note that Phase 111 recovers the spike's deliberate
storage-root-versus-HOME split by giving the project cwd a second temp directory distinct from the
temp `HOME`, since `HOME`-only relocation makes those two paths coincide.
</output>
