---
phase: 112-install-and-removal-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/persistence/state-io.ts
  - extensions/pi-claude-marketplace/persistence/migrate.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - .fallowrc.json
  - tests/persistence/state-io.test.ts
  - tests/persistence/migrate.test.ts
  - tests/persistence/migrate-config.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/plugin/list.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/reconcile/backfill.test.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/orchestrators/marketplace/shared.test.ts
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/types.test.ts
  - tests/transaction/phase-ledger.test.ts
autonomous: true
requirements: [WLIF-01, WLIF-02]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WLIF-01: installing a workflow-bearing plugin writes one envelope per admitted workflow into `workflowsSavedDir` and the persisted install record's `resources.workflows` names exactly those envelopes. The record is the only inventory that survives the process -- the envelopes sit outside every scope root and nothing on disk can be enumerated to rediscover them."
    - "WLIF-01: a `statePhase` failure after the workflows phase committed leaves NO envelope at any target path and NO staging tree under `workflowsStagingDir`. This is asserted as a filesystem fact after the failed install, not as a rejected promise."
    - "The workflows phase is the LAST bridge phase in the ledger array, so the only phase that can fail after it is `statePhase`. Its undo must therefore be driven by a `statePhase` failure, not a sibling-bridge failure -- the vehicle three existing disk-state undo tests use is unavailable to it."
    - "A plugin declaring no `workflows/` directory, or one whose directory admits nothing, runs the phase as a no-op: nothing staged, nothing recorded, and the undo removes nothing without erroring."
    - "The undo removes only names this phase actually placed, in a way that does not depend on removal order; a partial removal leaves the remainder still removable rather than aborting the unwind."
    - "WLIF-01: the removal payload is the array `onPlaced` reported, never `prep.result.stagedNames` and never a set derived from the thrown error's class. A pre-existing envelope at a colliding name that this commit did not place is still on disk, byte-unchanged, after the undo."
    - "WLIF-01: an undo whose `unstagePluginWorkflows` returns a non-empty `failed[]` throws `WorkflowsUnstageFailureError`, which the ledger records as a `RollbackPartial` and the renderer emits as a `[workflows] (rollback failed)` child row. A silent swallow would report a clean rollback while executable envelopes stayed on disk."
    - "WLIF-01 / PI-14: a `PathContainmentError` raised from the workflows undo propagates VERBATIM out of `runPhases`. It bypasses the `capture.rollbackPartials` / `capture.version` assignment, so the failure row carries no version and NO rollback-partial marker. That is the documented PI-14 trade, and a test pins it so a later reader does not `fix` it."
    - "Widening the ledger `phase` union does not change any existing arm's behavior: a failure in one of the five existing phases still reports under its own name."
    - "The union member is the literal string `workflows`, matching the bridge kind spelling used in `componentPaths`, the supported tuples and `resources`. A near-miss spelling compiles at the type site and diverges at every string comparison."
    - "WLIF-02: `Phase3Failure.phase`, `UpdatePhaseBridge` and the `PHASE3_FAILURE_PHASES` runtime tuple are three mirrored closed sets and all three carry the new member. Widening only the type leaves three `tsc` errors in `update.ts`; widening all three leaves exactly one, in the `tests/orchestrators/types.test.ts` exhaustiveness pin."
    - "`update.ts` gains the widened tuple and NO behavior change. It cannot yet produce a workflows failure; the widening is what lets the update re-stage land later without a second type commit."
    - "The `resources.workflows` member is REQUIRED, not optional, and every construction site of a plugin install record is compile-forced to answer for it. `clonePluginRecord` enumerates rather than spreads precisely so an omission is a compile error."
    - "A legacy `state.json` record written before this field existed is default-filled to `[]` by the migration before `STATE_VALIDATOR.Check` runs, exactly as `agents`, `mcpServers` and `hooks` are. No `schemaVersion` bump: the `hooks` precedent added a required resources member with a migrate fill and no bump."
    - "The `orchestrators` zone's `allow` array carries `bridges-workflows` in the SAME commit as the first orchestrator import of the bridge. Measured: without it `fallow dead-code` exits 1 naming the exact file, line and edge."
    - "`install.ts` holds at 100% direct coverage on branches, functions and lines. Every branch the sixth phase introduces is exercised by `install.ts`'s own owner test, not by an integration test."
  prohibitions:
    - "No transplant from `features/workflows-spike`. The spike predates the rewrite of `install.ts` and `reinstall.ts`; it is read for intent and every line is written against the current files."
    - "No `fallow-ignore` marker of any kind, and no ESLint disable directive added to keep a new symbol or branch off a report."
    - "No accepted coverage shortfall. Every file this plan touches is at 100% direct coverage today and none is on the accepted-shortfall list."
    - "No re-folding of `PathContainmentError` into `failed[]`. The bridge raises that class and both ledger sites re-throw it by class; softening it into a soft row returns a clean undo and destroys the bypass."
    - "No widening of `InstallLedgerSummary`. A workflows member there would be a field on a public projection that no consumer reads."
    - "No new `REASONS` closed-set member and no `docs/output-catalog.md` row amendment. The rollback-partial phase label is free-form and the unclassified cascade-failure default is truthful."
    - "No test-only export seam. No `__test_*` symbol, no module-global the test reaches into; the sabotage is a planted filesystem condition."
    - "No `schemaVersion` bump on the state file."
    - "No `--no-verify`, and no `--amend` after a failed hook run."
  artifacts:
    - path: "extensions/pi-claude-marketplace/persistence/state-io.ts"
      provides: "the required `resources.workflows` schema member and its `clonePluginRecord` enumeration"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/persistence/migrate.ts"
      provides: "the four-member default-fill list that repairs a legacy record before validation"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "`workflowsPrep` / `stagedWorkflowNames` on `InstallCtx`, the `workflowsPhase` sixth bridge slot, and the `statePhase` record axis"
      contains: "workflowsPhase"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      provides: "`WorkflowsUnstageFailureError`, the typed per-name removal failure"
      contains: "WorkflowsUnstageFailureError"
    - path: ".fallowrc.json"
      provides: "the `orchestrators` -> `bridges-workflows` allow-list edge"
      contains: "bridges-workflows"
    - path: "extensions/pi-claude-marketplace/shared/errors.ts"
      provides: "the widened `Phase3Failure.phase` closed set"
      contains: "workflows"
    - path: "tests/orchestrators/plugin/install.test.ts"
      provides: "the seeder `workflows` arm and the ledger happy-path plus undo disk-state cases"
      contains: "workflowsSavedDir"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/bridges/workflows/index.ts"
      via: "`prepareStageWorkflows` / `commitPreparedWorkflows` / `unstagePluginWorkflows` -- the first orchestrator import of the bridge, which is why the fallow allow-list string rides this commit"
      pattern: "bridges/workflows/index.ts"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/persistence/state-io.ts"
      via: "`statePhase` composes `resources.workflows` from `c.stagedWorkflowNames`; the schema member is what makes that composition type-check"
      pattern: "stagedWorkflowNames"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/transaction/phase-ledger.ts"
      via: "a throwing `undo` becomes a `RollbackPartial`; a `PathContainmentError` from the same `undo` is re-thrown by class instead"
      pattern: "runPhases"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts"
      via: "`WorkflowsUnstageFailureError` is imported from the one marketplace module a plugin ledger is allowed to reach"
      pattern: "WorkflowsUnstageFailureError"
---

<objective>
Give the install record a workflow inventory, then wire the workflows bridge into the install ledger
as its sixth bridge phase, with an undo that removes exactly what the commit placed.

Purpose: this is the phase's tracer. Every removal path in the plans that follow removes *by recorded
name*, so without `resources.workflows` there is nothing for them to read; and the sixth ledger phase
is the only place the envelopes are ever written. Proving both end to end on one plugin -- envelope on
disk, name in the record, both gone after a later-phase failure -- settles the architecture after one
commit instead of after four.

Output: a required `resources.workflows` record member with its migration, a `workflowsPhase` in the
literal ledger array, a typed per-name removal failure, the fallow allow-list edge, the three mirrored
closed sets widened, and the owner tests that hold `install.ts` at 100% direct coverage.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-workflows/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/workflows/STATE.md
@.planning/workstreams/workflows/phases/112-install-and-removal-lifecycle/112-CONTEXT.md
@.planning/workstreams/workflows/phases/112-install-and-removal-lifecycle/112-RESEARCH.md
@.planning/workstreams/workflows/phases/112-install-and-removal-lifecycle/112-PATTERNS.md
@CLAUDE.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
</context>

<working_directory>
The worktree `/home/acolomba/pi-claude-marketplace-workflows` on branch `features/workflow`.
`node_modules` here is a REAL directory. `.planning/config.json` has `"use_worktrees": false`, so
executors run sequentially on this branch; no agent worktree is created.

`.git` here is a FILE, not a directory, so the `trufflehog` pre-commit hook aborts structurally with
`failed to read index file`. Per `CLAUDE.md` §Git: run `pre-commit run --files <changed paths>`, then
confirm cleanliness with a filesystem-mode trufflehog scan over the same paths
(`--results=verified,unknown --fail`; clean is exit 0 with both `verified_secrets` and
`unverified_secrets` at 0), then commit with the `SKIP=trufflehog` prefix -- and `SKIP=` names that
one hook only. Never `--no-verify`. A failed hook means the commit did NOT happen, so re-stage and
re-run rather than amending.

No pre-commit hook is installed in this checkout, so `pre-commit` must be invoked by hand. The
`prettier` hook rewrites files mid-run and the commit still succeeds -- check `git status` after every
commit and fix with a follow-up commit, never an amend. Stage explicit paths; never `-A`, never
`-u .`. Commit subjects follow Conventional Commits, 5-72 characters, and mention no milestone or
phase number.
</working_directory>

<artifacts_this_phase_produces>
Every symbol Phase 112 creates, across all four plans. The plan-review-convergence source-grounding
pass reads this section to exclude newly-created symbols from drift verification.

| Change | Symbol | File | Owner plan |
|--------|--------|------|-----------|
| ADD | `resources.workflows` -- a REQUIRED `Type.Array(Type.String())` member of `PLUGIN_INSTALL_RECORD_SCHEMA` | `persistence/state-io.ts` | **112-01** |
| EDIT | `clonePluginRecord` gains the `workflows` enumeration | `persistence/state-io.ts` | **112-01** |
| EDIT | `ensureOneRecordResources`' default-fill field list gains a fourth member | `persistence/migrate.ts` | **112-01** |
| ADD | `workflowsPrep`, `stagedWorkflowNames` on `InstallCtx` | `orchestrators/plugin/install.ts` | **112-01** |
| ADD | `workflowsPhase` -- the sixth `Phase<InstallCtx>` bridge slot, placed between `mcpPhase` and `statePhase` | `orchestrators/plugin/install.ts` | **112-01** |
| EDIT | `statePhase`'s record composition gains the `workflows` axis | `orchestrators/plugin/install.ts` | **112-01** |
| ADD | `WorkflowsUnstageFailureError` (class; readonly frozen `failedWorkflows`) | `orchestrators/marketplace/shared.ts` | **112-01** |
| ADD | `"bridges-workflows"` in the `orchestrators` zone's `allow` array | `.fallowrc.json` | **112-01** |
| EDIT | `Phase3Failure.phase` gains `"workflows"` | `shared/errors.ts` | **112-01** |
| EDIT | `UpdatePhaseBridge` gains `"workflows"` | `orchestrators/types.ts` | **112-01** |
| EDIT | `PHASE3_FAILURE_PHASES` gains `"workflows"` | `orchestrators/plugin/update.ts` | **112-01** |
| EDIT | `UPDATE_PHASE_BRIDGES` gains a `workflows` member | `tests/orchestrators/types.test.ts` | **112-01** |
| EDIT | `PRODUCTION_PHASE_NAMES` gains `"workflows"` | `tests/transaction/phase-ledger.test.ts` | **112-01** |
| EXTEND | `writePluginComponents` / `seedPathMarketplaceWithPlugin` gain a `workflows` arm | `tests/orchestrators/plugin/install.test.ts` | **112-01** |
| ADD | `dropped.workflows` on `UnstageOutcome`, plus the sixth cascade slot in `cascadeUnstagePlugin` | `orchestrators/marketplace/shared.ts` | 112-02 |
| EDIT | `applyPartialCascadeFold` gains the `workflows` axis | `orchestrators/plugin/shared.ts` | 112-02 |
| EDIT | `cascadePluginsInPlace`'s hand-rolled filter gains a `workflows` line | `orchestrators/marketplace/remove.ts` | 112-02 |
| EXTEND | `seedFullCascade`, `seedMarketplace`, `seedFullPlugin`, `seedGitPlugin`, `seedRealDisabledMarketplace` gain workflows arms | `tests/orchestrators/marketplace/{shared,remove}.test.ts`, `tests/orchestrators/plugin/{uninstall,enable-disable}.test.ts` | 112-02 |
| EDIT | `PreparedHandles` / `PartialPreparedHandles` gain a `workflows` handle | `orchestrators/plugin/reinstall.ts` | 112-03 |
| EDIT | `prepareAllHandles` gains a fifth prepare; `abortPartialHandles` gains the workflows abort | `orchestrators/plugin/reinstall.ts` | 112-03 |
| EDIT | `replaceAll` returns a third member carrying the placed workflow names; both catch sites consume it | `orchestrators/plugin/reinstall.ts` | 112-03 |
| ADD | `unplaceWorkflows` (module-private) | `orchestrators/plugin/reinstall.ts` | 112-03 |
| EDIT | `resourcesFromHandles` gains the `workflows` axis | `orchestrators/plugin/reinstall.ts` | 112-03 |
| EXTEND | `seedMarketplace`, `seedInstalledGitSourcePlugin`, `seedDisabledInstall` gain workflows arms | `tests/orchestrators/plugin/reinstall.test.ts` | 112-03 |
| ADD | `garbageCollectWorkflowsStaging`, `WORKFLOWS_STAGING_MAX_AGE_MS` | `orchestrators/plugin/workflows-staging-gc.ts` (new file) | 112-04 |
| ADD | `tests/orchestrators/plugin/workflows-staging-gc.test.ts` (new file) | — | 112-04 |
| EDIT | sweeper call sites in `collectPostCommitWarnings` and `runPostUninstallCleanup` | `orchestrators/plugin/{install,uninstall}.ts` | 112-04 |
| EDIT | the stale ledger-count wording at nine sites | `install.ts`, `enable-disable.ts`, `marketplace/shared.ts`, `state-io.ts`, `docs/output-catalog.md`, `install.test.ts` | 112-04 |

**Nothing else is created.** No `replacePreparedWorkflows` bridge primitive and no `workflows` arm on
`ReplacementEntry`. No `stagedWorkflowNames` on `InstallLedgerSummary`. No `REASONS` closed-set member
and no `docs/output-catalog.md` row amendment. No `locations.workflowsStagingRoot` accessor. No
`update.ts` behavior change, no `info` / `list` read surface, no soft-dependency marker, no
`docs/workflows-compatibility.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a workflow-bearing plugin installs its envelope, the record names it, and a later-phase failure takes it back" -- one path only</name>
  <precondition>The workflows bridge triplet exists at HEAD. Verify with `grep -c 'unstagePluginWorkflows' extensions/pi-claude-marketplace/bridges/workflows/index.ts` before starting; a count of `0` means the preceding phase's barrel is missing and nothing in this plan can be wired.</precondition>
  <files>extensions/pi-claude-marketplace/persistence/state-io.ts, extensions/pi-claude-marketplace/persistence/migrate.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, .fallowrc.json, tests/persistence/state-io.test.ts, tests/persistence/migrate.test.ts, tests/orchestrators/plugin/install.test.ts, and every remaining test file the typecheck worklist names</files>
  <read_first>
    - `extensions/pi-claude-marketplace/persistence/state-io.ts` lines 100-180 -- `PLUGIN_INSTALL_RECORD_SCHEMA`'s `resources` object at 116-122 and `clonePluginRecord` at 147-180, whose own doc comment (138-141) explains why it enumerates rather than spreads
    - `extensions/pi-claude-marketplace/persistence/migrate.ts` lines 115-145 -- `ensureOneRecordResources` and the doc above it that names the three filled fields verbatim
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 316-380 (`InstallCtx`), 895-925 (the context literal), 1115-1147 (`mcpPhase` -- the analog to copy), 1149-1252 (`statePhase` and the literal phase array), 1253-1271 (the `capture` assignment the containment path bypasses)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 921-960 -- `skillsPhase`, whose comment states the sentinel-before-commit rule verbatim
    - `extensions/pi-claude-marketplace/transaction/phase-ledger.ts` in full (174 lines) -- the `Phase<C>` contract at 26-39, the containment re-throw at 86-103 and 121-134, and the `runPhases` doc at 149-151
    - `extensions/pi-claude-marketplace/bridges/workflows/types.ts` lines 89-200 -- `StageWorkflowsInput`, `PreparedWorkflowsStaging`, `CommitWorkflowsOptions.onPlaced` and its contract comment, `UnstageWorkflowsInput`, `UnstageWorkflowFailure`, `UnstageWorkflowsResult`
    - `extensions/pi-claude-marketplace/bridges/workflows/unstage.ts` in full -- the split between accumulated `failed[]` rows and the raised containment refusal
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` lines 50-65 -- `AgentsUnstageFailureError`, the sibling this plan's error class sits beside, and note it takes a pre-formatted message
    - `extensions/pi-claude-marketplace/shared/errors.ts` around `WorkflowNameCollisionError` -- the freeze-a-defensive-copy constructor this plan's error follows
    - `tests/orchestrators/plugin/install.test.ts` lines 306-321 (`withHermeticHome`), 338-346 (`writePluginComponents`' options bag), 527 (`seedPathMarketplaceWithPlugin`), 3328-3392 (the disk-state undo template), 7385-7444 and 7445-7500 (the two `statePhase` failure vehicles)
    - `tests/integration/workflow-kind-inversion.test.ts` lines 225-235 -- the canonical working fixture body and the envelope's exact JSON shape
    - `extensions/pi-claude-marketplace/domain/workflow-script.ts` lines 34-105 -- the verdict union; only `named` and `stem-fallback` carry a `generatedName` and therefore stage anything
    - `.fallowrc.json` lines 78-125 -- the `orchestrators` zone `allow` array and the already-present `bridges-workflows` zone and its outbound rule
    - `112-RESEARCH.md` §"Research Question 1", §Pitfall "resources.workflows is the hidden prerequisite", §Pitfall "the onPlaced payload vs the prepared names", §"Research Question 5", §"Research Question 6"
    - `112-PATTERNS.md` §1, §4, §5, §"Shared Patterns"
    - `.claude/rules/typescript-unit-testing.md` and `.claude/rules/typescript-comments.md` in full
  </read_first>
  <action>
This task lands in TWO green commits. Neither may leave a compile error or a red gate.

**COMMIT A -- the record inventory.**

Add `workflows: Type.Array(Type.String()),` to `PLUGIN_INSTALL_RECORD_SCHEMA`'s `resources` object as
a REQUIRED member, last, after `hooks`. Required rather than optional: every construction site must be
compile-forced to answer for it, which is the whole reason the sibling axes are required. Do not bump
`schemaVersion` -- the `hooks` axis set the precedent of a required resources member added with a
migrate fill and no bump, and the top-level key-set gate pins `resources` as one key either way.

Extend `clonePluginRecord`'s `resources` block with the matching `[...record.resources.workflows]`
copy. The enumeration is deliberate; a spread would alias the nested arrays.

In `migrate.ts`, extend `ensureOneRecordResources`' `as const` field list from three members to four so
a legacy record missing the key is filled with `[]` BEFORE validation runs, and update the doc comment
above it, which names the filled fields verbatim and is now short by one.

Then close the compile errors. Run the typecheck worklist command in the verify block; it reports
around 68 errors across 30 files, three of them production. Work the list to zero:

- `install.ts` -- add the axis to the `statePhase` record composition. At THIS commit it is
  `workflows: []`, which is truthful because nothing stages workflows yet; Commit B replaces the
  literal with the staged names.
- `reinstall.ts` -- add the axis to `resourcesFromHandles`' returned object, likewise `[]` at this
  commit. Plan 112-03 replaces it with the placed names.
- `state-io.ts` -- the remaining production error is the record literal inside this file.
- Every test file the list names: add the axis to each plugin-install-record literal. These are
  one-line additions and the typecheck output is the authoritative worklist -- re-run it after each
  batch rather than guessing at the set.

Add owner-test cases in `tests/persistence/state-io.test.ts` (a saved-then-loaded record round-trips
its workflow names; a record whose `resources` omits the key fails validation) and in
`tests/persistence/migrate.test.ts` (a legacy record missing the key is filled with an empty array and
the migration reports that it mutated; a record that already carries the key is left alone and the
migration reports no mutation).

Gate, then commit every path together.

**COMMIT B -- the sixth ledger phase.**

Add the one string `"bridges-workflows"` to the `orchestrators` zone's `allow` array in
`.fallowrc.json`. It rides THIS commit because this commit contains the first orchestrator import of
the bridge; without it the boundary sub-gate exits 1 naming the exact edge.

Declare `WorkflowsUnstageFailureError` in `orchestrators/marketplace/shared.ts`, beside
`AgentsUnstageFailureError`. That module is the one marketplace file a plugin ledger is allowed to
import from, and the cascade in plan 112-02 needs the same class. Follow the house error convention:
`extends Error`, `this.name` set to the class name in the constructor, a readonly structured field
carrying `readonly UnstageWorkflowFailure[]`, a pre-formatted message as the first constructor
argument (matching its sibling), and a doc comment citing WLIF-03.

**Copy semantics -- decide and state it.** Freeze a defensive copy: assign
`Object.freeze([...failedWorkflows])`, following the collision error this milestone already shipped
rather than the no-copy sibling next door. The reason is that the payload names EXECUTABLE files left
on disk and the error crosses the ledger boundary into a renderer that reads it long after the bridge
returned; a frozen copy is the only way the field is provably stable at read time. Record the choice
and this reason in a comment on the class, and assert it in the owner test.

In `install.ts`:

- Add `workflowsPrep?: PreparedWorkflowsStaging;` beside the four sibling prep handles and
  `stagedWorkflowNames: readonly string[];` beside the four sibling name arrays on `InstallCtx`, and
  initialize `stagedWorkflowNames: []` in the context literal beside its siblings. The name array is
  required, not optional, so the state phase's composition cannot forget it.
- Define `workflowsPhase` modelled on `mcpPhase`, NOT on `hooksPhase`. `hooksPhase` has no bridge
  staging handle at all, so its undo sentinel is a bare boolean and it teaches nothing about a
  prepare/commit/unstage triplet.
  - `do` reads the previously-recorded names off the pre-existing record --
    `c.stateSnapshot.marketplaces[c.marketplace]?.plugins[c.plugin]?.resources.workflows` -- and
    passes them as `previousWorkflowNames` through a conditional spread, because the field is optional
    and `exactOptionalPropertyTypes` rejects an explicit `undefined`. A fresh install finds no record
    and the bridge's re-stage branch stays inert; the enable path reaches the deliberately-kept
    disabled record, which is what lets the commit displace the plugin's own envelopes aside instead
    of hitting the occupancy refusal.
  - Assign `c.workflowsPrep = prep` and `c.stagedWorkflowNames = []` BEFORE the commit call. That
    assignment is the point after which envelopes can exist on disk and it is the sentinel the undo
    gates on. A `do` that throws inside the commit may have placed envelopes while the name array was
    still empty, which is exactly why the sentinel is the handle and not the array.
  - Pass an `onPlaced` callback whose entire body is `c.stagedWorkflowNames = placedNames`. That single
    assignment cannot throw, which is what keeps the known throwing-callback hazard un-triggered; put
    nothing else in it.
  - Push `prep.result.warnings` onto `bridgeWarnings`, not onto `discoveryWarnings`. The workflows
    bridge's array mixes per-file soft-fails with discovery warnings and is not separable at this
    site, which is the agents-bridge shape rather than the skills-bridge shape. Also push the commit's
    leak string when it is defined.
  - `undo` returns immediately when `c.workflowsPrep === undefined`, calls `unstagePluginWorkflows`
    with `previousWorkflowNames: c.stagedWorkflowNames`, and throws `WorkflowsUnstageFailureError`
    with a `; `-joined `<name>: <reason>` message when `result.failed.length > 0`. Do NOT discard the
    result the way the five sibling phases do: a leftover workflow envelope is executable code outside
    every scope root, so a silent swallow reports a clean rollback over a real leak.
- Insert `workflowsPhase` into the literal phase array between `mcpPhase` and `statePhase`.
  `statePhase` stays last because it is what writes the names the workflows phase produced. Do not
  refactor the literal array into a builder.
- Replace the `statePhase` record axis placeholder from Commit A with `[...c.stagedWorkflowNames]`.

In `tests/orchestrators/plugin/install.test.ts`, extend `writePluginComponents`' options bag with
`workflows?: { sourceName: string; body?: string }[]` and add the matching write loop, writing each
body to `<pluginRoot>/workflows/<sourceName>.js`. Default the body to a NAMED `meta` export -- a
default-export body classifies as skipped and writes zero envelopes, so a test using it passes for the
wrong reason. Every workflows case in this file must assert the envelope EXISTS before any sabotage.

Add these cases, all inside `withHermeticHome` with `locationsFor` called INSIDE the closure (the
helper sets the process HOME, which is what the workflow home derivation reads; calling it outside
points the saved directory at the developer's real home):

- **The happy path.** Install a plugin declaring one workflow. Assert the envelope file at
  `path.join(locations.workflowsSavedDir, "hello:greet.json")` parses to the exact three-key object the
  integration test pins, and that the saved record's `resources.workflows` deep-equals the single
  generated name. Compose the expected path with `path.join`, not with the async artifact-path
  composer -- a forgotten await there yields a leaf named after a promise instead of throwing.
- **The empty case.** A plugin with no workflows directory installs cleanly, records an empty array,
  and leaves the saved directory with no file for that plugin.
- **The re-stage read.** An enable-path install over a kept record whose `resources.workflows` names a
  previous envelope reaches the bridge's re-stage branch. This is the arm that exercises the defined
  side of the conditional spread.
- **The leak arm.** A commit whose returned leak string is defined lands that string on the bridge
  warnings channel.
- **The undo, asserted on disk.** Drive the failure from `statePhase` -- the workflows phase is the
  last bridge slot, so no sibling bridge can fail after it and the vehicle the three existing
  disk-state undo tests use is unavailable. Use the seeded-colliding-record vehicle. After the failed
  install assert, as filesystem facts: the envelope path does not exist; the saved directory holds no
  file naming this plugin; and no directory remains under the staging directory. Assert the state
  record is absent too. Do not settle for a rejected promise.
- **The undo no-op.** A failure BEFORE the workflows phase runs leaves `workflowsPrep` undefined and
  the undo removes nothing without throwing.
- **The failed-removal arm.** Make one placed envelope unremovable (replace the envelope file with a
  directory so the unlink raises rather than succeeding), drive the same `statePhase` failure, and
  assert the install fails with a rollback-partial marker naming the workflows phase.

Run the direct-coverage pair on `install.ts` and close every uncovered branch with a case in this
file. There is no accepted-shortfall precedent for this file.

Gate, then commit every path together.
  </action>
  <reversibility rating="costly">Adding a REQUIRED member to the persisted install-record schema changes what every future `state.json` write emits; a record written after this ships carries the key, and reversing the decision means another migration rather than a revert. The ledger phase itself is reversible.</reversibility>
  <verify>
    <automated>grep -c 'unstagePluginWorkflows' extensions/pi-claude-marketplace/bridges/workflows/index.ts</automated>
    <fails_when>the printed count is `0` -- the bridge barrel does not export the unstage primitive and the ledger has nothing to call</fails_when>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` at either commit boundary. Immediately after the schema edit and before the worklist is closed the same command MUST print a number near 68; a `0` there means the member did not land as required</fails_when>
    <automated>node --test tests/persistence/state-io.test.ts tests/persistence/migrate.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count</fails_when>
    <automated>node --test tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count, or the `tests` count is not at least seven higher than the count recorded before this task began</fails_when>
    <automated>grep -c 'hello:greet.json' tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>the printed count is `0` -- the happy-path case is not pinning a literal envelope filename, so it cannot distinguish a written envelope from a recomputed path</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</automated>
    <fails_when>non-zero exit, or the report is short of `hit === found` on branches, functions or lines</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/persistence/state-io.ts</automated>
    <fails_when>non-zero exit, or the report is short of `hit === found` on branches, functions or lines</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit. Before the allow-list string is added, the `dead-code` sub-gate MUST exit 1 printing a boundary violation naming `orchestrators` to `bridges-workflows`; a green run at that point means the bridge import did not land</fails_when>
    <automated>npm run test:corresponding &amp;&amp; npm run test:corresponding:negative</automated>
    <fails_when>non-zero exit, or the output contains `missing-test:`</fails_when>
    <automated>git show --pretty=format: --name-only HEAD | sed '/^$/d' | sort</automated>
    <fails_when>the printed set for the ledger commit omits `.fallowrc.json` -- the allow-list edge must ride the same commit as the first orchestrator import, not a follow-up</fails_when>
    <automated>git merge-base --is-ancestor features/workflows-spike HEAD; echo "ancestor=$?"</automated>
    <fails_when>the printed line is `ancestor=0` -- a spike commit entered this branch, which is the transplant the phase's own criterion forbids</fails_when>
    <automated>grep -rnE '^export (const|function|async function) __test_' extensions/pi-claude-marketplace/ | wc -l</automated>
    <fails_when>the printed count is anything other than `0` -- a test-only export declaration appeared, which is the spike's shape and has no place on this branch</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'workflows: Type.Array(Type.String())' extensions/pi-claude-marketplace/persistence/state-io.ts` prints `1`.
    - `grep -c 'record.resources.workflows' extensions/pi-claude-marketplace/persistence/state-io.ts` prints at least `1`.
    - `grep -c '"workflows"' extensions/pi-claude-marketplace/persistence/migrate.ts` prints at least `1`.
    - `grep -c 'workflowsPhase' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` prints at least `3` -- the declaration, the array slot, and at least one reference.
    - `grep -c 'stagedWorkflowNames' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` prints at least `4`.
    - `grep -c 'bridges-workflows' .fallowrc.json` prints `2` -- the pre-existing zone rule plus the new allow-list member.
    - `grep -c 'WorkflowsUnstageFailureError' extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` prints at least `2`.
    - `grep -c 'Object.freeze' extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` prints a count at least one higher than before this task, and the class's own field is one of the frozen values.
    - `npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l` prints `0`.
    - `node --test tests/orchestrators/plugin/install.test.ts` exits 0 with zero failures.
    - `node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` reports `hit === found` on branches, functions and lines.
    - `npm run fallow` exits 0.
    - `git merge-base --is-ancestor features/workflows-spike HEAD` exits non-zero -- no spike commit entered the branch, which is how the written-against-the-current-files criterion is checked rather than asserted.
    - `grep -rnE '^export (const|function|async function) __test_' extensions/pi-claude-marketplace/ | wc -l` prints `0`.
    - The undo case asserts a `stat` on the envelope path throws AND that a read of the staging directory yields no entries; a case asserting only that the install notification carried an error severity does not satisfy this.
  </acceptance_criteria>
  <done>A workflow-bearing plugin installs its envelope, the persisted record names it, a `statePhase` failure removes it from disk along with its staging tree, and both commits are green at every gate.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the rollback evidence the coverage gate does not force</name>
  <files>tests/orchestrators/plugin/install.test.ts, tests/transaction/phase-ledger.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</files>
  <read_first>
    - `tests/orchestrators/plugin/install.test.ts` line 2826 -- the containment case for a bridge PREPARE, which asserts the error text reaches the cause trailer and that no rollback-partial marker is present; this is the model to mirror for the UNDO direction
    - `tests/transaction/phase-ledger.test.ts` lines 1-20 (`PRODUCTION_PHASE_NAMES`) and 437-480 (the two identity assertions on a re-thrown containment error)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` lines 1253-1271 and 1850-1915 -- the `capture` assignment the containment path bypasses, and the failure-row composition that reads `capture.version`
    - `extensions/pi-claude-marketplace/bridges/workflows/types.ts` lines 131-147 -- the `onPlaced` contract sentence about unlinking a name this commit did not place
    - `extensions/pi-claude-marketplace/shared/path-safety.ts` -- `SymlinkRefusedError` and `PathContainmentError`, and which one a symlinked leaf raises
    - `.planning/workstreams/workflows/phases/111-workflows-bridge/111-REVIEW.md` §WR-11 and §WR-12, and `111-REVIEW-FIX.md` §WR-10 -- the two data-loss findings this task's assertions exist to prevent recurring, and the reasoning that keeps the containment refusal a throw
    - `docs/output-catalog.md` around line 56 -- the rollback-partial child row shape
    - `112-RESEARCH.md` §"Research Question 3" and §"Research Question 6"
    - `112-PATTERNS.md` §4
  </read_first>
  <action>
Task 1 closed every branch the coverage gate can see. Coverage proves a branch ran; it does not prove
what the branch left on disk. The preceding phase's review found two data-loss defects in exactly this
rollback code under 100% line, branch and function coverage. These cases assert the state after the
failure.

Add to `tests/orchestrators/plugin/install.test.ts`, each inside `withHermeticHome` with
`locationsFor` called inside the closure:

- **A foreign envelope at a colliding name survives the undo.** Seed a plugin whose workflow generates
  a name, and BEFORE the install place a second, differently-named envelope file in the saved directory
  that this install will not place -- a file another plugin or the user owns. Drive the `statePhase`
  failure. Assert the plugin's own envelope is gone AND the foreign file is still present with its
  original bytes. This is the assertion that fails if the undo ever unlinks the prepared names rather
  than the reported ones.

- **The removal is order-independent and completes.** Seed a plugin declaring three workflows so the
  commit places three envelopes, make ONE of them unremovable, and drive the `statePhase` failure.
  Assert the other two are gone from disk and that the raised failure names the one that could not be
  removed. A loop that aborts on the first failure leaves the remainder on disk and fails this case.

- **The rollback-partial child is rendered.** On the same sabotage, assert the single notification's
  text carries a rollback-partial marker naming the workflows phase. Read the expected phrasing from
  the output catalog rather than inventing one.

- **The containment refusal propagates verbatim.** Plant a symbolic link at the saved-directory leaf
  path one recorded name composes to, so the undo's per-name composition raises rather than unlinking,
  and drive the `statePhase` failure. Assert: the install fails; the notification carries NO
  rollback-partial marker; the refusal's text reaches the cause trailer; and the failure row carries no
  version. That last one is the documented trade -- the throw escapes the runner and never reaches the
  block that assigns the captured version -- and the case exists so a later reader does not "repair"
  it. Say so in a comment on the case, citing PI-14.

In `tests/transaction/phase-ledger.test.ts`, add the new phase name to `PRODUCTION_PHASE_NAMES`. It is
a test-local constant and nothing fails if it is missed, which is exactly why it will be missed: the
ledger's own suite would silently keep mirroring a five-bridge production while production runs six.

Finally, run `npm run fallow` and read the `health` sub-gate. Two independently-computed complexity
ceilings apply and they disagree by algorithm. If the ledger body now exceeds either, extract the
sixth phase's `do` and `undo` bodies into named module-private helpers taking the context as their
parameter -- the same remedy the ledger runner itself already records having applied for the same
reason. Do not add a threshold override; the config carries none today. If the gate is green, change
nothing and record in the SUMMARY that the extraction was measured as unnecessary.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count</fails_when>
    <automated>node --test tests/transaction/phase-ledger.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count</fails_when>
    <automated>grep -c 'PRODUCTION_PHASE_NAMES' tests/transaction/phase-ledger.test.ts</automated>
    <fails_when>the printed count is `0`, or `grep -c '"workflows"' tests/transaction/phase-ledger.test.ts` prints `0` -- the fixture phase universe still mirrors a five-bridge production</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit -- in particular a `health` finding naming the install ledger body, which means the extraction described in the action was needed and was not done</fails_when>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0`</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</automated>
    <fails_when>non-zero exit, or the report is short of `hit === found` on branches, functions or lines</fails_when>
  </verify>
  <acceptance_criteria>
    - A case exists whose assertion reads a file the install did NOT place and asserts its bytes are unchanged after the failed install.
    - A case exists that places three envelopes, makes one unremovable, and asserts the other two are absent from disk afterwards.
    - A case exists asserting the failure notification contains no rollback-partial marker on the containment path, and that the refusal text reaches the cause trailer.
    - `grep -c '"workflows"' tests/transaction/phase-ledger.test.ts` prints at least `1`.
    - `npm run fallow` exits 0.
    - `node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` reports `hit === found` on branches, functions and lines.
  </acceptance_criteria>
  <done>The rollback behaviour is pinned by assertions on disk state and on the rendered failure row, the ledger suite's phase universe mirrors production, and the complexity gate is green with the extraction either applied or measured unnecessary.</done>
</task>

<task type="auto">
  <name>Task 3: Widen the three mirrored ledger-phase closed sets</name>
  <files>extensions/pi-claude-marketplace/shared/errors.ts, extensions/pi-claude-marketplace/orchestrators/types.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, tests/orchestrators/types.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/shared/errors.ts` lines 340-375 -- `Phase3Failure` and the doc above it, which enumerates the bridges verbatim
    - `extensions/pi-claude-marketplace/orchestrators/types.ts` lines 138-155 -- `UpdatePhaseBridge` and its doc, which distinguishes it from the free-form rollback-partial label
    - `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` lines 1420-1450 -- `PHASE3_FAILURE_PHASES` and the header claim that a future bridge surfaces here as a type error
    - `tests/orchestrators/types.test.ts` lines 85-100 -- the `satisfies Record<UpdatePhaseBridge, true>` exhaustiveness pin
    - `112-RESEARCH.md` §Pitfall "the closed-set widening is a three-file change, not one", and §"WLIF numbering conflict"
  </read_first>
  <action>
Criterion 2 names one file. It is three, plus a test.

Add the new member as the literal string `workflows`, last, to each of: the `phase` member of
`Phase3Failure`; the `UpdatePhaseBridge` union; and the `PHASE3_FAILURE_PHASES` runtime tuple. The
spelling must match the bridge kind spelling used in the component paths, the supported tuples and the
record's resources -- a near-miss spelling type-checks at the declaration and diverges silently at every
string comparison.

Then add the matching member to the `satisfies Record<UpdatePhaseBridge, true>` object in
`tests/orchestrators/types.test.ts`. Measured: widening only the type leaves three errors, all in
`update.ts`; widening all three leaves exactly one, at that pin. If the typecheck after your edits
reports a different count, the set of mirrors is not the set you edited -- find the missing mirror
before proceeding.

Confirm by inspection that no existing arm changed: the widened tuple lets a workflows failure be
represented, but `update.ts` cannot produce one and gains no new behavior here. The update re-stage is
a later phase's work; this commit exists so that phase needs no second type commit. State that in the
SUMMARY, because the requirement text for this ID describes the update behavior while the roadmap
criterion describes only the widening, and the two are deliberately split.

Add a brief case to `tests/orchestrators/plugin/update.test.ts` only if the typecheck worklist demands
one; otherwise leave that file alone.
  </action>
  <reversibility rating="reversible">Three additive union members and one test key; each reverts with a one-line edit and nothing consumes the new member yet.</reversibility>
  <verify>
    <automated>npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l</automated>
    <fails_when>the printed count is anything other than `0` after all four edits. After editing only `shared/errors.ts` the same command MUST print `3`; a different number means the mirrors are not where the research measured them</fails_when>
    <automated>grep -c '"workflows"' extensions/pi-claude-marketplace/shared/errors.ts extensions/pi-claude-marketplace/orchestrators/types.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts</automated>
    <fails_when>any of the three printed per-file counts is `0` -- one of the three mirrored closed sets was missed</fails_when>
    <automated>node --test tests/orchestrators/types.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count</fails_when>
    <automated>node --test tests/orchestrators/plugin/update.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a nonzero `fail` count -- an existing update failure arm no longer reports under its own phase name</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"workflows"' extensions/pi-claude-marketplace/shared/errors.ts` prints at least `1`.
    - `grep -c '"workflows"' extensions/pi-claude-marketplace/orchestrators/types.ts` prints at least `1`.
    - `grep -c '"workflows"' extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` prints at least `1`.
    - `grep -c 'workflows' tests/orchestrators/types.test.ts` prints at least `1`.
    - `npm run typecheck 2>&amp;1 | grep -E '\): error TS' | wc -l` prints `0`.
    - `node --test tests/orchestrators/plugin/update.test.ts` exits 0 with zero failures, demonstrating no existing phase arm changed behavior.
  </acceptance_criteria>
  <done>All three mirrored closed sets carry the new member, the exhaustiveness pin is closed, and the update suite is unchanged and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-authored workflow script -> filesystem path under `$HOME` | an untrusted third-party file becomes an envelope at a path outside every scope root |
| persisted `state.json` -> removal payload | a recorded name read back from disk decides which file a later unlink targets |
| the workflows saved directory -> other writers | the directory is shared with the user's own workflows, the host engine, and every other plugin |
| in-flight ledger context -> rollback | the undo acts on names a callback reported, not on names the caller derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-01 | Tampering | `workflowsPhase.undo` in `orchestrators/plugin/install.ts` | high | mitigate | Remove only the names the commit's `onPlaced` callback reported. A foreign or restored-previous envelope at a colliding name is never unlinked; Task 2 pins this with a case that reads the untouched file's bytes after the failed install. |
| T-112-02 | Tampering | `workflowsPhase.undo` raising a containment refusal | high | mitigate | The bridge raises `PathContainmentError` rather than folding it into `failed[]`, and both ledger sites re-throw it by class. Not re-folded here; Task 2 pins the verbatim propagation and the absent rollback-partial marker. |
| T-112-03 | Information disclosure | envelopes left on disk after a failed install | high | mitigate | The undo is driven by a sentinel assigned before the commit, so a partially-completed commit still unwinds. Task 1 asserts absence on disk after the failure, not merely a rejected promise. |
| T-112-04 | Repudiation | a swallowed `failed[]` from the undo | medium | mitigate | A non-empty `failed[]` throws `WorkflowsUnstageFailureError`, which the ledger records as a rollback partial and the renderer emits as a child row naming the phase. |
| T-112-05 | Tampering | `resources.workflows` on the persisted record | medium | mitigate | The record is validated by the typebox install-record schema on every read, and the migration default-fills a legacy record before validation. The name is namespaced by the plugin prefix the bridge applies. |
| T-112-06 | Tampering | `workflowArtifactPath` composition of an untrusted generated name | medium | transfer | Already mitigated at the persistence chokepoint shipped by the preceding phase (`assertSafeName` plus `assertPathInside` against the saved directory). This plan composes no artifact path of its own and adds no second composer. |
| T-112-SC | Tampering | package-manager installs | low | accept | This plan runs no `npm install`. Every module it touches is in-repo and every tool it invokes is a committed devDependency, so the package-legitimacy gate has no candidate. |
</threat_model>

<verification>
- `npm run check` is green at each of the three task boundaries.
- `node scripts/test-coverage-direct.mjs` reports `hit === found` on branches, functions and lines for
  `orchestrators/plugin/install.ts`, `persistence/state-io.ts`, `persistence/migrate.ts`,
  `orchestrators/marketplace/shared.ts`, `shared/errors.ts`, `orchestrators/types.ts` and
  `orchestrators/plugin/update.ts`.
- `npm run fallow` exits 0, with the allow-list edge present in the same commit as the first
  orchestrator import of the bridge.
- `git status` is clean after each commit, or any prettier rewrite is landed as a follow-up commit
  rather than an amend.
</verification>

<success_criteria>
- Installing a workflow-bearing plugin writes its envelopes and the persisted record names them.
- A `statePhase` failure after the workflows phase committed leaves no envelope and no staging tree on
  disk, proven by filesystem assertions.
- An undo that cannot remove a name raises a typed error carrying per-name reasons, rendered as a
  rollback-partial child row naming the phase.
- A containment refusal from the undo propagates verbatim with no rollback-partial marker.
- The three mirrored ledger-phase closed sets and the exhaustiveness pin all carry the new member.
- The `orchestrators` zone allows the workflows bridge, in the commit that first imports it.
</success_criteria>

<output>
Create `.planning/workstreams/workflows/phases/112-install-and-removal-lifecycle/112-01-SUMMARY.md` when done.
Record: the typecheck error count observed immediately after the schema edit and after the worklist was
closed; whether the complexity extraction was needed; the copy-semantics choice made for the typed error
and why; and the split reading of the doubly-booked requirement ID.
</output>
