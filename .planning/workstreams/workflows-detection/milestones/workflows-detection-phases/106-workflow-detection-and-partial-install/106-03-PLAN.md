---
phase: 106-workflow-detection-and-partial-install
plan: 03
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/discover.test.ts
autonomous: true
requirements: [WDET-05, WDET-06]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "EDGE-07: A normal install creates no state or artifact and emits the existing partial hint. A fresh partial install stages supported artifacts and persists workflows only in compatibility metadata (WDET-05, WDET-06, D-106-05)."
    - "EDGE-08: Install retry, interruption, or parallel classification adds no workflow ledger phase or resource key. Existing atomic staging and state writes remain the only mutation path (WDET-06, D-106-05)."
    - "A failed partial install rolls back its supported artifacts, keeps the workflow sentinel at the source, and can be retried safely (NFR-1, NFR-3)."
    - "Reload discovery returns only `skillPaths` and `promptPaths`, even when workflow directories exist below test roots (WDET-06)."
  artifacts:
    - path: "tests/orchestrators/plugin/install.test.ts"
      provides: "fresh-install, interruption, rollback, retry, compatibility, and no-materialization evidence"
      contains: "workflows"
    - path: "tests/orchestrators/discover.test.ts"
      provides: "the exact closed discovery-result regression with workflow decoys"
      contains: "workflows"
  key_links:
    - from: "tests/orchestrators/plugin/install.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      via: "the public install entry point drives the current five-phase ledger and state projection"
      pattern: "installPlugin\\("
    - from: "tests/orchestrators/discover.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/discover.ts"
      via: "`aggregateDiscoveredResources` retains its exact two-array result"
      pattern: "aggregateDiscoveredResources"
---

<objective>
Lock the materialization and reload boundaries after the tracer.

The phase must treat workflows as compatibility metadata only. These tests
exercise normal rejection, partial staging, interrupted staging, retry, state
shape, disk targets, and reload discovery without changing production staging
or persistence code.

Purpose: prove WDET-05 and WDET-06 under the failure and retry shapes most likely
to expose an accidental generic-copy path.

Output: two expanded boundary suites. This plan changes no production file.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows-detection/ROADMAP.md
@.planning/workstreams/workflows-detection/REQUIREMENTS.md
@.planning/workstreams/workflows-detection/STATE.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-CONTEXT.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-RESEARCH.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-PATTERNS.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-VALIDATION.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-01-SUMMARY.md

<interfaces>
- `seedPathMarketplaceWithPlugin` returns the source `pluginRoot` for disk assertions.
- `writePluginComponents` has the fixture-only workflow option from Plan 01.
- The install ledger phases remain skills, commands, agents, hooks, and MCP only.
- Installation records separate `compatibility.unsupported` from five fixed resource arrays.
- `aggregateDiscoveredResources(user, project)` returns `DiscoveredResources` with `skillPaths` and `promptPaths` only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prove rejection, rollback, retry, and state boundaries with a sentinel</name>
  <files>tests/orchestrators/plugin/install.test.ts</files>

  <read_first>
    - Read the committed workflow tracer and fixture option from Plan 01 before editing the test.
    - Read the existing rollback-skills case that makes `commandsStagingDir` a file and forces `ENOTDIR`.
    - Read the existing hooks-to-MCP unwind case and choose the smaller skills-to-commands failure shape.
    - Read `runInstallLedgerBody`, the five phase declarations, and the record projection in `orchestrators/plugin/install.ts`. Trace callers, but do not modify this file.
    - Read `PLUGIN_INSTALL_RECORD_SCHEMA` in `persistence/state-io.ts`. Confirm the five resource keys before writing assertions.
  </read_first>

  <behavior>
    - A rejected normal install creates no record or artifact and shows the existing partial hint.
    - An interrupted partial install rolls back a committed skill, creates no record, and leaves the source sentinel untouched.
    - Removing the test-created blocker and retrying `--partial` succeeds without collision.
    - The successful record stores workflows only in `compatibility.unsupported` and keeps the five resource keys unchanged.
  </behavior>

  <action>
Expand the tracer fixture into one boundary family. Keep normal rejection in a
fresh hermetic environment. Assert no state record, supported target, workflow
target, or reload trailer.

For interruption and retry, seed a workflow plugin with a supported skill and
command. Before the first partial install, occupy `commandsStagingDir` with a
file exactly as the existing rollback-skills case does. Assert the error row,
skill rollback, absent state record, and source-only workflow sentinel.

Remove only that test-created blocker. Retry the same `installPlugin` call with
`partial: true`. Assert one supported skill and command, exact workflow
compatibility metadata, the existing five resource keys, and no sentinel below
the scope root outside the source marketplace. This proves D-106-05 without a
new ledger or resource path.

Run the inherited `tests/architecture/compat-01-no-expansion.test.ts` beside the
focused suite. Do not edit it. Commit only `install.test.ts` with
`test: protect workflow partial-install boundary` after all gates pass.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts tests/architecture/compat-01-no-expansion.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem tests/orchestrators/plugin/install.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files tests/orchestrators/plugin/install.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The normal rejection has the exact partial reason and hint, no state record, and no target artifact.
    - The induced commands-phase interruption removes the skill committed by the preceding phase.
    - The interrupted run does not write an installation record or move the workflow sentinel.
    - The retry succeeds after the fixture blocker is removed and does not collide with stale staging state.
    - The successful record stores exactly `["workflows"]` under compatibility and retains the five current resource keys.
    - No workflow file exists in skills, prompts, agents, MCP, hooks, staging, or plugin-data targets.
    - The inherited no-expansion architecture suite passes unchanged.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>Normal, interrupted, retried, and successful partial installs all preserve the workflow no-materialization boundary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep reload discovery closed when workflow directories exist</name>
  <files>tests/orchestrators/discover.test.ts</files>

  <read_first>
    - Read `tests/orchestrators/discover.test.ts` before editing it.
    - Read its temporary-root setup and exact `DiscoveredResources` assertions.
    - Read `aggregateDiscoveredResources` and the `DiscoveredResources` type in `orchestrators/discover.ts`. Trace the `index.ts` `resources_discover` caller, but do not modify production files.
  </read_first>

  <behavior>
    - Workflow decoy directories under user and project test roots do not change the discovered resource result.
    - Existing skill and prompt discovery stays byte-for-byte equivalent at the object boundary.
    - No `workflowPaths` field appears in the result type or object.
  </behavior>

  <action>
Add local workflow decoy directories and sentinel files to the existing user and
project discovery fixture. Call the public `aggregateDiscoveredResources`
function. Keep the current exact object assertion over `skillPaths` and
`promptPaths`.

Add an explicit key-set assertion so a future `workflowPaths` expansion fails.
Do not change `orchestrators/discover.ts`, `index.ts`, or a persistence schema.
Commit only this test with `test: preserve workflow discovery boundary` after
the focused test, secret scan, and pre-commit gates pass.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/discover.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem tests/orchestrators/discover.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files tests/orchestrators/discover.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Workflow decoys exist in both fixture scopes during the call.
    - The result has exactly the `skillPaths` and `promptPaths` keys.
    - Existing skill and prompt ordering remains unchanged.
    - No workflow source file appears in either returned array.
    - Production discovery and host event files are unchanged.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>Reload discovery remains a two-array contract and ignores workflow directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin source to staging targets | Untrusted workflow files sit beside supported component sources. |
| Ledger rollback to persistent state | An interruption must leave neither artifacts nor a ghost record. |
| Scope roots to Pi resource discovery | Reload returns only the supported resource classes that Pi understands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-02 | Elevation of privilege | staging and reload | high | mitigate | Seed an executable-looking sentinel and prove that no target or result contains it. |
| T-106-04 | Tampering | install record and resource inventory | high | mitigate | Assert exact compatibility placement and the unchanged five-key resource object. |
| T-106-07 | Denial of service | ledger retry after interruption | medium | mitigate | Force a real phase failure, prove rollback, remove the fixture blocker, and retry successfully. |
</threat_model>

## Artifacts this phase produces

This phase creates no production file, bridge, resource field, discovery field,
or executor. It produces these symbols and contract IDs:

- `UNSUPPORTED_COMPONENT_FIELDS.workflows`.
- The `UNSUPPORTED_COMPONENT_KINDS` tail member and fixed workflow directory convention.
- The `UnsupportedReason` workflow member and shared mapping.
- The `REASONS` workflow tail member and unsupported topic-group member.
- Fixture-only workflow seed support in `install.test.ts`.
- Catalog states `workflow-partially-available-inventory`, `workflow-install-rejection`, and `workflow-partial-install-success`.

<verification>
- Run each focused command after its task.
- Run `npm test` and `npm run test:integration` after both Wave 2 plans complete.
- Confirm that `git diff --name-only -- extensions/` is empty for this plan.
</verification>

<success_criteria>
- Normal rejection and partial success use fresh, isolated environments.
- An interrupted install rolls back and retries cleanly.
- Workflow files never enter state resources or target paths.
- Reload discovery stays closed to skills and prompts.
- No production staging, state, discovery, or runtime file changes.
</success_criteria>

<output>
Create `.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-03-SUMMARY.md` when done.
</output>
