---
phase: 106-workflow-detection-and-partial-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/resolver.ts
  - extensions/pi-claude-marketplace/shared/probe-classifiers.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/architecture/notify-closed-set-locks.test.ts
autonomous: true
requirements: [WDET-02, WDET-03, WDET-04, WDET-05, WDET-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A conventional `<pluginRoot>/workflows/` directory follows the complete resolver-to-install path without a manifest declaration (WDET-02, D-106-02)."
    - "A sound plugin with workflows resolves as `partially-available`, carries one `workflows` kind, and renders the exact `{workflows}` reason (WDET-03, WDET-04, D-106-03, D-106-04)."
    - "A normal install rejects the plugin with the existing partial hint. An install with `--partial` stages its supported skill only (WDET-05, D-106-05)."
    - "EDGE-03: A sound workflow-bearing plugin has one workflows entry. A structural defect stays unavailable and exposes no materializable root (WDET-03, D-106-06)."
    - "No workflow file enters a ledger phase, materialized resource list, reload result, or execution path (WDET-06, D-106-05)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/resolver.ts"
      provides: "the `workflows` unsupported-kind tail member and fixed directory convention"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      provides: "the single typed-kind-to-`workflows` reason mapping"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "the `REASONS` tail member for `workflows`"
      contains: "workflows"
    - path: "extensions/pi-claude-marketplace/shared/notify-reasons.ts"
      provides: "closed reason-topic coverage for `workflows`"
      contains: "workflows"
    - path: "tests/orchestrators/plugin/install.test.ts"
      provides: "the production-quality conventional-directory tracer and structural-precedence regression"
      contains: "WDET-02"
    - path: "tests/architecture/notify-closed-set-locks.test.ts"
      provides: "the deliberate reason count change from 39 to 40"
      contains: "40"
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/resolver.ts"
      to: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      via: "`ResolvedPlugin.unsupported` reaches `narrowUnsupportedKinds` through existing consumers"
      pattern: "workflows"
    - from: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "the classifier returns the closed `Reason` tail member"
      pattern: "workflows"
    - from: "extensions/pi-claude-marketplace/domain/resolver.ts"
      to: "tests/orchestrators/plugin/install.test.ts"
      via: "the real `installPlugin` path selects `requireInstallable` or `requirePartialInstallable`"
      pattern: "installPlugin\\("
---

<objective>
Deliver the production-quality tracer for one real workflow-bearing plugin.

The tracer starts with the literal conventional directory. It crosses resolver
classification, shared reason narrowing, normal rejection, partial consent,
supported staging, and compatibility persistence. This path implements
D-106-02 through D-106-05 without adding workflow support. The second task
protects D-106-06 at the same user entry point.

Purpose: prove the hardest constraint first. The plugin must remain useful with
explicit consent, but no workflow content can cross the materialization
boundary.

Output: four coordinated closed-set extensions, the matching reason-count
lock, and two end-to-end install regressions. This plan creates no production
file.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows-detection/ROADMAP.md
@.planning/workstreams/workflows-detection/REQUIREMENTS.md
@.planning/workstreams/workflows-detection/STATE.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-CONTEXT.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-RESEARCH.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-PATTERNS.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-VALIDATION.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-UI-SPEC.md

<interfaces>
- `UNSUPPORTED_COMPONENT_KINDS` derives `UnsupportedKind` in `domain/resolver.ts`.
- `UNSUPPORTED_COMPONENT_CONVENTIONS` supplies fixed `file` or `dir` stat rules.
- `narrowUnsupportedKinds(unsupported)` preserves first-seen reason order.
- `REASONS` derives the closed `Reason` union in `shared/notify.ts`.
- `installPlugin` already selects the normal or partial resolver gate from `opts.partial`.
- `writePluginComponents` is a fixture-only writer in `install.test.ts`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one conventional workflow directory through partial install</name>
  <reversibility rating="costly">The new tail member spans four shared contracts and all reason consumers, so a revert needs one coordinated closed-set change.</reversibility>
  <files>extensions/pi-claude-marketplace/domain/resolver.ts, extensions/pi-claude-marketplace/shared/probe-classifiers.ts, extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/notify-reasons.ts, tests/orchestrators/plugin/install.test.ts, tests/architecture/notify-closed-set-locks.test.ts</files>

  <read_first>
    - Read every listed file before editing it.
    - In `domain/resolver.ts`, read `UNSUPPORTED_COMPONENT_KINDS`, `UNSUPPORTED_COMPONENT_CONVENTIONS`, `declaresUnsupportedKind`, `hasUnsupportedConvention`, `collectUnsupportedKinds`, `decideResolution`, `requireInstallable`, and `requirePartialInstallable`.
    - Run `rg -n 'UNSUPPORTED_COMPONENT_KINDS|collectUnsupportedKinds|requirePartialInstallable' extensions tests` to trace the resolver callers before modification.
    - In `shared/probe-classifiers.ts`, read `UnsupportedReason`, `narrowUnsupportedKinds`, and `kindToReason`. Trace every `narrowUnsupportedKinds` caller with `rg`.
    - In `shared/notify.ts`, read `REASONS`, `Reason`, the plugin-row reason renderer, and the nearby count history. Do not change glyph or status tuples.
    - In `shared/notify-reasons.ts`, read the local `UnsupportedReason` topic union and `_ReasonsCoverageProof`.
    - In `install.test.ts`, read `writePluginComponents`, `seedPathMarketplaceWithPlugin`, the partial-install cases near the WR-03 cases, and the rollback cases that use real staging failures.
    - In `notify-closed-set-locks.test.ts`, read the exact `REASONS` length lock and its count-history comments. Keep every other closed-set lock unchanged.
  </read_first>

  <behavior>
    - A plugin with one supported skill and a literal `workflows/` directory resolves as partial without network access.
    - The normal install emits the exact partial row and retry hint, creates no record, and creates no target artifact.
    - The partial install emits the exact success row, stages the skill, stores `compatibility.unsupported: ["workflows"]`, and stores no workflow resource.
  </behavior>

  <action>
Write the failing install regression first. Extend the fixture-only options of
`writePluginComponents` with `workflows?: { sourceName: string; body?: string }[]`.
Write each item below `<pluginRoot>/workflows/`. Keep this option out of all
production types.

Use separate hermetic environments for the normal and partial runs. Seed one
supported skill and one sentinel workflow file. Drive both runs through the
real `installPlugin` entry point. Assert exact notification bytes from
`106-UI-SPEC.md` and inspect state plus target paths.

Append `workflows` to `UNSUPPORTED_COMPONENT_KINDS` per D-106-02 and D-106-03.
Add only the fixed `{ relativePath: "workflows", kind: "dir" }` convention.
Do not read the directory contents or a manifest-controlled path.

Add `workflows` to the closed `UnsupportedReason` union. Map the typed kind to
the same literal before the generic fallback per D-106-04. Append `workflows`
to `REASONS`, update its count from 39 to 40, and add it to the unsupported
topic union. Do not add a mapping to an orchestrator or to
`MANIFEST_FIELD_REASONS`.

Update the `REASONS` architecture lock title, assertion, and count-history
comment from 39 to 40 in the same task. Keep glyph, marketplace-status,
plugin-status, and marker locks unchanged.

Confirm that the partial record has the five existing resource keys only. The
workflow sentinel must remain under the source root. Commit these six paths
with `feat: detect unsupported workflow directories` after all gates pass.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts tests/architecture/notify-closed-set-locks.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem extensions/pi-claude-marketplace/domain/resolver.ts extensions/pi-claude-marketplace/shared/probe-classifiers.ts extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts tests/orchestrators/plugin/install.test.ts tests/architecture/notify-closed-set-locks.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files extensions/pi-claude-marketplace/domain/resolver.ts extensions/pi-claude-marketplace/shared/probe-classifiers.ts extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts tests/orchestrators/plugin/install.test.ts tests/architecture/notify-closed-set-locks.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `UNSUPPORTED_COMPONENT_KINDS.at(-1)` and `REASONS.at(-1)` are both `workflows`.
    - The architecture lock title, assertion, and count-history note all record the deliberate 40-member `REASONS` set.
    - The convention stats `path.join(pluginRoot, "workflows")` as a directory and no other workflow-derived path.
    - A normal install renders `(partially-available) {workflows}` with the existing install hint, then leaves no installation record.
    - A partial install renders `(partially-installed) {workflows}` with the normal reload rule and stages the supported skill.
    - The installation record stores `compatibility.unsupported` as exactly `["workflows"]` and retains only the five current resource keys.
    - The sentinel workflow file exists below the source root and nowhere below a materialized target.
    - The workflow path does not enter a bridge, ledger phase, resource field, discovery field, or execution call.
    - The filesystem scan reports zero verified and zero unverified secrets. All applicable pre-commit hooks pass with only TruffleHog skipped after that scan.
  </acceptance_criteria>

  <done>The conventional-directory tracer works from classification through explicit partial consent, and no workflow content leaves the source plugin root.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep structural failure ahead of the workflow soft signal</name>
  <files>tests/orchestrators/plugin/install.test.ts</files>

  <read_first>
    - Read the Task 1 workflow tracer in `install.test.ts` after its commit.
    - Read the existing `classifyEntityShapeError` and `partialHint` cases in the same file.
    - Read the existing malformed `mcpServers` fixture path and the normal `--partial` gate selection in `orchestrators/plugin/install.ts`. Do not modify production code.
  </read_first>

  <behavior>
    - A plugin with a malformed structural field and a workflow directory stays unavailable.
    - Passing `--partial` does not create a materializable root, a partial hint, a state record, or an artifact.
  </behavior>

  <action>
Add one install regression for D-106-06. Seed the same literal workflow
directory as the tracer and add an existing structural defect, such as the
malformed `mcpServers` shape used by the nearest resolver or install case.
Invoke the public install path with `partial: true`.

Assert the existing unavailable row and structural reason. Assert that the row
has no workflow reason and no partial hint. Assert that the operation creates
no state record, supported artifact, or workflow artifact. Do not change
`decideResolution`, the install gates, or messaging code to satisfy the test.

Commit only the test file with `test: preserve workflow structural precedence`
after the focused test, secret scan, and pre-commit gates pass.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem tests/orchestrators/plugin/install.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files tests/orchestrators/plugin/install.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The both-defects fixture returns the structural unavailable output, not a partial output.
    - The `--partial` option does not admit the plugin and does not add the existing partial hint.
    - The failed result exposes no materializable root through its public discriminated arm.
    - No state or target artifact survives the operation.
    - Production files are unchanged in this task.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>Structural invalidity still wins over workflow detection at the real install entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin root to resolver | An untrusted plugin controls files and manifest values below its root. |
| Resolver result to install ledger | A soft unsupported signal must not bypass structural validation or enter a supported bridge. |
| Typed reason to terminal renderer | The new kind must map to one closed reason without free-text drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-01 | Tampering | `collectUnsupportedKinds` | high | mitigate | Ignore declaration payload paths and stat only the fixed literal `workflows/` directory. |
| T-106-02 | Elevation of privilege | install ledger and bridges | high | mitigate | Add no reader, importer, spawn, bridge, or ledger phase. Prove sentinel absence at every target. |
| T-106-03 | Tampering | `decideResolution` and install gates | high | mitigate | Keep structural precedence unchanged and test `--partial` against the both-defects case. |
| T-106-05 | Repudiation | reason classifier and renderer | medium | mitigate | Use one typed mapping and closed tuple. Pin exact terminal bytes in the tracer. |
</threat_model>

## Artifacts this phase produces

This phase creates no production file, bridge, resource field, discovery field,
or runtime executor. It produces these closed-contract members and catalog IDs:

- `UNSUPPORTED_COMPONENT_FIELDS.workflows` in `domain/components/plugin.ts`.
- The `UNSUPPORTED_COMPONENT_KINDS` member `workflows` and its fixed directory convention.
- The `UnsupportedReason` member and mapping `workflows` in `shared/probe-classifiers.ts`.
- The `REASONS` tail member `workflows` and its topic-group member.
- Fixture-only workflow seed support in `install.test.ts`.
- Catalog states `workflow-partially-available-inventory`, `workflow-install-rejection`, and `workflow-partial-install-success`.

## Prohibition recall result

The two-stage recall and precision pass produced this audit:

| Requirement | Recall focus | Precision result |
|-------------|--------------|------------------|
| WDET-01 | Payload interpretation, schema mutation, and hidden declaration semantics | Routine correctness, or canon input safety. No bespoke item kept. |
| WDET-02 | Recursive scanning, custom paths, network lookup, and content inspection | Routine correctness, or canon path safety. No bespoke item kept. |
| WDET-03 | Structural bypass, duplicate state, and false materializability | Routine resolver correctness. No bespoke item kept. |
| WDET-04 | Misleading reasons, source-specific labels, and output drift | Routine contract correctness. No bespoke item kept. |
| WDET-05 | Consent bypass, unsupported staging, and partial-state ambiguity | Already explicit in WDET-05 and WDET-06. No omitted bespoke item kept. |
| WDET-06 | Materialization, execution, persistence growth, and reload discovery | Already explicit in WDET-06. No omitted bespoke item kept. |

Manifest path traversal is canon security work. The T-106-01 rows carry that
breadcrumb. Unintended script execution is also canon security work. The
T-106-02 rows carry that breadcrumb. WDET-06 and D-106-05 remain executable
truths even though the bespoke prohibition count is zero.

<verification>
- Run both task-level commands after each task.
- Run `npm test` and `npm run test:integration` after this wave.
- Confirm that the task commits use explicit pathspecs and Conventional Commit titles.
</verification>

<success_criteria>
- A real conventional workflow directory reaches the exact partial output and consent path.
- The supported skill works after the partial install.
- The workflow sentinel never leaves the source root.
- A structural defect remains unavailable under `--partial`.
- No production path reads or executes workflow content.
</success_criteria>

<output>
Create `.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-01-SUMMARY.md` when done.
</output>
