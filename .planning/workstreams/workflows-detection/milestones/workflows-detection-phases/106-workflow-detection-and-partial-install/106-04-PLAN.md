---
phase: 106-workflow-detection-and-partial-install
plan: 04
type: execute
wave: 3
depends_on: [106-01, 106-02, 106-03]
files_modified:
  - tests/shared/probe-classifiers.test.ts
  - tests/orchestrators/plugin/cross-surface-reason-parity.test.ts
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
autonomous: true
requirements: [WDET-04]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "EDGE-04: A declaration plus the directory, or repeated typed workflow kinds, merges to one workflows reason (WDET-04, D-106-04)."
    - "EDGE-05: An empty unsupported list emits no reason. One workflows kind emits exactly workflows. Missing plugin.json does not change directory detection (WDET-04)."
    - "EDGE-06: Workflows is appended to the canonical kind and reason tuples. Combined outputs preserve all current positions and place workflows last (WDET-04, D-106-04)."
    - "UI-EMPTY: An empty top-level collection renders `(no marketplaces)`. A marketplace with no plugins renders only its header."
    - "UI-LOADING: The renderer emits one final notification after completion and adds no intermediate loading state."
    - "UI-ERROR: Workflow-only rejection keeps the bare header and existing error summary. Structural failures keep unavailable precedence."
    - "UI-POPULATED: Populated blocks keep the column-zero header, two-space plugin indentation, and one blank line between marketplace blocks."
    - "UI-PARTIAL: The header gains no partial state. The plugin row carries the partial status and one canonical workflows reason."
    - "UI-OVERFLOW: Headers, rows, fixed copy, and column-66 list-description truncation keep their existing bytes. The host handles other wrapping."
    - "UI-CARDINALITY: Zero marketplaces use the sentinel. Zero plugins use a bare header. One and many items reuse the same grammar."
    - "UI-LONG-TEXT: Fixed copy has no interpolation. Names keep the current byte grammar, with no workflow-specific wrapping or ellipsis."
  artifacts:
    - path: "tests/shared/probe-classifiers.test.ts"
      provides: "single, empty, duplicate, and ordered workflow-reason cases"
      contains: "workflows"
    - path: "tests/orchestrators/plugin/cross-surface-reason-parity.test.ts"
      provides: "typed-kind parity across reason-bearing consumers"
      contains: "workflows"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "byte fixtures for workflow inventory, rejection, and partial success"
      contains: "workflow-partial"
    - path: "docs/output-catalog.md"
      provides: "the exact workflow reason and three terminal-state contracts"
      contains: "{workflows}"
  key_links:
    - from: "tests/orchestrators/plugin/cross-surface-reason-parity.test.ts"
      to: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      via: "all surfaces narrow the same typed kinds through `narrowUnsupportedKinds`"
      pattern: "narrowUnsupportedKinds"
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "each fixture key matches a `catalog-state` marker and compares renderer bytes"
      pattern: "workflow-(partially-available-inventory|install-rejection|partial-install-success)"
---

<objective>
Close every reason and terminal-output surface, then run the phase boundary.

D-106-04 requires one reason mapping and stable tail ordering. The UI contract
requires the existing terminal grammar, statuses, severity, hints, indentation,
and reload rules. This plan adds tests and catalog states around the production
path delivered by Plan 01.

Purpose: make output drift fail at the shared classifier and byte-equality
catalog boundaries.

Output: reason-parity regressions, three catalog fixtures, three catalog
states, and complete phase-gate evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows-detection/ROADMAP.md
@.planning/workstreams/workflows-detection/REQUIREMENTS.md
@.planning/workstreams/workflows-detection/STATE.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-CONTEXT.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-RESEARCH.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-PATTERNS.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-VALIDATION.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-UI-SPEC.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-01-SUMMARY.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-02-SUMMARY.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-03-SUMMARY.md

<interfaces>
- `narrowUnsupportedKinds` is the only typed unsupported-kind reason mapper.
- `REASONS` is append-only and has 40 members after Plan 01.
- Catalog UAT walks `catalog-state` markers and fixture keys in both directions.
- `notify` remains the only terminal renderer and computes severity plus reload trailers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lock workflow reason dedupe, order, and cross-surface parity</name>
  <files>tests/shared/probe-classifiers.test.ts, tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</files>

  <read_first>
    - Read both listed test files before editing them.
    - Read the current special-kind, duplicate, and mixed-order assertions in `probe-classifiers.test.ts`.
    - Read `PER_KIND_PARITY_CASES`, the multi-kind parity case, and `narrowResolverReasons` use in the cross-surface suite.
    - Trace `narrowUnsupportedKinds` and `narrowResolverReasons` callers. Confirm that the production mapper from Plan 01 is the only workflow branch.
  </read_first>

  <behavior>
    - Empty input returns no reason and one workflow kind returns exactly one workflow reason.
    - Duplicate workflow kinds return one reason.
    - Workflow combined with LSP or a generic kind preserves canonical order and places workflow last.
    - Typed list/info output and install-note output agree for the same kind list.
  </behavior>

  <action>
Add the single, empty, duplicate, and mixed workflow cases to the shared
classifier suite. Assert exact arrays. Required mixed outputs are
`["lsp", "workflows"]` and `["unsupported component", "workflows"]`.

Add one workflow row to `PER_KIND_PARITY_CASES`. Add a multi-kind case that
compares list/info output with install reason output. Use the typed unsupported
list as the policy source. Do not add a local workflow mapper or source-specific
reason.

Commit both tests with `test: lock workflow reason parity` after the focused
test, secret scan, and pre-commit gates pass.
  </action>

  <verify>
    <automated>node --test tests/shared/probe-classifiers.test.ts tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem tests/shared/probe-classifiers.test.ts tests/orchestrators/plugin/cross-surface-reason-parity.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files tests/shared/probe-classifiers.test.ts tests/orchestrators/plugin/cross-surface-reason-parity.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Empty, single, duplicate, and mixed workflow arrays have exact expected outputs.
    - Existing reason positions stay unchanged and workflows is last in combined outputs.
    - Typed-kind and install-note paths return byte-identical reason arrays.
    - Every per-kind surface uses the shared classifier.
    - No production file or orchestrator-local workflow branch changes in this task.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>All reason-bearing surfaces share one deduplicated and ordered workflow reason.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bind exact workflow terminal states to the catalog and close the phase</name>
  <files>tests/architecture/catalog-uat.test.ts, docs/output-catalog.md</files>

  <read_first>
    - Read both listed files before editing them.
    - Read the catalog UAT fixture map, both walk-direction tests, and the nearest partial inventory plus rejection fixtures.
    - Read the reasons-rendering section and partial install states in `docs/output-catalog.md`.
    - Read the three byte examples and all eight explicit UI rows in `106-UI-SPEC.md`.
  </read_first>

  <behavior>
    - Inventory renders `(partially-available) {workflows}` with info severity and no hint.
    - Normal rejection renders the existing error summary, exact row, and install partial hint without reload output.
    - Partial success renders `(partially-installed) {workflows}` and the existing reload trailer when supported artifacts changed.
  </behavior>

  <action>
Add fixtures named `workflow-partially-available-inventory`,
`workflow-install-rejection`, and `workflow-partial-install-success`. Add
matching catalog markers and exact byte blocks from `106-UI-SPEC.md`. Update the
stale reason-count prose to 40 and name `{workflows}` as a dedicated reason.

Preserve the existing summary, severity, hints, indentation, blank lines,
grouping, status tokens, description truncation, and reload triggers. Add no
workflow icon, heading, prompt, trailer, wrapping rule, or second confirmation.

Run the focused architecture tests. Run the phase boundary commands after this
task commit. If a formatter changes a committed path, scan and commit that
specific correction before the final full commands. Use
`docs: catalog workflow output states` for the task commit.
  </action>

  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem tests/architecture/catalog-uat.test.ts docs/output-catalog.md --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files tests/architecture/catalog-uat.test.ts docs/output-catalog.md</automated>
  </verify>

  <acceptance_criteria>
    - Every new fixture has one matching catalog marker, and both catalog walk directions pass.
    - The three byte blocks match the UI contract exactly before the host TUI adds display padding.
    - Existing glyphs, statuses, summary text, partial hints, indentation, and reload rules remain unchanged.
    - All eight UI-consideration rows in this plan's truths have direct fixture, existing-regression, or unchanged-contract evidence.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>The catalog binds the exact workflow output, and the complete phase gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Typed resolver kind to user-visible reason | A closed kind becomes terminal text on every reason-bearing command. |
| Renderer to catalog contract | Programmatic fixtures must match documented bytes in both directions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-05 | Repudiation | reason mapper and command surfaces | medium | mitigate | Pin per-kind and multi-kind equality through the shared classifier. |
| T-106-08 | Spoofing | terminal status and reason grammar | medium | mitigate | Keep closed statuses unchanged and bind three exact workflow states to catalog fixtures. |
| T-106-09 | Tampering | closed-set tuple maintenance | low | accept | The existing exact-length and type-coverage gates make accidental drift visible. No additional gate is necessary. |
</threat_model>

## Artifacts this phase produces

This phase creates no production file. It adds these symbols and contract IDs:

- `UNSUPPORTED_COMPONENT_FIELDS.workflows`.
- The `UNSUPPORTED_COMPONENT_KINDS` tail member and fixed workflow directory convention.
- The `UnsupportedReason` workflow member and shared mapping.
- The `REASONS` workflow tail member and unsupported topic-group member.
- Fixture-only workflow seed support in `install.test.ts`.
- Catalog states `workflow-partially-available-inventory`, `workflow-install-rejection`, and `workflow-partial-install-success`.

## Source coverage audit

| SOURCE | ID | Feature or constraint | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | - | Identify workflow-bearing plugins and install supported pieces with explicit partial consent | 01-04 | COVERED |
| REQ | WDET-01 | Admit declarations in both schemas | 02 | COVERED |
| REQ | WDET-02 | Detect literal workflow directories and named layouts | 01, 02 | COVERED |
| REQ | WDET-03 | Resolve partial and record the kind | 01, 02 | COVERED |
| REQ | WDET-04 | Show exact reason on every surface | 01, 04 | COVERED |
| REQ | WDET-05 | Reject normally and admit with partial consent | 01, 03 | COVERED |
| REQ | WDET-06 | Do not materialize or execute workflows | 01, 03 | COVERED |
| RESEARCH | R-01 | Extend the shared opaque schema field | 02 | COVERED |
| RESEARCH | R-02 | Append one kind and one fixed convention | 01 | COVERED |
| RESEARCH | R-03 | Map at the shared classifier boundary | 01, 04 | COVERED |
| RESEARCH | R-04 | Preserve install, state, and discovery boundaries | 01, 03 | COVERED |
| RESEARCH | R-05 | Update count lock and byte catalog together | 01, 04 | COVERED |
| RESEARCH | R-06 | Add no package, network path, or runtime workflow support | 01-04 | COVERED |
| CONTEXT | D-106-01 | Opaque top-level declarations | 02 | COVERED |
| CONTEXT | D-106-02 | Fixed directory presence and signal dedupe | 01, 02 | COVERED |
| CONTEXT | D-106-03 | Shared strict and loose detection | 02 | COVERED |
| CONTEXT | D-106-04 | Exact closed reason and stable order | 01, 04 | COVERED |
| CONTEXT | D-106-05 | Generic partial consent and compatibility-only persistence | 01, 03 | COVERED |
| CONTEXT | D-106-06 | Structural failure precedence | 01, 02 | COVERED |

Specless edge equality: 8 surfaced, 8 authored in plan truths, 0 flagged
assumptions. UI equality: 8 explicit rows, 8 authored in this plan, 0 silent
drops. Prohibition precision kept 0 bespoke items. Canon security breadcrumbs
appear in the threat models. The deterministic coverage detector returned
`detected: false`; this phase integrates no external service.

<verification>
- Run the focused tests after each task.
- Run `npm test` and `npm run test:integration` after the Wave 3 task commit.
- Run `npm run check` and `npm run test:e2e` as the phase boundary.
- Run a TruffleHog filesystem scan over every phase-modified path with `--results=verified,unknown --fail`.
- After that clean scan, run `SKIP=trufflehog pre-commit run --all-files`. Do not skip another hook.
- Confirm that `git status --short` is clean and every task commit uses an explicit pathspec.
</verification>

<success_criteria>
- Every WDET requirement has automated evidence.
- All eight specless edge truths and all eight UI rows have authored evidence.
- Every reason-bearing surface uses the exact `{workflows}` reason.
- Existing terminal bytes remain stable outside the one reason-tail addition.
- Full repository and pinned end-to-end test commands pass.
- No workflow bridge, resource field, discovery field, executor, or network operation exists.
</success_criteria>

<output>
Create `.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-04-SUMMARY.md` when done.
</output>
