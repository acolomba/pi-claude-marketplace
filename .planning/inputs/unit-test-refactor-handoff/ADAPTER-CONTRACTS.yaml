{"schema_version": 1, "purpose": "Behavior shared by each production adapter and its test double.", "layout_policy": "Replay the behavior. Reconsider the shared helper and fake layout under the new unit-test guidelines.", "contracts": [{"id": "git-ops", "disposition": "replay", "port": "GitOps", "production": "DEFAULT_GIT_OPS", "current_fake": "createFakeGitOps", "operations": ["clone", "fetch", "forceUpdateRef", "checkout", "resolveRef", "currentBranch", "resolveRemoteRef"], "capabilities": {"missing_values": "Missing local and remote refs reject. A detached HEAD returns undefined.", "aliasing": "Not applicable. Results are void, immutable strings, or undefined.", "overwrite": "A second forceUpdateRef replaces the first object ID.", "ordering": "Clone, fetch, resolve, force-update, and checkout leave the new remote commit checked out.", "deletion": "Not applicable. GitOps has no delete operation.", "validation": "Protected ref names, invalid refs, and object IDs other than exactly 40 lowercase hexadecimal characters reject."}, "hermetic_boundary": "Use temporary repositories. A local upload-pack server is sufficient for remote operations.", "negative_control": "A fake that accepts an invalid ref or object ID must fail only the validation case.", "current_evidence": ["tests/helpers/git-ops-contract.ts", "tests/platform/git.test.ts", "tests/helpers/git-fake.test.ts", "tests/fixtures/adapter-contracts/broken-git-ops.probe.ts"]}, {"id": "credential-ops", "disposition": "replay", "port": "CredentialOps", "production": "DEFAULT_CREDENTIAL_OPS", "current_fake": "createFakeCredentialOps", "operations": ["fill", "approve", "reject"], "capabilities": {"missing_values": "An unknown host returns null.", "aliasing": "Approve inputs and fill results do not share mutable credential objects with stored state.", "overwrite": "A later approval replaces the credential for the same host.", "ordering": "Approve, reject, and approve leave the final approved credential.", "deletion": "Reject removes only matching credentials. Repeated rejection is harmless.", "validation": "Carriage return, line feed, and NUL in host, username, or password reject before mutation."}, "hermetic_boundary": "Use command-scope Git configuration and a temporary credential store. Never read developer credentials.", "negative_control": "A fake that returns its stored credential object by reference must fail only the aliasing case.", "current_evidence": ["tests/helpers/credential-ops-contract.ts", "tests/platform/git-credential.test.ts", "tests/helpers/credential-fake.test.ts", "tests/fixtures/adapter-contracts/broken-credential-ops.probe.ts"]}, {"id": "device-flow-http", "disposition": "replay", "port": "DeviceFlowHttp", "production": "DEFAULT_DEVICE_FLOW_HTTP", "current_fake": "createFakeDeviceFlowHttp", "operations": ["requestCode", "pollToken"], "capabilities": {"missing_values": "Each missing device_code, user_code, verification_uri, expires_in, or interval field rejects with TypeError.", "aliasing": "Repeated device-code and success results do not share mutable object references.", "overwrite": "Not applicable. DeviceFlowHttp has no write operation.", "ordering": "Poll results preserve pending-then-success FIFO order.", "deletion": "Not applicable. DeviceFlowHttp has no delete operation.", "validation": "Required device-code fields preserve their public string and number types."}, "hermetic_boundary": "Replace fetch for each test case. Do not use a live identity provider.", "negative_control": "A fake that returns its stored response object by reference must fail only the aliasing case.", "current_evidence": ["tests/helpers/device-flow-http-contract.ts", "tests/domain/github-auth.test.ts", "tests/helpers/device-flow-fake.test.ts", "tests/fixtures/adapter-contracts/broken-device-flow-http.probe.ts"]}], "current_participation": {"disposition": "reconsider", "inventory": "tests/helpers/adapter-contract-inventory.ts", "owners": {"git-ops": ["tests/platform/git.test.ts", "tests/helpers/git-fake.test.ts"], "credential-ops": ["tests/platform/git-credential.test.ts", "tests/helpers/credential-fake.test.ts"], "device-flow-http": ["tests/domain/github-auth.test.ts", "tests/helpers/device-flow-fake.test.ts"]}, "note": "The future design can colocate each contract with its corresponding test. It must still run the same behavior against production and test-double implementations."}}
