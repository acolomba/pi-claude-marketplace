{"schema_version": 1, "purpose": "Ordered input graph for a future branch and milestone. This file does not authorize restart work in the current worktree.", "future_context": {"branch": "new feature branch, not selected here", "milestone": "new milestone, not created here", "planning_policy": "Treat this kit as imported evidence. Do not import old phase status or plan completion."}, "steps": [{"id": "R-00", "name": "Import and authenticate the kit", "depends_on": [], "disposition": "replay", "inputs": ["BASELINE.yaml", "inputs/typescript-unit-testing-guidelines.md", "inputs/typescript-unit-testing-rule.md"], "action": "Copy the complete preservation directory into the new milestone. Check all recorded hashes before planning.", "acceptance": ["All required files exist", "The two new guideline copies match BASELINE.yaml", "The future branch base is recorded separately"]}, {"id": "R-01", "name": "Compare the new base with the oracle content base", "depends_on": ["R-00"], "disposition": "reconsider", "inputs": ["BASELINE.yaml", "TRANSFORMATIONS.yaml"], "action": "Classify changes already present on the new base. Do not replay an upstream change twice.", "acceptance": ["Each changed production path is present, obsolete, or still required", "The seven main-only commits at export time have a recorded disposition"]}, {"id": "R-02", "name": "Lock stable product contracts", "depends_on": ["R-01"], "disposition": "replay", "inputs": ["BEHAVIOR-CONTRACTS.yaml", "PUBLIC-SURFACE.yaml", "PERSISTENCE-CONTRACTS.yaml"], "action": "Convert replay contracts into the new milestone requirements before module or test design.", "acceptance": ["Every replay contract has a requirement", "The resolver discriminant conflict has an explicit decision", "No requirement names a private old helper", "Persistence formats have migration criteria"]}, {"id": "R-03", "name": "Replay product corrections", "depends_on": ["R-02"], "disposition": "replay", "inputs": ["DECISIONS.md", "ORACLE-SCENARIOS.md"], "action": "Implement or retain the eight named product corrections. Prove each through public behavior.", "acceptance": ["Skill preloads survive update", "Staging warnings survive update", "Hook diagnostics survive", "Reconcile entry failures stay isolated", "Device-flow HTTP stays replaceable"]}, {"id": "R-04", "name": "Design the production module graph again", "depends_on": ["R-02"], "disposition": "reconsider", "inputs": ["TRANSFORMATIONS.yaml", "DECISIONS.md"], "action": "Use the symbol map and intent commits as discovery evidence. Select responsibility boundaries without assuming the current partitions.", "acceptance": ["Every moved responsibility has an owner", "Production callers use the selected owners", "No export exists only for a test"]}, {"id": "R-05", "name": "Define the new corresponding-test map", "depends_on": ["R-04"], "disposition": "reconsider", "inputs": ["inputs/typescript-unit-testing-guidelines.md", "inputs/typescript-unit-testing-rule.md", "TRANSFORMATIONS.yaml"], "action": "Create one corresponding test path for every TypeScript source path, including type-only modules and barrels.", "acceptance": ["No source exception is inferred from the old rule", "Each test directly imports and calls its source where executable", "Type-only and barrel modules still have a corresponding test"]}, {"id": "R-06", "name": "Recreate high-value oracle tests", "depends_on": ["R-03", "R-05"], "disposition": "replay", "inputs": ["ORACLE-SCENARIOS.md", "DIRTY-CHECKPOINT.patch"], "action": "Implement each oracle with case-private setup and independent expected values.", "acceptance": ["Each oracle maps to one or more corresponding tests", "Temporary files are case-private", "No assertion targets a private constant"]}, {"id": "R-07", "name": "Recreate adapter parity", "depends_on": ["R-05"], "disposition": "reconsider", "inputs": ["ADAPTER-CONTRACTS.yaml"], "action": "Run each preserved behavior against its production port and test double. Select a layout that complies with the new helper rules.", "acceptance": ["All six capability categories are covered or have a reason", "Each implementation gets fresh state", "One negative control fails for each port"]}, {"id": "R-08", "name": "Build replacement enforcement", "depends_on": ["R-05", "R-06", "R-07"], "disposition": "reconsider", "inputs": ["DECISIONS.md", "inputs/typescript-unit-testing-guidelines.md"], "action": "Build small fail-closed checks for corresponding files, direct pair coverage, test-only exports, case isolation, and strong mock use.", "acceptance": ["Each check has a planted negative control", "No check relies on a baseline count", "The old sharded runner and exemption register are absent"]}, {"id": "R-09", "name": "Close the replacement milestone", "depends_on": ["R-08"], "disposition": "replay", "inputs": ["COMPLETENESS-REPORT.md"], "action": "Run the project quality gate and the replacement milestone verification. Compare the result with every preserved contract and oracle.", "acceptance": ["npm run check passes", "All replay contracts pass", "Every reconsider item has a recorded decision", "Every drop item is absent or replaced"]}]}
