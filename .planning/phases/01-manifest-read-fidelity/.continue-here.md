---
context: phase
phase: 01-manifest-read-fidelity
task: 5
total_tasks: 10
status: in_progress
last_updated: 2026-09-14T02:16:00.224Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **A green gate is not evidence the thing it gates is sound** — `npm run check` passed all nine links at every plan boundary while three manifest readers disagreed on every stat errno except `ENOENT`. The agreement gate that exists specifically to catch that was structurally incapable of it. Structural mitigation: before trusting any gate, check that it exercises the axis it claims to protect, and that its test does not inject a seam which makes the failing path unreachable.

- [ ] CONSTRAINT: **A scope fence resting on a severity claim needs executed evidence for that claim** — D-01-24 fenced the commands bridge out of this phase because the defect "would warn identically." It does not; it double-installs one source file under two names. The fence was honoured for three plans before the review falsified it. Structural mitigation: when a decision excludes work because the harm is small, run the harm before accepting the exclusion.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Vacuous gate | `manifest-read-agreement.test.ts` plants three trees that vary only presence/absence — every reader agrees on those. D-01-07's present-but-unusable rule, where the real divergence lives, was never planted across readers. The resolver's own D-01-09 case injects a `statKind` map seam so the stat CANNOT throw, making the one divergent path unreachable by construction. | blocking | Every new gate runs a negative control against the pre-fix tree AND plants at least one case per rule it claims to enforce — not one case per input category. |
| Premise-free scope fence | A locked decision excluded the commands bridge on an unverified severity claim. | blocking | A scope fence citing low severity must cite executed evidence, not reasoning. |
| Doc comment overclaiming reachability | `VERSION_PATTERN` documents `~1.2`, `>=1.0.0 <2.0.0`, `1.x`, `*`, `\|\|` as "every semver range form upstream accepts". Those forms can only arrive through the OBJECT shape — `RANGE_MARKER` is the literal `"@^"`, so a bare string carrying any other range drops the whole element silently. | advisory | Doc comments on a pattern state which call paths can actually reach it. |
| Trusting harness LSP diagnostics | Mid-session diagnostics reported `Cannot find module '.../domain/dependencies.ts'` for a file that existed and typechecked clean. | advisory | `npm run typecheck` is the authority; harness `.ts` diagnostics in this repo are known-spurious. |

<current_state>
Phase 1 of milestone v1.20 (5 phases). All four plans are EXECUTED and committed —
29 commits since `8ffd155b`, full 9-link `npm run check` green at every plan
boundary, and three of the four plans negative-controlled their new gates against
the pre-fix tree.

Code review has RUN and found **2 critical, 5 warning, 4 info**. **No fix has been
applied.** That is the entire reason this is a pause and not a completion: the
suite is green and the phase is not sound.

The phase currently ships a functional regression (CR-01) and a three-way reader
disagreement (CR-02), both reproduced by executing the shipped code.
</current_state>

<completed_work>

- Discuss — 33 decisions captured interactively, then a 34th recorded at pause
- Research — `01-RESEARCH.md`, HIGH confidence, executed against the real modules
- Pattern mapping — `01-PATTERNS.md`, 11 files classified, 11/11 analogs found
- Validation strategy — `01-VALIDATION.md` seeded from research
- Planning — 4 plans, 10 tasks, 2 waves; plan-checker VERIFICATION PASSED
- Gates — requirements 7/7, decisions 33/33, post-planning gap analysis 40/40
- Execution — 4/4 plans:
  - 01-01 `4c261338` — `MANIFEST_CANDIDATES`, both readers rewired
  - 01-02 `a456086e` — dependency parser, info render, catalogued byte form
  - 01-03 `792b3c0d` — canonical component paths, `seenByDir` skill dedup
  - 01-04 `f32270fd` — third reader, agreement gate widened
- Code review — 17 files at standard depth, `01-REVIEW.md` written (UNCOMMITTED until this pause commit)
</completed_work>

<remaining_work>

**Immediately actionable (no decision needed):**
- CR-01 — give `discoverPluginCommands` the same collapse `skills` got; it already keys `seenByFile` on the absolute source file, so warn-and-install-anyway becomes skip. Extend `declared-component-path-overlap.test.ts` with a commands case. Authorised by D-01-34.
- CR-02 — reconcile the three readers' errno policy. Today: `readManifest` keeps its stat outside the `try` and `defaultStatKind` rethrows non-`ENOENT`; the version reader treats non-`ENOENT`/`ENOTDIR` as PRESENT; info's reader adds `EISDIR` to the absent set. Also fix the two false doc comments (`shared.ts:900` "Mirrors the stat gate in readManifest"; `resolver.ts:655` "unreadable, unparseable and schema-invalid are one rule").
- WR-02 — validate `declaringMarketplace` at the substitution point; export the token test from `domain/dependencies.ts` so both sides share one rule.
- WR-03 — add the length bound (`{1,64}`). **Do NOT drop the space from `VERSION_PATTERN`** — D-01-33 admits it deliberately for compound ranges.
- WR-04 — `seenByDir` should be a `Set<string>`, not a `Map` whose values are never read; hoist the repeated `path.resolve`.
- WR-05 — add cross-reader cases for the present-but-unusable axis, with NO injected seams. This is the gate that should have caught CR-02.
- WR-01 part 1 — correct the `VERSION_PATTERN` doc comment; add a test pinning `"foo@~1.0.0"` to whichever behavior is chosen.

**Blocked on an operator decision:**
- WR-01 part 2 — see `<blockers>`.

**Then:**
- `verify_phase_goal` (gsd-verifier) → `update_roadmap` → tick MANF-01..05 in REQUIREMENTS.md
- Phases 2-5 of v1.20
</remaining_work>

<decisions_made>

All 34 live in `01-CONTEXT.md` as `D-01-01`..`D-01-34`. **Read them in numeric
order and honour the supersede markers** — four decisions were overturned during
the session and a resuming agent that acts on the superseded text will build the
wrong thing:

- `D-01-18` **[SUPERSEDED by D-01-32]** — `info` prefers the plugin's own `plugin.json`, entry is the fallback. Upstream cross-checks the entry against `plugin.json` and warns when they disagree, treating the entry as a mirror that can go stale.
- `D-01-19` **[MOOT under D-01-32]** — no display-vs-resolution split left to record.
- `D-01-24` **[SUPERSEDED by D-01-34]** — the phase DOES widen to the commands bridge.
- `D-01-25` **corrected by D-01-33** — the allowlist extends to `version` and `sha`.

Upstream parity was verified by reading the compiled schema out of the installed
Claude Code 2.1.251 binary, not from docs. Three findings contradict the in-repo
spike (`.planning/spikes/004-claude-plugin-dependency-spec/README.md`), which is
now known-wrong on the bare-string contract:
- dependency entries get a POSITIVE allowlist (`^[A-Za-z0-9][-A-Za-z0-9._]*$`), stricter than upstream's own plugin-name check
- a `sha` constraint channel exists alongside `version`, both read out-of-band from the raw manifest
- the bare-string form may carry `@marketplace` AND a trailing `@^range`

Duplicate handling (D-01-31) is last-wins WITHIN one manifest, matching upstream's
`mze`. **Phase 3 must not copy that rule** — upstream accumulates and INTERSECTS
ranges across manifests and raises `range-conflict` on an unsatisfiable set.
</decisions_made>

<blockers>

- **WR-01 part 2 (needs operator decision).** `RANGE_MARKER` is the literal `"@^"`, so only caret ranges split off a bare string. `"foo@~1.0.0"` parses as name=`foo`, marketplace=`~1.0.0`, fails the token allowlist, and D-01-05 drops the whole element. When every element is unusable the line is omitted entirely — byte-identical to a plugin declaring no dependencies at all, on a phase whose stated purpose is to stop discarding declared constraints.

  Three options were posed and none chosen: (a) add a closed-set reason token so `info` says the dependencies were unreadable — costs a catalog state + FIXTURES entry in one commit; (b) document the limit and keep the silence; (c) widen the range marker to `@` + any version-leading character — rescues the common forms but diverges from upstream's caret-only regex.

  The operator asked a clarifying question about version handling and the question was never re-posed. **Part 1 is not blocked** and can land independently.

- **ROADMAP criterion 3 is honestly unproven** (not blocking, but do not let it auto-pass). The gate pins `ui5` and `ui-theme-designer` by their DECLARED SHAPES as fixtures; the criterion is worded against the real plugins, which needs a network install. 01-03 marked it `human_judgment: true` rather than claim it.
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-manifest-read-fidelity/01-REVIEW.md` — the 11 findings, with executed reproductions for both criticals. Start here; it is why this pause exists.
2. `.planning/phases/01-manifest-read-fidelity/01-CONTEXT.md` — 34 decisions, four with supersede markers. Numeric order, honour the markers.
3. `.planning/phases/01-manifest-read-fidelity/01-RESEARCH.md` — Pitfall 2 (the `check` chain is 9 links, not 6) and Pitfall 3 (the pairing gate dictates where new files may live).
4. `CLAUDE.md` — the commit protocol below is not optional.
5. `.planning/HANDOFF.json` — machine-readable form of this file.

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: Treating `npm run check` green as proof the phase is sound → the review found 2 criticals in a fully green tree. Run the review before believing the suite.
- [ANTI-PATTERN]: Writing a gate whose cases all vary the same axis → plant one case per RULE, and negative-control it against the pre-fix tree.
- [ANTI-PATTERN]: `git add -A` / `git add .` → the operator edits files concurrently in this same checkout. Stage explicit paths only.
- [ANTI-PATTERN]: `--amend` to recover from a failed pre-commit hook → a failed hook means the commit did not happen; amending rewrites the PREVIOUS commit.

## Infrastructure State

- **This checkout is a LINKED GIT WORKTREE.** Main checkout is `/home/acolomba/pi-claude-marketplace`; `.git` here is a FILE. Two consequences that bite every session:
  1. **TruffleHog's pre-commit hook structurally cannot run** — it is a git-mode scan and aborts with `failed to read index file: … not a directory`. Confirm cleanliness by the filesystem route CLAUDE.md documents, then commit with `SKIP=trufflehog`. Never extend `SKIP=` further; never `--no-verify`.
  2. `execute-plan.md` auto-detects "worktree mode" from that same `.git`-is-a-file test and skips STATE.md/ROADMAP.md updates even under `isolation: none`. That is correct here — the orchestrator owns those writes — but it looks like a bug worth working around, and it is not.
- **No pre-commit hook is installed locally** — run `pre-commit run --files <paths>` yourself BEFORE committing.
- Branch `features/manifest`, not protected. `isolation: none`, `use_worktrees: false` — executors run sequentially in this one tree.
- `.planning/milestone.lock` holds THIS session's id and pid. After the pause it is stale; a later `milestone` verb may need it cleared.
- `.planning/state.json` is untracked and stale (`phases: []`, "Project initialized"). Unrelated to this work, left alone.
- `.planning/STATE.md` is modified-uncommitted from `state.begin-phase`, and still carries a stale v1.19 block reading "Next: milestone v1.19 close-out".
- Harness LSP diagnostics on `.ts` are known-spurious here. `npm run typecheck` is the authority.

## Steps SKIPPED this session (deliberate, not forgotten)

- **UI-SPEC generation** — the `ui-plan-gate` fired `frontend: true` on `matchedToken: "component"`, matching the phrase "one normalized component path" and the `ui5` / `ui-theme-designer` plugin names. `hasFrontendEvidence: false`, `block: false`. Pure backend phase; `--skip-ui` passed to the planner. Expect the same false positive on later phases.
- **Structural fallow pre-pass in code review** — `fallow_enabled: false` in the review init. The full `npm run fallow` still ran inside `npm run check` at every plan boundary.
- **`--fix` was never dispatched** — the review's findings are entirely unaddressed.
- **Codebase-drift remap** — the drift check reported 68 "new directories" with `last_mapped_commit: null`, flagging long-standing root files as new. Advisory only, `block: false`; `.planning/codebase/*.md` accurately describes `extensions/`.

<context>
The shape of this session was: every time a gate went green, the interesting
finding came from somewhere else. The plan-checker passed cleanly and the
executors reported full-suite green at every boundary — and the two defects that
actually matter were found by the code review executing the shipped code, not by
any gate. One of them (CR-02) is invisible to the very gate written to catch it.

The research and review agents were both worth more than their cost, and both
earned that by RUNNING things: research reproduced the `ui-theme-designer`
warning counts against the real modules before planning, and the review
reproduced both criticals the same way. Prompts that ask an agent to execute
rather than reason are what produced every non-obvious finding here.

Four decisions were overturned mid-session as evidence arrived. The supersede
markers in CONTEXT.md are load-bearing — a resuming agent that reads D-01-18 or
D-01-24 without its marker will implement the opposite of what was decided.
</context>

<next_action>
Start with: read `01-REVIEW.md`, then dispatch `gsd-code-fixer` for CR-01, CR-02,
WR-02, WR-03 (length bound ONLY — keep the space in `VERSION_PATTERN` per
D-01-33), WR-04, WR-05, and WR-01 part 1. Hold WR-01 part 2 until the operator
answers the question in `<blockers>`. Then `verify_phase_goal`, `update_roadmap`,
tick MANF-01..05 in REQUIREMENTS.md, and continue to phase 2.
</next_action>
