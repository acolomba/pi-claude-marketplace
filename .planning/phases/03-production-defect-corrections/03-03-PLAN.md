---
phase: 03-production-defect-corrections
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts
  - extensions/pi-claude-marketplace/bridges/agents/types.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/shared.test.ts
autonomous: true
requirements: [PDEF-01]
estimate:
  tokens: 41000
  raw_tokens: 41000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Update and reinstall preview and live staging consume all resolved agent directories with the same ordered first-wins warning behavior as install (D-04, D-05, D-07)."
    - "The obsolete singular agent-directory field and `pickAgentsSourceDir` helper no longer exist after all consumers migrate (D-06)."
    - "Commands and skills retain their existing discovery and staging behavior (D-03)."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts
    - extensions/pi-claude-marketplace/bridges/agents/types.ts
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  key_links:
    - "discoverGeneratedNames.agentsDirs -> update/reinstall conflict preview -> prepareStagePluginAgents.agentsDirs."
    - "Final source surface contains only the promoted ordered-list representation for resolved agent directories."
  assumptions:
    - statement: "PDEF-01 null input is represented as an empty resolved path list throughout update and reinstall; this is flagged because no phase SPEC defines null separately."
      status: flagged-unverified
  prohibitions:
    - statement: "Preview and live staging must not derive agent inputs from different representations or directory subsets."
      source: flagged-unverified
      status: unresolved
      verification: null
---

<objective>
Complete the multi-directory agent correction across update and reinstall, then remove the singular representation per D-04 through D-07.

Purpose: Close BA-015 across every production consumer without expanding into deferred module splits.
Output: Update/reinstall regressions and a single canonical ordered-list agent-path contract.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-defect-corrections/03-02-SUMMARY.md
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@.planning/phases/03-production-defect-corrections/03-PATTERNS.md
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate update preview and staging to every resolved agent directory</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, tests/orchestrators/plugin/update.test.ts, .planning/phases/03-production-defect-corrections/03-02-SUMMARY.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, tests/orchestrators/plugin/update.test.ts</files>
  <behavior>
    - "Update conflict preview sees generated agents from every resolved directory."
    - "Three-phase update stages the identical ordered list and retains first-wins warnings across directories (D-05, D-07)."
    - "Empty and single-directory updates preserve current outcomes."
  </behavior>
  <action>Replace all update-side consumption of the singular discovery member with `agentsDirs`. Pass the same ordered list through preview, prepare, commit, and rollback bookkeeping without recomputing it. Add a hermetic two-directory case whose second directory changes both conflict detection and live staging, plus a cross-directory duplicate warning. Do not split `update.ts`, broaden cleanup reporting, or change command/skill behavior; those boundaries are outside this task per D-03.</action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/update.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, preview/staging disagree, a later directory is omitted, or update direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The update regression fails when the old singular member is restored and covers empty, single, multiple, and duplicate agent paths.</acceptance_criteria>
  <done>Update uses the promoted ordered-list representation end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate reinstall preview and staging to every resolved agent directory</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts, .planning/phases/03-production-defect-corrections/03-02-SUMMARY.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts</files>
  <behavior>
    - "Reinstall preview includes agents from later resolved directories."
    - "Reinstall staging consumes the same ordered list and emits existing duplicate warnings."
    - "Rollback snapshots cover artifacts created from every directory without weakening containment guards."
  </behavior>
  <action>Replace reinstall-side singular agent directory plumbing with the `agentsDirs` contract from Plan 03-02. Add a hermetic fixture with two directories and a declared/conventional duplicate, proving both preview and materialization use the identical list. Preserve reinstall transaction boundaries, rollback snapshots, containment checks, and notification semantics. Do not alter the already explicit notification cardinality in `reinstall.messaging.ts`.</action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, the second directory is absent, rollback omits a staged artifact, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The direct reinstall suite fails on singular-directory plumbing and passes with ordered multi-directory preview/staging and safe rollback.</acceptance_criteria>
  <done>Reinstall consumes all resolved agent component paths without changing transaction or safety behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the demoted singular agent-path representation</name>
  <read_first>extensions/pi-claude-marketplace/bridges/agents/types.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts, extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts, tests/orchestrators/plugin/shared.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/bridges/agents/types.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts, extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts, tests/orchestrators/plugin/shared.test.ts</files>
  <action>After install, update, and reinstall all consume `agentsDirs`, remove the compatibility alternative from StageAgentsInput, remove `DiscoveredGeneratedNames.agentsSourceDir`, delete `pickAgentsSourceDir` and its obsolete tests/comments, and keep only the list-valued path through the bridge. Update exact source comments to describe resolver order and multi-directory first-wins behavior. Do not remove unrelated helpers or pursue the deferred Phase 7 unused-type/member cleanup.</action>
  <verify>
    <automated>node --test tests/bridges/agents/types.test.ts tests/bridges/agents/stage.test.ts tests/orchestrators/plugin/discover-names.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/shared.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/agents/types.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/agents/stage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, any tracked source still references `agentsSourceDir` or `pickAgentsSourceDir`, or a changed owner misses direct coverage.</fails_when>
  </verify>
  <acceptance_criteria>Tracked production source has one ordered-list agent-path representation, with no singular helper/member or stale singular comment.</acceptance_criteria>
  <done>The multi-directory correction is complete across install/update/reinstall and the demoted representation is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| resolved plugin paths -> update/reinstall transaction | Package-controlled paths enter preview, staging, snapshot, and rollback workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-09 | Tampering | update/reinstall multi-directory staging | high | mitigate | Reuse resolver-owned paths and existing containment/symlink checks for every directory; regression-test rollback artifacts. |
| T-03-10 | Repudiation | duplicate agent selection | medium | mitigate | Preserve stable first-wins order and existing warnings across preview and live staging. |
| T-03-11 | Spoofing | ambiguous reconcile aliases | low | accept | No reconcile identity is changed in this plan; Plan 03-01 owns the fail-closed claim map. |
| T-03-12 | Information Disclosure | cleanup diagnostics | low | accept | This plan preserves existing diagnostics; the structured cleanup correction is isolated in Plan 03-07. |

## ASVS L1 Applicability and Completion Gate

| ASVS-L1 area | Applicability | Control and evidence |
|---|---|---|
| V5 Input Validation | Applies | Resolver-owned path lists retain containment and symlink validation through update/reinstall preview, staging, snapshot, and rollback. All three task commands verify T-03-09. |
| V2/V3/V4/V6 | Not applicable | This plan changes no authentication, session, authorization, or cryptographic boundary. |

Plan completion is blocked until all three task commands exit zero for T-03-09. Any preview/staging mismatch or path-guard regression is a blocking failure.
</threat_model>

<artifacts_this_phase_produces>
- The final `agentsDirs` contract is the sole source-to-bridge representation; the singular helper and member are removed.
</artifacts_this_phase_produces>

<verification>
Run all agent bridge and plugin owner suites together, then direct coverage for every changed production file. Treat a T-03-09 failure as blocking.
</verification>

<success_criteria>
BA-015 is closed for install, update, and reinstall with identical preview/live inputs and no surviving single-directory code path.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-03-SUMMARY.md` when done.
</output>
