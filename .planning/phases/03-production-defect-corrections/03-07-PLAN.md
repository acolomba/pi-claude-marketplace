---
phase: 03-production-defect-corrections
plan: 07
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - tests/shared/errors.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/marketplace/shared.test.ts
autonomous: true
requirements: [PDEF-05]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "StateLockHeldError maps to `lock held`; typed update and sibling errors map by class/discriminant/code, and known outcomes do not depend on message substrings (D-14, D-15, D-16, D-17)."
    - "Update cleanup keeps the primary failure unchanged and appends structured readonly phase/artifact/path context for every failed prepare/abort/commit/rollback cleanup, including commands and skills (D-18, D-19, D-20)."
    - "Successful cleanup emits no diagnostic; failed cleanup leaves the residual artifact and exact structured context visible in the error cause chain (D-21, D-22)."
  artifacts:
    - extensions/pi-claude-marketplace/shared/errors.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  key_links:
    - "Typed Error/discriminant/code -> owner-local reason narrowing -> stable ContentReason."
    - "Update cleanup operation -> readonly descriptor -> appended secondary cause/context, while the original Error remains primary."
  assumptions:
    - statement: "PDEF-05 empty/single/null input is interpreted at the reason list and cleanup-descriptor collection boundaries: empty means generic fallback/no cleanup diagnostic, single preserves one typed reason/descriptor, and null is not a valid typed collection value. This remains flagged because no SPEC defines it."
      status: flagged-unverified
    - statement: "PDEF-05 text equality means reason identity is based on class/discriminant/stable code, while diagnostic text is UTF-8 JavaScript string content retained for display only; no external encoding transform is introduced. This remains flagged because no SPEC defines text equality/encoding."
      status: flagged-unverified
  prohibitions:
    - statement: "Changing an Error message while preserving its typed identity must not change the selected user-facing reason."
      source: flagged-unverified
      status: unresolved
      verification: null
    - statement: "A cleanup failure must not replace or hide the primary update failure, and a successful cleanup must not create a diagnostic."
      source: flagged-unverified
      status: unresolved
      verification: null
---

<objective>
Replace message-based failure classification with typed reasons and preserve full structured update-cleanup diagnostics, per D-14 through D-22.

Purpose: Close OPLU-B-F01, OPU-F15, OPU-F26, OPU-F14, and OPU-F27 at their production owners.
Output: Stable typed failure routing plus structured cleanup context that preserves primary causality.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-defect-corrections/03-03-SUMMARY.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@.planning/phases/03-production-defect-corrections/03-RESEARCH.md
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify uninstall lock contention by typed error identity</name>
  <read_first>extensions/pi-claude-marketplace/shared/errors.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/shared/errors.test.ts, tests/orchestrators/plugin/uninstall.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/shared/errors.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/shared/errors.test.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <behavior>
    - "StateLockHeldError always maps to the stable `lock held` reason."
    - "Changing the error message does not change that reason."
    - "An unrecognized error follows the honest generic fallback without being relabeled unreadable."
  </behavior>
  <action>Add a message-variation regression, then make uninstall reason narrowing inspect `StateLockHeldError` directly. If a reusable stable code is needed by multiple owners, add a readonly literal code on the error class without making it mutable. Remove the known-error message-substring branch and keep the generic fallback honest. Preserve scope and lock-path fields, basename-only disclosure rules, and the original Error cause per D-14 through D-17.</action>
  <verify>
    <automated>node --test tests/shared/errors.test.ts tests/orchestrators/plugin/uninstall.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/errors.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, message variation changes the reason, lock contention maps to unreadable, generic failures claim a known reason, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The uninstall owner regression fails on the prior fallthrough and proves stable lock-held classification independent of wording.</acceptance_criteria>
  <done>Uninstall maps lock contention by typed identity and retains an honest generic fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove message-substring classification from update and sibling narrowers</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/marketplace/shared.test.ts, extensions/pi-claude-marketplace/shared/errors.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/marketplace/shared.test.ts</files>
  <behavior>
    - "Known update failures select reasons from existing error classes/discriminants/stable codes even when messages change."
    - "Marketplace sibling narrowing uses the same typed facts for unreadable, unparseable, and manifest absence."
    - "Unknown errors do not become a known reason because their message contains a keyword."
  </behavior>
  <action>Inventory the current known error constructors reaching `reasonsFromTypedError`, `narrowDirectFailReason`, and the sibling marketplace narrower. Route each known case by class, readonly discriminant, or stable literal code; add an owner-local typed error only where no existing producer fact can express a distinct outcome. Delete substring-based known classification and stale defensive comments. Add regressions that vary messages while preserving type and that inject keyword-bearing unknown Errors. Keep generic fallback wording and established reason order per D-14 through D-17.</action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/shared.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/update.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, a known type changes classification with wording, an unknown keyword message impersonates a known outcome, reason order changes, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>All terminally identified known classifications are typed, sibling behavior aligns, and message text is display-only.</acceptance_criteria>
  <done>OPU-F15 and OPU-F26 are closed without new string heuristics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve structured context for every failed update cleanup</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/shared/errors.ts, tests/orchestrators/plugin/update.test.ts, tests/shared/errors.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/shared/errors.ts, tests/orchestrators/plugin/update.test.ts, tests/shared/errors.test.ts</files>
  <behavior>
    - "Prepare, abort, commit, and rollback cleanup failures retain phase, artifact kind, path/identifier, and underlying Error for skills, commands, agents, and MCP artifacts."
    - "The original operational error remains the primary error; cleanup context is secondary and readonly."
    - "Successful cleanup adds no diagnostic, while failed cleanup leaves the expected residual artifact and exact context."
  </behavior>
  <action>Define a readonly structured cleanup descriptor in the shared error surface (or reuse an existing exact descriptor), then collect every cleanup failure in update across prepare/abort/commit/rollback for skills, commands, agents, and MCP. Append descriptors to the original error's cause/context without replacing it and without flattening them to strings. Ensure command and skill paths are included, eliminate discarded return values, and keep successful cleanup silent. Add exact error/context/filesystem assertions with case-owned temporary directories per D-18 through D-22. Do not create a new warning-channel notification.</action>
  <verify>
    <automated>node --test tests/shared/errors.test.ts tests/orchestrators/plugin/update.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/errors.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/update.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, any lifecycle/artifact cleanup loses context, a success emits context, the primary error changes, a failed artifact is absent, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The update owner suite independently forces cleanup failures for all four lifecycle phases and all artifact kinds, checks exact readonly descriptors and residual files, and fails on discarded diagnostics.</acceptance_criteria>
  <done>OPU-F14 and OPU-F27 are closed with primary-cause-preserving structured cleanup evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lock/config/filesystem errors -> user-facing reason | Operational failures cross from typed internals into stable content reasons. |
| cleanup subprocess/filesystem -> primary update failure | Secondary cleanup failures contribute diagnostics without superseding the primary cause. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-25 | Spoofing | message-based reason narrowing | high | mitigate | Classify known outcomes only by class/discriminant/stable code and test keyword-bearing unknown errors. |
| T-03-26 | Repudiation | update cleanup diagnostics | high | mitigate | Retain readonly phase/artifact/path descriptors and original causes; verify residual filesystem state. |
| T-03-27 | Information Disclosure | diagnostic paths | medium | mitigate | Preserve existing basename/redaction policy at user output while retaining structured internal context for diagnostics. |
| T-03-28 | Tampering | true transaction/containment guards | high | mitigate | Change only classification and diagnostic capture; retain transaction abort, containment, and state-write boundaries. |
| T-03-29 | Spoofing | ambiguous reconcile aliases | low | accept | This plan does not resolve canonical names; Plan 03-01 owns alias ambiguity. |
</threat_model>

<artifacts_this_phase_produces>
- A readonly cleanup-failure descriptor is added to `shared/errors.ts` only if the existing type cannot carry phase/artifact/path/cause; no new file is created.
</artifacts_this_phase_produces>

<verification>
Run all four focused owner suites and direct coverage for every changed production owner.
</verification>

<success_criteria>
All known failure reasons are typed and message-independent, and every failed update cleanup is visible as structured secondary context with unchanged primary causality.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-07-SUMMARY.md` when done.
</output>
