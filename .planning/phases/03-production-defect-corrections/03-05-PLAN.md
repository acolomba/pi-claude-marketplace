---
phase: 03-production-defect-corrections
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
  - extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts
  - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
  - tests/domain/components/hook-tool-names.test.ts
  - tests/domain/components/hooks/matcher.test.ts
  - tests/bridges/hooks/dispatch-exec.test.ts
autonomous: true
requirements: [PDEF-07]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Prototype-inherited keys such as `toString`, `constructor`, and `__proto__` are rejected as unsupported hook tool names and matcher selectors (D-23)."
    - "Synchronous dispatch no longer carries a no-producer non-dispatchable branch, while malformed input and path-safety guards remain intact (D-24, D-25)."
    - "Production code uses own-key checks or `Map` semantics without global prototype mutation, casts, ignores, or test-only exports (D-23, D-25)."
  artifacts:
    - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
    - extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
  key_links:
    - "Untrusted hook key -> own-key/Map membership -> typed supported value."
    - "BucketAEvent producer trace -> synchronous dispatch table index without an unreachable fallback branch."
  assumptions:
    - statement: "PDEF-07 has no classified SPEC edge set; the manual set is prototype-chain names, ordinary unsupported strings, every supported name, and the trace-proven no-producer dispatch branches authorized by DC-029/MFDEC01."
      status: flagged-unverified
  prohibitions: []
---

<objective>
Harden dynamic hook lookups against prototype-chain names and remove the terminal-ledger-authorized synchronous no-producer dispatch branch, per D-23 through D-25.

Purpose: Close DC-029 and the Phase 3 portion of MFDEC01 without weakening real trust-boundary guards.
Output: Own-key-safe lookups and synchronous dispatch control flow that matches current producers.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
@extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts
@extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject prototype-chain keys at both dynamic hook lookup owners</name>
  <read_first>extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts, extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts, tests/domain/components/hook-tool-names.test.ts, tests/domain/components/hooks/matcher.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts, extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts, tests/domain/components/hook-tool-names.test.ts, tests/domain/components/hooks/matcher.test.ts</files>
  <behavior>
    - "Every supported own key resolves to its current typed value."
    - "`toString`, `constructor`, `__proto__`, and an ordinary unknown string are unsupported in both owners."
    - "No test mutates `Object.prototype` and no production cast or test-only export is added."
  </behavior>
  <action>Add direct cases for supported keys and built-in prototype names, then replace truthy bracket lookup with `Object.hasOwn` plus a typed local retrieval, or a readonly `Map` if it better preserves the current public contract. Keep the existing unsupported result shape and caller error behavior. Do not add ignores, assertions that reinterpret an arbitrary string as a known key, mutable discriminator tables, or prototype mutation. This is the canonical prototype-key security mitigation for the phase (D-23, D-25).</action>
  <verify>
    <automated>node --test tests/domain/components/hook-tool-names.test.ts tests/domain/components/hooks/matcher.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, any prototype-chain name resolves as supported, a supported own key regresses, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>Both owner regressions fail on inherited property lookup and pass with own-key-safe behavior for all supported and adversarial keys.</acceptance_criteria>
  <done>Dynamic hook lookups reject prototype-chain names without weakening type safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the no-producer fallback from synchronous hook dispatch</name>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, tests/bridges/hooks/dispatch-exec.test.ts, extensions/pi-claude-marketplace/domain/components/hook-events.ts</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, tests/bridges/hooks/dispatch-exec.test.ts</files>
  <behavior>
    - "Every admitted BucketAEvent indexes its translator and dispatches through the existing path."
    - "Tests no longer fabricate a non-dispatchable BucketAEvent through a cast."
    - "Malformed payload, missing plugin, and unsafe-path guards remain unchanged."
  </behavior>
  <action>Use the terminal trace fact that the admitted BucketAEvent union equals the dispatchable table key set. Remove the `isDispatchableEvent` false branch only from `dispatch-exec`, delete the cast-created unreachable fixture, and index the translator table with the honest event type. Preserve all malformed-input, configuration, process, timeout, containment, and symlink guard branches; those have real untrusted producers and are not authorized for removal by D-24/D-25.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/dispatch-exec.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, a supported event fails dispatch, an actual trust-boundary guard disappears, a cast-only fixture remains, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The synchronous owner contains no no-producer non-dispatchable branch and its real safety and failure paths remain regression-covered.</acceptance_criteria>
  <done>Synchronous dispatch control flow matches the current event producer union.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hook JSON/tool selector -> internal lookup tables | Untrusted strings are used to select typed hook behavior. |
| admitted event -> process dispatch/rewake | Validated events cross into process execution and registry state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-17 | Elevation of Privilege | hook lookup tables | high | mitigate | Require own-key membership or Map lookup and regression-test prototype property names. |
| T-03-18 | Tampering | synchronous dispatch branch removal | high | mitigate | Remove only the complement proven empty by the current typed producer set; preserve malformed input, path, process, and lifecycle guards. |
| T-03-19 | Spoofing | ambiguous reconcile aliases | low | accept | Reconcile state is outside this plan; Plan 03-01 owns the one-to-one identity mitigation. |
| T-03-20 | Information Disclosure | cleanup diagnostics | low | accept | This plan changes no cleanup rendering; structured update cleanup is owned by Plan 03-07. |

## ASVS L1 Applicability and Completion Gate

| ASVS-L1 area | Applicability | Control and evidence |
|---|---|---|
| V5 Input Validation | Applies | Open-string selectors require own-key membership, and synchronous dispatch accepts only the producer-proven event union. Task 1 verifies T-03-17; Task 2 verifies T-03-18. |
| V2/V3/V4/V6 | Not applicable | This plan changes no authentication, session, authorization, or cryptographic boundary. |

Plan completion is blocked until the Task 1 command exits zero for T-03-17 and the Task 2 command exits zero for T-03-18.
</threat_model>

<artifacts_this_phase_produces>
- No new public files; dynamic lookup membership becomes own-key-safe and the synchronous no-producer branch is removed.
</artifacts_this_phase_produces>

<verification>
Run the three direct owner suites and direct coverage for every changed production source. Treat any T-03-17 or T-03-18 verification failure as blocking.
</verification>

<success_criteria>
Prototype-chain keys are unsupported, the synchronous authorized no-producer branch is gone, and all real trust-boundary guards remain test-covered.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-05-SUMMARY.md` when done.
</output>
