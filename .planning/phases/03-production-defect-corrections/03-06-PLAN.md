---
phase: 03-production-defect-corrections
plan: 06
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/transaction/rollback.test.ts
autonomous: true
requirements: [PDEF-08]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Install delegates rollback error shaping solely to transaction/rollback.ts `formatRollbackError` (D-11)."
    - "Containment errors and zero-partial failures preserve the original Error object with no partial framing; partial rollback wraps once with `cause` and raw rows (D-12)."
    - "Install transaction failure output and state rollback remain correct after duplicate local formatting is removed (D-13)."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - extensions/pi-claude-marketplace/transaction/rollback.ts
    - tests/orchestrators/plugin/install.test.ts
    - tests/transaction/rollback.test.ts
  key_links:
    - "runPhases failure -> formatRollbackError -> PluginFailedMessage cause/rollbackPartial -> renderer."
  assumptions:
    - statement: "PDEF-08 has no classified SPEC edges; the terminal rollback matrix is containment error, zero partials, one partial, and multiple partials, plus state restoration after failure."
      status: flagged-unverified
  prohibitions:
    - statement: "Install must not compose a second rollback summary or wrap an original error when the transaction reports no partial rollback rows."
      source: flagged-unverified
      status: unresolved
      verification: null
---

<objective>
Make install use the existing transaction-owned rollback error formatter as the sole rule, per D-11 through D-13.

Purpose: Close TXA-F001 while preserving ES-4 cause chains, containment safety, and install transaction behavior.
Output: One production call path and direct regressions for install plus the shared formatter contract.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-defect-corrections/03-02-SUMMARY.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@extensions/pi-claude-marketplace/transaction/rollback.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route install transaction failures through formatRollbackError</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/transaction/rollback.ts, tests/orchestrators/plugin/install.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/install.test.ts</files>
  <behavior>
    - "A containment failure surfaces the identical original Error and no rollback-partial rows."
    - "A failure with zero partials surfaces the identical original Error and no partial framing."
    - "A failure with partials surfaces one wrapper whose cause is the original and forwards each raw partial row exactly once."
    - "Failed install restores transaction state and a subsequent live install succeeds."
  </behavior>
  <action>Add install-owner regressions for the four rollback matrix cases, then import and call `formatRollbackError` at the failed `runPhases` boundary. Delete the install-local duplicate formatting logic and stale comments. Build PluginFailedMessage from the returned `error` and `rollbackPartials`; leave presentation to the existing renderer. Preserve containment bypass, state restoration, ledger order, and retryability exactly per D-11 through D-13.</action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, the original error identity changes without partials, a partial is lost/duplicated, state remains mutated, retry fails, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The direct install regression fails while local formatting remains and proves all four shared-formatter outcomes plus transaction retry.</acceptance_criteria>
  <done>Install has one rollback shaping path owned by `formatRollbackError`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the shared formatter containment and partial-row contract</name>
  <read_first>extensions/pi-claude-marketplace/transaction/rollback.ts, tests/transaction/rollback.test.ts</read_first>
  <files>tests/transaction/rollback.test.ts</files>
  <behavior>
    - "PathContainmentError and subclasses return the same Error with an empty partial list."
    - "Zero-partial ordinary errors return the same Error."
    - "One and multiple partials return one wrapper with cause identity and byte-equal raw descriptors."
  </behavior>
  <action>Expand the existing transaction owner suite only where the terminal matrix lacks direct evidence. Assert Error class/identity, cause, and exact structured rollback rows rather than display-message fragments. Keep fixtures hermetic and do not change the already-correct formatter implementation unless a new direct case exposes a real mismatch.</action>
  <verify>
    <automated>node --test tests/transaction/rollback.test.ts tests/orchestrators/plugin/install.test.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, containment or zero-partial cases wrap, partial cases lose cause/rows, or install diverges from the transaction contract.</fails_when>
  </verify>
  <acceptance_criteria>The transaction suite explicitly covers containment, zero, one, and multiple partials and the install suite consumes those exact semantics.</acceptance_criteria>
  <done>TXA-F001 is closed with the existing transaction helper as the sole tested production rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| failed filesystem transaction -> user-visible install failure | Containment errors and rollback diagnostics cross from transaction state into notification output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-21 | Repudiation | rollback error composition | medium | mitigate | Preserve original error identity when no partials exist and preserve every raw partial plus one cause wrapper otherwise. |
| T-03-22 | Tampering | containment guard | high | mitigate | Keep PathContainmentError bypass and prove it at install and transaction owners. |
| T-03-23 | Spoofing | ambiguous reconcile aliases | low | accept | Install rollback does not resolve aliases; Plan 03-01 owns that mitigation. |
| T-03-24 | Information Disclosure | cleanup diagnostics | low | accept | Only transaction rollback descriptors cross here; post-update cleanup context is owned by Plan 03-07. |
</threat_model>

<artifacts_this_phase_produces>
- No new symbols; `formatRollbackError` becomes the sole live install rollback formatter.
</artifacts_this_phase_produces>

<verification>
Run install and transaction rollback suites, then direct coverage for the sole changed production owner `install.ts`.
</verification>

<success_criteria>
The shared formatter has a production caller, duplicate install logic is gone, and install rollback/state behavior is regression-proved.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-06-SUMMARY.md` when done.
</output>
