---
phase: 03-production-defect-corrections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/types.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/bridges/agents/types.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/orchestrators/plugin/discover-names.test.ts
  - tests/orchestrators/plugin/install.test.ts
autonomous: true
requirements: [PDEF-01]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Install preview and agent staging consume every resolved `componentPaths.agents` directory in resolver order (D-04, D-06)."
    - "Duplicate generated agent names remain first-wins with an honest warning, including duplicates across different source directories (D-05)."
    - "The primary representation between discovery and staging is an ordered list of resolved component paths, not a singular agent directory (D-06)."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/agents/types.ts
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  key_links:
    - "MaterializablePlugin.componentPaths.agents -> discoverGeneratedNames -> StageAgentsInput.agentsDirs -> prepareStagePluginAgents."
    - "discoverPluginAgents preserves ordered first-wins output and warnings across all directories."
  assumptions:
    - statement: "PDEF-01 adjacency/equality is resolved as adjacent and non-adjacent duplicate names across ordered agent directories; path order is the stable tie-breaker per D-04/D-05."
      status: flagged-unverified
    - statement: "PDEF-01 empty and single-directory inputs are supported; null input is represented by an empty `componentPaths.agents` list because the resolver contract supplies arrays rather than null. This representation choice remains flagged because no SPEC states it."
      status: flagged-unverified
    - statement: "PDEF-01 stable ordering for equal elements is resolver order with first discovery retained and later equal names warned, per D-04/D-05."
      status: flagged-unverified
  prohibitions:
    - statement: "A later agent directory must not silently replace an earlier generated agent with the same name."
      source: flagged-unverified
      status: unresolved
      verification: null
---

<objective>
Promote ordered multi-directory agent paths through discovery, staging, and install so every resolved agent component participates, per D-04 through D-07.

Purpose: Close the install-side portion of terminal finding BA-015 without changing command or skill discovery.
Output: A list-valued bridge contract and end-to-end install regression for multiple agent directories.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@.planning/phases/03-production-defect-corrections/03-RESEARCH.md
@extensions/pi-claude-marketplace/bridges/agents/types.ts
@extensions/pi-claude-marketplace/bridges/agents/stage.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
</context>

<interfaces>
- `MaterializablePlugin.componentPaths.agents` is the resolver-owned ordered list.
- `discoverPluginAgents({ pluginName, agentsDirs })` already accepts multiple directories and owns first-wins warnings.
- Promote resolved component-path lists as the primary representation. Any singular field retained inside this plan is an execution-order compatibility seam only; Plan 03-03 removes it after the remaining callers migrate (D-06).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the staging bridge consume the canonical ordered agent-directory list</name>
  <read_first>extensions/pi-claude-marketplace/bridges/agents/types.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, tests/bridges/agents/types.test.ts, tests/bridges/agents/stage.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/bridges/agents/types.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, tests/bridges/agents/types.test.ts, tests/bridges/agents/stage.test.ts</files>
  <behavior>
    - "Two ordered agent directories are both discovered and staged."
    - "Equal generated names across directories keep the first artifact and emit the existing duplicate warning for the later artifact (D-05)."
    - "An empty list stages no agents and a one-element list preserves existing behavior."
  </behavior>
  <action>Change the StageAgentsInput contract and `prepareStagePluginAgents` flow to treat `agentsDirs` as the canonical ordered value and forward it unchanged to `discoverPluginAgents`. Add direct bridge regressions for empty, single, multiple, and cross-directory duplicate inputs. Preserve warning text, staging containment checks, destination naming, and discovery order. Keep compatibility with unmigrated update/reinstall callers through a typed internal input alternative during this plan sequence; do not expose two fields to downstream staging logic, and do not collapse the list.</action>
  <verify>
    <automated>node --test tests/bridges/agents/types.test.ts tests/bridges/agents/stage.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/agents/types.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/agents/stage.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, only the first directory is staged, duplicate order changes, warnings disappear, or either production owner misses direct coverage.</fails_when>
  </verify>
  <acceptance_criteria>The bridge accepts the ordered list, covers empty/single/multiple cases, and proves first-wins warning behavior across directories.</acceptance_criteria>
  <done>The agent staging bridge has one list-valued internal path and preserves all existing safety checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry all resolved agent directories through install preview and live staging</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/discover-names.test.ts, tests/orchestrators/plugin/install.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/discover-names.test.ts, tests/orchestrators/plugin/install.test.ts</files>
  <behavior>
    - "Conflict preview reports agent names from the second and later resolved directories."
    - "Live install stages agents from all resolved directories in the same order used by preview (D-07)."
    - "A duplicate declared agent plus conventional agent is included in the proof and retains first-wins plus warning semantics (D-05, D-07)."
  </behavior>
  <action>Replace `pickAgentsSourceDir` use in `discoverGeneratedNames` with the full ordered `resolved.componentPaths.agents` list and return that list as the primary discovery result. Update install preflight and live staging to consume the same list so conflict detection and materialization cannot diverge. Build hermetic fixtures with two distinct agent directories plus a declared/conventional duplicate. Preserve commands, skills, resolver precedence, and containment validation. Keep the singular result member only as the typed migration seam required by the still-unmigrated update/reinstall callers; Plan 03-03 deletes it after those consumers change.</action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/discover-names.test.ts tests/orchestrators/plugin/install.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/discover-names.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, preview or live staging omits a later directory, the two paths produce different names, or either owner misses direct coverage.</fails_when>
  </verify>
  <acceptance_criteria>A regression that fails on the old `pickAgentsSourceDir` path proves preview/live agreement across all directories and duplicate warning behavior.</acceptance_criteria>
  <done>Install resolves, previews, validates, and stages every ordered agent directory through the promoted list representation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| plugin package paths -> staged user files | Untrusted package-controlled component paths cross containment and duplicate-resolution checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05 | Tampering | multi-directory agent staging | high | mitigate | Preserve every existing containment/symlink guard for each directory and retain deterministic first-wins duplicate handling. |
| T-03-06 | Spoofing | duplicate generated agent names | medium | mitigate | Keep the first resolved artifact in resolver order and emit the existing warning for every later duplicate. |
| T-03-07 | Tampering | prototype-key lookup | low | accept | No dynamic dispatch table is changed here; canonical lookup mitigation is isolated in Plan 03-05. |
| T-03-08 | Information Disclosure | cleanup diagnostics | low | accept | This install staging slice does not change cleanup reporting; Plan 03-07 owns that boundary. |
</threat_model>

<artifacts_this_phase_produces>
- `StageAgentsInput.agentsDirs` and `DiscoveredGeneratedNames.agentsDirs` become the list-valued primary contract; no new files are created.
</artifacts_this_phase_produces>

<verification>
Run both bridge suites and both install-owner suites plus direct coverage for all four changed production owners.
</verification>

<success_criteria>
Install preview and staging consume all resolved agent paths, preserve ordering and warnings, and retain every existing trust-boundary guard.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-02-SUMMARY.md` when done.
</output>
