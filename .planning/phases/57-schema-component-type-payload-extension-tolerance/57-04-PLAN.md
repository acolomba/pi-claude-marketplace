---
phase: 57-schema-component-type-payload-extension-tolerance
plan: 04
type: execute
wave: 3
depends_on:
  - 57-01
  - 57-02
  - 57-03
files_modified:
  - tests/architecture/hooks-foundation.test.ts
  - tests/architecture/no-hooks-strict-additional-properties.test.ts
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
tags:
  - architecture-test
  - invariant
  - lockstep-guard

must_haves:
  truths:
    - "An architecture test pins `HOOK_CONFIG_SCHEMA` against `additionalProperties: true` (no strict gate anywhere) so a future contributor who slips `additionalProperties: false` into the hooks schema RED-fails CI before it lands (HOOK-03 forward-compat lockstep)"
    - "An architecture test pins `STATE_SCHEMA.schemaVersion` against `Type.Literal(1)` so the D-57-01 no-bump amendment is not regressed by a future contributor who reaches for `Type.Union([Type.Literal(1), Type.Literal(2)])` out of habit"
    - "An architecture test pins `PLUGIN_INSTALL_RECORD_SCHEMA.resources.hooks` against `Type.Array(Type.String())` (required, not Optional) so the additive migration discipline (D-57-01) is enforced"
    - "An architecture test pins `SUPPORTED_COMPONENT_KINDS` against the closed 4-tuple `[\"skills\", \"commands\", \"agents\", \"hooks\"]` AND pins `UNSUPPORTED_COMPONENT_KINDS` against a tuple that does NOT contain `\"hooks\"` (HOOK-01 admission invariant)"
    - "An architecture test pins the discriminated `installable: true | false` contract: a hook-only plugin with a valid `hooks/hooks.json` round-trips through `resolveStrict` AND `resolveLoose` to `installable: true`, AND TypeScript refuses to compile a hand-written caller that reads `pluginRoot` from a `notInstallable` variant (NFR-7 preserved across HOOK-01)"
    - "An idempotency test pins `ensurePluginResources` against double-apply: running the helper twice over a v1.12-shaped record returns `mutated: false` on the second pass (D-57-01 no-op-after-normalization invariant)"
  artifacts:
    - path: tests/architecture/hooks-foundation.test.ts
      provides: "Five invariant tests pinning the HOOK-01 / HOOK-02 / HOOK-03 contract surface against accidental regression"
    - path: tests/architecture/no-hooks-strict-additional-properties.test.ts
      provides: "One source-text gate test asserting the `hooks.ts` module never carries `additionalProperties: false` as a string literal anywhere (defense-in-depth against an `as never` cast smuggling strictness into the schema)"
  key_links:
    - from: tests/architecture/hooks-foundation.test.ts
      to: extensions/pi-claude-marketplace/persistence/state-io.ts
      via: "Static introspection of STATE_SCHEMA / PLUGIN_INSTALL_RECORD_SCHEMA via TypeBox's reflection API"
      pattern: "STATE_SCHEMA"
    - from: tests/architecture/hooks-foundation.test.ts
      to: extensions/pi-claude-marketplace/domain/resolver.ts
      via: "Tuple introspection of SUPPORTED_COMPONENT_KINDS / UNSUPPORTED_COMPONENT_KINDS"
      pattern: "SUPPORTED_COMPONENT_KINDS"
---

<objective>
Lock the four invariants HOOK-01 / HOOK-02 / HOOK-03 / D-57-01 with architecture-level tests, so subsequent phases (58–63) and any future hand-edit cannot regress the Phase 57 leaf-foundation contract by accident.

Purpose: Phase 57's load-bearing decisions (no schemaVersion bump, additive `resources.hooks: string[]`, lenient `additionalProperties: true` at every level of `HOOKS_CONFIG_SCHEMA`, discriminated `installable: true | false` resolver gate on parsed `hooks/hooks.json`) are all small textual diffs that an inattentive future contributor could revert in a single line. Architecture tests pin the invariants at CI time so the diff that reverts them is RED before it lands. Mirrors the existing architecture-test discipline at `tests/architecture/markers-snapshot.test.ts`, `tests/architecture/notify-grammar-invariant.test.ts`, etc.

Output: Two new architecture test files; no source-code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/57-schema-component-type-payload-extension-tolerance/57-CONTEXT.md
@.planning/phases/57-schema-component-type-payload-extension-tolerance/57-01-PLAN.md
@.planning/phases/57-schema-component-type-payload-extension-tolerance/57-02-PLAN.md
@.planning/phases/57-schema-component-type-payload-extension-tolerance/57-03-PLAN.md
@CLAUDE.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/persistence/state-io.ts
@extensions/pi-claude-marketplace/domain/components/hooks.ts
@extensions/pi-claude-marketplace/domain/resolver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the Phase 57 invariants in tests/architecture/hooks-foundation.test.ts</name>
  <read_first>
    - tests/architecture/markers-snapshot.test.ts (the closest analog — closed-set tuple pinning + frontmatter contract reading)
    - tests/architecture/notify-grammar-invariant.test.ts (TypeBox introspection pattern)
    - tests/architecture/config-state-write-seams.test.ts (the multi-file source-text gate pattern)
    - extensions/pi-claude-marketplace/persistence/state-io.ts (the exact `STATE_SCHEMA` / `PLUGIN_INSTALL_RECORD_SCHEMA` symbols to introspect)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (the `HOOKS_CONFIG_SCHEMA` and `HOOKS_VALIDATOR` symbols to introspect)
    - extensions/pi-claude-marketplace/domain/resolver.ts (`SUPPORTED_COMPONENT_KINDS`, `UNSUPPORTED_COMPONENT_KINDS`, `resolveStrict`, `resolveLoose`, `ResolvedPlugin` discriminated type)
  </read_first>
  <behavior>
    The test file at `tests/architecture/hooks-foundation.test.ts` carries five test blocks, each pinning one invariant:

    1. **"STATE_SCHEMA.schemaVersion stays Literal(1) (D-57-01: no bump)"** — introspect `STATE_SCHEMA` via TypeBox's reflection API (the schema JSON contains the literal value under the `schemaVersion` property's `const`); assert exact `const === 1` and assert the property is NOT a union of multiple literals.

    2. **"PLUGIN_INSTALL_RECORD_SCHEMA.resources.hooks is required Array(String) (HOOK-02 / D-57-01)"** — introspect the schema; assert `resources.hooks.type === "array"` and `resources.hooks.items.type === "string"`; assert `"hooks"` is in `resources.required` (typebox emits the `required` array on `Type.Object`); assert `resources` itself lists `hooks` alongside `skills`, `prompts`, `agents`, `mcpServers`.

    3. **"HOOKS_CONFIG_SCHEMA accepts unknown extension fields at every nesting level (HOOK-03 lenient stance)"** — call `HOOKS_VALIDATOR.Check` on a synthetic payload carrying unknown extension fields at the hook-entry level, at the hook-handler level, and at the top level (an unknown event key); assert ALL THREE return `true`. Additionally, introspect the JSON-Schema output of `HOOKS_CONFIG_SCHEMA` and assert NO sub-object anywhere in the tree carries `additionalProperties: false` (recursive walk of the schema JSON).

    4. **"SUPPORTED_COMPONENT_KINDS includes hooks AND UNSUPPORTED_COMPONENT_KINDS excludes hooks (HOOK-01 admission)"** — import both tuples; assert `SUPPORTED_COMPONENT_KINDS.includes("hooks")` is `true` and the tuple has exactly 4 members `["skills","commands","agents","hooks"]` (order is the locked closed set); assert `UNSUPPORTED_COMPONENT_KINDS.includes("hooks")` is `false`.

    5. **"resolveStrict and resolveLoose admit a hook-only plugin with valid hooks/hooks.json (NFR-7 + HOOK-01 end-to-end)"** — fixture: build a minimal `PluginEntry` (only `name` + `source`) and a `ResolveContext` where `statKind` returns `"dir"` for `<pluginRoot>` and `"file"` for `<pluginRoot>/hooks/hooks.json` (else `null`), and `readFileText` returns `'{"PreToolUse":[{"matcher":"Edit","hooks":[{"type":"command","command":"/bin/true"}]}]}'` for the hooks file (and the manifest file is `null`); call `resolveStrict(entry, ctx)`; assert `r.installable === true` and `r.supported.includes("hooks")` and `r.pluginRoot` is a non-empty string. Then call `resolveLoose(entry, ctx)` and assert the same. (NFR-7 type-level check: at the top of the file declare a `// @ts-expect-error` comment over a snippet that reads `r.pluginRoot` from a `ResolvedPluginNotInstallable` — the TS compiler must reject; this is a single-line trick covered in `tests/domain/resolver.types.test.ts`. The pattern there is the template; mirror it.)

    Each block uses `node:test` `test(...)` and `node:assert/strict`. Cite HOOK-01 / HOOK-02 / HOOK-03 / D-57-01 / D-57-02 / D-57-04 / NFR-7 in test titles where appropriate; do NOT use Phase / Plan / Wave references.
  </behavior>
  <action>
    Create `tests/architecture/hooks-foundation.test.ts` mirroring the style of `tests/architecture/markers-snapshot.test.ts`. Imports:

    - `test` and `beforeEach`/`afterEach` from `node:test`
    - `assert` from `node:assert/strict`
    - `STATE_SCHEMA` from `extensions/pi-claude-marketplace/persistence/state-io.ts`
    - `HOOKS_CONFIG_SCHEMA`, `HOOKS_VALIDATOR` from `extensions/pi-claude-marketplace/domain/components/hooks.ts`
    - `SUPPORTED_COMPONENT_KINDS`, `UNSUPPORTED_COMPONENT_KINDS`, `resolveStrict`, `resolveLoose` from `extensions/pi-claude-marketplace/domain/resolver.ts`
    - `PluginEntry` type and any necessary `ResolveContext` setup

    Write the five test blocks per the behavior list above. For the recursive `additionalProperties: false` walk in block 3, implement a small private helper inside the test file (no production code added). The function signature is: `function walkSchemaForStrictAdditionalProperties(schema: unknown, path: string[]): string[]` — returns an array of dotted paths where `additionalProperties: false` appears. The test asserts the return is `[]`.

    For the NFR-7 type-level check in block 5, add a `// @ts-expect-error -- NFR-7: pluginRoot is only readable on the installable variant` line above a snippet that attempts to read `pluginRoot` from the not-installable arm. The `@ts-expect-error` directive ensures the test file itself catches the regression (TypeScript fails the build if the compiler stops emitting the error).

    Cite by ID in test titles. Forbidden tokens (`Phase 57`, `Plan 04`, `Wave 3`, `Pitfall N`) must NOT appear.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/hooks-foundation.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>
    - `tests/architecture/hooks-foundation.test.ts` exists with 5 test blocks
    - All blocks GREEN
    - `grep -nE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]|Pitfall [0-9]+\b' tests/architecture/hooks-foundation.test.ts | grep -v '^[^:]*:[0-9]*:[[:space:]]*#'` returns nothing
    - `npm run typecheck` clean (the `@ts-expect-error` directive is honored)
  </done>
</task>

<task type="auto">
  <name>Task 2: Source-text gate on `additionalProperties: false` in hooks.ts + idempotency gate</name>
  <read_first>
    - tests/architecture/no-orchestrator-network.test.ts (the closest source-text-grep architecture-gate pattern)
    - tests/architecture/import-boundaries.test.ts (the file-list + readFileText + RegExp pattern)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (the file under guard)
    - extensions/pi-claude-marketplace/persistence/migrate.ts (`ensurePluginResources` — the idempotency target)
  </read_first>
  <behavior>
    Two tests in a single new file `tests/architecture/no-hooks-strict-additional-properties.test.ts`:

    1. **"domain/components/hooks.ts source never carries additionalProperties: false"** — read the source file as text; assert NO regex match for `additionalProperties\s*:\s*false`. (Defense-in-depth against an `as unknown as never` cast or a one-liner that slips strictness into the TypeBox schema. The architecture test in Task 1 already walks the introspected JSON-Schema; this is the textual partner gate.)

    2. **"ensurePluginResources is idempotent across a double-apply (D-57-01 invariant)"** — actually this fits BETTER in this new architecture test file because the existing `tests/persistence/migrate.test.ts` covers the behavior cases. The architecture-level pin reads the helper and verifies the property: build a synthetic v1.12-shaped record, run `ensurePluginResources` twice in sequence, assert the second call returns `mutated: false` and the record state is identical (deep-equal) to the post-first-call state.

    Mirror the style of `tests/architecture/no-orchestrator-network.test.ts` for the source-text grep test; use the standard `node:test` + `node:assert/strict` imports for the idempotency block. Cite HOOK-03 / D-57-01 in test titles by ID.
  </behavior>
  <action>
    Create `tests/architecture/no-hooks-strict-additional-properties.test.ts`.

    Block 1 (source-text gate):
    - `import { readFile } from "node:fs/promises";`
    - `import path from "node:path";`
    - In the `test(...)` body, compute the absolute path to `extensions/pi-claude-marketplace/domain/components/hooks.ts` from the test file's directory (mirror how `no-orchestrator-network.test.ts` resolves paths).
    - Read the file contents as UTF-8.
    - Strip comments before regex-matching: this is critical because a doc-comment could mention `additionalProperties: false` as part of explaining the HOOK-03 forward-compat stance and the test would false-positive. Strip line comments (`//` to end-of-line) and block comments (`/* ... */`). The regex pattern after stripping: `/additionalProperties\s*:\s*false/`. Assert NO match.
    - Title: `"hooks.ts source never carries 'additionalProperties: false' (HOOK-03 lenient stance)"`.

    Block 2 (idempotency):
    - Import the `migrateLegacyMarketplaceRecords` exported function (the public seam; `ensurePluginResources` is a module-internal helper so use the public seam to invoke it indirectly — feed in a parsed-state shape with one marketplace containing one plugin lacking `resources.hooks`; the public function calls `ensurePluginResources` internally and returns the `{ marketplaces, mutated }` result).
    - First call: assert `mutated === true` and `marketplaces["mp"].plugins["pl"].resources.hooks` equals `[]`.
    - Second call on the just-returned `{ marketplaces }` (passed back through as `parsed`): assert `mutated === false` and the deep shape is identical to the post-first-call state.
    - Title: `"migrateLegacyMarketplaceRecords is idempotent over the hooks default-fill (D-57-01)"`.

    Cite by ID; do NOT use Phase / Plan / Wave / Pitfall references.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/no-hooks-strict-additional-properties.test.ts</automated>
    <automated>npm run check</automated>
  </verify>
  <done>
    - `tests/architecture/no-hooks-strict-additional-properties.test.ts` exists with 2 test blocks
    - Both blocks GREEN
    - Comment-stripping is in place — adding a `// additionalProperties: false` line to `hooks.ts` does NOT trip the test (executor verifies by manual mental check, not by mutation)
    - `npm run check` GREEN
    - `grep -nE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]|Pitfall [0-9]+\b' tests/architecture/no-hooks-strict-additional-properties.test.ts | grep -v '^[^:]*:[0-9]*:[[:space:]]*#'` returns nothing
  </done>
</task>

</tasks>

<verification>
- `npm run check` GREEN
- The new test files together pin: D-57-01 (no schemaVersion bump + idempotency), HOOK-02 (resources.hooks shape), HOOK-03 (lenient additionalProperties), HOOK-01 (SUPPORTED tuple admission), NFR-7 (discriminated installable contract)
- A future commit that flips `Type.Literal(1)` to `Type.Union([Type.Literal(1), Type.Literal(2)])` RED-fails block 1 of Task 1
- A future commit that sets `additionalProperties: false` anywhere in `hooks.ts` RED-fails block 1 of Task 2
- A future commit that omits the `hooks` default-fill arm from `ensurePluginResources` RED-fails block 2 of Task 2
- A future commit that removes `"hooks"` from `SUPPORTED_COMPONENT_KINDS` RED-fails block 4 of Task 1
- A future commit that breaks the NFR-7 discriminated contract RED-fails the `@ts-expect-error` directive in block 5 of Task 1
</verification>

<success_criteria>
- Five HOOK-01 / HOOK-02 / HOOK-03 / D-57-01 invariants pinned at CI time
- One source-text defense-in-depth gate against an `additionalProperties: false` slip in `hooks.ts`
- One idempotency gate over the public migration seam
- `npm run check` GREEN
- No GSD-planning-artifact references introduced
</success_criteria>

<output>
Create `.planning/phases/57-schema-component-type-payload-extension-tolerance/57-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| 5 invariant tests | `tests/architecture/hooks-foundation.test.ts` | New architecture-gate file |
| 1 source-text gate + 1 idempotency gate | `tests/architecture/no-hooks-strict-additional-properties.test.ts` | New architecture-gate file |
