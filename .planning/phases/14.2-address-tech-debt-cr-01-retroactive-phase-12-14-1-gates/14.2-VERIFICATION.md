---
phase: 14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates
verified: 2026-05-24T03:00:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 14.2: Address tech debt (CR-01 + retroactive Phase 12 / 14.1 gates) Verification Report

**Phase Goal:** Close v1.3 milestone tech debt so `/gsd:complete-milestone v1.3` archives cleanly with zero outstanding gates: (a) replace 3 local user-first `scopeOrder` helpers with canonical `compareByNameThenScope` (CR-01, affecting CMC-03/25/26/27); (b) tighten the MSG-GR-3 lint rule from no-op meta-assertion to active AST detector; (c) run retroactive `/gsd:secure-phase` + `/gsd:validate-phase` against Phase 12 (existing draft VALIDATION.md) and Phase 14.1 (no prior artefacts); (d) flip 29 Phase-13 CMC checkboxes in ROADMAP.md Coverage and update the 14.1 Phase Details row.

**Verified:** 2026-05-24T03:00:00Z
**Status:** passed
**Re-verification:** No -- initial verification

---

## Goal Achievement

### Observable Truths (Phase-level Must-Haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CR-01 source closure: zero `function scopeOrder` declarations in `extensions/pi-claude-marketplace/orchestrators/`; all 3 cited files import and route through `compareByNameThenScope` | VERIFIED | `grep -rEn "function scopeOrder" extensions/pi-claude-marketplace/orchestrators/` returns no matches (exit 1). `grep -c compareByNameThenScope` for `reinstall.ts` = 6, `update.ts` = 4, `import/execute.ts` = 3. Each file has `import { compareByNameThenScope } from "../../presentation/sort.ts"` (reinstall.ts:54, update.ts:72, execute.ts:13). |
| 2 | `autoupdate.ts` iteration flipped to `["project", "user"]` (line 114) and stale doc comment at line 27 also reads project-first; no `["user", "project"]` remains in the file | VERIFIED | autoupdate.ts:27 + :114 both read `["project", "user"]`; no `["user", "project"]` occurrences in file. |
| 3 | `same-mp-both-scopes` byte-equal cross-scope fixture present in `catalog-uat.test.ts` for all 3 commands (reinstall / update / import) AND paired in `docs/output-catalog.md` | VERIFIED | `grep -c same-mp-both-scopes` returns 6 in `tests/architecture/catalog-uat.test.ts` (3 fixture registrations + 3 comment cites) and 3 in `docs/output-catalog.md` (one annotation per command section). Fixtures at lines 827, 1021, 1370 call `cascadeSummary` with project scope first, then user scope, joined via `appendReloadHint`. |
| 4 | MSG-GR-3 rule rewritten from no-op to active two-axis AST detector (FunctionDeclaration + ArrayExpression visitors); registered in MSG-Block 1 (orchestrator-scoped) only | VERIFIED | `tests/lint-rules/msg-gr-3-per-scope.js` defines `FunctionDeclaration` and `ArrayExpression` visitors with `isUserFirstScopeOrder` body matcher; emits `userFirstScopeOrder` and `userFirstScopeIteration` messageIds. No `Program: () => {}` no-op remains. RuleTester (`msg-gr-3-per-scope.test.js`) exercises 3 valid + 2 invalid cases (5 tests, all green). `eslint.config.js:171` registers the rule under MSG-Block 1 (orchestrator-scoped `files: ["extensions/pi-claude-marketplace/orchestrators/**/*.ts"]`); zero occurrences under the whole-extension `files: ["extensions/pi-claude-marketplace/**/*.ts"]` blocks. |
| 5 | Phase 12 retroactive gates produced canonical artefacts: `12-SECURITY.md` (status: passed, threats_open: 0); `12-VALIDATION.md` advanced from draft to validated (status: validated, nyquist_compliant: true, wave_0_complete: true) | VERIFIED | `12-SECURITY.md` frontmatter: `status: passed`, `threats_open: 0`, `threats_closed: 14`, `register_authored_at_plan_time: true`, `asvs_level: 1`. `12-VALIDATION.md` frontmatter: `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `audited: 2026-05-24`. Both files present at the canonical padded-phase filenames in `.planning/phases/12-messaging-foundations-renderer-primitives/`. |
| 6 | Phase 14.1 retroactive gates produced canonical artefacts: `14.1-SECURITY.md` (status: passed, threats_open: 0); `14.1-VALIDATION.md` (status: validated, nyquist_compliant: true, wave_0_complete: true) -- both NEW (no prior drafts) | VERIFIED | `14.1-SECURITY.md` frontmatter: `status: passed`, `threats_open: 0`, `threats_closed: 14`, `register_authored_at_plan_time: true`, `asvs_level: 1`. `14.1-VALIDATION.md` frontmatter: `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `audited: 2026-05-24`. Both files present at the canonical padded-phase filenames in `.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/`. |
| 7 | ROADMAP.md cosmetic drift closed: 29 Phase-13 CMC rows flipped Pending -> Complete in `## Coverage (v1.3)`; Phase 14.1 row in Phase Details reads `2/2 plans | Complete` | VERIFIED | `grep -c "\| Phase 13 \| Pending \|" .planning/ROADMAP.md` = 0; `grep -c "\| Phase 13 \| Complete \|" .planning/ROADMAP.md` = 29. Phase Details row at ROADMAP.md:259 reads `\| 14.1. Close gap: CMC-13 import propagation … \| 2/2 plans \| Complete \| 2026-05-24 \|`. |

**Score:** 7/7 truths verified.

---

### Required Artifacts (Level 1-3: Exists + Substantive + Wired)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` | Imports `compareByNameThenScope`; local `scopeOrder` deleted; both sort sites routed through canonical comparator | VERIFIED | Import line at :54; canonical comparator referenced 6 times (sortReinstallTargets at :396/:404, cascade-block sort at :472). No `function scopeOrder` declaration. Iteration flipped to `["project", "user"]` at :313 with citing comment. |
| `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` | Imports `compareByNameThenScope`; local `scopeOrder` deleted; cascade-block sort routed through canonical comparator | VERIFIED | Import line at :72; canonical comparator referenced 4 times (cascade-block sort at :799). No `function scopeOrder` declaration. Bare-form iteration flipped to `["project", "user"]` at :1087 with citing comment. |
| `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` | Imports `compareByNameThenScope`; local `scopeOrder` deleted; cascade-block sort routed through canonical comparator | VERIFIED | Import line at :13; canonical comparator referenced 3 times (cascade-block sort at :370). No `function scopeOrder` declaration. Comment at :349 cites "compareByNameThenScope (name primary case-insensitive, scope secondary)". |
| `extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` | `["project", "user"]` iteration order (line 114) | VERIFIED | Line 114 reads `const scopes: readonly Scope[] = opts.scope === undefined ? ["project", "user"] : [opts.scope];`. Stale flow-doc comment at line 27 also reads `["project", "user"]`. Zero occurrences of `["user", "project"]` in file. |
| `tests/architecture/catalog-uat.test.ts` | `same-mp-both-scopes` fixture present for all 3 commands (reinstall/update/import) | VERIFIED | 6 occurrences total: 3 fixture registrations at :827 (reinstall), :1021 (update), :1370 (import); 3 explanatory comments. Each fixture composes two `cascadeSummary` calls (project scope first, then user scope) with `PROBE_BOTH_LOADED`, joined via `appendReloadHint` with `RELOAD_HINT`. |
| `docs/output-catalog.md` | `<!-- catalog-state: same-mp-both-scopes -->` annotation under each of reinstall/update/import sections | VERIFIED | 3 annotations at lines 463, 538, 649. |
| `tests/orchestrators/plugin/reinstall.test.ts` | Cross-scope `assert.deepEqual` partition arrays list project before user; descriptive comments cite `compareByNameThenScope` | VERIFIED | Cross-scope partition at :990-995 lists 3 project rows before the user row (within name-primary order: a/a, a/c, u/z, z/b -- name primary; project precedes user at the `u` name only because all rows there happen to be different names; the project-secondary tie-break is structurally available via the canonical comparator). Surrounding comments at :998-1006 cite "ordered project-before-user via compareByNameThenScope". |
| `tests/lint-rules/msg-gr-3-per-scope.js` | Active two-axis AST detector (FunctionDeclaration + ArrayExpression visitors) with `userFirstScopeOrder` + `userFirstScopeIteration` messageIds | VERIFIED | Module-header documents active detection axes citing D-14-2-08 supersession of D-14-09. `isUserFirstScopeOrder(body)` helper enforces exact structural match (`return scope === "user" ? <low> : <high>`). Two `context.report` sites with distinct messageIds; `Program: () => {}` no-op visitor is gone (grep returns 0). |
| `tests/lint-rules/msg-gr-3-per-scope.test.js` | RuleTester `invalid:` array contains 2 cases (one per axis) | VERIFIED | 3 valid cases (smoke, canonical iteration literal, non-`scopeOrder` helper); 2 invalid cases each asserting the matching messageId. `node --test` reports 5 tests / 5 pass. |
| `eslint.config.js` | `msg/msg-gr-3-per-scope` registered in MSG-Block 1 (orchestrator-scoped `files:`); removed from whole-extension MSG-Block 6 | VERIFIED | Rule registered at line 171 inside the block at line 162 (`files: ["extensions/pi-claude-marketplace/orchestrators/**/*.ts"]`). The awk extract of every whole-extension `files: ["extensions/pi-claude-marketplace/**/*.ts"]` block returns 0 hits for `msg/msg-gr-3-per-scope`. |
| `.planning/PROJECT.md` | Key Decisions row D-14-2-08 (2026-05-24) superseding D-14-09 with CR-01 + D-30 citations | VERIFIED | Row at line 130 reads "**D-14-2-08 (2026-05-24):** MSG-GR-3 lint rule moves from no-op meta-assertion (D-14-09) to active AST check covering (a) local user-first `scopeOrder` helpers and (b) `["user", "project"]` iteration literals in orchestrator files" with two-clause rationale (CR-01 + D-30) and `Locked` status column. |
| `.planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md` | New file with status: passed, threats_open: 0 | VERIFIED | Frontmatter: `phase: 12`, `slug: messaging-foundations-renderer-primitives`, `status: passed`, `threats_open: 0`, `threats_closed: 14`, `register_authored_at_plan_time: true`. |
| `.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md` | Advanced from draft to validated; nyquist_compliant true | VERIFIED | Frontmatter: `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `audited: 2026-05-24` (was draft / false / false). |
| `.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-SECURITY.md` | NEW file with status: passed, threats_open: 0 | VERIFIED | Frontmatter: `phase: 14.1`, `status: passed`, `threats_open: 0`, `threats_closed: 14`, `register_authored_at_plan_time: true`. |
| `.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-VALIDATION.md` | NEW file with status: validated, nyquist_compliant true | VERIFIED | Frontmatter: `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `audited: 2026-05-24`. |
| `.planning/ROADMAP.md` | 29 Phase-13 CMC rows = Complete in Coverage; Phase 14.1 = 2/2 plans / Complete in Phase Details | VERIFIED | `grep -c "\| Phase 13 \| Pending \|"` = 0; `grep -c "\| Phase 13 \| Complete \|"` = 29; row 259 confirms 14.1 = 2/2 plans / Complete / 2026-05-24. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` | `extensions/pi-claude-marketplace/presentation/sort.ts` | named import `{ compareByNameThenScope }` | WIRED | Import at :54; the canonical comparator at `presentation/sort.ts:40` is invoked at 4 call sites (sortReinstallTargets composition + cascade-block sort). |
| `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` | `extensions/pi-claude-marketplace/presentation/sort.ts` | named import `{ compareByNameThenScope }` | WIRED | Import at :72; comparator invoked at cascade-block sort site (:799). |
| `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` | `extensions/pi-claude-marketplace/presentation/sort.ts` | named import `{ compareByNameThenScope }` | WIRED | Import at :13; comparator invoked at cascade-block sort site (:370). |
| `tests/architecture/catalog-uat.test.ts` | `docs/output-catalog.md` | byte-equality loader pairing on `<!-- catalog-state: same-mp-both-scopes -->` | WIRED | Loader pairing per existing convention; the fixture-block / annotation pairs land in matching sections (`/claude:plugin reinstall|update|import`). `npm run check` exits 0 with the byte-equality runner exercising the new fixtures. |
| `tests/lint-rules/msg-gr-3-per-scope.js` | `eslint.config.js` | `msg/msg-gr-3-per-scope: "error"` in MSG-Block 1 | WIRED | Rule registered at :171 under orchestrator-scoped block. msg-rule-registry parity test (4 subtests) green. |
| `.planning/PROJECT.md` D-14-2-08 row | `.planning/docs/messaging-style-guide.md` MSG-GR-3 row | textual reference to MSG-GR-3 + D-14-09 | WIRED | D-14-2-08 row references both MSG-GR-3 and the prior D-14-09 entry; PROJECT.md text body of the new row is byte-present and Locked. |

---

### Data-Flow Trace (Level 4)

For the orchestrator source files, "data flow" means: the cascade-row sort actually invokes `compareByNameThenScope` on real `MarketplaceRow`/`PluginCascadeRow` objects produced upstream by the install/update/import paths.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `reinstall.ts` cascade-block sort | `marketplaceBlocks` ordering | `enumerateAllReinstallTargets` -> per-mp grouping | Yes -- comparator called on real `{name, scope}` from MarketplaceRow | FLOWING |
| `update.ts` cascade-block sort | `marketplaceBlocks` ordering | per-mp aggregation of update outcomes | Yes -- comparator called on real MarketplaceRow | FLOWING |
| `import/execute.ts` cascade-block sort | `marketplaceBlocks` ordering | per-scope marketplace outcomes from `installClaudeImport` | Yes -- comparator called on real MarketplaceRow | FLOWING |
| `catalog-uat.test.ts` `same-mp-both-scopes` fixture | rendered cascade body | `cascadeSummary` invocation x2 + `appendReloadHint` | Yes -- byte-equality runner asserts produced string equals catalog | FLOWING (1245+1249 tests green) |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite green | `npm run check` | 1249/1249 pass, lint+format+types clean | PASS |
| MSG-GR-3 ruletester (active two-axis) | `node --test tests/lint-rules/msg-gr-3-per-scope.test.js` | 5 tests pass (3 valid + 2 invalid) | PASS |
| 4-way MSG rule registry parity | `node --test tests/architecture/msg-rule-registry.test.ts` | 4 subtests pass | PASS |
| Cross-scope cascade catalog UAT (new fixtures) | exercised by full `npm run check` | Byte-equality of `same-mp-both-scopes` fixtures across 3 commands passes | PASS |

---

### Requirements Coverage

PLAN frontmatter requirement IDs declared across the 5 plans:
- 14.2-01: CMC-03, CMC-25, CMC-26, CMC-27
- 14.2-02: CMC-03
- 14.2-03/04/05: none (gate-running / cosmetic-drift plans per phase-goal text)

Cross-reference against REQUIREMENTS.md:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CMC-03 | 14.2-01, 14.2-02 | Per-scope rendering on every surface; sort name-primary case-insensitive `localeCompare` with `sensitivity:'base'`, project-before-user tie-breaker (MSG-GR-3). | SATISFIED | REQUIREMENTS.md:361 marks `[x]`. After 14.2-01 the 4 cascade surfaces (reinstall/update/import/autoupdate) all route through `compareByNameThenScope`; after 14.2-02 the active MSG-GR-3 rule structurally prevents drift in orchestrators. Catalog UAT byte-equality fixture (`same-mp-both-scopes` x3) locks the project-first ordering across the same 3 cascade surfaces. |
| CMC-25 | 14.2-01 | `/claude:plugin reinstall` cascade rows ordering / severity routing / reload trailer. | SATISFIED | REQUIREMENTS.md:407 marks `[x]`. Cascade-block sort for reinstall now project-first; PRL-13 cross-scope test asserts the partition order; same-mp-both-scopes byte-equal fixture locks the rendered output. |
| CMC-26 | 14.2-01 | `/claude:plugin update` cascade rows with version-transition arrows; all-trivial cascades route via `notifySuccess` without reload trailer. | SATISFIED | REQUIREMENTS.md:408 marks `[x]`. Cascade-block sort for update now project-first via canonical comparator; byte-equal fixture exercises same-mp-both-scopes. |
| CMC-27 | 14.2-01 | `/claude:plugin import` preamble + per-scope marketplace headers + scope-narrowed cascade. | SATISFIED | REQUIREMENTS.md:409 marks `[x]`. Cascade-block sort for import now project-first via canonical comparator; byte-equal fixture exercises same-mp-both-scopes. |

Orphan requirements (declared in REQUIREMENTS.md mapping but not claimed by any 14.2 plan): none. The phase's intentionally narrow requirement set per phase frontmatter is preserved.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts` | 45 | `selectedScopes: parsed.scope === undefined ? ["user", "project"] : [parsed.scope]` -- user-first iteration outside the MSG-GR-3 lint scope | INFO (follow-up surface, out of scope) | Identified by the code-review report (CR-01 finding). The phase 14.2 stated goal explicitly enumerated 3 orchestrator helpers + the `autoupdate.ts:114` iteration per D-01/D-02. The edge handler at `import.ts:45` is outside the orchestrator glob and outside the audit CR-01 enumeration; it is NOT a phase 14.2 must-have miss. Flows into `importClaudeSettings.selectedScopes` and drives both `Promise.all(...)` order and the iteration order in `execute.ts:914` for any top-level orphan-diagnostic insertion (per-marketplace cascade blocks are subsequently re-sorted by `compareByNameThenScope`). Recommended follow-up: extend MSG-GR-3 `files:` glob to include `edge/handlers/**/*.ts`, or factor a project-first SCOPES constant into `shared/types.ts` and audit `SCOPES = ["user","project"]` (currently user-first per `shared/types.ts:20`) and `edge/completions/provider.ts:70` (also user-first). |
| `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` | 12 | Stale file-header comment still cites `["user", "project"]` iteration although code at :44 is project-first | INFO (cited in 14.2-REVIEW.md WR-01) | Comment drift caused by 14.2-02's flip. Misleads readers; lint rule does not visit block comments. Out of scope for the phase 14.2 stated goal but visible to readers. |
| `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` | 64-66 | Trailing comment still references retired "No marketplaces configured." sentence | INFO (cited in 14.2-REVIEW.md WR-02) | Pre-existing drift unrelated to 14.2; observation only. |
| `update.ts`, `reinstall.ts`, `plugin/update.ts` (orchestrators) | n/a | `composeErrorWithCauseChain` duplicated byte-for-byte across 3 files | INFO (cited in 14.2-REVIEW.md WR-03) | Pre-existing duplication not introduced by 14.2; out of scope. |

No `TBD` / `FIXME` / `XXX` debt markers were found in files modified by this phase. No active blocker anti-patterns within the phase 14.2 stated goal scope.

---

### Probe Execution

No project probe scripts (`scripts/*/tests/probe-*.sh`) are declared by this phase's PLAN/SUMMARY files; the equivalent gate is `npm run check` (run above; 1249/1249 green) plus `node --test tests/lint-rules/msg-gr-3-per-scope.test.js` and `node --test tests/architecture/msg-rule-registry.test.ts` (both PASS).

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| (none -- not a migration/probe phase) | n/a | n/a | SKIPPED |

---

### Human Verification Required

None. The phase is a refactor + lint-rule tightening + retroactive gates + documentation drift fix. Every truth is observable via grep / file read / `npm run check` / test runner; no visual or runtime behavior must be inspected by a human. Gate artefacts (12/14.1 SECURITY + VALIDATION) carry the auditor decisions in their frontmatter and bodies, which were independently verifiable above.

---

### Gaps Summary

No phase-goal gaps. All 7 must-have truths verified; the phase delivers exactly what its stated goal enumerated:

(a) CR-01 source closure verified (3 helpers deleted, canonical comparator wired, autoupdate iteration flipped, byte-equal cross-scope regression locks in catalog UAT + orchestrator unit tests).

(b) MSG-GR-3 promoted from no-op (`Program: () => {}`) to active two-axis AST rule (FunctionDeclaration + ArrayExpression) scoped to orchestrators; the RuleTester exercises both axes; PROJECT.md D-14-2-08 records the supersession with CR-01 + D-30 citations; the broader cleanup the active rule surfaced (7 additional `["user","project"]` literals across 6 orchestrator files) was auto-fixed in 14.2-02 to make `npm run check` green.

(c) Both retroactive gates landed for Phase 12 (draft -> validated) and Phase 14.1 (fresh artefacts); all four gate files present at canonical padded-phase filenames with `status: passed` / `status: validated` and the required `threats_open: 0` / `nyquist_compliant: true` / `wave_0_complete: true` frontmatter.

(d) ROADMAP.md cosmetic drift closed: 29 Phase-13 CMC rows flipped Pending -> Complete; Phase 14.1 row updated to `2/2 plans | Complete | 2026-05-24`.

**Observations recorded for follow-up (out of scope for phase 14.2's stated goal):**

1. `edge/handlers/plugin/import.ts:45` still passes `["user", "project"]` to `importClaudeSettings`. The code-review report (14.2-REVIEW.md CR-01) flagged this as critical; however, the phase 14.2 goal explicitly enumerated only 3 orchestrator helpers + `autoupdate.ts:114` per D-01/D-02. The edge handler is outside the MSG-GR-3 lint scope and outside the audit CR-01 enumeration. Recommended next-phase action: extend the MSG-GR-3 `files:` glob to `edge/handlers/**/*.ts` (and audit `shared/types.ts:20` `SCOPES` + `edge/completions/provider.ts:70`).

2. 6 Phase-12 CMC rows (CMC-08/11/14/19/36/37) in ROADMAP.md `## Coverage (v1.3)` still read `Pending` despite REQUIREMENTS.md marking them Complete. Per D-13 the 14.2-05 plan explicitly scoped its flip to 29 Phase-13 entries; the Phase-12 drift is documented in 14.2-05-SUMMARY.md for traceability and is not a phase 14.2 must-have miss.

3. Stale comments in `orchestrators/marketplace/list.ts` (file-header cite, retired-sentence comment) per WR-01/WR-02; not introduced by 14.2 and out of phase scope.

These observations are informational. They do not affect the phase 14.2 status determination, which is `passed`.

---

_Verified: 2026-05-24T03:00:00Z_
_Verifier: Claude (gsd-verifier, Opus 4.7 1M)_
