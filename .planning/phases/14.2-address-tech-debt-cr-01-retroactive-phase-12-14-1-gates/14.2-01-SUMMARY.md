---
phase: 14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates
plan: 01
subsystem: testing
tags: [cascade-ordering, msg-gr-3, compareByNameThenScope, refactor]

# Dependency graph
requires:
  - phase: 13-conformance-refactor-es-5
    provides: presentation/sort.ts compareByNameThenScope canonical comparator
  - phase: 14-drift-guard-test-alignment
    provides: MSG-GR-3 frontmatter + catalog-uat.test.ts byte-equality runner
provides:
  - Project-first cross-scope cascade ordering on reinstall, update, import, and autoupdate surfaces
  - same-mp-both-scopes byte-equality fixture across 3 command sections (catalog + UAT)
  - Project-before-user assertions in reinstall.test.ts PRL-04 and PRL-13 fixtures
  - Zero `function scopeOrder` declarations in extensions/pi-claude-marketplace/orchestrators
affects: [14.2-02 (MSG-GR-3 lint-rule tightening), v1.3-milestone-close]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Canonical comparator import: cascade-sort orchestrators route through compareByNameThenScope from presentation/sort.ts (project-first tie-break per MSG-GR-3)"
    - "Inverse comment-is-wrong fix: code catches up to a comment that already described the desired behavior (autoupdate.ts:114 flipped to match comment at lines 173-178)"

key-files:
  created: []
  modified:
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
    - tests/architecture/catalog-uat.test.ts
    - docs/output-catalog.md
    - tests/orchestrators/plugin/reinstall.test.ts

key-decisions:
  - "Updated stale doc comment at autoupdate.ts:27 alongside the line-114 flip so both file-internal documentations of the iteration order agree (kept the comment block at lines 173-178 untouched per task spec -- it already described project-first)."
  - "Did NOT modify tests/orchestrators/plugin/update.test.ts or tests/orchestrators/import/execute.test.ts because neither contained cross-scope deepEqual partition fixtures; the existing tests passed unchanged on the post-Task-1 codebase."

patterns-established:
  - "Per-test-file deferred verification: when an orchestrator refactor breaks downstream test fixtures, the unit tests for that orchestrator are updated in a separate task (Task 3) and committed under `test(...)` rather than co-mingled with the source `refactor(...)` commit. Each commit's CI is independently meaningful."

requirements-completed:
  - CMC-03
  - CMC-25
  - CMC-26
  - CMC-27

# Metrics
duration: 14min
completed: 2026-05-24
---

# Phase 14.2 Plan 01: Close audit CR-01 cascade-block ordering drift Summary

**Three orchestrator user-first `scopeOrder` helpers deleted and routed through the canonical project-first `compareByNameThenScope`; autoupdate scope iteration flipped to project-first; byte-equality cross-scope regression locks added at both the catalog UAT layer (3 fixtures) and the orchestrator unit-test layer (PRL-04 + PRL-13).**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-05-24T23:58:00Z (approx, from first commit)
- **Completed:** 2026-05-25T00:12:50Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- **CR-01 source fix (Task 1):** Deleted the three local `function scopeOrder` user-first helpers in `reinstall.ts`, `update.ts`, and `import/execute.ts`; routed all four cascade-sort call sites (`reinstall.ts` ×2, `update.ts` ×1, `import/execute.ts` ×1) through the canonical `compareByNameThenScope` from `presentation/sort.ts` (project-first tie-break per MSG-GR-3).
- **Autoupdate iteration fix (Task 2):** Flipped the `scopes` literal at `autoupdate.ts:114` from `["user", "project"]` to `["project", "user"]` so V8 stable-sort preserves project-first construction order on same-marketplace-name cross-scope ties -- making the existing comment at lines 173-178 truthful (inverse of the typical comment-is-wrong fix).
- **Regression locks (Task 3):** Added `same-mp-both-scopes` byte-equality fixture under each of `/claude:plugin reinstall`, `update`, and `import` in `tests/architecture/catalog-uat.test.ts`, paired with matching `<!-- catalog-state: same-mp-both-scopes -->` annotations under the same three command sections in `docs/output-catalog.md`. Updated PRL-04 and PRL-13 cross-scope `assert.deepEqual` partition arrays in `tests/orchestrators/plugin/reinstall.test.ts` to assert project-before-user ordering.
- **All four cascade surfaces (reinstall, update, import, autoupdate)** now render same-marketplace-name cross-scope cases project-first, aligning with every list / marketplace-list / cascade-summary surface that already routed through `compareByNameThenScope` (CMC-03, CMC-25, CMC-26, CMC-27 observably consistent across surfaces).
- **`npm run check` exits 0** (typecheck + ESLint + Prettier + 1245/1245 node:test).

## Task Commits

Each task committed atomically with `SKIP=trufflehog` (worktree-sandbox limitation per project CLAUDE.md):

1. **Task 1: Replace three local `scopeOrder` helpers with `compareByNameThenScope`** -- `f80772e` (refactor)
2. **Task 2: Flip autoupdate.ts:114 scope iteration order to `["project", "user"]`** -- `e3ef196` (fix)
3. **Task 3: Add `same-mp-both-scopes` catalog UAT fixture + paired catalog annotations + update reinstall.test.ts cross-scope assertions** -- `6f23ef6` (test)

## Files Created/Modified

- `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` -- Imported `compareByNameThenScope`; rewrote `sortReinstallTargets` to compose two canonical-comparator calls (marketplace primary, plugin secondary, both keyed on scope); swapped cascade-block sort to direct comparator call; deleted local `function scopeOrder`.
- `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` -- Imported `compareByNameThenScope`; swapped cascade-block sort to direct comparator call; updated cite comment from "via scopeOrder" to "via compareByNameThenScope"; deleted local `function scopeOrder`.
- `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` -- Imported `compareByNameThenScope`; swapped cascade-block sort to direct comparator call; updated cite comment from "scope (project-before-user via scopeOrder) primary, marketplace-name secondary" to "via compareByNameThenScope"; deleted local `function scopeOrder`.
- `extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` -- Flipped line-114 literal from `["user", "project"]` to `["project", "user"]`; updated matching flow-doc comment at line 27 so the file's two iteration-order documentations agree. Left the comment block at lines 173-178 untouched (already truthful for project-first per task spec).
- `tests/architecture/catalog-uat.test.ts` -- Added `same-mp-both-scopes` fixture under each of `/claude:plugin reinstall`, `update`, and `import` FixtureMaps; each composes two `cascadeSummary` calls (project scope first, user scope second) joined via `appendReloadHint`. Prettier reformatted on auto-format.
- `docs/output-catalog.md` -- Added three `<!-- catalog-state: same-mp-both-scopes -->` annotations + fenced text blocks under the matching command H2 sections; each documents the cross-scope tie-break visibly in the user-facing catalog with rationale tied to MSG-GR-3 / `compareByNameThenScope`.
- `tests/orchestrators/plugin/reinstall.test.ts` -- Updated PRL-04 cross-scope assertion (line ~684) from `["[user] uplug@ump", "[project] pplug@pmp"]` to `["[project] pplug@pmp", "[user] uplug@ump"]` (name-primary `pmp < ump`). Updated PRL-13 cross-scope `assert.deepEqual` partition array (lines ~990-995) to project-before-user via `compareByNameThenScope`; updated surrounding comments to cite the new ordering rationale.

## Decisions Made

- **D-DOC-1 (this plan):** Update stale flow-doc comment at `autoupdate.ts:27` alongside the line-114 flip. Rationale: line 27's documentation comment had the literal `["user", "project"]` token; leaving it stale would have made the two file-internal documentations of iteration order disagree, and the plan's Task 2 verify (`grep -c '\["user", "project"\]'` equals 0) would have failed. The comment block at lines 173-178 was left intact per task spec (already truthful for project-first).
- **D-DOC-2 (this plan):** Did NOT update `tests/orchestrators/plugin/update.test.ts` or `tests/orchestrators/import/execute.test.ts` because neither contained cross-scope `assert.deepEqual` partition fixtures. Verified by `npm test` on both files (zero failures after Task 1) plus a `grep` audit for `user.*project` / `project.*user` patterns in cross-scope `assert.deepEqual` arrays (none found). The catalog UAT layer (`same-mp-both-scopes` fixture for each of the three commands) provides the symmetric regression lock that the plan's D-04 requires across all three orchestrators.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 -- Missing Critical] Updated stale flow-doc comment at autoupdate.ts:27**
- **Found during:** Task 2 verification (`grep -c '\["user", "project"\]'` must equal 0)
- **Issue:** The plan only specified flipping line 114, but a flow-doc comment at line 27 carried the same literal `["user", "project"]` describing the old behavior. Leaving it stale would have left the file's two iteration-order documentations disagreeing AND would have failed the Task 2 acceptance criterion `grep -c '\["user", "project"\]'` equals 0.
- **Fix:** Updated comment to `["project", "user"]` so both documentation lines and the code agree.
- **Files modified:** `extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` (single comment line)
- **Verification:** `grep -c '\["user", "project"\]'` returns 0; `grep -c '\["project", "user"\]'` returns 2 (line 27 + line 114).
- **Committed in:** `e3ef196` (Task 2 commit).

**2. [Rule 3 -- Blocking] Re-ran prettier --write on tests/architecture/catalog-uat.test.ts**
- **Found during:** Final `npm run check` after Task 3 fixture additions
- **Issue:** Prettier flagged formatting issues in `tests/architecture/catalog-uat.test.ts` after the three new fixture insertions.
- **Fix:** Ran `npx prettier --write tests/architecture/catalog-uat.test.ts`.
- **Files modified:** `tests/architecture/catalog-uat.test.ts` (formatting only -- no semantic change)
- **Verification:** `npm run check` exits 0; all 1245 tests still pass.
- **Committed in:** `6f23ef6` (Task 3 commit, bundled).

---

**Total deviations:** 2 auto-fixed (1 missing-critical comment-truth update, 1 blocking formatter re-run)
**Impact on plan:** Both deviations were necessary to satisfy the plan's own acceptance criteria. No scope creep -- no behavior change beyond what the plan specified.

## Issues Encountered

- **TruffleHog worktree-sandbox limitation:** Every `git commit` had to be prefixed with `SKIP=trufflehog` per the project CLAUDE.md instruction. TruffleHog's auto-updater fails to spawn child processes under the worktree sandbox (`error preparing repo: failed to read index file: open .git/index: not a directory`); the underlying scan succeeds when run separately. Confirmed clean via `pre-commit run trufflehog --all-files` from the main repo path before each commit.
- **No genuine issues encountered during implementation.** The canonical comparator was already in production use on adjacent surfaces (`marketplace-list.ts`, `cascade-summary.ts`, `orchestrators/plugin/list.ts`), so the refactor was a mechanical drop-in.

## User Setup Required

None -- refactor only, no external service configuration.

## Next Phase Readiness

- **14.2-02 (MSG-GR-3 lint-rule tightening) unblocked.** The CR-01 fix ships before the lint-rule tightening so the rule does not fail against the unfixed orchestrator files mid-execution. Per CONTEXT.md `<code_context>` Integration Points, this is the (a) sequencing path: `14.2-02 depends_on 14.2-01`. The tightened rule will land cleanly against the now-canonical orchestrator code.
- **Audit CR-01 is closed on the source side.** CMC-03 (per-scope rendering), CMC-25 (`reinstall`), CMC-26 (`update`), and CMC-27 (`import`) are now observably consistent with every list/marketplace-list surface that uses `compareByNameThenScope`.
- **No blockers.** All four cascade surfaces tested and verified at byte-equality (catalog UAT) and `assert.deepEqual` (orchestrator unit tests). `npm run check` exits 0 across 1245 tests.

## Self-Check: PASSED

All claims verified:

1. **Created files exist:** `[ -f .planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-01-SUMMARY.md ]` (this file).
2. **Commits exist:**
   - `f80772e` (Task 1, `refactor(14.2-01): route cascade sorts through compareByNameThenScope`)
   - `e3ef196` (Task 2, `fix(14.2-01): flip autoupdate scope iteration order to project-first`)
   - `6f23ef6` (Task 3, `test(14.2-01): lock project-first cross-scope cascade ordering`)
3. **Source-side acceptance criteria verified:**
   - `grep -rE "function scopeOrder" extensions/pi-claude-marketplace/orchestrators/` returns no matches.
   - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` contains 5 occurrences of `compareByNameThenScope`; `update.ts` contains 3; `import/execute.ts` contains 3.
   - `grep -c '\["project", "user"\]' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` is 2; `grep -c '\["user", "project"\]'` is 0.
   - `grep -c "same-mp-both-scopes" tests/architecture/catalog-uat.test.ts` is 6 (>= 3 required); `grep -c "same-mp-both-scopes" docs/output-catalog.md` is 3.
4. **Test acceptance criteria verified:** `npm test tests/architecture/catalog-uat.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/import/execute.test.ts` exits 0; `npm run check` exits 0.

---

*Phase: 14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates*
*Completed: 2026-05-24*
