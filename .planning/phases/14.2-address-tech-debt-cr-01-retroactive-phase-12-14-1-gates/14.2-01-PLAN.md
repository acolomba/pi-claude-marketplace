---
phase: 14.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/import/execute.test.ts
autonomous: true
requirements:
  - CMC-03
  - CMC-25
  - CMC-26
  - CMC-27

must_haves:
  truths:
    - "Zero `function scopeOrder` declarations remain in `extensions/pi-claude-marketplace/orchestrators/**/*.ts` (AST/grep audit returns no match)."
    - "All cascade-block sorts in reinstall/update/import orchestrators call `compareByNameThenScope` directly from `presentation/sort.ts` (project-first tie-break per MSG-GR-3)."
    - "`autoupdate.ts:114` iterates `['project', 'user']` so V8 stable-sort preserves project-first construction order on same-marketplace-name cross-scope ties; the existing comment at lines 174-178 is now truthful."
    - "`tests/architecture/catalog-uat.test.ts` contains a `same-mp-both-scopes` fixture for each of `/claude:plugin reinstall`, `/claude:plugin update`, and `/claude:plugin import`, paired with a `<!-- catalog-state: same-mp-both-scopes -->` annotation in `docs/output-catalog.md` under each section."
    - "Cross-scope ordering assertions in `tests/orchestrators/plugin/reinstall.test.ts:984-988`, the analogous fixtures in `tests/orchestrators/plugin/update.test.ts`, and `tests/orchestrators/import/execute.test.ts` assert project-before-user partition order."
    - "`npm run check` exits 0 (typecheck + ESLint + Prettier + all node:test files)."
    - "Implements CONTEXT.md decisions D-01 (replace 3 local scopeOrder helpers with canonical compareByNameThenScope), D-02 (flip autoupdate.ts iteration order to project-first), D-03 (extend catalog-uat.test.ts with same-marketplace-name-cross-scope fixture), D-04 (update orchestrator unit-test cross-scope fixtures), and anchors D-15 (5-plan / 3-wave shape) as the Wave 1 first plan."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      provides: "Cascade block ordering via canonical `compareByNameThenScope` import; local `scopeOrder` helper deleted; both call sites (385-396 batch sort and 457-464 block sort) routed through the canonical comparator."
      contains: "compareByNameThenScope"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts"
      provides: "Cascade block ordering via canonical comparator; local `scopeOrder` deleted; call site 795-804 swapped."
      contains: "compareByNameThenScope"
    - path: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      provides: "Cascade block ordering via canonical comparator; local `scopeOrder` deleted; call site 367-374 swapped."
      contains: "compareByNameThenScope"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts"
      provides: "Scope iteration order flipped to `['project', 'user']` so stable-sort ties preserve project-first."
      contains: "[\"project\", \"user\"]"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "`same-mp-both-scopes` cross-scope fixture for reinstall/update/import surfaces; byte-equality locks project-first cascade-block ordering."
      contains: "same-mp-both-scopes"
    - path: "docs/output-catalog.md"
      provides: "Paired `<!-- catalog-state: same-mp-both-scopes -->` annotation under reinstall/update/import sections."
      contains: "same-mp-both-scopes"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      to: "extensions/pi-claude-marketplace/presentation/sort.ts"
      via: "named import { compareByNameThenScope }"
      pattern: "compareByNameThenScope"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts"
      to: "extensions/pi-claude-marketplace/presentation/sort.ts"
      via: "named import { compareByNameThenScope }"
      pattern: "compareByNameThenScope"
    - from: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      to: "extensions/pi-claude-marketplace/presentation/sort.ts"
      via: "named import { compareByNameThenScope }"
      pattern: "compareByNameThenScope"
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "byte-equality loader pairing on `<!-- catalog-state: same-mp-both-scopes -->`"
      pattern: "same-mp-both-scopes"
---

<objective>
Close audit CR-01 by replacing the three local `function scopeOrder` user-first helpers in `reinstall.ts`, `update.ts`, and `import/execute.ts` with the canonical project-first `compareByNameThenScope` from `presentation/sort.ts`, flipping the autoupdate iteration order so its stable-sort ties match the existing comment, and adding cross-scope regression locks at both the byte-equal catalog UAT layer and the orchestrator unit-test layer.

Purpose: CR-01 is the v1.3 milestone audit's CRITICAL drift: four orchestrator surfaces render cascade blocks user-first while every list/marketplace-list/cascade-summary surface in `presentation/` renders project-first via `compareByNameThenScope`. CMC-03 (per-scope rendering), CMC-25 (`reinstall`), CMC-26 (`update`), and CMC-27 (`import`) are observably inconsistent until this is fixed. The autoupdate.ts comment at lines 174-178 already correctly describes project-first ordering -- this is the inverse of the typical "comment is wrong" fix; the code must catch up to the comment.

Output: Three orchestrator source files lose their local helpers and route through the canonical comparator; one orchestrator iteration order flips one token; one byte-equality fixture (paired with three catalog annotations) and three orchestrator unit-test cross-scope fixtures lock the regression so MSG-GR-3 (which 14.2-02 will tighten to an active lint check) and `catalog-uat.test.ts` together prevent recurrence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md
@docs/messaging-style-guide.md
@docs/output-catalog.md

<interfaces>
<!-- Canonical comparator (presentation/sort.ts:35-52). All three orchestrators
     and the autoupdate iteration align to this contract. -->

From extensions/pi-claude-marketplace/presentation/sort.ts:
  - interface Sortable { readonly name: string; readonly scope: "user" | "project"; }
  - export function compareByNameThenScope(a: Sortable, b: Sortable): number
  - Semantics: name primary (case-insensitive `localeCompare`), scope secondary (project < user).

Existing analog (`orchestrators/plugin/list.ts:61`):
  import { compareByNameThenScope } from "../../presentation/sort.ts";
  // call sites at :611 and :641 pass MarketplaceRow / PluginListEntry directly --
  // both already carry `name` and `scope` structurally.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace three local `scopeOrder` helpers with `compareByNameThenScope` in reinstall.ts, update.ts, import/execute.ts</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts</files>
  <read_first>
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` sections "orchestrators/plugin/reinstall.ts -- delete `scopeOrder` + rewrite call sites (D-01)", "orchestrators/plugin/update.ts -- same shape (D-01)", "orchestrators/import/execute.ts -- same shape (D-01)", and "Canonical comparator import (all 3 orchestrator edits)" for full code excerpts of both the current state and the target shape.
    - `extensions/pi-claude-marketplace/presentation/sort.ts` lines 35-52 for the canonical `Sortable` interface and `compareByNameThenScope` body.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/list.ts` lines 61 (import) and 611, 641 (analog call sites already using the comparator).
    - `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` lines 385-396, 455-464, 706-708 (current code in this file).
    - `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` lines 795-804, 1062-1064.
    - `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` lines 346-374, 700-702.
  </read_first>
  <behavior>
    - When two cascade rows have the same marketplace name, the project-scope row renders before the user-scope row in the cascade body emitted by reinstall, update, and import.
    - When `sortReinstallTargets` (reinstall.ts) orders the batch execution list, project-scope targets precede user-scope targets within each marketplace-name group; plugin-name tie-break is preserved.
    - `function scopeOrder` is no longer defined in any orchestrator file under `extensions/pi-claude-marketplace/orchestrators/**/*.ts`.
  </behavior>
  <action>
    In each of `reinstall.ts`, `update.ts`, and `import/execute.ts`: add a named import for `compareByNameThenScope` from `../../presentation/sort.ts` (place it in the internal-imports group per the existing `import-x/order` ESLint rule). Delete each file's local `function scopeOrder` declaration. Rewrite each cascade-block sort to call `compareByNameThenScope(a.marketplace, b.marketplace)` directly -- `MarketplaceRow` already carries `{ name, scope }` structurally so no adapter is needed. In `reinstall.ts:385-396` (`sortReinstallTargets`), rewrite the batch sort to call `compareByNameThenScope` twice -- once on `{ name: a.marketplace, scope: a.scope }` for the outer key, then on `{ name: a.plugin, scope: a.scope }` for the inner key -- matching the PATTERNS.md "Target shape" excerpt. Update the descriptive comment at `update.ts:795` from "via scopeOrder" to "via compareByNameThenScope"; update the `execute.ts:346-348` comment from "scope (project-before-user via scopeOrder) primary, marketplace-name secondary" to "via compareByNameThenScope" since the canonical comparator now handles both axes in one call.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; grep -rEn "function scopeOrder" extensions/pi-claude-marketplace/orchestrators/ ; test "$(grep -rEc 'function scopeOrder' extensions/pi-claude-marketplace/orchestrators/ | grep -v ':0' | wc -l)" = 0 &amp;&amp; grep -c "compareByNameThenScope" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts extensions/pi-claude-marketplace/orchestrators/import/execute.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "function scopeOrder" extensions/pi-claude-marketplace/orchestrators/` returns no matches.
    - Each of the three modified files contains at least one occurrence of the literal token `compareByNameThenScope`.
    - Each of the three modified files contains the literal import path `"../../presentation/sort.ts"` (the named-import line for `compareByNameThenScope`).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The three orchestrator files import `compareByNameThenScope` directly from `presentation/sort.ts`, all four call sites (reinstall.ts ×2, update.ts ×1, import/execute.ts ×1) route through the canonical comparator, the local `scopeOrder` helpers are deleted, and the codebase still typechecks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip autoupdate.ts:114 scope iteration order to ['project', 'user']</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts</files>
  <read_first>
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "orchestrators/marketplace/autoupdate.ts -- flip iteration order (D-02)" for the one-token target and the rationale paragraph explaining V8 stable-sort + name-only `localeCompare` interaction.
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` lines 100-184 for the scopes-array declaration at line 114 and the comment block at lines 173-178 that already correctly describes project-first ordering.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md` `<specifics>` first bullet explaining the inverse-of-typical-fix framing (code catches up to comment, not comment catches up to code).
  </read_first>
  <behavior>
    - When the autoupdate result block lists two `marketplaceRows` with identical names but different scopes (one user, one project), the project-scope row appears first in the rendered output.
    - The behavior is achieved without changing the name-only `localeCompare` sort at lines ~178 -- only the construction-order iteration at line 114 changes.
  </behavior>
  <action>
    Replace the literal `["user", "project"]` in the `scopes` declaration on line 114 with `["project", "user"]`. Do not modify the sort at line ~178. Do not modify the comment block at lines 173-178 (it is already truthful for the target state per D-02). Confirm no other tokens on line 114 change.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; grep -c '\["project", "user"\]' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts &amp;&amp; test "$(grep -c '\["user", "project"\]' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts)" = 0 &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\["project", "user"\]' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` is at least 1.
    - `grep -c '\["user", "project"\]' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` equals 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Line 114 reads `["project", "user"]`; the file still typechecks; the comment at lines 173-178 has not been edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add same-mp-both-scopes cross-scope fixtures to catalog-uat.test.ts (paired with docs/output-catalog.md annotations) and update the three orchestrator unit-test cross-scope assertions</name>
  <files>tests/architecture/catalog-uat.test.ts, docs/output-catalog.md, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "tests/architecture/catalog-uat.test.ts -- same-marketplace-name-cross-scope fixture (D-03)" for the FixtureMap registration shape, including the `cascadeSummary` call shape with `PROBE_BOTH_LOADED` and the `appendReloadHint` pairing.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "tests/orchestrators/plugin/reinstall.test.ts:984-1001 -- update assertion order (D-04)" for the worked example (target shape: project-first partition order, comment update).
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "tests/orchestrators/plugin/update.test.ts & tests/orchestrators/import/execute.test.ts -- same shape (D-04)" for the symmetry instruction (grep each file for `user.*project` / `project.*user` in `assert.deepEqual` order-arrays and apply the same flip).
    - `tests/architecture/catalog-uat.test.ts` lines 1-150 (loader pairing logic), 248-288 (`same-plugin-both-scopes` analog), 613-622 / 824 / 974 (FixtureMap registration sites for reinstall/update/import).
    - `tests/orchestrators/plugin/reinstall.test.ts` lines 970-1006 (audit-cited fixture block, including the `assert.deepEqual` order array at 984-988 and body-regex assertions at 995-1000).
    - `docs/output-catalog.md` sections `## /claude:plugin reinstall`, `## /claude:plugin update`, `## /claude:plugin import` for existing `<!-- catalog-state: ... -->` annotation conventions.
  </read_first>
  <behavior>
    - For each of `/claude:plugin reinstall`, `/claude:plugin update`, `/claude:plugin import`, the byte-equality runner in `catalog-uat.test.ts` exercises a `same-mp-both-scopes` fixture: the same marketplace name (`official`) is installed in both user and project scope; the cascade body emits the project-scope block first, then the user-scope block, with a single trailing reload hint.
    - The three orchestrator unit tests' cross-scope `assert.deepEqual` partition arrays list project-scope entries before user-scope entries (alphabetical within each scope).
    - The descriptive comments on those orchestrator fixtures now read "project-before-user via compareByNameThenScope" (or equivalent) instead of "user-before-project".
  </behavior>
  <action>
    Step A (catalog + UAT): Under each of the three command sections in `docs/output-catalog.md`, add a `<!-- catalog-state: same-mp-both-scopes -->` HTML-comment annotation that renders a cascade where one marketplace named `official` exists in both project and user scope; the project-scope cascade block precedes the user-scope cascade block; one rendered row per scope; trailing reload hint present. In `tests/architecture/catalog-uat.test.ts`, register a `same-mp-both-scopes` fixture in each of the three FixtureMaps (`/claude:plugin reinstall`, `/claude:plugin update`, `/claude:plugin import`); each fixture composes two `cascadeSummary` calls (`scope: "project"` first, then `scope: "user"`) with `PROBE_BOTH_LOADED`, joins them with a newline, and appends `RELOAD_HINT` via the existing `appendReloadHint` helper. Use the status tokens appropriate to each command (`reinstalled`, `updated`, `installed`). Step B (orchestrator unit tests): In each of `tests/orchestrators/plugin/reinstall.test.ts`, `tests/orchestrators/plugin/update.test.ts`, and `tests/orchestrators/import/execute.test.ts`, grep for cross-scope `assert.deepEqual` partition arrays and cascade-body regexes that reference both `scope: "user"` and `scope: "project"`; flip the array element order so project-scope entries come first (alphabetical within scope); update the surrounding descriptive comment to "ordered project-before-user via `compareByNameThenScope` (name primary case-insensitive, scope secondary project-before-user)". Preserve all body-regex `assert.match` calls (they assert presence, not order). The audit-cited fixture at `reinstall.test.ts:984-988` is the exemplar; apply the same flip to every analog cross-scope fixture in the three test files.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; grep -c "same-mp-both-scopes" tests/architecture/catalog-uat.test.ts &amp;&amp; grep -c "same-mp-both-scopes" docs/output-catalog.md &amp;&amp; npm test -- --test-name-pattern "catalog-uat" &amp;&amp; npm test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/import/execute.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "same-mp-both-scopes" tests/architecture/catalog-uat.test.ts` is at least 3 (one fixture key per command section).
    - `grep -c "same-mp-both-scopes" docs/output-catalog.md` is at least 3 (one annotation per command section).
    - `npm test -- --test-name-pattern "catalog-uat"` exits 0 with the new fixtures passing byte-equality.
    - `npm test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/import/execute.test.ts` exits 0.
    - Every `assert.deepEqual` cross-scope partition array in those three orchestrator test files lists at least one `scope: "project"` entry before any `scope: "user"` entry (manual grep + read step in SUMMARY confirms zero stale user-first arrays).
  </acceptance_criteria>
  <done>Catalog UAT layer locks project-first cross-scope cascade ordering by byte-equality across reinstall/update/import; the three orchestrator unit-test cross-scope fixtures assert project-before-user ordering; `npm run check` exits 0 across the whole repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Refactor only -- no new inputs, no new IO, no new network/disk surface; the canonical comparator already exists and is used by other surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-01-01 | Tampering | n/a | accept | none -- refactor only; no inputs cross a new boundary. The change consolidates four sort/iteration sites onto a shared comparator already in production use. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 after Task 1.
- `npm run typecheck` exits 0 after Task 2.
- `npm run check` (typecheck + ESLint + Prettier + all tests) exits 0 after Task 3.
- `grep -rE "function scopeOrder" extensions/pi-claude-marketplace/orchestrators/` returns no matches.
- `grep -c '\["user", "project"\]' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` equals 0.
- `grep -c "same-mp-both-scopes" tests/architecture/catalog-uat.test.ts` is at least 3.
</verification>

<success_criteria>
1. The four cascade surfaces (`reinstall`, `update`, `import`, `autoupdate`) render same-marketplace-name cross-scope cases project-first, matching every list/marketplace-list surface that already uses `compareByNameThenScope`.
2. `tests/architecture/catalog-uat.test.ts` locks the project-first cross-scope cascade ordering by byte-equality for all three commands.
3. The three orchestrator unit-test cross-scope fixtures assert project-before-user partition ordering and carry truthful descriptive comments.
4. `npm run check` exits 0; no regression elsewhere in the suite.
5. The 14.2-02 tightening of MSG-GR-3 can land cleanly against the resulting orchestrator source (Wave 1 within-wave sequencing: 14.2-02 depends_on 14.2-01).
</success_criteria>

<output>
Create `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-01-SUMMARY.md` when done.
</output>
