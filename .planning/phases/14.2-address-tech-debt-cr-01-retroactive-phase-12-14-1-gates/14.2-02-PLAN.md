---
phase: 14.2
plan: 02
type: execute
wave: 1
depends_on:
  - 14.2-01
files_modified:
  - tests/lint-rules/msg-gr-3-per-scope.js
  - tests/lint-rules/msg-gr-3-per-scope.test.js
  - eslint.config.js
  - docs/messaging-style-guide.md
  - tests/architecture/msg-rule-registry.test.ts
  - .planning/PROJECT.md
autonomous: true
requirements:
  - CMC-03

must_haves:
  truths:
    - "`tests/lint-rules/msg-gr-3-per-scope.js` defines two active AST detectors: (a) `FunctionDeclaration` visitor flagging a `scopeOrder`-named helper whose `\"user\"` branch returns a strictly lower numeric value than the `\"project\"` branch; (b) `ArrayExpression` visitor flagging the two-element literal `[\"user\", \"project\"]`."
    - "The rule reports `messageId: \"userFirstScopeOrder\"` and `messageId: \"userFirstScopeIteration\"`; both messages cite MSG-GR-3 and direct the reader to `compareByNameThenScope` in `presentation/sort.ts` and `docs/messaging-style-guide.md` §1."
    - "`eslint.config.js` registers `msg/msg-gr-3-per-scope: \"error\"` under the orchestrator-scoped block (`files: [\"extensions/pi-claude-marketplace/orchestrators/**/*.ts\"]`) and removes it from the whole-extension meta-assertion bag (Block 6)."
    - "`tests/lint-rules/msg-gr-3-per-scope.test.js` `invalid:` array contains at least two RuleTester cases (one per detection axis), each asserting the corresponding `messageId`."
    - "`tests/architecture/msg-rule-registry.test.ts` exits 0 (4-way parity preserved: rule file ↔ eslint.config ↔ frontmatter ↔ style-guide row)."
    - "`docs/messaging-style-guide.md` MSG-GR-3 frontmatter and row no longer describe the rule as a \"structural meta-assertion\" or \"no-op\"; wording reads \"active AST lint check\" (or equivalent)."
    - "`.planning/PROJECT.md` Key Decisions table contains a new locked decision row dated 2026-05-24 superseding D-14-09's no-op classification with the active-rule classification, citing both the audit (CR-01 driver) and D-30 (v1.3 user-contract anchor)."
    - "`npm run check` exits 0 against the orchestrator source produced by 14.2-01 (no false positives in the canonical comparator import in reinstall.ts/update.ts/import/execute.ts; no false positives in `presentation/sort.ts` because the per-rule `files:` block scopes detection to orchestrators only)."
    - "Implements CONTEXT.md decisions D-05 (replace no-op AST visitor with active two-axis check: scopeOrder helper detection + user-first array literal detection), D-06 (apply rule to orchestrators only via per-rule files block in eslint.config.js), D-07 (4-way registry parity update -- rule docs, style-guide row, msg-rule-registry fixtures), and D-08 (PROJECT.md Key Decisions row recording the D-14-09 supersession with CR-01 and D-30 references)."
  artifacts:
    - path: "tests/lint-rules/msg-gr-3-per-scope.js"
      provides: "Active two-detector ESLint rule replacing the prior no-op meta-assertion."
      contains: "userFirstScopeOrder"
    - path: "tests/lint-rules/msg-gr-3-per-scope.test.js"
      provides: "RuleTester invalid-cases asserting the two messageIds."
      contains: "userFirstScopeIteration"
    - path: "eslint.config.js"
      provides: "MSG-GR-3 wired under orchestrator-scoped block; removed from whole-extension meta-assertion bag."
      contains: "msg/msg-gr-3-per-scope"
    - path: "docs/messaging-style-guide.md"
      provides: "MSG-GR-3 description updated to reflect active AST enforcement."
      contains: "MSG-GR-3"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row superseding D-14-09's no-op classification."
      contains: "MSG-GR-3"
  key_links:
    - from: "tests/lint-rules/msg-gr-3-per-scope.js"
      to: "eslint.config.js"
      via: "msgPlugin registration of `msg/msg-gr-3-per-scope`"
      pattern: "msg-gr-3-per-scope"
    - from: "tests/lint-rules/msg-gr-3-per-scope.js"
      to: "docs/messaging-style-guide.md"
      via: "createRule docs URL builder anchors to MSG-GR-3 row"
      pattern: "messaging-style-guide.md#msg-gr-3"
    - from: ".planning/PROJECT.md"
      to: "docs/messaging-style-guide.md"
      via: "D-14-2-08 supersession entry references MSG-GR-3 and D-14-09"
      pattern: "MSG-GR-3"
---

<objective>
Tighten the MSG-GR-3 lint rule from a no-op meta-assertion (`Program: () => {}` per D-14-09) to an active two-axis AST check that detects (a) local user-first `function scopeOrder` helpers and (b) `["user", "project"]` iteration literals in orchestrator files. This prevents CR-01-class regressions: any future drift that re-introduces a local helper or a user-first iteration literal in `extensions/pi-claude-marketplace/orchestrators/**/*.ts` will fail `npm run lint`.

Purpose: D-14-09 classified MSG-GR-3 as "structurally enforced" by `presentation/marketplace-list.ts` + `catalog-uat.test.ts` on the assumption that no other surface carried scope-ordering logic. CR-01 disproved that assumption -- three orchestrator helpers and one autoupdate iteration drifted in production. The audit recommendation explicitly calls for upgrading the rule from no-op to active. After 14.2-01 cleans the orchestrator source, 14.2-02 lands the rule against clean code so the first run is green; subsequent regressions fail loudly.

Output: One rule file rewritten with two AST detectors; one RuleTester test file extended with two invalid cases; one eslint.config.js block moved + rewired; one style-guide row description updated; one msg-rule-registry parity preserved; one PROJECT.md Key Decisions row appended documenting the D-14-09 → D-14-2-08 supersession.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md
@docs/messaging-style-guide.md

<interfaces>
<!-- The MSG-* rule library's active-rule boilerplate. Replicated across 34 rules. -->

From tests/lint-rules/msg-sd-2-soft-dep-predicate.js (FULL-IMPL active analog):
  import { ESLintUtils } from "@typescript-eslint/utils";
  const createRule = ESLintUtils.RuleCreator(
    (name) => `https://github.com/acolomba/pi-claude-marketplace/blob/main/docs/messaging-style-guide.md#${name.replace(/^msg-/, "msg-")}`,
  );
  export default createRule({
    name: "msg-sd-2-soft-dep-predicate",
    meta: { type: "problem", docs: { description: "..." }, messages: {...}, schema: [] },
    defaultOptions: [],
    create(context) { return { Literal(node) {...}, TemplateLiteral(node) {...} }; },
  });

From eslint.config.js (orchestrator-scoped block analog, current location of MSG-SR-1..6):
  {
    files: ["extensions/pi-claude-marketplace/orchestrators/**/*.ts"],
    plugins: { msg: msgPlugin },
    rules: { "msg/msg-sr-1-success-routing": "error", /* ... */ },
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite tests/lint-rules/msg-gr-3-per-scope.js from no-op to active two-axis AST detector + extend test file with invalid cases</name>
  <files>tests/lint-rules/msg-gr-3-per-scope.js, tests/lint-rules/msg-gr-3-per-scope.test.js</files>
  <read_first>
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "tests/lint-rules/msg-gr-3-per-scope.js -- rewrite from no-op to active AST detector (D-05)" for the active-rule body pattern, the `messages` block content, the `description` update wording, and the `isUserFirstScopeOrder` helper sketch.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "tests/lint-rules/msg-gr-3-per-scope.test.js -- extend with invalid cases" for the RuleTester `invalid:` array shape (two cases, one per detection axis) and the `valid:` array sanity check.
    - `tests/lint-rules/msg-sd-2-soft-dep-predicate.js` (FULL-IMPL active-rule template -- this is the canonical analog).
    - `tests/lint-rules/msg-sd-2-soft-dep-predicate.test.js` (mirror RuleTester file).
    - `tests/lint-rules/msg-gr-3-per-scope.js` lines 21-53 (current `createRule` boilerplate + no-op visitor to replace).
    - `tests/lint-rules/msg-gr-3-per-scope.test.js` lines 25-34 (current smoke-only test body to extend).
  </read_first>
  <behavior>
    - Given a JavaScript source containing `function scopeOrder(scope) { return scope === "user" ? 0 : 1; }`, the rule reports a `userFirstScopeOrder` violation on the function declaration.
    - Given a JavaScript source containing `const scopes = ["user", "project"];`, the rule reports a `userFirstScopeIteration` violation on the array expression.
    - Given the canonical `compareByNameThenScope` source (or any code using `["project", "user"]` iteration order or a non-`scopeOrder`-named function), the rule reports nothing.
    - RuleTester `valid:` array passes; RuleTester `invalid:` array reports exactly the expected `messageId` per case.
  </behavior>
  <action>
    Preserve the existing `createRule` boilerplate, the named export shape, and the docs-URL builder (these are identical across all 34 rules). Replace the `create(context) { return { Program: () => {} }; }` body with two visitors: a `FunctionDeclaration` visitor that returns immediately if `node.id?.name !== "scopeOrder"` and otherwise calls a local `isUserFirstScopeOrder(body)` helper to decide whether to call `context.report({ node, messageId: "userFirstScopeOrder" })`; and an `ArrayExpression` visitor that checks for exactly two `Literal` elements whose values are `"user"` and `"project"` in that order and calls `context.report({ node, messageId: "userFirstScopeIteration" })`. Add the `isUserFirstScopeOrder(body)` helper at module scope per the PATTERNS.md sketch -- check that the body is a `BlockStatement` with one `ReturnStatement` whose argument is a `ConditionalExpression` of shape `scope === "user" ? <lower-numeric> : <higher-numeric>`. Replace the single `structurallyEnforced` message with the two new messages exactly as worded in PATTERNS.md. Update the `docs.description` field with the wording from PATTERNS.md citing both detection axes and the canonical comparator. In `msg-gr-3-per-scope.test.js`, keep the existing `valid:` smoke case, add a `valid:` case for `const scopes = ["project", "user"];` (project-first iteration), and add two `invalid:` cases matching the PATTERNS.md examples (each with the corresponding `errors: [{ messageId: ... }]` assertion).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/lint-rules/msg-gr-3-per-scope.test.js &amp;&amp; grep -c "userFirstScopeOrder" tests/lint-rules/msg-gr-3-per-scope.js &amp;&amp; grep -c "userFirstScopeIteration" tests/lint-rules/msg-gr-3-per-scope.js &amp;&amp; test "$(grep -c 'Program: () => {}' tests/lint-rules/msg-gr-3-per-scope.js)" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/lint-rules/msg-gr-3-per-scope.test.js` exits 0 with the RuleTester invalid cases reporting the expected `messageId`s.
    - `grep -c "userFirstScopeOrder" tests/lint-rules/msg-gr-3-per-scope.js` is at least 2 (one in `messages`, one in `context.report`).
    - `grep -c "userFirstScopeIteration" tests/lint-rules/msg-gr-3-per-scope.js` is at least 2.
    - `grep -c "Program: () => {}" tests/lint-rules/msg-gr-3-per-scope.js` equals 0 (the no-op visitor is gone).
    - `grep -c "FunctionDeclaration" tests/lint-rules/msg-gr-3-per-scope.js` is at least 1.
    - `grep -c "ArrayExpression" tests/lint-rules/msg-gr-3-per-scope.js` is at least 1.
  </acceptance_criteria>
  <done>The rule file defines two active AST detectors with distinct messageIds; the RuleTester file's `invalid:` array exercises both axes; the no-op `Program: () => {}` visitor is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move msg-gr-3-per-scope to the orchestrator-scoped ESLint block, update style-guide MSG-GR-3 description, append PROJECT.md Key Decisions row, verify msg-rule-registry parity</name>
  <files>eslint.config.js, docs/messaging-style-guide.md, .planning/PROJECT.md, tests/architecture/msg-rule-registry.test.ts</files>
  <read_first>
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "eslint.config.js -- promote `msg-gr-3-per-scope` out of meta-assertion bag (D-06)" for the source location (MSG-Block 6 at lines 259-285, currently whole-extension scoped) and target location (MSG-Block 1 at lines 152-165, orchestrator-scoped). The recommendation is option (a): add to MSG-Block 1, remove from MSG-Block 6 -- minimum config churn.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "docs/messaging-style-guide.md -- MSG-GR-3 row description (D-07)" -- key directive: the style-guide body at line 112 may already be accurate; the verify-only step is to read line 112 and replace "structural meta-assertion" / "no-op lint rule" phrasings with "active AST lint check enforced by tests/lint-rules/msg-gr-3-per-scope.js" if any such phrase exists.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section ".planning/PROJECT.md -- D-08 supersession decision entry (D-08)" for the new row's full markdown, including the `**D-14-2-08 (2026-05-24):**` prefix, the two-clause justification (CR-01 + D-30), and the `Locked` status.
    - `tests/architecture/msg-rule-registry.test.ts` lines 135-162 for the 4-way parity assertion pattern -- specifically the literal-string scan that verifies `"msg/msg-gr-3-per-scope":` appears somewhere in `eslint.config.js` (the block-location move does not break parity).
    - `eslint.config.js` lines 152-165 (MSG-Block 1, orchestrator-scoped target), 259-285 (MSG-Block 6, current location to remove from).
    - `docs/messaging-style-guide.md` MSG-GR-3 row at approximately line 112, plus the YAML frontmatter MSG-GR-3 entry if present.
    - `.planning/PROJECT.md` Key Decisions table around line 129 (the D-30 row format reference).
  </read_first>
  <behavior>
    - `eslint.config.js` registers `"msg/msg-gr-3-per-scope": "error"` exactly once, under the block whose `files:` pattern is `["extensions/pi-claude-marketplace/orchestrators/**/*.ts"]`.
    - The literal `"msg/msg-gr-3-per-scope":` no longer appears under the whole-extension MSG-Block 6 (`files: ["extensions/pi-claude-marketplace/**/*.ts"]`).
    - `tests/architecture/msg-rule-registry.test.ts` finds the rule registered, frontmatter-listed, and style-guide-described -- the 4-way parity holds.
    - The style guide's MSG-GR-3 row contains no "structural meta-assertion" or "no-op lint rule" wording; an active-AST description replaces any such phrasing.
    - `.planning/PROJECT.md` Key Decisions table contains a `**D-14-2-08 (2026-05-24):**` row referencing both the audit (CR-01) and D-30.
  </behavior>
  <action>
    In `eslint.config.js`: add the line `"msg/msg-gr-3-per-scope": "error",` to the orchestrator-scoped block (the MSG-Block 1 group at ~lines 152-165 that already registers MSG-SR-1..6). Remove the existing `"msg/msg-gr-3-per-scope": "error",` line from the MSG-Block 6 whole-extension meta-assertion bag at ~lines 259-285. Verify the file still passes Prettier and the `import-x/order` rule. In `docs/messaging-style-guide.md`: locate the MSG-GR-3 row (around line 112) and the YAML frontmatter MSG-GR-3 entry (if present); replace any "structural meta-assertion" or "no-op" phrasing with "active AST lint check enforced by `tests/lint-rules/msg-gr-3-per-scope.js`" (preserve all other body text). Do NOT add or remove `status_tokens` / `reasons` / `markers` / `pattern_classes` frontmatter entries. In `.planning/PROJECT.md`: append a new row to the Key Decisions table after the most recent D-14-* decision, using the PATTERNS.md template exactly (`**D-14-2-08 (2026-05-24):** MSG-GR-3 lint rule moves from no-op meta-assertion (D-14-09) to active AST check ...` with the two-clause rationale and `Locked` status column). Run `node --test tests/architecture/msg-rule-registry.test.ts` to confirm 4-way parity holds.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/architecture/msg-rule-registry.test.ts &amp;&amp; awk '/files: \["extensions\/pi-claude-marketplace\/orchestrators\/\*\*\/\*\.ts"\]/,/^\s*\}/' eslint.config.js | grep -c 'msg/msg-gr-3-per-scope' &amp;&amp; awk '/files: \["extensions\/pi-claude-marketplace\/\*\*\/\*\.ts"\]/,/^\s*\}/' eslint.config.js | grep -c 'msg/msg-gr-3-per-scope' &amp;&amp; grep -c "D-14-2-08" .planning/PROJECT.md &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/msg-rule-registry.test.ts` exits 0.
    - In `eslint.config.js`, the `awk` extract of the orchestrator-scoped block (between its `files:` line and the next closing brace at the same indentation) contains at least one `msg/msg-gr-3-per-scope` line.
    - In `eslint.config.js`, the `awk` extract of the whole-extension block (between its `files: ["extensions/pi-claude-marketplace/**/*.ts"]` line and the next closing brace) contains zero `msg/msg-gr-3-per-scope` lines.
    - `grep -c "D-14-2-08" .planning/PROJECT.md` equals 1 (the new locked decision row).
    - `grep -ci "structural meta-assertion\|no-op lint rule" docs/messaging-style-guide.md` equals 0 for the MSG-GR-3 line range (sanity check via targeted line read).
    - `npm run check` exits 0 (full repository green against the post-14.2-01 orchestrator source and the post-Task-1 active rule).
  </acceptance_criteria>
  <done>The MSG-GR-3 rule is wired in the orchestrator-scoped ESLint block only; the style-guide description reflects active enforcement; PROJECT.md records the D-14-09 → D-14-2-08 supersession; `npm run check` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint-config | The MSG-GR-3 rule's per-rule `files:` block scopes detection to orchestrators only -- false-positive risk against the canonical comparator in `presentation/sort.ts` is mitigated structurally. False-negative risk for cross-scope drift outside `orchestrators/` is accepted with a doc note in PROJECT.md (the rule is intentionally scoped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-02-01 | Tampering | tests/lint-rules/msg-gr-3-per-scope.js | mitigate | Per-rule `files:` block in `eslint.config.js` scopes detection to `extensions/pi-claude-marketplace/orchestrators/**/*.ts`; the canonical `compareByNameThenScope` in `presentation/sort.ts` is outside that scope and cannot trip the rule. |
| T-14.2-02-02 | Information disclosure | tests/lint-rules/msg-gr-3-per-scope.js | accept | Accepted false-negative: cross-scope drift in non-orchestrator code (e.g., a new bridge file with a local `scopeOrder`) does not fail lint. Documented in PROJECT.md D-14-2-08 row as intentional scope; future expansion would extend the `files:` glob. |
</threat_model>

<verification>
- `node --test tests/lint-rules/msg-gr-3-per-scope.test.js` exits 0.
- `node --test tests/architecture/msg-rule-registry.test.ts` exits 0.
- `npm run check` exits 0 (after both tasks).
- `grep -c "Program: () => {}" tests/lint-rules/msg-gr-3-per-scope.js` equals 0.
- `grep -c "D-14-2-08" .planning/PROJECT.md` equals 1.
</verification>

<success_criteria>
1. MSG-GR-3 is an active two-axis AST lint rule, scoped to `extensions/pi-claude-marketplace/orchestrators/**/*.ts`, that would have caught CR-01 at lint time had it existed in Phase 14.
2. RuleTester exercises both detection axes with one valid + one invalid case each.
3. The style guide describes MSG-GR-3 as an active enforcement rule, not a meta-assertion.
4. PROJECT.md Key Decisions row D-14-2-08 records the supersession of D-14-09's no-op classification, citing CR-01 and D-30.
5. The 4-way parity test stays green; `npm run check` exits 0.
</success_criteria>

<output>
Create `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-02-SUMMARY.md` when done.
</output>
