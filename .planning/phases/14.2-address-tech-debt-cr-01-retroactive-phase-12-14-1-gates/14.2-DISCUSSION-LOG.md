# Phase 14.2: Address tech debt: CR-01 + retroactive Phase 12 / 14.1 gates - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-05-24
**Phase:** 14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates
**Areas discussed:** CR-01 remediation scope, MSG-GR-3 rule tightening, Retroactive gates approach, Cosmetic ROADMAP drift

---

## CR-01 remediation scope

### Q1 -- How should the 3 local `scopeOrder` helpers be remediated?

| Option | Description | Selected |
|--------|-------------|----------|
| Replace with compareByNameThenScope | Delete the local `function scopeOrder` and route the existing cascade-block sort through the canonical `compareByNameThenScope`. Single source of truth. | ✓ |
| Keep local helpers, invert to project-first | Flip `return scope === "user" ? 0 : 1;` to `return scope === "project" ? 0 : 1;`. Smaller diff but keeps the duplication. | |
| Move scope-only ordering into presentation/sort.ts | Add an exported `compareScopeProjectFirst(scope: Scope): number`. | |

**User's choice:** Replace with compareByNameThenScope
**Notes:** Aligns CR-01 fix with the existing canonical comparator that already serves every list/marketplace-list/cascade-summary surface.

### Q2 -- autoupdate.ts has scopes=["user", "project"] iteration but a comment claiming project-first. What should 14.2 do?

| Option | Description | Selected |
|--------|-------------|----------|
| Flip iteration to `["project", "user"]` | Aligns actual order with the comment and with MSG-GR-3 / `compareByNameThenScope`. | ✓ |
| Keep iteration, fix the comment | Rewrite comment to truthfully say user-enumerated-first. Preserves current behavior. | |
| Defer autoupdate change to a later phase | 14.2 fixes 3 cascade orchestrators + MSG-GR-3 only; autoupdate gets a one-line comment patch + TODO. | |

**User's choice:** Flip iteration to ["project", "user"]
**Notes:** Inverse of the typical "comment is wrong" fix -- here the code needs to catch up to the already-correct comment.

### Q3 -- How should the regression test for CR-01 be structured?

| Option | Description | Selected |
|--------|-------------|----------|
| Extend catalog-uat fixtures | Add same-marketplace-name-cross-scope fixture to `catalog-uat.test.ts`. Locks the bug at the user-contract surface. | ✓ |
| Architecture test against `compareByNameThenScope` usage | AST scan: no orchestrator defines local `function scopeOrder`; all 3 orchestrators import the canonical. | |
| Both | Belt-and-braces. | |

**User's choice:** Extend catalog-uat fixtures
**Notes:** The structural-enforcement axis (no local helpers) is covered by the tightened MSG-GR-3 rule chosen in the next area, so the catalog-uat extension is sufficient on its own.

### Q4 -- Beyond catalog-uat: also touch the existing orchestrator-level test fixtures the audit flagged?

| Option | Description | Selected |
|--------|-------------|----------|
| Update all 3 orchestrator test files | Update `reinstall.test.ts:984-988` + corresponding cross-scope fixtures in `update.test.ts` and `import/execute.test.ts`. | ✓ |
| Only reinstall.test.ts | The one the audit explicitly cited. | |
| Only catalog-uat | Defer orchestrator-test changes; rely on byte-equality alone. | |

**User's choice:** Update all 3 orchestrator test files
**Notes:** Avoids stale unit-level assertions that would break `npm run check` once the orchestrator code starts producing project-first cascade blocks.

---

## MSG-GR-3 rule tightening

### Q1 -- Tighten `msg-gr-3-per-scope.js` from no-op meta-assertion to an active lint check?

| Option | Description | Selected |
|--------|-------------|----------|
| Tighten the rule | Replace no-op `Program: () => {}` with active AST checks for local user-first helpers + user-first iteration arrays in orchestrator files. | ✓ |
| Keep no-op, add a one-shot architecture test instead | Architecture test scans for local scope-ordering helpers; MSG-GR-3 stays a meta-assertion per D-14-09. | |
| Leave the rule as no-op; rely on catalog-uat fixture only | New cross-scope fixture is the only lock. | |

**User's choice:** Tighten the rule
**Notes:** Structurally prevents CR-01-class regressions across the 34-rule drift-guard surface already enforced in `npm run lint`.

### Q2 -- What pattern should the tightened MSG-GR-3 rule actually detect?

| Option | Description | Selected |
|--------|-------------|----------|
| Both AST patterns | (a) `function scopeOrder` or arrow returning user-first numeric; (b) `["user", "project"]` array literals in orchestrators. | ✓ |
| Only the local-helper pattern | Iteration-order drift remains uncovered by lint. | |
| Custom -- describe via Other | | |

**User's choice:** Both AST patterns
**Notes:** Single rule covers both axes; subsumes the architecture-test option from CR-01 Q3.

### Q3 -- Where should the tightened MSG-GR-3 rule apply?

| Option | Description | Selected |
|--------|-------------|----------|
| All orchestrators | `extensions/pi-claude-marketplace/orchestrators/**/*.ts`. | ✓ |
| Whole extension surface | Catches `presentation/sort.ts`; needs allowlist for the canonical comparator. | |
| Only the 3 known-bad files | Hard-coded `files:` block; brittle for new orchestrators. | |

**User's choice:** All orchestrators
**Notes:** Mirrors how the other 33 MSG-* rules are scoped in `eslint.config.js`.

### Q4 -- How should we handle `msg-rule-registry.test.ts` parity when the rule body changes?

| Option | Description | Selected |
|--------|-------------|----------|
| Update parity fixtures | Update MSG-GR-3 description in registry + style-guide frontmatter to "active AST lint check". | ✓ |
| Keep the meta-assertion label in registry | Leave the registry describing it as meta-assertion but implement active check. | |

**User's choice:** Update parity fixtures
**Notes:** Triggers the D-14-09 supersession note recorded in PROJECT.md per D-08.

---

## Retroactive gates approach

### Q1 -- How should the missing Phase 12 / 14.1 gates be sequenced relative to 14.2's CR-01 work?

| Option | Description | Selected |
|--------|-------------|----------|
| Inline as 14.2 plans | All four gate commands run as plans inside 14.2. | ✓ |
| Run gates BEFORE CR-01 fix | Gates baseline first; tradeoff: VALIDATION.md may need re-run after CR-01 lands. | |
| Gates as a separate post-merge step | 14.2 ships CR-01 fix only; gates listed in CONTEXT.md/SUMMARY checklist. | |

**User's choice:** Inline as 14.2 plans
**Notes:** Closes all debts inside 14.2 so `/gsd:complete-milestone v1.3` has nothing left.

### Q2 -- Phase 12's VALIDATION.md is in draft with nyquist=false. How should 14.2 handle the existing file?

| Option | Description | Selected |
|--------|-------------|----------|
| Re-run `/gsd:validate-phase 12` to completion | Treat draft as checkpoint; let validator either generate coverage or accept gaps with rationale. | ✓ |
| Audit-only -- promote draft to validated without code changes | Document why nyquist=false is acceptable for primitives phase. | |
| Discard draft and re-validate from scratch | Risks losing partial work. | |

**User's choice:** Re-run `/gsd:validate-phase 12` to completion
**Notes:** Lets the validator agent do the work the original gate-run would have done.

### Q3 -- Should the secure-phase / validate-phase runs for 12 and 14.1 be wrapped as standard plans?

| Option | Description | Selected |
|--------|-------------|----------|
| Standard plans that invoke the slash commands | Plans whose tasks are `Skill(gsd:secure-phase 12)` etc. Gets executor atomic-commit + checkpoint. | ✓ |
| Run gate commands manually after 14.2-01/02 land | 14.2 has only 2 plans; user runs the 4 commands manually. | |

**User's choice:** Standard plans that invoke the slash commands
**Notes:** Plays cleanly with `/gsd:execute-phase 14.2` wave parallelization and produces SUMMARY.md trail per plan.

### Q4 -- Should Phase 12 / 14.1 retroactive gate plans run BEFORE or AFTER the CR-01 fix plans within 14.2?

| Option | Description | Selected |
|--------|-------------|----------|
| Gates AFTER CR-01 fix | Wave 1: CR-01 fix + MSG-GR-3. Wave 2: gates. | ✓ |
| Gates BEFORE CR-01 fix | Baseline first; risk doubling work on nyquist re-run. | |
| Parallel -- single wave | All 4 plans together; gates may miss the new fixture. | |

**User's choice:** Gates AFTER CR-01 fix
**Notes:** Phase 14.1's nyquist validation will see the new `catalog-uat.test.ts` cross-scope fixture as covered.

---

## Cosmetic ROADMAP drift

### Q1 -- Where should the cosmetic-drift fix land?

| Option | Description | Selected |
|--------|-------------|----------|
| Fold into 14.2 as a small plan | Add 14.2-05 (Wave 3): flip 29 CMC checkboxes + 14.1 phase-details row. | ✓ |
| Defer to `/gsd:complete-milestone v1.3` | Let the milestone workflow handle cosmetic drift when archiving. | |
| Quick-task right now (outside 14.2) | Run `/gsd-quick` to flip checkboxes; 14.2 only handles functional debt. | |

**User's choice:** Fold into 14.2 as a small plan
**Notes:** Closes ALL v1.3 tech debt inside 14.2.

### Q2 -- Should 14.2-05 also add a structural guard so future phases automatically flip ROADMAP checkboxes on plan completion?

| Option | Description | Selected |
|--------|-------------|----------|
| Investigate only; add note to 14.2-05 SUMMARY | Document the gap; fix belongs in the GSD plugin (different project). | ✓ |
| Add a planning-doc lint check inside this project | `tests/architecture/roadmap-coverage-parity.test.ts`. | |
| No guard; just fix the drift | Pure checkbox flip + row update. | |

**User's choice:** Investigate only; add note to 14.2-05 SUMMARY
**Notes:** Scope discipline -- this project is `pi-claude-marketplace`, not the GSD plugin.

### Q3 -- Final shape: 14.2 has 5 plans across 3 waves. Confirm or adjust?

| Option | Description | Selected |
|--------|-------------|----------|
| Confirm 5-plan / 3-wave shape | Wave 1: 14.2-01 + 14.2-02. Wave 2: 14.2-03 + 14.2-04. Wave 3: 14.2-05. | ✓ |
| Collapse 03+04 into a single retroactive-gates plan | Single plan, 4 sequential subagent calls. | |
| Split 01 into cascade-orchestrators + autoupdate plans | 6 plans / 3 waves. | |

**User's choice:** Confirm 5-plan / 3-wave shape
**Notes:** Planner can refine within these decisions but the wave/plan envelope is locked.

---

## Claude's Discretion

- Exact AST detector implementation in MSG-GR-3 (e.g., `@typescript-eslint/utils` `ESLintUtils.RuleCreator` helpers vs raw selectors). Planner picks based on parity with the other 33 MSG-* rules.
- Whether the `compareByNameThenScope` import in the 3 orchestrator files lives in a shared barrel or each imports directly from `presentation/sort.ts`. Planner picks based on existing import conventions in those files.
- Invocation pattern for retroactive-gate plans (`Skill(gsd:secure-phase 12)` vs `Bash(gsd-sdk ...)`). Planner picks based on slash-command skill availability and existing executor patterns.

## Deferred Ideas

- GSD-plugin enhancement: auto-flip ROADMAP.md Coverage checkboxes on plan completion. Investigation note in 14.2-05 SUMMARY documents the gap; fix belongs in the GSD plugin project.
- Project-local planning-doc parity lint test. Rejected in cosmetic-drift area.
- Splitting `14.2-01` into separate cascade-orchestrators and autoupdate plans. Rejected in plan-shape area.
- Architecture test scanning orchestrator files for local scope-ordering helpers. Subsumed by the tightened MSG-GR-3 rule.
