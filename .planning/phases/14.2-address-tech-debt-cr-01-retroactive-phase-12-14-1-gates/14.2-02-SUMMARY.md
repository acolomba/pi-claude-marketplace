---
phase: 14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates
plan: 02
subsystem: testing
tags: [eslint, msg-gr-3, ast-rule, drift-guard, lint-rule, msg-gr-3-per-scope]

# Dependency graph
requires:
  - phase: 14-drift-guard-test-alignment
    provides: 34-rule MSG-* drift-guard plugin, RuleTester wiring, msg-rule-registry 4-way parity test
  - phase: 14.2-01-cascade-block-ordering
    provides: clean orchestrator source (3 scopeOrder helpers deleted, autoupdate iteration flipped to project-first)
provides:
  - Active two-axis MSG-GR-3 lint rule (no longer a no-op meta-assertion) scoped to orchestrators/**/*.ts
  - Per-rule files: block ensuring detection cannot trip on the canonical compareByNameThenScope in presentation/sort.ts
  - PROJECT.md D-14-2-08 decision superseding D-14-09 with citations to CR-01 and D-30
  - Seven additional orchestrator user-first iteration literals cleaned up beyond CR-01's enumerated four sites
affects: [v1.3-milestone-close, future-orchestrator-additions, GR-3-compliance]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Active AST rule with per-rule files: containment: scope detection to orchestrators/ so canonical comparators in presentation/sort.ts are structurally exempt from false positives"
    - "Promotion-from-meta-assertion pattern: MSG-* rules can move between MSG-Block 6 (whole-extension no-op bag) and MSG-Block 1 (orchestrator-scoped active rules) as detection logic matures; the 4-way registry parity test (msg-rule-registry.test.ts) only requires the literal `msg/<name>:` appears somewhere in eslint.config.js, so the block move does not break parity"

key-files:
  created:
    - .planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-02-SUMMARY.md
  modified:
    - tests/lint-rules/msg-gr-3-per-scope.js
    - tests/lint-rules/msg-gr-3-per-scope.test.js
    - eslint.config.js
    - .planning/PROJECT.md
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts

key-decisions:
  - "D-DOC-1 (this plan): Did NOT modify the docs/messaging-style-guide.md MSG-GR-3 row body. Per the plan's `<read_first>` directive citing PATTERNS.md `verify-only step`, the row at line 112 contains no `structural meta-assertion` or `no-op lint rule` phrasing -- the description was already accurate for an active enforcement model and required zero text change. The `description` field tweak called for by D-07 lives in the lint rule's `docs.description` string (now updated) and the PROJECT.md D-14-2-08 row -- both delivered."
  - "D-DOC-2 (this plan): Cleaned up 7 additional `[\"user\", \"project\"]` iteration literals discovered by the freshly-active rule across 6 orchestrator files. Audit CR-01 had enumerated only 4 sites (3 scopeOrder helpers + autoupdate.ts:114, all in 14.2-01). The active rule from Task 1 surfaced the broader set: marketplace/list.ts:42, marketplace/shared.ts:407, marketplace/shared.ts:426, marketplace/update.ts:195, plugin/reinstall.ts:314, plugin/shared.ts:163, plugin/update.ts:1088. All flipped to `[\"project\", \"user\"]` per the canonical project-first ordering with a citing comment. Required for the plan's `npm run check` exits 0 acceptance criterion; classified as Rule 1/3 auto-fix below."

patterns-established:
  - "MSG-GR-3 detection axes: (a) FunctionDeclaration named `scopeOrder` whose body is `return scope === \"user\" ? <low> : <high>` -- user-first comparator helper; (b) two-element ArrayExpression of literal `\"user\"` then literal `\"project\"` -- iteration order whose stable-sort ties yield user-first output. Pattern (a) catches the local-helper drift CR-01 surfaced; pattern (b) catches the iteration drift autoupdate.ts:114 surfaced."
  - "Per-rule containment pattern for active MSG-* rules: when a rule's detection logic could false-positive against canonical declarations in non-orchestrator code (presentation/, shared/grammar/), the per-rule files: block scopes detection to orchestrators/**/*.ts only. The canonical declaration files (sort.ts comparator definition, reasons.ts enum) sit outside the glob and cannot trip."

requirements-completed:
  - CMC-03

# Metrics
duration: 12min
completed: 2026-05-24
---

# Phase 14.2 Plan 02: Tighten MSG-GR-3 lint rule from no-op to active AST check Summary

**Promotes `tests/lint-rules/msg-gr-3-per-scope.js` from a no-op meta-assertion (D-14-09) to an active two-axis AST rule detecting (a) local user-first `function scopeOrder` helpers and (b) `["user", "project"]` iteration literals in orchestrator files; cleans 7 additional drift sites the rule surfaced beyond audit CR-01's enumerated four; records the D-14-09 -> D-14-2-08 supersession in PROJECT.md.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-25T00:18:00Z (approx)
- **Completed:** 2026-05-25T00:31:00Z (approx)
- **Tasks:** 2
- **Files modified:** 9 (1 rule, 1 test, 1 eslint config, 1 PROJECT.md decision row, 6 orchestrator source files)

## Accomplishments

- **Active MSG-GR-3 enforcement (Task 1):** Replaced the no-op `Program: () => {}` visitor in `tests/lint-rules/msg-gr-3-per-scope.js` with two AST visitors (`FunctionDeclaration` for the `scopeOrder` helper drift, `ArrayExpression` for the iteration-literal drift) and a body-pattern matcher `isUserFirstScopeOrder` that requires the exact `return scope === "user" ? <low> : <high>` structural shape. Renamed the single `structurallyEnforced` message into two distinct `userFirstScopeOrder` / `userFirstScopeIteration` messages, each citing `compareByNameThenScope` and the style guide. Extended the RuleTester file with three `valid:` cases (smoke, canonical iteration literal, non-`scopeOrder` helper) and two `invalid:` cases (one per detection axis) -- TDD RED via the extended test → GREEN via the rewrite.
- **Block move + style-guide verification + decision row (Task 2):** Promoted `msg/msg-gr-3-per-scope` from MSG-Block 6 (whole-extension meta-assertion bag, 16 → 15 entries) to MSG-Block 1 (orchestrator-scoped active rules alongside MSG-SR-1..6) in `eslint.config.js`. Verified the style guide MSG-GR-3 row at line 112 already lacked any "structural meta-assertion" / "no-op lint rule" phrasing per the plan's verify-only spec -- no body change required. Appended Key Decisions row `D-14-2-08 (2026-05-24)` to `.planning/PROJECT.md` recording the D-14-09 supersession with explicit citations to audit CR-01 and D-30 (v1.3 user-contract anchor).
- **Residual drift cleanup (Task 2 deviation):** The freshly-active rule surfaced 7 additional `["user", "project"]` literals across 6 orchestrator files that 14.2-01 did NOT touch. Flipped each to `["project", "user"]` per the canonical MSG-GR-3 ordering, including two `MarketplaceNotFoundError` sites whose user-visible `scopes.join(", ")` error message now renders "project, user" instead of "user, project". Added a citing comment ("Iteration order is project-first per MSG-GR-3 / compareByNameThenScope") at each site.
- **`npm run check` exits 0:** typecheck + ESLint + Prettier + 1249/1249 node:test (up from 1245 baseline; the additional tests are from 14.2-01's catalog UAT fixtures landing in Wave 1 of this phase). `tests/architecture/msg-rule-registry.test.ts` 4-way parity (rule file ↔ ESLint config ↔ style-guide body ↔ plugin module) holds.

## Task Commits

Each task committed atomically with `SKIP=trufflehog` per project CLAUDE.md (TruffleHog hook's auto-updater fails to spawn under the worktree sandbox; underlying scan confirmed clean via `pre-commit run trufflehog --all-files` from the main repo path before each commit):

1. **Task 1: Activate MSG-GR-3 two-axis AST detector** -- `35cfdf3` (test)
2. **Task 2: Wire active MSG-GR-3 rule + clean residual user-first drift + record D-14-2-08** -- `09be954` (fix)

## Files Created/Modified

- `tests/lint-rules/msg-gr-3-per-scope.js` -- Replaced no-op `Program: () => {}` visitor with two active AST visitors. Added `isUserFirstScopeOrder(body)` helper that requires a `BlockStatement` with exactly one `ReturnStatement` whose argument is a `ConditionalExpression` of structural shape `scope === "user" ? <lowerNumeric> : <higherNumeric>`. Two distinct messageIds (`userFirstScopeOrder`, `userFirstScopeIteration`) each citing `compareByNameThenScope` from `presentation/sort.ts` and `docs/messaging-style-guide.md` §1. Updated `docs.description` to describe the active two-axis enforcement; updated the module-header comment block to record the D-14-2-08 supersession of D-14-09 and the per-rule `files:` containment rationale.
- `tests/lint-rules/msg-gr-3-per-scope.test.js` -- Extended the RuleTester. Three `valid:` cases: smoke (trivial code), canonical project-first iteration literal, non-`scopeOrder`-named helper. Two `invalid:` cases: user-first `scopeOrder` body (`userFirstScopeOrder`), `["user", "project"]` array literal (`userFirstScopeIteration`). Updated header comment to record the supersession + active-class wiring.
- `eslint.config.js` -- Added `"msg/msg-gr-3-per-scope": "error"` to MSG-Block 1 (orchestrator-scoped); removed it from MSG-Block 6 (whole-extension meta-assertion bag). MSG-Block 6 comment updated from "16 entries" to "15 entries" with a note pointing to the new MSG-Block 1 location and the D-14-2-08 reference. MSG-Block 1 comment updated to describe the addition and the false-positive containment rationale (canonical comparator in `presentation/sort.ts` is outside the orchestrator glob).
- `.planning/PROJECT.md` -- Appended Key Decisions row `D-14-2-08 (2026-05-24)` after the D-30 row. Two-clause rationale: (1) closes audit CR-01 with explicit enumeration of the 3 orchestrator helpers + autoupdate iteration; (2) explicitly upgrades-not-weakens D-14-09's structural-enforcement claim and reaffirms D-30 as the binding v1.3 user-contract. Documents the false-positive containment (per-rule `files:` block) so the row records both the change AND the constraint. The pre-commit `fix-unicode-dashes` hook normalized two pre-existing unicode em-dashes (in unrelated lines: the v1.3 Drift Guard ✓ entry and the CR-01 paragraph) to ASCII `--` per project policy; that change was incidental to this commit.
- `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts` -- Flipped SC-6 bare-form scope enumeration from `["user", "project"]` to `["project", "user"]` (line 42-43). Added project-first cite comment.
- `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` -- Two sites flipped: (a) `MarketplaceNotFoundError` searched-scopes argument at line 407 (the error message renders `scopes.join(", ")` user-visibly); (b) `loadVisibleMarketplaces` enumeration at line 426 with a project-first cite comment.
- `extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` -- Flipped `updateAllMarketplaces` enumeration at line 195 with a project-first cite comment.
- `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` -- Flipped `enumerateAllReinstallTargets` enumeration at line 313-314 with a project-first cite comment.
- `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` -- Flipped `MarketplaceNotFoundError` searched-scopes argument at line 163 (user-visible error-message ordering).
- `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` -- Flipped the bare-form target enumeration at line 1087-1088 with a project-first cite comment.

## Decisions Made

- **D-DOC-1 (this plan):** Did NOT modify `docs/messaging-style-guide.md` MSG-GR-3 row body. The plan's `<read_first>` directive citing PATTERNS.md "verify-only step" said to check line 112 for "structural meta-assertion" or "no-op lint rule" phrasing and replace if found. The row contains no such phrasing -- its description was already accurate for an active enforcement model. The `description` field tweak called for by D-07 is delivered in the lint rule's `meta.docs.description` (rewritten in Task 1) and in the PROJECT.md D-14-2-08 row (Task 2). Verification: `awk 'NR>=110 && NR<=115' docs/messaging-style-guide.md | grep -ci "structural meta-assertion\|no-op lint rule"` returns 0.
- **D-DOC-2 (this plan):** Cleaned up 7 additional `["user", "project"]` iteration literals beyond CR-01's enumerated 4 (which 14.2-01 cleaned up). Audit CR-01 surfaced the 3 user-first `scopeOrder` helpers + `autoupdate.ts:114` iteration -- 14.2-01 fixed those. The freshly-active rule from this plan's Task 1, run via `npm run lint` against the post-14.2-01 codebase, surfaced 7 more iteration-literal sites: `marketplace/list.ts:42`, `marketplace/shared.ts:407` and `:426`, `marketplace/update.ts:195`, `plugin/reinstall.ts:314`, `plugin/shared.ts:163`, `plugin/update.ts:1088`. Two of these (`shared.ts:407` in marketplace/, `shared.ts:163` in plugin/) pass scopes to `MarketplaceNotFoundError`, which renders `scopes.join(", ")` user-visibly in the error message -- the flip changes "user, project" to "project, user" wording. No test asserts the exact two-scope error wording (verified by `grep -rn "not found in" tests/`); single-scope tests asserting `/not found in project scope/` remain green.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] / [Rule 3 - Blocking] Cleaned 7 residual user-first iteration literals across 6 orchestrator files**

- **Found during:** Task 2 verification (`npm run check`).
- **Issue:** The plan's Task 2 acceptance criterion `npm run check exits 0` failed against the post-Task-1 active rule with 7 lint errors in orchestrator files NOT enumerated by audit CR-01 or fixed by 14.2-01. The seven sites are bare-form scope enumeration literals (`opts.scope === undefined ? ["user", "project"] : [opts.scope]`) plus two `MarketplaceNotFoundError` searched-scope arguments. They share the same drift signature as `autoupdate.ts:114` (which CR-01 cited and 14.2-01 fixed): V8 stable-sort on same-name cross-scope ties preserves the iteration-order construction order, so user-first iteration produces user-first user-visible output, contradicting MSG-GR-3.
- **Fix:** Flipped each `["user", "project"]` to `["project", "user"]` and added a citing comment ("Iteration order is project-first per MSG-GR-3 / compareByNameThenScope") at each site. For the two `MarketplaceNotFoundError` sites the user-visible error message now renders "project, user" instead of "user, project".
- **Files modified:** `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts`, `.../marketplace/shared.ts` (2 sites), `.../marketplace/update.ts`, `.../plugin/reinstall.ts`, `.../plugin/shared.ts`, `.../plugin/update.ts`.
- **Verification:** `npm run check` exits 0 (1249/1249 tests pass, all four checks green). `grep -rn '\["user", "project"\]' extensions/pi-claude-marketplace/orchestrators/` returns no matches outside the lint-rule fixtures.
- **Committed in:** `09be954` (Task 2 commit, bundled).

**2. [Rule 3 - Blocking] Pre-commit `fix-unicode-dashes` hook normalized two pre-existing unicode em-dashes in PROJECT.md**

- **Found during:** Task 2 pre-commit run.
- **Issue:** The pre-commit `fix-unicode-dashes` hook normalized two pre-existing unicode em-dashes (`--`, U+2014) to ASCII `--` in lines I did not touch (the v1.3 Drift Guard ✓ entry at the end of the line and the CR-01 paragraph under Active). Per CLAUDE.md guidance ("Run `pre-commit run --files <changed files>` **before** attempting `git commit`. Fix any failures, restage, and re-run until clean."), this is the canonical hook-driven cleanup workflow.
- **Fix:** Re-staged the hook-normalized file and re-ran pre-commit; all hooks clean except the known TruffleHog worktree-sandbox failure.
- **Files modified:** `.planning/PROJECT.md` (2 character substitutions, no semantic change).
- **Verification:** Second pre-commit run shows `Fix Unicode dash characters` as Passed.
- **Committed in:** `09be954` (Task 2 commit, bundled).

---

**Total deviations:** 2 auto-fixed (1 bug/blocking -- residual drift cleanup; 1 blocking -- incidental pre-commit normalization).
**Impact on plan:** Both deviations were necessary to satisfy the plan's acceptance criteria. Deviation 1 was the active rule doing its job -- the freshly-tightened detector found drift sites the audit had not enumerated, and per Rule 1/Rule 3 the fix is mechanical and identical to the canonical 14.2-01 pattern. No scope creep -- the additional sites all share the exact CR-01-class drift signature and were exactly what the rule was designed to prevent. Deviation 2 is a hook-driven incidental normalization on lines outside this plan's scope.

## Issues Encountered

- **TruffleHog worktree-sandbox limitation:** Both commits prefixed with `SKIP=trufflehog` per project CLAUDE.md. TruffleHog's auto-updater fails under the worktree sandbox (`error preparing repo: failed to read index file: open .git/index: not a directory`); the underlying scan succeeds when run from the main repo path. Confirmed clean via `pre-commit run trufflehog --all-files` (from `/home/acolomba/pi-claude-marketplace`) before each commit.
- **No genuine issues during implementation.** The active rule pattern was straightforward to mirror from `tests/lint-rules/msg-sd-2-soft-dep-predicate.js` (the canonical FULL-IMPL analog). The RuleTester TDD cycle worked as expected: extending the test file first produced two clean RED failures (one per detection axis), the rule rewrite produced GREEN with all 5 cases passing on first run.

## User Setup Required

None -- lint-rule tightening + mechanical drift fix, no external service configuration.

## Next Phase Readiness

- **14.2-03 / 14.2-04 (retroactive Phase 12 + Phase 14.1 gates) unblocked.** Wave 2 follows Wave 1 per D-10; gates run on a post-CR-01-clean codebase with the active MSG-GR-3 rule preventing regression. The retroactive `/gsd:secure-phase` and `/gsd:validate-phase` invocations should see no new threats or coverage gaps from this plan's surface.
- **Audit CR-01 is fully closed on the lint side.** The active rule guarantees that the 3 `scopeOrder` helpers + the 8 iteration-literal sites (1 from CR-01 + 7 surfaced by Task 2) cannot return without failing `npm run lint`. Future orchestrator additions that re-introduce either pattern will fail at lint time with a clear MSG-GR-3 violation citing the canonical `compareByNameThenScope`.
- **No blockers.** `npm run check` exits 0 across 1249 tests with the active rule live.

## Self-Check: PASSED

All claims verified:

1. **Created file exists:** `[ -f .planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-02-SUMMARY.md ]` (this file).
2. **Commits exist on `worktree-agent-a9c7e41676b448944`:**
   - `35cfdf3` (Task 1, `test(14.2-02): activate MSG-GR-3 two-axis AST detector`)
   - `09be954` (Task 2, `fix(14.2-02): wire active MSG-GR-3 rule and clean residual user-first drift`)
3. **Task 1 acceptance criteria verified:**
   - `grep -c "userFirstScopeOrder" tests/lint-rules/msg-gr-3-per-scope.js` is 3 (>=2 required).
   - `grep -c "userFirstScopeIteration" tests/lint-rules/msg-gr-3-per-scope.js` is 3 (>=2 required).
   - `grep -c "Program: () => {}" tests/lint-rules/msg-gr-3-per-scope.js` is 0.
   - `grep -c "FunctionDeclaration" tests/lint-rules/msg-gr-3-per-scope.js` is 2; `grep -c "ArrayExpression"` is 2.
   - `node --test tests/lint-rules/msg-gr-3-per-scope.test.js` exits 0 (5 tests pass: 3 valid + 2 invalid).
4. **Task 2 acceptance criteria verified:**
   - `awk '/files: \["extensions\/pi-claude-marketplace\/orchestrators\/\*\*\/\*\.ts"\]/,/^\s*\}/' eslint.config.js | grep -c 'msg/msg-gr-3-per-scope'` is 1.
   - `awk '/files: \["extensions\/pi-claude-marketplace\/\*\*\/\*\.ts"\]/,/^\s*\}/' eslint.config.js | grep -c 'msg/msg-gr-3-per-scope'` is 0.
   - `grep -c "D-14-2-08" .planning/PROJECT.md` is 1.
   - `awk 'NR>=110 && NR<=115' docs/messaging-style-guide.md | grep -ci "structural meta-assertion\|no-op lint rule"` is 0.
   - `npm run check` exits 0 (1249/1249 tests pass).
   - `node --test tests/architecture/msg-rule-registry.test.ts` exits 0 (4-way parity holds).

---

*Phase: 14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates*
*Completed: 2026-05-24*
