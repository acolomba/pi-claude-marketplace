---
phase: 14.2
plan: 03
type: execute
wave: 2
depends_on:
  - 14.2-01
  - 14.2-02
files_modified:
  - .planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md
  - .planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "`.planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md` exists and was produced by `/gsd:secure-phase 12`; its frontmatter indicates the secure-phase gate completed (e.g., `status: passed` / `threats_open: 0` / equivalent skill-defined fields)."
    - "`.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md` frontmatter has been advanced from `status: draft` to `status: validated` and `nyquist_compliant: false` to `nyquist_compliant: true` (or documents accepted gaps with a rationale block per the validator's contract)."
    - "Both gate runs were performed against the post-14.2-01 codebase (i.e., after the CR-01 fix landed) so the existing `catalog-uat.test.ts` cross-scope fixture is visible to the validator's nyquist sampling."
    - "The plan's SUMMARY.md captures (i) the two artefact paths produced, (ii) any new threats/gaps surfaced by the gate runs, (iii) the final status markers (`passed` / `threats_open=0` / `nyquist=true` or accepted-gaps rationale)."
    - "Implements CONTEXT.md decisions D-09 (gates as standard 14.2 plans with executor atomic-commit discipline), D-10 (Wave 2 follows Wave 1 so gates audit the post-CR-01 codebase), D-11 (treat Phase 12 draft VALIDATION.md as a checkpoint and re-run validate-phase to completion rather than discard or promote), and D-12 (standard secure-phase 12 run with no extra threat-model expectations beyond the existing PLAN.md threat models)."
  artifacts:
    - path: ".planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md"
      provides: "Phase 12 retroactive security gate artefact produced by `/gsd:secure-phase 12`."
    - path: ".planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md"
      provides: "Phase 12 retroactive nyquist validation completed from the existing draft (status: draft → status: validated; nyquist_compliant: false → nyquist_compliant: true OR accepted-gaps documented)."
  key_links:
    - from: ".planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md"
      to: ".planning/phases/12-messaging-foundations-renderer-primitives/*-PLAN.md"
      via: "secure-phase audits each plan's `<threat_model>` block"
      pattern: "threat_model"
    - from: ".planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md"
      to: "tests/architecture/catalog-uat.test.ts"
      via: "validator nyquist sampling reads test files referenced in PLAN.md `<verification>` blocks"
      pattern: "catalog-uat"
---

<objective>
Run the two retroactive GSD gates against Phase 12 (`/gsd:secure-phase 12` to produce the missing `12-SECURITY.md`, then `/gsd:validate-phase 12` to advance the existing draft `12-VALIDATION.md` to validated). Phase 12 is functionally complete (`12-VERIFICATION.md` status=passed) but never had the secure-phase gate run, and its validation artefact is still draft / `nyquist_compliant: false`.

Purpose: `/gsd:complete-milestone v1.3` enforces that every phase has SECURITY.md (status=passed, threats_open=0) and VALIDATION.md (status=validated, nyquist=true or accepted-gaps documented) before the milestone can archive. Per D-09, the gates are run as standard 14.2 plans (not post-execution manual steps) so the gsd-executor's atomic-commit + checkpoint discipline applies and the wave parallelization plays cleanly with `14.2-04` running the same gates against Phase 14.1.

Per D-10, gates run AFTER the CR-01 fix (Wave 2 follows Wave 1) so the validator's nyquist sampling sees the new `catalog-uat.test.ts` cross-scope fixture as covered, avoiding a re-run. Per D-11, the existing Phase 12 draft is treated as a checkpoint: the validator can either generate missing test coverage or document gaps as accepted with rationale, then flip status=validated. Per D-12, no special threat-model expectations -- the secure-phase agent surfaces whatever the existing Phase 12 PLAN.md threat models contain.

Output: Two artefact files in `.planning/phases/12-messaging-foundations-renderer-primitives/`; the plan's SUMMARY.md aggregates artefact paths + status markers + any surfaced gaps. This is `autonomous: false` because both slash commands may require user confirmation during their runs (e.g., the validator may pause on accepted-gap documentation).
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `/gsd:secure-phase 12` to produce `12-SECURITY.md`</name>
  <files>.planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md</files>
  <read_first>
    - `.planning/phases/12-messaging-foundations-renderer-primitives/` directory listing (verify the four PLAN.md files exist with `<threat_model>` blocks the secure-phase agent can read).
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md` decisions D-09 (gates as standard plans), D-10 (gates run on post-CR-01 codebase), D-12 (no special threat-model expectations).
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` "Retroactive gates (D-09..D-12)" section for the slash-command invocation pattern (the artefact's full shape is the slash command's contract; this plan only asserts existence + expected frontmatter markers).
  </read_first>
  <action>
    Invoke `/gsd:secure-phase 12` (Skill invocation per CONTEXT.md "Claude's Discretion" item 3). The skill reads the Phase 12 PLAN.md `<threat_model>` blocks and produces `.planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md`. Confirm the artefact file exists on disk; read its frontmatter and capture the status markers (e.g., `status:`, `threats_open:`, or whatever fields the secure-phase skill emits) for the SUMMARY.md. If the skill surfaces any new threats or gaps, capture them verbatim in the SUMMARY.md rather than amending PLAN.md threat models in-flight.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; test -f .planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md &amp;&amp; head -15 .planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md` exists.
    - The file's YAML frontmatter contains a status marker indicating the gate completed (e.g., `status: passed` or the skill's documented success token).
    - The file's frontmatter indicates zero open threats (e.g., `threats_open: 0` or the skill's equivalent), OR the SUMMARY.md captures any open-threats list with their dispositions.
  </acceptance_criteria>
  <done>`12-SECURITY.md` exists with frontmatter indicating the gate completed; any open threats are recorded in the plan SUMMARY.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run `/gsd:validate-phase 12` to advance the draft `12-VALIDATION.md` to validated</name>
  <files>.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md` lines 1-30 (current frontmatter: `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`) so the SUMMARY.md can record the before-state.
    - `.planning/phases/12-messaging-foundations-renderer-primitives/12-VERIFICATION.md` (status=passed; the verification gate already ran successfully, so the validator works from a green baseline).
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md` decision D-11 explaining that the existing draft is treated as a checkpoint -- the validator either generates missing test coverage or documents accepted gaps with rationale, then flips `status: validated`.
  </read_first>
  <action>
    Invoke `/gsd:validate-phase 12` (Skill invocation). The skill reads the existing draft `12-VALIDATION.md`, the four PLAN.md files, and `12-VERIFICATION.md`; performs nyquist sampling against tests referenced in the PLAN.md `<verification>` blocks (which now includes the post-14.2-01 cross-scope fixture in `catalog-uat.test.ts`); and rewrites the validation artefact in place. The expected end-state is `status: validated` and `nyquist_compliant: true`; if the validator finds genuine coverage gaps it cannot close, it documents them as accepted gaps with rationale and still flips `status: validated`. Capture the before/after of the three frontmatter markers (`status`, `nyquist_compliant`, `wave_0_complete`) and any accepted-gaps documentation in SUMMARY.md.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; grep -E "^status:|^nyquist_compliant:|^wave_0_complete:" .planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md` frontmatter `status:` field equals `validated` (not `draft`).
    - `.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md` frontmatter `nyquist_compliant:` field equals `true`, OR the file body contains an "Accepted Gaps" / equivalent rationale section documenting why nyquist remains false.
    - The plan SUMMARY.md captures the before-state, after-state, and any accepted-gaps rationale.
  </acceptance_criteria>
  <done>`12-VALIDATION.md` status is `validated`; either nyquist is true or accepted gaps are documented with rationale; SUMMARY.md captures the trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Read-only audit -- the secure-phase and validate-phase skills produce planning artefacts only; no source code changes, no test changes, no production behavior changes. The audit driver references the existing Phase 12 PLAN.md `<threat_model>` blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-03-01 | n/a | n/a | accept | No new threats introduced by running the retroactive gates; the gates ASSERT existing threat-model and validation discipline against Phase 12. If the secure-phase skill surfaces previously-undocumented threats, those are recorded in `12-SECURITY.md` and aggregated in this plan's SUMMARY.md per D-12. |
</threat_model>

<verification>
- `test -f .planning/phases/12-messaging-foundations-renderer-primitives/12-SECURITY.md` exits 0.
- `.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md` frontmatter `status:` field equals `validated`.
- The plan's SUMMARY.md lists both artefact paths and captures the gate run status markers.
</verification>

<success_criteria>
1. `12-SECURITY.md` exists; the secure-phase gate ran cleanly against the existing Phase 12 PLAN.md `<threat_model>` blocks.
2. `12-VALIDATION.md` advanced from `status: draft` to `status: validated`; nyquist is true OR accepted gaps are documented.
3. The plan SUMMARY.md aggregates: (i) artefact paths, (ii) any new threats/gaps the gates surfaced, (iii) final status markers. This is the verification trail `/gsd:complete-milestone v1.3` will read.
4. No source code or test changes in this plan (read-only audit).
</success_criteria>

<output>
Create `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-03-SUMMARY.md` when done. The SUMMARY.md MUST include:
- Artefact paths: `12-SECURITY.md`, `12-VALIDATION.md`.
- Before/after frontmatter markers for `12-VALIDATION.md` (`status`, `nyquist_compliant`, `wave_0_complete`).
- Any threats/gaps surfaced by the gate runs, with their dispositions.
- Final status: `12-SECURITY.md` status marker, `12-VALIDATION.md` final `status` + `nyquist_compliant` values.
</output>
