---
phase: 14.2
slug: address-tech-debt-cr-01-retroactive-phase-12-14-1-gates
status: passed
threats_open: 0
threats_closed: 6
asvs_level: 1
register_authored_at_plan_time: true
created: 2026-05-25
---

# Phase 14.2 -- Security

> Per-phase security contract: threat register, accepted risks, and audit
> trail. Phase 14.2 covered: cross-scope ordering refactor (01),
> MSG-GR-3 ESLint rule promotion (02), retroactive Phase 12 gates (03),
> retroactive Phase 14.1 gates (04), and ROADMAP Phase-13 row corrections
> (05).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| none (01) | Refactor only -- no new inputs, no new IO, no new network/disk surface; the canonical `compareByNameThenScope` comparator already exists and is used by other surfaces. | n/a |
| lint-config (02) | The MSG-GR-3 rule's per-rule `files:` block scopes AST detection to orchestrator + edge/handler files; false-positive risk against the canonical comparator in `presentation/sort.ts` is mitigated structurally. False-negative risk for cross-scope drift outside that glob is accepted (intentional scope per D-14-2-08). | ESLint AST nodes (build-time only; no runtime data crosses) |
| none (03) | Read-only audit -- the secure-phase and validate-phase skills produce planning artefacts only; no source code, test, or production behavior changes. | n/a |
| none (04) | Read-only audit -- same shape as 03. | n/a |
| none (05) | Documentation edit only -- no source code, test, or behavior changes. ROADMAP.md brought into parity with REQUIREMENTS.md (already correct). | n/a |

---

## Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Status |
|-----------|----------|-----------|-------------|------------|--------|
| T-14.2-01-01 | Tampering | n/a (refactor) | accept | None required -- refactor consolidates four sort/iteration sites onto the existing canonical comparator; no new inputs cross a new boundary. | closed |
| T-14.2-02-01 | Tampering | tests/lint-rules/msg-gr-3-per-scope.js | mitigate | Per-rule `files:` block in `eslint.config.js` (lines 180-187) scopes MSG-GR-3 detection to `extensions/pi-claude-marketplace/orchestrators/**/*.ts` and `extensions/pi-claude-marketplace/edge/handlers/**/*.ts`. The canonical `compareByNameThenScope` in `presentation/sort.ts` is outside that scope and cannot trip the rule. | closed |
| T-14.2-02-02 | Information disclosure | tests/lint-rules/msg-gr-3-per-scope.js | accept | Accepted false-negative: cross-scope drift in non-orchestrator/non-edge-handler code does not fail lint. Documented in PROJECT.md D-14-2-08 as intentional scope; future expansion would extend the `files:` glob. | closed |
| T-14.2-03-01 | n/a (gate-driver) | n/a | accept | No new threats introduced by running retroactive gates against Phase 12; the gates ASSERT existing threat-model and validation discipline. Surfaced gaps would be recorded in `12-SECURITY.md` and aggregated in 14.2-03 SUMMARY.md per D-12. None surfaced. | closed |
| T-14.2-04-01 | n/a (gate-driver) | n/a | accept | No new threats introduced. The gates ASSERT existing threat-model and validation discipline against Phase 14.1. Surfaced gaps would be recorded in `14.1-SECURITY.md` / `14.1-VALIDATION.md` and aggregated in 14.2-04 SUMMARY.md per D-12. None surfaced. | closed |
| T-14.2-05-01 | n/a (doc-edit) | n/a | accept | No threats introduced by a doc edit that asserts existing system state (ROADMAP.md Phase-13 rows brought to parity with REQUIREMENTS.md). The investigation note in 14.2-05 SUMMARY.md documents the underlying GSD-plugin gap (auto-flip on plan completion) without creating any new dependency or surface in this project. | closed |

*Status: open · closed*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-14.2-01 | T-14.2-01-01 | Refactor to canonical comparator -- no new attack surface. The four sites previously held local `scopeOrder` helpers or `["user", "project"]` literals; all four now route through `compareByNameThenScope`, which is already exercised by Phase 12 / Phase 14.1 catalog presentation tests. | secure-phase orchestrator (plan-time disposition) | 2026-05-25 |
| AR-14.2-02 | T-14.2-02-02 | Intentionally scoped lint detection. Cross-scope drift in non-orchestrator/non-edge-handler code is accepted as a future-expansion vector; the canonical comparator in `presentation/sort.ts` is the single source of truth and cannot be silently bypassed without also touching the orchestrator or edge/handler surfaces (which the rule guards). D-14-2-08 records the supersession over D-14-09. | secure-phase orchestrator (plan-time disposition) | 2026-05-25 |
| AR-14.2-03 | T-14.2-03-01 | Read-only retroactive audit driver -- no new threats can be introduced by running gates that produce planning artefacts only. The 14.2-03 SUMMARY.md confirms `## Threat Flags: None` and `Threats: 0 new (all 14 closed against plan-time register)`. | secure-phase orchestrator (plan-time disposition) | 2026-05-25 |
| AR-14.2-04 | T-14.2-04-01 | Same shape as AR-14.2-03 against Phase 14.1. The 14.2-04 SUMMARY.md confirms `0 unregistered flags surfaced` and `Threats: 0 new (all 14 closed against plan-time register)`. | secure-phase orchestrator (plan-time disposition) | 2026-05-25 |
| AR-14.2-05 | T-14.2-05-01 | Doc-only edit asserting existing system state -- no new dependency or surface. The investigation note about the GSD-plugin auto-flip gap is captured but produces no code change in this project. | secure-phase orchestrator (plan-time disposition) | 2026-05-25 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-05-25 | 6 | 6 | 0 | secure-phase orchestrator (short-circuit; register_authored_at_plan_time: true, threats_open: 0) |

### 2026-05-25 audit notes

- **Input state:** B (no prior SECURITY.md; 5 PLAN.md files with parseable `<threat_model>` blocks).
- **Short-circuit applied:** All 6 threats had plan-time dispositions (5 accept, 1 mitigate). T-14.2-02-01 (mitigate) verified directly against `eslint.config.js` (per-rule `files:` block at lines 180-187 scopes MSG-GR-3 to orchestrator + edge/handler globs, post-14.2-fix CR-01 widening). No new threats surfaced in any of the 5 plan SUMMARY.md files (`## Threat Flags: None` where present; section omitted where the plan was purely refactor/doc).
- **Mitigation evidence (T-14.2-02-01):** `eslint.config.js` MSG-Block 1b registers the rule under the per-rule glob; the canonical `compareByNameThenScope` in `extensions/pi-claude-marketplace/orchestrators/presentation/sort.ts` is outside the glob and cannot trip the rule (structural false-positive defense).
- **Cross-references:** Phase 14.2 plan-level SUMMARYs aggregate downstream secure-phase artefacts -- `12-SECURITY.md` (threats_open: 0, threats_closed: 14) and `14.1-SECURITY.md` (threats_open: 0, threats_closed: 14) -- which are the artefacts plans 14.2-03 and 14.2-04 produced via their own retroactive gates. This 14.2-SECURITY.md does not re-audit those downstream registers; it audits only the 14.2-internal threats raised by the 14.2 plans themselves.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: passed` set in frontmatter

**Approval:** verified 2026-05-25
