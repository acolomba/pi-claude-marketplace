---
phase: 14.2
plan: 05
type: execute
wave: 3
depends_on:
  - 14.2-03
  - 14.2-04
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 29 Phase-13 CMC rows in `.planning/ROADMAP.md ## Coverage (v1.3)` table read `| Phase 13 | Complete |` (none read `| Phase 13 | Pending |`)."
    - "The 29 flips match the authoritative source `.planning/REQUIREMENTS.md ## Coverage` table -- every CMC ID marked Complete in REQUIREMENTS.md is marked Complete in ROADMAP.md after the edit."
    - "The Phase Details row for Phase 14.1 in `.planning/ROADMAP.md` reads `| 14.1 | … | 2/2 plans | Complete |` instead of `0/2 plans | Planned`."
    - "The plan SUMMARY.md contains an investigation note documenting that `/gsd:execute-phase` does NOT auto-flip ROADMAP.md Coverage checkboxes on plan completion, treating the structural fix as a GSD-plugin ticket (separate project) per D-14 -- the 14.2-05 work is remediation only, NOT a structural fix."
    - "Edits are scoped to the `## Coverage (v1.3)` table and the `## Phase Details` table row for Phase 14.1; no other ROADMAP.md sections are touched (especially the Roadmap Evolution log, milestone metadata, the Phase 14.2 entry itself, or any Phase-12/Phase-14 rows beyond the 14.1 Phase Details row)."
    - "Implements CONTEXT.md decision D-13 (flip 29 Phase-13 CMC checkboxes in ROADMAP.md Coverage table cross-checked against REQUIREMENTS.md as authoritative source, plus update Phase Details row for 14.1 from 0/2 plans Planned to 2/2 plans Complete). D-14 (record-the-gap, do-not-fix-the-gap framing for the GSD-plugin auto-flip enhancement) is implemented by the SUMMARY.md investigation note."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "v1.3 Coverage table reflects REQUIREMENTS.md authoritative source; Phase Details row for 14.1 reflects completion."
      contains: "| Phase 13 | Complete |"
  key_links:
    - from: ".planning/ROADMAP.md ## Coverage (v1.3)"
      to: ".planning/REQUIREMENTS.md ## Coverage"
      via: "row-for-row cross-check (REQUIREMENTS is authoritative)"
      pattern: "CMC-[0-9]+.*Phase 13.*Complete"
---

<objective>
Close the cosmetic ROADMAP.md drift surfaced by the v1.3 milestone audit: 29 Phase-13 CMC rows in the `## Coverage (v1.3)` table still read `Pending` despite REQUIREMENTS.md (the authoritative source) marking them Complete; and the Phase Details row for Phase 14.1 still reads `0/2 plans | Planned` despite both 14.1 plans landing and `14.1-VERIFICATION.md` status=passed. Per D-14, the structural fix (teaching `/gsd:execute-phase` to auto-flip these checkboxes on plan completion) belongs in the GSD plugin (separate project) and is NOT in scope for this repo; the 14.2-05 work is remediation only, with an investigation note in the SUMMARY.md capturing the gap.

Purpose: `/gsd:complete-milestone v1.3` will read ROADMAP.md as part of milestone archival. Drift between ROADMAP.md Coverage and REQUIREMENTS.md Coverage produces audit noise. Wave 3 follows Wave 2 (`14.2-03` + `14.2-04`) so the ROADMAP edit lands after the retroactive gates have produced their artefacts -- this lets the SUMMARY.md confirm milestone-archive readiness across both surfaces in one pass.

Output: One file edited (`.planning/ROADMAP.md`) -- 29 Coverage table rows flipped and one Phase Details row updated. One SUMMARY.md with the investigation note framing.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.3-MILESTONE-AUDIT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md
@.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the 29 Phase-13 CMC rows in `.planning/ROADMAP.md ## Coverage (v1.3)` from Pending to Complete, cross-checked against REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md ## Coverage` table at lines ~695-770 -- read every CMC-NN row and record the Phase + Status. This is the authoritative source per D-13.
    - `.planning/ROADMAP.md ## Coverage (v1.3)` table at lines ~262-302 -- read every CMC-NN row and identify which read `Pending` for `Phase 13`. There are exactly 29 such rows per the CONTEXT directive count.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section ".planning/ROADMAP.md -- Coverage table flips + Phase Details row (D-13, D-14)" for the cross-reference instruction.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-CONTEXT.md` D-13 (29 Phase-13 flips, REQUIREMENTS.md cross-check) and the `<deferred>` block noting the rejected project-local parity test option.
  </read_first>
  <action>
    Build the list of 29 Phase-13 CMC IDs whose ROADMAP.md row reads `| Phase 13 | Pending |` (greppable as `grep -n "| Phase 13 | Pending |" .planning/ROADMAP.md`). For each such row, verify REQUIREMENTS.md `## Coverage` lists the same CMC ID as `Complete` -- if any one row disagrees with REQUIREMENTS.md (i.e., REQUIREMENTS shows the row still Pending), DO NOT flip it; instead, record the disagreement in SUMMARY.md and leave the row untouched. For every row where REQUIREMENTS.md says Complete, change the ROADMAP.md row text from `| Phase 13 | Pending |` to `| Phase 13 | Complete |` (table-cell width-preserving substitution). Do NOT touch the Phase-12 CMC rows (CMC-08, CMC-11, CMC-14, CMC-19, CMC-36, CMC-37) even though REQUIREMENTS.md marks them Complete -- D-13 explicitly scopes this plan to the 29 Phase-13 entries; record the additional Phase-12 drift as an observation in SUMMARY.md (no fix in this plan). Do NOT touch the Coverage summary block beneath the table (the "v1.3 requirements: 38 total / Mapped to phases: 38 / Unmapped: 0" lines remain accurate).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; test "$(grep -c '| Phase 13 | Pending |' .planning/ROADMAP.md)" = 0 &amp;&amp; test "$(grep -c '| Phase 13 | Complete |' .planning/ROADMAP.md)" = 29</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| Phase 13 | Pending |" .planning/ROADMAP.md` equals 0.
    - `grep -c "| Phase 13 | Complete |" .planning/ROADMAP.md` equals 29.
    - Every Phase-13 CMC ID listed as `Complete` in REQUIREMENTS.md is `Complete` in ROADMAP.md after the edit (manual cross-reference step recorded in SUMMARY.md).
    - The Coverage summary block beneath the Coverage table is unchanged (the "38 total / Mapped to phases: 38" lines are byte-identical).
    - The Phase-12 CMC rows (CMC-08/11/14/19/36/37) are NOT modified by this task -- only the 29 Phase-13 rows. The Phase-12 drift is recorded in SUMMARY.md as an observation, not a fix.
  </acceptance_criteria>
  <done>The 29 Phase-13 Coverage table rows are flipped from Pending to Complete; the cross-check against REQUIREMENTS.md is recorded in SUMMARY.md; the additional Phase-12 drift is documented as an out-of-scope observation.</done>
</task>

<task type="auto">
  <name>Task 2: Update the Phase Details row for Phase 14.1 in `.planning/ROADMAP.md`</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md ## Phase Details` table at lines ~250-260 -- find the row for `14.1. Close gap: CMC-13 import propagation` (or similarly-titled). The current state reads `0/2 plans | Planned`.
    - `.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/` directory listing to confirm both `14.1-01-PLAN.md` and `14.1-02-PLAN.md` exist and `14.1-VERIFICATION.md` is present.
    - `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-PATTERNS.md` section "tabular self-update" for the Phase Details row update target shape.
  </read_first>
  <action>
    Locate the Phase Details table row for Phase 14.1. Change the `Plans` cell from `0/2 plans` to `2/2 plans`. Change the `Status` cell from `Planned` to `Complete`. If the `Completed` cell is empty, fill it with the date that `14.1-VERIFICATION.md` was finalized (read its frontmatter or the most recent commit touching it; record the chosen date in SUMMARY.md). Do NOT modify any other row in the Phase Details table -- especially the Phase 14.2 row, which represents this current phase and remains Planned. Preserve table-column alignment.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; grep -E "14\.1.*2/2 plans.*Complete" .planning/ROADMAP.md &amp;&amp; test "$(grep -cE '14\.1.*2/2 plans.*Complete' .planning/ROADMAP.md)" = 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "14\.1.*2/2 plans.*Complete" .planning/ROADMAP.md` equals 1.
    - The Phase 14.2 row in the same table is unchanged (still represents an in-progress phase).
    - No other row in the Phase Details table is modified.
  </acceptance_criteria>
  <done>The Phase Details table's Phase 14.1 row reflects completion; no other rows are touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Documentation edit only -- no source code, test, or behavior changes. The change brings ROADMAP.md into parity with REQUIREMENTS.md (already correct) and Phase 14.1's verification artefact (already passed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-05-01 | n/a | n/a | accept | No threats introduced by a doc edit that asserts the existing system state. The investigation note in SUMMARY.md documents the underlying GSD-plugin gap (auto-flip on plan completion) without creating any new dependency or surface in this project. |
</threat_model>

<verification>
- `grep -c "| Phase 13 | Pending |" .planning/ROADMAP.md` equals 0.
- `grep -c "| Phase 13 | Complete |" .planning/ROADMAP.md` equals 29.
- `grep -cE "14\.1.*2/2 plans.*Complete" .planning/ROADMAP.md` equals 1.
- The plan SUMMARY.md contains the investigation note specified below.
</verification>

<success_criteria>
1. The 29 Phase-13 CMC rows in `## Coverage (v1.3)` read `Complete` instead of `Pending`, matching REQUIREMENTS.md.
2. The Phase 14.1 row in `## Phase Details` reads `2/2 plans | Complete`.
3. The SUMMARY.md investigation note records: (a) `/gsd:execute-phase` does NOT auto-flip ROADMAP.md Coverage checkboxes on plan completion (evidenced by this drift); (b) the 14.2-05 work is remediation only -- the structural fix (teaching the executor) is a GSD-plugin ticket, NOT in scope for `pi-claude-marketplace`; (c) no project-local architecture test for ROADMAP↔REQUIREMENTS parity is added (rejected option per D-14); (d) the Phase-12 CMC rows still showing Pending in ROADMAP.md are recorded as an additional cosmetic drift surface (D-13 explicitly scoped the flip to 29 Phase-13 entries; the 6 Phase-12 entries are noted but not edited in this plan).
4. No other ROADMAP.md sections are touched (Roadmap Evolution log, milestone metadata, the Phase 14.2 entry itself, the Coverage summary block, the Per-phase distribution block, the Phase Sequencing Rationale section -- all unchanged).
5. With Wave 1 (14.2-01, 14.2-02) green and Wave 2 (14.2-03, 14.2-04) gates passed, this plan closes the cosmetic-drift surface; `/gsd:complete-milestone v1.3` can now archive with zero outstanding gates.
</success_criteria>

<output>
Create `.planning/phases/14.2-address-tech-debt-cr-01-retroactive-phase-12-14-1-gates/14.2-05-SUMMARY.md` when done. The SUMMARY.md MUST include the following investigation note section verbatim (paraphrasing allowed for surrounding prose but the four numbered points must appear):

```
## Investigation Note: ROADMAP.md Coverage auto-flip gap

This 14.2-05 work is remediation, not a structural fix. The 29 Phase-13 + 1 Phase-14.1 ROADMAP.md staleness surface exists because:

1. `/gsd:execute-phase` does NOT currently auto-flip ROADMAP.md `## Coverage (v1.3)` checkboxes when a plan completes. The CMC-NN row remains `Pending` after the plan that addresses it lands, even though `REQUIREMENTS.md ## Coverage` (the authoritative source) was updated by the workflow.

2. The 14.2-05 plan flips the 29 Phase-13 rows by hand to match REQUIREMENTS.md. This closes the immediate drift but does NOT prevent recurrence.

3. The structural fix (teaching `/gsd:execute-phase` to auto-flip both Coverage tables on plan completion) belongs in the GSD plugin (separate project from `pi-claude-marketplace`). It is explicitly out of scope per D-14.

4. No project-local architecture test for ROADMAP↔REQUIREMENTS parity is added; this option was rejected in CONTEXT.md `<deferred>` because this project should not couple to GSD doc structure.

Additional observation (not fixed in this plan): the 6 Phase-12 CMC rows (CMC-08/11/14/19/36/37) in `## Coverage (v1.3)` also still read `Pending` despite REQUIREMENTS.md marking them `Complete`. D-13 explicitly scoped this plan to the 29 Phase-13 entries; the Phase-12 drift is noted here for traceability but not fixed.
```

The SUMMARY.md MUST also list:
- Total rows modified in this plan (29 in Coverage table + 1 in Phase Details table = 30 row edits).
- Confirmation of the cross-reference against REQUIREMENTS.md (CMC IDs flipped: enumerate them).
- Date used to fill the Phase 14.1 Completed cell (and how it was sourced).
- Files modified: only `.planning/ROADMAP.md`.
</output>
