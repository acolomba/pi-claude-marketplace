---
phase: 109-shared-contracts
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - "extensions/pi-claude-marketplace/shared/path-safety.ts"
  - "tests/shared/path-safety.test.ts"
autonomous: true
requirements:
  - MOD-02
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P109-15 directly imports and proves PathContainmentError; SymlinkRefusedError; assertPathInside without changing the current public surface (D-01)."
    - "Cover parent equal to child, direct child, one-step parent escape, deeper outside path, missing intermediate segment, symlink at each walked position, unreadable link target, and unexpected stat failure. Preserve component walk order when segments touch; numeric precision is not applicable."
    - "Every runtime case created or changed uses separate lowercase phase markers and case-owned mutable state; type-only evidence follows D-03 (D-02, D-03, D-04)."
    - "The owner alone reaches 100 percent direct function, line, and branch coverage for extensions/pi-claude-marketplace/shared/path-safety.ts."
  artifacts:
    - path: "tests/shared/path-safety.test.ts"
      provides: "Canonical mirrored owner for P109-15"
    - path: "extensions/pi-claude-marketplace/shared/path-safety.ts"
      provides: "Existing public contract under direct owner coverage; no new public symbols"
  key_links:
    - from: "tests/shared/path-safety.test.ts"
      to: "extensions/pi-claude-marketplace/shared/path-safety.ts"
      via: "direct relative import of the paired source"
      pattern: "from .*path-safety\\.ts"
    - from: "tests/shared/path-safety.test.ts"
      to: "scripts/test-coverage-direct.mjs"
      via: "focused source-to-owner mapping"
      pattern: "test:coverage:direct"
---

<objective>
Complete P109-15 as one pair-atomic owner plan per D-01 through D-04: Lock path containment, symlink refusal, and exceptional filesystem behavior.

Purpose: Preserve the current caller-visible contract while replacing brownfield or missing evidence with exact public-value and side-effect proof.
Output: tests/shared/path-safety.test.ts and, only if full public-surface coverage is impossible without it, the smallest behavior-preserving internal refactor in extensions/pi-claude-marketplace/shared/path-safety.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shared-contracts/109-CONTEXT.md
@.planning/phases/109-shared-contracts/109-RESEARCH.md
@.planning/phases/109-shared-contracts/109-PATTERNS.md
@.planning/phases/109-shared-contracts/109-VALIDATION.md
@extensions/pi-claude-marketplace/shared/path-safety.ts

</context>

<interfaces>
From `extensions/pi-claude-marketplace/shared/path-safety.ts`:
- Public exports in scope: PathContainmentError; SymlinkRefusedError; assertPathInside
- Public behavior to preserve: Normalize the owner and first attempt full direct coverage through the current public surface with no production edit. Use real temporary paths for normal containment/symlink behavior and test-local current-context filesystem control where the existing import shape permits it. Only if deterministic coverage of unexpected `lstat` propagation and unreadable-link fallback is impossible may this pair replace the source's named promise imports with the default `node:fs/promises` object so private calls use `fs.lstat` and `fs.readlink`. That fallback must be the smallest behavior-preserving internal dependency-access refactor and add no public export, test mode, reset hook, or state reader. Compare exact error class, inheritance, name, message, and public fields.
- Production callers must continue to compile and observe identical values and side effects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trace callers and establish the canonical P109-15 owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/path-safety.ts - paired production source and public exports: PathContainmentError; SymlinkRefusedError; assertPathInside
    - tests/shared/path-safety.test.ts - current owner if present; otherwise confirm it is absent before creating it
    - tests/shared/atomic-json.test.ts:17-68 - mapped PATTERNS.md analog for fresh real temporary paths, case-owned cleanup, and exact filesystem values
    - .planning/phases/109-shared-contracts/109-CONTEXT.md - D-01 through D-04 and notification decisions when applicable
    - .planning/phases/109-shared-contracts/109-RESEARCH.md - pair matrix and edge evidence for P109-15
    - .planning/phases/109-shared-contracts/109-PATTERNS.md - assigned analog and pair boundary
    - .claude/rules/typescript-unit-testing.md - executable owner-test contract
    - extensions/pi-claude-marketplace/shared/errors.ts
  </read_first>
  <files>extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts</files>
  <action>Before any edit, run CodeGraph for `extensions/pi-claude-marketplace/shared/path-safety.ts` and the listed exports, trace production callers, and record the caller-facing invariants in the execution notes and final summary. Run the current owner and focused coverage when the owner exists. Directly import the paired source from `tests/shared/path-safety.test.ts`. Apply the real-filesystem ownership pattern from `tests/shared/atomic-json.test.ts:17-68`: create a fresh temporary path boundary in every case, register cleanup before mutation, and compare complete path/error values. Normalize the owner and first attempt full direct coverage through the current public surface with no production edit. Use real temporary paths for normal containment and symlink behavior, and use test-local current-context filesystem control only where the existing import shape already permits it. Only if the focused evidence shows that deterministic coverage of unexpected `lstat` propagation or unreadable-link fallback is impossible without changing the source may this owning pair replace the named promise imports with the default `node:fs/promises` object so private calls use `fs.lstat` and `fs.readlink`. Record that impossibility evidence before the edit. The fallback must be the smallest behavior-preserving internal dependency-access refactor permitted by D-04 and the agent's discretion; it must add no public export, test mode, reset hook, state reader, or changed public signature. Compare exact error class, inheritance, name, message, and public fields. Per D-02, every runtime case added or changed uses exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; only one throwing or rejection expression may use lowercase `// act & assert`, and data rows always keep separate phases. This pair needs runtime evidence only; do not invent type-only ceremony. Per D-04, create fresh state and register cleanup before mutation. Do not add a coverage exemption, generic helper, or unrelated source edit.</action>
  <verify>
    <automated>node --test tests/shared/path-safety.test.ts</automated>
  </verify>
  <acceptance_criteria>The canonical owner directly imports `extensions/pi-claude-marketplace/shared/path-safety.ts`, its core public behavior passes alone, the caller trace is recorded, and no unrelated file changed.</acceptance_criteria>
  <done>P109-15 has a production-quality owner path through the paired source and its core contract.</done>
</task>

<task type="auto">
  <name>Task 2: Complete exact edge coverage and pair-local quality gates</name>
  <read_first>
    - tests/shared/path-safety.test.ts - Task 1 owner state and case names
    - extensions/pi-claude-marketplace/shared/path-safety.ts - branch and export inventory
    - .planning/phases/109-shared-contracts/109-VALIDATION.md - P109-15 command and secure behavior
    - .claude/rules/typescript-comments.md - durable source and test-title wording
  </read_first>
  <files>extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts</files>
  <action>Expand the owner only where a distinct public contract remains. Cover parent equal to child, direct child, one-step parent escape, deeper outside path, missing intermediate segment, symlink at each walked position, unreadable link target, and unexpected stat failure. Preserve component walk order when segments touch; numeric precision is not applicable. Compare whole public values, structured errors, arrays, side effects, or exact bytes to expectations authored independently of production constants and formatters. Keep data rows as sibling `test()` cases with no conditional branch in the loop body. Remove migration or work-session commentary from the changed owner. Keep the production source byte-identical when the no-production-edit attempt achieves complete direct coverage. If Task 1 recorded that the exceptional arms are otherwise unreachable, keep the permitted fallback refactor internal, behavior-preserving, and limited to dependency access. Confirm the public export surface is unchanged and that all callers identified in Task 1 still typecheck.</action>
  <verify>
    <automated>node --test tests/shared/path-safety.test.ts
npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/path-safety.ts
npm run typecheck
npx eslint extensions/pi-claude-marketplace/shared/path-safety.ts tests/shared/path-safety.test.ts
npx prettier --check extensions/pi-claude-marketplace/shared/path-safety.ts tests/shared/path-safety.test.ts
git diff --check</automated>
  </verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision rows are explicitly resolved; every applicable edge has an independently discriminating case; containment, every walked-position symlink refusal, exact structured errors, unreadable-link fallback, and unexpected-error propagation all pass; the owner reaches 100 percent direct functions, lines, and branches; lint, formatting, type checks when relevant, and diff checks pass. Under ASVS L1, any failed high-severity path mitigation blocks completion.</acceptance_criteria>
  <done>Lock path containment, symlink refusal, and exceptional filesystem behavior; public behavior and surface remain unchanged, and P109-15 is ready to commit as one source-owner pair.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                           | Description                                                                                                                      |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Production caller to shared export | Caller input crosses into `extensions/pi-claude-marketplace/shared/path-safety.ts`; tests preserve the accepted public contract. |
| Owner test to runtime boundary     | The test controls any filesystem, environment, cache, console, notification, or type-checking state it exercises per D-04.       |
| External API or package install    | N/A. P109-15 adds no SDK, service integration, dependency, or package-manager operation.                                         |

## STRIDE threat register

**Security policy:** ASVS L1; block completion on any unmitigated high-severity threat.

| Threat ID       | Category                           | Component                             | Severity | Disposition | Mitigation plan                                                                                                                                                   |
| --------------- | ---------------------------------- | ------------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-109-15        | Tampering / Elevation of privilege | path containment and symlink boundary | high     | mitigate    | Prove string containment before I/O, every path component's symlink refusal, exact structured errors, unreadable-link fallback, and unexpected-error propagation. |
| T-109-15-NA     | Spoofing                           | External identity boundary            | low      | accept      | N/A for this pair because it performs no authentication or external identity decision.                                                                            |
| </threat_model> |

<verification>
- Run `node --test tests/shared/path-safety.test.ts`.
- Run `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/path-safety.ts` and require 100 percent functions, lines, and branches.
- Under ASVS L1, treat any failure in containment, symlink refusal, exact error, unreadable-link fallback, or unexpected-error propagation evidence as a completion blocker.
- Run pair-local ESLint, formatting, type checks when relevant, and `git diff --check`.
- Confirm no test-only public surface, generic helper, coverage ignore, or unrelated legacy-suite edit was introduced.
</verification>

<success_criteria>

1. P109-15 satisfies MOD-02 for its one mirrored source-owner pair.
2. Its exact public values and side effects include defensible boundary, adjacency, empty, ordering, and precision outcomes.
3. D-01 through D-04 are visible in the implementation and automated evidence.
4. The owner and focused direct coverage pass without external services, developer state, or live network access.
   </success_criteria>

## Artifacts this phase produces

- `tests/shared/path-safety.test.ts`: canonical mirrored owner for `extensions/pi-claude-marketplace/shared/path-safety.ts`.
- Production symbols created: none. The source stays byte-identical when current public seams suffice; otherwise it receives only the recorded private dependency-access fallback, with exports and signatures unchanged.
- `.planning/phases/109-shared-contracts/109-15-SUMMARY.md`: execution evidence, caller trace, exact commands, and pair commit record.

<output>
Create `.planning/phases/109-shared-contracts/109-15-SUMMARY.md` when done.
</output>
