---
phase: 59-bridge-dispatch-core-debug-seam
plan: 03
type: execute
wave: 3
depends_on: [59-02]
files_modified:
  - extensions/pi-claude-marketplace/index.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - tests/architecture/hooks-dispatch.test.ts
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, DISP-04, OBS-01]
requirements_addressed: [DISP-01, DISP-02, DISP-03, DISP-04, OBS-01]
tags: [hooks, dispatch-wiring, apply-call-site, install-uninstall-cache, architecture-tests]

must_haves:
  truths:
    - "extensions/pi-claude-marketplace/index.ts converts the default-export factory to `export default async function claudeMarketplaceExtension(pi: ExtensionAPI): Promise<void>` (peer-dep types.d.ts line 1027 declares `ExtensionFactory = (pi: ExtensionAPI) => void | Promise<void>` and loader.d.ts line 15 shows `loadExtensionFromFactory` returns `Promise<Extension>`, so Pi awaits the factory's returned Promise before emitting any session lifecycle events). The factory body issues `await registerHooksBridge(pi, { projectCwdSource: \"deferred\", userCwd: homedir() });` BEFORE `registerClaudePluginCommand(pi, ...)` and `registerClaudeMarketplaceTools(pi)`, so the 7 pi.on registrations + user-scope cache hydrate are SYNCHRONOUSLY COMPLETED inside the awaited factory Promise — by the time Pi resolves the factory and proceeds to fire `session_start`, every required pi.on handler is already attached (DISP-01 + PATTERNS Pitfall 2 ordering). The `void` fire-and-forget fallback is EXPLICITLY REJECTED: it would race against the first session_start because Pi's loader (loader.d.ts) does not see the un-awaited inner Promise."
    - "Because the index.ts factory does NOT have access to an ExtensionContext or a project cwd at load time (resources_discover is the first event that delivers `event.cwd` per platform/pi-api.ts ResourcesDiscoverEvent shape), registerHooksBridge accepts a deferred-cwd shape: `opts.userCwd` defaults to `homedir()` (matching the existing index.ts pattern); `opts.projectCwd` is captured from the FIRST resources_discover event into a module-state field the router reads. The factory call registers the 7 pi.on handlers and hydrates the user-scope cache; the project-scope cache hydrates on the first resources_discover. The architecture test pins this two-step hydration contract."
    - "orchestrators/reconcile/apply.ts calls `rebuildRoutingTables(state, loc)` inside the per-scope loop (lines 790-835) AFTER `applyPlan(opts, readResult.plan, outcomes)` returns and BEFORE the `continue` falls through; the state snapshot is read via a brief read-only `withLockedStateTransaction(loc, async (tx) => rebuildRoutingTables(tx.state, loc))` (RESEARCH Open Question 1 resolution Option A — safest)."
    - "orchestrators/plugin/install.ts calls `addPluginConfigToCache(scope, marketplace, plugin, parsed)` inside the `withLockedStateTransaction(locations, async (tx) => { ... })` closure (around line 855-943) AFTER `runInstallLedger` succeeds and BEFORE `tx.save()`. The `parsed` value is the result of parseHooksConfig against the hooks.json the plugin's resources.hooks list references — if the plugin has no hooks (resources.hooks.length === 0) the cache-add is skipped."
    - "orchestrators/plugin/uninstall.ts calls `removePluginConfigFromCache(scope, marketplace, plugin)` inside its `withLockedStateTransaction(locations, async (tx) => { ... })` closure (around line 367) AFTER the state.marketplaces[mp].plugins[plugin] deletion mutation and BEFORE `tx.save()`. Always called unconditionally (idempotent — removing a missing key is a no-op)."
    - "reinstall.ts and update.ts are audited (read-only audit) and confirmed to route through installPlugin + uninstallPlugin entry points so the cache add/remove flows through the install/uninstall seams transitively. A comment in the audit summary documents the verification."
    - "tests/architecture/hooks-dispatch.test.ts pins 5 invariants per CONTEXT D-59-04: (a) registerHooksBridge calls pi.on exactly 7 times with the locked event-name set `{session_start, session_shutdown, session_before_compact, session_compact, input, tool_call, tool_result}`; (b) routingTable contains the 8 BUCKET_A_EVENTS keys after rebuild; (c) ordering is compareByNameThenScope cross-plugin + declaration index within plugin (re-uses the Plan 02 unit-test fixtures via assertion-only re-runs); (d) epoch mismatch causes dispatchHookExec to never be called; (e) tool_result composite handler reads event.isError and dispatches to the correct bucket."
    - "tests/architecture/hooks-dispatch.test.ts also pins OBS-01 invariants: (f) shared/debug-log.ts is the sole runtime debug-output module (import-graph scan: grep for `console.error` in `extensions/pi-claude-marketplace/**/*.ts` returns matches ONLY in shared/debug-log.ts; the per-file ESLint override is verified live via a parse of eslint.config.js); (g) domain/components/hooks.ts no longer defines `hookDebugLog` (the export is gone — `grep -c \"^export function hookDebugLog\\|^export const hookDebugLog\" extensions/pi-claude-marketplace/domain/components/hooks.ts` returns 0)."
    - "The architecture test uses a synthetic Pi mock (a record-and-replay double that captures pi.on call arguments) — pattern mirrors tests/architecture/hooks-foundation.test.ts but exercises bridge factory-time behavior rather than schema introspection."
    - "Apply-pass + cache flow is verified end-to-end via a synthetic-state test in tests/architecture/hooks-dispatch.test.ts: prep state.marketplaces with two installations (one with hooks, one without); call applyReconcile via a thin wrapper or by setting up the apply.ts path directly; observe rebuildRoutingTables was called once per scope; observe the routing table buckets contain only the hooks-having plugin's entries."
    - "All Phase 57/58 tests stay GREEN — this plan is additive wiring; the dispatch core ships behind the new architecture-test pin without disturbing the existing foundation tests."
  artifacts:
    - path: "extensions/pi-claude-marketplace/index.ts"
      provides: "Default export converted to `async function`; factory body extended with `await registerHooksBridge(pi, opts)` call AND `setProjectCwd(event.cwd)` (or equivalent) inside the resources_discover handler to capture project cwd for deferred hydration"
      contains: "registerHooksBridge"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts"
      provides: "Per-scope rebuildRoutingTables call site after applyPlan returns; uses brief read-only withLockedStateTransaction for state snapshot"
      contains: "rebuildRoutingTables"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "addPluginConfigToCache call inside the withLockedStateTransaction closure after runInstallLedger success"
      contains: "addPluginConfigToCache"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      provides: "removePluginConfigFromCache call inside the withLockedStateTransaction closure after the plugin record deletion"
      contains: "removePluginConfigFromCache"
    - path: "tests/architecture/hooks-dispatch.test.ts"
      provides: "Architecture-level invariant pins for DISP-01..04 + OBS-01 (7 blocks per the must_haves spec)"
      min_lines: 350
  key_links:
    - from: "extensions/pi-claude-marketplace/index.ts (extension factory)"
      to: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (registerHooksBridge)"
      via: "factory-time registration alongside existing bridge registrations"
      pattern: "registerHooksBridge\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts (per-scope loop)"
      to: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (rebuildRoutingTables)"
      via: "post-applyPlan rebuild call inside the existing for-of scopes loop"
      pattern: "rebuildRoutingTables\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (addPluginConfigToCache)"
      via: "cache-insert inside the per-plugin lock after runInstallLedger success"
      pattern: "addPluginConfigToCache\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (removePluginConfigFromCache)"
      via: "cache-remove inside the per-plugin lock after state mutation"
      pattern: "removePluginConfigFromCache\\("
    - from: "tests/architecture/hooks-dispatch.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts + shared/debug-log.ts + domain/components/hooks.ts"
      via: "static introspection + factory invocation against synthetic Pi mock + import-graph scan"
      pattern: "registerHooksBridge\\|hookDebugLog\\|getRoutingBucket"
---

<objective>
Wire the Plan 02 dispatch core into the extension lifecycle: register
the hooks bridge at extension-factory time (index.ts), rebuild the
routing table after each scope's apply pass (apply.ts), maintain the
parsed-config cache on install/uninstall (install.ts + uninstall.ts),
and lock the 7 architectural invariants of DISP-01..04 + OBS-01 with a
single test file at `tests/architecture/hooks-dispatch.test.ts`.
This plan completes Phase 59: after merge, the hook dispatch path is
end-to-end functional (with the EXECUTION layer still stubbed for a
later phase) and every load-bearing contract has a CI-enforced gate.

Purpose: D-59-02 + D-59-03 + DISP-01..04 + OBS-01 all converge on a
small set of wiring edits — one call site per integration point. The
architecture-test file is the load-bearing closure: it pins what Plan 02
built (count of pi.on calls, routing-table shape, ordering, epoch
defense, isError split, OBS-01 import-graph). The wiring edits are
deliberately minimal so future contributors can read the diff and see
exactly where the bridge slots into the existing extension lifecycle.

Output: 4 modified production files (1-3 line edits each), 1 new
architecture-test file (~350 LoC across 7 named blocks), with all
Phase 57/58 tests preserved GREEN.

## Artifacts this phase produces

- `registerHooksBridge` call at the extension factory site in `index.ts`
  (factory-time DISP-01 + D-59-02 + D-59-03 trigger). The default export
  is converted to `async` and the bridge registration is `await`ed so
  Pi's loader (which itself awaits the factory's returned Promise per
  loader.d.ts) blocks first-event emission until all 7 pi.on handlers
  are attached.
- `rebuildRoutingTables(state, loc)` call inside the per-scope loop in
  `orchestrators/reconcile/apply.ts` (DISP-02 trigger).
- `addPluginConfigToCache(scope, marketplace, plugin, parsed)` call
  inside the install.ts `withLockedStateTransaction` closure (D-59-02
  cache lifecycle — insert).
- `removePluginConfigFromCache(scope, marketplace, plugin)` call
  inside the uninstall.ts `withLockedStateTransaction` closure (D-59-02
  cache lifecycle — remove).
- 7 architecture-test invariant blocks in `tests/architecture/hooks-dispatch.test.ts`:
  (a) pi.on count + event-name set; (b) routing-table key set; (c)
  cross-plugin + within-plugin ordering; (d) epoch-mismatch no-op; (e)
  tool_result isError split; (f) OBS-01 sole-seam import-graph; (g)
  Phase 57 hookDebugLog stub removal.
- An "Audit" comment in `reinstall.ts` and `update.ts` documenting that
  these orchestrators delegate to install.ts + uninstall.ts (cache flow
  transitively correct; no edits needed in those two files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-CONTEXT.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-RESEARCH.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-PATTERNS.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-VALIDATION.md
@.claude/rules/typescript-comments.md

# Files this plan modifies (read first to confirm exact insertion points)
@extensions/pi-claude-marketplace/index.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts

# Plan 02 output (this plan consumes; do not modify)
@extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
@extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts
@extensions/pi-claude-marketplace/bridges/hooks/index.ts

# Plan 01 output (this plan tests but does not modify)
@extensions/pi-claude-marketplace/shared/debug-log.ts

# Transaction primitive (the cache add/remove sits inside this)
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts

# Architecture-test patterns to mirror
@tests/architecture/hooks-foundation.test.ts
@tests/architecture/hooks-supportability.test.ts
@tests/architecture/hooks-tool-name-map.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire registerHooksBridge into index.ts and rebuildRoutingTables into apply.ts</name>
  <files>
    extensions/pi-claude-marketplace/index.ts,
    extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/index.ts (full file — confirm the factory shape; the existing `onResourcesDiscover` typed-binding pattern at lines 19-26; the existing `registerClaudePluginCommand` + `registerClaudeMarketplaceTools` calls at lines 63-67; the import list at lines 1-16)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts lines 780-835 (the per-scope `for (const scope of scopes)` loop at line 790; the `await applyPlan(opts, readResult.plan, outcomes)` call at line 833; the `continue` paths in the catch/invalid-outcomes arms; the existing imports at the file top)
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (Plan 02 output — confirm the exact `registerHooksBridge` signature, the `rebuildRoutingTables` signature, the `_setProjectCwdForTest`-or-equivalent project-cwd capture API if Task 2's needs surfaced one)
    - extensions/pi-claude-marketplace/persistence/locations.ts lines 127-180 (locationsFor signature for the per-scope locations construction at the rebuild call site)
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts (withLockedStateTransaction signature — the rebuild call site uses a brief read-only closure per RESEARCH Open Question 1)
    - node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts line 1027 (`export type ExtensionFactory = (pi: ExtensionAPI) => void | Promise<void>;` — the peer-dep declares async factories as a first-class shape) AND node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/loader.d.ts line 15 (`loadExtensionFromFactory(...): Promise<Extension>` — Pi's loader awaits the factory's returned Promise, which means Pi will not emit session_start (or any other event) until the factory Promise resolves; this is what makes the async-await form race-free)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-CONTEXT.md § "Codebase landing sites" (the explicit list of call-site files; `index.ts` invokes `registerHooksBridge(pi, deps)`; `orchestrators/reconcile/apply.ts` adds `rebuildRoutingTables(state, loc)` call)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-PATTERNS.md § "`index.ts` (MODIFIED) — add `registerHooksBridge(pi)`" + § "`orchestrators/reconcile/apply.ts` (MODIFIED) — DISP-02 rebuild call site" (lines 139-167 — exact insertion-point guidance)
  </read_first>
  <behavior>
    `index.ts` modifications (factory shape LOCKED to async/await):
    - Add an import: `import { registerHooksBridge } from "./bridges/hooks/index.ts";` (the direct-path form, mirroring how the four existing bridges are imported through their own paths elsewhere in the codebase).
    - Convert the default-export factory signature from `export default function claudeMarketplaceExtension(pi: ExtensionAPI): void` to `export default async function claudeMarketplaceExtension(pi: ExtensionAPI): Promise<void>`. This conversion is REQUIRED, not optional: the peer-dep declares `export type ExtensionFactory = (pi: ExtensionAPI) => void | Promise<void>` at types.d.ts line 1027, and `loadExtensionFromFactory(...)` at loader.d.ts line 15 returns `Promise<Extension>` and internally awaits the factory's returned value. The `void` fire-and-forget alternative is REJECTED because it would race against the first session_start: Pi's loader does not see the un-awaited inner Promise from `void registerHooksBridge(...)`, so it would mark the extension loaded before the 7 pi.on calls completed.
    - Add `await registerHooksBridge(pi, { projectCwdSource: "deferred", userCwd: homedir() });` INSIDE the default-export factory body, placed AFTER the `onResourcesDiscover` registration block (closes at line 61) and BEFORE the existing `registerClaudePluginCommand(pi, ...)` call at line 63. Because the call is `await`ed inside the async factory, Pi's loader does not resolve the factory's outer Promise until the bridge's 7 pi.on registrations and user-scope cache hydrate have completed — this is the load-bearing race-free guarantee.
    - The `opts` shape: `{ projectCwdSource: "deferred", userCwd: homedir() }` — where `projectCwdSource: "deferred"` is a sentinel that tells registerHooksBridge to hydrate the user scope at factory time and defer project-scope hydration to a later signal. If Plan 02's registerHooksBridge signature ended up differently (e.g. requiring the cwd up front), this task adapts to that signature — the binding requirement is that the user-scope cache + 7 pi.on registrations DO happen at factory time before the awaited Promise resolves, and the project-scope hydrate may happen on first resources_discover.
    - Add a small wire-up inside the existing `onResourcesDiscover` handler (the existing arrow function at lines 27-61): a call to `setProjectCwdAndRehydrate(event.cwd)` (or whatever capture function Plan 02 exposed) BEFORE the existing `applyReconcile` call. The rehydrate is allowed to be async — `await` it before applyReconcile so the routing table is populated when applyReconcile's rebuild runs.

    `orchestrators/reconcile/apply.ts` modifications:
    - Add imports at the file top: `import { rebuildRoutingTables } from "../../bridges/hooks/index.ts";` matching the per-bridge import convention used elsewhere in apply.ts (today apply.ts imports the agents/commands/etc bridge functions via their own paths so the per-bridge path is the natural fit).
    - Inside the `for (const scope of scopes)` loop body, AFTER `await applyPlan(opts, readResult.plan, outcomes)` returns at line 833 and BEFORE the implicit fall-through to the next iteration: insert a brief read-only state-snapshot acquisition + rebuild call. Per RESEARCH Open Question 1 Option A (safest), this is:
      ```
      const loc = locationsFor(scope, opts.cwd);
      await withLockedStateTransaction(loc, async (tx) => {
        rebuildRoutingTables(tx.state, loc);
        // No tx.save() — read-only acquisition (the rebuild is pure cache-walk).
      });
      ```
    - The new lines sit INSIDE the existing try/catch isolation per WR-01 — a throw from withLockedStateTransaction is already coerced into an `invalid-block` outcome at the existing catch arm; the rebuild's read-only acquisition is brief enough that an EACCES/lock-held race surfaces as the same `invalid-block` shape with `basename: "state.json"`.
    - For the `readResult.plan === undefined` arm (line 832 — no plan to apply), the rebuild call MUST still happen (the scope may have had previously-installed plugins whose hooks need to be registered against the routing table on every reconcile pass). The cleanest path: extract the rebuild into a tiny inner helper `async function rebuildScopeRoutingTable(loc: ScopedLocations): Promise<void> { ... }` and call it after the `continue` arms — i.e. as the final action in the per-scope iteration body, regardless of whether applyPlan ran. If the scope's read-pass threw (the WR-01 isolation arm) the rebuild is SKIPPED (state is not safely loadable in that scope).
  </behavior>
  <action>
    EDIT `extensions/pi-claude-marketplace/index.ts`:
      1. Add the `import { registerHooksBridge } from "./bridges/hooks/index.ts";` line in the import block at the top (alphabetical group near the other `./` local imports; the existing imports today are all `./` local, so insert in alphabetical order — between `./bridges/...` (if any) and the other local imports).
      2. Change the default-export factory signature from `export default function claudeMarketplaceExtension(pi: ExtensionAPI): void {` to `export default async function claudeMarketplaceExtension(pi: ExtensionAPI): Promise<void> {`. This is LOCKED, not conditional: peer-dep types.d.ts line 1027 declares `ExtensionFactory = (pi: ExtensionAPI) => void | Promise<void>`; loader.d.ts line 15 declares `loadExtensionFromFactory(factory, ...): Promise<Extension>` — Pi's loader awaits the factory Promise before the extension's pi.on handlers can fire against any event. No conditional fallback to `void` is permitted (the void form races against first session_start).
      3. Insert `await registerHooksBridge(pi, { projectCwdSource: "deferred", userCwd: homedir() });` (or the equivalent shape the dispatch-core plan ended up exporting — match whatever Plan 02's `registerHooksBridge` signature exports) BETWEEN the `onResourcesDiscover(...)` registration block (closes at line 61) and the existing `registerClaudePluginCommand(pi, ...)` call at line 63. The `await` is REQUIRED — `void registerHooksBridge(...)` is forbidden per step 2's reasoning.
      4. Inside the existing `onResourcesDiscover("resources_discover", async (event, ctx) => { ... })` body, add a call to capture the project cwd into the router's deferred-hydration slot — the exact API is whatever the dispatch-core plan exposed (e.g. `await setProjectScopeCwdAndHydrate(event.cwd)`). Place this call BEFORE the existing `applyReconcile({ ctx, pi, cwd: event.cwd })` call so the routing table is populated when applyReconcile runs.
      5. Comment-hygiene: all new comments anchor by D-59-NN / DISP-NN / OBS-01; no phase/plan/wave refs. Add a one-line comment beside the `async` keyword anchoring `DISP-01`: "DISP-01: async factory; Pi loader awaits this Promise (loader.d.ts), so 7 pi.on registrations complete BEFORE first session_start emission."

    EDIT `extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts`:
      1. Add the imports `import { rebuildRoutingTables } from "../../bridges/hooks/index.ts";` near the other bridge-related imports in the file's top import block.
      2. Locate the per-scope loop body at lines 790-835. Add an inner async helper (at the file scope, outside `applyReconcile`) named `rebuildScopeRoutingTable(loc: ScopedLocations): Promise<void>` that acquires a brief read-only `withLockedStateTransaction(loc, async (tx) => rebuildRoutingTables(tx.state, loc));` (no tx.save). The helper is local to apply.ts (not exported).
      3. After the existing `if (readResult.plan !== undefined) { await applyPlan(opts, readResult.plan, outcomes); }` block, add `await rebuildScopeRoutingTable(loc);` (where `loc` is reconstructed via `locationsFor(scope, opts.cwd)` if it is not already in scope at that line). The rebuild runs regardless of whether applyPlan ran, EXCEPT in the WR-01 catch-and-continue path (where readResult was never assigned) — that arm `continue`s before reaching this code, so the rebuild is naturally skipped.
      4. Confirm the rebuild call is INSIDE the existing per-scope try-block so a transient lock-held / EACCES throw is coerced into an `invalid-block` outcome by the existing isolation pattern.
      5. Comment hygiene: D-NN-NN / REQ-IDs only.
  </action>
  <verify>
    <automated>npm run typecheck 2>&amp;1 | tail -10 &amp;&amp; node --test tests/architecture/hooks-foundation.test.ts tests/architecture/hooks-supportability.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "registerHooksBridge" extensions/pi-claude-marketplace/index.ts` &gt;= 2 (import + call).
    - `grep -c "rebuildRoutingTables" extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts` &gt;= 2 (import + call).
    - The default-export factory in index.ts is declared `async` and returns `Promise<void>`: `grep -c "export default async function claudeMarketplaceExtension" extensions/pi-claude-marketplace/index.ts` == 1.
    - The `await` keyword precedes the `registerHooksBridge` call site (no `void registerHooksBridge` form): `grep -c "await registerHooksBridge" extensions/pi-claude-marketplace/index.ts` &gt;= 1 AND `grep -c "void registerHooksBridge" extensions/pi-claude-marketplace/index.ts` == 0.
    - `npm run typecheck` GREEN.
    - Phase 57/58 architecture tests GREEN — no regression.
    - `npm run lint -- extensions/pi-claude-marketplace/index.ts extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts` GREEN.
    - No phase/plan/wave refs in either file's new comments (`grep -E '\\b(Phase|Plan|Wave|Pitfall|Pattern)\\s+[0-9]' <those two files>` returns only pre-existing matches inside domain-language contexts already audited per the comment policy).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire addPluginConfigToCache/removePluginConfigFromCache into install.ts and uninstall.ts</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts lines 700-770 (`runInstallLedger` end — confirm the `installCtx` populated at line 769; the `resources.hooks: []` literal at line 723; the state mutation block at lines 705-730)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts lines 845-945 (the outer `await withLockedStateTransaction(locations, async (tx) => { ... })` closure starting at line 855; the `runInstallLedger` call at line 866; the `installCtx = result.installCtx;` assignment at line 889; the `await tx.save();` at line 942)
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts lines 360-420 (the outer `await withLockedStateTransaction(locations, async (tx) => { ... })` closure starting at line 367; the `state.marketplaces[mp].plugins[plugin]` access pattern; the `delete` mutation site)
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (Plan 02 output — `addPluginConfigToCache` + `removePluginConfigFromCache` signatures)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (parseHooksConfig signature — called by install.ts BEFORE the cache add if the plugin declares hooks; or alternative: install.ts reads the already-parsed config from the resolved plugin context if the resolver already produced one)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-CONTEXT.md § "Codebase landing sites" lines 322-329 (install.ts / uninstall.ts edits enumerated)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-PATTERNS.md § "`orchestrators/plugin/install.ts` and `uninstall.ts` (MODIFIED)" lines 171-178
  </read_first>
  <behavior>
    `install.ts` modifications:
    - Add an import: `import { addPluginConfigToCache } from "../../bridges/hooks/index.ts";` near the other bridge imports.
    - Inside the outer `withLockedStateTransaction` closure (lines 855-943), AFTER the `installCtx = result.installCtx;` assignment at line 889 succeeds, AND only when the plugin actually declares hooks (i.e. `result.installCtx.resolved.hooksConfigPath !== undefined` — the Phase 57 resolver carries this field per resolver.ts lines 65-70), call `addPluginConfigToCache(scope, marketplace, plugin, hooksConfig)` where `hooksConfig` is parsed by calling `parseHooksConfig(rawHooksJson)`.
    - The rawHooksJson is read from disk via `fs.readFile(<pluginRoot>/<hooksConfigPath>, "utf8")` — the absolute path is composable from `result.installCtx.resolved.pluginRoot` (Phase 57 resolver output) and the relative `hooksConfigPath`. Use `path.join(installCtx.resolved.pluginRoot, installCtx.resolved.hooksConfigPath)`.
    - If parseHooksConfig returns `{ok: false}` here (which should be IMPOSSIBLE because the resolver already validated the config at install time per Phase 57/58 — the resolver flips the plugin to `installable: false` on parse failure), the install succeeded but the runtime cache cannot be populated. Defensive handling: log via hookDebugLog with a string like `"install: parsed hooks.json failed re-parse for ${plugin}@${marketplace}; reconcile rebuild will rehydrate from disk"`, do NOT throw (the install succeeds; reconcile will recover). Comment cites D-59-02 cache lifecycle.
    - The call sits BEFORE `tx.save()` at line 942 so a save-time failure rolls back BOTH the state mutation AND the cache add: actually, the cache is in-memory and not transactional with state.json. The correct ordering rationale: place the cache add AFTER the state mutation that records the install (so a sequencing crash before tx.save leaves no orphaned cache entry referring to a non-existent state record). Since cache mutations are synchronous Map ops, and tx.save is the LAST step before closure exit, the natural placement is: after `installCtx = result.installCtx;` (line 889), do the cache add. If the closure throws after that, the cache is left with an entry referring to a state mutation that the guard discards (ST-7 contract per the runInstallLedger comment at line 732-735). This is a known mild leak — but the next `/reload` resets the cache (D-59-03 epoch + factory-time hydrate from disk), so the leak is bounded. Document this trade-off in a comment.
    - Skip the cache add when `installCtx.resolved.hooksConfigPath === undefined` (the plugin declares no hooks, so resources.hooks is empty and there's nothing to cache).

    `uninstall.ts` modifications:
    - Add an import: `import { removePluginConfigFromCache } from "../../bridges/hooks/index.ts";` near the other bridge imports.
    - Inside the outer `withLockedStateTransaction` closure (starts at line 367), AFTER the `delete state.marketplaces[mp].plugins[plugin]` mutation site, call `removePluginConfigFromCache(scope, marketplace, plugin)`. The call is unconditional (the function is idempotent — removing a missing key is a no-op).
    - Position the call BEFORE `tx.save()` (the last step) so a save-time throw discards both the state mutation (via the guard's ST-7 contract) and the cache mutation simultaneously. Same in-memory-vs-on-disk trade-off as install.ts; document briefly.

    Behavioral verification — these edits do NOT add new architecture tests in this task (the integration story is covered by Task 3's architecture-test file). Instead, the existing `tests/orchestrators/plugin/install.test.ts` and `tests/orchestrators/plugin/uninstall.test.ts` test suites should stay GREEN — the cache add/remove is additive and does not change install/uninstall observable behavior on the happy path.
  </behavior>
  <action>
    EDIT `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts`:
      1. Add the import line `import { addPluginConfigToCache } from "../../bridges/hooks/index.ts";` near the existing bridge imports.
      2. Add an import for `parseHooksConfig` from `../../domain/components/hooks.ts` if it is not already imported.
      3. Locate the `installCtx = result.installCtx;` assignment near line 889. Immediately AFTER this line, add a conditional gate on `installCtx.resolved.hooksConfigPath !== undefined` (skip the block entirely when the plugin declares no hooks). Inside the gate: compose `hooksJsonPath` via `path.join(installCtx.resolved.pluginRoot, installCtx.resolved.hooksConfigPath)`; wrap the read+parse in a single try/catch where the try-body calls `await fs.readFile(hooksJsonPath, "utf8")` then `parseHooksConfig(raw)`, branches on the discriminated `parsed.ok` field — `true` → `addPluginConfigToCache(scope, marketplace, plugin, parsed.value);`; `false` → `hookDebugLog(\`install: parsed hooks.json failed re-parse for ${plugin}@${marketplace}: ${parsed.reason}\`);` — and the catch-body calls `hookDebugLog(\`install: hooks.json read failed for ${plugin}@${marketplace}: ${errorMessage(err)}\`);` with the typed err narrowed via `errorMessage` from `shared/errors.ts`. The conditional never throws: both failure arms are debug-log-only (resolver already validated at install entry; reconcile rehydrates on next pass).
      4. Confirm `fs`, `path`, `errorMessage`, `hookDebugLog` are already imported (the file imports many of these; add the missing ones from the file's existing import groups: `import fs from "node:fs/promises";`, `import path from "node:path";`, `import { errorMessage } from "../../shared/errors.ts";`, `import { hookDebugLog } from "../../shared/debug-log.ts";`).
      5. Comment block above the conditional cites `D-59-02` cache lifecycle; explain the parse-failure path is defensive (resolver already validated; reconcile will rehydrate).

    EDIT `extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts`:
      1. Add the import `import { removePluginConfigFromCache } from "../../bridges/hooks/index.ts";` near the existing bridge imports.
      2. Locate the `delete state.marketplaces[mp].plugins[plugin]` (or equivalent state mutation that removes the plugin record). Immediately AFTER this mutation and BEFORE `tx.save()`, add `removePluginConfigFromCache(scope, marketplace, plugin);` with a one-line comment "D-59-02: cache lifecycle — synchronous in-memory remove; idempotent."
      3. The call is unconditional; no `if (resources.hooks.length > 0)` guard — the function is idempotent and the cost of a missing-key delete is zero.

    AUDIT (do NOT edit — read-only verification):
      • Open `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` and confirm it routes through `installPlugin` and `uninstallPlugin` (the public functions exported from install.ts / uninstall.ts) rather than re-implementing the closures. If yes, the cache add/remove flows transitively. If no, document the gap as a SUMMARY-level finding (this plan does NOT widen scope to fix it; the audit just confirms the assumption).
      • Open `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` and perform the same audit. update.ts is conceptually uninstall-then-install on version mismatch; confirm it uses the public entry points.
  </action>
  <verify>
    <automated>npm run typecheck 2>&amp;1 | tail -10 &amp;&amp; node --test tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/uninstall.test.ts 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - `grep -c "addPluginConfigToCache" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` &gt;= 2 (import + call).
    - `grep -c "removePluginConfigFromCache" extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` &gt;= 2 (import + call).
    - `npm run typecheck` GREEN.
    - Existing install.test.ts + uninstall.test.ts GREEN — cache wiring is additive.
    - `npm run lint -- extensions/pi-claude-marketplace/orchestrators/plugin/install.ts extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` GREEN.
    - Audit comment in the SUMMARY: reinstall.ts + update.ts route through installPlugin/uninstallPlugin (or document the gap if not).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Architecture-test pin file (tests/architecture/hooks-dispatch.test.ts) — 7 invariant blocks for DISP-01..04 + OBS-01</name>
  <files>
    tests/architecture/hooks-dispatch.test.ts
  </files>
  <read_first>
    - tests/architecture/hooks-foundation.test.ts (block structure, file-header pattern, static-introspection technique — mirror exactly; the file uses 5 separator-comment-delimited blocks labelled "Block N: REQ-ID -- description")
    - tests/architecture/hooks-supportability.test.ts (Phase 58 architecture-test file — for the closed-set introspection pattern + the `assertNever`-style exhaustiveness pattern)
    - tests/architecture/hooks-tool-name-map.test.ts (inverse / count-lock invariant style for the per-mapping-entry test pattern)
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (Plan 02 output — confirm the test-only inspectors `_routingTableForTest`, `_bumpEpochForTest`, the `currentEpoch` reader; confirm `getRoutingBucket`)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts (Plan 02 output — confirm the `compositeHandlerFor` + `toolResultCompositeHandler` exports)
    - extensions/pi-claude-marketplace/shared/debug-log.ts (Plan 01 output — confirm it is the only file importing `process.env.PI_CLAUDE_MARKETPLACE_DEBUG`)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (Plan 01 modification — confirm `hookDebugLog` is gone from the export surface)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-CONTEXT.md § Architecture tests (lines 340-350 — the 5 D-59-04 architecture-test invariants Phase 59 is required to pin)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-PATTERNS.md § "`tests/architecture/hooks-dispatch.test.ts` (NEW)" (lines 192-217 — block structure + technique guidance)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-VALIDATION.md § "Per-Task Verification Map" (the 17-row test→requirement map)
  </read_first>
  <behavior>
    File-header doc-block (8-12 lines) per the hooks-foundation.test.ts pattern: state that this file pins DISP-01..04 + OBS-01 + D-59-01..05 invariants via static introspection + runtime mock-Pi factory invocation + import-graph scan. No phase/plan refs per the comment policy.

    Imports block (alphabetical groups):
    - From `node:test`: `test`.
    - From `node:assert/strict`: default import.
    - From `node:fs/promises`: `readFile`.
    - From `node:path`: `path` (for import-graph scans).
    - From the extension under test: `registerHooksBridge`, `rebuildRoutingTables`, `addPluginConfigToCache`, `removePluginConfigFromCache`, `currentEpoch`, type `RoutingEntry` from `bridges/hooks/index.ts`; test-only inspectors `_routingTableForTest`, `_bumpEpochForTest` from `bridges/hooks/event-router.ts` directly; `compositeHandlerFor`, `toolResultCompositeHandler` from `bridges/hooks/dispatch.ts`; `BUCKET_A_EVENTS` from `domain/components/hook-events.ts`.

    7 named blocks (separator-comment-delimited per the hooks-foundation pattern):

    **Block 1: DISP-01 — `pi.on(...)` registered exactly 7 times at factory time with locked event-name set**
    - `test("DISP-01: registerHooksBridge calls pi.on exactly 7 times with the locked Pi event names")`:
      • Construct a synthetic `pi` mock with an `on(event, handler)` method that pushes `event` into a `calls: string[]` array; stub the other ExtensionAPI methods needed by registerHooksBridge to no-ops (`getAllTools` returns `[]`; `on.bind` returns the mock's own `on`).
      • Stub `withLockedStateTransaction` (via test seam or by setting up a temp scope root with no state.json so the read-only acquisition returns empty state) so the hydrate step does not require real disk.
      • Call `await registerHooksBridge(piMock, { projectCwdSource: "deferred", userCwd: <tmpdir> });`.
      • Assert `piMock.calls.length === 7` and the set equality between `new Set(piMock.calls)` and the literal set `new Set(["session_start", "session_shutdown", "session_before_compact", "session_compact", "input", "tool_call", "tool_result"])`.

    **Block 2: DISP-01 / DISP-02 — routingTable has 8 Claude-event buckets keyed by BUCKET_A_EVENTS after rebuild**
    - `test("DISP-02: rebuildRoutingTables produces exactly the BUCKET_A_EVENTS 8-tuple keyset")`:
      • Construct a synthetic ExtensionState with a single user-scope marketplace containing one plugin whose `resources.hooks: ["hooks.json"]` and pre-populate the cache via `addPluginConfigToCache` with a minimal HooksConfig declaring one entry per BUCKET_A_EVENTS event.
      • Call `rebuildRoutingTables(state, locationsFor("user", <tmpdir>))`.
      • Read the routing table via `_routingTableForTest()`.
      • Assert the table's keyset, as `Array.from(table.keys()).sort()`, equals `[...BUCKET_A_EVENTS].sort()` (set-equality lock).

    **Block 3: DISP-04 — cross-plugin order matches compareByNameThenScope; within-plugin order matches declarationIndex**
    - `test("DISP-04: cross-plugin order is compareByNameThenScope (project-first, alphabetical)")`:
      • Synthetic state with 3 plugins: `{scope: "user", id: "alpha"}, {scope: "project", id: "beta"}, {scope: "project", id: "gamma"}`, each declaring one PreToolUse entry with matcher `""`.
      • Pre-populate cache; rebuild for BOTH scopes.
      • Read `getRoutingBucket("PreToolUse")` (or via `_routingTableForTest().get("PreToolUse")`).
      • Assert the order is `[beta, gamma, alpha]` (project-first, alphabetical within scope, user last).
    - `test("DISP-04: within-plugin declaration order is preserved via declarationIndex")`:
      • Single plugin with hooks.json declaring 3 PreToolUse handlers across 2 groups (group1: 1 handler, group2: 2 handlers).
      • Assert the PreToolUse bucket's entries are in source-declaration order (group1.h1, group2.h1, group2.h2).

    **Block 4: DISP-03 — epoch mismatch causes composite handler to no-op without invoking dispatchHookExec**
    - `test("DISP-03: composite handler with stale capturedEpoch no-ops on dispatch")`:
      • Create `compositeHandlerFor("PreToolUse", capturedEpoch=1)`.
      • Pre-populate routing table with one entry; mock `dispatchHookExec` via `t.mock.method` on the module (use the same module-cache pattern as the unit tests in Plan 02 Task 3).
      • Call `_bumpEpochForTest()` to advance `liveEpoch` to 2.
      • Invoke the handler with a synthetic tool_call event.
      • Assert the dispatchHookExec mock was called 0 times.
      • Note in a comment: per RESEARCH Pitfall 1, the real production zombie defense is Pi's _extensionRunner swap; this test exercises the epoch defense in isolation against a shared-module-instance path (the natural cross-instance reload path is hard to simulate in-process — documented limitation).

    **Block 5: D-59-01 — `tool_result` composite handler reads `event.isError` and dispatches to the correct bucket**
    - `test("D-59-01: toolResultCompositeHandler routes isError=true to PostToolUseFailure bucket")`:
      • Pre-populate routing table with one entry in the PostToolUseFailure bucket and a different entry in the PostToolUse bucket.
      • Mock dispatchHookExec to record which entry it received.
      • Call the handler with `{ toolName: "bash", isError: true }`.
      • Assert the mock received the PostToolUseFailure entry (and NOT the PostToolUse entry).
    - `test("D-59-01: toolResultCompositeHandler routes isError=false to PostToolUse bucket")`:
      • Same setup as above; call with `{ toolName: "bash", isError: false }`.
      • Assert the mock received the PostToolUse entry.

    **Block 6: OBS-01 — shared/debug-log.ts is the sole runtime debug-output module (import-graph scan)**
    - `test("OBS-01: console.error appears ONLY in shared/debug-log.ts within the extension source tree")`:
      • Scan the file tree under `extensions/pi-claude-marketplace/` recursively, reading every `.ts` file (exclude `tests/` — those mock console.error legitimately).
      • For each file, grep for `console.error\\(` (regex; allow leading whitespace).
      • Assert the only file with matches is `extensions/pi-claude-marketplace/shared/debug-log.ts`.
      • Suggested implementation: use a small recursive-walk helper inside the test or shell out via `child_process.execSync(\"grep -rln 'console\\\\.error(' extensions/pi-claude-marketplace/ --include='*.ts'\")`. Pick the form that runs fast (`<5s`) and works on the project's CI Node version.
    - `test("OBS-01: eslint.config.js per-file override scopes the console allowance to shared/debug-log.ts only")`:
      • Read `eslint.config.js` as text.
      • Assert the substring `"extensions/pi-claude-marketplace/shared/debug-log.ts"` appears within a `files:` array inside an override block whose `rules:` toggles `no-console` to `off`.
      • Assert no other override block toggles `no-console` to `off` for any file under `extensions/pi-claude-marketplace/` EXCEPT for the pre-existing `shared/notify.ts` and `persistence/migrate.ts` overrides (which are domain-allowlisted per IL-3).

    **Block 7: D-59-05 — Phase 57 `hookDebugLog` stub is removed from domain/components/hooks.ts**
    - `test("D-59-05: domain/components/hooks.ts no longer exports hookDebugLog")`:
      • Read the file as text.
      • Assert the regex `/^export\\s+(function|const)\\s+hookDebugLog\\b/m` does NOT match (export gone).
      • Assert the regex `/from\\s+["']\\.\\.\\/\\.\\.\\/shared\\/debug-log\\.ts["']/m` DOES match (import rewired to shared/debug-log.ts).
      • Assert `hookDebugLog\\(` appears at least 3 times in the file body (the three call sites preserved).

    Test infrastructure:
    - All blocks use `node:test` + `node:assert/strict` exclusively.
    - The synthetic Pi mock is a small inline factory (`function makePiMock(): { on: ..., calls: string[] }`).
    - The cache fixtures are constructed in-test (no external fixture files needed).
    - The full suite runs under `node --test tests/architecture/hooks-dispatch.test.ts` in &lt; 10 seconds.

    Coverage cross-check: this file pins 5 D-59-04 invariants (Blocks 1, 2, 3, 4, 5) + 2 OBS-01 invariants (Blocks 6, 7) = 7 named blocks total. Required for Phase 59 closure.
  </behavior>
  <action>
    WRITE `tests/architecture/hooks-dispatch.test.ts` per the behavior spec. File-header doc-block (no phase/plan refs); imports block; 7 named blocks separated by `// ────────...` separator comments.

    Block 1 implementation: the synthetic Pi mock is the smallest viable shape that satisfies registerHooksBridge's runtime calls. The mock exposes (a) an `on(event, handler)` method that pushes the `event` argument string into a `calls: string[]` array and returns `void`; (b) a `getAllTools()` method returning the empty array (needed by any soft-dep probe registerHooksBridge transitively calls). If registerHooksBridge uses the the existing `pi.on.bind(pi)` typed-binding pattern carried forward from the bridge factory and the dispatch-core plan, the `on` member is constructed via `Object.assign(<the on function>, { bind: () => <the same on function> })` so the `.bind` call returns a function that proxies to `on` with identical capture semantics. The mock is cast to `ExtensionAPI` via `(piMock as unknown as ExtensionAPI)` at the call site — type-loose by intent; architecture tests prioritize observable contract over compile-time fidelity of the mock surface.

    Block 4 epoch mismatch test: import `_bumpEpochForTest` directly from `extensions/pi-claude-marketplace/bridges/hooks/event-router.ts` (NOT through the barrel — these are not re-exported). Mock dispatchHookExec via `t.mock.module` or by direct in-place replacement of the function export — match the dispatch-core plan's Task 3 pattern for consistency.

    Block 6 console.error scan: prefer a small recursive walker inline (more portable than execSync); the scan is a one-pass directory traversal reading each .ts file's text and applying a regex. Exclude `node_modules/`, `dist/`, `tests/` paths explicitly. The scan should complete in `<1s` for a project this size.

    Block 7 assertions are pure regex against file text; no module instantiation needed.

    Comment hygiene throughout: D-NN-NN / REQ-IDs only; no phase/plan/wave refs in block titles, test names, or inline comments. Test names anchor by REQ ID (e.g. `"DISP-01: ..."`, `"D-59-01: ..."`).

    Run the full test file once during implementation to verify all 7 blocks PASS; if any block depends on a an export the dispatch-core plan delivers that the bridge does not yet provide (e.g. `_bumpEpochForTest` not yet exposed by the dispatch-core plan), add the export in event-router.ts as a small follow-up edit — this preserves the phase's atomic delivery.
  </action>
  <verify>
    <automated>node --test tests/architecture/hooks-dispatch.test.ts 2>&amp;1 | tail -30 &amp;&amp; npm run check 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - `tests/architecture/hooks-dispatch.test.ts` exists.
    - `node --test tests/architecture/hooks-dispatch.test.ts` reports "pass &gt;= 9" (Block 3 has 2 tests, Block 5 has 2 tests, Block 6 has 2 tests, others 1 each = 1+1+2+1+2+2+1 = 10 tests; the at-least bound tolerates planner's discretion on whether to split a block into multiple `test(...)` calls or fold into one).
    - All Phase 57/58 architecture tests stay GREEN.
    - `npm run check` GREEN at plan close (full lint + typecheck + tests + format).
    - `grep -E '\\b(Phase|Plan|Wave|Pitfall|Pattern)\\s+[0-9]' tests/architecture/hooks-dispatch.test.ts | wc -l` == 0.
    - The 5 must_haves truths about architecture-test invariants are observable: count-lock on 7 pi.on calls (Block 1), 8-bucket routing-table keyset (Block 2), cross-plugin + within-plugin ordering (Block 3), epoch-mismatch no-op (Block 4), `isError` split (Block 5), OBS-01 sole-seam import-graph (Block 6), hookDebugLog stub removal (Block 7).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.ts factory → registerHooksBridge | The factory passes a sentinel `projectCwdSource: \"deferred\"` to the bridge; the bridge MUST NOT attempt to construct a project-scope path from this sentinel — it must wait for the resources_discover signal. |
| apply.ts per-scope loop → rebuildRoutingTables | Brief read-only state acquisition; the rebuild MUST NOT call tx.save(). A drift here would silently rewrite state.json on every reconcile pass. |
| install.ts / uninstall.ts cache mutations → in-memory routing table | Cache add/remove are in-memory and not transactional with state.json; the comment must document the bounded leak (next `/reload` resets) so a future contributor understands the trade-off. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-01 | Tampering | apply.ts rebuild call inside per-scope loop | mitigate | The rebuild call is read-only via `withLockedStateTransaction(loc, async (tx) => rebuildRoutingTables(tx.state, loc))` with NO `tx.save()`. Architecture test Block 2 indirectly verifies by setting up rebuild and asserting no state.json mtime bump (if practical; otherwise the read-only contract is documented and the next phase's integration tests catch a drift). |
| T-59-03-02 | Tampering | install.ts cache-add after partial failure | accept | The cache is in-memory and bounded: a closure throw between `installCtx = result.installCtx` and `tx.save` leaves a cache entry without a corresponding state record. The next /reload resets the cache (factory-time hydrate rebuilds from state.json + disk per D-59-02). Comment documents the trade-off; no test pins the cleanup because the leak is bounded. |
| T-59-03-03 | Information Disclosure | architecture test import-graph scan | accept | The scan reads .ts source text from the local repo; no network call. NFR-9 absolute-path redaction is not needed at the test layer. |
| T-59-03-04 | Repudiation | architecture test for pi.on count | mitigate | The count-lock test (Block 1) red-fails on any registration drift — a future contributor who adds a 6th or 8th pi.on call to the hooks bridge triggers an immediate CI failure with the count delta. |
| T-59-03-05 | Denial of Service | apply.ts rebuild lock acquisition | accept | The read-only lock acquisition is brief (sub-ms after the lock-held grace). A pathological lock-held race surfaces as the existing WR-01 invalid-block outcome; no new failure mode. |
| T-59-03-06 | Elevation of Privilege | architecture test seam access via test-only exports | mitigate | The `_routingTableForTest` and `_bumpEpochForTest` exports are explicitly NOT re-exported via `bridges/hooks/index.ts`, so production code cannot import them through the public surface. The test file imports them directly from `bridges/hooks/event-router.ts` (the underscore-prefix convention signals test-only). |
| T-59-03-SC | Tampering | npm/pip/cargo installs | accept | No new package dependencies introduced; slopcheck baseline carries over. |
</threat_model>

<verification>
- `node --test tests/architecture/hooks-dispatch.test.ts` reports pass for all 7 named blocks.
- `node --test tests/architecture/hooks-foundation.test.ts tests/architecture/hooks-supportability.test.ts tests/architecture/hooks-tool-name-map.test.ts` GREEN — no Phase 57/58 regression.
- `node --test tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/dispatch-exec.test.ts` GREEN — Plan 02 unit tests preserved.
- `node --test tests/shared/debug-log.test.ts` GREEN — Plan 01 unit tests preserved.
- `node --test tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/uninstall.test.ts` GREEN — cache wiring is additive.
- `npm run check` GREEN at plan close (full lint + typecheck + tests + format).
- `git diff --stat extensions/pi-claude-marketplace/` shows: index.ts (+~10 lines), orchestrators/reconcile/apply.ts (+~10 lines), orchestrators/plugin/install.ts (+~20 lines), orchestrators/plugin/uninstall.ts (+~5 lines), and the Plan 02 + Plan 01 deltas already merged.
- `git diff --stat tests/architecture/` shows the new hooks-dispatch.test.ts (~350 LoC).
- Reinstall.ts and update.ts audit recorded in the SUMMARY (no code edits).
</verification>

<success_criteria>
- Hook dispatch is end-to-end wired: extension factory registers the bridge, reconcile-apply rebuilds the routing table per scope, install/uninstall maintain the cache.
- 7 architectural invariants of DISP-01..04 + OBS-01 are CI-locked in `tests/architecture/hooks-dispatch.test.ts`.
- All Phase 57/58 architecture tests stay GREEN.
- `npm run check` GREEN at plan close.
- Reinstall + update audit confirms cache flows transitively (or documents the gap).
- The dispatchHookExec stub remains a no-op (a later phase replaces the body in lockstep with the execution layer).
</success_criteria>

<output>
Create `.planning/phases/59-bridge-dispatch-core-debug-seam/59-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
