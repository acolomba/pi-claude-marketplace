---
phase: 59-bridge-dispatch-core-debug-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/debug-log.ts
  - extensions/pi-claude-marketplace/domain/components/hooks.ts
  - eslint.config.js
  - tests/shared/debug-log.test.ts
autonomous: true
requirements: [OBS-01]
requirements_addressed: [OBS-01]
tags: [hooks, debug-log, obs-01, shared-seam]

must_haves:
  truths:
    - "shared/debug-log.ts exists and exports `hookDebugLog(detail: string): void` with the same signature the Phase 57 stub had (D-59-05; D-58-03 hand-off seam contract preserved)."
    - "Calling `hookDebugLog(detail)` writes `[hooks] ${detail}` to `console.error` only when `process.env.PI_CLAUDE_MARKETPLACE_DEBUG === \"1\"`; any other value (including `\"0\"`, `\"true\"`, `undefined`, empty string) is silent (OBS-01 / IL-2)."
    - "domain/components/hooks.ts no longer defines or exports `hookDebugLog`; its three call sites (parseHooksConfig JSON-parse failure, schema-validation failure, supportability failure) re-import from shared/debug-log.ts via the relative path `../../shared/debug-log.ts`."
    - "eslint.config.js carries a per-file override block that disables `no-console` and `no-restricted-syntax` for `extensions/pi-claude-marketplace/shared/debug-log.ts` ONLY (mirrors the existing BLOCK B override for shared/notify.ts at eslint.config.js:138-145); no other file gains console access through this plan."
    - "npm run lint is GREEN with zero new `no-console` / `no-restricted-syntax` failures after the migration; the prior inline `eslint-disable-next-line` directives in hooks.ts are removed because the per-file override on debug-log.ts is now load-bearing (no in-file disables needed there)."
    - "Phase 57 architecture tests (tests/architecture/hooks-foundation.test.ts) and Phase 58 architecture tests (tests/architecture/hooks-supportability.test.ts) stay GREEN — the migration is a file move, not a behavior change."
    - "The Phase 57 hand-off comment in hooks.ts (the `TODO(OBS-01)` block above the removed stub) is dropped because the OBS-01 seam now lives at its canonical home and the TODO is satisfied."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/debug-log.ts"
      provides: "Sole runtime debug-output seam for the hooks dispatch path; env-gated console.error wrapper"
      exports: ["hookDebugLog"]
      min_lines: 15
    - path: "extensions/pi-claude-marketplace/domain/components/hooks.ts"
      provides: "Phase 57/58 baseline minus the local hookDebugLog stub; call sites now import from shared/debug-log.ts"
      contains: "import { hookDebugLog } from \"../../shared/debug-log.ts\""
    - path: "eslint.config.js"
      provides: "Per-file override block (mirrors BLOCK B for shared/notify.ts) allowing console access only inside shared/debug-log.ts"
      contains: "extensions/pi-claude-marketplace/shared/debug-log.ts"
    - path: "tests/shared/debug-log.test.ts"
      provides: "Unit tests pinning env-gated emission: env-on emits, env-off silent, three other env-var values silent (regression guards against accidental loosening)"
      min_lines: 40
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/components/hooks.ts (parseHooksConfig)"
      to: "extensions/pi-claude-marketplace/shared/debug-log.ts (hookDebugLog)"
      via: "named import from sibling sub-directory"
      pattern: "from\\s+\"\\.\\./\\.\\./shared/debug-log\\.ts\""
    - from: "eslint.config.js"
      to: "extensions/pi-claude-marketplace/shared/debug-log.ts"
      via: "per-file ESLint override block"
      pattern: "extensions/pi-claude-marketplace/shared/debug-log\\.ts"
---

<objective>
Create the canonical OBS-01 debug-output seam at
`extensions/pi-claude-marketplace/shared/debug-log.ts`, retire the Phase
57 local stub at `domain/components/hooks.ts`, and add the ESLint
per-file override that authorizes its sanctioned `console.error` call.
This plan is the leaf foundation for Phase 59: once shipped, the event
router (Plan 02) imports `hookDebugLog` from the new module without any
mid-plan cross-file coupling, and the dispatch path has a single
documented escape hatch from IL-2 / IL-3.

Purpose: D-59-05 locks the migration as a single source-of-truth seam.
The Phase 57 stub was always a hand-off — it carries a `TODO(OBS-01)`
marker that this plan closes. The migration is a file move + ESLint
override + import rewrite, not a behavior change; the signature
`hookDebugLog(detail: string): void` and the
`PI_CLAUDE_MARKETPLACE_DEBUG === "1"` gate are preserved byte-for-byte.

Output: new `shared/debug-log.ts` (~15-25 LoC), `domain/components/hooks.ts`
with the stub deleted and three call sites updated, an `eslint.config.js`
per-file override block, and `tests/shared/debug-log.test.ts` pinning the
env-gated emission contract.

## Artifacts this phase produces

- `hookDebugLog(detail: string): void` — re-homed at
  `extensions/pi-claude-marketplace/shared/debug-log.ts`; signature and
  body identical to the Phase 57 stub. Pure function: reads
  `process.env.PI_CLAUDE_MARKETPLACE_DEBUG` and forwards to
  `console.error` only on exact-equal `"1"`. No imports.
- New ESLint per-file override block in `eslint.config.js` adjacent to
  the existing BLOCK B override (the one for `shared/notify.ts`), files
  glob = `extensions/pi-claude-marketplace/shared/debug-log.ts`, rules
  `no-restricted-syntax: off` and `no-console: off`.
- `tests/shared/debug-log.test.ts` — three unit tests:
  env-on emits the prefixed line; env-off (unset) is silent; three other
  env-var values (`"0"`, `"true"`, empty string) are silent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-CONTEXT.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-RESEARCH.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-PATTERNS.md
@.planning/phases/59-bridge-dispatch-core-debug-seam/59-VALIDATION.md
@.claude/rules/typescript-comments.md

# Files this plan modifies (read first to understand current state)
@extensions/pi-claude-marketplace/domain/components/hooks.ts
@eslint.config.js

# Existing analog ESLint per-file override BLOCK B (mirror this exact shape)
# eslint.config.js:138-145 — already loaded above; see lines 138-145 for shared/notify.ts override
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared/debug-log.ts + ESLint per-file override + unit tests</name>
  <files>
    extensions/pi-claude-marketplace/shared/debug-log.ts,
    eslint.config.js,
    tests/shared/debug-log.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/components/hooks.ts lines 150-212 (the existing `hookDebugLog` stub + its three call sites — copy the function body verbatim, drop the `TODO(OBS-01)` JSDoc block)
    - eslint.config.js lines 130-160 (BLOCK A `no-console`/`no-restricted-syntax` errors at lines 130-136; BLOCK B per-file override for `shared/notify.ts` at lines 138-145; the migrate.ts per-file override at lines 147-159 — the new block sits adjacent to these)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-RESEARCH.md lines 831-869 (`shared/debug-log.ts` skeleton + per-file ESLint override snippet)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-PATTERNS.md § "shared/debug-log.ts (NEW) — OBS-01 seam" (lines 96-135 — pattern guidance)
    - .claude/rules/typescript-comments.md (loaded via system reminder — comment policy forbids phase/plan refs, allows D-NN-NN and REQ-IDs)
  </read_first>
  <behavior>
    Test surface for `tests/shared/debug-log.test.ts`:
    - `test("hookDebugLog: emits when PI_CLAUDE_MARKETPLACE_DEBUG === \"1\"")` — sets env var, mocks `console.error` via `t.mock.method(console, "error")`, calls `hookDebugLog("sample detail")`, asserts the mock was called exactly once with the literal string `"[hooks] sample detail"`, restores env + mock in try/finally.
    - `test("hookDebugLog: silent when PI_CLAUDE_MARKETPLACE_DEBUG is unset")` — deletes the env var (or saves+restores prior value), mocks console.error, calls hookDebugLog, asserts the mock was called zero times.
    - `test("hookDebugLog: silent on env values other than the literal \"1\"")` — iterates over a fixture array of values `["0", "true", "", "yes", "ON", "01", " 1 "]`, asserts the mock was called zero times for each (one mock per iteration, reset between). Locks the exact-equal `"1"` gate per OBS-01 contract.

    Behavior of `hookDebugLog` in `shared/debug-log.ts`:
    - Single named export `hookDebugLog(detail: string): void`.
    - Reads `process.env.PI_CLAUDE_MARKETPLACE_DEBUG`; on exact-equal `"1"`
      calls `console.error(\`[hooks] ${detail}\`)`; otherwise returns
      without side effect.
    - No imports, no external deps; pure leaf module.

    ESLint per-file override behavior:
    - The override block sits in `eslint.config.js` immediately after the
      existing BLOCK B override for `shared/notify.ts` (lines 138-145) so
      the local visual grouping of "per-file console allowances" stays
      adjacent.
    - Files glob: the single literal path
      `"extensions/pi-claude-marketplace/shared/debug-log.ts"`.
    - Rules: `"no-restricted-syntax": "off"` and `"no-console": "off"`.
      No other rule is touched (D-59-05 hygiene: minimal blast radius).
  </behavior>
  <action>
    WRITE `extensions/pi-claude-marketplace/shared/debug-log.ts`. File body:
      • A single doc-comment header (4-8 lines) describing the seam:
        "Sole runtime debug-output seam for the hooks dispatch path";
        anchor by D-59-05 / OBS-01 / IL-2 / IL-3 IDs; no phase/plan/wave
        references per .claude/rules/typescript-comments.md.
      • One named export `hookDebugLog` per the behavior spec.
      • NO inline `eslint-disable-next-line` directives — the per-file
        override carries the authorization. Defense-in-depth inline
        disables are explicitly excluded so a future drift of the per-file
        block is caught by a red lint, not silently absorbed.

    EDIT `eslint.config.js`. Insert a new override block IMMEDIATELY after
    the closing `},` of BLOCK B (the `shared/notify.ts` override at lines
    138-145). The new block follows the same object shape with `files`
    (single-element array) and `rules` (the two off-toggles). Add a
    one-line comment above the block citing OBS-01 / D-59-05 as the anchor
    and explaining the parallel with the BLOCK B `shared/notify.ts`
    rationale (sanctioned escape from IL-2). Do NOT touch BLOCK A
    (no-console error) — that error stays enforced everywhere else.

    WRITE `tests/shared/debug-log.test.ts`. Single import block
    (`node:test` + `node:assert/strict` + the function under test from the
    relative path `../../extensions/pi-claude-marketplace/shared/debug-log.ts`).
    Use `t.mock.method(console, "error")` (the `node:test` runtime mock
    primitive used by other tests under `tests/shared/`) so the mock auto-
    restores at test end. Env-var save/restore uses try/finally with the
    captured `original = process.env.PI_CLAUDE_MARKETPLACE_DEBUG` value
    restored verbatim (including `undefined`) — `delete process.env.X`
    when unset, otherwise assign the captured value back.
  </action>
  <verify>
    <automated>node --test tests/shared/debug-log.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `extensions/pi-claude-marketplace/shared/debug-log.ts` exists and exports `hookDebugLog`.
    - `node --test tests/shared/debug-log.test.ts` reports "pass 3" (one per behavior case).
    - `grep -c "hookDebugLog" extensions/pi-claude-marketplace/shared/debug-log.ts` == 1 (single export).
    - `grep -n "extensions/pi-claude-marketplace/shared/debug-log.ts" eslint.config.js` returns at least one match inside an override block.
    - `npm run lint -- extensions/pi-claude-marketplace/shared/debug-log.ts` is GREEN.
    - `npm run typecheck` is GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retire the Phase 57 hookDebugLog stub and rewire callers</name>
  <files>
    extensions/pi-claude-marketplace/domain/components/hooks.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (full file — confirm the stub is at lines 150-172, the three call sites are at lines 186, 193, 207; verify the import block at the top so the new `import { hookDebugLog } from "../../shared/debug-log.ts";` lands in the right alphabetical group)
    - extensions/pi-claude-marketplace/shared/debug-log.ts (created in Task 1 — the new import target)
    - .planning/phases/59-bridge-dispatch-core-debug-seam/59-RESEARCH.md lines 846-857 (caller rewrite spec — confirms only the three callers at 186/193/207 exist; `grep -rn "hookDebugLog" extensions/` returns 5 hits in `hooks.ts` only)
    - .claude/rules/typescript-comments.md (comment hygiene: drop the Phase 57 `TODO(OBS-01)` block — the seam is at its canonical home and the TODO is satisfied; the file header at lines 20-30 mentions the hand-off seam in domain language — keep that text but drop the "ships a stub" narrative)
  </read_first>
  <behavior>
    Source-side changes in `domain/components/hooks.ts`:
    - DELETE the JSDoc block at lines 153-166 (the `OBS-01 hand-off seam`
      doc + the `TODO(OBS-01)` block).
    - DELETE the function definition at lines 167-172 (`export function hookDebugLog` body) including the inline `eslint-disable-next-line` directive.
    - ADD a new import line in the existing imports block at the top of the file (sorted alphabetically alongside other `../../shared/` imports if any; otherwise grouped with sibling shared imports): `import { hookDebugLog } from "../../shared/debug-log.ts";`.
    - The three call sites at the previous lines 186, 193, 207 (now renumbered after deletion) — `hookDebugLog(reason);` in the JSON-parse failure arm, the schema-validation failure arm, and the supportability failure arm — REMAIN UNCHANGED (same function name, same single-string argument).
    - The file-header doc-block at lines 20-30 mentions "hookDebugLog is the OBS-01 hand-off seam" — update the prose to say "hookDebugLog is the OBS-01 debug-output seam (imported from shared/debug-log.ts)"; drop the "this module ships a stub" sentence; do not introduce phase/plan refs per the comment policy.
    - No other line in the file changes. Symbol `hookDebugLog` no longer appears in the export surface of `hooks.ts` — only as a callee.

    Verification behavior:
    - `parseHooksConfig` still returns `{ok:false, reason}` for malformed input and still calls `hookDebugLog(reason)` exactly once on each failure arm.
    - Phase 57 / 58 tests in tests/architecture/hooks-foundation.test.ts and tests/architecture/hooks-supportability.test.ts stay GREEN (no regression).
    - `grep -c "^export function hookDebugLog\|^export const hookDebugLog" extensions/pi-claude-marketplace/domain/components/hooks.ts` returns 0 (stub gone).
    - `grep -c "hookDebugLog(" extensions/pi-claude-marketplace/domain/components/hooks.ts` returns 3 (the three call sites preserved).
  </behavior>
  <action>
    EDIT `extensions/pi-claude-marketplace/domain/components/hooks.ts`:
      1. Locate the import block at the top of the file (currently includes typebox + sibling-module imports). Add the line
         `import { hookDebugLog } from "../../shared/debug-log.ts";`
         in the appropriate alphabetical group (after other `../../shared/...` imports if present; otherwise before the first `./...` sibling import).
      2. Delete the JSDoc block immediately above the local `hookDebugLog` stub (the doc that opens with "OBS-01 hand-off seam" and closes with the `TODO(OBS-01)` paragraph) AND the function body itself (5-6 lines). The deletion spans exactly one JSDoc + one function definition.
      3. Verify the three remaining call sites (`hookDebugLog(reason);` in `parseHooksConfig`'s three failure arms) are unchanged. Do not touch the surrounding logic.
      4. Update the file-header doc-block prose around lines 20-30 to drop the "this module ships a stub" narrative and re-anchor the seam reference to shared/debug-log.ts. Allowed anchors: `OBS-01`, `D-59-05`, `IL-2`, `IL-3`. Forbidden: `Phase NN`, `Plan NN`, `Wave N`. (Comment-policy file is loaded above.)
      5. Confirm by spot-grep that no other file in `extensions/pi-claude-marketplace/` references the now-removed local `hookDebugLog` export (it was always file-private; the re-export surface is via the explicit `export` keyword on the stub, which is now gone).
  </action>
  <verify>
    <automated>node --test tests/architecture/hooks-foundation.test.ts tests/architecture/hooks-supportability.test.ts tests/domain/components/hooks.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -c "^export function hookDebugLog\|^export const hookDebugLog" extensions/pi-claude-marketplace/domain/components/hooks.ts` == 0.
    - `grep -c "from \"../../shared/debug-log.ts\"" extensions/pi-claude-marketplace/domain/components/hooks.ts` == 1.
    - `grep -c "hookDebugLog(" extensions/pi-claude-marketplace/domain/components/hooks.ts` == 3.
    - Phase 57 + 58 architecture tests GREEN (no regression).
    - `grep -c "TODO(OBS-01)\|TODO.OBS-01" extensions/pi-claude-marketplace/domain/components/hooks.ts` == 0 (TODO satisfied and removed).
    - `npm run typecheck` GREEN.
    - `npm run lint` GREEN — the previously-required inline `eslint-disable-next-line` directives in this file are gone with the stub.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch-path code → `console.error` | IL-2 / IL-3 forbids runtime UI noise; the dispatch path may surface diagnostic detail only through `shared/debug-log.ts`. Untrusted user-input hooks.json failures + supportability triplets land here; their content is operator-only (env-gated). |
| ESLint per-file override → broader codebase | The override authorizes `console.*` in one file ONLY (`shared/debug-log.ts`). Any drift of the files glob would silently re-open IL-2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01-01 | Information Disclosure | hookDebugLog output | mitigate | Output is gated on exact-equal `PI_CLAUDE_MARKETPLACE_DEBUG === "1"`; unit test fixtures `["0", "true", "", "yes", "ON", "01", " 1 "]` lock the gate so a future fuzzy-truthy refactor red-fails immediately. |
| T-59-01-02 | Tampering | ESLint per-file override scope | mitigate | The files glob is the single literal `"extensions/pi-claude-marketplace/shared/debug-log.ts"`. A glob-widening drift would surface in code review; the architecture test in Plan 03 pins the file's no-other-console contract at runtime via static introspection. |
| T-59-01-03 | Repudiation | hookDebugLog write to console.error | accept | Operator-opt-in via env var; the file is local; no audit trail expected. NFR-9 absolute-path redaction is NOT enforced at this seam (matches Phase 57/58 baseline — debug-mode operator already has filesystem access; revisit if a security review escalates). |
| T-59-01-SC | Tampering | npm/pip/cargo installs | accept | No new package dependencies introduced (no `npm install` step in this plan). The slopcheck baseline carries over from v1.12; no [ASSUMED]/[SUS] tokens emitted. |
</threat_model>

<verification>
- `node --test tests/shared/debug-log.test.ts` reports "pass 3".
- `node --test tests/architecture/hooks-foundation.test.ts tests/architecture/hooks-supportability.test.ts tests/domain/components/hooks.test.ts` GREEN — Phase 57/58 contracts preserved.
- `npm run typecheck` GREEN.
- `npm run lint` GREEN — the only new file permitted to call `console.*` is `shared/debug-log.ts` per the per-file override block.
- `npm run check` GREEN at plan close (full lint + typecheck + tests + format).
- `git diff --stat extensions/pi-claude-marketplace/domain/components/hooks.ts` shows a net negative line count (~10-15 lines removed; 1 import line added).
- `git diff --stat eslint.config.js` shows ~10 added lines (single override block).
</verification>

<success_criteria>
- `shared/debug-log.ts` exists, exports `hookDebugLog`, and is the sole new debug-output seam.
- `domain/components/hooks.ts` no longer carries the local stub; the three callers re-import from shared/debug-log.ts.
- `eslint.config.js` carries a per-file override block scoped to `shared/debug-log.ts` only.
- `npm run check` GREEN at plan close.
- The Phase 57 `TODO(OBS-01)` marker is removed from the codebase (verifiable via `grep -rn "TODO(OBS-01)" extensions/`).
</success_criteria>

<output>
Create `.planning/phases/59-bridge-dispatch-core-debug-seam/59-01-SUMMARY.md` when done.
</output>
