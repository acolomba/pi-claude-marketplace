---
phase: 111-non-hook-component-bridges
plan: 20
type: execute
wave: 2
depends_on: [111-07]
files_modified:
  - tests/bridges/mcp/stage.test.ts
  - tests/bridges/integration-materialization-gate.test.ts
autonomous: true
requirements: [MOD-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P111-20 maps exactly extensions/pi-claude-marketplace/bridges/mcp/stage.ts to tests/bridges/mcp/stage.test.ts; no owner pair is merged, split, duplicated, or omitted."
    - "MCP staging preserves merge, collision, substitution, atomic lifecycle, rollback, foreign content, and cross-bridge isolation. Baseline COVERAGE_FAIL is input only and never skips normalization (D-05)."
    - "Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06)."
    - "Production behavior and exports remain unchanged; no test-only export, reset hook, state reader, mode, private seam, or second production owner is introduced (D-08, D-09)."
  artifacts:
    - path: tests/bridges/mcp/stage.test.ts
      provides: "Canonical direct owner for extensions/pi-claude-marketplace/bridges/mcp/stage.ts"
  key_links:
    - from: tests/bridges/mcp/stage.test.ts
      to: extensions/pi-claude-marketplace/bridges/mcp/stage.ts
      via: "direct .ts import and public behavior or compile-time contract"
      pattern: "stage"
---

<objective>
Normalize and re-prove P111-20: MCP staging preserves merge, collision, substitution, atomic lifecycle, rollback, foreign content, and cross-bridge isolation.

Purpose: MOD-04 requires one accountable direct owner for this source, including PASS baselines and missing mirrors.
Output: A normalized tests/bridges/mcp/stage.test.ts with complete direct evidence and no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-non-hook-component-bridges/111-CONTEXT.md
@.planning/phases/111-non-hook-component-bridges/111-RESEARCH.md
@.planning/phases/111-non-hook-component-bridges/111-VALIDATION.md
@.planning/phases/111-non-hook-component-bridges/111-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the canonical mcp/stage owner</name>
  <files>tests/bridges/mcp/stage.test.ts</files>
  <action>Rewrite tests/bridges/mcp/stage.test.ts around a direct import of extensions/pi-claude-marketplace/bridges/mcp/stage.ts. Prove prepareStageMcpServers, commitPreparedMcp, and abortPreparedMcp for no-op and staged paths, source provenance, substitution and injected environment, collisions, foreign top-level/server entries, exact mcp.json bytes, and the MCP-family case from integration.test.ts; absorb AS-8 by proving an empty resolved set materializes no MCP output. Every meaningful input, expected complete value, filesystem tree, environment mutation, and cleanup belongs to its case. A concern-local allocator may return fresh paths but not a scenario or oracle (D-01, D-03, D-08, D-09). Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06). Preserve extensions/pi-claude-marketplace/bridges/mcp/stage.ts byte-for-byte and add no test-only surface.</action>
  <verify>
    <automated>node --test tests/bridges/mcp/stage.test.ts</automated>
  </verify>
  <done>MCP staging preserves merge, collision, substitution, atomic lifecycle, rollback, foreign content, and cross-bridge isolation. One direct public path is green with complete independent expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Close edge and direct-coverage evidence</name>
  <files>tests/bridges/mcp/stage.test.ts, tests/bridges/integration-materialization-gate.test.ts</files>
  <action>Prove replacePreparedMcp, rollbackMcpReplacement, and finalizeMcpReplacement for exact previous bytes, atomic replacement, malformed destination, foreign preservation, rollback/finalize outcomes, and cleanup; after Plan 111-07 has absorbed AS-9, rewrite integration-materialization-gate.test.ts to one case-local MCP-only case proving MCP output exists while agents-index, agents, prompts, and skills targets remain absent. Every meaningful input, expected complete value, filesystem tree, environment mutation, and cleanup belongs to its case. A concern-local allocator may return fresh paths but not a scenario or oracle (D-01, D-03, D-08, D-09). Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06). AS-8 and the MCP-family integration assertion become direct stage-owner evidence; the retained supplemental case proves only genuine MCP-to-sibling isolation per D-04. Do not modify extensions/pi-claude-marketplace/bridges/mcp/stage.ts, another owner test, or another production pair.</action>
  <verify>
    <automated>npm run typecheck && npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/stage.ts</automated>
  </verify>
  <done>P111-20 reaches complete direct function, line, and branch coverage or the explicit type-only result, with no skip, todo, exception, or production delta.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks completion.

## Trust Boundaries

| Boundary                                                                                | Description                                                                                          |
| --------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| untrusted MCP declarations and mixed foreign mcp.json → atomic staged/replaced document | Exercise the existing local contract without widening production access or touching developer state. |

## STRIDE Threat Register

| Threat ID   | Category  | Component           | Severity | Disposition | Mitigation Plan                                                                                                                                                                                         |
| ----------- | --------- | ------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-111-20-01 | Tampering | MCP stage lifecycle | high     | mitigate    | Use hostile case-local inputs and complete independent outcomes to prove MCP staging preserves merge, collision, substitution, atomic lifecycle, rollback, foreign content, and cross-bridge isolation. |

Canon-security breadcrumb: path, symlink, collision, foreign-content, malformed-input, atomicity, and prototype-pollution concerns remain ASVS file/input-handling evidence for the secure-phase; no bespoke prohibition is minted.
</threat_model>

## Artifacts this phase produces

- Refines tests/bridges/mcp/stage.test.ts as the sole mirrored owner of extensions/pi-claude-marketplace/bridges/mcp/stage.ts.
- AS-8 and the MCP-family integration assertion become direct stage-owner evidence; the retained supplemental case proves only genuine MCP-to-sibling isolation per D-04.
- Creates no production symbol, test-only API, UI/AI contract, or external-API coverage artifact; MCP files configure local definitions.

<verification>
Run node --test tests/bridges/mcp/stage.test.ts, npm run typecheck, and npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/stage.ts. The owner must pass alone with complete evidence.
</verification>

<success_criteria>

- P111-20 appears exactly once and owns exactly one production source-test pair.
- MCP staging preserves merge, collision, substitution, atomic lifecycle, rollback, foreign content, and cross-bridge isolation.
- Baseline COVERAGE_FAIL does not bypass normalization; production remains unchanged.
- No unmitigated high-severity threat remains.
  </success_criteria>

<output>
Create .planning/phases/111-non-hook-component-bridges/111-20-SUMMARY.md when done.
</output>
