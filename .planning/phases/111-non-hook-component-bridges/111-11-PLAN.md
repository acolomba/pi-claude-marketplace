---
phase: 111-non-hook-component-bridges
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/commands/index.test.ts
autonomous: true
requirements: [MOD-04]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P111-11 maps exactly extensions/pi-claude-marketplace/bridges/commands/index.ts to tests/bridges/commands/index.test.ts; no owner pair is merged, split, duplicated, or omitted."
    - "The commands barrel exposes exactly the intended runtime bindings and opaque lifecycle types. Baseline MISSING is input only and never skips normalization (D-05)."
    - "Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06)."
    - "Production behavior and exports remain unchanged; no test-only export, reset hook, state reader, mode, private seam, or second production owner is introduced (D-08, D-09)."
  artifacts:
    - path: tests/bridges/commands/index.test.ts
      provides: "Canonical direct owner for extensions/pi-claude-marketplace/bridges/commands/index.ts"
  key_links:
    - from: tests/bridges/commands/index.test.ts
      to: extensions/pi-claude-marketplace/bridges/commands/index.ts
      via: "direct .ts import and public behavior or compile-time contract"
      pattern: "strictEqual|satisfies"
---

<objective>
Normalize and re-prove P111-11: The commands barrel exposes exactly the intended runtime bindings and opaque lifecycle types.

Purpose: MOD-04 requires one accountable direct owner for this source, including PASS baselines and missing mirrors.
Output: A new tests/bridges/commands/index.test.ts with complete direct evidence and no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-non-hook-component-bridges/111-CONTEXT.md
@.planning/phases/111-non-hook-component-bridges/111-RESEARCH.md
@.planning/phases/111-non-hook-component-bridges/111-VALIDATION.md
@.planning/phases/111-non-hook-component-bridges/111-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the canonical commands/index owner</name>
  <files>tests/bridges/commands/index.test.ts</files>
  <action>Create tests/bridges/commands/index.test.ts around a direct import of extensions/pi-claude-marketplace/bridges/commands/index.ts. Create the barrel owner and prove identity for discoverPluginCommands, abortPreparedCommands, commitPreparedCommands, finalizeCommandsReplacement, prepareStageCommands, replacePreparedCommands, rollbackCommandsReplacement, and unstagePluginCommands against their defining modules. Author expected types and bindings independently and do not widen production exports. Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06). Preserve extensions/pi-claude-marketplace/bridges/commands/index.ts byte-for-byte and add no test-only surface.</action>
  <verify>
    <automated>node --test tests/bridges/commands/index.test.ts</automated>
  </verify>
  <done>The commands barrel exposes exactly the intended runtime bindings and opaque lifecycle types. One direct public path is green with complete independent expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Close edge and direct-coverage evidence</name>
  <files>tests/bridges/commands/index.test.ts</files>
  <action>Import CommandsReplacement and PreparedCommandsStaging through the barrel in positive compiler expressions, prove opaque-handle narrowing, and finish the intended closed public surface without adding exports. Author expected types and bindings independently and do not widen production exports. Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06). Do not modify extensions/pi-claude-marketplace/bridges/commands/index.ts, another owner test, or another production pair.</action>
  <verify>
    <automated>npm run typecheck && npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/commands/index.ts</automated>
  </verify>
  <done>P111-11 reaches complete direct function, line, and branch coverage or the explicit type-only result, with no skip, todo, exception, or production delta.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks completion.

## Trust Boundaries

| Boundary                                                      | Description                                                                                          |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| defining bridge modules → orchestrator-facing commands barrel | Exercise the existing local contract without widening production access or touching developer state. |

## STRIDE Threat Register

| Threat ID   | Category  | Component                        | Severity | Disposition | Mitigation Plan                                                                           |
| ----------- | --------- | -------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-111-11-01 | Tampering | commands/index.ts public surface | low      | accept      | No runtime boundary is added; direct binding or compiler evidence detects contract drift. |

Canon-security breadcrumb: path, symlink, collision, foreign-content, malformed-input, atomicity, and prototype-pollution concerns remain ASVS file/input-handling evidence for the secure-phase; no bespoke prohibition is minted.
</threat_model>

## Artifacts this phase produces

- Creates tests/bridges/commands/index.test.ts as the sole mirrored owner of extensions/pi-claude-marketplace/bridges/commands/index.ts.
- No supplemental suite write belongs to this plan.
- Creates no production symbol, test-only API, UI/AI contract, or external-API coverage artifact; MCP files configure local definitions.

<verification>
Run node --test tests/bridges/commands/index.test.ts, npm run typecheck, and npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/commands/index.ts. The owner must pass alone with complete evidence.
</verification>

<success_criteria>

- P111-11 appears exactly once and owns exactly one production source-test pair.
- The commands barrel exposes exactly the intended runtime bindings and opaque lifecycle types.
- Baseline MISSING does not bypass normalization; production remains unchanged.
- No unmitigated high-severity threat remains.
  </success_criteria>

<output>
Create .planning/phases/111-non-hook-component-bridges/111-11-SUMMARY.md when done.
</output>
