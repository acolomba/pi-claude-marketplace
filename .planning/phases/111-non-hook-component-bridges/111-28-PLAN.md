---
phase: 111-non-hook-component-bridges
plan: 28
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/skills/rewrite-frontmatter.test.ts
autonomous: true
requirements: [MOD-04]
estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P111-28 maps exactly extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts to tests/bridges/skills/rewrite-frontmatter.test.ts; no owner pair is merged, split, duplicated, or omitted."
    - "Skill name rewriting preserves exact frontmatter/body bytes while replacing or inserting the intended name and rejecting mismatches. Baseline COVERAGE_FAIL is input only and never skips normalization (D-05)."
    - "Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06)."
    - "Production behavior and exports remain unchanged; no test-only export, reset hook, state reader, mode, private seam, or second production owner is introduced (D-08, D-09)."
  artifacts:
    - path: tests/bridges/skills/rewrite-frontmatter.test.ts
      provides: "Canonical direct owner for extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts"
  key_links:
    - from: tests/bridges/skills/rewrite-frontmatter.test.ts
      to: extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts
      via: "direct .ts import and public behavior or compile-time contract"
      pattern: "rewrite-frontmatter"
---

<objective>
Normalize and re-prove P111-28: Skill name rewriting preserves exact frontmatter/body bytes while replacing or inserting the intended name and rejecting mismatches.

Purpose: MOD-04 requires one accountable direct owner for this source, including PASS baselines and missing mirrors.
Output: A normalized tests/bridges/skills/rewrite-frontmatter.test.ts with complete direct evidence and no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-non-hook-component-bridges/111-CONTEXT.md
@.planning/phases/111-non-hook-component-bridges/111-RESEARCH.md
@.planning/phases/111-non-hook-component-bridges/111-VALIDATION.md
@.planning/phases/111-non-hook-component-bridges/111-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the canonical skills/rewrite-frontmatter owner</name>
  <files>tests/bridges/skills/rewrite-frontmatter.test.ts</files>
  <action>Rewrite tests/bridges/skills/rewrite-frontmatter.test.ts around a direct import of extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts. Prove rewriteFrontmatterName replaces an existing scalar name, inserts a missing name at the canonical position, and preserves the complete remaining document bytes. Every meaningful input, expected complete value, filesystem tree, environment mutation, and cleanup belongs to its case. A concern-local allocator may return fresh paths but not a scenario or oracle (D-01, D-03, D-08, D-09). Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06). Preserve extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts byte-for-byte and add no test-only surface.</action>
  <verify>
    <automated>node --test tests/bridges/skills/rewrite-frontmatter.test.ts</automated>
  </verify>
  <done>Skill name rewriting preserves exact frontmatter/body bytes while replacing or inserting the intended name and rejecting mismatches. One direct public path is green with complete independent expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Close edge and direct-coverage evidence</name>
  <files>tests/bridges/skills/rewrite-frontmatter.test.ts</files>
  <action>Close quoted, folded, literal, and multiline name-node replacement, CRLF/newline preservation, malformed frontmatter, missing block, parser mismatch, unsafe replacement text, and exact failure outcomes. Every meaningful input, expected complete value, filesystem tree, environment mutation, and cleanup belongs to its case. A concern-local allocator may return fresh paths but not a scenario or oracle (D-01, D-03, D-08, D-09). Every runtime callback uses lowercase // arrange, blank line, // act, blank line, // assert. Use // act & assert only for one assert.throws or assert.rejects expression; data rows keep separate phases (D-06). Do not modify extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts, another owner test, or another production pair.</action>
  <verify>
    <automated>npm run typecheck && npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts</automated>
  </verify>
  <done>P111-28 reaches complete direct function, line, and branch coverage or the explicit type-only result, with no skip, todo, exception, or production delta.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks completion.

## Trust Boundaries

| Boundary                                                     | Description                                                                                          |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| plugin-authored skill frontmatter → rewritten generated name | Exercise the existing local contract without widening production access or touching developer state. |

## STRIDE Threat Register

| Threat ID   | Category  | Component              | Severity | Disposition | Mitigation Plan                                                                                                                                                                                               |
| ----------- | --------- | ---------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-111-28-01 | Tampering | rewriteFrontmatterName | medium   | mitigate    | Use hostile case-local inputs and complete independent outcomes to prove Skill name rewriting preserves exact frontmatter/body bytes while replacing or inserting the intended name and rejecting mismatches. |

Canon-security breadcrumb: path, symlink, collision, foreign-content, malformed-input, atomicity, and prototype-pollution concerns remain ASVS file/input-handling evidence for the secure-phase; no bespoke prohibition is minted.
</threat_model>

## Artifacts this phase produces

- Refines tests/bridges/skills/rewrite-frontmatter.test.ts as the sole mirrored owner of extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts.
- No supplemental suite write belongs to this plan.
- Creates no production symbol, test-only API, UI/AI contract, or external-API coverage artifact; MCP files configure local definitions.

<verification>
Run node --test tests/bridges/skills/rewrite-frontmatter.test.ts, npm run typecheck, and npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/skills/rewrite-frontmatter.ts. The owner must pass alone with complete evidence.
</verification>

<success_criteria>

- P111-28 appears exactly once and owns exactly one production source-test pair.
- Skill name rewriting preserves exact frontmatter/body bytes while replacing or inserting the intended name and rejecting mismatches.
- Baseline COVERAGE_FAIL does not bypass normalization; production remains unchanged.
- No unmitigated high-severity threat remains.
  </success_criteria>

<output>
Create .planning/phases/111-non-hook-component-bridges/111-28-SUMMARY.md when done.
</output>
