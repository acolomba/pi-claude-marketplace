---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 05
type: execute
wave: 3
depends_on: [63-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/commands/plugin/info.test.ts
autonomous: true
requirements: [SURF-01]
must_haves:
  truths:
    - "`info <plugin>` for an INSTALLABLE plugin with `hooks/hooks.json` renders a multi-line `hooks:` block per D-63-04 — header at 4-space indent, entries at 6-space indent, ordered by declaration order from the parsed file."
    - "The `hooks:` block slots alphabetically between `commands:` and `mcp:` (D-63-07 / Phase 63-01 5-tuple)."
    - "`info <plugin>` for an UNAVAILABLE plugin still renders `components: not resolved` per the existing v1.0+ unavailable-row contract — NO hooks line."
    - "info.ts re-parses `<pluginRoot>/hooks/hooks.json` from disk via the existing `parseHooksConfig` (Pitfall 6 — resolver discards parsed value); ENOENT / parse failure routes through the existing `narrowProbeError` ladder unchanged (RESEARCH Open Question 3 recommendation)."
    - "info.ts projects parsed `HooksConfig` → `readonly HookSummaryEntry[]` exactly once at message-construction time; no string re-derivation at render time (Pitfall 5)."
    - "Inline handler-field flags (`asyncRewake`, `timeout`, `if`, `args`, `shell`, `rewakeMessage`, `rewakeSummary`) are NEVER rendered — D-63-05 explicitly omits them."
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
      provides: "composeResolvedComponents emits components.hooks?: readonly HookSummaryEntry[]; projectHookSummaryEntries projection helper"
    - path: tests/commands/plugin/info.test.ts
      provides: "multi-line hooks: block byte-form fixture + unavailable-plugin no-hooks-line fixture + alphabetical-slot fixture"
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
      to: extensions/pi-claude-marketplace/domain/components/hooks.ts
      via: "parseHooksConfig re-parse of hooks.json"
      pattern: "parseHooksConfig"
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
      to: extensions/pi-claude-marketplace/shared/notify.ts
      via: "HookSummaryEntry type + components.hooks? carrier field (from Plan 63-01)"
      pattern: "HookSummaryEntry"
---

<objective>
Wire the `info <plugin>` command to surface a multi-line `hooks:` block per D-63-04 by extending `composeResolvedComponents` in `orchestrators/plugin/info.ts` to re-parse `<pluginRoot>/hooks/hooks.json` (Pitfall 6) and project entries to the `readonly HookSummaryEntry[]` carrier defined in Plan 63-01. Closes SURF-01.

Purpose: `info` is a read-only surface; this plan completes the user-visible loop for the typed `HookSummary` model — Plan 63-01 added the types and renderer arm, Plan 63-04 wired the install cascade, and this plan provides the payload. The renderer arm (Plan 63-01) already emits the multi-line block; this plan only supplies the `components.hooks?` field on the message-type payload. RESEARCH Open Question 3 (info-time re-parse failure classification) is resolved by reusing the existing `narrowProbeError` ladder unchanged.

Output: `orchestrators/plugin/info.ts` gains a hooks branch in `composeResolvedComponents`; `tests/commands/plugin/info.test.ts` extends with the block byte-form and alphabetical-slot fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-CONTEXT.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md
@extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
@extensions/pi-claude-marketplace/domain/components/hooks.ts
@extensions/pi-claude-marketplace/domain/components/hook-events.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/domain/resolver.ts
</context>

# Artifacts this phase produces

- New hooks branch in `info.ts::composeResolvedComponents`.
- New local helper `projectHookSummaryEntries(parsed: HooksConfig): readonly HookSummaryEntry[]`.
- `composeResolvedComponents` return-type union grows `hooks?: readonly HookSummaryEntry[]`.
- `composeResolvedComponents` accepts the resolved-plugin's `hooksConfigPath` (already on `ResolvedPlugin` after Phase 57 D-57-03).
- New fixture cases in `tests/commands/plugin/info.test.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pluginRoot/hooks/hooks.json → info renderer | The renderer re-opens the on-disk file at info time — same chokepoint as install-time hydrate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-05-RP | Tampering | info-time hooks.json re-parse | mitigate | Reuse `parseHooksConfig` (HOOKS_VALIDATOR JIT + supportability gate); reuse `narrowProbeError` ladder unchanged for ENOENT / EACCES / SyntaxError classification (Open Question 3). |
| T-63-05-DE | Information disclosure (mild) | rendered handler fields | accept | D-63-05 explicitly omits inline rendering of `asyncRewake`, `timeout`, `if`, `args`, `shell`, `rewakeMessage`, `rewakeSummary`. The renderer emits ONLY `event(matcher)` or bare `event` — no info-time leak of handler internals. |
| T-63-05-PI | Information disclosure | pluginRoot path leak in error messages | mitigate | The `narrowProbeError` ladder already redacts probe-error paths to closed-set REASONS — info.ts inherits the existing chokepoint. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: composeResolvedComponents — hooks branch + projection helper</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (full file ~1-220 — locate `composeResolvedComponents` at lines 189-222 + its callers; note the existing `narrowProbeError` use site for ENOENT / parse-failure on the OTHER component kinds)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (parseHooksConfig + HooksConfig + HookHandlerEntry types; the parse-result discriminated union)
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (TOOL_EVENTS tuple + ToolEvent type — used by the projector to discriminate tool vs non-tool events)
    - extensions/pi-claude-marketplace/shared/notify.ts (HookSummaryEntry from Plan 63-01)
    - extensions/pi-claude-marketplace/domain/resolver.ts (ResolvedPlugin.hooksConfigPath — Phase 57 D-57-03)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (info.ts::composeResolvedComponents section, lines 166-191 — the binding sketch)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Pitfall 6 — info.ts must re-parse from disk; Pitfall 7 — components ordering; Open Question 3 — narrowProbeError reused)
  </read_first>
  <behavior>
    - Test 1: An installable plugin with `hooks/hooks.json` declaring two PreToolUse groups (matcher `"Bash"` and matcher `"Edit|Write"`) and one PostToolUse group (matcher `"Edit"`) and one SessionStart group produces `components.hooks` equal to:
      ```
      [
        { event: "PreToolUse", matcher: "Bash" },
        { event: "PreToolUse", matcher: "Edit|Write" },
        { event: "PostToolUse", matcher: "Edit" },
        { event: "SessionStart" },
      ]
      ```
      (declaration order preserved; tool events carry `matcher`; non-tool events do NOT).
    - Test 2: The rendered output of the message above contains the multi-line block at the alphabetical slot between `commands` and `mcp` (between any rendered `commands: ...` line and any rendered `mcp: ...` line).
    - Test 3: An unavailable plugin (`installable: false`) renders `components: not resolved` and NO `hooks:` line, regardless of whether `hooks/hooks.json` exists.
    - Test 4: A plugin where `hooksConfigPath` is undefined (no hooks at all) renders NO `hooks:` line; the rendered output contains exactly the `agents`/`commands`/`mcp`/`skills` lines that already render.
    - Test 5: A plugin where reading `hooks/hooks.json` fails with ENOENT (file was deleted between resolve and info) routes through `narrowProbeError` to the existing `(installed) {unreadable}` or equivalent closed-set REASON — the info renderer surfaces it through the same path as other component probe failures (no new REASON, no new code path).
  </behavior>
  <action>
    1. In `info.ts`, add a private `projectHookSummaryEntries(parsed: HooksConfig): readonly HookSummaryEntry[]` helper. The helper iterates `parsed` in declaration order (`Object.entries` may NOT preserve order — use the parsed shape's iteration contract from `domain/components/hooks.ts`; if the schema lacks a stable iteration order, build entries from a known-ordered source like the parse-result's preserved group list). For each handler entry under each event:
       - If `event in TOOL_EVENTS` (i.e., the event literal is one of `"PreToolUse"`, `"PostToolUse"`, `"PostToolUseFailure"`), push `{ event, matcher: group.matcher }`.
       - Otherwise push `{ event }` (the discriminated arm without matcher).
       - One entry per (event, group) combination — declaration order from the parsed shape; NOT per-handler aggregation. (Re-read Pitfall 6 to confirm the projection's granularity is "group", not "handler" — the renderer surfaces `event(matcher)` once per group.)

       Note: the exact projection granularity (group-level vs handler-level) MUST match the integration test fixtures in `tests/commands/plugin/info.test.ts`. The fixtures in this plan use group-level. If the parsed shape requires a different granularity, document the choice in the helper's doc-comment with the D-63-04 decision-ID anchor.

    2. In `composeResolvedComponents` (lines 189-222), add the hooks branch per PATTERNS lines 166-191:
       - Add an outer `let hooks: readonly HookSummaryEntry[] | undefined;` declaration.
       - Wrap the file read in a `try` block: `if (resolved.hooksConfigPath !== undefined) { const raw = await readFile(path.join(resolved.pluginRoot, resolved.hooksConfigPath), "utf8"); const parsed = parseHooksConfig(raw); if (parsed.ok) { hooks = projectHookSummaryEntries(parsed.value); } }`.
       - In the `catch` block: route the error through `narrowProbeError(err, ...)` exactly as the existing `agents` / `commands` / `mcp` / `skills` probe-failure branches do — no new REASON tokens (Open Question 3).

    3. Extend the function's return shape (the object spread at the bottom of `composeResolvedComponents` per PATTERNS lines 182-188): add `...(hooks !== undefined && hooks.length > 0 && { hooks })`. The object-literal key order does NOT affect rendering (the renderer iterates `COMPONENT_KINDS` from Plan 63-01) — but for source-readability place `hooks` between `commands` and `mcp` (Pitfall 7).

    4. The function signature's `resolved` parameter type must already carry `hooksConfigPath?: string` from Phase 57 D-57-03; verify and add the field to the type if missing. If the type lives in a separate interface, the executor updates only what's needed to thread the field through the existing call sites.

    5. Imports: add `readFile` from `node:fs/promises` if not present; add `parseHooksConfig` from `../../domain/components/hooks.ts`; add `HookSummaryEntry` from `../../shared/notify.ts`; add `TOOL_EVENTS` from `../../domain/components/hook-events.ts` (for the projection's tool-event discriminator).

    Do NOT introduce inline rendering of handler fields (D-63-05). Do NOT add a new REASON for info-time hooks read failures (Open Question 3: reuse `narrowProbeError` unchanged). Do NOT route info-time errors through `ctx.ui.notify` (IL-2; the existing info renderer aggregates errors into the message payload).
  </action>
  <verify>
    <automated>npm test -- tests/commands/plugin/info.test.ts</automated>
  </verify>
  <done>
    `composeResolvedComponents` reads `<pluginRoot>/<resolved.hooksConfigPath>` (or skips if `hooksConfigPath` undefined); parses via `parseHooksConfig`; projects to `HookSummaryEntry[]` in declaration order; returns `hooks?: readonly HookSummaryEntry[]` on the components object; routes probe errors through `narrowProbeError` unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/commands/plugin/info.test.ts — multi-line block + alphabetical slot + unavailable contract</name>
  <files>tests/commands/plugin/info.test.ts</files>
  <read_first>
    - tests/commands/plugin/info.test.ts (existing fixture conventions; locate any existing `unavailable` / `installable: false` test to mirror)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (the changes from Task 1)
    - extensions/pi-claude-marketplace/shared/notify.ts (the `kind === "hooks"` renderer arm — the test reads the rendered byte form)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-VALIDATION.md (Wave 0 info.test.ts entry)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Pitfall 6 + Pitfall 7)
  </read_first>
  <behavior>
    - Cover Test 1..5 from Task 1 `<behavior>` block.
    - The byte-form assertion uses string equality (or `assert.deepEqual` on lines array) so the multi-line block bytes are pinned exactly.
    - The alphabetical-slot assertion extracts the `hooks:` line index from the rendered output, then asserts it falls between the `commands:` line index (if any) and the `mcp:` line index (if any).
  </behavior>
  <action>
    1. Add a fixture builder helper to construct a synthetic `<pluginRoot>/hooks/hooks.json` file in a `mkdtemp` location; reuse the existing info-test harness's mock resolver to set `resolved.hooksConfigPath = "hooks/hooks.json"` and `resolved.pluginRoot = <tmpdir>`.

    2. Implement Test 1..5 from `<behavior>`. The "no hooks" case (Test 4) shares the same builder but sets `hooksConfigPath = undefined`; the unavailable case (Test 3) uses the existing `installable: false` fixture path with NO mock disk write.

    3. The ENOENT case (Test 5) creates the pluginRoot WITHOUT the hooks.json file (or `unlink`s it after the resolver runs) and asserts the rendered output contains the existing closed-set REASON form for unreadable probes — extract the expected token from the `narrowProbeError` test suite if needed.

    4. Do NOT add a new REASON token or new closed-set member for info-time hooks errors.
  </action>
  <verify>
    <automated>npm test -- tests/commands/plugin/info.test.ts</automated>
  </verify>
  <done>
    `tests/commands/plugin/info.test.ts` covers the 5 fixture cases; the multi-line block byte form is pinned; the alphabetical slot assertion passes; the unavailable-plugin contract is preserved.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/commands/plugin/info.test.ts` green.
- `npm run check` green from a clean checkout.
- `grep -n "parseHooksConfig\\|HookSummaryEntry\\|projectHookSummaryEntries" extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` shows the new branch + helper + import.
- `grep -c "asyncRewake\\|rewakeMessage\\|rewakeSummary\\|timeout:" extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` returns 0 in NEW code (D-63-05: no inline handler-field rendering).
- `grep -c "narrowProbeError" extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` returns at least 1 (Open Question 3: the existing ladder is reused, not extended).
</verification>

<success_criteria>
- SURF-01 satisfied: `info <plugin>` for an installable plugin with hooks renders the multi-line block per D-63-04; the block slots alphabetically between `commands` and `mcp` (driven by Plan 63-01's 5-tuple); unavailable plugins still render `components: not resolved`.
- Pitfall 5 averted: the renderer reads `HookSummaryEntry.event` / `.matcher` directly — no runtime string re-derivation; the projection happens ONCE at message-construction.
- Pitfall 6 averted: info.ts re-parses from disk (resolver discards parsed value); the existing parser is reused.
- Pitfall 7 averted: object-literal field placement is documentation-only; rendering order is enforced by `COMPONENT_KINDS` from Plan 63-01.
- Open Question 3 resolved: `narrowProbeError` ladder is reused unchanged; no new failure-classification surface.
- D-63-05 respected: NO inline handler-field flags rendered.
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-05-SUMMARY.md` when done. Summary records: the `composeResolvedComponents` branch's file:line; the projection helper's location; the 5 fixture cases; and a one-line "Wave 3 user-visible surface complete" handoff.
</output>
