---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 04
type: execute
wave: 3
depends_on: [63-02, 63-03]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/transaction/lifecycle-cascade.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02]
scope_note: |
  Scope acknowledgement (per checker review): 4 tasks edit 8 files across
  3 hand-rolled orchestrators (install.ts, update.ts, reinstall.ts) +
  shared.ts. This plan stays UNSPLIT because the 4 sites share an
  identical cascade-shape contract (hooks slot BETWEEN agents and mcp,
  per D-63-01) and any divergence is a real LIFE-01 / LIFE-02 regression.
  Splitting along the install / update boundary would force the
  integration test in Task 4 (`lifecycle-cascade.test.ts`) to either
  duplicate across both sub-plans (drift risk) or sit in only one of
  them (coverage gap until the second sub-plan lands). Mitigation:
  the executor commits each of the 4 site edits as its own commit
  inside the plan so a partial failure is a single revert, not a
  surgery on a merged blob.
must_haves:
  truths:
    - "`install.ts` literal Phase array becomes 6-element: `[skillsPhase, commandsPhase, agentsPhase, hooksPhase, mcpPhase, statePhase]` — hooks slot 4, between agents and mcp, per D-63-01."
    - "`update.ts` hand-rolled Phase 3a commit loop gains a `commitHookConfig`-style call (via `writeHookConfig`) between `commitPreparedAgents` and `commitPreparedMcp`; the prepare loop is NOT modified (hooks bridge has no staging dir per D-63-02)."
    - "`reinstall.ts` hand-rolled parallel-prepare/commit gains the same hooks slot between agents and mcp."
    - "`cascadeUnstagePlugin` in `orchestrators/marketplace/shared.ts` inserts a `removeHookConfig` call between the agents foreign-content guard and mcp; `UnstageOutcome.dropped` grows a `readonly hooks: readonly string[]` field between `agents` and `mcpServers` (declaration order matches cascade order)."
    - "Install row composition reads `resolved.orphanRewake` and pushes `\"orphan rewake\"` into the install-cascade row's `reasons[]` — exactly ONE reason per plugin, no per-handler aggregation."
    - "Install/uninstall of a plugin with hooks emits a single `NotificationMessage` plugin row through the existing v1.4 cascade AND triggers the existing reload-hint trailer — NO new top-level notify pattern, NO new state-change tokens (LIFE-02)."
    - "The post-state-commit `addInstalledPluginHooksToCache` hydrate path at `install.ts:331-361` is unchanged — the new `writeHookConfig` site closes the loop the hydrate already expects."
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
      provides: "hooksPhase Phase<InstallCtx>; 6-element phases array; orphan-rewake reason wiring; InstallCtx.hooksFileWritten flag"
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
      provides: "5th cascade slot in hand-rolled Phase 3a commit loop"
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
      provides: "5th cascade slot in hand-rolled parallel-prepare/commit"
    - path: extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
      provides: "cascadeUnstagePlugin hooks unstage between agents and mcp; UnstageOutcome.dropped.hooks field"
    - path: tests/transaction/lifecycle-cascade.test.ts
      provides: "integration test exercising all 4 sites with hooks present"
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/stage.ts
      via: "hooksPhase.do calls writeHookConfig; hooksPhase.undo calls removeHookConfig"
      pattern: "writeHookConfig\\|removeHookConfig"
    - from: extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/stage.ts
      via: "cascadeUnstagePlugin calls removeHookConfig"
      pattern: "removeHookConfig"
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
      to: extensions/pi-claude-marketplace/domain/resolver.ts
      via: "reads resolved.orphanRewake (set in Plan 63-03)"
      pattern: "orphanRewake"
---

<objective>
Wire the 5th cascade slot into ALL FOUR orchestrator sites (install.ts literal-array, update.ts hand-rolled Phase 3a commit loop, reinstall.ts hand-rolled parallel-prepare/commit, `cascadeUnstagePlugin` in marketplace/shared.ts), and connect Plan 63-03's `resolved.orphanRewake` flag to the install-cascade row's `reasons[]` so the `(installed) {orphan rewake}` token surfaces through the existing v1.4 `NotificationMessage` cascade. Closes LIFE-01 and LIFE-02.

Purpose: This is the LARGEST mechanical surface in Phase 63. The trap is that `update.ts` (Phase 3a commit loop around `commitPreparedSkills` / `commitPreparedCommands` / `commitPreparedAgents` / `commitPreparedMcp` near update.ts:1262-1298) and `reinstall.ts` (hand-rolled parallel-prepare/commit) use HAND-ROLLED cascades — not `runPhases` — so update/reinstall would silently drop hooks data if only `install.ts` were touched. The plan tasks are organized PER FILE (not per flow) so a planner sweep cannot miss a site.

Scope risk acknowledgement: see the `scope_note` in frontmatter. The 4 sites are coupled enough that splitting would create coordination overhead; the executor commits each site separately within this plan so a partial failure is a single revert.

Output: 4 source-file edits + 4 test-file edits (3 existing orchestrator tests modified, 1 new integration test), all landing under a single `npm run check` GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-CONTEXT.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-VALIDATION.md
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
@extensions/pi-claude-marketplace/bridges/hooks/stage.ts
@extensions/pi-claude-marketplace/bridges/hooks/index.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/domain/resolver.ts
</context>

# Artifacts this phase produces

- 6-element `phases` literal-array in `install.ts` (was 5-element).
- `hooksPhase: Phase<InstallCtx>` definition in `install.ts` with `do` (parse-then-write) and `undo` (rm if written) bodies.
- `InstallCtx.hooksFileWritten: boolean` flag.
- 5th cascade slot in `update.ts` Phase 3a commit loop.
- 5th cascade slot in `reinstall.ts` parallel-prepare/commit.
- `UnstageOutcome.dropped.hooks: readonly string[]` field + `removeHookConfig` call in `cascadeUnstagePlugin`.
- `reasons.push("orphan rewake")` site in `install.ts` cascade row composition (gated on `resolved.orphanRewake === true`).
- New integration test `tests/transaction/lifecycle-cascade.test.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pluginRoot → bridge writer | Plan 63-02's `writeHookConfig` is the chokepoint; this plan only wires the call sites. |
| state.json + hooks.json + addInstalledPluginHooksToCache | Three independent state surfaces must stay coherent across install / update / reinstall / uninstall. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-COH | Tampering | hooks.json on-disk vs state.resources.hooks vs bridge cache | mitigate | Phase ordering: hooksPhase runs BEFORE statePhase (D-63-01); statePhase records `resources.hooks` on success; `addInstalledPluginHooksToCache` hydrates AFTER state commit (install.ts:331-361 unchanged). On hooksPhase.undo, the file is removed and statePhase never commits the inventory entry (Phase ledger unwind). |
| T-63-04-CASCADE | Tampering | hand-rolled cascades in update.ts / reinstall.ts | mitigate | Tasks are organized PER FILE so all four sites are touched in lockstep. The integration test `tests/transaction/lifecycle-cascade.test.ts` exercises every site with a hooks-present fixture. |
| T-63-04-UNSTAGE | Tampering | UnstageOutcome.dropped shape stability | mitigate | The `hooks` field lands between `agents` and `mcpServers` in declaration order — every `Object.freeze` block (success + failure return paths at shared.ts:373-389) is updated in lockstep. |

This plan introduces NO direct disk I/O — every write goes through Plan 63-02's chokepoints.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: install.ts — hooksPhase definition + 6-element array + orphan-rewake reason wiring + InstallCtx field</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/install.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (lines 320-400 InstallCtx + addInstalledPluginHooksToCache; lines 577-800 the 5 Phase<InstallCtx> definitions + statePhase + the literal-array; lines 1010-1030 post-state-commit hydrate call; the cascade row composition site that builds `reasons[]` for the install message)
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts (writeHookConfig + removeHookConfig signatures from Plan 63-02)
    - extensions/pi-claude-marketplace/shared/notify.ts (REASONS tuple member `"orphan rewake"` from Plan 63-03)
    - extensions/pi-claude-marketplace/domain/resolver.ts (ResolvedPlugin.orphanRewake?: boolean from Plan 63-03; ResolvedPluginInstallableSchema.hooksConfigPath stores the relative path ONLY — the parsed value is discarded after `parseHooksConfig` succeeds; see resolver.ts:678 + 707)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (install.ts 5th cascade slot section)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Example 1 verbatim 6-element array; Pattern 1 hooksPhase.do sketch; the path-helper reuse note)
  </read_first>
  <behavior>
    - Test 1: `installPlugin` on a plugin whose manifest declares `hooks/hooks.json` writes the file at `<scopeRoot>/pi-claude-marketplace/hooks/<plugin>/hooks.json` after success.
    - Test 2: On a forced ledger unwind (force a downstream phase to throw), the hooks.json file is removed and the plugin's `state.resources.hooks` is NOT recorded.
    - Test 3: `installPlugin` on a plugin whose `hooks.json` carries `rewakeMessage` without `asyncRewake: true` emits a `NotificationMessage` whose plugin row carries `reasons: [..., "orphan rewake"]` (existing comma-join + brace composition produces `(installed) {orphan rewake}` in the rendered surface).
    - Test 4: `installPlugin` on a plugin with `asyncRewake: true` emits a `NotificationMessage` whose plugin row's `reasons` does NOT include `"orphan rewake"`.
    - Test 5: The Phase ledger order at the test seam is exactly `["skills", "commands", "agents", "hooks", "mcp", "state"]` (extract from `phases.map(p => p.name)`).
    - Test 6: `installPlugin` triggers the existing reload-hint cascade trailer (LIFE-02 — assertion uses the existing reload-hint fixture pattern).
  </behavior>
  <action>
    1. Add a `hooksFileWritten?: boolean;` field on the `InstallCtx` interface alongside the other "did I write?" flags. Match the mutability discipline of the sibling flags (`stagedAgentNames` / `stagedMcpServerNames`); the Phase `do` body writes the flag on success, and the `undo` body reads it.

    2. Add a new `hooksPhase: Phase<InstallCtx>` definition AFTER `agentsPhase` and BEFORE `mcpPhase` (file-visual order = cascade order):
       - `name: "hooks"`.
       - `do: async (c) => { ... }` body — unconditional re-parse pattern (no executor judgement). The resolver stores ONLY `hooksConfigPath` (the relative path) on `c.resolved`; the parsed `HooksConfig` value is discarded after the resolver's own `parseHooksConfig` call returns (see `domain/resolver.ts:678` + `707` — `partial.hooksConfigPath = hooksResult.relativePath`). To obtain the value the bridge needs, the phase body MUST re-read + re-parse the on-disk `hooks.json`. This mirrors the post-state-commit hydrate at `install.ts:340-360` (`readFile` → `parseHooksConfig(raw, ifCtx, compileIfPredicate)`), so the new phase body is structurally identical to existing in-file precedent.

         Body shape (no conditionals around the parse path):
         - early return if `c.resolved.hooksConfigPath === undefined` (plugin has no hooks);
         - `const raw = await readFile(path.join(c.resolved.pluginRoot, c.resolved.hooksConfigPath), "utf8");`
         - `const ifCtx = { homedir: homedir(), cwd: c.cwd, projectRoot: c.cwd };` (same shape as install.ts:351 — `cwd` doubles as `projectRoot`);
         - `const parsed = parseHooksConfig(raw, ifCtx, compileIfPredicate);`
         - if `!parsed.ok` throw a phase error carrying `parsed.reason` — this is a defensive guard; the resolver already validated the file at install-entry time, so a re-parse failure here is a true I/O fault and should unwind the ledger (D-57-04 parse-failure-flips-installable-false has already filtered out genuinely malformed configs upstream);
         - `await writeHookConfig({ locations: c.locations, pluginName: c.plugin, pluginRoot: c.resolved.pluginRoot, hooksValue: parsed.value });`
         - `c.hooksFileWritten = true;`.

       - `undo: async (c) => { if (c.hooksFileWritten !== true) return; await removeHookConfig({ locations: c.locations, pluginName: c.plugin }); }`.

    3. Insert `hooksPhase` into the `phases: readonly Phase<InstallCtx>[]` literal-array at index 3 (between `agentsPhase` and `mcpPhase`). Resulting array exactly mirrors the 63-RESEARCH.md Example 1 6-element layout:
       ```
       const phases: readonly Phase<InstallCtx>[] = [
         skillsPhase, commandsPhase, agentsPhase, hooksPhase, mcpPhase, statePhase,
       ];
       ```
       Per D-01 literal-array discipline — do NOT refactor to a dynamic builder.

    4. In the install-cascade row composition site that builds `reasons[]` (locate via `grep -n "reasons" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` and find the cascade-row builder — distinct from `MANIFEST_FIELD_REASONS` at install.ts:1568 which is for the unavailable arm), insert a guard:
       - `if (resolved.orphanRewake === true) { reasons.push("orphan rewake"); }`
       - Placement: same arm that builds the `(installed)` row for an installable plugin (NOT the `(unavailable)` / failed arms — orphan-rewake is an installed-with-warning surface per SURF-05).

    5. Imports: add `writeHookConfig` and `removeHookConfig` from `../../bridges/hooks/index.ts` (or the stage.ts module — match the existing import shape for the other bridge verbs). `parseHooksConfig`, `compileIfPredicate`, `readFile`, `homedir`, and `path` are ALREADY imported at install.ts:65-94 — do NOT add duplicate imports.

    6. Verify the post-state-commit hydrate call to `addInstalledPluginHooksToCache` at the line range install.ts:331-361 + 1019-1029 is NOT modified — the existing seam closes the loop the new write opens.

    7. In `tests/orchestrators/plugin/install.test.ts`, add the 6 fixture cases enumerated in `<behavior>`. Reuse the existing install test harness (locations factory, mock resolver, ledger-unwind injection helper). The Phase-order assertion (Test 5) iterates the local `phases` array; expose it via a test seam if not already exported.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/install.test.ts</automated>
  </verify>
  <done>
    `install.ts` `phases` array is the 6-element literal in the documented order; `hooksPhase.do` re-reads + re-parses the on-disk hooks.json (mirroring `install.ts:340-360`) and calls `writeHookConfig`; `.undo` calls `removeHookConfig` only when `hooksFileWritten === true`; `InstallCtx` carries `hooksFileWritten`; cascade row composition pushes `"orphan rewake"` when `resolved.orphanRewake === true`; `addInstalledPluginHooksToCache` call site is byte-for-byte unchanged; install test cases green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: update.ts — 5th cascade slot in hand-rolled Phase 3a commit loop</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (lines 742-797 `prepareUpdateHandles` — verify the prepare loop is for bridges with a tmp-staging dir; the hooks bridge has NO staging dir per D-63-02; lines 1262-1298 the Phase 3a commit loop with `commitPreparedSkills` / `commitPreparedCommands` / `commitPreparedAgents` / `commitPreparedMcp` — THIS is where the hooks slot lands; lines 1119-1129 post-state-commit cache+rebuild seam — UNCHANGED)
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts (writeHookConfig + removeHookConfig signatures)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (update.ts section)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (the multi-cascade hazard list — every hand-rolled cascade site must gain the hooks slot in lockstep)
  </read_first>
  <behavior>
    - Test 1: Updating a plugin from version A to version B where both ship `hooks/hooks.json` overwrites `<hooksDir>/<plugin>/hooks.json` atomically; the new content matches version B's `hooks.json`.
    - Test 2: Updating from version A (with hooks) to version B (without hooks) removes the hooks.json file in the commit phase.
    - Test 3: Updating from version A (without hooks) to version B (with hooks) writes the new hooks.json file in the commit phase.
    - Test 4: A failed commit of any LATER bridge (e.g., mcp commit fails) leaves the hooks slot's just-written file IN PLACE — `update.ts` Phase 3a continues across failures (D-03) and rollback is via the user-facing recovery hint (reinstall), NOT in-process rollback. The test asserts: hooks.json contains version B's content AND the aggregate failure NotificationMessage carries `reasons: ["rollback partial"]`.
  </behavior>
  <action>
    1. Architecture decision (unconditional — no executor hedge): the hooks bridge has NO staging dir (D-63-02), so it has no prepare/commit split. `writeHookConfig` IS the atomic write. The hooks slot lives in the **Phase 3a commit loop** at update.ts:1262-1298, NOT in `prepareUpdateHandles` at update.ts:742-797. Do NOT add a `handles.hooks` field to `PrepHandles`.

    2. In the Phase 3a commit loop at update.ts:1262-1298, insert a new commit step BETWEEN the `commitPreparedAgents` block (ending ~line 1292) and the `commitPreparedMcp` block (starting ~line 1294). The new block mirrors the existing try/catch + `phase3aFailures.push` pattern:

       ```
       try {
         if (installable.hooksConfigPath !== undefined) {
           const raw = await readFile(
             path.join(installable.pluginRoot, installable.hooksConfigPath),
             "utf8",
           );
           const ifCtx = { homedir: homedir(), cwd: args.cwd, projectRoot: args.cwd };
           const parsed = parseHooksConfig(raw, ifCtx, compileIfPredicate);
           if (!parsed.ok) {
             throw new Error(`hooks.json re-parse failed: ${parsed.reason}`);
           }
           await writeHookConfig({
             locations: args.locations,
             pluginName: plugin,
             pluginRoot: installable.pluginRoot,
             hooksValue: parsed.value,
           });
         } else {
           // Version B has no hooks; remove any stale file from version A.
           await removeHookConfig({ locations: args.locations, pluginName: plugin });
         }
       } catch (err) {
         phase3aFailures.push({ phase: "hooks", msg: errorMessage(err), cause: err });
       }
       ```

       The `parseHooksConfig` / `readFile` / `compileIfPredicate` / `homedir` / `path` imports ALREADY exist in update.ts (verified at lines 53, 67, 73, 83, 90). Add `writeHookConfig` + `removeHookConfig` imports next to the existing `bridges/hooks/index.ts` import block at update.ts:68-73.

    3. Extend the closed `Phase3Phase` tuple discipline:
       - `PHASE3_FAILURE_PHASES` at update.ts:851 currently lists `["skills", "commands", "agents", "mcp"] as const`. Add `"hooks"` between `"agents"` and `"mcp"` so the tuple becomes `["skills", "commands", "agents", "hooks", "mcp"] as const`.
       - The `Phase3Failure.phase` closed union in `shared/errors.ts` MUST also gain `"hooks"`. Locate via `grep -n "Phase3Failure\|Phase3Phase" extensions/pi-claude-marketplace/shared/errors.ts` and extend the union; the TypeScript strictness contract will surface every consumer site as a compile error if any are missed.
       - `finalizeUpdateRecord`'s per-bridge resource-update gating (uses `Set<Phase3Phase>`) MUST be extended to include the hooks slot — when hooks committed successfully (no entry in `phase3aFailures` for `"hooks"`), the per-bridge finalize writes `resources.hooks = [plugin]` when `installable.hooksConfigPath !== undefined` else `[]`; when hooks failed, the resource entry is NOT updated. This matches how the existing bridges gate finalize state writes on phase success.

    4. Add the same orphan-rewake reason wiring as install.ts to update.ts's success-row composition: if `installable.orphanRewake === true` on a successful update, push `"orphan rewake"` into the success row's `reasons`. Reuse the existing reason-push pattern at the success-row composition site (the cascade entrypoint or direct-path success site).

    5. Phase 3a commit-loop rollback semantics (matches existing bridges): update.ts does NOT roll forward or back inside Phase 3a — it CONTINUES across bridge-commit failures (D-03), aggregating into `phase3aFailures`. The user-facing recovery is via the existing `RECOVERY_PLUGIN_REINSTALL_PREFIX` hint. So a hooks slot that succeeded while a LATER bridge failed leaves the new hooks.json IN PLACE — matching how a successful skills/commands swap stays committed when mcp fails. There is no backup-restore primitive in update.ts; this is intentional (`update.ts:1101-1132` documents the truthful "we did not complete the swap" semantics).

    6. Post-state-commit cache+rebuild seam at update.ts:1119-1129 is UNCHANGED — the existing `readAndCacheUpdatedPluginHooks` call already reads from `installable.hooksConfigPath`, which now points at the just-written file.

    7. Tests: add Test 1..4 from `<behavior>` to `tests/orchestrators/plugin/update.test.ts`. Reuse the existing update test harness; the hooks-present / hooks-absent variants exercise the version-A-vs-version-B transitions. Test 4 specifically asserts hooks.json content matches version B AFTER a later-bridge failure — this pins the "no in-process rollback" semantics.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/update.test.ts</automated>
  </verify>
  <done>
    `update.ts` Phase 3a commit loop carries the hooks slot between `commitPreparedAgents` and `commitPreparedMcp` with the parse-then-write pattern; `PHASE3_FAILURE_PHASES` tuple grows `"hooks"`; `Phase3Failure.phase` union grows `"hooks"`; `finalizeUpdateRecord` gates `resources.hooks` on hooks-slot success; on a later-bridge failure the hooks file stays in place (no in-process restore — recovery via reinstall hint); update test cases green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: reinstall.ts — 5th cascade slot in hand-rolled parallel-prepare/commit</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts (lines 1117-1122 — note the existing post-state-commit `hooks.json` hydrate path is UNCHANGED; lines 1226-1254 parallel-prepare cascade `handles.skills` / `handles.commands` / `handles.agents` / `handles.mcp`)
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (reinstall.ts section)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (the multi-cascade hazard list)
  </read_first>
  <behavior>
    - Test 1: Reinstalling a plugin with hooks rewrites `<hooksDir>/<plugin>/hooks.json` from the resolved manifest (atomic-overwrite of the same path).
    - Test 2: Reinstalling a plugin without hooks removes any stale `<hooksDir>/<plugin>/` directory.
    - Test 3: A rollback during reinstall (force a later phase failure) — same semantic as update.ts: the just-written hooks file STAYS IN PLACE; recovery is via the reinstall hint, not in-process rollback. The test asserts hooks.json content matches the resolved manifest AND the failure NotificationMessage carries the expected partial-rollback surface.
    - Test 4: The cascade-order assertion confirms the parallel-prepare reaches the hooks slot in the documented sequence (skills → commands → agents → hooks → mcp).
  </behavior>
  <action>
    1. Architecture decision (unconditional): mirror update.ts Task 2. The hooks bridge has no staging dir, so the hooks slot lives in the COMMIT step (or the equivalent single-step site in the parallel-prepare/commit), NOT a prepare staging dir. If `reinstall.ts:1226-1254` is a parallel-prepare WITHOUT a separate commit phase (i.e., prepare-and-commit collapse into a single `await prepareStage* + commitPrepared*` site), insert the hooks parse-then-write at the equivalent in-place position between agents and mcp.

    2. Use the same parse-then-write body as update.ts Task 2 (`readFile` → `parseHooksConfig` → `writeHookConfig`); when the resolved plugin has `hooksConfigPath === undefined`, call `removeHookConfig` to clean a stale subtree from a prior install.

    3. Rollback symmetry with update.ts: on a later-bridge failure, the just-written hooks file STAYS IN PLACE — no in-process restore. The user's recovery surface is the existing reinstall recovery hint.

    4. The post-state-commit hydrate at reinstall.ts:1117-1122 is BYTE-FOR-BYTE UNCHANGED — the new write closes the loop the hydrate already expects.

    5. If `reinstall.ts` shares `PrepHandles` / `PHASE3_FAILURE_PHASES` / `Phase3Failure` with `update.ts` (check via `grep -n "PrepHandles\|PHASE3_FAILURE_PHASES\|Phase3Failure" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts`), the TypeScript strictness contract will surface dependencies as compile errors once Task 2 lands; resolve those by matching the Task 2 extensions (add `"hooks"` to the closed phase tuple) — do NOT introduce a separate reinstall-only tuple.

    6. Tests: add Test 1..4 from `<behavior>` to `tests/orchestrators/plugin/reinstall.test.ts`.
  </action>
  <verify>
    <automated>npm test -- tests/orchestrators/plugin/reinstall.test.ts</automated>
  </verify>
  <done>
    `reinstall.ts` parallel-prepare/commit carries the hooks slot between agents and mcp with the parse-then-write pattern; rollback path mirrors update.ts (no in-process restore); reinstall test cases green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: cascadeUnstagePlugin — hooks unstage between agents and mcp + UnstageOutcome.dropped.hooks field</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, tests/transaction/lifecycle-cascade.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts (lines 280-400 — `UnstageOutcome` interface at ~289-301; `cascadeUnstagePlugin` body at 316-380; agents foreign-content guard at ~349-364; Object.freeze blocks at 373-389)
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts (removeHookConfig signature)
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (the delegator — confirm there is NO direct edit needed there per PATTERNS)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (cascadeUnstagePlugin section + Wave 0 integration test entry)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-VALIDATION.md (Wave 0 lifecycle-cascade.test.ts entry)
  </read_first>
  <behavior>
    - Test 1: `cascadeUnstagePlugin` on a plugin with a present `<hooksDir>/<plugin>/hooks.json` removes the entire `<hooksDir>/<plugin>/` directory; `UnstageOutcome.dropped.hooks` contains the plugin name.
    - Test 2: `cascadeUnstagePlugin` on a plugin with NO hooks subtree returns `dropped.hooks: []` (or `dropped.hooks: [pluginName]` if the existing pattern records the attempt — match the existing per-kind shape for `agents` / `commands` / `mcp`); no throw.
    - Test 3: The unstage order is exactly `skills → commands → agents → hooks → mcp` (extract from a tracing test seam or by ordering the `await` calls).
    - Test 4 (integration, in `tests/transaction/lifecycle-cascade.test.ts`): exercise all 4 sites (install + update + reinstall + uninstall) with a hooks-present fixture; assert that after each op the `<hooksDir>/<plugin>/hooks.json` state matches expectations and that NotificationMessage carries the expected row.
  </behavior>
  <action>
    1. In `orchestrators/marketplace/shared.ts`, locate the `UnstageOutcome` interface (~line 289-301) and add `readonly hooks: readonly string[];` between `agents` and `mcpServers` (declaration order matches cascade order per PATTERNS).

    2. Update BOTH `Object.freeze` blocks (the success-return and failure-return paths around lines 373-389) to construct `dropped.hooks` from the new bridge call's return value.

    3. In `cascadeUnstagePlugin` (~lines 316-380), insert a new step between the agents foreign-content guard (~line 365) and the mcp call (~line 366):
       - `const hooksResult = await removeHookConfig({ locations, pluginName: plugin });`
       - `dropped.hooks = [hooksResult.removed];` (or the per-kind shape matching agents — choose based on what the existing `agents` / `commands` / `skills` records carry; for parity prefer a single-element array of the plugin name).

    4. The `uninstall.ts` orchestrator delegates to `cascadeUnstagePlugin`; NO direct edit there.

    5. Create `tests/transaction/lifecycle-cascade.test.ts` (Wave 0 dependency from VALIDATION.md). Tests:
       - Install plugin with hooks → assert `<hooksDir>/<plugin>/hooks.json` exists.
       - Update plugin → assert file overwritten.
       - Reinstall plugin → assert file overwritten again.
       - Uninstall plugin → assert `<hooksDir>/<plugin>/` removed (cascadeUnstagePlugin path).
       - LIFE-02 row assertion: after each op, the captured `NotificationMessage` payload carries the expected plugin row and the existing reload-hint trailer cascades.

    6. Imports: add `removeHookConfig` from `../../bridges/hooks/index.ts` (or `../../bridges/hooks/stage.ts`).
  </action>
  <verify>
    <automated>npm test -- tests/transaction/lifecycle-cascade.test.ts tests/orchestrators/marketplace</automated>
  </verify>
  <done>
    `UnstageOutcome.dropped` carries `hooks: readonly string[]` between `agents` and `mcpServers`; `cascadeUnstagePlugin` calls `removeHookConfig` between the agents foreign-content guard and the mcp unstage; both Object.freeze blocks updated; `tests/transaction/lifecycle-cascade.test.ts` exists and green; uninstall path coverage routes through the shared chokepoint.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/transaction/lifecycle-cascade.test.ts` green.
- `npm run check` green from a clean checkout.
- `grep -n "hooksPhase" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` finds the Phase definition AND the literal-array entry.
- `grep -c "writeHookConfig\|removeHookConfig" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts` returns at least 2 (the parse-then-write call site + the version-B-has-no-hooks remove call site).
- `grep -c "writeHookConfig\|removeHookConfig" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` returns at least 1.
- `grep -n "removeHookConfig" extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` finds the call between agents foreign-content guard and mcp.
- `grep -n "orphanRewake" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` finds the reason-push guard.
- `grep -n "addInstalledPluginHooksToCache" extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns the same line numbers as before the plan (byte-for-byte unchanged).
- `grep -v '^[[:space:]]*//' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts | grep -c "lossy synthesis\|LOSSY_SYNTHESIS"` returns 0 (SURF-03 stays unshipped).
</verification>

<success_criteria>
- LIFE-01 satisfied at ALL FOUR sites (install / update / reinstall / cascadeUnstagePlugin); the hooks slot lives BETWEEN agents and mcp per D-63-01.
- LIFE-02 satisfied: install + uninstall emit a `NotificationMessage` plugin row through the existing v1.4 cascade; existing reload-hint trailer fires; ZERO new notify call sites; ZERO new top-level message variants; ZERO new state-change tokens.
- SURF-05 wiring complete: `(installed) {orphan rewake}` surfaces via the existing reason composition (the source REASONS token was added in Plan 63-03; this plan only consumes it).
- All four cascade sites are touched in lockstep; the integration test exercises every path.
- `hookConfigPathFor` from Plan 63-02 is the single source of truth; `addInstalledPluginHooksToCache` reads the path the bridge wrote.
- SURF-03 synthesis-caveat warnings do NOT ship.
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-04-SUMMARY.md` when done. Summary records: the 4 cascade-site edits (file:line); the integration test pass/fail; the post-state-commit hydrate call's line numbers (proving the unchanged seam); and a one-line "Plan 63-05 (info renderer) is the last consumer plan" handoff.
</output>
</content>
</invoke>
