---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/domain/resolver.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [SURF-05]
must_haves:
  truths:
    - "`shared/notify.ts::REASONS` gains exactly one new tuple member: the literal string `\"orphan rewake\"` — placed after `\"not added\"` per RESEARCH Example 2."
    - "`domain/resolver.ts` detects orphan-rewake at parse time: any handler with `rewakeMessage !== undefined` or `rewakeSummary !== undefined` AND `asyncRewake !== true` flips `partial.orphanRewake = true` (one-per-plugin, regardless of N orphan handlers)."
    - "`ResolvedPlugin` interface gains optional `readonly orphanRewake?: boolean` field (alphabetical placement)."
    - "Plugins with `asyncRewake: true` install normally with NO `{orphan rewake}` reason."
    - "`docs/output-catalog.md` gains `(installed) {orphan rewake}` row(s) mirroring the Phase 58 `unsupported hooks` landing pattern — same byte-form discipline."
    - "`tests/architecture/catalog-uat.test.ts` gains byte-equality fixture(s) for the new `(installed) {orphan rewake}` row(s)."
    - "ALL FOUR changes (REASONS tuple + catalog row + UAT fixture + resolver flag) land in ONE atomic commit per D-58-01 atomic-supersession. Atomicity is enforced by the catalog-UAT byte-equality test failing in isolation if any of the four parts are missing — NOT by manual git-staging checks."
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/notify.ts
      provides: "REASONS tuple += \"orphan rewake\""
      contains: "\"orphan rewake\""
    - path: extensions/pi-claude-marketplace/domain/resolver.ts
      provides: "detectOrphanRewake helper + partial.orphanRewake assignment in applyHooksConfig + ResolvedPlugin.orphanRewake?: boolean field"
    - path: docs/output-catalog.md
      provides: "(installed) {orphan rewake} row(s) — atomic with REASONS addition"
    - path: tests/architecture/catalog-uat.test.ts
      provides: "byte-equality fixture row(s) for the new catalog row(s) — atomic with REASONS + catalog landings"
  key_links:
    - from: extensions/pi-claude-marketplace/domain/resolver.ts
      to: extensions/pi-claude-marketplace/shared/notify.ts
      via: "resolver sets partial.orphanRewake; install.ts cascade row composition (Plan 63-04) reads it and pushes \"orphan rewake\" into reasons[]"
      pattern: "orphanRewake"
    - from: docs/output-catalog.md
      to: tests/architecture/catalog-uat.test.ts
      via: "byte-equality gate — fixture rows are exact substrings of the catalog rows"
      pattern: "orphan rewake"
---

<objective>
Add the SURF-05 closed-set REASONS token `"orphan rewake"` to `shared/notify.ts::REASONS` AND the orphan-rewake detection seam in `domain/resolver.ts::applyHooksConfig` (per RESEARCH Open Question 1 recommendation: resolver-side, not install orchestrator) AND the matching `docs/output-catalog.md` row(s) AND the `tests/architecture/catalog-uat.test.ts` byte-equality fixture(s) — ALL FOUR in ONE atomic commit (D-58-01 atomic-supersession).

Purpose: SURF-05 ships ONE install-time warning when `rewakeMessage` / `rewakeSummary` are declared on a hook handler that lacks `asyncRewake: true`. The warning rides the existing v1.4 install-cascade row as a new REASONS member (no new notify call site; RECON-04 / IL-2). Plugins with `asyncRewake: true` install normally with no warning. Resolves RESEARCH Open Question 1 (detection seam = resolver) by locking the choice here so Plan 63-04 reads `resolved.orphanRewake` from a stable source.

Output: Four files modified in one commit; no behavior beyond surface text + a discriminated-union flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-CONTEXT.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/domain/resolver.ts
@extensions/pi-claude-marketplace/domain/components/hooks.ts
@docs/output-catalog.md
@tests/architecture/catalog-uat.test.ts
</context>

# Artifacts this phase produces

- New REASONS tuple member `"orphan rewake"` in `shared/notify.ts`.
- New `detectOrphanRewake(parsed)` helper + `partial.orphanRewake = true` assignment inside `applyHooksConfig` in `domain/resolver.ts`.
- New `readonly orphanRewake?: boolean` field on `ResolvedPlugin`.
- New `(installed) {orphan rewake}` row(s) in `docs/output-catalog.md` (one per affected op surface — mirror Phase 58 `unsupported hooks` placement).
- New byte-equality fixture row(s) in `tests/architecture/catalog-uat.test.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin hooks.json → resolver | Already-validated parsed payload (HOOKS_VALIDATOR per Phase 57 / 58); orphan-rewake detection is a pure structural scan of the validated value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03-ID | Information disclosure (mild) | plugin author shipping orphan `rewakeMessage` without `asyncRewake: true` | mitigate | SURF-05 warning surfaces the config bug as `(installed) {orphan rewake}`. Upstream Claude Code treats the orphan field as no-op (no security impact). |
| T-63-03-CT | Tampering | REASONS catalog drift | mitigate | D-58-01 atomic-supersession: REASONS tuple + catalog row + UAT fixture land in ONE commit; the byte-equality test fails in isolation if any are missing (atomicity is test-enforced, not process-enforced). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: REASONS tuple += "orphan rewake" + resolver orphan-rewake detection + ResolvedPlugin field</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/domain/resolver.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 72-104 REASONS tuple — confirm `"unsupported hooks"` already at line 81; locate `"not added"` for insertion point)
    - extensions/pi-claude-marketplace/domain/resolver.ts (lines 600-720 — readStandaloneHooks, applyHooksConfig, partial.hooksConfigPath assignment site near line 707; the `ResolvedPlugin` interface and `partial` accumulator shape)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (HooksConfig + HookHandlerEntry shape; confirm `rewakeMessage` / `rewakeSummary` / `asyncRewake` fields are present in HookHandlerEntry after Phase 62)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (resolver.ts::applyHooksConfig section + Seam 1 REASONS section)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Example 6 detectOrphanRewake skeleton lines 1167-1194; Open Question 1 recommendation)
  </read_first>
  <behavior>
    - Test 1: A plugin manifest where `hooks.json` declares a handler with `rewakeMessage: "x"` but NO `asyncRewake` field is parsed into a `ResolvedPlugin` whose `orphanRewake === true`.
    - Test 2: A plugin manifest where the same handler also sets `asyncRewake: true` parses into `orphanRewake === undefined` (or `false`; the absence-or-false invariant is documented).
    - Test 3: A plugin manifest where a handler sets `rewakeSummary: "x"` without `asyncRewake: true` also flips `orphanRewake = true` (covering both companion fields).
    - Test 4: A plugin with multiple groups across multiple events where ONLY ONE handler is orphaned still produces `orphanRewake === true` exactly once at the plugin level — there is no N-handler aggregation.
    - Test 5: `"orphan rewake"` appears in the `REASONS` tuple (introspect the tuple's `.includes("orphan rewake")` or expose a typed predicate).
  </behavior>
  <action>
    1. In `shared/notify.ts::REASONS` tuple (line 72-104), add `"orphan rewake",` immediately AFTER `"not added",` and BEFORE the closing `] as const;` — alphabetical position is NOT required (RESEARCH Example 2 places it last). Do NOT change any other line in the tuple. Do NOT rename `"unsupported hooks"` (already shipped Phase 58). Do NOT touch the `MANIFEST_FIELD_REASONS` set at `orchestrators/plugin/install.ts:1573`.

    2. In `domain/resolver.ts`, add a private `detectOrphanRewake(parsed: HooksConfig): boolean` helper colocated with `applyHooksConfig` (RESEARCH Example 6 lines 1167-1194 verbatim shape):
       ```
       for (const groups of Object.values(parsed)) {
         for (const group of groups) {
           for (const handler of group.hooks) {
             const hasRewakeField =
               handler.rewakeMessage !== undefined || handler.rewakeSummary !== undefined;
             const asyncRewakeTrue = handler.asyncRewake === true;
             if (hasRewakeField && !asyncRewakeTrue) return true;
           }
         }
       }
       return false;
       ```
       The function returns on the first match — one-per-plugin invariant.

    3. In `applyHooksConfig` (near line 707 where `partial.hooksConfigPath` is set on the success branch of `parseHooksConfig`), add a parallel assignment: `partial.orphanRewake = detectOrphanRewake(hooksResult.value);`. ONLY on the success branch — never on the parse-failure / unsupported-hooks branches (those plugins are flipped `installable: false` upstream and don't reach the row composition).

    4. Add `readonly orphanRewake?: boolean;` to the `ResolvedPlugin` interface (alphabetical placement; preserve existing field order otherwise). Per D-63-08.

    5. Do NOT add any `notify` call. Do NOT touch `install.ts` cascade row composition in this plan — Plan 63-04 reads `resolved.orphanRewake` and pushes the reason; this plan only provides the source data.
  </action>
  <verify>
    <automated>npm test -- tests/domain/resolver.test.ts tests/shared/notify.test.ts</automated>
  </verify>
  <done>
    `REASONS` tuple length grows by exactly 1 (the literal `"orphan rewake"` member); `ResolvedPlugin` has `orphanRewake?: boolean`; `applyHooksConfig` invokes `detectOrphanRewake` and writes `partial.orphanRewake` only on the parse-success branch; resolver unit tests cover the 4 detection cases; the existing `applyHooksConfig` test surface stays green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: docs/output-catalog.md + tests/architecture/catalog-uat.test.ts — atomic-supersession landing</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - docs/output-catalog.md (locate Phase 58 `unsupported hooks` rows at lines 59, 136, 182, 301, 534, 750, 1144 — same multi-row pattern is the analog)
    - tests/architecture/catalog-uat.test.ts (locate the byte-equality fixture rows for `unsupported hooks` — the new fixture rows mirror that shape)
    - extensions/pi-claude-marketplace/shared/notify.ts (the REASONS tuple addition from Task 1)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (docs/output-catalog.md + tests/architecture/catalog-uat.test.ts section — atomic supersession)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (atomic supersession; Pattern 3 lines 565-595)
  </read_first>
  <behavior>
    - Test 1: The catalog-UAT byte-equality runner emits no diff after the fixture additions (i.e., every fixture row appears verbatim in `docs/output-catalog.md`).
    - Test 2: `docs/output-catalog.md` contains at least one `(installed) {orphan rewake}` row under the install operation section (mirror Phase 58 layout — the catalog typically lists rows per orchestrator surface; reuse the same per-surface placement).
    - Test 3: A fixture row exercising a plugin with both `(installed) {orphan rewake}` and ANOTHER reason in the same brace (e.g., comma-joined `{orphan rewake, reload required}` if reload-hint flows through that fixture) renders byte-for-byte with `composeReasons`'s existing brace + comma-join.
  </behavior>
  <action>
    1. Inspect the Phase 58 `unsupported hooks` rows in `docs/output-catalog.md` (the `grep -n "unsupported hooks" docs/output-catalog.md` lines listed in the read_first). For each operation surface that needs the SURF-05 row (install at minimum; update / reinstall if the catalog enumerates them per-surface — match the existing layout), add a NEW row of the form `(installed) {orphan rewake}` under the appropriate orchestrator section. Preserve every byte of existing rows.

    2. In `tests/architecture/catalog-uat.test.ts`, add the corresponding byte-equality fixture row(s). Mirror the Phase 58 fixture additions for `unsupported hooks` — same string-equality shape, same per-surface coverage.

    3. Atomicity discipline: the Task 1 commit MUST bundle `shared/notify.ts` REASONS addition + `domain/resolver.ts` detection + `docs/output-catalog.md` row(s) + `tests/architecture/catalog-uat.test.ts` fixture(s) into ONE commit. The byte-equality test in `tests/architecture/catalog-uat.test.ts` is the ENFORCER — if any one of the four parts is missing from the staged change set, the test fails (a tuple-only commit fails because the new fixture row has no catalog row; a catalog-only commit fails because the fixture asserts a row that does not parse; etc.). Pre-commit hook (`npm run check`) MUST be green on the bundled diff before commit.

    Do NOT add any synthesis-caveat marker (`<lossy synthesis>`) or any other new closed-set token. Do NOT rename or remove any existing catalog row.
  </action>
  <verify>
    <automated>npm test -- tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    `npm test -- tests/architecture/catalog-uat.test.ts` green with the new fixture rows present. (Atomicity is test-enforced: a partial landing fails the byte-equality assertion; no manual git-staging check is required.)
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/domain/resolver.test.ts tests/shared/notify.test.ts tests/architecture/catalog-uat.test.ts` green.
- `npm run check` green from a clean checkout.
- `grep -c '"orphan rewake"' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 (the REASONS tuple member).
- `grep -c "orphan rewake" docs/output-catalog.md` returns at least 1 (catalog rows).
- `grep -c "orphan rewake" tests/architecture/catalog-uat.test.ts` returns at least 1 (UAT fixture).
- `grep -v '^[[:space:]]*//' extensions/pi-claude-marketplace/shared/notify.ts docs/output-catalog.md | grep -c "lossy synthesis\\|LOSSY_SYNTHESIS"` returns 0 (SURF-03 stays unshipped).
</verification>

<success_criteria>
- SURF-05 covered: `REASONS` += `"orphan rewake"`; resolver flips `partial.orphanRewake` on the parse-success branch when any handler is orphaned; one row per plugin regardless of N orphan handlers.
- D-58-01 atomic-supersession honored: REASONS tuple + catalog row + UAT fixture + resolver flag are mutually dependent — partial landing fails the byte-equality test.
- No new notify call site; the reason rides the existing v1.4 install-cascade row (RECON-04 / IL-2).
- Open Question 1 resolved: detection lives in `domain/resolver.ts::applyHooksConfig`; install.ts (Plan 63-04) reads `resolved.orphanRewake` only.
- Plugins with `asyncRewake: true` install with NO `{orphan rewake}` reason.
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-03-SUMMARY.md` when done. Summary records: the REASONS tuple line number; the resolver assignment line; the catalog-row file:line list; the UAT-fixture file:line list; the atomic-commit SHA; and a one-line "Plan 63-04 reads resolved.orphanRewake" handoff.
</output>
</content>
