---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 11
type: execute
wave: 2
depends_on:
  - 63-09
  - 63-10
files_modified:
  - README.md
autonomous: false
gap_closure: true
requirements: [SURF-06]
must_haves:
  truths:
    - "`README.md`'s `## Features` bullet list at lines 21-30 lists the Hooks component kind alongside Commands, Skills, Agents, and MCP servers — slotted in `COMPONENT_KINDS` alphabetical order (agents, commands, hooks, mcp, skills) between Commands and MCP servers."
    - "The Hooks bullet links to `docs/hooks.md` using the same markdown-link form as the existing `## Hook support` section at README.md:175 (`[Hook support reference](docs/hooks.md)`) — readers landing on the Features list reach the hook-support reference via the same anchor target."
    - "The runtime UAT recipe from `63-UAT.md` test 3 succeeds end-to-end against the pi-uat sandbox after plans 63-09 + 63-10 land: `scripts/pi.sh --home tmp/pi-uat` → `/reload` → `/claude:plugin install hookify@claude-plugins-official` produces `+ hookify@claude-plugins-official [user] (installed)` row + `Run /reload to activate hookify@claude-plugins-official` trailer + an on-disk `tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/hooks.json` file whose bytes match the bridge's write contract (the unwrapped inner record from the parser)."
    - "The runtime UAT recipe from `63-UAT.md` test 4 succeeds end-to-end: `/claude:plugin info hookify@claude-plugins-official` renders a `components:` block with a multi-line `hooks:` sub-block sitting alphabetically between `commands:` and `mcp:`; each hook entry renders as `<Event>(<matcher>)` for tool events and bare `<Event>` for non-tool events; the rendered block matches the SURF-01 / D-63-04 contract pinned by `tests/orchestrators/plugin/info.test.ts:1304-1571`."
    - "The runtime UAT recipe from `63-UAT.md` test 5 succeeds end-to-end: `/claude:plugin uninstall hookify@claude-plugins-official` produces `- hookify@claude-plugins-official [user] (uninstalled)` row + reload-hint trailer + the on-disk `tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/` directory is gone."
    - "All three runtime UAT verifications produce NO notify Error: / Warning: rows. The 63-UAT.md test 3/4/5 results are flipped from `issue` to `pass` based on the runtime evidence, and the UAT YAML status is updated accordingly (or a sibling re-verification artefact is produced)."
    - "`npm run check` is GREEN at the commit boundary (typecheck + ESLint + Prettier + unit tests + integration tests)."
  artifacts:
    - path: README.md
      provides: "Hooks bullet added to the `## Features` list at lines 21-30 (alphabetical slot between Commands and MCP servers); markdown link to `docs/hooks.md` matches the existing `## Hook support` section's link form."
      contains: "- Hooks. See [Hook support reference](docs/hooks.md)."
  key_links:
    - from: README.md
      to: docs/hooks.md
      via: "markdown link in both the new `## Features` bullet and the existing `## Hook support` section — readers reach the hook-support reference from two surfaces."
      pattern: "docs/hooks\\.md"
---

<objective>
Close UAT gap 3 (cosmetic): `README.md`'s `## Features` bullet list at lines 21-30 lists supported component kinds (Commands, Skills, Agents, MCP servers) but omits Hooks even though v1.13 ships full hook support and the README already has a separate `## Hook support` section at line 171. Add the missing bullet in COMPONENT_KINDS alphabetical order so a reader scanning the Features list gets the complete picture of v1.13's component kinds.

Then run the runtime UAT recipe from `63-UAT.md` tests 3/4/5 against the pi-uat sandbox — the binding runtime test for the wrapper-vs-settings parser fix from plan 63-09 and the cross-surface classifier parity fix from plan 63-10. The runtime UAT verifies that the wire-format fix actually installs hookify cleanly, that the info command renders the expected hooks block, and that uninstall cleans up the on-disk hooks dir. The runtime UAT is the closing-loop verification for the v1.13 milestone — it must be exercised before the v1.13 close-out.

Purpose: gap 3 is a one-line README change with no automated test surface (the existing `tests/docs/hooks-doc.test.ts` covers the hook-doc itself, not the README features bullet). The runtime UAT is the binding verification that the gap-1 + gap-2 fixes actually land the user-facing behavior the UAT recorded as failing — Pi REPL probes don't belong in `npm test` (per the UAT YAML test 3 `why_human` field, paraphrased). Combining the cosmetic doc fix with the runtime UAT into a single plan keeps the loop closure surface coherent: one commit for the doc fix, one human-verify checkpoint for the runtime UAT, one UAT-status update commit.

Output: one-line README addition (with conventional-commits message), one human-verify checkpoint capturing the runtime UAT outcome, one UAT-status update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md
@.planning/debug/hookify-unavailable-resolver-flip.md
@README.md
@docs/hooks.md
@extensions/pi-claude-marketplace/shared/notify.ts
@CLAUDE.md
</context>

# Gap 3 — README Features bullet (exact spec)

Current `README.md` lines 21-30:

```
## Features

Installs plugins from the Claude plugin marketplace that contain these components:

- Commands.
- Skills.
- Agents. Requires [pi-subagents](https://pi.dev/packages/pi-subagents).
- MCP servers. Requires [pi-mcp-adapter](https://pi.dev/packages/pi-mcp-adapter).

Plugins that contain unsupported components are marked as "unavailable".
```

Target: insert one bullet for Hooks in COMPONENT_KINDS alphabetical order (agents, commands, hooks, mcp, skills per `shared/notify.ts:2672-2679`). The bullet list reads top-to-bottom (Commands, Skills, Agents, MCP servers) — alphabetical within the COMPONENT_KINDS contract — so the Hooks bullet slots between Commands and MCP servers, or alternatively between Agents and MCP servers depending on how you interpret the current ordering. Resolve the ambiguity by following the COMPONENT_KINDS tuple order strictly:

```
## Features

Installs plugins from the Claude plugin marketplace that contain these components:

- Commands.
- Skills.
- Agents. Requires [pi-subagents](https://pi.dev/packages/pi-subagents).
- Hooks. See [Hook support reference](docs/hooks.md).
- MCP servers. Requires [pi-mcp-adapter](https://pi.dev/packages/pi-mcp-adapter).

Plugins that contain unsupported components are marked as "unavailable".
```

Reasoning: the current list is NOT strictly alphabetical (Commands, Skills, Agents, MCP servers — Skills before Agents). The list appears to be authored in "supportedness / pre-existing-companion-need" order: Commands and Skills require no companion extensions; Agents needs pi-subagents; MCP servers needs pi-mcp-adapter. Hooks ships in v1.13 with no companion-extension requirement (the runtime bridge lives inside this extension). The Hooks bullet sits with the no-companion bullets at the top, OR with the companion-requiring bullets at the bottom referencing the `docs/hooks.md` reference instead of a companion package.

**Decision:** slot the Hooks bullet between Agents and MCP servers (the position that matches `COMPONENT_KINDS` tuple order `["agents", "commands", "hooks", "mcp", "skills"]` if you list them in the order they appear in that tuple, walked left-to-right then placed: agents→commands→hooks→mcp→skills, which projects onto the README's commands→skills→agents→hooks→mcp→skills ordering by inserting hooks immediately before mcp). The bullet's "See [Hook support reference](docs/hooks.md)" phrasing distinguishes it from the companion-package callouts (no "Requires ..." clause; v1.13 hook support is in-extension).

If the executor reads the COMPONENT_KINDS tuple and concludes a different placement is more faithful, that's acceptable as long as the bullet exists and the link target is `docs/hooks.md`. Document the placement choice in the commit body. Do NOT reorder the existing bullets — surgical change only.

Period at end of bullet: the existing bullets end with periods ("Commands.", "Skills.", etc.). The Hooks bullet's text ends with a period after `docs/hooks.md).` to match the prevailing style.

# Runtime UAT — binding recipe from 63-UAT.md tests 3/4/5

The runtime UAT is the binding verification for the wrapper-vs-settings parser fix from plan 63-09 (it produced the original UAT failure on test 3) and for the cross-surface classifier parity fix from plan 63-10 (it produced the secondary symptom on the install cascade). Pi REPL probes are NOT part of `npm test`; they require human invocation against the pi-uat sandbox.

## Pre-conditions before running the UAT

1. Plans 63-09 + 63-10 have landed on `features/v1.13-hook-bridge`. `npm run check` is GREEN.
2. The pi-uat sandbox at `tmp/pi-uat/` still has `claude-plugins-official` marketplace added (last verified by 63-UAT.md test 1). If the sandbox has been cleared, re-bootstrap before running the recipes below.
3. The `scripts/pi.sh` launcher exists in the repo root and supports `--home <path>` (per the UAT YAML recipe).

## Recipe (test 3: install)

```
# Kill any running pi process from prior runs.
pkill -f 'pi.*--home' 2>/dev/null || true

# Launch Pi against the pi-uat sandbox.
scripts/pi.sh --home tmp/pi-uat

# In the Pi REPL:
/reload
/claude:plugin install hookify@claude-plugins-official

# Expected:
#   + hookify@claude-plugins-official [user] (installed)
#   Run /reload to activate hookify@claude-plugins-official.
#
# NOT expected:
#   - Any notify Error: / Warning: rows.
#   - Any (unavailable) / (failed) status token.
#   - Any {unsupported hooks} / {unsupported source} REASONS token.
```

Then from a separate shell (do NOT exit the Pi REPL):

```
ls tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/
# Expected: hooks.json file present.

head -20 tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/hooks.json
# Expected: valid JSON object with top-level event keys (PreToolUse,
# PostToolUse, Stop, UserPromptSubmit) — the UNWRAPPED inner record per
# the bridge stage-write contract. NOT the upstream wrapper form.
```

## Recipe (test 4: info)

In the same Pi REPL after the install above:

```
/claude:plugin info hookify@claude-plugins-official

# Expected: a multi-line `components:` block with a `hooks:` sub-block
# slotted alphabetically between `commands:` and `mcp:`. Each hook entry
# renders as `<Event>(<matcher>)` for tool events (PreToolUse / PostToolUse)
# or bare `<Event>` for non-tool events (Stop / UserPromptSubmit).
#
# Example expected rendering shape (exact field values depend on hookify's
# hooks.json contents):
#   components:
#     agents: ...
#     commands: ...
#     hooks:
#       PreToolUse(Bash)
#       PostToolUse(Bash)
#       Stop
#       UserPromptSubmit
#     mcp: ...
#     skills: ...
```

## Recipe (test 5: uninstall)

In the same Pi REPL:

```
/claude:plugin uninstall hookify@claude-plugins-official

# Expected:
#   - hookify@claude-plugins-official [user] (uninstalled)
#   Run /reload to deactivate hookify@claude-plugins-official.
#
# NOT expected:
#   - Any notify Error: / Warning: rows.
```

Then from the separate shell:

```
ls tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/ 2>/dev/null
# Expected: directory is missing OR empty (idempotent rm per NFR-3).
```

## UAT-status update after runtime verification

Once the three recipes have been exercised and the results are PASS for all three:

1. Update `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md`:
   - Test 3's `result:` field: `issue` → `pass`. Replace the `reported:` block with a brief evidence narrative (rendered cascade row + on-disk file path + "no Error: / Warning: rows"). Add a `closed_by:` field naming the plan that closed the gap (`closed_by: ["63-09", "63-10"]`).
   - Test 4's `result:` field: `issue` → `pass`. Replace `reported:` with the rendered `components:` block excerpt. Add `closed_by: ["63-09"]` (test 4 was downstream of test 3 per the UAT `note:` field).
   - Test 5's `result:` field: `issue` → `pass`. Replace `reported:` with the rendered cascade row + the on-disk verification. Add `closed_by: ["63-09"]`.
   - Top-level `status:` field: `diagnosed` → `passed` (all 7/7 tests pass once 3/4/5 flip; tests 1/2/6/7 already passed per the original UAT run).
   - Top-level `updated:` timestamp: set to the runtime-verification timestamp.
   - Summary block: update `passed: 4` → `passed: 7`; `issues: 3` → `issues: 0`.
   - The `## Gaps` block at the bottom: append a `closed: <timestamp>` field to each of the three gap entries; do NOT delete the gap entries (preserve the lineage record).

2. If any of the three recipes FAILS the runtime verification, do NOT update the UAT to `passed`. Instead, halt the plan, surface the failure to the user, and recommend a follow-up debug-session against the failing recipe. The plan's success criteria are NOT met unless all three recipes pass.

# Out of scope

- Any code change to `extensions/` — gap 3 is purely a docs change, and the runtime UAT verifies the production code already changed by plans 63-09 + 63-10.
- Any new automated test — the runtime UAT is human-verified per the UAT YAML `why_human` rationale (Pi REPL probes don't belong in the suite).
- v1.13 milestone close-out (`mvp-phase complete`, `gsd-cleanup`, git tag, npm publish) — this is the developer's call after the plan lands. The plan only closes the gap-closure pass.

<tasks>

<task type="auto">
  <name>Task 1: Add Hooks bullet to README.md ## Features list</name>
  <files>README.md</files>
  <action>
    1. Read `README.md` lines 21-30 in full to confirm the current list shape.
    2. Insert the new Hooks bullet per the plan's "Gap 3 — README Features bullet (exact spec)" section. Slot it between Agents and MCP servers (preferred placement; alternative placements are acceptable if documented in the commit body). The bullet's text MUST be exactly: `- Hooks. See [Hook support reference](docs/hooks.md).` (with the trailing period). Do NOT reorder, rephrase, or modify any of the four existing bullets.
    3. Run `pre-commit run --files README.md` until clean. The markdown-lint hook may complain about line length or markdownlint rules; address per the prevailing repo conventions (see prior `## Hook support` section landed in plan 63-06 for the working pattern). If markdownlint flags the new bullet specifically, capture the error and resolve before committing.
    4. Commit with message `docs(63-11): add Hooks bullet to README Features list` and a body explaining: (a) v1.13 ships hook support; (b) the existing `## Hook support` section at line 171 covers the hook-support story but the Features list at line 21-30 was missing the Hooks bullet; (c) the new bullet links to `docs/hooks.md` matching the existing section's link form; (d) closes UAT gap 3 (cosmetic) from `63-UAT.md`.
  </action>
  <verify>
    <automated>grep -F '- Hooks. See [Hook support reference](docs/hooks.md).' README.md</automated>
  </verify>
  <done>Hooks bullet exists in README.md's ## Features list with the exact text and link target; pre-commit hooks pass; commit landed on `features/v1.13-hook-bridge`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify runtime UAT against pi-uat sandbox (recipes 3, 4, 5)</name>
  <files>tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/hooks.json (verified on-disk after install), .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md (read-only context for the user)</files>
  <action>
    Present the runtime-UAT recipe to the user and wait for their verification. The recipe is purely human-operated against the pi-uat sandbox; no automated test surface exists (per the UAT YAML test 3 `why_human` rationale — Pi REPL probes don't belong in `npm test`). The executor presents the steps below verbatim, then BLOCKS pending the user's "approved" or "failed" response.

    Recipe to present to the user:

    1. From the repo root, kill any running pi process from prior runs:
       `pkill -f 'pi.*--home' 2>/dev/null || true`

    2. Launch Pi against the pi-uat sandbox:
       `scripts/pi.sh --home tmp/pi-uat`

    3. In the Pi REPL, run `/reload`. The process must boot without errors. `/claude:plugin list` must show the `claude-plugins-official` marketplace (if missing, re-bootstrap via `/claude:plugin bootstrap`).

    4. Test 3 (install): run `/claude:plugin install hookify@claude-plugins-official`. Expect: `+ hookify@claude-plugins-official [user] (installed)` cascade row + `Run /reload to activate hookify@claude-plugins-official.` trailer. NO notify Error: / Warning: rows. NO `(unavailable)` / `(failed)` / `{unsupported hooks}` / `{unsupported source}` tokens.

       Then from a separate shell:
       - `ls tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/` — expect `hooks.json` present.
       - `head -20 tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/hooks.json` — expect a valid JSON object with TOP-LEVEL event keys (`PreToolUse`, `PostToolUse`, `Stop`, `UserPromptSubmit`) — the UNWRAPPED inner record per the bridge stage-write contract, NOT the upstream wrapper form.

    5. Test 4 (info): in the same REPL run `/claude:plugin info hookify@claude-plugins-official`. Expect a multi-line `components:` block with a `hooks:` sub-block sitting alphabetically between `commands:` and `mcp:`. Each entry renders as `<Event>(<matcher>)` for tool events (PreToolUse/PostToolUse) or bare `<Event>` for non-tool events (Stop/UserPromptSubmit) — matches the SURF-01 / D-63-04 contract pinned by `tests/orchestrators/plugin/info.test.ts:1304-1571`.

    6. Test 5 (uninstall): in the same REPL run `/claude:plugin uninstall hookify@claude-plugins-official`. Expect: `- hookify@claude-plugins-official [user] (uninstalled)` cascade row + reload-hint trailer. NO notify Error: / Warning: rows.

       Then from the separate shell: `ls tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/ 2>/dev/null` — expect the directory missing OR empty.

    7. Capture the rendered cascade rows + `info` `components:` block + on-disk verification outputs for task 3's UAT-status update. The user types "approved" if all three recipes pass end-to-end, or "failed" + failure details if any recipe fails (the plan halts and the failure is surfaced for follow-up).
  </action>
  <verify>
    <human-check>User reports "approved" after exercising all three runtime UAT recipes against the pi-uat sandbox and confirming each produces the expected cascade rows, on-disk file state, and absence of Error: / Warning: rows. If the user reports "failed", the plan halts and Task 3 does NOT run.</human-check>
  </verify>
  <done>User has typed "approved" and provided the captured cascade-row / components-block / on-disk-verification outputs for use in Task 3's UAT-status update. If the user typed "failed", the plan is halted and Task 3 is skipped — the failure narrative is surfaced to the developer for follow-up (likely a new debug session).</done>
</task>

<task type="auto">
  <name>Task 3: Update 63-UAT.md test 3/4/5 results + status from diagnosed to passed</name>
  <files>.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md</files>
  <action>
    1. Read `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md` lines 1-280 in full (the file is ~365 lines).
    2. Update test 3's `result:` field from `issue` to `pass`. Replace the `reported:` block (the multi-line YAML string starting `failed: |`) with a brief evidence narrative based on the runtime-verification output captured in task 2. Suggested shape (executor adapts to actual captured output):
       ```
       result: pass
       evidence: |
         After plans 63-09 + 63-10 landed:
           ● claude-plugins-official [user] <autoupdate>
             + hookify@claude-plugins-official (installed)
             Run /reload to activate hookify@claude-plugins-official.
         On-disk: tmp/pi-uat/agent/pi-claude-marketplace/hooks/hookify/hooks.json
         exists, top-level keys PreToolUse / PostToolUse / Stop / UserPromptSubmit.
         No notify Error: / Warning: rows.
       closed_by: ["63-09", "63-10"]
       ```
       Keep the original `expected:` block unchanged; preserve the historical `reported:` content as `reported_pre_fix: |` (renamed) for the lineage record per the project's roadmap-evolution discipline.
    3. Update test 4's `result:` field from `issue` to `pass`. Use the same `evidence:` + `closed_by:` pattern. Preserve the original `note:` field (the downstream-of-test-3 narrative) and rename the `reported:` block to `reported_pre_fix:`.
    4. Update test 5's `result:` field from `issue` to `pass`. Same pattern. Preserve the original `note:` field and rename `reported:` to `reported_pre_fix:`.
    5. Update the top-level frontmatter:
       - `status: diagnosed` → `status: passed`
       - `updated:` → set to the current ISO-8601 timestamp.
    6. Update the `## Summary` block (currently lines ~265-272):
       - `passed: 4` → `passed: 7`
       - `issues: 3` → `issues: 0`
    7. In the `## Gaps` block (currently lines ~274-365), append a `closed: <timestamp>` field to each of the three gap entries. Do NOT delete the gap entries. Suggested addition for each:
       ```
       closed: <ISO timestamp>
       closed_by: ["63-09", "63-10"]  # gap 1 + gap 2
       # OR for gap 3:
       closed_by: ["63-11"]
       ```
    8. Run `pre-commit run --files .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md` until clean. The markdownlint / yamllint hooks may surface formatting nits; address per the prevailing repo conventions.
    9. Commit with message `docs(63-11): re-verify phase after gap closure (7/7 UAT)` and a body explaining: (a) plans 63-09 + 63-10 closed gaps 1 + 2; (b) plan 63-11 closed gap 3 (README bullet); (c) runtime UAT recipes 3/4/5 verified end-to-end against the pi-uat sandbox; (d) UAT status flipped from `diagnosed` to `passed`; (e) v1.13 milestone is ready for close-out (developer's call).
  </action>
  <verify>
    <automated>grep -E '^status: passed' .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md &amp;&amp; grep -cE '^result: pass$' .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md | grep -v '^[0-6]$'</automated>
  </verify>
  <done>UAT.md frontmatter status is `passed`; tests 3/4/5 results flipped to `pass` with evidence narratives; original `reported:` blocks preserved as `reported_pre_fix:` for lineage; summary counts updated to 7/0; gap entries annotated with `closed:` + `closed_by:` fields; commit landed on `features/v1.13-hook-bridge`.</done>
</task>

</tasks>

<verification>
End-of-plan checks (run from repo root):
- `grep -F '- Hooks. See [Hook support reference](docs/hooks.md).' README.md` — bullet present.
- `grep -c 'result: pass' .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md` — expects 7 (all seven tests pass) or higher if `pass` substring appears elsewhere; verify by reading the file structurally.
- `grep -E '^status: passed' .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-UAT.md` — top-level status flipped.
- `npm run check` — GREEN (the UAT update + README bullet do not affect code paths).
- Last commits on `features/v1.13-hook-bridge`: `docs(63-11): add Hooks bullet ...` → `docs(63-11): re-verify phase after gap closure ...` in that order, with the human-verify checkpoint between them.
</verification>

<success_criteria>
- README.md's `## Features` bullet list includes the Hooks component kind with a markdown link to `docs/hooks.md`.
- The runtime UAT recipes from `63-UAT.md` tests 3, 4, and 5 pass end-to-end against the pi-uat sandbox: hookify installs cleanly (no `unavailable`/`failed`/`unsupported hooks`/`unsupported source` tokens); on-disk hooks.json is written at the documented bridge path with the unwrapped inner record; `info hookify` renders the expected multi-line `hooks:` block in alphabetical slot; `uninstall hookify` removes the on-disk hooks dir cleanly.
- `63-UAT.md` is updated: tests 3/4/5 results flipped from `issue` to `pass` with evidence narratives; original `reported:` blocks preserved as `reported_pre_fix:` for lineage; top-level `status: passed`; summary counts updated to 7/0; gap entries annotated with `closed:` + `closed_by:` fields.
- `npm run check` is GREEN at the commit boundary.
- The v1.13 milestone is ready for close-out (developer's call — `mvp-phase complete`, `gsd-cleanup`, optional npm publish per the project's milestone-close discipline).
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-11-SUMMARY.md` when done. SUMMARY MUST capture: (a) the cosmetic doc fix landed; (b) the binding runtime UAT against the pi-uat sandbox was exercised and passed all three recipes; (c) the UAT status flipped from `diagnosed` to `passed` with the gap entries' lineage preserved; (d) v1.13 milestone close-out readiness (the developer's call on next steps — `mvp-phase complete` / `gsd-cleanup` / etc.).
</output>
