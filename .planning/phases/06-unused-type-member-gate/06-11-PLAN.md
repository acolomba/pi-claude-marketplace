---
phase: 06-unused-type-member-gate
plan: "11"
type: execute
wave: 7
depends_on: ["06-07"]
files_modified:
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - tests/architecture/flag-catalog-drift.test.ts
  - tests/edge/completions/data.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/edge/handlers/marketplace/shared.test.ts
  - tests/edge/handlers/tools.test.ts
  - scripts/check-unused-type-members.contracts.json
  - .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every edge row this plan clears is cleared by removing surface nothing reads, or by pointing a mirror at the declaration that is read -- never by widening a proof and never by a behavioural change."
    - "No slot is removed from a payload the Pi host or the calling agent receives; the two tool `details` shapes ship exactly the fields they ship today."
    - "The edge owner group's unread count is measured before and after, and each of the 17 rows is accounted for as repaired, contracted, or outstanding with a named reason."
    - "`npm run check` is exit 0 and native aggregate production unit coverage is still 100 percent with no module below it."
  artifacts:
    - path: extensions/pi-claude-marketplace/edge/completions/data.ts
      provides: "A completions seam whose declared minimal shape is actually minimal: the two slots no edge syntax reads are gone, and the structural hand-off from orchestrators/edge-deps.ts still satisfies it."
    - path: extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts
      provides: "A single-name delegate type derived from the orchestrator option shape its only implementation reads, instead of a hand-kept inline mirror."
    - path: extensions/pi-claude-marketplace/edge/handlers/tools.ts
      provides: "A payload loader whose parameter declares only what it reads, with the filter buckets still carried by the one type that owns them."
    - path: scripts/check-unused-type-members.contracts.json
      provides: "Engine-accepted `external-output` entries for the tool `details` slots that really do cross the Pi boundary, and nothing the engine refused."
    - path: .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
      provides: "A record regenerated against the repaired tree, reconciling on a fresh digest, with the surviving edge rows carrying this plan's verdict."
  key_links:
    - from: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/edge-deps.ts"
      via: "`edge/register.ts` assigns `makeLocationsResolver(...)` to the edge-side interface; removing a slot from the consumer must leave that structural hand-off compiling."
    - from: "extensions/pi-claude-marketplace/edge/handlers/tools.ts"
      to: "scripts/check-unused-type-members.contracts.json"
      via: "An `external-output` entry names a build site that fills the slot and a return whose contextual signature is declared in the installed Pi package; the engine refuses the pair if the value never reaches the boundary."
---
<objective>
Close the 17 unread type members the live triage attributes to the `edge` owner group: the tool payload and delegate shapes.

`edge` is the one owner with zero validated contracts -- 131 candidates, 111 runtime-observed, 3 test-only, 0 contract, 17 unread -- so no row here is explained by an existing exemption. All 17 are genuinely unread surface, and each needs a judged verdict rather than a mechanical edit.

Purpose: remove the edge surface nothing reads, derive the mirrors that only look unread because their reader declares its own copy, and prove at the Pi boundary the payload slots that leave the tree. Whatever cannot be honestly settled stays a finding with a recorded reason and a named owner.
Output: extensions/pi-claude-marketplace/edge/completions/data.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts, extensions/pi-claude-marketplace/edge/handlers/tools.ts, scripts/check-unused-type-members.contracts.json, .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md and the paired suites above.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
@.planning/phases/06-unused-type-member-gate/06-06-SUMMARY.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="tracer">
  <name>Drop the two completions-seam slots no edge syntax reads</name>
  <files>extensions/pi-claude-marketplace/edge/completions/data.ts, tests/architecture/flag-catalog-drift.test.ts, tests/edge/completions/data.test.ts, tests/edge/completions/provider.test.ts</files>
  <behavior>
    Before: the gate reports `LocationsResolver.marketplaceNamesCachePath` and `MarketplaceStateRecord.manifestPath` as unread.
    After: neither appears, the other three resolver members and `MarketplaceStateRecord.plugins` keep the verdicts they already hold, and no declaration anywhere in the tree becomes unread as a consequence.
  </behavior>
  <action>
    This is the thinnest slice that exercises the whole loop this plan repeats twice more: read the evidence, judge the row, edit one declaration, prove the structural hand-off still compiles, run the paired suites, and re-measure the gate. Do it first and completely.

    Start by confirming the evidence rather than trusting the recorded disposition. The triage files both rows under "Delegate parameter shape -- the concrete implementation reads it through its own declaration". That reading holds for the rows in the next task; it does not hold here, and the plan departs from it deliberately. `edge/completions/data.ts` reads `loadStateForScope`, `loadManifestForMarketplace` and `pluginCachePath` off the resolver, and reads `plugins` off the state record in `installedNamesInTarget`; it never names `marketplaceNamesCachePath` and never names `manifestPath`. Nor does any other production file: the structural twin in `orchestrators/edge-deps.ts` declares and implements `marketplaceNamesCachePath`, but the only syntax that calls it anywhere is `tests/orchestrators/edge-deps.test.ts`, which is why the triage already records the orchestrator-side member as test-only rather than runtime-observed. Re-establish both facts against source before editing; if either turns out to have a production reader, stop and record the row instead.

    Per D-02 and D-04, a build is a write and a declaration is not a reader, so an interface slot with no calling syntax is dead surface, not an analyzer artifact. 06-04's nested-provenance narrowing under-credits rather than over-credits, so it can hide a real read -- but it cannot invent a call site that is absent from every file, which is what makes these two safe to remove and distinguishes them from the payload rows in task 3.

    Remove `marketplaceNamesCachePath` from the `LocationsResolver` interface and `manifestPath` from `MarketplaceStateRecord`, both in `edge/completions/data.ts`. Nothing executable is deleted: both are type-only declarations, so no branch, function or line leaves the coverage graph. Update the surrounding block comments so they describe what the types now declare -- the `MarketplaceStateRecord` comment claims a minimal shape, and after this edit that claim is true. Keep decision and requirement identifiers in any comment you touch; strip nothing else.

    Then repair the consumers the deletion reaches. `tests/architecture/flag-catalog-drift.test.ts` annotates its stub resolver as `LocationsResolver`, so its `marketplaceNamesCachePath` member becomes an excess property and must go, and its `loadStateForScope` stub returns a record whose only slot is `manifestPath`, which after the edit shares no property with the target shape and fails the weak-type check -- give it the `plugins` slot the interface still declares, or the empty shape the assertion needs. `tests/edge/completions/data.test.ts` and `tests/edge/completions/provider.test.ts` build unannotated stub resolvers, so they compile either way; remove their now-dead `marketplaceNamesCachePath` members anyway, because leaving a stub member no interface asks for is exactly the drift this gate exists to catch.

    Verify the structural hand-off deliberately, not incidentally: `edge/register.ts` passes `makeLocationsResolver(process.cwd())` -- typed `LocationsResolverLike` in `orchestrators/edge-deps.ts` -- into `getArgumentCompletions`, which wants `LocationsResolver`. An implementation carrying a member the consumer no longer declares stays assignable; a typecheck run is the proof, and it is the one thing that would fail loudly if this edit were wrong.

    Do not touch `orchestrators/edge-deps.ts`. Its `LocationsResolverLike.marketplaceNamesCachePath`, the implementation at its line 149, and the two tests that exercise it are now unreachable from any consumer, but they are executable code covered by a paired test and they belong to the orchestrators owner group. Record that finding in the summary as a hand-off to plan 06-12 with the evidence above, and leave the code alone.
  </action>
  <fails_when>Fails if a member is deleted before confirming no production or test syntax reads it through this declaration; if `orchestrators/edge-deps.ts` or its paired test is edited here; if the gate's edge count moves by anything other than the two rows named; or if any suite is made to pass by loosening a stub's type rather than by correcting it.</fails_when>
  <verify>
    <automated>npm run typecheck && node --test "tests/edge/completions/*.test.ts" tests/architecture/flag-catalog-drift.test.ts</automated>
  </verify>
  <done>The two rows are gone from the live report, the resolver hand-off still type-checks at `edge/register.ts`, every touched suite passes, and the orchestrator-side twin is recorded as a hand-off rather than silently deleted.</done>
</task>

<task type="auto">
  <name>Point the two delegate mirrors at the declarations that read them</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts, extensions/pi-claude-marketplace/edge/handlers/tools.ts, tests/edge/handlers/marketplace/shared.test.ts, tests/edge/handlers/tools.test.ts</files>
  <behavior>
    Before: the gate reports five `SingleNameMarketplaceRun.opts` rows -- `ctx`, `pi`, `name`, `cwd`, `scope` -- and three `loadToolPluginPayload.params` rows -- `installed`, `available`, `unavailable`.
    After: none of the eight appears; `GetMarketplaceInfoOptions` keeps its existing verdict and gains no unread sibling; `ToolFilterBuckets` and the tool parameter schema are untouched; the rendered tool text and the structured `details` payload are byte-identical to today's for every seeded case in the paired suite.
  </behavior>
  <action>
    These eight rows are the ones the recorded disposition describes correctly: the edge builds the value, the reader declares its own copy of the shape, and per D-03 a matching property spelling in the reader's declaration is not a read of this one. The disposition offers two options -- point the delegate at the implementation's own parameter type, or keep the mirror and accept the finding. Take the first for both, because in both cases a single named declaration already exists and is already read.

    For the single-name marketplace shim: `SingleNameMarketplaceRun` declares an inline parameter object whose five slots the shim only ever writes, at the `run({ ctx, pi, name, cwd, scope })` call. The one production caller is `edge/handlers/marketplace/info.ts`, which hands in `getMarketplaceInfo`; that function reads all five slots off `GetMarketplaceInfoOptions` in `orchestrators/marketplace/info.ts`. Replace the inline object with that named type, keeping the return type unconstrained exactly as it is today. The import is a type-only import from `orchestrators/`, which the edge zone is already allowed -- the sibling handler imports the same module for its value. Two facts make the substitution safe and both should be checked rather than assumed: the shim passes an `ExtensionCommandContext`, and the installed declarations define that interface as extending `ExtensionContext`; and a run function whose own parameter adds an optional slot stays assignable under contravariance, which is what keeps the paired suite's second delegate working. Update the block comment above the type: it currently explains the inline mirror and the optional extra slot, and after this edit it should explain where the shape now comes from and why the shim did not need its own.

    For the tool payload loader: `loadToolPluginPayload` declares `installed`, `available` and `unavailable` on its `params` parameter and reads none of them -- its body reads only `params.scope` and `params.marketplace`, and every filter decision it makes comes from the `buckets` argument, whose `ToolFilterBuckets` type owns exactly that job and explains why in its own comment. The three slots are a duplicate of a decision made one call up, in `applyFilter`, whose own parameter declares and reads them. Remove them from the loader's parameter type. The call site passes the tool's schema-derived params object, not a fresh literal, so a narrower parameter accepts it unchanged; confirm that with the typecheck rather than by reasoning about it.

    Nothing executable changes in either edit -- both are parameter and alias declarations, so no line, branch or function leaves the coverage graph, and no notification, payload or rendered line can move. Treat any diff in the paired suites' expected output as a signal that the substitution was wrong, not as an expectation to update.

    Both paired suites derive their delegate types from the production declarations rather than restating them, so they should need no edit; run them and record the result either way. If one does need an edit, make it the minimum that follows the production type, and record it as a deviation.
  </action>
  <fails_when>Fails if the shim's parameter type is narrowed to a shape its caller cannot satisfy; if the loader keeps a slot it does not read; if any expected notification, rendered line or `details` payload in the paired suites is edited to accommodate the change; or if a row is cleared by deleting a slot the tool schema still advertises to the calling agent.</fails_when>
  <verify>
    <automated>npm run typecheck && npm run lint && node --test "tests/edge/handlers/**/*.test.ts"</automated>
  </verify>
  <done>All eight rows are gone from the live report, both delegate shapes name a declaration that is genuinely read, and the edge handler suites pass with their expectations unmodified.</done>
</task>

<task type="auto">
  <name>Prove the tool payload slots at the Pi boundary and record what stays</name>
  <precondition>No other phase-06 repair plan is mutating `extensions/` or `.planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md` while this task runs: the source digest and the triage record are one shared artifact, and a concurrent edit makes both this task's measurement and the other plan's stale.</precondition>
  <files>scripts/check-unused-type-members.contracts.json, extensions/pi-claude-marketplace/edge/handlers/tools.ts, .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md</files>
  <behavior>
    Each of the six payload rows -- the four slots of the `marketplaces` array in `registerListMarketplacesTool` and `PluginRow.marketplace` and `PluginRow.scope` -- is either accepted by the contract engine against this tree or refused by it with a named reason. An accepted entry names a build site that fills the slot and a return whose contextual signature is declared in the installed Pi package. A refused draft stays a finding. `ParsedCommandArgs.required` stays a finding with its reason and a named hand-off. The regenerated record reconciles against a freshly computed digest with zero stale, missing, duplicate, incomplete or invalid rows.
  </behavior>
  <action>
    Six rows here are payload slots that leave the tree and one is a compile-time filter. They are the two shapes that must not be repaired by deletion, and the task's job is to prove the first kind and record the second honestly.

    The six payload rows. `registerListMarketplacesTool` annotates a local array with four slots -- `name`, `scope`, `pluginCount`, `source` -- fills them at its `push`, and returns the array under `details`. `renderPluginPayload` builds `PluginRow` values whose `marketplace` and `scope` slots nothing reads back; `renderPluginRow` reads only `status`, `name`, `version` and `reasons`, and the rows travel into `details` under `plugins`. Both payloads are handed to the Pi tool surface and to the calling agent, so per D-05 and the research note that names this exact boundary, the honest answer is a validated `external-output` entry, not a deletion: removing a slot would silently change a shipped payload, which is a behavioural change this plan is not permitted to make.

    Draft one entry per row against the engine's closed schema -- identity, owner, key, category, purpose, plus the `origin` and `boundary` the `external-output` category demands -- and derive every coordinate from the compiler's own view of the declaration rather than typing it by hand, which is the method 06-06 recorded and the reason its 81 entries are evidence rather than assertion. The origin must be the property assignment that fills the slot; the boundary must be a return whose enclosing function has a contextual call signature declared outside this project, which is what the tool `execute` method has and what makes it a boundary at all. Submit them to the real engine one at a time against the real program and keep only what it accepts. Note that the live contract file currently holds no `external-output` entry of any kind -- 62 selections, 17 refinements, 2 brands -- so this is the category's first live use and the engine may well refuse some or all of it; the `PluginRow` pair in particular travels further from its build site to its return than the marketplace array does, and the prover bounds how far it will follow a value.

    A refusal is the correct answer and must be taken as one. Do not widen a prover, do not edit the analyzer, do not relax the hop bound, and do not restructure `tools.ts` so a value takes a shorter path to its return. Record each refused row with the engine's exact reason, and leave it a finding.

    `ParsedCommandArgs.required` at `edge/args-schema.ts` is a filter literal in a conditional type's `extends` clause. It does the same type-system work a two-argument selection does -- it is what makes a tail-optional positional come back as possibly-undefined -- and deleting it would change the public type. No category proves that form; 06-06 recorded it as an observed limit and its only twin is `PiToolName.toolName` in `domain/components/hook-tool-names.ts`, owned by plan 06-13. There is a tempting rewrite that would restate the filter as a two-argument selection the engine can prove. Do not take it. 06-CONTEXT's implementation discretion is explicit that an incomplete analysis is corrected in preference to changing working product code to satisfy it, and rewriting a public generic that every command handler's argument parsing flows through, purely to suit the analyzer, is the failure mode this phase exists to prevent. Accept the finding, and record that one bounded engine plan adding a conditional-clause proof with its own drift controls would clear this row and the domain twin together.

    Then close the record. Any edit under `extensions/` moves the source digest, so the first two tasks have already made the committed record stale by design; regenerate it once here, after the last source change, with the audit's inventory command. Regeneration carries every recorded disposition and note forward, so amend the surviving edge rows' notes in the ledger block to state this plan's verdict -- accepted finding, named hand-off, engine refusal reason -- rather than leaving 06-06's pre-repair reading in place. Re-run the audit's check command afterwards and record its exact output: it exits 1 while any member is unread, which is expected, and its reconciliation half must be exact, with zero stale, missing, duplicate, incomplete or invalid rows.

    Two coupling checks before you finish. First, contract drift: if any repair in this plan made a contracted member readable, the gate fails by name with a setup exit rather than a finding exit -- retire that entry, never widen a proof to keep it. The edge group carries no inherited contract entries, so this should not fire; confirm it rather than assume it. Second, re-measure coverage: every edit in this plan is type-only and no executable line should have moved, but the constraint is that any edit under `extensions/` is measured, so run the unit coverage gate on the final tree and report the aggregate rather than predicting it.

    Report the delta as three counts, not one. The edge group starts at 17 unread. State how many rows this plan repaired, how many it contracted, and how many it left outstanding with a reason, and name every outstanding row. A remaining row with recorded evidence is a better artifact than a zero reached by a widened proof, and this plan is expected to end with at least one.
  </action>
  <fails_when>Fails if an entry is written to the contract file without an engine acceptance against this tree; if a refusal is answered by widening the prover, editing the analyzer, or moving production code to shorten a value's path; if a payload slot is deleted instead of proved; if `ParsedCommandArgs` is rewritten to suit the engine; if the record is left stale or its surviving notes still read as 06-06 wrote them; or if the summary reports one number instead of the repaired, contracted and outstanding split.</fails_when>
  <verify>
    <automated>node scripts/check-unused-type-members.mjs --json; test $? -eq 1 && node --test tests/scripts/check-unused-type-members.contracts.test.ts</automated>
  </verify>
  <done>Every one of the 17 edge rows is accounted for as repaired, contracted or outstanding-with-a-reason; every contract entry in the file was accepted by the engine; the triage record reconciles against a fresh digest; and the outstanding rows carry their owner hand-offs.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands, exact results and any coverage delta in this plan's SUMMARY, using the IDs below. Do not generate an expected verdict from the analyzer's own report: read each row out of source before and after, the way 06-06 spot-checked its newly credited rows.

Every edit in this plan is type-only, so no production line, branch or function should leave or enter the coverage graph. That is a prediction, not evidence: run the aggregate unit gate on the final tree and record the measured result. Preserve all production coverage scripts, exclusions and direct pins; investigate any movement rather than adjusting a pin or a threshold.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-11-T1 | Before: the gate reports `LocationsResolver.marketplaceNamesCachePath` and `MarketplaceStateRecord.manifestPath` as unread. After: neither appears, the sibling resolver members and `MarketplaceStateRecord.plugins` keep their verdicts, no new unread row appears, and the `edge/register.ts` hand-off still type-checks. | Focused verify command above; record the covered positive, negative and failure paths, the preserved assertions and the measured source-coverage impact. |
| 06-11-T2 | Before: five `SingleNameMarketplaceRun.opts` rows and three `loadToolPluginPayload.params` rows. After: none of the eight, `GetMarketplaceInfoOptions` unchanged and with no new unread sibling, and every expected notification, rendered line and `details` payload in the paired suites unmodified. | Focused verify command above; record the covered positive, negative and failure paths, the preserved assertions and the measured source-coverage impact. |
| 06-11-T3 | Each payload entry is engine-accepted against this tree or engine-refused with its exact reason; no refused draft is rewritten to fit; `ParsedCommandArgs.required` stays a finding with a named hand-off; the regenerated record reconciles on a fresh digest with zero stale, missing, duplicate, incomplete or invalid rows. | Focused verify command above plus the plan-level `npm run check` and unit coverage runs; record the exact aggregate production result and confirm no direct pin moved. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Extension to Pi tool surface | The `details` payload of both registered tools leaves this tree for the host and the calling agent; a slot removed here is removed from a shipped contract no compiler in this repository checks. |
| Repair to clean verdict | A source edit must clear a row by removing unread surface or naming the real reader, never by making an unread member unobservable to the gate. |
| Evidence to contract file | A contract entry must be an engine acceptance against this tree, not an assertion written into a JSON file. |
| Record to measured tree | The triage record must reconcile against a digest recomputed from disk, not against the report it describes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-11-01 | Tampering | Payload slots crossing the Pi tool boundary | high | mitigate | Prove payload slots with `external-output` entries the engine accepts; forbid deletion of any slot the tool `details` shape ships, and treat a movement in the paired suites' expected payloads as a failure rather than an expectation to update. |
| T-06-11-02 | Tampering | Repair-to-verdict laundering | high | mitigate | Each cleared row is cleared only by removing surface with no reading syntax anywhere, or by naming a declaration that is genuinely read; per-row before and after evidence is read out of source, and the summary reports repaired, contracted and outstanding separately. |
| T-06-11-03 | Tampering | Contract-drift coupling | medium | mitigate | A repair that makes a contracted member readable fails the gate by name with a setup exit; retire the entry rather than widening the proof, and confirm the edge group's zero inherited entries rather than assuming them. |
| T-06-11-04 | Repudiation | Triage record and source digest | medium | mitigate | Regenerate the record once after the last source change, amend the surviving rows' notes to this plan's verdict, and record the reconciliation counts and the exact exit status; hold the record's single-writer precondition while doing so. |

No package installation is planned; use the installed TypeScript and Node already pinned by the repository.
</threat_model>

<verification>
Run each task's automated checks in the foreground and capture its exit status. On the completed plan run `npm run check` to exit 0 -- typecheck, lint, all four fallow sub-gates, format check, both corresponding-test gates, the direct-coverage negative gate, the unit suite and the integration suite -- and `npm run test:coverage:unit` on the same stable snapshot, reporting the native aggregate production result rather than a prediction of it.

Run `SKIP=trufflehog pre-commit run --files` over each commit's exact file set before attempting that commit; a failed hook means the commit did not happen, so fix, restage and re-run rather than amending. Commit messages follow Conventional Commits with no milestone or phase identifier, matching 06-01 through 06-07.

Assertions must name the exact declaration, member and verdict, never a successful enumeration. Live closure for this phase requires zero unread and zero unsupported findings after every bounded source repair; this plan closes only the edge owner group and is expected to leave named rows behind.
</verification>

<success_criteria>
- The edge owner group's unread count is measured on the tree before the first edit and on the final tree, and the difference is explained row by row.
- All 17 edge rows are accounted for in three separate counts: repaired, contracted, and outstanding with a recorded reason. The expected shape is 10 repaired -- two dead completions-seam slots and eight delegate-mirror slots -- up to 6 contracted at the Pi tool boundary, and at least 1 outstanding, which is `ParsedCommandArgs.required`. A contracted count below 6 is an acceptable and expected outcome; each refusal is recorded with the engine's reason and moves that row to outstanding.
- No production behaviour changed: no notification, rendered line, tool payload or command outcome differs, and every paired-suite expectation is unmodified.
- No slot was removed from either tool `details` payload, and every contract entry in the file was accepted by the engine against this tree.
- The triage record was regenerated after the last source change and reconciles against a fresh digest with zero stale, missing, duplicate, incomplete or invalid rows; the surviving edge rows carry this plan's verdict rather than 06-06's pre-repair reading.
- Two owner hand-offs are recorded with their evidence: the now-unreachable `marketplaceNamesCachePath` twin in `orchestrators/edge-deps.ts` with its implementation and its two test witnesses, to plan 06-12; and a bounded engine plan for the conditional-clause filter category, which would clear `ParsedCommandArgs.required` here and `PiToolName.toolName` in plan 06-13's group together.
- `npm run check` is exit 0 and native aggregate production unit coverage is still 100 percent with no module below it; no coverage threshold, direct pin, suppression or assertion contract was weakened.
- Every task's fails_when counterexample is exercised, and the assertion and coverage ledger is complete.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-11-SUMMARY.md when done. Record the per-row before-and-after accounting, the exact verification results and exit statuses, every engine refusal with its reason, the measured coverage result, the observed limits, and the owner hand-offs this plan raises.
</output>
