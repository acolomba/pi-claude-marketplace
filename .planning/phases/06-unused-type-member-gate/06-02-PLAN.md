---
phase: 06-unused-type-member-gate
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/check-unused-type-members.flow.mjs
  - scripts/check-unused-type-members.analysis.mjs
  - tests/scripts/check-unused-type-members.flow.test.ts
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A consumer read credits only source member paths actually transferred into that consumer."
    - "Callbacks, returns and nested containers retain production origins across aliases."
    - "Structural compatibility alone never manufactures an observation."
  artifacts:
    - path: scripts/check-unused-type-members.flow.mjs
      provides: "Directed value transfers across calls and containers implementation/evidence"
    - path: scripts/check-unused-type-members.analysis.mjs
      provides: "Directed value transfers across calls and containers implementation/evidence"
  key_links:
    - from: "scripts/check-unused-type-members.flow.mjs"
      to: "tests/scripts/check-unused-type-members.flow.test.ts"
      via: "Independently authored controls execute the real operation and assert exact observations and failures."
---
<objective>
Directed value transfers across calls and containers.

Purpose: A consumer read credits only source member paths actually transferred into that consumer.
Output: scripts/check-unused-type-members.flow.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.flow.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Trace one real argument-to-parameter read end to end</name>
  <files>scripts/check-unused-type-members.flow.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.flow.test.ts</files>
  <behavior>
    A produced A is passed to a B parameter and B.read credits A.read; an unrelated compatible A and the unread A sibling still fail.
  </behavior>
  <action>
    Per D-03 and D-04, write the failing CLI-backed fixture first and implement flow.mjs over the 06-01 model contract. Trace source-expression provenance through an actual argument into the checker-resolved implementation parameter and back from a read to the source path. Preserve intermediate inferred object/binding properties even when they are not candidate declarations. Transfers are directed and site-specific, never the Cartesian product of assignable types or identically named properties. Connect flow output to analysis.mjs and keep unsupported transfer sites explicit. The tracer includes actual source construction, parameter read, report witness and unrelated same-shaped negative controls.
  </action>
  <fails_when>Fails if compatibility without a call is accepted, sibling fields inherit the read, or the reversed call edge loses provenance.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.flow.test.ts</automated></verify>
  <done>Resolved call edges carry an exact source-to-consumer witness and leave unrelated compatible declarations unread.</done>
</task>

<task type="auto" tdd="true">
  <name>Expand local, return and callback transfers</name>
  <files>scripts/check-unused-type-members.flow.mjs, tests/scripts/check-unused-type-members.flow.test.ts</files>
  <behavior>
    Aliases, annotated assignments, conditionals, object nesting, returns and generic callbacks preserve only transferred paths. Callback input flows inward and callback return values outward.
  </behavior>
  <action>
    Per D-03, implement actual initialization/assignment/conditional/return edges, contextual object-literal paths, destructured result bindings, imported value aliases, and calls with optional/rest/generic parameters. Resolve overload/implementation relationships and track callback parameter objects and callback return values in their correct opposite directions. Model intersection, mapped-type, satisfies and refinement views as lineage transformations with evidence of an actual transfer; selection patterns do not themselves read values. Preserve joins as sets of possible origins, with cycles handled by the worklist rather than recursion cutoffs. Include separately declared anonymous callback shapes and controls for a passed-but-unread parameter, an uninvoked unrelated callable, reversed callback flow and a type-only alias. Require targeted unsupported diagnostics when an erased or unresolved call is the only potential consumer of candidate provenance.
  </action>
  <fails_when>Fails if merely declaring a callback reads fields, callback directions are swapped, an inferred intermediate drops origins, or refinement syntax is mistaken for execution.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.flow.test.ts</automated></verify>
  <done>Real aliases, generic/anonymous callbacks and returns retain exact provenance without type-only or reverse-flow credit.</done>
</task>

<task type="auto" tdd="true">
  <name>Carry nested provenance through containers without combinatorial traversal</name>
  <files>scripts/check-unused-type-members.flow.mjs, tests/scripts/check-unused-type-members.flow.test.ts</files>
  <behavior>
    Readonly arrays/tuples, Promise fulfillment and Map/WeakMap value transfer preserve nested paths; keys, unrelated slots and unread siblings receive no credit.
  </behavior>
  <action>
    Per D-03 and D-06, add symbol-resolved container semantics for array/tuple initialization, indexing, iteration and callbacks; Promise fulfillment/then/await; and Map/WeakMap construction, set/get and supported iteration. Track element/value paths and distinguish map keys from values and tuple positions from arbitrary unrelated elements. Model the researched nested backups/renamed replacement records through WeakMap using reduced fixtures retaining their real transfer shape. Cache symbol/type work and prune primitive/unrelated library types; measure fixture work counters and elapsed time without asserting a machine-specific duration. A deliberate low resource budget must produce an unsupported/budget diagnostic, never silently truncate. Unmodeled candidate-bearing container methods remain explicit unsupported cases until given sound directed semantics.
  </action>
  <fails_when>Fails if every structurally compatible container value is credited, Map keys are treated as read values, cycles hang, or budget exhaustion passes.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.flow.test.ts tests/scripts/check-unused-type-members.model.test.ts</automated></verify>
  <done>Container and cyclic-flow controls complete with precise witnesses and explicit bounded-analysis failures.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands/results and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not generate expected observations from the analyzer's declaration enumeration. These tasks add analyzer/configuration/control behavior without production .ts edits. Record focused analyzer case/branch coverage and assertion strength; preserve all production coverage scripts, exclusions and direct pins. The orchestrator runs the native aggregate unit and direct gates once per stable integration wave, not once per minor task.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-02-T1 | A produced A is passed to a B parameter and B.read credits A.read; an unrelated compatible A and the unread A sibling still fail. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-02-T2 | Aliases, annotated assignments, conditionals, object nesting, returns and generic callbacks preserve only transferred paths. Callback input flows inward and callback return values outward. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-02-T3 | Readonly arrays/tuples, Promise fulfillment and Map/WeakMap value transfer preserve nested paths; keys, unrelated slots and unread siblings receive no credit. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source/configuration/overlay text is parsed as data and must never execute. |
| Evidence to clean verdict | Symbol, flow and contract records must not manufacture observations or hide unexplained members. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch/configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-02-01 | Tampering | Candidate provenance and contract evidence | high | mitigate | Require exact declaration identity, directed origin/path witnesses and sibling/stale-evidence controls in the task suite. |
| T-06-02-02 | Elevation of privilege | Compiler inputs and subprocess controls | high | mitigate | Parse analyzed modules without importing/executing them; use argv arrays and contained temporary overlays, then verify cleanup. |
| T-06-02-03 | Denial of service | Recursive graph and malformed compiler input | medium | mitigate | Cycle-aware bounded worklists and explicit resource/setup diagnostics; a cutoff cannot return clean. |
| T-06-02-04 | Repudiation | Gate acceptance | medium | mitigate | Deterministic member-level reports, task assertion/coverage ledger and exact positive/negative process results. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run typecheck, lint and format checks on the completed wave; the orchestrator reuses one stable wave snapshot for unchanged native aggregate production unit coverage and direct pins. Tests must assert exact independently specified category, declaration/member and observation semantics, not successful enumeration. Track unsupported analysis explicitly. Live repository acceptance is deferred only to the scheduled post-Phase-5 reconciliation in 06-06; no intermediate fixture success claims the current production tree is clean.
</verification>

<success_criteria>
- A consumer read credits only source member paths actually transferred into that consumer.
- Callbacks, returns and nested containers retain production origins across aliases.
- Structural compatibility alone never manufactures an observation.
- Every task's fails_when counterexample is exercised, and its assertion/coverage ledger is complete.
- No production coverage threshold, direct pin or assertion contract is weakened.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-02-SUMMARY.md when done. Record implementation evidence, exact verification results, observed limits and any newly required bounded live-remediation plan IDs.
</output>
