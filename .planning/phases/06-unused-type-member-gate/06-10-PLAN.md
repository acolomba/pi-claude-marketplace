---
phase: 06-unused-type-member-gate
plan: "10"
type: execute
wave: 7
depends_on: ["06-07"]
files_modified:
  - extensions/pi-claude-marketplace/shared/fs-utils.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/bridges/commands/stage.ts
  - extensions/pi-claude-marketplace/bridges/skills/stage.ts
  - extensions/pi-claude-marketplace/bridges/agents/types.ts
  - extensions/pi-claude-marketplace/bridges/commands/types.ts
  - extensions/pi-claude-marketplace/bridges/skills/types.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-swap.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-replace.ts
  - tests/shared/fs-utils.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/bridges/commands/stage.test.ts
  - tests/bridges/skills/stage.test.ts
  - scripts/check-unused-type-members.contracts.json
  - scripts/test-coverage-direct.pin.json
  - .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No declaration in the tree gives a rollback rename pair a source slot, and the three bridges still remove every renamed replacement and restore every backup in reverse order with the same leak messages."
    - "The three staging input bundles declare only slots something reads; every build site the compiler named was updated, and no cast, alias, widened contract or added read keeps a surplus slot alive."
    - "The live gate reports zero findings under bridges/agents, bridges/commands, bridges/skills and shared/fs-utils.ts, down from 13, and no member anywhere else moved into unread."
    - "The audit reconciles: zero stale-source, stale-record, incomplete, duplicate and invalid problems, with only unread rows owned by the other repair plans left."
    - "npm run check exits 0 including all four fallow sub-gates, native aggregate production unit coverage is still 100 percent, and both direct pin rows were re-measured rather than relaxed."
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/fs-utils.ts
      provides: "The single rollback rename-pair shape, with the dead source slot gone"
    - path: extensions/pi-claude-marketplace/bridges/agents/stage.ts
      provides: "Agents rollback ledger and staging site carrying the repaired pair shape"
    - path: extensions/pi-claude-marketplace/bridges/commands/stage.ts
      provides: "Commands rollback ledger and staging site carrying the repaired pair shape"
    - path: extensions/pi-claude-marketplace/bridges/skills/stage.ts
      provides: "Skills rollback ledger and staging site carrying the repaired pair shape"
    - path: extensions/pi-claude-marketplace/bridges/agents/types.ts
      provides: "StageAgentsInput without the slot no agents code reads"
    - path: extensions/pi-claude-marketplace/bridges/commands/types.ts
      provides: "StageCommandsInput without the slot no commands code reads"
    - path: extensions/pi-claude-marketplace/bridges/skills/types.ts
      provides: "StageSkillsInput without the slot no skills code reads"
    - path: scripts/check-unused-type-members.contracts.json
      provides: "The same 81 validated entries, re-anchored to the coordinates the edits moved"
    - path: scripts/test-coverage-direct.pin.json
      provides: "The same two pinned dispositions, re-measured after the line counts moved"
    - path: .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
      provides: "A record regenerated against the repaired tree, with every carried note restored"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/fs-utils.ts"
      to: "extensions/pi-claude-marketplace/bridges/{agents,commands,skills}/stage.ts"
      via: "One rename-pair shape reaches three rollback ledgers, so a partial removal leaves the bridges disagreeing with the helper they all call."
    - from: "extensions/pi-claude-marketplace/bridges/{agents,commands,skills}/types.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/{install-outcome,update-swap,reinstall-replace}.ts"
      via: "Excess-property checking on the build-site literals is what enumerates the call sites a removed slot touches."
    - from: "the source edits"
      to: "scripts/check-unused-type-members.contracts.json and 06-LIVE-TRIAGE.md"
      via: "Both records are coordinate-addressed, so a line-shifting edit re-keys contract entries and disposition rows in the same change."
---

<objective>
Close the 13 unread type members owned by the three artifact bridges and the one
rollback helper row in `shared/fs-utils.ts`.

Purpose: 10 of the 13 are one dead field spread across four declaration sites,
and the four have to move together or the bridges and the helper they all call
stop describing the same value. The remaining 3 are surplus slots on the three
staging input bundles, each of which needs its full call-site consequence proved
before it is removed.
Output: the four repaired declaration sites, the three repaired input bundles,
their updated build sites and paired tests, re-anchored contract and pin
records, and a regenerated `06-LIVE-TRIAGE.md` that reconciles.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-06-SUMMARY.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/shared/fs-utils.ts
@extensions/pi-claude-marketplace/bridges/agents/stage.ts
@extensions/pi-claude-marketplace/bridges/commands/stage.ts
@extensions/pi-claude-marketplace/bridges/skills/stage.ts
</context>

<owned_rows>
These 13 rows, and no others, are this plan's scope. Every identifier below is
the row id recorded in `06-LIVE-TRIAGE.md` at digest `671cb0ae`. Confirm each id
against a fresh report before editing; a row that has already moved is a signal
the record is stale, not a licence to proceed.

**Group A -- the dead rename-pair source slot (10 rows, 4 declaration sites, ONE repair):**

| Row id | Declaration |
| --- | --- |
| `bridges/agents/stage.ts:71:23` | `AgentsReplacementInternals.renamed` |
| `bridges/agents/stage.ts:469:20` | the local ledger array in `replacePreparedAgents` |
| `bridges/agents/stage.ts:588:23` | `rollbackAgentsReplacementInternal` parameter |
| `bridges/commands/stage.ts:70:23` | `CommandsReplacementInternals.renamed` |
| `bridges/commands/stage.ts:400:20` | the local ledger array in `replacePreparedCommands` |
| `bridges/commands/stage.ts:512:23` | `rollbackCommandsReplacementInternal` parameter |
| `bridges/skills/stage.ts:62:23` | `SkillsReplacementInternals.renamed` |
| `bridges/skills/stage.ts:465:20` | the local ledger array in `replacePreparedSkills` |
| `bridges/skills/stage.ts:577:23` | `rollbackSkillsReplacementInternal` parameter |
| `shared/fs-utils.ts:225:32` | `RollbackReplacementInput.renamed` |

**Group B -- surplus slots on the staging input bundles (3 rows, 3 independent repairs):**

| Row id | Declaration | Slot |
| --- | --- | --- |
| `bridges/agents/types.ts:72:3` | `StageAgentsInput` | the resolved-plugin slot |
| `bridges/commands/types.ts:43:3` | `StageCommandsInput` | the marketplace-name slot |
| `bridges/skills/types.ts:29:3` | `StageSkillsInput` | the marketplace-name slot |
</owned_rows>

<tasks>

<task type="tracer">
  <name>Drop the dead rename-pair source slot across all four declaration sites at once</name>
  <precondition>`git status --porcelain -- extensions tests scripts` prints nothing, so the before/after analyzer reports measure only this plan's change.</precondition>
  <reversibility rating="reversible">The slot is inert data on an internal rollback ledger; restoring it is a four-line revert with no persisted or published shape behind it.</reversibility>
  <files>extensions/pi-claude-marketplace/shared/fs-utils.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, extensions/pi-claude-marketplace/bridges/commands/stage.ts, extensions/pi-claude-marketplace/bridges/skills/stage.ts, tests/shared/fs-utils.test.ts</files>
  <behavior>
    - `rollbackReplacementCommon` still removes every entry of its rename ledger in reverse order, with the same per-entry leak text, the same removal options per mode, and the same ordering against the backup restore and the cleanup step.
    - The three bridges still hand their rollback helper exactly the entries they renamed into place, and a fault injected on any one removal still yields one leak and lets its siblings succeed.
    - Group A's 10 rows disappear from the live report, and no member anywhere in the tree newly enters `unread` or leaves `runtime-observed`.
  </behavior>
  <action>
    Capture the pre-edit baselines first: `mkdir -p tmp`, then `node scripts/check-unused-type-members.mjs --json > tmp/members-before.json` (exit 1 is expected while other owners' rows remain; exit 2 is a stop) and `grep -rho "fallow-ignore" extensions tests scripts | wc -l > tmp/ignores-before.txt`. These two files are the evidence the later gates compare against.

    Then make one atomic repair. The rename ledger's pair type is declared in four places -- the shared helper's input interface in `shared/fs-utils.ts`, and in each of the three bridges: the `*ReplacementInternals` handle type, the local mutable ledger array inside the `replacePrepared*` function, and the rollback-internal function's parameter. Narrow the pair element in all four files to carry only the destination path, which is the sole property `rollbackReplacementCommon` reads (D-03: identity, not spelling, settles the read; the helper's loop body is the whole consumer). Do not do this one bridge at a time -- the shapes are structurally assignable, so a partial removal compiles while leaving three bridges describing a value their shared helper no longer declares.

    At each bridge's rename loop the entry currently pushed onto the ledger is the staged-paths element the loop is iterating, which still carries its own source path because the rename call above reads it. Push a fresh element carrying only the destination instead of forwarding the iterated element whole, so the value handed to the rollback helper matches its declared shape exactly and no undeclared property travels into it. Leave the staged-paths declaration itself alone: its source path is genuinely read by the rename call.

    Do not widen this into a rewrite of the ledger element to a bare path string, and do not introduce a named alias for the narrowed pair. The recorded disposition is to drop one slot; a bare-string rewrite changes the helper's loop body and its label text for no finding, and a one-field alias is an abstraction CLAUDE.md's simplicity rule does not want. If a later reader does judge an alias warranted, its only architecturally legal home is `shared/fs-utils.ts` beside `RemovalOps` and `RollbackReplacementLabels`, or `domain/`; `.fallowrc.json` forbids `bridges-agents`, `bridges-commands` and `bridges-skills` from importing one another, so a shared shape parked in a sibling bridge is a boundary violation the fallow gate refuses.

    Update the rollback fixtures in `tests/shared/fs-utils.test.ts` that build ledger entries with both paths so they build the narrowed shape. Those literals are assigned to an inferred-type local before being passed, so the compiler will not flag them; leaving them would keep test fixtures describing a shape production no longer has. Check `noUnusedLocals` afterwards for any staging-path local the edit orphans.

    Every number this task claims must be measured in the foreground with its exit status captured. Record the before and after finding counts and the four repaired sites in the SUMMARY.
  </action>
  <fails_when>Fails if any one of the four declaration sites still carries the slot, if the rollback helper's loop, leak text or removal options changed, if the rename ledger is fed a value carrying an undeclared property, if a shared shape lands in a sibling bridge, or if a member outside Group A changed status.</fails_when>
  <verify>
    <automated>npm run typecheck && node --test tests/shared/fs-utils.test.ts "tests/bridges/agents/*.test.ts" "tests/bridges/commands/*.test.ts" "tests/bridges/skills/*.test.ts" && mkdir -p tmp && { node scripts/check-unused-type-members.mjs --json > tmp/members-t1.json; test $? -ne 2; } && python3 -c "import json,sys; a=json.load(open('tmp/members-t1.json')); b=json.load(open('tmp/members-before.json')); left=[f for f in a['findings'] if f['key']=='from' and f['owner'].split('.')[-1]=='renamed']; prev={(f['path'],f['owner'],f['key']) for f in b['findings']}; new=[f for f in a['findings'] if (f['path'],f['owner'],f['key']) not in prev]; sys.stderr.write('group-a-left=%r\nnew-findings=%r\n' % (left,new)); sys.exit(1 if (left or new) else 0)"</automated>
  </verify>
  <done>All four declaration sites carry the narrowed pair, the three bridge suites and the fs-utils fault-injection suite pass unchanged, Group A's 10 rows are gone, and the report gained no finding that was not there before.</done>
</task>

<task type="auto">
  <name>Remove the three surplus staging input slots and re-anchor every record the edits move</name>
  <reversibility rating="costly">Re-adding a removed slot means re-threading it through three orchestrator build sites and roughly a hundred fixture literals across three bridge suites.</reversibility>
  <files>extensions/pi-claude-marketplace/bridges/agents/types.ts, extensions/pi-claude-marketplace/bridges/commands/types.ts, extensions/pi-claude-marketplace/bridges/skills/types.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update-swap.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-replace.ts, tests/bridges/agents/stage.test.ts, tests/bridges/commands/stage.test.ts, tests/bridges/skills/stage.test.ts, scripts/check-unused-type-members.contracts.json</files>
  <behavior>
    - Each of the three bridges' discover, prepare, commit, replace, rollback and unstage paths behaves exactly as before; only slots no code reads leave their input bundle.
    - The install ledger, the update swap and the reinstall replace all still build a complete input for each bridge, and their suites pass with no fixture asserting a changed result.
    - The contract engine still accepts all 81 entries against the repaired tree, so the gate runs to a member verdict rather than a setup failure.
  </behavior>
  <action>
    Confirm before removing, per D-02 and the phase's implementation discretion: an unused declaration comes out only after its full contract and call-site consequences are proved, and a slot is never kept alive by inventing a reader. For each Group B row, establish that nothing in the owning bridge reads the slot -- neither directly, nor through a spread, a whole-object operation or a delegate -- using the analyzer's own report plus a source search of the bridge directory. Record the evidence per slot. If any one of the three turns out to have a real reader the analysis missed, stop on that row, leave it in place, and record it as an analysis gap for a bounded analyzer plan rather than editing production code to suit the report.

    For the confirmed slots, remove the declaration from the input bundle in `bridges/{agents,commands,skills}/types.ts`. The agents bundle's removal also orphans the resolver-types import at the head of that file; remove it too rather than leaving a dead import. Then let the compiler enumerate the consequences: `npm run typecheck` names every object literal that now carries an excess property. Fix each named site by deleting the property -- in `orchestrators/plugin/install-outcome.ts`, `orchestrators/plugin/update-swap.ts`, `orchestrators/plugin/reinstall-replace.ts`, and the three bridge stage suites. Never silence one with a cast, a non-null assertion, a spread of a wider object or a widened parameter type. In the agents suite, deleting the passed property also orphans the locally-built resolver value at many sites; delete those locals where `noUnusedLocals` flags them and leave the ones still used.

    Three consequences of these edits are line-shifting, and all three must be repaired in this same change or the gate cannot even reach a member verdict.

    First, `scripts/check-unused-type-members.contracts.json` addresses each entry by `path:line:column`. Four entries sit below an edit point: the noop-replacement selections in the three bridges' `types.ts` and the refinement entry near the end of `install-outcome.ts` (which carries a second coordinate in its `refines` field). Re-derive each moved coordinate from the repaired source and update it. The entry count stays at 81, and no `purpose`, `category`, `owner` or `key` changes: these are the same proofs at new addresses, not new or widened ones. An entry whose coordinate no longer resolves is a setup failure, so a mis-anchored entry shows up as exit 2 rather than as a quiet allowance.

    Second, the same shift re-keys disposition rows in `06-LIVE-TRIAGE.md`, which carries notes forward by exact row id. Sixteen rows sit below the three `types.ts` edits and one below the `install-outcome.ts` edits; their evidence is unchanged but their addresses are not. Task 3 regenerates and restores them -- note here which rows moved and by how many lines so Task 3 has the mapping.

    Third, `scripts/test-coverage-direct.pin.json` records an exact reading for `install-outcome.ts`. Deleting lines from that file changes its line total, so the recorded reading stops matching and the direct gate refuses. Re-measure with `npm run test:coverage:direct:all` and update only the `reading` string. The `findingIds` and `reasons` must be byte-identical, and the uncovered branch count must not rise: a pin whose uncovered count grew is a coverage regression to investigate, not a reading to overwrite. Leave the second pin row alone unless its own module was edited.

    Add no `fallow-ignore` marker, no `ignoredClones` entry, no coverage exclusion and no contract entry. Narrowing three parallel bridge declarations can bring them closer together; if `fallow dupes` or `sonarjs/no-identical-functions` fires, resolve it by structure and record what you did.
  </action>
  <fails_when>Fails if a slot is removed without its no-reader evidence recorded, if any excess-property site is silenced by a cast or a widened type, if a contract entry's category, owner, key or purpose changes while re-anchoring, if a pin row's finding ids or reasons change or its uncovered branch count rises, or if a suppression of any kind is added.</fails_when>
  <verify>
    <automated>npm run typecheck && npm run lint && node --test "tests/bridges/agents/*.test.ts" "tests/bridges/commands/*.test.ts" "tests/bridges/skills/*.test.ts" "tests/orchestrators/plugin/*.test.ts" && mkdir -p tmp && { node scripts/check-unused-type-members.mjs --json > tmp/members-t2.json; test $? -ne 2; } && python3 -c "import json,sys; a=json.load(open('tmp/members-t2.json')); b=json.load(open('tmp/members-before.json')); owned=[f for f in a['findings'] if any(p in f['path'] for p in ('bridges/agents/','bridges/commands/','bridges/skills/','shared/fs-utils.ts'))]; prev={(f['path'],f['owner'],f['key']) for f in b['findings']}; new=[f for f in a['findings'] if (f['path'],f['owner'],f['key']) not in prev]; c=json.load(open('scripts/check-unused-type-members.contracts.json')); p=json.load(open('scripts/test-coverage-direct.pin.json')); n=int(open('tmp/ignores-before.txt').read().strip()); import subprocess; now=len(subprocess.run(['grep','-rho','fallow-ignore','extensions','tests','scripts'],capture_output=True,text=True).stdout.split()); sys.stderr.write('owned=%r\nnew=%r\ncontracts=%d pins=%d ignores=%d/%d\n' % (owned,new,len(c['contracts']),len(p['rows']),now,n)); sys.exit(1 if (owned or new or len(c['contracts'])!=81 or len(p['rows'])!=2 or now!=n) else 0)"</automated>
  </verify>
  <done>All 13 owned rows are gone from the live report, the report gained no finding, the contract file still holds 81 entries that the engine accepts against the repaired tree, both pin rows survive with unchanged dispositions, and the suppression count is unchanged.</done>
</task>

<task type="auto">
  <name>Regenerate the record, reconcile it and report the measured delta</name>
  <files>.planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md, scripts/test-coverage-direct.pin.json</files>
  <behavior>
    - The regenerated ledger reconciles against a freshly recomputed digest and a freshly run analysis: no stale-source, no stale-record, no incomplete, no duplicate, no invalid.
    - Every disposition row the edits re-keyed carries its evidence again at its new address, and no row outside this plan's four files lost or changed its note.
    - The only unread rows left are those a different repair plan owns.
  </behavior>
  <action>
    Run `node scripts/check-unused-type-members.audit.mjs --inventory`. Any source edit moves the digest, so the record has been reporting `stale-source` since Task 1; this rebuild is what closes it. The rebuild carries a note forward only when the row id and the row status both still match, so the seventeen rows the Task 2 line shifts re-keyed come back with an empty note and a pending disposition.

    Restore each of those, using the mapping Task 2 recorded. The evidence is the same evidence -- what moved is the address, and for the test-witnessed rows the witness coordinate inside the note text moved too, because the fixture edits shifted the lines the witness sits on. Re-read the fresh report for each row's current witness site and count rather than transcribing the old number; a note that names a coordinate nothing is at is exactly the stale evidence D-05 refuses. Do not write a note for a row that did not move, and do not carry a Group A or Group B note onto any surviving row: those rows are gone and their prose is about rows that no longer exist.

    Then close the loop. `node scripts/check-unused-type-members.audit.mjs --check` must report zero problems in every category except `unread`, and no `unread` problem may name any of this plan's four owner areas. It still exits 1 overall while the other repair plans' rows stand; that is the designed behaviour, not a failure of this plan.

    Run `npm run check` in the foreground with its exit status captured. It must be exit 0, including all four fallow sub-gates -- the architecture-boundary sub-gate is the one most at risk here, because it is the only mechanism that forbids one bridge kind from importing a sibling. Then run `npm run test:coverage:unit` and read `coverage/unit.lcov` directly: report the aggregate production function and branch totals and confirm no module is below 100 percent. Run `npm run test:coverage:direct:all` and confirm both pin rows match. If any production coverage number moved, name the exact function or branch and why the removal moved it; a dropped member can remove a branch from a count, and the honest answer is the new measurement with its cause, never a lowered threshold or a new exclusion.

    Record in the SUMMARY: the owner-group count before and after (13 to 0), the whole-tree unread count before and after with the difference attributed, the four repaired declaration sites, the three removed slots with their no-reader evidence, the re-anchored contract and pin coordinates, the exact commands with their exit statuses, and any row whose recorded disposition you judged unsafe to follow together with the reason.

    Commit each task separately with Conventional Commit messages that name no milestone, phase, plan or wave, and run `SKIP=trufflehog pre-commit run --files <the files in that commit>` before each commit, fixing and restaging until clean. This is a linked worktree, so the trufflehog skip is required. Never pass `--no-verify`, and never recover from a hook failure with `--amend`: a failed hook means the commit did not happen.
  </action>
  <fails_when>Fails if the ledger is left reporting stale-source or incomplete, if a restored note names a coordinate the fresh report does not hold, if an unread problem still names one of this plan's four owner areas, if any production coverage metric falls below 100 percent, or if a threshold, exclusion or pin disposition was changed to make a gate pass.</fails_when>
  <verify>
    <automated>mkdir -p tmp && { node scripts/check-unused-type-members.audit.mjs --check --json > tmp/audit-after.json; test $? -ne 2; } && python3 -c "import json,sys; r=json.load(open('tmp/audit-after.json')); bad=[p for p in r['problems'] if p['category']!='unread']; owned=[p for p in r['problems'] if any(q in p['id'] for q in ('bridges/agents/','bridges/commands/','bridges/skills/','shared/fs-utils.ts'))]; sys.stderr.write('non-unread=%r\nowned=%r\ncounts=%r\n' % (bad,owned,r['counts'])); sys.exit(1 if (bad or owned) else 0)" && npm run check && npm run test:coverage:unit && npm run test:coverage:direct:all</automated>
  </verify>
  <done>The ledger reconciles with only other owners' unread rows outstanding, npm run check is exit 0 with all four fallow sub-gates, aggregate production unit coverage is still 100 percent, both direct pins match, and the 13-to-0 delta is recorded with its measurements.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior
that would fail if weakened. On completion record exact commands, exit statuses
and any coverage delta in this plan's SUMMARY, using the IDs below. Do not derive
an expected observation from the analyzer's own enumeration: the owned-row list
in this plan is the independent expectation, and the report is the thing under
test. This plan edits production `.ts` files, so the aggregate unit coverage gate
and both direct pins are re-measured here rather than inherited from a wave
snapshot; preserve every coverage script, exclusion and pin disposition.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-10-T1 | The rollback helper's loop, leak text and removal options are unchanged; the three bridge suites and the fs-utils fault-injection suite pass unmodified; Group A's 10 rows leave the report and the before/after diff shows no finding gained. | Focused verify command above. No control flow is added or removed by this task, so any movement in a production branch count is itself the finding to report. |
| 06-10-T2 | Each removed slot carries recorded no-reader evidence from the report and from a source search; every excess-property site the compiler named was fixed by deletion, never by a cast or a widened type; contract entries stay at 81 with unchanged proofs at re-anchored coordinates; both pin rows keep their finding ids and reasons; the suppression count matches the pre-edit baseline. | Focused verify command above plus `npm run test:coverage:direct:all` for the re-measured `install-outcome.ts` reading. A risen uncovered-branch count is a regression to investigate, not a reading to overwrite. |
| 06-10-T3 | The audit reports zero problems outside `unread` and no `unread` problem naming this plan's owner areas; every re-keyed row's restored note names a coordinate the fresh report actually holds. | `npm run check`, `npm run test:coverage:unit` read directly from `coverage/unit.lcov`, and `npm run test:coverage:direct:all`, all on one stable snapshot with exit statuses captured. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Rollback ledger to the filesystem | The repaired shape feeds the only path that removes replacement artifacts and restores user backups after a failed install. |
| Evidence to clean verdict | A source repair must remove a member, never make an unread member merely look read. |
| Coordinate-addressed records to source | Contract entries, disposition rows and pin readings all address source by position, so an edit can silently invalidate a proof. |
| Bridge to sibling bridge | A shared shape parked in a sibling bridge would cross an architecture boundary the fallow gate is the only enforcer of. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-10-01 | Tampering | The three bridges' rollback ledgers and `rollbackReplacementCommon` | high | mitigate | The repair removes an inert slot and adds no control flow; the fs-utils fault-injection suite and all three bridge stage suites re-run unmodified, and the helper's loop body, leak text and removal options are pinned as unchanged. |
| T-06-10-02 | Repudiation | Gate acceptance after a production source change | high | mitigate | Before and after reports are diffed by declaration identity rather than by count, so the delta must be exactly the 13 owned rows and no finding may be gained; no suppression, exclusion, contract entry or threshold may be added, and the suppression count is compared against a pre-edit baseline. |
| T-06-10-03 | Tampering | Coordinate-addressed contract entries, disposition notes and pin readings | medium | mitigate | Every moved coordinate is re-derived from the repaired source in the same change, the contract count and every entry's proof fields stay fixed, restored notes must name a coordinate the fresh report holds, and a mis-anchored entry surfaces as a setup failure rather than as a quiet allowance. |
| T-06-10-04 | Elevation of privilege | Architecture boundaries between the three bridge zones | medium | mitigate | No shared type is introduced; if one ever is, its only legal homes are `shared/` or `domain/`, and all four fallow sub-gates including the boundary gate must be exit 0 at plan end. |
| T-06-10-05 | Information disclosure | Rollback leak messages naming absolute paths | low | accept | The leak text is unchanged by this repair and the removed slot never reached a message; absolute paths in user-visible output are already handled at the notify boundary, outside this plan's scope. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks in the foreground with exit statuses captured.
Task 3 additionally runs the full check chain and both coverage gates on one
stable snapshot taken after the last source edit; evidence recorded before a
source change does not count. The whole-tree member gate still exits 1 at plan
end because other repair plans' rows stand, and the audit still exits 1 for the
same reason -- what this plan proves is that zero of those rows belong to
`bridges/agents`, `bridges/commands`, `bridges/skills` or `shared/fs-utils.ts`,
that the record reconciles exactly, and that nothing anywhere else regressed.
</verification>

<success_criteria>
- All 13 owned rows are resolved by source repair: the owner-group unread count goes 13 to 0, measured on a fresh report, not inferred.
- The whole-tree unread count drops by exactly 13 and no member anywhere gained a finding or lost an observation.
- The dead rename-pair source slot is gone from all four declaration sites in one change, with no shared shape placed in a sibling bridge.
- Each removed staging-input slot has recorded no-reader evidence, and any row whose recorded disposition proved unsafe was left in place with its reason.
- `node scripts/check-unused-type-members.audit.mjs --check` reports zero stale-source, stale-record, incomplete, duplicate and invalid problems.
- `npm run check` exits 0 including all four fallow sub-gates; aggregate native production unit coverage is 100 percent with no module below it; both direct pin rows match.
- No coverage threshold, direct pin disposition, suppression, contract entry, ignored clone or assertion contract was added or weakened.
- Every task's `fails_when` counterexample is exercised and its assertion and coverage ledger row is complete.
</success_criteria>

<output>
Create `.planning/phases/06-unused-type-member-gate/06-10-SUMMARY.md` when done.
Record the measured before and after counts, the four repaired declaration sites,
the three removed slots with their evidence, every re-anchored coordinate, exact
commands with exit statuses, observed limits, and any row left unrepaired with
its reason.
</output>
