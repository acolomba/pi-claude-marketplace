---
phase: 98-lifecycle-regression-and-contract-documentation
plan: 06
type: execute
wave: 4
depends_on: ["98-01", "98-03", "98-04", "98-05"]
files_modified:
  - docs/output-catalog.md
  - docs/prd/pi-claude-marketplace-prd.md
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/README.md
autonomous: true
requirements: [DOC-08]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every count claim in the touched documentation equals the runtime constant it describes: the reason-set header states the current member count, and the status-token reference table has one row per plugin-status member."
    - "The status-token reference table lists one row per plugin-status member even where two members share a glyph — tokens that render the same character are documented as distinct rows, never merged."
    - "The reasons-brace convention states both the empty-list behavior (no brace is emitted when the composed list is empty) and the corrected enumeration of which row variants can carry a brace."
    - "The documentation states the contractual multi-reason emit order and the requirement that the reason tuple's declared order stays byte-stable for catalog stability."
    - "The version-token truncation contract — a twelve-hex persisted version renders as a seven-hex display form — remains stated and correct in every touched section."
    - "The design-document flowchart is redrawn to the current decision path (manifest load, then lookup, then the lookup discriminant, then the row form), not patched in place and not deleted."
    - "Every fenced block annotated as a catalog state is byte-identical to what the renderer emits after the sweep; the byte-equality gate is green."
    - "No retired vocabulary is reintroduced into the rewritten documentation."
  artifacts:
    - docs/output-catalog.md
    - docs/prd/pi-claude-marketplace-prd.md
  key_links:
    - "docs/output-catalog.md fenced catalog states -> tests/architecture/catalog-uat.test.ts byte-equality driver"
    - "docs/output-catalog.md + the design document -> tests/architecture/partial-vocabulary-guard.test.ts retired-vocabulary scan"
  prohibitions:
    - "MUST NOT resolve a documentation defect by deleting the falsified claim or the section that holds it — every named defect is corrected in place with the true statement, because a silently shrunk contract is indistinguishable from a corrected one to every later reader."
---

<objective>
Run the bounded DOC-08 accuracy sweep last, so the documentation describes the code as it actually
ships after the four carriers, the contract gate, and the coverage plans have landed.

D-98-06 fixes the depth: fix every named defect, and while inside each touched section also
correct any statement the new read behavior falsified. No restructuring beyond that — a bounded
accuracy sweep, not a rewrite.

D-98-07 fixes one shape: the design document's decision flowchart is REDRAWN to the current path,
not patched in place and not dropped.

Purpose: DOC-08 is the milestone's contract-documentation requirement; the output catalog is the
byte-level rendering authority and the design document is the specification readers reach for
first. Both currently carry claims the milestone falsified.
Output: a corrected catalog, a corrected design document, a redrawn flowchart, and four corrected
source-comment sites.

Tracer exemption: this plan changes no behavior. Its vertical slice would be a prose edit, which
carries no architectural information. The three tasks are grouped by document instead, so each
commit is one reviewable surface.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-CONTEXT.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-RESEARCH.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-PATTERNS.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-01-SUMMARY.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-03-SUMMARY.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-04-SUMMARY.md
@.planning/phases/97-disabled-state-classification-repair/deferred-items.md
@.claude/skills/simple-english/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Output-catalog accuracy sweep</name>
  <files>docs/output-catalog.md</files>
  <precondition>Plans 98-01 and 98-03 have landed, so every carrier-driven catalog state already exists and this task only corrects prose around it.</precondition>
  <read_first>
docs/output-catalog.md lines 40-80 (the Conventions register, including the reasons-brace bullet
whose variant enumeration is the stale count, and the soft-dependency-marker bullet whose
dep-bearing and non-dep-bearing variant counts must be re-derived), lines 128-160 (the two status
token reference tables), lines 405-425 (the manifest-absent inventory states and the on-disk
materialization claim in the prose beneath the first of them), lines 1475-1560 (the
fold-divergence states);
extensions/pi-claude-marketplace/shared/notify.ts lines 243-290 and 470-512 (the two token tuples
the tables must mirror), and the message interfaces that declare a reasons field and a
dependencies field (the authority for which variants can carry a brace and which can carry a
soft-dependency marker);
98-RESEARCH.md section "DOC-08 — every named defect, located and verified" (defects 3, 4, 9, 10)
and its "Additional falsified prose found in the same touched sections";
98-PATTERNS.md section "docs/output-catalog.md + docs/prd/... (DOC-08)" for the prose register and
the fenced-block byte contract.
  </read_first>
  <action>
Correct four named catalog defects and the falsified statements in the same sections.

Re-derive the reasons-brace convention against the message interfaces that actually declare a
reasons field, and rewrite the Conventions bullet's variant enumeration and its count to match.
Research recorded that the current enumeration predates the installed-family rows gaining reasons;
verify the current truth against the source before editing, and if the claim turns out already
correct, record that verification in the plan summary rather than silently leaving it — the
requirement is closed honestly either way. Apply the same re-derivation to the neighbouring
soft-dependency-marker bullet, whose dep-bearing and non-dep-bearing variant counts are stated the
same way.

Add the two missing rows to the plugin status token reference table so it has one row per member of
the plugin-status tuple, including the two partially-installed-family tokens. Two members that
render the same glyph get two rows, not one merged row; the glyph column repeats.

Correct the on-disk materialization claim in the prose beneath the manifest-absent inventory state.
The list surface never checks whether the record is materialized on disk; the true reason the row
keeps its clean token is that the inventory is record-backed. Rewrite the sentence to say what the
surface actually does.

Confirm the fold-divergence between generated installed names and manifest source names is
described consistently across the two catalog states that mention it and the plugin info
orchestrator's comments at the corresponding sites; make the wording agree, correcting whichever
side is wrong rather than both.

While inside each touched section, correct statements the new read behavior falsified — the
manifest-absent rows, the disabled-partial parity, and the enable-partial rows. Do not restructure
beyond the touched sections.

Every fenced block annotated as a catalog state is a byte contract: do not edit inside one. Prose
outside the fences is free. Match the surrounding register; the newer sections are written in the
project's simplified-English style, and the file uses double hyphens rather than long dashes.
  </action>
  <acceptance_criteria>
    - The plugin status token reference table has one row per member of the plugin-status tuple; verify by counting table rows and comparing against the tuple length printed from the source.
    - `grep -c 'the record is materialized on disk' docs/output-catalog.md` returns `0`.
    - The Conventions reasons-brace bullet's variant list matches the set of message interfaces that declare a reasons field; the summary records the derivation.
    - `git diff docs/output-catalog.md` shows no change inside any fenced block that follows a catalog-state annotation.
    - `node --test tests/architecture/catalog-uat.test.ts tests/architecture/partial-vocabulary-guard.test.ts` passes.
    - `pre-commit run --files docs/output-catalog.md` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/architecture/partial-vocabulary-guard.test.ts</automated>
  </verify>
  <done>The catalog's counts, its token reference table, and its manifest-absent prose state what the code does; every byte contract is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Design-document sweep and flowchart redraw</name>
  <files>docs/prd/pi-claude-marketplace-prd.md</files>
  <read_first>
docs/prd/pi-claude-marketplace-prd.md lines 340-420 (the PL-4 row with its glyph roster, glyph
meanings and token set; the PL-6 row describing the retired manifest-failure renderer; the section
heading and the diagram block immediately after the PL-9 row);
extensions/pi-claude-marketplace/shared/notify.ts lines 1540-1600 (the seven glyph constants and
their current meanings — note which glyph now carries the disabled state and which carries the
remote state) and lines 243-290 (the token tuple, to check the token set the row lists);
.planning/REQUIREMENTS.md the boundary requirement that settles the true manifest-failure output
(a bare failed marketplace header with no child rows);
extensions/pi-claude-marketplace/orchestrators/plugin/list.ts and the manifest lookup type it
consumes, for the current decision path the flowchart must depict;
98-RESEARCH.md section "DOC-08 — every named defect, located and verified" (defects 1 and 2 and the
three additional falsified statements).
  </read_first>
  <action>
Correct the row that describes the retired manifest-failure renderer: the true output is the bare
failed marketplace header with no child rows, settled by the boundary requirement and pinned by the
current tests. Rewrite the row to state that, and name the requirement that settles it.

Redraw the decision flowchart (D-98-07) to the current path: manifest load, then plugin lookup,
then a branch on the lookup discriminant, then the resulting row form. Drop the retired branch
that fell back to a state-derived installed row on a manifest warning. Redraw the whole diagram
in the file's existing diagram syntax rather than patching individual edges, and keep it small
enough to read at a glance.

Correct the three additional falsified statements in the same rows: add the two glyphs missing from
the roster; fix the glyph meanings, since the character formerly documented as the disabled marker
now carries the remote state and the disabled state moved to a different character; and remove the
token from the token set that is not a member of the token tuple, replacing it with the correct
member if one was meant.

Do not delete a row to resolve a defect. Every named defect is corrected in place with the true
statement.

Do not reintroduce retired vocabulary; the vocabulary guard scans this document as well as the
catalog.
  </action>
  <acceptance_criteria>
    - The design document's glyph roster lists all seven glyph characters exported by the notify module.
    - `grep -c 'could not load manifest' docs/prd/pi-claude-marketplace-prd.md` returns `0`.
    - `grep -c 'fallback to installed-from-state' docs/prd/pi-claude-marketplace-prd.md` returns `0`.
    - `grep -c '(present)' docs/prd/pi-claude-marketplace-prd.md` returns `0`.
    - The redrawn diagram block names the manifest load, the lookup, the lookup discriminant, and the resulting row forms.
    - `node --test tests/architecture/partial-vocabulary-guard.test.ts` passes.
    - `pre-commit run --files docs/prd/pi-claude-marketplace-prd.md` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/partial-vocabulary-guard.test.ts</automated>
  </verify>
  <done>The design document describes the shipped manifest-failure output and the shipped decision path; its glyph and token claims match the source.</done>
</task>

<task type="auto">
  <name>Task 3: Source-comment sweep and phase gate</name>
  <files>
extensions/pi-claude-marketplace/shared/notify-reasons.ts,
extensions/pi-claude-marketplace/shared/notify.ts,
extensions/pi-claude-marketplace/edge/handlers/tools.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/README.md
  </files>
  <precondition>The environment variable pointing at the globally installed companion agent package is exported for the full-suite run: two integration cases resolve the peer through it and fail locally against a stale or missing global install.</precondition>
  <read_first>
extensions/pi-claude-marketplace/shared/notify-reasons.ts lines 1-40 (the header block whose
stated member count is one short of the tuple's actual length, in two separate sentences);
extensions/pi-claude-marketplace/shared/notify.ts lines 2170-2200 (the render-path comments
claiming the list inventory row omits reasons, and the same claim repeated in the installed-message
interface doc block);
extensions/pi-claude-marketplace/orchestrators/plugin/list.ts around the site that stamps reasons
on an inventory row (the evidence that falsifies those comments);
extensions/pi-claude-marketplace/edge/handlers/tools.ts lines 155-170, 320-335, 398-410 (three
comments citing anchors that no surviving artifact defines);
extensions/pi-claude-marketplace/orchestrators/reconcile/README.md line 34 (the two-axis marker
prose);
extensions/pi-claude-marketplace/persistence/state-io.ts lines 150-160 (the disabled predicate,
which reads the enabled flag alone — the evidence that falsifies the README prose);
.planning/phases/97-disabled-state-classification-repair/deferred-items.md section 1.
  </read_first>
  <action>
Correct the reason-set header's member count in both sentences that state it, and name the decision
that changed it, so the number and its cause travel together.

Correct the render-path comments that claim the list inventory row omits its reasons and therefore
renders like a bare installed row. The list orchestrator stamps reasons on that row today. Fix the
claim at both sites — the render path and the message-interface doc block — and cite the
requirement that changed it.

Replace the three retired anchors in the tools handler with the requirement identifiers that
actually govern those lines, or, where no surviving requirement governs them, rewrite the comment to
state the behavior without an anchor. Do not leave a citation pointing at nothing.

Correct the reconcile README's two-axis marker prose. The disabled predicate reads the enabled flag
alone; the resources arrays number five, not four; and the installable discriminant is deliberately
not an input. Rewrite the sentence to state the single-axis predicate and name the requirement that
collapsed it.

Then run the phase gate: the full check with the companion-package environment variable exported,
capturing the exit code directly rather than through a pipe. Fix anything red before finishing.
  </action>
  <acceptance_criteria>
    - `grep -c '37-entry' extensions/pi-claude-marketplace/shared/notify-reasons.ts` returns `0`.
    - `grep -c 'so it renders byte-identically to a bare' extensions/pi-claude-marketplace/shared/notify.ts` returns `0`.
    - `grep -c 'RLD-04' extensions/pi-claude-marketplace/edge/handlers/tools.ts` returns `0`.
    - `grep -c 'all four `resources' extensions/pi-claude-marketplace/orchestrators/reconcile/README.md` returns `0`.
    - `git diff --stat extensions/` for this task shows comment and documentation lines only — no executable line changed.
    - The full check runs green with the companion-package environment variable exported; the observed exit code is recorded in the plan summary.
    - `pre-commit run --files <every file this plan changed>` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>Four source-comment sites state the current behavior with live anchors; the full check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> reader's mental model of the contract | prose is the only authority most readers consult before changing rendering |
| catalog fenced blocks -> byte-equality gate | catalog blocks are executable contract, not description |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-17 | Repudiation | the documentation sweep | high | mitigate | Every named defect is corrected in place, never deleted; the byte-equality gate and the retired-vocabulary guard both re-run, so a prose edit that shrinks or contradicts the shipped contract fails a test rather than passing as a correction |
| T-98-18 | Tampering | catalog fenced blocks | medium | mitigate | The acceptance criteria require the diff to show no change inside any annotated fenced block; the byte-equality driver fails on a single stray character |
| T-98-19 | Information disclosure | examples added to the catalog or the design document | low | mitigate | Examples use fixture names only; run the filesystem secret scan over every changed path before committing, per the worktree commit protocol |
| T-98-SC | Tampering | npm/pip/cargo installs | low | accept | This phase runs no package-manager install task; RESEARCH.md records the Package Legitimacy Audit as "Not applicable" |
</threat_model>

<verification>
- `node --test "tests/architecture/*.test.ts"` passes — in particular the catalog byte-equality gate, the retired-vocabulary guard, and the new no-expansion gate.
- The full check runs green with the companion-package environment variable exported; the exit code is captured directly.
- `pre-commit run --files <changed files>` passes for every changed path.
- `git diff --stat extensions/` shows comment-only changes for this plan.
</verification>

<success_criteria>
- All ten named DOC-08 defects are corrected, plus the three additional falsified statements found in the same sections.
- The design document's decision flowchart is redrawn to the current path per D-98-07.
- The disabled-state repair is documented: the disabled token covers the partial case, and the reconcile prose states the single-axis predicate.
- No documentation defect was resolved by deletion.
- The phase gate is green.
</success_criteria>

## Artifacts this phase produces (Plan 06)

**Modified documentation**
- `docs/output-catalog.md` — corrected reasons-brace and soft-dependency-marker variant enumerations, two added rows in the plugin status token reference table, corrected manifest-absent inventory prose, aligned fold-divergence wording
- `docs/prd/pi-claude-marketplace-prd.md` — corrected manifest-failure row, redrawn decision flowchart, corrected glyph roster, glyph meanings, and token set
- `extensions/pi-claude-marketplace/orchestrators/reconcile/README.md` — single-axis disabled-predicate prose

**Modified source comments (no executable change)**
- `extensions/pi-claude-marketplace/shared/notify-reasons.ts` — corrected reason-set member count in both header sentences
- `extensions/pi-claude-marketplace/shared/notify.ts` — corrected list-inventory-row reasons claim at both sites
- `extensions/pi-claude-marketplace/edge/handlers/tools.ts` — three retired anchors replaced with live ones or with anchor-free prose

**New files / catalog states / tokens / tests:** none.

<output>
Create `.planning/phases/98-lifecycle-regression-and-contract-documentation/98-06-SUMMARY.md` when done
</output>
