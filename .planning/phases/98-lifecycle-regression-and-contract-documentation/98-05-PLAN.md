---
phase: 98-lifecycle-regression-and-contract-documentation
plan: 05
type: execute
wave: 3
depends_on: ["98-03"]
files_modified:
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/marketplace/update.test.ts
autonomous: true
requirements: [LIFE-05, LIFE-06]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All three plugin-update enumeration paths — targeted, marketplace-bulk, and global-bulk — render the same `(skipped) {not in manifest}` row at warning severity for an installed record whose entry is absent from a manifest that loaded successfully."
    - "Running the same update twice over the same state renders a byte-identical notification the second time: the skip arm is idempotent."
    - "The skip arm writes no state — the installation record is byte-identical before and after, so an interrupted run cannot leave a partial record."
    - "Marketplace autoupdate renders `(skipped) {not in manifest}` for the same record, proven twice: once at the cascade mapper with a stubbed plugin-update function, and once end-to-end through the real single-plugin update so the origin in the shared preflight is covered too."
    - "The autoupdate cascade leaves the skipped plugin's installation record byte-identical, so a repeated marketplace update is a fixed point for that plugin."
    - "No test in this plan changes the process working directory — the end-to-end autoupdate case uses user scope with a hermetic home instead, because the single-plugin update reads the process working directory itself."
  artifacts:
    - tests/orchestrators/plugin/update.test.ts
    - tests/orchestrators/marketplace/update.test.ts
  key_links:
    - "enumerateTargets (three target shapes) -> runThreePhaseUpdate -> preflightUpdate's absent-entry arm (the single origin of the skip)"
    - "cascadeAutoupdates -> the injected plugin-update function -> outcomeToCascadePluginMessage (the re-narrowing half)"
  prohibitions: []
---

<objective>
Pin LIFE-05 and LIFE-06 with characterization coverage of the manifest-absent skip.

LIFE-05: the targeted enumeration path is already covered end-to-end and pins the byte row; the
marketplace-bulk and global-bulk paths are not covered at all. All three converge on one preflight
arm, so the gap is exactly the two missing target shapes plus the idempotency and no-state-write
edges.

LIFE-06: the autoupdate cascade is covered in two halves — the mapper re-narrowing (cheap and
deterministic, with a stubbed plugin-update function) and the origin (end-to-end through the real
single-plugin update). D-98-13 names the re-narrowing explicitly, and the file that owns the
cascade path today is the marketplace update suite, not the autoupdate-flag suite.

Purpose: LIFE-05 and LIFE-06 describe behavior the code already exhibits; the deliverable is the
coverage that keeps it exhibited.
Output: two new cases in the plugin update suite and two in the marketplace update suite, plus a
backward-compatible widening of that suite's path-marketplace seed helper.

Tracer exemption: this plan adds no production behavior — it characterizes paths that already ship
end-to-end. The end-to-end autoupdate case in Task 3 is itself the deepest vertical slice available
and is sequenced last because it depends on the seed widening.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-CONTEXT.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-RESEARCH.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-PATTERNS.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-03-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LIFE-05 — the two uncovered enumeration paths plus the idempotency and no-write edges</name>
  <files>tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
tests/orchestrators/plugin/update.test.ts lines 60-110 (`makeCtx`, `withHermeticHome`,
`makePluginRecord`), lines 177-300 (`seedPathMarketplace` and `rewriteManifest`), lines 425-463
(the targeted case whose fixture and byte assertion the two new cases copy verbatim, varying only
the target argument);
extensions/pi-claude-marketplace/orchestrators/plugin/update.ts lines 168-176 (the three target
shapes of the discriminated union), lines 1034-1046 (the single origin of the skip), lines
2650-2730 (the two enumeration functions);
98-RESEARCH.md section "LIFE-05 — the three update enumeration paths" (which includes the
marketplace-bulk case written out in full).
  </read_first>
  <action>
Add two cases to the plugin update suite under a section-divider comment citing LIFE-05 and
D-98-13, copying the targeted case's fixture setup exactly — the same path-marketplace seed, the
same installed version, the same manifest rewrite that drops the entry — and varying only the
target argument: one with the marketplace-bulk shape naming the seeded marketplace, one with the
global-bulk shape at project scope. Assert the same notification body string and the same warning
severity as the targeted case; copy the expected string from that case rather than retyping it.
Both bulk shapes produce the same row bytes because the scope bracket rides the marketplace header
and the plugin-row bracket is suppressed when the plugin and marketplace scopes agree.

Add two edge assertions inside these cases rather than as separate cases. First, idempotency: call
the update a second time over the same state and assert the second notification body is byte-identical
to the first — a repeated update over a manifest-absent record must not drift. Second, no state
write: capture the installation record before the update and assert it is deeply equal after, so
the skip arm is proven to return before any state mutation and an interruption cannot leave a
partial record.

Title the cases with the `LIFE-05:` prefix.
  </action>
  <acceptance_criteria>
    - `grep -c 'test("LIFE-05:' tests/orchestrators/plugin/update.test.ts` returns `2`.
    - Both cases assert a notification body byte-identical to the string the existing targeted case pins, and both assert warning severity.
    - One case calls the update twice and asserts the two notification bodies are equal.
    - One case captures the installation record before and after and asserts deep equality.
    - `node --test tests/orchestrators/plugin/update.test.ts` passes with every pre-existing case unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts</automated>
  </verify>
  <done>All three enumeration paths are covered by byte assertions; the skip arm is proven idempotent and write-free.</done>
</task>

<task type="auto">
  <name>Task 2: LIFE-06 — the cascade mapper re-narrowing</name>
  <files>tests/orchestrators/marketplace/update.test.ts</files>
  <read_first>
tests/orchestrators/marketplace/update.test.ts lines 40-95 (`makeCtx` and the suite's
hermetic-home helper, which yields both a home and a cwd), lines 95-150
(`seedGithubMarketplace` — note its autoupdate option and the paired config write), lines 180-200
(`makePluginRecord`), lines 810-860 (the autoupdate cascade case whose injected plugin-update
shape this case copies);
extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts lines 520-600
(`cascadeAutoupdates` — note that only a throw is caught and converted; a skipped outcome flows
through untouched), lines 690-760 (`outcomeToCascadePluginMessage` and the skip re-narrowing);
98-RESEARCH.md section "LIFE-06 — autoupdate cascade skip".
  </read_first>
  <action>
Add one case to the marketplace update suite that seeds an autoupdate-enabled marketplace with one
installed plugin record and injects a plugin-update function returning a skipped outcome carrying
the recorded version and the manifest-absent reason — the exact outcome shape the shared preflight
produces for a record whose entry is gone. Assert the rendered cascade row carries the
`(skipped) {not in manifest}` form, and assert the installation record is deeply equal before and
after so the cascade is a fixed point for a skipped plugin.

This case pins the re-narrowing half of LIFE-06: the reason is already correct when it leaves the
preflight, and the cascade mapper must carry it through rather than collapse it into its permissive
fallback. Cite LIFE-06 and D-98-13 in the section-divider comment; title the case with the
`LIFE-06:` prefix.
  </action>
  <acceptance_criteria>
    - `grep -c 'test("LIFE-06:' tests/orchestrators/marketplace/update.test.ts` returns `1` after this task.
    - The case asserts a rendered body containing the skipped token and the manifest-absent reason brace, and asserts the installation record is deeply equal before and after.
    - `node --test tests/orchestrators/marketplace/update.test.ts` passes with every pre-existing case unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>The cascade mapper is proven to carry the manifest-absent reason through, not to re-derive it from a fallback.</done>
</task>

<task type="auto">
  <name>Task 3: LIFE-06 — the end-to-end autoupdate origin</name>
  <files>tests/orchestrators/marketplace/update.test.ts</files>
  <read_first>
tests/orchestrators/marketplace/update.test.ts lines 660-700 (`seedPathMarketplace` — the helper
to widen; note it is project-scoped and takes neither an autoupdate flag nor plugin records), lines
95-150 (`seedGithubMarketplace` — the autoupdate seeding shape including the paired config write
that the widened path helper must mirror), lines 60-90 (the hermetic-home helper);
extensions/pi-claude-marketplace/orchestrators/plugin/update.ts lines 540-560
(`updateSinglePlugin` — the function reads the process working directory itself, which is the
fixture hazard this task avoids);
extensions/pi-claude-marketplace/persistence/locations.ts lines 140-150 (user-scope locations
ignore the working directory entirely — the property the workaround relies on);
tests/orchestrators/plugin/update.test.ts lines 177-300 (`seedPathMarketplace` and
`rewriteManifest` in the sibling suite, for the manifest-drop shape).
  </read_first>
  <action>
Widen the marketplace suite's path-marketplace seed helper with three optional options — a scope,
an autoupdate flag, and a plugin-record map — each defaulting to today's behavior so every existing
caller is unaffected. When the autoupdate flag is supplied, mirror the github helper's paired
config write so the orchestrators observe the autoupdate truth through the merged scope config,
not just the state record.

Add the end-to-end case: inside the hermetic home, seed a user-scope path-source marketplace with
autoupdate enabled and one installed plugin whose entry the manifest declares, then rewrite the
manifest to drop the entry, then run the marketplace update injecting the REAL single-plugin update
function. Assert the rendered cascade row carries the same `(skipped) {not in manifest}` form the
mapper-level case pins, and assert the installation record is unchanged.

Use user scope deliberately. The single-plugin update resolves its own working directory, so a
project-scope fixture under a temporary directory would never be found. Never change the process
working directory to compensate: that setting is process-global and unsafe when cases in a file
run concurrently. Record the reason in a comment citing LIFE-06 so a later author does not
"simplify" it back to project scope.

Title the case with the `LIFE-06:` prefix.
  </action>
  <acceptance_criteria>
    - The marketplace suite's path-marketplace seed helper accepts optional scope, autoupdate, and plugin-record options, and every pre-existing call site compiles unchanged.
    - `grep -c 'test("LIFE-06:' tests/orchestrators/marketplace/update.test.ts` returns `2` after this task.
    - `grep -c 'process.chdir' tests/orchestrators/marketplace/update.test.ts` returns `0`.
    - The end-to-end case passes the real single-plugin update function (not a stub) and asserts the same skipped row form as the mapper-level case.
    - `node --test tests/orchestrators/marketplace/update.test.ts` passes; capture the exit code directly rather than through a pipe.
    - `npm run typecheck && npm run lint` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/update.test.ts && npm run typecheck</automated>
  </verify>
  <done>Both halves of LIFE-06 are covered — the origin in the shared preflight and the re-narrowing in the cascade mapper — without touching process-global state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace manifest (marketplace-authored) -> update preflight lookup | manifest content decides whether a record is skipped |
| test fixture -> developer filesystem and process globals | the suites seed state under a temporary home and must not mutate process-wide settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-14 | Tampering | fixture isolation for the end-to-end autoupdate case | medium | mitigate | Use user scope with the suite's hermetic home, since user-scope locations ignore the working directory; the acceptance criteria negative-grep for the process-global directory change, which would corrupt sibling cases under concurrent execution |
| T-98-15 | Information disclosure | rendered cascade row for a manifest-absent record | low | accept | The rendered reason is a closed-set literal; no manifest content or filesystem path is echoed into the row |
| T-98-16 | Tampering | the widened seed helper | low | mitigate | Every new option is optional and defaults to today's behavior; the acceptance criterion requires all pre-existing call sites to compile unchanged, so no existing case's fixture silently changes shape |
| T-98-SC | Tampering | npm/pip/cargo installs | low | accept | This phase runs no package-manager install task; RESEARCH.md records the Package Legitimacy Audit as "Not applicable" |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/update.test.ts` passes, with two `LIFE-05:` cases and two `LIFE-06:` cases added and no pre-existing case changed.
- `npm run typecheck && npm run lint && npm run format:check` all exit 0.
- `git diff extensions/` is empty for this plan — LIFE-05 and LIFE-06 are characterization only.
</verification>

<success_criteria>
- LIFE-05 spans all three enumeration paths with the byte row pinned on each, plus the idempotency and no-state-write edges.
- LIFE-06 covers the autoupdate cascade in both halves, with the origin proven through the real single-plugin update.
- No production file is touched by this plan.
</success_criteria>

## Artifacts this phase produces (Plan 05)

**Modified symbols**
- the marketplace update suite's path-marketplace seed helper — gains optional scope, autoupdate, and plugin-record options (all backward compatible)

**New test titles**
- `LIFE-05: …` — 2 cases in `tests/orchestrators/plugin/update.test.ts` (marketplace-bulk, global-bulk), carrying the idempotency and no-state-write assertions
- `LIFE-06: …` — 2 cases in `tests/orchestrators/marketplace/update.test.ts` (cascade mapper, end-to-end origin)

**New files / catalog states / tokens:** none.

<output>
Create `.planning/phases/98-lifecycle-regression-and-contract-documentation/98-05-SUMMARY.md` when done
</output>
