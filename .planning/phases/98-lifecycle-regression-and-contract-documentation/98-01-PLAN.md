---
phase: 98-lifecycle-regression-and-contract-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/reconcile/notify.test.ts
autonomous: true
requirements: [IN-07, WR-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A fresh reconcile install of a plugin whose hook handler declares a rewake message without asyncRewake renders `{orphan rewake}` on the installed row, exactly as the re-enable arm already does."
    - "A clean install (no orphan rewake, no degraded kinds, no staged agents or MCP servers) renders byte-identically to today — every new outcome field is omitted when empty (NREG-01)."
    - "A re-enable of a plugin that staged agents renders `{requires pi-subagents}` when the companion extension is absent, on BOTH the standalone `/claude:plugin enable` row and the reconcile projection row."
    - "The standalone enable row's severity composes the two raises: a malformed-frontmatter degrade is `warning` regardless of the companion probe (WARN-01), and a missing companion is `warning` regardless of malformed kinds (SEV-01); a clean dropped-kind-only re-enable stays `info` (SEV-03)."
    - "`install.ts` gains no import from `enable-disable.ts` — the shared signals interface lives in `orchestrators/plugin/shared.ts`, which both files already import."
    - "No member is added to REASONS, STATUS_TOKENS, PLUGIN_STATUSES, MARKETPLACE_STATUSES, or the ICON_ glyph exports by either carrier (D-98-05)."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
    - docs/output-catalog.md
    - tests/architecture/catalog-uat.test.ts
  key_links:
    - "install.ts installed-outcome return -> reconcile/apply.ts install arm -> reconcile/notify.ts installedRowFromOutcome (the IN-07 projection path)"
    - "runEnableBranch installCtx.stagedAgentNames/stagedMcpServerNames -> SetEnabledOutcome fresh arm -> freshEnableRow dependencies (the WR-06 standalone path)"
    - "EnableDisablePluginOutcome enabled arm -> reconcile/apply.ts enable arm -> enabledRowFromOutcome dependencies (the WR-06 reconcile path)"
  prohibitions:
    - "MUST NOT silently add a status token, reason token, glyph, or state-schema field to make a carrier work — an apparent need for one is surfaced to the operator as a blocker, never added (D-98-05)."
---

<objective>
Land the two Phase-97 review carriers that share one type: IN-07 (thread `orphanRewake`
through the install outcome so the reconcile cascade renders `{orphan rewake}` for a fresh
install exactly as it now does for a re-enable, per D-98-01) and WR-06 (stop hard-coding
`dependencies: []` on both enable arms so the soft-dep markers and the SEV-01 raise apply on
re-enable as they do on install, per D-98-02).

Both carriers widen the SAME degradation-signal shape, so they land together on one interface
move — that is the cheapest sequencing and it turns the next such asymmetry into a compile
error rather than a review finding.

Purpose: D-98-05 requires the four carriers to land BEFORE the COMPAT-01 gate is authored, so
the gate's enumeration pins capture the post-carrier closed sets. Neither carrier may mint a
new token or glyph.
Output: a shared `LedgerDegradationSignals` interface in `orchestrators/plugin/shared.ts`, an
`orphanRewake` field on the install outcome contract, dependency-bearing enable rows on both
arms, and one new catalog state with its byte-equality fixture.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-CONTEXT.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-RESEARCH.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end `{orphan rewake}` on a fresh reconcile install — one path only</name>
  <files>
extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts,
tests/orchestrators/reconcile/notify.test.ts
  </files>
  <read_first>
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts lines 96-140 (the
`EnableDegradationSignals` interface being moved) and 300-320 (the `kind: "fresh"` signal-spread
idiom this task copies);
extensions/pi-claude-marketplace/orchestrators/plugin/install.ts lines 218-252 (the
`InstallPluginOutcome` installed arm) and 1840-1870 (the installed return);
extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts lines 80-115
(`PluginInstalledOutcome` and the `degradedKinds` optional-field doc-comment pattern) and 165-180
(the enable outcome's intersection with the signals shape);
extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts lines 600-620 (install arm
outcome construction) and 685-700 (the enable arm spread to mirror);
extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts lines 470-570
(`installedRowFromOutcome` and `enabledRowFromOutcome`);
98-PATTERNS.md section "orchestrators/plugin/shared.ts + install.ts + reconcile/* (IN-07, D-98-01)".
  </read_first>
  <behavior>
    - Test 1: a reconcile-applied `plugin-installed` outcome carrying the orphan-rewake signal renders `{orphan rewake}` on the `(installed)` row, at `info` severity.
    - Test 2: the same outcome carrying BOTH the orphan-rewake signal and a malformed skill kind renders `{orphan rewake, malformed skill}` in that order, at `warning` severity.
    - Test 3: a clean install outcome (no signals) renders with no reason brace and `info` severity — byte-identical to the pre-change row.
  </behavior>
  <action>
Move the degradation-signal interface out of `enable-disable.ts` into the sibling
`orchestrators/plugin/shared.ts`, renamed `LedgerDegradationSignals`, keeping every existing
field and its doc comment verbatim. Re-export `EnableDegradationSignals` from `enable-disable.ts`
as a type alias so `apply-outcomes.ts` and `apply.ts` keep their current import sites. The move
target is `shared.ts` because `enable-disable.ts` already imports `runInstallLedger` from
`install.ts`; importing the interface the other way would close a module cycle that no lint rule
catches. Both files already import `./shared.ts`, so this direction adds no new edge.

Add an optional `orphanRewake?: boolean` to the shape if it is not already there (it is —
carried over with the move), then intersect `LedgerDegradationSignals` into
`InstallPluginOutcome`'s `installed` arm so the compiler forces the install arm and the enable
arm to carry the same fields.

In `install.ts`, at the installed return, add the omit-when-false spread that copies
`installCtx.resolved.orphanRewake` onto the outcome, beside the existing `degradedKinds`
derivation. Omit the field entirely when false so a clean install's outcome shape is unchanged
(`exactOptionalPropertyTypes` is on — never assign the field an undefined value).

In `apply-outcomes.ts`, add the matching optional field to `PluginInstalledOutcome` with a
block doc comment citing SURF-05 / D-63-08, written in the same register as the neighbouring
`degradedKinds` field's WARN-01 / D-86-03 comment.

In `apply.ts`, add the same omit-when-false spread to the install arm's outcome construction,
mirroring the enable arm.

In `reconcile/notify.ts::installedRowFromOutcome`, compose the reasons array the way
`enabledRowFromOutcome` already does: push the orphan-rewake token FIRST, then the malformed
tokens. The emit order is contractual and matches `install.ts` and the enable arm. Severity is
unchanged — the orphan-rewake token moves no severity channel; the malformed-kinds rule alone
decides `warning` versus `info`.

Add the three reconcile row tests in `tests/orchestrators/reconcile/notify.test.ts`, mirroring
the existing enable-arm orphan-rewake case. Title each with the `IN-07:` requirement prefix; cite
`D-98-01` in the one explanatory comment. Do not name planning artifacts in any comment or title.
  </action>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts` exports `interface LedgerDegradationSignals` with the `unsupported`, `orphanRewake`, and `degradedKinds` fields.
    - `grep -c 'from "./enable-disable.ts"' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns `0` (the cycle direction stays closed).
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts | grep -c 'orphanRewake'` returns at least `2` (both row composers read it).
    - `node --test tests/orchestrators/reconcile/notify.test.ts` passes and reports 3 new `IN-07:`-prefixed tests.
    - `npm run typecheck` exits 0.
    - No file under `extensions/` gains a comment or test title containing a phase, plan, wave, or milestone reference (`.claude/rules/typescript-comments.md`).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/notify.test.ts && npm run typecheck</automated>
  </verify>
  <done>The install arm and the enable arm read one shared signals shape; a fresh reconcile install renders `{orphan rewake}` end-to-end; a clean install row is byte-identical to before.</done>
  <reversibility rating="reversible">An interface move plus one optional outcome field; reverting is a mechanical rename with no persisted or rendered contract pinned to it beyond the existing catalog state.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Standalone enable row carries the staged agent and MCP counts</name>
  <files>
extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts,
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts,
docs/output-catalog.md,
tests/architecture/catalog-uat.test.ts,
tests/orchestrators/plugin/enable-disable.test.ts
  </files>
  <read_first>
extensions/pi-claude-marketplace/orchestrators/plugin/install.ts lines 1740-1760 (the
`dependencies` derivation) and 1805-1820 (the `successSeverity` composition through
`companionSeverity` + `softDepStatus`);
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts lines 240-320
(`runEnableBranch`, the ledger context, the `kind: "fresh"` return), 930-1040 (`dispatchOutcome`
and `freshEnableRow`), 1039-1160 (`composeOutcomeRow` and its `fresh` call site);
extensions/pi-claude-marketplace/shared/notify-reasons.ts lines 60-100 (`companionSeverity`);
docs/output-catalog.md around the `soft-dep-on-installed` state (the register and block shape to
mirror) and the `## /claude:plugin enable` section states;
tests/architecture/catalog-uat.test.ts lines 1-60 (fixture contract) and the `enable-fresh`
FIXTURES entry;
98-PATTERNS.md section "orchestrators/plugin/enable-disable.ts (WR-06, D-98-02)".
  </read_first>
  <behavior>
    - Test 1: a re-enable whose ledger staged one or more agents renders `{requires pi-subagents}` when the companion extension probe reports it absent, at `warning` severity.
    - Test 2: the same re-enable with the companion present renders no marker and stays `info`.
    - Test 3: a re-enable that staged agents AND produced a malformed skill degrade stays `warning` and renders both the malformed token and the companion marker — the two raises compose, neither replaces the other.
    - Test 4: a re-enable that staged neither agents nor MCP servers renders byte-identically to today.
  </behavior>
  <action>
Add two optional boolean signals to `LedgerDegradationSignals` in `shared.ts` — one for "the
ledger staged at least one agent" and one for "the ledger staged at least one MCP server" —
each with a doc comment citing SEV-01 / D-98-02. Optional booleans keep every existing producer
valid and keep an unaffected outcome byte-identical.

In `runEnableBranch`'s `kind: "fresh"` return, populate the two signals from the ledger
context's staged-name arrays using the same omit-when-false spread idiom the neighbouring
`orphanRewake` and `degradedKinds` spreads use. Read only the array LENGTH — the staged names
themselves must never reach a rendered row.

Extract a small helper (for example `enableRowDependencies`) that turns those two signals into
the `Dependency[]` array, mirroring the derivation in `install.ts`. Extract rather than inline:
`sonarjs/cognitive-complexity` is an error at 15 and `freshEnableRow` already carries several
branches.

In `freshEnableRow`, replace both hard-coded empty dependency arrays with the derived array, and
replace the malformed-only severity ternary with a composition of the two rules: the malformed
raise wins outright, otherwise the companion probe decides via `companionSeverity`. Do not
replace one rule with the other — take the stronger of the two. `freshEnableRow` is a pure
composer today and does not hold the Pi reference, so add a soft-dependency probe parameter to
`freshEnableRow` and to `composeOutcomeRow`, threaded from `dispatchOutcome`, which already
holds the factory Pi reference. Delete the now-false explanatory comment above the first
dependency array and replace it with a one-line comment citing SEV-01 and the current behavior.

Amend `docs/output-catalog.md` under the `## /claude:plugin enable` section with a new
`<!-- catalog-state: enable-soft-dep -->` block showing the re-enable row carrying the companion
marker, written in the register of the surrounding states, and add the matching `FIXTURES` entry
in `tests/architecture/catalog-uat.test.ts` keyed by that section and state, with the mock Pi
factory that reports the companion absent and the expected severity. The catalog amendment, the
fixture, and the row assertions ship in the SAME commit as the behavior change.

Add the four row assertions in `tests/orchestrators/plugin/enable-disable.test.ts`, titled with
the `WR-06:` prefix.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts | grep -c 'dependencies: \[\]'` returns `0`.
    - `grep -c 'companionSeverity' extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` returns at least `1`.
    - `docs/output-catalog.md` contains exactly one `<!-- catalog-state: enable-soft-dep -->` marker, inside the `## /claude:plugin enable` H2 section.
    - `node --test tests/architecture/catalog-uat.test.ts` passes with the new state paired to a fixture (an unpaired catalog state fails the driver).
    - `node --test tests/orchestrators/plugin/enable-disable.test.ts` passes and every pre-existing assertion in that file is unchanged except where a row now legitimately carries the new marker.
    - `npm run lint` exits 0 — in particular no `sonarjs/cognitive-complexity` error on `freshEnableRow` or `composeOutcomeRow`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/enable-disable.test.ts tests/architecture/catalog-uat.test.ts && npm run lint</automated>
  </verify>
  <done>Both standalone enable arms derive their dependency list from the ledger's staged counts; the companion marker renders; the SEV-01 and WARN-01 raises compose; the catalog and its byte-equality fixture ship in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile enable projection carries the same dependency list</name>
  <files>
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts,
extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts,
docs/output-catalog.md,
tests/architecture/catalog-uat.test.ts,
tests/orchestrators/reconcile/notify.test.ts
  </files>
  <read_first>
extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts lines 470-570 (both row
composers — note that `installedRowFromOutcome` already reads `outcome.dependencies` while
`enabledRowFromOutcome` hard-codes an empty array, and that this file's severity rule is the
malformed rule alone on BOTH arms);
extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts lines 585-670 (the install and
enable arms and their `dependencies` handling);
extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts lines 150-175
(`EnableDisablePluginOutcome`'s `enabled` arm — the orchestrated outcome that must carry the
signals across the boundary);
98-PATTERNS.md section "orchestrators/plugin/enable-disable.ts (WR-06, D-98-02)" and the
"Optional-field spread" shared pattern.
  </read_first>
  <behavior>
    - Test 1: a reconcile-applied enable outcome that staged agents renders `{requires pi-subagents}` on the projected row when the companion is absent.
    - Test 2: the same outcome on the partially-installed arm renders the companion marker alongside the dropped-kind tokens, in the contractual order.
    - Test 3: an enable outcome with no staged agents or MCP servers renders byte-identically to today.
  </behavior>
  <action>
Carry the two staged-count signals across the orchestrated boundary: `EnableDisablePluginOutcome`'s
`enabled` arm already intersects the shared signals shape, so populate the two new fields where
that outcome is built from the ledger result. Add the matching optional fields (or the derived
`dependencies` array) to `PluginEnabledOutcome` in `apply-outcomes.ts` with a doc comment citing
SEV-01 / D-98-02, and populate them in `apply.ts`'s enable arm with the omit-when-false spread,
mirroring the install arm immediately above it.

In `enabledRowFromOutcome`, replace both hard-coded empty dependency arrays with the derived
array, mirroring `installedRowFromOutcome` in the same file. Leave this file's severity rule
alone: the reconcile projection's install arm does not apply the companion raise either, and
parity with the sibling arm in the same file is the contract this carrier restores. Correct the
two doc comments in this file whose stated reason for the empty dependency array is no longer
true — they explain an absence the carrier removes.

If the projected reconcile enable row's bytes are covered by an existing catalog state under a
reconcile H2 section, amend that state and its `FIXTURES` entry in the same commit; if no state
covers the soft-dep-bearing reconcile enable row, add one. Determine which by running the
catalog byte-equality gate after the code change and reading its diff — do not guess.

Add the three row assertions in `tests/orchestrators/reconcile/notify.test.ts`, titled with the
`WR-06:` prefix.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts | grep -c 'dependencies: \[\]'` returns `0`.
    - `grep -c 'carries no staged-name counts\|carry declaresAgents' extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts` returns `0` (the falsified doc comments are corrected, not left).
    - `node --test tests/orchestrators/reconcile/notify.test.ts tests/architecture/catalog-uat.test.ts` passes.
    - `npm run typecheck` and `npm run lint` both exit 0.
    - `node --test "tests/architecture/*.test.ts"` passes — in particular the closed-set locks and the notify stamp/wire coverage gates are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/notify.test.ts "tests/architecture/*.test.ts" && npm run typecheck</automated>
  </verify>
  <done>Both enable row composers — standalone verb and reconcile projection — derive their dependency list from the same ledger signals; no closed set gained a member.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin manifest / hooks source -> ledger context | plugin-authored content decides the orphan-rewake and staged-name signals |
| orchestrator outcome -> reconcile projection -> notify renderer | the only path by which a signal becomes user-visible bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-98-01 | Tampering | `orchestrators/plugin/shared.ts` `LedgerDegradationSignals` | low | mitigate | The interface is type-only and lives in the module both files already import; the acceptance criterion asserts `install.ts` gains no import from `enable-disable.ts`, so no ESM initialization cycle is introduced |
| T-98-02 | Information disclosure | reconcile installed row `{orphan rewake}` | low | accept | The token is a fixed closed-set literal; no plugin-authored text enters the row |
| T-98-03 | Information disclosure | enable row dependency derivation | low | mitigate | Only the LENGTH of the staged-name arrays is read; staged agent and MCP server names never reach a rendered row |
| T-98-SC | Tampering | npm/pip/cargo installs | low | accept | This phase runs no package-manager install task; RESEARCH.md records the Package Legitimacy Audit as "Not applicable" |
</threat_model>

<verification>
- `node --test tests/orchestrators/reconcile/notify.test.ts tests/orchestrators/plugin/enable-disable.test.ts` passes.
- `node --test "tests/architecture/*.test.ts"` passes — the catalog byte-equality gate, the closed-set locks, and the partial-vocabulary guard all green.
- `npm run typecheck && npm run lint && npm run format:check` all exit 0.
- No new member appears in any closed set: the counts pinned by `tests/architecture/notify-closed-set-locks.test.ts` are unchanged.
</verification>

<success_criteria>
- IN-07 closed: a fresh install's reconcile row renders `{orphan rewake}`; the asymmetry with the re-enable arm is gone and the shared shape makes the next such asymmetry a compile error.
- WR-06 closed: both enable arms render the soft-dep markers and take the SEV-01 raise, with the WARN-01 malformed raise composed rather than replaced.
- One new catalog state with a paired byte-equality fixture, shipped in the same commit as its behavior change.
- Zero additions to REASONS, STATUS_TOKENS, PLUGIN_STATUSES, MARKETPLACE_STATUSES, or the glyph exports.
</success_criteria>

## Artifacts this phase produces (Plan 01)

**New symbols**
- `LedgerDegradationSignals` — interface, `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts`
- `EnableDegradationSignals` — retained as a type alias re-exported from `orchestrators/plugin/enable-disable.ts`
- `enableRowDependencies` (or equivalently named local helper) — `orchestrators/plugin/enable-disable.ts`
- `orphanRewake?: boolean` — new optional field on `InstallPluginOutcome`'s `installed` arm and on `PluginInstalledOutcome`
- two optional staged-count signals on `LedgerDegradationSignals`, `EnableDisablePluginOutcome`'s `enabled` arm, and `PluginEnabledOutcome`

**New catalog states**
- `enable-soft-dep` under `## /claude:plugin enable` in `docs/output-catalog.md`, with its `FIXTURES` entry in `tests/architecture/catalog-uat.test.ts`
- possibly one reconcile-section state for the soft-dep-bearing enable projection row (added only if the byte-equality gate shows the row is not already covered)

**New test titles**
- `IN-07: …` (3, `tests/orchestrators/reconcile/notify.test.ts`)
- `WR-06: …` (4, `tests/orchestrators/plugin/enable-disable.test.ts`; 3, `tests/orchestrators/reconcile/notify.test.ts`)

**New status tokens / reason tokens / glyphs:** none — forbidden by D-98-05.

<output>
Create `.planning/phases/98-lifecycle-regression-and-contract-documentation/98-01-SUMMARY.md` when done
</output>
