---
phase: 09-reinstall-edge-bulk-ux
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-05-14T09:55:24.771Z
---

<current_state>
Phase 09 Plan 09-03 is partially implemented and paused mid-plan. Plans 09-01 and 09-02 are committed (`eb15d03` and `2f0a833`). Current uncommitted Plan 09-03 code changes are in completion files only: `extensions/pi-claude-marketplace/edge/completions/data.ts`, `extensions/pi-claude-marketplace/edge/completions/provider.ts`, and `tests/edge/completions/provider.test.ts`.

The Plan 09-03 changes have not been validated yet. Do not assume `provider.test.ts`, typecheck, lint, or formatting are green.
</current_state>

<completed_work>

- Plan 09-01: Bulk reinstall orchestrator - Done and committed as `eb15d03 feat(09-01): add bulk reinstall orchestrator`.
- Plan 09-02: Reinstall edge handler/router/register wiring - Done and committed as `2f0a833 feat(09-02): wire reinstall edge command`.
- Plan 09-03 Task 1: Add reinstall completion mode and `--force` positional skipping - In progress. `data.ts` now includes reinstall mode, installed-only filtering, and `extractPositionals(tokens, booleanFlags = [])` support; provider tests for installed-only reinstall completions and `reinstall --force <TAB>` were added.
- Plan 09-03 Task 2: Add provider branches for reinstall refs and `--force` flags - In progress. `provider.ts` now includes reinstall in top-level completions, reinstall-only `--force`, boolean-flag positional extraction hookup, and a reinstall ref branch with `allowMarketplaceOnly: true`; provider tests were added for `rei`, marketplace-only `@`, multi-marketplace half completion, and reinstall-only `--force`.
</completed_work>

<remaining_work>

- Finish Plan 09-03 Task 2 validation/fixes: run the completion tests and fix any failures from the partial edits.
- Plan 09-03 Task 3: add failure-semantics coverage:
  - `PRL-16 :: reinstall per-marketplace manifest soft-fail preserves other marketplace completions`
  - `PRL-16 :: reinstall state load errors propagate`
  - Ensure terminal reinstall completions preserve required trailing-space/no-trailing-space behavior.
- Run `node --test tests/edge/completions/provider.test.ts`.
- Run `npm run typecheck` and likely `npm run lint -- --quiet` before commit.
- Write `.planning/phases/09-reinstall-edge-bulk-ux/09-03-SUMMARY.md` after validation passes.
- Pre-commit and commit only the Plan 09-03 files unless deliberately including docs/state changes.
- Then continue to Plan 09-04 docs/final validation/traceability.
</remaining_work>

<decisions_made>

- Reinstall completions are installed-only like update/uninstall because reinstall must not suggest absent plugins.
- `extractPositionals` now takes explicit `booleanFlags`; only reinstall passes `["--force"]`, preserving existing list/filter behavior for arbitrary flags.
- Reinstall uses `allowMarketplaceOnly: true` so `reinstall @<marketplace>` completion works like update's marketplace-wide target form.
- `--force` completion is reinstall-specific and must not appear for install, uninstall, update, list, ls, or marketplace heads.
</decisions_made>

<blockers>

- No technical blockers.
- Git hygiene warning: unrelated or pre-existing modified files remain (`.gitignore`, `CLAUDE.md`, `.planning/STATE.md`, `.planning/config.json`). Do not include them in the Plan 09-03 code commit unless intentionally committing those state/docs updates.
- Historical placeholder scan reports many old phase files with TBD/placeholder text; no Phase 09 placeholder file was found.
</blockers>

<context>
The intended approach is already in place: `data.ts` owns the generic reinstall completion mode and boolean-flag positional skipping, while `provider.ts` owns reinstall command branching and reinstall-only `--force` suggestions. The fresh agent should start by adding Task 3 tests, then validate. Expect possible test failures around exact top-level sorted labels or exact completion values because provider tests were edited but not run after the last changes.
</context>

<next_action>
Start with: add the two remaining PRL-16 failure-semantics tests to `tests/edge/completions/provider.test.ts`, then run `node --test tests/edge/completions/provider.test.ts` and fix any failures.
</next_action>
