---
phase: 08-atomic-reinstall-core
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-05-14T00:35:22.972Z
---

<current_state>
Phase 08 (Atomic Reinstall Core) is paused in Wave 3, Plan 08-04, Task 1: Create reinstall orchestrator API and preflight. Plans 08-01, 08-02, and 08-03 are complete, committed, summarized, and marked complete in ROADMAP/STATE before this pause. Plan 08-04 has only just started: `extensions/pi-claude-marketplace/orchestrators/types.ts` has uncommitted `ReinstallPluginPartition` and `ReinstallPluginOutcome` definitions. `orchestrators/plugin/reinstall.ts` and `tests/orchestrators/plugin/reinstall.test.ts` do not exist yet.
</current_state>

<completed_work>

- Plan 08-01: Lock-held manual-save transaction helper and no-network guard - Done (`edf0487`, `420cadc`, `7b42f67`). Provides `withLockedStateTransaction` and guards future `reinstall.ts` against Git/network references.
- Plan 08-02: Backup-backed skills and commands replacement helpers - Done (`fe34ddd`, `3589ff1`, `58fdd93`). Provides `replacePreparedSkills`/`rollbackSkillsReplacement`/`finalizeSkillsReplacement` and command equivalents with opaque WeakMap-backed handles.
- Plan 08-03: Backup-backed agents and MCP replacement helpers - Done (`2cac16e`, `74961b2`, `f86e1ec`). Provides agents replacement with default foreign-content block and `force: true` rollback protection, plus MCP replacement that restores previous `mcp.json` text/absence.
- Plan 08-04 Task 1: In progress. Started by adding `ReinstallPluginPartition` and `ReinstallPluginOutcome` to `extensions/pi-claude-marketplace/orchestrators/types.ts`; not committed.
</completed_work>

<remaining_work>

- Plan 08-04 Task 1: Create `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts`, `tests/orchestrators/plugin/reinstall.test.ts`, plugin/top-level barrel exports, and full installed-only cached-manifest preflight. Preserve old version; do not import/call network/Git helpers; self-exempt the target plugin during cross-plugin conflict guard; prepare all bridge handles before replacement.
- Plan 08-04 Task 2: Implement atomic replacement/state-save/rollback using replacement order skills -> commands -> agents -> MCP, `await tx.save()`, reverse rollback MCP -> agents -> commands -> skills, and manual recovery detail on rollback failures.
- Plan 08-04 Task 3: Add post-success completion-cache invalidation, plugin data dir deletion after state commit, warning-only cleanup/cache failures, reload hint, soft-dependency warnings, final exports/outcome fields.
- Phase-level verification/review/completion still not run.
</remaining_work>

<decisions_made>

- Use `withLockedStateTransaction` for reinstall because the orchestrator must rollback physical resources after state-save failure.
- Keep bridge replacement handles opaque; internals live in bridge-private WeakMaps.
- Agents replacement blocks foreign previous content by default; `force: true` applies only to this plugin's previous index rows and remains rollback-protected.
- MCP rollback restores exact previous `mcp.json` text when it existed and removes the file when it was absent.
</decisions_made>

<blockers>
- None. No human actions pending.
- Background processes: none.
- Current phase placeholder scan found no placeholder/TBD files. A broad scan across all historical phases reports old placeholder/TBD strings in older docs/plans; do not treat those as current Phase 8 blockers.
</blockers>

<context>
The mental model for Plan 08-04 is: reinstall is a dedicated single-plugin core, not uninstall+install and not update. It reads cached `marketplace.json` only, resolves the installed plugin, preserves the existing installed record version and installedAt, prepares all bridge handles first, then physically replaces via rollback-safe bridge helpers under a lock-held manual-save transaction. If replacement or `tx.save()` fails, rollback physical changes and leave old state/resources/data available. Only after successful state commit should completion cache invalidation and plugin data deletion run; those post-success cleanup failures are warning-only.

Uncommitted files at pause:
- `.planning/STATE.md`: modified timestamp/position drift from GSD tooling.
- `.planning/config.json`: only missing final newline according to diff; likely incidental.
- `extensions/pi-claude-marketplace/orchestrators/types.ts`: real Plan 08-04 Task 1 partial work (reinstall outcome types).
- `AGENTS.md`: untracked pre-existing memory/context file; do not assume it belongs to Phase 8 implementation.
</context>

<next_action>
Start with: inspect `extensions/pi-claude-marketplace/orchestrators/types.ts` and decide whether to keep the promoted reinstall outcome types. Then create `tests/orchestrators/plugin/reinstall.test.ts` and `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` for Task 1. Export `reinstallPlugin` from `orchestrators/plugin/index.ts` and `orchestrators/index.ts`. First verification command: `node --test tests/orchestrators/plugin/reinstall.test.ts tests/architecture/no-orchestrator-network.test.ts`.
</next_action>
