---
phase: 110-persistence-and-transaction
plan: 09
type: execute
wave: 3
depends_on: [110-08]
files_modified:
  - extensions/pi-claude-marketplace/persistence/state-io.ts
  - tests/persistence/state-io.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "loadState returns the exact default for a missing file and distinguishes malformed JSON, root schema failure, non-ENOENT read failure, null/unknown source, and post-normalization schema failure (D-04, D-07)."
    - "Stored string/path/GitHub/valid URL sources normalize through the public funnel, while invalid URL and raw unknown forms keep their exact public outcomes (D-05)."
    - "Legacy migration returns complete normalized state and persists exact bytes without polling; replaying those bytes is an exact no-op (D-06, D-08)."
    - "saveState validates before atomic replacement, isRecordedButDisabled is directly proved, and direct coverage/typecheck are complete (D-15)."
  artifacts:
    - path: extensions/pi-claude-marketplace/persistence/state-io.ts
      provides: "Removal of redundant post-migration non-object marketplace guard after 110-08 type refinement"
    - path: tests/persistence/state-io.test.ts
      provides: "Canonical direct owner for state schema, load, normalize, migrate, persist, and save behavior"
  key_links:
    - from: tests/persistence/state-io.test.ts
      to: extensions/pi-claude-marketplace/persistence/state-io.ts
      via: "direct imports of public validator, state values, predicate, load, and save"
      pattern: "STATE_VALIDATOR|isRecordedButDisabled|loadState|saveState"
    - from: extensions/pi-claude-marketplace/persistence/state-io.ts
      to: extensions/pi-claude-marketplace/persistence/migrate.ts
      via: "object-valued MigrationResult from prerequisite 110-08"
      pattern: "migrateLegacyMarketplaceRecords|persistMigratedState"
---

<objective>
Prove the complete durable state funnel from unknown bytes through validation, legacy normalization, source normalization, exact persistence, and replay.

Purpose: The most widely consumed durable-state module can change without hidden format, warning, timing, or replay regressions.
Output: One behavior-preserving redundant-guard removal plus a complete direct owner at 100 percent coverage.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish missing, valid, save, and migrated state paths</name>
  <files>extensions/pi-claude-marketplace/persistence/state-io.ts, tests/persistence/state-io.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - extensions/pi-claude-marketplace/persistence/migrate.ts
    - tests/persistence/state-io.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/atomic-json.test.ts
    - tests/shared/completion-cache.test.ts
    - tests/domain/source.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>After prerequisite 110-08 exposes object-valued MigrationResult.marketplaces, remove only state-io.ts's redundant post-migration non-object marketplace guard; preserve all other logic and exports per D-13. Rebuild the owner around direct imports of DEFAULT_STATE, STATE_VALIDATOR, isRecordedButDisabled, loadState, and saveState. Use independent literal documents for missing, schema-version-2 valid, schema-version-1 legacy, and save cases; compare complete returned state and exact saved bytes per D-02, D-04, D-05, and D-08. Add direct true/false isRecordedButDisabled cases and clone behavior without resolvedSha. Every filesystem callback creates and cleans its own directory per D-01 and D-03. Every runtime case uses exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows and module-scope type evidence follow D-16.</action>
  <reversibility rating="reversible">The removed branch is unreachable under the refined migration result; undo is pair-local and changes no accepted bytes.</reversibility>
  <verify>
    <automated>npm run typecheck &amp;&amp; node --test tests/persistence/state-io.test.ts</automated>
  </verify>
  <acceptance_criteria>Missing, valid, legacy-migrated, save, disabled, and clone paths compare complete independent values; only the redundant guard changes in production; all callers typecheck.</acceptance_criteria>
  <done>The central durable-state path is runnable from literal bytes to normalized value and exact save.</done>
</task>

<task type="auto">
  <name>Task 2: Close validation, source, I/O, and fire-and-forget replay branches</name>
  <files>tests/persistence/state-io.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - extensions/pi-claude-marketplace/persistence/migrate.ts
    - extensions/pi-claude-marketplace/domain/source.ts
    - tests/persistence/state-io.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/completion-cache.test.ts
    - tests/shared/fs-utils.test.ts
    - tests/domain/source.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add independent cases for malformed JSON, root validator detail, empty STATE_VALIDATOR.Errors fallback through public load/save with t.mock.method, raw unknown source string, null source, stored path source, stored GitHub source, valid stored URL, invalid stored URL, non-ENOENT read failure, and post-normalization schema failure. Assert every complete error name/message/cause/stable field and disk effect per D-04 and D-07. Replace polling/sleeps in the migration-persistence case with a pre-registered case-local fsPromises.watch iterator and AbortController cleanup: start watching before loadState, await the state.json event, then compare the complete returned normalized state and exact persisted bytes. Load the persisted file again through loadState and require complete equality with no further mutation/write per D-05, D-06, D-08, and D-12. Keep all corrupt/failure fixtures in fresh case-owned directories per D-01, exact lowercase phases per D-16, and no shared root, snapshots, platform skips, broad timeouts, reset hooks, or state readers per D-03 and D-13. Re-prove every baseline owner behavior per D-14 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/state-io.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Every measured branch executes, watcher-based persistence has no sleep/poll, the replayed state is an exact no-op, errors/effects are complete, and direct coverage plus typecheck pass.</acceptance_criteria>
  <done>The state I/O owner is hermetic, fixed-point, failure-explicit, and coverage-complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                       |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| state.json bytes → ExtensionState                     | Malformed, legacy, or malicious source values must be rejected or normalized before use.          |
| normalized state → fire-and-forget atomic persistence | Background persistence must emit exact durable bytes without hiding failures or leaking watchers. |

## STRIDE Threat Register

| Threat ID   | Category          | Component                          | Severity | Disposition | Mitigation Plan                                                                                 |
| ----------- | ----------------- | ---------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-110-09-01 | Tampering         | loadState validation/source funnel | high     | mitigate    | Pin parse/schema/source variants, post-normalization validation, and complete errors.           |
| T-110-09-02 | Denial of service | migration persistence watcher      | medium   | mitigate    | Use a case-local AbortController watcher, exact cleanup, no polling, and replay no-op evidence. |
| T-110-09-03 | Tampering         | saveState atomic write             | high     | mitigate    | Prove validation before exact atomic bytes and unchanged disk on refusal.                       |

Canon-security breadcrumb: stored-source validation, containment, and atomic persistence are canon ASVS controls; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `loadState` internals by removing only the redundant non-object marketplace guard enabled by 110-08; no new public symbol is created.
- Refines `tests/persistence/state-io.test.ts` as the canonical owner of `ExtensionState`, `STATE_VALIDATOR`, `DEFAULT_STATE`, `isRecordedButDisabled`, `loadState`, and `saveState`.
- Creates no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/state-io.test.ts` and `npm run typecheck`; both must pass.
</verification>

<success_criteria>

- Missing/null/unknown, every source form, legacy replay, errors, exact bytes, and save refusal are independently pinned.
- Fire-and-forget persistence uses deterministic watcher synchronization without sleeps.
- Production change remains behavior-preserving and pair-local.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-09-SUMMARY.md` when done.
</output>
