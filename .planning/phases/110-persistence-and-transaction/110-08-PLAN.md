---
phase: 110-persistence-and-transaction
plan: 08
type: execute
wave: 2
depends_on: [110-06]
files_modified:
  - extensions/pi-claude-marketplace/persistence/migrate.ts
  - tests/persistence/migrate.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "migrateLegacyMarketplaceRecords maps null, primitive, array, missing-map, invalid-map, invalid-row, and valid legacy inputs to complete deterministic results (D-04, D-05)."
    - "Non-object marketplace and plugin rows are filtered without leaking invalid values into MigrationResult.marketplaces."
    - "A normalized result replay is an exact fixed point with unchanged complete value and `mutated: false` (D-06)."
    - "persistMigratedState keeps in-memory state on write failure, emits the complete sanctioned warning, and preserves exact filesystem effects (D-07, D-08)."
  artifacts:
    - path: extensions/pi-claude-marketplace/persistence/migrate.ts
      provides: "Object-valued MigrationResult.marketplaces type matching existing runtime filtering"
    - path: tests/persistence/migrate.test.ts
      provides: "Canonical direct owner for legacy transforms, fixed-point replay, and best-effort persistence"
  key_links:
    - from: tests/persistence/migrate.test.ts
      to: extensions/pi-claude-marketplace/persistence/migrate.ts
      via: "direct pure migration and persistence calls"
      pattern: "migrateLegacyMarketplaceRecords|persistMigratedState"
    - from: extensions/pi-claude-marketplace/persistence/migrate.ts
      to: extensions/pi-claude-marketplace/persistence/state-io.ts
      via: "MigrationResult object-valued invariant consumed by 110-09"
      pattern: "MigrationResult"
---

<objective>
Prove legacy state normalization, invalid-row filtering, exact fixed-point replay, and best-effort persistence while making the existing object invariant explicit.

Purpose: Corrupt legacy rows cannot survive normalization, and migration failures cannot silently alter the returned state.
Output: Pair-local MigrationResult type refinement plus a complete direct migration owner.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
@.planning/phases/110-persistence-and-transaction/110-06-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Refine and prove one complete legacy normalization path</name>
  <files>extensions/pi-claude-marketplace/persistence/migrate.ts, tests/persistence/migrate.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/migrate.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/persistence/migrate.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/domain/source.test.ts
    - tests/shared/atomic-json.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Refine MigrationResult.marketplaces from `Record&lt;string, unknown&gt;` to the object-valued record shape already guaranteed after migrateLegacyMarketplaceRecords filters invalid rows; do not change runtime logic or add a test-only export per D-13. Rebuild the owner around a direct public import and one independent literal legacy state that proves default manifestPath/marketplaceRoot, plugin resources/enabled normalization, optional autoupdate scrubbing, complete marketplace/plugin values, `mutated: true`, and caller-owned input mutation behavior. Use complete literal expected results rather than shared fixture JSON or production builders per D-02 and D-05. Every runtime case uses exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows keep separate phases and `satisfies MigrationResult` remains module-scoped per D-16.</action>
  <reversibility rating="reversible">The type documents object rows already enforced at runtime; undo is local and has no persistence migration cost.</reversibility>
  <verify>
    <automated>npm run typecheck &amp;&amp; node --test tests/persistence/migrate.test.ts</automated>
  </verify>
  <acceptance_criteria>The complete legacy value normalizes through the public transform, MigrationResult exposes only object-valued rows, and all callers compile without runtime change.</acceptance_criteria>
  <done>The migration transform's central object invariant is executable and statically visible.</done>
</task>

<task type="auto">
  <name>Task 2: Close invalid rows, replay, and persistence warning branches</name>
  <files>tests/persistence/migrate.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/migrate.ts
    - extensions/pi-claude-marketplace/shared/atomic-json.ts
    - tests/persistence/migrate.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/domain/source.test.ts
    - tests/shared/atomic-json.test.ts
    - tests/shared/debug-log.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Replace shared legacy JSON fixtures with independent readable literal rows and add exact cases for root null/primitive/array, absent marketplaces, null/primitive/array marketplaces, a non-object marketplace row, and a marketplace with a non-object plugin row. Ensure the invalid plugin row closes both resource and enabled skip paths, and compare the whole MigrationResult for every row per D-02, D-04, and D-05. Replay a fully normalized result through migrateLegacyMarketplaceRecords and assert complete equality plus `mutated: false` per D-06. For persistMigratedState, use a fresh case-owned real directory for success and failure, replace console.warn only case-locally at the public seam, and assert exact persisted bytes or the complete warning arguments including path and failure text, unchanged in-memory value, and disk state per D-01, D-07, and D-08. Keep exact lowercase phases per D-16, with no sleeps, snapshots, generic helpers, or test-only production APIs. Re-prove direct coverage per D-14 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/migrate.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>Both measured non-object rows execute, every invalid/empty input has a complete result, replay is an exact fixed point, persistence warning/effects are complete, and coverage/typecheck pass.</acceptance_criteria>
  <done>Legacy migration and best-effort persistence are fixed-point, failure-explicit, and coverage-complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                             | Description                                                               |
| ---------------------------------------------------- | ------------------------------------------------------------------------- |
| unknown legacy JSON → normalized marketplace records | Invalid nested rows must not survive as trusted objects.                  |
| normalized state → best-effort atomic persistence    | A write failure may warn but must not corrupt or discard in-memory state. |

## STRIDE Threat Register

| Threat ID   | Category    | Component                       | Severity | Disposition | Mitigation Plan                                                                            |
| ----------- | ----------- | ------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------ |
| T-110-08-01 | Tampering   | migrateLegacyMarketplaceRecords | high     | mitigate    | Pin every root/map/row invalid category, object-only results, and exact normalized output. |
| T-110-08-02 | Repudiation | persistMigratedState            | medium   | mitigate    | Assert complete warning, cause text, retained state, and exact disk effect.                |

Canon-security breadcrumb: legacy-input validation and atomic write failure handling are canon ASVS controls; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines existing `MigrationResult.marketplaces` to an object-valued record matching current runtime behavior; no new public symbol is created.
- Refines `tests/persistence/migrate.test.ts` as the canonical owner of `migrateLegacyMarketplaceRecords`, `persistMigratedState`, and migration helper behavior reached through them.
- Creates no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/migrate.test.ts` and `npm run typecheck`; both must pass.
</verification>

<success_criteria>

- Unknown/null/empty and invalid nested rows, replay ordering, warning behavior, and exact bytes are explicit.
- The production change is type-only and behavior-preserving.
- Direct coverage is 100 percent with the locked lowercase owner structure.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-08-SUMMARY.md` when done.
</output>
