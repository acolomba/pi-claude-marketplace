---
phase: 110-persistence-and-transaction
plan: 01
type: execute
wave: 2
depends_on: [110-02]
files_modified:
  - tests/persistence/agents-index-io.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Missing agents-index input returns the complete empty schema-version-1 view, while malformed JSON, wrong versions, invalid envelopes, and ordinary read failures remain distinct public failures (D-04, D-07)."
    - "A corrupt row is dropped without discarding valid adjacent rows, including the empty validator-detail fallback (D-04)."
    - "Saving validates the complete independent document and replaces agents-index.json with exact contractual JSON bytes (D-02, D-08)."
    - "Every filesystem case owns a fresh temporary directory and cleanup, and the owner directly reaches 100 percent function, line, and branch coverage (D-01, D-15)."
  artifacts:
    - path: tests/persistence/agents-index-io.test.ts
      provides: "Canonical direct owner for loadAgentsIndex and saveAgentsIndex"
  key_links:
    - from: tests/persistence/agents-index-io.test.ts
      to: extensions/pi-claude-marketplace/persistence/agents-index-io.ts
      via: "direct .ts import and public load/save calls"
      pattern: "loadAgentsIndex|saveAgentsIndex"
    - from: extensions/pi-claude-marketplace/persistence/agents-index-io.ts
      to: extensions/pi-claude-marketplace/persistence/agents-index-schema.ts
      via: "compiled envelope and row validators proved by prerequisite 110-02"
      pattern: "AGENTS_INDEX_(ENTRY_)?VALIDATOR"
---

<objective>
Make the agents-index I/O owner prove the accepted file envelope, row-isolation, failure, and exact atomic-write contracts.

Purpose: Maintainers can change agents-index persistence without silently changing missing-file behavior, corruption isolation, or stored bytes.
Output: A normalized mirrored owner with complete direct coverage; production exports and behavior remain unchanged per D-13 and baseline PASS is not treated as completion per D-14.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the public agents-index load/save path</name>
  <files>tests/persistence/agents-index-io.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/agents-index-io.ts
    - extensions/pi-claude-marketplace/persistence/agents-index-schema.ts
    - tests/persistence/agents-index-io.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/atomic-json.test.ts
    - tests/shared/completion-cache.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the owner around direct imports of loadAgentsIndex and saveAgentsIndex. Use independent literal schema-version-1 documents and complete expected LoadedAgentsIndex values for missing, valid-empty, valid-single, valid-multiple, optional-originalModel, and atomic-save paths per D-02, D-04, and D-08. Each filesystem case must create its own fresh temporary directory, immediately register case-owned cleanup, and keep all inputs inside it per D-01 and D-03. Every runtime case must contain exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; reserve lowercase `// act & assert` for one assert.throws or assert.rejects expression only, keep generated rows separated into the three phases, and keep any satisfies evidence at module scope per D-16. Preserve the production source and exports unless a genuine behavior-preserving pair-local seam is proven necessary per D-13.</action>
  <verify>
    <automated>node --test tests/persistence/agents-index-io.test.ts</automated>
  </verify>
  <acceptance_criteria>The owner directly imports the paired source; missing, valid, and save paths compare complete independent values and exact bytes; every filesystem case has its own temporary directory and cleanup.</acceptance_criteria>
  <done>The public happy path from literal stored document through load and atomic save is runnable and exact.</done>
</task>

<task type="auto">
  <name>Task 2: Close file-level and row-level corruption branches</name>
  <files>tests/persistence/agents-index-io.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/agents-index-io.ts
    - extensions/pi-claude-marketplace/persistence/agents-index-schema.ts
    - tests/persistence/agents-index-io.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/completion-cache.test.ts
    - tests/shared/fs-utils.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add independent cases for malformed JSON with the original cause, wrong schemaVersion, non-object envelope, missing agents array, invalid save with unchanged disk state, mixed valid and corrupt rows, and the exact dropped-row index/detail text. Reach the measured non-ENOENT read branch by placing a directory at the index path, and reach the empty AGENTS_INDEX_ENTRY_VALIDATOR.Errors fallback through loadAgentsIndex with a case-local t.mock.method replacement; assert the complete `(no detail available)` corruption string per D-04 and D-07. Keep corrupt and partial-failure directories case-owned per D-01, compare complete results and exact bytes per D-08, and apply the exact lowercase phase contract to every runtime callback per D-16. Do not add snapshots, shared fixture roots, a generic helper directory, test-only exports, reset hooks, or state readers per D-03 and D-13.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/agents-index-io.test.ts</automated>
  </verify>
  <acceptance_criteria>The non-ENOENT read and empty validator-detail branches execute through the public loader; corrupt rows are isolated with complete messages; invalid saves leave disk unchanged; direct coverage reports 100 percent functions, lines, and branches.</acceptance_criteria>
  <done>The complete agents-index I/O contract is re-proved and the pair is coverage-complete per D-14 and D-15.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary                                  | Description                                                                   |
| ----------------------------------------- | ----------------------------------------------------------------------------- |
| local JSON file → loader                  | Persisted bytes may be malformed, wrong-versioned, or contain malicious rows. |
| validated in-memory index → atomic writer | Invalid objects or partial replacement must not become durable state.         |

## STRIDE Threat Register

| Threat ID   | Category          | Component       | Severity | Disposition | Mitigation Plan                                                                             |
| ----------- | ----------------- | --------------- | -------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-110-01-01 | Tampering         | loadAgentsIndex | high     | mitigate    | Prove parse/envelope failures and per-row validation isolation with complete literal cases. |
| T-110-01-02 | Denial of service | saveAgentsIndex | medium   | mitigate    | Prove validation before write, exact atomic bytes, and case-owned failure cleanup.          |

Canon-security breadcrumb: corrupt persisted JSON and atomic replacement are ASVS file-handling controls for later secure-phase verification; no bespoke values, safety, or ethics prohibition applies to this utility pair.
</threat_model>

## Artifacts this phase produces

- Refines `tests/persistence/agents-index-io.test.ts` as the canonical direct owner of `loadAgentsIndex`, `saveAgentsIndex`, and `LoadedAgentsIndex`.
- Creates no new public production symbol, no test-only API, and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/agents-index-io.test.ts`; it must pass with one complete direct LCOV record at 100 percent functions, lines, and branches.
</verification>

<success_criteria>

- File corruption, row corruption, missing input, invalid save, exact bytes, and cleanup are independently observable.
- All runtime cases use the locked lowercase phase structure and direct paired import.
- Production behavior and exports remain unchanged.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-01-SUMMARY.md` when done.
</output>
