---
phase: 110-persistence-and-transaction
plan: 12
type: execute
wave: 3
depends_on: [110-06, 110-08]
files_modified:
  - tests/transaction/with-state-guard.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A case-local real scope lock encloses load, callback mutation, optional save, and release; two contenders never overlap."
    - "Contention and retry use controlled entered/release promises with no sleep, timeout assumption, shared lock, or platform skip (D-11, D-12)."
    - "Load/save failures use only the existing public dependency seams, while acquisition/release Error and non-Error failures preserve complete public results and release effects (D-07, D-11)."
    - "Duplicate tx.save(), callback Error/non-Error, release-after-success, release-after-failure, lock-held mapping, final state, and retry outcome are independently complete."
    - "Every lock/filesystem case owns and cleans its fresh directory, directly imports the paired module, and reaches 100 percent direct coverage (D-01, D-15)."
  artifacts:
    - path: tests/transaction/with-state-guard.test.ts
      provides: "Canonical direct owner for lock lifecycle, explicit state transaction, contention, release, and retry"
  key_links:
    - from: tests/transaction/with-state-guard.test.ts
      to: extensions/pi-claude-marketplace/transaction/with-state-guard.ts
      via: "direct withStateGuard and withLockedStateTransaction calls"
      pattern: "withStateGuard|withLockedStateTransaction"
    - from: extensions/pi-claude-marketplace/transaction/with-state-guard.ts
      to: extensions/pi-claude-marketplace/persistence/locations.ts
      via: "case-local .state-lock path established by 110-06"
      pattern: "stateLockPath"
    - from: extensions/pi-claude-marketplace/transaction/with-state-guard.ts
      to: extensions/pi-claude-marketplace/persistence/state-io.ts
      via: "public loadState/saveState dependency defaults and injected failure seams"
      pattern: "loadState|saveState"
---

<objective>
Prove the locked state lifecycle, deterministic contention, exact release failures, and retry behavior without timing or platform dependence.

Purpose: Concurrent state changes cannot overlap or leak a lock, and persistence failures remain exact and retry-safe.
Output: A normalized direct guard owner at complete coverage with production unchanged unless a real pair-local behavior-preserving seam is proven necessary per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
@.planning/phases/110-persistence-and-transaction/110-06-SUMMARY.md
@.planning/phases/110-persistence-and-transaction/110-08-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish one complete real-lock state transaction</name>
  <files>tests/transaction/with-state-guard.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/transaction/with-state-guard.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/fs-utils.test.ts
    - tests/shared/notify-context.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the owner around direct imports of withStateGuard and withLockedStateTransaction. Establish a real case-local location bundle and lock path in one fresh temporary scope, run a successful callback that mutates state and calls tx.save(), and compare the complete callback result, load/save call log, exact final state bytes, lock disappearance, and a successful immediate retry per D-01, D-02, D-08, and D-11. Add the no-save and duplicate-tx.save paths with complete save logs and final states. Every runtime case uses exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows retain separate phases and type-only evidence remains module-scoped per D-16. Preserve production exports and use only real dependency injection per D-13.</action>
  <verify>
    <automated>node --test tests/transaction/with-state-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>The real lock encloses load/mutation/save/release, duplicate save has the exact public effect, lock cleanup is observable, and immediate retry succeeds with complete final state.</acceptance_criteria>
  <done>One public state transaction is proven end-to-end through a real lock and durable state.</done>
</task>

<task type="auto">
  <name>Task 2: Close deterministic contention, acquisition, release, and persistence failures</name>
  <files>tests/transaction/with-state-guard.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/transaction/with-state-guard.ts
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - extensions/pi-claude-marketplace/shared/errors.ts
    - tests/transaction/with-state-guard.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/fs-utils.test.ts
    - tests/shared/completion-cache.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Replace chmod/process-platform skip branches with deterministic case-local method replacement. Use the existing loadState/saveState dependency seams only for their failure cases, and use t.mock.method on proper-lockfile for ordinary Error and non-Error acquisition failures plus release failures after success and after a non-Error callback throw. Assert the complete returned value/error, StateLockHeldError fields for a real contender, cause identity, save log, release attempts, lock disappearance, retained/final state, and subsequent retry per D-07 and D-11. For real contention, resolve an `entered` promise inside the first callback after acquisition, hold it on a separate release promise, start the second transaction only after entered resolves, assert that it cannot enter, then resolve release and retry; use no setTimeout, sleep, shared lock, broad timeout, or elapsed-time assertion per D-12. Give every case its own temporary directory and cleanup per D-01 and D-03, exact lowercase phases per D-16, and no test-only APIs/reset hooks/state readers per D-13. Re-prove every old PASS/failure behavior per D-14 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/transaction/with-state-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>Contention is promise-controlled and non-overlapping, both acquisition shapes and both release positions execute, load/save failures and duplicate save are complete, no skip/sleep remains, retry succeeds, and direct coverage is 100 percent.</acceptance_criteria>
  <done>The lock/transaction owner is deterministic, platform-independent, leak-free, retry-safe, and coverage-complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                            |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| concurrent process/callback → case-local state lock | Only one transaction may enter load/mutate/save at a time.                                             |
| transaction body/load/save → release in finally     | Any Error or non-Error failure must preserve public evidence and release or report the lock correctly. |

## STRIDE Threat Register

| Threat ID   | Category          | Component                     | Severity | Disposition | Mitigation Plan                                                                       |
| ----------- | ----------------- | ----------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------- |
| T-110-12-01 | Tampering         | withScopeLock contention      | high     | mitigate    | Prove real contender non-overlap with controlled promises and successful retry.       |
| T-110-12-02 | Denial of service | acquisition/release lifecycle | high     | mitigate    | Pin Error/non-Error acquisition and release failures, cleanup, and no platform skips. |
| T-110-12-03 | Tampering         | explicit state save lifecycle | high     | mitigate    | Prove load/save failures, duplicate save, final bytes, and lock-held state.           |

Canon-security breadcrumb: lost-update prevention and lock cleanup are canon ASVS business-logic/file-handling controls; no bespoke values, safety, or ethics prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `tests/transaction/with-state-guard.test.ts` as the canonical owner of `withStateGuard`, `withLockedStateTransaction`, `LockedStateTransaction`, and their real lock lifecycle.
- Creates no new public production symbol, test-only API, generic helper, or external-API coverage artifact.

## Multi-source coverage audit

| Source            | ID                          | Feature or requirement                                                             | Plans                      | Status  | Notes                                                                                                  |
| ----------------- | --------------------------- | ---------------------------------------------------------------------------------- | -------------------------- | ------- | ------------------------------------------------------------------------------------------------------ |
| GOAL              | —                           | Direct proof that durable-state replay and transaction recovery stay stable        | 110-01..110-12             | COVERED | Twelve exact source-owner pairs cover all durable and transaction modules.                             |
| REQ               | MOD-03                      | All 12 persistence and transaction pairs complete the pair contract                | 110-01..110-12             | COVERED | Every plan carries MOD-03 and one exact pair.                                                          |
| RESEARCH          | P110-01..P110-12            | Owner-specific measured branch closures and direct commands                        | matching plan              | COVERED | Each plan names its measured cases and focused command.                                                |
| RESEARCH          | dependency waves            | 02/06/11 → 01/03/05/08/10 → 04/07/09/12                                            | all plans                  | COVERED | Waves are exact; 09 depends on 08, while 12 depends on stable 06 location and 08 migration behavior.   |
| RESEARCH          | no installs/API/schema push | Existing Node/TypeBox/atomic/lock stack only                                       | all plans                  | COVERED | No package, COVERAGE.md, external service, or ORM task is created.                                     |
| CONTEXT           | D-01..D-04                  | Case-owned filesystem, literals, concern-local support, all stored edge categories | 01,03,04,05,06,07,08,09,12 | COVERED | Empty/null/corrupt/invalid/missing cases remain explicit.                                              |
| CONTEXT           | D-05..D-08                  | Public migration paths, replay fixed point, complete failures, exact stored bytes  | 07,08,09                   | COVERED | Migration plans run transforms/loaders twice and compare complete effects.                             |
| CONTEXT           | D-09..D-10                  | Every ledger position and complete newest-first rollback evidence                  | 10,11                      | COVERED | Schedule rows pin own undo, reverse order, causes, partials, leaks, and final state.                   |
| CONTEXT           | D-11..D-12                  | Real lock paths/public persistence seams and controlled-promise timing             | 12                         | COVERED | Contention uses two promises and no sleep/skip.                                                        |
| CONTEXT           | D-13..D-16                  | Preserve exports, normalize PASS, direct ownership/coverage, lowercase phases      | 01..12                     | COVERED | Every task cites the pair-local production and locked test contract.                                   |
| EDGE PROBE        | adjacency                   | Whole-entry collision, separator names, and lock contenders                        | 04,06,12                   | COVERED | Explicit public collision outcomes; no silent merge/overlap.                                           |
| EDGE PROBE        | empty                       | Missing/empty/null maps, documents, state, and partial arrays                      | 01,03,04,05,07,08,09,11    | COVERED | Complete values rather than a generic backstop.                                                        |
| EDGE PROBE        | ordering                    | Exact writer bytes and newest-first compensation                                   | 01,03,05,07,08,09,10       | COVERED | Contractual key/byte order and full transaction log are explicit.                                      |
| PROHIBITION PROBE | —                           | Utility-phase must-NOT recall/precision                                            | all plans                  | COVERED | Routine engineering items dropped; canon security uses breadcrumbs; zero bespoke prohibitions survive. |
| ASSUMPTION DELTA  | —                           | Automated phase scan                                                               | —                          | COVERED | `detected: false`; no decision is recorded.                                                            |

<verification>
Run `npm run test:coverage:direct -- tests/transaction/with-state-guard.test.ts`; it must pass with 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Real lock lifecycle, contention, release, load/save failure, duplicate save, final state, and retry are complete.
- No sleep, platform skip, shared lock root, or test-only production surface remains.
- All four planning source types and all fallback edge categories have explicit plan coverage with no gaps.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-12-SUMMARY.md` when done.
</output>
