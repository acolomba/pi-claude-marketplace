---
phase: 110-persistence-and-transaction
plan: 10
type: execute
wave: 2
depends_on: [110-11]
files_modified:
  - tests/transaction/phase-ledger.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 21000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every meaningful forward failure position has an independent named case, including the failing phase's own undo path (D-09)."
    - "Failure compensation records the failing phase's own undo first and completed phases newest-first, with the complete operation log and final context state (D-10)."
    - "No-undo, own-undo success/failure, several reverse failures, both PathContainmentError sites, and a non-Error forward throw preserve exact public results, causes, partials, and leaks (D-07, D-10)."
    - "The owner directly imports phase-ledger.ts and reaches 100 percent function, line, and branch coverage (D-15)."
  artifacts:
    - path: tests/transaction/phase-ledger.test.ts
      provides: "Canonical schedule owner for runPhases, failure compensation, partials, and leaks"
  key_links:
    - from: tests/transaction/phase-ledger.test.ts
      to: extensions/pi-claude-marketplace/transaction/phase-ledger.ts
      via: "direct Phase schedule rows executed through runPhases"
      pattern: "Phase|runPhases"
    - from: extensions/pi-claude-marketplace/transaction/phase-ledger.ts
      to: extensions/pi-claude-marketplace/shared/path-safety.ts
      via: "loud PathContainmentError propagation at forward and undo sites"
      pattern: "PathContainmentError"
---

<objective>
Turn the phase ledger into an exhaustive deterministic compensation schedule with complete newest-first evidence.

Purpose: Install and reinstall workflows can rely on exact recovery order, partial reporting, and cause preservation at every failure position.
Output: A normalized direct owner at complete coverage; production source and exports remain unchanged per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish one forward failure through complete compensation</name>
  <files>tests/transaction/phase-ledger.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/transaction/phase-ledger.ts
    - extensions/pi-claude-marketplace/transaction/rollback.ts
    - tests/transaction/phase-ledger.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/fs-utils.test.ts
    - tests/shared/errors.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the owner around direct imports of Phase, RunPhasesResult, and runPhases. Establish one production-quality schedule with three named phases where the last forward operation fails: record every do and undo call, make the failing phase's own undo run first, then compensate completed phases newest-first. Compare the complete operation log, full RunPhasesResult, original error identity, rollbackPartials, leaks, and final mutable context as independently authored values per D-02, D-09, and D-10. Also retain a complete all-success case. Every runtime case uses exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated schedule callbacks keep those phases, and `satisfies readonly Phase&lt;Context&gt;[]` evidence stays module-scoped per D-16. Keep production unchanged per D-13.</action>
  <verify>
    <automated>node --test tests/transaction/phase-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>The tracer fails if own undo is omitted, any compensation order changes, a result field/cause/leak is lost, or final context differs.</acceptance_criteria>
  <done>One complete forward-failure path proves the ledger's entire compensation flow.</done>
</task>

<task type="auto">
  <name>Task 2: Enumerate every failure position and exceptional undo branch</name>
  <files>tests/transaction/phase-ledger.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/transaction/phase-ledger.ts
    - extensions/pi-claude-marketplace/transaction/rollback.ts
    - extensions/pi-claude-marketplace/shared/path-safety.ts
    - tests/transaction/phase-ledger.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/fs-utils.test.ts
    - tests/shared/completion-cache.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Define independent named schedule rows that fail every meaningful forward phase position rather than sampling first/middle/last. Include a phase without undo, failing phase own-undo success, own-undo ordinary failure, one and several completed-phase undo failures, multiple partial rows in exact newest-first order, PathContainmentError thrown by the failing phase's own undo, PathContainmentError thrown by a completed-phase undo, and a non-Error forward throw normalized to the exact public Error result. Each row must compare the full do/undo log, complete result, original or normalized error, every partial cause/leak, and final context per D-07, D-09, and D-10. Keep deterministic synchronous/Promise controls with no real sleeps per D-12. Put exact lowercase arrange/act/assert phases inside every generated callback per D-16, with no aggregate-only includes assertions, snapshots, or test-only production seams. Re-prove all existing behavior per D-14 and direct coverage per D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/transaction/phase-ledger.test.ts</automated>
  </verify>
  <acceptance_criteria>Every forward position and undo branch has an independent case, all complete newest-first logs/results/causes/leaks/final states match, and direct coverage is 100 percent.</acceptance_criteria>
  <done>The ledger's ordering and recovery contract is exhaustive and coverage-complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                          |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| phase callbacks → transaction ledger | Forward and undo callbacks may throw Error, non-Error, containment errors, or leak-bearing failures. |

## STRIDE Threat Register

| Threat ID   | Category    | Component                        | Severity | Disposition | Mitigation Plan                                                                                                |
| ----------- | ----------- | -------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-110-10-01 | Tampering   | runPhases compensation order     | high     | mitigate    | Enumerate every forward position and compare complete own-first/newest-first operation logs and final context. |
| T-110-10-02 | Repudiation | rollback partial/cause reporting | high     | mitigate    | Pin every structured partial, original cause identity, leak, and containment bypass.                           |

Canon-security breadcrumb: transaction integrity and cause preservation are canon ASVS business-logic controls; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `tests/transaction/phase-ledger.test.ts` as the canonical owner of `Phase`, `RunPhasesResult`, `RollbackPartial`, and `runPhases` schedules.
- Creates no new public production symbol and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/transaction/phase-ledger.test.ts`; it must pass at 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Every failure position, ordering relation, partial, cause, leak, and final state is exact.
- The ordering fallback probe is resolved by complete newest-first comparison.
- Production behavior and exports remain unchanged.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-10-SUMMARY.md` when done.
</output>
