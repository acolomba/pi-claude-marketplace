---
phase: 110-persistence-and-transaction
plan: 05
type: execute
wave: 2
depends_on: [110-06]
files_modified:
  - tests/persistence/config-write-back.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Marketplace and plugin patch/delete operations preserve unrelated entries and emit one complete validated physical config document."
    - "Marketplace cascade handles an absent plugin map and removes every matching flat plugin key without removing adjacent names."
    - "Marketplace-only, plugin-only, and create-absent-entry batches omit the other arm safely and perform one exact atomic write."
    - "Each case uses independent literal input and complete exact JSON bytes in a fresh cleanup-owned directory (D-01, D-02, D-08)."
  artifacts:
    - path: tests/persistence/config-write-back.test.ts
      provides: "Canonical owner for config entry patch, delete, cascade, and batch write-back"
  key_links:
    - from: tests/persistence/config-write-back.test.ts
      to: extensions/pi-claude-marketplace/persistence/config-write-back.ts
      via: "direct calls to every public write-back operation"
      pattern: "writeMarketplaceConfigEntry|deleteMarketplaceConfigEntryWithCascade|writePluginConfigEntry|deletePluginConfigEntry|writeBatchedConfigPatch"
    - from: extensions/pi-claude-marketplace/persistence/config-write-back.ts
      to: extensions/pi-claude-marketplace/persistence/config-io.ts
      via: "single saveConfig validation/containment/atomic-write backstop"
      pattern: "saveConfig"
---

<objective>
Prove config write-back patches, cascades, omitted batch arms, and exact single-write documents.

Purpose: Reconcile write-back cannot silently lose unrelated desired state or emit partially shaped config files.
Output: A normalized direct owner at complete coverage with the production module unchanged per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one marketplace patch through validated atomic bytes</name>
  <files>tests/persistence/config-write-back.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/config-write-back.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/config-write-back.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/atomic-json.test.ts
    - tests/persistence/config-io.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the owner around direct imports of all five write-back exports. Establish the tracer with writeMarketplaceConfigEntry against one fresh case-owned directory, an independent literal current config, an independent literal patch, and a complete expected physical document. Read the result bytes and compare the exact two-space JSON and trailing newline per D-01, D-02, and D-08. Every runtime case must use exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows retain those phases and type-only evidence remains module-scoped per D-16. Preserve production behavior and exports per D-13.</action>
  <verify>
    <automated>node --test tests/persistence/config-write-back.test.ts</automated>
  </verify>
  <acceptance_criteria>The marketplace patch travels through saveConfig to one exact complete file while preserving unrelated entries and case-owned cleanup.</acceptance_criteria>
  <done>One public patch path is proven end-to-end through durable bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Close cascade, omitted-batch-arm, delete, and creation branches</name>
  <files>tests/persistence/config-write-back.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/config-write-back.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/config-write-back.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/atomic-json.test.ts
    - tests/domain/source.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add independent cases for marketplace cascade delete with no plugin map, cascade with matching and adjacent nonmatching flat keys, plugin write, plugin delete, marketplace-only batch, plugin-only batch, and a batch that creates absent marketplace and plugin entries. For every case, compare the complete saved document and exact bytes, and observe one write rather than deriving expectations from the returned patch per D-02 and D-08. Create and clean a fresh directory inside each callback per D-01 and D-03; keep all generated callbacks in exact lowercase arrange/act/assert phases per D-16. Do not add shared fixtures, snapshots, generic helpers, or a test-only write counter; use public filesystem effects and existing pair-local seams only per D-13. Re-prove every existing owner behavior per D-14 and reach full direct coverage per D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/config-write-back.test.ts</automated>
  </verify>
  <acceptance_criteria>The absent plugin map and both omitted batch arms execute, create-absent and delete paths preserve unrelated entries, every output is a complete exact document, and direct coverage is 100 percent.</acceptance_criteria>
  <done>All public write-back operations and measured branch gaps are coverage-complete.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary                               | Description                                                            |
| -------------------------------------- | ---------------------------------------------------------------------- |
| desired-state patch → persisted config | Partial patches must not erase unrelated entries or bypass validation. |

## STRIDE Threat Register

| Threat ID   | Category          | Component                  | Severity | Disposition | Mitigation Plan                                                                             |
| ----------- | ----------------- | -------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-110-05-01 | Tampering         | cascade and batch patching | high     | mitigate    | Pin matching/adjacent keys, omitted maps, creation, deletion, and complete saved documents. |
| T-110-05-02 | Denial of service | repeated physical writes   | medium   | mitigate    | Prove each public operation produces one complete atomic document through saveConfig.       |

Canon-security breadcrumb: atomic validated write-back is canon file/business-logic security; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `tests/persistence/config-write-back.test.ts` as the canonical owner of all marketplace, plugin, cascade, and batched write-back exports.
- Creates no new public production symbol and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/config-write-back.test.ts`; it must report 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Patch, delete, cascade, omitted-arm, and create-absent behavior is exact and independently asserted.
- Every filesystem case owns its directory and cleanup.
- Production exports and behavior remain unchanged.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-05-SUMMARY.md` when done.
</output>
