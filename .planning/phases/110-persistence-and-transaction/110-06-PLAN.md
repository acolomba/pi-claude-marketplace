---
phase: 110-persistence-and-transaction
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/persistence/locations.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "locationsFor returns the complete frozen user and project ScopedLocations bundles with exact fixed filenames."
    - "Every safe derived marketplace/plugin/staging path remains inside its exact scope root."
    - "Separator-bearing, empty, and unsafe derived names reject rather than escaping or aliasing an adjacent path."
    - "PI_CODING_AGENT_DIR changes are restored by the owning case, and direct coverage is 100 percent (D-01, D-15)."
  artifacts:
    - path: tests/persistence/locations.test.ts
      provides: "Canonical owner for scoped location bundles and safe derived paths"
  key_links:
    - from: tests/persistence/locations.test.ts
      to: extensions/pi-claude-marketplace/persistence/locations.ts
      via: "direct locationsFor and derived-path calls"
      pattern: "locationsFor|sourcesStagingDir"
    - from: extensions/pi-claude-marketplace/persistence/locations.ts
      to: extensions/pi-claude-marketplace/shared/path-safety.ts
      via: "safe-name and containment checks"
      pattern: "assertSafeName|assertPathInside"
---

<objective>
Pin every scoped location and safe derived path, including separator-bearing rejection and environment restoration.

Purpose: State, config, index, and lock consumers can rely on one deterministic contained path bundle.
Output: A normalized direct path owner at complete coverage with no production change per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish complete user and project location bundles</name>
  <files>tests/persistence/locations.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - tests/persistence/locations.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/fs-utils.test.ts
    - tests/shared/path-safety.test.ts
    - tests/shared/types.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the owner around direct imports of locationsFor and every derived-path method on ScopedLocations. For user and project scopes, create independent literal complete expected bundles that pin scopeRoot, extensionRoot, state.json, .state-lock, agents-index.json, claude-plugins.json, claude-plugins.local.json, and staging roots. Capture PI_CODING_AGENT_DIR before mutation and register exact restoration on the same test context per D-01 and D-03. Every runtime case uses exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows repeat the phases and any satisfies evidence stays module-scoped per D-16. Preserve exports and behavior per D-13.</action>
  <verify>
    <automated>node --test tests/persistence/locations.test.ts</automated>
  </verify>
  <acceptance_criteria>Both scopes compare complete frozen bundles and the environment returns to its exact prior present-or-absent state.</acceptance_criteria>
  <done>The fixed location bundle path is proven for both public scopes.</done>
</task>

<task type="auto">
  <name>Task 2: Close staging, separator, empty, and containment boundaries</name>
  <files>tests/persistence/locations.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - tests/persistence/locations.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/fs-utils.test.ts
    - tests/shared/path-safety.test.ts
    - tests/domain/name.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Exercise every derived marketplace, plugin, source, backup, and staging path with an independent safe name and complete exact expected path. Call the measured sourcesStagingDir gap with a safe UUID-like value and assert exact containment beneath its staging root. Add empty, active-platform separator, dot-segment, and separator-bearing names that reject through the public methods, with edit-adjacent safe neighbors where useful; this explicitly resolves the adjacency/collision probe. Use node:path to form platform-aware separators and do not skip a platform. Keep exact lowercase phases per D-16, restore all mutable environment state case-locally per D-01, and add no shared root, generic helper, test-only API, or production reader per D-03 and D-13. Re-prove all baseline owner behavior per D-14 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/locations.test.ts</automated>
  </verify>
  <acceptance_criteria>sourcesStagingDir executes, all safe paths equal independent exact values within their roots, unsafe and separator-bearing names reject without platform skips, and direct coverage is 100 percent.</acceptance_criteria>
  <done>The complete path and containment contract is normalized and coverage-complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                 |
| ------------------------------------- | --------------------------------------------------------------------------- |
| scope/name inputs → filesystem paths  | Separator-bearing or unsafe names must not escape fixed user/project roots. |
| process environment → user scope root | A test or caller-visible environment change must not leak across cases.     |

## STRIDE Threat Register

| Threat ID   | Category               | Component                           | Severity | Disposition | Mitigation Plan                                                         |
| ----------- | ---------------------- | ----------------------------------- | -------- | ----------- | ----------------------------------------------------------------------- |
| T-110-06-01 | Elevation of privilege | derived path methods                | high     | mitigate    | Prove safe-name rejection and exact containment for every derived path. |
| T-110-06-02 | Tampering              | PI_CODING_AGENT_DIR scope selection | medium   | mitigate    | Capture and restore exact prior environment state in the owning case.   |

Canon-security breadcrumb: path traversal and environment containment are canon ASVS controls; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `tests/persistence/locations.test.ts` as the canonical owner of `locationsFor`, `ScopedLocations`, and all derived path methods including `sourcesStagingDir`.
- Creates no new public production symbol and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/locations.test.ts`; it must pass at 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Complete scope bundles and every derived path are independently exact.
- Unsafe/empty/separator inputs reject without a platform skip.
- Environment ownership and lowercase phases satisfy D-01 and D-16.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-06-SUMMARY.md` when done.
</output>
