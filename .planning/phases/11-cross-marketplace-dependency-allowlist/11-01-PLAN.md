---
phase: 11-cross-marketplace-dependency-allowlist
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/manifest.ts
  - tests/domain/manifest.test.ts
  - tests/orchestrators/marketplace/add.test.ts
autonomous: true
requirements: [XMKT-01]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An absent allowCrossMarketplaceDependenciesOn field and an empty array both represent an empty allowlist."
    - "A present scalar, null, object, or mixed array invalidates the marketplace manifest with an error naming allowCrossMarketplaceDependenciesOn."
    - "Every string entry is retained verbatim, including duplicates, empty strings, and Unicode/control-containing values."
    - "Marketplace add uses the same cached compiled validator and creates no record for a malformed policy."
  artifacts:
    - path: extensions/pi-claude-marketplace/domain/manifest.ts
      provides: "One validated optional string-array field for later info and install consumers."
      contains: allowCrossMarketplaceDependenciesOn
    - path: tests/domain/manifest.test.ts
      provides: "Complete non-coercing schema matrix through the real cached loader."
    - path: tests/orchestrators/marketplace/add.test.ts
      provides: "Real command acceptance and field-naming rejection tracer."
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
      to: extensions/pi-claude-marketplace/domain/manifest.ts
      via: "The actual marketplace-add operation uses loadMarketplaceManifest and its typed failure."
      pattern: loadMarketplaceManifest
---

<objective>
Validate the marketplace allowlist through the existing marketplace command per D-11-05 and the D-11-01 empty default.

Purpose: Malformed policies cannot become silent empty lists; valid strings retain their exact meaning.
Output: Compiled schema extension, command tracer, and the complete cached-loader value matrix.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-CONTEXT.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-RESEARCH.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-PATTERNS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-VALIDATION.md
@.planning/HANDOFF-upstream-dependency-parity.md
@.planning/phases/03-dependency-resolution/03-CONTEXT.md
@docs/dependency-resolution.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@skills/typescript-comments/SKILL.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Execution discipline

Read each edited file before changing it and use CodeGraph to trace the named owners and callers. Source and tests follow the four project TypeScript skills. Use real temporary files, public exports, independent expected values, and the existing notification boundary; no new package is needed. All commands below run from the repository root. Preserve the five operator-owned local changes listed in RESEARCH.md. Keep contract-anchor edits limited to coordinates for unchanged owner/member identities.

## Planning decisions and discovery

Discovery level 0: the cached TypeBox validator, typed info projection, renderer, test runner, and catalog harness already exist. Research dated 2026-09-23 supplies upstream evidence; there is no new library choice.

Assumption-delta: NO CHANGE. The detected word "another" describes identities already represented as plugin@marketplace and marketplace-scoped records. Multiple marketplaces require a policy input, not a persistence migration or new scope model.

API-coverage detector examined the Phase 11 ROADMAP section plus CONTEXT using detectApiIntegration from the installed api-coverage module: detected=false, signals=[]. The CLI invocation reported no_input despite supplied input; the same deterministic function was therefore called with the actual nonempty scope text. This phase edits an existing local extension and introduces no external API integration. UI frontend=false; no ORM schema files or UI contract.

## Artifacts this phase produces

- MARKETPLACE_SCHEMA.allowCrossMarketplaceDependenciesOn and the derived MarketplaceManifest optional string[] field.
- No new production or test files.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject a malformed policy through the existing marketplace command</name>
  <reversibility rating="costly">The published manifest contract changes per D-11-05; reversal needs coordinated schema, documentation, and test changes but no data migration.</reversibility>
  <files>extensions/pi-claude-marketplace/domain/manifest.ts, tests/domain/manifest.test.ts, tests/orchestrators/marketplace/add.test.ts</files>
  <read_first>
    Read all three files in files, plus the canonical phase context, REQUIREMENTS XMKT, docs/messaging-style-guide.md, shared/errors.ts, and the marketplace add owner reached by CodeGraph. Read the TypeScript and testing skills listed in context.
  </read_first>
  <behavior>
    - The actual marketplace-add operation accepts a local manifest carrying ["tools"] and reports the existing invalid-manifest failure for a scalar policy.
    - loadMarketplaceManifest accepts an absent field and ["tools"] through the real cached loader.
    - A present scalar is rejected with the field identified; Task 2 expands the complete value matrix.
  </behavior>
  <action>
    Per D-11-05, add Type.Optional(Type.Array(Type.String())) to MARKETPLACE_SCHEMA under allowCrossMarketplaceDependenciesOn. Keep Compile/Check, InvalidMarketplaceManifestError, and the existing cached loader as the only parser. Add no coercion, filtering, name validation, sorting, or normalization of entries; D-11-05 specifies strings, not the dependency-token name grammar. Absence remains optional in the parsed object; consumers use an empty policy for absence per D-11-01.
    Start with a failing hermetic marketplace-add case and paired absent/string-array loader cases. Use the existing add operation and temporary path-source marketplace so the tracer crosses command orchestration, disk read, validation, error classification, and notify. Assert the typed validation error and the field's validator path; capture the actual library-owned message once when setting the expected complete diagnostic rather than guessing it. Preserve cache semantics and existing unknown-field handling.
  </action>
  <acceptance_criteria>
    - The scalar fixture raises InvalidMarketplaceManifestError and identifies allowCrossMarketplaceDependenciesOn.
    - The absent-field and ["tools"] loader cases preserve their exact parsed shapes.
    - The marketplace-add tracer emits its existing invalid-manifest row and creates no marketplace record for the rejected input.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/domain/manifest.test.ts tests/orchestrators/marketplace/add.test.ts</automated>
    <fails_when>Non-zero exit, a nonzero fail count, or a zero-test summary.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest.ts</automated>
    <fails_when>Non-zero exit or the manifest source-test pair is absent from the coverage report.</fails_when>
  </verify>
  <done>The real marketplace command accepts a string array and rejects a scalar with the field named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the complete non-coercing string-array contract</name>
  <files>extensions/pi-claude-marketplace/domain/manifest.ts, tests/domain/manifest.test.ts</files>
  <read_first>
    Read both edited files, tests/orchestrators/marketplace/add.test.ts, 11-CONTEXT.md D-11-01/05, 11-RESEARCH.md's schema evidence, and the source/test skills in context.
  </read_first>
  <behavior>
    - Absence, [], duplicate strings, empty strings, and Unicode/control-containing strings are accepted without changing their values or order.
    - Null, false, numbers, objects, scalar strings, and arrays containing a number or null fail with the field identified.
  </behavior>
  <action>
    Expand the paired cached-loader tests to every value shape above, per D-11-05. Compare complete parsed arrays and typed InvalidMarketplaceManifestError outcomes with the actual validator diagnostic recorded by Task 1. Preserve duplicates, empty strings, arbitrary Unicode, controls, and strings outside the dependency-token name grammar; the manifest contract specifies string entries. Do not add coercion, filtering, sorting, normalization, cache bypasses, or a second parser.
    Keep absent and empty behavior explicit per D-11-01. Refine MARKETPLACE_SCHEMA only if a matrix case exposes a defect, then repeat the real marketplace-add tracer so command classification cannot drift.
  </action>
  <acceptance_criteria>
    - Every invalid present shape raises InvalidMarketplaceManifestError and identifies allowCrossMarketplaceDependenciesOn.
    - All valid arrays survive the cached loader unchanged, including values that cannot name a valid dependency marketplace.
    - The cached loader and marketplace command share the same compiled validator.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/domain/manifest.test.ts tests/orchestrators/marketplace/add.test.ts</automated>
    <fails_when>Non-zero exit, a nonzero fail count, or a zero-test summary.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest.ts</automated>
    <fails_when>Non-zero exit or the manifest source-test pair is absent from the coverage report.</fails_when>
  </verify>
  <done>The complete D-11-05 matrix is tested without narrowing the set of valid string entries.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Untrusted marketplace JSON to cached manifest | A present malformed value must be rejected without coercion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-11-01 | Tampering | domain/manifest.ts | high | mitigate | Tasks 1-2 use the compiled string-array schema, real command rejection, and the complete malformed-value matrix. |

ASVS L1; unresolved high or critical threats block completion. No package install or external setup.
</threat_model>

<verification>
Run each task's focused tests and the direct manifest coverage gate, then npm run check after this wave. Preserve and report operator-owned configuration formatting failures accurately, and run later scripts independently if that baseline blocks the chain.
</verification>

<success_criteria>
D-11-05 is implemented, absence retains D-11-01's empty meaning, and both command and loader use one typed parser.
</success_criteria>

<output>
Create .planning/phases/11-cross-marketplace-dependency-allowlist/11-01-SUMMARY.md with test evidence and exact limitations.
</output>
