---
phase: 11-cross-marketplace-dependency-allowlist
plan: "05"
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - extensions/pi-claude-marketplace/domain/dependency-closure.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.ts
  - tests/domain/dependency-closure.test.ts
  - tests/orchestrators/plugin/install-cascade.test.ts
  - tests/orchestrators/plugin/install-flow.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.messaging.ts
  - tests/orchestrators/plugin/install-cascade.messaging.test.ts
  - tests/shared/notify-reasons.test.ts
  - scripts/check-unused-type-members.contracts.json
autonomous: true
requirements: [XMKT-01, XMKT-02]
estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A direct install uses only its root marketplace's parsed allowlist for every uninstalled foreign edge; being added alone grants no permission."
    - "Absent and empty allowlists permit same-root edges and refuse new foreign edges before dependency catalog lookup or materialization."
    - "Every edge is recorded before the installed check; installed dependency ranges remain merged and existing version checks still run."
    - "A recorded foreign dependency is exempt from the new policy even when its marketplace is absent; enabled records remain walls and disabled records retain direct install's existing read-through/re-enable behavior."
    - "For A@alpha -> B@beta -> C@gamma, alpha must allow both beta and gamma; beta's own list cannot authorize C during that direct install."
    - "A policy refusal changes no persistent state, config, plugin files, or marketplace clones and reports cross-marketplace plus both remedies."
    - "Marketplace matching is exact and case-sensitive; strings are neither trimmed nor normalized."
  artifacts:
    - path: extensions/pi-claude-marketplace/domain/dependency-closure.ts
      provides: "Explicit install policy and a cross-marketplace failure arm after installed-first recording."
      contains: DependencyInstallPolicy
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.ts
      provides: "Required root policy plus separate recorded and traversal key sets."
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts
      provides: "Cached, scope-aware root policy selection at both cascade entry points."
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.messaging.ts
      provides: "Structured policy failure converted into the locked reason and remedies."
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts
      to: extensions/pi-claude-marketplace/domain/manifest.ts
      via: "resolveInstallMarketplaceSource followed by loadMarketplaceManifest, using the exact selected manifest."
      pattern: allowCrossMarketplaceDependenciesOn
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.ts
      to: extensions/pi-claude-marketplace/domain/dependency-closure.ts
      via: "Required rootAllowedMarketplaces and full recorded keys become installPolicy."
      pattern: installPolicy
    - from: extensions/pi-claude-marketplace/domain/dependency-closure.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.messaging.ts
      via: "Structured cross-marketplace failure carries the policy root independently of the declarer."
      pattern: cross-marketplace
---

<objective>
Expand the proven direct-install path across transitive, installed/disabled, and exact-match cases, and verify the production refusal composer per D-11-01, D-11-02, D-11-03, and D-11-06.

Purpose: A plugin declaration cannot expand the root marketplace's allowed sources through an intermediary marketplace.
Output: Real transitive-install tracer, complete graph/installed regression cases, exact failure projection, and the remaining paired reason assertions.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-CONTEXT.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-RESEARCH.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-PATTERNS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-VALIDATION.md
@.planning/HANDOFF-upstream-dependency-parity.md
@.planning/phases/03-dependency-resolution/03-CONTEXT.md
@docs/dependency-resolution.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@skills/typescript-comments/SKILL.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-02-SUMMARY.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-03-SUMMARY.md
@.planning/phases/09-reload-installs-missing-dependencies/09-CONTEXT.md
@extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-04-SUMMARY.md
</context>

## Interface and ownership decisions

CodeGraph found two production consumers of resolveDependencyClosure: runInstallCascade and resolveEnableCascade. Enable deliberately passes an empty traversal installedKeys set so it can traverse already-installed disabled records. Installing new plugins is outside enable's behavior. Therefore the shared closure receives an optional, explicit installPolicy; the install cascade itself requires rootAllowedMarketplaces and always supplies that policy. An omitted policy is for the existing non-installing enable traversal only, not a permissive install default.

DependencyInstallPolicy has readonly allowedMarketplaces: ReadonlySet<string> and readonly recordedKeys: ReadonlySet<string>. ResolveDependencyClosureOptions gains readonly installPolicy?: DependencyInstallPolicy. WalkContext gains the parsed rootMarketplace. InstallCascadeOptions gains required readonly rootAllowedMarketplaces: ReadonlySet<string>. Existing installedKeys remains the traversal-stop set; policy.recordedKeys is the complete target-scope install inventory. The two sets have different jobs.

This separation follows D-11-02 and the published dependency document's explicit "A disabled plugin still counts as installed." Direct install still reads through and re-enables a disabled record, checking its newly missing descendants under the root policy. Reload still treats it as a wall per D-09-04. Version validation remains independent of the allowlist exemption.

The failure arm contains ok:false, reason:"cross-marketplace", key, requiredBy, marketplace, and rootMarketplace. That last field is the authority to name in the remedy, not necessarily the immediate declarer's marketplace.

The failure-union/caller contract is complete in 11-04. This plan expands its executable behavior and completes the additional paired reason assertions from 11-03. Narrow reads of the large install-flow test file to its existing direct-cascade and missing-dependency sections. Preserve all user-owned local changes.

## Artifacts this phase produces

- New real-install and owner regression cases for transitive root policy, installed/disabled records, guard order, exact strings, and failure projections.
- Additional paired notify-reasons assertions for cross-marketplace, retaining the existing completeness controls.
- DependencyInstallPolicy, ResolveDependencyClosureOptions.installPolicy, WalkContext.rootMarketplace, the cross-marketplace failure arm, InstallCascadeOptions.rootAllowedMarketplaces, and loadInstallRootAllowlist are existing 11-04 contracts; refine them only if a case exposes a defect.
- No new production symbol, file, error class, package, or persistence field.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove root authority, installed-record exemptions, and guard order</name>
  <files>extensions/pi-claude-marketplace/domain/dependency-closure.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.ts, tests/domain/dependency-closure.test.ts, tests/orchestrators/plugin/install-cascade.test.ts, tests/orchestrators/plugin/install-flow.test.ts</files>
  <read_first>
    Read all five edited files, plugin/enable-disable.ts, docs/dependency-resolution.md's installed-record section, Phase 3 D-03-10, Phase 9 D-09-04, and 11-PATTERNS.md's installed-first pattern.
  </read_first>
  <behavior>
    - A@alpha -> B@beta -> C@gamma rejects C when alpha allows only beta, even if beta allows gamma; alpha allowing both succeeds.
    - B in a foreign marketplace asking for a different plugin in alpha is allowed without listing alpha.
    - An installed foreign key skips permission and marketplace-known checks; every repeated edge still contributes its range.
    - A disabled recorded foreign key is exempt from permission but direct install traverses its declarations; a new unlisted child still fails. Reload's disabled key remains a wall.
    - Exact ["tools"] accepts tools; ["Tools"], [" tools"], and ["tools "] do not. Duplicate list entries do not change authorization.
  </behavior>
  <action>
    Start with a failing real-file install-flow case for A@alpha -> B@beta -> C@gamma. Through the real install operation, prove alpha allowing beta alone refuses C even when beta allows gamma; granting beta and gamma in alpha succeeds. Compare full state/config/artifact inventories on refusal and exact notify cause. This tracer reaches scope-selected manifest loading, closure policy, cascade orchestration, and notification.
    Expand the pure closure and cascade owner tests using public exports, whole structured outcomes, strict zero-interaction ports for forbidden lookup/materialization, and existing typed fixtures. Update all runInstallCascade test calls with an explicit rootAllowedMarketplaces set; old intentionally successful foreign cases grant their target, while same-marketplace cases use an empty set.
    Exercise policy present/absent paths, same-root and foreign paths, added and absent foreign targets, and both recorded sets. Pin denial precedence: an unlisted unknown foreign marketplace is cross-marketplace, while an allowlisted unknown target is marketplace-not-added. Installed enabled foreign keys remain skipped even if the target marketplace is missing; preserve recorded-version conflicts and range intersection. Pin the disabled-record distinction from the interface section, including fail-clean refusal of a newly missing descendant before any re-enable write.
    Keep enable traversal semantics unchanged: its no-install-policy path traverses existing records and reaches the existing enable missing-record handling. Do not add an allowlist read or install action to enable. Preserve cycle versus diamond behavior, root parsing, sha rejection, edge recording, and lookup order. Refine the small policy helper only where a behavioral test exposes a defect.
  </action>
  <acceptance_criteria>
    - Tests cover each graph and installed-state behavior listed above and compare the full failure payload including rootMarketplace.
    - Recorded ranges remain present when the installed check short-circuits authorization.
    - A blocked edge invokes neither its target lookup nor any install seam.
    - Existing enable and disabled re-enable tests retain their outcomes.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install-flow.test.ts</automated>
    <fails_when>Non-zero exit, a missing real A-to-B-to-C tracer result, or a zero-test summary.</fails_when>
    <automated>node --test tests/domain/dependency-closure.test.ts tests/orchestrators/plugin/install-cascade.test.ts tests/orchestrators/plugin/enable-disable.test.ts</automated>
    <fails_when>Non-zero exit, a nonzero fail count, or a zero-test summary.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/dependency-closure.ts extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.ts</automated>
    <fails_when>Non-zero exit or either requested pair is missing from the report.</fails_when>
  </verify>
  <done>Every new uninstalled edge answers to the correct root while existing installed, version, graph, and enablement semantics remain tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify refusal projection and the reason's paired classification contract</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.messaging.ts, tests/orchestrators/plugin/install-cascade.messaging.test.ts, tests/shared/notify-reasons.test.ts, scripts/check-unused-type-members.contracts.json</files>
  <read_first>
    Read all four edited files, tests/architecture/dependency-doc-agreement.test.ts, docs/dependency-resolution.md, extensions/pi-claude-marketplace/shared/notify-reasons.ts, the dependency-cross-marketplace catalog fixture, docs/messaging-style-guide.md, and the context's D-11-06. Trace cascadeFailureCause and composeCascadeFailureMessage callers before changing their mapping.
  </read_first>
  <behavior>
    - A transitive failure names its immediate declarer but edits the original cascade root's policy.
    - A failure whose key equals the cascade root emits one failed subject row, avoiding duplicate root rows.
    - The production composer and user documentation agree on the complete reason set.
  </behavior>
  <action>
    Exercise both exported failure projections, cascadeFailureCause and composeCascadeFailureMessage, with the new structured arm. Assert exact cause bytes, DependencyCascadeError identity/key, severity, no reload hint, and distinct-root versus same-root row behavior. Preserve the four separate name roles so a B->C failure under A tells the user to change A's marketplace policy.
    Reuse the exhaustive CLOSURE_FAILURES fixture and matching documentation row delivered by 11-04 and keep their agreement gate passing. Complete the additional notify-reasons paired-test work split out of 11-03: add the new token to applicable existing permitted/forbidden classification cases while retaining the completeness proof's missing/stray-partition controls. Add only meaningful type assertions or consumer behavior cases; do not duplicate TypeScript through runtime member enumeration. The earlier reason enrollment, ordering, count, and classification implementation stay in 11-03.
    Update only shifted type-member coordinates, using the real tool's owner/member output rather than guessed line arithmetic. Keep all coverage policies and exemptions unchanged.
  </action>
  <acceptance_criteria>
    - Both composer exports produce the catalog's reason and cause from the structured failure.
    - The exhaustive closure fixture covers cross-marketplace and the documentation failure-table set matches the production composer.
    - The paired notify-reasons checks include the new dependency reason while preserving existing permitted/forbidden shape controls.
    - No new coverage/type-member suppression is added.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/shared/notify-reasons.test.ts</automated>
    <fails_when>Non-zero exit, a classification mismatch, or a zero-test summary.</fails_when>
    <automated>node --test tests/orchestrators/plugin/install-cascade.messaging.test.ts tests/architecture/dependency-doc-agreement.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts</automated>
    <fails_when>Non-zero exit, a reason-set or byte mismatch, or a zero-test summary.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.messaging.ts</automated>
    <fails_when>Non-zero exit or either requested pair is absent from the report.</fails_when>
    <automated>npm run lint:type-members &amp;&amp; npm run lint:type-members:negative</automated>
    <fails_when>Non-zero exit or an unresolved contract anchor.</fails_when>
  </verify>
  <done>The actual refusal producers satisfy D-11-06 and the direct-install rule is documented without vocabulary drift.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Root marketplace policy to foreign dependency lookup | Availability cannot grant permission to execute new code. |
| Scope-local installed records to authorization | Existing installations and traversal state must remain distinct. |
| Closure failure to transaction phases | Denial must precede every persistent side effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-11-06 | Elevation of Privilege | dependency-closure.ts | high | mitigate | Task 1 proves fixed root authority, exact membership, and pre-lookup rejection through real A->B->C negative controls. |
| T-11-07 | Tampering | install-flow.ts policy loading | high | mitigate | The existing 11-04 loader stays authoritative; Task 1 drives it through real temporary manifests without bypassing validation. |
| T-11-08 | Tampering | install-cascade.ts | high | mitigate | Task 1 proves root-policy denial changes no state/config/artifact files before phases. |
| T-11-09 | Denial of Service | Installed/disabled traversal | medium | mitigate | Task 1 pins recorded exemption versus traversal stops and retains cycle/version controls. |
| T-11-10 | Spoofing | install-cascade.messaging.ts | medium | mitigate | Task 2 tests immediate declarer versus policy root, both correct remedies, and the paired reason classification. |

ASVS L1; high/critical findings block completion. D-11-03 and existing source containment remain enforced; no package-manager install.
</threat_model>

## Edge-probe resolution

XMKT-01 adjacency is explicit in the transitive, same-root return, installed/disabled, and diamond cases. XMKT-01 empty is explicit in omitted/empty policy cases. XMKT-01 ordering is explicit in recordEdge -> installed stop -> authorization -> known marketplace -> lookup, retaining ranges before every guard. XMKT-01 encoding is explicit in exact matching here and escaped display in 11-02.

Flagged assumption XMKT-02/unclassified remains unresolved in the deterministic report because its taxonomy did not classify the requirement. It is not auto-dismissed or converted into a backstop. D-11-02, the installed-record document, and the explicit owner tests above supply the implementation contract; verification must assess that evidence rather than infer success from the classifier.

<verification>
Run the focused tests and coverage gates, then npm run check at the wave boundary. Retain the prior no-orchestrator-network test; use existing offline fake transports, not a global fetch spy that misses isomorphic-git's HTTP path.
</verification>

<success_criteria>
XMKT-01 and XMKT-02 hold on direct installs; the missing-root cascade also receives its own policy, ready for original-edge authorization in 11-06.
</success_criteria>

<output>
Create .planning/phases/11-cross-marketplace-dependency-allowlist/11-05-SUMMARY.md.
</output>
