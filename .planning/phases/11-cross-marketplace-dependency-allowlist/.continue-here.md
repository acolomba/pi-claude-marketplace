---
context: phase
phase: 11-cross-marketplace-dependency-allowlist
task: 0
total_tasks: 3
status: paused
last_updated: 2026-09-23T10:35:46.975Z
---

<current_state>
Phase 10 is complete and committed. The project is in Phase 11's interactive
discussion step, before research or planning. The user selected option 4,
meaning all three open areas should be discussed: marketplace info output,
invalid allowlist field handling, and refusal message detail. No question in
those areas has been answered yet. Phase 11 contains only `.gitkeep`; it has no
CONTEXT.md, PLAN.md, research, verification, code edits, or implementation
commit.
</current_state>

<completed_work>

Completed tasks:

- Phase 10 was verified, reviewed clean, closed, and transitioned to Phase 11.
  The transition commit is `adf503e9`.
- Phase 11 requirements, roadmap decisions, upstream parity notes, architecture,
  and relevant source seams were inspected.
- The fixed behavior was separated from the remaining product choices.
- The user selected all three remaining discussion areas.

The important source findings are:

- `domain/manifest.ts` has one compiled `MARKETPLACE_SCHEMA` and one cached
  loader. It does not yet declare `allowCrossMarketplaceDependenciesOn`.
- `domain/dependency-closure.ts` records an edge before its guards. A dependency
  currently follows `already-installed -> marketplace-known -> cycle -> memo`.
  The new allowlist guard belongs after the installed check and before lookup.
- `orchestrators/marketplace/info.ts` reads the same cached marketplace manifest
  and currently projects only `description` from it.
- The upstream parity record pins the closed-set reason to `cross-marketplace`.
</completed_work>

<remaining_work>

- Discuss how `marketplace info` presents the allowlist, including absent and
  empty values.
- Discuss whether malformed field values invalidate the marketplace manifest or
  are tolerated in some defined way.
- Discuss the exact detail and remediation shown on a cross-marketplace refusal.
- Write the Phase 11 discussion artifacts only after those answers are complete.
- Continue the autonomous lifecycle through research, planning, execution,
  verification, review, and transition.
</remaining_work>

<decisions_made>

- The root plugin's marketplace owns the allowlist for all newly resolved
  cross-marketplace dependency edges. This is fixed by XMKT-01 and the roadmap.
- A missing `allowCrossMarketplaceDependenciesOn` field means an empty list.
- An already-installed dependency satisfies the declaration regardless of the
  allowlist. Preserve the installed-first guard order required by XMKT-02.
- Refusal uses the closed-set `cross-marketplace` reason and must name
  `allowCrossMarketplaceDependenciesOn`.
- Never auto-add a marketplace to satisfy a dependency; D-03-08 remains in
  force.
- The user chose to discuss all three remaining gray areas.
</decisions_made>

<blockers>

- No technical blocker exists.
- Human input is pending on the three selected discussion areas. The workflow
  must not create CONTEXT.md or planning artifacts before those answers.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured pause state and exact next action.
2. `.planning/ROADMAP.md` Phase 11 — fixed goal and success criteria.
3. `.planning/REQUIREMENTS.md` XMKT-01 and XMKT-02 — acceptance boundary.
4. `.planning/HANDOFF-upstream-dependency-parity.md` — upstream behavior and
   the settled `cross-marketplace` reason.
5. `extensions/pi-claude-marketplace/domain/manifest.ts` — shared parsed field
   source.
6. `extensions/pi-claude-marketplace/domain/dependency-closure.ts` — guard
   ordering and edge walk.
7. `extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts` — info
   projection using the cached manifest.

## Critical Anti-Patterns (do NOT repeat these)

- Do not invent defaults for the three discussion questions. Ask them in text
  mode and wait for the user's answer.
- Do not stage, rewrite, or reformat the user's existing local files:
  `.claude/settings.json`, `.codex/config.toml`, `.planning/config.json`,
  `.planning/state.json`, or `.mcp.json`.
- The local `.planning/config.json` formatting drift already makes the global
  format check fail. Use focused checks for handoff-only work and preserve it.

## Infrastructure State

- Branch: `features/manifest` at `adf503e9` before this handoff commit.
- No servers, watchers, test processes, or GSD executors are running.
- No non-terminal `.planning/async-jobs/*.json` manifests exist.
- The five local changes listed above predate this pause and are user-owned.
- The placeholder scan found only sentences that explicitly say no placeholders
  remain; it found no incomplete SUMMARY.md.

<context>
The next discussion should stay product-focused. The core policy is settled;
only its user-facing info presentation, invalid-input behavior, and refusal
detail remain open. The prior prompt offered these three areas and option 4 for
all of them. The user replied `4`, then paused before the first question.
</context>

<next_action>
Start with the first text-mode question about `marketplace info` output. Then
ask about malformed allowlist values and refusal message detail. After all
answers, finish `gsd-discuss-phase` for Phase 11 and resume the autonomous run.
</next_action>
