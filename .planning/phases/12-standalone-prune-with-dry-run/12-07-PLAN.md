---
phase: 12-standalone-prune-with-dry-run
plan: "07"
type: execute
wave: 4
depends_on: [12-03, 12-04]
files_modified:
  - extensions/pi-claude-marketplace/edge/flag-catalog.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
  - extensions/pi-claude-marketplace/edge/router.ts
  - tests/edge/flag-catalog.test.ts
  - tests/edge/handlers/plugin/prune.test.ts
  - tests/edge/router.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/architecture/flag-catalog-drift.test.ts
autonomous: true
requirements: [PRUNE-06, FLAG-02]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A user can put --dry-run before or after --scope and get the same read-only preview; a bare prune selects user scope (FLAG-02)."
    - "The independent flag-catalog drift guard pins prune's extra parse and completion sets to exactly --dry-run, excluding the shared --scope base flag."
    - "The command refuses -y, --keep-data, --prune, --local, unknown flags, and positional targets before state or file access; no confirmation prompt appears (D-02-05)."
    - "The existing uninstall extra set stays exactly --keep-data, --local, --prune, preserving FLAG-01."
    - "The registered route from Plan 03 continues to reach the real standalone prune handler for both accepted flag orders (PRUNE-06)."
  prohibitions: []
  artifacts:
    - path: extensions/pi-claude-marketplace/edge/flag-catalog.ts
      provides: "Exactly one prune extra flag, --dry-run, derived into parse and completion."
      contains: "prune:"
    - path: tests/architecture/flag-catalog-drift.test.ts
      provides: "Independent prune exact-set, usage, and completion pin."
  key_links:
    - from: extensions/pi-claude-marketplace/edge/flag-catalog.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
      via: "Handler consumes the exported catalog flag name."
      pattern: DRY_RUN_FLAG
    - from: extensions/pi-claude-marketplace/edge/router.ts
      to: extensions/pi-claude-marketplace/edge/completions/provider.ts
      via: "Router verb inventory and catalog drive completion labels."
      pattern: TOP_LEVEL_SUBCOMMANDS
---

<objective>
Close and independently pin the standalone prune flag surface.

Purpose: A destructive no-target command needs one explicit preview switch and no accidental aliases or pass-through flags.
Output: Exact parser, help, router, and completion contracts with direct and architecture tests; Plan 03 owns registration and operation direct pairs.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-PATTERNS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-03-SUMMARY.md
@.planning/phases/12-standalone-prune-with-dry-run/12-04-SUMMARY.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-comments/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Artifacts this phase produces

- The prune catalog entry owns --dry-run with parse and complete visibility; the handler uses its exported name.
- TOP_LEVEL_USAGE names prune [--scope user|project] [--dry-run], while TOP_LEVEL_SUBCOMMANDS and the help path expose prune.
- The existing tests/architecture/flag-catalog-drift.test.ts independently pins the exact prune parse/completion/usage set and retains the uninstall set.
- Plan 03 directly pins the production operations and register composition introduced by the Plan 01 tracer.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject every unsupported prune argument before effects</name>
  <files>extensions/pi-claude-marketplace/edge/flag-catalog.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts, tests/edge/flag-catalog.test.ts, tests/edge/handlers/plugin/prune.test.ts, tests/architecture/flag-catalog-drift.test.ts</files>
  <read_first>Read every existing edited file, edge/handlers/plugin/uninstall.ts, edge/handlers/plugin/shared.ts, edge/args.ts, 12-CONTEXT.md FLAG-02/D-02-05, and REQUIREMENTS.md FLAG-01/02. Trace the handler's scanner and catalog callers with CodeGraph.</read_first>
  <behavior>
    - Bare prune and prune --scope project invoke actual prune in exactly the chosen scope; --dry-run before or after --scope invokes preview without write.
    - Every other extra long or short flag and every operand is rejected at usage severity with both scope trees untouched.
    - Duplicate --dry-run has the existing consumed-set semantics and cannot trigger two operation calls or a mutation.
  </behavior>
  <action>Keep the prune flag-catalog entry exactly one --dry-run item, parse true and complete true, and export its name as DRY_RUN_FLAG so the handler mapping cannot silently drift. Use the scanner's consuming mode and a no-positionals residual parse; reject its local/write-target arm and all unexpected tokens before touching the operation. Build independent handler tests for flag-before-scope and scope-before-flag, absent scope, duplicate preview flag, adjacent unexpected operands, -y, --keep-data, --prune, --local, an unknown long flag, and invalid scope. Each rejection asserts both notification bytes and unchanged state trees. Add prune to the drift test's independent CatalogVerb inventory and pin HANDLER_ACCEPTED_PARSE_SETS.prune to the one literal --dry-run; leave the uninstall row unchanged. Do not add a prompt or auto-confirmation path per D-02-05.</action>
  <acceptance_criteria>
    - The accepted per-verb extra set is exactly [--dry-run]; unsupported input cannot invoke the mutating operation.
    - Scope and preview flags work in either order, while duplicate preview remains a single read-only call.
    - The independent FLAG-01 uninstall pin still matches its prior set.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/edge/flag-catalog.test.ts tests/edge/handlers/plugin/prune.test.ts tests/architecture/flag-catalog-drift.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a parse-set mismatch, or a rejected input changes either scope.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/flag-catalog.ts extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts</automated>
    <fails_when>Non-zero exit or either changed source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>Only the shared scope selection and catalog-owned --dry-run flag reach the prune operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin help and completion to the same flag contract</name>
  <files>extensions/pi-claude-marketplace/edge/router.ts, tests/edge/router.test.ts, tests/edge/completions/provider.test.ts, tests/architecture/flag-catalog-drift.test.ts</files>
  <read_first>Read every edited file, edge/completions/provider.ts, edge/handlers/plugin/help.ts, edge/register.ts, and 12-CONTEXT.md FLAG-02/D-02-05. Trace TOP_LEVEL_SUBCOMMANDS and TOP_LEVEL_USAGE consumers with CodeGraph.</read_first>
  <behavior>
    - Top-level completion includes prune, and prune flag completion offers shared --scope plus --dry-run, with no -y or other extra flag.
    - Help and unknown-command usage show prune [--scope user|project] [--dry-run] and do not promise a confirmation prompt.
    - The existing uninstall completion and help flag list remain unchanged.
  </behavior>
  <action>Complete the router's prune usage line and update its direct pair. Extend completion-provider tests and the drift guard's independent usage partition for prune; treat --scope as a shared base flag, excluded from the per-verb exact-set comparison. Verify that the provider derives prune's extra flag from flag-catalog rather than a new hardcoded list, and that the router's existing verb tuple is the source for head completion. Keep the current uninstall line and its FLAG-01 tests unchanged.</action>
  <acceptance_criteria>
    - Parser, catalog, completion, and usage all agree on exactly one prune extra flag.
    - Bare help, unknown-command usage, and completion expose the new verb and correct syntax.
    - Uninstall's FLAG-01 accepted set and help bytes retain their prior contract.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/edge/router.test.ts tests/edge/completions/provider.test.ts tests/architecture/flag-catalog-drift.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a help/completion mismatch, or uninstall's exact-set pin changes.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/router.ts</automated>
    <fails_when>Non-zero exit or the changed router source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>The published help and completion surface matches the FLAG-02 parser exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Untrusted command tokens to destructive prune | Unknown flags or an unexpected target must not fall through to a removal. |
| Catalog to completion/help | A suggested flag must correspond to an accepted behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-09 | Tampering | prune handler parser | high | mitigate | Task 1 rejects every unsupported argument before operation invocation and compares both scope trees; Plan 03 pins registration. |
| T-12-10 | Spoofing | flag catalog and completion | medium | mitigate | Tasks 1 and 2 pin parser, usage, catalog, and completion to one exact set. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run handler, router, completion, and catalog tests plus direct coverage and architecture tests. Plan 03 covers registration and operation pairs. No real network, account, or service setup is needed.
</verification>

<success_criteria>
FLAG-02 is closed by the independent exact-set guard: prune accepts only --dry-run beyond shared scope selection; no prompt or -y exists, and FLAG-01 is intact.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-07-SUMMARY.md when done.
</output>
