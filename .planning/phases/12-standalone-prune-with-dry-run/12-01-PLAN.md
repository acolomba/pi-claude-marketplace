---
phase: 12-standalone-prune-with-dry-run
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
  - extensions/pi-claude-marketplace/edge/router.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - tests/integration/standalone-prune.test.ts
autonomous: true
requirements: [PRUNE-06]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A Pi user can run /claude:plugin prune and remove an orphaned dependency install in the selected scope without naming or removing a primary plugin (PRUNE-06)."
    - "The actual sweep uses one fresh locked state snapshot, the existing whole-scope fixpoint, and at most one save (D-05-01, D-05-02)."
    - "A malformed declarer refuses the command before removal; a project-scope command leaves user-scope state untouched (D-05-05, D-05-07)."
    - "The existing uninstall path still allows removal of a depended-on named plugin and only prunes after that primary removal succeeds (Phase 6, D-05-03)."
  prohibitions: []
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
      provides: "Standalone actual-prune operation and single-scope transaction entry."
      exports: [createPrunePlugin, PrunePluginOptions]
    - path: extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
      provides: "No-target prune command handler."
      exports: [makePruneHandler]
    - path: tests/integration/standalone-prune.test.ts
      provides: "Real command-to-state tracer with disk and notification assertions."
  key_links:
    - from: extensions/pi-claude-marketplace/edge/router.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
      via: "Registered prune handler reached through the slash-command router."
      pattern: "case \"prune\""
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      via: "Shared guarded member sweep and post-commit cleanup."
      pattern: sweepOrphans
---

<objective>
Make the standalone prune command perform a real, locked dependency-orphan removal from the user or project scope.

Purpose: Users can clean accumulated dependency installs without removing a named plugin; Phase 5 uninstall behavior remains intact.
Output: Registered command, standalone operation, shared sweep seam, and a production-quality end-to-end command tracer. Plan 02 expands direct source/test pairs.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-PATTERNS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-VALIDATION.md
@.planning/phases/05-prune-on-uninstall/05-CONTEXT.md
@.planning/phases/06-load-time-dependency-check-and-allowed-uninstall/06-04-SUMMARY.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-comments/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Execution discipline

Read each edited file before editing and trace callers with CodeGraph first. Start the new tests red. Use one temporary directory per filesystem case and independent expected values. Plan 02 expands the sweep's direct source/test pairs; Plans 03 and 07 pin composition and edge pairs. No new package, persisted field, network operation, list/info marker, or confirmation prompt is part of this work. Preserve operator-owned dirty files. All commands run at the repository root.

## Artifacts this phase produces

- New production files: orchestrators/plugin/prune.ts and edge/handlers/plugin/prune.ts.
- New exported symbols: createPrunePlugin, PrunePluginOptions, createPruneOperation, makePruneHandler; a reusable sweep and finalizer from uninstall.ts.
- New test file: tests/integration/standalone-prune.test.ts. Plan 02 adds the prune direct pair; Plan 07 adds the handler pair.
- New command verb: prune. Plan 04 adds the --dry-run flag; Plan 07 pins its exact accepted set.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove one orphan through the registered prune command</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts, extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts, extensions/pi-claude-marketplace/edge/router.ts, extensions/pi-claude-marketplace/edge/register.ts, tests/integration/standalone-prune.test.ts</files>
  <read_first>Read every file in files that already exists, plus dependency-orphans.ts, edge/handlers/plugin/pending.ts, edge/handlers/plugin/uninstall.ts, transaction/with-state-guard.ts, persistence/locations.ts, shared/notify-context.ts, and the four project TypeScript skills in context. Trace createUninstallOperation, buildScopeDeclarationIndex, sweepOrphans, and registerClaudePluginCommand callers with CodeGraph before changing them. Use D-05-01/02/03/04/05/06/07, D-05-11/13, and PRUNE-06 as source of truth.</read_first>
  <behavior>
    - A user-scope command with one orphan dependency record removes that record and its staged artifacts, retains an unrelated explicit record, and emits one ordinary (uninstalled) {dependency pruned} row.
    - The user-scope command reports its actual removal through the registered route without touching a named primary plugin.
  </behavior>
  <action>Write the failing command-level integration case first, using the real registered slash command and a hermetic path-source fixture. Make ScopeDeclarationIndexOptions.exclude optional: uninstall still supplies its named primary key, while standalone prune supplies no exclusion so all installed records are declarers and candidates. Reuse pruneOrphans from domain/dependency-orphans.ts. In uninstall.ts expose the existing guarded sweep and post-commit finalizer for the standalone operation, replacing the required primary-key seed with an explicit initially-gone set: uninstall passes its removed primary, standalone passes an empty set. Keep the per-member isHeldBy recheck and add only successfully removed keys to gone. In new prune.ts, define PrunePluginOptions with ctx, pi, cwd, optional scope; resolve exactly one scope (user by default) with locationsFor, take withLockedStateTransaction once, build the full-scope offline declaration index before removal, run the shared sweep with keepData false, and call tx.save once when members were attempted so partial resource folds persist. If the index fails, emit one typed failed row for the named declarer with the unreadable reason and its redacted cause, at error severity with no reload hint; make no removal or save. After the lock releases, run the shared finalizer and emit one order-preserving notification cascade of existing composePrunedRow success or existing member-failure rows; use contiguous singleton marketplace blocks so alternating marketplaces stay in removal order. Bind createPrunePlugin through createPruneOperation in operations.ts. Add a no-target handler and register/router dispatch, top-level verb inventory, and usage text; it accepts shared --scope and rejects every other option or positional until the preview path is added. Use the existing structured notification boundary, never process output. D-05-03 remains specific to uninstall --prune; standalone prune has no fabricated primary.</action>
  <acceptance_criteria>
    - The integration case invokes the real registered prune verb and observes state, staged-file, and notification changes in the chosen scope.
    - A single orphan emits the exact existing actual prune row and one reload hint; no primary plugin is uninstalled.
    - The command reaches one locked transaction and at most one state save; the read and removal path is offline.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/integration/standalone-prune.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a wrong-scope change, or a missing actual prune row.</fails_when>
  </verify>
  <done>The registered command removes one real orphan end to end and reports it using the existing removal grammar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reach the project scope and refuse an unreadable declarer</name>
  <files>tests/integration/standalone-prune.test.ts</files>
  <read_first>Read the integration file created in Task 1, 12-CONTEXT.md D-05-05/07, persistence/locations.ts, and the failing-row source in orchestrators/plugin/uninstall.messaging.ts. Trace the registered prune handler through CodeGraph before extending the tests.</read_first>
  <behavior>
    - A project-scope command changes only project state and staged files; the user-scope copy remains byte-identical.
    - An unreadable installed declarer produces a named, redacted failed row before any member is removed or state is saved.
  </behavior>
  <action>Expand the real registered-command fixture with two scope trees and a corrupt declarer. Pin each complete state, tree, and notification against independent literal expectations, and assert that no network transport runs. These cases make D-05-05 same-scope selection and D-05-07 fail-closed refusal observable immediately after the tracer. Plan 02 owns the broader direct source/test-pair regressions and failed-member fixpoint matrix.</action>
  <acceptance_criteria>
    - Project scope removes its orphan without changing a byte in user scope.
    - An unreadable declarer emits a redacted failure and leaves both scope trees unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/integration/standalone-prune.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a wrong-scope mutation, or a failed declaration that removes a member.</fails_when>
  </verify>
  <done>Both scopes reach the command correctly and the declaration failure is safe end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Command tokens to one scope | An untrusted scope or unsupported token must not select or delete the wrong tree. |
| Installed manifests to removal decision | An unreadable declaration must not be treated as an empty declaration. |
| State transaction to staged files | A member failure must retain its installed record or persist an accurate partial fold. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-01 | Tampering | prune handler and locationsFor | high | mitigate | Task 1 validates exact scope and rejects extra tokens; integration tests compare both scope trees, and Plan 07 pins the parser's accepted set. |
| T-12-02 | Tampering | declaration index and member sweep | high | mitigate | Task 1 builds the complete offline index before removal and rechecks failed-member holders inside the lock; Plan 02 tests the failure matrix. |
| T-12-03 | Information disclosure | declaration failure output | medium | mitigate | Task 2 checks the redacted declarer cause and forbids absolute filesystem paths in emitted messages. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run the focused command integration gate after each task. Then run node --test tests/architecture/no-orchestrator-network.test.ts and npm run typecheck. Plan 02 completes direct source/test-pair coverage before preview work. A failed gate blocks the next wave.
</verification>

<success_criteria>
PRUNE-06 is observable through the real registered command: it removes one orphan in exactly one scope, reports the outcome, and refuses an unreadable declarer without effects. Plan 02 closes the full fixpoint and uninstall regressions.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-01-SUMMARY.md when done.
</output>
