---
phase: 12-standalone-prune-with-dry-run
plan: "03"
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - tests/orchestrators/plugin/operations.test.ts
  - tests/edge/register.test.ts
autonomous: true
requirements: [PRUNE-06]
estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The registered no-target prune command reaches the real createPruneOperation binding exactly once in user or project scope (PRUNE-06)."
    - "Rejected actual-prune operands never invoke an operation or read state, and existing command registrations and operation factories retain their routes."
  prohibitions: []
  artifacts:
    - path: tests/orchestrators/plugin/operations.test.ts
      provides: "Direct production-binding assertions for createPruneOperation."
    - path: tests/edge/register.test.ts
      provides: "Direct registration and no-target command-dispatch assertions."
  key_links:
    - from: extensions/pi-claude-marketplace/edge/register.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts
      via: "Registered prune handler reaches the production operation factory."
      pattern: createPruneOperation
---

<objective>
Pin the production composition behind the working actual-prune command.

Purpose: A passing command integration test should also have direct coverage at the factory and registration seams.
Output: Direct operations and register pairs for the no-target prune path.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-PATTERNS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-01-SUMMARY.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-comments/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Execution discipline

Read every edited source/test pair before editing and trace the operation factory and command registration callers with CodeGraph first. Start direct behavior cases red. Plan 07 owns the final --dry-run parser, help, and completion exact-set checks.

## Artifacts this phase produces

- Direct operation-factory coverage for the Plan 01 createPruneOperation binding.
- Direct registration coverage for makePruneHandler and the slash-command route.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the real prune operation factory</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts, tests/orchestrators/plugin/operations.test.ts</files>
  <read_first>Read both files, orchestrators/plugin/prune.ts, transaction/with-state-guard.ts, 12-CONTEXT.md PRUNE-06, and the operations direct-pair conventions. Trace createPruneOperation and createUninstallOperation callers with CodeGraph.</read_first>
  <behavior>
    - The production factory binds the real state transaction, cascade, hooks routing, and completion cache for the actual prune path.
    - Calling the factory alone has no state or filesystem effect; invoking the returned operation removes one orphan in the selected scope.
    - Existing operation exports still resolve to their prior implementations.
  </behavior>
  <action>Expand the operations direct pair for the createPruneOperation binding introduced by the tracer. Assert a real path-source orphan is removed through the returned operation and no network call occurs; cover default user and explicit project scope with separate temporary trees. Verify factory construction is inert, while invocation reaches one locked transaction and at most one save. If a missing binding is exposed, correct operations.ts without changing existing operation exports. Keep D-05-11 actual rows and PRUNE-06 no-primary semantics.</action>
  <acceptance_criteria>
    - Direct operation tests prove one real actual-prune invocation through production dependencies.
    - The other scope and existing operation factories remain untouched.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/operations.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a missing factory binding, or an invocation that changes the wrong scope.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts</automated>
    <fails_when>Non-zero exit or the operations source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>The actual prune factory is covered directly from construction through its real invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin registered no-target dispatch and rejection</name>
  <files>extensions/pi-claude-marketplace/edge/register.ts, tests/edge/register.test.ts</files>
  <read_first>Read both files, edge/router.ts, edge/handlers/plugin/prune.ts, edge/handlers/plugin/uninstall.ts, edge/args.ts, and 12-CONTEXT.md FLAG-02/D-02-05. Trace registerClaudePluginCommand and makePruneHandler callers with CodeGraph.</read_first>
  <behavior>
    - Registered bare prune reaches one actual operation in user scope; --scope project reaches only project scope.
    - An extra operand or unsupported actual-prune flag returns before operation creation or state access.
    - Every existing registered verb still dispatches to its previous handler.
  </behavior>
  <action>Extend the register direct pair for the public no-target prune command and its single createPruneOperation path. Assert whole notifications and both scope trees for accepted bare and project commands, plus rejected extra operand, unknown option, --local, --keep-data, --prune, and -y. Do not route prune through parseRequiredPluginMarketplaceRef because it has no named target. If the direct pair exposes missing handler wiring, correct register.ts. Keep the final dry-run flag-order matrix and exact accepted set in Plan 07, where the parser and catalog are owned together.</action>
  <acceptance_criteria>
    - The registered command reaches one actual prune operation for accepted scope-only input.
    - Unsupported actual-prune input never reaches the operation or changes state.
    - Existing registrations remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/edge/register.test.ts tests/orchestrators/plugin/operations.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, an unsupported token reaching state, or an existing route changing destination.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts</automated>
    <fails_when>Non-zero exit or the register source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>The real registered actual-prune path is covered directly without a named plugin target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Registered command to state operation | A rejected operand must not reach the destructive operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-09 | Tampering | register and operation binding | high | mitigate | Tasks 1 and 2 assert selected-scope invocation and that rejected input never reaches the operation. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run both direct pairs, their direct coverage gates, and the Plan 01 integration test. Plan 07 closes the final flag surface after preview is available.
</verification>

<success_criteria>
PRUNE-06's actual command has direct production-factory and registration evidence without changing prior verb routes.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-03-SUMMARY.md when done.
</output>
