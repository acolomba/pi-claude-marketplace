---
phase: 12-standalone-prune-with-dry-run
plan: "05"
type: execute
wave: 4
depends_on: [12-04]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
  - extensions/pi-claude-marketplace/shared/notification-types.ts
  - extensions/pi-claude-marketplace/shared/notification-dispatch.ts
  - tests/orchestrators/plugin/prune.test.ts
  - tests/shared/notification-types.test.ts
  - tests/shared/notification-dispatch.test.ts
  - tests/integration/standalone-prune.test.ts
autonomous: true
requirements: [PRUNE-06, PRUNE-07]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Bare prune and prune --dry-run each report Nothing to prune at info severity with the selected scope and a reason when no orphan qualifies (D-12-02)."
    - "An unreadable declarer remains an error before any removal; it never becomes the ordinary empty result (D-05-07)."
    - "Actual empty selection makes no state save; preview empty selection remains lock-free and nonpersisting (PRUNE-07)."
  prohibitions: []
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/notification-types.ts
      provides: "Typed PruneEmptyMessage in the closed notification union."
      contains: PruneEmptyMessage
    - path: extensions/pi-claude-marketplace/shared/notification-dispatch.ts
      provides: "Informational, scope-aware Nothing to prune rendering."
    - path: tests/integration/standalone-prune.test.ts
      provides: "Four scoped actual/preview empty outcomes through the registered command."
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
      to: extensions/pi-claude-marketplace/shared/notification-dispatch.ts
      via: "Both zero-candidate branches emit the same typed informational empty result."
      pattern: PruneEmptyMessage
---

<objective>
Give both standalone prune modes the same normal scoped no-orphans result.

Purpose: An empty sweep is a useful answer, not a removal failure.
Output: Typed informational empty message, actual and preview dispatch, and direct command-level tests.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-04-SUMMARY.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-comments/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Execution discipline

Read every edited file first and trace the notification union, dispatcher, and prune operation callers with CodeGraph. Begin each behavior change with a failing case. Keep the actual empty branch inside its fresh lock and the preview branch outside any mutation helper. Plan 06 pins final catalog bytes.

## Artifacts this phase produces

- New PruneEmptyMessage kind and scope-aware dispatcher sentence.
- Four command-level empty outcomes across actual/preview and user/project scope.
- Direct source/test-pair coverage for the changed prune, notification-types, and notification-dispatch modules.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report an actual empty sweep without saving state</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts, extensions/pi-claude-marketplace/shared/notification-types.ts, extensions/pi-claude-marketplace/shared/notification-dispatch.ts, tests/orchestrators/plugin/prune.test.ts, tests/shared/notification-types.test.ts, tests/shared/notification-dispatch.test.ts</files>
  <read_first>Read every task file, shared/notification-grammar.ts, orchestrators/plugin/uninstall.messaging.ts, and 12-CONTEXT.md D-12-02/D-05-07. Trace NotificationMessage and the actual prune result dispatch with CodeGraph.</read_first>
  <behavior>
    - Actual prune on user or project scope with zero orphan candidates emits exactly one info notification naming that scope and the reason.
    - A scope containing only explicit or held dependency installs has the same normal empty outcome and makes no save.
    - An unreadable declarer still yields a named failed row rather than an empty notification.
  </behavior>
  <action>Per D-12-02, add typed PruneEmptyMessage with scope to the shared NotificationMessage standalone-kind union and dispatcher. Use the exact sentence Nothing to prune in user scope: no orphaned dependency installs were found. and substitute project for the selected scope. Route the actual zero-candidate arm through this kind at info severity after its fresh locked selection; do not save when no member was attempted. Keep D-05-07 unreadable-declarer refusal distinct from the normal empty result. Add direct type, dispatch, and prune cases with independent whole-message, state, and save-count expectations. Do not alter uninstall --prune's existing no-extra-output behavior.</action>
  <acceptance_criteria>
    - Both actual scopes show the scoped sentence at info severity, without a failure row or reload trailer.
    - Empty actual calls make no save, while unreadable declarations still fail closed.
    - Named uninstall --prune retains its existing empty-sweep output.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/prune.test.ts tests/shared/notification-types.test.ts tests/shared/notification-dispatch.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, wrong scope or severity, or a declaration failure classified as empty.</fails_when>
  </verify>
  <done>Actual standalone prune reports ordinary scoped emptiness without saving or changing uninstall output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give preview the same empty result without any write</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts, tests/orchestrators/plugin/prune.test.ts, tests/integration/standalone-prune.test.ts</files>
  <read_first>Read all task files, persistence/state-io.ts, transaction/with-state-guard.ts, the Task 1 notification type and dispatcher, and 12-CONTEXT.md D-12-02/PRUNE-07. Trace the preview branch and notification call with CodeGraph.</read_first>
  <behavior>
    - Dry-run on empty user or project scope emits the same scoped info sentence as actual prune and no reload hint.
    - A missing state or a scope with only explicit or held records remains byte-identical after the empty preview.
    - A damaged declarer produces its existing failure rather than Nothing to prune.
  </behavior>
  <action>Per D-12-02 and PRUNE-07, route preview's zero-candidate branch through PruneEmptyMessage. Exercise two scopes times two modes through the real registered command, asserting independent complete notifications and the selected-scope sentence. For preview cases compare bytes, mtime, tree, config, and lock path before and after. Expand the prune direct pair for same-snapshot selection, repeatability, and declaration refusal so the new no-op arm cannot hide a failed parse. Keep the preview call outside withLockedStateTransaction and any filesystem writer.</action>
  <acceptance_criteria>
    - Four scope/mode combinations have the exact sentence and info severity.
    - Empty preview makes no disk change and has no reload hint.
    - Changed prune and shared notification modules pass direct coverage.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/prune.test.ts tests/shared/notification-types.test.ts tests/shared/notification-dispatch.test.ts tests/integration/standalone-prune.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, wrong scope text, or any preview write.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts extensions/pi-claude-marketplace/shared/notification-types.ts extensions/pi-claude-marketplace/shared/notification-dispatch.ts</automated>
    <fails_when>Non-zero exit or any changed source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>Both modes report the same normal no-orphans result, with preview remaining read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Selection result to user notification | A damaged declarer must not appear as an ordinary empty sweep. |
| Preview to filesystem | Even an empty preview must not create the extension root or lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-07 | Repudiation | empty prune notification | medium | mitigate | Tasks 1 and 2 pin the four full scoped messages and keep declaration failures distinct. |
| T-12-04 | Tampering | empty preview state read | high | mitigate | Task 2 compares bytes, mtime, tree, config, and lock path before and after. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run the focused direct pairs, registered-command integration, and direct coverage. Plan 06 pins the final message bytes in the closed output catalog.
</verification>

<success_criteria>
D-12-02 is observable in both scopes and both modes; no-orphans is normal information, while unreadable declarations remain failures.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-05-SUMMARY.md when done.
</output>
