---
phase: 12-standalone-prune-with-dry-run
plan: "08"
type: execute
wave: 6
depends_on: [12-03, 12-06, 12-07]
files_modified:
  - README.md
  - docs/dependency-resolution.md
  - .planning/BACKLOG.md
  - tests/architecture/dependency-doc-agreement.test.ts
  - tests/integration/standalone-prune.test.ts
autonomous: true
requirements: [PRUNE-06, PRUNE-07, FLAG-02]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "README and the dependency guide teach standalone prune and read-only --dry-run, with user default, explicit project scope, exact rows, and normal empty result."
    - "The docs preserve Phase 5 uninstall --prune behavior and Phase 6's allowed uninstall of a depended-on plugin; a reload does not auto-prune."
    - "PRUNE-CMD-01 is closed with its list/info orphan marker proposal dropped and not re-filed (D-12-03)."
    - "The focused unit, integration, architecture, direct-coverage, and full project checks substantiate PRUNE-06, PRUNE-07, and FLAG-02."
  prohibitions: []
  artifacts:
    - path: README.md
      provides: "Public prune command and dry-run usage."
    - path: docs/dependency-resolution.md
      provides: "Standalone orphan-sweep semantics and snapshot limitations."
    - path: .planning/BACKLOG.md
      provides: "Closed PRUNE-CMD-01 disposition without an orphan inventory marker."
  key_links:
    - from: docs/dependency-resolution.md
      to: tests/architecture/dependency-doc-agreement.test.ts
      via: "Documentation contract test checks the new command and retained uninstall semantics."
      pattern: prune
---

<objective>
Publish the completed command behavior, close its backlog item, and run the phase regression gates.

Purpose: Users need accurate usage and a trustworthy closeout after the actual, preview, output, and flag contracts are in place.
Output: README and dependency-guide updates, backlog closure, final integration checks, and a recorded full-gate result.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-VALIDATION.md
@.planning/phases/12-standalone-prune-with-dry-run/12-06-SUMMARY.md
@.planning/phases/12-standalone-prune-with-dry-run/12-07-SUMMARY.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@.planning/BACKLOG.md
@.agents/skills/simple-english/SKILL.md
@.agents/skills/humanizer/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Artifacts this phase produces

- README and dependency-resolution instructions for /claude:plugin prune [--scope user|project] [--dry-run].
- A closed PRUNE-CMD-01 backlog entry recording D-12-03's decision to drop the proposed list/info marker.
- Final doc-agreement and integration cases that prove the implemented behavior and prevent accidental documentation drift.

<tasks>

<task type="auto">
  <name>Task 1: Document standalone prune and close its backlog item</name>
  <files>README.md, docs/dependency-resolution.md, .planning/BACKLOG.md, tests/architecture/dependency-doc-agreement.test.ts</files>
  <read_first>Read every edited file, 12-CONTEXT.md D-12-01/02/03, Phase 12 ROADMAP success criteria, docs/output-catalog.md's prune and uninstall sections, and the Simple English skill in context. Trace the existing documentation-agreement test assertions before editing.</read_first>
  <action>Add the actual and preview command forms to README and the dependency guide. Explain that only dependency-installed records with no same-scope installed declarer qualify; disabled declarers still hold dependencies, explicit installs stay, and the sweep reaches a fixpoint. Explain that dry-run shows the current snapshot in pending tense and writes nothing; a later actual command rechecks its current state and can differ after concurrent changes or a member failure. Show both locked row phrases from D-12-01 and the scoped Nothing to prune result from D-12-02. Distinguish standalone prune from uninstall --prune, which first removes a named plugin and retains its existing no-extra-output case when no dependency qualifies. Correct README's stale claim that uninstall refuses a depended-on target: Phase 6 allows that removal and names the dependents. Update the reload paragraph so it points to either explicit prune command and still says reload never auto-prunes. In BACKLOG.md mark PRUNE-CMD-01 closed by this delivered command and dry-run, remove its proposed list/info marker from the picked-up scope, and record that D-12-03 drops the marker without a new backlog item. Extend the documentation-agreement test with exact anchors for these facts. Write the public prose in Simple English Plain mode.</action>
  <acceptance_criteria>
    - Both public docs show runnable prune and prune --dry-run syntax and the exact scope/row semantics.
    - Phase 5 and Phase 6 uninstall descriptions are accurate, and reload remains non-pruning.
    - PRUNE-CMD-01 is closed; no orphan marker is added to list, info, or another backlog entry.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/dependency-doc-agreement.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a stale uninstall-refusal claim, or a missing prune/dry-run contract anchor.</fails_when>
    <automated>npx prettier --check README.md docs/dependency-resolution.md .planning/BACKLOG.md</automated>
    <fails_when>Non-zero exit or a changed document fails formatting.</fails_when>
  </verify>
  <done>The public docs describe the shipped command and PRUNE-CMD-01 records the completed, marker-free disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the complete command against disk and regression boundaries</name>
  <files>tests/integration/standalone-prune.test.ts</files>
  <read_first>Read the integration file, its Phase 12 operation and handler, existing tests/orchestrators/plugin/uninstall.test.ts, tests/architecture/no-orchestrator-network.test.ts, 12-RESEARCH.md Common Pitfalls, the phase validation strategy, the pending-row tests, and all three requirement statements. Trace the handler's flag scanner and callers with CodeGraph. Read AGENTS.md commit and gate rules before reporting completion.</read_first>
  <behavior>
    - Actual prune: one locked save, same-scope fixpoint, ordinary actual rows, and fail-clean retry after a failed member.
    - Preview: same-snapshot parity with successful actual removal, no writes across absent, current, and legacy state, no lock or reload hint, and stale preview never drives a later actual removal.
    - Interleaved marketplace candidates retain fixpoint order in both modes; a failed actual member remains installed and can retain later dependencies that preview had listed.
    - Repeated previews are byte-identical; held, disabled-declarer, and explicit neighbors survive; an unreadable declarer produces a failure with no writes.
    - Flags: default and explicit scopes, both flag orders, exact accepted set, no prompt and no -y; Phase 5 and Phase 6 uninstall remain green.
  </behavior>
  <action>Fill any command-level test gaps left by Plans 01-07 using real temporary directories and independently written whole-state and whole-notification assertions. Cover the nine applicable specless probes: PRUNE-06's unclassified destructive state transition with no-op, scope, and failure checks; PRUNE-07 adjacency, empty, ordering, idempotency, and concurrent snapshot changes; FLAG-02 adjacency, empty input, and flag order. Use one literal expected order independent of pruneOrphans, including an alternating-marketplace chain and held, disabled-declarer, and explicit neighbors. Capture a preview, change the fixture between calls, and prove actual prune selects again inside its lock. Repeat previews against current, legacy, and absent trees; compare config, state bytes and mtime, artifacts, directories, and lock path before and after each call, and assert no network access or /reload hint. Make the integration suite discriminate a plausible wrong implementation: a hidden migration save, wrong-scope selection, reordered interleaved members, failed-member descendants removed, or unsupported flag reaching the operation must each fail. Run focused architecture tests, direct coverage for every changed production TypeScript pair, the integration suite, and the full npm run check chain. If the pre-existing operator-owned .planning/config.json formatting drift stops that chain, report the exact failing gate and run the unaffected downstream scripts independently; do not edit or stage that file. Before any executor commit, obey AGENTS.md pre-commit and fallow audit gates without bypassing hooks.</action>
  <acceptance_criteria>
    - All nine probe items are resolved by named integration, unit, or architecture assertions.
    - All new and changed TypeScript modules reach the project's direct-pair coverage requirement.
    - Preview preserves every disk observation, and actual results reflect their fresh locked snapshot and failed-member holds.
    - Full npm run check passes, or only the previously documented operator-owned formatting drift blocks the chain and every unaffected downstream gate is reported separately.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/integration/standalone-prune.test.ts tests/architecture/no-orchestrator-network.test.ts tests/architecture/flag-catalog-drift.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/dependency-doc-agreement.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a catalog/flag/doc drift, or a preview write or wrong-scope removal.</fails_when>
  </verify>
  <done>The integrated command and all requirement edges pass; full quality-gate results are recorded honestly.</done>
</task>

</tasks>

## Multi-source coverage audit

| Source | Item | Plans | Status |
| --- | --- | --- | --- |
| GOAL | See and remove same-scope orphaned dependency installs without uninstalling a primary; close the flag surface | 01-08 | COVERED |
| REQ | PRUNE-06 standalone actual removal and rows | 01-03, 05-06, 08 | COVERED |
| REQ | PRUNE-07 read-only dry-run listing | 04-06, 08 | COVERED |
| REQ | FLAG-02 exact --dry-run extra flag | 04, 07-08 | COVERED |
| RESEARCH | Full-scope, offline, disabled-declarer-aware, fail-closed fixpoint | 01-02, 08 | COVERED |
| RESEARCH | One locked actual transaction, one save, failed-member hold and cleanup | 01-02, 08 | COVERED |
| RESEARCH | Missing/legacy-state preview with no lock, migration save, or disk write | 04, 08 | COVERED |
| RESEARCH | Same selector/order, concurrent recheck, repeatability | 01-02, 04-05, 08 | COVERED |
| RESEARCH | Typed pending/actual/empty/error messages and closed output catalog | 01, 04-06 | COVERED |
| RESEARCH | Exact parser, scope default, completion, help, no network | 01, 03-04, 07-08 | COVERED |
| RESEARCH | Docs, backlog disposition, Phase 5 and 6 regressions | 02, 08 | COVERED |
| CONTEXT | D-12-01 pending and actual row contracts | 01, 04, 06 | COVERED |
| CONTEXT | D-12-02 informational scoped Nothing to prune | 05-06 | COVERED |
| CONTEXT | D-12-03 close PRUNE-CMD-01 and drop marker | 08 | COVERED |
| CONTEXT | D-05-01/02/04/05/06/07 whole-scope fail-closed semantics | 01-02 | COVERED |
| CONTEXT | D-05-11/13 actual rows and failed-member hold | 01-02, 06 | COVERED |
| CONTEXT | FLAG-02/D-02-05 one flag, no prompt or -y | 04, 07 | COVERED |

The former list/info orphan marker was dropped by D-12-03, so it is an exclusion, not a missing source item. No package install, external API, schema push, or frontend artifact belongs to this phase.

## Prohibition probe resolution

The adversarial recall pass considered surprise deletion of explicit or other-scope installs, a dry-run that writes or claims completion, a leaked path on declaration failure, and hidden extra flags. The precision pass retained no new bespoke prohibition: PRUNE-06/07, FLAG-02, D-12-01/02, and the high-severity threat mitigations already turn every candidate into an explicit automated acceptance truth or a standard security control. There is no descriptor-less prohibition to mark unverified; must_haves.prohibitions is empty in each plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Published instructions to destructive command | Stale syntax or incorrect uninstall claims can cause an unintended user action. |
| Implementation to verification evidence | A broad suite can pass while a direct source/test pair or read-only edge is untested. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-11 | Repudiation | README and dependency guide | medium | mitigate | Task 1 pins actual/preview and Phase 6 statements with the doc-agreement test. |
| T-12-12 | Tampering | integration and direct-pair evidence | high | mitigate | Task 2 uses real scope trees and explicit wrong-implementation-sensitive assertions, then runs direct coverage and full gates. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run npm run check from the repository root after the focused gate. If the operator-owned .planning/config.json formatting drift blocks it, do not change that file. Run the remaining typecheck, lint, fallow, unit/direct-coverage, integration, and type-member gates separately and state the exact limitation. The executor runs required fallow audit and pre-commit before any commit.
</verification>

<success_criteria>
All PRUNE-06, PRUNE-07, and FLAG-02 behaviors and nine edge-probe items are tested; output and flags match their closed contracts; docs and backlog match the delivered command; high-severity threats are mitigated.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-08-SUMMARY.md when done.
</output>
