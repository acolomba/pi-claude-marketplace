---
phase: 65.1
slug: reload-deferred-will-grammar-consistency
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-27
---

# Phase 65.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (built-in), Node >= 20.19.0 |
| **Config file** | none — globbed by package.json `test` script |
| **Quick run command** | `node --test "tests/architecture/notify-closed-set-locks.test.ts"` |
| **Full suite command** | `npm run check` (typecheck + lint + format:check + test + test:integration) |
| **Estimated runtime** | ~60 seconds (full `npm run check`) |

---

## Sampling Rate

- **After every task commit:** Run the single affected architecture/orchestrator test file (e.g. `node --test "tests/architecture/notify-closed-set-locks.test.ts"`)
- **After every plan wave:** Run `npm test` (unit suite)
- **Before `/gsd-verify-work`:** `npm run check` must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 65.1-WILL02 | — | 0 | WILL-02 | — / — | N/A | architecture/invariant | `node --test "tests/architecture/notify-grammar-invariant.test.ts"` | ❌ W0 (add agreement assertion) | ⬜ pending |
| 65.1-CLOSEDSET | — | 1 | WILL-01, WILL-03 | — / — | N/A | unit | `node --test "tests/architecture/notify-closed-set-locks.test.ts"` | ✅ (edit counts 22→20, 9→7) | ⬜ pending |
| 65.1-PROJECTION | — | 1 | WILL-01, WILL-03 | — / — | N/A | unit | `node --test "tests/orchestrators/reconcile/notify.test.ts"` | ✅ (rewrite mp will-token arms) | ⬜ pending |
| 65.1-BYTES | — | 1 | WILL-01, WILL-03 | — / — | N/A | byte-equality | `node --test "tests/architecture/catalog-uat.test.ts"` | ✅ (edit fixtures + add remove-cascade state) | ⬜ pending |
| 65.1-GRAMMAR | — | 1 | WILL-01, WILL-03 | — / — | N/A | invariant | `node --test "tests/architecture/notify-grammar-invariant.test.ts"` | ✅ (edit row regex) | ⬜ pending |
| 65.1-CHECK | — | 2 | WILL-04 | — / — | N/A | gate | `npm run check` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] WILL-02 agreement test — add to `tests/architecture/notify-grammar-invariant.test.ts` (or a new `notify-will-reload-agreement.test.ts`): for each pending `will *` fixture, assert the corresponding realized-transition cascade DOES emit the `/reload to pick up changes` trailer; for the retired actions (marketplace add / empty marketplace remove) assert the realized cascade does NOT. This is the WILL-02 verification anchor named by D-65.1-01.
- [ ] New catalog UAT state `marketplace-remove-with-installed-plugins` — renders `● mp [user]` header + `○ plugin (will uninstall)` cascade rows (no such fixture exists today).

*All other test infrastructure already exists; this phase edits counts and fixtures, it does not stand up new frameworks.*

---

## Manual-Only Verifications

*All phase behaviors have automated verification.* The byte-exact output contract is asserted by the catalog-uat runner and notify tests; the WILL-02 cross-surface agreement is asserted by the Wave 0 invariant test.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (WILL-02 agreement test, remove-cascade fixture)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
