---
phase: 08-direct-coverage
plan: 05
type: execute
wave: 3
depends_on: [08-01]
files_modified:
  - extensions/pi-claude-marketplace/shared/fs-utils.ts
  - extensions/pi-claude-marketplace/bridges/skills/stage.ts
  - extensions/pi-claude-marketplace/bridges/commands/stage.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-swap.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-replace.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - tests/platform/removal-ops-fake.ts
  - tests/platform/removal-ops-contract.ts
  - tests/platform/removal-ops-fake.test.ts
  - tests/shared/fs-utils.test.ts
  - tests/bridges/skills/stage.test.ts
  - tests/bridges/commands/stage.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/orchestrators/plugin/install-outcome.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
autonomous: true
requirements: [RCOV-02]

estimate:
  tokens: 185000
  raw_tokens: 185000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`cleanupStaging` performs its recursive removal through an injected collaborator, so a caller can make one specific cleanup call fail while its siblings succeed."
    - "`rollbackReplacementCommon` performs its replacement removals and its backup restores through the same injected collaborator, so the three stages it accumulates leak messages from are independently faultable."
    - "The collaborator is a required parameter with no default anywhere in the tree; `npm run typecheck` refuses a call site that forgets it."
    - "`commitPreparedSkills`, `commitPreparedCommands`, `commitPreparedAgents`, `prepareStageSkills`, `prepareStageCommands`, and `prepareStagePluginAgents` all take the collaborator, so the staging leak is observable from the entry point the G1 tests call."
    - "No `unstage*` function's signature changed, because none of them calls `cleanupStaging`."
    - "`npm run typecheck` exits 0 with the whole migration in place, so no forwarding module, compatibility adapter, or old-path re-export exists at any point."
    - "`createRemovalOpsFake` performs no real filesystem work: it records calls and simulates outcomes in memory, and refuses construction without an explicit memory boundary."
    - "The real `createRemovalOps()` and `createRemovalOpsFake(...)` both satisfy one shared contract function, and that contract is proved to fail against a deliberately broken fake kept private to its own negative control."
    - "`node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/shared/fs-utils.ts` prints `Direct coverage passed:`."
    - "`assertPathInside` still runs in front of every removal the port performs; the port replaces the syscall, not the containment chokepoint."
  artifacts:
    - extensions/pi-claude-marketplace/shared/fs-utils.ts
    - tests/platform/removal-ops-fake.ts
    - tests/platform/removal-ops-contract.ts
    - tests/platform/removal-ops-fake.test.ts
  key_links:
    - "`RemovalOps` declared in `shared/fs-utils.ts` -> every bridge and orchestrator: `.fallowrc.json` lets every zone import `shared`, so the declaration adds no forbidden edge."
    - "`InstallLedgerOptions.removalOps` -> `install-flow.ts` `buildInstallLedgerOptions` and `enable-disable.ts`'s `runInstallLedger` call: these two become the composition roots for the install path, which is what makes the `commitPrepared*` leak arms faultable from `install-outcome.test.ts`."
    - "`replacePreparedAgents(prepared, options?)` -> the parameter-order rule: a required parameter cannot follow an optional one, so the collaborator goes FIRST on every migrated signature or rides the options object, never appended after `options?`."
    - "`tests/platform/removal-ops-fake.ts` + `tests/platform/removal-ops-contract.ts` -> `tests/platform/removal-ops-fake.test.ts`: all three must exist and the suite must import both, or the direct gate and the correspondence gate stop treating the suite as a structural supplement and demand a production module that does not exist."
  prohibitions:
    - "Do NOT give the collaborator parameter a default value, an optional marker, or a `?? DEFAULT_REMOVAL_OPS` fallback at any call site (D-08-12, D-05-01)."
    - "Do NOT create a forwarding module, a compatibility adapter, an overload that omits the collaborator, or an old-path re-export as an intermediate state (D-06-11)."
    - "Do NOT widen the port beyond `rm` and `rename`; `pathExists`, `removeOrphanIfPresent`, `readDirEntriesTolerant`, and `isPlainMarkdownFile` keep calling `fs.lstat`, `fs.stat`, `fs.readdir`, and `fs.rm` directly (D-08-13, D-08-A08)."
    - "Do NOT port `removeOrphanIfPresent`, and do NOT remove or weaken any `assertPathInside` call."
    - "Do NOT append the collaborator after an optional parameter; `replacePreparedAgents` raises TS1016 if you do."
    - "Do NOT change any `unstage*` signature; none of them reaches `cleanupStaging`."
    - "Do NOT give `tests/platform/removal-ops-fake.ts` a `-fake.test.ts` that omits the contract import, and do NOT place the fake under `tests/shared/`, where the structural-supplement exemption does not reach."
    - "Do NOT add a test-only export, reset hook, state reader, global mutator, or test mode to any production module (MF-DEC-07, D-05-02)."
    - "Do NOT let `createRemovalOpsFake` touch the real filesystem."
    - "Do NOT write `Phase NN`, `Plan NN`, `Wave N`, or `Pitfall N` into any comment or test title."
---

<objective>
Give `cleanupStaging` a narrow production-owned removal port and migrate every production
caller in one change, so a cleanup FAILURE becomes observable through a real collaborator
instead of through builtin-module patching.

This is ROADMAP criterion 4 and it closes the accepted override recorded as the open remainder
of `06-VERIFICATION.md` G1 — the one must-have Phase 6 deferred here by name. G1's open half is
the interleaved leak-message ordering and the leaked-residue partition, observable only when one
specific `cleanupStaging` call fails while its siblings succeed, driven from OUTSIDE through the
entry point the G1 tests actually call. A port that stops at `cleanupStaging`'s own signature is
unreachable from those tests and closes nothing. That is why the reach is the point.

`D-08-A11` confirms one atomic plan by building it: production typechecks as a unit, the edits
are compiler-enumerated `TS2554`-shaped argument additions, `fallow health` stays at zero above
threshold, and no boundary edge is added. `D-06-12`'s escape hatch is unavailable — the only
smaller leaf contract is the one that fails criterion 4.

**This plan may not be split.** `cleanupStaging`'s signature change breaks all of its call sites
at once, so no intermediate state compiles and `D-08-12` requires every production caller to
migrate in the plan that introduces the port. Its three tasks are the internal sequence, not
separable units. The context cost is compiler output, not file reading: work from `npm run
typecheck`'s diagnostic list rather than reading twenty files end to end.

**One extension beyond the researched five composition roots, recorded rather than discovered.**
`D-08-A06` needs the three `commitPrepared*` leak arms in `install-outcome.ts` faultable from its
owner test, and `D-08-12` forbids a default. So `removalOps` becomes a required member of
`InstallLedgerOptions`, and `install-flow.ts` plus `enable-disable.ts` — not `install-outcome.ts`
— construct the real operations for the install path. That is six composition roots, eleven
production files, and two more test files than the research inventory, and it keeps plan 07 free
of any production seam change.

Purpose: replace the patching `TREF-08` removed with the injection criterion 4 authorizes.
Output: a required, named, typed removal collaborator threaded from every public bridge and
orchestrator entry point down to `cleanupStaging` and `rollbackReplacementCommon`, with a
reusable fake and a shared contract.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-direct-coverage/08-CONTEXT.md
@.planning/phases/08-direct-coverage/08-RESEARCH.md
@.planning/phases/08-direct-coverage/08-PATTERNS.md
@.planning/phases/06-assertion-and-module-refinement/06-VERIFICATION.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
</context>

## Artifacts this phase produces

Phase-level inventory: see `08-01-PLAN.md` §"Artifacts this phase produces" for the full
table. This plan creates six of them:

| Artifact | Kind | Home |
|---|---|---|
| `RemovalOps` | interface, exactly two members: `rm(target, options)` and `rename(from, to)` | `extensions/pi-claude-marketplace/shared/fs-utils.ts` |
| `createRemovalOps()` | production factory returning thin bindings to `fs.rm` and `fs.rename` | `extensions/pi-claude-marketplace/shared/fs-utils.ts` |
| `createRemovalOpsFake(options)`, `RemovalOpsFake`, `RemovalOpsFakeOptions`, `RemovalOpsFakeCalls` | reusable concern-owned test double with per-target fault injection | `tests/platform/removal-ops-fake.ts` |
| `removalOpsContract(createOps)` | shared adapter contract both the real adapter and the fake pass | `tests/platform/removal-ops-contract.ts` |
| `tests/platform/removal-ops-fake.test.ts` | structural-supplement suite; runs the contract against the fake and proves the contract fails against a deliberately broken fake private to that file | `tests/platform/` |
| `InstallLedgerOptions.removalOps` | required member on the existing ledger options type | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts` |

Names are this plan's discretion under `D-08-11` provided the factory follows `create*` and the
reusable double follows `create*Fake`; the names above are the ones the rest of the phase cites,
so if you choose differently, record the mapping in the SUMMARY and use it consistently.

Task 3's action names the two patching constructs it deletes from one case, and its acceptance
criteria assert their absence from that case. Both constructs legitimately survive elsewhere in
the same file — the acceptance criteria are scoped to the converted case for exactly that reason —
and they are source calls being removed, not comment text.

<!-- planner-discipline-allow: node:fs -->
<!-- planner-discipline-allow: t.mock.method -->
<!-- planner-discipline-allow: syncBuiltinESMExports -->
<!-- planner-region-allow: t.mock.method -->
<!-- planner-region-allow: syncBuiltinESMExports -->

**Diff-range convention for this plan.** Before this plan's first commit, the executor records
`PLAN_BASE=$(git rev-parse HEAD)`. Every `git diff` check below is written
`git diff "$PLAN_BASE"..HEAD -- <paths>` (adding `git status --porcelain -- <paths>` when the
working tree is also in question). A bare `git diff` with no range is empty once the work is
committed, so it would pass vacuously and prove nothing.

<tasks>

<task type="auto">
  <name>Task 1: Declare the port, build its fake and shared contract, and thread fs-utils.ts</name>
  <files>extensions/pi-claude-marketplace/shared/fs-utils.ts, tests/platform/removal-ops-fake.ts, tests/platform/removal-ops-contract.ts, tests/platform/removal-ops-fake.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/shared/fs-utils.ts` (all of it — the numbered "Three helpers" header at 1-19, `cleanupStaging` at 40-53, `removeOrphanIfPresent` at 107-122 which calls `fs.rm` twice and stays unported, `RollbackReplacementInput` at 141-170, and `rollbackReplacementCommon` at 186-244 with its three leak-accumulating stages and the `fs.mkdir` at line 206)
    - `extensions/pi-claude-marketplace/platform/git-credential.ts` lines 55-70 and 290-315 (`CredentialOps` — one doc line per verb citing the requirement it serves — and `createCredentialOps`, a factory returning an object literal of thin bindings and nothing else)
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts` lines 110-165 (`GitOps` and `DEFAULT_GIT_OPS`, the bound-reference constant pattern, and the `gitOps?:` plus `?? DEFAULT_GIT_OPS` threading this port must NOT copy)
    - `tests/platform/credential-ops-fake.ts` (all 104 lines — the `import type`-only discipline, the four-interface shape: an options type with an explicit `boundary` discriminant, a per-verb call log, a state view, and a bundle; and the `<verb>Error?: Error` members that are the fault-injection seam)
    - `tests/platform/git-ops-fake.ts` lines 1-20 and 95-115 (the type-only imports with `node:fs/promises` as the only value import, and the explicit boundary refusal `createGitOpsFake requires the explicit memory boundary`)
    - `tests/platform/git-ops-contract.ts` and `tests/platform/git-ops-fake.test.ts` (the shape of a shared contract function and of the suite that runs it plus its broken-fake negative control)
    - `.claude/rules/typescript-unit-testing.md` §"Shared adapter contracts" and §"Design production code for its tests"
    - `08-RESEARCH.md` §"The Removal Port: Exact Inventory" (the call-site census table, the signature list, the `fs.mkdir` note, the `removeOrphanIfPresent` tension) and §"Boundary and zone legality" (including why the fake gets no `-fake.test.ts` under `tests/shared/` and why `tests/platform/` is the home)
    - `08-CONTEXT.md` `D-08-11`, `D-08-12`, `D-08-13`, `D-08-14`, `D-08-A08`, `D-08-A11`
  </read_first>
  <action>
Declare the port where its consumer lives, build its double and contract, and thread the two
`fs-utils.ts` functions. This task leaves the tree NOT typechecking — the rest of the call sites
land in task 2 — which is the compiler-forced consequence of a required parameter.

**`RemovalOps` in `extensions/pi-claude-marketplace/shared/fs-utils.ts`.** Declare the interface
in the consuming module rather than in a new `shared/removal-ops.ts`: a new production module
would create a new source-test pair that must itself read complete, for an interface and a
three-line factory. Two members only:

- `rm(target: string, options: { recursive?: boolean; force?: boolean }): Promise<void>`
- `rename(from: string, to: string): Promise<void>`

Give each member one doc line naming the requirement it serves and the leak path it makes
faultable. Cite `RCOV-02` and `D-08-13`.

**`createRemovalOps(): RemovalOps`** in the same module: an object literal of thin bindings to
`fs.rm` and `fs.rename`, and nothing else. Follow `createCredentialOps`'s shape — construction
performs no filesystem work; only the operations do.

**Write the asymmetry down in the port's header.** Every other `*Ops` in this tree is threaded
as an optional member with a `?? DEFAULT_*` fallback at each site. `D-08-12` forbids that here,
so `RemovalOps` is the only required `*Ops` in the tree. State that, and state why: a defaulted
port lets the obligation go quiet the moment a new call site forgets it, and the project's own
rule is to wire real adapters in one composition module rather than default a parameter to a
live boundary. A reviewer must meet a decision, not an oversight.

**Extend the module header's numbered helper list.** It currently says "Three helpers". Add the
port and record two scope lines as decisions: `removeOrphanIfPresent` calls `fs.rm` twice and
stays unported, because criterion 4 names `cleanupStaging` and widening the port to every
`fs.rm` in this file is scope this phase does not have (`D-08-A08`); and the `fs.mkdir` that
re-creates a backup's destination parent during the restore stage stays direct, so the restore
stage is faultable through `rename` but not through `mkdir`. Say that the port replaces the
syscall and not the `assertPathInside` containment chokepoint in front of it (NFR-10).

**Thread `cleanupStaging`.** It becomes `cleanupStaging(ops: RemovalOps, dir: string, label: string)`
— collaborator FIRST. Its body calls `ops.rm(dir, { recursive: true, force: true })` in place of
`fs.rm`; the ENOENT swallow, the leak-message text, and the `Promise<string | undefined>` return
are unchanged byte for byte, because existing tests compare that message exactly.

**Thread `rollbackReplacementCommon`.** It already takes one input object, so add `ops` as a
REQUIRED member of `RollbackReplacementInput`. Replace `fs.rm(pair.to, rmOptions)` with
`input.ops.rm(...)`, `fs.rename(backup.to, backup.from)` with `input.ops.rename(...)`, and pass
`input.ops` to both `cleanupStaging` calls. Leave the `fs.mkdir` call as it is. Every leak
message string stays byte-identical.

**`tests/platform/removal-ops-fake.ts`.** Model it on `credential-ops-fake.ts`:

- import `RemovalOps` with `import type` only — a fake pulls contracts, never production runtime
  code — and import no production value;
- `RemovalOpsFakeOptions` with a required `readonly boundary: "memory"` discriminant, plus
  per-target fault injection: a map or array keyed by absolute target path to the `Error` that
  `rm` should reject with, and the same for `rename` keyed by its source path. Per-target keying
  is the seam G1 needs — it is how "one `cleanupStaging` call fails while its siblings succeed"
  becomes expressible;
- optionally a seed of paths that are considered to exist, so `rename` can answer an error for a
  missing source;
- `RemovalOpsFakeCalls` with one array per verb recording the arguments each call received, in
  order — the ordering record is what the interleaved-warning proof reads;
- `RemovalOpsFake` bundling `removalOps: RemovalOps`, `calls: RemovalOpsFakeCalls`, and a state
  view answering which targets the fake considers removed;
- `createRemovalOpsFake(options)` refusing construction when `options.boundary !== "memory"`,
  with a message in the form the existing fakes use.

The fake simulates in memory and performs NO real filesystem work. That is a security property,
not a style preference: a double that really removed things could reach outside its fixture.

**`tests/platform/removal-ops-contract.ts`.** One exported contract function taking a factory
that answers a `RemovalOps`, covering the categories the project's rule names: removing a path
that exists, removing a path that is absent with `force`, removing a tree with `recursive`,
renaming over an absent destination, renaming when the source is absent, and the ordering of two
successive calls. Where a category does not apply to a two-verb removal port, record why in a
comment rather than omitting it silently.

**`tests/platform/removal-ops-fake.test.ts`.** Import BOTH `./removal-ops-fake.ts` and
`./removal-ops-contract.ts` — the structural-supplement exemption in both the direct-coverage
gate and the correspondence gate requires the suite to sit at `tests/platform/<name>-fake.test.ts`
with a `<name>-fake.ts` and a `<name>-contract.ts` beside it AND to import both. Run the contract
against `createRemovalOpsFake({ boundary: "memory" })`, assert the boundary refusal, and prove
the contract has teeth by running it against a deliberately broken fake kept private to this
file — one whose `rm` silently does nothing — with the expectation written independently of the
contract.

The real adapter's half of the contract belongs in `tests/shared/fs-utils.test.ts`, which owns
`createRemovalOps`; task 2 wires it, driving the contract against a real `mkdtemp` root.
  </action>
  <acceptance_criteria>
    - `extensions/pi-claude-marketplace/shared/fs-utils.ts` exports `RemovalOps` and `createRemovalOps`, and `RemovalOps` declares exactly two members.
    - `cleanupStaging`'s first parameter is the collaborator and its leak-message template string is byte-identical to its current value.
    - `RollbackReplacementInput` declares `ops` without a `?` and without a default.
    - `extensions/pi-claude-marketplace/shared/fs-utils.ts` contains no `DEFAULT_REMOVAL_OPS` constant and no `?? create` expression.
    - `extensions/pi-claude-marketplace/shared/fs-utils.ts`'s header names `removeOrphanIfPresent` as a recorded unported exception and names the direct `fs.mkdir` in the restore stage.
    - `removeOrphanIfPresent`, `pathExists`, `readDirEntriesTolerant`, and `isPlainMarkdownFile` each keep their current signature and still call `fs` directly.
    - `git diff "$PLAN_BASE"..HEAD -- extensions/pi-claude-marketplace/shared/fs-utils.ts` removes no `assertPathInside` call, and `grep -c assertPathInside extensions/pi-claude-marketplace/shared/fs-utils.ts` is at least as high as it was at `$PLAN_BASE`.
    - `tests/platform/removal-ops-fake.ts` imports `RemovalOps` with `import type` and imports no production value.
    - `tests/platform/removal-ops-fake.ts` contains no `node:fs` or `node:fs/promises` import.
    - `createRemovalOpsFake` throws when called with any `boundary` other than `"memory"`.
    - `tests/platform/removal-ops-fake.test.ts` imports both `./removal-ops-fake.ts` and `./removal-ops-contract.ts`.
    - `tests/platform/removal-ops-fake.test.ts` contains a broken-fake negative control whose expectation is written without calling the contract.
    - `node --test tests/platform/removal-ops-fake.test.ts` reports `# fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/platform/removal-ops-fake.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a non-zero `# fail` count, or `# pass 0` (a contract that registered no case proves nothing)</fails_when>
    <automated>npx tsc --noEmit > /tmp/tsc-08-05-task1.log 2>&1; grep -E "tests/platform/removal-ops" /tmp/tsc-08-05-task1.log; test $? -eq 1</automated>
    <fails_when>exit 1 from the final `test`, meaning grep printed a diagnostic naming one of the three new `tests/platform/removal-ops-*` files; diagnostics naming the unmigrated call sites are expected at this point and are cleared by task 2</fails_when>
    <automated>grep -c "node:fs" tests/platform/removal-ops-fake.ts; test $(grep -c "node:fs" tests/platform/removal-ops-fake.ts) -eq 0</automated>
    <fails_when>exit 1 from the final `test`, meaning the in-memory fake imports a filesystem module and could reach outside its fixture</fails_when>
  </verify>
  <done>The port is declared in its consumer with the asymmetry and both scope exceptions written into the header, `cleanupStaging` and `rollbackReplacementCommon` perform their removals through it, and the fake plus its shared contract plus the contract's own negative control are green.</done>
  <reversibility rating="costly">The collaborator becomes part of the public shape of 21 signatures across eleven production files, so reverting means migrating roughly forty-six call sites and their owner tests back. Taken deliberately: `D-08-11` records this rating, and a port that stops at `cleanupStaging`'s own signature closes nothing.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Migrate every production call site and the mechanical test-side argument threading</name>
  <files>extensions/pi-claude-marketplace/bridges/skills/stage.ts, extensions/pi-claude-marketplace/bridges/commands/stage.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts, extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update-swap.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-replace.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, tests/shared/fs-utils.test.ts, tests/bridges/skills/stage.test.ts, tests/bridges/commands/stage.test.ts, tests/bridges/agents/stage.test.ts, tests/orchestrators/plugin/install-outcome.test.ts, tests/orchestrators/plugin/enable-disable.test.ts</files>
  <read_first>
    - `npm run typecheck`'s own diagnostic list — this is the primary input for this task. The compiler enumerates every site; do not hunt for them by reading files.
    - `extensions/pi-claude-marketplace/bridges/skills/stage.ts` signatures only: `prepareStageSkills` (162), `commitPreparedSkills` (317), `abortPreparedSkills` (377), `replacePreparedSkills` (392), `rollbackSkillsReplacement` (462), `finalizeSkillsReplacement` (478), and the private `rollbackSkillsReplacementInternal` (504)
    - `extensions/pi-claude-marketplace/bridges/commands/stage.ts` signatures only: 163, 297, 358, 373, 443, 459, 485
    - `extensions/pi-claude-marketplace/bridges/agents/stage.ts` signatures only: 97, 318, 419, and in particular `replacePreparedAgents` at 434-437, whose second parameter is `options?: ReplacePreparedAgentsOptions`, plus 528, 545, 571
    - `extensions/pi-claude-marketplace/orchestrators/plugin/clone-cache.ts` the three exported entry points `materializePluginClone`, `materializeOrRefreshPluginMirror`, `seedSameRepoPluginMirrors` and the private `promoteStagingToClone`, `seedOnePluginMirror`; note that `resolvePluginPin` and `resolveGitPluginRootWithSubdir` need nothing
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` `addMarketplace`'s three overload signatures at 528, 531, 534 and `AddMarketplaceOptions`, plus the private `runAddInGuard` and `addGitClonedInGuard` that hold all four `cleanupStaging` calls
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts` `InstallLedgerOptions` at 149-166 (which already carries `credentialOps?`, `deviceFlowHttp?`, `cloneCacheSeam?`, `cloneProbe?` as optional collaborator seams) and the six bridge call sites in `runInstallLedgerBody`
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts` `buildInstallLedgerOptions` at 204-210 — the single assembly point for `InstallLedgerOptions`
    - `extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts` the `transaction.runInstallLedger(...)` call at ~313 and the `InstallLedgerTransaction`-shaped seam at 212-220
    - `extensions/pi-claude-marketplace/orchestrators/plugin/update-swap.ts` and `reinstall-replace.ts` — for `reinstall-replace.ts`, note the injected `operations` bundle at 105-130 whose fields are typed `typeof <fn>`, so the bundle's TYPE updates itself and only the real invocations need an argument
    - `08-RESEARCH.md` §"Signatures that must gain the collaborator", §"Production composition roots", §"Test-side blast radius: 4 files, 205 lines — and nothing else", and §"Common Pitfalls" entry 4
  </read_first>
  <action>
Let the compiler drive. Run `npm run typecheck`, fix every `TS2554` / `TS2345` / `TS1360`
diagnostic, re-run, repeat until it exits 0. There is no discovery work in this task.

**Parameter order is a uniform rule, not a per-site choice.** `replacePreparedAgents` already has
an optional second parameter, and appending a required parameter after an optional one is
`TS1016`. So the collaborator goes **FIRST** on every migrated positional signature, which also
matches `cleanupStaging(ops, dir, label)`. Where a function already takes a single input or
options object that carries collaborators — `RollbackReplacementInput`, `AddMarketplaceOptions`,
`InstallLedgerOptions`, and `clone-cache.ts`'s `args` objects — add a required `ops` member to
that object instead. Do not mix the two conventions within one file.

**The three bridges: seven functions each, six exported and one private.** All 21 gain the
collaborator. No bridge constructs it: each one receives it and forwards it, which the probe
confirmed by reporting a local constant in `bridges/agents/stage.ts` as unused. Leave
`discover.ts`, `unstage.ts`, `frontmatter.ts`, and `index-mutation.ts` untouched — and in
particular leave every `unstage*` signature alone, because none of them calls `cleanupStaging`;
they call `rm` directly on their own targets, which `D-08-13` leaves unported.

**The six composition roots construct the real operations.** `reinstall-replace.ts` (15 sites),
`update-swap.ts` (12), `clone-cache.ts` (5), `marketplace/add.ts` (4), plus — this plan's
extension, recorded in the objective — `install-flow.ts` and `enable-disable.ts` for the install
path. Each calls `createRemovalOps()` once and threads the result. No `?? DEFAULT` anywhere.

**The install path, specifically.** Add `readonly removalOps: RemovalOps` as a REQUIRED member of
`InstallLedgerOptions` and pass `opts.removalOps` to the six bridge call sites in
`runInstallLedgerBody`. Then supply it at the two places that build those options:
`buildInstallLedgerOptions` in `install-flow.ts`, and the `transaction.runInstallLedger(...)`
options literal in `enable-disable.ts`. `install-outcome.ts` therefore does NOT construct the
port — which is the whole point: a test driving `runInstallLedger` can now pass a faulting
collaborator and reach the `commitPrepared*` leak arms, which plan 07 needs.

Do not add the member to `InstallPluginOptions` or to any edge-handler option type: the ledger
options are where it belongs, and `install-flow.ts` is the composition root above them.

**Mechanical test-side threading.** After production typechecks, the residual diagnostics are
argument-count errors in the owner tests. Research measured 205 lines across
`tests/bridges/agents/stage.test.ts` (72), `tests/bridges/commands/stage.test.ts` (60),
`tests/bridges/skills/stage.test.ts` (59), and `tests/shared/fs-utils.test.ts` (14), plus the
`InstallLedgerOptions` literals in `tests/orchestrators/plugin/install-outcome.test.ts` and
`tests/orchestrators/plugin/enable-disable.test.ts` that this plan's extension adds. Five of the
`fs-utils` errors are `TS1360` on a `satisfies RollbackReplacementInput` literal now missing
`ops`.

For each site, pass whichever collaborator the case's intent calls for:

- a case whose subject is real on-disk behavior gets `createRemovalOps()`;
- a case that only needs the call to succeed gets `createRemovalOpsFake({ boundary: "memory" })`'s
  `removalOps`;
- a case that currently injects a fault by patching a builtin is NOT converted here — pass
  `createRemovalOps()` so it keeps its present behavior, and leave the conversion to plan 06,
  which owns `D-08-14` and the G1 cases. Record which sites you deferred in the SUMMARY so plan
  06 has the list.

Expect no other test file to break: a double written `async (args) => ...` and typed
`typeof materializePluginClone` stays assignable when the real parameter type gains a member,
because the target supplies more than the source reads. If a file outside this plan's
`files_modified` does raise a diagnostic, stop and record it rather than widening the plan
silently.

**Wire the real adapter's half of the shared contract.** In `tests/shared/fs-utils.test.ts`, add
a case that runs `removalOpsContract` against a factory returning `createRemovalOps()` over a
per-case `mkdtemp` root, removed with `t.after`. That is what makes the real adapter and the
fake pass one contract, and it is also what covers `createRemovalOps`'s bindings for the
`fs-utils` pair's direct coverage.
  </action>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -rn "cleanupStaging(" extensions/pi-claude-marketplace/` shows every call passing a collaborator as its first argument.
    - No file under `extensions/` contains a `RemovalOps` parameter declared with a `?` or with a default value.
    - No file under `extensions/` contains a `DEFAULT_REMOVAL_OPS` identifier.
    - `extensions/pi-claude-marketplace/bridges/skills/unstage.ts`, `bridges/commands/unstage.ts`, and `bridges/agents/unstage.ts` have no diff.
    - `extensions/pi-claude-marketplace/bridges/skills/discover.ts`, `bridges/commands/discover.ts`, `bridges/agents/discover.ts`, `bridges/agents/frontmatter.ts`, and `bridges/agents/index-mutation.ts` have no diff.
    - `InstallLedgerOptions` declares `removalOps` without a `?`; `buildInstallLedgerOptions` in `install-flow.ts` and the options literal in `enable-disable.ts` each supply it from a `createRemovalOps()` call.
    - `install-outcome.ts` contains no `createRemovalOps(` call.
    - `git diff "$PLAN_BASE"..HEAD --name-only` lists no file outside this plan's `files_modified`.
    - `tests/shared/fs-utils.test.ts` invokes `removalOpsContract` with a factory returning `createRemovalOps()`.
    - `node --test tests/bridges/skills/stage.test.ts tests/bridges/commands/stage.test.ts tests/bridges/agents/stage.test.ts tests/shared/fs-utils.test.ts tests/orchestrators/plugin/install-outcome.test.ts tests/orchestrators/plugin/enable-disable.test.ts` reports `# fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit — the migration is not complete until the whole tree, tests included, typechecks as one unit; any remaining diagnostic is an unmigrated call site</fails_when>
    <automated>node --test tests/bridges/skills/stage.test.ts tests/bridges/commands/stage.test.ts tests/bridges/agents/stage.test.ts tests/shared/fs-utils.test.ts tests/platform/removal-ops-fake.test.ts tests/orchestrators/plugin/install-outcome.test.ts tests/orchestrators/plugin/enable-disable.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a non-zero `# fail` count, or `# pass 0`</fails_when>
    <automated>npm run fallow</automated>
    <fails_when>non-zero exit, or the output reports any function above `maxCognitive`, `maxCyclomatic`, or `maxUnitSize`, any dead-code finding, any forbidden boundary edge, or any duplicate clone</fails_when>
    <automated>git diff "$PLAN_BASE"..HEAD --name-only > /tmp/08-05-raw.txt || exit 1; git status --porcelain --short > /tmp/08-05-status.txt || exit 1; cut -c4- /tmp/08-05-status.txt >> /tmp/08-05-raw.txt; sort -u /tmp/08-05-raw.txt > /tmp/08-05-touched.txt; grep -v -E "^(extensions/pi-claude-marketplace/(shared/fs-utils\.ts|bridges/(skills|commands|agents)/stage\.ts|orchestrators/plugin/(clone-cache|install-outcome|install-flow|enable-disable|update-swap|reinstall-replace)\.ts|orchestrators/marketplace/add\.ts)|tests/(platform/removal-ops-(fake\.ts|contract\.ts|fake\.test\.ts)|shared/fs-utils\.test\.ts|bridges/(skills|commands|agents)/stage\.test\.ts|orchestrators/plugin/(install-outcome|enable-disable)\.test\.ts)|\.planning/)" /tmp/08-05-touched.txt; test $? -eq 1</automated>
    <fails_when>exit 1 from the final `test`, meaning a file outside this plan's declared `files_modified` was changed — the migration widened beyond its enumerated blast radius and must be reported rather than absorbed</fails_when>
  </verify>
  <done>`npm run typecheck` exits 0 with the whole migration in place, every `cleanupStaging` call passes a collaborator, no signature carries a default or an optional marker for it, no `unstage*` or `discover` module changed, and the real adapter passes the shared contract.</done>
  <reversibility rating="costly">The same rating as task 1: this task is where the forty-six call sites actually move.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Prove the port is the seam by faulting one cleanup through it, end to end</name>
  <files>tests/bridges/skills/stage.test.ts</files>
  <read_first>
    - `tests/bridges/skills/stage.test.ts` lines 1-50 (the `createRequire` scaffold at 3, the `filesystemPromises` constant at 22, and `allocateCasePaths` at 26-45 — one `mkdtemp` per case registered with `t.after`) and lines 1017-1088 (the case "returns the complete cleanup leak after a successful rename": its scenario construction, the `expectedLeak` string built from `prepared.stagingRoot`, the builtin surgery at 1051-1071, and the assertions below it)
    - `tests/platform/removal-ops-fake.ts` as written in task 1 (the per-target fault map and the call log)
    - `extensions/pi-claude-marketplace/bridges/skills/stage.ts` `commitPreparedSkills` as migrated in task 2
    - `08-PATTERNS.md` §"The G1 leak-message tests"
    - `08-RESEARCH.md` §"Can the Port Land in One Atomic Plan?" §"Residual risk the plan must own"
  </read_first>
  <action>
Convert exactly ONE existing case from builtin patching to the port, as the proof that the seam
reaches the entry point G1's tests call. The remaining conversions and the new interleaved and
residue-partition assertions belong to plan 06, which owns `D-08-14`.

Take `tests/bridges/skills/stage.test.ts`'s case at 1017-1088, "returns the complete cleanup
leak after a successful rename". Keep its entire scenario construction: the prefixed
`allocateCasePaths` allocation, the real on-disk skill fixture, the
`satisfies ResolvedPluginInstallable` literal, the `prepareStage*` call, the
`assert.strictEqual(prepared.kind, "staged")` guard, the `expectedLeak` string built from
`prepared.stagingRoot`, and the `// arrange` / `// act` / `// assert` three-block body.

Replace only the fault seam. Where the case currently binds the original `rm`, builds an
`EACCES`-coded error, and installs a `t.mock.method` over the required `node:fs/promises`
namespace that throws for `prepared.stagingRoot` and delegates otherwise, construct a
`createRemovalOpsFake({ boundary: "memory" })` whose per-target fault map answers that same
error for `prepared.stagingRoot` and nothing else, and pass its `removalOps` into
`commitPreparedSkills`. Delete that case's `t.mock.method` call, its `removal.mock.restore()`
`t.after`, and its two `syncBuiltinESMExports()` calls. Keep the assertions below it unchanged:
the exact leak string, the target-bytes read, and the surviving staging directory.

The fake simulates removal in memory, so the staging directory now survives because nothing
removed it rather than because a patched `rm` refused. That is a stronger statement of the same
contract and the existing `stat` assertion still proves it.

Remove the file's `createRequire` import, its `filesystemPromises` constant, and its remaining
`syncBuiltinESMExports` calls ONLY if no other case in the file still patches a builtin. They
do — the file also patches `cp` and has further `rm` and `rename` sites — so leave the scaffold
in place and add a header note recording which verbs remain patched in this file and why: `cp`
is outside the port's verb set, and the remaining `rm` and `rename` sites await plan 06's
per-site classification.

Record the residual builtin-patching census honestly in the SUMMARY, with its measured
membership rather than an inherited claim. `D-08-14` states the census pins two files; the tree
holds more. Name every file that patches a builtin module or a prototype and the verbs each
patches — at minimum `tests/shared/fs-utils.test.ts`, `tests/bridges/skills/stage.test.ts`,
`tests/orchestrators/plugin/uninstall.test.ts`, `tests/orchestrators/marketplace/add.test.ts`,
and `tests/bridges/commands/stage.test.ts` — and say for each verb whether the removal port
reaches it. Do not claim the class is closed.
  </action>
  <acceptance_criteria>
    - The converted case in `tests/bridges/skills/stage.test.ts` constructs a `createRemovalOpsFake` and passes its `removalOps` into `commitPreparedSkills`.
    - That case contains no `t.mock.method` call and no `syncBuiltinESMExports` call within its own body.
    - The case's `expectedLeak` assertion and its surviving-staging-directory assertion are unchanged from their current form.
    - `tests/bridges/skills/stage.test.ts` carries a header note naming the verbs still patched in that file and whether the port reaches each.
    - `node --test tests/bridges/skills/stage.test.ts` reports `# fail 0`.
    - `node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/shared/fs-utils.ts` prints a line beginning `Direct coverage passed: `.
    - The SUMMARY lists every test file that patches a builtin module or a prototype, the verbs each patches, and whether the removal port reaches each verb.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridges/skills/stage.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports a non-zero `# fail` count, or `# pass 0`</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/shared/fs-utils.ts</automated>
    <fails_when>non-zero exit, or stdout contains `Incomplete direct coverage for`, or stdout carries no `Direct coverage passed:` line</fails_when>
    <automated>npm run typecheck && npx eslint extensions/pi-claude-marketplace tests --max-warnings=0 && npx prettier --check "**/*.{js,json,ts}"</automated>
    <fails_when>non-zero exit, or prettier lists any file under "Code style issues found"</fails_when>
    <automated>npm run test:corresponding && npm run test:corresponding:negative</automated>
    <fails_when>non-zero exit; in particular a failure naming `tests/platform/removal-ops-fake.test.ts` means the structural-supplement exemption did not apply and the suite is being asked for a production module that does not exist</fails_when>
  </verify>
  <done>One cleanup failure is driven through the injected collaborator from `commitPreparedSkills`, with no builtin patched in that case, the `fs-utils` pair reads complete, and the residual patching census is recorded with its real membership.</done>
  <reversibility rating="reversible">One test case's fault seam, plus a recorded census.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied path -> `assertPathInside` -> the removal syscall | NFR-10 refuses any write outside the scope roots; the port sits BEHIND that chokepoint and replaces only the syscall |
| injected `RemovalOps` -> the filesystem | every `rm` and `rename` in the staging and replacement lifecycle now flows through one substitutable object |
| test double -> the filesystem | a double that performed real removal could reach outside its fixture |
| plugin-supplied staging content -> `rename` into the live location | a bridge moves plugin content into a scope root under a generated name |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `RemovalOps` widened into a general filesystem facade | high | mitigate | The interface declares exactly two members and the acceptance criteria assert it; `pathExists`, `removeOrphanIfPresent`, `readDirEntriesTolerant`, and `isPlainMarkdownFile` are each asserted to keep their current signature and direct `fs` calls; callers keep owning containment and `git diff` is asserted to remove no `assertPathInside` call |
| T-08-02 | Tampering | `createRemovalOpsFake` performing real filesystem work | medium | mitigate | The fake imports `RemovalOps` as a type only and imports no filesystem module at all, asserted by a criterion and by a `grep` verify; it records and simulates in memory, and refuses construction without the explicit `"memory"` boundary |
| T-08-16 | Elevation of Privilege | a defaulted or optional collaborator letting a new call site silently bypass injection | high | mitigate | `D-08-12` makes the parameter required with no default; the acceptance criteria assert no `?`, no default, and no `DEFAULT_REMOVAL_OPS` identifier anywhere under `extensions/`, and `npm run typecheck` exiting 0 is the gate that a forgotten site cannot pass |
| T-08-17 | Tampering | an intermediate forwarding seam or compatibility adapter surviving the migration | medium | mitigate | `D-06-11` forbids it and the compiler enforces it: the signature change breaks all sites at once, so `npm run typecheck` exiting 0 is only reachable with the whole migration in place; a `git diff --name-only` verify refuses any file outside the enumerated blast radius |
| T-08-18 | Repudiation | a converted leak case that passes for a new reason | medium | mitigate | Task 3 keeps the converted case's `expectedLeak` string, target-bytes read, and surviving-directory assertions byte-identical, so the case still discriminates the same contract; the SUMMARY records the residual patching census with measured membership rather than claiming the class closed |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install and adds no dependency |
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `node --test` over the six migrated test files plus `tests/platform/removal-ops-fake.test.ts` exits 0 with `# fail 0`.
- `npm run fallow` exits 0 with no finding above threshold and no new clone.
- `npm run test:corresponding && npm run test:corresponding:negative` exits 0.
- `node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/shared/fs-utils.ts` prints `Direct coverage passed:`.
- `git diff "$PLAN_BASE"..HEAD --name-only` lists no file outside this plan's `files_modified`.
- Wave boundary: `npm run check`.
</verification>

<success_criteria>
- `cleanupStaging` and `rollbackReplacementCommon` perform every `rm` and `rename` through a required, named, typed collaborator, threaded from the public bridge and orchestrator entry points the G1 tests drive (ROADMAP criterion 4, RCOV-02).
- The whole tree typechecks as one unit with no forwarding seam, compatibility adapter, or optional marker at any point.
- The port's verb set is exactly `rm` and `rename`, with `removeOrphanIfPresent` and the restore-stage `fs.mkdir` recorded as decisions in the module header.
- A reusable in-memory fake and one shared contract exist, the real adapter passes the same contract, and the contract is proved to fail against a deliberately broken fake.
- One cleanup failure is observable from `commitPreparedSkills` with no builtin patched, and the residual patching census is recorded with its real membership.
</success_criteria>

<output>
Create `.planning/phases/08-direct-coverage/08-05-SUMMARY.md` when done.
</output>
