---
phase: 60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
  - extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/session-start.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/user-prompt-submit.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-tool-use.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use-failure.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-compact.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/post-compact.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts
  - tests/bridges/hooks/payloads/session-start.test.ts
  - tests/bridges/hooks/payloads/user-prompt-submit.test.ts
  - tests/bridges/hooks/payloads/pre-tool-use.test.ts
  - tests/bridges/hooks/payloads/post-tool-use.test.ts
  - tests/bridges/hooks/payloads/post-tool-use-failure.test.ts
  - tests/bridges/hooks/payloads/pre-compact.test.ts
  - tests/bridges/hooks/payloads/post-compact.test.ts
  - tests/bridges/hooks/payloads/session-end.test.ts
  - tests/architecture/hooks-translators.test.ts
autonomous: true
requirements:
  - PAYL-01
user_setup: []

must_haves:
  truths:
    - "A `mapPiToClaudeToolName(name: string): string` helper exists at `domain/components/hook-tool-names.ts` and returns the Claude-form capitalization for any of the seven Pi-form tool names (`bash`, `read`, `edit`, `write`, `grep`, `find`, `ls`) or the input name unchanged for any other string (CustomToolCallEvent passthrough per D-60-04 / Pitfall 1)."
    - "Eight `translate(event, ctx): ClaudeStdin` functions exist at `bridges/hooks/payloads/<event>.ts`, one per bucket-A Claude event, exporting a single `translate` symbol each (PAYL-01)."
    - "PreToolUse / PostToolUse / PostToolUseFailure translators call `mapPiToClaudeToolName` on `event.toolName` to populate Claude's `tool_name` field (TOOL-01 reuse)."
    - "A `TranslationContext` type and `buildTranslationContext(ctx: ExtensionContext): TranslationContext` factory exist at `bridges/hooks/translation-context.ts` exposing `sessionId`, `transcriptPath`, `cwd` derived from `ctx.cwd` and `ctx.sessionManager.getSessionId()` / `getSessionFile()` with an empty-string fallback for absent transcript (per Pitfall 7)."
    - "An architecture test at `tests/architecture/hooks-translators.test.ts` round-trips one fixture per event and verifies each translator emits byte-equal JSON; per-event unit tests under `tests/bridges/hooks/payloads/<event>.test.ts` cover edge cases (CustomToolCallEvent passthrough for the three tool events; SessionStart `reason` propagation; PostToolUseFailure `error` propagation)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts"
      provides: "mapPiToClaudeToolName helper colocated with PI_TO_CLAUDE_TOOL_NAMES const map"
      exports: ["mapPiToClaudeToolName"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts"
      provides: "TranslationContext type + buildTranslationContext factory"
      exports: ["TranslationContext", "buildTranslationContext"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/session-start.ts"
      provides: "SessionStart payload translator (PAYL-01)"
      exports: ["translate", "SessionStartStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/user-prompt-submit.ts"
      provides: "UserPromptSubmit payload translator (PAYL-01)"
      exports: ["translate", "UserPromptSubmitStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-tool-use.ts"
      provides: "PreToolUse payload translator with Pi→Claude tool-name map (PAYL-01 + TOOL-01)"
      exports: ["translate", "PreToolUseStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use.ts"
      provides: "PostToolUse payload translator (PAYL-01 + TOOL-01)"
      exports: ["translate", "PostToolUseStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use-failure.ts"
      provides: "PostToolUseFailure payload translator (PAYL-01 + TOOL-01)"
      exports: ["translate", "PostToolUseFailureStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-compact.ts"
      provides: "PreCompact payload translator (PAYL-01)"
      exports: ["translate", "PreCompactStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/post-compact.ts"
      provides: "PostCompact payload translator (PAYL-01)"
      exports: ["translate", "PostCompactStdin"]
    - path: "extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts"
      provides: "SessionEnd payload translator (PAYL-01)"
      exports: ["translate", "SessionEndStdin"]
    - path: "tests/architecture/hooks-translators.test.ts"
      provides: "Wave 0 architecture pin for PAYL-01 + D-60-04: 8 round-trip fixtures + TOOL-01 application gate"
  key_links:
    - from: "bridges/hooks/payloads/pre-tool-use.ts"
      to: "domain/components/hook-tool-names.ts"
      via: "mapPiToClaudeToolName import"
      pattern: "mapPiToClaudeToolName"
    - from: "bridges/hooks/payloads/post-tool-use.ts"
      to: "domain/components/hook-tool-names.ts"
      via: "mapPiToClaudeToolName import"
      pattern: "mapPiToClaudeToolName"
    - from: "bridges/hooks/payloads/post-tool-use-failure.ts"
      to: "domain/components/hook-tool-names.ts"
      via: "mapPiToClaudeToolName import"
      pattern: "mapPiToClaudeToolName"
    - from: "tests/architecture/hooks-translators.test.ts"
      to: "bridges/hooks/payloads/*.ts"
      via: "import { translate } from each payload"
      pattern: "import.*translate.*payloads"
---

<objective>
Stand up the translator + tool-name helper foundation for Phase 60.

Purpose: PAYL-01 closure. Eight hand-authored `translate(event, ctx): ClaudeStdin` functions under `bridges/hooks/payloads/`, plus a `mapPiToClaudeToolName(name)` helper (locked in must_haves; CONTEXT.md D-60-04 referenced this symbol but it does NOT exist yet — Phase 60 ships it), plus a `TranslationContext` type/factory at `bridges/hooks/translation-context.ts` so the exec body (Plan 60-02) has a typed, tested input ready to call without further design.

Output: 9 new TypeScript modules (8 translators + 1 context module), 1 new helper export, 9 new test files (8 per-event unit + 1 architecture-test round-trip pin). Zero modifications to dispatch.ts / dispatch-exec.ts / event-router.ts / orchestrators — this plan creates only the inputs the exec body will consume.

REQ closure (this plan): PAYL-01 (translators + TOOL-01 reuse via helper).

Decision coverage: D-60-04 (hand-authored `translate()` per file); references D-58-04 (TOOL-01 location at `domain/components/hook-tool-names.ts`) which is unchanged here, only extended with the helper.

User decisions implemented:
- D-60-04: hand-authored per-event translators (no schema-driven engine)
- TOOL-01 reuse: thin `mapPiToClaudeToolName` helper colocated with the const map (chosen over const-direct-with-fallback per Research Open Question 1 recommendation — single tested call site for the three tool translators)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-hook-execution-payload-translators-env-vars/60-CONTEXT.md
@.planning/phases/60-hook-execution-payload-translators-env-vars/60-RESEARCH.md
@.planning/phases/60-hook-execution-payload-translators-env-vars/60-PATTERNS.md
@.planning/phases/60-hook-execution-payload-translators-env-vars/60-VALIDATION.md
@extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
@extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts
@extensions/pi-claude-marketplace/platform/pi-api.ts
@docs/research/claude-hook-config-syntax.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `mapPiToClaudeToolName` helper and define `TranslationContext` foundation</name>
  <files>extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts, extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts, tests/architecture/hooks-tool-name-map.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts (existing PI_TO_CLAUDE_TOOL_NAMES const map at lines 77-88; the helper goes after the const)
    - tests/architecture/hooks-tool-name-map.test.ts (Phase 58 architecture-test for the const map — review patterns; extend if needed)
    - .planning/phases/60-hook-execution-payload-translators-env-vars/60-RESEARCH.md § "Pi → Claude tool-name lookup" (lines 749-774; canonical helper shape)
    - .planning/phases/60-hook-execution-payload-translators-env-vars/60-PATTERNS.md § "domain/components/hook-tool-names.ts (MAYBE — add helper)" (lines 286-296)
    - extensions/pi-claude-marketplace/platform/pi-api.ts (ExtensionContext shape + ReadonlySessionManager fields exposed)
    - node_modules/@earendil-works/pi-coding-agent/dist/core/session-manager.d.ts (ReadonlySessionManager.getSessionId returns string; getSessionFile returns string | undefined)
    - .claude/rules/typescript-comments.md (comment policy — D-NN-NN / REQ-IDs only)
  </read_first>
  <behavior>
    - Test 1 (helper unit, in hooks-tool-name-map.test.ts): `mapPiToClaudeToolName("bash")` returns `"Bash"`; same for the other 6 Pi-form names returning their Claude-form (`"Read"`, `"Edit"`, `"Write"`, `"Grep"`, `"Glob"`, `"LS"`).
    - Test 2 (helper unit): `mapPiToClaudeToolName("mcp__server__tool")` returns `"mcp__server__tool"` (passthrough; D-60-04 CustomToolCallEvent path; Pitfall 1).
    - Test 3 (helper unit): `mapPiToClaudeToolName("Bash")` (already capitalized) returns `"Bash"` (passthrough; the helper is one-way Pi→Claude, not bidirectional).
    - Test 4 (TranslationContext factory unit, in this task — colocate with helper test or create `tests/bridges/hooks/translation-context.test.ts`; planner choice): `buildTranslationContext(ctx)` returns an object with `sessionId = ctx.sessionManager.getSessionId()`, `cwd = ctx.cwd`, `transcriptPath = ctx.sessionManager.getSessionFile() ?? ""` (empty-string fallback per Pitfall 7).
  </behavior>
  <action>
    Add a new exported function `mapPiToClaudeToolName(name: string): string` to `domain/components/hook-tool-names.ts` immediately after the `PI_TO_CLAUDE_TOOL_NAMES` declaration (the const map ends around line 88). Implementation: `return (PI_TO_CLAUDE_TOOL_NAMES as Record<string, string>)[name] ?? name;`. JSDoc anchors: PAYL-01, TOOL-01, D-60-04, Pitfall 1 (CustomToolCallEvent passthrough). NO `Phase 60` / `Plan 01` / `Pitfall N` strings in source comments (typescript-comments policy).

    Create `extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts` exporting (a) an interface `TranslationContext` with readonly fields `sessionId: string`, `transcriptPath: string`, `cwd: string`; (b) a factory `buildTranslationContext(ctx: ExtensionContext): TranslationContext` that reads `ctx.cwd`, `ctx.sessionManager.getSessionId()`, and `ctx.sessionManager.getSessionFile()` (with empty-string fallback when undefined per D-60-06 / Pitfall 7). Import `ExtensionContext` from `../../platform/pi-api.ts`. JSDoc anchors: D-60-06 (per-session env file path uses sessionId), Pitfall 7 (transcriptPath fallback). The file is bridge-internal; do NOT re-export through `bridges/hooks/index.ts`.

    Extend `tests/architecture/hooks-tool-name-map.test.ts` with a new test block for `mapPiToClaudeToolName` exercising the 4 behaviors above (7 Pi-form mappings, MCP literal passthrough, already-Claude passthrough, unknown string passthrough). Per-block heading uses the existing Phase 58 convention (REQ-IDs / D-NN, no `Phase N` / `Plan N`).

    Add a TranslationContext factory test fixture — either inline in `hooks-tool-name-map.test.ts` (if cohesive) or a new `tests/bridges/hooks/translation-context.test.ts`. Mock `ExtensionContext` with a stub `sessionManager` exposing `getSessionId()` returning a fixed string and `getSessionFile()` returning a fixed string (case 1) and undefined (case 2). Assert (a) sessionId == stubbed value, (b) cwd == stubbed `ctx.cwd`, (c) transcriptPath == stubbed value in case 1 and `""` in case 2.

    Do NOT modify the existing const map or its Phase 58 architecture test invariants. Do NOT widen the helper into bidirectional translation (Claude→Pi mapping is unrelated to PAYL-01).
  </action>
  <verify>
    <automated>node --test tests/architecture/hooks-tool-name-map.test.ts tests/bridges/hooks/translation-context.test.ts 2>/dev/null; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function mapPiToClaudeToolName" extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts` returns exactly one match.
    - `grep -n "export interface TranslationContext\|export function buildTranslationContext" extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts` returns exactly two matches.
    - `node --test tests/architecture/hooks-tool-name-map.test.ts` exits 0 with the new test cases (helper passthrough, 7 mappings, already-Claude passthrough) green.
    - The `TranslationContext.transcriptPath` empty-string fallback for `getSessionFile() === undefined` is exercised in at least one test fixture.
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no new TS errors introduced).
    - `grep -nE "Phase [0-9]|Plan [0-9]|Pitfall [0-9]|Pattern [0-9]" extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts | grep -v '^[^:]*:[^:]*:\s*//.*\(D-\|EXEC-\|PAYL-\|HOOK-\|WR-\|NFR-\|TOOL-\)'` returns no offenders (comment policy gate).
  </acceptance_criteria>
  <done>The Pi→Claude tool-name helper exists and is unit-tested; the TranslationContext type + factory exist and are unit-tested; the typecheck stays green; comment policy holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 8 bucket-A payload translators with per-event fixtures</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/payloads/session-start.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/user-prompt-submit.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-tool-use.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use-failure.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-compact.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/post-compact.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts, tests/bridges/hooks/payloads/session-start.test.ts, tests/bridges/hooks/payloads/user-prompt-submit.test.ts, tests/bridges/hooks/payloads/pre-tool-use.test.ts, tests/bridges/hooks/payloads/post-tool-use.test.ts, tests/bridges/hooks/payloads/post-tool-use-failure.test.ts, tests/bridges/hooks/payloads/pre-compact.test.ts, tests/bridges/hooks/payloads/post-compact.test.ts, tests/bridges/hooks/payloads/session-end.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts (Task 1 output — TranslationContext shape)
    - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts (Task 1 output — mapPiToClaudeToolName helper for tool events)
    - extensions/pi-claude-marketplace/platform/pi-api.ts (re-exports of `SessionStartEvent`, `SessionShutdownEvent`, `SessionBeforeCompactEvent`, `SessionCompactEvent`, `InputEvent`, `ToolCallEvent`, `ToolResultEvent`)
    - node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/types.d.ts (per-event payload shapes — SessionStart `reason`, SessionShutdown `reason`, ToolCall `toolName`/`input`, ToolResult `output`/`isError`, etc.)
    - docs/research/claude-hook-config-syntax.md § 3 "Hook stdin payload contract per event type" (authoritative per-event Claude-side field set — driver for translator output shapes)
    - docs/research/claude-hooks-vs-pi-events.md § "Bucket A 1:1 mapping table" (Pi field ↔ Claude field correspondence)
    - .planning/phases/60-hook-execution-payload-translators-env-vars/60-RESEARCH.md § Pattern 4 "Per-event payload translator" (canonical translator shape, lines 360-401)
    - .planning/phases/60-hook-execution-payload-translators-env-vars/60-RESEARCH.md § Pitfall 1 (`event.toolName` for CustomToolCallEvent is open `string`)
    - .planning/phases/60-hook-execution-payload-translators-env-vars/60-PATTERNS.md § "bridges/hooks/payloads/<event>.ts (NEW × 8)" (canonical shape, lines 107-148)
    - .claude/rules/typescript-comments.md
  </read_first>
  <behavior>
    - Test per translator file (one per event): given a representative Pi event payload + a stubbed TranslationContext, `translate(event, ctx)` returns the byte-equal expected Claude stdin JSON. Each test asserts `JSON.stringify(translate(event, ctx))` against an inline expected JSON string (hand-authored fixture pair).
    - PreToolUse / PostToolUse / PostToolUseFailure each additionally have a CustomToolCallEvent test (toolName = `mcp__server__tool`, no map entry) confirming passthrough via `mapPiToClaudeToolName` (Pitfall 1 closure).
    - PreToolUse / PostToolUse / PostToolUseFailure each additionally have a known-Pi-name test (toolName = `bash`) confirming capitalization to `Bash` via the helper.
    - SessionStart test asserts `hook_event_name === "SessionStart"` and the `reason` field is propagated verbatim from `SessionStartEvent.reason`.
    - SessionEnd test asserts `hook_event_name === "SessionEnd"` and the `reason` field is propagated.
    - PreCompact / PostCompact tests assert `hook_event_name` is the matching string and `trigger` is propagated from the Pi event.
    - UserPromptSubmit test asserts `hook_event_name === "UserPromptSubmit"` and `prompt` is propagated from the InputEvent payload.
    - PostToolUseFailure test additionally asserts that error-shaped output (Pi `event.output`) is propagated into the Claude `tool_response` (or whatever field the upstream contract names; consult `docs/research/claude-hook-config-syntax.md § 3`).
    - Every fixture sets all common-envelope fields (`session_id`, `transcript_path`, `cwd`, `hook_event_name`) from the stubbed TranslationContext and asserts they appear in the output.
  </behavior>
  <action>
    Create 8 translator files under `extensions/pi-claude-marketplace/bridges/hooks/payloads/`. Each file:

    1. Imports the per-event Pi payload type from `../../../platform/pi-api.ts` (e.g. `SessionStartEvent`, `ToolCallEvent`, `InputEvent`, etc.).
    2. Imports `TranslationContext` from `../translation-context.ts`.
    3. For the three tool events ONLY: imports `mapPiToClaudeToolName` from `../../../domain/components/hook-tool-names.ts`.
    4. Defines and exports an interface `<Event>Stdin` describing the Claude-side payload shape (readonly fields; literal `hook_event_name`).
    5. Exports `translate(event: <PiEvent>, ctx: TranslationContext): <Event>Stdin` returning the per-event mapping.

    Per-event field maps (driven by `docs/research/claude-hook-config-syntax.md § 3`):

    - **session-start.ts:** common envelope + `source` derived from `SessionStartEvent.reason`. NOTE — Claude's contract names this field `source` (not `reason`); confirm exact name from `claude-hook-config-syntax.md § 3` and use that. `hook_event_name: "SessionStart"`.
    - **session-end.ts:** common envelope + `reason` from `SessionShutdownEvent.reason`. `hook_event_name: "SessionEnd"`.
    - **pre-compact.ts:** common envelope + `trigger` from `SessionBeforeCompactEvent.trigger`. `hook_event_name: "PreCompact"`.
    - **post-compact.ts:** common envelope + `trigger` from `SessionCompactEvent.trigger`. `hook_event_name: "PostCompact"`.
    - **user-prompt-submit.ts:** common envelope + `prompt` from `InputEvent` (confirm Pi field name from `pi-api.ts`; usually `event.input` or `event.text`). `hook_event_name: "UserPromptSubmit"`.
    - **pre-tool-use.ts:** common envelope + `tool_name: mapPiToClaudeToolName(event.toolName)` + `tool_input: event.input`. `hook_event_name: "PreToolUse"`.
    - **post-tool-use.ts:** common envelope + `tool_name: mapPiToClaudeToolName(event.toolName)` + `tool_input: event.input` (input replay; consult § 3 for exact field name) + `tool_response: event.output`. `hook_event_name: "PostToolUse"`.
    - **post-tool-use-failure.ts:** common envelope + `tool_name: mapPiToClaudeToolName(event.toolName)` + `tool_input: event.input` + `tool_response: event.output` (Pi propagates errored output here per the dispatch.ts isError split). `hook_event_name: "PostToolUseFailure"`.

    JSDoc anchor each translator with REQ-ID `PAYL-01`, decision `D-60-04`. The three tool translators additionally anchor `TOOL-01` and `Pitfall 1`-equivalent (use surviving anchor — drop the bare `Pitfall N`; cite `CustomToolCallEvent passthrough` or `TOOL-01` exhaustively). The header docstring of each file is 3-6 lines: what the translator produces, which REQ/D-NN it implements, and which Pi event type it consumes.

    Create 8 test files under `tests/bridges/hooks/payloads/`. Each test file imports `translate` from its sibling source file, builds a stub `TranslationContext` literal (`{ sessionId: "sess-1", transcriptPath: "/tmp/t.jsonl", cwd: "/proj" }`), constructs a representative Pi event (cast through the peer-dep type), invokes `translate`, and asserts `JSON.stringify(actual)` equals a hand-authored expected JSON string. Tool-event tests add the additional CustomToolCallEvent + known-Pi-name cases described in `<behavior>`.

    Do NOT export the translators through `bridges/hooks/index.ts` (D-04 opaque-handle convention — bridge-internal only). Do NOT modify `bridges/hooks/dispatch.ts` or `dispatch-exec.ts` (that wiring lives in Plan 60-02 and 60-03).

    Comment policy: NO `Phase 60`, `Plan 01`, `Pitfall N`, `Pattern N` references in source. JSDoc traceability uses `PAYL-01`, `TOOL-01`, `D-60-04`.
  </action>
  <verify>
    <automated>node --test tests/bridges/hooks/payloads/ 2>/dev/null; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `ls extensions/pi-claude-marketplace/bridges/hooks/payloads/*.ts | wc -l` returns 8.
    - `ls tests/bridges/hooks/payloads/*.test.ts | wc -l` returns 8.
    - `grep -lE '^export function translate' extensions/pi-claude-marketplace/bridges/hooks/payloads/*.ts | wc -l` returns 8.
    - `grep -lE 'mapPiToClaudeToolName' extensions/pi-claude-marketplace/bridges/hooks/payloads/{pre,post}-tool-use*.ts | wc -l` returns 3 (the three tool translators).
    - `grep -L 'mapPiToClaudeToolName' extensions/pi-claude-marketplace/bridges/hooks/payloads/{session,user,pre-compact,post-compact}*.ts | wc -l` returns 5 (the five non-tool translators do NOT import the helper; pre-compact + post-compact are two files, session-start + session-end + user-prompt-submit are three — total 5).
    - `node --test tests/bridges/hooks/payloads/` exits 0; all 8 per-event tests pass.
    - `npx tsc --noEmit -p tsconfig.json` exits 0.
    - `grep -nE "Phase [0-9]|Plan [0-9]|Pitfall [0-9]|Pattern [0-9]" extensions/pi-claude-marketplace/bridges/hooks/payloads/*.ts | grep -vE '(D-[0-9]|EXEC-|PAYL-|HOOK-|WR-|TOOL-|NFR-)' | grep -v '^\s*$'` returns no offenders.
    - The translators are NOT re-exported through `bridges/hooks/index.ts` (`grep -nE "payloads" extensions/pi-claude-marketplace/bridges/hooks/index.ts` returns no matches).
  </acceptance_criteria>
  <done>All 8 bucket-A translators ship with byte-equal fixture tests; TypeScript compilation stays green; comment policy holds; the three tool translators route through `mapPiToClaudeToolName`.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 architecture-test scaffold pinning PAYL-01 + D-60-04 round-trip invariant</name>
  <files>tests/architecture/hooks-translators.test.ts</files>
  <read_first>
    - tests/architecture/hooks-foundation.test.ts (Phase 57 baseline — per-block header convention; how Phase 60's test should look)
    - tests/architecture/hooks-dispatch.test.ts (Phase 59 baseline — per-block layout reference)
    - tests/architecture/hooks-supportability.test.ts (Phase 58 baseline — closed-set fixture-driven gate style)
    - extensions/pi-claude-marketplace/bridges/hooks/payloads/ (Task 2 output — all 8 translators)
    - .planning/phases/60-hook-execution-payload-translators-env-vars/60-VALIDATION.md (Wave 0 requirements — `hooks-translators.test.ts` listed)
    - .claude/rules/typescript-comments.md
  </read_first>
  <action>
    Create `tests/architecture/hooks-translators.test.ts` as the Wave 0 architecture pin for PAYL-01 / D-60-04. The file iterates over a closed set of 8 events (SessionStart, UserPromptSubmit, PreToolUse, PostToolUse, PostToolUseFailure, PreCompact, PostCompact, SessionEnd) and asserts that:

    Block A — Translator presence:
    - For each event in the bucket-A 8-tuple, the corresponding `bridges/hooks/payloads/<kebab-event>.ts` file exists and exports a single `translate` function (introspection via `import * as mod` + `typeof mod.translate === "function"`).

    Block B — Round-trip fixtures:
    - For each event, hand-authored `(piEvent, translationContext, expectedJson)` fixture triple. Test asserts `JSON.stringify(translate(piEvent, ctx))` equals `expectedJson`. Fixtures inline in the test file (per Phase 58 / 59 per-block convention) — no external fixture directory.

    Block C — TOOL-01 application gate:
    - For each of the three tool events (PreToolUse, PostToolUse, PostToolUseFailure), assert that when `event.toolName = "bash"` (Pi-form lowercase), the output JSON contains `"tool_name":"Bash"` (Claude-form capitalized). This pins the `mapPiToClaudeToolName` invariant at the architecture-test level (not just per-translator unit level).

    Block D — CustomToolCallEvent passthrough:
    - For each of the three tool events, assert that when `event.toolName = "mcp__server__tool"`, the output contains `"tool_name":"mcp__server__tool"` (passthrough; the helper's `??` fallback arm is exercised).

    Per-block heading uses the Phase 58/59 convention with REQ/D-NN anchors only. No `Phase N` / `Plan N` / `Pitfall N` strings.

    The test file scaffolds Wave 0 — Plan 60-02 and 60-03 use it as the architecture pin during their iteration. After this task lands, the file must be GREEN against the 8 translators shipped in Task 2.
  </action>
  <verify>
    <automated>node --test tests/architecture/hooks-translators.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/hooks-translators.test.ts` exits 0 with all blocks (A presence, B round-trip × 8, C TOOL-01 × 3, D passthrough × 3) passing.
    - `grep -cE '"SessionStart"|"UserPromptSubmit"|"PreToolUse"|"PostToolUse"|"PostToolUseFailure"|"PreCompact"|"PostCompact"|"SessionEnd"' tests/architecture/hooks-translators.test.ts` returns at least 16 (each event appears in at least 2 places: closed-set declaration + fixture).
    - `grep -nE "Phase [0-9]|Plan [0-9]|Pitfall [0-9]|Pattern [0-9]" tests/architecture/hooks-translators.test.ts | grep -vE '(D-[0-9]|EXEC-|PAYL-|HOOK-|WR-|TOOL-|NFR-)' | grep -v '^\s*$'` returns no offenders.
    - `npm run check 2>&1 | tail -50` shows the new test file in the test run output with 0 failures attributed to it.
  </acceptance_criteria>
  <done>The Wave 0 architecture pin for PAYL-01 / D-60-04 is GREEN; subsequent plans inherit the closed-set 8-event invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi runtime → translator | Pi event payloads enter the translator; payloads are runtime-shaped (peer-dep typed) but could theoretically be malformed |
| Translator → child stdin (NOT in this plan) | The translator output is consumed by Plan 60-02's spawn body; this plan creates the input boundary only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-01 | Information disclosure | `mapPiToClaudeToolName` passthrough for arbitrary `string` arm | accept | The CustomToolCallEvent toolName is plugin-supplied (`mcp__server__tool`); passthrough exposes the name verbatim to Claude's stdin (this IS the expected upstream behavior). No secret material flows through `event.toolName`. |
| T-60-01-02 | Tampering | A translator output that drops or renames Claude's expected field (`tool_name`, `hook_event_name`) | mitigate | Per-event round-trip fixtures in `hooks-translators.test.ts` Block B + per-event unit tests in `tests/bridges/hooks/payloads/` byte-equal-assert the full output JSON, catching any field drop at CI time. |
| T-60-01-03 | Tampering | A new bucket-A event added in v1.14+ without an accompanying translator | mitigate | `hooks-translators.test.ts` Block A iterates over the BucketAEvent closed-set tuple (imported from `domain/components/hook-events.ts`); a new event without a matching translator file fails the presence assertion. |
| T-60-01-04 | Information disclosure | `transcriptPath` empty-string fallback exposes nothing — but a hook that reads `transcript_path` and fails open could log a misleading error | accept | Documented in Pitfall 7; the alternative (synthesized fake path) is more misleading. Phase 63 SURF-06 docs will document the contract for plugin authors. |
| T-60-SC | Tampering | No new npm/pip/cargo installs in this plan | n/a | Phase 60 ships zero new dependencies — no package legitimacy check required. |
</threat_model>

<verification>
- `node --test tests/bridges/hooks/payloads/ tests/architecture/hooks-translators.test.ts tests/architecture/hooks-tool-name-map.test.ts tests/bridges/hooks/translation-context.test.ts` exits 0.
- `npx tsc --noEmit -p tsconfig.json` exits 0.
- `npm run check` shows zero regressions across Phase 57/58/59 architecture tests; the new tests appear in the run summary.
- Comment policy gate: `grep -rnE "Phase [0-9]|Plan [0-9]|Pitfall [0-9]|Pattern [0-9]" extensions/pi-claude-marketplace/bridges/hooks/payloads/ extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts | grep -vE '(D-[0-9]|EXEC-|PAYL-|HOOK-|WR-|TOOL-|NFR-)' | grep -v '^\s*$'` returns no offenders.
</verification>

<success_criteria>
- PAYL-01 closure: 8 translators exist, each tested for round-trip fidelity; TOOL-01 reuse via `mapPiToClaudeToolName` applied to the three tool events.
- D-60-04 closure: hand-authored per-file translators (no schema engine introduced).
- Wave 0 architecture pin (`hooks-translators.test.ts`) GREEN, ready for Plan 60-02 / 60-03 to use.
- `TranslationContext` foundation ready for Plan 60-02 exec body to consume.
- Zero modifications to dispatch.ts / dispatch-exec.ts / event-router.ts / orchestrators / index.ts (later plans own those).
- `npm run check` GREEN.
</success_criteria>

<output>
Create `.planning/phases/60-hook-execution-payload-translators-env-vars/60-01-SUMMARY.md` when done. Include: artifact list (8 translators + helper + TranslationContext + 8 unit tests + 1 architecture test), REQ/D-NN closure status (PAYL-01 satisfied; D-60-04 + TOOL-01 reuse), Wave 0 status (hooks-translators.test.ts GREEN), and explicit traceability table (Pi field → Claude field per event) for the downstream plans to reference without re-reading `docs/research/claude-hook-config-syntax.md`.
</output>
</content>
</invoke>
