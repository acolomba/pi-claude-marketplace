---
phase: 108-domain-and-platform
plan: 04
type: execute
wave: 7
depends_on: [108-22]
files_modified:
  - tests/domain/components/hook-if-targets.test.ts
  - tests/orchestrators/marketplace/_fixtures/README.md
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/marketplace/update.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every supported hook-if target maps to its exact Pi target and precedence remains stable (D-01)."
    - "The canonical owner applies D-02 through D-04 to all modified runtime cases."
    - "The assigned marketplace consumers and fixture reference use concern-local support without production-pair overclaim (D-07)."
  artifacts:
    - path: tests/domain/components/hook-if-targets.test.ts
      provides: Canonical mirrored owner for hook-if-targets.ts
  key_links:
    - from: tests/domain/components/hook-if-targets.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hook-if-targets.ts
      via: direct mapping and precedence imports
  prohibitions:
    - "No expected mapping may be generated from production keys or values."
---

<objective>
Normalize the hook-if target owner while preserving its complete mapping and precedence contract (D-01 through D-05).

Purpose: Prevent silent target omissions or priority changes.
Output: A compliant, direct owner with literal mapping evidence.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-22-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hook-if-targets.test.ts` — modified mapping owner; no production symbol added.
- The two marketplace tests and fixture README — bounded concern-local support migration only; their production pairs remain open.

<tasks>
<task type="auto">
  <name>Task 1: Normalize the complete hook-if target mapping</name>
  <files>tests/domain/components/hook-if-targets.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hook-if-targets.ts` — authoritative mapping exports.
    - `tests/domain/components/hook-if-targets.test.ts` — existing owner.
    - `.claude/rules/typescript-unit-testing.md` — required structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-object assertion guidance.
  </read_first>
  <action>Rewrite the existing complete-mapping case to use an independently literal object and normalize it per D-01, D-02, and D-04. Assert every supported input target and exact Pi output in one whole-value comparison. Use `// act &amp; assert` only if the test contains one throwing/rejection expression per D-03. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hook-if-targets.test.ts</automated></verify>
  <acceptance_criteria>
    - The complete exported mapping equals a literal object not derived from production data.
    - No supported key is asserted only indirectly.
    - Exact required phase comments appear in all modified runtime cases.
  </acceptance_criteria>
  <done>The complete target mapping is independently locked.</done>
</task>
<task type="auto">
  <name>Task 2: Preserve precedence and direct source ownership</name>
  <files>tests/domain/components/hook-if-targets.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hook-if-targets.ts` — precedence order source.
    - `tests/domain/components/hook-if-targets.test.ts` — normalized mapping case.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope boundary.
  </read_first>
  <action>Normalize the precedence case and assert the entire priority sequence as a literal ordered array. Add an explicit uniqueness assertion over the literal-supported targets so equality does not silently collapse two mappings. Keep the expected sequence independent and do not edit the already-approved rules or guidelines per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hook-if-targets.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-if-targets.ts</automated></verify>
  <acceptance_criteria>
    - Precedence equals the complete literal source-defined sequence.
    - Supported targets are unique and every mapping is represented.
    - Direct coverage accepts this owner as the source correspondent.
  </acceptance_criteria>
  <done>Mapping membership and precedence remain stable under the canonical owner.</done>
</task>
<task type="auto">
  <name>Task 3: Migrate the remaining marketplace consumers and fixture reference</name>
  <files>tests/orchestrators/marketplace/_fixtures/README.md, tests/orchestrators/marketplace/remove.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <read_first>
    - `tests/platform/credential-ops-fake.ts`, `tests/domain/device-flow-fake.ts`, and `tests/platform/git-ops-fake.ts` — stabilized guarded replacements.
    - The three listed consumers — helper imports/path documentation, factory calls, and cleanup.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-02 through D-04, D-07, D-18, and D-19.
  </read_first>
  <action>Update this non-overlapping marketplace batch and fixture README to the concern-local helper paths and guarded factories, preserving scenarios and documentation meaning without migration-history prose. Import-only edits do not rewrite cases. If a factory call changes a runtime case body, normalize the whole case with exact lowercase `// arrange`, `// act`, and `// assert` comments in order with blank-line separation, or exact lowercase `// act &amp; assert` only for one throwing/rejection expression; preserve D-03 type-only exceptions and case-owned cleanup. Do not edit consumer production sources or delete generic helpers here.</action>
  <verify><automated>node --test tests/orchestrators/marketplace/remove.test.ts tests/orchestrators/marketplace/update.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-if-targets.ts</automated></verify>
  <acceptance_criteria>
    - Both tests and the fixture reference point only to concern-local support.
    - Modified runtime bodies use the exact lowercase D-02/D-03 form and retain cleanup.
    - No consumer production pair is claimed complete.
  </acceptance_criteria>
  <done>The remaining marketplace migration slice is bounded and green.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| hook target → target mapper | Manifest-controlled target names select runtime destinations. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-04-01 | Tampering | target map and precedence | medium | mitigate | Complete literal object and ordered-array assertions detect changed keys, values, and priority. |
| T-108-04-02 | Elevation of Privilege | target selection | low | accept | Module exports static data only; owner locks the entire allowed mapping and has no I/O boundary. |
| T-108-04-03 | Tampering | marketplace support imports | medium | mitigate | Run both consumers after guarded concern-local migration and verify the fixture path is current. |
</threat_model>
<verification>
- `node --test tests/domain/components/hook-if-targets.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-if-targets.ts`
- `npm run check`
</verification>
<success_criteria>
- Complete mappings and precedence remain unchanged.
- The direct owner is structurally compliant.
- Canonical test guidelines remain unchanged, while Task 3 updates the fixture README path reference.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-04-SUMMARY.md` when done.</output>
