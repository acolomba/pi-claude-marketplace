---
phase: 108-domain-and-platform
plan: 21
type: execute
wave: 4
depends_on: [108-18]
files_modified:
  - tests/platform/git-credential.test.ts
  - tests/platform/credential-ops-contract.ts
  - tests/platform/credential-ops-fake.ts
  - tests/platform/credential-ops-fake.test.ts
  - tests/platform/credential-process-fake.ts
  - scripts/check-corresponding-tests.mjs
  - scripts/check-corresponding-tests.negative.mjs
autonomous: true
requirements: [MOD-01, PRES-03, PRES-04]
estimate: {tokens: 65000, raw_tokens: 65000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The production credential adapter and concern-local fake run the same complete `CredentialOps` cases from fresh explicit factories (D-06, D-07, D-08)."
    - "Process mechanics cover stdin/stdout/exit/error/timeout/termination/cleanup without real `git credential`, keychain, PATH fallback, or developer credentials (D-09, D-16, D-18, D-19)."
    - "The private mutable-alias fake fails exactly `returns a credential copy that cannot mutate stored state` and passes every other case (D-11 through D-14)."
    - "The correspondence gate classifies valid concern-local fake/contract evidence structurally and rejects lookalikes (D-10)."
  artifacts:
    - path: tests/platform/git-credential.test.ts
      provides: Canonical owner, production contract participant, and process mechanics
    - path: tests/platform/credential-ops-contract.ts
      provides: Callable `registerCredentialOpsContract` and ordered case manifest
    - path: tests/platform/credential-ops-fake.ts
      provides: Guarded concern-local credential fake
    - path: tests/platform/credential-ops-fake.test.ts
      provides: Fake participant and surgical negative control
    - path: tests/platform/credential-process-fake.ts
      provides: Deterministic injected `CredentialSpawn` process model
    - path: scripts/check-corresponding-tests.mjs
      provides: Structural supplemental-evidence classification
  key_links:
    - from: tests/platform/credential-ops-contract.ts
      to: tests/platform/git-credential.test.ts
      via: production participant invokes the same fresh-factory registrar
    - from: tests/platform/credential-ops-fake.test.ts
      to: tests/platform/credential-ops-contract.ts
      via: fake participant and direct callable planted-defect control
    - from: scripts/check-corresponding-tests.mjs
      to: tests/platform/credential-ops-fake.test.ts
      via: structural fake-test suffix/import/companion validation rather than allowlist
  prohibitions:
    - "No real credential helper, OS keychain, PATH lookup, token value, or live process fallback is allowed."
    - "No named supplemental exemption list may be added; generic-helper deletion waits for the post-carrier migration batches and 108-23's no-reference proof."
---

<objective>
Make the `git-credential.ts` pair the credential contract and supplemental-evidence gate carrier (D-01 through D-19 where applicable).

Purpose: Prove credential behavior and process safety with a sensitive shared contract, then establish the structural category later adapter fakes use.
Output: Normalized owner, callable contract, guarded fake, deterministic process seam, exact negative control, and structural supplemental gate ready for the later bounded import batches.

Ruling on the second research open question: 108-21 owns the supplemental-evidence gate because it introduces the first contract/fake supplement. Plans 108-12 and 108-22 depend on it and must satisfy the same structural rule.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-VALIDATION.md
@.planning/phases/108-domain-and-platform/108-18-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/platform/git-credential.test.ts` — modified canonical owner and production contract participant.
- `tests/platform/credential-ops-contract.ts` — new `registerCredentialOpsContract`, callable cases, and literal ordered case names.
- `tests/platform/credential-ops-fake.ts` — new `createCredentialOpsFake` guarded factory.
- `tests/platform/credential-ops-fake.test.ts` — new fake participant and private `createAliasingCredentialOpsFake` planted defect.
- `tests/platform/credential-process-fake.ts` — new `createCredentialProcessFake` implementing injected `CredentialSpawn` behavior.
- `scripts/check-corresponding-tests.mjs` and `.negative.mjs` — modified structural supplemental classification and positive/lookalike fixtures.

<tasks>
<task type="auto">
  <name>Task 1: Normalize the production owner through injected CredentialSpawn mechanics</name>
  <files>tests/platform/git-credential.test.ts, tests/platform/credential-process-fake.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/platform/git-credential.ts` — `CredentialOps`, `CredentialSpawn`, wire encoding, process lifecycle, and public factory.
    - `tests/platform/git-credential.test.ts` — existing owner, including the PATH-dependent default-adapter case to replace.
    - `tests/helpers/credential-mock.ts` — current deterministic support behavior.
    - `tests/platform/git-auth-callbacks.test.ts` — credential callback consumer mechanics.
    - `.claude/rules/typescript-unit-testing.md` — structure, doubles, and cleanup rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — public-boundary and negative-path guidance.
  </read_first>
  <action>Normalize every owner case per D-01 through D-04. Every created or modified runtime case must contain exact lowercase `// arrange`, `// act`, and `// assert` comments in that order with blank-line separation, or exact lowercase `// act &amp; assert` only for a single throwing/rejection expression; type-only checks follow D-03 without artificial comments. Create `credential-process-fake.ts` as an explicitly injected `CredentialSpawn` model that records argv/stdin, controls stdout/stderr/exit/error timing, exposes termination, and proves cleanup. Replace the default-adapter PATH-dependent test with production `createCredentialOps` cases using this seam. Cover `fill`, `approve`, and `reject`; exact stdin records and terminating blank line; parsed stdout; nonzero exit; spawn error; timeout; termination; cleanup; and wire-input validation rejecting newline, carriage return, and NUL in every encoded field. Poison unexpected real spawn/keychain/PATH access per D-16/D-18, use synthetic credentials only, and restore timers/env/mocks in each case per D-19.</action>
  <verify><automated>node --test tests/platform/git-credential.test.ts</automated></verify>
  <acceptance_criteria>
    - No case executes `git credential`, reads a keychain, or depends on PATH.
    - Exact argv/stdin/stdout/exit/error/timeout/termination/cleanup behavior is observable.
    - Newline, carriage-return, and NUL wire injection is rejected before spawn.
    - Every touched runtime case uses exact lowercase phase comments.
  </acceptance_criteria>
  <done>The production adapter is hermetically covered at its public and injected-process boundaries.</done>
</task>
<task type="auto">
  <name>Task 2: Register the complete CredentialOps contract and exact planted defect</name>
  <files>tests/platform/credential-ops-contract.ts, tests/platform/credential-ops-fake.ts, tests/platform/credential-ops-fake.test.ts, tests/platform/git-credential.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/platform/git-credential.ts` — complete public `CredentialOps` port semantics.
    - `tests/platform/git-credential.test.ts` — production participant factory from Task 1.
    - `tests/helpers/credential-mock.ts` — behavior to preserve in the concern-local fake.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-06 through D-14 and D-18 contract rules.
    - `.claude/rules/typescript-unit-testing.md` — shared-contract and negative-control standards.
  </read_first>
  <action>Create callable cases and `registerCredentialOpsContract`, each receiving a fresh explicitly guarded participant factory per D-06/D-11/D-18. Every contract registration, fake-participant case, and planted negative-control runtime case created or modified here must use exact lowercase `// arrange`, `// act`, and `// assert` comments in that order with blank-line separation, or exact lowercase `// act &amp; assert` only for one throwing/rejection expression; do not add runtime structure to type-only evidence per D-03. Cover the complete `fill`/`approve`/`reject` port: missing/found credentials, complete results/errors, mutation/aliasing isolation, approve overwrite, reject deletion/idempotence, validation, and source-defined ordering; record any inapplicable category beside the literal case manifest per D-08. Invoke the same registrar from the production owner and `credential-ops-fake.test.ts`, while keeping spawn mechanics and fake controls outside the contract per D-09. Define a private broken fake in the supplemental test whose `fill` returns its stored mutable object; call cases directly and assert the literal failure-name array is exactly `["returns a credential copy that cannot mutate stored state"]`, with every other case passing, per D-12 through D-14.</action>
  <verify><automated>node --test tests/platform/git-credential.test.ts tests/platform/credential-ops-fake.test.ts</automated></verify>
  <acceptance_criteria>
    - Production and fake execute the same full public-port cases from fresh state, resolving PRES-03.
    - The planted defect yields a non-empty one-element literal failure set and all other ordered cases pass, resolving PRES-04's adjacency/empty/ordering probes.
    - Expected results and the defect case name are independent of fake internals.
  </acceptance_criteria>
  <done>The credential contract proves behavioral parity and detects exactly its central aliasing invariant.</done>
</task>
<task type="auto">
  <name>Task 3: Establish structural supplemental evidence and close the credential carrier</name>
  <files>scripts/check-corresponding-tests.mjs, scripts/check-corresponding-tests.negative.mjs, tests/platform/credential-ops-contract.ts, tests/platform/credential-ops-fake.ts, tests/platform/credential-ops-fake.test.ts</files>
  <read_first>
    - `scripts/check-corresponding-tests.mjs` — current owner-only filename derivation.
    - `scripts/check-corresponding-tests.negative.mjs` — current fixture harness and exact diagnostics.
    - `tests/platform/credential-ops-contract.ts` — companion evidence shape.
    - `tests/platform/credential-ops-fake.ts` and `credential-ops-fake.test.ts` — first valid supplemental trio.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-07, D-10, and D-19 constraints.
  </read_first>
  <action>Extend correspondence classification so a `*-fake.test.ts` under `tests/domain` or `tests/platform` is supplemental only when its concern-local fake and contract companions exist and its imports structurally connect them; preserve ordinary mirrored-owner derivation and reject suffix/lookalike files missing those companions per D-10. Add deterministic negative-runner fixtures proving one valid structural supplement passes and lookalike/missing-companion cases produce exact diagnostics; do not add a named allowlist. Any runtime case source created inside these fixtures must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; allow lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and give legitimate type-only `satisfies`/`@ts-expect-error` evidence no runtime phases. Finalize the concern-local credential API but do not change or delete generic-helper consumers in this plan. Record that 108-02/03/04/05/07 own bounded post-108-22 consumer batches and 108-23 owns the final no-reference proof/deletion.</action>
  <verify><automated>npm run test:corresponding &amp;&amp; npm run test:corresponding:negative &amp;&amp; node --test tests/platform/git-credential.test.ts tests/platform/credential-ops-fake.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/platform/git-credential.ts</automated></verify>
  <acceptance_criteria>
    - Valid structural fake/contract evidence is supplemental; missing-companion/lookalike evidence fails with exact diagnostics.
    - No filename allowlist is introduced; generic-helper relocation remains explicitly open for bounded later plans.
    - The concern-local credential API is stable and no env/timer/mock leaks.
    - Correspondence, negative fixture, focused participants, and direct coverage pass.
  </acceptance_criteria>
  <done>The credential contract carrier and reusable structural supplemental gate are complete before device/Git carriers and later migration batches run.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| credential fields → helper stdin | Sensitive caller data crosses an OS-process wire format. |
| helper stdout/exit/timing → CredentialOps | Untrusted process behavior becomes credential results/errors. |
| fake/contract test → correspondence classifier | Supplemental evidence could masquerade as an owner or exemption. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-21-01 | Information Disclosure | credential values/process output | high | mitigate | Synthetic credentials only, no logging, no real helper/keychain, and no PATH fallback. |
| T-108-21-02 | Tampering | credential wire encoder | high | mitigate | Reject LF, CR, and NUL before spawn; assert exact encoded stdin. |
| T-108-21-03 | Denial of Service | process timeout/termination | high | mitigate | Deterministic timeout, kill, error, exit, and cleanup cases through injected `CredentialSpawn`. |
| T-108-21-04 | Tampering | fake/production contract | high | mitigate | Same registrar plus exact one-failure aliasing defect. |
| T-108-21-05 | Tampering | correspondence classifier | high | mitigate | Companion/import structure and negative lookalike fixtures; no named exemptions. |
| T-108-21-06 | Tampering | env/timer/mock global state | high | mitigate | Fresh guarded factories and case-local restoration required by D-18/D-19. |
</threat_model>
<verification>
- `node --test tests/platform/git-credential.test.ts tests/platform/credential-ops-fake.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/platform/git-credential.ts`
- `npm run test:corresponding`
- `npm run test:corresponding:negative`
- `npm run test:coverage:direct:all`
- `npm run check`
</verification>
<success_criteria>
- Production/fake credential parity covers the entire port and the exact planted defect is sensitive.
- Wire/process mechanics are hermetic and injection-safe.
- Structural supplemental classification is reusable by 108-12 and 108-22.
- Generic helper/import debt is assigned to bounded post-carrier plans without claiming other pairs complete.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-21-SUMMARY.md` when done.</output>
