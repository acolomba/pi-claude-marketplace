---
phase: 108-domain-and-platform
plan: 13
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/manifest-cache.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Manifest caching remains path-scoped by mtime/size, preserves success/error identity, and handles stat/load races (D-01)."
    - "All touched cases use D-02 through D-04 with complete result/error assertions."
  artifacts:
    - path: tests/domain/manifest-cache.test.ts
      provides: Canonical mirrored owner for manifest-cache.ts
  key_links:
    - from: tests/domain/manifest-cache.test.ts
      to: extensions/pi-claude-marketplace/domain/manifest-cache.ts
      via: direct cache API imports and injected filesystem operations
  prohibitions:
    - "No case may depend on the developer filesystem or share cache state with another case."
---

<objective>
Normalize the manifest-cache owner and preserve cache identity, invalidation, and race behavior (D-01 through D-05).

Purpose: Make cache hits, misses, negative caching, and file races deterministic.
Output: A compliant direct owner with fresh per-case cache/filesystem state.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/manifest-cache.test.ts` — modified owner for cache hit/miss/error semantics.

<tasks>
<task type="auto">
  <name>Task 1: Normalize cache hit, invalidation, and identity cases</name>
  <files>tests/domain/manifest-cache.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/manifest-cache.ts` — cache-key, identity, and invalidation logic.
    - `tests/domain/manifest-cache.test.ts` — current owner and fakes.
    - `.claude/rules/typescript-unit-testing.md` — structure and cleanup.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — identity/whole-value assertions.
  </read_first>
  <action>Normalize success cases per D-01, D-02, and D-04 with fresh cache and filesystem doubles per case. Assert same path/mtime/size returns the same object reference, while changed mtime or size reloads; prove two paths with equal metadata remain independent. Compare complete loaded manifests and use D-03 only for a single throwing/rejection expression. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/manifest-cache.test.ts</automated></verify>
  <acceptance_criteria>
    - Hit identity, metadata invalidation, and path separation are explicit.
    - Loader call counts and complete manifest values are asserted.
    - No case shares mutable cache state.
  </acceptance_criteria>
  <done>Positive caching and invalidation semantics are normalized.</done>
</task>
<task type="auto">
  <name>Task 2: Lock negative caching and filesystem-race paths</name>
  <files>tests/domain/manifest-cache.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/manifest-cache.ts` — stat failure, loader failure, and vanish-after-stat branches.
    - `tests/domain/manifest-cache.test.ts` — normalized positive cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Normalize/add cases proving a repeated loader failure is returned as the same cached error object, a stat failure is a miss rather than cached success, and a file that vanishes between stat and load remains uncached so the next call retries. Assert exact call sequences and complete errors; keep docs untouched per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/manifest-cache.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest-cache.ts</automated></verify>
  <acceptance_criteria>
    - Negative-cache identity is explicit.
    - Stat failure and vanish-during-load both retry on the next call as specified.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Error caching and filesystem races are deterministic and directly covered.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| filesystem metadata/content → manifest cache | Mutable external files become trusted cached values. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-13-01 | Tampering | cache invalidation | high | mitigate | Path/mtime/size, equal-metadata separation, and reload cases lock the cache key. |
| T-108-13-02 | Denial of Service | negative cache/race handling | medium | mitigate | Same-error identity, stat failure, and vanish/retry cases preserve bounded retry behavior. |
</threat_model>
<verification>
- `node --test tests/domain/manifest-cache.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest-cache.ts`
- `npm run check`
</verification>
<success_criteria>
- Cache hit, invalidation, error, and race contracts remain unchanged.
- State is fresh and deterministic per case.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-13-SUMMARY.md` when done.</output>
