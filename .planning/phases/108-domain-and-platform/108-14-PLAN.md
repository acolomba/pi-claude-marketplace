---
phase: 108-domain-and-platform
plan: 14
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/manifest-lookup.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Manifest lookup remains exact, case-sensitive, Unicode-sensitive, and first-match wins (D-01)."
    - "The owner uses D-02 through D-04 and complete result values."
  artifacts:
    - path: tests/domain/manifest-lookup.test.ts
      provides: Canonical mirrored owner for manifest-lookup.ts
  key_links:
    - from: tests/domain/manifest-lookup.test.ts
      to: extensions/pi-claude-marketplace/domain/manifest-lookup.ts
      via: direct lookup import
  prohibitions:
    - "No case-folding or Unicode normalization may be introduced by test setup."
---

<objective>
Normalize manifest lookup tests and preserve exact first-match name semantics (D-01 through D-05).

Purpose: Prevent aliases and duplicate ordering from changing silently.
Output: A compliant canonical owner with explicit equality boundaries.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/manifest-lookup.test.ts` — modified exact-name lookup owner.

<tasks>
<task type="auto">
  <name>Task 1: Normalize exact and missing lookup results</name>
  <files>tests/domain/manifest-lookup.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/manifest-lookup.ts` — exact comparison and return contract.
    - `tests/domain/manifest-lookup.test.ts` — current owner.
    - `.claude/rules/typescript-unit-testing.md` — D-02 structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole result expectations.
  </read_first>
  <action>Normalize all current cases per D-01, D-02, and D-04. Assert a complete matching entry for an exact name and `undefined` for empty input, absent input, and a one-character lookalike. Keep fixtures literal and use D-03 only for legitimate one-expression cases. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/manifest-lookup.test.ts</automated></verify>
  <acceptance_criteria>
    - Exact match returns the complete original entry.
    - Empty, absent, and adjacent lookalike names do not match.
    - All touched cases use exact lowercase phase comments.
  </acceptance_criteria>
  <done>Exact and missing lookup behavior is normalized.</done>
</task>
<task type="auto">
  <name>Task 2: Lock case, Unicode, and duplicate first-match boundaries</name>
  <files>tests/domain/manifest-lookup.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/manifest-lookup.ts` — equality and iteration order.
    - `tests/domain/manifest-lookup.test.ts` — normalized base cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 boundary.
  </read_first>
  <action>Add literal cases proving case-changed and canonically distinct Unicode strings do not alias an exact name. Add duplicate equal names with distinguishable complete entries and assert the first entry wins, resolving MOD-01 adjacency/equality and stable ordering for lookup. Leave docs unchanged per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/manifest-lookup.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest-lookup.ts</automated></verify>
  <acceptance_criteria>
    - Case and Unicode lookalikes remain distinct.
    - Duplicate exact names return the first complete entry.
    - Direct coverage accepts the owner.
  </acceptance_criteria>
  <done>Equality and duplicate ordering contracts are explicit and covered.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| requested name → manifest entry selection | Caller-controlled name selects package metadata. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-14-01 | Spoofing | exact-name lookup | high | mitigate | Case, Unicode, one-character, empty, and exact equality cases prevent unintended aliases. |
| T-108-14-02 | Tampering | duplicate resolution | medium | mitigate | Distinguishable duplicate fixtures lock first-match iteration order. |
</threat_model>
<verification>
- `node --test tests/domain/manifest-lookup.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest-lookup.ts`
- `npm run check`
</verification>
<success_criteria>
- Exact, missing, case, Unicode, and duplicate semantics remain stable.
- The canonical owner passes direct coverage.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-14-SUMMARY.md` when done.</output>
