---
phase: 108-domain-and-platform
plan: 20
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/version.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Version hashes remain stable across filesystem enumeration order and approved text normalization, while relevant content changes alter the hash (D-01)."
    - "Ignored paths/symlinks and SHA truncation preserve exact outputs under D-02 through D-04 structure."
  artifacts:
    - path: tests/domain/version.test.ts
      provides: Canonical mirrored owner for version.ts
  key_links:
    - from: tests/domain/version.test.ts
      to: extensions/pi-claude-marketplace/domain/version.ts
      via: direct computeHashVersion and shaVersion imports with case-owned trees
  prohibitions:
    - "No test tree may escape its temporary root, follow symlinks, or leave residue."
---

<objective>
Normalize version hashing tests and preserve stable ordering, normalization, ignore, and truncation contracts (D-01 through D-05).

Purpose: Keep content-derived versions reproducible across machines and directory order.
Output: A hermetic canonical owner using literal expected hashes and case-owned trees.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/version.test.ts` — modified owner for filesystem and SHA version exports.

<tasks>
<task type="auto">
  <name>Task 1: Normalize content, normalization, ignore, and SHA cases</name>
  <files>tests/domain/version.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/version.ts` — hashing traversal, normalization, ignore, and truncation logic.
    - `tests/domain/version.test.ts` — current owner and temporary-tree helpers.
    - `.claude/rules/typescript-unit-testing.md` — phase/cleanup rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — deterministic whole outputs.
  </read_first>
  <action>Normalize current cases per D-01, D-02, and D-04 using a fresh case-owned tree. Assert complete hashes for ordinary content, BOM and CRLF normalization equivalence, bare-CR distinction, ignored `.git`, `node_modules`, and `.DS_Store` contents, skipped symlinks, and exact `shaVersion` truncation. Use literal expected strings and D-03 only for one-expression tests; remove every temporary tree in case cleanup.</action>
  <verify><automated>node --test tests/domain/version.test.ts</automated></verify>
  <acceptance_criteria>
    - Text normalization equivalences/differences and ignore rules are exact.
    - SHA truncation is asserted as a complete string.
    - Temp roots and symlinks are case-owned and removed.
  </acceptance_criteria>
  <done>Content and SHA version behavior is normalized and hermetic.</done>
</task>
<task type="auto">
  <name>Task 2: Lock empty-tree and equal-element traversal ordering</name>
  <files>tests/domain/version.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/version.ts` — sorted traversal and empty-tree behavior.
    - `tests/domain/version.test.ts` — normalized hashing cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Add an exact empty-tree hash case and two trees created in opposite filesystem insertion orders with identical relative paths/content; assert identical complete hashes. Add equal-content files at distinct paths and assert path order contributes in the source-defined sorted sequence, then mutate one byte and assert a different hash. This resolves MOD-01 empty, equality/adjacency, and stable ordering probes for hashing. Leave docs unchanged per D-05.</action>
  <verify><automated>node --test tests/domain/version.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/version.ts</automated></verify>
  <acceptance_criteria>
    - Empty tree has a fixed expected version.
    - Opposite creation order yields the same hash; one-byte change yields a different hash.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Filesystem ordering and empty/equality boundaries are deterministic and covered.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| filesystem tree → content version | Mutable files and paths determine integrity identity. |
| test temp tree → host filesystem | Hash tests create paths/symlinks that require containment. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-20-01 | Tampering | traversal/hash inputs | high | mitigate | Sorted-order, equal-content path, one-byte change, ignore, and symlink cases lock inputs. |
| T-108-20-02 | Information Disclosure | temporary trees | medium | mitigate | Synthetic data, case-owned roots, and cleanup prevent residue. |
</threat_model>
<verification>
- `node --test tests/domain/version.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/version.ts`
- `npm run check`
</verification>
<success_criteria>
- Hashes remain stable across enumeration and approved normalization.
- Relevant content/path changes remain discriminating.
- D-01 through D-05 and temp cleanup are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-20-SUMMARY.md` when done.</output>
