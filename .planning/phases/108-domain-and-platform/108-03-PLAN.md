---
phase: 108-domain-and-platform
plan: 03
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/components/hook-events.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Hook event tuples and closed-set classifications retain exact membership and order (D-01)."
    - "All touched runtime cases use the D-02 structure, and complete arrays/sets are compared as whole values (D-03, D-04)."
  artifacts:
    - path: tests/domain/components/hook-events.test.ts
      provides: Canonical mirrored owner for hook-events.ts
  key_links:
    - from: tests/domain/components/hook-events.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hook-events.ts
      via: direct tuple and predicate imports
  prohibitions:
    - "No expected event collection may be derived from the production collection under test."
---

<objective>
Normalize the hook-event owner and lock exact ordered closed sets (D-01 through D-05).

Purpose: Preserve event routing semantics, including ordering and membership boundaries.
Output: A compliant canonical owner with literal independent expectations.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hook-events.test.ts` — modified owner for event tuples and dispatchability.

<tasks>
<task type="auto">
  <name>Task 1: Normalize literal hook-event collection assertions</name>
  <files>tests/domain/components/hook-events.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hook-events.ts` — authoritative tuples and derived closed sets.
    - `tests/domain/components/hook-events.test.ts` — current owner.
    - `.claude/rules/typescript-unit-testing.md` — runtime phase structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — independent expected values.
  </read_first>
  <action>Normalize all owner cases per D-01, D-02, and D-04. Compare `BUCKET_A_EVENTS`, `TOOL_EVENTS`, and the non-tool field/closed-set exports to literal arrays in exact source-defined order; do not compute expected members by filtering or spreading the production values. Use D-03 only for legitimate one-expression cases.</action>
  <verify><automated>node --test tests/domain/components/hook-events.test.ts</automated></verify>
  <acceptance_criteria>
    - Each exported event collection equals an independently written complete literal.
    - Tuple order is asserted, including elements that share a classification.
    - Every touched runtime case has exact lowercase phase comments.
  </acceptance_criteria>
  <done>All hook event collections have independent, ordered contract evidence.</done>
</task>
<task type="auto">
  <name>Task 2: Lock dispatchability equality, empty, and ordering edges</name>
  <files>tests/domain/components/hook-events.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hook-events.ts` — `isDispatchableEvent` and membership implementation.
    - `tests/domain/components/hook-events.test.ts` — normalized literal expectations.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 no-doc-churn decision.
  </read_first>
  <action>Add table rows covering every accepted event and source-grounded rejected boundaries: empty text, case-changed text, and a one-character lookalike. Assert the tool subset's exact relative order within the complete event order. This concretely resolves the MOD-01 empty, adjacency, and equal-classification ordering probes for this module. Keep expected values literal and leave docs untouched per D-05.</action>
  <verify><automated>node --test tests/domain/components/hook-events.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-events.ts</automated></verify>
  <acceptance_criteria>
    - Every supported event returns true and each boundary lookalike returns false.
    - Empty text returns false.
    - Tool-event order and all closed-set members match literal expectations.
  </acceptance_criteria>
  <done>The event owner's full public surface and ordering boundary pass direct coverage.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| event name → dispatch predicate | Untrusted event text controls dispatch eligibility. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-03-01 | Spoofing | `isDispatchableEvent` | medium | mitigate | Exact accepted literals plus case-changed, empty, and lookalike rejections lock the closed set. |
| T-108-03-02 | Tampering | event tuple order | medium | mitigate | Independently literal whole-array assertions detect omissions, additions, and reordering. |
</threat_model>
<verification>
- `node --test tests/domain/components/hook-events.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-events.ts`
- `npm run check`
</verification>
<success_criteria>
- Event membership and order remain exact.
- Empty and adjacent text boundaries are explicit.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-03-SUMMARY.md` when done.</output>
