---
phase: 108-domain-and-platform
plan: 10
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/components/mcp.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "The MCP component schema accepts object records, including empty/arbitrary values, and rejects arrays, null, and primitives (D-01)."
    - "All runtime cases are normalized per D-02 through D-04 without changing guidelines (D-05)."
  artifacts:
    - path: tests/domain/components/mcp.test.ts
      provides: Canonical mirrored owner for mcp.ts
  key_links:
    - from: tests/domain/components/mcp.test.ts
      to: extensions/pi-claude-marketplace/domain/components/mcp.ts
      via: direct schema and validator imports
  prohibitions:
    - "No accepted object may be asserted only field-by-field."
---

<objective>
Normalize the MCP component owner and preserve its exact object-record boundary (D-01 through D-05).

Purpose: Distinguish accepted arbitrary configuration records from non-record JSON values.
Output: A complete canonical schema/validator owner.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/mcp.test.ts` — modified owner for MCP schema and validator exports.

<tasks>
<task type="auto">
  <name>Task 1: Normalize accepted MCP record shapes</name>
  <files>tests/domain/components/mcp.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/mcp.ts` — exported schema/validator contract.
    - `tests/domain/components/mcp.test.ts` — current owner.
    - `.claude/rules/typescript-unit-testing.md` — runtime structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value schema expectations.
  </read_first>
  <action>Normalize accepted cases per D-01, D-02, and D-04. Cover an empty object and a record containing nested object, scalar, array, false, and null values where the record contract permits arbitrary values. Assert the complete validated/parsing result and use D-03's combined comment only for a single assertion expression.</action>
  <verify><automated>node --test tests/domain/components/mcp.test.ts</automated></verify>
  <acceptance_criteria>
    - Empty and heterogeneous records are accepted exactly as exported.
    - Accepted values are asserted as complete records.
    - Exact phase-comment structure appears in every modified runtime case.
  </acceptance_criteria>
  <done>The full accepted MCP record surface is normalized.</done>
</task>
<task type="auto">
  <name>Task 2: Lock null, array, and primitive rejection boundaries</name>
  <files>tests/domain/components/mcp.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/mcp.ts` — non-record rejection branches.
    - `tests/domain/components/mcp.test.ts` — normalized accepted cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Add a literal invalid-value table containing null, empty/non-empty arrays, string, number, and boolean at the top-level MCP boundary. Assert exact rejection behavior for each; do not confuse nested arbitrary values with invalid top-level values. This concretely resolves MOD-01 empty/null and adjacency of empty object versus empty array. Leave docs unchanged per D-05.</action>
  <verify><automated>node --test tests/domain/components/mcp.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/mcp.ts</automated></verify>
  <acceptance_criteria>
    - Empty object is accepted while empty array and null are rejected.
    - Every primitive top-level kind reaches public rejection.
    - Direct coverage accepts the owner.
  </acceptance_criteria>
  <done>The MCP record/non-record boundary is explicit and complete.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| manifest MCP value → MCP record validator | Untrusted configuration crosses a shape boundary. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-10-01 | Tampering | MCP validator | high | mitigate | Explicit object/null/array/primitive boundary cases prevent non-record shapes from passing. |
| T-108-10-02 | Denial of Service | arbitrary nested MCP values | low | accept | This module validates top-level shape only; whole-record cases preserve that documented public scope. |
</threat_model>
<verification>
- `node --test tests/domain/components/mcp.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/mcp.ts`
- `npm run check`
</verification>
<success_criteria>
- Object records and non-record inputs remain correctly distinguished.
- Empty and null behavior is explicit.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-10-SUMMARY.md` when done.</output>
