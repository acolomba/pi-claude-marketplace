---
phase: 108-domain-and-platform
plan: 02
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/clone-key.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Clone, mirror, and canonical URL keys retain their exact deterministic outputs (D-01)."
    - "The canonical owner uses exact D-02 phase comments and whole-value assertions across every touched runtime case (D-03, D-04)."
  artifacts:
    - path: tests/domain/clone-key.test.ts
      provides: Canonical mirrored owner for clone-key.ts
  key_links:
    - from: tests/domain/clone-key.test.ts
      to: extensions/pi-claude-marketplace/domain/clone-key.ts
      via: direct imports of pluginCloneKey, pluginMirrorKey, and canonicalCloneUrl
  prohibitions:
    - "No hash fixture may depend on machine paths, locale, time, or network state."
---

<objective>
Normalize the clone-key owner while preserving deterministic URL/SHA key semantics (D-01 through D-05).

Purpose: Make equality boundaries and deterministic hashes trustworthy through the mirrored owner.
Output: A compliant `clone-key.test.ts` with full public-export coverage.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/clone-key.test.ts` — modified owner for all three exported key functions.

<tasks>
<task type="auto">
  <name>Task 1: Normalize deterministic clone and mirror key cases</name>
  <files>tests/domain/clone-key.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/clone-key.ts` — exact hash composition and truncation contract.
    - `tests/domain/clone-key.test.ts` — current examples and expected digests.
    - `.claude/rules/typescript-unit-testing.md` — D-02-compatible structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value assertion rules.
  </read_first>
  <action>Normalize every existing `pluginCloneKey` and `pluginMirrorKey` case per D-01, D-02, and D-04. Preserve SHA-256 inputs and 12-character slices exactly, use explicit fixed URL/SHA fixtures, and assert complete string outputs. Use the D-03 combined phase only for a single assertion expression.</action>
  <verify><automated>node --test tests/domain/clone-key.test.ts</automated></verify>
  <acceptance_criteria>
    - Identical URL/SHA inputs return identical clone keys and fixed expected digests.
    - Mirror keys retain the source-defined 12-character digest format.
    - Every touched runtime case uses the approved exact lowercase comment structure.
  </acceptance_criteria>
  <done>Clone and mirror key behavior is normalized without altering deterministic output.</done>
</task>
<task type="auto">
  <name>Task 2: Lock adjacent inputs and canonical URL selection</name>
  <files>tests/domain/clone-key.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/clone-key.ts` — GitHub canonicalization branch and digest inputs.
    - `tests/domain/clone-key.test.ts` — normalized owner.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-03 and D-05 boundaries.
  </read_first>
  <action>Add source-grounded adjacency cases proving that an identical URL/SHA pair is stable while a one-character URL change or SHA change produces a different key. Assert `canonicalCloneUrl` selects the GitHub canonical URL only for the supported GitHub source shape and otherwise retains the ordinary URL. Keep empty strings only where the source type accepts them; do not invent null behavior outside the public type. Leave documentation unchanged per D-05.</action>
  <verify><automated>node --test tests/domain/clone-key.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/clone-key.ts</automated></verify>
  <acceptance_criteria>
    - Equal inputs are stable and each adjacent one-character change is distinguishable.
    - GitHub and non-GitHub canonical URL branches are asserted as full strings.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>The owner resolves the MOD-01 equality probe and covers every clone-key export directly.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| source URL/SHA → filesystem key | Caller-controlled identity text becomes a cache key. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-02-01 | Tampering | clone/mirror key derivation | medium | mitigate | Fixed whole-value digests plus adjacent-input cases detect key collisions caused by input omission. |
| T-108-02-02 | Information Disclosure | key fixtures | low | accept | Fixtures contain only synthetic public URLs and SHAs, with no credential material. |
</threat_model>

<verification>
- `node --test tests/domain/clone-key.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/clone-key.ts`
- `npm run check`
</verification>
<success_criteria>
- All three exports retain exact deterministic behavior.
- Equality and adjacent-input boundaries are explicit.
- D-01 through D-05 are satisfied without production or documentation churn.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-02-SUMMARY.md` when done.</output>
