---
phase: 108-domain-and-platform
plan: 06
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hooks.ts
  - tests/domain/components/hooks.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/bridges/commands/discover.test.ts
  - tests/bridges/commands/stage.test.ts
  - tests/bridges/integration-foreign-content.test.ts
  - tests/bridges/integration-materialization-gate.test.ts
autonomous: true
requirements: [MOD-01, RES-01]
estimate: {tokens: 60000, raw_tokens: 60000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The hooks owner covers wrapper and bare inputs, all public projections, summary arms, matcher defaults, and validation failures (D-01)."
    - "Only branches proven unreachable from exported inputs are removed; public results remain unchanged."
    - "All touched runtime cases follow D-02 through D-04, and no coverage ignore or rule edit is introduced (D-05)."
    - "Seven bridge fixture constructions carry an exact literal `installable: true` before the resolver union makes that field required, and the batch typechecks both before and after 108-18 (RES-01)."
  artifacts:
    - path: tests/domain/components/hooks.test.ts
      provides: Complete canonical owner for hooks.ts
    - path: extensions/pi-claude-marketplace/domain/components/hooks.ts
      provides: Exported hooks behavior without unreachable defensive branches
  key_links:
    - from: tests/domain/components/hooks.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hooks.ts
      via: direct imports of parsing and projection exports
  prohibitions:
    - "No coverage ignore, fabricated invalid internal state, or production test-only export may be added."
    - "No resolver field may be made optional, cast away, or added to a bridge production source by the fixture-preflight batch."
---

<objective>
Repair direct coverage for the hooks module through public behavior and removal of proven unreachable branches, then land the first typecheck-safe resolver fixture-preflight batch (D-01 through D-05, RES-01).

Purpose: Turn the current coverage failure into trustworthy public-contract evidence.
Output: Complete owner cases, a smaller behavior-equivalent production implementation, and seven forward-compatible bridge fixture literals required before 108-18.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hooks.test.ts` — modified canonical owner covering all hooks exports.
- `extensions/pi-claude-marketplace/domain/components/hooks.ts` — modified only to delete the three research-identified unreachable fallbacks; no public symbol added.
- `tests/bridges/agents/stage.test.ts`, `tests/bridges/commands/discover.test.ts`, `tests/bridges/commands/stage.test.ts`, `tests/bridges/integration-foreign-content.test.ts`, and `tests/bridges/integration-materialization-gate.test.ts` — seven supporting resolver fixture constructions staged for the required boolean discriminant; none owns or completes a bridge production pair here.

<tasks>
<task type="auto">
  <name>Task 1: Expand the owner across every reachable parse and projection path</name>
  <files>tests/domain/components/hooks.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks.ts` — public parse/projection exports and current uncovered arms.
    - `extensions/pi-claude-marketplace/domain/components/hooks/schema.ts` — validation invariant source.
    - `tests/domain/components/hooks.test.ts` — legacy owner to normalize.
    - `tests/domain/components/hooks/schema.test.ts` — closest schema-validation analog.
    - `.claude/rules/typescript-unit-testing.md` — exact phase structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value cases.
  </read_first>
  <action>Normalize all owner cases per D-01, D-02, and D-04, then add public-behavior cases for complete wrapper and bare configurations; primitive, null, and array wrapper rejection; accepted and rejected handler groups; compiled `if` maps; both summary projection arms; default matcher behavior; and persisted-summary projection. Import and call every public projection directly. Compare complete parsed/projected values, and use D-03's combined comment only for a single throwing/rejection expression. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hooks.test.ts</automated></verify>
  <acceptance_criteria>
    - Bare and wrapped empty configurations have explicit complete expected results.
    - Primitive, null, array, invalid-group, and valid-group paths are exercised through public exports.
    - Every projection and both summary arms have whole-value assertions.
  </acceptance_criteria>
  <done>All reachable hooks behavior is driven through the canonical owner.</done>
</task>
<task type="auto">
  <name>Task 2: Remove unreachable fallbacks and close direct coverage</name>
  <files>extensions/pi-claude-marketplace/domain/components/hooks.ts, tests/domain/components/hooks.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks.ts` — fallbacks previously at research lines 147-148, 315-317, and 342-344.
    - `extensions/pi-claude-marketplace/domain/components/hooks/schema.ts` — proof that validated handler/group shapes cannot reach those fallbacks.
    - `tests/domain/components/hooks.test.ts` — expanded owner that protects public behavior.
    - `.planning/phases/108-domain-and-platform/108-RESEARCH.md` — measured coverage and exact dead-branch findings.
  </read_first>
  <action>Demonstrate from the validator and dense iteration invariants that the fallback after validated wrapper selection, the missing dense-array group branch, and the missing validated-handler branch cannot be reached by any exported input. Delete only those defensive branches, preserving the exported signatures and success/error values. Do not add ignore directives, synthetic internal-state hooks, or documentation changes per D-05. Re-run the public owner after each source deletion. If any runtime case body is modified while protecting the cleanup, it must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; lowercase `// act &amp; assert` is allowed only for one throwing/rejection expression, while legitimate type-only `satisfies`/`@ts-expect-error` evidence receives no artificial phases (D-02 through D-04).</action>
  <verify><automated>node --test tests/domain/components/hooks.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks.ts</automated></verify>
  <acceptance_criteria>
    - The three research-identified unreachable fallbacks are absent and no other public branch is removed.
    - The focused owner still passes after production cleanup.
    - Direct source coverage reaches the repository's required 100% threshold without ignores.
  </acceptance_criteria>
  <done>The pair passes direct coverage solely through reachable public behavior and justified dead-code removal.</done>
</task>
<task type="auto">
  <name>Task 3: Stage the agents, commands, and integration resolver fixtures</name>
  <files>tests/bridges/agents/stage.test.ts, tests/bridges/commands/discover.test.ts, tests/bridges/commands/stage.test.ts, tests/bridges/integration-foreign-content.test.ts, tests/bridges/integration-materialization-gate.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/resolver.ts` — the pre-108-18 union whose materializable arms do not yet require the boolean.
    - Each listed supporting test — seven existing `ResolvedPluginInstallable` construction sites and their surrounding runtime cases.
    - `.planning/REQUIREMENTS.md` — RES-01's required literal boolean and pair-atomic delivery policy.
    - `.planning/phases/108-domain-and-platform/108-PATTERNS.md` — the pre-resolver fixture-field spread pattern.
    - `.claude/rules/typescript-unit-testing.md` — exact lowercase runtime-case structure.
  </read_first>
  <action>Per RES-01, add a module-scope exact-literal field bag equivalent to `{ installable: true } as const` in each supporting test that needs one, and spread it into all seven existing installable fixture constructions: one in agents/stage, three in commands/discover, one in commands/stage, one in integration-foreign-content, and one in integration-materialization-gate. The spread is required so this commit remains structurally type-safe against the pre-change union while also satisfying the required field after 108-18; do not use `as ResolvedPlugin...`, `any`, an optional property, a compatibility re-export, or a resolver production edit. Preserve every scenario and all other fixture fields. Every runtime case body modified in these supporting tests must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; lowercase `// act &amp; assert` is allowed only when act and assertion are one throwing/rejection expression. Legitimate type-only `satisfies` and `@ts-expect-error` evidence remains at module scope without artificial runtime phases, per D-02 through D-04.</action>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/bridges/agents/stage.test.ts tests/bridges/commands/discover.test.ts tests/bridges/commands/stage.test.ts tests/bridges/integration-foreign-content.test.ts tests/bridges/integration-materialization-gate.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks.ts</automated></verify>
  <acceptance_criteria>
    - All seven named materializable fixtures expose the exact literal true field without a cast, optional shim, or production resolver edit.
    - The complete repository typecheck passes before 108-18 makes the field required, and all five supporting suites preserve their scenarios.
    - Every runtime case body changed by this migration follows the exact lowercase D-02/D-03 structure; type-only evidence receives no fake phases.
    - This plan still owns exactly the hooks.ts/hooks.test.ts pair and claims no bridge pair complete.
  </acceptance_criteria>
  <done>The first seven construction sites are forward-compatible, green, and authorized before the strict resolver carrier executes.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| manifest hooks value → parser/validator | Untrusted JSON-like input becomes executable hook configuration. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-06-01 | Tampering | hooks parser | high | mitigate | Explicit primitive/null/array/invalid-group rejection and whole parsed values lock validation containment. |
| T-108-06-02 | Elevation of Privilege | compiled hook projections | high | mitigate | Cases cover accepted/rejected groups, compiled condition maps, defaults, and both summary arms. |
| T-108-06-03 | Repudiation | dead-branch removal | medium | mitigate | Removal is limited to the three research-identified branches and guarded by focused tests plus direct coverage. |
| T-108-06-04 | Tampering | pre-resolver bridge fixtures | high | mitigate | Exact true-literal field bags, pre-change typecheck, and the five focused supporting suites prevent an optional/cast-based RES-01 bypass. |
</threat_model>
<verification>
- `node --test tests/domain/components/hooks.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks.ts`
- `npm run typecheck`
- `node --test tests/bridges/agents/stage.test.ts tests/bridges/commands/discover.test.ts tests/bridges/commands/stage.test.ts tests/bridges/integration-foreign-content.test.ts tests/bridges/integration-materialization-gate.test.ts`
- `npm run check`
</verification>
<success_criteria>
- Direct coverage no longer fails.
- All exported parse/projection behavior is explicitly asserted.
- Seven bridge fixture constructions are typecheck-safe before and after the strict RES-01 union change.
- No ignore, test-only production hook, or contract drift is introduced.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-06-SUMMARY.md` when done.</output>
