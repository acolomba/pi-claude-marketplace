---
phase: 108-domain-and-platform
plan: 12
type: execute
wave: 5
depends_on: [108-21]
files_modified:
  - tests/domain/github-auth.test.ts
  - tests/domain/device-flow-contract.ts
  - tests/domain/device-flow-fake.ts
  - tests/domain/device-flow-fake.test.ts
autonomous: true
requirements: [MOD-01, PRES-03, PRES-04]
estimate: {tokens: 62000, raw_tokens: 62000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "The production device-flow adapter and concern-local fake run the same fresh-factory public-port cases (D-06, D-07, D-08)."
    - "HTTP request mechanics, polling statuses, injected waits, and fresh responses are directly covered without live network or real time (D-09, D-17)."
    - "A private broken fake violates only response consumption, fails exactly `consumes polling responses in order`, and passes every other contract case (D-11, D-12, D-13, D-14)."
    - "Factories require explicit local/fake boundaries and every fetch/wait/mock is case-owned and restored (D-18, D-19)."
  artifacts:
    - path: tests/domain/github-auth.test.ts
      provides: Canonical mirrored owner and production device-flow contract participant
    - path: tests/domain/device-flow-contract.ts
      provides: Callable `registerDeviceFlowContract` and ordered case-name manifest
    - path: tests/domain/device-flow-fake.ts
      provides: Guarded concern-local fake/factory
    - path: tests/domain/device-flow-fake.test.ts
      provides: Fake participant and surgical negative control
  key_links:
    - from: tests/domain/device-flow-contract.ts
      to: tests/domain/github-auth.test.ts
      via: production participant invokes the shared registrar with a fresh public-port factory
    - from: tests/domain/device-flow-fake.test.ts
      to: tests/domain/device-flow-contract.ts
      via: fake participant plus direct callable planted-defect control
  prohibitions:
    - "No live HTTP request, real timer delay, global harness, or un-restored `globalThis.fetch` is allowed."
    - "No consumer import migration or generic-helper deletion occurs before all three concern-local replacement APIs exist; bounded batches and deletion are assigned to 108-02/03/04/05/07 and 108-23."
---

<objective>
Make the `github-auth.ts` pair the device-flow contract carrier, preserving HTTP mechanics and proving production/fake behavioral parity (D-01 through D-19 where applicable).

Purpose: Trust OAuth device flow without live network, real waits, or self-confirming fakes.
Output: Normalized owner, shared public-port contract, guarded fake, and planted-defect proof, ready for bounded consumer migration after the Git carrier.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-VALIDATION.md
@.planning/phases/108-domain-and-platform/108-21-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/github-auth.test.ts` — modified canonical owner and production contract participant.
- `tests/domain/device-flow-contract.ts` — new `registerDeviceFlowContract`, callable case functions, and literal ordered case names.
- `tests/domain/device-flow-fake.ts` — new `createDeviceFlowFake` guarded factory.
- `tests/domain/device-flow-fake.test.ts` — new fake participant and private `createNonConsumingDeviceFlowFake` negative control.

<tasks>
<task type="auto">
  <name>Task 1: Normalize the production owner and lock injected HTTP mechanics</name>
  <files>tests/domain/github-auth.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/github-auth.ts` — public flow, injected wait, private request behavior, and status handling.
    - `tests/domain/github-auth.test.ts` — canonical owner to normalize.
    - `tests/helpers/device-flow-mock.ts` — existing response/queue behavior to preserve during relocation.
    - `tests/shared/device-flow-prompt.test.ts` — closest public-flow consumer analog.
    - `.claude/rules/typescript-unit-testing.md` — exact structure and cleanup rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value and negative-path guidance.
  </read_first>
  <action>Normalize every modified production-owner case per D-01 through D-04. Every created or modified runtime case must contain exact lowercase `// arrange`, `// act`, and `// assert` comments in that order with blank-line separation, or exact lowercase `// act &amp; assert` only when the case has one throwing/rejection expression; type-only checks use the D-03 exception. Replace `globalThis.fetch` through the test context for each case, return a fresh `Response` for each request, inject a recording wait, and assert the exact authorization/token request method, URL, headers, and encoded body through public `initiateDeviceFlow`. Cover success, `authorization_pending`, `slow_down`, terminal OAuth error, malformed response, HTTP failure, and cancellation/termination where exported. Restore fetch and all mocks in case-local cleanup per D-17 and D-19; do not export private transport functions.</action>
  <verify><automated>node --test tests/domain/github-auth.test.ts</automated></verify>
  <acceptance_criteria>
    - No test performs live network or waits on real time.
    - Exact request values and complete public results/errors are asserted.
    - Each fetch replacement and wait recorder is fresh and restored by its case.
  </acceptance_criteria>
  <done>The production adapter's public behavior and transport mechanics are hermetically covered.</done>
</task>
<task type="auto">
  <name>Task 2: Register one shared device-flow contract and prove its sensitivity</name>
  <files>tests/domain/device-flow-contract.ts, tests/domain/device-flow-fake.ts, tests/domain/device-flow-fake.test.ts, tests/domain/github-auth.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/github-auth.ts` — public device-flow port and result/error semantics.
    - `tests/domain/github-auth.test.ts` — production factory seam established in Task 1.
    - `tests/platform/credential-ops-contract.ts` — concern-local registrar pattern from 108-21.
    - `tests/platform/credential-ops-fake.test.ts` — callable negative-control pattern from 108-21.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-06 through D-14 and D-18 decisions.
  </read_first>
  <action>Create `registerDeviceFlowContract` as callable case functions registered with `node:test`; each case obtains a fresh participant from an explicitly guarded factory. Every contract registration, fake-participant case, and planted negative-control runtime case created or modified here must use exact lowercase `// arrange`, `// act`, and `// assert` comments in that order with blank-line separation, or exact lowercase `// act &amp; assert` only for one throwing/rejection expression; do not invent comments for type-only checks per D-03. Cover every public request-code/poll-token operation, complete success and error results, polling response order/consumption, input validation, aliasing/mutation safety, and state reset; record ordering or mutation categories as inapplicable only with a source-based comment next to the contract case manifest per D-08. Invoke the same registrar from the production owner and fake supplement per D-06/D-09. Inside `device-flow-fake.test.ts`, define a private broken fake whose `pollToken` reads but does not consume the queue head; call the case functions directly and assert the literal failure-name array equals exactly `["consumes polling responses in order"]`, with all other cases passing, per D-11 through D-14. Guard factories against missing explicit local/fake boundaries and poison live-network access per D-18.</action>
  <verify><automated>node --test tests/domain/github-auth.test.ts tests/domain/device-flow-fake.test.ts</automated></verify>
  <acceptance_criteria>
    - Production and fake invoke the same registrar with fresh participants.
    - The broken fake produces a non-empty literal failure set containing exactly one named case, while every other ordered case passes; this resolves PRES-04 adjacency, empty, and ordering probes.
    - The contract enumerates the complete public port, resolving the PRES-03 unclassified probe.
  </acceptance_criteria>
  <done>The shared contract detects the central response-consumption defect without coupled expectations.</done>
</task>
<task type="auto">
  <name>Task 3: Close the device-flow owner and supplemental carrier gates</name>
  <files>tests/domain/github-auth.test.ts, tests/domain/device-flow-contract.ts, tests/domain/device-flow-fake.ts, tests/domain/device-flow-fake.test.ts</files>
  <read_first>
    - `tests/domain/device-flow-contract.ts` — final callable case manifest.
    - `tests/domain/device-flow-fake.ts` — stable concern-local replacement API consumed by later migration batches.
    - `tests/domain/device-flow-fake.test.ts` — fake participant and exact planted defect.
    - `tests/domain/github-auth.test.ts` — canonical owner and production participant.
    - `scripts/check-corresponding-tests.mjs` — supplemental classification already established by 108-21.
  </read_first>
  <action>Finalize the concern-local device-flow replacement API without changing generic-helper consumers in this plan. Ensure every created or modified runtime case across the owner, contract, fake participant, and negative control uses exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; allow lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and give legitimate type-only `satisfies`/`@ts-expect-error` evidence no runtime phases. Run the owner and fake participant together, direct coverage for `github-auth.ts`, and both correspondence gates. Record in the summary that P108-02/03/04/05/07 migrate consumers only after 108-22 stabilizes all replacement APIs and that P108-23 owns the final no-reference proof and helper deletion; do not claim D-07 relocation complete here.</action>
  <verify><automated>node --test tests/domain/github-auth.test.ts tests/domain/device-flow-fake.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/github-auth.ts &amp;&amp; npm run test:corresponding &amp;&amp; npm run test:corresponding:negative</automated></verify>
  <acceptance_criteria>
    - The concern-local device-flow API is stable for the bounded post-carrier migration batches.
    - Every owner/contract/fake runtime case uses the exact lowercase structure and no global mock survives a case.
    - Focused owner, fake participant, direct coverage, and correspondence gates pass without a relocation-completion claim.
  </acceptance_criteria>
  <done>The device-flow contract carrier is complete and hermetic; generic-helper relocation remains explicitly assigned to later bounded plans.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| device-flow request → OAuth HTTP endpoint | Authorization data crosses a remote boundary in production. |
| queued response → polling state | Mutable fake/adapter state determines token delivery order. |
| test replacement → global fetch/wait state | Test doubles can contaminate later cases. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-12-01 | Information Disclosure | device code/token responses | high | mitigate | Synthetic values only, whole results without logging, and no live HTTP. |
| T-108-12-02 | Tampering | HTTP request encoding | high | mitigate | Exact method/URL/header/body assertions through the public flow. |
| T-108-12-03 | Denial of Service | polling wait/status loop | high | mitigate | Injected wait plus pending/slow-down/terminal/cancellation cases; no real timer. |
| T-108-12-04 | Tampering | global fetch/mock state | high | mitigate | Test-context replacement and case-local restoration required for every case (D-19). |
| T-108-12-05 | Tampering | fake/production contract | high | mitigate | Same registrar for both participants and exact one-failure planted defect (D-06, D-13). |
</threat_model>
<verification>
- `node --test tests/domain/github-auth.test.ts tests/domain/device-flow-fake.test.ts`
- `npm run test:corresponding`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/github-auth.ts`
- `npm run test:coverage:direct:all`
- `npm run check`
</verification>
<success_criteria>
- Production/fake parity covers the full public device-flow port.
- The exact planted defect is detected by one and only one named case.
- No live network/timer/global leak exists in the carrier, and generic-helper relocation remains open until 108-23.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-12-SUMMARY.md` when done.</output>
