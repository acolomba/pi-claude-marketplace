---
phase: 108-domain-and-platform
plan: 23
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/platform/pi-api.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Pi API directory/frontmatter/load-state/soft-dependency probes retain their exact runtime results and failure handling (D-01)."
    - "Runtime cases follow D-02/D-04; interface-only contracts use D-03's compile-time pattern."
  artifacts:
    - path: tests/platform/pi-api.test.ts
      provides: Canonical mirrored runtime/type owner for pi-api.ts
  key_links:
    - from: tests/platform/pi-api.test.ts
      to: extensions/pi-claude-marketplace/platform/pi-api.ts
      via: direct public probe imports and module-scope interface assignments
  prohibitions:
    - "No case may inspect or mutate the developer's real Pi agent directory or process environment without restoration."
    - "No interface-only assertion may be disguised as a runtime test (D-03)."
---

<objective>
Normalize the Pi API owner while preserving runtime probes and compile-time interface contracts (D-01 through D-05).

Purpose: Keep the platform boundary trustworthy without depending on a developer installation.
Output: A compliant, hermetic owner for every `pi-api.ts` public export.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/platform/pi-api.test.ts` — modified canonical owner for directory, frontmatter, loaded-component, soft-dependency, and interface exports.

<tasks>
<task type="auto">
  <name>Task 1: Normalize directory, frontmatter, and loaded-component probes</name>
  <files>tests/platform/pi-api.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/platform/pi-api.ts` — all runtime probes and exported interface types.
    - `tests/platform/pi-api.test.ts` — current owner and process/filesystem doubles.
    - `.claude/rules/typescript-unit-testing.md` — exact runtime/type-only rules and cleanup.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — public behavior/whole-value expectations.
  </read_first>
  <action>Normalize every `getAgentDir`, `parseFrontmatter`, `hasLoadedPiSubagents`, and `hasLoadedPiMcpAdapter` runtime case per D-01, D-02, and D-04. Use explicit injected or case-owned paths/state rather than the developer Pi directory; assert complete parsed frontmatter and exact true/false/error-fallback results for empty, present, absent, and malformed inputs. Use D-03 only for a runtime case with exactly one assertion expression, and restore any env/module/process replacement in case cleanup.</action>
  <verify><automated>node --test tests/platform/pi-api.test.ts</automated></verify>
  <acceptance_criteria>
    - Directory and frontmatter outputs are complete values.
    - Both loaded-component probes cover present, absent, and failure paths.
    - No case reads developer state or leaks env/module mocks.
    - All modified runtime cases use exact lowercase phase comments.
  </acceptance_criteria>
  <done>Pi directory, parsing, and load-state behavior is normalized and hermetic.</done>
</task>
<task type="auto">
  <name>Task 2: Lock soft-dependency outcomes and interface-only contracts</name>
  <files>tests/platform/pi-api.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/platform/pi-api.ts` — `softDepStatus` outcomes and exported interfaces.
    - `tests/platform/pi-api.test.ts` — normalized runtime owner and current type evidence.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-03/D-05 scope.
  </read_first>
  <action>Normalize/add `softDepStatus` cases for every exported status outcome, including empty/absent and thrown probe behavior, asserting the complete status value. Preserve interface-only contracts at module scope with positive `satisfies` assignments and targeted `@ts-expect-error` assignments; do not wrap them in runtime tests or add AAA comments per D-03. Keep rules/docs unchanged per D-05 and run typecheck plus direct coverage.</action>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/platform/pi-api.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/platform/pi-api.ts</automated></verify>
  <acceptance_criteria>
    - Every soft-dependency status and failure fallback is explicit.
    - Positive interface assignments compile and negative assignments consume expected errors.
    - No interface-only case is represented as runtime behavior.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Pi API runtime and compile-time contracts are complete in their proper evidence forms.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| process/filesystem/module state → Pi capability probes | Host-controlled state determines reported capabilities. |
| frontmatter text → parsed metadata | Untrusted text crosses a parser boundary. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-23-01 | Information Disclosure | agent-directory probe | high | mitigate | Explicit test paths only; no developer directory reads or output. |
| T-108-23-02 | Tampering | frontmatter parser | medium | mitigate | Complete valid/empty/malformed input and output/error cases. |
| T-108-23-03 | Denial of Service | loaded/soft-dependency probes | medium | mitigate | Thrown/absent cases assert stable fallback and all replacements are restored. |
</threat_model>
<verification>
- `node --test tests/platform/pi-api.test.ts`
- `npm run typecheck`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/platform/pi-api.ts`
- `npm run check`
</verification>
<success_criteria>
- Every Pi API runtime export and interface contract remains covered.
- No developer state, global mock, or type-only fake test enters the suite.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-23-SUMMARY.md` when done.</output>
