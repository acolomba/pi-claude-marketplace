---
phase: 108-domain-and-platform
plan: 05
type: execute
wave: 6
depends_on: [108-22]
files_modified:
  - tests/domain/components/hook-tool-names.test.ts
  - tests/orchestrators/plugin/bootstrap.test.ts
  - tests/orchestrators/plugin/clone-cache-seed.test.ts
  - tests/orchestrators/plugin/clone-cache.test.ts
  - tests/orchestrators/plugin/fetch.test.ts
  - tests/orchestrators/plugin/info-manifest-absent.test.ts
  - tests/orchestrators/plugin/info.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Hook tool names preserve the exact forward mapping, inverse mapping, and unknown-name fallback (D-01)."
    - "Every modified runtime case follows D-02, D-03, and D-04 without documentation churn (D-05)."
    - "The assigned plugin consumers use concern-local guarded adapter support with case-owned cleanup (D-07, D-18, D-19)."
  artifacts:
    - path: tests/domain/components/hook-tool-names.test.ts
      provides: Canonical mirrored owner for hook-tool-names.ts
  key_links:
    - from: tests/domain/components/hook-tool-names.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
      via: direct mapping-function imports
  prohibitions:
    - "No expected inverse map may be computed from the production forward map."
---

<objective>
Normalize the hook tool-name owner and preserve bidirectional name mapping plus fallback behavior (D-01 through D-05).

Purpose: Make tool-name translation changes immediately visible at the public boundary.
Output: A compliant direct owner with independent mappings.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-22-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hook-tool-names.test.ts` — modified owner for forward/inverse name translation.
- The six listed plugin tests — bounded concern-local adapter import/call migration only; their production pairs remain open.

<tasks>
<task type="auto">
  <name>Task 1: Normalize complete forward and inverse tool-name mappings</name>
  <files>tests/domain/components/hook-tool-names.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts` — forward and inverse public mapping behavior.
    - `tests/domain/components/hook-tool-names.test.ts` — existing canonical cases.
    - `.claude/rules/typescript-unit-testing.md` — exact runtime structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — independent expectations.
  </read_first>
  <action>Normalize every existing mapping case per D-01, D-02, and D-04. Use literal tables that list each supported input/output pair in both directions; do not construct one expected direction from the other or from production exports. Assert each complete returned string, and reserve D-03's combined phase for single-expression cases only. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hook-tool-names.test.ts</automated></verify>
  <acceptance_criteria>
    - Every supported mapping has literal forward and inverse evidence.
    - Expected inverse values are independent of the production forward mapping.
    - All touched runtime cases use exact lowercase phase comments.
  </acceptance_criteria>
  <done>The bidirectional mapping is independently preserved.</done>
</task>
<task type="auto">
  <name>Task 2: Lock empty, case-changed, and unknown fallback behavior</name>
  <files>tests/domain/components/hook-tool-names.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts` — passthrough fallback branches.
    - `tests/domain/components/hook-tool-names.test.ts` — normalized mapping table.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 no-doc-edit constraint.
  </read_first>
  <action>Add explicit fallback cases for an empty string, a case-changed known name, and an unrelated name in both mapping directions. Assert that each is returned unchanged, which grounds the module's empty and adjacency behavior in its source contract. Keep literal inputs and outputs and leave rules/guidelines unchanged per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hook-tool-names.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts</automated></verify>
  <acceptance_criteria>
    - Empty and unknown names pass through unchanged.
    - A case-changed known name does not alias the supported literal.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Supported and fallback name behavior is complete and directly covered.</done>
</task>
<task type="auto">
  <name>Task 3: Migrate bootstrap, clone, fetch, and info consumers</name>
  <files>tests/orchestrators/plugin/bootstrap.test.ts, tests/orchestrators/plugin/clone-cache-seed.test.ts, tests/orchestrators/plugin/clone-cache.test.ts, tests/orchestrators/plugin/fetch.test.ts, tests/orchestrators/plugin/info-manifest-absent.test.ts, tests/orchestrators/plugin/info.test.ts</files>
  <read_first>
    - `tests/platform/credential-ops-fake.ts`, `tests/domain/device-flow-fake.ts`, and `tests/platform/git-ops-fake.ts` — stabilized guarded replacements.
    - Each listed plugin consumer — generic imports, fake controls, and cleanup ownership.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-02 through D-04, D-07, D-18, and D-19.
  </read_first>
  <action>Update this non-overlapping six-file plugin batch to concern-local adapter imports and explicit guarded fake/local boundaries, preserving every scenario and fake control. Do not claim the consumer production pairs complete. Import-only edits do not rewrite cases; if a call-site adjustment changes a runtime case body, normalize the whole case with exact lowercase `// arrange`, `// act`, and `// assert` comments in order with blank-line separation, or exact lowercase `// act &amp; assert` only for one assertion expression. Preserve D-03 type-only exceptions and case-owned cleanup; do not delete generic helpers here.</action>
  <verify><automated>node --test tests/orchestrators/plugin/bootstrap.test.ts tests/orchestrators/plugin/clone-cache-seed.test.ts tests/orchestrators/plugin/clone-cache.test.ts tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/info-manifest-absent.test.ts tests/orchestrators/plugin/info.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts</automated></verify>
  <acceptance_criteria>
    - All six plugin consumers use concern-local support and preserve current behavior.
    - Every changed case body follows the exact lowercase D-02/D-03 form and owns cleanup.
    - No consumer production source or generic helper is edited here.
  </acceptance_criteria>
  <done>The bootstrap/clone/fetch/info migration slice is bounded and green.</done>
</task>
</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| external tool name → internal tool name | Caller-controlled text crosses a name-translation boundary. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-05-01 | Spoofing | tool-name mappings | medium | mitigate | Complete literal forward/inverse cases plus case-changed fallback prevent unintended aliases. |
| T-108-05-02 | Tampering | inverse mapping evidence | low | mitigate | Expected inverse tables are literal and independent, so coupled errors cannot self-confirm. |
| T-108-05-03 | Tampering | plugin support imports | medium | mitigate | Run the exact six consumers after guarded concern-local migration and preserve cleanup. |
</threat_model>
<verification>
- `node --test tests/domain/components/hook-tool-names.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts`
- `npm run check`
</verification>
<success_criteria>
- Forward, inverse, and passthrough behavior remain unchanged.
- Edge boundaries are explicit and whole-value asserted.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-05-SUMMARY.md` when done.</output>
