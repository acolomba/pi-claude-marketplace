---
phase: 108-domain-and-platform
plan: 11
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/components/plugin.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Plugin entry and manifest validation preserve required name/source fields and all supported component/MCP shapes (D-01)."
    - "The owner applies D-02 through D-04 to all runtime cases and preserves type-only evidence correctly."
  artifacts:
    - path: tests/domain/components/plugin.test.ts
      provides: Canonical mirrored owner for plugin.ts
  key_links:
    - from: tests/domain/components/plugin.test.ts
      to: extensions/pi-claude-marketplace/domain/components/plugin.ts
      via: direct schema/validator imports
  prohibitions:
    - "No type-only assertion may be wrapped as a runtime case (D-03)."
---

<objective>
Normalize the plugin component owner and preserve entry/manifest schema boundaries (D-01 through D-05).

Purpose: Keep plugin metadata validation complete across runtime and type contracts.
Output: A compliant canonical owner for all `plugin.ts` exports.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/plugin.test.ts` — modified owner for plugin entry, component, MCP, and manifest validators/types.

<tasks>
<task type="auto">
  <name>Task 1: Normalize valid plugin entry and manifest values</name>
  <files>tests/domain/components/plugin.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/plugin.ts` — public schemas, validators, and types.
    - `tests/domain/components/plugin.test.ts` — current owner.
    - `tests/domain/components/mcp.test.ts` — closest MCP shape analog.
    - `.claude/rules/typescript-unit-testing.md` — runtime/type-only rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value guidance.
  </read_first>
  <action>Normalize all accepted runtime cases per D-01, D-02, and D-04. Assert complete plugin entries requiring `name` and `source`, opaque component payloads, MCP string/map forms, and manifest values whose name is optional and source is not an entry requirement. Keep each public validator/schema directly imported and use D-03 only for genuine single-expression cases.</action>
  <verify><automated>node --test tests/domain/components/plugin.test.ts</automated></verify>
  <acceptance_criteria>
    - Each supported entry, component, MCP, and manifest shape has a complete expected result.
    - Entry requirements are not incorrectly applied to manifest-only values.
    - Runtime cases use exact lowercase phase comments.
  </acceptance_criteria>
  <done>The accepted plugin schema surface is independently locked.</done>
</task>
<task type="auto">
  <name>Task 2: Lock rejected shapes and compile-time constraints</name>
  <files>tests/domain/components/plugin.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/plugin.ts` — invalid-shape branches and exported types.
    - `tests/domain/components/plugin.test.ts` — normalized accepted cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-03/D-05 constraints.
  </read_first>
  <action>Add explicit runtime rejection cases for missing entry name, missing entry source, invalid MCP top-level shapes, null, arrays, and primitives. Preserve compile-time-only constraints with module-scope `satisfies` and `@ts-expect-error` assignments, without runtime wrappers or phase comments, per D-03. Do not edit guideline files per D-05.</action>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/domain/components/plugin.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/plugin.ts</automated></verify>
  <acceptance_criteria>
    - Missing required entry fields and non-record values are rejected through public validators.
    - Positive/negative type assignments compile with expected errors consumed.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Runtime and compile-time plugin contracts are complete without fake type tests.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| manifest plugin value → plugin validators | Untrusted package metadata crosses a validation boundary. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-11-01 | Tampering | plugin entry validator | high | mitigate | Missing-name/source and invalid-shape cases lock required runtime fields. |
| T-108-11-02 | Elevation of Privilege | component/MCP payload selection | medium | mitigate | Complete accepted/rejected shape matrices preserve the exact public boundary. |
</threat_model>
<verification>
- `node --test tests/domain/components/plugin.test.ts`
- `npm run typecheck`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/plugin.ts`
- `npm run check`
</verification>
<success_criteria>
- Plugin entry, component, MCP, and manifest contracts remain unchanged.
- Runtime and type-only evidence follow their distinct rules.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-11-SUMMARY.md` when done.</output>
