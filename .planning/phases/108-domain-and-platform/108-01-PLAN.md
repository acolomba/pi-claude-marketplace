---
phase: 108-domain-and-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/domain/auth-registry.test.ts
autonomous: true
requirements: [MOD-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The GitHub and GitLab provider descriptors, host lookup, token environment variables, usernames, and scopes remain observably unchanged (D-01)."
    - "Every runtime case in the owner test has the exact lowercase arrange/act/assert structure, with the single-expression exception used only where D-03 permits it (D-02, D-03, D-04)."
    - "The canonical owner passes focused execution and direct source coverage without changing the approved testing rules (D-05)."
  artifacts:
    - path: tests/domain/auth-registry.test.ts
      provides: Canonical mirrored owner for auth-registry.ts
  key_links:
    - from: tests/domain/auth-registry.test.ts
      to: extensions/pi-claude-marketplace/domain/auth-registry.ts
      via: direct imports of GITHUB_PROVIDER, GITLAB_PROVIDER, and findProviderForHost
  prohibitions:
    - "No live authentication, token value, or environment credential may enter this test."
    - "No documentation or rule wording changes are in scope (D-05)."
---

<objective>
Establish the production-quality Phase 108 tracer by proving one complete source-to-owner-to-direct-coverage path for the authentication registry (D-01 through D-05).

Purpose: Demonstrate the mirrored-test structure and preserved-public-contract workflow before any expansion plan runs.
Output: A compliant canonical owner that locks provider metadata and lookup behavior.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/auth-registry.test.ts` — modified canonical owner; no production export is added.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the GitHub provider path from source contract through direct coverage</name>
  <files>tests/domain/auth-registry.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/auth-registry.ts` — source-of-truth descriptor and lookup implementation.
    - `tests/domain/auth-registry.test.ts` — existing canonical owner and assertions to preserve.
    - `.claude/rules/typescript-unit-testing.md` — mandatory test structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value and naming guidance.
  </read_first>
  <action>Normalize the GitHub descriptor and exact-host lookup cases in the existing owner per D-01, D-02, and D-04. Keep exact lowercase `// arrange`, `// act`, and `// assert` phase comments in that order with blank lines between phases. Assert the complete GitHub descriptor as one value, including `github.com`, `GH_TOKEN`, `GITHUB_TOKEN`, `x-access-token`, and the scopes exported by the source. Keep token contents absent from fixtures and output.</action>
  <verify>
    <automated>node --test tests/domain/auth-registry.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/auth-registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - The focused owner exits 0 and direct coverage accepts `auth-registry.ts`.
    - The GitHub assertion compares the complete exported descriptor, not a subset of fields.
    - Runtime cases contain the exact phase-comment spellings required by D-02.
  </acceptance_criteria>
  <done>The GitHub happy path is a permanent, runnable vertical slice from public export to compliant owner and direct coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Lock GitLab, unknown-host, and exact-equality boundaries</name>
  <files>tests/domain/auth-registry.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/auth-registry.ts` — exact GitLab metadata and lookup equality.
    - `tests/domain/auth-registry.test.ts` — tracer result and remaining cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-03 and D-05 constraints.
  </read_first>
  <action>Normalize and strengthen the GitLab and unknown-host cases. Assert the full GitLab descriptor including `gitlab.com`, `GITLAB_TOKEN`, `oauth2`, and `read_repository`; prove exact known hosts resolve while an empty string, a case-changed host, and a one-character lookalike remain unknown. This resolves the MOD-01 adjacency and empty-input probes for this registry through explicit source-grounded equality cases. Use `// act &amp; assert` only for a genuinely single assertion expression per D-03, and leave rule documentation untouched per D-05.</action>
  <verify>
    <automated>node --test tests/domain/auth-registry.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/auth-registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - Known GitHub and GitLab hosts resolve to their exact descriptors.
    - Empty, case-changed, and lookalike hosts return `undefined`.
    - GitLab scopes and credential metadata are checked as a whole value.
  </acceptance_criteria>
  <done>The registry's complete public contract and its equality boundary are preserved by the canonical owner.</done>
</task>

</tasks>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| host string → provider registry | Caller-controlled host text selects credential metadata. |
| test process → developer environment | Tests must not read or reveal real credential values. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-01-01 | Spoofing | `findProviderForHost` | medium | mitigate | Exact-host, case-changed, lookalike, and empty-host cases lock strict matching. |
| T-108-01-02 | Information Disclosure | provider credential metadata | high | mitigate | Assert environment-variable names only; never seed or print token values. |
| T-108-01-03 | Tampering | owner/source correspondence | medium | mitigate | Direct source import plus direct-coverage gate prevents proxy ownership and contract drift. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source item | Coverage |
|---|---|
| GOAL: reliable domain/platform modules with mirrored tests and preserved contracts | Plans 108-01 through 108-23, one roadmap pair each |
| MOD-01 | All 23 plans; equality/empty/order probes are concretely distributed across 108-01, 108-02, 108-03, 108-06, 108-08, 108-10, 108-14, 108-19, and 108-20 |
| RES-01 | 108-18 closes the resolver-owned boolean/result-union boundary; its complete ledger assigns lexical consumer migration to P113-24, P114-07/08/09/10/11/12/14, and P115-06/07 without claiming those later pairs complete |
| PRES-03 | 108-12, 108-21, and 108-22 shared callable contract registrars |
| PRES-04 | 108-12, 108-21, and 108-22 surgical planted-defect controls |
| RESEARCH: full audit, resolver discriminant, three port contracts, hermetic adapters, supplemental gate | 108-01..23, 108-18, 108-12/21/22, and 108-21 respectively |
| CONTEXT D-01..D-05 | Every owner normalization plan; first proven here |
| CONTEXT D-06..D-14 | Contract carrier plans 108-12, 108-21, and 108-22; structural gate in 108-21 |
| CONTEXT D-15 | Hermetic local-repository Git carrier 108-22 |
| CONTEXT D-16 | Injected credential-process carrier 108-21 |
| CONTEXT D-17 | Injected HTTP/wait device-flow carrier 108-12 |
| CONTEXT D-18..D-19 | Guarded factories and case-owned cleanup in 108-12, 108-21, and 108-22 |
| Edge probes: MOD-01 adjacency/empty/ordering | Exact equality in 108-01/02/14; empty/null in 108-06/10/19; stable order in 108-03/08/20 |
| Edge probes: RES-01 adjacency/empty/ordering | Boolean arms, unavailable minimal inputs, and declared/implicit ordering in 108-18 |
| Edge probe: PRES-03 unclassified | Full port-operation matrices in 108-12/21/22 |
| Edge probes: PRES-04 adjacency/empty/ordering | Exact one-case failure, non-empty literal failure set, and declared case order in 108-12/21/22 |

No source item is missing. There is no bespoke product-values or ethics prohibition for this internal test refactor; correctness is enforced by tests and security is enforced by each plan's threat model.
</source_audit>

<verification>
- `node --test tests/domain/auth-registry.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/auth-registry.ts`
- `npm run check`
</verification>

<success_criteria>
- The meaningful tracer passes before any dependent plan executes.
- Provider metadata and exact lookup boundaries remain unchanged.
- The canonical test satisfies D-01 through D-05 and owns the source directly.
</success_criteria>

<output>
Create `.planning/phases/108-domain-and-platform/108-01-SUMMARY.md` when done.
</output>
