---
phase: 108-domain-and-platform
plan: 09
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/components/hooks/schema.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "HOOKS_VALIDATOR continues to require command handlers while preserving accepted extra properties (D-01)."
    - "Runtime schema cases follow D-02/D-04, and compile-time-only checks use D-03's `satisfies`/`@ts-expect-error` exception."
  artifacts:
    - path: tests/domain/components/hooks/schema.test.ts
      provides: Canonical mirrored owner for hooks/schema.ts
  key_links:
    - from: tests/domain/components/hooks/schema.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hooks/schema.ts
      via: direct validator and type imports
  prohibitions:
    - "No fake runtime test or AAA comment may be added solely to exercise a TypeScript type (D-03)."
---

<objective>
Normalize the hooks schema owner while preserving its runtime validator and compile-time contract (D-01 through D-05).

Purpose: Keep runtime and type evidence distinct and precise.
Output: A compliant schema owner with explicit accepted/rejected shapes.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hooks/schema.test.ts` — modified runtime/type owner; no production symbol added.

<tasks>
<task type="auto">
  <name>Task 1: Normalize accepted and rejected HOOKS_VALIDATOR shapes</name>
  <files>tests/domain/components/hooks/schema.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks/schema.ts` — validator and exported type definitions.
    - `tests/domain/components/hooks/schema.test.ts` — current runtime/type cases.
    - `.claude/rules/typescript-unit-testing.md` — runtime and type-only structure rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — schema assertion guidance.
  </read_first>
  <action>Normalize all runtime validator cases per D-01, D-02, and D-04. Use complete accepted objects for a command handler with and without extra properties, and explicit rejected values for missing command, empty object, null, arrays, and primitives. Assert the validator's exact boolean/result contract rather than internal schema details.</action>
  <verify><automated>node --test tests/domain/components/hooks/schema.test.ts</automated></verify>
  <acceptance_criteria>
    - Required-command and extra-property behavior is explicit.
    - Null, array, primitive, empty-object, and missing-command inputs are covered.
    - Runtime cases use exact lowercase phase comments.
  </acceptance_criteria>
  <done>The runtime schema boundary is completely normalized.</done>
</task>
<task type="auto">
  <name>Task 2: Preserve compile-time schema shapes without fake runtime cases</name>
  <files>tests/domain/components/hooks/schema.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks/schema.ts` — exported TypeScript types.
    - `tests/domain/components/hooks/schema.test.ts` — normalized runtime cases and current type checks.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-03/D-05 decisions.
  </read_first>
  <action>Keep type-only evidence at module scope with positive `satisfies` assignments and negative `@ts-expect-error` assignments for required/invalid fields per D-03. Do not wrap these assignments in `test()`, and do not add phase comments to them. Run typecheck alongside focused direct coverage; leave rule wording unchanged per D-05.</action>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/domain/components/hooks/schema.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/schema.ts</automated></verify>
  <acceptance_criteria>
    - Positive type assignments compile and each negative assignment is consumed by its expected error.
    - No type-only assertion is disguised as a runtime test.
    - Direct coverage accepts the owner.
  </acceptance_criteria>
  <done>Runtime and compile-time schema contracts are both preserved in their proper forms.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| JSON-like handler → runtime schema | Untrusted manifest data crosses a validation boundary. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-09-01 | Tampering | `HOOKS_VALIDATOR` | high | mitigate | Complete invalid-shape matrix and required-command cases lock runtime rejection. |
| T-108-09-02 | Elevation of Privilege | exported handler types | medium | mitigate | `satisfies` and negative type assignments lock required fields without fake runtime evidence. |
</threat_model>
<verification>
- `node --test tests/domain/components/hooks/schema.test.ts`
- `npm run typecheck`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/schema.ts`
- `npm run check`
</verification>
<success_criteria>
- Runtime validator and TypeScript type contracts remain intact.
- D-03 type-only exception is followed exactly.
- The canonical owner passes direct coverage.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-09-SUMMARY.md` when done.</output>
