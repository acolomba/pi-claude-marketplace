---
phase: 108-domain-and-platform
plan: 16
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/name.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 22000, raw_tokens: 22000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Safe-name validation and all generated extension/plugin/marketplace names preserve exact outputs and rejection boundaries (D-01)."
    - "All modified runtime cases use D-02 through D-04 and independent complete strings."
  artifacts:
    - path: tests/domain/name.test.ts
      provides: Canonical mirrored owner for name.ts
  key_links:
    - from: tests/domain/name.test.ts
      to: extensions/pi-claude-marketplace/domain/name.ts
      via: direct validation and generated-name imports
  prohibitions:
    - "No generated-name expectation may call another production name generator."
---

<objective>
Normalize name validation/generation tests and preserve exact safety boundaries (D-01 through D-05).

Purpose: Keep filesystem/package identifiers deterministic and reject unsafe names.
Output: A compliant owner for every public name export.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/name.test.ts` — modified owner for `assertSafeName` and generated-name exports.

<tasks>
<task type="auto">
  <name>Task 1: Normalize safe and generated name outputs</name>
  <files>tests/domain/name.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/name.ts` — validation rules and all generators.
    - `tests/domain/name.test.ts` — current owner.
    - `.claude/rules/typescript-unit-testing.md` — D-02 structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — complete-string expectations.
  </read_first>
  <action>Normalize accepted validation and every generated extension/plugin/marketplace name case per D-01, D-02, and D-04. Assert exact complete strings from literal inputs, including punctuation/case behavior the source preserves. Do not derive one expected name by calling another production generator. Use D-03 only for a single throwing/rejection expression. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/name.test.ts</automated></verify>
  <acceptance_criteria>
    - Every public generator is directly imported and exact-string asserted.
    - Accepted boundary names remain accepted.
    - All touched runtime cases have exact lowercase phase comments.
  </acceptance_criteria>
  <done>Safe and generated name success behavior is normalized.</done>
</task>
<task type="auto">
  <name>Task 2: Lock empty, delimiter, traversal, and control-character rejection</name>
  <files>tests/domain/name.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/name.ts` — invalid-name branches and errors.
    - `tests/domain/name.test.ts` — normalized success cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Build a literal invalid-name table covering empty text, path delimiters, dot/traversal forms, NUL/control text, and source-defined adjacent valid/invalid length or character boundaries. Assert the exact public error for each and verify a one-character safe neighbor remains accepted. Leave docs unchanged per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/name.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/name.ts</automated></verify>
  <acceptance_criteria>
    - Every source-defined unsafe class has a literal rejection row.
    - Adjacent safe/unsafe names differ by a single relevant boundary.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Name safety and deterministic generation are fully covered.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| caller name → generated package/filesystem identifier | Untrusted text becomes a persistent identifier. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-16-01 | Tampering | `assertSafeName` | high | mitigate | Empty, delimiter, traversal, control, and adjacent valid/invalid cases lock containment. |
| T-108-16-02 | Spoofing | generated names | medium | mitigate | Literal complete-string assertions cover every generator independently. |
</threat_model>
<verification>
- `node --test tests/domain/name.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/name.ts`
- `npm run check`
</verification>
<success_criteria>
- All generators and safety branches remain exact.
- Unsafe names cannot cross the public validator.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-16-SUMMARY.md` when done.</output>
