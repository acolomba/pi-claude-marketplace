---
phase: 108-domain-and-platform
plan: 08
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/components/hooks/partition.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Hook partitioning preserves group and handler order while returning exact accepted and rejected collections (D-01)."
    - "All modified cases use D-02 through D-04 structure and independent whole outputs."
  artifacts:
    - path: tests/domain/components/hooks/partition.test.ts
      provides: Canonical mirrored owner for partition.ts
  key_links:
    - from: tests/domain/components/hooks/partition.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hooks/partition.ts
      via: direct `partitionHooks` import
  prohibitions:
    - "No expected partition may reuse or mutate the input object under test."
---

<objective>
Normalize the hook partition owner and preserve accepted/rejected output ordering (D-01 through D-05).

Purpose: Prevent unknown groups or handlers from leaking into executable output.
Output: A compliant owner with complete partition values and ordering edges.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hooks/partition.test.ts` — modified owner for `partitionHooks`.

<tasks>
<task type="auto">
  <name>Task 1: Normalize complete accepted and rejected partitions</name>
  <files>tests/domain/components/hooks/partition.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks/partition.ts` — partition traversal and result shape.
    - `tests/domain/components/hooks/partition.test.ts` — current owner.
    - `extensions/pi-claude-marketplace/domain/components/hook-events.ts` — supported group/event closed sets.
    - `.claude/rules/typescript-unit-testing.md` — D-02 structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value assertions.
  </read_first>
  <action>Normalize every current case per D-01, D-02, and D-04. For mixed known/unknown groups and handlers, compare the entire accepted structure and entire rejection-record list as independently literal values. Preserve the exact `Object.entries`-derived group order and handler order. Use D-03 only for true single-expression cases.</action>
  <verify><automated>node --test tests/domain/components/hooks/partition.test.ts</automated></verify>
  <acceptance_criteria>
    - Accepted groups/handlers and all rejection records are asserted in one complete result.
    - Input group and handler order is reflected exactly in output order.
    - Unknown entries do not appear in accepted output.
  </acceptance_criteria>
  <done>Mixed partitions and rejection evidence are complete and ordered.</done>
</task>
<task type="auto">
  <name>Task 2: Lock empty, duplicate, and all-rejected ordering edges</name>
  <files>tests/domain/components/hooks/partition.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks/partition.ts` — empty/all-rejected omission behavior.
    - `tests/domain/components/hooks/partition.test.ts` — normalized mixed case.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Add complete cases for an empty configuration, a group with no supported handlers, repeated equal handler values in distinct positions, and multiple rejected records. Assert that empty/all-rejected output omits the group, repeated entries retain input order where the input representation permits them, and rejection records remain stably ordered. This resolves MOD-01 empty, adjacency/equality, and ordering probes for partitioning. Leave documentation untouched per D-05.</action>
  <verify><automated>node --test tests/domain/components/hooks/partition.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/partition.ts</automated></verify>
  <acceptance_criteria>
    - Empty input returns the exact empty partition result.
    - Groups with zero supported handlers are absent from accepted output.
    - Equal/rejected entries retain source-defined stable order.
  </acceptance_criteria>
  <done>Partition empty and ordering boundaries are explicit and directly covered.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| hook configuration → executable/rejected partitions | Untrusted hooks are separated from executable data. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-08-01 | Elevation of Privilege | `partitionHooks` | high | mitigate | Mixed, all-rejected, and unknown-group cases prove unsupported handlers never enter accepted output. |
| T-108-08-02 | Tampering | partition ordering | medium | mitigate | Literal whole results lock group, handler, and rejection-record order. |
</threat_model>
<verification>
- `node --test tests/domain/components/hooks/partition.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/partition.ts`
- `npm run check`
</verification>
<success_criteria>
- Partition content and order remain unchanged.
- Empty and all-rejected behavior is explicit.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-08-SUMMARY.md` when done.</output>
