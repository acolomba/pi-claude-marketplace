# Phase 06 Install PRE-EDIT Ledger

Status: READY
Hub: extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
Legacy test: tests/orchestrators/plugin/install.test.ts

Fresh evidence was captured on 2026-09-09 with:

```text
codegraph explore "PRE-EDIT install: trace every export, production caller, owner test, gate, document reference, dependency edge, and cycle for extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
```

The result is stored at `/tmp/phase06-install-preedit-codegraph.txt` (514 lines,
25,027 bytes). CodeGraph found three hub exports, the outcome projection caller,
the transaction and bridge dependencies, and the direct flow consumers. A
tracked-root literal scan supplied the architecture, scanner, and prose links
that are outside the symbol graph. No unresolved dependency loop was reported.

## Complete ownership and dependency map

| Category               | Current owner                                                                                                               | Destination                                                                                                                                             | Evidence                                                                                                                                               |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| exported symbol        | `InstallLedgerContext` mutable transaction scratchpad                                                                       | private `InstallLedgerContext` in `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                            | Only the outcome projection consumes the shape; no public caller requires it.                                                                          |
| exported symbol        | `InstallLedgerContextResult` internal ledger discriminant                                                                   | private `InstallLedgerContextResult` in `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                      | Only the ledger executor and outcome projection consume the internal context-bearing arm.                                                              |
| exported symbol        | `executeInstallLedger` guard-free executor                                                                                  | private `executeInstallLedger` in `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                            | The public owner remains `runInstallLedger`; the duplicate legacy test case already lives under that boundary.                                         |
| exported symbol        | clone cache seam and probe                                                                                                  | `extensions/pi-claude-marketplace/orchestrators/plugin/install-clone-probe.ts`                                                                          | Plan 06-34 moved `InstallCloneCacheSeam` and `probeInstallClone` to the direct leaf.                                                                   |
| exported symbol        | declared-enabled policy                                                                                                     | `extensions/pi-claude-marketplace/orchestrators/plugin/install-declared-enabled.ts`                                                                     | Plan 06-34 moved `declaredEnabledForInstall` to the direct leaf.                                                                                       |
| exported symbol        | disabled cascade and hooks routing                                                                                          | `extensions/pi-claude-marketplace/orchestrators/plugin/install-disable-cascade.ts`                                                                      | Plan 06-35 moved `composeInstallDisableCascade` and `InstallHooksRouting` to the direct leaf.                                                          |
| exported symbol        | public ledger contracts and installed outcome projection                                                                    | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | `InstallLedgerOptions`, transaction/failure/summary/result types, `runInstallLedger`, and `installedPluginOutcome` already expose the caller boundary. |
| exported symbol        | install options, transaction composition, and factories                                                                     | `extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts`                                                                                 | Plan 06-36 moved `InstallPluginOptions`, `InstallTransaction`, `createInstallPlugin`, and `createNodeInstallPlugin` to the flow owner.                 |
| production caller      | `install-outcome.ts` imports `executeInstallLedger` and `InstallLedgerContext`                                              | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | Merge makes the executor and scratchpad private to the public ledger owner.                                                                            |
| production caller      | `install-flow.ts` calls `runInstallLedger` and projects `installedPluginOutcome`                                            | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | Existing direct import remains unchanged after the merge.                                                                                              |
| production caller      | `enable-disable.ts` calls `runInstallLedger` inside its own lock                                                            | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | Existing direct import preserves the non-reentrant-lock contract.                                                                                      |
| production caller      | install edge handler constructs the node install flow                                                                       | `extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts`                                                                                 | Existing direct import preserves the public command boundary.                                                                                          |
| production caller      | import execution constructs the node install flow                                                                           | `extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts`                                                                                 | Existing direct import preserves cascade diagnostics and warning folding.                                                                              |
| production caller      | reconcile apply constructs the node install flow                                                                            | `extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts`                                                                                 | Existing direct import preserves reconcile install behavior.                                                                                           |
| source-scanning gate   | `scripts/check-phase-06-hub-ledger.mjs` legacy-hub inventory                                                                | `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`                                                                                       | Retired install pair is removed; generic lifecycle focus advances to the next live Phase 06 hub without weakening exact-set closure.                   |
| source-scanning gate   | `tests/scripts/check-phase-06-hub-ledger.test.ts` PRE-EDIT fixture                                                          | `tests/orchestrators/plugin/update.test.ts`                                                                                                             | Positive and fail-closed fixture cases now exercise the exact Plan 06-39 pair.                                                                         |
| source-scanning gate   | `tests/architecture/no-orchestrator-network.test.ts` ledger target                                                          | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | The guard-free ledger remains checked for forbidden Git/network surface after ownership moves.                                                         |
| source-scanning gate   | `tests/architecture/manifest-lookup-drift.test.ts` non-absence lookup                                                       | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | The cached-manifest lookup and stated install purpose move together.                                                                                   |
| source-scanning gate   | `tests/orchestrators/plugin/install-flow.test.ts` NFR-5 source probe                                                        | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | The exact source assertion continues to inspect the ledger implementation.                                                                             |
| documentation comment  | `extensions/pi-claude-marketplace/shared/errors.ts` concurrent-install owner note                                           | `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts`                                                                              | `ConcurrentInstallError` remains thrown by the guard-free ledger.                                                                                      |
| documentation comment  | `tests/edge/handlers/plugin/install.test.ts` producer-owner note                                                            | `tests/orchestrators/plugin/install-flow.test.ts`                                                                                                       | The complete command flow and exact notifications live in the direct flow suite.                                                                       |
| documentation comment  | `tests/orchestrators/plugin/enable-disable.test.ts` rollback-partial note                                                   | `tests/orchestrators/plugin/install-outcome.test.ts`                                                                                                    | The public ledger outcome suite owns rollback projection while flow keeps end-to-end composition.                                                      |
| documentation comment  | `tests/architecture/cross-op-convergence.test.ts` install scope note                                                        | `tests/orchestrators/plugin/install-flow.test.ts`                                                                                                       | The direct flow suite retains explicit-scope install proof.                                                                                            |
| documentation comment  | Phase 06 `CONTEXT.md`, `RESEARCH.md`, `PATTERNS.md`, `VALIDATION.md`, Plans 06-34..38, and Summaries 06-01/07..09/20/33..35 | `06-install-PREEDIT.md` plus direct owner files and tests                                                                                               | Historical records remain immutable evidence; this ledger records their final ownership resolution.                                                    |
| test ownership         | clone cache hit, miss, auth retry, and failure cases                                                                        | `tests/orchestrators/plugin/install-clone-probe.test.ts`                                                                                                | Direct suite preserves exact clone seam behavior and coverage.                                                                                         |
| test ownership         | marketplace and plugin declared-enabled precedence                                                                          | `tests/orchestrators/plugin/install-declared-enabled.test.ts`                                                                                           | Direct suite preserves exact preference precedence and coverage.                                                                                       |
| test ownership         | disabled install cascade order, severity, and hook routing                                                                  | `tests/orchestrators/plugin/install-disable-cascade.test.ts`                                                                                            | Direct suite preserves exact cascade rows and coverage.                                                                                                |
| test ownership         | marketplace-absent, summary projection, rollback capture, and public outcomes                                               | `tests/orchestrators/plugin/install-outcome.test.ts`                                                                                                    | The former legacy marketplace-absent case is present once under the public ledger boundary.                                                            |
| test ownership         | one complete install transaction, state/tree effects, exact Plan 06-01 notification arrays, and factory composition         | `tests/orchestrators/plugin/install-flow.test.ts`                                                                                                       | The sole end-to-end flow suite retains complete ordered output and transaction proof.                                                                  |
| completeness invariant | five production owners have five mirrored direct suites                                                                     | `install-clone-probe.test.ts`, `install-declared-enabled.test.ts`, `install-disable-cascade.test.ts`, `install-outcome.test.ts`, `install-flow.test.ts` | Corresponding-test and hub-closure gates require each named pair.                                                                                      |
| completeness invariant | legacy marketplace-absent case occurs once                                                                                  | `tests/orchestrators/plugin/install-outcome.test.ts`                                                                                                    | Task 06-38-01 removed its duplicate body from the tracked legacy test before deletion.                                                                 |
| completeness invariant | lifecycle imports use direct flow/leaf owners                                                                               | `tests/orchestrators/plugin/reinstall.test.ts` and `tests/orchestrators/plugin/update.test.ts`                                                          | Both suites import install flow and clone seams from direct named owners.                                                                              |
| completeness invariant | tracked-root exact legacy path count reaches zero after atomic deletion                                                     | `extensions`, `tests`, `scripts`, `docs`, and `eslint.config.js`                                                                                        | Every nonhistorical literal reference has a destination above; the final tracked-root scan is fail-closed.                                             |
| dependency edge        | `install-flow.ts` -> public ledger boundary                                                                                 | `install-flow.ts` -> `install-outcome.ts`                                                                                                               | One-way command composition; the outcome owner does not import the flow owner.                                                                         |
| dependency edge        | `enable-disable.ts` -> public ledger boundary                                                                               | `enable-disable.ts` -> `install-outcome.ts`                                                                                                             | One-way re-materialization call under the enable/disable transaction lock.                                                                             |
| dependency edge        | `install-outcome.ts` -> clone probe, domain, bridge, persistence, shared, and transaction primitives                        | `install-outcome.ts` -> named lower-level owners                                                                                                        | Fresh CodeGraph shows the ledger implementation depends only on lower-level primitives.                                                                |
| dependency edge        | `install-flow.ts` -> clone preference, disabled cascade, and outcome leaves                                                 | `install-flow.ts` -> four named direct owners                                                                                                           | Flow composes leaves; none imports `install-flow.ts`.                                                                                                  |
| dependency edge        | edge/import/reconcile callers -> flow factory                                                                               | command callers -> `install-flow.ts`                                                                                                                    | One-way factory consumption; no owner imports an edge or command caller.                                                                               |

## Gate result

Deletion is authorized. All three remaining hub exports become private details of
the public outcome owner. Every responsibility, production caller, test owner,
scanner, prose reference, completeness check, and dependency edge has one direct
destination. The fresh graph and import scan show only one-way owner edges.
The generic checker fixture is rotated to the update hub/test pair before any
install deletion, preserving all positive and fail-closed assertions.
