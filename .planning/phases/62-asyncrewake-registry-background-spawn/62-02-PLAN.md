---
phase: 62
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  - tests/architecture/no-shell-out.test.ts
autonomous: true
requirements:
  - HOOK-06
  - EXEC-05
user_setup: []
tags:
  - hooks
  - async-rewake
  - registry
  - spawn
  - whitelist-amendment

must_haves:
  truths:
    - "`bridges/hooks/async-rewake/registry.ts` is the SOLE third sanctioned `node:child_process` import site in the extension tree (after `platform/git-credential.ts` and `bridges/hooks/dispatch-exec.ts`) per D-62-01 / D-58-01"
    - "`spawnAndRegister(entry, event, ctx)` builds a translated stdin payload via the Phase 60 translators, prepares env (existing CLAUDE_* set PLUS the new `PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH=<dispatchId>` marker), calls `spawn(command, args, { detached: false, stdio: ['pipe','pipe','pipe'], shell, cwd, env })`, arms the EXEC-02 timer ladder verbatim, adds an `AsyncRewakeEntry` to the in-memory `Map<dispatchId, AsyncRewakeEntry>`, persists the PID table atomically, and resolves IMMEDIATELY without awaiting child exit (EXEC-05 / D-62-01 / D-62-03)"
    - "On child `exit` event: the registered exit handler cancels the timer ladder, removes the registry entry, persists the PID table (fire-and-forget), gates on the captured-epoch zombie defense (D-59-03 reuse), surfaces `rewakeSummary` via `ctx.ui.notify(text, 'info')` when set (IL-2 EXEMPTION documented), and on exit code 2 ONLY injects via `ctx.sendMessage({ customType: 'claude-hook-rewake', display: false, content: <rewakeMessage + '\\n\\n' + body>, details: { pluginId, dispatchId } }, { deliverAs: ctx.isIdle() ? 'nextTurn' : 'followUp' })` (HOOK-06 / D-62-02..04)"
    - "`tests/architecture/no-shell-out.test.ts` is amended atomically (same commit) with the FIRST commit that adds `import { spawn } from 'node:child_process'` to `registry.ts`: ALLOWED_CHILD_PROCESS_FILES grows from TWO to THREE entries including `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts`; the `whitelist: exactly two files ...` assertion is renamed to `exactly three files ...` and its expected array is updated in lockstep (D-58-01 atomic-supersession)"
    - "`shutdownInMemoryChildren()` walks the in-memory `Map`, calls `child.kill('SIGKILL')` on each entry (best-effort try/catch), and clears the map — used at factory entry by Plan 03's `event-router.ts` wiring (D-62-05 `/reload` clear)"
    - "`reapOrphans(loc)` reads the PID table, probes each PID via `process.kill(pid, 0)`, on Linux verifies `/proc/<pid>/environ` marker against the recorded `dispatchId`, SIGKILLs owned PIDs, soft-skips on macOS / read failure / marker mismatch (NEVER kills strangers), then `unlinkPidTable(loc)` — used at factory entry by Plan 03's wiring (D-62-05)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      provides: "spawnAndRegister, shutdownInMemoryChildren, reapOrphans, AsyncRewakeEntry interface, MARKER_ENV constant, internal Map<dispatchId, AsyncRewakeEntry>"
      min_lines: 250
    - path: "tests/architecture/no-shell-out.test.ts"
      provides: "ALLOWED_CHILD_PROCESS_FILES grows TWO->THREE, sibling assertion renamed exactly two -> exactly three"
      contains: "async-rewake/registry.ts"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts"
      via: "RingBuffer + STDERR_CAP_BYTES + STDOUT_CAP_BYTES imports"
      pattern: "from \"\\./ring-buffer\\.ts\""
    - from: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts"
      via: "readPidTable + writePidTable + unlinkPidTable imports"
      pattern: "from \"\\./pid-table\\.ts\""
    - from: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts"
      via: "installTimerLadder reuse (EXEC-02 verbatim)"
      pattern: "from \"\\.\\./exec-timer\\.ts\""
    - from: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts"
      via: "buildTranslationContext + per-event translators reuse"
      pattern: "from \"\\.\\./translation-context\\.ts\""
    - from: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts"
      via: "currentEpoch() import for captured-epoch defense"
      pattern: "from \"\\.\\./event-router\\.ts\""
    - from: "tests/architecture/no-shell-out.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      via: "ALLOWED_CHILD_PROCESS_FILES closed-set membership"
      pattern: "async-rewake/registry\\.ts"
---

<objective>
Assemble the `bridges/hooks/async-rewake/registry.ts` module — the THIRD
and FINAL sanctioned `node:child_process` import site in the extension
tree — and amend `tests/architecture/no-shell-out.test.ts` in the SAME
commit per the D-58-01 atomic-supersession lesson. The registry holds
the in-memory `Map<dispatchId, AsyncRewakeEntry>`, the spawn-and-register
synchronous step, the per-child exit handler that wires HOOK-06's
exit-code-2 injection contract through `ctx.sendMessage`, the
`rewakeSummary` UI surface (the single sanctioned IL-2 exemption per
Phase 62), the `shutdownInMemoryChildren()` `/reload` cleanup walk, and
the `reapOrphans(loc)` factory-entry orphan reap that reads the PID
table Plan 01 wrote.

Purpose: This plan owns the load-bearing OS-level concern (child process
construction + lifetime management). The closed-set whitelist amendment
MUST land in the same commit as the first source line that imports
`node:child_process` from the new file path — otherwise the architecture
test would go RED on Plan 03's wiring step and we'd ship a broken
intermediate state (D-58-01 lesson from Phase 58).

Output: 1 NEW file `bridges/hooks/async-rewake/registry.ts` (~250-300 LoC),
1 MODIFIED test `tests/architecture/no-shell-out.test.ts` (3 surgical
edits: whitelist set, sibling assertion, docstring). The registry compiles
against the Wave-1 leaves WITHOUT touching `dispatch-exec.ts` or
`event-router.ts` — those wirings land in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-asyncrewake-registry-background-spawn/62-CONTEXT.md
@.planning/phases/62-asyncrewake-registry-background-spawn/62-RESEARCH.md
@.planning/phases/62-asyncrewake-registry-background-spawn/62-PATTERNS.md
@.planning/phases/62-asyncrewake-registry-background-spawn/62-VALIDATION.md
@.planning/phases/62-asyncrewake-registry-background-spawn/62-01-PLAN.md
@.planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-CONTEXT.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
@extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
@extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts
@extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
@extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts
@extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts
@extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts
@extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts
@extensions/pi-claude-marketplace/shared/atomic-json.ts
@extensions/pi-claude-marketplace/shared/path-safety.ts
@extensions/pi-claude-marketplace/shared/debug-log.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
@tests/architecture/no-shell-out.test.ts
</context>

## Artifacts this phase produces

This plan creates the following new symbols. The plan-review-convergence
source-grounding pass should exclude these from drift verification:

- **Modules / files (new):**
  - `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts`
- **Modules / files (modified):**
  - `tests/architecture/no-shell-out.test.ts` (closed-set whitelist 2 -> 3)
- **Classes / interfaces:** `AsyncRewakeEntry` (readonly-field interface
  carrying `dispatchId`, `pid`, `scope`, `marketplace`, `pluginId`,
  `claudeEvent`, `spawnedAt`, `rewakeMessage`, `rewakeSummary`, `child`,
  `ladder`, `stdoutBuffer`, `stderrBuffer`, `capturedEpoch`).
- **Functions / helpers:** `spawnAndRegister`, `shutdownInMemoryChildren`,
  `reapOrphans`, internal `onChildExit`, `onChildError`,
  `persistPidTableForLoc`, `isPidAlive`, `readProcEnvironMarker`,
  `buildInjectionContent`, `prepareAsyncEnv`, `planAsyncSpawn`,
  `_setSpawnForTest`, `_resetSpawnForTest`, `_setOrphanProbesForTest`,
  `_resetOrphanProbesForTest`, `_setDispatchIdGeneratorForTest`,
  `_resetDispatchIdGeneratorForTest`, `_getRegistryForTest` (test seams
  matching the Phase 60 `dispatch-exec.ts` pattern).
- **Module-state:** `const asyncRewakeRegistry = new Map<string, AsyncRewakeEntry>()`
  (private; only test seam reads). `let activeSpawn: SpawnImpl = spawn`
  (test seam mirror of `dispatch-exec.ts:117`).
- **Constants:** `MARKER_ENV = "PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH"`,
  `DEFAULT_TIMEOUT_MS = 600_000`, `BODY_SEPARATOR = "\n\n"`,
  `TRUNCATED_PREFIX = "[…truncated]\n\n"`, `REWAKE_CUSTOM_TYPE =
  "claude-hook-rewake"`.
- **Test file amendments:** `tests/architecture/no-shell-out.test.ts`
  `ALLOWED_CHILD_PROCESS_FILES` grows from 2 to 3 entries (sorted), the
  sibling `whitelist: exactly two files...` assertion is renamed to
  `exactly three files...` and its expected array is updated, and the
  file's documentation comment block adds a third paragraph naming the
  registry as the EXEC-05 / HOOK-06 / D-62-01 third sanctioned site.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic-supersession — amend tests/architecture/no-shell-out.test.ts AND create the registry.ts skeleton with the first import "node:child_process" in the SAME commit</name>
  <files>
    extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts,
    tests/architecture/no-shell-out.test.ts
  </files>
  <read_first>
    - tests/architecture/no-shell-out.test.ts (full file ~117 LoC; ALLOWED_CHILD_PROCESS_FILES at :57-60; the `whitelist: exactly two files...` sibling assertion at :112-117; the docstring header block at :40-56 documenting "TWO sanctioned"; the introspection iteration that calls `ALLOWED_CHILD_PROCESS_FILES.has(rel)` at :87)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts (lines 1-150; the file-leading whitelist-comment block at :43-49 + the `import { spawn } from "node:child_process"` at :51 + the SpawnImpl test seam at :116-131; the THIRD sanctioned site's file-leading block MUST cross-reference this same convention)
    - extensions/pi-claude-marketplace/platform/git-credential.ts (lines 1-30; the SECOND sanctioned site for comparison of file-leading block shape; sample only — no need to read in full)
    - .planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-CONTEXT.md (D-58-01 verbatim — the atomic-supersession lesson: byte-form rename of a closed-set test invariant MUST land in the SAME commit as the first source line that breaks the old invariant; otherwise the suite is RED in the intermediate state)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-CONTEXT.md (lines 296-311 Discretion "tests/architecture/no-shell-out.test.ts atomic amendment" — verbatim Planner instruction to place the test amendment in the very first task that creates `registry.ts`)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-PATTERNS.md (lines 339-381 verbatim 3-change diff for no-shell-out.test.ts: whitelist set, sibling assertion, docstring header)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-RESEARCH.md (lines 1305-1359 verbatim ALLOWED_CHILD_PROCESS_FILES end-state; the file-leading whitelist comment header for registry.ts; the assertion form `assert.deepEqual([...ALLOWED_CHILD_PROCESS_FILES].sort(), [...])`)
  </read_first>
  <behavior>
    - `tests/architecture/no-shell-out.test.ts` ALLOWED_CHILD_PROCESS_FILES is exactly `new Set(["extensions/pi-claude-marketplace/platform/git-credential.ts", "extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts", "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"])`.
    - The sibling `whitelist: exactly two files may import node:child_process` test title is renamed to `whitelist: exactly three files may import node:child_process` and its `assert.deepEqual([...ALLOWED_CHILD_PROCESS_FILES].sort(), [...])` expected array is updated to the 3-element sorted set.
    - The file's leading documentation comment block gains a third paragraph documenting the registry as the EXEC-05 / HOOK-06 / D-62-01 third sanctioned site (the async-rewake background-spawn surface, divergent from EXEC-01..04 because the parent does NOT await child exit).
    - `bridges/hooks/async-rewake/registry.ts` exists as a stub file with: file-leading comment block in the `bridges/hooks/<concern>/` convention; the SAME whitelist-cross-reference paragraph the file-leading dispatch-exec.ts uses ("the SECOND of exactly THREE sanctioned ... import sites" -> for registry.ts it's "the THIRD of exactly THREE sanctioned ... import sites"); `import { spawn } from "node:child_process";`; a stub `spawnAndRegister`, `shutdownInMemoryChildren`, `reapOrphans` triplet that satisfies the typechecker (each stub returns immediately or throws an explicit `not yet implemented` Error that Task 2 replaces).
    - `node --test tests/architecture/no-shell-out.test.ts` passes after both files commit together.
    - `npx tsc --noEmit` GREEN.
  </behavior>
  <action>
    Edit `tests/architecture/no-shell-out.test.ts` and create `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` AS ONE COMMIT. The test amendment AND the source file creation MUST land in the SAME commit per D-58-01 (otherwise the suite goes RED on the intermediate commit). Use `git add` on both files before the commit; do not commit them separately.

    **Edit 1 — `tests/architecture/no-shell-out.test.ts`**.

    Change A — ALLOWED_CHILD_PROCESS_FILES at :57-60. Add the third entry, keeping alphabetical order inside the source declaration is NOT required (Set membership is order-independent for the introspection), BUT the sorted expected array in Change B MUST be alphabetical:

    ```
    const ALLOWED_CHILD_PROCESS_FILES: ReadonlySet<string> = new Set([
      "extensions/pi-claude-marketplace/platform/git-credential.ts",
      "extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts",
      "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts",
    ]);
    ```

    Change B — sibling assertion at :112-117. Rename title `exactly two files` -> `exactly three files`. Update the expected array (sorted):

    ```
    test("whitelist: exactly three files may import node:child_process", () => {
      assert.deepEqual([...ALLOWED_CHILD_PROCESS_FILES].sort(), [
        "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts",
        "extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts",
        "extensions/pi-claude-marketplace/platform/git-credential.ts",
      ]);
    });
    ```

    Change C — docstring header at :40-56 (the leading comment block that names the TWO sanctioned sites and explains the closed-set discipline). Update every `TWO`/`two` reference to `THREE`/`three`. Add a third paragraph paralleling the existing EXEC-01..04 paragraph, naming the new site:

    > EXEC-05 / HOOK-06 / D-62-01 third sanctioned site: the async-rewake
    > registry at extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    > is the bridge-owned background-spawn site for `asyncRewake: true`
    > hook entries. Spawn options diverge from the sync EXEC-01..04 path
    > (`detached: false` + fire-and-forget exit handler watching for code 2
    > -> `pi.sendMessage` injection); the OS-level concern lives here
    > because no pure-JS equivalent exists for arbitrary plugin-supplied
    > commands AND the registry owns in-memory + persisted (PID table)
    > state for `/reload`-safe SIGKILL. Adding a FOURTH file requires the
    > same edit-here + edit-sibling-assertion discipline.

    The existing `// ALLOWED_CHILD_PROCESS_FILES above AND this assertion's expected array` cross-reference comment at :109-110 must stay; if it says "exactly two" anywhere in the surrounding lines, rename to "exactly three". NO Phase/Plan/Wave/Pitfall/Pattern tokens in any new comment.

    **Edit 2 — create `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts`** as a stub file that introduces `import { spawn } from "node:child_process"`. File-leading comment block (matches the `dispatch-exec.ts:43-49` whitelist cross-reference shape verbatim, adjusted to "third"):

    ```
    // extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    //
    // Bridge-owned asyncRewake registry. Spawns hook children detached-but-in-
    // parent-process-group, holds them in an in-memory Map keyed by a UUID
    // dispatchId, watches each child for exit code 2, and on exit-2 injects
    // (`rewakeMessage` + body) into Pi's model context via ctx.sendMessage with
    // `display: false` matching Claude Code's <system-reminder> semantic.
    //
    // This is the THIRD of exactly THREE sanctioned `node:child_process` import
    // sites in the extension tree (closed-set whitelist at
    // tests/architecture/no-shell-out.test.ts). Adding a FOURTH site requires
    // amending the whitelist set AND the sibling assertion in the SAME commit
    // (D-58-01 atomic-supersession). The first two sites are
    // platform/git-credential.ts (AUTH-08) and bridges/hooks/dispatch-exec.ts
    // (EXEC-01..04 sync path). This site (HOOK-06 / EXEC-05 / D-62-01) diverges
    // from the sync site: `detached: false` + the parent does NOT await child
    // exit; a per-child handler watches for code 2 and triggers the HOOK-06
    // injection contract.
    ```

    Then add the SOLE `node:child_process` import line:

    ```
    import { spawn, type ChildProcess } from "node:child_process";
    ```

    Plus minimal stubs that compile under the typechecker. Use stubs that throw `Error("not yet implemented; Task 2 wires this")` so Task 2's body fills them in. Stub signatures (the FINAL public surface — Task 2 expands the bodies, not the signatures):

    ```
    export interface AsyncRewakeEntry { /* readonly-field shape - leave declared at minimum but allow Task 2 to fill */ }

    export async function spawnAndRegister(
      entry: RoutingEntry,
      event: unknown,
      ctx: ExtensionContext,
    ): Promise<void> { throw new Error("not yet implemented"); }

    export function shutdownInMemoryChildren(): void { /* Task 2 fills */ }

    export async function reapOrphans(loc: ScopedLocations): Promise<void> { /* Task 2 fills */ }
    ```

    Task 1 stubs ARE allowed to be incomplete — Task 2 expands. The point of Task 1 is to atomically add the `import "node:child_process"` line AND the no-shell-out.test.ts amendment in one commit so the closed-set test stays GREEN. Plan 03 will wire the dispatcher/router to these symbols; do NOT call them anywhere yet.

    Run `npx tsc --noEmit` after both edits. The stub file must satisfy the typechecker (use `unknown` placeholders for unused parameters if necessary). Use `void` returns where possible to avoid signature mismatches.

    Run `node --test tests/architecture/no-shell-out.test.ts` after the commit. Expected: 2/2 tests pass; the whitelist counts THREE files and the `bridges/hooks/async-rewake/registry.ts` entry is recognized (the introspection at :87 iterates all `.ts` files under `extensions/` and checks `ALLOWED_CHILD_PROCESS_FILES.has(rel)`; the new file imports `node:child_process` so the test would RED if it weren't in the set).

    Commit with a single Conventional Commit message naming both files; do NOT split the commit. Pre-commit hook (`pre-commit run --files <files>`) must be GREEN before `git commit`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; node --test tests/architecture/no-shell-out.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    `tests/architecture/no-shell-out.test.ts` ALLOWED_CHILD_PROCESS_FILES has exactly 3 entries; the sibling test is renamed `whitelist: exactly three files...`; the expected array in the deepEqual is the 3-element alphabetical sort; the docstring header has the third paragraph naming the registry; `bridges/hooks/async-rewake/registry.ts` exists with `import { spawn, type ChildProcess } from "node:child_process"`; `node --test tests/architecture/no-shell-out.test.ts` GREEN (2/2 pass); `npx tsc --noEmit` GREEN; both file edits land in ONE commit (verify via `git log -1 --name-only` shows both paths); `grep -nE 'Phase [0-9]+|Pitfall [0-9]+|Pattern [0-9]+|Plan [0-9]+-[0-9]+|Wave [0-9]+|Task [0-9]+' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts tests/architecture/no-shell-out.test.ts | grep -v '^#'` returns no matches.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill registry.ts with spawnAndRegister, AsyncRewakeEntry, per-child exit handler (HOOK-06 inject), shutdownInMemoryChildren, reapOrphans (D-62-05), and test seams</name>
  <files>
    extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts (full file ~507 LoC — Phase 60 sync sibling; CRITICAL READS: import block at :51-75, SpawnImpl test seam at :116-131, dispatchHookExec body at :146-161, the prepareEnv helper at :275-303, the planSpawn EXEC-04 discriminator at :309-336, spawnAndCollect body at :338-440, the serializeWithTruncation helper at :249-265 — these are the reuse points D-60-* established)
    - extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts (full file; installTimerLadder signature + the TimerLadder return shape with `.cancel()`)
    - extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts (full file; buildTranslationContext + the 8 per-event translator dispatch — the async branch uses the SAME translator chain)
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (lines 120-184 — `liveEpoch` cell, `currentEpoch()` export; lines 580-614 the registerHooksBridge body for context; the SCOPES tuple and locationsFor pattern Plan 03 will use)
    - extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts (full file; HookExecResult discriminated union — async branch returns `{kind:"noop"}` to the reducer per D-62-01)
    - extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts (sampling — does it export `serializeWithTruncation`? If yes, registry.ts imports from here. If no, registry.ts can call the dispatch-exec.ts local helper OR Plan 02 may extract it to a shared module per RESEARCH Open Question #2 — implementer picks; the OUT criterion is "registry.ts does NOT duplicate the truncation logic")
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts (the file created in Plan 01 Task 1 — RingBuffer class signature + STDERR_CAP_BYTES + STDOUT_CAP_BYTES constants)
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts (the file created in Plan 01 Task 2 — readPidTable, writePidTable, unlinkPidTable, PidTableEntry interface, ASYNC_REWAKE_PIDS_FILENAME)
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (sampling — BucketAEvent type, the 8-event tuple for typing the `claudeEvent` field on AsyncRewakeEntry)
    - extensions/pi-claude-marketplace/shared/path-safety.ts (sample to confirm `assertPathInside` signature; registry.ts uses it for every derived path)
    - extensions/pi-claude-marketplace/shared/debug-log.ts (full file; hookDebugLog signature)
    - extensions/pi-claude-marketplace/shared/errors.ts (full file; errorMessage + assertNever signatures)
    - extensions/pi-claude-marketplace/platform/pi-api.ts (sampling — ExtensionContext interface; verify `ctx.sendMessage(message, opts)` signature, `ctx.isIdle(): boolean`, `ctx.ui.notify(message, severity)` — match the names that exist; D-62 research lines 285-291 / 857-860 of peer-dep types.d.ts confirm `ctx.sendMessage` is the seam, not `pi.sendMessage`)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-RESEARCH.md (lines 410-565 "Pattern: Discriminated `AsyncRewakeEntry` carrying captured-epoch + ring buffers" and "Pattern: `child_process.spawn` with detached=false + ring-buffered stderr/stdout" — full code sketches with line-by-line rationale; lines 700-790 "Pattern: Captured-epoch zombie defense" with the verbatim onChildExit body sketch including the OutcomeKind discriminated `"inject" | "silent" | "noop"` arm + `assertNever`; lines 1265-1340 the dispatch-exec delegation arm shape Plan 03 will install; lines 1326-1340 the ctx.sendMessage content shape)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-CONTEXT.md (lines 75-180 D-62-01..04 verbatim; lines 256-330 Claude's Discretion — pick `crypto.randomUUID()` for dispatchId; pick `\n\n` separator; pick `"info"` severity for rewakeSummary; pick single-file architecture-test; pick try/catch around ctx.sendMessage)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-PATTERNS.md (lines 22-126 the registry.ts pattern assignments and analog references — spawn options pattern, env prep pattern, exec-form/shell-form planSpawn, timer ladder reuse, EPIPE stdin defense, never-throws contract, captured-epoch defense, OutcomeKind exhaustiveness)
    - .planning/phases/62-asyncrewake-registry-background-spawn/62-VALIDATION.md (the per-task verification map; rows 62-02-01 / 62-02-02 are this task's binding contract)
    - .claude/rules/typescript-comments.md (full file)
  </read_first>
  <behavior>
    - `spawnAndRegister(entry, event, ctx)` resolves AFTER the synchronous spawn + registry-add + PID-table persist completes; resolves BEFORE the child exits. Wall-clock latency from call to resolve is bounded by the file-system write of the PID table (typically <10ms in tests).
    - `spawnAndRegister` constructs `dispatchId = randomUUID()` (UUID v4 from `node:crypto`) for every call; two concurrent calls produce distinct dispatchIds even within the same microtask.
    - The spawned child's `env` includes ALL of `prepareEnv`'s outputs PLUS `PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH=<dispatchId>` — the marker is exposed to the child so the orphan-reap's `/proc/<pid>/environ` check can confirm ownership later.
    - Spawn options are pinned: `{ cwd: env.CLAUDE_PROJECT_DIR, env, stdio: ["pipe", "pipe", "pipe"], shell: <planSpawn.shell>, detached: false }`. The argument shape between `command` and `[...args]` follows the EXEC-04 discriminator from `dispatch-exec.ts:309-336` verbatim.
    - The EXEC-02 timer ladder is installed via `installTimerLadder(child, timeoutMs)` where `timeoutMs = typeof entry.handlerDecl.timeout === "number" ? entry.handlerDecl.timeout : 600_000` (DEFAULT_TIMEOUT_MS).
    - Stdin: `child.stdin.on("error", hookDebugLog-arm)` attached BEFORE `child.stdin.end(stdinJson)` (EPIPE defense; mirrors the sync path).
    - Stderr/stdout: `child.stderr.on("data", buf => stderrBuffer.write(buf))` and `child.stdout.on("data", buf => stdoutBuffer.write(buf))` where `stderrBuffer = new RingBuffer(STDERR_CAP_BYTES)` and `stdoutBuffer = new RingBuffer(STDOUT_CAP_BYTES)`.
    - `child.once("exit", (code, signal) => onChildExit(dispatchId, code, signal, ctx))` and `child.once("error", err => onChildError(dispatchId, err))` registered AFTER stream listeners but BEFORE `child.stdin.end(...)`.
    - The registry add happens BEFORE the PID-table persist returns (so a crash mid-write of the PID table still leaves the in-memory entry; the next `/reload` walks it via `shutdownInMemoryChildren`).
    - `onChildExit(dispatchId, code, signal, ctx)`:
      1. Reads entry via `asyncRewakeRegistry.get(dispatchId)`; if `undefined`, returns (double-fire guard against `error` + `exit` both firing).
      2. Cancels the timer ladder via `entry.ladder.cancel()`.
      3. Removes from registry via `asyncRewakeRegistry.delete(dispatchId)`.
      4. Persists the PID table fire-and-forget via `void persistPidTableForLoc(entry)`.
      5. Captured-epoch defense: `if (entry.capturedEpoch !== currentEpoch()) { hookDebugLog(...); return; }`.
      6. `rewakeSummary` UI surface (independent of exit code; the IL-2 EXEMPTION): `if (entry.rewakeSummary !== undefined && entry.rewakeSummary.length > 0) try { ctx.ui.notify(entry.rewakeSummary, "info") } catch (err) { hookDebugLog(...) }`.
      7. If `code !== 2`: `hookDebugLog("async-rewake: silent completion code=...")`; return (no inject).
      8. Build `body`: `const { text: stderrText, truncated: stderrTrunc } = entry.stderrBuffer.read(); const { text: stdoutText, truncated: stdoutTrunc } = entry.stdoutBuffer.read(); const body = stderrText.length > 0 ? stderrText : stdoutText;`. If `body.length === 0`, `hookDebugLog("async-rewake: exit 2 with empty body — no injection")`; return.
      9. Build `content` via `buildInjectionContent(entry.rewakeMessage, body, truncated)` where `truncated = stderrText.length > 0 ? stderrTrunc : stdoutTrunc`. The function prepends `TRUNCATED_PREFIX = "[…truncated]\n\n"` when truncated is true, then concatenates `rewakeMessage` (when non-empty) + `BODY_SEPARATOR = "\n\n"` + body. When `rewakeMessage` is undefined or empty, content is just the (possibly-truncated-prefixed) body.
      10. Pick lane: `const lane: "nextTurn" | "followUp" = ctx.isIdle() ? "nextTurn" : "followUp"`.
      11. `try { void ctx.sendMessage({ customType: REWAKE_CUSTOM_TYPE, content, display: false, details: { pluginId: entry.pluginId, dispatchId } }, { deliverAs: lane }); } catch (err) { hookDebugLog(...) }`.
    - `onChildError(dispatchId, err)`: `hookDebugLog("async-rewake: child error ..."); entry = asyncRewakeRegistry.get(dispatchId); if (entry) { entry.ladder.cancel(); asyncRewakeRegistry.delete(dispatchId); void persistPidTableForLoc(entry); }`. Does NOT inject; does NOT call notify; `error` is treated as "child failed to even start" — pure cleanup.
    - `shutdownInMemoryChildren(): void` — synchronous; iterates `asyncRewakeRegistry.values()` calling `entry.ladder.cancel()` then `try { entry.child.kill("SIGKILL") } catch {}`, then `asyncRewakeRegistry.clear()`. Idempotent. Plan 03 calls this at factory entry.
    - `reapOrphans(loc): Promise<void>` — reads `readPidTable(loc)`; for each entry, calls `isPidAlive(entry.pid)`; on alive AND `process.platform === "linux"`, calls `readProcEnvironMarker(entry.pid)` and compares against `entry.dispatchId`; on match calls `process.kill(entry.pid, "SIGKILL")` (try/catch around the kill); on mismatch logs and skips; on non-Linux logs "soft-skip" and skips (conservative path); on dead, skips. Finally calls `unlinkPidTable(loc)`. Plan 03 calls this at factory entry.
    - `isPidAlive(pid: number): boolean` — `try { process.kill(pid, 0); return true; } catch (err) { const code = (err as NodeJS.ErrnoException).code; if (code === "ESRCH") return false; if (code === "EPERM") return true; return false; }`. The `EPERM` case (alive-but-not-ours) returns true but `reapOrphans` then routes to the marker check which will FAIL the comparison and soft-skip — net effect: we never kill what we don't own.
    - `readProcEnvironMarker(pid: number): Promise<string | undefined>` — `if (process.platform !== "linux") return undefined; try { const raw = await fs.readFile(`/proc/${pid}/environ`, "utf8"); for (const pair of raw.split("\\0")) { const eq = pair.indexOf("="); if (eq === -1) continue; if (pair.slice(0, eq) === MARKER_ENV) return pair.slice(eq + 1); } return undefined; } catch (err) { hookDebugLog(...); return undefined; }`. Async; called only on Linux.
    - The discriminated `OutcomeKind = "inject" | "silent" | "noop"` arm (NFR-7 + assertNever) is used inside `onChildExit` to pin exhaustiveness — even though only one return path actually calls `ctx.sendMessage`, the arm makes the three possible outcomes explicit and lets the architecture test pin the union shape.
    - All test seams mirror the dispatch-exec.ts pattern: `let activeSpawn: SpawnImpl = spawn; export function _setSpawnForTest(fake) { activeSpawn = fake } export function _resetSpawnForTest() { activeSpawn = spawn }`. Same for `_setOrphanProbesForTest({ killProbe, environReader })` (RESEARCH Open Question #3 recommended), `_setDispatchIdGeneratorForTest(() => "fixed-id")`, and `_getRegistryForTest(): ReadonlyMap<string, AsyncRewakeEntry>`.
    - The registry NEVER calls `console.error` / `process.stderr.write` / `ctx.ui.notify` for diagnostics. The ONLY `ctx.ui.notify` call is the `rewakeSummary` UI surface (the IL-2 EXEMPTION per Phase 62). Every other failure arm routes through `hookDebugLog`.
    - Multi-hook fan-in: two concurrent `spawnAndRegister` calls create two distinct `dispatchId`s and two registry entries; the per-child exit handlers fire independently without coordination.
    - `persistPidTableForLoc(entry)` constructs the persisted `PidTableEntry` from the in-memory `AsyncRewakeEntry`, walks the current `asyncRewakeRegistry.values()` to build the full list (entries are derived from the in-memory map; the PID table is a snapshot), and calls `writePidTable(loc, list)`. The loc is derived from `entry.scope` + `locationsFor` (Plan 02 may import locationsFor from `persistence/locations.ts` — the SCOPES handling is symmetric per Plan 03's wiring).
    - Per Open Question #1: registry.ts does NOT extend the Phase 60 `ensureSharedDataDir` gate; `atomicWriteJson`'s internal `mkdir({ recursive: true })` of `path.dirname(filePath)` covers the cold-start case (verified at `shared/atomic-json.ts:25`). Document this in a code comment.
  </behavior>
  <action>
    Expand `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` from the Task 1 stub to a full ~250-350 LoC module. Preserve the Task 1 file-leading whitelist-comment block verbatim; ADD a second paragraph describing the responsibility boundary (spawn-and-register synchronous step + per-child exit handler + orphan-reap glue + in-memory `Map<dispatchId, AsyncRewakeEntry>`); ADD a third paragraph documenting the IL-2 exemption for `rewakeSummary`. NO Phase/Plan/Wave/Pitfall/Pattern tokens; use decision IDs (D-62-01..05, D-58-01, D-59-03, D-60-01..04) and requirement IDs (HOOK-06, EXEC-05, NFR-1, NFR-2, NFR-7, NFR-10) as the sole traceability anchors per `.claude/rules/typescript-comments.md`.

    **Imports** (4 levels deep: `bridges/hooks/async-rewake/registry.ts` -> `../../../shared/...`, `../exec-timer.ts`, `./ring-buffer.ts`, `./pid-table.ts`):
    - `node:child_process` (spawn already imported in Task 1; ADD `type ChildProcess` if not already aliased)
    - `import { randomUUID } from "node:crypto";`
    - `import { readFile } from "node:fs/promises";`
    - `import { homedir } from "node:os";`
    - `import path from "node:path";`
    - `import { errorMessage } from "../../../shared/errors.ts";`
    - `import { hookDebugLog } from "../../../shared/debug-log.ts";`
    - `import { assertPathInside } from "../../../shared/path-safety.ts";` (for any derived path containment if registry.ts constructs one itself; pid-table.ts already guards its own paths)
    - `import { locationsFor, type ScopedLocations } from "../../../persistence/locations.ts";` (verify exact export names)
    - `import { installTimerLadder, type TimerLadder } from "../exec-timer.ts";` (verify TimerLadder is exported; if it is only a structural type, import `type` accordingly)
    - `import { buildTranslationContext } from "../translation-context.ts";` (plus whatever translator-dispatch helper Phase 60 exposed — search the file for the entry seam; the sync path at dispatch-exec.ts:152-155 uses it the same way)
    - `import { currentEpoch } from "../event-router.ts";`
    - `import { RingBuffer, STDERR_CAP_BYTES, STDOUT_CAP_BYTES } from "./ring-buffer.ts";`
    - `import { readPidTable, writePidTable, unlinkPidTable, type PidTableEntry } from "./pid-table.ts";`
    - `import type { RoutingEntry } from "../event-router.ts";` (verify RoutingEntry is exported there; dispatch-exec.ts imports it from the same source per :63)
    - `import type { ExtensionContext } from "../../../platform/pi-api.ts";`
    - `import type { BucketAEvent } from "../../../domain/components/hook-events.ts";`

    If the EXEC-04 `planSpawn` helper at `dispatch-exec.ts:309-336` is local-private, copy its body into registry.ts as a private `planAsyncSpawn` helper (do NOT extract Phase 60's helper in this plan — minimize churn). Same for the `prepareEnv` helper (lines 275-303): copy it as a private `prepareAsyncEnv` that takes an additional `dispatchId` argument and injects `PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH=dispatchId` into the returned env record AFTER the existing `CLAUDE_*` block. Document the copy with a JSDoc note: "copy of dispatch-exec.ts:prepareEnv plus the asyncRewake marker; planner option B (lockstep-helper-evolution) deferred to v1.14+ if duplication becomes load-bearing".

    For `serializeWithTruncation`: if `wire-protocol.ts` exports it, import from there (cleaner); otherwise duplicate from `dispatch-exec.ts:249-265` with the same JSDoc note. Plan-time: implementer decides at read time.

    Define constants near the top (after imports, before the registry Map):

    ```
    export const MARKER_ENV = "PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH" as const;
    const DEFAULT_TIMEOUT_MS = 600_000;
    const BODY_SEPARATOR = "\n\n";
    const TRUNCATED_PREFIX = "[…truncated]\n\n";
    const REWAKE_CUSTOM_TYPE = "claude-hook-rewake" as const;
    ```

    Define `interface AsyncRewakeEntry` with the readonly fields from RESEARCH lines 431-447 verbatim: `dispatchId`, `pid`, `scope: "user" | "project"`, `marketplace: string`, `pluginId: string`, `claudeEvent: BucketAEvent`, `spawnedAt: string` (ISO), `rewakeMessage: string | undefined`, `rewakeSummary: string | undefined`, `child: ChildProcess`, `ladder: TimerLadder`, `stdoutBuffer: RingBuffer`, `stderrBuffer: RingBuffer`, `capturedEpoch: number`.

    Module-private state:
    ```
    const asyncRewakeRegistry = new Map<string, AsyncRewakeEntry>();
    type SpawnImpl = typeof spawn;
    let activeSpawn: SpawnImpl = spawn;
    let dispatchIdGenerator: () => string = () => randomUUID();
    let orphanProbes: {
      readonly killProbe: (pid: number, sig: number | NodeJS.Signals) => void;
      readonly environReader: (pid: number) => Promise<string>;
    } = {
      killProbe: (pid, sig) => process.kill(pid, sig),
      environReader: (pid) => readFile(`/proc/${pid}/environ`, "utf8"),
    };
    ```

    Implement `spawnAndRegister`, `onChildExit`, `onChildError`, `shutdownInMemoryChildren`, `reapOrphans`, `isPidAlive`, `readProcEnvironMarker`, `buildInjectionContent`, and `persistPidTableForLoc` per the behavior contract above. Reference the verbatim sketches in RESEARCH.md lines 466-565, 700-790, 800-890, 920-960. Where the sketches use `pi.sendMessage` vs `ctx.sendMessage`, use `ctx.sendMessage` (the closure already carries `ctx`; no `pi` available — RESEARCH lines 786-794 confirm).

    Add an `OutcomeKind = "inject" | "silent" | "noop"` discriminator inside `onChildExit` (the three branches before the final dispatch: silent on captured-epoch mismatch, silent on code !== 2, inject on code === 2 with body), and pin the arm with `assertNever(outcome)` at the bottom even though only the inject arm actually calls `ctx.sendMessage`. NFR-7 exhaustiveness.

    Test seams (mirror `dispatch-exec.ts:116-131`):
    ```
    export function _setSpawnForTest(fake: SpawnImpl): void { activeSpawn = fake; }
    export function _resetSpawnForTest(): void { activeSpawn = spawn; }
    export function _setDispatchIdGeneratorForTest(gen: () => string): void { dispatchIdGenerator = gen; }
    export function _resetDispatchIdGeneratorForTest(): void { dispatchIdGenerator = () => randomUUID(); }
    export function _setOrphanProbesForTest(probes: { killProbe; environReader }): void { orphanProbes = probes; }
    export function _resetOrphanProbesForTest(): void { orphanProbes = { killProbe: (pid, sig) => process.kill(pid, sig), environReader: (pid) => readFile(`/proc/${pid}/environ`, "utf8") }; }
    export function _getRegistryForTest(): ReadonlyMap<string, AsyncRewakeEntry> { return asyncRewakeRegistry; }
    ```

    Use the 74-dash-rule section dividers between major code blocks: (1) imports + constants + types, (2) module state + test seams, (3) public spawnAndRegister, (4) private exit/error handlers + buildInjectionContent, (5) public shutdownInMemoryChildren + reapOrphans, (6) private isPidAlive + readProcEnvironMarker + persistPidTableForLoc + planAsyncSpawn + prepareAsyncEnv.

    Wrap `spawnAndRegister`'s synchronous body in a try/catch at the outer level: on throw, `hookDebugLog("async-rewake: spawnAndRegister threw ...")` and return without registering anything (fire-and-forget; the reducer already received `{kind:"noop"}` from `dispatchHookExec` per Plan 03's delegation arm — so a throw here is silently absorbed). Inner steps (spawn, registry add, PID-table write) each have their own try/catch with `hookDebugLog` arms.

    Run `npx tsc --noEmit` after writing. Run `npm run check`; the existing Phase 57-61 suites must continue passing; the new file has no test yet (Plan 03 adds the architecture test).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E 'async-rewake/registry' || (npm run check 2>&amp;1 | tail -20)</automated>
  </verify>
  <done>
    `registry.ts` exists at the target path with `spawnAndRegister`, `shutdownInMemoryChildren`, `reapOrphans`, `AsyncRewakeEntry`, `MARKER_ENV`, and all 7 test seams (`_setSpawnForTest`, `_resetSpawnForTest`, `_setDispatchIdGeneratorForTest`, `_resetDispatchIdGeneratorForTest`, `_setOrphanProbesForTest`, `_resetOrphanProbesForTest`, `_getRegistryForTest`) exported; `npx tsc --noEmit` GREEN; `npm run check` GREEN (existing tests pass; no test added for registry yet — Plan 03 adds it); `grep -c 'hookDebugLog' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` returns at least 8 (one per failure arm: spawn throw, child error, sendMessage throw, ui.notify throw, captured-epoch mismatch, silent completion, ring-buffer empty-body, marker-mismatch, soft-skip — at least 8 of these documented); `grep -c 'ctx.ui.notify' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` returns exactly 1 (the IL-2 EXEMPTION for rewakeSummary); `grep -c 'console.error\|process.stderr.write' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` returns 0; `grep -c 'detached: false' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` returns at least 1; `grep -nE 'Phase [0-9]+|Pitfall [0-9]+|Pattern [0-9]+|Plan [0-9]+-[0-9]+|Wave [0-9]+|Task [0-9]+' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts | grep -v '^#'` returns no matches.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin author -> `command` + `args` fields on the hook handler | Untrusted shell text crossing into `spawn`; treated as plugin-author-controlled per the existing Phase 60 trust model (LIFE-03 in Phase 63 handles symlink-escape protection at install time). Plan 02 inherits the same trust model with no new input source. |
| in-memory `Map<dispatchId, AsyncRewakeEntry>` -> Plan 03 wirings | Internal data; only `_getRegistryForTest` exposes it; production callers consume only `spawnAndRegister`, `shutdownInMemoryChildren`, `reapOrphans` (public seam). |
| `process.kill(pid, "SIGKILL")` on a potentially-recycled PID | OS race window between `kill(pid, 0)` liveness probe and `kill(pid, "SIGKILL")` actual reap; mitigated by the `/proc/<pid>/environ` marker check on Linux + soft-skip on macOS (NEVER kill a stranger). |
| child process -> Pi model context via `ctx.sendMessage` | Untrusted child stderr/stdout content travels into the LLM context window; `display: false` ensures user-invisible / model-visible (matches `<system-reminder>` semantic per peer-dep `messages.d.ts:32-39`); the bridge consents to model-only injection on behalf of the operator (installing the plugin IS the trust grant). |
| `rewakeSummary` -> `ctx.ui.notify` | The single sanctioned IL-2 EXEMPTION runtime notify; documented in the file-leading comment block + pinned by Plan 03's architecture test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | `spawn(command, args, ...)` from `registry.ts` | accept | `command` + `args` come from the plugin manifest, not from runtime user input. Phase 60's EXEC-04 trust model applies unchanged. The async branch does NOT introduce a new untrusted input source; it shares the same source as the sync branch. |
| T-62-02 | Spoofing / model-context injection | `ctx.sendMessage({ display: false, content: <stderr> }, ...)` on exit code 2 | mitigate | `display: false` matches Claude Code's `<system-reminder>` semantic (user-invisible / model-visible); the operator is the consenting party (installing the plugin IS the trust grant). Content is the child's own stderr/stdout — the plugin's product. Architecture test in Plan 03 pins `display === false` byte-equality on the injected payload. |
| T-62-03 | Tampering | Declaration-order interleave (manifest controls order) | accept | The reducer's `{kind: "noop"}` return from the async branch preserves D-60-02 first-block-wins / mutate-compose / stop semantics. No new attack surface — the order is plugin-author-controlled in the same way the sync sites are. |
| T-62-04 | DoS | Ring-buffer overflow from a noisy child | mitigate | RingBuffer caps at 64KB stderr / 1MB stdout (Plan 01's leaf); cannot OOM the parent. `_truncated` latch surfaces as the `[…truncated]` prefix on injected content so the model knows the trigger frame is not the full picture. |
| T-62-05 | Tampering | PID-table corruption / race | mitigate | NFR-1 atomic-write via `atomicWriteJson` (Plan 01's leaf). Marker env var `PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH=<dispatchId>` checked against `/proc/<pid>/environ` BEFORE SIGKILL — prevents the bridge from killing a stranger process if a PID was recycled between writes. |
| T-62-08 | Atomic supersession of architecture invariant | `tests/architecture/no-shell-out.test.ts` whitelist 2 -> 3 | mitigate | Task 1 lands the whitelist amendment AND the first `import "node:child_process"` line in the SAME commit (D-58-01 lesson). The pre-commit hook + the planner gate on this convention. |
| T-62-09 | IL-2 exemption | `ctx.ui.notify(rewakeSummary, "info")` from `onChildExit` | document | This is the single sanctioned runtime notify call from `bridges/hooks/async-rewake/`. Documented in the file-leading comment block + pinned by Plan 03's architecture test as the ONLY allowed `ctx.ui.notify` call from `async-rewake/`. Severity is `"info"` per `shared/notify.ts` Severity closed set (verified `"info" | "warning" | "error"`). |
| T-62-10 | Multi-hook fan-in correctness | Two concurrent `spawnAndRegister` calls | mitigate | `randomUUID()` per call; the dispatchId is collision-free and never derived from `Date.now()` + handler index. Two concurrent calls create two independent registry entries; per-child exit handlers fire independently without coordination. |
| T-62-SC | Tampering | npm / pip / cargo installs | mitigate | Zero new packages installed in Plan 02. RESEARCH.md § "Package Legitimacy Audit" confirms `node:child_process`, `node:crypto`, `node:fs/promises`, `node:os`, `node:path` are all built-ins; `write-file-atomic` and `typebox` are already project deps reused via Plan 01's leaves. No `[ASSUMED]` / `[SUS]` checkpoints needed. |

The captured-epoch defense (T-62-07) and orphan-reap (T-62-06) threats
have full mitigations in this plan; Plan 03's architecture test pins the
captured-epoch comparison + the orphan-reap marker check.

</threat_model>

<verification>
- `npx tsc --noEmit` GREEN: `registry.ts` typechecks against Wave-1 leaves and Phase 60 reuse points without changes to Phase 60 source.
- `node --test tests/architecture/no-shell-out.test.ts` GREEN: 2/2 pass with the new 3-element whitelist (the `whitelist: exactly three files` test passes; the introspection that walks the codebase recognizes `bridges/hooks/async-rewake/registry.ts` as a sanctioned `node:child_process` consumer).
- `npm run check` GREEN end-to-end (Phase 57 / 58 / 59 / 60 / 61 suites continue passing; no test added for registry yet — Plan 03 adds it).
- Atomic-supersession audit: `git log -1 --name-only HEAD` (after the Task 1 commit) shows BOTH `tests/architecture/no-shell-out.test.ts` AND `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` in the same commit. Same-commit landing is the D-58-01 contract.
- Comment-policy compliance: `grep -nE 'Phase [0-9]+|Pitfall [0-9]+|Pattern [0-9]+|Plan [0-9]+-[0-9]+|Wave [0-9]+|Task [0-9]+' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts tests/architecture/no-shell-out.test.ts | grep -v '^#'` returns no matches.
- `ctx.ui.notify` discipline: `grep -c 'ctx.ui.notify' extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` is exactly 1 (the IL-2 EXEMPTION for `rewakeSummary`). `console.error` / `process.stderr.write` count is 0.
- `detached: false` is the spawn-options value (NEVER `detached: true` — that breaks `/reload` SIGKILL).
- The `MARKER_ENV` constant equals `"PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH"` byte-for-byte.
- The discriminated `OutcomeKind` arm with `assertNever` is present in `onChildExit` (NFR-7).
- Plan 03's wiring import surface is exactly: `spawnAndRegister`, `shutdownInMemoryChildren`, `reapOrphans` (3 public symbols + the test seams).
</verification>

<success_criteria>
- 1 new file: `extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts` (~250-350 LoC).
- 1 modified file: `tests/architecture/no-shell-out.test.ts` (3 surgical edits: whitelist set 2->3, sibling assertion title + expected array, docstring header third paragraph).
- Atomic-supersession respected (D-58-01): the two file edits land in ONE commit.
- Plan 03 can import `{ spawnAndRegister, shutdownInMemoryChildren, reapOrphans }` from `./async-rewake/registry.ts` to wire `dispatch-exec.ts` and `event-router.ts` without any further symbol additions in `registry.ts`.
- Plan 03's architecture test (Wave 0 dependency for HOOK-06 + EXEC-05) can use the test seams (`_setSpawnForTest`, `_setDispatchIdGeneratorForTest`, `_setOrphanProbesForTest`, `_getRegistryForTest`) to mock the spawn surface deterministically.
- Zero new runtime dependencies (every primitive is a built-in or an existing project dep via the Wave-1 leaves).
- IL-2 EXEMPTION documented in the file-leading comment block (the single `ctx.ui.notify(rewakeSummary, "info")` call site).
- NFR-7 exhaustiveness preserved (`OutcomeKind` discriminator + `assertNever` arm in `onChildExit`).
- NFR-10 containment: every derived path is guarded (the PID-table guard is in Plan 01's `pid-table.ts`; any path constructed in `registry.ts` itself is guarded inline).
- Comment policy enforced: no `Phase NN` / `Plan NN-NN` / `Wave N` / `Pitfall N` / `Pattern N` / `Task N` tokens.
- `npm run check` GREEN.
</success_criteria>

<output>
Create `.planning/phases/62-asyncrewake-registry-background-spawn/62-02-SUMMARY.md` when done.
</output>
