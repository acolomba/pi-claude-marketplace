---
phase: 93-substitution-completion
plan: 02
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - extensions/pi-claude-marketplace/bridges/commands/stage.ts
  - extensions/pi-claude-marketplace/bridges/commands/types.ts
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/bridges/agents/types.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - tests/bridges/commands/stage.test.ts
  - tests/bridges/agents/convert.test.ts
  - tests/orchestrators/plugin/install.test.ts
autonomous: true
requirements: [SUB-02]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A project-scope command whose content contains ${CLAUDE_PROJECT_DIR} materializes with that token replaced by the install cwd (SUB-02, commands arm)."
    - "A project-scope agent whose body contains ${CLAUDE_PROJECT_DIR} materializes with that token replaced by the install cwd (SUB-02, agents arm)."
    - "A command or agent containing ${CLAUDE_SKILL_DIR} keeps it literal in both scopes (skill-scoped variable — not fed to commands/agents)."
    - "User-scope commands/agents keep ${CLAUDE_PROJECT_DIR} literal."
    - "After orchestrator threading, a project-scope install/reinstall/update delivers projectDir into staged skill/command/agent content end-to-end."
  artifacts:
    - "extensions/pi-claude-marketplace/bridges/commands/stage.ts — projectDir at the substitute call"
    - "extensions/pi-claude-marketplace/bridges/commands/types.ts — StageCommandsInput.cwd"
    - "extensions/pi-claude-marketplace/bridges/agents/convert.ts — projectDir consumed at the substitute call"
    - "extensions/pi-claude-marketplace/bridges/agents/stage.ts — computes scope-gated projectDir, threads it into convertAgent"
    - "extensions/pi-claude-marketplace/bridges/agents/types.ts — StageAgentsInput.cwd"
    - "install.ts / reinstall.ts / update.ts — cwd threaded at all skills/commands/agents stage call sites"
    - "tests/orchestrators/plugin/install.test.ts — end-to-end project-scope delivery assertion"
  key_links:
    - "projectDir = (locations.scope === project) ? cwd : undefined at every non-skill call site"
    - "convertAgent gains projectDir?; agents/stage.ts is the only place scope+cwd meet to compute it before convertAgent runs"
    - "orchestrator cwd sources: install.ts c.cwd, reinstall.ts input.cwd, update.ts args.cwd — mirror the Phase 92 MCP threading in the same functions"
  assumptions:
    - "FLAGGED (edge classification unresolved for SUB-02): commands and agents receive NO skillDir — ${CLAUDE_SKILL_DIR} stays literal there because the variable is skill-scoped upstream. Surfaced for reviewer."
    - "FLAGGED (edge classification unresolved for SUB-02): projectDir for commands/agents is the install cwd, scope-gated to project scope only. Surfaced for reviewer."
  prohibitions:
    - "${CLAUDE_SKILL_DIR} MUST NOT be substituted in command or agent content."
    - "User-scope ${CLAUDE_PROJECT_DIR} MUST NOT be substituted in commands or agents."
    - "An absent projectDir MUST NOT substitute an empty string."
    - "Threading cwd MUST NOT change command/agent behavior for content without ${CLAUDE_PROJECT_DIR}."
    - "The marketplace autoupdate cascade MUST continue to work — cwd is threaded from the same context the cascade already carries."
---

<objective>
Complete SUB-02 across the two remaining substitution surfaces (commands, agents)
and deliver project-scope `${CLAUDE_PROJECT_DIR}` in production by threading the
install `cwd` from all three orchestrators to every skills/commands/agents stage
call site. Builds on the substitution architecture proven by plan 93-01.

Purpose: 93-01 extended the shared helper and the skills bridge but did not touch
the orchestrators, so projectDir is inert in production until cwd is threaded.
This plan wires the commands and agents call sites to feed scope-gated projectDir
and threads cwd end-to-end, then proves delivery with an orchestrator-level test.

Output: commands/agents bridges feeding projectDir; `cwd` on StageCommandsInput /
StageAgentsInput; cwd threaded at all nine stage-input construction sites across
install/reinstall/update; per-bridge scope-arm tests; an end-to-end delivery test.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-substitution-completion/93-CONTEXT.md
@.claude/rules/typescript-comments.md

@extensions/pi-claude-marketplace/shared/vars.ts
@extensions/pi-claude-marketplace/bridges/commands/stage.ts
@extensions/pi-claude-marketplace/bridges/commands/types.ts
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@extensions/pi-claude-marketplace/bridges/agents/stage.ts
@extensions/pi-claude-marketplace/bridges/agents/types.ts
@extensions/pi-claude-marketplace/bridges/mcp/stage.ts
@tests/bridges/commands/stage.test.ts
@tests/bridges/agents/convert.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commands bridge — scope-gated projectDir</name>
  <files>extensions/pi-claude-marketplace/bridges/commands/stage.ts, extensions/pi-claude-marketplace/bridges/commands/types.ts, tests/bridges/commands/stage.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/commands/stage.ts (prepareStageCommands :168, its destructure :171, the substituteClaudeVars call :240, the per-command loop)
    - extensions/pi-claude-marketplace/bridges/commands/types.ts (StageCommandsInput)
    - extensions/pi-claude-marketplace/shared/vars.ts (extended ClaudePluginVars from 93-01)
    - tests/bridges/commands/stage.test.ts (fixture + scope construction)
  </read_first>
  <behavior>
    - A project-scope command body containing ${CLAUDE_PROJECT_DIR} materializes with cwd substituted.
    - A user-scope command body containing ${CLAUDE_PROJECT_DIR} keeps it literal.
    - A command body containing ${CLAUDE_SKILL_DIR} keeps it literal in both scopes (no skillDir supplied).
    - ${CLAUDE_PLUGIN_ROOT} / ${CLAUDE_PLUGIN_DATA} substitution is unchanged.
  </behavior>
  <action>
    In bridges/commands/types.ts add `readonly cwd?: string` to StageCommandsInput
    with the same doc comment shape as StageSkillsInput.cwd (project-scope install
    cwd for ${CLAUDE_PROJECT_DIR}; optional). In bridges/commands/stage.ts
    destructure `cwd` from input in prepareStageCommands and extend the
    substituteClaudeVars call (:240) to pass
    `{ pluginRoot, pluginData: pluginDataDir, projectDir: locations.scope === "project" ? cwd : undefined }`.
    Do NOT pass skillDir — commands are not skill-scoped, so ${CLAUDE_SKILL_DIR}
    must stay literal via the helper's pass-through. Cite SUB-02; no phase/plan
    references in comments per .claude/rules/typescript-comments.md.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/commands/stage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Project-scope staged command file: `${CLAUDE_PROJECT_DIR}` replaced by cwd.
    - User-scope staged command file: literal `${CLAUDE_PROJECT_DIR}` retained.
    - Both scopes: literal `${CLAUDE_SKILL_DIR}` retained in a command that references it.
    - `node --test tests/bridges/commands/stage.test.ts` exits 0.
  </acceptance_criteria>
  <done>StageCommandsInput carries optional cwd; the command substitute call feeds scope-gated projectDir and never skillDir; both scope arms tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agents bridge — scope-gated projectDir threaded through convertAgent</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, extensions/pi-claude-marketplace/bridges/agents/types.ts, tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (convertAgent input object :434, the substituteClaudeVars body call :530)
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts (prepareStagePluginAgents :97, its destructure :101, the convertAgent call :135, where locations/scope are available)
    - extensions/pi-claude-marketplace/bridges/agents/types.ts (StageAgentsInput)
    - tests/bridges/agents/convert.test.ts (fixture + how convertAgent input is built)
  </read_first>
  <behavior>
    - convertAgent with projectDir set substitutes ${CLAUDE_PROJECT_DIR} in the agent body.
    - convertAgent with projectDir omitted leaves ${CLAUDE_PROJECT_DIR} literal.
    - An agent body containing ${CLAUDE_SKILL_DIR} keeps it literal (no skillDir supplied).
    - Existing agent conversion (tools, model, frontmatter) is unchanged.
  </behavior>
  <action>
    In bridges/agents/convert.ts add an optional `projectDir?: string` field to the
    convertAgent input object type, destructure it, and pass it into the
    substituteClaudeVars body call (:530) as
    `{ pluginRoot, pluginData: pluginDataDir, projectDir }` (no skillDir). In
    bridges/agents/types.ts add `readonly cwd?: string` to StageAgentsInput. In
    bridges/agents/stage.ts destructure `cwd` from input and compute
    `const projectDir = locations.scope === "project" ? cwd : undefined;` once,
    then pass `projectDir` into each convertAgent call (:135) — agents/stage.ts is
    the sole point where scope and cwd meet before convertAgent runs. Cite SUB-02;
    no phase/plan references in comments or test titles.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/agents/convert.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - convertAgent given projectDir substitutes `${CLAUDE_PROJECT_DIR}` in the converted body.
    - convertAgent without projectDir retains the literal `${CLAUDE_PROJECT_DIR}`.
    - A converted agent referencing `${CLAUDE_SKILL_DIR}` retains it literal.
    - `node --test tests/bridges/agents/convert.test.ts` exits 0.
  </acceptance_criteria>
  <done>convertAgent accepts optional projectDir; agents/stage.ts computes scope-gated projectDir and threads it; StageAgentsInput carries optional cwd; scope arms tested.</done>
</task>

<task type="auto">
  <name>Task 3: Thread cwd from all three orchestrators; prove end-to-end delivery</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, tests/orchestrators/plugin/install.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (InstallCtx.cwd :282; skills call :897; commands call :936; agents prepareStagePluginAgents call :972; how Phase 92 threaded cwd to MCP in this file)
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts (input.cwd :1490; skills :1500; commands :1509; agents :1518; existing `cwd: input.cwd` MCP threading :1530)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (args.cwd :208/:652; skills :1139; commands :1148; agents call)
    - tests/orchestrators/plugin/install.test.ts (existing install orchestrator test harness + scope setup)
  </read_first>
  <action>
    Thread `cwd` into every skills/commands/agents stage-input construction across
    the three orchestrators, mirroring the Phase 92 MCP `cwd:` threading already
    present in the same functions. install.ts: add `cwd: c.cwd` to the
    prepareStageSkills (:897), prepareStageCommands (:936), and
    prepareStagePluginAgents (:972) input objects. reinstall.ts: add
    `cwd: input.cwd` to the skills (:1500), commands (:1509), and agents (:1518)
    input objects. update.ts: add `cwd: args.cwd` to the skills (:1139), commands
    (:1148), and agents input objects. Add an orchestrator-level test to
    tests/orchestrators/plugin/install.test.ts that runs a project-scope install of
    a fixture plugin whose skill/command/agent content contains
    `${CLAUDE_PROJECT_DIR}` and asserts the materialized files carry the install
    cwd (closing the optional-cwd silent-miss risk that a compiler cannot catch).
    Cite SUB-02; no phase/plan references in comments or test titles.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/orchestrators/plugin/install.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - All nine stage-input constructions (3 bridges × 3 orchestrators) pass `cwd`.
    - A project-scope install materializes skill/command/agent files with `${CLAUDE_PROJECT_DIR}` replaced by the install cwd (end-to-end).
    - A user-scope install leaves `${CLAUDE_PROJECT_DIR}` literal in the same files.
    - `npm run check` is green.
  </acceptance_criteria>
  <done>cwd flows from every orchestrator to every substitution call site; a project-scope install proves projectDir delivery end-to-end; full check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin author → materialized command/agent content | Plugin-supplied command/agent files carry `${CLAUDE_*}` tokens; projectDir = install cwd is project-controlled. |
| orchestrator → stage inputs | cwd flows from install/reinstall/update contexts; a dropped thread silently disables projectDir for that path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-04 | Tampering | commands/agents stage | medium | mitigate | Scope-gated projectDir (`scope === project ? cwd : undefined`); user-scope and absent-cwd paths pass through via the helper — no empty-string substitution, no user-scope leak. Per-bridge scope-arm tests. |
| T-93-05 | Tampering / correctness | orchestrator cwd threading | medium | mitigate | Optional cwd cannot be compiler-enforced across all nine sites, so an end-to-end project-scope install test asserts real delivery — closing the silent-miss gap. |
| T-93-06 | Elevation / containment | stage write paths | low | accept | Substitution alters content only; command/agent write destinations remain assertPathInside-guarded (NFR-10); no path is composed from projectDir. |
| T-93-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs in this phase — supply-chain row not applicable. |
</threat_model>

<verification>
- `node --test tests/bridges/commands/stage.test.ts tests/bridges/agents/convert.test.ts tests/orchestrators/plugin/install.test.ts` exits 0.
- `npm run check` green (typecheck + lint + format + tests + integration).
</verification>

<success_criteria>
- SUB-02 fully delivered: project-scope skills, commands, and agents substitute `${CLAUDE_PROJECT_DIR}` to the install cwd; user-scope stays literal.
- `${CLAUDE_SKILL_DIR}` stays literal in commands and agents (skill-scoped).
- End-to-end delivery proven at the orchestrator level, not just the bridge unit level.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 93-02)

**New threading fields:**
- `StageCommandsInput.cwd?: string` (bridges/commands/types.ts).
- `StageAgentsInput.cwd?: string` (bridges/agents/types.ts).
- `convertAgent` input gains `projectDir?: string` (bridges/agents/convert.ts).

**Bridge wiring:**
- Commands substitute call feeds `{ pluginRoot, pluginData, projectDir: scope === "project" ? cwd : undefined }` (no skillDir).
- `agents/stage.ts` computes `projectDir = scope === "project" ? cwd : undefined` and threads it into each `convertAgent` call.

**Orchestrator threading (9 sites):**
- `install.ts` adds `cwd: c.cwd` to prepareStageSkills / prepareStageCommands / prepareStagePluginAgents inputs.
- `reinstall.ts` adds `cwd: input.cwd` to the same three.
- `update.ts` adds `cwd: args.cwd` to the same three.

**New test names (requirement-ID titled, no phase refs):**
- `tests/bridges/commands/stage.test.ts`: SUB-02 project-scope projectDir; user-scope literal; `${CLAUDE_SKILL_DIR}` literal both scopes.
- `tests/bridges/agents/convert.test.ts`: SUB-02 projectDir present/absent; `${CLAUDE_SKILL_DIR}` literal.
- `tests/orchestrators/plugin/install.test.ts`: SUB-02 end-to-end project-scope install delivers cwd into materialized skill/command/agent files; user-scope literal.
</artifacts_produced>

<output>
Create `.planning/phases/93-substitution-completion/93-02-SUMMARY.md` when done.
</output>
