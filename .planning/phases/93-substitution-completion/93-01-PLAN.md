---
phase: 93-substitution-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/vars.ts
  - extensions/pi-claude-marketplace/bridges/skills/stage.ts
  - extensions/pi-claude-marketplace/bridges/skills/types.ts
  - tests/bridges/skills/stage.test.ts
  - tests/shared/vars.test.ts
autonomous: true
requirements: [SUB-01, SUB-02]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A project-scope skill whose content contains ${CLAUDE_SKILL_DIR} materializes with that token replaced by the skill's installed directory (SUB-01)."
    - "A project-scope skill whose content contains ${CLAUDE_PROJECT_DIR} materializes with that token replaced by the install cwd (SUB-02, skills arm)."
    - "The same skill staged under user scope keeps ${CLAUDE_PROJECT_DIR} literal in its materialized content (SUB-02 divergence)."
    - "${CLAUDE_PLUGIN_ROOT} and ${CLAUDE_PLUGIN_DATA} continue to substitute byte-identically for content that contains no embedded placeholder literal."
    - "A substitution value that itself contains a ${CLAUDE_*} literal is inserted verbatim and never re-expanded (T-03-01)."
  artifacts:
    - "extensions/pi-claude-marketplace/shared/vars.ts — extended ClaudePluginVars + single-pass substituteClaudeVars"
    - "extensions/pi-claude-marketplace/bridges/skills/stage.ts — both call sites pass skillDir + scope-gated projectDir"
    - "extensions/pi-claude-marketplace/bridges/skills/types.ts — StageSkillsInput.cwd"
    - "tests/shared/vars.test.ts — helper-contract assertions"
    - "tests/bridges/skills/stage.test.ts — end-to-end skills substitution assertions"
  key_links:
    - "skillDir value = path.join(locations.skillsTargetDir, skill.generatedName) — identical to the staged targetDir"
    - "projectDir value = (locations.scope === project) ? input.cwd : undefined — undefined means pass-through, never empty string"
    - "substituteClaudeVars single-pass alternation replacer — the sole guarantor of the T-03-01 no-re-expansion property for the four-variable set"
  assumptions:
    - "FLAGGED (edge classification unresolved for SUB-01): ${CLAUDE_SKILL_DIR} is treated as skill-scoped — it resolves ONLY in skill content, to the skill's installed dir; commands/agents never receive a skillDir value. Surfaced for reviewer; derived from the requirement text + upstream variable scoping."
    - "FLAGGED (edge classification unresolved for SUB-02): ${CLAUDE_PROJECT_DIR} resolves to the install cwd (the project root), NOT scopeRoot (which is <cwd>/.pi); user-scope occurrences pass through untouched. Surfaced for reviewer; derived from the requirement text + the hook lane's CLAUDE_PROJECT_DIR: transCtx.cwd precedent."
  prohibitions:
    - "An absent skillDir or projectDir MUST NOT substitute an empty string."
    - "User-scope ${CLAUDE_PROJECT_DIR} MUST NOT be substituted."
    - "A substitution value MUST NOT be re-expanded by a later pass."
    - "An unknown ${...} token MUST NOT be altered."
    - "${CLAUDE_PLUGIN_ROOT} / ${CLAUDE_PLUGIN_DATA} substitution behavior MUST NOT change for content without embedded placeholder literals."
---

<objective>
Extend the shared substitution primitive `shared/vars.ts::substituteClaudeVars` to
resolve `${CLAUDE_SKILL_DIR}` (SUB-01) and project-scope `${CLAUDE_PROJECT_DIR}`
(SUB-02) alongside the existing `${CLAUDE_PLUGIN_ROOT}` / `${CLAUDE_PLUGIN_DATA}`
set, and wire both skills-bridge call sites to feed the two new values. This is
the phase's vertical tracer: it proves the substitution architecture end-to-end
on the skills surface — the only surface that consumes BOTH new variables.

Purpose: SUB-01 is delivered in full here (the skill's installed dir derives
from `locations`, needing no orchestrator change); the SUB-02 skills arm is
delivered structurally (bridge computes projectDir), with production delivery
through the orchestrators landing in plan 93-02.

Output: an extended, single-pass `substituteClaudeVars`; both skills call sites
feeding skillDir + scope-gated projectDir; a `cwd` field on `StageSkillsInput`;
end-to-end skills tests and helper-contract unit tests.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-substitution-completion/93-CONTEXT.md
@.claude/rules/typescript-comments.md

@extensions/pi-claude-marketplace/shared/vars.ts
@extensions/pi-claude-marketplace/bridges/skills/stage.ts
@extensions/pi-claude-marketplace/bridges/skills/types.ts
@tests/shared/vars.test.ts
@tests/bridges/skills/stage.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extend substituteClaudeVars (single-pass) and wire the skills bridge end-to-end</name>
  <files>extensions/pi-claude-marketplace/shared/vars.ts, extensions/pi-claude-marketplace/bridges/skills/stage.ts, extensions/pi-claude-marketplace/bridges/skills/types.ts, tests/bridges/skills/stage.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/vars.ts (current chained-replaceAll implementation + ClaudePluginVars interface)
    - extensions/pi-claude-marketplace/bridges/skills/stage.ts (augmentSkillDescription signature :152 and its ${...} call :168; the whole-file call :298; the per-skill loop where skill.generatedName and locations.skillsTargetDir are in scope; targetDir computation :244)
    - extensions/pi-claude-marketplace/bridges/skills/types.ts (StageSkillsInput interface)
    - extensions/pi-claude-marketplace/bridges/mcp/types.ts (Phase 92 precedent: StageMcpInput.cwd shape and doc comment)
    - tests/bridges/skills/stage.test.ts (existing fixtures + how a project/user scope StageSkillsInput is constructed)
  </read_first>
  <behavior>
    - substituteClaudeVars with all four values present replaces each of the four tokens with its value.
    - substituteClaudeVars with skillDir omitted leaves every ${CLAUDE_SKILL_DIR} literal untouched (no empty string).
    - substituteClaudeVars with projectDir omitted leaves every ${CLAUDE_PROJECT_DIR} literal untouched (no empty string).
    - A pluginRoot value of "/a/${CLAUDE_PLUGIN_DATA}/b" plus a distinct pluginData value: the injected ${CLAUDE_PLUGIN_DATA} inside the substituted pluginRoot stays literal in the output (T-03-01 no re-expansion).
    - prepareStageSkills on a project-scope input (cwd supplied) staging a skill whose body contains all four tokens: the materialized staged file has all four substituted (skillDir = <skillsTargetDir>/<generatedName>, projectDir = cwd).
    - prepareStageSkills on a user-scope input staging the same skill: ${CLAUDE_PROJECT_DIR} remains literal; ${CLAUDE_SKILL_DIR}, ${CLAUDE_PLUGIN_ROOT}, ${CLAUDE_PLUGIN_DATA} still substitute.
  </behavior>
  <action>
    In shared/vars.ts, extend `ClaudePluginVars` with two optional readonly fields
    `skillDir?: string` and `projectDir?: string` (keep `pluginRoot` / `pluginData`
    required). Reimplement `substituteClaudeVars` as a SINGLE-PASS alternation
    replace: match `/\$\{(CLAUDE_PLUGIN_ROOT|CLAUDE_PLUGIN_DATA|CLAUDE_SKILL_DIR|CLAUDE_PROJECT_DIR)\}/g`
    and, in the replacer function, map the captured name to its value
    (PLUGIN_ROOT→pluginRoot, PLUGIN_DATA→pluginData, SKILL_DIR→skillDir,
    PROJECT_DIR→projectDir); when the mapped value is `undefined`, return the
    matched literal unchanged. This structurally guarantees three properties in
    one construct: absent field → literal pass-through (never empty string);
    unknown ${...} → not in the alternation → untouched; and T-03-01 — the scan
    runs left-to-right once and inserted values are never re-scanned, so a value
    containing a placeholder literal cannot re-expand (the prior chained-replaceAll
    could re-expand across passes; this closes that latent gap). Rewrite the module
    header/JSDoc to describe the single-pass behavior accurately; cite SUB-01,
    SUB-02, T-03-01, NFR-1, NFR-10 as anchors; per .claude/rules/typescript-comments.md
    use NO phase/plan/wave references.

    In bridges/skills/stage.ts, add optional `cwd?: string` handling: destructure
    `cwd` from input in prepareStageSkills (mirror StageMcpInput.cwd from Phase 92).
    Inside the per-skill loop, build one vars object of type ClaudePluginVars:
    `{ pluginRoot, pluginData: pluginDataDir, skillDir: path.join(locations.skillsTargetDir, skill.generatedName), projectDir: locations.scope === "project" ? cwd : undefined }`.
    Reuse the already-computed `targetDir` (:244/:245, `path.join(locations.skillsTargetDir, skill.generatedName)`) as skillDir rather than recomputing — it is the same path and already assertPathInside-guarded. Pass this vars object to BOTH the augmentSkillDescription call (extend that helper's `vars` parameter type at :152 from `{ pluginRoot; pluginData }` to `ClaudePluginVars` so its internal substituteClaudeVars call at :168 forwards the new fields) and the whole-file substituteClaudeVars call (:298, replacing the inline `{ pluginRoot, pluginData: pluginDataDir }`). Do not substitute skillDir/projectDir anywhere the value is absent — the undefined→pass-through happens structurally in the helper.

    In bridges/skills/types.ts, add `readonly cwd?: string` to StageSkillsInput with
    a doc comment: project-scope install cwd, substituted for ${CLAUDE_PROJECT_DIR}
    (SUB-02); optional so user-scope and un-threaded callers pass through. Keep it
    optional (this phase's cwd feeds substitution only, unlike Phase 92's required
    StageMcpInput.cwd which also drove the MC-4 collision scan).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/skills/stage.test.ts && npx tsc -p extensions/pi-claude-marketplace/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - prepareStageSkills with a project-scope `locations` and `cwd` set, staging a skill whose body contains `${CLAUDE_SKILL_DIR}`, `${CLAUDE_PROJECT_DIR}`, `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`: the staged file content shows skillDir = `<skillsTargetDir>/<generatedName>`, projectDir = the supplied cwd, and both plugin vars substituted.
    - The same skill staged with a user-scope `locations`: the staged file content still contains the literal `${CLAUDE_PROJECT_DIR}`, while the other three are substituted.
    - Typecheck passes: extending `augmentSkillDescription`'s `vars` param to `ClaudePluginVars` and adding `StageSkillsInput.cwd` compiles with the existing install/reinstall/update callers (which do not yet pass cwd — optional field).
    - `node --test tests/bridges/skills/stage.test.ts` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Pure content-substitution + an optional input field; revertible by restoring the chained implementation. No persisted schema or write-path change.</reversibility>
  <done>substituteClaudeVars resolves the four-variable set single-pass; both skills call sites feed skillDir + scope-gated projectDir; StageSkillsInput carries optional cwd; the skills stage test proves both scope arms.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the helper contract with unit tests (prohibitions + edge assumptions)</name>
  <files>tests/shared/vars.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/vars.ts (the extended helper from Task 1)
    - tests/shared/vars.test.ts (existing test structure and describe/test naming)
  </read_first>
  <action>
    Extend tests/shared/vars.test.ts with assertions that pin the pure-function
    contract of the extended substituteClaudeVars. Cover: (a) all four tokens
    substitute when all four values are present; (b) omitting `skillDir` leaves
    every `${CLAUDE_SKILL_DIR}` literal in the output (assert the literal is
    present, NOT an empty string); (c) omitting `projectDir` leaves every
    `${CLAUDE_PROJECT_DIR}` literal in the output; (d) an unknown token such as
    `${CLAUDE_SOMETHING_ELSE}` passes through unchanged; (e) no re-expansion —
    with pluginRoot set to a value that embeds `${CLAUDE_PLUGIN_DATA}` and a
    distinct pluginData, the embedded literal survives verbatim in the output
    (T-03-01); (f) byte-identity — content that references only the two plugin
    vars (no embedded placeholders) yields exactly the pre-existing substituted
    output. Use requirement/finding IDs (SUB-01, SUB-02, T-03-01) in test titles;
    per .claude/rules/typescript-comments.md do NOT put phase/plan/wave refs in
    any describe/test title or comment.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/shared/vars.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts that with `skillDir` absent, the output still contains the literal `${CLAUDE_SKILL_DIR}` (and is not shortened to an empty substitution).
    - A test asserts that with `projectDir` absent, the output still contains the literal `${CLAUDE_PROJECT_DIR}`.
    - A test injects a placeholder-bearing pluginRoot value and asserts the embedded `${CLAUDE_PLUGIN_DATA}` literal is NOT replaced (no re-expansion).
    - A test asserts an unknown `${...}` token is unchanged.
    - `node --test tests/shared/vars.test.ts` exits 0.
  </acceptance_criteria>
  <done>The helper-contract unit tests encode every prohibition (no empty-string substitution, no re-expansion, unknown-token pass-through) and both flagged edge assumptions, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin author → materialized skill content | Plugin-supplied skill files carry `${CLAUDE_*}` tokens; substitution values (pluginRoot, pluginData, skillDir, projectDir) are install-derived paths, one of which (projectDir = cwd) is user/project-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-93-01 | Tampering | substituteClaudeVars | medium | mitigate | Single-pass alternation replacer; matched values inserted verbatim, output never re-scanned, so a value embedding a `${CLAUDE_*}` literal cannot re-expand (T-03-01). Property test in Task 2. |
| T-93-02 | Tampering | substituteClaudeVars / skills stage | medium | mitigate | Replacer returns the matched literal when the mapped value is `undefined` — an absent skillDir/projectDir never collapses to an empty string that would corrupt materialized content. Tests in Tasks 1–2. |
| T-93-03 | Information disclosure / Elevation | skills stage write path | low | accept | Substitution alters file CONTENT only; write destinations (`skillsTargetDir`, staging dirs) are unchanged and remain assertPathInside-guarded (NFR-10). No new path is composed from a substituted value. |
| T-93-SC | Tampering | package installs | n/a | accept | No npm/pip/cargo installs in this phase — supply-chain row not applicable. |
</threat_model>

<verification>
- `node --test tests/shared/vars.test.ts tests/bridges/skills/stage.test.ts` exits 0.
- `npx tsc -p extensions/pi-claude-marketplace/tsconfig.json --noEmit` passes (optional-field extensions compile against existing callers).
- `npm run check` green before handing off to plan 93-02.
</verification>

<success_criteria>
- SUB-01: a skill's `${CLAUDE_SKILL_DIR}` resolves to its installed dir in materialized content, proven by the skills stage test.
- SUB-02 (skills arm): project-scope `${CLAUDE_PROJECT_DIR}` resolves to cwd; user-scope stays literal, proven by both scope arms of the skills stage test.
- T-03-01 preserved (strengthened): no re-expansion, proven by the placeholder-injection property test.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 93-01)

**`shared/vars.ts` — extended `ClaudePluginVars`:**
```
interface ClaudePluginVars {
  readonly pluginRoot: string;   // unchanged
  readonly pluginData: string;   // unchanged
  readonly skillDir?: string;    // NEW — ${CLAUDE_SKILL_DIR}; absent → pass-through
  readonly projectDir?: string;  // NEW — ${CLAUDE_PROJECT_DIR}; absent → pass-through
}
```
- `substituteClaudeVars(content, vars)` reimplemented as a single-pass alternation
  replace over `/\$\{(CLAUDE_PLUGIN_ROOT|CLAUDE_PLUGIN_DATA|CLAUDE_SKILL_DIR|CLAUDE_PROJECT_DIR)\}/g`;
  replacer returns the matched literal when the mapped value is `undefined`.

**New threading field:**
- `StageSkillsInput.cwd?: string` (bridges/skills/types.ts) — project-scope install cwd.

**Bridge wiring:**
- `augmentSkillDescription` `vars` param widened from `{ pluginRoot; pluginData }` to `ClaudePluginVars`.
- Both skills call sites feed `skillDir = path.join(locations.skillsTargetDir, generatedName)` and `projectDir = scope === "project" ? cwd : undefined`.

**New test names (requirement-ID titled, no phase refs):**
- `tests/shared/vars.test.ts`: SUB-01 skillDir substitution; SUB-02 projectDir substitution; absent-field pass-through; unknown-token pass-through; T-03-01 no re-expansion; plugin-var byte-identity.
- `tests/bridges/skills/stage.test.ts`: SUB-01/SUB-02 project-scope full substitution; user-scope `${CLAUDE_PROJECT_DIR}` literal.
</artifacts_produced>

<output>
Create `.planning/phases/93-substitution-completion/93-01-SUMMARY.md` when done.
</output>
