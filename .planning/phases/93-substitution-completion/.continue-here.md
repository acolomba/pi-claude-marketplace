---
context: phase
phase: 93-substitution-completion
task: 1
total_tasks: 5
status: in_progress
last_updated: 2026-08-03T19:07:08.949Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| node --test directory arg | `node --test tests/bridges/mcp/` fails with one opaque ERR_TEST_FAILURE wrapper in this repo while all tests pass when files are listed explicitly (hit during Phase 92 wave-1 gate) | advisory | Always pass explicit `.test.ts` file lists to `node --test`; never a bare directory |
| decision-coverage tag placement | The gate parses only `- **D-NN[ [tags]]:**` bullets; `[informational]` must IMMEDIATELY follow the ID (Phase 90 gate failed twice until `**D-90-02 [informational] (RESOLVED…):**`); a CONTEXT with zero D-NN bullets returns passed:false (0/0) — vacuous, not a real block (Phase 93) | advisory | Put tags right after the ID; treat 0/0 as vacuous-pass with rationale |
| module state vs /reload | Extension module top-level state does NOT survive Pi `/reload` (clearExtensionCache + jiti moduleCache:false re-evaluates fresh) while process.env DOES survive — falsified Phase 90's original module-baseline design | advisory | Already baked into D-90-01 (env-var ledger); never design reload-durable state on module scope |

<current_state>
Autonomous --interactive run of milestone v1.17 env-parity, paused at a CLEAN
wave boundary inside Phase 93 (substitution-completion). HEAD = fa865156 on
features/env-parity, working tree clean, no uncommitted files.

- Phase 90: implemented + review clean; verification human_needed → DEFERRED
  (STATE.md Deferred Verification: `/gsd-verify-work 90`; 2 live-Pi items in
  90-UAT.md). Must close before milestone archive.
- Phase 91: COMPLETE + sealed (passed, Nyquist-compliant, threat-secure).
- Phase 92: COMPLETE + sealed (passed 24/24; safeSet __proto__ hardening chain
  landed across all four parsed-key sinks incl. unstage.ts).
- Phase 93: wave 1 (plan 93-01) DONE and merged (engine + skills call sites;
  41 targeted tests green, typecheck clean, tracking committed). Wave 2
  (plan 93-02: commands/agents arms + orchestrator cwd threading + e2e test)
  was NEVER STARTED — the executor dispatch was rejected at pause. Expected
  dispatch base was fa865156.
- Phase 94: not started.
</current_state>

<completed_work>

- Phase 90 (deferred UAT only), Phase 91, Phase 92: see HANDOFF.json completed_tasks.
- Phase 93 plan 93-01 (2/2 tasks): single-pass alternation `substituteClaudeVars`
  (4 vars, absent-field pass-through, T-03-01 strengthened), skills call sites
  wired, vars contract pinned (12/12 + 29/29 tests). Merge commit 0c1e82af,
  tracking fa865156.
</completed_work>

<remaining_work>

- Plan 93-02 (3 tasks, wave 2): commands stage + agents convert SUB-02 arms,
  optional `cwd` threading through install/update/reinstall orchestrators,
  end-to-end project-scope install test. Zero work exists yet.
- Phase 93 post-execution: full `npm run check`, code review + fix loop,
  gsd-verifier, phase.complete, validate-phase + secure-phase hooks.
- Phase 94 (docs, DOC-06/07): sequential non-worktree per convention; MUST fold
  in the carrier items (below).
- Phase 90 UAT closure; then milestone lifecycle (audit → complete → cleanup),
  with runtime UAT BEFORE cleanup archives phases (house convention).
</remaining_work>

<decisions_made>

- D-90-01 (revised): PATH ownership via pi-only env-var ledger
  `PI_CLAUDE_MARKETPLACE_PATH` (module-baseline falsified; STOP-and-ask fired;
  user chose ledger).
- D-91-01/D-91-02: behavioral drift guard; CLAUDE_SESSION_ID alias pinned in
  both hook lanes.
- WR-02 (91 review, user-decided): NO scrubbing of inherited parent
  CLAUDE_CODE_*/ANTHROPIC_* vars — document nested-host caveat in 94 DOC-06.
- D-92-01/D-92-02: whole-entry deep substitution; stdio-only injection.
- Phase 93: no-gray-area assessment; research deliberately skipped (CONTEXT.md
  carries session-verified file:line facts); planner chose optional `cwd`
  (closed by e2e test 02-T3).
</decisions_made>

<blockers>
- Phase 90 live-Pi UAT (human): 2 items in 90-UAT.md — not blocking 93/94 code
  work; blocking milestone close.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine state incl. next_action and carrier items
2. `.planning/phases/93-substitution-completion/93-02-PLAN.md` — the un-started wave-2 plan
3. `.planning/phases/93-substitution-completion/93-CONTEXT.md` — locked decisions (no-gray-area)
4. `.planning/phases/93-substitution-completion/93-01-SUMMARY.md` — what wave 1 built (extended vars contract)
5. `.planning/STATE.md` — Deferred Verification table (Phase 90)

## Phase 94 Carrier Items (do NOT lose — fold into 94's CONTEXT/DOC-06)
- WR-02 nested-host caveat: inherited parent `CLAUDE_CODE_*`/`ANTHROPIC_*` vars ride the spread into hook envs; deliberately not scrubbed (non-interference).
- Pi-only `PI_CLAUDE_MARKETPLACE_PATH` PATH-ledger env var (visible to children).
- Pi-only `CLAUDE_SESSION_ID` alias in bash children AND hook envs (SENV-03, D-91-02).
- MCP spawn-order caveat + session-switch staleness (already in DOC-06 requirement text).
- User-scope `${CLAUDE_PROJECT_DIR}` pass-through divergence (SUB-02, documented).

## Infrastructure State
- Stray worktree: `.claude/worktrees/agent-a7b89223e91f006e1` @ a1500568 (branch `worktree-agent-a7b89223e91f006e1`) — in NO manifest from this session (likely fixer leftover). Inspect its commits vs features/env-parity before removing; do NOT bulk-clean (fail-closed policy).
- Pre-existing user worktrees (leave alone): `.claude/worktrees/context-window-1m`, `.worktrees/force-install-only`.
- Known environment issue: 2 pi-subagents integration tests fail locally (stale global peer via `npm root -g`); `npm run check` still exits 0 — not a regression signal.
- The three SUMMARYs matching a placeholder grep (92-01, 92-02, 93-01) are body-text matches, not unfilled templates — verified complete this session.

<context>
Interactive autonomous pipeline: discuss questions answered by the user per
phase; plan/execute inline; review+fix auto-chained; verifier routes sealing.
Per-phase pattern established over 90-92: worktree executor (SKIP=trufflehog
prefix per CLAUDE.md), cleanup-wave merge, targeted-then-full check gates,
review→fix→re-review loop to clean, verifier, phase.complete, then
validate-phase (Nyquist) + secure-phase (SECURITY.md) hooks inline.
</context>

<next_action>
Start with: /gsd-resume-work, then /gsd-autonomous --interactive --from 93 —
execute-phase 93 skips 93-01 (SUMMARY exists) and dispatches only plan 93-02
(wave 2), then runs the post-execution chain; after 93 seals, Phase 94 discuss
must fold in the carrier items above; close Phase 90 UAT before lifecycle.
</next_action>
