---
context: phase
phase: 114-plugin-and-marketplace-lifecycle
plan: 16
task: 1
total_tasks: 1
status: paused
last_updated: 2026-09-01T17:02:40.786Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: One pair per plan and commit — The first gap plan grouped install, reinstall, and uninstall, which the plan checker rejected because each source-test pair must remain independently owned and committed. Execute only one owner plan at a time and stage exact owned paths.
- [ ] CONSTRAINT: Immutable retry-closure baseline — An earlier parallel-wave design could capture a mutable HEAD after another closure commit. Reuse exactly `9e5fb1e4a922bd6eaed06f9f26f0adfdfc3c78b3` from `114-15-SUMMARY.md`; never recapture or replace it.
- [ ] CONSTRAINT: Coverage is not retry evidence — The first canonical verifier found green direct coverage but no second exported invocation from the exact first-call partial state. Every representative must make two AST-visible calls to the same exported entrypoint over the same root/mode/target and repair only the injected fault.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern                         | Description                                                                                                              | Severity | Prevention Mechanism                                                                                                                    |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Multi-owner gap commit          | The initial closure plan combined three owner pairs, defeating independent ownership and rollback.                       | blocking | Keep 114-16 limited to reinstall plus its summary and 114-17 limited to uninstall plus its summary; commit exact paths once per plan.   |
| Mutable closure base            | Same-wave execution allowed another commit to move HEAD before baseline capture.                                         | blocking | Execute sequentially and consume the literal `Retry closure base:` value recorded by Plan 114-15.                                       |
| Coverage substituted for replay | The original owners reached 100% direct coverage without demonstrating a second exported call from real failure residue. | blocking | AST-audit two direct same-export calls per representative and assert exact first/second outcomes, bytes, trees, residue, and schedules. |

<current_state>
Phase 114 is paused between gap Plans 114-15 and 114-16. Fifteen of seventeen plans have summaries. Plan 114-15 completed in commit `2323c8940f638f5138145512582ffe8fc18de085`; Plans 114-16 and 114-17 have not started. HEAD is expected to be `2323c894` before the handoff commit.

The install retry closure is test-only and green: 13/13 focused retry cases, 134/134 full owner TAP, and 238/238 branches, 51/51 functions, and 2453/2453 lines. Every stable-prefix case contains exactly two direct `installPlugin` calls. The immutable closure base is `9e5fb1e4a922bd6eaed06f9f26f0adfdfc3c78b3`.

The earlier Phase 114 canonical verifier still reports `gaps_found` at 71/75 because reinstall and uninstall retry proof remain open. That report is intentionally stale until Plans 114-16 and 114-17 finish and a fresh verifier replaces it.
</current_state>

<completed_work>

Completed this continuation:

- Finalized three pair-atomic gap plans and resolved the dependency-order warning.
- Independent final plan review passed with zero blockers and zero warnings.
- Committed the plans as `49aaa1dd` and synchronized execution state as `9e5fb1e4`.
- Completed Plan 114-15 as `2323c894` with its install-only semantic-equivalence matrix and all required gates green.
- Preserved every production source byte-for-byte during Plan 114-15.
  </completed_work>

<remaining_work>

- Plan 114-16: Add deterministic reinstall failure-then-retry representatives to `tests/orchestrators/plugin/reinstall.test.ts`; create `114-16-SUMMARY.md`; inherit the exact Plan 114-15 baseline; commit only that owner and summary.
- Plan 114-17: Add uninstall failure-then-retry representatives to `tests/orchestrators/plugin/uninstall.test.ts`; create `114-17-SUMMARY.md`; use the inherited baseline for the closure-wide production diff; run all aggregate Phase 114 gates.
- Run focused post-gap code review and fix any findings atomically.
- Refresh validation/security evidence if the new test summaries make them stale.
- Spawn a fresh canonical `gsd-verifier`; completion requires the grouped retry blocker to be replaced by a passing MOD-07 verdict.
- Run conversational UAT only after canonical verification passes, then transition Phase 114 and continue autonomous work at Phase 115.
  </remaining_work>

<decisions_made>

- All single-owner supplemental cases are absorbed into direct owners; only genuine cross-boundary integrations remain.
- Each source-test pair is one plan and one implementation commit.
- Runtime AAA labels are lowercase and separate: `// arrange`, `// act`, `// assert`. Use `// act & assert` only for one `assert.throws()` or `assert.rejects()` expression. Keep data rows separate.
- Alphabetize presentation and static inventories only. Preserve behavior-bearing caller, scope, reason, transaction, rollback, lifecycle, and insertion order.
- The shipped public contract wins stale tests.
- Production edits are allowed only for actual defects or CodeGraph-proven unreachable private cleanup. Add no seams, exports, pragmas, or coverage exceptions.
- Direct and cascade modes require semantic parity with named differences.
- Assert real mutation units, partial outcomes, safe retry, and literal schedules; never invent command-wide rollback.
  </decisions_made>

<blockers>

- No active technical blocker.
- No pending human action.
- No outstanding asynchronous external job.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable paused state and exact next action.
2. `.planning/phases/114-plugin-and-marketplace-lifecycle/114-15-SUMMARY.md` — immutable base and completed install matrix.
3. `.planning/phases/114-plugin-and-marketplace-lifecycle/114-16-PLAN.md` — next pair-atomic execution contract.
4. `.planning/phases/114-plugin-and-marketplace-lifecycle/114-17-PLAN.md` — final owner and aggregate closure contract.
5. `.planning/phases/114-plugin-and-marketplace-lifecycle/114-VERIFICATION.md` — exact grouped retry gap to replace.
6. `.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md` — locked phase decisions.
7. `.planning/phases/114-plugin-and-marketplace-lifecycle/114-PATTERNS.md` — ownership, AAA, ordering, and deterministic-fault enforcement.
8. `.planning/METHODOLOGY.md` if it exists — apply its analytical lenses before assumptions.

## Critical Anti-Patterns (do NOT repeat these)

- Multi-owner closure plan: it broke the one-pair ownership contract → execute 114-16 and 114-17 separately and commit exact paths.
- Recaptured baseline: it could exclude an earlier gap commit from production-diff proof → consume the literal SHA from `114-15-SUMMARY.md`.
- Green coverage treated as replay proof: it caused the 71/75 verification gap → require a real second same-export invocation from exact first-call state.
- Fictional rollback or cleanup: it would assert behavior the command does not provide → preserve and assert the real partial state, safe result, and forward schedule.

## Infrastructure State

- Active subagents: none.
- Running servers/watchers: none.
- Async external jobs: none.
- Production changes from Plan 114-15: none.
- Uncommitted user-owned files to preserve and never stage: `.claude/settings.json`, `.codex/config.toml`, `.claude/CLAUDE.md`, `.codegraph/`, `.mcp.json`, `.planning/milestone.lock`, `.planning/research/.cache/`, `AGENTS.md`, and `a.out`.
- `.codegraph/` exists. Follow `AGENTS.md`: use CodeGraph before grep/find or direct file reads for code understanding and location.

## Pre-Execution Critique Required

- Design artifacts: `114-16-PLAN.md` and `114-17-PLAN.md`.
- Critique focus: one-owner atomicity, same-export second invocations, sole fault repair, exact first/second state and schedules, immutable baseline reuse, lowercase AAA, and closure-wide production no-change proof.
- Gate status: satisfied before pause. The final independent `gsd-plan-checker` review reported zero blockers and zero warnings. Repeat only if either plan changes.

<context>
The safe execution order is fixed: install is complete; reinstall must run next; uninstall and aggregate gates run last. Do not parallelize these plans. Plan 114-16 must read the base from `114-15-SUMMARY.md` rather than current HEAD. Plan 114-17 must use that same base across install, reinstall, and uninstall production sources and must hand the frozen tree to a fresh canonical verifier rather than editing `114-VERIFICATION.md` itself.

The original phase-level review, validation, and security reports were green before the retry gap was found. Treat them as evidence to refresh after gap execution, not as substitutes for fresh post-gap checks. UAT is pending and must not start until the canonical verifier passes.
</context>

<next_action>
Start with: run `$gsd-resume-work`, explicitly explain all three blocking constraints, verify `2323c894` and the literal retry base, then execute only `114-16-PLAN.md` with a typed `gsd-executor`.
</next_action>
