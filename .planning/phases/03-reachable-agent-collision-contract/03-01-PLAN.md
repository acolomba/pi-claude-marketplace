---
phase: 03-reachable-agent-collision-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - scripts/test-coverage-direct.pin.json
autonomous: true
requirements: [FINAL-01]
must_haves:
  truths:
    - The existing AG-11 check rejects an empty explicit tool allowlist with the same complete diagnostic.
    - The emitter accepts only a validated nonempty explicit list and does not duplicate an unreachable check.
    - Aggregate unit production coverage returns to exactly 100 percent without exclusions or weakened assertions.
  artifacts:
    - path: extensions/pi-claude-marketplace/bridges/agents/convert.ts
      provides: Type-level evidence carried from the existing runtime validation to the emitter
  key_links:
    - from: assertMappedToolsNonEmpty
      to: toolsFields
      via: narrowed tool mapping union
---

<objective>Restore the exact unit baseline through truthful modeling of an already validated value.</objective>
<tasks>
<task type="auto">
<name>Carry the validated tool-list contract forward</name>
<files>extensions/pi-claude-marketplace/bridges/agents/convert.ts</files>
<action>Change the existing AG-11 validator into an assertion function over the mapping value. Narrow only the explicit mapping arm to a nonempty string tuple. Keep the same omitted-tool behavior and user-facing errors. Have toolsFields consume the narrowed mapping and emit the existing list directly, removing the second unreachable throw. No cast, suppression, new export, or test-only entry point.</action>
<verify><automated>node --test tests/bridges/agents/convert.test.ts</automated></verify>
<done>All existing positive/negative conversion assertions pass and the compiler rejects unvalidated input to toolsFields.</done>
</task>
<task type="auto">
<name>Measure aggregate and owner coverage separately</name>
<files>scripts/test-coverage-direct.pin.json</files>
<action>Run the real direct owner gate and complete unit coverage outside the sandbox's faulty process-pipe path. Remove only the now-unnecessary convert.ts shortfall pin after actual direct measurements reach 100%; preserve the two unrelated direct-owner pins and all enforcement thresholds. Record raw production LCOV totals, including zero uncovered lines/functions/branches.</action>
<verify><automated>env -u PI_CODING_AGENT_DIR npm run test:coverage:unit</automated></verify>
<done>Exactly 100 percent aggregate production coverage and passing direct gate; record actual counts.</done>
</task>
</tasks>
<output>Write 03-01-SUMMARY.md with measured coverage evidence.</output>
