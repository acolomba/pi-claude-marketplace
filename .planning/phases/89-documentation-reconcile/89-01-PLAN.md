---
phase: 89-documentation-reconcile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/output-catalog.md
  - docs/research/issue-103-stop-stopfailure-promotion.md
autonomous: true
requirements: [DOC-04, DOC-05]
must_haves:
  truths:
    - "docs/research/issue-103-stop-stopfailure-promotion.md attributes agent_settled to 0.80.5 at all four cited sites (executive summary, cost line, sources table, Pi API surface); no 0.80.4 attribution remains anywhere in docs/."
    - "The peer-floor version stated in the issue-103 doc is exactly 0.80.5 (not the non-existent 0.80.4 npm release) — the D-89-06 nuance is preserved (CHANGELOG attributes it to 0.80.4; npm has no 0.80.4; 0.80.3 -> 0.80.5; typings first ship in 0.80.5)."
    - "The issue-103 doc's Stale-doc inventory section reads as reconciled (the two DOC-04/DOC-05 edits are described as done, not as dangling future-tense pending work)."
    - "docs/output-catalog.md:390 partial-hook prose example names a still-unsupported event (Notification), not Stop, as the unsupportable-event example."
    - "The byte-pinned fenced blocks in docs/output-catalog.md are unchanged and npm run check stays green (catalog-uat, hooks-cap-notify, partial-vocabulary-guard all pass)."
  artifacts:
    - docs/output-catalog.md
    - docs/research/issue-103-stop-stopfailure-promotion.md
  key_links:
    - "docs/output-catalog.md:390 partial-hook prose (edited) <-> the catalog-uat byte-pinned fenced block at lines 385-388 (must stay decoupled — prose edit only)."
    - "issue-103 doc is the pointer target DOC-04/DOC-05 reference; its 0.80.5 correction and reconciled stale-doc inventory keep that authority consistent."
  prohibitions:
    - "Do not modify any byte-pinned fenced ```text block in docs/output-catalog.md (the partially-installed-inventory-hooks block at 385-388 and the stop-override-cap block near line 2264)."
    - "Do not introduce any partial-vocabulary-guard forbidden token (retired force / (unsupported)-verdict vocabulary) into docs/output-catalog.md."
    - "Do not perform a full design-prose audit of the issue-103 doc — only the version rider and the stale-doc-inventory reconcile touch; the line-73 'optionally firmed by HTTP status' phrasing stays untouched."
---

<objective>
Land the two carried-in riders (from Phase 87) and prove the only test-coupled edit in
this docs-only phase is byte-safe: re-point the `docs/output-catalog.md` Stop-as-unsupported
example to a still-unsupported event (D-89-07), and correct the issue-103 authority doc's
`agent_settled` version attribution to `0.80.5` at every mention (D-89-06).

This plan LEADS the phase intentionally (tracer-first for a docs phase): `docs/output-catalog.md`
is the ONLY document in phase 89 read by any test, so proving that edit byte-safe against the
full `npm run check` gate first de-risks the whole phase before the larger narrative reconciles
(DOC-04 in plan 02, DOC-05 in plan 03) begin.

Purpose: keep the two authority/reference documents (the disposition-model authority
`output-catalog.md` and the DOC-04/DOC-05 pointer target `issue-103-*.md`) consistent with the
shipped v1.16 Stop/StopFailure behavior.
Output: two edited markdown files; the three `output-catalog.md`-coupled tests still green.

Flagged assumption (Assumptions Log A1, research): `Notification` is the chosen still-unsupported
replacement for `Stop` in the output-catalog example. Any non-bucket-A event is contract-valid;
`Notification` is selected only because it is the suite's canonical unsupported-event example
(D-87-06). This is Claude's discretion, recorded so the reviewer is not surprised.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-documentation-reconcile/89-CONTEXT.md
@.planning/phases/89-documentation-reconcile/89-RESEARCH.md
</context>

<artifacts_produced>
This plan creates or modifies (no code symbols; documentation only):
- `docs/output-catalog.md` — single-token prose re-point at line 390 (D-89-07).
- `docs/research/issue-103-stop-stopfailure-promotion.md` — four version corrections + one stale-doc-inventory consistency touch (D-89-06).

Phase-wide artifact set (across all three plans): `docs/hooks-compatibility.md`,
`docs/research/claude-hooks-vs-pi-events.md`, `docs/research/issue-103-stop-stopfailure-promotion.md`,
`docs/output-catalog.md`.
</artifacts_produced>

<tasks>

<task type="tracer">
  <name>Task 1: Re-point the output-catalog partial-hook example (D-89-07) and prove byte-safety</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - docs/output-catalog.md (read lines 381-401 — the "Partially-installed inventory row -- partial-hook plugin" section; note the fenced ```text block at 385-388 and the prose paragraph at 390)
    - .planning/phases/89-documentation-reconcile/89-RESEARCH.md (the "Riders — D-89-07" section and the "Test-Coupling Map" — the prose-vs-fenced-block byte-safety analysis)
    - tests/architecture/catalog-uat.test.ts (confirms it extracts only fenced-block bodies paired with `<!-- catalog-state: -->` markers, never prose)
    - tests/architecture/partial-vocabulary-guard.test.ts (confirms the retired-vocabulary scan)
    - tests/architecture/hooks-cap-notify.test.ts (confirms it reads only the stop-override-cap fenced block)
  </read_first>
  <action>
    In the prose paragraph at docs/output-catalog.md line 390 (inside the
    "Partially-installed inventory row -- partial-hook plugin" section, NOT inside any fenced
    block), the parenthetical example currently reads "a non-bucket-A event such as `Stop`".
    Change only the event name in that parenthetical to `Notification`, so it reads
    "a non-bucket-A event such as `Notification`". This is a single inline-code token change in
    running prose. Rationale: the named event is now bucket-A (supported) per Phase 87, so it is
    the wrong example for an unsupportable event; `Notification` is the canonical still-unsupported
    example used across the suite (D-87-06).
    Do NOT touch the fenced ```text block at lines 385-388 (`● official [user] <autoupdate>` /
    `◉ hook-plugin v1.0.0 (partially-installed) {unsupported hooks}`) — it is byte-pinned by
    catalog-uat.test.ts. Do NOT touch the stop-override-cap fenced block near line 2264 — it is
    byte-pinned by hooks-cap-notify.test.ts. Introduce no retired force/verdict vocabulary tokens.
    Keep the edit to the single parenthetical event name; change nothing else in the paragraph.
    <reversibility rating="reversible">Git-tracked single-token markdown prose edit; `git checkout` restores prior state. No one-way door.</reversibility>
  </action>
  <verify>
    <automated>grep -n "non-bucket-A event such as \`Notification\`" docs/output-catalog.md && node --test tests/architecture/catalog-uat.test.ts tests/architecture/hooks-cap-notify.test.ts tests/architecture/partial-vocabulary-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "non-bucket-A event such as \`Notification\`" docs/output-catalog.md` returns exactly one line (the re-pointed example at ~line 390).
    - `git diff docs/output-catalog.md` contains no line that begins with ` ```text ` or that lies inside a fenced block (only the one prose line changes); the diff is a single one-token hunk.
    - `node --test tests/architecture/catalog-uat.test.ts tests/architecture/hooks-cap-notify.test.ts tests/architecture/partial-vocabulary-guard.test.ts` exits 0 (all three coupled tests pass).
  </acceptance_criteria>
  <done>The partial-hook example names `Notification` instead of `Stop`; all three output-catalog-coupled tests pass; no fenced block changed.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the issue-103 doc version attribution to 0.80.5 (D-89-06) and reconcile its stale-doc inventory</name>
  <files>docs/research/issue-103-stop-stopfailure-promotion.md</files>
  <read_first>
    - docs/research/issue-103-stop-stopfailure-promotion.md (read the executive summary lines 5-13, sources table lines 17-24, Pi API surface lines 34-41, and the Stale-doc inventory lines 98-101)
    - .planning/phases/89-documentation-reconcile/89-RESEARCH.md (the "Riders — D-89-06" table enumerating each current version-string site and its context, and the "Consistency touch" note)
    - .planning/phases/89-documentation-reconcile/89-CONTEXT.md (D-89-06 — the version nuance to preserve)
  </read_first>
  <action>
    At the four sites the research inventory's D-89-06 table cites (executive summary line 8,
    cost line 12, sources table line 21, Pi API surface line 37), correct the `agent_settled`
    version attribution to `0.80.5`. The exact current (incorrect) string at each site is listed
    in the research inventory read_first — replace each with `0.80.5`. Preserve the D-89-06 nuance
    where the prose has room: the upstream CHANGELOG attributes `agent_settled` to a patch the npm
    registry never released (0.80.3 -> 0.80.5), and the typings first ship in 0.80.5, so `>=0.80.5`
    is the correct installable floor (matches package.json peerDependencies and FLOOR-01). On the
    cost line, the `>=0.74.0` prior floor stays as the "from" side of the bump; only the "to" side
    becomes `>=0.80.5`.
    Consistency touch (within this pass): the § "Stale-doc inventory" bullets (lines 100-101)
    describe the two DOC-04/DOC-05 edits in future tense ("the Stop/StopFailure rows flip to
    supported..."). After this milestone those edits are done — rewrite the bullets so the section
    reads as reconciled (the section header already says "reconciled by this milestone"), leaving
    no dangling future-tense claim.
    Do NOT touch the line-73 StopFailure-classifier prose ("optionally firmed by HTTP status
    tracked via `after_provider_response`") — it sits inside the doc's "optionally" envelope, is
    not falsified, and a full design-prose audit of this doc is out of scope.
    <reversibility rating="reversible">Git-tracked markdown edit; `git checkout` restores prior state.</reversibility>
  </action>
  <verify>
    <automated>test "$(grep -Ec '0\.80\.4' docs/research/issue-103-stop-stopfailure-promotion.md)" = "0" && test "$(grep -Ec '0\.80\.5' docs/research/issue-103-stop-stopfailure-promotion.md)" -ge "4"</automated>
    <human-check>The stale-doc inventory bullets read as reconciled (no dangling future-tense "will flip"); the version nuance (npm never released the 4th patch; typings first ship in 0.80.5) is preserved in prose.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '0\.80\.4' docs/research/issue-103-stop-stopfailure-promotion.md` returns 0 (all four sites corrected).
    - `grep -Ec '0\.80\.5' docs/research/issue-103-stop-stopfailure-promotion.md` returns at least 4.
    - The cost line still shows the `>=0.74.0` prior floor as the "from" side of the bump (only the "to" side changed).
    - The § "Stale-doc inventory" bullets no longer state the DOC-04/DOC-05 edits in unfulfilled future tense.
    - Line 73's `after_provider_response` classifier phrasing is unchanged (`git diff` shows no hunk touching it).
  </acceptance_criteria>
  <done>All four `agent_settled` version mentions read 0.80.5; the stale-doc inventory reads reconciled; the nuance is preserved; line 73 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human reader -> docs | Static markdown consumed by humans; no runtime code path executes these files. |
| test suite -> docs/output-catalog.md | Three architecture tests byte-read specific fenced blocks of this one doc; a prose edit that strays into a pinned block would break the gate. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-01 | Tampering | docs/output-catalog.md byte-pinned fenced blocks | low | mitigate | Prose-only single-token edit outside every fenced block; catalog-uat + hooks-cap-notify + partial-vocabulary-guard run in `npm run check` and fail on any byte drift. |
| T-89-02 | Information disclosure | issue-103 doc prose | low | accept | Public design/authority doc; no secrets; git-tracked and PR-reviewed. |
| T-89-SC | Tampering (supply chain) | package installs | n/a | accept | No npm/pip/cargo install occurs in this docs-only phase; supply-chain vector not present. |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts tests/architecture/hooks-cap-notify.test.ts tests/architecture/partial-vocabulary-guard.test.ts` passes after the output-catalog edit.
- `grep -Ec '0\.80\.4' docs/research/issue-103-stop-stopfailure-promotion.md` returns 0.
- `npm run check` stays green (NFR-6) — full-gate confirmation for the one test-coupled edit.
- Human review (at /gsd-verify-work): the version nuance and reconciled stale-doc inventory read correctly.
</verification>

<success_criteria>
- output-catalog partial-hook example names a still-unsupported event; three coupled tests green.
- issue-103 doc: all four version mentions read 0.80.5; stale-doc inventory reconciled; line 73 untouched.
- No fenced block in output-catalog.md modified.
</success_criteria>

<output>
Create `.planning/phases/89-documentation-reconcile/89-01-SUMMARY.md` when done.
</output>
