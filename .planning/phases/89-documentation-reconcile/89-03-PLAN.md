---
phase: 89-documentation-reconcile
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/research/claude-hooks-vs-pi-events.md
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "docs/research/claude-hooks-vs-pi-events.md no longer states 'agent_end is observation-only' as the reason Stop cannot block; the cross-mapping `Stop` row reflects that Stop is dispatched off `agent_settled` with full decision control (block re-entry via sendMessage followUp+triggerTurn) and points to docs/research/issue-103-stop-stopfailure-promotion.md."
    - "`agent_settled` appears as row #31 in the Pi extension-event inventory (Trigger: agent run fully settled — no retry/compaction/queued continuation will run; Control: Observation, no payload); the Pi inventory count prose reads 31 (Claude's count stays 30)."
    - "The cross-mapping `StopFailure` row reflects the `stopReason` protocol contract (error/length endings surface as the final assistant message's stopReason; classification from errorMessage), NOT `after_provider_response` synthesis, and points to the issue-103 doc."
    - "Only claims falsified by v1.16 shipping change (Stop/StopFailure cells in the naive summary, feasibility totals, bucket-D synthesis table, path-forward, and marketplace bucket labels where consistency requires); the historical feasibility analysis for buckets E/F/G/H, the soft-dep audit, and non-Stop marketplace coverage are unchanged."
    - "The doc is corrected in place — no strikethrough and no preserved-history 'superseded' relics; the date/status line reflects the amendment."
  artifacts:
    - docs/research/claude-hooks-vs-pi-events.md
  key_links:
    - "cross-mapping `Stop` and `StopFailure` rows -> pointer to docs/research/issue-103-stop-stopfailure-promotion.md (DOC-05 requires the pointers)."
    - "Pi event inventory row #31 `agent_settled` <- the shipped fire-point (settle.ts); `agent_end` row stays accurate for agent_end itself."
  prohibitions:
    - "Do not re-base the feasibility analysis — bucket-E/F/G/H rows, the pi-subagents/pi-mcp-adapter soft-dep audit, and the non-Stop marketplace coverage are not edited; only claims falsified by v1.16 shipping change (Pitfall 3)."
    - "Do not use strikethrough or preserved-history 'superseded' relics — correct in place; git carries history (D-89-05)."
---

<objective>
Amend `docs/research/claude-hooks-vs-pi-events.md` in place (D-89-05) so it is internally
consistent with the shipped v1.16 Stop/StopFailure design: retire the naive-table
"`agent_end` is observation-only" claim, add `agent_settled` to the Pi event inventory, supersede
StopFailure's `after_provider_response` synthesis with the `stopReason` protocol contract, and add
the DOC-05-required pointers to `docs/research/issue-103-stop-stopfailure-promotion.md`. Only claims
the shipping falsifies change; the dated research note stays a research note — the still-accurate
historical feasibility analysis (buckets E/F/G/H, the soft-dep audit, non-Stop marketplace
coverage) is preserved (Pitfall 3).

This doc is read by no test (grep: NONE) — zero byte-test risk; `format:check` does not gate
markdown. Correctness is proven structurally by grep assertions here and confirmed by human review
against the research Stale-Claim Inventory §B at /gsd-verify-work.

Flagged assumptions (recorded, never silently dropped):
- Spec-less probe DOC-05 (`unclassified` — "review manually") remains UNRESOLVED: there is no
  crisp boundary/precision predicate to author for DOC-05 beyond the §B row-by-row inventory. It is
  surfaced here for manual review at /gsd-verify-work rather than converted to a false-precision
  acceptance criterion.
- The issue-103 doc's line-73 StopFailure-classifier phrasing ("optionally firmed by HTTP status
  via `after_provider_response`") is intentionally left untouched (see plan 02/plan 01 scope note);
  this plan's B8 touch-up concerns only THIS doc's `after_provider_response` no-Claude-analog note.

Purpose: a reader of the research note sees a coherent account where Stop/StopFailure are shipped
(not lossy syntheses), with pointers to the authority doc for the shipped design.
Output: one amended markdown file, internally consistent post-correction.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-documentation-reconcile/89-CONTEXT.md
@.planning/phases/89-documentation-reconcile/89-RESEARCH.md
</context>

<artifacts_produced>
This plan modifies (documentation only; no code symbols):
- `docs/research/claude-hooks-vs-pi-events.md` — correct-in-place amendment (rows B1-B12 of the research Stale-Claim Inventory §B).

Phase-wide artifact set (across all three plans): `docs/hooks-compatibility.md`,
`docs/research/claude-hooks-vs-pi-events.md`, `docs/research/issue-103-stop-stopfailure-promotion.md`,
`docs/output-catalog.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Date/status line, Pi inventory count, agent_settled row #31, executive-summary Stop retire (B1-B4)</name>
  <files>docs/research/claude-hooks-vs-pi-events.md</files>
  <read_first>
    - docs/research/claude-hooks-vs-pi-events.md (the date/status line 3, executive summary 5-16, the "Pi extension events" prose 80-83 and inventory table 84-115)
    - .planning/phases/89-documentation-reconcile/89-RESEARCH.md (Stale-Claim Inventory §B rows B1-B4)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ "Pi API surface" line 37 — the agent_settled definition to mirror in the inventory row)
    - .planning/phases/89-documentation-reconcile/89-CONTEXT.md (D-89-05 correct-in-place; date/status phrasing is Claude's discretion)
  </read_first>
  <action>
    Apply the B1-B4 header/inventory edits from the research inventory §B:
    B1 (date/status line 3): update it to reflect the amendment — the current line marks the note
    "not yet reviewed"; amend it to note the v1.16 reconcile (exact phrasing is Claude's
    discretion, e.g. add "amended 2026-07-31: Stop/StopFailure reconciled with shipped v1.16
    behavior"). No strikethrough, no "superseded" relic.
    B2 (Pi inventory count): the executive summary (line 7) gives the Pi extension-event count as
    30, and the "Pi extension events" prose (line 82) says "Thirty events as documented" — change
    the Pi count to 31 in both (with `agent_settled` added below; the exact current phrasing at
    each site is quoted in research inventory §B row B2). Claude Code's 30-event count is upstream
    and stays unchanged.
    B3 (executive summary lines 13-14): retire the "Stop is the single biggest correctness risk...
    it's a bucket-D lossy synthesis" and "load-bearing test case" framing for Stop-as-lossy — Stop
    is now shipped bucket-A with full decision control, not a bucket-D synthesis. Rephrase to state
    Stop shipped with full decision control and add a pointer to
    `docs/research/issue-103-stop-stopfailure-promotion.md`.
    B4 (Pi event inventory table 84-115): add `agent_settled` as row #31 — Trigger: "agent run
    fully settled — no automatic retry, compaction, or queued continuation will run"; Control:
    "Observation" (carries no payload). Leave the existing `agent_end` row (line 98) unchanged — it
    stays accurate for `agent_end` itself.
    <reversibility rating="reversible">Git-tracked markdown edit; `git checkout` restores prior state.</reversibility>
  </action>
  <verify>
    <automated>grep -q 'agent_settled' docs/research/claude-hooks-vs-pi-events.md && test "$(grep -c 'Pi has 30 extension events' docs/research/claude-hooks-vs-pi-events.md)" = "0" && grep -q 'issue-103-stop-stopfailure-promotion.md' docs/research/claude-hooks-vs-pi-events.md</automated>
    <human-check>The date/status line reads as amended (no strikethrough); the Pi inventory table has an agent_settled row #31 and reads 31 total; the executive-summary Stop framing no longer calls Stop a bucket-D lossy synthesis.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'agent_settled' docs/research/claude-hooks-vs-pi-events.md` returns at least 1 (the new inventory row plus any cross-mapping references).
    - `grep -c 'Pi has 30 extension events' docs/research/claude-hooks-vs-pi-events.md` returns 0 (count updated to 31).
    - The doc contains a pointer to `issue-103-stop-stopfailure-promotion.md`.
    - The date/status line no longer reads only "not yet reviewed" — it records the amendment.
  </acceptance_criteria>
  <done>Date/status amended; Pi count is 31 with an agent_settled inventory row; the executive-summary Stop-as-lossy framing is retired with a pointer to the authority doc.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-mapping Stop/StopFailure rows, naive summary, feasibility + bucket-D + path-forward, marketplace labels (B5-B12)</name>
  <files>docs/research/claude-hooks-vs-pi-events.md</files>
  <read_first>
    - docs/research/claude-hooks-vs-pi-events.md (cross-mapping rows 139-140, naive summary 154-164, no-Claude-analog note 178, feasibility table + totals 195-211, bucket-D synthesis table 234-242, path-forward 304-308, marketplace coverage 316-388)
    - .planning/phases/89-documentation-reconcile/89-RESEARCH.md (Stale-Claim Inventory §B rows B5-B12, the "Note on B10" caveat, and Pitfall 3)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ Dispatcher design, § "Stop decision control", § "StopFailure arm" — the shipped stopReason gate + errorMessage classifier)
  </read_first>
  <action>
    Apply the B5-B12 correction-in-place edits from the research inventory §B; add pointers to
    `docs/research/issue-103-stop-stopfailure-promotion.md` where each falsified claim is corrected:
    B5 (cross-mapping `Stop` row, line 139): retire the stale claim on that row — that Pi's
    `agent_end` is observation-only and the bridge therefore cannot honor a block-to-continue
    decision (the exact current text is quoted in research inventory §B row B5). Replace with: Stop
    is dispatched off `agent_settled` with full decision control (block re-entry via
    `sendMessage(..., { deliverAs: "followUp", triggerTurn: true })`); point to the issue-103 doc.
    Fidelity marker is Claude's discretion, consistent with the table's existing key.
    B6 (cross-mapping `StopFailure` row, line 140): replace the `after_provider_response (partial)`
    synthesis claim on that row — Pi exposes HTTP status/headers but, per the stale text, lacks a
    turn-ended-by-error terminal event (exact text in research inventory §B row B6) — with the
    `stopReason` protocol contract: `error`/`length` endings surface as the final assistant
    message's `stopReason`; classification from `errorMessage`; point to the issue-103 doc.
    B7 (naive summary buckets 158-164): correct the Stop/StopFailure cells the shipping falsifies
    (Stop/StopFailure are no longer lossy). Keep the naive-1:1-view framing as a dated artifact —
    the note (line 164) about two Stop intents can stay as historical context but must not read as
    a current limitation.
    B8 (no-Claude-analog note, line 178): the `after_provider_response` row note "closest is
    `StopFailure` error-type matcher" — touch up so it no longer implies StopFailure synthesizes
    from `after_provider_response` (D-89-05 explicit). This is the ONLY `after_provider_response`
    edit in scope for this doc.
    B9 (feasibility table + totals 195-211): correct the cells the shipping falsifies — Stop and
    StopFailure are now direct/shipped, not bucket-D synthesis (adjust the bucket-A vs bucket-D
    membership and any "shippable today" total that named them as synthesis). Do NOT wholesale
    re-count every bucket — only correct what shipping falsifies (Deferred Ideas / Pitfall 3).
    B10 (bucket-D synthesis table 234-242): correct the `Stop (block-to-continue)` and
    `StopFailure` rows — shipped Stop uses the `agent_settled` settle dispatcher (the timing shift
    persists but is documented, not a lossy synthesis on the next idle); shipped StopFailure uses
    the `stopReason` gate + `errorMessage` classifier, not `after_provider_response` HTTP-status
    synthesis. Point to the issue-103 doc.
    B11 (path forward 304-308): correct the "Stop is the load-bearing test case... bucket-D
    synthesis for Stop MUST round-trip" framing — Stop shipped as bucket-A with full decision
    control; point to the issue-103 doc for the shipped design.
    B12 (marketplace coverage 316-388): judgment call (D-89-05) — correct the "Stop -> D" bucket
    labels for `ralph-wiggum`/`hookify`/`security-guidance` and "Stop is the highest-risk supported
    event" ONLY where consistency requires; do NOT re-run the audit. `ralph-wiggum`/`hookify`
    flipping to fully available is confirmed shipped (issue-103 line 13) — reflect that where the
    prose otherwise reads as unresolved risk.
    Preserve everything the shipping does NOT falsify: bucket-E/F/G/H rows, the
    pi-subagents/pi-mcp-adapter soft-dep audit, `FileChanged`/`CwdChanged` synthesis prose, and the
    non-Stop marketplace findings (Pitfall 3).
    <reversibility rating="reversible">Git-tracked markdown edit; `git checkout` restores prior state.</reversibility>
  </action>
  <verify>
    <automated>test "$(grep -c 'Bridge cannot honor' docs/research/claude-hooks-vs-pi-events.md)" = "0" && test "$(grep -c 'has no .turn ended by error. terminal event' docs/research/claude-hooks-vs-pi-events.md)" = "0" && test "$(grep -c 'issue-103-stop-stopfailure-promotion.md' docs/research/claude-hooks-vs-pi-events.md)" -ge "2"</automated>
    <human-check>The cross-mapping Stop/StopFailure rows read as shipped (agent_settled dispatch / stopReason contract) with issue-103 pointers; the naive summary and feasibility/bucket-D/path-forward cells that shipping falsifies are corrected; the E/F/G/H and non-Stop analysis is untouched.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Bridge cannot honor' docs/research/claude-hooks-vs-pi-events.md` returns 0 (the retired Stop cross-mapping claim is gone).
    - `grep -c 'has no .turn ended by error. terminal event' docs/research/claude-hooks-vs-pi-events.md` returns 0 (the retired StopFailure synthesis claim is gone).
    - `grep -c 'issue-103-stop-stopfailure-promotion.md' docs/research/claude-hooks-vs-pi-events.md` returns at least 2 (DOC-05-required pointers land at the corrected Stop/StopFailure claims).
    - The bucket-E/F/G/H tables and the "Soft-dep extension event surfaces" audit are unchanged (`git diff` shows no hunk in those sections).
    - No strikethrough syntax (`~~`) and no "superseded" relic marker were introduced (correct-in-place, D-89-05).
  </acceptance_criteria>
  <done>The cross-mapping Stop/StopFailure rows and every shipping-falsified naive/feasibility/bucket-D/path-forward/marketplace cell are corrected in place with issue-103 pointers; the still-accurate historical analysis is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human reader -> docs | Static markdown research note consumed by humans; no runtime code path executes this file. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-05 | Tampering | claude-hooks-vs-pi-events.md content accuracy | low | mitigate | Grep assertions gate the retired-claim removal and pointer insertion; human review at /gsd-verify-work confirms prose against the research Stale-Claim Inventory §B and that the preserved historical analysis is untouched. |
| T-89-06 | Repudiation | preserved-history relics / over-editing | low | accept | D-89-05 mandates correct-in-place (no strikethrough, no "superseded" relics); Pitfall 3 bounds the edit to shipping-falsified claims only; git carries history. |
| T-89-SC | Tampering (supply chain) | package installs | n/a | accept | No npm/pip/cargo install occurs in this docs-only phase; supply-chain vector not present. |
</threat_model>

<verification>
- `grep -c 'Bridge cannot honor' docs/research/claude-hooks-vs-pi-events.md` returns 0.
- `grep -c 'issue-103-stop-stopfailure-promotion.md' docs/research/claude-hooks-vs-pi-events.md` returns at least 2.
- `npm run check` stays green (NFR-6) — this doc is not test-coupled; the full gate confirms nothing else regressed.
- Human review (at /gsd-verify-work): the amended doc matches the research Stale-Claim Inventory §B; buckets E/F/G/H and the soft-dep/non-Stop analysis are unchanged.
</verification>

<success_criteria>
- The naive-table "agent_end is observation-only" Stop claim is retired; agent_settled is inventory row #31 (count 31).
- The StopFailure cross-mapping reflects the stopReason protocol contract, not after_provider_response synthesis.
- DOC-05-required pointers to the issue-103 doc land at the corrected Stop/StopFailure claims.
- Only shipping-falsified claims changed; the historical feasibility analysis is preserved; no strikethrough/superseded relics.
</success_criteria>

<output>
Create `.planning/phases/89-documentation-reconcile/89-03-SUMMARY.md` when done.
</output>
