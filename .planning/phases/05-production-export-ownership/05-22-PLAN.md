---
phase: 05-production-export-ownership
plan: "22"
type: execute
wave: 3
depends_on: ["05-06"]
files_modified:
  - "extensions/pi-claude-marketplace/shared/path-safety.ts"
  - "tests/shared/path-safety.test.ts"
  - "extensions/pi-claude-marketplace/shared/path-containment.ts"
  - "tests/shared/path-containment.test.ts"
  - "extensions/pi-claude-marketplace/shared/errors.ts"
  - "tests/shared/errors.test.ts"
  - "extensions/pi-claude-marketplace/shared/errors-bridges.ts"
  - "tests/shared/errors-bridges.test.ts"
  - "extensions/pi-claude-marketplace/shared/markers.ts"
  - "tests/shared/markers.test.ts"
  - "tests/integration/concurrent-install.test.ts"
  - "tests/integration/load-reconcile-race.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Own filesystem containment and retire orphan errors preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/path-safety.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/shared/path-containment.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/shared/errors.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/shared/errors-bridges.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/shared/path-safety.test.ts"
      to: "extensions/pi-claude-marketplace/shared/path-safety.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own filesystem containment and retire orphan errors.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/shared/path-safety.ts
@extensions/pi-claude-marketplace/shared/path-containment.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/shared/errors-bridges.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Separate containment policy from the real filesystem binding</name>
  <files>extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts, extensions/pi-claude-marketplace/shared/path-containment.ts, tests/shared/path-containment.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, move the complete injected containment policy, its required inspector/guard types, path classification, symlink checks and shared error classes to shared/path-containment.ts. path-safety.ts stays the existing public Node adapter: import createPathSafetyGuard, bind its actual inspector and keep the public assertPathInside plus still-consumed error/type re-exports. The exported factory therefore gains a real production reader. Make LexicalTraversalError private within the policy owner; preserve its observable name, parent class, normalized fields and no-filesystem-work ordering. Move policy tests with the complete concern and retain real temporary-filesystem adapter tests at the old owner. No external import paths or containment guarantees may change. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/shared/path-safety.test.ts tests/shared/path-containment.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Retire the two uncalled error classes</name>
  <files>extensions/pi-claude-marketplace/shared/errors.ts, tests/shared/errors.test.ts, extensions/pi-claude-marketplace/shared/errors-bridges.ts, tests/shared/errors-bridges.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/errors.ts, tests/shared/errors.test.ts, extensions/pi-claude-marketplace/shared/errors-bridges.ts, tests/shared/errors-bridges.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-02 and D-03, reconfirm that ConcurrentUninstallError and AgentForeignContentError have no production construction/consumption path, then retire those classes and their exclusive constructor tests. Preserve all live lock, foreign-content, manual-recovery and path-containment error behavior through the existing real classes. Keep the canonical assertNever tests from 05-06. Record the no-caller evidence and each retired assertion; do not privatize an unused class merely to leave dead code behind. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/shared/errors.test.ts tests/shared/errors-bridges.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Observe lock marker bytes through the public operation</name>
  <files>extensions/pi-claude-marketplace/shared/markers.ts, tests/shared/markers.test.ts, tests/integration/concurrent-install.test.ts, tests/integration/load-reconcile-race.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/markers.ts, tests/shared/markers.test.ts, tests/integration/concurrent-install.test.ts, tests/integration/load-reconcile-race.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01 and D-03, make STATE_LOCK_HELD_PREFIX private and preserve marker encoding/parsing via the public marker operations. Replace the two integration imports with independent expected bytes or real public lock outcomes, retaining their precise concurrent-install/load-reconcile assertions. Keep all error/status/diagnostic checks that distinguish a real lock refusal from subprocess or setup failure. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/shared/markers.test.ts tests/integration/concurrent-install.test.ts tests/integration/load-reconcile-race.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-22-01 | Tampering | extensions/pi-claude-marketplace/shared/path-safety.ts | high | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-22-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-22-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-22-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
