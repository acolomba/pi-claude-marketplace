---
phase: 14.1
slug: close-gap-cmc-13-propagate-declaresagents-mcp-through-import
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-24
audited: 2026-05-24
---

# Phase 14.1 -- Validation Strategy

> Per-phase validation contract; post-execution Nyquist audit reconciled against HEAD on 2026-05-24. The phase was a typed-plumbing change that propagates the `declaresAgents` / `declaresMcp` predicates from `InstallPluginOutcome.installed` through the `/claude:plugin import` orchestrator's switch arm and cascade-row build so per-row `{requires pi-subagents}` / `{requires pi-mcp}` soft-dep markers fire on import-surface `(installed)` rows identically to the standalone install command. The contract is single-requirement (CMC-13); the verification map decomposes that requirement into six observable dimensions.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (built-in) + native TS strip per `package.json` (Node >=22.18) |
| **Config file** | `package.json` `scripts.test` / `scripts.check` (no separate config) |
| **Quick run command** | `node --test tests/<layer>/<file>.test.ts` (per file) or `npm test -- --test-name-pattern "<area>"` |
| **Full suite command** | `npm run check` (typecheck + ESLint + Prettier + tests) |
| **Estimated runtime** | ~25-60 seconds for `npm run check`; <3s per targeted test file |
| **Current baseline** | 1245+/1245+ tests pass at HEAD (per phase 14.1-VERIFICATION.md `npm run check` exit 0; audited 2026-05-24) |

---

## Sampling Rate

- **After every task commit:** Run the targeted test file (`node --test tests/orchestrators/import/execute.test.ts` for Plan 14.1-01; `node --test tests/architecture/catalog-uat.test.ts` for Plan 14.1-02)
- **After every plan wave:** Run `npm run check`
- **Before `/gsd:verify-work`:** `npm run check` must be green (NFR-6 gate)
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

> Audited against HEAD on 2026-05-24. Every row resolves to a behavioral or source-byte test that runs as part of `npm run check`.

| # | Plan | Requirement | CMC-13 Dimension | Threat Ref | Expected Behavior | Test Type | Automated Command | Test File / Source Loc | Status |
|---|------|-------------|------------------|------------|-------------------|-----------|-------------------|------------------------|--------|
| 1 | 14.1-01 | **CMC-13** | Producer-side REQUIRED fields (NFR-7) | T-14.1-06 (Elevation: widening to `boolean \| undefined`) | `InstallPluginOutcome.installed` carries `readonly declaresAgents: boolean` and `readonly declaresMcp: boolean` as REQUIRED (NOT `boolean \| undefined`); `PluginInstalledOutcome` in `import/execute.ts` mirrors the contract. `grep -nE '(declaresAgents\|declaresMcp)\??: boolean \| undefined'` returns zero matches in `install.ts` + `import/execute.ts`. Typecheck (`npx tsc --noEmit`) is the structural gate: any consumer attempting to read these as optional fails to compile. | source-byte grep + typecheck | `grep -nE '(declaresAgents\|declaresMcp)\\??: boolean \\| undefined' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts extensions/pi-claude-marketplace/orchestrators/import/execute.ts; npx tsc --noEmit -p tsconfig.json` | `install.ts:126-127`, `import/execute.ts:64-65`; `npx tsc` clean | ✅ green |
| 2 | 14.1-01 | **CMC-13** | Single-source derivation (D-02) + spoofing mitigation (T-14.1-01) | T-14.1-01 (Spoofing: forged outcome) | Both predicates derived at the install-outcome construction site in `install.ts` from `installCtx.stagedAgentNames.length > 0` / `installCtx.stagedMcpServerNames.length > 0`. The expression form is identical to the existing standalone cascade-row site (`install.ts:762-763`). `grep -cE 'declaresAgents: installCtx\\.stagedAgentNames\\.length > 0' install.ts` returns `2` (the standalone cascade-row site at line 762 AND the new outcome-return site at line 786); same count for `declaresMcp`. Mitigation: predicate values are derived inside the same `withStateGuard` closure that populated `installCtx` -- no external producer can supply a forged outcome. | source-byte grep (count) | `grep -cE 'declaresAgents: installCtx\\.stagedAgentNames\\.length > 0' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` (must be `2`) | `install.ts:762-763` (standalone) + `install.ts:786-787` (outcome return) | ✅ green |
| 3 | 14.1-01 | **CMC-13** | Propagation through `importPluginsFromMarketplace` switch arm + cascade-row build (D-04, D-05, D-11) | T-14.1-02 (Tampering: predicate corruption in transit) | The case-`installed` switch arm at `import/execute.ts:840-851` extends the `installedPlugins.push({...})` object with `declaresAgents: outcome.declaresAgents` / `declaresMcp: outcome.declaresMcp` (direct assignment, not conditional spread). The cascade-row build at `import/execute.ts:465-475` reads `o.declaresAgents` / `o.declaresMcp` -- production code contains ZERO `declaresAgents: false` / `declaresMcp: false` literals on this file. The deferred-work TODO comment is replaced with a one-line `// CMC-13 / MSG-SD-1..3` reference at line 464. The four new behavioral tests `propagates declaresAgents=true (agents-only)` / `mcp-only` / `both` / `neither` in `tests/orchestrators/import/execute.test.ts:515,550,585,622` inject a test-double `installPlugin` returning each predicate combination and assert: (a) `result.installedPlugins[0].declaresAgents` / `.declaresMcp` equal the injected booleans, AND (b) the rendered body from `formatClaudeImportSummary(result, UNLOADED_PROBE)` matches `/(installed) {requires pi-subagents}/` / `/(installed) {requires pi-mcp}/` / combined per case. | unit (behavior) + source-byte grep | `node --test tests/orchestrators/import/execute.test.ts; grep -cE 'declaresAgents: false\|declaresMcp: false' extensions/pi-claude-marketplace/orchestrators/import/execute.ts` (must be `0`) | `tests/orchestrators/import/execute.test.ts:515-656` (4 propagation tests) + `import/execute.ts:464-475` (cascade-row) + `:840-851` (switch arm) | ✅ green |
| 4 | 14.1-01 | **CMC-13** | Renderer marker emission (the consumer end of the typed pipe) | T-14.1-02 (Tampering: renderer suppression) | `presentation/compact-line.ts:443-464` `composeReasons` emits `requires pi-subagents` iff (`declaresAgents === true` AND `!probe.piSubagentsLoaded`); same for `pi-mcp`. The renderer-level invariants are locked by five behavioral tests in `tests/presentation/compact-line.test.ts:329-395`: agents-only fires, mcp-only fires, both-fire combined, companion-loaded suppresses, declares=false suppresses regardless of probe. Plan 14.1's typed-plumbing change does not modify the renderer; these tests are the consumer-side non-regression backstop confirming the propagated predicates actually reach the marker emission site. | unit (behavior) | `node --test tests/presentation/compact-line.test.ts` | `tests/presentation/compact-line.test.ts:329-395` (CMC-13 / MSG-SD-1..3 marker emission) | ✅ green |
| 5 | 14.1-02 | **CMC-13** | `docs/output-catalog.md` `(section, state)` pair documents the byte-exact import-time output (D-07) | T-14.1-02-01 (Spoofing: docs/renderer drift) | A new `<!-- catalog-state: soft-dep-markers -->` block exists under `## /claude:plugin import` at `docs/output-catalog.md:629`. The fenced body (lines 631-639) demonstrates the agents-only form `(installed) {requires pi-subagents}` (line 635) AND the combined form `(installed) {requires pi-subagents, requires pi-mcp}` (line 636) -- closed-set ordering `pi-subagents` before `pi-mcp` per MSG-SD-1. Catalog-UAT runner at `catalog-uat.test.ts:1741` extracts every `(section, state)` pair and looks up the matching fixture function; the new pair is registered by the runner automatically. Negative: the wrong-order substring `requires pi-mcp, requires pi-subagents` is absent from non-heading lines (`grep -E ... \| grep -v '^#' \| wc -l` returns `0`). | source-byte grep + integration (round-trip) | `grep -cE '^<!-- catalog-state: soft-dep-markers -->$' docs/output-catalog.md` (must be `1`); `node --test tests/architecture/catalog-uat.test.ts` | `docs/output-catalog.md:627-645` (fixture body + notes) | ✅ green |
| 6 | 14.1-02 | **CMC-13** | Byte-equal pairing fixture renders via production composer (D-08) | T-14.1-02-02 (Tampering: hand-composed body bypassing renderer) | `tests/architecture/catalog-uat.test.ts:1194-1229` contains the `"soft-dep-markers": () =>` fixture function under the `"/claude:plugin import"` fixture-map entry. The function constructs ONE marketplace block with two `PluginCascadeRow` records (`declaresAgents: true, declaresMcp: false` for the agents-only row; `declaresAgents: true, declaresMcp: true` for the dual row), renders through `cascadeSummary({ marketplace, rows, probe: PROBE_BOTH_UNLOADED }).message`, and returns `appendReloadHint(...)`. No hand-composed string literals -- the body flows through the production composer per D-08. The byte-equal-pairing test at `catalog-uat.test.ts:1741` is the structural anti-drift mitigation: any divergence between the docs fence and the rendered output fails the test. Verified: the fixture invokes `cascadeSummary(` and references `PROBE_BOTH_UNLOADED` inside the function body (1 match each in the new fixture; total file references brought to 5). | integration (round-trip) | `node --test tests/architecture/catalog-uat.test.ts` (test 1 `every <!-- catalog-state: --> annotation pairs byte-equal with a fixture`) | `tests/architecture/catalog-uat.test.ts:1194-1229` (fixture function) + `:1741` (pairing runner) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · MISSING (no test)*

---

## Non-Regression Verifications

| Asset | Why Phase 14.1 must not regress it | Verification | Status |
|-------|-----------------------------------|--------------|--------|
| `tests/presentation/compact-line.test.ts` (CMC-13 / MSG-SD-1..3 marker coverage at lines 329-395) | The renderer is the consumer end of Phase 14.1's typed pipe. Plan 14.1-01 does not modify `composeReasons`; if any test in the file regressed, the propagated predicates would no longer surface the documented markers. | `node --test tests/presentation/compact-line.test.ts` -- 33/33 tests pass at HEAD | ✅ green |
| `tests/architecture/markers-snapshot.test.ts` (closed-set marker snapshot) | Phase 14.1 does not introduce or rename any marker; the soft-dep markers `{requires pi-subagents}` / `{requires pi-mcp}` already exist in the snapshot from Phase 13. | `node --test tests/architecture/markers-snapshot.test.ts` -- 5/5 tests pass at HEAD | ✅ green |
| `tests/architecture/grammar-frontmatter.test.ts` (closed-set drift gate) | Phase 14.1 does not extend the STATUS_TOKENS / REASONS closed sets; the existing 28-reason set already includes the soft-dep entries. The drift test must stay green. | `node --test tests/architecture/grammar-frontmatter.test.ts` -- 6/6 tests pass at HEAD | ✅ green |
| `extensions/pi-claude-marketplace/orchestrators/types.ts:32-42` (`PluginCascadeRow.declaresAgents/Mcp` consumer schema) | D-12 explicitly forbids modifying the consumer schema; the gap was only in the import-side producer wiring. The optional `boolean` form on `PluginCascadeRow` accepts the narrowed `boolean` from `o.declaresAgents` without schema change. | Source inspection (file unchanged in Phase 14.1 commits) + `npx tsc --noEmit` exit 0 -- the cascade-row build at `import/execute.ts:467-474` typechecks against the unchanged consumer schema | ✅ green |
| Standalone install path CMC-13 conformance (`install.ts:762-763` cascade-row site) | D-13 forbids modifying any other CMC-13 callsite (install / reinstall / update / list / uninstall) -- they are already CMC-13-compliant. The Phase 14.1 widening adds the predicates as a new derivation at the outcome-return site only; the existing standalone cascade-row derivation at line 762 is unchanged. | `grep -cE 'declaresAgents: installCtx\.stagedAgentNames\.length > 0' install.ts` returns `2` (the original standalone site plus the new outcome-return site); the original line is at 762, unchanged | ✅ green |
| Failure-path locals at `install.ts:247-248` (`failureDeclaresAgents` / `failureDeclaresMcp`) | D-14 forbids touching rollback / failure paths. The MSG-SD-3 contract says markers do not fire on `(failed)` rows; the failure-path locals stay `false` defaults. | Source inspection (`install.ts:247-248,643-644` unchanged in Phase 14.1 commits) + the renderer tests at `compact-line.test.ts:329-395` already cover the negative case | ✅ green |
| `tests/architecture/catalog-uat.test.ts:1741` byte-equal pairing runner (unchanged) | Per Plan 14.1-02 `<action>`, adding a new `(section, state)` pair on both sides is the entire integration -- the runner itself must not be modified. | The runner at line 1741 iterates every parsed catalog example against the fixture map; the new `soft-dep-markers` entry is exercised automatically. Test passes (3/3 in `catalog-uat.test.ts`). | ✅ green |

---

## Wave 0 Requirements

*All Phase 14.1 test infrastructure existed before the phase ran (the typed-plumbing change widened existing test-doubles + added 4 new tests in an existing file + added a new fixture function to an existing test file). No new test framework, fixture module, or top-level test file was required.*

- [x] `tests/orchestrators/import/execute.test.ts` -- existed pre-phase; Plan 14.1-01 widened all 12 historical `status: "installed"` test-doubles (lines 156, 203, 248, 309, 345, 398, 453, 578, 624, 656, 685, 741 per the plan; updated line numbers at HEAD: 160-161, 212-213, 262-263, 328-329, 369-370, 427-428, 487-488, 670-671, 774-775, etc.) AND added 4 new behavioral tests at lines 515-656
- [x] `tests/architecture/catalog-uat.test.ts` -- existed pre-phase; Plan 14.1-02 added the `"soft-dep-markers"` fixture function at lines 1194-1229 as a peer of `"fresh-mixed-both-scopes"`; the byte-equal-pairing runner at line 1741 was unchanged
- [x] `tests/presentation/compact-line.test.ts` -- existed pre-phase (Phase 12/13); five CMC-13 / MSG-SD-1..3 marker tests at lines 329-395 are the consumer-side non-regression backstop; not modified in Phase 14.1
- [x] `docs/output-catalog.md` -- existed pre-phase; Plan 14.1-02 inserted a new `<!-- catalog-state: soft-dep-markers -->` block at line 629 under the existing `## /claude:plugin import` section

*Wave 0 is COMPLETE; the file inventory matches reality at HEAD and all behavioral tests pass.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions | Status |
|----------|-------------|------------|-------------------|--------|
| `docs/output-catalog.md:627-645` (soft-dep-markers fixture body) reads as natural prose -- the headings, notes paragraph, and bullet count match the existing voice of adjacent fixtures (`fresh-mixed-both-scopes`, `scope-project-narrow`, `source-mismatch`) | D-07 (per Claude's Discretion) + style-guide voice consistency | Voice / readability is a reviewer judgment; no automated assertion can capture "matches existing voice." The byte-equal-pairing runner at `catalog-uat.test.ts:1741` covers structural correctness (the fenced body equals the rendered output), but not editorial quality. | `glow docs/output-catalog.md` or render in any markdown viewer; compare the new fixture's headings / notes paragraph against the three adjacent fixtures in the same section. | ✅ verified (per Phase 14.1-02-SUMMARY.md Self-Check: PASSED; reviewer-confirmed during the `8987c4e` commit cycle that the notes paragraph matches existing voice and the headings are consistent with peers) |
| Phase 14.1's CMC-13 closure is the final piece of the v1.3 Milestone Audit `gaps.requirements[id=CMC-13]` (the audit downgraded CMC-13 from Complete to PARTIAL on the import surface only) -- the v1.3 audit ledger must reflect SATISFIED after this phase | CMC-13 audit reconciliation | Cross-document audit-ledger reconciliation is a PR-shape and roadmap-tracking concern, not a unit invariant. | Confirmed at `14.1-VERIFICATION.md` (status: passed, 13/13 must-haves verified) + 14.1-01-SUMMARY.md `Next Phase Readiness` section ("CMC-13 closure: this plan satisfies the entirety of the audit's `gaps.requirements[id=CMC-13]` finding") | ✅ verified |

---

## Audit Findings (2026-05-24)

**Audit method:** Each row in the per-task map was checked against HEAD: (a) the cited test file was read to confirm it asserts the requirement; (b) the test was run in isolation via `node --test <file>` to confirm it passes; (c) the implementation file was inspected to confirm the behavior under test matches the requirement.

**Result:** All 6 per-task rows resolved to ✅ green via behavioral or source-byte tests that run as part of `npm run check`. No new tests had to be generated -- Plans 14.1-01 (4 new behavioral propagation tests in `tests/orchestrators/import/execute.test.ts`) and 14.1-02 (one new fixture function in `tests/architecture/catalog-uat.test.ts` + new docs fixture) combined cover every CMC-13 dimension on the import surface with a test that can fail.

**Wave 0:** All test files and the consumer schema existed before Phase 14.1 ran. The phase's deliverable was typed-plumbing widening + test-double widening + 4 new tests + 1 new docs fixture + 1 new test fixture function. The Wave 0 inventory is unchanged at HEAD.

**Forward-progress drift (not gaps):** None. Phase 14.1 was an internal typed-plumbing change with single-requirement scope (CMC-13). No closed-set membership changed, no renderer behavior changed, no consumer schema changed.

**Threat model status (from 14.1-SECURITY.md, status: passed, threats_open: 0):** T-14.1-01..06 (STRIDE) all dispositioned. The Elevation-of-Privilege threat T-14.1-06 (`boolean | undefined` widening) is structurally mitigated by NFR-7 + the source-byte grep in Row 1. The Spoofing threat T-14.1-01 (forged outcome) is N/A because predicate derivation happens inside the same `withStateGuard` closure that populated `installCtx`. The two T-14.1-02 Tampering threats (predicate corruption in transit / renderer suppression) are covered by Rows 3 and 4 of the per-task map.

**Security:** 14.1-SECURITY.md status=passed, threats_open=0; 14.1-VERIFICATION.md status=passed (13/13 truths verified).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (Plans 14.1-01 + 14.1-02 both ship behavioral verify blocks; no manual-only task verification)
- [x] Sampling continuity: every plan task has at least one automated verify (no 3-task dry run)
- [x] Wave 0 covers all dependencies (test files + docs fixture pre-existed; the phase widened them)
- [x] No watch-mode flags in any task command
- [x] Feedback latency < 60s per `npm run check` (~25-30s observed at HEAD)
- [x] `nyquist_compliant: true` set in frontmatter -- every gap row resolves to a behavioral or source-byte test that runs in `npm run check`

**Approval:** validated (2026-05-24)
