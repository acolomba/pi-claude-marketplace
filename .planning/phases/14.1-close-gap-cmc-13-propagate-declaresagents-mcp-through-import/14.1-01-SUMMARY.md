---
phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import
plan: 01
subsystem: orchestrators
tags: [cmc-13, msg-sd, soft-dep, discriminated-union, import, typed-plumbing]

# Dependency graph
requires:
  - phase: 13-conformance-refactor-es-5-supersession
    provides: "Per-row soft-dep marker grammar `{requires pi-subagents}` / `{requires pi-mcp}` and CMC-13 wiring on install / reinstall / update / list / uninstall surfaces"
provides:
  - "REQUIRED `declaresAgents` / `declaresMcp` boolean fields on `InstallPluginOutcome.installed` variant"
  - "REQUIRED `declaresAgents` / `declaresMcp` boolean fields on `import/execute.ts:PluginInstalledOutcome`"
  - "Predicate propagation through the import orchestrator's case-`installed` switch arm onto `installedPlugins[]`"
  - "Predicate propagation onto the import cascade-row build, replacing hard-coded `false` literals"
  - "Four new unit tests covering all four predicate combinations (agents-only, mcp-only, both, neither)"
affects: [phase-14-cmc-38-drift-guard, future-soft-dep-work, import-surface-conformance]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Discriminated-union widening with REQUIRED boolean predicates (NFR-7) -- no `boolean | undefined`"
    - "Single-source predicate derivation: `installCtx.stagedAgentNames.length > 0` reused at outcome-return AND standalone cascade-row sites"
    - "Direct-assignment (not conditional-spread) propagation when fields are REQUIRED by both producer and consumer"
    - "Typed-plumbing change: producer (install.ts outcome) → typed switch-arm extraction → cascade-row composer -- no string parsing of `postCommitWarnings`"

key-files:
  created: []
  modified:
    - "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts -- widened `InstallPluginOutcome.installed` variant + success-return site"
    - "extensions/pi-claude-marketplace/orchestrators/import/execute.ts -- widened `PluginInstalledOutcome`, extended case-`installed` switch arm, replaced cascade-row literals with propagated predicates, removed deferred-work TODO"
    - "tests/orchestrators/import/execute.test.ts -- widened 12 historical `status: \"installed\"` test-doubles + 1 literal `PluginInstalledOutcome`; added 4 new predicate-propagation test cases"

key-decisions:
  - "Per D-01: predicates are REQUIRED (not optional) on the `installed` variant; NFR-7 enforcement"
  - "Per D-02: single-source derivation from `installCtx.staged*Names.length > 0` -- no second derivation path"
  - "Per D-06: typed plumbing only -- no `postCommitWarnings` string parsing"
  - "Per D-14: other variants (`already-installed`, `unavailable`, `uninstallable`, `unexpected-failure`) unchanged -- soft-dep markers do not fire on those states under MSG-SD-3"
  - "Rule 3 auto-fix: also widened the literal `installedPlugins[]` construction at line 502-516 of execute.test.ts; not in the plan's enumerated 12 historical doubles but required for typecheck to pass after `PluginInstalledOutcome` widening"

patterns-established:
  - "Typed-plumbing change: when fixing a producer-consumer gap, widen the typed boundary and propagate via direct assignment, not via free-text string parsing or default-`false` literals"
  - "Test-double widening pattern: when a discriminated-union variant gains REQUIRED fields, ALL existing test-doubles that return that variant must be widened in the SAME task as the type change -- typecheck-coherent atomic edit cluster"
  - "Cascade-row assertion via `formatClaudeImportSummary(result, probe)`: pass an unloaded probe to surface the markers and assert on the rendered body without mocking the renderer"

requirements-completed: [CMC-13]

# Metrics
duration: ~30min
completed: 2026-05-24
---

# Phase 14.1 Plan 01: Plumb CMC-13 declaresAgents/Mcp through import cascade rows -- Summary

**Closed the CMC-13 gap on `/claude:plugin import` by widening `InstallPluginOutcome.installed` with REQUIRED `declaresAgents` / `declaresMcp` predicates and propagating them through the import orchestrator's switch arm and cascade-row build, replacing hard-coded `false` literals with typed plumbing.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-05-24T13:00:00Z
- **Completed:** 2026-05-24T13:35:45Z
- **Tasks:** 1 (atomic typecheck-coherent edit cluster)
- **Files modified:** 3

## Accomplishments

- `InstallPluginOutcome.installed` discriminated-union variant gained REQUIRED `readonly declaresAgents: boolean` and `readonly declaresMcp: boolean` (NFR-7: no `boolean | undefined`).
- Predicates set at the single success-return site in `install.ts` from `installCtx.stagedAgentNames.length > 0` / `installCtx.stagedMcpServerNames.length > 0` -- same expression form as the existing standalone cascade-row site at `install.ts:751-752` (single-source rule, D-02).
- `PluginInstalledOutcome` in `import/execute.ts` mirrors the widening; the case-`installed` switch arm propagates `outcome.declaresAgents` / `outcome.declaresMcp` onto every `installedPlugins[]` entry (direct assignment, not conditional spread -- fields are REQUIRED).
- Cascade-row build at `import/execute.ts:459-477` now reads `o.declaresAgents` / `o.declaresMcp` -- zero literal `false` values in production code.
- Multi-line deferred-work TODO comment removed and replaced with a single-line `// CMC-13 / MSG-SD-1..3: predicates propagated from install outcome` reference (D-11).
- All 12 historical `status: "installed"` test-doubles widened with `declaresAgents: false, declaresMcp: false`; the additional literal `PluginInstalledOutcome` in the `formatClaudeImportSummary` reload-hint test also widened (typecheck consequence -- Rule 3 auto-fix).
- 4 new unit tests cover all predicate combinations (agents-only, mcp-only, both, neither) and assert both the outcome surface AND the rendered cascade-row body via `formatClaudeImportSummary(result, unloadedProbe)`.
- `npm run check` exits 0 (1146/1146 tests pass).

## Task Commits

Each task was committed atomically:

1. **Task 1: Widen `InstallPluginOutcome.installed` + `PluginInstalledOutcome` + import cascade-row literals; update all 12 historical test-doubles; add 4 new predicate-propagation tests** -- `db913bb` (feat)

## Files Created/Modified

- `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` -- widened `InstallPluginOutcome.installed` variant (lines 113-128); added `declaresAgents` / `declaresMcp` direct assignments at the success-return site (lines 776-779); updated JSDoc block above the type to document the CMC-13 contract.
- `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` -- widened `PluginInstalledOutcome` interface (lines 49-66); extended case-`installed` switch arm (lines 847-860) to propagate predicates; replaced hard-coded `false` literals and removed multi-line TODO comment in the cascade-row build (lines 459-471 in the new layout); added single-line CMC-13 reference.
- `tests/orchestrators/import/execute.test.ts` -- widened all 12 historical `installPlugin` test-doubles returning `status: "installed"` with `declaresAgents: false, declaresMcp: false`; widened the literal `PluginInstalledOutcome` construction at line 505 (Rule 3 typecheck auto-fix); imported `SoftDepProbe` type; added 4 new test cases covering agents-only, mcp-only, both, neither predicate combinations; new tests assert both `result.installedPlugins[0]` outcome surface AND rendered cascade-row body via `formatClaudeImportSummary(result, UNLOADED_PROBE)`.

## Decisions Made

- Followed all 14 plan decisions (D-01..D-14) verbatim.
- Rule 3 auto-fix: extended Task 1's scope to widen the literal `PluginInstalledOutcome` construction at `tests/orchestrators/import/execute.test.ts` line 505 (the `formatClaudeImportSummary` reload-hint test). This was NOT one of the plan's enumerated 12 historical `installPlugin` test-doubles, but the `PluginInstalledOutcome` interface widening (D-03) made it a typecheck failure. Documented as a deviation below.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 -- Blocking] Widened literal `PluginInstalledOutcome` construction in reload-hint test**

- **Found during:** Task 1 (after Group 3 widening of `PluginInstalledOutcome`)
- **Issue:** The test at line 502 (`formatClaudeImportSummary includes the canonical reload-hint trailer when changedResources is true`) constructs an `installedPlugins[]` literal that matches the `PluginInstalledOutcome` interface. Plan enumerated 12 historical `installPlugin` test-doubles to widen, but did not account for this literal `PluginInstalledOutcome` construction site, which became a typecheck error once `declaresAgents` / `declaresMcp` were made REQUIRED on the interface.
- **Fix:** Added `declaresAgents: false, declaresMcp: false` to the literal (mirrors the test-double pattern). Test behavior unchanged -- it asserts reload-hint trailer and cascade-row rendering, neither of which exercises the predicate.
- **Files modified:** tests/orchestrators/import/execute.test.ts (line 514-515)
- **Verification:** `npx tsc --noEmit` exits 0; `npm test -- tests/orchestrators/import/execute.test.ts` exits 0; the test continues to assert its original matchers (`/\/reload to pick up changes/` and `/● my-plugin \[user\] \(installed\)/`).
- **Committed in:** db913bb (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking -- typecheck consequence of REQUIRED-fields widening)
**Impact on plan:** Necessary for typecheck. The deviation is the same shape as the 12 historical test-double widenings the plan explicitly authorized; the plan's enumeration just missed this one literal-construction site. No scope creep, no behavior change.

## Issues Encountered

- None. The single atomic typecheck-coherent edit cluster landed cleanly. `npm run check` passed on first try after all six edit groups + new tests were applied.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 14.1 closure: this plan satisfies the entirety of the audit's `gaps.requirements[id=CMC-13]` finding. The integration finding `import surface hard-codes soft-dep predicates to false` is now structurally impossible -- production code contains zero literal-false assignments for these predicates.
- Phase 14 (CMC-38 drift guard) can now proceed without retroactive failure on the import surface -- every `(installed)` row inside `/claude:plugin import` cascades will fire the marker correctly when companion extensions are unloaded.
- Out-of-scope items per the phase CONTEXT.md remain deferred: CMC-16 (`renderManualRecovery` orphan), CMC-34 (6 edge handlers using `notifyError` instead of `notifyUsageError`), optional `install.ts:751-752` deduplication, MARKETPLACE_LABEL_PROBE DRY cleanup, `transaction/rollback.ts` literal-drift hardening.

## Self-Check: PASSED

**Files verified:**
- FOUND: extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (modified)
- FOUND: extensions/pi-claude-marketplace/orchestrators/import/execute.ts (modified)
- FOUND: tests/orchestrators/import/execute.test.ts (modified)

**Commit verified:**
- FOUND: db913bb (feat(14.1-01): plumb CMC-13 declaresAgents/Mcp through import)

**Acceptance criteria verified via grep (all gates green):**
- `readonly declaresAgents: boolean` present in both install.ts (line 126) and execute.ts (line 63)
- `readonly declaresMcp: boolean` present in both install.ts (line 127) and execute.ts (line 64)
- Single-source derivation: `declaresAgents: installCtx.stagedAgentNames.length > 0` appears exactly 2x in install.ts (standalone cascade-row + outcome-return site)
- Single-source derivation: `declaresMcp: installCtx.stagedMcpServerNames.length > 0` appears exactly 2x in install.ts
- Other variants untouched: no `status: "(already-installed|unavailable|uninstallable|unexpected-failure)"; readonly declares` matches
- Switch-arm propagation: `declaresAgents: outcome.declaresAgents` and `declaresMcp: outcome.declaresMcp` present in execute.ts (lines 856-857)
- Cascade-row propagation: `declaresAgents: o.declaresAgents` and `declaresMcp: o.declaresMcp` present in execute.ts (lines 475-476)
- Zero `declaresAgents: false` in production execute.ts (literal-false removed)
- Zero `declaresMcp: false` in production execute.ts (literal-false removed)
- Zero `boolean | undefined` widenings in either file (NFR-7 enforced)
- Deferred-work comment `follow-up (sub-wave 2b` removed (0 matches)
- Positive comment `CMC-13 / MSG-SD-1..3: predicates propagated from install outcome` present (1 match)
- No `postCommitWarnings`-based predicate parsing (D-06 enforced)
- 12+ historical test-double widenings present (14 sites have both `declaresAgents: false` and `declaresMcp: false`)
- 4 new predicate-propagation test cases present (agents-only, mcp-only, both, neither)
- `npm run check` exits 0 (1146/1146 tests pass)

---
*Phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import*
*Completed: 2026-05-24*
