---
phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import
plan: 02
type: execute
wave: 2
depends_on:
  - 14.1-01
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements:
  - CMC-13

must_haves:
  truths:
    - "A new `<!-- catalog-state: soft-dep-markers -->` fixture block exists under the `## /claude:plugin import` section of `docs/output-catalog.md`."
    - "The fixture body demonstrates per-row soft-dep markers on import cascade rows in two forms: `(installed) {requires pi-subagents}` (agents-only) and `(installed) {requires pi-subagents, requires pi-mcp}` (combined), using the existing reasons-ordering grammar from `docs/output-catalog.md` lines 229-230, 273, 299, 399."
    - "A paired fixture function under `\"/claude:plugin import\"` in `tests/architecture/catalog-uat.test.ts` constructs the rows via `PluginCascadeRow` records with `declaresAgents: true` / `declaresMcp: true` and the existing `PROBE_BOTH_UNLOADED` stub, then renders through `cascadeSummary` (no hand-composed string literals)."
    - "The byte-equal-pairing test at `tests/architecture/catalog-uat.test.ts:~1572` round-trips the new fixture: the fenced docs body equals the rendered output exactly."
    - "`npm run check` exits 0 (the new catalog-uat round-trip passes alongside the rest of the suite)."
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "New `soft-dep-markers` catalog fixture under `## /claude:plugin import` section."
      contains: "catalog-state: soft-dep-markers"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "Paired fixture function under the `\"/claude:plugin import\"` entry that constructs cascade rows with `declaresAgents`/`declaresMcp` set and renders through `cascadeSummary` + `PROBE_BOTH_UNLOADED`."
      contains: "\"soft-dep-markers\":"
  key_links:
    - from: "docs/output-catalog.md (soft-dep-markers fixture body)"
      to: "tests/architecture/catalog-uat.test.ts (soft-dep-markers fixture function)"
      via: "byte-equal-pairing test at line ~1572 — fenced docs body MUST equal renderer output exactly"
      pattern: "soft-dep-markers"
    - from: "tests/architecture/catalog-uat.test.ts soft-dep-markers fixture function"
      to: "extensions/pi-claude-marketplace/presentation/cascade-summary.ts cascadeSummary"
      via: "round-trip via the production renderer; no hand-composed literals (D-08)"
      pattern: "cascadeSummary\\(\\{ marketplace.*rows.*probe: PROBE_BOTH_UNLOADED"
---

<objective>
Add a `<!-- catalog-state: soft-dep-markers -->` fixture under `## /claude:plugin import` in `docs/output-catalog.md` and the paired fixture function in `tests/architecture/catalog-uat.test.ts` that round-trips the new soft-dep marker behavior through the production renderer.

Purpose: With the plumbing in Plan 14.1-01 landed, the catalog UAT becomes the end-to-end gate that proves the predicates reach `composeReasons` and emit the documented `{requires pi-subagents}` / `{requires pi-subagents, requires pi-mcp}` strings on import-cascade rows. This is the second of the two CMC-13 closure deliverables called out in the v1.3 milestone audit Gap Closure Plan §2.

Output: One new documented catalog example + one new test fixture that the existing byte-equal-pairing runner exercises automatically.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-CONTEXT.md
@.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-01-PLAN.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase by the planner. -->

From `docs/output-catalog.md:523-595` (CURRENT `/claude:plugin import` section — the new fixture goes here, after the existing `source-mismatch` fixture and before the `______` divider on line 597):

The section currently contains three fixtures: `fresh-mixed-both-scopes` (lines 527-558), `scope-project-narrow` (lines 560-575), `source-mismatch` (lines 577-595).

Each fixture uses this exact form:

```markdown
### <Title>

<!-- catalog-state: <kebab-name> -->

```text
<fenced body — what the user sees>
```
```

Reasons grammar (from `docs/output-catalog.md` lines 229-230, 273, 299, 399):
- Single reason: `(installed) {requires pi-subagents}`
- Combined reasons: `(installed) {requires pi-subagents, requires pi-mcp}` — comma-space separator, `pi-subagents` first, `pi-mcp` second (closed-set ordering already established on adjacent surfaces).
- Closed-set definition: `docs/output-catalog.md:91` lists the soft-dep reasons.

From `extensions/pi-claude-marketplace/presentation/compact-line.ts:80-83` (the canonical `SoftDepProbe` interface — VERIFIED field names):

```typescript
export interface SoftDepProbe {
  readonly piSubagentsLoaded: boolean;
  readonly piMcpAdapterLoaded: boolean;
}
```

NOTE: The field names are `piSubagentsLoaded` / `piMcpAdapterLoaded` — NOT `hasSubagents` / `hasMcp`. (An earlier draft of this plan quoted incorrect names; the correct names are above and match what the test file's probe constants already use.)

From `tests/architecture/catalog-uat.test.ts:156-169` (CURRENT — available probe stubs, already defined in the file):

```typescript
const PROBE_BOTH_LOADED: SoftDepProbe = {
  piSubagentsLoaded: true,
  piMcpAdapterLoaded: true,
};
const PROBE_SUBAGENTS_UNLOADED: SoftDepProbe = {
  piSubagentsLoaded: false,
  piMcpAdapterLoaded: true,
};
const PROBE_BOTH_UNLOADED: SoftDepProbe = {
  piSubagentsLoaded: false,
  piMcpAdapterLoaded: false,
};
```

For the combined `{requires pi-subagents, requires pi-mcp}` row, use `PROBE_BOTH_UNLOADED`. For the agents-only row, `PROBE_BOTH_UNLOADED` also works (pi-mcp is unloaded but the plugin doesn't declare mcp, so the marker doesn't fire) — pick `PROBE_BOTH_UNLOADED` for the whole fixture so both rows render under the same probe (matching the per-fixture single-probe convention seen elsewhere in the file). Reference the constant by name (`PROBE_BOTH_UNLOADED`); the executor does not need to re-derive the field shape.

From `tests/architecture/catalog-uat.test.ts:974-1104` (REFERENCE — existing `/claude:plugin import` fixture entries; the new entry slots in as a peer of `fresh-mixed-both-scopes`):

```typescript
"/claude:plugin import": {
  "fresh-mixed-both-scopes": () => {
    const blocks: Array<{ mp: MarketplaceRow; rows: PluginCascadeRow[] }> = [
      { mp: { kind: "marketplace", name: "...", scope: "...", marker: "autoupdate", outcomeClass: "ok", status: "added" },
        rows: [ { kind: "plugin-cascade", name: "...", scope: "...", status: "installed" } ] },
      // ... more blocks
    ];
    const bodies = blocks.map(
      (b) => cascadeSummary({ marketplace: b.mp, rows: b.rows, probe: PROBE_BOTH_LOADED }).message,
    );
    return appendReloadHint(`Claude plugin import summary\n\n${bodies.join("\n")}`, RELOAD_HINT);
  },
  // ... more fixtures
},
```

From `tests/architecture/catalog-uat.test.ts:100-101` (state-comment regex — confirms `soft-dep-markers` is a valid state name; lowercase + hyphens):

```typescript
const stateRe = /^<!-- catalog-state: ([a-z0-9-]+) -->\s*$/;
```

From `tests/architecture/catalog-uat.test.ts:1572` (byte-equal-pairing test — the runner that exercises every `(section, state)` pair):

The runner iterates over every example parsed from `docs/output-catalog.md`, looks up the matching fixture under `[section][state]` in the fixture map, calls the fixture function, and asserts the rendered string equals the fenced body byte-for-byte. Adding a new `(section, state)` pair on both sides is the entire integration — no runner changes needed.

From `extensions/pi-claude-marketplace/presentation/cascade-summary.ts` (UNCHANGED — the renderer the fixture targets):

`cascadeSummary({ marketplace, rows, probe }).message` returns the rendered cascade body for one marketplace block. The fixture function composes one or more such bodies and joins with `\n` between blocks, matching the existing fixtures' pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `soft-dep-markers` fixture to docs/output-catalog.md under `/claude:plugin import`</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - docs/output-catalog.md (lines 523-597 — the entire `## /claude:plugin import` section; new fixture inserts between the `source-mismatch` fixture body and the `______` divider on line 597)
    - docs/output-catalog.md (lines 91, 229-230, 273, 299, 399 — existing soft-dep reason grammar and ordering; new example MUST honor `pi-subagents` before `pi-mcp` in the combined form)
    - .planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-CONTEXT.md (re-read D-07; review the "Specific Ideas" body sketch at lines 163-172 — illustrative-only, executor matches existing voice)
    - docs/messaging-style-guide.md (MSG-SD-1..3 — predicate firing rules; verify the new example's combined row satisfies MSG-SD-3, i.e. markers fire only when declares AND unloaded, both true here)
  </read_first>
  <action>
    Insert a new fixture block in `docs/output-catalog.md` between the end of the `source-mismatch` fixture (after line 595, before the `______` divider on line 597). The new block follows the same shape as the three existing fixtures in this section:

    1. Heading line: `### Per-row soft-dep markers on import cascade rows` (or similar — match the existing voice in the section).
    2. State discriminator line: `<!-- catalog-state: soft-dep-markers -->` (state name per D-07).
    3. Triple-backtick `text` fenced body containing the rendered cascade. Fixture shape:
       - One marketplace header line: `● <marketplace-name> [<scope>] (added)` or similar — pick a minimal single-marketplace block (one header, two child rows) to keep the example focused on the soft-dep markers, not on the marketplace-level complexity already covered by `fresh-mixed-both-scopes`.
       - Two child plugin-cascade rows:
         - Agents-only: `  ● <agent-plugin-name> [<scope>] (installed) {requires pi-subagents}`
         - Combined: `  ● <dual-plugin-name> [<scope>] (installed) {requires pi-subagents, requires pi-mcp}`
       - One blank line then the reload-hint trailer: `/reload to pick up changes` (this entire body must be byte-equal to what the renderer produces for the matching fixture function in Task 2).
    4. A short "Notes:" paragraph below the fence (3-5 short bullets max, matching the existing voice). At minimum mention: (a) the markers fire only when the plugin declares the resource AND the companion extension is unloaded (MSG-SD-1..3); (b) the combined-row ordering is `pi-subagents` before `pi-mcp` per the closed-set grammar; (c) `(uninstalled)` rows never carry these markers (MSG-SD-3) — this is a NEGATIVE example reminder, not a row to render.

    Use concrete names like `agent-only-plugin` and `dual-plugin` and a marketplace like `claude-plugins-official` (project scope) to keep the example minimal. Do NOT include the autoupdate marker per Claude's Discretion in CONTEXT.md (the autoupdate orthogonality is covered by `fresh-mixed-both-scopes`). Do NOT modify any other section of `output-catalog.md`.

    **Catalog-fixture authoring loop (standard pattern):** The exact byte-sequence of the fenced body MUST match what Task 2's fixture function renders. The two tasks form a coordinated authoring pair — Task 1 writes the docs side and Task 2 writes the fixture-function side, and the byte-equal-pairing test at `catalog-uat.test.ts:~1572` is the structural gate that confirms they agree. There is no automated verification possible for Task 1 in isolation that the fenced body is renderer-correct (only Task 2's round-trip can prove that). The recommended authoring order is: (a) write Task 2's fixture function first; (b) run the catalog-uat byte-pairing test to capture the actual rendered output from the failure diff; (c) paste that bytes-exact output into Task 1's docs fence; (d) re-run the test until it passes. Either order is acceptable, but Task 1 cannot be verified independently — the round-trip check in Task 2 is the source of truth.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; grep -nE '^<!-- catalog-state: soft-dep-markers -->$' docs/output-catalog.md &amp;&amp; grep -nE 'requires pi-subagents, requires pi-mcp' docs/output-catalog.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (fixture state discriminator present): `grep -nE '^<!-- catalog-state: soft-dep-markers -->$' docs/output-catalog.md` returns exactly 1 match, AND the match's line number is between line 523 (`## /claude:plugin import`) and line 605 (next `##` section start — currently `## /claude:plugin bootstrap` at line 599).
    - Source assertion (single-reason form present): `grep -nE '\(installed\) \{requires pi-subagents\}$' docs/output-catalog.md` returns at least 1 match within the new fixture's fenced body (line range determined by reading the file after edit).
    - Source assertion (combined-reason form present, correct ordering): `grep -nE '\(installed\) \{requires pi-subagents, requires pi-mcp\}' docs/output-catalog.md` returns at least 1 match within the new fixture's fenced body. The substring `requires pi-mcp, requires pi-subagents` (wrong order) must NOT appear: `grep -E 'requires pi-mcp, requires pi-subagents' docs/output-catalog.md | grep -v '^#' | wc -l` returns `0`.
    - Source assertion (reload-hint present): the new fenced body contains `/reload to pick up changes` (the canonical trailer per CMC-14).
    - Negative assertion (no autoupdate marker per Claude's Discretion): the new fixture's marketplace header line does NOT contain `<autoupdate>` (executor confirms by reading the inserted block).
    - Pre-commit: `pre-commit run --files docs/output-catalog.md` exits 0 (Prettier format on .md content).
  </acceptance_criteria>
  <done>
    New `<!-- catalog-state: soft-dep-markers -->` block exists under `## /claude:plugin import`, contains a minimal single-marketplace cascade with one agents-only row and one combined-marker row, honors the closed-set ordering (`pi-subagents` before `pi-mcp`), includes the canonical reload-hint trailer, and is accompanied by a short notes paragraph that ties back to MSG-SD-1..3.

    **Verification deferred:** the fenced body's byte-correctness (that the rendered output equals the docs body exactly) is NOT verified by this task in isolation — it is structurally verified by Task 2's round-trip test at `catalog-uat.test.ts:~1572`. Task 1 lands the documented surface; Task 2 proves it round-trips through the production renderer. The two tasks form the standard catalog-fixture authoring pair.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add paired fixture function in catalog-uat.test.ts and verify round-trip</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts (full file 1-1675 — focus on: probe definitions at lines 156-170 (use `piSubagentsLoaded` / `piMcpAdapterLoaded` field names — already defined); the `"/claude:plugin import"` fixture entry at lines 974-1104 where the `fresh-mixed-both-scopes` fixture lives; the byte-equal-pairing test at around line 1572)
    - extensions/pi-claude-marketplace/presentation/cascade-summary.ts (the `cascadeSummary({ marketplace, rows, probe })` signature — confirm the executor uses it correctly)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (lines 80-83 for the `SoftDepProbe` shape; lines 438-451 `composeReasons` — verify it emits `{requires pi-subagents}` and `{requires pi-subagents, requires pi-mcp}` in the closed-set order; this is the renderer the fixture exercises end-to-end)
    - docs/output-catalog.md (the new fixture inserted in Task 1 — confirm the fenced body matches what Task 2's fixture function produces)
    - .planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-CONTEXT.md (re-read D-08)
  </read_first>
  <behavior>
    - A new entry `"soft-dep-markers": () => string` exists inside the `"/claude:plugin import"` fixture-map object in `tests/architecture/catalog-uat.test.ts`, peer of the existing `"fresh-mixed-both-scopes"` / `"scope-project-narrow"` / `"source-mismatch"` entries.
    - The fixture function constructs a `blocks: Array<{ mp: MarketplaceRow; rows: PluginCascadeRow[] }>` array with exactly ONE marketplace block (single header + two child rows) to match the minimal docs fixture shape from Task 1.
    - The marketplace header: `{ kind: "marketplace", name: "claude-plugins-official", scope: "project", outcomeClass: "ok", status: "added" }` (no `marker: "autoupdate"` — match the docs fixture).
    - The two child plugin-cascade rows:
      - `{ kind: "plugin-cascade", name: "agent-only-plugin", scope: "project", status: "installed", declaresAgents: true, declaresMcp: false }` — produces `(installed) {requires pi-subagents}` under `PROBE_BOTH_UNLOADED`.
      - `{ kind: "plugin-cascade", name: "dual-plugin", scope: "project", status: "installed", declaresAgents: true, declaresMcp: true }` — produces `(installed) {requires pi-subagents, requires pi-mcp}` under `PROBE_BOTH_UNLOADED`.
    - The function calls `cascadeSummary({ marketplace: b.mp, rows: b.rows, probe: PROBE_BOTH_UNLOADED }).message` for the single block and returns `appendReloadHint(\`Claude plugin import summary\n\n${body}\`, RELOAD_HINT)` (matching the join-and-wrap form used by `fresh-mixed-both-scopes` at lines 1099-1103).
    - The byte-equal-pairing test at line ~1572 passes for the new `("/claude:plugin import", "soft-dep-markers")` pair (this is automatic — the runner iterates every parsed `(section, state)` pair against the fixture map).
    - Negative: no hand-composed body string. The function MUST go through `cascadeSummary` per D-08 ("the fixture must render via the existing `cascadeSummary` + `PluginCascadeRow` composers — no hand-composed literals — so the round-trip exercises the production renderer with the new predicates set").
  </behavior>
  <action>
    Add a new entry to the `"/claude:plugin import"` fixture-map object in `tests/architecture/catalog-uat.test.ts` (located at lines 974-1104). Insert the new `"soft-dep-markers"` entry as a peer of the existing entries (recommended position: immediately after `"fresh-mixed-both-scopes"` ends around line 1104, before `"scope-project-narrow"` begins at line 1106 — but any position within the same object is equivalent for the runner).

    Implementation:
    1. Construct `blocks: Array<{ mp: MarketplaceRow; rows: PluginCascadeRow[] }>` with ONE block: header `{ kind: "marketplace", name: "claude-plugins-official", scope: "project", outcomeClass: "ok", status: "added" }` and rows array containing the two `PluginCascadeRow` records described in `<behavior>` (agent-only with `declaresAgents: true, declaresMcp: false`, dual-plugin with `declaresAgents: true, declaresMcp: true`).
    2. Map blocks through `cascadeSummary({ marketplace: b.mp, rows: b.rows, probe: PROBE_BOTH_UNLOADED }).message` — note `PROBE_BOTH_UNLOADED` (already defined at line 166, with the correct `piSubagentsLoaded: false, piMcpAdapterLoaded: false` field shape), NOT `PROBE_BOTH_LOADED` (which would suppress the markers).
    3. Return `appendReloadHint(\`Claude plugin import summary\n\n${bodies.join("\n")}\`, RELOAD_HINT)` (matching the `fresh-mixed-both-scopes` pattern at lines 1099-1103).

    After landing the fixture function, run the catalog-uat suite and let the byte-equal-pairing test fail loudly if Task 1's docs fenced body doesn't exactly match the rendered output. Then synchronize the docs body to match. (This is the standard catalog authoring loop; either side can be the source of truth, but the test asserts equality.)

    DO NOT modify the byte-equal-pairing runner at line ~1572 — adding the new `(section, state)` pair on both sides is the entire integration. DO NOT modify any other fixture or any other section of the file. DO NOT redeclare or modify the `PROBE_BOTH_UNLOADED` constant (use it by name from its existing declaration).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace &amp;&amp; npm test -- tests/architecture/catalog-uat.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (fixture entry present in correct section): `grep -nE '"soft-dep-markers":\s*\(\)\s*=>' tests/architecture/catalog-uat.test.ts` returns exactly 1 match, AND that match's line number is within the `"/claude:plugin import"` fixture-map block (between line 974 and line ~1200).
    - Source assertion (uses the production renderer per D-08): `grep -nE 'PROBE_BOTH_UNLOADED' tests/architecture/catalog-uat.test.ts | wc -l` is at least 2 (the existing probe definition AND at least one new use inside the soft-dep-markers fixture function). The new fixture function MUST reference `cascadeSummary` (not construct a hand-composed string): the executor confirms by inspecting the inserted function body.
    - Source assertion (predicates set on the rows): the new fixture function source contains both `declaresAgents: true` (for both rows) and `declaresMcp: true` (for the combined row). Quick check: `awk '/"soft-dep-markers":/{flag=1} flag; /^    \},?$/&&flag{flag=0}' tests/architecture/catalog-uat.test.ts | grep -cE 'declaresAgents: true'` returns `2`; same awk pipeline grepping `declaresMcp: true` returns `1`.
    - Behavior assertion (round-trip passes — THIS is the structural mitigation for Task 1's deferred byte-correctness verification): `npm test -- tests/architecture/catalog-uat.test.ts 2>&amp;1 | grep -E 'soft-dep-markers'` shows the byte-equal-pairing test for `("/claude:plugin import", "soft-dep-markers")` passes (no `fail`, no diff output). Equivalently `npm test -- tests/architecture/catalog-uat.test.ts` exits 0.
    - Quality gate: `npm run check` exits 0 (NFR-6).
    - Pre-commit: `pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    `"soft-dep-markers"` fixture function added as peer of `"fresh-mixed-both-scopes"` under `"/claude:plugin import"` in `tests/architecture/catalog-uat.test.ts`; uses `cascadeSummary` + `PROBE_BOTH_UNLOADED` + `PluginCascadeRow` records with `declaresAgents`/`declaresMcp` set; byte-equal-pairing test passes for the new pair (this round-trip is also the structural mitigation that closes the verification gap deferred from Task 1); `npm run check` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies a documentation file (`docs/output-catalog.md`) and a test fixture (`tests/architecture/catalog-uat.test.ts`). Neither is on a production code path. No new input boundary, no new auth/permission surface, no new network, no new file write in production. |

## STRIDE Threat Register (per phase security_enforcement gate)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-02-01 | Spoofing | Docs fixture body diverges from rendered output (silent contract drift) | mitigate | The byte-equal-pairing test at `tests/architecture/catalog-uat.test.ts:~1572` is the structural mitigation — any drift between the docs fence and the rendered output fails `npm run check`. Acceptance criteria require the test to pass on the new `(section, state)` pair. |
| T-14.1-02-02 | Tampering | Test fixture constructs hand-composed string bypassing the production renderer | mitigate | D-08 explicitly forbids hand-composed literals; the acceptance criteria assert `cascadeSummary` is called and `PROBE_BOTH_UNLOADED` is referenced inside the new fixture function. If a future contributor regresses to a hand-composed body, the regression is visible in code review and the predicate-propagation contract is not actually exercised end-to-end. |
| T-14.1-02-03 | Repudiation | Audit trail for the soft-dep-marker contract | accept (N/A) | No telemetry per IL-4. The catalog itself is the audit trail (`docs/output-catalog.md` is the v1.3 user-contract per D-30). |
| T-14.1-02-04 | Information Disclosure | Docs example exposes that plugins can declare agents and MCP | accept (LOW) | This is documentation of a public feature. No new disclosure surface. |
| T-14.1-02-05 | Denial of Service | New test adds runtime to `npm run check` | accept (N/A) | One additional cascadeSummary call + one byte-equal comparison. Negligible. |
| T-14.1-02-06 | Elevation of Privilege | Fixture function elevates a closed-set value | accept (N/A) | The fixture function only constructs `PluginCascadeRow` records (TypeScript-typed) and calls `cascadeSummary`. No privilege elevation surface. |
| T-14.1-02-SC | Tampering | npm/pip/cargo installs | accept (N/A) | This plan adds NO new package dependencies. No `npm install` runs. No `package.json` change. The supply-chain attack surface is unchanged. |

**Overall risk posture:** The threat surface for this plan is minimal — docs + test fixture only, no production code. The byte-equal-pairing test is the structural anti-drift mitigation, and the `cascadeSummary` round-trip requirement (D-08) prevents the fixture from silently bypassing the renderer.
</threat_model>

<verification>
- Round-trip: `npm test -- tests/architecture/catalog-uat.test.ts` exits 0, including the new `("/claude:plugin import", "soft-dep-markers")` byte-equal-pairing case.
- Format: `npm run format:check` exits 0 (Prettier formats both .md and .ts).
- Lint: `npm run lint` exits 0.
- Aggregate: `npm run check` exits 0 (NFR-6).
- Pre-commit: `pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts` exits 0.
- Manual sanity: read the new docs fixture body — the rendered text should be visibly equivalent to what a user would see after running `/claude:plugin import` with both companion extensions unloaded and both plugins declaring the corresponding resources.
</verification>

<success_criteria>
- `docs/output-catalog.md` has a new `<!-- catalog-state: soft-dep-markers -->` fixture under `## /claude:plugin import`, demonstrating both single-marker and combined-marker forms with the correct closed-set ordering.
- `tests/architecture/catalog-uat.test.ts` has a paired `"soft-dep-markers"` fixture function under `"/claude:plugin import"` that renders through `cascadeSummary` + `PROBE_BOTH_UNLOADED` (no hand-composed literals).
- The byte-equal-pairing test passes for the new pair (`npm test -- tests/architecture/catalog-uat.test.ts` exits 0).
- `npm run check` exits 0.
- The end-to-end CMC-13 closure is now verified: Plan 14.1-01 ships the plumbing, Plan 14.1-02 ships the catalog-UAT proof that the plumbing reaches the renderer.
</success_criteria>

<output>
Create `.planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-02-SUMMARY.md` when done.
</output>
