---
phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import
verified: 2026-05-24T14:07:18Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---

# Phase 14.1: Close Gap CMC-13 -- Propagate `declaresAgents`/`declaresMcp` Through Import -- Verification Report

**Phase Goal:** Propagate `declaresAgents` / `declaresMcp` predicates from the install path through the import-orchestrator cascade rows so per-row `{requires pi-subagents}` / `{requires pi-mcp}` soft-dep markers fire on `/claude:plugin import`. Replaces the hard-coded `declaresAgents=false` / `declaresMcp=false` literals at `extensions/pi-claude-marketplace/orchestrators/import/execute.ts:474-475` that the v1.3 milestone audit flagged as a BLOCKER integration-layer gap on CMC-13. After this phase, CMC-13 moves from PARTIAL (per audit) to SATISFIED on every cascade surface (install / reinstall / update / import).

**Verified:** 2026-05-24T14:07:18Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths (Plan 14.1-01 + Plan 14.1-02 merged)

| #   | Truth                                                                                                                                                                                                                                                                                                                          | Status     | Evidence                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | `InstallPluginOutcome.installed` variant carries `declaresAgents` and `declaresMcp` as REQUIRED `readonly boolean` fields (no widening to `boolean \| undefined`).                                                                                                                                                              | VERIFIED   | `install.ts:126-127` -- `readonly declaresAgents: boolean; readonly declaresMcp: boolean;` directly inside the `"installed"` variant; surrounding 4 non-installed variants unchanged.    |
| 2   | Both predicates derived at the install-outcome construction site in `install.ts` from `installCtx.stagedAgentNames.length > 0` / `installCtx.stagedMcpServerNames.length > 0` (single source, matching the standalone cascade row).                                                                                            | VERIFIED   | `install.ts:786-787` outcome return AND `install.ts:762-763` standalone cascade row -- identical expression form (`grep` returns 2 matches each).                                         |
| 3   | `PluginInstalledOutcome` in `import/execute.ts` carries the same two REQUIRED `readonly boolean` fields, and the case-`installed` switch arm propagates them onto every `installedPlugins[]` entry.                                                                                                                            | VERIFIED   | `execute.ts:63-64` interface declaration; `execute.ts:856-857` switch arm propagation `declaresAgents: outcome.declaresAgents` / `declaresMcp: outcome.declaresMcp`.                       |
| 4   | The cascade-row build at `import/execute.ts:474-475` (now `:475-476`) no longer contains the hard-coded literals `declaresAgents: false` / `declaresMcp: false`; instead it reads `o.declaresAgents` / `o.declaresMcp`.                                                                                                        | VERIFIED   | `execute.ts:475-476` -- `declaresAgents: o.declaresAgents` / `declaresMcp: o.declaresMcp`. `grep -c 'declaresAgents: false' execute.ts` returns 0 (production literal-false eliminated).   |
| 5   | The deferred-work comment block at `import/execute.ts:467-473` is removed; a one-line CMC-13 / MSG-SD-1..3 reference replaces it per D-11.                                                                                                                                                                                     | VERIFIED   | `execute.ts:467` -- `// CMC-13 / MSG-SD-1..3: predicates propagated from install outcome`. `grep 'follow-up (sub-wave 2b'` returns 0 matches in execute.ts.                               |
| 6   | All 12 historical `installPlugin` test-double returns that return `status: "installed"` in `tests/orchestrators/import/execute.test.ts` are widened to include `declaresAgents: ..., declaresMcp: ...` so the contract holds across the test suite. (Plus 1 Rule 3 auto-fix on a literal `PluginInstalledOutcome`.)            | VERIFIED   | `grep -c 'declaresAgents: false' tests/orchestrators/import/execute.test.ts` returns 15 (12 historical doubles + 1 literal outcome + 2 from new tests' `false` defaults).                |
| 7   | An import-surface unit test asserts the new outcome surface AND the cascade row carry the predicates for: (a) agents-only, (b) mcp-only, (c) both, (d) neither.                                                                                                                                                                | VERIFIED   | `execute.test.ts:515,550,585,622` -- 4 new tests `propagates declaresAgents=true (agents-only)` / `mcp-only` / `both` / `neither`; each asserts outcome AND rendered cascade body.        |
| 8   | A new `<!-- catalog-state: soft-dep-markers -->` fixture block exists under the `## /claude:plugin import` section of `docs/output-catalog.md`.                                                                                                                                                                                 | VERIFIED   | `docs/output-catalog.md:599` -- exactly 1 match; line 599 falls between the `## /claude:plugin import` section (line 523) and the next section divider (line 617).                       |
| 9   | The fixture body demonstrates per-row soft-dep markers on import cascade rows: `(installed) {requires pi-subagents}` and `(installed) {requires pi-subagents, requires pi-mcp}`, using the existing reasons-ordering grammar.                                                                                                  | VERIFIED   | `docs/output-catalog.md:605` agents-only form; `:606` combined form with `pi-subagents` before `pi-mcp`. Wrong-order substring absent (`grep 'requires pi-mcp, requires pi-subagents'` returns 0). |
| 10  | A paired fixture function under `"/claude:plugin import"` in `tests/architecture/catalog-uat.test.ts` constructs the rows via `PluginCascadeRow` records with `declaresAgents: true` / `declaresMcp: true` and the existing `PROBE_BOTH_UNLOADED` stub, then renders through `cascadeSummary` (no hand-composed string literals). | VERIFIED   | `catalog-uat.test.ts:1106` -- `"soft-dep-markers": () => { ... cascadeSummary({ ..., probe: PROBE_BOTH_UNLOADED }).message ... }`; rows have `declaresAgents: true` (x2) and `declaresMcp: true` (x1). |
| 11  | The byte-equal-pairing test at `tests/architecture/catalog-uat.test.ts:1572` round-trips the new fixture: the fenced docs body equals the rendered output exactly.                                                                                                                                                            | VERIFIED   | `npm test -- tests/architecture/catalog-uat.test.ts` -- test 1 `catalog UAT: every <!-- catalog-state: --> annotation pairs byte-equal with a fixture` passes; `npm run check` exits 0.    |
| 12  | NFR-7 enforced: no `boolean \| undefined` widening on either file.                                                                                                                                                                                                                                                              | VERIFIED   | `grep -nE '(declaresAgents\|declaresMcp)\??: boolean \| undefined' install.ts execute.ts` returns no matches.                                                                              |
| 13  | `npm run check` exits 0 (typecheck + ESLint + Prettier + node:test all green).                                                                                                                                                                                                                                                  | VERIFIED   | Aggregate run: 1146 tests pass / 0 fail / 0 cancelled / 0 skipped; tsc, eslint, and prettier all exit 0.                                                                                  |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact                                                                                                              | Expected                                                                                                                       | Status     | Details                                                                                                                                                                                                          |
| --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts`                                                    | Widened `InstallPluginOutcome.installed` variant + predicate derivation at return site                                          | VERIFIED   | Levels 1-3: exists, substantive (lines 122-134 type widening; 786-787 derivation), wired (consumed by `import/execute.ts` switch arm). Level 4 N/A (type/data plumbing).                                          |
| `extensions/pi-claude-marketplace/orchestrators/import/execute.ts`                                                    | Widened `PluginInstalledOutcome`; propagation in case-`installed` switch arm; cascade-row build reads `o.declaresAgents`/`Mcp` | VERIFIED   | Levels 1-3: exists, substantive (lines 49-65 interface; 475-476 cascade row; 856-857 switch arm), wired through `formatClaudeImportSummary` to `cascadeSummary` → `composeReasons`.                              |
| `tests/orchestrators/import/execute.test.ts`                                                                          | 12+ historical `installed` doubles widened + 4 new predicate-propagation tests                                                  | VERIFIED   | Levels 1-3: exists, substantive (15 occurrences of `declaresAgents: false`; 4 new tests at lines 515/550/585/622), wired into node:test runner.                                                                  |
| `docs/output-catalog.md`                                                                                              | New `<!-- catalog-state: soft-dep-markers -->` fixture under `## /claude:plugin import`                                         | VERIFIED   | Levels 1-3: exists (line 599), substantive (full fixture body lines 597-615 with both single and combined marker forms), wired into byte-equal-pairing runner via state-comment regex.                          |
| `tests/architecture/catalog-uat.test.ts`                                                                              | Paired `soft-dep-markers` fixture function calling `cascadeSummary` + `PROBE_BOTH_UNLOADED`                                     | VERIFIED   | Levels 1-3: exists (line 1106), substantive (1106-1141 function body uses production `cascadeSummary` -- no hand-composed string), wired through pairing runner at line ~1572.                                  |

### Key Link Verification

| From                                                                                          | To                                                                                                | Via                                                                                       | Status | Details                                                                                                                                                                              |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `install.ts` (`InstallPluginOutcome.installed`)                                               | `import/execute.ts` (`PluginInstalledOutcome` via case-`installed` switch arm)                    | Typed propagation through switch arm                                                      | WIRED  | `execute.ts:856-857` reads `outcome.declaresAgents` / `outcome.declaresMcp` from the install outcome typed channel. Discriminated-union narrowing is type-system-enforced.            |
| `import/execute.ts` (`PluginCascadeRow.declaresAgents/Mcp`)                                   | `presentation/compact-line.ts (composeReasons)` via `SoftDepProbe`                                 | Cascade rows → `cascadeSummary` → `renderRow` → `composeReasons`                          | WIRED  | `execute.ts:475-476` sets predicates on the cascade row; the cascade summary path consumes them via the renderer. `npm run check` passes -- typecheck confirms wire-through.          |
| `docs/output-catalog.md` (soft-dep-markers fixture body)                                      | `tests/architecture/catalog-uat.test.ts` (soft-dep-markers fixture function)                      | Byte-equal-pairing test                                                                   | WIRED  | Test 1 of the catalog UAT suite passes; iterates every `(section, state)` pair parsed from docs and asserts byte-equal against the fixture function's output.                       |
| `tests/architecture/catalog-uat.test.ts` (fixture function)                                   | `extensions/pi-claude-marketplace/presentation/cascade-summary.ts (cascadeSummary)`               | Round-trip via production renderer; no hand-composed literals (D-08)                      | WIRED  | Function calls `cascadeSummary({ marketplace: b.mp, rows: b.rows, probe: PROBE_BOTH_UNLOADED }).message`. Renderer output equals docs body byte-for-byte.                              |

### Data-Flow Trace (Level 4)

Not applicable in the conventional component-state sense. This phase is a pure typed-plumbing change: the data is a boolean predicate flowing through TypeScript discriminated unions. Trace:

1. **Source:** `installCtx.stagedAgentNames` / `installCtx.stagedMcpServerNames` (populated by bridge prepare phases inside `withStateGuard`).
2. **Producer:** `install.ts:786-787` -- predicates set on `InstallPluginOutcome.installed`.
3. **Switch-arm extraction:** `execute.ts:856-857` -- moved onto `PluginInstalledOutcome.declaresAgents/Mcp`.
4. **Cascade-row composer:** `execute.ts:475-476` -- forwarded onto `PluginCascadeRow`.
5. **Renderer:** `cascadeSummary` → `renderRow` → `composeReasons` (in `presentation/compact-line.ts`) -- emits `{requires pi-subagents}` / `{requires pi-mcp}` markers conditionally on `(declares && !companion-loaded)`.

Status: FLOWING. The 4 new unit tests in `tests/orchestrators/import/execute.test.ts` and the byte-equal-pairing catalog UAT test both exercise this end-to-end with real predicate values.

### Behavioral Spot-Checks

| Behavior                                                                                                                                | Command                                                                                                  | Result                                                | Status |
| --------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------ |
| Production code contains zero hard-coded `false` for `declaresAgents` / `declaresMcp` predicates.                                       | `grep -c 'declaresAgents: false\|declaresMcp: false' extensions/pi-claude-marketplace/orchestrators/import/execute.ts` | `0`                                                   | PASS   |
| Single-source derivation occurs exactly twice in `install.ts` (standalone cascade-row + new outcome-return site).                       | `grep -cE 'declaresAgents: installCtx\.stagedAgentNames\.length > 0' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` | `2`                                                   | PASS   |
| Switch-arm propagation present.                                                                                                          | `grep -cE 'declaresAgents: outcome\.declaresAgents' extensions/pi-claude-marketplace/orchestrators/import/execute.ts`                  | `1`                                                   | PASS   |
| Cascade-row propagation present.                                                                                                         | `grep -cE 'declaresAgents: o\.declaresAgents' extensions/pi-claude-marketplace/orchestrators/import/execute.ts`                        | `1`                                                   | PASS   |
| NFR-7 enforced (no `boolean \| undefined`).                                                                                              | `grep -nE '(declaresAgents\|declaresMcp)\??: boolean \| undefined' install.ts execute.ts`                | no matches                                            | PASS   |
| Combined-row ordering correct (`pi-subagents` before `pi-mcp`).                                                                          | `grep 'requires pi-mcp, requires pi-subagents' docs/output-catalog.md`                                   | no matches outside `^#` headings                      | PASS   |
| Catalog fixture state name unique and present.                                                                                           | `grep -cE '^<!-- catalog-state: soft-dep-markers -->$' docs/output-catalog.md`                           | `1`                                                   | PASS   |
| Paired test fixture function present.                                                                                                    | `grep -nE '"soft-dep-markers":\s*\(\)\s*=>' tests/architecture/catalog-uat.test.ts`                      | line 1106 (within `/claude:plugin import` block)      | PASS   |
| Aggregate quality gate.                                                                                                                  | `npm run check`                                                                                          | exit 0; typecheck + lint + format:check + tests green | PASS   |
| Catalog UAT byte-equal-pairing test passes (new pair exercised).                                                                         | `npm test -- tests/architecture/catalog-uat.test.ts`                                                     | test 1 `every <!-- catalog-state: --> annotation pairs byte-equal with a fixture` passes; 0 failures | PASS   |

### Probe Execution

Not applicable. Phase 14.1 is an internal typed-plumbing change for an extension framework; there are no `scripts/*/tests/probe-*.sh` scripts and no PLAN/SUMMARY declarations of probes. Verification reduces to running the project's `npm run check` aggregate gate, which executed successfully above.

### Requirements Coverage

| Requirement | Source Plan        | Description                                                                                                                                                                                                                                                                                                                       | Status     | Evidence                                                                                                                                                                                          |
| ----------- | ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CMC-13      | 14.1-01, 14.1-02   | Soft-dep markers fire on installed / updated / reinstalled rows, on `list`-rendering rows, and per-row inside `import` / `update` / `reinstall` cascades whenever (a) the plugin declares the corresponding resource AND (b) the companion extension is unloaded; markers MUST NOT appear on `(uninstalled)` rows (MSG-SD-1..3). | SATISFIED  | Plan 14.1-01 plumbs predicates through `InstallPluginOutcome.installed` → `PluginInstalledOutcome` → `PluginCascadeRow`; Plan 14.1-02 catalog UAT round-trip confirms the marker fires end-to-end on the import surface; pre-existing test `CMC-13 / MSG-SD-1..2 soft-dep markers emit per row when (declares AND companion unloaded)` passes. |

CMC-13 was previously marked Complete (Phase 13) at line 742 of REQUIREMENTS.md but downgraded to PARTIAL by the v1.3 milestone audit on the import surface specifically. Phase 14.1 closes that gap; CMC-13 now SATISFIED on every cascade surface (install / reinstall / update / import / list).

No orphaned requirements: Phase 14.1 declares only CMC-13 in PLAN frontmatter, and the audit's gap closure plan explicitly lists CMC-13 as the only requirement scoped to this phase.

### Anti-Patterns Found

| File                                                                | Line | Pattern                            | Severity | Impact                                                                                                                                                                |
| ------------------------------------------------------------------- | ---- | ---------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)                                                              | --   | --                                 | --       | No `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` markers introduced in any of the 5 modified files. The deferred-work comment block at `execute.ts:467-473` was REMOVED and replaced with a single-line CMC-13 reference (D-11). |

### Human Verification Required

(none)

This phase is a fully observable typed-plumbing change with both unit-test and catalog-UAT byte-equal coverage. The new soft-dep markers' visual presentation is already covered by the catalog fixture (`docs/output-catalog.md:597-615`) and asserted byte-for-byte against the production renderer's output. No real-time behavior, external service integration, or UX-quality judgment is required to validate the goal.

### Gaps Summary

No gaps. The phase achieves its stated goal: all 13 must-haves verified; CMC-13 closure on the import surface is structurally complete and provably wired end-to-end through both unit tests (4 new predicate-propagation cases) and the catalog UAT round-trip (`soft-dep-markers` fixture). Production code contains zero `declaresAgents: false` / `declaresMcp: false` literals on the import surface, and NFR-7 (no `boolean | undefined` discriminated-union widening) is structurally enforced.

The v1.3 milestone audit's BLOCKER finding "`import/execute.ts` hard-codes soft-dep predicates" is fully resolved: the predicate values now flow through typed discriminated unions from `InstallPluginOutcome.installed` through `PluginInstalledOutcome` to `PluginCascadeRow` to `composeReasons` with no truncation, default-to-false, or string-parsing intermediation.

---

*Verified: 2026-05-24T14:07:18Z*
*Verifier: Claude (gsd-verifier)*
