---
phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import
plan: 02
subsystem: docs+test-fixtures
tags: [cmc-13, msg-sd, soft-dep, catalog, byte-equal-pairing, import]

# Dependency graph
requires:
  - phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import
    provides: "Predicate propagation (declaresAgents / declaresMcp) on the import surface via Plan 14.1-01 (`db913bb`)"
provides:
  - "New `<!-- catalog-state: soft-dep-markers -->` fixture under `## /claude:plugin import` in `docs/output-catalog.md` demonstrating the agents-only `(installed) {requires pi-subagents}` form and the combined `(installed) {requires pi-subagents, requires pi-mcp}` form"
  - "Paired `soft-dep-markers` fixture function under `\"/claude:plugin import\"` in `tests/architecture/catalog-uat.test.ts` that round-trips the docs fence through the production `cascadeSummary` composer under `PROBE_BOTH_UNLOADED`"
  - "End-to-end CMC-13 closure on the import surface: the byte-equal-pairing test at `catalog-uat.test.ts:1572` proves the new predicates plumbed by Plan 14.1-01 reach `composeReasons` and emit the documented markers on import cascade rows"
affects: [phase-14-cmc-38-drift-guard, import-surface-conformance]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Catalog-fixture authoring pair: docs fenced body + test fixture function joined by byte-equal-pairing runner (D-08); the test is the structural anti-drift mitigation"
    - "Single-marketplace minimal cascade fixture (one header + two child rows) -- keeps the example focused on soft-dep markers, not on marketplace-level complexity already covered by `fresh-mixed-both-scopes`"
    - "Use `PROBE_BOTH_UNLOADED` for both rows under a single fixture so the agents-only row and the combined row render under the same probe (per-fixture single-probe convention)"

key-files:
  created:
    - ".planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-02-SUMMARY.md -- this file"
  modified:
    - "docs/output-catalog.md -- new fixture block inserted at lines 597-615 under `## /claude:plugin import`, before the `______________________________________________________________________` divider and the `## /claude:plugin bootstrap` H2 heading"
    - "tests/architecture/catalog-uat.test.ts -- new `soft-dep-markers` fixture function inserted at lines 1106-1142 under the `\"/claude:plugin import\"` fixture-map entry, as a peer of `fresh-mixed-both-scopes` / `scope-project-narrow` / `source-mismatch`"

key-decisions:
  - "Per D-07: state name is `soft-dep-markers` (lowercase + hyphens; matches the `catalog-state` regex `[a-z0-9-]+`)"
  - "Per D-08: fixture function renders via `cascadeSummary` + `PluginCascadeRow` composers -- no hand-composed literals; round-trip exercises the production renderer with the new predicates"
  - "Per Claude's Discretion (CONTEXT.md): omit the `<autoupdate>` marker on the new fixture's marketplace header -- the autoupdate orthogonality is covered by `fresh-mixed-both-scopes`; this example stays minimal and focused on the soft-dep markers"
  - "Used `PROBE_BOTH_UNLOADED` for the whole fixture (one probe per fixture; both rows render under the same probe). The agents-only row fires only `{requires pi-subagents}` because the plugin does not declare MCP; the combined row fires both because both companions are unloaded AND the plugin declares both resources"

patterns-established:
  - "Catalog-fixture authoring loop: write the test fixture function first OR write the docs body first -- the byte-equal-pairing runner asserts equality. There is no automated verification possible for the docs side in isolation; the round-trip is the source of truth."
  - "Minimal single-marketplace cascade fixture pattern: one marketplace header + two child rows is the smallest shape that demonstrates per-row marker variation (one row fires single marker, the other fires combined)."

requirements-completed: []
# Note: CMC-13 was already marked complete by Plan 14.1-01 (predicate plumbing).
# This plan ships the catalog-UAT proof that the plumbing reaches the renderer
# on the import surface end-to-end -- the structural verification gate, not a
# new requirement. CMC-13 remains the single requirement closed by this phase
# and is recorded under Plan 14.1-01.

# Metrics
duration: ~10min
completed: 2026-05-24
---

# Phase 14.1 Plan 02: Add soft-dep-markers catalog fixture under /claude:plugin import -- Summary

**Closed the CMC-13 end-to-end verification gap on `/claude:plugin import` by adding a `soft-dep-markers` catalog fixture (docs + paired test function) that round-trips the new per-row `{requires pi-subagents}` / `{requires pi-subagents, requires pi-mcp}` markers through the production `cascadeSummary` renderer with the new predicates set.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-05-24T13:40:00Z (approx.)
- **Completed:** 2026-05-24T13:50:00Z
- **Tasks:** 2 (docs + test-fixture authoring pair)
- **Files modified:** 2

## Accomplishments

- New `<!-- catalog-state: soft-dep-markers -->` block exists under `## /claude:plugin import` in `docs/output-catalog.md` (lines 597-615). The fenced body demonstrates a minimal single-marketplace cascade with two child rows: the agents-only form `(installed) {requires pi-subagents}` and the combined form `(installed) {requires pi-subagents, requires pi-mcp}`, honoring the closed-set ordering (`pi-subagents` before `pi-mcp`) per MSG-SD-1.
- Paired fixture function `"soft-dep-markers"` exists in `tests/architecture/catalog-uat.test.ts` (lines 1106-1142) under the `"/claude:plugin import"` fixture-map entry. The function constructs the marketplace block via `PluginCascadeRow` records with `declaresAgents: true` (both rows) and `declaresMcp: true` (combined row only), then renders through `cascadeSummary({ marketplace, rows, probe: PROBE_BOTH_UNLOADED }).message` -- no hand-composed string literals, satisfying D-08.
- The byte-equal-pairing test at `tests/architecture/catalog-uat.test.ts:1572` now exercises the new `("/claude:plugin import", "soft-dep-markers")` pair automatically. The runner was not modified -- adding the new `(section, state)` pair on both sides was the entire integration.
- The new fixture's notes paragraph ties back to MSG-SD-1, MSG-SD-2, and MSG-SD-3 (the predicate firing rules: declares AND companion-unloaded; never on `(uninstalled)` rows).
- `npm run check` exits 0 (1146/1146 tests pass; new pair is included).
- CMC-13 closure on the import surface is now structurally complete: Plan 14.1-01 shipped the plumbing, Plan 14.1-02 ships the catalog-UAT proof that the plumbing reaches the renderer.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add `soft-dep-markers` fixture to `docs/output-catalog.md` under `/claude:plugin import`** -- `8987c4e` (docs)
2. **Task 2: Add paired fixture function in `catalog-uat.test.ts` and verify round-trip** -- `0e29e37` (test)

## Files Created/Modified

- `docs/output-catalog.md` -- inserted a new fixture block between the end of the `source-mismatch` fixture (line 595) and the `______________________________________________________________________` divider (originally line 597, now shifted to line 617). The new block is 20 lines: heading + state discriminator + fenced body (10 lines including blank lines) + notes paragraph (3 bullets).
- `tests/architecture/catalog-uat.test.ts` -- inserted a 37-line `"soft-dep-markers"` fixture function as a peer of `"fresh-mixed-both-scopes"` under the `"/claude:plugin import"` fixture-map entry. The function follows the same join-and-wrap pattern as `fresh-mixed-both-scopes` (constructs blocks, maps through `cascadeSummary`, joins, prepends `Claude plugin import summary\n\n`, appends reload-hint via `appendReloadHint`).

## Decisions Made

- Followed all 14 plan decisions (D-01..D-14) from the phase CONTEXT.md verbatim.
- Followed Claude's Discretion guidance to omit the `<autoupdate>` marker on the new fixture's marketplace header -- the autoupdate orthogonality is covered by `fresh-mixed-both-scopes`; the new fixture stays minimal and focused on the soft-dep markers.
- Used `PROBE_BOTH_UNLOADED` (not `PROBE_BOTH_LOADED`) for the whole fixture so both rows render under the same probe. The agents-only row's `{requires pi-mcp}` marker is suppressed because the plugin does not declare MCP (`declaresMcp: false`), not because the companion is loaded.
- Used the project scope (`scope: "project"`) for the single marketplace and both child rows to match a coherent minimal scenario (the `fresh-mixed-both-scopes` fixture already covers cross-scope rendering).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 -- Blocking] Restored `## /claude:plugin bootstrap` H2 heading + horizontal rule deleted by mdformat**

- **Found during:** Task 1 first pre-commit run
- **Issue:** The first invocation of `pre-commit run --files docs/output-catalog.md` invoked the `mdformat` hook, which restructured the file and dropped the `______________________________________________________________________` horizontal rule + the `## /claude:plugin bootstrap` H2 heading immediately following the new fixture block. The diff at that point showed both lines removed and no compensating heading inserted -- a destructive structural collapse.
- **Fix:** Manually restored both the horizontal rule and the H2 heading after the new fixture's notes paragraph. Subsequent pre-commit runs preserved the structure (the file was now in a shape mdformat considered stable).
- **Files modified:** docs/output-catalog.md (lines 615-621 in the final file: notes-paragraph end -> `______________________________________________________________________` -> `## /claude:plugin bootstrap` -> Single-shot setup paragraph)
- **Verification:** `grep -n '^## ' docs/output-catalog.md` confirms all H2 sections from the pre-change baseline are still present (line 619 = `## /claude:plugin bootstrap`; downstream sections at 642 / 673 / etc. unchanged from baseline).
- **Committed in:** `8987c4e` (Task 1 commit, after the restoration)

**2. [Rule 1 -- Bug] Reworded notes paragraph to avoid markdownlint MD038 violation**

- **Found during:** Task 1 first pre-commit run
- **Issue:** The first draft of the notes paragraph included a code span `` ` , ` `` (literal comma-space inside backticks) to illustrate the join character between reasons. markdownlint-cli2 flagged this as MD038 (`no-space-in-code` -- spaces inside code span elements).
- **Fix:** Rephrased "joined by `` ` , ` ``" to "joined by a literal comma-space inside the `{}` block" -- describes the join character in prose rather than emitting it as a code span. Semantic content unchanged.
- **Files modified:** docs/output-catalog.md (line 614 in the final file)
- **Verification:** `markdownlint-cli2` passes; `pre-commit run --files docs/output-catalog.md` exits 0 (with `SKIP=trufflehog` per CLAUDE.md worktree guidance).
- **Committed in:** `8987c4e` (Task 1 commit, after the rewording)

---

**Total deviations:** 2 auto-fixed (1 blocking restoration of mdformat-collapsed structure; 1 lint compliance reword)
**Impact on plan:** Both are pre-commit-hook hygiene fixes; neither affects the plan's substantive output. The new fixture's fenced body is byte-equal to what the production renderer produces, and the byte-equal-pairing test gates that fact.

## Issues Encountered

- mdformat's structural collapse on the first pre-commit run (deviation #1). Not blocking -- restored by hand and re-committed once.
- No other issues. The byte-equal-pairing test passed on the first run after both tasks landed (the rendered output matched the docs fence exactly; no diff iteration needed).

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- Phase 14.1 closure: this plan closes the second of the two CMC-13 deliverables called out in the v1.3 milestone audit Gap Closure Plan §2 (plumbing in Plan 14.1-01 + catalog-UAT proof in Plan 14.1-02).
- Phase 14 (CMC-38 drift-guard test suite) can now proceed without retroactive failure on the import surface -- every `(installed)` row inside `/claude:plugin import` cascades will fire the documented marker correctly when companion extensions are unloaded, and the byte-equal-pairing test gates that contract on every `npm run check`.
- Out-of-scope items per the phase CONTEXT.md remain deferred: CMC-16 (`renderManualRecovery` orphan), CMC-34 (6 edge handlers using `notifyError` instead of `notifyUsageError`), optional `install.ts:751-752` deduplication, `MARKETPLACE_LABEL_PROBE` DRY cleanup, `transaction/rollback.ts` literal-drift hardening.

## Self-Check: PASSED

**Files verified:**

- FOUND: docs/output-catalog.md (modified, lines 597-615 new fixture; lines 617-619 horizontal rule + bootstrap H2 restored)
- FOUND: tests/architecture/catalog-uat.test.ts (modified, lines 1106-1142 new fixture function)
- FOUND: .planning/phases/14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import/14.1-02-SUMMARY.md (this file)

**Commits verified (via `git log --oneline -5`):**

- FOUND: 8987c4e (docs(14.1-02): add soft-dep-markers fixture under /claude:plugin import)
- FOUND: 0e29e37 (test(14.1-02): pair soft-dep-markers fixture with cascade round-trip)

**Acceptance criteria verified via grep (all gates green):**

- `<!-- catalog-state: soft-dep-markers -->` present exactly 1x in `docs/output-catalog.md` at line 599 (between `## /claude:plugin import` at line 523 and `## /claude:plugin bootstrap` at line 619)
- `(installed) {requires pi-subagents}` present in the new fixture body (line 605)
- `(installed) {requires pi-subagents, requires pi-mcp}` present in the new fixture body (line 606); the wrong-order substring `requires pi-mcp, requires pi-subagents` is absent (0 matches outside `^#` headings)
- Canonical reload-hint trailer `/reload to pick up changes` present in the fenced body
- No `<autoupdate>` marker on the new fixture's marketplace header (per Claude's Discretion guidance)
- `"soft-dep-markers": () =>` fixture entry present exactly 1x in `tests/architecture/catalog-uat.test.ts` at line 1106 (within the `"/claude:plugin import"` block 974-1240)
- `PROBE_BOTH_UNLOADED` referenced inside the new fixture function (the new use brings the total occurrence count to 5)
- `declaresAgents: true` appears exactly 2x inside the new fixture block (both rows)
- `declaresMcp: true` appears exactly 1x inside the new fixture block (combined row only)
- New fixture function references `cascadeSummary` (production renderer) -- no hand-composed body string
- `npm test -- tests/architecture/catalog-uat.test.ts` exits 0 (the byte-equal-pairing test passes for the new pair)
- `npm run check` exits 0 (typecheck + lint + format:check + tests; 1146/1146 tests pass)
- Pre-commit clean: `SKIP=trufflehog pre-commit run --files docs/output-catalog.md tests/architecture/catalog-uat.test.ts` exits 0; `pre-commit run trufflehog --all-files` from the main repo exits 0 (worktree-isolated `SKIP=trufflehog` documented per project CLAUDE.md)

---

*Phase: 14.1-close-gap-cmc-13-propagate-declaresagents-mcp-through-import*
*Completed: 2026-05-24*
