---
phase: 02-uninstall-data-disposition-and-the-uninstall-option-seam
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - extensions/pi-claude-marketplace/edge/handlers/shared.ts
  - tests/edge/handlers/shared.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02-03: An uninstall operation with keepData true removes installed artifacts and its installation record while preserving every byte in its data directory."
    - "D-02-04: Omitted or false keepData deletes non-empty data after durable uninstall, without a prompt; reconcile uses that same default."
    - "D-02-01: Preservation keeps the current success notification bytes and orchestrated outcome contract."
    - "D-02-06: Cleanup uses only the already-resolved scope; retained data is not enrolled in a new collection process."
    - "D-02-05: The consuming scanner rejects unsupported short and long options; existing pass-through callers retain their contract."
    - "Data preservation leaves completion-cache invalidation, hook-route removal, and unused clone collection active."
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      provides: "UninstallPluginOptions.keepData threaded through uninstallPluginWithTransaction to runPostUninstallCleanup"
      contains: "keepData"
    - path: extensions/pi-claude-marketplace/edge/handlers/shared.ts
      provides: "extractLocalFlag consuming mode, alongside its compatible pass-through mode"
      exports: [extractLocalFlag]
    - path: tests/orchestrators/plugin/uninstall.test.ts
      provides: "Real filesystem disposition and cleanup/failure assertions through the public uninstall factories"
      contains: "keepData"
    - path: tests/edge/handlers/shared.test.ts
      provides: "Direct extraction, scope-value, duplicate and rejected-token contracts"
      contains: "--keep-data"
    - path: tests/orchestrators/reconcile/apply.test.ts
      provides: "applyReconcile removes non-empty data after a declaration is dropped"
      contains: "applyReconcile"
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      to: extensions/pi-claude-marketplace/persistence/locations.ts
      via: "Deletion resolves pluginDataDir only when keepData is not true, outside the removal-error catch"
      pattern: "pluginDataDir"
    - from: extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      via: "applyPluginUninstalls uses createNodeUninstallPlugin and omits the preservation option"
      pattern: "createNodeUninstallPlugin"
---

<objective>
Deliver the shared uninstall data policy and a compatible scanner extension, per D-02-01, D-02-03, D-02-04, D-02-05, and D-02-06.

Purpose: Prove preservation through the existing public uninstall operation before exposing the command option. Keep reconcile on the same default.
Output: Tested optional keepData policy, consuming scanner mode, and seeded reconcile evidence.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-CONTEXT.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-RESEARCH.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-PATTERNS.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-VALIDATION.md
@CLAUDE.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md

Use CodeGraph before locating code or tracing callers. Existing source-test owners take precedence over obsolete paths in historical summaries. Discovery level 0: existing scanner, catalog, transaction, locations and test infrastructure suffice; no package installation, service setup, storage migration, or UI design is required.

The first tracer enters through createNodeUninstallPlugin, the same public operation bound by the handler and reconcile. It crosses the existing bridge, durable state, cleanup, and notification path. Plan 02 adds the command-entry tracer once this operation is proven. This keeps each task within five modified files and avoids temporary parsing or cleanup implementations.
</context>

<assumption_delta_decision>
The detector reported pluralization on "also" and "second" in the roadmap. Primary noun: a per-invocation uninstall options bundle for an existing scope/marketplace/plugin identity. Decision: no-change. UninstallPluginOptions already owns operation policy; add optional keepData there. Extend the existing scanner in place, preserving its original callers. A later uninstall option joins these same structures. There is no new identifier, alternate operation, persisted disposition, or required preservation setting.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve real data through the public uninstall operation</name>
  <reversibility rating="reversible">An optional in-memory policy changes no stored schema; the settled deletion default and public notification shape remain intact.</reversibility>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <read_first>
    extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts — UninstallPluginOptions, UninstallTransaction, runPostUninstallCleanup, uninstallPluginWithTransaction, and both exported factories.
    tests/orchestrators/plugin/uninstall.test.ts — seed helpers, bridge-removal case, PU-2 cleanup case, factory contract and public outcome assertions.
    extensions/pi-claude-marketplace/persistence/locations.ts — pluginDataDir containment contract; read only.
    extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts and extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts — existing operation callers; read only.
  </read_first>
  <behavior>
    - A real installed plugin with a nested data file uninstalls with keepData true: full file bytes and directory entries survive; generated artifacts, installed record, and standalone configuration declaration disappear.
    - The same operation with the option omitted removes the non-empty directory.
    - Standalone success notification and orchestrated success outcome remain exactly their existing values.
  </behavior>
  <action>
    Begin with a failing preservation case through createNodeUninstallPlugin and real temporary files. Use one existing installed-plugin fixture and independently chosen nested data bytes. Per D-02-03, add documented readonly keepData?: boolean to UninstallPluginOptions. Thread the value through the existing transaction.runPostCommitCleanup call into runPostUninstallCleanup, normalizing omission to false. Extend the existing transaction contract and its exact owner expectations where needed; do not add a second uninstall operation or cleanup implementation.

    Guard only the data-specific path resolution and removal. When keepData is true, skip both locations.pluginDataDir and rm for data. When it is false or omitted, retain path resolution outside the rm catch so a safety refusal propagates. Keep cleanup after durable state commit and all existing early failure exits. Completion-cache invalidation, hooks routing, clone collection, configuration writes and artifact removal keep their established scheduling. Per D-02-01, leave the notification model and rendered success bytes unchanged. Per D-02-06, use the selected ScopedLocations without another scope search or data collector.

    Extend only the paired owner. Assert the complete durable state, affected configuration, artifact footprint and retained bytes before interaction checks. Use the existing public factories and injected transaction ports; add no test-only exports. Keep new functions within both lint and fallow complexity limits.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/uninstall.test.ts</automated>
    <fails_when>The command exits nonzero or the uninstall owner reports zero executed tests.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts</automated>
    <fails_when>The command exits nonzero or the uninstall source-test pair reports incomplete line, function, or branch coverage.</fails_when>
  </verify>
  <acceptance_criteria>
    - The new preservation case fails against unconditional deletion and passes through the real operation after the change.
    - Nested bytes survive keepData true while the installed record and generated resources are removed.
    - Omission still removes non-empty data and exact success/output assertions pass.
    - The deletion containment check remains outside the tolerated filesystem-removal failure catch.
  </acceptance_criteria>
  <done>The public operation supports preservation end to end and retains its default deletion, output, and transactional contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the existing scanner with consumable boolean options</name>
  <reversibility rating="costly">The scanner is shared by existing handlers; preserve its array-form contract and confine stricter short-option rejection to the new consuming form.</reversibility>
  <files>extensions/pi-claude-marketplace/edge/handlers/shared.ts, tests/edge/handlers/shared.test.ts</files>
  <read_first>
    extensions/pi-claude-marketplace/edge/handlers/shared.ts — extractLocalFlag and its caller traces.
    tests/edge/handlers/shared.test.ts — exact returned-object assertions, scope-value skipping, pass-through extras, and the existing single-dash residual case.
    extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts and extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts — downstream consumption of catalog-derived flags; read only.
  </read_first>
  <behavior>
    - Existing calls with an omitted fourth argument or readonly string[] retain the same returned object, pass-through residuals, duplicate-local behavior, and single-dash handling.
    - A new consuming form accepts catalog-supplied boolean names, returns the detected names, and removes every occurrence from residualArgs.
    - Consuming mode accepts the preservation flag before or after the reference and treats duplicates as a single true boolean.
    - Consuming mode rejects unknown long and short options, including --delete-data, -y, --yes, and --prune, with the complete existing usage-error shape.
    - A token immediately after --scope remains its scope value even when it looks like an accepted or rejected option; it must not enable a boolean or produce a scanner error.
  </behavior>
  <action>
    Per D-02-05 and the recorded discretion, extend extractLocalFlag in this module rather than introduce another parser module. Preserve the fourth argument's existing readonly string[] form. Add a named object form with required consumeLongFlags: readonly string[]. Return consumedFlags as a ReadonlySet of encountered accepted names for this form; existing callers keep their exact local/residualArgs result shape. Use a documented interface and overloads if needed to expose the consuming result without casts at the handler. Keep one token walk owning scope-value precedence, local extraction, accepted extra extraction, and unknown-option rejection.

    In consuming mode, retain --scope and its following token for the reference parser, consume accepted booleans position-independently, and reject any other option-shaped token through notifyUsageError. Do not mistake a scope value for an option. Preserve the original mode's tested single-dash and pass-through behavior. The scanner receives allowed names from its caller and knows no uninstall-specific literal.

    Write independent expected objects and sets in the paired scanner owner. Keep all existing cases, including the single-dash residual case, and add sibling cases for the consuming form, option placement, duplicates, missing scope values, scope values that resemble flags, and rejected tokens in both positions. No generic parser framework, new dependency, or extra module is needed.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/shared.test.ts</automated>
    <fails_when>The command exits nonzero or the scanner owner reports zero executed tests.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/shared.ts</automated>
    <fails_when>The command exits nonzero or the scanner source-test pair reports incomplete line, function, or branch coverage.</fails_when>
  </verify>
  <acceptance_criteria>
    - Existing callers' exact return shapes and positional behavior remain covered and unchanged.
    - New consuming callers receive detected booleans and reference-only residual arguments without reparsing raw text.
    - Unknown-option notifications identify the offending token and preserve supplied usage text.
    - Scope-value tests prove that flag-looking values cannot switch on preservation.
  </acceptance_criteria>
  <done>One scanner supports the existing pass-through path and the new strict consuming path required by uninstall.</done>
</task>

<task type="auto">
  <name>Task 3: Prove cleanup safety and reconcile's promptless default</name>
  <files>tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <read_first>
    tests/orchestrators/plugin/uninstall.test.ts — containment refusal, cleanup leak, cascade failure, state-save failure, cache/routing and clone collection cases.
    tests/orchestrators/reconcile/apply.test.ts — dropped-declaration removal fixture and second-reconcile silence assertions.
    extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts — applyPluginUninstalls omission of disposition; read only.
  </read_first>
  <behavior>
    - Explicit false and omission remove nested data without any confirmation interaction; missing data is harmless.
    - keepData true preserves exact bytes even when the data-specific path would refuse resolution; other post-commit hygiene still runs.
    - A failed cascade or state save retains seeded data because deletion never ran; an already-gone operation leaves previously retained data alone.
    - A successful preservation uninstall still removes hook routes, invalidates completion cache and collects an unreferenced git-source clone.
    - Dropping a declaration and applying reconcile removes seeded data, installed artifacts and record; the next reconcile is silent.
    - A user-scope operation leaves a separately seeded project-scope data sentinel untouched, and vice versa.
  </behavior>
  <action>
    Expand the existing owners to prove D-02-03, D-02-04 and D-02-06 using non-empty nested data with independent byte expectations. Exercise omission, explicit false, explicit true and missing-data cases through public operations. Preserve the existing containment-failure and swallowed-cleanup-leak cases. Add data assertions to failure/retry cases so moving deletion before commit would fail. Preserve retained data on the already-gone path; do not introduce a sweep for data without an installed record.

    For keepData true, prove actual completion-cache invalidation, hook-route removal and unused clone collection through their existing public state observations. Use the existing symlink/containment fixture pattern to prove that preservation does not resolve or delete the data path. Scope-isolation evidence may seed another scope, but production must continue using only its resolved scope, per D-02-06.

    In the reconcile owner's dropped-declaration case, seed a real nested data file before the first applyReconcile call. Assert removal after the first pass and preserve the exact aggregate notification, artifact/state checks, and second-pass silence. Leave applyPluginUninstalls production code unchanged: it already omits keepData and therefore uses the shared default per D-02-04. Both command and reconcile contexts must expose no new confirmation interaction. Per D-02-01, compare complete output values without weakening existing assertions.

    Keep direct source-test ownership: operation internals belong to its owner; reconcile tests observe the consumer's public result. Do not skip the existing FIFO concurrency fixture when sandbox process restrictions apply; use the authorized execution environment identified in research. If a regression requires production repair, correct the task that owns that source before marking this task complete.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/reconcile/apply.test.ts</automated>
    <fails_when>The command exits nonzero or either named owner reports zero executed tests; an EPERM FIFO result leaves this check unpassed.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts</automated>
    <fails_when>The command exits nonzero or the reconcile source-test pair reports incomplete line, function, or branch coverage.</fails_when>
  </verify>
  <acceptance_criteria>
    - False/omitted options and reconcile delete non-empty data; true preserves complete nested bytes.
    - Seeded data survives pre-commit failure, already-gone operations and an uninstall in the other scope.
    - Preservation does not prevent actual cache, routing or clone cleanup.
    - The reconcile fixture proves the first removal and second-pass silence with no prompt.
  </acceptance_criteria>
  <done>Both data dispositions and reconcile have direct evidence, including failure ordering, scope isolation, and independent cleanup.</done>
</task>

</tasks>

<threat_model>
Security policy: ASVS level 1; high findings block. No package installation is planned.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw arguments to typed options | Command input can select policy or be mistaken for a reference |
| Resolved scope to filesystem | Plugin identifiers and data paths cross into recursive removal |
| Durable uninstall to cleanup | Failed uninstall must retain data; preservation must still retire runtime resources |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | runPostUninstallCleanup | high | mitigate | Task 1 guards only data work; Task 3 proves retained bytes, containment refusal, commit ordering and scope isolation |
| T-02-02 | Tampering | extractLocalFlag consuming form | high | mitigate | Task 2 rejects unsupported options before dispatch and preserves scope-value parsing; Plan 02 proves zero mutation at the handler |
| T-02-03 | Elevation of privilege | Uninstall runtime cleanup | medium | mitigate | Task 3 proves hook-route retirement, cache invalidation and unused clone collection under preservation |
| T-02-04 | Denial of service | Reconcile uninstall | medium | mitigate | Task 3 proves omitted disposition deletes without prompting and a repeated reconcile is silent |
</threat_model>

<flagged_assumptions>
The spec-less engine classified all three requirements as unclassified/unresolved. Its report remains: applicable 3, resolved 0, unresolved 3. These rows are not engine-resolved or backstop-verified.

| Requirement | Engine category/status | Flagged assumption grounded in settled context | Explicit behavioral evidence |
|-------------|------------------------|-----------------------------------------------|------------------------------|
| DATA-01 | unclassified / unresolved | Keeping means the entire existing directory and bytes survive successful uninstall | 02-01 Tasks 1 and 3; 02-02 Task 1 |
| DATA-02 | unclassified / unresolved | Omitted/false policy retains promptless post-commit deletion, including non-empty data | 02-01 Tasks 1 and 3; 02-02 Task 1 |
| DATA-03 | unclassified / unresolved | Config-driven removal uses the same omitted default, then converges silently | 02-01 Task 3 |

These are explicit implementation checks derived from D-02-03/D-02-04, not new unresolved product decisions.
</flagged_assumptions>

<prohibition_recall>
Recall considered ten candidates per requirement: data loss, partial byte changes, surviving artifacts, stale routes, stale completions, skipped clone collection, scope crossing, premature deletion, unexpected prompts, and misleading success/default descriptions. The candidates are dispositioned as follows:
- Data bytes, artifacts, cache/routes, clones, commit ordering, missing-data handling and repeated calls are ordinary correctness concerns: drop from prohibitions and cover them in task behavior.
- Prompt policy, unsupported aliases, success wording, retained-data collection and entry-point consistency are already explicit locked product constraints: cover D-02-01 through D-02-06 rather than invent additional prohibitions.
- Path traversal, unsafe symlinks, scope authorization, information disclosure and general data-retention/consent concerns are canon security: refer to the threat register and $gsd-secure-phase; do not mint them here.
No additional bespoke values/safety prohibition survives the precision pass. Kept prohibition count is zero; no descriptor or claim of verified prohibition enforcement is authored.
</prohibition_recall>

<verification>
Run the exact focused owner commands above, then npm run typecheck, npm run lint, npm run fallow, and npm run test:corresponding as applicable during implementation. At the completed wave run npm run check. Run npm run test:coverage:direct:all after the shared scanner or transaction contract changes; do not confuse aggregate coverage with pair coverage. Long-running full gates are wave checks, outside the under-60-second focused feedback loop. No disabled tests, weakened coverage gates, or environment-error substitutions are acceptable.
</verification>

<success_criteria>
The real uninstall operation preserves data only when requested; deletion remains the shared promptless default. Existing pass-through scanner contracts pass alongside consuming-mode checks. Reconcile and failure/cleanup behavior have direct evidence. The full wave gate passes before Plan 02.
</success_criteria>

<output>
Create .planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-01-SUMMARY.md with task results, direct coverage evidence, commands and the three flagged engine classifications.
</output>
