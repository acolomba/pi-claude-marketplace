---
phase: 02-uninstall-data-disposition-and-the-uninstall-option-seam
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - extensions/pi-claude-marketplace/edge/flag-catalog.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
  - tests/edge/flag-catalog.test.ts
  - tests/edge/handlers/plugin/uninstall.test.ts
  - tests/architecture/flag-catalog-drift.test.ts
  - docs/output-catalog.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02-03: /claude:plugin uninstall --keep-data removes the installed plugin while preserving its nested persistent data bytes."
    - "D-02-04: Omitting --keep-data removes data without confirmation; documentation states that reconcile uses this same default."
    - "D-02-02: Usage and catalog-derived completions offer --keep-data with a concise preservation description."
    - "D-02-05: Uninstall rejects --delete-data, -y, --yes and --prune as unknown flags before any mutation, while retaining --scope and --local."
    - "D-02-01: Successful preservation uses the existing uninstall success format with no new report or path trailer."
    - "D-02-06: The command introduces neither cross-scope data checks nor retained-data collection."
  artifacts:
    - path: extensions/pi-claude-marketplace/edge/flag-catalog.ts
      provides: "CATALOG.uninstall preservation entry with parse and complete enabled"
      contains: "--keep-data"
    - path: extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
      provides: "makeUninstallHandler consuming catalog-owned names and forwarding keepData"
      exports: [makeUninstallHandler]
    - path: tests/edge/handlers/plugin/uninstall.test.ts
      provides: "Command-to-filesystem preservation, default deletion and no-mutation rejection cases"
      contains: "--keep-data"
    - path: tests/edge/flag-catalog.test.ts
      provides: "Independent expected uninstall completion descriptions and accepted names"
      contains: "--keep-data"
    - path: tests/architecture/flag-catalog-drift.test.ts
      provides: "Exact uninstall accepted-set pin including preservation and the shared target flag"
      contains: "--keep-data"
    - path: docs/output-catalog.md
      provides: "Preservation/default policy beside the existing uninstall byte contract"
      contains: "--keep-data"
  key_links:
    - from: extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
      to: extensions/pi-claude-marketplace/edge/flag-catalog.ts
      via: "passThroughFlagNames(uninstall) supplies consumeLongFlags to the existing scanner"
      pattern: "passThroughFlagNames"
    - from: extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      via: "Detected --keep-data forwards keepData true through the existing operation call"
      pattern: "keepData"
    - from: extensions/pi-claude-marketplace/edge/completions/provider.ts
      to: extensions/pi-claude-marketplace/edge/flag-catalog.ts
      via: "Existing flagCompletions consumes completionFlagEntries"
      pattern: "completionFlagEntries"
---

<objective>
Expose uninstall data preservation through the command, usage, completions and documentation, per all six D-02 decisions.

Purpose: Give users the settled opt-out through the shared seam proven by Plan 01, with consistent errors and success output.
Output: Catalog-owned option, command behavior tests, exact drift pins and concise policy documentation.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-CONTEXT.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-RESEARCH.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-PATTERNS.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-VALIDATION.md
@.planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-01-SUMMARY.md
@CLAUDE.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/simple-english/SKILL.md

Plan 01 supplies UninstallPluginOptions.keepData and extractLocalFlag's named consuming form. Use its public contract directly. The existing completion provider already consumes the catalog, and reconcile already omits the option. Neither needs a separate production path.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run preservation from the command through the shared operation</name>
  <reversibility rating="one-way">The new public command option is a compatibility promise; D-02-02 and D-02-03 already authorize it, so no repeated decision checkpoint is needed.</reversibility>
  <files>extensions/pi-claude-marketplace/edge/flag-catalog.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts, tests/edge/flag-catalog.test.ts, tests/edge/handlers/plugin/uninstall.test.ts, tests/architecture/flag-catalog-drift.test.ts</files>
  <read_first>
    extensions/pi-claude-marketplace/edge/flag-catalog.ts — CATALOG.uninstall, passThroughFlagNames, completionFlagEntries, parseFlagNames.
    extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts — USAGE, makeUninstallHandler and existing scope/write-target forwarding.
    extensions/pi-claude-marketplace/edge/handlers/shared.ts — consuming scanner contract from Plan 01.
    extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts — catalog-derived accepted extras and opt-in option forwarding pattern.
    tests/edge/handlers/plugin/uninstall.test.ts — real temporary workspace and readObservedEffects whole-footprint checks.
    tests/edge/flag-catalog.test.ts and tests/architecture/flag-catalog-drift.test.ts — independent expected entries and HANDLER_ACCEPTED_PARSE_SETS.uninstall.
    extensions/pi-claude-marketplace/edge/completions/provider.ts — existing catalog consumer; read only.
  </read_first>
  <behavior>
    - Calling the real handler with --keep-data before or after the reference removes the installed record/artifacts while preserving independently seeded nested data bytes.
    - Duplicate --keep-data options behave like existing duplicate booleans, with one successful uninstall.
    - Preservation composes with --scope user, --scope project and --local; selected configuration and scope behavior remain correct.
    - Without the flag, the handler deletes non-empty data with no confirmation interaction.
    - --delete-data, -y, --yes, --prune, an unrelated long option and --keep-data=false reject in prefix and suffix positions with exact unknown-flag output and the seeded installation/data footprint unchanged.
    - Empty/invalid references and malformed scope values follow current usage behavior; a flag-looking scope value cannot silently activate preservation.
    - Completion and parse-set owners independently expect --keep-data followed by --local; the existing global --scope entry remains unchanged.
  </behavior>
  <action>
    Start with one failing command-to-filesystem preservation case using the real makeUninstallHandler and a seeded workspace. Per D-02-02, add a CATALOG.uninstall entry named --keep-data before WRITE_TARGET_FLAG_ENTRY, with parse and complete true and description "Preserve the plugin's persistent data directory". Use passThroughFlagNames("uninstall") as the consumeLongFlags list in extractLocalFlag's new form. The handler must consume the scanner's detected flags and residualArgs; do not scan raw arguments again or maintain a second accepted-name list.

    Update USAGE to "Usage: /claude:plugin uninstall <plugin>@<marketplace> [--scope user|project] [--keep-data] [--local]". Parse the residual with the existing parseRequiredPluginMarketplaceRef. Forward keepData: true only when the scanner reports --keep-data; otherwise omit the property. Preserve optional scope forwarding and local: true forwarding. Per D-02-03 and D-02-04, the existing shared operation supplies both dispositions and promptless defaults. Per D-02-01, do not modify the success renderer or add notifications.

    Update the catalog owner's complete expected descriptions, parse names and pass-through names; update the architecture guard's ordered uninstall pin to ["--keep-data", "--local"]. Retain independent expectations rather than deriving expected values from the catalog. Do not widen the accepted set to satisfy the guard. Use the unchanged completion provider's existing tests as integration evidence.

    Expand the handler owner with all behavior cases above. On rejection, seed installed artifacts, declaration, state and persistent data so a wrong dispatch would be observable; compare complete notification and whole before/after effects, plus exact data bytes. Per D-02-05, short-option rejection comes from the scanner's consuming mode. Per D-02-06, keep scope isolation through existing resolution; no cross-scope data search or collection is introduced. Cover both dispositions and the local branch directly in this owner without substituting orchestrator coverage.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/shared.test.ts tests/edge/handlers/plugin/uninstall.test.ts tests/edge/flag-catalog.test.ts tests/architecture/flag-catalog-drift.test.ts</automated>
    <fails_when>The command exits nonzero or any named runtime owner reports zero executed tests; changed seeded data or installation state on rejection fails the handler assertions.</fails_when>
    <automated>node --test tests/edge/completions/provider.test.ts</automated>
    <fails_when>The command exits nonzero or the completion-provider owner reports zero executed tests.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts</automated>
    <fails_when>The command exits nonzero or the uninstall-handler source-test pair reports incomplete line, function, or branch coverage.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/flag-catalog.ts</automated>
    <fails_when>The command exits nonzero or the flag-catalog source-test pair reports incomplete line, function, or branch coverage.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both flag positions, duplicate flags, both scopes and local write-target cases reach the real operation and preserve data.
    - The no-flag command removes seeded data and causes no confirmation interaction.
    - All rejected options leave the entire installed footprint and data bytes unchanged.
    - Catalog and drift expectations pin exactly --keep-data and --local for this phase; completions retain --scope.
    - Handler output remains the existing exact uninstall success block.
  </acceptance_criteria>
  <done>Users can run the documented preservation option end to end; accepted flags, completions and rejection behavior agree.</done>
</task>

<task type="auto">
  <name>Task 2: Document the data policy beside the existing uninstall contract</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    docs/output-catalog.md — uninstall command heading, success blocks and adjacent command-option explanations.
    docs/messaging-style-guide.md — notification grammar and usage-error conventions.
    .planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-CONTEXT.md — D-02-01 and D-02-02.
  </read_first>
  <action>
    Per D-02-01 and D-02-02, add a short option/usage explanation adjacent to the uninstall command contract. State that --keep-data preserves persistent plugin data while uninstall removes the plugin, and omission deletes data without confirmation. State that config-driven reconcile uses this same deletion default, per D-02-04 and DATA-03. Use the same flag ordering and command syntax as Task 1.

    Leave the current success and soft-dependency-success catalog text blocks and catalog-state markers byte-identical. The option does not create a new notification variant, retained-path trailer, or separate success report. Use plain wording following the existing documentation style. Future pruning and retained-data collection are outside this phase and are not advertised as available. Do not create a new catalog state solely for an option whose output bytes are unchanged.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/uninstall.test.ts tests/edge/flag-catalog.test.ts tests/architecture/flag-catalog-drift.test.ts</automated>
    <fails_when>The command exits nonzero or any named runtime owner reports zero executed tests.</fails_when>
    <automated>pre-commit run --files docs/output-catalog.md</automated>
    <fails_when>The command exits nonzero or reports a failed hook for docs/output-catalog.md.</fails_when>
    <human-check>Compare the edited uninstall paragraph with the exact handler USAGE, completion description, and promptless reconcile default. Compare the existing success text blocks with the pre-edit version; their bytes must match.</human-check>
    <fails_when>The documentation omits either disposition or an existing success text block differs from its pre-edit bytes.</fails_when>
  </verify>
  <acceptance_criteria>
    - The uninstall section explains preservation, promptless default deletion and reconcile behavior in the existing style.
    - The option syntax and order match the tested handler and catalog.
    - Existing success block bytes and catalog-state markers remain unchanged.
  </acceptance_criteria>
  <done>Users can find the preservation option and its default policy without a changed success-output contract.</done>
</task>

</tasks>

<threat_model>
Security policy: ASVS level 1; high findings block. No external service, package installation, schema push, or credential setup is involved.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User argument text to uninstall operation | Unrecognized options must not initiate destructive default behavior |
| Catalog to handler and completion provider | Advertised options must match the accepted command contract |
| Operation outcome to user notification | Data policy must be explained without contradicting existing output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | makeUninstallHandler | high | mitigate | Task 1 rejects unknown short/long options through the consuming scanner and proves the full footprint stays unchanged |
| T-02-05 | Tampering | Catalog and accepted-set guard | medium | mitigate | Task 1 updates independent completion expectations and the exact ordered uninstall pin |
| T-02-06 | Information disclosure | Uninstall success output | low | mitigate | Tasks 1 and 2 keep existing output bytes and introduce no retained-data path trailer |
</threat_model>

<dependency_graph>
| Task | Needs | Creates | Checkpoint |
|------|-------|---------|------------|
| 02-01-01 | Existing uninstall operation and filesystem fixtures | Typed keepData policy through real cleanup | No |
| 02-01-02 | Existing scanner and caller contracts | Compatible consuming form and strict rejection evidence | No |
| 02-01-03 | 02-01-01 | Default, failure, cleanup and reconcile evidence | No |
| 02-02-01 | 02-01-01 and 02-01-02 | Catalog-driven command-to-filesystem preservation | No |
| 02-02-02 | 02-02-01 | Tested command policy documented beside existing output | No |

Plan 02 depends on Plan 01's operation and scanner contracts. Waves are 1 then 2; there is no Phase 5 prerequisite. Task 02-01-03 extends its plan's operation owner sequentially. Plan file ownership does not overlap across waves. No non-file shared resource or external setup adds another dependency.
</dependency_graph>

<source_audit>
| Source | ID | Feature or constraint | Plan/task | Status |
|--------|----|-----------------------|-----------|--------|
| GOAL | Phase 2 | Opt out of data deletion through one shared uninstall option seam | 02-01-01/02; 02-02-01 | COVERED |
| GOAL | SC-1 | Remove artifacts/record while retaining nested data | 02-01-01/03; 02-02-01 | COVERED |
| GOAL | SC-2 | Default promptless deletion | 02-01-01/03; 02-02-01 | COVERED |
| GOAL | SC-3 | Reconcile deletion and repeated convergence | 02-01-03 | COVERED |
| GOAL | SC-4 | Usage, completions and rejected aliases | 02-01-02; 02-02-01/02 | COVERED |
| REQ | DATA-01 | Preserve persistent data on request | 02-01-01/03; 02-02-01 | COVERED |
| REQ | DATA-02 | Delete without a prompt by default | 02-01-01/03; 02-02-01 | COVERED |
| REQ | DATA-03 | Reconcile uses omitted default | 02-01-03; 02-02-02 | COVERED |
| CONTEXT | D-02-01 | Exact current success output, no report/trailer | 02-01-01/03; 02-02-01/02 | COVERED |
| CONTEXT | D-02-02 | Catalog, usage, concise help, default documentation | 02-02-01/02 | COVERED |
| CONTEXT | D-02-03 | Keep data, remove artifacts and record | 02-01-01/03; 02-02-01 | COVERED |
| CONTEXT | D-02-04 | Promptless command/reconcile default | 02-01-01/03; 02-02-01/02 | COVERED |
| CONTEXT | D-02-05 | Rejected aliases, existing scope/target, pruning boundary | 02-01-02; 02-02-01 | COVERED |
| CONTEXT | D-02-06 | Existing scope partition, no retained-data collection | 02-01-01/03; 02-02-01 | COVERED |
| RESEARCH | R-1 | Existing dependencies and module ownership; no migration | Both plan contexts and task file lists | COVERED |
| RESEARCH | R-2 | One-pass extraction; preserve old pass-through and scope-value semantics | 02-01-02; 02-02-01 | COVERED |
| RESEARCH | R-3 | Optional operation boolean; reconcile omission | 02-01-01/03 | COVERED |
| RESEARCH | R-4 | Guard only data path resolution/removal after commit | 02-01-01/03 | COVERED |
| RESEARCH | R-5 | Preserve cache, hooks, clone hygiene and error behavior | 02-01-01/03 | COVERED |
| RESEARCH | R-6 | Keep deletion containment outside removal catch | 02-01-01/03 | COVERED |
| RESEARCH | R-7 | Update both catalog owner and architecture pin | 02-02-01 | COVERED |
| RESEARCH | R-8 | Real nested bytes, default/false, missing data, failed uninstall | 02-01-01/03 | COVERED |
| RESEARCH | R-9 | Full owner coverage, hermetic tests and whole-project gate | All task verification blocks and both plan verification blocks | COVERED |
| RESEARCH | R-10 | Existing FIFO requires permitted subprocess execution | 02-01-03 and 02-VALIDATION.md | COVERED |
| RESEARCH | R-11 | No new output variant, preserve notification boundary | 02-01-01; 02-02-01/02 | COVERED |
| RESEARCH | R-12 | Offline uninstall and existing atomic/retry behavior | 02-01 context, Tasks 1/3 and full project gate | COVERED |

Deferred exclusions: --prune and FLAG-01 implementation belong to Phase 5; retained-data garbage collection and the unrelated unused-type-members todo remain deferred. No source item assigned to Phase 2 is missing.
</source_audit>

<flagged_assumptions>
Carry the three unclassified/unresolved DATA-01/02/03 engine rows from Plan 01 into the phase result without changing their engine status. Task 1 adds command-entry evidence for DATA-01/02; Plan 01 Task 3 owns DATA-03's runtime proof. The data-policy decisions themselves are settled. Prohibition recall and its zero-kept precision result are recorded in Plan 01; no descriptor-less item is silently omitted.
</flagged_assumptions>

<verification>
After Task 1, require complete direct coverage for handler and catalog, and passing completion/drift tests. After the completed wave run npm run check. Carry forward the completed direct-all result from Plan 01 unless later changes alter a shared contract, fake, or harness, in which case rerun npm run test:coverage:direct:all. Require the final source tree to pass the complete gate, including typecheck, lint, fallow, formatting, test pairing, unit and integration tests.
</verification>

<success_criteria>
All four roadmap success criteria, DATA-01/02/03 and D-02-01 through D-02-06 have execution evidence. Usage, completions, command dispatch, shared default, error rejection and documentation agree. The phase remains ready for a later option on the same seam without implementing it here.
</success_criteria>

<output>
Create .planning/phases/02-uninstall-data-disposition-and-the-uninstall-option-seam/02-02-SUMMARY.md with source-audit coverage, task evidence, full-gate result and unresolved engine classifications carried honestly.
</output>
