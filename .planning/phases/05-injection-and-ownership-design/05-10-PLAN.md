---
phase: 05-injection-and-ownership-design
plan: 10
type: execute
wave: 7
depends_on: [05-09]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
  - extensions/pi-claude-marketplace/shared/completion-cache.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
  - tests/architecture/flag-catalog-drift.test.ts
  - tests/architecture/hooks-async-rewake.test.ts
  - tests/architecture/hooks-if-field.test.ts
  - tests/bridges/hooks/async-rewake/registry.test.ts
  - tests/bridges/hooks/dispatch.test.ts
  - tests/bridges/hooks/event-adapters.test.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/bridges/hooks/routing-state.test.ts
  - tests/bridges/hooks/settle.test.ts
  - tests/edge/completions/data.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/edge/handlers/plugin/list.test.ts
  - tests/integration/hooks-additionalcontext-end-to-end.test.ts
  - tests/integration/hooks-cross-scope-reconcile.test.ts
  - tests/integration/hooks-dispatch-end-to-end.test.ts
  - tests/integration/hooks-spawn-end-to-end.test.ts
  - tests/integration/transaction-lifecycle-cascade.test.ts
  - tests/orchestrators/marketplace/add.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/shared/completion-cache.test.ts
autonomous: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A fresh production-consumer census governs terminal-surface removal: production behavior is preserved through a lifecycle-named operation when genuinely consumed, while declarations used only by tests are deleted (D-05-07)."
    - "No resetEpoch, resetRoutingState, or resetCompletionCache declaration, export, import, call, or test-shaped replacement remains; every affected case obtains a fresh HooksRuntime or CompletionCache from its case-owned fixture (D-05-07)."
    - "The list handler keeps its catalog-derived private flag set and exact public parsing/usage behavior, while its BOOLEAN_FLAGS export/import and derived equality case are gone; the independent literal catalog pin remains exact (D-05-08)."
    - "Each of the eleven MF-DEC-07 roots has exactly one terminal classification and one production-shaped proof: ordinary portable behavior uses a fresh real temporary tree, and only the classified irreproducible fault, race, probe, or schedule uses its required consumer-owned port."
    - "applyReconcile and bootstrapClaudePlugin remain exactly the two behavioral-composition exceptions, with complete public outcomes and unchanged operation, commit, rollback, cleanup, routing, invalidation, diagnostic, and notification order."
    - "All authorized createRequire and syncBuiltinESMExports machinery remains byte-for-byte available for Phase 6, and no Phase 6 split, ownership migration, or global-patch removal is performed (D-05-03, D-05-09)."
    - "The exact source line `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` remains in scripts/revalidation.mjs, no new Fallow suppression exists, and focused, direct-pair, security, and full repository gates pass."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - extensions/pi-claude-marketplace/shared/completion-cache.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
    - tests/bridges/hooks/routing-state.test.ts
    - tests/shared/completion-cache.test.ts
    - tests/architecture/flag-catalog-drift.test.ts
    - tests/edge/handlers/plugin/list.test.ts
  key_links:
    - "fresh test/extension fixture -> createHooksRuntime -> registration, hydration, routing, settle, and async public behavior without a cleanup export"
    - "fresh test/command fixture -> createCompletionCache -> completion reads and mutation invalidation without process-global cleanup"
    - 'makeListHandler -> private parseFlagNames("list") set -> public listPlugins options, while the architecture suite owns an independent literal catalog pin'
    - "eleven MF-DEC-07 owner suites -> classified consumer-owned ports or real temporary trees -> explicit real production adapters"
    - "Phase 5 final census -> retained global-patch inventory and exact Fallow source line -> Phase 6 handoff"
  prohibitions:
    - "No test-only export or reset, __deps member, ForTest surface, optional/default/dead port, hidden singleton fallback, raw mutable-state exposure, or test-shaped branch."
    - "No broadening, weakening, or interaction-only replacement of the exactly two approved behavioral-composition exceptions."
    - "No change to established operation scheduling, scope order, commit eligibility, rollback, cleanup, route rebuild, invalidation, Error identity, diagnostics, silence, notification count, severity, or bytes."
    - "No Phase 6 createRequire/syncBuiltinESMExports/global-patch work, resolver/notify/install/update/reinstall/list/catalog split, source-test ownership move, or other large-module split."
    - "No edit to tests/bridges/skills/stage.test.ts, PID-table redesign, plugin-info split, uninstall decomposition, excluded bridge/skill work, coverage exclusion, lint suppression, or new Fallow suppression."
---

<objective>
After a fresh production-consumer census, remove the terminal test-only reset/export and BOOLEAN_FLAGS surfaces, migrate every surviving test caller to fresh production-shaped fixtures, retain the independent catalog pin and public handler behavior, and close Phase 5 with exact classification, boundary, security, focused, direct, and repository evidence (D-05-03, D-05-07, D-05-08, D-05-09).

Purpose: Leave Phase 5 with only genuine production-owned contracts, isolated tests that exercise public behavior, and an auditable handoff that preserves all Phase 6 work and every established transaction/lifecycle invariant.
Output: Terminal public-surface cleanup, fresh runtime/cache test lifecycle, retained list/catalog behavior, and complete eleven-root plus repository-close evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-04-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-06-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-09-SUMMARY.md

<interfaces>
Plan 05-04 provides the final `HooksRuntime` and `createHooksRuntime()` names, production lifecycle transition, and runtime-bound hook fixture shape. Use those exact identifiers from 05-04-SUMMARY.md; do not recreate a compatibility singleton or cleanup operation.

Plan 05-06 provides the final `CompletionCache` and factory names, cache operations, and completion fixture shape. Use the exact summary contract; schema versions, TTL, poison behavior, thrown values, memory-only invalidation, and disk drop remain unchanged.

Plan 05-09 provides the completed extension-owned runtime/cache/adapter graph and the final apply/bootstrap composition evidence. A test that needs the joined graph constructs the same production-shaped extension or command fixture; it does not assemble a test-only alternate graph.

`makeListHandler` keeps its local `parseFlagNames("list")`-derived set. Only its test-facing re-export disappears. `tests/architecture/flag-catalog-drift.test.ts` keeps the independently authored `HANDLER_ACCEPTED_PARSE_SETS.list` literal row, and `tests/edge/handlers/plugin/list.test.ts` remains the public handler proof.
</interfaces>
</context>

## Edge assumptions

- The current production-consumer census at Task 1 start is authoritative. Removal occurs only after CodeGraph and production-only text search agree that a named terminal surface has no genuine production caller; a newly discovered production behavior is migrated to the lifecycle/factory contract rather than deleted.
- Every test case owns a fresh runtime/cache directly or calls a factory fixture that constructs fresh instances per invocation. No suite-level mutable instance, cleanup registry, or reset-after-use lifecycle is acceptable.
- All eleven MF-DEC-07 roots have exactly one classification: BSKL-019, HHD-011, HSA-026, OPEF-F04, OPEF-F09, the full-path OPIA-F05 root, OPIC-F27, OPLU-B-F10, OPR-B-F14, ORA-F15, and SHC-F003.
- Ordinary reproducible filesystem behavior receives no port and stays on a case-owned real temporary tree. Only the already classified irreproducible fault, timing edge, operation schedule, rollback point, probe sequence, hydration read, or state-read race uses a narrow required port supplied by production composition.
- Production operation schedules and order remain exact across hook registration/reload, hydration, dispatch, settle, async child handling, install/reinstall/uninstall/enable-disable transactions, reconcile, completion invalidation, rollback, cleanup, diagnostics, and notifications.
- The private list flag set remains derived from the production catalog. The independent literal pin is not recomputed through production code, and public list-handler cases remain the authority for accepted and rejected arguments.
- Phase 6 owns every authorized builtin synchronization removal and approved resolver, notify, install, update, reinstall, list, and catalog split. This plan records and preserves that boundary without beginning it.
- The source comment `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` remains exactly one whole line in `scripts/revalidation.mjs`; all eleven current `fallow-ignore` occurrences across eight source/test/script files remain the measured no-addition baseline.

## Artifacts this plan produces

| File family                                            | Contract or evidence completed                                                                                                                                                                |
| ------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hooks routing source and former reset consumers        | No terminal cleanup export or call; each case/fixture creates its own production HooksRuntime and retains exact callback/state/message assertions.                                            |
| Completion cache source and former reset consumers     | No whole-cache cleanup export or call; each case/fixture creates its own production CompletionCache and retains exact cache/file/public-result assertions.                                    |
| Plugin list handler and flag architecture/owner suites | Private catalog-derived parsing remains; the test-facing flag export and tautological equality case disappear while the independent literal row and public behavior remain.                   |
| Phase-close evidence in `05-10-SUMMARY.md`             | Eleven unique root classifications, exactly two composition exceptions, Phase 6 inventory, ASVS L1 mitigations, static censuses, focused suites, direct coverage, and full repository result. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace fresh runtime and cache fixtures through terminal reset removal</name>
  <read_first>AGENTS.md, .planning/phases/05-injection-and-ownership-design/05-04-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-06-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-09-SUMMARY.md, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, extensions/pi-claude-marketplace/shared/completion-cache.ts, tests/bridges/hooks/routing-state.test.ts, tests/bridges/hooks/event-router.test.ts, tests/shared/completion-cache.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, extensions/pi-claude-marketplace/shared/completion-cache.ts, tests/architecture/flag-catalog-drift.test.ts, tests/architecture/hooks-async-rewake.test.ts, tests/architecture/hooks-if-field.test.ts, tests/bridges/hooks/async-rewake/registry.test.ts, tests/bridges/hooks/dispatch.test.ts, tests/bridges/hooks/event-adapters.test.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/routing-state.test.ts, tests/bridges/hooks/settle.test.ts, tests/edge/completions/data.test.ts, tests/edge/completions/provider.test.ts, tests/integration/hooks-additionalcontext-end-to-end.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts, tests/integration/hooks-dispatch-end-to-end.test.ts, tests/integration/hooks-spawn-end-to-end.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/plugin/update.test.ts, tests/shared/completion-cache.test.ts</files>
  <behavior>
    - "A production-only CodeGraph plus text census finds no genuine caller that requires the three terminal whole-state cleanup surfaces before their declarations are removed."
    - "Two cases run in either order and through independently invoked fixtures without sharing hook generation/routes/context/settle/child state or completion memory/poison/TTL state."
    - "Former hook cleanup callers obtain the 05-04 production runtime per case and drive real registration, input, mutation, or callback behavior; former completion cleanup callers obtain the 05-06 cache per case and drive public cache/completion/mutation behavior."
    - "Owner assertions continue to compare exact callback results, persisted state/configuration/cache bytes, files and trees, notifications, errors, cleanup, and operation order before supplemental port observations."
    - "No terminal reset name or renamed whole-state cleanup surface remains in production or tests after the migration."
  </behavior>
  <action>Read the three dependency summaries before using any factory or fixture identifier. Use CodeGraph first on the final HooksRuntime factory, CompletionCache factory, and each terminal whole-state cleanup symbol, then run separate production-only and test caller censuses. Per D-05-07, let the current production census govern disposition: if a real production behavior has appeared, route that behavior through the already production-owned lifecycle operation with its domain name; otherwise delete the obsolete declaration and export. Migrate every surviving test caller returned by the census to construct a fresh runtime or cache inside the case or through a concern-owned fixture that constructs on every invocation. Do not share a mutable instance through module scope or suite setup, and do not add teardown cleanup to simulate isolation. Add the failing tracer first: run two representative hook/cache cases in both orders using separate production-shaped fixtures and prove exact public outcomes plus state/file isolation. Preserve every existing independently authored result, tree, byte, Error, message, notification, cleanup, and ordering assertion, and preserve builtin synchronization lines in affected tests for D-05-03. Keep ordinary filesystem cases on their real case-owned temporary trees; the migration does not introduce another port.</action>
  <verify><automated>if rg -n '\b(resetEpoch|resetRoutingState|resetCompletionCache)\b' extensions/pi-claude-marketplace tests --glob '*.ts'; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; node --test tests/architecture/flag-catalog-drift.test.ts tests/architecture/hooks-async-rewake.test.ts tests/architecture/hooks-if-field.test.ts tests/bridges/hooks/async-rewake/registry.test.ts tests/bridges/hooks/dispatch.test.ts tests/bridges/hooks/event-adapters.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/routing-state.test.ts tests/bridges/hooks/settle.test.ts tests/edge/completions/data.test.ts tests/edge/completions/provider.test.ts tests/integration/hooks-additionalcontext-end-to-end.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/integration/hooks-dispatch-end-to-end.test.ts tests/integration/hooks-spawn-end-to-end.test.ts tests/integration/transaction-lifecycle-cascade.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/plugin/update.test.ts tests/shared/completion-cache.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/completion-cache.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts extensions/pi-claude-marketplace/shared/completion-cache.ts tests/architecture/flag-catalog-drift.test.ts tests/architecture/hooks-async-rewake.test.ts tests/architecture/hooks-if-field.test.ts tests/bridges/hooks/async-rewake/registry.test.ts tests/bridges/hooks/dispatch.test.ts tests/bridges/hooks/event-adapters.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/routing-state.test.ts tests/bridges/hooks/settle.test.ts tests/edge/completions/data.test.ts tests/edge/completions/provider.test.ts tests/integration/hooks-additionalcontext-end-to-end.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/integration/hooks-dispatch-end-to-end.test.ts tests/integration/hooks-spawn-end-to-end.test.ts tests/integration/transaction-lifecycle-cascade.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/plugin/update.test.ts tests/shared/completion-cache.test.ts --max-warnings=0</automated><fails_when>The production census shows a genuine caller and its behavior is deleted; the post-edit census finds any terminal name; any named suite, direct coverage, typecheck, or ESLint gate fails or discovers zero tests; two cases or fixtures share runtime/cache state; a case uses suite cleanup, a hidden singleton, raw internals, or a renamed reset; an ordinary filesystem case gains a port; or any exact public result, state/config/cache byte, tree, Error, message, notification, cleanup, invalidation, rollback, or operation order changes.</fails_when></verify>
  <acceptance_criteria>Every former terminal cleanup caller uses a fresh production-shaped lifecycle or cache fixture, public observations remain exact, and the three obsolete reset surfaces are absent after a current-consumer disposition (D-05-03, D-05-07).</acceptance_criteria>
  <reversibility rating="costly">Removing shared-process cleanup finalizes the lifecycle/factory ownership boundary across many owner suites; reversing it would retouch every runtime/cache consumer and reintroduce cross-case state.</reversibility>
  <done>Runtime and cache isolation comes exclusively from construction and production lifecycle behavior, with no terminal reset symbol or test-shaped replacement left.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Privatize list flags while retaining independent and public proofs</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-06-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-09-SUMMARY.md, extensions/pi-claude-marketplace/edge/flag-catalog.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts, tests/architecture/flag-catalog-drift.test.ts, tests/edge/flag-catalog.test.ts, tests/edge/handlers/plugin/list.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts, tests/architecture/flag-catalog-drift.test.ts, tests/edge/handlers/plugin/list.test.ts</files>
  <behavior>
    - "The list handler's catalog-derived boolean-filter set remains module-private and accepts installed, available, unavailable, partial, and remote in the same combinations and order-independent form."
    - "The architecture suite no longer imports the handler's private set or compares that set with the same production-derived parse set."
    - "The independent `HANDLER_ACCEPTED_PARSE_SETS.list` row remains exactly `[\"--available\", \"--installed\", \"--partial\", \"--remote\", \"--unavailable\"]` and still gates every catalog verb."
    - "Public handler cases retain exact options forwarded to listPlugins plus exact usage errors for unknown long flags, excess positionals, and scope handling."
  </behavior>
  <action>Use CodeGraph first on `makeListHandler`, its local catalog-derived set, `parseFlagNames`, the architecture import, and every public list-handler case. Per D-05-07 and D-05-08, delete only the test-facing re-export, its architecture import, and the equality case whose expected value is derived from the same production parser. Keep the private constant derived from `parseFlagNames("list")`; do not duplicate its values in production. Retain the independent literal row verbatim and the architecture loop that compares every catalog parse set with its independently authored handler contract. In the handler owner, retain or strengthen public cases so each accepted list filter reaches the exact `listPlugins` option bag and rejected tokens retain exact usage behavior. Do not replace public results with private-set assertions, do not broaden the catalog API, and do not change list orchestration, notifications, tally, or output bytes.</action>
  <verify><automated>if rg -n 'export[[:space:]]*\{[^}]*BOOLEAN_FLAGS' extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; if rg -n '\bBOOLEAN_FLAGS\b' tests --glob '*.ts'; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; rg -n -F 'const BOOLEAN_FLAGS = parseFlagNames("list");' extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts &amp;&amp; rg -n -F 'list: ["--available", "--installed", "--partial", "--remote", "--unavailable"],' tests/architecture/flag-catalog-drift.test.ts &amp;&amp; node --test tests/architecture/flag-catalog-drift.test.ts tests/edge/flag-catalog.test.ts tests/edge/handlers/plugin/list.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts tests/architecture/flag-catalog-drift.test.ts tests/edge/flag-catalog.test.ts tests/edge/handlers/plugin/list.test.ts --max-warnings=0</automated><fails_when>The handler re-export or any test reference remains; the private catalog-derived set or independent literal pin is deleted, re-derived, reordered, or weakened; any accepted filter, unknown option, positional, or scope behavior changes; public owner assertions are replaced with internal equality; or a named suite, direct coverage, typecheck, or ESLint gate fails or discovers zero tests.</fails_when></verify>
  <acceptance_criteria>The list flag implementation is private, the tautological architecture coupling is absent, and independent literal catalog coverage plus public list-handler behavior remain exact (D-05-07, D-05-08).</acceptance_criteria>
  <done>No test can import the list handler's private flag set, while catalog drift and every public parsing outcome remain discriminating and fully covered.</done>
</task>

<task type="auto">
  <name>Task 3: Seal eleven-root, Phase 6, ASVS L1, and repository gates</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-02-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-03-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-04-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-05-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-06-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-07-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-08-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-09-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-RESEARCH.md, scripts/revalidation.mjs, tests/bridges/skills/unstage.test.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/stage.test.ts, tests/orchestrators/plugin/fetch.test.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/shared/path-safety.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>No source files; verification-only task whose evidence is recorded in 05-10-SUMMARY.md.</files>
  <behavior>
    - "The final census contains exactly the eleven authorized MF-DEC-07 roots, each once, with one owner, one terminal classification, one required real adapter when a port is authorized, and one public owner proof; no twelfth root receives a port."
    - "Every ordinary success, missing-state, corrupt-state, and portable filesystem failure stays on a fresh real temporary tree; only the root's classified irreproducible edge uses its narrow consumer-owned production port."
    - "applyReconcile and bootstrapClaudePlugin remain the only two broad behavioral-composition proofs and retain real child calls, complete exact state/configuration/scope-tree/results, and exact notifications."
    - "The final global-patch census remains fourteen syncBuiltinESMExports files with 83 invocations and nine createRequire invocations, entirely in tests, ready for Phase 6 without any removal or split work in Phase 5."
    - "The measured Fallow baseline remains eleven occurrences across eight files, including the exact revalidation source comment, and the Phase 5 diff adds no dependency-bag/test-only/suppression surface."
    - "Focused owners, every direct pair, TypeScript, ESLint, Fallow, Prettier, corresponding-test gates, unit tests, integration tests, and the complete repository check all pass."
  </behavior>
  <action>Use CodeGraph first to enumerate the final production consumers and real adapters for the eleven root owners, then reconcile the result against the root table in 05-RESEARCH and Plans 05-01 through 05-09. Record one row per root in 05-10-SUMMARY.md with root ID, source owner, terminal classification, production adapter or real-tree disposition, public owner test, and relevant passing command; reject duplicates, omissions, an extra port, or an ordinary behavior routed through a collaborator. Inspect the two approved composition paths and record that their real children plus complete observable assertions remain intact and no third exception exists. Re-run the builtin synchronization census before gates and compare it with the measured Phase 5 boundary; D-05-03 and D-05-09 require exact retention for Phase 6. Compare the production/test/script diff from Phase 5's recorded state head `8d417bc5b749c9256ad037ec360765d3fc92b9ab` and reject added dependency-bag, test-only, or suppression lines. Preserve the exact revalidation comment and measured Fallow inventory. Run the eleven owner suites plus apply/bootstrap and PID safety, then all direct pairs and the complete repository check. If a gate fails, repair only an in-scope Phase 5 regression in its existing owner; do not enter the excluded global-patch, split, stage-skill, PID, info-split, or uninstall-decomposition work.</action>
  <verify><automated>node --test tests/bridges/skills/unstage.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/stage.test.ts tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/info.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/persistence/state-io.test.ts tests/orchestrators/reconcile/apply.test.ts tests/shared/path-safety.test.ts tests/orchestrators/plugin/bootstrap.test.ts tests/bridges/hooks/async-rewake/registry.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; if rg -n '\b(resetEpoch|resetRoutingState|resetCompletionCache)\b' extensions/pi-claude-marketplace tests --glob '*.ts'; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; if rg -n 'export[[:space:]]*\{[^}]*BOOLEAN_FLAGS|import[^;]*BOOLEAN_FLAGS' extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts tests --glob '*.ts'; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; if rg -n '(createRequire|syncBuiltinESMExports)' extensions/pi-claude-marketplace --glob '*.ts'; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; test "$(rg -l 'syncBuiltinESMExports\(' tests --glob '*.ts' | wc -l | tr -d '[:space:]')" = 14 &amp;&amp; test "$(rg -o 'syncBuiltinESMExports\(' tests --glob '*.ts' | wc -l | tr -d '[:space:]')" = 83 &amp;&amp; test "$(rg -o 'createRequire\(' tests --glob '*.ts' | wc -l | tr -d '[:space:]')" = 9 &amp;&amp; rg -n -x -F '// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor' scripts/revalidation.mjs &amp;&amp; test "$(rg -l 'fallow-ignore' extensions/pi-claude-marketplace scripts tests --glob '*.ts' --glob '*.mjs' --glob '*.js' | wc -l | tr -d '[:space:]')" = 8 &amp;&amp; test "$(rg -o 'fallow-ignore' extensions/pi-claude-marketplace scripts tests --glob '*.ts' --glob '*.mjs' --glob '*.js' | wc -l | tr -d '[:space:]')" = 11 &amp;&amp; git diff --quiet 8d417bc5b749c9256ad037ec360765d3fc92b9ab -- scripts/revalidation.mjs tests/bridges/skills/stage.test.ts &amp;&amp; added_lines="$(git diff --unified=0 8d417bc5b749c9256ad037ec360765d3fc92b9ab -- extensions/pi-claude-marketplace tests scripts)" &amp;&amp; if printf '%s\n' "$added_lines" | rg '^\+[^+].*(__deps|ForTest|fallow-ignore)'; then exit 1; else status=$?; test "$status" -eq 1; fi &amp;&amp; npm run test:coverage:direct:all &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; npm run format:check &amp;&amp; npm run check</automated><fails_when>Any root is missing, duplicated, reclassified without authority, or joined by an extra port; ordinary behavior uses a collaborator; a real adapter is absent, optional, defaulted inside the consumer, or dead; either approved composition loses real children or complete observations; operation order changes; a terminal surface remains; the builtin synchronization inventory differs from fourteen files/83 sync calls/nine loader calls or reaches production; the exact source comment, eight-file/eleven-occurrence Fallow baseline, excluded-file diff, or no-added-surface gate fails; or any focused, direct, type, lint, Fallow, format, corresponding, unit, integration, or full repository command fails or discovers zero tests.</fails_when></verify>
  <acceptance_criteria>All eleven roots have one and only one authorized disposition, the public surface and fresh-fixture rules are closed, the exact Phase 6 and suppression boundaries are intact, every ASVS L1 mitigation has executable evidence, and the complete repository is green (D-05-03, D-05-07, D-05-08, D-05-09).</acceptance_criteria>
  <done>Phase 5 closes with exact classification and behavioral evidence, no terminal test seam, no scope leak into Phase 6, and all focused/direct/full gates passing.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                                   | Description                                                                                                                     |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Test process -> production module surface                                  | A test-reachable whole-state operation can mutate another case or extension lifecycle and conceal shared ownership.             |
| Case fixture -> HooksRuntime/CompletionCache                               | A reused instance can leak routing, pending context, settle, child, cache, poison, or TTL state across tests.                   |
| Real filesystem -> eleven classified consumers                             | Over-broad or misplaced ports can bypass containment/symlink behavior or turn ordinary behavior into interaction-only evidence. |
| Mutation/composition -> persisted state, runtime, cache, and notifications | Changed scheduling can expose stale routes/completions or incorrect rollback while weak tests still pass.                       |
| Phase 5 -> Phase 6 global-patch/split program                              | Premature removal or extraction can strand source-test ownership and make the later authorized sequence unverifiable.           |

## STRIDE Threat Register

| Threat ID | Category                           | Component                           | Severity | Disposition | Mitigation Plan                                                                                                                                                                                |
| --------- | ---------------------------------- | ----------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-54   | Tampering / Information disclosure | Terminal reset and export surface   | high     | mitigate    | Remove production/test reachability after a fresh consumer census, use fresh runtime/cache construction, and enforce repository-wide zero-symbol checks plus isolation owner cases.            |
| T-05-55   | Tampering / Information disclosure | Per-case lifecycle ownership        | high     | mitigate    | Construct runtime/cache instances inside each case or per-invocation fixture and run order-independent public behavior tests that prove no cross-case state.                                   |
| T-05-56   | Tampering / Elevation of privilege | Eleven hidden-dependency boundaries | high     | mitigate    | Audit exactly eleven unique classifications, retain real temporary trees for ordinary behavior, require explicit real adapters for narrow ports, and run containment/path-safety owner suites. |
| T-05-57   | Tampering / Repudiation            | Transaction and composition order   | high     | mitigate    | Run complete apply/bootstrap and transaction-lifecycle suites with real children, exact results/files/trees/errors/notifications, and full operation-order assertions.                         |
| T-05-58   | Tampering / Repudiation            | Phase 6 handoff                     | medium   | mitigate    | Pin the fourteen-file/83-invocation synchronization and nine-loader-call test census, forbid production occurrences, and verify excluded files and split ownership remain unchanged.           |
| T-05-59   | Denial of service / Repudiation    | Quality and suppression gates       | low      | accept      | A local gate may consume execution time, but the bounded cost is tolerated because exact Fallow inventory plus focused/direct/full commands prevent hidden dead seams and unverified behavior. |

ASVS Level 1 disposition: every high-severity state-leak, filesystem-boundary, and transaction-order threat blocks completion. The medium Phase 6 handoff threat crosses the primary source-ownership boundary and is mitigated with an exact static census. The low local verification-cost risk is accepted because tests are offline and deterministic. This plan installs no package and changes no authentication, cryptographic, external-service, persisted-schema, or PID-table boundary.
</threat_model>

## Source coverage audit

| Source   | ID                      | Feature or constraint                                                                                     | Plan/task             | Status  | Notes                                                                                                                                  |
| -------- | ----------------------- | --------------------------------------------------------------------------------------------------------- | --------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| GOAL     | -                       | Legitimate test seams with correctly owned runtime state and public contracts                             | 05-10 / Tasks 1-3     | COVERED | Fresh fixture ownership, terminal-surface removal, public behavior, and phase-close evidence complete the outcome.                     |
| REQ      | TREF-04                 | Every hidden dependency is classified; ordinary behavior stays real and ports are narrow/production-owned | 05-10 / Task 3        | COVERED | The final audit requires exactly eleven unique rows, real adapter reachability, real-tree default behavior, and complete owner suites. |
| REQ      | TREF-05                 | Mutable module state has lifecycle/factory ownership without test resets                                  | 05-10 / Task 1        | COVERED | Every surviving caller creates fresh runtime/cache state and all three terminal cleanup surfaces disappear.                            |
| REQ      | TREF-06                 | Tests use public contracts and terminal test-shaped surfaces are removed                                  | 05-10 / Tasks 1-2     | COVERED | Reset callers migrate to public fixtures; flag tests retain independent/public evidence without an internal export.                    |
| RESEARCH | eleven-root table       | BSKL-019 through SHC-F003 each receive exactly one authorized terminal disposition                        | 05-10 / Task 3        | COVERED | Summary evidence and focused owner suites close all eleven without authorizing another root.                                           |
| RESEARCH | terminal public cleanup | Remove/reset or privatize the named reset and list-flag surfaces after current censuses                   | 05-10 / Tasks 1-2     | COVERED | Robust static gates cover production and tests while positive gates retain private and public contracts.                               |
| RESEARCH | fresh ownership         | Runtime/cache isolation comes from construction, never cleanup                                            | 05-10 / Task 1        | COVERED | Per-case/per-fixture factories and order-independent behavior prove isolation.                                                         |
| RESEARCH | validation/security     | Focused owners, direct coverage, full check, containment, stale state, and Phase 6 inventory              | 05-10 / Task 3        | COVERED | ASVS L1 register maps each primary boundary to a named static or behavioral gate.                                                      |
| CONTEXT  | D-05-03                 | Phase 6 owns builtin-global patch removal                                                                 | 05-10 / Tasks 1 and 3 | COVERED | Affected tests retain synchronization code and the final exact census blocks premature removal.                                        |
| CONTEXT  | D-05-07                 | Trace-preserving removal of terminal test-shaped surfaces                                                 | 05-10 / Tasks 1-2     | COVERED | Fresh production census precedes deletion; public behavior replaces internal/reset coupling.                                           |
| CONTEXT  | D-05-08                 | Remove BOOLEAN_FLAGS test coupling while retaining independent pin and handler behavior                   | 05-10 / Task 2        | COVERED | Negative export/import checks and positive literal/private/public checks enforce the whole decision.                                   |
| CONTEXT  | D-05-09                 | Keep approved split and ownership migrations in Phase 6                                                   | 05-10 / Task 3        | COVERED | Static inventory and diff checks retain current files and reject excluded split work.                                                  |

D-05-01, D-05-02, D-05-04, D-05-05, and D-05-06 are implemented by Plans 05-01 through 05-09 and re-gated here through the exact eleven-root, two-composition, fresh-lifecycle, real-tree, and operation-order evidence. Phase 6 global-patch removal and large-module splits, `tests/bridges/skills/stage.test.ts`, PID-table redesign, plugin-info splitting, uninstall decomposition, excluded bridges/skills, and stale documentation work are CONTEXT.md exclusions, not source-audit gaps.

<verification>
Run every task command in order. Confirm the fresh consumer censuses precede removal, all former reset callers now own fresh production-shaped runtime/cache fixtures, the list flag set is private, the independent literal pin and public handler cases remain exact, and no terminal name survives in production or tests. Reconcile one and only one classification row for each of the eleven MF-DEC-07 roots, with ordinary cases on real temporary trees and required real adapters for classified irreproducible cases. Run the complete apply/bootstrap, transaction, path-safety, and PID safety suites; all direct pairs; TypeScript; ESLint; Fallow; Prettier; and `npm run check`. Verify the exact Phase 6 global-patch census, the exact source comment, the measured suppression baseline, unchanged excluded files, and no added test-only/dead/optional/default port or large split.
</verification>

<success_criteria>

- A current production-consumer census precedes every terminal deletion, and no production behavior is lost.
- No named terminal reset exists or is replaced by another whole-state test hook; all affected cases own fresh runtime/cache fixtures.
- The plugin list flag set is private, the tautological internal equality test is gone, and independent literal plus public handler proofs remain exact.
- Exactly eleven authorized hidden-dependency roots have exactly one classification and public owner proof; ordinary behavior has no port.
- applyReconcile and bootstrapClaudePlugin remain the only two broad behavioral compositions with exact schedules, state, trees, results, errors, diagnostics, and notifications.
- The fourteen-file/83-invocation syncBuiltinESMExports and nine-invocation createRequire test inventory remains intact and absent from production for Phase 6.
- `scripts/revalidation.mjs` retains the exact required Fallow comment, the measured suppression baseline does not grow, excluded files remain unchanged, and no forbidden surface or split enters the Phase 5 diff.
- Focused owner suites, direct coverage for every pair, typecheck, lint, Fallow, format, unit, integration, and full repository gates pass.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-10-SUMMARY.md` when done.
</output>
