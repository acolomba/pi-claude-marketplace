---
phase: 05-injection-and-ownership-design
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
requirements: [TREF-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ORA-F15 has exactly one terminal classification: a required reconcile-owned state reader controls only the selected state-read race (D-05-01)."
    - "The public applyReconcile path binds the real reader and retains locks, real child orchestrators, persistence, route work, result projection, and notifications."
    - "applyReconcile remains one of exactly two approved behavioral-composition exceptions and gains no child-operation bundle (D-05-02)."
    - "Race and ordinary cases assert complete results, exact state and both scope trees, failure isolation, redaction, silence, and notification bytes."
    - "Phase 6 retains builtin patch removal, and no split or test-only/default/dead seam enters the apply pair (D-05-03)."
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
      provides: "Required reconcile state-reader contract and explicit real composition"
    - path: tests/orchestrators/reconcile/apply.test.ts
      provides: "Selected-read race plus complete behavioral-composition evidence"
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
      to: extensions/pi-claude-marketplace/transaction/with-state-guard.ts
      via: "required reader supplies only the selected load to the existing locked transaction"
    - from: tests/orchestrators/reconcile/apply.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
      via: "public apply results, persisted state, trees, and notifications remain primary assertions"
---

<objective>
Isolate ORA-F15 behind one required reconcile-owned state reader while preserving the full applyReconcile composition (D-05-01, D-05-02, D-05-03).

Purpose: Reproduce the selected state-read race without replacing real reconcile children or weakening observable effects.
Output: A narrow state-read contract, explicit real adapter, and complete race/composition owner evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md
</context>

## Edge assumptions

- The authorized race occurs after selection of one state snapshot. It does not authorize lock, save, child-orchestrator, route, projection, or notification substitution.
- The real adapter uses the same state load that the locked transaction already performs.
- Plan 05-13 threads runtime ownership through real reconcile installation. This plan does not add a runtime or split apply.
- `bootstrapClaudePlugin` remains the only other composition exception and gains no seam here.

## Artifacts this plan produces

| File family                                  | Contract or proof                                                                                                         |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `orchestrators/reconcile/apply.ts` and owner | Required state-reader contract, explicit real binding, controlled race, and retained full two-scope composition evidence. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the selected state-read race through real reconcile apply</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/apply.test.ts, extensions/pi-claude-marketplace/transaction/with-state-guard.ts, extensions/pi-claude-marketplace/persistence/state-io.ts, .planning/phases/05-injection-and-ownership-design/05-CONTEXT.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "The required readonly reader exposes only the state load used by the reconcile read pass."
    - "The public apply export binds that reader to the real state loader and follows the same internal apply factory as the race case."
    - "A case-local reader returns the selected real snapshot, then changes backing state before the unchanged apply flow continues."
    - "The raced scope converges from the selected snapshot, the sibling scope still completes, and final rows remain canonically ordered."
  </behavior>
  <action>Per D-05-01, define a readonly reconcile state-reader contract with one operation matching the existing selected state load. Require it in an internal production-used apply factory, thread it only into the read pass and locked transaction load slot, and compose the public `applyReconcile` export from an explicit real reader. Migrate only the ORA-F15 race case to the factory. In that case, read real case-owned state, return the selected snapshot, perform the competing write, then allow the unchanged real apply flow to continue. Keep planning, locks, save, routing, child orchestration, projection, and notification production code intact. Assert final public rows, both scopes' exact state and trees, sibling progress, error identity/redaction, silence, unrelated bytes, and exact notification order before reader interactions.</action>
  <verify><automated>node --test tests/orchestrators/reconcile/apply.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts tests/orchestrators/reconcile/apply.test.ts --max-warnings=0</automated><fails_when>The owner discovers zero tests or fails; the reader controls more than the selected load, is optional, dead, or bypassed by production; lock/save behavior changes; the race becomes interaction-only; sibling progress, rows, state/tree bytes, redaction, silence, or notifications weaken; or a broad/test-only/default seam appears.</fails_when></verify>
  <acceptance_criteria>ORA-F15 has one required state-read classification, and the same real apply composition produces the controlled race plus complete public state and message evidence (D-05-01).</acceptance_criteria>
  <reversibility rating="costly">D-05-03 makes this reader the authorized replacement seam before Phase 6 patch removal, so widening it requires coordinated apply and owner migration.</reversibility>
  <done>The selected-read race is reproducible through one required production contract without replacing any reconcile child.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the complete applyReconcile composition contract</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/apply.test.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts, extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts, tests/orchestrators/plugin/bootstrap.test.ts, .planning/phases/05-injection-and-ownership-design/05-RESEARCH.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "Every non-race apply case uses the public production export and real temporary state/configuration/scope trees."
    - "Real install, uninstall, enable, disable, marketplace, and routing children remain composed inside apply with current ordering and failure isolation."
    - "The suite asserts whole reconcile outcomes, exact persisted bytes and scope trees, silence or exact notifications, and unrelated-file preservation."
    - "Reader forwarding evidence supplements these assertions and never replaces them."
  </behavior>
  <action>Audit the apply owner after the reader migration and preserve every existing public result, state, tree, error, ordering, silence, and exact-notification assertion. Strengthen only cases where the new factory path otherwise leaves production binding unproven. Keep ordinary cases on the public export and real case-owned filesystem. Per D-05-02, reject any child-orchestrator bundle, broad dependencies object, replaced child, or interaction-only expectation. Run the unchanged bootstrap owner as the second composition-preservation control, but do not modify it. Confirm the two named flows remain the only composition exceptions. Per D-05-03, retain the apply suite's current builtin synchronization code for Phase 6 and add no suppression.</action>
  <verify><automated>node --test tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts tests/orchestrators/reconcile/apply.test.ts --max-warnings=0</automated><fails_when>Either composition suite discovers zero tests or fails; apply substitutes a real child or gains a dependency bundle; a complete result, state/configuration/tree, error, silence, notification, order, or failure-isolation assertion is removed; bootstrap changes; or a third composition exception or deferred cleanup appears.</fails_when></verify>
  <acceptance_criteria>applyReconcile remains the first of exactly two behavioral-composition exceptions with real children and complete public observations; bootstrap remains the unchanged second exception (D-05-02, D-05-03).</acceptance_criteria>
  <reversibility rating="costly">This preserves a deliberate composition-testing boundary used by later runtime/cache wiring, so reversing it would retouch multiple owner suites and real child contracts.</reversibility>
  <done>The apply pair proves both the narrow race seam and the unchanged full behavioral composition.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                 |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| Persisted state to selected reconcile snapshot | State can change after selection, but locking, scope isolation, and final persistence must remain truthful. |
| Apply composition to real mutation children    | A broad substitute can hide ordering, rollback, or state/tree drift.                                        |
| Reconcile outcome to user notification         | Errors, partial rows, silence, and notifications must retain exact attribution and redaction.               |

## STRIDE Threat Register

| Threat ID | Category                | Component                 | Severity | Disposition | Mitigation Plan                                                                                                                                 |
| --------- | ----------------------- | ------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-14   | Tampering / Repudiation | selected state read       | high     | mitigate    | Restrict the reader to one load, preserve locking, and assert selected snapshot, competing write, final state/tree, and exact cascade together. |
| T-05-15   | Tampering               | composed child operations | high     | mitigate    | Keep real children and complete public-effect assertions; prohibit child bundles and interaction-only replacement.                              |
| T-05-16   | Information disclosure  | error projection          | medium   | mitigate    | Preserve exact redaction, failure isolation, public rows, and notifications across both scopes.                                                 |
| T-05-17   | Tampering               | production adapter        | medium   | mitigate    | Require the reader and bind the public export to the real loader with direct-pair and type checks.                                              |

ASVS Level 1: both high-severity state-integrity threats block completion. This plan adds no package, schema, authentication, or external-service change.
</threat_model>

## Source coverage audit

| Source   | ID                    | Feature or constraint                                         | Plan/task         | Status  | Notes                                                            |
| -------- | --------------------- | ------------------------------------------------------------- | ----------------- | ------- | ---------------------------------------------------------------- |
| GOAL     | -                     | Legitimate seam with public composition evidence              | 05-04 / Tasks 1-2 | COVERED | The race is narrow and complete apply effects remain primary.    |
| REQ      | TREF-04               | Classify hidden dependency and preserve selected compositions | 05-04 / Tasks 1-2 | COVERED | ORA-F15 is classified once; apply remains a real composition.    |
| RESEARCH | eleven-root table     | Reconcile state changes after a selected read                 | 05-04 / Task 1    | COVERED | Only the state-load boundary is controlled.                      |
| RESEARCH | composition exception | Keep real apply children and complete observations            | 05-04 / Task 2    | COVERED | Public outcomes, both scope trees, and exact messages remain.    |
| CONTEXT  | D-05-01               | Narrow required production port                               | 05-04 / Task 1    | COVERED | One load operation has explicit real composition.                |
| CONTEXT  | D-05-02               | Preserve exactly applyReconcile and bootstrapClaudePlugin     | 05-04 / Task 2    | COVERED | Apply remains real; bootstrap is unchanged and run as a control. |
| CONTEXT  | D-05-03               | Phase 6 owns builtin patch removal                            | 05-04 / Tasks 1-2 | COVERED | Current synchronization machinery remains.                       |

The other ten roots retain their unique assignments in Plans 05-01 through 05-03 and 05-05. Runtime propagation and the final joined proof remain in Plans 05-13 and 05-27. Phase 6 splits and patch removal are exclusions.

<verification>
Run both task commands and `npm run check`. Confirm the two frontmatter files are the only modified files, ORA-F15 has one reader, apply retains real children and complete public evidence, bootstrap is unchanged, no third composition exception appears, and `scripts/revalidation.mjs` keeps its exact Fallow line.
</verification>

<success_criteria>

- ORA-F15 is classified through one required, production-bound state reader.
- The controlled race preserves locking, scope isolation, final state/tree bytes, results, errors, silence, and notifications.
- applyReconcile and bootstrapClaudePlugin remain exactly the two composition exceptions.
- No child bundle, optional/default/test-only/dead seam, new suppression, split, or Phase 6 cleanup appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-04-SUMMARY.md` when done.
</output>
