---
phase: 05-injection-and-ownership-design
plan: 31
type: execute
wave: 23
depends_on: [05-28, 05-30]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
  - tests/bridges/hooks/routing-state.test.ts
  - extensions/pi-claude-marketplace/shared/completion-cache.ts
  - tests/shared/completion-cache.test.ts
autonomous: true
requirements: [TREF-05, TREF-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Fresh production and test censuses prove zero callers before `resetEpoch`, `resetRoutingState`, and `resetCompletionCache` are deleted (D-05-07)."
    - "Hooks routing tests use explicit fresh runtime/state owners while preserving exact epoch, pending-settle, route, result, and cleanup behavior."
    - "CompletionCache tests use fresh instances while preserving exact memory/file bytes, TTL, poison/recovery, invalidation, and concurrency behavior (D-05-05)."
    - "No renamed whole-state cleanup, hidden singleton, raw-state export, or bounded transition binding remains after deletion (D-05-06, D-05-07)."
    - "Phase 6 global-patch and file-split inventory remains unchanged (D-05-03, D-05-09)."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - tests/bridges/hooks/routing-state.test.ts
    - extensions/pi-claude-marketplace/shared/completion-cache.ts
    - tests/shared/completion-cache.test.ts
  key_links:
    - "zero-caller census -> reset export deletion -> static absence gate"
    - "fresh HooksRuntime/routing owner -> exact routing-state behaviors"
    - "fresh CompletionCache -> exact cache bytes/TTL/poison/invalidation behaviors"
  prohibitions:
    - "No deletion before a fresh zero-caller census, replacement cleanup alias, hidden singleton, raw state, __deps, ForTest, or optional/default/dead port."
    - "No behavior assertion removal, broad production refactor, Phase 6 patch removal/split, excluded-file edit, or new Fallow suppression."
---

<objective>
Run fresh production/test censuses, delete `resetEpoch`, `resetRoutingState`, and `resetCompletionCache` only after all genuine callers have migrated, and prove no renamed whole-state cleanup, hidden singleton, raw state, or behavior loss remains (D-05-06, D-05-07).

Purpose: Complete trace-preserving global-reset removal only after caller migrations have made deletion safe.
Output: Reset-free routing-state and completion-cache modules with isolated owner tests and static absence evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-28-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-30-SUMMARY.md
</context>

## Edge assumptions

- Start each task with CodeGraph callers plus an `rg` production/test census. A non-definition caller is a hard stop, not permission to expand scope.
- Plans 05-28 and 05-30 migrate every known caller; this plan owns only the two production definitions and their paired owner tests.
- Delete comments, exports, delegates, and bounded transition bindings that exist only for whole-state cleanup; do not delete genuine lifecycle operations.
- Preserve every behavior assertion by reconstructing state through a fresh public owner and real operations.
- Keep `createRequire`/`syncBuiltinESMExports` patching and Phase 6 split candidates unchanged.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Delete hooks reset surfaces after a zero-caller census</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-28-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-30-SUMMARY.md, extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, tests/bridges/hooks/routing-state.test.ts, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, tests/bridges/hooks/runtime.test.ts, tests/bridges/hooks/event-router.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, tests/bridges/hooks/routing-state.test.ts</files>
  <behavior>
    - "A fresh CodeGraph/text census finds no `resetEpoch` or `resetRoutingState` caller outside their definitions and paired owner tests before deletion."
    - "The two reset exports, reset-only comments/delegates, and any bounded transition binding are absent after deletion."
    - "Routing-state cases construct fresh explicit owners and preserve exact epoch advancement, pending/settle, route, result, error, and real cleanup behavior."
    - "No renamed whole-state cleanup, hidden singleton, raw routing-state export, optional/default/dead port, or new test-only surface appears."
  </behavior>
  <action>Use CodeGraph followed by repository-wide production/test text search to prove the two hooks reset symbols have no caller beyond their definitions and this paired owner suite. If any genuine caller remains, stop and report it without editing. Otherwise remove the two exports, reset-only comments and delegates, plus every transition binding retained solely to bridge the migration. Rewrite owner tests to construct a fresh explicit routing/runtime owner inside each case and drive epoch, pending, settle, routing, and cleanup through real operations. Preserve exact values, ordering, errors, and isolation assertions. Inspect the resulting public/private surface for semantically equivalent whole-state cleanup, hidden module ownership, raw state, or unused ports and remove only migration residue within these two files. Do not change runtime/event-router/integration files or Phase 6 patching.</action>
  <verify><automated>! rg -n 'resetEpoch|resetRoutingState' extensions/pi-claude-marketplace tests &amp;&amp; node --test tests/bridges/hooks/routing-state.test.ts tests/bridges/hooks/runtime.test.ts tests/bridges/hooks/event-router.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts tests/bridges/hooks/routing-state.test.ts --max-warnings=0</automated><fails_when>The pre-edit census has a genuine caller; the absence gate finds either deleted symbol; a test/coverage/type/lint command fails or discovers zero tests; a bounded transition binding, renamed cleanup, hidden singleton, raw state, or dead/default port remains; epoch/pending/settle/route/result/error/cleanup assertions weaken; an out-of-scope file changes; or Phase 6/suppression work appears.</fails_when></verify>
  <acceptance_criteria>The hooks reset surfaces and bounded migration bindings are absent after a proven zero-caller census, while exact owner behavior remains green (D-05-04, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">The removed compatibility surface is recoverable from history; the census and retained public behaviors make deletion safe.</reversibility>
  <done>Hooks routing ownership has no whole-state reset or bounded transition residue and retains exact lifecycle behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete completion reset surfaces after a zero-caller census</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-30-SUMMARY.md, extensions/pi-claude-marketplace/shared/completion-cache.ts, tests/shared/completion-cache.test.ts, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/completions/provider.ts, tests/edge/completions/data.test.ts, tests/edge/completions/provider.test.ts, tests/orchestrators/plugin/install.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/shared/completion-cache.ts, tests/shared/completion-cache.test.ts</files>
  <behavior>
    - "A fresh CodeGraph/text census finds no `resetCompletionCache` caller outside its definition and paired owner test before deletion."
    - "The reset export, reset-only comments/delegates, process-global maps, and bounded transition binding are absent after deletion."
    - "CompletionCache cases use fresh instances and retain exact memory/file serialization, clock/TTL, poison/recovery, invalidation, locking, and concurrency behavior."
    - "No renamed whole-cache cleanup, hidden singleton, raw cache export, optional/default/dead port, or new test-only surface appears."
  </behavior>
  <action>Use CodeGraph followed by repository-wide production/test text search to prove the completion reset symbol has no caller beyond its definition and paired owner suite. If any genuine caller remains, stop without editing. Otherwise remove its export, reset-only documentation/delegates, process-global maps, and every transition binding retained solely for migration. Rewrite the paired tests around fresh CompletionCache instances and explicit temp-tree ownership per case. Preserve exact in-memory/file bytes, keying, clock and TTL boundaries, poison/recovery, invalidation, locking, concurrency, cleanup, and error assertions. Inspect for any semantic replacement cleanup, hidden module-owned cache, raw cache exposure, or unused port and remove only migration residue within these two files. Keep completion provider/registration and mutation suites read/verify-only.</action>
  <verify><automated>! rg -n 'resetCompletionCache' extensions/pi-claude-marketplace tests &amp;&amp; node --test tests/shared/completion-cache.test.ts tests/edge/completions/data.test.ts tests/edge/completions/provider.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/completion-cache.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/shared/completion-cache.ts tests/shared/completion-cache.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>The pre-edit census has a genuine caller; the absence gate finds the deleted symbol; a test/coverage/type/lint/Fallow command fails or discovers zero tests; global cache state, bounded transition residue, renamed cleanup, raw state, or a dead/default port remains; bytes/TTL/poison/invalidation/concurrency behavior weakens; an out-of-scope file changes; or suppression/Phase 6 work appears.</fails_when></verify>
  <acceptance_criteria>The completion reset and all bounded/global migration residue are absent after a proven zero-caller census, while exact cache behavior remains green (D-05-05, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">The obsolete compatibility export is recoverable from history and all surviving behavior is covered by fresh-instance tests.</reversibility>
  <done>Completion ownership has no global reset, singleton map, or bounded transition residue and retains exact cache behavior.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                              | Description                                                                       |
| ------------------------------------- | --------------------------------------------------------------------------------- |
| Caller census -> deletion             | An incomplete graph/text inventory can remove a still-used compatibility surface. |
| Fresh owner -> persisted state        | Fixture migration must not hide real route/cache lifecycle behavior.              |
| Cleanup removal -> public/private API | A renamed reset or hidden singleton would preserve the original vulnerability.    |

## STRIDE threat register

| Threat ID | Category                           | Component               | Severity | Disposition | Mitigation plan                                                                               |
| --------- | ---------------------------------- | ----------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------- |
| T-05-134  | Denial of service                  | reset deletion ordering | high     | mitigate    | Require CodeGraph plus text zero-caller evidence before either deletion.                      |
| T-05-135  | Tampering / Information disclosure | owner isolation         | high     | mitigate    | Rebuild every case with fresh explicit owners and retain exact public behavior assertions.    |
| T-05-136  | Elevation of privilege             | replacement test seam   | high     | mitigate    | Audit exports, module state, delegates, and ports for semantic replacements and dead residue. |
| T-05-137  | Tampering                          | Phase 6 inventory       | medium   | mitigate    | Keep global patch and split-candidate files read-only.                                        |

ASVS Level 1: safe deletion, lifecycle/cache isolation, seam non-reintroduction, and public behavior preservation are blocking controls.
</threat_model>

## Source coverage audit

| Source   | ID                                 | Feature or constraint                                           | Plan/task         | Status  | Notes                                                  |
| -------- | ---------------------------------- | --------------------------------------------------------------- | ----------------- | ------- | ------------------------------------------------------ |
| GOAL     | -                                  | Correctly owned runtime state and public contracts              | 05-31 / Tasks 1-2 | COVERED | Terminal reset/global-state removal is evidence-gated. |
| REQ      | TREF-05                            | No reset exports or process-global lifecycle/cache state        | 05-31 / Tasks 1-2 | COVERED | Both owner modules and paired tests are in scope.      |
| REQ      | TREF-06                            | Trace-preserving cleanup with exact public behavior             | 05-31 / Tasks 1-2 | COVERED | Static absence plus focused/direct verification.       |
| RESEARCH | caller/dead-code census            | Delete only after all callers migrate                           | 05-31 / Tasks 1-2 | COVERED | Fresh graph and text censuses precede each edit.       |
| CONTEXT  | D-05-04, D-05-05, D-05-06, D-05-07 | Owned lifecycles/caches, genuine transitions, terminal deletion | 05-31 / Tasks 1-2 | COVERED | No bounded transition binding survives.                |
| CONTEXT  | D-05-03, D-05-09                   | Preserve Phase 6 patch/split inventory                          | 05-31 / Tasks 1-2 | COVERED | Those files remain read-only.                          |

BOOLEAN_FLAGS cleanup remains Plan 05-32 and exact closure audits remain Plan 05-33. Phase 6 and exclusions are not gaps.

<verification>
Run both task commands. Confirm only the four listed files change, task scopes are two files each, the three reset names are absent repository-wide, and no renamed whole-state cleanup, singleton/global map, raw state, optional/default/dead port, or bounded transition binding remains. Confirm exact routing/cache behavior, the Phase 6 patch/split inventory, and the Fallow comment `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` remain unchanged with no new suppression.
</verification>

<success_criteria>

- Fresh graph/text censuses prove zero callers before deletion.
- All three reset exports and every reset-only/bounded/global migration binding are absent.
- Routing and completion owner tests isolate by construction and retain exact behavior.
- No semantic replacement reset, hidden singleton, raw state, dead/default port, or weakened assertion appears.
- Phase 6 inventory, exclusions, and the exact Fallow suppression line remain untouched.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-31-SUMMARY.md` when done.
</output>
