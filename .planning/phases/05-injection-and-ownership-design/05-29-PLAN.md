---
phase: 05-injection-and-ownership-design
plan: 29
type: execute
wave: 21
depends_on: [05-25, 05-26]
files_modified:
  - tests/integration/hooks-additionalcontext-end-to-end.test.ts
  - tests/integration/hooks-cross-scope-reconcile.test.ts
  - tests/integration/hooks-dispatch-end-to-end.test.ts
  - tests/integration/hooks-spawn-end-to-end.test.ts
  - tests/integration/transaction-lifecycle-cascade.test.ts
autonomous: true
requirements: [TREF-05, TREF-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every hooks integration reset caller uses a fresh extension/runtime fixture and no process-global cleanup surface (D-05-04, D-05-06)."
    - "Additional context, dispatch, cross-scope reconcile, spawn, and transaction-cascade tests retain exact public results, state/tree, route, process, cleanup, and notification assertions."
    - "Plugin-orchestrator behavior in transaction integration uses the runtime/cache graph wired by operation-specific plans; those owner suites remain read/verify-only census gates."
    - "Production reset definitions remain until completion callers migrate in Plan 05-30 and Plan 05-31 proves the final zero-caller census (D-05-07)."
  artifacts:
    - tests/integration/hooks-additionalcontext-end-to-end.test.ts
    - tests/integration/hooks-cross-scope-reconcile.test.ts
    - tests/integration/hooks-dispatch-end-to-end.test.ts
    - tests/integration/hooks-spawn-end-to-end.test.ts
    - tests/integration/transaction-lifecycle-cascade.test.ts
  key_links:
    - "fresh extension/runtime fixture -> registered callback -> exact end-to-end result"
    - "one lifecycle graph -> real plugin mutation -> routes/cache/state/tree agreement"
    - "post-task reset census -> only Plan 05-30 callers remain before deletion"
  prohibitions:
    - "No production edit, early reset deletion, replacement reset alias, __deps, ForTest, raw state access, optional/default/dead port, or shared mutable fixture."
    - "No owner-suite edit outside the five integration files and no weakening of the two composition exceptions."
    - "No Phase 6 work, excluded-file edit, or new Fallow suppression."
---

<objective>
Migrate hooks integration and plugin-orchestrator reset callers to fresh extension/runtime fixtures, partitioning integration and orchestrator owners so each task stays bounded while exact transaction, route, state/tree, error, cleanup, and notification assertions remain (D-05-04, D-05-06, D-05-07).

Purpose: Remove integration dependence on global hooks cleanup after all operation-specific runtime/cache paths are wired.
Output: Five reset-free integration suites using fresh lifecycle fixtures and exact end-to-end public evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-25-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md
</context>

## Edge assumptions

- Repeat the hooks reset caller census across all tests before editing and record the five in-scope integration callers plus any remaining Plan 05-30 callers.
- Construct a fresh extension or HooksRuntime per case according to the public layer under test. Do not share runtime/cache owners in module or suite setup.
- Operation-specific plugin owners were migrated by Plans 05-13 through 05-25 and remain read/verify-only here.
- Preserve every complete public assertion. Remove only reset setup and direct internal-state coupling.
- Do not delete production reset/transition surfaces; Plan 05-31 owns deletion after Plan 05-30.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Migrate context, cross-scope, and dispatch integrations</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, tests/integration/hooks-additionalcontext-end-to-end.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts, tests/integration/hooks-dispatch-end-to-end.test.ts, extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, tests/orchestrators/reconcile/apply.test.ts, tests/bridges/hooks/event-router.test.ts</read_first>
  <files>tests/integration/hooks-additionalcontext-end-to-end.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts, tests/integration/hooks-dispatch-end-to-end.test.ts</files>
  <behavior>
    - "Each integration case creates a fresh extension/runtime graph and registers callbacks through production composition."
    - "No case imports/calls hooks reset functions or reaches raw routing, pending, or settle state."
    - "One-shot additional context, reducer order, attribution, exact Pi returns, project-before-user reconcile, routes, state/trees, errors, silence, and notifications remain exact."
    - "Separate lifecycle fixtures remain isolated without cleanup calls."
  </behavior>
  <action>After recording the caller census, replace reset-based setup in these three integration suites with a fresh extension or explicit HooksRuntime created inside each case. Drive registration, resources-discover, SessionStart, dispatch, and reconcile through production entrypoints. Preserve exact candidate routes, one-shot additional context, reducer precedence, attribution, state/configuration, both scope trees, result ordering, errors/redaction, silence, and notifications. Assert public effects before any call-order evidence. Remove only reset imports/calls and direct internal-state setup. Per D-05-07, add no production accessor or renamed cleanup. Do not edit apply/event-router owners or production files.</action>
  <verify><automated>node --test tests/integration/hooks-additionalcontext-end-to-end.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/integration/hooks-dispatch-end-to-end.test.ts tests/orchestrators/reconcile/apply.test.ts tests/bridges/hooks/event-router.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/integration/hooks-additionalcontext-end-to-end.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/integration/hooks-dispatch-end-to-end.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; a reset import/call remains in the three files; cases share lifecycle state or reach raw internals; context/dispatch/reconcile/state/tree/result/message assertions weaken; production or owner suites change; resets are deleted early; or a seam/Phase 6/excluded change appears.</fails_when></verify>
  <acceptance_criteria>The three callback/reconcile integration suites isolate by construction and retain exact end-to-end public behavior without hooks reset access (D-05-04, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">The change migrates integration fixtures behind stable production entrypoints without altering contracts.</reversibility>
  <done>Context, cross-scope reconcile, and dispatch integrations are reset-free and behaviorally exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate spawn and transaction-cascade integrations</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-25-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, tests/integration/hooks-spawn-end-to-end.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts</read_first>
  <files>tests/integration/hooks-spawn-end-to-end.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts</files>
  <behavior>
    - "Each case owns a fresh runtime/cache graph across spawn or real plugin transaction cascades."
    - "No reset call/import remains; real reload, terminal, commit, rollback, and cleanup behavior supplies state transitions."
    - "Spawn timers/buffers/messages/PID behavior and install/reinstall/uninstall state/tree/routes/cache/results/notifications remain exact."
    - "The post-task census records only completion-reset callers assigned to Plan 05-30 before terminal deletion."
  </behavior>
  <action>Replace hooks reset setup in the spawn and transaction-cascade suites with fresh per-case lifecycle owners passed through production composition. Drive child termination and re-registration with real lifecycle operations. Drive install, reinstall, and uninstall through the operation-specific runtime/cache paths from Plans 05-22 through 05-25. Preserve exact timers, buffers, signals, PID bytes and marker checks, transaction order, compensation/rollback, state/configuration/scope trees, routes, completion rows, cleanup, errors, results, warnings, and notifications. Run plugin owner suites read/verify-only. Record the post-task hooks-reset census and fail if an unassigned caller remains. Do not delete production surfaces before Plan 05-31.</action>
  <verify><automated>node --test tests/integration/hooks-spawn-end-to-end.test.ts tests/integration/transaction-lifecycle-cascade.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/integration/hooks-spawn-end-to-end.test.ts tests/integration/transaction-lifecycle-cascade.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; a reset call/import remains in the two files; lifecycle state is shared; spawn/PID/transaction/state/tree/routes/cache/results/cleanup/messages weaken; an unassigned hooks-reset caller remains; production/owner files change; resets are deleted early; or a seam/suppression/Phase 6 change appears.</fails_when></verify>
  <acceptance_criteria>Spawn and transaction-cascade integrations use fresh lifecycle owners, retain exact public behavior, and leave no unassigned hooks-reset caller (D-05-04, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">The fixture migration is local to two integration suites and leaves production contracts unchanged.</reversibility>
  <done>All five in-scope integration suites are reset-free, and deletion remains safely ordered after Plan 05-30.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                  | Description                                                                         |
| ----------------------------------------- | ----------------------------------------------------------------------------------- |
| Integration case -> lifecycle graph       | Shared owners can leak routes, children, context, or completion state across cases. |
| Real transaction -> runtime/cache effects | Fixture changes must preserve commit, rollback, cleanup, and exact public outcomes. |
| Test migration -> production surface      | A convenience accessor can recreate the test-only seam.                             |

## STRIDE threat register

| Threat ID | Category                                   | Component                  | Severity | Disposition | Mitigation plan                                                                           |
| --------- | ------------------------------------------ | -------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-05-126  | Tampering / Information disclosure         | integration isolation      | high     | mitigate    | Construct fresh owners per case and prove separate fixtures cannot observe each other.    |
| T-05-127  | Tampering / Repudiation                    | public end-to-end behavior | high     | mitigate    | Preserve complete state/tree/route/cache/result/error/message assertions.                 |
| T-05-128  | Tampering                                  | test-only access           | high     | mitigate    | Prohibit production edits and use only established public composition contracts.          |
| T-05-129  | Denial of service / Elevation of privilege | spawn/PID behavior         | high     | mitigate    | Preserve marker checks, signals, timers, buffers, PID bytes, and exact terminal behavior. |

ASVS Level 1: lifecycle isolation, public transaction integrity, test-surface integrity, and process safety are high-severity blocking threats.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                 | Plan/task         | Status  | Notes                                      |
| -------- | ------------------------- | ----------------------------------------------------- | ----------------- | ------- | ------------------------------------------ |
| GOAL     | -                         | Correctly owned runtime state and public contracts    | 05-29 / Tasks 1-2 | COVERED | Five integrations isolate by construction. |
| REQ      | TREF-05                   | Runtime/cache state belongs to extension lifecycles   | 05-29 / Tasks 1-2 | COVERED | Fresh owners replace reset setup.          |
| REQ      | TREF-06                   | Tests use public contracts without reset hooks        | 05-29 / Tasks 1-2 | COVERED | Exact end-to-end effects remain primary.   |
| RESEARCH | reset migration           | Integration and orchestrator paths use fresh fixtures | 05-29 / Tasks 1-2 | COVERED | Exact five-file partition is 3+2.          |
| CONTEXT  | D-05-04, D-05-06, D-05-07 | One lifecycle, genuine transitions, ordered deletion  | 05-29 / Tasks 1-2 | COVERED | Production deletion waits for Plan 05-30.  |

Completion reset callers remain Plan 05-30. Terminal deletion, BOOLEAN_FLAGS cleanup, and final audits remain Plans 05-31 through 05-33. Phase 6 and exclusions are not gaps.

<verification>
Run both task commands. Confirm only the five integration files change, task scopes are three and two files, no hooks reset import/call remains there, exact public assertions remain, and production/operation-owner files stay unchanged. Confirm reset deletion waits for Plans 05-30/05-31, the Fallow comment is byte-exact as `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor`, and no suppression or Phase 6/excluded work appears.
</verification>

<success_criteria>

- All five integration suites use fresh extension/runtime/cache fixtures.
- No suite calls hooks resets or reads raw lifecycle state.
- Exact context, dispatch, reconcile, spawn, PID, transaction, state/tree, route/cache, cleanup, result, and notification behavior remains green.
- Operation-specific owner suites stay read/verify-only and no unassigned reset caller remains.
- No early deletion, new test seam, shared fixture, suppression, Phase 6 work, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-29-SUMMARY.md` when done.
</output>
