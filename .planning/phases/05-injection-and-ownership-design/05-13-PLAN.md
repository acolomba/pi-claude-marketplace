---
phase: 05-injection-and-ownership-design
plan: 13
type: execute
wave: 7
depends_on: [05-04, 05-05, 05-12]
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/edge/handlers/plugin/install.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct plugin install receives the lifecycle HooksRuntime through required edge plumbing and the production-used install transaction contract from Plan 05-05 (D-05-01, D-05-04)."
    - "Runtime route mutation occurs only at the existing successful commit boundary; every pre-commit failure and rollback leaves the prior routes exact (D-05-06)."
    - "resources-discover hydrates the project scope before applyReconcile, and real reconcile installation receives that same runtime through required apply options."
    - "applyReconcile keeps real install and sibling children, complete public outcomes, both scope trees, state, failure isolation, silence, and exact notifications (D-05-02)."
    - "Install preserves prepare, replace, failing-step compensation, reverse rollback, abort, save, finalization, cleanup, results, warnings, and exact error identity."
    - "No completion-cache invalidation is added here; Plan 05-22 owns that distinct post-ledger-success path."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - tests/edge/handlers/plugin/install.test.ts
    - tests/orchestrators/plugin/install.test.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
    - tests/orchestrators/reconcile/apply.test.ts
  key_links:
    - "root-owned HooksRuntime -> edge install handler -> required install transaction -> post-commit route visibility"
    - "resources-discover hydration -> applyReconcile options -> real install child -> same runtime"
    - "apply owner -> real children -> complete outcome/state/tree/notification assertions"
  prohibitions:
    - "No optional runtime, __deps, ForTest, test-only reset/export, child-operation bundle, dead port, or second runtime."
    - "No weakening or broadening of the two composition exceptions and no completion-cache work assigned to Plan 05-22."
    - "No Phase 6 patch removal/split, stage/PID/info/uninstall decomposition, or new Fallow suppression."
---

<objective>
Trace runtime ownership through plugin install and real reconcile installation: required edge plumbing, install transaction port, reconcile reader/options, hydrate-before-reconcile order, route visibility, exact rollback/state/tree/results, and complete apply composition (D-05-01, D-05-02, D-05-04, D-05-06).

Purpose: Make newly installed hook routes visible through the lifecycle runtime without weakening rollback or full apply composition.
Output: Required install/apply runtime plumbing and complete direct plus reconciled installation evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-04-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-05-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md
</context>

## Edge assumptions

- Read dependency summaries before editing and use their executed HooksRuntime, install transaction, reconcile reader, and edge dependency identifiers.
- The install transaction port controls only the classified schedule from Plan 05-05. Ordinary files, containment, validation, and public results stay real.
- Hook route updates use the lifecycle runtime only at the current durable boundary. They do not define a generic rollback/reset operation.
- applyReconcile remains a full-observation composition exception with real children. The authorized state reader remains narrow and separate.
- Completion invalidation is a later, separately ordered effect. Do not preempt Plan 05-22.
- Preserve Phase 6 machinery, exact existing Fallow comment, and the excluded stage/PID/info/uninstall boundaries.

## Artifacts this plan produces

| File family                     | Contract or proof                                                                             |
| ------------------------------- | --------------------------------------------------------------------------------------------- |
| Edge/install path and owners    | Required lifecycle runtime reaches the real install transaction and committed route update.   |
| Reconcile apply/types and owner | Same runtime reaches real install children while apply retains complete composition evidence. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace lifecycle runtime through direct plugin install</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-05-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/edge/handlers/plugin/install.test.ts, tests/orchestrators/plugin/install.test.ts, tests/edge/register.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/edge/handlers/plugin/install.test.ts, tests/orchestrators/plugin/install.test.ts</files>
  <behavior>
    - "The registered install handler forwards the root runtime as a required value into the real install transaction factory."
    - "A successful durable install hydrates/adds its hook configuration and rebuilds only the supplied runtime before returning the exact public result."
    - "Prepare, replace, save, compensation, abort, rollback, or cleanup failure leaves runtime routes consistent with the final durable state."
    - "State/configuration bytes, scope tree, result rows, warnings, errors, cleanup eligibility, and notifications remain exact."
  </behavior>
  <action>Use the executed Plan 05-05 transaction contract and Plan 05-12 lifecycle graph. Make the registered handler require and forward the supplied HooksRuntime into the existing install factory and semantic transaction port. Route the current hooks cache/hydration finalization through that runtime at the same durable-success point. Keep validation, containment, materialization, ledger phases, config/state writes, result projection, warnings, notification timing, and public signatures exact. In rollback cases, prove routes match the final persisted state rather than clearing all runtime state. Add complete direct-install owner assertions and route visibility after success. Do not add cache invalidation, widen the transaction port, or create an optional/runtime default.</action>
  <verify><automated>node --test tests/edge/handlers/plugin/install.test.ts tests/orchestrators/plugin/install.test.ts tests/edge/register.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts extensions/pi-claude-marketplace/orchestrators/plugin/install.ts tests/edge/handlers/plugin/install.test.ts tests/orchestrators/plugin/install.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; runtime or transaction port is optional, defaulted, broad, dead, or bypassed; route mutation crosses the durable boundary; rollback routes diverge from persisted state; operation/compensation/cleanup order, errors, trees, results, warnings, or notifications change; or completion work appears.</fails_when></verify>
  <acceptance_criteria>Direct install uses the root runtime through the required production transaction path and exposes routes only when durable state permits them (D-05-01, D-05-04, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">The runtime now crosses the install transaction boundary that Phase 6 later moves, so changes require coordinated handler, orchestrator, and owner migration.</reversibility>
  <done>Direct install preserves exact transaction behavior and makes committed hooks visible through the owning runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry the same runtime through real reconcile installation</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-04-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts, tests/orchestrators/reconcile/apply.test.ts, extensions/pi-claude-marketplace/index.ts, tests/integration/hooks-cross-scope-reconcile.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "Apply options require the lifecycle runtime beside the narrow reconcile state reader; no default or child bundle is accepted."
    - "Project hydration completes before reconcile starts, and real install children receive the same runtime used by registered callbacks."
    - "Successful and failed installs produce exact routes consistent with final state while sibling scope work remains isolated."
    - "The apply suite retains complete outcomes, both scope trees, state/configuration, errors/redaction, silence, ordering, and notification bytes."
  </behavior>
  <action>Extend the executed reconcile option contract with the required HooksRuntime and thread it through only the real mutation children that need route effects. Preserve the separate Plan 05-04 state reader and do not add an orchestrator bundle. Ensure the root's resources-discover path still hydrates project scope before it calls apply, then apply passes that same runtime to real install behavior. Keep scope order, locks, read-pass isolation, planning, all other real children, backfill position, route rebuild boundaries, outcome projection, silence, and single cascade exact. Strengthen the apply owner with full state/tree/result/notification observations for successful, failed, rolled-back, and sibling-scope install paths. Run the cross-scope integration and bootstrap owner without modifying them.</action>
  <verify><automated>node --test tests/orchestrators/reconcile/apply.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/orchestrators/plugin/bootstrap.test.ts tests/orchestrators/plugin/install.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts tests/orchestrators/reconcile/apply.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; apply omits/defaults runtime, replaces a child, widens dependencies, or bypasses the authorized reader; hydrate-before-reconcile order changes; a failure leaves stale routes; complete results/state/trees/errors/silence/notifications weaken; bootstrap changes; or a third composition exception appears.</fails_when></verify>
  <acceptance_criteria>Real reconcile installation shares the registered runtime and preserves the complete applyReconcile composition proof (D-05-02, D-05-04, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">The runtime is now part of reconcile options and real child wiring, so changing it affects apply, install, root composition, and cross-scope evidence.</reversibility>
  <done>Direct and reconciled installs share one lifecycle runtime with exact rollback, route, state, tree, result, and notification behavior.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                | Description                                                                                 |
| --------------------------------------- | ------------------------------------------------------------------------------------------- |
| Edge install request -> transaction     | Untrusted names and sources cross validation, containment, and durable mutation boundaries. |
| Durable install state -> runtime routes | Routes must reflect committed state and must not survive a failed rollback.                 |
| Reconcile plan -> real install children | Replacement with test doubles can hide lock, rollback, state/tree, or notification defects. |

## STRIDE threat register

| Threat ID | Category                           | Component               | Severity | Disposition | Mitigation plan                                                                                               |
| --------- | ---------------------------------- | ----------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------- |
| T-05-53   | Tampering / Elevation of privilege | install transaction     | high     | mitigate    | Preserve validation, containment, required semantic operations, and exact ledger order with real-tree owners. |
| T-05-54   | Tampering                          | route commit boundary   | high     | mitigate    | Update only the supplied runtime at durable success and assert rollback routes against exact final state.     |
| T-05-55   | Tampering / Repudiation            | apply composition       | high     | mitigate    | Keep real children and complete outcome, state, tree, error, silence, and notification observations.          |
| T-05-56   | Information disclosure             | errors and diagnostics  | medium   | mitigate    | Preserve exact identity/redaction and scope attribution across direct and reconciled failure cases.           |
| T-05-57   | Denial of service                  | sibling-scope isolation | medium   | mitigate    | Preserve per-scope failure isolation and canonical result order.                                              |

ASVS Level 1: install integrity, route-state agreement, and full apply composition are high-severity blocking threats. No package or external service changes.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                                         | Plan/task         | Status  | Notes                                                           |
| -------- | ------------------------- | ----------------------------------------------------------------------------- | ----------------- | ------- | --------------------------------------------------------------- |
| GOAL     | -                         | Legitimate seams with lifecycle-owned state and public contracts              | 05-13 / Tasks 1-2 | COVERED | Runtime reaches direct and real reconciled install.             |
| REQ      | TREF-04                   | Required classified install/state-reader contracts                            | 05-13 / Tasks 1-2 | COVERED | Executed ports remain narrow and production-used.               |
| REQ      | TREF-05                   | One runtime owns route state                                                  | 05-13 / Tasks 1-2 | COVERED | Direct and reconcile paths use the root identity.               |
| REQ      | TREF-06                   | Public behavior replaces hidden state access                                  | 05-13 / Tasks 1-2 | COVERED | Exact routes, state, trees, results, and messages are asserted. |
| RESEARCH | install/apply routing     | Hydrate before reconcile and update routes at existing transaction boundaries | 05-13 / Tasks 1-2 | COVERED | Owner and integration suites prove both paths.                  |
| CONTEXT  | D-05-01, D-05-04, D-05-06 | Required ports and genuine runtime transitions                                | 05-13 / Tasks 1-2 | COVERED | No default, reset, or second runtime appears.                   |
| CONTEXT  | D-05-02, D-05-03, D-05-09 | Preserve apply composition and Phase 6 boundaries                             | 05-13 / Task 2    | COVERED | Real children stay composed and no split lands.                 |

Completion invalidation remains assigned to Plan 05-22. The final two-composition proof remains in Plan 05-27, reset removal in Plans 05-28 through 05-32, and Phase 6 work remains excluded.

<verification>
Run both task commands and `npm run check`. Confirm only the eight frontmatter files change, Task 1 changes five files and Task 2 changes three, runtime/ports are required and production-used, apply keeps real children and full observations, no completion wiring or forbidden seam appears, and the exact existing Fallow comment remains byte-for-byte unchanged.
</verification>

<success_criteria>

- Direct and reconciled install use the extension lifecycle runtime.
- Route visibility matches durable state across success, failure, compensation, rollback, abort, finalization, and cleanup.
- applyReconcile remains a full-observation composition exception with real children and exact public evidence.
- Required install and state-reader contracts stay narrow, separate, and production-used.
- No second lifetime, reset/test seam, cache work, split, suppression, or Phase 6 cleanup appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-13-SUMMARY.md` when done.
</output>
