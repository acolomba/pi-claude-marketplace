---
phase: 05-injection-and-ownership-design
plan: 32
type: execute
wave: 23
depends_on: [05-30]
files_modified:
  - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
  - tests/architecture/flag-catalog-drift.test.ts
  - tests/edge/handlers/plugin/list.test.ts
autonomous: true
requirements: [TREF-06]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`BOOLEAN_FLAGS` is no longer exported, imported, or compared through a derived self-equality assertion (D-05-08)."
    - 'Plugin list parsing retains a private `parseFlagNames("list")` result and exact public accepted/rejected flag behavior.'
    - 'The independent literal catalog pin remains exactly `list: ["--available", "--installed", "--partial", "--remote", "--unavailable"]` (D-05-08).'
    - "No unrelated command catalog, handler, Phase 6 split candidate, reset surface, or suppression changes."
  artifacts:
    - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
    - tests/architecture/flag-catalog-drift.test.ts
    - tests/edge/handlers/plugin/list.test.ts
  key_links:
    - 'private parseFlagNames("list") -> list handler parsing -> exact public rows/errors'
    - "independent literal catalog pin -> architecture drift detection"
    - "production-consumer census -> export/import removal -> static absence gate"
  prohibitions:
    - "No deletion or weakening of the independent literal flag pin, no change to public accepted/rejected flags, and no broad catalog refactor."
    - "No __deps, ForTest, raw state, optional/default/dead port, reset alias, Phase 6 split, excluded-file edit, or new Fallow suppression."
---

<objective>
Remove only the test-facing `BOOLEAN_FLAGS` export/import and derived equality case; retain the private catalog-derived set, independent literal catalog pin, and exact public list-handler accepted/rejected behavior (D-05-07, D-05-08).

Purpose: Remove a test-coupled export without deleting the independent public contract that detects catalog drift.
Output: A private list-handler flag set plus independent architecture and public handler contract tests.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-30-SUMMARY.md
</context>

## Edge assumptions

- Use CodeGraph and `rg` to classify every production/test consumer before changing export visibility. An unexpected production consumer is a hard stop.
- The handler still derives its private set from `parseFlagNames("list")`; do not duplicate the literal pin into production.
- The architecture test's literal list row is an independent public catalog contract and must remain byte-for-byte exact.
- Public handler tests must prove all five accepted flags and representative unknown, wrong-command, malformed, and conflicting inputs.
- Phase 6 owns the plugin-list file split. This plan changes only export visibility and contract evidence within the existing file.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Privatize list-handler flags after a consumer census</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-30-SUMMARY.md, extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts, tests/architecture/flag-catalog-drift.test.ts, tests/edge/handlers/plugin/list.test.ts, extensions/pi-claude-marketplace/shared/flag-catalog.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/catalog.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts, tests/architecture/flag-catalog-drift.test.ts, tests/edge/handlers/plugin/list.test.ts</files>
  <behavior>
    - "The pre-edit CodeGraph/text census finds no legitimate production consumer of the `BOOLEAN_FLAGS` export."
    - "The list handler keeps a private value derived from `parseFlagNames(\"list\")` and handles all five supported flags exactly."
    - "The architecture suite retains the literal list row and stops importing/comparing the handler-owned value to a value derived from the same catalog."
    - "Public list behavior retains exact rows, filtering, ordering, errors, silence, and notifications for accepted and rejected inputs."
  </behavior>
  <action>Use CodeGraph plus production/test text search to classify every consumer of the list handler's exported flag value. Stop without editing if any production consumer requires the export. Otherwise make the existing catalog-derived value private, remove its test import and only the self-equality assertion derived from the same source. Keep the independent literal list row unchanged in the architecture suite. Update the handler suite only as needed to exercise the private value through public command behavior, covering all five accepted switches and exact rejection paths. Preserve rows, filtering/order, unavailable/remote/installed/partial semantics, errors, silence, and notifications. Do not alter the shared flag catalog, plugin catalog, handler structure, or Phase 6 split boundary.</action>
  <verify><automated>! rg -n 'export.*BOOLEAN_FLAGS|import.*BOOLEAN_FLAGS' extensions/pi-claude-marketplace tests &amp;&amp; node --test tests/architecture/flag-catalog-drift.test.ts tests/edge/handlers/plugin/list.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts tests/architecture/flag-catalog-drift.test.ts tests/edge/handlers/plugin/list.test.ts --max-warnings=0</automated><fails_when>The consumer census finds a legitimate production use; the static gate finds an export/import; a test/coverage/type/lint command fails or discovers zero tests; the private catalog-derived value disappears; the literal pin changes; a supported/rejected public behavior weakens; an out-of-scope catalog/handler file changes; or a seam/suppression/Phase 6 change appears.</fails_when></verify>
  <acceptance_criteria>The test-coupled export/import/self-equality is gone, the handler keeps a private catalog-derived set, and the independent literal/public behavior contracts remain exact (D-05-08).</acceptance_criteria>
  <reversibility rating="reversible">The visibility and test-coupling cleanup is local and recoverable without changing the public command contract.</reversibility>
  <done>The list handler no longer exposes its internal flag set and all independent contracts remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove independent catalog drift and public rejection contracts</name>
  <read_first>tests/architecture/flag-catalog-drift.test.ts, tests/edge/handlers/plugin/list.test.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts, extensions/pi-claude-marketplace/shared/flag-catalog.ts, .planning/phases/05-injection-and-ownership-design/05-CONTEXT.md</read_first>
  <files>tests/architecture/flag-catalog-drift.test.ts, tests/edge/handlers/plugin/list.test.ts</files>
  <behavior>
    - "The literal command-catalog row remains independent of production derivation and detects added, removed, renamed, or reordered list flags."
    - "The public handler suite proves each supported flag plus unknown flag, wrong-command flag, malformed value, and conflicting selection behavior."
    - "Exact success rows/order and exact rejection error/silence/notification behavior remain asserted."
    - "No direct production-owned flag constant import, private-state access, or replacement test-only seam appears."
  </behavior>
  <action>Review the two tests after privatization and remove only residual assertions that reconstruct production from its own derived data. Keep the independent literal command row as a direct five-element equality contract with stable order. Ensure public list-handler cases exercise each supported switch and representative invalid or conflicting inputs through the registered/handler contract, asserting exact rows/order, errors, silence, and notifications. Prefer complete public results over spy-only call-order checks. Do not introduce an accessor, type escape, alternate export, duplicated production literal, or catalog-file edit.</action>
  <verify><automated>node --test tests/architecture/flag-catalog-drift.test.ts tests/edge/handlers/plugin/list.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/architecture/flag-catalog-drift.test.ts tests/edge/handlers/plugin/list.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; the literal row is absent, derived, reordered, or weakened; any supported/rejected public behavior lacks exact rows/error/silence/notification evidence; a production-owned constant is imported; an accessor/alternate export/test seam appears; an out-of-scope file changes; or a suppression/Phase 6/excluded change appears.</fails_when></verify>
  <acceptance_criteria>The architecture pin is genuinely independent and the handler suite proves exact accepted/rejected public behavior without private-state coupling (D-05-08).</acceptance_criteria>
  <reversibility rating="reversible">The test refinements are local and preserve stronger public contracts.</reversibility>
  <done>Catalog drift and list-handler behavior are independently and publicly verified after export removal.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                      | Description                                                          |
| --------------------------------------------- | -------------------------------------------------------------------- |
| Catalog data -> handler parser                | Drift can silently accept or reject the wrong public flags.          |
| Production-derived value -> architecture test | Comparing a value to itself cannot detect contract drift.            |
| User flag input -> list handler               | Malformed or conflicting input must fail with exact public behavior. |

## STRIDE threat register

| Threat ID | Category                | Component             | Severity | Disposition | Mitigation plan                                                                                |
| --------- | ----------------------- | --------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-05-138  | Tampering               | command catalog drift | high     | mitigate    | Retain the exact independent literal row and ordered equality assertion.                       |
| T-05-139  | Elevation of privilege  | test-coupled export   | medium   | mitigate    | Remove export/import after a production-consumer census and enforce static absence.            |
| T-05-140  | Tampering / Repudiation | public list input     | high     | mitigate    | Exercise all accepted flags and representative rejection classes with exact outcomes.          |
| T-05-141  | Denial of service       | Phase 6 list split    | low      | accept      | Keep the cohesive file unchanged except for scoped visibility cleanup; Phase 6 owns splitting. |

ASVS Level 1: catalog integrity, public input validation, and production/test boundary integrity are explicit controls.
</threat_model>

## Source coverage audit

| Source   | ID                  | Feature or constraint                                                                                 | Plan/task         | Status  | Notes                                                                 |
| -------- | ------------------- | ----------------------------------------------------------------------------------------------------- | ----------------- | ------- | --------------------------------------------------------------------- |
| GOAL     | -                   | Legitimate seams and preserved public contracts                                                       | 05-32 / Tasks 1-2 | COVERED | Private production state is tested through public behavior.           |
| REQ      | TREF-06             | Trace-preserving removal without behavior loss                                                        | 05-32 / Tasks 1-2 | COVERED | Export coupling is removed while independent/public evidence remains. |
| RESEARCH | catalog seam census | Remove only derived equality, retain literal pin                                                      | 05-32 / Tasks 1-2 | COVERED | Consumer census gates visibility change.                              |
| CONTEXT  | D-05-07, D-05-08    | Remove only terminal test-facing surface; keep private parse result, literal pin, and public behavior | 05-32 / Tasks 1-2 | COVERED | All removal and retention clauses are explicit.                       |
| CONTEXT  | D-05-09             | Phase 6 owns plugin-list split                                                                        | 05-32 / Tasks 1-2 | COVERED | No split or broad refactor occurs.                                    |

Final exact closure audits remain Plan 05-33. Phase 6 and exclusions are not gaps.

<verification>
Run both task commands. Confirm only the three listed files change, task scopes are three and two files, no `BOOLEAN_FLAGS` export/import remains, the private `parseFlagNames("list")` result remains, and the independent literal row is exactly `list: ["--available", "--installed", "--partial", "--remote", "--unavailable"]`. Confirm exact accepted/rejected public behavior, the Fallow comment `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor`, and no new seam, suppression, Phase 6 work, or excluded change.
</verification>

<success_criteria>

- The production-consumer census proves export removal is safe.
- No test imports or derived self-equality remain.
- The handler retains its private catalog-derived flag set.
- The independent five-flag literal pin and exact accepted/rejected public behavior remain green.
- No accessor, replacement export, catalog refactor, Phase 6 split, suppression, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-32-SUMMARY.md` when done.
</output>
