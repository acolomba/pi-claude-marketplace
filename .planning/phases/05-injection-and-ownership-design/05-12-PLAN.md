---
phase: 05-injection-and-ownership-design
plan: 12
type: execute
wave: 6
depends_on: [05-08, 05-09, 05-11]
files_modified:
  - extensions/pi-claude-marketplace/index.ts
  - extensions/pi-claude-marketplace/edge/types.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - tests/index.test.ts
  - tests/edge/types.test.ts
  - tests/edge/register.test.ts
autonomous: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One claudeMarketplaceExtension invocation constructs exactly one HooksRuntime and one CompletionCache, and every root/edge consumer receives those same identities (D-05-04, D-05-05)."
    - "Edge dependency types require both owned values; no optional field, default selector, test-only constructor, or second lifetime exists (D-05-01)."
    - "The runtime is immediately used by hook registration and resources-discover hydration, while the cache is immediately used by registered completion reads."
    - "The bounded completion transition from Plan 05-07/05-08 is removed from edge ownership without changing schemas, TTL, poison, invalidation, completion rows, or registration order."
    - "Same-extension reads share cache state, separate extension invocations are isolated, and same-runtime re-registration invalidates stale callbacks without assuming Pi disposal (D-05-06)."
    - "applyReconcile and bootstrapClaudePlugin remain the only full-observation composition exceptions; this plan adds no third exception and does not weaken either suite (D-05-02)."
  artifacts:
    - extensions/pi-claude-marketplace/index.ts
    - extensions/pi-claude-marketplace/edge/types.ts
    - extensions/pi-claude-marketplace/edge/register.ts
    - tests/index.test.ts
    - tests/edge/types.test.ts
    - tests/edge/register.test.ts
  key_links:
    - "claudeMarketplaceExtension -> createHooksRuntime/createCompletionCache -> registerHooksBridge/registerClaudePluginCommand"
    - "edge required dependency graph -> registered completion provider -> supplied CompletionCache"
    - "resources-discover callback -> supplied HooksRuntime -> project hydration before reconcile"
  prohibitions:
    - "No __deps, ForTest, test-only export/reset, optional/default/dead port, raw mutable state, or second runtime/cache lifetime."
    - "No Phase 6 createRequire/syncBuiltinESMExports removal or resolver/notify/install/update/reinstall/list/catalog split."
    - "No stage/PID/info/uninstall decomposition and no new Fallow suppression; preserve the existing Fallow comment byte-for-byte."
---

<objective>
Establish final root/edge lifecycle plumbing: construct one runtime and one cache in `claudeMarketplaceExtension`, pass them as required values through edge types/registration, use them immediately for hook and completion behavior, and remove the bounded cache transition owner (D-05-01, D-05-04, D-05-05).

Purpose: Replace bounded transition ownership with explicit lifecycle composition while preserving live hooks and completion behavior.
Output: Required root/edge contracts, one owned graph per extension load, and public lifecycle/completion isolation evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-08-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-09-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-11-SUMMARY.md
</context>

## Edge assumptions

- Use the executed dependency summaries for final factory, interface, and transition identifiers. Keep their required production contracts intact.
- Plan 05-09 already owns the one runtime construction. Add the cache beside it; do not replace or duplicate the runtime.
- Plan 05-08 establishes the live marketplace-name disposition. Preserve that branch and schema-2 invalidation exactly.
- Old callbacks can remain registered. Re-registration on the same runtime, not disposal, invalidates their generation before side effects.
- Later plans pass these same identities into mutation handlers. Do not create per-operation cache/runtime instances as temporary plumbing.
- applyReconcile and bootstrapClaudePlugin remain exactly the two full-observation composition exceptions. Phase 6 and the excluded stage/PID/info/uninstall work remain outside this plan.

## Artifacts this plan produces

| File family                            | Contract or proof                                                                                    |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Extension root, edge types, and owners | One lifecycle construction site and required runtime/cache dependency graph.                         |
| Edge registration and owner            | Immediate hook/completion use of the supplied instances and removal of the bounded cache transition. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one runtime and cache into the required edge contract</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-08-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-09-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-11-SUMMARY.md, extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/edge/types.ts, tests/index.test.ts, tests/edge/types.test.ts, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, extensions/pi-claude-marketplace/shared/completion-cache.ts</read_first>
  <files>extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/edge/types.ts, tests/index.test.ts, tests/edge/types.test.ts</files>
  <behavior>
    - "One extension invocation creates one runtime and one cache before registrations; a second invocation creates two independent owners."
    - "The edge dependency contract requires the exact HooksRuntime and CompletionCache production types with no optional/default form."
    - "The runtime passed to initial hooks registration is the runtime captured by resources-discover hydration and stale-generation checks."
    - "The cache passed to command registration is the cache used by plugin-index completion reads, and separate extension invocations do not share memory."
  </behavior>
  <action>Start with owner cases that construct two extension instances and distinguish their public route and completion observations. Per D-05-04 and D-05-05, retain the executed Plan 05-09 runtime construction and construct one CompletionCache beside it at the top of `claudeMarketplaceExtension`. Extend the consumer-owned edge dependency type with both required values, using the exact executed contracts from dependency summaries. Pass the same runtime to hook registration and resources-discover hydration, and pass the same cache to edge command registration. Keep initialization, await points, registration order, cwd capture, error reporting, and public registrations exact. Per D-05-01, reject optional members, fallback factories, raw state, test constructors, and any second lifetime.</action>
  <verify><automated>node --test tests/index.test.ts tests/edge/types.test.ts tests/edge/register.test.ts tests/architecture/hooks-lifecycle.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/types.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/index.ts extensions/pi-claude-marketplace/edge/types.ts tests/index.test.ts tests/edge/types.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; a load constructs more than one runtime or cache; separate loads share state; either edge value is optional, defaulted, dead, or bypassed; lifecycle or registration order changes; stale callbacks rely on disposal; or a test-only/raw-state surface appears.</fails_when></verify>
  <acceptance_criteria>The root owns one required runtime/cache pair per extension load and passes their exact identities into production edge composition (D-05-01, D-05-04, D-05-05).</acceptance_criteria>
  <reversibility rating="costly">This fixes the lifetime used by all later mutation routes, so changing it requires coordinated edge, hook, completion, and orchestrator migration.</reversibility>
  <done>One extension lifecycle creates and exposes one production-used HooksRuntime and CompletionCache through required edge types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove bounded cache ownership and prove immediate live use</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-08-SUMMARY.md, extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/edge/register.ts, tests/index.test.ts, tests/edge/register.test.ts, tests/edge/completions/provider.test.ts, tests/edge/completions/data.test.ts, extensions/pi-claude-marketplace/shared/completion-cache.ts</read_first>
  <files>extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/edge/register.ts, tests/index.test.ts, tests/edge/register.test.ts</files>
  <behavior>
    - "Command registration closes over the supplied lifecycle cache and every plugin completion mode reads that identity."
    - "Two reads in one extension retain cache-hit semantics, while the same key in a peer extension performs its own authoritative read."
    - "The bounded cache transition identifier from Plans 05-07/05-08 has no owner or production consumer after this task."
    - "Hook registration still uses the supplied lifecycle runtime, and old-generation callbacks remain inert before all effects."
  </behavior>
  <action>Change edge registration to require and capture the root-supplied cache instead of owning or importing the bounded transition selected by Plans 05-07/05-08. Route that value into the existing provider/data chain without changing candidate routing or callback shape. Remove the bounded transition owner from the listed root/edge files after a production-consumer census proves no remaining use; do not edit cache semantics or add a renamed binding. Add same-extension and peer-extension cases that assert complete candidate rows, state-read failures, registration behavior, and isolation before any forwarding evidence. Keep the runtime path from Plan 05-09/05-11 unchanged. Per D-05-02, run apply/bootstrap owners as read/verify-only controls and do not add another composition exception.</action>
  <verify><automated>node --test tests/index.test.ts tests/edge/register.test.ts tests/edge/completions/provider.test.ts tests/edge/completions/data.test.ts tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/index.ts extensions/pi-claude-marketplace/edge/register.ts tests/index.test.ts tests/edge/register.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; registration owns/imports another cache; a plugin completion bypasses the supplied cache; the bounded transition remains live or is renamed; candidate rows, errors, registration order, or runtime behavior changes; apply/bootstrap loses full observations; or a forbidden seam, suppression, split, or global-patch removal appears.</fails_when></verify>
  <acceptance_criteria>Registered hooks and completions immediately use the root-owned identities, and no bounded cache owner or second lifetime remains (D-05-01, D-05-04, D-05-05).</acceptance_criteria>
  <reversibility rating="costly">All mutation plans depend on this single cache identity reaching edge registration, so rollback would reintroduce transition ownership across many consumers.</reversibility>
  <done>The extension root and edge share one live runtime/cache graph with isolated peer lifecycles and no transition cache.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                                  | Description                                                                                       |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| Pi extension load -> owned graph                          | A wrong construction site can leak route, process, or completion state across extension loads.    |
| Root dependencies -> edge registration                    | Optional/default selection can bypass visible production ownership or create an unused test seam. |
| Persisted hooks/cache input -> live callbacks/completions | Validated data must enter only the matching lifecycle instance.                                   |

## STRIDE threat register

| Threat ID | Category                           | Component                    | Severity | Disposition | Mitigation plan                                                                                        |
| --------- | ---------------------------------- | ---------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------ |
| T-05-48   | Tampering / Information disclosure | runtime/cache construction   | high     | mitigate    | Construct one pair per extension invocation and prove independent public behavior across two loads.    |
| T-05-49   | Tampering                          | edge dependency selection    | high     | mitigate    | Require both values, remove transition ownership, and fail type/owner tests on omission or fallback.   |
| T-05-50   | Tampering / Denial of service      | stale hook callbacks         | high     | mitigate    | Preserve same-runtime generation checks before every old-callback effect without disposal assumptions. |
| T-05-51   | Tampering                          | completion freshness         | high     | mitigate    | Prove registered readers use the root cache and same/peer instance behavior stays exact.               |
| T-05-52   | Repudiation                        | registration and diagnostics | medium   | mitigate    | Preserve exact registration order, public results, errors, and messages in root/edge owners.           |

ASVS Level 1: all high-severity ownership, stale-callback, and freshness threats block completion until focused owner and lifecycle suites pass. No package or external service changes.
</threat_model>

## Source coverage audit

| Source   | ID                                 | Feature or constraint                                            | Plan/task         | Status  | Notes                                                                 |
| -------- | ---------------------------------- | ---------------------------------------------------------------- | ----------------- | ------- | --------------------------------------------------------------------- |
| GOAL     | -                                  | Correctly owned runtime state and public contracts               | 05-12 / Tasks 1-2 | COVERED | The extension root owns and uses one graph per load.                  |
| REQ      | TREF-04                            | Production-owned seams only                                      | 05-12 / Task 1    | COVERED | Required edge values have visible production composition.             |
| REQ      | TREF-05                            | Runtime/cache lifecycle ownership                                | 05-12 / Tasks 1-2 | COVERED | Transition ownership is replaced by the final root lifetime.          |
| REQ      | TREF-06                            | Public evidence without test cleanup                             | 05-12 / Tasks 1-2 | COVERED | Isolation and live behavior use public registrations and completions. |
| RESEARCH | required routing                   | One root graph reaches hooks and completions                     | 05-12 / Tasks 1-2 | COVERED | The executed dependency contracts are threaded unchanged.             |
| CONTEXT  | D-05-01, D-05-04, D-05-05          | Required production seams and one runtime/cache                  | 05-12 / Tasks 1-2 | COVERED | No optional/default/dead or second lifetime is permitted.             |
| CONTEXT  | D-05-02, D-05-03, D-05-06, D-05-09 | Preserve compositions, lifecycle semantics, and Phase 6 boundary | 05-12 / Task 2    | COVERED | Controls run read-only and deferred work stays excluded.              |

Plans 05-13 through 05-25 route mutation consumers through this graph. Terminal reset/public-surface cleanup remains in Plans 05-28 through 05-32, and Phase 6 work remains excluded.

<verification>
Run both task commands and `npm run check`. Confirm only the six frontmatter files change, each task changes at most four files, one runtime/cache pair exists per load, the transition cache is absent from root/edge ownership, apply/bootstrap remain exact controls, no forbidden seam or new Fallow marker appears, and `scripts/revalidation.mjs` retains the byte-exact existing line `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor`.
</verification>

<success_criteria>

- One extension invocation owns exactly one runtime and one cache.
- Required edge composition uses both identities immediately for hooks and completions.
- Separate invocations remain isolated, and stale callbacks stay inert without disposal.
- The bounded cache transition is removed without schema, completion, or registration drift.
- No prohibited surface, second lifetime, new suppression, Phase 6 work, or excluded decomposition appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md` when done.
</output>
