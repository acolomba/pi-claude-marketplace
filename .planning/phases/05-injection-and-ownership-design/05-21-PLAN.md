---
phase: 05-injection-and-ownership-design
plan: 21
type: execute
wave: 15
depends_on: [05-17, 05-20]
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - tests/edge/register.test.ts
  - tests/edge/handlers/marketplace/update.test.ts
  - tests/orchestrators/marketplace/update.test.ts
autonomous: true
requirements: [TREF-04, TREF-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Registered named and all-target marketplace update receives the same root CompletionCache used by add, remove, and completions (D-05-05)."
    - "Each successfully persisted marketplace target drops its own plugin-index cache before cascade; failed, skipped, and no-effect targets do not."
    - "Named and all-target updates preserve project-before-user and target order, eligibility, classifications, state, errors, rows, and notifications."
    - "The Plan 05-17 runtime-bound PluginUpdateFn retains its exact shared signature and cascade behavior; the cache is carried beside it, not added to it (D-05-06, D-05-09)."
    - "The next same-cache completion rebuilds each successful target exactly; unrelated, failed, other-scope, and peer-cache rows remain exact (D-05-01)."
    - "applyReconcile and bootstrapClaudePlugin remain exactly the two full-observation exceptions; no Phase 6 or excluded decomposition work lands."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    - tests/edge/register.test.ts
    - tests/edge/handlers/marketplace/update.test.ts
    - tests/orchestrators/marketplace/update.test.ts
  key_links:
    - "root CompletionCache -> registered marketplace update -> named/all-target orchestrator"
    - "successful marketplace persistence -> target plugin-index drop -> PluginUpdateFn cascade -> exact aggregate notification"
    - "same-instance completion rebuild -> successful targets only; peer/unrelated rows remain isolated"
  prohibitions:
    - "No PluginUpdateFn signature change, __deps, ForTest, test-only reset/export, optional/default/dead cache port, second lifetime, or broad invalidation bundle."
    - "No weakening/broadening of applyReconcile/bootstrapClaudePlugin exceptions."
    - "No Phase 6 update split/patch removal, excluded stage/PID/info/uninstall decomposition, or new Fallow suppression."
---

<objective>
Route named and all-target marketplace update through the same cache, dropping each successfully persisted target before cascade; preserve project-before-user order, no-effect/failure eligibility, exact same-instance rows, state, classifications, and notifications (D-05-01, D-05-05, D-05-06).

Purpose: Make post-update completions authoritative per target while preserving PluginUpdateFn shape, target order, classifications, state, and notifications.
Output: Required edge/update cache wiring and exact per-target invalidation, cascade, rebuild, isolation, and public outcome evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-17-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-20-SUMMARY.md
</context>

## Edge assumptions

- Use Plan 05-17's executed PluginUpdateFn and Plan 05-20's executed cache/edge identifiers.
- Keep PluginUpdateFn's shared signature unchanged. Marketplace update receives CompletionCache separately through handler/orchestrator composition.
- Per target, durable marketplace persistence decides eligibility. Drop that target's plugin index before its plugin cascade uses current state.
- Preserve named/all selection, project-before-user order, within-scope target order, skip/failure/no-effect classifications, and aggregate notification behavior.
- Cache invalidation failure retains current hygiene/error policy and does not change which durable target state committed.
- Preserve both composition exceptions, Phase 6 machinery, exact Fallow comment, and excluded stage/PID/info/uninstall boundaries.

## Artifacts this plan produces

| File family                               | Contract or proof                                                                                             |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Edge register/update handler and owners   | Required lifecycle-cache forwarding for named and all-target public update.                                   |
| Marketplace update orchestrator and owner | Per-successful-target cache drop before cascade with exact order, classification, rebuild, and notifications. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the lifecycle cache through named and all-target update</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-17-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-20-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts, tests/edge/register.test.ts, tests/edge/handlers/marketplace/update.test.ts, tests/edge/completions/data.test.ts, tests/orchestrators/plugin/update.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts, tests/edge/register.test.ts, tests/edge/handlers/marketplace/update.test.ts</files>
  <behavior>
    - "The registered update handler forwards the same required CompletionCache used by add/remove/completions beside the unchanged PluginUpdateFn."
    - "Named/all parsing, scope selection, project-before-user order, accepted/no-effect/failure results, warnings, and notifications remain exact."
    - "Successful public update changes next same-extension completion rows for that target; peer and unrelated target rows stay unchanged."
    - "No handler-local cache, optional/default selector, or PluginUpdateFn signature change exists."
  </behavior>
  <action>Extend edge registration and the marketplace update handler to require and forward the root CompletionCache separately from the Plan 05-17 runtime-bound PluginUpdateFn. Preserve the shared callback type exactly. Keep named/all parsing, scope selection, project-before-user order, target enumeration, results, classifications, warnings, notification behavior, and registration order. Add handler/registration cases that seed several target/scope completion rows, execute named and all updates, and assert exact next same-instance rows plus peer/unrelated isolation before forwarding evidence. Run plugin update and completion suites read/verify-only. Do not construct another cache or change callback ownership.</action>
  <verify><automated>node --test tests/edge/register.test.ts tests/edge/handlers/marketplace/update.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/update.test.ts tests/edge/completions/data.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts tests/edge/register.test.ts tests/edge/handlers/marketplace/update.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; cache is optional, defaulted, dead, bypassed, or constructed in handler; PluginUpdateFn changes; named/all order/results/classifications/notifications drift; same-instance rows stay stale; peer/unrelated rows change; or another lifetime/deferred work appears.</fails_when></verify>
  <acceptance_criteria>Registered named/all marketplace update carries one lifecycle cache beside the unchanged PluginUpdateFn and preserves exact public ordering and outcomes (D-05-05, D-05-06, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">The cache and shared update callback now cross the public marketplace update surface, so changes require coordinated edge, cascade, and completion migration.</reversibility>
  <done>The public update handler uses the root cache and unchanged runtime-bound callback with exact named/all behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop each successful target before cascade and preserve classifications</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-17-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-20-SUMMARY.md, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, tests/orchestrators/marketplace/update.test.ts, extensions/pi-claude-marketplace/edge/register.ts, tests/edge/register.test.ts, tests/orchestrators/plugin/update.test.ts, tests/shared/completion-cache.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, tests/edge/register.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <behavior>
    - "For each target, successful marketplace persistence is followed by that target's plugin-index drop before PluginUpdateFn cascade."
    - "Failed, skipped, and no-effect targets do not drop; successful peers continue in exact project-before-user and target order."
    - "Invalidation/cascade failures retain exact per-target state, eligibility, classification, cleanup, row, warning, and notification behavior."
    - "The next same-cache read rebuilds exact rows for successful targets only; unrelated/failed/peer-cache entries remain exact."
  </behavior>
  <action>Thread the required CompletionCache into marketplace update without changing PluginUpdateFn. At each existing target loop, preserve marketplace persistence and classification. When a target persists successfully, drop only that scope/marketplace plugin-index entry before invoking the existing cascade callback. Preserve no-effect, skipped, failure, and continued-target eligibility, project-before-user ordering, rollback/cleanup, aggregate result construction, silence, and exact notification timing. Retain the current cache-drop failure policy and test it without altering durable state. Add named/all owner cases covering mixed success/failure/no-effect, multiple scopes, invalidation failure, cascade failure, exact same-instance rebuilds, and unrelated/peer isolation. Assert state/tree/rows/classifications/errors/warnings/notifications before call logs.</action>
  <verify><automated>node --test tests/orchestrators/marketplace/update.test.ts tests/edge/register.test.ts tests/edge/handlers/marketplace/update.test.ts tests/orchestrators/plugin/update.test.ts tests/shared/completion-cache.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts tests/orchestrators/marketplace/update.test.ts extensions/pi-claude-marketplace/edge/register.ts tests/edge/register.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; target drop occurs before persistence or after cascade, touches failed/no-effect/unrelated targets, changes PluginUpdateFn, or changes project-before-user/target order; exact state/rows/classifications/errors/warnings/cleanup/notifications drift; or a forbidden seam/suppression/split appears.</fails_when></verify>
  <acceptance_criteria>Each successfully persisted marketplace target invalidates only its lifecycle-cache row before cascade, with exact order, eligibility, rebuild, state, classification, and notification behavior (D-05-01, D-05-05, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">This order links per-target persistence, completion freshness, and the shared update cascade, so changes affect named and all-target public contracts.</reversibility>
  <done>Named and all-target updates refresh only successful cache targets before cascade and preserve exact aggregate behavior.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                  | Description                                                                               |
| ----------------------------------------- | ----------------------------------------------------------------------------------------- |
| Named/all input -> marketplace target set | User selection expands across scopes and targets with defined precedence and eligibility. |
| Durable target persistence -> cache drop  | Wrong timing or target can expose stale rows or erase valid completion state.             |
| Cache drop -> plugin update cascade       | Ordering and callback shape determine per-target state, routes, rows, and notifications.  |

## STRIDE threat register

| Threat ID | Category                           | Component                      | Severity | Disposition | Mitigation plan                                                                                 |
| --------- | ---------------------------------- | ------------------------------ | -------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-05-91   | Tampering / Elevation of privilege | target selection/order         | high     | mitigate    | Preserve validation, named/all eligibility, project-before-user, and within-scope target order. |
| T-05-92   | Tampering                          | persistence/drop/cascade order | high     | mitigate    | Assert successful persistence then target drop then unchanged PluginUpdateFn callback.          |
| T-05-93   | Tampering / Information disclosure | target/cache isolation         | high     | mitigate    | Drop exact scope/marketplace entries and prove failed/unrelated/peer rows stay unchanged.       |
| T-05-94   | Repudiation                        | mixed target classifications   | medium   | mitigate    | Preserve exact state, rows, classifications, errors, warnings, and notifications.               |
| T-05-95   | Denial of service                  | invalidation/cascade failure   | medium   | mitigate    | Keep current continuation, cleanup, and committed-state behavior per target.                    |

ASVS Level 1: target selection, mutation order, and cache isolation are high-severity blocking threats.
</threat_model>

## Source coverage audit

| Source   | ID                              | Feature or constraint                                  | Plan/task         | Status  | Notes                                                     |
| -------- | ------------------------------- | ------------------------------------------------------ | ----------------- | ------- | --------------------------------------------------------- |
| GOAL     | -                               | Lifecycle-owned cache and public contracts             | 05-21 / Tasks 1-2 | COVERED | Named/all updates refresh exact successful targets.       |
| REQ      | TREF-04                         | Preserve production contracts and real behavior        | 05-21 / Task 2    | COVERED | Cache is required; update transaction/cascade stays real. |
| REQ      | TREF-05                         | One cache owns completion/invalidation state           | 05-21 / Tasks 1-2 | COVERED | Root identity reaches update and completion.              |
| RESEARCH | marketplace update invalidation | Successful target drops before cascade                 | 05-21 / Task 2    | COVERED | Ordering, eligibility, rows, and isolation are explicit.  |
| CONTEXT  | D-05-01, D-05-05, D-05-06       | Required cache and genuine per-target invalidation     | 05-21 / Tasks 1-2 | COVERED | No optional/default/dead or second cache appears.         |
| CONTEXT  | D-05-03, D-05-09                | Keep PluginUpdateFn shape and Phase 6 update split out | 05-21 / Tasks 1-2 | COVERED | Current callback/module ownership stays intact.           |

Plugin cache invalidators continue in Plans 05-22 through 05-25. Final graph, composition, and terminal cleanup remain Plans 05-26 through 05-33. Exclusions are not gaps.

<verification>
Run both task commands and `npm run check`. Confirm only the six frontmatter files change, each task changes four files, PluginUpdateFn stays unchanged, successful targets drop after persistence and before cascade, named/all ordering and exact rows remain, no second cache/deferred work appears, and the Fallow comment remains byte-exact.
</verification>

<success_criteria>

- Registered named and all-target marketplace update uses the lifecycle cache beside the unchanged PluginUpdateFn.
- Successful targets persist, drop their cache entry, then cascade in exact project-before-user and target order.
- Failed, skipped, no-effect, unrelated, other-scope, and peer-cache targets retain exact state and rows.
- State/tree, classifications, errors, warnings, cleanup, results, completion rebuilds, and notifications remain exact.
- No forbidden seam, second lifetime, callback drift, weakened composition, suppression, Phase 6 work, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-21-SUMMARY.md` when done.
</output>
