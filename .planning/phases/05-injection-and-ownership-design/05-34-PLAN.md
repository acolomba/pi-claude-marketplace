---
phase: 05-injection-and-ownership-design
plan: 34
type: execute
wave: 25
depends_on: [05-02, 05-14, 05-26, 05-27, 05-31, 05-32, 05-33]
files_modified: []
autonomous: true
gap_closure: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All eleven MF-DEC-07 roots retain exactly one legitimate classification, live production binding, and public proof under TREF-04 and D-05-01 through D-05-03."
    - "`applyReconcile` and `bootstrapClaudePlugin` remain exactly the two behavioral-composition exceptions required by D-05-02, with no third exception or weakened public evidence."
    - "TREF-05 and TREF-06 remain closed: lifecycle/factory ownership is live, the terminal seam manifest is absent, the port manifest is narrow and production-used, and D-05-04 through D-05-08 remain satisfied."
    - "The reviewed Phase 6 handoff is ratified as 13 files/85 `syncBuiltinESMExports(` calls and 9 files/9 `createRequire(` calls, with the exact three Phase 5 attribution deltas and no broad removal or split campaign (GD-05-01, D-05-03, D-05-09)."
    - "The user-owned untracked `.mcp.json` remains byte-identical at SHA-256 `09237a8c19625daeb677211c6ca9b38e01aa12eede0f4b5f2c26ed6ec7b449e6`; the in-place aggregate stop is recorded, every independent downstream gate passes, and canonical `npm run check` passes in a disposable clean tracked HEAD worktree that excludes the file (GD-05-02)."
    - "Task 3's dynamic npm prefix is an explicit `verify_command_path_resolvability` reviewed exception: deterministic probe status `REVIEWED_DYNAMIC_TARGET` and reason `runtime_mktemp_child_awaits_worktree_add` are recorded, and the readonly `snapshot_root` is proved to be exactly `$snapshot_parent/tracked-head` before any filesystem, Git, or npm target use."
    - "The exact sole Fallow comment remains unchanged, no new suppression appears, and the executor creates only `05-34-SUMMARY.md`; any genuine implementation or tracked-gate failure halts without repair (GD-05-03, GD-05-04)."
  artifacts:
    - path: ".planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md"
      provides: "Complete evidence-only gap-closure ledger and independent-verification handoff"
  key_links:
    - from: ".planning/phases/05-injection-and-ownership-design/05-VERIFICATION.md"
      to: ".planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md"
      via: "Both failed truths become explicit ratification, attribution, workspace-isolation, and green-gate evidence"
      pattern: "13 files/85 calls|clean tracked HEAD|npm run check"
    - from: "8c46250dea76be1059110c54f00fe33139db55ba"
      to: "2ef1533255a6b93aa42c622d73c272406b0fae9e"
      via: "Exact HSA-026, event-router, and enable-disable per-file patch-count attribution"
      pattern: "8 -> 0|2 -> 10|11 -> 13"
    - from: "tracked HEAD"
      to: "canonical npm run check"
      via: "Detached temporary worktree with repository node_modules linked under its ignored path and no `.mcp.json`"
      pattern: "git worktree add --detach|npm --prefix"
    - from: ".planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md"
      to: "Phase 05 independent re-verification"
      via: "Rerun handoff after all evidence gates pass"
      pattern: "gsd-verify-work 05"
  prohibitions:
    - "No source, test, configuration, roadmap, requirement, historical plan, historical summary, verification report, ignore file, or `.mcp.json` edit."
    - "No restoration of obsolete builtin patching, global-patch removal, split extraction, excluded-file edit, or suppression."
    - "No gate skip, retry with weaker settings, failure silencing, package installation, or retained temporary worktree."
---

<objective>
Close the two Phase 05 verifier gaps with evidence only: ratify the reviewed 13-file/85-call Phase 6 patch manifest and its three exact Phase 5 deltas, then prove the tracked HEAD with the canonical repository check in a disposable clean worktree while preserving the user-owned `.mcp.json` byte-for-byte (TREF-04, TREF-05, TREF-06; D-05-01 through D-05-09; GD-05-01 through GD-05-04).

Purpose: Replace the stale Plan 05-33 audit premise with the reviewed lifecycle/port attribution and distinguish an in-place untracked-workspace obstruction from the tracked repository's canonical result.
Output: `05-34-SUMMARY.md` containing exact manifests, command results, cleanup proof, implementation-tree immutability proof, and the independent verification rerun handoff.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-VERIFICATION.md
@.planning/phases/05-injection-and-ownership-design/05-02-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-14-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-27-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-31-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-32-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-33-PLAN.md
@.planning/phases/05-injection-and-ownership-design/05-33-SUMMARY.md
</context>

## Locked gap decisions

- **GD-05-01:** Ratify the reviewed current manifest at 13 files/85 `syncBuiltinESMExports(` calls. Plan 05-02's HSA-026 change from 8 to 0 is required narrow-port migration. Plan 05-26's event-router change from 2 to 10 and Plan 05-14's enable-disable change from 11 to 13 are required lifecycle evidence. Retain `createRequire(` at 9 files/9 calls. Prove that no broad Phase 6 removal or split campaign occurred. Never restore obsolete builtin patching.
- **GD-05-02:** `.mcp.json` is untracked and user-owned. Preserve SHA-256 `09237a8c19625daeb677211c6ca9b38e01aa12eede0f4b5f2c26ed6ec7b449e6`. Do not edit, ignore, or suppress it. Record the expected in-place aggregate stop, run the later gates independently, and run canonical `npm run check` against tracked HEAD in a disposable detached worktree that does not contain this untracked file. Clean the exact temporary worktree through `git worktree remove` on every exit.
- **GD-05-03:** Preserve exactly one line `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor`. Add no suppression.
- **GD-05-04:** This plan is evidence-only. The executor can create only `05-34-SUMMARY.md`. A genuine source, test, type, lint, Fallow, correspondence, negative-control, unit, integration, or clean tracked canonical failure halts without repair.

## Exact eleven-root and port manifest

| Root                                                                                             | Owner                                    | Legitimate boundary                                                                                           | Production binding and public proof                                                                                                          |
| ------------------------------------------------------------------------------------------------ | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `BSKL-019`                                                                                       | `bridges/skills/unstage.ts`              | `SkillsUnstageRemover.removeTree`; ordinary removal uses real case-owned temporary trees                      | Private Node remover -> `createUnstagePluginSkills` -> owner result/tree/error/order evidence                                                |
| `HHD-011`                                                                                        | `bridges/hooks/event-router.ts`          | `HooksHydrationReader.loadState` for hydration order and selected read races                                  | Root lifecycle -> `createHooksHydration(runtime, reader)` -> event-router and index owner evidence                                           |
| `HSA-026`                                                                                        | `bridges/hooks/stage.ts`                 | `HooksTreeInspector` limited to `lstat`, `readdir`, `readlink`, and `realpath`; ordinary staging remains real | Private Node inspector -> `createWriteHookConfig` -> exact bytes, containment, skip, and fault evidence                                      |
| `OPEF-F04`                                                                                       | `orchestrators/plugin/fetch.ts`          | Fetch-owned `FetchStatus` for post-clone visibility                                                           | Real status -> `createFetchPlugins` -> materialization, credential, staging, result, and notice evidence                                     |
| `OPEF-F09`                                                                                       | `orchestrators/plugin/enable-disable.ts` | `EnableDisableTransaction` plus root-owned `HooksRouting`; portable file behavior remains real                | Node transaction and lifecycle routing -> `createSetPluginEnabled` -> durable-state, rollback, route, and notice evidence                    |
| `.planning/reviews/unit-test-adversarial/adversarial/orchestrators-plugin-install-a.md#OPIA-F05` | `orchestrators/plugin/install.ts`        | `InstallTransaction` plus supplied `HooksRouting` and `CompletionCache`                                       | Node transaction -> `createInstallPlugin` -> exact schedule, compensation, state/tree, result, and notice evidence                           |
| `OPIC-F27`                                                                                       | `orchestrators/plugin/info.ts`           | `PluginInfoReader` for selected UTF-8 reads and directory listing; ordinary missing/corrupt trees remain real | Private Node reader -> `createGetPluginInfo` -> exact error, safety, row, and network-fence evidence                                         |
| `OPLU-B-F10`                                                                                     | `orchestrators/plugin/uninstall.ts`      | Cohesive `UninstallTransaction` plus supplied `HooksRouting` and `CompletionCache`                            | Node transaction -> `createUninstallPlugin` -> exact cascade, partial state, cleanup, result, and notice evidence                            |
| `OPR-B-F14`                                                                                      | `orchestrators/plugin/reinstall.ts`      | `ReinstallTransaction` plus supplied `HooksRouting` and `CompletionCache`                                     | Node transaction -> `createReinstallPlugin` -> exact replacement, rollback, finalization, cleanup, result, and notice evidence               |
| `ORA-F15`                                                                                        | `orchestrators/reconcile/apply.ts`       | `ReconcileStateReader.loadState` only for the selected-state race                                             | Real loader -> `createApplyReconcile`; real mutation children preserve result, state/configuration/tree, order, silence, and notices         |
| `SHC-F003`                                                                                       | `shared/path-safety.ts`                  | `PathSafetyInspector` limited to `lstat` and `readlink`                                                       | Private Node inspector -> `createPathSafetyGuard` -> lexical short-circuit, inspection order, symlink refusal, containment, and exact errors |

## Exact two-exception manifest

| Exception               | Real composition retained                                                                                                                  | Exact public evidence retained                                                                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `applyReconcile`        | Real marketplace remove/add, plugin uninstall/install, enable/disable, and backfill children; only the classified state reader is supplied | Complete results, project-before-user order, state/configuration, both scope trees, route/cache effects, failures, redaction, silence, and notification bytes |
| `bootstrapClaudePlugin` | Real `addMarketplace` followed by real marketplace autoupdate through the supplied lifecycle completion cache                              | Success, convergence, partial success, add failure, state/configuration, both scope trees, cache isolation, child order, and exact notification bytes         |

## Exact terminal-seam and static manifest

- Absent terminal surfaces: `resetEpoch`, `resetRoutingState`, `resetCompletionCache`, an exported or imported `BOOLEAN_FLAGS`, and any whole-state cleanup replacement in `routing-state.ts` or `completion-cache.ts`.
- Absent bounded transition residue: `transitionRoutingState`, `TRANSITION_ROUTING_STATE`, `NODE_TRANSITION_RUNTIME`, `NODE_HOOKS_HYDRATION`, `transitionCompletionCacheOwner`, and `transitionCompletionCache`.
- Retained production lifecycle behavior: runtime-bound `HooksRouting`, the production-used `resetSettleState(runtime)` transition, private completion-cache memory, and the unchanged PID-table policy. The production settle transition is not a test cleanup seam.
- Port liveness: the eleven interfaces/factories in the root manifest exist in their owners, real production composition reaches them, focused public suites pass, Fallow reports no dead capability, and the Phase 5 production diff adds no `__deps` or `ForTest` surface.
- Phase 6 boundary: the only added production/test pair since phase base `8c46250dea76be1059110c54f00fe33139db55ba` is `bridges/hooks/runtime.ts` with `tests/bridges/hooks/runtime.test.ts`; `tests/bridges/skills/stage.test.ts` is unchanged; resolver, notify, install, update, reinstall, list, and catalog splits remain deferred; info remains excluded; uninstall remains cohesive.
- Suppression boundary: the Phase 5 production/test/script diff adds no Fallow, ESLint, TypeScript, or coverage suppression, and `scripts/revalidation.mjs` retains the one exact authorized Fallow line.

## Reviewed global-patch manifests and attribution

### `syncBuiltinESMExports(` at implementation tracking base `2ef1533255a6b93aa42c622d73c272406b0fae9e`

| Calls | Test file                                           |
| ----: | --------------------------------------------------- |
|     2 | `tests/bridges/commands/discover.test.ts`           |
|    10 | `tests/bridges/hooks/event-router.test.ts`          |
|    16 | `tests/bridges/skills/stage.test.ts`                |
|     4 | `tests/bridges/skills/unstage.test.ts`              |
|     3 | `tests/index.test.ts`                               |
|    13 | `tests/orchestrators/plugin/enable-disable.test.ts` |
|     2 | `tests/orchestrators/plugin/fetch.test.ts`          |
|     2 | `tests/orchestrators/plugin/info.test.ts`           |
|    17 | `tests/orchestrators/plugin/install.test.ts`        |
|     4 | `tests/orchestrators/plugin/reinstall.test.ts`      |
|     2 | `tests/orchestrators/plugin/uninstall.test.ts`      |
|     2 | `tests/orchestrators/reconcile/apply.test.ts`       |
|     8 | `tests/shared/path-safety.test.ts`                  |

Total: exactly 13 files and 85 calls.

### `createRequire(` at implementation tracking base `2ef1533255a6b93aa42c622d73c272406b0fae9e`

Each file contains exactly one call: `tests/bridges/commands/discover.test.ts`, `tests/bridges/skills/stage.test.ts`, `tests/orchestrators/import/execute.test.ts`, `tests/orchestrators/plugin/enable-disable.test.ts`, `tests/orchestrators/plugin/install.test.ts`, `tests/orchestrators/plugin/reinstall.test.ts`, `tests/orchestrators/plugin/scope-tree-inventory.ts`, `tests/orchestrators/plugin/uninstall.test.ts`, and `tests/orchestrators/reconcile/apply.test.ts`.

Total: exactly 9 files and 9 calls.

### Required Phase 5 attribution from phase base `8c46250dea76be1059110c54f00fe33139db55ba`

| File                                                | Base -> tracking base | Attribution                               | Locked disposition                                                       |
| --------------------------------------------------- | --------------------: | ----------------------------------------- | ------------------------------------------------------------------------ |
| `tests/bridges/hooks/stage.test.ts`                 |                8 -> 0 | Plan 05-02 commits `b23e7bc8`, `7293e503` | Required HSA-026 narrow-port migration under D-05-01/D-05-03/GD-05-01    |
| `tests/bridges/hooks/event-router.test.ts`          |               2 -> 10 | Plan 05-26 commits `f0c95ff8`, `a1e0030d` | Required stale-callback lifecycle evidence under D-05-04/GD-05-01        |
| `tests/orchestrators/plugin/enable-disable.test.ts` |              11 -> 13 | Plan 05-14 commit `cb78cb04`              | Required durable-state/routing lifecycle evidence under D-05-04/GD-05-01 |

<tasks>

<task type="tracer">
  <name>Task 1: Ratify exact ownership, patch, exception, seam, and Phase 6 manifests</name>
  <precondition>Implementation tracking commit `2ef1533255a6b93aa42c622d73c272406b0fae9e` and phase base `8c46250dea76be1059110c54f00fe33139db55ba` resolve, and tracked source/test/script/package bytes do not differ from the implementation tracking commit.</precondition>
  <files>.planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md only</files>
  <read_first>AGENTS.md, .planning/phases/05-injection-and-ownership-design/05-CONTEXT.md, .planning/phases/05-injection-and-ownership-design/05-VERIFICATION.md, .planning/phases/05-injection-and-ownership-design/05-02-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-14-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-27-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-31-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-32-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-33-SUMMARY.md</read_first>
  <behavior>
    - "The summary copies the exact eleven-root and port table above with no shortened full-path root, duplicate, omission, or twelfth row (TREF-04, D-05-01, D-05-03)."
    - "The summary records exactly the two exception rows above and current focused public proof for both real-child flows (TREF-04, TREF-06, D-05-02)."
    - "The terminal-seam, bounded-transition, port-liveness, suppression, and Phase 6 static manifests above all hold (TREF-05, TREF-06, D-05-04 through D-05-09)."
    - "The summary ratifies the exact 13-file/85-call and 9-file/9-call manifests, plus all three base-to-tracking attribution deltas, under GD-05-01."
    - "The 13-file root/exception suite and six-file terminal-ownership suite pass with zero failures, skips, or todos."
  </behavior>
  <action>Reconcile the exact manifests above against the ledger, the cited completed-plan evidence, current production/test liveness, and both fixed Git commits. Record every row and count in the summary. Treat the three attributed changes as required Phase 5 port/lifecycle work per GD-05-01, not as Phase 6 work. Prove current owner and exception behavior with both focused suites. Use CodeGraph before any new source-location search. Stop on any mismatch. Per GD-05-04, do not repair or edit the implementation tree.</action>
  <verify>
    <automated>set -euo pipefail; phase_base=8c46250dea76be1059110c54f00fe33139db55ba; tracking_base=2ef1533255a6b93aa42c622d73c272406b0fae9e; git cat-file -e "$phase_base^{commit}" &amp;&amp; git cat-file -e "$tracking_base^{commit}" &amp;&amp; git diff --quiet "$tracking_base"..HEAD -- extensions/pi-claude-marketplace tests scripts package.json package-lock.json tsconfig.json eslint.config.js .fallowrc.json &amp;&amp; count_fixed() { node -e 'const fs = require("node:fs"); const text = fs.readFileSync(process.argv[1], "utf8"); const needle = process.argv[2]; process.stdout.write(String(text.split(needle).length - 1));' "$1" "$2"; } &amp;&amp; count_at() { count_tmp="$(mktemp /tmp/phase05-34-count.XXXXXX)" || return; if ! git show "$1:$2" &gt;"$count_tmp"; then rm -f -- "$count_tmp" || return; return 1; fi; if GSD_COUNT_NEEDLE="$3" node -e 'const fs = require("node:fs"); const text = fs.readFileSync(0, "utf8"); process.stdout.write(String(text.split(process.env.GSD_COUNT_NEEDLE).length - 1));' &lt;"$count_tmp"; then count_status=0; else count_status=$?; fi; rm -f -- "$count_tmp" || return 1; return "$count_status"; } &amp;&amp; expected_sync_files="$(printf '%s\n' tests/bridges/commands/discover.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/skills/stage.test.ts tests/bridges/skills/unstage.test.ts tests/index.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/info.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/reconcile/apply.test.ts tests/shared/path-safety.test.ts)" &amp;&amp; test "$(rg -l 'syncBuiltinESMExports\(' tests | sort)" = "$expected_sync_files" &amp;&amp; test "$(rg -o 'syncBuiltinESMExports\(' tests | wc -l | tr -d ' ')" -eq 85 &amp;&amp; test "$(count_fixed tests/bridges/commands/discover.test.ts 'syncBuiltinESMExports(')" -eq 2 &amp;&amp; test "$(count_fixed tests/bridges/hooks/event-router.test.ts 'syncBuiltinESMExports(')" -eq 10 &amp;&amp; test "$(count_fixed tests/bridges/skills/stage.test.ts 'syncBuiltinESMExports(')" -eq 16 &amp;&amp; test "$(count_fixed tests/bridges/skills/unstage.test.ts 'syncBuiltinESMExports(')" -eq 4 &amp;&amp; test "$(count_fixed tests/index.test.ts 'syncBuiltinESMExports(')" -eq 3 &amp;&amp; test "$(count_fixed tests/orchestrators/plugin/enable-disable.test.ts 'syncBuiltinESMExports(')" -eq 13 &amp;&amp; test "$(count_fixed tests/orchestrators/plugin/fetch.test.ts 'syncBuiltinESMExports(')" -eq 2 &amp;&amp; test "$(count_fixed tests/orchestrators/plugin/info.test.ts 'syncBuiltinESMExports(')" -eq 2 &amp;&amp; test "$(count_fixed tests/orchestrators/plugin/install.test.ts 'syncBuiltinESMExports(')" -eq 17 &amp;&amp; test "$(count_fixed tests/orchestrators/plugin/reinstall.test.ts 'syncBuiltinESMExports(')" -eq 4 &amp;&amp; test "$(count_fixed tests/orchestrators/plugin/uninstall.test.ts 'syncBuiltinESMExports(')" -eq 2 &amp;&amp; test "$(count_fixed tests/orchestrators/reconcile/apply.test.ts 'syncBuiltinESMExports(')" -eq 2 &amp;&amp; test "$(count_fixed tests/shared/path-safety.test.ts 'syncBuiltinESMExports(')" -eq 8 &amp;&amp; expected_create_files="$(printf '%s\n' tests/bridges/commands/discover.test.ts tests/bridges/skills/stage.test.ts tests/orchestrators/import/execute.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/scope-tree-inventory.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/reconcile/apply.test.ts)" &amp;&amp; test "$(rg -l 'createRequire\(' tests | sort)" = "$expected_create_files" &amp;&amp; test "$(rg -o 'createRequire\(' tests | wc -l | tr -d ' ')" -eq 9 &amp;&amp; for create_file in $expected_create_files; do test "$(count_fixed "$create_file" 'createRequire(')" -eq 1; done &amp;&amp; git cat-file -e "$phase_base:tests/bridges/hooks/stage.test.ts" &amp;&amp; git cat-file -e "$tracking_base:tests/bridges/hooks/stage.test.ts" &amp;&amp; git cat-file -e "$phase_base:tests/bridges/hooks/event-router.test.ts" &amp;&amp; git cat-file -e "$tracking_base:tests/bridges/hooks/event-router.test.ts" &amp;&amp; git cat-file -e "$phase_base:tests/orchestrators/plugin/enable-disable.test.ts" &amp;&amp; git cat-file -e "$tracking_base:tests/orchestrators/plugin/enable-disable.test.ts" &amp;&amp; test "$(count_at "$phase_base" tests/bridges/hooks/stage.test.ts 'syncBuiltinESMExports(')" -eq 8 &amp;&amp; test "$(count_at "$tracking_base" tests/bridges/hooks/stage.test.ts 'syncBuiltinESMExports(')" -eq 0 &amp;&amp; test "$(count_at "$phase_base" tests/bridges/hooks/event-router.test.ts 'syncBuiltinESMExports(')" -eq 2 &amp;&amp; test "$(count_at "$tracking_base" tests/bridges/hooks/event-router.test.ts 'syncBuiltinESMExports(')" -eq 10 &amp;&amp; test "$(count_at "$phase_base" tests/orchestrators/plugin/enable-disable.test.ts 'syncBuiltinESMExports(')" -eq 11 &amp;&amp; test "$(count_at "$tracking_base" tests/orchestrators/plugin/enable-disable.test.ts 'syncBuiltinESMExports(')" -eq 13 &amp;&amp; phase_added="$(git diff --diff-filter=A --name-only "$phase_base".."$tracking_base" -- extensions/pi-claude-marketplace tests)" &amp;&amp; test "$phase_added" = "$(printf '%s\n' extensions/pi-claude-marketplace/bridges/hooks/runtime.ts tests/bridges/hooks/runtime.test.ts)" &amp;&amp; git diff --quiet "$phase_base".."$tracking_base" -- tests/bridges/skills/stage.test.ts &amp;&amp; ! rg -n 'resetEpoch|resetRoutingState|resetCompletionCache|export.*BOOLEAN_FLAGS|import.*BOOLEAN_FLAGS|transitionRoutingState|TRANSITION_ROUTING_STATE|NODE_TRANSITION_RUNTIME|NODE_HOOKS_HYDRATION|transitionCompletionCacheOwner|transitionCompletionCache' extensions/pi-claude-marketplace tests &amp;&amp; ! rg -n '^[[:space:]]*(export[[:space:]]+)?(async[[:space:]]+)?(function|const)[[:space:]]+(reset|clearAll|clear[A-Z].*(State|Cache))' extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts extensions/pi-claude-marketplace/shared/completion-cache.ts &amp;&amp; production_diff="$(git diff --unified=0 "$phase_base".."$tracking_base" -- extensions/pi-claude-marketplace)" &amp;&amp; ! printf '%s\n' "$production_diff" | rg '^\+.*(__deps|ForTest)' &amp;&amp; phase_diff="$(git diff --unified=0 "$phase_base".."$tracking_base" -- extensions/pi-claude-marketplace tests scripts/revalidation.mjs)" &amp;&amp; ! printf '%s\n' "$phase_diff" | rg '^\+.*(fallow-ignore|eslint-disable|@ts-ignore|@ts-nocheck|node:coverage ignore)' &amp;&amp; test "$(rg -Fxc '// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor' scripts/revalidation.mjs)" -eq 1 &amp;&amp; node --test tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/plugin/bootstrap.test.ts tests/edge/handlers/plugin/bootstrap.test.ts tests/bridges/skills/unstage.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/stage.test.ts tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/info.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/shared/path-safety.test.ts &amp;&amp; node --test tests/bridges/hooks/runtime.test.ts tests/bridges/hooks/routing-state.test.ts tests/shared/completion-cache.test.ts tests/edge/handlers/plugin/list.test.ts tests/architecture/hooks-lifecycle.test.ts tests/architecture/flag-catalog-drift.test.ts</automated>
    <fails_when>The tracking or phase-base commit is missing; tracked implementation bytes drift from `2ef15332`; the root table is not exactly eleven unique full IDs; the port or exception manifest differs; any terminal/bounded/replacement seam remains; a production port is added through `__deps` or `ForTest`; the sync manifest differs from 13 files/85 calls; the `createRequire` manifest differs from 9 files/9 calls; any attribution is not exactly 8 to 0, 2 to 10, and 11 to 13; any added pair differs from hooks runtime; the excluded skills-stage test changed; a suppression or broad Phase 6 split/removal appears; the exact Fallow line differs; a focused command fails or discovers a skip/todo; or any source/test repair occurs.</fails_when>
  </verify>
  <acceptance_criteria>The summary records the complete exact root, port, exception, terminal/static, patch, attribution, suppression, and Phase 6 manifests with both focused suites green (TREF-04, TREF-05, TREF-06; D-05-01 through D-05-09; GD-05-01, GD-05-03, GD-05-04).</acceptance_criteria>
  <reversibility rating="reversible">This task records evidence only and cannot change the implementation contract.</reversibility>
  <done>The stale 14/83 premise is replaced by exact 13/85 plus 9/9 evidence without restoring obsolete patching or crossing the Phase 6 boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Prove all direct pairs and independent in-place repository gates</name>
  <precondition>Task 1's exact manifests and focused suites are green, `.mcp.json` remains untracked at the locked SHA-256, and `node_modules` exists for the repository's installed toolchain.</precondition>
  <files>.planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md only</files>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md, .planning/phases/05-injection-and-ownership-design/05-VERIFICATION.md, .planning/phases/05-injection-and-ownership-design/05-33-PLAN.md, .planning/phases/05-injection-and-ownership-design/05-33-SUMMARY.md, package.json, scripts/revalidation.mjs</read_first>
  <behavior>
    - "All 16 exact direct source-test pairs listed in the automated command pass 100% branch, function, and line coverage."
    - "TypeScript, repository ESLint, Fallow, corresponding-test, corresponding negative-control, direct-coverage negative-control, full unit, and full integration gates pass independently."
    - "The unmodified in-place canonical `npm run check` reaches `format:check`, exits nonzero only because it includes the untracked `.mcp.json`, and records that obstruction without converting it into a tracked repository failure."
    - "The `.mcp.json` hash is identical before and after all in-place commands, the file remains untracked, and no ignore/suppression entry is added (GD-05-02, GD-05-03)."
    - "Tracked implementation bytes remain identical to `2ef15332`, the sole Fallow line remains exact, and no source/test repair occurs (GD-05-04)."
  </behavior>
  <action>Run every direct pair and every independent gate named above exactly as written. Capture and print the in-place aggregate output and status. Classify only its locked `.mcp.json` format obstruction as expected under GD-05-02. Run every later member of the canonical check independently so the early aggregate stop cannot hide correspondence, negative-control, unit, or integration failures. Record exact commands and exit statuses for the final summary. Stop on any other failure and do not repair.</action>
  <verify>
    <automated>set -eu; tracking_base=2ef1533255a6b93aa42c622d73c272406b0fae9e; locked_mcp_sha=09237a8c19625daeb677211c6ca9b38e01aa12eede0f4b5f2c26ed6ec7b449e6; test -d node_modules &amp;&amp; ! git ls-files --error-unmatch .mcp.json &gt;/dev/null 2&gt;&amp;1 &amp;&amp; test "$(sha256sum .mcp.json | cut -d' ' -f1)" = "$locked_mcp_sha" &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/skills/unstage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/event-router.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/stage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/info.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/path-safety.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/runtime.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/completion-cache.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; { set +e; in_place_output="$(npm run check 2&gt;&amp;1)"; in_place_status=$?; set -e; } &amp;&amp; printf '%s\n' "$in_place_output" &amp;&amp; test "$in_place_status" -ne 0 &amp;&amp; printf '%s\n' "$in_place_output" | rg -Fq '.mcp.json' &amp;&amp; printf '%s\n' "$in_place_output" | rg -Fq 'format:check' &amp;&amp; npm run test:corresponding &amp;&amp; npm run test:corresponding:negative &amp;&amp; npm run test:coverage:direct:negative &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; test "$(sha256sum .mcp.json | cut -d' ' -f1)" = "$locked_mcp_sha" &amp;&amp; ! git ls-files --error-unmatch .mcp.json &gt;/dev/null 2&gt;&amp;1 &amp;&amp; git diff --quiet "$tracking_base"..HEAD -- extensions/pi-claude-marketplace tests scripts package.json package-lock.json tsconfig.json eslint.config.js .fallowrc.json .gitignore .prettierignore &amp;&amp; test "$(rg -Fxc '// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor' scripts/revalidation.mjs)" -eq 1</automated>
    <fails_when>Any direct pair is omitted or fails 100% branch/function/line coverage; typecheck, lint, Fallow, correspondence, either negative control, full unit, or full integration fails or discovers a skip/todo; the in-place aggregate unexpectedly passes, stops before format, or reports an obstruction other than `.mcp.json`; the file becomes tracked, changes hash, gains an ignore/suppression, or is edited; tracked implementation bytes drift; the exact Fallow line differs; a command is weakened or silenced; or the executor repairs source/tests.</fails_when>
  </verify>
  <acceptance_criteria>All 16 direct pairs and all independent gates are green, the exact in-place `.mcp.json` stop is recorded, and both the implementation tree and locked user-owned bytes remain unchanged (TREF-04, TREF-05, TREF-06; GD-05-02 through GD-05-04).</acceptance_criteria>
  <reversibility rating="reversible">The commands are read-only apart from tool-owned temporary files and the normal final summary.</reversibility>
  <done>Every gate hidden behind the in-place format stop has independent green evidence, with the workspace obstruction accurately recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Run canonical check on clean tracked HEAD and hand off independent verification</name>
  <precondition>Tasks 1 and 2 are green; `.mcp.json` is still untracked at the locked SHA-256; repository `node_modules` exists; and no tracked implementation path differs from `2ef15332`.</precondition>
  <files>.planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md only</files>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-VERIFICATION.md, .planning/phases/05-injection-and-ownership-design/05-33-SUMMARY.md, package.json</read_first>
  <behavior>
    - "The summary records the explicit `verify_command_path_resolvability` review as status `REVIEWED_DYNAMIC_TARGET` with reason `runtime_mktemp_child_awaits_worktree_add`; this is intentional because the target cannot exist until runtime creates its `mktemp` parent and adds the detached worktree."
    - "A detached worktree is created at one `mktemp`-owned `/tmp/phase05-34-check.*` path from the exact tracked HEAD recorded in the summary."
    - "The worktree contains no `.mcp.json`, has no Git-visible changes, links only its ignored `node_modules` path to the repository's installed dependencies, and runs the unmodified canonical `npm run check`."
    - "The canonical clean tracked command passes every type, lint, Fallow, format, correspondence, negative-control, unit, and integration stage."
    - "The exact temporary worktree is removed through `git worktree remove --force` on success and failure, its parent directory is empty and removed, and no worktree registration survives."
    - "The summary records the checked HEAD, clean status, absent untracked file, canonical result, cleanup result, unchanged implementation diff, unchanged `.mcp.json` hash, and the `$gsd-verify-work 05` rerun handoff."
  </behavior>
  <action>Treat `npm --prefix "$snapshot_root" run check` as an explicit reviewed exception to static verify-command path resolution: record probe status `REVIEWED_DYNAMIC_TARGET` and reason `runtime_mktemp_child_awaits_worktree_add` in the command output and summary. Create one detached temporary worktree from tracked HEAD. Assign `snapshot_root` once, as the readonly exact child `$snapshot_parent/tracked-head`, immediately after the `mktemp` parent is created; prove that equality and initial nonexistence before any filesystem, Git, or npm operation targets it. Install the cleanup trap before `git worktree add`. Link the existing repository dependency directory only at the ignored worktree `node_modules` path; do not install packages. Assert clean worktree status and absence of the user-owned file before running the canonical script. Let the trap remove only the exact registered worktree and its `mktemp` parent on every exit. After the green result, record the complete closure ledger in `05-34-SUMMARY.md` and hand it to a fresh independent Phase 05 verifier. Per GD-05-04, do not edit `05-VERIFICATION.md` or any other artifact yourself.</action>
  <verify>
    <automated>set -eu; project_root="$(git rev-parse --show-toplevel)"; tracking_base=2ef1533255a6b93aa42c622d73c272406b0fae9e; locked_mcp_sha=09237a8c19625daeb677211c6ca9b38e01aa12eede0f4b5f2c26ed6ec7b449e6; checked_head="$(git rev-parse HEAD)"; test -d "$project_root/node_modules" &amp;&amp; test "$(sha256sum "$project_root/.mcp.json" | cut -d' ' -f1)" = "$locked_mcp_sha" &amp;&amp; ! git -C "$project_root" ls-files --error-unmatch .mcp.json &gt;/dev/null 2&gt;&amp;1 &amp;&amp; git -C "$project_root" diff --quiet "$tracking_base"..HEAD -- extensions/pi-claude-marketplace tests scripts package.json package-lock.json tsconfig.json eslint.config.js .fallowrc.json .gitignore .prettierignore &amp;&amp; snapshot_parent="$(mktemp -d /tmp/phase05-34-check.XXXXXX)" &amp;&amp; readonly snapshot_parent &amp;&amp; test -d "$snapshot_parent" &amp;&amp; snapshot_root="$snapshot_parent/tracked-head" &amp;&amp; readonly snapshot_root &amp;&amp; test "$snapshot_root" = "$snapshot_parent/tracked-head" &amp;&amp; test ! -e "$snapshot_root" &amp;&amp; path_probe_status=REVIEWED_DYNAMIC_TARGET &amp;&amp; path_probe_reason=runtime_mktemp_child_awaits_worktree_add &amp;&amp; printf 'verify_command_path_resolvability status=%s reason=%s target=%s\n' "$path_probe_status" "$path_probe_reason" "$snapshot_root" &amp;&amp; cleanup_snapshot() { cleanup_status=$?; trap - EXIT HUP INT TERM; worktree_listing="$(git -C "$project_root" worktree list --porcelain)" || { cleanup_status=1; worktree_listing=""; }; if printf '%s\n' "$worktree_listing" | rg -Fqx "worktree $snapshot_root"; then git -C "$project_root" worktree remove --force "$snapshot_root" || cleanup_status=1; fi; if test -e "$snapshot_root"; then cleanup_status=1; fi; if test -d "$snapshot_parent"; then rmdir "$snapshot_parent" || cleanup_status=1; fi; exit "$cleanup_status"; } &amp;&amp; trap cleanup_snapshot EXIT HUP INT TERM &amp;&amp; git -C "$project_root" worktree add --detach "$snapshot_root" "$checked_head" &amp;&amp; test ! -e "$snapshot_root/.mcp.json" &amp;&amp; ln -s "$project_root/node_modules" "$snapshot_root/node_modules" &amp;&amp; test -z "$(git -C "$snapshot_root" status --porcelain --untracked-files=all)" &amp;&amp; test "$(git -C "$snapshot_root" rev-parse HEAD)" = "$checked_head" &amp;&amp; npm --prefix "$snapshot_root" run check &amp;&amp; test "$(sha256sum "$project_root/.mcp.json" | cut -d' ' -f1)" = "$locked_mcp_sha" &amp;&amp; ! git -C "$project_root" ls-files --error-unmatch .mcp.json &gt;/dev/null 2&gt;&amp;1 &amp;&amp; git -C "$project_root" diff --quiet "$tracking_base"..HEAD -- extensions/pi-claude-marketplace tests scripts package.json package-lock.json tsconfig.json eslint.config.js .fallowrc.json .gitignore .prettierignore</automated>
    <fails_when>The explicit path-resolvability review is absent or records any status/reason other than `REVIEWED_DYNAMIC_TARGET` / `runtime_mktemp_child_awaits_worktree_add`; `snapshot_root` is assigned anywhere except once as the readonly exact child `$snapshot_parent/tracked-head`, is used as a filesystem/Git/npm target before that proof, or is later reassigned; the temporary parent is not an exact `mktemp` path under `/tmp`; worktree HEAD differs from the recorded tracked HEAD; `.mcp.json` appears in the worktree, becomes tracked, or changes hash; the dependency link points outside the repository's existing `node_modules`; Git reports a worktree change; canonical `npm run check` fails or omits a stage; a package is installed; cleanup targets any path other than the exact temporary worktree, fails, or leaves a registration/directory; tracked implementation bytes drift; the summary omits a command/result/manifest/handoff; the executor edits `05-VERIFICATION.md`; or any source/test repair occurs.</fails_when>
  </verify>
  <acceptance_criteria>The unmodified canonical repository check is green against clean tracked HEAD without `.mcp.json`; the exact temporary worktree is removed; locked workspace and implementation bytes remain unchanged; only `05-34-SUMMARY.md` is created; and the independent `$gsd-verify-work 05` rerun handoff is explicit (TREF-04, TREF-05, TREF-06; D-05-01 through D-05-09; GD-05-01 through GD-05-04).</acceptance_criteria>
  <reversibility rating="reversible">The detached worktree is temporary, names one exact commit, and is removed on every exit without changing tracked data.</reversibility>
  <done>Phase 05 has a complete evidence-only closure record ready for fresh independent verification.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                                 | Description                                                                                                |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Revalidation and executed summaries -> closure manifests | Stale counts, shortened IDs, or incomplete attribution can misclassify Phase 5 work as a Phase 6 campaign. |
| Untracked workspace -> tracked repository gate           | User-owned `.mcp.json` can obstruct an in-place format scan without representing tracked HEAD.             |
| Main repository -> detached temporary worktree           | A wrong commit, dependency path, or cleanup target can test the wrong tree or damage unrelated state.      |
| Gate output -> `05-34-SUMMARY.md`                        | Omitted, weakened, or reclassified failures can falsely claim closure.                                     |

## STRIDE threat register

Security enforcement uses ASVS Level 1. All high-severity threats below are mitigated before closure.

| Threat ID  | Category                                        | Component                                      | Severity | Disposition | Mitigation plan                                                                                                                                                      |
| ---------- | ----------------------------------------------- | ---------------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-34-01 | Tampering / Repudiation                         | Eleven-root, two-exception, and port manifests | high     | mitigate    | Reconcile exact full IDs and owner bindings against the ledger, cited summaries, CodeGraph, focused public tests, and direct pairs.                                  |
| T-05-34-02 | Tampering                                       | Phase 6 patch/split handoff                    | high     | mitigate    | Assert the exact 13/85 and 9/9 per-file manifests, all three fixed-commit deltas, the sole added runtime pair, and unchanged excluded skills-stage test.             |
| T-05-34-03 | Elevation of privilege / Information disclosure | Terminal seams and injected capabilities       | high     | mitigate    | Prove the exact terminal/bounded manifest absent, production diff free of new `__deps`/`ForTest` seams, real adapters live, and Fallow green.                        |
| T-05-34-04 | Tampering / Repudiation                         | User-owned `.mcp.json`                         | high     | mitigate    | Require untracked status and the locked SHA-256 before and after every gate; do not edit, ignore, copy, or suppress the file.                                        |
| T-05-34-05 | Tampering / Denial of service                   | Detached worktree and cleanup                  | high     | mitigate    | Resolve exact HEAD, create beneath one `mktemp` parent, assert clean status, use no install, and trap `git worktree remove` plus parent `rmdir` for the exact path.  |
| T-05-34-06 | Repudiation                                     | Verification ledger                            | high     | mitigate    | Record every exact command, status, expected in-place stop, independent result, clean canonical result, cleanup proof, and implementation diff before rerun handoff. |
| T-05-34-SC | Tampering                                       | Package supply chain                           | high     | mitigate    | Install no package; use only the repository's existing `node_modules`, Node runtime, npm scripts, Git, and core shell tools.                                         |

ASVS Level 1 disposition: every primary trust boundary and every high-impact component has a concrete blocking mitigation and automated evidence.
</threat_model>

## Multi-source coverage audit

| Source           | ID                         | Feature or constraint                                                                                         | Plan/task         | Status  | Notes                                                                  |
| ---------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------- | ------- | ---------------------------------------------------------------------- |
| GOAL             | -                          | Legitimate seams, correctly owned runtime state, and public contracts                                         | 05-34 / Tasks 1-3 | COVERED | Existing implementation is re-proved without repair.                   |
| REQ              | TREF-04                    | Eleven classified hidden dependencies and two real-child exceptions                                           | 05-34 / Tasks 1-2 | COVERED | Exact manifests, focused suites, and all direct pairs.                 |
| REQ              | TREF-05                    | Lifecycle/factory ownership without test-only resets                                                          | 05-34 / Tasks 1-3 | COVERED | Terminal/static absence, owner liveness, Fallow, and clean full gate.  |
| REQ              | TREF-06                    | Public contracts and trace-preserving terminal removal                                                        | 05-34 / Tasks 1-3 | COVERED | Exception evidence, static absence, focused/direct/full gates.         |
| RESEARCH         | Eleven-root classification | Real temporary trees or narrow consumer-owned production ports                                                | 05-34 / Task 1    | COVERED | All eleven exact root rows and owner bindings retained.                |
| RESEARCH         | Validation strategy        | Focused, direct, type, lint, format, Fallow, correspondence, negative, unit, integration, and canonical gates | 05-34 / Tasks 1-3 | COVERED | In-place obstruction is recorded; tracked HEAD passes canonical check. |
| RESEARCH         | Phase 5/6 boundary         | Retain patch machinery and defer seven split families                                                         | 05-34 / Task 1    | COVERED | Reviewed 13/85 plus 9/9 handoff and exact added-pair census.           |
| CONTEXT          | D-05-01                    | Eleven roots use real trees or the smallest legitimate live port                                              | 05-34 / Tasks 1-2 | COVERED | Exact root/port manifest and behavior evidence.                        |
| CONTEXT          | D-05-02                    | Exactly two behavioral compositions                                                                           | 05-34 / Task 1    | COVERED | Apply and bootstrap only.                                              |
| CONTEXT          | D-05-03                    | Phase 6 owns builtin-patch removal                                                                            | 05-34 / Task 1    | COVERED | No restoration or broad removal; corrected inventory ratified.         |
| CONTEXT          | D-05-04                    | One HooksRuntime per lifecycle                                                                                | 05-34 / Tasks 1-3 | COVERED | Lifecycle focused/full proof and exact event-router attribution.       |
| CONTEXT          | D-05-05                    | One CompletionCache per composition owner                                                                     | 05-34 / Tasks 1-3 | COVERED | Cache owner focused/direct/full proof.                                 |
| CONTEXT          | D-05-06                    | Only production lifecycle behavior clears state                                                               | 05-34 / Task 1    | COVERED | Terminal/replacement seam manifest is absent.                          |
| CONTEXT          | D-05-07                    | Trace-preserving removal of terminal test-shaped surfaces                                                     | 05-34 / Task 1    | COVERED | Exact terminal and bounded-transition static manifest.                 |
| CONTEXT          | D-05-08                    | Private `BOOLEAN_FLAGS` with independent public/catalog proof                                                 | 05-34 / Tasks 1-2 | COVERED | Focused owner plus direct list pair.                                   |
| CONTEXT          | D-05-09                    | Seven large split families stay in Phase 6                                                                    | 05-34 / Task 1    | COVERED | Only the runtime pair was added; info/uninstall boundaries retained.   |
| VERIFICATION GAP | Patch inventory            | Replace stale 14/83 with attributed 13/85 and retained 9/9                                                    | 05-34 / Task 1    | COVERED | GD-05-01 is explicit and fail-closed.                                  |
| VERIFICATION GAP | Repository gate            | Preserve `.mcp.json`, record in-place stop, and prove clean tracked HEAD                                      | 05-34 / Tasks 2-3 | COVERED | GD-05-02 uses an exact temporary worktree and canonical script.        |
| GAP DECISION     | GD-05-03                   | Sole exact Fallow line and zero new suppression                                                               | 05-34 / Tasks 1-3 | COVERED | Static diff and full gates.                                            |
| GAP DECISION     | GD-05-04                   | Summary-only evidence; genuine failure halts                                                                  | 05-34 / Tasks 1-3 | COVERED | No implementation or historical artifact is writable.                  |

Deferred Phase 6 builtin removal and module splits, the excluded info split, PID-table redesign, stale documentation counts, and unrelated stage work remain explicit non-gaps and receive no implementation task.

<verification>
Run all three task commands without weakening. Confirm that `05-34-SUMMARY.md` records the exact eleven-root, two-exception, terminal-seam, port/static, 13/85, 9/9, three-delta, suppression, focused, 16-direct-pair, independent-gate, in-place-stop, clean-worktree canonical, cleanup, and immutable-byte evidence. Explicitly review the intentional dynamic verify target: the deterministic probe must record status `REVIEWED_DYNAMIC_TARGET`, reason `runtime_mktemp_child_awaits_worktree_add`, and the proof that readonly `snapshot_root` was assigned exactly as `$snapshot_parent/tracked-head` before any target use. Confirm that no artifact other than `05-34-SUMMARY.md` was created or changed by the executor. Then hand off to a fresh independent verifier with `$gsd-verify-work 05`; the verifier, not this executor, owns any update to `05-VERIFICATION.md`.
</verification>

<success_criteria>

- The stale 14-file/83-call condition is superseded by the exact reviewed 13-file/85-call manifest and three required Phase 5 deltas, while `createRequire` remains 9 files/9 calls.
- All eleven root classifications, both real-child exceptions, every terminal/static absence, and every narrow production port remain proven under TREF-04, TREF-05, TREF-06, and D-05-01 through D-05-09.
- Focused tests, all 16 direct pairs, typecheck, lint, Fallow, correspondence, both negative controls, full unit, and full integration gates pass.
- The in-place canonical stop names only the user-owned untracked `.mcp.json`, whose exact hash and untracked status remain unchanged.
- Canonical `npm run check` passes against clean tracked HEAD without `.mcp.json`, and the exact temporary worktree is removed on every exit.
- The executor creates only `05-34-SUMMARY.md`, performs no repair, and supplies a complete independent Phase 05 verification rerun handoff.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-34-SUMMARY.md` when done. Do not create or edit any other persistent artifact.
</output>
