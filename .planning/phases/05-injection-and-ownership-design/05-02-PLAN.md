---
phase: 05-injection-and-ownership-design
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
  - extensions/pi-claude-marketplace/bridges/hooks/index.ts
  - extensions/pi-claude-marketplace/index.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/bridges/hooks/index.test.ts
  - tests/index.test.ts
  - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
  - tests/bridges/hooks/stage.test.ts
autonomous: true
requirements: [TREF-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "HHD-011 has exactly one terminal classification: a required hooks-owned state reader serves factory, project, and lazy hydration paths (D-05-01)."
    - "HSA-026 has exactly one terminal classification: a required four-operation hooks tree inspector controls only irreproducible staging inspection faults (D-05-01)."
    - "Hydration and staging keep separate capabilities because their operations, consumers, and lifetimes differ."
    - "Production composition supplies explicit required Node adapters; neither consumer selects a default or carries a test-only/dead seam."
    - "Hydration order, cache replacement, diagnostics, path containment, no-follow traversal, write bytes, and exact errors remain unchanged."
    - "Phase 6 retains builtin-module patch removal, and the existing Fallow line remains byte-exact (D-05-03)."
  artifacts:
    - path: extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
      provides: "Required hydration-reader contract and production hydration flow"
    - path: extensions/pi-claude-marketplace/bridges/hooks/stage.ts
      provides: "Required four-operation staging inspector and Node composition"
    - path: tests/bridges/hooks/event-router.test.ts
      provides: "Hydration order, failure, and forwarding evidence"
    - path: tests/bridges/hooks/stage.test.ts
      provides: "Real-tree containment and controlled inspection-fault evidence"
  key_links:
    - from: extensions/pi-claude-marketplace/index.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
      via: "one required hydration reader passed to registration and project hydration"
    - from: extensions/pi-claude-marketplace/bridges/hooks/stage.ts
      to: extensions/pi-claude-marketplace/shared/path-safety.ts
      via: "inspector-driven no-follow walk before the existing path guard and atomic write"
---

<objective>
Classify HHD-011 and HSA-026 through separate, required production capabilities while preserving hooks hydration and staging behavior (D-05-01, D-05-03).

Purpose: Replace two authorized hidden filesystem controls without widening either consumer or crossing the Phase 6 cleanup boundary.
Output: Required hooks hydration and staging contracts, explicit Node adapters, and exact public owner evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md
</context>

## Edge assumptions

- Plan 05-01 supplies the required-adapter and real-tree pattern. Reuse the executed names from its summary when they differ from research recommendations.
- `loadState` remains the semantic hydration operation. The reader does not own route parsing, cache replacement, diagnostics, or lifecycle state.
- The staging inspector contains only `readlink`, `realpath`, `readdir`, and `lstat`. Atomic writes, removals, name checks, and containment policy stay outside it.
- Plan 05-09 binds hydration to `HooksRuntime`; this plan makes the reader required now without moving routing state early.
- Phase 6 removes the retained `createRequire` and `syncBuiltinESMExports` machinery. This plan must not edit `tests/bridges/skills/stage.test.ts`.

## Artifacts this plan produces

| File family                                                               | Contract or proof                                                                                                                    |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `bridges/hooks/{event-router,index}.ts`, extension `index.ts`, and owners | `HooksHydrationReader`, explicit Node adapter, and one required value across all current hydration entry points.                     |
| `bridges/hooks/stage.ts` and owner                                        | `HooksTreeInspector`, required writer factory, explicit Node adapter, and controlled failure/order proofs beside real-tree behavior. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one required reader through every hooks hydration path</name>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/bridges/hooks/index.ts, extensions/pi-claude-marketplace/index.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/index.test.ts, tests/index.test.ts, extensions/pi-claude-marketplace/persistence/state-io.ts, .planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/bridges/hooks/index.ts, extensions/pi-claude-marketplace/index.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/index.test.ts, tests/index.test.ts</files>
  <behavior>
    - "The hydration reader exposes only the current state-load operation and preserves its exact result and error contract."
    - "Factory registration, resources-discover project hydration, and captured lazy SessionStart hydration receive the same required production reader."
    - "Factory hydration reads user before project; each failed read preserves default-state degradation and the exact debug diagnostic."
    - "Project hydration removes only the old project prefix before its read and leaves user entries intact on project failure."
  </behavior>
  <action>Per D-05-01, define a readonly hooks-owned hydration reader and an explicit Node-backed adapter around the existing state load. Require the reader in `registerHooksBridge` options and `hydrateProjectScopeForCwd`, thread it through private hydration helpers, and capture the same value in the lazy session-start route. Re-export the genuine contract through the hooks barrel. Select the Node adapter once in `claudeMarketplaceExtension` and pass it to factory-time registration plus resources discovery. Add case-local readers only for HHD-011 read order, selected failure, and cache-replacement cases. Retain real temporary state files for ordinary behavior. Assert cache contents, exact roots, registration sequence, diagnostics, and public callback behavior before call logs. Leave routing ownership to Plans 05-06 and 05-09. Retain all Phase 6 patch machinery per D-05-03.</action>
  <verify><automated>node --test tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/index.test.ts tests/index.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/event-router.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/index.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/bridges/hooks/event-router.ts extensions/pi-claude-marketplace/bridges/hooks/index.ts extensions/pi-claude-marketplace/index.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/index.test.ts tests/index.test.ts --max-warnings=0</automated><fails_when>A suite discovers zero tests or fails; a hydration path selects another reader; the reader is optional or broader than state loading; production bypasses it; order, cache replacement, diagnostics, registrations, or stale-callback behavior changes; or a test-only/default/dead surface appears.</fails_when></verify>
  <acceptance_criteria>HHD-011 is classified exactly once, all production hydration routes receive one required real reader, and exact state, order, and diagnostic behavior remains public (D-05-01).</acceptance_criteria>
  <reversibility rating="costly">D-05-03 and Plan 05-09 carry this required reader into runtime composition, so contract churn would retouch edge, callback, and owner callers.</reversibility>
  <done>The production hydration path uses one explicit reader from extension composition through state load and route-cache effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind hook staging to its four-operation tree inspector</name>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts, extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts, .planning/phases/05-injection-and-ownership-design/05-PATTERNS.md</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts</files>
  <behavior>
    - "The required staging inspector exposes exactly readdir, lstat, realpath, and readlink, with an explicit Node-backed production binding."
    - "The writer validates names, walks without following links, resolves detected links, applies path safety, then writes atomically in the existing order."
    - "ENOENT and ENOTDIR directory reads remain skips; selected inspection failures retain identity; unreadable link diagnostics retain the exact fallback."
    - "Ordinary writes, nested trees, contained and escaping links, removal, exact results, and unrelated bytes stay on real case-owned filesystems."
  </behavior>
  <action>Per D-05-01, add a readonly hooks tree inspector with only the four HSA-026 operations and require it in the hook-config writer factory. Capture it through the subtree walk and symlink diagnostic helpers. Keep `assertSafeName`, Plan 05-01 path safety, `atomicWriteJson`, and removal outside the inspector. Compose the existing `writeHookConfig` export from one explicit Node adapter so production traverses the factory. Use case-local inspectors only for the authorized readlink, realpath, readdir, and lstat failure/order cases. Keep ordinary staging on temporary trees and retain exact path, tree, error, and diagnostic assertions. Do not edit the skills-stage suite or remove builtin synchronization code per D-05-03.</action>
  <verify><automated>node --test tests/bridges/hooks/stage.test.ts tests/shared/path-safety.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/stage.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/bridges/hooks/stage.ts tests/bridges/hooks/stage.test.ts --max-warnings=0</automated><fails_when>A suite discovers zero tests or fails; the inspector includes writes, removals, or policy; it is optional, shared with another root, dead, or bypassed by production; traversal, containment, skip behavior, diagnostics, errors, or bytes change; or excluded Phase 6 work appears.</fails_when></verify>
  <acceptance_criteria>HSA-026 is classified exactly once, all four controlled operations traverse the production writer, and real containment plus write behavior remains exact (D-05-01, D-05-03).</acceptance_criteria>
  <reversibility rating="costly">Phase 6 consumes this contract when it removes global patching, so widening or renaming it requires coordinated production and owner migration.</reversibility>
  <done>Hooks staging has one narrow production-owned inspector and no general filesystem or test-only seam.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                         |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Persisted extension state to hydration | Stored paths and records enter route parsing only through current validation and degradation behavior.              |
| Plugin hooks tree to staging writer    | Directory entries and link targets cross containment checks before bytes reach managed hook storage.                |
| Required capability to production flow | A substitute can force an authorized read fault but cannot control validation, rendering, writes, or unrelated I/O. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                  | Severity | Disposition | Mitigation Plan                                                                                                                            |
| --------- | ---------------------------------- | -------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-06   | Tampering / Elevation of privilege | hooks hydration            | high     | mitigate    | Keep path branding, parsing, and cache decisions outside the reader; prove corrupt and outside records are skipped with exact diagnostics. |
| T-05-07   | Tampering / Elevation of privilege | staging subtree walk       | high     | mitigate    | Limit the inspector to four read operations, never follow links during traversal, and retain real path-safety and foreign-byte tests.      |
| T-05-08   | Tampering                          | production adapter binding | medium   | mitigate    | Require both capabilities, bind explicit Node adapters, and use direct-pair plus forwarding tests to detect bypass.                        |
| T-05-09   | Repudiation                        | diagnostics and order      | low      | mitigate    | Preserve exact read order, debug output, errors, paths, and tree results in owner assertions.                                              |

ASVS Level 1: both high-severity path/state threats block completion until focused real-tree and owner tests pass. This plan adds no package or external service.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                                               | Plan/task         | Status  | Notes                                                                      |
| -------- | ------------------------- | ----------------------------------------------------------------------------------- | ----------------- | ------- | -------------------------------------------------------------------------- |
| GOAL     | -                         | Legitimate test seams with public contracts                                         | 05-02 / Tasks 1-2 | COVERED | Two required production capabilities replace only classified hidden reads. |
| REQ      | TREF-04                   | Classify hidden dependencies and keep ordinary behavior real                        | 05-02 / Tasks 1-2 | COVERED | HHD-011 and HSA-026 each receive one separate classification.              |
| RESEARCH | eleven-root table         | Hydration reader and four-operation staging inspector                               | 05-02 / Tasks 1-2 | COVERED | The two rows retain their exact owners and do not authorize another root.  |
| RESEARCH | path and assertion safety | Preserve containment, order, diagnostics, and public effects                        | 05-02 / Tasks 1-2 | COVERED | Owner commands cover real trees and controlled faults.                     |
| CONTEXT  | D-05-01                   | Ports are narrow, required, production-owned, and reserved for irreproducible cases | 05-02 / Tasks 1-2 | COVERED | Each consumer owns one explicit real adapter.                              |
| CONTEXT  | D-05-03                   | Phase 6 owns builtin patch removal                                                  | 05-02 / Tasks 1-2 | COVERED | Existing patch machinery and excluded skills-stage suite remain intact.    |

Other root classifications are assigned exactly once to Plans 05-01 and 05-03 through 05-05. Mutable-state ownership, public reset cleanup, the two composition proofs, and final audits remain assigned to later outline rows. Deferred Phase 6 splits and patch removal are exclusions, not gaps.

<verification>
Run both task commands and `npm run check`. Inspect the scoped diff for exactly the eight frontmatter files. Confirm two separate required adapters, no added suppression, unchanged `tests/bridges/skills/stage.test.ts`, retained Phase 6 patch machinery, and the exact existing line `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` in `scripts/revalidation.mjs`.
</verification>

<success_criteria>

- HHD-011 and HSA-026 each have one required consumer-owned classification.
- Production and controlled cases traverse the same hydration or staging flow.
- State effects, containment, no-follow traversal, exact errors, diagnostics, bytes, and operation order remain unchanged.
- No optional/default/test-only/dead seam, new suppression, excluded-file edit, Phase 6 patch removal, or large-module split appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-02-SUMMARY.md` when done.
</output>
