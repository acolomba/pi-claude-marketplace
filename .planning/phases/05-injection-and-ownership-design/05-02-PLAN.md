---
phase: 05-injection-and-ownership-design
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
  - extensions/pi-claude-marketplace/bridges/hooks/index.ts
  - extensions/pi-claude-marketplace/index.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/bridges/hooks/index.test.ts
  - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
  - tests/bridges/hooks/stage.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
  - tests/orchestrators/plugin/fetch.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/orchestrators/plugin/info.test.ts
autonomous: true
requirements: [TREF-04]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "HHD-011 is classified exactly once as a hooks-hydration state reader shared only by factory-time and project-scope hydration, which have the same state-read meaning and extension lifetime (D-05-01)."
    - "HSA-026 is classified exactly once as a hooks-staging tree inspector whose readlink, realpath, readdir, and lstat operations preserve the real containment walk and diagnostics (D-05-01)."
    - "OPEF-F04 is classified exactly once as a fetch-owned status probe that preserves pre-materialization, materialization, and fresh post-materialization order (D-05-01)."
    - "OPIC-F27 is classified exactly once as an info-owned read/list contract; ordinary missing, corrupt, containment, and component-discovery behavior stays on case-owned temporary trees (D-05-01)."
    - "No capability is shared across hydration, staging, fetch, and info because their consumers, meanings, and lifetimes differ; each production path supplies an explicit real adapter."
    - "Exact notifications, debug diagnostics, typed errors, containment decisions, state/cache effects, filesystem bytes, and observable operation order remain intact."
    - "The existing createRequire/syncBuiltinESMExports machinery and the byte-exact temporary Fallow suppression remain for Phase 6 (D-05-03)."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
    - extensions/pi-claude-marketplace/bridges/hooks/index.ts
    - extensions/pi-claude-marketplace/index.ts
    - tests/bridges/hooks/event-router.test.ts
    - tests/bridges/hooks/index.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
    - tests/bridges/hooks/stage.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
    - tests/orchestrators/plugin/fetch.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
    - tests/orchestrators/plugin/info.test.ts
  key_links:
    - "claudeMarketplaceExtension -> NODE_HOOKS_HYDRATION_READER -> registerHooksBridge/hydrateProjectScopeForCwd -> loadState"
    - "writeHookConfig -> createHookConfigWriter -> HooksTreeInspector -> ordered containment walk"
    - "fetchPlugins -> createFetchPlugins -> FetchStatusProbe -> pre/post materialization classification -> notification"
    - "getPluginInfo -> createGetPluginInfo -> PluginInfoReader -> contained reads/listing -> exact inventory notification"
  prohibitions:
    - statement: "Do not introduce test-only exports, reset hooks, __deps, optional real-adapter fallbacks, unused defaults, or dead ports."
      source: "Phase 5 specless-probe fallback"
      status: flagged-unverified
    - statement: "Do not weaken applyReconcile or bootstrapClaudePlugin composition proofs into interaction-only assertions or broaden the exception beyond those two flows."
      source: "Phase 5 specless-probe fallback"
      status: flagged-unverified
    - statement: "Do not execute Phase 6 global-patch removal or large-module splits, and do not touch bridges/skills/stage.test.ts, PID-table behavior, the info split, or uninstall cohesion."
      source: "Phase 5 specless-probe fallback"
      status: flagged-unverified
---

<objective>
Classify and wire hooks hydration, hooks staging, plugin-fetch status, and plugin-info read/list as separate consumer-owned capabilities (D-05-01, D-05-03).

Purpose: Complete four TREF-04 roots without creating a generic filesystem abstraction, changing ordinary real-tree tests, or crossing the Phase 5/6 cleanup boundary.
Output: Four required capability contracts, visible production adapter wiring, and owner proofs for forwarding, order, containment, diagnostics, results, and notifications.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

| File                                               | Symbols or proofs introduced by this plan                                                                                                                                                                                 |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bridges/hooks/event-router.ts`                    | Exported readonly `HooksHydrationReader` and `NODE_HOOKS_HYDRATION_READER`; required reader parameters on `registerHooksBridge` and `hydrateProjectScopeForCwd`; private reader threading through `hydrateCacheFromDisk`. |
| `bridges/hooks/index.ts`                           | Public forwarding of the hydration reader type/value alongside the existing bridge entrypoints.                                                                                                                           |
| `index.ts`                                         | One production hydration-reader value forwarded to factory-time, resources-discover, and lazy session-start hydration paths.                                                                                              |
| `tests/bridges/hooks/{event-router,index}.test.ts` | State-read sequence, same-adapter forwarding, degradation diagnostics, cache replacement, and barrel-contract proofs.                                                                                                     |
| `bridges/hooks/stage.ts`                           | Exported readonly `HooksTreeInspector` and `createHookConfigWriter(inspector)`; private Node inspector; existing `writeHookConfig` composed from the factory.                                                             |
| `tests/bridges/hooks/stage.test.ts`                | Inspector order/failure forwarding plus unchanged containment, symlink, diagnostic, and real-tree proofs.                                                                                                                 |
| `orchestrators/plugin/fetch.ts`                    | Exported readonly `FetchStatusProbe` and `createFetchPlugins(statusProbe)`; private real status adapter; existing `fetchPlugins` composed from the factory.                                                               |
| `tests/orchestrators/plugin/fetch.test.ts`         | Exact pre-probe/materialize/post-probe forwarding and notification/tree assertions.                                                                                                                                       |
| `orchestrators/plugin/info.ts`                     | Exported readonly `PluginInfoReader` and `createGetPluginInfo(reader)`; private real read/list adapter; existing `getPluginInfo` composed from the factory.                                                               |
| `tests/orchestrators/plugin/info.test.ts`          | Targeted read/list failure forwarding with exact reason order, notification bytes, and containment proofs.                                                                                                                |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require one state reader across every hooks hydration path</name>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/bridges/hooks/index.ts, extensions/pi-claude-marketplace/index.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/index.test.ts, tests/index.test.ts, extensions/pi-claude-marketplace/persistence/state-io.ts, .planning/phases/05-injection-and-ownership-design/05-RESEARCH.md</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/bridges/hooks/index.ts, extensions/pi-claude-marketplace/index.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/index.test.ts</files>
  <behavior>
    - "`HooksHydrationReader` exposes only `loadState(extensionRoot)` with the current `loadState` result/error contract; `NODE_HOOKS_HYDRATION_READER` delegates to the real persistence operation."
    - "`registerHooksBridge` and `hydrateProjectScopeForCwd` require the reader, and one production value is forwarded through factory-time user/project hydration, resources-discover project hydration, and the captured session-start lazy hydration path."
    - "Factory hydration reads user then project state, degrades each failed read to the existing default-state/debug behavior, and continues rebuilding in the current order."
    - "Project hydration removes only the prior project cache prefix before its read; a failed read leaves user cache intact and emits the current diagnostic without registering stale project entries."
  </behavior>
  <action>Per D-05-01, define the readonly `HooksHydrationReader` around the semantic `loadState(extensionRoot)` operation and export the explicit `NODE_HOOKS_HYDRATION_READER` used by production. Make the reader required in `registerHooksBridge` options and as the second argument of `hydrateProjectScopeForCwd`; thread it through private `hydrateCacheFromDisk` and capture that same value in the session-start callback. Re-export the contract/value through the hooks barrel. In `claudeMarketplaceExtension`, select the Node reader once and pass it to initial registration and resources-discover so the later callback and explicit project hydration share one capability with one extension lifetime. Update owner callers to pass the real adapter and add case-local state readers only for HHD-011 sequencing/failure cases. Assert user-before-project reads, exact extension roots, cache contents, registration order, debug strings, and production forwarding before reader call logs. Retain real saved-state/temp-tree cases and all stale-callback generation behavior; Plan 05-04, not this task, owns runtime-state movement.</action>
  <verify><automated>node --test tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/index.test.ts tests/index.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/event-router.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/index.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/bridges/hooks/event-router.ts extensions/pi-claude-marketplace/bridges/hooks/index.ts extensions/pi-claude-marketplace/index.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/index.test.ts</automated><fails_when>Any owner suite fails or discovers zero tests; a hydration path imports or selects its own reader, the reader is optional or broader than state load, factory/project/session-start order or cache replacement changes, exact diagnostics/registrations/state effects weaken, stale callback behavior changes, or a reset/test-only/dead surface or new suppression is added.</fails_when></verify>
  <acceptance_criteria>HHD-011 has one required hooks-owned state-reader classification, the edge supplies one real adapter to all three production hydration routes, and owner tests preserve exact order, state effects, and diagnostics.</acceptance_criteria>
  <reversibility rating="costly">The required reader will be carried into the hooks runtime composition in D-05-04, so later signature churn would require coordinated edge, callback, and owner-test migration.</reversibility>
  <done>Hooks hydration exposes a real production seam without changing lifecycle state ownership ahead of its dedicated plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind hook staging to its own four-operation tree inspector</name>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts, extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts, .planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-PATTERNS.md</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts</files>
  <behavior>
    - "`HooksTreeInspector` contains exactly readdir, lstat, realpath, and readlink capabilities; `createHookConfigWriter(inspector)` requires it and the production `writeHookConfig` export uses an explicit Node-backed inspector."
    - "The writer validates the plugin name, walks the hooks subtree without following links, resolves a detected link and root, applies the real path-safety guard, then writes atomically in the existing order."
    - "ENOENT/ENOTDIR directory reads remain clean skips; targeted readdir/lstat/realpath failures retain their thrown identity, and a readlink diagnostic failure still records `<unreadable>` without hiding the symlink refusal."
    - "Ordinary writes, nested directories, in-tree links, escaping links, and `removeHookConfig` remain real case-owned filesystem behavior with exact result/path/tree/error assertions."
  </behavior>
  <action>Per D-05-01, add the readonly `HooksTreeInspector` with only the four HSA-026 inspection operations and make `createHookConfigWriter(inspector)` require it. Capture the inspector through the existing subtree walk, one-level directory read, symlink resolution, and safe link-target diagnostic helpers; keep `assertSafeName`, Phase 05-01's production path guard, `atomicWriteJson`, and `rm` outside this port. Compose the existing `writeHookConfig` export from one private Node-backed inspector so current production callers use the factory path without signature churn. Add narrow owner cases that inject only the irreproducible readlink/realpath/readdir/lstat faults and assert operation order, exact thrown value or diagnostic fallback, containment error fields, and unchanged tree bytes. Keep ordinary cases on the real writer and temporary tree. Preserve all existing global-patch imports/calls for D-05-03 and do not edit `tests/bridges/skills/stage.test.ts`.</action>
  <verify><automated>node --test tests/bridges/hooks/stage.test.ts tests/shared/path-safety.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/stage.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/bridges/hooks/stage.ts tests/bridges/hooks/stage.test.ts</automated><fails_when>Either suite fails or discovers zero tests; the port includes writes/removal/path policy, is optional, is reused by another owner, or production bypasses it; traversal, containment, ENOENT/ENOTDIR, diagnostics, exact errors, write result/tree bytes, or operation order changes; the excluded skills stage suite changes; Phase 6 patch machinery is removed; or a new suppression appears.</fails_when></verify>
  <acceptance_criteria>HSA-026 has one required hooks-staging inspector classification, all four authorized fault operations traverse the production writer flow, and containment plus exact diagnostics remain proven on real and controlled paths.</acceptance_criteria>
  <reversibility rating="costly">D-05-03 makes this inspector the stable seam Phase 6 will use when deleting global patching, so renaming or widening it later would migrate both production composition and its owner suite.</reversibility>
  <done>Hooks staging has a narrow inspector without acquiring a general filesystem dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Separate fetch status probing from plugin-info reads and lists</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, tests/orchestrators/plugin/fetch.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts, extensions/pi-claude-marketplace/shared/path-safety.ts, .planning/phases/05-injection-and-ownership-design/05-RESEARCH.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, tests/orchestrators/plugin/fetch.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts</files>
  <behavior>
    - "`FetchStatusProbe` owns only git-source presence and manifest-entry classification; `createFetchPlugins(statusProbe)` requires it and the production `fetchPlugins` export binds the existing real probe operations."
    - "Pinned fetch order remains presence probe -> materialize only when cold -> fresh manifest classification -> reason probe when needed -> exact notification; a cache that becomes visible between probes renders from the fresh result."
    - "`PluginInfoReader` owns only UTF-8 text read and directory-entry list operations; `createGetPluginInfo(reader)` requires it and the production `getPluginInfo` export binds real Node operations."
    - "Info preserves containment before reads, ENOENT/ENOTDIR collapse rules, non-benign failure classification, exact reason ordering, component order, notification bytes, fetch behavior, and network prohibition for bare info."
  </behavior>
  <action>Implement two independent consumer contracts per D-05-01. In `fetch.ts`, define readonly `FetchStatusProbe` methods for the current `makePresenceProbe(locations)(source)` and `probeManifestEntry(entry, marketplaceRoot, locations)` semantics, then make `createFetchPlugins(statusProbe)` require that capability and thread it through the no-op gate, fresh row, and reason-bearing resolution. Compose `fetchPlugins` from a private real adapter. Add an OPEF-F04 owner case whose case-local status probe records pre/materialize/post order and changes classification after the real case-owned tree is materialized; preserve exact notification, credential, staging-tree, and clone-tree assertions. In `info.ts`, define readonly `PluginInfoReader` operations `readText(path)` and `listEntries(path)` matching the current UTF-8/Dirent behavior; make `createGetPluginInfo(reader)` require it, thread it only through the existing direct info read/list helpers, and compose `getPluginInfo` from a private real adapter. Add OPIC-F27 owner cases for targeted read and list failures while retaining exact public notifications, reason order, debug diagnostics, containment, hook summary, and component inventory assertions. Do not merge the fetch probe with the info reader: their meanings and lifetimes differ. Do not widen either factory into an orchestrator dependency bag, alter existing clone/auth boundaries, introduce network into bare info, or split `info.ts`. Keep existing global-patch machinery for Phase 6 per D-05-03.</action>
  <verify><automated>node --test tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/info.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/info.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts tests/orchestrators/plugin/fetch.test.ts extensions/pi-claude-marketplace/orchestrators/plugin/info.ts tests/orchestrators/plugin/info.test.ts</automated><fails_when>Either owner suite fails or discovers zero tests; fetch and info share a capability, either factory argument is optional or production-bypassed, fetch probe/materialize/classify order changes, stale pre-fetch status is rendered, info containment/read/list/reason/component order changes, bare info reaches network, full-value result/tree/notification/diagnostic assertions weaken, `info.ts` is split, Phase 6 patch machinery is removed, or a new suppression appears.</fails_when></verify>
  <acceptance_criteria>OPEF-F04 and OPIC-F27 each have one separate required capability, production and controlled paths use the same factories, and exact fetch freshness plus info read/list behavior remains observable end to end.</acceptance_criteria>
  <reversibility rating="costly">These required factory contracts become Phase 6's replacement seams for patched status/read/list failures; contract changes remain possible but would require coordinated owner-test and composition updates.</reversibility>
  <done>All four roots in this plan are classified and wired without shared unrelated capabilities, hidden defaults, or behavior loss.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                                                                                  |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Persisted state -> hooks hydration             | Stored marketplace/plugin paths and hooks slugs influence cache entries and later event dispatch.                                                            |
| Plugin hooks tree -> staging writer            | Directory entries and symlink targets cross a containment boundary before hooks configuration is copied.                                                     |
| Clone cache -> fetch status                    | Filesystem visibility and classification determine whether network materialization runs and what status is reported.                                         |
| Marketplace/plugin tree -> info renderer       | File contents and directory names become user-visible inventory and diagnostic reasons.                                                                      |
| Required capability -> production control flow | A substituted reader/probe may force one authorized failure edge but must not control validation, rendering, state mutation, notification, or unrelated I/O. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                       | Severity | Disposition | Mitigation Plan                                                                                                                                                                            |
| --------- | ---------------------------------- | ------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-05-06   | Tampering / Elevation of privilege | hooks hydration                 | high     | mitigate    | Retain slug containment and absolute-root branding before cache insertion; require one state reader and prove corrupted/outside records are logged and skipped with exact cache state.     |
| T-05-07   | Tampering / Elevation of privilege | hooks staging subtree walk      | high     | mitigate    | Limit the inspector to readdir/lstat/realpath/readlink, never follow links during traversal, route resolved targets through `assertPathInside`, and verify outside bytes remain unchanged. |
| T-05-08   | Tampering / Denial of service      | fetch status sequence           | medium   | mitigate    | Keep the pre-probe/materialize/fresh-post-probe order inside the orchestrator, preserve failure-to-row narrowing, and prove the rendered status comes from the post-materialization tree.  |
| T-05-09   | Information disclosure / Tampering | plugin info reads and lists     | high     | mitigate    | Preserve path containment and the network-free bare-info boundary; limit the reader to targeted UTF-8 reads/Dirent lists and retain closed-set error classification before rendering.      |
| T-05-10   | Tampering                          | adapter composition             | medium   | mitigate    | Require every new factory argument, bind explicit real adapters in production, forward the hooks reader from the edge, and verify full public results before operation logs.               |
| T-05-11   | Repudiation                        | debug and notification surfaces | low      | mitigate    | Retain exact diagnostics, reason ordering, notification bytes, and operation logs in owner assertions so controlled faults cannot silently change attribution.                             |

ASVS Level 1 disposition: all high-severity filesystem/path threats are blocking until the focused real-tree containment and owner suites pass. This plan changes no authentication, cryptography, schema, package, or external-API integration boundary.
</threat_model>

<verification>
Run all owner suites named in the tasks, direct coverage for every modified production module, `npm run typecheck`, focused ESLint, and final `npm run check`. Inspect the scoped diff to confirm the four capabilities remain separate, every production factory has explicit real wiring, no new production file lacks a mirrored owner, the existing `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` line is byte-exact, and no createRequire/syncBuiltinESMExports cleanup or Phase 6 split entered this plan.
</verification>

<success_criteria>

- HHD-011, HSA-026, OPEF-F04, and OPIC-F27 each receive one terminal consumer-owned classification.
- Only same-meaning, same-lifetime operations share a capability; no port is shared across the four owners.
- Required production adapters and case-local controlled faults traverse the same control flow.
- Real containment, state/cache effects, tree bytes, operation order, diagnostics, result rows, and notification bytes remain exact.
- No new test-only/reset/default/dead seam, Fallow suppression, global-patch removal, large-module split, or excluded-file edit is introduced.
  </success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-02-SUMMARY.md` when done.
</output>
