---
phase: 05-injection-and-ownership-design
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - tests/orchestrators/plugin/install.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - tests/orchestrators/plugin/uninstall.test.ts
autonomous: true
requirements: [TREF-04]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The Phase 5 classification is complete for all eleven MF-DEC-07 roots: 05-01 and 05-02 establish six roots, while this plan closes OPEF-F09, OPIA-F05, OPLU-B-F10, OPR-B-F14, and ORA-F15 (D-05-01)."
    - "Reconcile receives only a required state-read capability for the authorized post-read race; applyReconcile still composes real orchestrators and proves its full public result, persisted state, scope tree, and exact notifications (D-05-02)."
    - "Enable/disable, install, reinstall, and uninstall expose separate consumer-owned transaction ports whose operations name existing semantic transaction steps rather than a shared filesystem or dependency bag."
    - "Every production export is bound to an explicit real adapter, and owner tests use a substitute only for an irreproducible schedule, rollback point, state-read race, or post-commit failure (D-05-01)."
    - "Forward operation order, failing-step compensation, reverse rollback order, partial-rollback reporting, abort persistence, finalization, cleanup, exact errors, and exact notifications remain unchanged."
    - "bootstrapClaudePlugin remains the second and only other behavioral-composition exception, with complete state, configuration, scope-tree, and exact-notification assertions and no bespoke seam (D-05-02)."
    - "Phase 6 retains ownership of createRequire/syncBuiltinESMExports removal and the approved install/reinstall/module splits; this plan shapes movable contracts without performing those splits (D-05-03, D-05-09)."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
    - tests/orchestrators/reconcile/apply.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
    - tests/orchestrators/plugin/enable-disable.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - tests/orchestrators/plugin/install.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - tests/orchestrators/plugin/reinstall.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    - tests/orchestrators/plugin/uninstall.test.ts
  key_links:
    - "applyReconcile production export -> required ReconcileStateReader -> withLockedStateTransaction loadState slot -> real state loader"
    - "setPluginEnabled production export -> required enable/disable transaction adapter -> runInstallLedger or cascade unstage -> config/state save and routing rebuild"
    - "installPlugin production export -> required install transaction adapter -> existing ordered phase ledger -> rollback and finalization"
    - "reinstallPlugin production export -> required reinstall transaction adapter -> prepare/replace/save/finalize/cleanup schedule"
    - "uninstallPlugin production export -> required uninstall transaction adapter -> cascade/config/state/post-commit schedule"
    - "applyReconcile and bootstrapClaudePlugin owner suites -> real composed operations -> public results plus exact state/tree/notification observations"
  prohibitions:
    - statement: "Do not introduce test-only exports, reset hooks, __deps, optional real-adapter fallbacks, unused defaults, or dead ports."
      source: "Phase 5 specless-probe fallback"
      status: flagged-unverified
    - statement: "Do not weaken applyReconcile or bootstrapClaudePlugin composition proofs into interaction-only assertions or broaden the exception beyond those two flows."
      source: "Phase 5 specless-probe fallback"
      status: flagged-unverified
    - statement: "Do not execute Phase 6 global-patch removal or large-module splits, and do not touch bridges/skills/stage.test.ts, PID-table behavior, the info split, or uninstall cohesion."
      source: "Phase 5 specless-probe fallback"
      status: flagged-unverified
---

<objective>
Complete the exact eleven-root dependency classification by adding the five remaining production-owned state-read and transaction ports while preserving both behavioral-composition exceptions (D-05-01, D-05-02, D-05-03, D-05-09).

Purpose: Replace hidden process-wide control of irreproducible races and transaction schedules with required, consumer-owned production contracts without weakening real filesystem, rollback, or composition evidence.
Output: A reconcile state reader; separate enable/disable, install, reinstall, and uninstall transaction adapters; and owner regressions proving real adapter order, rollback behavior, public state/tree effects, and exact notifications.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md
@extensions/pi-claude-marketplace/transaction/with-state-guard.ts
@extensions/pi-claude-marketplace/transaction/phase-ledger.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
@tests/orchestrators/plugin/bootstrap.test.ts
</context>

## Artifacts this plan produces

| File family                                           | Contract or proof introduced by this plan                                                                                                                                                           |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `orchestrators/reconcile/apply.ts` + owner test       | A required reconcile-owned state reader controlling only the selected state load; the production export remains bound to the real loader and the owner suite retains full composition observations. |
| `orchestrators/plugin/enable-disable.ts` + owner test | A required enable/disable transaction adapter for the authorized semantic schedule and routing-rebuild failure points, with portable config/write behavior left on real temporary trees.            |
| `orchestrators/plugin/install.ts` + owner test        | A required install-owned transaction adapter matching the existing phase-ledger prepare, replace, rollback, abort, cleanup, and finalization semantics.                                             |
| `orchestrators/plugin/reinstall.ts` + owner test      | A required reinstall-owned transaction adapter that can move with Phase 6 without performing the split, preserving state-save compensation and post-success ordering.                               |
| `orchestrators/plugin/uninstall.ts` + owner test      | A required uninstall-owned cohesive workflow adapter preserving cascade, config, state-save, and post-commit cleanup order.                                                                         |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate the reconcile state-read race without weakening apply composition</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/apply.test.ts, extensions/pi-claude-marketplace/transaction/with-state-guard.ts, extensions/pi-claude-marketplace/persistence/state-io.ts, extensions/pi-claude-marketplace/index.ts, .planning/phases/05-injection-and-ownership-design/05-CONTEXT.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "A required readonly ReconcileStateReader exposes only the state load used by the reconcile read pass; the existing applyReconcile production export binds it to the real loadState adapter."
    - "A case-local reader can return the real selected snapshot and then mutate the backing state before apply continues, reproducing ORA-F15 without replacing planning, child orchestrators, locks, persistence, routing rebuild, projection, or notification."
    - "The raced scope converges from the selected snapshot under the existing per-scope isolation contract, while the sibling scope still completes and final output stays canonically ordered."
    - "All unchanged apply cases retain real temporary state/config files, public return/silence behavior, exact persisted state, complete scope tree, redacted errors, and exact notification bytes."
  </behavior>
  <action>Implement ORA-F15 per D-05-01 by defining a readonly reconcile-owned state-reader contract with one load operation matching the state transaction's existing `loadState` slot. Make an internal `createApplyReconcile(stateReader)` require that capability, thread it only into `readPassForScope` and `withLockedStateTransaction`, and compose the existing `applyReconcile` export from an explicit real reader. Migrate only the selected-read race case to the factory: wrap a real state load, preserve the returned snapshot, perform the competing write after selection, and then let the unchanged real apply flow continue. Per D-05-02, do not provide child-orchestrator, planner, routing, projection, notification, lock, or save substitutes; retain and strengthen the existing assertions for public result/silence, both scopes' exact persisted state and tree, exact notification order/bytes, failure isolation, and unrelated bytes. Retain the authorized global builtin mutation cases for Phase 6 per D-05-03.</action>
  <verify><automated>node --test tests/orchestrators/reconcile/apply.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts tests/orchestrators/reconcile/apply.test.ts --max-warnings=0</automated><fails_when>The owner suite fails or discovers zero tests; the reader controls more than the selected load, is optional, is bypassed by production, or changes lock/save behavior; the race becomes an interaction-only case; public result/silence, sibling-scope progress, state/tree bytes, error redaction, notification bytes/order, or unrelated files change; a bespoke apply dependency bundle or new suppression appears; or Phase 6 patch machinery is removed.</fails_when></verify>
  <acceptance_criteria>ORA-F15 is classified exactly once as a required reconcile state-read boundary, while applyReconcile remains the first behavioral-composition exception and proves the same complete public effects (D-05-01, D-05-02).</acceptance_criteria>
  <reversibility rating="costly">D-05-03 makes this narrow reader the authorized bridge from the current global-patch test to Phase 6 ownership, so later widening would require coordinated apply and owner-test migration.</reversibility>
  <done>The real production reader and the case-local race reader execute the same apply composition path, with full public state/tree/notification evidence preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose enable/disable and install transaction schedules through required ports</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, tests/orchestrators/plugin/enable-disable.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/install.test.ts, extensions/pi-claude-marketplace/transaction/phase-ledger.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, .planning/phases/05-injection-and-ownership-design/05-RESEARCH.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, tests/orchestrators/plugin/enable-disable.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, tests/orchestrators/plugin/install.test.ts</files>
  <behavior>
    - "A required enable/disable-owned adapter controls only the semantic install-ledger, cascade, config transaction, and hooks-routing schedule points authorized by OPEF-F09; ordinary config read/write failures stay on real case-owned temporary trees."
    - "Enable still re-materializes inside the outer locked transaction, disable still cascades before replacing the record, partial drops still shrink state before failure is surfaced, and a routing-rebuild failure remains a post-effect diagnostic rather than changing the command result."
    - "A required install-owned adapter exposes the existing semantic prepare, replace, rollback/abort, cleanup, and finalization operations needed by OPIA-F05 without granting a generic filesystem capability."
    - "Install executes the same phase-ledger order, invokes the failing phase's compensation first, reverses completed compensation order, preserves rollbackPartial rows and original Error identity, and commits state only at the existing point."
  </behavior>
  <action>Per D-05-01, create separate readonly `EnableDisableTransactionOps` and `InstallTransactionOps` contracts and internal factories whose arguments are required. Derive each method directly from an existing semantic transaction step; keep path selection, name and containment validation, state/result construction, notification projection, and portable config/filesystem behavior in the consumers. Bind the unchanged public `setPluginEnabled` and `installPlugin` exports to explicit real adapters so production and owner tests traverse the same factories. For OPEF-F09, let cases control only irreproducible rollback ordering and hooks routing mutation failure; retain real temporary-tree coverage for ordinary config selection, invalid bytes, permission failure, write-back, and scope precedence. For OPIA-F05, migrate the eight schedule cases to the install-owned adapter and assert the exact forward sequence, failing-step undo, reverse compensation, abort, cleanup, finalization, state/tree effects, error identity, rollbackPartial content/order, and notifications. Preserve existing genuine credential, device-flow, clone-cache, and phase-ledger contracts rather than absorbing them into a super-bag. Keep the contracts movable for Phase 6 but do not split install or remove authorized global-patch machinery per D-05-03 and D-05-09.</action>
  <verify><automated>node --test tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/install.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts tests/orchestrators/plugin/enable-disable.test.ts extensions/pi-claude-marketplace/orchestrators/plugin/install.ts tests/orchestrators/plugin/install.test.ts --max-warnings=0</automated><fails_when>Either owner suite fails or discovers zero tests; a port is optional, dead, test-only, shared across unrelated consumers, or wider than the semantic schedule; portable filesystem cases become call-log tests; transaction, compensation, rollbackPartial, state-save, hooks-routing, cleanup, finalization, exact-error, state/tree, or notification order changes; a genuine existing port is folded into a broad bundle; a new suppression appears; or install splitting/global-patch cleanup is performed early.</fails_when></verify>
  <acceptance_criteria>OPEF-F09 and OPIA-F05 each have a distinct required production transaction port, and every controlled schedule is backed by unchanged public state, filesystem, error, rollback, and notification evidence (D-05-01, D-05-03, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">These contracts must move with Phase 6's install ownership split without semantic churn; widening or combining them would create cross-phase migration work.</reversibility>
  <done>Enable/disable and install use visible real adapters in production and narrow schedule substitutes in tests while preserving every transaction invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve cohesive reinstall and uninstall transactions and close the eleven-root matrix</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/orchestrators/plugin/uninstall.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts, tests/orchestrators/plugin/bootstrap.test.ts, extensions/pi-claude-marketplace/transaction/with-state-guard.ts, extensions/pi-claude-marketplace/transaction/phase-ledger.ts, .planning/phases/05-injection-and-ownership-design/05-PATTERNS.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <behavior>
    - "A required reinstall-owned adapter controls only OPR-B-F14's prepare/replace/save/rollback/abort/finalize/post-save schedule; state snapshots, conflicts, resolution, public outcomes, and notification projection remain real."
    - "Reinstall preserves prepare-all before replace, exact replacement order, state-save compensation, reverse rollback, finalized replacement cleanup, partial warnings, disabled-record skip, and cold/warm clone behavior."
    - "A required uninstall-owned adapter controls only OPLU-B-F10's cascade/config/state-save/post-commit schedule while uninstall remains one cohesive transactional workflow."
    - "Uninstall keeps containment checks outside swallowed cleanup, preserves config-before-state-save ordering, saves partial cascade shrinkage, keeps AG-5 abort behavior, and runs cache/data/clone cleanup only after commit."
    - "The completed matrix names exactly the eleven authorized roots from 05-RESEARCH and introduces no port for any other global-patch use."
    - "bootstrapClaudePlugin remains unchanged as the second composition exception and its owner suite asserts complete state, configuration, scope tree, and exact notifications."
  </behavior>
  <action>Per D-05-01, define separate readonly `ReinstallTransactionOps` and `UninstallTransactionOps` contracts and internal factories with required adapters. For reinstall, expose only existing semantic schedule points needed to force prepare, replace, state-save, compensation, abort, finalization, and post-save failures; migrate the OPR-B-F14 schedule cases away from hidden builtin control while retaining exact public state/tree/error/warning/notification assertions. Do not absorb credential, device-flow, auth memo, clone-cache, or the completion-cache contract scheduled for later Phase 5 plans; do not retain moved schedule operations solely behind the existing test descriptor. For uninstall, move its existing cascade control and only the irreproducible cross-bridge/config/state/post-commit schedule points into one cohesive consumer-owned transaction adapter, bind the public export to real implementations, and keep validation, containment, config parsing, locked state mutation, outcome construction, and notification projection in the orchestrator. Assert exact forward and rollback order plus state/tree bytes for every partial and abort arm. Finish with a current eleven-root census against the 05-RESEARCH table and prove no unlisted root received a port. Per D-05-02, run but do not edit the bootstrap owner suite; preserve its real addMarketplace/autoupdate composition and complete observable assertions. Per D-05-03 and D-05-09, leave uninstall cohesive, leave install/reinstall split ownership and global-patch deletion to Phase 6, and make the new transaction contracts movable without changing their behavior.</action>
  <verify><automated>node --test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts tests/orchestrators/plugin/reinstall.test.ts extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts tests/orchestrators/plugin/uninstall.test.ts --max-warnings=0</automated><fails_when>Either owner suite or either composition suite fails or discovers zero tests; reinstall/uninstall share a generic port or select optional real schedule operations inside the consumer; normal filesystem behavior moves to substitutes; prepare/replace/save/rollback/abort/finalize/cleanup order, exact state/tree bytes, failure identity, partial warnings, outcomes, or notifications change; containment errors are swallowed; apply/bootstrap become interaction-only or gain bespoke seams; the root census is not exactly eleven; an unauthorized root is ported; uninstall is split; a new suppression appears; or Phase 6 patch machinery is removed.</fails_when></verify>
  <acceptance_criteria>OPR-B-F14 and OPLU-B-F10 are classified through separate required production transaction ports, all eleven MF-DEC-07 roots now have one terminal classification, and both MF-DEC-03 composition exceptions retain complete public evidence (D-05-01, D-05-02, D-05-03, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">The reinstall adapter is a contract boundary for Phase 6's approved ownership move, while uninstall deliberately remains cohesive; changing either choice later requires coordinated transaction-test migration.</reversibility>
  <done>The five remaining roots are closed with narrow real-adapter contracts, exact transaction behavior remains observable, and the only composition exceptions are applyReconcile and bootstrapClaudePlugin.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                                                                                                                  |
| ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Persisted state/config -> reconcile and lifecycle plans | Files may be stale, concurrently changed, malformed, or scope-conflicting; selected snapshots and per-scope isolation must remain truthful.                                  |
| Required transaction port -> filesystem/state mutation  | A substituted schedule operation can force an authorized fault but must not bypass validation, containment, lock ownership, result construction, or notification projection. |
| Prepared/staged paths -> replace, rollback, and cleanup | Paths derived from manifests and stored records cross destructive write/remove boundaries and must remain contained and ordered.                                             |
| Composed orchestrator -> user-visible result            | applyReconcile and bootstrapClaudePlugin must prove real aggregate effects and exact messages rather than collaborator interaction alone.                                    |

## STRIDE Threat Register

| Threat ID | Category                           | Component                          | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                     |
| --------- | ---------------------------------- | ---------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-11   | Tampering / Repudiation            | reconcile selected-state read      | high     | mitigate    | Restrict `ReconcileStateReader` to the transaction's load operation, preserve locking and per-scope isolation, and assert the selected snapshot, competing write, final state/tree, and exact cascade together.     |
| T-05-12   | Tampering / Elevation of privilege | lifecycle transaction ports        | high     | mitigate    | Keep path/name/containment checks and mutation decisions in each orchestrator; ports expose only semantic operations already executed by the real adapter and cannot authorize arbitrary filesystem paths.          |
| T-05-13   | Tampering / Denial of service      | phase compensation and rollback    | high     | mitigate    | Preserve exact forward order, failing-step own undo, reverse completed-step rollback, original Error identity, rollbackPartial ordering, abort persistence, and partial-effect assertions in all four owner suites. |
| T-05-14   | Information disclosure             | state, path, and error surfaces    | medium   | mitigate    | Preserve basename-only/redacted public diagnostics while retaining exact Error causes internally; owner tests reject absolute-path leakage and assert unrelated files remain unchanged.                             |
| T-05-15   | Repudiation / Tampering            | apply/bootstrap composition proofs | medium   | mitigate    | Keep real child orchestrators and assert public results, complete persisted state/config/scope trees, and exact notification bytes; port call logs may only supplement those observations.                          |
| T-05-16   | Tampering                          | production adapter selection       | medium   | mitigate    | Require each factory argument and bind unchanged public exports to explicit real adapters; TypeScript, direct-pair coverage, and forwarding regressions fail if production bypasses a port.                         |
| T-05-17   | Denial of service                  | post-commit cleanup                | low      | accept      | Existing best-effort cleanup intentionally cannot reverse a committed user operation; preserve warning/swallow policy, containment checks, and idempotent retry behavior exactly.                                   |

ASVS Level 1 disposition: all high-severity state-integrity, path-authority, and rollback threats are blocking and receive owner-test mitigations; medium threats on the primary state/result boundaries are also mitigated. The accepted low cleanup risk is bounded by existing containment, post-commit ordering, and retry semantics. This plan installs no package and crosses no external service boundary.
</threat_model>

<verification>
Run all five changed owner suites plus `tests/orchestrators/plugin/bootstrap.test.ts`, direct-pair coverage for every changed production module, `npm run typecheck`, focused ESLint, and `npm run check`. Confirm the implementation evidence maps the exact eleven roots from 05-RESEARCH to exactly one terminal classification each; no extra root receives a port; applyReconcile and bootstrapClaudePlugin remain the only composition exceptions; and every task-level prohibition in frontmatter holds. Confirm the changed-file diff adds no suppression, leaves `scripts/revalidation.mjs` unchanged with the exact line `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor`, retains all authorized createRequire/syncBuiltinESMExports machinery for Phase 6, and does not edit the deferred files or perform a large-module split.
</verification>

<success_criteria>

- OPEF-F09, OPIA-F05, OPLU-B-F10, OPR-B-F14, and ORA-F15 each have one narrow required production port, completing the exact eleven-root classification.
- Public exports visibly bind explicit real adapters; owner substitutes control only authorized races, schedules, rollback points, or post-commit faults.
- Forward, save, compensation, rollback, abort, finalization, cleanup, result, state/tree, error, and notification behavior remains exact.
- applyReconcile and bootstrapClaudePlugin remain the only behavioral-composition exceptions and keep complete public-effect assertions.
- No new test descriptor, optional/default/dead seam, broad filesystem bag, suppression, unauthorized root, Phase 6 patch removal, or large-module split appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-03-SUMMARY.md` when done.
</output>
