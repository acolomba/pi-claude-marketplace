---
phase: 117-extension-entry-and-final-gate
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/edge/notification-boundary.ts
  - tests/edge/handlers/marketplace/add.test.ts
  - tests/edge/handlers/marketplace/autoupdate.test.ts
  - tests/edge/handlers/marketplace/info.test.ts
  - tests/edge/handlers/marketplace/list.test.ts
  - tests/edge/handlers/marketplace/remove.test.ts
  - tests/edge/handlers/marketplace/shared.test.ts
  - tests/edge/handlers/marketplace/update.test.ts
  - tests/edge/handlers/plugin/bootstrap.test.ts
  - tests/edge/handlers/plugin/enable-disable.test.ts
  - tests/edge/handlers/plugin/fetch.test.ts
  - tests/edge/handlers/plugin/import.test.ts
  - tests/edge/handlers/plugin/info.test.ts
  - tests/edge/handlers/plugin/install.test.ts
  - tests/edge/handlers/plugin/list.test.ts
  - tests/edge/handlers/plugin/pending.test.ts
  - tests/edge/handlers/plugin/reinstall.test.ts
  - tests/edge/handlers/plugin/shared.test.ts
  - tests/edge/handlers/plugin/uninstall.test.ts
  - tests/edge/handlers/plugin/update.test.ts
  - tests/edge/handlers/shared.test.ts
  - tests/edge/register.test.ts
  - tests/edge/router.test.ts
  - tests/orchestrators/import/execute.test.ts
  - tests/orchestrators/plugin/bootstrap.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/orchestrators/reconcile/pending.test.ts
files_deleted:
  - tests/helpers/notification-boundary.ts
autonomous: true
requirements: [SUITE-02, DEL-03]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The Pi notification boundary lives beside its dominant consumer tier — 22 of its 26 consumers are edge suites. RED when: any of the 28 import lines still resolves through the old directory, which stops the whole tree typechecking."
    - "The four surviving cross-tier imports are named in the summary rather than left to be rediscovered: the import-execute, plugin-bootstrap, reconcile-apply and reconcile-pending orchestrator suites reach across the tier boundary by design, and no configured boundary rule governs `tests/`."
    - "The module's own body is unchanged. Its climb to the platform types is the same at the new home because both are two levels below the repository root — proved by typecheck, not by counting segments."
    - "The two suites that now import it as a sibling rather than a parent move to a different import-order group. RED when: their import blocks are left as they were, which ESLint reports as an order violation."
    - "No copy of the module is made. Roughly twenty phase-116 proofs depend on this one definition, and `fallow dupes` runs at a three-line threshold, so a per-tier duplicate is the drift this milestone exists to remove."
  artifacts:
    - path: tests/edge/notification-boundary.ts
      provides: "The strict Pi notification boundary, beside the 22 edge suites that dominate its consumer set"
  key_links:
    - from: tests/edge/register.test.ts
      to: tests/edge/notification-boundary.ts
      via: "a sibling import replacing the cross-directory one"
      pattern: "createNotificationBoundary"
    - from: tests/orchestrators/reconcile/apply.test.ts
      to: tests/edge/notification-boundary.ts
      via: "one of the four accepted cross-tier imports named by D-117-05"
      pattern: "createNotificationBoundary"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not change the boundary helper's behaviour, signature, defaults or documented counts. Its `toolProbes` parameter stays required and a count of zero keeps meaning no expectation at all; roughly twenty proofs rest on that."
    - "This plan must not touch `package.json`, must not delete `tests/helpers/`, and must not move the marketplace seed. Both belong to 117-07."
    - "This plan must not rewrite any consumer beyond its import statement and whatever import ordering ESLint then demands."
---

<objective>
Move the busiest support module in the repository to the tier that owns it.

Purpose: SUITE-02 bars a generic test-support directory. The Pi notification boundary is the largest
resident of `tests/helpers/` by consumer count — measured at 26 consumers over 28 import lines, of
which 22 consumers are edge suites and 4 are orchestrator suites. D-117-04 sends it beside its
dominant consumer; D-117-05 accepts and names the four that then import across the tier boundary
rather than hiding them, because `.fallowrc.json`'s zone rules govern `extensions/` and not `tests/`,
so nothing configured is broken by them. D-117-06 already rejected duplicating it per tier.

This is the plan with the widest edit surface in the phase and the one where a wrong specifier is
loudest — the whole tree stops typechecking. It changes no behaviour: the module body is untouched.

Output: one boundary definition under `tests/edge/`, 28 rewritten import lines, and four cross-tier
consumers named in the summary.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/116-edge-surface/.continue-here.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the Pi notification boundary beside the edge tier</name>
  <files>tests/edge/notification-boundary.ts and the 26 consumer suites listed in files_modified</files>
  <read_first>
    - tests/helpers/notification-boundary.ts
    - tests/edge/register.test.ts
    - tests/edge/handlers/shared.test.ts
    - tests/orchestrators/reconcile/apply.test.ts
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
`git mv tests/helpers/notification-boundary.ts tests/edge/notification-boundary.ts`, then rewrite
every consumer specifier in the same commit. A commit that moves the module without fixing its
consumers does not typecheck, so the move and the rewrites are one concern under DEL-01. The moved
file's body is untouched, which keeps git's similarity detection reporting a rename.

Do not change the module: not its signature, not its parameter names, not its defaults, not its
header. Its `toolProbes` parameter stays required and a count of zero keeps meaning "state no
expectation at all", because a count written as a zero-times expectation is inert in `strong-mock` and
roughly twenty phase-116 proofs rest on the fixed version. The module has no path header comment and
no reference to its own directory, so nothing inside it needs editing — confirm that by reading it
before you move it.

Both the old and the new home are two levels below the repository root, so the module's climb to the
platform types is unchanged. Confirm that with `npm run typecheck`, not by counting segments.

Rewrite the specifiers. Measured shape of the current tree, to be re-derived rather than trusted —
the operator edits this checkout concurrently, so run the grep yourself first:

- the seventeen suites under the two handler subdirectories climb three levels today and climb two
  afterwards;
- the shared handler suite climbs two today and one afterwards;
- the two suites directly under the edge root climb one today and become SIBLING imports afterwards,
  which moves the statement into a different `import-x/order` group and needs the import block
  reordered;
- the four orchestrator suites climb two today and afterwards reach across into the edge root. These
  are the four D-117-05 names — record them in the summary as accepted cross-tier imports, with the
  reason no configured boundary rule governs them.

Two consumers import both the factory and a type from this module in two separate statements, because
the house import ordering puts type-only imports last. That is why the count is 28 lines over 26
files. Fix both lines in each.

Rewrite nothing else in any consumer. No case body, no expectation, no count changes.

Run ESLint over the whole of `tests/edge` and `tests/orchestrators` and let it tell you the ordering;
do not guess it.

There is no plant to run here beyond what the toolchain already forces: a wrong specifier does not
typecheck, and a wrong module would not satisfy the twenty-odd suites that verify against it. Say that
in the summary rather than inventing a plant that cannot fail. What DOES need reporting is the suite
total, read from the runner's own `ℹ tests` line after the move — it must be unchanged, because this
plan removes and adds no case. Do not compute it.

Change no production file.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm exec -- eslint tests &amp;&amp; npm exec -- prettier --check tests/edge/notification-boundary.ts &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; ! rg -n 'helpers/notification-boundary' tests &amp;&amp; ! rg -n 'Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]|moved from|relocated' tests/edge/notification-boundary.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'notification-boundary' &amp;&amp; test ! -e tests/helpers/notification-boundary.ts &amp;&amp; git diff --quiet -- extensions/ package.json &amp;&amp; git diff -M --stat HEAD -- tests/edge/notification-boundary.ts</automated>
    <fails_when>Non-zero exit from any link: an `error TS` line from typecheck (which is what a missed specifier produces), any ESLint problem line including an import-order violation in the two suites whose import became a sibling, a `[warn]` path from the format check, a non-zero `fallow` exit, a non-zero `npm test` or integration exit, the old-directory scan finding a surviving reference anywhere under `tests/`, the relocation-history scan finding a migration note, the correspondence gate naming the boundary module, the old file still existing, `git diff --quiet` exiting 1 because a production file or `package.json` changed, or the rename-aware diff showing the moved file as anything other than a rename.</fails_when>
  </verify>
  <acceptance_criteria>
The module is at `tests/edge/notification-boundary.ts`, `git status` records a rename, and its body is
byte-identical to its previous content. All 28 import lines across 26 consumer suites resolve to the
new path and the whole tree typechecks. ESLint is silent, including on the two suites whose import
moved into the sibling group. `npm test` reports a total read from the runner's own `ℹ tests` line
that is unchanged from the pre-move total, also read from the runner, and the summary states both
numbers as measurements rather than as a derivation. The summary names the four accepted cross-tier
consumers explicitly. No consumer is edited beyond its import statement and the ordering ESLint
demanded. `git diff` shows no change under `extensions/` and none to `package.json`.
  </acceptance_criteria>
  <done>The Pi notification boundary lives beside the edge tier, all 26 consumers resolve, and the four cross-tier imports are named.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. This plan relocates one test-support module and changes no
runtime surface.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-03-A | Tampering | the shared boundary definition roughly twenty phase-116 proofs verify against | high | mitigate | The module body is moved unchanged and the rename-aware diff is asserted, so a silent edit to its counts or defaults cannot ride along with the move. |
| T-117-03-B | Denial of Service | the whole unit suite, which stops typechecking if one specifier is missed | medium | mitigate | Typecheck, ESLint over all of `tests`, `npm test` and the integration suite all run before the commit, and the old-directory scan covers the whole `tests` tree. |
| T-117-03-C | Tampering | production sources and the npm script globs | high | mitigate | The verify block ends with `git diff --quiet -- extensions/ package.json`. |
</threat_model>

<verification>
Run each gate separately and read its exit code. Do not use `npm run check`: its `format:check` link
fails on the operator's pre-existing untracked files and short-circuits before the tests run. Do not
touch those files and do not edit `.prettierignore`.

One commit. Before it, scan the changed paths for secrets by the filesystem route with explicit
literal paths — this shell does not word-split and trufflehog's git-mode scan aborts structurally
here: `TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem <the literal paths> --results=verified,unknown --fail`, confirming non-zero
chunk and byte counts. Then `npm exec -- prettier --check <the literal paths>` and
`SKIP=trufflehog,npm-format-check pre-commit run --files <the literal paths>`. Git hooks are not
installed here, so a clean `git commit` is not evidence hooks passed. Never `--all-files`, never
`--no-verify`. Use `git commit -F`. Stage only the literal paths this plan owns; never `git add -A`.

Confirm with `git status --short` and `git log -1 --stat`, and check the rename appears.
</verification>

<success_criteria>

- `tests/helpers/` holds exactly one file afterwards: the marketplace seed.
- The move is recorded as a rename and the module body is unchanged.
- `npm test` and `npm run test:integration` pass, and the unit total is unchanged from before the
  move, both figures read from the runner.
- The four accepted cross-tier consumers are named in the summary.
- Nothing under `extensions/` and nothing in `package.json` changed.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-03-SUMMARY.md` when done.
</output>
