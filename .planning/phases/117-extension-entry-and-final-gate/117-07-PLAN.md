---
phase: 117-extension-entry-and-final-gate
plan: "07"
type: execute
wave: 2
depends_on: ["117-02", "117-03"]
files_modified:
  - tests/edge/handlers/marketplace-seed.ts
  - tests/edge/handlers/marketplace/autoupdate.test.ts
  - tests/edge/handlers/marketplace/info.test.ts
  - tests/edge/handlers/marketplace/list.test.ts
  - tests/edge/handlers/marketplace/remove.test.ts
  - tests/edge/handlers/marketplace/update.test.ts
  - tests/edge/handlers/plugin/enable-disable.test.ts
  - tests/edge/handlers/plugin/fetch.test.ts
  - tests/edge/handlers/plugin/info.test.ts
  - tests/edge/handlers/plugin/install.test.ts
  - tests/edge/handlers/plugin/list.test.ts
  - tests/edge/handlers/plugin/reinstall.test.ts
  - tests/edge/handlers/plugin/uninstall.test.ts
  - tests/edge/handlers/plugin/update.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/orchestrators/plugin/list.test.ts
  - package.json
files_deleted:
  - tests/helpers/marketplace-seed.ts
autonomous: true
requirements: [SUITE-02, SUITE-04, DEL-03]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The marketplace seed lives beside its dominant consumer group — 13 of its 15 consumers are handler suites. RED when: any of the 15 import lines still resolves through the old directory, which stops the whole tree typechecking."
    - "The seed's OWN production imports change depth, and this is the one edit in the dissolution that a mechanical find-and-replace over consumers would miss: the module moves from two levels below the repository root to three, so all three of its own specifiers gain one climb. RED when: they are left alone — typecheck reports an unresolved module for each."
    - "The two surviving cross-tier imports are named in the summary rather than left to be rediscovered: the plugin-info and plugin-list orchestrator suites reach across the tier boundary by design, and no configured boundary rule governs `tests/`."
    - "`tests/helpers/` does not exist afterwards, as a directory or as an entry in either npm glob. RED when: the directory survives, which a filesystem check catches, or a glob alternative survives, which a scan of the two script strings catches."
    - "Removing the `helpers` alternative changes nothing about which files run — measured before the phase at 249 matched paths with and without it, because the only test file it ever matched has already moved to a root the glob names. It is removed for honesty, and the glob-completeness control proves the matched set is still exactly the tree."
    - "No exemption list, ownership registry or generic support directory replaces what is deleted. SUITE-04 bars all three, and the seed lands beside a concern rather than in a new shared home."
  artifacts:
    - path: tests/edge/handlers/marketplace-seed.ts
      provides: "The marketplace fixture seed, beside the 13 handler suites that dominate its consumer set"
  key_links:
    - from: tests/edge/handlers/plugin/install.test.ts
      to: tests/edge/handlers/marketplace-seed.ts
      via: "a parent import one level up, replacing the three-level climb into the old directory"
      pattern: "marketplace-seed"
    - from: tests/orchestrators/plugin/list.test.ts
      to: tests/edge/handlers/marketplace-seed.ts
      via: "one of the two accepted cross-tier imports named by D-117-05"
      pattern: "marketplace-seed"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not add the root-level owner pattern to either glob. That is 117-08's edit on the same two lines; re-read `package.json` rather than working from a remembered copy."
    - "This plan must not create a replacement shared directory under any name, and must not add an exemption entry to either gate."
    - "This plan must not rewrite the seed's body beyond its own three import specifiers and its path header comment. Its pre-existing double assertions stay as they are."
---

<objective>
Close `tests/helpers/` out: move the last resident, delete the directory, and stop the two npm globs
naming it.

Purpose: SUITE-02 bars a generic test-support directory. Three of the four modules are already beside
their concerns; the marketplace seed is the last, with 15 consumers of which 13 are handler suites.
D-117-04 sends it beside that dominant group and D-117-05 accepts and names the two orchestrator
suites that then import across the tier boundary.

This move carries the one edit in the whole dissolution that a mechanical consumer rewrite would
miss. The seed goes one directory DEEPER than every other move, so its own three production imports
gain a climb. Nothing about the consumers tells you that; only reading the module does.

Output: `tests/helpers/` gone from the filesystem and from both npm globs, with the matched file set
proven unchanged by the control 117-01 landed.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/116-edge-surface/.continue-here.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the marketplace seed beside the handler suites</name>
  <files>tests/edge/handlers/marketplace-seed.ts and the 15 consumer suites listed in files_modified</files>
  <read_first>
    - tests/helpers/marketplace-seed.ts
    - tests/edge/handlers/plugin/install.test.ts
    - tests/orchestrators/plugin/list.test.ts
    - .planning/codebase/CONVENTIONS.md
  </read_first>
  <action>
`git mv tests/helpers/marketplace-seed.ts tests/edge/handlers/marketplace-seed.ts`, then make every
edit the move forces, in one commit. A commit that moves the module without fixing its consumers does
not typecheck, so the move and the rewrites are one concern under DEL-01.

Read the module first. Two edits inside it:

- Its own three production import specifiers. Unlike every other move in this dissolution, the new
  home is one directory DEEPER than the old one, so each of the three gains a climb. Fix all three
  and let typecheck confirm; do not assume a count — read the file and count them yourself.
- Its path header comment names the old location. Update it to the new one. Add no note about having
  moved: SUITE-03 forbids relocation history in a test file and git already holds it.

Rewrite nothing else in the module. It carries pre-existing double assertions; they are not this
plan's concern and rewriting them would also destroy the rename similarity the move depends on.

Consumer edits, measured on the current tree and to be re-derived rather than trusted — the operator
edits this checkout concurrently, so run the grep yourself first:

- the thirteen suites under the two handler subdirectories climb three levels today and one level
  afterwards;
- the two orchestrator suites climb two levels today and afterwards reach across into the handler
  directory. These are the two D-117-05 names — record them in the summary as accepted cross-tier
  imports, with the reason no configured boundary rule governs them.

Each consumer has exactly one import line for this module. Rewrite nothing in any consumer beyond that
line and whatever import ordering ESLint then demands.

Run ESLint over the whole of `tests` and let it tell you the ordering; do not guess it.

There is no plant to invent here: a wrong specifier does not typecheck and a wrong module would fail
the fifteen suites that seed fixtures from it. Say that in the summary rather than writing a proof
that cannot fail. Report the `npm test` total read from the runner's own `ℹ tests` line — it must be
unchanged, because this task adds and removes no case. Do not compute it.

Change no production file. Do not touch `package.json` in this task.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm exec -- eslint tests &amp;&amp; npm exec -- prettier --check tests/edge/handlers/marketplace-seed.ts &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; ! rg -n 'helpers/marketplace-seed' tests &amp;&amp; ! rg -n 'Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]|moved from|relocated' tests/edge/handlers/marketplace-seed.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'marketplace-seed' &amp;&amp; test ! -e tests/helpers/marketplace-seed.ts &amp;&amp; git diff --quiet -- extensions/ package.json &amp;&amp; git diff -M --stat HEAD -- tests/edge/handlers/marketplace-seed.ts</automated>
    <fails_when>Non-zero exit from any link: an `error TS` line from typecheck — which is what a missed consumer specifier OR a missed climb inside the module itself produces — any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow`, `npm test` or integration exit, the old-directory scan finding a surviving reference anywhere under `tests/`, the relocation-history scan finding a migration note, the correspondence gate naming the seed, the old file still existing, `git diff --quiet` exiting 1 because a production file or `package.json` changed, or the rename-aware diff showing the moved file as anything other than a rename.</fails_when>
  </verify>
  <acceptance_criteria>
The module is at `tests/edge/handlers/marketplace-seed.ts` with a rename recorded, all three of its
own production specifiers resolve from the deeper location, and all 15 consumer specifiers resolve to
the new path. ESLint is silent. `npm test` and the integration suite pass, with the unit total read
from the runner and unchanged from before the move — both figures stated as measurements. The summary
names the two accepted cross-tier consumers and states how many of the module's own specifiers changed
depth. No consumer is edited beyond its import statement. `git diff` shows no change under
`extensions/` and none to `package.json`.
  </acceptance_criteria>
  <done>The marketplace seed lives beside the handler suites, all 15 consumers resolve, and the two cross-tier imports are named.</done>
</task>

<task type="auto">
  <name>Task 2: Delete tests/helpers and stop both npm globs naming it</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - tests/architecture/unit-suite-glob-completeness.test.ts
  </read_first>
  <action>
Re-read `package.json` from disk before editing it. Do not work from a remembered copy: three
workstreams in this phase touch the same two script lines, and 117-08 will edit them again after this
plan.

Confirm `tests/helpers/` holds no file, then remove the directory itself. Git tracks no empty
directory, so if the four moves are complete there is nothing left to commit from it — but the
directory may still exist on disk and must not.

Delete the `helpers` alternative from the brace list in BOTH the `test` and the `test:coverage:unit`
scripts. Change nothing else in either script: not the concurrency expansion, not the reporter flags,
not the other alternatives, and not the root-level pattern that 117-08 will add.

This edit is honesty, not function. Measured before this phase began: the two globs match 249 paths
with the alternative present and 249 without it, because the only test file the alternative ever
matched has already moved to a root the brace list names. Verify that claim rather than restating it —
the completeness control 117-01 landed asserts the glob-matched set equals the tree, so a green run of
that control after this edit is the proof, and it is the reason the control had to land first.

Report the `npm test` total read from the runner's own `ℹ tests` line before and after this edit. Both
figures are measurements; do not compute either.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/architecture/unit-suite-glob-completeness.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm exec -- prettier --check package.json &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; ! rg -n 'helpers' package.json &amp;&amp; test ! -d tests/helpers &amp;&amp; ! rg -n 'tests/helpers' tests scripts &amp;&amp; git diff --quiet -- extensions/</automated>
    <fails_when>Non-zero exit from any link: the glob-completeness control failing, which means the matched set no longer equals the tree; an `error TS` line from typecheck; any ESLint problem line; a `[warn]` path from the format check on the edited manifest; a non-zero `fallow`, `npm test` or integration exit; the manifest scan still finding the alternative; the directory still existing; any surviving reference to the old directory under `tests` or `scripts`; or `git diff --quiet` exiting 1 because a production file changed.</fails_when>
  </verify>
  <acceptance_criteria>
`tests/helpers/` does not exist as a directory and neither npm glob names it. The glob-completeness
control passes, which is the proof that removing the alternative changed nothing about which files
run. `npm test` totals before and after this edit are both read from the runner's `ℹ tests` line, are
equal, and are quoted in the summary as measurements. No other part of either script string changed,
and the root-level owner pattern is NOT added by this plan. `git diff` shows no change under
`extensions/`.
  </acceptance_criteria>
  <done>SUITE-02 is satisfied: no generic test-support directory exists in the repository and no npm script names one.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. This plan relocates one test-support module and edits two npm
script strings; it changes no runtime surface.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-07-A | Denial of Service | the unit suite, which a wrong glob edit can silently shrink | high | mitigate | The glob-completeness control landed by 117-01 asserts the matched set equals the tree, and the `npm test` totals before and after are both read from the runner rather than computed. |
| T-117-07-B | Tampering | the seed module's own production imports, which change depth | high | mitigate | Typecheck reports an unresolved module for each missed climb; the task names the depth change explicitly because no consumer edit reveals it. |
| T-117-07-C | Tampering | `package.json`, contended by three workstreams on the same two lines | medium | mitigate | The task re-reads the manifest from disk before editing, changes only the one alternative, and the verify block pins everything under `extensions/`. |
</threat_model>

<verification>
Run each gate separately and read its exit code. Do not use `npm run check`: its `format:check` link
fails on the operator's pre-existing untracked files and short-circuits before the tests run. Do not
touch those files and do not edit `.prettierignore`.

Commit twice, one per task, so the move and the manifest edit are separable. Before each commit, scan
the changed paths for secrets by the filesystem route with explicit literal paths — this shell does
not word-split and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem <the literal paths> --results=verified,unknown --fail`, confirming non-zero
chunk and byte counts. Then `npm exec -- prettier --check <the literal paths>` and
`SKIP=trufflehog,npm-format-check pre-commit run --files <the literal paths>`. Git hooks are not
installed here, so a clean `git commit` is not evidence hooks passed. Never `--all-files`, never
`--no-verify`. Use `git commit -F`. Stage only the literal paths this plan owns; never `git add -A`.

Confirm each commit with `git status --short` and `git log -1 --stat`, and check the rename appears on
the first.
</verification>

<success_criteria>

- `tests/helpers/` is gone from the filesystem, from both npm globs, and from every reference under
  `tests` and `scripts`.
- The seed move is recorded as a rename and all three of its own specifiers were re-depthed.
- The glob-completeness control passes after the manifest edit.
- `npm test` and `npm run test:integration` pass, with the unit total unchanged and read from the
  runner at every step.
- The two accepted cross-tier consumers are named in the summary.
- Nothing under `extensions/` changed, and no exemption list or replacement shared directory was
  created.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-07-SUMMARY.md` when done.
</output>
