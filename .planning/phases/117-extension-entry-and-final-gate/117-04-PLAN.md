---
phase: 117-extension-entry-and-final-gate
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/architecture/cross-surface-reason-parity.test.ts
  - tests/architecture/integration-materialization-gate.test.ts
files_deleted:
  - tests/orchestrators/plugin/cross-surface-reason-parity.test.ts
  - tests/bridges/integration-materialization-gate.test.ts
autonomous: true
requirements: [OWN-06, OWN-04, DEL-03]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Neither suite has a single owning production module: the reason-parity suite compares a presenter's narrowing against a shared classifier, and the materialization-gate suite drives a parse step, a stage step and a locations bundle together. That is why they relocate rather than fold — D-117-01 sends a supplement to the mirrored owner only when one module owns it."
    - "The correspondence gate stops reporting both as unexpected tests, and reports no row for either new path, because the architecture root is exempt by root. RED when: either file is placed in a corresponding root instead."
    - "Every case each suite carries still passes at the new path with its production specifiers resolving. RED when: the depth change is missed on the suite that climbs three levels today — the tree stops typechecking."
    - "Neither suite is rewritten. Both keep every case they have; this is a relocation, not a rewrite, and its whole value is that git records a rename so the history survives."
    - "This plan does not touch the third relocation candidate. The source-scanning mechanic's own suite is claimed by both D-117-01 and D-117-04, it is ONE move, and 117-02 owns it."
  artifacts:
    - path: tests/architecture/cross-surface-reason-parity.test.ts
      provides: "The cross-surface reason-parity supplement, in the root the correspondence gate exempts"
    - path: tests/architecture/integration-materialization-gate.test.ts
      provides: "The materialization-gate supplement, in the root the correspondence gate exempts"
  key_links:
    - from: tests/architecture/cross-surface-reason-parity.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
      via: "a production import whose climb shortens by one level with the move"
      pattern: "narrowResolverReasons"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not restore an exemption list, an ownership registry or any name-keyed opt-out. SUITE-04 bars them and the gate's root exemption is structural — it needs no new entry."
    - "This plan must not move `tests/helpers/source-scan.test.ts`. That is 117-02's move."
    - "This plan must not rewrite, merge, split or drop any case in either suite."
---

<objective>
Resolve the two orphan supplements that span modules, by relocation rather than by folding.

Purpose: the correspondence gate reports eight violations, seven of them unexpected tests. D-117-01
disposes of each by subject: a supplement folds into the mirrored owner of the module it measures, and
relocates to the architecture root only when it spans modules and so has no single owner. These two
are the span cases. The gate already exempts the architecture, end-to-end and integration roots
structurally, by root, so the relocation needs no gate change and no exemption entry — which matters,
because SUITE-04 bars restoring an exemption-list mechanism.

Neither suite changes. Both keep every case. The whole point is that git records a rename, so the
history reaches back through the move.

Output: two fewer correspondence violations, two suites in the root that owns cross-module evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/116-edge-surface/.continue-here.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the cross-surface reason-parity supplement</name>
  <files>tests/architecture/cross-surface-reason-parity.test.ts</files>
  <read_first>
    - tests/orchestrators/plugin/cross-surface-reason-parity.test.ts
    - scripts/check-corresponding-tests.mjs
  </read_first>
  <action>
`git mv tests/orchestrators/plugin/cross-surface-reason-parity.test.ts tests/architecture/cross-surface-reason-parity.test.ts`
and, in the same commit, shorten the production import specifiers by one level: the suite sits three
levels below the repository root today and two levels below it afterwards. Read the file first and fix
every relative specifier it carries, not only the ones on the first import line — one of its imports
spans several lines and its specifier is on the closing line.

Change nothing else. No case is renamed, merged, split or dropped. If the file's header names its old
directory, correct it to the new one and add no note about having moved: SUITE-03 forbids relocation
history in a test file and git already holds it.

State in the summary WHY this suite has no single owner, in one sentence, from having read it — it
compares a presenter's reason narrowing against a shared classifier, so neither module owns it — and
name the two production modules it spans. That sentence is the justification for relocating instead of
folding, and it is the only thing distinguishing this disposition from a folded one.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/architecture/cross-surface-reason-parity.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/architecture/cross-surface-reason-parity.test.ts &amp;&amp; npm exec -- prettier --check tests/architecture/cross-surface-reason-parity.test.ts &amp;&amp; npm run fallow &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'cross-surface-reason-parity' &amp;&amp; ! rg -n 'Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]|moved from|relocated' tests/architecture/cross-surface-reason-parity.test.ts &amp;&amp; test ! -e tests/orchestrators/plugin/cross-surface-reason-parity.test.ts &amp;&amp; git diff --quiet -- extensions/ package.json</automated>
    <fails_when>Non-zero exit from any link: a `# fail` count above 0 in the focused run, an `error TS` line from typecheck (which is what a missed depth change produces), any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, the correspondence gate still naming this suite in any verdict, the relocation-history scan finding a migration note, the old path still existing, or `git diff --quiet` exiting 1.</fails_when>
  </verify>
  <acceptance_criteria>
The suite is at its new path with a rename recorded, every case it had still passes, and the
correspondence gate names it in no verdict — neither as an unexpected test at the old path nor as
anything at the new one. The summary names the two production modules the suite spans and says in one
sentence why neither owns it. No case was rewritten. `git diff` shows no change under `extensions/`
and none to `package.json`.
  </acceptance_criteria>
  <done>The cross-surface reason-parity supplement sits in the architecture root and one more correspondence violation is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the materialization-gate supplement</name>
  <files>tests/architecture/integration-materialization-gate.test.ts</files>
  <read_first>
    - tests/bridges/integration-materialization-gate.test.ts
    - scripts/check-corresponding-tests.mjs
  </read_first>
  <action>
`git mv tests/bridges/integration-materialization-gate.test.ts tests/architecture/integration-materialization-gate.test.ts`.

This one is a pure move: the suite already sits two levels below the repository root and stays two
levels below it, so every production specifier it carries is unchanged. Read the file and confirm that
before you rely on it — if any specifier does change, fix it in the same commit.

Change nothing else. No case is renamed, merged, split or dropped, and no note about having moved is
added.

State in the summary WHY this suite has no single owner, in one sentence, from having read it — it
drives a parse step, a stage step and a scope-rooted locations bundle together — and name the
production modules it spans.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/architecture/integration-materialization-gate.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/architecture/integration-materialization-gate.test.ts &amp;&amp; npm exec -- prettier --check tests/architecture/integration-materialization-gate.test.ts &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'integration-materialization-gate' &amp;&amp; ! rg -n 'Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]|moved from|relocated' tests/architecture/integration-materialization-gate.test.ts &amp;&amp; test ! -e tests/bridges/integration-materialization-gate.test.ts &amp;&amp; git diff --quiet -- extensions/ package.json</automated>
    <fails_when>Non-zero exit from any link: a `# fail` count above 0 in the focused run, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` or `npm test` exit, the correspondence gate still naming this suite, the relocation-history scan finding a migration note, the old path still existing, or `git diff --quiet` exiting 1.</fails_when>
  </verify>
  <acceptance_criteria>
The suite is at its new path with a rename recorded at full similarity, every case it had still
passes, and the correspondence gate names it in no verdict. `npm test` passes and the summary reports
its total read from the runner's own `ℹ tests` line, unchanged because no case was added or removed.
The summary names the production modules the suite spans. `git diff` shows no change under
`extensions/` and none to `package.json`.
  </acceptance_criteria>
  <done>The materialization-gate supplement sits in the architecture root and one more correspondence violation is resolved.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. This plan relocates two test files and changes no runtime
surface.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-04-A | Repudiation | the evidence these two suites carry, which no mirrored owner reproduces | high | mitigate | Both are moved with every case intact and each is run individually at its new path; the summary names the modules each spans so a later reader can tell relocation from deletion. |
| T-117-04-B | Tampering | the correspondence gate's violation set | medium | mitigate | The gate is re-run after each move and asserted to name neither suite in any verdict, which distinguishes a resolved orphan from one merely renamed into a second violation. |
| T-117-04-C | Tampering | production sources and the npm script globs | high | mitigate | Both verify blocks end with `git diff --quiet -- extensions/ package.json`. |
</threat_model>

<verification>
Run each gate separately and read its exit code. Do not use `npm run check`: its `format:check` link
fails on the operator's pre-existing untracked files and short-circuits before the tests run. Do not
touch those files and do not edit `.prettierignore`.

Commit twice, one per task, so `git log --follow` works on each moved file. Before each commit, scan
the changed path for secrets by the filesystem route with an explicit literal path — this shell does
not word-split and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem <the literal path> --results=verified,unknown --fail`, confirming non-zero
chunk and byte counts. Then `npm exec -- prettier --check <the literal path>` and
`SKIP=trufflehog,npm-format-check pre-commit run --files <the literal path>`. Git hooks are not
installed here, so a clean `git commit` is not evidence hooks passed. Never `--all-files`, never
`--no-verify`. Use `git commit -F`. Stage only the literal paths this plan owns; never `git add -A`.

Confirm each commit with `git status --short` and `git log -1 --stat`, and check the rename appears.
</verification>

<success_criteria>

- Both suites sit under `tests/architecture/`, each with a rename recorded.
- The correspondence gate names neither suite in any verdict — two more violations resolved.
- Every case in both suites still passes, and no case was rewritten.
- No exemption list, ownership registry or name-keyed opt-out was created.
- Nothing under `extensions/` and nothing in `package.json` changed.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-04-SUMMARY.md` when done.
</output>
