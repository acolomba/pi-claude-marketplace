---
phase: 115-composition-orchestrators
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/orchestrators/plugin/bootstrap.test.ts
  - tests/orchestrators/plugin/scope-tree-inventory.ts
autonomous: true
requirements: [MOD-08]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - "Calling bootstrapClaudePlugin twice against one tree in a single case produces the complete three-row notification log across both calls, leaves the state bytes after the second call identical to the bytes after the first, and leaves the scope-root inventory identical."
    - "D-115-03: bootstrap composes the real addMarketplace and setMarketplaceAutoupdate against a case-owned temporary tree with only the network edge faked; no injection seam is added to bootstrap.ts."
    - "Every case owns one temporary tree and registers its removal plus the HOME and agent-directory restore through t.after(), and the fail-fast git fake refuses every remote outside the declared allow-list."
    - "The ExtensionContext and ExtensionAPI doubles are strict interaction mocks with exact parameters and explicit final verification, so the notification cardinality is a promise rather than an accident."
    - "The file header carries no work-session narrative; the durable clone-before-name-check ordering fact survives as a one-line comment on the case that depends on it."
  artifacts:
    - path: tests/orchestrators/plugin/bootstrap.test.ts
      provides: "Sole mirrored direct owner for orchestrators/plugin/bootstrap.ts, contract-compliant, with a genuine repeated-call proof"
    - path: tests/orchestrators/plugin/scope-tree-inventory.ts
      provides: "Header widened once to name every consuming owner; this plan is its sole editor in the phase"
  key_links:
    - from: tests/orchestrators/plugin/bootstrap.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
      via: "direct bootstrapClaudePlugin import driving the real composed orchestrators"
      pattern: "bootstrapClaudePlugin"
---

<objective>
Re-prove one-keystroke onboarding and close the phase's repeated-call success criterion.

Purpose: `bootstrap.ts` already passes direct coverage at 5/5 branches, 1/1 functions, and
134/134 lines, but success criterion 4 of the phase — "bootstrap and pending-state behavior
remain idempotent and stable across repeated calls" — is not actually proved. All three
existing idempotence cases seed the post-state by hand and call `bootstrapClaudePlugin` exactly
once. None calls it twice. On top of that the suite has no phase markers, three doubles built
by casting through `unknown`, an existence assertion where the whole record is the promise, and
a file header carrying a work-session narrative that SUITE-03 forbids.

D-115-03 settles the double strategy: bootstrap's contract is the resulting on-disk state, so
it drives the real `addMarketplace` and `setMarketplaceAutoupdate` against a case-owned tree
with only the network edge faked, and no seam is added to production for the test.

Output: a contract-compliant owner that calls the orchestrator twice in one case and pins the
notifications, the state bytes, and the tree.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-composition-orchestrators/115-CONTEXT.md
@.planning/phases/115-composition-orchestrators/115-RESEARCH.md
@.planning/phases/115-composition-orchestrators/115-PATTERNS.md
@.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the bootstrap owner and add a genuine two-call idempotence proof</name>
  <files>tests/orchestrators/plugin/bootstrap.test.ts, tests/orchestrators/plugin/scope-tree-inventory.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
    - tests/orchestrators/plugin/bootstrap.test.ts
    - tests/orchestrators/plugin/uninstall.test.ts
    - tests/orchestrators/plugin/scope-tree-inventory.ts
    - tests/orchestrators/marketplace/add.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - tests/platform/git-ops-fake.ts
    - .planning/phases/115-composition-orchestrators/115-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/orchestrators/plugin/bootstrap.test.ts` against the on-disk regime that
`tests/orchestrators/plugin/uninstall.test.ts` already proves, and add the repeated-call case
the phase's fourth success criterion needs.

Case structure. `bootstrapClaudePlugin` is the single exported entrypoint, so cases stay at the
top level. Every runtime case carries separate lowercase `// arrange`, `// act`, `// assert`
phases in that order, blank-line separated, with the combined marker reserved for a single
throwing or rejecting expression. Case titles state public behavior; keep durable requirement
and decision anchors, drop everything that names a planning artifact.

Add the repeated-call case. In one case, create the hermetic user scope, build the context
doubles sized for three notifications, build the git fake over the official-marketplace fixture,
call `bootstrapClaudePlugin` twice against the same working directory, and between the two calls
capture the state bytes and the scope-root inventory. Then assert, in one assertion block: the
complete notification array across both calls as three whole records, in order — the added row,
the autoupdate marker row, and the already-autoupdate row; the state file bytes after the
second call equal the bytes captured after the first; the scope-root inventory after the second
call equals the inventory captured after the first; and the clone call count is two, with a
one-line comment recording why — a GitHub source derives the marketplace name from inside the
cloned manifest, so `addMarketplace` clones before it can run the duplicate-name check, and the
idempotent re-run therefore clones once more. That is existing designed behavior, not a defect.
Read `bootstrap.ts` and confirm the exact shipped notification strings from the current suite
before writing them down; the shipped bytes outrank any expectation in this description.

Keep and normalize the five existing behavioral cases: clean state, already bootstrapped,
half-configured autoupdate flip, user-scope-only with no project state file created, and the
non-duplicate clone error that propagates while the autoupdate step is never reached.

Doubles. Replace the three doubles built by casting through `unknown` with the strict-mock
recipe from `tests/orchestrators/marketplace/add.test.ts:173-201`:
`mock<ExtensionAPI>({ exactParams: true, name: "extension API" })`,
`mock<ExtensionContext>({ exactParams: true, name: "extension context" })`, and a notification
user-interface mock, each created inside the case, each with its complete promised interaction
stated by exact call count, and each explicitly verified at the end of the case after the
result and state assertions. Do not use wildcard argument matchers or unbounded call counts.
Keep the network edge on `createGitOpsFake` with an explicit remote allow-list so an unexpected
remote fails fast; that allow-list, not the absence of a call, is the offline proof.

Hermetic scope. Replace the `finally`-only environment restore with one `mkdtemp` root per case
and a `t.after()`, registered before the act phase, that removes every temporary root, restores
`HOME` to its prior presence and value, and restores or deletes `PI_CODING_AGENT_DIR`.

Assertions. Replace existence checks such as testing that a marketplace key is present with a
whole-value `assert.deepStrictEqual` on the complete state record. Compare the config and state
files as complete bytes where the bytes are the contract. Use the shared
`tests/orchestrators/plugin/scope-tree-inventory.ts` helper for the path inventory rather than
adding a fourth local snapshot copy.

File header. Delete the work-session narrative at the end of the header block — the two
sentences that weigh an earlier planning expectation against the behavior the suite observed.
Keep the durable fact about clone-before-name-check ordering, moved to a single line on the
case that depends on it.

Shared helper header. This plan is the sole editor of
`tests/orchestrators/plugin/scope-tree-inventory.ts` in this phase: widen its first header
sentence so it names the bootstrap and reconcile owners as consumers alongside the install,
reinstall, and uninstall proofs. Change no behavior in that module. P115-05 and P115-06 import
it read-only and must not touch it.

Coverage. `bootstrap.ts` is at 100 percent today; re-verify after the rewrite. Add no
production seam, export, pragma, or coverage exception, and change no file under
`extensions/`.
  </action>
  <verify><automated>node --test tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/plugin/bootstrap.test.ts tests/orchestrators/plugin/scope-tree-inventory.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/plugin/bootstrap.test.ts tests/orchestrators/plugin/scope-tree-inventory.ts &amp;&amp; npm run fallow &amp;&amp; node --test tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts &amp;&amp; rg -q 't\.after\(' tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; rg -q 'bootstrapClaudePlugin' tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)|pre-execution claim' tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; git diff --check -- tests/orchestrators/plugin/bootstrap.test.ts tests/orchestrators/plugin/scope-tree-inventory.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts extensions/pi-claude-marketplace/orchestrators/marketplace</automated></verify>
  <acceptance_criteria>
`node --test tests/orchestrators/plugin/bootstrap.test.ts` passes alone and
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts`
reports 100 percent functions, lines, and branches with no coverage exception added. One case
calls `bootstrapClaudePlugin` twice and asserts, as whole values, the complete three-row
notification log across both calls, byte-identical state after the second call, an identical
scope-root inventory, and the clone count with its one-line reason. Every runtime case has
separate lowercase arrange, act, and assertion phases, and the count of arrange markers equals
the count of runtime cases. Every case owns one temporary tree with removal and environment
restore registered through `t.after()`. Every context and Pi double is a strict mock with
exact parameters and an explicit final verification; no double assertion through `unknown`
remains. The git fake carries an explicit remote allow-list. The header work-session narrative
is gone. The three sibling plugin owners that share the inventory helper still pass. `git diff`
shows no change under `extensions/`. `npm run check` is green end to end, allowing only the
pre-existing local `format:check` noise from untracked `.mcp.json` and
`.planning/research/.cache/*.json` files, which this plan must not touch.
  </acceptance_criteria>
  <done>P115-04 proves onboarding idempotence across repeated calls and is contract-compliant at 100 percent direct coverage.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded by this plan, so the package-legitimacy gate does not apply.

| Threat ID  | Category                           | Component                              | Severity | Disposition | Mitigation Plan                                                                                                                          |
| ---------- | ---------------------------------- | -------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| T-115-04-A | Spoofing / Information Disclosure  | git remote and credential boundary     | high     | mitigate    | `createGitOpsFake` with an explicit remote allow-list; any unlisted remote fails fast; no developer credential and no live remote is reachable. |
| T-115-04-B | Tampering                          | temporary trees, `HOME`, `PI_CODING_AGENT_DIR` | high     | mitigate    | One `mkdtemp` root per case; removal and environment restore registered through `t.after()` before the act phase; user-scope writes are contained in the hermetic home. |
| T-115-04-C | Repudiation                        | notification cardinality               | medium   | mitigate    | Strict mocks with exact parameters and explicit verification make the notification count a promise; the two-call case pins the complete ordered log. |

</threat_model>

<verification>
Run the focused owner alone, the direct per-pair coverage gate, repository typecheck, scoped
lint and format checks, the whole-repository `fallow` chain, the three sibling plugin owners
that consume the shared inventory helper, and the prohibited-pattern scan; confirm no
production file changed. Before committing, run `pre-commit run --all-files` and
`npm run check`; the only tolerated failure is the pre-existing untracked-file `format:check`
noise described in the acceptance criteria.
</verification>

<success_criteria>

- Repeated `bootstrapClaudePlugin` calls are proved idempotent within one case, by notification
  log, state bytes, and tree inventory.
- `orchestrators/plugin/bootstrap.ts` keeps 100 percent direct functions, lines, and branches.
- The real composition runs with only the network edge faked and no production seam added.
- Every double is role-correct, strict, and explicitly verified.
- The shared tree-inventory helper is widened once, by this plan alone.

</success_criteria>

<output>
Create .planning/phases/115-composition-orchestrators/115-04-SUMMARY.md when done.
</output>
