---
phase: 115-composition-orchestrators
plan: 06
type: execute
wave: 2
depends_on: ["115-04"]
files_modified:
  - tests/orchestrators/reconcile/backfill.test.ts
autonomous: true
requirements: [MOD-08]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "The version-stamp gate, the force-installed scan, the strict-superset promotion rule, and the per-plugin isolation arms are proved through the backfill entrypoint with complete, independently authored outcomes and complete on-disk state."
    - "A pristine scope with no state to stamp, a populated state reached on the state-file-absent path, a failed re-materialize carrying no reasons, and a promotion whose re-resolve reports an orphan rewake or a degraded component are each proved by a behavioral case."
    - "D-115-03: backfill drives the real re-materialize against a case-owned temporary tree with only the network edge faked; no injection seam is added to backfill.ts."
    - "No double is built by casting through unknown, and no process-wide runner mock tracker is imported; every double comes from the current test context or from a strict interaction mock created inside the case."
    - "Every case owns one temporary tree and registers its removal plus the HOME and agent-directory restore through t.after()."
  artifacts:
    - path: tests/orchestrators/reconcile/backfill.test.ts
      provides: "Sole mirrored direct owner for orchestrators/reconcile/backfill.ts at 100 percent direct coverage"
  key_links:
    - from: tests/orchestrators/reconcile/backfill.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts
      via: "direct backfill entrypoint import driving the real gated re-materialize"
      pattern: "backfill"
---

<objective>
Close the backfill scan owner: six branches and a full contract normalization.

Purpose: `backfill.ts` is the smallest real coverage gap among the reconcile pairs — 54 of 60
branches and 452 of 461 lines — and every missing branch is reachable through the module's own
exported surface with no production change. The suite already proves twenty strong behaviors:
the version-stamp gate in both directions, full and partial promotion, the strict-superset
rule, the no-network scan, three isolation rules, the disabled-partial skip with its enabled
control, both failure arms, and per-plugin isolation. What it lacks is the contract shape: no
phase markers anywhere, nine doubles built by casting through `unknown`, and the process-wide
mock tracker imported from the runner instead of taken from the case context.

D-115-03 settles the double strategy: backfill's contract is the resulting on-disk state, so it
drives the real re-materialize against a case-owned tree with only the network edge faked, and
no seam is added to production for the test.

Output: a contract-compliant owner at 100 percent direct coverage with no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-composition-orchestrators/115-CONTEXT.md
@.planning/phases/115-composition-orchestrators/115-RESEARCH.md
@.planning/phases/115-composition-orchestrators/115-PATTERNS.md
@.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the backfill owner and replace its doubles</name>
  <files>tests/orchestrators/reconcile/backfill.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/orchestrators/reconcile/backfill.test.ts
    - tests/orchestrators/plugin/uninstall.test.ts
    - tests/orchestrators/plugin/scope-tree-inventory.ts
    - tests/orchestrators/marketplace/add.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - tests/platform/git-ops-fake.ts
    - .planning/phases/115-composition-orchestrators/115-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite the existing twenty cases in `tests/orchestrators/reconcile/backfill.test.ts` to the
contract, using `tests/orchestrators/plugin/uninstall.test.ts` as the analog for the on-disk
regime. Preserve every behavior the suite already proves; this is a shape change, not a scope
reduction.

Read the module first and organize the file by what it actually exports: one top-level
`describe()` per exported entrypoint, never nested. Every runtime case carries separate
lowercase `// arrange`, `// act`, `// assert` phases in that order, blank-line separated, with
the combined marker reserved for one throwing or rejecting expression. Data rows still use
separate phases.

Replace the piecemeal single-value and existence assertions with one `assert.deepStrictEqual`
per public surface: the complete returned outcome list, the complete state record read back
through the persistence loader, and the complete scope-root path inventory. Use the shared
`tests/orchestrators/plugin/scope-tree-inventory.ts` helper for the inventory rather than
adding another local snapshot copy. Import it read-only and do not edit it — P115-04 is its
sole editor in this phase.

Delete the process-wide mock tracker import from the runner near line 26. Every double must
come from the current test context (`t.mock.fn()`, `t.mock.method()`) or from a strict
interaction mock created inside the case. State leaking across cases through a shared tracker
is exactly what that rule prevents.

Replace the nine doubles built by casting through `unknown` with hand-built typed literals
checked by `satisfies`, following the record factory shape at
`tests/orchestrators/plugin/uninstall.test.ts:99-113`: every field written out, fixed
timestamps so the record stays byte-comparable, no broad partial widening. Replace the
module-scope shared Pi handle with a per-case
`mock<ExtensionAPI>({ exactParams: true, name: "extension API" })`, and build the context and
notification doubles as strict mocks with exact parameters, complete promised call counts, and
an explicit final verification placed after the result and state assertions.

Keep `createGitOpsFake` at the network edge with an explicit remote allow-list, so an
unexpected remote fails fast. That allow-list is the offline proof, not the absence of a call.

Replace any environment restore that lives only in a `finally` with one `mkdtemp` root per case
and a `t.after()` registered before the act phase that removes every temporary root, restores
`HOME` to its prior presence and value, and restores or deletes `PI_CODING_AGENT_DIR`.

Rename placeholder-named values to production roles. Case titles state public behavior, may
cite a durable requirement or decision ID, and cite no plan, phase, wave, or ticket. Convert
any row loop inside a case into a `for` loop that wraps `test()`, one sibling case per row,
with a `satisfies readonly {...}[]` tail on the row array so a missing or misspelled field is a
compile error.

Change no file under `extensions/`.
  </action>
  <verify><automated>node --test tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; npm run fallow &amp;&amp; rg -q 't\.after\(' tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; ! rg -n 'import test, \{ mock \} from "node:test"' tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts tests/orchestrators/plugin/scope-tree-inventory.ts</automated></verify>
  <acceptance_criteria>
`node --test tests/orchestrators/reconcile/backfill.test.ts` passes alone with every behavior
the suite proved before still proved. Every runtime case has separate lowercase arrange, act,
and assertion phases, and the count of arrange markers equals the count of runtime cases. No
double assertion through `unknown`, no process-wide runner mock tracker, no module-scope shared
Pi handle, no wildcard matcher, and no unbounded-count expectation remains. Every case owns one
temporary tree with removal and environment restore registered through `t.after()`. The git
fake carries an explicit remote allow-list. `git diff` shows no change under `extensions/` and
none to the shared tree-inventory helper.
  </acceptance_criteria>
  <done>The backfill owner is contract-shaped, hermetic, and free of casts and shared trackers.</done>
</task>

<task type="auto">
  <name>Task 2: Close the six uncovered backfill branches with behavioral cases</name>
  <files>tests/orchestrators/reconcile/backfill.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
    - tests/orchestrators/reconcile/backfill.test.ts
    - .planning/phases/115-composition-orchestrators/115-RESEARCH.md
  </read_first>
  <action>
Add the cases that close the remaining branches. Re-run the direct coverage gate first and work
from the current zero-hit records rather than trusting the list below: research pinned its
numbers to an earlier tree, and Task 1 changed the file.

The six expected gaps, each with the situation it needs:

- the per-scope entry's guard for an absent state, taken when the read pass yields no state at
  all — a genuinely pristine scope;
- the body of the loop over recorded marketplaces inside the force-installed predicate, which
  today only ever runs against a state with zero marketplaces — give it a populated state on
  the path where the state file is absent;
- the fallback operand that classifies a failed re-materialize when the outcome carries no
  reasons at all, so the notes are joined into a synthetic error and classified instead;
- the conditional spread that records an orphan rewake on a promotion whose re-resolve reports
  one;
- the two operands of the conditional spread that records degraded component kinds on a
  promotion whose re-materialize degraded a component.

Provoke each by shaping the inputs on disk — the state file's presence and content, the
recorded plugin's resources, the marketplace fixture's manifest, the stored version stamp —
never by adding a parameter to production and never by casting an impossible value into an
entrypoint. Each case supplies complete case-local inputs and one complete independently
authored expectation: the whole returned outcome list, and the whole state record read back
afterwards.

If a branch will not close through the exported surface, do not add a coverage exception and do
not fabricate a value. Report the exact branch, why it resists, and the caller trace, so its
disposition can be decided rather than papered over.

Change no file under `extensions/`.
  </action>
  <verify><automated>node --test tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; git diff --check -- tests/orchestrators/reconcile/backfill.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts</automated></verify>
  <acceptance_criteria>
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts`
reports 100 percent functions, lines, and branches with the owner run alone, and no coverage
exception was added. Each newly closed branch is closed by a behavioral case whose expectation
is a complete, hand-authored value, provoked by shaping inputs on disk rather than by a seam or
a cast. `git diff` shows no change under `extensions/`. `npm run check` is green end to end,
allowing only the pre-existing local `format:check` noise from untracked `.mcp.json` and
`.planning/research/.cache/*.json` files, which this plan must not touch.
  </acceptance_criteria>
  <done>P115-06 is at 100 percent direct coverage with no production change.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded by this plan, so the package-legitimacy gate does not apply.

| Threat ID  | Category                          | Component                              | Severity | Disposition | Mitigation Plan                                                                                                                             |
| ---------- | --------------------------------- | -------------------------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| T-115-06-A | Spoofing / Information Disclosure | git remote boundary during re-materialize | high     | mitigate    | `createGitOpsFake` with an explicit remote allow-list; the backfill scan is documented no-network and any unlisted remote fails fast.           |
| T-115-06-B | Tampering                         | temporary trees, `HOME`, `PI_CODING_AGENT_DIR` | high     | mitigate    | One `mkdtemp` root per case; removal and environment restore registered through `t.after()` before the act phase.                              |
| T-115-06-C | Elevation of Privilege            | shared runner mock tracker             | medium   | mitigate    | The process-wide tracker import is deleted; every double is context-owned or created inside its case, so no case can influence another.        |
| T-115-06-D | Repudiation                       | coverage evidence                      | medium   | mitigate    | Branches close through behavioral cases only; no exception, pragma, or fabricated value is permitted, and an unclosable branch is reported instead. |

</threat_model>

<verification>
Run the focused owner alone, the direct per-pair coverage gate, repository typecheck, scoped
lint and format checks, the whole-repository `fallow` chain, and the prohibited-pattern scan;
confirm no production file and no shared helper changed. Before committing, run
`pre-commit run --all-files` and `npm run check`; the only tolerated failure is the pre-existing
untracked-file `format:check` noise described in the acceptance criteria.
</verification>

<success_criteria>

- `orchestrators/reconcile/backfill.ts` reaches 100 percent direct functions, lines, and
  branches with its owner run alone.
- Every previously proved behavior survives, and the six missing branches close through
  disk-shaped inputs.
- Every double is role-correct, typed without casts, and owned by its case.
- Every case is hermetic and the network edge fails fast on an unexpected remote.
- No production file changes.

</success_criteria>

<output>
Create .planning/phases/115-composition-orchestrators/115-06-SUMMARY.md when done.
</output>
