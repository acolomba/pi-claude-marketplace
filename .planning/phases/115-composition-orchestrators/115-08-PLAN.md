---
phase: 115-composition-orchestrators
plan: 08
type: execute
wave: 2
depends_on: ["115-04", "115-07"]
files_modified:
  - tests/orchestrators/reconcile/pending.test.ts
autonomous: true
requirements: [MOD-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Two invocations against unchanged inputs produce byte-identical notification arguments and leave every file under both scope roots byte-identical, so the pending advisory is idempotent and mutates nothing."
    - "A state-load failure whose cause is not a syntax error narrows through the general probe classifier, and a malformed local configuration file is reported as an invalid block just as the base file already is."
    - "D-115-03: pending reads real config and state from a case-owned temporary tree and delegates the projection; no injection seam is added to pending.ts."
    - "No double is built by casting through unknown, no module-scope shared Pi handle survives, and no process-wide runner mock tracker is imported."
    - "The sanctioned legacy-migration warning is either arranged away or captured and asserted through a case-owned console replacement; the console is never globally silenced."
  artifacts:
    - path: tests/orchestrators/reconcile/pending.test.ts
      provides: "Sole mirrored direct owner for orchestrators/reconcile/pending.ts at 100 percent direct coverage"
  key_links:
    - from: tests/orchestrators/reconcile/pending.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
      via: "direct pending entrypoint import asserted through notification arguments and an unchanged tree"
      pattern: "pending"
---

<objective>
Close the pending advisory owner: two branches and the heaviest cast load in the phase.

Purpose: `pending.ts` is the best-shaped of the seven existing suites behaviorally — it already
proves the empty steady state, two-invocation byte-identical idempotency, mutation-free
snapshots, the invalid-manifest abort that reports a basename rather than an absolute path,
corrupt-state containment, ordering, project-first scope fan-out, three pre-migration-window
cases, and five force-preview partitions. Phase success criterion 4's pending half is therefore
already satisfied behaviorally and needs preserving, not inventing.

The debt is elsewhere. Twenty doubles are built by casting through `unknown` in only 769 lines,
including a module-scope shared Pi handle; the process-wide mock tracker is imported from the
runner; no case carries phase markers; and two branches remain uncovered — a state-load failure
whose cause is not a syntax error, and a malformed local configuration file.

D-115-03 settles the double strategy: pending reads real config and state off a real tree and
delegates its projection, so no seam is added to production for the test.

Output: a contract-compliant owner at 100 percent direct coverage with no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-composition-orchestrators/115-CONTEXT.md
@.planning/phases/115-composition-orchestrators/115-RESEARCH.md
@.planning/phases/115-composition-orchestrators/115-PATTERNS.md
@.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the pending owner and eliminate its twenty casts</name>
  <files>tests/orchestrators/reconcile/pending.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/orchestrators/reconcile/pending.test.ts
    - tests/orchestrators/plugin/list.test.ts
    - tests/orchestrators/plugin/scope-tree-inventory.ts
    - tests/orchestrators/marketplace/add.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - .planning/phases/115-composition-orchestrators/115-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite the sixteen existing cases in `tests/orchestrators/reconcile/pending.test.ts` to the
contract, using `tests/orchestrators/plugin/list.test.ts` as the exact analog: same shape —
read state and config across scopes, mutate nothing, emit one notification. Preserve every
behavior the suite already proves; this is a shape change, not a scope reduction.

Read the module first and organize the file by what it exports: one top-level `describe()` per
exported entrypoint, never nested. Every runtime case carries separate lowercase `// arrange`,
`// act`, `// assert` phases in that order, blank-line separated, with the combined marker
reserved for one throwing or rejecting expression.

Eliminate all twenty doubles built by casting through `unknown`, starting with the module-scope
shared Pi handle near line 41. Build each per case with
`mock<ExtensionAPI>({ exactParams: true, name: "extension API" })`,
`mock<ExtensionContext>({ exactParams: true, name: "extension context" })`, and a notification
user-interface mock, each with its complete promised interaction stated by exact call count and
an explicit final verification after the result and state assertions. Sizing the mock to zero
expected notifications is how the silent-advisory cases prove silence: an unpromised call
throws immediately.

Delete the process-wide mock tracker import from the runner near line 26; every double must
come from the current test context or from a strict mock created inside the case.

Keep the no-mutation proof and make it whole-value. Follow the composition at
`tests/orchestrators/plugin/uninstall.test.ts:3032-3050`: use the shared
`tests/orchestrators/plugin/scope-tree-inventory.ts` helper for the complete path inventory
under each scope root, plus explicit byte equality on the two files that carry the contract,
the state file and the configuration file. Do not add a fifth local byte-snapshot copy — with
the duplication gate at its current threshold that is a real risk, and the composition above
gives the same byte-level promise. Import the shared helper read-only; P115-04 is its sole
editor in this phase.

Assert the notification arguments as one complete array compared whole, and keep the existing
two-invocation idempotency case, which is what satisfies the phase's fourth success criterion
for pending. Replace piecemeal single-value and existence assertions accordingly.

Replace any environment restore that lives only in a `finally` with one `mkdtemp` root per case
and a `t.after()` registered before the act phase that removes every temporary root, restores
`HOME` to its prior presence and value, and restores or deletes `PI_CODING_AGENT_DIR`.

Handle the legacy-migration warning explicitly rather than by accident. Two pre-migration cases
currently make production emit its sanctioned legacy-migration warning to standard error, which
is uncontrolled noise in the gate output. Choose per case: either shape the fixture so the
migration save succeeds and the warning does not fire, or, where the unpersistable migration is
genuinely the case's promise, replace the console method through the current test context and
assert the complete argument list. Do not globally silence the console and do not change
production — that warning is the single sanctioned exception in the codebase.

Rename placeholder-named values to production roles. Case titles state public behavior, may
cite a durable requirement or decision ID, and cite no plan, phase, wave, or ticket. Convert
any row loop inside a case into a `for` loop that wraps `test()`, one sibling case per row,
with a `satisfies readonly {...}[]` tail on the row array.

Change no file under `extensions/`.
  </action>
  <verify><automated>node --test tests/orchestrators/reconcile/pending.test.ts > /tmp/p115-08-pending.log 2>&amp;1; PENDING_RC=$?; cat /tmp/p115-08-pending.log; test "$PENDING_RC" -eq 0 &amp;&amp; ! rg -q 'Legacy marketplace migration' /tmp/p115-08-pending.log &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/reconcile/pending.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/reconcile/pending.test.ts &amp;&amp; npm run fallow &amp;&amp; rg -q 't\.after\(' tests/orchestrators/reconcile/pending.test.ts &amp;&amp; ! rg -n 'import test, \{ mock \} from "node:test"' tests/orchestrators/reconcile/pending.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/reconcile/pending.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts tests/orchestrators/plugin/scope-tree-inventory.ts</automated></verify>
  <acceptance_criteria>
`node --test tests/orchestrators/reconcile/pending.test.ts` passes alone with every behavior
the suite proved before still proved, including the two-invocation byte-identical idempotency
case. The run emits no uncontrolled legacy-migration warning on standard error: each such case
either arranges the warning away or captures it through a case-owned console replacement and
asserts its complete arguments. Every runtime case has separate lowercase arrange, act, and
assertion phases, and the count of arrange markers equals the count of runtime cases. All
twenty double assertions through `unknown` are gone, including the module-scope shared Pi
handle; no process-wide runner mock tracker, wildcard matcher, or unbounded-count expectation
remains, and every mock is explicitly verified. The no-mutation proof compares the complete
path inventory plus explicit bytes for the state and configuration files, with no new
byte-snapshot helper added. `git diff` shows no change under `extensions/` and none to the
shared tree-inventory helper.
  </acceptance_criteria>
  <done>The pending owner is contract-shaped, cast-free, hermetic, and quiet.</done>
</task>

<task type="auto">
  <name>Task 2: Close the two uncovered pending branches</name>
  <files>tests/orchestrators/reconcile/pending.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
    - extensions/pi-claude-marketplace/shared/probe-classifiers.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/orchestrators/reconcile/pending.test.ts
    - .planning/phases/115-composition-orchestrators/115-RESEARCH.md
  </read_first>
  <action>
Re-run the direct coverage gate first and work from the current zero-hit records; research
pinned its numbers to an earlier tree and Task 1 changed the file.

The two expected gaps:

- the fall-through return in the state-load failure classifier, which delegates to the general
  probe classifier. Today only the arm for a cause that is a syntax error runs. Provoke the
  other arm with a state-load failure whose cause is something else — an unreadable state file
  producing a permission error, or a pre-held advisory lock, both of which the repository
  already provokes deterministically elsewhere (the lock file is configured with no retries, so
  a held lock surfaces immediately as the typed lock-held error). Assert the complete resulting
  notification, including the reason token, and assert the typed error by class and stable
  fields wherever an error reaches the surface.
- the guard that reports an invalid local configuration file. Only the base configuration file
  is proved invalid today. Write a malformed local configuration file and assert the complete
  reported block, including that the diagnostic names the local file's basename rather than an
  absolute path, matching the existing base-file case's contract.

Provoke both by shaping inputs on disk. Do not add a parameter to production, do not cast an
impossible value into an entrypoint, and do not add a coverage exception. If a branch will not
close through the exported surface, report the exact branch, why it resists, and the caller
trace.

Change no file under `extensions/`.
  </action>
  <verify><automated>node --test tests/orchestrators/reconcile/pending.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/reconcile/pending.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/reconcile/pending.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/reconcile/pending.test.ts &amp;&amp; git diff --check -- tests/orchestrators/reconcile/pending.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts</automated></verify>
  <acceptance_criteria>
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts`
reports 100 percent functions, lines, and branches with the owner run alone, and no coverage
exception was added. Both new cases are behavioral, provoked by shaping inputs on disk, and
assert one complete independently authored notification; the invalid-local-configuration case
also pins the basename-not-absolute-path diagnostic contract. `git diff` shows no change under
`extensions/`. `npm run check` is green end to end, allowing only the pre-existing local
`format:check` noise from untracked `.mcp.json` and `.planning/research/.cache/*.json` files,
which this plan must not touch.
  </acceptance_criteria>
  <done>P115-08 is at 100 percent direct coverage with no production change.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded by this plan, so the package-legitimacy gate does not apply.

| Threat ID  | Category               | Component                                      | Severity | Disposition | Mitigation Plan                                                                                                                            |
| ---------- | ---------------------- | ---------------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-115-08-A | Tampering              | temporary trees, `HOME`, `PI_CODING_AGENT_DIR` | high     | mitigate    | One `mkdtemp` root per case; removal and environment restore registered through `t.after()` before the act phase.                              |
| T-115-08-B | Tampering              | the no-mutation promise                        | high     | mitigate    | Complete path inventory under both scope roots plus explicit byte equality on the state and configuration files, compared before and after.    |
| T-115-08-C | Information Disclosure | diagnostic path leakage                        | medium   | mitigate    | The invalid-local-configuration case pins the basename-only diagnostic, matching the shipped contract for the base file.                       |
| T-115-08-D | Repudiation            | uncontrolled warning output                    | medium   | mitigate    | The sanctioned legacy-migration warning is arranged away or captured and asserted per case; the console is never globally silenced and production is unchanged. |

</threat_model>

<verification>
Run the focused owner alone and confirm no uncontrolled migration warning reaches standard
error, then the direct per-pair coverage gate, repository typecheck, scoped lint and format
checks, the whole-repository `fallow` chain, and the prohibited-pattern scan; confirm no
production file and no shared helper changed. Before committing, run `pre-commit run --all-files`
and `npm run check`; the only tolerated failure is the pre-existing untracked-file
`format:check` noise described in the acceptance criteria.
</verification>

<success_criteria>

- `orchestrators/reconcile/pending.ts` reaches 100 percent direct functions, lines, and branches
  with its owner run alone.
- Repeated pending invocations remain byte-identical in output and mutate nothing, proved by a
  whole-value inventory and explicit file bytes.
- Every double is a strict, typed, case-owned mock with an explicit verification; no cast
  through `unknown` survives.
- The suite produces no uncontrolled warning output and does not silence the console.
- No production file changes.

</success_criteria>

<output>
Create .planning/phases/115-composition-orchestrators/115-08-SUMMARY.md when done.
</output>
