---
phase: 88-agent-settled-dispatcher-stop-contract-stopfailure
plan: 03
type: execute
wave: 3
depends_on: ["88-02"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
  - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - docs/output-catalog.md
  - tests/bridges/hooks/settle.test.ts
  - tests/architecture/hooks-dispatch.test.ts
  - tests/architecture/hooks-cap-notify.test.ts
autonomous: true
requirements: [STOP-07]
must_haves:
  truths:
    - "A per-session `stop_hook_active` flag is set when the bridge blocks-and-re-enters and cleared on the next genuine `input` event; bridge-injected custom messages do NOT pass through `input`, so the flag never self-clears (STOP-07)."
    - "The consecutive-block counter increments ONLY on a block outcome; ANY non-block outcome (allow/noop, additionalContext-without-block, continue:false) RESETS it to zero (D-88-06 — upstream counts 8 *consecutive* blocks)."
    - "The 8th consecutive block trips the override cap: the bridge does NOT re-enter and emits a one-shot warning notify; the one-shot latch is per-session and re-arms when the counter resets (D-88-06)."
    - "The cap-trip warning is warning severity via a `shared/notify.ts` seam through `ctx.ui.notify` (IL-2), with a non-empty summary first line on the `Warning:` grammar and a `\\n\\n` detail block naming the plugin and stating 8 consecutive blocks / turn ended despite an active block (D-88-01)."
    - "Edge STOP-07 boundary: the 8th consecutive block trips the cap; the 7th does not."
    - "Edge STOP-07 adjacency: at exactly 8 the cap fires exactly once (one-shot latch); a 9th consecutive block does NOT re-notify until a reset re-arms the latch."
    - "Edge STOP-07 empty: zero blocks -> counter stays 0, no cap, no notify, stop_hook_active false."
    - "Edge STOP-07 ordering: a non-block outcome interleaved between blocks resets the consecutive counter (only consecutive blocks count)."
    - "Edge STOP-07 precision: the count is exact consecutive blocks, not approximate; reset on any non-block outcome (D-88-06)."
  prohibitions:
    - statement: "MUST NOT silently override a Stop hook's block when the 8-consecutive-block cap trips — the override MUST surface the one-shot warning so a livelocking hook is visible to the user, never suppressed without notice (transparency; the tri-state-severity intent behind D-88-01)."
      category: transparency
      verification: judgment
  artifacts:
    - "cap-trip warning seam in shared/notify.ts (e.g. notifyStopHookOverrideCap)"
    - "docs/output-catalog.md cap-trip entry (documentation parity)"
    - "tests/architecture/hooks-cap-notify.test.ts (byte-equality on the seam output)"
    - "loop-state cells in settle.ts (stop_hook_active, consecutive-block counter, one-shot latch) + input-reset"
  key_links:
    - "block re-entry -> set stop_hook_active + increment counter; non-block outcome -> reset counter + re-arm latch; `input` event -> clear stop_hook_active + reset counter (STOP-07)."
    - "counter === 8 -> suppress re-entry + one-shot notify (cap trip); latch prevents a second notify until reset."
    - "settle handler determines cap-trip state and stamps warning severity -> notify.ts renders (notify is a dumb renderer, does not probe state)."
---

<objective>
Deliver the STOP-07 loop protections that make the Stop block contract safe
against a livelocking hook: a per-session `stop_hook_active` flag threaded into
the Stop stdin payload, a consecutive-block counter with upstream's "8
*consecutive*" reset semantics, an override cap that suppresses re-entry and
fires a one-shot warning, and the `input`-driven reset. The cap-trip warning
ships as a sanctioned `shared/notify.ts` seam with a byte-stable catalog entry.

Purpose: match upstream's loop-protection contract exactly (superseding
PAYL-V2-04's draft cap of 10) so a naive `ralph-wiggum`-style hook cannot
livelock the agent, and the user is told when it is overridden.
Output: loop-state cells + input-reset in the settle/router layer, the
cap-trip notify seam, catalog entry, and byte-equality coverage.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-CONTEXT.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-PATTERNS.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
- Extends `bridges/hooks/settle.ts`: `stop_hook_active` flag, consecutive-block counter, one-shot cap latch (all reset via `resetSettleState`), the cap-trip suppression + notify call, and the `input`-reset accessor.
- Extends `bridges/hooks/event-router.ts`: `pi.on("input")` reset subscription (dedicated) + reset in `registerHooksBridge`.
- Extends `shared/notify.ts`: `notifyStopHookOverrideCap` (warning-severity bridge seam).
- Extends `docs/output-catalog.md`: cap-trip warning entry.
- New test `tests/architecture/hooks-cap-notify.test.ts`; extends `settle.test.ts` + the DISP-01 pin.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stop_hook_active flag, 8-consecutive-block cap, input reset (STOP-07, D-88-06)</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, tests/bridges/hooks/settle.test.ts, tests/architecture/hooks-dispatch.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts (the loop-state cells declared in plan 01; the Stop adapter from plan 02 whose block arm sets the flag/counter)
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts (pendingSessionStartContext cell + reset at 182/803; the `pi.on("input", compositeHandlerFor("UserPromptSubmit",...))` at 843; currentEpoch at 295)
    - extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts (stop_hook_active is read from the synthetic event — the settle handler must source it from the loop-state cell)
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md (Runtime State Inventory; Pitfall 4 self-clear; Open Q3 dedicated-vs-fold input subscription; Open Q4 counter reset scope)
    - tests/architecture/hooks-dispatch.test.ts (the DISP-01 pin — now at 10 from plan 01; a dedicated 2nd `input` subscription grows calls.length but NOT the Set)
  </read_first>
  <behavior>
    - block re-entry sets stop_hook_active true and the NEXT Stop payload carries stop_hook_active:true.
    - a genuine `input` event clears stop_hook_active to false and resets the counter to 0.
    - counter: 7 consecutive blocks -> re-enter each time, no cap; the 8th block -> NO re-entry + cap trips.
    - a non-block outcome (noop/mutate/allow) between blocks resets the counter, so the cap needs 8 fresh consecutive blocks afterward (ordering/precision edges).
    - one-shot latch: after the cap trips, a 9th consecutive block does NOT notify again; a counter reset re-arms it.
    - bridge-injected re-entry (sendMessage) does NOT itself clear stop_hook_active (the reset is `input`-only) — assert the flag survives a re-entry.
  </behavior>
  <action>
    Realize the loop-state cells in settle.ts: `stopHookActive: boolean`,
    `consecutiveBlockCount: number`, `capNotifiedThisSession: boolean` (the
    one-shot latch). All three reset in `resetSettleState()` (epoch hygiene,
    called from `registerHooksBridge`). In the Stop adapter's block arm: BEFORE
    re-entering, increment `consecutiveBlockCount`; if it reaches 8 (D-88-06:
    the 8th consecutive block), do NOT re-enter, fire the one-shot cap notify
    (task 2's seam) guarded by `capNotifiedThisSession`, and set the latch;
    otherwise set `stopHookActive = true` and re-enter. On ANY non-block
    outcome (noop, mutate/additionalContext, continue:false stop), reset
    `consecutiveBlockCount = 0` and `capNotifiedThisSession = false` (D-88-06 —
    additionalContext-without-block is a non-block outcome and resets, matching
    the flagged STOP-05 assumption). The synthetic Stop event's
    `stop_hook_active` field is sourced from `stopHookActive` at translate time.

    Add a dedicated `pi.on("input", ...)` reset subscription in event-router.ts
    (a small epoch-guarded closure exported from settle.ts, e.g.
    `inputResetHandlerFor(capturedEpoch)`) that clears `stopHookActive` and
    resets the counter + latch. Use a dedicated subscription (research Open Q3)
    — Pi supports multiple handlers per event; if runtime testing later shows
    coalescing, fold into the existing UserPromptSubmit handler instead (flag in
    the SUMMARY). Update the DISP-01 pin: `calls.length` 10->11; the locked
    event-name Set is UNCHANGED (input already present) — assert both the count
    grows and the Set does not.

    Extend settle.test.ts with the behavior cases (drive N blocks via the
    executor spy; assert re-entry count, cap suppression at 8, one-shot latch,
    counter reset on non-block, and stop_hook_active survives re-entry but
    clears on a simulated input event).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/hooks/settle.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/hooks-dispatch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 7 blocks re-enter; the 8th suppresses re-entry and trips the cap exactly once (one-shot).
    - A non-block outcome resets the counter and re-arms the latch (verified by re-tripping after reset).
    - `input` clears stop_hook_active; a bridge re-entry does not.
    - DISP-01 pin: `calls.length === 11`, locked event-name Set unchanged from the plan-01 10-name set.
  </acceptance_criteria>
  <done>The loop-protection state machine matches upstream: 8 consecutive blocks trip a one-shot cap, non-block outcomes reset, stop_hook_active clears only on genuine input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cap-trip warning seam + catalog entry + byte-equality coverage (D-88-01, D-88-07)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, docs/output-catalog.md, tests/architecture/hooks-cap-notify.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (notifyDiagnostic at 355-365 — the warning-severity `${header}\n\n${lines.join("\n")}` bridge-seam template; notifyAsyncRewakeSummary at 380-386 — the IL-2 bridge exemption precedent)
    - tests/architecture/hooks-async-rewake.test.ts (the byte-equality precedent for a direct notify seam — mirror its assertion shape for the cap-trip seam)
    - tests/architecture/notify-grammar-invariant.test.ts (header 1-24: it drives ONLY error/warning NotificationMessage fixtures via `notify()`, closed summary regex `N (plugin|marketplace) operation(s) ... failed|skipped.`)
    - tests/architecture/catalog-uat.test.ts (header 1-48: the runner drives `notify(mockCtx, mockPi, message)` over `NotificationMessage` fixtures ONLY)
    - docs/output-catalog.md (the `<!-- catalog-state: STATE -->` + fenced block convention around the success-with-orphan-rewake entry at 437-448)
    - docs/messaging-style-guide.md (notify grammar the summary line must satisfy)
  </read_first>
  <behavior>
    - notifyStopHookOverrideCap(ctx, pluginId) calls ctx.ui.notify exactly once with severity "warning".
    - the emitted string's first line is a non-empty summary (e.g. "Stop hook override cap reached.") followed by `\n\n` and a detail line naming pluginId and stating 8 consecutive blocks / turn ended despite an active block.
    - byte-equality: the emitted string matches the docs/output-catalog.md fenced block for the new cap-trip state, byte-for-byte.
  </behavior>
  <action>
    Add a bridge-seam `notifyStopHookOverrideCap(ctx, pluginId)` to
    shared/notify.ts modeled on `notifyDiagnostic`: emit
    `Stop hook override cap reached.\n\n<detail naming the plugin + 8
    consecutive blocks / turn ended despite an active block>` at `"warning"`
    severity via a single `ctx.ui.notify(...)` call (IL-2). Finalize the exact
    wording within the notify grammar (summary line on the `Warning:` label per
    the style guide). The settle handler (task 1) determines the cap-trip state
    and calls this seam — notify stays a dumb renderer (does not probe state).

    STRUCTURAL RECONCILIATION (D-88-07 — surface, do not silently exempt): the
    catalog-UAT runner (catalog-uat.test.ts) and the notify-grammar-invariant
    test both drive `notify()` over structured `NotificationMessage` cascade
    fixtures ONLY. The cap-trip is a bridge diagnostic seam (like
    notifyDiagnostic / notifyAsyncRewakeSummary), NOT a marketplace
    NotificationMessage — so it CANNOT be hosted in the catalog-uat FIXTURES map
    without modeling it as a fake cascade (which would misrepresent it). The
    honest reconciliation: (1) the seam satisfies the grammar invariant's
    STRUCTURAL rule (non-empty summary first line + `\n\n` block) — the same
    shape notifyDiagnostic already ships without tripping the invariant, because
    the invariant only walks NotificationMessage fixtures; (2) deliver the
    byte-equality coverage D-88-01 requires via a DEDICATED seam byte-test
    `tests/architecture/hooks-cap-notify.test.ts` (precedent:
    hooks-async-rewake.test.ts byte-asserts notifyAsyncRewakeSummary) that reads
    the new catalog block and asserts the seam's `ctx.ui.notify` output matches
    it byte-for-byte; (3) add the documentation-parity entry to
    docs/output-catalog.md with a `<!-- catalog-state: stop-override-cap -->`
    marker + fenced block. Record this scope fact in the SUMMARY so it is not
    mistaken for a silent exemption.

    Add the catalog entry under the appropriate H2 section following the
    existing convention. Write `tests/architecture/hooks-cap-notify.test.ts`
    with a mock ctx capturing `ui.notify` args, asserting the exact string +
    `"warning"` severity + single-call, and byte-equality against the catalog
    block (read docs/output-catalog.md at test time like catalog-uat does).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/hooks-cap-notify.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/notify-grammar-invariant.test.ts tests/architecture/catalog-uat.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `notifyStopHookOverrideCap` emits one warning-severity `ctx.ui.notify` with a non-empty summary line + `\n\n` detail block naming the plugin.
    - `hooks-cap-notify.test.ts` asserts byte-equality against the new `docs/output-catalog.md` cap-trip block.
    - `notify-grammar-invariant.test.ts` and `catalog-uat.test.ts` stay green (the seam is not a NotificationMessage; the scope fact is recorded in the SUMMARY, not silently exempted).
    - No `process.stdout`/`console.*` in the cap-trip path (IL-2).
  </acceptance_criteria>
  <done>The cap-trip warning ships through the sanctioned notify boundary at warning severity, byte-stable in the catalog, with the grammar-invariant scope reconciled honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| malicious/buggy hook stdout -> repeated block re-entry | a hook that always blocks would livelock the agent loop |
| cap-trip state -> `ctx.ui.notify` | user-visible warning; must not leak beyond the notify boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-02 | Denial of Service | a Stop hook that always returns `decision:block` driving unbounded re-entry | high | mitigate | 8-consecutive-block override cap suppresses re-entry + one-shot warning (STOP-07). The cap IS the mitigation; the counter's consecutive-reset semantics (D-88-06) bound it to genuine livelock, not intermittent blocks. |
| T-88-06 | Information disclosure | cap-trip detail naming the plugin surfaced outside the notify boundary | low | mitigate | Single `ctx.ui.notify` seam (IL-2); no `process.stdout`/`console.*`; the no-shell-out gate + IL-2 discipline already enforce the boundary. |
</threat_model>

<verification>
- `node --test tests/bridges/hooks/settle.test.ts tests/architecture/hooks-dispatch.test.ts tests/architecture/hooks-cap-notify.test.ts` green.
- `node --test tests/architecture/notify-grammar-invariant.test.ts tests/architecture/catalog-uat.test.ts` green.
- `npm run typecheck` green.
</verification>

<success_criteria>
- STOP-07 delivered: stop_hook_active flag, 8-consecutive-block cap, one-shot warning, input reset, D-88-06 counter semantics.
- Cap-trip warning byte-stable in the catalog with dedicated byte coverage; grammar-invariant scope reconciled (D-88-07), not silently exempted.
- Five STOP-07 edge criteria pinned; one transparency prohibition (P-88-01) authored as judgment-tier.
</success_criteria>

<output>
Create `.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-03-SUMMARY.md` when done.
</output>
