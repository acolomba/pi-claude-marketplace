---
phase: 88-agent-settled-dispatcher-stop-contract-stopfailure
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts
  - tests/bridges/hooks/settle.test.ts
  - tests/bridges/hooks/payloads/stop.test.ts
autonomous: true
requirements: [STOP-02, STOP-03, STOP-04, STOP-05, STOP-06]
must_haves:
  truths:
    - "The bespoke settle result adapter handles all four `HookExecResult` arms on the Stop path: `{kind:\"block\",reason}` -> re-enter (STOP-03); `{kind:\"mutate\",additionalContext}` -> re-enter with the context as content (STOP-05); `{kind:\"stop\"}` (continue:false) -> do NOT re-enter (STOP-06); `{kind:\"noop\"}` -> nothing."
    - "STOP-04 exit-2 is free: a hook exiting code 2 already maps to `{kind:\"block\",reason:stderr.trim()}` in parseHookStdout, so the block arm re-enters with the stderr reason — verified by a fixture, not re-implemented."
    - "STOP-06 aggregate precedence (D-88-05): if ANY Stop hook in the group yields `{kind:\"stop\"}` (continue:false), the settle handler does NOT re-enter, regardless of any other hook's block — the adapter scans the reduced bucket for any stop before honoring a block."
    - "The Stop stdin envelope (STOP-02) carries exactly: `session_id`, `transcript_path`, `cwd`, `hook_event_name:\"Stop\"`, `last_assistant_message`, `stop_hook_active`; `background_tasks` and `session_crons` keys are absent."
    - "A Stop hook declaring `asyncRewake:true` does NOT silently break the block contract: the settle path bypasses the fire-and-forget async branch for Stop (so the synchronous decision is available), OR degrades to no-block with a hookDebugLog — decided explicitly, not left implicit (research A5)."
    - "Edge STOP-06 adjacency: mixed outcomes across hooks where any hook returns continue:false suppress re-entry even when another returns block (order-independent aggregate, D-88-05)."
    - "Edge STOP-06 empty: an empty Stop bucket on `stopReason:\"stop\"` -> no dispatch, no re-entry, stop_hook_active unchanged."
    - "Edge STOP-06 ordering: when a stop and a block both occur across hooks, the aggregate honors the stop irrespective of declaration order."
  artifacts:
    - "tests/bridges/hooks/payloads/stop.test.ts (Stop envelope byte/shape)"
    - "Full bespoke Stop result adapter in settle.ts (block/mutate/stop/noop arms)"
  key_links:
    - "reduceBucket('Stop') result + a bucket scan for any {kind:'stop'} -> the re-enter/no-re-enter decision (STOP-06 aggregate over first-block-wins)."
    - "parseHookStdout exit-2 arm -> {kind:'block',reason} -> the same sendMessage re-entry (STOP-04 rides STOP-03's machinery)."
    - "synthetic settle event.last_assistant_message + event.stop_hook_active -> translateStop (the settle handler builds the synthetic event; TranslationContext is untouched)."
---

<objective>
Complete the Stop decision-control contract on top of the tracer's happy path:
the bespoke result adapter grows from the single block arm to all four
`HookExecResult` arms (block re-entry, additionalContext re-entry, continue:false
no-re-entry with aggregate cross-hook precedence, noop), exit-2 is proven to
ride the existing wire-protocol block arm, and the Stop stdin envelope is
finalized with the standard common fields plus `last_assistant_message` and
`stop_hook_active`.

Purpose: deliver STOP-02..06 at 100% hook-observable fidelity — every payload
field and decision arm a Stop hook script can depend on.
Output: a complete Stop adapter + the `payloads/stop.ts` envelope, both
fixture-proven.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-CONTEXT.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-PATTERNS.md
@docs/research/issue-103-stop-stopfailure-promotion.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
- Extends `bridges/hooks/settle.ts`: full bespoke Stop result adapter (all four arms + aggregate continue:false scan + asyncRewake-on-Stop gate).
- Finalizes `bridges/hooks/payloads/stop.ts`: `StopStdin` + `translate`.
- New test `tests/bridges/hooks/payloads/stop.test.ts`; extends `tests/bridges/hooks/settle.test.ts`.
</artifacts_produced>

<flagged_assumptions>
The edge-probe left four rows `unclassified` (surfaced here, not silently
dropped — these are explicit assumptions the executor should resolve during
implementation and the verifier should treat as unresolved-until-shown):
- STOP-02 unclassified: envelope field set is defined by the upstream contract
  (§ Stop payload in issue-103) — assumed complete as {common + last_assistant_message + stop_hook_active}; no additional Pi-only field is smuggled in.
- STOP-03 unclassified: re-entry lane is assumed to be `followUp`+`triggerTurn`
  in all settle states (agent is idle at settle) — the `nextTurn` idle lane
  async-rewake uses is deliberately NOT copied.
- STOP-04 unclassified: exit-2 mapping is assumed identical to every other
  bucket-A event (parseHookStdout is event-agnostic) — no Stop-specific exit
  handling.
- STOP-05 unclassified: `additionalContext`-without-block is assumed to re-enter
  via the SAME mechanism as a block (feedback labeling, not block labeling), and
  to count as a NON-block outcome for the plan-03 consecutive-block counter.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the bespoke Stop result adapter — exit-2, additionalContext, aggregate continue:false (STOP-03, STOP-04, STOP-05, STOP-06)</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, tests/bridges/hooks/settle.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts (the plan-01 tracer adapter — currently only the block arm)
    - extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts (the arm map: exit-2 -> {kind:"block",reason} at 37-39; continue:false -> {kind:"stop"} at 105-108; decision:block at 110-112; additionalContext -> {kind:"mutate"} at 154-157)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts (reduceBucket 177-229: first-block-wins/stop short-circuit at 199-206; the ReducedBucket shape)
    - extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts (the four-arm HookExecResult union)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts (the asyncRewake `=== true` fire-and-forget branch at 176-194 — why a Stop hook declaring it must be gated)
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-PATTERNS.md (bespoke adapter arm map; Pitfall 3 aggregate precedence; anti-pattern A5)
  </read_first>
  <behavior>
    - block: single hook returns {kind:"block",reason:"stop please"} -> one sendMessage(content:"stop please", followUp+triggerTurn, display:false).
    - exit-2: executor stub returns parseHookStdout(2,"","boom") == {kind:"block",reason:"boom"} -> one sendMessage(content:"boom").
    - mutate/additionalContext: {kind:"mutate",additionalContext:"keep going"} with no block -> one sendMessage(content:"keep going").
    - continue:false single hook: {kind:"stop"} -> zero sendMessage.
    - aggregate precedence: bucket of two hooks, hook A -> {kind:"block",reason} and hook B -> {kind:"stop"} in EITHER declaration order -> zero sendMessage (any stop suppresses re-entry, D-88-05).
    - empty bucket on stop -> zero sendMessage.
    - asyncRewake:true on a Stop entry -> no silent block loss (either dispatched synchronously OR a hookDebugLog no-block degrade; assert the chosen behavior).
  </behavior>
  <action>
    Grow the tracer's single-arm adapter into a total switch over the reduced
    Stop outcome. Because `reduceBucket` is first-block/stop-wins (dispatch.ts:
    199-206), the reduced RESULT alone cannot express STOP-06 aggregate
    precedence across multiple hooks. Implement D-88-05 aggregate precedence by
    running the Stop bucket and scanning for ANY `{kind:"stop"}` (continue:false)
    among the fired entries before honoring a `{kind:"block"}`: if any hook
    signalled stop, do NOT re-enter regardless of another hook's block. Options
    to realize this: (a) walk the bucket collecting outcomes rather than relying
    on the short-circuited reduce, or (b) a two-pass — first detect any stop,
    then reduce for the block/mutate content. Pick the approach that reuses the
    existing executor seam (`_setExecutorForTest`-swappable) and document it.

    Adapter arms (over the `HookExecResult` union produced by parseHookStdout):
    `{kind:"block",reason}` -> `sendMessage(reason ?? "", followUp+triggerTurn,
    display:false)` (STOP-03; exit-2 already maps here so STOP-04 is covered
    with NO new code — prove it with a fixture, do not add a Stop-specific exit
    path); `{kind:"mutate", additionalContext}` -> `sendMessage(additionalContext,
    followUp+triggerTurn, display:false)` when `additionalContext` is present
    (STOP-05); `{kind:"stop"}` or any-stop-in-bucket -> return without re-entry
    (STOP-06); `{kind:"noop"}` -> nothing. Wrap each `sendMessage` in try/catch +
    hookDebugLog.

    Gate the asyncRewake-on-Stop hazard (research A5): dispatchHookExec routes
    `asyncRewake === true` to `spawnAndRegister` and returns `{kind:"noop"}`
    BEFORE a decision exists, which would silently drop a Stop block. Decide
    explicitly — the recommended stance is that the settle Stop path must obtain
    a SYNCHRONOUS decision, so route Stop-bucket executors through the sync
    exec path (bypass the async branch for Stop) OR, if that is out of scope,
    log a hookDebugLog no-block degrade for a Stop entry declaring asyncRewake.
    Whichever is chosen, assert it in a test so the behavior is pinned.

    Extend settle.test.ts with the behavior cases above using the executor spy
    seam and a two-hook synthetic bucket for the aggregate case.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/hooks/settle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All eight behavior cases pass in settle.test.ts.
    - No Stop-specific exit-code handling was added (STOP-04 is proven to ride the parseHookStdout block arm via a fixture).
    - The aggregate continue:false test passes in BOTH hook declaration orders.
    - The asyncRewake-on-Stop behavior is pinned by an assertion (no silent block loss).
  </acceptance_criteria>
  <done>Stop decision control is complete: block, exit-2, additionalContext, and aggregate continue:false precedence all deliver the correct re-enter/no-re-enter outcome, fixture-proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize the Stop stdin envelope (STOP-02)</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts, tests/bridges/hooks/payloads/stop.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts (the canonical common-fields translator — copy its shape)
    - extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts (the plan-01 draft to finalize)
    - extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts (sessionId/transcriptPath/cwd; transcriptPath falls back to "")
    - tests/bridges/hooks/payloads/session-end.test.ts (the sibling test structure to mirror)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ Stop payload — the field set and the background_tasks/session_crons omission rationale)
  </read_first>
  <behavior>
    - translate({last_assistant_message:"done", stop_hook_active:true}, ctx) -> {session_id, transcript_path, cwd, hook_event_name:"Stop", last_assistant_message:"done", stop_hook_active:true}.
    - stop_hook_active:false is carried verbatim (not omitted).
    - the returned object has NO `background_tasks` and NO `session_crons` key (assert absence, not just falsy).
    - transcript_path is "" when ctx.transcriptPath is "" (lazy session file).
  </behavior>
  <action>
    Finalize `payloads/stop.ts`: `StopStdin` interface with `session_id`,
    `transcript_path`, `cwd`, `hook_event_name: "Stop"` (literal),
    `last_assistant_message: string`, `stop_hook_active: boolean`. `translate`
    reads the common three from `TranslationContext` and the two Stop-specific
    fields from the synthetic settle event object (the settle handler passes a
    `{last_assistant_message, stop_hook_active}` synthetic into the `event`
    slot — mirrors how tool translators read `event.toolName`). Omit
    `background_tasks`/`session_crons` entirely (contract-legal — Pi has no task
    registry). Write `tests/bridges/hooks/payloads/stop.test.ts` mirroring
    `payloads/session-end.test.ts`, asserting the exact object shape and the
    ABSENCE of the two omitted keys (`assert.ok(!("background_tasks" in out))`).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/hooks/payloads/stop.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The envelope carries exactly the six fields; `background_tasks`/`session_crons` are asserted absent (not merely undefined).
    - `stop_hook_active:false` round-trips verbatim.
    - Test file structure mirrors the sibling payload tests.
  </acceptance_criteria>
  <done>The Stop stdin envelope is final and shape-pinned; the settle handler feeds it the cached message + loop flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook subprocess stdout/stderr -> `pi.sendMessage` content | untrusted hook decision/reason text becomes model-visible re-entry content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-01 | Tampering/Info | block/additionalContext reason from untrusted hook stdout/stderr injected via `sendMessage({display:false})` | medium | accept | Upstream requires the reason verbatim (STOP-03/04/05); `display:false` suppresses the UI echo; content is model-visible by contract, same boundary as async-rewake exit-2. Sanitization would break fidelity — accepted, injection referred to /gsd-secure-phase. |
| T-88-05 | Elevation/Repudiation | a Stop hook declaring `asyncRewake:true` silently escaping the block contract | medium | mitigate | Explicit asyncRewake-on-Stop gate (sync decision or logged no-block degrade), pinned by a test — no silent block loss (research A5). |
</threat_model>

<verification>
- `node --test tests/bridges/hooks/settle.test.ts tests/bridges/hooks/payloads/stop.test.ts` green.
- `npm run typecheck` green.
- Spot-check `node --test tests/architecture/hooks-dispatch.test.ts` still green (no subscription-count change this plan).
</verification>

<success_criteria>
- STOP-02..06 delivered: full Stop adapter (block/exit-2/additionalContext/aggregate continue:false) + finalized Stop envelope.
- asyncRewake-on-Stop hazard gated and pinned.
- Four unclassified edge rows surfaced as flagged assumptions (not silently dropped).
</success_criteria>

<output>
Create `.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-02-SUMMARY.md` when done.
</output>
