---
phase: 01-live-evidence-revalidation
plan: 71
type: execute
wave: 19
depends_on: ["01-70"]
files_modified:
  - scripts/revalidation.mjs
autonomous: true
gap_closure: true
requirements: [RVAL-01, RVAL-02, RVAL-03, RVAL-04]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`npm run check` exits 0 with no complexity suppression holding it up: `scripts/revalidation.mjs` carries zero `fallow-ignore` markers and `fallow health --fail-on-issues` reports zero above-threshold functions."
    - "`validateScopeChangeStructure` measures at or below every configured ceiling on both independent engines: fallow cyclomatic <= 20, fallow cognitive <= 15, and ESLint `sonarjs/cognitive-complexity` <= 15."
    - "The refactor changes no observable behavior: every existing violation code, target, message, exit status and stdout/stderr byte in `tests/architecture/revalidation.test.ts` and `scripts/revalidation.negative.mjs` still holds, unedited (D-10)."
    - "`node scripts/revalidation.mjs scope-impact --check` still prints exactly `Scope impact valid: 40 records.`"
    - "Direct source coverage for `scripts/revalidation.mjs` remains 100% for branches, functions and lines."
  artifacts:
    - path: "scripts/revalidation.mjs"
      provides: "Structural scope-row validator split below both complexity ceilings, with no suppression marker"
      contains: "validateScopeChangeStructure"
  key_links:
    - from: "scripts/revalidation.mjs"
      to: ".fallowrc.json"
      via: "fallow health thresholds maxCyclomatic 20 / maxCognitive 15 measured with no per-function exception"
      pattern: "validateScopeChangeStructure"
    - from: "scripts/revalidation.mjs"
      to: "tests/architecture/revalidation.test.ts"
      via: "unedited whole-CLI expectations acting as the behavior-invariance oracle for the split"
      pattern: "deepStrictEqual"
---

<objective>
Close the Phase 1 quality-gate gap honestly: split `validateScopeChangeStructure` under both independently-computed complexity ceilings and delete the suppression marker that is currently holding `npm run check` green.

Purpose: The gate is only evidence if it measures. A `fallow-ignore` marker that names this work as its own removal target is a deferred failure, not a passing gate, and it also records a planning-artifact reference that this repository's comment policy forbids.
Output: One behavior-preserving refactor in `scripts/revalidation.mjs` and a measured, unsuppressed `npm run check`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/typescript-comments.md
@.planning/phases/01-live-evidence-revalidation/01-VERIFICATION.md
@scripts/revalidation.mjs
@.fallowrc.json
@eslint.config.js

<interfaces>
Contracts this plan must preserve exactly:

- `validateScopeChangeStructure(change, violations, { requireContractKind = false } = {})` returns `{ valid, locator }`. `valid` is `valid && identityValid && locator !== undefined`; `locator` is whatever `validateScopeAnchors` returned, or `undefined` when the row identity was already invalid. It is called from two places: `validatePlanningContracts` (with `requireContractKind: true`, consuming both fields) and `validateScopeChange` (with no options, ignoring the return).
- The seven violation codes the function can currently push are `invalid-scope-change-id`, `incomplete-scope-change`, `invalid-requirement-id`, `invalid-scope-action`, `invalid-scope-kind`, `invalid-scope-signature`, plus whatever `validateScopeRowIdentity` and `validateScopeAnchors` push. Their `code`, `target` and `message` strings are pinned byte-for-byte by existing tests.
- Push ORDER inside this function is not observable. Both consumers sort before emitting: `validateLedger` sorts on `code\0target\0message` and `validatePlanningContracts` sorts on `code\0target`. A split may therefore reorder pushes freely, but must not add, drop, or reword one.
- `violation(code, target, message)` and `identityIsSafe(candidate)` are the existing local helpers; `SCOPE_ACTIONS` is the existing frozen action set.

Measured baseline on the current tree (2026-09-12):
- fallow health reports exactly one above-threshold function once the marker is removed: `scripts/revalidation.mjs:1133 validateScopeChangeStructure — 22 ! cyclomatic, 14 cognitive, 69 lines`. Cyclomatic 22 is the breaching dimension; cognitive 14 has only one point of headroom under the ceiling of 15.
- `npx eslint --print-config scripts/revalidation.mjs` resolves `sonarjs/cognitive-complexity` to `[2, 15]` and `sonarjs/no-identical-functions` to `[2, 3]`. `no-restricted-syntax` is `[0]` here — the `process.stdout` ban is scoped to `extensions/` and does not apply to `scripts/`.
- `npm test` reports 6109 unit tests, 0 fail / 0 skipped / 0 todo. `npm run test:integration` reports 32.
</interfaces>
</context>

## Artifacts this plan produces

No new file, package, schema, export, or external interface. This plan edits exactly one tracked file: `scripts/revalidation.mjs`. It must not edit `tests/architecture/revalidation.test.ts` or `scripts/revalidation.negative.mjs` — those two files are this plan's invariance oracle, and editing them would destroy the proof.

## Known out-of-scope red: strict `validate`

Measured 2026-09-12: `node scripts/revalidation.mjs validate` exits 1 with 945 `unsafe-reference`
violations. Every one is a stale source/test path in the canonical ledger's finding references —
`orchestrators/plugin/install.ts`, `shared/notify.ts`, `domain/resolver.ts`, `orchestrators/plugin/{list,update,reinstall}.ts`,
`tests/architecture/catalog-uat.test.ts` and their test pairs — pointing at modules that later
refactors split or renamed (for example commit `a5a21ed0`, which retired the install lifecycle hub).

This is PRE-EXISTING drift, it is OUTSIDE `npm run check` (the chain does not invoke `validate`), and
it is OUTSIDE both gaps this plan set closes. Do NOT chase it here, and above all do NOT edit
`01-REVALIDATION.json` to silence it — re-pointing 945 evidence references is a separate decision the
developer owns. No verify command in this plan asserts `validate` succeeds.

<tasks>

<task type="tracer">
  <name>Task 1: Split the structural scope-row validator below both complexity ceilings</name>
  <files>scripts/revalidation.mjs</files>
  <precondition>`node_modules/` is installed in this checkout, so `npx fallow` and `npx eslint` resolve locally and offline. Measured present on 2026-09-12; if `npx fallow health` cannot resolve the binary, halt and report rather than installing anything.</precondition>
  <read_first>
    - scripts/revalidation.mjs lines 1120-1215 (the marker, the whole function, and `validateScopeChange` immediately after it)
    - scripts/revalidation.mjs lines 1090-1132 (`validateScopeRowIdentity`, for the shape a sibling helper takes here)
    - .fallowrc.json (the `health` block)
    - .claude/rules/typescript-comments.md
  </read_first>
  <behavior>
    - Invariant 1: for any `change` value, the multiset of `{code, target, message}` triples pushed onto `violations` is identical before and after the split.
    - Invariant 2: for any `change` value, the returned `{ valid, locator }` pair is identical before and after the split, including the `locator === undefined` case that makes `valid` false.
    - Invariant 3: `validateScopeChange`'s call site — which passes no options object and discards the return — behaves identically, so strict `validate` output is unchanged.
    - Invariant 4: the `requireContractKind: false` default still suppresses the `invalid-scope-kind` and `invalid-scope-signature` checks entirely.
  </behavior>
  <action>Delete the complexity suppression comment sitting immediately above `validateScopeChangeStructure` (currently line 1133) and split the function body so the remaining function measures at or under fallow cyclomatic 20, fallow cognitive 15, and ESLint `sonarjs/cognitive-complexity` 15. Cyclomatic 22 is the breaching dimension and cognitive 14 has only one point of headroom, so target a split that removes decision points rather than one that merely moves nesting around. The two highest-value extractions are (a) the six-way disjunction that guards `requirementId`, `action` and `rationale` presence, which contributes six decision points on its own, and (b) the `requireContractKind` pair — the kind classification plus the requirement-signature check — which are the only two branches gated on that option. Extract each into its own module-private helper that takes `change` and `violations` and pushes the same `violation(...)` calls with the same code, target and message strings; do not change any string, do not introduce a new violation code, and do not merge two existing violations into one. Keep `identityIsSafe` and `SCOPE_ACTIONS` checks wherever they land. Give each helper a distinct body: `sonarjs/no-identical-functions` is an error at threshold 3 for this file, so two helpers that differ only in their literals will fail lint. Comment only where the reason is non-obvious, in the present tense, and cite no planning artifact — the deleted marker's `Phase 01-71` reference is exactly the form `.claude/rules/typescript-comments.md` forbids, so do not reintroduce it in any wording. Do not touch `tests/architecture/revalidation.test.ts` or `scripts/revalidation.negative.mjs`; their unedited byte-exact expectations are the only proof that this refactor preserved behavior.</action>
  <acceptance_criteria>`scripts/revalidation.mjs` contains no suppression marker; `npx fallow health --fail-on-issues --format human` exits 0 and its "High complexity functions" section reports zero entries; `npx eslint scripts/revalidation.mjs` exits 0; the full focused suite and the standalone negative runner both pass with `tests/architecture/revalidation.test.ts` and `scripts/revalidation.negative.mjs` unmodified in the working tree.</acceptance_criteria>
  <verify>
    <automated>! grep -q 'fallow-ignore' scripts/revalidation.mjs &amp;&amp; npx fallow health --fail-on-issues --format human &amp;&amp; npx eslint scripts/revalidation.mjs &amp;&amp; node --test tests/architecture/revalidation.test.ts &amp;&amp; node scripts/revalidation.negative.mjs &amp;&amp; test -z "$(git diff --name-only -- tests/architecture/revalidation.test.ts scripts/revalidation.negative.mjs)"</automated>
  </verify>
  <fails_when>Any suppression marker survives in `scripts/revalidation.mjs`; fallow still reports an above-threshold function; ESLint reports a cognitive-complexity or identical-functions error; any focused test or negative control fails; either oracle file shows a working-tree diff; a violation code, target or message string changed; or a new comment records a phase, plan or wave reference.</fails_when>
  <done>The blocking validator measures under every configured ceiling on both engines with nothing suppressed, and the unedited expectation set proves its behavior did not move.</done>
</task>

<task type="auto">
  <name>Task 2: Record the unsuppressed hard gate on a clean tracked snapshot</name>
  <files>scripts/revalidation.mjs</files>
  <read_first>
    - CLAUDE.md (git, pre-commit and commit-message rules)
    - package.json (the `check` script chain)
  </read_first>
  <behavior>
    - `npm run check` exits 0 end to end — typecheck, ESLint, all three fallow sub-gates, format check, both corresponding-test gates, the direct-coverage negative gate, the unit suite, and the integration suite.
    - The unit suite reports at least 6109 passing tests with 0 fail, 0 skipped and 0 todo; the integration suite reports 32. This plan adds no test, so the unit count must not drop below the measured 6109 baseline.
    - `node scripts/revalidation.mjs scope-impact --check` prints exactly `Scope impact valid: 40 records.`
    - Direct coverage for `scripts/revalidation.mjs` is 100% branches, 100% functions, 100% lines — the split must not strand an unreached helper.
  </behavior>
  <action>Run the whole gate chain and the scope-impact check, then capture their exact output in the plan SUMMARY as the evidence record. Run direct coverage for `scripts/revalidation.mjs` and confirm every extracted helper is reached; if a helper is unreached, that means the split created a branch the existing cases cannot enter, which is a behavior change — report it rather than adding a test to paper over it, because this plan's contract is that the test file stays unedited. Before committing, run `pre-commit run --files scripts/revalidation.mjs`, fix any failure, restage and re-run until clean; never pass `--no-verify` and never amend to recover from a hook failure. Note that the pre-commit prettier hook can rewrite files mid-commit, so check `git status` after the commit lands and fix any residue with a follow-up commit rather than an amend. Commit with `SKIP=trufflehog` because this is a linked worktree, on branch `features/refine-unit-tests`, never on main. Write a Conventional Commits message whose title is at most 72 characters and whose body lines are at most 80, describing the refactor and the removed suppression in terms of the complexity ceilings — name no milestone, phase, plan or wave.</action>
  <acceptance_criteria>`npm run check` exits 0 with no suppression marker in the tree; unit >= 6109 with zero fail/skip/todo and integration == 32; `scope-impact --check` prints its exact success line; direct coverage is 100/100/100; exactly one file (`scripts/revalidation.mjs`) is staged and committed.</acceptance_criteria>
  <verify>
    <automated>npm run check &amp;&amp; node scripts/revalidation.mjs scope-impact --check &amp;&amp; npm run test:coverage:direct -- scripts/revalidation.mjs</automated>
  </verify>
  <fails_when>`npm run check` exits nonzero for any reason; the unit count falls below 6109 or reports a skip/todo; the integration count is not 32; `scope-impact --check` prints anything other than its exact success line; direct coverage is below 100% on any dimension; a file outside `scripts/revalidation.mjs` is staged; a pre-commit hook is bypassed; or the commit message names a planning artifact.</fails_when>
  <done>The Phase 1 hard gate is green on its own merits, with the measurement recorded and nothing suppressing it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| complexity suppression marker -> quality gate | A `fallow-ignore` comment is untrusted authority: it silences a configured gate at the author's word, with no counter-evidence, and survives indefinitely once written. |
| canonical ledger -> structural scope-row validator | `validateScopeChangeStructure` is the first reader of untrusted ledger rows; weakening any branch during the split would admit a malformed row downstream. |
| working tree -> commit | Only `scripts/revalidation.mjs` is authorized to change; a wider stage would silently carry unrelated edits from a concurrently-edited checkout into this commit. |

## STRIDE Threat Register

Security enforcement uses ASVS Level 1 and blocks unresolved high-severity threats.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-71-01 | Repudiation | `fallow-ignore` complexity suppression | high | mitigate | Delete the marker and prove the gate green by measurement: `fallow health --fail-on-issues` exits 0 with an empty high-complexity section, and a grep asserts no marker survives in the file. |
| T-01-71-02 | Tampering | structural scope-row validation during the split | high | mitigate | Keep the test file and negative runner unedited as the invariance oracle; assert a zero-length `git diff` over both; preserve every violation code, target and message string verbatim. |
| T-01-71-03 | Elevation of Privilege | a helper that silently drops a branch | high | mitigate | Require 100% direct branch/function/line coverage for `scripts/revalidation.mjs`, so an unreached extracted helper fails the gate instead of shipping as dead permissiveness. |
| T-01-71-04 | Spoofing | commit scope in a concurrently-edited checkout | medium | mitigate | Stage `scripts/revalidation.mjs` by explicit path only, run `pre-commit run --files` on that path, and re-inspect `git status` after the commit for hook-written residue. |
| T-01-71-05 | Denial of Service | full gate runtime | low | accept | `npm run check` takes roughly four minutes by prior measurement; this is the configured gate and is run once here, so the cost is accepted rather than sampled. |
| T-01-71-SC | Tampering | package installs | high | mitigate | No package installation or dependency change is authorized. Use the already-installed `fallow`, `eslint` and Node built-ins only; if a binary does not resolve, halt and report. |
</threat_model>

<verification>
  <automated>! grep -q 'fallow-ignore' scripts/revalidation.mjs &amp;&amp; npm run check &amp;&amp; node scripts/revalidation.mjs scope-impact --check &amp;&amp; node scripts/revalidation.negative.mjs &amp;&amp; npm run test:coverage:direct -- scripts/revalidation.mjs</automated>
  <fails_when>A suppression marker survives; any gate command exits nonzero; `scope-impact --check` prints anything other than `Scope impact valid: 40 records.`; direct coverage is not 100% on all three dimensions; or `tests/architecture/revalidation.test.ts` or `scripts/revalidation.negative.mjs` was modified.</fails_when>
</verification>

<success_criteria>
- The Phase 1 quality gate is green by measurement rather than by suppression, closing the verification report's second failed truth on its own terms.
- `validateScopeChangeStructure` passes both independently-computed complexity engines with no per-function exception in `.fallowrc.json` and no inline marker.
- Behavior is provably unchanged: the two files that pin the validator's byte-exact output were not edited, and they still pass.
- Exactly one tracked file changes; no test, planning contract, canonical ledger, generated report, package manifest or product file is touched.
</success_criteria>

<output>
Create `.planning/phases/01-live-evidence-revalidation/01-71-SUMMARY.md` when done. Record the before-and-after fallow measurement for `validateScopeChangeStructure` (cyclomatic, cognitive, lines), the exact `npm run check` result with its unit and integration counts, and confirm that neither oracle file was edited.
</output>
