---
phase: 12
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
autonomous: true
requirements: [CMC-19]
requirements_addressed: [CMC-19]
tags: [messaging, notify, severity-routing, docs]

must_haves:
  truths:
    - "shared/notify.ts continues to export exactly four sanctioned wrappers: notifySuccess, notifyWarning, notifyError, notifyUsageError"
    - "Wrapper SIGNATURES are unchanged: notifySuccess(ctx, message), notifyWarning(ctx, message), notifyError(ctx, message, cause?), notifyUsageError(ctx, message, usageBlock) -- D-CMC-11 / D-CMC-12 affirmation"
    - "Wrapper BODIES are unchanged in Phase 12 (notifyError MSG-CC-1 rewrite is Phase 13 per D-CMC-12)"
    - "An expanded docs comment block at the file head NAMES the four wrappers and links to style guide §10 MSG-SR-1..7 as the governance contract (D-CMC-13 part a)"
    - "Import path remains stable: callers continue to import from shared/notify.ts directly; no new barrel introduced (D-CMC-13 part b -- affirmation only)"
    - "shared/notify.ts is the SOLE ctx.ui.notify callsite (D-07); no new direct callsite added"
    - "No 5th wrapper (notifyCascadeSummary), no optional structured-payload arg -- D-CMC-11 explicitly rejects these"
    - "eslint.config.js is NOT modified (CMC-37 / IL-3 discipline preserved; the per-file BLOCK B override for shared/notify.ts is untouched)"
    - "No code logic changes in this plan -- docs-only edit; npm run check stays green trivially"
    - "RELOAD_HINT_PREFIX in shared/markers.ts is NOT touched (D-CMC-08 retention through Phase 12)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "Four sanctioned notify wrappers with expanded docs comment naming them and linking style guide §10 MSG-SR-1..7"
      exports: ["notifySuccess", "notifyWarning", "notifyError", "notifyUsageError"]
      contains: "MSG-SR-1"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts header comment"
      to: "docs/messaging-style-guide.md §10 MSG-SR-1..7"
      via: "doc comment citing the governance contract"
      pattern: "MSG-SR"
---

<objective>
Affirm the CMC-19 deliverable as a two-part docs-only touch: (a) expand the existing header comment in `extensions/pi-claude-marketplace/shared/notify.ts` to NAME the four sanctioned wrappers (`notifySuccess`, `notifyWarning`, `notifyError`, `notifyUsageError`) and link them to style guide §10 MSG-SR-1..7 as the governance contract; (b) confirm the import path is stable (callers import from `shared/notify.ts` directly; no new barrel is introduced or required).

Purpose: CMC-19 governs the severity-wrapper contract: severity is structural via the wrapper name (`notifySuccess` / `notifyWarning` / `notifyError` / `notifyUsageError`), NEVER embedded as a `[error]` / `[warning]` prefix in message text (PRD §6.12 ES-2, reaffirmed by MSG-SR-7). D-CMC-11 locks the API shape unchanged in Phase 12: no fifth `notifyCascadeSummary` helper, no optional structured-payload arg, composition lives in Phase 13's `presentation/` composers which return strings that flow verbatim into the wrappers. D-CMC-13 specifies that Phase 12's deliverable is the inventory affirmation only -- a docs comment naming the wrappers + a confirmation that the import path is stable.

Output: One docs-only edit to `shared/notify.ts` expanding the file-head comment block; no code changes; no new files; no barrel.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md
@docs/messaging-style-guide.md
@docs/prd/pi-claude-marketplace-prd.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/presentation/index.ts
@CLAUDE.md

<interfaces>
<!-- CURRENT public surface of shared/notify.ts (verified by Read in this session, lines 22-64).
     D-CMC-11 locks all four signatures unchanged in Phase 12. -->

```typescript
export function notifySuccess(ctx: ExtensionContext, message: string): void;
export function notifyWarning(ctx: ExtensionContext, message: string): void;
export function notifyError(ctx: ExtensionContext, message: string, cause?: unknown): void;
export function notifyUsageError(ctx: ExtensionContext, message: string, usageBlock: string): void;
```

<!-- TARGET surface after this plan: IDENTICAL. Only the header comment block expands. -->
</interfaces>

<existing_header_comment>
<!-- Current docs comment block at extensions/pi-claude-marketplace/shared/notify.ts:5-19 -->

/**
 * shared/notify.ts -- the SOLE sanctioned ctx.ui.notify call site (D-07).
 *
 * Severity is part of the function name. The Pi API's `notify(msg, type?)`
 * accepts a magic-string `"info" | "warning" | "error"` second arg; a typo
 * like `"warining"` silently degrades to `"info"` because there is no
 * exhaustiveness check. Severity-named wrappers eliminate that class of bug.
 *
 * The eslint per-file override in eslint.config.js (D-06 / BLOCK B) disables
 * `no-restricted-syntax` for this file, so inline `eslint-disable-next-line`
 * comments are unnecessary here (they would trigger
 * `reportUnusedDisableDirectives` warnings). The per-file override is the
 * single audit surface; this comment documents the sanctioned-use intent in
 * its place.
 */
</existing_header_comment>

<barrel_decision>
<!-- D-CMC-13 part b: confirm import path stability. Verified by reading presentation/index.ts (this
     session) -- the barrel does NOT re-export the notify wrappers. Callers import directly from
     ../../shared/notify.ts (relative path varies by call site). Research §2.4 explicitly notes:
     "no new barrel is required; affirm the existing import path is stable." -->

NO barrel changes in this plan. presentation/index.ts is not modified. The four wrappers remain
imported directly from shared/notify.ts.
</barrel_decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the file-head docs comment in shared/notify.ts to name the four sanctioned wrappers (notifySuccess, notifyWarning, notifyError, notifyUsageError) and link them to style guide §10 MSG-SR-1..7; affirm the import path is stable; do NOT change signatures, bodies, exports, eslint, or any other file</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (FULL file -- current 65-line module with the existing 15-line docs comment at lines 5-19; the four wrapper signatures at lines 22, 27, 44, 62; understand the exact wording of the existing comment before extending it)
    - docs/messaging-style-guide.md (lines 200-360 -- §10 Severity Routing section; identify MSG-SR-1 through MSG-SR-7 by their IDs and the per-wrapper guidance each one provides; this is the source of the governance link in the new comment text)
    - docs/prd/pi-claude-marketplace-prd.md §6.12 ES-2 (no [error]/[warning] prefix embedding -- the rule MSG-SR-7 reaffirms; the new comment should cite this lineage)
    - docs/prd/pi-claude-marketplace-prd.md §6.13 IL-2 (single output channel via ctx.ui.notify -- the D-07 rule the existing comment already cites)
    - extensions/pi-claude-marketplace/presentation/index.ts (full file -- VERIFY the barrel does NOT re-export the notify wrappers; this verifies D-CMC-13 part b's "import path is stable" affirmation; if the barrel re-exports the wrappers, the decision is to LEAVE the barrel as-is and not add a new one)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§2.4 -- full diff sketch for the docs comment expansion; explicit "no code change" guidance; the barrel decision)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md §"File-header comment style (cite requirement IDs + decision IDs)" (lines 662-686) -- this is the binding pattern for the docs-comment edit; PATTERNS.md has NO dedicated notify-wrapper section, and the "File-header comment style" pattern explicitly cites `shared/notify.ts:5-19` (line 664) as one of its canonical example files. Use the existing 15-line header comment in `shared/notify.ts` as the in-file style template and the PATTERNS.md pattern as the cross-cutting governance.
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md (D-CMC-11, D-CMC-12, D-CMC-13 -- the three decisions this task implements; verify each constraint before editing)
    - eslint.config.js (lines 75-140 -- verify BLOCK B contains the per-file override for shared/notify.ts; this override is what makes inline eslint-disable comments unnecessary here, per the existing comment; the new comment block should preserve this lineage)
  </read_first>
  <action>
    EXPAND the existing file-head docs comment in `extensions/pi-claude-marketplace/shared/notify.ts` (currently 15 lines at lines 5-19) to a fuller block that adds the CMC-19 inventory affirmation. The existing comment text is good and stays; the expansion ADDS the wrapper inventory + style-guide link at the end of the block (before the closing `*/`).

    The expanded comment block has two parts:
    - PART A (existing -- keep verbatim): the current 15-line comment naming D-07, the severity-typo-prevention rationale, and the per-file eslint override discipline.
    - PART B (new -- add to the end of the comment, before the closing `*/`): a new section titled "SANCTIONED WRAPPERS (CMC-19 Phase 12 affirmation)" that:
      - Names each of the four wrappers with its signature and the corresponding MSG-SR rule ID from style guide §10
      - States that Phase 13 composers return strings that flow VERBATIM into these wrappers (no fifth wrapper, no structured-payload arg, no cascade-summary helper) per D-CMC-11
      - States that severity remains STRUCTURAL via the wrapper name -- never embedded as a `[error]` / `[warning]` prefix in message text per PRD §6.12 ES-2 / MSG-SR-7

    The new section (PART B) should follow this content shape (use as a structural guide, NOT a copy-paste body -- preserve JSDoc-style formatting with leading ` * `):

    ```
    *
    * SANCTIONED WRAPPERS (CMC-19 Phase 12 affirmation, governed by style guide §10 MSG-SR-1..7):
    *
    *   (§10 numbering: MSG-SR-1..3 govern single-shot severity routing -- one rule per wrapper for the
    *    non-cascade case; MSG-SR-4..6 govern cascade-summary routing -- those rules pick BETWEEN
    *    notifySuccess and notifyWarning for cascade summaries and never assign to notifyUsageError;
    *    MSG-SR-7 is the dedicated usage-error rule for notifyUsageError.)
    *
    *   - notifySuccess(ctx, message)               -- default severity (MSG-SR-1)
    *   - notifyWarning(ctx, message)               -- "warning" severity (MSG-SR-2)
    *   - notifyError(ctx, message, cause?)         -- "error" severity (MSG-SR-3)
    *   - notifyUsageError(ctx, message, usageBlock) -- "error" severity (MSG-SR-7)
    *
    * Phase 13 composers return strings that flow VERBATIM into these wrappers;
    * no fifth wrapper, no structured-payload arg, no cascade-summary helper is
    * added (D-CMC-11). Severity remains structural via the wrapper name --
    * never embedded as a "[error]" / "[warning]" prefix in message text
    * (PRD §6.12 ES-2, reaffirmed by MSG-SR-7).
    *
    * Import path (D-CMC-13): callers import the wrappers directly from this file
    * (e.g., `import { notifySuccess } from "../../shared/notify.ts"`). No
    * presentation/ barrel re-exports the wrappers in Phase 12; affirm the
    * existing direct-import path is stable.
    ```

    Verify the exact MSG-SR-N rule mappings by reading `docs/messaging-style-guide.md` §10 (see <read_first>). If §10's per-wrapper rule numbering differs from the structural guide above (e.g., MSG-SR-1 vs MSG-SR-2 for `notifySuccess`), use the actual rule IDs from §10. The point is to LINK each wrapper to its governing rule; the exact rule-number assignment is sourced from the style guide.

    DO NOT change:
    - Any function signature (D-CMC-11): the four wrappers stay exactly as `notifySuccess(ctx, message)`, `notifyWarning(ctx, message)`, `notifyError(ctx, message, cause?)`, `notifyUsageError(ctx, message, usageBlock)`.
    - Any function body (D-CMC-11, D-CMC-12): `notifyError`'s body stays as `\`${message}${causeText}\`` with `\nCause: ${errorMessage(cause)}` -- Phase 13 owns the MSG-CC-1 cause-chain rewrite.
    - The existing 15-line comment text (preserve verbatim; only EXTEND with the new section).
    - Imports: `errorMessage` from `./errors.ts` and `ExtensionContext` type from `../platform/pi-api.ts` stay unchanged.
    - Any other file. This plan touches ONLY `shared/notify.ts`.
    - `eslint.config.js` (CMC-37 -- no config widening; the per-file BLOCK B override for shared/notify.ts stays as-is).
    - `presentation/index.ts` (D-CMC-13 barrel decision -- affirm-only; no new re-export added).
    - `docs/messaging-style-guide.md` (Plan 12-03 owns the §14.1 doc edit; no MSG-SR-* docs edit needed in Phase 12).

    If `docs/messaging-style-guide.md` §10's per-wrapper rule-ID assignments differ from the structural guide in this `<action>` block, follow the actual style guide (it is the binding contract). Note the actual rule IDs in the SUMMARY.md.
  </action>
  <verify>
    <automated>grep -c "notifySuccess\|notifyWarning\|notifyError\|notifyUsageError" extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <automated>grep -c "MSG-SR" extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <automated>grep -F "CMC-19" extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <automated>grep -F "D-CMC-11" extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <automated>grep -F "D-CMC-13" extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <automated>grep -c "export function notify" extensions/pi-claude-marketplace/shared/notify.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run check</automated>
    <automated>test -z "$(git diff --name-only HEAD -- eslint.config.js)"</automated>
    <automated>test -z "$(git diff --name-only HEAD -- extensions/pi-claude-marketplace/presentation/index.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - Source (four wrappers still exported, signatures unchanged): `grep -c "export function notify" extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 4
    - Source (each wrapper named in the docs comment): `grep -c "notifySuccess\|notifyWarning\|notifyError\|notifyUsageError" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 8 (4 export-function lines + 4 comment-naming lines)
    - Source (style-guide link present): `grep -c "MSG-SR" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 match (citing at least one MSG-SR-N rule ID from style guide §10)
    - Source (CMC-19 traceability): `grep -F "CMC-19" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 match
    - Source (D-CMC-11 traceability -- no 5th wrapper / no structured arg): `grep -F "D-CMC-11" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 match
    - Source (D-CMC-13 traceability -- import path affirmation): `grep -F "D-CMC-13" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 match
    - Source (signatures verbatim unchanged): `grep -F "export function notifySuccess(ctx: ExtensionContext, message: string): void" extensions/pi-claude-marketplace/shared/notify.ts` returns 1 match (signature byte-identical)
    - Source (signatures verbatim unchanged): `grep -F "export function notifyError(ctx: ExtensionContext, message: string, cause?: unknown): void" extensions/pi-claude-marketplace/shared/notify.ts` returns 1 match
    - Source (notifyError body unchanged per D-CMC-12): `grep -F "\\nCause: \${errorMessage(cause)}" extensions/pi-claude-marketplace/shared/notify.ts` returns 1 match (the legacy MSG-CC-1-precursor body stays; Phase 13 rewrites it)
    - Source (existing 15-line comment preserved): `grep -F "SOLE sanctioned ctx.ui.notify call site (D-07)" extensions/pi-claude-marketplace/shared/notify.ts` returns 1 match
    - Source (existing eslint discipline preserved): `grep -F "per-file override in eslint.config.js" extensions/pi-claude-marketplace/shared/notify.ts` returns 1 match
    - Source (no new direct ctx.ui.notify callsite): `grep -rn "ctx.ui.notify" extensions/pi-claude-marketplace/` returns ONLY the 4 lines inside shared/notify.ts (D-07 enforced)
    - Source (no other file touched): `git diff --name-only HEAD` for this plan's commit lists ONLY `extensions/pi-claude-marketplace/shared/notify.ts` (and nothing else -- eslint.config.js, presentation/index.ts, shared/markers.ts, all unchanged)
    - CLI: `npm run typecheck` exits 0 (no signature change; typecheck is trivially green)
    - CLI: `npm run lint` exits 0 (no new restricted-syntax violations; the per-file override stays valid)
    - CLI: `npm run check` exits 0 (full structural gate green)
    - CLI (non-regression): `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0 (out-of-scope file untouched)
  </acceptance_criteria>
  <done>shared/notify.ts has an expanded file-head comment naming the four sanctioned wrappers with their MSG-SR-* governance links, citing CMC-19 / D-CMC-11 / D-CMC-13. No signature change, no body change, no eslint widening, no barrel change. npm run check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a docs-only edit to a single file's header comment. No code path, no I/O surface, no new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.04-01 | Tampering | wrapper signatures via "minor cleanup" | mitigate | Acceptance criteria assert each signature is byte-identical to current (grep -F for the exact signature string); D-CMC-11 prohibits any signature change; reviewer can spot signature deltas in the diff |
| T-12.04-02 | Tampering | eslint per-file override widening | mitigate | Acceptance criterion `git diff --name-only HEAD -- eslint.config.js` is empty enforces CMC-37 at the diff level |
| T-12.04-03 | Information Disclosure | docs comment | accept | The comment names internal decision IDs (CMC-19, D-CMC-11, D-CMC-13) and rule IDs (MSG-SR-N) that are all already public in `.planning/` and `docs/` |

**Summary:** No new attack surface introduced. This plan is a single-file docs-only edit; no logic changes, no I/O changes, no signature changes. ASVS L1 not applicable (no input handling, no network, no auth, no crypto, no data flow). Threats are internal: signature drift (D-CMC-11 violation) and eslint widening (CMC-37 violation) are both blocked by acceptance criteria that assert byte-identical preservation.
</threat_model>

<verification>
- `npm run check` exits 0 at end of plan (trivially green -- no code logic changes).
- `npm run typecheck` exits 0 -- signatures unchanged.
- `grep -c "export function notify" extensions/pi-claude-marketplace/shared/notify.ts` returns exactly 4 (the four wrappers).
- `grep -c "MSG-SR" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 (style-guide §10 governance link present).
- `grep -F "CMC-19" extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 match.
- `grep -rn "ctx.ui.notify" extensions/pi-claude-marketplace/` returns ONLY the 4 lines in shared/notify.ts (D-07 single-callsite discipline preserved).
- `git diff --name-only HEAD -- eslint.config.js` is empty (CMC-37).
- `git diff --name-only HEAD -- extensions/pi-claude-marketplace/presentation/index.ts` is empty (D-CMC-13 barrel decision).
- `git diff --name-only HEAD -- extensions/pi-claude-marketplace/shared/markers.ts` is empty (D-CMC-08 retention).
- This plan's commit modifies ONLY `extensions/pi-claude-marketplace/shared/notify.ts`.
</verification>

<success_criteria>
- shared/notify.ts file-head comment block is expanded with the CMC-19 inventory affirmation: names the four wrappers (notifySuccess, notifyWarning, notifyError, notifyUsageError), links each to a MSG-SR-N rule from style guide §10, cites D-CMC-11 (no 5th wrapper / no structured payload), cites D-CMC-13 (import path stability).
- The existing 15-line docs comment (D-07 / eslint override discipline) is preserved verbatim.
- The four wrapper signatures and bodies are byte-identical to the prior file.
- No other file is modified.
- eslint.config.js is untouched.
- presentation/index.ts is untouched (no new barrel re-export added).
- shared/markers.ts is untouched (D-CMC-08 retention).
- npm run check is green.
</success_criteria>

<output>
Create `.planning/phases/12-messaging-foundations-renderer-primitives/12-04-SUMMARY.md` when done. The SUMMARY.md should record the actual MSG-SR-N rule-ID assignments used in the docs comment (sourced from style guide §10) so a reviewer can verify the link integrity without re-reading the style guide.
</output>
