---
phase: 12
plan: 02
type: execute
wave: 2
depends_on: [12-01, 12-03, 12-04]
files_modified:
  - extensions/pi-claude-marketplace/presentation/reload-hint.ts
  - extensions/pi-claude-marketplace/presentation/index.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - tests/presentation/reload-hint.test.ts
autonomous: true
requirements: [CMC-14]
requirements_addressed: [CMC-14]
tags: [messaging, reload-hint, presentation, callsite-migration]

must_haves:
  truths:
    - "presentation/reload-hint.ts exports `reloadHint(names: readonly string[]): string` returning `/reload to pick up changes` when names is non-empty and `''` otherwise -- three-verb selector retired"
    - "presentation/reload-hint.ts no longer exports `ReloadVerb`; presentation/index.ts barrel no longer re-exports `ReloadVerb`"
    - "All 8 reloadHint callsites pass only the names array (no verb literal) -- install.ts:690, uninstall.ts:237, update.ts:731, reinstall.ts:372, reinstall.ts:871, marketplace/update.ts:358, marketplace/remove.ts:278, import/execute.ts:339-341"
    - "tests/presentation/reload-hint.test.ts no longer references ReloadVerb, no longer asserts the three verb-variant outputs, no longer imports RELOAD_HINT_PREFIX"
    - "RELOAD_HINT_PREFIX in shared/markers.ts is NOT deleted (D-CMC-08 retention through Phase 12; the markers-snapshot test still imports and asserts it; deletion is Phase 13's atomic three-file edit)"
    - "tests/architecture/markers-snapshot.test.ts is NOT modified by this plan"
    - "D-11 layering preserved: presentation/reload-hint.ts no longer imports from shared/markers.ts (the import is removed because the new trailer is a local const)"
    - "D-07 single-callsite discipline for ctx.ui.notify preserved: no new direct call added (composers return strings; wrappers in shared/notify.ts emit)"
    - "eslint.config.js is NOT modified (CMC-37 / IL-3 discipline preserved)"
    - "npm run check (typecheck + ESLint + Prettier + tests) is green after every task -- the signature change is type-level breaking, so all 8 callsites + test rewrite + barrel edit MUST land atomically"
    - "D-CMC-10 carve-out is recorded in PLAN.md (this file) and SUMMARY.md: the 8 reload-hint callsite trailers DO change user-visible output in Phase 12, authorized by roadmap criterion #2 (verb selector gone) which structurally requires the trailer to change wherever the composer is called; roadmap criterion #4 (output unchanged except migrate.ts) is read as authorized-by-criterion-#2 for this carve-out"
    - "D-CMC-10 carve-out is cited in CHANGELOG.md in the release entry that includes Phase 12 (binding): the entry MUST contain the literal string 'D-CMC-10' and a sentence stating '8 reload-hint callsite trailers now emit /reload to pick up changes; Phase 12 carve-out per D-CMC-10; roadmap criterion #2 authorizes' (or substantively equivalent prose). Verification: `grep -F \"D-CMC-10\" CHANGELOG.md` returns at least one match in the Phase-12 release entry. If CHANGELOG.md does not yet exist or the Phase-12 release entry has not been opened, the release-PR step (per CLAUDE.md) MUST create / extend it before merge."
  artifacts:
    - path: "extensions/pi-claude-marketplace/presentation/reload-hint.ts"
      provides: "Single-trailer reload-hint composer + appendReloadHint join helper"
      exports: ["reloadHint", "appendReloadHint"]
      contains: "RELOAD_HINT_TRAILER"
      min_lines: 15
    - path: "extensions/pi-claude-marketplace/presentation/index.ts"
      provides: "Presentation barrel (ReloadVerb re-export removed)"
      exports: ["reloadHint", "appendReloadHint"]
    - path: "tests/presentation/reload-hint.test.ts"
      provides: "Behavioral tests for the single-trailer composer + appendReloadHint join"
      contains: "MSG-RH-1"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "presentation/reload-hint.ts"
      via: "import reloadHint, appendReloadHint"
      pattern: "reloadHint\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      to: "presentation/reload-hint.ts"
      via: "import reloadHint"
      pattern: "reloadHint\\("
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      to: "presentation/reload-hint.ts"
      via: "two callsites at lines 372 and 871"
      pattern: "reloadHint\\("
    - from: "tests/presentation/reload-hint.test.ts"
      to: "presentation/reload-hint.ts"
      via: "import reloadHint, appendReloadHint"
      pattern: "reloadHint\\("
---

<objective>
Collapse the reload-hint composer to the single canonical trailer `/reload to pick up changes`, retire the three-verb selector, migrate all 8 callsites to the new signature in the same atomic edit, and rewrite the reload-hint test for the new behavior. This is the typecheck-breaking signature change; everything in scope must land together or `npm run check` fails mid-plan.

Purpose: CMC-14 / MSG-RH-1 require a single canonical trailer; the legacy three-verb selector ("Run /reload to load it.", "Run /reload to refresh ...", "Run /reload to drop ...") is structurally incompatible with the new contract. The signature change `reloadHint(verb, names)` → `reloadHint(names)` is the breaking change that forces atomic migration. Roadmap criterion #2 ("the three-verb selector is gone from presentation/reload-hint.ts") authorizes the D-CMC-10 carve-out: the 8 callsite trailers do change user-visible output in Phase 12, which is otherwise scoped to migrate.ts alone.

Output: A 15-line composer module, an updated barrel, 8 migrated orchestrator callsites, and a rewritten test suite of 5 assertions covering empty / single / multi / appendReloadHint suppression / appendReloadHint join.

## D-CMC-10 Carve-Out (recorded per planner guidance)

Roadmap Phase 12 Success Criterion #4 says "user-visible output is unchanged except for the single migrate.ts diagnostic." Under D-CMC-06 / D-CMC-07, the 8 reload-hint callsites in this plan emit the new trailer `/reload to pick up changes` in Phase 12 instead of the legacy `Run /reload to <verb> "..."`. This IS a real user-visible change beyond migrate.ts.

This carve-out is authorized by Roadmap Phase 12 Success Criterion #2: "the three-verb selector is gone from `presentation/reload-hint.ts`." Removing the verb selector from the composer structurally requires the trailer to change wherever the composer is called. The two criteria are read together: criterion #2 specifies WHAT changes (the composer's contract), and criterion #4 specifies WHAT ELSE does NOT change (the rest of the user-visible surface area -- Phase 13 owns the per-command catalog conformance).

CHANGELOG.md entry MUST cite this carve-out at PR-merge time so a reviewer reading criterion #4 in isolation does not mis-read the diff as a roadmap violation. CLAUDE.md states "Before creating a PR, offer to record the change in CHANGELOG.md" -- Phase 12 ships a CHANGELOG entry as part of the release-PR step, so the citation requirement is structurally enforced (see Task 2 binding acceptance criterion and the `must_haves.truths` constraint covering the release-PR step). The other (non-reload-hint) user-visible surfaces are still Phase 13's mechanical refactor scope and are NOT touched in Phase 12.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md
@docs/messaging-style-guide.md
@extensions/pi-claude-marketplace/presentation/reload-hint.ts
@extensions/pi-claude-marketplace/presentation/index.ts
@extensions/pi-claude-marketplace/presentation/plugin-list.ts
@extensions/pi-claude-marketplace/shared/markers.ts
@tests/presentation/reload-hint.test.ts
@tests/architecture/markers-snapshot.test.ts
@CLAUDE.md

<interfaces>
<!-- CURRENT public surface of presentation/reload-hint.ts (from grep on the current file): -->
```typescript
export type ReloadVerb = "load" | "refresh" | "drop";
export function reloadHint(verb: ReloadVerb, names: readonly string[]): string;
export function appendReloadHint(body: string, hint: string): string;
```

<!-- TARGET public surface after this plan (D-CMC-06): -->
```typescript
// ReloadVerb type DELETED
export function reloadHint(names: readonly string[]): string;
export function appendReloadHint(body: string, hint: string): string;
```

<!-- New file-private constant (D-CMC-07; analog: MAX_LINE_COLUMN in presentation/plugin-list.ts:30): -->
```typescript
const RELOAD_HINT_TRAILER = "/reload to pick up changes";
```

<!-- All 8 callsites pass only the names argument (verb literal dropped). Names expressions pass through unchanged. -->
</interfaces>

<callsite_enumeration>
<!-- VERIFIED 8 callsites (NOT 6 as 12-CONTEXT.md drift suggests). Researcher and pattern mapper
     both confirmed reinstall.ts has two callsites at lines 372 and 871. -->

| # | File | Line | Current verb arg | Names expression (unchanged) |
|---|------|------|------------------|------------------------------|
| 1 | orchestrators/plugin/install.ts | 690 | "load" | `stagedAny ? [plugin] : []` |
| 2 | orchestrators/plugin/uninstall.ts | 237 | "drop" | `droppedAny ? [plugin] : []` |
| 3 | orchestrators/plugin/update.ts | 731 | "refresh" | `updatedNames` |
| 4 | orchestrators/plugin/reinstall.ts | 372 | "refresh" | `changedNames` |
| 5 | orchestrators/plugin/reinstall.ts | 871 | "refresh" | `outcome.resourcesChanged ? [outcome.name] : []` |
| 6 | orchestrators/marketplace/update.ts | 358 | "refresh" | `updatedNames` |
| 7 | orchestrators/marketplace/remove.ts | 278 | "drop" | `removedSorted` |
| 8 | orchestrators/import/execute.ts | 339-341 | "load" (multi-line) | `result.installedPlugins.filter((o) => o.resourcesChanged).map((o) => o.plugin)` |
</callsite_enumeration>

<scope_carve_outs>
<!-- The following are EXPLICITLY out-of-scope for this plan and must not be modified: -->

1. RELOAD_HINT_PREFIX export in extensions/pi-claude-marketplace/shared/markers.ts -- RETAINED per D-CMC-08. Phase 13's atomic three-file edit deletes the constant, the markers-snapshot row, and the PRD §6.12 row in one commit.

2. tests/architecture/markers-snapshot.test.ts -- NOT modified. The markers-snapshot test still imports and asserts RELOAD_HINT_PREFIX === "Run /reload to ".

3. appendReloadHint blank-line-above conformance (style guide §5 MSG-RH-1: "preceded by one blank line") -- DEFERRED to Phase 13 per research R7 + Open Question 2. Keep the single-newline join. Add a TODO comment in appendReloadHint referencing MSG-RH-1.

4. eslint.config.js -- NOT modified.

5. docs/messaging-style-guide.md -- NOT modified by this plan (Plan 12-03 owns the §14.1 atomic doc edit).
</scope_carve_outs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite tests/presentation/reload-hint.test.ts for the new single-trailer behavior (drop verb-variant assertions; drop RELOAD_HINT_PREFIX import; add empty/single/multi-name + appendReloadHint suppression + appendReloadHint join cases)</name>
  <files>tests/presentation/reload-hint.test.ts</files>
  <read_first>
    - tests/presentation/reload-hint.test.ts (full file -- current shape with 3 verb-variant assertions + 1 RELOAD_HINT_PREFIX byte-equality test + 2 appendReloadHint cases; the assertions to delete are explicitly enumerated in 12-PATTERNS.md "tests/presentation/reload-hint.test.ts (REWRITE)" section)
    - extensions/pi-claude-marketplace/presentation/reload-hint.ts (current file -- to understand the current signature `reloadHint(verb, names)` before Task 2 changes it to `reloadHint(names)`; the test in this task is written to the TARGET signature)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§3.4 -- recommended 5-test set with exact assertion strings: empty, single, multi, appendReloadHint suppression, appendReloadHint join)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md ("tests/presentation/reload-hint.test.ts (REWRITE)" section -- explicit before/after deltas; test-naming convention MSG-RH-1 from style-guide §5)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md (CMC-14 composer row -- automated command `npm test tests/presentation/reload-hint.test.ts`)
    - docs/messaging-style-guide.md (lines 480-540 -- §5 MSG-RH-1 specification: single canonical trailer, blank-line-above note is a Phase 13 concern per scope_carve_outs above)
  </read_first>
  <behavior>
    - Test 1: "MSG-RH-1: empty names returns empty string (suppression)" -- `reloadHint([])` === `""`
    - Test 2: "MSG-RH-1: single name returns the canonical trailer" -- `reloadHint(["foo"])` === `"/reload to pick up changes"`
    - Test 3: "MSG-RH-1: multi name returns the same canonical trailer (names ignored beyond non-empty check)" -- `reloadHint(["alpha", "beta", "gamma"])` === `"/reload to pick up changes"`
    - Test 4: "appendReloadHint: empty hint returns bare body (suppression)" -- `appendReloadHint("Body content", "")` === `"Body content"`
    - Test 5: "appendReloadHint: non-empty hint joins with single newline" -- `appendReloadHint("Body content", "/reload to pick up changes")` === `"Body content\n/reload to pick up changes"`
  </behavior>
  <action>
    REWRITE `tests/presentation/reload-hint.test.ts` as a fresh small file (~30-40 lines). The current 46-line file is replaced; the file path stays the same.

    Imports (drop the RELOAD_HINT_PREFIX import; keep only the composer/join function imports):
    - `import assert from "node:assert/strict";`
    - `import test from "node:test";`
    - `import { appendReloadHint, reloadHint } from "../../extensions/pi-claude-marketplace/presentation/reload-hint.ts";`
    - DO NOT import `RELOAD_HINT_PREFIX` from `shared/markers.ts` -- the markers-snapshot test (`tests/architecture/markers-snapshot.test.ts`) is the SOLE remaining consumer (D-CMC-08 retention).
    - DO NOT import `ReloadVerb` -- the type is deleted by Task 2.

    Tests (5 total, in this order -- names quoted exactly so the acceptance criteria can grep for them):
    - `test("MSG-RH-1: empty names returns empty string (suppression)", () => { assert.equal(reloadHint([]), ""); });`
    - `test("MSG-RH-1: single name returns the canonical trailer", () => { assert.equal(reloadHint(["foo"]), "/reload to pick up changes"); });`
    - `test("MSG-RH-1: multi name returns the same canonical trailer (names ignored beyond non-empty check)", () => { assert.equal(reloadHint(["alpha", "beta", "gamma"]), "/reload to pick up changes"); });`
    - `test("appendReloadHint: empty hint returns bare body (suppression)", () => { assert.equal(appendReloadHint("Body content", ""), "Body content"); });`
    - `test("appendReloadHint: non-empty hint joins with single newline", () => { assert.equal(appendReloadHint("Body content", "/reload to pick up changes"), "Body content\n/reload to pick up changes"); });`

    Deletions from the current file (these MUST not appear in the new file):
    - The `RELOAD_HINT_PREFIX` import line
    - The `reloadHint("load", ...)`, `reloadHint("refresh", ...)`, `reloadHint("drop", ...)` 3-verb assertion tests
    - The "RELOAD_HINT_PREFIX is byte-for-byte 'Run /reload to '" assertion (markers-snapshot test covers it)

    Test-naming convention: Use `MSG-RH-1` (the style-guide §5 requirement ID) instead of the legacy `RH-1` / `RH-2` (per research §3.4).

    NOTE: This task is RED first -- the test will FAIL until Task 2 changes the composer signature. That is intentional. Task 2 closes the cycle.
  </action>
  <verify>
    <automated>! grep -E "ReloadVerb|reloadHint\\(\"(load|refresh|drop)\"|RELOAD_HINT_PREFIX" tests/presentation/reload-hint.test.ts</automated>
    <automated>grep -c "MSG-RH-1" tests/presentation/reload-hint.test.ts</automated>
    <automated>grep -c "appendReloadHint" tests/presentation/reload-hint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/presentation/reload-hint.test.ts` contains exactly 5 `test(` invocations
    - Source: `grep -E "ReloadVerb|reloadHint\\(\"(load|refresh|drop)\"|RELOAD_HINT_PREFIX" tests/presentation/reload-hint.test.ts` returns NO matches
    - Source: `grep -c "MSG-RH-1" tests/presentation/reload-hint.test.ts` returns at least 3 (three composer tests using the requirement ID)
    - Source: At least one test asserts `assert.equal(reloadHint([]), "")` exactly (empty names → empty string)
    - Source: At least one test asserts `assert.equal(reloadHint(["foo"]), "/reload to pick up changes")` (single-name canonical trailer)
    - Source: At least one test contains the literal string `"alpha", "beta", "gamma"` (multi-name → same trailer)
    - Source: At least one `appendReloadHint(` assertion verifies the empty-suppression branch (`""` → bare body)
    - Source: At least one `appendReloadHint(` assertion verifies the join branch produces `"Body content\n/reload to pick up changes"` (single-newline join -- blank-line-above is Phase 13's concern)
    - Behavior (intentional RED until Task 2): At this point in the plan, `npm test -- tests/presentation/reload-hint.test.ts` may FAIL because the composer signature has not yet been changed. Task 2 turns it GREEN.
  </acceptance_criteria>
  <done>Test file is rewritten with 5 assertions matching the new contract: 3 MSG-RH-1 cases (empty/single/multi) + 2 appendReloadHint cases (suppression/join). No references to ReloadVerb, three-verb literal first-args, or RELOAD_HINT_PREFIX remain. Tests are expected to fail until Task 2 lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite presentation/reload-hint.ts to the single-trailer composer (new local RELOAD_HINT_TRAILER const; reloadHint signature drops verb arg; ReloadVerb type deleted; appendReloadHint unchanged with TODO for blank-line-above), update presentation/index.ts barrel (drop ReloadVerb re-export), and migrate all 8 orchestrator callsites in the same task -- atomic typecheck-breaking change</name>
  <files>extensions/pi-claude-marketplace/presentation/reload-hint.ts, extensions/pi-claude-marketplace/presentation/index.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/presentation/reload-hint.ts (full file -- current 50-line composer with ReloadVerb type and verb-conditioned trailer formatting; the entire file is replaced)
    - extensions/pi-claude-marketplace/presentation/index.ts (full file -- current barrel; only the `export type { ReloadVerb }` line is deleted)
    - extensions/pi-claude-marketplace/presentation/plugin-list.ts (lines 21-45 -- `MAX_LINE_COLUMN = 66` private-const idiom; analog for `RELOAD_HINT_TRAILER = "/reload to pick up changes"`; D-06 corollary "PRIVATE to this file" applies)
    - extensions/pi-claude-marketplace/shared/markers.ts (full file -- confirm `RELOAD_HINT_PREFIX` export STAYS; the composer no longer imports it, but the constant is retained per D-CMC-08)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (lines 680-700 -- context for the line-690 callsite; understand surrounding flow before the 1-token edit)
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (lines 230-245 -- context for line-237 callsite)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts (lines 725-740 -- context for line-731 callsite)
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts (lines 365-380 and 865-880 -- context for BOTH callsites at 372 and 871; CONTEXT.md drift omitted these two; verified by research §2.3 and pattern mapper)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (lines 350-365 -- context for line-358 callsite)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (lines 273-285 -- context for line-278 callsite)
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts (lines 335-345 -- context for line-339-341 multi-line callsite; verb arg is on its own line so the edit drops a full line + comma)
    - tests/architecture/markers-snapshot.test.ts (lines 40-71 -- confirm RELOAD_HINT_PREFIX retention contract before touching shared/markers.ts; non-regression check)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§2.2 -- full target file body for reload-hint.ts; §2.3 -- callsite migration table; §R1, §R2, §R3 -- the three highest-risk scenarios for this task)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md ("presentation/reload-hint.ts (REWRITE)", "presentation/index.ts (MODIFY; barrel)", "Orchestrator callsite migrations (8 sites, mechanical)" sections -- explicit deltas and the multi-line shape for execute.ts:339-345)
  </read_first>
  <behavior>
    - After this task, tests/presentation/reload-hint.test.ts (rewritten in Task 1) passes -- all 5 assertions GREEN.
    - `npm run typecheck` exits 0 -- every callsite now matches the new 1-arg signature; no straggling `ReloadVerb` import compiles.
    - `npm run lint` exits 0 -- no new restricted-syntax violations.
    - `grep -rE 'reloadHint\\("(load|refresh|drop)"' extensions/pi-claude-marketplace/` returns NO matches -- no callsite still passes a string-literal verb as first arg.
    - `grep -rn 'reloadHint(' extensions/pi-claude-marketplace/` returns exactly 8 callsite matches in `orchestrators/` (plus the function definitions in `presentation/reload-hint.ts`).
  </behavior>
  <action>
    This task is the typecheck-breaking atomic edit. It MUST land all four pieces in one step: composer rewrite, barrel update, all 8 callsite migrations. Splitting any of these into separate tasks would leave `npm run check` red mid-plan, violating NFR-6.

    Piece 1 -- rewrite `extensions/pi-claude-marketplace/presentation/reload-hint.ts` (full file replacement, ~15-25 lines):
    - File-header comment cites CMC-14 / MSG-RH-1 (style guide §5), D-CMC-06 (replace-in-place migration), D-CMC-07 (local trailer constant -- not in shared/markers.ts which is transitional, not in shared/grammar/ which would be over-extraction for a one-consumer constant), and the MAX_LINE_COLUMN private-constant analog in `presentation/plugin-list.ts:30`.
    - REMOVE `import { RELOAD_HINT_PREFIX } from "../shared/markers.ts";` -- the new composer does not need it. The constant STAYS in shared/markers.ts per D-CMC-08 (only the import here is removed).
    - REMOVE `export type ReloadVerb = "load" | "refresh" | "drop";` -- verb selector retired per D-CMC-06.
    - Define a file-private `const RELOAD_HINT_TRAILER = "/reload to pick up changes";` directly under the imports (analog: `presentation/plugin-list.ts:30`'s `MAX_LINE_COLUMN`).
    - Rewrite `reloadHint` to the new signature: `export function reloadHint(names: readonly string[]): string { return names.length > 0 ? RELOAD_HINT_TRAILER : ""; }` -- single ternary, no verb branching, no name interpolation.
    - KEEP `appendReloadHint(body, hint)` body unchanged: `return hint === "" ? body : \`${body}\\n${hint}\`;` -- single-newline join. Add a TODO docstring referencing MSG-RH-1's "preceded by one blank line" requirement; note that the conformance pass to a double-newline join is Phase 13's mechanical refactor scope (per research R7 + Open Question 2).

    Piece 2 -- update `extensions/pi-claude-marketplace/presentation/index.ts` (single-line deletion):
    - DELETE the line `export type { ReloadVerb } from "./reload-hint.ts";`
    - KEEP `export { appendReloadHint, reloadHint } from "./reload-hint.ts";` unchanged.
    - All other re-exports (soft-dep helpers, renderMarketplaceList) unchanged.

    Piece 3 -- migrate all 8 orchestrator callsites (drop the verb literal first-arg; names expression passes through unchanged). Use the table in `<callsite_enumeration>` above as the binding enumeration. Examples (apply the pattern to all 8):
    - `orchestrators/plugin/install.ts:690` -- `reloadHint("load", stagedAny ? [plugin] : [])` becomes `reloadHint(stagedAny ? [plugin] : [])`
    - `orchestrators/plugin/uninstall.ts:237` -- `reloadHint("drop", droppedAny ? [plugin] : [])` becomes `reloadHint(droppedAny ? [plugin] : [])`
    - `orchestrators/plugin/update.ts:731` -- `reloadHint("refresh", updatedNames)` becomes `reloadHint(updatedNames)`
    - `orchestrators/plugin/reinstall.ts:372` -- `reloadHint("refresh", changedNames)` becomes `reloadHint(changedNames)`
    - `orchestrators/plugin/reinstall.ts:871` -- `reloadHint("refresh", outcome.resourcesChanged ? [outcome.name] : [])` becomes `reloadHint(outcome.resourcesChanged ? [outcome.name] : [])`
    - `orchestrators/marketplace/update.ts:358` -- `reloadHint("refresh", updatedNames)` becomes `reloadHint(updatedNames)`
    - `orchestrators/marketplace/remove.ts:278` -- `reloadHint("drop", removedSorted)` becomes `reloadHint(removedSorted)`
    - `orchestrators/import/execute.ts:339-341` -- multi-line; the `"load",` is on its own line; drop the line and the trailing comma. After: the `reloadHint(` opens, the names-expression argument is the only argument, the `)` closes.

    For each callsite, if there is an adjacent in-file comment that references "verb 'load'" / "verb 'refresh'" / "verb 'drop'" (stale after migration), UPDATE the comment to remove the verb-specific language (e.g., "compose a reload hint for the changed names" or similar; the existing surrounding intent -- when to suppress vs emit -- stays). Research notes this is a cleanliness concern, not mandatory; do it as part of the same callsite edit so reviewer sees a clean diff.

    Piece 4 -- DO NOT touch:
    - `extensions/pi-claude-marketplace/shared/markers.ts` (RELOAD_HINT_PREFIX retained per D-CMC-08)
    - `tests/architecture/markers-snapshot.test.ts` (still asserts RELOAD_HINT_PREFIX byte-equality)
    - `eslint.config.js` (CMC-37 -- no config widening)
    - `docs/messaging-style-guide.md` (Plan 12-03's atomic doc edit)

    Optional R1 mitigation (RECOMMENDED): Add a single inline comment in `shared/markers.ts` on or directly above the `RELOAD_HINT_PREFIX` export line, e.g., `// D-CMC-08: retained as a snapshot-test-only export through end of Phase 12; Phase 13's atomic ES-5 three-file edit deletes this constant + markers-snapshot row + PRD §6.12 row in one commit. Do NOT delete in Phase 12 -- markers-snapshot test fails.` This guards against a well-meaning cleanup pass that notices the export is no longer imported by source. If you add this comment, it is the ONLY edit allowed in shared/markers.ts; the constant value and export keyword stay byte-identical.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
    <automated>npm test -- tests/presentation/reload-hint.test.ts</automated>
    <automated>npm test -- tests/architecture/markers-snapshot.test.ts</automated>
    <automated>! grep -rE "reloadHint\\(\"(load|refresh|drop)\"" extensions/pi-claude-marketplace/</automated>
    <automated>! grep -F "ReloadVerb" extensions/pi-claude-marketplace/presentation/index.ts</automated>
    <automated>! grep -F "ReloadVerb" extensions/pi-claude-marketplace/presentation/reload-hint.ts</automated>
    <automated>! grep -rF "ReloadVerb" extensions/pi-claude-marketplace/orchestrators/</automated>
    <automated>! grep -F "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/presentation/reload-hint.ts</automated>
    <automated>grep -F "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/shared/markers.ts</automated>
    <automated>grep -F "RELOAD_HINT_TRAILER" extensions/pi-claude-marketplace/presentation/reload-hint.ts</automated>
    <automated>grep -F "/reload to pick up changes" extensions/pi-claude-marketplace/presentation/reload-hint.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `extensions/pi-claude-marketplace/presentation/reload-hint.ts` contains `const RELOAD_HINT_TRAILER = "/reload to pick up changes";` (file-private)
    - Source: `extensions/pi-claude-marketplace/presentation/reload-hint.ts` contains `export function reloadHint(names: readonly string[]): string` -- exact 1-arg signature
    - Source: `grep -F "ReloadVerb" extensions/pi-claude-marketplace/presentation/reload-hint.ts` returns NO match (type deleted)
    - Source: `grep -F "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/presentation/reload-hint.ts` returns NO match (import dropped)
    - Source: `grep -F "ReloadVerb" extensions/pi-claude-marketplace/presentation/index.ts` returns NO match (barrel re-export removed)
    - Source: `extensions/pi-claude-marketplace/presentation/index.ts` still contains `export { appendReloadHint, reloadHint } from "./reload-hint.ts";` (value re-exports kept)
    - Source (all 8 callsites migrated): `grep -rE "reloadHint\\(\"(load|refresh|drop)\"" extensions/pi-claude-marketplace/` returns NO match
    - Source (callsite count): `grep -rcE "reloadHint\\(" extensions/pi-claude-marketplace/orchestrators/ | grep -v ':0$'` shows reloadHint called in exactly 7 orchestrator files (install.ts, uninstall.ts, update.ts, reinstall.ts [twice], marketplace/update.ts, marketplace/remove.ts, import/execute.ts) summing to 8 callsites
    - Source (ReloadVerb purged from orchestrators): `grep -rF "ReloadVerb" extensions/pi-claude-marketplace/orchestrators/` returns NO match
    - Source (D-CMC-08 retention): `grep "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/shared/markers.ts` returns the export declaration line -- value unchanged at `"Run /reload to "`
    - Behavior: `npm test -- tests/presentation/reload-hint.test.ts` exits 0 (5 tests pass -- Task 1's RED test set turns GREEN here)
    - Behavior (non-regression): `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0 (D-CMC-08 retention not broken)
    - CLI: `npm run typecheck` exits 0 (signature change is consistent across all 8 callsites + barrel + composer)
    - CLI: `npm run lint` exits 0 (no new restricted-syntax violations)
    - CLI: `npm run check` exits 0 (full structural gate -- typecheck + ESLint + Prettier + tests + architecture)
    - Source (eslint not widened): `git diff --name-only HEAD -- eslint.config.js` is empty
    - Source (style guide untouched): `git diff --name-only HEAD -- docs/messaging-style-guide.md` is empty (Plan 12-03 owns it)
    - Source (markers-snapshot test untouched): `git diff --name-only HEAD -- tests/architecture/markers-snapshot.test.ts` is empty
    - Source (D-CMC-10 CHANGELOG citation -- BINDING per W-1 / CLAUDE.md release-PR step): The release entry in `CHANGELOG.md` that includes Phase 12 MUST cite the carve-out. Concretely: `grep -F "D-CMC-10" CHANGELOG.md` returns at least one match, and that match appears under the Phase-12 release heading. The entry MUST include (verbatim or paraphrased) the carve-out sentence: "8 reload-hint callsite trailers now emit /reload to pick up changes; Phase 12 carve-out per D-CMC-10; roadmap criterion #2 authorizes". If `CHANGELOG.md` does not yet exist, this task MUST create it with the Phase-12 release entry containing the citation; if a Phase-12 release entry already exists without the citation, this task MUST extend it. This is a structural ship-gate: the carve-out cannot ship without the CHANGELOG note. (Source: CONTEXT.md D-CMC-10 -- "Planner SHOULD record this carve-out explicitly in PLAN.md / CHANGELOG.md"; promoted to MUST per W-1.)
  </acceptance_criteria>
  <done>Composer is collapsed to the single-trailer form, ReloadVerb is deleted, all 8 callsites compile under the new 1-arg signature, the rewritten reload-hint test passes, markers-snapshot test stays green, and npm run check is green end-to-end. RELOAD_HINT_PREFIX in shared/markers.ts is retained (optionally annotated with a do-not-delete comment).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → reload-hint output | The composer renders a fixed literal trailer (no name interpolation in the new form), eliminating any concern about names containing shell metacharacters, quote-injection in the rendered output, or markdown-control characters. |
| orchestrators → presentation/reload-hint.ts | Pure function call; names argument is an array of plugin/marketplace identifiers already validated upstream by the orchestrator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.02-01 | Information Disclosure | reloadHint output | accept | New trailer is a fixed literal; no plugin/marketplace names are emitted in the trailer text (a regression from the old form which interpolated names -- the new form is strictly less revealing) |
| T-12.02-02 | Tampering | RELOAD_HINT_PREFIX deletion | mitigate | Task 2 verification asserts `grep "RELOAD_HINT_PREFIX" shared/markers.ts` still returns the export; optional inline do-not-delete comment guards against well-meaning cleanup (R1 mitigation) |
| T-12.02-03 | Denial of Service | callsite migration miss | mitigate | `grep -rE "reloadHint\\(\"(load|refresh|drop)\"" extensions/` returns 0 matches as acceptance criteria; typecheck catches any missed callsite immediately (signature is type-level breaking) -- R2 mitigation |

**Summary:** No new attack surface; the new composer EMITS LESS user data than the old (no name interpolation in the trailer). ASVS L1 not applicable (no input handling, no auth, no crypto). The threat surface is internal: regression risk on RELOAD_HINT_PREFIX deletion (R1) and missed callsites (R2), both mitigated by acceptance criteria asserting source-level invariants.
</threat_model>

<verification>
- `npm run check` exits 0 at end of plan (full structural gate).
- `npm test -- tests/presentation/reload-hint.test.ts` exits 0 -- 5 tests pass.
- `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0 -- D-CMC-08 retention not regressed.
- `grep -rE "reloadHint\\(\"(load|refresh|drop)\"" extensions/pi-claude-marketplace/` returns 0 matches -- no callsite still passes a string-literal verb.
- `grep -rF "ReloadVerb" extensions/pi-claude-marketplace/` returns 0 matches outside of any updated stale-comment context -- type is fully purged from source.
- `grep "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/shared/markers.ts` returns the export line -- constant retained for snapshot test (D-CMC-08).
- `git diff --name-only HEAD -- eslint.config.js` is empty (CMC-37 / IL-3 discipline preserved).
- `git diff --name-only HEAD -- docs/messaging-style-guide.md tests/architecture/markers-snapshot.test.ts` is empty (out-of-scope files untouched).
- PLAN.md (this file) records the D-CMC-10 carve-out narrative in the `<objective>` block; SUMMARY.md MUST echo it; CHANGELOG.md MUST cite it in the Phase-12 release entry (`grep -F "D-CMC-10" CHANGELOG.md` returns at least one match in the Phase-12 entry -- binding per W-1).
</verification>

<success_criteria>
- presentation/reload-hint.ts is a ~15-25 line file exporting `reloadHint(names: readonly string[]): string` (returning the canonical trailer or "") and `appendReloadHint(body, hint)` (single-newline join with a TODO comment for Phase 13 blank-line conformance). The trailer literal `/reload to pick up changes` lives as a file-private `RELOAD_HINT_TRAILER` const. No `ReloadVerb` type. No `RELOAD_HINT_PREFIX` import.
- presentation/index.ts no longer re-exports `ReloadVerb`; the value re-exports for `reloadHint` and `appendReloadHint` are intact.
- All 8 orchestrator callsites pass only the names array -- install.ts:690, uninstall.ts:237, update.ts:731, reinstall.ts:372, reinstall.ts:871, marketplace/update.ts:358, marketplace/remove.ts:278, import/execute.ts:339-341.
- tests/presentation/reload-hint.test.ts has exactly 5 tests passing: 3 MSG-RH-1 (empty/single/multi) + 2 appendReloadHint (suppression/join).
- npm run check is green at end of plan.
- shared/markers.ts RELOAD_HINT_PREFIX export is untouched (export and value byte-identical; optional do-not-delete comment is the only allowed addition).
- tests/architecture/markers-snapshot.test.ts is untouched.
- eslint.config.js is untouched.
- docs/messaging-style-guide.md is untouched (Plan 12-03 owns the §14.1 doc edit).
- PLAN.md (this file) records the D-CMC-10 carve-out narrative explicitly in the objective; SUMMARY.md MUST echo it; CHANGELOG.md MUST cite the carve-out in the Phase-12 release entry (`grep -F "D-CMC-10" CHANGELOG.md` returns at least one match -- binding per W-1).
</success_criteria>

<output>
Create `.planning/phases/12-messaging-foundations-renderer-primitives/12-02-SUMMARY.md` when done. The SUMMARY.md MUST include a "D-CMC-10 Carve-Out" section restating the user-visible reload-hint trailer change and the authorizing roadmap criterion #2 -- so a reviewer reading SUMMARY.md does not need to re-derive the carve-out from the diff.
</output>
