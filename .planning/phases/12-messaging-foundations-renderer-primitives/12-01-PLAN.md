---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts
  - extensions/pi-claude-marketplace/shared/grammar/reasons.ts
  - tests/architecture/grammar-frontmatter.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md
autonomous: true
requirements: [CMC-08, CMC-11]
requirements_addressed: [CMC-08, CMC-11]
tags: [messaging, grammar, constants, drift-test]

must_haves:
  truths:
    - "Importing STATUS_TOKENS yields exactly the 14 entries in docs/messaging-style-guide.md frontmatter status_tokens: block (no reinstalled token)"
    - "Importing REASONS yields exactly the 23 entries in the frontmatter reasons: block (frontmatter is the binding count; CONTEXT/REQ/ROADMAP previously said 24, reconciled to 23 in this plan)"
    - "A new drift test under tests/architecture/ fails fast when STATUS_TOKENS or REASONS diverge from the style-guide frontmatter"
    - "REQUIREMENTS.md CMC-08 no longer claims a 15th 'reinstalled' status token (researcher confirmed 'reinstalled' is internal partition kind, not user-visible)"
    - "REQUIREMENTS.md CMC-11, ROADMAP Phase 12 scope, and CONTEXT.md domain text say '23 reasons' (frontmatter binding)"
    - "shared/grammar/ is a new sibling to shared/markers.ts and shared/notify.ts; it sits below presentation/ and persistence/ (D-11 layering preserved)"
    - "shared/notify.ts remains the SOLE ctx.ui.notify callsite (D-07); no new direct callsite anywhere in this plan"
    - "RELOAD_HINT_PREFIX in shared/markers.ts is NOT touched by this plan (D-CMC-08 retention through end of Phase 12; deletion is Phase 13's atomic three-file edit)"
    - "npm run check (typecheck + ESLint + Prettier + tests) is green after every task in this plan (NFR-6)"
    - "No new console.warn callsite, no process.stdout/.stderr write, no eslint.config.js widening (IL-2/IL-3 preserved)"
    - "D-CMC-10 carve-out (user-visible reload-hint trailer change) is NOT in scope for this plan; landed in 12-02"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts"
      provides: "Closed STATUS_TOKENS array + StatusToken literal union"
      exports: ["STATUS_TOKENS", "StatusToken"]
      contains: "as const"
    - path: "extensions/pi-claude-marketplace/shared/grammar/reasons.ts"
      provides: "Closed REASONS array + Reason literal union"
      exports: ["REASONS", "Reason"]
      contains: "as const"
    - path: "tests/architecture/grammar-frontmatter.test.ts"
      provides: "Set-equality drift test against style-guide frontmatter; throws-on-missing-key guards"
      contains: "extractFrontmatterList"
  key_links:
    - from: "tests/architecture/grammar-frontmatter.test.ts"
      to: "extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts"
      via: "import { STATUS_TOKENS }"
      pattern: "STATUS_TOKENS"
    - from: "tests/architecture/grammar-frontmatter.test.ts"
      to: "extensions/pi-claude-marketplace/shared/grammar/reasons.ts"
      via: "import { REASONS }"
      pattern: "REASONS"
    - from: "tests/architecture/grammar-frontmatter.test.ts"
      to: "docs/messaging-style-guide.md"
      via: "readFile + regex extract"
      pattern: "extractFrontmatterList"
---

<objective>
Land the closed-set grammar primitives (STATUS_TOKENS and REASONS) and their drift guard, then reconcile the planning-doc inconsistencies surfaced by research: (a) drop the spurious "+ reinstalled" clause from REQUIREMENTS.md CMC-08, and (b) align REQUIREMENTS.md CMC-11 / ROADMAP / CONTEXT.md from "24 reasons" to "23 reasons" because the style-guide frontmatter (binding) has 23 entries.

Purpose: These constants are the foundation Phase 13's mechanical refactor will type its renderer callsites against (`function renderRow(status: StatusToken)`), and the drift test is the spring-loaded gate that makes the style-guide frontmatter the binding contract. Landing the doc reconciliations in the same plan keeps the count assertion coherent across all artifacts (D-CMC-15 atomic-alignment spirit).

Output: Two new pure-data modules under `shared/grammar/`, one new architectural drift test, and three doc edits that reconcile the codebase planning artifacts with the binding frontmatter.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md
@docs/messaging-style-guide.md
@extensions/pi-claude-marketplace/presentation/plugin-list.ts
@tests/helpers/prd-extract.ts
@tests/architecture/markers-snapshot.test.ts
@CLAUDE.md

<interfaces>
<!-- The constants modules export the following shape, derived from analog
     presentation/plugin-list.ts:45 ("PluginRenderStatus" inline-union) but in
     the FULLER form per D-CMC-03 (array + indexed-access derived union),
     because Phase 14's drift test iterates the array. -->

From extensions/pi-claude-marketplace/presentation/plugin-list.ts:
```typescript
// Inline-union shorthand (analog -- DO NOT use this form in shared/grammar/)
export type PluginRenderStatus = "installed" | "available" | "uninstallable";

// Private layout constant idiom (analog for D-CMC-07 in 12-02; reference only here)
const MAX_LINE_COLUMN = 66;
```

The shared/grammar/ shape (D-CMC-03) is:
```typescript
export const STATUS_TOKENS = ["installed", /* ... 13 more entries ... */] as const;
export type StatusToken = (typeof STATUS_TOKENS)[number];
```

Hand-rolled regex extractor scaffold (analog: tests/helpers/prd-extract.ts -- 28 lines, pure regex against a known-shape markdown surface). The new drift test defines extractFrontmatterList LOCALLY (D-CMC-04 keeps the loader test-local in Phase 12; Phase 14 owns the richer reader).
</interfaces>

<binding_frontmatter>
<!-- The binding contract. Constants modules MUST be byte-equal to these lists.
     Verified at docs/messaging-style-guide.md:3-41 (this session). -->

status_tokens (14 entries, order from frontmatter):
  1. installed
  2. updated
  3. uninstalled
  4. added
  5. removed
  6. available
  7. unavailable
  8. upgradable
  9. skipped
  10. failed
  11. rollback failed
  12. manual recovery
  13. no marketplaces
  14. no plugins

reasons (23 entries, order from frontmatter):
  1. up-to-date
  2. not found
  3. already installed
  4. not installed
  5. not in manifest
  6. invalid manifest
  7. no longer installable
  8. unsupported source
  9. hooks
  10. lspServers
  11. requires pi-subagents
  12. requires pi-mcp
  13. rollback partial
  14. unreadable
  15. unparseable
  16. unreadable manifest
  17. source mismatch
  18. plugins remain
  19. concurrently uninstalled
  20. concurrently updated
  21. stale clone
  22. duplicate name
  23. lock held
</binding_frontmatter>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared/grammar/status-tokens.ts and shared/grammar/reasons.ts with as-const arrays + derived literal unions, byte-equal to the style-guide frontmatter</name>
  <files>extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts, extensions/pi-claude-marketplace/shared/grammar/reasons.ts</files>
  <read_first>
    - docs/messaging-style-guide.md (lines 1-60 -- frontmatter; this IS the binding contract; copy entries verbatim in frontmatter order)
    - extensions/pi-claude-marketplace/presentation/plugin-list.ts (lines 1-80 -- file-header comment style, MAX_LINE_COLUMN private-constant idiom for reference, PluginRenderStatus inline-union analog)
    - extensions/pi-claude-marketplace/shared/markers.ts (full file -- sibling module under shared/; understand the layering boundary and header-comment convention before adding a new shared/grammar/ sibling)
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 1-19 -- header-comment style for shared/ modules)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§2.1 -- diff sketches with full file content; §3.1 -- REASONS count finding; §2.5 -- drift test consumer shape; do NOT inline the suggested file body literally; use as guidance for shape and header text)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md (the two "shared/grammar/*.ts (NEW; shared/grammar; pure-data)" sections -- explicit deltas vs the inline-union analog and the as-const array + derived-union full-form requirement)
  </read_first>
  <behavior>
    - Test 1 (drift, set-equality): STATUS_TOKENS sorted equals frontmatter status_tokens block sorted (14 entries)
    - Test 2 (drift, set-equality): REASONS sorted equals frontmatter reasons block sorted (23 entries)
    - Test 3 (extractor guard): extractFrontmatterList throws on missing frontmatter
    - Test 4 (extractor guard): extractFrontmatterList throws on missing key
    - Type-level: `const x: StatusToken = "installed"` compiles; `const y: StatusToken = "reinstalled"` does NOT compile (covered by typecheck, not a runtime test)
  </behavior>
  <action>
    Create the two files under a new directory `extensions/pi-claude-marketplace/shared/grammar/`. Each file follows D-CMC-03's `as const` + derived-literal-union shape -- NOT the inline-union shorthand from `presentation/plugin-list.ts:45`. The drift test (Task 2) iterates the array, which requires a runtime value.

    For `status-tokens.ts`:
    - Export `STATUS_TOKENS` as a `readonly` tuple via `as const`. Members: the 14 entries listed in `<binding_frontmatter>` above, in frontmatter order (drift test sorts before compare so order does not affect correctness, but mirroring frontmatter order is a readability nicety).
    - Export `StatusToken = (typeof STATUS_TOKENS)[number]` derived literal union.
    - DO NOT add a 15th `reinstalled` token (D-CMC-08 reconciliation; researcher confirmed `reinstalled` is `ReinstallPluginPartition` discriminant at `orchestrators/types.ts:12`, never user-visible).
    - DO NOT define a sub-union or branded type for `(no marketplaces)` / `(no plugins)` (D-CMC-05: flat membership).
    - File-header comment style mirrors `shared/markers.ts` + `shared/notify.ts`: file path on line 1; blank line; cite CMC-08 + the binding frontmatter location (`docs/messaging-style-guide.md` `status_tokens:`); cite the drift test (`tests/architecture/grammar-frontmatter.test.ts`) as the guard; cite D-CMC-01 (location), D-CMC-02 (one file per set), D-CMC-03 (shape), D-CMC-05 (flat membership) as the locking decisions; note that `(no marketplaces)` / `(no plugins)` are flat members and the bare-token render shape is a Phase 13 renderer concern.

    For `reasons.ts`:
    - Same shape: `REASONS` as `as const` array; `Reason` derived literal union.
    - Members: the 23 entries listed in `<binding_frontmatter>`, in frontmatter order.
    - DO NOT include surrounding braces in the stored constants (the `{<reason>}` brace form is composed by the renderer at emission time in Phase 13).
    - File-header comment cites CMC-11 + the binding frontmatter location; cites the same drift test; cites D-CMC-01..D-CMC-03 as locking decisions; notes that the count is 23 (research-confirmed against the binding frontmatter; the previously-stated "24" in REQUIREMENTS / ROADMAP / CONTEXT is reconciled to 23 in Task 3 of this plan).

    Layering note in both file headers: `shared/grammar/` sits below `presentation/` and `persistence/` per D-11 -- importable from anywhere without violating layer rules.

    Do NOT yet create the drift test (Task 2) or edit planning docs (Task 3); those are separate tasks for atomicity.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>grep -c '"installed"\|"updated"\|"uninstalled"\|"added"\|"removed"\|"available"\|"unavailable"\|"upgradable"\|"skipped"\|"failed"\|"rollback failed"\|"manual recovery"\|"no marketplaces"\|"no plugins"' extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts</automated>
    <automated>grep -F "reinstalled" extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts; test $? -ne 0</automated>
    <automated>grep -F "as const" extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts</automated>
    <automated>grep -F "as const" extensions/pi-claude-marketplace/shared/grammar/reasons.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` exists and contains `export const STATUS_TOKENS = [` followed by exactly 14 string literals (one per frontmatter entry) and `] as const;`, followed by `export type StatusToken = (typeof STATUS_TOKENS)[number];`
    - Source: `extensions/pi-claude-marketplace/shared/grammar/reasons.ts` exists and contains `export const REASONS = [` followed by exactly 23 string literals and `] as const;`, followed by `export type Reason = (typeof REASONS)[number];`
    - Source: `grep -F "reinstalled" extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` returns NO match (D-CMC-08 reconciliation)
    - Source: Neither file imports from `presentation/`, `persistence/`, `orchestrators/`, or `bridges/` (D-11 layering -- shared/grammar/ sits below all layers)
    - Source: Each file-header comment cites the relevant CMC requirement ID, the binding frontmatter location (`docs/messaging-style-guide.md`), and at least D-CMC-01 + D-CMC-03
    - CLI: `npm run typecheck` exits 0 (the `as const` + indexed-access derivation must compile under TypeScript strict mode)
    - CLI: `npm run lint` exits 0 (no ESLint regressions; no new restricted-syntax violations)
    - Source (non-regression): `extensions/pi-claude-marketplace/shared/markers.ts` still exports `RELOAD_HINT_PREFIX` (D-CMC-08 retention -- Phase 12 must not delete it; verify `grep "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/shared/markers.ts` returns the export line)
    - Source (non-regression): `eslint.config.js` is unchanged in this task's diff (`git diff --name-only HEAD -- eslint.config.js` is empty)
  </acceptance_criteria>
  <done>STATUS_TOKENS (14 entries) and REASONS (23 entries) are exported from new shared/grammar/ modules with as-const + derived-union shape, no reinstalled token in the status set, typecheck passes, and the layering boundary is preserved. RELOAD_HINT_PREFIX in shared/markers.ts is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/architecture/grammar-frontmatter.test.ts -- drift test asserting STATUS_TOKENS and REASONS set-equality vs the style-guide frontmatter, with throws-on-missing-key guards</name>
  <files>tests/architecture/grammar-frontmatter.test.ts</files>
  <read_first>
    - tests/architecture/markers-snapshot.test.ts (lines 1-71 -- direct scaffolding analog: file-I/O + regex extract + assert.deepEqual; REPO_ROOT pattern via fileURLToPath; per-pair assertion loop)
    - tests/helpers/prd-extract.ts (full file -- hand-rolled regex extractor precedent that researcher cited as the binding pattern for D-CMC-04; understand the regex shape, the throws-on-missing-row guard, the no-yaml-dep approach)
    - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts (read the file Task 1 just created so the test's import path and constant identifier match exactly)
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts (same -- confirm exported identifier)
    - docs/messaging-style-guide.md (lines 1-60 -- frontmatter shape; the regex must handle the `^<key>:\n  - <value>` form ending at the next top-level key or `---`)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§2.5 -- full test-file sketch including the extractFrontmatterList regex; the test recommendation is 4 assertions: 2 set-equality + 2 extractor guards)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md (the "tests/architecture/grammar-frontmatter.test.ts (NEW)" section -- full deltas vs markers-snapshot analog; the local-extractor decision per D-CMC-04)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md (CMC-08 + CMC-11 rows -- automated command target: `npm test tests/architecture/grammar-frontmatter.test.ts`)
  </read_first>
  <behavior>
    - Test A: "STATUS_TOKENS is set-equal to style-guide frontmatter status_tokens" -- passes when both arrays have the same 14 members irrespective of order; fails with a clear message naming both lengths if they diverge
    - Test B: "REASONS is set-equal to style-guide frontmatter reasons" -- passes when both arrays have the same 23 members; fails clearly on divergence
    - Test C: extractFrontmatterList throws "no YAML frontmatter found" when the input has no `---` delimited head
    - Test D: extractFrontmatterList throws `key "..." not found` when the requested key is absent from a present frontmatter
  </behavior>
  <action>
    Create the new test file `tests/architecture/grammar-frontmatter.test.ts` using the scaffolding shape from `tests/architecture/markers-snapshot.test.ts:1-16` (imports: `node:assert/strict`, `node:fs/promises` readFile, `node:path`, `node:test`, `node:url` fileURLToPath; REPO_ROOT resolved from `import.meta.url`).

    Define a LOCAL function `extractFrontmatterList(md: string, key: string): string[]` inside this test file (D-CMC-04: keep test-local; do NOT extract to `tests/helpers/` in Phase 12 -- Phase 14's richer reader owns the eventual consolidation). The regex shape is fixed because the frontmatter is fixed-shape:
    - First, extract the frontmatter block via `^---\n([\s\S]*?)\n---\n` against the file head; throw `Error("messaging-style-guide.md: no YAML frontmatter found at file head")` if no match.
    - Then, match `^<key>:\n((?:  - .+\n)+)` within the captured frontmatter (multiline flag); throw `Error('messaging-style-guide.md frontmatter: key "<key>" not found')` if no match.
    - Split the captured block on `\n`, strip the `  - ` prefix from each non-empty line, return the array. Throw `Error('messaging-style-guide.md frontmatter: key "<key>" has no items')` if the result is empty.

    Import `STATUS_TOKENS` from `../../extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` and `REASONS` from `../../extensions/pi-claude-marketplace/shared/grammar/reasons.ts`.

    Define `STYLE_GUIDE_PATH = path.join(REPO_ROOT, "docs/messaging-style-guide.md")`.

    Write four tests:
    - "D-CMC-04: STATUS_TOKENS is set-equal to style-guide frontmatter status_tokens" -- async; readFile STYLE_GUIDE_PATH utf8; extractFrontmatterList(md, "status_tokens"); `assert.deepEqual([...STATUS_TOKENS].sort(), [...frontmatterTokens].sort(), \`STATUS_TOKENS drift vs frontmatter -- code has ${STATUS_TOKENS.length}, frontmatter has ${frontmatterTokens.length}\`);`
    - "D-CMC-04: REASONS is set-equal to style-guide frontmatter reasons" -- analogous, with REASONS / "reasons"
    - "extractFrontmatterList throws if frontmatter is missing" -- `assert.throws(() => extractFrontmatterList("# No frontmatter here\n", "status_tokens"), /no YAML frontmatter found/);`
    - "extractFrontmatterList throws if key is missing" -- `assert.throws(() => extractFrontmatterList("---\nversion: 1.0\nother:\n  - a\n---\n", "status_tokens"), /key "status_tokens" not found/);`

    DO NOT add a `yaml` or `js-yaml` dependency (researcher's RECOMMENDATION: hand-rolled per `prd-extract.ts` precedent; adopting a YAML dep for one test is over-extraction and requires a checkpoint:human-verify gate per project guidelines).

    DO NOT extract the regex helper to `tests/helpers/grammar-extract.ts` (D-CMC-04: stays test-local in Phase 12).
  </action>
  <verify>
    <automated>npm test -- tests/architecture/grammar-frontmatter.test.ts</automated>
    <automated>grep -c "extractFrontmatterList" tests/architecture/grammar-frontmatter.test.ts</automated>
    <automated>grep -F "STYLE_GUIDE_PATH" tests/architecture/grammar-frontmatter.test.ts</automated>
    <automated>! grep -E "^import.*from ['\"](yaml|js-yaml)['\"]" tests/architecture/grammar-frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/architecture/grammar-frontmatter.test.ts` exists and imports `STATUS_TOKENS` and `REASONS` from the new `shared/grammar/` modules
    - Source: The file defines `extractFrontmatterList` as a LOCAL function (not imported from `tests/helpers/`)
    - Source: NO import from `yaml`, `js-yaml`, or any other YAML library (`grep -E "from ['\"](yaml|js-yaml)['\"]" tests/architecture/grammar-frontmatter.test.ts` returns nothing)
    - Behavior: `npm test -- tests/architecture/grammar-frontmatter.test.ts` exits 0 and runs at least 4 tests (2 set-equality + 2 extractor guards)
    - Behavior: Both set-equality tests pass given the constants from Task 1 -- STATUS_TOKENS length must be 14 and REASONS length must be 23 (validated against the binding frontmatter at `docs/messaging-style-guide.md:3-41`)
    - Source: `package.json` is unchanged (`git diff --name-only HEAD -- package.json package-lock.json` is empty -- no new dependency added)
    - CLI (non-regression): `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0 (D-CMC-08 retention not regressed)
  </acceptance_criteria>
  <done>The new drift test exists under tests/architecture/, asserts set-equality for both STATUS_TOKENS and REASONS against the style-guide frontmatter, includes two extractor guards, uses no YAML library, and passes locally. Markers-snapshot test stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile planning docs -- drop "+ reinstalled" clause from REQUIREMENTS.md CMC-08; update "24 reasons" to "23 reasons" in REQUIREMENTS.md CMC-11, ROADMAP.md Phase 12 scope and Success Criteria, and CONTEXT.md domain text</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 370-395 -- full CMC-08 and CMC-11 entries; CMC-08 currently lists 14 frontmatter tokens "plus the `reinstalled` token used in reinstall cascades" which is the clause to drop; CMC-11 says "24 reasons" which becomes "23")
    - .planning/ROADMAP.md (lines 125-156 -- Phase 12 Scope and Success Criteria; line 136 says "all 24 reasons including the v1.3 additions"; line 149 success criterion #1 says "exactly the 24 entries in `reasons:`"; both become "23")
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md (full file -- domain text in `<domain>` section may say "24 reasons"; also `<deferred>` section flags this exact reconciliation as the planner's responsibility; this task fulfills that responsibility)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§3.1 -- "DROP" recommendation with evidence table; §2.1 -- REASONS COUNT DISCREPANCY paragraph with frontmatter-binding rationale; §3.3 -- plan decomposition assigns the doc reconciliation to Plan 12-01)
    - docs/messaging-style-guide.md (lines 1-60 -- frontmatter; this IS the count source of truth)
  </read_first>
  <action>
    Perform three coordinated planning-doc edits. The frontmatter (`docs/messaging-style-guide.md:1-60`) is the binding contract: 14 status tokens, 23 reasons. The drift test from Task 2 enforces this. The doc reconciliations bring REQUIREMENTS.md, ROADMAP.md, and CONTEXT.md into alignment with the constants and the drift gate.

    Edit 1 -- `.planning/REQUIREMENTS.md` CMC-08:
    - Locate the CMC-08 entry (around line 372). It currently ends with: "...`no marketplaces`, `no plugins`, plus the `reinstalled` token used in reinstall cascades). The legacy `unchanged` partition is folded into `(skipped) {up-to-date}` -- `unchanged` is not a distinct token (style guide §3.2)."
    - Remove the clause ", plus the `reinstalled` token used in reinstall cascades" -- close the parenthesis directly after "`no plugins`". Rationale (cite inline as a follow-up sentence or footnote): "Per Phase 12 research §3.1, `reinstalled` is the `ReinstallPluginPartition` discriminant at `orchestrators/types.ts:12`, never rendered as a user-visible parenthesised token; the closed set is the 14 frontmatter entries."
    - Keep the `unchanged` folding sentence intact.

    Edit 2 -- `.planning/REQUIREMENTS.md` CMC-11:
    - Locate the CMC-11 entry (around line 378). It says: "Reasons render only from the closed enum in the style-guide §4 frontmatter (24 reasons). New v1.3 additions over the V1 set: ..."
    - Replace "(24 reasons)" with "(23 reasons; the binding count is the frontmatter, per Phase 12 research §2.1 -- previously written as 24 in this requirement and the roadmap; reconciled to match the style-guide frontmatter via the Phase 12 drift test at tests/architecture/grammar-frontmatter.test.ts)".

    Edit 3 -- `.planning/ROADMAP.md` Phase 12 scope text (line 136):
    - Current: "all 24 reasons including the v1.3 additions"
    - New: "all 23 reasons including the v1.3 additions" -- and append a parenthetical note "(frontmatter at docs/messaging-style-guide.md is the binding count; reconciled from 24 to 23 in Plan 12-01 per Phase 12 research §2.1)".

    Edit 4 -- `.planning/ROADMAP.md` Phase 12 Success Criterion #1 (line 149):
    - Current: "importing the reasons enum module yields exactly the 24 entries in `reasons:` (programmatic equality test against the YAML)"
    - New: "importing the reasons enum module yields exactly the 23 entries in `reasons:` (programmatic equality test against the YAML)"

    Edit 5 -- `.planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md`:
    - Locate any "24" references that mean "reasons count" -- likely in the `<domain>` opening section (the "Closed `status-tokens` and `reasons` constants modules" enumeration) and possibly in `<deferred>` where the issue was flagged.
    - Update any "24 reasons" → "23 reasons". Add a brief reconciliation note where appropriate citing Plan 12-01 as the landing site. Do NOT remove the `<deferred>` block's framing -- the reconciliation IS what this task lands; the deferred-item note can be updated to past tense ("Resolved in Plan 12-01 -- frontmatter binding; count is 23.").

    DO NOT modify `docs/messaging-style-guide.md` in this task (that is Plan 12-03's atomic edit per D-CMC-15).
    DO NOT modify any code files in this task.
    DO NOT modify `.planning/STATE.md` (that updates happen automatically via gsd-sdk during commit flow).
  </action>
  <verify>
    <automated>! grep -F "plus the \`reinstalled\` token used in reinstall cascades" .planning/REQUIREMENTS.md</automated>
    <automated>! grep -F "24 reasons" .planning/REQUIREMENTS.md</automated>
    <automated>! grep -F "24 reasons" .planning/ROADMAP.md</automated>
    <automated>! grep -F "24 entries in \`reasons:\`" .planning/ROADMAP.md</automated>
    <automated>grep -F "23 reasons" .planning/REQUIREMENTS.md</automated>
    <automated>grep -F "23 reasons" .planning/ROADMAP.md</automated>
    <automated>npm test -- tests/architecture/grammar-frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -F "plus the \`reinstalled\` token used in reinstall cascades" .planning/REQUIREMENTS.md` returns NO match (clause dropped)
    - Source: `grep -F "24 reasons" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md` returns NO match (no remaining "24 reasons")
    - Source: `grep -F "24 entries in \`reasons:\`" .planning/ROADMAP.md` returns NO match (Success Criterion #1 updated)
    - Source: `grep -F "23 reasons" .planning/REQUIREMENTS.md` returns at least one match (CMC-11 updated)
    - Source: `grep -F "23 reasons" .planning/ROADMAP.md` returns at least one match (Phase 12 scope updated)
    - Source: `git diff --name-only HEAD` includes `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md` -- and does NOT include `docs/messaging-style-guide.md` (deferred to Plan 12-03 per D-CMC-15)
    - Behavior (drift test still green): `npm test -- tests/architecture/grammar-frontmatter.test.ts` exits 0 -- doc edits do not affect the binding frontmatter, so the drift test continues to pass with REASONS.length === 23
    - CLI: `npm run check` exits 0 (full structural gate green at end of plan)
  </acceptance_criteria>
  <done>REQUIREMENTS.md CMC-08 no longer asserts a 15th `reinstalled` token; REQUIREMENTS.md CMC-11, ROADMAP.md Phase 12 scope + Success Criterion #1, and CONTEXT.md domain text all say "23 reasons" (matching the binding frontmatter). Drift test stays green. Style-guide doc edit deferred to Plan 12-03 per D-CMC-15.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → tests | The constants modules are pure data exported under shared/. The test layer reads them; no untrusted input crosses any boundary. |
| docs → tests | The drift test reads `docs/messaging-style-guide.md` as a trusted in-tree file. No network, no user input, no external data source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.01-01 | Tampering | shared/grammar/*.ts constants | mitigate | Drift test (Task 2) fails CI if constants diverge from frontmatter; frontmatter changes require explicit doc edit |
| T-12.01-02 | Information Disclosure | grammar-frontmatter.test.ts (file read) | accept | The style-guide is a tracked in-tree doc, not sensitive; readFile is scoped to a fixed absolute path under REPO_ROOT |
| T-12.01-03 | Denial of Service | extractFrontmatterList regex | accept | Inputs are fixed in-tree files; no untrusted input source; regex is linear-time on bounded input |

**Summary:** No new attack surface introduced. The constants modules are pure data; the drift test reads a tracked doc with a hand-rolled regex over a fixed-shape input. ASVS L1 not applicable (no input handling, no network, no auth, no crypto).
</threat_model>

<verification>
- `npm run check` exits 0 at end of plan (full structural gate: typecheck + ESLint + Prettier + tests).
- Drift test exists and passes: `npm test -- tests/architecture/grammar-frontmatter.test.ts` exits 0.
- Markers-snapshot test still green (D-CMC-08 retention): `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0.
- No new npm dependencies: `git diff --name-only HEAD -- package.json package-lock.json` is empty.
- No eslint.config.js changes: `git diff --name-only HEAD -- eslint.config.js` is empty.
- D-CMC-08 retention: `grep "RELOAD_HINT_PREFIX" extensions/pi-claude-marketplace/shared/markers.ts` still returns the export line.
- Doc reconciliation landed: no "24 reasons" or "+ reinstalled" clause remains in REQUIREMENTS.md / ROADMAP.md / 12-CONTEXT.md.
</verification>

<success_criteria>
- STATUS_TOKENS exported from shared/grammar/status-tokens.ts as a 14-entry `as const` array + derived `StatusToken` literal union; no `reinstalled` member.
- REASONS exported from shared/grammar/reasons.ts as a 23-entry `as const` array + derived `Reason` literal union.
- tests/architecture/grammar-frontmatter.test.ts exists, contains 4 tests (2 set-equality + 2 extractor guards), uses no YAML dependency, and passes.
- REQUIREMENTS.md, ROADMAP.md, and CONTEXT.md are reconciled: CMC-08 has no "+ reinstalled" clause; CMC-11 / ROADMAP / CONTEXT say "23 reasons" (not 24).
- npm run check is green at end of plan.
- shared/markers.ts RELOAD_HINT_PREFIX export is untouched (D-CMC-08).
- eslint.config.js is untouched (IL-3 / CMC-37 discipline preserved).
- docs/messaging-style-guide.md is untouched (Plan 12-03 owns the §14.1 atomic doc edit per D-CMC-15).
</success_criteria>

<output>
Create `.planning/phases/12-messaging-foundations-renderer-primitives/12-01-SUMMARY.md` when done.
</output>
