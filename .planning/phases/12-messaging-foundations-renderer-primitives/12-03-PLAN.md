---
phase: 12
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/persistence/migrate.ts
  - docs/messaging-style-guide.md
  - tests/persistence/migrate.test.ts
autonomous: true
requirements: [CMC-36, CMC-37]
requirements_addressed: [CMC-36, CMC-37]
tags: [messaging, migrate, console-warn, IL-3, style-guide]

must_haves:
  truths:
    - "persistence/migrate.ts:178 console.warn body is BYTE-EXACT the §14.1 wording: Legacy marketplace migration could not be persisted to ${stateJsonPath}; the in-memory normalized state is being used and the on-disk state.json is unchanged. Cause: ${errMsg}."
    - "The line directly above the console.warn call is the verbatim IL-3 disable comment: // eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail"
    - "eslint.config.js is NOT modified (CMC-37 forbids config-file rule widening; the inline disable is the SOLE audit surface)"
    - "Exactly one console.warn callsite exists in persistence/migrate.ts (IL-3 single-callsite discipline preserved)"
    - "docs/messaging-style-guide.md §14 MSG-LC-1 row no longer says 'Phase 12 PROPOSES the new wording (below); Phase 13 owns the persistence/migrate.ts:178 byte change' -- replaced with 'Phase 12 LANDED the new wording (below); the byte change at persistence/migrate.ts:178 lives in the same Phase 12 PR (per CONTEXT.md D-CMC-14 / D-CMC-15)'"
    - "docs/messaging-style-guide.md §14.1 heading no longer says 'Proposed new wording (Phase 13 applies)' -- replaced with 'Wording (Phase 12 landed)'"
    - "docs/messaging-style-guide.md §14.1 no longer contains the sentence 'Phase 13's planner has FINAL discretion on the exact wording.' -- replaced with text affirming the wording landed in Phase 12 per D-CMC-14, structurally satisfies MSG-LC-1, with IL-3 inline disable preserved per D-CMC-16 / MSG-LC-2"
    - "The migrate.ts byte change and the style-guide §14.1 doc edit land in the SAME plan / PR (D-CMC-15 atomic alignment -- no transient window where the doc says 'discretion' while the bytes already exist)"
    - "tests/persistence/migrate.test.ts asserts the new wording byte-exact under simulated save failure AND asserts the IL-3 comment is preserved verbatim directly above the console.warn"
    - "No new attack surface: rewording preserves no-PII / no-secret-disclosure discipline; the only interpolated values (stateJsonPath, errMsg) were already present in the prior wording"
    - "npm run check (typecheck + ESLint + Prettier + tests) is green after every task (NFR-6)"
    - "RELOAD_HINT_PREFIX in shared/markers.ts is NOT touched (D-CMC-08; deletion is Phase 13's atomic three-file edit)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/persistence/migrate.ts"
      provides: "Rewritten IL-3 sanctioned console.warn at line ~178; IL-3 inline disable comment preserved verbatim above"
      contains: "Legacy marketplace migration could not be persisted to"
    - path: "docs/messaging-style-guide.md"
      provides: "§14 MSG-LC-1 row updated; §14.1 heading + closing paragraph rewritten to past-tense 'Phase 12 landed' framing"
      contains: "Phase 12 LANDED"
    - path: "tests/persistence/migrate.test.ts"
      provides: "Updated runtime-emission test (regex needles match new wording) + new source-byte assertions for CMC-36 wording and CMC-37 IL-3 comment preservation"
      contains: "Legacy marketplace migration could not be persisted"
  key_links:
    - from: "extensions/pi-claude-marketplace/persistence/migrate.ts (line 178 console.warn)"
      to: "docs/messaging-style-guide.md §14.1 wording block"
      via: "byte-exact match between code template literal and documented wording"
      pattern: "Legacy marketplace migration could not be persisted to"
    - from: "tests/persistence/migrate.test.ts"
      to: "extensions/pi-claude-marketplace/persistence/migrate.ts"
      via: "readFile + regex assertion on source bytes; t.mock.method(console, 'warn', ...) capture under simulated EIO failure"
      pattern: "Legacy marketplace migration could not be persisted"
---

<objective>
Land the §14.1 byte-exact wording at `persistence/migrate.ts:178` (CMC-36), preserve the IL-3 inline eslint-disable comment verbatim directly above the rewritten call (CMC-37), and -- in the SAME plan / PR -- update `docs/messaging-style-guide.md` §14 + §14.1 to remove the "Phase 13 PROPOSES / discretion" framing and replace with past-tense "Phase 12 landed" wording (D-CMC-15 atomic-alignment requirement).

Purpose: The single sanctioned `console.warn` is the only out-of-channel emission permitted by IL-3 / PRD §6.13. Its wording is the legacy-migration save-failure diagnostic; the §14.1 sentence form (terminal period, no compact-grammar tokens, no MANUAL RECOVERY REQUIRED prefix) is structurally required by MSG-LC-1. D-CMC-14 locks the §14.1 proposed wording as the binding text -- no tightening pass. D-CMC-15 requires the code byte change and the style-guide doc edit to land in the same PR so the doc and the code never diverge across a publication window.

Output: One byte-exact code rewrite (single template-literal body inside an existing try/catch), one three-edit doc update (§14 row tail + §14.1 heading + §14.1 closing paragraph), and a test that asserts the wording and the IL-3 comment preservation as source-byte invariants.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md
@.planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md
@docs/messaging-style-guide.md
@docs/prd/pi-claude-marketplace-prd.md
@extensions/pi-claude-marketplace/persistence/migrate.ts
@tests/persistence/migrate.test.ts
@tests/architecture/markers-snapshot.test.ts
@eslint.config.js
@CLAUDE.md

<binding_wording>
<!-- D-CMC-14: the §14.1 proposed wording becomes the binding text -- locked literally,
     no tightening, no bikeshedding. <path> = ${stateJsonPath}; <errMsg> = ${errMsg}. -->

Target console.warn template-literal body (byte-exact):

`Legacy marketplace migration could not be persisted to ${stateJsonPath}; the in-memory normalized state is being used and the on-disk state.json is unchanged. Cause: ${errMsg}.`

Target IL-3 inline disable comment (BYTE-IDENTICAL to current; do not reorder rules, do not change comma/whitespace, do not edit the rationale text):

// eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail
</binding_wording>

<current_state>
<!-- Verified at extensions/pi-claude-marketplace/persistence/migrate.ts:173-181 (from research §2.6
     and pattern mapper). The surrounding try/catch + errMsg extraction stays UNCHANGED; only the
     template-literal body inside console.warn(...) changes. -->

```typescript
try {
  await atomicWriteJson(stateJsonPath, normalizedState);
} catch (err) {
  const errMsg = errorMessage(err);
  // eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail
  console.warn(
    `pi-claude-marketplace: failed to persist migrated state to ${stateJsonPath} (${errMsg}); continuing with in-memory normalized state. Original state.json is unchanged.`,
  );
}
```
</current_state>

<style_guide_edit_targets>
<!-- Three specific docs/messaging-style-guide.md edits identified by research §2.7 and pattern mapper.
     Frontmatter (status_tokens, reasons, markers, pattern_classes) MUST stay untouched. -->

Edit A -- §14 MSG-LC-1 table row tail (around line 490):
  Current closing: "Phase 12 PROPOSES the new wording (below); Phase 13 owns the persistence/migrate.ts:178 byte change. (per CONTEXT.md D-24)"
  Target closing: "Phase 12 LANDED the new wording (below); the byte change at persistence/migrate.ts:178 lives in the same Phase 12 PR (per CONTEXT.md D-CMC-14 / D-CMC-15)."

Edit B -- §14.1 heading (around line 493):
  Current: "### 14.1 Proposed new wording (Phase 13 applies)"
  Target: "### 14.1 Wording (Phase 12 landed)"

Edit C -- §14.1 closing paragraph (around line 507):
  Current: "Phase 13's planner has FINAL discretion on the exact wording. The proposal above satisfies the structural constraints (sentence form, terminal period, no compact-grammar tokens, no severity prefix); Phase 13 may tighten or expand based on operator-feedback channels available at refactor time. Phase 12 ships the contract, not the bytes."
  Target: "The wording above is the binding text shipped at persistence/migrate.ts:178 in Phase 12 per D-CMC-14. It satisfies the structural constraints of MSG-LC-1 (sentence form, terminal period, no compact-grammar tokens, no severity prefix). The IL-3 inline eslint-disable-next-line comment is preserved verbatim above the call per D-CMC-16 / MSG-LC-2."

KEEP UNCHANGED:
- §14.1 "Today's wording" code block (the legacy form, kept for historical clarity).
- §14.1 "Proposed replacement under the guide's tone and punctuation rules" code block (the wording IS what landed; the framing in surrounding prose flips, but the byte content of the codeblock -- the target wording -- stays identical).
- §14.2 "The IL-3 boundary is single-call-site" subsection (reaffirms the eslint chokepoint; no edit needed).
- Frontmatter (`status_tokens:`, `reasons:`, `markers:`, `pattern_classes:`) -- NOT touched. The Plan 12-01 drift test reads it as-is.
</style_guide_edit_targets>

<test_strategy>
<!-- Existing tests/persistence/migrate.test.ts has an IL-3 console.warn capture test at lines 93-128
     that asserts the OLD wording via two regexes. Those regexes must be updated to match the NEW
     wording. THREE new source-byte tests are added per research §4 sketch to lock the wording and
     the IL-3 comment preservation as source-level invariants. -->

EXISTING test to UPDATE (in tests/persistence/migrate.test.ts, ~lines 93-128):
- Change `assert.match(warnArg, /pi-claude-marketplace: failed to persist migrated state/)` → `assert.match(warnArg, /Legacy marketplace migration could not be persisted/)`
- Change `assert.match(warnArg, /continuing with in-memory normalized state/)` → `assert.match(warnArg, /the in-memory normalized state is being used/)`
- Add `assert.match(warnArg, /Cause: /)` -- verifies the new "Cause:" prefix in the sentence
- KEEP `assert.ok(warnArg.includes(targetThatCannotBeWritten), ...)` -- `${stateJsonPath}` interpolation is preserved in the new wording

NEW tests to ADD (in same file, or a new tests/persistence/migrate-warn-wording.test.ts -- planner discretion; co-located is preferred for IL-3 cohesion):
- "CMC-36: persistence/migrate.ts warn body matches style guide §14.1 wording" -- readFile MIGRATE_PATH; assert.ok src.includes(byte-exact template literal); assert.ok !src.includes("failed to persist migrated state to")
- "CMC-37: IL-3 eslint-disable-next-line comment is preserved verbatim above the warn" -- regex match for the exact comment followed by console.warn(
- "CMC-37: exactly one sanctioned warn callsite in persistence/migrate.ts" -- count of `console.warn(` regex matches in source MUST equal 1
</test_strategy>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests/persistence/migrate.test.ts -- adjust existing IL-3 runtime-capture test regexes to match the new §14.1 wording, and add three source-byte assertions for CMC-36 wording + CMC-37 IL-3 comment preservation + CMC-37 single-callsite count</name>
  <files>tests/persistence/migrate.test.ts</files>
  <read_first>
    - tests/persistence/migrate.test.ts (full file -- locate the existing `t.mock.method(console, "warn", ...)` capture test around lines 93-128; understand the mock pattern, the temp-directory fixture, the `targetThatCannotBeWritten` path setup; the two `assert.match` regexes to update are within this test)
    - extensions/pi-claude-marketplace/persistence/migrate.ts (lines 160-185 -- current console.warn body to confirm the wording targets; verify the `errorMessage` helper is imported from ./errors.ts; verify the `atomicWriteJson` import path)
    - tests/architecture/markers-snapshot.test.ts (lines 1-30 -- the REPO_ROOT + fileURLToPath pattern for resolving MIGRATE_PATH absolute path; this scaffolding is copied for the new source-byte tests)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§4 -- full test-file sketch for migrate-warn-wording tests with exact `expected` string and exact regex for the IL-3 preservation check; §2.6 -- byte-exact rewrite contract; §R6 -- IL-3 comment drift risk and the regex shape that catches it)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md ("tests/persistence/migrate.test.ts (EXTEND)" section -- explicit before/after for the two `assert.match` regexes; the three new tests to add; the location decision (co-locate or split))
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-VALIDATION.md (CMC-36 + CMC-37 rows -- automated commands: source-grep + npm test invocations; also the manual verification row for D-CMC-15 atomic-PR check)
    - eslint.config.js (lines 75-140 -- verify the IL-3 rule shape: `no-restricted-syntax` for `console.warn` with an inline `eslint-disable-next-line ... -- IL-3:` exception; this is the rule the test asserts is satisfied by the comment line)
  </read_first>
  <behavior>
    - Test A (UPDATED -- existing IL-3 runtime capture): The mock-capture test now matches `/Legacy marketplace migration could not be persisted/`, `/the in-memory normalized state is being used/`, and `/Cause: /` -- and continues to assert the file path appears in the warn arg
    - Test B (NEW -- CMC-36 source wording): Reading `persistence/migrate.ts` source confirms the template literal contains the byte-exact §14.1 wording, AND the legacy "failed to persist migrated state to" is fully absent
    - Test C (NEW -- CMC-37 IL-3 comment preservation): Source matches the exact regex `/\/\/ eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail\n\s*console\.warn\(/` -- the comment is directly above the warn, byte-identical, with the rule list in the exact order `no-restricted-syntax, no-console`
    - Test D (NEW -- CMC-37 single-callsite count): `/console\.warn\(/g` in `persistence/migrate.ts` returns exactly 1 match
  </behavior>
  <action>
    Update `tests/persistence/migrate.test.ts` to (a) align the existing runtime-capture test with the new wording, and (b) add three new source-byte assertions that lock CMC-36 / CMC-37 invariants.

    Piece 1 -- UPDATE the existing test at ~lines 93-128 (`"IL-3 persistMigratedState swallows write failures and emits ONE console.warn"` -- or whatever the exact name is; locate by the `t.mock.method(console, "warn", ...)` capture pattern):
    - Replace `assert.match(warnArg, /pi-claude-marketplace: failed to persist migrated state/)` with `assert.match(warnArg, /Legacy marketplace migration could not be persisted/)`
    - Replace `assert.match(warnArg, /continuing with in-memory normalized state/)` with `assert.match(warnArg, /the in-memory normalized state is being used/)`
    - Add a new assertion immediately after: `assert.match(warnArg, /Cause: /);` -- verifies the new sentence's Cause prefix
    - KEEP the `assert.ok(warnArg.includes(targetThatCannotBeWritten), ...)` assertion -- `${stateJsonPath}` interpolation is preserved in the new wording
    - KEEP the `assert.equal(warnMock.mock.callCount(), 1, ...)` assertion -- single-callsite contract preserved

    Piece 2 -- ADD a constant `MIGRATE_PATH` near the top of the file (after existing imports), using the standard REPO_ROOT + fileURLToPath scaffolding from `tests/architecture/markers-snapshot.test.ts:13-16`:
    - `const REPO_ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../..");`
    - `const MIGRATE_PATH = path.join(REPO_ROOT, "extensions/pi-claude-marketplace/persistence/migrate.ts");`
    - Ensure `path`, `fileURLToPath`, `readFile` are imported (likely some already are; add `import { readFile } from "node:fs/promises"`, `import path from "node:path"`, `import { fileURLToPath } from "node:url"` if missing).

    Piece 3 -- ADD three new tests at the bottom of the file:

    Test 3a -- `"CMC-36: persistence/migrate.ts warn body matches style guide §14.1 wording"`:
    - Async; readFile MIGRATE_PATH utf8.
    - Define `expected` as the EXACT template-literal string (including backticks and `${...}` interpolation literals -- this is a source-byte assertion against the file contents, so the `expected` value is itself a JavaScript string containing the source text):
      `const expected = "\`Legacy marketplace migration could not be persisted to ${stateJsonPath}; the in-memory normalized state is being used and the on-disk state.json is unchanged. Cause: ${errMsg}.\`";`
      (Use string escapes appropriately; the goal is that `src.includes(expected)` is true when the migrate.ts file contains the byte-exact template literal.)
    - `assert.ok(src.includes(expected), 'Expected §14.1 wording at persistence/migrate.ts; not found.');`
    - `assert.ok(!src.includes("failed to persist migrated state to"), "Legacy wording 'failed to persist migrated state to' must be fully replaced (CMC-36)");`

    Test 3b -- `"CMC-37: IL-3 eslint-disable-next-line comment is preserved verbatim above the warn"`:
    - Async; readFile MIGRATE_PATH utf8.
    - Define `expectedPattern` as the regex literal: `/\/\/ eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail\n\s*console\.warn\(/`
    - `assert.match(src, expectedPattern, "IL-3 inline disable must appear directly above the warn (CMC-37 / D-CMC-16)");`

    Test 3c -- `"CMC-37: exactly one sanctioned warn callsite in persistence/migrate.ts"`:
    - Async; readFile MIGRATE_PATH utf8.
    - `const matches = src.match(/console\.warn\(/g) ?? [];`
    - `assert.equal(matches.length, 1, "persistence/migrate.ts must have exactly one sanctioned warn (IL-3)");`

    Planner discretion: Co-locate the three new tests in `tests/persistence/migrate.test.ts` (RECOMMENDED -- keeps IL-3 coverage together) OR split into `tests/persistence/migrate-warn-wording.test.ts` (research §4 suggested name; narrower scope). Co-location is preferred for review cohesion; the existing IL-3 mock test is already in `tests/persistence/migrate.test.ts`, and adding the wording / preservation / count guards next to it surfaces the full IL-3 contract in one file.

    NOTE: This task is RED first for the three new tests -- they will FAIL until Task 2 rewrites the warn body. The existing runtime-capture test (after the regex updates) will also FAIL until Task 2 lands. That is intentional. Task 2 closes the cycle.
  </action>
  <verify>
    <automated>grep -c "Legacy marketplace migration could not be persisted" tests/persistence/migrate.test.ts</automated>
    <automated>grep -c "the in-memory normalized state is being used" tests/persistence/migrate.test.ts</automated>
    <automated>grep -c "Cause: " tests/persistence/migrate.test.ts</automated>
    <automated>grep -c "CMC-36\|CMC-37" tests/persistence/migrate.test.ts</automated>
    <automated>! grep -F "pi-claude-marketplace: failed to persist migrated state" tests/persistence/migrate.test.ts</automated>
    <automated>! grep -F "continuing with in-memory normalized state" tests/persistence/migrate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "Legacy marketplace migration could not be persisted" tests/persistence/migrate.test.ts` returns at least 2 (one in the runtime-capture regex, one in the new source-byte test's `expected` string)
    - Source: `grep -F "the in-memory normalized state is being used" tests/persistence/migrate.test.ts` returns at least 1 match (runtime regex updated)
    - Source: `grep -F "Cause: " tests/persistence/migrate.test.ts` returns at least 1 match (new assertion added)
    - Source: `grep -E "CMC-36|CMC-37" tests/persistence/migrate.test.ts` returns at least 3 matches (one per new test name)
    - Source (old regexes removed): `grep -F "pi-claude-marketplace: failed to persist migrated state" tests/persistence/migrate.test.ts` returns NO match
    - Source (old regexes removed): `grep -F "continuing with in-memory normalized state" tests/persistence/migrate.test.ts` returns NO match
    - Source (IL-3 preservation regex literal present): `grep -F "no-restricted-syntax, no-console -- IL-3: load-time migrate save fail" tests/persistence/migrate.test.ts` returns at least 1 match (inside the new test's regex literal)
    - Source (single-callsite count test present): `grep -F "must have exactly one sanctioned warn" tests/persistence/migrate.test.ts` returns at least 1 match
    - Behavior (intentional RED until Task 2): `npm test -- tests/persistence/migrate.test.ts` may FAIL at this point -- the migrate.ts source has not yet been updated. Task 2 turns it GREEN.
  </acceptance_criteria>
  <done>Test file is updated: existing runtime-capture regexes match the new wording; three new source-byte tests assert CMC-36 wording, CMC-37 IL-3 comment preservation, and CMC-37 single-callsite count. Tests are expected to fail until Task 2 lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the console.warn body at persistence/migrate.ts line 178 to the §14.1 byte-exact wording (D-CMC-14), preserve the IL-3 inline disable comment verbatim above (D-CMC-16), AND in the same task atomically update docs/messaging-style-guide.md §14 row tail + §14.1 heading + §14.1 closing paragraph per D-CMC-15</name>
  <files>extensions/pi-claude-marketplace/persistence/migrate.ts, docs/messaging-style-guide.md</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/migrate.ts (lines 160-185 -- full context for the line-178 console.warn; verify the `try { atomicWriteJson(stateJsonPath, normalizedState); } catch (err) { const errMsg = errorMessage(err); ... }` structure; the `stateJsonPath` and `errMsg` template variables are preserved in the new wording)
    - docs/messaging-style-guide.md (lines 484-512 -- full §14 + §14.1 + §14.2 region; identify the three edit sites by exact substring; the "Today's wording" and "Proposed replacement" code blocks stay intact)
    - docs/messaging-style-guide.md (lines 1-60 -- frontmatter; CONFIRM untouched after edit, since Plan 12-01's drift test reads it; the §14 edits are below the frontmatter and unrelated)
    - eslint.config.js (lines 75-140 -- the IL-3 rule shape; this task does NOT modify eslint.config.js but the test in Task 1 verifies the comment continues to satisfy the rule)
    - tests/persistence/migrate.test.ts (the file Task 1 updated -- verify the assertions the new wording must satisfy)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-RESEARCH.md (§2.6 -- byte-exact rewrite contract with the template literal target; §2.7 -- style guide §14.1 doc edit with each target sentence; §R5 -- atomic-PR requirement and the risk if the doc edit is forgotten or split into a later PR; §R6 -- IL-3 comment drift risk)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-PATTERNS.md ("persistence/migrate.ts:173-181 (REWRITE)", "docs/messaging-style-guide.md §14.1 (DOC EDIT)" sections -- explicit before/after for both files including the deltas-vs-analog table for the doc edit)
    - .planning/phases/12-messaging-foundations-renderer-primitives/12-CONTEXT.md (D-CMC-14, D-CMC-15, D-CMC-16 -- the three binding decisions this task implements)
  </read_first>
  <behavior>
    - After this task, `npm test -- tests/persistence/migrate.test.ts` exits 0 -- all assertions from Task 1 (the updated runtime-capture + the three new source-byte tests) are GREEN.
    - The source at `persistence/migrate.ts` contains the byte-exact §14.1 template literal as the sole `console.warn` body in the file.
    - The IL-3 inline disable comment is byte-identical to the prior file (rule list order, comma, whitespace, rationale text all unchanged).
    - `eslint.config.js` is NOT modified -- `git diff --name-only HEAD -- eslint.config.js` is empty.
    - `docs/messaging-style-guide.md` frontmatter is NOT modified -- Plan 12-01's drift test still passes.
    - The doc and code align: a reviewer reading §14.1 sees "Phase 12 landed"; reading `persistence/migrate.ts:178` sees the matching bytes; there is no transient window where the doc says "discretion" while the bytes already exist (D-CMC-15).
  </behavior>
  <action>
    This task is the D-CMC-15 atomic edit. Both files MUST be modified in the same task / commit. Splitting them into separate tasks risks the doc-vs-code window R5 explicitly warns against.

    Piece 1 -- rewrite the `console.warn` body in `extensions/pi-claude-marketplace/persistence/migrate.ts` (around line 178):
    - Locate the `try { await atomicWriteJson(stateJsonPath, normalizedState); } catch (err) { const errMsg = errorMessage(err); ... console.warn(...) ... }` block.
    - The line directly above the `console.warn(` MUST be byte-identical to the current `// eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail` -- do NOT reorder the rule names, do NOT change comma/whitespace, do NOT alter the `-- IL-3: load-time migrate save fail` rationale text.
    - Replace ONLY the template-literal body inside `console.warn(...)`. The new body is byte-exact (use the literal characters; preserve the `${stateJsonPath}` and `${errMsg}` interpolations exactly):
      `` `Legacy marketplace migration could not be persisted to ${stateJsonPath}; the in-memory normalized state is being used and the on-disk state.json is unchanged. Cause: ${errMsg}.` ``
    - The surrounding try/catch + `errMsg = errorMessage(err)` + closing `);` all stay UNCHANGED.
    - No other change to this file. No other `console.warn` site is added (IL-3 single-callsite contract).

    Piece 2 -- atomically update `docs/messaging-style-guide.md` in the SAME task (D-CMC-15):

    Edit A -- §14 MSG-LC-1 row tail (around line 490):
    - Locate the table row whose ID column is `**MSG-LC-1**`.
    - The row's last sentence currently ends: "Phase 12 PROPOSES the new wording (below); Phase 13 owns the persistence/migrate.ts:178 byte change. (per CONTEXT.md D-24)"
    - Replace that sentence with: "Phase 12 LANDED the new wording (below); the byte change at persistence/migrate.ts:178 lives in the same Phase 12 PR (per CONTEXT.md D-CMC-14 / D-CMC-15)."
    - Preserve the rest of the row (the structural-shape paragraph beginning "The single sanctioned console.warn... MUST stay SENTENCE FORM..." stays intact).

    Edit B -- §14.1 heading (around line 493):
    - Current: `### 14.1 Proposed new wording (Phase 13 applies)`
    - Target: `### 14.1 Wording (Phase 12 landed)`

    Edit C -- §14.1 closing paragraph (around line 507):
    - Current paragraph: "Phase 13's planner has FINAL discretion on the exact wording. The proposal above satisfies the structural constraints (sentence form, terminal period, no compact-grammar tokens, no severity prefix); Phase 13 may tighten or expand based on operator-feedback channels available at refactor time. Phase 12 ships the contract, not the bytes."
    - Replace ENTIRE paragraph with: "The wording above is the binding text shipped at persistence/migrate.ts:178 in Phase 12 per D-CMC-14. It satisfies the structural constraints of MSG-LC-1 (sentence form, terminal period, no compact-grammar tokens, no severity prefix). The IL-3 inline eslint-disable-next-line comment is preserved verbatim above the call per D-CMC-16 / MSG-LC-2."

    Piece 3 -- KEEP UNCHANGED in `docs/messaging-style-guide.md`:
    - Frontmatter (lines 1-60) -- `status_tokens:`, `reasons:`, `markers:`, `pattern_classes:`. Plan 12-01's drift test reads these as-is.
    - §14.1 "Today's wording" code block (the legacy form). Surrounding prose framing can shift from "Today's" to "Phase 12 superseded this on-disk" if needed for readability -- but the code block bytes inside the backticks stay the legacy literal as historical record.
    - §14.1 "Proposed replacement under the guide's tone and punctuation rules" code block. The bytes inside the backticks are the wording that landed -- they ARE the byte-exact §14.1 form. Framing prose around the block can shift from "Proposed replacement" to "Shipped at persistence/migrate.ts:178 in Phase 12" or similar.
    - §14.2 "The IL-3 boundary is single-call-site" subsection. No edit needed.
    - §15 ES-5 Replacement Table. No edit needed (Phase 13 owns the atomic three-file ES-5 edit).

    Piece 4 -- DO NOT touch:
    - `eslint.config.js` (CMC-37 -- no config widening).
    - `extensions/pi-claude-marketplace/shared/markers.ts` (D-CMC-08 retention -- Plan 12-02 territory, but also out-of-scope for this plan).
    - Any other file under `extensions/pi-claude-marketplace/` (Plan 12-01 owns the constants/drift; Plan 12-02 owns the reload-hint composer; Plan 12-04 owns the notify wrapper docs comment).
  </action>
  <verify>
    <automated>grep -F "Legacy marketplace migration could not be persisted to \${stateJsonPath}" extensions/pi-claude-marketplace/persistence/migrate.ts</automated>
    <automated>grep -F "the in-memory normalized state is being used and the on-disk state.json is unchanged" extensions/pi-claude-marketplace/persistence/migrate.ts</automated>
    <automated>grep -F "Cause: \${errMsg}." extensions/pi-claude-marketplace/persistence/migrate.ts</automated>
    <automated>! grep -F "pi-claude-marketplace: failed to persist migrated state to" extensions/pi-claude-marketplace/persistence/migrate.ts</automated>
    <automated>awk '/console\.warn/{print prev} {prev=$0}' extensions/pi-claude-marketplace/persistence/migrate.ts | grep -F "IL-3: load-time migrate save fail"</automated>
    <automated>grep -c "console.warn(" extensions/pi-claude-marketplace/persistence/migrate.ts</automated>
    <automated>grep -F "Phase 12 LANDED the new wording" docs/messaging-style-guide.md</automated>
    <automated>grep -F "### 14.1 Wording (Phase 12 landed)" docs/messaging-style-guide.md</automated>
    <automated>grep -F "The wording above is the binding text shipped at persistence/migrate.ts:178 in Phase 12" docs/messaging-style-guide.md</automated>
    <automated>! grep -F "Phase 12 PROPOSES the new wording" docs/messaging-style-guide.md</automated>
    <automated>! grep -F "Phase 13 owns the persistence/migrate.ts:178 byte change" docs/messaging-style-guide.md</automated>
    <automated>! grep -F "Phase 13's planner has FINAL discretion" docs/messaging-style-guide.md</automated>
    <automated>! grep -F "### 14.1 Proposed new wording (Phase 13 applies)" docs/messaging-style-guide.md</automated>
    <automated>npm test -- tests/persistence/migrate.test.ts</automated>
    <automated>npm run check</automated>
    <automated>test -z "$(git diff --name-only HEAD -- eslint.config.js)"</automated>
  </verify>
  <acceptance_criteria>
    - Source (migrate.ts byte-exact): `grep -F "Legacy marketplace migration could not be persisted to \${stateJsonPath}; the in-memory normalized state is being used and the on-disk state.json is unchanged. Cause: \${errMsg}." extensions/pi-claude-marketplace/persistence/migrate.ts` returns at least 1 match (the full byte-exact template literal)
    - Source (migrate.ts legacy wording removed): `grep -F "pi-claude-marketplace: failed to persist migrated state to" extensions/pi-claude-marketplace/persistence/migrate.ts` returns NO match
    - Source (IL-3 comment preserved verbatim directly above warn): `awk '/console\.warn/{print prev} {prev=$0}' extensions/pi-claude-marketplace/persistence/migrate.ts | grep -F "// eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail"` returns a match
    - Source (single-callsite): `grep -c "console.warn(" extensions/pi-claude-marketplace/persistence/migrate.ts` returns exactly 1
    - Source (style guide §14 row updated): `grep -F "Phase 12 LANDED the new wording" docs/messaging-style-guide.md` returns at least 1 match
    - Source (style guide §14.1 heading updated): `grep -F "### 14.1 Wording (Phase 12 landed)" docs/messaging-style-guide.md` returns at least 1 match
    - Source (style guide §14.1 closing paragraph updated): `grep -F "The wording above is the binding text shipped at persistence/migrate.ts:178 in Phase 12 per D-CMC-14" docs/messaging-style-guide.md` returns at least 1 match
    - Source (old framing removed): `grep -F "Phase 12 PROPOSES the new wording" docs/messaging-style-guide.md` returns NO match
    - Source (old framing removed): `grep -F "Phase 13 owns the persistence/migrate.ts:178 byte change" docs/messaging-style-guide.md` returns NO match
    - Source (old framing removed): `grep -F "Phase 13's planner has FINAL discretion" docs/messaging-style-guide.md` returns NO match
    - Source (old heading removed): `grep -F "### 14.1 Proposed new wording (Phase 13 applies)" docs/messaging-style-guide.md` returns NO match
    - Source (frontmatter untouched -- Plan 12-01 drift test still works): `head -60 docs/messaging-style-guide.md | grep -c "  - "` returns the same count as before this task (14 status_tokens + 23 reasons + 2 markers + 12 pattern_classes = 51 bullets)
    - Source (eslint.config.js untouched): `git diff --name-only HEAD -- eslint.config.js` is empty
    - Source (D-CMC-15 atomic-PR): `git diff --name-only HEAD` includes BOTH `extensions/pi-claude-marketplace/persistence/migrate.ts` AND `docs/messaging-style-guide.md` (the two edits land in the same plan / PR)
    - Behavior: `npm test -- tests/persistence/migrate.test.ts` exits 0 (Task 1's RED tests turn GREEN -- runtime-capture matches new wording; source-byte assertions confirm CMC-36 wording, CMC-37 comment preservation, CMC-37 single-callsite count)
    - Behavior (non-regression): `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0 (D-CMC-08 retention not regressed)
    - Behavior (non-regression): `npm test -- tests/architecture/grammar-frontmatter.test.ts` exits 0 (Plan 12-01 drift test still green; frontmatter untouched)
    - CLI: `npm run check` exits 0 (full structural gate at end of plan)
  </acceptance_criteria>
  <done>persistence/migrate.ts:178 console.warn body is byte-exact the §14.1 wording with the IL-3 comment preserved verbatim above; docs/messaging-style-guide.md §14 row tail, §14.1 heading, and §14.1 closing paragraph are updated to past-tense "Phase 12 landed" framing in the SAME plan / PR; eslint.config.js is untouched; npm run check is green; markers-snapshot test and Plan 12-01's drift test both stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrate.ts → console.warn (Pi log / stderr) | The rewritten message interpolates two values from the load-time migrate path: `stateJsonPath` (an absolute path computed from the scope root, already trusted as the destination of the atomic write) and `errMsg` (the error's `.message`, sanitized via `errorMessage()` to handle non-Error throws). No new external input is interpolated. |
| docs/messaging-style-guide.md → reviewers | The doc edit is text-framing only; no semantic change to the wording proposal block. The frontmatter (binding contract) is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.03-01 | Information Disclosure | console.warn body | accept | The new wording interpolates ONLY `stateJsonPath` (already user-known: it's the file the migration was trying to write) and `errMsg` (the error's `.message`, sanitized by `errorMessage()`); no new secret, PII, or environment data is surfaced; the prior wording disclosed the same two values |
| T-12.03-02 | Tampering | IL-3 inline disable comment drift | mitigate | Task 1 adds a regex source-byte test (`/\/\/ eslint-disable-next-line no-restricted-syntax, no-console -- IL-3: load-time migrate save fail\n\s*console\.warn\(/`) that asserts the comment is byte-identical and directly above the warn; R6 mitigation |
| T-12.03-03 | Tampering | eslint.config.js widening | mitigate | Acceptance criterion `git diff --name-only HEAD -- eslint.config.js` is empty enforces CMC-37 at the diff level; reviewer can spot a config-file edit in the PR diff |
| T-12.03-04 | Tampering | additional console.warn callsite | mitigate | Task 1 adds a count-assertion test (`/console\.warn\(/g` returns exactly 1 match); also enforced by ESLint's `no-restricted-syntax` + `no-console` rules across the rest of the file tree |
| T-12.03-05 | Repudiation | doc-code divergence (D-CMC-15 risk R5) | mitigate | Task 2 makes the migrate.ts edit + style-guide edit ATOMIC -- both land in the same task / PR; acceptance criterion `git diff --name-only HEAD` must include both files; no transient publication window |

**Summary:** No new attack surface; the rewording preserves the prior no-PII / no-secret-disclosure discipline (same two interpolations). Threats are internal: IL-3 comment drift (R6) and eslint-config widening (CMC-37 violation) are blocked by acceptance criteria + the new source-byte test. The D-CMC-15 atomic-PR requirement is enforced by listing both files in the task's `<files>` and asserting both appear in the diff. ASVS L1 not applicable (no network, no input handling, no auth, no crypto).
</threat_model>

<verification>
- `npm run check` exits 0 at end of plan (full structural gate).
- `npm test -- tests/persistence/migrate.test.ts` exits 0 -- runtime-capture + three source-byte tests all GREEN.
- `npm test -- tests/architecture/markers-snapshot.test.ts` exits 0 -- D-CMC-08 retention not regressed.
- `npm test -- tests/architecture/grammar-frontmatter.test.ts` exits 0 -- Plan 12-01 drift test still green; frontmatter untouched.
- `grep "console.warn(" extensions/pi-claude-marketplace/persistence/migrate.ts | wc -l` returns 1 -- IL-3 single-callsite.
- `awk '/console\.warn/{print prev} {prev=$0}' extensions/pi-claude-marketplace/persistence/migrate.ts | grep -F "IL-3: load-time migrate save fail"` returns a match -- IL-3 comment preserved verbatim directly above.
- `git diff --name-only HEAD` includes BOTH `persistence/migrate.ts` AND `docs/messaging-style-guide.md` (D-CMC-15 atomic-PR).
- `git diff --name-only HEAD -- eslint.config.js` is empty (CMC-37 / IL-3 discipline preserved).
- `git diff --name-only HEAD -- tests/architecture/markers-snapshot.test.ts` is empty (D-CMC-08 / out-of-scope file untouched).
- Manual verification (D-CMC-15 atomic-PR check, per 12-VALIDATION.md): At PR review, confirm both `persistence/migrate.ts` and `docs/messaging-style-guide.md` appear in the diff and that §14.1 no longer says "Phase 13 PROPOSES" or "Phase 13's planner has FINAL discretion".
</verification>

<success_criteria>
- persistence/migrate.ts line ~178 console.warn body is byte-exact the §14.1 binding wording with the IL-3 inline disable comment preserved verbatim above.
- docs/messaging-style-guide.md §14 MSG-LC-1 row tail, §14.1 heading, and §14.1 closing paragraph are updated to past-tense "Phase 12 landed" framing.
- docs/messaging-style-guide.md frontmatter is untouched (Plan 12-01's drift test still passes).
- tests/persistence/migrate.test.ts has updated runtime-capture regexes + three new source-byte tests passing.
- eslint.config.js is untouched (CMC-37 / IL-3 discipline preserved at config level).
- Exactly one `console.warn(` callsite exists in persistence/migrate.ts.
- D-CMC-15 atomic-PR is enforced: both files appear in the same diff.
- npm run check is green at end of plan.
</success_criteria>

<output>
Create `.planning/phases/12-messaging-foundations-renderer-primitives/12-03-SUMMARY.md` when done.
</output>
