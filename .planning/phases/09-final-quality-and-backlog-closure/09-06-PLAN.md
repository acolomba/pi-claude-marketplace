---
phase: 09-final-quality-and-backlog-closure
plan: 06
type: execute
wave: 6
depends_on: ["09-05"]
autonomous: true
requirements: [CLOSE-01, CLOSE-02]
files_modified:
  - .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-09-10: `npm run check` and `npm run test:coverage:direct:all` were both run on the final tree and both exited 0, and their real output is recorded verbatim. Neither is reported from an earlier run."
    - "D-09-16: the two human-checkable pin claims were read and argued rather than recorded as unverifiable, and the argument — including the residual each one carries — is written into the ledger."
    - "D-09-17: the `direct-coverage` CI job carries one explicit `unresolved` row that names what is unproven and what would close it, and it is not folded into a passing claim."
    - "D-09-14: `09-CLOSURE-LEDGER.md` exists and carries a row per closed item — the eight requirement IDs, the seven backlog items plus the one todo, the five window entries this phase touched, and the three caveats — every disposition drawn from the one closed five-word set."
    - "D-09-18: no ledger row describes a coverage reading as evidence that an assertion is strong."
    - "Every disposition word in the ledger matches the word written into `.planning/BACKLOG.md` for the same item, so the two records cannot disagree."
  artifacts:
    - .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md
  key_links:
    - "Each ledger row's Evidence cell carries a command run this cycle with its verbatim output, or a file-and-line citation a reader can follow — a row that says an item is closed and cannot show it is not a record."
    - "The ledger's disposition vocabulary is the same closed set `.planning/BACKLOG.md` now uses, which is what keeps the backlog and the ledger from drifting apart."
    - "The final-tree measurement in this plan is what `CLOSE-01` claims; every change that lands after it is a planning document no test reads."
  prohibitions:
    - "No coverage reading described as assertion strength."
    - "No sixth disposition word, and no free-prose status."
    - "No caveat folded into a passing claim."
    - "No measurement quoted from an earlier phase or an earlier plan in this phase where D-09-10 requires a final-tree run."
    - "No bare `pre-commit run` — every invocation carries the `SKIP=trufflehog` prefix."
---

<objective>
Measure the complete quality suite and the whole-tree coverage sweep on the final tree, verify the
two human-checkable pin claims by reading them, and write the phase's single closure record.

Purpose: this is what `CLOSE-01` and `CLOSE-02` actually deliver. Everything before it changed the
tree; this plan states what the tree now is, in one vocabulary, with an evidence column, and names
the one thing that genuinely cannot be settled from inside this repository.
Output: two verbatim final-tree measurements, two argued pin claims with their residuals, and
`09-CLOSURE-LEDGER.md`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-final-quality-and-backlog-closure/09-CONTEXT.md
@.planning/phases/09-final-quality-and-backlog-closure/09-RESEARCH.md
@.planning/phases/09-final-quality-and-backlog-closure/09-PATTERNS.md
@.planning/phases/09-final-quality-and-backlog-closure/09-02-SUMMARY.md
@.planning/phases/09-final-quality-and-backlog-closure/09-03-SUMMARY.md
@.planning/phases/09-final-quality-and-backlog-closure/09-04-SUMMARY.md
@.planning/phases/09-final-quality-and-backlog-closure/09-05-SUMMARY.md
@.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md
</context>

<edge_coverage>
The fourth of the deterministic edge probe's four `unresolved` items lands on this plan. It came
back `unclassified — review manually`, so it is read here rather than dismissed.

| Requirement | Category | Probe | Applicable here? | Where it is covered |
| --- | --- | --- | --- | --- |
| CLOSE-02 | unclassified | "review manually" | **Yes.** Read for a documentary requirement, the edge that matters is the boundary of the closed disposition set: a row whose verdict falls outside the five permitted words, or a row whose verdict disagrees with the same item's verdict in `.planning/BACKLOG.md`. Either one lets the two records drift, which is the failure D-09-14 names. | Task 3's acceptance counts the `Disposition` column's values with a pattern that matches only the five permitted words and requires the total to equal the row count, so an out-of-set word and a missing row both fail. A separate criterion requires each word to be character-identical to the one plan 09-05 wrote for the same item. |

The other three probe items — adjacency, empty and ordering against the coverage pin — are covered
in `09-02-PLAN.md`, with a second `empty` instance in `09-04-PLAN.md`. None of the four was
dismissed as inapplicable.
</edge_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Measure the complete suite and the whole-tree sweep on the final tree</name>

  <precondition>
    Plan 09-05 is committed and `git status --porcelain` prints nothing. Every code and gate-data
    change this phase makes has landed; only planning documents change after this point, and no test
    in the tree reads the ones that remain.
  </precondition>

  <read_first>
    - `package.json` `scripts.check` and the four direct-coverage arms.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-CONTEXT.md` D-09-10 — what "the
      complete project quality suite" means and the rule that neither run is reported from an earlier
      one.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-02-SUMMARY.md` and
      `09-03-SUMMARY.md` — the two earlier measurements this one supersedes as the final-tree claim.
  </read_first>

  <files>none — this task measures and records; it changes no file</files>

  <action>
    Run both commands from the repository root on the committed final tree, in this order, and let
    each finish.

    1. `npm run check` — the nine-command chain. Record the exit status, the unit suite's `# tests` /
       `# pass` / `# fail` / `# skipped` block verbatim, the integration suite's equivalent block
       verbatim, and the wall clock.
    2. `npm run test:coverage:direct:all` — the strict whole-tree arm. Record the exit status, the
       pair count, and the final summary line verbatim.

    Record the commit SHA both were taken at. Record the date. State in the SUMMARY that this is the
    final-tree run and that the only changes landing after it are planning documents no test reads —
    name them: the closure ledger created by Task 3, and whatever the phase-close step writes to
    `.planning/STATE.md` and `.planning/ROADMAP.md`.

    Do not reuse either figure from plan 09-02 or plan 09-03. Those were taken on earlier trees and
    are correct statements about those trees; this one is the statement `CLOSE-01` makes.

    If either command exits non-zero, stop and report. Do not write a ledger row claiming a green
    suite.

    This task produces no commit. Its deliverable is the recorded measurement.
  </action>

  <acceptance_criteria>
    - `npm run check` exits 0, and its unit and integration summary blocks are quoted verbatim in the
      SUMMARY, each showing `# fail 0`.
    - `npm run test:coverage:direct:all` exits 0, and its pair count and final line are quoted
      verbatim.
    - The SUMMARY names the commit SHA and the date both runs were taken at.
    - `git status --porcelain` prints nothing before and after both runs.
    - The SUMMARY states explicitly that neither figure was carried over from plan 09-02 or 09-03.
  </acceptance_criteria>

  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit from any sub-command in the chain, or a `# fail 1` or greater summary line in either test suite</fails_when>
    <automated>npm run test:coverage:direct:all</automated>
    <fails_when>non-zero exit, or a `Direct coverage failed` line naming any module that carries no pin row</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>any output — the measurement must be taken over a clean committed tree</fails_when>
  </verify>

  <done>
    Both final-tree measurements exited 0 and their real output is recorded verbatim with the commit
    SHA they were taken at.
  </done>
</task>

<task type="auto">
  <name>Task 2: Read and argue the two human-checkable pin claims (D-09-16)</name>

  <read_first>
    - `scripts/test-coverage-direct.pin.json` — both rows and all four `reasons` entries verbatim.
      These are the claims under examination.
    - `.planning/phases/08-direct-coverage/08-04-SUMMARY.md` — deliverable D5 and the standing caveat:
      the gate can prove the reading and the negative harness can prove the comparison, but only a
      reader can judge whether a `reasons` entry is true, and that difference is the difference
      between a pin and an allow-list.
    - `extensions/pi-claude-marketplace/bridges/commands/discover.ts` — `nameCommandInDir` and the
      narrowing arm around its call site.
    - `extensions/pi-claude-marketplace/shared/errors-bridges.ts` — the `CommandNameError` constructor.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts` — the manifest-entry
      re-check and the hooks re-parse guard.
    - `extensions/pi-claude-marketplace/domain/manifest.ts`,
      `extensions/pi-claude-marketplace/domain/manifest-cache.ts`,
      `extensions/pi-claude-marketplace/domain/components/plugin.ts`,
      `extensions/pi-claude-marketplace/domain/components/hooks.ts`, and
      `extensions/pi-claude-marketplace/domain/hooks-resolution.ts` — the two validation paths the
      re-checks sit behind.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-RESEARCH.md` §6 — the reading already
      done, including the two places it found the argument stronger than the pin states and the one
      residual the pin omits.
  </read_first>

  <files>none — this task reads and records; Task 3 writes the argument into the ledger</files>

  <action>
    Read each claim against the code and state whether it holds. Do not accept the research document's
    conclusion without tracing it yourself — the whole point of D-09-16 is that a reason is a claim
    only a reader can judge, and inheriting the judgement reproduces exactly the defect the caveat
    names.

    **Claim 1 — the `CommandNameError` narrowing arm in `bridges/commands/discover.ts`.** Establish
    whether any input can make the thrown value fail the `instanceof` check. Trace: is
    `nameCommandInDir` reachable from anywhere outside its own module; can its body throw anything
    other than the one error class it constructs; can that constructor itself throw before the
    instance exists. Record the answer and the reason the arm cannot simply be deleted.

    **Claim 2 — the manifest-entry re-check in `orchestrators/plugin/install-outcome.ts`.** Trace the
    value under re-validation back to the parse that produced it and establish whether the schema that
    accepted it is the same schema the re-check applies. Then record the residual the pin's text does
    not name: the manifest cache returns hits by reference and documents its results as read-only, so
    a caller that violated that contract could make the arm fire. That is a program defect rather than
    an input, so the reachability claim stands — but naming it makes the pin a better record.

    **Claim 3 — the hooks re-parse guard in the same file.** Establish whether the parse option that
    differs between the two calls can change the ok-or-not verdict for identical bytes, by reading
    where each failure return sits relative to where that option is consulted. Then establish whether
    the two calls read the same file at two different times. Record the conclusion honestly: this arm
    is not input-reachable, but it is reachable by a filesystem mutation between the two reads, which
    is a real window and is exactly what the guard is for. Do not soften that into "unreachable".

    For each claim record: the file-and-line citations you traced, the conclusion, and the residual if
    any. Task 3 writes these into the ledger.

    This task produces no commit. Its deliverable is the recorded argument.
  </action>

  <acceptance_criteria>
    - The SUMMARY carries one argument per claim, each citing the specific files and line ranges
      traced, each ending in a stated conclusion.
    - Claim 1's argument establishes module-privacy and the single throw path, and states why deleting
      the arm is not available without a restructure.
    - Claim 2's argument names the residual the pin omits — the cache's by-reference read-only
      contract — and states why the reachability claim still stands.
    - Claim 3's argument states that the arm is reachable by a filesystem mutation between the two
      reads, and does not describe it as unreachable.
    - No argument concludes with a claim that a coverage reading proves an assertion is strong.
    - Every argument is written from a trace performed in this task, and the SUMMARY says so.
  </acceptance_criteria>

  <verify>
    <automated>grep -rn "nameCommandInDir" extensions/</automated>
    <fails_when>output naming any file other than `extensions/pi-claude-marketplace/bridges/commands/discover.ts` — the module-privacy premise of claim 1 would be false</fails_when>
    <automated>grep -c 'PLUGIN_ENTRY_VALIDATOR' extensions/pi-claude-marketplace/orchestrators/plugin/install-outcome.ts extensions/pi-claude-marketplace/domain/components/plugin.ts</automated>
    <fails_when>either file reporting `0` — the identical-schema premise of claim 2 could not be traced</fails_when>
    <automated>node scripts/test-coverage-direct.mjs extensions/pi-claude-marketplace/bridges/commands/discover.ts</automated>
    <fails_when>non-zero exit, or a reading other than the one the pin row states</fails_when>
  </verify>

  <done>
    All three pin `reasons` claims were traced against the source this cycle, each carries a stated
    conclusion with its citations, and the two residuals the pin's own text does not name are written
    down.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write 09-CLOSURE-LEDGER.md</name>

  <read_first>
    - `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` — the nearest artifact in the
      tree. Copy its preamble discipline (why the evidence column exists), its table shape, and its
      split-disposition prose sections. Do NOT copy its status vocabulary: its column carries free
      prose, and this ledger's is a closed five-word set.
    - The SUMMARY files from plans 09-01 through 09-05, and this plan's Task 1 and Task 2 records.
    - `.planning/REQUIREMENTS.md` §"Evidence and History" — the register voice for the evidence-only
      rows.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-CONTEXT.md` D-09-14 through D-09-18.
  </read_first>

  <files>.planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md</files>

  <action>
    Create the ledger at
    `.planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md`.

    **Preamble.** State what the document is: every item this phase closed, the disposition it carries
    now, and a command run this cycle or a file-and-line citation that establishes the claim. State
    why the evidence column exists — a row that says an item is closed and cannot show it is not a
    record. State the closed disposition set and that nothing outside it appears. State the
    reachability boundary once, in its own sentence: a complete coverage reading is evidence that
    every arm ran under the owner test, and it says nothing about whether that test asserted anything
    worth asserting — so no row in this document uses a coverage reading to claim an assertion is
    strong.

    **Table.** Five columns: `Item`, `Kind`, `Disposition`, `Carried by`, `Evidence`. `Kind` is one of
    `requirement`, `backlog`, `todo`, `window`, `caveat`. `Disposition` is one of `implemented`,
    `evidence-only`, `superseded`, `deferred`, `unresolved`, and nothing else.

    Rows, in this order:

    - **Eight `requirement` rows** — `GGAT-01`, `GGAT-03`, `GGAT-04`, `RCOV-01`, `RCOV-02`, `RCOV-03`,
      `CLOSE-01`, `CLOSE-02`, each `implemented`. `Carried by` names which of the two seal changes
      moved it. `Evidence` carries the `scope-impact --check` output and the control-suite pass count
      recorded in `09-03-SUMMARY.md`. For `GGAT-01`, `GGAT-03` and `GGAT-04`, also cite the phase-7
      verification that passed them, so a reader can see they were pinned rather than unfinished.
    - **Seven `backlog` rows and one `todo` row** — `TESTQ-01`, `FLOW-09`, `REASON-01`, `FLOW-07`,
      `COV-01`, `AGCOL-01`, `GAUTH-01`, and the unused-type-member todo. Each row's disposition word
      must be the identical word written into `.planning/BACKLOG.md` by plan 09-05. `Evidence` cites
      the backlog line and the requirement clause or measurement that carried it.
    - **Five `window` rows** — entries 9, 19, 21, 22 and 30. Entries 19, 21 and 22 are `implemented`,
      carried by the production rewrite `RCOV-02`'s clause records and closed through the ledger's own
      verb. Entry 30 is `implemented` with a split-disposition section, because half of it was fixed
      outright and half was accepted as superseded. Entry 9 is `unresolved` — deliberately retained,
      not deferred: its own repaired text says it stays open as the durable record of a residual risk
      rather than as a pending action. `Evidence` carries the post-closure count from
      `09-04-SUMMARY.md`.
    - **Three `caveat` rows** — the `discover.ts` pin claim and the `install-outcome.ts` pin claims,
      both `evidence-only`, carrying the arguments from Task 2; and the `direct-coverage` CI job,
      `unresolved`.

    **Split-disposition sections.** Below the table, add one `## <item> — a split disposition` section
    for every row whose single word would mislead. At minimum: `REASON-01` (the terminal route
    shipped; the reroute the item asked for did not), `GAUTH-01` (the cause line exists and is wired
    at one of five call sites), window entry 30, both pin-claim rows including the residual each
    carries, and the `direct-coverage` CI job.

    The CI-job section must name three things precisely: what the local proxy does prove, what it
    cannot — the GitHub-side ref creation the in-job base assertions exist to catch — and that whether
    the job is a required status check is a protected-branch setting that lives in no tracked file.
    State what would close the row: the job's first real run on a pull request. Do not fold it into a
    passing claim and do not describe it as low risk.

    **Final measurement section.** Quote Task 1's two measurements verbatim with their commit SHA and
    date, and state that the only changes landing after them are planning documents no test reads.

    Phase and plan numbers are legitimate in a planning artifact, so `Carried by` may name them. They
    remain barred from source comments.

    Run `SKIP=trufflehog pre-commit run --files .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md`
    and iterate until clean before committing. After committing, re-run the seal gate: a
    `.planning/`-only commit should not move it, and confirming that is cheap.
  </action>

  <acceptance_criteria>
    - `.planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md` exists and is
      tracked by git.
    - Its table has exactly the five columns `Item`, `Kind`, `Disposition`, `Carried by`, `Evidence`.
    - It carries 24 rows: 8 `requirement`, 7 `backlog`, 1 `todo`, 5 `window`, 3 `caveat`.
    - Every value in the `Disposition` column is one of `implemented`, `evidence-only`, `superseded`,
      `deferred`, `unresolved`. No other word appears in that column.
    - Every row's `Evidence` cell is non-empty and carries either a command with its output or a
      file-and-line citation.
    - For each of the eight items plan 09-05 disposed, the ledger's disposition word is character-
      identical to the word in `.planning/BACKLOG.md` or the todo file.
    - The window row for entry 9 reads `unresolved`, not `deferred`, and the ledger says it is
      deliberately retained.
    - Exactly one row carries `unresolved` in the `caveat` kind, and it is the `direct-coverage` CI
      job.
    - The ledger contains the sentence stating that a complete coverage reading is reachability
      evidence only, and no row uses a coverage reading to claim an assertion is strong.
    - The ledger quotes Task 1's `npm run check` and `npm run test:coverage:direct:all` output
      verbatim with the commit SHA.
    - The open-window distribution the ledger names includes phase 116.
    - `node scripts/revalidation.mjs scope-impact --check` still prints
      `Scope impact valid: 40 records.` after the commit.
  </acceptance_criteria>

  <verify>
    <automated>git ls-files -- .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md</automated>
    <fails_when>empty output — the ledger was not created or not committed</fails_when>
    <automated>grep -c '| unresolved |' .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md</automated>
    <fails_when>any output other than `2` — one for window entry 9 and one for the direct-coverage CI job</fails_when>
    <automated>grep -oE '\| (implemented|evidence-only|superseded|deferred|unresolved) \|' .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md | wc -l</automated>
    <fails_when>any output other than `24` — a row is missing, or a row carries a word outside the closed set</fails_when>
    <automated>grep -c '116' .planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md</automated>
    <fails_when>an output of `0` — the open-window distribution omits the largest remaining group</fails_when>
    <automated>node scripts/revalidation.mjs scope-impact --check</automated>
    <fails_when>any output other than `Scope impact valid: 40 records.`, or non-zero exit</fails_when>
  </verify>

  <done>
    One closure record exists carrying 24 rows in one vocabulary, each with real evidence; the caveats
    that cannot be closed from inside this repository are named as such rather than folded into a
    passing claim; and the final-tree measurement is quoted verbatim with the commit it was taken at.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
Created by this plan:

| Symbol / path | Kind | Home |
| --- | --- | --- |
| `.planning/phases/09-final-quality-and-backlog-closure/09-CLOSURE-LEDGER.md` | new planning artifact | the phase directory |

Plus two recorded results that live in `09-06-SUMMARY.md` and are quoted by the ledger: the
final-tree `npm run check` and `npm run test:coverage:direct:all` output, and the three traced pin
`reasons` arguments with their residuals.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage pin `reasons` → the gate's authority | A `reasons` entry is a claim no gate in this repository can check. If a reason is wrong, the pin is an allow-list with good manners. |
| the closure record → any future reader | The ledger is the durable statement of what this milestone did; a row that overstates is indistinguishable from one that is true. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-23 | Repudiation | the coverage pin's `reasons` entries | high | mitigate | Task 2 traces all three claims against the source this cycle rather than inheriting a conclusion, and writes the two residuals the pin's own text omits into the ledger. Inheriting the judgement would reproduce the exact defect the standing caveat names. |
| T-09-24 | Repudiation | the `direct-coverage` CI job's status | high | mitigate | One explicit `unresolved` row naming what is unproven, why it cannot be proved locally, and what would close it. The acceptance criterion counts `unresolved` rows so the caveat cannot be quietly upgraded. |
| T-09-25 | Repudiation | a `CLOSE-01` claim over a stale measurement | high | mitigate | Task 1 runs both commands on the committed final tree, records the SHA, and states that only planning documents land afterwards. Reuse of an earlier figure is barred in `must_haves.prohibitions`. |
| T-09-26 | Repudiation | a coverage reading read as assertion strength | medium | mitigate | Stated once in the preamble as a boundary and barred per-row; D-09-18 is the decision and the acceptance requires the sentence to be present. |
| T-09-27 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; `package.json` is untouched. |
</threat_model>

<verification>
- `npm run check` and `npm run test:coverage:direct:all` both exit 0 on the final tree, recorded
  verbatim with their commit SHA.
- `09-CLOSURE-LEDGER.md` is tracked, carries 24 rows, and every `Disposition` value is inside the
  closed five-word set.
- Exactly two rows read `unresolved`: window entry 9 and the `direct-coverage` CI job.
- Every disposition word matches the word plan 09-05 wrote into `.planning/BACKLOG.md` for the same
  item.
- `node scripts/revalidation.mjs scope-impact --check` still prints `Scope impact valid: 40 records.`
</verification>

<success_criteria>
The milestone's confirmed work is measured as a whole on the tree that ships it, the bundled backlog
is closed with an audit trail in one vocabulary, and the two things that cannot be settled from
inside this repository are named as unresolved rather than absorbed into a passing claim.
</success_criteria>

<output>
Create `.planning/phases/09-final-quality-and-backlog-closure/09-06-SUMMARY.md` when done.
</output>
