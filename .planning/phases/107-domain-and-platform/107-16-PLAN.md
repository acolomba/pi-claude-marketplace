---
phase: 107-domain-and-platform
plan: 16
type: execute
wave: 1
depends_on: [107-15]
files_modified:
  - extensions/pi-claude-marketplace/platform/git-credential.ts
  - tests/platform/git-credential.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Fill sends the host-only wire request and returns complete credentials.
    - Fill returns null for process, exit, timeout, and incomplete-output failures.
    - Approve and reject send complete credential wire requests.
    - Approve and reject treat process failures as best-effort no-ops.
    - Control characters cannot enter a credential attribute.
  prohibitions:
    - Do not test a helper mock instead of production credential operations.
    - Do not touch the developer's credential helper or keychain.
    - Do not use fixed temporary paths or optional smoke tests.
    - Do not wait for the production timeout.
    - Do not keep coverage-line or design-history comments in the test.
---

<objective>
Refactor the git credential adapter source-test pair.

Expose a narrow process factory, then test the complete wire protocol and
failure policy with fresh case-owned process fakes and strict spawn commands.
</objective>

<tasks>

<task type="auto">
  <name>Replace the git credential adapter test</name>
  <files>
extensions/pi-claude-marketplace/platform/git-credential.ts,
tests/platform/git-credential.test.ts
  </files>
  <action>
Create credential operations from an explicit spawn boundary and configurable
timeout. Keep the default operations unchanged. Replace helper and shell-script
tests with direct factory tests and one restored PATH failure for the default
spawn adapter.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
