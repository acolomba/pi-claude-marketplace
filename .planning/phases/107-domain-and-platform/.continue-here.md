---
context: phase
phase: 107-domain-and-platform
task: 19
total_tasks: 25
status: in_progress
last_updated: 2026-08-28T23:31:49.437Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. This constraint was discovered during this work.
> Acknowledge it explicitly before proceeding.

- [ ] CONSTRAINT: GSD workflow bypass — Phase 106 and Phase 107 through Plan
      19 were executed with hand-written artifacts before the GSD skills became
      available — resume through `$gsd-resume-work` and reconcile planning
      state before any source edit.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
| --- | --- | --- | --- |
| Manual GSD emulation | Plans, summaries, and STATE updates were written directly instead of through the installed GSD skills. | blocking | Run `$gsd-resume-work`, verify the active phase, and route the next action through the GSD workflow. |
| Formatting PROJECT.md | A full Prettier pass damaged the large decision table and exposed an unescaped pipe problem. | advisory | Do not run Prettier on `.planning/PROJECT.md`; inspect and edit only the required rows. |
| Phase auto-detection | The pause workflow's generic `PLAN.md` probe did not match numbered plan files. | advisory | Trust `.planning/STATE.md` and the numbered files under `107-domain-and-platform`. |

<current_state>

Phase 107, Plan 19 is the active unit. Hook partitioning has been extracted to
`domain/components/hooks/partition.ts`, re-exported from `hooks.ts`, and given
a direct corresponding test. Focused and affected tests pass. ESLint,
TypeScript, and direct coverage pass. Direct coverage is 33/33 branches, 6/6
functions, and 131/131 lines.

The implementation is complete, but `107-19-SUMMARY.md` does not exist and
STATE still says Plan 19 is in progress. Stop here and reconcile this state
through GSD before starting another module.

</current_state>

<completed_work>

Completed tasks:

- Phase 106: direct coverage runner, corresponding-test checker, negative
  controls, `strong-mock`, and package scripts.
- Plans 107-01 through 107-16: name, MCP component, manifest lookup, plugin
  root, clone key, hook if-targets, plugin component, manifest, auth registry,
  version, manifest cache, hook tool names, hook events, GitHub auth, Pi API,
  and Git credential pairs.
- Plan 107-17: extracted and covered hook matcher parsing.
- Plan 107-18: extracted and covered the hook TypeBox schema.
- Plan 107-19: extracted and covered hook partitioning; summary/state closure
  remains.

All completed pairs report direct 100% branch, function, and line coverage.

</completed_work>

<remaining_work>

- Plan 107-19: write the summary and update STATE through the GSD workflow.
- Extract and test hook if-predicate compilation.
- Extract and test hook summary projection.
- Finish the top-level hook parser test and remove migrated duplicate cases.
- Refactor `domain/source.ts`, `domain/resolver.ts`, and `platform/git.ts` one
  production module at a time.
- Continue Phases 108 through 116 from ROADMAP.md.
- Run the full test suite and `npm run check` at the phase or milestone gate.

</remaining_work>

<decisions_made>

- Keep resolver `state` with three values and add `installable: true | false`.
- Internalize hook event and tool-name metadata when consumers do not need it.
- Inject private HTTP, wait, spawn, and timeout boundaries at real adapter
  seams. Do not add generic test helpers.
- Split the hook monolith by stable responsibility while preserving `hooks.ts`
  compatibility re-exports.
- Remove the unreachable hook-table desynchronization branch. The exact table
  type now enforces the invariant.
- Keep tests free of migration-history comments and source-location lore.

</decisions_made>

<blockers>

- Workflow blocker: the current planning state has not been validated by the
  installed GSD skills. The workaround is `$gsd-resume-work` followed by state
  reconciliation.
- No technical, external, or human-action blocker exists.

</blockers>

## Required Reading (in order)

1. This handoff and `.planning/HANDOFF.json` — exact pause state.
2. `AGENTS.md` — repository and Git constraints.
3. `docs/guidelines/typescript-unit-testing-guidelines.md` — authoritative
   test design.
4. `.claude/rules/typescript-unit-testing.md` — authoring checklist that also
   applies in Codex.
5. `.planning/inputs/unit-test-refactor-handoff/START-HERE.md` — preserved
   evidence limits.
6. `.planning/STATE.md` and `.planning/ROADMAP.md` — milestone position.
7. `107-19-PLAN.md` and Plans 107-17/18 summaries — immediate module context.

## Critical Anti-Patterns (do NOT repeat these)

- Manual GSD emulation: do not create the next plan or edit source until the
  resume workflow has reconciled the current state.
- Do not apply the abandoned handoff patch or replay its old file layout.
- Do not use source-text tests when exported runtime behavior can prove the
  contract.
- Do not run Prettier over `.planning/PROJECT.md`.

## Infrastructure State

- Branch: `features/unit-test-refactor`.
- Base HEAD: `06887215` (`chore: refine agent skill installation (#151)`).
- Worktree: heavily dirty; no refactor commit exists.
- Async jobs: none.
- Background servers or watchers: none.
- External credentials or developer keychain changes: none.
- Last checks: TypeScript and `git diff --check` pass. Plan 19 focused tests,
  affected resolver tests, ESLint, and direct coverage pass.
- Not yet run: complete test suite, all-pair coverage, and `npm run check`.

<context>

Continue methodically with one production module at a time. The next intended
hook concern was if-predicate compilation, followed by summary projection and
the reduced top-level parser. That intent is not authorization to start it
before GSD reconciliation. Preserve the user's goal that the suite should read
as if it was designed under the guidelines from the start.

</context>

<next_action>

Start with: run `$gsd-resume-work`, acknowledge the blocking constraint, and
reconcile Plan 107-19. Then record its completion before planning the hook
if-predicate module.

</next_action>
