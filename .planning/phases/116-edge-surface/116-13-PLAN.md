---
phase: 116-edge-surface
plan: "13"
type: execute
wave: 4
depends_on: ["116-00", "116-01"]
files_modified:
  - tests/edge/handlers/marketplace/update.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The absence or presence of the optional name positional selects between the all-marketplaces workflow and the single-marketplace workflow, and that selection is edge-owned and exhaustively exercised in both directions."
    - "Both workflows receive the identical gitOps port and the identical pluginUpdate port the handler was constructed with, proven by each port recording the work its workflow performed."
    - "The parse-failure callback collapses the duplicated usage block to the missing-argument sentence and passes any other diagnostic through verbatim, then returns without reaching either workflow — the region uncovered at the start of this plan."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This pair carries no exhaustiveness claim: marketplace/update.ts selects between two workflows with an if statement over an optional value, not a switch over a closed union, so no missing-arm plant has a target here."
    - "The pair reaches 100 percent direct functions and lines and every reachable branch, and lands with functions and lines both complete and exactly ONE uncovered branch (measured 2026-09-02 at branches 11/12; the branch denominator tracks suite strength, so it is recorded as an observation, not a gate). The single shortfall is edge/handlers/marketplace/update.ts:41 — the message === USAGE collapse arm of the parse-failure callback. It is unreachable at runtime here for a STRUCTURAL reason, not a compiler-forced one: parseCommandArgs passes the usage string to the callback only for a REQUIRED positional, and this schema declares its sole positional required false, so the arm is dead for THIS module and stays LIVE for the sibling handlers that declare a required positional. Proved by measurement, not inspection: 170 argument shapes produced no notification beginning with the missing-argument sentence, a plant on the arm's literal stayed GREEN, and an inverted-condition plant turned the rejection case RED. This is the shortfall class D-116-01a admits as amended; no coverage-exception pragma is added and no production file changes."
  artifacts:
    - path: tests/edge/handlers/marketplace/update.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/marketplace/update.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/marketplace/update.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
      via: "two injected ports whose call recorders prove which workflow ran and that both ports were forwarded"
      pattern: "makeMarketplaceUpdateHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the marketplace update shim, whose real promise is a two-way workflow
selection driven purely by argument arity.

Purpose: `edge/handlers/marketplace/update.ts` is 72 lines and sits at 7/9 branches, 2/3 functions,
and 64/72 lines. The uncovered region is `update.ts:38-43` and `update.ts:47-48` — the parse-failure
callback body and the early return that follows — reached by an unrecognised scope value, which the
existing four-case suite never drives.

It is one of the four handlers carrying real injected collaborators: `deps.gitOps` and
`deps.pluginUpdate` are declared members of the dependency object, and both are forwarded into
whichever of the two workflows the arity selects. That double forward, plus the selection itself, is
what this owner proves (locked decision D-116-05 / option O3).

Output: one exhaustive, hermetic owner for `edge/handlers/marketplace/update.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the marketplace update owner with both ports recording and both selection arms exercised</name>
  <files>tests/edge/handlers/marketplace/update.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
    - extensions/pi-claude-marketplace/edge/args-schema.ts
    - extensions/pi-claude-marketplace/edge/types.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/marketplace/update.test.ts
    - tests/platform/git-ops-fake.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/marketplace/update.test.ts`. One export, so a flat sequence of `test()`
bodies with no `describe()`.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts`: one `mkdtemp`
working root and one `mkdtemp` home root per case; the home and agent-directory environment
variables saved with an own-property check and restored in a `t.after()` registered **before** the
act, with the agent-directory variable deleted rather than overwritten; and a context-owned
replacement of the global network entry point that throws on any call and exposes its call count. No
`finally` block. Build the Pi boundary with `createNotificationBoundary`, never a hand-rolled context
object; rejecting cases size it at one emission and zero probes, delegating cases at the counts the
workflow actually spends. Delegating cases state `ctx.cwd` through the boundary `cwd` option with
the read count the handler performs.

Build the git port with `createGitOpsFake({ boundary: "memory", ... })` from
`tests/platform/git-ops-fake.ts` — never a hand-rolled literal of git functions. Build the plugin
update port as a strict interaction mock created inside the case, with exact-parameter matching, its
type derived from the dependency object's own member type rather than restated by hand, its complete
promised call stated in one `when()`, and `verify()` as the last line after the result assertions.
Seed marketplaces with `tests/helpers/marketplace-seed.ts`.

**The selection proof, both directions.** This is the module's real promise:

- No positional: the all-marketplaces workflow runs. Prove it by seeding **two** marketplaces and
  asserting both were touched — the git port recorder holds a fetch for each, compared as one whole
  value. A single-marketplace run would touch only one, so this case discriminates.
- One positional name: the single-marketplace workflow runs. Prove it by seeding two marketplaces
  and asserting the recorder holds work for the named one **only**. Without the second seeded
  marketplace this case passes for either workflow.

**Both port forwards.** In each of the two selection cases, assert the git port recorder is
non-empty in exactly the shape above, and verify the plugin update mock's stated call. Both
recordings are only possible if the identical port objects the handler was constructed with reached
the workflow. Do not re-derive either workflow's outcome value or notification body — their own
pairs own those at 100 percent direct coverage.

**Rest of the matrix:**

- One above the accepted arity — two positionals: the optional single-positional schema takes the
  first token, so read the module and state the observed outcome. Assert which workflow ran and with
  what name rather than assuming a rejection.
- Each scope value supplied, as a row table, and scope omitted: the scope member is present in the
  workflow options only when supplied, observable through which scope's records were touched. Seed
  the same marketplace name in both scopes so a wrong scope is visible.
- Mutually exclusive scope selectors supplied together: this handler does not consume the
  scope-target flag, so read the module and state what happens to that token. Assert the observed
  outcome; do not guess.
- An unrecognised scope value: this is the uncovered region. Exactly one notification whose whole
  value is the parse diagnostic, a blank line, and **the update usage block written out by hand**;
  then `verifyBoundary()`, an empty git port recorder, and a plugin update mock with no stated
  expectation — which throws on any call, so a green case proves neither workflow ran.
- A diagnostic that is not the usage string reaches the user verbatim rather than being collapsed to
  the missing-argument sentence — the discriminating case for the collapse rule.

Write every expected notification out by hand as one whole value including the blank line. Do not
read the usage string back off the handler and do not build an expected message by calling the
production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID; they may not
cite a plan, phase, wave, or task. Every runtime case carries separate lowercase `// arrange`,
`// act`, `// assert` phases in that order, blank-line separated. Name values after their production
role. Use no double assertion through `unknown`, no broad partial cast, no wildcard matcher, no
unbounded-count expectation, and no module replacement or loader hook.

Plant (D-116-04): invert the selection guard so a supplied name routes to the all-marketplaces
workflow, confirm both selection cases go RED, and revert. Then change the handler to construct a
fresh git port instead of forwarding the injected one, confirm the recorder assertions go RED, and
revert. Record both verbatim failure outputs. A plant that stays GREEN is a finding.

This pair makes no exhaustiveness claim. The selection is an `if` over an optional value, not a
`switch` over a closed union, so no missing-arm plant has a target here.

Do not restate what a gate already owns: no case asserts the absence of direct process output, no
case re-proves the positional-schema behavior owned by `tests/edge/args-schema.test.ts`, and no case
re-derives either update workflow outcome owned by its own orchestrator pair.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/marketplace/update.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts 2&gt;&amp;1 | node -e 'let s="";process.stdin.on("data",function(d){s+=d}).on("end",function(){var v=/^Incomplete direct coverage for extensions\/pi-claude-marketplace\/edge\/handlers\/marketplace\/update\.ts: branches (\d+)\/(\d+)$/m.exec(s);if(!v)throw new Error("no documented incomplete verdict, or lines/functions are no longer complete");if(v[2]-v[1]!==1)throw new Error("expected exactly 1 uncovered branch, saw "+(v[2]-v[1]));});' &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/marketplace/update.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/marketplace/update.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/marketplace/update.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/marketplace/update.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/marketplace/update.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, the coverage gate not printing an `Incomplete direct coverage for .../edge/handlers/marketplace/update.ts:` line; that line carrying a `lines` or `functions` clause, which would mean lines or functions are no longer complete; more or fewer than exactly one uncovered branch (denominator minus numerator other than 1); or a passing verdict, which means the documented shortfall no longer holds and must be reported rather than edited away, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/marketplace/update.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. Both selection cases seed two marketplaces so the all-marketplaces and single-marketplace outcomes are distinguishable. The git port is `createGitOpsFake`, never a hand-rolled literal, and the plugin update port is a strict interaction mock verified at the end of its case. The rejection case uses a mock with no stated expectation, so a green result proves neither workflow ran.
  </acceptance_criteria>
  <done>P116-13 is contract-compliant, proves both selection arms and both port forwards, and is at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-13-A | Tampering | a state-changing marketplace update that fans out across every recorded marketplace when no name is supplied | high | mitigate | Both selection arms are proven with two seeded marketplaces so a wrong fan-out is visible; every rejection is proven to leave both ports untouched and both workflows unreached. |
| T-116-13-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-13-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/marketplace/update.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/marketplace/update.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/marketplace/update.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-13-SUMMARY.md when done.
</output>
