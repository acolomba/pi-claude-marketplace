---
phase: 116-edge-surface
plan: "17"
type: execute
wave: 5
depends_on: ["116-00", "116-02", "116-07"]
files_modified:
  - tests/edge/handlers/plugin/import.test.ts
  - tests/edge/handlers/import.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "makeImportHandler calls its injected import delegate exactly once with the complete options object — context, Pi handle, working directory, selected scopes, and git port — every member stated, which is the one literal exact-argument proof available anywhere in the handler tier."
    - "An omitted scope flag selects both scopes in the stated order; a supplied scope flag selects exactly that one scope."
    - "The delegate member is optional: omitting it runs the real import workflow, which is a distinct branch and is exercised against a hermetic tree."
    - "Import accepts zero positionals; any positional is rejected with its own sentence before the workflow runs."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "The test file moves from the parent directory to its mirrored path in the same commit as the rewrite, because the correspondence gate rejects any intermediate state where only one member of the pair exists."
    - "This pair carries no exhaustiveness claim: plugin/import.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
    - "The pair reaches 100 percent direct functions and lines and every reachable branch, landing with functions and lines both complete and exactly ONE uncovered branch. The absolute branch numbers are measured by the executor and recorded in the summary as an observation, not authored here as a gate. The single shortfall is edge/handlers/plugin/import.ts:31 — the string-conversion arm of the error formatter, which only a non-Error throw could reach — which is unreachable at runtime and exists only because the `unknown` type of a `catch` clause binding forces it. This is the one shortfall class D-116-01a admits; no coverage-exception pragma is added and no production file changes."
  artifacts:
    - path: tests/edge/handlers/plugin/import.test.ts
      provides: "New sole mirrored direct owner for edge/handlers/plugin/import.ts, closing two correspondence-gate violations with one move"
  key_links:
    - from: tests/edge/handlers/plugin/import.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts
      via: "exact-argument strong mock on the optional injected import delegate"
      pattern: "makeImportHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Move the misplaced import test to its mirrored path and rewrite it as the one handler owner that can
honour the exact-argument promise literally.

Purpose: `edge/handlers/plugin/import.ts` is the only handler in the tier with a real orchestrator
seam. Its dependency object declares an optional `importClaudeSettings` member, and the handler calls
that member when supplied. That makes the complete options object statable in a strict interaction
mock — the literal form of D-116-05, available nowhere else among the seventeen handlers.

Two structural obligations come with it. The existing test sits at `tests/edge/handlers/import.test.ts`,
one directory above its mirrored path, and the correspondence gate counts that as both a missing test
and an unexpected test; one move closes both (D-116-09). The move and the rewrite must land in the
**same commit**, because the gate rejects any state where only one member of a changed pair exists on
disk. And the handler's error formatter carries a branch for a thrown non-error value that the
tokenizer can never produce. It exists only because a `catch (err)` binding is typed `unknown`, and
removing it needs a type assertion this phase bars, so the pair lands one branch short under
D-116-01a with zero uncovered lines.

Output: one new owner at the mirrored path, the old path gone, and a documented, argued
single-branch shortfall. No production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the test to its mirrored path and rewrite it with an exact-argument delegate mock</name>
  <files>tests/edge/handlers/plugin/import.test.ts, tests/edge/handlers/import.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts
    - extensions/pi-claude-marketplace/edge/args.ts
    - extensions/pi-claude-marketplace/orchestrators/import/index.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/import.test.ts
    - tests/orchestrators/import/execute.test.ts
    - tests/platform/git-ops-fake.ts
    - tests/helpers/notification-boundary.ts
    - tests/orchestrators/edge-deps.test.ts
    - scripts/check-corresponding-tests.mjs
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Start with `git mv tests/edge/handlers/import.test.ts tests/edge/handlers/plugin/import.test.ts`,
then rewrite the moved file in place. Do not create the new file separately and delete the old one —
the move must be a single rename so history follows it, and both halves must be staged together.
Keep the whole of this task in one commit; the correspondence gate throws on an intermediate state
where a changed pair is missing a member.

Rewrite the file as a flat sequence of `test()` bodies with no `describe()` — the module has one
runtime export, `makeImportHandler`, plus a dependency-object type.

**Harness.** Build the Pi boundary with `createNotificationBoundary`, never a hand-rolled context
object. Rejection cases size it at one emission and zero probes; the real-workflow case sizes it at
the counts the workflow spends, read from the module. Every delegating case states `ctx.cwd` through
the boundary `cwd` option with the read count the handler performs. Own a hermetic tree per
`tests/orchestrators/edge-deps.test.ts` for the real-workflow case: two `mkdtemp` roots, the home and
agent-directory variables restored through a `t.after()` registered before the act with the
agent-directory variable deleted, and a context-owned fail-fast network entry point exposing its call
count. Build the git port with `createGitOpsFake({ boundary: "memory", ... })`.

**The exact-argument case** — the reason this plan exists. Derive the delegate type from the
dependency object's own member type rather than restating the options shape. Create it as
`mock&lt;…&gt;({ exactParams: true, name: "import claude settings" })` inside the case. State one
`when()` carrying the **complete** options object — the context, the Pi handle, the working
directory, the selected scopes array, and the git port — with every member written out and no
wildcard matcher. Assert the boundary's recorded notifications, then call `verify()` as the last line.

Cover:

- Scope omitted: the selected scopes array is both scopes in the order the module states. Write that
  array out by hand in the `when()`; the order is part of the contract.
- Each scope value supplied, as a row table: the selected scopes array is exactly that one scope.
- The git port member is forwarded unchanged: the same object the handler was constructed with
  appears in the stated options.
- The delegate is optional and its absence is a distinct branch: construct the handler **without**
  the member and let the real import workflow run against the hermetic tree. Assert one minimal
  effect the real workflow produces rather than re-deriving its outcome, which its own pair owns at
  100 percent direct coverage. This is the only way to cover that branch.
- Accepted arity of zero positionals. One above it — one positional, and again with two: exactly one
  notification whose whole value is the no-positional-arguments sentence, a blank line, and **the
  import usage block written out by hand**, then `verifyBoundary()` and a delegate mock with no
  stated expectation, which throws on any call so a green case proves the workflow never ran.
- An unrecognised scope value: the tokenizer throws and exactly one notification carries the thrown
  error's own message with the usage block appended; the delegate is never called.
- Mutually exclusive scope selectors supplied together — a scope flag plus the scope-target flag:
  this handler parses raw arguments directly and runs no flag scan, so read the module and state
  where the scope-target token lands. Assert the observed outcome; do not guess.
- Offline: assert the network entry point's call count is zero in the delegate-mocked cases.

Write every expected notification out by hand as one whole value including the blank line. Do not
read the usage string back off the handler and do not build an expected message by calling the
production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID; they may not
cite a plan, phase, wave, or task. Every runtime case carries separate lowercase `// arrange`,
`// act`, `// assert` phases in that order, blank-line separated. Name values after their production
role. Use no double assertion through `unknown`, no broad partial cast, no wildcard matcher, no
unbounded-count expectation, and no module replacement or loader hook.

Plant (D-116-04): change the no-scope branch to select a single scope instead of both, confirm the
omitted-scope case goes RED, and revert. Then change the handler to pass a freshly-constructed git
port instead of the injected one, confirm the port-forward member of the stated options goes RED, and
revert. Record both verbatim failure outputs.

This pair makes no exhaustiveness claim; `plugin/import.ts` has no `switch`. Do not restate what a
gate already owns: no case asserts the absence of direct process output, no case re-proves the
tokenizer behavior owned by `tests/edge/args.test.ts`, and no case re-derives the import workflow
outcome owned by its own orchestrator pair.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/import.test.ts &amp;&amp; test ! -e tests/edge/handlers/import.test.ts &amp;&amp; node scripts/check-corresponding-tests.mjs; test $? -eq 1 &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'tests/edge/handlers/(plugin/)?import\.test\.ts'</automated>
    <fails_when>non-zero exit: a `# fail` count greater than 0 in the `node --test` TAP summary, the old path still existing on disk, or either `import.test.ts` path still appearing in the correspondence-gate output as a missing-test or unexpected-test violation.</fails_when>
  </verify>
  <acceptance_criteria>
`git status` shows the file as a rename, not as an add plus a delete.
`node scripts/check-corresponding-tests.mjs` no longer names either `tests/edge/handlers/import.test.ts`
or `tests/edge/handlers/plugin/import.test.ts`, and its total violation count is two lower than
before this task. The exact-argument case states the complete options object with every member
written out, uses no wildcard matcher, and ends with a verification call. The delegate-absent case
runs the real workflow against a hermetic tree. Both halves of the change are staged and committed
together.
  </acceptance_criteria>
  <done>The import pair sits at its mirrored path with a literal exact-argument proof, and two correspondence violations are closed by one move.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the pair and document the compiler-mandated branch shortfall</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts
    - extensions/pi-claude-marketplace/edge/args.ts
    - extensions/pi-claude-marketplace/shared/errors.ts
    - tests/edge/handlers/plugin/import.test.ts
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
After every behavioral case from Task 1 passes, run the direct coverage gate for
`extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts` and record its verdict line
verbatim.

The catch block formats the thrown value with a conditional that falls back to a string conversion
when the thrown value is not an error. The only throw that reaches this catch comes from the
argument tokenizer, which throws an `Error` at every one of its throw sites, so that fallback arm is
unreachable at runtime and **no case can cover it**. It exists only because a `catch (err)` binding
is typed `unknown`, which cannot be narrowed to `Error` without leaving a residual arm; removing it
needs a type assertion, which this phase's own anti-pattern grep bars.

The expected end state is therefore: **zero uncovered lines, and exactly one uncovered branch — the
string-conversion arm at `import.ts:31`.** The conditional sits on a single line that the tokenizer
case does execute, so only the branch is short. Record the concrete branch numbers the gate prints;
they cannot be predicted here because this pair has no owner at its mirrored path today.

**Change no production file.** Do not delete the arm, do not add a coverage-exception pragma, and do
not write a case that cannot fail in order to look covered. In particular, do **not** replace the
local conditional with the shared error-message helper the sibling handlers use: that would reach
100 percent, but it is a production change this milestone does not authorize, and it belongs to
whoever consolidates the error-formatting surface later.

Record in the summary: the gate verdict line verbatim, that the shortfall is exactly one branch at
`import.ts:31`, that a `catch` binding typed `unknown` is what forces the arm to exist, and that
D-116-01a admits exactly this class. If the gate reports any uncovered **line**, or more than one
uncovered branch, or an uncovered branch anywhere else, stop and report it — the documented
shortfall has changed.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/import.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts 2&gt;&amp;1 | node -e 'let s="";process.stdin.on("data",function(d){s+=d}).on("end",function(){var v=/^Incomplete direct coverage for extensions\/pi-claude-marketplace\/edge\/handlers\/plugin\/import\.ts: branches (\d+)\/(\d+)$/m.exec(s);if(!v)throw new Error("no documented incomplete verdict, or lines/functions are no longer complete");if(v[2]-v[1]!==1)throw new Error("expected exactly 1 uncovered branch, saw "+(v[2]-v[1]));});' &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/import.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/import.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/import.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/import.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/import.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, the coverage gate not printing an `Incomplete direct coverage for .../plugin/import.ts:` line; that line carrying a `lines` or `functions` clause, which would mean lines or functions are no longer complete; more or fewer than exactly one uncovered branch (denominator minus numerator other than 1); or a passing verdict, which means the documented shortfall no longer holds and must be reported rather than edited away, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/import.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The correspondence gate no longer names either import test path, and its violation count is two lower than before this plan. The pair lands at the documented shortfall and no further: the coverage gate prints an `Incomplete direct coverage for .../plugin/import.ts:` verdict carrying a branches clause and no lines or functions clause, whose branch numbers differ by exactly one; no coverage-exception pragma is added; and the summary quotes the verdict line verbatim with its measured branch numbers together with the D-116-01a argument for it.
  </acceptance_criteria>
  <done>P116-17 sits at its mirrored path, carries the phase one literal exact-argument proof, and is at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-17-A | Tampering | a bulk cascade install driven by an imported configuration file, reached through the one injectable handler seam | high | mitigate | The delegate call is a strict mock stating the complete options object including the selected scope list and the git port, so a stray or wrong-valued member fails the case; every rejection uses a mock with no stated expectation, so a green case proves the cascade never started. |
| T-116-17-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-17-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/import.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/import.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/import.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions with its owner run alone, and lands with
  exactly ONE uncovered branch at the range D-116-01a documents for this pair. The absolute branch
  numbers are an observation, not a gate: V8 collapses a branch range whose count does not diverge
  from its enclosing block, so the denominator tracks suite strength rather than source shape.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- The pair lands at the documented D-116-01a shortfall and no further, with no production file changed and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-17-SUMMARY.md when done.
</output>
