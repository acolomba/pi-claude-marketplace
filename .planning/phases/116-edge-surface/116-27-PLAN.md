---
phase: 116-edge-surface
plan: "27"
type: execute
wave: 6
depends_on: ["116-00"]
files_modified:
  - tests/edge/handlers/tools.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "registerListMarketplacesTool and registerListPluginsTool each register exactly one tool with its declared name and parameter schema, and the registered callbacks are captured and invoked rather than merely registered."
    - "projectRowStatus maps every reachable plugin status onto the three-value tool surface and throws a named error for every status the list surface cannot produce."
    - "The three status projections that return a non-nullable value carry a real exhaustiveness guarantee: deleting an arm from any of them makes the typecheck fail, and each is planted to prove it."
    - "All four switches carry a real missing-arm gate. The three with a non-nullable return raise TS2366; the version reader, whose return includes undefined, raises TS7030 instead because noImplicitReturns (tsconfig.json:11) makes the end of a no-default switch reachable. Research predicted a missing arm would compile clean there; a compiler repro run against this repository settings disproves it, and that correction is the phase D-116-14 finding."
    - "The version reader arms for statuses the status projection throws on are unreachable at runtime and are deliberately KEPT. Deleting them raises TS7030, and the two shapes that compile — a trailing return, or a default arm — each trade the uncovered branch for an uncovered line, and the default arm additionally destroys the missing-arm gate. Keeping them is the shape that keeps the gate firing."
    - "The filter parameters select which status buckets appear, and an unmatched bucket is skipped rather than rendered empty."
    - "This read-only tool surface never reaches the network: a context-owned fail-fast network entry point records zero calls across every case."
    - "The pair covers every reachable line and branch, landing at lines 512/518 with the uncovered set being exactly edge/handlers/tools.ts:392-394 and 399-401 — the version-reader arms kept to preserve the TS7030 gate. This shortfall is NOT the D-116-01a compiler-narrowing class; it is unreachable production logic retained because removing it would weaken a compile-time gate, and it awaits operator ratification. No coverage-exception pragma is added and no production file changes."
  artifacts:
    - path: tests/edge/handlers/tools.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/tools.ts, contract-compliant, and the phase whole exhaustiveness obligation"
  key_links:
    - from: tests/edge/handlers/tools.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/tools.ts
      via: "captured registration callbacks plus a typecheck-backed exhaustiveness plant on each sound switch"
      pattern: "projectRowStatus"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the read-only tool surface, and discharge the phase's entire exhaustiveness
obligation, which is locked to this plan alone.

Purpose: `edge/handlers/tools.ts` is 518 lines with four exports and sits at 86/106 branches and
503/518 lines. It is also the only module in the tier with switches over a **closed union**. The
preceding phase proved with a compiler repro that a switch is exhaustiveness-checked only when its
return type excludes undefined; four gates elsewhere in the codebase had been deleted against a
guarantee TypeScript never made.

Three of this module's four switches return a non-nullable value and therefore do carry the
guarantee. The fourth — the version reader — returns a union that includes undefined, so a deleted
arm compiles clean. That is the same class of hole D-116-14 was written about, one function away
from three sound ones, and it is this plan's job to prove all four cases empirically rather than
assume them.

A compiler repro run during planning corrects the premise for the fourth switch. `noImplicitReturns`
(`tsconfig.json:11`) makes the end of a `default`-less switch reachable once an arm is deleted, so a
missing arm there raises **TS7030**, not nothing. All four switches are gated; only the diagnostic
differs. That also settles what to do with the reader's two unreachable arms: deleting them raises
TS7030, and both shapes that do compile — a trailing `return`, or a `default` — trade the uncovered
branch for an uncovered line, with the `default` shape additionally destroying the gate. The arms
stay.

Output: one exhaustive, hermetic, offline-proven owner, four recorded plants, and an argued
shortfall. No production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the tools owner with captured registration callbacks and full status coverage</name>
  <files>tests/edge/handlers/tools.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/tools.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - extensions/pi-claude-marketplace/domain/source.ts
    - tests/edge/handlers/tools.test.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/tools.test.ts` in the G6 shape: one top-level `describe()` per exported
entrypoint — the two registration functions and the exported status projection — and no nesting. The
tool status union is a type.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts`: two `mkdtemp`
roots per case, the home and agent-directory environment variables saved with an own-property check
and restored in a `t.after()` registered **before** the act with the agent-directory variable deleted
rather than overwritten, and a context-owned replacement of the global network entry point that
throws on any call and exposes its call count. No `finally` block. Assert the call count is zero in
every case, by count and never by an error message.

**Registration.** Build the Pi handle as a strict interaction mock created inside the case with
exact-parameter matching, so the registration call itself is the promise. **Capture the registered
callback** off the recorded call and invoke it — merely asserting that registration happened leaves
the tool body unexecuted, which is why function coverage is short today. Assert the registered name
and the declared parameter schema as whole values against hand-authored literals, then drive the
captured callback against a seeded tree and assert its rendered output.

**Status coverage.** The exported status projection takes the full plugin status union, so drive
**every** member of that union through it as a row table: each reachable status asserting its
three-value projection, and each unreachable-on-this-surface status asserting the thrown error by
class and its structured fields, never by message substring. Then drive the tool callback against
seeded rows so the private label and key projections are reached through behavior rather than
directly — they are module-private and must stay so.

**Filters and rendering.** Cover each filter parameter alone, in combination, and absent; prove an
unmatched bucket is skipped rather than rendered as an empty section. Cover a plugin row that carries
a version, one that does not, and one whose scope falls back to the marketplace scope. Compare each
rendered payload as one whole value against a hand-authored literal; do not build an expected string
by calling the production renderer.

Case titles state public behavior and may cite a durable requirement or decision ID — `D-02`,
`D-54-01`, `FSTAT-02`, `FSTAT-04`, `D-66-03`, `USTAT-01`, and `D-15-04` are the right anchors — and
may not cite a plan, phase, wave, or task. Every runtime case carries separate lowercase
`// arrange`, `// act`, `// assert` phases in that order, blank-line separated. Name values after
their production role. Use no double assertion through `unknown`, no broad partial cast, no wildcard
matcher, no unbounded-count expectation, and no module replacement or loader hook.

Do not restate what a gate already owns: no case asserts the absence of direct process output, and no
case re-derives the list workflow outcome owned by its own orchestrator pair.

If this plan runs out of room, stop and report exactly which groups are complete and which remain.
Do not sample and do not shrink the status matrix to fit.

Change no production file in this task.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/tools.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/tools.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/tools.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/tools.test.ts &amp;&amp; git diff --quiet -- extensions/</automated>
    <fails_when>non-zero exit: a `# fail` count greater than 0 in the `node --test` TAP summary, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, `rg -c` finding zero arrange markers, or `git diff --quiet -- extensions/` exiting 1 because a production file changed in this task.</fails_when>
  </verify>
  <acceptance_criteria>
Every registered callback is captured off the recorded registration call and invoked; no case asserts
only that registration happened. Every member of the plugin status union is driven through the
exported status projection, with the unreachable statuses asserted by error class and structured
fields rather than by message substring. Every rendered payload is compared as one whole value
against a hand-authored literal, never against a value produced by the production renderer. Every
case asserts the fail-fast network entry point recorded zero calls, by call count. `git diff` shows
no change under `extensions/` at the end of this task.
  </acceptance_criteria>
  <done>The tools owner reaches every registered callback and every status arm through behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Plant all four switches and record the corrected version-reader finding</name>
  <files>tests/edge/handlers/tools.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/tools.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/tools.test.ts
    - .planning/phases/116-edge-surface/116-RESEARCH.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
This task discharges the phase's whole exhaustiveness obligation. It is scoped to this plan alone:
every other plan in the phase states that its pair carries no exhaustiveness claim, because no other
module in the tier switches on a closed union.

**The four plants.** For each of the four switches, delete one arm, run `npm run typecheck`, record
the verbatim result, and revert:

1. The status projection returning the three-value tool status. Expect **RED**, `TS2366`.
2. The private label projection returning a string. Expect **RED**, `TS2366`.
3. The private key projection returning a three-value character union. Expect **RED**, `TS2366`.
4. The version reader, whose return type includes undefined. Research predicted this would stay
   GREEN. **Expect RED with `TS7030: Not all code paths return a value.`** — `noImplicitReturns`
   (`tsconfig.json:11`) makes the end of the function reachable once an arm is deleted, and the
   switch carries no `default`. A repro run during planning with this repository's own compiler
   settings confirms it. If it stays GREEN, that is the finding and must be recorded as such; if it
   goes RED as expected, record that research's premise was wrong and that all four switches are
   gated, three by TS2366 and this one by TS7030.

If plant 1, 2, or 3 stays GREEN, stop and report it — the phase's whole exhaustiveness claim rests
on those three.

**Keep the unreachable arms. Change no production file.** The version reader's arms for the statuses
the status projection throws on are unreachable, and they are kept deliberately. Deleting them makes
the switch non-exhaustive, which raises TS7030 under the same setting plant 4 exercises. The only two
shapes that compile are worse: a trailing `return undefined;` is itself unreachable, so the uncovered
branch simply becomes an uncovered line; and a `default: return undefined;` is likewise uncovered
**and** silences the missing-arm gate for every future status. Per the operator's rule — prefer the
shape that keeps the gate firing — the shape is the current one, unmodified.

The pair therefore lands at **lines 512/518** with the uncovered set being exactly
`tools.ts:392-394` and `tools.ts:399-401`, plus their branches. Record the gate's verdict line
verbatim and the measured branch numbers.

**Close the gap by behavior**, since the compiler cannot: confirm the Task 1 status matrix drives the
version reader for every status reachable on the list surface and asserts the value it returns. Add
any missing status row.

Record in the summary: all four plant results verbatim, the declared return type of each of the four
switches, the coverage verdict line, and the argument for keeping the unreachable arms. Note
explicitly that this shortfall is **not** the D-116-01a compiler-narrowing class and awaits operator
ratification.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/tools.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/tools.ts 2&gt;&amp;1 | rg -q '^Incomplete direct coverage for extensions/pi-claude-marketplace/edge/handlers/tools.ts: ' &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/tools.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/tools.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/tools.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/tools.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/tools.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/tools.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, the coverage gate not printing an `Incomplete direct coverage for .../tools.ts:` verdict line, or printing one whose uncovered set is anything other than `392-394` and `399-401` — a wider set is a regression and a passing verdict means the documented shortfall no longer holds; either direction fails and must be reported rather than edited away, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/tools.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The summary records all four plant results verbatim and quotes each of the four declared return types; plant 4 is recorded against the corrected expectation of TS7030 rather than a clean compile. The pair lands at lines 512/518 with the uncovered set being exactly `tools.ts:392-394` and `tools.ts:399-401`, and the summary states that this shortfall is not the D-116-01a class and awaits ratification.
  </acceptance_criteria>
  <done>P116-27 is contract-compliant, discharges the phase whole exhaustiveness obligation with four recorded plants, and lands at the argued shortfall with no production change.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-27-A | Tampering | an LLM-callable read-only tool that projects internal plugin state onto a public status vocabulary | high | mitigate | Every status in the union is driven through the projection, with the statuses the surface cannot produce asserted to throw by error class and structured fields; the three sound exhaustiveness gates are planted to prove they fire, and the unsound one is recorded and closed by runtime cases. |
| T-116-27-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-27-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/tools.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/tools.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/tools.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- All four exhaustiveness plants are recorded verbatim, including the corrected expectation that the version reader raises TS7030 rather than compiling clean.
- No production file changes; the unreachable version-reader arms are kept because removing them would weaken the missing-arm gate.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-27-SUMMARY.md when done.
</output>
