---
phase: 116-edge-surface
plan: "21"
type: execute
wave: 5
depends_on: ["116-00", "116-02", "116-08", "116-23"]
files_modified:
  - tests/edge/handlers/plugin/pending.test.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "makePendingHandler accepts zero positionals plus an optional scope flag and forwards the working directory and the selected scope into the pending reconcile workflow."
    - "A first positional that looks like a long flag is rejected with the unknown-option sentence, while any other positional is rejected with the too-many-arguments sentence, and both leave the workflow unreached."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This read-only path never reaches the network and writes no file: a context-owned fail-fast network entry point records zero calls across every case."
    - "This owner carries the exact-argument gap D-116-05 scopes to the thirteen seamless handlers: the pending workflow is reached by direct import with no injection point, so the delegation is observed through one minimal sized effect rather than as a stated argument list. The negative half of D-116-06 is proven in full."
    - "This pair carries no exhaustiveness claim: plugin/pending.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
    - "The pair reaches 100 percent direct functions, lines, and branches with no coverage exception, which requires removing the one nullish fallback that is unreachable through the module exports."
  artifacts:
    - path: tests/edge/handlers/plugin/pending.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/plugin/pending.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/plugin/pending.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts
      via: "two distinct positional rejections plus boundary sizing over the shared prelude"
      pattern: "makePendingHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the pending shim, and close the one branch that cannot be reached from outside
the module.

Purpose: `edge/handlers/plugin/pending.ts` is 56 lines and sits at 8/9 branches with **every line
covered**, which is the signature of a branch inside an expression rather than a missing statement.
It is the nullish fallback on the first positional: the guard that precedes it has already proven the
positional list is non-empty, so the index read always yields a value and the fallback arm cannot be
entered. No case can cover it, and the milestone forbids a coverage exception, so the project rule's
own remedy applies — remove the dead code.

The module's real promise is that it distinguishes two rejection sentences from the shape of the
first positional, and that neither reaches the workflow.

Output: one exhaustive, hermetic, offline-proven owner plus the smallest production edit that
removes the unreachable fallback.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the pending owner and remove the unreachable nullish fallback</name>
  <files>tests/edge/handlers/plugin/pending.test.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
    - extensions/pi-claude-marketplace/edge/args.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/plugin/pending.test.ts
    - tests/edge/handlers/marketplace/autoupdate.test.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <reversibility rating="costly">Removing production code in a phase whose stated scope is tests
  only. Reversible by `git revert`, but it enlarges the phase diff and must be recorded and
  reviewable rather than silent.</reversibility>
  <action>
Two pieces of work, in this order.

**First, the owner.** Rewrite `tests/edge/handlers/plugin/pending.test.ts` as a flat sequence of
`test()` bodies with no `describe()`. Copy the hermetic scope from
`tests/orchestrators/edge-deps.test.ts` — two `mkdtemp` roots per case, the home and agent-directory
environment variables saved with an own-property check and restored in a `t.after()` registered
before the act with the agent-directory variable deleted rather than overwritten, and a context-owned
fail-fast network entry point exposing its call count. No `finally` block. Build the Pi boundary with
`createNotificationBoundary`, never a hand-rolled context object; copy the no-seam
negative-delegation case verbatim from `tests/edge/handlers/marketplace/autoupdate.test.ts`.
Rejection cases size the boundary at one emission and zero probes; delegating cases size it at the
counts the workflow spends, read from the module, and state `ctx.cwd` with the read count the handler
performs.

Cover:

- Accepted arity of zero positionals with no flags: the pending workflow runs against a seeded tree
  and its diff appears. That output is the one minimal effect; do not re-derive the workflow's
  outcome or full notification body, which its own pair owns at 100 percent direct coverage.
- One above the accepted arity — one ordinary positional: exactly one notification whose whole value
  is the too-many-arguments sentence, a blank line, and **the pending usage block written out by
  hand**, then `verifyBoundary()`. Add a two-positional row.
- One above the accepted arity — a first positional that begins with the long-flag prefix: the
  **unknown-option** sentence naming the offending token verbatim, not the too-many-arguments
  sentence. This pair of cases is the discriminating proof for the shape test; either one alone
  passes for a handler that always emits the same sentence.
- A long-flag-shaped token as the **second** positional with an ordinary first: the too-many-arguments
  sentence, because the shape test reads only the first. State that outcome explicitly.
- Each scope value supplied, as a row table, and scope omitted: the diff comes from the scope the
  flags selected. Seed differing state in both scopes so a wrong scope is visible.
- Mutually exclusive scope selectors supplied together: this handler does not consume the
  scope-target flag, so read the module and state where that token lands — it reaches the positional
  list and triggers one of the two rejections. Assert which.
- An unrecognised scope value: the shared prelude notifies once with the thrown error's own message
  and the usage block, and the workflow never runs.
- Offline and no writes: assert the network entry point's call count is zero in every case, and
  assert the hermetic tree's file listing is unchanged across the delegating case, which is the
  read-only proof.

Write every expected notification out by hand as one whole value including the blank line. Do not
read the usage string back off the handler and do not build an expected message by calling the
production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID — `DIFF-01`,
`D-53-01`, and `NFR-5` are the right anchors — and may not cite a plan, phase, wave, or task. Every
runtime case carries separate lowercase `// arrange`, `// act`, `// assert` phases in that order,
blank-line separated. Name values after their production role. Use no double assertion through
`unknown`, no broad partial cast, and no module replacement or loader hook.

**Second, the unreachable fallback.** After every behavioral case passes, re-run the direct coverage
gate; the branch count will still be short by one with no uncovered lines. Make the smallest edit to
`extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts` that removes the nullish fallback
on the first positional while keeping `makePendingHandler`'s exported signature and both rejection
sentences byte-identical, and while keeping the file free of non-null assertions and casts under
`noUncheckedIndexedAccess`. Restructuring the guard so the first element is bound in a position where
the compiler already knows it is present is the intended shape; do not introduce a second
always-true condition in its place, because that is the same dead branch under a new name. If no
such edit exists, **stop and report it** — that is an operator decision, not an executor one. Do not
add a coverage exception, do not add an ignore pragma, and do not write a case that cannot fail.

Record in the summary: the exact production lines changed, the argument that the fallback is
unreachable through the module's exports, and the before-and-after coverage numbers.

Plant (D-116-04): swap the two rejection sentences, confirm both discriminating cases go RED, and
revert. Record the verbatim failure output.

This pair makes no exhaustiveness claim; `plugin/pending.ts` has no `switch`. Do not restate what a
gate already owns: no case asserts the absence of direct process output, no case re-proves the
prelude owned by `tests/edge/handlers/plugin/shared.test.ts`, and no case re-derives the pending
workflow outcome owned by its own orchestrator pair.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/pending.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/pending.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/pending.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/pending.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/pending.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/pending.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/pending.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. The only change under `extensions/` is the removal of the unreachable nullish fallback in `edge/handlers/plugin/pending.ts`, and `tests/helpers/notification-boundary.ts` is unchanged. The summary records every plant this plan names,
its verbatim RED output, and the revert. The two positional rejections are proven to differ by the shape of the first token, and the pair is planted by swapping the sentences. The delegating case asserts the hermetic tree file listing is unchanged, which is the read-only proof. Every case asserts the fail-fast network entry point recorded zero calls.
  </acceptance_criteria>
  <done>P116-21 is contract-compliant, distinguishes its two rejection sentences, and is at 100 percent direct coverage with the unreachable fallback gone.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-21-A | Tampering | a read-only pending diff that must never reach the network nor write a file | high | mitigate | The network entry point is asserted uncalled by call count and the hermetic tree listing is asserted unchanged, and every rejection is proven to leave the workflow unreached. |
| T-116-21-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-21-C | Tampering | the production source of this pair, edited by this plan | high | mitigate | The pin still covers all three `shared.ts` helpers, `flag-catalog.ts`, and the shared boundary helper; the edit is confined to removing one unreachable fallback, is named in the plan before it is made, and both rejection sentences are asserted byte-identical by cases written before the edit. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/pending.test.ts extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/pending.test.ts extensions/pi-claude-marketplace/edge/handlers/plugin/pending.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/pending.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- The only production change is the removal of the unreachable nullish fallback in `edge/handlers/plugin/pending.ts`, with the exported signature of `makePendingHandler` and both rejection sentences unchanged; the shared boundary helper is untouched.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-21-SUMMARY.md when done.
</output>
