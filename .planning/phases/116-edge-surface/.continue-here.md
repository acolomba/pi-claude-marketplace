---
context: phase
phase: 116-edge-surface
task: 11
total_tasks: 31
status: executing
last_updated: 2026-09-02T20:30:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was measured in this working tree during this phase. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **A green test proves nothing until you make it fail.** Eleven plans in, D-116-04's
  mandatory plant has caught defects in ten of them that no gate and no review found. Plant every non-obvious
  proof: change what it pins, confirm RED, revert, record it. **A plant that stays GREEN is a
  finding** — narrow or strengthen the claim, never paper over it.
- [ ] CONSTRAINT: **`npm run check` never runs the tests.** The chain is
  `typecheck && lint && fallow && format:check && test && test:integration`, and `format:check` fails
  on PRE-EXISTING UNTRACKED operator files (`.mcp.json`, seven `.planning/research/.cache/*.json`).
  It short-circuits there. Run `npm run typecheck`, `npm run lint`, `npm run fallow`, `npm test`, and
  `npm run test:integration` SEPARATELY and check each exit code. Do NOT touch those files.
- [ ] CONSTRAINT: **`git commit` runs no hooks in this checkout.** `core.hooksPath` resolves
  elsewhere and has no `pre-commit`, so a successful commit is not evidence hooks passed. Run
  `pre-commit run --files <explicit paths>` by hand. Never `--all-files`, never `--no-verify`.
- [ ] CONSTRAINT: **This shell does not word-split.** `"$TH" filesystem $PATHS` passes ONE argument,
  scans nothing, and exits 0. Pass explicit literal paths and confirm trufflehog's reported
  `chunks`/`bytes` are non-zero. Backticks inside `git commit -m` EXECUTE — use `git commit -F`.
- [ ] CONSTRAINT: **Stage explicit literal paths only.** The operator edits unrelated files in this
  checkout concurrently. Never `git add -A`, never `git add .`. Confirm with `git status --short`
  and `git log -1 --stat` after every commit.

**Do not proceed until all boxes are checked.**

## Commit recipe (operator-approved 2026-09-02)

```bash
TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)
"$TH" filesystem <explicit literal paths> --results=verified,unknown --fail
npm exec -- prettier --check <explicit literal paths>
SKIP=trufflehog,npm-format-check pre-commit run --files <explicit literal paths>
```

`SKIP=` is widened to exactly those two hooks. `trufflehog` because this linked worktree breaks its
git-mode scan (the filesystem route above is the real check); `npm-format-check` because it runs the
repo-wide `format:check` that fails on the operator's untracked files (the per-file `prettier --check`
restores that coverage). This supersedes the earlier "do not extend SKIP beyond trufflehog".

## Measured findings — carry these forward, do NOT re-derive

| Finding | Consequence |
|---|---|
| **`strong-mock`'s `.times(0)` is inert.** It installs the stub, serves every call, and `verify()` reports clean. Confirmed independently. | Wave 1 fixed `tests/helpers/notification-boundary.ts` (`af7c501f`): a count of 0 now states no expectation at all. `createNotificationBoundary(1, 0)` and `(0, 0)` mean what they claim. ~20 D-116-06 proofs depended on this. |
| **Over-read failure shape.** An under-read is caught cleanly by `verifyBoundary()`. An over-read or unstated read returns strong-mock's pending-call **proxy function**, dying downstream as a raw `TypeError` — `ctx.ui.notify is not a function`, or `The "path" argument must be of type string. Received function`. | That error means a member you did not state, not a bug in the code under test. Do NOT state `ctx.ui` just to get a prettier diagnostic — it weakens a zero-emission silence proof (116-23 plant B). |
| **The probe count is PER-ORCHESTRATOR and cannot be inherited from the helper's docs OR from a sibling.** The helper documents "one probe per emission". 116-15 measured **4** on its delegating path (its orchestrator's context cascade runs two of its own); 116-16 measured **2** on the same kind of path (one cascade, one probe). | **MEASURE your probe count through a counting context BEFORE writing a line.** Naming the probe instead of the mistake would have hidden this. |
| **A NEGATIVE ASSERTION WHOSE SUBJECT CANNOT CHANGE IS VACUOUS** (116-16). Every earlier Group-C owner paired `verifyBoundary()` with an empty-footprint assertion. `fetch` PERSISTS NOTHING, so that assertion would pass whether or not the workflow ran — it proves the module, not the case. 116-16 REMOVED it rather than inherit it. | Before copying a sibling's negative, ask what would have to change for it to fail. If the answer is "nothing this module can do", delete it and say why. Same family as the identical-outcome tautology below. |
| **`strong-mock`'s `thenThrow` cannot supply a NON-`Error`** — `getError` normalizes it (116-15). And an orchestrator that catches its own throws leaves a handler's defence-in-depth catch unreachable by any ordinary route. | To force a failure the orchestrator would otherwise swallow, use a cast-free **`Proxy` over the boundary's own context** that throws on ONE named read (116-15 used the single `cwd` read). Every other Pi member still resolves through `createNotificationBoundary`, and `verifyBoundary()` still governs the emission — so the proof stays inside the sanctioned boundary rather than reaching around it. |
| **`toolProbes` is REQUIRED**, no default. | `0` on `notifyUsageError` paths (no soft-dep probe there); the real count on `notify()` paths. |
| **Two strong-mock facts** (measured by 116-12): two distinct empty mock proxies compare as **unequal**; an extra key set to `undefined` also mismatches. | Rely on these; do not re-probe. |
| **`createNotificationBoundary` returns `mock<ExtensionAPI>`, and reading a generic method off it as a VALUE is an `unbound-method` lint error** (116-27). | If a proof needs to read a boundary method rather than call it, that helper is the wrong tool. Say so; do not suppress the rule. |
| **The Group-C negative fires at the FIRST UNSTATED BOUNDARY MEMBER THE WORKFLOW REACHES AFTER ITS OWN ERROR HANDLING.** Three distinct diagnostics measured so far, all from the same plant. The phase's normative G5 excerpt says an emission "would be a second `ctx.ui` access past its `times(1)` count" — 116-10 measured that as wrong for `marketplace/list`, where the orchestrator reads `opts.cwd` inside `locationsFor` on its FIRST line and dies in `path.join` as `The "path" argument must be of type string. Received function`. **116-08 then measured 116-10's correction as ALSO not universal**: `setMarketplaceAutoupdate` wraps `flipOneScope` in a try/catch, CATCHES that same `ERR_INVALID_ARG_TYPE`, routes it to `notifyAutoupdateScopeFailure` (`orchestrators/marketplace/autoupdate.ts:499`), and only THERE makes the second `ctx.ui` access — dying as `ctx.ui.notify is not a function`. **116-09 then measured a THIRD**: `getMarketplaceInfo` runs no catch at all, so forwarding `ctx.cwd` dies as `ERR_INVALID_ARG_TYPE` at `persistence/locations.ts:145`, while a literal cwd runs to completion and dies on the emission count at `orchestrators/marketplace/info.ts:184`. 116-11 measured a FOURTH pair, same shape as `info` but landing at `orchestrators/marketplace/remove.ts:783`. | **Omitting `cwd` stays load-bearing — that is the constant.** WHICH diagnostic appears is a property of the orchestrator's error handling, not of the pattern. **No Group-C plan may promise its plant's diagnostic or stack trace before running it.** Run the plant, then write down what it actually said. Run BOTH variants (forwarding `ctx.cwd`, and with a literal working directory) — they differ. |
| **`parseCommandArgs` iterates `schema.positional.entries()` — the SCHEMA, not the input** (116-10). An empty positional schema performs zero iterations, so surplus tokens are silently **DROPPED**, never rejected. Measured: `official` and `official extra` produce identical output. | Any plan claiming "one above the accepted arity is rejected with a usage error" is FALSE against such a handler. Check the schema's arity first. Where the accepted arity is zero, there is also nothing "one below" it. |
| **`--local` is silently dropped by handlers that never call `extractLocalFlag`** (116-10). It reaches `parseArgs` as an ordinary token, lands on `positional`, and vanishes. `--scope user --local` lists the user scope alone. | Nothing is rejected. Do not author a `--local` rejection case for a handler that does not call `extractLocalFlag`. |
| **`--local` behaves DIFFERENTLY in every handler measured — FIVE outcomes from five siblings.** `list.ts` DROPS it (116-10). `update.ts` takes it as the MARKETPLACE NAME (116-13). `add.ts` ACCEPTS `--scope project --local` (116-07): `extractLocalFlag` consumes `--scope <value>` as a downstream-owned PAIR and filters only the scope-target token, so both members reach the workflow. `autoupdate.ts` also ACCEPTS it (116-08), flip landing in `claude-plugins.local.json`. `info.ts` puts it in the NAME position (116-09) — it never calls `extractLocalFlag` and declares one REQUIRED positional. `remove.ts` ACCEPTS it (116-11, third acceptance): it reaches `extractLocalFlag` through the shared opener, so `--scope user --local` honours both members. | **Never carry a `--local` claim across handlers — there is no default to assume.** Measure it on the module you own, every time. Six for six have differed from what their plan assumed. The discriminators are whether the module reaches `extractLocalFlag` (directly OR through the shared opener) and whether its positional is required. |
| **The arity `must_haves` truth has now been FALSE in FIVE plans running** (116-07, 116-08, 116-09, 116-10, 116-13) — but WHICH half fails depends on the schema. A single **optional** positional (116-08) means ZERO is also accepted, so there is no arity "one below" AND no rejection "one above" — both halves false. A single **required** positional (116-09) DOES reject zero, so the lower half HOLDS and only the surplus half fails. | **Read your schema's `required` field before writing the case.** The surplus half is false either way (see the schema-not-input rule below); the lower half is only false for an optional positional. |
| **HOW STRONG A PORT-FORWARD PROOF CAN BE DEPENDS ON THE MOCKING MECHANISM — measure yours, never copy a sibling's claim.** With strong-mock's `when()` (116-17): re-boxing the port (`gitOps: { ...deps.gitOps }`) stays **GREEN** because the bag is compared STRUCTURALLY, but wrapping ONE member (`clone: (o) => deps.gitOps.clone(o)`) goes **RED** — member identity IS provable. With a RECORDER fake (116-14): **both** the re-box AND the member-wrap stay GREEN; only replacing the implementation outright (`DEFAULT_GIT_OPS`) goes RED. | **State only what your mechanism measures.** `when()` → "every operation runs through the injected implementation, member by member". Recorder → only "the operation is CARRIED OUT BY the injected implementation" — not object identity, not member identity. 116-14 ran two green companion plants specifically to BOUND its claim rather than inherit the stronger one. Never add a second case for the container; that is one case run twice. |
| **A count derived by ARITHMETIC is a guess, not a measurement.** 116-05 reported `npm test` at `5041` as "5058 − 17 dropped cases", but it had also ADDED cases, so the subtraction was never valid. It propagated into this handoff and four dispatches before 116-17 measured directly and reported the conflict. The orchestrator then ran the suite: **5052/5052 across 291 suites**. | **Read the runner's `ℹ tests` line. Never compute a suite total from a delta.** The same rule this phase applies to coverage numbers applies to test counts. |
| **A DATA FIELD is invisible to every gate in this repo** (116-05, the most important finding of the phase). `allowMarketplaceOnly` is set per head by `pluginRefBranchConfig` — true for update/fetch/reinstall, false for the other five. It selects nothing at a plain cursor and it is **a field, not a branch**, so coverage cannot see it: **a wrong value for EVERY head would have passed the suite, typecheck, lint, fallow AND the 100-percent coverage gate.** | **Complete branch coverage is not complete proof.** Before calling an owner done, ask what the module reads that is a VALUE rather than a path — config records, lookup tables, per-mode flags, status sets — and pin each entry in a row table against a hand-authored expectation. 116-05 added an eight-row cursor table; flipping one head's flag turns exactly that head's row RED. Same class as the milestone's known optional-field silent-omission defect. |
| **"Assert the identical outcome" is a TAUTOLOGY TEMPLATE — treat any such phrasing as suspect** (116-11). Its plan asked for the scope-target flag "supplied and omitted, driven before and after the positional, asserting the identical outcome." Unfalsifiable against the real module: `removeMarketplace` reads `opts.local` in exactly ONE place (`orchestrators/marketplace/remove.ts:681`) to pick a PRECONDITION target, then the success write-back calls `cascadeRemoveFromLayer` on BOTH layers unconditionally (405-406). In a valid-config fixture the three forms are byte-identical, so the case passes whatever the module does. | Find the fixture where the flag actually DISCRIMINATES — 116-11 used a workspace whose `claude-plugins.local.json` fails schema validation, where supplying the flag aborts the removal and omitting it does not. Its plant then turned exactly the two flag-supplied rows RED, and would have left the plan's version fully GREEN. **A case asserting two inputs agree proves nothing unless you show an input where they disagree.** |
| **A plant that leaves a case GREEN is an OBSERVATION to record with its reason, not a failure to hide** (116-09 plant B). Swapping the delegate left the scope-target case green because both workflows emit the same `{not added}` row for an absent name — that case's claim is about where the flag LANDS, and a sibling plant carries its scope half. | Record which plant carries which half of a case's claim. A green plant means you found the wrong discriminator, not that the case is worthless. |
| **Delegation is observable as the CONFIG footprint** (116-08, SPLIT-01): a flip never writes `state.json`; it lands in `claude-plugins.json` or `claude-plugins.local.json`. | Pair this with 116-07's scope footprint. Together they are how a seamless handler's effect is measured without a seam. |
| **Scope is observable as an ON-DISK FOOTPRINT, not through the options bag** (116-07). Which scope root holds `state.json`, and whether the write-back landed in `claude-plugins.json` or `claude-plugins.local.json`. | That is what turns a scope-target flag's "present only when supplied" into a measurement rather than a restatement. A notification row can be identical either way. |
| **`grep -c` on a forward site counts HEADER COMMENTS as call sites** (116-07: `grep -c deps.gitOps` reported 2, one was a comment, so a single plant WAS in scope). | Count real call sites, not matches, before deciding how many plants a port needs. The 116-13 two-plant rule applies to genuine multi-site forwards only. |
| **A clone recorder cannot hold a hand-authored directory literal** (116-07) — the guard stages into `sourcesStagingDir(randomUUID())`. | Substitute a token only for a UUID leaf UNDER the expected scope's staging root, so the substitution still carries information: a clone into the wrong scope root then fails on its raw directory. |
| **A port forwarded from two call sites needs two plants, not one** (116-13). Removing one arm's forward leaves the other arm's cases green, so a single plant is under-scoped and passes while proving less than it claims. | Count the forward sites first, then plant each separately. |
| **A github-source workflow cannot be driven through a bare `createGitOpsFake` at all** (116-14, unavoidable there because the source is hard-coded): `DOMException [DataCloneError]: (host) => credentialFill(host, runGitCredential) could not be cloned` at `git-ops-fake.ts:129`. | Have the port DROP that downstream-owned bundle and delegate every operation to the fake. |
| **A github fixture source attaches a `GitAuthBundle` whose functions make `createGitOpsFake`'s `structuredClone` recorder THROW** (116-13). | Seed url sources on a provider-less host instead — same git sequence, authless. |
| **Branch-coverage numbers are NOT a property of the source.** V8 emits a branch range only when its count diverges from its enclosing block, so strengthening a suite raises numerator AND denominator together. 116-02 authored `25/26`, measured `28/29`; 116-26 derived `13/14`, measured `14/15`. Nothing regressed in either. | **Never pin an absolute branch pair.** D-116-01a was amended to pin the shortfall's IDENTITY: an `Incomplete direct coverage for` verdict, denominator minus numerator exactly 1, and the exact uncovered line set. Line counts ARE stable and stay pinned. |
| **An uninstantiated generic type query silently widens a proof.** `Parameters<typeof f>[N]` collapses a generic `P` to `unknown`. | Use the instantiated form: `Parameters<typeof f<ConcreteType>>[N]`. |
| **Do not derive a test input from the same call the module under test derives it from.** | Tautological — stays green whatever the module does. 116-23 caught this in its own plan. |
| **A `! rg -n '<pattern>'` gate matches comments, not just code.** | Do not write a header sentence containing a token your own verify block forbids (116-30). |
| **A clean `tsc --noEmit` globally proves every `@ts-expect-error` binds**, because an unattached one raises TS2578. | Relevant to any future type-only owner. |

## The pattern that matters most

**Twenty-two of twenty-three executed plans found a defect in their own plan**, and twenty-one were proofs the plan
*specified* that could not have failed: a tautological catalog-driven input; a case that was one case
run twice; a `must_haves` promise with no reachable target; a named verb returning `[]`; an
anti-shadowing invariant that is FALSE against the real module (116-29 — four names, `list`/`ls`/
`info`/`update`, are in both exported vocabularies by design); and a duplicate-entry assertion whose
only self-contained form transforms an expectation from an actual, which the task's own acceptance
criteria forbid.

The plan-checker passed all 31 plans clean — those defects are invisible without executing against
the real module.

**Never implement a plan-specified case you can see cannot fail.** Strengthen or narrow it, and
report it. Every executor so far has done this correctly.

## Tooling defects — expect these, repair by hand

- **`roadmap.update-plan-progress` mangles ROADMAP.md every single time** (eleven for eleven): it injects
  a duplicate bare 31-line plan list beside the descriptive one and collapses the progress-table row
  alignment. Diff and hand-repair before staging.
- **`state.advance-plan` is unreliable on the position line** — it increments blindly and sometimes
  reverts to a bare ordinal, which reads as though plans completed in roadmap order. STATE.md's
  Current Position NAMES the completed plans; keep that shape and append yours.
- **`state.update-progress` writes nothing**, returning `"phase scope is unscoped, not complete"`.
- **Git stores no rename — it DETECTS one by similarity** (116-17). A move-plus-total-rewrite is not a
  rename at any threshold (`git diff --cached -C -M10% --summary` printed a bare delete + create), so a
  plan demanding both "one commit" and "`git status` shows a rename" is self-contradictory. Split it:
  a pure move commit, then the rewrite. `git log --follow` then works.
- **`state.record-metric` DOUBLE-INCREMENTS `completed_plans`** (116-05 measured 193 → 194 → 195 for one
  plan). Check the value after calling it and correct by hand.
- **ROADMAP carries the plan count in TWO places** — a prose line and the progress table — and they
  drift independently. 116-05 found the prose stale at 17/31 while the table read 18/31. Fix both.
- **`state.add-decision`** rejects `--summary-file` paths outside the repo root, and has
  double-prefixed `[Phase 116]:` on entries.
- **`phase.complete` cannot write the root planning files** while workstream mode is active and
  neither workstream holds v1.19. Hand-apply the transition, as `92c95690` and `3465e8c3` did.

## Current state

**Waves 1-4 are ALL CLOSED. 23 of 31 plans complete**: 116-00 through 116-17, plus 116-23,
116-26, 116-27, 116-29, 116-30. typecheck/lint/fallow all 0. WINDOWS ledger: 13 open, 18 total.

**FOUR distinct parser/arity/flag combinations measured so far — there is no bucket to fall into.**

| | zero positionals | surplus positional | `--scope X --local` |
|---|---|---|---|
| `parseCommandArgs` + optional (6 marketplace) | accepted | **dropped** | four different answers |
| `parseArgs` (116-14 `bootstrap`, 116-17 `import`) | none below zero | **rejected** in shared opener | ordinary positional token |
| `parseCommandArgs` + required (116-15 `enable-disable`) | **rejected** | dropped | accepted, both honoured |
| `parseArgs` + own guards (116-16 `fetch`) | accepted (it IS the all form) | **rejected by the handler's own guard** | **rejected**, `Unknown flag: "--local".` |

`--local` has now produced **eight** distinct outcomes. 116-16 is the FIRST time the inherited
"mutually exclusive selectors are rejected" truth actually held. **Measure your own module — every
single owner that assumed has been wrong.**

**Everything left is wave 5 (ten more plugin handlers) plus 116-28 (register).**

**`npm test` baseline is 5070/5070 across 293 suites** (orchestrator measured 5052 at 116-17's
close; 116-14, 116-15 and 116-16 added 2, 7 and 9, each READ from the runner's `ℹ tests` line). `test:integration` 31/31. Earlier figures in
this file (5041, 5044) were wrong; see the arithmetic-versus-measurement row above.

**The whole marketplace tier is DONE** (116-07 … 116-13). Its six handlers measured the same three
plan claims and found all three wrong or module-specific EVERY time — see the `--local`, arity and
Group-C-negative rows above. The eleven plugin handlers in wave 5 inherit the same three questions.

**Naming trap, already fixed once:** a non-plan report named `*-SUMMARY.md` in the phase directory is
COUNTED as a plan summary by `find-phase`, `phase-plan-index` and `progress.bar`. The D-116-01a pin
report was named that way and inflated the phase to 17-of-16 until it was renamed to
`116-D116-01A-PINS-REPORT.md`. **Never name a non-plan artifact `*-SUMMARY.md`.**

**116-08 is the normative Group-C reference.** The nine remaining seamless owners (116-15, 116-16,
116-18, 116-19, 116-20, 116-21, 116-22, 116-24, 116-25) copy its shape: boundary sized at one emission
and zero probes with `cwd` OMITTED, both scopes seeded, the whole notification list compared,
`verifyBoundary()` last, plus an on-disk negative. Read `116-08-SUMMARY.md` before writing one.

**D-116-14 is fully discharged** (116-27). All four `tools.ts` switches planted, all four RED, all
reverted: `projectRowStatus`/`statusLabel`/`statusKey` raise **TS2366**, `pluginVersion` raises
**TS7030**. Research's "compiles clean" premise for the fourth is disproved. No later plan carries an
exhaustiveness obligation.

**Both production licences are now spent.** 116-06 (`flag-catalog.ts`) and 116-27 (`tools.ts`) are
done. **No remaining plan may touch a production file.** If a branch cannot be reached without one,
that is a finding to report, not a licence to edit.

**Remaining, in DAG-recomputed wave order** (the plans declare 8 waves; `phase-plan-index` recomputes
6 from `depends_on`. Both satisfy every hard dependency, and execution is sequential either way
because `workflow.use_worktrees=false`. The autonomous run follows the recomputed order, which is why
116-27 and 116-29 — declared wave 6, each depending on 116-00 alone — ran first):

- **Wave 3 is CLOSED** — 116-03, 116-10, 116-13 all done.
- **Wave 4 is CLOSED.**
- **Wave 5** — 116-18, 116-19, 116-20, 116-21, 116-22, 116-24, 116-25 remain. All
  nine are seamless **Group C** (`createNotificationBoundary(1, 0)` + `verifyBoundary()`, exact-argument
  gap recorded in `must_haves`). **116-21 is also a D-116-01a claimant and MUST pin
  `plugin/pending.ts:39`.** 116-14 and 116-17 are DONE.
- **Wave 6** — 116-28 (register)

## Locked operator decisions — do not reopen

- **D-116-05 = O3.** Handlers with a real seam (`plugin/import.ts`) or an injected port
  (`marketplace/add.ts`, `marketplace/update.ts`, `plugin/bootstrap.ts`) plus the three `shared.ts`
  helpers get exact-argument mocks. The thirteen seamless handlers get complete edge-side proof plus
  the D-116-06 negative that the orchestrator was never called, via `createNotificationBoundary(1, 0)`
  + `verifyBoundary()`. Each Group-C plan records the exact-argument gap in `must_haves`.
- **D-116-01a**: the identity pin above, no coverage-exception pragma ever. **The four-claimant list
  is SUPERSEDED — see the amendment section below.** Seven claimants: 116-02, 116-21, 116-26 and
  **116-17 (PINNED** — `plugin/import.ts:31`, compiler-forced by the `unknown`-typed catch binding,
  ledger entry 18), plus 116-03, 116-05 and 116-13 found by measurement. Three remain unexecuted:
  116-21 is still ahead in wave 5.
- **Exactly two plans could touch production: 116-06 (`flag-catalog.ts`) and 116-27 (`tools.ts`).
  BOTH ARE DONE — the licence is spent and no remaining plan may edit a production file.** This
  changed twice: the earlier "only 116-27" invariant was superseded mid-phase by adding 116-06, and
  116-27's own edit went one step past its plan's stated scope (see below).
- **116-27 made a second production edit inside its licensed file**, beyond its plan's "only the
  parameter type and the arms the compiler removed" wording: the unreachable `String(err)` side of an
  inline `err instanceof Error ? err.message : String(err)` now calls `shared/errors.ts`'s
  `errorMessage(err)` (byte-identical body, proven by its own pair). Contained to `tools.ts`, recorded
  in that SUMMARY, and flagged to the operator. Do not extend it further.
- **D-116-14 is scoped to 116-27 alone.** Only the four `tools.ts` switches turn on a closed union.
  `router.ts` switches on open `string` with a `default` arm — no exhaustiveness claim exists there.
  Note: a missing arm raises **TS7030** under `noImplicitReturns`, not TS2366 as research first
  claimed; research was corrected in five places.
- **`BOOLEAN_FLAGS` stays** in `edge/handlers/plugin/list.ts`; 116-20 records it as an observation
  for Phase 117 and takes no action.

## D-116-01a IS AMENDED — the claimant list is now open to measurement

**Operator decision, 2026-09-02.** The locked four-claimant list is superseded. **Any pair that
MEASURES an unreachable branch becomes a claimant and MUST pin the shortfall identity** — it may no
longer report it as prose. Full text is in `116-CONTEXT.md` under D-116-01a; the short form:

- Pin the IDENTITY, never an absolute branch pair: an `Incomplete direct coverage for <source>:`
  verdict with branch numbers matched loosely, denominator minus numerator exactly 1, and the exact
  uncovered line set. Lines and functions stay pinned as measured.
- `must_haves` names the exact line range, states the branch is unreachable at runtime, and gives
  the REASON — the compiler setting where one forces it, otherwise the structural reason.
- Proof by MEASUREMENT, not inspection: a plant that stays GREEN plus one independent route
  (construction, or brute force over the reachable input space).
- **No coverage-exception pragma, ever.** That ban is untouched.
- File it in `.planning/WINDOWS.md` too, so the phase boundary sweeps them as one set.
- A branch you merely failed to reach is a GAP, not a claim. The plant is what separates them.

Claimants now: the original four (116-02, 116-17, 116-21, 116-26) plus three found by measurement —
**116-03** (`completions/data.ts:188`, ledger entry 16, COMPILER-FORCED by `Array.prototype.at()`'s
`T | undefined` typing), **116-13** (`marketplace/update.ts:41`, ledger entry 15, STRUCTURAL: dead here
because this schema's positional is optional, live for siblings declaring a required one), and
**116-05** (`completions/provider.ts:125`, ledger entry 17, STRUCTURAL: the `description === undefined`
arm of `optionalDescription`, unreachable because every entry `flagCompletions` builds carries a
description — both producers are a written-out literal and `completionFlagEntries` over a `FlagEntry`
whose `description` is required).

**The amendment is doing real work: two of the three are STRUCTURAL**, which the original
compiler-forced-only wording could not have admitted. 116-03 and 116-13 were reopened at `ed0e490f` to
add pins they had reported as prose under the old rule.

Two stale production comments were also found and NOT acted on (no licence):
`data.ts:1-12` advertises a `getScopeCompletions` export that does not exist, and `data.ts:361` says
`--partial` "widens" the install set when it in fact SHIFTS it (`INSTALL_STATUSES` is
`{available, remote}`; `PARTIAL_INSTALL_STATUSES` is `{available, partially-available}`) — stale since
`remote` joined.

## Deferred to the phase boundary — do NOT sweep mid-flight

- The `173/204` pair total in ROADMAP.md's Total row and STATE.md's prose has not moved as pairs
  completed. It wants one deliberate sweep when wave 8 closes.
- The `REQUIREMENTS.md` per-pair Status column lapsed at Phase 110; 115 rows across Phases 110-114
  still read `Open` despite all five being verified complete. Awaiting an operator decision.
- `MOD-09` stays `Pending` — it is shared by every plan in this phase and closes only at phase end.

## Orchestration notes

- `ISOLATION=none` (`workflow.use_worktrees=false`): executors run SEQUENTIALLY on the shared tree,
  one plan at a time. Never dispatch two at once.
- Dispatch `gsd-executor` with `model="opus"`. **Put
  `.claude/gsd-core/workflows/execute-plan.md` first in `<required_reading>`** — it is NOT in the
  agent definition, unlike `checkpoints.md`, `executor-examples.md`, and `summary.md`, which the
  definition already `@`-includes.
- Feed each executor the findings table above. Rediscovery is the main cost of a context reset.
