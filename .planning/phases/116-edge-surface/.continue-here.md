---
context: phase
phase: 116-edge-surface
task: 11
total_tasks: 31
status: executing
last_updated: 2026-09-02T20:30:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was measured in this working tree during this phase. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **A green test proves nothing until you make it fail.** Eleven plans in, D-116-04's
  mandatory plant has caught defects in ten of them that no gate and no review found. Plant every non-obvious
  proof: change what it pins, confirm RED, revert, record it. **A plant that stays GREEN is a
  finding** — narrow or strengthen the claim, never paper over it.
- [ ] CONSTRAINT: **`npm run check` never runs the tests.** The chain is
  `typecheck && lint && fallow && format:check && test && test:integration`, and `format:check` fails
  on PRE-EXISTING UNTRACKED operator files (`.mcp.json`, seven `.planning/research/.cache/*.json`).
  It short-circuits there. Run `npm run typecheck`, `npm run lint`, `npm run fallow`, `npm test`, and
  `npm run test:integration` SEPARATELY and check each exit code. Do NOT touch those files.
- [ ] CONSTRAINT: **`git commit` runs no hooks in this checkout.** `core.hooksPath` resolves
  elsewhere and has no `pre-commit`, so a successful commit is not evidence hooks passed. Run
  `pre-commit run --files <explicit paths>` by hand. Never `--all-files`, never `--no-verify`.
- [ ] CONSTRAINT: **This shell does not word-split.** `"$TH" filesystem $PATHS` passes ONE argument,
  scans nothing, and exits 0. Pass explicit literal paths and confirm trufflehog's reported
  `chunks`/`bytes` are non-zero. Backticks inside `git commit -m` EXECUTE — use `git commit -F`.
- [ ] CONSTRAINT: **Stage explicit literal paths only.** The operator edits unrelated files in this
  checkout concurrently. Never `git add -A`, never `git add .`. Confirm with `git status --short`
  and `git log -1 --stat` after every commit.

**Do not proceed until all boxes are checked.**

## Commit recipe (operator-approved 2026-09-02)

```bash
TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)
"$TH" filesystem <explicit literal paths> --results=verified,unknown --fail
npm exec -- prettier --check <explicit literal paths>
SKIP=trufflehog,npm-format-check pre-commit run --files <explicit literal paths>
```

`SKIP=` is widened to exactly those two hooks. `trufflehog` because this linked worktree breaks its
git-mode scan (the filesystem route above is the real check); `npm-format-check` because it runs the
repo-wide `format:check` that fails on the operator's untracked files (the per-file `prettier --check`
restores that coverage). This supersedes the earlier "do not extend SKIP beyond trufflehog".

## Measured findings — carry these forward, do NOT re-derive

| Finding | Consequence |
|---|---|
| **`strong-mock`'s `.times(0)` is inert.** It installs the stub, serves every call, and `verify()` reports clean. Confirmed independently. | Wave 1 fixed `tests/helpers/notification-boundary.ts` (`af7c501f`): a count of 0 now states no expectation at all. `createNotificationBoundary(1, 0)` and `(0, 0)` mean what they claim. ~20 D-116-06 proofs depended on this. |
| **Over-read failure shape.** An under-read is caught cleanly by `verifyBoundary()`. An over-read or unstated read returns strong-mock's pending-call **proxy function**, dying downstream as a raw `TypeError` — `ctx.ui.notify is not a function`, or `The "path" argument must be of type string. Received function`. | That error means a member you did not state, not a bug in the code under test. Do NOT state `ctx.ui` just to get a prettier diagnostic — it weakens a zero-emission silence proof (116-23 plant B). |
| **`toolProbes` is REQUIRED**, no default. | `0` on `notifyUsageError` paths (no soft-dep probe there); the real count on `notify()` paths. |
| **Two strong-mock facts** (measured by 116-12): two distinct empty mock proxies compare as **unequal**; an extra key set to `undefined` also mismatches. | Rely on these; do not re-probe. |
| **`createNotificationBoundary` returns `mock<ExtensionAPI>`, and reading a generic method off it as a VALUE is an `unbound-method` lint error** (116-27). | If a proof needs to read a boundary method rather than call it, that helper is the wrong tool. Say so; do not suppress the rule. |
| **Branch-coverage numbers are NOT a property of the source.** V8 emits a branch range only when its count diverges from its enclosing block, so strengthening a suite raises numerator AND denominator together. 116-02 authored `25/26`, measured `28/29`; 116-26 derived `13/14`, measured `14/15`. Nothing regressed in either. | **Never pin an absolute branch pair.** D-116-01a was amended to pin the shortfall's IDENTITY: an `Incomplete direct coverage for` verdict, denominator minus numerator exactly 1, and the exact uncovered line set. Line counts ARE stable and stay pinned. |
| **An uninstantiated generic type query silently widens a proof.** `Parameters<typeof f>[N]` collapses a generic `P` to `unknown`. | Use the instantiated form: `Parameters<typeof f<ConcreteType>>[N]`. |
| **Do not derive a test input from the same call the module under test derives it from.** | Tautological — stays green whatever the module does. 116-23 caught this in its own plan. |
| **A `! rg -n '<pattern>'` gate matches comments, not just code.** | Do not write a header sentence containing a token your own verify block forbids (116-30). |
| **A clean `tsc --noEmit` globally proves every `@ts-expect-error` binds**, because an unattached one raises TS2578. | Relevant to any future type-only owner. |

## The pattern that matters most

**Ten of eleven executed plans found a defect in their own plan**, and six were proofs the plan
*specified* that could not have failed: a tautological catalog-driven input; a case that was one case
run twice; a `must_haves` promise with no reachable target; a named verb returning `[]`; an
anti-shadowing invariant that is FALSE against the real module (116-29 — four names, `list`/`ls`/
`info`/`update`, are in both exported vocabularies by design); and a duplicate-entry assertion whose
only self-contained form transforms an expectation from an actual, which the task's own acceptance
criteria forbid.

The plan-checker passed all 31 plans clean — those defects are invisible without executing against
the real module.

**Never implement a plan-specified case you can see cannot fail.** Strengthen or narrow it, and
report it. Every executor so far has done this correctly.

## Tooling defects — expect these, repair by hand

- **`roadmap.update-plan-progress` mangles ROADMAP.md every single time** (eleven for eleven): it injects
  a duplicate bare 31-line plan list beside the descriptive one and collapses the progress-table row
  alignment. Diff and hand-repair before staging.
- **`state.advance-plan` is unreliable on the position line** — it increments blindly and sometimes
  reverts to a bare ordinal, which reads as though plans completed in roadmap order. STATE.md's
  Current Position NAMES the completed plans; keep that shape and append yours.
- **`state.update-progress` writes nothing**, returning `"phase scope is unscoped, not complete"`.
- **`state.add-decision`** rejects `--summary-file` paths outside the repo root, and has
  double-prefixed `[Phase 116]:` on entries.
- **`phase.complete` cannot write the root planning files** while workstream mode is active and
  neither workstream holds v1.19. Hand-apply the transition, as `92c95690` and `3465e8c3` did.

## Current state

12 of 31 plans complete: **116-00, 116-01, 116-02, 116-03, 116-04, 116-06, 116-12, 116-23, 116-26,
116-27, 116-29, 116-30**. Correspondence gate **14 → 10**; four of this phase's seven violations closed.
`npm test` at 5037/5037 across 291 suites, `test:integration` 31/31, typecheck/lint/fallow all 0.

**D-116-14 is fully discharged** (116-27). All four `tools.ts` switches planted, all four RED, all
reverted: `projectRowStatus`/`statusLabel`/`statusKey` raise **TS2366**, `pluginVersion` raises
**TS7030**. Research's "compiles clean" premise for the fourth is disproved. No later plan carries an
exhaustiveness obligation.

**Both production licences are now spent.** 116-06 (`flag-catalog.ts`) and 116-27 (`tools.ts`) are
done. **No remaining plan may touch a production file.** If a branch cannot be reached without one,
that is a finding to report, not a licence to edit.

**Remaining, in DAG-recomputed wave order** (the plans declare 8 waves; `phase-plan-index` recomputes
6 from `depends_on`. Both satisfy every hard dependency, and execution is sequential either way
because `workflow.use_worktrees=false`. The autonomous run follows the recomputed order, which is why
116-27 and 116-29 — declared wave 6, each depending on 116-00 alone — ran first):

- **Wave 3** — 116-10, 116-13 (marketplace list/update). 116-03 (completions/data) is DONE.
- **Wave 4** — 116-05 (completions/provider), 116-07, 116-08, 116-09, 116-11 (marketplace add,
  autoupdate, info, remove)
- **Wave 5** — 116-14 … 116-22, 116-24, 116-25 (plugin handlers; **116-17 carries the atomic `git mv`**)
- **Wave 6** — 116-28 (register)

## Locked operator decisions — do not reopen

- **D-116-05 = O3.** Handlers with a real seam (`plugin/import.ts`) or an injected port
  (`marketplace/add.ts`, `marketplace/update.ts`, `plugin/bootstrap.ts`) plus the three `shared.ts`
  helpers get exact-argument mocks. The thirteen seamless handlers get complete edge-side proof plus
  the D-116-06 negative that the orchestrator was never called, via `createNotificationBoundary(1, 0)`
  + `verifyBoundary()`. Each Group-C plan records the exact-argument gap in `must_haves`.
- **D-116-01a**, as amended: exactly four claimants (116-02, 116-17, 116-21, 116-26), the identity
  pin above, no coverage-exception pragma ever.
- **Exactly two plans could touch production: 116-06 (`flag-catalog.ts`) and 116-27 (`tools.ts`).
  BOTH ARE DONE — the licence is spent and no remaining plan may edit a production file.** This
  changed twice: the earlier "only 116-27" invariant was superseded mid-phase by adding 116-06, and
  116-27's own edit went one step past its plan's stated scope (see below).
- **116-27 made a second production edit inside its licensed file**, beyond its plan's "only the
  parameter type and the arms the compiler removed" wording: the unreachable `String(err)` side of an
  inline `err instanceof Error ? err.message : String(err)` now calls `shared/errors.ts`'s
  `errorMessage(err)` (byte-identical body, proven by its own pair). Contained to `tools.ts`, recorded
  in that SUMMARY, and flagged to the operator. Do not extend it further.
- **D-116-14 is scoped to 116-27 alone.** Only the four `tools.ts` switches turn on a closed union.
  `router.ts` switches on open `string` with a `default` arm — no exhaustiveness claim exists there.
  Note: a missing arm raises **TS7030** under `noImplicitReturns`, not TS2366 as research first
  claimed; research was corrected in five places.
- **`BOOLEAN_FLAGS` stays** in `edge/handlers/plugin/list.ts`; 116-20 records it as an observation
  for Phase 117 and takes no action.

## OPEN OPERATOR DECISION — raise before the verification gate

**A fifth D-116-01a-class unreachable branch exists, outside the locked four-claimant list.**
`extensions/pi-claude-marketplace/edge/completions/data.ts:188` — the right-hand side of
`allTokens.at(-1) ?? ""`. 116-03 proved it unreachable three ways: by construction, by a brute-force
probe over all 65,536 BMP code points in five input shapes (0 hits), and by a plant that left all 66
cases green. The pair therefore stands at **branches 109/110**, functions 31/31, lines 610/610.

116-03 is not a D-116-01a claimant and both production licences are spent, so it was **reported as a
finding rather than pinned or excepted** — the reversible default. Three ways to close it:

1. Amend D-116-01a to five claimants and have 116-03 pin the shortfall identity (an
   `Incomplete direct coverage for` verdict, denominator minus numerator exactly 1, and the exact
   uncovered line set — never an absolute branch pair).
2. Leave it as a recorded finding and let the phase verification account for one pair at 109/110.
3. Hand it to Phase 117 as a production rewrite (`compiler-forced-unreachable-branches`, D-116-01a).

Same class as the four already accepted. **This needs an answer before the phase verification gate,
not before the next plan.**

Two stale production comments were also found and NOT acted on (no licence):
`data.ts:1-12` advertises a `getScopeCompletions` export that does not exist, and `data.ts:361` says
`--partial` "widens" the install set when it in fact SHIFTS it (`INSTALL_STATUSES` is
`{available, remote}`; `PARTIAL_INSTALL_STATUSES` is `{available, partially-available}`) — stale since
`remote` joined.

## Deferred to the phase boundary — do NOT sweep mid-flight

- The `173/204` pair total in ROADMAP.md's Total row and STATE.md's prose has not moved as pairs
  completed. It wants one deliberate sweep when wave 8 closes.
- The `REQUIREMENTS.md` per-pair Status column lapsed at Phase 110; 115 rows across Phases 110-114
  still read `Open` despite all five being verified complete. Awaiting an operator decision.
- `MOD-09` stays `Pending` — it is shared by every plan in this phase and closes only at phase end.

## Orchestration notes

- `ISOLATION=none` (`workflow.use_worktrees=false`): executors run SEQUENTIALLY on the shared tree,
  one plan at a time. Never dispatch two at once.
- Dispatch `gsd-executor` with `model="opus"`. **Put
  `.claude/gsd-core/workflows/execute-plan.md` first in `<required_reading>`** — it is NOT in the
  agent definition, unlike `checkpoints.md`, `executor-examples.md`, and `summary.md`, which the
  definition already `@`-includes.
- Feed each executor the findings table above. Rediscovery is the main cost of a context reset.
