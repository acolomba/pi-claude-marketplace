---
phase: 112-hook-runtime
plan: 02
type: execute
wave: 5
depends_on: [112-01, 112-03, 112-09, 112-10, 112-15, 112-16, 112-17, 112-18, 112-19, 112-20, 112-21, 112-22, 112-23, 112-24, 112-25, 112-27, 112-29, 112-30]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  - tests/bridges/hooks/async-rewake/registry.test.ts
  - tests/architecture/hooks-async-rewake.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-02 owns the complete async registry lifecycle; every baseline and supplemental case is re-proved rather than inherited (D-01, D-06)."
    - "Spawn, stream, timer, persistence, exit, notification, orphan, multi-child, and reload outcomes use complete boundaries and case-owned state (D-04, D-05, D-07-D-13, D-18-D-21)."
    - "Within each stdout and stderr stream order is stable, while no fabricated total order between the two streams is asserted (D-10)."
    - "CodeGraph-confirmed test-only readers `asyncRewakeEntries` and `awaitPidTablePersist` are removed, and tests observe public effects/filesystem lifecycle instead of internal cells (D-05, D-18)."
  artifacts:
    - path: tests/bridges/hooks/async-rewake/registry.test.ts
      provides: "New canonical direct owner of async-rewake/registry.ts"
    - path: tests/architecture/hooks-async-rewake.test.ts
      provides: "Pruned cross-lane env-parity and bridge-reload contracts only"
  key_links:
    - from: tests/bridges/hooks/async-rewake/registry.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
      via: "direct spawnAndRegister, shutdown, and orphan-reap public effects"
      pattern: "spawnAndRegister|shutdownInMemoryChildren|reapOrphans"
---

<objective>
Create the canonical async-rewake registry owner and reduce its supplemental suite to genuine cross-module contracts.

Purpose: MOD-05 needs exact background-child lifecycle proof without unsafe PIDs, hidden readers, or competing contract authorities.
Output: A complete direct owner, removal of two test-only exports, and one narrowly retained supplemental suite.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one background child from spawn through cleanup</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, tests/bridges/hooks/async-rewake/registry.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    - tests/bridges/hooks/async-rewake/registry.test.ts
    - tests/architecture/hooks-async-rewake.test.ts
    - tests/platform/credential-process-fake.ts
    - tests/platform/git-credential.test.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing direct owner. Use the existing `SpawnDeps.spawnImpl` and a narrow EventEmitter child with real PassThrough stdin/stdout/stderr. Per D-12, use real Node PassThrough streams inside process doubles for buffering, UTF-8 decoding, end/flush, and listener lifecycle; reserve custom emitters only for event orderings real streams cannot stage. Prove one dispatchable event end to end: exact command/args/shell/cwd/env, absent optional async stdin after delivery, marker `PI_CLAUDE_MARKETPLACE_REWAKE_DISPATCH`, listener registration, PID snapshot, within-stream output, exit-code-2 rewake, idle `nextTurn`, summary destination, timer cancellation, listener removal, PID unlink, and registry shutdown. Each case owns its streams, root, entry, epoch, Pi/session probes, timers, and cleanup. Apply D-01, D-02, D-04, D-05, D-07-D-13, and D-18-D-21. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports registry.ts and one full spawn-to-rewake path passes with exact boundary values, public outcome, PID bytes, signals/listeners/timers, and cleanup; PassThrough owns byte behavior; no live arbitrary PID or hidden reader is used.</acceptance_criteria>
  <done>The async registry has one production-quality background lifecycle under direct ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Complete registry interleavings, orphan safety, and supplemental pruning</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, tests/bridges/hooks/async-rewake/registry.test.ts, tests/architecture/hooks-async-rewake.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    - tests/bridges/hooks/async-rewake/registry.test.ts
    - tests/architecture/hooks-async-rewake.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts
    - extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
    - tests/shared/completion-cache.test.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add independent cases for non-dispatch skip, spawn throw, missing PID kill, child error plus late terminal duplicate guard, every exit result, stale epoch, stderr-over-stdout selection, truncation framing `[…truncated]\n` and `\n\n`, send failure, busy `followUp`, normal/error/exit-before-pipe-close/interleaved event sequences, same-scope and cross-scope live children, one settlement while another persists, registration/removal snapshots, reload shutdown, liveness failure, exact Linux marker match/mismatch/read failure, non-Linux soft skip, kill failure, and final unlink. Preserve only per-stream order. Use injected `OrphanProbes`, never a real arbitrary PID. Remove CodeGraph-confirmed no-production-caller exports `asyncRewakeEntries` and `awaitPidTablePersist`; replace their test observations with public send/notify effects, scoped PID files, process signals, and awaited shutdown. Move all single-module cases from `hooks-async-rewake.test.ts` into the owner and retain there only named cross-lane environment parity and bridge-reload lifecycle cases per D-06. Keep exact semantic diagnostics without incidental punctuation per D-13. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/registry.test.ts &amp;&amp; node --test tests/architecture/hooks-async-rewake.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/async-rewake/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>The owner reports 100 percent direct function, line, and branch coverage; every listed child ordering, result, stream, registry, persistence, orphan, scope, and cleanup partition has complete independent evidence; the two test-only exports are absent with no production caller break; the supplemental suite names and tests only cross-lane env parity and bridge-reload lifecycle; all commands pass.</acceptance_criteria>
  <done>P112-02 is direct, safe against PID reuse and hung output, and the sole supplemental carrier is pruned.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; an unmitigated high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| plugin hook declaration → background child | Untrusted command/env/stdin starts an OS process. |
| persisted PID row → OS signal | Stored process identity can target a live PID only after ownership proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-03 | Denial of service / Elevation | reapOrphans | high | mitigate | Require liveness and exact Linux marker equality; soft-skip mismatch/read failure/non-Linux and use injected probes only. |
| T-112-04 | Denial of service | spawnAndRegister | high | mitigate | Preserve independent caps, timer ladder, terminal idempotence, listener removal, signals, and full cleanup. |
| T-112-05 | Information disclosure | async env/session registry | high | mitigate | Assert exact env/session precedence and case-local restoration; shut down all live children and unlink scoped state. |

Canon process, PID, and environment risks remain in STRIDE; no bespoke values/safety/ethics prohibition survives the precision pass.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/async-rewake/registry.test.ts` as the sole mirrored owner.
- Removes production exports `asyncRewakeEntries` and `awaitPidTablePersist`; creates no replacement reader or symbol.
- Refines `tests/architecture/hooks-async-rewake.test.ts` to two genuine cross-module contracts.
- Creates `.planning/phases/112-hook-runtime/112-02-PLAN.md` and, after execution, `112-02-SUMMARY.md`.

<verification>
Run the owner, retained supplemental suite, typecheck, and direct coverage. The owner must reach 100 percent and leave no child, stream, timer, listener, PID row, session, or registry state behind.
</verification>

<success_criteria>
- P112-02 owns exactly registry.ts and its mirrored test.
- Process, stream, persistence, notification, orphan, and reload partitions are complete.
- The supplemental carrier contains only named cross-module contracts.
- No unsafe PID signal, test-only reader, or unmitigated high threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-02-SUMMARY.md` when done.
</output>
