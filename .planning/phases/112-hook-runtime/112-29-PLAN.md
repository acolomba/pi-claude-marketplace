---
phase: 112-hook-runtime
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/timeout.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: timeout cases own complete hook/config inputs and independently authored exact expectations under lowercase phase comments."
    - "D-13/D-15: absent or unusable overrides choose exact blocking defaults of 30 seconds for UserPromptSubmit, 1.5 seconds for SessionEnd, and 600 seconds for every other blocking or background event."
    - "Positive integer and fractional overrides are preserved; zero, negative, string, NaN, and positive/negative infinity values are rejected with the production-defined diagnostic behavior."
    - "Large positive values remain accepted with no unsupported upper clamp; scheduling, clamping, and timer races remain solely in plan 112-09."
  artifacts:
    - tests/bridges/hooks/timeout.test.ts
  key_links:
    - "hook event/background mode + declared timeout -> validated seconds/default/diagnostic"
---

<objective>
Refactor timeout tests into an exact validation and default-selection contract.

Purpose: Prove accepted values, unusable-value diagnostics, and event defaults without inventing a clamp (D-01, D-02, D-04, D-13, D-15).
Output: Refactored timeout owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove declared positive timeout values and exact event defaults</name>
  <files>tests/bridges/hooks/timeout.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/timeout.ts, tests/bridges/hooks/timeout.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, D-13, and D-15, rewrite the owner with complete local hook/config inputs and literal expected seconds. Cover an ordinary positive integer and a positive fractional override, then absence of an override for blocking `UserPromptSubmit` at exactly 30, blocking `SessionEnd` at exactly 1.5, another blocking event at exactly 600, and a background event at exactly 600. Author each expectation independently rather than importing timeout constants or iterating a generated table. Use lowercase `// arrange`, blank line, `// act`, blank line, `// assert`. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/timeout.test.ts</automated></verify>
  <acceptance_criteria>Positive integer/fractional values and all four default partitions return exact literal seconds, each case is self-contained, the focused owner passes, and no source change occurs.</acceptance_criteria>
  <done>The accepted-value and default-selection path is proved end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaust unusable values, diagnostics, and large-positive behavior</name>
  <files>tests/bridges/hooks/timeout.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/timeout.ts, tests/bridges/hooks/timeout.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Create separately authored cases for zero, a negative number, a string, `NaN`, positive infinity, and negative infinity. For each, assert the exact applicable default plus the production-defined diagnostic emission and ensure diagnostics occur only for a declared but unusable value, not an absent value. Preserve a large valid positive value exactly as supplied and assert no upper clamp. Do not add clock-threshold, timer-cancellation, or race assertions; plan 112-09 owns them. Keep runtime phase comments lowercase and case-owned. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/timeout.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/timeout.test.ts</automated></verify>
  <acceptance_criteria>Every unusable category, diagnostic/no-diagnostic branch, and large-positive/no-upper-clamp behavior is explicit; direct coverage passes with no scheduling assertion, shared mutable setup, or coverage exception.</acceptance_criteria>
  <done>The timeout owner covers every live configuration-value and fallback partition while leaving scheduling to 112-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured timeout -> runtime timer | Untrusted timeout configuration controls resource duration and must be finite and positive or fall back safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-04 | Denial of Service | timeout validation/defaults | high | mitigate | Exact finite/positive partitions, safe event defaults, boundary assertions, and cleanup verification prevent unusable values from controlling timers. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/timeout.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-29-PLAN.md`.

<verification>
Run `node --test tests/bridges/hooks/timeout.test.ts` and `npm run test:coverage:direct -- tests/bridges/hooks/timeout.test.ts`; confirm only the owner test is modified and no upper clamp is asserted.
</verification>

<success_criteria>
Timeout validation and defaults are explicit for positive, large positive, absent, zero, negative, nonnumeric, and nonfinite inputs without changing production or duplicating timer scheduling.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-29-SUMMARY.md` when done.
</output>
