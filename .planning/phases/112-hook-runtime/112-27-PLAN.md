---
phase: 112-hook-runtime
plan: 27
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/spawn-helpers.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-05: spawn-helper cases use complete local commands/values and exact whole expectations with lowercase phases."
    - "D-07/D-08/D-09/D-25: args-present values always use exec-form with `shell: false`; args-absent values retain shell-form semantics; serialization enforces the 256 KiB UTF-8 byte cap."
    - "Below-cap, exact-cap, one-byte-over, and multibyte cases are explicit; `_truncated: true` overrides conflicting input without mutating the input."
    - "D-10: these helper tests do not assert a fabricated global stdout/stderr ordering contract."
  artifacts:
    - tests/bridges/hooks/spawn-helpers.test.ts
  key_links:
    - "raw hook command -> planSpawn shell/exec decision"
    - "arbitrary hook result -> bounded UTF-8 JSON serialization"
---

<objective>
Create the dedicated spawn-helper owner test for execution-form planning and bounded serialization.

Purpose: Centralize exact security and byte-boundary behavior without duplicating subprocess integration tests (D-01, D-02, D-04, D-05, D-07, D-08, D-09, D-10, D-25).
Output: New owner test only; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Lock shell-form and exec-form planning</name>
  <files>tests/bridges/hooks/spawn-helpers.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/spawn-helpers.ts, tests/bridges/hooks/spawn-helpers.test.ts, tests/bridges/hooks/dispatch-exec.test.ts, tests/architecture/hooks-exec.test.ts, tests/architecture/hooks-async-rewake.test.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 for complete pure expectations and A2 for the process-boundary double. Create the owner test and, per D-07, D-08, D-09, and D-25, independently cover args-present commands including an empty array, mixed primitive argument stringification, exact command/argv, and `shell: false`; then cover args-absent shell command preservation, explicit shell selection, and missing/empty command behavior exactly as the current signature defines. Use complete literal whole-plan expectations and lowercase runtime phases. Absorb the pure helper assertions currently duplicated by exec/async owners, but do not edit those files here because plans 112-04 and 112-02 are their sole supplemental carriers. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/spawn-helpers.test.ts</automated></verify>
  <acceptance_criteria>Args present versus absent, empty args, mixed stringification, explicit shell, and missing command have exact outputs; shell-form/exec-form separation is unambiguous; the new owner passes without production or supplemental changes.</acceptance_criteria>
  <done>The full process-spawn planning decision is proved at its pure helper boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaust 256 KiB UTF-8 serialization boundaries</name>
  <files>tests/bridges/hooks/spawn-helpers.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/spawn-helpers.ts, tests/bridges/hooks/spawn-helpers.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 for complete pure expectations and A2 for the process-boundary double. Per D-04 and D-25, build exact byte-size inputs around 256 * 1024 bytes and assert below-cap, exact-cap, one-byte-over, and multibyte UTF-8 outcomes using `Buffer.byteLength` only to construct/check input size, never to derive the expected serialized object. Cover pre-existing `_truncated` input being overwritten to literal `true`, ordinary object non-mutation, oversized primitive and array wrapping, and the documented permitted envelope overshoot after adding truncation metadata. Each case owns its value and complete decoded expectation. Do not assert inter-stream global ordering (D-10). Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/spawn-helpers.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/spawn-helpers.test.ts</automated></verify>
  <acceptance_criteria>Below/exact/one-over/multibyte boundaries, metadata override, object immutability, primitive/array wrapping, and permitted envelope overshoot are explicit; direct owner coverage passes without a coverage exception.</acceptance_criteria>
  <done>Bounded serialization is exhaustively covered by UTF-8 byte count and value shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured command -> child-process options | Shell interpretation must occur only for shell-form commands. |
| arbitrary hook result -> serialized payload | Untrusted output must be constrained by the 256 KiB byte policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-01 | Elevation of Privilege | `planSpawn` | high | mitigate | Exact args-present `shell: false` and args-absent shell-form tests preserve execution-mode separation. |
| T-112-04 | Denial of Service | bounded serialization | high | mitigate | UTF-8 below/exact/one-over tests enforce the 256 KiB cap and deterministic truncation metadata. |
</threat_model>

## Artifacts this phase produces

- New owner test: `tests/bridges/hooks/spawn-helpers.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-27-PLAN.md`.

<verification>
Run the new focused test and direct coverage; confirm no source or supplemental file changed and no assertion promises global stdout/stderr order.
</verification>

<success_criteria>
Spawn planning and bounded serialization have exact execution-mode and UTF-8 boundary coverage in their dedicated owner.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-27-SUMMARY.md` when done.
</output>
