---
phase: 112-hook-runtime
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/post-tool-use-failure.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: each failure translation case owns a complete input, a whole seven-key expectation, and lowercase phase comments."
    - "D-22: PostToolUseFailure preserves nested tool input, response, and context identity while mapping built-in and custom tool names exactly."
    - "D-06: this pair changes only its owner test; malformed process output remains owned by dispatch-exec plan 112-04."
  artifacts:
    - tests/bridges/hooks/payloads/post-tool-use-failure.test.ts
  key_links:
    - "translatePostToolUseFailure input -> exact PostToolUseFailure wire payload"
---

<objective>
Make the PostToolUseFailure translator contract explicit and independently testable.

Purpose: Lock its full envelope, tool-name mapping, and reference-preservation behavior (D-01, D-02, D-04, D-06, D-22).
Output: Refactored owner test only; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the complete built-in-tool failure envelope</name>
  <files>tests/bridges/hooks/payloads/post-tool-use-failure.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use-failure.ts, tests/bridges/hooks/payloads/post-tool-use-failure.test.ts, tests/bridges/hooks/payloads/post-tool-use.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Using D-01, D-02, D-04, and D-22, author a complete built-in-tool input and assert the exact seven-key PostToolUseFailure payload with literal session/transcript/cwd/event/tool-name values plus the original tool-input, tool-response, and context references in their declared positions. Independently write the expected object; do not reuse an input builder as the oracle. Use lowercase arrange/act/assert comments with required blank lines. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/post-tool-use-failure.test.ts</automated></verify>
  <acceptance_criteria>The built-in case asserts every output key, exact event and tool-name literals, and nested reference identity; the owner test passes without production edits.</acceptance_criteria>
  <done>The built-in failure path is proved end to end through the translator.</done>
</task>

<task type="auto">
  <name>Task 2: Prove custom-tool mapping and immutable nested values</name>
  <files>tests/bridges/hooks/payloads/post-tool-use-failure.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use-failure.ts, tests/bridges/hooks/payloads/post-tool-use-failure.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add an independently authored custom-tool case that exercises the production name-mapping branch and uses distinct nested input/response objects. Assert the complete seven-key payload and identity of both nested objects and context, then assert the translator did not mutate those inputs. Per D-06, do not move malformed stdout/JSON coverage here; it remains with plan 112-04. Leave `tests/architecture/hooks-translators.test.ts` unchanged because this pair is not a supplemental carrier. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/post-tool-use-failure.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/post-tool-use-failure.test.ts</automated></verify>
  <acceptance_criteria>Built-in and custom mapping branches have whole-payload assertions, nested values are preserved and unmutated, direct coverage passes, and no supplemental file changes.</acceptance_criteria>
  <done>All owner-specific PostToolUseFailure mapping and identity partitions are covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator input -> hook payload | Tool-controlled nested input and failure response cross into the wire payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-06 | Tampering | PostToolUseFailure translator | high | mitigate | Exact name mapping, whole-envelope checks, nested identity, and non-mutation assertions preserve the mutation whitelist and payload boundary. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/post-tool-use-failure.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-16-PLAN.md`.

<verification>
Run the focused node test and direct coverage command; verify only the owner test changed and no translator supplemental was edited.
</verification>

<success_criteria>
The seven-key failure payload is covered for built-in and custom names with immutable case-local inputs and no test-only seam.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-16-SUMMARY.md` when done.
</output>
