---
phase: 04-hermetic-test-infrastructure
plan: 05
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - extensions/pi-claude-marketplace/platform/pi-api.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - tests/platform/pi-api.test.ts
  - tests/shared/notify.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
autonomous: true
requirements: [TREF-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Notification and tool-probe collaborators expose only the members production actually consumes."
    - "Marketplace update and enable-disable tests use typed values with no ExtensionContext or ExtensionAPI assertion."
    - "Notification bytes, tool observations, and production runtime behavior remain unchanged."
  artifacts:
    - extensions/pi-claude-marketplace/platform/pi-api.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/orchestrators/marketplace/update.test.ts
  key_links:
    - "consumer-declared Pi ports -> orchestrator options -> shared notify"
  prohibitions:
    - statement: "Do not replace broad assertions with Partial, unknown, a double assertion, or an unverified permissive mock."
      source: 04-CONTEXT.md D-19 and TypeScript unit-testing skill
      status: resolved
      verification: "Typecheck plus assertion census in changed tests."
---

<objective>
Introduce honest consumer-declared Pi ports and remove the terminal broad casts from marketplace update and enable-disable.

Purpose: Make test values type-check against the production surface actually used instead of fabricating full SDK objects.
Output: Narrow structural ports, behavior-neutral option typing, and typed owner fixtures.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hermetic-test-infrastructure/04-CONTEXT.md
@.planning/phases/04-hermetic-test-infrastructure/04-RESEARCH.md
@extensions/pi-claude-marketplace/platform/pi-api.ts
@extensions/pi-claude-marketplace/shared/notify.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the narrow notification and tool-inventory ports</name>
  <read_first>extensions/pi-claude-marketplace/platform/pi-api.ts, extensions/pi-claude-marketplace/shared/notify.ts, tests/platform/pi-api.test.ts, tests/shared/notify.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/platform/pi-api.ts, extensions/pi-claude-marketplace/shared/notify.ts, tests/platform/pi-api.test.ts, tests/shared/notify.test.ts</files>
  <behavior>- "A real ExtensionContext/ExtensionAPI remains structurally accepted, while typed test collaborators need only ui.notify and getAllTools."</behavior>
  <action>Declare small readonly production ports for the exact notification UI and tool-inventory members consumed by `notify`/`softDepStatus`. Use named interfaces, not broad `Pick` casts. Narrow the shared functions to those ports without changing execution. Add type-level positive and negative owner cases where appropriate and preserve complete notification output tests.</action>
  <verify><automated>node --test tests/platform/pi-api.test.ts tests/shared/notify.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/notify.ts &amp;&amp; npm run typecheck</automated><fails_when>The command exits non-zero, direct coverage fails, a real SDK caller no longer type-checks, or notification behavior changes.</fails_when></verify>
  <acceptance_criteria>The production contract names only the two consumed capabilities and remains compatible with real Pi objects.</acceptance_criteria>
  <done>The typed dependency foundation is available to orchestrators and tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove marketplace and enable-disable SDK assertions</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/enable-disable.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/enable-disable.test.ts</files>
  <behavior>- "Typed recording UI and tool-inventory values drive the same complete outputs and state transitions with no broad SDK cast."</behavior>
  <action>Narrow only the `ctx` and `pi` option members that flow exclusively to the new ports. Type local literals with annotations or `satisfies`; use exact strong mocks only where interaction itself is the promise. Delete `as ExtensionContext` and `as ExtensionAPI` in the two owner suites. Preserve all whole-value result, state, bytes, and notification assertions.</action>
  <verify><automated>node --test tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/enable-disable.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/platform/pi-api.ts extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts tests/platform/pi-api.test.ts tests/shared/notify.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/enable-disable.test.ts --max-warnings=0</automated><fails_when>The command exits non-zero, an SDK assertion remains in either owner, notification/state behavior changes, or lint reports a violation.</fails_when></verify>
  <acceptance_criteria>Both terminal owners use exact typed collaborator shapes and production behavior is unchanged.</acceptance_criteria>
  <done>OMAUB-F15 and the typed-double portion of OPEF-F13 are closed.</done>
</task>

</tasks>

<threat_model>
T-04-06 (Tampering, medium): an over-wide cast can silently omit a capability production begins to use. Consumer-declared ports plus strict type checks make that dependency change visible. No authorization, session, or cryptographic behavior changes.
</threat_model>

<verification>
Run direct owners, typecheck, ESLint, and a changed-test census for broad SDK assertions.
</verification>

<success_criteria>
The shared notification boundary and two terminal owners use exact typed ports with unchanged public behavior.
</success_criteria>

<output>
Create `.planning/phases/04-hermetic-test-infrastructure/04-05-SUMMARY.md` when done.
</output>
