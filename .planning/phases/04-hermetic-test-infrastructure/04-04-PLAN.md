---
phase: 04-hermetic-test-infrastructure
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - tests/domain/auth-registry.test.ts
  - tests/orchestrators/auth-host.test.ts
autonomous: true
requirements: [AUTH-01, TREF-02]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Host prefixes, suffixes, and subdomains never select the GitHub or GitLab provider."
    - "Omitting optional auth collaborators is proven by invoking public behavior without network work."
    - "A realistic callback failure retains its exact result or Error identity through the shared Git fake."
  artifacts:
    - tests/domain/auth-registry.test.ts
    - tests/orchestrators/auth-host.test.ts
  key_links:
    - "clone URL host -> exact provider lookup -> auth bundle callback -> Git fake call ledger"
  prohibitions:
    - statement: "Do not change production host policy or add live HTTP fallback."
      source: 04-CONTEXT.md D-13 through D-16
      status: resolved
      verification: "Only owner tests change and all HTTP behavior is injected or bypassed by a credential hit."
---

<objective>
Add the discriminating authentication cases selected by AUTH-01.

Purpose: Make exact host selection, optional collaborator behavior, and realistic failure propagation observable through public contracts.
Output: Hostile-host domain cases and production-shaped auth-host cases.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hermetic-test-infrastructure/04-03-SUMMARY.md
@.planning/phases/04-hermetic-test-infrastructure/04-CONTEXT.md
@extensions/pi-claude-marketplace/domain/auth-registry.ts
@extensions/pi-claude-marketplace/orchestrators/auth-host.ts
@tests/platform/git-ops-fake.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject hostile provider-host neighbors</name>
  <read_first>extensions/pi-claude-marketplace/domain/auth-registry.ts, tests/domain/auth-registry.test.ts, extensions/pi-claude-marketplace/orchestrators/auth-host.ts</read_first>
  <files>tests/domain/auth-registry.test.ts</files>
  <behavior>- "Every prefixed, suffixed, subdomain-shaped, case-changed, port-bearing, and opposite-provider host returns undefined; exact supported hosts retain their exact provider object."</behavior>
  <action>Add typed table rows that create one sibling `test()` per hostile host for both providers. Use independent literal expected values and keep exact supported-host cases separate. Include provider disjointness so a permissive predicate cannot select the wrong provider. Do not alter production equality predicates.</action>
  <verify><automated>node --test tests/domain/auth-registry.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/auth-registry.ts</automated><fails_when>The command exits non-zero, a hostile neighbor resolves, exact providers change, or direct owner coverage fails.</fails_when></verify>
  <acceptance_criteria>The historical `endsWith` mutation and equivalent widening mutations are killed.</acceptance_criteria>
  <done>DCORE-001 is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove optional and failing auth collaborators through public behavior</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/auth-host.ts, tests/orchestrators/auth-host.test.ts, tests/platform/git-ops-fake.ts, tests/platform/credential-ops-fake.ts</read_first>
  <files>tests/orchestrators/auth-host.test.ts</files>
  <behavior>
    - "With deviceFlowHttp and authMemo omitted, a seeded credential is returned and no HTTP collaborator is touched."
    - "A function-bearing auth callback recorded by Git operations can be invoked and preserves its exact failure result or thrown Error identity."
  </behavior>
  <action>Replace the self-referential optional-collaborator assertion with a credential-hit case that invokes `onAuthRequired`, compares the complete authentication value and credential ledger, and cannot reach the default network adapter. Add a realistic failure case that passes the production-shaped bundle through `createGitOpsFake`, invokes the recorded callback, and asserts exact failure propagation. Preserve strong-mock verification order after public results and state.</action>
  <verify><automated>node --test tests/orchestrators/auth-host.test.ts tests/platform/git-ops-fake.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/auth-host.ts</automated><fails_when>The command exits non-zero, discovers zero tests, the optional case asserts its own fixture, any network path is reachable, callback identity changes, or direct coverage fails.</fails_when></verify>
  <acceptance_criteria>Optional and failing collaborators are observed through real public callbacks, not fixture shape.</acceptance_criteria>
  <done>ORR-F011 and the AUTH-01 portion of PLT-F020 are closed.</done>
</task>

</tasks>

<threat_model>
T-04-04 (Spoofing, high): a hostile lookalike host could receive a supported provider's credential flow. T-04-05 (Repudiation, medium): an erased callback failure could make auth diagnostics untrustworthy. Both are blocked on exact hostile rows and identity-preserving callback execution.
</threat_model>

<verification>
Run both owner suites and direct coverage for auth registry and auth host. Confirm no global fetch replacement or real URL access was added.
</verification>

<success_criteria>
Exact providers alone match, optional dependencies are genuinely optional, and realistic auth failures survive production-shaped test collaborators.
</success_criteria>

<output>
Create `.planning/phases/04-hermetic-test-infrastructure/04-04-SUMMARY.md` when done.
</output>
