---
phase: 10-constraint-aware-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-preflight.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts
  - extensions/pi-claude-marketplace/shared/notification-types.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - extensions/pi-claude-marketplace/shared/notification-grammar.ts
  - docs/output-catalog.md
  - tests/orchestrators/plugin/update-constraint-gate.test.ts
  - tests/orchestrators/plugin/update-preflight.test.ts
  - tests/orchestrators/plugin/update-cascade.test.ts
  - tests/orchestrators/plugin/update-row.test.ts
  - tests/shared/notification-grammar.test.ts
  - tests/shared/notification-types.test.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/compat-01-no-expansion.test.ts
  - tests/architecture/catalog-uat/catalog-contract.test.ts
  - tests/architecture/catalog-uat/catalog-parser.test.ts
  - tests/architecture/catalog-uat/fixtures/plugin-update.ts
autonomous: false
requirements: [UPDT-02]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `update <plugin>@<mp>` whose target is declared by two installed records with disjoint ranges is not updated: `preparePluginUpdate` returns a `skipped` partition carrying the new closed-set token, and the rendered row names both declaring plugins on a 4-space-indented cause line (UPDT-02, D-10-01 stage one, D-10-10 disjoint arm)."
    - "The constraining plugins are named on the cause line sorted lexicographically by `name@marketplace`, so the line is byte-stable however the state document happens to order its marketplaces and plugins (UPDT-02 ordering edge, D-10-11)."
    - "A declaring record that is installed but disabled still constrains, and the cause line marks it as currently disabled; a declaring record that is enabled carries no marking (D-10-06, D-10-11)."
    - "Constraints are scope-local: the gate reads the ONE state document `preparePluginUpdate` already loaded for the target scope, so a bare `update` fanning out over project and user evaluates each scope independently and a record in the other scope constrains nothing (D-10-07)."
    - "Provenance never reaches the gate: a holder is admitted on its declaration alone, so a directly-installed plugin that something also depends on is held exactly as a dependency-provenance one is (D-10-08)."
    - "A target with ZERO declaring records is unconstrained: `intersectDependencyRanges([])` returns the wildcard, the gate returns its unconstrained verdict, and NEITHER tag-probe seam is called even once (UPDT-01 empty edge, success criterion 3's regression proof)."
    - "A second `update` of the same held plugin produces the identical held row and writes nothing to `state.json`: the hold is computed, never recorded (UPDT-02 idempotency edge)."
    - "A bulk `update` that holds one plugin still updates the others in the same run; the held verdict returns before `makeUpdateCloneProbe` is composed, so no clone is materialized for the held target (UPDT-02 concurrency edge, D-10-03)."
    - "The gate takes no lock and holds none across a probe: it evaluates the snapshot `preparePluginUpdate` already loaded, and the only serialization is the record write the swap performs inside `withLockedStateTransaction`, unchanged. A parallel update in another scope reads a different state document (D-10-07) and neither blocks nor is blocked by this one; a parallel same-scope write after the snapshot is not re-read, which is the same window every other preflight decision already sits in (UPDT-01 concurrency edge)."
    - "The gate runs AFTER `triageUpdateMembership` and BEFORE `resolveUpdateCandidate`, so it is only ever asked about a plugin that has both an install record and a manifest entry (D-10-03)."
    - "When the fail-closed declaration walk refuses, the target's update is skipped with the SAME new token and the walk's own `cause.message` is the cause line, naming the unreadable declarer and carrying no absolute path (D-10-05)."
    - "The closed reason set is 62 members with the new token at the tail; `Reason`, `CommandPrivateReason`, both `EXPECTED_REASONS` tuples, `REASON_ENROLLMENT`'s length assertion and the catalog's member-count prose all agree in one commit (D-10-09)."
    - "The new token is NOT a member of `IDEMPOTENT_REASONS`, so `skipSeverity` returns `warning` for the held row with no bespoke branch on either cascade (D-10-12)."
    - "Every pre-existing `skipped` row in the catalog renders byte-identically after `PluginSkippedMessage` gains an optional `cause` and `composePluginLinesWith` admits `\"skipped\"`: no other producer sets the field, and `renderIndentedCauseChain(undefined, \"    \")` returns the empty string so nothing is pushed."
    - statement: "When two candidate release tags carry equal semver versions, the tie is resolved by `domain/release-tag.ts::selectHighestSatisfyingTag` alone; the gate re-sorts no candidate list and returns the probe's `pinned` arm verbatim."
      verification: backstop
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts"
      provides: "evaluateUpdateConstraint, describeConstraint, UpdateConstraintVerdict, ConstraintHolder, UpdateConstraintSeam"
      contains: "evaluateUpdateConstraint"
    - path: "tests/orchestrators/plugin/update-constraint-gate.test.ts"
      provides: "the leaf's paired test at 100% direct coverage (the pin file has an empty rows list, so there is no exemption budget)"
      contains: "evaluateUpdateConstraint"
    - path: "extensions/pi-claude-marketplace/shared/notification-types.ts"
      provides: "the 62nd Reason member and PluginSkippedMessage.cause"
      contains: "readonly cause?: Error;"
    - path: "extensions/pi-claude-marketplace/shared/notification-grammar.ts"
      provides: "composePluginLinesWith's widened cause-bearing status gate"
      contains: "p.status === \"skipped\""
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts"
      provides: "constraintCauseFor -- the one carrier both update cascades read the held/disclosed cause line through"
      contains: "constraintCauseFor"
    - path: "docs/output-catalog.md"
      provides: "the update-held-by-dependents catalog state and the 62-member reasons sentence"
      contains: "catalog-state: update-held-by-dependents"
    - path: "tests/architecture/catalog-uat/fixtures/plugin-update.ts"
      provides: "the held-row fixture the contract test drives notify() with"
      contains: "update-held-by-dependents"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/update-preflight.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts"
      via: "preparePluginUpdate awaits the gate through an optional injected field defaulted to the real implementation, between triageUpdateMembership and makeUpdateCloneProbe"
      pattern: "constraintGate"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts"
      via: "the gate inverts buildScopeDeclarationDetail's declarations map instead of walking the state a third time"
      pattern: "buildScopeDeclarationDetail"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts"
      via: "projectSkippedOutcome reads constraintCauseFor and spreads the cause onto the PluginSkippedMessage"
      pattern: "constraintCauseFor"
    - from: "extensions/pi-claude-marketplace/shared/notification-types.ts"
      to: "extensions/pi-claude-marketplace/shared/notify-reasons.ts"
      via: "the new Reason literal gets its home in CommandPrivateReason; without it the coverage proof is a TS2344 at the declaration site"
      pattern: "dependents constrain"
  prohibitions:
    - statement: "The gate MUST NOT add a second range-intersection evaluator: it calls `intersectDependencyRanges`, which is where the 4096-character input cap and the 1024-conjunct projection cap live. Calling `semver` directly from the gate loses both."
      status: unresolved
      verification: null
    - statement: "The gate MUST NOT add a third declaration walk to `dependency-index.ts`; it inverts `buildScopeDeclarationDetail`'s existing output (D-10-04)."
      status: unresolved
      verification: null
    - statement: "The held verdict MUST NOT gain `cause`, `toVersion` or `phaseFailures` on `PluginUpdateSkippedOutcome`; the cause text rides the existing `notes` channel, and the `never` proofs on `PreflightFailedOutcome` stay load-bearing (NFR-7)."
      status: unresolved
      verification: null
    - statement: "The new token MUST NOT be added to `IDEMPOTENT_REASONS`, and neither cascade may gain a bespoke severity branch for it (D-10-12)."
      status: unresolved
      verification: null
    - statement: "No new symbol may be exported for a test alone; every export is reached from production code or it is not exported (fallow production dead-code gate)."
      status: unresolved
      verification: null
---

<objective>
Put a constraint gate in the plugin-update preflight and carry ONE held row all
the way to rendered bytes: a plugin whose installed dependents hold it to ranges
that admit nothing is not updated, and its row names who is holding it.

This plan takes the thinnest end-to-end path through every layer the phase
touches -- the new leaf, the preflight, the closed reason vocabulary, the row
grammar's cause channel, the manual update cascade, and the output catalog --
using the arm that needs no tag query at all: disjoint declared ranges. Stage
one's tag probe (plan 10-02), stage two's post-fetch guard and the path-source
fallback (plan 10-03) build out from the proven slice.

Purpose: UPDT-02's user-visible half, plus the closed-set amendment D-10-09
needs, landed once so plans 10-02 through 10-04 never touch the vocabulary
again.

Output: `update-constraint-gate.ts` and its paired test, the gate's call site in
`preparePluginUpdate`, the 62nd reason, `PluginSkippedMessage.cause` and the
renderer gate that makes it visible, and the `update-held-by-dependents` catalog
state.

Reversibility ruling carried from CONTEXT.md: D-10-09 is rated one-way -- the
token is published user-visible output and a catalog state. Task 1 is the
`checkpoint:decision` that settles its exact wording before any of the nine
pinning surfaces move; Task 2 carries the rating for the record.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-constraint-aware-update/10-CONTEXT.md
@.planning/phases/10-constraint-aware-update/10-RESEARCH.md
@.planning/phases/10-constraint-aware-update/10-PATTERNS.md
@.claude/rules/typescript-style.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase creates. Nothing below exists in the tree yet, so a
drift check must not flag them as unresolved references. Plan 10-01 lands every
item unless a later plan is named.

**New production module** --
`extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts`:

- `ConstraintHolder` -- `{ key: string; range: string; disabled: boolean }`, one
  installed record that declares the target, with the range it declared and its
  enabled bit read off the state document.
- `UpdateConstraintSeam` -- `{ buildScopeDeclarationDetail; probeDependencyTags;
  probeMarketplaceTags }`, the injectable operations, defaulted to the real
  imports through a frozen module constant. The two probe members arrive in plan
  10-02; plan 10-01 declares the seam with `buildScopeDeclarationDetail` alone.
- `UpdateConstraintOptions` -- `{ plugin, marketplace, entry, marketplaceRoot,
  state, locations, seam?, tagMemo?, marketplaceTagMemo?, auth }`.
- `UpdateConstraintVerdict` -- the three-arm result:
  `{ kind: "unconstrained" }` |
  `{ kind: "admits"; range; holders; pin?; fellBackToCurrentCopy }` |
  `{ kind: "held"; cause: string }`.
- `evaluateUpdateConstraint(options): Promise<UpdateConstraintVerdict>` -- stage
  one.
- `describeConstraint(range, holders, arm): string` -- the ONE cause-line
  composer, shared by every arm (Claude's discretion, settled here: the
  holder-naming half is identical across all of them, so three composers would
  be three places to drift the disabled marking).
- `admitResolvedVersion(...)` -- stage two, added by plan 10-03.

**New test module** --
`tests/orchestrators/plugin/update-constraint-gate.test.ts`, paired per
`scripts/test-coverage-direct.mjs::sourceToTest`.

**New closed-set member** -- `"dependents constrain"` on
`shared/notification-types.ts::Reason` (tail position, set size 62) and on
`shared/notify-reasons.ts::CommandPrivateReason`. Exact wording is Task 1's
checkpoint; every reference below assumes the recommendation is accepted.

**New field on an existing type** --
`shared/notification-types.ts::PluginSkippedMessage.cause?: Error`.

**New exported helper on an existing module** --
`orchestrators/plugin/update-row.ts::constraintCauseFor`.

**New optional field on `PreparePluginUpdateOptions`** -- `constraintGate?:
typeof evaluateUpdateConstraint`, the injected composition field, beside
`cloneCacheSeam`.

**New catalog state** -- `update-held-by-dependents` in
`docs/output-catalog.md`'s `## \`/claude:plugin update\`` section, with its
fixture entry in `tests/architecture/catalog-uat/fixtures/plugin-update.ts`.

**Landed by later plans** -- `UpdateConstraintDisclosure` on
`orchestrators/types.ts` and the `constraint?` carriers on
`PreparedPluginUpdate` / `PluginUpdateUpdatedOutcome` /
`PluginUpdateUnchangedOutcome` (10-03); `update-held-out-of-range`,
`update-current-copy` and `update-up-to-date-constrained` catalog states
(10-03).

**New test case names (verbatim titles the verifier can grep for):**
`UPDT-02: disjoint declared ranges hold the update and name both declarers`,
`UPDT-02: holders are named in lexicographic key order`,
`UPDT-02: a disabled declarer is named and marked`,
`UPDT-01: a target no installed record declares queries no tag listing`,
`UPDT-02: an unreadable declarer holds the update and names itself`,
`UPDT-02: a held update writes nothing and repeats byte-identically`,
`D-10-03: the gate runs after triage and before candidate resolution`.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Settle the new closed-set reason token's exact wording (D-10-09)</name>

  <read_first>
    - .planning/phases/10-constraint-aware-update/10-CONTEXT.md (D-10-09, D-10-10 and the `Reversibility: one-way` rating; the `<specifics>` block's upstream strings)
    - docs/messaging-style-guide.md (the `REASONS` paragraph under "Type Model Reference", and the "Output Grammar Summary" rules on what a brace may and may not interpolate)
    - extensions/pi-claude-marketplace/shared/notification-types.ts (lines 14-177 -- the whole `Reason` union with its per-member rationale comments; read `dependents unsatisfied` at 138-151, `dependents remain` at 165-171 and `no matching version` at 67-72 as the three nearest neighbours)
    - docs/output-catalog.md (line 63 -- the reasons paragraph that states the member count and enumerates the append-only dependency block)
  </read_first>

  <decision>
    D-10-09 is LOCKED: the held row carries ONE NEW closed-set reason token, and
    neither existing neighbour may be reused. What is open is the token's exact
    text, which CONTEXT.md assigns to the planner's discretion against
    `docs/messaging-style-guide.md`. The decision is one-way: the token is
    published user-visible output and a catalog state, so removing it later
    means a catalog amendment plus a byte-count change on every pinned surface.

    The constraints the wording must satisfy: one to three lowercase words;
    subject-first grammar; it must interpolate nothing (the set is a literal
    union, so no identifier can ride the token -- the constraining plugin names
    ride the cause line, on the `dependency cycle` and `dependents unsatisfied`
    precedent); and it must be truthful across all three arms D-10-10 folds onto
    it (disjoint declared ranges, no tag satisfying the intersection, and a
    fetched version outside the intersection).
  </decision>

  <options>
    <option id="proceed-as-recommended">
      <name>`dependents constrain` -- CONTEXT.md's own working name</name>
      <pros>Two lowercase words, subject-first, and structurally identical to the `dependents remain` member already in the set (subject `dependents`, present-tense verb). Its subject is the plugins doing the holding, which is the one fact the row needs and the one the cause line then expands. It makes no claim about what the source advertised, so it stays truthful on the post-fetch-guard arm where a version WAS found and simply is not admissible -- the exact reason `no matching version` was ruled out. It states no contradiction, so it does not collide with `version conflict`'s two existing subjects.</pros>
      <cons>`constrain` is transitive with its object left implicit, so the token reads as a fragment until the cause line supplies the target. The same is true of `dependents remain`, which shipped, so the precedent absorbs it.</cons>
    </option>
    <option id="held-by-dependents">
      <name>`held by dependents`</name>
      <pros>Three lowercase words; states the outcome (the update was held) rather than the cause, which matches what the user observes.</pros>
      <cons>Not subject-first: the row's subject is the plugin, and the token opens on a past participle with the subject implied, which no member of the current 61 does. It also duplicates the `(skipped)` status token's own claim -- the brace exists to say WHY, not to restate that nothing happened.</cons>
    </option>
    <option id="other">
      <name>Another wording</name>
      <pros>May read better against the style guide than either above.</pros>
      <cons>Tasks 2 and 3 do not start until it is settled; every one of the nine pinning surfaces spells the literal.</cons>
    </option>
  </options>

  <resume-signal>Reply `proceed-as-recommended`, `held-by-dependents`, or give another two-or-three-word lowercase token.</resume-signal>

  <acceptance_criteria>
    - A reply is recorded in the plan SUMMARY naming the token the developer chose and the exact words they used.
    - The token recorded in the SUMMARY is one to three lowercase words and interpolates no identifier.
    - Tasks 2 and 3 spell that exact literal on every one of the nine pinning surfaces; no surface spells the working name if the developer chose otherwise.
  </acceptance_criteria>

  <done>The developer has answered, the answer is recorded verbatim in the SUMMARY, and the chosen literal is what Tasks 2 and 3 spell.</done>

  <prompt>
    This phase adds one new reason token to the closed 61-member set, for the
    row that reports a plugin whose update was held because the plugins that
    depend on it constrain its version. The token is user-visible inside the
    row's `{...}` brace and becomes a catalog state, which makes it one-way.

    Recommended: `dependents constrain`. It is CONTEXT.md's working name, it
    matches the `dependents remain` member's subject-first two-word shape, and
    it stays truthful on all three situations D-10-10 folds onto it -- including
    the one where a version WAS found and is simply not admissible, which is
    what rules out reusing `no matching version`.

    The alternative considered was `held by dependents`, rejected because it is
    not subject-first and restates what `(skipped)` already says.
  </prompt>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End to end -- disjoint dependent ranges hold one update, and the row names the holders</name>
  <reversibility rating="one-way">The reason token and its catalog state are published user-visible output; Task 1's checkpoint settles the wording before this task spells it on nine surfaces.</reversibility>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/update-preflight.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts,
    extensions/pi-claude-marketplace/shared/notification-types.ts,
    extensions/pi-claude-marketplace/shared/notify-reasons.ts,
    extensions/pi-claude-marketplace/shared/notification-grammar.ts,
    docs/output-catalog.md,
    tests/orchestrators/plugin/update-constraint-gate.test.ts,
    tests/orchestrators/plugin/update-preflight.test.ts,
    tests/orchestrators/plugin/update-cascade.test.ts,
    tests/orchestrators/plugin/update-row.test.ts,
    tests/shared/notification-grammar.test.ts,
    tests/shared/notification-types.test.ts,
    tests/architecture/notify-closed-set-locks.test.ts,
    tests/architecture/compat-01-no-expansion.test.ts,
    tests/architecture/catalog-uat/catalog-contract.test.ts,
    tests/architecture/catalog-uat/catalog-parser.test.ts,
    tests/architecture/catalog-uat/fixtures/plugin-update.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install-clone-probe.ts (whole file -- the seam-interface / frozen-default / `options.seam ?? REAL_...` composition shape this leaf copies)
    - extensions/pi-claude-marketplace/orchestrators/plugin/dependency-tag-probe.ts (whole file -- the module-header form for a leaf deliberately absent from the network-free target list, the `AUTH-09` credential paragraph, and the memo-only-on-success rule)
    - extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts (lines 92-159 `AddressedDependency` / `ScopeDeclarationDetailResult`, 256-276 `buildScopeDeclarationDetail`, and the header's fail-closed and cause-line paragraphs at 23-46)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/dependency-verdict.ts (lines 140-190 -- `recordedPlugins`' sidecar map beside the declarations walk, and `constraintsByKey`'s fold; the inversion here is the same fold keyed the other direction)
    - extensions/pi-claude-marketplace/domain/dependency-range.ts (whole file -- `intersectDependencyRanges` with its two caps, `isUnconstrainedRange`, `renderConstraintRange`)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-preflight.ts (whole file, 629 lines -- `PreparePluginUpdateOptions` at 85-97, `makeUpdateCloneProbe` at 143-227, `skippedCandidate` at 298-312, `triageUpdateMembership` at 368-402, `preparePluginUpdate` at 541-618)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts (lines 43-161 -- `cascadeSkipSeverity`, `projectSkippedOutcome`, `outcomeToCascadePluginMessage`)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts (whole file -- the leaf-with-no-back-edges header and `updatedRowFromOutcome`)
    - extensions/pi-claude-marketplace/shared/notification-types.ts (lines 5-185 the `Reason` union and `ContentReason`, 343-362 `PluginDisabledMessage.cause` and its JSDoc -- the field-shape precedent -- and 452-458 `PluginSkippedMessage`)
    - extensions/pi-claude-marketplace/shared/notification-grammar.ts (lines 1595-1656 `composePluginLinesWith`, and 1061-1068 `renderIndentedCauseChain`'s empty-string short-circuit)
    - extensions/pi-claude-marketplace/shared/notify-reasons.ts (lines 1-100 the header narrative and its running member arithmetic, 105-135 `IDEMPOTENT_REASONS` / `skipSeverity`, 325-430 `CommandPrivateReason` and the `_UncoveredReason` / `_ExtraReason` proof)
    - extensions/pi-claude-marketplace/orchestrators/types.ts (lines 289-361 -- `PluginUpdateSkippedOutcome` and the `notes` channel's documented purpose)
    - tests/orchestrators/plugin/dependency-tag-probe.test.ts (lines 1-60 -- the `NotificationContext` / `CredentialOps` stubs and the seam-double factory the new leaf test copies)
    - tests/orchestrators/plugin/update-preflight.test.ts (lines 1-120 -- `pluginRecord`, `SeedOptions`, `seedUpdate`, and how a marketplace + state document is seeded on disk)
    - tests/architecture/catalog-uat/fixtures/plugin-update.ts (lines 1-120 -- the fixture-entry shape, `piWithBothLoaded()`, `expectedSeverity`, `cardinality`)
    - tests/architecture/catalog-uat/catalog-contract.test.ts (lines 36-130 -- the running state-count narrative and `EXPECTED_STATE_COUNT` / `EXPECTED_UTF8_BYTES`; 410-436 -- what the contract actually asserts)
    - tests/architecture/notify-closed-set-locks.test.ts (lines 38-60 `REASON_ENROLLMENT`, 161-250 the narrative and the length assertion)
    - docs/output-catalog.md (line 63 the reasons paragraph; lines 1400-1460 the update section's first catalog states, for the anchor-then-fence shape)
    - docs/messaging-style-guide.md (the row-grammar and cause-chain rules)
  </read_first>
  <behavior>
    - `evaluateUpdateConstraint` with a state where `alpha@mp` declares `target` at `^1.0.0` and `beta@mp` declares `target` at `^2.0.0` returns `{ kind: "held", cause: <text> }`, and the text names `alpha@mp` before `beta@mp`.
    - The same state with the declarers registered in the opposite order in `state.marketplaces` returns the identical `cause` text.
    - With `beta@mp` recorded `enabled: false`, the cause text marks `beta@mp` as currently disabled and leaves `alpha@mp` unmarked.
    - Flipping a declarer's `provenance` between the direct and dependency values changes neither the verdict nor the cause text.
    - The gate consults only the `state` object it was handed: a second scope's state document is never loaded, and a declarer present only in that other document does not appear among the holders.
    - With NO record declaring `target`, the verdict is `{ kind: "unconstrained" }`, and a seam whose `probeDependencyTags` / `probeMarketplaceTags` doubles count their calls reports zero calls.
    - With one declarer at `^1.0.0` and one at `>=1.2.0`, the verdict is `{ kind: "admits", range: <the folded range>, holders: [both], fellBackToCurrentCopy: false }` and `pin` is absent (stage one's probe lands in plan 10-02).
    - A declarer whose declarations cannot be established (`buildScopeDeclarationDetail` returns its `ok: false` arm) yields `{ kind: "held" }` whose cause text is the walk's own `cause.message`, unchanged.
    - `preparePluginUpdate` on a held target returns `{ partition: "skipped", name, fromVersion: <record version>, reasons: [<the token>], notes: [<the cause text>] }` and never composes a clone probe.
    - `preparePluginUpdate` on an unconstrained target returns exactly what it returns today for the same fixture -- same partition, same fields, same values.
    - `projectSkippedOutcome` on a held outcome produces a `PluginSkippedMessage` with `severity: "warning"` and `cause` set from the notes; on every other skipped outcome the message has no `cause` key at all.
    - `composePluginLinesWith` on a `skipped` message with no `cause` pushes exactly one line (the row); with a `cause` it pushes the row plus one 4-space-indented `cause: ...` line.
    - The catalog contract renders the `update-held-by-dependents` fixture byte-for-byte equal to the fenced block in the document.
  </behavior>
  <action>
Wire ONE held update from the declaration walk to rendered bytes. Only the
disjoint arm -- the one that needs no tag query at all. Stage one's probe
(plan 10-02) and stage two's guard (plan 10-03) are NOT in scope here; do not
stub them in.

**The leaf (`update-constraint-gate.ts`, NEW).** Give it a module header on
`dependency-tag-probe.ts`'s model stating four things: that it answers "what do
the installed records that declare this plugin hold it to"; that it is
deliberately ABSENT from `tests/architecture/gate-targets.ts`'s
`NETWORK_FREE_TARGETS` for the same reason its two probe siblings are, and is
composed and invoked through an injected field whose name the gate does not
match (D-10-19); that AUTH-09 holds -- the credential bundle is threaded in from
the caller and no credential value is read, stored or placed on a returned arm;
and that the range algebra is `intersectDependencyRanges`' alone, so the two
project-owned caps are never bypassed.

Declare `ConstraintHolder`, `UpdateConstraintSeam`, `UpdateConstraintOptions`
and `UpdateConstraintVerdict` with the shapes listed in
`<artifacts_this_phase_produces>`. Freeze the real seam in a module constant
(`Object.freeze`, as `REAL_MARKETPLACE_TAG_LISTING_SEAM` does) and select it
with `options.seam ?? REAL_...`.

`evaluateUpdateConstraint` does, in order: call
`seam.buildScopeDeclarationDetail({ state, locations })` with NO `exclude` --
the same call `buildScopeSatisfactionVerdict` makes, because the target is at
once a potential dependency and a potential declarer; on its `ok: false` arm
return `{ kind: "held", cause: failure.cause.message }` unchanged (D-10-05 --
the walk's message IS the rendered cause line and is already path-redacted, so
do not re-wrap it and do not chain `{ cause }`); otherwise invert the
`declarations` map: for each `[declarerKey, deps]` entry keep the elements whose
`` `${dep.name}@${dep.marketplace}` `` equals `` `${plugin}@${marketplace}` ``,
skipping the target's own self-declaration (`declarerKey` equal to the target
key), and for each survivor push a `ConstraintHolder` carrying `declarerKey`,
the element's `version` (skip elements with no `version` -- an unconstrained
declaration holds the key but names no range), and `disabled` read with
`isRecordedButDisabled` off `state.marketplaces[mp]?.plugins[name]` for the
declarer, exactly as `recordedPlugins` builds its sidecar map. Read the
declarer's `enabled` bit and NOTHING else off its record -- `provenance` is
never consulted, because the question is who declares this key and not how the
record got here, so a directly-installed holder holds exactly as a
dependency-provenance one does (D-10-08). The `state` the gate walks is the ONE
document `preparePluginUpdate` already loaded for the target scope, so
constraints are scope-local by construction and the gate must never load a
second scope's state (D-10-07). State both facts in the module header. Sort the
holders by `key` with `localeCompare` before anything reads them, so the
rendered line is stable whatever order the state document walks in. With no
holders, or with
holders whose folded range `isUnconstrainedRange` accepts, return
`{ kind: "unconstrained" }` and make no further call. Otherwise fold
`holders.map((h) => h.range)` through `intersectDependencyRanges`: its `ok: true`
arm returns `{ kind: "admits", range, holders, fellBackToCurrentCopy: false }`;
its `ok: false` arm returns `{ kind: "held", cause: describeConstraint(...) }`
for all three failure reasons (`disjoint`, `invalid`, `too-complex`) -- one
token, and the cause line says which situation it is (D-10-10).

`describeConstraint(range, holders, arm)` is the ONE composer. Its output is a
single line with no leading indent (the renderer indents it) and no absolute
path. Compose it as: an arm clause, then ` -- required by ` and the holders
joined `", "`, each rendered as `"<key>"` and, when `disabled`, followed by
` (currently disabled)`. The arm clauses are fixed text: for a disjoint fold,
that the declared ranges admit no version in common; for an invalid one, that a
declared range could not be read; for an over-cap one, that the declared ranges
are too complex to combine. Pass every range through `renderConstraintRange`
before it reaches the text, so a synthesized intersection cannot flood the line.
Export `describeConstraint` -- plans 10-02 and 10-03 add arms to it, and the
paired test drives it directly.

**The call site (`update-preflight.ts`).** Add
`readonly constraintGate?: typeof evaluateUpdateConstraint;` to
`PreparePluginUpdateOptions` immediately after `cloneCacheSeam`, with a comment
naming D-10-02 and D-10-19: the leaf is composed through this field, the field
name is not one the network gate matches, and production omits it. In
`preparePluginUpdate`, immediately after the `if ("partition" in triaged)`
return and BEFORE `makeUpdateCloneProbe` (D-10-03), `await` the gate with the
triaged entry, `marketplace.marketplaceRoot`, the loaded `state`,
`options.locations`, and the SAME auth bundle literal the clone probe is
composed with -- build that bundle object once into a local and pass it to both,
so there is only one credential composition in this function (AUTH-09). Then
branch ONCE:
`if (verdict.kind === "held") { return skippedCandidate({ plugin, fromVersion: triaged.record.version }, [verdict.cause], [<the token>]); }`
Keep every other constraint branch inside the leaf -- ESLint's cognitive
complexity cap is 15 and `preparePluginUpdate` is already close to it, which is
D-10-02's own stated reason. Bind the `admits` and `unconstrained` arms to a
local the later plans consume; in THIS plan neither arm changes anything
downstream.

**The vocabulary (`notification-types.ts`, `notify-reasons.ts`).** Append the
token settled in Task 1 to the tail of the `Reason` union with a rationale
comment in the file's established form: it names the CONDITION and the
constraining plugins ride the cause line, because a reason is one to three
lowercase words and the set is a literal union so no token can interpolate an
identifier; `version conflict` cannot carry it because nothing contradicts
anything here; `no matching version` cannot carry it because that token claims
the source advertised nothing in range, which is false on the arm where a
version was found and is simply not admissible. Cite `UPDT-02`, `D-10-09` and
`D-10-10`; cite no phase number and no research artifact number. Give the token
its home in `notify-reasons.ts::CommandPrivateReason` with a two-sentence
comment (owned by the plugin-update preflight; NOT idempotent, because the
update the user asked for was not carried out) and extend the header's running
member arithmetic by one sentence ending `(61 to 62)`. Do NOT touch
`IDEMPOTENT_REASONS` -- leaving the token out of it is what makes
`skipSeverity` return `warning` on both cascades with no code change (D-10-12).

**The cause channel (`notification-types.ts`, `notification-grammar.ts`).** Add
`readonly cause?: Error;` to `PluginSkippedMessage`, with JSDoc modelled on
`PluginDisabledMessage.cause`: name the ONE thing it carries (the
constraining-plugin naming of a held update, D-10-11), state that every other
`skipped` producer omits it and stays byte-frozen, and state why it rides the
cause chain -- the sentence interpolates plugin identifiers and the cause chain
is the only channel in this grammar that legally interpolates one. In
`composePluginLinesWith`, add `|| p.status === "skipped"` to the cause-bearing
status condition, with a comment in the style of the `LOAD-01` / `LOAD-03`
paragraphs directly above it explaining why a `skipped` row now interpolates and
that it is the first `skipped` partition to do so. Add a
`notification-grammar.test.ts` case proving a `skipped` message with no `cause`
still composes exactly one line.

**The cause carrier (`update-row.ts`).** Export
`constraintCauseFor(outcome: PluginUpdateSkippedOutcome): Error | undefined`,
returning `new Error(outcome.notes.join(" "))` only when
`outcome.reasons.includes(<the token>)` and `outcome.notes.length > 0`, and
`undefined` otherwise. Rewrite the module header's opening sentence: the module
is the shared row-fact leaf for BOTH update cascades, owning the `(updated)`
partition's composer and the constraint cause-line carrier the `skipped` and
`unchanged` rows read; the leaf-with-no-back-edges argument in the rest of the
header is unchanged and is exactly why the carrier lives here rather than in the
gate leaf -- the autoupdate cascade must not gain a static edge onto a module
that imports the declaration index and the tag probes.

**The manual cascade (`update-cascade.ts`).** In `projectSkippedOutcome`'s final
return, spread `...(cause !== undefined && { cause })` from
`constraintCauseFor(outcome)`. Leave `cascadeSkipSeverity` untouched and add an
inline comment above its `return skipSeverity(reasons)` line stating that the
held-update token relies on this default to reach `warning` and must never be
added to `IDEMPOTENT_REASONS`, and that this is a deliberate divergence from the
`info`-for-autoupdate split the `(updated)` partition uses (D-10-12).

**The catalog.** Add ONE fixture entry `update-held-by-dependents` to
`PLUGIN_UPDATE_FIXTURES["/claude:plugin update"]` -- `expectedSeverity:
"warning"`, `cardinality: "single"`, one marketplace block, one plugin row with
`status: "skipped"`, the target's recorded version, `reasons: [<the token>]`,
`severity: "warning"`, `needsReload: false` and a `cause` naming two declarers,
one of them disabled. Author the matching `<!-- catalog-state:
update-held-by-dependents -->` anchor and fenced block in
`docs/output-catalog.md`'s update section, writing the bytes you intend the user
to see. Update line 63's reasons paragraph: the member count becomes 62 and the
new token joins the enumerated append-only block. Then move the three derived
tripwire constants to their MEASURED values -- `EXPECTED_STATE_COUNT` 222 to
223 in `catalog-contract.test.ts`, the same count in `catalog-parser.test.ts`'s
`loadCatalogExamples parses all 222 independent catalog tuples` case (title and
assertion both), and `EXPECTED_UTF8_BYTES` to the summed byte length the
contract reports. Extend the contract file's running state-count narrative with
one sentence ending `(222 -> 223)`. The user contract is the fenced block you
authored and the `renderFailures.length === 0` assertion that drives `notify()`
against it; the three counts are tripwires derived from it, so read them off the
failing assertion rather than computing them by hand -- but never adjust the
fenced block to match rendered output.

**The remaining vocabulary pins.** Append the token to `EXPECTED_REASONS` in
BOTH `tests/shared/notification-types.test.ts` and
`tests/architecture/compat-01-no-expansion.test.ts` (tail position, matching the
union's declaration order), add it to `REASON_ENROLLMENT` in
`notify-closed-set-locks.test.ts`, move that file's length assertion and test
title from 61 to 62, and extend its narrative with one sentence ending
`(61 -> 62)`. All nine surfaces move in ONE commit or the locks and the
completeness proof fail.

**Tests first.** Write the `<behavior>` cases under the verbatim titles listed
in `<artifacts_this_phase_produces>` -- in the new leaf test, in
`update-preflight.test.ts`, in `update-cascade.test.ts`, in `update-row.test.ts`
and in `notification-grammar.test.ts` -- and watch them fail for the right
reason before implementing. The leaf test injects a `buildScopeDeclarationDetail`
double, so it needs no filesystem; `update-preflight.test.ts` injects
`constraintGate` and reuses `seedUpdate`. The leaf ships at 100% direct coverage
or `npm run test:coverage:direct:commit` fails outright -- the pin file carries
an empty exemption list.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update-constraint-gate.test.ts tests/orchestrators/plugin/update-preflight.test.ts tests/orchestrators/plugin/update-cascade.test.ts tests/orchestrators/plugin/update-row.test.ts tests/shared/notification-grammar.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports `# fail` greater than 0, or `# pass 0` (the new cases did not run at all)</fails_when>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/shared/notification-types.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/no-orchestrator-network.test.ts</automated>
    <fails_when>non-zero exit, or the contract output prints a `renderFailures` diff (the fenced catalog block and the rendered row disagree byte-for-byte), or the locks assertion still reads 61</fails_when>
    <automated>npx prettier --check docs/output-catalog.md</automated>
    <fails_when>non-zero exit, or the output lists `docs/output-catalog.md` as unformatted</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; npm run test:corresponding &amp;&amp; npm run test:coverage:direct:commit</automated>
    <fails_when>non-zero exit, or the ESLint output names a `sonarjs/cognitive-complexity` finding on `preparePluginUpdate`, or the direct-coverage report names an uncovered line or branch in `update-constraint-gate.ts`</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts tests/orchestrators/plugin/update-constraint-gate.test.ts` exits 0 after the commit.
    - `grep -c 'update-constraint-gate' tests/architecture/gate-targets.ts` prints 0 (the leaf stays out of the network-free target list, D-10-19).
    - `grep -c 'from "semver"' extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts` prints 0, and `grep -c 'intersectDependencyRanges' extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts` prints 1 or more.
    - `sed -n '/^const IDEMPOTENT_REASONS/,/as const;/p' extensions/pi-claude-marketplace/shared/notify-reasons.ts | grep -c 'dependents'` prints 0.
    - `grep -c 'Object.keys(REASON_ENROLLMENT).length, 62' tests/architecture/notify-closed-set-locks.test.ts` prints 1.
    - A leaf-test case titled `UPDT-02: holders are named in lexicographic key order` asserts the same `cause` string from two states whose `marketplaces` / `plugins` key orders are reversed.
    - A leaf-test case titled `UPDT-01: a target no installed record declares queries no tag listing` asserts `{ kind: "unconstrained" }` AND that its seam doubles recorded zero probe calls.
    - A leaf-test case titled `UPDT-02: a disabled declarer is named and marked` asserts the cause string contains the disabled declarer's key followed by the currently-disabled marking and that the enabled declarer's key is not followed by it.
    - A leaf-test case asserts two states differing only in a declarer's `provenance` produce `deepStrictEqual` verdicts (D-10-08).
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts | grep -c 'provenance'` prints 0, and the same filtered grep for `loadState` prints 0 (D-10-07 / D-10-08: the gate reads one handed-in state document and never the record's origin).
    - `grep -v '^\s*[/*]' extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts | grep -cE 'withLockedStateTransaction|transaction\.save'` prints 0 (UPDT-01 concurrency edge: the gate takes no lock and writes nothing).
    - A leaf-test case titled `UPDT-02: an unreadable declarer holds the update and names itself` asserts the returned `cause` is `===` the walk failure's own `cause.message`.
    - An `update-preflight.test.ts` case titled `D-10-03: the gate runs after triage and before candidate resolution` injects a `constraintGate` double and a `cloneCacheSeam` whose members throw, seeds a held target, and asserts the returned outcome is the `skipped` partition (the throwing seam was never reached).
    - An `update-preflight.test.ts` case titled `UPDT-02: a held update writes nothing and repeats byte-identically` reads `state.json` before and after two `preparePluginUpdate` calls and asserts the bytes are unchanged and both outcomes `deepStrictEqual`.
    - An `update-cascade.test.ts` case asserts a held outcome projects `severity: "warning"` and a `cause` whose message equals the outcome's joined notes, and a second case asserts a `{up-to-date}` skipped outcome projects a message with no `cause` key (`Object.hasOwn(msg, "cause") === false`).
    - A `notification-grammar.test.ts` case asserts `composePluginLinesWith` on a `skipped` message with no `cause` returns an array of length 1.
    - `grep -c 'catalog-state: update-held-by-dependents' docs/output-catalog.md` prints 1, and `grep -c '62-member' docs/output-catalog.md` prints 1.
    - `grep -En 'Phase [0-9]|Pitfall [0-9]|Pattern [0-9]' extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts extensions/pi-claude-marketplace/orchestrators/plugin/update-preflight.ts extensions/pi-claude-marketplace/orchestrators/plugin/update-row.ts extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts extensions/pi-claude-marketplace/shared/notification-types.ts extensions/pi-claude-marketplace/shared/notify-reasons.ts extensions/pi-claude-marketplace/shared/notification-grammar.ts` prints nothing.
  </acceptance_criteria>
  <done>A plugin held by two disjoint dependent ranges is skipped by `update &lt;plugin&gt;@&lt;mp&gt;`, its row carries the new token at warning severity with a 4-space-indented cause line naming both declarers in key order and marking the disabled one, the closed set is 62 members across all nine pinning surfaces, and the catalog renders the row byte-for-byte.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The fail-closed arm and the unconstrained regression, proven on the gate's own seams</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts,
    tests/orchestrators/plugin/update-constraint-gate.test.ts,
    tests/orchestrators/plugin/update-preflight.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts (the state after Task 2)
    - extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts (lines 118-171 -- the failure arm's contract and `unreadableDeclarer`'s message form; the header paragraph at 38-42 on why the message is built without `{ cause }` chaining)
    - extensions/pi-claude-marketplace/shared/redact-absolute-paths.ts (what the walk already applied, so the gate must not re-apply or undo it)
    - docs/dependency-resolution.md (the fail-closed paragraph describing the uninstall refusal's shape -- the model D-10-05 differs from only in skipping rather than refusing)
    - tests/orchestrators/plugin/dependency-index.test.ts (the cases that drive `buildScopeDeclarationDetail` to its `ok: false` arm, for the failure shapes the double must reproduce)
    - tests/orchestrators/plugin/update-preflight.test.ts (the state after Task 2)
  </read_first>
  <behavior>
    - Each of the walk's four refusal shapes (marketplace manifest load threw, the marketplace does not list the declarer, the declarer's own manifest is present but unusable, and the declaration parses as unusable) reaches the gate's held arm and carries the walk's message verbatim.
    - The held cause text from a refusal contains no `/` -rooted absolute path, because the walk already redacted it and the gate neither re-wraps nor re-formats it.
    - A refusal short-circuits: the gate returns before any fold, so a state whose only declarations are pathological ranges never reaches `intersectDependencyRanges`.
    - A target whose only declarer is ITSELF (a plugin that declares its own key) is unconstrained -- the self-declaration is skipped, not folded.
    - A declarer that names the target but declares no `version` contributes a holder with no range, and with no other declarer the fold sees an empty range list and the verdict is unconstrained.
    - `preparePluginUpdate` on an unconstrained target with a real (undoubled) gate returns an outcome `deepStrictEqual` to the outcome the same fixture produces with the gate bypassed entirely.
  </behavior>
  <action>
Close the leaf's remaining arms and prove the unconstrained path is untouched.
No new module, no new export beyond what Task 2 declared.

**Fail-closed (D-10-05).** Confirm the refusal arm added in Task 2 returns
`{ kind: "held", cause: failure.cause.message }` with the message passed through
unchanged, and add a comment stating WHY the gate does not re-wrap it: the walk
built that message without chaining an underlying error precisely so the
renderer's cause-chain walk cannot print a raw path behind it, and re-wrapping
would reintroduce exactly that. State the difference from the uninstall
refusal's shape in one clause -- the update is SKIPPED, not refused.

**Self-declaration and range-less declarations.** In the inversion, skip an
element whose declarer key equals the target key, with a comment saying a plugin
that declares itself constrains nothing. Keep holders whose element carries no
`version` out of the range list handed to the fold, but keep them in `holders`
for the cause line only if they ever reach it; if a holder set folds to the
wildcard the verdict is unconstrained and the holders are discarded. Make the
rule explicit in a comment: a declaration HOLDS the key whatever it names, but
only a declared RANGE constrains a version.

**The unconstrained regression (success criterion 3).** Add the
`update-preflight.test.ts` case that runs the SAME fixture twice -- once with
`constraintGate` omitted entirely (production default path with a real gate over
a state that declares nothing) and once with a `constraintGate` double that
returns `{ kind: "unconstrained" }` -- and asserts the two outcomes
`deepStrictEqual`. This is not a value compared to itself: both runs execute the
full preflight including candidate resolution, and the first run genuinely
executes the gate. Name it so the verifier can find it.

**Coverage.** Drive every remaining branch of the leaf from its paired test --
each refusal shape, the self-declaration skip, the range-less declaration, the
wildcard fold, and each of `intersectDependencyRanges`' three failure reasons.
The direct-coverage gate admits no shortfall.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update-constraint-gate.test.ts tests/orchestrators/plugin/update-preflight.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports `# fail` greater than 0</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; npm run test:corresponding &amp;&amp; npm run test:coverage:direct:commit</automated>
    <fails_when>non-zero exit, or the direct-coverage report names any uncovered line, function or branch in `update-constraint-gate.ts`</fails_when>
  </verify>
  <acceptance_criteria>
    - A leaf-test case exists for each of the four walk refusal shapes, and each asserts `verdict.cause === failure.cause.message` by identity of the string the double returned.
    - A leaf-test case asserts the held cause text from a refusal does not match `/(^|\s)\//` (no absolute path reaches the line).
    - A leaf-test case asserts a target whose only declarer is itself returns `{ kind: "unconstrained" }`.
    - A leaf-test case asserts a declarer naming the target with no `version` yields `{ kind: "unconstrained" }`.
    - Leaf-test cases cover all three `intersectDependencyRanges` failure reasons and assert three distinct arm clauses in the cause text.
    - An `update-preflight.test.ts` case asserts, by `deepStrictEqual`, that one fixture's outcome is identical with the production gate and with an injected unconstrained double.
    - `npm run test:coverage:direct:commit` reports `update-constraint-gate.ts` at 100% lines, functions and branches.
    - `grep -En 'Phase [0-9]|Pitfall [0-9]|Pattern [0-9]' extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts` prints nothing.
  </acceptance_criteria>
  <done>Every arm of the gate is reachable and covered, an unreadable declarer holds the update and names itself with the walk's own message, and an unconstrained plugin's preflight outcome is proven identical with and without the gate in the path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin manifest / marketplace entry -> declaration walk -> range fold | Author-controlled `dependencies` declarations supply the range texts the gate folds. |
| state.json record -> holder list -> cause line | Recorded plugin keys and the `enabled` bit decide who is named on a user-visible line. |
| declaration-walk failure -> cause line | A refusal message composed from a marketplace path reaches a rendered notification row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Denial of service | `update-constraint-gate.ts` range fold | medium | mitigate | The gate calls `intersectDependencyRanges` and nothing else; that function measures total input against a 4096-character cap and the projected conjunct count against a 1024 cap BEFORE any parse. A prohibition and an acceptance criterion (`from "semver"` count is 0) keep a second evaluator out. |
| T-10-02 | Information disclosure | `describeConstraint` cause line | medium | mitigate | The line interpolates only `name@marketplace` keys and ranges already passed through `renderConstraintRange`; the fail-closed arm forwards the walk's own message, which `dependency-index.ts` already built path-redacted and without cause chaining. Task 3 asserts no absolute path reaches the line. |
| T-10-03 | Spoofing | holder naming | low | mitigate | A holder's key is the walk's own `${name}@${marketplace}` composition over recorded names, not free text; the disabled marking is read from the state record rather than supplied by the declaration. |
| T-10-04 | Tampering | closed-set vocabulary | low | mitigate | The amendment lands on nine pinning surfaces in one commit; five independent gates (`notify-closed-set-locks`, `compat-01-no-expansion`, `notification-types.test`, `catalog-contract`, the `_UncoveredReason` / `_ExtraReason` proof) fail on a partial edit. |
| T-10-05 | Elevation of privilege | `composePluginLinesWith` widened gate | low | mitigate | The widening is additive: `renderIndentedCauseChain(undefined, "    ")` returns the empty string and pushes nothing, so every existing `skipped` producer -- none of which sets `cause` -- keeps byte-frozen output. An acceptance criterion asserts the one-line composition. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | This phase adds no runtime or dev dependency; RESEARCH.md records the Package Legitimacy Gate as not applicable, and no task runs a package-manager install. |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/update-constraint-gate.test.ts tests/orchestrators/plugin/update-preflight.test.ts tests/orchestrators/plugin/update-cascade.test.ts tests/orchestrators/plugin/update-row.test.ts tests/shared/notification-grammar.test.ts`
- `node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/shared/notification-types.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/no-orchestrator-network.test.ts`
- `npx prettier --check docs/output-catalog.md`
- `npm run typecheck && npm run lint && npm run fallow && npm run test:corresponding && npm run test:coverage:direct:commit`
</verification>

<success_criteria>
- UPDT-02 has a code path and rendered bytes: a plugin held by its dependents is skipped and its row names them.
- The closed reason set is 62 members, landed across nine surfaces in one commit, with the row documented as `update-held-by-dependents`.
- The gate is composed through an injected field and stays out of `NETWORK_FREE_TARGETS`; `update-flow.ts` and `update-preflight.ts` remain the only git consumers.
- The new leaf ships at 100% direct coverage with no entry in the exemption pin file.
- An unconstrained plugin's preflight outcome is byte-identical with the gate in the path.
</success_criteria>

<flagged_assumptions>
- UPDT-01 `ordering` edge probe row: resolved as a BACKSTOP rather than an explicit criterion. When two candidate release tags carry equal semver versions, the tie is settled inside `domain/release-tag.ts::selectHighestSatisfyingTag`, whose ordering contract this phase neither reads nor changes. The gate's own obligation -- that it re-sorts no candidate list and returns the probe's `pinned` arm verbatim -- lands in plan 10-02 and is asserted there; the tie-break itself stays an inherited property.
- `describeConstraint`'s arm clauses are fixed text authored in Task 2 and are user-visible. They are NOT byte-pinned by the catalog until the fixture in Task 2 renders one of them; the other two arms' clauses are covered by the leaf test's string assertions only. Plans 10-03 adds catalog states for the remaining arms.
</flagged_assumptions>

<output>
Create `.planning/phases/10-constraint-aware-update/10-01-SUMMARY.md` when done
</output>
