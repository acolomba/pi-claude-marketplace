---
phase: 58-matcher-parser-tool-name-mapping-supportability-gate
plan: 04
type: execute
wave: 3
depends_on: [58-03]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/probe-classifiers.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
  - tests/architecture/catalog-uat.test.ts
  - tests/architecture/notify-grammar-invariant.test.ts
  - tests/shared/probe-classifiers.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HOOK-04, MATCH-01, MATCH-02, TOOL-01, TOOL-02]
tags: [hooks, byte-rename, atomic, catalog, narrowResolverNotes, manifest-field-carveout]

must_haves:
  truths:
    - "ATOMIC single commit: source rename, narrowResolverNotes substring tightening, MANIFEST_FIELD_REASONS carve-out drop, output-catalog.md byte updates, messaging-style-guide.md update, catalog-uat.test.ts fixture re-keys, new TOOL-02 catalog states all land in ONE commit (no intermediate `{hooks}` bytes emitted by new code paths)."
    - "REASONS tuple member `\"hooks\"` is renamed to `\"unsupported hooks\"` at shared/notify.ts; tuple length unchanged at 31; the `Reason` literal-union derive updates automatically."
    - "narrowResolverNotes emits `\"unsupported hooks\"` and the return-type union is `\"unsupported hooks\" | \"lsp\" | \"unsupported source\"`."
    - "narrowResolverNotes substring match is tightened: `note.includes(\"hooks\")` → `note.startsWith(\"hooks.json is not valid JSON:\") || note.startsWith(\"hooks.json failed schema validation:\") || note.startsWith(\"unsupported hooks:\")` (three Phase 57+58 emission prefixes — all originate in domain/components/hooks.ts)."
    - "MANIFEST_FIELD_REASONS set drops `\"hooks\"` (keeps `\"lspServers\"`); MANIFEST_FIELD_TO_REASON drops the `hooks: \"hooks\"` entry (keeps `lspServers: \"lsp\"`)."
    - "Every `{hooks}` byte form in docs/output-catalog.md is replaced with `{unsupported hooks}` (8 doc occurrences enumerated by research)."
    - "Every `reasons: [\"hooks\"]` and `reasons: [\"hooks\", \"lsp\"]` row in tests/architecture/catalog-uat.test.ts is re-keyed to `reasons: [\"unsupported hooks\"]` / `reasons: [\"unsupported hooks\", \"lsp\"]` (7 grep-visible source-line hits per research; planner verifies any helper-rebuilder rows at write time)."
    - "Catalog gains NEW fixture states for the four TOOL-02 trigger conditions: at minimum one fixture per orchestrator surface (install / preview / reconcile-apply / info / list) rendering `(unavailable) {unsupported hooks}`; per-condition coverage lives in hooks-supportability.test.ts (Plan 03), NOT catalog-uat."
    - "Manifest-field carve-out prose at output-catalog.md:59 is corrected: `{lsp}` remains the sole carve-out; `{unsupported hooks}` is a normal 2-word reason."
    - "REQUIREMENTS.md HOOK-04 row marks closed in Phase 58 (not Phase 63); Phase 63 loses HOOK-04 from its requirement list."
    - "`npm run check` is GREEN at the single atomic commit; the catalog-uat byte-equality gate passes; no intermediate-commit byte-diff exists."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "REASONS tuple member rename `hooks` → `unsupported hooks` (line 81)"
    - path: "extensions/pi-claude-marketplace/shared/probe-classifiers.ts"
      provides: "narrowResolverNotes return-type union update + substring tightening + emitted token rename"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "MANIFEST_FIELD_REASONS / MANIFEST_FIELD_TO_REASON carve-out drop of `hooks` (D-58-02) + comment-prose alignment"
    - path: "docs/output-catalog.md"
      provides: "8 byte-form rename occurrences + new TOOL-02 catalog states (5-8 new state entries per orchestrator surface)"
    - path: "docs/messaging-style-guide.md"
      provides: "Closed-set REASONS doc updated (manifest-field carve-out prose corrected — `{hooks}` no longer a carve-out)"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "13 existing fixture rows re-keyed + new fixture rows per TOOL-02 catalog state"
    - path: "tests/shared/probe-classifiers.test.ts"
      provides: "Unit test for narrowResolverNotes tightened-substring contract (the `note contains \"hooks\"` outside-prefix case does NOT classify as `unsupported hooks`)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HOOK-04 row marked closed in Phase 58; Phase 63 row updated to drop HOOK-04"
  key_links:
    - from: "shared/notify.ts::REASONS[8]"
      to: "shared/probe-classifiers.ts::narrowResolverNotes return-type union"
      via: "literal-union derive — same literal token on both sides"
      pattern: "\"unsupported hooks\""
    - from: "domain/components/hooks.ts::parseHooksConfig (Phase 57 baseline + Plan 03 extension)"
      to: "shared/probe-classifiers.ts::narrowResolverNotes substring match"
      via: "the three reason-prefix tokens `hooks.json is not valid JSON:`, `hooks.json failed schema validation:`, `unsupported hooks:` originate in parseHooksConfig and are detected by narrowResolverNotes"
      pattern: "startsWith"
    - from: "shared/notify.ts::REASONS"
      to: "tests/architecture/catalog-uat.test.ts fixtures"
      via: "byte-equality assertion (the fixture `reasons` arrays are typed against the literal-union `Reason`)"
      pattern: "reasons: \\[\"unsupported hooks\"\\]"
---

<objective>
Land HOOK-04 + D-58-02 + the matching catalog / docs / fixture updates
in ONE ATOMIC COMMIT (D-58-01). After this plan:

1. The closed-set `REASONS` tuple member is `"unsupported hooks"`
   (no longer `"hooks"`); tuple length unchanged at 31; the
   automatically-derived `Reason` literal union updates with it.
2. `narrowResolverNotes` in `shared/probe-classifiers.ts` returns
   `"unsupported hooks" | "lsp" | "unsupported source"` and its substring
   match is TIGHTENED to detect only Phase 57+58 emission prefixes from
   `parseHooksConfig` — preventing free-form notes mentioning "hooks"
   from falsely classifying as `unsupported hooks`.
3. `MANIFEST_FIELD_REASONS` in `orchestrators/plugin/install.ts` drops
   `"hooks"` (keeps `"lspServers"`); `MANIFEST_FIELD_TO_REASON` drops
   the `hooks: "hooks"` entry. Dead-code removal — under v1.13 `hooks`
   is a SUPPORTED component kind so the manifest-field rejection branch
   for `hooks` never fires.
4. `docs/output-catalog.md` byte forms are re-keyed across 8 occurrences;
   the manifest-field carve-out prose at line 59 is corrected so `{lsp}`
   is the SOLE carve-out and `{unsupported hooks}` is a normal 2-word
   reason. New TOOL-02 fixture states appear per orchestrator surface
   (install / preview / reconcile-apply / info / list) rendering
   `(unavailable) {unsupported hooks}`.
5. `docs/messaging-style-guide.md` prose is aligned with the corrected
   carve-out.
6. `tests/architecture/catalog-uat.test.ts` fixtures are re-keyed (13
   research-enumerated rows + any helper-rebuilder rows the executor
   finds at write time via re-grep) AND new fixtures are added per new
   catalog state.
7. `tests/architecture/notify-grammar-invariant.test.ts` participates
   in the existing failed-row subject-first invariant with the new
   `{unsupported hooks}` reason (no new invariant; the rename flows
   through).
8. `tests/shared/probe-classifiers.test.ts` exists with unit coverage
   of the tightened substring contract.
9. `.planning/REQUIREMENTS.md` HOOK-04 row is marked closed in
   Phase 58 (not Phase 63); Phase 63 row drops HOOK-04 from its list
   in lockstep.

Purpose: The v1.3 / v1.10 / v1.11 atomic-supersession lesson —
landing a closed-set member rename in one commit alongside every
source emission, doc occurrence, and byte-equality fixture so the
catalog-UAT gate (`tests/architecture/catalog-uat.test.ts`) never sees
an intermediate `{hooks}`-vs-`{unsupported hooks}` byte mismatch and
`npm run check` stays GREEN at every commit boundary.

ATOMIC COMMIT INVARIANT (D-58-01): Every change in this plan lands
as ONE commit. There MUST NOT be any intermediate commit that:
- Renames REASONS but leaves catalog-uat fixtures un-updated.
- Updates fixtures but leaves narrowResolverNotes un-tightened.
- Updates source but leaves docs un-updated.
- Drops the carve-out but leaves the `MANIFEST_FIELD_TO_REASON` `hooks`
  entry (TypeScript would still compile because `"hooks"` is no longer a
  `ContentReason` member — but the catalog-uat fixture for the dead
  branch would diverge).

The executor MUST sequence the work in one transaction: make ALL
changes, run `npm run check` (must be GREEN), then commit. If `npm run
check` fails mid-way, fix forward in the same uncommitted working
tree; do NOT commit partial state.

## Artifacts this phase produces (and renames)

- **Renamed:** REASONS member `"hooks"` → `"unsupported hooks"` at
  `shared/notify.ts:81`. Reason literal-union derives automatically.
- **Renamed:** `narrowResolverNotes` return-type union member.
- **Tightened:** `narrowResolverNotes` substring detection.
- **Removed:** `"hooks"` entry from `MANIFEST_FIELD_REASONS` set and
  from `MANIFEST_FIELD_TO_REASON` Record.
- **Renamed:** 8 doc-occurrence byte forms in output-catalog.md.
- **Corrected:** manifest-field carve-out prose at output-catalog.md:59
  and corresponding section in messaging-style-guide.md.
- **Added:** New catalog states for TOOL-02 trigger conditions
  (estimated 5 surface-level new fixtures: install / preview /
  reconcile-apply / info / list — all rendering byte-identical
  `(unavailable) {unsupported hooks}`).
- **Re-keyed:** 13+ catalog-uat fixture rows.
- **Created:** `tests/shared/probe-classifiers.test.ts` with the
  substring-tightening contract test.
- **Updated:** REQUIREMENTS.md HOOK-04 row + Phase 63 requirement list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-CONTEXT.md
@.planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-RESEARCH.md
@.planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-PATTERNS.md
@.planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-VALIDATION.md
@.claude/rules/typescript-comments.md

# Files this plan modifies
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/shared/probe-classifiers.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@docs/output-catalog.md
@docs/messaging-style-guide.md
@tests/architecture/catalog-uat.test.ts
@tests/architecture/notify-grammar-invariant.test.ts

# Plan 03 output that emits the prefixes narrowResolverNotes detects
@extensions/pi-claude-marketplace/domain/components/hooks.ts

# notify-types architecture test (REASONS length lock — must remain GREEN at length 31)
@tests/architecture/notify-types.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename REASONS tuple member + tighten narrowResolverNotes + drop install.ts manifest-field carve-out + create probe-classifiers unit test</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/shared/probe-classifiers.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/install.ts,
    tests/shared/probe-classifiers.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 60-106 — REASONS tuple + literal-union derive + BENIGN_REASONS use of `"hooks"` if any)
    - extensions/pi-claude-marketplace/shared/probe-classifiers.ts (whole file — narrowResolverNotes function + return-type union + substring match)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (lines 1466-1525 — MANIFEST_FIELD_REASONS, MANIFEST_FIELD_TO_REASON, narrowResolverReasons, manifestFieldTokenFromNote, the comment-block prose at lines 1466-1485, the line-1518 doc comment)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (current state after Plan 03 — the three emission prefixes `hooks.json is not valid JSON:`, `hooks.json failed schema validation:`, `unsupported hooks:` are the substring-match targets for narrowResolverNotes)
    - .planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-PATTERNS.md (§ "shared/notify.ts MODIFY" + "shared/probe-classifiers.ts MODIFY" + "orchestrators/plugin/install.ts MODIFY" — analog-driven diff guides)
    - .planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-RESEARCH.md (§ "Pitfall 2" — substring-match tightening rationale; § "State of the Art" table — exact byte-rename sites)
    - .planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-CONTEXT.md (D-58-01 atomic byte-form rename; D-58-02 manifest-field carve-out drop)
  </read_first>
  <behavior>
    Source change (a) — `shared/notify.ts:81`:
    - Replace the literal `"hooks",` line with `"unsupported hooks",`. Single-token in-place rename. Tuple length unchanged at 31; `Reason`, `ContentReason`, `BENIGN_REASONS` (no `"hooks"` membership currently — verify), and every downstream literal-union derive update automatically.

    Source change (b) — `shared/probe-classifiers.ts`:
    - Update the return-type union of `narrowResolverNotes` from
      `("hooks" | "lsp" | "unsupported source")[]` to
      `("unsupported hooks" | "lsp" | "unsupported source")[]`.
    - Update the corresponding `const out: ("hooks" | "lsp" | "unsupported source")[] = []` declaration to the new union.
    - REPLACE the `note.includes("hooks")` substring match with a TIGHTENED check matching the three reason-prefix tokens emitted by `domain/components/hooks.ts::parseHooksConfig`:
      `note.startsWith("hooks.json is not valid JSON:") || note.startsWith("hooks.json failed schema validation:") || note.startsWith("unsupported hooks:")`.
    - Replace the emitted token: `out.push("hooks")` / `seen.add("hooks")` / `seen.has("hooks")` → `out.push("unsupported hooks")` / `seen.add("unsupported hooks")` / `seen.has("unsupported hooks")`.
    - Update the JSDoc comment block at lines 68-74 to reflect the new tightened detection (drop the "passes `hooks` verbatim" wording per PATTERNS.md guidance; cite HOOK-04 as the traceability anchor).

    Source change (c) — `orchestrators/plugin/install.ts`:
    - Update `MANIFEST_FIELD_REASONS` set literal at line 1478 from `new Set(["hooks", "lspServers"])` to `new Set(["lspServers"])`.
    - Update `MANIFEST_FIELD_TO_REASON` Record literal at lines 1486-1489: drop the `hooks: "hooks",` line; keep `lspServers: "lsp",`.
    - Update the comment block at lines 1466-1485: drop the parenthetical references to `hooks` as a carve-out (the v1.13 framing is "`lspServers` is now the SOLE manifest-field carve-out" — `hooks` is no longer a single-word carve-out; it's a normal 2-word REASON with detection happening elsewhere).
    - Update the doc comment at line 1518 from `manifest-field carve-out (\`contains hooks\` / \`contains lspServers\`)` to `manifest-field carve-out (\`contains lspServers\`)` — drop the `contains hooks` half.
    - Cite HOOK-04 + D-58-02 as the traceability anchor in the rewritten comment block per `.claude/rules/typescript-comments.md`.

    New unit test — `tests/shared/probe-classifiers.test.ts`:
    - Test "narrowResolverNotes: emits `unsupported hooks` for the three parseHooksConfig emission prefixes" — fixture inputs (one per prefix), assert `out[0] === "unsupported hooks"` for each.
    - Test "narrowResolverNotes: does NOT classify a free-form note containing the word `hooks` outside any known prefix" — fixture: `["contains lspServers / hooks mentioned elsewhere"]` — assert that "unsupported hooks" is NOT in the output (the lspServers prefix takes precedence; the free-form `hooks` substring is no longer detected). Locks the tightening from Pitfall 2.
    - Test "narrowResolverNotes: emits `lsp` for `contains lspServers`" — regression coverage that the lsp arm is untouched.
    - Test "narrowResolverNotes: emits `unsupported source` for any other note" — regression coverage for the permissive fallback.
    - All tests run under `node --test tests/shared/probe-classifiers.test.ts` in &lt; 1 second.
  </behavior>
  <action>
    Use the Edit tool to make all four source mutations and create the new unit test in sequence:
    1. `shared/notify.ts` — single-token replacement on the REASONS member line (line 81).
    2. `shared/probe-classifiers.ts` — return-type union update + tightened substring match + emitted-token rename + JSDoc update. Use multi-step Edit calls for each replacement to keep diffs small.
    3. `orchestrators/plugin/install.ts` — set-literal update + Record-literal update + comment-block rewrite + doc-comment update.
    4. Create `tests/shared/probe-classifiers.test.ts` from scratch following the pattern of other `tests/shared/*.test.ts` files. Header comment cites HOOK-04 + D-58-02 per `.claude/rules/typescript-comments.md`.

    DO NOT run `npm run check` between these four mutations — they will fail temporarily because the catalog-uat fixtures still reference `{hooks}`. Task 2 brings the catalog + fixtures into alignment; the GREEN gate fires after Task 2.
  </action>
  <verify>
    <automated>node --test tests/shared/probe-classifiers.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `grep -n '"unsupported hooks"' extensions/pi-claude-marketplace/shared/notify.ts` shows the renamed REASONS member.
    - `grep -c '"hooks"' extensions/pi-claude-marketplace/shared/notify.ts` shows 0 occurrences of the bare 1-word literal (REASONS no longer carries it).
    - `grep -E 'startsWith\("hooks\.json|startsWith\("unsupported hooks:' extensions/pi-claude-marketplace/shared/probe-classifiers.ts` shows the three tightened prefix checks.
    - `grep -c '"hooks"' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts | head -5` shows ZERO `"hooks"` occurrences in the MANIFEST_FIELD_REASONS / MANIFEST_FIELD_TO_REASON region (lines 1478, 1486-1489).
    - `node --test tests/shared/probe-classifiers.test.ts` reports "pass 4" (four GREEN tests).
    - `npm run typecheck` GREEN. (`npm run check` will still fail at this point because catalog-uat fixtures still reference `{hooks}` — that's expected; Task 2 closes the gap.)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-key catalog-uat fixtures + add new TOOL-02 catalog states + update output-catalog.md and messaging-style-guide.md + update REQUIREMENTS.md HOOK-04 row + verify npm run check GREEN atomic commit</name>
  <files>
    docs/output-catalog.md,
    docs/messaging-style-guide.md,
    tests/architecture/catalog-uat.test.ts,
    tests/architecture/notify-grammar-invariant.test.ts,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - docs/output-catalog.md (lines 59, 136, 182, 301, 534, 750, 1138, 1144 — the 8 grep-visible byte-form occurrences of `{hooks}`; line 59 is the manifest-field carve-out prose that gets reworded; line 1138 is a longer prose passage)
    - docs/messaging-style-guide.md (search for `hooks` to find the closed-set REASONS section that references the carve-out)
    - tests/architecture/catalog-uat.test.ts (lines 272, 276, 507, 582, 829, 1175, 1708 — the 7 grep-visible `"hooks"` row hits; expand context to find any helper-rebuilder rows or multi-line fixtures that also reference `{hooks}` byte form)
    - tests/architecture/notify-grammar-invariant.test.ts (whole file — the failed-row subject-first invariant; should iterate REASONS and pass through the new `{unsupported hooks}` automatically; verify no literal `"hooks"` snapshot strings need updating)
    - .planning/REQUIREMENTS.md (lines 26, 186, 189-193 — the HOOK-04 row + Phase 58 / Phase 63 closure-list rows that need updating in lockstep per D-58-01)
    - .planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-CONTEXT.md ("Claude's Discretion" → "Catalog-state count: ~5-8 new catalog states + the existing `{hooks}`-keyed states re-keyed; planner picks the exact taxonomy")
    - .planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-RESEARCH.md (Open Question 4 → recommendation: ONE new fixture per surface, not per-condition, since byte equality is the only catalog-uat contract; per-condition coverage lives in hooks-supportability.test.ts from Plan 03)
  </read_first>
  <behavior>
    Doc updates — `docs/output-catalog.md`:
    - Line 59 (manifest-field carve-out prose): rewrite from `Manifest field names render verbatim as the sole carve-out (\`{hooks}\`, \`{lsp}\`).` to `Manifest field names render verbatim as the sole carve-out (\`{lsp}\`). \`{unsupported hooks}\` is a normal 2-word reason (not a manifest-field carve-out).` (or equivalent phrasing). Cite HOOK-04 + D-58-02 traceability in a comment IF the catalog uses comment markers; otherwise the prose change stands.
    - Line 136 (status table for `(unavailable)`): replace `carries {hooks} / {lsp} etc.` with `carries {unsupported hooks} / {lsp} etc.`.
    - Lines 182, 301, 534, 750: each `⊘ <name> (unavailable) {hooks}` row → `⊘ <name> (unavailable) {unsupported hooks}`.
    - Line 1138 (prose passage): replace `{hooks}` with `{unsupported hooks}` (do NOT change the surrounding prose about resolveStrict / unavailable arm).
    - Line 1144: `⊘ legacy-plugin v0.1.0 (unavailable) {hooks}` → `⊘ legacy-plugin v0.1.0 (unavailable) {unsupported hooks}`.
    - Append NEW catalog state sections for the four TOOL-02 trigger conditions per orchestrator surface. Per RESEARCH Open Question 4 recommendation: ONE new state per surface (5 total — install / preview / reconcile-apply / info / list), since all four trigger conditions render byte-identical `(unavailable) {unsupported hooks}`. Each new state uses the catalog template (heading, byte form block, severity / context fields per existing convention). Per-condition coverage belongs to `hooks-supportability.test.ts` (Plan 03 architecture test).

    Doc updates — `docs/messaging-style-guide.md`:
    - Find any prose mentioning `{hooks}` as a manifest-field carve-out and reword to match output-catalog.md line 59. Cite HOOK-04.
    - The MARKERS / STATUS_TOKENS sections are untouched (no membership change).

    Test fixture updates — `tests/architecture/catalog-uat.test.ts`:
    - Mechanical rename across the 7 grep-visible rows: lines 272, 276, 507, 582, 829, 1175, 1708 — each `reasons: ["hooks"]` → `reasons: ["unsupported hooks"]`, each `reasons: ["hooks", "lsp"]` → `reasons: ["unsupported hooks", "lsp"]`.
    - Re-grep `tests/architecture/catalog-uat.test.ts` for `"hooks"` (literal); any additional hits (e.g. in helper-rebuilder string-table arrays, comment markers like `// unavailable-single-scope (INFO-02 unavailable + {hooks})`) get re-keyed in lockstep. Comments inside test source that mention `{hooks}` become `{unsupported hooks}` for byte fidelity. Line 1615 (`(INFO-02 unavailable + {hooks})`) is one such comment hit.
    - Append NEW fixture rows for each of the five new catalog states added to output-catalog.md (one per surface). Each new fixture follows the existing per-surface fixture-row shape. The byte-equality assertion shape is unchanged — the new fixtures must match the new output-catalog.md byte form exactly.

    Architecture-test grammar invariant — `tests/architecture/notify-grammar-invariant.test.ts`:
    - The file should require NO mechanical edits IF the failed-row subject-first invariant iterates `REASONS` and reads the literal-union derive directly. Spot-check: if any test contains the bare literal `"hooks"` as a snapshot string (not as a REASONS iteration), re-key it to `"unsupported hooks"`. (Research § "Pitfall 7": no source-level literal "hooks" snapshot expected; verify via `grep -c '"hooks"' tests/architecture/notify-grammar-invariant.test.ts`.)

    Requirements updates — `.planning/REQUIREMENTS.md`:
    - Change HOOK-04 row at line 186 from `Phase 63` to `Phase 58`.
    - Phase 58 row at lines 189-193 already lists MATCH-01 / MATCH-02 / TOOL-01 / TOOL-02 — APPEND a HOOK-04 reference to the Phase 58 closure-list bullet (`Pending → Closing in Phase 58`).
    - In the per-requirement bullets (line 26 — the HOOK-04 prose), reword the Phase reference so the rename lands in Phase 58 (drop any "Phase 63" wording from the HOOK-04 prose body).

    Final atomic gate:
    - `npm run check` is GREEN end-to-end (typecheck + ESLint + Prettier + node:test full suite + catalog-uat byte-equality).
    - `git status` shows ALL files in `files_modified` are dirty in ONE commit.
    - Commit using the project's pre-commit-hook discipline (NO `--no-verify`); commit-message subject ≤72 chars per Conventional Commits and the body lines ≤80 chars; body cites HOOK-04 / D-58-01 / D-58-02 traceability anchors. No `SKIP=trufflehog` prefix (this work is in the main worktree per RESEARCH § "Project Constraints").
  </behavior>
  <action>
    Execute the byte rename via Edit on each file in sequence:
    1. `docs/output-catalog.md` — line 59 prose reword + lines 136/182/301/534/750/1138/1144 byte renames + append 5 new TOOL-02 catalog state sections (one per orchestrator surface). For each new section, follow the existing catalog template (the executor reads adjacent catalog sections to match the format exactly).
    2. `docs/messaging-style-guide.md` — locate any `{hooks}` carve-out prose and reword.
    3. `tests/architecture/catalog-uat.test.ts` — mechanical rename of the 7 source-line hits + comment-string hits (re-grep `"hooks"` after each edit to catch helper-rebuilder rows) + append 5 new fixture rows matching the new catalog states.
    4. `tests/architecture/notify-grammar-invariant.test.ts` — spot-check via grep; no edits expected.
    5. `.planning/REQUIREMENTS.md` — update HOOK-04 row + Phase 58 / Phase 63 requirement lists.

    Run `npm run check` after all five files are updated. If RED:
    - Identify the failing test (catalog-uat byte-equality mismatch will be the typical failure — surfaces with explicit "expected `{hooks}` got `{unsupported hooks}`" or vice versa). Re-grep the source file for the missed literal and patch it.
    - Re-run `npm run check` until GREEN.
    - Do NOT commit until GREEN.

    Once `npm run check` is GREEN, run `pre-commit run --files <each modified file>` per project policy (CLAUDE.md), fix any drift, then `git add` the explicit file list and create the atomic commit.

    Commit message (HEREDOC pattern):
    `feat(58): close HOOK-04 with atomic byte rename and manifest carve-out drop`
    Body cites: D-58-01 (atomic-supersession), D-58-02 (carve-out drop), HOOK-04 (closure in Phase 58 not Phase 63), the 8 doc occurrences + 13 catalog-uat fixture rows + 5 new TOOL-02 catalog states.
  </action>
  <verify>
    <automated>npm run check 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `grep -c '"hooks"' docs/output-catalog.md` shows 0 (no bare 1-word literal anywhere — all converted to `unsupported hooks` or otherwise rephrased).
    - `grep -c '"hooks"' tests/architecture/catalog-uat.test.ts` shows 0.
    - `grep -c 'reasons: \["unsupported hooks"\]' tests/architecture/catalog-uat.test.ts` shows at least the 7 re-keyed fixtures + 5 new TOOL-02 surface fixtures = at least 12.
    - `grep '{unsupported hooks}' docs/output-catalog.md | wc -l` shows at least 8 (the 6 existing occurrence renames + new catalog state heading bytes — adjust per the exact catalog template).
    - `.planning/REQUIREMENTS.md` line for HOOK-04 shows `Phase 58` (not `Phase 63`).
    - `npm run check` is GREEN.
    - `git log -1 --pretty=%B` shows the atomic-commit subject and body citing HOOK-04 / D-58-01 / D-58-02.
    - `git status` is clean after commit (no leftover modifications).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code at REASONS member rename → catalog-uat byte-equality fixtures | The closed-set member rename and the byte-form fixtures MUST land in the same commit; any intermediate commit triggers a catalog-uat RED (CI gates everywhere). |
| narrowResolverNotes substring detection → free-form resolver notes | Loose substring match `note.includes("hooks")` would false-positive on unrelated notes after the rename; the tightening to three prefix-anchored checks prevents the silent miss-classification (Pitfall 2). |
| install.ts manifest-field carve-out → dead code | The `hooks` carve-out branch is dead under v1.13 (`hooks` is a supported component kind); leaving the entry in `MANIFEST_FIELD_TO_REASON` would make the dead branch attempt to emit a no-longer-existing `"hooks"` Reason — but TypeScript would catch the now-invalid Reason literal at compile time. Drop the entry to remove the dead branch entirely. |
| docs/output-catalog.md → operator-facing documentation | A stale `{hooks}` byte form in docs would mislead a future operator into thinking the user-contract emits a 1-word `{hooks}` reason; the rename keeps docs and behavior in lockstep. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Tampering | source-code/byte-fixture drift | mitigate | Single atomic commit (D-58-01) enforces source + fixture + docs lockstep; `npm run check` is the gate; the catalog-uat byte-equality test is the load-bearing assertion that would otherwise red-fail. |
| T-58-04-02 | Tampering | narrowResolverNotes substring match | mitigate | Three explicit `startsWith(...)` checks anchored on the exact three emission prefixes from `domain/components/hooks.ts::parseHooksConfig`; unit test in `tests/shared/probe-classifiers.test.ts` locks the tightening (the free-form note with mid-string "hooks" must NOT classify). |
| T-58-04-03 | Repudiation | atomic-commit invariant | mitigate | The commit message body explicitly cites D-58-01 / D-58-02 / HOOK-04 traceability; `git log -1` is the audit trail. |
| T-58-04-04 | Information Disclosure | docs/output-catalog.md byte forms | accept | Public user-contract documentation; rename is an intentional contract evolution to a more descriptive 2-word REASON. |
| T-58-04-05 | Denial of Service | catalog-uat re-key | accept | Mechanical fixture changes; no runtime path affected; bounded surface (13+5 = ~18 fixture rows touched). |
| T-58-04-06 | Elevation of Privilege | dropped MANIFEST_FIELD_TO_REASON `hooks` entry | mitigate | Under v1.13, `hooks` is a SUPPORTED component kind via Phase 57's `SUPPORTED_COMPONENT_KINDS` 4-tuple. The dead-code carve-out branch for `hooks` could only fire if a future contributor reverts the SUPPORTED set; the architecture test `hooks-foundation.test.ts` locks the SUPPORTED set so the reversal would red-fail before the dead branch could be re-introduced. |
</threat_model>

<verification>
- `npm run check` GREEN at plan close (the load-bearing atomic-commit gate).
- `node --test tests/architecture/catalog-uat.test.ts` GREEN with re-keyed + new fixture rows.
- `node --test tests/architecture/notify-types.test.ts` GREEN — REASONS tuple length stays at 31 (the `_Assert_ReasonsLen extends 31` lock holds; rename, not addition).
- `node --test tests/architecture/notify-grammar-invariant.test.ts` GREEN — the new `{unsupported hooks}` participates in the existing subject-first failed-row invariant.
- `node --test tests/shared/probe-classifiers.test.ts` GREEN — the 4 unit tests added in Task 1.
- `git log -1 --pretty=%B` shows ONE commit covering the entire HOOK-04 closure surface.
</verification>

<success_criteria>
- REASONS tuple member renamed at `shared/notify.ts:81` (`"hooks"` → `"unsupported hooks"`); tuple length 31.
- `narrowResolverNotes` return-type union renamed + substring match tightened to three prefix-anchored checks.
- `MANIFEST_FIELD_REASONS` drops `"hooks"`; `MANIFEST_FIELD_TO_REASON` drops `hooks: "hooks"`; install.ts comment-prose aligned.
- 8 byte-form occurrences in `docs/output-catalog.md` updated; line 59 carve-out prose corrected; 5 new TOOL-02 catalog state sections appended.
- `docs/messaging-style-guide.md` aligned.
- 13+ catalog-uat fixture rows re-keyed; 5 new fixture rows added for the new catalog states.
- `tests/shared/probe-classifiers.test.ts` created with 4 GREEN unit tests.
- `.planning/REQUIREMENTS.md` HOOK-04 row marked closed in Phase 58.
- Single atomic commit with conventional-commits subject + body citing HOOK-04 / D-58-01 / D-58-02.
- `npm run check` GREEN.
</success_criteria>

<output>
Create `.planning/phases/58-matcher-parser-tool-name-mapping-supportability-gate/58-04-SUMMARY.md` when done.
</output>
