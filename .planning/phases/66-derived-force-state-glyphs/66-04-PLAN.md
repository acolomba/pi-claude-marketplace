---
phase: 66-derived-force-state-glyphs
plan: 04
type: execute
wave: 2
depends_on: ["66-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
  - tests/orchestrators/reconcile/pending.test.ts
autonomous: true
requirements: [FSTAT-06]
must_haves:
  truths:
    - "A reconcile pending `will install` row whose planned plugin re-resolves `unsupported` (no-network cache) renders `(will force install)` instead of `(will install)`."
    - "A planned install that resolves `installable` still renders `(will install)` (no force modifier)."
    - "The reconcile planner (`plan.ts`) stays pure — supportability resolution happens only in the non-pure `pending` orchestrator (planner-purity gate stays GREEN)."
    - "The candidate supportability is resolved with no network access (NFR-5)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts"
      provides: "renderWillInstall renders `(will force install)` when the row carries force"
      contains: "will force install"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts"
      provides: "no-network resolveStrict of each pluginsToInstall entry feeding the force flag"
      contains: "resolveStrict"
  key_links:
    - from: "orchestrators/reconcile/pending.ts"
      to: "orchestrators/reconcile/notify.ts buildReconcilePendingNotification"
      via: "force lookup passed into the will-install row builder"
      pattern: "buildReconcilePendingNotification"
    - from: "orchestrators/reconcile/reconcile.messaging.ts renderWillInstall"
      to: "PluginWillInstallMessage.force"
      via: "force modifier -> (will force install) label"
      pattern: "will force install"
---

<objective>
Implement FSTAT-06's preview token on the only pending/preview surface in the
codebase: the reconcile pending diff. This resolves Open Question Q3.

Q3 resolution (locked): the reconcile pending diff emits `will install` /
`will uninstall` / `will enable` / `will disable` and models NO update action —
there is no `will update` token anywhere (confirmed by exhaustive grep across
`extensions/`, `tests/`, `docs/`, and the PRD). The `will force install` half maps
cleanly onto the existing `will install` row via the `force?: boolean` render
modifier added to `PluginWillInstallMessage` in 66-01 (no new closed-set token —
consistent with the locked +2 tripwire bump). The producer is the non-pure
`pending` orchestrator, which resolves each `pluginsToInstall` candidate through
the existing no-network `resolveStrict` cache path and stamps `force: true` when it
re-resolves `unsupported` (the honest preview of degrade-not-block apply).

`will force update` has NO preview surface in the current architecture (reconcile
performs no updates; no update dry-run exists; no `will update` token to modify).
Per the locked +2 closed-set bump it is NOT added as a new token. This plan
implements the reachable `will force install` half in full; the `will force update`
surface gap is reported to the orchestrator as a documented architectural fact, not
a silent omission.

Purpose: preview a forced load-time install as `will force install`.
Output: a force-modified reconcile pending renderer + producer + unit coverage.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-derived-force-state-glyphs/66-CONTEXT.md
@.planning/phases/66-derived-force-state-glyphs/66-RESEARCH.md
@.planning/phases/66-derived-force-state-glyphs/66-PATTERNS.md
@extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render `(will force install)` from the force modifier and produce it from a no-network candidate resolve</name>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts (the file being modified): `renderWillInstall` (~82, the `(will install)` joinTokens row), `PENDING_STATUSES`/`PENDING_CONTEXT` (~66-118)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (the file being modified): `buildReconcilePendingNotification` (~208), the `pluginsToInstall` -> `status: "will install"` loop (~231-238)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts (the file being modified): the `planReconcile` call + `buildReconcilePendingNotification(plans)` call (~202), the NFR-5 no-network discipline header, and the per-scope plan accumulation
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts `availableRowMessage` (~350): the `resolveStrict(manifestEntry, { marketplaceRoot })` no-network call shape to reuse
    - shared/notify.ts: `PluginWillInstallMessage.force` (added in 66-01) and the central will-install arm rendering `(will force install)`
  </read_first>
  <action>
    (a) `reconcile.messaging.ts::renderWillInstall`: emit `"(will force install)"` when
    the row's `force === true`, else `"(will install)"` (mirror the central will-install
    arm extended in 66-01). No new `PENDING_STATUSES` token — `force` is a modifier on
    the existing `will install` row (consistent with the locked +2 bump).
    (b) `notify.ts::buildReconcilePendingNotification`: accept an optional force lookup
    (e.g. a `ReadonlySet<string>` keyed by `(scope, marketplace, plugin)`, or a
    pre-resolved `force` flag carried on each `PlannedPluginInstall`) and stamp
    `force: true` on the `pluginsToInstall` will-install child row when the entry is in
    the lookup.
    (c) `pending.ts`: BEFORE calling `buildReconcilePendingNotification(plans)`, resolve
    each `pluginsToInstall` entry's supportability via the existing no-network
    `resolveStrict` cache path (derive `marketplaceRoot` from the recorded marketplace
    clone the same way the apply path does); build the force lookup from entries whose
    `resolved.state === "unsupported"`; pass it into `buildReconcilePendingNotification`.
    The resolution MUST live in `pending.ts` (the non-pure orchestrator), NOT in
    `plan.ts` — `tests/architecture/reconcile-planner-purity.test.ts` must stay GREEN,
    and `tests/architecture/no-orchestrator-network.test.ts` must stay GREEN (NFR-5:
    cache only, no git op). Keep the single-`notify()`-per-invocation IL-2 discipline.
    Use `FSTAT-06`/`NFR-5` IDs in comments. (Byte wording is finalized in Phase 70.)
    Add a `D-66-04`/`FSTAT-06` comment in `reconcile.messaging.ts` recording that
    `will force update` has no current preview surface (reconcile performs no updates;
    no `will update` token exists) and is therefore not emitted here.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p extensions/pi-claude-marketplace && node --test tests/architecture/reconcile-planner-purity.test.ts tests/architecture/no-orchestrator-network.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `renderWillInstall` emits `(will force install)` iff the row carries `force === true`, else `(will install)`.
    - `pending.ts` calls `resolveStrict` for `pluginsToInstall` entries via the cache path; `grep -c 'resolveStrict' extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts` >= 1.
    - `plan.ts` is unchanged for resolution; `node --test tests/architecture/reconcile-planner-purity.test.ts tests/architecture/no-orchestrator-network.test.ts` exits 0.
    - A `D-66-04`/`FSTAT-06` comment documents the `will force update` surface gap.
    - `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>The reconcile pending diff renders `will force install` for a planned install that would degrade, resolved no-network, with planner purity intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile pending unit coverage for will-force-install</name>
  <files>tests/orchestrators/reconcile/pending.test.ts</files>
  <read_first>
    - tests/orchestrators/reconcile/pending.test.ts (the file being modified): existing `will install` pending-row cases (seeding a config-declared-but-unrecorded plugin) and the injected no-network resolve harness
    - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts: the producer from Task 1
    - 66-VALIDATION.md "Per-Task Verification Map" FSTAT-06 row + "Wave 0 Requirements" pending/preview entry
  </read_first>
  <action>
    Add reconcile pending cases (titles `FSTAT-06`):
    (1) a config-declared-but-unrecorded plugin whose cached candidate resolves
    `unsupported` renders a `(will force install)` row.
    (2) a config-declared-but-unrecorded plugin whose candidate resolves `installable`
    still renders `(will install)`.
    (3) assert the resolution used the injected no-network cache resolver and that no
    git/network op fired (reuse the no-network assertion harness).
    Do NOT add `<!-- catalog-state: -->` blocks to `docs/output-catalog.md` (Phase 70
    boundary) — prove the byte form through the pending orchestrator's `notify()` output.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/pending.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the `(will force install)` row for an unsupported planned install; another asserts `(will install)` for an installable one; both pass.
    - A test asserts no network/git op fired during pending resolution.
    - `docs/output-catalog.md` is byte-unchanged (`git diff --quiet docs/output-catalog.md`).
    - `node --test tests/orchestrators/reconcile/pending.test.ts` exits 0.
  </acceptance_criteria>
  <done>The will-force-install preview path is locked by unit tests, no-network and catalog-untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace cache tree -> `resolveStrict` (pending) | Each planned install's cached source tree is resolved (no network) to decide the force modifier. |
| reconcile plan -> pending notification | The pure plan plus the pending-orchestrator force lookup compose one `notify()` payload (IL-2). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-07 | Information disclosure | Network fetch during a read-only preview leaking activity / violating NFR-5 | mitigate | Reuse `resolveStrict` (cache only); `no-orchestrator-network.test.ts` grep-gate + a unit no-network assertion enforce it. |
| T-66-08 | Tampering | Planner purity broken by adding resolution to `plan.ts` | mitigate | Resolution lives only in the non-pure `pending.ts`; `reconcile-planner-purity.test.ts` stays GREEN. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase. |
</threat_model>

<verification>
- `npm run check` GREEN; `node --test tests/orchestrators/reconcile/pending.test.ts tests/architecture/reconcile-planner-purity.test.ts tests/architecture/no-orchestrator-network.test.ts`.
- `docs/output-catalog.md` byte-unchanged (Phase 70 boundary).
- `will force update` surface gap recorded in code + SUMMARY (no silent omission).
</verification>

<success_criteria>
- The reconcile pending diff renders `will force install` for a planned install
  that would degrade, resolved no-network, with planner purity intact.
- The `will force update` surface gap is explicitly documented.
- `npm run check` GREEN.
</success_criteria>

<output>
Create `.planning/phases/66-derived-force-state-glyphs/66-04-SUMMARY.md` when done.
</output>
