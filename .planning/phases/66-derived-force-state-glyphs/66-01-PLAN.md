---
phase: 66-derived-force-state-glyphs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/edge/handlers/tools.test.ts
autonomous: true
requirements: [FSTAT-02, FSTAT-04, FSTAT-06]
must_haves:
  truths:
    - "A force-installed plugin row renders the byte form `◉ <name> v.. (force-installed)` (U+25C9, distinct from `●` U+25CF)."
    - "A force-upgradable plugin row renders `● <name> v.. (force-upgradable)` (reuses the installed glyph because the row is currently clean)."
    - "Every render site over the plugin-status union compiles only when it handles the two new arms (assertNever enforcement)."
    - "force-installed and force-upgradable project to `installed` on the tool surface (`projectRowStatus`)."
    - "A `will install` pending row carrying `force: true` renders `(will force install)`."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "ICON_FORCE_INSTALLED glyph, force-installed/force-upgradable tuple entries + arm interfaces + render-switch cases, info status widening + glyph mapper, will-install force modifier"
      contains: "ICON_FORCE_INSTALLED"
    - path: "extensions/pi-claude-marketplace/edge/handlers/tools.ts"
      provides: "force-installed/force-upgradable -> installed projection in projectRowStatus"
      contains: "force-installed"
  key_links:
    - from: "tests/architecture/notify-closed-set-locks.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "PLUGIN_STATUSES.length 15->17, STATUS_TOKENS.length 22->24"
      pattern: "PLUGIN_STATUSES.length"
    - from: "extensions/pi-claude-marketplace/shared/notify.ts renderPluginRow"
      to: "ICON_FORCE_INSTALLED / ICON_INSTALLED"
      via: "force-installed -> installedLikeRow, force-upgradable -> pluginRow"
      pattern: "case \"force-installed\""
---

<objective>
Extend the central status vocabulary in `shared/notify.ts` with two derived
force statuses and their byte forms, and update the only two non-`renderPluginRow`
status projections so the closed set stays GREEN. This is the FSTAT-02/04
enforcement foundation every Wave 2 consumer builds on.

Implements D-66-03: add `force-installed` (new glyph `ICON_FORCE_INSTALLED = "◉"`,
U+25C9) and `force-upgradable` (reuses `ICON_INSTALLED = "●"`) as discriminated
union arms, force every render site via the existing `assertNever`, bump the two
runtime length tripwires, and add a `force?: boolean` modifier to the existing
`will install` pending arm so FSTAT-06 can render `(will force install)` from the
same token (no new closed-set pending token — the locked bump is +2 realized
statuses only).

Purpose: lock the token + glyph VALUES and the render byte forms so the
orchestrators in Wave 2 only wire producers, never invent vocabulary.
Output: extended `notify.ts` union/glyph/render, `tools.ts` projection, bumped
closed-set tripwires, direct-`notify()` byte tests for the new rows.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-derived-force-state-glyphs/66-CONTEXT.md
@.planning/phases/66-derived-force-state-glyphs/66-RESEARCH.md
@.planning/phases/66-derived-force-state-glyphs/66-PATTERNS.md
@.planning/phases/66-derived-force-state-glyphs/66-VALIDATION.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/edge/handlers/tools.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add force-installed/force-upgradable to the notify.ts status vocabulary, glyph, arms, render switch, info mapper, and will-install force modifier</name>
 <files>extensions/pi-claude-marketplace/shared/notify.ts, tests/architecture/notify-closed-set-locks.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (the file being modified): `STATUS_TOKENS` (~198-221), `PLUGIN_STATUSES` (~373-389), `ICON_*` constants (~1293-1306), `PluginInstalledMessage` (~555-563, the TransitionMessageBase analog), `PluginUpgradableMessage` (~668-675, the MessageBase analog), `PluginWillInstallMessage` (~741), `PluginNotificationMessage` union (~791-806), `renderPluginRow` switch + `assertNever` (~1827-1988, will-install arm ~1922), `installedLikeRow` (~1799), `pluginRow` (~1756), `PluginInfoRowBase.status` Extract (~1063-1064), `pluginInfoStatusGlyph` (~2738-2752)
    - 66-PATTERNS.md "shared/notify.ts (model + view)" Edits 1-6 (the six in-file analog edits) and the glyph/render code excerpts
    - tests/architecture/notify-closed-set-locks.test.ts (~33-39): the 15/22 length asserts to bump
  </read_first>
  <action>
    Six in-place edits to `notify.ts`, all mirroring an existing arm (D-66-03):
    (1) Add `"force-installed"` and `"force-upgradable"` to BOTH `STATUS_TOKENS`
    (place `force-upgradable`/`force-installed` adjacent to `upgradable`/`installed`)
    and `PLUGIN_STATUSES` — these are TWO separate tuples with no compile binding;
    only the length tripwire catches an omission. Result: STATUS_TOKENS 22->24,
    PLUGIN_STATUSES 15->17.
    (2) Add `export const ICON_FORCE_INSTALLED = "◉";` (U+25C9 FISHEYE) next to the
    other `ICON_*` with a `D-66-03 / FSTAT-02` doc-comment noting it is byte-distinct
    from `ICON_INSTALLED` `●` (U+25CF) and marks the realized force-degraded row;
    force-upgradable reuses `ICON_INSTALLED` because the row is currently clean.
    (3) Add `PluginForceInstalledMessage` mirroring `PluginInstalledMessage` verbatim
    (extends `TransitionMessageBase`, required `severity`/`needsReload`, `status:
    "force-installed"`, plus name/dependencies/version?/scope?/reasons?/description?)
    and `PluginForceUpgradableMessage` mirroring `PluginUpgradableMessage` verbatim
    (extends `MessageBase`, `status: "force-upgradable"`, required `reasons`,
    version?/scope?/description?); append both to the `PluginNotificationMessage` union.
    (4) Add the two `renderPluginRow` cases: `case "force-installed"` -> `installedLikeRow(ICON_FORCE_INSTALLED, p, mpScope, renderVersion(p.version), "(force-installed)", p.reasons, probe)`;
    `case "force-upgradable"` -> `pluginRow(ICON_INSTALLED, p, mpScope, "(force-upgradable)", probe)`.
    (5) Add `"force-installed"` to the `PluginInfoRowBase.status` `Extract<PluginStatus, ...>` union (do NOT add force-upgradable — info is single-plugin, FSTAT-07).
    (6) Add `case "force-installed": return ICON_FORCE_INSTALLED;` to `pluginInfoStatusGlyph`.
    Then add `readonly force?: boolean;` to `PluginWillInstallMessage` and make the
    `renderPluginRow` `case "will install"` emit the label `"(will force install)"`
    when `p.force === true`, else `"(will install)"` (FSTAT-06 — same token, render
    modifier; no new closed-set pending token, consistent with the +2 bump).
    Finally bump `tests/architecture/notify-closed-set-locks.test.ts`: `PLUGIN_STATUSES.length`
    15->17, `STATUS_TOKENS.length` 22->24, and update both test titles
    (`"closed 15-entry"`->`"closed 17-entry"`, `"closed 22-entry"`->`"closed 24-entry"`).
    Use `D-66-03`/`FSTAT-02`/`FSTAT-04`/`FSTAT-06` IDs in comments; never GSD phase/plan numbers.
  </action>
  <verify>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts && npx tsc --noEmit -p extensions/pi-claude-marketplace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'force-installed\|force-upgradable' extensions/pi-claude-marketplace/shared/notify.ts` returns >= 6 (two tuples x2 + arm + render + info).
    - `grep -F 'ICON_FORCE_INSTALLED = "◉"' extensions/pi-claude-marketplace/shared/notify.ts` matches (the glyph is U+25C9, NOT `●` U+25CF).
    - `renderPluginRow` has both `case "force-installed":` and `case "force-upgradable":`; `npx tsc --noEmit` passes (proves the `assertNever` tail is satisfied — no unhandled arm).
    - `pluginInfoStatusGlyph` has `case "force-installed": return ICON_FORCE_INSTALLED;`.
    - `PluginWillInstallMessage` has `force?: boolean`; the will-install arm renders `(will force install)` when set.
    - `node --test tests/architecture/notify-closed-set-locks.test.ts` exits 0 with the bumped 17/24 asserts.
  </acceptance_criteria>
  <done>Both force statuses exist in both tuples, carry distinct glyphs, are rendered by `renderPluginRow` and `pluginInfoStatusGlyph`, the will-install arm has a `force` modifier, and the closed-set tripwires are GREEN at 17/24.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project force-installed/force-upgradable to `installed` on the tool surface, and add renderer + projection byte tests</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/tools.ts, tests/shared/notify-v2.test.ts, tests/edge/handlers/tools.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/tools.ts (the file being modified): `projectRowStatus` (~159-190, the `installed`/`upgradable` -> `"installed"` cases, default `throw new Error(...)` at ~186 — NOT assertNever, so a missing case compiles but throws at RUNTIME); `statusLabel` (~192) and `statusKey` (~235) are over `ToolPluginStatus` and need NO force cases
    - 66-PATTERNS.md "edge/handlers/tools.ts" + "tests/edge/handlers/tools.test.ts" sections (upgradable->`[installed]` template ~581-608)
    - tests/shared/notify-v2.test.ts (~404-429): the existing `upgradable` direct-`notify()` byte test (the exact template for the new force-row byte cases)
  </read_first>
  <action>
    In `tools.ts::projectRowStatus`, add `case "force-installed":` and
    `case "force-upgradable":` to the SAME return arm as `installed`/`upgradable`
    (both ARE installed on the tool surface, D-66-03) with a `D-66-03` comment;
    leave `statusLabel`/`statusKey` untouched. Then add tests:
    (a) `tests/shared/notify-v2.test.ts` — two direct-`notify()` byte cases mirroring
    the `upgradable` test at ~404: a `force-installed` row asserts the rendered body
    contains `◉ <name> ... (force-installed)` (distinct `◉` glyph), a `force-upgradable`
    row asserts `● <name> ... (force-upgradable)` (reuses `●`). Use `FSTAT-02`/`FSTAT-04`
    test titles. Do NOT touch `docs/output-catalog.md` (Phase 70 boundary).
    (b) `tests/edge/handlers/tools.test.ts` — assert a force-installed and a
    force-upgradable plugin both project to `[installed]` / `status === "installed"`
    (template: the existing upgradable->`[installed]` test).
  </action>
  <verify>
    <automated>node --test tests/shared/notify-v2.test.ts tests/edge/handlers/tools.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `projectRowStatus` returns `"installed"` for both `"force-installed"` and `"force-upgradable"` inputs (no runtime throw).
    - `tests/shared/notify-v2.test.ts` contains a case asserting the `◉ ... (force-installed)` byte form and a case asserting `● ... (force-upgradable)`; both pass.
    - `tests/edge/handlers/tools.test.ts` asserts both force statuses project to `[installed]`; passes.
    - `node --test tests/shared/notify-v2.test.ts tests/edge/handlers/tools.test.ts` exits 0.
  </acceptance_criteria>
  <done>The tool surface maps both force statuses to `installed`, and the new row byte forms are locked by direct-`notify()` assertions without touching the catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolver `unsupported[]` kind list -> render path | Typed component-kind list (already TypeBox-validated by the Phase 64 `ResolvedPlugin` schema) flows into the new render arms. No new untrusted input crosses here. |
| plugin manifest name/description -> row composer | User-supplied strings rendered by `installedLikeRow`/`pluginRow`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | Malformed/over-long plugin name or description reaching the new force render arms | mitigate | Reuse the existing `pluginRow`/`installedLikeRow` composers verbatim; they inherit `truncateDescription` + whitespace-normalizing hard-wrap (T-42-01) that neutralize control chars. No new composer. |
| T-66-02 | Information disclosure | New glyph/token surface leaking internal state | accept | Display is derived from data already shown (installed/unsupported); no secrets, no new field. ASVS L1: no control required. |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `npm run check` GREEN (typecheck + ESLint + Prettier + `node --test`) — the
  `assertNever` tails enumerate any unhandled render site; the length tripwires
  confirm both tuples were updated.
- `node --test tests/architecture/notify-closed-set-locks.test.ts tests/shared/notify-v2.test.ts tests/edge/handlers/tools.test.ts`.
- `docs/output-catalog.md` and `docs/messaging-style-guide.md` remain byte-unchanged (Phase 70 boundary).
</verification>

<success_criteria>
- `force-installed` (`◉`) and `force-upgradable` (`●`) exist in `STATUS_TOKENS`
  (24) and `PLUGIN_STATUSES` (17), render their locked byte forms, and project to
  `installed` on the tool surface.
- The `will install` pending arm carries a `force?: boolean` modifier that renders
  `(will force install)`.
- `npm run check` GREEN; catalog untouched.
</success_criteria>

<output>
Create `.planning/phases/66-derived-force-state-glyphs/66-01-SUMMARY.md` when done.
</output>
