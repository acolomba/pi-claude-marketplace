---
phase: 99-post-audit-tech-debt-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
autonomous: true
requirements: [D-99-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low # no calibration samples in .planning/; factor 1.0 applied

must_haves:
  truths:
    - "The version-less autoupdate cascade skip row has a catalog state under the existing `## /claude:plugin marketplace update <name>` section, with its FIXTURES entry landing in the SAME commit (D-99-04)."
    - "The description-bearing variant count reads nine, naming `partially-installed` and `partially-upgradable` alongside the seven already listed, and names the interface authority the way the 98-06 sweep established."
    - "No dangling anchor pair survives at the seven paired source sites; each sentence keeps its true prose and loses only the undefined identifier."
    - "The eight files where the second anchor carries its own live, unrelated meaning are untouched."
    - "`catalog-uat` passes in both directions: no annotated catalog state without a fixture, no fixture without an annotation."
  artifacts:
    - "docs/output-catalog.md — one new catalog state (autoupdate cascade skip row) plus the corrected variant-count sentence"
    - "tests/architecture/catalog-uat.test.ts — one new FIXTURES entry under the `/claude:plugin marketplace update <name>` key"
  key_links:
    - "new catalog state annotation -> its FIXTURES entry under key `\"/claude:plugin marketplace update <name>\"` (catalog-uat.test.ts:3307) -> the inverse-walk assertion (catalog-uat.test.ts:4694)"
    - "the corrected count -> the message interfaces in shared/notify.ts that declare `description?` -> the gate that pins the status set"
  prohibitions:
    - "Do NOT change any code line. This plan edits documentation and comments only; `git diff -U0 extensions/` must contain no non-comment line."
    - "Do NOT change the cascade row's rendered bytes. 98-06 found the version-less form deliberate and byte-pinned in `tests/orchestrators/marketplace/update.test.ts`; this plan DOCUMENTS the asymmetry, it does not repair it."
    - "Do NOT sweep the second anchor mechanically. Eight files carry it with a different live meaning (substitution helper lock, forward-compat tail, POST-state-commit ordering, fail-fast state lock, row-type registry, agents-substitution corollary). Only the SEVEN sites where it appears paired with the list-inventory anchor are in scope; read each before editing."
    - "The removed anchor text must not reappear anywhere in the edited files, including inside a comment narrating its own removal."
    - "Do NOT edit inside a fenced block that `catalog-uat` byte-compares unless you are adding a new annotated state. Run the suite after EVERY edit pass, not only at the end — the gate is what proves an edit stayed outside the fences."
---

<objective>
Close the three documentation-only deferrals 98-06 recorded rather than silently skipped
(D-99-04): give the version-less autoupdate cascade skip row a catalog state with its byte
fixture, correct the stale description-bearing variant count from seven to nine, and drop the
dangling anchor pair at the seven paired source sites while leaving the eight files where that
identifier still means something else alone.

Purpose: each item is the same defect class 98-06 spent a plan removing — a document stating
something the code has falsified, or a surface the catalog does not describe at all. Leaving them
open past the milestone means the closure sweep shipped with three known holes it had already
located. The anchor work also carries a live hazard: the pair is undefined in every surviving
artifact, so a reader following it finds nothing, while a mechanical removal would strip a
legitimate identifier from eight unrelated files.

Output: a catalog whose marketplace-update section describes the cascade skip row it emits, a
correct variant enumeration, and seven source comments that keep their true sentences without a
dead reference.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-post-audit-tech-debt-closure/99-CONTEXT.md
@.planning/phases/99-post-audit-tech-debt-closure/99-RESEARCH.md
@.planning/phases/98-lifecycle-regression-and-contract-documentation/98-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog state and byte fixture for the version-less autoupdate cascade skip row</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - The section already exists — planner confirmed it this session, resolving research open
      question 3: `docs/output-catalog.md` heading `## /claude:plugin marketplace update <name>`
      at line 2028, and the matching FIXTURES key
      `"/claude:plugin marketplace update <name>"` at `tests/architecture/catalog-uat.test.ts:3307`.
      The state is ADDED to the existing section under the existing key; no new section, no new key.
    - Read `docs/output-catalog.md` from the section heading to its end, to see the states already
      annotated there and pick a non-colliding `catalog-state:` slug.
    - Read the FIXTURES block starting at `catalog-uat.test.ts:3307` to copy the exact entry shape.
    - Read the two narrated cases in `tests/orchestrators/marketplace/update.test.ts` that pin the
      cascade skip row and the plugin-update skip row (grep for the skip-row byte assertions and
      the comment above them explaining the asymmetry) — the fixture must reproduce the cascade
      form the code actually emits, not a hand-written guess.
  </read_first>
  <action>
    Add one catalog state describing the cascade skip row the marketplace-update mapper emits: the
    mapper forwards name, scope and reasons but no version, so the cascade row carries no version
    token while the single-plugin update row does. State the asymmetry as deliberate and name the
    mapper arm responsible, so a future reader does not read it as a rendering bug and "fix" a
    byte-pinned row.

    Ship the FIXTURES entry in the same commit as the catalog annotation. `catalog-uat` walks in
    both directions — an annotated state with no fixture and a fixture with no annotation each
    fail — so a split commit is red in one direction no matter which half lands first.

    Take the fixture bytes from the pinned test, not from prose.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    A new `catalog-state:` annotation exists under the marketplace-update section with a fenced
    block reproducing the version-less cascade skip row; a FIXTURES entry with the same
    (section, state) key exists; the catalog-uat suite passes both its forward and its inverse
    walk; `tests/orchestrators/marketplace/update.test.ts` is unchanged and still green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the description-bearing variant count from seven to nine</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - `grep -n "seven list-surface variants" docs/output-catalog.md` — the sentence sits at
      roughly line 340, inside the `disabled-inventory` region and its PL-4 paragraph. Confirm
      whether the sentence is inside a fenced block that catalog-uat byte-compares BEFORE editing;
      it should be ordinary prose between fences, but check rather than assume.
    - `grep -n "description?" extensions/pi-claude-marketplace/shared/notify.ts` — the interfaces
      that declare the optional description field are the authority for the count.
  </read_first>
  <action>
    Re-derive the count from the message interfaces rather than incrementing the stale number:
    a variant supports the description line exactly when its interface declares the optional
    description field. 98-06 established that `PluginPartiallyInstalledMessage` and
    `PluginPartiallyUpgradableMessage` both declare it, which makes the true count nine.

    Rewrite the sentence to list all nine and keep the cascade-only exclusion clause intact.
    Follow the pattern 98-06 established for every corrected count in this document: state the
    number, name the runtime authority it mirrors, and name what last moved it — a bare corrected
    number drifts again at the next amendment, which is how this one became wrong.

    Change nothing else in that paragraph, and nothing inside any fence.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/architecture/partial-vocabulary-guard.test.ts</automated>
  </verify>
  <done>
    The sentence enumerates nine variants including the two partially-installed-family ones, names
    its interface authority, and passes both the byte-equality gate and the retired-vocabulary
    scan. `git diff docs/output-catalog.md` for this task shows only prose lines outside fences.
  </done>
</task>

<task type="auto">
  <name>Task 3: Drop the dangling paired anchor at the seven source sites</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts, extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    Re-run the enumeration before editing — the counts below were verified this session but the
    tree is mutable:
    `grep -rn "RLD-04" extensions/` <!-- planner-discipline-allow: RLD-04 -->

    Expect exactly seven paired hits: `orchestrators/plugin/list.ts` at four sites (a header
    comment, a doc-block line, an inline comment above the installed-row message emit, and an
    inline comment about the safe access path under TS strict), `orchestrators/plugin/list.messaging.ts`
    at one doc-block site, and `shared/notify.ts` at two sites — one doc-block line about the
    list-only inventory row and one inline comment where the pair sits inside parentheses after a
    still-live `PL-4` reference.

    Research corrected the inherited count twice: the CONTEXT and 98-06 both say six and both name
    the wrong files. Trust the grep, not the prose. Confirm the total is seven before you start and
    record the number you found in the summary.
  </read_first>
  <action>
    At each of the seven sites, remove ONLY the dangling identifier pair and keep the sentence.
    The prose at every one of these sites is true — it describes the list surface's steady-state
    inventory row and the status token it emits — and only the reference is dead: ROADMAP.md
    already records that neither identifier is defined in any surviving artifact and says not to
    carry them forward.

    Two of the seven need care rather than deletion of a whole clause:
    - the `shared/notify.ts` site where the pair sits parenthesized after `PL-4`: drop the
      parenthetical, keep `PL-4`, which is live.
    - any site where the pair opens the sentence: re-flow the remaining sentence so it does not
      begin mid-clause.

    Leave the EIGHT files where the second identifier carries its own live, unrelated meaning
    completely untouched: the skills-bridge substitution-helper note, the domain source-parser
    forward-compat tail, the install orchestrator's POST-state-commit ordering notes, the uninstall
    orchestrator's ordering note, the shared errors module's fail-fast state-lock note, the
    notify-context row-type registry note, the shared vars substitution lock, and the agents
    converter's substitution corollary. None of those pairs with the list-inventory anchor; each was
    read this session and each is genuine. Verify by grepping for the paired form specifically, not
    for either identifier alone.

    Do not narrate the removal in a comment. A comment saying which reference was dropped
    reintroduces the very text the acceptance criterion greps for, and adds a historical record the
    comment policy excludes — git already has the history.
  </action>
  <verify>
    <automated>! grep -rn "RLD-04" extensions/ &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run format:check</automated>
  </verify>
  <done>
    The paired-anchor grep over `extensions/` returns no match; the second identifier still appears
    in exactly the eight unrelated files with its own meaning; `git diff -U0 extensions/` filtered
    to non-comment lines is empty; typecheck, lint and format-check are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none newly crossed) | Documentation and comment edits only. No executable line changes, no input parsing, no disk write outside the docs and one test fixture map. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-99-03-01 | Repudiation | `docs/output-catalog.md` byte contract | medium | mitigate | A prose edit that strays inside a byte-compared fence silently changes a documented contract. Mitigation: run `catalog-uat` after every edit pass, not only at the end; the gate is what proves an edit stayed outside the fences (the pattern 98-06 established). |
| T-99-03-02 | Tampering | the eight unrelated comment sites | medium | mitigate | A mechanical sweep over the bare identifier would strip live traceability from eight files. Mitigation: the paired-form grep is the only accepted selector, the eight excluded files are enumerated in the task, and the acceptance criterion greps the paired form specifically. |
| T-99-03-03 | Information Disclosure | committed diff | low | mitigate | TruffleHog filesystem-mode scan over the changed paths before the worktree commit, per the documented protocol; `SKIP=trufflehog` is applied to that commit only and never extended to another hook. |
| T-99-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is installed. RESEARCH.md §Package Legitimacy Audit records "Not applicable" for the whole phase. |
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Kind | Location |
|---|---|---|
| autoupdate cascade skip-row catalog state | NEW catalog state | `docs/output-catalog.md`, under `## /claude:plugin marketplace update <name>` |
| its byte fixture | NEW FIXTURES entry | `tests/architecture/catalog-uat.test.ts`, key `"/claude:plugin marketplace update <name>"` |
| corrected description-bearing variant enumeration (nine) | amended prose | `docs/output-catalog.md` |
| seven re-anchored comment sites | amended comments | `list.ts` (4), `list.messaging.ts` (1), `notify.ts` (2) |

Closed sets after this plan: unchanged — no reason token, status token or glyph is added or
removed. Catalog state count: +1.
</artifacts_this_phase_produces>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` green in both walk directions.
- `node --test tests/architecture/partial-vocabulary-guard.test.ts` green.
- `node --test tests/architecture/compat-01-no-expansion.test.ts` green and UNCHANGED.
- `node --test tests/orchestrators/marketplace/update.test.ts` green and UNCHANGED (the cascade
  row's bytes did not move).
- `git diff -U0 extensions/` contains no non-comment line.
- `npm run typecheck && npm run lint && npm run format:check` green.
</verification>

<success_criteria>
All three 98-06 deferrals are closed: the cascade skip row is documented and fixture-pinned, the
variant count is nine with its authority named, and the dangling paired anchor is gone from seven
sites with the eight live-meaning files untouched.
</success_criteria>

<tracer_exemption>
Tracer-first is exempted with a recorded reason: this plan changes no behavior and touches no
layer boundary. Its deliverable is documentation and comments; there is no runnable path to trace.
</tracer_exemption>

<output>
Create `.planning/phases/99-post-audit-tech-debt-closure/99-03-SUMMARY.md` when done.
Record in it: the catalog-state slug and its FIXTURES key, the nine variants as enumerated, the
paired-anchor site count actually found (research says seven; CONTEXT and 98-06 both said six —
record which was right), and confirmation that the eight live-meaning files are untouched.
</output>
