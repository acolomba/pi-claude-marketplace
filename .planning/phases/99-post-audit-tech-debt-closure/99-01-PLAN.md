---
phase: 99-post-audit-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/types.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/reconcile/notify.test.ts
autonomous: true
requirements: [D-99-02c]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low # no calibration samples in .planning/; factor 1.0 applied

must_haves:
  truths:
    - "No outcome interface in orchestrators/types.ts declares a member named `stagedAgents` or `stagedMcpServers`; those two names belong solely to the boolean signals in orchestrators/plugin/shared.ts (D-99-02c)."
    - "`ReinstallReinstalledOutcome` and `PluginUpdateUpdatedOutcome` each carry the staged generated-name arrays under names that read as name lists, matching the existing in-tree precedent `ledgerCtx.stagedAgentNames`."
    - "Every existing assertion about staged agent/MCP names still passes; no rendered byte and no runtime behavior changes (NREG-01)."
  artifacts:
    - "extensions/pi-claude-marketplace/orchestrators/types.ts — both outcome interfaces renamed"
    - "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts — producer renamed"
    - "extensions/pi-claude-marketplace/orchestrators/plugin/update.ts — producer renamed"
  key_links:
    - "types.ts field name -> reinstall.ts:1759-1760 producer -> reinstall.test.ts:343-344 deep-equal assertions"
    - "types.ts field name -> update.ts:1898-1899/1940-1943 producer -> marketplace/update.test.ts cascade fixtures"
    - "the freed `stagedAgents`/`stagedMcpServers` names -> `LedgerDegradationSignals` (shared.ts:84,90) -> plan 99-04's `extends` (this rename is the enabler, research finding 1)"
  prohibitions:
    - "Do NOT remove or widen `install.ts:258`'s `Omit<LedgerDegradationSignals, \"stagedAgents\" | \"stagedMcpServers\">`. Its doc comment at :239 records a deliberate exclusion (installPlugin never advertises those signals), not a collision workaround. Touching it is out of scope."
    - "Do NOT rename the boolean members in `orchestrators/plugin/shared.ts` or any of their consumers (`enable-disable.ts`, `reconcile/apply.ts`). The booleans keep their names; only the string arrays move."
    - "Do NOT change any producer's VALUE, only its key. `resources.agents`, `handles.agents.result.recorded.map(...)`, `declaresAgents: <array>.length > 0` keep their exact right-hand sides."
    - "Do NOT add a catalog state, a reason token, or a status token. This rename has zero rendered-vocabulary surface."
    - "Comments must cite durable IDs only (D-99-02c, WR-04, NREG-01) — never `Phase 99`, `Plan 01`, `Wave 1`, or `Pitfall N` (.claude/rules/typescript-comments.md)."
---

<objective>
Rename the two `readonly string[]` staged-name field pairs apart from the identically-named
boolean degradation signals, on BOTH outcome interfaces that declare them (D-99-02c).

Purpose: D-99-02c names reinstall only, but research finding 1 verified the identical
declaration on `PluginUpdateUpdatedOutcome` (`orchestrators/types.ts:148-149`) as well as
`ReinstallReinstalledOutcome` (`:26-27`). Renaming only one leaves the hazard half-standing
AND leaves plan 99-04 blocked: `PluginUpdateUpdatedOutcome extends LedgerDegradationSignals`
is a `TS2430` incompatible-property error while `stagedAgents: readonly string[]` collides
with `stagedAgents?: boolean`. This is a scope CLARIFICATION of the locked hazard, not a scope
expansion — the operator locked the hazard and the hazard has two sites. Landing this before
plan 99-04 is the phase's one hard ordering constraint; inverting it makes the `Omit`/`Pick`
workaround permanent, which is exactly the type confusion D-99-02c exists to remove.

Output: both interfaces, both producers, and all test-fixture consumers renamed, with
`npm run typecheck` green at every commit and no rendered byte moved.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-post-audit-tech-debt-closure/99-CONTEXT.md
@.planning/phases/99-post-audit-tech-debt-closure/99-RESEARCH.md

@extensions/pi-claude-marketplace/orchestrators/types.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the reinstall outcome's staged-name arrays</name>
  <files>extensions/pi-claude-marketplace/orchestrators/types.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/reconcile/notify.test.ts</files>
  <read_first>
    Run these first; the line numbers below were verified 2026-08-10 but the tree is mutable:
    - `grep -n "stagedAgents\|stagedMcpServers" extensions/pi-claude-marketplace/orchestrators/types.ts`
      (expect the pair at :26-27 on `ReinstallReinstalledOutcome`, the pair at :148-149 on
      `PluginUpdateUpdatedOutcome`, and narrating prose at :32-36 and :140)
    - `grep -rn "stagedAgents\|stagedMcpServers" tests/ | wc -l` (39 at research time)
    - `grep -rln "stagedAgents\|stagedMcpServers" tests/` (four suites)
    - Read `extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts:80-110` — the boolean
      declarations and the `Pick<LedgerDegradationSignals, ...>` consumer that must NOT move.
  </read_first>
  <action>
    Rename `stagedAgents` -> `stagedAgentNames` and `stagedMcpServers` -> `stagedMcpServerNames`
    on `ReinstallReinstalledOutcome` in `orchestrators/types.ts` and at its producer
    (`reinstall.ts:1759-1760`, the two lines immediately preceding `declaresAgents:`).

    Use `stagedAgentNames` / `stagedMcpServerNames` — the discretionary naming choice is settled
    by in-tree precedent: `enable-disable.ts:304-305` and `install.ts:400-401` already carry
    ledger-context fields under exactly those names, so the name-vs-flag distinction becomes
    self-documenting rather than novel.

    Update the narrating prose in `types.ts` (the doc lines near :32-36 that spell the old field
    names inside a `stagedAgents.length > 0` explanation) so the comment names the new fields.
    State WHY the names differ from the booleans: the arrays are generated names, the booleans in
    `shared.ts` are presence flags, and the two must not be confusable (D-99-02c).

    Update the test consumers that belong to the REINSTALL outcome. Classify each occurrence
    before editing rather than renaming blind: an occurrence typed as `ReinstallReinstalledOutcome`
    (including `reinstall.test.ts:343-344`, the only two real deep-equal assertions) moves in this
    task; an occurrence typed as `PluginUpdateUpdatedOutcome` moves in Task 2. `npm run typecheck`
    is the arbiter — if a fixture you moved belongs to the other interface, typecheck fails
    immediately and you move it back.

    Keep every right-hand side byte-identical. This is a key rename, not a value change.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; node --test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/reconcile/notify.test.ts</automated>
  </verify>
  <done>
    `ReinstallReinstalledOutcome` declares `stagedAgentNames` / `stagedMcpServerNames`; no
    occurrence of the old names survives in `types.ts`'s reinstall interface or in `reinstall.ts`;
    `npm run typecheck` exits 0 and the reinstall suite's two deep-equal assertions pass unchanged
    in substance (only the accessed key name differs).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename the update outcome's staged-name arrays and prove the collision is gone</name>
  <files>extensions/pi-claude-marketplace/orchestrators/types.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <read_first>
    - `grep -n "stagedAgents\|stagedMcpServers" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`
      (expect the local consts at :1898-1899, the outcome keys at :1940-1941, the
      `declaresAgents`/`declaresMcp` derivations at :1942-1943, and a narrating comment at :30)
    - Re-run `grep -rn "stagedAgents\|stagedMcpServers" tests/` to see what Task 1 left behind.
  </read_first>
  <action>
    Rename the same two field names on `PluginUpdateUpdatedOutcome` (`types.ts:148-149`) and at
    its producer in `update.ts`. The producer computes two local consts from
    `handles.agents.result.recorded` / `handles.mcp.result.recorded` and then shorthand-spreads
    them into the outcome; rename the locals too so the shorthand stays shorthand, and keep the
    `declaresAgents` / `declaresMcp` derivations reading `<renamed>.length > 0`.

    Re-narrate `types.ts:140` (the doc sentence naming both old field names) and the `update.ts`
    header comment near :30 that says the success outcome carries `stagedAgents`/`stagedMcpServers`.

    Move the remaining test fixtures — `marketplace/update.test.ts` carries roughly 22
    object-literal `stagedAgents: []` / `stagedMcpServers: []` cascade fixtures typed against the
    update outcome; `plugin/update.test.ts` carries the rest.

    Finish by proving the two names are now free for the boolean signals: the residual grep below
    must return nothing for the three renamed files. `shared.ts`, `install.ts`, `enable-disable.ts`
    and `reconcile/apply.ts` legitimately keep the old names for their BOOLEAN members and are not
    in the grep set.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; ! grep -rn "stagedAgents\b\|stagedMcpServers\b" extensions/pi-claude-marketplace/orchestrators/types.ts extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/update.ts &amp;&amp; node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>
    Both outcome interfaces carry the renamed arrays; the residual grep over `types.ts`,
    `reinstall.ts` and `update.ts` returns no match; `npm run typecheck` exits 0; all four affected
    suites pass; `npm run lint` reports no new finding. Plan 99-04 can now write
    `PluginUpdateUpdatedOutcome extends LedgerDegradationSignals` without a `TS2430`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none newly crossed) | This plan renames two in-memory TypeScript interface members. No input crosses a boundary, no file format changes, no persisted key moves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-99-01-01 | Tampering | `orchestrators/types.ts` outcome interfaces | low | mitigate | The renamed members are in-memory only; COMPAT-01's separate persisted-install-record key-set pin (`compat-01-no-expansion.test.ts:342`) is untouched, so no `state.json` key can drift out of this rename. Verified by running the COMPAT-01 suite unchanged. |
| T-99-01-02 | Tampering | boolean signal consumers (`shared.ts`, `apply.ts`, `enable-disable.ts`) | low | accept | Every boolean consumer compares `=== true`, so a mis-renamed array could not silently satisfy one; TypeScript rejects `readonly string[]` where `boolean \| undefined` is required. `npm run typecheck` is the control. |
| T-99-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package. RESEARCH.md §Package Legitimacy Audit records "Not applicable; this phase installs no external packages", so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required. |
</threat_model>

<artifacts_this_phase_produces>
No new file, symbol, catalog state, reason token or status token. The plan produces two RENAMED
interface members on two existing interfaces:

| Artifact | Kind | Location |
|---|---|---|
| `ReinstallReinstalledOutcome.stagedAgentNames` | renamed member (was `stagedAgents`) | `orchestrators/types.ts` |
| `ReinstallReinstalledOutcome.stagedMcpServerNames` | renamed member (was `stagedMcpServers`) | `orchestrators/types.ts` |
| `PluginUpdateUpdatedOutcome.stagedAgentNames` | renamed member (was `stagedAgents`) | `orchestrators/types.ts` |
| `PluginUpdateUpdatedOutcome.stagedMcpServerNames` | renamed member (was `stagedMcpServers`) | `orchestrators/types.ts` |

Catalog state after this plan: unchanged. Closed sets after this plan: unchanged.
</artifacts_this_phase_produces>

<verification>
- `npm run typecheck` exits 0 after each task.
- `node --test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/reconcile/notify.test.ts` — all green.
- `node --test tests/architecture/compat-01-no-expansion.test.ts` — green and UNCHANGED (no closed set moved).
- Residual-name grep over `types.ts`, `reinstall.ts`, `update.ts` returns no match.
- `git diff` shows no changed right-hand side: every hunk is a key rename or a comment.
</verification>

<success_criteria>
Both string-array pairs are renamed apart from the boolean signals on BOTH outcome interfaces;
`install.ts`'s deliberate `Omit` is untouched; typecheck, lint and the four affected suites are
green; no rendered byte and no persisted key moved.
</success_criteria>

<tracer_exemption>
Tracer-first is exempted for this plan with a recorded reason: the plan adds no behavior and
crosses no layer. It is a pure key rename whose end-to-end proof is `npm run typecheck` over the
whole tree — a thin vertical slice would have nothing to demonstrate that typecheck does not
already demonstrate more completely.
</tracer_exemption>

<output>
Create `.planning/phases/99-post-audit-tech-debt-closure/99-01-SUMMARY.md` when done.
Record in it: the exact new field names, the count of test occurrences moved per suite, and
confirmation that `install.ts`'s `Omit` was left in place with its reason.
</output>
