---
phase: 02-containment-and-input-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/mcp/types.ts
  - tests/bridges/mcp/types.test.ts
  - extensions/pi-claude-marketplace/bridges/mcp/stage.ts
  - tests/bridges/mcp/stage.test.ts
  - extensions/pi-claude-marketplace/bridges/mcp/unstage.ts
  - tests/bridges/mcp/unstage.test.ts
autonomous: true
requirements: [PDEF-03]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PDEF-03: A Pi user with an absent scoped mcpServers field keeps the existing clean no-op behavior in both stage and unstage."
    - "PDEF-03: A present object-valued mcpServers field continues through normal stage or unstage behavior."
    - "PDEF-03: A present null, string, array, boolean, or number value reaches one shared total classifier and produces the same stable typed failure family in stage and unstage before enumeration or mutation."
    - "PDEF-03: Each consumer keeps its existing malformed-JSON and malformed-top-level-document policy; only the mcpServers field policy changes."
    - "The specless PDEF-03 fallback row is resolved by direct type, stage, and unstage owner tests with exact outcomes and filesystem evidence."
  artifacts:
    - "extensions/pi-claude-marketplace/bridges/mcp/types.ts and tests/bridges/mcp/types.test.ts own truthful RawMcpDoc boundary typing."
    - "extensions/pi-claude-marketplace/bridges/mcp/stage.ts and tests/bridges/mcp/stage.test.ts own the shared runtime classifier, typed malformed-field error, and first production-quality fail-closed path."
    - "extensions/pi-claude-marketplace/bridges/mcp/unstage.ts and tests/bridges/mcp/unstage.test.ts apply the identical policy to removal."
  key_links:
    - "RawMcpDoc.mcpServers remains unknown until the shared classifier narrows an object map."
    - "prepareStageMcpServers classifies after readScopedDoc's existing top-level policy and before partitionExistingServers, collision work, or _nextDoc construction."
    - "unstageMcpServers classifies after its existing top-level policy and before Object.entries or atomicWriteJson."
  prohibitions:
    - "Malformed field handling cannot mutate configuration or masquerade as success."
    - "Tests cannot touch developer state or add test-only seams."
---

<objective>
Correct PDEF-03 by making the raw MCP boundary truthful and giving stage and unstage one total, typed, fail-closed field policy.

Purpose: Prevent malformed scoped MCP data from being coerced, unexpectedly enumerated, reported as a successful no-op, or written over.
Output: A production-quality stage tracer, the matching unstage expansion, and exact hermetic owner evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-containment-and-input-safety/02-CONTEXT.md
@.planning/phases/02-containment-and-input-safety/02-RESEARCH.md
@.planning/phases/02-containment-and-input-safety/02-PATTERNS.md
@.planning/phases/02-containment-and-input-safety/02-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make MCP stage reject one malformed stored field through the shared typed boundary</name>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/mcp/types.ts
    - tests/bridges/mcp/types.test.ts
    - extensions/pi-claude-marketplace/bridges/mcp/stage.ts
    - tests/bridges/mcp/stage.test.ts
    - extensions/pi-claude-marketplace/bridges/mcp/parse.ts
  </read_first>
  <files>extensions/pi-claude-marketplace/bridges/mcp/types.ts, tests/bridges/mcp/types.test.ts, extensions/pi-claude-marketplace/bridges/mcp/stage.ts, tests/bridges/mcp/stage.test.ts</files>
  <behavior>
    - "PDEF-03 tracer: a scoped document whose own mcpServers property is null makes prepareStageMcpServers reject with MalformedMcpServersError carrying the exact mcpJsonPath and classified value kind, and bytes plus bigint inode/size/mtimeNs/ctimeNs remain identical."
    - "RawMcpDoc accepts unknown mcpServers values at compile time while StageMcpInput.servers remains Record<string, unknown>."
    - "The shared classifier distinguishes the JSON-realizable states: absent field, object map, and malformed null/string/array/boolean/number presence."
    - "Existing malformed JSON and malformed top-level stage documents retain their current warning/replacement behavior."
  </behavior>
  <action>Start with the failing stage filesystem case, then implement the first complete PDEF-03 path. Change RawMcpDoc.mcpServers to unknown while keeping types.ts type-only; its owner test uses type-only assertions for the raw unknown boundary and the unchanged validated StageMcpInput.servers contract. In stage.ts, add a documented MalformedMcpServersError and one exported total classifier over the parsed RawMcpDoc plus source path. Return a discriminated missing/present classification and throw the typed error for every JSON-realizable present non-record shape: null, string, array, boolean, or number. This plan deliberately selects a typed rejection class as the smallest consistent public shape: current stage callers already propagate typed preparation failures, so adding result arms would force unrelated install/update/reinstall churn. Route prepareStageMcpServers through it only after readScopedDoc has applied the existing top-level policy, and before partitioning, collision reads, or prepared-write construction. Stage is the runtime owner and exports the classifier solely because unstage imports it without a cycle; do not add a direct classifier-only runtime test or turn the type-only contract module into a runtime utility. Do not add a special branch for an in-memory own property whose value is undefined, a test-only export, broad cast, per-server validation, or new top-level-document policy. Write independent test() cases with role-only names and lowercase AAA comments; compare the complete structured error and exact original bytes/metadata before any interaction evidence.</action>
  <acceptance_criteria>
    - "The null-valued stage case fails red before production changes and passes after them."
    - "RawMcpDoc.mcpServers is unknown, while validated StageMcpInput.servers is unchanged."
    - "MalformedMcpServersError and the classifier are ordinary stage exports used by unstage, not test-only seams; types.ts remains type-only."
    - "Stage classifies before any collision read or potential write and preserves exact bytes and metadata on refusal."
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridges/mcp/types.test.ts tests/bridges/mcp/stage.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/types.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/stage.ts</automated>
  </verify>
  <fails_when>The malformed field resolves as noop/staged, throws an untyped incidental error, reaches collision work, changes bytes or metadata, weakens current top-level behavior, adds a broad cast, or either direct pair is below 100% functions/lines/branches.</fails_when>
  <done>The first real malformed MCP stage path fails closed end to end through the shared typed owner, with exact no-write evidence and green direct owner coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same malformed-field contract to MCP unstage and close the value matrix</name>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/mcp/types.ts
    - extensions/pi-claude-marketplace/bridges/mcp/unstage.ts
    - tests/bridges/mcp/unstage.test.ts
    - extensions/pi-claude-marketplace/bridges/mcp/stage.ts
    - tests/bridges/mcp/stage.test.ts
    - extensions/pi-claude-marketplace/bridges/mcp/index.ts
  </read_first>
  <files>extensions/pi-claude-marketplace/bridges/mcp/unstage.ts, tests/bridges/mcp/unstage.test.ts, tests/bridges/mcp/stage.test.ts</files>
  <behavior>
    - "PDEF-03: sibling cases for null, string, array, boolean, and number produce MalformedMcpServersError with the same exact structured fields in stage and unstage."
    - "Absent mcpServers returns each flow's exact existing no-op result, and an object map still stages/removes normally."
    - "Every malformed unstage case preserves the complete original bytes and bigint inode/size/mtimeNs/ctimeNs."
    - "Malformed JSON and top-level null/string/array retain unstage's existing throw/no-op policy and do not get relabeled as malformed mcpServers."
  </behavior>
  <action>Write the unstage failures first and replace the legacy array/string-as-no-op expectations. Call the Task 1 classifier after unstage's current JSON parse and top-level object policy but before Object.entries. Let the same typed error propagate to existing bridge/orchestrator callers; do not translate it into warnings or a successful empty result. Complete the malformed-value matrix in both owner suites, with one sibling test() per typed row, fresh case-owned temporary files, independent expected values, complete error class/field assertions, and exact no-write bytes plus metadata. Retain missing-field no-op, object behavior, malformed JSON behavior, top-level primitive behavior, atomicWriteJson ownership, and all current unrelated results. Inspect direct callers in bridges/mcp/index.ts, orchestrators/plugin/install.ts, and orchestrators/marketplace/shared.ts to confirm typed rejection propagation requires no production edit; change no caller merely to add coverage.</action>
  <acceptance_criteria>
    - "Stage and unstage import one classifier and expose one typed malformed-field failure family."
    - "All present malformed shapes fail before Object.entries and atomicWriteJson."
    - "The legacy string/array no-op expectations are gone; absence and object behavior remain exact."
    - "Caller inspection confirms the rejection propagates through existing typed error paths without unrelated lifecycle changes."
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridges/mcp/types.test.ts tests/bridges/mcp/stage.test.ts tests/bridges/mcp/unstage.test.ts tests/bridges/mcp/index.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/stage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/unstage.ts</automated>
  </verify>
  <fails_when>Stage and unstage classify separately, any malformed presence succeeds/no-ops, top-level policy broadens, a write-and-restore can pass, callers unexpectedly swallow the typed failure, tests share mutable state, or direct coverage regresses.</fails_when>
  <done>PDEF-03 is complete: raw typing is truthful, both public flows share a total classifier, every malformed presence has one typed no-write outcome, and the owner suites pin the unchanged policies.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                            |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| Parsed scoped mcp.json → MCP bridge              | Untrusted JSON supplies an unknown mcpServers field before stage or unstage enumerates or persists it. |
| MCP prepare/unstage → atomic configuration write | Classification must complete before a document can become write-eligible.                              |

## STRIDE Threat Register

| Threat ID       | Category                      | Component                                            | Severity | Disposition | Mitigation Plan                                                                                                                                                                                             |
| --------------- | ----------------------------- | ---------------------------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01         | Tampering / Denial of service | RawMcpDoc, prepareStageMcpServers, unstageMcpServers | high     | mitigate    | Model the field as unknown; classify own-property absence/object/malformed once; throw MalformedMcpServersError before enumeration, collision work, or atomicWriteJson; prove unchanged bytes and metadata. |
| T-02-01-R       | Repudiation                   | malformed MCP public outcome                         | medium   | mitigate    | Carry stable structured source-path and value-kind fields and assert the complete error rather than a message substring.                                                                                    |
| T-02-01-SC      | Tampering                     | package supply chain                                 | low      | accept      | No package-manager install occurs; Phase 2 uses existing Node and repository tooling only.                                                                                                                  |
| </threat_model> |

## Artifacts this phase produces

- Created symbols: `MalformedMcpServersError` and the shared total raw-field classifier in `bridges/mcp/stage.ts`; exact classifier result type if named by the implementation.
- Created files: none.
- Created schemas, migrations, external-service configuration, UI artifacts, or test-only seams: none.

<verification>
- Run the focused type/stage/unstage/index suites named above.
- Run direct coverage independently for each changed production-owner pair: types, stage, and unstage.
- Run `npm run typecheck` after the wave; Phase 2's final `npm run check` is owned by Plan 02-03.
</verification>

<success_criteria>

- PDEF-03's absent/object/malformed trichotomy is stable and shared.
- Malformed present values cannot enumerate, write, or report success.
- Existing top-level policies remain byte-for-byte behaviorally distinct.
- The specless fallback is closed by exact automated owner evidence, not a backstop or manual check.
  </success_criteria>

<output>
Create `.planning/phases/02-containment-and-input-safety/02-01-SUMMARY.md` when done.
</output>
