---
phase: 07-gate-integrity
plan: 13
type: execute
wave: 4
depends_on: ["07-02", "07-10", "07-12"]
files_modified:
  - tests/architecture/no-test-only-production-surface.test.ts
autonomous: true
requirements: [GGAT-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No `__`-prefixed member survives on any interface, type, or options object under `extensions/pi-claude-marketplace/`, and a gate proves it by walking the tree rather than by naming files."
    - "The gate fails against a temp-root copy in which one real production file has been given a `__`-prefixed member, passes against unmutated copies of the same files, and passes when the token appears only inside a comment."
    - "The nominal-type brand `declare const __absolutePluginRootBrand: unique symbol` is carved out explicitly and the carve-out is documented beside the pattern, so a future reader knows it was classified rather than missed."
    - "The carve-out is narrow enough that removing the brand from the tree would not silently widen it: it matches the `declare const … : unique symbol` form, not the file."
    - "The gate's walk asserts a non-zero file count, so a walk that stopped matching fails rather than passing over nothing."
    - "The typed optional seams `cloneCacheSeam?`, `credentialOps?`, `deviceFlowHttp?`, and the three promoted reinstall members are recorded as classified-and-kept, not silently unmatched."
  artifacts:
    - tests/architecture/no-test-only-production-surface.test.ts
  key_links:
    - "The gate ships only after `07-10` and `07-12` removed both live offenders, so it lands green against a tree that satisfies it rather than teaching readers to ignore a red gate."
    - "It reuses `assertNoForbiddenSurface` with an injected root and the shared temp-root controls, so it adds no fixture mechanic of its own and does not approach `duplicates.threshold: 3`."
  prohibitions:
    - "No allow-list forgives a named offender; the only exclusion is the structural `unique symbol` brand form, and it is documented."
    - "No `fallow-ignore` marker is added to make the gate pass."
    - "The gate does not match a `__`-prefixed identifier inside a comment; comments are stripped before matching."
    - "No production module is modified by this plan."
    - "No identifier or comment introduced here names a phase, plan, wave, or task number."
---

<objective>
Gate the class both live offenders belonged to, so a `__`-prefixed test-only member cannot return
to the production tree.

Purpose: `MF-DEC-07` and `D-05-01` forbid the form, and two instances survived until this phase —
one named in the decisions and one found only when research read the sibling file. A rule that
depends on someone remembering it has already failed twice here. `D-07-18` is explicit that no
allow-list entry is permitted, because this milestone has repeatedly found allow-lists go stale and
silent.

Output: a walking gate over `extensions/**` with a documented structural carve-out, an offender
derived from a real file, and both benign controls.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gate-integrity/07-CONTEXT.md
@.planning/phases/07-gate-integrity/07-RESEARCH.md
@.planning/phases/07-gate-integrity/07-PATTERNS.md
@.planning/phases/07-gate-integrity/07-10-SUMMARY.md
@.planning/phases/07-gate-integrity/07-12-SUMMARY.md
@.claude/rules/typescript-comments.md
@.claude/rules/typescript-unit-testing.md
@tests/architecture/gate-targets.ts
@tests/architecture/temp-root-control.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the walking gate with its offender and both benign controls</name>
  <files>tests/architecture/no-test-only-production-surface.test.ts</files>
  <behavior>
    - Scanning the real tree passes, and the walk reports a non-zero file count.
    - Scanning a temp root in which one real production file has been given a `readonly __somethingForTests?: X` member fails, with a message naming `MF-DEC-07` and stating the sanctioned alternative.
    - Scanning a temp root holding unmutated copies of the same files passes.
    - Scanning a temp root in which the `__`-prefixed token appears only inside a line comment passes.
    - Scanning a temp root containing the `declare const __brand: unique symbol` form passes, and scanning one where that same identifier appears as an interface member fails — the carve-out is structural, not name-based.
  </behavior>
  <read_first>
    - `tests/architecture/no-orchestrator-network.test.ts` as rewired by `07-01` (target list plus pattern list plus one `assertNoForbiddenSurface` call, the `ScanReport` visitation assertion, and the three temp-root control cases)
    - `tests/architecture/temp-root-control.ts` (`withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss`)
    - `tests/architecture/source-scan.ts` (`assertNoForbiddenSurface`'s `opts.root`, the `ScanReport`, and the mandatory `stripComments` step)
    - `extensions/pi-claude-marketplace/domain/plugin-root.ts` lines 10-22 (`declare const __absolutePluginRootBrand: unique symbol` — the structural carve-out)
    - `tests/architecture/import-boundaries.test.ts` around the `assert.ok(files.length > 0, …)` walk assertion (the non-zero-count form)
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §6 "Other `__`-prefixed / test-only production surface" (the full census and each entry's disposition) and §11 (the complexity ceilings that apply to test helpers)
  </read_first>
  <action>
Create `tests/architecture/no-test-only-production-surface.test.ts`. Build its target list by
walking every `.ts` file under the registry's `EXTENSION_ROOT_REL` rather than naming files: the
obligation is that no such member exists anywhere, and a named list would silently miss the next
module. Assert the walk returned a non-zero file count before scanning, with a message naming
`D-07-03`.

Write one forbidden pattern matching a `__`-prefixed identifier in a member or property position —
an identifier beginning with a double underscore followed by a letter, appearing before a `?:` or
`:` type annotation. Make it non-global: a `/g` regex carries `lastIndex` across `.test()` calls
and skips every second file in the walk, which `import-boundaries.test.ts:226-228` already records.

Carve out the nominal-type brand structurally rather than by file or by name. The one legitimate
occurrence is `declare const __absolutePluginRootBrand: unique symbol`, a type-system marker with
no runtime value. Exclude the `declare const … : unique symbol` form, so the carve-out survives the
brand being renamed and does not survive the same identifier appearing as an interface member.
Document the carve-out beside the pattern, naming what it excludes and why a name-based or
file-based exclusion would be weaker.

Delegate the scan to `assertNoForbiddenSurface` with the walked target list, capture the
`ScanReport`, and deep-compare `visited` against the walked list. Own the failure message here:
name `MF-DEC-07` and `D-05-01`, and state the sanctioned alternative — a production-owned
collaborator on the flow's composition root, in the form `ReinstallTransaction.replaceOperations`
now takes, or a named typed optional member in the form `cloneCacheSeam?` takes.

Add the three control cases using `withTempRoot`, materializing a small subset of the real files
rather than all of `extensions/**`: an offender that plants a `__`-prefixed member onto a real
file's copy, an unmutated benign control, and a comment near-miss. Add a fourth control for the
carve-out: a temp root carrying the brand form passes, and one carrying the same identifier as an
interface member fails.

Keep every helper small. ESLint's cognitive-complexity rule is off for `tests/**` and Fallow's is
not, so run the Fallow health gate rather than reading a green lint as evidence.
  </action>
  <verify>
    <automated>node --test tests/architecture/no-test-only-production-surface.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npx fallow health --fail-on-issues --format human</automated>
    <fails_when>non-zero exit, or the output reports any unit above threshold</fails_when>
    <automated>npm run typecheck && npx eslint tests/architecture/no-test-only-production-surface.test.ts --max-warnings=0</automated>
    <fails_when>non-zero exit, or eslint prints any line containing "error" or "warning"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/no-test-only-production-surface.test.ts` reports at least 5 passing cases: the real-tree scan, offender, unmutated benign, comment near-miss, and the two carve-out arms.
    - The gate scans the real tree green: `git grep -c "__" extensions/ --include=*.ts` is recorded in the SUMMARY alongside the gate's own offender count of 0.
    - Temporarily adding a `readonly __probeMember?: string` to a real production interface makes the real-tree case fail; observe it, restore, and record the observation in the SUMMARY.
    - The carve-out matches the `declare const … : unique symbol` form, not a filename and not the brand's identifier: `grep -c "plugin-root" tests/architecture/no-test-only-production-surface.test.ts` returns 0 and `grep -c "__absolutePluginRootBrand" tests/architecture/no-test-only-production-surface.test.ts` returns 0.
    - `grep -c "mkdtemp" tests/architecture/no-test-only-production-surface.test.ts` returns 0; the temp-root lifecycle comes from `temp-root-control.ts`.
    - No `fallow-ignore` marker was added: `git grep -c "fallow-ignore" -- extensions tests scripts` prints the same per-file counts before and after, and both listings are recorded against the eleven markers the codebase documentation censuses.
  </acceptance_criteria>
  <done>A walking gate proves no `__`-prefixed member survives under `extensions/`, fires on a real-file-derived offender, and stays green on both benign controls and the structural brand carve-out.</done>
</task>

<task type="auto">
  <name>Task 2: Record the classified-and-kept surface so nothing is silently unmatched</name>
  <files>tests/architecture/no-test-only-production-surface.test.ts</files>
  <read_first>
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §6 (the disposition table: the brand, `ReinstallReplacement.operations`, the three `cloneCacheSeam?` fields, and the confirmation that zero `_setXForTest` / `__test_*` seams survive)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts` lines 158-172, `fetch.ts` lines 105-118, `info.ts` lines 120-135 (the authorized typed-optional port form)
    - `.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md` (the port classification that authorizes them)
    - `.planning/phases/07-gate-integrity/07-12-SUMMARY.md` (the three promoted reinstall members, which join the same classification)
  </read_first>
  <action>
The gate's pattern matches `__`-prefixed members. Several optional seams under `extensions/` are
deliberately kept and are not matched by it: `cloneCacheSeam?` on three orchestrators plus the three
members `07-12` promoted onto the reinstall options, `credentialOps?`, `deviceFlowHttp?`, and
`ReinstallReplacement.operations`. Silence is not classification — a reader cannot tell an
intentionally-unmatched seam from one the pattern happens to miss.

Add a case to this gate that names the classified-and-kept set explicitly and asserts each named
member still exists in its module, so the classification is a live assertion rather than a comment.
If one of them is later removed, the case fails and forces a conscious re-classification instead of
leaving a stale claim behind. Cite `D-05-01` and state the criterion that distinguishes a kept
typed optional seam from a forbidden bag: a kept seam is a single named capability with its own
type and its own doc line, supplied by a consumer that genuinely owns it; a forbidden bag is an
anonymous container of several capabilities behind one `__`-prefixed member.

Also assert the absence of the two retired seam spellings — `_setXForTest` and `__test_`-prefixed
identifiers — over the same walked target list, so their return is caught by the same gate rather
than by nothing. Research measured zero live occurrences of either; every current hit is a comment
recording the pattern's removal, and comments are stripped before matching.

Confirm the gate stays green against the tree as `07-10` and `07-12` left it, and record in the
SUMMARY that both live offenders were removed before this gate shipped.
  </action>
  <verify>
    <automated>node --test tests/architecture/no-test-only-production-surface.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>node --test "tests/architecture/*.test.ts"</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run typecheck && npx eslint tests --max-warnings=0 && npx fallow health --fail-on-issues --format human && npx prettier --check "tests/architecture/*.ts"</automated>
    <fails_when>non-zero exit, or eslint prints "error"/"warning", or fallow reports any unit above threshold, or prettier names a file under "Code style issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate carries a case naming every classified-and-kept optional seam and asserting each still exists in its module; renaming one makes that case fail — observe it for one member, restore, and record the observation.
    - The gate asserts zero occurrences of `_setXForTest` and of `__test_`-prefixed identifiers over the walked target list, on comment-stripped source.
    - `node --test "tests/architecture/*.test.ts"` exits 0.
    - The SUMMARY records that `__operations` and both `__deps` members were removed by earlier plans in this phase before this gate shipped, with the `git grep` counts confirming zero.
  </acceptance_criteria>
  <done>Every deliberately-kept optional seam is a live assertion rather than an unmatched silence, and the two retired seam spellings are gated by the same file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → orchestrator options | A `__`-prefixed options member is a substitution point for production collaborators reachable by anything that can construct the options object. |
| test process → filesystem | The gate's controls create and delete real directories under `os.tmpdir()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-37 | Tampering | reintroduced `__`-prefixed collaborator bag | high | mitigate | The gate walks the whole extension tree rather than a named list, so a bag added to a module nobody thought to enumerate is still caught. No allow-list forgives a named offender; the only exclusion is the structural `unique symbol` brand form, which carries no runtime value and cannot substitute a collaborator. |
| T-07-38 | Repudiation | a silently-unmatched kept seam | medium | mitigate | Task 2 turns the classified-and-kept set into a live assertion, so a reader can tell an intentional exclusion from a pattern that happens to miss. A comment claiming a classification is worth less than a case that fails when the classification stops holding. |
| T-07-39 | Tampering | temp-root controls | medium | mitigate | Every write is joined from `withTempRoot`'s `mkdtemp` return value, and disposal happens in that helper's `finally`; the gate calls `mkdtemp` and `rm` nowhere itself, which the acceptance criteria assert. |
| T-07-40 | Repudiation | a gate landing red against its own tree | high | mitigate | The gate is sequenced after `07-10` and `07-12` removed both live offenders, and the SUMMARY records the zero counts. A gate that ships red is one people learn to skip. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction: this plan installs no package and changes no `package.json` line. |
</threat_model>

## Artifacts this phase produces

New files:

- `tests/architecture/gate-targets.ts` — the literal repo-relative target registry (`D-07-05`)
- `tests/architecture/gate-targets.test.ts` — registry resolution clauses plus the self-hosting meta-gate (`D-07-06`)
- `tests/architecture/temp-root-control.ts` — shared mkdtemp / copy / mutate helper
- `tests/architecture/eslint-effective-config.ts` — shared `calculateConfigForFile` resolver and the three offender blocks
- `tests/architecture/eslint-effective-config.test.ts` — the `no-console` severity sweep gate (`GGAT-03`)
- `tests/architecture/closed-set-enrollment.test.ts` — `ClaudeHookEvent` / `Dependency` enrollment gate (`GGAT-04`)
- `tests/architecture/no-test-only-production-surface.test.ts` — `__`-prefixed member gate (`GGAT-04`)
- `tests/architecture/unowned-exports-census.test.ts` — pinned production-unowned-export census (`GGAT-04`)
- `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` — closed-by-earlier-phase evidence record

New exported symbols:

- `ScanReport` (`visited`, `waived`) and the `opts.root` parameter on `assertNoForbiddenSurface` — `tests/architecture/source-scan.ts`
- `withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss` — `tests/architecture/temp-root-control.ts`
- `resolveEffectiveConfig`, `BLANKET_NO_CONSOLE_OFF`, `ZONE_SUBSTITUTION`, `RESTRICTED_PATHS_OFF` — `tests/architecture/eslint-effective-config.ts`
- Registry constants in `tests/architecture/gate-targets.ts`: `EXTENSION_ROOT_REL`, `ORCHESTRATORS_REL`, `ARCHITECTURE_DIR_REL`, `NETWORK_FREE_TARGETS`, `MISSING_TARGET_PROBES`, `NO_CONSOLE_EXEMPT_TARGETS`, `ZONE_FOLDER_TARGETS`, `PLUGIN_LEDGER_TARGETS`, `MARKETPLACE_LEDGER_TARGETS`, `HOOKS_LIFECYCLE_TARGETS`, `VOCABULARY_GUARD_DOC_TARGETS`, `UNOWNED_EXPORT_CENSUS`, plus one group per migrated gate
- `selectBase(root)` and the `root` parameter on `gitLines` / `changedPaths` / `pairsForChangedPaths` — `scripts/test-coverage-direct.mjs`
- `_BucketAEventsCoverageProof` (type) — `extensions/pi-claude-marketplace/domain/components/hook-events.ts`

Production-owned collaborators replacing test-only surface:

- `ReinstallTransaction.replaceOperations: ReinstallReplaceOperations` plus the required second parameter of `replaceReinstalledPlugin(input, operations)` replace `ReplaceReinstalledPluginInput.__operations` — `orchestrators/plugin/reinstall-replace.ts`
- `stateTransaction?`, `removeDataDir?`, `cloneCacheSeam?` become typed top-level members of `ReinstallPluginOptions` / `ReinstallPluginsOptions`, replacing `__deps` and retiring `ReinstallPluginDeps` — `orchestrators/plugin/reinstall-flow.ts`

ESLint offender configs are **not** committed as files. Each offender is
`new ESLint({ cwd: REPO_ROOT, overrideConfigFile: "eslint.config.js", overrideConfig: [<exactly one block>] })`.

Removed by this phase: `ReplaceReinstalledPluginInput.__operations`; `ReinstallPluginDeps` and both
`__deps` members; the `export` keyword on `MARKETPLACE_VALIDATOR` and `surfacePostCommitWarnings`;
`loadZones()`; the `import-boundaries.test.ts` canary case and `tests/fixtures/bad-imports/**`;
`eslint.config.js` BLOCK D; the `hooks-dispatch.test.ts` config-scrape case; the duplicated byte
pins at `markers-snapshot.test.ts:55-70`; the orphaned comment at the end of
`orchestrators/plugin/info.ts`.

<verification>
- `node --test "tests/architecture/*.test.ts"` exits 0.
- `npm run typecheck && npx eslint tests --max-warnings=0` exits 0.
- `npx fallow health --fail-on-issues` reports "0 above threshold".
- A planted `__`-prefixed member on a real production interface was observed failing the real-tree case.
</verification>

<success_criteria>
- The `__`-prefixed class is gated by a walking scan with no allow-list.
- The one legitimate occurrence is carved out structurally and documented.
- Every deliberately-kept optional seam is asserted rather than silently unmatched.
</success_criteria>

<output>
Create `.planning/phases/07-gate-integrity/07-13-SUMMARY.md` when done. Record the observed planted
offender, the observed kept-seam negative control, the walked file count, and the `git grep` counts
confirming zero `__operations` and zero `__deps`.
</output>
