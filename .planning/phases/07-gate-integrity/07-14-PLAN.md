---
phase: 07-gate-integrity
plan: 14
type: execute
wave: 4
depends_on: ["07-02", "07-12"]
files_modified:
  - tests/orchestrators/plugin/install-flow.test.ts
  - tests/orchestrators/plugin/update-flow.test.ts
  - tests/orchestrators/plugin/list-flow.test.ts
  - tests/shared/notification-dispatch.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/domain/plugin-resolver.test.ts
  - tests/orchestrators/plugin/reinstall-flow.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/edge/handlers/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/reinstall-record.test.ts
  - tests/platform/pi-api.test.ts
  - tests/persistence/state-io.test.ts
  - tests/architecture/partial-vocabulary-guard.test.ts
autonomous: true
requirements: [GGAT-01]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The vocabulary guard reads the recursive unit-test sources it claims to police, not only the non-recursive `tests/architecture/` directory, so retired terminology in an owner suite fails the gate."
    - "The 137 retired-vocabulary lines the widened scope reaches are repaired, except the three out-of-scope homonyms, which get narrow per-token allowlist entries in the guard's existing `.filter` form."
    - "The three homonyms are distinguished from genuine stale vocabulary by what they name: a Pi `AgentMessage` role, a Pi `StopReason`, and a JSON fixture key are out-of-scope senses, while a bare `\"unsupported\"` reason is stale because every live reason literal carries an interior space."
    - "The guard fails against a temp-root copy carrying a planted retired token and passes against the unmutated copy, so the widened scope is proved to fire rather than merely to read more files."
    - "The guard asserts its collected source set is non-empty and reports how many files it read, so a collection that stopped matching fails rather than passing over nothing."
    - "Every repaired test title and comment keeps its durable requirement and decision IDs; only the retired vocabulary changes."
  artifacts:
    - tests/architecture/partial-vocabulary-guard.test.ts
  key_links:
    - "`collectGuardedSources()` is the single collection point; widening it there reaches every clause in the file at once rather than per-clause."
    - "The guard's existing `filesContaining` / `filesMatching` helpers already support a `.filter(f => f !== ALLOW)` allowlist, which is the established form for the three homonyms."
  prohibitions:
    - "No allowlist entry is added for a line that is genuine stale vocabulary rather than an out-of-scope homonym."
    - "No repair removes a durable requirement, decision, or finding ID from a test title or comment."
    - "No repair changes what a case asserts; only its wording changes."
    - "No token list in the guard is narrowed to make a repair unnecessary."
    - "No identifier or comment introduced here names a phase, plan, wave, or task number."
---

<objective>
Widen the vocabulary guard to the recursive unit-test sources it already claims to police, repair
the retired terminology that widening reaches, and prove the widened scope fires.

Purpose: `collectGuardedSources()` reads the whole extension tree, two docs, `tests/architecture/*.ts`
non-recursively, and one `catalog-uat` file. It never reads
`tests/{bridges,domain,edge,orchestrators,persistence,platform,scripts,shared,transaction}/**` —
271 files, of which 12 carry 137 lines of retired vocabulary today. A guard that names a scope it
does not read is the phase's subject in miniature.

Output: a guard that reads what it claims, 137 lines repaired or classified, three narrow homonym
allowlists with their reasoning recorded, and a planted-offender control.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gate-integrity/07-CONTEXT.md
@.planning/phases/07-gate-integrity/07-RESEARCH.md
@.planning/phases/07-gate-integrity/07-PATTERNS.md
@.claude/rules/typescript-comments.md
@.claude/rules/typescript-unit-testing.md
@tests/architecture/partial-vocabulary-guard.test.ts
@tests/architecture/gate-targets.ts
@tests/architecture/temp-root-control.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the three largest suites' retired vocabulary</name>
  <files>tests/orchestrators/plugin/install-flow.test.ts, tests/orchestrators/plugin/update-flow.test.ts, tests/orchestrators/plugin/list-flow.test.ts</files>
  <read_first>
    - `tests/architecture/partial-vocabulary-guard.test.ts` lines 150-240 (`ABSENT_FLAGS`, `ABSENT_STATUS_LITERALS`, `ABSENT_RENDER_TOKENS`, `ABSENT_IDENTIFIERS`, `ABSENT_FORCE_PROSE` — the token lists that define what "retired" means) and lines 240-320 (the allowlist `.filter` form and the presence assertions that protect the out-of-scope homonyms)
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §8 (the per-file repair census, the representative stale forms with line numbers, and the correction that the install suite was split so the stale terminology now lives in `install-flow.test.ts`)
    - `tests/orchestrators/plugin/install-flow.test.ts` around lines 488, 591, 874, 4234, 4242, 4296, 4347-4390 (the representative stale titles and prose)
    - `.claude/rules/typescript-comments.md` (durable IDs are required and kept; planning-artifact references are forbidden)
    - `docs/output-catalog.md` and `docs/messaging-style-guide.md` (the live vocabulary the repairs move to)
  </read_first>
  <action>
Apply the guard's own token lists to these three suites and repair every match. Research measured
37, 37, and 29 matching lines respectively.

Repair means replacing the retired term with the live one, not deleting the line. The retired
forms are verdict and flag vocabulary that was renamed; the live vocabulary is in
`docs/output-catalog.md` and the guard's own presence assertions. A test title that says a plugin
"blocks without `--force`" describes a behaviour that still happens under a different name — restate
it, and keep every `PHOOK-`, `SEV-`, `FSTAT-`, and `D-` identifier the title carries. Those IDs are
the traceability anchors the comment rule requires; the vocabulary around them is what rots.

Do not change what any case asserts. If a repair would require changing an expected string, stop
and record it: that is a behaviour question, not a vocabulary one, and it belongs in the SUMMARY
rather than in a silent edit.

Verify by running the three owner suites and by re-applying the guard's token lists to the three
files, expecting zero matches. Record the before and after match counts per file.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/install-flow.test.ts tests/orchestrators/plugin/update-flow.test.ts tests/orchestrators/plugin/list-flow.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts</automated>
    <fails_when>non-zero exit, or the output reports a coverage shortfall for that module</fails_when>
    <automated>npx eslint tests/orchestrators/plugin --max-warnings=0</automated>
    <fails_when>non-zero exit, or eslint prints any line containing "error" or "warning"</fails_when>
  </verify>
  <acceptance_criteria>
    - Re-applying the guard's token lists to the three files yields zero matches; record the before counts (37, 37, 29) and the after counts in the SUMMARY.
    - The three suites' case counts are unchanged; record them before and after.
    - Every repaired title retains its durable requirement and decision IDs; spot-check five repaired titles in the SUMMARY showing the ID before and after.
    - No expected string in any assertion changed: count the `assert.` occurrences in each of the three files before and after with `grep -c "assert\." <file>`, record both counts, and require they match; any difference is explained line by line.
  </acceptance_criteria>
  <done>The three largest suites carry no retired vocabulary, with every durable ID preserved and no assertion touched.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the five mid-sized suites</name>
  <files>tests/shared/notification-dispatch.test.ts, tests/orchestrators/plugin/info.test.ts, tests/domain/plugin-resolver.test.ts, tests/orchestrators/plugin/reinstall-flow.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <read_first>
    - `tests/architecture/partial-vocabulary-guard.test.ts` lines 150-240 (the token lists)
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §8 (the per-file counts: 9, 7, 4, 4, and 3)
    - `.planning/phases/07-gate-integrity/07-12-SUMMARY.md` (the reinstall-flow suite as `07-12` left it, so this repair applies to the migrated file rather than the pre-migration one)
    - The five files themselves
  </read_first>
  <action>
Apply the same repair to the five mid-sized suites, using the same rule: replace the retired term,
keep every durable ID, change no assertion.

`tests/orchestrators/plugin/reinstall-flow.test.ts` was rewritten at 54 call sites by `07-12`.
Re-run the token lists against the current file rather than against the counts research measured,
because the migration may have moved or duplicated a line. Record both the measured-before count
and the current count.

For each match, decide repair or homonym before editing, and record the decision. Research
classified three occurrences repo-wide as out-of-scope homonyms, none of them in these five files —
so a homonym found here is new information and belongs in the SUMMARY, not in a silent allowlist
entry.
  </action>
  <verify>
    <automated>node --test tests/shared/notification-dispatch.test.ts tests/orchestrators/plugin/info.test.ts tests/domain/plugin-resolver.test.ts tests/orchestrators/plugin/reinstall-flow.test.ts tests/orchestrators/marketplace/update.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npx eslint tests --max-warnings=0</automated>
    <fails_when>non-zero exit, or eslint prints any line containing "error" or "warning"</fails_when>
  </verify>
  <acceptance_criteria>
    - Re-applying the guard's token lists to the five files yields zero matches, or yields only occurrences classified as homonyms in the SUMMARY with their reasoning.
    - The five suites' case counts are unchanged; record them before and after.
    - The SUMMARY records the current match count for `reinstall-flow.test.ts` alongside the count research measured, with any difference explained.
  </acceptance_criteria>
  <done>The five mid-sized suites carry no unclassified retired vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widen the guard, allowlist the three homonyms, and prove it fires</name>
  <files>tests/architecture/partial-vocabulary-guard.test.ts, tests/edge/handlers/plugin/reinstall.test.ts, tests/orchestrators/plugin/reinstall-record.test.ts, tests/platform/pi-api.test.ts, tests/persistence/state-io.test.ts</files>
  <behavior>
    - `collectGuardedSources()` reads every `.ts` file under `tests/` except the guard itself and the separately-scripted `e2e` and `integration` roots, in addition to everything it already read.
    - The collected source count is non-zero and is asserted, so a collection that stopped matching fails.
    - Every absence clause passes against the repaired tree.
    - A temp-root copy of one collected file carrying a planted retired token fails the guard; the unmutated copy passes.
    - Each of the three homonym allowlist entries is scoped to one path and one token, and removing an entry makes exactly one clause fail.
  </behavior>
  <read_first>
    - `tests/architecture/partial-vocabulary-guard.test.ts` lines 50-110 (`readInto`, `collectExtensionSources`, `collectGuardedSources`, `SELF`, the non-recursive `ARCH_DIR` read) and lines 240-320 (the `ALLOW` filter form and the presence assertions)
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §8 "Three of those 137 hits are out-of-scope homonyms" (the three exact sites and why `reinstall-record.test.ts:249` is not one of them)
    - `tests/platform/pi-api.test.ts` lines 90-105 and `tests/persistence/state-io.test.ts` around line 462 (the three homonym sites)
    - `tests/orchestrators/plugin/reinstall-record.test.ts` around line 249 (the genuine stale `reasons: ["unsupported"]`)
    - `extensions/pi-claude-marketplace/shared/notification-types.ts` lines 10-20 (the live reason literals, each carrying an interior space, which is what distinguishes them from the bare token)
    - `tests/architecture/temp-root-control.ts` and `tests/architecture/gate-targets.ts`
  </read_first>
  <action>
Repair the two remaining genuine sites first: `tests/edge/handlers/plugin/reinstall.test.ts` (3
lines) and `tests/orchestrators/plugin/reinstall-record.test.ts:249`, whose
`reasons: ["unsupported"]` is stale rather than a homonym — every live reason literal
(`"unsupported source"`, `"unsupported component"`, `"unsupported hooks"`) carries an interior
space, so a bare `"unsupported"` names no live reason.

Widen `collectGuardedSources()` to read every `.ts` file under `tests/` recursively, excluding the
guard file itself (it necessarily spells the retired tokens to forbid them) and excluding the `e2e`
and `integration` roots, which have their own npm scripts and are outside the unit suite this guard
serves. Keep the existing extension-tree, docs, and `catalog-uat` reads — the widening adds, it does
not replace. Add an assertion that the collected map is non-empty and record the collected file
count in the failure message, so a collection that stopped matching fails loudly instead of passing
over nothing.

Add exactly three allowlist entries, each in the guard's existing `.filter(f => f !== ALLOW)` form,
each scoped to one path and one token, each with a comment stating the out-of-scope sense it
protects: a Pi `AgentMessage` role and a Pi `StopReason` in `tests/platform/pi-api.test.ts`, and a
JSON fixture key in `tests/persistence/state-io.test.ts`. Say in each comment what makes it a
homonym rather than stale vocabulary — it names a Pi-side value or a stored key, not this project's
verdict vocabulary. Do not widen an entry to a whole file where one token suffices.

Prove the widened guard fires. Add a case that materializes one collected file into a temp root
through `withTempRoot` and `materializeTargets`, plants a retired token into the copy, and asserts
the guard's clause fails against the temp root; add the unmutated benign control beside it. This
requires the clause under test to accept an injected root — extract the smallest helper that lets
one clause run against a supplied source map, rather than making the whole collection injectable, so
the change stays inside both complexity ceilings.

Then run the guard against the real tree and confirm every clause passes. If a clause fails on a
file this plan did not name, that is a file research's census missed: repair it and record it, do
not add an allowlist entry to make the run green.
  </action>
  <verify>
    <automated>node --test tests/architecture/partial-vocabulary-guard.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npx fallow health --fail-on-issues --format human && npm run typecheck && npx eslint tests --max-warnings=0</automated>
    <fails_when>non-zero exit, or fallow reports any unit above threshold, or eslint prints "error"/"warning"</fails_when>
  </verify>
  <acceptance_criteria>
    - `collectGuardedSources()` reads a file count strictly greater than before; record both counts in the SUMMARY, with the after count including the 271 previously-unscanned unit-test files.
    - The guard carries exactly three allowlist entries added by this plan, each naming one path and one token, each with a homonym rationale comment.
    - Removing one allowlist entry makes exactly one clause fail; observe it for one entry, restore, and record the observation.
    - The planted-offender case fails and the unmutated benign control passes; observe both and record them.
    - `npm test` exits 0, and the SUMMARY records the total unit-suite case count alongside the count `07-06` recorded.
    - No file outside this plan's `files_modified` needed an allowlist entry; if one did, it is named in the SUMMARY as a census miss with its repair.
  </acceptance_criteria>
  <done>The guard reads the scope it claims, fires on a planted offender, and carries exactly three narrow, justified homonym allowlists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | The planted-offender control creates and deletes a directory under `os.tmpdir()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-41 | Repudiation | a guard naming a scope it does not read | high | mitigate | The widening is proved by a collected-file count assertion and a planted-offender control, not by reading the new code. A guard that reads more files but was never seen to fire on one of them has changed nothing observable. |
| T-07-42 | Repudiation | an allowlist entry hiding genuine drift | high | mitigate | Exactly three entries, each scoped to one path and one token, each carrying its homonym rationale, each proved load-bearing by removal. Any further entry required to make the run green is recorded as a census miss and repaired instead. |
| T-07-43 | Tampering | temp-root control | medium | mitigate | The control uses `withTempRoot` and `materializeTargets`; every write is joined from the `mkdtemp` return value and disposal happens in that helper's `finally`. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction: this plan installs no package and changes no `package.json` line. |

No production module is modified by this plan, so no ASVS L1 production-surface threat applies.
</threat_model>

## Artifacts this phase produces

New files:

- `tests/architecture/gate-targets.ts` — the literal repo-relative target registry (`D-07-05`)
- `tests/architecture/gate-targets.test.ts` — registry resolution clauses plus the self-hosting meta-gate (`D-07-06`)
- `tests/architecture/temp-root-control.ts` — shared mkdtemp / copy / mutate helper
- `tests/architecture/eslint-effective-config.ts` — shared `calculateConfigForFile` resolver and the three offender blocks
- `tests/architecture/eslint-effective-config.test.ts` — the `no-console` severity sweep gate (`GGAT-03`)
- `tests/architecture/closed-set-enrollment.test.ts` — `ClaudeHookEvent` / `Dependency` enrollment gate (`GGAT-04`)
- `tests/architecture/no-test-only-production-surface.test.ts` — `__`-prefixed member gate (`GGAT-04`)
- `tests/architecture/unowned-exports-census.test.ts` — pinned production-unowned-export census (`GGAT-04`)
- `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` — closed-by-earlier-phase evidence record

New exported symbols:

- `ScanReport` (`visited`, `waived`) and the `opts.root` parameter on `assertNoForbiddenSurface` — `tests/architecture/source-scan.ts`
- `withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss` — `tests/architecture/temp-root-control.ts`
- `resolveEffectiveConfig`, `BLANKET_NO_CONSOLE_OFF`, `ZONE_SUBSTITUTION`, `RESTRICTED_PATHS_OFF` — `tests/architecture/eslint-effective-config.ts`
- Registry constants in `tests/architecture/gate-targets.ts`: `EXTENSION_ROOT_REL`, `ORCHESTRATORS_REL`, `ARCHITECTURE_DIR_REL`, `NETWORK_FREE_TARGETS`, `MISSING_TARGET_PROBES`, `NO_CONSOLE_EXEMPT_TARGETS`, `ZONE_FOLDER_TARGETS`, `PLUGIN_LEDGER_TARGETS`, `MARKETPLACE_LEDGER_TARGETS`, `HOOKS_LIFECYCLE_TARGETS`, `VOCABULARY_GUARD_DOC_TARGETS`, `UNOWNED_EXPORT_CENSUS`, plus one group per migrated gate
- `selectBase(root)` and the `root` parameter on `gitLines` / `changedPaths` / `pairsForChangedPaths` — `scripts/test-coverage-direct.mjs`
- `_BucketAEventsCoverageProof` (type) — `extensions/pi-claude-marketplace/domain/components/hook-events.ts`

Production-owned collaborators replacing test-only surface:

- `ReinstallTransaction.replaceOperations: ReinstallReplaceOperations` plus the required second parameter of `replaceReinstalledPlugin(input, operations)` replace `ReplaceReinstalledPluginInput.__operations` — `orchestrators/plugin/reinstall-replace.ts`
- `stateTransaction?`, `removeDataDir?`, `cloneCacheSeam?` become typed top-level members of `ReinstallPluginOptions` / `ReinstallPluginsOptions`, replacing `__deps` and retiring `ReinstallPluginDeps` — `orchestrators/plugin/reinstall-flow.ts`

ESLint offender configs are **not** committed as files. Each offender is
`new ESLint({ cwd: REPO_ROOT, overrideConfigFile: "eslint.config.js", overrideConfig: [<exactly one block>] })`.

Removed by this phase: `ReplaceReinstalledPluginInput.__operations`; `ReinstallPluginDeps` and both
`__deps` members; the `export` keyword on `MARKETPLACE_VALIDATOR` and `surfacePostCommitWarnings`;
`loadZones()`; the `import-boundaries.test.ts` canary case and `tests/fixtures/bad-imports/**`;
`eslint.config.js` BLOCK D; the `hooks-dispatch.test.ts` config-scrape case; the duplicated byte
pins at `markers-snapshot.test.ts:55-70`; the orphaned comment at the end of
`orchestrators/plugin/info.ts`.

<verification>
- `node --test tests/architecture/partial-vocabulary-guard.test.ts` exits 0 with the widened collection.
- `npm test` exits 0.
- `npx fallow health --fail-on-issues` reports "0 above threshold".
- The planted offender was observed failing, the unmutated control passing, and one allowlist removal was observed failing exactly one clause.
</verification>

<success_criteria>
- The guard reads every unit-test source it claims to police, and asserts the collection is non-empty.
- 137 retired-vocabulary lines are repaired or classified, with no durable ID lost.
- Exactly three narrow homonym allowlists exist, each proved load-bearing.
</success_criteria>

<output>
Create `.planning/phases/07-gate-integrity/07-14-SUMMARY.md` when done. Record the per-file match
counts before and after, the collected-file count before and after, the three allowlist entries with
their rationale, the observed planted-offender and allowlist-removal controls, and any census miss.
</output>
