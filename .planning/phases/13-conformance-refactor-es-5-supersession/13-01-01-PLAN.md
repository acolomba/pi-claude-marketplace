---
phase: 13-conformance-refactor-es-5-supersession
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/presentation/compact-line.ts
  - extensions/pi-claude-marketplace/presentation/sort.ts
  - extensions/pi-claude-marketplace/presentation/index.ts
  - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts
  - docs/messaging-style-guide.md
  - tests/presentation/compact-line.test.ts
autonomous: true
requirements:
  - CMC-01
  - CMC-02
  - CMC-04
  - CMC-05
  - CMC-06
  - CMC-07
  - CMC-09
  - CMC-10
  - CMC-12
  - CMC-13
  - CMC-34

must_haves:
  truths:
    - "The `RowSpec` discriminated union is the sole compact-line emission contract; every Wave 2 callsite constructs a typed `RowSpec` and the renderer owns MSG-GR-1..5 token order."
    - "`STATUS_TOKENS` extends to 15 entries with `reinstalled` and `docs/messaging-style-guide.md` YAML frontmatter + §3 status-tokens table mirror the change so `tests/architecture/grammar-frontmatter.test.ts` stays green."
    - "`(upgradable)` is structurally constrained to `PluginListRow.status` only (MSG-PL-4) -- TS compile fails if any other variant tries to set it."
    - "`(uninstalled)` rows cannot structurally carry soft-dep predicates (MSG-SD-3) -- the `PluginInlineUninstalledRow` variant has no `declaresAgents/Mcp` fields."
    - "`presentation/sort.ts::compareByNameThenScope` is the SINGLE per-scope sort comparator (MSG-GR-3); future surfaces consume this helper."
  artifacts:
    - path: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      provides: "RowSpec union + grammar-aware renderer + iconForPluginRow + private icon constants"
      exports: ["renderRow", "RowSpec", "PluginInlineRow", "PluginInlineUninstalledRow", "PluginCascadeRow", "PluginListRow", "MarketplaceRow", "EmptyToken", "EntityErrorRow", "Scope", "SoftDepProbe"]
    - path: "extensions/pi-claude-marketplace/presentation/sort.ts"
      provides: "compareByNameThenScope helper (MSG-GR-3)"
      exports: ["compareByNameThenScope"]
    - path: "extensions/pi-claude-marketplace/presentation/index.ts"
      provides: "barrel updated with new exports"
    - path: "extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts"
      provides: "STATUS_TOKENS extended to 15 entries with reinstalled (D-13-20)"
      contains: "reinstalled"
    - path: "docs/messaging-style-guide.md"
      provides: "frontmatter status_tokens: + §3 table extended with reinstalled"
      contains: "reinstalled"
    - path: "tests/presentation/compact-line.test.ts"
      provides: "unit tests for token order (CMC-01), reasons block (CMC-04), plugin icons (CMC-06), per-row soft-dep emission (CMC-13), MSG-IC-1..3"
  key_links:
    - from: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      to: "extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts"
      via: "import { StatusToken }"
      pattern: "Extract<StatusToken,"
    - from: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      to: "extensions/pi-claude-marketplace/shared/grammar/reasons.ts"
      via: "import { Reason }"
      pattern: "readonly Reason\\[\\]"
    - from: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      to: "extensions/pi-claude-marketplace/shared/errors.ts"
      via: "import { assertNever }"
      pattern: "assertNever\\(row\\)"
    - from: "tests/architecture/grammar-frontmatter.test.ts"
      to: "extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts"
      via: "set-equality assertion against docs/messaging-style-guide.md frontmatter"
      pattern: "STATUS_TOKENS"
---

<objective>
Land the keystone Wave 1 renderer primitives: the `RowSpec` discriminated union and grammar-aware `renderRow` composer in `presentation/compact-line.ts`, the `compareByNameThenScope` sort helper in `presentation/sort.ts`, the `presentation/index.ts` barrel update, the `STATUS_TOKENS` extension to 15 entries adding `reinstalled` (D-13-20) with byte-equal updates to `docs/messaging-style-guide.md` YAML frontmatter + §3 status-tokens table, and the unit test suite for the renderer at `tests/presentation/compact-line.test.ts`.

Purpose: This plan defines the contract every Wave 2 sub-wave consumes unchanged. The renderer owns MSG-GR-1 token order, the `@<marketplace>` carve-out (MSG-GR-2), the `<marker>` slot (MSG-GR-5), the `[<scope>]` brackets, the `{reasons}` block (MSG-GR-4), the icon discipline (MSG-IC-1..3), and the per-row soft-dep injection (MSG-SD-1..3). Wave 2 sub-waves cannot start until this lands.

Output: 6 new/modified source files + 1 doc file + 1 new test file; together they encode the entire compact-line grammar in TypeScript types + a single `switch (row.kind)` renderer with `assertNever` exhaustiveness.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-VALIDATION.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
@extensions/pi-claude-marketplace/presentation/plugin-list.ts
@extensions/pi-claude-marketplace/presentation/marketplace-list.ts
@extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts
@extensions/pi-claude-marketplace/shared/grammar/reasons.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/platform/pi-api.ts
@extensions/pi-claude-marketplace/orchestrators/edge-deps.ts

<interfaces>
Key codebase types and contracts the executor must consume directly:

From `extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` (Phase 12 carry-forward, lines 37-54):
```typescript
export const STATUS_TOKENS = [
  "installed", "updated", "uninstalled", "added", "removed",
  "available", "unavailable", "upgradable", "skipped", "failed",
  "rollback failed", "manual recovery", "no marketplaces", "no plugins",
] as const;
export type StatusToken = (typeof STATUS_TOKENS)[number];
```
After this plan's D-13-20 edit, the array has 15 entries with `"reinstalled"` inserted after `"updated"`.

From `extensions/pi-claude-marketplace/shared/grammar/reasons.ts` (Phase 12 carry-forward):
```typescript
export const REASONS = [/* 23 entries; binding count per Phase 12 D-CMC-04 */] as const;
export type Reason = (typeof REASONS)[number];
```

From `extensions/pi-claude-marketplace/shared/errors.ts:12`:
```typescript
export function assertNever(x: never): never {
  throw new Error(`Unexpected value: ${String(x)}`);
}
```

From `extensions/pi-claude-marketplace/platform/pi-api.ts` (existing soft-dep probe shape; consumed via `presentation/soft-dep.ts` re-export):
```typescript
export interface SoftDepStatus {
  readonly piSubagentsLoaded: boolean;
  readonly piMcpAdapterLoaded: boolean;
}
export function softDepStatus(pi: ExtensionAPI): SoftDepStatus;
```
This is the EXACT shape the renderer accepts as its `SoftDepProbe` injected dependency.

Existing precedent at `extensions/pi-claude-marketplace/presentation/plugin-list.ts:22-25, 30, 32-38, 45`:
- Private icon constants `ICON_INSTALLED = "●"`, `ICON_AVAILABLE = "○"`, `ICON_UNINSTALLABLE = "⊘"` -- THESE MOVE to compact-line.ts (a second consumer arrives; D-CMC-07 promotion criterion satisfied).
- `MAX_LINE_COLUMN = 66` and `truncateColumn66(s)` -- THESE STAY private in plugin-list.ts (list-only per MSG-PL-1; consumed by compact-line.ts ONLY within the `PluginListRow` rendering path). Move them to compact-line.ts ONLY if PluginListRow rendering is implemented inside compact-line.ts; otherwise keep them in plugin-list.ts and call the helper from there in sub-wave 2d.
- `PluginRenderStatus = "installed" | "available" | "uninstallable"` literal-union precedent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend STATUS_TOKENS to 15 entries (D-13-20) and mirror in style guide</name>
  <files>extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts, docs/messaging-style-guide.md</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts (current 14-entry array + header comment that explicitly says "no 15th token")
    - docs/messaging-style-guide.md lines 1-30 (YAML frontmatter + opening sections; verify the current `status_tokens:` list has exactly 14 entries)
    - docs/messaging-style-guide.md §3 status-tokens table (find the markdown table that lists every token with the operator-mental-model column)
    - tests/architecture/grammar-frontmatter.test.ts (the byte-equality test that this edit MUST keep green)
    - docs/output-catalog.md lines 145-160 (catalog status-token reference) and lines 345-360 (reinstall cascade examples showing `(reinstalled)`)
  </read_first>
  <behavior>
    - `STATUS_TOKENS.length === 15`
    - `STATUS_TOKENS` contains `"reinstalled"` between `"updated"` and `"uninstalled"`
    - `docs/messaging-style-guide.md` YAML frontmatter `status_tokens:` list contains 15 entries with `reinstalled` byte-equal positioned after `updated`
    - `docs/messaging-style-guide.md` §3 status-tokens table has a `reinstalled` row with the operator-mental-model description
    - `tests/architecture/grammar-frontmatter.test.ts` STILL passes (the set-equality between code and frontmatter holds)
    - The header comment block at `shared/grammar/status-tokens.ts:30-35` (which currently says "there is NO 15th user-visible token") is rewritten to cite D-13-20 and the catalog `(reinstalled)` emission contract
  </behavior>
  <action>
    Per D-13-20: extend `STATUS_TOKENS` from 14 to 15 entries by adding the literal string `"reinstalled"` to the `as const` array in `shared/grammar/status-tokens.ts`. Place it immediately after `"updated"` (semantic grouping with the cascade-success tokens). Rewrite the file-header comment block (currently lines 30-35) that states "there is NO 15th user-visible token for the reinstall cascade" -- this reasoning was inverted by D-13-20. The new comment must cite D-13-20 and reference the `docs/output-catalog.md` reinstall cascade examples that render `(reinstalled)` on cascade rows.

    Mirror the change in `docs/messaging-style-guide.md` in the SAME commit (the byte-equality drift test in `tests/architecture/grammar-frontmatter.test.ts` will fail otherwise): (a) add `  - reinstalled` as a new line in the YAML frontmatter `status_tokens:` block, positioned after `  - updated` so the order matches the TS array; (b) add a `reinstalled` row to the §3 status-tokens table with the operator-mental-model column reading "operation just ran: reinstall partition" (per D-13-20 binding text).

    DO NOT touch `tests/architecture/grammar-frontmatter.test.ts`. The set-equality assertion is intentionally direction-agnostic; this plan's edits keep it green by adding the same token on both sides.

    Using `"reinstalled"` per D-13-20 (research suggested amending the catalog to `(installed)` -- rejected; the catalog stays unchanged and the closed set extends per the locked decision).
  </action>
  <verify>
    <automated>node --test tests/architecture/grammar-frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/grammar-frontmatter.test.ts` exits 0
    - `grep -c '"reinstalled"' extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` returns at least 1
    - `grep -cE '^\s*"(installed|updated|reinstalled|uninstalled|added|removed|available|unavailable|upgradable|skipped|failed|rollback failed|manual recovery|no marketplaces|no plugins)"' extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` returns at least 15
    - `grep -c '^  - reinstalled$' docs/messaging-style-guide.md` returns at least 1
    - `grep -nE '^\| `reinstalled`' docs/messaging-style-guide.md` returns at least 1 line (the §3 status-tokens table row)
    - `grep -c 'there is NO 15th user-visible token' extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` returns 0 (legacy header comment rewritten)
    - `grep -c 'D-13-20' extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts` returns at least 1
  </acceptance_criteria>
  <done>
    `STATUS_TOKENS.length === 15`; `node --test tests/architecture/grammar-frontmatter.test.ts` exits 0; `grep -c '^  - reinstalled$' docs/messaging-style-guide.md` returns at least 1; the §3 table has a row whose first cell contains `reinstalled`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create presentation/sort.ts with compareByNameThenScope (MSG-GR-3)</name>
  <files>extensions/pi-claude-marketplace/presentation/sort.ts</files>
  <read_first>
    - docs/messaging-style-guide.md §7 (MSG-GR-3 sort policy; per-scope; project-before-user tie-breaker; `localeCompare` with `sensitivity: 'base'`)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts:255-260 (current inline `localeCompare` precedent; the pattern this helper replaces)
    - extensions/pi-claude-marketplace/presentation/marketplace-list.ts (current implicit scope-bucket sort; will be replaced in sub-wave 2c)
    - extensions/pi-claude-marketplace/presentation/plugin-list.ts (existing `Scope = "user" | "project"` literal; the comparator must accept structurally `{ name: string; scope: Scope }`)
  </read_first>
  <behavior>
    - `compareByNameThenScope({name:"alpha",scope:"user"}, {name:"beta",scope:"user"}) < 0`
    - `compareByNameThenScope({name:"ALPHA",scope:"user"}, {name:"alpha",scope:"user"}) === 0` (case-insensitive `sensitivity: 'base'`)
    - `compareByNameThenScope({name:"alpha",scope:"project"}, {name:"alpha",scope:"user"}) < 0` (project before user when names tie)
    - `compareByNameThenScope({name:"alpha",scope:"user"}, {name:"alpha",scope:"project"}) > 0`
    - Sort is stable when both name and scope tie (returns 0; caller-side ordering preserved by Array.prototype.sort stability)
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/presentation/sort.ts` as a single-purpose pure-helper file (Phase 12 precedent: `reload-hint.ts` and `soft-dep.ts` are single-purpose modules per D-13-15).

    Export a single named function `compareByNameThenScope` accepting two structurally-typed parameters `{ readonly name: string; readonly scope: "user" | "project" }` and returning `number`. The implementation calls `a.name.localeCompare(b.name, undefined, { sensitivity: 'base' })` for the primary comparison; on tie (return value 0), use a strict ternary tie-breaker mapping project → -1 and user → +1 (`a.scope === b.scope ? 0 : a.scope === 'project' ? -1 : 1`) per MSG-GR-3 project-before-user.

    File-header comment must cite MSG-GR-3 and link to the style-guide §7 binding text. Define a local type alias `interface Sortable { readonly name: string; readonly scope: "user" | "project" }` and use it in the signature; this keeps the helper generic across `MarketplaceRow`, `PluginListRow`, and any other row carrying `name` + `scope`.

    No imports from anywhere else in the codebase (pure helper; closed under MSG-GR-3 only).
  </action>
  <verify>
    <automated>npm run typecheck && grep -c 'localeCompare' extensions/pi-claude-marketplace/presentation/sort.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - `test -f extensions/pi-claude-marketplace/presentation/sort.ts` succeeds
    - `grep -c 'export function compareByNameThenScope' extensions/pi-claude-marketplace/presentation/sort.ts` returns at least 1
    - `grep -c 'localeCompare' extensions/pi-claude-marketplace/presentation/sort.ts` returns at least 1
    - `grep -c "sensitivity: 'base'" extensions/pi-claude-marketplace/presentation/sort.ts` returns at least 1
    - `grep -c 'MSG-GR-3' extensions/pi-claude-marketplace/presentation/sort.ts` returns at least 1 (file-header citation)
    - `grep -cE '^(import|from)' extensions/pi-claude-marketplace/presentation/sort.ts` returns 0 (pure helper; no codebase imports)
  </acceptance_criteria>
  <done>
    File exists; typecheck passes; `grep -c 'localeCompare' extensions/pi-claude-marketplace/presentation/sort.ts` returns at least 1; `grep -c "sensitivity: 'base'" extensions/pi-claude-marketplace/presentation/sort.ts` returns at least 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create presentation/compact-line.ts with RowSpec union + renderRow + token grammar</name>
  <files>extensions/pi-claude-marketplace/presentation/compact-line.ts, tests/presentation/compact-line.test.ts</files>
  <read_first>
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md §"Pattern 1: RowSpec discriminated union" (the proposed 8-variant union with explicit `kind`; lines 256-391)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md §"NEW: presentation/compact-line.ts" (lines 51-134; pattern assignments + soft-dep probe shape)
    - docs/messaging-style-guide.md §2 (MSG-GR-1 token order); §3 status tokens; §4 reasons; §6 icon discipline; §12 soft-dep
    - docs/output-catalog.md "Conventions" section (lines 5-129; grammar templates) + per-command sections that show every rendered shape (the renderer must emit byte-equal output to every example)
    - extensions/pi-claude-marketplace/presentation/plugin-list.ts:1-95 (existing private-icon + literal-union + iconFor precedent; lines 22-24 `ICON_*` constants move to compact-line.ts)
    - extensions/pi-claude-marketplace/shared/grammar/status-tokens.ts (Task 1 output -- now 15 entries including `reinstalled`)
    - extensions/pi-claude-marketplace/shared/grammar/reasons.ts (23-entry Reason literal union)
    - extensions/pi-claude-marketplace/shared/errors.ts:12 (assertNever exhaustiveness check)
    - extensions/pi-claude-marketplace/platform/pi-api.ts:38-81 (SoftDepStatus shape + softDepStatus + hasLoadedPiSubagents/McpAdapter)
    - tests/presentation/plugin-list.test.ts (existing unit-test pattern: `test(name, () => { const out = render(...); assert.equal/match(...) })`)
  </read_first>
  <behavior>
    The RowSpec union has 9 variants per RESEARCH.md, each discriminated by an explicit `kind` literal field. The renderer's main switch exhausts the union via `assertNever`. Each variant defines its own narrowed `status` type via `Extract<StatusToken, ...>` so misuse fails at TS compile.

    Required variants (each gets a unit test):
    - `kind: "plugin-inline"`: name + marketplace (always) + scope + version? + status (installed/updated/failed/rollback failed/unavailable; NO uninstalled) + reasons? + declaresAgents? + declaresMcp?. Emits `<icon> <name>@<mp> [<scope>] [v<ver>] (<status>) [{<reasons>}]`. Soft-dep marker injected per MSG-SD-1..3.
    - `kind: "plugin-inline-uninstalled"`: name + marketplace + scope + version? + status === "uninstalled" (literal). NO declaresAgents/Mcp fields (MSG-SD-3 structural enforcement). Emits `○ <name>@<mp> [<scope>] [v<ver>] (uninstalled)`.
    - `kind: "plugin-cascade"`: name + scope + version? + status (installed/updated/uninstalled/skipped/failed/available/unavailable/upgradable/reinstalled/rollback failed) + reasons? + declaresAgents? + declaresMcp?. NO marketplace field (MSG-GR-2 carve-out -- marketplace lives in cascade header). Emits `<icon> <name> [<scope>] [v<ver>] (<status>) [{<reasons>}]`.
    - `kind: "plugin-list"`: name + scope + version? + status (installed/upgradable/available/unavailable) + reasons? + description? + declaresAgents? + declaresMcp?. `(upgradable)` STRUCTURALLY constrained here ONLY (MSG-PL-4 / CMC-09). MSG-PL-6 scope-bracket carve-out: when status is `"available"` or `"unavailable"`, the rendered line OMITS the `[<scope>]` token.
    - `kind: "marketplace"`: name + scope + marker? (`"autoupdate"` | `"no autoupdate"`) + status? (added/removed/updated/skipped/failed) + reasons? + outcomeClass (`"ok"` | `"failure"`). Icon dispatched by outcomeClass per MSG-IC-3 (CMC-07). Emits `<icon> <name> [<scope>] [<marker>] [(<status>)] [{<reasons>}]`.
    - `kind: "empty"`: token (`"no marketplaces"` | `"no plugins"`). Emits the bare token only -- no icon, no name, no brackets (MSG-ER-1 / CMC-10): `(no marketplaces)` or `(no plugins)`.
    - `kind: "manual-recovery"`: resource (plain string -- system-level name, NO `@<mp>` or `[<scope>]`) + reasons (required, non-empty) + orphanDetails? (free-form indented children per §18.2). Emits `⊘ <resource> (manual recovery) {<reasons>}`. Soft-dep markers never apply here.
    - `kind: "rollback-child"`: phaseLabel + status (`"failed"` | `"rollback failed"`) + reasons (required). Emits at the bare compact-line level; the indentation is applied by the parent composer (`rollback-partial.ts`, separate plan).
    - `kind: "entity-error"`: name + marketplace? + scope? + status (`"failed"` | `"unavailable"`) + reasons (required, non-empty). Emits `⊘ <name>[@<mp>] [<scope>] (<status>) {<reasons>}` per MSG-NC-1 / CMC-34.

    The renderer accepts `SoftDepProbe = { readonly piSubagentsLoaded: boolean; readonly piMcpAdapterLoaded: boolean }` as an injected dependency. The soft-dep injection logic: for variants that carry `declaresAgents/Mcp` fields, append `requires pi-subagents` to the reasons block iff `declaresAgents === true && probe.piSubagentsLoaded === false`; same for `requires pi-mcp` (CMC-12 / CMC-13 / MSG-SD-1..3). The two markers may co-occur in a single comma-joined `{}` block. NEVER on `plugin-inline-uninstalled` (structural).

    Test coverage (`tests/presentation/compact-line.test.ts`):
    - Token order MSG-GR-1: every variant tested with maximal-slot input asserts byte-equal output matching the spec's `<icon> <name>[@<mp>] [<scope>] [<marker>] [<ver>] (<status>) {<reasons>}` order.
    - `@<marketplace>` carve-out (CMC-02): `plugin-cascade` row never emits `@<mp>`; `plugin-inline` always does.
    - Icon discipline (CMC-06 / MSG-IC-1..3): `(installed)`/`(updated)`/`(upgradable)` → `●`; `(available)`/`(uninstalled)` → `○`; `(failed)`/`(rollback failed)`/`(manual recovery)`/`(unavailable)` → `⊘`; `(skipped) {up-to-date}` → `●` but `(skipped) {source mismatch}` → `⊘`.
    - Marketplace icon (CMC-07): `outcomeClass: "ok"` → `●`; `outcomeClass: "failure"` → `⊘`.
    - Reasons block (CMC-04): empty `reasons` array OMITS `{}` entirely; single-reason emits `{reason}`; multi-reason emits `{reason1, reason2}` comma-joined.
    - Empty token (CMC-10): `empty` variant emits `(no plugins)` / `(no marketplaces)` literally, no leading icon.
    - Soft-dep emission (CMC-13 / MSG-SD-1..3): declares=true + companion unloaded → marker emitted; declares=false → marker omitted; declares=true + companion loaded → marker omitted.
    - MSG-PL-4 / CMC-09 negative: TS compile fails if a `plugin-inline` or `plugin-cascade` row tries to set `status: "upgradable"`. (Add an `// @ts-expect-error` test in the test file to lock this structurally.)
    - MSG-SD-3 negative: TS compile fails if a `plugin-inline-uninstalled` row tries to set `declaresAgents`. (Another `// @ts-expect-error`.)
    - MSG-PL-6: `plugin-list` with `status: "available"` or `"unavailable"` omits `[<scope>]`; with `status: "installed"` or `"upgradable"` keeps it.
    - `assertNever` exhaustiveness: the renderer's main switch ends with `default: return assertNever(row)`.
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/presentation/compact-line.ts` per the RESEARCH.md Pattern 1 proposal (9-variant explicit-`kind` discriminated union). Place the 3 icon constants `ICON_INSTALLED = "●"`, `ICON_AVAILABLE = "○"`, `ICON_UNINSTALLABLE = "⊘"` as file-private `const` at the top (migrated from `plugin-list.ts:22-24`; these are now the SINGLE source of MSG-IC-1..3 across surfaces per PATTERNS.md). DO NOT migrate `MAX_LINE_COLUMN` / `truncateColumn66` here -- those stay private to plugin-list.ts because MSG-PL-1 (column-66 truncation) is list-only; sub-wave 2d's renderer will call out to plugin-list.ts's internal helper when rendering `PluginListRow.description`.

    Use explicit `kind` literal discriminants per RESEARCH.md A1 (departing from the inferred-union codebase precedent for grep-ability + exhaustive-switch ergonomics in Phase 14's drift guard).

    Each variant's `status` type uses `Extract<StatusToken, ...>` to narrow the closed set per variant -- this is the structural enforcement for CMC-09 (MSG-PL-4), MSG-SD-3, and MSG-PL-6. Per-variant narrowings:
    - `PluginInlineRow.status`: `Extract<StatusToken, "installed" | "updated" | "failed" | "rollback failed" | "unavailable">`
    - `PluginInlineUninstalledRow.status`: literal `"uninstalled"`; NO declaresAgents/Mcp fields
    - `PluginCascadeRow.status`: `Extract<StatusToken, "installed" | "updated" | "uninstalled" | "skipped" | "failed" | "available" | "unavailable" | "upgradable" | "reinstalled" | "rollback failed">`
    - `PluginListRow.status`: `Extract<StatusToken, "installed" | "upgradable" | "available" | "unavailable">`
    - `MarketplaceRow.status`: optional `Extract<StatusToken, "added" | "removed" | "updated" | "skipped" | "failed">`
    - `EmptyToken.token`: `Extract<StatusToken, "no marketplaces" | "no plugins">`
    - `EntityErrorRow.status`: `Extract<StatusToken, "failed" | "unavailable">`
    - `RollbackChild.status`: `Extract<StatusToken, "failed" | "rollback failed">`

    Export the `RowSpec` union, every variant interface (as `export interface ...`), the `Scope = "user" | "project"` literal, the `SoftDepProbe` shape (matches `platform/pi-api.ts::SoftDepStatus` structurally; re-declare as a local interface to keep D-11 layering clean -- presentation may import platform/ but the shape is small enough to inline), and the `renderRow(row: RowSpec, probe: SoftDepProbe): string` function.

    Implement `renderRow` as a single `switch (row.kind)` with a case per variant and `default: return assertNever(row);`. Internal helpers (file-private, NOT exported): `renderPluginInline`, `renderPluginInlineUninstalled`, `renderPluginCascade`, `renderPluginList`, `renderMarketplace`, `renderEmpty`, `renderManualRecovery` (the variant; the parent-composer file is a separate plan), `renderRollbackChild`, `renderEntityError`, `iconForPluginRow(status, isTrivialSkip)`, `iconForMarketplace(outcomeClass)`, `renderReasons(reasons, declaresAgents, declaresMcp, probe)`, `renderVersion(version)`, `renderScope(scope, omit)`.

    `renderReasons` is the per-row soft-dep injection point: it starts from the caller-provided `reasons` array, appends `"requires pi-subagents"` iff `declaresAgents && !probe.piSubagentsLoaded`, appends `"requires pi-mcp"` iff `declaresMcp && !probe.piMcpAdapterLoaded`, joins the resulting array with `", "`, wraps in `{}`, and returns `""` when the resulting array is empty (MSG-GR-4 forbids emitting empty `{}`). The two soft-dep reasons are Reason literals from the closed set -- verify they exist in `shared/grammar/reasons.ts`; if absent, surface as a planning-context discrepancy.

    The MSG-PL-6 scope-bracket carve-out lives inside `renderPluginList`: when `row.status === "available" || row.status === "unavailable"`, omit the `[<scope>]` slot from the output.

    The icon dispatcher `iconForPluginRow(status, isTrivialSkip)` maps per MSG-IC-1..3:
    - `installed` / `updated` / `upgradable` / `reinstalled` → `ICON_INSTALLED`
    - `available` / `uninstalled` → `ICON_AVAILABLE`
    - `failed` / `rollback failed` / `manual recovery` / `unavailable` → `ICON_UNINSTALLABLE`
    - `skipped`: branch on `isTrivialSkip` (caller passes `reasons.length === 1 && reasons[0] === "up-to-date"`); trivial → `ICON_INSTALLED` (plugin remains installed); non-trivial → `ICON_UNINSTALLABLE` (e.g., `{source mismatch}` means plugin is NOT installed). Catalog confirms: `(skipped) {up-to-date}` renders with `●`; `(skipped) {source mismatch}` renders with `⊘`.

    `iconForMarketplace(outcomeClass)` maps `"ok"` → `ICON_INSTALLED`; `"failure"` → `ICON_UNINSTALLABLE`.

    Imports: `assertNever` from `../shared/errors.ts`; `StatusToken` from `../shared/grammar/status-tokens.ts`; `Reason` from `../shared/grammar/reasons.ts`. NO imports from `persistence/` (D-11 layering).

    Create `tests/presentation/compact-line.test.ts` following the `tests/presentation/plugin-list.test.ts` pattern. One `test(...)` block per behavior bullet listed in `<behavior>` above. Use `assert.equal` for byte-equality assertions; use `// @ts-expect-error` lines inside test functions to lock the MSG-PL-4 / MSG-SD-3 structural negatives. Each test imports `renderRow` + the relevant variant interface from `../../extensions/pi-claude-marketplace/presentation/compact-line.ts`.
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/presentation/compact-line.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - `node --test tests/presentation/compact-line.test.ts` exits 0
    - `grep -c 'export type RowSpec' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -c 'export function renderRow' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -c 'assertNever(row)' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -cE 'kind: "(plugin-inline|plugin-inline-uninstalled|plugin-cascade|plugin-list|marketplace|empty|manual-recovery|rollback-child|entity-error)"' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 9 (one per RowSpec variant)
    - `grep -c 'ICON_INSTALLED = "●"' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -c 'ICON_AVAILABLE = "○"' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -c 'ICON_UNINSTALLABLE = "⊘"' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -c 'Extract<StatusToken' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 5 (per-variant narrowing)
    - `grep -c 'SoftDepProbe' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
    - `grep -c '@ts-expect-error' tests/presentation/compact-line.test.ts` returns at least 2 (MSG-PL-4 + MSG-SD-3 structural negatives)
    - `grep -cE '^(import .* from )?"\.\./.*persistence' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns 0 (no persistence imports per D-11)
  </acceptance_criteria>
  <done>
    `tsc --noEmit` exits 0; `node --test tests/presentation/compact-line.test.ts` exits 0; `grep -c 'export type RowSpec' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1; `grep -c 'assertNever(row)' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1; `grep -cE 'kind: "(plugin-inline|plugin-inline-uninstalled|plugin-cascade|plugin-list|marketplace|empty|manual-recovery|rollback-child|entity-error)"' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns 9 or more.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update presentation/index.ts barrel with new exports</name>
  <files>extensions/pi-claude-marketplace/presentation/index.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/presentation/index.ts (current 3 export blocks at lines 1-15)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md §"MODIFIED: presentation/index.ts" (the exact additions per D-13-15)
  </read_first>
  <behavior>
    - `compact-line.ts` public surface re-exported: `renderRow` + every RowSpec variant interface as a type export
    - `sort.ts::compareByNameThenScope` re-exported
    - `cascade-summary.ts`, `manual-recovery.ts`, `rollback-partial.ts`, `cause-chain.ts` re-exports are ADDED as forward placeholders (those files don't exist yet; this plan adds them as commented placeholders OR we defer their barrel addition to Plan 13-01-02; pick the deferred approach to avoid breaking import resolution in this plan)
    - The deprecated `subagentWarningIfNeeded` + `mcpAdapterWarningIfNeeded` re-exports are LEFT IN PLACE during Wave 1 (Plan 13-01-03 evaluates whether to delete or thin `soft-dep.ts`)
    - `tsc --noEmit` exits 0 after the edit (no broken imports anywhere in the codebase)
  </behavior>
  <action>
    Edit `extensions/pi-claude-marketplace/presentation/index.ts` to add two new export blocks (the cause-chain / cascade-summary / manual-recovery / rollback-partial exports are added by Plan 13-01-02 when those files land):

    1. Add `export { renderRow } from "./compact-line.ts";` plus a `export type { RowSpec, PluginInlineRow, PluginInlineUninstalledRow, PluginCascadeRow, PluginListRow, MarketplaceRow, EmptyToken, ManualRecoveryLine, RollbackChild, EntityErrorRow, Scope, SoftDepProbe } from "./compact-line.ts";` block.

    2. Add `export { compareByNameThenScope } from "./sort.ts";`.

    Leave the existing 3 blocks unchanged (`appendReloadHint` / `reloadHint` from `reload-hint.ts`; the 4 soft-dep helper re-exports from `soft-dep.ts`; `renderMarketplaceList` from `marketplace-list.ts`). Plan 13-01-02 adds the cause-chain / cascade-summary / manual-recovery / rollback-partial exports; Plan 13-01-03 evaluates whether to remove `subagentWarningIfNeeded` / `mcpAdapterWarningIfNeeded` re-exports.

    Verify the typecheck passes after this edit (the new exports must be importable from anywhere the barrel is consumed; sub-waves 2a-2d will import from here).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` exits 0
    - `grep -c 'renderRow' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1
    - `grep -c 'compareByNameThenScope' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1
    - `grep -cE 'export type \{[^}]*RowSpec' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1 (RowSpec exported as type)
    - `grep -c 'from "./compact-line.ts"' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1
    - `grep -c 'from "./sort.ts"' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1
    - `grep -c 'cascadeSummary\|renderManualRecovery\|renderRollbackPartial\|causeChainTrailer' extensions/pi-claude-marketplace/presentation/index.ts` returns 0 (those exports land in Plan 13-01-02 -- this task only adds compact-line + sort)
  </acceptance_criteria>
  <done>
    `tsc --noEmit` exits 0; `grep -c 'renderRow' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1; `grep -c 'compareByNameThenScope' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer ↔ orchestrator (`renderRow(row, probe)`) | Orchestrators construct typed `RowSpec` values from validated state; the renderer is a pure transform with no I/O |
| Style guide YAML frontmatter ↔ code closed set | The frontmatter is the binding contract; `tests/architecture/grammar-frontmatter.test.ts` enforces set-equality every run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `STATUS_TOKENS` array | mitigate | `tests/architecture/grammar-frontmatter.test.ts` byte-equality assertion fails CI on any code/frontmatter drift |
| T-13-02 | Information Disclosure | `renderRow` token emission | accept | All inputs are validated state; the renderer surfaces only `name`/`version`/`scope`/`status`/`reasons` -- no stack traces, no absolute paths (NFR-9 invariant); soft-dep injection probes companion-loaded state only |
| T-13-03 | Denial of Service | `renderRow` switch dispatch | accept | Pure switch over closed union; O(1); no recursion; `assertNever` runtime safety net |

Phase 13 has no application-security domain (no new I/O, no new auth, no new persistence shapes) per RESEARCH.md §"Security Domain". The cause-chain cycle DoS mitigation lives in Plan 13-01-02 where `causeChainTrailer` is implemented.
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `node --test tests/architecture/grammar-frontmatter.test.ts` exits 0 (STATUS_TOKENS extension didn't break Phase 12 drift test)
- `node --test tests/presentation/compact-line.test.ts` exits 0 (every behavior bullet has at least one assertion)
- `npm run check` exits 0 (typecheck + ESLint + Prettier + full suite; no other test regresses)
- `grep -c 'export type RowSpec' extensions/pi-claude-marketplace/presentation/compact-line.ts` returns at least 1
- `grep -c 'compareByNameThenScope' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 1
- `grep -c '^  - reinstalled$' docs/messaging-style-guide.md` returns at least 1
</verification>

<success_criteria>
1. `STATUS_TOKENS` has 15 entries with `"reinstalled"` after `"updated"`; the frontmatter + §3 table mirror the change byte-equal-positioned per D-13-20.
2. `presentation/compact-line.ts` exports the 9-variant `RowSpec` discriminated union with explicit `kind` discriminants and a `renderRow(row, probe)` function whose `switch (row.kind)` ends with `default: return assertNever(row);`.
3. CMC-09 / MSG-PL-4 is structurally enforced: `// @ts-expect-error` lines in `tests/presentation/compact-line.test.ts` lock the `(upgradable)` constraint to `PluginListRow` only.
4. CMC-13 / MSG-SD-3 is structurally enforced: `// @ts-expect-error` lines lock the inability to set `declaresAgents/Mcp` on `PluginInlineUninstalledRow`.
5. `presentation/sort.ts::compareByNameThenScope` is the single per-scope sort comparator per MSG-GR-3.
6. `presentation/index.ts` re-exports `renderRow` + every RowSpec variant interface + `compareByNameThenScope`; downstream Wave 2 sub-waves consume only the barrel.
7. The unit-test suite in `tests/presentation/compact-line.test.ts` covers token order (CMC-01), `@<mp>` carve-out (CMC-02), reasons block (CMC-04), icon discipline (CMC-06), marketplace icon (CMC-07), empty token (CMC-10), per-row soft-dep emission (CMC-13), MSG-PL-6 scope-bracket carve-out, and exhaustive-switch via `assertNever`.
8. `npm run check` stays green.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-01-01-SUMMARY.md` when done, citing every CMC-* requirement structurally addressed by this plan, the new file paths, and the test commands that verify it.
</output>
