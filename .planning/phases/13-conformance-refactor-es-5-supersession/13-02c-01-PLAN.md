---
phase: 13-conformance-refactor-es-5-supersession
plan: 02c-01
type: execute
wave: 2
depends_on:
  - 13-01-01
  - 13-01-02
  - 13-01-03
  - 13-02a-01
  - 13-02b-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - extensions/pi-claude-marketplace/presentation/marketplace-list.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
  - extensions/pi-claude-marketplace/platform/pi-api.ts
  - tests/presentation/marketplace-list.test.ts
autonomous: true
requirements:
  - CMC-03
  - CMC-05
  - CMC-07
  - CMC-10
  - CMC-15
  - CMC-16
  - CMC-29
  - CMC-30
  - CMC-31
  - CMC-32
  - CMC-33
  - CMC-34

must_haves:
  truths:
    - "`/claude:plugin marketplace list` renders flat `MarketplaceRow[]` sorted by `compareByNameThenScope` -- the legacy `user scope marketplaces:` / `project scope marketplaces:` group headers are gone (CMC-29 / MSG-GR-3)."
    - "`/claude:plugin marketplace add` emits `● <name> [<scope>] [<autoupdate>] (added)` -- github-source carries marker; path-source omits it (CMC-30)."
    - "`/claude:plugin marketplace remove` is conditional: clean = bare `MarketplaceRow{status:'removed'}` + reload-hint; partial = header `(failed) {plugins remain}` + indented `PluginCascadeRow[]` + reload-hint + retry anchor coexisting (CMC-31 / CMC-15)."
    - "`/claude:plugin marketplace update` emits cascade when autoupdate ON; standalone marketplace row when autoupdate OFF; legacy `Updated marketplace \"X\"...` sentence form retired (CMC-32)."
    - "`/claude:plugin marketplace autoupdate enable|disable` emits flat marketplace rows with marker as outcome (no status token); `<no autoupdate>` appears ONLY on disable result rows (CMC-33 / MSG-GR-5)."
    - "Manual recovery on `mp remove` system-level failure emits separate `⊘ <resource> (manual recovery) {<reason>}` line via `renderManualRecovery` (CMC-16)."
    - "Edge handlers' entity-shape errors emit `EntityErrorRow` compact lines (CMC-34); usage errors stay sentence-form."
    - "Empty `marketplace list` renders `(no marketplaces)` via `EmptyToken` (CMC-10)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts"
      provides: "Single notify call with renderMarketplaceList payload (rewritten in presentation/marketplace-list.ts)"
    - path: "extensions/pi-claude-marketplace/presentation/marketplace-list.ts"
      provides: "Rewritten flat-list renderer consuming MarketplaceRow + EmptyToken; uses compareByNameThenScope; no scope-group headers"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      provides: "Conditional CMC-31 form (bare clean vs header+children partial) + CMC-15 dual-trailer (reload+retry) + CMC-16 manual-recovery line"
    - path: "extensions/pi-claude-marketplace/platform/pi-api.ts"
      provides: "Deletion of subagentWarningIfNeeded + mcpAdapterWarningIfNeeded (RESEARCH.md Open Question 3; all callers migrated by sub-waves 2a + 2b + 2c)"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      to: "extensions/pi-claude-marketplace/presentation/cascade-summary.ts"
      via: "import { cascadeSummary }"
      pattern: "cascadeSummary\\(\\{"
    - from: "extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts"
      to: "extensions/pi-claude-marketplace/presentation/manual-recovery.ts"
      via: "import { renderManualRecovery }"
      pattern: "renderManualRecovery\\("
---

<objective>
Sub-wave 2c: migrate the five marketplace orchestrators (`list`, `add`, `remove`, `update`, `autoupdate`) and the marketplace edge handlers to consume the Wave 1 primitives. This sub-wave exercises the broadest set of cross-cutting MSG-* rules: per-scope rendering (MSG-GR-3 / CMC-03), `<marker>` slot (MSG-GR-5 / CMC-05), marketplace outcome-class icon (CMC-07), empty bare-token (CMC-10), reload-hint + retry anchor coexistence (CMC-15), manual recovery (CMC-16), severity routing (CMC-20). Also finalizes the deletion of `subagentWarningIfNeeded` / `mcpAdapterWarningIfNeeded` from `platform/pi-api.ts` per RESEARCH.md Open Question 3.

Internal ordering (per CONTEXT.md Claude's Discretion): land `mp remove` and `mp update` LAST inside this sub-wave -- they exercise the most cross-cutting rules (mp remove: CMC-31 conditional form + CMC-15 dual-trailer + CMC-16 manual recovery; mp update: CMC-32 autoupdate-on vs autoupdate-off + CMC-20 severity). Land `mp list`, `mp add`, `mp autoupdate` first because they touch simpler renderer paths.

Output: 5 modified orchestrator files + 1 rewritten presentation renderer + 3 modified edge handlers + 1 `platform/pi-api.ts` deletion + 1 rewritten test. Catalog UAT in Wave 3 plan #1 is the binding verification.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
@extensions/pi-claude-marketplace/presentation/compact-line.ts
@extensions/pi-claude-marketplace/presentation/cascade-summary.ts
@extensions/pi-claude-marketplace/presentation/manual-recovery.ts
@extensions/pi-claude-marketplace/presentation/sort.ts
@extensions/pi-claude-marketplace/presentation/reload-hint.ts
@extensions/pi-claude-marketplace/presentation/marketplace-list.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts
@extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
@extensions/pi-claude-marketplace/platform/pi-api.ts

<interfaces>
From `extensions/pi-claude-marketplace/presentation/compact-line.ts` (Wave 1 output -- consumed verbatim):
- `MarketplaceRow` interface: required `name`, `scope`, `outcomeClass: "ok" | "failure"`; optional `marker: "autoupdate" | "no autoupdate"`, optional `status: Extract<StatusToken, "added" | "removed" | "updated" | "skipped" | "failed">`, optional `reasons`.
- `EmptyToken` for `(no marketplaces)` empty case.
- `ManualRecoveryLine` for system-level manual-recovery emission.
- `EntityErrorRow` for edge-handler entity errors.

From `extensions/pi-claude-marketplace/presentation/cascade-summary.ts` (Wave 1 output): `cascadeSummary({marketplace, rows, probe}) → {message, severity}`.

From `extensions/pi-claude-marketplace/presentation/sort.ts` (Wave 1 output): `compareByNameThenScope({name, scope}, {name, scope})`.

From `extensions/pi-claude-marketplace/presentation/manual-recovery.ts` (Wave 1 output): `renderManualRecovery(line, probe)`.

From `extensions/pi-claude-marketplace/presentation/marketplace-list.ts:33-82` (the current renderer to rewrite):
- Today: separates rows into user-then-project buckets with `${scope} scope marketplaces:` group headers; iterates and emits `${ICON} ${m.name} (${logical})${auto}` per row.
- New: flat list sorted by `compareByNameThenScope`; emits `MarketplaceRow` rows via `renderRow`; empty case is `EmptyToken{token: "no marketplaces"}`.

Existing callsites per RESEARCH.md §2.1 (must migrate or delete):
- `marketplace/list.ts:62` -- single `notifySuccess(ctx, renderMarketplaceList(allRecords))`
- `marketplace/add.ts:135/142` -- post-commit warning + success sentence-form
- `marketplace/remove.ts:195/245/251/279` -- cache leak + aggregated `formatErrorWithCauses` + failure warning + clean success
- `marketplace/update.ts:175/317/319/332/359` -- empty case + single-shot failure + cache leak + cascade summary
- `marketplace/autoupdate.ts:92/113/138/142` -- target-not-found error + single-shot failure + empty case + multi-mp result

Edge handler callsites: `edge/handlers/marketplace/{add,remove,update}.ts` -- contain `notifyError(ctx, USAGE)` and `notifyError(ctx, errorMessage(err))` for entity-shape errors; entity-shape errors migrate to `EntityErrorRow`; usage errors stay sentence-form.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite presentation/marketplace-list.ts + migrate orchestrators/marketplace/list.ts (CMC-03 / CMC-07 / CMC-10 / CMC-29)</name>
  <files>extensions/pi-claude-marketplace/presentation/marketplace-list.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts, tests/presentation/marketplace-list.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/presentation/marketplace-list.ts (full file ~82 lines; the renderer to rewrite)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts (full file; callsite at line 62)
    - docs/output-catalog.md §"`/claude:plugin marketplace list`" (lines 563-588; empty case + mixed-scopes flat per-scope sort with project-before-user tie-break)
    - docs/messaging-style-guide.md §7 MSG-GR-3 (flat lists; no group headers)
    - tests/presentation/marketplace-list.test.ts (existing tests; will need updates)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts + sort.ts (Wave 1 output)
  </read_first>
  <behavior>
    - Empty case (CMC-10): emits `(no marketplaces)` via `EmptyToken` (D-13-05/D-13-06, `kind: "empty"`). Catalog form: literally `(no marketplaces)` on a single line.
    - Non-empty (CMC-29 / MSG-GR-3 per D-13-15): flat list of `MarketplaceRow` (D-13-05) lines sorted by `compareByNameThenScope` (name primary case-insensitive, project-before-user tie-break). NO `user scope marketplaces:` / `project scope marketplaces:` group headers.
    - Each marketplace row: `<icon> <name> [<scope>] [<marker>] (<source.logical>)` -- but the catalog at lines 569-588 shows source-logical is included as a parenthesised label. Verify against the catalog: the marketplace-list row format in `mp list` is `<icon> <name> [<scope>] [<marker>]` and the source kind/logical name is NOT included as a parenthesised suffix in the new style guide (the source kind drives the `marker` choice: github → autoupdate marker present; path → no marker). Re-read the catalog at lines 563-588 carefully to determine the exact rendered form -- if the catalog shows the source label, then the renderer needs to emit it as a free-form trailing token; if not, the renderer omits it.

    PER CATALOG INSPECTION REQUIRED BY EXECUTOR: read the exact rendered form in `docs/output-catalog.md` §"`/claude:plugin marketplace list`" and reproduce byte-equal. The `MarketplaceRow.outcomeClass` for non-failure list rows is `"ok"`; `marker` is `"autoupdate"` if the marketplace has autoupdate ON, undefined otherwise; `status` is undefined (list rows are labels, not outcomes); `reasons` is undefined.

    - Sort order (MSG-GR-3 / D-13-15): `compareByNameThenScope` applied to ALL rows together (not per-scope-bucket).
  </behavior>
  <action>
    Rewrite `extensions/pi-claude-marketplace/presentation/marketplace-list.ts`:

    1. Drop the existing `byScope` bucketing (lines 51-79) and the per-scope group headers (`${scope} scope marketplaces:`).
    2. New signature: `renderMarketplaceList(records: MarketplaceListInput): string` where `MarketplaceListInput` is the per-scope flat list the orchestrator constructs (e.g., `readonly { name, scope, autoupdate, source }[]`).
    3. Empty case: return `renderRow({kind: "empty", token: "no marketplaces"}, dummyProbe)` (the bare token form).
    4. Non-empty: convert each input record to a `MarketplaceRow` per:
       ```
       {
         kind: "marketplace",
         name: record.name, scope: record.scope,
         marker: record.autoupdate ? "autoupdate" : undefined,
         outcomeClass: "ok",
         // status and reasons are undefined for list-rendering label rows
       }
       ```
    5. Sort rows by `compareByNameThenScope` -- pass the array through `[...rows].sort(compareByNameThenScope)`.
    6. Render each row via `renderRow(row, dummyProbe)`; join with `\n`. Return the joined string.
    7. Verify the rendered form is byte-equal to the catalog example. If the catalog DOES include the `(<source.logical>)` parenthesised label, the renderer needs an additional `sourceLogical` field on the row OR a list-specific helper to compose the line (the catalog is the binding contract -- match it exactly).

    Migrate `extensions/pi-claude-marketplace/orchestrators/marketplace/list.ts:62`: no change to the call shape (`notifySuccess(ctx, renderMarketplaceList(allRecords))`) -- the renderer signature update is internal. Verify the orchestrator passes a flat array (or convert to flat array if the existing call passes a per-scope structure).

    Rewrite `tests/presentation/marketplace-list.test.ts`:
    - Empty payload → assert `(no marketplaces)` byte-equal.
    - Mixed-scopes payload (e.g., `[{name:"alpha", scope:"user"}, {name:"alpha", scope:"project"}, {name:"beta", scope:"user"}]`) → assert sort order is `alpha [project]`, `alpha [user]`, `beta [user]` (project-before-user tie-break per MSG-GR-3).
    - Marker presence: `autoupdate: true` → emit `<autoupdate>`; `autoupdate: false` → omit.
    - No `${scope} scope marketplaces:` group headers in output (assert absence via `grep`).
  </action>
  <verify>
    <automated>npm run check && node --test tests/presentation/marketplace-list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `node --test tests/presentation/marketplace-list.test.ts` exits 0
    - `grep -c 'scope marketplaces:' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns 0 (group headers retired per CMC-29 / D-13-15)
    - `grep -c 'compareByNameThenScope' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns at least 1 (MSG-GR-3 sort per D-13-15)
    - `grep -c 'kind: "marketplace"\|kind: "empty"' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns at least 1 (D-13-05/D-13-06 row discriminants consumed)
    - `grep -c 'renderRow' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns at least 1
    - `grep -c '"no marketplaces"' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns at least 1 (empty case via EmptyToken per CMC-10)
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; `node --test tests/presentation/marketplace-list.test.ts` exits 0; `grep -c 'scope marketplaces:' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns 0 (group headers gone); `grep -c 'compareByNameThenScope' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns at least 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate orchestrators/marketplace/add.ts (CMC-30) + orchestrators/marketplace/autoupdate.ts (CMC-33)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts (full file; callsites at lines 135/142 per RESEARCH.md)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts (full file; callsites at lines 92/113/138/142 per RESEARCH.md)
    - docs/output-catalog.md §"`/claude:plugin marketplace add <source>`" (lines 589-621; path source -- no marker; github source -- `<autoupdate>` marker; failure)
    - docs/output-catalog.md §"`/claude:plugin marketplace autoupdate <enable|disable> <name>`" (lines 691-721; mixed flips + already-on/off; marker-as-outcome, no status token; `<no autoupdate>` only on disable)
    - docs/messaging-style-guide.md §2 MSG-GR-5 (`<marker>` between scope and status; `<no autoupdate>` only on autoupdate disable outcome)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (MarketplaceRow)
  </read_first>
  <behavior>
    ADD:
    - Success path-source: `● <name> [<scope>] (added)` -- no marker (path-source defaults autoupdate=OFF; marker omitted when OFF per MSG-GR-5).
    - Success github-source: `● <name> [<scope>] <autoupdate> (added)` -- marker present (github-source defaults autoupdate=ON).
    - Failure: `notifyError(ctx, errorMessage(err), err)` -- cause-chain auto-appends. Entity-shape failures (e.g., duplicate name) → edge handler migration (Task 4).
    - Post-commit cache leak: `notifyWarning(ctx, ...)` sentence-form preserved.

    AUTOUPDATE:
    - Enable success: `● <name> [<scope>] <autoupdate>` -- NO status token (marker is the outcome per CMC-33).
    - Enable already-on: `● <name> [<scope>] <autoupdate> {already enabled}` -- marker + Reason.
    - Disable success: `● <name> [<scope>] <no autoupdate>` -- marker is `"no autoupdate"`.
    - Disable already-off: `● <name> [<scope>] <no autoupdate> {already disabled}`.
    - Target-not-found / mp-doesn't-exist: edge-handler entity error → `EntityErrorRow` (Task 4).
    - Empty case (CMC-10) -- the legacy `No marketplaces configured.` sentence is replaced by `(no marketplaces)` via `EmptyToken` (D-13-05/D-13-06) + `notifySuccess`.
    - Multi-mp result: emit each marketplace as its own row; concatenate with `\n`; route via `notifySuccess` (autoupdate flips are always successes; failures live at the edge layer).
  </behavior>
  <action>
    ADD migration (`extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts`):

    1. Replace `notifySuccess(opts.ctx, \`Added marketplace "${recordedName}" in ${opts.scope} scope.\`)` (line 142) with:
       ```
       const probe = softDepStatus(opts.pi);
       const row: MarketplaceRow = {
         kind: "marketplace",
         name: recordedName, scope: opts.scope,
         status: "added",
         outcomeClass: "ok",
         marker: opts.source.kind === "github" ? "autoupdate" : undefined,
       };
       notifySuccess(opts.ctx, renderRow(row, probe));
       ```

    2. Post-commit cache leak warning at line 135: STAYS sentence-form (free-form trailer per RESEARCH.md guidance).

    3. The legacy `formatErrorWithCauses` removal happened in Plan 13-01-02; verify no remnant in this file.

    4. MA-11 binding: NO reload-hint trailer on marketplace add (per PRD §5.1.1 MA-11; the source adds resources only at install-time, not add-time). Do NOT emit `appendReloadHint`.

    AUTOUPDATE migration (`extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts`):

    1. Replace `notifySuccess(ctx, "No marketplaces configured.")` (line 138) with `notifySuccess(ctx, renderRow({kind: "empty", token: "no marketplaces"}, probe))`.

    2. Multi-mp result (line 142): rewrite the loop that today composes `lines.join("\n")` of sentence-form text. Build per-mp `MarketplaceRow`:
       ```
       const probe = softDepStatus(ctx.pi);
       const lines: string[] = [];
       for (const mp of results) {
         const row: MarketplaceRow = {
           kind: "marketplace",
           name: mp.name, scope: mp.scope,
           marker: enable ? "autoupdate" : "no autoupdate",
           outcomeClass: "ok",
           // status is undefined (marker is the outcome per CMC-33)
           reasons: mp.alreadyMatching
             ? [enable ? "already enabled" : "already disabled"]
             : undefined,
         };
         lines.push(renderRow(row, probe));
       }
       notifySuccess(ctx, lines.join("\n"));
       ```
       Verify `"already enabled"` and `"already disabled"` exist in `shared/grammar/reasons.ts:34-58`. If absent, surface as a planning gap.

    3. Single-shot failure at line 92/113 (`notifyError(ctx, errorMessage(err), err)`): the error path goes through `notifyError`; trailer auto-appends. For entity-shape failures (target not found), the edge handler should emit `EntityErrorRow` -- the orchestrator passes the bare error through.

    4. Delete imports of `formatErrorWithCauses` (gone per Plan 13-01-02).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `grep -c 'Added marketplace' extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns 0 (legacy sentence retired per CMC-30)
    - `grep -c 'No marketplaces configured\.' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0 (legacy sentence retired)
    - `grep -c 'MarketplaceRow\|renderRow' extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns at least 1 (D-13-05/D-13-06 row consumed)
    - `grep -c 'MarketplaceRow\|renderRow' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns at least 1
    - `grep -c 'no autoupdate' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns at least 1 (CMC-33 disable marker per MSG-GR-5)
    - `grep -c 'marker:' extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns at least 1 (autoupdate marker dispatched by source kind per CMC-30)
    - `grep -c 'outcomeClass:' extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns at least 1 (CMC-07 icon dispatch)
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; `grep -c 'Added marketplace' extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` returns 0 (legacy sentence retired); `grep -c 'No marketplaces configured\.' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns 0 (legacy sentence retired); `grep -c 'MarketplaceRow\|renderRow' extensions/pi-claude-marketplace/orchestrators/marketplace/{add,autoupdate}.ts` returns at least 2; `grep -c 'no autoupdate' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns at least 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate orchestrators/marketplace/update.ts (CMC-32) and remove.ts (CMC-31 / CMC-15 / CMC-16)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts (full file; callsites at lines 175/317/319/332/359)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts (full file; callsites at lines 195/245/251/279; pay attention to the partial-failure path with reload-hint + retry anchor coexistence)
    - docs/output-catalog.md §"`/claude:plugin marketplace update <name>`" (lines 655-690; autoupdate-off manifest-only refresh; mixed plugin outcomes; mp-level failure)
    - docs/output-catalog.md §"`/claude:plugin marketplace remove <name>`" (lines 622-654; clean bare-row; partial header + children + reload + retry coexistence)
    - docs/output-catalog.md §"Manual recovery anchors" (lines 722-742; the manual-recovery example)
    - docs/messaging-style-guide.md §10 MSG-SR-4..6 (cascade severity); §5 MSG-RH-1 (reload-hint); §9 MSG-MR-1..2 (manual-recovery line shape)
    - extensions/pi-claude-marketplace/presentation/cascade-summary.ts + manual-recovery.ts + reload-hint.ts
  </read_first>
  <behavior>
    MP UPDATE:
    - Empty case (CMC-10) -- no marketplaces configured in scope: `(no marketplaces)` via `EmptyToken` (D-13-05) + `notifySuccess`.
    - Autoupdate OFF (manifest-only refresh): single `MarketplaceRow{outcomeClass:"ok", status:"updated"}` row + reload-hint trailer. NO cascade children (autoupdate is off; no plugins refreshed).
    - Autoupdate ON: cascade -- marketplace header `(updated)` + indented `PluginCascadeRow[]` children for each plugin partition (`(updated)` / `(skipped) {up-to-date}` / `(skipped) {no longer installable}` / `(failed) {<reason>}`). Routes via `cascadeSummary` dispatch.
    - Single-shot mp-level failure (clone failed, manifest unparseable): `notifyError(ctx, errorMessage(err), err)` -- cause-chain auto-appends. The catalog shows the legacy "Marketplace update failed..." sentence form retiring; entity-shape failures emit `EntityErrorRow` at the edge layer instead.
    - The legacy `Updated marketplace "X" in <scope> scope.` summary line is GONE (CMC-32 binding).

    MP REMOVE:
    - Clean success (CMC-31 bare-row form): single `MarketplaceRow{outcomeClass:"ok", status:"removed"}` row + reload-hint trailer (when any plugin's resources were removed). NO marketplace-header + children structure (clean removes are a single line).
    - Partial failure (some plugins failed to unstage, the marketplace record is retained per MR-3): `MarketplaceRow{outcomeClass:"failure", status:"failed", reasons:["plugins remain"]}` HEADER + `PluginCascadeRow[]` indented children for each failed plugin. CMC-15 dual-trailer: reload-hint trailer (when any resource WAS removed) followed by blank line then `Fix the underlying issue and retry.` retry anchor. The retry anchor is a free-form trailer per §18.2.
    - MR-6 post-state cleanup failures: separate `notifyWarning(ctx, ...)` sentence-form (post-commit cleanup leaks).
    - Manual-recovery surfaces (CMC-16 / MSG-MR-1 per D-13-15) -- e.g., agent index unparseable, system-level: emit via `renderManualRecovery({resource:"agent index", reasons:["unparseable"]}, probe)` (D-13-05 `kind: "manual-recovery"` variant) as a separate top-level line preceded by blank line. Orchestrator composes `parent_op_body + "\n\n" + renderManualRecovery(...)` into ONE `notifyWarning` body per MSG-MR-1.
    - Cascade severity (CMC-20 / MSG-SR-4..6 / D-13-08): any failed plugin → `notifyWarning`; clean → `notifySuccess`. `notifyError` never used for mp remove cascade summaries (composer returns `{message, severity}`, orchestrator destructures + dispatches).
    - Aggregated soft-dep trailer GONE: per-row markers via `renderRow` on cascade children replace the legacy `subagentWarn` / `mcpWarn` lines at remove.ts:265-266.
  </behavior>
  <action>
    MP UPDATE migration (`extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts`):

    1. Replace `notifySuccess(ctx, "No marketplaces configured.")` at line 175 with `notifySuccess(ctx, renderRow({kind: "empty", token: "no marketplaces"}, probe))`.

    2. Replace the cascade emission at the bottom of the orchestrator (line 332/359) with the `cascadeSummary` pattern (mirror sub-wave 2a's approach):
       ```
       const probe = softDepStatus(ctx.pi);
       const mpRow: MarketplaceRow = {
         kind: "marketplace", name: mp.name, scope: mp.scope,
         marker: mp.autoupdate ? "autoupdate" : undefined,
         status: "updated",
         outcomeClass: "ok",
       };
       const rows: PluginCascadeRow[] = autoupdateOn ? cascadeChildren : [];
       if (rows.length === 0) {
         // autoupdate-off path: marketplace row alone + reload-hint
         const body = renderRow(mpRow, probe);
         const hint = reloadHint(["__manifest_only__"]);  // any non-empty array triggers the trailer
         notifySuccess(ctx, appendReloadHint(body, hint));
       } else {
         const {message, severity} = cascadeSummary({marketplace: mpRow, rows, probe});
         const changed = rows.filter(r => r.status === "updated").map(r => r.name);
         const hint = reloadHint(changed);
         const dispatch = severity === "warning" ? notifyWarning : notifySuccess;
         dispatch(ctx, appendReloadHint(message, hint));
       }
       ```

    3. Single-shot mp-level failure (line 317/319): `notifyError(ctx, errorMessage(err), err)` -- the trailer auto-appends. Move the `Updated marketplace "X"...` legacy sentence form into deletion.

    MP REMOVE migration (`extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts`):

    1. CLEAN-success path (around line 279, `notifySuccess(opts.ctx, appendReloadHint(body, hint))`): replace the manual `body` composition with:
       ```
       const probe = softDepStatus(opts.pi);
       const cleanRow: MarketplaceRow = {
         kind: "marketplace",
         name: opts.name, scope: opts.scope,
         status: "removed",
         outcomeClass: "ok",
       };
       const body = renderRow(cleanRow, probe);
       const hint = reloadHint(removedSorted);  // existing removedSorted; non-empty when any plugin's resources were removed
       notifySuccess(opts.ctx, appendReloadHint(body, hint));
       ```

    2. PARTIAL-failure path (around line 251, `notifyWarning(opts.ctx, failureWarning(opts.name, failedPlugins))`): replace with the CMC-31 header + children form + CMC-15 dual-trailer:
       ```
       const probe = softDepStatus(opts.pi);
       const headerRow: MarketplaceRow = {
         kind: "marketplace",
         name: opts.name, scope: opts.scope,
         status: "failed",
         reasons: ["plugins remain"],
         outcomeClass: "failure",
       };
       const childRows: PluginCascadeRow[] = failedPlugins.map(fp => ({
         kind: "plugin-cascade",
         name: fp.plugin, scope: fp.scope,
         status: "failed",
         reasons: [/* closed-set Reason from fp.cause */],
         declaresAgents: false, declaresMcp: false,
       }));
       const {message, severity} = cascadeSummary({marketplace: headerRow, rows: childRows, probe});
       // severity will be "warning" per cascadeSeverity rules
       const reloadTrailer = reloadHint(removedSorted);  // non-empty when any successful plugin resource was removed
       const retryAnchor = "Fix the underlying issue and retry.";  // free-form per §18.2
       let body = message;
       if (reloadTrailer !== "") body = appendReloadHint(body, reloadTrailer);
       body = `${body}\n\n${retryAnchor}`;  // blank line between reload trailer (or message) and retry anchor per CMC-15
       notifyWarning(opts.ctx, body);
       ```
       The CMC-15 binding: when both fire, reload is ABOVE retry with blank line between. Verify against catalog lines 642-651.

    3. Manual-recovery emission (MR-9 or similar system-level recovery): when an agent-index unparseable / state.json system failure surfaces, the orchestrator emits via `renderManualRecovery`:
       ```
       const recoveryLine = renderManualRecovery({
         kind: "manual-recovery",
         resource: "agent index",  // or "state.json"
         reasons: ["unparseable"],  // or other closed-set Reason
       }, probe);
       const body = `${cascadeBody}\n\n${recoveryLine}`;  // blank line above per MSG-MR-1
       notifyWarning(opts.ctx, body);
       ```
       Identify the system-level failure call paths in the existing `remove.ts` (lines 195/245 -- the post-state cleanup leak + the aggregated `formatErrorWithCauses` paths probably touch this). If the existing code doesn't have a manual-recovery path today, the migration creates one only when the catalog requires it -- verify against catalog lines 722-736.

    4. Remove imports of `formatErrorWithCauses`, `subagentWarningIfNeeded`, `mcpAdapterWarningIfNeeded` from this file (per Plans 13-01-02 + 13-01-03).

    5. Delete the legacy `subagentWarn` / `mcpWarn` aggregated-trailer block at remove.ts:265-266. The per-row markers via `renderRow` on `PluginCascadeRow` children replace it.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `grep -c 'Updated marketplace' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 0 (CMC-32 legacy sentence retired)
    - `grep -rc 'cascadeSummary' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 2 (D-13-08 consumer)
    - `grep -c 'renderManualRecovery' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (CMC-16 / D-13-15 manual-recovery composer; emit only when catalog requires it)
    - `grep -c 'Fix the underlying issue and retry' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (CMC-15 retry anchor on partial-failure)
    - `grep -c '"plugins remain"' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (CMC-31 partial-failure reason)
    - `grep -c 'kind: "marketplace"' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1
    - `grep -c '"no marketplaces"' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns at least 1 (CMC-10 empty case via EmptyToken)
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; `grep -c 'Updated marketplace' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` returns 0 (legacy sentence retired); `grep -c 'cascadeSummary' extensions/pi-claude-marketplace/orchestrators/marketplace/{remove,update}.ts` returns at least 2; `grep -c 'renderManualRecovery' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (if the catalog requires manual-recovery emission on mp remove); `grep -c 'Fix the underlying issue and retry' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (the CMC-15 retry anchor); `grep -c 'plugins remain' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Migrate edge/handlers/marketplace/{add,remove,update}.ts entity-shape errors + finalize platform/pi-api.ts deletion of trailer helpers</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/remove.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts, extensions/pi-claude-marketplace/platform/pi-api.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts + remove.ts + update.ts (current files; identify `notifyError(ctx, ...)` vs `notifyUsageError(ctx, ...)` calls)
    - extensions/pi-claude-marketplace/platform/pi-api.ts (the source of `subagentWarningIfNeeded` + `mcpAdapterWarningIfNeeded`; verify NO remaining callers across the codebase before deletion)
    - docs/messaging-style-guide.md §13 MSG-NC-1..2 (entity-shape compact vs usage-error sentence)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (EntityErrorRow)
    - Grep across the codebase for remaining trailer-helper consumers: `grep -rln 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded' extensions/pi-claude-marketplace/ tests/`
  </read_first>
  <behavior>
    EDGE HANDLERS:
    - Entity-shape errors (CMC-34 / MSG-NC-1 per D-13-05) -- `MarketplaceNotFoundError`, `MarketplaceDuplicateNameError`, `StaleSourceCloneError`, unknown marketplace name, source-parse-failure resolving to a specific entity: emit `EntityErrorRow` (`kind: "entity-error"`) compact line via `renderRow` + `notifyError`. Examples:
      - `MarketplaceNotFoundError("foo", ["user"])` → `EntityErrorRow{name:"foo", scope:"user", status:"failed", reasons:["not found"]}`
      - `MarketplaceDuplicateNameError("foo", "project")` → `EntityErrorRow{name:"foo", scope:"project", status:"failed", reasons:["duplicate name"]}`
      - `StaleSourceCloneError(absPath)` → `EntityErrorRow{name: derivedName, scope, status:"failed", reasons:["stale clone"]}` (verify in closed Reason set)
    - Usage errors (missing `--scope`, malformed `<source>`, etc.) per MSG-NC-2 / MSG-SR-7: STAY `notifyUsageError(ctx, message, usageBlock)`. D-13-05's `EntityErrorRow` does NOT cover the usage-error path.

    PLATFORM/PI-API.TS DELETION (RESEARCH.md Open Question 3):
    - Verify `subagentWarningIfNeeded` and `mcpAdapterWarningIfNeeded` have ZERO callers across `extensions/pi-claude-marketplace/` + `tests/` (every caller migrated by sub-waves 2a, 2b, 2c -- their per-row marker replacement).
    - Delete the two export functions from `platform/pi-api.ts`.
    - `presentation/soft-dep.ts` and `presentation/index.ts` already dropped the re-exports in Plan 13-01-03.
    - The 3 probe helpers (`hasLoadedPiMcpAdapter`, `hasLoadedPiSubagents`, `softDepStatus`) STAY.
  </behavior>
  <action>
    For each `edge/handlers/marketplace/{add,remove,update}.ts`:

    1. Grep for `notifyError(ctx, ...)` calls; classify each:
       - Entity-shape (the operation is targeting a specific named marketplace that's malformed/missing/duplicate): migrate to `EntityErrorRow` + `renderRow` + `notifyError`.
       - Generic error (I/O failure, lock contention, malformed JSON in state): pass `errorMessage(err)` + bare `err` to `notifyError`; cause-chain auto-appends.
    2. Map each entity-shape error to an `EntityErrorRow` payload with the closed-set Reason. Verify each reason exists in `shared/grammar/reasons.ts:34-58`.
    3. Keep `notifyUsageError(ctx, ...)` calls UNCHANGED.

    For `platform/pi-api.ts`:

    1. Run `grep -rln 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded' extensions/pi-claude-marketplace/ tests/` -- expect zero results EXCEPT the export definitions in `platform/pi-api.ts` itself. If any non-platform caller remains, that callsite must be migrated FIRST (it indicates an earlier sub-wave missed a migration).
    2. Delete the two functions: `subagentWarningIfNeeded(pi, names)` and `mcpAdapterWarningIfNeeded(pi, names)`. Remove any private helper functions referenced only by these two.
    3. Verify `npm run check` exits 0 after the deletion -- if it fails, the verification of zero callers was incomplete; revert and migrate the remaining caller.

    The probe helpers (`hasLoadedPiMcpAdapter`, `hasLoadedPiSubagents`, `softDepStatus`) and the `SoftDepStatus` interface STAY -- they're consumed by `renderRow` (Wave 1 keystone).
  </action>
  <verify>
    <automated>npm run check && node --test tests/edge/router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `node --test tests/edge/router.test.ts` exits 0
    - `grep -rln 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded' extensions/pi-claude-marketplace/ tests/` returns 0 lines (D-13-07 / Open Question 3: helpers fully deleted from platform/pi-api.ts; per-row markers replace)
    - `grep -rc 'EntityErrorRow' extensions/pi-claude-marketplace/edge/handlers/marketplace/` returns at least 3 (one per add/remove/update handler per CMC-34 / D-13-05)
    - `grep -rc 'notifyUsageError' extensions/pi-claude-marketplace/edge/handlers/marketplace/` returns at least 3 (usage-error sentence form preserved per MSG-NC-2)
    - `grep -c 'export function subagentWarningIfNeeded\|export function mcpAdapterWarningIfNeeded' extensions/pi-claude-marketplace/platform/pi-api.ts` returns 0 (helpers deleted)
    - `grep -c 'export function softDepStatus\|export function hasLoadedPi' extensions/pi-claude-marketplace/platform/pi-api.ts` returns at least 2 (the 3 probe helpers stay)
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; `node --test tests/edge/router.test.ts` exits 0; `grep -rln 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded' extensions/pi-claude-marketplace/ tests/` returns 0 lines (helpers fully gone); `grep -c 'EntityErrorRow' extensions/pi-claude-marketplace/edge/handlers/marketplace/` (recursive) returns at least 3 (one per handler); `grep -c 'notifyUsageError' extensions/pi-claude-marketplace/edge/handlers/marketplace/` (recursive) preserves the original count (usage errors unchanged).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marketplace orchestrator ↔ user-supplied names | Names enter via user input; rendered into compact lines via `MarketplaceRow.name` (no escaping needed -- the renderer emits the string verbatim, and any sanitization happens upstream in the source parser) |
| `renderManualRecovery` ↔ system-level resource names | Resource names are HARD-CODED ("agent index", "state.json") -- not user-supplied; no injection risk |
| `cascadeSummary` ↔ failed-plugin children | Each `PluginCascadeRow.reasons` is closed-set `Reason[]`; no raw exception messages embedded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Tampering | CMC-31 partial-failure ordering | mitigate | `cascadeSummary` always sorts children via `compareByNameThenScope`; the order is deterministic across runs |
| T-13-14 | Information Disclosure | mp remove failure cause chains | mitigate | `notifyError`'s body uses `causeChainTrailer` (depth 5; only `.message` surfaced; NFR-9) |
| T-13-15 | Denial of Service | `cascadeSeverity` on large remove cascades | accept | Linear scan over `PluginCascadeRow[]`; O(n) where n is the number of plugins in the marketplace; unbounded in theory but bounded in practice (a marketplace with 10000 plugins is implausible) |

NFR-9 invariant inherited from earlier plans.
</threat_model>

<verification>
- `npm run check` exits 0 (full suite)
- `node --test tests/edge/router.test.ts` exits 0
- `node --test tests/presentation/marketplace-list.test.ts` exits 0
- `grep -c 'scope marketplaces:' extensions/pi-claude-marketplace/presentation/marketplace-list.ts` returns 0 (group headers retired)
- `grep -c 'Added marketplace\|Updated marketplace' extensions/pi-claude-marketplace/orchestrators/marketplace/` (recursive) returns 0 (legacy sentence forms retired)
- `grep -rln 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded' extensions/pi-claude-marketplace/ tests/` returns 0 lines (helpers fully deleted)
- `grep -c 'Fix the underlying issue and retry' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (CMC-15 retry anchor present on partial path)
- `grep -c 'plugins remain' extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts` returns at least 1 (CMC-31 partial-failure Reason)
- `grep -c 'no autoupdate' extensions/pi-claude-marketplace/orchestrators/marketplace/autoupdate.ts` returns at least 1 (CMC-33 disable marker)
- `tests/architecture/no-legacy-markers.test.ts` exits 0 (no regression)
</verification>

<success_criteria>
1. `marketplace list` emits flat per-scope rows sorted via `compareByNameThenScope`; group headers retired (CMC-03 / CMC-29). Empty case is `(no marketplaces)` (CMC-10).
2. `marketplace add` emits `MarketplaceRow` with marker dispatched by source kind (github→autoupdate; path→no marker) (CMC-30 / CMC-05).
3. `marketplace remove` is conditional: clean → bare `MarketplaceRow{status:"removed"}`; partial → header `(failed) {plugins remain}` + children + reload-hint + retry anchor coexisting per CMC-15 (CMC-31).
4. `marketplace update` cascade-on emits header + children via `cascadeSummary`; cascade-off emits standalone marketplace row; legacy `Updated marketplace "X"...` sentence retired (CMC-32).
5. `marketplace autoupdate enable|disable` emits marker as outcome (no status token); `<no autoupdate>` only on disable; `{already enabled/disabled}` on idempotent flips (CMC-33).
6. Manual-recovery emission on system-level failure (agent index, state.json) emits via `renderManualRecovery` (CMC-16).
7. Edge handler entity-shape errors emit `EntityErrorRow` compact lines (CMC-34 completion for marketplace commands).
8. Marketplace icon dispatched by `outcomeClass` -- `●` for ok, `⊘` for failure (CMC-07).
9. `subagentWarningIfNeeded` and `mcpAdapterWarningIfNeeded` deleted from `platform/pi-api.ts` (RESEARCH.md Open Question 3 resolved).
10. `npm run check` stays green; ESLint allow-list + static-audit + grammar-frontmatter all pass.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-02c-01-SUMMARY.md` when done, citing CMC-03 / CMC-05 / CMC-07 / CMC-10 / CMC-15 / CMC-16 / CMC-29 / CMC-30 / CMC-31 / CMC-32 / CMC-33 / CMC-34, the modified files, the deleted helpers from `platform/pi-api.ts`, and the rewritten test.
</output>
