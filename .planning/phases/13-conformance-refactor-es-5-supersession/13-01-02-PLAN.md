---
phase: 13-conformance-refactor-es-5-supersession
plan: 01-02
type: execute
wave: 1
depends_on:
  - 13-01-01
files_modified:
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/presentation/cause-chain.ts
  - extensions/pi-claude-marketplace/presentation/cascade-summary.ts
  - extensions/pi-claude-marketplace/presentation/manual-recovery.ts
  - extensions/pi-claude-marketplace/presentation/rollback-partial.ts
  - extensions/pi-claude-marketplace/presentation/reload-hint.ts
  - extensions/pi-claude-marketplace/presentation/index.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
  - tests/presentation/cause-chain.test.ts
  - tests/presentation/cascade-summary.test.ts
  - tests/presentation/manual-recovery.test.ts
  - tests/presentation/rollback-partial.test.ts
  - tests/presentation/reload-hint.test.ts
autonomous: true
requirements:
  - CMC-15
  - CMC-16
  - CMC-17
  - CMC-18
  - CMC-20

must_haves:
  truths:
    - "Cause-chain emission is a single composer (`causeChainTrailer`) emitting `cause: <l1> -> <l2> -> ... [(truncated)]` per MSG-CC-1 -- the legacy ` -- caused by: ` joiner is gone."
    - "`notifyError(ctx, message, cause?)` automatically appends the MSG-CC-1 trailer with two-newline separation; the 7 existing `formatErrorWithCauses` callsites can pass bare `err` (sub-waves 2a/2b/2c migrate the callers)."
    - "`cascadeSeverity(rows) -> 'success' | 'warning'` is a pure helper; cascade-summary returns `{message, severity}`; orchestrators destructure and dispatch (D-13-08) -- composer-internal `ctx.ui.notify` is forbidden structurally because the composer doesn't import it."
    - "`presentation/reload-hint.ts:56` joins with `\\n\\n` per MSG-RH-1 blank-line discipline -- the Phase 12 D-CMC-06 deferred edit lands."
    - "Manual-recovery and rollback-partial composers route through `renderRow` from compact-line.ts; their public surface is a single function each."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/errors.ts"
      provides: "depth-5 cause-chain walker (relocated from orchestrators/marketplace/shared.ts:443-478); allows shared/notify.ts to consume it without crossing D-11"
      exports: ["causeChainTrailer"]
    - path: "extensions/pi-claude-marketplace/presentation/cause-chain.ts"
      provides: "thin re-export of causeChainTrailer for presentation-layer consumers"
      exports: ["causeChainTrailer"]
    - path: "extensions/pi-claude-marketplace/presentation/cascade-summary.ts"
      provides: "cascadeSummary({marketplace, rows}) -> {message, severity}; cascadeSeverity(rows) helper"
      exports: ["cascadeSummary", "cascadeSeverity", "CascadeSeverity", "CascadeSummaryInput", "CascadeSummaryOutput"]
    - path: "extensions/pi-claude-marketplace/presentation/manual-recovery.ts"
      provides: "renderManualRecovery(line: ManualRecoveryLine) composer with MSG-MR-1 blank-line discipline"
      exports: ["renderManualRecovery"]
    - path: "extensions/pi-claude-marketplace/presentation/rollback-partial.ts"
      provides: "renderRollbackPartial(parent, children) parent + indented children composer per MSG-RP-1"
      exports: ["renderRollbackPartial"]
    - path: "extensions/pi-claude-marketplace/presentation/reload-hint.ts"
      provides: "appendReloadHint with MSG-RH-1 blank-line join (`\\n\\n${hint}`)"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "notifyError with MSG-CC-1 cause-chain trailer (D-CMC-12 deferred work)"
    - path: "tests/presentation/cause-chain.test.ts"
      provides: "depth 0/1/3/5/6/cycle/non-Error coverage for CMC-18 / MSG-CC-1"
    - path: "tests/presentation/cascade-summary.test.ts"
      provides: "severity routing coverage for CMC-20 / MSG-SR-4..6"
    - path: "tests/presentation/manual-recovery.test.ts"
      provides: "shape contract coverage for CMC-16 / MSG-MR-1..2"
    - path: "tests/presentation/rollback-partial.test.ts"
      provides: "parent + indented children coverage for CMC-17 / MSG-RP-1"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "extensions/pi-claude-marketplace/shared/errors.ts"
      via: "import { causeChainTrailer }"
      pattern: "causeChainTrailer\\(cause\\)"
    - from: "extensions/pi-claude-marketplace/presentation/cause-chain.ts"
      to: "extensions/pi-claude-marketplace/shared/errors.ts"
      via: "re-export"
      pattern: "export \\{ causeChainTrailer \\}"
    - from: "extensions/pi-claude-marketplace/presentation/cascade-summary.ts"
      to: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      via: "import { renderRow, PluginCascadeRow, MarketplaceRow, SoftDepProbe }"
      pattern: "renderRow\\("
---

<objective>
Land the remaining Wave 1 composers: the depth-5 cause-chain walker (relocated to `shared/errors.ts` per PATTERNS.md to resolve D-11 layering -- `shared/notify.ts` cannot import from `presentation/`; re-export from `presentation/cause-chain.ts` for presentation-layer consumers), the cascade-summary helper that returns `{message, severity}`, the manual-recovery and rollback-partial composers, the one-line MSG-RH-1 blank-line fix to `presentation/reload-hint.ts:56`, and the body rewrite of `notifyError` that automatically appends the MSG-CC-1 trailer (D-CMC-12 deferred work from Phase 12).

Purpose: Sub-wave 2a (cascades) and sub-wave 2c (`mp remove` partial / manual recovery) cannot proceed without these composers. The `notifyError` rewrite is gating: orchestrators in every sub-wave pass bare `err` to `notifyError` and let it append the trailer once, retiring `formatErrorWithCauses(err)` at the callsites.

Output: 5 new presentation/ files (cause-chain.ts as re-export + cascade-summary.ts + manual-recovery.ts + rollback-partial.ts + presentation/index.ts barrel update); 1 modified shared/ file (errors.ts gains the walker; notify.ts gains the trailer-aware body); 1 modified orchestrator (marketplace/shared.ts -- delete the relocated `formatErrorWithCauses`); 1 one-line modification to reload-hint.ts; 4 new unit tests + 1 existing test update.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/presentation/reload-hint.ts
@extensions/pi-claude-marketplace/presentation/compact-line.ts
@extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts

<interfaces>
From `extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts:443-478` (the existing depth-5 walker to relocate):
```typescript
export function formatErrorWithCauses(err: unknown, maxDepth: number = 5): string {
  const parts: string[] = [];
  let current: unknown = err;
  for (let depth = 0; depth < maxDepth && current !== undefined; depth++) {
    const message = errorCauseMessage(current);
    parts.push(message);
    if (current instanceof Error && current.cause !== undefined && current.cause !== current) {
      current = current.cause;
    } else { break; }
  }
  return parts.join(" -- caused by: ");
}
function errorCauseMessage(current: unknown): string {
  if (current instanceof Error) return current.message;
  return typeof current === "string" ? current : Object.prototype.toString.call(current);
}
```
The walker logic is correct (depth-5, cycle detection via `current.cause !== current`, non-Error fallback); only the rendered shape needs to change per MSG-CC-1: prefix `cause: `, joiner ` -> `, `(truncated)` suffix on the last link when the walk hits the depth bound mid-chain.

From `extensions/pi-claude-marketplace/shared/notify.ts:70-73` (the body to rewrite):
```typescript
export function notifyError(ctx: ExtensionContext, message: string, cause?: unknown): void {
  const causeText = cause === undefined ? "" : `\nCause: ${errorMessage(cause)}`;
  ctx.ui.notify(`${message}${causeText}`, "error");
}
```

From `extensions/pi-claude-marketplace/presentation/reload-hint.ts:55-57` (the one-line MSG-RH-1 edit; the TODO at lines 47-53 references this):
```typescript
export function appendReloadHint(body: string, hint: string): string {
  return hint === "" ? body : `${body}\n${hint}`;
}
```

From `extensions/pi-claude-marketplace/presentation/compact-line.ts` (Plan 13-01-01 output -- consumed here):
- `renderRow(row: RowSpec, probe: SoftDepProbe): string`
- `PluginCascadeRow`, `MarketplaceRow`, `ManualRecoveryLine`, `RollbackChild` interfaces
- `SoftDepProbe` injected-dependency type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement causeChainTrailer in shared/errors.ts + thin re-export in presentation/cause-chain.ts</name>
  <files>extensions/pi-claude-marketplace/shared/errors.ts, extensions/pi-claude-marketplace/presentation/cause-chain.ts, tests/presentation/cause-chain.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/errors.ts (current file -- existing `errorMessage` + `assertNever` + several Error subclasses; lines 1-15 are the relevant export header)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts:443-478 (the existing `formatErrorWithCauses` + private `errorCauseMessage` -- the seed for the new walker)
    - docs/messaging-style-guide.md §11 (MSG-CC-1 binding text: lowercase `cause:`, space-arrow-space joiner, depth 5, `(truncated)` suffix, omitted when no chained cause)
    - docs/output-catalog.md rendered examples that include cause chains (e.g. install failure with runtime + cause chain in §"`/claude:plugin install`" -- line ~290-310)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md §"MODIFIED: shared/notify.ts" + §"NEW: presentation/cause-chain.ts" (lines 253-303 and 636-661; the PATTERNS.md recommendation to place the walker in shared/errors.ts and re-export from presentation/cause-chain.ts to resolve D-11 layering)
  </read_first>
  <behavior>
    - `causeChainTrailer(undefined) === ""`
    - `causeChainTrailer(null) === ""`
    - `causeChainTrailer(new Error("root"))` (no `.cause`) returns `"cause: root"`
    - Depth-2 chain: `new Error("a", { cause: new Error("b") })` returns `"cause: a -> b"`
    - Depth-5 chain: 5-deep cause graph returns `"cause: l1 -> l2 -> l3 -> l4 -> l5"` (NO truncated suffix; walked exactly to bound)
    - Depth-6 chain: the 6th level is unreached; returns `"cause: l1 -> l2 -> l3 -> l4 -> l5 (truncated)"`
    - Cycle: `const e = new Error("loop"); e.cause = e;` returns `"cause: loop"` (no infinite loop; cycle detection inherited from `current.cause !== current`)
    - Non-Error cause: `new Error("a", { cause: "stringy" })` returns `"cause: a -> stringy"`
    - Non-Error non-string cause: `new Error("a", { cause: { x: 1 } })` returns `"cause: a -> [object Object]"` (per the existing `errorCauseMessage` fallback)
    - The 23-entry `Reason` literal union from `shared/grammar/reasons.ts` is NOT involved -- cause-chain renders Error messages verbatim, not Reason tokens.
    - `presentation/cause-chain.ts` re-exports `causeChainTrailer` for presentation-layer consumers without crossing D-11 (`shared/` may not import from `presentation/`; `presentation/` may import from `shared/`).
  </behavior>
  <action>
    Add a new exported function `causeChainTrailer(err: unknown): string` to `extensions/pi-claude-marketplace/shared/errors.ts`. Resolution of the PATTERNS.md open question (where the depth-5 walker lives): place it in `shared/errors.ts` because `shared/notify.ts` cannot import from `presentation/` (D-11), and `notifyError`'s rewritten body (Task 4 below) must call `causeChainTrailer`. Place a thin re-export in `presentation/cause-chain.ts` so presentation-layer consumers (cascade-summary.ts, future Wave 2 callers) reference the composer via `presentation/`.

    Implementation: structurally identical to the existing `formatErrorWithCauses` walk at `orchestrators/marketplace/shared.ts:443-478` -- depth bound 5, cycle detection (`current.cause !== current`), non-Error fallback through the existing `Object.prototype.toString.call` shape. THREE differences from the legacy walker:
    1. Joiner becomes `" -> "` (space-arrow-space) per MSG-CC-1.
    2. Prefix becomes `"cause: "` (lowercase) per MSG-CC-1.
    3. When the walk hits the depth bound AND `current` still has a non-null/undefined value (i.e., there's more chain unreached), append `" (truncated)"` to the LAST emitted link.
    4. Return `""` when the input `err` is `undefined` or `null` so callers can do `body + (trailer === "" ? "" : "\n\n" + trailer)` without extra guards.

    Implementation detail: track a `truncated: boolean` flag inside the walk. When the loop exits via the depth-bound break, set `truncated = true` only if `current` is still non-null and would have continued (i.e., the cause chain extended further). On return, if truncated, mutate `links[links.length - 1] += " (truncated)"` before joining.

    Use a private helper `linkMessage(c: unknown): string` (rename from `errorCauseMessage` to avoid namespace collision with the existing `errorMessage`):
    - `c instanceof Error` → `c.message`
    - `typeof c === "string"` → `c`
    - otherwise → `Object.prototype.toString.call(c)`

    Create `extensions/pi-claude-marketplace/presentation/cause-chain.ts` as a thin re-export module:
    ```
    // presentation/cause-chain.ts -- MSG-CC-1 cause-chain trailer.
    // The walker logic lives in shared/errors.ts so shared/notify.ts can consume it
    // without crossing D-11 (shared/ → presentation/ is forbidden). This file is the
    // presentation-layer re-export so Wave 2 sub-waves import "presentation/cause-chain.ts".
    export { causeChainTrailer } from "../shared/errors.ts";
    ```

    Create `tests/presentation/cause-chain.test.ts` with one `test(...)` per behavior bullet listed above. Use `assert.equal(causeChainTrailer(input), expected)` patterns. Import from `presentation/cause-chain.ts` (NOT from `shared/errors.ts`) so the re-export is exercised.
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/presentation/cause-chain.test.ts</automated>
  </verify>
  <done>
    `tsc --noEmit` exits 0; `node --test tests/presentation/cause-chain.test.ts` exits 0; `grep -c 'export function causeChainTrailer' extensions/pi-claude-marketplace/shared/errors.ts` returns at least 1; `grep -c 'causeChainTrailer' extensions/pi-claude-marketplace/presentation/cause-chain.ts` returns at least 1; depth-5 vs depth-6 chains produce different outputs (the depth-6 has ` (truncated)`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create presentation/cascade-summary.ts (cascadeSummary + cascadeSeverity)</name>
  <files>extensions/pi-claude-marketplace/presentation/cascade-summary.ts, tests/presentation/cascade-summary.test.ts</files>
  <read_first>
    - docs/messaging-style-guide.md §10 (MSG-SR-4..6 cascade severity routing; never `notifyError` on cascade; all-trivial → success; any non-trivial skipped/failed → warning)
    - docs/output-catalog.md "Severity routing" section (lines 130-141; the binding table per MSG-SR-4..6) + reinstall/update/import cascade examples
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md §"`cascadeSeverity` helper" (lines 808-867; the proposed implementation including `isTrivialUpToDate`)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md §"NEW: presentation/cascade-summary.ts" (lines 137-202)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (Plan 13-01-01 output: `renderRow`, `PluginCascadeRow`, `MarketplaceRow`, `SoftDepProbe`)
    - extensions/pi-claude-marketplace/presentation/sort.ts (Plan 13-01-01 output: `compareByNameThenScope` for intra-cascade row sort)
  </read_first>
  <behavior>
    - `cascadeSeverity([])` → `"success"` (vacuously trivial; an empty cascade is a success)
    - `cascadeSeverity([{status:"installed",...}])` → `"success"`
    - `cascadeSeverity([{status:"updated",...}])` → `"success"`
    - `cascadeSeverity([{status:"reinstalled",...}])` → `"success"`
    - `cascadeSeverity([{status:"uninstalled",...}])` → `"success"`
    - `cascadeSeverity([{status:"available",...}])` → `"success"`
    - `cascadeSeverity([{status:"skipped", reasons:["up-to-date"]}])` → `"success"` (trivial up-to-date)
    - `cascadeSeverity([{status:"skipped", reasons:["not in manifest"]}])` → `"warning"` (non-trivial)
    - `cascadeSeverity([{status:"skipped", reasons:["source mismatch"]}])` → `"warning"`
    - `cascadeSeverity([{status:"failed",...}])` → `"warning"`
    - `cascadeSeverity([{status:"rollback failed",...}])` → `"warning"`
    - `cascadeSeverity([{status:"unavailable",...}])` → `"warning"`
    - `cascadeSeverity([{status:"upgradable",...}])` → `"success"` (list-rendering only; not a cascade-failure indicator)
    - `cascadeSeverity` never returns `"error"` (MSG-SR-6 forbids `notifyError` for cascade summaries)
    - Mixed-row severity is the OR (any single non-trivial row makes the whole cascade `"warning"`)
    - `cascadeSummary({marketplace, rows})` returns `{message: string, severity: 'success' | 'warning'}` where the message is the marketplace-header row + indented per-row lines (2-space indent), rows sorted by `compareByNameThenScope`
    - The composer does NOT call `ctx.ui.notify` (D-13-08 / D-07 enforcement) -- importing `shared/notify.ts` from this file is explicitly forbidden
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/presentation/cascade-summary.ts`. Export:
    - `export type CascadeSeverity = "success" | "warning";`
    - `export interface CascadeSummaryInput { readonly marketplace: MarketplaceRow; readonly rows: readonly PluginCascadeRow[]; readonly probe: SoftDepProbe; }` -- per D-13-08; probe must be passed in so the renderer can probe companion-loaded state for per-row soft-dep markers.
    - `export interface CascadeSummaryOutput { readonly message: string; readonly severity: CascadeSeverity; }`
    - `export function cascadeSeverity(rows: readonly PluginCascadeRow[]): CascadeSeverity` -- iterate rows; if any has status `"failed"` / `"rollback failed"` / `"unavailable"` OR (status === `"skipped"` AND NOT trivially up-to-date), return `"warning"`. Otherwise return `"success"`. The `"upgradable"` status is treated as success (list-rendering token; not a cascade failure indicator).
    - `export function cascadeSummary(input: CascadeSummaryInput): CascadeSummaryOutput` -- build `lines: string[] = [renderRow(input.marketplace, input.probe)]`; sort `input.rows` via `[...input.rows].sort(compareByNameThenScope)`; for each sorted row, push `"  " + renderRow(row, input.probe)` (2-space indent); return `{message: lines.join("\n"), severity: cascadeSeverity(input.rows)}`.

    Use a private helper `isTrivialUpToDate(r: PluginCascadeRow): boolean` returning `r.reasons !== undefined && r.reasons.length === 1 && r.reasons[0] === "up-to-date"`.

    Import `renderRow`, `PluginCascadeRow`, `MarketplaceRow`, `SoftDepProbe` from `./compact-line.ts`. Import `compareByNameThenScope` from `./sort.ts`. DO NOT import from `../shared/notify.ts` -- the composer must not be able to call `ctx.ui.notify`. The orchestrator destructures `{message, severity}` and calls the matching wrapper (`severity === "warning" ? notifyWarning : notifySuccess`).

    Create `tests/presentation/cascade-summary.test.ts`. For each behavior bullet, one `test(...)`. Build minimal `PluginCascadeRow` payloads via the variant interface (just enough fields to pass through `renderRow`); for `cascadeSummary` tests, construct a `MarketplaceRow` header + 1-3 rows and assert the message has the marketplace line first, sorted indented rows after; verify the severity matches `cascadeSeverity(rows)`.

    A negative-import test: `grep -c "from \"../shared/notify\"" extensions/pi-claude-marketplace/presentation/cascade-summary.ts` MUST return 0 (this is enforced via the verify command, not a runtime assertion).
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/presentation/cascade-summary.test.ts && test "$(grep -c 'from \"\.\./shared/notify' extensions/pi-claude-marketplace/presentation/cascade-summary.ts)" = "0"</automated>
  </verify>
  <done>
    `tsc --noEmit` exits 0; `node --test tests/presentation/cascade-summary.test.ts` exits 0; `grep -c 'export function cascadeSummary' extensions/pi-claude-marketplace/presentation/cascade-summary.ts` returns at least 1; `grep -c 'export function cascadeSeverity' extensions/pi-claude-marketplace/presentation/cascade-summary.ts` returns at least 1; no import of `shared/notify` from this file (D-13-08 enforcement).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create presentation/manual-recovery.ts and presentation/rollback-partial.ts composers</name>
  <files>extensions/pi-claude-marketplace/presentation/manual-recovery.ts, extensions/pi-claude-marketplace/presentation/rollback-partial.ts, tests/presentation/manual-recovery.test.ts, tests/presentation/rollback-partial.test.ts</files>
  <read_first>
    - docs/messaging-style-guide.md §9 (MSG-MR-1..2: manual-recovery as separate top-level line, blank line above, no `@<mp>` / `[<scope>]` for system-level resources, free-form indented children allowed)
    - docs/messaging-style-guide.md §8 (MSG-RP-1: rollback-partial parent + 2-space-indented children; each child its own compact line with its own status token)
    - docs/output-catalog.md "Manual recovery" section (lines 722-742; example output for triggered-by-failing-install)
    - docs/output-catalog.md install rollback example (~lines 304-310; `(failed) {rollback partial}` parent + indented children)
    - docs/output-catalog.md update rollback example (~lines 437-450)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (Plan 13-01-01 output: `renderRow`, `ManualRecoveryLine`, `RollbackChild`, `PluginInlineRow`, `PluginCascadeRow`, `SoftDepProbe`)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md §"NEW: presentation/manual-recovery.ts" (lines 206-226) + §"NEW: presentation/rollback-partial.ts" (lines 230-249)
  </read_first>
  <behavior>
    `renderManualRecovery(line: ManualRecoveryLine): string`:
    - Renders the compact-line via `renderRow({kind: "manual-recovery", ...line}, dummyProbe)` (manual-recovery has no soft-dep concerns)
    - The output is the rendered compact line ALONE (no blank-line prefix from this composer; the orchestrator composes `parent_op_body + "\n\n" + renderManualRecovery(line)` to provide the MSG-MR-1 blank-line discipline)
    - When `line.orphanDetails` is non-empty, each detail is appended on its own line indented 2 spaces (free-form per §18.2; not a `RowSpec` row)
    - Example output for `{resource: "agent index", reasons: ["unparseable"], orphanDetails: []}`: `⊘ agent index (manual recovery) {unparseable}`

    `renderRollbackPartial(parent: PluginInlineRow | PluginCascadeRow, children: readonly RollbackChild[]): string`:
    - Renders the parent via `renderRow(parent, dummyProbe)` -- the parent row must already carry `status: "failed"` and `reasons` that include `"rollback partial"` (multi-phase) or the relevant single-phase reason
    - For each child, append `"  " + renderRow({kind: "rollback-child", ...child}, dummyProbe)` (2-space indent)
    - When `children` is empty, return just the parent line (no children block)
    - Cause-chain trailer is composed by the CALLER after the rollback block (not by this composer) per MSG-RP-1's binding text

    Both composers return a single string (no severity); the orchestrator decides which `notify*` wrapper to dispatch to.

    Soft-dep probe: both composers accept a `SoftDepProbe` parameter for consistency with `renderRow`'s signature, even though manual-recovery and rollback-child variants never carry soft-dep predicates. Pass through to `renderRow`.
  </behavior>
  <action>
    Create `extensions/pi-claude-marketplace/presentation/manual-recovery.ts`:
    ```
    // presentation/manual-recovery.ts -- MSG-MR-1..2 manual-recovery composer.
    // The compact-line rendering lives in compact-line.ts (kind: "manual-recovery");
    // this file owns the blank-line + orphanDetails composition rule.
    ```
    Export `renderManualRecovery(line: ManualRecoveryLine, probe: SoftDepProbe): string`. Implementation:
    1. Build the head line via `renderRow({kind: "manual-recovery", resource: line.resource, reasons: line.reasons, orphanDetails: line.orphanDetails}, probe)`.
    2. If `line.orphanDetails` is non-empty, append each detail on its own line: `head + "\n" + line.orphanDetails.map(d => "  " + d).join("\n")`.
    3. Return the result. NO blank-line prefix -- caller composes that.

    Create `extensions/pi-claude-marketplace/presentation/rollback-partial.ts`:
    ```
    // presentation/rollback-partial.ts -- MSG-RP-1 rollback-partial composer.
    // Parent + indented children; the parent row must already carry status "failed"
    // and the relevant reasons (caller's responsibility). Cause-chain trailer is
    // composed by the caller AFTER the rollback block.
    ```
    Export `renderRollbackPartial(parent: PluginInlineRow | PluginCascadeRow, children: readonly RollbackChild[], probe: SoftDepProbe): string`. Implementation:
    1. Build parent line via `renderRow(parent, probe)`.
    2. If `children.length === 0`, return just the parent line.
    3. Otherwise build indented children: `lines.push("  " + renderRow({kind: "rollback-child", ...c}, probe))` for each `c` in `children`.
    4. Return `[parent, ...indentedChildren].join("\n")`.

    Create `tests/presentation/manual-recovery.test.ts` with at least the following coverage:
    - System-level resource with single reason: `{resource: "agent index", reasons: ["unparseable"]}` → `⊘ agent index (manual recovery) {unparseable}`
    - Multi-reason: `{resource: "state.json", reasons: ["unparseable", "stale clone"]}` → `⊘ state.json (manual recovery) {unparseable, stale clone}`
    - With orphanDetails: append indented children
    - No `@<mp>` / `[<scope>]` (structural: ManualRecoveryLine variant has no marketplace/scope fields)

    Create `tests/presentation/rollback-partial.test.ts`:
    - Parent + 2 children: parent line first, then 2-space-indented children with their own status tokens
    - Empty children: just the parent line
    - Single-phase parent: parent reasons include `"<phase>"` (e.g., `"agents"`); multi-phase parent reasons include `"rollback partial"`

    Each test imports from the new file under test.
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/presentation/manual-recovery.test.ts tests/presentation/rollback-partial.test.ts</automated>
  </verify>
  <done>
    Both test files pass; `grep -c 'export function renderManualRecovery' extensions/pi-claude-marketplace/presentation/manual-recovery.ts` returns at least 1; `grep -c 'export function renderRollbackPartial' extensions/pi-claude-marketplace/presentation/rollback-partial.ts` returns at least 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Rewrite notifyError to append cause-chain trailer; apply MSG-RH-1 blank-line fix to reload-hint.ts; update barrel + reload-hint test</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/presentation/reload-hint.ts, extensions/pi-claude-marketplace/presentation/index.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, tests/presentation/reload-hint.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts:70-73 (the current notifyError body to rewrite)
    - extensions/pi-claude-marketplace/shared/notify.ts:57-69 (the comment block above notifyError describing the Phase 6 → Phase 13 deferred work)
    - extensions/pi-claude-marketplace/presentation/reload-hint.ts:47-57 (the TODO block at lines 47-53 + the body at lines 55-57)
    - extensions/pi-claude-marketplace/presentation/index.ts (current 3 export blocks + the additions from Plan 13-01-01)
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts:443-478 (formatErrorWithCauses + its private errorCauseMessage helper -- both deleted in this task)
    - tests/presentation/reload-hint.test.ts (current byte-equality assertions; lines that check the `\n${hint}` join need to be updated to `\n\n${hint}`)
    - docs/messaging-style-guide.md §5 (MSG-RH-1 "preceded by one blank line") and §11 (MSG-CC-1 trailer)
  </read_first>
  <behavior>
    - `notifyError(ctx, "msg", undefined)` calls `ctx.ui.notify("msg", "error")` (no trailer; same as today)
    - `notifyError(ctx, "msg", new Error("root"))` calls `ctx.ui.notify("msg\n\ncause: root", "error")` (MSG-CC-1 trailer appended with blank-line separation)
    - `notifyError(ctx, "msg", new Error("a", { cause: new Error("b") }))` calls `ctx.ui.notify("msg\n\ncause: a -> b", "error")`
    - `notifyError(ctx, "msg", "stringy")` calls `ctx.ui.notify("msg\n\ncause: stringy", "error")` (non-Error cause)
    - `appendReloadHint("body", "/reload to pick up changes")` returns `"body\n\n/reload to pick up changes"` (TWO newlines per MSG-RH-1)
    - `appendReloadHint("body", "")` returns `"body"` unchanged
    - `formatErrorWithCauses` is DELETED from `orchestrators/marketplace/shared.ts`; the 5-7 existing callsites do NOT migrate in this plan (they migrate during sub-waves 2a/2b/2c when each orchestrator is touched; `npm run check` MUST stay green during this task -- the callsites pass `errorMessage(err)` + `err` to `notifyError` and let `notifyError` append the trailer; existing callsites that use `formatErrorWithCauses` must be migrated as part of THIS task because deleting the export breaks them)
    - `presentation/index.ts` barrel includes the 5 new presentation exports from this plan: `cascadeSummary`, `cascadeSeverity`, `renderManualRecovery`, `renderRollbackPartial`, `causeChainTrailer` (the cause-chain re-export module)
    - `tests/presentation/reload-hint.test.ts` updated to assert `\n\n${hint}` join
  </behavior>
  <action>
    Sub-task A (notify.ts body rewrite):
    Edit `extensions/pi-claude-marketplace/shared/notify.ts:70-73`. Replace the body of `notifyError` with:
    1. Import `causeChainTrailer` from `./errors.ts` (Task 1 output).
    2. Compute `const trailer = cause === undefined ? "" : causeChainTrailer(cause);`.
    3. Compute `const body = trailer === "" ? message : `${message}\n\n${trailer}`;` (TWO newlines per MSG-CC-1 blank-line separation, same discipline as MSG-RH-1).
    4. Call `ctx.ui.notify(body, "error");`.
    Update the surrounding doc comment (currently at lines 57-69) to reflect that Phase 13's D-CMC-12 deferred work has landed: cite MSG-CC-1, mention the depth-5 walker in `shared/errors.ts`, and update the NFR-9 invariant note (the trailer surfaces only `.message`; no stack traces; depth bound 5 prevents cycle DoS).
    Keep the existing `errorMessage(cause)` import (used elsewhere in the file -- actually, after this edit `errorMessage` may no longer be referenced inside `notifyError`; remove the import if it becomes unused, but verify no other function in `notify.ts` uses it before removing).

    Sub-task B (reload-hint.ts one-line edit):
    Edit `extensions/pi-claude-marketplace/presentation/reload-hint.ts:56`. Change the template literal in the `appendReloadHint` body from `` `${body}\n${hint}` `` to `` `${body}\n\n${hint}` ``. Update the TODO comment block at lines 47-53: remove the "Phase 13" TODO marker; replace with a brief note that MSG-RH-1's blank-line discipline is now satisfied.

    Sub-task C (formatErrorWithCauses deletion + caller migration):
    Delete `formatErrorWithCauses` and its private `errorCauseMessage` helper from `orchestrators/marketplace/shared.ts:443-478`. Use grep to find every caller:
    ```
    grep -rn 'formatErrorWithCauses' extensions/pi-claude-marketplace/ tests/
    ```
    For each caller, migrate per the following rules:
    - `notifyError(ctx, formatErrorWithCauses(err), err)` → `notifyError(ctx, errorMessage(err), err)` (the trailer now lands inside `notifyError`'s body).
    - `notifyWarning(ctx, formatErrorWithCauses(err))` → `notifyWarning(ctx, errorMessage(err) + "\n\n" + causeChainTrailer(err))` (import `causeChainTrailer` from `presentation/cause-chain.ts`; the warning wrapper does NOT auto-append).
    - Any other callsite: import `causeChainTrailer` and compose inline.

    The expected caller set per RESEARCH.md lines 614-616: `orchestrators/plugin/{install,uninstall,update,reinstall}.ts`, `orchestrators/marketplace/{remove,update}.ts`, `orchestrators/import/execute.ts`. Migrate each one in this task -- if `npm run check` fails because a caller still references the deleted symbol, this task is incomplete.

    Sub-task D (barrel update):
    Edit `extensions/pi-claude-marketplace/presentation/index.ts` to add:
    - `export { cascadeSummary, cascadeSeverity } from "./cascade-summary.ts";`
    - `export type { CascadeSeverity, CascadeSummaryInput, CascadeSummaryOutput } from "./cascade-summary.ts";`
    - `export { renderManualRecovery } from "./manual-recovery.ts";`
    - `export { renderRollbackPartial } from "./rollback-partial.ts";`
    - `export { causeChainTrailer } from "./cause-chain.ts";`

    Sub-task E (reload-hint test update):
    Edit `tests/presentation/reload-hint.test.ts`. Find every byte-equality assertion that asserts the `\n${hint}` join (existing single-newline form). Update to assert `\n\n${hint}`. Keep the test count unchanged (no new tests; just shape update to match MSG-RH-1).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    `npm run check` exits 0 (typecheck + ESLint + Prettier + full test suite); `grep -c 'formatErrorWithCauses' extensions/pi-claude-marketplace/` returns 0 (helper gone, all callers migrated); `grep -c 'causeChainTrailer(cause)' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1; `grep -c '\\\\n\\\\n\${hint}' extensions/pi-claude-marketplace/presentation/reload-hint.ts` returns at least 1; `tests/presentation/reload-hint.test.ts` asserts the double-newline join.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `notifyError` ↔ caller-supplied `cause` | The cause may be ANY thrown value; the walker must handle Error, string, and arbitrary object safely |
| `causeChainTrailer` ↔ cause chain | The chain may form a cycle (an Error whose `.cause` is itself or a graph) or have unbounded depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial of Service | `causeChainTrailer` cycle walk | mitigate | Depth bound 5 + cycle detection (`current.cause !== current`) inherited from existing `formatErrorWithCauses` walker; O(5) max iterations |
| T-13-05 | Information Disclosure | `notifyError` body | mitigate | Trailer surfaces only `Error.message` (or string/`Object.prototype.toString` fallback for non-Error); no `.stack`, no absolute paths -- NFR-9 invariant preserved (the existing `errorMessage` helper already strips these) |
| T-13-06 | Tampering | `cascadeSeverity` boundary | mitigate | Pure function over typed `PluginCascadeRow[]`; closed-set `status` from `Extract<StatusToken,...>`; no untrusted input enters this code path |

NFR-9 ("no stack traces / absolute paths leak via notifyError") is preserved structurally: the new body computes `trailer = causeChainTrailer(cause)` and `causeChainTrailer` only emits `linkMessage(c)` which is `Error.message | string | Object.prototype.toString.call(c)`. No path to surface `.stack` exists in any code path.
</threat_model>

<verification>
- `npm run check` exits 0 (full suite)
- `node --test tests/presentation/cause-chain.test.ts` exits 0
- `node --test tests/presentation/cascade-summary.test.ts` exits 0
- `node --test tests/presentation/manual-recovery.test.ts` exits 0
- `node --test tests/presentation/rollback-partial.test.ts` exits 0
- `node --test tests/presentation/reload-hint.test.ts` exits 0 (updated for double-newline join)
- `grep -rc 'formatErrorWithCauses' extensions/pi-claude-marketplace/` returns 0 (helper deleted; all callers migrated)
- `grep -c 'causeChainTrailer' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1
- `grep -c 'cascadeSummary\|cascadeSeverity\|renderManualRecovery\|renderRollbackPartial\|causeChainTrailer' extensions/pi-claude-marketplace/presentation/index.ts` returns at least 5
</verification>

<success_criteria>
1. `causeChainTrailer(err)` in `shared/errors.ts` walks `Error.cause` to depth 5 with cycle detection and emits `cause: <l1> -> <l2> -> ... [(truncated)]`; re-exported from `presentation/cause-chain.ts` for presentation-layer consumers.
2. `notifyError(ctx, message, cause?)` automatically appends the MSG-CC-1 trailer with `\n\n` separation (D-CMC-12 deferred work landed).
3. `cascadeSummary({marketplace, rows, probe})` returns `{message, severity}` with severity computed by `cascadeSeverity(rows)`; the composer does NOT import `shared/notify.ts` (D-13-08 structural enforcement).
4. `renderManualRecovery` and `renderRollbackPartial` composers route through `renderRow` and own their respective MSG-MR-1..2 / MSG-RP-1 composition rules.
5. `presentation/reload-hint.ts:56` joins body + hint with `\n\n` per MSG-RH-1; `tests/presentation/reload-hint.test.ts` updated accordingly.
6. `formatErrorWithCauses` is deleted from `orchestrators/marketplace/shared.ts`; every caller migrated to either `notifyError(..., bare-err)` or inline `causeChainTrailer(err)` composition.
7. `presentation/index.ts` barrel includes the 5 new exports (`cascadeSummary`, `cascadeSeverity`, `renderManualRecovery`, `renderRollbackPartial`, `causeChainTrailer`).
8. `npm run check` stays green.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-01-02-SUMMARY.md` when done, citing the 5 CMC requirements addressed (CMC-15 composer support / CMC-16 / CMC-17 / CMC-18 / CMC-20), the new + modified file paths, and the 4 new test commands.
</output>
