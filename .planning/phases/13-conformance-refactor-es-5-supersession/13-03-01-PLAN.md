---
phase: 13-conformance-refactor-es-5-supersession
plan: 03-01
type: execute
wave: 3
depends_on:
  - 13-02a-01
  - 13-02b-01
  - 13-02c-01
  - 13-02d-01
files_modified:
  - tests/architecture/catalog-uat.test.ts
  - docs/output-catalog.md
autonomous: true
requirements:
  - CMC-22
  - CMC-23
  - CMC-24
  - CMC-25
  - CMC-26
  - CMC-27
  - CMC-28
  - CMC-29
  - CMC-30
  - CMC-31
  - CMC-32
  - CMC-33
  - CMC-34

must_haves:
  truths:
    - "`tests/architecture/catalog-uat.test.ts` reads `docs/output-catalog.md` at test time, extracts each per-command H2 section's fenced rendered examples (tagged with a deterministic state-label discriminator), pairs each example with a programmatic fixture, and asserts byte-equality against the rendered output of the new Wave 1 + Wave 2 renderers."
    - "The catalog UAT is the BINDING verification gate for every per-command requirement (CMC-22..34) -- if any byte-equality assertion fails, Wave 3 plan #2 (the ES-5 atomic commit) does not run."
    - "The catalog itself is the SOLE source of truth for the rendered examples -- the test does NOT duplicate the examples into test code; it parses the .md at test time per D-30 (catalog is the source of truth)."
    - "Each per-command H2 section in `docs/output-catalog.md` is annotated (by this plan) with discriminator markers (HTML comments before each fenced block) so the test can pair fenced blocks with their state-label."
    - "The catalog UAT is the Wave 3 plan-#1 gate per D-13-04 -- BEFORE the ES-5 atomic commit lands, this test MUST be green."
  artifacts:
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "Catalog byte-equality runner; parses fenced examples; pairs with fixtures; asserts byte-equal"
    - path: "docs/output-catalog.md"
      provides: "HTML-comment discriminators added BEFORE each fenced example block so the parser can pair examples with state-labels"
  key_links:
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "docs/output-catalog.md"
      via: "readFile + regex extraction"
      pattern: "readFile.*output-catalog.md"
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      via: "import { renderRow }"
      pattern: "renderRow"
---

<objective>
Wave 3 plan #1: the catalog UAT. Add `tests/architecture/catalog-uat.test.ts` which reads `docs/output-catalog.md` at test time, extracts each per-command H2 section's fenced rendered examples, pairs each with a programmatic fixture (the orchestrator-equivalent state that should produce that example), and asserts byte-equality against the renderer output.

Purpose: This is the PRE-COMMIT GATE for the Wave 3 ES-5 atomic commit (plan #2). If any byte-equality assertion fails, Wave 3 plan #2 DOES NOT RUN. The catalog UAT is the binding verification for every per-command requirement (CMC-22..34) and the primary tool for catching regressions across the entire mechanical refactor.

Output: 1 new test file + annotations to `docs/output-catalog.md` (HTML-comment discriminators before each fenced block).
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@extensions/pi-claude-marketplace/presentation/compact-line.ts
@extensions/pi-claude-marketplace/presentation/cascade-summary.ts
@extensions/pi-claude-marketplace/presentation/manual-recovery.ts
@extensions/pi-claude-marketplace/presentation/rollback-partial.ts
@extensions/pi-claude-marketplace/presentation/sort.ts
@extensions/pi-claude-marketplace/presentation/plugin-list.ts
@extensions/pi-claude-marketplace/presentation/marketplace-list.ts
@tests/architecture/grammar-frontmatter.test.ts
@tests/architecture/markers-snapshot.test.ts
@tests/helpers

<interfaces>
From RESEARCH.md "Pitfall 7: Catalog UAT extraction shape" (lines 530-535; the binding parsing approach -- anchor extraction to per-command H2 sections):

The catalog format (verified by reading `docs/output-catalog.md`):
- Per-command H2 sections (e.g., `## /claude:plugin list`, `## /claude:plugin install <plugin>@<marketplace>`)
- Each section contains multiple fenced blocks (triple-backtick); each block is a rendered example for a specific state.
- Non-command H2 sections (e.g., `## Conventions`, `## Severity routing`, `## Status token reference`) contain grammar templates -- NOT renderer-output examples; these MUST NOT be extracted.

PROPOSED discriminator format (this plan adds these annotations to `docs/output-catalog.md`):
- Before each fenced renderer-output block, insert an HTML comment of the form `<!-- catalog-state: STATE -->` where STATE is a kebab-case label naming the rendered state.
- The parser extracts the STATE name and pairs it with the immediately-following fenced block content.
- Templates in non-command sections (Conventions / Severity routing / Status token reference) DO NOT carry the discriminator and are ignored by the parser.

From the existing test patterns:
- `tests/architecture/grammar-frontmatter.test.ts:36-60` (regex-extract MD frontmatter pattern)
- `tests/helpers/prd-extract.ts::extractEs5MarkerLiterals` (regex-extract literal table from .md by section heading)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate docs/output-catalog.md with HTML-comment state discriminators</name>
  <files>docs/output-catalog.md</files>
  <read_first>
    - docs/output-catalog.md (full file, 813 lines; identify every fenced renderer-output block and every per-command H2 section)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md "Catalog UAT runner shape" (the "deterministic marker" guidance; HTML comments are the proposed discriminator)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md Pitfall 7 (lines 530-535)
  </read_first>
  <behavior>
    - Every fenced renderer-output block under a per-command H2 section is preceded by an HTML comment of the form `<!-- catalog-state: STATE -->`.
    - STATE is a deterministic kebab-case label (e.g., `empty`, `single-mp-mixed`, `same-plugin-both-scopes`, `project-orphan-folded`, `mp-add-github`, `mp-update-autoupdate-on`, `install-success-with-soft-dep`, `install-failure-rollback-partial`).
    - Two different fenced blocks within the same H2 MUST have different STATE names.
    - Templates in the non-command H2 sections (Conventions, Severity routing, Status token reference, Empty / no-op surfaces, Usage errors, Resolutions to apply, Cross-references) DO NOT carry the discriminator. The parser sees no `<!-- catalog-state: -->` comment immediately before the fence, so it skips the block.
    - The `## Manual recovery anchors` section is treated as a per-command section for annotation purposes (the manual-recovery line is its own surface per MSG-MR-1) and its fenced blocks are annotated.
  </behavior>
  <action>
    Edit `docs/output-catalog.md` to add HTML-comment state discriminators before each fenced renderer-output block in the per-command sections. The per-command sections (per the grep run earlier) are:

    - `## Conventions` (line 5) -- DO NOT annotate (grammar templates only)
    - `## Severity routing` (line 130) -- DO NOT annotate (informational table)
    - `## Status token reference` (line 142) -- DO NOT annotate (informational table)
    - `## /claude:plugin list` (line 162) -- ANNOTATE every fenced block
    - `## /claude:plugin install <plugin>@<marketplace>` (line 264) -- ANNOTATE
    - `## /claude:plugin uninstall <plugin>@<marketplace>` (line 312) -- ANNOTATE
    - `## /claude:plugin reinstall` (line 345) -- ANNOTATE
    - `## /claude:plugin update` (line 419) -- ANNOTATE
    - `## /claude:plugin import` (line 471) -- ANNOTATE
    - `## /claude:plugin bootstrap` (line 541) -- ANNOTATE
    - `## /claude:plugin marketplace list` (line 563) -- ANNOTATE
    - `## /claude:plugin marketplace add <source>` (line 589) -- ANNOTATE
    - `## /claude:plugin marketplace remove <name>` (line 622) -- ANNOTATE
    - `## /claude:plugin marketplace update <name>` (line 655) -- ANNOTATE
    - `## /claude:plugin marketplace autoupdate <enable|disable> <name>` (line 691) -- ANNOTATE
    - `## Manual recovery anchors` (line 722) -- ANNOTATE
    - `## Empty / no-op surfaces` (line 743) -- DO NOT annotate (informational)
    - `## Usage errors` (line 754) -- DO NOT annotate (usage-error surface is sentence-form; tested via `tests/edge/router.test.ts` not byte-equal-against-catalog)
    - `## Resolutions to apply to...` (line 767) -- DO NOT annotate
    - `## Cross-references` (line 809) -- DO NOT annotate

    For each annotated section, walk every triple-backtick fenced block. Before each block, on its own line, insert a single-line HTML comment naming the rendered state in kebab-case.

    Naming convention for STATE:
    - kebab-case
    - descriptive of the rendered state (not the command -- the command is implicit from the parent H2 section)
    - example labels:
      - `list`: `empty`, `single-mp-mixed`, `same-plugin-both-scopes`, `project-orphan-folded`, `soft-dep-on-installed`, `unparseable-mp`, `zero-plugin-mp-block`, `multiple-mps`
      - `install`: `success`, `success-with-soft-dep`, `failure-unsupported-features`, `failure-runtime-with-cause`, `failure-rollback-partial`
      - `reinstall`: `all-reinstalled`, `mixed-reinstalled-skipped-failed`, `all-failed-no-reload-hint`, `plugin-became-unavailable`, `bare-multi-mp`
      - etc.

    Two different sections may share the same STATE name (e.g., both `install` and `update` have a `failure-with-cause` state); the pairing is keyed by `(section, STATE)` in the test, not by STATE alone.

    Use a deliberate, repeatable pass: walk the catalog top-to-bottom, identify each fenced block, and insert the comment line.

    Do NOT change any fenced-block content. The annotations are additive ONLY.
  </action>
  <verify>
    <automated>test "$(grep -c 'catalog-state:' docs/output-catalog.md)" -ge 30 && grep -q '^## /claude:plugin list' docs/output-catalog.md</automated>
  </verify>
  <done>
    `grep -c 'catalog-state:' docs/output-catalog.md` returns at least 30 (one per fenced renderer-output block in per-command sections; the exact count varies with how many examples each section has -- count manually if needed); every per-command H2 section has at least one annotated fenced block; templates in `## Conventions` / `## Severity routing` / `## Status token reference` are NOT annotated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/architecture/catalog-uat.test.ts byte-equality runner</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - docs/output-catalog.md (Task 1 output -- now annotated with `<!-- catalog-state: -->` discriminators)
    - tests/architecture/grammar-frontmatter.test.ts (MD-extraction precedent at lines 36-60)
    - tests/architecture/markers-snapshot.test.ts (regex-extract literal block from MD precedent)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (Wave 1 output -- renderRow + every RowSpec variant)
    - extensions/pi-claude-marketplace/presentation/cascade-summary.ts (Wave 1 output -- cascadeSummary)
    - extensions/pi-claude-marketplace/presentation/manual-recovery.ts (Wave 1 output -- renderManualRecovery)
    - extensions/pi-claude-marketplace/presentation/rollback-partial.ts (Wave 1 output -- renderRollbackPartial)
    - extensions/pi-claude-marketplace/presentation/plugin-list.ts (Wave 2 sub-wave 2d output -- renderPluginList)
    - extensions/pi-claude-marketplace/presentation/marketplace-list.ts (Wave 2 sub-wave 2c output -- renderMarketplaceList)
    - tests/helpers (any reusable extraction or assertion helpers)
  </read_first>
  <behavior>
    - At test-run time, the file:
      1. Reads `docs/output-catalog.md`.
      2. Walks H2 boundaries (lines starting with `## /claude:plugin` or `## Manual recovery anchors`); within each per-command section, identifies every `<!-- catalog-state: NAME -->` comment immediately followed by a fenced block. The block content is the EXPECTED rendered string.
      3. For each `(section, STATE, expected)` triple, builds a programmatic fixture (the RowSpec payload + probe that the orchestrators would construct for that state) and calls the corresponding renderer (compact-line `renderRow`, cascade-summary `cascadeSummary`, plugin-list `renderPluginList`, marketplace-list `renderMarketplaceList`, manual-recovery `renderManualRecovery`, rollback-partial `renderRollbackPartial`).
      4. `assert.equal(actual, expected, "section=... state=...")` -- byte-equality. The first failure surfaces the section + STATE in the error message for fast diagnosis.
    - The test file CONTAINS the programmatic fixtures inline (one per `(section, STATE)` pair). The fixtures must match the orchestrators' construction logic exactly -- if a future code change makes the fixtures wrong, the failing assertion is the signal.
    - For sections that involve cascades or multi-mp output (e.g., `reinstall` multi-mp form, `import` multi-scope multi-mp), the fixture composes multiple cascade-summary outputs joined with two newlines and appends a reload-hint if applicable.
    - For sections where the renderer requires a `SoftDepProbe`, the fixture supplies one explicitly (e.g., `{piSubagentsLoaded: false, piMcpAdapterLoaded: true}` for the soft-dep examples).
    - Tests that exercise a state requiring extra context (e.g., a specific error type for the cause-chain trailer) construct the `Error` chain inline.
  </behavior>
  <action>
    Create `tests/architecture/catalog-uat.test.ts`. Structure outline (the executor fills in the actual TypeScript per the codebase's existing test patterns):

    1. File header imports: `node:assert/strict`, `node:fs/promises::readFile`, `node:path`, `node:test`, `node:url::fileURLToPath`. Plus imports from the new presentation modules: `renderRow` from `presentation/compact-line.ts`; `cascadeSummary` from `presentation/cascade-summary.ts`; `renderManualRecovery` from `presentation/manual-recovery.ts`; `renderRollbackPartial` from `presentation/rollback-partial.ts`; `renderPluginList` from `presentation/plugin-list.ts`; `renderMarketplaceList` from `presentation/marketplace-list.ts`; `appendReloadHint`, `reloadHint` from `presentation/reload-hint.ts`. Plus type-only imports for each RowSpec variant interface.

    2. A `loadCatalogExamples()` helper that returns `readonly { section: string; state: string; expected: string }[]`. Implementation:
       - Read `docs/output-catalog.md` via `readFile(catalogPath, "utf8")`.
       - Split on newlines; iterate lines.
       - On `## ` prefix, update `currentSection`.
       - On `<!-- catalog-state: NAME -->` regex match, peek forward to the next fenced-code-block opener (triple-backtick optionally followed by language tag), capture body lines until the closing fence, emit one example record.
       - Return the collected examples.

    3. A FIXTURES dictionary keyed by `[section][STATE]` returning a `() => string` factory. The factory builds the RowSpec payload(s) and invokes the appropriate renderer; the return value is the expected byte-equal output.

       Section entries to create:
       - `/claude:plugin list` -- one factory per annotated STATE (empty, single-mp-mixed, same-plugin-both-scopes, project-orphan-folded, soft-dep-on-installed, unparseable-mp, zero-plugin-mp-block, multiple-mps, etc.)
       - `/claude:plugin install <plugin>@<marketplace>` -- one factory per STATE (success, success-with-soft-dep, failure-unsupported-features, failure-runtime-with-cause, failure-rollback-partial)
       - `/claude:plugin uninstall <plugin>@<marketplace>` -- success, failure
       - `/claude:plugin reinstall` -- one factory per STATE
       - `/claude:plugin update` -- one factory per STATE
       - `/claude:plugin import` -- one factory per STATE
       - `/claude:plugin bootstrap` -- fresh, already-installed
       - `/claude:plugin marketplace list` -- empty, mixed-scopes
       - `/claude:plugin marketplace add <source>` -- path-source, github-source, failure
       - `/claude:plugin marketplace remove <name>` -- clean, partial
       - `/claude:plugin marketplace update <name>` -- autoupdate-off, autoupdate-on-mixed, mp-failure
       - `/claude:plugin marketplace autoupdate <enable|disable> <name>` -- enable-mixed, disable-with-already-disabled, disable-not-found
       - `Manual recovery anchors` -- one factory for the manual-recovery example

    4. A single top-level `test(...)` block that:
       - Calls `loadCatalogExamples()` to get the examples.
       - For each example, looks up the factory at `FIXTURES[section][STATE]`; if missing, records a "NO FIXTURE" failure.
       - Otherwise, invokes the factory, compares the returned string byte-equal to `expected`; if they differ, records a failure with the diff (or just the expected + actual strings).
       - At the end, `assert.deepEqual(failures, [], detailed-message)` -- the error message lists every failing `(section, STATE)` pair with the diff so the executor can diagnose at a glance.

    5. Use `dummyProbe = { piSubagentsLoaded: true, piMcpAdapterLoaded: true }` as the default `SoftDepProbe`; override per-fixture when a soft-dep example requires `piSubagentsLoaded: false` (or similar).

    6. For sections involving cascade-summary: the factory constructs the input (`marketplace` row + `rows` array + `probe`), calls `cascadeSummary(input)`, then composes `message` with the reload-hint trailer if the catalog example includes it (`appendReloadHint(message, hint)`).

    7. For the `Manual recovery anchors` section: the factory uses `renderManualRecovery(line, probe)` directly. If the catalog example includes a preceding parent-op body, the factory composes that body plus a blank line plus the manual-recovery line.

    8. For rollback-partial examples: the factory uses `renderRollbackPartial(parent, children, probe)` and may concatenate `causeChainTrailer(err)` with the appropriate two-newline separation.

    9. EXPECTED COUNT: there are at least 30 annotated fenced blocks (Task 1 output). The FIXTURES map must have an entry for EVERY annotated `(section, STATE)` pair; missing entries surface as "NO FIXTURE" failures. The executor of this plan iterates: run the test, observe missing fixtures, add them, re-run -- until all pass.

    10. The test runs under `node --test tests/architecture/catalog-uat.test.ts` in ≤5s (per VALIDATION.md sampling rate target). Reading + parsing the MD + running ~30-50 fixture functions is well within budget.

    11. The test is the GATE for Wave 3 plan #2 (the ES-5 atomic commit). If this test fails, plan #2 must not run; the executor of plan #2 verifies catalog-uat.test.ts is green as a precondition.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts && npm run check</automated>
  </verify>
  <done>
    `node --test tests/architecture/catalog-uat.test.ts` exits 0 (every catalog example pairs with a fixture AND every fixture produces byte-equal output); `npm run check` exits 0; the test file imports from at least 5 of the new presentation/ composers (compact-line, cascade-summary, manual-recovery, rollback-partial, plugin-list, marketplace-list); the loadCatalogExamples helper handles per-command H2 boundaries and `<!-- catalog-state: -->` discriminator extraction.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog UAT ↔ docs/output-catalog.md | The test reads the catalog at runtime; if the catalog is mutated, the test reflects the new contract -- which is desired (the catalog is the binding source of truth per D-30) |
| Fixture-to-state pairing | The fixture map is the ONLY duplication between code and catalog; missing fixtures surface as "NO FIXTURE" assertion failures, not silently-skipped tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-18 | Tampering | Catalog UAT pairing | mitigate | Every annotated catalog block MUST have a fixture entry; missing entries surface as test failures, not silent passes |
| T-13-19 | Information Disclosure | N/A | n/a | This test only reads the catalog + runs renderers; no I/O beyond the local catalog file |

NO new security threats. The catalog UAT is a pure byte-equality test.
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` exits 0
- `npm run check` exits 0 (full suite)
- `grep -c 'catalog-state:' docs/output-catalog.md` returns at least 30
- `grep -c 'FIXTURES' tests/architecture/catalog-uat.test.ts` returns at least 1
- The test runtime is at most 5 seconds (per VALIDATION.md sampling rate)
</verification>

<success_criteria>
1. `docs/output-catalog.md` is annotated with `<!-- catalog-state: -->` HTML comments before every per-command fenced renderer-output block; grammar templates in non-command sections are NOT annotated.
2. `tests/architecture/catalog-uat.test.ts` exists and passes; the test reads the catalog at runtime, extracts examples by `(section, STATE)` pairs, and asserts byte-equality against the renderer output for EVERY annotated example.
3. Every per-command requirement (CMC-22..34) is verified by at least one fixture in the test: list, install, uninstall, reinstall, update, import, bootstrap, marketplace list, marketplace add, marketplace remove (clean + partial), marketplace update (autoupdate-on + autoupdate-off), marketplace autoupdate enable, marketplace autoupdate disable, entity-shaped error.
4. The test is the GATE for Wave 3 plan #2 -- before the ES-5 atomic commit lands, this test MUST be green.
5. `npm run check` stays green.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-03-01-SUMMARY.md` when done, citing CMC-22 through CMC-34 (every per-command requirement), the new test file, the catalog annotation count, and the GATING relationship to Wave 3 plan #2.
</output>
