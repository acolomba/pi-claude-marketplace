---
phase: 13-conformance-refactor-es-5-supersession
plan: 02b-01
type: execute
wave: 2
depends_on:
  - 13-01-01
  - 13-01-02
  - 13-01-03
  - 13-02a-01
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
autonomous: true
requirements:
  - CMC-09
  - CMC-10
  - CMC-13
  - CMC-17
  - CMC-23
  - CMC-24
  - CMC-28

must_haves:
  truths:
    - "`/claude:plugin install <plugin>@<marketplace>` renders inline `● <plugin>@<marketplace> [<scope>] v<ver> (installed)` via `PluginInlineRow` -- no marketplace header (CMC-23)."
    - "`/claude:plugin uninstall <plugin>@<marketplace>` renders inline `○ <plugin>@<marketplace> [<scope>] v<ver> (uninstalled)` via `PluginInlineUninstalledRow` -- soft-dep markers structurally absent (CMC-24 / MSG-SD-3)."
    - "`/claude:plugin bootstrap` renders single-marketplace `(added)` with `<autoupdate>` marker, or `(skipped) {already installed}` (CMC-28)."
    - "Install failure with rollback-partial renders the parent `(failed) {rollback partial}` row + indented per-phase children + cause-chain trailer (CMC-17)."
    - "Edge handler entity-shape errors (e.g., unknown plugin) emit `⊘ <plugin>@<marketplace> [<scope>] (failed) {<reason>}` compact lines via `EntityErrorRow` (MSG-NC-1). Usage errors STAY sentence-form via `notifyUsageError` (MSG-NC-2 / MSG-SR-7) -- unchanged."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      provides: "Single-plugin install success/failure emission via PluginInlineRow + renderRollbackPartial"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts"
      provides: "Single-plugin uninstall emission via PluginInlineUninstalledRow"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts"
      provides: "Bootstrap emission via composed marketplace/add + autoupdate enable; the bootstrap-specific outcome rendering"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts"
      provides: "Entity-shape errors (unknown plugin/marketplace) emit EntityErrorRow compact lines (MSG-NC-1); usage errors unchanged"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts"
      provides: "Same entity-vs-usage split as install"
  key_links:
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/presentation/compact-line.ts"
      via: "import { renderRow, PluginInlineRow, RollbackChild }"
      pattern: "PluginInlineRow"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/install.ts"
      to: "extensions/pi-claude-marketplace/presentation/rollback-partial.ts"
      via: "import { renderRollbackPartial }"
      pattern: "renderRollbackPartial\\("
---

<objective>
Sub-wave 2b: migrate the three single-plugin orchestrators (`install`, `uninstall`, `bootstrap`) to consume the Wave 1 primitives. These do NOT use `cascadeSummary` -- they emit a single inline compact line per success outcome. Also migrate the corresponding edge handlers (`edge/handlers/plugin/install.ts`, `edge/handlers/plugin/uninstall.ts`) for entity-shape errors per MSG-NC-1 (CMC-34 partial coverage; the marketplace edge handlers migrate in sub-wave 2c).

Purpose: This is the single-plugin-shape sub-wave. The renderer's `PluginInlineRow` variant always includes `marketplace` (MSG-GR-2 inline form -- the `@<marketplace>` token is kept inline because there's no header). The structural enforcement of MSG-PL-4 / CMC-09 (no `(upgradable)` on result rows) lives in the type narrowing of `PluginInlineRow.status` (Plan 13-01-01 excludes `"upgradable"` from this variant's `Extract<>`).

Output: 3 modified orchestrator files + 2 modified edge handlers. Catalog UAT in Wave 3 plan #1 is the binding verification.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
@extensions/pi-claude-marketplace/presentation/compact-line.ts
@extensions/pi-claude-marketplace/presentation/rollback-partial.ts
@extensions/pi-claude-marketplace/presentation/reload-hint.ts
@extensions/pi-claude-marketplace/presentation/cause-chain.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts

<interfaces>
From `extensions/pi-claude-marketplace/presentation/compact-line.ts` (Wave 1 output -- consumed verbatim):
- `PluginInlineRow` interface with required `marketplace` (MSG-GR-2 inline form -- carries `@<mp>`), `name`, `scope`, optional `version`, `status: Extract<StatusToken, "installed" | "updated" | "failed" | "rollback failed" | "unavailable">` (NO `"uninstalled"` -- that variant is `PluginInlineUninstalledRow`; NO `"upgradable"` -- structurally MSG-PL-4), optional `reasons`, optional `declaresAgents/Mcp`.
- `PluginInlineUninstalledRow` interface: `status === "uninstalled"` (literal); NO `declaresAgents/Mcp` fields (MSG-SD-3 structural).
- `EntityErrorRow` interface for edge-handler entity errors: `kind: "entity-error"`, optional `marketplace`, optional `scope`, required `name`, required `status: Extract<StatusToken, "failed" | "unavailable">`, required `reasons`.
- `RollbackChild` interface: `phaseLabel`, `status: Extract<StatusToken, "failed" | "rollback failed">`, required `reasons`.

From `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts:657-691` (the existing single-plugin success callsite):
```typescript
const subagentWarn = subagentWarningIfNeeded(pi, installCtx.stagedAgentNames);
const mcpWarn = mcpAdapterWarningIfNeeded(pi, installCtx.stagedMcpServerNames);
if (!orchestrated) {
  let body = `Installed plugin "${plugin}" from marketplace "${marketplace}".`;
  if (subagentWarn !== "") body = `${body}\n${subagentWarn}`;
  if (mcpWarn !== "") body = `${body}\n${mcpWarn}`;
  const depsNote = installCtx.resolved.notes.find(/* ... */);
  if (depsNote !== undefined) body = `${body}\n${depsNote}`;
  const hint = reloadHint(stagedAny ? [plugin] : []);
  notifySuccess(ctx, appendReloadHint(body, hint));
}
```
This migrates to a `PluginInlineRow` + `renderRow(row, probe)` + `reloadHint` + `appendReloadHint` pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate orchestrators/plugin/install.ts (CMC-23, CMC-17, CMC-09 structural, CMC-13)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (full file; callsites at lines 580/592/610/625/642/653/691 per RESEARCH.md §2.1)
    - docs/output-catalog.md §"`/claude:plugin install <plugin>@<marketplace>`" (lines 264-311; success; success-with-soft-dep; failure-unsupported-features `{hooks, lspServers}`; failure-runtime + cause-chain; failure-with-rollback-partial)
    - docs/messaging-style-guide.md §6 MSG-IC-1..3 (icons); §11 MSG-CC-1 (cause-chain); §8 MSG-RP-1 (rollback-partial)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (Wave 1 output -- PluginInlineRow + EntityErrorRow + RollbackChild)
    - extensions/pi-claude-marketplace/presentation/rollback-partial.ts (Wave 1 output -- renderRollbackPartial)
    - extensions/pi-claude-marketplace/platform/pi-api.ts:76-81 (softDepStatus probe constructor)
  </read_first>
  <behavior>
    - Success: emits `● <plugin>@<marketplace> [<scope>] v<ver> (installed)` via `PluginInlineRow` (D-13-05/D-13-06 discriminated union, `kind: "plugin-inline"`) + `renderRow`; reload-hint trailer fires when `stagedAny === true`; per-row soft-dep markers fire per D-13-07 when (declares AND companion unloaded).
    - Failure: `notifyError(ctx, errorMessage(err), err)` -- cause-chain trailer auto-appends via Plan 13-01-02's `notifyError` body.
    - Rollback-partial (per CMC-17 / MSG-RP-1): build `parentRow = PluginInlineRow{status: "failed", reasons: ["rollback partial"]}` + `children = RollbackChild[]` (one per failed phase); `body = renderRollbackPartial(parent, children, probe)` from D-13-15's composer. Append cause-chain trailer per MSG-CC-1 (pass `err` to `notifyError` which auto-appends per D-CMC-12 / D-13-15). Verify catalog at lines 304-310.
    - `subagentWarn` / `mcpWarn` aggregated trailer LINES are REMOVED per D-13-07 (per-row markers replace aggregated form). The orchestrator builds `PluginInlineRow.declaresAgents = installCtx.stagedAgentNames.length > 0` and `declaresMcp = installCtx.stagedMcpServerNames.length > 0`; the renderer probes companion-loaded state per CMC-13 / MSG-SD-1..3.
    - PI-13 `depsNote` ("dependencies declaration") -- this is informational text that does NOT fit the compact-line grammar. Decide: either (a) compose as a separate post-success notification (likely `notifyWarning` if it's a soft warning) OR (b) inline as a free-form trailer after the compact line. The catalog's install success-with-deps state (if any) is the binding example. If the catalog does not show `depsNote` rendered at all, the orchestrator's emission can drop it OR convert to a single Reason in the compact line (e.g., `{has dependencies}` -- requires a closed-set Reason). Default behavior: preserve as a separate `notifyWarning` AFTER the success notify, sentence-form. This is a free-form trailer per §18.2 if not in the closed set.
    - `formatErrorWithCauses` no longer in the file (Plan 13-01-02 deleted it).
    - CMC-09 / MSG-PL-4 structural verification (D-13-05 narrowing): TS compile fails if any `PluginInlineRow` here tries to set `status: "upgradable"`. Plan 13-01-01's `Extract<StatusToken, ...>` excludes the token; structural enforcement, no runtime check.
  </behavior>
  <action>
    Migrate the single-plugin install success/failure paths:

    1. SUCCESS (orchestrator's `!orchestrated` branch, lines 657-691): replace the manual body composition with:
       ```
       const probe = softDepStatus(pi);
       const successRow: PluginInlineRow = {
         kind: "plugin-inline",
         name: plugin, marketplace, scope,
         version: installCtx.resolved.version,
         status: "installed",
         declaresAgents: installCtx.stagedAgentNames.length > 0,
         declaresMcp: installCtx.stagedMcpServerNames.length > 0,
       };
       const body = renderRow(successRow, probe);
       const hint = reloadHint(stagedAny ? [plugin] : []);
       notifySuccess(ctx, appendReloadHint(body, hint));
       ```

    2. Preserve `depsNote` handling: if `installCtx.resolved.notes` includes a PI-13 deps note, emit it AS A SEPARATE NOTIFICATION after the success: `if (depsNote) notifyWarning(ctx, depsNote);` (sentence-form per the free-form §18.2 escape; verify against the catalog -- if PI-13's example is NOT in the catalog, this is at planner discretion; the safer choice is a follow-up `notifyWarning`). Document this choice in the SUMMARY.

    3. FAILURE (the `notifyError(ctx, cause, err)` paths at lines 580/592/610/625): pass `errorMessage(err)` instead of the legacy `formatErrorWithCauses(err)` text. The trailer auto-appends via Plan 13-01-02's `notifyError` body. If any callsite still wraps the message inline (e.g., `notifyError(ctx, \`Install failed: ${formatErrorWithCauses(err)}\`)`) -- migrate to `notifyError(ctx, "Install failed: " + errorMessage(err), err)`.

    4. ROLLBACK-PARTIAL path (around lines 580 / wherever the rollback failure surfaces): the existing code probably emits the legacy `ROLLBACK_PARTIAL = "(rollback partial: "` prefix. After Plan 13-01-03 enforces the ESLint allow-list, this site MUST be migrated. Build:
       ```
       const parentRow: PluginInlineRow = {
         kind: "plugin-inline",
         name: plugin, marketplace, scope,
         version: installCtx.resolved.version,  // or undefined if not yet known
         status: "failed",
         reasons: failures.length > 1 ? ["rollback partial"] : [failures[0]!.phase as Reason],
         declaresAgents: false, declaresMcp: false,
       };
       const children: RollbackChild[] = failures.map(f => ({
         kind: "rollback-child",
         phaseLabel: f.phase,
         status: "rollback failed",  // or "failed" depending on whether the rollback itself failed vs the phase failed
         reasons: [/* closed-set Reason from f.msg or inferred */],
       }));
       const rollbackBlock = renderRollbackPartial(parentRow, children, probe);
       notifyError(ctx, rollbackBlock, err);  // notifyError auto-appends cause-chain trailer
       ```
       Verify the catalog at lines 304-310 for the exact rendered shape.

    5. Delete imports of `subagentWarningIfNeeded`, `mcpAdapterWarningIfNeeded`, `formatErrorWithCauses` (per Plan 13-01-02 + 13-01-03 they're gone or restricted).

    6. Add imports: `import { renderRow } from "../../presentation/compact-line.ts"`; `import type { PluginInlineRow, RollbackChild } from "../../presentation/compact-line.ts"`; `import { renderRollbackPartial } from "../../presentation/rollback-partial.ts"`; `import { softDepStatus } from "../../platform/pi-api.ts"` (probe). Maintain D-11 layering: `orchestrators/` may import from `presentation/`.

    7. Update any existing unit tests at `tests/orchestrators/plugin/install*.test.ts` that asserted the old body shape (sentence-form "Installed plugin..."). Update to assert the new compact-line shape via byte-equality. The catalog UAT in Wave 3 plan #1 is the binding source; per-orchestrator tests should mirror catalog examples.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `grep -c 'Installed plugin' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns 0 (legacy sentence-form gone per CMC-23)
    - `grep -c 'PluginInlineRow\|renderRow\|renderRollbackPartial' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least 2 (D-13-05/D-13-15 primitives consumed per CMC-23 + CMC-17)
    - `grep -c 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded\|formatErrorWithCauses' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns 0 (D-13-07 per-row markers + D-CMC-12 notifyError trailer auto-append)
    - `grep -c 'kind: "plugin-inline"' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least 1 (D-13-05/D-13-06 discriminant)
    - `grep -c 'declaresAgents\|declaresMcp' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least 1 (D-13-07 per-row soft-dep predicate)
    - `grep -c 'softDepStatus' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least 1 (probe injection)
    - `grep -c 'appendReloadHint' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least 1 (reload-hint trailer composed)
    - Existing tests at `tests/orchestrators/plugin/install*.test.ts` updated to assert the new RowSpec output (D-13-05); no test regressions
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; `grep -c 'Installed plugin' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns 0 (legacy sentence-form gone); `grep -c 'PluginInlineRow\|renderRow\|renderRollbackPartial' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns at least 2; `grep -c 'subagentWarningIfNeeded\|mcpAdapterWarningIfNeeded\|formatErrorWithCauses' extensions/pi-claude-marketplace/orchestrators/plugin/install.ts` returns 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate orchestrators/plugin/uninstall.ts (CMC-24) and orchestrators/plugin/bootstrap.ts (CMC-28)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts (full file; callsites at lines 153/172/193/210/238 per RESEARCH.md §2.1)
    - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts (full file; this orchestrator composes `addMarketplace` + `setMarketplaceAutoupdate`, so its emission is partly inherited; the bootstrap-specific catalog output is the marketplace `(added) <autoupdate>` or `(skipped) {already installed}` row)
    - docs/output-catalog.md §"`/claude:plugin uninstall <plugin>@<marketplace>`" (lines 312-344; success; success-when-plugin-had-soft-dep-resources -- marker NOT emitted on uninstalled rows; failure)
    - docs/output-catalog.md §"`/claude:plugin bootstrap`" (lines 541-562; fresh; re-run when already bootstrapped)
    - docs/messaging-style-guide.md §6 (icon discipline: `(uninstalled)` → `○`); §12 (MSG-SD-3: no soft-dep on uninstalled rows)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (PluginInlineUninstalledRow + MarketplaceRow)
  </read_first>
  <behavior>
    UNINSTALL:
    - Success: emits `○ <plugin>@<marketplace> [<scope>] v<ver> (uninstalled)` via `PluginInlineUninstalledRow` (D-13-05/D-13-06, `kind: "plugin-inline-uninstalled"`) + `renderRow`; reload-hint trailer when any resource was removed.
    - Soft-dep markers NEVER fire on uninstalled rows (D-13-07 / MSG-SD-3) -- structurally enforced because `PluginInlineUninstalledRow` has no `declaresAgents/Mcp` fields per D-13-05.
    - Failure: `notifyError(ctx, errorMessage(err), err)` -- cause-chain auto-appends.
    - PU-5 silent-converge: when `ConcurrentUninstallError` fires, NO notification is emitted (this is the existing PRD-binding behavior; do not change).
    - PU-4 data-dir cleanup leak: emits `notifyWarning` with the leaked path in sentence form (post-commit leak; sentence form preserved per RESEARCH.md note that leaks are not entity outcomes).

    BOOTSTRAP:
    - Bootstrap composes `addMarketplace` + `setMarketplaceAutoupdate`. Per W-01 sub-wave serialisation (D-13-02): sub-wave 2c migrates `marketplace/add.ts` + `marketplace/autoupdate.ts` BEFORE this plan's executor finalises bootstrap behavior (sub-wave 2b runs after 2a but BEFORE 2c per D-13-02; so the bootstrap fresh-add path's reliance on 2c's `marketplace/add.ts` emission means: in this plan, either delegate to the (not-yet-migrated) `marketplace/add.ts` and accept that the fresh-add catalog conformance lands when 2c migrates, OR emit `MarketplaceRow` directly via `renderRow` here. **PREFER direct emission** when bootstrap currently has its own success line; sub-wave 2c will deduplicate later. Bootstrap's OWN emission is either:
      (a) Successful add: `● <name> [<scope>] <autoupdate> (added)` -- the composed `marketplace/add.ts` (sub-wave 2c) handles this; bootstrap doesn't need additional rendering.
      (b) Already-bootstrapped (idempotent re-run): `● <name> [<scope>] <autoupdate> (skipped) {already installed}` -- bootstrap orchestrator emits this directly via `MarketplaceRow` + `renderRow`.
    - Since bootstrap forces `autoupdate=ON`, the `<autoupdate>` marker is ALWAYS present on the bootstrap output row.
    - Migration scope here: ensure bootstrap.ts does NOT hand-format legacy sentence-form strings; either delegate to sub-wave 2c's `marketplace/add.ts` (preferred) or emit `MarketplaceRow` directly.
  </behavior>
  <action>
    UNINSTALL migration (`extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts`):

    1. SUCCESS (around line 210, the `notifySuccess(ctx, ...)` for uninstall success): replace the legacy `Uninstalled plugin "${plugin}" from marketplace "${marketplace}".` body with:
       ```
       const probe = softDepStatus(pi);
       const uninstalledRow: PluginInlineUninstalledRow = {
         kind: "plugin-inline-uninstalled",
         name: plugin, marketplace, scope,
         version: removedRecord.version,
         status: "uninstalled",
       };
       const body = renderRow(uninstalledRow, probe);
       const hint = reloadHint(anyResourceRemoved ? [plugin] : []);
       notifySuccess(ctx, appendReloadHint(body, hint));
       ```
       Note: `PluginInlineUninstalledRow` has NO `declaresAgents/Mcp` fields -- the type system prevents soft-dep markers from being emitted on uninstalled rows (MSG-SD-3). Verify the catalog at lines 327 / lines 339-344 confirms `(uninstalled)` rows NEVER carry soft-dep markers.

    2. FAILURE: replace `notifyError(ctx, formatErrorWithCauses(err), err)` with `notifyError(ctx, errorMessage(err), err)` (Plan 13-01-02 made `formatErrorWithCauses` invalid; this completes the migration).

    3. PU-4 data-dir leak: `notifyWarning(ctx, ...)` sentence-form STAYS unchanged (post-commit leaks are not cascade summaries and not entity outcomes; they're free-form warnings per §18.2).

    4. PU-5 silent-converge (`ConcurrentUninstallError`): no notification, behavior preserved.

    5. Update tests at `tests/orchestrators/plugin/uninstall*.test.ts` to assert the new compact-line shape.

    BOOTSTRAP migration (`extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts`):

    1. If bootstrap emits ANY notify calls of its own (verify with grep `notifySuccess\|notifyWarning\|notifyError` in bootstrap.ts), migrate them:
       - Already-bootstrapped (skipped) path: emit `MarketplaceRow{kind:"marketplace", name, scope, marker: "autoupdate", status: "skipped", reasons: ["already installed"], outcomeClass: "ok"}` via `renderRow` + `notifySuccess`.
       - Fresh-add path: ideally delegate to `marketplace/add.ts`'s output (sub-wave 2c handles); if bootstrap currently composes its own success line, replace with delegation.
    2. If bootstrap purely composes other orchestrators (no own notifies), no changes needed in this task -- sub-wave 2c's `marketplace/add.ts` migration handles the rendering.

    3. Verify `{already installed}` is a member of `shared/grammar/reasons.ts:34-58`. If not, surface as a planning gap (the closed Reason set needs to cover this -- it's a Phase 12 deliverable).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `grep -c 'Uninstalled plugin' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0 (legacy sentence retired per CMC-24)
    - `grep -c 'PluginInlineUninstalledRow\|renderRow' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least 1 (D-13-05 dedicated uninstalled variant; MSG-SD-3 structural)
    - `grep -rc 'formatErrorWithCauses' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts` returns 0
    - `grep -c 'kind: "plugin-inline-uninstalled"' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least 1 (D-13-06 discriminant)
    - `grep -c 'declaresAgents\|declaresMcp' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0 (D-13-07/MSG-SD-3: uninstalled variant structurally has no soft-dep fields)
    - For bootstrap.ts: either zero own-notify calls (delegates to mp/add.ts -- migrated in sub-wave 2c per W-01 serialisation) OR `grep -c 'MarketplaceRow' extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts` returns at least 1 (idempotent skip path uses the row directly per CMC-28)
    - `grep -c '"already installed"' extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts` is at least 1 IF the orchestrator emits the skip row directly (else 0 if delegated)
    - Existing tests at `tests/orchestrators/plugin/{uninstall,bootstrap}*.test.ts` updated to assert the new shapes; no regressions
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; `grep -c 'Uninstalled plugin' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns 0 (legacy sentence form retired); `grep -c 'PluginInlineUninstalledRow\|renderRow' extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts` returns at least 1; `grep -c 'formatErrorWithCauses' extensions/pi-claude-marketplace/orchestrators/plugin/{uninstall,bootstrap}.ts` returns 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate edge/handlers/plugin/{install,uninstall}.ts entity-shape errors (MSG-NC-1 / CMC-34 partial)</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts (current file -- look for `notifyError(ctx, ...)` calls at the edge layer; per RESEARCH.md §2.1 edge-layer note, lines 42/55/61 use `notifyError(ctx, USAGE)` for entity-shape failures)
    - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts (mirror file)
    - docs/messaging-style-guide.md §13 (MSG-NC-1 entity-shape compact-line form; MSG-NC-2 usage-error sentence form)
    - docs/output-catalog.md §"Usage errors" (lines 754-766; usage-error sentence form preserved)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (EntityErrorRow variant)
    - extensions/pi-claude-marketplace/shared/notify.ts (notifyUsageError -- usage errors stay sentence-form)
  </read_first>
  <behavior>
    - Entity-shape errors (unknown plugin, unknown marketplace, not-installed, already-installed, etc.) emit COMPACT-LINE form via `EntityErrorRow` (D-13-05/D-13-06, `kind: "entity-error"`):
      `⊘ <plugin>@<marketplace> [<scope>] (failed) {<reason>}`
      Dispatched via `notifyError` (single-shot error severity per MSG-SR-3). The cause-chain trailer auto-appends if a cause is passed.
    - Usage errors (missing required arg, invalid `--scope` value, etc.) STAY sentence-form via `notifyUsageError(ctx, message, usageBlock)`. Per MSG-NC-2 / MSG-SR-7 / CMC-34 binding (D-13-05 entity-row narrowing does NOT cover usage-error path) -- discipline UNCHANGED.
    - The split between entity-shape and usage errors lives in the edge handler: entity errors arise AFTER argument parsing (the entity doesn't exist or can't be operated on); usage errors arise DURING argument parsing (the args are malformed).
  </behavior>
  <action>
    For `edge/handlers/plugin/install.ts`:

    1. Identify every `notifyError(ctx, ...)` call (not `notifyUsageError`). For each, classify:
       - Entity-shape (e.g., "Plugin not found in marketplace", "already installed", "marketplace does not exist"): MIGRATE to compact-line via `EntityErrorRow`.
       - Generic error (e.g., I/O failure, lock contention): KEEP as `notifyError(ctx, message, err)` -- the cause-chain auto-appends.

    2. For each entity-shape error, replace:
       ```
       notifyError(opts.ctx, `Plugin "${plugin}" not found in marketplace "${marketplace}".`);
       ```
       with:
       ```
       const probe = softDepStatus(opts.pi);
       const errorRow: EntityErrorRow = {
         kind: "entity-error",
         name: plugin,
         marketplace,
         scope,
         status: "failed",
         reasons: ["not in manifest"],  // verify in shared/grammar/reasons.ts
       };
       notifyError(opts.ctx, renderRow(errorRow, probe));
       ```

    3. The set of entity-shape errors to migrate (per PRD §5.2.1):
       - PI-3 `Plugin "<name>" not found in marketplace "<mp>"` → `reasons: ["not in manifest"]`
       - PI-4 `Plugin "<name>" is not installable: <notes>` → `reasons: ["not installable"]` (verify in closed Reason set)
       - PI-5 `already installed` → `reasons: ["already installed"]`
       - PI-6 cross-plugin conflict → KEEP sentence-form (it's a multi-name aggregation, not a single-entity error -- MSG-NC-1 specifies single-entity compact form)
       - PI-1 invalid `<plugin>@<marketplace>` token → USAGE ERROR (parsing failure) -- stays `notifyUsageError`.

    4. Verify each mapped Reason exists in `shared/grammar/reasons.ts:34-58`. If `"not installable"` is not in the closed set, surface as a planning gap and either escalate or use the closest closed-set Reason.

    5. Keep `notifyUsageError(ctx, message, usageBlock)` calls UNCHANGED -- argument parsing failures retain sentence form per MSG-NC-2 / MSG-SR-7. Verify with a grep that `notifyUsageError` callsites in this file emit usage-block-shaped error text (not compact lines).

    Mirror the migration in `edge/handlers/plugin/uninstall.ts` for its entity-shape errors (PU-3 not-found, PU-7 foreign-content failure, etc.).
  </action>
  <verify>
    <automated>npm run check && node --test tests/edge/router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `node --test tests/edge/router.test.ts` exits 0
    - `grep -c 'EntityErrorRow' extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts` returns at least 1 (CMC-34 / MSG-NC-1 entity-shape errors via D-13-05 variant)
    - `grep -c 'EntityErrorRow' extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts` returns at least 1
    - `grep -c 'notifyUsageError' extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts` returns at least 1 (MSG-NC-2 usage-error sentence form preserved)
    - `grep -c 'kind: "entity-error"' extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts` returns at least 1
    - `grep -c 'kind: "entity-error"' extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts` returns at least 1
  </acceptance_criteria>
  <done>
    `npm run check` exits 0; existing `tests/edge/router.test.ts` (per VALIDATION.md row "CMC-34") still passes; `grep -c 'EntityErrorRow' extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts` returns at least 1; `grep -c 'notifyUsageError' extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts` returns at least 1 (usage-error path preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge handler ↔ orchestrator entity-shape errors | Entity errors at the edge layer carry user-supplied plugin/marketplace names; rendered into compact lines via `EntityErrorRow` |
| `PluginInlineUninstalledRow` ↔ MSG-SD-3 | The variant structurally has no `declaresAgents/Mcp` fields, preventing soft-dep markers on `(uninstalled)` rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Information Disclosure | Edge handler entity-shape errors | mitigate | `EntityErrorRow.reasons` constrained to `Reason` closed set; no raw exception messages leaked into the compact line; `notifyError`'s cause-chain trailer surfaces only `.message` per NFR-9 |
| T-13-12 | Tampering | MSG-SD-3 enforcement | mitigate | `PluginInlineUninstalledRow` lacks `declaresAgents/Mcp` fields structurally; TS compile fails on any attempt to add them |

NFR-9 invariant inherited from earlier plans.
</threat_model>

<verification>
- `npm run check` exits 0 (full suite)
- `grep -c 'Installed plugin\|Uninstalled plugin' extensions/pi-claude-marketplace/orchestrators/plugin/` (recursive) returns 0 (legacy sentence forms retired)
- `grep -c 'PluginInlineRow\|PluginInlineUninstalledRow' extensions/pi-claude-marketplace/orchestrators/plugin/` (recursive) returns at least 2
- `grep -c 'EntityErrorRow' extensions/pi-claude-marketplace/edge/handlers/plugin/` (recursive) returns at least 2 (install + uninstall entity errors migrated)
- `grep -c 'notifyUsageError' extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts` returns at least 1 (usage error path preserved per MSG-NC-2)
- `tests/architecture/no-legacy-markers.test.ts` exits 0 (no legacy strings re-introduced)
- ESLint allow-list passes (no banned marker imports)
</verification>

<success_criteria>
1. `orchestrators/plugin/install.ts` emits via `PluginInlineRow` for success and `renderRollbackPartial` for rollback-partial failures; the legacy `Installed plugin "..."` sentence + aggregated soft-dep trailers are gone (CMC-23 / CMC-17 / CMC-13).
2. `orchestrators/plugin/uninstall.ts` emits via `PluginInlineUninstalledRow`; soft-dep markers structurally excluded on uninstalled rows (CMC-24 / MSG-SD-3).
3. `orchestrators/plugin/bootstrap.ts` emits via `MarketplaceRow` (or delegates to `marketplace/add.ts` -- sub-wave 2c handles); `<autoupdate>` marker always present on bootstrap output (CMC-28).
4. Edge handlers (`edge/handlers/plugin/install.ts`, `edge/handlers/plugin/uninstall.ts`) emit entity-shape errors as compact `EntityErrorRow` lines per MSG-NC-1 (CMC-34 partial coverage for plugin commands; marketplace edge handlers migrate in sub-wave 2c).
5. Usage errors (`notifyUsageError`) stay sentence-form per MSG-NC-2 / MSG-SR-7 -- not touched by this plan.
6. CMC-09 / MSG-PL-4 structurally enforced: `PluginInlineRow.status` does NOT include `"upgradable"` (Plan 13-01-01's narrowing); TS compile fails if any install/uninstall/bootstrap code path tries to set it.
7. `npm run check` stays green; ESLint allow-list + static-audit pass.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-02b-01-SUMMARY.md` when done, citing CMC-09 / CMC-10 / CMC-13 / CMC-17 / CMC-23 / CMC-24 / CMC-28, the modified files, and the entity-shape vs usage-error split applied to the edge handlers.
</output>
