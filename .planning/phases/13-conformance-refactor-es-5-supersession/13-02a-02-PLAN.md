---
phase: 13-conformance-refactor-es-5-supersession
plan: 02a-02
type: execute
wave: 2
depends_on:
  - 13-02a-01
  - 13-01-02
  - 13-01-03
files_modified:
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/bridges/skills/stage.ts
  - extensions/pi-claude-marketplace/bridges/commands/stage.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/transaction/rollback.ts
  - tests/transaction/rollback.test.ts
  - tests/architecture/no-legacy-markers.test.ts
  - eslint.config.js
autonomous: true
requirements:
  - CMC-16
  - CMC-17
  - CMC-11
  - CMC-15
tags:
  - manual-recovery
  - rollback-partial
  - MSG-MR-1
  - MSG-MR-2
  - MSG-RP-1
  - CMC-16
  - CMC-17
  - ES-5-supersession

must_haves:
  truths:
    - "Gate A: `grep -rn 'MANUAL_RECOVERY_REQUIRED\\|MANUAL RECOVERY REQUIRED' extensions/ tests/` returns matches ONLY in `extensions/pi-claude-marketplace/shared/markers.ts` (export definition), `tests/architecture/markers-snapshot.test.ts` (snapshot fixture row), and `tests/architecture/no-legacy-markers.test.ts` (the pinned LEGACY_MARKER_STRINGS audit list). Zero matches in any other production or test file."
    - "Gate B: `grep -rn 'ROLLBACK_PARTIAL\\|(rollback partial:' extensions/ tests/` returns matches ONLY in `extensions/pi-claude-marketplace/shared/markers.ts` (export definition), `tests/architecture/markers-snapshot.test.ts` (snapshot fixture row), `tests/architecture/no-legacy-markers.test.ts` (audit list), `extensions/pi-claude-marketplace/transaction/phase-ledger.ts` (header docstring only -- still allow-listed), and `extensions/pi-claude-marketplace/transaction/rollback.ts` (header docstring only after rewrite). Zero matches in any other emission site."
    - "Gate C: `npm run check` exits 0 -- typecheck + ESLint + Prettier + full test suite all green (1121+/1121+ tests)."
    - "Gate D: ESLint `no-restricted-imports` allow-list entries for `bridges/{agents,skills,commands}/stage.ts`, `orchestrators/plugin/reinstall.ts`, and `transaction/rollback.ts` are REMOVED from `eslint.config.js` BLOCK E `ignores`; the tests/** allow-list entry for `tests/transaction/rollback.test.ts` in BLOCK E-2 is also REMOVED. After removal, the ESLint gate independently proves no `MANUAL_RECOVERY_REQUIRED` / `ROLLBACK_PARTIAL` import survives."
    - "Gate E: User-visible rendered output for the migrated reinstall failure cascade path (the `outcomeToCascadeRow` branch that previously consumed `narrowReasons` matching `MANUAL RECOVERY REQUIRED`) still produces `⊘ <name> [<scope>] (failed) {rollback partial}` at the catalog-conformant compact-line shape, AND `tests/architecture/catalog-uat.test.ts` continues to pass byte-for-byte."
    - "Gate F: After this plan lands, deleting the `MANUAL_RECOVERY_REQUIRED` and `ROLLBACK_PARTIAL` exports from `extensions/pi-claude-marketplace/shared/markers.ts` would surface typecheck/lint failures ONLY inside `shared/markers.ts` itself, `tests/architecture/markers-snapshot.test.ts`, and `tests/architecture/no-legacy-markers.test.ts` (which doesn't import from markers.ts -- pins literals locally). NO production .ts file under `extensions/pi-claude-marketplace/` retains a `MANUAL_RECOVERY_REQUIRED` or `ROLLBACK_PARTIAL` import. `13-03-02`'s scope reduces to constants + snapshot + PRD + ESLint cleanup, with zero production callsite touches required."
    - "Existing 7 D-03 contract tests in `tests/transaction/rollback.test.ts` are migrated (not deleted) -- the test count is preserved at >= 7; each assertion now targets the new structural / rendered output shape rather than the literal `ROLLBACK_PARTIAL` marker prefix."
    - "PathContainmentError / SymlinkRefusedError bypass at `transaction/rollback.ts::formatRollbackError` line 42 is preserved exactly -- `formatRollbackError(result, original)` returns the original error reference unchanged (`assert.strictEqual(got, original)`) when `original instanceof PathContainmentError`."
    - "ES-4 Error.cause chain at `transaction/rollback.ts::formatRollbackError` is preserved -- when a new Error is constructed, `new Error(..., { cause: originalError })` is set so downstream `notifyError`'s depth-5 cause-chain walk can traverse."
    - "Binding regression guard (F-2): `tests/edge/handlers/plugin/reinstall.test.ts` contains an integration test that drives `reinstallPlugin` (or `reinstallPlugins`) through a forced `ManualRecoveryError` fixture and asserts the cascade row body matches `/⊘ <name> \\[<scope>\\] \\(failed\\) \\{rollback partial\\}/` byte-for-byte. ALSO the existing line-225 assertion is strengthened from `/⊘ hello \\[project\\] \\(failed\\)/` to require the `{rollback partial}` Reason block. Without these, the structural pivot in `narrowReason` could silently drop or mis-map the closed-set Reason and the existing tests would still pass (the catalog UAT is fixture-driven on PluginInlineRow/PluginCascadeRow values, NOT through the reinstall.ts → outcomeToCascadeRow → renderRow path)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/errors.ts"
      provides: "ManualRecoveryError class (NEW): readonly typed Error subclass carrying `leaks: readonly string[]` for bridge-replacement rollback leak aggregation. Mirrors the PluginUpdatePhase3Error precedent at lines 258-265 (readonly typed payload + Error.cause via super)."
      contains: "export class ManualRecoveryError"
    - path: "extensions/pi-claude-marketplace/bridges/skills/stage.ts"
      provides: "Throws ManualRecoveryError instead of marker-prefixed Error when rollback leaks; MANUAL_RECOVERY_REQUIRED import removed."
    - path: "extensions/pi-claude-marketplace/bridges/commands/stage.ts"
      provides: "Same migration as skills/stage.ts."
    - path: "extensions/pi-claude-marketplace/bridges/agents/stage.ts"
      provides: "Same migration as skills/stage.ts."
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts"
      provides: "`errorWithManualRecovery` helper rewritten to construct a ManualRecoveryError (chaining `cause`); the 3 call sites at ~line 638, ~731, ~756 unchanged in their throw shape; `narrowReason` substring branch at line 574 replaced with structural `instanceof ManualRecoveryError` (or cause-chain check) so cascade-row mapping no longer parses legacy text; MANUAL_RECOVERY_REQUIRED import removed. The merge of leaks across bridge + orchestrator-source paths is dedup-safe -- either via `[...new Set([...err.leaks, ...leaks])]` with a unit-test assertion, OR documented inline as structurally disjoint by phase prefix (per F-5 tightening). Pick whichever costs less; either form is acceptable so long as the no-double-count invariant is provably true."
    - path: "extensions/pi-claude-marketplace/transaction/rollback.ts"
      provides: "`formatRollbackError` rewritten to compose its user-visible message using the SAME closed-set tokens (`(failed) {rollback partial}` parent + `[<phase>] (rollback failed) {rollback partial}` indented children) that `renderRollbackPartial` produces, hand-composed inline because the transaction layer does not own a plugin row context. Closed-set CMC-11 Reasons preserved; free-text `msg` fields surface only via ES-4 `Error.cause` chain (not in the rendered child rows themselves, mirroring `orchestrators/plugin/install.ts:802-839` `composeRollbackPartialBody` precedent which intentionally drops `p.msg` from child rows for the same reason). PathContainmentError / SymlinkRefusedError bypass preserved verbatim. ES-4 `cause: originalError` chain preserved verbatim. ROLLBACK_PARTIAL import removed. Header docstring rewritten to point at the new composer; the `(rollback partial: ...)` string is removed from the docstring so the no-legacy-markers.test.ts allow-list entry can be removed in Task 3."
    - path: "tests/transaction/rollback.test.ts"
      provides: "7 D-03 contract tests migrated: assertions on `ROLLBACK_PARTIAL` literal-prefix presence replaced with assertions on the new rendered shape (parent `(failed) {rollback partial}` + indented children `[<phase>] (rollback failed) {rollback partial}`). The 1-partial test asserts `got.message.includes(\"[p1] (rollback failed) {rollback partial}\")` AND `!got.message.toLowerCase().includes(\"reason\")` (F-4 tightening -- drops ambiguity). PathContainmentError + SymlinkRefusedError bypass tests preserved structurally (the `assert.strictEqual(got, original)` references are unchanged; only the absent-marker negative assertion is rephrased). ROLLBACK_PARTIAL import removed."
    - path: "tests/architecture/no-legacy-markers.test.ts"
      provides: "ALLOW_LIST shrunk: `extensions/pi-claude-marketplace/transaction/rollback.ts` (currently line 67) and `tests/transaction/rollback.test.ts` (currently line 69) entries DELETED after Task 3 rewrites the corresponding docstrings to drop the legacy literal. The three canonical entries (`shared/markers.ts`, `tests/architecture/markers-snapshot.test.ts`, `tests/architecture/no-legacy-markers.test.ts`) PRESERVED. The `transaction/phase-ledger.ts` entry (currently line 68) STAYS -- that header docstring is out of scope for this plan per `<scope_boundary>`. The file-top rationale comment block (lines 18-37) is updated to reflect the shrunk set; stale historical notes (e.g. 'Removed in sub-wave 2b' / 'Removed in sub-wave 2c') are pruned or rewritten if they no longer apply (F-8 hygiene)."
    - path: "eslint.config.js"
      provides: "BLOCK E `ignores` entries DELETED for the 3 bridges + reinstall.ts + transaction/rollback.ts (lines 297-301). BLOCK E-2 `ignores` entry DELETED for `tests/transaction/rollback.test.ts` (line 392). The marker-name restriction `paths[]` entries themselves are PRESERVED (they protect the rest of the codebase). The Phase 13 D-13-09 surrounding comments are updated to reflect that Wave 2 sub-wave 2a continuation has completed."
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/{skills,commands,agents}/stage.ts"
      to: "extensions/pi-claude-marketplace/shared/errors.ts::ManualRecoveryError"
      via: "throw new ManualRecoveryError(message, leaks, { cause: err })"
      pattern: "new ManualRecoveryError"
    - from: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts::narrowReason"
      to: "extensions/pi-claude-marketplace/shared/errors.ts::ManualRecoveryError"
      via: "structural check on outcome notes carrying a serialized ManualRecoveryError shape, or refactor narrowReasons to receive structural input"
      pattern: "ManualRecoveryError|instanceof"
    - from: "extensions/pi-claude-marketplace/transaction/rollback.ts::formatRollbackError"
      to: "extensions/pi-claude-marketplace/transaction/rollback.ts (hand-composed inline message body)"
      via: "hand-composed inline using the SAME closed-set tokens that renderRollbackPartial produces -- `(failed) {rollback partial}` parent + `[<phase>] (rollback failed) {rollback partial}` indented children -- mirroring the orchestrators/plugin/install.ts:802-839 composeRollbackPartialBody precedent. NOT a direct call to renderRollbackPartial (rejected because that composer requires a plugin row the transaction layer cannot own -- no plugin name / marketplace / scope context at this layer). The structured RollbackPartial[] data is propagated via Error.cause so the upstream notify chokepoint can render the ES-4 trailer."
      pattern: "\\(failed\\) \\{rollback partial\\}|\\[.*\\] \\(rollback failed\\) \\{rollback partial\\}"
    - from: "tests/architecture/no-legacy-markers.test.ts::ALLOW_LIST"
      to: "extensions/pi-claude-marketplace/transaction/rollback.ts + tests/transaction/rollback.test.ts (allow-list entries removed)"
      via: "Task 3 rewrites the docstrings to drop the legacy literal; allow-list entries are then removed so the CMC-35 gate binds tighter"
      pattern: "transaction/rollback.ts|tests/transaction/rollback.test.ts"
    - from: "tests/architecture/catalog-uat.test.ts"
      to: "this plan's migrated callsites"
      via: "byte-equality of rendered output against docs/output-catalog.md fixtures (NOTE: the catalog UAT is fixture-driven on PluginInlineRow / PluginCascadeRow values, NOT through the reinstall.ts → outcomeToCascadeRow → renderRow path; the F-2 binding integration test in tests/edge/handlers/plugin/reinstall.test.ts is the binding regression guard for the migrated chokepoint)"
      pattern: "catalog-state:"
---

<objective>
Wave 2 sub-wave 2a continuation: close the 6 legacy ES-5 marker emission callsites that Plan 13-02a-01 deliberately deferred. The cascade-summary migration (13-02a-01) handled the orchestrator-level rendering for reinstall / update / import; this plan handles the lower-level error-construction sites that those orchestrators consume.

Three structural moves:
1. **Bridges -> structured Error.** The 3 bridge `stage.ts` files (`skills`, `commands`, `agents`) currently throw `new Error(\`...${MANUAL_RECOVERY_REQUIRED}${leaks}\`)` when rollback-replacement leaks occur. Replace with a new `ManualRecoveryError` class (typed `leaks: readonly string[]` payload + `cause: originalError`) defined in `shared/errors.ts`, mirroring the `PluginUpdatePhase3Error` precedent at lines 258-265. Bridges throw the structured Error; the marker prefix is dropped from the message text.
2. **Reinstall consumer migration.** The `errorWithManualRecovery` helper in `orchestrators/plugin/reinstall.ts` (lines 944-957) constructs the marker-prefixed Error today. Rewrite it to construct (or rethrow) a `ManualRecoveryError` carrying the merged leak set. The legacy substring reader at `narrowReason` line 574 (`note.includes("MANUAL RECOVERY REQUIRED") || note.includes("manual recovery")`) is replaced with a structural check against the new Error type so the cascade-row mapping continues to produce `(failed) {rollback partial}` rows for the same underlying conditions. A binding integration test in `tests/edge/handlers/plugin/reinstall.test.ts` (F-2 binding regression guard) AND a strengthening of the existing line-225 regex make the structural pivot Reason-drop-safe.
3. **Rollback chokepoint migration.** The D-03 single-chokepoint `formatRollbackError` in `transaction/rollback.ts` (lines 41-54) currently composes `(rollback partial: [<phase>] <msg>; ...)` via string concatenation. Rewrite to hand-compose its user-visible message INLINE using the SAME closed-set tokens that `renderRollbackPartial` produces (`(failed) {rollback partial}` parent + `[<phase>] (rollback failed) {rollback partial}` indented children). Direct delegation to `renderRollbackPartial` is REJECTED because that composer requires a plugin row the transaction layer cannot own. The hand-composed approach mirrors the `orchestrators/plugin/install.ts:802-839` `composeRollbackPartialBody` precedent (which itself intentionally drops `p.msg` from child rows because `msg` is not in the closed REASONS set; the phaseLabel + status pair carries the user-visible failure shape; free-text msg surfaces via Error.cause's depth-5 trailer at the notify boundary). PathContainmentError / SymlinkRefusedError bypass and ES-4 `cause: originalError` chain are preserved verbatim. The 7 D-03 contract tests in `tests/transaction/rollback.test.ts` are migrated to assert the new rendered shape; the architecture-test allow-list shrinks by 2 entries in lockstep.

Purpose: After this plan lands, the 5 marker exports in `shared/markers.ts` (specifically `MANUAL_RECOVERY_REQUIRED` and `ROLLBACK_PARTIAL`) have zero remaining importers outside `shared/markers.ts` itself and the architecture-test fixtures. Plan `13-03-02` (the ES-5 atomic three-file commit) becomes structurally landable: its scope reduces to deleting the constants + snapshot rows + ESLint marker-restriction entries + rewriting PRD §6.12, with NO production callsite touches required.

Output: 6 callsite-migration edits + 1 new Error class + 1 architecture-test allow-list cleanup + 1 ESLint allow-list cleanup committed across 4 atomic Conventional Commits (one per task).

Autonomous: true. No checkpoints. The migration is pure refactor: rendered output is byte-equivalent at the catalog-conformance level (verified by `tests/architecture/catalog-uat.test.ts`), and the existing test suite (modulo the 7 D-03 contract tests, which are migrated in lockstep with the chokepoint) gates correctness. The F-2 binding integration test additionally gates the reinstall.ts → outcomeToCascadeRow → renderRow path that catalog UAT does NOT exercise.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-VALIDATION.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-02a-01-SUMMARY.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-01-02-PLAN.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-01-03-PLAN.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
@extensions/pi-claude-marketplace/presentation/manual-recovery.ts
@extensions/pi-claude-marketplace/presentation/rollback-partial.ts
@extensions/pi-claude-marketplace/presentation/compact-line.ts
@extensions/pi-claude-marketplace/presentation/index.ts
@extensions/pi-claude-marketplace/shared/markers.ts
@extensions/pi-claude-marketplace/shared/grammar/reasons.ts
@extensions/pi-claude-marketplace/shared/errors.ts
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/update.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From `extensions/pi-claude-marketplace/shared/errors.ts:258-265` (precedent: PluginUpdatePhase3Error -- readonly typed payload + Error.cause via super):
```typescript
export class PluginUpdatePhase3Error extends Error {
  readonly failures: readonly Phase3Failure[];
  constructor(message: string, failures: readonly Phase3Failure[], options?: ErrorOptions) {
    super(message, options);
    this.name = "PluginUpdatePhase3Error";
    this.failures = failures;
  }
}
```

From `extensions/pi-claude-marketplace/presentation/manual-recovery.ts:27-36`:
```typescript
export function renderManualRecovery(line: ManualRecoveryLine, probe: SoftDepProbe): string;
// line: { kind: "manual-recovery"; resource: string; reasons: readonly Reason[]; orphanDetails?: readonly string[] }
// Returns: `${headCompactLine}` or `${headCompactLine}\n${twoSpaceIndentedDetails}` if details present.
```

From `extensions/pi-claude-marketplace/presentation/rollback-partial.ts:34-51`:
```typescript
export function renderRollbackPartial(
  parent: PluginInlineRow | PluginCascadeRow,
  children: readonly RollbackChild[],
  probe: SoftDepProbe,
): string;
// Returns: `${renderRow(parent, probe)}` or `${parentLine}\n${twoSpaceIndentedChildren}`.
```

From `extensions/pi-claude-marketplace/presentation/compact-line.ts:226-231` (RollbackChild shape -- used by formatRollbackError migration):
```typescript
export interface RollbackChild {
  readonly kind: "rollback-child";
  readonly phaseLabel: string;        // e.g. "[skills/prompts]", "[agents]"
  readonly status: Extract<StatusToken, "failed" | "rollback failed">;
  readonly reasons: readonly Reason[]; // closed-set CMC-11; "rollback partial" is the canonical
}
```

From `extensions/pi-claude-marketplace/shared/grammar/reasons.ts:46-77` (closed REASONS set -- "rollback partial" is the canonical for parent + children):
```typescript
export const REASONS = [..., "rollback partial", ...] as const;
export type Reason = (typeof REASONS)[number];
```

From `extensions/pi-claude-marketplace/transaction/phase-ledger.ts` (the input shape `formatRollbackError` consumes):
```typescript
export interface RunPhasesResult {
  readonly ok: boolean;
  readonly error: Error;
  readonly rollbackPartials: readonly { readonly phase: string; readonly msg: string }[];
  readonly leaks: readonly string[];
}
```

From `extensions/pi-claude-marketplace/orchestrators/plugin/install.ts:802-839` (BINDING PRECEDENT for the Task 3 hand-composed inline pattern -- `formatRollbackError` follows the same `(failed) {rollback partial}` parent + `[<phase>] (rollback failed) {rollback partial}` children token shape, INLINE, without delegating to `renderRollbackPartial` because the transaction layer does not own a plugin row context):
```typescript
function composeRollbackPartialBody(args: {
  plugin: string;
  marketplace: string;
  scope: Scope;
  version: string | undefined;
  declaresAgents: boolean;
  declaresMcp: boolean;
  partials: readonly RollbackPartial[];
  probe: SoftDepProbe;
}): string {
  const parent: PluginInlineRow = {
    kind: "plugin-inline",
    name: args.plugin,
    marketplace: args.marketplace,
    scope: args.scope,
    ...(args.version !== undefined && args.version !== "" && { version: args.version }),
    status: "failed",
    reasons: ["rollback partial"] as const,
    declaresAgents: args.declaresAgents,
    declaresMcp: args.declaresMcp,
  };
  const children: readonly RollbackChild[] = args.partials.map((p) => ({
    kind: "rollback-child",
    phaseLabel: `[${p.phase}]`,
    status: "rollback failed",
    // The free-text `p.msg` is not in the closed REASONS set; narrow to the
    // canonical parent reason so the child stays inside CMC-11. The
    // phaseLabel + status pair carries the user-visible failure shape.
    reasons: ["rollback partial"] as const,
  }));
  return renderRollbackPartial(parent, children, args.probe);
}
```

NOTE FOR EXECUTOR (Task 3): `composeRollbackPartialBody` calls `renderRollbackPartial` because the orchestrator layer DOES own a plugin row context. The transaction layer does NOT (no plugin name / marketplace / scope), so Task 3 hand-composes the same TOKEN shape inline without invoking `renderRollbackPartial` directly. The closed-set tokens are reused verbatim; only the rendering path differs.

From `extensions/pi-claude-marketplace/orchestrators/plugin/update.ts:893-928` (REFERENCE: spliceRollbackPartials pattern for cascade-context -- NOT used in transaction/rollback.ts since rollback.ts produces a single-line message, not a cascade).

From `eslint.config.js:297-301` (BLOCK E `ignores` allow-list entries to REMOVE):
```javascript
"extensions/pi-claude-marketplace/bridges/agents/stage.ts",
"extensions/pi-claude-marketplace/bridges/skills/stage.ts",
"extensions/pi-claude-marketplace/bridges/commands/stage.ts",
"extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts",
"extensions/pi-claude-marketplace/transaction/rollback.ts",
```

From `eslint.config.js:392` (BLOCK E-2 `ignores` allow-list entry to REMOVE):
```javascript
"tests/transaction/rollback.test.ts",
```

From `tests/architecture/no-legacy-markers.test.ts:59-70` (current ALLOW_LIST shape -- 6 entries; Task 3 removes 2):
```typescript
const ALLOW_LIST: ReadonlySet<string> = new Set([
  // Canonical sources (until Wave 3 atomic deletion):
  "extensions/pi-claude-marketplace/shared/markers.ts",
  "tests/architecture/markers-snapshot.test.ts",
  "tests/architecture/no-legacy-markers.test.ts",
  // Legitimate header-docstring mentions (D-03 / PI-14 / AS-4 chokepoint
  // contract). Wave 2 sub-wave 2a migrates the production chokepoint; the
  // header docstrings get rewritten or moved at that time.
  "extensions/pi-claude-marketplace/transaction/rollback.ts",          // REMOVE in Task 3
  "extensions/pi-claude-marketplace/transaction/phase-ledger.ts",       // STAYS (out of scope)
  "tests/transaction/rollback.test.ts",                                  // REMOVE in Task 3
]);
```

From `tests/edge/handlers/plugin/reinstall.test.ts:225` (existing assertion to STRENGTHEN per F-2):
```typescript
// CURRENT:
assert.match(defaultAttempt.notifications[0]?.message ?? "", /⊘ hello \[project\] \(failed\)/);
// AFTER Task 2 strengthening:
assert.match(defaultAttempt.notifications[0]?.message ?? "", /⊘ hello \[project\] \(failed\) \{rollback partial\}/);
```
</interfaces>

<intermediate_state>
NOTE TO EXECUTOR -- interim commits between Task 1 and Task 4 leave the
codebase in a transient state where some tests assert the new shape and
some still assert the legacy shape. The user-visible cause-chain
trailer text ALSO differs between the Task 1 and Task 2 commit
boundaries: Task 1 removes the `MANUAL RECOVERY REQUIRED:` prefix from
the bridge throw site but `errorWithManualRecovery` still re-adds it
at the orchestrator wrapper, so the net trailer text is unchanged at
the Task 1 boundary. Task 2 then drops the wrapper-level prefix
entirely, at which point the trailer text shape DOES change
user-visibly. **Do NOT deploy or release at the Task 1 boundary OR at
the Task 2 boundary** -- the plan delivers a single atomic conformance
pass; intermediate boundaries are for internal CI verification only.
(F-6 release-safety tightening.)
</intermediate_state>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ManualRecoveryError and migrate the 3 bridge stage.ts callsites</name>
  <files>
    extensions/pi-claude-marketplace/shared/errors.ts,
    extensions/pi-claude-marketplace/bridges/skills/stage.ts,
    extensions/pi-claude-marketplace/bridges/commands/stage.ts,
    extensions/pi-claude-marketplace/bridges/agents/stage.ts,
    tests/shared/errors.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/errors.ts (full file; PluginUpdatePhase3Error at lines 258-265 is the binding precedent for readonly typed payload + super-options cause chain)
    - extensions/pi-claude-marketplace/bridges/skills/stage.ts (read lines 25-50 for imports and lines 290-310 for the throw site at line 298)
    - extensions/pi-claude-marketplace/bridges/commands/stage.ts (read lines 28-50 for imports and lines 280-300 for the throw site at line 287)
    - extensions/pi-claude-marketplace/bridges/agents/stage.ts (read lines 33-55 for imports and lines 445-465 for the throw site at line 455)
    - tests/shared/errors.test.ts (if exists; find existing class-error test patterns to mirror) -- if file does not exist, create alongside the new class
    - tests/bridges/agents/stage.test.ts (the test at lines 792-825 documents the no-leak-rethrow expectation; verify it does not assert on the legacy MANUAL_RECOVERY_REQUIRED prefix -- the comment at line 797 mentions the absence, not the presence)
  </read_first>
  <behavior>
    Test plan (RED first; then GREEN):
    - Test 1: `new ManualRecoveryError("staging failed", ["agents: leak A", "skills: leak B"]).message === "staging failed"` -- the marker prefix is NOT embedded in the message text; payload lives structurally on the `leaks` field.
    - Test 2: `new ManualRecoveryError("base", ["x"], { cause: rootErr }).cause === rootErr` -- ErrorOptions cause-chain works per the PluginUpdatePhase3Error precedent.
    - Test 3: `new ManualRecoveryError("m", ["x"]).name === "ManualRecoveryError"` -- the name field is set so downstream `instanceof` and structural type-tag checks work.
    - Test 4: `new ManualRecoveryError("m", ["a", "b"]).leaks` returns a readonly array equal to `["a", "b"]` (defensively `Object.freeze`d or `readonly` from the type).
    - Test 5: `err instanceof ManualRecoveryError` returns true; `err instanceof Error` returns true.
    - Test 6: For each of the 3 bridges (skills, commands, agents), test that when `rollback{Bridge}ReplacementInternal` returns a non-empty `leaks` array, the catch block throws a `ManualRecoveryError` whose `.leaks` array contains the expected per-bridge phase-prefixed leak strings AND whose `.cause` is the original triggering `err`. The bridge's existing per-bridge test fixtures (e.g. `tests/bridges/skills/stage.test.ts`) likely already cover the no-leak / leak branches; add at least one targeted assertion per bridge confirming `instanceof ManualRecoveryError` on the leak branch.
  </behavior>
  <action>
    Step 1 -- Add `ManualRecoveryError` class to `extensions/pi-claude-marketplace/shared/errors.ts`. Mirror the `PluginUpdatePhase3Error` shape (lines 258-265): `export class ManualRecoveryError extends Error` with `readonly leaks: readonly string[]` field and `constructor(message: string, leaks: readonly string[], options?: ErrorOptions)` that calls `super(message, options)` and sets `this.name = "ManualRecoveryError"` and `this.leaks = leaks`. Place the new class adjacent to `PluginUpdatePhase3Error` (around line 258) so the related-error grouping stays cohesive. Document at the class header why the marker prefix is NOT embedded in the message: per CMC-16 / MSG-MR-1..2 the manual-recovery anchor is composed at the notify boundary via `renderManualRecovery`; the bridges produce STRUCTURED data, not pre-formatted message text. Reference the consumer migration in `orchestrators/plugin/reinstall.ts::narrowReason` (which will type-check the Error instead of substring-matching the message).

    Step 2 -- Write the unit tests described in `<behavior>` (Tests 1-5) in `tests/shared/errors.test.ts` (create if absent; otherwise append a `describe`-block-equivalent grouping of `test()` calls). Run them. They MUST initially FAIL (RED) -- the class does not exist when the tests are written first. Then verify they PASS after Step 1's class definition lands (GREEN). Use `node --test tests/shared/errors.test.ts` for the targeted run.

    Step 3 -- Migrate `extensions/pi-claude-marketplace/bridges/skills/stage.ts`:
    - Line 30: REMOVE the `import { MANUAL_RECOVERY_REQUIRED } from "../../shared/markers.ts";` line.
    - Add an import: `import { ManualRecoveryError } from "../../shared/errors.ts";` (consolidate with existing `../../shared/errors.ts` imports if any present).
    - Line 298: REPLACE the `throw new Error(\`${errorMessage(err)} ${MANUAL_RECOVERY_REQUIRED}${leaks.join("; ")}\`, { cause: err });` with `throw new ManualRecoveryError(errorMessage(err), leaks, { cause: err });`. The structural payload (`leaks` as a readonly array) supersedes the joined-string message text.

    Step 4 -- Migrate `extensions/pi-claude-marketplace/bridges/commands/stage.ts` (identically to Step 3): drop import at line 33, add `ManualRecoveryError` import from `../../shared/errors.ts`, rewrite throw at line 287.

    Step 5 -- Migrate `extensions/pi-claude-marketplace/bridges/agents/stage.ts` (identically): drop import at line 38, add `ManualRecoveryError` import from `../../shared/errors.ts`, rewrite throw at line 455.

    Step 6 -- Add Test 6 from `<behavior>` for at least one bridge (agents/stage.test.ts is the most thorough fixture per the file's docstring; verify which existing test exercises the leak path and add `assert.ok(err instanceof ManualRecoveryError)` and `assert.deepEqual(err.leaks, [...])` assertions). If existing bridge tests assert on `err.message.includes("MANUAL RECOVERY REQUIRED")` (per the grep result they do NOT -- verify), migrate those assertions to the structural form.

    Step 7 -- Verify with `node --test tests/shared/errors.test.ts tests/bridges/skills/stage.test.ts tests/bridges/commands/stage.test.ts tests/bridges/agents/stage.test.ts`. ALL must pass.

    Step 8 -- Commit per CLAUDE.md: run `pre-commit run --files extensions/pi-claude-marketplace/shared/errors.ts extensions/pi-claude-marketplace/bridges/skills/stage.ts extensions/pi-claude-marketplace/bridges/commands/stage.ts extensions/pi-claude-marketplace/bridges/agents/stage.ts tests/shared/errors.test.ts tests/bridges/skills/stage.test.ts tests/bridges/commands/stage.test.ts tests/bridges/agents/stage.test.ts`, then `SKIP=trufflehog git commit -m "feat(13-02a-02): introduce ManualRecoveryError and migrate bridge stage.ts callsites (CMC-16)"`.

    NOTE on downstream coupling AND release safety (F-6): this commit leaves `orchestrators/plugin/reinstall.ts` temporarily in a state where `errorWithManualRecovery` still composes the legacy marker prefix on the OUTER wrapping Error -- that helper is migrated in Task 2. Between Task 1 and Task 2, the user-visible Error message string emitted by `notifyError` for a leak-on-rollback scenario will lose the leading `MANUAL RECOVERY REQUIRED:` substring (the bridges no longer add it) but will still gain it back at the orchestrator wrapper level -- the existing `narrowReason` substring check at line 574 will continue to map the outcome `notes` text correctly. After Task 2 the structural type-check replaces the substring check. **Do NOT deploy or release at the Task 1 boundary -- the user-visible cause-chain trailer text differs between Task 1 and Task 2 commits.** Run `npm run check` between tasks to confirm no test regressions from the temporary intermediate state.
  </action>
  <verify>
    <automated>node --test tests/shared/errors.test.ts tests/bridges/skills/stage.test.ts tests/bridges/commands/stage.test.ts tests/bridges/agents/stage.test.ts</automated>
    <automated>npm run check</automated>
    <automated>grep -cE "MANUAL_RECOVERY_REQUIRED" extensions/pi-claude-marketplace/bridges/skills/stage.ts extensions/pi-claude-marketplace/bridges/commands/stage.ts extensions/pi-claude-marketplace/bridges/agents/stage.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/errors.test.ts` covers the 5 `ManualRecoveryError` shape tests + at least 1 bridge-level structural assertion per bridge (3 total). All pass.
    - `grep -v '^#' extensions/pi-claude-marketplace/bridges/skills/stage.ts | grep -c "MANUAL_RECOVERY_REQUIRED"` returns 0 (comment-stripped to avoid header-prose self-invalidation per planner grep gate hygiene). Same for commands/stage.ts and agents/stage.ts.
    - `grep -nE "import.*ManualRecoveryError" extensions/pi-claude-marketplace/bridges/{skills,commands,agents}/stage.ts` returns 3 matches (one per bridge).
    - `grep -nE "export class ManualRecoveryError" extensions/pi-claude-marketplace/shared/errors.ts` returns 1 match.
    - `npm run check` exits 0 (full suite green; existing 1121+ tests still pass; the temporary intermediate state of `reinstall.ts` still using the legacy marker is allowed because BLOCK E `ignores` for `orchestrators/plugin/reinstall.ts` is still in place -- removed in Task 4).
    - Task committed via Conventional Commit per CLAUDE.md.
  </acceptance_criteria>
  <done>
    ManualRecoveryError class defined; 3 bridge stage.ts callsites
    migrated; targeted bridge-level assertions added; `npm run check`
    green; committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate orchestrators/plugin/reinstall.ts errorWithManualRecovery + narrowReason; add F-2 binding integration test</name>
  <files>
    extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts,
    tests/orchestrators/plugin/reinstall.test.ts,
    tests/edge/handlers/plugin/reinstall.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts (read lines 1-100 for imports and lines 460-595 for outcomeToCascadeRow + narrowReasons + narrowReason -- the structural consumers; read lines 630-760 for the 3 `errorWithManualRecovery` call sites at lines 638, 731, 756; read lines 944-957 for the helper definition itself; read lines 165-200 for the runLockedReinstall catch block that consumes the thrown Error)
    - extensions/pi-claude-marketplace/shared/errors.ts (verify ManualRecoveryError landed in Task 1)
    - tests/orchestrators/plugin/reinstall.test.ts (find any test that asserts on `notes` containing "MANUAL RECOVERY REQUIRED" or "manual recovery" -- per Task 0 grep none exist directly, but verify before migrating)
    - tests/edge/handlers/plugin/reinstall.test.ts (FULL FILE -- especially lines 200-255 which contain the `defaultAttempt` block with the line-225 assertion that needs strengthening per F-2; ALSO inspect the existing fixture helpers `withHermeticHome`, `seedInstalledAgentPlugin`, `writeAgentPluginTree`, `makeCtx`, `makePi` to understand how to force a leak-on-rollback failure for the new integration test)
  </read_first>
  <behavior>
    Test plan (RED before GREEN):
    - Test 1: When the orchestrator's leak-aggregation path catches a `ManualRecoveryError` (thrown by a bridge from Task 1) and its rollback leaks are non-empty, the rewrapped error returned by `errorWithManualRecovery(err, leaks)` is itself a `ManualRecoveryError` whose `.leaks` contains the merged set (original err's leaks if it was a `ManualRecoveryError`, plus the new leaks from the higher-level rollback). The `.message` is the bare underlying error message -- NO `MANUAL RECOVERY REQUIRED:` prefix.
    - Test 2: When `leaks.length === 0`, `errorWithManualRecovery(err, [])` returns the original `err` unchanged (preserve current short-circuit).
    - Test 3: When a `ManualRecoveryError` reaches the outcome aggregator (the `catch (err)` at reinstall.ts line 179 or line 251), the cascade row produced by `outcomeToCascadeRow` for the resulting `failed` outcome carries `reasons: ["rollback partial"]` -- same end-user-visible cascade-row shape as before, derived structurally (not via substring text-match).
    - Test 4: `narrowReason` no longer substring-matches `"MANUAL RECOVERY REQUIRED"` / `"manual recovery"`. Instead, the cascade-row derivation receives structural input -- either via a new field on the failed outcome (e.g. `outcome.failureClass === "manual-recovery"`) populated when the catch block detects `err instanceof ManualRecoveryError`, OR via the cause-chain on the captured outcome (whichever is cleaner -- the executor decides during implementation; both forms keep the cascade output byte-identical to Task 1's output).
    - Test 5: An end-to-end test through `reinstallPlugin` with a forced leak-on-rollback failure produces a cascade row matching `/⊘ <name> \[<scope>\] \(failed\) \{rollback partial\}/` AND the notes field on the returned outcome does NOT contain the legacy `MANUAL RECOVERY REQUIRED:` substring (the message text is now structurally derived from the new error class).
    - Test 6 (NEW -- F-2 BINDING REGRESSION GUARD): In `tests/edge/handlers/plugin/reinstall.test.ts`, add an integration test that drives the reinstall edge handler through a forced leak-on-rollback fixture (use `writeFile(agentPath, "manual foreign bytes", "utf8")` to seed the foreign-content condition, then invoke `reinstallPlugin` via the handler). Assert the cascade row body matches `/⊘ <name> \[<scope>\] \(failed\) \{rollback partial\}/` BYTE-FOR-BYTE -- both the `(failed)` status token AND the `{rollback partial}` Reason block. This is the binding regression guard: catalog UAT runs fixture-driven on PluginInlineRow / PluginCascadeRow values DIRECTLY and does NOT exercise the `reinstall.ts → outcomeToCascadeRow → renderRow` path, so without this test the `narrowReason` structural pivot in Step 5 could silently drop or mis-map the `"rollback partial"` closed-set Reason while all existing tests pass.
    - Test 7 (NEW -- F-2 STRENGTHENING): The EXISTING assertion at `tests/edge/handlers/plugin/reinstall.test.ts:225` is strengthened from `/⊘ hello \[project\] \(failed\)/` to `/⊘ hello \[project\] \(failed\) \{rollback partial\}/`. This catches Reason-drop drift via the existing seeded `defaultAttempt` flow without requiring a separate fixture build-out.
    - Test 8 (NEW -- F-5 DEDUP REGRESSION GUARD): A unit test asserting that when `errorWithManualRecovery(err, leaks)` is called with `err instanceof ManualRecoveryError` carrying leaks `["agents: foo"]` and the new `leaks` argument is also `["agents: foo"]`, the resulting merged `.leaks` array has length 1 (NOT 2). Per F-5, the implementation may either dedup via `[...new Set([...err.leaks, ...leaks])]` OR document inline that bridge-source + orchestrator-source leaks are structurally disjoint by phase prefix; the test asserts the no-double-count invariant regardless of which approach the executor picks. If the executor chooses the "structurally disjoint by phase prefix" approach, this test asserts the disjointness with a constructed counterexample input and documents the disjointness invariant inline.
  </behavior>
  <action>
    Step 1 -- Read `extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` end-to-end so you understand the full Error lifecycle: bridges throw → `runLockedReinstall` catches at lines 637/730/754 and rewraps via `errorWithManualRecovery` → throws up → `reinstallPlugin` catches at line 179 → `composeErrorWithCauseChain(err)` writes `notes: [message]` → bulk path's `outcomeToCascadeRow` calls `narrowReasons(outcome.notes)` → `narrowReason` substring-matches `"MANUAL RECOVERY REQUIRED"` to return `"rollback partial"`.

    Step 2 -- Decide between two structural designs. Option (a): extend `ReinstallFailedOutcome` (in `orchestrators/types.ts`) with a discriminated `failureClass?: "manual-recovery" | undefined` field; the catch block sets it via `err instanceof ManualRecoveryError`; `outcomeToCascadeRow` reads the field directly. Option (b): keep the outcome type unchanged and have `outcomeToCascadeRow` (or its `narrowReasons` callee) accept the original error reference alongside the notes; the catch block stores the error on a transient field consumed by the renderer. **Prefer Option (a)** -- it follows the established pattern in this codebase (`PluginUpdateOutcome.phaseFailures` added in Plan 13-02a-01 for the same kind of "renderer-input via structural outcome field" motivation per 13-02a-01-SUMMARY.md key-decisions). Do NOT widen the outcome.notes free-text shape -- that is the legacy carrier this migration is retiring.

    Step 3 -- Rewrite `errorWithManualRecovery` (lines 944-957). New shape:
    - Drop `MANUAL_RECOVERY_REQUIRED` import.
    - Import `ManualRecoveryError` from `../../shared/errors.ts`.
    - Behavior: if `leaks.length === 0`, return `err instanceof Error ? err : new Error(errorMessage(err))` (preserve the short-circuit semantics).
    - If `leaks.length > 0` and `err instanceof ManualRecoveryError`, return `new ManualRecoveryError(err.message, dedupedLeaks, { cause: err })` where `dedupedLeaks` is either `[...new Set([...err.leaks, ...leaks])]` (dedup by structural string-identity -- F-5 preferred path; matches Test 8 assertion) OR -- if you can prove via comments referencing the bridge phase-naming convention that bridge-source leaks have a `<bridge-name>:` prefix and orchestrator-source leaks have a different shape -- a plain spread `[...err.leaks, ...leaks]` with an inline JSDoc explaining the disjointness invariant. Pick ONE approach; document the choice in a code comment; Test 8 binds either way.
    - If `leaks.length > 0` and `err` is any other Error / unknown, return `new ManualRecoveryError(errorMessage(err), leaks, { cause: err })`.
    - The function signature stays `(err: unknown, leaks: readonly string[]): Error` -- callers at lines 638, 731, 756 do not change.

    Step 4 -- Add `failureClass: "manual-recovery"` (or equivalent structural-marker) to `ReinstallFailedOutcome` in `orchestrators/types.ts`. Populate it in the two catch blocks at `reinstallPlugin` (line 179) and `reinstallPlugins` (line 251) when the caught `err instanceof ManualRecoveryError`. The catch blocks remain notify-boundary-aware: `notifyError` at line 185 still receives `err`, so the cause-chain trailer composition for the single-plugin error surface is unchanged; the structural field is only consumed by the cascade-row mapper.

    Step 5 -- Rewrite `narrowReason` (line 574). Remove the substring branches for `"MANUAL RECOVERY REQUIRED"` and `"manual recovery"`. The branch's behavior (returning `"rollback partial"` as the closed-set Reason) moves into `outcomeToCascadeRow` -- when the outcome's `failureClass === "manual-recovery"`, prepend `"rollback partial"` to the narrowed reasons list (deduplicating if `narrowReasons` already produced it via the residual `"rollback"` substring branch at line 582). The `"rollback"` substring branch at line 582 stays -- it covers non-manual-recovery rollback scenarios -- but the `MANUAL RECOVERY REQUIRED` branch is gone. After the edit, `narrowReason` has no remaining references to the legacy marker text.

    Step 6 -- Drop the `MANUAL_RECOVERY_REQUIRED` import at line 55 of reinstall.ts.

    Step 7 (F-2 BINDING REGRESSION GUARD -- REQUIRED) -- Add the new integration test (Test 6) to `tests/edge/handlers/plugin/reinstall.test.ts`. The test seeds an agent-plugin tree, forces the bridge to throw `ManualRecoveryError` with `leaks: [{ phase: "phase3a", msg: "foreign previous content" }]` (or the closest analog matching the existing `defaultAttempt` flow's foreign-content seeding pattern at lines 207-225), invokes the reinstall handler, and asserts BOTH:
      (a) The cascade row body matches `/⊘ <name> \[<scope>\] \(failed\) \{rollback partial\}/` byte-for-byte; AND
      (b) The notify trailer (via `defaultAttempt.notifications[0]?.message`) contains the original `foreign previous content` text via the ES-4 cause-chain trailer (assert via `notifications[0]?.message.includes("foreign previous content")` or equivalent -- confirms the structured `.leaks` payload still reaches the user-visible surface via Error.cause).
    This is the binding regression guard. Without it, the structural pivot in Step 5 could silently drop the `"rollback partial"` Reason and all existing tests would still pass.

    Step 8 (F-2 STRENGTHENING -- REQUIRED) -- Strengthen the EXISTING assertion at `tests/edge/handlers/plugin/reinstall.test.ts:225`. Change `/⊘ hello \[project\] \(failed\)/` to `/⊘ hello \[project\] \(failed\) \{rollback partial\}/` so the existing test catches Reason-drop drift via the seeded `defaultAttempt` flow. The surrounding comment block at lines 218-224 already documents the `MANUAL_RECOVERY_REQUIRED branch` → `rollback partial` mapping; update that comment to reference the new structural `failureClass === "manual-recovery"` mapping path.

    Step 9 -- Update or migrate any reinstall tests that assert on `notes` text containing the legacy marker. Per the Task 0 grep, the existing tests assert on rendered cascade-row output, not on notes text -- so the migration is structural only, no test-fixture string updates needed. Verify by running `node --test tests/orchestrators/plugin/reinstall.test.ts tests/edge/handlers/plugin/reinstall.test.ts`.

    Step 10 (F-5 DEDUP TEST -- REQUIRED) -- Add Test 8 to either `tests/orchestrators/plugin/reinstall.test.ts` or `tests/shared/errors.test.ts` (executor's choice -- wherever the `errorWithManualRecovery` helper is most naturally exercised). Assert the no-double-count invariant via a constructed counterexample input regardless of whether the executor picked the `Set`-dedup or the disjointness-by-prefix approach in Step 3.

    Step 11 -- Verify the full reinstall flow: `node --test tests/orchestrators/plugin/reinstall.test.ts tests/edge/handlers/plugin/reinstall.test.ts tests/orchestrators/plugin/reinstall.partial-leak.test.ts` (the last path is illustrative -- use whatever existing leak-scenario test fixture is in the codebase; check `tests/orchestrators/plugin/` listing).

    Step 12 -- Verify `npm run check`. The ESLint allow-list still includes `orchestrators/plugin/reinstall.ts` (removed in Task 4), so the file is permitted to NOT import legacy markers, but the import is verified gone via `grep`.

    Step 13 -- Commit per CLAUDE.md: `pre-commit run --files extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts extensions/pi-claude-marketplace/orchestrators/types.ts tests/edge/handlers/plugin/reinstall.test.ts tests/orchestrators/plugin/reinstall.test.ts && SKIP=trufflehog git commit -m "feat(13-02a-02): migrate reinstall.ts errorWithManualRecovery + narrowReason to structural ManualRecoveryError + add F-2 binding integration test (CMC-16/CMC-11)"`.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts tests/edge/handlers/plugin/reinstall.test.ts</automated>
    <automated>npm run check</automated>
    <automated>grep -v '^//' extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts | grep -cE "MANUAL_RECOVERY_REQUIRED|MANUAL RECOVERY REQUIRED"</automated>
    <automated>grep -nE "instanceof ManualRecoveryError|failureClass.*manual-recovery|ManualRecoveryError" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts</automated>
    <automated>grep -nE "\{rollback partial\}" tests/edge/handlers/plugin/reinstall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts | grep -E "MANUAL_RECOVERY_REQUIRED|MANUAL RECOVERY REQUIRED"` returns ZERO matches (comment-stripped per grep gate hygiene; both the import and the legacy substring branches in narrowReason are gone).
    - `grep -nE "import.*ManualRecoveryError" extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts` returns at least 1 match.
    - The cascade-row mapping for a failed-with-leaks reinstall produces `(failed) {rollback partial}` byte-identically to the pre-migration output (verified by `tests/edge/handlers/plugin/reinstall.test.ts` regex assertion at line 225 passing with the STRENGTHENED form `/⊘ hello \[project\] \(failed\) \{rollback partial\}/`).
    - The F-2 binding integration test exists in `tests/edge/handlers/plugin/reinstall.test.ts` and asserts BOTH the cascade row body `(failed) {rollback partial}` AND the trailer text contains the original `foreign previous content` (cause-chain ES-4) byte-for-byte.
    - The F-5 dedup/disjointness test exists and passes regardless of which merge approach the executor picks.
    - `npm run check` exits 0.
    - Task committed per CLAUDE.md.
  </acceptance_criteria>
  <done>
    reinstall.ts errorWithManualRecovery + narrowReason migrated to
    structural ManualRecoveryError; F-2 binding integration test
    added; line-225 regex strengthened to require `{rollback partial}`
    block; F-5 dedup test added; full check green; committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate transaction/rollback.ts::formatRollbackError + shrink no-legacy-markers allow-list</name>
  <files>
    extensions/pi-claude-marketplace/transaction/rollback.ts,
    tests/transaction/rollback.test.ts,
    tests/architecture/no-legacy-markers.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/transaction/rollback.ts (full file -- 55 lines; understand the D-02/PI-14 PathContainmentError bypass at line 42, the ES-4 cause chain at line 53, and the legacy marker composition at line 52)
    - tests/transaction/rollback.test.ts (full file -- 169 lines, 7 D-03 contract tests; understand exactly which assertion patterns must be migrated and which structural invariants must be preserved)
    - extensions/pi-claude-marketplace/presentation/rollback-partial.ts (the renderer this migration MIRRORS -- NOT calls directly -- verify the closed-set token shape `(failed) {rollback partial}` + `[<phase>] (rollback failed) {rollback partial}` so the hand-composed inline output matches byte-for-byte)
    - extensions/pi-claude-marketplace/presentation/compact-line.ts (lines 226-231 for RollbackChild shape; lines 93-106 for PluginInlineRow shape -- for understanding the token vocabulary the hand-composed body reproduces)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (lines 802-839 for composeRollbackPartialBody -- the BINDING PRECEDENT for the hand-composed inline pattern; the install.ts version DELEGATES to renderRollbackPartial because the orchestrator owns the plugin row context, but the TOKEN VOCABULARY and the `p.msg`-drop convention are the binding precedent this task mirrors)
    - extensions/pi-claude-marketplace/transaction/phase-ledger.ts (lines 80-90 area for the RunPhasesResult shape and the existing PathContainmentError bypass -- confirm structural alignment; the file's header docstring is OUT OF SCOPE for this plan, it stays in the no-legacy-markers ALLOW_LIST per `<scope_boundary>`)
    - docs/output-catalog.md (lines 480-490: the catalog form for `failed-with-rollback-partial` in update cascade -- this is the byte-conformant target shape for the renderer-composed message; lines 330-333 for the parent + indented children token sequence)
    - tests/architecture/no-legacy-markers.test.ts (lines 59-70 -- the current 6-entry ALLOW_LIST; Task 3 removes 2 entries: `extensions/pi-claude-marketplace/transaction/rollback.ts` at line 67 and `tests/transaction/rollback.test.ts` at line 69. ALSO the file-top rationale comment block at lines 18-37 needs updates to reflect the shrunk set per F-8)
  </read_first>
  <behavior>
    Test plan (the 7 existing D-03 tests are MIGRATED, not added; one new test for the rendered-output shape is added; total target >= 8 tests):

    Migrated assertions (REPLACE the literal `ROLLBACK_PARTIAL` prefix checks):
    - Test "empty partials returns original error unchanged" (line 24): UNCHANGED -- preserves the zero-partial fast path.
    - Test "2 partials emit ES-5 marker exactly" (line 36) → RENAME to "2 partials emit rendered rollback-partial shape": assert `got.message` contains `(failed) {rollback partial}` (the catalog parent-row token) AND contains `[skills/prompts]` AND contains `[agents]` (the bracketed phase labels). The previous assertion that the message starts with `staging failed` is PRESERVED -- that is the original error message followed by a blank-line separator and the rendered block.
    - Test "1 partial produces single-element marker (no trailing semicolon)" (line 59) → RENAME to "1 partial produces single rendered child row": assert `got.message.includes("[p1] (rollback failed) {rollback partial}")` AND `got.message.includes("(failed) {rollback partial}")` AND `!got.message.toLowerCase().includes("reason")` (the latter is the F-4 tightening -- case-insensitive guard against legacy "reason: foo" prose seeping back in). The "no semicolon" assertion is RETIRED -- the new format uses indented newlines, not semicolon-separated joins.
    - Test "ES-4 cause set to originalError" (line 73): UNCHANGED -- preserves the cause-chain contract.
    - Test "D-03 single-chokepoint: marker prefix is the imported ROLLBACK_PARTIAL constant" (line 85): REPLACED with "D-03 single-chokepoint: rendered shape is composed via hand-composed inline tokens". The new assertion: `got.message.includes("(failed) {rollback partial}")` and `got.message.includes("[p1]")` and `got.message.includes("(rollback failed) {rollback partial}")`. The `ROLLBACK_PARTIAL` import is dropped.
    - Test "PI-14 / D-02: PathContainmentError originalError bypasses rollback-partial wrapping" (line 113): UPDATED -- the strict-equal assertion to the original instance is PRESERVED. The negative assertion that the message does NOT contain `ROLLBACK_PARTIAL` is REPLACED with: message does NOT contain `(failed) {rollback partial}` (the new rendered token). Same semantic guarantee, new probe text.
    - Test "PI-14 / D-02: SymlinkRefusedError (subclass) bypasses rollback-partial wrapping" (line 139): same treatment as the PathContainmentError test.

    New test (NEW):
    - Test "rendered output is byte-equivalent to the catalog form for a 2-phase rollback": Compose a `RunPhasesResult` with 2 partials `[{phase: "skills/prompts", msg: "rm failed"}, {phase: "agents", msg: "index unreadable"}]`. Assert the rendered message body matches the catalog form at `docs/output-catalog.md` line 330-333 structurally -- the parent row uses `(failed) {rollback partial}`, the children are indented at 2 spaces, each child carries `[<phase>]` and the `(rollback failed) {rollback partial}` token. The free-text `msg` fields surface ONLY in the original error message (preserved by ES-4 cause chain), NOT in the rendered child rows themselves (per the closed-set CMC-11 narrowing; the phaseLabel + status pair carries the failure shape, free-text msg lives on Error.cause for the depth-5 trailer).

    Allow-list shrink verification:
    - After Task 3 lands, `node --test tests/architecture/no-legacy-markers.test.ts` still passes with the 2 removed entries gone. This proves the migrated `transaction/rollback.ts` + `tests/transaction/rollback.test.ts` genuinely contain no legacy literal (not just that they're allow-listed).
  </behavior>
  <action>
    Step 1 -- Read `transaction/rollback.ts` and the 7 tests in `rollback.test.ts` end-to-end. Confirm the structural invariants you must preserve: (a) `originalError instanceof PathContainmentError` short-circuits with verbatim return; (b) `result.rollbackPartials.length === 0` short-circuits with verbatim return; (c) when wrapping into a new Error, `cause: originalError` is set for ES-4 cause-chain traversal.

    Step 2 -- Design decision: how does `formatRollbackError` construct the `PluginInlineRow | PluginCascadeRow` parent that `renderRollbackPartial` requires, given that `transaction/rollback.ts` has NO plugin name / scope / marketplace context at its call site (it operates at the transaction primitive layer; the plugin context lives in the orchestrator above)? The 3 viable options:
    - **Option A:** Use an `EntityErrorRow` for the parent -- but that variant is not accepted by `renderRollbackPartial`'s type signature. Reject.
    - **Option B (CHOSEN):** Keep `transaction/rollback.ts` PRODUCING A STRING MESSAGE (as it does today), but compose it inline using the SAME closed-set tokens that `renderRollbackPartial` produces WITHOUT calling that composer directly (rejected because that composer requires a plugin row the transaction layer cannot own -- no plugin name / marketplace / scope context at this layer). Specifically: compose the message as `${originalError.message}\n\n  (failed) {rollback partial}\n${childrenJoined}` where `childrenJoined` is the per-phase child block constructed inline. This is the cleanest path because the transaction layer has no semantic right to construct a `PluginInlineRow` (which requires `name`, `marketplace`, `scope` fields the layer does not own). The closed-set CMC-11 Reasons (`"rollback partial"`) and the bracketed phase label form are reusable verbatim. The hand-composed approach mirrors the `orchestrators/plugin/install.ts:802-839` `composeRollbackPartialBody` precedent's token vocabulary AND its `p.msg`-drop convention (free-text msg surfaces via Error.cause, not in the rendered child rows themselves).
    - **Option C:** Keep the message text unchanged (so this chokepoint emits the legacy `(rollback partial: ...)` form) but read it from a NEW chokepoint constant defined in `presentation/` instead of `shared/markers.ts`. This kicks the can down the road and does NOT close the migration. Reject.

    **Implementation: Option B** -- `transaction/rollback.ts` composes the rendered text inline. The new message format matches the orchestrator-cascade form at `docs/output-catalog.md` line 330: parent body `(failed) {rollback partial}` plus indented per-phase children `[<phase>] (rollback failed) {rollback partial}` joined by newlines. The shape is hand-composed using the same closed-set tokens that `renderRollbackPartial` uses, keeping the user-visible output byte-equivalent to the orchestrator-cascade equivalent without forcing layer violations. The free-text `msg` field on each ledger partial is dropped from the rendered shape AND is preserved on the original error's message chain via the ES-4 `cause` link (the depth-5 walker in `notifyError` surfaces it).

    Step 3 -- Rewrite `formatRollbackError` body. Replacement structure:
    - Keep imports of `PathContainmentError` and `RunPhasesResult`.
    - REMOVE the import of `ROLLBACK_PARTIAL` from `../shared/markers.ts` (the file-leading import at line 15).
    - Preserve the `if (originalError instanceof PathContainmentError) { return originalError; }` short-circuit at line 42.
    - Preserve the `if (result.rollbackPartials.length === 0) { return originalError; }` short-circuit at line 46.
    - Replace lines 50-53 with the new composition: compose a body string consisting of (a) the original error message, (b) a blank-line separator, (c) the parent line `(failed) {rollback partial}` (NOT indented at the transaction layer -- the orchestrator-cascade splice happens at a higher layer if applicable), (d) the per-phase children joined by `\n`, each child formatted as `  [<phase>] (rollback failed) {rollback partial}` (2-space-indented). Return `new Error(${composedBody}, { cause: originalError })` to preserve ES-4 cause chain.
    - The `msg` field on each `RollbackPartial` is NOT embedded in the rendered child line (per CMC-11 closed-set Reasons; free-text msgs leak via Error.cause depth-5 walker -- same precedent as `composeRollbackPartialBody` in install.ts at line 802-839 which intentionally drops `p.msg` from child rows for the same reason).

    Step 4 -- Rewrite the file header docstring (lines 1-14 + lines 31-40). Remove every literal mention of `(rollback partial: ...)`; replace with references to the new rendered shape (`(failed) {rollback partial}` parent + indented `[<phase>] (rollback failed) {rollback partial}` children). Document that this chokepoint composes via the same closed-set tokens as the `renderRollbackPartial` composer but inline (without crossing layers to construct a plugin row), citing the install.ts:802-839 precedent.

    Step 5 -- Remove `extensions/pi-claude-marketplace/transaction/rollback.ts` from the ALLOW_LIST in `tests/architecture/no-legacy-markers.test.ts` (line 67). The file no longer contains the literal `(rollback partial:` string after Step 4. Verify by running `node --test tests/architecture/no-legacy-markers.test.ts` -- it MUST pass. Also update the file-top rationale comment block (lines 18-37) to remove the bullet entry for `transaction/rollback.ts` and add a short note explaining the entry was removed because Plan 13-02a-02 Task 3 migrated the chokepoint docstring to the closed-set token form. (F-8 hygiene: while in this file, ALSO scan for any stale historical notes such as "Removed in sub-wave 2b" / "Removed in sub-wave 2c" that no longer apply after this plan lands; remove or rewrite them.)

    Step 6 -- Rewrite the 7 tests in `tests/transaction/rollback.test.ts` per the `<behavior>` test plan. Drop the `ROLLBACK_PARTIAL` import at line 4. The 1-partial test explicitly asserts `got.message.includes("[p1] (rollback failed) {rollback partial}")` AND `got.message.includes("(failed) {rollback partial}")` AND `!got.message.toLowerCase().includes("reason")` (F-4 -- drop ambiguity). Drop the `tests/transaction/rollback.test.ts` allow-list entry in `tests/architecture/no-legacy-markers.test.ts` (line 69) AFTER rewriting the test file's docstring (lines 13-22) to not contain `(rollback partial: ...)`. Update the file-top rationale comment block accordingly. Add the new "rendered output is byte-equivalent" test described in `<behavior>`.

    Step 7 -- Verify: `node --test tests/transaction/rollback.test.ts tests/architecture/no-legacy-markers.test.ts tests/architecture/catalog-uat.test.ts`. ALL must pass. The catalog UAT is run as a NO-REGRESSION CHECK on the renderer fixtures -- it does NOT exercise the migrated chokepoint directly (catalog UAT is fixture-driven on `PluginInlineRow` / `PluginCascadeRow` values DIRECTLY, NOT through `formatRollbackError`'s output path). The new byte-equivalence test added in Step 6 is the BINDING verification for the migrated path. (F-7 softening -- catalog UAT is no-regression, not chokepoint-binding for this task.)

    Step 8 -- `npm run check`. Confirm 1121+/1121+ tests pass (the test count rises by 1 from the NEW byte-equivalence test).

    Step 9 -- Commit per CLAUDE.md: `pre-commit run --files extensions/pi-claude-marketplace/transaction/rollback.ts tests/transaction/rollback.test.ts tests/architecture/no-legacy-markers.test.ts && SKIP=trufflehog git commit -m "feat(13-02a-02): migrate transaction/rollback.ts formatRollbackError to hand-composed inline rollback-partial shape + shrink allow-list (CMC-17)"`.
  </action>
  <verify>
    <automated>node --test tests/transaction/rollback.test.ts</automated>
    <automated>node --test tests/architecture/no-legacy-markers.test.ts tests/architecture/catalog-uat.test.ts</automated>
    <automated>npm run check</automated>
    <automated>grep -v '^//' extensions/pi-claude-marketplace/transaction/rollback.ts | grep -cE "ROLLBACK_PARTIAL|\(rollback partial:"</automated>
    <automated>grep -v '^//' tests/transaction/rollback.test.ts | grep -cE "ROLLBACK_PARTIAL|\(rollback partial:"</automated>
    <automated>grep -c "transaction/rollback.ts\|tests/transaction/rollback.test.ts" tests/architecture/no-legacy-markers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' extensions/pi-claude-marketplace/transaction/rollback.ts | grep -E "ROLLBACK_PARTIAL|\(rollback partial:"` returns ZERO matches (comment-stripped per grep gate hygiene).
    - `grep -v '^//' tests/transaction/rollback.test.ts | grep -E "ROLLBACK_PARTIAL|\(rollback partial:"` returns ZERO matches.
    - `tests/transaction/rollback.test.ts` has >= 8 tests (7 migrated + 1 new byte-equivalence test). All pass.
    - The 1-partial test explicitly asserts `got.message.includes("[p1] (rollback failed) {rollback partial}")` AND `!got.message.toLowerCase().includes("reason")` (F-4).
    - `tests/architecture/no-legacy-markers.test.ts` ALLOW_LIST no longer contains `extensions/pi-claude-marketplace/transaction/rollback.ts` or `tests/transaction/rollback.test.ts` entries. The test still passes (proving the literals are absent, not just allow-listed). File-top rationale comment block updated to reflect the shrunk set; stale historical notes pruned (F-8).
    - PathContainmentError + SymlinkRefusedError bypass tests assert `assert.strictEqual(got, original)` (preserved unchanged structurally).
    - The new Error returned by `formatRollbackError` for a non-PathContainmentError partial-rollback has `.cause === originalError`.
    - `npm run check` exits 0.
    - Task committed per CLAUDE.md.
  </acceptance_criteria>
  <done>
    transaction/rollback.ts formatRollbackError migrated to hand-composed
    inline closed-set tokens; 7 D-03 contract tests migrated + 1 byte-
    equivalence test added; no-legacy-markers ALLOW_LIST shrunk by 2
    entries; F-4 + F-8 tightenings applied; full check green; committed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Cutover gate -- remove ESLint allow-list entries and run final verification</name>
  <files>
    eslint.config.js
  </files>
  <read_first>
    - eslint.config.js (read lines 275-360 for BLOCK E and lines 360-440 for BLOCK E-2; the allow-list entries to remove are at lines 297-301 and line 392)
    - tests/architecture/no-legacy-markers.test.ts (already updated in Task 3; verify the final state has only 4 allow-list entries: the 3 canonical entries + `transaction/phase-ledger.ts` which stays per `<scope_boundary>` -- the 2 docstring entries for `transaction/rollback.ts` + `tests/transaction/rollback.test.ts` removed by Task 3)
  </read_first>
  <action>
    Step 1 -- Edit `eslint.config.js`:
    - In BLOCK E `ignores` (around lines 297-301), DELETE the 5 lines:
      - `"extensions/pi-claude-marketplace/bridges/agents/stage.ts",`
      - `"extensions/pi-claude-marketplace/bridges/skills/stage.ts",`
      - `"extensions/pi-claude-marketplace/bridges/commands/stage.ts",`
      - `"extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts",`
      - `"extensions/pi-claude-marketplace/transaction/rollback.ts",`
    - In BLOCK E-2 `ignores` (around line 392), DELETE the line:
      - `"tests/transaction/rollback.test.ts",`
    - Update the surrounding comments at lines 286-296 and lines 374-391 to reflect that Wave 2 sub-wave 2a continuation has COMPLETED; the 5 production-callsite + 1 test-callsite allow-list entries are gone. The remaining `shared/markers.ts` and `markers-snapshot.test.ts` ignores STAY (those are the canonical export site + the snapshot test, deleted in 13-03-02). Adjust the comment text accordingly. (F-8 hygiene -- while editing the comment blocks, ALSO scan for any stale historical notes such as "Removed in sub-wave 2b" / "Removed in sub-wave 2c" around lines 374-391 that no longer apply after this plan lands; remove or rewrite them. The same hygiene pass that Task 3 performed on `tests/architecture/no-legacy-markers.test.ts` applies here for the BLOCK E-2 comments around lines 374-391.)

    Step 2 -- Verify the structural assertion that the migration is complete:
    - Run `grep -rEn "MANUAL_RECOVERY_REQUIRED|MANUAL RECOVERY REQUIRED|ROLLBACK_PARTIAL|\(rollback partial:" extensions/pi-claude-marketplace/ tests/ 2>&1 | grep -v "shared/markers.ts" | grep -v "tests/architecture/markers-snapshot.test.ts" | grep -v "tests/architecture/no-legacy-markers.test.ts" | grep -v "transaction/phase-ledger.ts"`. EXPECTED: zero remaining lines (every other match is either a header docstring in a still-allow-listed file or was already migrated).
    - If `transaction/phase-ledger.ts` still has the `(rollback partial: ...)` docstring mention and is still in the no-legacy-markers ALLOW_LIST (it is -- that entry is OUTSIDE this plan's scope per the plan's `<scope_boundary>`), it counts as expected and not an offender.

    Step 3 -- Run `npm run check`. Must exit 0. The ESLint rule is now actively enforcing zero `MANUAL_RECOVERY_REQUIRED` / `ROLLBACK_PARTIAL` imports across the 6 previously-allow-listed files; if ANY import slipped through Tasks 1-3, this is where it surfaces. If a lint error fires, return to the relevant Task (1, 2, or 3) and fix the residual import -- do NOT re-add the allow-list entry as a workaround.

    Step 4 -- Run `node --test tests/architecture/catalog-uat.test.ts` independently. This is the binding byte-equality verification that the catalog-conformant rendered shapes (in particular `failure-rollback-partial` for plugin install, `failed-with-rollback-partial` for update cascade, and `install-failure-with-anchor` for the manual-recovery anchor) are still produced byte-for-byte after the migrations. The runner reads `docs/output-catalog.md` HTML-comment `<!-- catalog-state: STATE -->` discriminators and pairs each with a renderer-output fixture; any drift introduced by Tasks 1-3 surfaces here. (NOTE per F-7: this is a no-regression check on renderer fixtures; the BINDING regression guard for the reinstall.ts chokepoint is the F-2 integration test added in Task 2 Step 7, NOT the catalog UAT.) Per Plan 13-03-02's pre-commit gate, this plan's completion is also a prerequisite for 13-03-02.

    Step 5 -- Final audit: run the 6 verification commands from `<acceptance_criteria>` Gate A, Gate B, Gate D (per the plan's must_haves.truths). Confirm all 6 gates pass cleanly. Document the gate results in the eventual SUMMARY.md.

    Step 6 -- Commit per CLAUDE.md: `pre-commit run --files eslint.config.js && SKIP=trufflehog git commit -m "feat(13-02a-02): retire ESLint allow-list entries for migrated ES-5 marker callsites (CMC-16/CMC-17)"`.
  </action>
  <verify>
    <automated>npm run check</automated>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/architecture/no-legacy-markers.test.ts</automated>
    <automated>grep -rEn "MANUAL_RECOVERY_REQUIRED|MANUAL RECOVERY REQUIRED" extensions/pi-claude-marketplace/ tests/ | grep -v "shared/markers.ts" | grep -v "tests/architecture/markers-snapshot.test.ts" | grep -v "tests/architecture/no-legacy-markers.test.ts"</automated>
    <automated>grep -rEn "ROLLBACK_PARTIAL|\(rollback partial:" extensions/pi-claude-marketplace/ tests/ | grep -v "shared/markers.ts" | grep -v "tests/architecture/markers-snapshot.test.ts" | grep -v "tests/architecture/no-legacy-markers.test.ts" | grep -v "transaction/phase-ledger.ts"</automated>
    <automated>grep -v '^//' eslint.config.js | grep -cE "bridges/agents/stage.ts|bridges/skills/stage.ts|bridges/commands/stage.ts|orchestrators/plugin/reinstall.ts|transaction/rollback.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The 5 BLOCK E `ignores` entries for `bridges/{agents,skills,commands}/stage.ts`, `orchestrators/plugin/reinstall.ts`, `transaction/rollback.ts` are DELETED from `eslint.config.js`.
    - The BLOCK E-2 `ignores` entry for `tests/transaction/rollback.test.ts` is DELETED.
    - `grep -E "MANUAL_RECOVERY_REQUIRED|MANUAL RECOVERY REQUIRED"` (Gate A) returns matches ONLY in `shared/markers.ts`, `tests/architecture/markers-snapshot.test.ts`, and `tests/architecture/no-legacy-markers.test.ts`. Zero in any other file.
    - `grep -E "ROLLBACK_PARTIAL|\(rollback partial:"` (Gate B) returns matches ONLY in `shared/markers.ts`, `tests/architecture/markers-snapshot.test.ts`, `tests/architecture/no-legacy-markers.test.ts`, and `transaction/phase-ledger.ts` (still allow-listed; out of scope per `<scope_boundary>`). Zero in `transaction/rollback.ts` or `tests/transaction/rollback.test.ts`.
    - `npm run check` exits 0 (Gate C): typecheck + ESLint + Prettier + full test suite all green.
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 (Gate E): every catalog-state byte-equality fixture passes.
    - Stale historical notes ("Removed in sub-wave 2b" / "Removed in sub-wave 2c") in BLOCK E + BLOCK E-2 comment blocks pruned or rewritten if no longer applicable (F-8).
    - Task committed per CLAUDE.md.
    - Plan 13-03-02 is now structurally unblocked (Gate F): its scope reduces to constants + snapshot + PRD + ESLint marker-restriction-rule cleanup, with NO production callsite touches required. Confirm by reading the `13-03-02-PLAN.md` `files_modified` block and noting that nothing under `extensions/pi-claude-marketplace/orchestrators/`, `extensions/pi-claude-marketplace/bridges/`, or `extensions/pi-claude-marketplace/transaction/` appears.
  </acceptance_criteria>
  <done>
    ESLint BLOCK E + BLOCK E-2 allow-list entries for the 6 migrated
    callsites deleted; stale historical-note hygiene applied;
    Gate A-F audit passes; Plan 13-03-02 structurally unblocked;
    committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge stage.ts error path → orchestrator reinstall.ts catch block | A bridge's `replace{Bridge}Replacement` failure produces an Error that crosses the layer boundary from bridges/ to orchestrators/. The Error carries leak data (paths, error messages) that must surface user-visibly. |
| orchestrator reinstall.ts → notify boundary | The orchestrator's `notifyError` call exposes `errorMessage(err)` + the depth-5 cause-chain trailer to the user. NFR-9 / T-13-05 mandate that ONLY `.message` surfaces -- no `.stack`, no absolute paths beyond what `.message` itself carries. |
| transaction/rollback.ts → orchestrator caller | `formatRollbackError` returns an Error whose message text crosses up to the orchestrator's notify call. PathContainmentError / SymlinkRefusedError must surface VERBATIM (PI-14) without being folded into the rollback-partial wrapper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02a-02-01 | Tampering (string-shape regression) | `transaction/rollback.ts::formatRollbackError` rewrite | mitigate | The 7 migrated D-03 contract tests + the new byte-equivalence test gate every structural invariant: (a) zero-partial fast path returns original instance, (b) ES-4 cause chain set, (c) PathContainmentError + SymlinkRefusedError bypass returns original instance verbatim, (d) rendered shape contains `(failed) {rollback partial}` parent + `[<phase>] (rollback failed) {rollback partial}` indented children. A regression to the legacy `(rollback partial: ...)` form would fail (d); a regression that drops the cause chain would fail (b); a regression that folds PathContainmentError would fail (c). The 1-partial test additionally asserts `!got.message.toLowerCase().includes("reason")` (F-4) so prose like "reason: foo" cannot seep back into the message body. |
| T-13-02a-02-02 | Information Disclosure (NFR-9 / T-13-05 -- leak data surfacing) | `bridges/stage.ts` → `ManualRecoveryError` carries `leaks: readonly string[]`; the orchestrator's `notifyError` cause-chain walker surfaces the original error's `.message` | mitigate by reuse | The depth-5 walker (`causeChainTrailer` from Plan 13-01-02) surfaces ONLY `.message` (or `string` verbatim / `Object.prototype.toString.call` fallback). It does NOT surface `err.leaks` directly. Operators see the same leak-path text they saw under the legacy `MANUAL RECOVERY REQUIRED: <leaks>` form because the leak paths are embedded in the original bridge error's `.message` via the legacy `errorMessage(err) ${leaks.join("; ")}` concatenation -- but the NEW design moves that concatenation into the renderer at the notify boundary (the leak set is RECONSTRUCTED from `.leaks` at render time, NOT pre-composed into the message). Net effect: same user-visible text; structural typing replaces stringly-typed text matching. NFR-9 invariant unchanged. |
| T-13-02a-02-03 | Tampering (closed-set CMC-11 regression) | `narrowReason` rewrite in `reinstall.ts` | mitigate | The structural `failureClass === "manual-recovery"` check (or equivalent -- see Task 2 Step 2 design decision) produces `"rollback partial"` as the closed-set Reason, byte-identical to the legacy substring branch's output. The cascade-row rendered shape `⊘ <name> [<scope>] (failed) {rollback partial}` is unchanged. Verified end-to-end by (a) the STRENGTHENED line-225 regex `/⊘ hello \[project\] \(failed\) \{rollback partial\}/` in `tests/edge/handlers/plugin/reinstall.test.ts` (F-2 strengthening -- Task 2 Step 8) AND (b) the NEW F-2 binding integration test added in Task 2 Step 7 that drives reinstallPlugin through a forced `ManualRecoveryError` fixture and asserts BOTH the cascade row body byte-for-byte AND the cause-chain trailer text. Catalog UAT alone is NOT sufficient -- it is fixture-driven on PluginInlineRow / PluginCascadeRow values directly and does not exercise the reinstall.ts → outcomeToCascadeRow → renderRow chokepoint. |
| T-13-02a-02-04 | Tampering (ESLint cutover gate weakening) | Removing 6 allow-list entries in `eslint.config.js` could leave the gate underspecified | mitigate | The `paths[].importNames` restriction in BLOCK E + BLOCK E-2 is PRESERVED (only the per-file `ignores` are removed). After this plan, ANY file under `extensions/pi-claude-marketplace/**` OR `tests/**` that re-imports `MANUAL_RECOVERY_REQUIRED` / `ROLLBACK_PARTIAL` fails the lint check. The cutover STRENGTHENS the gate; it does not weaken it. Verified by Gate C (`npm run check`) and the 6 grep-based verification commands in Task 4. |
| T-13-02a-02-05 | Tampering (leak double-count regression -- F-5) | `errorWithManualRecovery` merge of `err.leaks` + new `leaks` | mitigate | The F-5 dedup test added in Task 2 Step 10 binds the no-double-count invariant via a constructed counterexample input. The implementation chooses ONE of two paths: (a) `[...new Set([...err.leaks, ...leaks])]` dedup, or (b) inline JSDoc documenting that bridge-source + orchestrator-source leaks are structurally disjoint by phase prefix. Either path passes the test; the choice is documented in a code comment for future maintainers. |
| T-13-02a-02-SC | Tampering (supply chain) | npm/pip/cargo installs | N/A | This plan introduces NO new package installs. All migration is in-tree TypeScript refactor consuming Wave 1 composers and an existing `presentation/` barrel. No `Package Legitimacy Audit` rows needed. |
</threat_model>

<verification>
## Phase-Level Verification

After all 4 tasks complete:

1. **Gate A (manual-recovery cleanup):**
   ```bash
   grep -rEn "MANUAL_RECOVERY_REQUIRED|MANUAL RECOVERY REQUIRED" extensions/pi-claude-marketplace/ tests/ | \
     grep -vE "shared/markers\.ts|tests/architecture/markers-snapshot\.test\.ts|tests/architecture/no-legacy-markers\.test\.ts"
   ```
   EXPECTED: zero output lines.

2. **Gate B (rollback-partial cleanup):**
   ```bash
   grep -rEn 'ROLLBACK_PARTIAL|\(rollback partial:' extensions/pi-claude-marketplace/ tests/ | \
     grep -vE "shared/markers\.ts|tests/architecture/markers-snapshot\.test\.ts|tests/architecture/no-legacy-markers\.test\.ts|transaction/phase-ledger\.ts"
   ```
   EXPECTED: zero output lines.

3. **Gate C (full suite + lint + typecheck):**
   ```bash
   npm run check
   ```
   EXPECTED: exit 0; >= 1121 tests pass.

4. **Gate D (ESLint allow-list cleanup):**
   ```bash
   grep -v '^//' eslint.config.js | grep -cE 'bridges/agents/stage\.ts|bridges/skills/stage\.ts|bridges/commands/stage\.ts|orchestrators/plugin/reinstall\.ts|transaction/rollback\.ts|tests/transaction/rollback\.test\.ts'
   ```
   EXPECTED: zero (comment-stripped per planner grep gate hygiene).

5. **Gate E (catalog UAT byte-equality + F-2 binding integration test):**
   ```bash
   node --test tests/architecture/catalog-uat.test.ts tests/edge/handlers/plugin/reinstall.test.ts
   ```
   EXPECTED: all `<!-- catalog-state: ... -->` fixtures pair byte-equal AND the F-2 binding integration test in the reinstall edge-handler test asserts both `(failed) {rollback partial}` row body AND the cause-chain trailer byte-for-byte. The strengthened line-225 regex also passes.

6. **Gate F (13-03-02 readiness -- manual check):**
   - Read `.planning/phases/13-conformance-refactor-es-5-supersession/13-03-02-PLAN.md` `files_modified` block.
   - Confirm NO file path under `extensions/pi-claude-marketplace/orchestrators/`, `extensions/pi-claude-marketplace/bridges/`, or `extensions/pi-claude-marketplace/transaction/` (other than `shared/markers.ts`) is listed. Plan 13-03-02's scope is constants + snapshot + PRD + ESLint marker-restriction-rule cleanup ONLY.
   - Confirm structurally that deleting the `MANUAL_RECOVERY_REQUIRED` and `ROLLBACK_PARTIAL` exports from `shared/markers.ts` would compile cleanly (no production .ts importer remains). Surface this via `npx tsc --noEmit` after a dry-run deletion (do NOT actually delete in this plan -- that's 13-03-02's atomic commit).
</verification>

<success_criteria>
- The 6 callsite migrations (4 manual-recovery + 2 rollback-partial) are complete; the legacy ES-5 marker imports are gone from every emission site.
- `npm run check` exits 0 with >= 1121 tests passing (test count rises by ~8: 5 new ManualRecoveryError unit tests + 1 new byte-equivalence rollback test + 1 F-2 binding integration test + 1 F-5 dedup test; minor +/- depending on Task 1's bridge-level assertions).
- The 7 D-03 contract tests are migrated (not deleted) -- `tests/transaction/rollback.test.ts` test count is >= 8 (7 migrated + 1 new).
- `tests/architecture/catalog-uat.test.ts` passes byte-for-byte across all catalog-state fixtures (NO-REGRESSION guard on renderer fixtures; NOT the chokepoint-binding test for the reinstall path -- that's the F-2 integration test in `tests/edge/handlers/plugin/reinstall.test.ts`).
- The F-2 binding integration test exists in `tests/edge/handlers/plugin/reinstall.test.ts` and asserts BOTH the cascade row body `(failed) {rollback partial}` AND the cause-chain trailer text byte-for-byte. The line-225 regex is strengthened to require the `{rollback partial}` Reason block.
- The F-5 dedup test exists and passes regardless of which merge approach the executor picks for `errorWithManualRecovery`.
- `tests/architecture/no-legacy-markers.test.ts` ALLOW_LIST contains only 4 entries after Task 3: the 3 canonical entries (`shared/markers.ts`, `markers-snapshot.test.ts`, `no-legacy-markers.test.ts`) + `transaction/phase-ledger.ts` (out of scope; STAYS). The 2 entries for `transaction/rollback.ts` and `tests/transaction/rollback.test.ts` REMOVED.
- ESLint BLOCK E + BLOCK E-2 allow-list entries for the 6 migrated callsites are deleted; the `paths[].importNames` restriction rules themselves are preserved, providing forward-protection.
- F-1, F-2, F-3, F-4, F-5, F-6, F-7, F-8 checker findings addressed:
  - F-1: `tests/architecture/no-legacy-markers.test.ts` added to `files_modified` (count now 9).
  - F-2: New binding integration test in reinstall edge-handler + strengthened line-225 regex.
  - F-3: `must_haves.artifacts` + `key_links` for `transaction/rollback.ts` aligned with Task 3 Option B (hand-composed inline).
  - F-4: 1-partial test explicitly asserts `got.message.includes("[p1] (rollback failed) {rollback partial}")` AND `!got.message.toLowerCase().includes("reason")`.
  - F-5: Either `Set`-dedup OR documented disjointness-by-prefix; F-5 dedup test binds either way.
  - F-6: Intermediate state block carries release-safety note for Task 1 + Task 2 boundaries.
  - F-7: Catalog UAT framed as no-regression check; binding verification is the new byte-equivalence test added in Task 3 Step 6.
  - F-8: Stale historical notes ("Removed in sub-wave 2b" / "Removed in sub-wave 2c") in `no-legacy-markers.test.ts` and `eslint.config.js` comment blocks pruned or rewritten.
- Plan 13-03-02 is structurally landable: its scope reduces to constants + snapshot + PRD + ESLint marker-restriction-rule cleanup. The dry-run deletion of `MANUAL_RECOVERY_REQUIRED` and `ROLLBACK_PARTIAL` exports from `shared/markers.ts` produces typecheck/lint failures ONLY in `shared/markers.ts` itself and the architecture-test fixtures, never in production code.
- Phase 13 STATE.md gets updated from "BLOCKED at Wave 9" to "Wave 9 unblocked; 13-03-02 ready to execute" -- the 8/9 → 9/9 transition happens after 13-03-02 lands.
- 4 atomic Conventional Commits committed per CLAUDE.md (one per task), each prefixed with `SKIP=trufflehog` per the worktree workaround.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-02a-02-SUMMARY.md` when done. The SUMMARY must include:

- `tags:` frontmatter covering `manual-recovery`, `rollback-partial`, `MSG-MR-1`, `MSG-MR-2`, `MSG-RP-1`, `CMC-16`, `CMC-17`, `ES-5-supersession`.
- `requires:` frontmatter listing the dependency on 13-02a-01 (Wave 2a cascade orchestrators), 13-01-02 (Wave 1 composers -- `renderManualRecovery` + `renderRollbackPartial`), and 13-01-03 (cutover gates -- ESLint rule + no-legacy-markers audit).
- `provides:` frontmatter naming the 6 export targets: (a) `ManualRecoveryError` class in `shared/errors.ts`; (b) bridge-stage.ts structural-Error throw shape; (c) reinstall.ts `narrowReason` structural check replacing legacy substring branch; (d) `transaction/rollback.ts::formatRollbackError` hand-composed inline output shape using closed-set tokens; (e) no-legacy-markers.test.ts ALLOW_LIST shrunk by 2 entries; (f) ESLint allow-list cutover gate retirement.
- `affects:` frontmatter explicitly naming `13-03-02` as the unblocked downstream plan.
- A "Gate Results" subsection documenting the 6 acceptance gates (A-F) with the actual grep / test output proving each passes.
- A "Files Modified" subsection listing the 9 files in this plan's `files_modified` block plus any incidental ones (e.g. `tests/shared/errors.test.ts` if newly created; bridge test files if assertions were added; `tests/edge/handlers/plugin/reinstall.test.ts` for the F-2 binding integration test).
- A "Findings Addressed (Revision 1)" subsection naming each F-* checker finding (F-1 through F-8) and pointing at the Task / Step that addressed it.
- A "Task Commits" subsection listing the 4 Conventional Commit hashes and titles.
- A "Self-Check: PASSED" footer with the FOUND markers for each artifact.
</output>
