---
phase: 13-conformance-refactor-es-5-supersession
plan: 03-02
type: execute
wave: 3
depends_on:
  - 13-03-01
files_modified:
  - extensions/pi-claude-marketplace/shared/markers.ts
  - tests/architecture/markers-snapshot.test.ts
  - docs/prd/pi-claude-marketplace-prd.md
  - eslint.config.js
autonomous: false
requirements:
  - CMC-35

must_haves:
  truths:
    - "The 5 legacy ES-5 marker exports are DELETED from `extensions/pi-claude-marketplace/shared/markers.ts`."
    - "The 5 ES-5 snapshot rows are DELETED from `tests/architecture/markers-snapshot.test.ts` AND the `assert.equal(literals.length, 5, ...)` assertion is also dropped because PRD §6.12 will return 0 ES-5 literals post-edit."
    - "PRD §6.12 ES-5 row is REWRITTEN to a brief pointer per D-13-11: `v1.3 supersedes the V1 ES-5 marker strings; see docs/messaging-style-guide.md §15 (Supersession of ES-5) for the replacement table.`"
    - "ESLint BLOCK E's marker entries (the 3 path-variant entries added in Plan 13-01-03) and the `shared/markers.ts` + `markers-snapshot.test.ts` ignores are DELETED -- the markers no longer exist to be restricted."
    - "All four edits land in a SINGLE git commit per D-13-03 (style guide §15 supersession contract is contractually atomic). The single commit is the milestone v1.3 user-contract change boundary per D-30."
    - "The static-audit `tests/architecture/no-legacy-markers.test.ts` from Plan 13-01-03 stays in place AND continues to enforce zero re-introductions across the rest of the codebase's lifetime."
    - "Pre-commit gate: BEFORE this plan's atomic commit runs, the executor MUST verify `node --test tests/architecture/catalog-uat.test.ts` is green (Wave 3 plan #1 gate per D-13-04). If catalog UAT is red, this plan does NOT run."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/markers.ts"
      provides: "5 ES-5 marker exports DELETED; RECOVERY_PLUGIN_REINSTALL_PREFIX (line 26) and STATE_LOCK_HELD_PREFIX (line 37) STAY (Phase 5/7 extensions outside ES-5)"
    - path: "tests/architecture/markers-snapshot.test.ts"
      provides: "5 expected[] entries DELETED + literals.length === 5 assertion DELETED; remaining AG-5 + PUP-6 + D-08 marker assertions STAY"
    - path: "docs/prd/pi-claude-marketplace-prd.md"
      provides: "Section 6.12 ES-5 row REWRITTEN to brief pointer to style guide §15"
    - path: "eslint.config.js"
      provides: "BLOCK E marker entries and snapshot-test ignores DELETED; Pi peer-import restriction (the original BLOCK E content) preserved"
  key_links:
    - from: "git commit"
      to: "4 modified files"
      via: "single atomic commit"
      pattern: "git show HEAD --stat"
---

<objective>
Wave 3 plan #2: the ES-5 atomic three-file edit (D-13-03). In a SINGLE git commit, perform the four edits that complete the milestone v1.3 user-contract change:
1. Delete the 5 ES-5 marker exports from `shared/markers.ts`.
2. Delete the corresponding 5 rows + the `literals.length === 5` assertion from `tests/architecture/markers-snapshot.test.ts`.
3. Rewrite PRD §6.12 ES-5 to a brief pointer to style guide §15 per D-13-11.
4. Delete the ESLint marker-restriction entries from BLOCK E (the 3 path-variant entries added in Plan 13-01-03 + the snapshot-test ignores).

Purpose: This is the milestone v1.3 user-contract change boundary per D-30. The catalog UAT in Wave 3 plan #1 is the GATE -- if it's not green, this plan does not run. After this commit, the 5 legacy ES-5 strings no longer appear anywhere in the codebase except `docs/messaging-style-guide.md` §15 (the supersession table) and `tests/architecture/no-legacy-markers.test.ts` (which pins them as fixtures). The static-audit test continues to enforce zero re-introductions for the lifetime of the codebase.

Output: 4 files modified in a SINGLE git commit; the commit message follows Conventional Commits format per the project's `CLAUDE.md` guideline.

Autonomous: false. The atomic three-file edit is a milestone user-contract change. After the edits land and tests pass, a human checkpoint confirms the change before the commit is pushed. The checkpoint is a verify step, not an action step -- Claude does the edit + commit; the human verifies post-commit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-RESEARCH.md
@.planning/phases/13-conformance-refactor-es-5-supersession/13-PATTERNS.md
@docs/messaging-style-guide.md
@extensions/pi-claude-marketplace/shared/markers.ts
@tests/architecture/markers-snapshot.test.ts
@tests/helpers
@docs/prd/pi-claude-marketplace-prd.md
@eslint.config.js

<interfaces>
From `extensions/pi-claude-marketplace/shared/markers.ts:9-13` (the 5 exports to DELETE):
```typescript
export const PI_SUBAGENTS_NOT_LOADED = "pi-subagents is not loaded; ";
export const PI_MCP_ADAPTER_NOT_LOADED = "pi-mcp-adapter is not loaded; ";
export const RELOAD_HINT_PREFIX = "Run /reload to ";
export const MANUAL_RECOVERY_REQUIRED = "MANUAL RECOVERY REQUIRED: ";
export const ROLLBACK_PARTIAL = "(rollback partial: ";
```
Lines 26 (`RECOVERY_PLUGIN_REINSTALL_PREFIX`) and 37 (`STATE_LOCK_HELD_PREFIX`) are Phase 5/7 extensions explicitly outside the ES-5 set -- they STAY.

From `tests/architecture/markers-snapshot.test.ts:42-55` (the snapshot test rows + length assertion to DELETE):
- Lines 49-55: the `expected[]` array of 5 `[displayed-string, marker-export]` tuples.
- Lines 42-46: the `assert.equal(literals.length, 5, ...)` assertion -- after PRD §6.12 is rewritten to the pointer (Task 3 below), `extractEs5MarkerLiterals(prdContent)` returns 0 literals, so this assertion FAILS without deletion.
- The remaining `test(...)` blocks at lines 88-125 (AG-5 + PUP-6 + D-08 marker assertions) STAY.

From `docs/prd/pi-claude-marketplace-prd.md` §6.12 (verify via `grep -n 'ES-5\|pi-subagents is not loaded' docs/prd/pi-claude-marketplace-prd.md`): the existing ES-5 row lists the 5 backtick-quoted literals. The new content per D-13-11 is the single-line pointer.

From `eslint.config.js` BLOCK E (after Plan 13-01-03 -- the post-Wave-1 state): contains 3 marker-restriction entries in `paths[]` AND `ignores` includes `shared/markers.ts` + `markers-snapshot.test.ts`. These additions are REMOVED in this plan; the original Pi peer-import entry STAYS.

From `tests/helpers/prd-extract.ts::extractEs5MarkerLiterals` (the PRD-extraction helper consumed by the snapshot test; behavior: extracts the 5 backtick-quoted literals from PRD §6.12). After Task 3, the helper finds 0 literals -- the snapshot test's `literals.length === 5` assertion fails without Task 2's deletion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-commit gate verification</name>
  <files></files>
  <read_first>
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md D-13-04 (the "Wave 3 plan #1 IS the gate" binding text)
    - tests/architecture/catalog-uat.test.ts (Wave 3 plan #1 output)
  </read_first>
  <behavior>
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 -- catalog UAT is green BEFORE the atomic commit runs.
    - `npm run check` exits 0 on the baseline (post-Wave-3-plan-#1 state).
    - If either is red, this plan ABORTS without making any edits.
  </behavior>
  <action>
    Run the catalog UAT and full check suite to verify the Wave 3 plan #1 gate is green:
    ```
    node --test tests/architecture/catalog-uat.test.ts
    npm run check
    ```
    Both MUST exit 0. If either fails, STOP -- file an issue / revert / re-run the failing sub-wave's plan; do not proceed with the atomic commit.

    Do not make any file edits in this task; it's a verification-only step.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 (Wave 3 plan #1 catalog UAT is green per D-13-04 gating)
    - `npm run check` exits 0 (baseline full suite)
    - No edits made to any file during this task (verification-only step)
    - If either command fails, the executor MUST abort and not proceed to Task 2
  </acceptance_criteria>
  <done>
    Both commands exit 0; the executor has confirmed the gate is green; ready to proceed to Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic three-file edit + ESLint cleanup</name>
  <files>extensions/pi-claude-marketplace/shared/markers.ts, tests/architecture/markers-snapshot.test.ts, docs/prd/pi-claude-marketplace-prd.md, eslint.config.js</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/markers.ts (current file; lines 9-13 to delete; lines 26-37 to preserve)
    - tests/architecture/markers-snapshot.test.ts (current file; lines 42-55 to delete; remaining test blocks at lines 88-125 to preserve)
    - docs/prd/pi-claude-marketplace-prd.md (find section 6.12 ES-5 via `grep -n 'ES-5' docs/prd/pi-claude-marketplace-prd.md`; the ES-5 row to rewrite)
    - eslint.config.js (the post-Plan-13-01-03 state; BLOCK E with 3 marker entries + 2 extra ignores; the entries to delete)
    - tests/helpers/prd-extract.ts (the extraction helper -- consume but don't modify)
    - docs/messaging-style-guide.md §15 (the supersession table -- the PRD pointer points HERE; verify the link target text)
    - .planning/phases/13-conformance-refactor-es-5-supersession/13-CONTEXT.md D-13-11 (the exact pointer wording: "v1.3 supersedes the V1 ES-5 marker strings; see `docs/messaging-style-guide.md` §15 (Supersession of ES-5) for the replacement table.")
  </read_first>
  <behavior>
    - 4 files edited; SINGLE git commit covering all 4.
    - `extensions/pi-claude-marketplace/shared/markers.ts`: 5 export lines deleted (`PI_SUBAGENTS_NOT_LOADED`, `PI_MCP_ADAPTER_NOT_LOADED`, `RELOAD_HINT_PREFIX`, `MANUAL_RECOVERY_REQUIRED`, `ROLLBACK_PARTIAL`); the file header comment about "DO NOT EDIT without updating PRD §6.12" can be rewritten (the original 5 markers are gone; only the Phase 5/7 extension markers remain); the `RECOVERY_PLUGIN_REINSTALL_PREFIX` and `STATE_LOCK_HELD_PREFIX` exports STAY.
    - `tests/architecture/markers-snapshot.test.ts`: the ES-5-specific `test(...)` block (the one with the `expected[]` array of 5 tuples and the `literals.length === 5` assertion at lines 42-46) is DELETED in its entirety. The remaining test blocks (AG-5 + PUP-6 + D-08 marker assertions at lines 88-125 or wherever they live) STAY.
    - `docs/prd/pi-claude-marketplace-prd.md` section 6.12: the ES-5 row's content (the 5 backtick-quoted literals + any surrounding prose specific to those 5) is REPLACED with the D-13-11 pointer sentence. The numbering ES-1..ES-5 is preserved (so back-references like "PRD §6.12 ES-2" still resolve); only the ES-5 row's body changes.
    - `eslint.config.js`: BLOCK E reverts to its post-Plan-7 / pre-Plan-13-01-03 state -- the 3 marker-restriction entries are removed from `paths[]`, the snapshot-test + no-legacy-markers-test entries are removed from `ignores`, and the `files: [..., "tests/**/*.ts"]` widening is rolled back (or kept if other tests need it). The original Pi peer-import restriction STAYS.
    - The commit message follows Conventional Commits per `CLAUDE.md`: type is `chore` (it's a doc/config supersession, not a feature or fix), scope is `13`, subject ≤72 chars, body explains the milestone boundary + cites D-13-03 + D-13-11.
    - All checks pass post-commit: `npm run check` exits 0; `node --test tests/architecture/no-legacy-markers.test.ts` STILL exits 0 (the static-audit gate continues to enforce); `node --test tests/architecture/markers-snapshot.test.ts` exits 0 (the remaining AG-5 + PUP-6 + D-08 assertions still pass).
  </behavior>
  <action>
    Perform the 4 edits in a single working session, then commit them all together:

    SUB-TASK A: `extensions/pi-claude-marketplace/shared/markers.ts`
    1. Delete lines 9-13 (the 5 `export const` ES-5 marker definitions).
    2. Update the file-header comment (lines 1-7) -- the existing comment says "DO NOT EDIT without updating docs/prd/pi-claude-marketplace-prd.md §6.12". This is no longer accurate (the ES-5 row is replaced with a pointer). Rewrite the header to describe the Phase 5/7 extension markers (`RECOVERY_PLUGIN_REINSTALL_PREFIX`, `STATE_LOCK_HELD_PREFIX`) and cite the v1.3 ES-5 supersession (D-13-03 / D-13-11 / style guide §15).
    3. Preserve `RECOVERY_PLUGIN_REINSTALL_PREFIX` (existing line 26) and `STATE_LOCK_HELD_PREFIX` (existing line 37) with their existing doc comments unchanged.

    SUB-TASK B: `tests/architecture/markers-snapshot.test.ts`
    1. Find the ES-5-specific `test(...)` block (the one containing the `expected[]` array of 5 tuples). Delete the entire block.
    2. Delete the `assert.equal(literals.length, 5, ...)` assertion (lines 42-46 per RESEARCH.md; verify exact location).
    3. Delete imports of the 5 deleted markers from `extensions/pi-claude-marketplace/shared/markers.ts` (e.g., `import { PI_SUBAGENTS_NOT_LOADED, ... } from "../../extensions/.../shared/markers.ts"`). Leave imports of `RECOVERY_PLUGIN_REINSTALL_PREFIX` and `STATE_LOCK_HELD_PREFIX` intact -- the remaining tests for those still need them.
    4. Verify the remaining `test(...)` blocks for AG-5 (bridges/agents/marker.ts), PUP-6 (RECOVERY_PLUGIN_REINSTALL_PREFIX), and D-08 (STATE_LOCK_HELD_PREFIX) are intact.
    5. If `extractEs5MarkerLiterals` is exported from `tests/helpers/prd-extract.ts` and consumed nowhere else, leave it -- it's vestigial but harmless. If it's consumed only by the deleted test block, leave it alone (it might be useful for future PRD-extraction tests; deletion is optional and outside this plan's scope).

    SUB-TASK C: `docs/prd/pi-claude-marketplace-prd.md` §6.12 ES-5
    1. Use `grep -n 'ES-5\|^## §6.12\|pi-subagents is not loaded' docs/prd/pi-claude-marketplace-prd.md` to find the ES-5 row's exact location.
    2. The ES-5 row currently lists 5 backtick-quoted literals (the 5 markers). Replace the ES-5 row's body with the single sentence per D-13-11:
       ```
       ES-5: v1.3 supersedes the V1 ES-5 marker strings; see `docs/messaging-style-guide.md` §15 (Supersession of ES-5) for the replacement table.
       ```
       (Or, if the ES-5 row is a markdown table entry, adapt the wording to fit the table column shape.)
    3. Preserve the ES-1..ES-4 rows unchanged.
    4. Preserve the ES-5 label / row number / table column structure so back-references resolve (other PRD sections reference "ES-2 no `[error]` / `[warning]` prefix" -- those references must continue to find ES-2 unchanged).

    SUB-TASK D: `eslint.config.js`
    1. In BLOCK E (lines 258-278 in the pre-Plan-13-01-03 state; expanded by Plan 13-01-03), remove the 3 marker-restriction entries from `paths[]` (the entries with `importNames: ["PI_SUBAGENTS_NOT_LOADED", ...]`).
    2. Roll back the `ignores` field to its pre-Plan-13-01-03 state -- remove `extensions/pi-claude-marketplace/shared/markers.ts`, `tests/architecture/markers-snapshot.test.ts`, `tests/architecture/no-legacy-markers.test.ts` from the `ignores` array. Leave only the original entry: `["extensions/pi-claude-marketplace/platform/pi-api.ts"]`.
    3. Roll back the `files` field -- if Plan 13-01-03 expanded it from `["extensions/pi-claude-marketplace/**/*.ts"]` to `["extensions/pi-claude-marketplace/**/*.ts", "tests/**/*.ts"]`, decide whether to keep the `"tests/**/*.ts"` extension. If no other test needs the Pi peer-import restriction, roll it back to extensions-only. If preserving the test coverage is desired, keep both (but the snapshot-test ignore is no longer needed because the markers are gone).
    4. The post-edit BLOCK E should structurally match its pre-Plan-13-01-03 form -- a single `paths[]` entry for `@earendil-works/pi-coding-agent`, an `ignores` containing only `extensions/pi-claude-marketplace/platform/pi-api.ts`, and `files: ["extensions/pi-claude-marketplace/**/*.ts"]` (or the chosen test-extension scope).

    AFTER ALL 4 SUB-TASKS:
    1. Run `npm run check` -- MUST exit 0. If anything fails, diagnose, fix, and re-run.
    2. Run `node --test tests/architecture/no-legacy-markers.test.ts` -- MUST exit 0 (the static-audit gate stays green; literals are still pinned in the test file as fixtures, and the ALLOW_LIST already covers `shared/markers.ts` so the now-empty file is fine).
    3. Run `node --test tests/architecture/catalog-uat.test.ts` -- MUST exit 0 (no regression).

    COMMIT:
    1. Stage all 4 files: `git add extensions/pi-claude-marketplace/shared/markers.ts tests/architecture/markers-snapshot.test.ts docs/prd/pi-claude-marketplace-prd.md eslint.config.js`.
    2. Verify `git status` shows exactly these 4 files modified (no others).
    3. Run `pre-commit run --files extensions/pi-claude-marketplace/shared/markers.ts tests/architecture/markers-snapshot.test.ts docs/prd/pi-claude-marketplace-prd.md eslint.config.js` to clear hooks BEFORE committing per `CLAUDE.md` guideline. Fix any hook failures, restage, re-run until clean.
    4. Run `git commit` with a Conventional Commits message:
       ```
       chore(13): ES-5 supersession atomic three-file edit

       Per D-13-03 + D-13-11 + style guide §15 supersession contract:
       - Delete the 5 ES-5 marker exports from shared/markers.ts
       - Delete the 5 snapshot rows + literals.length === 5 assertion from
         tests/architecture/markers-snapshot.test.ts (the PRD-extract helper
         returns 0 literals post-edit; the legacy assertion is unrecoverable)
       - Rewrite docs/prd/pi-claude-marketplace-prd.md §6.12 ES-5 to a brief
         pointer to docs/messaging-style-guide.md §15
       - Roll back the Plan 13-01-03 ESLint marker-restriction additions
         from eslint.config.js BLOCK E (markers no longer exist)

       The static-audit gate at tests/architecture/no-legacy-markers.test.ts
       (Plan 13-01-03) stays in place and continues to enforce zero
       re-introductions across the rest of the codebase's lifetime.

       This is the milestone v1.3 user-contract change boundary per D-30.
       Rollback: git revert <this-commit-sha> (D-13-13).
       ```
    5. After the commit lands, run `git show HEAD --stat` -- verify exactly 4 files are listed.
    6. Re-run `npm run check` ONE MORE TIME post-commit to confirm the working tree + commit are both green.
  </action>
  <verify>
    <automated>npm run check && node --test tests/architecture/no-legacy-markers.test.ts && node --test tests/architecture/catalog-uat.test.ts && test "$(git show HEAD --stat --name-only HEAD | grep -cE '^(extensions/pi-claude-marketplace/shared/markers\.ts|tests/architecture/markers-snapshot\.test\.ts|docs/prd/pi-claude-marketplace-prd\.md|eslint\.config\.js)$')" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 post-commit
    - `node --test tests/architecture/no-legacy-markers.test.ts` exits 0 (static-audit gate green post-deletion; literals pinned in the test survive marker deletion per D-13-12)
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 (no regression in per-command UAT)
    - `git show HEAD --stat --name-only HEAD | grep -cE '^(extensions/pi-claude-marketplace/shared/markers\.ts|tests/architecture/markers-snapshot\.test\.ts|docs/prd/pi-claude-marketplace-prd\.md|eslint\.config\.js)$'` returns 4 (exactly 4 files in the single atomic commit per D-13-03)
    - `git log -1 --pretty=%s` matches the Conventional Commits pattern `chore(13): ES-5 supersession...`
    - `grep -c 'PI_SUBAGENTS_NOT_LOADED\|PI_MCP_ADAPTER_NOT_LOADED\|RELOAD_HINT_PREFIX\|MANUAL_RECOVERY_REQUIRED\|ROLLBACK_PARTIAL' extensions/pi-claude-marketplace/shared/markers.ts` returns 0 (all 5 ES-5 markers deleted)
    - `grep -c 'RECOVERY_PLUGIN_REINSTALL_PREFIX\|STATE_LOCK_HELD_PREFIX' extensions/pi-claude-marketplace/shared/markers.ts` returns at least 2 (Phase 5/7 extension markers preserved)
    - `grep -c 'PI_SUBAGENTS_NOT_LOADED' eslint.config.js` returns 0 (ESLint allow-list rolled back; marker-restriction entries deleted)
    - `grep -c 'literals.length, 5' tests/architecture/markers-snapshot.test.ts` returns 0 (length assertion deleted)
    - `grep -c 'D-13-03\|D-13-11' git_show_HEAD_output_dummy_ignore` -- commit body cites D-13-03 + D-13-11 (verify via `git show HEAD`)
    - PRD §6.12 ES-5 row content is the brief pointer per D-13-11 (`grep -c 'see .docs/messaging-style-guide.md. .15' docs/prd/pi-claude-marketplace-prd.md` returns at least 1)
    - ES-1..ES-4 rows in PRD §6.12 remain unchanged (back-reference resolution preserved)
    - `pre-commit run --files extensions/pi-claude-marketplace/shared/markers.ts tests/architecture/markers-snapshot.test.ts docs/prd/pi-claude-marketplace-prd.md eslint.config.js` exits 0
  </acceptance_criteria>
  <done>
    `npm run check` exits 0 post-commit; `node --test tests/architecture/no-legacy-markers.test.ts` exits 0; `node --test tests/architecture/catalog-uat.test.ts` exits 0; `git show HEAD --stat` lists exactly 4 files: `extensions/pi-claude-marketplace/shared/markers.ts`, `tests/architecture/markers-snapshot.test.ts`, `docs/prd/pi-claude-marketplace-prd.md`, `eslint.config.js`; the commit subject matches the Conventional Commits pattern `chore(13): ES-5 supersession...`; `grep -c 'PI_SUBAGENTS_NOT_LOADED\|PI_MCP_ADAPTER_NOT_LOADED\|RELOAD_HINT_PREFIX\|MANUAL_RECOVERY_REQUIRED\|ROLLBACK_PARTIAL' extensions/pi-claude-marketplace/shared/markers.ts` returns 0 (all 5 markers gone).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <!-- Checkpoint task exempt from auto-task <acceptance_criteria> contract per checker W-02 and references/checkpoints.md: human-verify tasks rely on <how-to-verify> + <resume-signal> instead. -->
  <name>Task 3: Human checkpoint -- verify atomic ES-5 supersession commit landed correctly</name>
  <files>extensions/pi-claude-marketplace/shared/markers.ts, tests/architecture/markers-snapshot.test.ts, docs/prd/pi-claude-marketplace-prd.md, eslint.config.js</files>
  <what-built>
    A single atomic git commit deleting the 5 ES-5 marker exports from `shared/markers.ts`, deleting the 5 corresponding rows + length assertion from `tests/architecture/markers-snapshot.test.ts`, rewriting `docs/prd/pi-claude-marketplace-prd.md` section 6.12 ES-5 to a brief pointer to style guide section 15, and rolling back the Plan 13-01-03 ESLint marker-restriction additions.

    This is the milestone v1.3 user-contract change boundary per D-30.
  </what-built>
  <how-to-verify>
    1. Run `git show HEAD --stat` and verify exactly 4 files are listed:
       - `extensions/pi-claude-marketplace/shared/markers.ts`
       - `tests/architecture/markers-snapshot.test.ts`
       - `docs/prd/pi-claude-marketplace-prd.md`
       - `eslint.config.js`
    2. Run `git show HEAD` and read the commit subject + body; confirm the message follows the Conventional Commits format and cites D-13-03 + D-13-11.
    3. Run `npm run check` and verify it exits 0.
    4. Run `node --test tests/architecture/no-legacy-markers.test.ts` and verify it exits 0 (the static-audit gate stays green).
    5. Run `node --test tests/architecture/catalog-uat.test.ts` and verify it exits 0 (no regression in per-command UAT).
    6. Run `grep -c 'PI_SUBAGENTS_NOT_LOADED\|PI_MCP_ADAPTER_NOT_LOADED\|RELOAD_HINT_PREFIX\|MANUAL_RECOVERY_REQUIRED\|ROLLBACK_PARTIAL' extensions/pi-claude-marketplace/shared/markers.ts` and verify it returns 0.
    7. Open `docs/prd/pi-claude-marketplace-prd.md` and read section 6.12 ES-5: confirm the row is the brief pointer per D-13-11, not the legacy 5-literal list.
    8. Open `extensions/pi-claude-marketplace/shared/markers.ts` and confirm only `RECOVERY_PLUGIN_REINSTALL_PREFIX` and `STATE_LOCK_HELD_PREFIX` exports remain.
    9. Open `eslint.config.js` BLOCK E and confirm the 3 marker-restriction entries have been removed (only `@earendil-works/pi-coding-agent` entry remains in `paths[]`; only `platform/pi-api.ts` remains in `ignores`).

    Rollback (if regressions surface in user testing): `git revert <commit-sha>` per D-13-13 restores all 4 files atomically. The static-audit test continues to prevent re-introductions.
  </how-to-verify>
  <action>
    Pause execution and prompt the human reviewer to run the 9 verification steps listed in `<how-to-verify>` above against the post-commit working tree. Do not advance until the reviewer responds via the resume-signal.

    This is a blocking gate: the milestone v1.3 user-contract change boundary requires human acknowledgement that the atomic commit landed correctly and no regressions surface in the verification steps. If the reviewer reports an issue, the recovery path is `git revert <commit-sha>` per D-13-13 -- the static-audit test continues to prevent re-introductions during rollback + retry.
  </action>
  <verify>
    Human reviewer types "approved" via the resume-signal after running the 9 verification steps in `<how-to-verify>`. If any step fails, the reviewer types "revert" (or describes the issue) and Task 2's commit is reverted per D-13-13.
  </verify>
  <done>
    Reviewer types "approved" via the resume-signal; the milestone v1.3 user-contract change boundary has been acknowledged.
  </done>
  <resume-signal>Type "approved" to acknowledge the milestone v1.3 user-contract boundary has landed correctly; type "revert" to roll back per D-13-13; or describe any issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ES-5 atomic commit ↔ codebase history | The commit is a user-contract supersession; rollback via `git revert <sha>` per D-13-13 is the recovery path |
| Static-audit gate ↔ post-commit codebase | `tests/architecture/no-legacy-markers.test.ts` continues to enforce zero re-introductions even after `shared/markers.ts` deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-20 | Tampering | Atomic-commit integrity | mitigate | All 4 edits land in a SINGLE commit per D-13-03; partial edits leave the test suite red (markers-snapshot.test.ts asserts `literals.length === 5` which fails on PRD-pointer state) |
| T-13-21 | Tampering | Post-commit re-introduction | mitigate | `tests/architecture/no-legacy-markers.test.ts` (Plan 13-01-03) pins the 5 literals and continues to scan the codebase forever |

NO new application-security threats. This is a doc + config supersession.
</threat_model>

<verification>
- `npm run check` exits 0 post-commit
- `node --test tests/architecture/no-legacy-markers.test.ts` exits 0
- `node --test tests/architecture/catalog-uat.test.ts` exits 0
- `git show HEAD --stat` lists exactly 4 files
- `git log -1 --pretty=%s` matches the Conventional Commits pattern `chore(13): ES-5 supersession...`
- `grep -c 'PI_SUBAGENTS_NOT_LOADED\|PI_MCP_ADAPTER_NOT_LOADED\|RELOAD_HINT_PREFIX\|MANUAL_RECOVERY_REQUIRED\|ROLLBACK_PARTIAL' extensions/pi-claude-marketplace/shared/markers.ts` returns 0
- `grep -cE '^(extensions/pi-claude-marketplace/shared/markers\.ts|tests/architecture/markers-snapshot\.test\.ts|docs/prd/pi-claude-marketplace-prd\.md|eslint\.config\.js)$'` against `git show HEAD --stat --name-only HEAD` returns exactly 4
- Human checkpoint approved
</verification>

<success_criteria>
1. SINGLE git commit lists exactly 4 modified files: `shared/markers.ts`, `markers-snapshot.test.ts`, `pi-claude-marketplace-prd.md`, `eslint.config.js`.
2. The 5 ES-5 marker exports are gone from `shared/markers.ts`; the Phase 5/7 extension markers stay.
3. `markers-snapshot.test.ts` ES-5 test block + length assertion gone; AG-5 + PUP-6 + D-08 marker tests stay.
4. PRD §6.12 ES-5 is the brief pointer per D-13-11; ES-1..ES-4 rows unchanged.
5. ESLint BLOCK E reverted to pre-Plan-13-01-03 state (only Pi peer-import restriction).
6. `npm run check` exits 0 post-commit; static-audit `no-legacy-markers.test.ts` stays green; catalog UAT stays green.
7. Commit message follows Conventional Commits + cites D-13-03 + D-13-11.
8. Human checkpoint approved -- milestone v1.3 user-contract change boundary acknowledged.
</success_criteria>

<output>
Create `.planning/phases/13-conformance-refactor-es-5-supersession/13-03-02-SUMMARY.md` when done, citing CMC-35 completion, the single-commit SHA, the 4 modified files, and any human-checkpoint notes. Update `.planning/REQUIREMENTS.md` Coverage table marking CMC-35 as Complete.
</output>
