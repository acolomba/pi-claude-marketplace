---
phase: 84-emit-an-agent-local-skillpath-field-on-every-generated-agent
plan: 04
type: execute
wave: 2
depends_on: ["84-01", "84-02"]
files_modified: []
autonomous: false
requirements: [AGSK-06]
must_haves:
  truths:
    - "SC-4 / D-84-03: a live FOREGROUND (async: false) spawn of a skill-referencing bridged agent loads and uses the skill on demand under pi-subagents 0.35.x lazy delivery"
    - "The verified A/B reproduces: WITH the emitted skillPath the subagent reads SKILL.md and emits its unique token; WITHOUT skillPath it does not (prints the no-skill sentinel)"
    - "The live check uses async: false to avoid the pi-subagents #526 detached-async-runner confound, which is unrelated to skill resolution"
  artifacts: []
  key_links:
    - "the bridged agent's emitted skillPath (Plan 84-01) is the single variable toggled between the two A/B runs"
    - "pi-subagents floor >=0.35.0 (Plan 84-02) must be satisfied in the live environment for skillPath to exist"
---

<objective>
Reproduce the SC-4 end-to-end acceptance signal: a real foreground subagent
spawn of a skill-referencing bridged agent loads and uses the skill. This is
inherently a live-environment check — not expressible as a `node:test` unit
test — so it is a blocking human-verify checkpoint, run after the emitter
(Plan 84-01) and the peer floor (Plan 84-02) are in place.

Purpose: SC-1/SC-2 prove the pointer is emitted and resolves deterministically;
SC-4 proves the whole pipeline works in a live spawn, closing issue #86
end-to-end. The A/B (with vs without skillPath) is the acceptance evidence.

Output: captured A/B transcript recorded in the plan SUMMARY as UAT evidence.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-NOTES.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-RESEARCH.md
</context>

## Artifacts this phase produces (scoped to this plan)

- UAT evidence only (no source files): the captured with/without-skillPath A/B transcript, recorded in the SUMMARY.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live foreground A/B spawn of a skill-referencing bridged agent (SC-4)</name>
  <files>(no file written -- live spawn transcripts captured into the SUMMARY)</files>
  <read_first>
    - 84-NOTES.md "Verification" (the already-performed three-way A/B: deterministic resolver + live spawn; the unique-token vs NO_SKILL_LOADED signal)
    - 84-RESEARCH.md "Common Pitfalls" (the #526 confound: use async: false; a `typebox/compile` failure is #526, not a skillPath regression) and "Validation Architecture" SC-4 row
    - the generated agent file produced by Plan 84-01 (confirm it carries both `skills:` and the new `skillPath:` line)
  </read_first>
  <what-built>
    The emitter now writes `skillPath: ../pi-claude-marketplace/resources/skills` on every generated agent with a non-empty skills list (Plan 84-01), and the pi-subagents peer floor is declared at `>=0.35.0` (Plan 84-02). SC-1 (unit) and SC-2 (deterministic resolver contract) are already green. This checkpoint confirms the live runtime behavior.
  </what-built>
  <action>
    Present the live A/B recipe to the user and BLOCK pending their result. No automated test surface exists — SC-4 is inherently a live-environment check (RESEARCH Validation Architecture). The executor presents the steps below, then waits.

    Recipe to present (reproduces 84-NOTES "Verification" on pi-subagents 0.35.x with a real bridged-skill install):

    1. Install (or reuse) a Claude plugin whose agent references one of the plugin's own skills, so the generated agent at `<scope>/agents/<name>.md` carries both a `skills:` line and the new `skillPath: ../pi-claude-marketplace/resources/skills` line. Confirm the skill is staged at `<scope>/pi-claude-marketplace/resources/skills/<generatedName>/SKILL.md`, whose body instructs the agent to emit a unique token when the skill loads.
    2. Spawn the bridged agent as a FOREGROUND subagent (`async: false`) with a task that requires the skill; observe the subagent reads SKILL.md and emits the unique token. Use async: false deliberately: pi-subagents #526 is a detached-async-runner TypeBox module-resolution regression unrelated to skill resolution; a failure printing `Cannot find module 'typebox/compile'` is #526, not a skillPath regression (do not debug it as one).
    3. Remove ONLY the `skillPath:` line from the same generated agent file and spawn again with the identical task; observe the subagent does NOT load the skill (prints the no-skill sentinel, e.g. `NO_SKILL_LOADED`).
    4. Confirm the delta is caused solely by the `skillPath` line, and capture both transcripts.
  </action>
  <verify>
    <human-check>User runs the foreground (async: false) A/B and reports: WITH skillPath the subagent used the skill (unique token present); WITHOUT skillPath it did not (sentinel present). Any `typebox/compile` failure is reported as #526, not a skillPath regression.</human-check>
  </verify>
  <resume-signal>Type "approved" with both A/B transcripts, or describe what diverged (and whether any divergence was #526 rather than a skillPath issue).</resume-signal>
  <acceptance_criteria>
    - The user ran a FOREGROUND (async: false) spawn of a skill-referencing bridged agent.
    - WITH the emitted skillPath: the subagent loaded and used the skill (unique token in the transcript).
    - WITHOUT the skillPath line: the subagent did not load the skill (no-skill sentinel in the transcript).
    - Both transcripts are captured into the SUMMARY as the SC-4 UAT evidence.
  </acceptance_criteria>
  <done>The live foreground A/B is captured and approved: skillPath is the deciding factor between the subagent using and not using the skill (SC-4, D-84-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridged agent frontmatter → live pi-subagents spawn | The live subagent reads the emitted skillPath and resolves the skill at spawn time; verification only, no new code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-05 | Denial of Service | live-spawn verification confounded by pi-subagents #526 | low | accept | The checkpoint mandates `async: false` (foreground), which does not reach the #526 detached-async-runner TypeBox import; a `typebox/compile` failure is explicitly called out as #526, not a skillPath regression, so it will not be misdiagnosed. |
</threat_model>

<verification>
- Live foreground (async: false) A/B captured: WITH skillPath → skill used (unique token); WITHOUT skillPath → skill not used (sentinel).
- Human approval recorded with both transcripts in the SUMMARY.
</verification>

<success_criteria>
SC-4 satisfied: a live foreground subagent spawn loads and uses the bridged skill on demand, and the A/B confirms skillPath is the deciding factor (D-84-03).
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-04-SUMMARY.md` when done, including the captured A/B transcripts.
</output>
