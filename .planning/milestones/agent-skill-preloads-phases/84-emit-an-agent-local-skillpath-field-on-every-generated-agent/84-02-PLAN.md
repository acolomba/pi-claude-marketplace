---
phase: 84-emit-an-agent-local-skillpath-field-on-every-generated-agent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - .planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md
autonomous: true
requirements: [AGSK-06, AGSK-04]
must_haves:
  truths:
    - "SC-3 / D-84-03: package.json declares `pi-subagents` at `>=0.35.0` as an OPTIONAL peer (peerDependencies + peerDependenciesMeta.optional: true); npm run check stays green and no node_modules/pi-subagents is installed"
    - "The README prerequisites bullet for pi-subagents states the `>=0.35.0` floor"
    - "REQUIREMENTS.md gains AGSK-06 (acceptance mirroring the four ROADMAP success criteria) and re-amends AGSK-04 to the single-state `(available on demand)` legend (D-84-01); the traceability table and coverage counts are updated"
  artifacts:
    - package.json
    - README.md
    - .planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md
  key_links:
    - "peerDependenciesMeta.pi-subagents.optional: true keeps npm from installing or erroring on the absent peer"
    - "no-telemetry-deps.test.ts scans peerDependencies; pi-subagents matches no forbidden telemetry pattern"
---

<objective>
Declare the pi-subagents version floor and update the requirement/spec docs.
This plan is independent of the code change (disjoint files) and runs in
parallel with Plan 84-01.

Purpose: SC-3 requires `package.json` to declare `pi-subagents >=0.35.0` (where
agent-local `skillPath` shipped) while keeping `npm run check` green. Because
pi-subagents has ZERO prior representation in package.json and is a soft,
presence-probed runtime peer (never imported), this is an ADDITION as an
OPTIONAL peer — not a bump and not a `dependencies` entry (RESEARCH findings 2
and A2). AGSK-06 must be recorded in REQUIREMENTS.md and AGSK-04 re-amended to
the single-state legend (D-84-01).

Output: package.json with the optional peer, README floor note, and updated
REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/ROADMAP.md
@.planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-PATTERNS.md
@package.json
</context>

## Artifacts this phase produces (scoped to this plan)

- NEW package.json `peerDependencies` entry `pi-subagents: >=0.35.0` plus a NEW `peerDependenciesMeta` block marking it `optional: true`.
- CHANGED README prerequisites bullet: pi-subagents floor `>=0.35.0`.
- NEW requirement AGSK-06 in REQUIREMENTS.md; re-amended AGSK-04 (single-state legend); updated traceability + coverage.

<tasks>

<task type="auto">
  <name>Task 1: Add pi-subagents as an optional peer dependency</name>
  <files>package.json, README.md</files>
  <read_first>
    - package.json (the existing `peerDependencies` block with three REQUIRED peers and NO `peerDependenciesMeta`; the `dependencies`/`devDependencies` blocks — pi-subagents must NOT be added there)
    - README.md (the prerequisites bullet linking pi-subagents, currently unversioned)
    - 84-RESEARCH.md "Package Legitimacy Audit" (pi-subagents verdict OK, disposition Approved — peerDependencies entry only) and the pitfall "Adding pi-subagents to dependencies instead of peerDependencies"
    - 84-PATTERNS.md "package.json (config)" target pattern
    - tests/architecture/no-telemetry-deps.test.ts (confirms peerDependencies is scanned; pi-subagents matches no forbidden pattern)
  </read_first>
  <action>
    In package.json, add `pi-subagents` at `>=0.35.0` to the `peerDependencies` block (keep the block's existing key ordering convention), and add a new top-level `peerDependenciesMeta` block declaring `pi-subagents` as `optional: true` so npm neither installs nor errors on the absent peer (D-84-03, RESEARCH A2). Do NOT add pi-subagents to `dependencies` or `devDependencies` — it is presence-probed via the Pi tool registry, never imported as code. Run `npm install` to refresh `package-lock.json`, then confirm the lockfile did NOT gain a `node_modules/pi-subagents` entry (the pitfall warning sign). In README.md, update the pi-subagents prerequisites bullet to state the `>=0.35.0` floor (RESEARCH Open Question 1 recommends including this to keep the two floor statements in sync). Keep the pi-subagents CHANGELOG/upstream `skillPath` PR citation as #428 if referenced anywhere.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json'); if(p.peerDependencies['pi-subagents']!=='>=0.35.0') process.exit(1); if(!p.peerDependenciesMeta || !p.peerDependenciesMeta['pi-subagents'] || p.peerDependenciesMeta['pi-subagents'].optional!==true) process.exit(1)"` exits 0.
    - `node -e "const p=require('./package.json'); if(p.dependencies['pi-subagents']||p.devDependencies['pi-subagents']) process.exit(1)"` exits 0 (not in dependencies/devDependencies).
    - `test -d node_modules/pi-subagents && exit 1 || exit 0` (no installed pi-subagents tree after npm install).
    - `node --test tests/architecture/no-telemetry-deps.test.ts` exits 0.
    - `grep -n ">=0.35.0" README.md` returns at least one match on the pi-subagents prerequisites bullet.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test tests/architecture/no-telemetry-deps.test.ts</automated>
  </verify>
  <done>package.json declares pi-subagents >=0.35.0 as an optional peer (peerDependenciesMeta.optional true), not a dependency; no pi-subagents tree is installed; README states the floor; no-telemetry test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Record AGSK-06 and re-amend AGSK-04 in REQUIREMENTS.md</name>
  <files>.planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md</files>
  <read_first>
    - .planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md (the v1 Requirements list including the current two-part AGSK-04 text; the Traceability table; the Coverage counts)
    - .planning/workstreams/issue-86-agent-skill-preloads/ROADMAP.md §Phase 84 (the four success criteria to mirror into AGSK-06)
    - 84-RESEARCH.md "Phase Requirements" table (the drafted AGSK-06 acceptance text)
    - 84-CONTEXT.md D-84-01 (the single-state legend re-amendment) and the Claude's Discretion note authorizing the AGSK-06 wording
  </read_first>
  <action>
    Add a new requirement AGSK-06 (agent-local skillPath resolution) whose acceptance mirrors the four ROADMAP Phase 84 success criteria: (1) a non-empty-skills agent emits the fixed-constant skillPath line and skill-less agents stay byte-identical; (2) given that skillPath, pi-subagents resolveSkillsWithFallback resolves the bridged skill by its generated name and it does not enter the parent/global catalog; (3) package.json declares an optional pi-subagents `>=0.35.0` peer and `npm run check` stays green; (4) a live foreground subagent spawn loads and uses the skill on demand. Re-amend AGSK-04: replace its two-state legend wording with the single-state form — every referenced Claude skill maps to its Pi name annotated `(available on demand)` (D-84-01, superseding the earlier preloaded/on-demand split). Update the Traceability table: add an AGSK-06 row mapped to Phase 84, and mark AGSK-04 as amended in Phase 84. Update the Coverage counts (v1 requirements total and mapped) to include AGSK-06. Use ASCII throughout (`--`, not em dashes) so the fix-unicode-dashes hook stays clean on commit messages; `.planning/` file bodies are excluded from the hook but the commit MESSAGE must be ASCII.
  </action>
  <acceptance_criteria>
    - `grep -c "AGSK-06" .planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md` returns at least 2 (definition + traceability row).
    - AGSK-04's text in REQUIREMENTS.md no longer contains a two-state legend description; it states the single `(available on demand)` annotation and cites D-84-01.
    - The Traceability table contains an AGSK-06 row referencing Phase 84.
    - The Coverage section counts reflect the added requirement (6 v1 requirements, all mapped, 0 unmapped).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && grep -q "AGSK-06" .planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md && grep -q "available on demand" .planning/workstreams/issue-86-agent-skill-preloads/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>REQUIREMENTS.md carries AGSK-06 (four-part acceptance mirroring the ROADMAP SCs), AGSK-04 re-amended to the single-state legend citing D-84-01, and an updated traceability table + coverage count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json peer declaration → npm tooling | A declarative optional peer entry; npm never installs or executes pi-subagents from it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-SC | Tampering | pi-subagents peer declaration (supply chain) | low | accept | pi-subagents is added ONLY as an optional peerDependency (declarative, `optional: true`); npm never resolves or installs it, and this repo never imports it (presence-probed via the Pi tool registry). No package-manager install task exists, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. RESEARCH Package Legitimacy Audit verdict: OK (multi-year registry history, verified on npm). |
| T-84-03 | Tampering | README/docs floor statement | low | accept | Documentation-only; the floor is declarative and human-facing. No runtime enforcement is added (the soft-dep probe stays presence-only, no version awareness — RESEARCH Anti-Pattern). |
</threat_model>

<verification>
- `node --test tests/architecture/no-telemetry-deps.test.ts` green.
- package.json: `pi-subagents >=0.35.0` in peerDependencies with `peerDependenciesMeta.pi-subagents.optional: true`; absent from dependencies/devDependencies; no `node_modules/pi-subagents` installed.
- README prerequisites bullet states `>=0.35.0`.
- REQUIREMENTS.md carries AGSK-06 and the re-amended AGSK-04.
- Wave merge: `npm run check` stays green (SC-3).
</verification>

<success_criteria>
SC-3 satisfied: the optional peer floor is declared and `npm run check` stays green. AGSK-06 and the re-amended AGSK-04 are recorded (D-84-01, D-84-03).
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-02-SUMMARY.md` when done.
</output>
