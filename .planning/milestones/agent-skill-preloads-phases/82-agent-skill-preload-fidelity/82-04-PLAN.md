---
phase: 82-agent-skill-preload-fidelity
plan: 04
type: execute
wave: 3
depends_on: ["82-02", "82-03"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - tests/bridges/agents/frontmatter.test.ts
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGSK-04]

must_haves:
  truths:
    - "D-82-04: the legend renders at the top of the generated body, immediately after the provenance HTML comment, before the original agent prose"
    - "D-82-05: the legend heading is exactly `## Pi coding agent skill legend` with the markdown heading + section shape"
    - "D-82-06: only `<this-plugin>:<skill>` tokens whose skill resolves into knownSkills get a legend entry; annotation is `(preloaded in your context)` when the generated name is in the emitted skills list, `(not available in this session)` otherwise; cross-plugin and unknown-skill tokens get NO entry"
    - "D-82-07: detection scans the whole substituted body including fenced code blocks (no fence parsing, no inline rewriting)"
    - "AGSK-04 reference-gating: an agent body with no matching tokens produces byte-identical output to today — verified by the plan 82-01 corpus plus an explicit no-token test"
    - "Phase SC-1..SC-4: the issue #86 canonical agent converts end to end (parseFrontmatter -> convertAgent) with `tools: bash,read`, `skills: spec-tree-review-changes`, `droppedTools: Skill` + D-82-09 warning, clean droppedFields, and a preloaded legend entry"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      provides: "SkillLegendEntry type; optional legend field on emitGeneratedAgentFile input; legend block rendering with exact byte layout"
      contains: "Pi coding agent skill legend"
    - path: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      provides: "Private detectSkillTokens + escapeRegExp helpers; convertAgent step 7->8 legend wiring"
      contains: "detectSkillTokens"
    - path: "tests/bridges/agents/convert.test.ts"
      provides: "Detection-edge pins and the issue #86 canonical end-to-end exact-fileContent test"
    - path: "tests/bridges/agents/frontmatter.test.ts"
      provides: "Legend rendering byte-layout pins (present / absent / empty)"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      via: "convertAgent passes SkillLegendEntry[] into emitGeneratedAgentFile"
      pattern: "legend"
    - from: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      to: "extensions/pi-claude-marketplace/domain/name.ts"
      via: "detection candidates resolved through generatedSkillName (read-only)"
      pattern: "generatedSkillName"
---

<objective>
Implement AGSK-04: generated agents whose body references `<plugin>:<source-skill>` tokens carry a converter-authored skill legend mapping each referenced Claude skill name to its Pi skill name, placed immediately after the provenance comment (D-82-04), with the locked heading and section shape (D-82-05), known-skill-only detection (D-82-06), and whole-body scanning (D-82-07). Close the phase with the issue #86 canonical end-to-end test covering SC-1..SC-4.

Resolved discretion choices (from 82-RESEARCH.md Open Questions — encode exactly, do not revisit):
- Annotations are the ASCII parenthesized forms `(preloaded in your context)` and `(not available in this session)` — satisfies AGSK-04/SC-4 verbatim and survives the fix-unicode-dashes hook (the D-82-05 example's em-dash variant is superseded within the declared discretion area).
- Both entry kinds include the Pi-name mapping with the U+2192 arrow (AGSK-04 requires the mapping for EACH referenced skill; D-82-05's example shape is extended within discretion).
- Entry order: first occurrence in body order, deduped by token.
- The arrow is written as the `→` escape in every .ts source/test literal (real arrow in output bytes; ASCII source survives pre-commit and BSD grep).

Rendered legend block (exact output bytes, shown here with the real arrow):

    ## Pi coding agent skill legend

    These instructions reference Claude skills by their original names. In this Pi session:

    - `spec-tree:review-changes` → skill `spec-tree-review-changes` (preloaded in your context)
    - `spec-tree:other-skill` → skill `spec-tree-other-skill` (not available in this session)

Entry line template: `- ` + backtick(token) + ` → skill ` + backtick(generatedName) + ` (` + annotation + `)`.

Output: legend rendering in the emitter, detection + wiring in convert.ts, detection/rendering/e2e test pins; byte-identity corpus still green.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` — the orchestrator commits.
- ASCII-only source literals; never a literal em-dash (U+2014) or literal arrow in .ts files.
- Comment/test-title anchors: `AGSK-04`, `D-82-04`..`D-82-07`, `#86`, `SC-N` — never `Phase 82`/plan/wave references.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-PATTERNS.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-02-SUMMARY.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-03-SUMMARY.md
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Legend rendering in emitGeneratedAgentFile (contract first)</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (emitGeneratedAgentFile ~lines 174-234 — especially the assembly tail ~226-233 with its leading-blank-line discipline and the `generatedFrontmatter + "\n" + provenanceComment + bodyFinal` return; GeneratedFrontmatterFields interface style ~133-140; optional-field emission pattern ~189-202)
    - tests/bridges/agents/frontmatter.test.ts (emitter test shape ~lines 85-123)
    - 82-RESEARCH.md Pattern 4 rendering half + Pitfall 6 (no-legend byte layout must not drift; `includes`-style tests will not catch separator drift)
  </read_first>
  <behavior>
    - Test 1 (Pitfall 6 guard): emit with `legend` undefined returns bytes EXACTLY equal to a constant captured from the same input before this change (assert.equal on the whole string) — no separator drift
    - Test 2: emit with `legend: []` returns the identical bytes as undefined
    - Test 3 (D-82-04/D-82-05): emit with two entries — `{ token: "spec-tree:review-changes", generatedName: "spec-tree-review-changes", preloaded: true }` and `{ token: "spec-tree:other-skill", generatedName: "spec-tree-other-skill", preloaded: false }` — produces output whose bytes read, in order: frontmatter block and provenance comment exactly as today, then one blank line, then `## Pi coding agent skill legend`, blank line, `These instructions reference Claude skills by their original names. In this Pi session:`, blank line, the two entry lines (preloaded first) per the objective's template with the `→` arrow, then one blank line, then the original body bytes unchanged. Pin with assert.equal against a full-output constant
  </behavior>
  <action>
    In frontmatter.ts, export an interface `SkillLegendEntry { readonly token: string; readonly generatedName: string; readonly preloaded: boolean }` following the module's readonly-field style. Extend `emitGeneratedAgentFile`'s input with an optional `legend?: readonly SkillLegendEntry[]`. When `legend` is undefined or empty, the assembly expression must remain byte-for-byte today's `generatedFrontmatter + "\n" + provenanceComment + bodyFinal` path. When non-empty, insert the legend block between the provenance comment and the body such that the byte order in behavior Test 3 holds (derive exact separator handling from the current tail: the body's existing leading-blank-line normalization supplies the blank line after the last entry — read the tail before deciding where `"\n"` literals go). Annotation strings: `preloaded in your context` / `not available in this session` inside parentheses. All literals ASCII; arrow as `→`. Keep rendering logic entirely in frontmatter.ts (single byte-assembly seam per the module header contract).

    Write the three behavior tests first (capture Test 1's constant from the unmodified emitter), confirm Test 3 fails, then implement until green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/frontmatter.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1-3 pass with assert.equal on FULL output strings (no includes-only assertions)
    - `grep -c 'u2192' extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` is >= 1 and a byte-level scan finds no literal U+2192 or U+2014 in the file (`node -e` check or `grep -a`, not bare BSD grep)
    - The exported `SkillLegendEntry` name and field names match exactly: token, generatedName, preloaded (Task 2 consumes this contract)
    - Byte-identity corpus green and untouched
  </acceptance_criteria>
  <done>Emitter renders the locked legend shape when given entries and is provably byte-identical when not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Token detection and convertAgent wiring</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (convertAgent step 7->8 ~lines 371-401 — substituteClaudeVars then emitGeneratedAgentFile call; private-helper style of splitCsv ~79-108 and dedupePreservingOrder ~110-121; mapSkills result shape)
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (the SkillLegendEntry contract and legend field added in Task 1)
    - extensions/pi-claude-marketplace/domain/name.ts (generatedSkillName — candidate class below guarantees assertSafeName cannot throw, but read the assertions to confirm)
    - 82-RESEARCH.md Pattern 4 detection half + Pitfall 7 (boundary false positives/negatives)
  </read_first>
  <behavior>
    All via convertAgent with pluginName "spec-tree", knownSkills ["spec-tree-review-changes"] unless noted:
    - Test 1 (D-82-06 preloaded): raw skills `spec-tree:review-changes`, body `Invoke spec-tree:review-changes on the diff.` -> fileContent contains the legend with one entry annotated `(preloaded in your context)`
    - Test 2 (D-82-06 not preloaded): raw skills absent, same body -> entry annotated `(not available in this session)` (skill known but not emitted)
    - Test 3 (D-82-06 unknown skill): body `Use spec-tree:phantom.` -> NO legend (no heading in fileContent)
    - Test 4 (D-82-06 cross-plugin): body `Use other-plugin:review-changes.` -> NO legend
    - Test 5 (Pitfall 7 boundary): body `Use other-spec-tree:review-changes.` -> NO legend (lookbehind rejects the embedded match)
    - Test 6 (Pitfall 7 punctuation): body `Use spec-tree:review-changes.` (sentence-final period) -> legend entry present for `spec-tree:review-changes` (candidate class excludes `.`)
    - Test 7 (dedupe + order): body referencing `spec-tree:review-changes` twice plus a second known skill token later (add "spec-tree-other" to knownSkills, reference `spec-tree:other` after the duplicates) -> exactly two entries, first-occurrence order
    - Test 8 (D-82-07 fences): the token appearing ONLY inside a fenced code block still yields a legend entry
    - Test 9 (AGSK-04 reference-gating): token-free body -> fileContent contains no `Pi coding agent skill legend` heading and equals the pre-legend output for that input (exact assert against a constant captured before wiring, or structural equality with a legend-free emit)
  </behavior>
  <action>
    In convert.ts add two private module-level helpers next to splitCsv: `escapeRegExp(value: string): string` (MDN one-liner replacing `[.*+?^${}()|[\]\\]` with `\\$&` — no analog exists in the repo, per 82-PATTERNS.md) and `detectSkillTokens(body: string, pluginName: string, knownSkills: readonly string[], emittedSkills: readonly string[]): SkillLegendEntry[]` (import the type from frontmatter.ts). Detection: global regex built as `(?<![A-Za-z0-9_:-])` + escapeRegExp(pluginName) + `:` + capture group `([A-Za-z0-9_-]+)`; for each match compute `generatedSkillName(pluginName, candidate)` (the candidate class cannot produce empty/`.`/`..`, so no throw path; confirm against name.ts assertions), keep the entry only when the generated name is in knownSkills (D-82-06), set `preloaded` by membership in emittedSkills, dedupe by full token keeping first occurrence. Scan the whole body verbatim — no fence stripping (D-82-07).

    Wire in convertAgent between steps 7 and 8: run detectSkillTokens on `substitutedBody` (the emitted text) with `skillsResult.emit` as emittedSkills, and pass the result via the emit input's `legend` field. Passing an empty array is safe (Task 1 pinned empty === undefined bytes). Do not touch warnings, provenance fields, or any other emit input.

    Write the behavior tests first through convertAgent (helpers stay private), confirm Tests 1-8 fail and 9 passes, then implement until green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1-9 pass; every pre-existing convert.test.ts test still passes
    - Legend entries render only for same-plugin tokens resolving into knownSkills; boundary and punctuation edges pinned (Tests 5, 6)
    - `detectSkillTokens` and `escapeRegExp` are NOT exported from convert.ts (grep: `grep -n 'export' extensions/pi-claude-marketplace/bridges/agents/convert.ts` shows no new export lines beyond the existing surface)
    - The detection regex source contains the lookbehind `(?<![A-Za-z0-9_:-])` and candidate class `[A-Za-z0-9_-]+` (source assertion in convert.ts)
    - Byte-identity corpus green and untouched
  </acceptance_criteria>
  <done>Reference-gated legend detection wired end to end; token-free bodies byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Issue #86 canonical end-to-end pin and phase gate</name>
  <files>tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (makeDiscovered; frontmatter-block isolation pattern ~lines 363-365)
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (parseFrontmatter — the e2e test drives the full parse -> convert pipeline)
    - 82-RESEARCH.md "Issue #86 canonical fixture" code example and phase SC-1..SC-4 in the ROADMAP excerpt within 82-RESEARCH.md phase_requirements
  </read_first>
  <action>
    Add the canonical end-to-end test (title anchored `#86`): build the source text `---\nname: changes-reviewer\ndescription: Reviews changes\ntools: Bash, Read, Skill\nskills:\n  - spec-tree:review-changes\n---\n\nInvoke spec-tree:review-changes on the diff.\n`, run `parseFrontmatter`, feed raw/body through makeDiscovered (sourceName "changes-reviewer", generatedName "pi-claude-marketplace-spec-tree-changes-reviewer") into convertAgent with pluginName "spec-tree", knownSkills ["spec-tree-review-changes"], mapModel false. Assert every SC facet individually AND pin the complete fileContent:
    (SC-1) the frontmatter block contains the exact lines `tools: bash,read` and `skills: spec-tree-review-changes`, and droppedFields shows no `- spec-tree` entry;
    (SC-2) provenance shows `droppedTools` containing `Skill` and warnings containing the exact D-82-09 string (already pinned in plan 82-03 — reuse the same literal);
    (SC-4) the body carries the legend with the single entry `spec-tree:review-changes` -> `spec-tree-review-changes` annotated `(preloaded in your context)`;
    finally `assert.equal(out.fileContent, CANONICAL_EXPECTED)` with a full-file constant captured from the now-complete implementation and reviewed line by line against the SC facts before pinning.

    Add a sibling reference-gated twin (SC-5 facet): the same source WITHOUT the body token (body `Review the diff.`) converts with NO legend heading in fileContent while keeping the same frontmatter and provenance facts.

    Then run the phase gate: full `npm test`, `npm run check`, and confirm the plan-82-01 corpus is untouched.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - The canonical test asserts all of: `tools: bash,read` line, `skills: spec-tree-review-changes` line, droppedTools containing `Skill`, exact D-82-09 warning string, no `- spec-tree` droppedFields token, legend entry with `(preloaded in your context)` — each as its own assertion, plus one whole-fileContent assert.equal
    - The reference-gated twin asserts the absence of `Pi coding agent skill legend` in fileContent
    - `npm run check` passes end to end (NFR-6)
    - `git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts` exits 0 — the pre-fix corpus survived the entire phase unmodified
    - `git status --porcelain` shows changes confined to the four files in this plan's files_modified
  </acceptance_criteria>
  <done>Issue #86 reproduction converts correctly end to end with every phase success criterion pinned; quality gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent body -> legend scanner | Untrusted body prose is regex-scanned and can influence legend content in the generated child system prompt |
| plugin name -> regex construction | Plugin name is interpolated into a RegExp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-10 | Tampering | Legend injection: crafted body tokens placing markdown/HTML into the legend | mitigate | Entries render only when the token matches the constrained class `[A-Za-z0-9_-]+` after `pluginName:` AND resolves into knownSkills; backticks, brackets, and HTML metacharacters cannot appear in a rendered token or generated name (Task 2 Tests 3-5) |
| T-82-11 | DoS | ReDoS via attacker-controlled body | mitigate | Single linear character-class regex, no nested quantifiers; escapeRegExp neutralizes metacharacters in the interpolated plugin name |
| T-82-12 | DoS | assertSafeName throw from scanner candidates | mitigate | Candidate class structurally excludes empty/`.`/`..`; confirmed against name.ts assertions in Task 2 read_first |
| T-82-13 | Tampering | Legend block perturbing provenance-comment integrity | mitigate | Legend renders strictly AFTER the closed `-->`; Task 1 Test 3 pins full-output bytes; no-legend path byte-pinned (Test 1) |

No packages installed; no network.
</threat_model>

<verification>
- `node --test tests/bridges/agents/frontmatter.test.ts` and `node --test tests/bridges/agents/convert.test.ts` green
- `node --test tests/bridges/agents/convert-byte-identity.test.ts` green with `git diff --exit-code` on that file (pre-fix corpus survived all three implementation plans)
- `npm run check` green (NFR-6)
</verification>

<success_criteria>
- Phase SC-4: bodies referencing `spec-tree:review-changes` carry the legend with correct preloaded/not-available annotations; D-82-04..D-82-07 all hold and are test-pinned
- Phase SC-1/SC-2 end to end: the issue #86 canonical agent converts with correct frontmatter, provenance, warning, and legend — whole fileContent pinned
- Phase SC-5: token-free bodies and all seven corpus classes byte-identical to pre-fix HEAD
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-04-SUMMARY.md` when done.
</output>
