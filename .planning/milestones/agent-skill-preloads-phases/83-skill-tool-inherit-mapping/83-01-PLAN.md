---
phase: 83-skill-tool-inherit-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGSK-05]

must_haves:
  truths:
    - "D-83-06 disallowed direction: a whole-file exact-equality pin captures the HEAD (Phase 82) output of a Skill-declared-but-disallowed input BEFORE any converter change, and passes green against unmodified HEAD"
    - "The pinned input declares Skill in tools: AND lists Skill in disallowedTools -- the only Skill-bearing input class whose bytes must NOT change in this phase (ROADMAP success criterion 2)"
    - "The pin's expected constant holds raw literal bytes (no interpolated test constants), so later constant renames cannot touch it -- it survives the rest of the phase untouched as the byte-identity gate"
  artifacts:
    - path: "tests/bridges/agents/convert.test.ts"
      provides: "DISALLOWED_SKILL_EXPECTED whole-file constant + the AGSK-05 / D-83-06 disallowed-direction pin test"
      contains: "DISALLOWED_SKILL_EXPECTED"
  key_links:
    - from: "tests/bridges/agents/convert.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      via: "convertSpecTree helper (existing) driving convertAgent"
      pattern: "convertSpecTree"
---

<objective>
Capture the Wave 0 requirement from 83-VALIDATION.md: pin the current HEAD (Phase 82) whole-file output of a `Skill`-declared-but-disallowed input BEFORE any Phase 83 converter change. Per D-83-06 the no-change guarantee is relative to Phase 82 output; per D-83-01 an agent with `Skill` in `disallowedTools` keeps `inheritSkills: false` and (ROADMAP success criterion 2) produces byte-identical output. That is only provable if the expected bytes are captured from unmodified HEAD -- same discipline as the 82-01 pre-fix corpus capture (83-RESEARCH.md "Wave 0 Gaps"; the byte-identity corpus itself covers only non-Skill inputs, so this is the one Skill-bearing class needing a fresh capture).

Purpose: later plans key the D-83-04 warning branch and the `inheritSkills` flag on the combined declared-AND-allowed boolean; this pin is the guard against 83-RESEARCH.md Pitfall 1 (branching on declaration alone would give the disallowed input the new wording and break byte-identity).

Output: one new test + one raw-literal whole-file constant in `tests/bridges/agents/convert.test.ts`, green against HEAD, before any source change.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` -- the orchestrator commits. Leave changes staged-ready.
- ASCII-only source literals; never a literal em-dash (U+2014).
- Comment/test-title anchors: `AGSK-05`, `D-83-06`, `D-83-01`, `#86` only -- never `Phase 83`/plan/wave references (`.claude/rules/typescript-comments.md`).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@tests/bridges/agents/convert.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the HEAD fileContent for the declared-but-disallowed input</name>
  <files>(scratch only -- no repo files modified)</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (the `makeDiscovered` builder ~lines 21-32 and the `convertSpecTree` helper ~lines 420-433 -- the capture must mirror their fixed inputs EXACTLY, including the helper's `pi-claude-marketplace-acme-bot` generatedName default)
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (convertAgent input contract; mapTools disallowed handling ~lines 244-292)
    - extensions/pi-claude-marketplace/bridges/agents/types.ts (DiscoveredAgent shape)
  </read_first>
  <action>
    Write a throwaway capture script in your scratchpad directory (NOT in the repo; never commit it). The script imports `convertAgent` from `extensions/pi-claude-marketplace/bridges/agents/convert.ts` (relative file imports run under native TS strip; execute with `node <script>.ts`) and replicates exactly what `convertSpecTree({ description: "d", tools: "Bash, Read, Skill", disallowedTools: "Skill" })` does in convert.test.ts: `pluginName: "spec-tree"`, `pluginRoot: "/root"`, `pluginDataDir: "/data"`, `knownSkills: ["spec-tree-review-changes"]`, `sourceHash: "abc"`, `mapModel: false`, and a discovered object matching `makeDiscovered` defaults with that raw: `sourceName: "bot"`, `generatedName: "pi-claude-marketplace-acme-bot"`, `sourcePath: "/abs/path/source.md"`, `sourceHash: "abc123"`, `body: "Body content."`. Print the resulting `out.fileContent` between unambiguous delimiters (e.g. `=== DISALLOWED ===`).

    Run against the current unmodified HEAD and save the captured output to a scratchpad file for Task 2. Confirm `git status --porcelain` shows no repo modifications from this task.
  </action>
  <verify>
    <automated>node <scratchpad>/capture-disallowed.ts prints one delimited fileContent block and exits 0; git status --porcelain is empty</automated>
  </verify>
  <acceptance_criteria>
    - The printed block starts with `---\n` (generated frontmatter opening) and contains the exact lines `tools: bash,read` and `inheritSkills: false`
    - The provenance comment inside the block contains `droppedTools: Skill` and a warnings line containing the substring `skills discovery disabled` (today's D-82-09 wording -- disallowedTools has zero effect at HEAD because Skill has no TOOL_MAP entry)
    - The captured bytes are pure ASCII (token-free body means no legend, no U+2192 arrow)
    - No file under the worktree repo is created or modified (`git status --porcelain` empty)
  </acceptance_criteria>
  <done>One verbatim fileContent string captured from unmodified HEAD, stored in scratchpad for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the disallowed-direction bytes in convert.test.ts</name>
  <files>tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (the AGSK-03 section ~lines 580-630 -- the new test goes immediately after the "Skill in disallowedTools only is ignored" test ~line 618; whole-file pin shape from the #86 canonical test ~lines 846-901)
    - The captured constant from Task 1 (scratchpad)
  </read_first>
  <action>
    In `tests/bridges/agents/convert.test.ts`, immediately after the existing test "AGSK-03 Skill in disallowedTools only is ignored: no drop entry, no warning" (~line 618), add a module-level constant `DISALLOWED_SKILL_EXPECTED` holding the Task 1 capture as a RAW template literal -- do NOT interpolate `${SKILL_DROP_WARNING}` or any other test constant; the warning line must be literal bytes so later constant renames cannot touch this pin. Add one test titled exactly: `AGSK-05 / D-83-06 Skill declared but disallowed is pinned at pre-AGSK-05 bytes`. The test calls `convertSpecTree({ description: "d", tools: "Bash, Read, Skill", disallowedTools: "Skill" })` and asserts:
    (1) `assert.deepEqual([...out.droppedTools], ["Skill"])` (D-83-02: the drop record is independent of the disallow),
    (2) `assert.match(frontmatterOf(out.fileContent), /^inheritSkills: false$/m)`,
    (3) `assert.equal(out.fileContent, DISALLOWED_SKILL_EXPECTED)` (whole file).

    Add a doc comment above the constant stating the contract: captured from the converter at the pre-AGSK-05 HEAD; per D-83-06 / D-83-01 a Skill-declared-but-disallowed agent must keep these exact bytes (inheritSkills: false and the pre-AGSK-05 warning wording) and this constant must never be edited to make a converter change pass. Anchors: `AGSK-05`, `D-83-06`, `D-83-01`, `#86`. All literals ASCII.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/bridges/agents/convert.test.ts` passes with the new test green against UNMODIFIED source (no extension code changed by this plan)
    - The new test asserts the ENTIRE `out.fileContent` with `assert.equal` against `DISALLOWED_SKILL_EXPECTED`
    - Constant independence (source assertion): the `DISALLOWED_SKILL_EXPECTED` template literal contains no `${` interpolation -- check: `sed -n '/DISALLOWED_SKILL_EXPECTED =/,/^\`;$/p' tests/bridges/agents/convert.test.ts | grep -c '\${'` outputs 0
    - `npm run check` passes (typecheck + ESLint + Prettier + full unit + integration)
    - `git status --porcelain` lists exactly one modified file: `tests/bridges/agents/convert.test.ts`
  </acceptance_criteria>
  <done>Disallowed-direction pin exists, green at HEAD, ready to be the D-83-06 invariant for plans 83-02 and 83-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent frontmatter -> converter | Untrusted tools/disallowedTools strings enter mapTools (exercised read-only by this plan's test) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Tampering | Silent byte drift of the declared-but-disallowed output during the AGSK-05 change | mitigate | This plan IS the mitigation: whole-file exact pin captured at HEAD; plans 83-02/83-03 gate on it staying green (83-RESEARCH.md Pitfall 1 guard) |
| T-83-02 | DoS | Capture/test input triggering a converter throw | accept | Input is a documented safe class (mirrors the existing disallowed-only test); any throw fails the run immediately; test-only code, no runtime surface |

No packages installed; no network; no new attack surface -- plan adds one test to an existing file.
</threat_model>

<verification>
- `node --test tests/bridges/agents/convert.test.ts` green against unmodified HEAD (new pin included)
- `node --test tests/bridges/agents/convert-byte-identity.test.ts` green and file untouched
- `npm run check` green (NFR-6)
- `git status --porcelain` shows only `tests/bridges/agents/convert.test.ts`
</verification>

<success_criteria>
- The D-83-06 disallowed-direction whole-file pin exists, captured pre-change, with a raw-literal constant
- The pin is an exact-equality assertion on the whole generated file plus droppedTools and inheritSkills facets
- Suite green; the pin becomes the ROADMAP success-criterion-2 invariant for the rest of the phase
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-01-SUMMARY.md` when done.
</output>
