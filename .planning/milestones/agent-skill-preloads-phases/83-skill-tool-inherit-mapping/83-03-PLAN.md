---
phase: 83-skill-tool-inherit-mapping
plan: 03
type: execute
wave: 3
depends_on: ["83-01", "83-02"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGSK-05]

must_haves:
  truths:
    - "D-83-01: Skill present in source tools: AND not present in disallowedTools emits `inheritSkills: true`; Skill disallowed or not declared emits `inheritSkills: false`; the omitted-tools default (read,bash,edit) never flips the flag -- no other trigger exists"
    - "D-83-02: Skill remains recorded in droppedTools whether the flag is true or false"
    - "D-83-04: Skill-declaring-and-allowed agents get the new exact-pinned discovery warning (mapping + on-demand + names-differ pointer to the body legend); declared-but-disallowed agents keep the exact Phase 82 D-82-09 wording; the warning stays in the tools slot of the aggregate order"
    - "D-83-05 end to end: a Skill-declaring agent with a known-but-not-emitted body token renders the legend entry `(available on demand)`; the same body without Skill (or with Skill disallowed) renders `(not available in this session)`; preloaded annotation unchanged"
    - "D-83-06 both directions: the 83-01 disallowed whole-file pin and the seven-class byte-identity corpus stay green untouched; the #86 canonical whole-file pin changes exactly as specified (inheritSkills line + warning line only, legend entry still preloaded)"
    - "D-83-07: a pinning test documents that a skill in the emitted skills: list coexists with inheritSkills: true (eager full-content injection + lazy catalog listing under the same Pi name) -- no dedup logic"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      provides: "ToolMappingResult.inheritSkills computed once in mapTools from raw Claude-side tokens; warning wording branch; convertAgent step-8 threading to the emitter"
      contains: "inheritSkills"
    - path: "tests/bridges/agents/convert.test.ts"
      provides: "SKILL_INHERIT_WARNING / SKILL_DROP_WARNING_NO_INHERIT constants; D-83-01 matrix, D-83-04 wording, D-83-05 e2e, D-83-07 duplication pins; updated #86 canonical whole-file pin"
      contains: "SKILL_INHERIT_WARNING"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      via: "convertAgent step 8 passes inheritSkills: toolsResult.inheritSkills into emitGeneratedAgentFile's frontmatter fields"
      pattern: "inheritSkills: toolsResult.inheritSkills"
    - from: "tests/bridges/agents/convert.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      via: "exact-equality pins of both warning wordings and the inheritSkills frontmatter line"
      pattern: "SKILL_INHERIT_WARNING"
---

<objective>
Implement the mapping half of AGSK-05 and close the phase: compute a single `inheritSkills` boolean in `mapTools` (D-83-01: `Skill` declared in `tools:` AND not present in `disallowedTools`), key BOTH the Skill-drop warning wording (D-83-04) and the emitted frontmatter flag on that one boolean, thread it to the plan-83-02 emitter field in `convertAgent` step 8, and pin every ROADMAP success criterion. `Skill` stays in `droppedTools` either way (D-83-02) -- the capability expansion (child sessions gain skill-catalog read access) is user-accepted per D-83-03, with the D-83-04 warning as the disclosure control.

Resolved discretion choices (finalized -- encode exactly, do not revisit):
- D-83-04 warning string, exact bytes (single-quoted TS literal needs `child\'s`; ASCII; lowercase ` -- ` continuation; no trailing period; the legend pointer reads acceptably when the reference-gated legend is absent -- do NOT make the string conditional on legend presence):
  `dropped tool "Skill" -- mapped to Pi skill discovery (inheritSkills: true): installed Pi skills are discoverable in the child's catalog and loadable on demand; catalog names are Pi names, which differ from Claude skill names (see the skill legend in the agent body)`
- Test-constant naming: rename `SKILL_DROP_WARNING` to `SKILL_DROP_WARNING_NO_INHERIT` (bytes unchanged -- still the exact D-82-09 string) and add `SKILL_INHERIT_WARNING` holding the new string.
- Warning-site restructure: lift the Skill push OUT of the `mapToolTokens` loop into `mapTools` after the flag is computed (the loop currently fires before `disallowedTools` is read -- 83-RESEARCH.md's ordering subtlety). Order is preserved: the only other tools-slot warning (omitted-`tools:`) can never co-occur with a Skill declaration because the default tokens contain no Skill.
- Disallowed detection reads RAW Claude-side tokens via the existing `splitCsv` (exact match, case-sensitive `"Skill"`, consistent with TOOL_MAP lookup convention); the existing Pi-name disallow filter can never see Skill because Skill has no TOOL_MAP entry (83-RESEARCH.md Pitfall 3). Compute `splitCsv(rawDisallowed)` once and reuse it in the existing filter block.

Output: convert.ts flag computation + warning branch + threading; convert.test.ts sanctioned carve-outs updated and all new D-83 pins added; `npm run check` green as the phase gate.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` -- the orchestrator commits.
- ASCII-only source literals; ` -- ` never an em-dash (U+2014); any arrow as the `→` escape.
- Do NOT touch `tests/bridges/agents/convert-byte-identity.test.ts` or the 83-01 `DISALLOWED_SKILL_EXPECTED` pin/constant -- if either fails, the implementation is wrong (83-RESEARCH.md Pitfalls 1-2).
- Comment/test-title anchors: `AGSK-05`, `D-83-01`..`D-83-07`, `D-82-09` (where the old wording is referenced), `#86`, `SC-N` -- never `Phase 83`/plan/wave references (`.claude/rules/typescript-comments.md`).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-PATTERNS.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-02-SUMMARY.md
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute the inherit flag, branch the warning, thread to the emitter</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (ToolMappingResult ~74-78; splitCsv ~80-109; mapToolTokens ~221-242 with the in-loop Skill push and the AGSK-03/D-82-08/D-82-09 doc comment ~214-220; mapTools ~244-292 including the default-tokens branch and the Pi-name disallow filter; convertAgent step 3 ~465 and step 8 emit call ~513-533)
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (the GeneratedFrontmatterFields.inheritSkills? contract added by the emitter plan -- confirm field name and default-false semantics before threading)
    - tests/bridges/agents/convert.test.ts (SKILL_DROP_WARNING constant ~586; the four carve-out tests: exact-warning ~589-594, aggregate order ~620-630, #86 canonical whole-file pin ~846-901, no-body-token twin ~903-916; the DISALLOWED_SKILL_EXPECTED pin from the Wave 1 plan -- read to confirm you must NOT touch it)
    - 83-PATTERNS.md "bridges/agents/convert.ts" section (exact seams and the compute-once/reuse note for splitCsv)
  </read_first>
  <behavior>
    All via existing helpers (convertSpecTree / frontmatterOf) unless noted; write these first, confirm RED, then implement:
    - Test A (D-83-01 declared+allowed, D-83-02): `convertSpecTree({ description: "d", tools: "Bash, Read, Skill" })` -> `assert.match(frontmatterOf(...), /^inheritSkills: true$/m)`, `assert.deepEqual([...out.droppedTools], ["Skill"])`, warnings include `SKILL_INHERIT_WARNING` and do NOT include `SKILL_DROP_WARNING_NO_INHERIT`
    - Test B (D-83-01 declared+disallowed, D-83-04 old wording): `convertSpecTree({ description: "d", tools: "Bash, Read, Skill", disallowedTools: "Skill" })` -> `/^inheritSkills: false$/m`, warnings include `SKILL_DROP_WARNING_NO_INHERIT` and NOT `SKILL_INHERIT_WARNING` (the 83-01 whole-file pin remains the byte-level guard)
    - Test C (D-83-01 not declared): `convertSpecTree({ description: "d", tools: "Read,Bash" })` -> `/^inheritSkills: false$/m` and no warning starting with `dropped tool`
    - Test D (D-83-01 omitted tools): `convertSpecTree({ description: "d" })` -> `/^inheritSkills: false$/m` (default read,bash,edit never flips the flag); existing omitted-tools warning assertion unchanged
    - Carve-outs updated BY DESIGN (D-83-06 sanctioned -- deliberate edits, not collateral): the exact-warning test ~589-594 asserts `SKILL_INHERIT_WARNING`; the aggregate-order test ~620-630 expects `[SKILL_INHERIT_WARNING, crossPluginSkillWarning("other-plugin:x")]` (order contract itself unchanged); the #86 canonical constant ~846-901 flips exactly two byte ranges -- `inheritSkills: false` -> `inheritSkills: true` and the warnings interpolation `${SKILL_DROP_WARNING}` -> `${SKILL_INHERIT_WARNING}` -- while the legend entry line stays `(preloaded in your context)` per D-83-05; the twin ~903-916 swaps its warning-membership constant to `SKILL_INHERIT_WARNING`
  </behavior>
  <action>
    In convert.ts:
    (1) Extend `ToolMappingResult` with `readonly inheritSkills: boolean;`.
    (2) In `mapTools`, compute `disallowedTokens = splitCsv(rawDisallowed)` ONCE near the top and reuse that array in the existing Pi-name filter block (replace its own splitCsv call); compute `skillDeclared = tokens.includes("Skill")` on the post-default token list and `inheritSkills = skillDeclared && !disallowedTokens.includes("Skill")` -- raw Claude-side token check, exact match, case-sensitive (83-RESEARCH.md Assumption A1; note the case convention in a comment).
    (3) Remove the `token === "Skill"` push from the `mapToolTokens` loop (the function returns to pure mapping/dropping); after the `mapToolTokens` call in `mapTools`, when `skillDeclared` is true push exactly one warning chosen by the boolean: `inheritSkills` true -> the new D-83-04 string from the objective; false -> the existing D-82-09 string byte-unchanged. Extend (do not contradict) the ~214-220 doc comment: it now documents both wordings and that the branch keys on declared-AND-allowed (anchors: AGSK-03, AGSK-05, D-82-09, D-83-01, D-83-04).
    (4) Return `inheritSkills` in the result; in `convertAgent` step 8 add `inheritSkills: toolsResult.inheritSkills` to the frontmatter object passed to `emitGeneratedAgentFile` (plain field, no optional-spread helper -- the emitter defaults undefined to false and always-passing the boolean is byte-safe). `ConvertedAgent`/types.ts stay unchanged; the droppedTools plumbing is untouched (D-83-02 is free).

    In convert.test.ts: rename `SKILL_DROP_WARNING` -> `SKILL_DROP_WARNING_NO_INHERIT` (same bytes; update every reference EXCEPT the raw-literal `DISALLOWED_SKILL_EXPECTED` constant, which contains no interpolation by design); add `SKILL_INHERIT_WARNING` with the objective's exact string (doc comment anchored D-83-04); add Tests A-D and update the four carve-outs as specified in the behavior block.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/frontmatter.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests A-D pass; the AGSK-05 / D-83-06 disallowed pin passes with its constant byte-unchanged (source assertion: `git diff tests/bridges/agents/convert.test.ts` contains no hunk touching `DISALLOWED_SKILL_EXPECTED` or its test body)
    - The exact new warning string appears once in convert.ts and once in convert.test.ts: `grep -c 'mapped to Pi skill discovery' extensions/pi-claude-marketplace/bridges/agents/convert.ts tests/bridges/agents/convert.test.ts` reports 1 for each file
    - The D-82-09 string is still present byte-identical in convert.ts (grep for `skills discovery disabled` returns exactly 1 match in convert.ts)
    - splitCsv is called on rawDisallowed exactly once inside mapTools (source assertion: one `splitCsv(rawDisallowed)` occurrence in convert.ts)
    - The #86 canonical whole-file pin passes with `inheritSkills: true` and the new warning line, and its legend line still reads `(preloaded in your context)`
    - Byte-identity corpus green and untouched (`git diff --exit-code`)
  </acceptance_criteria>
  <done>The single boolean drives flag emission and warning wording; all four sanctioned carve-outs updated; both byte-identity guards (corpus + disallowed pin) green untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Legend on-demand e2e pins, D-83-07 duplication pin, and phase gate</name>
  <files>tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (legendEntryLine helper ~638-642; convertSpecTreeWithBody ~644-658; the two-state analog test "known-but-not-emitted skill token annotates not available" ~673-686; the stale disallowed-only comment ~612-613)
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (renderSkillLegend third-state implementation from the emitter plan -- the exact annotation strings being asserted end to end)
  </read_first>
  <action>
    All additions to convert.test.ts (behavior already lands via Task 1 + the emitter plan, so these pins should pass on first run -- they are the AGSK-05 end-to-end contract, not RED/GREEN cycles):

    (1) Refactor `legendEntryLine(token, generatedName, preloaded)` so the third parameter is the annotation TEXT (one of the exact strings `preloaded in your context`, `not available in this session`, `available on demand`) instead of a boolean; update existing call sites mechanically -- every previously pinned expected line must stay byte-identical (arrow stays the `→` escape).
    (2) D-83-05 on-demand e2e: `convertSpecTreeWithBody({ description: "d", tools: "Read, Skill" }, "Consult spec-tree:review-changes when needed.")` (skills: absent, so the known skill is not emitted) -> fileContent includes `legendEntryLine("spec-tree:review-changes", "spec-tree-review-changes", "available on demand")` and the frontmatter matches `/^inheritSkills: true$/m`.
    (3) D-83-05 both directions: same body with `tools: "Read"` -> the entry line carries `not available in this session`; and with `tools: "Read, Skill", disallowedTools: "Skill"` -> also `not available in this session` (inherit off means the Phase 82 two-state legend, even though Skill was declared).
    (4) D-83-07 duplication pin: `convertSpecTree({ description: "d", tools: "Read, Skill", skills: "spec-tree:review-changes" })` -> frontmatter matches BOTH `/^skills: spec-tree-review-changes$/m` AND `/^inheritSkills: true$/m`. Test title: `AGSK-05 / D-83-07 a preloaded skill also remains discoverable in the inherited catalog (accepted duplication)`. Doc comment: the skill is eagerly full-content injected via skills: AND lazily listed in the child's inherited catalog under the same Pi name; accepted behavior, no dedup logic (anchor: D-83-07).
    (5) Refresh the stale comment at ~612-613 in the disallowed-only test: replace the "out of scope here (AGSK-05)" sentence with a statement of the landed semantics -- Skill absent from tools: means the flag stays false per D-83-01; disallowedTools alone never flips it (comment-only edit; the test's assertions are unchanged).

    Then run the phase gate: full `npm run check`, and confirm the corpus and the disallowed pin are untouched.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - The on-demand e2e pin asserts the exact rendered entry line (arrow escape + `(available on demand)`) AND `/^inheritSkills: true$/m` in the same output
    - Both opposite-direction pins assert the exact `not available in this session` annotation (no-Skill and Skill-disallowed inputs)
    - The D-83-07 test asserts both frontmatter lines in one output and its comment names eager injection + lazy catalog coexistence
    - The refreshed ~612-613 comment no longer contains the phrase `out of scope`
    - `npm run check` exit 0 (NFR-6 phase gate); `git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts` exit 0
    - `git status --porcelain` for this plan shows changes confined to `extensions/pi-claude-marketplace/bridges/agents/convert.ts` and `tests/bridges/agents/convert.test.ts`
  </acceptance_criteria>
  <done>All four ROADMAP success criteria pinned end to end; duplication edge documented; quality gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent frontmatter -> mapTools | Untrusted tools:/disallowedTools: strings decide the inherit flag |
| generated agent -> child Pi session | inheritSkills: true grants the child read access to the user's installed Pi skill catalog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-06 | Elevation of privilege | Child sessions gain skill-catalog discovery/read access | accept | Deliberate, user-accepted capability parity per D-83-03; scoped to agents that explicitly declared the Skill tool and did not disallow it (D-83-01); the D-83-04 warning is the disclosure control; install-time opt-out consciously deferred (CONTEXT deferred idea -- do not implement) |
| T-83-07 | Tampering | Warning-string injection into the provenance comment | mitigate | Both wordings are compile-time ASCII literals routed through the existing sanitizeProvenance path (`-->` escaping); no user data enters the strings |
| T-83-08 | Spoofing | Disallow bypass: checking disallowedTools via the TOOL_MAP Pi-name path would make `disallowedTools: Skill` a silent no-op | mitigate | Raw-token `splitCsv(rawDisallowed).includes("Skill")` check (Pitfall 3); Test B + the 83-01 whole-file pin prove the disallowed direction |
| T-83-09 | Tampering | Byte drift for non-Skill agents | mitigate | Byte-identity corpus + 83-01 disallowed pin gate both tasks' verify; canonical pin constrains the Skill-direction delta to exactly two byte ranges |

No packages installed; no network; no new I/O surface.
</threat_model>

<verification>
- `node --test tests/bridges/agents/convert.test.ts`, `node --test tests/bridges/agents/frontmatter.test.ts`, `node --test tests/bridges/agents/convert-byte-identity.test.ts` all green
- `git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts` exit 0 (corpus survived the phase)
- The 83-01 `DISALLOWED_SKILL_EXPECTED` pin green with constant untouched
- `npm run check` green (NFR-6)
</verification>

<success_criteria>
- ROADMAP SC-1: Skill-declaring agents (the #86 canonical included) convert with `inheritSkills: true`, `Skill` in droppedTools, and the exact D-83-04 discovery warning -- whole-file pinned
- ROADMAP SC-2: Skill-disallowed and non-declaring agents keep `inheritSkills: false` byte-identically to Phase 82 (83-01 pin + corpus green untouched)
- ROADMAP SC-3: known-but-not-preloaded skills annotate `(available on demand)` for Skill-declaring agents, `(not available in this session)` otherwise -- pinned end to end
- ROADMAP SC-4: the eager+lazy duplication edge documented by the D-83-07 pinning test
- `npm run check` green; AGSK-05 complete
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-03-SUMMARY.md` when done.
</output>
