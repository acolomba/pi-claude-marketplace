---
phase: 83.1-silent-skill-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGSK-03, AGSK-05]

must_haves:
  truths:
    - "D-83.1-01: converting a Skill-declaring agent yields warnings deepEqual [] in every branch -- both the phase-82 string ('skills discovery disabled') and the phase-83 string ('mapped to Pi skill discovery') are gone from extensions/ and tests/; the sweep grep -rn -a over both trees returns zero lines"
    - "D-83.1-02: Skill never appears in droppedTools -- the allowed branch converts with droppedTools [] and inheritSkills: true, the disallowed branch converts with droppedTools [] and inheritSkills: false, and genuinely unmapped tools (WebFetch) keep their droppedTools entries via the unchanged-green guard test"
    - "D-83.1-04: DISALLOWED_SKILL_EXPECTED and the #86 canonical whole-file pins are deliberately re-pinned to droppedTools: (none) / warnings: (none) with every other byte of each constant untouched; convert-byte-identity.test.ts has a zero-line diff and all seven corpus classes stay green"
    - "inheritSkills computation and emission are fenced: the computation-branch tests (convert.test.ts ~715-738) and the emission pins (frontmatter.test.ts ~446-487) pass with zero edits"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      provides: "mapToolTokens Skill exclusion (Seam A) and warning-free mapTools"
      contains: "token === \"Skill\""
    - path: "tests/bridges/agents/convert.test.ts"
      provides: "silent-conversion assertions + deliberately re-pinned whole-file constants"
      contains: "droppedTools: (none)"
  key_links:
    - from: "tests/bridges/agents/convert.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      via: "convertSpecTree helper driving convertAgent; exact-equality pins on out.droppedTools / out.warnings / out.fileContent"
      pattern: "convertSpecTree"
    - from: "extensions/pi-claude-marketplace/bridges/agents/convert.ts (mapToolTokens dropped array)"
      to: "provenance comment, ConvertedAgent.droppedTools, agents-index.json, install notify"
      via: "single upstream classification point (Seam A) -- no downstream filters needed"
      pattern: "mapToolTokens"
---

<objective>
Make the `Skill` tool convert silently (amended AGSK-03 / AGSK-05): remove both Skill-specific provenance warnings and exclude `Skill` from `droppedTools` in every branch, then re-pin the affected convert.test.ts fixtures to the silent contract. Per D-83.1-01 there is nothing to warn about -- declared-and-allowed maps to `inheritSkills: true` (reproducing Claude's Skill-tool capability) and declared-and-disallowed suppresses it exactly as Claude Code does (disallowed wins). Per D-83.1-02, `Skill` was translated, not dropped, so it never belongs in `droppedTools`; a single exclusion at the `mapToolTokens` classification seam (Seam A, the RESEARCH recommendation adopted under the CONTEXT discretion grant) silences all four downstream consumers -- provenance comment, `ConvertedAgent.droppedTools`, `agents-index.json`, install notify -- with zero further edits.

The `inheritSkills` computation, threading, and emission MUST NOT change (phase boundary). Legend rendering is NOT touched in this plan -- that is plan 83.1-02.

Purpose: generated output stops overclaiming isolation that real child sessions do not have (phase 83 UAT finding).
Output: silent `convert.ts`, re-pinned `convert.test.ts`, full `npm run check` green.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` -- the orchestrator commits. Leave changes staged-ready.
- ASCII-only source literals; never a literal em-dash (U+2014); `--` (double hyphen) is the separator convention in prose strings.
- Comment/test-title anchors: `AGSK-03`, `AGSK-04`, `AGSK-05`, `D-83.1-01`..`D-83.1-04`, still-valid `D-83-01`, `D-83-07`, `D-82-01`..`D-82-07`, `#86` only -- never `Phase 83.1`/plan/wave references (`.claude/rules/typescript-comments.md`). RETIRE superseded anchors from every edited region: `D-82-08`, `D-82-09`, `D-83-02`, `D-83-04`, `D-83-06`.
- New/rewritten comments must NOT quote the retired warning strings verbatim -- the phase sweep greps must reach zero.
- All verification greps use `grep -a` (macOS BSD grep silently skips glyph-bearing files as binary).
- Line numbers below are current-HEAD anchors verified 2026-07-19; if the branch has moved, locate regions by the quoted strings/test titles instead.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-CONTEXT.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@tests/bridges/agents/convert.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Silence Skill in convert.ts (Seam A exclusion + warning block removal)</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (TOOL_MAP ~L54-62; mapToolTokens doc comment + body ~L216-240; mapTools skillDeclared/inheritSkills computation ~L262-270; warning block ~L274-284; return sites ~L297-312; AG-11 throw ~L488-497)
    - .planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-RESEARCH.md (Current State Map items 1-2 and Code Examples -- the verified target shape for mapToolTokens)
    - .claude/rules/typescript-comments.md (anchor policy for the rewritten comments)
  </read_first>
  <action>
    Make exactly three edits to convert.ts; nothing else in the file changes.

    (1) Seam A exclusion (D-83.1-02, discretion call resolved per the RESEARCH recommendation): inside `mapToolTokens` (~L227-240), before the `TOOL_MAP[token]` lookup in the for-loop, insert an exact-match skip -- `if (token === "Skill") { continue; }` -- so `Skill` is neither mapped nor dropped. Exact, case-sensitive comparison, consistent with the existing `tokens.includes("Skill")` check at ~L269 and the file's "exact match" TOOL_MAP semantics. Precede it with a comment anchored `AGSK-03 / D-83.1-02` stating Skill is translated to inheritSkills (computed in mapTools from raw tokens), never dropped -- no droppedTools entry and no warning in any branch.

    (2) Warning removal (D-83.1-01): delete lines ~274-284 wholesale -- the comment block beginning "AGSK-03 / AGSK-05: the Skill drop is the only explained drop" AND the entire `if (skillDeclared) { warnings.push(...) }` statement containing both string literals (the phase-83 string starting `'dropped tool "Skill" -- mapped to Pi skill discovery (inheritSkills: true): ...'` at ~L281 and the phase-82 string starting `'dropped tool "Skill" -- generated agents run with skills discovery disabled (inheritSkills: false); ...'` at ~L282). Do NOT delete `const skillDeclared` (~L269) -- it still feeds the kept `inheritSkills` computation at ~L270; the typecheck catches over-deletion.

    (3) Doc-comment rewrite: the `mapToolTokens` doc comment (~L216-226) narrates the now-false "ONLY the Skill drop is explained" strategy and cites superseded `D-82-08` / `D-82-09` / `D-83-04`. Rewrite it to the silent-translation contract: `Skill` is excluded from classification entirely (neither mapped nor dropped) because it translates to the `inheritSkills` flag computed in `mapTools`; every other TOOL_MAP miss still records a `droppedTools` entry. Anchors: `AGSK-03`, `AGSK-05`, `D-83.1-01`, `D-83.1-02`. Do not quote either retired warning string in the new comment.

    FENCED -- must not change: `ToolMappingResult.inheritSkills` (~L74-80), the inheritSkills computation (~L262-270; its comment cites `D-83-01`, which is still valid -- keep), both `mapTools` return sites (~L297-312), the threading at ~L542, and the AG-11 empty-mapped-list throw (~L488-497; an agent declaring ONLY `Skill` still throws -- pre-existing, out of scope).

    Expected intermediate state: `tests/bridges/agents/convert.test.ts` goes red after this task (its pins still assert the old contract) -- that is intentional; Task 2 re-pins it. The byte-identity corpus and frontmatter suites must stay green NOW (no corpus class declares Skill; frontmatter.ts is untouched).
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/bridges/agents/convert-byte-identity.test.ts tests/bridges/agents/frontmatter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn -a "skills discovery disabled\|mapped to Pi skill discovery" extensions/` outputs zero lines (both warning strings gone from source)
    - `grep -n -a 'token === "Skill"' extensions/pi-claude-marketplace/bridges/agents/convert.ts` finds the Seam A skip inside mapToolTokens
    - `grep -n -a "skillDeclared" extensions/pi-claude-marketplace/bridges/agents/convert.ts` still shows both the declaration and its use in the inheritSkills computation (neither deleted)
    - `grep -n -a "D-82-08\|D-82-09\|D-83-04" extensions/pi-claude-marketplace/bridges/agents/convert.ts` outputs zero lines (superseded anchors retired); `grep -n -a "D-83-01" extensions/pi-claude-marketplace/bridges/agents/convert.ts` still matches (valid anchor kept)
    - `npm run typecheck` exits 0
    - `node --test tests/bridges/agents/convert-byte-identity.test.ts` green AND `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts` prints nothing (zero-line diff)
    - `node --test tests/bridges/agents/frontmatter.test.ts` green with zero edits to that file
    - `git status --porcelain` lists exactly one modified file: `extensions/pi-claude-marketplace/bridges/agents/convert.ts`
  </acceptance_criteria>
  <done>convert.ts emits no Skill warning and records no Skill droppedTools entry in any branch; inheritSkills computation/threading byte-untouched; corpus + frontmatter suites green; convert.test.ts intentionally red pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin convert.test.ts to the silent-conversion contract</name>
  <files>tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (the AGSK-03 section ~L581-738; the DISALLOWED_SKILL_EXPECTED constant + test ~L632-674; the #86 canonical test + twin ~L1038-1112 -- these regions receive every edit in this task)
    - .planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-PATTERNS.md (sections "Deliberate re-pin of whole-file constants" -- the commit 4a62c64c mechanics to copy -- and "Exact-equality assertion style" and "Comment anchors")
    - .planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-RESEARCH.md (Current State Map item 5, the full change-inventory table)
  </read_first>
  <action>
    Apply the following change inventory to convert.test.ts. Locate each region by the quoted title/string (line numbers are pre-edit hints). Assertion style throughout: spread-copy readonly arrays before deepEqual (`assert.deepEqual([...out.droppedTools], [])`), `assert.match(frontmatterOf(out.fileContent), /^key: value$/m)` for single frontmatter lines, `assert.equal(out.fileContent, CONSTANT)` for whole-file pins. Keep asserting on the RESULT record (`out.droppedTools` / `out.warnings`), not only fileContent -- stage/index tests use empty fixtures and would never catch a record/comment divergence.

    (a) Section header comment (~L581-583, "AGSK-03 / D-82-08 / D-82-09: Skill-drop provenance warning in tools mapping"): rewrite to a silent-conversion header anchored `AGSK-03 / D-83.1-01 / D-83.1-02`.

    (b) Delete both warning constants with their doc comments: `SKILL_DROP_WARNING_NO_INHERIT` (~L585-591) and `SKILL_INHERIT_WARNING` (~L593-598). Every remaining reference to either identifier must be rewritten in this task -- the typecheck finds stragglers.

    (c) Test "AGSK-03 / AGSK-05 / D-83-04 dropping an allowed Skill tool emits the exact discovery warning" (~L600-605): retitle to the silent claim (e.g. "AGSK-03 / AGSK-05 / D-83.1-01 an allowed Skill tool converts silently: no droppedTools entry, no warning"). Same input (`convertSpecTree({ description: "d", tools: "Bash, Read, Skill" })`); assertions become `assert.deepEqual([...out.droppedTools], [])`, `assert.deepEqual([...out.warnings], [])` (input has description + tools, so no other warning fires), and keep the `assert.match(frontmatterOf(out.fileContent), /^tools: bash,read$/m)` pin unchanged.

    (d) UNCHANGED -- zero edits: "non-Skill drops stay silent" WebFetch guard (~L607-611, the SC-4 genuinely-unmapped pin), omitted-tools implicit-default warning (~L613-620), "Skill in disallowedTools only is ignored" (~L622-630).

    (e) `DISALLOWED_SKILL_EXPECTED` (~L632-662) -- the D-83.1-04 deliberate re-pin. The old guard comment says "pinned at pre-AGSK-05 bytes ... never edit"; it was pinned to the superseded contract and this re-pin is the sanctioned exception. Per the 4a62c64c mechanics: edit ONLY the two contract-changed lines inside the raw template literal -- `droppedTools: Skill` becomes `droppedTools: (none)` and the `warnings: dropped tool "Skill" -- generated agents run with skills discovery disabled ...` line becomes `warnings: (none)` -- leaving every other byte untouched (including `inheritSkills: false`). Rewrite the doc comment to state it now pins the amended-AGSK-03 silent contract (a Skill-declared-but-disallowed agent converts with no Skill warning, no droppedTools entry, inheritSkills: false -- Claude-consistent disallowed-wins suppression), keep the "raw literal on purpose, no interpolation, never edit to make a converter change pass" clause, anchors `AGSK-03`, `AGSK-05`, `D-83.1-01`, `D-83.1-02`, `D-83.1-04`, `#86`. Its test (~L664-674): change `assert.deepEqual([...out.droppedTools], ["Skill"])` to `[]`, keep the `/^inheritSkills: false$/m` match and the whole-file `assert.equal` against the re-pinned constant, retitle dropping `D-83-06` and "pre-AGSK-05 bytes".

    (f) Warnings-order test (~L676-689, expects `[SKILL_INHERIT_WARNING, crossPluginSkillWarning(...)]`): keep the test and input; the expected array becomes exactly `[crossPluginSkillWarning("other-plugin:x")]`. Retitle to state Skill contributes zero warnings even alongside other warnings (anchors `AGSK-03 / D-83.1-01`).

    (g) Test "D-83-02 ... keeps the drop record" (~L695-702): keep the `inheritSkills: true` assertion (~L697); droppedTools expectation becomes `[]`; replace both warning-includes assertions (~L700-701) with `assert.deepEqual([...out.warnings], [])`; retitle with `D-83.1-02` (D-83-02 is superseded -- e.g. "AGSK-05 / D-83.1-02 Skill declared and allowed: inheritSkills true, no drop record, no warning").

    (h) Test "Skill declared but disallowed keeps inheritSkills: false and the no-inherit wording" (~L704-713): keep the `inheritSkills: false` assertion (~L710); replace both warning-includes assertions (~L711-712) with `assert.deepEqual([...out.warnings], [])`; retitle dropping the wording claim (e.g. "AGSK-03 / D-83.1-01 Skill declared but disallowed: inheritSkills false, silent").

    (i) UNCHANGED -- zero edits (fenced inheritSkills guards): ~L715-724 and ~L729-738 (D-83-07 duplication).

    (j) `#86 canonical` whole-file test (~L1038-1097) -- the SC-1 re-pin and the CONTEXT "Specific Ideas" target. Assertions: `assert.deepEqual([...out.droppedTools], ["Skill"])` (~L1053) becomes `[]`; the `assert.ok(out.warnings.includes(SKILL_INHERIT_WARNING))` (~L1054) becomes `assert.deepEqual([...out.warnings], [])`. Pin lines inside the expected constant: `droppedTools: Skill` (~L1084) becomes `droppedTools: (none)`; `warnings: ${SKILL_INHERIT_WARNING}` (~L1085) becomes the literal `warnings: (none)` (the interpolation goes away with the deleted constant). `inheritSkills: true` (~L1075), the `droppedFields: (none)` line, and the preloaded legend entry line (~L1092) stay byte-identical. Retitle: remove ", warning," and any superseded anchors; keep `#86` and AGSK anchors.

    (k) Canonical twin without body token (~L1099-1112): droppedTools expectation (~L1107) becomes `[]`; replace the `SKILL_INHERIT_WARNING` includes-assertion (~L1108) with `assert.deepEqual([...out.warnings], [])`.

    Do NOT touch: the legend regions (`LegendAnnotation` union ~L747-748, annotation tests ~L792-937) -- they belong to plan 83.1-02 and stay green because frontmatter.ts is unchanged; the helpers `convertSpecTree`, `convertSpecTreeWithBody`, `frontmatterOf`, `legendEntryLine`, `crossPluginSkillWarning`; and `tests/bridges/agents/convert-byte-identity.test.ts` (zero edits, ever).
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts` all green
    - `grep -rn -a "skills discovery disabled\|mapped to Pi skill discovery" extensions/ tests/` outputs zero lines (both retired strings gone from code AND tests)
    - `grep -c -a "SKILL_INHERIT_WARNING\|SKILL_DROP_WARNING_NO_INHERIT" tests/bridges/agents/convert.test.ts` outputs 0 (constants and all references gone)
    - The `DISALLOWED_SKILL_EXPECTED` literal contains the exact lines `droppedTools: (none)` and `warnings: (none)`, still contains `inheritSkills: false`, and has no interpolation: `sed -n '/DISALLOWED_SKILL_EXPECTED = /,/^\`;$/p' tests/bridges/agents/convert.test.ts | grep -c '\${'` outputs 0
    - The #86 canonical expected constant contains `droppedTools: (none)`, `warnings: (none)`, and `inheritSkills: true`
    - `grep -n -a "D-82-08\|D-82-09\|D-83-02\|D-83-04\|D-83-06\|pre-AGSK-05" tests/bridges/agents/convert.test.ts` outputs zero lines (superseded anchors retired from all edited regions)
    - `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts` prints nothing (D-83.1-04 corpus untouched)
    - `npm run check` green (typecheck + ESLint + Prettier + unit + integration -- NFR-6)
    - `git status --porcelain` lists exactly two modified files: `extensions/pi-claude-marketplace/bridges/agents/convert.ts` and `tests/bridges/agents/convert.test.ts`
  </acceptance_criteria>
  <done>Silent-conversion contract fully pinned: allowed and disallowed Skill branches both assert empty droppedTools/warnings, whole-file pins re-captured per D-83.1-04, WebFetch guard and fenced inheritSkills tests green with zero edits, full check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent frontmatter -> converter | Untrusted tools/disallowedTools strings enter mapTools; this plan narrows what the converter REPORTS about them, not how it parses them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83.1-01 | Repudiation | provenance comment / agents-index.json (Skill warning + droppedTools entry removed) | accept | Deliberate, UAT-validated intent: the removed signal overclaimed isolation that real child sessions do not have; worst case a user loses a redundant provenance line. No security boundary crossed; genuinely unmapped tools keep their droppedTools entries (guard test) |
| T-83.1-02 | Tampering | Byte drift of non-Skill agent output during the convert.ts edit | mitigate | Seven-class byte-identity corpus must stay green with a zero-line diff (D-83.1-04); WebFetch droppedTools guard test unchanged-green |

No packages installed; no network; no user-input parsing changes; no privilege boundaries. No high-severity threats identified.
</threat_model>

<verification>
- `node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts` green
- `grep -rn -a "skills discovery disabled\|mapped to Pi skill discovery" extensions/ tests/` returns zero lines
- `git diff --stat -- tests/bridges/agents/convert-byte-identity.test.ts` empty
- `npm run check` green (NFR-6)
- `git status --porcelain` shows only convert.ts and convert.test.ts
</verification>

<success_criteria>
- SC-1: a Skill-declaring agent converts with `droppedTools: (none)`, `warnings: (none)`, `inheritSkills: true` (re-pinned #86 canonical constant + unit assertions on the result record)
- SC-2: a Skill declared-and-disallowed agent converts with `droppedTools: (none)`, `warnings: (none)`, `inheritSkills: false` (re-pinned DISALLOWED_SKILL_EXPECTED)
- SC-4 (provenance half): WebFetch/multi-unknown droppedTools tests unchanged-green; all seven byte-identity corpus classes green with zero edits to the corpus file
- inheritSkills computation/emission fenced tests pass with zero edits
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/83.1-silent-skill-mapping/83.1-01-SUMMARY.md` when done.
</output>
