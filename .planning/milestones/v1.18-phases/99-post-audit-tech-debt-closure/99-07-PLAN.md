---
phase: 99-post-audit-tech-debt-closure
plan: 07
type: execute
wave: 5
depends_on: ["99-06"]
files_modified:
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/install.test.ts
autonomous: true
requirements: [D-99-05b]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low # no calibration samples in .planning/; factor 1.0 applied

must_haves:
  truths:
    - "The residual uncovered rare-failure arms are MEASURED against the tree as this phase leaves it, not assumed from the 2026-06-12 capture (D-99-05b)."
    - "With this plan complete, all four debt groups the operator pulled into scope are closed — none re-deferred (D-99-01)."
    - "The three in-scope modules — update, reinstall and install — gain coverage on their measured residual failure and rollback arms, biggest absolute chunk first."
    - "The two modules and the wiring module the carrier also names are recorded as explicit NON-goals of D-99-05b, not silently dropped, and the coverage-exclusion question is re-filed as a fresh todo rather than decided here."
    - "No test duplicates an arm an earlier phase already covered."
  artifacts:
    - "a measured residual-arm list, recorded in the plan summary (the coverage report itself is a build output, not a committed artifact)"
    - "new failure-arm cases in the update, reinstall and install suites"
    - "a fresh todo carrying the wiring-module coverage-versus-exclusion decision"
  key_links:
    - "the coverage run -> the per-file line data in the emitted unit lcov report -> the residual arm list -> the cases written in tasks 2 and 3"
    - "the carrier's named scope -> D-99-05b's bound (update / reinstall / install only) -> the recorded non-goals"
  prohibitions:
    - "Do NOT write a single test before the measurement lands. The captured percentages are 2026-06-12 vintage and four phases of coverage work have landed on exactly these three files since; writing from the stale table is pure waste."
    - "Do NOT expand past the bound. The marketplace-update orchestrator, the import execute path and the dependency-wiring module are named by the carrier but explicitly OUTSIDE D-99-05b's locked scope. Record them; do not test them."
    - "Do NOT add a coverage-exclusion entry to the Sonar configuration. An exclusion inflates the metric without adding safety, and the carrier requires it be an explicit call — that call belongs to a fresh todo, not to this plan."
    - "Do NOT copy an existing test body to cover an adjacent arm. The identical-function lint rule will reject it, and a near-duplicate hides which arm actually regressed."
    - "Do NOT chase a coverage percentage. The deliverable is the named rare-failure and rollback arms covered, not a number; state which arms were covered and which measured-uncovered lines were deliberately left, with a reason."
    - "Run the gate with the companion-peer root exported. Without it, the two companion-extension integration tests resolve a stale global peer and fail locally for environmental reasons unrelated to the change."
---

<objective>
Measure the residual uncovered rare-failure arms in the update, reinstall and install orchestrators
as this phase leaves them, then cover the arms the measurement actually names, within the bound
D-99-05b locks (D-99-05b).

Purpose: the carrier's per-file table is more than a year old and predates four phases of coverage
work aimed at exactly these three modules. The honest scope cannot be produced by reading source —
it requires the coverage run — so this plan is deliberately measure-then-scope, and it runs LAST so
the measurement reflects every change the phase made rather than a moving target. The arms
themselves are the ones where a silent regression hurts most: failure and rollback paths that no
happy-path test reaches.

Output: a recorded residual list, cases for the arms inside the bound, and a fresh todo carrying
the one decision the bound excludes.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-post-audit-tech-debt-closure/99-CONTEXT.md
@.planning/phases/99-post-audit-tech-debt-closure/99-RESEARCH.md
@.planning/todos/pending/2026-06-12-coverage-sweep-test-rare-failure-arms-in-update-reinstall-in.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure the residual and fix the scope</name>
  <files>tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
    - The carrier todo in full: its per-file table, its ordering instruction (biggest absolute chunk
      first), and the wiring-module question it leaves open.
    - `grep -n "test:coverage:unit" package.json` — confirm the script and the report destination
      before running it; the research read it as emitting an lcov report under the coverage
      directory.
  </read_first>
  <precondition>
    The companion-peer root is exported for the run; without it two integration tests resolve a
    stale global peer and fail for environmental reasons. Halt and report if the peer path does not
    exist rather than running the gate against a stale global.
  </precondition>
  <action>
    Run the unit coverage script and read the per-file line data for the three in-scope modules out
    of the emitted lcov report. Produce the residual list: for each of the three modules, which
    failure and rollback arms are still uncovered, expressed as named arms (what the branch does),
    not as bare line numbers, so the list survives the next edit to the file.

    Order the list by absolute uncovered size, biggest first, per the carrier.

    Record two things explicitly in this task's commit message body and later in the summary:
    1. The residual list as measured, with the measurement date, so the next reader can tell a
       covered arm from a re-measured one.
    2. The non-goals. The marketplace-update orchestrator and the import execute path are named by
       the carrier but sit outside D-99-05b's locked bound; the dependency-wiring module's
       coverage-versus-exclusion question is likewise outside it and must be re-filed as a fresh
       todo under the pending todos directory rather than decided here. State in that todo why an
       exclusion inflates the metric without adding safety, so the next reader inherits the reasoning
       rather than the conclusion.

    If the measurement shows an arm the carrier named is already covered, say so and drop it. That
    is a finding, not a gap.

    This task writes the todo file and no test. It closes when the scope is fixed.
  </action>
  <verify>
    <automated>test -s coverage/unit.lcov &amp;&amp; ls .planning/todos/pending/ | grep -i "coverage"</automated>
  </verify>
  <done>
    A coverage report exists for this tree state; the residual arm list is written into the summary
    and the commit body, ordered by size; the out-of-bound items are recorded as non-goals; a fresh
    todo carrying the wiring-module decision exists under the pending todos directory.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover the measured residual arms in the update orchestrator</name>
  <files>tests/orchestrators/plugin/update.test.ts</files>
  <read_first>
    - The residual list from Task 1.
    - The update suite's existing failure-arm cases, so a new case exercises a genuinely different
      arm rather than a near-duplicate the identical-function lint rule will reject.
    - The existing fixture helpers under the plugin test fixtures directory — reuse them; a new
      bespoke fixture for an arm an existing fixture can reach is avoidable surface.
  </read_first>
  <behavior>
    - Each measured residual arm in the update module has a case that reaches it and asserts the
      observable consequence — the outcome partition, the rendered row, or the rollback's restored
      state — rather than merely executing the line.
    - Each new case fails if its arm's behavior changes, and turns exactly one case red when it does.
  </behavior>
  <action>
    Work the update module's residual arms in the order Task 1 fixed. The three-phase rollback paths
    are the expected bulk: a phase that throws after earlier phases committed must unwind them, and
    the assertion is on the restored state plus the reported partial-rollback detail, not on the
    throw alone.

    Keep the user-scope hermetic-home discipline for any fixture that resolves scope, since the
    single-plugin update reads the process working directory itself.

    One arm per case. Assert consequences, not line execution.

    Stop when the module's residual list is worked or when a remaining arm is unreachable without
    fabricating a state the product cannot produce — in that case record the arm and why it was left
    rather than forcing it.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update.test.ts &amp;&amp; npm run lint</automated>
  </verify>
  <done>
    Every reachable residual arm named for the update module has a case asserting its observable
    consequence; the suite is green; lint reports no identical-function finding; any arm left
    uncovered is recorded with its reason.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover the measured residual arms in reinstall and install, then re-measure</name>
  <files>tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/install.test.ts</files>
  <read_first>
    - The residual list entries for these two modules.
    - Their existing failure-arm cases, for the same non-duplication reason as Task 2.
  </read_first>
  <behavior>
    - Each measured residual arm in the reinstall and install modules has a case reaching it and
      asserting its observable consequence.
    - After the sweep, a re-measurement shows the named arms covered.
  </behavior>
  <action>
    Cover the two modules' residual arms, same discipline as Task 2: one arm per case, consequence
    assertions, existing fixtures reused.

    Then re-run the coverage script and record the after-numbers for the three in-scope modules
    beside the before-numbers from Task 1. The deliverable is the named arms covered — report the
    percentages as evidence, not as the target, and do not add a test purely to move one.

    Close with the phase gate: run the full check with the companion-peer root exported and capture
    the exit code directly rather than inferring it from output.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/install.test.ts &amp;&amp; npm run lint</automated>
  </verify>
  <done>
    Both modules' reachable residual arms are covered; the re-measured per-file numbers are recorded
    beside the originals; the full check passes with the peer root exported and the captured exit
    code is zero.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none newly crossed) | This plan adds test cases only. No production line changes; no new input, path or persisted value is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-99-07-01 | Tampering | rollback correctness in the mutating orchestrators | medium | mitigate | Uncovered rollback arms are where a silent regression does the most damage: a partial unwind leaves a half-materialized plugin on disk. This plan is the mitigation — the new cases assert restored state, not merely that a throw occurred. |
| T-99-07-02 | Repudiation | the coverage metric | low | mitigate | A coverage-exclusion entry would raise the reported number without adding safety, misrepresenting the tree's real state. Mitigated by prohibiting the exclusion here and re-filing the decision as an explicit todo with its reasoning attached. |
| T-99-07-03 | Information Disclosure | test fixtures | low | mitigate | Fixtures must carry no real credential or token. TruffleHog filesystem-mode scan over the changed paths before the worktree commit, per the documented protocol. |
| T-99-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — no package is installed. RESEARCH.md §Package Legitimacy Audit records "Not applicable" for the whole phase. |
</threat_model>

<artifacts_this_phase_produces>
| Artifact | Kind | Location |
|---|---|---|
| the measured residual arm list (before and after) | recorded in the summary and commit bodies | `99-07-SUMMARY.md` |
| new rare-failure and rollback cases | NEW test cases | update, reinstall and install suites |
| the wiring-module coverage-versus-exclusion decision | NEW todo | `.planning/todos/pending/` |

No production symbol, no new production file, no catalog state, no closed-set member, no Sonar
configuration change.
</artifacts_this_phase_produces>

<verification>
- A coverage report exists for this tree state and the per-file before/after numbers for the three
  in-scope modules are recorded.
- `node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/install.test.ts` green.
- `npm run lint` green — no identical-function finding from a copied case body.
- A fresh todo exists carrying the out-of-bound wiring-module decision.
- The full check passes with the companion-peer root exported, exit code captured directly.
</verification>

<success_criteria>
The residual is measured rather than assumed, the arms inside D-99-05b's bound are covered with
consequence assertions, the out-of-bound items are recorded as non-goals with the excluded decision
re-filed, and the phase gate is green.
</success_criteria>

<tracer_exemption>
Tracer-first is exempted with a recorded reason: this plan is test-only and adds no user-reachable
behavior. Its leading task is a measurement whose output defines the remaining tasks — the
information-first ordering a tracer exists to provide is already the plan's shape.
</tracer_exemption>

<output>
Create `.planning/phases/99-post-audit-tech-debt-closure/99-07-SUMMARY.md` when done.
Record in it: the before and after per-file numbers with their measurement dates, the residual arm
list as named arms, every arm deliberately left uncovered with its reason, the recorded non-goals,
and the path of the fresh todo.
</output>
