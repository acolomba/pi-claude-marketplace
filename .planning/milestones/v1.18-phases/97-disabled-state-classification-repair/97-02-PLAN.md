---
phase: 97-disabled-state-classification-repair
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - tests/orchestrators/plugin/list.test.ts
  - tests/orchestrators/plugin/info-manifest-absent.test.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
  - docs/output-catalog.md
autonomous: true
requirements: [ENBL-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`plugin list` renders a disabled partially-installed record as `(disabled)`, distinct from an enabled partially-installed record in the same marketplace block (ENBL-06)."
    - "In one marketplace block a disabled partial and an enabled partial render as two distinct rows — `◍ <name> v<v> (disabled)` and `◉ <name> v<v> (partially-installed) {lsp}` — never the same token (ENBL-06)."
    - "The disabled partial's row carries no reason brace at all: no empty brace pair, no unsupported-kind token, no `{not in manifest}` (ENBL-06 composing with INV-04)."
    - "Reclassifying a record from partially-installed to disabled does not move its row within the marketplace block; row order stays the state insertion order (ENBL-06)."
    - "`plugin info --fetch` on a disabled partial emits the `{already disabled}` skip note rather than the state-only `{not in manifest}` note (ENBL-06)."
    - "No source comment and no catalog prose still describes the disabled marker as requiring `compatibility.installable`."
    - "No new catalog state is added: the disabled partial's bytes are identical to the existing `disabled-inventory` block, per D-97-01 discretion anchor 1 resolved toward canonical parity."
  artifacts:
    - path: "tests/orchestrators/plugin/list.test.ts"
      provides: "ENBL-06 byte-exact disabled-partial vs enabled-partial contrast row test"
      contains: "ENBL-06"
    - path: "docs/output-catalog.md"
      provides: "Corrected disabled-row trigger prose (no new catalog-state block)"
      contains: "disabled-inventory"
  key_links:
    - from: "tests/orchestrators/plugin/list.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      via: "listPlugins byte-exact cascade assertion over a seeded disabled partial"
      pattern: "\\(disabled\\)"
    - from: "docs/output-catalog.md"
      to: "extensions/pi-claude-marketplace/persistence/state-io.ts"
      via: "trigger prose names the single predicate's new home"
      pattern: "isRecordedButDisabled"
---

<objective>
Pin the ENBL-06 rendering contract that the ENBL-05 collapse makes reachable: a
disabled partially-installed record renders `(disabled)`, bare, distinct from an
enabled partial, and its `--fetch` skip note switches to `{already disabled}`.
Then reconcile every source comment and catalog prose fragment that still
describes the retired two-axis marker.

Purpose: the collapse changes what users see. Without byte-exact pins the new
rendering is unprotected, and without the prose correction the binding user
contract (`docs/output-catalog.md`) states a trigger condition the code no longer
uses.

Output: two byte-exact rendering tests and a clean sweep of the stale-marker prose
inventory. No new catalog state — the disabled partial's bytes are identical to
the existing `disabled-inventory` block.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-disabled-state-classification-repair/97-CONTEXT.md
@.planning/phases/97-disabled-state-classification-repair/97-RESEARCH.md
@.planning/phases/97-disabled-state-classification-repair/97-PATTERNS.md
@.planning/phases/97-disabled-state-classification-repair/97-01-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Byte-pin the disabled-partial list row against the enabled-partial contrast</name>
  <files>tests/orchestrators/plugin/list.test.ts</files>

  <read_first>
    - `tests/orchestrators/plugin/list.test.ts` lines 120-232 — the local `seedMarketplace` factory. It already carries both axes: `disabled` empties every `resources.*` array and sets `enabled: false`, while `unsupported` drives `installable: unsupported.length === 0`. No factory change is needed.
    - `tests/orchestrators/plugin/list.test.ts` lines 978-1008 — the existing ENBL-04 canonical-disabled test: the `◍ alpha v1.2.3 (disabled)` row match, the `(installed)`/`(unavailable)` negative assertions, and the `severity === undefined` assertion.
    - `tests/orchestrators/plugin/list.test.ts` lines 1465-1500 — the byte-exact join idiom over a full cascade, including the `"  ◉ remote v1.0.0 (partially-installed) {lsp}"` row form (the `lspServers` kind narrows to the `lsp` reason token).
    - `tests/orchestrators/plugin/list.test.ts` lines 1061-1090 — the D-63-04 hooks-only over-reach guard that must stay green unchanged.
  </read_first>

  <behavior>
    - One marketplace, two plugins seeded in insertion order: `alpha` with `disabled: true, unsupported: ["lspServers"]`, then `beta` with `unsupported: ["lspServers"]` and no `disabled` flag.
    - The rendered cascade is byte-equal to the three-line join: the marketplace header, then `  ◍ alpha v1.0.0 (disabled)`, then `  ◉ beta v1.0.0 (partially-installed) {lsp}`.
    - The disabled row carries no brace: the rendered `alpha` line must not contain `{`.
    - Severity of the inventory notification stays `undefined` (info).
    - Row order matches seed order — `alpha` precedes `beta` — proving reclassification does not move a row.
  </behavior>

  <action>
Add one test to `list.test.ts`, placed beside the existing ENBL-04
canonical-disabled test so the pair reads as a family. Seed a single marketplace
whose `installed` map declares `alpha` first (disabled plus an unsupported kind)
and `beta` second (the same unsupported kind, enabled). Assert the whole
notification message with a byte-exact `join("\n")` comparison rather than
separate row matches — the byte form is the contract, and the join proves order
and absence together.

Add three targeted negative assertions on the `alpha` row so the failure message
names the specific regression: it must not carry a brace, must not render
`(partially-installed)`, and must not render `{not in manifest}`. Assert the
notification severity is `undefined`.

Title the test citing ENBL-06 and INV-04, stating that a disabled partial renders
bare `(disabled)` at parity with the canonical disabled row while the enabled
partial beside it keeps its unsupported-kind brace.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/list.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The new test asserts message byte-equality against a `join("\n")` whose second line is `  ◍ alpha v1.0.0 (disabled)` and whose third line is `  ◉ beta v1.0.0 (partially-installed) {lsp}`.
    - The `alpha` row assertion proves brace absence on that row specifically (extract the `alpha` line, then assert it contains no `{`) — not a whole-output brace check, which the `beta` row would defeat.
    - `node --test tests/orchestrators/plugin/list.test.ts` exits 0, including the untouched D-63-04 hooks-only guard at :1066 which must still render `(installed)`.
    - The seeded `alpha` entry uses the file's own local `seedMarketplace` factory with no factory signature change.
  </acceptance_criteria>

  <done>The two partial-record shapes render as two distinct, byte-pinned rows in one block, in seed order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the info --fetch skip-note switch for a disabled partial</name>
  <files>tests/orchestrators/plugin/info-manifest-absent.test.ts</files>

  <read_first>
    - `tests/orchestrators/plugin/info-manifest-absent.test.ts` lines 948-990 — the existing D-96-04 `--fetch` all-disabled test: two notifications, the first the `(disabled)` inventory block at `undefined` severity, the second the `warning`-severity skip note whose body is the four-line join ending `  ⊘ alpha v1.0.0 (skipped) {already disabled}`.
    - `tests/orchestrators/plugin/info-manifest-absent.test.ts` lines 990-1020 — the sibling bare-`info` test asserting NO skip note, which the new fixture axis must not disturb.
    - The CR-01 repro added by plan `97-01` in the same file — the new test is its `--fetch` sibling and copies its seeding.
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` around line 2190 — the `already disabled` reason token on the all-disabled early return, and `partitionDisabledScopes` at :2077 which now routes the partial out of the state-only arm.
  </read_first>

  <behavior>
    - Seeding a manifest-absent record with `disabled: true, unsupported: ["lspServers"]` and calling `getPluginInfo` with `fetch: true` yields exactly two notifications.
    - Notification 1: `undefined` severity, byte-equal to the two-line disabled cascade.
    - Notification 2: `warning` severity, byte-equal to the four-line skip note ending `  ⊘ alpha v1.0.0 (skipped) {already disabled}`.
    - The reason token is `already disabled`, not `not in manifest` — the record no longer reaches the state-only arm.
  </behavior>

  <action>
Add one test to `info-manifest-absent.test.ts` directly after the existing D-96-04
`--fetch` all-disabled test, copying it verbatim except for adding
`unsupported: ["lspServers"]` to the seeded `alpha` entry. Keep both notification
assertions byte-exact.

Add one explicit negative assertion that the second notification does not contain
`not in manifest`, because that token is precisely what the pre-collapse state-only
arm emitted for this shape and is the regression the test exists to catch.

Title it citing ENBL-06 and D-96-04, stating that the skip reason for a disabled
partial is the disabled cause rather than the manifest-absence cause.
  </action>

  <verify>
    <automated>node --test tests/orchestrators/plugin/info-manifest-absent.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - The new test asserts `notifications.length === 2`, `notifications[0].severity === undefined`, and `notifications[1].severity === "warning"`.
    - `notifications[1].message` is asserted byte-equal to a four-line join ending `  ⊘ alpha v1.0.0 (skipped) {already disabled}`.
    - A negative assertion proves the second notification's rendered message does not carry the manifest-absence reason token. The subject is an in-memory notification string, not source text, so a source comment cannot influence this assertion. <!-- planner-discipline-allow: not in manifest -->
    - `node --test tests/orchestrators/plugin/info-manifest-absent.test.ts` exits 0, including the untouched bare-`info` sibling that must still emit no skip note.
  </acceptance_criteria>

  <done>The disabled partial's `--fetch` behavior is pinned on the correct cause token.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the stale two-axis-marker prose across render surfaces and the catalog</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts, docs/output-catalog.md</files>

  <read_first>
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 720-755 — the `PluginDisabledMessage` doc block at :722-740 (whose "empty-resources + availability marker" clause is now false) and the interface at :745-751 which has no `reasons` field by construction.
    - `extensions/pi-claude-marketplace/shared/notify.ts` lines 990-1000 — the second stale marker description.
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts` lines 400-408 — the bucket-population comment naming the old marker and pointing at `plan.ts`.
    - `extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts` lines 14-22 — the marker definition comment.
    - `docs/output-catalog.md` lines 145-155, 320-340, 2185-2220 — the four prose fragments describing the trigger, plus the fenced `catalog-state: disabled-inventory` block at :324 which must be left byte-identical.
    - `tests/architecture/catalog-uat.test.ts` lines 1-40 — the byte gate, to confirm it compares only fenced `catalog-state` blocks and not surrounding prose.
  </read_first>

  <action>
Rewrite each stale fragment to state the current rule: the disabled marker is the
explicit `enabled: false` boolean on the install record, read through the single
`isRecordedButDisabled` predicate in `persistence/state-io.ts`; availability
(`compatibility.installable`) is an orthogonal axis and is not part of the marker.
Where a fragment points at `orchestrators/reconcile/plan.ts::isRecordedButDisabled`
as the load-bearing predicate, repoint it at the new home. Where a fragment claims
the empty-resources arrays are part of the marker, drop that clause — it was
superseded by ENBL-02 and the residual mention misleads.

In `shared/notify.ts`, keep the existing statement that `PluginDisabledMessage`
carries no reasons by construction and strengthen it with the composition it now
guarantees: a row type with no reasons field cannot emit a manifest-absence
reason, which makes INV-04's constraint structural. Cite ENBL-05, ENBL-06 and
INV-04.

In `docs/output-catalog.md`, edit only the four prose fragments. Do not touch any
fenced block carrying a `catalog-state` marker, do not add a new
`catalog-state` marker, and do not change the disabled row's bytes — the
disabled partial renders byte-identically to the existing `disabled-inventory`
state, so the byte gate must stay untouched. Note in the plan summary that
`docs/prd/pi-claude-marketplace-prd.md`, the catalog's brace-bearing-variant
count, the missing `(partially-installed)` status-token table row, and the
`notify-reasons.ts` header count remain Phase 98 DOC-08 work and are deliberately
out of scope here.

Apply `.claude/rules/typescript-comments.md` to every rewritten comment: durable
spec IDs only, no phase or plan or wave numbers.
  </action>

  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts && npm run lint && npm run format:check</automated>
  </verify>

  <acceptance_criteria>
    - Reading the four touched files end to end, no surviving sentence describes the disabled marker as a conjunction of empty resource arrays with an availability flag. Every description names the explicit disabled boolean alone. Verify by reading the diff, not by a bare token grep — the availability field name legitimately survives in prose that calls it the ORTHOGONAL axis.
    - Every surviving reference to the load-bearing predicate names `persistence/state-io.ts`, not `orchestrators/reconcile/plan.ts`.
    - `git diff -- docs/output-catalog.md` touches no line inside a fenced block and adds no `catalog-state:` marker; `node --test tests/architecture/catalog-uat.test.ts` exits 0.
    - `npm run lint` and `npm run format:check` exit 0.
  </acceptance_criteria>

  <done>No render-surface comment or catalog prose fragment still describes the retired two-axis marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → `shared/notify.ts` | Orchestrators stamp status, severity and reasons; notify renders. A row that states a fact the record does not hold is a state lie crossing to the user. |
| `docs/output-catalog.md` → user | The catalog is a binding user contract enforced byte-wise by `catalog-uat`; prose outside the fenced blocks is unenforced and can drift. |

No network path, no filesystem write outside the repository, no new input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-97-05 | Information Disclosure | `orchestrators/plugin/list.ts` and `info.ts` disabled row builders | low | accept | The bare `(disabled)` row omits the record's unsupported-kind detail, so a user cannot see from that row which components were dropped at install time. Accepted per the D-97-01 discretion anchor resolved toward canonical parity: `PluginDisabledMessage` carries no reasons by construction, the detail remains available after `enable`, and no sensitive data is involved. |
| T-97-08 | Spoofing | `docs/output-catalog.md` prose outside fenced blocks | low | mitigate | Prose describing a trigger condition the code no longer uses misleads a reader into trusting a false contract. Mitigated by Task 3's sweep; the fenced blocks stay under the existing `catalog-uat` byte gate, which this plan does not weaken. |
| T-97-09 | Repudiation | the `--fetch` skip note | low | mitigate | Reporting `{not in manifest}` for a record skipped because it is disabled misattributes the cause. Mitigated by Task 2's byte pin plus the explicit negative assertion on the wrong token. |
| T-97-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task in this plan; RESEARCH.md `## Package Legitimacy Audit` records "Not applicable". |
</threat_model>

<verification>
- `node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/info-manifest-absent.test.ts tests/architecture/catalog-uat.test.ts` exits 0.
- `npm run typecheck`, `npm run lint`, `npm run format:check` all exit 0.
- The full suite runs green with `PI_SUBAGENTS_ROOT` pointed at Pi's managed `pi-subagents`.
</verification>

<success_criteria>
- A disabled partial and an enabled partial render as two distinct, byte-pinned rows in one marketplace block, in seed order.
- The disabled partial's `--fetch` skip note carries the disabled cause, not the manifest-absence cause.
- No comment or catalog prose still describes the retired two-axis marker; no catalog state is added.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 97-02)

**New tests**
- `ENBL-06 / INV-04: a disabled partial renders bare (disabled) beside an enabled partial's (partially-installed) {lsp} in the same block` (`tests/orchestrators/plugin/list.test.ts`)
- `ENBL-06 / D-96-04: info --fetch on a disabled PARTIAL emits the {already disabled} skip note, not {not in manifest}` (`tests/orchestrators/plugin/info-manifest-absent.test.ts`)

**Prose corrections (no behavior change)**
- `extensions/pi-claude-marketplace/shared/notify.ts` — the `PluginDisabledMessage` doc block and the second marker description
- `extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts` — the bucket-population comment
- `extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts` — the marker definition comment
- `docs/output-catalog.md` — four trigger-prose fragments, all outside fenced blocks

**Catalog states added:** none. Deliberately deferred to Phase 98 DOC-08: the PRD's PL-6 row and
5.3.1 flowchart, the brace-bearing-variant count, the missing `(partially-installed)` status-token
table row, and the `notify-reasons.ts` header count.
</artifacts_produced>

<output>
Create `.planning/phases/97-disabled-state-classification-repair/97-02-SUMMARY.md` when done
</output>
