---
milestone: v1.17
audited: 2026-08-04T17:40:57Z
status: passed
scores:
  requirements: 14/14
  phases: 5/5
  integration: 9/9
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 90-session-environment-initialization
    items:
      - "VALIDATION.md status: draft (NOT-VALIDATED per #2117) — validate-phase never reconciled it after the 90-02/90-03 gap-closure rounds; behavior coverage itself is strong (20/20 must-haves re-verified, UAT 3/3, full suite green)"
  - phase: 91-hook-environment-parity
    items:
      - "Doc hygiene: 91-01-SUMMARY.md predates review-fix commit 96cb08c5 and does not mention it — narrative staleness only, verification covered the fixed state"
---

# Milestone v1.17 (env-parity) — Audit Report

**Audited:** 2026-08-04 · **Status:** passed · **Requirements:** 14/14 satisfied

## Definition of Done (from ROADMAP)

A skill, command, hook, or MCP server process launched through Pi sees the
Claude Code environment exactly as under Claude Code: session vars
(`CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID`, pi-only `CLAUDE_SESSION_ID`) on
Pi's live `process.env` at session start, each installed enabled plugin's
`<pluginRoot>/bin` appended to `PATH` (PENV-01), the same session set on both
hook spawn lanes (drift-guarded), stage-time `${CLAUDE_*}` substitution and
env injection in `mcp.json`, install-time `${CLAUDE_SKILL_DIR}` /
project-scope `${CLAUDE_PROJECT_DIR}` substitution in staged content, and an
authoritative `docs/env-vars.md` matrix with `docs/hooks-compatibility.md`
reconciled against it.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|--------------|---------------------|-----------------|-------|
| SENV-01 | 90 | passed | — (see note) | [x] Complete | satisfied |
| SENV-02 | 90 | passed | — (see note) | [x] Complete | satisfied |
| SENV-03 | 90 | passed | — (see note) | [x] Complete | satisfied |
| PENV-01 | 90 | passed | 90-03 | [x] Complete | satisfied |
| HENV-01 | 91 | passed | — (see note) | [x] Complete | satisfied |
| HENV-02 | 91 | passed | — (see note) | [x] Complete | satisfied |
| MENV-01 | 92 | passed | — (see note) | [x] Complete | satisfied |
| MENV-02 | 92 | passed | — (see note) | [x] Complete | satisfied |
| MENV-03 | 92 | passed | — (see note) | [x] Complete | satisfied |
| MENV-04 | 92 | passed | — (see note) | [x] Complete | satisfied |
| SUB-01 | 93 | passed | — (see note) | [x] Complete | satisfied |
| SUB-02 | 93 | passed | 93-02 | [x] Complete | satisfied |
| DOC-06 | 94 | passed | 94-01 | [x] Complete | satisfied |
| DOC-07 | 94 | passed | 94-01 | [x] Complete | satisfied |

**Frontmatter note:** `requirements-completed` frontmatter exists only on the
newer SUMMARYs (90-03, 93-02, 94-01). For the older SUMMARYs the coverage was
confirmed manually from each phase VERIFICATION.md requirement/truth table:
90-VERIFICATION truths 1–3 (SENV-01/02/03), 91-VERIFICATION truths 1–8
(HENV-01/02), 92-VERIFICATION truths 1–4 (MENV-01..04), and 93-VERIFICATION's
requirements table (SUB-01 SATISFIED). No requirement is orphaned — every
REQ-ID appears in at least one VERIFICATION with evidence.

## Phase Verifications

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 90 session-environment-initialization | passed | 20/20 | Re-verified after 90-02/90-03 gap closure; UAT 3/3 (90-UAT.md) |
| 91 hook-environment-parity | passed | 9/9 | Drift guard pins both spawn lanes |
| 92 mcp-staging-parity | passed | 24/24 | Includes post-SUMMARY `__proto__`-safety review-fix commits |
| 93 substitution-completion | passed | 3/3 | All 9 orchestrator threading sites confirmed by direct read |
| 94 environment-variable-documentation | passed | 15/15 | pi-mcp-adapter claims cross-checked against installed 2.10.0 source |

## Cross-Phase Integration (checker: PASS)

Integration score 9/9, flow score 5/5, zero blockers/warnings. Full test
suite and typecheck re-run green at audit HEAD. Evidence highlights:

1. **session_start → bash child** — `index.ts` wires `applySessionEnv` (session_start) + `recomputePluginPath` (resources_discover) onto live `process.env`; Pi's `getShellEnv()` spread delivers both to every child (SENV-01/02/03, PENV-01).
2. **Hook lanes share the Phase 90 producer** — both `prepareEnv` and `prepareAsyncEnv` spread the single `claudeSessionEnvFor(transCtx.sessionId)` producer post-spread; drift guard green (HENV-01/02).
3. **Two substitution seams, one threading** — `bridges/mcp/substitute.ts` and `shared/vars.ts` are independently implemented but consistently scoped; project-scope `cwd` feeds `CLAUDE_PROJECT_DIR`/`projectDir` identically (MENV-01..04, SUB-01/02).
4. **Docs match shipped behavior at HEAD** — including after the 260804-gcs applyPathLedger change; `docs/env-vars.md`'s ledger-safety claim ("removes only the entries it previously added") is true before and after (DOC-06/07).
5. **PENV-01 ledger round-trip** — post-fix `applyPathLedger` and the new `asAbsolutePluginRoot` delimiter rejection are absorbed consistently: the throw lands inside pre-existing non-fatal try/catch at all three write sites and both read sites.

## Post-Verification Closure

Quick task **260804-gcs** (2026-08-04, after all phase verifications) closed
the two advisories carried forward in 90-VERIFICATION's Gaps Summary:

- **WR-01** — `applyPathLedger` no longer strips non-owned empty PATH segments (commit `f1d4c265`, 6 contract tests).
- **IN-01** — `asAbsolutePluginRoot` rejects `path.delimiter`-bearing roots, closing the ledger round-trip leak (commit `aeef0882`, 1 test).

The integration checker verified both changes at audit HEAD. The
REQUIREMENTS.md bookkeeping note from 90-VERIFICATION (checkboxes pending) is
also resolved — all 14 checkboxes `[x]`, traceability all Complete.

## Nyquist Compliance

| Phase | VALIDATION.md | Status | Action |
|-------|---------------|--------|--------|
| 90 | exists | draft → NOT-VALIDATED (#2117) | `/gsd-validate-phase 90` (optional coverage TODO) |
| 91 | exists | validated, compliant | — |
| 92 | exists | validated, compliant | — |
| 93 | exists | validated, compliant | — |
| 94 | exists | validated, compliant | — |

nyquist:
  compliant_phases: ["91", "92", "93", "94"]
  partial_phases: []
  not_validated_phases: ["90"]
  missing_phases: []
  overall: "4 COMPLIANT, 1 NOT-VALIDATED (coverage TODO, not compliance failure — #2117)"

## Known Environment Issue (not a finding)

Two integration tests (`tests/integration/` provenance-invisibility /
skill-path-resolution) resolve the pi-subagents peer from `npm root -g` and
fail locally on a stale global version; they skip in CI. Pre-existing,
documented in every phase verification, unrelated to milestone code.

---
_Audited by: Claude (audit-milestone orchestrator + gsd-integration-checker)_
