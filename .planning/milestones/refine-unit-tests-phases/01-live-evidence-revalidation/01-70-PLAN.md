---
phase: 01-live-evidence-revalidation
plan: 70
type: execute
wave: 18
depends_on: ["01-69"]
files_modified:
  - scripts/revalidation.mjs
  - scripts/revalidation.negative.mjs
  - tests/architecture/revalidation.test.ts
autonomous: true
gap_closure: true
requirements: [RVAL-01, RVAL-02, RVAL-03, RVAL-04]
estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`scope-impact --check` reads both live planning contracts and returns success only when all 40 evidence-backed scope rows round-trip to them (RVAL-04, D-23)."
    - "Missing, duplicate, moved, or structurally drifted requirement clauses and Phase 2-9 routes fail closed with deterministic diagnostics while stable IDs and phase numbers remain enforced (D-19 through D-23)."
    - "The non-check `scope-impact` command retains its existing sorted JSON projection, and RVAL-01 through RVAL-03 remain green."
    - "All planning-contract fixtures are case-owned, offline, and removed after each test; no check reads outside the injected repository root (D-14)."
  artifacts:
    - path: "scripts/revalidation.mjs"
      provides: "Real scope-impact planning-contract parser and blocking --check behavior"
      contains: "handleScopeImpact"
    - path: "tests/architecture/revalidation.test.ts"
      provides: "Direct positive and fail-closed RVAL-04 CLI evidence"
      contains: "RVAL-04 scope-impact"
    - path: "scripts/revalidation.negative.mjs"
      provides: "Standalone planted planning-contract offenders"
      contains: "scope-impact"
  key_links:
    - from: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json"
      to: ".planning/REQUIREMENTS.md"
      via: "32 SCOPE-REQ rows, action disposition, and path/section/ID-qualified afterAnchor validation"
      pattern: "SCOPE-REQ-"
    - from: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json"
      to: ".planning/ROADMAP.md"
      via: "eight SCOPE-ROUTE-PHASE-02..09 rows, stable phase identities, and exact active-requirement routing"
      pattern: "SCOPE-ROUTE-PHASE-"
    - from: "tests/architecture/revalidation.test.ts"
      to: "scripts/revalidation.mjs"
      via: "public main/CLI execution against case-owned roots and the current 40-row repository contract"
      pattern: "scope-impact.*--check"
---

<objective>
Close the remaining Phase 1 verification gap by making the advertised scope-impact check enforce the evidence-derived planning contracts.

Purpose: Turn the current no-op `--check` flag into the reproducible RVAL-04 gate promised by Plans 01-68 and 01-69, without reopening the sealed evidence decisions or broadening implementation scope.
Output: A fail-closed CLI contract check, direct architecture tests, and standalone planted negative controls.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-live-evidence-revalidation/01-CONTEXT.md
@.planning/phases/01-live-evidence-revalidation/01-RESEARCH.md
@.planning/phases/01-live-evidence-revalidation/01-PATTERNS.md
@.planning/phases/01-live-evidence-revalidation/01-VERIFICATION.md
@.planning/phases/01-live-evidence-revalidation/01-VALIDATION.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@.planning/phases/01-live-evidence-revalidation/01-68-SUMMARY.md
@.planning/phases/01-live-evidence-revalidation/01-69-SUMMARY.md
@scripts/revalidation.mjs
@scripts/revalidation.negative.mjs
@tests/architecture/revalidation.test.ts

<interfaces>
Existing contracts to preserve:
- `main(args, runtime)` parses `--check` and already passes `{ projectRoot, ledger, options, runtime }` to every command handler.
- `handleScopeImpact` currently accepts only `{ ledger, runtime }` and writes `deriveScopeImpact(ledger)`; this missing projectRoot/options consumption is the verified break.
- `deriveScopeImpact(ledger)` returns the established sorted JSON projection without before/after anchors; invocation without `--check` must remain byte-compatible.
- `assertSafeRelativePath(projectRoot, candidate)` already rejects missing, absolute, dot-segment, symlink-escape, and non-file paths and is the required read boundary for both planning contracts.
- `runCli`, `runInProcess`, and `createCliFixture` are the existing public-CLI test seams; add no test-only production export.
</interfaces>
</context>

## Artifacts this phase produces

No new file path, package, schema, or external interface. This plan gives the existing `scope-impact --check` flag real blocking semantics in `scripts/revalidation.mjs` and extends its two tracked evidence owners: `tests/architecture/revalidation.test.ts` and `scripts/revalidation.negative.mjs`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire the 40-row scope crosswalk through both planning contracts</name>
  <files>scripts/revalidation.mjs, tests/architecture/revalidation.test.ts</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/01-live-evidence-revalidation/01-VERIFICATION.md
    - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - scripts/revalidation.mjs
    - tests/architecture/revalidation.test.ts
  </read_first>
  <behavior>
    - The current repository command `node scripts/revalidation.mjs scope-impact --check` reads `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, validates exactly 32 requirement rows plus eight Phase 2-9 route rows, exits zero, and emits exactly `Scope impact valid: 40 records.\n` rather than the JSON projection.
    - Invoking `scope-impact` without `--check` preserves the existing sorted JSON output exactly.
    - If either planning contract is absent, outside the injected root, a symlink escape, or not a regular file, the command exits nonzero and identifies the rejected contract path without printing success output.
    - Requirement rows round-trip by exact stable ID, actual requirements section, active-versus-evidence disposition, and a nonblank path/section/ID-qualified `afterAnchor`; route rows round-trip by exact `PHASE-02` through `PHASE-09` identity, phase number/title, and the active requirement set declared by that roadmap phase.
  </behavior>
  <action>Write the RVAL-04 CLI cases first and run the focused name pattern to witness the current false pass. Then change `handleScopeImpact` to consume `projectRoot` and `options`. Keep the existing output branch unchanged when `options.check` is not true. In the check branch, read the two fixed planning paths only through `assertSafeRelativePath`; parse the requirement-definition bullets separately from the traceability table, parse the numbered roadmap phase sections and their `Requirements` declarations, and accumulate deterministic structured violations before setting `runtime.exitCode = 1`. Require the ledger crosswalk to contain exactly 40 unique rows: one `SCOPE-REQ-<ID>` row for each of the 32 stable requirements and one `SCOPE-ROUTE-PHASE-NN` row for each stable Phase 2-9 route. Validate each `beforeAnchor` and `afterAnchor` as a nonblank, distinct, three-part path-qualified locator; require the after locator's path, section/phase, stable ID, and disposition to resolve to the parsed live contract. Enforce D-19 and D-21 by requiring `move-to-evidence` rows outside active roadmap routing while `keep` and `narrow/split` rows remain defined and route exactly once. Enforce D-20 and D-23 by requiring unique Phase 2-9 numbers/titles and exact requirement membership without renumbering. Use the existing CLI boundary and do not add a test-only export, package, network call, fuzzy similarity threshold, or alternate planning source.</action>
  <acceptance_criteria>The real current contracts produce 40/40 matches; the same ledger fails when either contract is absent; check mode demonstrably opens both contract files; ordinary scope-impact JSON remains unchanged; and all new filesystem cases use a fresh temporary root with cleanup registered before acting.</acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/revalidation.test.ts --test-name-pattern='RVAL-04 scope-impact' &amp;&amp; node scripts/revalidation.mjs scope-impact --check</automated>
  </verify>
  <fails_when>The focused test or live command exits nonzero, the current crosswalk count is not exactly 40 (32 requirement plus eight route rows), check mode succeeds without opening both contracts, a missing contract still exits zero, ordinary mode output changes, or a temporary fixture survives cleanup.</fails_when>
  <done>`scope-impact --check` is a real end-to-end planning-contract gate and closes the verifier's disconnected ledger-to-contract link.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fail-closed contract and anchor drift controls</name>
  <files>scripts/revalidation.mjs, scripts/revalidation.negative.mjs, tests/architecture/revalidation.test.ts</files>
  <read_first>
    - .planning/phases/01-live-evidence-revalidation/01-CONTEXT.md
    - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - scripts/revalidation.mjs
    - scripts/revalidation.negative.mjs
    - tests/architecture/revalidation.test.ts
    - .agents/skills/typescript-unit-testing-review/SKILL.md
    - .agents/skills/typescript-google-style-review/SKILL.md
  </read_first>
  <behavior>
    - One independently named test per planted offender proves failure for a missing requirement definition, duplicate stable requirement definition, changed requirement section/disposition, missing Phase 2-9 route, duplicate route, changed roadmap requirement membership, phase renumbering/title drift, and malformed or misdirected `afterAnchor`.
    - Each negative case asserts the whole CLI result with a stable violation code, target, empty success output, and nonzero status; no weak truthy assertion or production-derived expected value is used.
    - The standalone negative runner plants missing and tampered REQUIREMENTS.md/ROADMAP.md controls under its own temporary root, observes rejection, restores the benign fixture between mutations, and leaves the real repository untouched.
    - Strict ledger validation also rejects absent, blank, identical, malformed, or path-mismatched before/after scope anchors, while all current 40 records remain valid.
  </behavior>
  <action>Add the offender cases before any implementation adjustment and confirm each fails for the intended reason. Build every expected requirement/phase set independently from short explicit fixture literals or from a copied current contract followed by one controlled mutation; never ask the production parser to generate the expected value. Extend `validateScopeChange` so anchors are mandatory, distinct, parseable, path-confined, and consistent with the row identity. Extend the planning-contract checker only as needed to make each public-CLI case discriminate a plausible wrong implementation: checking file existence only, counting 40 ledger rows without reading contracts, accepting duplicate IDs, ignoring history disposition, accepting a missing route, or accepting a renumbered phase. Mirror the core missing/tampered-contract witnesses in `scripts/revalidation.negative.mjs`. Follow the repository unit-test rules: sibling `test()` cases, lowercase arrange/act/assert comments, whole structured comparisons, case-owned `mkdtemp`, cleanup registered with `t.after()` or `finally`, no shared mutable fixture, no credentials/network, no test-only seam, and no weakening of existing assertions.</action>
  <acceptance_criteria>Every listed offender makes the public check fail while the untouched 40-row fixture and live contracts pass; standalone negative controls pass only after observing all intended rejections; strict validation covers anchor shape; the full focused suite and direct source coverage remain green.</acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/revalidation.test.ts &amp;&amp; node scripts/revalidation.negative.mjs &amp;&amp; npm run test:coverage:direct -- scripts/revalidation.mjs</automated>
  </verify>
  <fails_when>Any command exits nonzero; any offender returns success or the wrong violation target; an expected result is derived through production code; direct branch/function/line coverage falls below 100%; a test skips/todos/onlys a case; or a temporary path, global mutation, credential, network request, or untracked output escapes its case.</fails_when>
  <done>The planning-contract gate has positive current evidence and fail-first controls for every verified missing/drift/route/anchor failure class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical ledger → planning-contract checker | Repository JSON controls which requirements and phase routes are expected and is treated as untrusted structured input. |
| REQUIREMENTS.md / ROADMAP.md → future phase execution | Contract drift can silently authorize stale work or remove terminal work if the checker false-passes. |
| injected repository root → filesystem | Tests and CLI callers can supply a root; reads must remain inside it and reject missing/non-regular/symlink-escaped contracts. |

## STRIDE Threat Register

Security enforcement uses ASVS Level 1 and blocks unresolved high-severity threats.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-70-01 | Tampering / Repudiation | `scope-impact --check` semantic crosswalk | high | mitigate | Parse both contracts, compare the exact 32+8 identity/routing sets, validate after locators, emit deterministic violations, and prove planted offenders through the public CLI. |
| T-01-70-02 | Spoofing | duplicate or moved requirement/phase identities | high | mitigate | Require unique stable IDs, Phase 2-9 numbers/titles, action-consistent active/history disposition, and exact one-to-one active routing. |
| T-01-70-03 | Information Disclosure / Tampering | injected-root planning file reads | high | mitigate | Resolve only fixed repository-relative paths through `assertSafeRelativePath`; reject missing files, non-files, and symlink escape before reading. |
| T-01-70-04 | Denial of Service | malformed Markdown planning contracts | medium | mitigate | Bound the parser to the two local files, accumulate deterministic structural violations, and cover malformed/duplicate structures without network or recursive scans. |
| T-01-70-05 | Repudiation | non-check compatibility | low | accept | The established sorted JSON projection is retained byte-for-byte and protected by a direct regression; this branch does not authorize planning. |
| T-01-70-SC | Tampering | package installs | high | mitigate | No package installation or dependency change is authorized; use Node built-ins and existing repository scripts only. |
</threat_model>

<verification>
  <automated>node --test tests/architecture/revalidation.test.ts &amp;&amp; node scripts/revalidation.negative.mjs &amp;&amp; node scripts/revalidation.mjs validate &amp;&amp; node scripts/revalidation.mjs scope-impact --check &amp;&amp; npm run test:coverage:direct -- scripts/revalidation.mjs</automated>
  <fails_when>Any command exits nonzero; the check reports anything other than 40 validated rows split 32+8; either planning contract is not read; strict ledger validation accepts malformed anchors; a planted offender passes; non-check JSON drifts; or direct source coverage is not 100% for branches, functions, and lines.</fails_when>
</verification>

<success_criteria>
- The authoritative 40-row crosswalk is reproducibly enforced against the live REQUIREMENTS.md and ROADMAP.md rather than merely printed.
- Missing/tampered requirements, evidence dispositions, routes, phase numbers/titles, memberships, and after locators fail closed through the public CLI.
- RVAL-04 becomes independently verifiable while RVAL-01, RVAL-02, and RVAL-03 remain unchanged and green.
- Only the three tracked revalidation CLI/test files change; no planning contract, canonical ledger, generated report, package manifest, schema, or runtime product file changes.
</success_criteria>

## Flagged assumptions from spec-less edge fallback

The fallback supplied nine unresolved probes. Input count and output count remain equal at nine; none is silently resolved. They remain flagged because the structured `01-VERIFICATION.md` exposes only the RVAL-04 planning-contract blocker, and this additive plan does not broaden that verified gap.

| Probe | Requirement | Classification | Disposition in 01-70 |
|---|---|---|---|
| adjacency | RVAL-01 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| empty | RVAL-01 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| ordering | RVAL-01 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| concurrency | RVAL-01 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| adjacency | RVAL-02 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| empty | RVAL-02 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| ordering | RVAL-02 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| unclassified | RVAL-03 | unresolved fallback probe | FLAGGED — unchanged, not an actionable structured gap |
| unclassified | RVAL-04 | unresolved fallback probe | FLAGGED — unchanged; this plan closes only the verifier's classified round-trip blocker |

## Integration, UI, schema, and prohibition declarations

- UI gate: `frontend=false`; no UI-SPEC or visual checkpoint applies.
- External API integration: none; all inputs are tracked local repository artifacts and tests remain offline.
- Schema push or assumption-model transition: none; no ORM schema, datastore, migration, external state, or new package is in scope.
- Prohibition recall: zero bespoke values/safety/ethics prohibitions were surfaced for this local validation utility. Canon filesystem containment and untrusted-input safety are enforced by T-01-70-01 through T-01-70-04 and the planted filesystem controls.

## Multi-Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Establish complete reproducible scope before code changes | 01-01 through 01-70 | COVERED | 01-70 repairs the sole failed reproducibility link after the completed evidence, decision, and rewrite sequence. |
| REQ | RVAL-01 | Individually inspect the exact 110-file corpus | 01-01 through 01-57; preserved by 01-70 | COVERED | No review corpus or ledger census change is authorized. |
| REQ | RVAL-02 | Complete current-evidence manifest | 01-01 through 01-57; preserved by 01-70 | COVERED | Strict ledger validation remains in the final command chain. |
| REQ | RVAL-03 | Resolve nine premise-first operator decisions | 01-58 through 01-66; preserved by 01-70 | COVERED | No decision record or route is reopened. |
| REQ | RVAL-04 | Evidence-derived scope removal/rewrite with a real round-trip check | 01-67 through 01-70 | COVERED | 01-70 makes `scope-impact --check` read and enforce both planning contracts. |
| RESEARCH | — | Repository-native Node validator, deterministic errors, and no new dependency | 01-01, 01-70 | COVERED | Existing stack and public CLI boundary only. |
| RESEARCH | — | Hermetic case-owned filesystem fixtures and path confinement | 01-01, 01-70 | COVERED | Temporary roots, fixed relative paths, and planted offender controls. |
| RESEARCH | — | Scope reconciliation preserves stable IDs/history and validates active traceability | 01-67 through 01-70 | COVERED | Exact 32 requirement plus eight route equality, action disposition, and after locators. |
| CONTEXT | D-01–D-10 | Canonical ledger and blocking validation semantics | 01-01 through 01-57; D-10 reinforced by 01-70 | COVERED | 01-70 consumes the sealed ledger without changing its evidence model. |
| CONTEXT | D-11–D-14 | Evidence threshold and hermeticity | 01-02 through 01-57; D-14 reinforced by 01-70 | COVERED | Public behavior and case-owned local filesystem evidence. |
| CONTEXT | D-15–D-18 | Sequential operator dossiers and durable choices | 01-58 through 01-66 | COVERED | Existing resolved decisions remain authoritative. |
| CONTEXT | D-19–D-23 | Trace-preserving scope rewrite and pre-planning round trip | 01-67 through 01-70 | COVERED | 01-70 closes D-23's missing executable enforcement without altering the chosen scope. |
| CONTEXT | deferred | None | — | EXCLUDED | CONTEXT.md records no deferred ideas. |

<output>
Create `.planning/phases/01-live-evidence-revalidation/01-70-SUMMARY.md` when done.
</output>
