---
phase: 01-live-evidence-revalidation
plan: 55
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04", "01-05", "01-06", "01-07", "01-08", "01-09", "01-10", "01-11", "01-12", "01-13", "01-14", "01-15", "01-16", "01-17", "01-18", "01-19", "01-20", "01-21", "01-22", "01-23", "01-24", "01-25", "01-26", "01-27", "01-28", "01-29", "01-30", "01-31", "01-32", "01-33", "01-34", "01-35", "01-36", "01-37", "01-38", "01-39", "01-40", "01-41", "01-42", "01-43", "01-44", "01-45", "01-46", "01-47", "01-48", "01-49", "01-50", "01-51", "01-52", "01-53", "01-54"]
files_modified:
  - scripts/revalidation.mjs
  - tests/architecture/revalidation.test.ts
  - .planning/phases/01-live-evidence-revalidation/shards/01-08.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-09.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-10.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-11.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-13.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-26.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-27.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-33.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-50.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-51.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-52.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-53.json
  - .planning/phases/01-live-evidence-revalidation/shards/01-54.json
  - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
  - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.md
autonomous: true
requirements: [RVAL-01, RVAL-02]
estimate:
  tokens: 125000
  raw_tokens: 125000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All 53 exclusive shards merge in assignment order into exactly 110 individually completed file records, 2,897 claims, and 2,437 findings without losing or overwriting evidence (D-01, D-03, D-04, D-05, D-10)."
    - "The merge rejects missing, duplicate, unexpected, and wrong-owner shard plans before replacing either canonical artifact (D-04, D-10)."
    - "The valueless --allow-incomplete, --allow-inconclusive, and --allow-pending-decisions flags each relax only their named transition class; pending-decision allowance accepts and preserves exactly MF-DEC-01 through MF-DEC-09 (D-03, D-10, D-13)."
    - "Generated Markdown is byte-derived from canonical JSON, and both destinations use atomic per-file replacement after the merged ledger validates (D-02, D-10)."
    - "Mechanical repairs retain every historical claim and evidence package without deciding cross-shard duplicates/conflicts or resolving an operator evidence gap assigned to plans 01-56 and 01-57 (D-05, D-09, D-13, D-14)."
  artifacts:
    - path: "scripts/revalidation.mjs"
      provides: "Validated, atomic merge CLI with three independent transition flags"
    - path: "tests/architecture/revalidation.test.ts"
      provides: "Public-CLI regression coverage for merge rejection, transition validation, and atomic output"
    - path: ".planning/phases/01-live-evidence-revalidation/shards/"
      provides: "Fifteen diagnosed shards inspected, with trace-preserving edits limited to the thirteen malformed shards"
    - path: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json"
      provides: "Deterministically merged 110-file claim ledger"
    - path: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.md"
      provides: "Generated merged evidence view"
  key_links:
    - from: "scripts/revalidation.mjs"
      to: "tests/architecture/revalidation.test.ts"
      via: "subprocess tests exercise the public CLI without test-only exports"
    - from: ".planning/phases/01-live-evidence-revalidation/shards/"
      to: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json"
      via: "pre-write assignment and merged-ledger validation followed by atomic replacement"
    - from: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json"
      to: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.md"
      via: "renderRevalidation is the only Markdown source"
---

<objective>
Repair the bounded shard/schema defects exposed by the first merge attempt, then merge every review shard through a validated atomic CLI path.

Purpose: Establish exact transport and coverage integrity before plan 01-56 adjudicates semantic duplicate/conflict candidates and plan 01-57 resolves evidence gaps.
Output: Tested merge tooling, bounded mechanical repairs across the 15 diagnosed shard sets, deterministically merged canonical JSON, and generated Markdown.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-live-evidence-revalidation/01-CONTEXT.md
@.planning/phases/01-live-evidence-revalidation/01-CORPUS-ASSIGNMENT.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION-SCHEMA.md
@.planning/phases/01-live-evidence-revalidation/01-01-SUMMARY.md
@.planning/phases/01-live-evidence-revalidation/01-56-PLAN.md
@.planning/phases/01-live-evidence-revalidation/01-57-PLAN.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
</context>

## Artifacts this plan produces

`scripts/revalidation.mjs` gains real boolean `--allow-inconclusive` and `--allow-pending-decisions` options and makes `merge-shards` validate before atomically replacing the canonical JSON and generated Markdown. No production module or owner unit test changes in this plan; the only TypeScript change is the architecture test for this repository evidence tool. The repair inspects all 15 diagnosed shard files, uses 01-24/01-25 as read-only collision anchors, and modifies only the other 13.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the corrected merge CLI through one complete public path</name>
  <files>scripts/revalidation.mjs, tests/architecture/revalidation.test.ts</files>
  <read_first>
    - scripts/revalidation.mjs
    - tests/architecture/revalidation.test.ts
    - .planning/phases/01-live-evidence-revalidation/01-CORPUS-ASSIGNMENT.md
    - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION-SCHEMA.md
    - .agents/skills/typescript-unit-testing-review/SKILL.md
  </read_first>
  <action>Correct the existing public CLI contract per D-02, D-03, D-04, D-10, and D-13. Parse `--allow-incomplete`, `--allow-inconclusive`, `--allow-pending-decisions`, and `--check` as independent valueless booleans. Give `validateLedger` three independent contexts: `allowIncomplete` suppresses only `incomplete-file`; `allowInconclusive` suppresses only `inconclusive-finding`; and `allowPendingDecisions` suppresses only `pending-decision`, and only when the preserved pending decision IDs are exactly MF-DEC-01 through MF-DEC-09 with no missing, duplicate, or unexpected ID. Every identity, assignment, link, category, route, reference, method, outcome, count, duplicate-topology, resolved-decision-premise, sensitive-evidence, and Markdown-drift violation remains blocking under every combination. Before constructing the plan map, make `mergeShards` reject duplicate shard plan IDs and shard plans absent from the assignment; retain the existing missing-plan and exact owner/path-order rejection. In the `merge-shards` CLI branch, validate the complete merged ledger against the live project root before any destination write, including exact 110 and 45/58/7 inventory counts. Make `--check` perform the full merge and validation without changing destinations. Without `--check`, serialize the JSON and derived Markdown first, then replace each destination through a same-directory temporary file and rename with cleanup on failure; do not print JSON as a substitute for writing it. Add public-CLI subprocess cases using case-owned temporary roots that prove: all four booleans need no value; each transition flag relaxes only its named class; incomplete files still fail when only the other two allowances are active; pending-decision allowance accepts and preserves exactly MF-DEC-01 through MF-DEC-09 and rejects a missing or unexpected decision ID; missing, duplicate, unexpected, and wrong-owner shards fail without changing pre-existing destination bytes; a wrong 45/58/7 category distribution fails before either write; successful `--check` leaves both pre-existing destinations byte-identical; a successful publishing merge writes both files; repeated publishing merges are byte-identical; and Markdown equals `renderRevalidation` output. Do not export the parser, writer, or `main` solely for tests, and install no package.</action>
  <acceptance_criteria>A temporary fixture can traverse shard directory → validated merged ledger → atomically replaced JSON → generated Markdown through the public CLI, while every listed negative fixture fails before a destination changes.</acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/revalidation.test.ts</automated>
  </verify>
  <fails_when>The architecture test exits nonzero, any invalid or `--check` merge changes a destination, a boolean consumes a following option as its value, one allowance hides another class, the pending-decision set differs from MF-DEC-01 through MF-DEC-09, or a successful merge requires stdout redirection.</fails_when>
  <done>The merge CLI has an executable, regression-tested contract that plan tasks 2-3 and plans 01-56/01-57 can rely on.</done>
</task>

<task type="auto">
  <name>Task 2: Apply only the proven mechanical/schema shard repairs</name>
  <files>.planning/phases/01-live-evidence-revalidation/shards/01-08.json, .planning/phases/01-live-evidence-revalidation/shards/01-09.json, .planning/phases/01-live-evidence-revalidation/shards/01-10.json, .planning/phases/01-live-evidence-revalidation/shards/01-11.json, .planning/phases/01-live-evidence-revalidation/shards/01-13.json, .planning/phases/01-live-evidence-revalidation/shards/01-26.json, .planning/phases/01-live-evidence-revalidation/shards/01-27.json, .planning/phases/01-live-evidence-revalidation/shards/01-33.json, .planning/phases/01-live-evidence-revalidation/shards/01-50.json, .planning/phases/01-live-evidence-revalidation/shards/01-51.json, .planning/phases/01-live-evidence-revalidation/shards/01-52.json, .planning/phases/01-live-evidence-revalidation/shards/01-53.json, .planning/phases/01-live-evidence-revalidation/shards/01-54.json</files>
  <read_first>
    - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION-SCHEMA.md
    - .planning/phases/01-live-evidence-revalidation/01-CORPUS-ASSIGNMENT.md
    - .planning/phases/01-live-evidence-revalidation/shards/01-08.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-09.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-10.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-11.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-13.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-24.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-25.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-26.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-27.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-33.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-50.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-51.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-52.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-53.json
    - .planning/phases/01-live-evidence-revalidation/shards/01-54.json
  </read_first>
  <action>Inspect the 15 diagnosed shard sets and apply the bounded transform already proved against them, preserving D-05/D-07 traceability; 01-24 and 01-25 are read-only comparison anchors, so only the 13 paths in this task's `<files>` may change. For 01-08, replace ANG-002's directory reference with `tests/architecture/partial-vocabulary-guard.test.ts`. For 01-09 through 01-11, materialize `duplicateOf: null` on every non-duplicate finding that omitted the field, retaining the six existing duplicate links exactly. For 01-10, move HSA-013's source reference to `extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts`. For 01-13 and any other retired reference with no live one-to-one owner, use the approved trace-preserving removal: remove it from active path references and add an explicit `N/A` reference reason naming the retired path; never infer staleness from absence per D-12. Compare collisions against 01-24/01-25 without editing those anchors; in 01-26/01-27 only, namespace each colliding finding ID under its owning corpus path and update every local claim, duplicate, decision, and scope link through one old→new map, leaving 01-49's valid OPIC links untouched. For 01-33, normalize the eleven `evidence-only` routes to `evidence-only closure`; move the four test paths incorrectly stored in `sourceRefs` into `testRefs` with an explicit test-only `N/A` source reason, and give the remaining twelve empty `testRefs` explicit `N/A` reasons without changing their evidence status. For 01-50/01-51, treat each existing local source-claim ID as the preserved report label: set `label` to that old ID, set `id` to `<filePath>#<oldId>`, derive `findingId` by reversing the existing finding `claimIds`, and update file/finding claim links through the same map; fail rather than guess if an old claim has zero or multiple finding owners. Apply the same preserved-local-ID rule to 01-52 through 01-54 (`CLAIM-NNN` is already present after `#`, so update `label` and all links consistently). Normalize the six 01-50/01-51 report categories to `first-pass`. Recalculate derived outcomes with the validator's existing rule after all link changes: all six 01-50/01-51 files, all three 01-53 files, and 01-54's `transaction.md` record must receive their derived value. Translate 01-50's retired `extensions/src` and `extensions/test` references by current symbol to the live `extensions/pi-claude-marketplace/orchestrators/plugin/` and `tests/orchestrators/plugin/` owners (including enable-disable, git-source-probe, and kebab-case names); where no live one-to-one owner exists, use the same trace-preserving `N/A` record. Repair the known moved references in 01-54 to the live notification-boundary/producer evidence. Do not add or choose canonical duplicate links, resolve first-pass/adversarial disagreements, change an evidence status, execute a new probe, clear an inconclusive finding, or make an operator choice: those are plan 01-56 or 01-57 work per D-09, D-11, D-13, and D-14. The transform must finish with exactly 15 diagnosed shards inspected, 13 shards modified, 110 files, 2,897 source claims, and 2,437 findings across all shards.</action>
  <acceptance_criteria>Every repaired shard retains the same file/claim/finding cardinality and evidence payload, all schema fields and current path references are valid, every claim is namespaced and linked exactly once, and no semantic adjudication has been performed.</acceptance_criteria>
  <verify>
    <automated>node scripts/revalidation.mjs merge-shards --assignment .planning/phases/01-live-evidence-revalidation/01-CORPUS-ASSIGNMENT.md --shard-dir .planning/phases/01-live-evidence-revalidation/shards --check --allow-inconclusive --allow-pending-decisions</automated>
  </verify>
  <fails_when>The command exits nonzero; any shard file/path owner changes; totals differ from 110/2,897/2,437; an old claim cannot be mapped one-to-one; a valid current reference is replaced by `N/A`; or a status/conflict/operator outcome is adjudicated.</fails_when>
  <done>All and only the 15 diagnosed shard sets are structurally valid and trace-preserving, so semantic duplicate/conflict review remains reachable in plan 01-56.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the transport-complete canonical ledger</name>
  <files>.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json, .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.md</files>
  <read_first>
    - .planning/phases/01-live-evidence-revalidation/01-CORPUS-ASSIGNMENT.md
    - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION-SCHEMA.md
    - scripts/revalidation.mjs
  </read_first>
  <action>Invoke the corrected `merge-shards` command against all 53 exclusive shards with both `--allow-inconclusive` and `--allow-pending-decisions`; do not redirect stdout and never edit generated Markdown directly. Require the pre-write gate to prove exact assignment membership and order, 110/45/58/7 file coverage, 2,897 unique namespaced claims, 2,437 unique findings, valid bidirectional claim links, schema-valid current references/routes/outcomes, complete file reviews, and no unapproved data loss. Preserve exactly the base ledger's MF-DEC-01 through MF-DEC-09 pending records and scope records. Run the public validator with the same two explicit allowances after publication and rerun the focused architecture test; the only tolerated nonterminal states are inconclusive evidence assigned to 01-56/01-57 and those nine pending decisions assigned to 01-58 through 01-66.</action>
  <acceptance_criteria>The canonical JSON is the deterministic merge of all assignments and the canonical Markdown is its exact generated view; both can be regenerated byte-identically without consuming downstream adjudication.</acceptance_criteria>
  <verify>
    <automated>node scripts/revalidation.mjs inventory &amp;&amp; node scripts/revalidation.mjs merge-shards --assignment .planning/phases/01-live-evidence-revalidation/01-CORPUS-ASSIGNMENT.md --shard-dir .planning/phases/01-live-evidence-revalidation/shards --allow-inconclusive --allow-pending-decisions &amp;&amp; node scripts/revalidation.mjs validate --allow-inconclusive --allow-pending-decisions &amp;&amp; node --test tests/architecture/revalidation.test.ts</automated>
  </verify>
  <fails_when>Any command exits nonzero, a pending file review is tolerated, output counts or order differ, a claim/finding disappears or overwrites another, a destination is partially written, Markdown drifts, or the merge performs plan 01-56/01-57 adjudication.</fails_when>
  <done>All 110 inputs and every claim are published deterministically with transport/coverage integrity, ready for semantic canonicalization in plan 01-56.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| parallel shards → canonical ledger | Independently authored evidence may collide, omit, or overwrite |
| merge validation → repository artifacts | Invalid or interrupted input must not partially replace canonical evidence |
| unresolved proof → downstream authority | Mechanical repair must not silently adjudicate evidence or operator policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-55-01 | Tampering | shard merge | high | mitigate | Reject missing, duplicate, unexpected, and wrong-owner shard plans; validate all merged invariants before destination replacement. |
| T-01-55-02 | Tampering/Denial of Service | canonical writes | high | mitigate | Serialize both outputs first and use same-directory atomic replacement with cleanup; negative CLI fixtures prove invalid input leaves prior bytes unchanged. |
| T-01-55-03 | Repudiation | mechanical identity repair | medium | mitigate | Use deterministic old→new maps, retain every claim/evidence record, and leave semantic duplicate/conflict adjudication to 01-56. |
| T-01-55-04 | Information Disclosure/Elevation | non-hermetic probes | high | mitigate | Run no new evidence probe in this repair; leave every unresolved authorization choice to 01-57. |
| T-01-55-SC | Tampering | package installs | high | mitigate | No package install is authorized. |
</threat_model>

<verification>
`node scripts/revalidation.mjs validate --allow-inconclusive --allow-pending-decisions` passes with 110/110 completed files, exact 45/58/7 categories, 2,897 unique linked claims, 2,437 unique findings, exactly MF-DEC-01 through MF-DEC-09 intentionally pending, all structural/schema checks active, and byte-current Markdown. Each allowance is independent; strict zero-inconclusive and operator-decision gates remain downstream.
</verification>

<success_criteria>
The complete corpus is merged with exact transport/coverage integrity; every claim survives traceably; invalid input cannot replace canonical artifacts; and plans 01-56/01-57 retain exclusive ownership of semantic conflict resolution and evidence-gap choices.
</success_criteria>

<output>
Create `.planning/phases/01-live-evidence-revalidation/01-55-SUMMARY.md` when done.
</output>
