---
phase: 01-live-evidence-revalidation
plan: 72
type: execute
wave: 20
depends_on: ["01-71"]
files_modified:
  - scripts/revalidation.mjs
  - scripts/revalidation.negative.mjs
  - tests/architecture/revalidation.test.ts
  - .planning/phases/01-live-evidence-revalidation/01-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [RVAL-01, RVAL-02, RVAL-03, RVAL-04]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The exact Phase 2-9 titles are sealed in `scripts/revalidation.mjs` outside the mutable ledger, and `scope-impact --check` compares BOTH mutable inputs — the parsed ROADMAP.md heading and the ledger `afterAnchor` — against that seal rather than against each other (D-20, D-23)."
    - "The exact canonical action for each of the 40 stable scope rows is sealed outside the mutable ledger, and a valid-but-wrong action such as PDEF-01 moving from `narrow/split` to `keep` fails closed (D-19, D-21, D-23)."
    - "Coordinated Phase 8 title-plus-afterAnchor drift exits nonzero through the public CLI; reverting the drift restores exit 0 and the exact line `Scope impact valid: 40 records.` (D-10)."
    - "Both new drift classes have a public-CLI control in `tests/architecture/revalidation.test.ts` AND an independent standalone control in `scripts/revalidation.negative.mjs`, each asserting the whole `{status, stdout, stderr}` result against a literal expectation (D-14)."
    - "The two new sealed tables are pinned to the existing sealed keysets by test, so a dropped or added entry is reported rather than silently narrowing the contract."
    - "`npm run check` exits 0 with no complexity suppression in the tree; unit >= 6109 with zero fail/skip/todo, integration == 32; direct coverage for `scripts/revalidation.mjs` stays 100% branches/functions/lines."
  artifacts:
    - path: "scripts/revalidation.mjs"
      provides: "Sealed phase-title and sealed scope-action contracts, compared against both mutable inputs"
      contains: "SEALED_PHASE_CONTRACTS"
    - path: "tests/architecture/revalidation.test.ts"
      provides: "Public-CLI controls for coordinated title/afterAnchor drift and valid-but-wrong action drift, plus keyset pins for both new tables"
      contains: "SEALED_REQUIREMENT_ACTIONS"
    - path: "scripts/revalidation.negative.mjs"
      provides: "Independent standalone witnesses for the same two drift classes"
      contains: "scope-action-contract"
    - path: ".planning/phases/01-live-evidence-revalidation/01-VALIDATION.md"
      provides: "Current final command-and-status record for the Phase 1 hard gate"
      contains: "npm run check"
  key_links:
    - from: "scripts/revalidation.mjs"
      to: ".planning/ROADMAP.md"
      via: "sealed exact Phase 2-9 title compared against the parsed `### Phase N: <title>` heading"
      pattern: "SEALED_PHASE_CONTRACTS"
    - from: "scripts/revalidation.mjs"
      to: ".planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json"
      via: "sealed exact action compared against each of the 40 scope rows, and sealed title compared against each route `afterAnchor`"
      pattern: "scope-action-contract"
    - from: "tests/architecture/revalidation.test.ts"
      to: "scripts/revalidation.mjs"
      via: "plant-then-revert public-CLI round trip on a case-owned temporary root"
      pattern: "scope-impact.*--check"
---

<objective>
Close the remaining Phase 1 scope-seal gap: make the exact Phase 2-9 titles and the exact per-row scope actions contracts held outside the mutable ledger, so the checker stops comparing one mutable input against another and starts comparing both against a fixed seal.

Purpose: Two independently reproduced drift probes still return exit 0 today. A gate that cannot be shown to fail is exactly the defect class this work exists to retire, so every new seal ships with a planting proof (D-10).
Output: Two new sealed tables, two new fail-closed violation classes, matched public-CLI and standalone controls, and a refreshed hard-gate record.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/typescript-comments.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-live-evidence-revalidation/01-CONTEXT.md
@.planning/phases/01-live-evidence-revalidation/01-VERIFICATION.md
@.planning/phases/01-live-evidence-revalidation/01-VALIDATION.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@scripts/revalidation.mjs
@scripts/revalidation.negative.mjs
@tests/architecture/revalidation.test.ts

<interfaces>
Existing surfaces this plan extends. Confirm each against the live file before editing — line numbers shift.

- `SEALED_REQUIREMENT_SIGNATURES` and `SEALED_REQUIREMENT_ROUTES` are frozen object literals keyed by bare requirement ID. `SEALED_REQUIREMENT_IDS` is DERIVED from `Object.keys(SEALED_REQUIREMENT_ROUTES)` so the lookup is total by construction. Do not reintroduce a presence check on it and do not change that derivation.
- `validatePhaseContracts(requirements, phases, rows, locators, violations)` loops phase numbers 1..9, builds `PHASE-NN`, fetches the ledger route row and the parsed roadmap phase, and calls `validatePhaseRequirements` then `validatePhaseAfterAnchor`.
- `validatePhaseAfterAnchor(phaseId, number, phase, change, locators, violations)` currently compares `locators.get(change.id)?.after[1]` against the template `` `${phaseId} / Phase ${number} ${phase.title}` ``. Both sides of that comparison are mutable inputs. This is the exact hole probe (a) walks through.
- `parseRoadmapContract` captures the title from `` /^### Phase (\d+): (.+)$/gm `` into `phase.title`, so the sealed value is the heading text with no leading `Phase N: ` prefix.
- `validateRequirementDisposition(requirementId, action, disposition, violations)` branches only on `action === "move-to-evidence"`. `validateRequirementClause` likewise selects the history map only for that one action. Both treat `keep` and `narrow/split` as interchangeable. This is the exact hole probe (b) walks through.
- `validatePlanningContracts(ledger, requirementsMarkdown, roadmapMarkdown)` builds `rows` (row id -> change), `validRowIds` and `locators`, then calls `validateRequirementContracts` and `validatePhaseContracts`, then checks the 40-row count, then sorts violations on `code\0target`.
- `violation(code, target, message)` is the existing constructor. `SCOPE_ACTIONS` is the frozen set `{keep, move-to-evidence, narrow/split}`.
- `validateRequirementClause` is the idiom to copy for a sealed lookup: it COMPARES `SEALED_REQUIREMENT_SIGNATURES[requirementId]` rather than reading through it, so an unsealed ID is reported instead of throwing on a property of `undefined`.
- Test seams: `runCli(projectRoot, args)` (child process, returns `{status, stdout, stderr}`), `createScopeImpactFixture(t)` (mkdtemp copy of the ledger plus both contracts, cleanup registered via `t.after`), and `sealedTableIds(source, table)` (reads a frozen table's keys out of the validator source; its regex is `` /^ {2}"([A-Z]+-\d+)":/gm ``, which matches both `"AUTH-01":` and `"PHASE-02":` but NOT `"SCOPE-REQ-AUTH-01":`). Add no test-only production export.
- `scripts/revalidation.negative.mjs` uses `invokeCli(projectRoot, args)` against its own `mkdtemp` root, restores the benign contract bytes between mutations, and ends by writing `Revalidation negative controls passed.`

Sealed values measured on the live tree (2026-09-12). Re-read the live files and confirm before writing them as literals; a wrong literal fails closed immediately, which is the desired failure mode but wastes a cycle.

Exact Phase 2-9 titles, from `.planning/ROADMAP.md` `### Phase N:` headings:

| Phase | Sealed title |
|---|---|
| PHASE-02 | Containment and Input Safety |
| PHASE-03 | Production Defect Corrections |
| PHASE-04 | Hermetic Test Infrastructure |
| PHASE-05 | Injection and Ownership Design |
| PHASE-06 | Assertion and Module Refinement |
| PHASE-07 | Gate Integrity |
| PHASE-08 | Direct Coverage |
| PHASE-09 | Final Quality and Backlog Closure |

Exact canonical action for all 40 rows, from `01-REVALIDATION.json` (`narrow/split` 31, `keep` 7, `move-to-evidence` 2):

| Action | Rows |
|---|---|
| keep | CLOSE-01, PDEF-03, RCOV-03, RVAL-01, RVAL-02, RVAL-03, RVAL-04 |
| move-to-evidence | GGAT-02, RCOV-04 |
| narrow/split (requirements) | AUTH-01, CLOSE-02, GGAT-01, GGAT-03, GGAT-04, PDEF-01, PDEF-02, PDEF-04, PDEF-05, PDEF-06, PDEF-07, PDEF-08, RCOV-01, RCOV-02, TREF-01, TREF-02, TREF-03, TREF-04, TREF-05, TREF-06, TREF-07, TREF-08, TREF-09 |
| narrow/split (routes) | PHASE-02 through PHASE-09, all eight |

Phase 1's title is deliberately NOT sealed: no scope row references it, so it carries no scope-impact semantic. The report scopes the seal to Phase 2-9.
</interfaces>

<existing_expectation_impact>
Exactly ONE existing expectation changes, and the change is intentional. Every other expectation in both oracle files must stay byte-identical.

- `tests/architecture/revalidation.test.ts`, test `RVAL-04 scope-impact rejects phase title drift`: it rewrites the ROADMAP.md Phase 8 heading only and currently expects stderr `scope-after-anchor: SCOPE-ROUTE-PHASE-08: afterAnchor does not resolve to phase`. Once the seal is the anchor's authority, a roadmap-only title change no longer disagrees with the anchor — it disagrees with the seal — so this case must expect the new phase-title violation instead. Exit status stays 1, stdout stays empty, and the assertion stays a whole-result `deepStrictEqual`; nothing is weakened. Record this diagnostic change in the SUMMARY.

Two guards keep every other expectation stable, and both are mandatory:
- The sealed-action comparison runs ONLY when `SCOPE_ACTIONS.has(change.action)` is true. An unrecognized action already emits `invalid-scope-action`; the existing case that sets PDEF-01's action to `invent` expects that one line alone, and a second violation would break it. This mirrors `validateRequirementDisposition`, which already guards its sealed comparison behind `routeIsValid && statusIsValid`.
- Both sealed lookups emit nothing when the sealed entry is absent. An unsealed row id already emits `unexpected-scope-requirement` and a missing roadmap phase already emits `missing-phase-route`; the standalone `EVIL-99` control expects that exact five-line set, and an extra line would break it.

The sealed-action check belongs to the planning-contract path only (`validatePlanningContracts`). Do NOT add it to the strict `validateLedger` path — that path has fixtures carrying arbitrary actions on synthetic rows, and seeding it would break unrelated cases.
</existing_expectation_impact>
</context>

## Artifacts this plan produces

No new file path, package, schema, or external interface. Two new frozen tables and two new violation codes inside `scripts/revalidation.mjs`, matching controls in the two existing evidence owners, and a refreshed `01-VALIDATION.md`. This plan must NOT edit `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, or `01-REVALIDATION.json` — those are the contracts under seal, and editing them to make a check pass would invert the whole gate.

## Known out-of-scope red: strict `validate`

Measured 2026-09-12: `node scripts/revalidation.mjs validate` exits 1 with 945 `unsafe-reference`
violations. Every one is a stale source/test path in the canonical ledger's finding references —
`orchestrators/plugin/install.ts`, `shared/notify.ts`, `domain/resolver.ts`, `orchestrators/plugin/{list,update,reinstall}.ts`,
`tests/architecture/catalog-uat.test.ts` and their test pairs — pointing at modules that later
refactors split or renamed (for example commit `a5a21ed0`, which retired the install lifecycle hub).

This is PRE-EXISTING drift, it is OUTSIDE `npm run check` (the chain does not invoke `validate`), and
it is OUTSIDE both gaps this plan set closes. Do NOT chase it here, and above all do NOT edit
`01-REVALIDATION.json` to silence it — re-pointing 945 evidence references is a separate decision the
developer owns. No verify command in this plan asserts `validate` succeeds.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Seal the exact Phase 2-9 titles and close the coordinated title/anchor probe</name>
  <files>scripts/revalidation.mjs, tests/architecture/revalidation.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md (the eight `### Phase N:` headings, lines 218-860)
    - scripts/revalidation.mjs: the sealed-table block near lines 58-141, `parseRoadmapContract`, `validatePhaseContracts`, `validatePhaseAfterAnchor`, `validatePlanningContracts`
    - tests/architecture/revalidation.test.ts: `createScopeImpactFixture`, `runCli`, `sealedTableIds`, the keyset-pin test `RVAL-04 seals the same requirement IDs in the signature and route tables`, and the test `RVAL-04 scope-impact rejects phase title drift`
    - .planning/phases/01-live-evidence-revalidation/01-VERIFICATION.md (the gap-1 `missing` list)
  </read_first>
  <behavior>
    - Test 1 (the coordinated probe, written first and witnessed failing): a case-owned root where the ROADMAP.md Phase 8 heading AND the SCOPE-ROUTE-PHASE-08 `beforeAnchor`/`afterAnchor` are changed together must make `scope-impact --check` exit 1 with empty stdout. Before the implementation lands, this case must be observed returning exit 0 with the success line — that observation is what proves the case is not vacuous.
    - Test 2 (roadmap-only title drift): the existing case keeps exit 1 and empty stdout, now reporting the sealed-title violation.
    - Test 3 (anchor-only route drift): changing only the SCOPE-ROUTE-PHASE-08 `afterAnchor` still exits 1 with the existing `scope-after-anchor` code, target and message unchanged.
    - Test 4 (keyset pin): the sealed phase table declares exactly the eight keys PHASE-02 through PHASE-09, asserted by membership AND by length so two empty lists cannot compare equal.
    - Test 5 (positive control): the untouched fixture still exits 0 with exactly `Scope impact valid: 40 records.` on stdout and empty stderr.
  </behavior>
  <action>Write all five cases first and run the focused name pattern to witness the coordinated case falsely passing. Then add a module-private frozen `SEALED_PHASE_CONTRACTS` table keyed `PHASE-02` through `PHASE-09`, each value a frozen object carrying the exact heading title and the canonical action for that route row, placed beside the existing sealed tables and following their formatting so `sealedTableIds` can read its keys. Add a check that the parsed roadmap `phase.title` equals the sealed title, emitting a new violation code distinct from every existing one, targeted at the phase id, with a message naming the roadmap title as the party that differs from the sealed contract. Change `validatePhaseAfterAnchor` so the expected anchor segment is built from the SEALED title rather than from `phase.title`, which makes the seal the authority for both mutable inputs instead of letting them vouch for each other. Guard every sealed lookup so an absent entry emits nothing, copying the compare-rather-than-read-through idiom already used for the requirement signature table. Update the one existing roadmap-only title-drift expectation to the new code, keeping the whole-result `deepStrictEqual` shape and the exit-1/empty-stdout assertions intact. Follow the repository unit-test rules: sibling `test()` cases, lowercase arrange/act/assert comments, whole structured comparisons against literal expectations, case-owned `mkdtemp` roots with cleanup registered before acting, no shared mutable fixture, no network, no test-only production export. Cite requirement and decision IDs in comments where the rationale is non-obvious; cite no phase, plan or wave.</action>
  <acceptance_criteria>The coordinated title/anchor case exits 1 after the change and was observed exiting 0 before it; roadmap-only and anchor-only drift each produce exactly one violation and remain distinguishable from one another; the keyset pin asserts eight members and length eight; the untouched fixture still prints the exact 40-record line; and the live repository command still succeeds.</acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/revalidation.test.ts --test-name-pattern='RVAL-04' &amp;&amp; node scripts/revalidation.mjs scope-impact --check</automated>
  </verify>
  <fails_when>The coordinated case passes; it was never observed failing first; the anchor-only case changed its violation code, target or message; more than one existing expectation in either oracle file changed; the sealed table is keyed in a shape `sealedTableIds` cannot read; the live command stops printing `Scope impact valid: 40 records.`; or a temporary fixture survives its case.</fails_when>
  <done>A roadmap phase title can no longer drift with its own ledger anchor for company — both are measured against a fixed contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal the exact action for all 40 scope rows and close the action-drift probe</name>
  <files>scripts/revalidation.mjs, tests/architecture/revalidation.test.ts</files>
  <read_first>
    - .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json (the `action` field on all 40 `scopeChanges` rows)
    - .planning/phases/01-live-evidence-revalidation/01-CONTEXT.md (D-19, D-20, D-21, D-23)
    - scripts/revalidation.mjs: `validateRequirementDisposition`, `validateRequirementClause`, `validateRequirementContracts`, `validatePlanningContracts`, `SCOPE_ACTIONS`
    - tests/architecture/revalidation.test.ts: the case that sets PDEF-01's action to `invent` and expects `invalid-scope-action` alone
  </read_first>
  <behavior>
    - Test 1 (the action probe, written first and witnessed failing): a case-owned root where SCOPE-REQ-PDEF-01's action changes from `narrow/split` to `keep` and NEITHER planning document changes must make `scope-impact --check` exit 1 with empty stdout and a single sealed-action violation targeted at the row. Before the implementation lands it must be observed exiting 0 with the success line.
    - Test 2 (the reverse direction): a `keep` row such as SCOPE-REQ-RVAL-01 changed to `narrow/split` also fails closed, proving the seal is an exact-value contract and not a one-way `keep`-is-suspicious heuristic.
    - Test 3 (a route row): SCOPE-ROUTE-PHASE-08's action changed to `keep` fails closed, proving all 40 rows are sealed and not just the 32 requirement rows.
    - Test 4 (structurally invalid action is unchanged): the existing case setting PDEF-01's action to `invent` still expects exactly `invalid-scope-action: SCOPE-REQ-PDEF-01: invent` and nothing else.
    - Test 5 (a `move-to-evidence` row): GGAT-02 changed to `keep` fails closed, and the case asserts the whole violation set so any additional clause or disposition violation the flip causes is pinned rather than ignored.
    - Test 6 (keyset pin): the sealed requirement-action table declares exactly the same 32 IDs as the signature and route tables, asserted by membership and by length.
  </behavior>
  <action>Write all six cases first and run the focused pattern to witness the two probe directions falsely passing. Then add a module-private frozen `SEALED_REQUIREMENT_ACTIONS` table keyed by bare requirement ID with the exact canonical action for each of the 32 requirement rows, formatted like the neighbouring sealed tables; the eight route actions already live in `SEALED_PHASE_CONTRACTS` from the previous task, so all 40 rows are covered without a third table. Add one small module-private validator, called from the planning-contract path only, that walks the row map and compares each row's action against its sealed value, emitting a new violation code distinct from every existing one and targeted at the full row id. Gate that comparison on `SCOPE_ACTIONS.has(change.action)` so a structurally invalid action keeps producing exactly the one violation it produces today, and skip silently when no sealed entry exists so unsealed-row diagnostics stay a single line. Do not seed this check into the strict ledger path. Keep the new validator small enough that it clears fallow cyclomatic 20, fallow cognitive 15 and ESLint cognitive 15 on its own, and give it a body that is not a near-duplicate of the phase-title validator, because `sonarjs/no-identical-functions` is an error at threshold 3 for this file. Build every expected value in the tests from a short explicit literal or from a copied contract plus one controlled mutation; never ask the production parser to produce the expected value.</action>
  <acceptance_criteria>Both probe directions and the route-row case exit 1 after the change and were observed exiting 0 before it; the `invent` case's expectation is byte-unchanged; the keyset pin asserts 32 members and length 32; the untouched fixture and the live repository command still print exactly `Scope impact valid: 40 records.`; and the new validator introduces no above-threshold function.</acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/revalidation.test.ts &amp;&amp; node scripts/revalidation.mjs scope-impact --check &amp;&amp; npx fallow health --fail-on-issues --format human &amp;&amp; npx eslint scripts/revalidation.mjs</automated>
  </verify>
  <fails_when>Either probe direction passes; either was never observed failing first; the `invent` expectation changed; a sealed action check fires on the strict `validate` path; the sealed table's keyset differs from the other two sealed tables; fallow or ESLint reports a new above-threshold function; or the live command stops succeeding.</fails_when>
  <done>A canonical scope action can no longer be swapped for another legal action without the gate saying so.</done>
</task>

<task type="auto">
  <name>Task 3: Prove both seals fail closed from outside the suite, then restore and record the hard gate</name>
  <files>scripts/revalidation.negative.mjs, .planning/phases/01-live-evidence-revalidation/01-VALIDATION.md</files>
  <read_first>
    - scripts/revalidation.negative.mjs (the whole scope-impact control block and its restore-between-mutations pattern)
    - .planning/phases/01-live-evidence-revalidation/01-VALIDATION.md
    - CLAUDE.md (git, pre-commit and commit-message rules)
  </read_first>
  <behavior>
    - The standalone runner plants coordinated Phase 8 title-plus-anchor drift under its own temporary root, asserts the whole `{status, stdout, stderr}` result against a literal expectation, restores the benign bytes, and continues.
    - The standalone runner plants PDEF-01 action drift from `narrow/split` to `keep` with both planning documents untouched, asserts the whole result against a literal expectation, and restores.
    - A plant-then-revert round trip is executed against a disposable copy of the repository root, never against the real tree: each planted drift exits nonzero, and after reverting, the same command exits 0 and prints exactly `Scope impact valid: 40 records.` Both halves are recorded, because a plant that fails without a matching revert that passes proves only that something is broken, not that the seal is the thing that caught it.
    - The runner still ends by writing `Revalidation negative controls passed.` and leaves the real repository untouched.
    - `01-VALIDATION.md` records the current commands and their results, superseding the stale claim that the gate was last green at the earlier plan; prior recorded runs are kept, not rewritten.
  </behavior>
  <action>Add the two new standalone controls to the existing scope-impact block in the negative runner, following its established shape: mutate under the runner's own temporary root, assert the whole CLI result against a literal expected stderr, then restore the benign bytes before the next mutation. Then run the plant-then-revert round trip yourself on a disposable copy of the repository root created under the scratch directory — copy `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` and the canonical ledger into it, invoke the checker with the root override, and record the four observed results: coordinated drift nonzero, reverted copy exit 0 with the exact 40-record line, action drift nonzero, reverted copy exit 0 with the exact 40-record line. Never mutate the real `.planning/` files. Then run the whole gate chain plus direct coverage and capture the exact output. Refresh `01-VALIDATION.md` by appending a dated section that records the commands actually run and their results — the full gate, the scope-impact check, the focused suite counts, the standalone runner, and direct coverage — and correct the sign-off line so it no longer asserts a green seal that has since been superseded; do not delete or reword the historical sections above it and do not renumber anything. Before committing, run `pre-commit run --files` over every changed path, fix any failure, restage and re-run until clean; never pass `--no-verify` and never amend to recover from a hook failure, and re-inspect `git status` afterwards because the prettier hook can rewrite files mid-commit. Commit with `SKIP=trufflehog` on branch `features/refine-unit-tests`, never on main, staging explicit paths only because this checkout is edited concurrently. Use a Conventional Commits message with a title at most 72 characters and body lines at most 80, naming the sealed contracts and the closed drift classes, and naming no milestone, phase, plan or wave.</action>
  <acceptance_criteria>Both new standalone controls pass and the runner still prints its exact success line; the plant-then-revert round trip is recorded with all four observations and touched no real planning file; `npm run check` exits 0 with no suppression marker in the tree; unit >= 6109 with zero fail/skip/todo and integration == 32; direct coverage for `scripts/revalidation.mjs` is 100% on branches, functions and lines; and `01-VALIDATION.md` states the current gate result with its history intact.</acceptance_criteria>
  <verify>
    <automated>node scripts/revalidation.negative.mjs &amp;&amp; ! grep -q 'fallow-ignore' scripts/revalidation.mjs &amp;&amp; npm run check &amp;&amp; node scripts/revalidation.mjs scope-impact --check &amp;&amp; npm run test:coverage:direct -- scripts/revalidation.mjs</automated>
  </verify>
  <fails_when>A standalone control passes vacuously or the runner's success line changes; the revert half of either round trip does not return exit 0 with the exact 40-record line; any real `.planning/` file was mutated; `npm run check` exits nonzero; the unit count falls below 6109 or reports a skip/todo; the integration count is not 32; direct coverage drops below 100% on any dimension; `01-VALIDATION.md` loses a historical section; or a pre-commit hook is bypassed.</fails_when>
  <done>Both new seals are proved fail-closed by planting from outside the suite, the revert proves the seal is what caught the drift, and the phase's hard-gate record matches reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical ledger -> planning-contract checker | `01-REVALIDATION.json` is mutable structured input. Any field it supplies that also acts as its own expected value is an unguarded self-assertion. |
| REQUIREMENTS.md / ROADMAP.md -> later-phase planning | A false-passing seal authorizes stale scope or silently drops terminal scope for every downstream phase; this is the highest-impact failure in the milestone. |
| sealed tables -> the two mutable inputs | The seal is the only fixed authority. If a check compares two mutable inputs to each other, coordinated drift passes, which is the defect being closed. |
| injected repository root -> filesystem | Probes and controls supply their own root; every read must stay inside it and no plant may reach the real `.planning/` tree. |

## STRIDE Threat Register

Security enforcement uses ASVS Level 1 and blocks unresolved high-severity threats.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-72-01 | Tampering / Repudiation | coordinated phase-title and route-anchor drift | high | mitigate | Seal the exact Phase 2-9 titles outside the ledger, compare BOTH the parsed roadmap heading and the ledger anchor against that seal, and prove the coordinated probe exits nonzero through the public CLI and the standalone runner. |
| T-01-72-02 | Tampering | valid-but-wrong scope action drift | high | mitigate | Seal the exact action for all 40 rows, compare each row against it, and prove both directions (`narrow/split` to `keep` and `keep` to `narrow/split`) plus a route row fail closed. |
| T-01-72-03 | Repudiation | a new seal that cannot be shown to fail | high | mitigate | Every new seal ships with a plant-then-revert round trip: planted drift exits nonzero, reverted copy exits 0 with the exact 40-record line. Tests are written and observed failing before the implementation lands. |
| T-01-72-04 | Spoofing | sealed-table keyset drift | high | mitigate | Pin both new tables by membership and by length against the existing sealed keysets, so a dropped entry is reported rather than silently narrowing the contract; keep every sealed lookup a compare, never a read-through. |
| T-01-72-05 | Elevation of Privilege | editing a contract to make the gate pass | high | mitigate | This plan touches no planning contract and no canonical ledger. Verification asserts the live `scope-impact --check` still succeeds against the unmodified real files, and all plants happen on disposable copies. |
| T-01-72-06 | Denial of Service | malformed planning markdown | medium | mitigate | Both new checks are bounded lookups over an already-parsed map with no recursion, no network and no unbounded scan; violations accumulate deterministically and sort before emission. |
| T-01-72-07 | Information Disclosure | temporary probe roots | low | mitigate | Every case owns a `mkdtemp` root with cleanup registered before acting; the standalone runner removes both roots in its `finally`. |
| T-01-72-SC | Tampering | package installs | high | mitigate | No package installation or dependency change is authorized. Use Node built-ins, the existing repository scripts, and the already-installed `fallow` and `eslint` only. |
</threat_model>

<verification>
  <automated>node --test tests/architecture/revalidation.test.ts &amp;&amp; node scripts/revalidation.negative.mjs &amp;&amp; node scripts/revalidation.mjs scope-impact --check &amp;&amp; ! grep -q 'fallow-ignore' scripts/revalidation.mjs &amp;&amp; npm run check &amp;&amp; npm run test:coverage:direct -- scripts/revalidation.mjs</automated>
  <fails_when>Any command exits nonzero; `scope-impact --check` prints anything other than `Scope impact valid: 40 records.`; either drift probe still passes; a suppression marker is present; direct coverage is not 100% on all three dimensions; the unit count falls below 6109 or the integration count is not 32; or any file outside the four declared paths changed.</fails_when>
</verification>

<success_criteria>
- The two independently reproduced false-pass probes are closed, and each is closed by a seal held outside the mutable ledger rather than by a tighter comparison between two mutable inputs.
- Every new seal is proved fail-closed by planting: drift exits nonzero, revert restores the exact success line.
- Both new drift classes have a public-CLI control and an independent standalone control, each asserting the whole structured CLI result.
- Exactly one pre-existing expectation changed, it is documented, and it was not weakened.
- RVAL-04 becomes independently verifiable while RVAL-01, RVAL-02 and RVAL-03 stay green, and the hard gate stays green with nothing suppressing it.
- No planning contract, canonical ledger, generated report, STATE.md, ROADMAP.md, package manifest or product file is modified.
</success_criteria>

<output>
Create `.planning/phases/01-live-evidence-revalidation/01-72-SUMMARY.md` when done. Record the four plant-then-revert observations verbatim, the one changed expectation and why, and the exact gate output.
</output>
