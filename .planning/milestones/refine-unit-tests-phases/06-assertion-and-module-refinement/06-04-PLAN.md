---
phase: 06-assertion-and-module-refinement
plan: "04"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tests/index.test.ts
  - tests/orchestrators/import/execute.test.ts
  - tests/orchestrators/plugin/scope-tree-inventory.ts
autonomous: true
requirements: [TREF-08]
estimate: {tokens: 15000, raw_tokens: 15000, tasks: 2, confidence: low}
must_haves:
  truths: ["All remaining authorized patch consumers are removed and the exact residual census is only the two protected files (D-06-07, D-06-10)."]
  artifacts: [tests/index.test.ts, tests/orchestrators/plugin/scope-tree-inventory.ts]
  key_links: ["Phase 5 manifest -> classified removals -> exact residual census"]
  prohibitions: []
---
<objective>Close authorized global-patch removal with the exact residual census. Purpose: establish a clean structural boundary before module splitting. Output: final patch-free scoped consumers and a deterministic census.</objective>
<execution_context>@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/REQUIREMENTS.md
@.planning/phases/06-assertion-and-module-refinement/06-CONTEXT.md
@.planning/phases/06-assertion-and-module-refinement/06-RESEARCH.md
@.planning/phases/05-test-infrastructure-hardening/05-34-SUMMARY.md</context>
<flagged_assumptions>- TREF-08 unclassified probe: the authorized end-state is exactly 2 files/18 `syncBuiltinESMExports(` calls and 2 files/2 `createRequire(` calls; ER-F19 dense indexing remains Phase 8.</flagged_assumptions>
<tasks>
<task type="auto" tdd="true">
  <name>06-04-01: Remove index and import executor patching</name>
  <files>tests/index.test.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>tests/index.test.ts; tests/orchestrators/import/execute.test.ts; .planning/phases/05-test-infrastructure-hardening/05-34-SUMMARY.md; .planning/phases/06-assertion-and-module-refinement/06-RESEARCH.md</read_first>
  <behavior>- Index/import cases use injected or case-owned state. - Public registration/import outcomes remain exact.</behavior>
  <action>Replace authorized builtin loading/patching with existing registration and import seams. Preserve observable command registration, import state, notifications, and cleanup; introduce no ignore pragma or test-only export. Apply PATTERNS.md `Authorized global-patch removal`, following its `Real-state analogs` (`tests/domain/manifest-cache.test.ts` and `tests/orchestrators/plugin/install.test.ts`) for hermetic case-owned state: remove patch/restoration together and retain public registration/import outcome proof.</action>
  <acceptance_criteria>Neither file contains the scoped patch calls and both owner suites retain their public-result assertions.</acceptance_criteria>
  <verify><automated>node --test tests/index.test.ts tests/orchestrators/import/execute.test.ts</automated><fails_when>non-zero exit or a scoped patch call remains</fails_when></verify>
  <done>Index and import tests are isolated.</done>
</task>
<task type="auto" tdd="true">
  <name>06-04-02: Remove scope-tree loader patch and prove exact residual census</name>
  <files>tests/orchestrators/plugin/scope-tree-inventory.ts</files>
  <read_first>tests/orchestrators/plugin/scope-tree-inventory.ts; tests/bridges/skills/stage.test.ts; tests/orchestrators/plugin/uninstall.test.ts; .planning/phases/05-test-infrastructure-hardening/05-34-SUMMARY.md</read_first>
  <behavior>- Scope-tree inventory uses its explicit inputs. - Residual counts equal the two protected sites only.</behavior>
  <action>Remove `createRequire` from the scope-tree helper using tracked direct imports/inputs. Then run a git-tracked census and require exactly the sanctioned residual: `tests/bridges/skills/stage.test.ts` plus `tests/orchestrators/plugin/uninstall.test.ts`, totaling 18 `syncBuiltinESMExports(` calls and 2 `createRequire(` calls. This plan does not modify either exclusion. Apply PATTERNS.md `Authorized global-patch removal`, including its exact residual allowlist/census contract and `Real-state analogs` for hermetic case-owned inputs; preserve the helper's public outcome proof.</action>
  <acceptance_criteria>The helper has no loader patch; census reports exactly 2/18 and 2/2; every other Phase 5 manifest path is zero.</acceptance_criteria>
  <verify><automated>npm run typecheck && npm run fallow && P06_SYNC_FILES="$(git grep -l 'syncBuiltinESMExports(' -- '*.ts')" && P06_SYNC_LINES="$(git grep -n 'syncBuiltinESMExports(' -- '*.ts')" && P06_REQUIRE_FILES="$(git grep -l 'createRequire(' -- '*.ts')" && P06_REQUIRE_LINES="$(git grep -n 'createRequire(' -- '*.ts')" && test "$(printf '%s\n' "$P06_SYNC_FILES" | wc -l)" -eq 2 && test "$(printf '%s\n' "$P06_SYNC_LINES" | wc -l)" -eq 18 && test "$(printf '%s\n' "$P06_REQUIRE_FILES" | wc -l)" -eq 2 && test "$(printf '%s\n' "$P06_REQUIRE_LINES" | wc -l)" -eq 2</automated><fails_when>non-zero exit or any residual file/call count differs from 2/18 and 2/2</fails_when></verify>
  <done>The authorized global-patch program closes at the exact protected census.</done>
</task>
</tasks>
<artifacts_this_phase_produces>No new paths or symbols; the final exact global-patch census becomes an enforced phase invariant.</artifacts_this_phase_produces>
<threat_model>
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-02 | Tampering | builtin modules | high | mitigate | Exact tracked census permits only two sanctioned exclusions. |
| T-06-05 | Tampering | structural closure | medium | mitigate | Fallow and typecheck detect bypasses and stale loader forms. |
</threat_model>
<verification><automated>node --test tests/index.test.ts tests/orchestrators/import/execute.test.ts && npm run typecheck && npm run fallow</automated><fails_when>non-zero exit</fails_when></verification>
<success_criteria>Authorized patch removal is complete and the residual inventory is exact.</success_criteria>
<output>Create `.planning/phases/06-assertion-and-module-refinement/06-04-SUMMARY.md` when done.</output>
