---
phase: 09-final-quality-and-backlog-closure
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
autonomous: true
requirements: [CLOSE-01, CLOSE-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - scripts/revalidation.mjs
  - tests/architecture/revalidation.test.ts

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-09-01: all eight sealed IDs — GGAT-01, GGAT-03, GGAT-04, RCOV-01, RCOV-02, RCOV-03, CLOSE-01, CLOSE-02 — end this plan at `Complete` in every carrier that names them."
    - "D-09-02: the flip lands in two atomic changes. Change A carries the six upstream IDs; change B carries CLOSE-01 and CLOSE-02 and lands only after the complete quality suite has been measured green on a tree that already carries change A."
    - "D-09-03 (corrected): each change moves the checkbox, the traceability row, the sealed route entry, and the one planted literal in `tests/architecture/revalidation.test.ts` that the flip invalidates — in one commit."
    - "D-09-03: no clause signature is recomputed. A status flip does not disturb the signature, and `.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json` is not edited by this plan."
    - "D-09-04: `node scripts/revalidation.mjs scope-impact --check` prints `Scope impact valid: 40 records.` and exits 0 after each change."
    - "The seal's own control suite passes at full strength after each change — 136 cases, zero failures."
    - "D-09-10: `npm run check` was measured green on the change-A tree and its real output recorded before change B landed."
  artifacts:
    - .planning/REQUIREMENTS.md
    - scripts/revalidation.mjs
    - tests/architecture/revalidation.test.ts
  key_links:
    - "The `## Traceability` row and the `SEALED_REQUIREMENT_ROUTES` entry for one ID are compared against each other by the route-contract check; either one moving alone exits 1."
    - "`tests/architecture/revalidation.test.ts` copies the live `.planning/REQUIREMENTS.md` into its fixture, so a flip invalidates any case that plants against a literal drawn from it."
    - "The pre-commit `npm-coverage-direct` hook fires on both `scripts/revalidation.mjs` and `tests/**/*.ts`, so a seal commit cannot land without its test edit."
  prohibitions:
    - "No clause-signature recomputation, and no edit to `01-REVALIDATION.json`, for a pure status flip."
    - "No single combined flip of all eight IDs — CLOSE-01 may not be flipped in the same change as the work it describes."
    - "No use of `gsd-tools requirements mark-complete` on its own; it writes two of the three surfaces and leaves the gate red."
    - "No split of one ID's carriers across commits."
    - "No bare `pre-commit run` — every invocation carries the `SKIP=trufflehog` prefix."
---

<objective>
Flip the eight sealed requirement IDs from `Pending` to `Complete` in two atomic changes, with the
complete quality suite measured green between them.

Purpose: `CLOSE-01` asserts the suite passes after all terminal work is complete. Flipping it in the
same change as that work writes a claim nobody has measured — the exact defect class this milestone
exists to retire. The split costs one extra commit and buys a claim that was true before it was
written.
Output: eight IDs reading `Complete` in every carrier, a gate that exits 0 after each change, and a
recorded verbatim suite measurement taken between them.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-final-quality-and-backlog-closure/09-CONTEXT.md
@.planning/phases/09-final-quality-and-backlog-closure/09-RESEARCH.md
@.planning/phases/09-final-quality-and-backlog-closure/09-02-SUMMARY.md
@.planning/phases/08-direct-coverage/08-08-SUMMARY.md
@.planning/phases/08-direct-coverage/08-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seal change A — the six upstream IDs</name>

  <precondition>
    `node scripts/revalidation.mjs scope-impact --check` prints `Scope impact valid: 40 records.` and
    exits 0 on the tree as it stands before any edit. If it does not, the baseline is already broken
    and nothing in this task can be attributed to the flip.
  </precondition>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the eight `- [ ]` clauses at lines 81, 84, 86, 92, 97, 107, 113
      and 116; the `## Traceability` table at lines 161-194, including an existing `Complete` row as
      the padding template; and `## Evidence and History` at lines 121-139 for the register the
      later plans match.
    - `scripts/revalidation.mjs` — `SEALED_REQUIREMENT_SIGNATURES` at line 58,
      `SEALED_REQUIREMENT_ROUTES` at line 93, the route-contract check at lines 2283-2298,
      `validateRequirementClause` at line 2300, and `normalizeRequirementClause` at line 2129 so you
      can see for yourself that the capture group begins after the `**ID**:` colon and the checkbox
      character is outside it.
    - `tests/architecture/revalidation.test.ts` — `createScopeImpactFixture` around line 289, which
      copies the real `.planning/REQUIREMENTS.md` into a temp root; and the case at line 1219 whose
      `marker` literal names GGAT-03's checkbox.
    - `.planning/phases/08-direct-coverage/08-09-SUMMARY.md` — the planting proof that a single-ID
      flip exits 1.
    - `.planning/phases/07-gate-integrity/07-VERIFICATION.md` and
      `.planning/phases/08-direct-coverage/08-VERIFICATION.md` — the evidence that these six IDs are
      pinned rather than unfinished.
  </read_first>

  <files>
    .planning/REQUIREMENTS.md,
    scripts/revalidation.mjs,
    tests/architecture/revalidation.test.ts
  </files>

  <action>
    One commit, covering all four carriers for all six IDs: `GGAT-01`, `GGAT-03`, `GGAT-04`,
    `RCOV-01`, `RCOV-02`, `RCOV-03`. A partial edit is not a smaller version of this change; it is a
    red gate.

    **Carrier 1 — the checkboxes.** In `.planning/REQUIREMENTS.md`, change `- [ ]` to `- [x]` on the
    definition line of each of the six IDs. Change nothing after the `**ID**:` colon. The clause text
    is what the sealed signature hashes, and 22 of the 30 IDs already read `- [x]` — this is the
    human-readable surface, and `CLOSE-02`'s honesty obligation requires it even though the gate does
    not read it.

    **Carrier 2 — the traceability rows.** In the same file's `## Traceability` table, change each of
    the six rows' `Status` cell from `Pending` to `Complete`. The table is hand-maintained — no
    formatter touches `.planning/` markdown — so preserve the column widths exactly: the `Status`
    cell is fourteen characters wide, so `Pending` followed by seven spaces becomes `Complete`
    followed by six. Copy the padding from an existing `Complete` row rather than counting.

    **Carrier 3 — the sealed routes.** In `scripts/revalidation.mjs`'s `SEALED_REQUIREMENT_ROUTES`,
    change each of the six entries' `status` from `Pending` to `Complete`. Leave every `route` value
    alone — the route is what phase membership is checked against and it does not move. Keys are
    alphabetically sorted; the flip shortens each line, so every one of them stays on the existing
    single-line form. The status string must match the traceability cell exactly.

    **Carrier 4 — the invalidated planted literal.** In `tests/architecture/revalidation.test.ts` at
    line 1219, the case `RVAL-04 scope-impact rejects an evidence ID with an active definition`
    plants against GGAT-03's checkbox marker as it reads in the live file. Update that marker's
    checkbox character to match. If the marker still names the unflipped form, its `.replace()` finds
    nothing, no violation is planted, and the case passes while proving nothing.

    **Do NOT touch** `.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json`. A status
    flip leaves every `requirementSignature` intact — the normalizer's capture starts after the ID
    colon and its pattern's character class accepts both checkbox states. Recomputing a signature
    here would rewrite a seal for a clause whose text did not change. Do not run
    `gsd-tools requirements mark-complete` either: it writes the checkbox and the traceability row
    and has no knowledge of the sealed table, so it reports success and leaves the gate red.

    Verify with `node scripts/revalidation.mjs scope-impact --check` BEFORE running pre-commit. Then
    run `SKIP=trufflehog pre-commit run --files .planning/REQUIREMENTS.md scripts/revalidation.mjs tests/architecture/revalidation.test.ts`
    and iterate until clean. That invocation fires the lint, format, typecheck, fallow and
    coverage-direct hooks; budget four to five minutes. Commit with a scope naming the artifact, such
    as `requirements`, never a phase or plan number.
  </action>

  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*\(GGAT-01\|GGAT-03\|GGAT-04\|RCOV-01\|RCOV-02\|RCOV-03\)\*\*:' .planning/REQUIREMENTS.md`
      prints `0`.
    - `grep -c '^- \[x\] \*\*\(GGAT-01\|GGAT-03\|GGAT-04\|RCOV-01\|RCOV-02\|RCOV-03\)\*\*:' .planning/REQUIREMENTS.md`
      prints `6`.
    - `grep -c '^| \(GGAT-01\|GGAT-03\|GGAT-04\|RCOV-01\|RCOV-02\|RCOV-03\) .*| Complete ' .planning/REQUIREMENTS.md`
      prints `6`.
    - Every line of the `## Traceability` table is the same character length as it was before the
      edit.
    - `grep -c '"\(GGAT-01\|GGAT-03\|GGAT-04\|RCOV-01\|RCOV-02\|RCOV-03\)": Object.freeze({ route: "Phase [78]", status: "Complete" })' scripts/revalidation.mjs`
      prints `6`.
    - `git hash-object .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json` prints
      `66218013acf1a69bd28b2380fdcf0c531556dff0` — the blob it carried on entry to this phase.
    - `node scripts/revalidation.mjs scope-impact --check` prints exactly
      `Scope impact valid: 40 records.` and exits 0.
    - `node --test tests/architecture/revalidation.test.ts` reports `# pass 136` and `# fail 0`.
    - Both `CLOSE-01` and `CLOSE-02` still read `Pending` in all three of their carriers — this
      change does not touch them.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/revalidation.mjs scope-impact --check</automated>
    <fails_when>any output other than `Scope impact valid: 40 records.`, or non-zero exit</fails_when>
    <automated>node --test tests/architecture/revalidation.test.ts</automated>
    <fails_when>fewer than 136 passing cases, or a summary line reading `# fail 1` or greater</fails_when>
    <automated>git hash-object .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json</automated>
    <fails_when>any output other than `66218013acf1a69bd28b2380fdcf0c531556dff0` — the scope-change ledger must be byte-identical after a pure status flip</fails_when>
    <automated>grep -c 'CLOSE-0[12]     | Phase 9                             | Pending       |' .planning/REQUIREMENTS.md</automated>
    <fails_when>any output other than `2`</fails_when>
  </verify>

  <done>
    Six IDs read `Complete` in the checkbox, the traceability row and the sealed route table; the one
    invalidated planted literal is updated in the same commit; the gate exits 0 with its exact
    baseline message; the control suite passes 136 of 136; and the scope-change ledger is untouched.
  </done>

  <reversibility rating="costly">
    Undoing a flip means moving all four carriers back together. The gate fails closed on any pair
    disagreeing, so a partial revert is a red tree, not a smaller revert (D-09-02).
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Measure the complete quality suite on the change-A tree</name>

  <precondition>
    Task 1 is committed and `git status --porcelain` prints nothing. The measurement is a statement
    about a committed tree; taking it over uncommitted edits makes it a statement about a tree that
    may never exist.
  </precondition>

  <read_first>
    - `package.json` `scripts.check` — the nine sub-commands the chain runs, in order.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-CONTEXT.md` D-09-10 — what "the
      complete project quality suite" means and the rule that neither run is reported from an earlier
      one.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-02-SUMMARY.md` — the coverage
      measurement this one sits on top of.
  </read_first>

  <files>none — this task measures and records; it changes no file</files>

  <action>
    Run `npm run check` from the repository root on the committed change-A tree and let the whole
    chain finish. It runs typecheck, lint, fallow, format:check, the two corresponding-test gates,
    the direct-coverage negative control, the unit suite and the integration suite, in that order,
    and it stops at the first non-zero exit.

    Record in the SUMMARY, verbatim: the final exit status, the unit suite's `# tests` / `# pass` /
    `# fail` / `# skipped` summary lines, and the integration suite's equivalents. Record the wall
    clock. This is the measurement that licenses change B under D-09-02, so it must be quoted, not
    paraphrased, and it must not be carried over from any earlier run in this phase or any earlier
    phase.

    If the chain exits non-zero, stop. Do not proceed to Task 3, do not flip CLOSE-01, and report
    which sub-command failed and its output. A red suite at this point means a claim the seal is
    about to make is false, which is precisely what this ordering exists to catch.

    This task produces no commit. Its deliverable is the recorded measurement.
  </action>

  <acceptance_criteria>
    - `npm run check` exits 0.
    - The SUMMARY quotes the unit suite's summary block verbatim, including `# fail 0`.
    - The SUMMARY quotes the integration suite's summary block verbatim, including `# fail 0`.
    - The SUMMARY states the date and the commit SHA the measurement was taken at, and states that
      the tree carried change A and not change B.
    - `git status --porcelain` prints nothing before and after the run.
  </acceptance_criteria>

  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit from any sub-command in the chain, or a `# fail 1` or greater summary line in either test suite</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>any output — the measurement must be taken over a clean committed tree</fails_when>
  </verify>

  <done>
    The complete quality suite is measured green on the committed change-A tree and its real output
    is recorded verbatim, so the claim change B is about to write is true before it is written.
  </done>
</task>

<task type="auto">
  <name>Task 3: Seal change B — CLOSE-01 and CLOSE-02</name>

  <precondition>
    Task 2 recorded `npm run check` exiting 0 on the committed change-A tree. Without that recorded
    measurement, `CLOSE-01`'s clause asserts something unmeasured and this task must not run.
  </precondition>

  <read_first>
    - `.planning/REQUIREMENTS.md` lines 113-119 — the two `CLOSE` clauses, and lines 193-194 — their
      traceability rows.
    - `scripts/revalidation.mjs` lines 95-96 — the two sealed route entries.
    - `tests/architecture/revalidation.test.ts` around line 1026 — the case
      `RVAL-04 scope-impact ignores content after a four-space fence pseudo-closer`, whose
      `traceabilityRow` literal is copied from the live file.
    - The measurement Task 2 recorded.
  </read_first>

  <files>
    .planning/REQUIREMENTS.md,
    scripts/revalidation.mjs,
    tests/architecture/revalidation.test.ts
  </files>

  <action>
    One commit, same four-carrier shape as Task 1, for `CLOSE-01` and `CLOSE-02` only.

    In `.planning/REQUIREMENTS.md`, flip both checkboxes to the checked form and both
    `## Traceability` status cells from `Pending` to `Complete`, preserving the fourteen-character
    cell width. In `scripts/revalidation.mjs`, flip both `SEALED_REQUIREMENT_ROUTES` entries' `status`
    to `Complete`, leaving `route: "Phase 9"` alone.

    In `tests/architecture/revalidation.test.ts` around line 1026, the `traceabilityRow` literal
    reproduces `CLOSE-02`'s traceability row as it reads in the live file, and the case plants a
    mutation by replacing it. Update the literal's status token to the flipped value. If it still
    names the unflipped form, the replacement finds nothing and the case passes having planted
    nothing — a gate reporting success over zero inspected bytes, which is the class of defect this
    milestone spent a phase removing.

    Do not recompute any signature and do not touch `01-REVALIDATION.json`.

    Verify with `node scripts/revalidation.mjs scope-impact --check` before pre-commit. Then run
    `SKIP=trufflehog pre-commit run --files .planning/REQUIREMENTS.md scripts/revalidation.mjs tests/architecture/revalidation.test.ts`
    and iterate until clean before committing.
  </action>

  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` prints `0` — no requirement clause in the
      file still reads unchecked.
    - `grep -c 'Pending' .planning/REQUIREMENTS.md` prints `0`.
    - `grep -c 'status: "Pending"' scripts/revalidation.mjs` prints `0`.
    - `grep -c '"CLOSE-0[12]": Object.freeze({ route: "Phase 9", status: "Complete" })' scripts/revalidation.mjs`
      prints `2`.
    - `git hash-object .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json` prints
      `66218013acf1a69bd28b2380fdcf0c531556dff0` — the blob it carried on entry to this phase.
    - `node scripts/revalidation.mjs scope-impact --check` prints exactly
      `Scope impact valid: 40 records.` and exits 0.
    - `node --test tests/architecture/revalidation.test.ts` reports `# pass 136` and `# fail 0`.
  </acceptance_criteria>

  <verify>
    <automated>node scripts/revalidation.mjs scope-impact --check</automated>
    <fails_when>any output other than `Scope impact valid: 40 records.`, or non-zero exit</fails_when>
    <automated>node --test tests/architecture/revalidation.test.ts</automated>
    <fails_when>fewer than 136 passing cases, or a summary line reading `# fail 1` or greater</fails_when>
    <automated>grep -c 'Pending' .planning/REQUIREMENTS.md scripts/revalidation.mjs</automated>
    <fails_when>either file reporting a count other than `0`</fails_when>
    <automated>git hash-object .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json</automated>
    <fails_when>any output other than `66218013acf1a69bd28b2380fdcf0c531556dff0`</fails_when>
  </verify>

  <done>
    All eight sealed IDs read `Complete` in every carrier, no `Pending` status remains in either
    enforced surface, both invalidated planted literals were updated in the same commit as the flip
    that invalidated them, the gate exits 0, and the control suite passes 136 of 136.
  </done>

  <reversibility rating="costly">
    Same shape as Task 1 — a partial revert leaves the route-contract check red (D-09-02).
  </reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new symbol. It produces one recorded result and one changed contract:

| Artifact | Kind | Outcome |
| --- | --- | --- |
| The verbatim `npm run check` measurement on the change-A tree | recorded result (in `09-03-SUMMARY.md`) | the evidence that licenses `CLOSE-01`'s flip |
| `SEALED_REQUIREMENT_ROUTES` in `scripts/revalidation.mjs` | sealed data table | eight entries move from `Pending` to `Complete` |
| `.planning/REQUIREMENTS.md` checkboxes and `## Traceability` rows | spec carrier | eight IDs move to the completed form |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → gate script | The requirement contract is data compared across two files; the checker is the only thing that keeps them from drifting apart. |
| live repository files → the gate's own control suite | `tests/architecture/revalidation.test.ts` copies the real `.planning/REQUIREMENTS.md` into its fixture, so the live file is untrusted input to the control suite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Repudiation | the `CLOSE-01` clause | high | mitigate | The two-change split (D-09-02) plus Task 2's recorded verbatim measurement make the claim true before it is written. Task 3 carries a `<precondition>` that names the recorded measurement as its entry condition. |
| T-09-11 | Tampering | `tests/architecture/revalidation.test.ts` planted literals | high | mitigate | Both invalidated literals are updated in the same commit as the flip that invalidates them, and each task's acceptance requires the control suite to report its full 136 passing cases — a silently no-op plant would still report `# fail 0` but is caught by the `# pass 136` floor together with the literal-specific greps. |
| T-09-12 | Tampering | `SEALED_REQUIREMENT_SIGNATURES` | medium | mitigate | The plan forbids any signature recomputation and asserts `01-REVALIDATION.json` is untouched, because a status flip provably leaves every clause hash intact and a gratuitous re-sign would reseal a clause whose text never changed. |
| T-09-13 | Elevation | `gsd-tools requirements mark-complete` | medium | mitigate | Named as prohibited on its own: it writes two of the three surfaces, reports success, and leaves the route-contract check red. |
| T-09-14 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; `package.json` is untouched. |
</threat_model>

<verification>
- `node scripts/revalidation.mjs scope-impact --check` prints `Scope impact valid: 40 records.` after
  change A and again after change B.
- `node --test tests/architecture/revalidation.test.ts` reports `# pass 136` / `# fail 0` after each.
- `npm run check` exits 0 on the change-A tree, with its output recorded verbatim.
- `grep -c 'Pending' .planning/REQUIREMENTS.md scripts/revalidation.mjs` prints `0` for both files at
  the end of the plan.
- `git hash-object .planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json` still
  prints `66218013acf1a69bd28b2380fdcf0c531556dff0` after both seal commits.
</verification>

<success_criteria>
Eight requirement IDs read `Complete` in all of their carriers, every fail-closed check that reads
them exits 0, and the claim `CLOSE-01` makes was measured on a real tree before it was written down.
</success_criteria>

<output>
Create `.planning/phases/09-final-quality-and-backlog-closure/09-03-SUMMARY.md` when done. The
closure ledger quotes Task 2's measurement, so record it verbatim with its commit SHA.
</output>
