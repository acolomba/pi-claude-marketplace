---
phase: 09-final-quality-and-backlog-closure
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
autonomous: true
requirements: [CLOSE-02]
files_modified:
  - .planning/WINDOWS.md

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-09-11: the rows 9 and 30 desync is repaired losslessly — the rendered table's authoritative text is written INTO the fenced JSON before any verb regenerates the table, so regeneration destroys nothing."
    - "D-09-12b: entry 9's status stays `open` deliberately. Only its description moves."
    - "D-09-12a: entry 30's status flip to `fixed` is accompanied by hand-written frontmatter counts, because `parseLedger` refuses a ledger whose frontmatter disagrees with its entries."
    - "D-09-12: entries 19, 21 and 22 are closed through the `windows fixed` verb, not by hand, because each says it closes only by a production rewrite that has already landed."
    - "D-09-13: nothing else is closed. The remaining open entries from phases 86, 88, 115, 116 and 117 stay open."
    - "The ledger's write verbs run without the drift guard refusing, and the regenerated table agrees with the fenced JSON field for field."
  artifacts:
    - .planning/WINDOWS.md
  key_links:
    - "The fenced JSON is the sole source of truth; `writeLedgerAtomic` re-renders the table from it and refuses if the on-disk table disagrees."
    - "`parseLedger` is a second, independent fail-closed check on the frontmatter counts, behind the table-drift guard — fixing the first reveals the second."
    - "After the verb runs it owns the counts; the hand-written pair is only needed to get past `parseLedger` on the repair edit itself."
  prohibitions:
    - "No hand-edit of the rendered markdown table — the verb owns it."
    - "No status change to entry 9."
    - "No closing of any entry other than 19, 21 and 22."
    - "No new closure vocabulary — the ledger's own `open` / `fixed` / `waived` set is the only one this file uses."
    - "No bare `pre-commit run` — every invocation carries the `SKIP=trufflehog` prefix."
---

<objective>
Repair the window ledger's rows 9 and 30 desync without losing a word of the resolution prose, then
close the three entries whose stated closure condition has already been met.

Purpose: the ledger's write verbs are blocked today by a drift guard, and three entries read `open`
while their own text says they close only by a production rewrite that landed inside this milestone.
A record that is false on its face is exactly what the closure requirement exists to fix.
Output: a ledger whose table and JSON agree field for field, with nineteen entries open and the
three false-open rows closed through the tool that owns the file.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-final-quality-and-backlog-closure/09-CONTEXT.md
@.planning/phases/09-final-quality-and-backlog-closure/09-RESEARCH.md
@.planning/phases/09-final-quality-and-backlog-closure/09-PATTERNS.md
@.planning/phases/08-direct-coverage/deferred-items.md
</context>

<edge_coverage>
One of the deterministic edge probe's four `unresolved` items has a second instance in this plan.

| Requirement | Category | Probe | Applicable here? | Where it is covered |
| --- | --- | --- | --- | --- |
| CLOSE-01 | empty | "What is the result for empty, single-element, or null input?" | **Yes, a second instance** — entry 30 carries `resolved_at: null` today, and null is a legal value the gate accepts. | Task 1 settles it deliberately rather than by default: the entry gets a populated timestamp, because leaving it null would also pass the gate but would make the closed row the only one in the file that cannot say when it closed. The rejected alternative is recorded in the action. |

The other three probe items are covered in `09-02-PLAN.md` (adjacency, empty, ordering against the
coverage pin) and `09-06-PLAN.md` (the CLOSE-02 unclassified item, read as the closed-disposition
vocabulary boundary).
</edge_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Repair the rows 9 and 30 desync in the fenced JSON and the frontmatter</name>

  <precondition>
    `node .claude/gsd-core/bin/gsd-tools.cjs windows status` exits 0 and reports `"open_count": 23`.
    The read verb is not blocked by the drift guard — only the write verbs are — so this is the
    baseline the repair moves from.
  </precondition>

  <read_first>
    - `.planning/WINDOWS.md` in full: the frontmatter counts block at lines 1-8, the rendered table's
      rows for ids 9 and 30, and the fenced JSON entries for ids 9 and 30 near the end of the file.
      Read both representations of both rows side by side before editing either.
    - `.planning/phases/08-direct-coverage/deferred-items.md` — the discovery record, including its
      claim that the disagreement is description-only. That claim is right about the JSON and wrong
      about the table: row 30 disagrees on status too.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-RESEARCH.md` §4.1 through §4.4 — the
      byte-level delta for both rows, the drift guard's refusal message, the second fail-closed check
      on the counts, and the proof that the full repair works end to end.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-PATTERNS.md` §D — the exact field
      shape of a `fixed` entry, using entry 31 as the template, including a populated `resolved_at`.
  </read_first>

  <files>.planning/WINDOWS.md</files>

  <action>
    Four edits, all inside the fenced JSON block and the frontmatter. Touch the rendered markdown
    table with nothing — the verb in Task 2 regenerates it, and a hand-edit there is what the drift
    guard refuses.

    **Edit 1 — entry 9's description.** The table's version of this description replaces the JSON
    version's closing sentence. In the JSON entry's `description`, replace the trailing sentence
    that asks for an operator decision with the table's `DECIDED 2026-09-02:` paragraph, copied from
    the rendered row verbatim, ending at `Resolution recorded in 115-VERIFICATION.md human_verification
    item 2.` Everything before that closing sentence is already identical in both representations
    and must not be retyped — edit only the tail.

    **Edit 2 — entry 9's status stays `open`.** Do not change it. Its own repaired text says it stays
    open deliberately, as the durable record of a residual risk rather than as a pending action
    (D-09-12b). Changing it would contradict the sentence being copied in.

    **Edit 3 — entry 30's description and status.** The table's version of this description is the
    JSON version's text followed by an appended `RESOLVED 2026-09-04 by operator decision:` paragraph.
    Append that paragraph to the JSON `description` verbatim, ending at `blocks nothing.` Change the
    entry's `status` from `open` to `fixed`, and set its `resolved_at` from null to the entry's own
    `recorded_at` timestamp — `2026-09-04T01:03:42.619Z`, which is the date its own prose names. A
    populated `resolved_at` is the shape every other `fixed` entry carries; leaving it null would also
    pass the gate but would make the closed row the only one in the file that cannot say when it
    closed.

    **Edit 4 — the frontmatter counts.** Entry 30 moving out of `open` and into `fixed` makes the
    frontmatter disagree with the entries, and `parseLedger` refuses on exactly that. Change
    `open_count` from 23 to 22 and `fixed_count` from 8 to 9 in the same edit. Leave `waived_count`,
    `total_count` and `last_updated` alone — the verb in Task 2 takes ownership of all of them from
    here.

    Confirm with a read verb before committing: `node .claude/gsd-core/bin/gsd-tools.cjs windows status`
    must exit 0 and report the new counts. Then run
    `SKIP=trufflehog pre-commit run --files .planning/WINDOWS.md` and iterate until clean. A
    `.planning/`-only commit fires none of the npm hooks and none of the markdown hooks, so this one
    is cheap.
  </action>

  <acceptance_criteria>
    - `.planning/WINDOWS.md` frontmatter reads `open_count: 22` and `fixed_count: 9`;
      `waived_count: 0` and `total_count: 31` are unchanged.
    - The fenced JSON entry with `"id": 9` has `"status": "open"` and `"resolved_at": null`, and its
      `description` ends with `Resolution recorded in 115-VERIFICATION.md human_verification item 2.`
    - The fenced JSON entry with `"id": 30` has `"status": "fixed"` and
      `"resolved_at": "2026-09-04T01:03:42.619Z"`, and its `description` ends with `blocks nothing.`
    - `grep -c '"status": "open"' .planning/WINDOWS.md` prints `22`.
    - `git diff .planning/WINDOWS.md` shows changes only inside the frontmatter block and the fenced
      JSON block — no line of the rendered markdown table appears in the diff.
    - For both entries, the JSON `description` is now character-identical to the table cell's text,
      allowing for the table's `\|` and `\\` cell escaping.
    - `node .claude/gsd-core/bin/gsd-tools.cjs windows status` exits 0 and reports
      `"open_count": 22`.
  </acceptance_criteria>

  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs windows status | grep -m1 '"open_count"'</automated>
    <fails_when>any output other than a line whose value is `22`, or non-zero exit</fails_when>
    <automated>grep -c '"status": "open"' .planning/WINDOWS.md</automated>
    <fails_when>any output other than `22`</fails_when>
    <automated>grep -c '"resolved_at": "2026-09-04T01:03:42.619Z"' .planning/WINDOWS.md</automated>
    <fails_when>an output of `0` — entry 30 must carry a populated resolution timestamp</fails_when>
  </verify>

  <done>
    Both desynced descriptions carry the table's authoritative text inside the JSON, entry 30 is
    `fixed` with a populated resolution timestamp, entry 9 is still `open` with only its description
    moved, the frontmatter counts agree with the entries, and the read verb exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Close entries 19, 21 and 22 through the ledger's own verb</name>

  <precondition>
    Task 1 is committed and `node .claude/gsd-core/bin/gsd-tools.cjs windows status` exits 0. The
    write verbs run the table-drift guard first and the counts check second; both must be satisfied
    before any of them can write.
  </precondition>

  <read_first>
    - `.planning/WINDOWS.md` fenced JSON entries 19, 21 and 22 — read each one's `description` and
      confirm for yourself that each names a dense-index guard it calls compiler-forced and
      unreachable, and says it closes only by a production rewrite.
    - `.planning/REQUIREMENTS.md` `RCOV-02`'s clause — the record that those three guards were
      honestly rewritten to typed iteration.
    - `.planning/phases/09-final-quality-and-backlog-closure/09-RESEARCH.md` §4.5 and §4.6 — the
      confirmation that all three are false-open, and the measured open-count breakdown by phase.
  </read_first>

  <files>.planning/WINDOWS.md</files>

  <action>
    From the repository root, run the ledger's own close verb once per entry, in ascending id order:
    `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 19`, then the same for `21`, then for `22`.
    Each must exit 0. The verb owns the rendered table, the `resolved_at` timestamps and the
    frontmatter counts from here — do not hand-edit any of them.

    Close nothing else. Nineteen entries remain open after this, distributed across phases 86, 88,
    115, 116 and 117, and phase 116 is the largest remaining group. Closing any of them on narrative
    rather than terminal evidence is what `D-22` bars and what D-09-13 refuses; whether an open window
    should block a release is a policy question for the operator at ship time, not something this
    plan settles by emptying the file.

    After the three verb runs, confirm the regenerated table agrees with the fenced JSON field for
    field. The simplest proof is that a further write verb would not refuse — but rather than mutate
    the ledger again, parse both representations and compare every field of all 31 entries; a
    mismatch count of zero is the proof.

    Run `SKIP=trufflehog pre-commit run --files .planning/WINDOWS.md` and iterate until clean before
    committing. One commit for the three closures.
  </action>

  <acceptance_criteria>
    - Each of the three `windows fixed` invocations exits 0.
    - `node .claude/gsd-core/bin/gsd-tools.cjs windows status` reports `"open_count": 19` and
      `"fixed_count": 12`; `waived_count` is still `0` and `total_count` is still `31`.
    - `grep -c '"status": "open"' .planning/WINDOWS.md` prints `19`.
    - The fenced JSON entries with ids 19, 21 and 22 each read `"status": "fixed"` and each carries a
      non-null `resolved_at`.
    - A field-by-field comparison of all 31 rendered table rows against all 31 fenced JSON entries
      reports zero mismatches, and that zero is recorded in the SUMMARY.
    - The SUMMARY names the post-closure open distribution by phase, and names phase 116 — omitting
      it produces a record that is wrong on its face.
    - No entry other than 19, 21 and 22 changed status in this plan.
  </acceptance_criteria>

  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs windows status | grep -m1 '"open_count"'</automated>
    <fails_when>any output other than a line whose value is `19`, or non-zero exit</fails_when>
    <automated>grep -c '"status": "open"' .planning/WINDOWS.md</automated>
    <fails_when>any output other than `19`</fails_when>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs windows status | grep -m1 '"fixed_count"'</automated>
    <fails_when>any output other than a line whose value is `12`</fails_when>
  </verify>

  <done>
    Entries 19, 21 and 22 read `fixed` with resolution timestamps the verb wrote, the counts agree
    with the entries, the rendered table and the fenced JSON agree field for field, and the nineteen
    remaining open entries are untouched.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new symbol and no new file. It produces one changed record:

| Artifact | Kind | Outcome |
| --- | --- | --- |
| `.planning/WINDOWS.md` | JSON-backed ledger | rows 9 and 30 repaired losslessly; entry 30 closed by hand behind two fail-closed checks; entries 19, 21 and 22 closed through the verb; `open_count` moves 23 → 19 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered table ↔ fenced JSON | Two representations of the same records in one file; the JSON is authoritative and regeneration overwrites the prose side, so any text that exists only in the table is destroyed unless it is copied in first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-15 | Tampering | the resolution prose in the rendered table | high | mitigate | The repair copies the table's authoritative text into the JSON BEFORE any verb regenerates the table, so what regeneration overwrites is a byte-identical copy of what was saved. Task 1's acceptance requires both descriptions to be character-identical across the two representations. |
| T-09-16 | Repudiation | a window entry closed without evidence | high | mitigate | Only entries 19, 21 and 22 close, and only because each names a production rewrite that landed and `RCOV-02`'s clause records. The plan's prohibitions bar closing anything else, and the acceptance requires the post-closure open count to be exactly 19. |
| T-09-17 | Repudiation | entry 9 silently closing during a description repair | medium | mitigate | Named as a prohibition and asserted twice: entry 9's status must still read `open` and its `resolved_at` must still be null after Task 1. |
| T-09-18 | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; `package.json` is untouched. |
</threat_model>

<verification>
- `node .claude/gsd-core/bin/gsd-tools.cjs windows status` exits 0 and reports `"open_count": 19`,
  `"waived_count": 0`, `"fixed_count": 12`, `"total_count": 31`.
- A field-by-field comparison of the rendered table against the fenced JSON reports zero mismatches
  across all 31 entries.
- `git log --stat -- .planning/WINDOWS.md` shows exactly two commits from this plan: the hand repair
  and the verb-driven closures.
</verification>

<success_criteria>
The ledger's two representations agree, its write verbs are unblocked, the three entries whose
stated closure condition has already been met are closed by the tool that owns the file, and every
other open entry is exactly as it was.
</success_criteria>

<output>
Create `.planning/phases/09-final-quality-and-backlog-closure/09-04-SUMMARY.md` when done. Record the
post-closure open distribution by phase — the closure ledger quotes it, and the earlier draft of that
list omitted phase 116.
</output>
