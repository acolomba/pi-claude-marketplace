---
phase: 04-hermetic-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/bridges/mcp/stage.test.ts
  - tests/architecture/cross-op-convergence.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
autonomous: true
requirements: [TREF-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Every terminally implicated user-scope helper controls PI_CODING_AGENT_DIR as well as HOME."
    - "Project MCP staging cannot merge a developer's user MCP configuration."
    - "Suite-specific completion-cache and interaction cleanup remains intact."
  artifacts:
    - tests/bridges/mcp/stage.test.ts
    - tests/orchestrators/marketplace/update.test.ts
    - tests/orchestrators/plugin/enable-disable.test.ts
  key_links:
    - "project MCP scope -> effective server lookup -> case-owned user location"
    - "shared environment primitive -> sibling user-scope orchestrator suites"
  prohibitions:
    - statement: "Do not migrate already-correct unrelated suites solely for uniform naming."
      source: 04-RESEARCH.md
      status: resolved
      verification: "Changed-file census matches this plan."
---

<objective>
Finish the terminal environment migration and close the effective-MCP user-state escape.

Purpose: Make all confirmed user-scope and merged-scope paths independent of host configuration.
Output: Migrated sibling suites and an MCP owner regression that fails under an ambient agent directory.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hermetic-test-infrastructure/04-01-SUMMARY.md
@.planning/phases/04-hermetic-test-infrastructure/04-CONTEXT.md
@.planning/phases/04-hermetic-test-infrastructure/04-RESEARCH.md
@tests/platform/hermetic-environment.ts
@extensions/pi-claude-marketplace/bridges/mcp/collision-slots.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate effective MCP staging from ambient user state</name>
  <read_first>tests/platform/hermetic-environment.ts, tests/bridges/mcp/stage.test.ts, extensions/pi-claude-marketplace/bridges/mcp/collision-slots.ts, extensions/pi-claude-marketplace/persistence/locations.ts</read_first>
  <files>tests/bridges/mcp/stage.test.ts</files>
  <behavior>
    - "Project staging merges only the case-owned project and case-owned user MCP locations."
    - "An ambient MCP sentinel outside the case root is neither read nor changed."
  </behavior>
  <action>Make `createProjectScope` or its caller enter the shared hermetic environment before any effective-server lookup. Add a discriminating case with different case-owned and poison-user server names, then assert the exact staged result and unchanged poison path. Preserve real temporary filesystem behavior and add no production path seam.</action>
  <verify><automated>node --test tests/bridges/mcp/stage.test.ts</automated><fails_when>The suite fails, discovers zero tests, the poison server affects collision results, or the external path changes.</fails_when></verify>
  <acceptance_criteria>The MCP owner proves merged lookup is fully case-owned.</acceptance_criteria>
  <done>BMCP-001 is closed under the active TREF-01 scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the remaining implicated user-scope helpers</name>
  <read_first>tests/platform/hermetic-environment.ts, tests/architecture/cross-op-convergence.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/plugin/update.test.ts</read_first>
  <files>tests/architecture/cross-op-convergence.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/plugin/update.test.ts</files>
  <behavior>- "Each affected suite obtains user state from the callback's agentDir and restores exact ambient values after every case."</behavior>
  <action>Replace the seven remaining HOME-only helpers selected by the terminal cross-cutting evidence with narrow wrappers around `withHermeticEnvironment`. Retain local `resetCompletionCache`, pending interaction verification, retry counts, and callback shapes where they are behaviorally necessary. Update stale comments that claim HOME alone controls Pi user state. Do not modify already-correct marketplace list/info/autoupdate suites.</action>
  <verify><automated>node --test tests/architecture/cross-op-convergence.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/enable-disable.test.ts tests/orchestrators/plugin/info.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/plugin/update.test.ts</automated><fails_when>Any suite fails, discovers zero tests, cache cleanup disappears, interaction verification is skipped, or a selected helper still controls HOME alone.</fails_when></verify>
  <acceptance_criteria>All selected sibling helpers use the shared two-variable boundary with their original suite-specific lifecycle intact.</acceptance_criteria>
  <done>The terminal hermetic environment inventory is migrated.</done>
</task>

</tasks>

<threat_model>
T-04-02 (Information disclosure/tampering, high): merged MCP and user-scope tests can consume or overwrite developer configuration. Mitigate with an explicit case-owned agent directory and poison-location regressions. Completion is blocked until every focused suite passes and no selected HOME-only helper remains.
</threat_model>

<verification>
Run the MCP suite and all migrated owners. Census the selected helpers for direct HOME-only mutation and inspect cleanup restoration.
</verification>

<success_criteria>
Confirmed user and merged MCP paths operate only on case-owned state and preserve each suite's existing lifecycle guarantees.
</success_criteria>

<output>
Create `.planning/phases/04-hermetic-test-infrastructure/04-02-SUMMARY.md` when done.
</output>
