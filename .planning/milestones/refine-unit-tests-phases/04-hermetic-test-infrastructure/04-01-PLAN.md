---
phase: 04-hermetic-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/platform/hermetic-environment.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/list.test.ts
autonomous: true
requirements: [TREF-01]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Every migrated callback sees HOME and PI_CODING_AGENT_DIR beneath one case-owned temporary root."
    - "The exact prior presence and value of both environment variables is restored after success or failure."
    - "User-scope install and list operations cannot read or write a pre-existing ambient agent directory."
  artifacts:
    - tests/platform/hermetic-environment.ts
    - tests/orchestrators/plugin/install.test.ts
    - tests/orchestrators/plugin/list.test.ts
  key_links:
    - "case-owned environment -> locationsFor(user) -> temporary agent directory"
  prohibitions:
    - statement: "No production test mode, location override, reset hook, or generic tests/helpers module may be added."
      source: 04-CONTEXT.md D-07
      status: resolved
      verification: "Inspect changed paths and run the focused suites."
---

<objective>
Create the concern-owned hermetic environment primitive and use it in the two directly terminal user-scope suites.

Purpose: Close the precedence hole where changing only HOME still permits real Pi agent state through PI_CODING_AGENT_DIR.
Output: One typed test-platform helper plus discriminating install and list owner proofs.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hermetic-test-infrastructure/04-CONTEXT.md
@.planning/phases/04-hermetic-test-infrastructure/04-RESEARCH.md
@.planning/phases/04-hermetic-test-infrastructure/04-PATTERNS.md
@tests/orchestrators/scope-fanout.test.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a case-owned Pi environment boundary</name>
  <read_first>tests/orchestrators/scope-fanout.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/list.test.ts, extensions/pi-claude-marketplace/persistence/locations.ts</read_first>
  <files>tests/platform/hermetic-environment.ts, tests/orchestrators/plugin/install.test.ts</files>
  <behavior>
    - "The callback receives readonly root, home, agentDir, and cwd paths that all belong to one fresh temporary root."
    - "HOME and PI_CODING_AGENT_DIR are restored exactly when the callback resolves or rejects."
    - "A user-scope install leaves an independently created ambient agent directory unchanged."
  </behavior>
  <action>Create `tests/platform/hermetic-environment.ts` beside other boundary fakes. It must create one temporary root, create separate home/agent/cwd children, snapshot both variables including absence, assign both variables before invoking the callback, restore them in `finally`, and remove only the exact root. Replace install's HOME-only wrapper while preserving its cache/setup behavior. Add an install owner case that pre-poisons PI_CODING_AGENT_DIR, exercises a real user-scope path, asserts the case-owned state, and compares the poison directory before and after.</action>
  <verify><automated>node --test tests/orchestrators/plugin/install.test.ts</automated><fails_when>The suite fails, discovers zero tests, the poison path changes, either environment variable is restored incorrectly, or a production seam is added.</fails_when></verify>
  <acceptance_criteria>The helper controls the higher-precedence agent directory and install proves user state remains inside the case root.</acceptance_criteria>
  <done>Install no longer depends on developer HOME or Pi agent state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate plugin list without weakening filesystem assertions</name>
  <read_first>tests/platform/hermetic-environment.ts, tests/orchestrators/plugin/list.test.ts</read_first>
  <files>tests/orchestrators/plugin/list.test.ts</files>
  <behavior>- "Plugin list reads user state from the assigned temporary agent directory and preserves every existing exact tree and notification assertion."</behavior>
  <action>Replace list's HOME-only wrapper with the shared environment boundary. Adapt callbacks to use the returned paths and keep the retrying cleanup semantics in the shared helper or a narrow local wrapper as appropriate. Add or retain a poison-directory assertion that would fail if PI_CODING_AGENT_DIR escaped the boundary. Do not normalize expected state or notification bytes.</action>
  <verify><automated>node --test tests/orchestrators/plugin/list.test.ts tests/orchestrators/plugin/install.test.ts</automated><fails_when>Either suite fails, discovers zero tests, list reads ambient state, or any existing whole-value assertion is weakened.</fails_when></verify>
  <acceptance_criteria>Both direct terminal suites use the same typed environment primitive and remain behaviorally unchanged.</acceptance_criteria>
  <done>The first hermetic migration is complete.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Risk                                                           | Mitigation                                                                                           |
| -------------------------------------------- | -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| process environment -> user-scope filesystem | Ambient configuration can redirect tests into developer state. | Assign both location inputs to one case root and prove an external poison directory stays unchanged. |

T-04-01 (Tampering, high) is blocking until the focused install/list command proves case-owned writes and exact restoration. Authentication and cryptographic controls are not changed by this plan.
</threat_model>

<verification>
Run both owner suites and inspect the changed files for writes outside the callback-provided root.
</verification>

<success_criteria>
HOME and PI_CODING_AGENT_DIR are controlled together, exact prior state is restored, and install/list user cases cannot touch ambient agent state.
</success_criteria>

<output>
Create `.planning/phases/04-hermetic-test-infrastructure/04-01-SUMMARY.md` when done.
</output>
