---
phase: 04-hermetic-test-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - tests/platform/git-ops-fake.ts
  - tests/platform/git-ops-fake.test.ts
  - tests/orchestrators/marketplace/add.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/plugin/clone-cache.test.ts
  - tests/orchestrators/plugin/fetch.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/update.test.ts
autonomous: true
requirements: [AUTH-01, TREF-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Clone, fetch, and remote-ref calls accept and record real function-bearing auth bundles without DataCloneError."
    - "Recorded callbacks keep their exact identity and remain invocable."
    - "Consumers no longer strip auth or mutate the shared call ledger to work around the fake."
  artifacts:
    - tests/platform/git-ops-fake.ts
    - tests/platform/git-ops-fake.test.ts
  key_links:
    - "production Git option -> typed fake snapshot -> observable auth callback"
  prohibitions:
    - statement: "Do not serialize, erase, recreate, or stringify auth callbacks."
      source: 04-CONTEXT.md D-09 through D-12
      status: resolved
      verification: "Direct fake tests compare callback identity and invoke it."
---

<objective>
Make the shared Git fake faithful to function-bearing production collaborators and delete every local auth-stripping workaround.

Purpose: Allow realistic authentication failure paths to use the same collaborator shape as production.
Output: Typed snapshots, direct fake regressions, and simpler downstream adapters.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hermetic-test-infrastructure/04-02-SUMMARY.md
@.planning/phases/04-hermetic-test-infrastructure/04-CONTEXT.md
@.planning/phases/04-hermetic-test-infrastructure/04-PATTERNS.md
@extensions/pi-claude-marketplace/platform/git.ts
@tests/platform/credential-ops-fake.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record function-bearing Git options through explicit typed snapshots</name>
  <read_first>tests/platform/git-ops-fake.ts, tests/platform/git-ops-fake.test.ts, extensions/pi-claude-marketplace/platform/git.ts, tests/platform/credential-ops-fake.ts</read_first>
  <files>tests/platform/git-ops-fake.ts, tests/platform/git-ops-fake.test.ts</files>
  <behavior>
    - "Clone, fetch, and resolveRemoteRef preserve the original readonly auth bundle and callback functions."
    - "Mutation of data-only input fields after a call cannot rewrite recorded history."
    - "Configured fake failures and remote allow-lists behave exactly as before."
  </behavior>
  <action>Replace generic `structuredClone` at the three auth-bearing ingress points with explicit functions typed to each production option. Clone data-only fields and retain the auth bundle only when present. Add independent direct cases for clone, fetch, and resolveRemoteRef that compare complete recorded options, callback identity, invocation result/error identity, and normal public fake state. Do not use a broad generic cast.</action>
  <verify><automated>node --test tests/platform/git-ops-fake.test.ts &amp;&amp; npm run test:coverage:direct:all</automated><fails_when>The command exits non-zero, discovers zero tests, DataCloneError survives, callback identity changes, data aliases call history, or the shared-support coverage gate fails.</fails_when></verify>
  <acceptance_criteria>Every auth-bearing Git operation records a production-faithful, mutation-safe call.</acceptance_criteria>
  <done>The shared fake owns the function-bearing contract once.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove downstream strip-and-reattach adapters</name>
  <read_first>tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/clone-cache.test.ts, tests/orchestrators/plugin/fetch.test.ts, tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/update.test.ts</read_first>
  <files>tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/clone-cache.test.ts, tests/orchestrators/plugin/fetch.test.ts, tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/update.test.ts</files>
  <behavior>- "Consumer-specific fixture, ref, filesystem, allow-list, and configured-error behavior is unchanged while auth passes directly to the shared fake."</behavior>
  <action>Delete only destructuring that removes `auth` and `Object.assign` calls that repair the ledger afterward. Forward the original options into the corrected fake. Preserve wrapper behavior for fixture directory creation, synthetic refs, remote maps, and injected errors. Strengthen any existing auth observation to compare the recorded callable bundle rather than a parallel variable.</action>
  <verify><automated>node --test tests/platform/git-ops-fake.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/clone-cache.test.ts tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/info.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update.test.ts</automated><fails_when>Any suite fails, discovers zero tests, an auth-stripping workaround remains, or consumer-specific fake behavior changes.</fails_when></verify>
  <acceptance_criteria>All eight consumers use the shared auth-bearing behavior directly.</acceptance_criteria>
  <done>PLT-F020 is corrected at the shared boundary and every traced workaround is gone.</done>
</task>

</tasks>

<threat_model>
T-04-03 (Spoofing/repudiation, high): erasing auth callbacks makes tests observe a collaborator different from production and can hide credential failures. Mitigate with exact typed snapshots, callback identity/invocation assertions, and all dependent owner suites.
</threat_model>

<verification>
Run the shared fake suite, all eight dependents, the all-pairs direct coverage gate, and a source census for auth-stripping patterns.
</verification>

<success_criteria>
Function-bearing auth is accepted, recorded, and callable through the shared fake with no downstream repair layer.
</success_criteria>

<output>
Create `.planning/phases/04-hermetic-test-infrastructure/04-03-SUMMARY.md` when done.
</output>
