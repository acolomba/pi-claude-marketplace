---
phase: 03-production-defect-corrections
plan: 12
type: execute
wave: 2
depends_on: [03-04, 03-05]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  - extensions/pi-claude-marketplace/domain/components/hook-events.ts
  - tests/bridges/hooks/async-rewake/registry.test.ts
  - tests/domain/components/hook-events.test.ts
autonomous: true
requirements: [PDEF-07]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The async-rewake path has no no-producer non-dispatchable branch after the synchronous consumer is corrected (D-24, D-25)."
    - "The domain no longer exports a runtime guard for the empty complement of the admitted event union (D-24)."
    - "Registry capacity, timeout, process-lifecycle, cleanup, and path-safety guards remain unchanged and covered (D-24, D-25)."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts
    - tests/bridges/hooks/async-rewake/registry.test.ts
    - tests/domain/components/hook-events.test.ts
  key_links:
    - "Admitted BucketAEvent union -> async-rewake translator table without an unreachable fallback."
    - "Both dispatch consumers stop importing isDispatchableEvent before the domain export is deleted."
  assumptions:
    - statement: "PDEF-07 branch removal is limited to the no-producer complement proven by DC-029/MFDEC01; every runtime trust-boundary guard remains in place."
      status: flagged-unverified
  prohibitions: []
---

<objective>
Remove the async-rewake no-producer fallback and its obsolete domain guard after both live consumers use the admitted event union, per D-24 and D-25.

Purpose: Complete the trace-preserving branch correction without weakening runtime safety checks.
Output: Honest async dispatch control flow and a domain surface without an empty-complement runtime guard.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-defect-corrections/03-04-SUMMARY.md
@.planning/phases/03-production-defect-corrections/03-05-SUMMARY.md
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
@extensions/pi-claude-marketplace/domain/components/hook-events.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the async-rewake no-producer fallback</name>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, extensions/pi-claude-marketplace/domain/components/hook-events.ts, tests/bridges/hooks/async-rewake/registry.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, tests/bridges/hooks/async-rewake/registry.test.ts</files>
  <behavior>
    - "Every admitted event reaches the async-rewake translator table (D-24)."
    - "Tests no longer fabricate a non-dispatchable admitted event through a cast (D-25)."
    - "Registry capacity, timeout, process lifecycle, cleanup, and path-safety behavior remains exact."
  </behavior>
  <action>Use the terminal producer trace to remove only the `isDispatchableEvent` false branch from the async-rewake registry. Index the translator table with the honest admitted event type and delete the cast-created unreachable fixture. Preserve capacity, pid/process validation, timeout, safe-path, and cleanup branches because untrusted producers can reach them. Do not add casts, ignore directives, test-only exports, mutable discriminators, or prototype mutation per D-24 and D-25.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/registry.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, an admitted event fails translation, a real registry or path guard changes, a cast-only fixture remains, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The async owner contains no no-producer false branch, every admitted event dispatches, and each live safety guard remains regression-covered.</acceptance_criteria>
  <done>Async-rewake control flow matches the current event producer union without weakening runtime guards.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the obsolete empty-complement domain guard</name>
  <read_first>extensions/pi-claude-marketplace/domain/components/hook-events.ts, tests/domain/components/hook-events.test.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts</read_first>
  <files>extensions/pi-claude-marketplace/domain/components/hook-events.ts, tests/domain/components/hook-events.test.ts</files>
  <behavior>
    - "The domain exports no runtime membership guard for an empty event complement (D-24)."
    - "The admitted event set and dispatchable translator keys remain exactly equal."
    - "Compact supportability retains the exact manual and auto values established by D-08 through D-10."
  </behavior>
  <action>After both consumers stop using the guard, delete `DISPATCHABLE_MEMBERS` and `isDispatchableEvent`. Update the domain comments and direct tests to assert exact admitted-to-dispatchable equality through public values. Delete only the artificial complement case. Preserve the compact-trigger supportability contract from Plan 03-04 and do not pursue the deferred Phase 7 unused-member work per D-24 and D-25.</action>
  <verify>
    <automated>node --test tests/domain/components/hook-events.test.ts tests/bridges/hooks/dispatch-exec.test.ts tests/bridges/hooks/async-rewake/registry.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hook-events.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, the obsolete guard remains, event-set equality fails, compact supportability changes, or direct coverage fails.</fails_when>
  </verify>
  <acceptance_criteria>The domain guard and its artificial case are absent, both consumers compile against the exact event union, and the compact contract remains intact.</acceptance_criteria>
  <done>The terminally authorized branch removal is complete across async dispatch and the domain owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| admitted hook event -> async process dispatch | Validated event data selects a translator before entering process and registry state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-41 | Tampering | async dispatch branch removal | high | mitigate | Remove only the producer-proven empty complement and preserve every reachable registry, process, cleanup, and path guard. |
| T-03-42 | Denial of Service | registry capacity and timeout paths | medium | mitigate | Keep capacity and timeout behavior unchanged and run the direct registry suite. |
| T-03-43 | Spoofing | compact supportability values | low | accept | Plan 03-04 owns translation; this plan reruns the domain suite and changes no compact value. |

## ASVS L1 Applicability and Completion Gate

| ASVS-L1 area | Applicability | Control and evidence |
|---|---|---|
| V5 Input Validation | Applies | The closed admitted-event union selects translators while reachable runtime guards remain. Tasks 1 and 2 jointly verify T-03-41. |
| V2/V3/V4/V6 | Not applicable | This plan changes no authentication, session, authorization, or cryptographic boundary. |

Plan completion is blocked until both task commands exit zero for T-03-41. Any removed reachable guard or event-set mismatch is a blocking failure.
</threat_model>

<artifacts_this_phase_produces>
- No new public files; the async no-producer branch and obsolete `isDispatchableEvent` domain surface are removed.
</artifacts_this_phase_produces>

<verification>
Run the async registry, domain event, and synchronous dispatch suites with direct coverage for both changed production owners. Treat any T-03-41 failure as blocking.
</verification>

<success_criteria>
Async dispatch has no unreachable complement branch, the obsolete domain guard is gone, and all live registry and trust-boundary guards remain tested.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-12-SUMMARY.md` when done.
</output>
