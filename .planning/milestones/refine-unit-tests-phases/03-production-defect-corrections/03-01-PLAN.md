---
phase: 03-production-defect-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/integration/reconcile-plan-convergence.test.ts
autonomous: true
requirements: [PDEF-08]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A declared marketplace alias maps to exactly one recorded canonical marketplace identity, and reconciliation reaches steady state after one apply (D-26, D-27)."
    - "Missing or multiply claimed source aliases fail closed without mutating the manifest-backed canonical state (D-27)."
    - "Alias regression fixtures use distinct declared and recorded names and prove a second apply is a no-op without network access (D-27)."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
    - tests/orchestrators/reconcile/plan.test.ts
    - tests/orchestrators/reconcile/apply.test.ts
    - tests/integration/reconcile-plan-convergence.test.ts
  key_links:
    - "diffMarketplaces resolves declared source claims into a one-to-one declared-name -> canonical recorded-name map consumed by plugin planning."
    - "planReconcile output is applied by applyReconcilePlan and compared against the persisted manifest on the second pass."
  assumptions:
    - statement: "PDEF-08 has no SPEC Edge Coverage classification; alias-cardinality, zero-match, multi-match, and repeated-apply cases are treated as the manual edge set derived from D-26/D-27 and terminal findings ORA-F03/ORA-F33."
      status: flagged-unverified
  prohibitions:
    - statement: "Reconciliation must not silently choose the first recorded marketplace when one declared source has multiple canonical claim candidates."
      source: flagged-unverified
      status: unresolved
      verification: null
---

<objective>
Correct marketplace alias reconciliation so a declared source claim has one canonical recorded identity and converges without remove/re-add churn, per D-26 and D-27.

Purpose: Close terminal findings ORA-F03 and ORA-F33 with regression evidence at the pure planner and applied-state boundaries.
Output: A one-to-one source-claim map plus hermetic plan/apply convergence regressions.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-live-evidence-revalidation/01-REVALIDATION.json
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@.planning/phases/03-production-defect-corrections/03-RESEARCH.md
@.planning/phases/03-production-defect-corrections/03-PATTERNS.md
@extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
@extensions/pi-claude-marketplace/persistence/state-io.ts
</context>

<interfaces>
- `planReconcile(merged, state, scope): ReconcilePlan` is pure and deterministic.
- `samePlannedSource(recorded, declared)` supplies the source-equivalence decision; human-readable source text is not identity.
- Promote a resolved one-to-one declared-name -> canonical recorded-name claim map as the primary representation. The old direct declared-key lookup and unordered first matching record are demoted implementation details, not parallel identities (D-26).
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one distinct alias from declaration through apply and second-pass convergence</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/reconcile/plan.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/integration/reconcile-plan-convergence.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts, tests/orchestrators/reconcile/plan.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/integration/reconcile-plan-convergence.test.ts</files>
  <behavior>
    - "A declaration `declared-name` whose source equals recorded `canonical-name` plans no duplicate marketplace add/remove and resolves plugin references through `canonical-name` (D-26)."
    - "Applying that plan preserves the manifest's canonical key and a second plan against the resulting state is empty (D-27)."
    - "The convergence fixture uses distinct names and a hermetic local source; no network path is exercised (D-27)."
  </behavior>
  <action>First add the distinct-name planner, apply, and integration cases so they fail against the current `findRecordedBySource`/`declaredAndRecorded` behavior. Replace the set-plus-first-match flow with a deterministic one-to-one claim map created before marketplace and plugin bucket construction. Thread only the canonical recorded key downstream; never treat the declared alias as a second recorded identity. Preserve the existing manifest key, scope, source normalization, bucket ordering, and pure/no-I/O planner contract per D-26 and D-27.</action>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/plan.test.ts tests/orchestrators/reconcile/apply.test.ts tests/integration/reconcile-plan-convergence.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, the distinct alias produces duplicate add/remove work, canonical state changes key, or the second plan is non-empty.</fails_when>
  </verify>
  <acceptance_criteria>The direct planner regression fails when the one-to-one claim map is removed, the applied fixture preserves `canonical-name`, and the second pass produces all empty buckets.</acceptance_criteria>
  <done>One distinct declared alias converges through real plan/apply state without network access or canonical identity churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed for absent and ambiguous source claims</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts, tests/orchestrators/reconcile/plan.test.ts, tests/orchestrators/reconcile/apply.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts, tests/orchestrators/reconcile/plan.test.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "A source with no recorded canonical candidate follows the ordinary declared-add path without inventing an alias claim."
    - "A source with two otherwise eligible recorded candidates does not choose by object-entry order and produces an honest non-mutating conflict outcome (D-27)."
    - "Two declarations cannot claim the same canonical record, and stable input order does not alter the result (D-26)."
  </behavior>
  <action>Add zero-candidate, two-candidate, and duplicate-claim cases before completing the resolver. Make candidate cardinality explicit and deterministic, and route ambiguity through an existing honest mismatch/dangling result or a typed planner error if the current plan union cannot express it. Do not mutate the input merged config or extension state. Preserve exact plan ordering and all unrelated reconcile buckets. Vary source text formatting only through the existing `samePlannedSource` semantics; do not add string-message classification.</action>
  <verify>
    <automated>node --test tests/orchestrators/reconcile/plan.test.ts tests/orchestrators/reconcile/apply.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts</automated>
    <fails_when>The command exits non-zero, discovers zero tests, candidate order selects a different canonical record, ambiguity mutates state, or direct coverage is below the repository gate.</fails_when>
  </verify>
  <acceptance_criteria>Zero and multiple source candidates have distinct deterministic outcomes, two declarations cannot share one canonical record, and messages are not used as identity.</acceptance_criteria>
  <done>Alias resolution is one-to-one, deterministic, fail-closed, and manifest-preserving for all terminally authorized cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| config declaration -> persisted reconcile state | User-authored marketplace aliases cross into canonical manifest identity and action planning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering | `diffMarketplaces` source claims | high | mitigate | Count normalized candidates, enforce one-to-one claims, and fail closed on ambiguity before producing mutations. |
| T-03-02 | Spoofing | declared alias -> canonical recorded name | high | mitigate | Record and consume the canonical key from the resolved claim map; regression-prove the declared key never becomes parallel state. |
| T-03-03 | Tampering | prototype-key lookup | low | accept | This plan does not perform dynamic object-key dispatch; Plan 03-05 owns the canonical own-key mitigation and this plan introduces no new indexed trust boundary. |
| T-03-04 | Information Disclosure | cleanup diagnostics | low | accept | No cleanup subprocess is used by reconciliation planning; Plan 03-07 owns structured cleanup diagnostics. |

## ASVS L1 Applicability and Completion Gate

| ASVS-L1 area | Applicability | Control and evidence |
|---|---|---|
| V5 Input Validation | Applies | Source claims require exactly one canonical candidate and one claimant before mutation. Both task commands verify T-03-01 and T-03-02. |
| V2/V3/V4/V6 | Not applicable | This plan changes no authentication, session, authorization, or cryptographic boundary. |

Plan completion is blocked until both task commands exit zero for T-03-01 and T-03-02. Any ambiguous mutation or canonical identity change is a blocking failure.
</threat_model>

<artifacts_this_phase_produces>
- `diffMarketplaces` gains a deterministic one-to-one source-claim representation; no new public files or external APIs are created.
</artifacts_this_phase_produces>

<verification>
Run the three focused reconcile suites and direct coverage for `plan.ts`. Treat any T-03-01 or T-03-02 failure as blocking.
</verification>

<success_criteria>
Terminal alias findings ORA-F03 and ORA-F33 are closed by a regression that fails on the previous first-match behavior and proves second-pass convergence.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-01-SUMMARY.md` when done.
</output>
