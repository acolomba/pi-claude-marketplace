---
phase: 03-production-defect-corrections
plan: 14
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
  - tests/orchestrators/import/execute.test.ts
  - tests/orchestrators/reconcile/pending.test.ts
autonomous: true
requirements: [PDEF-01, PDEF-08]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Import declares plural before producing zero, one, or many rows (D-28)."
    - "Reconcile pending declares plural without mutating the one-to-one canonical alias map or persisted state (D-26, D-27, D-28)."
    - "Notification metadata never triggers network work or changes plan contents, ordering, or canonical identity (D-27, D-28)."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts
    - tests/orchestrators/import/execute.test.ts
    - tests/orchestrators/reconcile/pending.test.ts
  key_links:
    - "Plural import/reconcile invocation -> explicit cardinality -> notifyWithContext."
    - "Reconcile plan canonical identity -> pending notification projection with no state mutation."
  assumptions:
    - statement: "PDEF-08 apply-twice alias convergence is established by Plan 03-01; this plan changes only notification metadata and reruns that integration proof."
      status: flagged-unverified
  prohibitions:
    - statement: "Notification metadata must not alter reconcile plan contents, canonical alias identity, or persisted state."
      source: flagged-unverified
      status: unresolved
      verification: null
---

<objective>
Declare plural cardinality for import and reconcile pending while preserving the canonical alias state established by D-26 and D-27.

Purpose: Isolate these independent producer families from the plugin-surface cardinality plan.
Output: Exact import and reconcile owner regressions plus unchanged alias convergence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-defect-corrections/03-01-SUMMARY.md
@.planning/phases/03-production-defect-corrections/03-CONTEXT.md
@extensions/pi-claude-marketplace/shared/notify-context.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Declare plural import cardinality</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/orchestrators/import/execute.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/orchestrators/import/execute.test.ts</files>
  <behavior>- "Import declares plural before producing zero, one, or many rows, and row count never changes tally semantics (D-28)."</behavior>
  <action>Pass explicit `plural` cardinality at every import notification call before results exist. Add exact zero, one, and many output cases. Preserve import ordering, persistence, error severity, and no-network fixtures per D-28.</action>
  <verify><automated>node --test tests/orchestrators/import/execute.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/import/execute.ts</automated><fails_when>The command exits non-zero, discovers zero tests, the plural tally follows row count, import state changes unexpectedly, network is used, or direct coverage fails.</fails_when></verify>
  <acceptance_criteria>Import always declares plural, exact output covers zero, one, and many rows, and persistence behavior remains unchanged.</acceptance_criteria>
  <done>Import carries caller-owned structural cardinality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declare plural reconcile cardinality without changing alias state</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts, tests/orchestrators/reconcile/pending.test.ts, tests/integration/reconcile-plan-convergence.test.ts, .planning/phases/03-production-defect-corrections/03-01-SUMMARY.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts, tests/orchestrators/reconcile/pending.test.ts</files>
  <behavior>
    - "Reconcile pending declares plural before producing zero, one, or many rows (D-28)."
    - "Notification projection leaves the plan, canonical alias identity, and persisted state unchanged (D-26, D-27)."
    - "The distinct alias fixture still converges on a second apply without network work (D-27)."
  </behavior>
  <action>Pass explicit `plural` cardinality at every reconcile pending notification call. Add exact zero, one, and many output cases and compare plan and state objects before and after notification. Rerun the distinct declared-to-canonical alias convergence case. Preserve no-network fixtures and the one-to-one source-claim map per D-26 through D-28.</action>
  <verify><automated>node --test tests/orchestrators/reconcile/pending.test.ts tests/integration/reconcile-plan-convergence.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts</automated><fails_when>The command exits non-zero, discovers zero tests, the plural tally follows row count, reconcile plan or state changes, alias convergence fails, network is used, or direct coverage fails.</fails_when></verify>
  <acceptance_criteria>Reconcile pending always declares plural, notification is non-mutating, and the canonical alias mapping still reaches a no-network fixed point.</acceptance_criteria>
  <done>Reconcile notification metadata is explicit without changing alias identity or state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| import/reconcile input and canonical state -> notification metadata | User and config structure selects aggregate output without permission to alter planned or persisted identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-46 | Repudiation | import and reconcile tallies | medium | mitigate | Pass caller-owned plural cardinality and test exact zero, one, and many output. |
| T-03-47 | Spoofing | reconcile aliases | high | mitigate | Keep notification projection out of plan/state and rerun distinct-name convergence with no network work. |
| T-03-48 | Tampering | persisted import/reconcile state | medium | mitigate | Compare complete plan and state values before and after notification in owner regressions. |

## ASVS L1 Applicability and Completion Gate

| ASVS-L1 area | Applicability | Control and evidence |
|---|---|---|
| V5 Input Validation | Applies | Structural invocation data selects plural output, and reconcile accepts only the one-to-one canonical claim established by Plan 03-01. Task 2 verifies T-03-47. |
| V2/V3/V4/V6 | Not applicable | This plan changes no authentication, session, authorization, or cryptographic boundary. |

Plan completion is blocked until Task 2's automated command exits zero for T-03-47 and proves unchanged canonical alias state plus fixed-point convergence.
</threat_model>

<artifacts_this_phase_produces>
- No new symbols; import and reconcile pending producers gain explicit plural cardinality.
</artifacts_this_phase_produces>

<verification>
Run both direct owner suites, reconcile convergence, and direct coverage for both changed sources. Treat a T-03-47 failure as blocking.
</verification>

<success_criteria>
Import and reconcile pending always declare plural, exact output is row-count-independent, and alias plan/state identity remains unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-production-defect-corrections/03-14-SUMMARY.md` when done.
</output>
