---
phase: 07-gate-integrity
plan: 11
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/architecture/markers-snapshot.test.ts
autonomous: true
requirements: [GGAT-01, GGAT-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`orchestrators/plugin/fetch.ts` reaches `resolveStrict` through a static import, or keeps the dynamic form with a recorded reason a static import was rejected — measured from a real `npm run check` run, not assumed."
    - "The byte-parity behaviour `reasonedRow` produces is unchanged either way: the same rows, the same reasons, the same narrowing."
    - "Each marker byte pin has exactly one owner: the module's owner test keeps the pins the rule requires it to carry, and the architecture snapshot keeps only what the owner test cannot express."
    - "The architecture snapshot's header cites no file that does not exist, and the gate it points readers at is one that is actually present and running."
    - "No comment in `orchestrators/plugin/info.ts` describes a re-export that is not there."
    - "The three findings closed here each carry recorded current evidence rather than an assertion that they are closed."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
    - tests/architecture/markers-snapshot.test.ts
  key_links:
    - "The dynamic-`import()` pattern the network gate gained in `07-01` is about the gate's blind spot, not about this one call site; converting `fetch.ts` closes the finding and the gate keeps the pattern regardless."
    - "`tests/shared/markers.test.ts` is the markers module's owner test, which the project's unit-testing rule requires, so it is the pin's owner; `markers-snapshot.test.ts` keeps the `locationsFor` assertion and the agents-bridge markers, which come from other modules."
  prohibitions:
    - "No comment survives the code it described."
    - "No byte pin is deleted from both its owners; every pin keeps exactly one home."
    - "No dynamic import is kept without a recorded reason drawn from an observed `npm run check` result."
    - "No identifier or comment introduced here names a phase, plan, wave, or task number."
---

<objective>
Close three small terminal findings that each leave a reader trusting something untrue: a dynamic
import documented by what it resolves rather than why it is dynamic, a byte pin asserted in two
places with a dangling citation to a gate that does not exist, and a comment describing a re-export
that was removed.

Purpose: these are criterion-4 instances inside the gate corpus and the production tree — a stale
reference that reports success. The markers snapshot points readers at
`tests/architecture/no-legacy-markers.test.ts`, which is not in the repository, and
`orchestrators/plugin/info.ts` ends with three lines describing surface that is gone.

Output: `fetch.ts`'s import form settled by an observed run, one owner per byte pin, a header that
cites only files that exist, and no orphaned narration.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gate-integrity/07-CONTEXT.md
@.planning/phases/07-gate-integrity/07-RESEARCH.md
@.planning/phases/07-gate-integrity/07-PATTERNS.md
@.claude/rules/typescript-comments.md
@.claude/rules/typescript-unit-testing.md
@extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
@tests/architecture/markers-snapshot.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settle the fetch dynamic import by running the gate, and delete the orphaned comment</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts` lines 25-60 (the existing static `domain/manifest.ts` and `domain/source.ts` imports, which prove the zone edge is legal and already used) and lines 468-495 (`reasonedRow`, its doc block, and the `await import("../../domain/plugin-resolver.ts")` at `:483`)
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` last 6 lines (the orphaned comment describing a removed test-only re-export)
    - `.planning/phases/07-gate-integrity/07-CONTEXT.md` §"Rulings Taken After Research" (`OPEFR-F007`: try static first, keep dynamic only if `npm run check` goes red, and record why)
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §9 "`OPEFR-F007`'s disposition" and §1 (the correction that the specifier is `domain/plugin-resolver.ts`, not `domain/resolver.ts`)
    - `.planning/codebase/ARCHITECTURE.md` §"Architectural Constraints" (the `orchestrators` → `domain` zone edge and the `no-orchestrator-network` `FORBIDDEN_TARGETS` membership of `fetch.ts`)
    - `.claude/rules/typescript-comments.md` §"Narration of code that no longer exists"
  </read_first>
  <action>
Convert `fetch.ts:483`'s `await import("../../domain/plugin-resolver.ts")` to a static import of
`resolveStrict` at the top of the file, in the existing import group beside the other `domain/`
imports. Then run `npm run check` in full. The zone edge is legal and already exercised statically
by two other `domain/` imports in the same file, so the plausible failure is a load-order or cycle
effect the dynamic form was hiding — and the only way to know is to run it.

If `npm run check` stays green, keep the static import and record the result. The finding closes
outright, which is better than documenting a form that has no reason.

If `npm run check` goes red, revert to the dynamic form and add one sentence of rationale to
`reasonedRow`'s doc block naming the concrete failure observed — the cycle, the load-order effect,
or the failing gate — not a general statement about deferring cost. Record the verbatim failure in
the SUMMARY. A dynamic import with a recorded reason is an acceptable outcome; a dynamic import
with a restated description of what it resolves is the finding.

Either way, the doc block at `:468-472` needs its gap closed: it explains what `reasonedRow`
re-resolves and why byte-parity with `list` matters, and says nothing about the import form. Add
the form's rationale or remove the ambiguity by making it static.

Separately, delete the three-line comment at the end of `orchestrators/plugin/info.ts` describing a
test-only re-export of the shared classifier. The re-export does not exist, so the comment narrates
code that is gone, which the comment rule forbids. Do not replace it with a note saying the
re-export was removed — that is the same defect in the past tense. Delete it and leave the file
ending at its last statement.
  </action>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit from any link in the chain; if this is the static-import attempt, the failure text is the evidence and the plan's fallback branch applies</fails_when>
    <automated>node --test tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/info.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts</automated>
    <fails_when>non-zero exit, or the output reports a coverage shortfall for that module</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the `npm run check` result for the static-import attempt verbatim, and states which branch was taken.
    - If static: `grep -c 'await import("../../domain/plugin-resolver.ts")' extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts` returns 0 and `grep -c 'from "../../domain/plugin-resolver.ts"' extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts` returns at least 1.
    - If dynamic is retained: `reasonedRow`'s doc block names the concrete observed failure, and the SUMMARY carries that failure's output.
    - `tail -1 extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` is a statement, not a comment; `grep -c "Test-only re-export" extensions/pi-claude-marketplace/orchestrators/plugin/info.ts` returns 0.
    - `npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts` exits 0, proving the import-form change did not move a branch out of reach.
  </acceptance_criteria>
  <done>The fetch import form is settled by an observed gate run rather than by argument, and no comment in either orchestrator describes code that is not there.</done>
</task>

<task type="auto">
  <name>Task 2: Give each marker byte pin exactly one owner and repair the dangling citation</name>
  <files>tests/architecture/markers-snapshot.test.ts</files>
  <read_first>
    - `tests/architecture/markers-snapshot.test.ts` in full (the header at `:13-21` with its citation to `tests/architecture/no-legacy-markers.test.ts`, the agents-bridge pins at `:34-44`, the two duplicated pins at `:55-70`, and the `locationsFor` case at `:72-76`)
    - `tests/shared/markers.test.ts` lines 1-45 (the owner test's byte assertions on the same two constants)
    - `.claude/rules/typescript-unit-testing.md` (the one-owner-test-per-production-module rule that makes `tests/shared/markers.test.ts` mandatory)
    - `.planning/phases/07-gate-integrity/07-CONTEXT.md` §"Rulings Taken After Research" (`SHC-F047`: the owner test keeps the pins; the snapshot keeps only what the owner test cannot express)
  </read_first>
  <action>
Confirm first that `tests/shared/markers.test.ts` asserts both `RECOVERY_PLUGIN_REINSTALL_PREFIX`
and `STATE_LOCK_HELD_PREFIX` byte-for-byte. It is the module's owner test, which the project's
unit-testing rule requires to exist, so it is the pin's home; a one-export module's owner test has
no other way in.

Remove the two duplicated byte pins from `markers-snapshot.test.ts` and the `import * as markers`
that served only them. Keep the agents-bridge pins at `:34-44` — they assert constants from
`bridges/agents/marker.ts`, a different module — and keep the `locationsFor` case at `:72-76`,
which asserts a relationship between two modules that no single owner test can express.

Repair the header. It cites `tests/architecture/no-legacy-markers.test.ts` as the gate that blocks
re-introduction of the five superseded literals, and that file is not in the repository. Establish
what actually blocks them today — search for the literals and for whatever gate names them — and
either cite the real gate or state plainly that the byte pins in the owner test are the whole
control. Do not leave a citation that cannot be followed: a gate whose header points at a file that
does not exist is the same class of defect as a gate whose target list stopped resolving, and this
one sits inside the gate corpus itself.

Record in the SUMMARY where the five superseded ES-5 literals are actually blocked today, with the
search you ran. If nothing blocks them, say so — that is a finding for the backlog, not something
to paper over with a rewritten sentence.
  </action>
  <verify>
    <automated>node --test tests/architecture/markers-snapshot.test.ts tests/shared/markers.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/markers.ts</automated>
    <fails_when>non-zero exit, or the output reports a coverage shortfall for that module</fails_when>
    <automated>node --test "tests/architecture/*.test.ts" && npx eslint tests --max-warnings=0</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0", or eslint prints "error"/"warning"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "no-legacy-markers" tests/architecture/markers-snapshot.test.ts` returns 0.
    - Every file path cited in `markers-snapshot.test.ts`'s header resolves on disk; verify each with `ls` and record the check in the SUMMARY.
    - `grep -c "RECOVERY_PLUGIN_REINSTALL_PREFIX" tests/architecture/markers-snapshot.test.ts` returns 0 and the same grep against `tests/shared/markers.test.ts` returns at least 1 — each pin has exactly one owner.
    - The `locationsFor` case and the three agents-bridge pins survive: `node --test tests/architecture/markers-snapshot.test.ts` reports 4 passing cases.
    - `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/markers.ts` exits 0.
    - The SUMMARY records where the five superseded ES-5 literals are blocked today, with the search used to establish it.
  </acceptance_criteria>
  <done>Each marker byte pin has one owner, the snapshot keeps only cross-module assertions, and the header cites only files that exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged agent file → safety check | The agents-bridge marker constants are the substrings every staged agent file's basename and body must contain; the safety check rejects foreign content by them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-30 | Tampering | agents-bridge marker constants | high | mitigate | Marker drift would make the safety check reject every previously-staged agent file as foreign content, or accept content it should not. The byte pins are retained — one owner each, none deleted from both homes — and the acceptance criteria assert the surviving case count so a pin cannot be lost in the trim. |
| T-07-31 | Repudiation | the dangling gate citation | medium | mitigate | A header pointing at a gate that does not exist tells a reader a control is in place when it is not. The citation is repaired against a search of the tree, and if no gate blocks the superseded literals the SUMMARY records that rather than rewording the sentence. |
| T-07-32 | Tampering | `fetch.ts` import-form change | low | mitigate | A static import changes module evaluation order. The decision is taken from a full `npm run check` run rather than from reasoning, and the module's per-pair coverage run confirms no branch moved out of reach. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction: this plan installs no package and changes no `package.json` line. |
</threat_model>

## Artifacts this phase produces

New files:

- `tests/architecture/gate-targets.ts` — the literal repo-relative target registry (`D-07-05`)
- `tests/architecture/gate-targets.test.ts` — registry resolution clauses plus the self-hosting meta-gate (`D-07-06`)
- `tests/architecture/temp-root-control.ts` — shared mkdtemp / copy / mutate helper
- `tests/architecture/eslint-effective-config.ts` — shared `calculateConfigForFile` resolver and the three offender blocks
- `tests/architecture/eslint-effective-config.test.ts` — the `no-console` severity sweep gate (`GGAT-03`)
- `tests/architecture/closed-set-enrollment.test.ts` — `ClaudeHookEvent` / `Dependency` enrollment gate (`GGAT-04`)
- `tests/architecture/no-test-only-production-surface.test.ts` — `__`-prefixed member gate (`GGAT-04`)
- `tests/architecture/unowned-exports-census.test.ts` — pinned production-unowned-export census (`GGAT-04`)
- `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` — closed-by-earlier-phase evidence record

New exported symbols:

- `ScanReport` (`visited`, `waived`) and the `opts.root` parameter on `assertNoForbiddenSurface` — `tests/architecture/source-scan.ts`
- `withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss` — `tests/architecture/temp-root-control.ts`
- `resolveEffectiveConfig`, `BLANKET_NO_CONSOLE_OFF`, `ZONE_SUBSTITUTION`, `RESTRICTED_PATHS_OFF` — `tests/architecture/eslint-effective-config.ts`
- Registry constants in `tests/architecture/gate-targets.ts`: `EXTENSION_ROOT_REL`, `ORCHESTRATORS_REL`, `ARCHITECTURE_DIR_REL`, `NETWORK_FREE_TARGETS`, `MISSING_TARGET_PROBES`, `NO_CONSOLE_EXEMPT_TARGETS`, `ZONE_FOLDER_TARGETS`, `PLUGIN_LEDGER_TARGETS`, `MARKETPLACE_LEDGER_TARGETS`, `HOOKS_LIFECYCLE_TARGETS`, `VOCABULARY_GUARD_DOC_TARGETS`, `UNOWNED_EXPORT_CENSUS`, plus one group per migrated gate
- `selectBase(root)` and the `root` parameter on `gitLines` / `changedPaths` / `pairsForChangedPaths` — `scripts/test-coverage-direct.mjs`
- `_BucketAEventsCoverageProof` (type) — `extensions/pi-claude-marketplace/domain/components/hook-events.ts`

Production-owned collaborators replacing test-only surface:

- `ReinstallTransaction.replaceOperations: ReinstallReplaceOperations` plus the required second parameter of `replaceReinstalledPlugin(input, operations)` replace `ReplaceReinstalledPluginInput.__operations` — `orchestrators/plugin/reinstall-replace.ts`
- `stateTransaction?`, `removeDataDir?`, `cloneCacheSeam?` become typed top-level members of `ReinstallPluginOptions` / `ReinstallPluginsOptions`, replacing `__deps` and retiring `ReinstallPluginDeps` — `orchestrators/plugin/reinstall-flow.ts`

ESLint offender configs are **not** committed as files. Each offender is
`new ESLint({ cwd: REPO_ROOT, overrideConfigFile: "eslint.config.js", overrideConfig: [<exactly one block>] })`.

Removed by this phase: `ReplaceReinstalledPluginInput.__operations`; `ReinstallPluginDeps` and both
`__deps` members; the `export` keyword on `MARKETPLACE_VALIDATOR` and `surfacePostCommitWarnings`;
`loadZones()`; the `import-boundaries.test.ts` canary case and `tests/fixtures/bad-imports/**`;
`eslint.config.js` BLOCK D; the `hooks-dispatch.test.ts` config-scrape case; the duplicated byte
pins at `markers-snapshot.test.ts:55-70`; the orphaned comment at the end of
`orchestrators/plugin/info.ts`.

<verification>
- `npm run check` exits 0.
- `node --test tests/architecture/markers-snapshot.test.ts tests/shared/markers.test.ts` exits 0 with 4 cases surviving in the snapshot gate.
- Three `npm run test:coverage:direct` runs exit 0 for `fetch.ts`, `info.ts`, and `shared/markers.ts`.
- Every path cited in the snapshot gate's header was confirmed to resolve.
</verification>

<success_criteria>
- The `fetch.ts` import form is settled by an observed `npm run check` result, recorded either way.
- Each marker byte pin has exactly one owner and the dangling citation is gone.
- No comment in the tree describes the removed `info.ts` re-export.
</success_criteria>

<output>
Create `.planning/phases/07-gate-integrity/07-11-SUMMARY.md` when done. Record the static-import
attempt's `npm run check` result verbatim and which branch was taken, the header-citation resolution
check, and where the five superseded ES-5 marker literals are blocked today.
</output>
