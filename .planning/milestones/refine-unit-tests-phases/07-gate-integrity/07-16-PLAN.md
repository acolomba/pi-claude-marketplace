---
phase: 07-gate-integrity
plan: 16
type: execute
wave: 6
depends_on: ["07-03", "07-04", "07-05", "07-06", "07-07", "07-09", "07-11", "07-15"]
files_modified:
  - tests/architecture/gate-targets.test.ts
  - tests/architecture/gate-targets.ts
autonomous: true
requirements: [GGAT-01, GGAT-03, GGAT-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A production path named anywhere under `tests/architecture/**` outside the registry is an offender, so a literal-match stale-path scan reads one file and sees every guarded target."
    - "A production target assembled from segments anywhere under `tests/architecture/**` outside the registry is an offender, so the composed form cannot reintroduce the invisibility the literal rule closes."
    - "Module specifiers are excluded from both rules, with the reason recorded: a stale relative specifier breaks the typecheck or throws at runtime, which is louder than any gate could be."
    - "Comments are excluded from both rules, with the reason recorded: a file header legally names the path it guards, and every scanning clause in this tree strips comments before matching."
    - "The meta-gate fails against a temp-root copy of a gate file carrying a planted production path outside the registry, passes against the unmutated copy, and passes when the path appears only inside a comment."
    - "The meta-gate excludes itself and the registry module explicitly, since both necessarily spell what they forbid."
    - "The whole `npm run check` chain is green, and every gate this phase touched has been observed to fail against a planted offender at least once."
  artifacts:
    - tests/architecture/gate-targets.test.ts
  key_links:
    - "The meta-gate is compared against the composed-target census `07-06` measured, so a production composer that was neither migrated nor recorded is caught here rather than assumed absent."
    - "The `.mjs` gate scripts keep their own path handling and are outside this gate's scope, because they cannot import a `.ts` registry."
  prohibitions:
    - "No gate file under `tests/architecture/**` names a production path outside the registry, in a literal or assembled from segments."
    - "No exclusion is added to the meta-gate for a specific gate file; the only exclusions are the registry, the meta-gate itself, and the two structural rules for module specifiers and comments."
    - "No phase-level claim is recorded without a command that was run this cycle to establish it."
    - "No identifier or comment introduced here names a phase, plan, wave, or task number."
---

<objective>
Land the self-hosting meta-gate that makes the registry enforceable, then close the phase against
the whole quality chain with every gate proved to fire.

Purpose: `D-07-05` is a convention until something enforces it. `D-07-06` makes the registry
self-hosting: the gate scans the gate files themselves, so the next gate that spells a production
path locally fails instead of quietly reintroducing the invisibility criterion 4 names. It gets the
same offender and benign controls as every other gate, because a meta-gate nobody has seen fail is
the same liability as any other.

Output: two enforcement rules over `tests/architecture/**` with their exclusions justified, a
temp-root control set, a cross-check against the composed-target census, and a green
`npm run check`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-gate-integrity/07-CONTEXT.md
@.planning/phases/07-gate-integrity/07-RESEARCH.md
@.planning/phases/07-gate-integrity/07-PATTERNS.md
@.planning/phases/07-gate-integrity/07-VALIDATION.md
@.planning/phases/07-gate-integrity/07-06-SUMMARY.md
@.claude/rules/typescript-comments.md
@.claude/rules/typescript-unit-testing.md
@tests/architecture/gate-targets.ts
@tests/architecture/gate-targets.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two enforcement rules and their justified exclusions</name>
  <files>tests/architecture/gate-targets.test.ts</files>
  <behavior>
    - Scanning the real `tests/architecture/**` tree finds zero offenders under both rules.
    - A gate file carrying the string literal `extensions/pi-claude-marketplace/domain/manifest.ts` outside the registry is an offender under the literal rule.
    - A gate file carrying the same path prefixed `./` is also an offender; a relative module specifier beginning `../` is not.
    - A gate file calling `path.join` with a `.ts`-suffixed string literal argument is an offender under the composed rule.
    - The same production path appearing only inside a line or block comment is not an offender under either rule.
    - The registry module and the meta-gate file itself are excluded, and the exclusion is by exact path, not by pattern.
  </behavior>
  <read_first>
    - `tests/architecture/gate-targets.test.ts` (the resolution, non-emptiness, duplicate, and non-composition clauses `07-02` added — this task extends the same file)
    - `tests/architecture/partial-vocabulary-guard.test.ts` lines 50-110 and 240-260 (the directory-walk, self-exclusion, and allowlist-filter forms this gate mirrors)
    - `tests/architecture/source-scan.ts` (`stripComments` and its mandatory-use rationale; `assertNoForbiddenSurface` with `opts.root`)
    - `tests/architecture/temp-root-control.ts` (`withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss`)
    - `.planning/phases/07-gate-integrity/07-02-SUMMARY.md` and `.planning/phases/07-gate-integrity/07-06-SUMMARY.md` (the literal census and the composed-target census this gate is measured against)
    - `.planning/phases/07-gate-integrity/07-RESEARCH.md` §11 ranked risk 2 (the shape that keeps the meta-gate inside both complexity ceilings: a pure `namedProductionPaths(source)` extractor and a separate set-difference assertion)
  </read_first>
  <action>
Extend `tests/architecture/gate-targets.test.ts` with the two enforcement rules, keeping the file's
existing clauses untouched.

Split the work so neither function carries both parsing and policy, per the measured complexity
risk: write a pure `namedProductionPaths(source)` that takes comment-stripped source and returns the
production paths it names, and a separate assertion that subtracts the registry's own entries and
deep-equals the remainder against an empty array. ESLint's cognitive rule is off for `tests/**` and
Fallow's is not, so run the Fallow health gate rather than reading a green lint as evidence.

Rule one, literals. Over every `.ts` file under `tests/architecture/**` except the registry module
and this gate file, strip comments and find every string literal matching a repo-relative production
path — one beginning `extensions/pi-claude-marketplace/` or `./extensions/pi-claude-marketplace/`.
Each such literal is an offender. Write the exclusion reasoning into the clause: a module specifier
begins `../`, so this pattern does not match one, and that is deliberate — a stale relative specifier
breaks `npm run typecheck` or throws at runtime, which is a louder failure than a gate can produce.
Comments are stripped for the same reason every scanning clause in this tree strips them: a file
header legally names the path it guards.

Rule two, compositions. Over the same file set, find every `path.join` call whose argument list
includes a string literal ending in `.ts`. Each is an offender: a target assembled from a root
constant and a name is exactly what a literal-match stale-path scan cannot see, which is criterion 4.
Exclude `tests/architecture/temp-root-control.ts`, which joins a caller-supplied root with a
caller-supplied relative path and names no target of its own — state that reasoning beside the
exclusion.

Exclude the registry module and this gate file by exact path, not by a pattern that a future file
could accidentally match. Both necessarily spell what they forbid.

Own the failure message: name `D-07-05` and `D-07-06`, list every offending file with the path it
named, and state the sanctioned alternative — add the path to the registry group carrying that
obligation and import it.

Add the control set with `withTempRoot`: an offender that materializes a real gate file and plants a
production path literal into the copy; a benign control over the unmutated copy; a comment near-miss
that plants the same path inside a line comment; and a composed offender that plants a `path.join`
with a `.ts` literal. Derive each offender from a real gate file rather than authoring a synthetic
one, so it cannot drift from the thing it represents.
  </action>
  <verify>
    <automated>node --test tests/architecture/gate-targets.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npx fallow health --fail-on-issues --format human</automated>
    <fails_when>non-zero exit, or the output reports any unit above threshold</fails_when>
    <automated>npm run typecheck && npx eslint tests/architecture --max-warnings=0 && npx prettier --check "tests/architecture/*.ts"</automated>
    <fails_when>non-zero exit, or eslint prints "error"/"warning", or prettier names a file under "Code style issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/gate-targets.test.ts` reports at least 9 passing cases: the four `07-02` clauses plus the two enforcement rules and their four controls.
    - Both enforcement rules report zero offenders against the real tree; record the scanned file count in the SUMMARY.
    - Planting a production path literal into a real gate file makes the literal rule fail naming that file and that path; observe it, restore, and record the output.
    - Planting a `path.join` with a `.ts` literal into a real gate file makes the composed rule fail; observe it, restore, and record the output.
    - The same path inside a comment does not fail either rule; observe it and record.
    - The exclusions are exactly three files — the registry, this gate, and `temp-root-control.ts` — each with a comment stating its reason: `grep -c "EXCLUDED\|SELF" tests/architecture/gate-targets.test.ts` is recorded and every exclusion is justified in the SUMMARY.
  </acceptance_criteria>
  <done>The registry is self-hosting: a production path named or assembled outside it anywhere under `tests/architecture/**` fails a gate that has been observed to fire.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-check the meta-gate against both censuses and close the composed-target loop</name>
  <files>tests/architecture/gate-targets.test.ts, tests/architecture/gate-targets.ts</files>
  <read_first>
    - `.planning/phases/07-gate-integrity/07-02-SUMMARY.md` (the literal census: per-file production path counts at the start of the phase)
    - `.planning/phases/07-gate-integrity/07-06-SUMMARY.md` (the composed-target census, its production / non-production classification, and any production composer routed here as unmigrated)
    - `.planning/phases/07-gate-integrity/07-04-SUMMARY.md`, `07-05-SUMMARY.md`, `07-07-SUMMARY.md`, `07-14-SUMMARY.md` (the per-file before and after counts each migration recorded)
    - `tests/architecture/gate-targets.ts` (the registry as every plan left it)
  </read_first>
  <action>
Reconcile the meta-gate's result against the two censuses rather than trusting a green run. The
gate reporting zero offenders is consistent with two very different states: every path migrated, or
the rules matching nothing they should. The censuses are the independent side.

Sum the per-file literal counts `07-02` recorded and the per-file after-counts the four migration
plans recorded. Every literal that existed at the start of the phase must now be either a registry
entry, a module specifier, or a comment. Account for each one, and record any that is none of the
three.

Take the composed-target census `07-06` produced and confirm every row classified "production
composer" is migrated. If `07-06` routed any composer here as unmigrated, migrate it now — adding
its targets to the registry as the only registry write in this wave — or, if it turns out to be a
non-production composition, record the reclassification with the evidence. Do not leave a row
unresolved: a composition that is neither migrated nor classified is the state criterion 4 exists to
prevent.

Confirm the registry's own totals: how many groups, how many entries, how many resolve, and the
`MISSING_TARGET_PROBES` group still deliberately not resolving. Record the numbers.

Record explicitly that `scripts/check-corresponding-tests.mjs` and `scripts/test-coverage-direct.mjs`
keep their own path censuses and do not read the registry. `D-07-07` puts `.mjs` scripts out of
scope because they cannot import a `.ts` module, and recording it stops a later reader assuming one
registry covers both.
  </action>
  <verify>
    <automated>node --test tests/architecture/gate-targets.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>node --test "tests/architecture/*.test.ts"</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run typecheck && npx eslint tests --max-warnings=0 && npx fallow health --fail-on-issues --format human</automated>
    <fails_when>non-zero exit, or eslint prints "error"/"warning", or fallow reports any unit above threshold</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY carries the reconciliation: the starting literal count per file, and for each, whether it is now a registry entry, a module specifier, or a comment. Every literal is accounted for.
    - Every row of `07-06`'s composed-target census is resolved: migrated, or reclassified with evidence. No row is left open.
    - The SUMMARY records the registry's group count, entry count, resolving count, and the `MISSING_TARGET_PROBES` count.
    - The SUMMARY records that the two `.mjs` gate scripts keep their own censuses by design.
  </acceptance_criteria>
  <done>The meta-gate's green result is corroborated by two independent censuses, and no composition or literal from the starting tree is unaccounted for.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase against the full chain with every gate proved to fire</name>
  <files>tests/architecture/gate-targets.test.ts</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 7: Gate Integrity" (the four success criteria, including criterion 4's composed-path obligation)
    - `.planning/REQUIREMENTS.md` §"Gate Integrity" (`GGAT-01`, `GGAT-03`, `GGAT-04`)
    - `.planning/phases/07-gate-integrity/07-VALIDATION.md` (the per-requirement verification map every task inherited from)
    - Every SUMMARY file in `.planning/phases/07-gate-integrity/`
    - `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` (as `07-15` wrote it)
  </read_first>
  <action>
Run the full quality chain and close the phase on recorded evidence rather than on a green run
alone.

Build the fired-controls ledger: one row per gate this phase created or modified, naming the
offender that was planted, the command that observed it fail, and the SUMMARY that records it. A
gate with no row has never been seen to fail, and `CONVENTIONS.md` states the rule plainly — a gate
wants a test that plants the violation, not one that reads the config. Any gate this phase touched
without a fired control is a gap; plant one now or record why the class does not admit one.

Walk the four roadmap success criteria against the ledger and the SUMMARYs. Criterion 1 needs
visitation, an offender, and a benign control on every terminal scanning-gate gap, plus deterministic
base selection and the fail-closed zero-selection case. Criterion 2 needs varied effective-config
sources and broad overrides across both `FLOW-07` gaps with visitation proved. Criterion 3 needs the
closed-set and delegated-contract gates exercising real production consumers and the public seams
the approved splits created. Criterion 4 needs every composed-target gate discoverable without a
literal path in its own source and still resolving each target. For each, name the command and the
SUMMARY that establishes it.

Run `npm run check` in full, then `npm run test:integration` and `npm run test:coverage:direct:negative`
explicitly even though the chain includes them, and record each exit status and case count. Record
the total unit-suite case count against the counts `07-06` and `07-14` recorded, so a case silently
dropped anywhere in the phase is visible as a number rather than as an absence.

Run `pre-commit run --all-files` before committing, as the project requires, and fix and restage
until clean. Do not use `--no-verify` and do not amend to recover from a hook failure.
  </action>
  <verify>
    <automated>npm run check</automated>
    <fails_when>non-zero exit from any link in the chain, or any fallow sub-run prints a line starting with "✗"</fails_when>
    <automated>npm run test:integration</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run test:coverage:direct:negative</automated>
    <fails_when>non-zero exit, or the harness reports any planted state as unrefused</fails_when>
    <automated>pre-commit run --all-files</automated>
    <fails_when>non-zero exit, or any hook reports "Failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 and the SUMMARY records the unit and integration case counts.
    - The fired-controls ledger has a row for every gate this phase created or modified, each naming the planted offender, the observing command, and the SUMMARY that records it.
    - Each of the four roadmap success criteria is answered with a named command and a named SUMMARY.
    - `pre-commit run --all-files` exits clean before the commit, and the SUMMARY records that it was run before rather than after.
    - `git grep -c "__operations"`, `git grep -c "__deps"`, `git grep -c "loadZones"`, and `git grep -c "bad-imports"` all return 0.
    - The total unit-suite case count is recorded alongside the counts `07-06` and `07-14` recorded, with any difference attributed.
  </acceptance_criteria>
  <done>The phase closes on a green chain, a ledger showing every gate has been observed to fire, and a criterion-by-criterion answer backed by named commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | The meta-gate's controls create and delete real directories under `os.tmpdir()`. |
| gate corpus → itself | The meta-gate reads and judges the other gate files, including its own directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-48 | Repudiation | a meta-gate that matches nothing | high | mitigate | A zero-offender result is consistent with total compliance and with a rule that matches nothing. Four planted controls prove each rule fires, and Task 2 corroborates the result against two independently-measured censuses rather than accepting the green run. |
| T-07-49 | Repudiation | a per-file exclusion accumulating | high | mitigate | Exactly three exclusions exist, each an exact path with a stated reason, and adding a fourth for a specific gate file is prohibited. A meta-gate with a growing exclusion list is an allow-list wearing a different name — the failure mode `D-07-18` names. |
| T-07-50 | Tampering | temp-root controls | medium | mitigate | Every write is joined from `withTempRoot`'s `mkdtemp` return value and disposal happens in that helper's `finally`; the gate calls neither `mkdtemp` nor `rm` directly. |
| T-07-51 | Repudiation | closing the phase on a green run alone | high | mitigate | The fired-controls ledger requires a named planted offender and an observing command per gate, and each roadmap criterion is answered with a named command and SUMMARY. A green chain proves nothing failed; the ledger proves something can. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction: this plan installs no package and changes no `package.json` line. The whole phase installs nothing; `npm ci` already provides `node:test`, ESLint 10.8.1, TypeScript 6.0.3, Fallow 3.20.0 (signature self-verified), and git 2.55.0. |
</threat_model>

## Artifacts this phase produces

New files:

- `tests/architecture/gate-targets.ts` — the literal repo-relative target registry (`D-07-05`)
- `tests/architecture/gate-targets.test.ts` — registry resolution clauses plus the self-hosting meta-gate (`D-07-06`)
- `tests/architecture/temp-root-control.ts` — shared mkdtemp / copy / mutate helper
- `tests/architecture/eslint-effective-config.ts` — shared `calculateConfigForFile` resolver and the three offender blocks
- `tests/architecture/eslint-effective-config.test.ts` — the `no-console` severity sweep gate (`GGAT-03`)
- `tests/architecture/closed-set-enrollment.test.ts` — `ClaudeHookEvent` / `Dependency` enrollment gate (`GGAT-04`)
- `tests/architecture/no-test-only-production-surface.test.ts` — `__`-prefixed member gate (`GGAT-04`)
- `tests/architecture/unowned-exports-census.test.ts` — pinned production-unowned-export census (`GGAT-04`)
- `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` — closed-by-earlier-phase evidence record

New exported symbols:

- `ScanReport` (`visited`, `waived`) and the `opts.root` parameter on `assertNoForbiddenSurface` — `tests/architecture/source-scan.ts`
- `withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss` — `tests/architecture/temp-root-control.ts`
- `resolveEffectiveConfig`, `BLANKET_NO_CONSOLE_OFF`, `ZONE_SUBSTITUTION`, `RESTRICTED_PATHS_OFF` — `tests/architecture/eslint-effective-config.ts`
- Registry constants in `tests/architecture/gate-targets.ts`: `EXTENSION_ROOT_REL`, `ORCHESTRATORS_REL`, `ARCHITECTURE_DIR_REL`, `NETWORK_FREE_TARGETS`, `MISSING_TARGET_PROBES`, `NO_CONSOLE_EXEMPT_TARGETS`, `ZONE_FOLDER_TARGETS`, `PLUGIN_LEDGER_TARGETS`, `MARKETPLACE_LEDGER_TARGETS`, `HOOKS_LIFECYCLE_TARGETS`, `VOCABULARY_GUARD_DOC_TARGETS`, `UNOWNED_EXPORT_CENSUS`, plus one group per migrated gate
- `selectBase(root)` and the `root` parameter on `gitLines` / `changedPaths` / `pairsForChangedPaths` — `scripts/test-coverage-direct.mjs`
- `_BucketAEventsCoverageProof` (type) — `extensions/pi-claude-marketplace/domain/components/hook-events.ts`

Production-owned collaborators replacing test-only surface:

- `ReinstallTransaction.replaceOperations: ReinstallReplaceOperations` plus the required second parameter of `replaceReinstalledPlugin(input, operations)` replace `ReplaceReinstalledPluginInput.__operations` — `orchestrators/plugin/reinstall-replace.ts`
- `stateTransaction?`, `removeDataDir?`, `cloneCacheSeam?` become typed top-level members of `ReinstallPluginOptions` / `ReinstallPluginsOptions`, replacing `__deps` and retiring `ReinstallPluginDeps` — `orchestrators/plugin/reinstall-flow.ts`

ESLint offender configs are **not** committed as files. Each offender is
`new ESLint({ cwd: REPO_ROOT, overrideConfigFile: "eslint.config.js", overrideConfig: [<exactly one block>] })`.

Removed by this phase: `ReplaceReinstalledPluginInput.__operations`; `ReinstallPluginDeps` and both
`__deps` members; the `export` keyword on `MARKETPLACE_VALIDATOR` and `surfacePostCommitWarnings`;
`loadZones()`; the `import-boundaries.test.ts` canary case and `tests/fixtures/bad-imports/**`;
`eslint.config.js` BLOCK D; the `hooks-dispatch.test.ts` config-scrape case; the duplicated byte
pins at `markers-snapshot.test.ts:55-70`; the orphaned comment at the end of
`orchestrators/plugin/info.ts`.

<verification>
- `npm run check`, `npm run test:integration`, and `npm run test:coverage:direct:negative` all exit 0.
- `pre-commit run --all-files` exits clean before the commit.
- Four meta-gate controls were observed: literal offender, composed offender, unmutated benign, comment near-miss.
- Both censuses reconcile against the meta-gate's zero-offender result.
- The fired-controls ledger covers every gate this phase created or modified.
</verification>

<success_criteria>
- The registry is enforced by a self-hosting gate that has been observed to fire on both rules.
- Every literal and every composition from the starting tree is accounted for against two censuses.
- All four roadmap success criteria are answered with a named command and a named SUMMARY.
- `npm run check` is green and no gate this phase touched lacks a fired control.
</success_criteria>

<output>
Create `.planning/phases/07-gate-integrity/07-16-SUMMARY.md` when done. Record the fired-controls
ledger, the criterion-by-criterion answers, the two-census reconciliation, the registry totals, the
unit and integration case counts, and the `pre-commit run --all-files` result.
</output>
