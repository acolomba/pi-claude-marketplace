---
phase: 07-gate-integrity
plan: 05
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - tests/architecture/no-lifecycle-default-enabled-read.test.ts
  - tests/architecture/compat-01-no-expansion.test.ts
  - tests/architecture/manifest-read-seam.test.ts
  - tests/architecture/scope-order-drift.test.ts
  - tests/architecture/no-hooks-strict-additional-properties.test.ts
  - tests/architecture/no-split-01-cast-reads.test.ts
  - tests/architecture/reconcile-planner-purity.test.ts
autonomous: true
requirements: [GGAT-01]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Seven more architecture gates address their production targets through registry imports, leaving no repo-relative production path literal in any of them."
    - "The lifecycle default-enabled gate fails against a temp-root copy whose one mutated file carries the forbidden read, passes against the unmutated copy of the same real files, and passes when the forbidden token appears only inside a comment."
    - "The lifecycle default-enabled gate deep-compares the paths it opened against its registry group, and asserts the group is non-empty."
    - "Every other gate in this group asserts its declared list or its directory walk is non-empty."
    - "Each gate keeps its own patterns and its own requirement-anchored failure message; only the source of its targets changes."
  artifacts:
    - tests/architecture/no-lifecycle-default-enabled-read.test.ts
    - tests/architecture/compat-01-no-expansion.test.ts
    - tests/architecture/manifest-read-seam.test.ts
    - tests/architecture/scope-order-drift.test.ts
    - tests/architecture/no-hooks-strict-additional-properties.test.ts
    - tests/architecture/no-split-01-cast-reads.test.ts
    - tests/architecture/reconcile-planner-purity.test.ts
  key_links:
    - "`no-lifecycle-default-enabled-read.test.ts` is the second consumer of `assertNoForbiddenSurface`'s `opts.root`, so it is where the shared temp-root mechanic proves it generalizes past its first caller."
    - "`compat-01-no-expansion.test.ts` and `manifest-lookup-drift.test.ts` are the two importers of the exported `REPO_ROOT`; that import stays, only their path literals move."
  prohibitions:
    - "No gate in this group keeps a repo-relative production path as a local string literal."
    - "No temp-root control writes to any path not joined from its own `mkdtemp` return value, and no `rm` targets anything other than that value."
    - "No assertion, pattern, or failure message is weakened while re-pointing a gate."
    - "No identifier or comment introduced here names a phase, plan, wave, or task number."
    - "No new group is added to `tests/architecture/gate-targets.ts` by this plan."
---

<objective>
Re-point the remaining seven literal-path architecture gates at the registry, and give the one
that uses the shared scan mechanic the full `D-07-04` control set — offender, unmutated benign,
and comment near-miss — so the temp-root mechanic is proved on a second caller rather than only
on the one it was built for.

Purpose: a mechanism proved once can be an accident of its first caller. `no-lifecycle-default-enabled-read.test.ts`
is the shared scan's other production consumer, so it is the cheapest place to find out whether
the injected root and the `ScanReport` generalize.

Output: seven gates whose targets come from `gate-targets.ts`, one of them carrying a complete
offender and benign control set, and no repo-relative production path literal left in any of them.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gate-integrity/07-CONTEXT.md
@.planning/phases/07-gate-integrity/07-RESEARCH.md
@.planning/phases/07-gate-integrity/07-PATTERNS.md
@.planning/phases/07-gate-integrity/07-02-SUMMARY.md
@.claude/rules/typescript-comments.md
@.claude/rules/typescript-unit-testing.md
@tests/architecture/gate-targets.ts
@tests/architecture/temp-root-control.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the lifecycle default-enabled gate registry targets, a visitation proof, and both controls</name>
  <files>tests/architecture/no-lifecycle-default-enabled-read.test.ts, tests/architecture/compat-01-no-expansion.test.ts, tests/architecture/manifest-read-seam.test.ts</files>
  <behavior>
    - The lifecycle gate scanning the real tree passes and reports `visited` deep-equal to its registry group.
    - The same gate scanning a temp root whose copy of one target carries a planted forbidden read rejects with the gate's own requirement-anchored message.
    - The same gate scanning a temp root holding unmutated copies of the same targets passes.
    - The same gate scanning a temp root whose copy carries the forbidden token only inside a line comment passes, proving `stripComments` still runs.
    - An emptied registry group makes the gate fail on its non-emptiness assertion rather than pass over zero targets.
  </behavior>
  <read_first>
    - `tests/architecture/no-lifecycle-default-enabled-read.test.ts` (its 4 targets, its 2 patterns, and the `assertNoForbiddenSurface` call site at `:70`)
    - `tests/architecture/no-orchestrator-network.test.ts` as rewired by `07-01` (the registry-import form, the `ScanReport` visitation assertion, and the three temp-root control cases — this task copies that shape)
    - `tests/architecture/temp-root-control.ts` (`withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss`)
    - `tests/architecture/compat-01-no-expansion.test.ts` lines 90-130 and 320-340 (its `REPO_ROOT` import and its three path literals)
    - `tests/architecture/manifest-read-seam.test.ts` (its two path literals)
  </read_first>
  <action>
Re-point all three gates at the registry. The offending form is a string literal beginning with
`extensions/pi-claude-marketplace` or `./extensions/pi-claude-marketplace`; a module specifier
beginning with `../` is not an offending form and stays. `compat-01-no-expansion.test.ts` and
`manifest-read-seam.test.ts` keep their `REPO_ROOT` import from `./source-scan.ts` — one root
constant serves the whole directory.

For `no-lifecycle-default-enabled-read.test.ts`, capture the `ScanReport` from its existing call
site, assert its registry group is non-empty, and deep-compare `report.visited` against that group
with a message naming `D-07-03` and stating that a waived or unreadable target drops out of
`visited`. Then add three cases, each owning its own temp root through `withTempRoot`: an offender
that materializes the group, plants the forbidden default-enabled read onto the copy of one target,
and `assert.rejects` the scan run with `{ root }`; an unmutated benign control that expects the
scan to resolve with `visited` deep-equal to the group; and a near-miss that plants the forbidden
token inside a line comment on the same copy and expects the scan to resolve.

Derive the offender text from the real file rather than hand-authoring it: read the target through
`plantOffender` and append a line that uses the gate's own forbidden pattern against a real
identifier already present in that module. A hand-authored offender drifts from the target it
represents, which is the failure mode `D-07-01` exists to prevent.

For `compat-01-no-expansion.test.ts` and `manifest-read-seam.test.ts`, move their path literals to
registry imports and add a non-emptiness assertion on each declared list or walk. Change no
pattern, no failure message, and no assertion semantics.

Use lowercase `// arrange`, `// act`, `// assert` phases, name every value after its production
role, and give every case its own state — no `before()`, no module-level fixture.
  </action>
  <verify>
    <automated>node --test tests/architecture/no-lifecycle-default-enabled-read.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/architecture/manifest-read-seam.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npx fallow health --fail-on-issues --format human</automated>
    <fails_when>non-zero exit, or the output reports any unit above threshold</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/no-lifecycle-default-enabled-read.test.ts` reports at least 4 passing cases: the real-tree scan plus offender, benign, and near-miss controls.
    - Removing the planted line from the offender case and re-running that case alone makes it fail; restore and record the observation in the SUMMARY.
    - For each of the three files, a scan for string literals matching `^["'`](\./)?extensions/pi-claude-marketplace` over the comment-stripped source returns zero matches; record before counts of 4, 3, and 2.
    - The lifecycle gate contains a `report.visited` deep-compare against its registry group and a non-emptiness assertion whose message names `D-07-03`.
    - `grep -c "mkdtemp" tests/architecture/no-lifecycle-default-enabled-read.test.ts` returns 0; the temp-root lifecycle comes from `temp-root-control.ts`.
  </acceptance_criteria>
  <done>The shared temp-root and injected-root mechanics are proved on a second consumer, and three more gates take their targets from the registry.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point the four remaining single-literal gates</name>
  <files>tests/architecture/scope-order-drift.test.ts, tests/architecture/no-hooks-strict-additional-properties.test.ts, tests/architecture/no-split-01-cast-reads.test.ts, tests/architecture/reconcile-planner-purity.test.ts</files>
  <read_first>
    - `tests/architecture/gate-targets.ts` (the groups seeded by `07-02`)
    - `tests/architecture/scope-order-drift.test.ts` (its `import.meta.dirname` root derivation and its two path literals)
    - `tests/architecture/no-hooks-strict-additional-properties.test.ts`, `tests/architecture/no-split-01-cast-reads.test.ts`, `tests/architecture/reconcile-planner-purity.test.ts` (one path literal each)
    - `tests/architecture/no-lifecycle-default-enabled-read.test.ts` as rewired in Task 1 (the in-plan precedent)
  </read_first>
  <action>
Apply the same move to the four remaining gates. Replace each repo-relative production path literal
with a registry import and add a non-emptiness assertion on the declared list or directory walk,
matching Task 1's form and message.

`scope-order-drift.test.ts` derives its root from `import.meta.dirname` rather than the shared
`REPO_ROOT`. Replace that derivation with the import from `./source-scan.ts` so the directory has
one root constant rather than a private copy per gate; `hooks-lifecycle.test.ts` does the same
thing and is handled by `07-06-PLAN.md`.

Then run the whole architecture suite and confirm the total case count is unchanged relative to
the count `07-04` recorded, plus the four cases Task 1 added. A migration that silently drops a
case is the exact class this phase exists to make impossible, so the count is evidence, not
bookkeeping.
  </action>
  <verify>
    <automated>node --test tests/architecture/scope-order-drift.test.ts tests/architecture/no-hooks-strict-additional-properties.test.ts tests/architecture/no-split-01-cast-reads.test.ts tests/architecture/reconcile-planner-purity.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>node --test "tests/architecture/*.test.ts"</automated>
    <fails_when>non-zero exit, or the TAP summary reports anything other than "# fail 0"</fails_when>
    <automated>npm run typecheck && npx eslint tests/architecture --max-warnings=0 && npx fallow health --fail-on-issues --format human && npx prettier --check "tests/architecture/*.ts"</automated>
    <fails_when>non-zero exit, or eslint prints "error"/"warning", or fallow reports any unit above threshold, or prettier names a file under "Code style issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - For each of the four files, a scan for string literals matching `^["'`](\./)?extensions/pi-claude-marketplace` over the comment-stripped source returns zero matches.
    - `grep -c "import.meta.dirname" tests/architecture/scope-order-drift.test.ts` returns 0; it imports `REPO_ROOT` from `./source-scan.ts`.
    - `node --test "tests/architecture/*.test.ts"` exits 0, and the SUMMARY records the total case count alongside the count `07-04` recorded, accounting for exactly the cases this plan added.
    - Each of the seven files this plan touched carries a non-emptiness assertion naming `D-07-03`.
  </acceptance_criteria>
  <done>No repo-relative production path literal remains in any gate this plan owns, and the architecture suite's case count is accounted for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem | The lifecycle gate's three new controls create and delete real directories under `os.tmpdir()`. |
| registry data → filesystem read | Target paths move from gate-local literals to registry imports and are joined onto a scan root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-13 | Tampering | temp-root controls in the lifecycle gate | medium | mitigate | The gate calls `withTempRoot` / `materializeTargets` / `plantOffender` from `temp-root-control.ts` and writes no path of its own; every write is joined from the `mkdtemp` return value and disposal happens in that helper's `finally`. |
| T-07-14 | Denial of service | temp-root disposal | medium | mitigate | `rm` is confined to `withTempRoot`, which removes only the `mkdtemp` return value. No gate in this plan calls `rm` directly — the acceptance criteria assert `mkdtemp` does not appear in the gate file. |
| T-07-15 | Repudiation | the seven re-pointed gates | high | mitigate | Each gains a non-emptiness assertion, and the lifecycle gate additionally deep-compares `visited` against its registry group, so a gate cannot pass having inspected nothing. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction: this plan installs no package and changes no `package.json` line. |
</threat_model>

## Artifacts this phase produces

New files:

- `tests/architecture/gate-targets.ts` — the literal repo-relative target registry (`D-07-05`)
- `tests/architecture/gate-targets.test.ts` — registry resolution clauses plus the self-hosting meta-gate (`D-07-06`)
- `tests/architecture/temp-root-control.ts` — shared mkdtemp / copy / mutate helper
- `tests/architecture/eslint-effective-config.ts` — shared `calculateConfigForFile` resolver and the three offender blocks
- `tests/architecture/eslint-effective-config.test.ts` — the `no-console` severity sweep gate (`GGAT-03`)
- `tests/architecture/closed-set-enrollment.test.ts` — `ClaudeHookEvent` / `Dependency` enrollment gate (`GGAT-04`)
- `tests/architecture/no-test-only-production-surface.test.ts` — `__`-prefixed member gate (`GGAT-04`)
- `tests/architecture/unowned-exports-census.test.ts` — pinned production-unowned-export census (`GGAT-04`)
- `.planning/phases/07-gate-integrity/07-FINDING-DISPOSITIONS.md` — closed-by-earlier-phase evidence record

New exported symbols:

- `ScanReport` (`visited`, `waived`) and the `opts.root` parameter on `assertNoForbiddenSurface` — `tests/architecture/source-scan.ts`
- `withTempRoot`, `materializeTargets`, `plantOffender`, `plantBenignNearMiss` — `tests/architecture/temp-root-control.ts`
- `resolveEffectiveConfig`, `BLANKET_NO_CONSOLE_OFF`, `ZONE_SUBSTITUTION`, `RESTRICTED_PATHS_OFF` — `tests/architecture/eslint-effective-config.ts`
- Registry constants in `tests/architecture/gate-targets.ts`: `EXTENSION_ROOT_REL`, `ORCHESTRATORS_REL`, `ARCHITECTURE_DIR_REL`, `NETWORK_FREE_TARGETS`, `MISSING_TARGET_PROBES`, `NO_CONSOLE_EXEMPT_TARGETS`, `ZONE_FOLDER_TARGETS`, `PLUGIN_LEDGER_TARGETS`, `MARKETPLACE_LEDGER_TARGETS`, `HOOKS_LIFECYCLE_TARGETS`, `VOCABULARY_GUARD_DOC_TARGETS`, `UNOWNED_EXPORT_CENSUS`, plus one group per migrated gate
- `selectBase(root)` and the `root` parameter on `gitLines` / `changedPaths` / `pairsForChangedPaths` — `scripts/test-coverage-direct.mjs`
- `_BucketAEventsCoverageProof` (type) — `extensions/pi-claude-marketplace/domain/components/hook-events.ts`

Production-owned collaborators replacing test-only surface:

- `ReinstallTransaction.replaceOperations: ReinstallReplaceOperations` plus the required second parameter of `replaceReinstalledPlugin(input, operations)` replace `ReplaceReinstalledPluginInput.__operations` — `orchestrators/plugin/reinstall-replace.ts`
- `stateTransaction?`, `removeDataDir?`, `cloneCacheSeam?` become typed top-level members of `ReinstallPluginOptions` / `ReinstallPluginsOptions`, replacing `__deps` and retiring `ReinstallPluginDeps` — `orchestrators/plugin/reinstall-flow.ts`

ESLint offender configs are **not** committed as files. Each offender is
`new ESLint({ cwd: REPO_ROOT, overrideConfigFile: "eslint.config.js", overrideConfig: [<exactly one block>] })`.

Removed by this phase: `ReplaceReinstalledPluginInput.__operations`; `ReinstallPluginDeps` and both
`__deps` members; the `export` keyword on `MARKETPLACE_VALIDATOR` and `surfacePostCommitWarnings`;
`loadZones()`; the `import-boundaries.test.ts` canary case and `tests/fixtures/bad-imports/**`;
`eslint.config.js` BLOCK D; the `hooks-dispatch.test.ts` config-scrape case; the duplicated byte
pins at `markers-snapshot.test.ts:55-70`.

<verification>
- `node --test "tests/architecture/*.test.ts"` exits 0.
- `npm run typecheck && npx eslint tests/architecture --max-warnings=0` exits 0.
- `npx fallow health --fail-on-issues` reports "0 above threshold".
- The lifecycle gate's offender control was observed failing when its planted line was removed.
</verification>

<success_criteria>
- Seven gates address production targets only through `gate-targets.ts`.
- The shared temp-root and injected-root mechanics are proved on a second consumer.
- Per-file literal counts fall from 4/3/2/2/1/1/1 to 0.
</success_criteria>

<output>
Create `.planning/phases/07-gate-integrity/07-05-SUMMARY.md` when done. Record per-file literal
counts before and after, the total architecture-suite case count, and the observed offender-control
negative check.
</output>
