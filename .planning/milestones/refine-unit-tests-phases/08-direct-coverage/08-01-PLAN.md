---
phase: 08-direct-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/test-coverage-direct.report.mjs
  - scripts/test-coverage-direct.negative.mjs
  - .pre-commit-config.yaml
autonomous: true
requirements: [RCOV-01, RCOV-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run test:coverage:direct:report` runs to completion over every pair `productionPaths()` enumerates and prints both its row-count line and its `Verdicts:` tally."
    - "`npm run test:coverage:direct:negative` refuses when the reporter's pair-enumeration call is given a numeric second argument, so the repaired call carries its own planted control inside `npm run check`."
    - "`pre-commit run --all-files` completes with no hook reporting `files were modified by this hook`."
    - "The enumeration sweep ran in this repository checkout, so the `scripts/revalidation.mjs` pair was measured rather than aborted."
    - statement: "The sweep's recorded verdict tally and every `accepted-shortfall` row with its exact reading string are written into this plan's SUMMARY, because `coverage/` is gitignored and cannot carry the classification input."
      verification: backstop
  artifacts:
    - scripts/test-coverage-direct.report.mjs
    - scripts/test-coverage-direct.negative.mjs
    - .pre-commit-config.yaml
    - .planning/phases/08-direct-coverage/08-01-SUMMARY.md
  key_links:
    - "`scripts/test-coverage-direct.report.mjs` -> `pairForPath` from `scripts/test-coverage-direct.mjs`: the callback must pass ONE argument, or `Array.prototype.map`'s index lands in `selectedProjectRoot`."
    - "`scripts/test-coverage-direct.negative.mjs` -> `verdictFor` (already imported) and `pairForPath`: the planted control needs no new import."
    - "`.pre-commit-config.yaml` `fix-unicode-dashes` `exclude:` -> `tests/architecture/revalidation.test.ts`, which pins the em-dash bytes `scripts/revalidation.mjs` emits."
  prohibitions:
    - "Do NOT pass an exported root-taking gate function as a bare callback to `map`, `forEach`, or `filter` anywhere under `scripts/`."
    - "Do NOT teach `scripts/test-coverage-direct.report.mjs` what an acceptable reading is; it measures and records, and files no verdict (D-08-19)."
    - "Do NOT drop or rewrite the em-dash bytes in `scripts/revalidation.mjs` or `tests/architecture/revalidation.test.ts` to satisfy the dash hook (D-08-20)."
    - "Do NOT read `coverage/all-pairs.jsonl` as a baseline; it holds 83 of 230 rows and stops at its first refusal (D-08-04)."
    - "Do NOT run either sweep in a copy, a scratch tree, or a `.planning`-less worktree (D-08-A10)."
    - "Do NOT carry a shortfall count into this phase from the roadmap, `CONTRIBUTING.md`, or `08-CONTEXT.md` (D-08-02, D-08-10)."
    - "Do NOT use `--no-verify`; `SKIP=<hook-id>` is the only sanctioned pre-commit escape."
    - "Do NOT write `Phase NN`, `Plan NN`, `Wave N`, or `Pitfall N` into any comment or test title."
---

<objective>
Repair the instrument the rest of the phase depends on, give it a planted control, clear
the pre-commit blocker, and produce the one measured enumeration the classification work
reads.

`scripts/test-coverage-direct.report.mjs:112` passes `pairForPath` as a bare `map`
callback, so `Array.prototype.map`'s index argument lands in the `selectedProjectRoot`
parameter that `c0241c82` added. The reporter exits 1 after 0.6 s having measured nothing
and has never run to completion (D-08-A01). `RCOV-01` names it as the baseline generator
and `D-08-08` orders two full runs through it, so this repair blocks the whole phase.

Nothing in `npm run check` runs the reporter. That is exactly how the defect shipped and
then survived a code review inside the phase that introduced it, so the repair carries its
own planted control in the existing negative harness (D-08-A09).

`pre-commit run --all-files` is red today on `fix-unicode-dashes` over
`tests/architecture/revalidation.test.ts`. This phase's own deliverable is a new
pre-commit hook, and a new hook cannot be verified inside a run that is already failing
(D-08-20, D-08-A12).

Purpose: make measurement possible, then measure once, honestly, before anything is
classified.
Output: a reporter that completes, a control that refuses its regression, a green
`--all-files` run, and a SUMMARY carrying the measured verdict tally and every refused row.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-direct-coverage/08-CONTEXT.md
@.planning/phases/08-direct-coverage/08-RESEARCH.md
@.planning/phases/08-direct-coverage/08-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

## Artifacts this phase produces

Every symbol, file, id, and argument Phase 8 creates. Plans 02-09 create the rest; this
list is the phase-level inventory so no plan invents a second name for the same thing.

| Artifact | Kind | Home | Created by |
|---|---|---|---|
| `RemovalOps` | interface, members `rm(target, options)` and `rename(from, to)` | `extensions/pi-claude-marketplace/shared/fs-utils.ts` | plan 05 |
| `createRemovalOps()` | production factory | `extensions/pi-claude-marketplace/shared/fs-utils.ts` | plan 05 |
| `createRemovalOpsFake(options)`, `RemovalOpsFake`, `RemovalOpsFakeOptions`, `RemovalOpsFakeCalls` | reusable concern-owned test double | `tests/platform/removal-ops-fake.ts` | plan 05 |
| `removalOpsContract(createOps)` | shared adapter contract | `tests/platform/removal-ops-contract.ts` | plan 05 |
| `tests/platform/removal-ops-fake.test.ts` | structural-supplement suite (fake's negative control) | `tests/platform/` | plan 05 |
| `scripts/test-coverage-direct.pin.json` | committed accepted-shortfall pin data | `scripts/` | plan 04, regenerated by plan 08 |
| `loadCoveragePin(selectedProjectRoot)` | thin pin I/O + shape validation | `scripts/test-coverage-direct.pin.mjs` | plan 04 |
| `assertPinnedReadings(observed, pinRows)` | pure bidirectional comparison | `scripts/test-coverage-direct.pin.mjs` | plan 04 |
| `shortfallReadingOf(error, sourcePath)` | exported pure reading parser | `scripts/test-coverage-direct.mjs` | plan 04 |
| `--base <ref>` | new gate CLI argument, fail-closed explicit base | `scripts/test-coverage-direct.mjs` | plan 04 |
| `test:coverage:direct:commit` | npm script, commit-scoped gate invocation | `package.json` | plan 09 |
| `npm-coverage-direct` | pre-commit local hook id | `.pre-commit-config.yaml` | plan 09 |
| `direct-coverage` / `direct coverage (changed pairs, Node 24)` | CI job id / name | `.github/workflows/ci.yml` | plan 09 |
| `runtimeAdvancingGeneration` (or the test's chosen name) | per-case `HooksRuntime` spread decorator | `tests/bridges/hooks/event-router.test.ts` | plan 03 |
| `pinnedRow` shared literal | negative-harness fixture for the four pin cases | `scripts/test-coverage-direct.negative.mjs` | plan 04 |

This plan creates no new file. It repairs two scripts, widens one hook exclusion, and
produces one measured record in its SUMMARY.

**Diff-range convention for this plan.** Before this plan's first commit, the executor records
`PLAN_BASE=$(git rev-parse HEAD)`. Every `git diff` check below is written
`git diff "$PLAN_BASE"..HEAD -- <paths>` (adding `git status --porcelain -- <paths>` when the
working tree is also in question). A bare `git diff` with no range is empty once the work is
committed, so it would pass vacuously and prove nothing.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the whole-tree reporter measures every pair and a planted regression refuses" — one instrument only</name>
  <files>scripts/test-coverage-direct.report.mjs, scripts/test-coverage-direct.negative.mjs</files>
  <read_first>
    - `scripts/test-coverage-direct.report.mjs` (all 151 lines — the broken callback is at line 112; `verdictFor` at 41; the header's independence promise at 12-21)
    - `scripts/test-coverage-direct.mjs` lines 60-112 (`pairForPath`'s `selectedProjectRoot` parameter and its doc comment) and lines 585-595 (`runAllPairs`, which already wraps the same call correctly)
    - `scripts/test-coverage-direct.negative.mjs` lines 1-75 (its imports — `verdictFor` is already imported — the `fixtureRoot`/`projectRoot` constants, and the `inRepo` helper) and lines 218-310 (the `assertReportComplete` group: one shared literal, a `doesNotThrow` control first, then one `assert.throws` per divergence class with a comment saying why no other check could refuse it)
    - `08-RESEARCH.md` §"Finding 0 (blocking)" (the stack trace, the dated root cause, and the three planner obligations it creates)
  </read_first>
  <action>
Repair the reporter's pair enumeration and plant its regression in the same task, so the
repaired line is covered by something that runs inside `npm run check`.

In `scripts/test-coverage-direct.report.mjs` at line 112, replace the bare callback
reference in the enumeration loop with an explicit arrow that passes only the module path —
`(modulePath) => pairForPath(modulePath)` — matching the form `runAllPairs` already uses at
`scripts/test-coverage-direct.mjs:590`. Change nothing else about the loop, the row shape,
`rowFor`, `verdictFor`, or the usage text. The reporter must keep filing no verdict and
keep not reading the ledger (D-08-19).

Then add one case to `scripts/test-coverage-direct.negative.mjs`, inside the existing
top-level `try` block and beside the `assertReportComplete` group so the file's one
`fixtureRoot` and its `finally` disposal still cover everything. The case plants the arity
bug directly against the gate's exported `pairForPath`: import `pairForPath` alongside the
five names the file already imports from the gate, pick one real in-tree module path
(`extensions/pi-claude-marketplace/shared/atomic-json.ts` is already declared in the file
as `realSourcePath`), and assert two things in the file's established order —

1. the control first: `assert.doesNotThrow` that calling it with the path alone answers a
   pair, with a comment stating that without this control the refusal below could be firing
   on a bad path rather than on the arity;
2. the refusal: `assert.throws` that calling it with the path and a numeric second argument
   refuses, matching the message Node's `path.resolve` produces for a numeric first
   segment. Use a `/regex/` rather than a whole-string `{ message }` match, because the
   message text belongs to Node and not to this repository.

Write a comment above the refusal naming the state it plants — a root-taking exported gate
function reached through an array-iteration callback that supplies the element index as a
second argument — and why no other check in `npm run check` refuses it: nothing in the
check chain invokes the reporter at all.

Extend this plan's `process.stdout.write` summary line at the end of the harness to name
the new control alongside the ones it already lists, keeping the file's single-sentence
style.

Do not introduce a new `mkdtemp` root, a new fixture repository, or a second LCOV literal;
this case needs none (D-08-A13: the harness's helpers are module-private, so the case lives
inside the file and imports nothing from it).
  </action>
  <acceptance_criteria>
    - `scripts/test-coverage-direct.report.mjs` contains no occurrence of the string `modulePaths.map(pairForPath)`.
    - `scripts/test-coverage-direct.report.mjs`'s import list from `./test-coverage-direct.mjs` is unchanged: `assertReportComplete`, `pairForPath`, `productionPaths`, `runPair` — no new import, and in particular nothing named `pin` or `Pin`.
    - `scripts/test-coverage-direct.negative.mjs` imports `pairForPath` from `./test-coverage-direct.mjs`.
    - The new case's refusal assertion passes a number as `pairForPath`'s second argument and expects a throw; the control assertion passes one argument and expects none.
    - `node -e 'import("./scripts/test-coverage-direct.mjs").then(m => m.productionPaths().slice(0, 3).map((p) => m.pairForPath(p)))'` exits 0.
    - `npm run test:coverage:direct:negative` prints its final summary line and that line names the new reporter-enumeration control.
    - `npx eslint scripts/test-coverage-direct.report.mjs scripts/test-coverage-direct.negative.mjs --max-warnings=0` exits 0.
    - `npx prettier --check "scripts/**/*.mjs"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:coverage:direct:negative</automated>
    <fails_when>non-zero exit, or the final stdout line does not mention the reporter pair-enumeration control (a harness that printed its old summary ran none of the new assertions)</fails_when>
    <automated>node -e 'import("./scripts/test-coverage-direct.report.mjs").then(() => process.stdout.write("reporter module loaded\n"))'</automated>
    <fails_when>non-zero exit, or `reporter module loaded` absent from stdout</fails_when>
    <automated>npx eslint scripts/test-coverage-direct.report.mjs scripts/test-coverage-direct.negative.mjs --max-warnings=0 && npx prettier --check "scripts/**/*.mjs"</automated>
    <fails_when>non-zero exit, or prettier lists either script under "Code style issues found"</fails_when>
  </verify>
  <done>The reporter builds a pair for every enumerated module without throwing, and `npm run test:coverage:direct:negative` refuses the arity regression while its control passes — so the repaired line is inside `npm run check`.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the fix-unicode-dashes exclusion so pre-commit --all-files is green before a new hook joins it</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - `.pre-commit-config.yaml` lines 44-60 (the `texthooks` block, the existing `fix-unicode-dashes` `exclude:` regex at line 56 and the four-line comment above it at 51-54) and lines 100-125 (the four `npm-*` local hooks, for the shape plan 09 copies)
    - `08-RESEARCH.md` §"`.pre-commit-config.yaml` — the `D-08-20` em-dash fix, verified" (the reproduced before/after hook output, the exact widened regex, and the widened comment)
    - `08-CONTEXT.md` `D-08-20` and `D-08-A12` (why widening beats dropping the dash from either side, and the single-offender scan)
  </read_first>
  <action>
Widen the `fix-unicode-dashes` hook's `exclude:` regex from
`^(\.planning/|scripts/revalidation\.mjs$)` to
`^(\.planning/|scripts/revalidation\.mjs$|tests/architecture/revalidation\.test\.ts$)`.

Widen the four-line comment above the hook in the same edit. The comment today explains
only `scripts/revalidation.mjs`; a reader meeting the widened regex needs the second half
of the reason. State both: `scripts/revalidation.mjs` emits the em-dash that `.planning/`
documents carry and `.planning/` is excluded here, and
`tests/architecture/revalidation.test.ts` pins the exact bytes that script emits — so
rewriting either side's dash literals breaks the planning-contract parser, whose `RVAL-04`
record scan stops matching every row. Keep the comment within yamllint's and yamlfmt's
tolerance (the block already passes both) and cite `D-08-20` and `RVAL-04` rather than any
planning-step reference.

Touch no other hook, no `rev:` pin, and no other `exclude:`. In particular leave the
`prettier` mirror hook's `prettier@3.9.5` pin alone — the installed devDependency is 3.9.6
and `package.json` declares `^3.8.3`, which is real drift but is not this phase's
obligation.
  </action>
  <acceptance_criteria>
    - `.pre-commit-config.yaml`'s `fix-unicode-dashes` `exclude:` value matches `^(\.planning/|scripts/revalidation\.mjs$|tests/architecture/revalidation\.test\.ts$)` exactly.
    - The comment block immediately above `- id: fix-unicode-dashes` names `tests/architecture/revalidation.test.ts` and cites `RVAL-04`.
    - `git diff "$PLAN_BASE"..HEAD --stat -- .pre-commit-config.yaml` reports only `.pre-commit-config.yaml`, and `git diff "$PLAN_BASE"..HEAD -- .pre-commit-config.yaml` shows no change to any `rev:` line or to any hook id other than `fix-unicode-dashes`.
    - `pre-commit run --all-files` reports no hook with `files were modified by this hook`.
  </acceptance_criteria>
  <verify>
    <automated>pre-commit run fix-unicode-dashes --all-files</automated>
    <fails_when>non-zero exit, or the output contains `files were modified by this hook`</fails_when>
    <automated>pre-commit run --all-files</automated>
    <fails_when>non-zero exit, or any hook line reports `Failed`, or the output contains `files were modified by this hook`</fails_when>
  </verify>
  <done>`pre-commit run --all-files` is green, so plan 09 can verify a newly added local hook inside a run that is not already failing.</done>
</task>

<task type="auto">
  <name>Task 3: Run the enumeration sweep in this repository and record the measured shortfall set</name>
  <files>.planning/phases/08-direct-coverage/08-01-SUMMARY.md</files>
  <read_first>
    - `scripts/test-coverage-direct.report.mjs` as repaired in task 1 (the `Verdicts:` tally line at the end of `main`, and the per-row `Direct coverage shortfall:` line at 118-122)
    - `scripts/test-coverage-direct.mjs` lines 455-485 (`assertCompleteCoverage` — the one place the reading string `branches H/F, functions H/F, lines H/F` is formatted, which is the exact string the pin will later compare)
    - `08-RESEARCH.md` §"The Measured Baseline" (the 229-row tally, the 230th-pair abort condition, and the timing table) and §"Shortfall Dispositions, Measured"
    - `08-CONTEXT.md` `D-08-02`, `D-08-08`, `D-08-10`, `D-08-A02`, `D-08-A10`
  </read_first>
  <precondition>The working tree is this repository checkout and `.planning/` is present, because `tests/architecture/revalidation.test.ts` reads it and `verdictFor` rethrows a focused-test failure instead of recording it.</precondition>
  <action>
Run `npm run test:coverage:direct:report` to completion in this checkout. It takes roughly
eight minutes and writes one JSON row per pair to the gitignored
`coverage/all-pairs-report.ndjson`. Do not run it in a copy, a scratch directory, or a
worktree (D-08-A10).

Assert the run completed rather than halted: its last two stdout lines are the
`All-pair report written: <rows> rows in <seconds>s on <node> to <path>` line and the
`Verdicts: <tally>` line, and the row count equals `productionPaths().length`. A run that
exits non-zero after fewer rows than that measured a partial tree, which is the failure
mode this phase exists to end.

Then write the measurement into this plan's SUMMARY, because `coverage/` is gitignored and
the classification work in plans 02, 03, 05, and 07 consumes this record:

1. the measured pair count from `productionPaths().length`, with the reproduction command;
2. the verbatim `Verdicts:` tally line;
3. one row per `accepted-shortfall` verdict, giving the `sourcePath` and the **exact**
   reading string the report recorded for it — the string `assertCompleteCoverage` formats,
   not a paraphrase, because the pin compares that string byte for byte;
4. one row per `type-only` verdict, by path;
5. for each `accepted-shortfall` row, the disposition this phase has already decided for
   it, citing the decision: rewrite (`D-08-09`, `D-08-A03`), tests (`D-08-A04`, research §5,
   `D-08-A06`), or pin candidate (`D-08-03`, `D-08-A05`);
6. for each `accepted-shortfall` row that NO decision names — a module nobody has measured
   before — an explicit "unclassified, needs a disposition" entry naming the module and its
   reading, and a line in the SUMMARY's blockers section escalating it. `D-08-09a` forbids
   pinning such a row by analogy: it gets a rewrite, a test, or an escalation.

Write no count into any file outside the SUMMARY. Do not edit `CONTRIBUTING.md`,
`.planning/REQUIREMENTS.md`, or `.planning/ROADMAP.md` in this plan — plan 08 rewrites those
from the SECOND sweep, which is the one that reflects the final tree (`D-08-08`).

If the report halts on a row with `Focused test failed:` rather than recording a coverage
verdict, that is a test failure in the tree and not a coverage reading: stop, name the
failing suite, and report it as a blocker rather than re-running the sweep elsewhere.
  </action>
  <acceptance_criteria>
    - `npm run test:coverage:direct:report` exits 0.
    - Its stdout contains a line starting `All-pair report written: ` whose row count equals the value `node -e 'import("./scripts/test-coverage-direct.mjs").then(m => console.log(m.productionPaths().length))'` prints.
    - Its stdout contains a line starting `Verdicts: ` and that line names `complete`, `type-only`, and `accepted-shortfall`.
    - `wc -l < coverage/all-pairs-report.ndjson` equals the same pair count.
    - `08-01-SUMMARY.md` contains the verbatim `Verdicts:` line, one entry per `accepted-shortfall` row with its exact reading string, and a named disposition or an explicit escalation for each.
    - `08-01-SUMMARY.md` contains no claim of the form "N modules fall short" that is copied from `.planning/ROADMAP.md`, `CONTRIBUTING.md`, or `08-CONTEXT.md` rather than read off this run.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:coverage:direct:report</automated>
    <fails_when>non-zero exit, or stdout carries no line starting `Verdicts: `, or the `All-pair report written:` row count is lower than `productionPaths().length` (a halted sweep reports a plausible-looking partial tally)</fails_when>
    <automated>node -e 'const fs=require("node:fs");const rows=fs.readFileSync("coverage/all-pairs-report.ndjson","utf8").split("\n").filter(Boolean).map(JSON.parse);import("./scripts/test-coverage-direct.mjs").then(m=>{const expected=m.productionPaths().length;if(rows.length!==expected){throw new Error(`report holds ${rows.length} rows, productionPaths enumerates ${expected}`)}process.stdout.write(`rows ${rows.length} verdicts ${JSON.stringify(rows.reduce((a,r)=>({...a,[r.verdict]:(a[r.verdict]??0)+1}),{}))}\n`)})'</automated>
    <fails_when>non-zero exit, or the printed `rows` count differs from `productionPaths().length`</fails_when>
  </verify>
  <done>One complete sweep of every enumerated pair has run in this repository, and its verdict tally plus every refused row with its exact reading is recorded in the SUMMARY as the classification input for plans 02, 03, 05, and 07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree -> gate scripts | `changedPaths` and `selectBase` read ref names and path names out of `git` stdout; those values reach later `git` invocations and `path.resolve` |
| LCOV file -> `assertCompleteCoverage` | the focused test run writes a file the gate parses into a verdict |
| `.pre-commit-config.yaml` -> every hook | a widened `exclude:` is a reduction in what the repository scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Repudiation | `scripts/test-coverage-direct.report.mjs` enumeration loop | high | mitigate | Task 1 plants the arity regression in `scripts/test-coverage-direct.negative.mjs`, which runs inside `npm run check`; task 3's verify asserts the printed row count equals `productionPaths().length`, so a halted sweep cannot read as a completed one |
| T-08-06 | Tampering | `fix-unicode-dashes` `exclude:` widening | medium | mitigate | `D-08-A12` scanned every git-tracked file, outside the hook's and pre-commit's existing exclusions, for U+2010..U+2015 and U+2212 and found exactly one offender; task 2's verify runs `pre-commit run --all-files` so a masked second offender would surface as a different hook failing |
| T-08-05 | Injection | `selectBase` ref name from `git` stdout | low | accept | Already mitigated upstream of this plan: `baseCandidateName = /^[A-Za-z0-9._/-]+$/` refuses anything else, and every git call is an argument array, never a shell string. This plan changes neither |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan runs no package-manager install. No dependency is added, removed, or re-pinned; the `prettier@3.9.5` hook pin is explicitly left untouched |
</threat_model>

<verification>
- `npm run test:coverage:direct:negative` exits 0 and its summary line names the new control.
- `pre-commit run --all-files` exits 0 with no `files were modified by this hook`.
- `npm run test:coverage:direct:report` exits 0 and reports one row per enumerated pair.
- Wave boundary: `npm run typecheck && npm run lint && npm run fallow && npm run format:check && npm run test:coverage:direct:negative && npm test`.
</verification>

<success_criteria>
- The reporter runs to completion over every enumerated pair (RCOV-01's baseline generator works at all).
- The repair is guarded by a planted control that sits inside `npm run check` (RCOV-01).
- `pre-commit run --all-files` is green, clearing the precondition for plan 09's new hook (RCOV-03).
- The SUMMARY carries the measured verdict tally, every refused row with its exact reading, and a named disposition or escalation for each — with no count inherited from any prior artifact.
</success_criteria>

<output>
Create `.planning/phases/08-direct-coverage/08-01-SUMMARY.md` when done.
</output>
