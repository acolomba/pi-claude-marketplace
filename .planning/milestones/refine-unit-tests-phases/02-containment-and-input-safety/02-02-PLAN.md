---
phase: 02-containment-and-input-safety
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - extensions/pi-claude-marketplace/shared/path-safety.ts
  - tests/shared/path-safety.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/bridges/commands/stage.test.ts
  - tests/persistence/locations.test.ts
autonomous: true
requirements: [PDEF-02]
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PDEF-02: A contained absolute child remains accepted after normalization."
    - "PDEF-02: A raw child containing a lexical .. segment produces an honest LexicalTraversalError whose normalized fields and traversal-specific message describe the refusal, while outside bytes/tree remain unchanged."
    - "PDEF-02: Every existing child component is lstat-walked from the normalized root and the first symlink, including an intermediate one, is rejected before a read or write."
    - "PDEF-02: The lenient plugin-info path converts containment refusal into its exact existing unreadable/unavailable row without reading outside the marketplace root; command staging and state-derived location paths likewise cannot mutate outside their roots."
    - "The specless PDEF-02 fallback row is resolved by the shared owner plus explicit plugin-info, command-stage, and locations consumer checks."
  artifacts:
    - "extensions/pi-claude-marketplace/shared/path-safety.ts owns the normalized lexical and real-filesystem policy."
    - "tests/shared/path-safety.test.ts owns contained-absolute, normalized traversal, missing leaf, and intermediate-symlink behavior."
    - "The three existing consumer owner suites prove lenient read, manifest staging, and state-derived path outcomes without duplicating policy."
  key_links:
    - "Implementation order decides the raw-child segment gate before normalization or filesystem inspection; a refused path is normalized only for LexicalTraversalError diagnostics, while accepted paths use one normalized operand pair for containment errors and the lstat walk."
    - "plugin/info.ts, bridges/commands/stage.ts, and persistence/locations.ts continue to call assertPathInside before their affected reads, writes, or returned paths."
    - "All other live assertPathInside callers inherit the shared repair without per-consumer helpers."
  prohibitions:
    - "Containment cannot reject contained absolute paths or read/write outside the root."
    - "Tests cannot touch developer state or add test-only seams."
---

<objective>
Correct PDEF-02 at the shared path-containment chokepoint and prove its live lenient-read, manifest, and state-derived consumers.

Purpose: Refuse raw lexical traversal before it can erase physical components, keep effective and checked paths identical afterward, and prevent intermediate-symlink escape before I/O.
Output: One centralized repair with exact typed owner evidence and targeted consumer regressions.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-containment-and-input-safety/02-CONTEXT.md
@.planning/phases/02-containment-and-input-safety/02-RESEARCH.md
@.planning/phases/02-containment-and-input-safety/02-PATTERNS.md
@.planning/phases/02-containment-and-input-safety/02-VALIDATION.md
@.planning/phases/02-containment-and-input-safety/02-01-SUMMARY.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject raw lexical traversal end to end at the shared containment owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/path-safety.ts
    - tests/shared/path-safety.test.ts
  </read_first>
  <files>extensions/pi-claude-marketplace/shared/path-safety.ts, tests/shared/path-safety.test.ts</files>
  <behavior>
    - "PDEF-02 tracer: with root/a as a real symlink, the raw child root/a/../b rejects as LexicalTraversalError; its structured parent and child fields use path.resolve(root) and path.resolve(rawChild), its exact message says lexical traversal is forbidden, and the outside symlink target's complete bytes/tree remain unchanged."
    - "An already absolute child under the normalized root is accepted, including redundant dot segments."
    - "A contained path with a nonexistent leaf remains accepted after existing components are checked."
    - "A contained spelling whose intermediate existing component is a symlink rejects as SymlinkRefusedError with exact normalized parent, child, linkPath, and linkTarget."
  </behavior>
  <action>Write failing owner cases first using one mkdtemp root per test and real filesystem entries. Add a documented LexicalTraversalError that extends PathContainmentError, is thrown by the production raw-segment branch, keeps normalized parent and child fields, sets name to `LexicalTraversalError`, and overrides the inherited escape message with the exact honest text `${label} contains forbidden lexical traversal (parent: ${normalizedParent}, target: ${normalizedChild}).`; this is the smallest traversal-specific member of the existing typed family, so current `instanceof PathContainmentError` consumers continue to work. Split the raw child spelling into path components and detect any segment exactly equal to `..`; retain safe redundant `.` spellings and contained absolute children. As an explicit implementation/code-review invariant, make this raw traversal decision before any path.resolve call, filesystem inspection, or successful return. Once refused, compute normalizedParent and normalizedChild solely for the typed diagnostic and throw without containment or component walking. For an accepted raw spelling, compute the same normalized operands and use them for isPathInside/path.relative, PathContainmentError and SymlinkRefusedError construction, segment derivation, the starting walk root, and every lstat path. Add the decisive real fixture with root/a as a symlink and raw root/a/../b: assert the complete LexicalTraversalError projection and compare the outside target's complete bytes and tree before/after. Do not claim this no-global-patching real-filesystem case observes zero lstat/read calls; the source ordering review proves the pre-filesystem invariant. The component walk is not evidence for this case because normalization erases a; it remains the separate protection for symlinks on accepted lexical paths. Preserve equality, contained absolute paths, redundant-dot paths, first-ENOENT success, existing typed error inheritance/fields, and per-component lstat/readlink behavior. Do not replace the walk with leaf-only realpath, create a second policy, add a test hook, or attempt to close the documented concurrent TOCTOU residual risk.</action>
  <acceptance_criteria>
    - "The real root/a symlink plus raw root/a/../b regression fails before the repair and passes with the exact LexicalTraversalError class/name/message/normalized fields and unchanged outside bytes/tree."
    - "Source review confirms the raw .. decision precedes normalization and filesystem inspection; refused paths normalize only for diagnostics and throw without walking, while accepted paths use one normalized operand pair throughout."
    - "Contained absolute, equality, missing-leaf, first/intermediate/final-symlink, and non-ENOENT behavior remain directly covered."
    - "LexicalTraversalError is a production-used PathContainmentError subclass; SymlinkRefusedError and ordinary outside-root PathContainmentError behavior remain intact."
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/shared/path-safety.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/path-safety.ts</automated>
  </verify>
  <fails_when>Lexical traversal uses the inherited escapes message with normalized-inside fields, the raw traversal decision follows normalization or filesystem work, a refused path reaches containment/walking, any later decision or error uses raw while I/O uses normalized paths, a contained absolute child is rejected, an intermediate symlink is followed, ENOENT becomes failure, tests claim to observe uninstrumented filesystem calls or patch builtins/globals, or direct coverage is below 100%.</fails_when>
  <done>PDEF-02's shared chokepoint rejects raw lexical traversal before normalization and every existing symlink component on accepted paths before I/O while preserving legitimate contained paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove lenient-read, manifest-stage, and state-path consumers inherit the shared repair</name>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
    - tests/orchestrators/plugin/info.test.ts
    - extensions/pi-claude-marketplace/bridges/commands/stage.ts
    - tests/bridges/commands/stage.test.ts
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - tests/persistence/locations.test.ts
    - extensions/pi-claude-marketplace/shared/path-safety.ts
  </read_first>
  <files>tests/orchestrators/plugin/info.test.ts, tests/bridges/commands/stage.test.ts, tests/persistence/locations.test.ts</files>
  <behavior>
    - "PDEF-02: plugin info given a path source that normalizes outside the marketplace root emits its exact existing lenient unreadable/unavailable row and never reads the outside plugin manifest/components."
    - "PDEF-02: command staging refuses a raw lexical traversal, normalized outside path, or intermediate-symlink source/target before copying, and complete target-tree bytes/state remain unchanged."
    - "PDEF-02: each affected locationsFor method preserves valid absolute outputs and rejects a real intermediate symlink before returning a state-derived path."
    - "All live assertPathInside call sites remain routed through the shared owner; AUDIT-007 creates no code or test change."
  </behavior>
  <action>Use CodeGraph to record the complete live assertPathInside caller census at execution time, then add only the owner regressions whose observable Phase 2 behavior changes. In plugin/info.test.ts, construct a case-owned marketplace root plus an outside sentinel and assert the complete public info row/notification before confirming the sentinel was not read or altered. In commands/stage.test.ts, use a real traversal or intermediate symlink at the existing pre-I/O boundary and assert the exact typed failure plus complete source/target bytes and tree state. In locations.test.ts, exercise the state-derived async location methods against case-owned real components and assert exact returned absolute paths or structured typed refusal. Keep production consumers byte-identical unless a live call is discovered to perform I/O before assertPathInside; if that occurs, stop and report the dependency conflict instead of inventing a per-consumer policy. Keep independent test() cases, fresh state, AAA comments, complete values first, and no real home/network/global patching.</action>
  <acceptance_criteria>
    - "The caller census includes plugin/info.ts, bridges/commands/stage.ts, persistence/locations.ts, and every other live caller, with no forked containment helper."
    - "The lenient info flow cannot inspect the outside sentinel and retains its public non-throwing projection."
    - "Command staging performs no outside or target mutation on refusal."
    - "State-derived location methods return exact contained absolute paths and reject real symlink components through the shared error family."
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/info.test.ts tests/bridges/commands/stage.test.ts tests/persistence/locations.test.ts tests/shared/path-safety.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/info.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/commands/stage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/locations.ts</automated>
  </verify>
  <fails_when>A consumer duplicates policy, plugin info reads outside its marketplace root, command stage changes the target on refusal, locations returns a symlink-routed path, expectations are partial/interaction-only, AUDIT-007 re-enters scope, or any changed test's direct pair loses complete coverage.</fails_when>
  <done>PDEF-02 is proven at its single owner and its explicit lenient-read, manifest-stage, and state-derived consumers with hermetic exact evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                         |
| ------------------------------------------------ | ----------------------------------------------------------------------------------- |
| Manifest/state path text → filesystem owner root | Untrusted or persisted path material must remain within its normalized owning root. |
| Checked path → subsequent read/write/return      | Existing symlink components must not redirect later I/O after lexical acceptance.   |

## STRIDE Threat Register

| Threat ID       | Category                           | Component                            | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                 |
| --------------- | ---------------------------------- | ------------------------------------ | -------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-02         | Tampering / Elevation of privilege | assertPathInside lexical containment | high     | mitigate    | Decide raw `..` refusal before normalization/filesystem inspection, normalize refused operands only for an honest LexicalTraversalError diagnostic, and reject normalized outside paths for accepted spellings. |
| T-02-03         | Tampering / Elevation of privilege | assertPathInside component walk      | high     | mitigate    | lstat every existing normalized component and throw SymlinkRefusedError on the first symlink, including intermediates; prove outside bytes/tree remain unchanged.                                               |
| T-02-02-R       | Denial of service                  | legitimate contained absolute paths  | medium   | mitigate    | Preserve equality, contained absolute paths, and missing future leaves with explicit owner and consumer cases.                                                                                                  |
| T-02-02-SC      | Tampering                          | package supply chain                 | low      | accept      | No dependency installation occurs.                                                                                                                                                                              |
| </threat_model> |

## Artifacts this phase produces

- Created symbols: production-used `LexicalTraversalError extends PathContainmentError`; existing `assertPathInside`, `PathContainmentError`, and `SymlinkRefusedError` retain ownership and names.
- Created files: none.
- Created per-consumer policies, schemas, migrations, UI artifacts, external-service configuration, or test-only seams: none.

<verification>
- Run the shared owner and three targeted consumer suites.
- Run direct coverage independently for path-safety and each changed consumer-test pair.
- Run `npm run typecheck` after the wave; final repository validation remains in Plan 02-03.
</verification>

<success_criteria>

- PDEF-02 rejects raw lexical traversal before normalization, normalized outside paths, and every existing symlink component on accepted paths before I/O.
- Legitimate contained absolute paths and missing future leaves still work.
- The lenient info surface stays lenient without escaping containment.
- The specless fallback is closed by owner/consumer automation rather than manual or generic backstop evidence.
  </success_criteria>

<output>
Create `.planning/phases/02-containment-and-input-safety/02-02-SUMMARY.md` when done.
</output>
