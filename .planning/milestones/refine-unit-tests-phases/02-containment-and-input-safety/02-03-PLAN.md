---
phase: 02-containment-and-input-safety
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - extensions/pi-claude-marketplace/index.ts
  - tests/index.test.ts
autonomous: true
requirements: [PDEF-04]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PDEF-04: Aggregate discovery remains fail-loud in orchestrators/discover.ts, while the root resources_discover callback converts its failure to exactly { skillPaths: [], promptPaths: [] }."
    - "PDEF-04: Reconcile and plugin-PATH work completed before discovery failure remains exact and is neither rolled back nor replayed within the failed invocation."
    - "PDEF-04: Removing a transient discovery fault lets the next call through the same registered callback run normally and return exact discovered resources without restart or poisoned state."
    - "PDEF-04: Every skipped-scope warning is attempted independently even when each host notification throws, and no notification failure escapes resources_discover."
    - "The specless PDEF-04 fallback row is resolved by root lifecycle state/result/recovery checks plus the existing fail-loud aggregator owner test."
  artifacts:
    - "extensions/pi-claude-marketplace/index.ts owns the narrow aggregate-discovery containment boundary."
    - "tests/index.test.ts owns exact first-call partial state, empty result, second-call recovery, and independent warning-attempt evidence."
    - "tests/orchestrators/discover.test.ts remains the unchanged direct proof that the aggregator itself fails loudly."
  key_links:
    - "The catch surrounds only aggregateDiscoveredResources and its result projection, after hydrate/reconcile/PATH work."
    - "The fallback is constructed per failed invocation and introduces no persistent failure flag or module state."
    - "The notification try/catch remains inside the skipped-scope loop so one host throw cannot suppress later attempts."
  prohibitions:
    - "Discovery containment cannot roll back/replay completed state, return partial resources, poison the next reload, or suppress later warning attempts."
    - "Tests cannot touch developer state or add test-only seams."
---

<objective>
Correct PDEF-04 at the root resources_discover boundary while preserving exact completed lifecycle progress and clean reload recovery.

Purpose: Keep Pi usable after transient aggregate discovery or host-notification failures without hiding failures from lower-level aggregator callers.
Output: Narrow root containment, two-call recovery evidence, independent warning evidence, and the final Phase 2 repository gate.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-containment-and-input-safety/02-CONTEXT.md
@.planning/phases/02-containment-and-input-safety/02-RESEARCH.md
@.planning/phases/02-containment-and-input-safety/02-PATTERNS.md
@.planning/phases/02-containment-and-input-safety/02-VALIDATION.md
@.planning/phases/02-containment-and-input-safety/02-02-SUMMARY.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Contain one transient aggregate-discovery failure and recover on the next callback</name>
  <read_first>
    - extensions/pi-claude-marketplace/index.ts
    - tests/index.test.ts
    - extensions/pi-claude-marketplace/orchestrators/discover.ts
    - tests/orchestrators/discover.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin-path.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  </read_first>
  <files>extensions/pi-claude-marketplace/index.ts, tests/index.test.ts</files>
  <behavior>
    - "PDEF-04 tracer: the first call through the registered resources_discover callback encounters a real case-owned aggregate discovery filesystem fault and returns exactly { skillPaths: [], promptPaths: [] } without rejecting."
    - "After that first call, exact persisted reconcile state and PATH state/effects match the work completed before aggregation; neither operation is rolled back or invoked twice inside the call."
    - "After the case removes the transient filesystem fault, a second call through the same callback runs the normal lifecycle and returns the complete expected skillPaths and promptPaths."
    - "Calling aggregateDiscoveredResources directly under its owner test continues to reject the same complete failure; containment exists only at the host boundary."
  </behavior>
  <action>Begin with one failing two-invocation root test using createHermeticScope and a real temporary filesystem shape that makes aggregate discovery fail without patching node builtins, globals, the network, or production dependencies. Capture the callback registered by the normal extension factory and invoke that same callback twice. Around only aggregateDiscoveredResources and its result projection in index.ts, add a narrow try/catch that logs through the existing debug seam and returns freshly constructed empty skillPaths/promptPaths. Leave hydrate, applyReconcile, recomputePluginPath, their existing failure boundaries, and the aggregator unchanged. Add no sticky flag, retry inside the failed call, rollback, alternate aggregator, dependency injection, reset export, or test branch. Assert the full return first, then exact persisted configuration/state/PATH effects and exact call/order observations already public through the harness; remove the real transient fault and assert the full second result and state.</action>
  <acceptance_criteria>
    - "The root test fails on the current escaping rejection and passes with a catch around aggregation only."
    - "First-call output is exactly two empty arrays, never a partial aggregate."
    - "Completed reconcile/PATH state survives exactly and is not replayed inside the failed call."
    - "The same callback succeeds on call two after only the case-owned fault is removed."
    - "The discover orchestrator remains byte-identical and fail-loud under its existing owner suite."
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/index.test.ts tests/orchestrators/discover.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts</automated>
  </verify>
  <fails_when>The catch covers reconcile/PATH, the aggregator is changed or silenced, the failure returns partial resources or rejects, completed state is rolled back/replayed, recovery needs restart/reset, a global/builtin is patched, or direct index coverage is below 100%.</fails_when>
  <done>The root lifecycle contains a real transient aggregate failure, preserves prior progress, and recovers through the next normal callback invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove all skipped-scope warnings survive hostile host notifications and seal Phase 2</name>
  <read_first>
    - extensions/pi-claude-marketplace/index.ts
    - tests/index.test.ts
    - tests/orchestrators/plugin-path.test.ts
    - .planning/phases/02-containment-and-input-safety/02-VALIDATION.md
  </read_first>
  <files>tests/index.test.ts</files>
  <behavior>
    - "PDEF-04: with two skipped PATH scopes, a host notification function that throws on every call still records both exact warning attempts in order."
    - "Each notification throw is contained independently and aggregate discovery still returns its exact normal or empty result according to its own outcome."
    - "No warning failure mutates completed reconcile/PATH state or poisons a following resources_discover call."
    - "All Phase 2 changed source-test pairs pass direct coverage and the complete repository gate."
  </behavior>
  <action>Add a sibling root lifecycle case that produces two genuine skipped scopes through case-owned unreadable/invalid state and supplies a host notifier whose boundary records the exact arguments then throws for every attempt. Assert the complete resources result and persisted/PATH state before comparing the entire attempted-notification sequence; prove the second warning occurs despite the first throw and that neither throw escapes. Keep the existing try/catch inside the loop and preserve existing warning text, visibility, debug logging, and notification architecture; this requirement proves containment, not Phase 3+ message redesign. Re-run all three Phase 2 focused groups and every direct changed pair, then run npm run check. Fix only Phase 2 regressions introduced by these plans; report unrelated failures without expanding scope.</action>
  <acceptance_criteria>
    - "Both skipped-scope notifications are attempted with exact message/level/order while both host calls throw."
    - "The callback's complete result and completed state are asserted before notification interactions."
    - "No manual verification, test-only hook, shared mutable fixture, network, real home, or process-global patch is used."
    - "Every changed source-owner pair reaches 100% functions/lines/branches and npm run check passes."
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/bridges/mcp/types.test.ts tests/bridges/mcp/stage.test.ts tests/bridges/mcp/unstage.test.ts tests/shared/path-safety.test.ts tests/orchestrators/plugin/info.test.ts tests/bridges/commands/stage.test.ts tests/persistence/locations.test.ts tests/index.test.ts tests/orchestrators/discover.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/types.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/stage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/mcp/unstage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/path-safety.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/info.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/bridges/commands/stage.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/locations.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; npm run check</automated>
  </verify>
  <fails_when>The first notify throw suppresses the second, a notification escapes, result/state assertions are partial or follow interaction-only proof, tests touch developer/global state, direct coverage is incomplete, or the repository gate fails because of a Phase 2 change.</fails_when>
  <done>PDEF-04 and Phase 2 are sealed by exact root recovery, independent warning attempts, complete direct-pair coverage, and a green repository gate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                       |
| ----------------------------------------------------- | --------------------------------------------------------------------------------- |
| Discovery aggregator → Pi resources_discover callback | A complete lower-level failure must not reject the host lifecycle callback.       |
| Plugin PATH skipped scopes → host notification API    | Host notification code is untrusted and may throw independently for each warning. |

## STRIDE Threat Register

| Threat ID       | Category                        | Component                         | Severity | Disposition | Mitigation Plan                                                                                                                                    |
| --------------- | ------------------------------- | --------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-04         | Denial of service               | resources_discover aggregate call | high     | mitigate    | Catch only aggregate discovery at the root callback, return exact empty paths, preserve earlier state, and prove clean second-call recovery.       |
| T-02-05         | Repudiation / Denial of service | skipped-scope warning loop        | medium   | mitigate    | Retain one try/catch per iteration, log each host throw, and assert all exact warning attempts even when every notification rejects synchronously. |
| T-02-04-R       | Tampering                       | completed reconcile/PATH state    | high     | mitigate    | Keep containment after completed operations and assert exact persisted/PATH state plus absence of rollback or same-call replay.                    |
| T-02-04-SC      | Tampering                       | package supply chain              | low      | accept      | No dependency installation occurs.                                                                                                                 |
| </threat_model> |

## Artifacts this phase produces

- Created symbols: none; the existing extension factory and callback retain their public surface.
- Created files: none.
- Created retry state, reset hooks, dependency seams, schemas, migrations, UI artifacts, or external-service configuration: none.

<verification>
- Run the focused lifecycle and fail-loud aggregator suites after each behavior task.
- Run direct coverage for index.ts and every earlier Phase 2 changed pair.
- Run final `npm run check`; no manual-only verification remains.
</verification>

<success_criteria>

- PDEF-04 returns exact empty discovery on aggregate failure without losing completed progress.
- A second callback succeeds after the transient case-owned fault is removed.
- Every skipped-scope warning remains independently attempted under hostile notification behavior.
- PDEF-02, PDEF-03, and PDEF-04 all pass focused, direct-coverage, type/style, and complete repository gates.
- The specless fallback is closed by explicit automated owner/consumer predicates, not backstop or manual verification.
  </success_criteria>

<output>
Create `.planning/phases/02-containment-and-input-safety/02-03-SUMMARY.md` when done.
</output>
