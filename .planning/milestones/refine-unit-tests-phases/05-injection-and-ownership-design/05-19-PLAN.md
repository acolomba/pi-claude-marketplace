---
phase: 05-injection-and-ownership-design
plan: 19
type: execute
wave: 13
depends_on: [05-12, 05-18]
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - tests/edge/register.test.ts
  - tests/edge/handlers/marketplace/add.test.ts
  - tests/orchestrators/marketplace/add.test.ts
autonomous: true
requirements: [TREF-04, TREF-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Registered marketplace add receives the root CompletionCache as a required value and forwards that same instance into the real add orchestrator (D-05-05)."
    - "After durable add commit, names schema-2 invalidation occurs before plugin-index drop on the supplied cache; no pre-commit/rollback/no-effect path invalidates."
    - "The next public completion read through the same extension rebuilds authoritative rows, while a peer cache remains isolated."
    - "Names-file and plugin-index invalidation failures retain the exact existing swallowed hygiene policy and never rewrite committed state (D-05-06)."
    - "Source classification, validation, containment, clone/config/state/scope tree, results, errors, warnings, and notifications remain exact (D-05-01)."
    - "bootstrapClaudePlugin still calls real add then autoupdate with full state/configuration/tree/notification evidence; it gains no bespoke seam (D-05-02)."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
    - tests/edge/register.test.ts
    - tests/edge/handlers/marketplace/add.test.ts
    - tests/orchestrators/marketplace/add.test.ts
  key_links:
    - "root CompletionCache -> registered add handler -> real add orchestrator"
    - "durable add commit -> schema-2 names invalidation -> plugin-index drop -> next same-instance completion rebuild"
    - "bootstrap real child -> marketplace add -> unchanged autoupdate composition and exact observations"
  prohibitions:
    - "No __deps, ForTest, test-only reset/export, optional/default/dead cache port, second cache lifetime, or broad invalidation bundle."
    - "No weakening/broadening of applyReconcile/bootstrapClaudePlugin composition exceptions."
    - "No Phase 6 patch/split work, excluded stage/PID/info/uninstall decomposition, or new Fallow suppression."
---

<objective>
Route marketplace add from edge registration through the lifecycle cache; invalidate schema-2 names then plugin-index data after durable commit and prove exact next-read rebuild, isolated peer caches, swallowed hygiene failures, state/config/tree, and notifications (D-05-01, D-05-05, D-05-06).

Purpose: Make the next completion authoritative without changing add transaction, hygiene, state, tree, result, or notification behavior.
Output: Required edge/add cache wiring and exact same-instance invalidation, rebuild, isolation, failure, and public-effect evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-18-SUMMARY.md
</context>

## Edge assumptions

- Use Plan 05-12's executed CompletionCache contract and root/edge dependency identifiers.
- Plan 05-08 decided the marketplace-name memory disposition. Preserve schema 2, scoped names path, and real file invalidation in either executed branch.
- Add invalidation is a post-commit hygiene sequence: names first, plugin index second. Failures keep the current silent/swallowed policy.
- Ordinary filesystem, clone, validation, containment, and state behavior stays real. The cache is not a general transaction port.
- bootstrapClaudePlugin remains a real add-plus-autoupdate full-observation exception. Run its owner read/verify-only.
- Preserve one cache lifecycle, Phase 6 machinery, exact Fallow comment, and excluded stage/PID/info/uninstall work.

## Artifacts this plan produces

| File family                            | Contract or proof                                                                                                |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Edge register/add handler and owners   | Required lifecycle cache forwarding with exact public handler behavior.                                          |
| Marketplace add orchestrator and owner | Post-commit names/plugin invalidation, same-instance completion rebuild, and exact transaction/hygiene evidence. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the lifecycle cache through registered marketplace add</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts, tests/edge/register.test.ts, tests/edge/handlers/marketplace/add.test.ts, extensions/pi-claude-marketplace/edge/types.ts, tests/edge/completions/data.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts, tests/edge/register.test.ts, tests/edge/handlers/marketplace/add.test.ts</files>
  <behavior>
    - "The registered add handler requires and forwards the same CompletionCache used by command completions."
    - "Source/scope/autoupdate parsing, accepted/rejected/no-effect behavior, results, errors, and notifications remain exact."
    - "No handler-local cache, optional/default selector, or invalidation-only substitute exists."
    - "A successful add through the public handler changes the next same-extension completion while a peer extension remains isolated."
  </behavior>
  <action>Extend edge registration and the marketplace add handler to require and forward the root CompletionCache into the real add orchestrator. Preserve parser, source normalization/classification, scope/autoupdate selection, result mapping, registration order, errors, warnings, and notification behavior. Add handler/registration cases that seed completion state, perform real add, then assert the complete next public completion rows through the same extension and unchanged peer-cache rows. Assert public outcomes before forwarding evidence. Do not construct a cache in the handler or introduce an invalidation-only dependencies object. Run bootstrap owner read/verify-only and preserve its real child calls.</action>
  <verify><automated>node --test tests/edge/register.test.ts tests/edge/handlers/marketplace/add.test.ts tests/orchestrators/marketplace/add.test.ts tests/edge/completions/data.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/add.ts tests/edge/register.test.ts tests/edge/handlers/marketplace/add.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; cache is optional, defaulted, dead, bypassed, or created in handler; same-instance completion stays stale; peer cache changes; parsing/results/errors/notifications drift; bootstrap is replaced or changed; or another lifetime/deferred work appears.</fails_when></verify>
  <acceptance_criteria>Registered marketplace add uses the lifecycle cache and proves authoritative same-instance completion plus peer isolation through public behavior (D-05-05, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">The cache now crosses public add and bootstrap's real child path, so changes require coordinated edge, orchestrator, completion, and composition evidence.</reversibility>
  <done>The public add route reaches the root cache and exposes committed additions on the next same-extension completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve post-commit names and plugin-index invalidation order</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-12-SUMMARY.md, extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, tests/orchestrators/marketplace/add.test.ts, extensions/pi-claude-marketplace/edge/register.ts, tests/edge/register.test.ts, tests/shared/completion-cache.test.ts, tests/edge/completions/data.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, tests/edge/register.test.ts, tests/orchestrators/marketplace/add.test.ts</files>
  <behavior>
    - "Only durable successful add invalidates, in exact order: scoped names file/memory disposition, then target plugin-index memory/file."
    - "Pre-commit, rollback, duplicate/no-effect, and failed add paths do not invalidate or change completion rows."
    - "Each invalidation hygiene failure follows current swallowed behavior and leaves exact committed state/configuration/clone tree and notification."
    - "The next same-cache read rebuilds exact schema-2 names and schema-6 plugin rows; unrelated and peer-cache entries remain exact."
  </behavior>
  <action>Replace any module-level add invalidators with operations on the required lifecycle CompletionCache. Keep names invalidation before plugin-index drop and execute both only after the existing durable state/configuration/clone commit. Preserve the current independent swallowed-hygiene behavior for either failure, including continued later hygiene if current semantics require it. Do not move validation, source resolution, clone, lock, persistence, result, or notification behavior into the cache. Add owner cases for success, duplicate/no-effect, pre-commit failure, rollback, each invalidation failure, unrelated targets, peer cache isolation, and exact next-read rows/bytes. Preserve complete state/configuration/scope-tree and notification assertions, plus bootstrap's unchanged real composition.</action>
  <verify><automated>node --test tests/orchestrators/marketplace/add.test.ts tests/edge/register.test.ts tests/edge/handlers/marketplace/add.test.ts tests/shared/completion-cache.test.ts tests/edge/completions/data.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts tests/orchestrators/marketplace/add.test.ts extensions/pi-claude-marketplace/edge/register.ts tests/edge/register.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; invalidation precedes commit, changes order, touches failed/unrelated/peer targets, or aborts committed behavior contrary to current hygiene policy; schema/rows/state/config/tree/results/errors/notifications drift; bootstrap weakens; or a forbidden seam/suppression/split appears.</fails_when></verify>
  <acceptance_criteria>Marketplace add invalidates the supplied cache only after commit, in exact names-then-plugin order, and preserves exact rebuild, isolation, failure, state, tree, and notification behavior (D-05-01, D-05-05, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">This establishes cross-operation cache freshness used by completions and bootstrap, so changing order or identity affects multiple public flows.</reversibility>
  <done>Committed marketplace add refreshes only the owning cache with exact invalidation order and unchanged transaction/hygiene semantics.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                      | Description                                                                                    |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Marketplace source -> clone/state transaction | User-controlled source and manifest data cross validation, containment, and durable storage.   |
| Durable add -> lifecycle cache                | Early, missed, or wrong-instance invalidation can expose nonexistent or stale completion data. |
| Cache files -> completion rows                | Schema-2 and schema-6 bytes must be validated and rebuilt for the correct scope/marketplace.   |

## STRIDE threat register

| Threat ID | Category                           | Component                      | Severity | Disposition | Mitigation plan                                                                                      |
| --------- | ---------------------------------- | ------------------------------ | -------- | ----------- | ---------------------------------------------------------------------------------------------------- |
| T-05-81   | Tampering / Elevation of privilege | add validation/clone           | high     | mitigate    | Preserve source classification, containment, manifest checks, lock, and exact real-tree behavior.    |
| T-05-82   | Tampering                          | invalidation eligibility/order | high     | mitigate    | Invalidate only after durable commit, names before plugin index, with exact failure/no-effect cases. |
| T-05-83   | Tampering / Information disclosure | cache identity/isolation       | high     | mitigate    | Require root cache and prove same-instance rebuild plus peer/unrelated isolation.                    |
| T-05-84   | Repudiation                        | results and notifications      | medium   | mitigate    | Preserve exact state/configuration/tree, rows, errors, warnings, and notification bytes.             |
| T-05-85   | Denial of service                  | hygiene failures               | medium   | mitigate    | Retain existing swallowed policy and independent cleanup progression after commit.                   |

ASVS Level 1: source/clone integrity, post-commit eligibility, and cache identity are high-severity blocking threats.
</threat_model>

## Source coverage audit

| Source   | ID                           | Feature or constraint                                  | Plan/task         | Status  | Notes                                                      |
| -------- | ---------------------------- | ------------------------------------------------------ | ----------------- | ------- | ---------------------------------------------------------- |
| GOAL     | -                            | Lifecycle-owned cache with public contracts            | 05-19 / Tasks 1-2 | COVERED | Real add changes next same-instance completion.            |
| REQ      | TREF-04                      | Production-owned seam and real ordinary behavior       | 05-19 / Task 2    | COVERED | Cache is required; filesystem/clone stays real.            |
| REQ      | TREF-05                      | One cache owns read/invalidation state                 | 05-19 / Tasks 1-2 | COVERED | Root instance reaches add and completion.                  |
| RESEARCH | marketplace add invalidation | Names then plugin drop after commit                    | 05-19 / Task 2    | COVERED | Exact order, failure, rows, and peer isolation are tested. |
| CONTEXT  | D-05-01, D-05-05, D-05-06    | Required cache and genuine post-commit invalidation    | 05-19 / Tasks 1-2 | COVERED | No optional/default/dead or second cache exists.           |
| CONTEXT  | D-05-02, D-05-03, D-05-09    | Preserve bootstrap composition and deferred boundaries | 05-19 / Tasks 1-2 | COVERED | Real add child stays intact and Phase 6 work stays out.    |

Marketplace remove and update follow in Plans 05-20 and 05-21. Plugin invalidators and terminal cleanup remain later. Exclusions are not gaps.

<verification>
Run both task commands and `npm run check`. Confirm only the six frontmatter files change, each task changes four files, the root cache is required, invalidation is post-commit names-then-plugin, exact rebuild/isolation/hygiene behavior remains, bootstrap stays full-observation, and the Fallow comment remains byte-exact.
</verification>

<success_criteria>

- Registered marketplace add and real orchestration use the lifecycle cache.
- Durable success invalidates names then plugin-index data; ineligible paths do not invalidate.
- Same-instance next reads rebuild exact rows while peer/unrelated caches remain unchanged.
- State/configuration/clone tree, schemas, errors, warnings, hygiene, results, and notifications remain exact.
- No forbidden seam, second lifetime, weakened composition, suppression, Phase 6 work, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-19-SUMMARY.md` when done.
</output>
