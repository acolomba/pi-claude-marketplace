---
phase: 05-injection-and-ownership-design
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
  - tests/orchestrators/plugin/fetch.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/orchestrators/plugin/info.test.ts
autonomous: true
requirements: [TREF-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "OPEF-F04 has exactly one terminal classification: a required fetch-owned status capability controls pre/post materialization visibility (D-05-01)."
    - "OPIC-F27 has exactly one terminal classification: a required info-owned read/list capability controls targeted irreproducible read failures (D-05-01)."
    - "Fetch and info keep separate contracts; neither gains a general filesystem bag, optional adapter, or test-only/dead seam."
    - "Fetch renders fresh post-materialization status and preserves clone, credentials, trees, results, and notifications."
    - "Info remains cohesive and network-free while preserving containment, reason order, inventory order, diagnostics, and notification bytes (D-05-09)."
    - "Phase 6 retains builtin patch removal and the excluded info split (D-05-03, D-05-09)."
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
      provides: "Required fetch-status capability and explicit real production binding"
    - path: tests/orchestrators/plugin/fetch.test.ts
      provides: "Fresh post-materialization status and exact public-effect proof"
    - path: extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
      provides: "Required info read/list capability without module split"
    - path: tests/orchestrators/plugin/info.test.ts
      provides: "Targeted failures plus real-tree public inventory proof"
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts
      via: "required status capability around existing presence and manifest classifications"
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
      to: extensions/pi-claude-marketplace/shared/path-safety.ts
      via: "containment remains in the consumer before required UTF-8 reads and directory listings"
---

<objective>
Classify OPEF-F04 and OPIC-F27 through distinct required fetch-status and plugin-info capabilities (D-05-01, D-05-03, D-05-09).

Purpose: Replace hidden global control of two irreproducible read boundaries while retaining fresh status, contained inventory reads, and exact public output.
Output: Two consumer-owned contracts, explicit real adapters, and focused owner regressions.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-01-SUMMARY.md
</context>

## Edge assumptions

- OPEF-F04 authorizes status visibility control, not clone, credential, notification, or tree mutation injection.
- OPIC-F27 authorizes UTF-8 reads and directory listings only. Ordinary missing/corrupt trees stay on real temporary filesystems.
- Bare plugin info remains network-free. The independently deferred info split remains outside Phase 5.
- The contracts must be production-used and required before Phase 6 removes builtin patching.

## Artifacts this plan produces

| File family                               | Contract or proof                                                                               |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `orchestrators/plugin/fetch.ts` and owner | Required status capability, explicit real binding, and pre/materialize/post order evidence.     |
| `orchestrators/plugin/info.ts` and owner  | Required read/list capability, retained cohesion, and exact contained inventory/error evidence. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace fresh post-materialization status through plugin fetch</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, tests/orchestrators/plugin/fetch.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts, .planning/phases/05-injection-and-ownership-design/05-RESEARCH.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, tests/orchestrators/plugin/fetch.test.ts</files>
  <behavior>
    - "The fetch-owned capability contains only existing source-presence and manifest-entry classification operations."
    - "Production requires and binds the real status capability; the public fetch export traverses the same factory as controlled cases."
    - "Order remains presence probe, conditional materialization, fresh manifest classification, conditional reason probe, and exact notification."
    - "A source that becomes visible during materialization renders from the fresh status, while public rows, tree bytes, credentials, and notifications remain exact."
  </behavior>
  <action>Per D-05-01, define a readonly fetch-status capability from the existing source-presence and manifest-entry classification operations. Require it in a production-used fetch factory and compose the public fetch export from an explicit real adapter. Thread the capability only through the cold/no-op gate, post-materialization refresh, and reason-bearing resolution. Add an OPEF-F04 case whose local capability records pre/materialize/post order and changes status after the real temporary clone tree is materialized. Assert the complete result row, staging and clone trees, credentials behavior, exact notification, and failure identity before the call log. Keep auth, Git, staging, and notification ports separate. Retain Phase 6 builtin synchronization machinery per D-05-03.</action>
  <verify><automated>node --test tests/orchestrators/plugin/fetch.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts tests/orchestrators/plugin/fetch.test.ts --max-warnings=0</automated><fails_when>The owner discovers zero tests or fails; the capability is optional, broad, dead, shared with info, or bypassed by production; pre/materialize/post order or fresh rendering changes; result, clone/staging bytes, credentials behavior, errors, or notifications weaken; or a new suppression or Phase 6 cleanup appears.</fails_when></verify>
  <acceptance_criteria>OPEF-F04 has one required fetch-owned classification, and fresh post-materialization status remains proven through complete public filesystem and notification effects (D-05-01).</acceptance_criteria>
  <reversibility rating="costly">Phase 6 uses this production contract to remove hidden builtin control, so contract changes require coordinated fetch and owner migration.</reversibility>
  <done>Plugin fetch uses one required real status capability and renders the authoritative post-materialization result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate plugin-info reads without splitting the workflow</name>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts, extensions/pi-claude-marketplace/shared/path-safety.ts, extensions/pi-claude-marketplace/persistence/locations.ts, .planning/phases/05-injection-and-ownership-design/05-CONTEXT.md</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts</files>
  <behavior>
    - "The info-owned capability contains only UTF-8 text reading and directory-entry listing with current Node result shapes."
    - "Production requires and binds the real reader; containment, parsing, error classification, rendering, and notification stay in info."
    - "ENOENT and ENOTDIR preserve current collapse behavior; selected non-benign failures retain exact identity and reason placement."
    - "Bare info remains network-free and preserves exact hook summary, component inventory order, diagnostics, and notification bytes."
  </behavior>
  <action>Per D-05-01, define a readonly plugin-info reader for the current UTF-8 read and directory-list operations. Require it in a production-used info factory and compose the public info export from explicit Node operations. Route only the existing direct file and directory reads through it. Keep path derivation, containment, parsing, missing/corrupt classification, inventory construction, reason order, rendering, and notifications in the cohesive info module. Add OPIC-F27 cases for selected read and list failures, while ordinary missing, corrupt, and populated trees continue through the real export and temporary filesystem. Assert complete returned inventory, exact reasons, debug diagnostics, tree safety, and notifications. Per D-05-09, do not extract info helpers or add network behavior. Per D-05-03, retain Phase 6 patch machinery.</action>
  <verify><automated>node --test tests/orchestrators/plugin/info.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/info.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/info.ts tests/orchestrators/plugin/info.test.ts --max-warnings=0</automated><fails_when>The owner discovers zero tests or fails; the reader is optional, broader than read/list, dead, or bypassed; containment, missing/corrupt classification, reason/component order, diagnostics, or notification bytes change; bare info reaches network; the module is split; or deferred Phase 6 work appears.</fails_when></verify>
  <acceptance_criteria>OPIC-F27 has one required info-owned read/list classification, and the cohesive no-network workflow retains exact public inventory, failure, and notification behavior (D-05-01, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">The production reader becomes Phase 6's patch-removal seam while the module remains cohesive, so changing it retouches owner and production composition.</reversibility>
  <done>Plugin info exposes only its legitimate read/list boundary and keeps every ordinary path on real case-owned state.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                                          |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Clone cache to fetch status         | Filesystem visibility determines materialization eligibility and the user-visible fetch status.                      |
| Plugin tree to info renderer        | Paths, file contents, and directory entries become user-visible inventory only after containment and classification. |
| Required capability to orchestrator | Controlled reads can force one authorized edge but cannot own mutation, rendering, notification, or unrelated I/O.   |

## STRIDE Threat Register

| Threat ID | Category                           | Component             | Severity | Disposition | Mitigation Plan                                                                                                          |
| --------- | ---------------------------------- | --------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-05-10   | Tampering / Denial of service      | fetch status sequence | high     | mitigate    | Preserve pre/materialize/post order and prove the rendered row comes from fresh real-tree state.                         |
| T-05-11   | Information disclosure / Tampering | plugin-info reads     | high     | mitigate    | Keep containment in the consumer, limit the reader to text/list operations, and retain exact closed-set error rendering. |
| T-05-12   | Tampering                          | adapter composition   | medium   | mitigate    | Require explicit real adapters and fail owner/type/direct-pair checks if production bypasses either factory.             |
| T-05-13   | Repudiation                        | user-visible reasons  | low      | mitigate    | Assert complete rows, reason ordering, diagnostics, and exact notification bytes.                                        |

ASVS Level 1: the high-severity freshness and path-disclosure threats block completion. This plan changes no authentication, schema, package, or external-service contract.
</threat_model>

## Source coverage audit

| Source   | ID                 | Feature or constraint                                                          | Plan/task         | Status  | Notes                                                                      |
| -------- | ------------------ | ------------------------------------------------------------------------------ | ----------------- | ------- | -------------------------------------------------------------------------- |
| GOAL     | -                  | Legitimate production seams with public behavior                               | 05-03 / Tasks 1-2 | COVERED | Fetch and info receive separate required contracts with complete outcomes. |
| REQ      | TREF-04            | Classify hidden dependencies and preserve ordinary real-state tests            | 05-03 / Tasks 1-2 | COVERED | OPEF-F04 and OPIC-F27 are each classified once.                            |
| RESEARCH | eleven-root table  | Fetch status race and targeted info read/list failures                         | 05-03 / Tasks 1-2 | COVERED | Each root keeps its current source/test owner and minimum authority.       |
| RESEARCH | composition safety | Production uses explicit adapters; public results precede interaction evidence | 05-03 / Tasks 1-2 | COVERED | Direct owner commands enforce forwarding and exact effects.                |
| CONTEXT  | D-05-01            | Use narrow ports only for irreproducible behavior                              | 05-03 / Tasks 1-2 | COVERED | Ordinary tree behavior stays real.                                         |
| CONTEXT  | D-05-03            | Phase 6 owns builtin patch removal                                             | 05-03 / Tasks 1-2 | COVERED | Current synchronization code remains.                                      |
| CONTEXT  | D-05-09            | Do not execute the info or approved large-module splits                        | 05-03 / Task 2    | COVERED | Info remains in its current file and source-test pair.                     |

The remaining nine roots are assigned exactly once to Plans 05-01, 05-02, 05-04, and 05-05. Runtime/cache ownership and terminal cleanup remain assigned to later outline plans. Excluded Phase 6 work is not an audit gap.

<verification>
Run both task commands and `npm run check`. Confirm the four frontmatter files are the only modified files, both capabilities are required and production-used, ordinary filesystem cases remain real, the info workflow stays cohesive and network-free, no suppression is added, and Phase 6 machinery remains intact. Confirm `scripts/revalidation.mjs` retains the exact existing Fallow line.
</verification>

<success_criteria>

- OPEF-F04 and OPIC-F27 each have one and only one terminal classification.
- Required production adapters and controlled tests use the same consumer factories.
- Fetch freshness, trees, results, errors, info containment, inventory, reasons, diagnostics, and notifications remain exact.
- No shared/broad/optional/default/test-only/dead port, info split, new suppression, or Phase 6 patch cleanup appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-03-SUMMARY.md` when done.
</output>
