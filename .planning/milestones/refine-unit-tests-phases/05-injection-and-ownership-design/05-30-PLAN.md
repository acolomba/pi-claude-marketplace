---
phase: 05-injection-and-ownership-design
plan: 30
type: execute
wave: 22
depends_on: [05-29]
files_modified:
  - tests/architecture/flag-catalog-drift.test.ts
  - tests/edge/completions/data.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/orchestrators/marketplace/add.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
autonomous: true
requirements: [TREF-05, TREF-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every remaining completion-reset caller uses a fresh per-case CompletionCache or command fixture after all mutation wiring is complete (D-05-05, D-05-06)."
    - "Cache data/provider tests retain exact bytes, TTL, poison, recovery, ordering, and invalidation behavior without process-global cleanup."
    - "Marketplace and plugin mutation tests retain exact public rows, state/tree, result, warning, error, cleanup, and notification effects."
    - "Production reset definitions remain until Plan 05-31 proves a zero-caller census and performs terminal deletion (D-05-07)."
    - "The independent literal command-catalog pin remains unchanged for Plan 05-32 (D-05-08)."
  artifacts:
    - tests/architecture/flag-catalog-drift.test.ts
    - tests/edge/completions/data.test.ts
    - tests/edge/completions/provider.test.ts
    - tests/orchestrators/marketplace/add.test.ts
    - tests/orchestrators/marketplace/update.test.ts
    - tests/orchestrators/plugin/install.test.ts
    - tests/orchestrators/plugin/reinstall.test.ts
    - tests/orchestrators/plugin/uninstall.test.ts
  key_links:
    - "fresh cache/command fixture -> public completion provider -> exact cache bytes and rows"
    - "durable marketplace/plugin mutation -> cache invalidation -> authoritative next completion"
    - "post-task caller census -> Plan 05-31 terminal deletion gate"
  prohibitions:
    - "No production edit, early reset deletion, replacement reset alias, shared cache fixture, __deps, ForTest, raw state access, or optional/default/dead port."
    - "No BOOLEAN_FLAGS export/import cleanup or independent literal catalog-pin change before Plan 05-32."
    - "No Phase 6 work, excluded-file edit, weakened assertion, or new Fallow suppression."
---

<objective>
Migrate the complete completion-reset caller census to fresh per-case CompletionCache/command fixtures after mutation wiring is complete; serialize overlapping plugin tests after hooks migration and preserve exact cache bytes, TTL, poison, invalidation, public rows, and mutation effects (D-05-05, D-05-06, D-05-07).

Purpose: Remove the final test callers of global completion cleanup without racing lifecycle migrations or changing public completion/mutation contracts.
Output: Eight reset-free completion, marketplace, plugin, and catalog suites ready for safe production reset deletion.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-29-SUMMARY.md
</context>

## Edge assumptions

- Repeat the full resetCompletionCache caller census before editing and map every genuine caller to one of these eight suites; halt on any unassigned caller.
- Plans 05-22 through 05-25 already wire operation-specific invalidation. Their production owners are read/verify-only here.
- Create the CompletionCache or complete command composition inside each case. Do not share cache instances through module/suite setup.
- Preserve cold/warm/expired/poisoned cache bytes, TTL boundaries, invalidation timing, public rows, and exact mutation side effects.
- Keep production reset definitions until Plan 05-31 reruns the census. Keep the independent literal command-catalog pin unchanged until Plan 05-32.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Migrate completion, marketplace, and catalog reset callers</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-29-SUMMARY.md, tests/architecture/flag-catalog-drift.test.ts, tests/edge/completions/data.test.ts, tests/edge/completions/provider.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts, extensions/pi-claude-marketplace/shared/completion-cache.ts, extensions/pi-claude-marketplace/edge/completions/data.ts, extensions/pi-claude-marketplace/edge/completions/provider.ts, tests/shared/completion-cache.test.ts</read_first>
  <files>tests/architecture/flag-catalog-drift.test.ts, tests/edge/completions/data.test.ts, tests/edge/completions/provider.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <behavior>
    - "Each case constructs a fresh CompletionCache or command fixture and never imports/calls the global completion reset."
    - "Completion data/provider cases preserve exact cold, warm, TTL-expired, poisoned, recovered, ordered, filtered, and byte-level outcomes."
    - "Marketplace add/update cases preserve exact commit eligibility, invalidation timing, state/tree, public rows, results, errors, warnings, and notifications."
    - "The architecture suite's independent literal catalog pin is unchanged; only its reset-based setup may migrate."
  </behavior>
  <action>Record the complete completion-reset caller census, then replace reset-based setup in the completion data/provider and marketplace add/update suites with a fresh CompletionCache or full command fixture inside every case. Pass owners only through the production constructors and registered public commands established by earlier plans. Preserve exact serialized cache bytes, timestamps and TTL boundaries, poison/recovery cases, filtering/order, pre-commit and rollback ineligibility, post-commit invalidation, state/configuration/scope trees, rows, errors, warnings, results, cleanup, and notifications. In the architecture suite, migrate only lifecycle setup needed to remove its reset call; leave the independent literal list-command flag pin byte-for-byte unchanged for D-05-08. Keep production definitions for Plan 05-31 and do not edit production or stable owner suites.</action>
  <verify><automated>node --test tests/architecture/flag-catalog-drift.test.ts tests/edge/completions/data.test.ts tests/edge/completions/provider.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/shared/completion-cache.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/architecture/flag-catalog-drift.test.ts tests/edge/completions/data.test.ts tests/edge/completions/provider.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; a reset import/call remains in these five files; any case shares a cache owner; cache bytes/TTL/poison/recovery/order or marketplace commit/state/tree/rows/result/error/warning/notification assertions weaken; the independent literal catalog pin changes; production/stable owner files change; or a seam/suppression/Phase 6 change appears.</fails_when></verify>
  <acceptance_criteria>Five suites isolate completion state by construction while retaining exact cache and marketplace behavior, and the independent literal pin remains intact (D-05-05, D-05-06, D-05-08).</acceptance_criteria>
  <reversibility rating="reversible">The change is confined to test fixture ownership and preserves production/public contracts.</reversibility>
  <done>Completion, marketplace, and catalog cases use fresh owners with exact behavior and no reset caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate plugin mutation reset callers and close the caller census</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-22-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-24-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-25-SUMMARY.md, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/shared/completion-cache.ts, tests/integration/transaction-lifecycle-cascade.test.ts</read_first>
  <files>tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <behavior>
    - "Install, reinstall, and uninstall cases each receive a fresh registered command/cache graph without global cleanup."
    - "Pre-commit and rollback paths do not invalidate; durable success invalidates exactly once and the next public completion is authoritative."
    - "Exact state/configuration, project/user trees, rows, results, errors, warnings, cleanup, and notifications remain unchanged."
    - "A repository-wide post-task census finds no genuine reset caller; only the production definition/export remains for Plan 05-31."
  </behavior>
  <action>After Task 1, migrate plugin install, reinstall, and uninstall tests to fresh registered command/cache graphs per case, using the operation-specific composition produced by Plans 05-22, 05-24, and 05-25. Preserve the full public contract: eligibility boundaries, durable ledger/state timing, rollback and compensation, invalidation count/order, authoritative next completion rows, project/user trees, result envelopes, exact errors/warnings, cleanup, and notifications. Keep the transaction integration suite read/verify-only because Plan 05-29 already migrated it. Rerun a repository-wide semantic and text caller census; fail on every genuine remaining caller or duplicated cleanup behavior. Leave the production definition/export untouched for Plan 05-31.</action>
  <verify><automated>node --test tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/integration/transaction-lifecycle-cascade.test.ts tests/shared/completion-cache.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/uninstall.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; a reset caller remains outside the production definition/export; a plugin case shares a cache graph; invalidation eligibility/order or state/tree/rows/result/error/warning/cleanup/notification behavior weakens; production/integration/cache-owner files change; a replacement cleanup appears; or a suppression/Phase 6/excluded change appears.</fails_when></verify>
  <acceptance_criteria>All plugin mutation tests are reset-free, exact public invalidation behavior is preserved, and the final caller census is zero before deletion (D-05-05, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">The test-only fixture migration can be reverted without changing production behavior.</reversibility>
  <done>All eight scoped suites are reset-free and Plan 05-31 has a proven zero-caller deletion precondition.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                               | Description                                                                   |
| -------------------------------------- | ----------------------------------------------------------------------------- |
| Test case -> CompletionCache owner     | Shared state can leak rows, poison, timestamps, or invalidation across cases. |
| Durable mutation -> cache invalidation | Invalidating before commit or on rollback can expose false public state.      |
| Caller migration -> production reset   | Early deletion or an incomplete census can break hidden consumers.            |

## STRIDE threat register

| Threat ID | Category                           | Component                           | Severity | Disposition | Mitigation plan                                                                        |
| --------- | ---------------------------------- | ----------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------- |
| T-05-130  | Tampering / Information disclosure | completion fixture isolation        | high     | mitigate    | Construct fresh owners per case and retain poison/TTL/byte-level assertions.           |
| T-05-131  | Tampering / Repudiation            | plugin and marketplace invalidation | high     | mitigate    | Assert no pre-commit/rollback invalidation and exact post-commit public results.       |
| T-05-132  | Denial of service                  | caller census and deletion ordering | high     | mitigate    | Require repository-wide zero-caller evidence while leaving definitions for Plan 05-31. |
| T-05-133  | Tampering                          | independent catalog pin             | medium   | mitigate    | Keep the literal catalog contract unchanged until its dedicated cleanup plan.          |

ASVS Level 1: cache isolation, durable mutation integrity, deletion ordering, and public contract preservation are blocking controls.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                   | Plan/task         | Status  | Notes                                                                 |
| -------- | ------------------------- | ------------------------------------------------------- | ----------------- | ------- | --------------------------------------------------------------------- |
| GOAL     | -                         | Correctly owned runtime state and public contracts      | 05-30 / Tasks 1-2 | COVERED | All remaining completion callers isolate by construction.             |
| REQ      | TREF-05                   | Completion state belongs to command/lifecycle owners    | 05-30 / Tasks 1-2 | COVERED | Fresh owners replace global cleanup.                                  |
| REQ      | TREF-06                   | Public completion and mutation behavior remains exact   | 05-30 / Tasks 1-2 | COVERED | Bytes, rows, trees, results, errors, and notifications stay asserted. |
| RESEARCH | reset caller census       | Migrate all callers before terminal deletion            | 05-30 / Tasks 1-2 | COVERED | Eight files are partitioned 5+3.                                      |
| CONTEXT  | D-05-05, D-05-06, D-05-07 | Per-command cache, genuine transitions, ordered removal | 05-30 / Tasks 1-2 | COVERED | Plan 05-31 alone deletes definitions.                                 |
| CONTEXT  | D-05-08                   | Retain independent literal catalog pin                  | 05-30 / Task 1    | COVERED | Pin remains unchanged for Plan 05-32.                                 |

Terminal reset deletion remains Plan 05-31, BOOLEAN_FLAGS cleanup remains Plan 05-32, and final exact audits remain Plan 05-33. Phase 6 and exclusions are not gaps.

<verification>
Run both task commands. Confirm only the eight test files change, task scopes are five and three files, every case owns a fresh cache/command fixture, no genuine completion-reset caller remains, and production reset definitions still exist. Confirm exact cache/mutation/public assertions and the independent literal pin remain intact, the Fallow comment is exactly `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor`, and no new suppression or Phase 6/excluded work appears.
</verification>

<success_criteria>

- All eight scoped suites use fresh CompletionCache/command owners and no global completion cleanup.
- Cache bytes, TTL, poison/recovery, invalidation eligibility/order, rows, state/trees, results, errors, warnings, cleanup, and notifications remain exact.
- The repository-wide caller census is zero while the production definition/export remains available for Plan 05-31.
- The independent literal catalog pin is unchanged.
- No production edit, new seam, shared fixture, early deletion, suppression, Phase 6 work, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-30-SUMMARY.md` when done.
</output>
