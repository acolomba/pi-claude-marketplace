---
phase: 05-injection-and-ownership-design
plan: 15
type: execute
wave: 9
depends_on: [05-05, 05-14]
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - tests/edge/register.test.ts
  - tests/edge/handlers/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Direct uninstall receives the root HooksRuntime through required edge plumbing and the cohesive Plan 05-05 uninstall transaction port (D-05-01, D-05-04, D-05-09)."
    - "Configuration removal precedes state persistence, partial cascade state remains exact, and route removal follows the existing committed boundary."
    - "Pre-commit and rollback failures retain routes; post-commit cleanup cannot restore removed routes or alter the committed result (D-05-06)."
    - "Real applyReconcile uninstall children receive the same runtime and preserve scope isolation, canonical rows, exact trees/state/errors/silence/notifications (D-05-02)."
    - "Containment, AG-5 abort, data and clone cleanup eligibility, errors, warnings, results, and exact notification semantics remain unchanged."
    - "Uninstall remains one cohesive transaction; completion-cache invalidation stays assigned to Plan 05-25 and no Phase 6 split lands."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
    - tests/edge/register.test.ts
    - tests/edge/handlers/plugin/uninstall.test.ts
    - tests/orchestrators/plugin/uninstall.test.ts
    - tests/orchestrators/reconcile/apply.test.ts
  key_links:
    - "registered uninstall -> cohesive transaction port -> committed state/configuration -> supplied runtime route removal"
    - "applyReconcile -> real uninstall child -> same runtime -> complete apply projection"
    - "commit boundary -> route removal -> independent data/clone cleanup and exact notification"
  prohibitions:
    - "No uninstall decomposition, child-operation bundle, __deps, ForTest, test-only reset/export, optional/default/dead port, or second runtime."
    - "No weakening or broadening of applyReconcile/bootstrapClaudePlugin full-observation exceptions."
    - "No completion-cache invalidation before Plan 05-25, Phase 6 patch/split work, PID/info/stage change, or new Fallow suppression."
---

<objective>
Route direct and reconciled uninstall through the lifecycle runtime and cohesive uninstall transaction port; preserve config/state order, partial cascade, containment, post-commit cleanup, routes, errors, results, and exact notifications (D-05-02, D-05-04, D-05-06, D-05-09).

Purpose: Remove runtime routes only when uninstall state commits while retaining full transaction and apply composition behavior.
Output: Required direct/reconciled runtime wiring and exact state, tree, rollback, cleanup, result, and notification evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-05-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-14-SUMMARY.md
</context>

## Edge assumptions

- Use executed summary identifiers for the runtime and cohesive uninstall transaction contract.
- The uninstall port contains semantic schedule operations already used by production. Do not split it or grant a generic filesystem/cache/hook bundle.
- Runtime route removal follows the existing committed removal/partial-cascade boundary. Rollback never uses a whole-runtime reset.
- Completion-cache invalidation is deliberately later because Plan 05-25 proves post-commit cache eligibility on the completed graph.
- applyReconcile stays a real-child full-observation composition. bootstrapClaudePlugin remains the only other exception.
- Keep containment, Phase 6 machinery, exact existing Fallow comment, PID/info/stage exclusions, and uninstall cohesion intact.

## Artifacts this plan produces

| File family                            | Contract or proof                                                                   |
| -------------------------------------- | ----------------------------------------------------------------------------------- |
| Edge/uninstall path and owners         | Required runtime reaches cohesive direct uninstall and exact public effects.        |
| Uninstall/apply composition and owners | Real reconciled uninstall shares runtime without weakening complete apply evidence. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace runtime ownership through cohesive direct uninstall</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-05-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-14-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/edge/register.test.ts, tests/edge/handlers/plugin/uninstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/edge/register.test.ts, tests/edge/handlers/plugin/uninstall.test.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <behavior>
    - "The registered handler forwards the lifecycle runtime and required cohesive transaction port through the production uninstall path."
    - "Full and partial committed removals delete only target routes after configuration/state effects; unrelated and peer-runtime routes remain exact."
    - "Validation, containment, cascade, configuration, or save failure preserves the route set required by final durable state."
    - "Post-commit data/clone cleanup retains current independent eligibility, error/warning, result, and notification behavior."
  </behavior>
  <action>Use the executed Plan 05-05 uninstall contract and Plan 05-14 runtime graph. Make edge registration and the uninstall handler require and forward the same HooksRuntime into the cohesive transaction factory. Replace transition route mutation with supplied-runtime operations at the current committed full/partial removal boundary. Preserve target validation, path containment, cascade order, configuration-before-state persistence, AG-5 abort, compensation/rollback, outcome construction, and independent post-commit data/clone cleanup. Add direct owner cases for full, partial, no-effect, pre-commit failure, rollback, and cleanup failure. Assert exact state/configuration/tree, routes, unrelated bytes, errors, warnings, rows, and notifications before port call logs. Do not add completion invalidation or split uninstall.</action>
  <verify><automated>node --test tests/edge/register.test.ts tests/edge/handlers/plugin/uninstall.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/plugin/uninstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts tests/edge/register.test.ts tests/edge/handlers/plugin/uninstall.test.ts tests/orchestrators/plugin/uninstall.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; runtime/transaction is optional, broad, dead, or bypassed; uninstall is decomposed; route removal crosses commit/rollback; config/state/partial/containment/cleanup order, trees, errors, rows, warnings, or notifications drift; or cache/Phase 6 work appears.</fails_when></verify>
  <acceptance_criteria>Direct uninstall uses one lifecycle runtime and the cohesive required transaction port, with routes matching exact durable full/partial/rollback state (D-05-04, D-05-06, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">Runtime identity now participates in the cohesive uninstall schedule and later cache invalidation, so contract changes require coordinated edge, transaction, and apply migration.</reversibility>
  <done>Direct uninstall preserves exact transaction and cleanup behavior while mutating only committed target routes in the owning runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve real reconciled uninstall and full apply observations</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-14-SUMMARY.md, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "applyReconcile forwards its required runtime to the real uninstall child without substituting any child operation."
    - "Committed full and partial reconcile removals update target routes; failed or rolled-back removals retain routes consistent with exact final state."
    - "Sibling scopes and unrelated routes remain isolated, and canonical project-before-user outcome order is unchanged."
    - "Apply retains complete results, state/configuration, scope trees, errors/redaction, silence, failure isolation, and exact notification bytes."
  </behavior>
  <action>Thread the runtime already required by Plan 05-13 apply options into the real uninstall child and cohesive transaction contract. Preserve all other children and the authorized state reader. Keep per-scope isolation, ordering, backfill/rebuild positions, projection, silence, and single notification exact. Expand the apply owner only with observations needed to prove direct and reconciled route agreement on committed, partial, failed, and rolled-back removals. Retain complete state/configuration and both scope-tree assertions before transaction interactions. Per D-05-02, prohibit any uninstall/apply child bundle or mock replacement. Run bootstrap and cross-scope suites read/verify-only, and leave completion invalidation to Plan 05-25.</action>
  <verify><automated>node --test tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/reconcile/apply.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/reconcile/apply.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; apply replaces or bundles a real child; runtime is omitted/defaulted; route/full/partial/rollback state diverges; sibling isolation, canonical order, complete state/trees/errors/silence/notifications weaken; bootstrap changes; uninstall splits; or cache/deferred work appears.</fails_when></verify>
  <acceptance_criteria>Real apply-child uninstall shares the lifecycle runtime and keeps the complete full-observation composition contract (D-05-02, D-05-04, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">The runtime now links apply and cohesive uninstall behavior; reversing it affects real-child composition, cross-scope state, and later cache work.</reversibility>
  <done>Direct and reconciled uninstall preserve exact cohesive transaction behavior and full apply evidence on one runtime.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                    | Description                                                                                 |
| ------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Uninstall target -> destructive transaction | Names and persisted records select configuration, state, route, data, and clone removal.    |
| Commit/partial state -> runtime routes      | Premature or broad route deletion can execute stale state or erase unrelated hooks.         |
| Apply plan -> real uninstall child          | Substitution can hide rollback, partial cascade, scope isolation, and notification defects. |

## STRIDE threat register

| Threat ID | Category                           | Component                      | Severity | Disposition | Mitigation plan                                                                                        |
| --------- | ---------------------------------- | ------------------------------ | -------- | ----------- | ------------------------------------------------------------------------------------------------------ |
| T-05-62   | Tampering / Elevation of privilege | cohesive uninstall transaction | high     | mitigate    | Preserve validation, containment, config/state order, AG-5 abort, and exact semantic port boundaries.  |
| T-05-63   | Tampering                          | route removal eligibility      | high     | mitigate    | Remove only committed target routes and prove partial, rollback, unrelated, and peer-runtime behavior. |
| T-05-64   | Tampering / Repudiation            | apply composition              | high     | mitigate    | Keep real uninstall and complete state/tree/result/error/silence/notification evidence.                |
| T-05-65   | Denial of service                  | independent cleanup            | medium   | mitigate    | Preserve post-commit data/clone cleanup ordering, warning policy, and retryable state.                 |
| T-05-66   | Information disclosure             | path/error projection          | medium   | mitigate    | Retain containment, redaction, scope attribution, and unrelated-byte assertions.                       |

ASVS Level 1: destructive transaction, route eligibility, and apply-composition threats are high severity and block completion.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                            | Plan/task         | Status  | Notes                                                      |
| -------- | ------------------------- | ---------------------------------------------------------------- | ----------------- | ------- | ---------------------------------------------------------- |
| GOAL     | -                         | Legitimate seams and lifecycle-owned state                       | 05-15 / Tasks 1-2 | COVERED | Direct and reconciled uninstall share the root runtime.    |
| REQ      | TREF-04                   | Preserve classified uninstall transaction and apply composition  | 05-15 / Tasks 1-2 | COVERED | The port stays cohesive; apply keeps real children.        |
| REQ      | TREF-05                   | Runtime owns hook routes                                         | 05-15 / Tasks 1-2 | COVERED | Commit/partial/rollback effects use one instance.          |
| REQ      | TREF-06                   | Public outcomes replace reset/internal state tests               | 05-15 / Tasks 1-2 | COVERED | Exact routes, bytes, rows, and notifications are observed. |
| RESEARCH | uninstall routing         | Preserve config/state order, partial cascade, cleanup, and apply | 05-15 / Tasks 1-2 | COVERED | Direct and composition owners cover every boundary.        |
| CONTEXT  | D-05-02, D-05-04, D-05-06 | Real apply child and genuine runtime effects                     | 05-15 / Tasks 1-2 | COVERED | No child substitute or reset appears.                      |
| CONTEXT  | D-05-09                   | Keep uninstall cohesive and Phase 6 work out                     | 05-15 / Tasks 1-2 | COVERED | Current module ownership remains.                          |

Cache invalidation remains assigned to Plan 05-25, final composition proof to Plan 05-27, and reset/public-surface cleanup to Plans 05-28 through 05-32. Exclusions are not gaps.

<verification>
Run both task commands and `npm run check`. Confirm only the eight frontmatter files change, Task 1 changes six files and Task 2 changes four, uninstall stays cohesive, runtime and transaction ports are required, apply retains full observations, no cache or deferred work appears, and the existing Fallow comment remains byte-exact.
</verification>

<success_criteria>

- Direct and reconciled uninstall use one lifecycle runtime and the required cohesive transaction contract.
- Runtime routes match committed, partial, failed, and rolled-back persisted state.
- Configuration/state order, containment, abort, cleanup, results, errors, warnings, and notifications remain exact.
- applyReconcile retains real children and complete scope/state/tree observations.
- No uninstall split, forbidden seam, second lifetime, cache work, suppression, Phase 6 cleanup, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-15-SUMMARY.md` when done.
</output>
