---
phase: 05-injection-and-ownership-design
plan: 28
type: execute
wave: 21
depends_on: [05-26]
files_modified:
  - tests/architecture/hooks-async-rewake.test.ts
  - tests/architecture/hooks-if-field.test.ts
  - tests/bridges/hooks/async-rewake/registry.test.ts
  - tests/bridges/hooks/dispatch.test.ts
  - tests/bridges/hooks/event-adapters.test.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/bridges/hooks/routing-state.test.ts
  - tests/bridges/hooks/settle.test.ts
autonomous: true
requirements: [TREF-05, TREF-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every hooks unit/architecture reset caller in the eight-file census uses a fresh per-case HooksRuntime or a real registration/reload transition (D-05-04, D-05-06)."
    - "Tests observe public routing, callback, settle, async child, PID, result, and message behavior rather than resetting or reading module state (D-05-07)."
    - "Exact event matching, reducer order, pending context, settle cap/latch, stale-callback, process-safety, and PID behavior remain unchanged."
    - "The reset exports and bounded transition remain in production until Plans 05-29 and 05-30 complete their caller migrations; this plan performs no deletion."
  artifacts:
    - tests/architecture/hooks-async-rewake.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - tests/bridges/hooks/async-rewake/registry.test.ts
    - tests/bridges/hooks/dispatch.test.ts
    - tests/bridges/hooks/event-adapters.test.ts
    - tests/bridges/hooks/event-router.test.ts
    - tests/bridges/hooks/routing-state.test.ts
    - tests/bridges/hooks/settle.test.ts
  key_links:
    - "fresh per-case HooksRuntime -> owner callback factories -> public results/messages"
    - "same-runtime re-registration -> stale callback no-op -> live generation remains active"
    - "test census -> no reset caller in the eight owned suites"
  prohibitions:
    - "No production edit, early reset deletion, replacement reset alias, __deps, ForTest, raw state access, optional/default/dead port, or shared test runtime."
    - "No weakening of applyReconcile/bootstrapClaudePlugin or their exact exception count."
    - "No Phase 6 work, excluded-file edit, or new Fallow suppression."
---

<objective>
Migrate the complete hooks unit/architecture reset-caller census to fresh per-case HooksRuntime fixtures and public lifecycle/callback observations; retain exact behavior while leaving deletion to the terminal reset plan (D-05-04, D-05-06, D-05-07).

Purpose: Remove test dependence on process-global cleanup before deleting the production reset surfaces.
Output: Eight owner/architecture suites isolated by construction with exact public lifecycle, route, settle, async, and process-safety evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md
</context>

## Edge assumptions

- Run a fresh test-only census for `resetEpoch` and `resetRoutingState` before editing and record every caller in the summary.
- Construct a fresh HooksRuntime inside each test or per-case fixture. Do not share mutable runtime instances across cases or use hooks for whole-suite cleanup.
- Use real registration/reload/input/terminal behavior where lifecycle transitions are under test. Do not call a differently named whole-state clear.
- Preserve every existing public assertion; replace only setup/reset coupling and internal-state observations.
- Do not delete production reset/transition surfaces until Plans 05-29 and 05-30 finish and Plan 05-31 repeats the zero-caller census.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Migrate async, if-field, registry, and dispatch owners to fresh runtimes</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, tests/architecture/hooks-async-rewake.test.ts, tests/architecture/hooks-if-field.test.ts, tests/bridges/hooks/async-rewake/registry.test.ts, tests/bridges/hooks/dispatch.test.ts, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts</read_first>
  <files>tests/architecture/hooks-async-rewake.test.ts, tests/architecture/hooks-if-field.test.ts, tests/bridges/hooks/async-rewake/registry.test.ts, tests/bridges/hooks/dispatch.test.ts</files>
  <behavior>
    - "Each case owns a fresh runtime and passes it through the public async/dispatch factory path."
    - "No case imports or calls hooks reset functions or observes writable internal state."
    - "Child ownership, timers, buffers, stale terminal behavior, PID serialization, marker checks, matcher filtering, reducer order, and Pi results remain exact."
    - "A deliberately separate runtime remains isolated without clearing either instance."
  </behavior>
  <action>Record the caller census, then replace reset-based setup in these four suites with `createHooksRuntime()` per case and explicit runtime forwarding through production callback/factory APIs. When a test requires generation change or cleanup, drive the real registration/reload or terminal operation. Keep `SpawnDeps` and `OrphanProbes` separate, use fresh child doubles and temporary filesystem state per case, and retain exact public results, messages, buffers, signals, PID bytes, errors, and call order. Remove assertions whose only subject is a reset surface only after equivalent lifecycle behavior is asserted. Per D-05-07, add no test-access export or raw state reader. Do not modify production or delete reset symbols in this plan.</action>
  <verify><automated>node --test tests/architecture/hooks-async-rewake.test.ts tests/architecture/hooks-if-field.test.ts tests/bridges/hooks/async-rewake/registry.test.ts tests/bridges/hooks/dispatch.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/architecture/hooks-async-rewake.test.ts tests/architecture/hooks-if-field.test.ts tests/bridges/hooks/async-rewake/registry.test.ts tests/bridges/hooks/dispatch.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; a reset call/import remains in the four files; cases share runtime state; a test reaches raw internals or adds a production seam; public async/PID/dispatch behavior or assertions weaken; production resets are deleted early; or Phase 6/excluded work appears.</fails_when></verify>
  <acceptance_criteria>The first four census suites isolate hooks state through fresh runtimes and public lifecycle behavior with exact async, PID, matcher, reducer, and result evidence (D-05-04, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">The migration changes test fixture ownership without changing production contracts; each suite can be adjusted locally while the runtime API remains stable.</reversibility>
  <done>The async/if-field/registry/dispatch group contains no reset caller and preserves exact public behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate event, routing, and settle owners without early deletion</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, tests/bridges/hooks/event-adapters.test.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/routing-state.test.ts, tests/bridges/hooks/settle.test.ts, extensions/pi-claude-marketplace/bridges/hooks/runtime.ts, extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/bridges/hooks/settle.ts</read_first>
  <files>tests/bridges/hooks/event-adapters.test.ts, tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/routing-state.test.ts, tests/bridges/hooks/settle.test.ts</files>
  <behavior>
    - "Every case owns a fresh runtime and uses public runtime/callback operations for routes, context, settle state, and generation."
    - "Re-registration and genuine input transitions replace reset calls where lifecycle changes are part of behavior."
    - "Route order, one-shot context, stale entry/post-await behavior, settle last-write/cap/latch/precedence, exact messages, and notifications remain unchanged."
    - "The four files contain no hooks reset import/call, while production reset/transition definitions remain for later migrations."
  </behavior>
  <action>Replace reset setup in these four suites with a fresh runtime for each case and pass it through the existing public routing, event-router, adapter, and settle factories. Use same-runtime re-registration for generation invalidation and genuine input for settle transitions. Preserve exact route entries and order, callback returns, one-shot context, stale no-effects, cap/latch behavior, warnings, messages, errors, and notifications. Keep assertions against public outputs and readonly domain values; remove direct module-state/reset assertions only with equivalent behavior coverage. Do not edit production or delete/reset-rename any surface. Record the post-task caller census and leave every remaining integration/operation caller for Plans 05-29 and 05-30.</action>
  <verify><automated>node --test tests/bridges/hooks/event-adapters.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/routing-state.test.ts tests/bridges/hooks/settle.test.ts tests/architecture/hooks-lifecycle.test.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint tests/bridges/hooks/event-adapters.test.ts tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/routing-state.test.ts tests/bridges/hooks/settle.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; a reset call/import remains in the four files; a shared runtime or raw-state assertion appears; route/context/stale/settle/results/messages weaken; production surfaces are deleted or renamed; the census omits remaining callers; or a seam, suppression, or Phase 6/excluded change appears.</fails_when></verify>
  <acceptance_criteria>The remaining four hooks owner suites use fresh runtimes and genuine lifecycle transitions with exact public route, context, stale, settle, result, and message behavior (D-05-04, D-05-06, D-05-07).</acceptance_criteria>
  <reversibility rating="reversible">This is a test-fixture migration within stable public runtime contracts.</reversibility>
  <done>All eight unit/architecture census suites are reset-free while production deletion remains deferred to Plan 05-31.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                              | Description                                                                                 |
| ------------------------------------- | ------------------------------------------------------------------------------------------- |
| Test case -> HooksRuntime             | Shared fixtures can leak routes, children, context, or settle state across cases.           |
| Lifecycle event -> public observation | Replacing resets must preserve actual registration, input, terminal, and callback behavior. |
| Test migration -> production API      | A convenience export can recreate the test-only seam being removed.                         |

## STRIDE threat register

| Threat ID | Category                           | Component          | Severity | Disposition | Mitigation plan                                                                                 |
| --------- | ---------------------------------- | ------------------ | -------- | ----------- | ----------------------------------------------------------------------------------------------- |
| T-05-122  | Tampering / Information disclosure | case isolation     | high     | mitigate    | Construct one fresh runtime per case and prove peer isolation without cleanup calls.            |
| T-05-123  | Tampering                          | lifecycle fidelity | high     | mitigate    | Drive genuine registration/reload/input/terminal operations and preserve exact public outcomes. |
| T-05-124  | Tampering                          | test-only access   | high     | mitigate    | Prohibit new production exports/raw state and verify tests use existing public contracts.       |
| T-05-125  | Denial of service                  | async/PID behavior | medium   | mitigate    | Preserve timers, buffers, serialization, process checks, and cleanup assertions.                |

ASVS Level 1: case isolation, lifecycle fidelity, and production-surface integrity are high-severity blocking threats.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                      | Plan/task         | Status  | Notes                                              |
| -------- | ------------------------- | ---------------------------------------------------------- | ----------------- | ------- | -------------------------------------------------- |
| GOAL     | -                         | Correctly owned runtime state and public contracts         | 05-28 / Tasks 1-2 | COVERED | Eight suites isolate state by construction.        |
| REQ      | TREF-05                   | Mutable hooks state belongs to runtime lifecycles          | 05-28 / Tasks 1-2 | COVERED | Fresh instances replace process cleanup.           |
| REQ      | TREF-06                   | Tests use public contracts and remove reset coupling       | 05-28 / Tasks 1-2 | COVERED | Reset callers disappear without weaker assertions. |
| RESEARCH | reset migration           | Unit/architecture suites create fresh runtimes             | 05-28 / Tasks 1-2 | COVERED | Exact eight-file census is partitioned 4+4.        |
| CONTEXT  | D-05-04, D-05-06, D-05-07 | One runtime, genuine transitions, trace-preserving removal | 05-28 / Tasks 1-2 | COVERED | Deletion waits for Plans 05-29/05-30.              |

Integration callers remain Plan 05-29, completion callers Plan 05-30, deletion Plan 05-31, and BOOLEAN_FLAGS/final audit Plans 05-32/05-33. Phase 6 and exclusions are not gaps.

<verification>
Run both task commands. Confirm only the eight test files in frontmatter change, each task changes four files, all eight are free of hooks reset imports/calls, public assertions remain exact, and production reset/transition surfaces are not deleted early. Confirm the existing Fallow comment remains byte-exact as `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` and no suppression or Phase 6/excluded work appears.
</verification>

<success_criteria>

- All eight hooks unit/architecture suites use fresh per-case runtimes or genuine lifecycle transitions.
- No suite imports/calls a hooks reset or reaches raw mutable state.
- Exact routes, callbacks, settle, async, PID, results, errors, messages, and notifications remain green.
- Production deletion waits for all later caller migrations and Plan 05-31's fresh census.
- No new test seam, shared fixture, suppression, Phase 6 work, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-28-SUMMARY.md` when done.
</output>
