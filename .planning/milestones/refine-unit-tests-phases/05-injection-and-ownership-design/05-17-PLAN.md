---
phase: 05-injection-and-ownership-design
plan: 17
type: execute
wave: 11
depends_on: [05-16]
files_modified:
  - extensions/pi-claude-marketplace/index.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts
  - tests/edge/handlers/plugin/update.test.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/marketplace/update.test.ts
autonomous: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The extension root creates one runtime-bound PluginUpdateFn whose existing shared signature is unchanged (D-05-04, D-05-09)."
    - "Direct update, all-target update, marketplace refresh, and autoupdate cascade use that same closure and lifecycle runtime."
    - "Finalization updates routes at the existing committed boundary; skipped, failed, rolled-back, and cleanup-failure targets retain exact route/state agreement (D-05-06)."
    - "Target order, project-before-user precedence, rollback, cleanup, results, classifications, errors, warnings, and notifications remain unchanged."
    - "The update transaction remains in its current module, and the PluginUpdateFn shared callback contract is neither widened nor cache-parameterized here (D-05-09)."
    - "Index, edge-registration, cross-operation, apply, and bootstrap suites remain read/verify-only gates unless listed in frontmatter."
  artifacts:
    - extensions/pi-claude-marketplace/index.ts
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts
    - tests/edge/handlers/plugin/update.test.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
    - tests/orchestrators/plugin/update.test.ts
    - tests/orchestrators/marketplace/update.test.ts
  key_links:
    - "extension root -> runtime-bound PluginUpdateFn -> edge update handler and marketplace/autoupdate consumers"
    - "plugin update finalization -> supplied runtime route refresh -> exact public update outcome"
    - "marketplace target order -> shared PluginUpdateFn -> per-target state/route/result classification"
  prohibitions:
    - "No PluginUpdateFn signature change, __deps, ForTest, test-only reset/export, optional/default/dead port, or second runtime/cache lifetime."
    - "No cache invalidation before Plan 05-23 and no weakening/broadening of apply/bootstrap composition exceptions."
    - "No Phase 6 update split/global-patch removal, excluded decomposition, or new Fallow suppression."
---

<objective>
Route direct update, all-target update, and marketplace/autoupdate cascade through one runtime-bound `PluginUpdateFn` without changing its shared signature; retain finalization, routing, target order, rollback, cleanup, results, and notifications (D-05-04, D-05-06, D-05-09).

Purpose: Keep update route finalization lifecycle-owned while preserving all transaction, target-order, cleanup, result, and notification behavior.
Output: One root-bound update closure and exact direct/cascade runtime, rollback, state, route, and public outcome evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-16-SUMMARY.md
</context>

## Edge assumptions

- Use Plan 05-16's executed root/runtime and handler identifiers.
- `PluginUpdateFn` already connects direct, bulk, marketplace, and autoupdate paths. Keep its shared signature exact by binding runtime in the root closure.
- Update finalization and rollback order remain current production behavior. Runtime route refresh is part of finalization, not a general cache reset.
- Plan 05-23 later binds CompletionCache into the same root closure without changing the callback signature.
- Existing index, edge-register, transaction-lifecycle, apply, bootstrap, and cross-operation suites are read/verify-only unless listed in frontmatter.
- Preserve Phase 6 update-split/global-patch inventory, exact Fallow comment, and excluded stage/PID/info/uninstall boundaries.

## Artifacts this plan produces

| File family                                             | Contract or proof                                                                               |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Extension root, edge register/update handler, and owner | One runtime-bound production update closure with unchanged shared callback shape.               |
| Plugin/marketplace update orchestrators and owners      | Exact direct, bulk, cascade, rollback, finalization, cleanup, route, and notification behavior. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one runtime-bound PluginUpdateFn from root to handler</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-16-SUMMARY.md, extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts, tests/edge/handlers/plugin/update.test.ts, extensions/pi-claude-marketplace/edge/types.ts, tests/index.test.ts, tests/edge/register.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/index.ts, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts, tests/edge/handlers/plugin/update.test.ts</files>
  <behavior>
    - "claudeMarketplaceExtension binds its one runtime into one PluginUpdateFn and supplies that closure to command registration."
    - "The shared callback signature remains byte-for-byte type-compatible for direct, all-target, marketplace, and autoupdate callers."
    - "The registered handler forwards exact target/scope/all flags and returns unchanged single/bulk results and notifications."
    - "No handler or downstream callback constructs, selects, or defaults another runtime."
  </behavior>
  <action>At the extension root, create one production `PluginUpdateFn` closure that captures the lifecycle HooksRuntime and delegates to the existing update orchestrator. Pass that same closure through edge registration and the update handler. Preserve the existing shared function signature, command parser, target/scope/all selection, callback return type, registration order, results, warnings, and notifications. Update only the handler owner among tests in this task; run index and register owners read/verify-only. Add exact single/all handler outcomes plus forwarding evidence. Per D-05-09, do not add a runtime parameter to `PluginUpdateFn`, split update, or bind completion cache in this plan.</action>
  <verify><automated>node --test tests/edge/handlers/plugin/update.test.ts tests/index.test.ts tests/edge/register.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/index.ts extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/plugin/update.ts tests/edge/handlers/plugin/update.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; PluginUpdateFn changes signature; more than one runtime/update closure is created; handler/runtime is optional, defaulted, dead, or bypassed; parsing/results/warnings/notifications change; unlisted tests are modified; or cache/split work appears.</fails_when></verify>
  <acceptance_criteria>One root runtime is captured by one production PluginUpdateFn with its shared signature and public handler behavior unchanged (D-05-04, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">The closure is shared by direct and cascade update paths and later gains the lifecycle cache, so changing it touches every update consumer.</reversibility>
  <done>The root, registration, and handler use one runtime-bound PluginUpdateFn without signature or public behavior drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve direct, bulk, marketplace, and autoupdate runtime effects</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-16-SUMMARY.md, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/marketplace/update.test.ts, extensions/pi-claude-marketplace/index.ts, tests/integration/transaction-lifecycle-cascade.test.ts, tests/orchestrators/reconcile/apply.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <behavior>
    - "Successful direct update finalization refreshes only target routes in the captured runtime after durable state succeeds."
    - "All-target, marketplace, and autoupdate paths call the same PluginUpdateFn in exact current target and project-before-user order."
    - "Skipped, failed, rolled-back, and cleanup-failure targets retain exact state/tree/routes, eligibility, and classifications."
    - "Exact results, errors, warnings, cleanup, cascade rows, silence, and notifications remain unchanged."
  </behavior>
  <action>Change plugin update to consume the runtime captured by the root-bound closure and route its current hooks finalization through that instance. Preserve prepare, materialize, replace, save, compensation, rollback, abort, cleanup, direct result, and notification order. Keep marketplace update's existing `PluginUpdateFn` call shape and exact named/all-target ordering; do not add a runtime argument or hidden selector. Add owner cases proving the same runtime receives successful direct and cascade finalization, while skip/failure/rollback paths preserve exact persisted state, trees, routes, errors, warnings, eligibility, rows, and notifications. Run the root/edge/cross-operation suites read/verify-only. Leave CompletionCache binding to Plan 05-23.</action>
  <verify><automated>node --test tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/update.test.ts tests/edge/handlers/plugin/update.test.ts tests/index.test.ts tests/edge/register.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/update.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/plugin/update.ts extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/marketplace/update.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; callback signature/order changes; another runtime or optional/default selector appears; finalization crosses the durable boundary; skip/failure/rollback routes or state drift; exact errors/results/classifications/warnings/cleanup/notifications weaken; or cache/split/suppression work appears.</fails_when></verify>
  <acceptance_criteria>All update modes use one runtime-bound callback and preserve exact transaction, ordering, rollback, cleanup, route, result, and notification semantics (D-05-04, D-05-06, D-05-09).</acceptance_criteria>
  <reversibility rating="costly">The shared update callback is a cross-operation contract that Phase 6 later moves, so runtime changes require coordinated direct and cascade migration.</reversibility>
  <done>Direct, bulk, marketplace, and autoupdate update paths share one runtime-bound PluginUpdateFn with exact effects.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                   | Description                                                                                    |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------- |
| Extension root -> shared update callback   | A hidden/default runtime can split state across direct and cascade callers.                    |
| Update transaction -> runtime finalization | Routes must reflect only durable successful updates and exact rollback state.                  |
| Marketplace targets -> repeated callback   | Ordering, scope precedence, and per-target eligibility determine user-visible cascade results. |

## STRIDE threat register

| Threat ID | Category                           | Component                    | Severity | Disposition | Mitigation plan                                                                          |
| --------- | ---------------------------------- | ---------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-05-71   | Tampering / Information disclosure | shared runtime closure       | high     | mitigate    | Bind one root runtime without changing PluginUpdateFn and prove all consumers use it.    |
| T-05-72   | Tampering                          | update commit/finalization   | high     | mitigate    | Preserve transaction order and assert route/state agreement across success and rollback. |
| T-05-73   | Tampering / Repudiation            | bulk/cascade ordering        | high     | mitigate    | Assert exact target order, scope precedence, eligibility, rows, and notifications.       |
| T-05-74   | Denial of service                  | cleanup and partial failures | medium   | mitigate    | Preserve independent cleanup, failure continuation, exact warnings, and committed state. |
| T-05-75   | Information disclosure             | error projection             | medium   | mitigate    | Retain exact failure identity/redaction and scope/target attribution.                    |

ASVS Level 1: shared identity, transaction finalization, and cascade-order threats are high severity and block completion.
</threat_model>

## Source coverage audit

| Source   | ID               | Feature or constraint                                              | Plan/task         | Status  | Notes                                               |
| -------- | ---------------- | ------------------------------------------------------------------ | ----------------- | ------- | --------------------------------------------------- |
| GOAL     | -                | Lifecycle-owned state with public contracts                        | 05-17 / Tasks 1-2 | COVERED | One runtime-bound callback drives all update modes. |
| REQ      | TREF-04          | Preserve existing production update contracts                      | 05-17 / Tasks 1-2 | COVERED | No callback widening or new hidden seam appears.    |
| REQ      | TREF-05          | Runtime owns update route effects                                  | 05-17 / Tasks 1-2 | COVERED | Root closure carries one identity.                  |
| REQ      | TREF-06          | Public outcomes replace internal/reset evidence                    | 05-17 / Tasks 1-2 | COVERED | Exact state/routes/rows/messages remain primary.    |
| RESEARCH | update routing   | Shared PluginUpdateFn covers direct, bulk, marketplace, autoupdate | 05-17 / Tasks 1-2 | COVERED | Signature and target order remain exact.            |
| CONTEXT  | D-05-04, D-05-06 | One runtime and genuine finalization effects                       | 05-17 / Tasks 1-2 | COVERED | No second lifetime or reset appears.                |
| CONTEXT  | D-05-03, D-05-09 | Keep update split and patch removal in Phase 6                     | 05-17 / Tasks 1-2 | COVERED | Current module and callback ownership stay intact.  |

Reinstall/runtime routing follows in Plan 05-18. Cache-bound update follows in Plan 05-23, and reset/public-surface cleanup remains later. Exclusions are not gaps.

<verification>
Run both task commands and `npm run check`. Confirm only the eight frontmatter files change, each task changes four files, PluginUpdateFn signature/order remains exact, one runtime reaches all update modes, unlisted suites remain unmodified, no cache/deferred work appears, and the existing Fallow comment remains byte-exact.
</verification>

<success_criteria>

- One root-bound PluginUpdateFn carries the lifecycle runtime to direct, bulk, marketplace, and autoupdate paths.
- The shared callback signature and target/scope ordering stay unchanged.
- Finalization, rollback, cleanup, routes, state/tree bytes, classifications, results, errors, warnings, and notifications remain exact.
- Read/verify-only integration suites remain unmodified.
- No forbidden seam, second lifetime, cache work, update split, suppression, Phase 6 cleanup, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-17-SUMMARY.md` when done.
</output>
