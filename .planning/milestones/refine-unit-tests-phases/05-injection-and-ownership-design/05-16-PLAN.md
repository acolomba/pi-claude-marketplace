---
phase: 05-injection-and-ownership-design
plan: 16
type: execute
wave: 10
depends_on: [05-15]
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - tests/edge/register.test.ts
  - tests/edge/handlers/plugin/import.test.ts
  - tests/orchestrators/import/execute.test.ts
autonomous: true
requirements: [TREF-05, TREF-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Registered import handling receives the root HooksRuntime as a required value and forwards it through the real import workflow (D-05-04)."
    - "Import reuses the existing required child mutation contracts and one runtime; it gains no child-operation bundle, second lifetime, or reset (D-05-02, D-05-06)."
    - "Marketplace and plugin import actions preserve current order, target/scope resolution, validation, state/configuration/scope trees, results, errors, warnings, and notifications."
    - "Each real child mutation updates routes only at its existing durable boundary and unrelated or peer-runtime routes stay exact."
    - "applyReconcile and bootstrapClaudePlugin remain the only full-observation composition exceptions; import does not become a third exception."
    - "No Phase 6 extraction, completion-cache invalidation, stage/PID/info/uninstall decomposition, or new suppression appears."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - tests/edge/register.test.ts
    - tests/edge/handlers/plugin/import.test.ts
    - tests/orchestrators/import/execute.test.ts
  key_links:
    - "root runtime -> registered import handler -> real import executor -> existing child mutations"
    - "import operation order -> child transaction commit -> same-runtime route effects"
    - "public import outcome -> exact state/configuration/tree/notification assertions"
  prohibitions:
    - "No import child-operation bundle, __deps, ForTest, test-only reset/export, optional/default/dead port, or second runtime/cache lifetime."
    - "No third composition exception and no weakening of applyReconcile/bootstrapClaudePlugin."
    - "No Phase 6 patch/split work, completion invalidation, excluded decomposition, or new Fallow suppression."
---

<objective>
Route import handling and its real child mutations through the lifecycle runtime without a second runtime, reset, child-operation bundle, order change, or Phase 6 extraction (D-05-02, D-05-04, D-05-06).

Purpose: Keep imported hook routes consistent with real child commits while preserving exact import ordering and public outcomes.
Output: Required edge/import runtime propagation and complete import state, route, result, error, and notification evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-15-SUMMARY.md
</context>

## Edge assumptions

- Use Plan 05-15's executed runtime and child transaction identifiers. Do not add a generic import dependencies object.
- Import orchestrates real child mutations. Its owner asserts complete public outcomes rather than replacing children with interaction-only substitutes.
- The same runtime flows through all child mutations in one import. Each child retains its own commit, rollback, cleanup, and notification contract.
- Import does not own completion-cache invalidation. Later mutation plans attach the cache to the same child paths.
- applyReconcile and bootstrapClaudePlugin remain exactly the two full-observation exceptions; import remains an ordinary owner-tested workflow.
- Preserve Phase 6 machinery, exact Fallow comment, and excluded stage/PID/info/uninstall work.

## Artifacts this plan produces

| File family                                 | Contract or proof                                                                                          |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Edge registration/import handler and owners | Required root-runtime forwarding with exact parsing and public handler behavior.                           |
| Import executor and owner                   | One runtime reaches ordered real children with exact aggregate state, routes, outcomes, and notifications. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the root runtime into registered import handling</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-15-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts, tests/edge/register.test.ts, tests/edge/handlers/plugin/import.test.ts, extensions/pi-claude-marketplace/edge/types.ts, tests/orchestrators/import/execute.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts, tests/edge/register.test.ts, tests/edge/handlers/plugin/import.test.ts</files>
  <behavior>
    - "The registered import handler receives the same required runtime used by install, enable/disable, and uninstall."
    - "The handler forwards one runtime with exact parsed file/scope/flag values into the real import executor."
    - "Invalid, missing, no-effect, partial, and accepted inputs retain exact handler results, errors, warnings, and notifications."
    - "No handler-local runtime, optional fallback, reset, or child bundle exists."
  </behavior>
  <action>Extend the existing edge/import handler contract to require the root HooksRuntime and forward it to the real import executor. Preserve command registration order, parsing, scope resolution, import-file reading, validation, error mapping, result rows, and notification behavior. Add handler and registration owner cases that assert exact public outcomes before forwarding evidence, including invalid, no-effect, partial, and accepted inputs. Prove the exact runtime identity reaches execution and peer runtime state is unaffected. Per D-05-02 and D-05-06, do not replace real import children, define a child-operation bag, or add lifecycle cleanup.</action>
  <verify><automated>node --test tests/edge/register.test.ts tests/edge/handlers/plugin/import.test.ts tests/orchestrators/import/execute.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts tests/edge/register.test.ts tests/edge/handlers/plugin/import.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; runtime is optional, defaulted, dead, bypassed, or constructed in the handler; parsing/scope/results/errors/warnings/notifications change; children are replaced or bundled; peer runtime changes; or cache/deferred work appears.</fails_when></verify>
  <acceptance_criteria>Registered import forwards one required lifecycle runtime into the real production workflow with exact handler behavior (D-05-02, D-05-04, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">Runtime identity now crosses the import handler and ordered mutation graph, so changes require coordinated edge, executor, and child-owner migration.</reversibility>
  <done>The import command enters production execution with the root runtime and unchanged public parsing, results, errors, and notifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve ordered real child mutations on one runtime</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-15-SUMMARY.md, extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/edge/handlers/plugin/import.test.ts, tests/orchestrators/import/execute.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/edge/handlers/plugin/import.test.ts, tests/orchestrators/import/execute.test.ts</files>
  <behavior>
    - "The executor passes one runtime through all real marketplace/plugin child mutations in current declaration and target order."
    - "Each successful child exposes only its committed routes; failed, rolled-back, skipped, and partial children leave routes matching exact durable state."
    - "Aggregate state, configuration, both scope trees, results, classifications, errors, warnings, and notifications remain exact."
    - "Import remains an ordinary workflow and does not become a full-observation exception or add a child-operation seam."
  </behavior>
  <action>Make the import executor require and forward the HooksRuntime to its existing real child calls, using executed signatures from Plans 05-13 through 05-15. Keep child selection, declaration/target order, stop/continue policy, per-child lock/transaction boundaries, aggregation, error identity, warnings, and notification projection exact. Add executor cases for mixed success, skip, failure, rollback, and partial effects that assert full state/configuration/scope trees and runtime routes before any call-order evidence. Preserve real child calls in production and avoid an injected child bundle. Run transaction lifecycle plus both composition-exception suites read/verify-only. Do not split the executor or move ownership into Phase 6 files.</action>
  <verify><automated>node --test tests/orchestrators/import/execute.test.ts tests/edge/handlers/plugin/import.test.ts tests/integration/transaction-lifecycle-cascade.test.ts tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/import/execute.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/import/execute.ts extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts tests/orchestrators/import/execute.test.ts tests/edge/handlers/plugin/import.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; children receive different runtimes, are mocked/bundled, or change order; commit/rollback routes diverge; state/configuration/trees/results/classifications/errors/warnings/notifications weaken; a third exception, reset, cache work, split, or suppression appears.</fails_when></verify>
  <acceptance_criteria>Import preserves real ordered child behavior and complete public outcomes while all route effects use one lifecycle runtime (D-05-02, D-05-04, D-05-06).</acceptance_criteria>
  <reversibility rating="costly">Import aggregates several runtime-aware child contracts, so changing the identity or order affects all child owners and transaction integration.</reversibility>
  <done>All import child mutations share one runtime and retain exact aggregate state, route, error, result, and notification behavior.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                                   | Description                                                                     |
| ------------------------------------------ | ------------------------------------------------------------------------------- |
| Import file/input -> operation graph       | Untrusted content selects scopes, marketplaces, plugins, and ordered mutations. |
| Import executor -> real child transactions | A child bundle or changed order can hide rollback and partial state defects.    |
| Child durable effects -> lifecycle routes  | All child route changes must use one runtime and match final persisted state.   |

## STRIDE threat register

| Threat ID | Category                           | Component                     | Severity | Disposition | Mitigation plan                                                                           |
| --------- | ---------------------------------- | ----------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-05-67   | Tampering / Elevation of privilege | import validation and targets | high     | mitigate    | Preserve parsing, scope validation, containment, and exact real-child selection.          |
| T-05-68   | Tampering                          | child order and rollback      | high     | mitigate    | Keep real child calls and assert complete mixed success/failure/partial state and order.  |
| T-05-69   | Tampering / Information disclosure | runtime identity              | high     | mitigate    | Require one supplied runtime and prove peer isolation plus route/durable-state agreement. |
| T-05-70   | Repudiation                        | aggregate outcomes            | medium   | mitigate    | Preserve exact classifications, errors, warnings, rows, and notifications.                |

ASVS Level 1: import validation, real-child ordering, and runtime identity are high-severity blocking threats. No package or external service changes.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                             | Plan/task         | Status  | Notes                                                |
| -------- | ------------------------- | ------------------------------------------------- | ----------------- | ------- | ---------------------------------------------------- |
| GOAL     | -                         | Lifecycle-owned state and public contracts        | 05-16 / Tasks 1-2 | COVERED | One runtime reaches the real import graph.           |
| REQ      | TREF-05                   | Runtime ownership across child mutations          | 05-16 / Tasks 1-2 | COVERED | All route effects use the root identity.             |
| REQ      | TREF-06                   | Public behavior without reset/test seams          | 05-16 / Tasks 1-2 | COVERED | Complete aggregate outcomes and routes are asserted. |
| RESEARCH | hooks mutation routing    | Import forwards runtime through existing children | 05-16 / Task 2    | COVERED | Ordering and transaction boundaries remain exact.    |
| CONTEXT  | D-05-02, D-05-04, D-05-06 | Preserve composition exceptions and one runtime   | 05-16 / Tasks 1-2 | COVERED | Import gains no child bundle or reset.               |
| CONTEXT  | D-05-03, D-05-09          | Keep Phase 6 and excluded splits out              | 05-16 / Tasks 1-2 | COVERED | Current module ownership remains.                    |

Plugin update routing follows in Plan 05-17. Completion invalidation and terminal cleanup remain in later plans; excluded work is not a gap.

<verification>
Run both task commands and `npm run check`. Confirm only the six frontmatter files change, both tasks change four files, one runtime reaches every real child, exact order/state/trees/routes/outcomes remain, no third composition exception or forbidden seam appears, and the existing Fallow comment stays byte-exact.
</verification>

<success_criteria>

- Registered import and its real child graph use one lifecycle runtime.
- Child selection, ordering, transaction boundaries, rollback, partial effects, routes, and durable state remain exact.
- Complete public results, classifications, errors, warnings, scope trees, and notifications remain green.
- applyReconcile and bootstrapClaudePlugin remain the only full-observation exceptions.
- No child bundle, reset/test seam, second lifetime, cache work, split, suppression, or Phase 6 cleanup appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-16-SUMMARY.md` when done.
</output>
