---
phase: 05-injection-and-ownership-design
plan: 08
type: execute
wave: 3
depends_on: [05-07]
files_modified:
  - extensions/pi-claude-marketplace/shared/completion-cache.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - extensions/pi-claude-marketplace/persistence/locations.ts
  - tests/shared/completion-cache.test.ts
  - tests/edge/completions/data.test.ts
  - tests/orchestrators/marketplace/add.test.ts
  - tests/persistence/locations.test.ts
autonomous: true
requirements: [TREF-05, TREF-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh production-only census decides the marketplace-name memory surface: delete it only when no production consumer exists; otherwise migrate the genuine consumer (D-05-07)."
    - "Authoritative scoped name completion continues to read current state, preserve project/user precedence and unique ordering, and propagate state-read failures."
    - "Marketplace names schema version 2, scoped cache path, real file invalidation, ENOENT idempotence, and exact non-benign failure behavior remain live (D-05-05)."
    - "Artificial memory-priming assertions disappear only with the dead surface; marketplace add retains exact public state/config/tree/result/notification and names-file deletion evidence."
    - "Plan 05-07's plugin-index cache, transition identity, schema 6, TTL, poison, and completion behavior remain unchanged."
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/completion-cache.ts
      provides: "Census-driven marketplace-name memory disposition with live schema-2 invalidation"
    - path: extensions/pi-claude-marketplace/edge/completions/data.ts
      provides: "Authoritative scoped marketplace-name completion"
    - path: extensions/pi-claude-marketplace/persistence/locations.ts
      provides: "Accurate live names-cache path contract"
    - path: tests/orchestrators/marketplace/add.test.ts
      provides: "Real committed names-file invalidation and complete add behavior"
  key_links:
    - from: extensions/pi-claude-marketplace/edge/completions/data.ts
      to: extensions/pi-claude-marketplace/persistence/state-io.ts
      via: "authoritative rebuild for each requested scope"
    - from: extensions/pi-claude-marketplace/shared/completion-cache.ts
      to: extensions/pi-claude-marketplace/persistence/locations.ts
      via: "schema-2 file invalidation uses the existing derived scoped path"
    - from: tests/orchestrators/marketplace/add.test.ts
      to: extensions/pi-claude-marketplace/shared/completion-cache.ts
      via: "public add commit proves real names-file invalidation without memory priming"
---

<objective>
Resolve marketplace-name memory after a fresh consumer census while preserving authoritative completion and schema-2 file invalidation (D-05-05, D-05-07).

Purpose: Remove only a proven dead in-memory contract and retain every live cross-process cache path and public mutation effect.
Output: A census-backed disposition, exact authoritative name reads, accurate path ownership, and real invalidation evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-07-SUMMARY.md
</context>

## Edge assumptions

- Research found no production consumer of `getMarketplaceNames` or `memMarketplaceNames`, but the executor must repeat the census immediately before editing.
- The schema-2 file is not dead. Marketplace add/remove invalidators and cross-process behavior retain its path and deletion contract.
- If the fresh census finds a genuine production reader, migrate that reader to Plan 05-07's required cache instance and retain complete behavior. Do not delete a live contract.
- Plan 05-12 removes the bounded cache transition. This plan must not create another lifetime or alter plugin-index behavior.

## Artifacts this plan produces

| File family                          | Contract or proof                                                                                             |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| Completion cache/data and owners     | Fresh census, dead-memory removal or genuine-reader migration, and unchanged authoritative scoped completion. |
| Locations and marketplace-add owners | Accurate schema-2 path contract plus exact real-file invalidation after public add behavior.                  |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve marketplace-name memory from a fresh production census</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-07-SUMMARY.md, extensions/pi-claude-marketplace/shared/completion-cache.ts, extensions/pi-claude-marketplace/edge/completions/data.ts, tests/shared/completion-cache.test.ts, tests/edge/completions/data.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/shared/completion-cache.ts, extensions/pi-claude-marketplace/edge/completions/data.ts, tests/shared/completion-cache.test.ts, tests/edge/completions/data.test.ts</files>
  <behavior>
    - "The summary records a production-only CodeGraph and text census for the marketplace-name memory reader and map immediately before edits."
    - "With zero genuine consumers, the reader, memory map, private read helper, and cases existing only for that surface are removed together."
    - "If a genuine consumer exists, it receives the required CompletionCache and retains complete public behavior; no live contract is deleted."
    - "Marketplace-name completion still rebuilds both scopes from authoritative state with exact precedence, deduplication, ordering, and error identity."
  </behavior>
  <action>Use CodeGraph first, then a production-only text census for `getMarketplaceNames` and `memMarketplaceNames`. Record exact callers in the plan summary before editing. If the census is empty outside the defining module, apply D-05-07 trace-preserving removal to the reader export, memory map, private file-read helper, and artificial owner cases. If a genuine consumer exists, pass Plan 05-07's required cache to it and retain its complete observable contract. In both branches, keep `getMarketplaceNamesAcrossScopes` on direct authoritative state rebuilds and preserve project-before-user precedence, unique names, order, and state-read errors. Do not alter plugin-index maps, schema 6, TTL, poison, registered read propagation, or the production transition identity.</action>
  <verify><automated>node --test tests/shared/completion-cache.test.ts tests/edge/completions/data.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/completion-cache.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/completions/data.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/shared/completion-cache.ts extensions/pi-claude-marketplace/edge/completions/data.ts tests/shared/completion-cache.test.ts tests/edge/completions/data.test.ts --max-warnings=0</automated><fails_when>The census is omitted or not recorded; a genuine caller is deleted; dead memory survives a zero-consumer result; authoritative reads, precedence, deduplication, order, or error identity changes; plugin-index behavior or transition identity changes; or a new reset/default/test-only/dead surface appears.</fails_when></verify>
  <acceptance_criteria>The marketplace-name memory surface follows its current production census, and authoritative scoped completion retains exact behavior in either permitted branch (D-05-05, D-05-07).</acceptance_criteria>
  <reversibility rating="costly">Removing a public-looking but unconsumed reader is local only after the census; a discovered live caller instead requires coordinated cache propagation.</reversibility>
  <done>The memory disposition is evidence-backed, and no live marketplace-name behavior is lost.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve schema-2 paths and real marketplace invalidation</name>
  <read_first>extensions/pi-claude-marketplace/shared/completion-cache.ts, extensions/pi-claude-marketplace/persistence/locations.ts, tests/shared/completion-cache.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/persistence/locations.test.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts</read_first>
  <files>extensions/pi-claude-marketplace/shared/completion-cache.ts, extensions/pi-claude-marketplace/persistence/locations.ts, tests/shared/completion-cache.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/persistence/locations.test.ts</files>
  <behavior>
    - "MARKETPLACE_NAMES_CACHE_SCHEMA remains exactly schema version 2 and locations derive the same scoped path."
    - "Names-file invalidation unlinks only that path, treats ENOENT as success, and retains established non-ENOENT behavior."
    - "Marketplace add invalidates the real names file only after its durable behavior and preserves exact state, configuration, tree, result, notifications, and unrelated bytes."
    - "An artificial add case that primes dead memory is removed only when its public invalidation evidence remains covered elsewhere."
  </behavior>
  <action>Keep the marketplace-names schema, path, and invalidation operation as live production contracts. Update only stale locations documentation that describes a removed memory reader. Preserve exact path derivation and ENOENT-silent unlink behavior. In the marketplace-add owner, remove memory priming only if Task 1 deleted the dead reader. Retain or strengthen public assertions for durable state/configuration, full scope tree, names-file absence, unrelated bytes, returned outcome, and exact notifications. Do not modify marketplace-add production code in this row. Keep the Plan 05-07 transition binding required and unchanged for Plan 05-12 removal.</action>
  <verify><automated>node --test tests/shared/completion-cache.test.ts tests/edge/completions/data.test.ts tests/orchestrators/marketplace/add.test.ts tests/persistence/locations.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/completion-cache.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/completions/data.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/locations.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/shared/completion-cache.ts extensions/pi-claude-marketplace/edge/completions/data.ts extensions/pi-claude-marketplace/persistence/locations.ts tests/shared/completion-cache.test.ts tests/edge/completions/data.test.ts tests/orchestrators/marketplace/add.test.ts tests/persistence/locations.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; schema 2, cache path, deletion scope, ENOENT behavior, public add outcome/state/tree/notifications, or unrelated bytes change; an artificial case remains after its sole surface is removed; marketplace production wiring is pulled forward; or the transition/cache semantics drift.</fails_when></verify>
  <acceptance_criteria>Schema-2 file invalidation stays a live, exact, real-filesystem contract regardless of the memory-reader census result (D-05-05, D-05-07).</acceptance_criteria>
  <reversibility rating="costly">The schema-2 path is a cross-process persisted contract retained across later invalidator wiring; changing it would require coordinated migration.</reversibility>
  <done>Marketplace names retain authoritative reads and real schema-2 invalidation without artificial memory ownership.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                       |
| ----------------------------------------------------- | --------------------------------------------------------------------------------- |
| Production-consumer census to public-surface deletion | An incomplete census can delete genuine behavior or leave dead state reachable.   |
| Authoritative state to completion names               | Current project/user records determine public suggestions and error behavior.     |
| Scoped cache path to file unlink                      | Invalidation must affect only the schema-2 artifact and preserve unrelated files. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                  | Severity | Disposition | Mitigation Plan                                                                                             |
| --------- | ---------------------------------- | -------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-05-31   | Tampering / Repudiation            | census-driven removal      | high     | mitigate    | Record CodeGraph and production text consumers immediately before deletion and branch safely on the result. |
| T-05-32   | Tampering                          | authoritative name rebuild | high     | mitigate    | Preserve direct scoped state reads, precedence, deduplication, order, and original failures.                |
| T-05-33   | Tampering / Elevation of privilege | names-file invalidation    | high     | mitigate    | Retain the derived scoped path, unlink only that file, and assert foreign bytes remain unchanged.           |
| T-05-34   | Denial of service                  | missing cache file         | low      | accept      | ENOENT remains an idempotent success because absence already represents the desired state.                  |

ASVS Level 1: census integrity, authoritative candidates, and scoped deletion are high-severity blocking threats. This plan changes no authentication, package, or external service.
</threat_model>

## Source coverage audit

| Source   | ID                  | Feature or constraint                                                 | Plan/task         | Status  | Notes                                                                            |
| -------- | ------------------- | --------------------------------------------------------------------- | ----------------- | ------- | -------------------------------------------------------------------------------- |
| GOAL     | -                   | Correctly owned state and public contracts                            | 05-08 / Tasks 1-2 | COVERED | Dead memory follows evidence; live authoritative and disk behavior remains.      |
| REQ      | TREF-05             | Mutable completion state has factory ownership                        | 05-08 / Task 1    | COVERED | Unconsumed names memory is removed or a genuine reader migrates to the cache.    |
| REQ      | TREF-06             | Test-shaped surfaces follow trace-preserving removal                  | 05-08 / Tasks 1-2 | COVERED | Artificial cases disappear only with dead behavior; public invalidation remains. |
| RESEARCH | resolved condition  | Fresh census controls the reader/map disposition                      | 05-08 / Task 1    | COVERED | Both safe branches are explicit.                                                 |
| RESEARCH | live names artifact | Preserve schema 2, path, authoritative rebuild, and real invalidation | 05-08 / Task 2    | COVERED | Owner and marketplace-add suites cover exact public effects.                     |
| CONTEXT  | D-05-05             | Completion state has legitimate factory ownership                     | 05-08 / Tasks 1-2 | COVERED | No second memory lifetime appears.                                               |
| CONTEXT  | D-05-07             | Remove only terminal test-shaped or dead surfaces                     | 05-08 / Tasks 1-2 | COVERED | A current production census precedes any deletion.                               |

Plugin-index ownership is covered by Plan 05-07. Final cache composition, invalidators, reset migration, and transition removal remain assigned to Plans 05-12 and 05-19 through 05-31. Phase 6 work and other deferred areas are exclusions.

<verification>
Run both task commands and `npm run check`. Confirm exactly seven frontmatter files, recorded census evidence, a safe reader/map disposition, unchanged authoritative completion and schema-2 invalidation, no second cache lifetime or new reset, retained Plan 05-07 transition for Plan 05-12, no suppression, and no Phase 6 work.
</verification>

<success_criteria>

- A fresh production census governs removal or migration of marketplace-name memory.
- Authoritative project/user completion behavior remains exact.
- Schema 2, scoped path, real invalidation, ENOENT behavior, add outcomes, state/tree, and notifications remain exact.
- Plugin-index and transition behavior from Plan 05-07 remains unchanged until Plan 05-12.
- No live contract is deleted and no dead/test-only/default/optional replacement appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-08-SUMMARY.md` when done.
</output>
