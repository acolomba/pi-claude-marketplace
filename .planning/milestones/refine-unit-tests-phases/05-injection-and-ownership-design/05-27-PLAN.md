---
phase: 05-injection-and-ownership-design
plan: 27
type: execute
wave: 21
depends_on: [05-25, 05-26]
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - tests/edge/register.test.ts
  - tests/edge/handlers/plugin/bootstrap.test.ts
  - tests/orchestrators/plugin/bootstrap.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
autonomous: true
requirements: [TREF-04, TREF-05, TREF-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "bootstrapClaudePlugin and applyReconcile remain exactly the two behavioral-composition exceptions on the final runtime/cache graph (D-05-02)."
    - "Bootstrap calls real marketplace add and then real autoupdate with one lifecycle graph and no bespoke dependency-injection seam."
    - "Apply calls its real mutation children with one lifecycle graph and only the independently classified state reader (D-05-01)."
    - "Both owners retain complete exact state, configuration, scope-tree, results, ordering, failures, silence, and notification assertions."
    - "The cache/runtime refactor adds no interaction-only replacement and preserves exact cross-operation effects (D-05-05)."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
    - tests/edge/register.test.ts
    - tests/edge/handlers/plugin/bootstrap.test.ts
    - tests/orchestrators/plugin/bootstrap.test.ts
    - tests/orchestrators/reconcile/apply.test.ts
  key_links:
    - "registered bootstrap -> real addMarketplace -> real autoupdate -> one runtime/cache graph"
    - "applyReconcile -> real mutation children -> exact two-scope state/tree/results"
    - "exact composition census -> bootstrapClaudePlugin + applyReconcile only"
  prohibitions:
    - "No bespoke bootstrap/apply child bundle, __deps, ForTest, test-only reset/export, optional/default/dead port, raw state, or second lifetime."
    - "No third behavioral-composition exception or weakened public-effect assertion."
    - "No Phase 6 builtin-patch removal/split, excluded decomposition, or new Fallow suppression."
---

<objective>
Preserve exactly the two behavioral-composition exceptions on the final graph: bootstrap still calls real add then autoupdate, and apply still calls real children; both retain complete exact state/config/scope-tree/results, ordering, failures, silence, and notifications (D-05-01, D-05-02, D-05-05).

Purpose: Prove that explicit ownership did not replace the two deliberately selected end-to-end composition contracts.
Output: Final bootstrap and apply composition evidence on one lifecycle graph plus an exact two-exception census.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-injection-and-ownership-design/05-CONTEXT.md
@.planning/phases/05-injection-and-ownership-design/05-RESEARCH.md
@.planning/phases/05-injection-and-ownership-design/05-PATTERNS.md
@.planning/phases/05-injection-and-ownership-design/05-VALIDATION.md
@.planning/phases/05-injection-and-ownership-design/05-25-SUMMARY.md
@.planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md
</context>

## Edge assumptions

- Use the completed runtime/cache graph and executed mutation contracts from dependency summaries.
- Bootstrap remains real add followed by real autoupdate. Apply keeps every real child plus its narrow state reader.
- Runtime/cache forwarding is legitimate only where real production children consume it. Neither exception receives a test-specific bundle.
- Public state/configuration/scope trees, results, failures, silence, and exact notifications remain primary; call order only supplements them.
- Keep every Phase 6 and explicit exclusion boundary intact.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve bootstrap as real add-then-autoupdate composition</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-25-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts, extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts, tests/edge/register.test.ts, tests/edge/handlers/plugin/bootstrap.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts, extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts, tests/edge/register.test.ts, tests/edge/handlers/plugin/bootstrap.test.ts, tests/orchestrators/plugin/bootstrap.test.ts</files>
  <behavior>
    - "Registered bootstrap carries the root runtime/cache into real marketplace add and then real autoupdate."
    - "No bootstrap-specific child seam, operation bundle, or replacement of either real child exists."
    - "Complete state/configuration, both scope trees, cache/completion effects, results, errors, target order, and notifications remain exact."
    - "Add failure, autoupdate failure, partial success, no-effect, and silence paths retain current behavior."
  </behavior>
  <action>Thread the completed lifecycle graph through edge bootstrap registration and the real `bootstrapClaudePlugin` flow only as required by its existing add and autoupdate children. Preserve the exact real-child order: marketplace add completes before autoupdate begins. Keep source/scope parsing, target order, state/configuration writes, route/cache effects, partial and failure projection, silence, and exact notifications. Retain complete filesystem and public-result assertions before any order evidence. Per D-05-02, do not introduce a bootstrap dependencies bundle or replace either child. Add no injection/reset surface, and keep Phase 6 splits and builtin synchronization untouched.</action>
  <verify><automated>node --test tests/edge/register.test.ts tests/edge/handlers/plugin/bootstrap.test.ts tests/orchestrators/plugin/bootstrap.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/edge/register.ts extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts tests/edge/register.test.ts tests/edge/handlers/plugin/bootstrap.test.ts tests/orchestrators/plugin/bootstrap.test.ts --max-warnings=0</automated><fails_when>A suite fails or discovers zero tests; a real child is replaced/bundled; add/autoupdate order changes; runtime/cache identity changes; complete state/configuration/trees/results/errors/silence/notifications weaken; a new seam or third exception appears; or Phase 6/excluded work appears.</fails_when></verify>
  <acceptance_criteria>bootstrapClaudePlugin remains a full-observation real add-then-autoupdate composition on one lifecycle graph (D-05-01, D-05-02, D-05-05).</acceptance_criteria>
  <reversibility rating="costly">Bootstrap intentionally spans real child boundaries; changing its composition proof affects add, update, cache, runtime, and edge owners.</reversibility>
  <done>Bootstrap retains exact real-child ordering and complete public state, tree, result, failure, silence, and notification evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve apply real children and seal the two-exception census</name>
  <read_first>.planning/phases/05-injection-and-ownership-design/05-25-SUMMARY.md, .planning/phases/05-injection-and-ownership-design/05-26-SUMMARY.md, extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/edge/register.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/orchestrators/plugin/bootstrap.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts</read_first>
  <files>extensions/pi-claude-marketplace/edge/register.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/edge/register.test.ts, tests/orchestrators/reconcile/apply.test.ts</files>
  <behavior>
    - "Apply receives the root runtime/cache and keeps its required state reader separate from every real mutation child."
    - "Install, uninstall, enable/disable, update/reinstall/backfill, and marketplace children remain real with exact project-before-user behavior."
    - "Complete state/configuration, both scope trees, routes/cache rows, outcomes, errors/redaction, silence, and notification bytes remain exact."
    - "A production/test census identifies exactly applyReconcile and bootstrapClaudePlugin as behavioral-composition exceptions."
  </behavior>
  <action>Audit and complete lifecycle-graph forwarding through apply without replacing, grouping, or defaulting any child. Preserve the independently authorized state reader, lock/read/plan/apply order, project-before-user scope order, sibling failure isolation, backfill position, state/tree/routes/cache effects, result projection, redaction, silence, and exact notifications. Add or retain full public assertions before call order. Run a source/test census for composition-specific factories or child substitution and record exactly `applyReconcile` and `bootstrapClaudePlugin` in the summary; fail if any third exception exists or either named flow is absent. Per D-05-02, remove no behavior merely to satisfy the census. Add no test seam, suppression, or Phase 6/excluded change.</action>
  <verify><automated>node --test tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/plugin/bootstrap.test.ts tests/edge/register.test.ts tests/integration/hooks-cross-scope-reconcile.test.ts tests/integration/transaction-lifecycle-cascade.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/register.ts &amp;&amp; npm run typecheck &amp;&amp; npx eslint extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts extensions/pi-claude-marketplace/edge/register.ts tests/orchestrators/reconcile/apply.test.ts tests/edge/register.test.ts --max-warnings=0 &amp;&amp; npm run fallow</automated><fails_when>A command fails or discovers zero tests; apply replaces/bundles/defaults a child; the state reader widens; scope/order/state/tree/routes/cache rows/results/errors/silence/notifications weaken; the census differs from exactly two named exceptions; or a seam, suppression, split, or builtin-patch removal appears.</fails_when></verify>
  <acceptance_criteria>applyReconcile remains the second full-observation real-child composition, and the exact exception set contains only applyReconcile and bootstrapClaudePlugin (D-05-01, D-05-02, D-05-05).</acceptance_criteria>
  <reversibility rating="costly">The exact exception boundary protects broad end-to-end behavior while other tests use narrow ports; changing it retouches many owners and review assumptions.</reversibility>
  <done>The final graph preserves exactly two composition exceptions with complete public observations and no third exception.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                              | Description                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------- |
| Bootstrap -> real add/autoupdate      | Replacement can hide state, tree, cache, route, or ordering defects.                    |
| Apply -> real mutation children       | A broad bundle can hide rollback, scope isolation, and notification defects.            |
| Test architecture -> exception census | Drift can silently expand privileged composition or erase required end-to-end evidence. |

## STRIDE threat register

| Threat ID | Category                | Component                         | Severity | Disposition | Mitigation plan                                                                                           |
| --------- | ----------------------- | --------------------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------------------- |
| T-05-118  | Tampering / Repudiation | bootstrap composition             | high     | mitigate    | Keep real add then autoupdate and assert complete state/config/tree/result/failure/notification behavior. |
| T-05-119  | Tampering / Repudiation | apply composition                 | high     | mitigate    | Keep every real child plus exact two-scope state/tree/route/cache/result evidence.                        |
| T-05-120  | Tampering               | exception census                  | high     | mitigate    | Record exactly the two named exceptions and fail on absence or expansion.                                 |
| T-05-121  | Information disclosure  | error and notification projection | medium   | mitigate    | Preserve exact redaction, silence, scope attribution, and notification bytes.                             |

ASVS Level 1: real-child composition and exception-census integrity are high-severity blocking threats.
</threat_model>

## Source coverage audit

| Source   | ID                        | Feature or constraint                                | Plan/task         | Status  | Notes                                                           |
| -------- | ------------------------- | ---------------------------------------------------- | ----------------- | ------- | --------------------------------------------------------------- |
| GOAL     | -                         | Legitimate seams with public contracts               | 05-27 / Tasks 1-2 | COVERED | Both selected compositions remain end to end.                   |
| REQ      | TREF-04                   | Preserve exactly two behavioral compositions         | 05-27 / Tasks 1-2 | COVERED | Real children and full observations remain.                     |
| REQ      | TREF-05                   | Use the final lifecycle graph                        | 05-27 / Tasks 1-2 | COVERED | Runtime/cache reach genuine child consumers.                    |
| REQ      | TREF-06                   | Public outcomes replace test-shaped access           | 05-27 / Tasks 1-2 | COVERED | Exact state, trees, rows, results, and messages remain primary. |
| RESEARCH | composition exceptions    | Bootstrap and apply only                             | 05-27 / Tasks 1-2 | COVERED | The summary records the exact census.                           |
| CONTEXT  | D-05-01, D-05-02, D-05-05 | Narrow ports plus two full compositions on one cache | 05-27 / Tasks 1-2 | COVERED | No bespoke bundle or third exception appears.                   |

Reset caller migration/removal and BOOLEAN_FLAGS cleanup remain Plans 05-28 through 05-32. Final eleven-root/two-exception closure remains Plan 05-33. Phase 6 and exclusions are not gaps.

<verification>
Run both task commands and `npm run check`. Confirm only the eight frontmatter files change, Task 1 changes six files and Task 2 changes four, bootstrap remains real add then autoupdate, apply keeps real children, and the exception census equals exactly two. Confirm `scripts/revalidation.mjs` retains exactly `// fallow-ignore-next-line complexity -- temporary; remove after Phase 01-71 refactor` and no suppression is added.
</verification>

<success_criteria>

- bootstrapClaudePlugin calls real add then real autoupdate on one lifecycle graph.
- applyReconcile calls real children with only its narrow state reader.
- Both retain complete exact state, configuration, scope-tree, result, order, failure, silence, and notification evidence.
- The composition-exception census contains exactly those two flows.
- No new test seam, third exception, weakened assertion, suppression, Phase 6 work, or excluded change appears.

</success_criteria>

<output>
Create `.planning/phases/05-injection-and-ownership-design/05-27-SUMMARY.md` when done.
</output>
