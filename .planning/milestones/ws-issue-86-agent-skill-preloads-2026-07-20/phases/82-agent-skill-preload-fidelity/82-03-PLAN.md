---
phase: 82-agent-skill-preload-fidelity
plan: 03
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGSK-02, AGSK-03]

must_haves:
  truths:
    - "AGSK-02: a `skills:` token qualified with the plugin's own name (`spec-tree:review-changes`) maps to the same generated Pi skill name as its bare form; a cross-plugin qualifier (`other-plugin:some-skill`) is dropped with a warning naming the full token"
    - "Stripped-qualifier remainders that would make generatedSkillName throw (empty, `.`, `..`) warn-and-drop instead of throwing — no new install-failure mode (82-RESEARCH.md Pitfall 2)"
    - "D-82-08: the new dropped-tool warning fires ONLY when the dropped token is exactly `Skill`; all other dropped tools keep today's silent droppedTools behavior"
    - "D-82-09: the Skill-drop warning is the exact string `dropped tool \"Skill\" -- generated agents run with skills discovery disabled (inheritSkills: false); only the skills listed in skills: are preloaded into the child's context`"
    - "Warnings aggregate order is unchanged (description -> model -> tools -> thinking -> skills); the Skill warning lands in the tools slot, cross-plugin warnings in the skills slot"
    - "The byte-identity corpus from plan 82-01 is untouched and green"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      provides: "mapSkills qualifier stripping + cross-plugin warn-drop + throw guards; mapTools Skill-drop warning"
      contains: "dropped tool \"Skill\""
    - path: "tests/bridges/agents/convert.test.ts"
      provides: "Pins: qualified/bare convergence, cross-plugin drop, no-throw guards, exact D-82-09 string, silent non-Skill drops, warnings order, provenance sanitization"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      to: "extensions/pi-claude-marketplace/domain/name.ts"
      via: "qualifier-stripped remainder delegated to generatedSkillName (name.ts stays read-only)"
      pattern: "generatedSkillName"
    - from: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      via: "warnings arrays rendered into the provenance comment through formatOptionalProvenanceList -> sanitizeProvenance"
      pattern: "warnings"
---

<objective>
Implement AGSK-02 and AGSK-03 in `extensions/pi-claude-marketplace/bridges/agents/convert.ts`: plugin-qualified skill mapping in `mapSkills` (same-plugin qualifier strips and delegates to `generatedSkillName`; cross-plugin qualifier warns-and-drops naming the token) and the D-82-09 provenance warning in `mapTools` fired only when the `Skill` tool is dropped (D-82-08).

Purpose: the issue #86 agent's `spec-tree:review-changes` preload currently never matches knownSkills (mapping half of the bug), and the child LLM's parent gets no explanation for the dropped `Skill` tool.

Output: qualifier-aware mapSkills, Skill-aware mapTools, exact-string test pins; byte-identity corpus still green.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` — the orchestrator commits.
- Do NOT touch `frontmatter.ts` or `frontmatter.test.ts` (owned by plan 82-02 in the same wave). `domain/name.ts` is read-only.
- ASCII-only string literals (the D-82-09 string uses ASCII ` -- ` and is commit-safe verbatim).
- Comment/test-title anchors: `AGSK-02`, `AGSK-03`, `D-82-08`, `D-82-09`, `#86` — never `Phase 82`/plan/wave references.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@extensions/pi-claude-marketplace/domain/name.ts
@tests/bridges/agents/convert.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Qualifier handling in mapSkills (AGSK-02)</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (mapSkills ~lines 256-279; splitCsv ~79-108; warning-string convention at lines 144 and 274; convertAgent warnings assembly ~319-361)
    - extensions/pi-claude-marketplace/domain/name.ts (generatedSkillName ~64-77 prefix elision; assertSafeName ~23-51 — THROWS on empty, `.`, `..`, path separators, control chars)
    - tests/bridges/agents/convert.test.ts (makeDiscovered builder ~17-28; warning-assertion shape ~156-167; AG-11 throw-assertion shape ~169-189)
    - 82-RESEARCH.md Pattern 2 (qualifier sketch + critical guard) and Pitfall 2
  </read_first>
  <behavior>
    - Test 1 (AGSK-02/#86): pluginName "spec-tree", knownSkills ["spec-tree-review-changes"], raw skills `spec-tree:review-changes` -> generated frontmatter contains `skills: spec-tree-review-changes` (identical result to bare `review-changes`)
    - Test 2 (AGSK-02): qualified redundant-prefix form `spec-tree:spec-tree-review-changes` converges on the same `spec-tree-review-changes` (generatedSkillName elision preserved through stripping)
    - Test 3 (AGSK-02): cross-plugin token `other-plugin:some-skill` -> warnings contains the EXACT string `skill reference "other-plugin:some-skill" is qualified with a different plugin -- dropped (only this plugin's skills can be preloaded)`, no `skills:` frontmatter line is emitted for it, and NO additional `unknown skill reference` warning fires for the same token
    - Test 4 (no-throw guard): raw skills `spec-tree:` -> convertAgent does NOT throw; warnings contains `unknown skill reference "spec-tree:" -- dropped`
    - Test 5 (no-throw guard): raw skills `spec-tree:.` and `spec-tree:..` -> convertAgent does NOT throw; each warn-drops with the unknown-skill wording naming the FULL original token
    - Test 6 (unchanged bare behavior): bare unknown `phantom` still warns `unknown skill reference "phantom" -- dropped`; bare known name still emits; duplicate bare tokens (`knowledge,knowledge`, knownSkills ["acme-knowledge"]) still emit `skills: acme-knowledge,acme-knowledge` — NO dedupe added
    - Test 7 (provenance sanitization, threat T-82-07): cross-plugin token `evil-->qual:skill` -> the generated fileContent contains exactly ONE `-->` occurrence (the provenance comment terminator); the crafted token cannot close the HTML comment early
  </behavior>
  <action>
    In `mapSkills`, process each token from splitCsv: find `token.indexOf(":")`. No colon -> today's exact path (delegate the whole token). Colon present -> split into qualifier (before first colon) and rest (after). If qualifier !== pluginName: push the cross-plugin warning (exact template: `skill reference "${token}" is qualified with a different plugin -- dropped (only this plugin's skills can be preloaded)`) and continue — never fall through to the unknown-skill warning. If qualifier === pluginName: guard the rest BEFORE delegation — when rest is `""`, `"."`, or `".."`, push `unknown skill reference "${token}" -- dropped` (full original token) and continue (assertSafeName would throw on these; a warn-drop must never become a throw). Otherwise call `generatedSkillName(pluginName, rest)` and keep today's knownSkills-membership emit/warn logic, with the unknown-skill warning still naming the FULL original token (e.g. `spec-tree:phantom`), not the stripped remainder. Do NOT add dedupe to the emit list (byte-identity for duplicate-bearing bare-form agents; pi-subagents dedupes downstream).

    Write the behavior tests first through `convertAgent` (all mapping helpers stay private, per the module's existing convention), confirm Tests 1-5 fail against unmodified code and 6 passes, then implement until all green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior Tests 1-7 pass; every pre-existing convert.test.ts test still passes
    - `assert.doesNotThrow` guards prove `spec-tree:`, `spec-tree:.`, `spec-tree:..` inputs convert without throwing (each with its warning present)
    - The cross-plugin warning string is pinned with an exact-equality or exact-substring assertion of the full sentence (not a loose regex)
    - Byte-identity corpus green and untouched (`git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts` exits 0)
    - `domain/name.ts` has no diff (`git diff --exit-code extensions/pi-claude-marketplace/domain/name.ts` exits 0)
  </acceptance_criteria>
  <done>Same-plugin qualified skills map like bare forms; cross-plugin qualifiers warn-and-drop naming the token; no throw paths introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skill-drop provenance warning in mapTools (AGSK-03)</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, tests/bridges/agents/convert.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (mapTools ~lines 148-205: drop loop ~168-177, omitted-tools default warning ~161-164, disallowedTools handling ~181-189; TOOL_MAP ~53-61; convertAgent warnings slots ~319-361)
    - tests/bridges/agents/convert.test.ts (exact-equality snapshot convention ~265-278)
    - 82-RESEARCH.md Pattern 3 (placement rationale + interaction notes) and Pitfall 1 (warnings order is provenance-visible bytes)
  </read_first>
  <behavior>
    - Test 1 (AGSK-03/D-82-09): raw tools `Bash, Read, Skill` -> provenance `droppedTools` contains `Skill`, generated `tools:` line is `tools: bash,read` (source order), and warnings contains the EXACT string `dropped tool "Skill" -- generated agents run with skills discovery disabled (inheritSkills: false); only the skills listed in skills: are preloaded into the child's context`
    - Test 2 (D-82-08): raw tools `Read,WebFetch` -> `WebFetch` lands in droppedTools and warnings contains NO string starting with `dropped tool` (other drops stay silent)
    - Test 3 (D-82-08 interaction): raw with NO tools key -> only today's omitted-tools default warning; no Skill warning (default `read,bash,edit` contains no Skill)
    - Test 4 (interaction): raw tools `Read,Bash` with disallowedTools `Skill` -> no droppedTools entry for Skill, no Skill warning (disallowed tokens not in TOOL_MAP are ignored today; Skill/disallowedTools semantics belong to AGSK-05, out of scope here)
    - Test 5 (warnings order, Pitfall 1): raw tools `Bash, Read, Skill` + raw skills `other-plugin:x` -> `assert.deepEqual(out.warnings, [<exact D-82-09 string>, <exact cross-plugin string for "other-plugin:x">])` — tools slot strictly before skills slot
  </behavior>
  <action>
    In `mapTools`' drop loop, when the dropped token is exactly `Skill` (case-sensitive, matching TOOL_MAP's exact-match convention), push the D-82-09 string (verbatim from Test 1 above — it is ASCII-safe) into mapTools' own `warnings` array, after the token is added to `dropped`. No other dropped token gets a warning (D-82-08 — warning every drop would change output bytes for agents this phase must leave byte-identical). Because the warning is pushed inside mapTools, convertAgent's existing `warnings.push(...toolsResult.warnings)` places it in the tools slot with zero aggregation changes.

    Write the behavior tests first, confirm Tests 1 and 5 fail against unmodified code and 2-4 pass, then implement until all green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - The D-82-09 warning string in convert.ts is byte-identical to the string pinned in Test 1 (single source literal; test asserts exact equality, not substring)
    - Test 5's `assert.deepEqual` on the FULL `out.warnings` array passes, pinning aggregate order
    - Dropping any non-Skill tool produces zero new warnings (Test 2)
    - `npm run check` passes; byte-identity corpus green and untouched
    - `git status --porcelain` shows changes only to `extensions/pi-claude-marketplace/bridges/agents/convert.ts` and `tests/bridges/agents/convert.test.ts`
  </acceptance_criteria>
  <done>Skill drops are explained in provenance with the exact D-82-09 wording; all other drops byte-identical to today.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin frontmatter values -> mapping/provenance | Untrusted `skills:`/`tools:` tokens flow into warnings strings that render inside the provenance HTML comment of the generated agent file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-06 | DoS | assertSafeName throw on stripped remainders (`plugin:`, `plugin:.`, `plugin:..`) turning warn-drop into install failure | mitigate | Explicit guard before generatedSkillName delegation (Task 1); pinned by doesNotThrow tests — 82-RESEARCH.md Pitfall 2 |
| T-82-07 | Tampering | Provenance HTML-comment breakout via `-->` inside a crafted cross-plugin token echoed in a warning | mitigate | Warnings already pass through formatOptionalProvenanceList -> sanitizeProvenance in frontmatter.ts (inherited, no new code); verified by Task 1 Test 7 (exactly one `-->` in fileContent) |
| T-82-08 | Tampering | Unvalidated names reaching generated `skills:` frontmatter | mitigate | Emit list still requires knownSkills membership after generatedSkillName (allowlist unchanged); cross-plugin and malformed tokens can never reach the emit path |
| T-82-09 | Information Disclosure | Warning text echoing token content | accept | Tokens originate from the plugin's own files and render only into that plugin's generated agent provenance; no cross-tenant data exists |

No packages installed; no network.
</threat_model>

<verification>
- `node --test tests/bridges/agents/convert.test.ts` green (new + pre-existing)
- `node --test tests/bridges/agents/convert-byte-identity.test.ts` green with `git diff --exit-code` on that file
- `npm run check` green (NFR-6)
</verification>

<success_criteria>
- Phase SC-2: `droppedTools: Skill` conversions carry the exact D-82-09 warning
- Phase SC-3: cross-plugin qualified entries warn-and-drop naming the token; bare names map exactly as today
- Mapping half of SC-1: `spec-tree:review-changes` emits `skills: spec-tree-review-changes`
- No new throw paths; warnings order pinned; corpus untouched
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-03-SUMMARY.md` when done.
</output>
