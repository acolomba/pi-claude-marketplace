---
phase: 82-agent-skill-preload-fidelity
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
  - tests/bridges/agents/frontmatter.test.ts
autonomous: true
requirements: [AGSK-01]

must_haves:
  truths:
    - "D-82-01: a frontmatter key with an empty inline value followed by `- item` lines folds the items into that key's CSV value for ANY key; unsupported list-valued keys later land in droppedFields as one clean key name with no bogus `- token` fragments"
    - "D-82-02: the lenient line-based parser is kept — no YAML dependency is added; AG-6 tolerance (colon in unquoted values) still passes"
    - "D-82-03: mixed form (inline value + dash items under one key) resolves as inline-value-wins / dash-items-ignored, documented in the parser comment and pinned by a test"
    - "The issue #86 dash line `- spec-tree:review-changes` folds verbatim into raw.skills (no first-colon split of dash items)"
    - "The byte-identity corpus from plan 82-01 is untouched and green (AGSK-01 CSV/inline-array byte-identity clause)"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      provides: "Dash-list folding in parseFrontmatter; updated module header and parseFrontmatter doc comment describing folding and the D-82-03 choice"
      contains: "D-82-03"
    - path: "tests/bridges/agents/frontmatter.test.ts"
      provides: "Parser pins: colon-bearing fold, multi-item fold, clean unsupported-key raw output, mixed form, orphan dash, bare dash, CRLF twin, existing-forms-unchanged"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      via: "folded values are comma-joined strings consumed by splitCsv with zero splitCsv changes"
      pattern: "raw\\["
---

<objective>
Implement AGSK-01: extend `parseFrontmatter` in `extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` with dash-list folding so the documented Claude block-list `skills:`/`tools:` form parses with list values intact (D-82-01), while keeping the lenient line-based parser with no YAML dependency (D-82-02) and pinning the mixed-form choice (D-82-03).

Purpose: today the line `  - spec-tree:review-changes` is split on its FIRST colon into a bogus `- spec-tree` key, which lands in droppedFields and empties `raw.skills` — the root mechanism of issue #86.

Output: dash-folding parser + updated comments + parser test pins; byte-identity corpus still green.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` — the orchestrator commits.
- Do NOT touch `convert.ts` or `convert.test.ts` (owned by plan 82-03 in the same wave).
- ASCII-only string literals in .ts sources/tests (fix-unicode-dashes hook rewrites U+2014).
- Comment/test-title anchors: `AGSK-01`, `D-82-01`, `D-82-02`, `D-82-03`, `#86`, `AG-6` — never `Phase 82`/plan/wave references.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@tests/bridges/agents/frontmatter.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dash-list folding in parseFrontmatter (tests + implementation)</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (the parse loop at ~lines 95-114: trim, blank-skip, indexOf(":"), first-colon split; module header contract lines ~8-18)
    - tests/bridges/agents/frontmatter.test.ts (AG-6 inline frontmatter-string test shape at ~lines 14-25; CRLF analog at ~lines 47-53)
    - 82-RESEARCH.md Pattern 1 (fold sketch: lastKey/lastKeyFoldable tracking, comma-join, continue before the colon path) and its byte-identity class table
  </read_first>
  <behavior>
    - Test 1 (AGSK-01/#86): input `---\nskills:\n  - spec-tree:review-changes\n---\n\nBody.\n` (plus name/description lines) yields `raw.skills === "spec-tree:review-changes"` — colon-bearing dash items fold verbatim, never colon-split
    - Test 2 (D-82-01): `tools:\n  - Read\n  - Bash\n` yields `raw.tools === "Read,Bash"` (comma-joined, source order)
    - Test 3 (D-82-01): unsupported list key `hooks:\n  - a:b\n  - c\n` yields raw containing key `hooks` with value `a:b,c` and NO key beginning with `- ` (clean droppedFields precondition)
    - Test 4 (D-82-03): mixed form `tools: Read\n  - Edit\n` yields `raw.tools === "Read"` — inline value wins, dash items under a non-empty value are ignored
    - Test 5: orphan dash items (a `- x` line before any key) are ignored; a bare `-` line is ignored
    - Test 6 (CRLF twin): Test 2's input with `\r\n` line endings yields the same `raw.tools === "Read,Bash"`
    - Test 7 (unchanged forms): CSV (`tools: Read,Bash`), inline-array (`tools: ["Read", "Bash"]`), and an empty-value key with no dash items (`skills:` followed directly by another `key: value` line -> `raw.skills === ""`) all parse exactly as today
  </behavior>
  <action>
    In `parseFrontmatter`'s line loop, add fold state per 82-RESEARCH.md Pattern 1: `lastKey: string | null` and `lastKeyFoldable: boolean` (true only when the key's inline value was empty). Handle dash lines (`line.startsWith("- ")` or `line === "-"`) BEFORE the `indexOf(":")` colon split and `continue` so dash lines never reach the colon path. Fold rule: item text is everything after `- `, trimmed, taken verbatim (no colon split); append into `raw[lastKey]` comma-joined (`item` when the value is empty, else `value + "," + item`). Ignore: dash items when `lastKey === null`, when `lastKeyFoldable` is false (D-82-03: inline wins), and empty items (`-` alone). On every successful `key: value` parse, update `lastKey = key`, `lastKeyFoldable = value === ""`. A folded key stays foldable (subsequent `- item` lines keep appending).

    Behavior contract notes to encode: known limitation that items containing literal commas would split downstream (document in comment, D-82-02's 27-agent scan found none); quoted items like `- "quoted-item"` fold with quotes intact because `splitCsv` in convert.ts strips per-item quotes downstream — do NOT strip quotes in the parser.

    Write the tests in the <behavior> list first in `tests/bridges/agents/frontmatter.test.ts` using the existing inline frontmatter-string shape, confirm the new ones fail against the unmodified parser (except Test 7, which must already pass), then implement until all green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/frontmatter.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 7 behavior tests pass; every pre-existing test in frontmatter.test.ts still passes (AG-6 colon-tolerance and CRLF tests included)
    - `parseFrontmatter` output for the issue #86 frontmatter (`skills:` + `  - spec-tree:review-changes`) is `raw.skills === "spec-tree:review-changes"` with no `- spec-tree` key present in raw
    - `node --test tests/bridges/agents/convert-byte-identity.test.ts` passes and `git diff --exit-code tests/bridges/agents/convert-byte-identity.test.ts` exits 0 (corpus untouched)
    - No import of any YAML library appears in frontmatter.ts (`! grep -q "js-yaml\|from \"yaml\"" extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts`) — D-82-02
  </acceptance_criteria>
  <done>Block-list frontmatter parses with values intact; existing forms and corpus bytes unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update stale parser comments and run the full quality gate</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (module header ~lines 8-18 and parseFrontmatter doc comment ~lines 71-75 — both currently claim "no list-of-dash arrays", now stale)
    - .claude/rules/typescript-comments.md (anchor policy)
  </read_first>
  <action>
    Update both stale comments: the module header's "no list-of-dash arrays" sentence and the `parseFrontmatter` doc comment must now describe (a) dash-list folding — a key with an empty inline value followed by `- item` lines folds items into a comma-joined value, any key (D-82-01, AGSK-01, #86); (b) the mixed-form rule — an inline value wins and subsequent dash items are ignored (D-82-03); (c) the known comma-in-item limitation. Keep the existing rationale that the parser deliberately mirrors pi-subagents' line-based parsing rather than real YAML (D-82-02). Anchor with `AGSK-01` / `D-82-01` / `D-82-03` / `#86` identifiers, never GSD phase/plan references.
  </action>
  <verify>
    <automated>! grep -q "no list-of-dash arrays" extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts && grep -q "D-82-03" extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - The stale phrase `no list-of-dash arrays` no longer appears anywhere in frontmatter.ts
    - The literal token `D-82-03` appears in a frontmatter.ts comment describing the inline-wins mixed-form rule
    - `npm run check` passes end to end (typecheck + ESLint + Prettier + unit + integration)
    - `git status --porcelain` shows changes only to `extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` and `tests/bridges/agents/frontmatter.test.ts`
  </acceptance_criteria>
  <done>Parser comments document the new contract; full check green; plan file footprint is exactly the two owned files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent .md -> parseFrontmatter | Untrusted plugin frontmatter text crosses into the parser; folded values flow onward to convert.ts mapping (which validates via TOOL_MAP allowlist / knownSkills membership / assertSafeName) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03 | Tampering | Crafted dash items injecting content into generated frontmatter | mitigate | Folded values are plain strings entering the SAME downstream validation as CSV values today (splitCsv -> TOOL_MAP/knownSkills allowlists; provenance passes through sanitizeProvenance). Test 3 pins that unsupported keys cannot smuggle `- token` keys into raw |
| T-82-04 | DoS | Pathological frontmatter (thousands of dash lines) | accept | Linear single-pass string loop, no regex backtracking, no recursion; bounded by file size already read into memory by discover.ts — no new asymptotic cost |
| T-82-05 | Spoofing | None — no identity surface in parsing | accept | No authn/authz in scope (ASVS V2/V3/V4 not applicable per 82-RESEARCH.md security domain) |

No packages installed (D-82-02 forbids YAML deps); no network.
</threat_model>

<verification>
- `node --test tests/bridges/agents/frontmatter.test.ts` green (new + pre-existing)
- `node --test tests/bridges/agents/convert-byte-identity.test.ts` green with `git diff --exit-code` on that file
- `npm run check` green (NFR-6)
</verification>

<success_criteria>
- Issue #86 block-list frontmatter parses to `raw.skills === "spec-tree:review-changes"` (phase SC-1 parser half)
- All D-82-01/D-82-02/D-82-03 truths hold and are pinned by tests
- CSV, inline-array, empty-value, CRLF forms parse byte-identically (phase SC-5 parser half); corpus untouched
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-02-SUMMARY.md` when done.
</output>
