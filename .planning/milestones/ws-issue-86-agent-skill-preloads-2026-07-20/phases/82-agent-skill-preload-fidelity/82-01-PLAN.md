---
phase: 82-agent-skill-preload-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/agents/convert-byte-identity.test.ts
autonomous: true
requirements: [AGSK-01, AGSK-04]

must_haves:
  truths:
    - "Running the new byte-identity test against unmodified HEAD passes: seven full-fileContent exact-equality pins capture today's generated output (AGSK-01 CSV/inline-array byte-identity clause; AGSK-04 token-free-body byte-identity clause)"
    - "None of the seven corpus classes contains a Phase 82 fix trigger: no dash block lists, no plugin-qualified skills tokens, no Skill tool, no body skill tokens"
    - "The corpus file survives untouched through every later plan (later plans assert `git diff --exit-code` on it)"
  artifacts:
    - path: "tests/bridges/agents/convert-byte-identity.test.ts"
      provides: "Full-fileContent regression pins for the seven unchanged input classes, captured at HEAD before any implementation change"
      min_lines: 80
      contains: "assert.equal"
  key_links:
    - from: "tests/bridges/agents/convert-byte-identity.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      via: "convertAgent import"
      pattern: "convertAgent"
    - from: "tests/bridges/agents/convert-byte-identity.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      via: "parseFrontmatter import (full-pipeline classes 1, 2, 7)"
      pattern: "parseFrontmatter"
---

<objective>
Pin the current (pre-fix) generated-agent bytes for the seven input classes that Phase 82 must leave byte-identical, BEFORE any implementation task modifies the converter. This is the Wave 0 requirement from 82-VALIDATION.md and the mitigation for 82-RESEARCH.md Pitfall 5 ("regression corpus captured AFTER the change proves nothing").

Purpose: SC-5 of the phase goal — agents outside the fix classes must produce byte-identical output — is only provable if the expected bytes are captured from unmodified HEAD. Plans 82-02..82-04 all verify against this corpus.

Output: `tests/bridges/agents/convert-byte-identity.test.ts`, green against HEAD, committed before any source change.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` — the orchestrator commits. Leave changes staged-ready.
- Comment/test-title anchors: `AGSK-01`, `AGSK-04`, `#86` only — never `Phase 82`/plan/wave references (`.claude/rules/typescript-comments.md`).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@tests/bridges/agents/convert.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture HEAD fileContent constants for the seven corpus classes</name>
  <files>(scratch only — no repo files modified)</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (the `makeDiscovered` builder and `convertAgent` call shape — lines ~17-28 and any test invoking convertAgent)
    - extensions/pi-claude-marketplace/bridges/agents/convert.ts (convertAgent input contract, TOOL_MAP, SUPPORTED_SOURCE_FIELDS)
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (parseFrontmatter signature for the full-pipeline classes)
    - extensions/pi-claude-marketplace/bridges/agents/types.ts (DiscoveredAgent shape)
  </read_first>
  <action>
    Write a throwaway capture script in your scratchpad directory (NOT in the repo; never commit it). The script imports `convertAgent` from `extensions/pi-claude-marketplace/bridges/agents/convert.ts` and `parseFrontmatter` from `frontmatter.ts` (relative file URL imports work under native TS strip; run with `node <script>.ts`), builds the seven inputs below, and prints each resulting `out.fileContent` between unambiguous delimiters (e.g. `=== CLASS 1 ===`). Common fixed inputs for every class: `pluginName: "acme"`, `pluginRoot: "/root"`, `pluginDataDir: "/data"`, `sourceHash: "abc"`, `mapModel: false`, discovered built like convert.test.ts's `makeDiscovered` (sourceName "bot", generatedName "pi-claude-marketplace-acme-bot", sourcePath "/abs/path/source.md", sourceHash "abc123", body "Body content." unless the class says otherwise).

    The seven classes (from 82-RESEARCH.md "regression corpus classes" — deliberately free of every fix trigger):
    1. Full pipeline, CSV tools + bare CSV skills (known + unknown mix): parseFrontmatter on the text `---\nname: bot\ndescription: d\ntools: Read,Bash\nskills: knowledge,phantom\n---\n\nBody.\n`, feed resulting raw/body into convertAgent with `knownSkills: ["acme-knowledge"]`.
    2. Full pipeline, inline-array tools: parseFrontmatter on `---\nname: bot\ndescription: d\ntools: ["Read", "Bash"]\n---\n\nBody.\n`, `knownSkills: []`.
    3. Extra frontmatter keys -> droppedFields: direct raw `{ description: "d", tools: "Read", color: "blue", hooks: "x" }`, `knownSkills: []`.
    4. Missing `tools:` -> default + existing warning: direct raw `{ description: "d" }`, `knownSkills: []`.
    5. disallowedTools filtering: direct raw `{ description: "d", tools: "Read,Bash,Edit", disallowedTools: "Edit" }`, `knownSkills: []`.
    6. Description fallback: direct raw `{ tools: "Read" }` (no description key), `knownSkills: []`.
    7. Full pipeline, CRLF source: parseFrontmatter on `---\r\nname: bot\r\ndescription: d\r\ntools: Read,Bash\r\nskills: knowledge\r\n---\r\n\r\nBody.\r\n`, `knownSkills: ["acme-knowledge"]`.

    Run the script against the current unmodified HEAD and save the seven captured outputs to a scratchpad file for Task 2. Confirm `git status` shows no repo modifications from this task.
  </action>
  <verify>
    <automated>node <scratchpad>/capture-byte-identity.ts prints seven delimited fileContent blocks and exits 0; git status --porcelain is empty</automated>
  </verify>
  <acceptance_criteria>
    - The capture script runs to completion under the repo's Node (v26 local) and prints exactly seven delimited blocks, each starting with `---\n` (generated frontmatter opening)
    - Class 1 output contains the line `skills: acme-knowledge` and a warning mentioning `unknown skill reference "phantom"` in its provenance block (proves known+unknown mix behaves as today)
    - Class 4 output contains `tools: read,bash,edit` (today's omitted-tools default)
    - No file under the worktree repo is created or modified (`git status --porcelain` empty)
  </acceptance_criteria>
  <done>Seven verbatim fileContent strings captured from unmodified HEAD, stored in scratchpad for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write convert-byte-identity.test.ts pinning the seven classes</name>
  <files>tests/bridges/agents/convert-byte-identity.test.ts</files>
  <read_first>
    - tests/bridges/agents/convert.test.ts (import style, makeDiscovered convention, strict-assert usage)
    - tests/bridges/agents/marker.test.ts (byte-for-byte assert.equal convention)
    - The captured constants from Task 1 (scratchpad)
  </read_first>
  <action>
    Create `tests/bridges/agents/convert-byte-identity.test.ts` following the sibling files' conventions: `import assert from "node:assert/strict"`, `import test from "node:test"`, relative `.ts` imports of `convertAgent` (and `parseFrontmatter` for classes 1, 2, 7), a local `makeDiscovered` helper mirroring convert.test.ts (do not export from convert.test.ts — copy the small builder). For each of the seven classes from Task 1, one `test(...)` that rebuilds the exact same input and asserts `assert.equal(out.fileContent, EXPECTED_N)` where `EXPECTED_N` is a template-literal constant holding the verbatim Task 1 capture (for class 7, keep the input CRLF escapes `\r\n` exactly).

    Test titles must anchor with requirement IDs, e.g. `AGSK-01 byte-identity: CSV tools + bare skills fileContent is pinned at pre-fix bytes` and use `AGSK-04` for a title noting the token-free-body clause. Include a file-header comment stating the contract: these constants were captured from the pre-fix converter and MUST NOT change during the #86 fix (anchors: `AGSK-01`, `AGSK-04`, `#86`).

    Keep all string literals ASCII (no em-dash U+2014 anywhere — the fix-unicode-dashes pre-commit hook rewrites it).
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/convert-byte-identity.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/bridges/agents/convert-byte-identity.test.ts` reports 7 passing tests, 0 failing, against unmodified source
    - Each test asserts the ENTIRE `out.fileContent` with `assert.equal` against an inline constant (no `includes`/regex-only assertions)
    - Fix-trigger absence (source assertion on test INPUT data): no class input contains the `Skill` tool token in a `tools:` value, no input frontmatter text contains a dash-list continuation line, and no `skills:` input value contains a colon-qualified token (`acme:...`). Check: `grep -n '"Skill"\|acme:' tests/bridges/agents/convert-byte-identity.test.ts` returns no matches inside input literals (matches in `knownSkills` identifiers do not count and the quoted-token pattern avoids them)
    - `npm run check` passes (typecheck + ESLint + Prettier + full unit suite)
    - `git status --porcelain` lists exactly one new file: `tests/bridges/agents/convert-byte-identity.test.ts`
  </acceptance_criteria>
  <done>Corpus test exists, green against HEAD, ready to be the invariant for plans 82-02..82-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin agent .md -> converter | Untrusted plugin content enters parseFrontmatter/convertAgent (exercised read-only by this plan's tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Tampering | Silent byte drift of generated agents during the fix | mitigate | This plan IS the mitigation: full-fileContent exact pins captured at HEAD; later plans gate on `git diff --exit-code` of this file |
| T-82-02 | DoS | Test inputs triggering converter throws | accept | Inputs are the seven documented safe classes; any throw fails the test run immediately (fail-clean, test-only code, no runtime surface) |

No packages installed; no network; no new attack surface — plan writes one test file.
</threat_model>

<verification>
- `node --test tests/bridges/agents/convert-byte-identity.test.ts` — 7/7 green against unmodified HEAD
- `npm run check` green (NFR-6)
- `git status --porcelain` shows only the new test file
</verification>

<success_criteria>
- Seven full-fileContent pins exist, captured pre-fix, covering: CSV tools+skills, inline-array tools, extra fields, missing-tools default, disallowedTools, description fallback, CRLF source
- Every pin is an exact-equality assertion on the whole generated file
- Suite green; corpus becomes the SC-5 invariant for the rest of the phase
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/82-agent-skill-preload-fidelity/82-01-SUMMARY.md` when done.
</output>
