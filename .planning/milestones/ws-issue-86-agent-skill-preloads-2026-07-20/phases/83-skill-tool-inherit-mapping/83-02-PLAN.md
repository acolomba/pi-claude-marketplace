---
phase: 83-skill-tool-inherit-mapping
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
  - tests/bridges/agents/frontmatter.test.ts
autonomous: true
requirements: [AGSK-05]

must_haves:
  truths:
    - "D-83-01 emitter half: `emitGeneratedAgentFile` accepts an optional `inheritSkills?: boolean` on the frontmatter fields; `true` emits the exact lowercase line `inheritSkills: true`, omitted/false emits `inheritSkills: false` byte-identically to today"
    - "AG-8 order holds: `inheritSkills:` remains the LAST generated frontmatter field, in the trio position after systemPromptMode and inheritProjectContext"
    - "D-83-05 render half: with inheritSkills true, a not-preloaded legend entry renders annotation `(available on demand)`; the preloaded annotation `(preloaded in your context)` is unchanged; with inheritSkills false both Phase 82 annotations render byte-identically"
    - "D-83-06 emitter direction: every pre-existing frontmatter.test.ts pin (NO_LEGEND_EXPECTED, empty/undefined legend, two-entry legend, AG-8 order) stays green with ZERO edits, and convert.test.ts (including the 83-01 disallowed pin) plus the byte-identity corpus stay green untouched"
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      provides: "Optional inheritSkills field on GeneratedFrontmatterFields; conditional trio line; renderSkillLegend third annotation state"
      contains: "inheritSkills?"
    - path: "tests/bridges/agents/frontmatter.test.ts"
      provides: "Direct-emitter inheritSkills: true full-output pin; legend on-demand render pin"
      contains: "available on demand"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      via: "emitGeneratedAgentFile passes frontmatter.inheritSkills ?? false into renderSkillLegend"
      pattern: "renderSkillLegend\\(legend"
---

<objective>
Implement the emitter half of AGSK-05: split `inheritSkills` out of the hardcoded frontmatter trio into an optional field defaulting to `false` (D-83-01's emission seam), and teach `renderSkillLegend` the D-83-05 third annotation state for known-but-not-preloaded skills when skill inheritance is on. No convert.ts change in this plan -- the converter does not pass the field yet, so ALL generated bytes across the whole suite remain identical to Phase 82 (D-83-06), proven by the untouched convert.test.ts pins (including the 83-01 disallowed pin) and the byte-identity corpus.

Resolved discretion choices (finalized -- encode exactly, do not revisit):
- D-83-05 annotation text is exactly `available on demand` (rendered parenthesized: `(available on demand)`). The entry line already maps to the Pi name via the arrow (`` `token` -> skill `pi-name` ``), which conveys "under their Pi name" without repeating it; matches the parallel structure of the two Phase 82 annotations.
- The `inheritSkills:` line stays in the trio position as the LAST frontmatter field (AG-8; CONTEXT discretion note).
- Emit only lowercase `true`/`false` -- pi-subagents 0.28.0 parses `frontmatter.inheritSkills === "true"` by string equality; any other spelling silently falls back to the pi-subagents default (83-RESEARCH.md Pitfall 6).

Output: frontmatter.ts emitter + render changes with new direct-emitter pins; every pre-existing pin green with zero edits.

Execution notes:
- Work from the worktree root `/Users/acolomba/src/pi-claude-marketplace/.worktrees/issue-86-agent-skill-preloads/`.
- Do NOT run `git commit` -- the orchestrator commits.
- ASCII-only source literals; the arrow is always the `→` escape, never a literal U+2192; never a literal em-dash (U+2014).
- Comment/test-title anchors: `AGSK-05`, `D-83-01`, `D-83-05`, `D-83-06`, `AG-8`, `#86` -- never `Phase 83`/plan/wave references (`.claude/rules/typescript-comments.md`).
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-PATTERNS.md
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@tests/bridges/agents/frontmatter.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optional inheritSkills field and conditional trio line</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (GeneratedFrontmatterFields ~lines 184-191 with the `model?`/`thinking?` optional-field pattern; the single hardcoded trio statement ~line 272; the "intentionally hardcoded" comment ~lines 251-254; the AG-8 order comment ~lines 235-236)
    - tests/bridges/agents/frontmatter.test.ts (makeLegendEmitInput ~336-354; NO_LEGEND_EXPECTED ~356-377; the AG-8 field-order test ~169-207 -- it asserts relative offsets of `"inheritSkills:"` key-with-colon, value-agnostic, so it must survive as-is)
    - 83-PATTERNS.md "bridges/agents/frontmatter.ts" section (exact target shape for the conditional trio line)
  </read_first>
  <behavior>
    - Test 1 (D-83-01 true path): `emitGeneratedAgentFile({ ...base, frontmatter: { ...base.frontmatter, inheritSkills: true } })` (base from makeLegendEmitInput, no legend) produces output byte-identical to `NO_LEGEND_EXPECTED` EXCEPT the single line `inheritSkills: false` becomes `inheritSkills: true`. Pin with assert.equal on a full-output constant. RED before implementation: the emitter ignores the unknown field at runtime and still emits `inheritSkills: false`.
    - Test 2 (AG-8): in the Test 1 output, `inheritSkills: true` is still the last frontmatter line before the closing `---` (assert via regex on the frontmatter block or index comparison, consistent with the existing order test's approach).
    - Guard (D-83-06, no new test needed): every pre-existing frontmatter.test.ts pin passes with zero edits -- omitted field keeps emitting `inheritSkills: false` bytes.
  </behavior>
  <action>
    In frontmatter.ts: add `readonly inheritSkills?: boolean;` to `GeneratedFrontmatterFields` after `skills`, following the `model?`/`thinking?` optional-field style. Replace the third string of the single trio statement (~line 272) so the pushed values become `"systemPromptMode: replace"`, `"inheritProjectContext: true"`, and a template literal producing `inheritSkills: true` when `frontmatter.inheritSkills ?? false` is true, `inheritSkills: false` otherwise -- lowercase literals only (pi-subagents 0.28.0 string-equality parse). Keep the field in the trio position (AG-8: last frontmatter field). Update the stale comment at ~251-254: systemPromptMode and inheritProjectContext remain extension-side hardcoded defaults, while inheritSkills is now derived from the source agent's Skill tool declaration (anchor: AGSK-05, D-83-01); do not contradict the AG-8 order comment.

    Write the behavior tests first (Test 1 constant derived from NO_LEGEND_EXPECTED with only the inheritSkills line flipped), confirm Test 1 fails RED, then implement until green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/frontmatter.test.ts && node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert.test.ts tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 pins the FULL output with assert.equal (no includes-only assertion) and passes GREEN after implementation
    - Zero edits to pre-existing frontmatter.test.ts tests: `git diff tests/bridges/agents/frontmatter.test.ts` shows only added lines inside the new test region (no modified/removed lines in NO_LEGEND_EXPECTED, the legend pins, or the AG-8 order test)
    - Source assertion: `grep -c 'inheritSkills?' extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` outputs 1, and `grep -cE 'inheritSkills: (True|TRUE|yes|on)' extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` outputs 0
    - convert.test.ts (including the 83-01 `DISALLOWED_SKILL_EXPECTED` pin) and the byte-identity corpus pass untouched (`git diff --exit-code` on both)
  </acceptance_criteria>
  <done>Emitter emits `inheritSkills: true` when and only when the optional field is true; omitted field is byte-identical to Phase 82 output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Legend third annotation state in renderSkillLegend</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (renderSkillLegend ~lines 318-336 with the annotation ternary and the `→` escape; its single call site in the assembly return ~line 308)
    - tests/bridges/agents/frontmatter.test.ts (the two-entry legend pin ~lines 389-435 -- the template to clone; its expected constant carries `inheritSkills: false`, one `(preloaded in your context)` line, one `(not available in this session)` line)
    - 83-PATTERNS.md "Legend third state" excerpt (exact current ternary and call-site shape)
  </read_first>
  <behavior>
    - Test 1 (D-83-05): clone the two-entry legend test with `inheritSkills: true` added to the frontmatter fields. Expected full output differs from the existing two-entry expected constant in exactly two byte ranges: the frontmatter line `inheritSkills: false` becomes `inheritSkills: true`, and the not-preloaded entry's annotation `(not available in this session)` becomes `(available on demand)`. The preloaded entry line, heading, intro sentence, and all separators are byte-identical. Pin with assert.equal on the full output. RED before implementation: the render still emits `(not available in this session)`.
    - Guard (D-83-05 both directions, no new test needed): the existing two-entry test (no inheritSkills field -> false) still pins `(not available in this session)` with zero edits.
  </behavior>
  <action>
    In frontmatter.ts: change the private `renderSkillLegend(legend)` signature to `renderSkillLegend(legend, inheritSkills: boolean)` (second parameter required -- private function, single call site). In the annotation ternary, keep the `preloaded` arm as `"preloaded in your context"` unchanged; the not-preloaded arm becomes `inheritSkills ? "available on demand" : "not available in this session"` (exact strings; pure ASCII; no separator characters needed). Entry-line template otherwise unchanged, arrow stays the `→` escape. Update the single call site in the assembly return (~line 308) to pass `frontmatter.inheritSkills ?? false`. Add a brief doc-comment line on renderSkillLegend noting the third state is gated on skill inheritance (anchor: AGSK-05, D-83-05).

    Write the behavior test first, confirm it fails RED on the annotation byte range, then implement until green.
  </action>
  <verify>
    <automated>node --test tests/bridges/agents/frontmatter.test.ts && node --test tests/bridges/agents/convert.test.ts && node --test tests/bridges/agents/convert-byte-identity.test.ts && git diff --exit-code tests/bridges/agents/convert.test.ts tests/bridges/agents/convert-byte-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test 1 passes with assert.equal on the FULL output; the preloaded entry line in its expected constant is byte-identical to the existing two-entry constant's preloaded line
    - The exact annotation string `available on demand` appears in frontmatter.ts exactly once: `grep -c '"available on demand"' extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts` outputs 1
    - No literal U+2192 or U+2014 anywhere in frontmatter.ts (byte-level check via `grep -a` or a `node -e` scan, not bare BSD grep); `→` escapes still present
    - Zero edits to pre-existing frontmatter.test.ts tests (same git-diff added-lines-only check as Task 1)
    - convert.test.ts and the byte-identity corpus still pass untouched (`git diff --exit-code` on both)
  </acceptance_criteria>
  <done>Render layer supports all three annotation states, keyed on the inherit flag; both Phase 82 states byte-identical when the flag is false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| converter data -> generated agent bytes | Legend entries and frontmatter fields become child-session system-prompt bytes via the emitter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Tampering | Byte drift of the default (inheritSkills omitted/false) emit path | mitigate | Optional-field-default-false design; every pre-existing full-output pin plus the 83-01 disallowed pin and byte-identity corpus gate each task's verify |
| T-83-04 | Tampering | Wrong boolean spelling silently falling back to the pi-subagents default (feature ships broken with green tests) | mitigate | Task 1 pins the exact lowercase line `inheritSkills: true`; acceptance grep rejects `True`/`yes`/`on` spellings |
| T-83-05 | Tampering | Annotation-string injection into the legend | accept | The new annotation is a compile-time ASCII literal chosen here; token/name content is unchanged from the Phase 82-mitigated constrained classes (no new injection surface) |

No packages installed; no network; no new input surface.
</threat_model>

<verification>
- `node --test tests/bridges/agents/frontmatter.test.ts` green (new pins + all pre-existing pins, zero edits to the latter)
- `node --test tests/bridges/agents/convert.test.ts` and `node --test tests/bridges/agents/convert-byte-identity.test.ts` green with `git diff --exit-code` on both files (converter untouched by this plan -- all generated bytes still Phase 82-identical)
- `npm run check` green (NFR-6)
</verification>

<success_criteria>
- `GeneratedFrontmatterFields.inheritSkills?: boolean` exists; true emits the exact lowercase `inheritSkills: true` line in the AG-8 trio position; omitted/false is byte-identical to Phase 82
- `renderSkillLegend` renders `(available on demand)` for not-preloaded entries only when the inherit flag is true; both Phase 82 annotations preserved otherwise
- All pre-existing pins green with zero edits; the emitter contract is ready for plan 83-03's convert.ts threading
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/83-skill-tool-inherit-mapping/83-02-SUMMARY.md` when done.
</output>
