---
phase: 83.1
slug: silent-skill-mapping
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-19
---

# Phase 83.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | node:test (Node v26.5.0, native TS strip) |
| **Config file** | none — glob in package.json `test` script |
| **Quick run command** | `node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts tests/bridges/agents/convert-byte-identity.test.ts` |
| **Full suite command** | `npm run check` |
| **Estimated runtime** | ~10 seconds (quick) / ~60 seconds (full) |

---

## Sampling Rate

- **After every task commit:** Run the targeted three-file `node --test` invocation (quick command above)
- **After every plan wave:** Run `npm test`
- **Before `/gsd-verify-work`:** `npm run check` must be green (NFR-6)
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 83.1-01/02 | 01, 02 | 1 | AGSK-03/04/05 (amended) | — | N/A | unit | quick run command | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

Success-criteria → assertion map (from 83.1-RESEARCH.md Validation Architecture):

| SC | Observable assertion | File |
|----|----------------------|------|
| SC-1 | Re-pinned `#86 canonical` whole-file constant (`droppedTools: (none)`, `warnings: (none)`, `inheritSkills: true`) + unit tests asserting `out.droppedTools` and `out.warnings` deepEqual `[]` | tests/bridges/agents/convert.test.ts |
| SC-2 | Re-pinned `DISALLOWED_SKILL_EXPECTED` whole-file constant (`droppedTools: (none)`, `warnings: (none)`, `inheritSkills: false`) | tests/bridges/agents/convert.test.ts |
| SC-3 | Legend pins read `(available on demand)` in both inheritSkills branches; `grep -rn -a "not available in this session" extensions/ tests/` returns zero | tests/bridges/agents/frontmatter.test.ts, convert.test.ts, shell |
| SC-4 | WebFetch/multi-unknown droppedTools tests unchanged-green; all 7 byte-identity corpus classes green with zero edits to convert-byte-identity.test.ts | tests/bridges/agents/convert.test.ts, convert-byte-identity.test.ts |

Fenced-behavior guards (must pass with ZERO test edits): inheritSkills emission (frontmatter.test.ts:446-487), inheritSkills computation branches (convert.test.ts:715-724, 729-738, 622-630), AG-11 empty-tools throw (convert.test.ts:173, 195), install notify formatting (stage.test.ts:1008-1039).

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Audit 2026-07-19

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

All three phase requirements (AGSK-03 / AGSK-04 / AGSK-05, amended forms) were
already COVERED by exact-equality pins landed during execution and review — no
new tests were generated. Cross-reference:

- **AGSK-03 (amended)** — silent Skill conversion, no `droppedTools`, no warning;
  genuinely unmapped tools keep entries. Pinned in `tests/bridges/agents/convert.test.ts`
  (result-record `deepEqual []` on `droppedTools`/`warnings` in both branches,
  the `DISALLOWED_SKILL_EXPECTED` and `#86 canonical` whole-file constants, the
  WebFetch guard, and the Skill-co-occurring-with-unmapped-tool drop pin).
  Commits `0e634e6f`, `c03548cf` (WR-02), `263581ee` (IN-02).
- **AGSK-05 (amended)** — declared-and-allowed Skill → `inheritSkills: true`,
  silent; non-declaring agents keep `false`. Pinned in `convert.test.ts`
  computation-branch tests plus the AG-11 empty-tools error note.
  Commits `0e634e6f`, `bb255c06` (WR-01).
- **AGSK-04 (amended)** — legend note per referenced token, non-preloaded state
  unified to "(available on demand)"; reference-free bodies byte-identical.
  Pinned in `tests/bridges/agents/frontmatter.test.ts` (two-entry legend pin
  under `inheritSkills: false`) and the inverted SC-3 pin in `convert.test.ts`.
  Commits `3f1e78d4`, `3dcdc56b` (IN-01).

Verification re-run at audit time: `node --test` over the three agent-bridge
suites = 100 pass / 0 fail; `convert-byte-identity.test.ts` corpus unchanged
(0 lines across `0e634e6f~1..263581ee`). `npm run check` green (NFR-6).

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-19 — all three amended requirements covered by
existing exact-equality pins; no gaps, no generated tests, byte-identity corpus
at zero edits.
