---
phase: 84-emit-an-agent-local-skillpath-field-on-every-generated-agent
plan: 03
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - tests/integration/skill-path-resolution.test.ts
autonomous: true
requirements: [AGSK-06]
must_haves:
  truths:
    - "SC-2: given the emitter's actual skillPath output, pi-subagents' resolveSkillsWithFallback resolves the bridged skill by its generated name (returned in `resolved`, not `missing`)"
    - "SC-2: the resolved skill does NOT enter the parent/global catalog — discoverAvailableSkills(runtimeCwd) excludes the generated name (invocation-private guarantee)"
    - "The test skips gracefully (t.skip, exit 0) when pi-subagents is not reachable, keeping `npm run check` green in CI where the optional peer is absent"
  artifacts:
    - tests/integration/skill-path-resolution.test.ts
  key_links:
    - "resolveSkillsWithFallback is called with localBaseDir = path.dirname(agentFilePath), mirroring pi-subagents' own foreground/background call sites (0.35.1 execution.ts)"
    - "the staged skill lives at <scope>/pi-claude-marketplace/resources/skills/<generatedName>/SKILL.md, the layout locations.ts skillsTargetDir produces"
---

<objective>
Add the SC-2 resolver-contract integration test: prove that the skillPath the
emitter writes actually resolves the bridged skill through pi-subagents' own
`resolveSkillsWithFallback`, and that the skill stays invocation-private (never
entering the global catalog). This is genuinely new coverage — no existing test
exercises the cross-package resolution contract.

Purpose: SC-1 (Plan 84-01) proves the pointer is emitted; SC-2 proves the
pointer resolves. The test must call pi-subagents' real resolver (never a
reimplementation — RESEARCH "Don't Hand-Roll"), staging a real skill install
that mirrors the production `skillsTargetDir` layout.

Output: `tests/integration/skill-path-resolution.test.ts`, run under
`npm run test:integration`, that passes where pi-subagents 0.35.x is reachable
and skips cleanly where it is not.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-RESEARCH.md
@.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-PATTERNS.md
@tests/integration/hooks-spawn-end-to-end.test.ts
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@extensions/pi-claude-marketplace/persistence/locations.ts
</context>

## Artifacts this phase produces (scoped to this plan)

- NEW test file: `tests/integration/skill-path-resolution.test.ts` (SC-2 resolver-contract; optional-peer-guarded with graceful skip).

<important_correction>
The PATTERNS.md example imports the resolver via `import("pi-subagents/src/agents/skills.js")`. That bare-specifier subpath is NOT in pi-subagents' package `exports` map (which exposes only `.`, `./background-work`, `./delegation`) and therefore throws `ERR_PACKAGE_PATH_NOT_EXPORTED` even when pi-subagents IS installed — `resolveSkillsWithFallback` and `discoverAvailableSkills` live in the un-exported internal `src/agents/skills.ts`. The test MUST locate the installed package and import the internal module by an absolute file path / `pathToFileURL(...)`, wrapped in try/catch, and `t.skip()` on ANY failure (not installed, path not found, loader cannot strip the external `.ts`). Because pi-subagents is neither a `dependencies` nor `devDependencies` entry, this test will normally SKIP in CI — that is by design and keeps `npm run check` green.
</important_correction>

<tasks>

<task type="auto">
  <name>Task 1: SC-2 resolver-contract integration test with graceful skip</name>
  <files>tests/integration/skill-path-resolution.test.ts</files>
  <read_first>
    - tests/integration/hooks-spawn-end-to-end.test.ts (the real-fixture/real-call posture: mkdtemp scope root, writeFile, temp-dir teardown via rm in a finally/t.after; node:test imports)
    - extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts (`emitGeneratedAgentFile` — use it to produce the agent file so the test verifies the ACTUAL emitted skillPath, closing the loop with Plan 84-01)
    - extensions/pi-claude-marketplace/persistence/locations.ts (`skillsTargetDir` = `<extensionRoot>/resources/skills`; the extensionRoot is `<scope>/pi-claude-marketplace`, so the agent at `<scope>/agents/<name>.md` reaches skills via `../pi-claude-marketplace/resources/skills`)
    - 84-RESEARCH.md "Architecture Patterns" Pattern 2 (the exact resolveSkillsWithFallback call signature and localBaseDir contract verified against installed 0.35.1) and "Validation Architecture" Wave 0 Gaps (the graceful-skip requirement)
    - the installed resolver source if reachable (e.g. `/opt/homebrew/lib/node_modules/pi-subagents/src/agents/skills.ts`) to confirm the ResolvedSkill `.name` field, the `discoverAvailableSkills(cwd)` return shape, and the SKILL.md frontmatter shape `collectFilesystemSkills`/`readSkill` expects
  </read_first>
  <action>
    Create a node:test integration test. Locate the installed pi-subagents package and dynamically import its internal `src/agents/skills.ts` by an absolute file path via `pathToFileURL`, inside try/catch; on any failure call `t.skip("pi-subagents resolver not reachable")` and return. Resolve the package location from an env override first (a `PI_SUBAGENTS_ROOT`-style variable) then fall back to the conventional global module path; do NOT use a bare-specifier subpath import (it is blocked by the exports map — see the correction block above). Arrange a hermetic fixture with `mkdtemp`: create `<scope>/agents/`, produce a generated agent via `emitGeneratedAgentFile` with a non-empty `skills` list containing one generated name, and write it to `<scope>/agents/<name>.md`; assert the written file literally contains `skillPath: ../pi-claude-marketplace/resources/skills`. Stage a real skill at `<scope>/pi-claude-marketplace/resources/skills/<generatedName>/SKILL.md` with the minimal frontmatter the resolver reads (name + description). Call `resolveSkillsWithFallback([generatedName], skillCwd, runtimeCwd, ["../pi-claude-marketplace/resources/skills"], path.dirname(agentFilePath))` — localBaseDir is the agent-file directory, per RESEARCH Pattern 2 — and assert the result's `resolved` contains an entry whose `name` equals the generated name and `missing` does not. Then assert `discoverAvailableSkills(runtimeCwd)` returns no entry with that name (invocation-private: the skill is off the global scan roots). Tear down the temp dir with `rm(dir, { recursive: true, force: true })` in a finally or `t.after`. Cite SC-2 / AGSK-06 in the test title and D-84-04 where the emitted-constant path is asserted.
  </action>
  <acceptance_criteria>
    - `node --test tests/integration/skill-path-resolution.test.ts` exits 0 (passes where pi-subagents is reachable; skips cleanly otherwise — never fails on absence).
    - The test imports the resolver by a file path / `pathToFileURL`, NOT by the bare specifier `pi-subagents/src/agents/skills.js`; a `grep -n "pi-subagents/src/agents/skills" tests/integration/skill-path-resolution.test.ts` finds no bare-specifier dynamic import.
    - The test calls `emitGeneratedAgentFile` (or `convertAgent`) so the resolved skillPath is the emitter's real output, and asserts the written agent file contains `skillPath: ../pi-claude-marketplace/resources/skills`.
    - The success path asserts BOTH `resolved` contains the generated name AND `discoverAvailableSkills` excludes it.
    - `npm run test:integration` exits 0 with the new test present.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acolomba/src/pi-claude-marketplace && node --test tests/integration/skill-path-resolution.test.ts</automated>
  </verify>
  <done>The SC-2 test resolves the emitter's real skillPath through pi-subagents' own resolveSkillsWithFallback, asserts the skill stays out of the global catalog, and skips gracefully when the optional peer is unreachable — keeping npm run check green in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → installed pi-subagents module | The test loads an external package's internal module by file path to exercise the real resolver; it is test-only code, never shipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-04 | Tampering | dynamic import of an external module by file path (test-only) | low | accept | The import target is resolved from an env override / conventional install path and is wrapped in try/catch with a graceful skip; it runs only under the test runner, never in shipped extension code. No plugin/user-controlled input feeds the path. |
| T-84-02 | Information Disclosure | invocation-private resolution | low | accept | The test asserts the positive guarantee: the resolved skill does NOT enter `discoverAvailableSkills` (global catalog), matching pi-subagents' throwaway localByName map behavior. |
</threat_model>

<verification>
- `node --test tests/integration/skill-path-resolution.test.ts` green (pass-or-skip, never fail-on-absence).
- `npm run test:integration` green with the new file.
- The test uses the real emitter output and pi-subagents' real resolver; it never reimplements the resolution walk.
</verification>

<success_criteria>
SC-2 satisfied: the emitted skillPath resolves the bridged skill via pi-subagents' own resolver and the skill stays invocation-private. The test is CI-safe (graceful skip) so `npm run check` stays green regardless of whether the optional peer is installed.
</success_criteria>

<output>
Create `.planning/workstreams/issue-86-agent-skill-preloads/phases/84-emit-an-agent-local-skillpath-field-on-every-generated-agent/84-03-SUMMARY.md` when done.
</output>
