---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - tests/shared/notify.test.ts
autonomous: true
requirements: [SURF-02]
must_haves:
  truths:
    - "`shared/notify.ts` exports `ClaudeHookEvent` aliased to `BucketAEvent` (the 8-event tuple from `domain/components/hook-events.ts`)."
    - "`shared/notify.ts` exports a `HookSummaryEntry` discriminated union: tool events statically carry `matcher: string`; non-tool events statically cannot carry one."
    - "`shared/notify.ts` exports a `HookSummary` interface wrapping `readonly HookSummaryEntry[]`."
    - "`PluginInfoComponentsResolved.components` gains optional `hooks?: readonly HookSummaryEntry[]`, alphabetically placed between `commands` and `mcp` in the type declaration."
    - "`COMPONENT_KINDS` is a 5-tuple in alphabetical order `[\"agents\", \"commands\", \"hooks\", \"mcp\", \"skills\"]`; the readonly-tuple length stays the structural-exhaustiveness gate."
    - "`appendResolvedComponentLines` has a `kind === \"hooks\"` arm that emits the D-63-04 multi-line block; every other kind keeps the single-line comma-join path."
    - "The hooks renderer arm reads `entry.event` and `entry.matcher` directly via the `\"matcher\" in entry` discriminator predicate — never re-derives event strings."
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/notify.ts
      provides: "ClaudeHookEvent / HookSummaryEntry / HookSummary exports + components.hooks field + 5-tuple COMPONENT_KINDS + hooks renderer arm"
    - path: tests/shared/notify.test.ts
      provides: "Discriminator exhaustiveness coverage + multi-line hooks: block byte-form fixture"
  key_links:
    - from: extensions/pi-claude-marketplace/shared/notify.ts
      to: extensions/pi-claude-marketplace/domain/components/hook-events.ts
      via: "type re-export of BUCKET_A_EVENTS / TOOL_EVENTS tuples"
      pattern: "from \"\\.\\./domain/components/hook-events\\.ts\""
    - from: extensions/pi-claude-marketplace/shared/notify.ts
      to: tests/architecture/catalog-uat.test.ts
      via: "byte-equality on rendered hooks: block (no token changes here — only structural type additions)"
      pattern: "appendResolvedComponentLines"
---

<objective>
Land the 4 type-only seams in `shared/notify.ts` that every downstream plan in Phase 63 consumes: (1) `ClaudeHookEvent` re-export, (2) `HookSummaryEntry` discriminated union + `HookSummary` interface, (3) `PluginInfoComponentsResolved.components.hooks?` optional field, and (4) `COMPONENT_KINDS` 4-tuple → 5-tuple plus the `kind === "hooks"` renderer arm emitting the D-63-04 multi-line block.

Purpose: This plan is foundational. Plans 63-02 / 63-03 / 63-04 / 63-05 all consume `HookSummaryEntry` (per D-63-06) and the `components.hooks?` field (per D-63-07) without touching the type model. The plan implements decisions D-63-04 / D-63-06 / D-63-07 and resolves RESEARCH Open Question 4 (carrier shape) by passing raw `readonly HookSummaryEntry[]` at the message-payload boundary while keeping `HookSummary` as a public export.

Output: Type and renderer changes in `shared/notify.ts`; expanded unit tests pinning the multi-line block byte form and the discriminator exhaustiveness. Zero new REASONS tokens, zero new notify call sites, zero source-of-truth duplication.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-CONTEXT.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/domain/components/hook-events.ts
@tests/shared/notify.test.ts
</context>

# Artifacts this phase produces

- `ClaudeHookEvent` type alias in `shared/notify.ts` (re-export of `BucketAEvent`).
- `HookSummaryEntry` discriminated union in `shared/notify.ts`.
- `HookSummary` interface in `shared/notify.ts`.
- `PluginInfoComponentsResolved.components.hooks?: readonly HookSummaryEntry[]` field.
- `COMPONENT_KINDS` 5-tuple `["agents", "commands", "hooks", "mcp", "skills"]`.
- `appendResolvedComponentLines` `kind === "hooks"` arm emitting multi-line block per D-63-04.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure compile-time type additions and a deterministic in-memory rendering arm. No untrusted input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01-RE | Tampering | `appendResolvedComponentLines` `kind === "hooks"` arm | mitigate | Discriminator `"matcher" in entry` is structurally exhaustive; the union has only two arms and both render — no `entry.event` re-derivation, no runtime guard, no string-API regression (Pitfall 5). |
| T-63-01-EX | Information disclosure (mild) | rendered `hooks:` block | accept | Output is plugin-author-supplied event + matcher already validated by `HOOKS_VALIDATOR`; renderer emits verbatim. |

No I/O, no command execution, no file writes in this plan.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ClaudeHookEvent / HookSummaryEntry / HookSummary + components.hooks? + 5-tuple COMPONENT_KINDS</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 1-110 imports + REASONS tuple, 1000-1075 PluginInfoComponentsResolved interface, 2570-2615 COMPONENT_KINDS + appendResolvedComponentLines)
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (BUCKET_A_EVENTS, BucketAEvent, TOOL_EVENTS, ToolEvent — lines 35-64)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (Seam 2, Seam 3, Seam 4+5 sections)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Pattern 2 lines 521-562 + Example 4 lines 1073-1140 verbatim transformation; Pitfall 5 re-derivation ban)
  </read_first>
  <behavior>
    - Test 1: `ClaudeHookEvent` is assignable from every literal in `BUCKET_A_EVENTS`; assignment from `"FileChanged"` (bucket-B) fails to compile.
    - Test 2: Constructing `HookSummaryEntry` with `event: "PreToolUse"` REQUIRES a `matcher: string` field; constructing with `event: "SessionStart"` FORBIDS a `matcher` field (compile-time + at-runtime fixture coverage).
    - Test 3: `COMPONENT_KINDS` is a `readonly [ComponentKind, ComponentKind, ComponentKind, ComponentKind, ComponentKind]` whose `.length` is exactly 5 and whose order matches `["agents", "commands", "hooks", "mcp", "skills"]`.
    - Test 4: `appendResolvedComponentLines` invoked with `components.hooks = [{ event: "PreToolUse", matcher: "Bash" }, { event: "PreToolUse", matcher: "Edit|Write" }, { event: "PostToolUse", matcher: "Edit" }, { event: "SessionStart" }]` produces exactly:
      ```
          hooks:
            PreToolUse(Bash)
            PreToolUse(Edit|Write)
            PostToolUse(Edit)
            SessionStart
      ```
      (6-space indent for entries — 4-space `hooks:` header + 2-space per-entry indent — D-63-04).
    - Test 5: `appendResolvedComponentLines` with `components.hooks = undefined` or `components.hooks = []` emits NO `hooks:` header line.
    - Test 6: For non-hooks kinds (`agents`, `commands`, `mcp`, `skills`), the existing single-line comma-join path is preserved verbatim (regression guard against the renderer arm leaking into other kinds).
  </behavior>
  <action>
    In `shared/notify.ts`:

    1. Add import from `../domain/components/hook-events.ts`: bring `BUCKET_A_EVENTS` (value), `TOOL_EVENTS` (value), and types `BucketAEvent` + `ToolEvent`. Do NOT re-export the value tuples — they remain `domain/components/hook-events.ts`'s single source of truth. Per RESEARCH Open Question 4, also import `BUCKET_A_EVENTS` and `TOOL_EVENTS` only if referenced at value-space (e.g., for an `assertNever`-style guard inside the renderer); otherwise import the types alone.

    2. Insert above the existing message-type interfaces (close to existing `BUCKET_A_EVENTS` consumers; preserve file's "types first, then renderers" structure):
       - `export type ClaudeHookEvent = BucketAEvent;`
       - `export type HookSummaryEntry = | { readonly event: ToolEvent; readonly matcher: string } | { readonly event: Exclude<ClaudeHookEvent, ToolEvent> };`
       - `export interface HookSummary { readonly entries: readonly HookSummaryEntry[]; }`

       The discriminator predicate is structural (`"matcher" in entry`); no explicit `assertNever` arm is required because the union has exactly two arms and both render successfully — per D-63-06 + RESEARCH Pattern 2.

    3. In the existing `interface PluginInfoComponentsResolved` (currently at line 1041), insert `readonly hooks?: readonly HookSummaryEntry[];` between `commands` and `mcp` (alphabetical placement per D-63-07).

    4. Update the `COMPONENT_KINDS` declaration (currently at line 2583): widen the tuple type from a 4-element readonly tuple to a 5-element readonly tuple AND insert the literal `"hooks"` between `"commands"` and `"mcp"`. Resulting literal: `["agents", "commands", "hooks", "mcp", "skills"]`. The comment block at lines 2575-2581 explaining the tuple-length contract must be updated from "4 entries" / "5th key" to "5 entries" / "6th key" (PATTERNS Seam 4+5 callout).

    5. In `appendResolvedComponentLines` (currently at line 2598), grow the `for (const kind of COMPONENT_KINDS)` body with a leading `if (kind === "hooks")` arm:
       - When `entries = components.hooks` is `undefined` or empty, emit nothing (do NOT push the header alone).
       - Otherwise push `"    hooks:"` then iterate entries; for each, push `\`      ${entry.event}(${entry.matcher})\`` when `"matcher" in entry`, else push `\`      ${entry.event}\``.
       - End the arm with `continue;` so the existing single-line comma-join path below executes for every non-hooks kind unchanged.

    Do NOT introduce a `TypeBox` runtime schema for `HookSummary` (RESEARCH Claude's Discretion + "Don't Hand-Roll" — the value is built from an already-validated parsed config). Per D-63-07.
  </action>
  <verify>
    <automated>npm test -- tests/shared/notify.test.ts</automated>
  </verify>
  <done>
    `shared/notify.ts` exports `ClaudeHookEvent`, `HookSummaryEntry`, `HookSummary`; `PluginInfoComponentsResolved.components` carries `hooks?: readonly HookSummaryEntry[]` between `commands` and `mcp`; `COMPONENT_KINDS` is the 5-tuple in alphabetical order; `appendResolvedComponentLines` emits the multi-line block per D-63-04 with 6-space per-entry indent; the comment block on the tuple-length gate now states "5 entries / 6th key"; nothing else in `shared/notify.ts` changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/shared/notify.test.ts with discriminator + multi-line hooks: block fixtures</name>
  <files>tests/shared/notify.test.ts</files>
  <read_first>
    - tests/shared/notify.test.ts (existing `appendResolvedComponentLines` cases — locate the describe/suite block that tests COMPONENT_KINDS output)
    - extensions/pi-claude-marketplace/shared/notify.ts (the changes from Task 1)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (Seam 4+5)
  </read_first>
  <behavior>
    - The discriminator test asserts a `HookSummaryEntry` with `event: "PreToolUse"` and missing `matcher` is a TypeScript-level error (use `// @ts-expect-error` directive) and that `event: "SessionStart"` with a `matcher` field is also `@ts-expect-error`.
    - The renderer test feeds the entries fixture from Task 1 Test 4 and asserts the emitted `lines` array contains exactly the 5 expected strings in the documented order (`hooks:` header followed by 4 entries) at exact 4-space / 6-space indents.
    - The empty-hooks test asserts `components.hooks = []` and `components.hooks = undefined` both emit zero lines and that other kinds (`agents = ["agent-a"]`) still render their comma-join line `    agents: agent-a` unchanged.
    - The other-kinds regression test asserts that for `components = { agents: ["a"], commands: ["b"], mcp: ["c"], skills: ["d"] }` with no `hooks` field, the emitted output equals the legacy 4-line output (`agents: a`, `commands: b`, `mcp: c`, `skills: d` — each prefixed by 4 spaces).
  </behavior>
  <action>
    In `tests/shared/notify.test.ts`:

    1. Add a new `test()` block (or extend the existing `describe` for `appendResolvedComponentLines`) covering the 4 fixture cases enumerated in `<behavior>`. Reuse the test file's existing assertion helpers and `lines: string[]` accumulator pattern.

    2. Use `// @ts-expect-error` directives on the discriminator misuse cases — node:test runs after typecheck, so a missing `@ts-expect-error` would fail `npm run check`. Per `.claude/rules/typescript-comments.md`, the `@ts-expect-error` line must carry a one-line WHY comment (e.g., `// @ts-expect-error PreToolUse requires matcher per D-63-06`).

    3. Do NOT modify existing tests for other kinds beyond the regression fixture — keep the byte-form of every existing assertion untouched.
  </action>
  <verify>
    <automated>npm test -- tests/shared/notify.test.ts</automated>
  </verify>
  <done>
    `tests/shared/notify.test.ts` contains the 4 new fixture cases; `npm test -- tests/shared/notify.test.ts` is green; existing test bytes are unchanged; the discriminator misuse cases compile-fail without `@ts-expect-error` and compile-pass with it.
  </done>
</task>

</tasks>

<verification>
- Typecheck: `npx tsc --noEmit` passes (the discriminator union compiles; the 5-tuple length is structurally exact).
- `npm test -- tests/shared/notify.test.ts` green.
- `npm run check` green from a clean checkout.
- `grep -n "COMPONENT_KINDS" extensions/pi-claude-marketplace/shared/notify.ts` shows the 5-element tuple literal in alphabetical order.
- `grep -n "ClaudeHookEvent\\|HookSummaryEntry\\|HookSummary" extensions/pi-claude-marketplace/shared/notify.ts` shows the 3 new exports.
- `grep -c "lossy synthesis\\|LOSSY_SYNTHESIS" extensions/pi-claude-marketplace/shared/notify.ts` returns 0 (SURF-03 stays unshipped — Pitfall 4).
</verification>

<success_criteria>
- D-63-06 / D-63-07 implemented at the shared/notify.ts seam exactly per PATTERNS.md.
- No new REASONS tuple member in this plan (the `"orphan rewake"` addition lives in 63-03 to land atomically with its catalog row).
- No new notify call sites; no new top-level message variant.
- Multi-line `hooks:` block byte form pinned at 4-space header + 6-space per-entry indent per D-63-04.
- Renderer reads `entry.event` and `entry.matcher` directly — no string re-derivation (Pitfall 5 averted).
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-01-SUMMARY.md` when done. Summary records: exact line locations of the 3 new exports, the new tuple literal, and the renderer arm; the test count added; the typecheck result; and a one-line "ready for plans 63-02..05 to consume" handoff.
</output>
