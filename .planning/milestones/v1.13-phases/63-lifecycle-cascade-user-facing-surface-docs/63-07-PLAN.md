---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 07
type: execute
wave: 4
depends_on: []
files_modified:
  - tests/architecture/scope-fences-63.test.ts
autonomous: true
requirements: [SURF-03, SURF-04]
must_haves:
  truths:
    - "SURF-03 ships NOTHING in v1.13: no synthesis-caveat token in `shared/notify.ts::REASONS`, no `<lossy synthesis>` marker family in any v1.13 source file, no install-arm warning emission outside the orphan-rewake row added by Plan 63-03."
    - "SURF-04 ships NOTHING in v1.13: no `commands/plugin/hooks.ts` file or any equivalent edge handler, no new `/claude:plugin hooks` command surface, no hook-count column added to the `list` command output (existing byte-form preserved)."
    - "HOOK-04 was COMPLETED in Phase 58 per D-58-01 — `shared/notify.ts::REASONS` contains `\"unsupported hooks\"` (at line 81 verified 2026-06-16); `MANIFEST_FIELD_REASONS` in `orchestrators/plugin/install.ts:1573` is `new Set([\"lspServers\"])` and does NOT contain `\"hooks\"`. Phase 63 confirms this prior state via the architecture test."
    - "The confirmation architecture test (`tests/architecture/scope-fences-63.test.ts`) pins these invariants by grep so any future regression — accidental introduction of a synthesis token, a `/claude:plugin hooks` edge handler, or a hook-count list column — fails the test."
  artifacts:
    - path: tests/architecture/scope-fences-63.test.ts
      provides: "Architecture-lint test pinning SURF-03 absence + SURF-04 non-additions + HOOK-04 prior-completion invariants"
  key_links:
    - from: tests/architecture/scope-fences-63.test.ts
      to: extensions/pi-claude-marketplace/shared/notify.ts
      via: "grep for absence of lossy-synthesis tokens; grep for presence of \"unsupported hooks\""
      pattern: "lossy synthesis\\|unsupported hooks"
    - from: tests/architecture/scope-fences-63.test.ts
      to: extensions/pi-claude-marketplace/commands/plugin/
      via: "directory scan for absence of hooks.ts edge handler"
      pattern: "hooks\\.ts"
    - from: tests/architecture/scope-fences-63.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
      via: "grep for MANIFEST_FIELD_REASONS = new Set([\"lspServers\"]) (no \"hooks\")"
      pattern: "MANIFEST_FIELD_REASONS"
---

<objective>
Lock the SURF-03 (deferred to v1.14+) and SURF-04 (perma-forbidden in v1.13) NON-additions via a single architecture-lint test that grep-asserts the absence of every forbidden source pattern, AND confirm via the same test that HOOK-04 (REASONS rename, shipped in Phase 58 per D-58-01) is still in place — so the requirements coverage gate records SURF-03 and SURF-04 as covered.

Purpose: The planning directive states "this plan exists so the requirements coverage gate (§13) accepts SURF-03, SURF-04 as covered." Two of the three confirmation surfaces (HOOK-04 already-shipped; SURF-03 v1.14+ defer; SURF-04 perma-no-add) have ZERO implementation work — Phase 63's contribution is a single architecture test that pins the absence-invariants so accidental future regressions fail loudly. The test is small and lives in `tests/architecture/`, the existing project pattern for invariant pins.

Output: One new test file. ZERO source edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-CONTEXT.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md
@.planning/REQUIREMENTS.md
@extensions/pi-claude-marketplace/shared/notify.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
@extensions/pi-claude-marketplace/commands/plugin/
@extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
</context>

# Artifacts this phase produces

- New `tests/architecture/scope-fences-63.test.ts` — single file with 5 invariant assertions.

<threat_model>
No security-relevant changes. This is a test-only deliverable that pins absence-invariants by grep. The test introduces NO new code in the application source tree and NO new disk-state surface.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/architecture/scope-fences-63.test.ts — pin SURF-03 / SURF-04 absence + HOOK-04 presence</name>
  <files>tests/architecture/scope-fences-63.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (lines 72-104 — confirm `"unsupported hooks"` present at line 81, no `"lossy synthesis"` member; line 81 verified by earlier grep 2026-06-16)
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts (line 1573 — confirm `MANIFEST_FIELD_REASONS` = `new Set(["lspServers"])`; no `"hooks"`)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (existing list output composition — confirm no `hooks` column)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Pitfall 3 — HOOK-04 already done; Pitfall 4 — SURF-03 must NOT ship; SURF-04 absent-additions list)
    - tests/architecture/ (existing test files — pick one as the style baseline; e.g., any architecture/no-shell-out style test for the grep-by-readFile pattern)
  </read_first>
  <behavior>
    - Test 1 (SURF-03 absence — no lossy-synthesis tokens): grep `shared/notify.ts` for the substrings `"lossy synthesis"`, `"lossy"`, `"LOSSY_SYNTHESIS"`, `"<lossy synthesis>"`; assert ZERO matches. The grep covers the source file; the comment policy `.claude/rules/typescript-comments.md` already prohibits `Pitfall N` references but does not block the literal string `"lossy synthesis"` if it slipped in.
    - Test 2 (SURF-04 absence — no `/claude:plugin hooks` edge handler): `fs.access('extensions/pi-claude-marketplace/commands/plugin/hooks.ts')` REJECTS with ENOENT. Also assert via directory scan: `readdir('extensions/pi-claude-marketplace/commands/plugin/', {withFileTypes: true})` includes NO entry whose name matches `/^hooks\.(ts|js|cjs|mjs)$/`.
    - Test 3 (SURF-04 absence — no hook-count column on list): grep the list-command source (the file that builds `list` rows; identify via `grep -rln "componentsResolved\\|component_count\\|component count" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts extensions/pi-claude-marketplace/commands/plugin/list.ts` if either exists) for the substrings `"hookCount"`, `"hook_count"`, `"hooks count"`, `"hooks-column"`, `"hookColumn"`. ZERO matches.
    - Test 4 (HOOK-04 presence — `"unsupported hooks"` token in REASONS): grep `shared/notify.ts` for the substring `"unsupported hooks"`; assert AT LEAST one match. This pins the prior-completion invariant from D-58-01.
    - Test 5 (HOOK-04 presence — `MANIFEST_FIELD_REASONS` excludes `"hooks"`): grep `orchestrators/plugin/install.ts` for the line containing `MANIFEST_FIELD_REASONS`; assert the matched line contains `"lspServers"` AND does NOT contain `"hooks"` (verified by checking that no string literal `"hooks"` appears in the same line as `MANIFEST_FIELD_REASONS`).
  </behavior>
  <action>
    1. Create `tests/architecture/scope-fences-63.test.ts` using `node --test` + `node:assert/strict` + `node:fs/promises` + `node:path`.

    2. Implement Test 1..5 from `<behavior>`. Each test reads the relevant source file once via `readFile(..., "utf8")` and runs the substring / regex assertions.

    3. For Test 2 (directory scan), use `readdir(path, { withFileTypes: true })` then filter for `dirent.isFile()` and the regex above; assert the resulting array is empty.

    4. The test file's top-level doc-comment cites the three guarded REQs by ID (`SURF-03`, `SURF-04`, `HOOK-04`) and the source decisions (`D-58-01` for the HOOK-04 prior completion) per the project's TypeScript comment policy at `.claude/rules/typescript-comments.md` — decision IDs and REQ-IDs are EXPLICITLY allowed as traceability anchors in code comments. Do NOT reference phase numbers (`Phase 63`, `Phase 58`) in the comment.

    5. Do NOT touch any application source file. Do NOT add a "deletion" of any existing code — the test asserts what is already absent.

    6. If Test 3's grep target file path is ambiguous (the list-command source may live in `orchestrators/plugin/list.ts` OR `commands/plugin/list.ts`), the test asserts the absence across BOTH potential paths — if a file does not exist, the per-file assertion is trivially satisfied (skip its grep).
  </action>
  <verify>
    <automated>npm test -- tests/architecture/scope-fences-63.test.ts</automated>
  </verify>
  <done>
    `tests/architecture/scope-fences-63.test.ts` exists; all 5 invariant tests pass against the current codebase state; the file references SURF-03 / SURF-04 / HOOK-04 / D-58-01 by ID in its doc-comment for traceability; no source file is modified.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/architecture/scope-fences-63.test.ts` green.
- `npm run check` green from a clean checkout.
- `grep -c '"lossy synthesis"' extensions/pi-claude-marketplace/shared/notify.ts` returns 0 (Pitfall 4 satisfied).
- `grep -c '"unsupported hooks"' extensions/pi-claude-marketplace/shared/notify.ts` returns at least 1 (HOOK-04 prior completion).
- `ls extensions/pi-claude-marketplace/commands/plugin/hooks.* 2>&1 | grep -c "No such"` returns 1 (no standalone hooks command — SURF-04).
- `grep -E "hookCount|hook_count|hooks count" extensions/pi-claude-marketplace/orchestrators/plugin/list.ts 2>/dev/null | wc -l` returns 0 (no hook-count column — SURF-04).
</verification>

<success_criteria>
- SURF-03 covered by recorded-absence: the architecture test pins zero `"lossy synthesis"` references; no synthesis-caveat warnings ship in v1.13 sources.
- SURF-04 covered by recorded-absence: the architecture test pins no `/claude:plugin hooks` edge handler exists and no hook-count column lands on `list`.
- HOOK-04 prior-completion confirmed: the architecture test pins `"unsupported hooks"` in REASONS and excludes `"hooks"` from `MANIFEST_FIELD_REASONS` — D-58-01 atomic-supersession state preserved.
- Pitfall 3 averted: no Phase 63 code renames `"hooks"` → `"unsupported hooks"` (already done in Phase 58).
- Pitfall 4 averted: no new closed-set token starting with `lossy` / `synthesis`; no new install-arm warning emission outside the orphan-rewake row.
- Future regression on any of the three invariants fails the test loudly.
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-07-SUMMARY.md` when done. Summary records: the test pass count; the 5 invariant assertions and their source-file targets; the prior `"unsupported hooks"` line number from `shared/notify.ts`; and a one-line "scope fences locked; v1.13 milestone close-out ready" handoff.
</output>
