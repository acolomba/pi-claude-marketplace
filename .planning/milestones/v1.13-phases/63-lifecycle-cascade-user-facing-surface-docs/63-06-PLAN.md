---
phase: 63-lifecycle-cascade-user-facing-surface-docs
plan: 06
type: execute
wave: 4
depends_on: []
files_modified:
  - docs/hooks.md
  - README.md
  - tests/docs/hooks-doc.test.ts
autonomous: true
requirements: [SURF-06]
must_haves:
  truths:
    - "`docs/hooks.md` exists at the repository root `docs/` directory; the file is the first-time-reader hook-support doc per D-63-09."
    - "`README.md` carries a new `## Hook support` top-level section linking to `docs/hooks.md`; placed between `## Configuration files` and `## /claude:plugin reference` per RESEARCH Pitfall 10."
    - "`docs/hooks.md` contains in this exact section order per D-63-09: intro paragraph → How hooks run under Pi → Supported events table (8 events) → Worked examples (6 H3 subsections per D-63-11) → Unsupported events → Tool name mapping → What happens to my plugin? → Marketplace coverage → Further reading."
    - "The Further reading section cross-references exactly TWO authority docs per D-63-10: `https://code.claude.com/docs/en/hooks` (URL) and the `@mariozechner/pi-coding-agent` package (name only, no URL — Assumption A7)."
    - "`docs/hooks.md` contains ZERO internal jargon: grep returns 0 for `bucket`, `REQ-`, `Phase ` (capital-P + space), `D-` (decision-ID pattern), `<lossy synthesis>`, `Pitfall`, `Pattern ` (capital-P + space) — per RESEARCH Pitfall 9."
    - "`docs/hooks.md` contains all 8 supported event names verbatim: SessionStart, UserPromptSubmit, PreToolUse, PostToolUse, PostToolUseFailure, PreCompact, PostCompact, SessionEnd."
    - "`docs/hooks.md` uses Claude Code's own field names verbatim: `matcher`, `if`, `asyncRewake`, `timeout`, `command`, `args`."
    - "`docs/hooks.md` documents that the bridge sets `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_ROOT`, and `CLAUDE_PLUGIN_DATA` in the hook child process environment — verified against the exec layer at `bridges/hooks/dispatch-exec.ts:329` + `bridges/hooks/async-rewake/registry.ts:608` (both call `assertPathInside(loc.dataRoot, pluginData, \"CLAUDE_PLUGIN_DATA\")` and set the env var); the var is also documented in `docs/research/claude-hook-config-syntax.md` section 5."
    - "All 6 worked examples ship per D-63-11: auto-formatter, bash-safety net, SessionStart rule injection, prompt audit log, background security review (asyncRewake), compaction snapshot."
  artifacts:
    - path: docs/hooks.md
      provides: "first-time-reader hook-support doc"
      min_lines: 150
    - path: README.md
      provides: "new ## Hook support section linking to docs/hooks.md"
    - path: tests/docs/hooks-doc.test.ts
      provides: "jargon-leak grep test + 8-event coverage test + cross-ref test + README-link test"
  key_links:
    - from: README.md
      to: docs/hooks.md
      via: "markdown link [Hook support reference](docs/hooks.md)"
      pattern: "docs/hooks\\.md"
    - from: docs/hooks.md
      to: code.claude.com/docs/en/hooks
      via: "Further reading bullet (URL)"
      pattern: "code\\.claude\\.com/docs/en/hooks"
    - from: docs/hooks.md
      to: "@mariozechner/pi-coding-agent"
      via: "Further reading bullet (package name, no URL)"
      pattern: "pi-coding-agent"
---

<objective>
Ship the user-facing hook-support documentation: a new `docs/hooks.md` first-time-reader doc following the D-63-09 section order and D-63-11 worked-example list, plus a new `## Hook support` section in `README.md` linking to it. Closes SURF-06.

Purpose: SURF-06 mandates plain-English writing for two reader personas — plugin authors evaluating "will my plugin work?" and end users seeing `(unavailable) {unsupported hooks}` and wondering why. Internal jargon (bucket-A/D taxonomy, REQ-IDs, phase numbers, decision IDs, `<lossy synthesis>` markers, `Pitfall N` / `Pattern N` references) is FORBIDDEN in the doc itself. The new architecture lint test enforces the jargon-leak prohibition by grep, the 8-event coverage by grep, and the two cross-references by grep — all as a single `tests/docs/hooks-doc.test.ts` file (Wave 0 dependency).

Output: One new doc, one README section, one new architecture-lint test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-CONTEXT.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md
@.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-VALIDATION.md
@README.md
@docs/messaging-style-guide.md
@docs/research/claude-hook-config-syntax.md
@docs/research/claude-hooks-vs-pi-events.md
@extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
@extensions/pi-claude-marketplace/domain/components/hook-events.ts
@extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
@extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
</context>

# Artifacts this phase produces

- New `docs/hooks.md` (~200-400 lines; first-time-reader voice; D-63-09 section order; D-63-11 worked examples).
- New `## Hook support` section in `README.md` (heading style `##`, placement between `## Configuration files` and `## /claude:plugin reference`).
- New `tests/docs/hooks-doc.test.ts` architecture-lint test (jargon-leak grep + 8-event coverage + cross-refs + README link presence).

<threat_model>
## Trust Boundaries

No security-relevant changes. Doc-only deliverables.

The only marginal threat is the cross-reference URL — Pitfall + threat-model coverage:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-06-XR | Spoofing | doc cross-reference URL | mitigate | The Further reading URL is hard-coded to `https://code.claude.com/docs/en/hooks` (Claude's official domain). The `@mariozechner/pi-coding-agent` cross-reference is by package name only (no URL — Assumption A7 hedges against URL drift). The architecture-lint test pins both. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/hooks.md per D-63-09 / D-63-11 content blueprint</name>
  <files>docs/hooks.md</files>
  <read_first>
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Example 8 lines 1218-1307 — the binding content blueprint; Pitfall 9 — jargon list)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (docs/hooks.md section — section order)
    - docs/research/claude-hook-config-syntax.md (verbatim Claude Code field names; per-event stdin contract; environment variable contract — section 5 lists CLAUDE_PROJECT_DIR / CLAUDE_PLUGIN_ROOT / CLAUDE_PLUGIN_DATA as the three bridge-set env vars for plugin hooks)
    - docs/research/claude-hooks-vs-pi-events.md (Pi ↔ Claude tool-name mapping audit + currently-unmapped Claude tools list)
    - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts (the authoritative tool-name map for the mapping table)
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (BUCKET_A_EVENTS for the 8-event list)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts (line 329 — exec layer sets `CLAUDE_PLUGIN_DATA: pluginData` in the hook child env; line 323 asserts containment)
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts (line 608 — async-rewake registry sets the same `CLAUDE_PLUGIN_DATA: pluginData` env var on re-wake)
    - docs/messaging-style-guide.md (tone/voice baseline — plain prose, no jargon)
    - README.md (existing top-level section voice — `## Configuration files` at line 130 is the tone analog)
    - https://google.github.io/styleguide/docguide/style.html (the user's global writing style — Google Markdown guide; sentence case headings, no emoji, no decorative punctuation)
  </read_first>
  <action>
    Create `docs/hooks.md` with the following structure. Length target: 200-400 lines. Apply the Google Markdown style guide (sentence-case headings; no emoji; one sentence per line is OK but not mandatory; prefer short paragraphs).

    1. **Title and intro paragraph.** Single `# Hook support` H1 (the file's only H1). One paragraph that says: hooks let Claude Code plugins observe and react to session events and tool calls; this marketplace translates each hook into a Pi event subscription so hooks run under Pi the same way they would under Claude Code; the rest of the doc explains which events work in v1.13, which do not, and what to expect when installing a plugin that ships hooks.

    2. **## How hooks run under Pi** — one paragraph. State that the hook child process runs with cwd equal to the user's project directory (Pi `ctx.cwd`), that absolute paths to commands are allowed, that `${CLAUDE_PLUGIN_ROOT}` is the plugin-relative interpolation idiom, and that `$PATH` applies. Mention that the bridge sets three env vars in the hook child environment: `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_ROOT`, and `CLAUDE_PLUGIN_DATA`. The values are verified-real (not speculative): the exec layer at `bridges/hooks/dispatch-exec.ts:329` and the async-rewake registry at `bridges/hooks/async-rewake/registry.ts:608` both set `CLAUDE_PLUGIN_DATA: pluginData` after `assertPathInside(loc.dataRoot, pluginData, "CLAUDE_PLUGIN_DATA")`; `docs/research/claude-hook-config-syntax.md` section 5 documents the public contract. Do NOT mention internal architecture (no bucket-A/D, no phase numbers, no REQ-IDs).

    3. **## Supported events** — a markdown table with two columns (Event, Description) and 8 rows. Use the verbatim event names from `BUCKET_A_EVENTS`. Plain-English one-paragraph descriptions per event (RESEARCH Example 8 lines 1240-1249 shows the table shape). Each description names the firing condition and the plugin author's typical use case in one to two sentences.

    4. **## Worked examples** — six H3 subsections (`### Auto-formatter`, `### Bash-safety net`, `### Session start rule injection`, `### Prompt audit log`, `### Background security review`, `### Compaction snapshot`). Each subsection is 15-30 lines and contains:
       - 1-2 sentences describing the use case in plain English.
       - A fenced JSON block showing the `hooks.json` snippet (use real, runnable JSON — schema-valid per `HOOKS_VALIDATOR`).
       - 1-2 sentences explaining what happens at runtime (which event fires, what the handler reads from stdin, what it writes to stdout, what exit codes mean).

       Per D-63-11 all 6 examples ship — do NOT trim. The background security review example MUST demonstrate `asyncRewake: true` + `rewakeMessage`. The bash-safety net example MUST demonstrate the `if: Bash(...)` permission-rule glob.

    5. **## Unsupported events** — bulleted list of unsupported event groups, each with a one-line plain-English reason. Use the groups from SURF-06's REQ text:
       - "Deferred for engineering reasons" — FileChanged, Stop, CwdChanged, PostToolBatch, UserPromptExpansion, StopFailure, SubagentStart, SubagentStop.
       - "Blocked on upstream Pi support" — Notification, PermissionRequest, PermissionDenied, MessageDisplay, TeammateIdle, Elicitation, ElicitationResult, WorktreeCreate, WorktreeRemove.
       - "Permanently inapplicable to Pi" — ConfigChange, Setup, InstructionsLoaded, TaskCreated, TaskCompleted.

       Each line uses plain English. Do NOT mention bucket letters; do NOT cite REQ-IDs.

    6. **## Tool name mapping** — a two-column markdown table mapping Pi tool names (lowercase) to Claude tool names (capitalized). Populate from `domain/components/hook-tool-names.ts`. Below the table, a one-paragraph "Currently unmapped Claude tools" section listing the Claude tools without a Pi analog (e.g., MultiEdit, NotebookEdit, WebFetch, Task, ...) — this list lives in `docs/research/claude-hooks-vs-pi-events.md`.

    7. **## What happens to my plugin?** — plain-language decision tree (use bullet points, not a flowchart). Examples:
       - "If your plugin uses only events in the Supported events list, your plugin's hooks will fire under Pi."
       - "If your plugin references any other event, your plugin will install with `(unavailable) {unsupported hooks}` and its hooks will not fire."
       - "If your plugin uses a tool-name matcher (`PreToolUse` with `matcher: \"MultiEdit\"`) for a Claude tool that has no Pi analog, your plugin will install with `(unavailable) {unsupported hooks}`."
       - "If your plugin uses a regular-expression matcher, your plugin will install with `(unavailable) {unsupported hooks}` — only literal matchers and pipe-OR alternation (`Edit|Write`) are supported in v1.13."
       - "If your plugin uses a handler type other than `\"command\"` (for example `\"http\"`), your plugin will install with `(unavailable) {unsupported hooks}`."

       Close with a one-paragraph note on why we chose strict supportability over partial support (plain English: a partially-loaded hook set would surprise the plugin author; failing fast surfaces the gap so they can plan around it).

    8. **## Marketplace coverage** — short paragraph noting that 10 of 13 plugins in the bundled marketplace install with hooks fully supported in v1.13; the remaining 3 (`ralph-wiggum`, `hookify`, `security-guidance`) declare hooks targeting unsupported events and install with `(unavailable) {unsupported hooks}`. Mention that future milestones may expand support.

    9. **## Further reading** — two bullets:
       - `- [Claude Code hooks reference](https://code.claude.com/docs/en/hooks) — the upstream authoritative field reference (precedence rules, full \`if\`-permission-rule grammar, environment variables, exit-code contract).`
       - `- Pi extension API documentation via the \`@mariozechner/pi-coding-agent\` package — how hooks integrate with the Pi runtime (event emission, message injection, user notifications).`

    JARGON GUARD (per Pitfall 9): the final doc MUST NOT contain any of these tokens (case-sensitive):
    - `bucket-A`, `bucket-B`, `bucket-C`, `bucket-D`, `bucket-E`, `bucket-F`, `bucket-G`, `bucket-H`, `bucket A`, `bucket D`, etc.
    - Any `REQ-` prefix.
    - Any `Phase ` (capital-P, space).
    - Any `D-NN-NN` decision ID pattern.
    - `<lossy synthesis>`.
    - `Pitfall ` (capital-P, space).
    - `Pattern N` references like `Pattern 1`, `Pattern 4`.
    - Any reference to `.planning/`, `GSD`, `RESEARCH.md`, `CONTEXT.md`, or any other internal planning artefact.

    Use Claude Code's verbatim field names: `matcher`, `if`, `asyncRewake`, `timeout`, `command`, `args`. The doc is allowed to reference Claude tool names (`Bash`, `Edit`, `Read`, etc.) and Pi event names verbatim.

    SANITY CHECK before declaring done: run `grep -E "bucket-[A-Z]|bucket [A-H]|REQ-|D-[0-9]|<lossy synthesis>|Pitfall |Pattern [0-9]" docs/hooks.md` — must return zero matches. Also run `grep -c "Phase " docs/hooks.md` — must return zero (the doc never references phase numbers).

    A6 sanity check: re-verify the "10/13" marketplace coverage figure by inspecting the bundled marketplace plugin set if accessible; if the number differs, use the verified count.
  </action>
  <verify>
    <automated>npm test -- tests/docs/hooks-doc.test.ts</automated>
  </verify>
  <done>
    `docs/hooks.md` exists, follows the 9-section structure per D-63-09, contains the 6 worked examples per D-63-11, lists all 8 BUCKET_A events verbatim, references the two authority docs per D-63-10, documents the three bridge-set env vars (`CLAUDE_PROJECT_DIR` / `CLAUDE_PLUGIN_ROOT` / `CLAUDE_PLUGIN_DATA`) per the exec-layer source citation, contains zero internal-jargon tokens; the architecture-lint test (Task 3) is green against it.
  </done>
</task>

<task type="auto">
  <name>Task 2: README.md — new ## Hook support section linking to docs/hooks.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — locate `## Configuration files` at line 130 and `## /claude:plugin reference` at line 171; the new section goes between them per Pitfall 10)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-PATTERNS.md (README.md section — Phase 63 ## Hook support shape lines 340-352 verbatim)
  </read_first>
  <action>
    1. Insert a new section between `## Configuration files` (and its subsections) and `## /claude:plugin reference`. The section heading is exactly `## Hook support` (sentence-case per the Google style guide — "Hook" is title-case because it leads a noun phrase; "support" lowercases as part of the phrase per Google Markdown headings convention; verify against the existing README sections' casing — most use Sentence case, e.g., `## Configuration files`).

    2. Body content (mirror PATTERNS lines 340-349 verbatim, with one short paragraph + a `See` link):
       ```
       ## Hook support

       Claude Code plugins can ship hook handlers that fire on session
       events and tool calls. This marketplace translates those hooks
       into Pi event subscriptions so they run under Pi the same way
       they would under Claude Code.

       See [Hook support reference](docs/hooks.md) for the supported
       events, worked examples, and a per-plugin compatibility guide.
       ```

    3. Do NOT wrap the new section in `<details>`; use the same `##` heading style as adjacent sections.

    4. Do NOT modify any other line of `README.md`.
  </action>
  <verify>
    <automated>npm test -- tests/docs/hooks-doc.test.ts</automated>
  </verify>
  <done>
    `README.md` contains the `## Hook support` heading at the correct placement; the body links to `docs/hooks.md` via markdown link; no other README lines change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/docs/hooks-doc.test.ts — architecture-lint for jargon + 8 events + cross-refs + README link</name>
  <files>tests/docs/hooks-doc.test.ts</files>
  <read_first>
    - tests/architecture/ (locate any existing docs-lint test for tone/structure — mirror the assertion style)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-VALIDATION.md (Wave 0 hooks-doc.test.ts entry)
    - .planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-RESEARCH.md (Pitfall 9 jargon list; Phase Requirements → Test Map for SURF-06 — lines 1414-1418 enumerate the grep predicates)
  </read_first>
  <behavior>
    - Test 1 (`docs/hooks.md` exists): `fs.access('docs/hooks.md')` resolves without throwing.
    - Test 2 (README.md links to docs/hooks.md): `readFile('README.md')` contains the substring `docs/hooks.md`.
    - Test 3 (8 events present): `readFile('docs/hooks.md')` contains each of `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PreCompact`, `PostCompact`, `SessionEnd` verbatim (case-sensitive substring match).
    - Test 4 (two cross-refs present): `readFile('docs/hooks.md')` contains BOTH `code.claude.com/docs/en/hooks` AND `pi-coding-agent` substrings.
    - Test 5 (zero internal jargon): for each forbidden token in `["bucket-A", "bucket A", "bucket-B", "bucket-C", "bucket-D", "bucket D", "bucket-E", "bucket-F", "bucket-G", "bucket-H", "REQ-", "<lossy synthesis>", "Pitfall "]`, `readFile('docs/hooks.md').includes(token)` returns false (the test iterates and asserts zero hits).
    - Test 6 (no decision-ID pattern + no phase-number references): regex `/D-\d{2}-\d{2}/` returns zero matches against `docs/hooks.md`; substring `"Phase "` returns zero matches.
    - Test 7 (no path leak): regex `/\.planning\/|RESEARCH\.md|CONTEXT\.md/` returns zero matches against `docs/hooks.md`.
    - Test 8 (6 worked-example H3 headings present): each of `### Auto-formatter`, `### Bash-safety net`, `### Session start rule injection`, `### Prompt audit log`, `### Background security review`, `### Compaction snapshot` (or close variants — the exact wording is allowed to vary by leading lowercase prefix per Google sentence-case style) — verify via case-insensitive substring presence of the EXAMPLE NAMES from D-63-11.
  </behavior>
  <action>
    1. Create `tests/docs/hooks-doc.test.ts` using `node --test` + `node:assert/strict` + `node:fs/promises.readFile`.

    2. Implement Test 1..8 from `<behavior>`. Use absolute paths constructed from `process.cwd()` (the project root). Each test reads the doc once and runs multiple assertions over the cached content to keep the suite fast.

    3. The forbidden-token loop (Test 5) uses a `for (const token of FORBIDDEN_TOKENS)` over a `const FORBIDDEN_TOKENS = [...] as const;` declaration so adding a new forbidden token is a one-line edit.

    4. The regex tests (Test 6, Test 7) use `String.prototype.match(/.../)` + assert `result === null`.

    5. The H3 worked-example test (Test 8) does a case-insensitive substring search for the canonical example names (`auto-formatter`, `bash-safety`, `session start`, `prompt audit`, `background security`, `compaction snapshot`) so trivial heading-case variations don't fail the test.

    6. Place the file at `tests/docs/hooks-doc.test.ts` — follows the existing per-area test directory layout.
  </action>
  <verify>
    <automated>npm test -- tests/docs/hooks-doc.test.ts</automated>
  </verify>
  <done>
    `tests/docs/hooks-doc.test.ts` exists; all 8 cases green against the Task 1 / Task 2 outputs; the forbidden-token iteration covers every Pitfall 9 token.
  </done>
</task>

</tasks>

<verification>
- `npm test -- tests/docs/hooks-doc.test.ts` green.
- `npm run check` green (typecheck + lint + format-check + tests).
- `grep -E "bucket-[A-Z]|bucket [A-H]|REQ-|D-[0-9]|<lossy synthesis>|Pitfall |Pattern [0-9]" docs/hooks.md` returns zero matches.
- `grep -c "Phase " docs/hooks.md` returns 0.
- `grep -c "code.claude.com/docs/en/hooks" docs/hooks.md` returns at least 1.
- `grep -c "pi-coding-agent" docs/hooks.md` returns at least 1.
- `grep -c "CLAUDE_PLUGIN_DATA" docs/hooks.md` returns at least 1 (the env-var bullet in `## How hooks run under Pi`).
- `grep -c "## Hook support" README.md` returns exactly 1.
- `grep -c "docs/hooks.md" README.md` returns at least 1.
- All 8 event names appear in `docs/hooks.md` verbatim.
- All 6 worked-example headings appear in `docs/hooks.md`.
</verification>

<success_criteria>
- SURF-06 satisfied: `docs/hooks.md` exists, README links to it, content follows D-63-09 section order + D-63-11 worked-example list + D-63-10 two cross-refs.
- Pitfall 9 averted: zero internal-jargon tokens in the doc (architecture-lint test enforces).
- Pitfall 10 averted: README `## Hook support` heading style matches adjacent sections; placement between `## Configuration files` and `## /claude:plugin reference`.
- A6 (marketplace coverage 10/13) sanity-checked during writing; if the verified number differs, the doc uses the verified count.
- CLAUDE_PLUGIN_DATA documentation is grounded in source (verified at `bridges/hooks/dispatch-exec.ts:329` + `bridges/hooks/async-rewake/registry.ts:608`); no speculative env-var fabrications ship.
- The architecture-lint test is reusable for v1.14+ doc edits (forbidden-token list is a one-line extension).
</success_criteria>

<output>
Create `.planning/phases/63-lifecycle-cascade-user-facing-surface-docs/63-06-SUMMARY.md` when done. Summary records: `docs/hooks.md` line count; the README placement line; the architecture-lint test pass count; the verified marketplace-coverage figure (10/13 or corrected); and a one-line "first-time-reader doc shipped" handoff.
</output>
</content>
