---
phase: 46-type-model-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - tests/architecture/notify-types.test.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify-v2.test.ts
autonomous: true
requirements: [TYPE-01, TYPE-02, TYPE-03, TYPE-04]

must_haves:
  truths:
    - "A dedicated marketplace-not-added NotificationMessage variant carries only name + optional scope (no marketplaceScope/marketplaceDetails placeholders, no runtime renderer carve-out) -- per D-46-01 / TYPE-01"
    - "reasons: ['not added', 'permission denied'] is a compile error (not a render-time length===1 guard) because not added is excluded from the row reasons type -- per D-46-02 / TYPE-02"
    - "The 5 standalone-rendered kinds are enumerated in exactly one isInfoKind guard, and all four consumers (computeSeverity, buildSummaryLine, shouldEmitReloadHint, notify() early-dispatch) route through it with an assertNever tail -- per D-46-04 / TYPE-03"
    - "MarketplaceNotificationMessage is a per-status discriminated union where reasons is reachable only on the skipped arm and details only on the list arm -- per D-46-03 / TYPE-04"
    - "Every v1.0-v1.9 command renders byte-identical output (catalog-UAT byte-equality GREEN); the four {not added} info fixtures are re-keyed to the new variant while asserting identical bytes -- per D-46-05"
    - "npm run check exits 0 over the whole change as one atomic unit (no intermediate RED) -- per D-46-06 / NFR-6"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "MarketplaceNotAddedMessage variant (6th NotificationMessage arm); ContentReason = Exclude<Reason,'not added'>; per-status MarketplaceNotificationMessage union; isInfoKind guard; renderMarketplaceNotAdded renderer"
      contains: "marketplace-not-added"
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts"
      provides: "buildNotAddedMessage returns the new variant (placeholders deleted)"
      contains: "marketplace-not-added"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "inline not-added branch returns the new variant (placeholders deleted)"
      contains: "marketplace-not-added"
    - path: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      provides: "buildImportNotificationMarketplaces final mapping constructs a concrete per-status arm; dead block.reasons removed"
    - path: "tests/architecture/notify-types.test.ts"
      provides: "TYPE-01..04 compile proofs: 6-arm arity, no-placeholder, ContentReason exclusion, per-status co-occurrence"
      contains: "_Assert_NotifSixArms"
  key_links:
    - from: "notify() early-dispatch + dispatchInfoMessage (notify.ts:2106 / 2058)"
      to: "renderMarketplaceNotAdded"
      via: "isInfoKind routes marketplace-not-added through dispatchInfoMessage"
      pattern: "marketplace-not-added"
    - from: "row reasons fields (notify.ts:438/453/498/521/535/585/700)"
      to: "ContentReason"
      via: "readonly ContentReason[] retype"
      pattern: "ContentReason"
    - from: "renderMpHeader switch (notify.ts:917)"
      to: "MpSkipped.reasons / MpList.details"
      via: "per-status union narrowing + assertNever tail"
      pattern: "assertNever\\(mp.status\\)"
---

<objective>
Reshape `extensions/pi-claude-marketplace/shared/notify.ts` (plus `orchestrators/{marketplace,plugin}/info.ts`, `orchestrators/import/execute.ts`, and the three proof/fixture test files) so the message shapes that permitted the v1.10 attribution drift become **unrepresentable**. Four type-model deliverables land together (TYPE-01..04):

1. **TYPE-01:** a dedicated `MarketplaceNotAddedMessage` variant (the 6th `NotificationMessage` arm) carrying only `name` + optional `scope` -- no placeholder `marketplaceScope`/`marketplaceDetails`, no runtime renderer carve-out.
2. **TYPE-02:** `ContentReason = Exclude<Reason, "not added">`; row `reasons` fields retyped so `["not added", "permission denied"]` is a compile error, not a render-time guard.
3. **TYPE-03:** a single `isInfoKind` guard enumerating the 5 standalone-rendered kinds in one place, routed through by all four consumers each with an `assertNever` tail.
4. **TYPE-04:** `MarketplaceNotificationMessage` converted to a per-status discriminated union (reasons only on the skipped arm, details only on the list arm).

Purpose: make the audit's type foot-guns (B-1/M-5, B-2, B-3, B-7) structurally impossible so Phases 47-48 build their attribution corrections on a sound type model. This phase is a **pure type-model foundation** -- it changes ZERO rendered bytes for any v1.0-v1.9 command (D-46-05) and adds ZERO catalog states.

Output: the reshaped type model + lifted renderer + switched info construction sites + the import/execute per-status arm constructor + the re-keyed/extended proofs and fixtures, ALL landing in ONE atomic commit (D-46-06 / NFR-6) gated by `npm run check` exit 0.

**ATOMIC-LANDING CONTRACT (D-46-06):** This plan is implemented as one continuous edit set with NO intermediate commit and NO intermediate RED. The three tasks below are an ordering for the executor; the orchestrator commits the whole change exactly once after `npm run check` is GREEN. Do NOT add a commit step. Run `npm run typecheck` iteratively while editing (fastest TYPE-* signal -- an unused `@ts-expect-error` is itself a typecheck failure).
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-type-model-foundations/46-CONTEXT.md
@.planning/phases/46-type-model-foundations/46-RESEARCH.md
@.planning/phases/46-type-model-foundations/46-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Type-model reshape in shared/notify.ts (variant + ContentReason + per-status mp union + isInfoKind + lifted renderer)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/shared/notify.ts` -- the file being reshaped. Read the exact current shapes (RESEARCH §"Exact Current Shapes", line table): `REASONS` 69-99 + `Reason` 101 (LEAVE UNCHANGED); the 5 row `reasons` fields at 438/453/498/521/535; `MarketplaceNotificationMessage` 580-587; `CascadeNotificationMessage` 609-612; `PluginInfoRowBase.reasons?` 694-701 (field at 700); `NotificationMessage` union 777-782; `renderMpHeader` switch 917-993 (assertNever tail 989); `computeSeverity` 1391-1452 (info gate 1402-1411); `buildSummaryLine` 1535-1569 (info gate 1543-1550); `shouldEmitReloadHint` 1595-1626 (info gate 1603-1610); `renderPluginInfo` carve-out `if` 1964-1977 (body 1969-1976); `dispatchInfoMessage` switch 2058-2074; `notify()` early-dispatch gate 2106-2114.
    - `.planning/phases/46-type-model-foundations/46-RESEARCH.md` -- Patterns 1-5 (§"Architecture Patterns", lines 96-225) for the exact type shapes; "Don't Hand-Roll" (237-248); "Anti-Patterns to Avoid" (226-235); Pitfalls 1, 3 (433-464); Code Examples (485-530).
    - `.planning/phases/46-type-model-foundations/46-CONTEXT.md` -- D-46-01 (variant shape), D-46-01a (byte-neutral render), D-46-02/02a (ContentReason; REASONS untouched), D-46-03/03a (per-status union; failed arm carries NO reasons), D-46-04 (isInfoKind membership + per-consumer behavior), D-46-07 (6th arm).
    - `extensions/pi-claude-marketplace/shared/types.ts:16` -- `Scope` (already imported in notify.ts:5; no new import).
    - `extensions/pi-claude-marketplace/shared/errors.ts:12` -- `assertNever(x: never): never`.
  </read_first>
  <action>
    Apply all type-model changes to `shared/notify.ts` (the renderer's sole-source-of-truth file). Do NOT touch `REASONS` (69-99) or `Reason` (101) per D-46-02a.

    **TYPE-02 -- ContentReason (D-46-02):** Add `export type ContentReason = Exclude<Reason, "not added">;` near the `Reason` definition. Retype the row `reasons` fields from `readonly Reason[]` to `readonly ContentReason[]` at all seven sites (RESEARCH §"Which row types carry reasons", 294-310): `PluginUnavailableMessage.reasons` (438), `PluginUpgradableMessage.reasons` (453), `PluginFailedMessage.reasons` (498), `PluginSkippedMessage.reasons` (521), `PluginManualRecoveryMessage.reasons` (535), the new mp `skipped` arm's `reasons` (was 585), and `PluginInfoRowBase.reasons?` (700). Retype all seven per RESEARCH recommendation (byte-neutral; no site constructs `not added` on any of them -- grep-confirmed only two `"not added"` construction sites exist, both becoming the variant). Do NOT change `composeReasons`'s parameter type -- it stays `readonly Reason[] | undefined` so the new variant renderer can pass the literal `["not added"]` (RESEARCH note, 321-327).

    **TYPE-01 -- variant + lifted renderer (D-46-01 / D-46-01a):** Add the interface `MarketplaceNotAddedMessage { readonly kind: "marketplace-not-added"; readonly name: string; readonly scope?: Scope }` (final name at your discretion per D-46-01) carrying ONLY those fields -- no `marketplaceScope`/`marketplaceDetails`. Add it as the 6th arm of the `NotificationMessage` union (777-782). Create a file-private `renderMarketplaceNotAdded(message, probe: SoftDepStatus): string` by lifting the body of the `renderPluginInfo` `{not added}` carve-out (1969-1976): `joinTokens([ICON_UNINSTALLABLE, message.name, message.scope === undefined ? "" : ` + "`[${message.scope}]`" + `, renderVersion(undefined), "(failed)", composeReasons(["not added"], false, false, probe)])`. The renderer hard-codes the `["not added"]` literal (the variant has no `reasons` field); the version slot collapses to `""` (variant carries no version). **Then DELETE the carve-out `if` block from `renderPluginInfo` (1964-1977)** so the predicate no longer special-cases `["not added"]` (Pitfall 3: leaving it causes a TS2367 once `PluginInfoRowBase.reasons?` is `ContentReason[]`, because `plugin.reasons[0] === "not added"` can no longer match a `ContentReason`).

    **TYPE-04 -- per-status mp union (D-46-03 / D-46-03a):** Replace the single open-optional `MarketplaceNotificationMessage` interface (580-587) with a discriminated union: one arm per `MarketplaceStatus` (`added`/`removed`/`updated`/`failed`/`autoupdate enabled`/`autoupdate disabled`/`skipped`) plus a list/inventory arm with `status?: undefined`. Common fields `name: string`, `scope: Scope`, `plugins: readonly PluginNotificationMessage[]` on every arm. `reasons?: readonly ContentReason[]` ONLY on the `skipped` arm (keep it OPTIONAL -- RESEARCH "Critical narrowing note" 184-190: autoupdate idempotent flips and the mp-update no-op both construct `skipped` with reasons, but `allBenign(undefined)` returns false and `computeSeverity` arm 4 reads `mp.reasons` on the skipped arm). `details?: MarketplaceDetails` ONLY on the list arm. The `failed` arm carries NO `reasons` (D-46-03a -- the reason rides a child plugin row, e.g. update.ts:740-742). Model the list arm as `status?: undefined` so the many status-omitted construction sites compile unchanged and the renderer's `case undefined:` (966) narrows to it (Pitfall 1). Internal arm interface names at your discretion (e.g. `MpAdded`/`MpSkipped`/`MpList`). The `renderMpHeader` switch (917-993) needs NO structural rewrite -- each `case` now narrows to exactly one arm: `case "skipped"` reaches `mp.reasons` (skipped arm), `case undefined` reaches `mp.details` (list arm), other arms have neither; keep the existing `default: assertNever(mp.status)` tail (989).

    **TYPE-03 -- single isInfoKind guard (D-46-04):** Add a `StandaloneKind` type alias enumerating the 5 standalone-rendered kinds in EXACTLY ONE place: `"marketplace-info" | "plugin-info" | "marketplace-info-cascade" | "plugin-info-cascade" | "marketplace-not-added"`, and a type-predicate `function isInfoKind(m: NotificationMessage): m is Extract<NotificationMessage, { kind: StandaloneKind }>`. Document it as "standalone-dispatched kinds" (keep the name `isInfoKind` per TYPE-03 wording even though the set now includes a failure kind). Retrofit all FOUR consumers to call `isInfoKind` and add an `assertNever` exhaustiveness tail where missing (today only `dispatchInfoMessage` + the `notify()` cascade gate have it; `computeSeverity`/`buildSummaryLine`/`shouldEmitReloadHint` use bare `||` chains over 4 kinds with no `assertNever`). Per-consumer behavior for the new `marketplace-not-added` arm (RESEARCH table 213-219): `computeSeverity` -> `"error"`; `buildSummaryLine` -> `""`; `shouldEmitReloadHint` -> `false`; `dispatchInfoMessage` -> add `case "marketplace-not-added": body = renderMarketplaceNotAdded(message, probe); break;` to its switch (2058-2074); `notify()` early-dispatch (2106-2114) -> route via `isInfoKind` so the new kind reaches `dispatchInfoMessage`. After routing through `isInfoKind`, the three cascade-only functions narrow `message` to the cascade arm and keep their existing cascade bodies. Adding a future standalone kind then becomes a compile error in `isInfoKind` (one place) + the `assertNever` tails.

    Do not place fenced code blocks in this action -- the shapes are specified by RESEARCH Patterns 1-5; implement against those signatures.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace/extensions/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` (`tsc --noEmit`) exits 0 after this task's edits AND the Task 2 construction-site edits (the two are interdependent: the variant + union are unused until Task 2 switches the sites and the import refactor lands; typecheck may surface unconverted sites until Task 2 completes -- iterate until exit 0 across Tasks 1+2).
    - `export type ContentReason = Exclude<Reason, "not added">;` exists; `"not added" extends ContentReason` is `false` (proven by the Task 3 `_Assert_NotAddedExcluded`).
    - The `NotificationMessage` union has 6 arms including `MarketplaceNotAddedMessage` with `kind: "marketplace-not-added"` carrying ONLY `kind`/`name`/`scope?` (no `marketplaceScope`, no `marketplaceDetails`).
    - `renderMarketplaceNotAdded` exists and the `renderPluginInfo` `{not added}` carve-out `if` block (was 1964-1977) is DELETED (grep `renderPluginInfo` body shows no `=== "not added"` comparison remaining).
    - `MarketplaceNotificationMessage` is a discriminated union; `reasons` reachable only on the `skipped` arm, `details` only on the list (`status?: undefined`) arm, the `failed` arm has neither `reasons` nor `details` (proven by Task 3 `_NoReasonsOnMpFailed` / `_NoDetailsOnMpSkipped`).
    - Exactly one `StandaloneKind`/`isInfoKind` definition; `computeSeverity`, `buildSummaryLine`, `shouldEmitReloadHint`, and the `notify()` early-dispatch all reference `isInfoKind`; all four close their kind-switch with `assertNever` (grep `isInfoKind` returns its single definition + 4 consumer call sites).
    - `REASONS` (69-99) and `Reason` (101) are byte-unchanged.
  </acceptance_criteria>
  <done>The type model is reshaped: 6-arm union with the new variant, ContentReason exclusion on all 7 row reasons fields, per-status mp union, single isInfoKind guard with assertNever across all four consumers, lifted renderMarketplaceNotAdded with the renderPluginInfo carve-out deleted. Typecheck reaches exit 0 once Task 2 finishes the construction-site sweep.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Switch the info construction sites to the variant + import/execute per-status arm constructor (byte-neutral sweep)</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts:88-108` -- `buildNotAddedMessage` (placeholder `marketplaceScope: scope ?? "user"` at 94, `marketplaceDetails: { autoupdate: false }` at 95; sole caller at 154). NOTE `buildManifestFailureMessage` (117-134) stays a `plugin-info` payload -- it carries a CONTENT reason, NOT `not added`, and is UNAFFECTED (RESEARCH note 353-357).
    - `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts:550-562` -- the inline not-added branch (placeholder `marketplaceScope: opts.scope ?? "user"` at 553, `marketplaceDetails: { autoupdate: false }` at 554).
    - `extensions/pi-claude-marketplace/orchestrators/import/execute.ts` -- `MarketplaceBlock` interface 292-299 (mutable `status?`/`reasons?`); `block.status`/`block.reasons` assignments (373/378/383/391-392 -- only the source-mismatch path 391-392 sets `reasons`); the child source-mismatch `PluginFailedMessage` row 418-426 (carries `reasons: ["not added"]`... NO -- carries `["source mismatch"]`; the mp-level `block.reasons` is DEAD data never rendered by `renderMpHeader`'s failed arm); the conditional-spread final mapping 463-476.
    - `.planning/phases/46-type-model-foundations/46-RESEARCH.md` -- "The 3 info construction sites" (329-357); the 32-site sweep table (359-410, all 30 non-info sites compile unchanged); site #32 deep-dive + Pitfall 2 (411-451); Assumption A1 (the "standard-path coexistence" third site is dispatch-coexistence, not a 3rd literal -- 550) and A2 (import refactor -- 551).
    - `.planning/phases/46-type-model-foundations/46-CONTEXT.md` -- D-46-05 (the 3 confirmed info construction sites; byte-neutral), D-46-03a (failed mp arm carries no reasons).
  </read_first>
  <action>
    Switch the two literal info construction sites to the new variant and adapt the one import accumulator that cannot satisfy the per-status union. All edits are byte-neutral (RESEARCH: 30/32 sites compile unchanged; these are the only behavioral touches, all byte-preserving).

    **Site 1 -- `marketplace/info.ts::buildNotAddedMessage` (88-108):** Replace the returned object with `{ kind: "marketplace-not-added", name, ...(scope !== undefined && { scope }) }` (carry the marketplace `name`; include `scope` only when a `--scope` was requested, matching the current `plugin.scope` conditional spread at 103). Delete the placeholder `marketplaceScope`/`marketplaceDetails` lines. Update the function's docstring (82-87) to describe the dedicated variant instead of the `plugin-info` carve-out. The sole caller at 154 is unchanged (`notify(opts.ctx, opts.pi, buildNotAddedMessage(...))`).

    **Site 2 -- `plugin/info.ts` inline not-added branch (550-562):** Replace the `const message: NotificationMessage = { kind: "plugin-info", ... }` literal with `{ kind: "marketplace-not-added", name: opts.marketplace, ...(opts.scope !== undefined && { scope: opts.scope }) }`. Delete the placeholder `marketplaceScope`/`marketplaceDetails` and the embedded `plugin` object. Refresh the comment (540-549) to describe the variant. The `notify(opts.ctx, opts.pi, message)` call at 563 is unchanged.

    **Site 3 -- "standard-path coexistence" (A1):** This is NOT a third literal construction. Verify `getMarketplaceInfo` (info.ts) and `getPluginInfo` (plugin/info.ts) still type-check and dispatch correctly with the new variant coexisting alongside the standard `marketplace-info`/`marketplace-info-cascade`/`plugin-info`/`plugin-info-cascade` emissions and `buildManifestFailureMessage`. No new construction. (Grep confirms only TWO `"not added"` construction sites in the extension -- both are Sites 1 and 2.)

    **Site #32 -- `import/execute.ts` per-status arm constructor (Pitfall 2 / A2):** The conditional-spread final object (463-476) produces `{ name; scope; status?; reasons?; plugins }`, which matches NO single arm of the per-status union. Refactor the `.map((block) => ...)` to construct the correct concrete arm per `block.status` (a `switch (block.status)` returning the matching arm object). The import path only ever sets `status` to `"added"`/`"updated"`/`"failed"` (lines 373/378/383/391) or leaves it absent -- so the switch needs arms for those plus the `undefined` (list) arm. ALSO remove the now-dead `block.reasons` field from the `MarketplaceBlock` interface (297) and the `block.reasons = ["source mismatch"]` assignment (391-392): `renderMpHeader`'s `failed` arm renders only `(failed)` and never reads `mp.reasons`, so this field is dead data -- the source-mismatch reason already rides the child `PluginFailedMessage` row (418-426), which is unchanged. Removing it is byte-neutral and aligns with D-46-03a (failed arm carries no reasons) and the union (failed arm has no reasons field). Keep this minimal -- the full B-6 reducer cleanup (TYPE-F3) is deferred post-v1.10.

    Do NOT touch the other 30 construction sites in the sweep table -- they compile unchanged against the new union (the `MpList` arm modeled as `status?: undefined` accepts every status-omitted literal; the `failed`/`added`/`updated`/`skipped` literals match their arms structurally).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace/extensions/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0 with Tasks 1+2 both applied (no construction site left unconverted).
    - `marketplace/info.ts::buildNotAddedMessage` and `plugin/info.ts`'s not-added branch each return `{ kind: "marketplace-not-added", name, scope? }` with NO `marketplaceScope`/`marketplaceDetails`/`plugin` placeholder fields (grep `marketplaceScope` returns zero hits in both not-added paths).
    - Grep across the extension shows exactly ZERO `reasons: ["not added"]` construction sites remaining (the literal is now confined to `renderMarketplaceNotAdded`).
    - `import/execute.ts` `buildImportNotificationMarketplaces` returns objects each satisfying a concrete union arm via `block.status`; the `MarketplaceBlock` interface no longer has a `reasons?` field and the `block.reasons = ["source mismatch"]` assignment is gone.
    - `buildManifestFailureMessage` (marketplace/info.ts:117-134) is unchanged (still a `plugin-info` payload -- carries a content reason, not `not added`).
  </acceptance_criteria>
  <done>The two info construction sites emit the new variant (placeholders deleted); import/execute.ts constructs concrete per-status arms and drops the dead block.reasons; the standard-path orchestrators still type-check. Typecheck exits 0 for the whole source change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TYPE-01..04 compile proofs + re-key info fixtures (assert IDENTICAL bytes); full npm run check GREEN gate</name>
  <files>tests/architecture/notify-types.test.ts, tests/architecture/catalog-uat.test.ts, tests/shared/notify-v2.test.ts</files>
  <read_first>
    - `tests/architecture/notify-types.test.ts` -- the proof harness. Read: `_MarketplaceMessageExpected` + `_Assert_MarketplaceMessageShape` (`_mms`) 234-244 (REWRITE target, Pitfall 5); the `_rr*` reason-required asserts 423-444 (stay GREEN -- `ContentReason[] extends Reason[]`); `_Assert_ReasonsLen` (`_l4`, REASONS===29) 654-655 and `_Assert_NotAddedMember` (`_l4b`) 657-658 (KEEP UNCHANGED per D-46-02a); `_Assert_NotifFourArms` (`_l9b`) 843-853 and `_Assert_NotifFiveArms` (`_l10b`) 897-909 (extend to six).
    - `tests/architecture/catalog-uat.test.ts` -- the byte-equality runner. Read the four `{not added}` fixtures: `absent-from-both` 1351-1372, `scope-mismatch-not-added` 1374-1395 (header comment "Anchor preserved byte-identical. DO NOT modify." -- refers to BYTES, which hold; reconcile the comment), `missing-marketplace-not-added-absent-from-both` 1580-1601, `missing-marketplace-not-added-scope-mismatch` 1603-1621.
    - `tests/shared/notify-v2.test.ts` -- the not-added renderer tests: "Phase 42 / INFO-04: {not added} row renders as bare column-0 plugin row..." 2663-2688, "...never carries a reload-hint..." 2690-2715. The OTHER plugin-info/plugin-info-cascade fixtures (2784, 2830, 3064+) are standard `plugin-info` rows that do NOT use `["not added"]` -- they stay `plugin-info`, UNCHANGED.
    - `.planning/phases/46-type-model-foundations/46-RESEARCH.md` -- "Code Examples" 485-530 (the exact `@ts-expect-error` / `_Assert_*` proof bodies for TYPE-01/02/04); "Validation Architecture" 588-665 (the re-key inventory + "What GREEN looks like"); Pitfalls 4 (arity) + 5 (shape) 466-483.
    - `.planning/phases/46-type-model-foundations/46-VALIDATION.md` -- the Requirement->Test map (44-48) and Sign-Off checklist.
    - `.planning/phases/46-type-model-foundations/46-CONTEXT.md` -- D-46-05 (re-key asserting identical bytes; NO new catalog states; catalog markdown NOT edited), D-46-07 (6-arm arity assert), Claude's Discretion (exact assert constants; retire/keep `_Assert_NotifFourArms`).
  </read_first>
  <action>
    Add the TYPE-01..04 compile proofs and re-key the four info fixtures to the new variant while asserting IDENTICAL bytes. Do NOT edit `docs/output-catalog.md` (no new catalog states; byte forms unchanged -- D-46-05).

    **notify-types.test.ts (additive + one rewrite):**
    - ADD the TYPE-02 illegal-mix proof: an `// @ts-expect-error` directive on a `const _illegal: readonly ContentReason[] = ["not added", "permission denied"];` plus `type _Assert_NotAddedExcluded = "not added" extends ContentReason ? never : true; export const _car: _Assert_NotAddedExcluded = true;` (RESEARCH 488-494).
    - ADD the TYPE-01 no-placeholder proofs: `type _VNotAdded = Extract<NotificationMessage, { kind: "marketplace-not-added" }>;` with `// @ts-expect-error` on `export type _NoMpScope = _VNotAdded["marketplaceScope"];` and `export type _NoMpDetails = _VNotAdded["marketplaceDetails"];`, plus a positive field-shape assert (`_VNotAdded extends { kind; name: string; scope?: _Scope } ? true : never`) (RESEARCH 497-507).
    - ADD the 6-arm arity proof `_Assert_NotifSixArms` extending the `_Assert_NotifFiveArms` chain with `Extract<NotificationMessage, { kind: "marketplace-not-added" }> extends never ? never : true` (D-46-07 / Pitfall 4). Retire OR keep `_Assert_NotifFourArms` per your discretion (RESEARCH recommends keeping it as cheap regression coverage).
    - ADD the TYPE-04 co-occurrence proofs: `type _MpSkipped = Extract<MarketplaceNotificationMessage, { status: "skipped" }>; type _MpList = Extract<..., { status?: undefined }>; type _MpFailed = Extract<..., { status: "failed" }>;` then `_Assert_ReasonsOnSkipped` (skipped.reasons assignable), `// @ts-expect-error` on `_NoReasonsOnMpFailed = _MpFailed["reasons"]`, `// @ts-expect-error` on `_NoDetailsOnMpSkipped = _MpSkipped["details"]` (RESEARCH 509-522).
    - REWRITE `_Assert_MarketplaceMessageShape` (`_mms`, 234-244) from the open-optional `_MarketplaceMessageExpected` struct to per-arm `Extract` proofs that prove the new co-occurrence (Pitfall 5) -- the old all-optional struct no longer proves the constraint under a union.
    - KEEP UNCHANGED `_Assert_ReasonsLen` (`_l4`) and `_Assert_NotAddedMember` (`_l4b`) -- D-46-02a forbids touching them. The `_rr*` asserts (423-444) stay GREEN as-is (`ContentReason` is a subtype of `Reason`); optionally tighten to `ContentReason[]` if proving the row-level exclusion is desired (your discretion).

    **catalog-uat.test.ts (re-key 4 fixtures, identical bytes -- D-46-05):** Change each from `kind: "plugin-info"` (+ placeholder `marketplaceScope`/`marketplaceDetails` + `plugin.status: "failed"` + `reasons: ["not added"]` + `componentsResolved: false`) to the variant:
    - `absent-from-both` (1351-1372) -> `{ kind: "marketplace-not-added", name: "ghost-mp" }` (no scope -> no bracket).
    - `scope-mismatch-not-added` (1374-1395) -> `{ kind: "marketplace-not-added", name: "my-mp", scope: "user" }`; reconcile the "DO NOT modify" anchor comment to clarify it preserves BYTES (which hold), not the fixture's literal shape.
    - `missing-marketplace-not-added-absent-from-both` (1580-1601) -> `{ kind: "marketplace-not-added", name: "ghost-mp" }`.
    - `missing-marketplace-not-added-scope-mismatch` (1603-1621) -> `{ kind: "marketplace-not-added", name: "ghost-mp", scope: "user" }`.
    All four keep `expectedSeverity: "error"` and their catalog-state byte blocks unchanged. The runner asserts byte-equality against `docs/output-catalog.md` (lines ~890/900/1016/1031) -- those bytes are unchanged.

    **notify-v2.test.ts (re-key 2 tests):** The two Phase-42 INFO-04 not-added tests (2663-2688 and 2690-2715) re-key to construct `{ kind: "marketplace-not-added", name: "my-mp", scope: "user" }`; keep `assert.equal(args[0], "⊘ my-mp [user] (failed) {not added}")` and `args[1] === "error"` for the first, and the no-`/reload` assertion for the second. These prove TYPE-03's new-arm behavior (severity `error`, no reload-hint). Leave the other plugin-info fixtures (2784/2830/3064+) as `plugin-info` -- UNCHANGED.

    After all edits, run the full `npm run check` and ensure exit 0 (typecheck + eslint + prettier + tests). Fix any prettier/eslint formatting on the touched files (do NOT use `--no-verify`; the orchestrator owns the commit and runs pre-commit hooks).
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace/extensions/pi-claude-marketplace && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 (typecheck + eslint + prettier:check + `node --test` suite all GREEN) -- the single atomic-commit gate (D-46-06 / NFR-6).
    - Every new `@ts-expect-error` directive is CONSUMED (an unused `@ts-expect-error` is itself a typecheck failure -- this is how `_NoMpScope`/`_NoMpDetails`/`_illegal`/`_NoReasonsOnMpFailed`/`_NoDetailsOnMpSkipped` prove negative presence). All new `_Assert_*` constants (`_car`, `_vna`, `_Assert_NotifSixArms`, `_mrs`) evaluate to `true`.
    - `node --test tests/architecture/catalog-uat.test.ts` passes: the 4 re-keyed `{not added}` fixtures render byte-identically (`⊘ ghost-mp (failed) {not added}` with no bracket; `⊘ my-mp [user] (failed) {not added}` with bracket) to `docs/output-catalog.md`. `docs/output-catalog.md` is NOT edited (`git diff --stat docs/output-catalog.md` empty).
    - `node --test tests/shared/notify-v2.test.ts` passes: the two re-keyed not-added tests assert `⊘ my-mp [user] (failed) {not added}` + `error` severity + no `/reload`.
    - `_Assert_ReasonsLen` (`_l4`) and `_Assert_NotAddedMember` (`_l4b`) are byte-unchanged (REASONS still 29 members).
    - No new catalog states added; no v1.0-v1.9 command byte string changed (the full catalog-uat byte-equality runner stays GREEN over every existing state).
  </acceptance_criteria>
  <done>All TYPE-01..04 compile proofs are in notify-types.test.ts (6-arm arity, no-placeholder, ContentReason exclusion, per-status co-occurrence); the four info fixtures are re-keyed to the variant asserting identical bytes; the two notify-v2 not-added tests prove the new-arm severity/reload behavior; `npm run check` exits 0 over the whole atomic change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase introduces NO new trust boundary. It is a compile-time type-model reshape of in-repo source with zero I/O, zero network (NFR-5 preserved), zero new dependencies, and zero on-disk state-shape changes (RESEARCH §"Runtime State Inventory": nothing found in any category). The only mutable surface is the in-repo test fixtures, covered by the byte-equality runner.

| Boundary | Description |
|----------|-------------|
| (none new) | No untrusted input crosses any boundary in this phase; all changes are static types + renderer dispatch + test proofs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | re-keyed info fixtures silently changing rendered bytes | mitigate | catalog-UAT byte-equality runner asserts the 4 `{not added}` states render IDENTICAL bytes to `docs/output-catalog.md`; `docs/output-catalog.md` is NOT edited (D-46-05) -- any byte drift fails the gate. |
| T-46-02 | Tampering | per-status mp union admitting an illegal arm (e.g. reasons on failed) | mitigate | TYPE-04 `@ts-expect-error` co-occurrence proofs (`_NoReasonsOnMpFailed`, `_NoDetailsOnMpSkipped`) fail typecheck if the constraint regresses. |
| T-46-03 | Tampering | structural+content reason mix re-becoming representable | mitigate | TYPE-02 `@ts-expect-error _illegal` + `_Assert_NotAddedExcluded` fail typecheck if `not added` leaks back onto a row `reasons` field. |
| T-46-04 | Elevation of Privilege | a future NotificationMessage kind silently bypassing a consumer | mitigate | TYPE-03 single `isInfoKind` + `assertNever` tails in all four consumers make an unhandled kind a compile error; `_Assert_NotifSixArms` locks the arity. |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (RESEARCH: zero external packages, zero package.json churn). The `## Package Legitimacy Audit` gate is N/A -- no install tasks exist; nothing to verify. |
</threat_model>

<verification>
**Phase gate (the single atomic-commit gate, D-46-06 / NFR-6):**
- `cd extensions/pi-claude-marketplace && npm run check` exits 0 (typecheck + eslint + prettier:check + `node --test` suite).
- `npm run typecheck` exits 0 with every new `@ts-expect-error` CONSUMED and every new `_Assert_*` constant `= true` (TYPE-01..04 compile proofs).
- `node --test tests/architecture/catalog-uat.test.ts` GREEN -- the 4 re-keyed `{not added}` states byte-identical; `docs/output-catalog.md` unedited (`git diff --stat docs/output-catalog.md` empty).
- `node --test tests/shared/notify-v2.test.ts` GREEN -- the 2 re-keyed not-added renderer tests pass.
- `node --test tests/architecture/notify-types.test.ts` GREEN -- `_l4`/`_l4b` (REASONS===29, `not added` membership) byte-unchanged.
- Grep proof of TYPE-01: zero `reasons: ["not added"]` construction sites outside `renderMarketplaceNotAdded`; zero `marketplaceScope`/`marketplaceDetails` in the not-added paths.
- Byte-neutrality proof: the full catalog-uat byte-equality runner stays GREEN over EVERY existing v1.0-v1.9 catalog state (no byte change); no new catalog state added.
</verification>

<success_criteria>
Maps to ROADMAP Phase 46 SC#1-5:
1. **(SC#1 / TYPE-01)** A dedicated `marketplace-not-added` variant carries only `name`/`scope?`; placeholder `marketplaceScope`/`marketplaceDetails` and the runtime renderer carve-out are gone from both confirmed construction sites + the standard-path coexistence; `notify-types.test.ts` proves the placeholder fields are absent (`@ts-expect-error _NoMpScope`/`_NoMpDetails`).
2. **(SC#2 / TYPE-02)** `["not added", "permission denied"]` is a compile error (`@ts-expect-error _illegal` consumed; `_Assert_NotAddedExcluded` true); `not added` no longer shares the open `Reason[]` row field.
3. **(SC#3 / TYPE-03)** The 5 standalone kinds are enumerated in one `isInfoKind` guard; `computeSeverity`/`buildSummaryLine`/`shouldEmitReloadHint`/`notify()` early-dispatch all route through it with `assertNever` tails (today 3 of 4 lack it); a new kind is a compile error in every consumer.
4. **(SC#4 / TYPE-04)** `MarketplaceNotificationMessage` is a per-status discriminated union; a list-surface row cannot carry `reasons` and a skipped row cannot carry `details` (`@ts-expect-error _NoDetailsOnMpSkipped`/`_NoReasonsOnMpFailed` consumed).
5. **(SC#5 / D-46-05/06)** `npm run check` exits 0; catalog-UAT byte-equality GREEN with ZERO output-byte changes for any v1.0-v1.9 command; the reshape + variant + guard + re-keyed fixtures land in ONE atomic commit (orchestrator-owned).
</success_criteria>

<output>
Create `.planning/phases/46-type-model-foundations/46-01-SUMMARY.md` when done.
</output>
