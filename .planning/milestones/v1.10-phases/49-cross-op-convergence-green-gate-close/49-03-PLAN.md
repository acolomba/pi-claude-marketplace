---
phase: 49-cross-op-convergence-green-gate-close
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - tests/architecture/cross-op-convergence.test.ts
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: []   # Verification + closure phase -- NO requirement closure. Targets ROADMAP SC#1 (cross-op convergence proof), SC#3 (catalog inverse-walk / no orphan fixture), SC#4 (GREEN gate), SC#5 (traceability verify). Records ACCEPT of RESEARCH adjudication items 3 (Phase 47 IN-02) + 4 (Phase 47 IN-01).

must_haves:
  truths:
    - "A dedicated cross-op convergence test asserts every converged op (info / install / uninstall / reinstall / plugin-update / marketplace-remove / autoupdate / the newly-converged marketplace-update) emits the BYTE-IDENTICAL `⊘ <name> [scope?] (failed) {not added}` row for the marketplace-absent precondition -- the SC#1 Class-C-closed proof, asserting byte-identity ACROSS ops (not each op against its own block)."
    - "Both canonical rows are covered: the explicit-scope `[scope]` form and the bare bracketless absent-from-both form."
    - "The catalog-uat runner gains an inverse-walk assertion: every FIXTURES (section,state) key has a matching parsed catalog annotation -- orphan-fixture detection (SC#3 `no orphan/stale state`, both directions)."
    - "`npm run check` exits 0 with test count >= 1473 (Phase 45 baseline; expect >= 1502 + the new tests); NFR-5/7/10 unaffected."
    - "REQUIREMENTS.md maps all 15 v1.10 requirements with no TBD (SC#5 verified -- no edit needed)."
  artifacts:
    - path: "tests/architecture/cross-op-convergence.test.ts"
      provides: "The SC#1 cross-op byte-identity matrix over the marketplace-absent precondition"
      min_lines: 40
      contains: "marketplace-not-added"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "An inverse-walk coverage assertion (fixture -> catalog) closing the orphan-fixture half of SC#3"
      contains: "catalog annotation"
  key_links:
    - from: "tests/architecture/cross-op-convergence.test.ts"
      to: "shared/notify.ts renderMarketplaceNotAdded (the one variant + one renderer the 8 ops share)"
      via: "notify(ctx, pi, { kind: \"marketplace-not-added\", name, scope? }) byte capture, asserted equal across ops"
      pattern: "marketplace-not-added"
    - from: "tests/architecture/catalog-uat.test.ts inverse walk"
      to: "FIXTURES (section,state) keys + parsed catalog annotations"
      via: "iterate every FIXTURES key, assert a matching catalog-state annotation was parsed"
      pattern: "FIXTURES"
---

<objective>
Lock Class-C closure as a first-class, regression-locked assertion and gate the milestone GREEN. Add
the dedicated SC#1 cross-op convergence test (the breadth proof the catalog-uat structurally cannot
make -- it checks each state against its OWN block, never state-A bytes == state-B bytes), close the
SC#3 orphan-fixture half-gap (the catalog-uat walks catalog->fixture only; add the inverse
fixture->catalog walk), verify SC#5 traceability (RESEARCH confirms clean -- no edit), and prove the
SC#4 GREEN gate (`npm run check` exit 0, count >= 1473, NFR-5/7/10 unaffected). Record the ACCEPT
adjudications for Phase 47 IN-01 (zero-delta save, perf -- out of convergence scope) and Phase 47 IN-02
(preflightUpdate concurrent-removal `{not in manifest}`, a rare TOCTOU race NOT in the SC#1 matrix) so
the milestone audit sees they were adjudicated, not missed.

Purpose: convergence is achieved by every op constructing the SAME variant + one renderer (Plans
46-48 + 49-01); this plan PROVES no op slipped its own row in. After it: `/gsd-verify-work` ->
`/gsd-audit-milestone` -> `/gsd-complete-milestone`.

Output: a new `tests/architecture/cross-op-convergence.test.ts`, an inverse-walk assertion in
`catalog-uat.test.ts`, and a SUMMARY recording the GREEN-gate evidence + the ACCEPT rationales.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-cross-op-convergence-green-gate-close/49-RESEARCH.md
@.planning/phases/49-cross-op-convergence-green-gate-close/49-CONTEXT.md
@CLAUDE.md

# The variant + renderer the convergence proof drives (one variant, one renderer = byte-identity):
# shared/notify.ts: MarketplaceNotAddedMessage variant (~877-896), renderMarketplaceNotAdded
#   (~2124-2136) -> "⊘ <name> [scope?] (failed) {not added}", dispatched via dispatchInfoMessage,
#   severity error.

# The byte runner whose inverse walk we extend:
@tests/architecture/catalog-uat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the SC#1 cross-op convergence byte-identity matrix test</name>
  <files>tests/architecture/cross-op-convergence.test.ts</files>
  <read_first>
    - .planning/phases/49-cross-op-convergence-green-gate-close/49-RESEARCH.md "Cross-Op Convergence
      Matrix (SC#1) -- VERDICT" table (the 7 op-paths + info model) and "Pattern 1: Cross-op convergence
      test" (the recommended renderer-level matrix: all converged ops emit the IDENTICAL
      `{ kind: "marketplace-not-added", name, scope? }` payload; build the canonical payload once,
      capture the byte string, assert each op's emission equals THAT exact string and equals each other)
      and "Pitfall 4: Forgetting the scope-bracket variants" (TWO canonical rows: explicit-scope with
      bracket + bare bracketless; note `install` ALWAYS carries a scope so it has no bracketless
      variant -- document the asymmetry, do NOT assert a bracketless install row).
    - tests/architecture/catalog-uat.test.ts lines 142-187 (makeCtx() + the piWith*Loaded() mock
      factories -- reuse the same mock-ctx capture idiom: a `mock.fn()` ctx.ui.notify, read
      `ctx.ui.notify.mock.calls[0].arguments`).
    - extensions/pi-claude-marketplace/shared/notify.ts: the MarketplaceNotAddedMessage variant shape
      and renderMarketplaceNotAdded byte form (the canonical row both scope variants produce); confirm
      `notify(ctx, pi, message)` is the public entrypoint and the standalone dispatch path.
  </read_first>
  <action>
    Create `tests/architecture/cross-op-convergence.test.ts` (node:test + node:assert/strict). The test
    proves byte-IDENTITY across ops for the marketplace-absent precondition -- the cross-op equality is
    the NEW information the catalog-uat cannot provide (do NOT re-implement the catalog-uat; do NOT
    assert each op against its own catalog block -- assert each op's emission against the SHARED
    canonical row, per Pitfall 3).

    Recommended renderer-level matrix (RESEARCH Pattern 1, shape 1 -- primary):
    (a) Build the canonical payloads ONCE through the public `notify()` via a fresh mock ctx
        (reuse the makeCtx() + piWithBothLoaded() idiom -- author a small local helper or import the
        existing makeCtx if exported; if not exported, replicate the ~10-line mock-ctx factory):
          - explicit-scope: `{ kind: "marketplace-not-added", name: "ghost-mp", scope: "project" }`
            -> capture body string; assert it === "⊘ ghost-mp [project] (failed) {not added}" and the
            severity arg === "error".
          - bare: `{ kind: "marketplace-not-added", name: "ghost-mp" }` -> capture; assert it ===
            "⊘ ghost-mp (failed) {not added}" (no bracket), severity "error".
    (b) Assert byte-IDENTITY across the converged op set. Because all converged ops (info, install,
        uninstall, reinstall, plugin-update, marketplace-remove, autoupdate, AND the newly-converged
        marketplace-update from Plan 49-01) construct the IDENTICAL variant + share the ONE renderer,
        the convergence proof is: enumerate the op names in a list (a string[] documenting the matrix --
        info, install, uninstall, reinstall, update (plugin form), marketplace remove, autoupdate,
        marketplace update), and for each, run the SAME canonical payload through `notify()` and assert
        the captured body equals the canonical row captured in (a) AND equals every other op's capture.
        The assertion that matters: `assert.equal(opEmission, canonicalRow)` for all ops and both
        scope variants -- proving no op can slip its own row in (a future regression that gave one op a
        different row would break this).
        Add an inline comment documenting the `install`-always-carries-scope asymmetry: install has NO
        bracketless variant (the edge defaults the scope), so the bare/bracketless matrix row is
        asserted over the ops that DO support a bare form (uninstall/reinstall/plugin-update/
        marketplace-remove/autoupdate/marketplace-update/info), and the explicit-scope row is asserted
        over ALL ops including install.
    (c) Add a guard asserting `ctx.ui.notify` is called exactly once per payload (one emission per op,
        IL-2).

    Keep the test self-contained and deterministic -- no disk, no network (NFR-5 by construction; it
    drives the renderer through mock ctx only). Cite SC#1 / Class-C-closed + the RESEARCH matrix in the
    test file's header comment; KEEP no historical-fiction narrative.

    Note (RESEARCH Pattern 1, shape 2): the orchestrator-level regression for the ONE op that needed a
    fix (marketplace update, no-raw-throw, explicit + bare) was already added in Plan 49-01's update
    orchestrator test -- do NOT duplicate it here; this file owns the BREADTH byte-identity matrix.
  </action>
  <verify>
    <automated>node --test tests/architecture/cross-op-convergence.test.ts</automated>
  </verify>
  <done>
    `tests/architecture/cross-op-convergence.test.ts` exists and asserts both canonical rows
    (explicit-scope `⊘ ghost-mp [project] (failed) {not added}` + bare `⊘ ghost-mp (failed) {not added}`)
    are byte-identical across the converged op matrix incl. the newly-converged marketplace-update,
    each at severity error, one emission per payload. `node --test tests/architecture/cross-op-convergence.test.ts`
    exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the catalog-uat inverse-walk (fixture -> catalog) assertion for SC#3 orphan detection</name>
  <files>tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - tests/architecture/catalog-uat.test.ts lines 62-140 (loadCatalogExamples: parses every
      `<!-- catalog-state: STATE -->` into `{ section, state, expected }` examples) and lines 2047-2160
      (the forward walk: catalog -> fixture, failing as `missing-fixture` when a catalog state has no
      fixture -- this is the HALF that already exists).
    - tests/architecture/catalog-uat.test.ts lines 189-227 (the FIXTURES map: outer key = section H2
      string, inner key = state string -- the inverse walk iterates THESE keys).
    - .planning/phases/49-cross-op-convergence-green-gate-close/49-RESEARCH.md "SC#3 -- Catalog
      byte-lock" (the orphan-detection gap: the driver walks catalog->fixture only; a FIXTURES entry
      with no catalog annotation is silently never exercised. The fix: assert every (section,state)
      FIXTURES key has a matching parsed catalog annotation -- the inverse walk).
  </read_first>
  <action>
    Add a NEW `test(...)` to tests/architecture/catalog-uat.test.ts (a sibling of the existing
    forward-walk test -- do NOT modify the forward-walk test's body) that performs the inverse walk:
    (a) `const catalog = await readFile(CATALOG_PATH, "utf8"); const examples = loadCatalogExamples(catalog);`
        Build a Set of `${section}::${state}` keys from the parsed examples.
    (b) Iterate every outer key (section) and inner key (state) of the `FIXTURES` map; for each
        `(section, state)` pair, assert a matching `${section}::${state}` annotation exists in the
        parsed-examples Set. Collect orphans (fixtures with no catalog annotation) into a list and
        `assert.fail` with a readable `[ORPHAN FIXTURE] section=… state=…` listing if any exist.
    (c) Title the test to make the SC#3 intent explicit, e.g.
        "catalog UAT inverse walk: every FIXTURES (section,state) has a matching catalog annotation
        (no orphan/stale fixture)".

    This makes SC#3 "no orphaned/stale catalog state remains" a real both-directions gate: the forward
    walk (catalog->fixture) catches an undocumented-fixture gap; this inverse walk (fixture->catalog)
    catches an orphan fixture. When Plans 49-01/49-02 added new states+fixtures, this inverse check
    confirms they stay paired.

    Cite SC#3 in the test comment; KEEP no historical narrative.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts</automated>
  </verify>
  <done>
    The catalog-uat file has a second test performing the fixture->catalog inverse walk; it is GREEN
    (every FIXTURES key -- including the marketplace-update update-missing-not-added* states from
    49-01 and the marketplace-info manifest-invalid state from 49-02 -- has a matching catalog
    annotation, no orphan). `node --test tests/architecture/catalog-uat.test.ts` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify SC#5 traceability + prove the SC#4 GREEN gate; record the ACCEPT adjudications</name>
  <files>.planning/phases/49-cross-op-convergence-green-gate-close/49-03-SUMMARY.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the Traceability table: all 15 v1.10 requirements TYPE-01..04 /
      ATTR-01..10 / SCOPE-01 mapped to Phases 46-48, coverage "Mapped: 15, Unmapped: 0", no TBD --
      RESEARCH "SC#5" confirms clean; this task VERIFIES, edits only if a TBD or stale mapping is found).
    - .planning/phases/49-cross-op-convergence-green-gate-close/49-RESEARCH.md "Adjudication of the 4
      deferred items": items 3 (Phase 47 IN-02, preflightUpdate concurrent-removal `{not in manifest}`
      -- ACCEPT: rare TOCTOU race, NOT in the SC#1 marketplace-absent matrix, truthful reason exists but
      routing it adds a catalog state for a race path with no user report) and 4 (Phase 47 IN-01,
      install M1 zero-delta state save -- ACCEPT: perf nicety, not a reason/attribution issue, explicitly
      out of convergence scope).
  </read_first>
  <action>
    (a) SC#5 -- Run `grep -c TBD .planning/REQUIREMENTS.md` (expect 0) and confirm all 15 v1.10 IDs
        (TYPE-01..04, ATTR-01..10, SCOPE-01) appear in the Traceability table mapped to a phase with no
        TBD. RESEARCH says clean -- if so, NO edit. ONLY if a TBD or stale/unmapped row is found,
        reconcile it surgically (do NOT flip the milestone status line -- that is
        `/gsd-complete-milestone`'s job, not this phase's).

    (b) SC#4 -- Run the full GREEN gate `npm run check` (typecheck + ESLint + Prettier:check + npm test).
        Confirm exit 0 and test count >= 1473 (Phase 45 baseline; expect >= 1502 from 46-48 + the new
        49 convergence/inverse-walk/probe tests, so strictly higher). Record the exact final count.
        Spot-confirm NFR-5/7/10 are unaffected: the convergence fix (49-01) makes zero gitOps on the
        not-added path (pre-guard loadState only); the IN-02 fix (49-02) is read-only classification;
        no resolver change (NFR-7); no new disk writes outside the scope root (NFR-10).

    (c) ACCEPT RATIONALES -- In the SUMMARY, record that Phase 47 IN-01 (zero-delta state save, perf)
        and Phase 47 IN-02 (preflightUpdate concurrent-removal `{not in manifest}`, rare TOCTOU race
        not in the SC#1 matrix) were ADJUDICATED as ACCEPT-with-rationale (not missed), per RESEARCH
        adjudication items 3+4, so the milestone audit sees them as decided. Also record that
        marketplace-update convergence (item 1) and IN-02 manifest parity (item 2) were CLOSED in
        Plans 49-01 and 49-02 respectively.

    IMPORTANT: do NOT write STATE.md or ROADMAP.md -- the orchestrator owns those. This task's only file
    write is the SUMMARY. Do NOT commit -- the orchestrator owns commits.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    `grep -c TBD .planning/REQUIREMENTS.md` returns 0 and all 15 v1.10 reqs are mapped (SC#5 verified);
    `npm run check` exits 0 with test count >= 1473 (record the exact count, expect >= the 1502 Phase-48
    baseline plus the new 49 tests); NFR-5/7/10 confirmed unaffected; the SUMMARY records the GREEN-gate
    evidence + the ACCEPT rationales for Phase 47 IN-01/IN-02 and the CLOSE of items 1+2. No STATE.md /
    ROADMAP.md writes; no commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → renderer (mock ctx) | the convergence + inverse-walk tests drive the renderer through a mock ctx only -- no disk, no network (NFR-5 by construction) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Tampering | a future op regressing to its own non-canonical `{not added}` row | mitigate | the cross-op byte-identity matrix asserts every op equals the shared canonical row, so a divergent row fails CI (the SC#1 regression lock) |
| T-49-06 | Tampering | an orphan/stale fixture silently never exercised | mitigate | the inverse-walk assertion fails if any FIXTURES key lacks a catalog annotation (SC#3 both-directions gate) |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; `node:test` already in use; `npm run check` runs the existing gate only |
</threat_model>

<verification>
- `node --test tests/architecture/cross-op-convergence.test.ts` GREEN (SC#1 byte-identity matrix, both
  scope variants, incl. the newly-converged marketplace-update op).
- `node --test tests/architecture/catalog-uat.test.ts` GREEN (forward walk unchanged + new inverse
  walk; no orphan fixture).
- `node --test tests/architecture/notify-types.test.ts` GREEN (SC#2 REASONS length-lock 29 +
  `not added` member -- unchanged, confirms no new member crept in across 49-01/49-02).
- `grep -c TBD .planning/REQUIREMENTS.md` -> 0 (SC#5).
- `npm run check` exit 0, test count >= 1473 (SC#4); NFR-5/7/10 unaffected.
</verification>

<success_criteria>
- The SC#1 cross-op convergence is proven by a dedicated byte-identity matrix test (Class-C closure is
  a first-class regression lock).
- SC#3 is a both-directions gate (forward catalog->fixture + new inverse fixture->catalog).
- SC#5 traceability verified clean (all 15 mapped, no TBD).
- SC#4 GREEN gate proven: `npm run check` exit 0, count >= 1473, NFR-5/7/10 unaffected.
- Phase 47 IN-01 + IN-02 recorded as ACCEPT-with-rationale; items 1+2 recorded as CLOSED. Plan is
  independently GREEN. No STATE.md/ROADMAP.md writes; no commit (orchestrator owns both).
</success_criteria>

<output>
Create `.planning/phases/49-cross-op-convergence-green-gate-close/49-03-SUMMARY.md` when done.
</output>
