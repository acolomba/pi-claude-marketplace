---
phase: 49-cross-op-convergence-green-gate-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
  - tests/orchestrators/marketplace/update.test.ts
autonomous: true
requirements: []   # Verification + closure phase -- NO requirement closure. Targets ROADMAP SC#1 (cross-op convergence). Closes RESEARCH adjudication item 1 (marketplace-update gap).

must_haves:
  truths:
    - "`marketplace update <missing-mp>` (explicit scope) emits the canonical standalone `⊘ <name> [scope] (failed) {not added}` row (severity error, no reload-hint) instead of throwing MarketplaceNotFoundError raw past the orchestrator."
    - "`marketplace update <missing-mp>` (bare, absent from both scopes) emits the bracketless `⊘ <name> (failed) {not added}` row."
    - "A genuine refresh failure (MarketplaceUpdateError / network / clone / StateLockHeldError / invalid-manifest) still routes to the existing `(failed)` cascade path -- the convergence catch reroutes ONLY MarketplaceNotFoundError."
    - "The `marketplace update <name>` catalog section documents both new byte forms; catalog-uat byte-equality is GREEN."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts"
      provides: "MarketplaceNotFoundError catch-and-reroute to the marketplace-not-added variant, mirroring remove.ts::resolveScopeOrNotifyNotAdded"
      contains: "kind: \"marketplace-not-added\""
    - path: "docs/output-catalog.md"
      provides: "Two new catalog-state blocks under the marketplace-update section"
      contains: "catalog-state: update-missing-not-added"
    - path: "tests/orchestrators/marketplace/update.test.ts"
      provides: "Regression coverage that the raw throw is gone (explicit-scope + bare forms) byte-locked to the canonical row"
  key_links:
    - from: "orchestrators/marketplace/update.ts"
      to: "shared/notify.ts renderMarketplaceNotAdded"
      via: "notify(ctx, pi, { kind: \"marketplace-not-added\", name, scope? })"
      pattern: "marketplace-not-added"
    - from: "docs/output-catalog.md (update-missing-not-added states)"
      to: "tests/architecture/catalog-uat.test.ts FIXTURES['/claude:plugin marketplace update <name>']"
      via: "catalog-state annotation paired with a notify() fixture"
      pattern: "update-missing-not-added"
---

<objective>
Close the ONE residual Class-C convergence gap the audit named: `marketplace update <missing-mp>`
still throws `MarketplaceNotFoundError` raw past the orchestrator boundary (its edge handler has no
try/catch, confirmed), unlike `remove`/`autoupdate` which already catch-and-reroute. Make the
marketplace-form update miss converge on the canonical standalone `(failed) {not added}` variant so
ROADMAP SC#1 is literally true and the milestone does not ship a live Class-C instance.

Purpose: SC#1 lists `update` in the convergence matrix. The plugin-update `@<mp>` form already
converged in Phase 47; the marketplace-form `marketplace update <name>` is a separate orchestrator
that still throws raw. This plan mirrors remove.ts's already-reviewed catch-and-reroute pattern,
reusing the existing `MarketplaceNotAddedMessage` variant -- NO new REASONS member (tuple stays 29),
NO new mechanism. Byte-additive: it adds emissions + catalog states + fixtures + tests; it mutates
no existing byte form.

Output: a narrowed `MarketplaceNotFoundError` catch in `updateMarketplace`, two new catalog states
(explicit-scope + bare), two paired catalog-uat fixtures, a regression test, and a lockstep update of
the existing absent-name test so the convergence is byte-locked rather than accidentally compatible.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/49-cross-op-convergence-green-gate-close/49-RESEARCH.md
@.planning/phases/49-cross-op-convergence-green-gate-close/49-CONTEXT.md
@CLAUDE.md

# The pattern to mirror (already-reviewed catch-and-reroute):
@extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts

# The file being modified:
@extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts

# The renderer + variant (DO NOT modify -- read to know the exact byte form):
# shared/notify.ts: renderMarketplaceNotAdded (~lines 2124-2136) emits
#   "⊘ <name> [scope?] (failed) {not added}"  (scope present => [scope]; absent => no bracket)
#   dispatched via dispatchInfoMessage (case "marketplace-not-added"); severity = error.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the MarketplaceNotFoundError catch-and-reroute to updateMarketplace</name>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/marketplace/remove.ts lines 163-225
      (resolveScopeOrNotifyNotAdded: the EXACT pattern -- bare form catches resolveScopeFromState's
      MarketplaceNotFoundError and emits `{ kind: "marketplace-not-added", name }` with NO scope
      bracket; explicit-scope form does a single pre-guard loadState read and emits the variant WITH
      the requested scope bracket; a non-MarketplaceNotFoundError is re-thrown).
    - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts lines 200-225
      (updateMarketplace: the resolve seam -- `resolveScopeFromState` for the no-scope form,
      direct `{ scope, locations }` for the explicit-scope form) and lines 433-448
      (snapshotAfterRefresh: where `withStateGuard` throws `MarketplaceNotFoundError(name, [scope])`
      for an explicit-scope miss) and lines 740-766 (refreshOneMarketplace's existing catch, which
      currently swallows the MarketplaceNotFoundError into a synthetic `(failed)` row -- this is the
      non-converged behavior being replaced for the not-added case).
    - extensions/pi-claude-marketplace/shared/notify.ts ~lines 877-896 (MarketplaceNotAddedMessage
      variant: `{ kind: "marketplace-not-added"; name: string; scope?: Scope }`) and ~lines 2124-2136
      (renderMarketplaceNotAdded byte form).
  </read_first>
  <behavior>
    - Test (explicit scope, absent): `updateMarketplace({ name: "ghost", scope: "project", cwd, ... })`
      against empty state emits ONE notify whose body === "⊘ ghost [project] (failed) {not added}"
      and severity === "error"; no MarketplaceNotFoundError escapes; state untouched.
    - Test (bare, absent from both): `updateMarketplace({ name: "ghost", cwd, ... })` (no scope)
      against empty state emits ONE notify whose body === "⊘ ghost (failed) {not added}" (NO bracket)
      and severity === "error".
    - Test (genuine refresh failure still routes to cascade): a seeded marketplace whose refresh
      throws MarketplaceUpdateError (e.g. checkout/manifest failure) still renders the existing
      `(failed)` synthetic-child cascade row -- the catch does NOT swallow it (the mp-failure-network
      / update-path-invalid-manifest behaviors are unchanged).
  </behavior>
  <action>
    In `updateMarketplace` (the MU-1 single-name form, around lines 201-225), wrap the
    scope-resolution + refresh so a `MarketplaceNotFoundError` for a missing marketplace routes to the
    canonical standalone `{not added}` variant instead of escaping raw. Mirror remove.ts's
    `resolveScopeOrNotifyNotAdded` shape (do NOT copy its plugin-cascade body -- update has no
    per-plugin unstage; this is purely the existence gate):

    (a) Bare form (`opts.scope === undefined`): wrap the `await resolveScopeFromState(opts.name, ...)`
        call in a try/catch. On `err instanceof MarketplaceNotFoundError`, call
        `notify(opts.ctx, opts.pi, { kind: "marketplace-not-added", name: opts.name })` (NO scope key
        -- the bare-form absent-from-both emits no bracket, matching remove.ts:191) and `return`. Re-throw
        any non-MarketplaceNotFoundError. Do NOT modify resolveScopeFromState's throw contract (shared
        with the bare form's existing callers).

    (b) Explicit-scope form (`opts.scope !== undefined`): add a single pre-guard
        `loadState(locations.extensionRoot)` existence read BEFORE calling `refreshOneMarketplace`
        (mirror remove.ts:199-209). If `preState.marketplaces[opts.name] === undefined`, call
        `notify(opts.ctx, opts.pi, { kind: "marketplace-not-added", name: opts.name, scope: opts.scope })`
        and `return`. This blocks the explicit-scope miss BEFORE it reaches
        snapshotAfterRefresh/withStateGuard (which would throw `MarketplaceNotFoundError(name, [scope])`
        at lines 437-438). Use `loadState` (already imported) -- NFR-5: a network-free state read, no gitOps.

    Implement (a)+(b) as a private helper (e.g. `resolveScopeOrNotifyNotAdded`) returning
    `{ scope, locations } | undefined`, exactly paralleling remove.ts. `updateMarketplace` calls it;
    on `undefined` it returns immediately (the variant was already emitted); otherwise it proceeds to
    `refreshOneMarketplace` with the resolved scope/locations.

    Caveat (Pitfall 2): the catch in (a) MUST narrow to `err instanceof MarketplaceNotFoundError`
    only. Genuine refresh failures (`MarketplaceUpdateError`, network/clone, `StateLockHeldError`,
    `InvalidMarketplaceManifestError`) MUST keep their existing `(failed)` cascade routing through
    `refreshOneMarketplace`'s catch -- do NOT broaden the catch. The pre-guard read in (b) also must
    not be reached by `updateAllMarketplaces` (the bare-enumeration form), which enumerates existing
    records and never hits a named-but-missing marketplace.

    Add the typed-cause `instanceof MarketplaceNotFoundError` import if not already present
    (`MarketplaceNotFoundError` is already imported from shared/errors.ts at update.ts:104-111).
    Strip any historical phase/wave narrative from new comments; KEEP decision/requirement IDs
    (cite ATTR-06 / D-48-C precedent + SC#1 in the helper docstring, mirroring remove.ts:163-179).
  </action>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>
    `updateMarketplace` no longer lets MarketplaceNotFoundError escape for a missing marketplace:
    explicit-scope renders `⊘ <name> [scope] (failed) {not added}`, bare renders
    `⊘ <name> (failed) {not added}`, both severity error; genuine refresh failures still cascade.
    `grep -n 'throw new MarketplaceNotFoundError' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts`
    still shows ONLY the snapshotAfterRefresh defense-in-depth site (line ~438) -- which is now
    unreachable for the explicit-scope miss because the pre-guard read blocks it first.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document both new byte forms in the catalog + pair catalog-uat fixtures + lock the regression test</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat.test.ts, tests/orchestrators/marketplace/update.test.ts</files>
  <read_first>
    - docs/output-catalog.md lines 1216-1280 (the `## `/claude:plugin marketplace update <name>``
      section: existing states update-no-op-skipped / update-autoupdate-noop-skipped /
      manifest-refresh-changed / mixed-outcomes / mp-failure-network / update-path-invalid-manifest --
      the new not-added states go in THIS section).
    - docs/output-catalog.md lines 559-583 (the plugin `reinstall` / `update` sections'
      `missing-marketplace-not-added` + `missing-marketplace-not-added-absent-from-both` states -- copy
      the exact byte forms and the surrounding prose conventions; the explicit-scope form carries the
      `[scope]` bracket, the bare form does not).
    - tests/architecture/catalog-uat.test.ts lines 189-227 (FIXTURES map shape + per-fixture
      composition rules: `pi` factory, `message`, optional `expectedSeverity`) and the FIXTURES outer
      key for marketplace update (`"/claude:plugin marketplace update <name>"`).
    - tests/architecture/catalog-uat.test.ts lines 2047-2160 (the byte-equality + severity-arg
      driver: every `<!-- catalog-state: X -->` must have a matching `(section, state)` fixture or it
      fails as `missing-fixture`).
    - tests/orchestrators/marketplace/update.test.ts lines 1469-1489 (the existing
      "snapshotAfterRefresh: MarketplaceNotFoundError when name absent" test -- it currently asserts
      only the loose `severity === "error"` + `message.includes("ghost")`; the convergence fix changes
      the byte form, so this test MUST be tightened to the exact canonical row in lockstep).
  </read_first>
  <action>
    (a) CATALOG -- Add two new annotated blocks to the `## `/claude:plugin marketplace update <name>``
        section in docs/output-catalog.md, modeled on the reinstall/update `missing-marketplace-not-added`
        states (lines 559-583). Each block: a short prose paragraph (what triggers it; severity error;
        no reload-hint; cite SC#1 cross-op convergence + ATTR-06/D-48-C precedent), the
        `<!-- catalog-state: STATE -->` discriminator, then the fenced byte body:
          - state `update-missing-not-added` (explicit scope): body
            `⊘ ghost-mp [project] (failed) {not added}`
          - state `update-missing-not-added-absent-from-both` (bare, no bracket): body
            `⊘ ghost-mp (failed) {not added}`
        Use the SAME `name`/`scope` tokens the fixtures use (Task choice: name `ghost-mp`, scope
        `project`) so the byte forms match exactly. If mdformat reformats the file on commit, that is
        expected -- byte-equality of the fenced blocks must hold (atomic supersession).

    (b) FIXTURES -- In tests/architecture/catalog-uat.test.ts, add two inner entries under the
        `"/claude:plugin marketplace update <name>"` outer key:
          - `"update-missing-not-added": { pi: piWithBothLoaded(), message: { kind: "marketplace-not-added", name: "ghost-mp", scope: "project" }, expectedSeverity: "error" }`
          - `"update-missing-not-added-absent-from-both": { pi: piWithBothLoaded(), message: { kind: "marketplace-not-added", name: "ghost-mp" }, expectedSeverity: "error" }`
        (Match the existing fixture composition idiom; the variant carries no soft-dep markers so any
        loaded-pi factory works -- use piWithBothLoaded for consistency. `expectedSeverity: "error"`
        because renderMarketplaceNotAdded routes through computeSeverity to error.)

    (c) REGRESSION TEST -- In tests/orchestrators/marketplace/update.test.ts:
        - TIGHTEN the existing test at lines 1469-1489: rename/retarget it to assert the EXACT
          canonical byte form. Drive `updateMarketplace({ ctx, pi, name: "ghost", scope: "project",
          cwd, gitOps })` against empty state and assert `notifications.length === 1`,
          `notifications[0].message === "⊘ ghost [project] (failed) {not added}"`, and
          `notifications[0].severity === "error"`. Replace the loose `message.includes("ghost")` /
          stale "calls notifyError" comment with the exact-string assertion + a comment citing SC#1
          convergence (the byte form is the canonical {not added} model, no longer a synthetic
          `(failed)` cascade row).
        - ADD a sibling bare-form regression test: drive `updateMarketplace({ ctx, pi, name: "ghost",
          cwd, gitOps })` (NO scope) against empty state and assert the body ===
          "⊘ ghost (failed) {not added}" (no bracket), severity error, and that no error throws
          (the call resolves without rejection -- proving the raw MarketplaceNotFoundError is caught).
        Use the existing `makeCtx()` / `withHermeticHome` / `makeMockGitOps` helpers already in the file.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat.test.ts tests/orchestrators/marketplace/update.test.ts</automated>
  </verify>
  <done>
    Both new catalog states are documented and paired with fixtures (no `missing-fixture` failure);
    catalog-uat byte-equality + severity-arg checks are GREEN; the marketplace-update regression tests
    (explicit-scope tightened + bare-form added) assert the exact canonical `{not added}` byte forms;
    `node --test tests/architecture/catalog-uat.test.ts tests/orchestrators/marketplace/update.test.ts`
    exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI → `marketplace update` orchestrator | untrusted marketplace name crosses here; a missing name must produce a closed-set structured row, not a raw error escaping to the Pi command runner |
| orchestrator → disk (state.json) | the pre-guard existence read is a network-free `loadState` (NFR-5); no writes occur on the not-added path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Information Disclosure | `updateMarketplace` raw `MarketplaceNotFoundError` escape | mitigate | catch-and-reroute to the closed-set `marketplace-not-added` variant so no raw stack/message leaks to the command runner (the Class-C fix itself) |
| T-49-02 | Tampering | over-broad catch swallowing genuine refresh failures | mitigate | narrow the catch to `instanceof MarketplaceNotFoundError`; re-throw all else so MarketplaceUpdateError/network/lock/invalid-manifest keep their `(failed)` cascade routing (Pitfall 2; locked by the unchanged mp-failure-network / update-path-invalid-manifest catalog-uat fixtures) |
| T-49-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan; no dependency surface change |
</threat_model>

<verification>
- `node --test tests/orchestrators/marketplace/update.test.ts` GREEN (regression: explicit-scope +
  bare not-added byte forms; genuine-refresh-failure cascade unchanged).
- `node --test tests/architecture/catalog-uat.test.ts` GREEN (two new states paired; no
  missing-fixture; the unchanged mp-failure-network / update-path-invalid-manifest fixtures stay GREEN
  -- proves the catch did not swallow genuine failures).
- `grep -c 'throw new MarketplaceNotFoundError' extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts`
  returns 1 (only the snapshotAfterRefresh defense-in-depth site remains; it is now unreachable for
  the explicit-scope miss).
- NFR-5: the not-added path makes zero gitOps calls (pre-guard `loadState` only).
</verification>

<success_criteria>
- `marketplace update <missing-mp>` converges on the canonical `(failed) {not added}` standalone
  variant (explicit-scope with bracket + bare without bracket), reusing the existing variant -- no new
  REASONS member, REASONS stays 29.
- Genuine refresh failures keep the existing `(failed)` cascade path.
- Catalog documents both byte forms; catalog-uat byte-equality + the marketplace-update orchestrator
  regression are GREEN. Plan is independently GREEN (no intermediate RED -- byte changes land with
  their catalog/fixture/test pairs).
</success_criteria>

<output>
Create `.planning/phases/49-cross-op-convergence-green-gate-close/49-01-SUMMARY.md` when done.
</output>
