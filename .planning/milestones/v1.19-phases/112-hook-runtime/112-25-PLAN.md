---
phase: 112-hook-runtime
plan: 25
type: execute
wave: 4
depends_on: [112-13]
files_modified:
  - tests/bridges/hooks/routing-state.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-05: every public routing-state verb is exercised with complete case-owned values and cleanup under lowercase phases."
    - "D-18: live epoch, pending SessionStart context, parsed-config cache, and routing buckets are observed only through public operations; no private state reader/reset export is added."
    - "Empty/missing, append/overwrite, stable read order, clear/delete, and composite-reset outcomes are explicit."
    - "The unique side-map-to-RoutingEntry parser contract remains solely in plan 112-13; this owner tests state semantics only."
  artifacts:
    - tests/bridges/hooks/routing-state.test.ts
  key_links:
    - "public epoch/cache/pending/bucket verbs -> module-private routing state -> public reads/reset"
---

<objective>
Refactor routing-state tests around every public state transition and isolation boundary.

Purpose: Prove state semantics without exporting private cells or sharing mutable setup (D-01, D-02, D-04, D-05, D-18).
Output: Refactored routing-state owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@.planning/phases/112-hook-runtime/112-13-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove epoch and pending-context transitions through public verbs</name>
  <files>tests/bridges/hooks/routing-state.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, tests/bridges/hooks/routing-state.test.ts, tests/architecture/hooks-if-field.test.ts, .planning/phases/112-hook-runtime/112-13-SUMMARY.md, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A4 (case-owned state with cleanup registered before action). Per D-01, D-02, D-04, D-05, and D-18, replace shared setup with cases that establish and tear down state through existing public lifecycle verbs. Cover current/bump/reset epoch behavior, including exact successive values; cover pending SessionStart context empty read, empty append/skip behavior, multiple append order, read stability, clear, and read-after-clear. Use complete literal contexts and expectations in every case. Do not expose `liveEpoch` or `pendingSessionStartContext`; do not add a generic reset beyond the existing production lifecycle. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/routing-state.test.ts</automated></verify>
  <acceptance_criteria>Epoch and pending-context transitions are observed through public operations, empty/append/order/clear outcomes are exact, each case restores state, and no production accessor is created.</acceptance_criteria>
  <done>Public epoch and pending-context APIs prove their full state lifecycle independently.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaust parsed-cache, bucket, and composite-reset semantics</name>
  <files>tests/bridges/hooks/routing-state.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, tests/bridges/hooks/routing-state.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A4 (case-owned state with cleanup registered before action). Cover parsed-config cache missing/set/read/overwrite/delete/read-after-delete and routing-bucket default/set/read/replace/read-all behavior with complete case-owned config/table values. Assert stable per-bucket/read-all ordering exactly as implemented, without inventing a cross-stream global order. Exercise the existing composite lifecycle reset and prove it clears `parsedConfigCache`, `routingTable`, `liveEpoch`, and pending context through their public outcomes. The additional-context supplemental is not edited here: plan 112-07 is its sole deletion carrier, and plan 112-13 owns only the unique parser chain. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/routing-state.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/routing-state.test.ts</automated></verify>
  <acceptance_criteria>Missing/set/overwrite/delete, default/set/replace/all, stable ordering, and composite reset have explicit public assertions; all state is restored per case; direct coverage passes with owner-only changes.</acceptance_criteria>
  <done>All public routing-state verbs and reset effects are covered without private-cell access.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session/router callers -> module-private state | Epoch, config, pending context, and route buckets can otherwise leak across sessions or cases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Spoofing | routing-state session ownership | high | mitigate | Case-owned lifecycle setup/cleanup and composite-reset assertions prove isolation through public behavior. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/routing-state.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-25-PLAN.md`.

<verification>
Run the focused test and direct coverage; confirm no production file or supplemental suite changed and no private state identifier became exported.
</verification>

<success_criteria>
Every public routing-state transition, empty boundary, stable ordering, and reset effect is independently covered with complete cleanup.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-25-SUMMARY.md` when done.
</output>
