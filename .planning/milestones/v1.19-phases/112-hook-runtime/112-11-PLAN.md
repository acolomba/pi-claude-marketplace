---
phase: 112-hook-runtime
plan: 11
type: execute
wave: 2
depends_on: [112-12]
files_modified:
  - tests/bridges/hooks/if-field/bash.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-11 proves quote-aware separators, escapes, substitution, unmatched input, recursion/fail-open, wrappers, xargs, dedupe, and specificity (D-01, D-04)."
    - "Equal extracted candidates retain first-seen stable order; duplicates collapse without reordering and empty input has an explicit result."
    - "Wrapper vocabulary is exactly timeout/time/nice/nohup/stdbuf/xargs and recursion stops at 8 without executing shell text."
    - "Bash truth-table evidence is absorbed into this owner but the shared hooks-if-field file is edited only by carrier 112-13 (D-06)."
  artifacts:
    - path: tests/bridges/hooks/if-field/bash.test.ts
      provides: "New canonical direct Bash if-field owner"
  key_links:
    - from: tests/bridges/hooks/if-field/bash.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/if-field/bash.ts
      via: "direct parser/compiler exports over literal command strings"
      pattern: "extract|compile.*Bash"
---

<objective>
Create the Bash if-field parser owner with complete syntax, wrapper, recursion, ordering, and fail-open proof.

Purpose: MOD-05 needs deterministic matcher extraction without executing untrusted command text.
Output: One complete direct owner; shared supplemental pruning remains with 112-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/research/claude-hook-config-syntax.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove quoted command extraction and stable deduplication</name>
  <files>tests/bridges/hooks/if-field/bash.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/bash.ts
    - tests/bridges/hooks/if-field/bash.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - tests/bridges/agents/convert.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
    - docs/research/claude-hook-config-syntax.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing owner with complete literal commands covering unquoted/quoted separators, escapes, repeated candidates, and a direct glob. Prove extraction respects quote boundaries, preserves first-seen order, removes duplicates, and selects the more specific glob when promised. Keep every command, compiled value, and complete expected list independently authored; no test invokes a shell. Apply D-01, D-02, D-04, D-05, and the D-06 absorption boundary. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/if-field/bash.test.ts</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports bash.ts; quote/separator/escape/dedupe/specificity behavior has complete stable-order expectations and the focused node test passes; no shared supplemental edit occurs.</acceptance_criteria>
  <done>The primary quote-aware extraction path is direct and green.</done>
</task>

<task type="auto">
  <name>Task 2: Complete substitution, wrapper, recursion, unmatched, xargs, and empty partitions</name>
  <files>tests/bridges/hooks/if-field/bash.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/bash.ts
    - tests/bridges/hooks/if-field/bash.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
    - docs/research/claude-hook-config-syntax.md
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add independent sibling cases for nested `$()` and backticks, interpolation detection, unmatched quote/substitution fail-open, empty pieces and input, recursion depth 7 versus exact cap 8, nested wrapper chains, wrapper arguments/options for timeout/time/nice/nohup/stdbuf, xargs flags and command start, direct glob fallback, and specificity override. Use complete outputs and stable ordering; do not build an oracle parser. Absorb all Bash rows from hooks-if-field.test.ts without editing it; Plan 112-13 is the only final carrier. Keep lowercase phases and no production change per D-01, D-02, D-04-D-06. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/if-field/bash.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/if-field/bash.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; separator/quote/escape/substitution/unmatched/recursion/wrapper/xargs/empty/dedupe/specificity partitions are complete and stable; no shell execution, shared oracle, supplemental edit, production delta, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-11 is complete and its leaf evidence is ready for carrier 112-13.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| plugin Bash declaration → matcher metadata | Untrusted shell text is parsed but must never execute in this owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-01 | Tampering / Elevation | Bash if-field parser | high | mitigate | Exercise literal syntax/recursion/fail-open partitions without invoking a shell or interpolating test input into a command. |

No bespoke prohibition survives; shell injection is canon and remains in STRIDE.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/if-field/bash.test.ts` as the sole mirrored owner.
- Creates no production symbol and leaves `bash.ts` unchanged.
- Creates `112-11-PLAN.md` and `112-11-SUMMARY.md` after execution.

<verification>
Focused node test, typecheck, and direct coverage pass at 100 percent.
</verification>

<success_criteria>
- P112-11 owns exactly one pair.
- Bash extraction, ordering, recursion, wrappers, and fail-open behavior is complete.
- Supplemental evidence is absorbed without a competing edit.
- No shell execution or high injection threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-11-SUMMARY.md` when done.
</output>
