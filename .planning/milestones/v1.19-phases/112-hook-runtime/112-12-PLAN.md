---
phase: 112-hook-runtime
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/if-field/glob.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-12 proves literal/star/globstar/slash tokenization, command/path boundaries, six anchor arms, containment, bare-name scanning, and metadata (D-01, D-04)."
    - "Empty and zero-segment inputs, adjacent slash/star boundaries, zero/multiple-segment globstars, and normalized-base cases have explicit outcomes."
    - "When anchors compare equally, documented precedence and input order remain stable; containment misses never escape the base."
    - "Glob/path truth tables are absorbed without editing the shared hooks-if-field file reserved for carrier 112-13 (D-06)."
  artifacts:
    - path: tests/bridges/hooks/if-field/glob.test.ts
      provides: "New canonical direct glob compiler owner"
  key_links:
    - from: tests/bridges/hooks/if-field/glob.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
      via: "direct token/compiler/matcher exports"
      pattern: "compile|match"
---

<objective>
Create the glob if-field owner with exact token, anchor, containment, normalization, and ordering proof.

Purpose: MOD-05 requires command/path matching that cannot escape its base or hide adjacency behavior.
Output: One complete direct owner; no production or shared supplemental edit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/research/claude-hook-config-syntax.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one compiled path glob from tokens to containment match</name>
  <files>tests/bridges/hooks/if-field/glob.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
    - tests/bridges/hooks/if-field/glob.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - tests/bridges/agents/convert.test.ts
    - docs/research/claude-hook-config-syntax.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing owner with one complete normalized base and path glob containing literal, slash, star, and globstar tokens. Assert the entire compiled metadata literal and both in-base match and outside-base miss through public exports. Author token/metadata expectations independently rather than invoking another compiler. Use lowercase phases and apply D-01, D-02, D-04-D-06. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/if-field/glob.test.ts</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports glob.ts; one token→metadata→match path proves exact tokens, normalized base, containment, and complete output; the node test passes.</acceptance_criteria>
  <done>The core contained glob compilation path is direct and green.</done>
</task>

<task type="auto">
  <name>Task 2: Complete command boundaries, anchors, globstars, bare names, and edge inputs</name>
  <files>tests/bridges/hooks/if-field/glob.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
    - tests/bridges/hooks/if-field/glob.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - docs/research/claude-hook-config-syntax.md
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add complete sibling cases for each token kind, Bash command boundaries and `:*` sugar, path segment boundaries, all six exact anchor precedence arms and equal-anchor stability, outside containment, bare-name any-depth scanning, zero-segment and multiple-segment globstar matches, empty pattern/base inputs where accepted, adjacent star/slash behavior, and normalized bases with platform separators. Assert entire compiled metadata and matches independently. Absorb glob/path tables from hooks-if-field.test.ts without editing it; 112-13 owns final pruning. Apply D-01, D-02, D-04-D-06. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/if-field/glob.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/if-field/glob.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; token, command/path boundary, anchor, containment, bare-name, globstar, empty, adjacency, stable-order, and metadata partitions are explicit; no duplicate compiler oracle, supplemental edit, production delta, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-12 is complete and supplies the leaf contract for 112-11/13.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| plugin glob/path declaration → filesystem match decision | Untrusted patterns must respect normalized containment and command boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-02 | Tampering / Information disclosure | glob compiler/matcher | high | mitigate | Prove anchors, normalization, containment misses, globstar boundaries, and complete compiled metadata. |

No bespoke prohibition survives; path traversal is canon and remains in STRIDE.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/if-field/glob.test.ts` as the sole mirrored owner.
- Creates no production symbol and leaves `glob.ts` unchanged.
- Creates `112-12-PLAN.md` and `112-12-SUMMARY.md` after execution.

<verification>
Focused node test, typecheck, and direct coverage pass at 100 percent.
</verification>

<success_criteria>
- P112-12 owns exactly one pair.
- Token, anchor, containment, globstar, adjacency, empty, and ordering behavior is complete.
- Supplemental evidence is absorbed without file overlap.
- No high path threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-12-SUMMARY.md` when done.
</output>
