---
phase: 112-hook-runtime
plan: 28
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
  - tests/bridges/hooks/stage.test.ts
  - tests/bridges/hooks/symlink-escape.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-05/D-06: stage cases own real temporary directory trees and cleanup; the symlink supplemental is deleted only here."
    - "D-UTR-12: missing/non-directory trees, in-tree links, direct/buried escapes, portable normalized paths, deterministic unreadable-link fallback, and unexpected I/O have explicit containment outcomes."
    - "Atomic stage bytes, safe names, containment, idempotence, and existing/missing removal are independently asserted."
    - "The private `stack.pop()` undefined guard is removed only after live CodeGraph call-path/source proof confirms it is unreachable; no impossible state is manufactured."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
    - tests/bridges/hooks/stage.test.ts
  key_links:
    - "case-owned filesystem tree -> symlink containment walk -> stage/remove decision"
    - "validated staged command -> exact atomic file bytes inside hooks root"
---

<objective>
Refactor staging and symlink-containment coverage into its owner and remove one unreachable private guard.

Purpose: Prove real filesystem safety and atomic lifecycle behavior without filesystem injection or impossible test state (D-01, D-02, D-04, D-05, D-06, D-UTR-12).
Output: Refactored stage pair, deleted duplicate supplemental, and no new production symbol.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove real filesystem containment and remove unreachable guard</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts, tests/bridges/hooks/symlink-escape.test.ts, extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts, tests/bridges/hooks/async-rewake/pid-table.test.ts, .planning/PROJECT.md, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A1 (case-owned temporary root and exact filesystem effects). Before editing, query live CodeGraph for `assertNoSymlinkEscapeInHooksSubtree` and its callers. If the current source still initializes `stack` nonempty, loops only while length is positive, calls `stack.pop()` once per iteration, and pushes only directory paths, remove the private undefined-result guard as unreachable; if any premise changed, preserve it and record the contradictory proof in the summary. Never manufacture an impossible stack state. Build case-owned real temp trees for missing and non-directory hook subtrees, ordinary nested directories, an in-tree symlink, a direct escape, and a buried escape. Make normalization portable and deterministic: create a directory link as a Windows junction when `process.platform === "win32"` and as a directory symlink otherwise; independently resolve both the case-owned plugin root and link target with Node `realpath` and assert the normalized values carried by the typed containment error instead of hard-coding `/var` versus `/private/var`. No platform skip is allowed. Use lowercase phases and `finally` cleanup; do not add filesystem injection. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/stage.test.ts</automated></verify>
  <acceptance_criteria>Live proof controls the private guard disposition, real filesystem cases distinguish safe/missing/non-directory/escaping shapes, typed containment fields are exact, and all temp artifacts are removed on pass or failure.</acceptance_criteria>
  <done>The containment walk is proven through real directory shapes and contains no provably unreachable guard.</done>
</task>

<task type="auto">
  <name>Task 2: Complete stage/remove lifecycle and retire duplicate supplemental</name>
  <files>tests/bridges/hooks/stage.test.ts, tests/bridges/hooks/symlink-escape.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/stage.ts, tests/bridges/hooks/stage.test.ts, tests/bridges/hooks/symlink-escape.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A1 (case-owned temporary root and exact filesystem effects). Move every unique symlink assertion into the owner. CodeGraph proves the fallback follows a successful `realpath` plus containment failure and remains reachable under concurrent link replacement, so retain `readSymlinkTargetSafe` as a TOCTOU defense. Exercise it deterministically through the existing Node filesystem boundary: create a real escaping link so `realpath` succeeds and containment fails, use `mock.method(fs.promises, "readlink", ...)` to reject only `readlink`, call `syncBuiltinESMExports()` so the already-imported `node:fs/promises` binding sees the mock, and register restoration plus a second `syncBuiltinESMExports()` with `t.after` before acting. Assert exact `<unreadable>` in `SymlinkRefusedError`; add no production parameter/export/seam. Use the same boundary technique for an unexpected I/O propagation case only where no stable real shape exists. Independently cover safe-name acceptance/refusal, exact staged atomic bytes, confinement under the hooks root, idempotent restage, removal of an existing artifact, and missing-artifact removal. Assert whole bytes and paths from literal contract values, not production helpers. Delete `tests/bridges/hooks/symlink-escape.test.ts` as the sole D-06 carrier only after the owner retains every unique assertion. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/stage.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/stage.test.ts &amp;&amp; test ! -e tests/bridges/hooks/symlink-escape.test.ts</automated></verify>
  <acceptance_criteria>Containment and normalized error fields pass without a platform skip; the readlink rejection deterministically produces exact `<unreadable>` and restores the built-in binding; unexpected I/O, safe-name, exact bytes, idempotence, and existing/missing removal partitions pass; the supplemental file is absent; production change is limited to the CodeGraph-proven unreachable pop guard.</acceptance_criteria>
  <done>Stage owns every staging and symlink-safety contract, and the duplicate suite is retired exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured hook path -> filesystem | Staged artifacts and traversed links must remain inside the designated hooks root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-02 | Elevation of Privilege | stage path/symlink containment | high | mitigate | Real direct/buried escape refusal, normalization, safe-name, and exact confinement tests preserve containment and symlink refusal. |
</threat_model>

## Artifacts this phase produces

- Modified production file: `extensions/pi-claude-marketplace/bridges/hooks/stage.ts`, conditionally removing only the live-proven unreachable private guard.
- Modified owner test: `tests/bridges/hooks/stage.test.ts`.
- Deleted supplemental test: `tests/bridges/hooks/symlink-escape.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-28-PLAN.md`.

<verification>
Run the focused owner, direct coverage, and supplemental-absence command; query CodeGraph before the source edit and review the diff for guard-only production change.
</verification>

<success_criteria>
Stage has complete real-filesystem containment and lifecycle coverage, the duplicate suite is removed, and only live-proven dead private code changes.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-28-SUMMARY.md` when done.
</output>
