---
phase: 112-hook-runtime
plan: 23
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/stop.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: Stop cases use complete local inputs, whole six-key expectations, and lowercase phase comments."
    - "D-03/D-22: synthetic Stop input evidence remains at module scope while exact text and active-state values propagate through the complete payload."
    - "D-05: each case owns its context/session values; stop re-entry and observer behavior remains with plan 112-26."
  artifacts:
    - tests/bridges/hooks/payloads/stop.test.ts
  key_links:
    - "translateStop text/active/context -> exact six-key Stop payload"
---

<objective>
Refactor the Stop translator owner into an exact six-key wire contract.

Purpose: Prove text, active state, identity, context, and type evidence without duplicating settle-loop behavior (D-01, D-02, D-03, D-04, D-05, D-22).
Output: Refactored Stop owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the complete active Stop payload</name>
  <files>tests/bridges/hooks/payloads/stop.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts, tests/bridges/hooks/payloads/stop.test.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts, tests/bridges/hooks/payloads/stop-failure.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-03, D-04, D-05, and D-22, author a complete local active Stop input and an independent whole six-key payload expectation containing exact `Stop`, session/transcript/cwd, text, active flag, and context values according to the live signature. Assert context identity and no extra fields. Keep positive `satisfies` and targeted `@ts-expect-error` synthetic-input evidence at module scope; use lowercase phases only in the runtime case. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/stop.test.ts</automated></verify>
  <acceptance_criteria>The active case asserts the full six-key object and exact literals, module-scope type evidence compiles, and the focused test passes without modifying production.</acceptance_criteria>
  <done>The active Stop translation is proved through the complete wire payload.</done>
</task>

<task type="auto">
  <name>Task 2: Cover inactive and empty-text partitions</name>
  <files>tests/bridges/hooks/payloads/stop.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts, tests/bridges/hooks/payloads/stop.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add separately authored complete cases for inactive state and empty text, with distinct local contexts and whole six-key expected objects. Do not use a shared case generator. Do not pull stop aggregation, block/context re-entry, notification, or executor-send assertions into this pair; those remain with plan 112-26. Leave the retained translator supplemental unchanged. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/stop.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/stop.test.ts</automated></verify>
  <acceptance_criteria>Active/inactive and nonempty/empty text outcomes are explicit, context/session values are isolated, direct coverage passes, and settle responsibilities stay outside this owner.</acceptance_criteria>
  <done>The Stop translator's owner-specific runtime partitions are independently complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session lifecycle -> Stop payload | Session identity, active state, and text enter an outgoing hook payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Spoofing | Stop session/context | high | mitigate | Complete case-local payload assertions prove exact identity/context propagation and prevent cross-case session leakage. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/stop.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-23-PLAN.md`.

<verification>
Run the focused test and direct coverage; confirm owner-only changes and module-scope type evidence.
</verification>

<success_criteria>
Stop translation is exact across active/inactive and empty/nonempty text without settle-loop duplication or a new test seam.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-23-SUMMARY.md` when done.
</output>
