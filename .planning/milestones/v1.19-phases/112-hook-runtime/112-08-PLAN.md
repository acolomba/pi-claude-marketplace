---
phase: 112-hook-runtime
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/exec-result.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-08 owns the exact noop/block/mutate/stop union and allow/deny/ask permission vocabulary through module-scope compiler evidence (D-01, D-03)."
    - "Missing, extra, and invalid discriminants/permission fields fail only through targeted `@ts-expect-error` checks; no fake runtime cases or phase comments are added (D-03)."
    - "The only runtime behavior, assertNever, throws the stable error for an impossible value without widening production exports (D-03, D-05)."
  artifacts:
    - path: tests/bridges/hooks/exec-result.test.ts
      provides: "New canonical type/runtime owner of exec-result.ts"
  key_links:
    - from: tests/bridges/hooks/exec-result.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts
      via: "direct type imports plus assertNever runtime import"
      pattern: "HookExecResult|assertNever"
---

<objective>
Create the exact compiler/runtime owner for hook execution result unions.

Purpose: MOD-05 requires closed result and permission contracts without runtime type theater or new exports.
Output: One owner module with positive/negative compiler evidence and one assertNever case.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish every valid result and permission arm at module scope</name>
  <files>tests/bridges/hooks/exec-result.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts
    - tests/bridges/hooks/exec-result.test.ts
    - tests/bridges/agents/types.test.ts
    - tests/bridges/agents/index.test.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing owner and place positive `satisfies` evidence at module scope for complete noop, block, mutate, and stop values, including allow, deny, and ask permission decisions and each arm's optional/required fields. Follow A5; author literal values independently and add no runtime wrapper, phase comment, production export, or metadata table. Apply D-01 and D-03. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/exec-result.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports exec-result.ts; all four exact arms and three permission values satisfy the exported types at module scope; node test and typecheck pass; no runtime case exists solely for type evidence.</acceptance_criteria>
  <done>The positive closed-union contract is compiler-proved.</done>
</task>

<task type="auto">
  <name>Task 2: Reject malformed result shapes and prove assertNever</name>
  <files>tests/bridges/hooks/exec-result.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts
    - tests/bridges/hooks/exec-result.test.ts
    - tests/bridges/agents/types.test.ts
    - tests/bridges/agents/index.test.ts
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add adjacent targeted `@ts-expect-error` expressions for missing discriminant, invalid kind, missing required arm data, extra cross-arm fields, and invalid permission value. Add only the genuine runtime `assertNever` case, with lowercase arrange/act/assert and stable error type/content. Do not use `as any`, a double assertion, a runtime export-key oracle, or production change. Apply D-02, D-03, and D-05. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/exec-result.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/exec-result.test.ts</automated>
  </verify>
  <acceptance_criteria>Every targeted invalid shape is rejected by TypeScript, assertNever throws as specified, and direct coverage/type-only handling passes; no extra production surface, fake runtime type case, shared mutable state, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-08 owns the complete closed result contract.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

No listed Phase 112 runtime trust boundary is crossed by this type model; downstream wire/adapters own T-112-06.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-N/A-08 | Tampering | HookExecResult type surface | low | accept | Closed discriminants and permission vocabulary are compiler-proved; no I/O or authority boundary exists here. |

The prohibition precision pass yields zero bespoke values/safety/ethics items.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/exec-result.test.ts` as the sole owner.
- Creates no production symbol and leaves `exec-result.ts` unchanged.
- Creates `112-08-PLAN.md` and `112-08-SUMMARY.md` after execution.

<verification>
Focused node test, typecheck, and direct coverage/type-only recognition pass.
</verification>

<success_criteria>
- P112-08 owns exactly one pair.
- Positive and negative type contracts plus assertNever are complete.
- No production widening or fake type runtime exists.
- No applicable high threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-08-SUMMARY.md` when done.
</output>
