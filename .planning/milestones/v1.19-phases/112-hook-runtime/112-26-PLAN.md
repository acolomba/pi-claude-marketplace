---
phase: 112-hook-runtime
plan: 26
type: execute
wave: 8
depends_on: [112-05, 112-22, 112-23, 112-25]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
  - tests/bridges/hooks/settle.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-05: settle cases own executors, events, session/state inputs, and complete cleanup under lowercase phase comments."
    - "D-18: cache/one-shot, stop aggregation, epoch guards, rendering, re-entry, cap, notification, input reset, and send-failure behavior are observed through production outputs and lifecycle APIs."
    - "The test-only `settleCacheSnapshot` and `loopProtectionState` exports are removed after CodeGraph caller proof; no replacement reader/reset hook is added."
    - "The exact eighth re-entry is suppressed at cap 8, while prior entries are accepted; no mutation buckets or fixture oracles replace public evidence."
    - "Every matching observer runs in declared order; noop, block, mutate, and stop observer results are collected then discarded without decision, re-entry, message, or short-circuit effects, followed by full cleanup."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
    - tests/bridges/hooks/settle.test.ts
  key_links:
    - "executor observer events + routing epoch -> settle aggregation/cache -> next payload/sendMessage/notification"
---

<objective>
Refactor settle's stateful runtime contract and remove its two test-oriented state exports.

Purpose: Cover all live settle outcomes through public evidence and case-owned lifecycle instead of introspection (D-01, D-02, D-04, D-05, D-18).
Output: Refactored owner pair with narrower production exports; no new production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@.planning/phases/112-hook-runtime/112-05-SUMMARY.md
@.planning/phases/112-hook-runtime/112-22-SUMMARY.md
@.planning/phases/112-hook-runtime/112-23-SUMMARY.md
@.planning/phases/112-hook-runtime/112-25-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Replace settle state introspection with a public observer path</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, tests/bridges/hooks/settle.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, tests/bridges/hooks/settle.test.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts, tests/bridges/hooks/dispatch.test.ts, extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts, tests/bridges/hooks/routing-state.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A4 (case-owned state with cleanup registered before action). First confirm in the live CodeGraph that `settleCacheSnapshot` and `loopProtectionState` have no production callers. Remove those test-oriented exports from `extensions/pi-claude-marketplace/bridges/hooks/settle.ts` without changing live settle behavior, and do not add a replacement export, metadata table, reset hook, state reader, or test mode. Rewrite one tracer case so case-owned executor observer events enter settle and the assertion uses `sendMessage`, emitted executor behavior, and/or the next public payload to prove cache/one-shot handling. Cover cache miss, cache hit/one-shot consumption, and stale epoch rejection using complete local executors, state, and cleanup. Use lowercase runtime phases. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/settle.test.ts</automated></verify>
  <acceptance_criteria>Both test-oriented exports are absent after CodeGraph confirmation, cache/one-shot and epoch outcomes are visible at public boundaries, focused tests pass, and no replacement introspection seam exists.</acceptance_criteria>
  <done>Settle's first stateful path is observable without private-state exports.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaust aggregation, reasons, rendering, re-entry, and failure cleanup</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, tests/bridges/hooks/settle.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, tests/bridges/hooks/settle.test.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts, tests/bridges/hooks/payloads/stop.test.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts, tests/bridges/hooks/payloads/stop-failure.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A4 (case-owned state with cleanup registered before action). Using independently authored cases, cover the complete stop-reason partitions `stop`, `error`, `length`, `pending`, `aborted`, `toolUse`, and deferred behavior present in the live union; Stop aggregation precedence; delivery to every matching StopFailure observer in declared order; render output; block/context re-entry; the exact boundary in which entries before cap 8 proceed and the eighth is suppressed; `claude-hook-stop-block` type value; cap notification; input reset; and executor/send failure cleanup. Feed matching observers distinct noop, block, mutate, and stop results and prove all observers run without short-circuit, then prove those outcomes are discarded: no decision, re-entry, message, or mutation effect is emitted. Assert full listener/cache/input/session cleanup after both success and failure via sent messages, observer events, later public calls, and case-owned spies only. Do not inspect internal caches/counters, add mutation buckets, or alter supplemental suites. Use lowercase phases, allowing lowercase `// act &amp; assert` only for a single `assert.rejects()`. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/settle.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/settle.test.ts</automated></verify>
  <acceptance_criteria>Every named reason, aggregation branch, matching observer and declared-order position, discarded noop/block/mutate/stop outcome, rendering, stale epoch, re-entry boundary, cap notification, reset, and send-failure cleanup has an exact public assertion; discarded outcomes cause no decision/re-entry/message/short-circuit effect; state is isolated per case; direct coverage passes and source change is limited to deleting the two dead exports.</acceptance_criteria>
  <done>The entire settle runtime contract is explicit and private state remains private.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor/session events -> settle state | Asynchronous events and cached results must remain scoped to the current session epoch. |
| settle result -> outgoing payload | Only approved aggregate/re-entry effects may reach dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Spoofing | settle epoch/cache/re-entry state | high | mitigate | Public stale-epoch, one-shot, cap, reset, and failure-cleanup assertions prove session isolation without exposing state. |
| T-112-06 | Tampering | settle aggregation/output | high | mitigate | Exact reason precedence, observer delivery, rendering, and re-entry whitelist assertions constrain outgoing effects. |
</threat_model>

## Artifacts this phase produces

- Modified production file: `extensions/pi-claude-marketplace/bridges/hooks/settle.ts`, removing `settleCacheSnapshot` and `loopProtectionState` only.
- Modified owner test: `tests/bridges/hooks/settle.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-26-PLAN.md`.

<verification>
Run the focused node test and direct coverage; use CodeGraph to confirm the removed names have no callers/exports and inspect the source diff to ensure no production behavior or replacement seam changed.
</verification>

<success_criteria>
Settle's full public behavior is covered with exact cap/epoch boundaries and case-owned cleanup, while two dead test seams are removed without replacement.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-26-SUMMARY.md` when done.
</output>
