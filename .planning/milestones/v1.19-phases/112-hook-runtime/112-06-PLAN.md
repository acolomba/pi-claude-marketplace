---
phase: 112-hook-runtime
plan: 06
type: execute
wave: 5
depends_on: [112-08, 112-25]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts
  - tests/bridges/hooks/event-adapters.test.ts
  - tests/architecture/hooks-adapters.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-06 proves every noop/block/mutate/stop adapter arm, object guard, optional-key absence, SessionStart capture, observation drop, and diagnostic (D-01, D-04, D-18)."
    - "Tool-result patches whitelist only array content and boolean isError; routing discriminators and tool identity cannot be overwritten (D-22, T-112-06)."
    - "CodeGraph-confirmed legacy `adaptObservationResult` has no production dispatch caller and is removed rather than becoming a protected test seam (D-03, D-05)."
    - "All adapter-only supplemental cases move into this owner before hooks-adapters.test.ts is deleted by its sole carrier (D-06)."
  artifacts:
    - path: tests/bridges/hooks/event-adapters.test.ts
      provides: "New canonical direct owner of event-adapters.ts"
    - path: tests/architecture/hooks-adapters.test.ts
      provides: "Deleted duplicate adapter suite"
  key_links:
    - from: tests/bridges/hooks/event-adapters.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts
      via: "direct adapter export calls over complete event/results"
      pattern: "adapt.*Result|capture"
---

<objective>
Create the event-adapter owner, close the mutation whitelist, and remove the legacy test-oriented export and duplicate suite.

Purpose: MOD-05 requires hook decisions to adapt without overwriting routing identity or leaking pending SessionStart state.
Output: A complete direct owner, narrower production surface, and one removed supplemental authority.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one whitelisted tool-result mutation</name>
  <files>tests/bridges/hooks/event-adapters.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts
    - tests/bridges/hooks/event-adapters.test.ts
    - tests/architecture/hooks-adapters.test.ts
    - tests/bridges/agents/convert.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing direct owner with a complete tool-result event and mutate outcome containing allowed and hostile fields. Prove only array `content` and boolean `isError` are applied, while event discriminator, tool name, call identity, and unrelated routing fields remain exact. Assert the complete adapted object, source identity/immutability where promised, and cleanup. Use independent literals and lowercase phases per D-01, D-02, D-04, D-05, D-18, and D-22. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/event-adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports event-adapters.ts; one complete mutation path proves allowed fields, rejected hostile fields, preserved routing/tool identity, optional absence, and no source mutation; the node test passes.</acceptance_criteria>
  <done>The highest-risk adapter mutation path is directly owned and green.</done>
</task>

<task type="auto">
  <name>Task 2: Complete result arms, capture lifecycle, diagnostics, and supplemental removal</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts, tests/bridges/hooks/event-adapters.test.ts, tests/architecture/hooks-adapters.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts
    - tests/bridges/hooks/event-adapters.test.ts
    - tests/architecture/hooks-adapters.test.ts
    - tests/bridges/hooks/session-start-additional-context.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/exec-result.ts
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add independent complete cases for non-object drops, tool-call object-only patches, every noop/block/mutate/stop arm for all adapter exports, wrong-type omissions, true optional-key absence, SessionStart additional-context capture with marketplace/plugin provenance and ordered pending state, non-SessionStart observation drops, and semantic diagnostics. Remove `adaptObservationResult` after the existing CodeGraph proof that no production dispatcher calls it; do not replace or widen it. Move all `hooks-adapters.test.ts` behavior into this owner and delete that file as the sole D-06 carrier. Absorb only the adapter portion of session-start-additional-context without editing that shared file; 112-07 removes it after 112-25. Use public routing lifecycle cleanup per D-05 and D-18. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>test ! -e tests/architecture/hooks-adapters.test.ts &amp;&amp; node --test tests/bridges/hooks/event-adapters.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/event-adapters.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; all guards, result arms, whitelist, optional absence, capture/provenance, drop, and diagnostic cases are complete and case-local; `adaptObservationResult` and hooks-adapters.test.ts are absent with production callers/typecheck unaffected; no new export, reader, reset, mutation bypass, shared oracle, or coverage exception exists.</acceptance_criteria>
  <done>P112-06 is complete, production behavior is preserved with a narrower internal-only surface, and duplicate authority is removed.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| untrusted hook output → Pi event/result | Hook-controlled JSON attempts to mutate host routing values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-06 | Tampering | tool call/result adapters | high | mitigate | Enforce object guards, exact content/isError whitelist, wrong-type omission, discriminator/tool preservation, and complete output assertions. |

No bespoke prohibition survives; output-overwrite risk is canon and hard-gated here.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/event-adapters.test.ts` as the sole mirrored owner.
- Removes legacy production export `adaptObservationResult` and creates no replacement symbol.
- Deletes `tests/architecture/hooks-adapters.test.ts` after absorption.
- Creates `112-06-PLAN.md` and `112-06-SUMMARY.md` after execution.

<verification>
The owner and direct coverage pass at 100 percent; typecheck passes; the duplicate suite and unused export are absent.
</verification>

<success_criteria>
- P112-06 owns exactly one pair.
- Adapter guards, outcomes, mutation whitelist, capture lifecycle, and diagnostics are complete.
- Supplemental cleanup and production narrowing happen once in this owner.
- No high-severity overwrite threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-06-SUMMARY.md` when done.
</output>
