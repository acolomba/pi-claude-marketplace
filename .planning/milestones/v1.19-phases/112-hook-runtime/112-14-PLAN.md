---
phase: 112-hook-runtime
plan: 14
type: execute
wave: 10
depends_on: [112-07, 112-28]
files_modified:
  - tests/bridges/hooks/index.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-14 proves binding identity for exactly seven intended runtime exports after router/stage settle (D-01, D-03)."
    - "Targeted module-scope compiler negatives prove internal bindings remain absent from the barrel; no production export is added (D-03, D-05)."
    - "Runtime binding cases use lowercase phases and independently import defining bindings rather than invoking behavior through the barrel (D-02, D-04)."
  artifacts:
    - path: tests/bridges/hooks/index.test.ts
      provides: "New canonical direct barrel owner"
  key_links:
    - from: tests/bridges/hooks/index.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/index.ts
      via: "direct barrel imports compared to defining-module bindings"
      pattern: "strictEqual"
---

<objective>
Create the hook barrel owner with exact runtime binding identity and closed compile-time surface evidence.

Purpose: MOD-05 applies the same ownership rule to barrels without re-testing their implementations or widening exports.
Output: One exact barrel owner; no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the seven runtime re-exports are defining bindings</name>
  <files>tests/bridges/hooks/index.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/index.ts
    - tests/bridges/hooks/index.test.ts
    - tests/bridges/agents/index.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
    - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing barrel owner and compare exact binding identity for `hydrateProjectScopeForCwd`, `readAndCachePluginHooks`, `registerHooksBridge`, `rebuildRoutingTables`, `removePluginConfigFromCache`, `writeHookConfig`, and `removeHookConfig` against direct defining-module imports. Use one top-level entrypoint group per binding, lowercase phases, and `assert.strictEqual`; do not enumerate object keys or call implementations. Apply D-01-D-05. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/index.test.ts</automated>
  </verify>
  <acceptance_criteria>All seven named runtime exports are direct binding identities, each case uses canonical phases, and the node test passes without behavior proxies or production changes.</acceptance_criteria>
  <done>The complete intended runtime barrel surface is directly proved.</done>
</task>

<task type="auto">
  <name>Task 2: Prove internal binding absence without widening the barrel</name>
  <files>tests/bridges/hooks/index.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/index.ts
    - tests/bridges/hooks/index.test.ts
    - tests/bridges/agents/index.test.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add targeted module-scope `@ts-expect-error` evidence that internal translator tables, state cells/readers, predicate internals, timer/stream internals, and private stage walker bindings are absent from the barrel namespace. Do not claim or invent public type re-exports, enumerate runtime object keys, use truthiness proxies, or add an export. Run the pair only after 112-07 and 112-28 establish final public sources. Apply D-03 and D-05. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/index.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/index.test.ts</automated>
  </verify>
  <acceptance_criteria>Each named internal binding fails compile at module scope, the seven runtime identity cases and direct barrel coverage pass, and no public type claim, runtime wrapper, new export, production delta, supplemental edit, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-14 owns the exact closed hook barrel contract.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

No listed runtime trust boundary is crossed by binding/type identity; underlying router/stage plans own their threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-N/A-14 | Information disclosure | hook barrel surface | low | accept | Targeted compiler negatives keep internal state/process/path symbols private; no I/O occurs here. |

The prohibition precision pass yields zero bespoke items.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/index.test.ts` as the sole mirrored barrel owner.
- Creates no production symbol and leaves `index.ts` unchanged.
- Creates `112-14-PLAN.md` and `112-14-SUMMARY.md` after execution.

<verification>
Focused node test, typecheck, and direct barrel coverage/type recognition pass.
</verification>

<success_criteria>
- P112-14 owns exactly one pair.
- Exactly seven runtime binding identities are proved.
- Internal symbols remain unavailable without production widening.
- No applicable high threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-14-SUMMARY.md` when done.
</output>
