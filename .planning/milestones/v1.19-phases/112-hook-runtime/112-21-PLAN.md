---
phase: 112-hook-runtime
plan: 21
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/session-start.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: SessionStart cases own complete inputs and whole five-key expectations under lowercase phase comments."
    - "D-22: event, source, identity fields, and context cross the translator exactly without extra keys."
    - "D-05: context/session values are case-local; additional-context dispatch behavior remains owned by plans 112-06 and 112-07."
  artifacts:
    - tests/bridges/hooks/payloads/session-start.test.ts
  key_links:
    - "translateSessionStart input source/context -> exact SessionStart payload"
---

<objective>
Refactor the SessionStart translator owner into a precise five-key wire contract.

Purpose: Prove source and context propagation without duplicating router/adapter responsibilities (D-01, D-02, D-04, D-05, D-22).
Output: Refactored owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one complete SessionStart payload</name>
  <files>tests/bridges/hooks/payloads/session-start.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/session-start.ts, tests/bridges/hooks/payloads/session-start.test.ts, tests/bridges/hooks/payloads/session-end.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Using D-01, D-02, D-04, D-05, and D-22, author a complete case-local input with distinct session/transcript/cwd/source/context values. Assert an independently written five-key payload including exact `SessionStart`, the live source literal, and the original context reference. Use strict deep equality and the locked lowercase runtime comment sequence. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/session-start.test.ts</automated></verify>
  <acceptance_criteria>All five fields, exact event/source literals, and context identity are asserted; no partial matcher or shared fixture supplies the expected payload.</acceptance_criteria>
  <done>The primary SessionStart translation is proved through its complete envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Cover source and accepted empty-value branches</name>
  <files>tests/bridges/hooks/payloads/session-start.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/session-start.ts, tests/bridges/hooks/payloads/session-start.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add independent whole-payload cases for every source branch present in the declared input and accepted empty session/transcript/cwd values. Keep contexts case-owned and do not introduce unsupported null cases. Do not move additional-context adaptation or hydration assertions into this owner; those belong to plans 112-06/112-07. Leave `tests/architecture/hooks-translators.test.ts` unchanged. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/session-start.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/session-start.test.ts</automated></verify>
  <acceptance_criteria>Every live source branch and applicable empty string has an explicit complete expected object, direct coverage passes, case state is isolated, and supplemental/router behavior is not duplicated.</acceptance_criteria>
  <done>The SessionStart translator contract is complete within its owner boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session lifecycle -> translator | Session identity, source, and context enter an outgoing hook payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Spoofing | SessionStart identity/context | high | mitigate | Exact, case-local whole-payload assertions prevent identity substitution and cross-session context leakage. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/session-start.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-21-PLAN.md`.

<verification>
Run the focused owner test and direct coverage; verify the diff contains only this pair's owner test.
</verification>

<success_criteria>
SessionStart translation has exact source, context, identity, and empty-value coverage without duplicating adapter/router concerns.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-21-SUMMARY.md` when done.
</output>
