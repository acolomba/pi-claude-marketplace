---
phase: 112-hook-runtime
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/post-tool-use.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: every PostToolUse case contains full local inputs and an independently authored seven-key expectation under lowercase phase comments."
    - "D-22: built-in and custom tool names map exactly while tool input, response, and context retain identity."
    - "The translator adds no unspecified fields and does not mutate nested values."
  artifacts:
    - tests/bridges/hooks/payloads/post-tool-use.test.ts
  key_links:
    - "translatePostToolUse input -> exact PostToolUse payload"
---

<objective>
Refactor the successful post-tool-use translator owner around its exact wire contract.

Purpose: Prove every field, mapping branch, and nested reference without a shared oracle (D-01, D-02, D-04, D-22).
Output: Refactored owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Lock the built-in PostToolUse payload</name>
  <files>tests/bridges/hooks/payloads/post-tool-use.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use.ts, tests/bridges/hooks/payloads/post-tool-use.test.ts, tests/bridges/hooks/payloads/post-tool-use-failure.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, and D-22, create a built-in-tool case with complete local session, transcript, cwd, tool-name, tool-input, tool-response, and context values. Assert a literal whole seven-key payload including exact `PostToolUse` event spelling and the production-defined built-in name mapping. Preserve nested object identity. Use only lowercase `// arrange`, blank line, `// act`, blank line, `// assert` runtime phases. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/post-tool-use.test.ts</automated></verify>
  <acceptance_criteria>The focused owner passes with a complete seven-key expectation, exact literals, identity assertions, no partial-object matcher, and no production modification.</acceptance_criteria>
  <done>The primary successful tool translation is proved from input through the entire output envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Cover custom mapping and non-mutation</name>
  <files>tests/bridges/hooks/payloads/post-tool-use.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/post-tool-use.ts, tests/bridges/hooks/payloads/post-tool-use.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Author a separate custom-tool case with different literal values and independently repeat the complete expected payload. Assert exact custom-name mapping, reference identity for tool input/response/context, and equality of the original nested inputs after translation. Do not generate expectations with the translator or production constants. Keep `tests/architecture/hooks-translators.test.ts` unchanged because RESEARCH assigns no supplemental-carrier action here. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/post-tool-use.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/post-tool-use.test.ts</automated></verify>
  <acceptance_criteria>Both mapping partitions are explicit, nested inputs stay unchanged, every runtime case uses lowercase phases, owner coverage passes, and no supplemental file changes.</acceptance_criteria>
  <done>The PostToolUse owner exhausts its successful translation branches independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator input -> hook payload | Tool-controlled names and nested values enter the hook wire envelope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-06 | Tampering | PostToolUse translator | high | mitigate | Whole-payload, exact name-mapping, identity, and non-mutation assertions prevent unapproved payload mutation. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/post-tool-use.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-17-PLAN.md`.

<verification>
Run `node --test tests/bridges/hooks/payloads/post-tool-use.test.ts` and its direct coverage command; inspect the plan diff for owner-only scope.
</verification>

<success_criteria>
Successful post-tool translation has exact built-in/custom whole-payload coverage without mutation, fixture oracles, or new seams.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-17-SUMMARY.md` when done.
</output>
