---
phase: 112-hook-runtime
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/pre-tool-use.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: every PreToolUse case owns a complete input and literal six-key expectation under lowercase phase comments."
    - "D-22: built-in and custom tool names map exactly while tool input and context retain identity."
    - "No partial matcher, production constant, or generic payload factory acts as the expectation oracle."
  artifacts:
    - tests/bridges/hooks/payloads/pre-tool-use.test.ts
  key_links:
    - "translatePreToolUse input -> exact six-key PreToolUse payload"
---

<objective>
Refactor the pre-tool-use translator owner around its complete public payload.

Purpose: Lock every envelope field and tool-name branch using independent expectations (D-01, D-02, D-04, D-22).
Output: Refactored owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove the complete built-in PreToolUse payload</name>
  <files>tests/bridges/hooks/payloads/pre-tool-use.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-tool-use.ts, tests/bridges/hooks/payloads/pre-tool-use.test.ts, tests/bridges/hooks/payloads/post-tool-use.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, and D-22, build one complete case-local input and assert the exact six-key PreToolUse object: literal session/transcript/cwd/event/tool-name values plus the original tool-input and context references as defined by the live signature. Select a built-in tool name that traverses the production mapping branch. Author the expected object from the contract, not from a helper or source constant, and use the locked lowercase arrange/act/assert comment layout. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/pre-tool-use.test.ts</automated></verify>
  <acceptance_criteria>The built-in case asserts the exact six-key envelope, exact event/name literals, nested identity, and no extra keys; the owner passes without source changes.</acceptance_criteria>
  <done>The built-in PreToolUse translation is proved end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Cover custom tool mapping and input non-mutation</name>
  <files>tests/bridges/hooks/payloads/pre-tool-use.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-tool-use.ts, tests/bridges/hooks/payloads/pre-tool-use.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add a separately authored custom-tool case with unique input/context objects and another complete six-key expectation. Assert exact custom-name mapping, reference identity, and that the nested tool input is unchanged after translation. Do not edit the retained `tests/architecture/hooks-translators.test.ts`; this pair is not its carrier. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/pre-tool-use.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/pre-tool-use.test.ts</automated></verify>
  <acceptance_criteria>Built-in and custom partitions are independently explicit, tool input remains unmutated, direct coverage passes, and runtime comments are lowercase.</acceptance_criteria>
  <done>The PreToolUse owner covers every owner-specific mapping and identity contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator input -> hook payload | Tool-controlled name and input enter the pre-tool hook envelope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-06 | Tampering | PreToolUse translator | high | mitigate | Exact whole-envelope mapping, identity, and non-mutation assertions preserve allowed fields and reject silent mutation drift. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/pre-tool-use.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-19-PLAN.md`.

<verification>
Run the focused owner test and direct coverage; verify the diff contains only the owner test.
</verification>

<success_criteria>
The six-key PreToolUse envelope is covered for built-in and custom tools with no shared oracle, mutation, or new seam.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-19-SUMMARY.md` when done.
</output>
