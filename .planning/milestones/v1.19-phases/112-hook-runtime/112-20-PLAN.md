---
phase: 112-hook-runtime
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/session-end.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: SessionEnd cases have complete local inputs, whole five-key expectations, and lowercase phase comments."
    - "D-22: the exact SessionEnd event, reason, and context values cross the translator without extra fields."
    - "D-05: each case owns its context/session values and leaves no shared mutable state."
  artifacts:
    - tests/bridges/hooks/payloads/session-end.test.ts
  key_links:
    - "translateSessionEnd input reason/context -> exact SessionEnd payload"
---

<objective>
Make the SessionEnd translator's full payload and reason propagation explicit.

Purpose: Replace fixture-shaped assertions with independent, case-owned wire contracts (D-01, D-02, D-04, D-05, D-22).
Output: Refactored SessionEnd owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove a complete SessionEnd translation</name>
  <files>tests/bridges/hooks/payloads/session-end.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts, tests/bridges/hooks/payloads/session-end.test.ts, tests/bridges/hooks/payloads/session-start.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, D-05, and D-22, construct a complete local SessionEnd input with distinctive session/transcript/cwd/reason/context values and compare translation to an independently written five-key payload with exact `SessionEnd` event spelling and the production-defined reason field. Assert context identity. Apply the exact lowercase arrange/act/assert phase-comment format. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/session-end.test.ts</automated></verify>
  <acceptance_criteria>The complete key set, exact event and reason, and context identity are asserted; the focused owner passes without source modification.</acceptance_criteria>
  <done>One real SessionEnd input is proven through the full translator envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the declared reason and empty-value partitions</name>
  <files>tests/bridges/hooks/payloads/session-end.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts, tests/bridges/hooks/payloads/session-end.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add separately authored cases for each reason branch required by the live input type and for accepted empty session/transcript/cwd values. Repeat a whole five-key expectation in each case; do not loop over a data table or derive values from source constants. Keep case-owned context values and leave the cross-translator supplemental suite unchanged. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/session-end.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/session-end.test.ts</automated></verify>
  <acceptance_criteria>Every declared owner-specific reason is literal and explicit, accepted empty strings are preserved, cases share no mutable state, direct coverage passes, and no supplemental file changes.</acceptance_criteria>
  <done>The SessionEnd contract covers all live reason and applicable empty-value branches independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session lifecycle -> translator | Session identity and termination reason cross a lifecycle boundary into the outgoing payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Spoofing | SessionEnd identity/context | high | mitigate | Complete case-owned envelopes assert exact session, transcript, cwd, reason, and context propagation without state leakage. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/session-end.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-20-PLAN.md`.

<verification>
Run the focused test and direct coverage command; confirm owner-only file scope.
</verification>

<success_criteria>
SessionEnd translation is independently covered for exact identity, context, event, and every declared reason.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-20-SUMMARY.md` when done.
</output>
