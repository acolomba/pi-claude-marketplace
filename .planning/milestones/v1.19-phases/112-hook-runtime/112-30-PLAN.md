---
phase: 112-hook-runtime
plan: 30
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/translation-context.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-05: translation-context cases own session/environment inputs, complete expected contexts, and cleanup under lowercase runtime phases."
    - "D-03: readonly shape evidence uses positive `satisfies` and targeted `@ts-expect-error` checks at module scope."
    - "Exact session ID, transcript path, cwd, and empty-session-file fallback behavior are observed through the public context result."
    - "The barrel does not acquire a new context export and no state reader, metadata table, or test mode is added."
  artifacts:
    - tests/bridges/hooks/translation-context.test.ts
  key_links:
    - "session file/environment/cwd -> public translation context fields and fallback"
---

<objective>
Refactor translation-context tests around its complete public value and readonly type boundary.

Purpose: Prove identity-path derivation, empty fallback, access behavior, and type evidence without widening exports (D-01, D-02, D-03, D-04, D-05).
Output: Refactored owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove a complete translation context from case-owned session inputs</name>
  <files>tests/bridges/hooks/translation-context.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts, tests/bridges/hooks/translation-context.test.ts, extensions/pi-claude-marketplace/bridges/hooks/index.ts, tests/bridges/agents/index.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A5 (module-scope type evidence and binding identity). Per D-01, D-02, D-04, and D-05, construct a complete case-owned session/environment input using the existing production boundary, invoke the public context creator, and assert the entire context value with literal session ID, transcript path, cwd, and every other field in the live result. Independently author the expected object; do not spread the result or import expected constants. If the contract specifies getter access count/order, assert the exact documented sequence with a case-owned observer; otherwise do not fabricate one. Restore all environment/session state in `finally`. Use lowercase runtime phase comments. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/translation-context.test.ts</automated></verify>
  <acceptance_criteria>The normal result has an exact complete expectation, session/transcript/cwd values are literal, any access-order assertion is contract-backed, cleanup always runs, and focused tests pass without production edits.</acceptance_criteria>
  <done>A complete translation context is proved through its public production boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Cover empty fallback and module-scope readonly evidence</name>
  <files>tests/bridges/hooks/translation-context.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts, tests/bridges/hooks/translation-context.test.ts, extensions/pi-claude-marketplace/bridges/hooks/index.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A5 (module-scope type evidence and binding identity). Add an independent case for an empty session file/value and assert the exact production fallback with a whole expected context. Per D-03, place positive `satisfies` evidence for the complete readonly context shape and targeted `@ts-expect-error` checks for invalid mutation/field types at module scope; keep type-only lines outside runtime phases. Confirm the barrel remains unchanged and does not export a new test/context seam. Do not add null behavior beyond the declared input. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/translation-context.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/translation-context.test.ts</automated></verify>
  <acceptance_criteria>Empty-session fallback is exact, readonly positive and negative checks compile at module scope, all state is restored per case, direct coverage passes, and `extensions/pi-claude-marketplace/bridges/hooks/index.ts` has no new export.</acceptance_criteria>
  <done>The runtime and type-level translation-context contracts are complete without widening production API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session/environment -> translation context | Session identity and filesystem location must not leak or cross-contaminate between invocations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-05 | Spoofing | translation context identity | high | mitigate | Exact case-owned identity/path expectations plus unconditional environment/session cleanup preserve isolation. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/translation-context.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-30-PLAN.md`.

<verification>
Run the focused owner and direct coverage; inspect the barrel diff as empty and confirm type evidence stays at module scope.
</verification>

<success_criteria>
Translation context has complete normal/fallback runtime coverage, readonly compile-time evidence, and case-owned isolation without an export change.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-30-SUMMARY.md` when done.
</output>
