---
phase: 112-hook-runtime
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/post-compact.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: post-compact cases use complete local inputs and independently authored whole-payload expectations under lowercase arrange, act, and assert comments."
    - "D-22: the five-key PostCompact envelope preserves the supplied context object by identity and emits the exact event and trigger literals without extra keys."
    - "Empty string values remain valid payload values; the test does not substitute a fabricated null-input contract."
  artifacts:
    - tests/bridges/hooks/payloads/post-compact.test.ts
  key_links:
    - "translatePostCompact input -> exact five-key wire payload"
---

<objective>
Refactor the post-compact translator owner into explicit, case-local contract tests.

Purpose: Prove its complete wire envelope without deriving expected values from production constants or shared fixtures (D-01, D-02, D-04, D-22).
Output: One independently runnable owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one complete PostCompact payload</name>
  <files>tests/bridges/hooks/payloads/post-compact.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/post-compact.ts, tests/bridges/hooks/payloads/post-compact.test.ts, tests/bridges/hooks/payloads/pre-compact.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, and D-22, rewrite the happy-path case with a complete local translator input and a literal whole-object expectation for the five-key envelope, including event name `PostCompact`, trigger `auto`, session/transcript/cwd values, and the exact context reference required by the live signature. Use lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; do not import expected constants or spread production output into the expectation. Modify only the owner test; leave `extensions/pi-claude-marketplace/bridges/hooks/payloads/post-compact.ts` and `tests/architecture/hooks-translators.test.ts` unchanged. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/post-compact.test.ts</automated></verify>
  <acceptance_criteria>The owner asserts all five keys and their exact literals, context identity is explicit, no extra-key tolerance is used, the focused test passes, and neither production nor supplemental translator files change.</acceptance_criteria>
  <done>A complete PostCompact translation is proved from input to exact wire payload.</done>
</task>

<task type="auto">
  <name>Task 2: Partition empty values and payload exactness</name>
  <files>tests/bridges/hooks/payloads/post-compact.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/post-compact.ts, tests/bridges/hooks/payloads/post-compact.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add a separately authored case with empty session/transcript/cwd strings if accepted by the declared input, retaining those exact strings and the case-owned context object. Assert the complete five-key object again rather than a partial match. Keep one behavior per case, runtime comments lowercase, and do not add null behavior outside the production type. Record that the retained cross-translator supplemental suite is not edited because RESEARCH assigns no carrier action to this pair. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/post-compact.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/post-compact.test.ts</automated></verify>
  <acceptance_criteria>Accepted empty strings have explicit outcomes, every case owns all inputs and its whole expectation, direct owner coverage passes without an exception, and the supplemental suite remains unchanged.</acceptance_criteria>
  <done>The owner covers its complete envelope and applicable empty-value boundary independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No listed Phase 112 security boundary applies: this pure translator copies already-validated compact metadata and does not execute commands, traverse paths, mutate registries, manage timers, or cross process/session state.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-NA-15 | Tampering | `translatePostCompact` payload shape | low | accept | Exact whole-object tests prevent accidental field addition, removal, or literal drift; no security-sensitive boundary is introduced. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/post-compact.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-15-PLAN.md`.

<verification>
Run `node --test tests/bridges/hooks/payloads/post-compact.test.ts` and `npm run test:coverage:direct -- tests/bridges/hooks/payloads/post-compact.test.ts`; confirm `git diff --name-only` for this plan names only the owner test.
</verification>

<success_criteria>
The pair has complete, case-local PostCompact wire-contract coverage with lowercase phases and no fixture oracle, production change, or supplemental edit.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-15-SUMMARY.md` when done.
</output>
