---
phase: 112-hook-runtime
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/hook-env.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-10 proves exact environment precedence from process env through hook paths, marker, and session values (D-01, D-20)."
    - "CLAUDE_ENV_FILE exists only for SessionStart, CLAUDE_CODE_REMOTE is genuinely absent, contained plugin data is exact, and inherited unknown keys survive."
    - "Every mutated process property records prior existence and value and restores it in case-local cleanup registered before action (D-05, D-20)."
    - "Complete env objects and optional-key absence are asserted independently; no shared process setup or new reset hook exists (D-04, D-05)."
  artifacts:
    - path: tests/bridges/hooks/hook-env.test.ts
      provides: "New canonical direct hook environment owner"
  key_links:
    - from: tests/bridges/hooks/hook-env.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/hook-env.ts
      via: "direct prepareHookEnv calls over complete RoutingEntry/context/locations"
      pattern: "prepareHookEnv"
---

<objective>
Create the hook environment owner with exact precedence, absence, containment, and process restoration proof.

Purpose: MOD-05 requires blocking and async children to receive the same isolated session/plugin environment.
Output: One complete direct owner; no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/env-vars.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove exact SessionStart environment precedence</name>
  <files>tests/bridges/hooks/hook-env.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/hook-env.ts
    - tests/bridges/hooks/hook-env.test.ts
    - tests/shared/completion-cache.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts
    - docs/env-vars.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing owner with a complete RoutingEntry, TranslationContext, and ScopedLocations. Capture exact existence/value for every process env key to mutate, register restoration first, then arrange conflicts at each layer and prove precedence: process env, CLAUDE_PROJECT_DIR/CLAUDE_PLUGIN_ROOT/CLAUDE_PLUGIN_DATA, extra marker, and session env last. For SessionStart assert exact CLAUDE_ENV_FILE plus contained plugin-data path and complete env object. Apply D-01, D-02, D-04, D-05, D-20, and D-22 optional-absence discipline. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/hook-env.test.ts</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports hook-env.ts; a complete SessionStart case proves every precedence layer, exact paths/values, and exact process restoration; the focused node test passes.</acceptance_criteria>
  <done>The highest-precedence complete environment path is direct and green.</done>
</task>

<task type="auto">
  <name>Task 2: Complete event absence, inherited-key, and restoration partitions</name>
  <files>tests/bridges/hooks/hook-env.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/hook-env.ts
    - tests/bridges/hooks/hook-env.test.ts
    - tests/architecture/hooks-async-rewake.test.ts
    - tests/architecture/hooks-exec.test.ts
    - docs/env-vars.md
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add independent cases for non-SessionStart true CLAUDE_ENV_FILE absence, exact CLAUDE_CODE_REMOTE absence even when inherited input contains it, inherited unknown-key preservation, process property initially absent versus present-with-value, session env collision wins, marker collision wins before session spread, user/project contained paths, and complete restoration after success/failure. Serialize only cases that mutate the same process keys; avoid beforeEach and shared env objects. Absorb env-only expectations from legacy execution/async suites into this owner without editing their carrier files; leave one named cross-lane parity case for 112-02. Use lowercase phases per D-02 and apply D-04, D-05, and D-20. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/hook-env.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/hook-env.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; precedence, contained paths, event-specific file, remote-key absence, inherited keys, and exact restoration partitions are complete; no process state leaks, shared setup, supplemental edit, production delta, reset hook, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-10 is complete and hermetic for child environment construction.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| process/session/plugin values → child environment | Global and scoped values cross into hook child processes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-05 | Information disclosure / Tampering | prepareHookEnv | high | mitigate | Prove exact precedence, contained paths, optional-key absence, inherited-key handling, and exact per-case process restoration. |

No bespoke prohibition survives; environment leakage is canon and hard-gated here.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/hook-env.test.ts` as the sole mirrored owner.
- Creates no production symbol and leaves `hook-env.ts` unchanged.
- Creates `112-10-PLAN.md` and `112-10-SUMMARY.md` after execution.

<verification>
Focused node test, typecheck, and direct coverage pass at 100 percent with exact process restoration.
</verification>

<success_criteria>
- P112-10 owns exactly one pair.
- Environment precedence, path, absence, inheritance, and cleanup behavior is complete.
- No process-wide state leaks across cases.
- No high-severity environment threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-10-SUMMARY.md` when done.
</output>
