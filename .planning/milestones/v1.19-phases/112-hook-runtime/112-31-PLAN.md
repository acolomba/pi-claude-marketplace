---
phase: 112-hook-runtime
plan: 31
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/wire-protocol.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04/D-05: wire cases own complete exit/stdout inputs and whole outcomes under lowercase phase comments."
    - "D-13/D-24: exit status and JSON shape partitions resolve deterministically; semantic diagnostics never throw."
    - "Decision precedence is exact: top-level stop before block, top-level decisions before nested decisions, nested deny before mutation, and mutation before top-level suppressOutput."
    - "Every allowed mutation field and conflict branch, allow/ask reason typing, wrong-type omission, and final no-op outcome is explicit."
  artifacts:
    - tests/bridges/hooks/wire-protocol.test.ts
  key_links:
    - "process exit/stdout -> JSON/shape validation -> precedence-ordered wire decision"
---

<objective>
Refactor hook wire interpretation into an exhaustive precedence and malformed-input contract.

Purpose: Prove every exit, JSON, decision, mutation, diagnostic, and no-op partition from complete inputs (D-01, D-02, D-04, D-05, D-13, D-24).
Output: Refactored wire owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Exhaust exit status, JSON shape, and top-level precedence</name>
  <files>tests/bridges/hooks/wire-protocol.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts, tests/bridges/hooks/wire-protocol.test.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, tests/bridges/hooks/dispatch-exec.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, D-05, D-13, and D-24, use complete local exit/stdout inputs and independently authored whole results to cover exit 2, another nonzero exit, null exit, zero with empty output, malformed JSON, JSON primitive, JSON null, and JSON object. Prove top-level stop wins before block and top-level decisions win before nested decisions using conflicting complete objects. Assert exact semantic diagnostics and prove diagnostic reporting never throws. Use lowercase arrange/act/assert comments; a combined lowercase act-and-assert phase is permitted only for one `assert.throws()` or `assert.rejects()` expression. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/wire-protocol.test.ts</automated></verify>
  <acceptance_criteria>Every exit and JSON-shape partition has an exact outcome, conflicting objects prove top-level precedence, diagnostics are exact/nonthrowing, and the focused owner passes without a source edit.</acceptance_criteria>
  <done>The wire interpreter's entry validation and first precedence tiers are proved end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaust nested decisions, mutation whitelist, typing, and no-op</name>
  <files>tests/bridges/hooks/wire-protocol.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts, tests/bridges/hooks/wire-protocol.test.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Author separate complete cases proving nested deny wins before mutation; each allowed mutation field is accepted with its exact output; every conflict branch between mutation fields follows the live precedence; mutation wins before top-level `suppressOutput`; allow and ask decisions retain correctly typed reasons; wrong-type optional fields are omitted rather than coerced; and an otherwise valid empty object produces the final no-op result. Do not generalize through a mutation table or shared oracle. Assert stable decision precedence only, not a fabricated global stdout/stderr order. Keep every expected result literal and all runtime phases lowercase. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/wire-protocol.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/wire-protocol.test.ts</automated></verify>
  <acceptance_criteria>Nested-deny, each mutation field/conflict, mutation-versus-suppressOutput, allow/ask reason typing, wrong-type omission, and no-op branches have independently authored exact expectations; direct coverage passes with no mutation-whitelist widening.</acceptance_criteria>
  <done>The wire interpreter's complete precedence and mutation contract is explicit and exhaustive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child-process output -> runtime decision | Untrusted exit status and JSON can request control flow or mutations and must be interpreted by fixed precedence and a strict whitelist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-06 | Tampering | wire decision/mutation interpreter | high | mitigate | Exhaustive precedence, type omission, mutation-field whitelist, malformed-input, and no-op assertions constrain untrusted output to declared effects. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/wire-protocol.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-31-PLAN.md`.

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Refactor every Hook Runtime source-test owner to deterministic, explicit unit contracts | COVERED | 112-01 through 112-31, one ROADMAP pair per plan |
| REQ | MOD-05 | COVERED | Present in every plan's `requirements` field |
| RESEARCH | Complete 31-row owner matrix | COVERED | Row N maps one-to-one to plan 112-N |
| RESEARCH | Sole supplemental carriers | COVERED | 112-02 async-rewake prune; 112-04 hooks-exec delete and hooks-translators byte-equal prune; 112-05 hooks-reducer delete; 112-06 hooks-adapters delete; 112-07 additional-context delete/hooks-dispatch prune; 112-13 hooks-if-field prune-or-delete; 112-28 symlink-escape delete |
| RESEARCH | Retained lifecycle/foundation/cap-notify/schema/integration suites and narrow hooks-translators remainder | COVERED | Lifecycle/foundation/cap-notify/schema/integration suites remain unchanged; after 112-04 pruning, hooks-translators retains only translator-module completeness and shared tool-name mapping |
| CONTEXT | D-01 through D-06 | COVERED | D-01/D-02/D-04 across all runtime owners; D-03 in 08/13/14/22/23/30; D-05 in stateful owners; D-06 in sole carriers |
| CONTEXT | D-07 through D-12 | COVERED | 02/04/27 execution/serialization and 02/03/04/27 stream ordering; 02/04 process ownership |
| CONTEXT | D-13 through D-17 | COVERED | 02/04/29/31 error/timeout semantics and 09 timer boundaries/cleanup |
| CONTEXT | D-18 through D-21 | COVERED | 02/05/06/07/25/26 lifecycle/state; 02/07 routing/reload; 02/07/10 environment; 01/02 PID/process cleanup |
| CONTEXT | D-22 through D-25 | COVERED | 15-24 translator envelopes; 04 real process tracer; 31 wire precedence; 27 byte/shell policy |
| EDGE | Adjacency | COVERED | 03/04/09/27 exact cap versus one-over and just-before versus exact timer predicates |
| EDGE | Empty/null/zero | COVERED | 03/04/08/10/11/12/15/18/21/23/24/25/27/29/31 at owners whose live inputs accept them |
| EDGE | Ordering | COVERED | 02/03/05/07/11/12/13/25/31 stable local order; 02/04/27 explicitly reject fabricated global stdout/stderr order |
| PROHIBITION | Recall stage | REVIEWED | Raw candidates were routine refactor/test-quality or canonical injection/path/PID/env/mutation risks |
| PROHIBITION | Precision stage | RESOLVED | Routine items remain edge/code-review concerns; canonical safety risks are breadcrumbed to T-112-01 through T-112-06; no bespoke prohibition survives and no `check_*` descriptor is authored |

<verification>
Run `node --test tests/bridges/hooks/wire-protocol.test.ts` and `npm run test:coverage:direct -- tests/bridges/hooks/wire-protocol.test.ts`; then validate all 31 plans for schema, structure, exact ROADMAP pair mapping, D-ID coverage, supplemental ownership, threat models, artifact inventories, and lowercase phase-comment directives.
</verification>

<success_criteria>
Wire interpretation has exact exhaustive branch coverage, and the 31-plan set covers every GOAL, MOD-05 requirement, research row, locked decision, deterministic edge probe, and security breadcrumb with no silent omission.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-31-SUMMARY.md` when done.
</output>
