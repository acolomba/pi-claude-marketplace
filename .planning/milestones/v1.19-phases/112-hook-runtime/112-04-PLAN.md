---
phase: 112-hook-runtime
plan: 04
type: execute
wave: 6
depends_on: [112-02, 112-03, 112-09, 112-10, 112-15, 112-16, 112-17, 112-18, 112-19, 112-20, 112-21, 112-22, 112-23, 112-24, 112-27, 112-29, 112-30, 112-31]
files_modified:
  - tests/bridges/hooks/dispatch-exec.test.ts
  - tests/architecture/hooks-exec.test.ts
  - tests/architecture/hooks-translators.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-04 directly proves blocking and async delegation, all translator admission, spawn, stream, stdin, timer, terminal, parse, and cleanup branches (D-01, D-07-D-13, D-23)."
    - "Empty/zero, exact-cap, one-byte-over, split-UTF-8, decoder-flush, independent stdout/stderr, and overflow cleanup are explicit; no global inter-stream order is invented (D-10)."
    - "The stdin error listener precedes end, exact serialized bytes are delivered, EPIPE and synchronous end failures are contained, and async stdin absence is preserved (D-11)."
    - "All single-module hooks-exec evidence moves into this owner and the duplicate suite is removed by this sole carrier (D-06)."
    - "After owners 112-15 through 112-24 establish direct envelopes, this sole carrier removes only the byte-equal translator round-trip block and its EVENT_FIXTURES/EXPECTED_JSON data while retaining cross-module translator completeness and shared tool-name mapping (D-06, D-22)."
  artifacts:
    - path: tests/bridges/hooks/dispatch-exec.test.ts
      provides: "Canonical direct dispatch-exec owner"
    - path: tests/architecture/hooks-exec.test.ts
      provides: "Deleted duplicate suite after absorption"
    - path: tests/architecture/hooks-translators.test.ts
      provides: "Pruned cross-module suite retaining only translator completeness and shared tool-name mapping"
  key_links:
    - from: tests/bridges/hooks/dispatch-exec.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
      via: "direct dispatchHookExec call with injected spawn boundary"
      pattern: "dispatchHookExec"
---

<objective>
Complete the blocking subprocess owner and retire the duplicate architecture execution suite.

Purpose: MOD-05 requires exact, bounded, never-leaking child execution for all hook payloads and malformed unknown input.
Output: One complete direct owner with realistic streams and no competing single-module suite.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one blocking hook through a real portable child boundary</name>
  <files>tests/bridges/hooks/dispatch-exec.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
    - tests/bridges/hooks/dispatch-exec.test.ts
    - tests/architecture/hooks-exec.test.ts
    - tests/platform/git-credential.test.ts
    - tests/platform/credential-process-fake.ts
    - extensions/pi-claude-marketplace/bridges/hooks/spawn-helpers.ts
    - extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Normalize one full blocking happy path using a small portable `process.execPath` program only to prove the default OS spawn boundary. Author a complete typed event and entry, exact command/args/shell false/cwd/env/stdin bytes, stdout/stderr, exit, parsed HookExecResult, listener/timer cleanup, and child completion. Keep args-present exec-form distinct from shell-form. For all controllable paths use the existing spawn dependency, not module substitution. Use lowercase phases and complete independent outcomes per D-01, D-02, D-04, D-05, D-07, D-09, D-11, D-13, and D-23. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/dispatch-exec.test.ts</automated>
  </verify>
  <acceptance_criteria>A portable real child proves the default spawn arm; the direct owner independently asserts exact process boundary, input bytes, result, terminal state, and cleanup; no shell interpolation, live external process, hidden seam, or real wait is used.</acceptance_criteria>
  <done>One blocking hook executes end to end through the real production process boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Complete malformed payload, stream, terminal, stdin, and delegation partitions</name>
  <files>tests/bridges/hooks/dispatch-exec.test.ts, tests/architecture/hooks-exec.test.ts, tests/architecture/hooks-translators.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
    - tests/bridges/hooks/dispatch-exec.test.ts
    - tests/architecture/hooks-exec.test.ts
    - tests/architecture/hooks-translators.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    - extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
    - extensions/pi-claude-marketplace/bridges/hooks/hook-env.ts
    - extensions/pi-claude-marketplace/bridges/hooks/spawn-helpers.ts
    - extensions/pi-claude-marketplace/bridges/hooks/wire-protocol.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Use injected child doubles with PassThrough streams to cover async `=== true` delegation, missing Pi, delegation rejection containment, all ten translator keys with required-field lists `[]`, `["text"]`, or `["toolName", "input"]`, and unknown payload null/non-object/missing/wrong-shape diagnostics with public never-throw noop. Per D-12, use real Node PassThrough streams inside process doubles for buffering, UTF-8 decoding, end/flush, and listener lifecycle; reserve custom emitters only for event orderings real streams cannot stage. Stage normal close, spawn error, exit-before-pipe-close, duplicate/late events, null streams, exact stdout/stderr caps, one-byte overflow in each lane, multiple chunks, split UTF-8, decoder end flush, independent stream order, listener removal, immediate overflow ladder, exact stdin bytes, listener-before-end, EPIPE, synchronous end throw, timer cancellation, and parse result. Assert complete boundaries and outcomes per D-07-D-13 and D-23; do not assert a total stdout/stderr order. Absorb all direct hooks-exec cases and delete `tests/architecture/hooks-exec.test.ts` as the sole D-06 carrier; retain OS install-to-child proof only in existing integration suites. Because this plan depends on every payload owner 112-15 through 112-24 and exercises all ten translator dispatch paths, make it the sole carrier for `tests/architecture/hooks-translators.test.ts`: remove only the byte-equal per-translator round-trip test block and its `EVENT_FIXTURES` and `EXPECTED_JSON` data, retain the translator-module completeness test and the shared tool-name mapping test with genuine cross-module titles, and do not move their assertions into the dispatch-exec owner. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>test ! -e tests/architecture/hooks-exec.test.ts &amp;&amp; node --test tests/bridges/hooks/dispatch-exec.test.ts &amp;&amp; node --test tests/architecture/hooks-translators.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/dispatch-exec.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; all translator, unknown-input, process, stream, stdin, timer, terminal, delegation, and cleanup partitions are independently asserted; hooks-exec.test.ts is absent only after absorption; hooks-translators.test.ts has no byte-equal round-trip block, EVENT_FIXTURES, or EXPECTED_JSON but its translator-module completeness and shared tool-name mapping tests pass; integration OS-spawn proof remains; no production change, test-only seam, shared mutable fixture, coverage exception, or global stream-order assertion exists.</acceptance_criteria>
  <done>P112-04 is complete and the duplicate execution authority is removed.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| unknown hook payload/declaration → child process | Untrusted data selects translation and process input. |
| child streams/timers → runtime result | Untrusted output and non-terminating children consume bounded resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-01 | Tampering / Elevation | spawn planning | high | mitigate | Preserve args-present exec form with shell false, exact arguments, and no interpolation of test data. |
| T-112-04 | Denial of service | stream/timer settlement | high | mitigate | Enforce independent byte caps, listener removal, SIGTERM/SIGKILL cleanup, duplicate settlement guard, and exact timer cancellation. |
| T-112-05 | Information disclosure | child env/session | high | mitigate | Assert complete environment precedence/absence and case-local restoration through leaf contracts. |

Canon command, resource, and environment risks are handled by STRIDE; no bespoke prohibition is minted.
</threat_model>

## Artifacts this phase produces

- Refines `tests/bridges/hooks/dispatch-exec.test.ts` as the sole mirrored owner.
- Deletes `tests/architecture/hooks-exec.test.ts` after all direct behavior is absorbed.
- Prunes `tests/architecture/hooks-translators.test.ts` by removing only byte-equal round-trip duplication and its `EVENT_FIXTURES`/`EXPECTED_JSON`, retaining translator completeness and shared tool-name mapping.
- Creates no new production symbol and leaves `dispatch-exec.ts` unchanged.
- Creates `.planning/phases/112-hook-runtime/112-04-PLAN.md` and `112-04-SUMMARY.md` after execution.

<verification>
The focused owner, pruned translator architecture suite, typecheck, and direct coverage pass; hooks-exec is absent; complete child/stream/timer cleanup is asserted.
</verification>

<success_criteria>
- P112-04 owns exactly one pair.
- Blocking/async, payload, process, stream, stdin, terminal, parse, and cleanup contracts are complete.
- The hooks-exec supplemental is removed and hooks-translators is narrowly pruned by this sole carrier.
- No injection, output-bound, environment, or cleanup threat remains open.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-04-SUMMARY.md` when done.
</output>
