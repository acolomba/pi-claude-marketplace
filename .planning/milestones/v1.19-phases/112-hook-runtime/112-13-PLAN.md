---
phase: 112-hook-runtime
plan: 13
type: execute
wave: 3
depends_on: [112-11, 112-12]
files_modified:
  - tests/bridges/hooks/if-field/index.test.ts
  - tests/architecture/hooks-if-field.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-13 owns exact predicate types/re-exports, compile partitions, evaluate partitions, cwd fallback, MCP matching, and exhaustive dispatch (D-01, D-03, D-04)."
    - "Empty/non-tool/unknown values fail open as documented; equal matches preserve stable declaration order."
    - "The five exact predicate arms are match-all, bash, path-tool, mcp-literal, and mcp-server-prefix; no internal metadata export is added (D-03, D-05)."
    - "This sole carrier prunes hooks-if-field to only a genuine parseHooksConfig side-map→RoutingEntry cross-module chain (D-06)."
  artifacts:
    - path: tests/bridges/hooks/if-field/index.test.ts
      provides: "New canonical direct predicate composition owner"
    - path: tests/architecture/hooks-if-field.test.ts
      provides: "Pruned cross-module config-to-routing chain"
  key_links:
    - from: tests/bridges/hooks/if-field/index.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts
      via: "direct compile/evaluate exports and type re-exports"
      pattern: "compileIfPredicate|evaluateIfPredicate"
---

<objective>
Create the if-field composition owner and reduce its shared architecture suite to one genuine cross-module chain.

Purpose: MOD-05 needs exact fail-open predicate compilation/evaluation across Bash, path, and MCP declarations.
Output: One complete direct owner and one narrowly retained supplemental suite.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/research/claude-hook-config-syntax.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one path-tool predicate from compile through evaluation</name>
  <files>tests/bridges/hooks/if-field/index.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts
    - tests/bridges/hooks/if-field/index.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - tests/bridges/agents/index.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/bash.ts
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
    - docs/research/claude-hook-config-syntax.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Create the missing owner. At module scope, prove the exact exported predicate/re-export shape. At runtime compile a complete path-tool declaration to an independently authored `path-tool` predicate, evaluate it against complete tool input and cwd, and assert the full boolean/metadata outcome. Keep type evidence outside runtime phases; runtime case uses lowercase phases. Apply D-01-D-05. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/if-field/index.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The new owner directly imports index.ts; exact type/re-export evidence is module-scoped and one complete path-tool compile/evaluate path passes without internal export widening.</acceptance_criteria>
  <done>The composition seam between declaration, compiled predicate, and evaluation is direct and green.</done>
</task>

<task type="auto">
  <name>Task 2: Complete predicate vocabulary, fail-open, MCP, cwd, and carrier partitions</name>
  <files>tests/bridges/hooks/if-field/index.test.ts, tests/architecture/hooks-if-field.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts
    - tests/bridges/hooks/if-field/index.test.ts
    - tests/architecture/hooks-if-field.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/bash.ts
    - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - docs/research/claude-hook-config-syntax.md
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add complete module-scope positive/negative evidence for exact predicate arms `match-all`, `bash`, `path-tool`, `mcp-literal`, and `mcp-server-prefix`. Add runtime cases for compile empty, non-tool, known/unknown tool, Bash parse fail-open, path membership/non-membership, missing command, cwd fallback, MCP literal equality, MCP server prefix, wrong server, and exhaustive dispatch. Assert stable row order and complete values. After 112-11/12 absorb their truth tables, prune hooks-if-field.test.ts to only a uniquely named `parseHooksConfig` side-map→RoutingEntry integration if no retained integration duplicates it; otherwise remove the file. Run the resulting supplemental path or absence gate. Do not add production exports or duplicate parsers. Apply D-01-D-06. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/if-field/index.test.ts &amp;&amp; (test ! -e tests/architecture/hooks-if-field.test.ts || node --test tests/architecture/hooks-if-field.test.ts) &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/if-field/index.test.ts</automated>
  </verify>
  <acceptance_criteria>The owner reports 100 percent direct functions, lines, and branches; exact predicate types, compile/evaluate/fail-open/path/MCP/cwd/exhaustive partitions are complete; the supplemental file is either absent because duplicated or contains only the named config-to-routing chain and passes; no test-only metadata, shared oracle, production delta, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-13 is complete and the sole if-field supplemental disposition is final.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| hook if declaration/event input → routing predicate | Untrusted shell/path/MCP metadata selects whether a hook runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-01 | Tampering / Elevation | Bash predicate composition | high | mitigate | Preserve parser fail-open rules without executing shell text. |
| T-112-02 | Tampering / Disclosure | path-tool predicate | high | mitigate | Prove contained membership, cwd fallback, non-membership, and exact compiled forms. |

No bespoke prohibition survives; shell/path risks remain canonical STRIDE controls.
</threat_model>

## Artifacts this phase produces

- Creates `tests/bridges/hooks/if-field/index.test.ts` as the sole mirrored owner.
- Refines or removes `tests/architecture/hooks-if-field.test.ts` according to its unique cross-module chain.
- Creates no production symbol and leaves `if-field/index.ts` unchanged.
- Creates `112-13-PLAN.md` and `112-13-SUMMARY.md` after execution.

<verification>
Focused owner, conditional supplemental, typecheck, and direct coverage pass at 100 percent.
</verification>

<success_criteria>
- P112-13 owns exactly one pair.
- Predicate types, compile/evaluate, path, MCP, fail-open, empty, and ordering behavior is complete.
- Supplemental pruning happens only here.
- No high shell/path threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-13-SUMMARY.md` when done.
</output>
