---
phase: 112-hook-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/async-rewake/pid-table.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-01 is the sole owner of pid-table.ts and re-proves the accepted baseline instead of treating its prior test as completion (D-01)."
    - "Absent, malformed, stale, atomic read/write, defensive-copy, scope, ENOENT, write-failure, and non-ENOENT unlink partitions are observable through real case-owned temporary roots (D-04, D-05, D-21)."
    - "The failure boundary is a real `_shared` non-directory shape; no filesystem injection or new production test seam is introduced (D-05, D-21)."
    - "Every runtime case uses lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; the combined form is limited to one throw/rejection expression (D-02)."
  artifacts:
    - path: tests/bridges/hooks/async-rewake/pid-table.test.ts
      provides: "Canonical direct owner of async-rewake/pid-table.ts"
  key_links:
    - from: tests/bridges/hooks/async-rewake/pid-table.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts
      via: "direct import of path/read/write/unlink exports"
      pattern: "pidTablePath|readPidTable|writePidTable|unlinkPidTable"
---

<objective>
Normalize and complete the PID-table persistence owner, including deterministic write and unlink failures through the real filesystem.

Purpose: MOD-05 requires the async PID ledger to remain fail-clean, scope-contained, and directly covered without a test-only boundary.
Output: One hermetic owner test with complete direct branch, line, and function evidence.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one scoped PID-table write/read lifecycle</name>
  <files>tests/bridges/hooks/async-rewake/pid-table.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts
    - tests/bridges/hooks/async-rewake/pid-table.test.ts
    - tests/architecture/hooks-async-rewake.test.ts
    - tests/bridges/skills/stage.test.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Rewrite the owner around direct pid-table exports. In one production-quality slice, allocate a fresh root, author one complete version-1 entry with pid, dispatchId, scope, marketplace, plugin, and spawnedAt, write it atomically to `_shared/async-rewake-pids.json`, read the complete entry array and exact persisted JSON bytes, prove caller-array mutation does not alias stored data, and unlink it. Register recursive cleanup before acting. Preserve pid-table.ts byte-for-byte and use the A1 real-filesystem analog. Apply D-01, D-02, D-04, D-05, and D-21. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/pid-table.test.ts</automated>
  </verify>
  <acceptance_criteria>The owner directly imports pid-table.ts; the path suffix, version 1 envelope, six entry fields, exact bytes, returned values, defensive copy, and final absence are independently asserted; all mutable filesystem state is case-owned and cleaned; the focused node test passes.</acceptance_criteria>
  <done>A complete scoped write/read/unlink path works through the real production filesystem boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Close malformed, stale, scope, and filesystem-failure partitions</name>
  <files>tests/bridges/hooks/async-rewake/pid-table.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/pid-table.ts
    - tests/bridges/hooks/async-rewake/pid-table.test.ts
    - tests/architecture/hooks-async-rewake.test.ts
    - tests/bridges/skills/stage.test.ts
    - extensions/pi-claude-marketplace/shared/atomic-json.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add independent cases for absent file, malformed JSON, null/object/entries shape mismatch, stale version, separate user/project roots, read non-ENOENT degradation, unlink ENOENT, and semantic diagnostics. Stage both missing coverage arms without injection: create the case root, place a regular file at its `_shared` path, and call write/unlink on the child path so the existing atomic-write or unlink boundary reports ENOTDIR or another non-ENOENT error. Assert never-throw results, diagnostic category and relevant path/error text, and unchanged case tree. Absorb PID-file-only expectations from the architecture suite into this owner without editing that shared file; Plan 112-02 is its sole final carrier per D-06. Keep complete case-local inputs, lowercase phases, and cleanup per D-02, D-04, D-05, and D-21. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/pid-table.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/async-rewake/pid-table.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches for pid-table.ts; both previously uncovered diagnostic arms are reached through real case-owned filesystem shapes; empty/malformed/stale/scope partitions have complete independent outcomes; no production source, supplemental file, filesystem injection, shared root, skip, or coverage exception is added.</acceptance_criteria>
  <done>P112-01 is normalized, hermetic, pair-atomic, and complete for MOD-05.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; an unmitigated high-severity threat blocks completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| case-owned storage root → PID ledger | Persisted process metadata crosses a contained local filesystem boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-02 | Tampering | pidTablePath/read/write/unlink | high | mitigate | Use hardcoded suffixes, containment, isolated roots, exact bytes, real non-directory failures, and full cleanup; never touch a live user PID table. |

Prohibition recall/precision outcome: routine file-handle and correctness concerns were dropped; traversal is canon and remains in this threat model, so no bespoke prohibition is minted.
</threat_model>

## Artifacts this phase produces

- Refines `tests/bridges/hooks/async-rewake/pid-table.test.ts` as the sole mirrored owner.
- Creates no new production symbol; `pid-table.ts` remains byte-for-byte unchanged.
- Creates `.planning/phases/112-hook-runtime/112-01-PLAN.md` and, after execution, `112-01-SUMMARY.md`.

<verification>
Run the focused node test and direct coverage command. Both must pass; direct coverage must report 100 percent functions, lines, and branches.
</verification>

<success_criteria>
- P112-01 owns exactly one production source-test pair.
- Exact path, version, entry, read/write/unlink, error, and cleanup contracts are proved.
- PID-file-only duplicate evidence is ready for the sole 112-02 carrier to prune.
- No test-only production seam or unmitigated high-severity threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-01-SUMMARY.md` when done.
</output>
