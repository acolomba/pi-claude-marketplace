---
phase: 112-hook-runtime
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/async-rewake/ring-buffer.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-03 re-proves every RingBuffer byte boundary even though the baseline was green (D-01)."
    - "Empty and zero-capacity writes, exact fill, one-byte over, wraparound, oversized chunks, arbitrary bytes, and split UTF-8 have explicit complete outcomes (D-10)."
    - "Chronological order is stable within this stream; the phase does not infer a global stdout/stderr order (D-10)."
    - "Each case owns a fresh buffer and independent expected bytes with lowercase phases (D-02, D-04, D-05)."
  artifacts:
    - path: tests/bridges/hooks/async-rewake/ring-buffer.test.ts
      provides: "Canonical direct RingBuffer owner"
  key_links:
    - from: tests/bridges/hooks/async-rewake/ring-buffer.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts
      via: "direct constructor/write/read calls"
      pattern: "new RingBuffer|write|read"
---

<objective>
Normalize the raw-byte ring-buffer owner and make every adjacency, empty, overflow, and ordering boundary explicit.

Purpose: MOD-05 depends on bounded async output that preserves the chronological tail without character-count assumptions.
Output: One independent, complete direct owner; no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove exact fill and chronological overflow tail</name>
  <files>tests/bridges/hooks/async-rewake/ring-buffer.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts
    - tests/bridges/hooks/async-rewake/ring-buffer.test.ts
    - tests/bridges/agents/convert.test.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Normalize a direct owner slice that constructs a fresh capacity-4 buffer, proves an exact four-byte fill is not truncated, then independently proves a fifth byte drops only the oldest byte and returns the chronological tail with the truncation latch. Author input buffers and complete `{ data, truncated }` expectations independently. Use lowercase phases per D-02 and preserve ring-buffer.ts byte-for-byte per D-01, D-04, D-05, and D-10. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/ring-buffer.test.ts</automated>
  </verify>
  <acceptance_criteria>Exact-cap and one-byte-over cases are distinct, independently authored, direct, and green; chronological tail and truncation state are compared as complete values; every case owns a new buffer.</acceptance_criteria>
  <done>The core bounded chronological-tail contract is directly proved.</done>
</task>

<task type="auto">
  <name>Task 2: Complete empty, zero, wrap, oversized, raw-byte, and UTF-8 partitions</name>
  <files>tests/bridges/hooks/async-rewake/ring-buffer.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts
    - tests/bridges/hooks/async-rewake/ring-buffer.test.ts
    - tests/bridges/agents/convert.test.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add sibling cases for empty read, empty write, zero capacity with empty and non-empty input, multiple-chunk fill, physical wraparound, a chunk exactly capacity, a chunk one byte over, a much larger chunk retaining only its tail, latched truncation after later reads/writes, arbitrary non-text bytes, and a UTF-8 sequence split at a byte boundary whose raw tail is accepted without decoding. Include the production caps 65_536 and 1_048_576 as explicit capacity evidence without allocating shared buffers. Data rows still contain separate lowercase phases. Do not edit registry behavior or a supplemental suite; apply D-01, D-02, D-04, D-05, and D-10. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/async-rewake/ring-buffer.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/async-rewake/ring-buffer.test.ts</automated>
  </verify>
  <acceptance_criteria>The owner reports 100 percent functions, lines, and branches; all empty/null-equivalent, exact-touch, one-over, stable-order, and byte partitions are explicit; no expectation decodes or derives through production logic; no shared buffer, production delta, supplemental edit, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-03 is normalized and complete for the bounded raw-byte store.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| child output bytes → bounded in-memory buffer | Untrusted output must stay within a fixed byte capacity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-04 | Denial of service | RingBuffer | high | mitigate | Prove zero/exact/overflow/oversized byte caps, chronological tail, and bounded storage with complete raw-byte assertions. |

No bespoke prohibition survives the recall/precision pass; bounded-output risk is canon and covered here.
</threat_model>

## Artifacts this phase produces

- Refines `tests/bridges/hooks/async-rewake/ring-buffer.test.ts` as the sole owner.
- Creates no new production symbol and leaves `ring-buffer.ts` unchanged.
- Creates `.planning/phases/112-hook-runtime/112-03-PLAN.md` and `112-03-SUMMARY.md` after execution.

<verification>
The focused owner and direct coverage command pass at 100 percent functions, lines, and branches.
</verification>

<success_criteria>
- P112-03 owns exactly one pair.
- All empty, adjacency, overflow, ordering, wrap, and raw-byte truths are explicit.
- Every buffer and expectation is case-local.
- No unmitigated high threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-03-SUMMARY.md` when done.
</output>
