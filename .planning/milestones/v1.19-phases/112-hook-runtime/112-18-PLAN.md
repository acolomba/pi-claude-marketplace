---
phase: 112-hook-runtime
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/pre-compact.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: pre-compact cases use complete local inputs, whole expectations, and lowercase phase comments."
    - "D-22: the five-key PreCompact envelope emits exact event/trigger literals and preserves context identity without extra fields."
    - "Accepted empty string inputs retain their exact values; no unsupported null contract is invented."
  artifacts:
    - tests/bridges/hooks/payloads/pre-compact.test.ts
  key_links:
    - "translatePreCompact input -> exact five-key wire payload"
---

<objective>
Refactor the pre-compact translator owner into a complete literal contract.

Purpose: Prove its envelope and empty-value behavior without production-derived oracles (D-01, D-02, D-04, D-22).
Output: Refactored owner test only; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one complete PreCompact envelope</name>
  <files>tests/bridges/hooks/payloads/pre-compact.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-compact.ts, tests/bridges/hooks/payloads/pre-compact.test.ts, tests/bridges/hooks/payloads/post-compact.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, and D-22, author a complete local input and literal five-key expected object using exact `PreCompact` and `auto` values, supplied session/transcript/cwd strings, and the original context reference. Assert the whole payload and absence of extra fields through strict deep equality. Use the locked lowercase runtime phase comment sequence. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/pre-compact.test.ts</automated></verify>
  <acceptance_criteria>The happy path asserts every key and exact literal, context identity is explicit, no shared builder supplies expected values, and the owner test passes without a source edit.</acceptance_criteria>
  <done>The complete PreCompact payload is proved end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Cover accepted empty values and exact key set</name>
  <files>tests/bridges/hooks/payloads/pre-compact.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/pre-compact.ts, tests/bridges/hooks/payloads/pre-compact.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add a separately authored case for any session/transcript/cwd empty strings accepted by the input type. Assert the entire five-key object with those strings unchanged and a case-owned context. Do not add a null partition the production signature cannot receive. Keep each case independent and leave `tests/architecture/hooks-translators.test.ts` untouched because it has no carrier disposition here. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/pre-compact.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/pre-compact.test.ts</automated></verify>
  <acceptance_criteria>Applicable empty values are explicit, whole expected objects remain independently authored, direct coverage passes, runtime comments are lowercase, and the supplemental suite is unchanged.</acceptance_criteria>
  <done>The owner covers both normal and empty-value PreCompact contracts without fabricating behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No listed Phase 112 security boundary applies: this pure translator neither executes commands nor handles containment, PID markers, byte/timer limits, shared state, or mutations.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-NA-18 | Tampering | `translatePreCompact` payload shape | low | accept | Exact five-key assertions detect accidental shape drift; no security-sensitive operation is added. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/pre-compact.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-18-PLAN.md`.

<verification>
Run the focused node test and direct coverage command; confirm the plan touches only its owner test.
</verification>

<success_criteria>
The PreCompact owner proves exact normal and accepted empty-value envelopes with no source or supplemental change.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-18-SUMMARY.md` when done.
</output>
