---
phase: 112-hook-runtime
plan: 24
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/user-prompt-submit.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: UserPromptSubmit cases own complete inputs and whole five-key expectations under lowercase phase comments."
    - "D-22: prompt text, exact event, identity values, and context propagate without extra fields."
    - "Empty and non-ASCII prompt values remain byte-for-byte intact; malformed process-output behavior remains with plan 112-04."
  artifacts:
    - tests/bridges/hooks/payloads/user-prompt-submit.test.ts
  key_links:
    - "translateUserPromptSubmit prompt/context -> exact five-key payload"
---

<objective>
Make the UserPromptSubmit translator payload exact across ordinary and text-boundary inputs.

Purpose: Lock whole-envelope and text-preservation behavior using case-owned values (D-01, D-02, D-04, D-22).
Output: Refactored owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove a complete UserPromptSubmit envelope</name>
  <files>tests/bridges/hooks/payloads/user-prompt-submit.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/user-prompt-submit.ts, tests/bridges/hooks/payloads/user-prompt-submit.test.ts, extensions/pi-claude-marketplace/bridges/hooks/translation-context.ts, tests/bridges/hooks/translation-context.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-04, and D-22, build a complete local prompt submission input and assert an independently written five-key payload with exact `UserPromptSubmit`, session/transcript/cwd, prompt, and context values as defined by the live function. Assert context identity and the exact key set. Use lowercase arrange/act/assert comments and no shared expected-payload builder. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/user-prompt-submit.test.ts</automated></verify>
  <acceptance_criteria>The normal case asserts all five keys, exact event/prompt, context identity, and no extras; the focused owner passes with no source change.</acceptance_criteria>
  <done>A normal prompt submission is proved through its complete translator envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Cover empty and non-ASCII prompt preservation</name>
  <files>tests/bridges/hooks/payloads/user-prompt-submit.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/user-prompt-submit.ts, tests/bridges/hooks/payloads/user-prompt-submit.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Add one complete empty-prompt case and one complete prompt containing multi-byte non-ASCII text. Independently repeat the whole five-key expectation and assert byte-for-byte string preservation. Do not add malformed JSON/process cases because plan 112-04 owns dispatch-exec failures. Leave `tests/architecture/hooks-translators.test.ts` unchanged. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/user-prompt-submit.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/user-prompt-submit.test.ts</automated></verify>
  <acceptance_criteria>Ordinary, empty, and non-ASCII prompts are explicit complete payload cases, direct coverage passes, comments are lowercase, and no dispatch/supplemental concern enters this owner.</acceptance_criteria>
  <done>The UserPromptSubmit translator preserves all applicable prompt-text boundaries exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No listed Phase 112 threat applies to this pure copy translator: command execution/mutation semantics remain downstream, and this pair only proves exact text and envelope propagation.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-NA-24 | Tampering | UserPromptSubmit payload shape | low | accept | Whole-object and exact text-preservation assertions detect accidental field or encoding drift without adding a new security boundary. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/user-prompt-submit.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-24-PLAN.md`.

<verification>
Run the owner test and direct coverage; confirm only its test file changed.
</verification>

<success_criteria>
UserPromptSubmit translation has exact normal, empty, and non-ASCII whole-payload coverage without source or supplemental edits.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-24-SUMMARY.md` when done.
</output>
