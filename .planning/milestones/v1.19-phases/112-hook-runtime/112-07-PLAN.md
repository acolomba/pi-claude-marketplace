---
phase: 112-hook-runtime
plan: 07
type: execute
wave: 9
depends_on: [112-02, 112-05, 112-25, 112-26]
files_modified:
  - tests/bridges/hooks/event-router.test.ts
  - tests/bridges/hooks/session-start-additional-context.test.ts
  - tests/architecture/hooks-dispatch.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-07 directly proves cache, hydration, rebuild, pending-context drain, reload, orphan, and exactly-11-registration behavior (D-01, D-18, D-19)."
    - "Routing table order is stable within every bucket, project/user scope remains separated, and reload cannot delete another live entry (D-19)."
    - "Every case owns router/session/filesystem state and resets it through production lifecycle APIs (D-05, D-18, D-20)."
    - "The additional-context suite is removed and hooks-dispatch retains only OBS-01 repository-wide static logging constraints under this sole carrier (D-06)."
  artifacts:
    - path: tests/bridges/hooks/event-router.test.ts
      provides: "Canonical direct event-router owner"
    - path: tests/architecture/hooks-dispatch.test.ts
      provides: "Pruned repository-wide OBS-01 static contract"
  key_links:
    - from: tests/bridges/hooks/event-router.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
      via: "direct cache/hydrate/rebuild/register exports and Pi event registration"
      pattern: "registerHooksBridge|rebuildRoutingTables|hydrateProjectScopeForCwd"
---

<objective>
Complete the event-router lifecycle owner and finish the shared routing/additional-context supplemental migration.

Purpose: MOD-05 needs deterministic reload, cache, hydration, scope, pending context, orphan, and event registration behavior.
Output: A complete direct owner, one deleted bridge supplement, and one narrowly retained architecture contract.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one bridge reload from state reset to registered handlers</name>
  <files>tests/bridges/hooks/event-router.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
    - tests/bridges/hooks/event-router.test.ts
    - tests/architecture/hooks-dispatch.test.ts
    - tests/bridges/hooks/session-start-additional-context.test.ts
    - tests/shared/completion-cache.test.ts
    - tests/bridges/skills/stage.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Normalize one full registerHooksBridge slice with a fresh case root, state/config, production lifecycle cleanup, recording Pi API, and explicit operation log. Prove epoch bump, pending/settle reset, async-child shutdown, cache hydration, table rebuild, orphan reap, then exactly 11 registrations across ten event names with `input` twice. Assert the complete ordered log and registration payloads independently, including no stale state after cleanup. Apply D-01, D-02, D-04, D-05, D-18-D-20. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/event-router.test.ts</automated>
  </verify>
  <acceptance_criteria>One reload path directly proves reset→shutdown→hydrate→rebuild→reap→registration order, exact event registrations, complete state effects, and cleanup with no shared root or hidden reader; the focused node test passes.</acceptance_criteria>
  <done>The bridge lifecycle is wired end to end under the canonical owner.</done>
</task>

<task type="auto">
  <name>Task 2: Complete cache, hydration, drain, scope, defensive, and carrier partitions</name>
  <files>tests/bridges/hooks/event-router.test.ts, tests/bridges/hooks/session-start-additional-context.test.ts, tests/architecture/hooks-dispatch.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
    - tests/bridges/hooks/event-router.test.ts
    - tests/bridges/hooks/session-start-additional-context.test.ts
    - tests/architecture/hooks-dispatch.test.ts
    - tests/architecture/hooks-lifecycle.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add complete cases for cache-key scope/plugin separation and idempotence; disk read/parse failure; one-shot context drain before dispatch; epoch rejection; all ten bucket rebuilds with stable declaration order; user/project hydration with disabled, empty, containment, brand, read, and parse partitions; project-prefix replacement; shared-directory missing/failure; load-state degradation; reload cleanup with multiple live children/scopes; orphan reap order; defensive flatten arms reachable through valid inputs; and lazy SessionStart project hydration. Use real case-owned roots and public lifecycle APIs only. Move router/drain evidence from both supplemental files into the owner, delete `session-start-additional-context.test.ts`, and prune `hooks-dispatch.test.ts` to the named repository-wide OBS-01 static logging constraints; do not absorb retained lifecycle/foundation/integration contracts. Apply D-06 and D-18-D-21. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>test ! -e tests/bridges/hooks/session-start-additional-context.test.ts &amp;&amp; node --test tests/bridges/hooks/event-router.test.ts &amp;&amp; node --test tests/architecture/hooks-dispatch.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/event-router.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; cache, drain, epoch, bucket, hydration, scope, reload, orphan, and registration partitions are independently asserted with cleanup; the additional-context suite is absent; hooks-dispatch contains only named OBS-01 static constraints and passes; retained cross-module suites remain; no test-only surface, shared router/root, impossible input, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-07 is complete and both assigned supplemental dispositions are final.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| scoped config/storage → live route registrations | Local declarations cross containment and lifecycle boundaries into executable handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-02 | Tampering / Disclosure | hydrateProjectScopeForCwd | high | mitigate | Preserve branded contained paths, disabled/invalid refusal, scope separation, and case-owned real roots. |
| T-112-03 | Denial of service / Elevation | reload orphan reap | high | mitigate | Preserve registry ownership proof before signals and verify reap order after shutdown. |
| T-112-05 | Information disclosure / Tampering | reload/session/router state | high | mitigate | Prove epoch reset, pending drain, scope isolation, exact handler registration, and complete case cleanup. |

No bespoke prohibition is minted; path, PID, and session isolation are canon and hard-gated above.
</threat_model>

## Artifacts this phase produces

- Refines `tests/bridges/hooks/event-router.test.ts` as the sole mirrored owner.
- Deletes `tests/bridges/hooks/session-start-additional-context.test.ts` after 06/07/25 absorption.
- Refines `tests/architecture/hooks-dispatch.test.ts` to OBS-01 static cross-module constraints only.
- Creates no production symbol and leaves `event-router.ts` unchanged.
- Creates `112-07-PLAN.md` and `112-07-SUMMARY.md` after execution.

<verification>
The owner/direct coverage, retained supplemental suite, and typecheck pass; the removed suite is absent; all lifecycle resources are clean.
</verification>

<success_criteria>
- P112-07 owns exactly one pair.
- Cache, hydration, routing, reload, orphan, pending-context, and registration behavior is complete.
- Supplemental migration is performed exactly once by this carrier.
- No high path/PID/session threat remains open.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-07-SUMMARY.md` when done.
</output>
