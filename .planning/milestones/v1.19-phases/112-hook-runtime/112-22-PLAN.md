---
phase: 112-hook-runtime
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/payloads/stop-failure.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-04: StopFailure cases own full inputs and whole envelope expectations with lowercase phases."
    - "D-03/D-22: positive type evidence and targeted negative checks remain at module scope while optional error_details is omitted or present exactly as specified."
    - "Billing, rate-limit, overload, auth, server, model, invalid, length, and unknown classification branches have explicit independently authored outcomes."
    - "Classifier ordering and bounded status-code partitions are observable without a production state reader or test mode."
  artifacts:
    - tests/bridges/hooks/payloads/stop-failure.test.ts
  key_links:
    - "StopFailure input/error -> classifier precedence -> exact error_details/output payload"
---

<objective>
Make the StopFailure translator and error classifier exhaustive at its public boundary.

Purpose: Lock optional-field semantics, classifier precedence, status bounds, and type contract (D-01, D-02, D-03, D-04, D-22).
Output: Refactored owner test; no production symbol is created.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Lock complete StopFailure envelopes and optional-field semantics</name>
  <files>tests/bridges/hooks/payloads/stop-failure.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts, tests/bridges/hooks/payloads/stop-failure.test.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/stop.ts, tests/bridges/hooks/payloads/stop.test.ts, .planning/phases/112-hook-runtime/112-CONTEXT.md, .planning/phases/112-hook-runtime/112-RESEARCH.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Per D-01, D-02, D-03, D-04, and D-22, rewrite cases with complete local StopFailure inputs and strict whole-envelope expectations. Prove the no-error path omits `error_details` rather than assigning `undefined`, and prove a representative classified error includes the exact production-defined detail object. Keep positive `satisfies` evidence and narrowly targeted `@ts-expect-error` checks at module scope; no runtime comment wraps type-only lines. Runtime cases use lowercase arrange/act/assert comments. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/stop-failure.test.ts</automated></verify>
  <acceptance_criteria>Absent/present error_details key sets are exact, base envelope fields are complete, module-scope positive/negative type checks compile, and focused tests pass without source edits.</acceptance_criteria>
  <done>The StopFailure envelope and optional property contract are proved end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Exhaust classifier precedence and boundary partitions</name>
  <files>tests/bridges/hooks/payloads/stop-failure.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts, tests/bridges/hooks/payloads/stop-failure.test.ts, .planning/phases/112-hook-runtime/112-VALIDATION.md, .claude/rules/typescript-unit-testing.md, .planning/phases/112-hook-runtime/112-PATTERNS.md</read_first>
  <action>Apply PATTERNS A3 (pure transform with an independently authored complete expectation). Create separately authored cases for billing, rate-limit, overload, authentication, server, model, invalid-request, length, and unknown classification outcomes named in the research matrix. Exercise overlapping indicators to prove the production precedence order, lower/upper in-range status values and just-outside values for bounded status classification, and exact mapping of a length outcome to `max_output_tokens`. Use whole expected outputs in every case, not a table loop or classifier-export seam. Leave Stop observer/re-entry behavior for plan 112-26 and leave the retained translator supplemental unchanged. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify><automated>node --test tests/bridges/hooks/payloads/stop-failure.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/payloads/stop-failure.test.ts</automated></verify>
  <acceptance_criteria>All nine classifier families, overlap precedence, status boundaries, length mapping, and unknown fallback are explicit; direct coverage passes with no production export, partial expectation, shared case state, or supplemental edit.</acceptance_criteria>
  <done>The StopFailure owner exhausts its complete classifier contract through public translation results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error input -> classified hook payload | Untrusted error text/status is reduced to a bounded, allowed error detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-112-06 | Tampering | StopFailure classifier/payload | high | mitigate | Exact precedence, bounded status partitions, optional-key semantics, and whole-envelope assertions constrain output to the declared classification whitelist. |
</threat_model>

## Artifacts this phase produces

- Modified owner test: `tests/bridges/hooks/payloads/stop-failure.test.ts`.
- New production files or symbols: none.
- Plan artifact: `.planning/phases/112-hook-runtime/112-22-PLAN.md`.

<verification>
Run the focused node test and direct coverage command; confirm classifier results are observed only through the public translator and the diff is owner-only.
</verification>

<success_criteria>
StopFailure has exact envelope, type, optional-property, precedence, boundary, and fallback coverage without exposing classifier internals.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-22-SUMMARY.md` when done.
</output>
