---
phase: 112-hook-runtime
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bridges/hooks/exec-timer.test.ts
autonomous: true
requirements: [MOD-05]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P112-09 re-proves timer behavior despite a green baseline and uses only the current test context's fake timers (D-01, D-14)."
    - "Signals are absent just before and present exactly at SIGTERM and five-second SIGKILL boundaries; cancellation suppresses both (D-14)."
    - "Zero, fractional, maximum delay, above-maximum clamp, negative/non-finite, exit, and repeated-cancel partitions are explicit (D-15, D-16)."
    - "Exactly two unreferenced handles are installed, exact handles are cleared idempotently, and no timer survives settlement (D-17)."
  artifacts:
    - path: tests/bridges/hooks/exec-timer.test.ts
      provides: "Canonical direct timer-ladder owner"
  key_links:
    - from: tests/bridges/hooks/exec-timer.test.ts
      to: extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
      via: "direct installTimerLadder calls with test-context timers"
      pattern: "installTimerLadder"
---

<objective>
Normalize the escalation timer owner with exact boundary, race, handle, and cleanup proof.

Purpose: MOD-05 requires deterministic hung-child termination without wall-clock waits or leaked handles.
Output: A complete direct timer owner; no production change.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hook-runtime/112-CONTEXT.md
@.planning/phases/112-hook-runtime/112-RESEARCH.md
@.planning/phases/112-hook-runtime/112-PATTERNS.md
@.planning/phases/112-hook-runtime/112-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove both escalation signals at exact deadlines</name>
  <files>tests/bridges/hooks/exec-timer.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
    - tests/bridges/hooks/exec-timer.test.ts
    - tests/platform/git-credential.test.ts
    - .planning/phases/112-hook-runtime/112-CONTEXT.md
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Normalize a current-TestContext timer case for timeout 1 second. Tick 999 ms and prove no signal, tick 1 ms and prove exactly SIGTERM, tick 4_999 ms and prove no additional signal, then tick 1 ms and prove exactly SIGKILL. Record the two handles, delays 1_000 and 5_000, unref calls, kill calls, clears, and final timer state as complete independent values. Register cancellation before acting. Apply D-01, D-02, D-04, D-05, D-14, and D-17. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/exec-timer.test.ts</automated>
  </verify>
  <acceptance_criteria>Just-before and exact deadline assertions discriminate early/late firing; signals, handles, delays, unref, clears, and cleanup are complete and case-owned; no real timer or process-wide mock is used.</acceptance_criteria>
  <done>The two-leg exact escalation schedule is directly proved.</done>
</task>

<task type="auto">
  <name>Task 2: Complete delay normalization, exit, cancellation, and late-race partitions</name>
  <files>tests/bridges/hooks/exec-timer.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
    - tests/bridges/hooks/exec-timer.test.ts
    - tests/platform/git-credential.test.ts
    - .planning/phases/112-hook-runtime/112-RESEARCH.md
    - .planning/phases/112-hook-runtime/112-VALIDATION.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>Add independent cases for zero, positive fractional ceiling, maximum representable delay, above-maximum clamping, negative, NaN, positive/negative infinity fail-open behavior; prior exit at SIGTERM by code and signal; exit between SIGTERM and SIGKILL; cancellation before either leg and between legs; repeated cancellation; late callbacks after finished child; and kill-return false where behavior differs. Assert exact two handles, both unref, exact clear identities, idempotence, and no remaining timers after every case. Data rows retain separate phases. Apply D-14-D-17 without relying on timeout.ts for expected values. Use `// arrange`, a blank line, `// act`, a blank line, and `// assert` in lowercase for every runtime case; use lowercase `// act &amp; assert` only for one `assert.throws()` or `assert.rejects()` expression, and keep type-only evidence at module scope.</action>
  <verify>
    <automated>node --test tests/bridges/hooks/exec-timer.test.ts &amp;&amp; npm run test:coverage:direct -- tests/bridges/hooks/exec-timer.test.ts</automated>
  </verify>
  <acceptance_criteria>The direct gate reports 100 percent functions, lines, and branches; every configured/invalid/max adjacency, exit/cancel race, handle/unref/clear, idempotence, and leak partition is explicit and independently expected; no real wait, shared timer, production delta, skip, or coverage exception exists.</acceptance_criteria>
  <done>P112-09 is normalized and complete for exact timer escalation.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1.

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| configured timeout/child state → OS signals | Scheduling decides when a potentially hung child is terminated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-112-04 | Denial of service | installTimerLadder | high | mitigate | Exact deadlines, clamping, exit/cancel races, unref, exact clears, idempotence, and zero leaked timers are direct assertions. |

No bespoke prohibition survives; hung-child safety is canon and fully represented here.
</threat_model>

## Artifacts this phase produces

- Refines `tests/bridges/hooks/exec-timer.test.ts` as the sole owner.
- Creates no production symbol and leaves `exec-timer.ts` unchanged.
- Creates `112-09-PLAN.md` and `112-09-SUMMARY.md` after execution.

<verification>
Focused node test and direct coverage pass at 100 percent with no live timer.
</verification>

<success_criteria>
- P112-09 owns exactly one pair.
- All exact timing, delay, race, handle, and cleanup truths are proved.
- Only test-context timers are used.
- No high-severity hung-child threat remains.
</success_criteria>

<output>
Create `.planning/phases/112-hook-runtime/112-09-SUMMARY.md` when done.
</output>
