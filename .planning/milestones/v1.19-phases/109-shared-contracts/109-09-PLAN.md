---
phase: 109-shared-contracts
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - "extensions/pi-claude-marketplace/shared/fs-utils.ts"
  - "tests/shared/fs-utils.test.ts"
autonomous: true
requirements:
  - MOD-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P109-09 directly imports and proves cleanupStaging; pathExists; removeOrphanIfPresent; RollbackReplacementLabels; RollbackReplacementInput; rollbackReplacementCommon; resolveGitSubdirRoot; readDirEntriesTolerant; isPlainMarkdownFile without changing the current public surface (D-01)."
    - "Cover missing and present paths, file/tree orphan modes, empty/single/multiple rollback inputs, equal-name items in stable reverse input order, contained/escaping/missing subdirectories one path step apart, `ENOENT`/`ENOTDIR` versus adjacent unexpected error codes, and mark numeric precision as not applicable."
    - "Every runtime case created or changed uses separate lowercase phase markers and case-owned mutable state; type-only evidence follows D-03 (D-02, D-03, D-04)."
    - "The owner alone reaches 100 percent direct function, line, and branch coverage for extensions/pi-claude-marketplace/shared/fs-utils.ts."
  artifacts:
    - path: "tests/shared/fs-utils.test.ts"
      provides: "Canonical mirrored owner for P109-09"
    - path: "extensions/pi-claude-marketplace/shared/fs-utils.ts"
      provides: "Existing public contract under direct owner coverage; no new public symbols"
  key_links:
    - from: "tests/shared/fs-utils.test.ts"
      to: "extensions/pi-claude-marketplace/shared/fs-utils.ts"
      via: "direct relative import of the paired source"
      pattern: "from .*fs-utils\\.ts"
    - from: "tests/shared/fs-utils.test.ts"
      to: "scripts/test-coverage-direct.mjs"
      via: "focused source-to-owner mapping"
      pattern: "test:coverage:direct"
---

<objective>
Complete P109-09 as one pair-atomic owner plan per D-01 through D-04: Lock filesystem cleanup, rollback, materialization, and filtering.

Purpose: Preserve the current caller-visible contract while replacing brownfield or missing evidence with exact public-value and side-effect proof.
Output: tests/shared/fs-utils.test.ts plus a behavior-preserving internal refactor in extensions/pi-claude-marketplace/shared/fs-utils.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shared-contracts/109-CONTEXT.md
@.planning/phases/109-shared-contracts/109-RESEARCH.md
@.planning/phases/109-shared-contracts/109-PATTERNS.md
@.planning/phases/109-shared-contracts/109-VALIDATION.md
@extensions/pi-claude-marketplace/shared/fs-utils.ts

</context>

<interfaces>
From `extensions/pi-claude-marketplace/shared/fs-utils.ts`:
- Public exports in scope: cleanupStaging; pathExists; removeOrphanIfPresent; RollbackReplacementLabels; RollbackReplacementInput; rollbackReplacementCommon; resolveGitSubdirRoot; readDirEntriesTolerant; isPlainMarkdownFile
- Public behavior to preserve: Normalize every owner case and cover each export. Replace permission-mode and `t.skip` branches with deterministic current-context filesystem method substitution. Refactor the source's named `node:fs/promises` imports to the default promise object and call `fs.lstat`, `fs.mkdir`, `fs.readdir`, `fs.rename`, `fs.rm`, and `fs.stat`; this keeps the public surface and behavior unchanged while allowing case-owned `t.mock.method` failures. Compare complete leak arrays, exact reverse rollback order, materialization variants, tolerant read behavior, and markdown decisions.
- Production callers must continue to compile and observe identical values and side effects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trace callers and establish the canonical P109-09 owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/fs-utils.ts - paired production source and public exports: cleanupStaging; pathExists; removeOrphanIfPresent; RollbackReplacementLabels; RollbackReplacementInput; rollbackReplacementCommon; resolveGitSubdirRoot; readDirEntriesTolerant; isPlainMarkdownFile
    - tests/shared/fs-utils.test.ts - current owner if present; otherwise confirm it is absent before creating it
    - tests/shared/atomic-json.test.ts:17-68 - mapped PATTERNS.md analog for fresh real temporary directories, case-owned cleanup, and exact filesystem values
    - .planning/phases/109-shared-contracts/109-CONTEXT.md - D-01 through D-04 and notification decisions when applicable
    - .planning/phases/109-shared-contracts/109-RESEARCH.md - pair matrix and edge evidence for P109-09
    - .planning/phases/109-shared-contracts/109-PATTERNS.md - assigned analog and pair boundary
    - .claude/rules/typescript-unit-testing.md - executable owner-test contract
    - extensions/pi-claude-marketplace/shared/path-safety.ts
  </read_first>
  <files>extensions/pi-claude-marketplace/shared/fs-utils.ts, tests/shared/fs-utils.test.ts</files>
  <action>Before any edit, run CodeGraph for `extensions/pi-claude-marketplace/shared/fs-utils.ts` and the listed exports, trace production callers, and record the caller-facing invariants in the execution notes and final summary. Run the current owner and focused coverage when the owner exists. Directly import the paired source from `tests/shared/fs-utils.test.ts`. Apply the real-filesystem ownership pattern from `tests/shared/atomic-json.test.ts:17-68`: create a fresh temporary directory in each filesystem case, register removal before mutation, and assert complete paths, arrays, errors, and bytes. Normalize every owner case and cover each export. Replace permission-mode and `t.skip` branches with deterministic current-context filesystem method substitution. Refactor the source's named `node:fs/promises` imports to the default promise object and call `fs.lstat`, `fs.mkdir`, `fs.readdir`, `fs.rename`, `fs.rm`, and `fs.stat`; this keeps the public surface and behavior unchanged while allowing case-owned `t.mock.method` failures. Compare complete leak arrays, exact reverse rollback order, materialization variants, tolerant read behavior, and markdown decisions. The named internal source refactor is allowed by the agent's discretion because it exposes no test surface and preserves every caller-visible value. Per D-02, every runtime case added or changed uses exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; only one throwing or rejection expression may use lowercase `// act & assert`, and data rows always keep separate phases. This pair needs runtime evidence only; do not invent type-only ceremony. Per D-04, create fresh state and register cleanup before mutation. Do not add a public export, reset hook, state reader, test mode, coverage exemption, generic helper, or unrelated source edit.</action>
  <verify>
    <automated>node --test tests/shared/fs-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>The canonical owner directly imports `extensions/pi-claude-marketplace/shared/fs-utils.ts`, its core public behavior passes alone, the caller trace is recorded, and no unrelated file changed.</acceptance_criteria>
  <done>P109-09 has a production-quality owner path through the paired source and its core contract.</done>
</task>

<task type="auto">
  <name>Task 2: Complete exact edge coverage and pair-local quality gates</name>
  <read_first>
    - tests/shared/fs-utils.test.ts - Task 1 owner state and case names
    - extensions/pi-claude-marketplace/shared/fs-utils.ts - branch and export inventory
    - .planning/phases/109-shared-contracts/109-VALIDATION.md - P109-09 command and secure behavior
    - .claude/rules/typescript-comments.md - durable source and test-title wording
  </read_first>
  <files>extensions/pi-claude-marketplace/shared/fs-utils.ts, tests/shared/fs-utils.test.ts</files>
  <action>Expand the owner only where a distinct public contract remains. Cover missing and present paths, file/tree orphan modes, empty/single/multiple rollback inputs, equal-name items in stable reverse input order, contained/escaping/missing subdirectories one path step apart, `ENOENT`/`ENOTDIR` versus adjacent unexpected error codes, and mark numeric precision as not applicable. Compare whole public values, structured errors, arrays, side effects, or exact bytes to expectations authored independently of production constants and formatters. Keep data rows as sibling `test()` cases with no conditional branch in the loop body. Remove migration or work-session commentary from the changed owner. The named internal source refactor is allowed by the agent's discretion because it exposes no test surface and preserves every caller-visible value. Confirm the public export surface is unchanged and that all callers identified in Task 1 still typecheck.</action>
  <verify>
    <automated>node --test tests/shared/fs-utils.test.ts
npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/fs-utils.ts
npm run typecheck
npx eslint extensions/pi-claude-marketplace/shared/fs-utils.ts tests/shared/fs-utils.test.ts
npx prettier --check extensions/pi-claude-marketplace/shared/fs-utils.ts tests/shared/fs-utils.test.ts
git diff --check</automated>
  </verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision rows are explicitly resolved; every applicable edge has an independently discriminating case; the owner reaches 100 percent direct functions, lines, and branches; lint, formatting, type checks when relevant, and diff checks pass.</acceptance_criteria>
  <done>Lock filesystem cleanup, rollback, materialization, and filtering; public behavior and surface remain unchanged, and P109-09 is ready to commit as one source-owner pair.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                           | Description                                                                                                                   |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Production caller to shared export | Caller input crosses into `extensions/pi-claude-marketplace/shared/fs-utils.ts`; tests preserve the accepted public contract. |
| Owner test to runtime boundary     | The test controls any filesystem, environment, cache, console, notification, or type-checking state it exercises per D-04.    |
| External API or package install    | N/A. P109-09 adds no SDK, service integration, dependency, or package-manager operation.                                      |

## STRIDE threat register

**Security policy:** ASVS L1; block completion on any unmitigated high-severity threat.

| Threat ID       | Category                      | Component                                | Severity | Disposition | Mitigation plan                                                                                                                                          |
| --------------- | ----------------------------- | ---------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-109-09        | Tampering / Denial of service | filesystem cleanup and rollback boundary | medium   | mitigate    | Use real case-owned trees for normal effects and current-context method failures for deterministic cleanup, rollback, and tolerant-read error contracts. |
| T-109-09-NA     | Spoofing                      | External identity boundary               | low      | accept      | N/A for this pair because it performs no authentication or external identity decision.                                                                   |
| </threat_model> |

<verification>
- Run `node --test tests/shared/fs-utils.test.ts`.
- Run `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/fs-utils.ts` and require 100 percent functions, lines, and branches.
- Run pair-local ESLint, formatting, type checks when relevant, and `git diff --check`.
- Confirm no test-only public surface, generic helper, coverage ignore, or unrelated legacy-suite edit was introduced.
</verification>

<success_criteria>

1. P109-09 satisfies MOD-02 for its one mirrored source-owner pair.
2. Its exact public values and side effects include defensible boundary, adjacency, empty, ordering, and precision outcomes.
3. D-01 through D-04 are visible in the implementation and automated evidence.
4. The owner and focused direct coverage pass without external services, developer state, or live network access.
   </success_criteria>

## Artifacts this phase produces

- `tests/shared/fs-utils.test.ts`: canonical mirrored owner for `extensions/pi-claude-marketplace/shared/fs-utils.ts`.
- Production symbols created: none. The source receives only the named private dependency-access refactor; its exports and signatures remain identical.
- `.planning/phases/109-shared-contracts/109-09-SUMMARY.md`: execution evidence, caller trace, exact commands, and pair commit record.

<output>
Create `.planning/phases/109-shared-contracts/109-09-SUMMARY.md` when done.
</output>
