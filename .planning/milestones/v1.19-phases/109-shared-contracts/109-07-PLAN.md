---
phase: 109-shared-contracts
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/shared/errors.test.ts"
autonomous: true
requirements:
  - MOD-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P109-07 directly imports and proves errorMessage; isErrnoException; assertNever; causeChainTrailer; composeErrorWithCauseChain; appendLeakToError; appendLeaks; StaleSourceCloneError; MarketplaceDuplicateNameError; MarketplaceNotFoundError; MarketplaceUpdateError; InvalidMarketplaceManifestError; UnsupportedSourceError; CrossPluginConflictError; ConcurrentInstallError; ConcurrentUninstallError; StateLockHeldError; Phase3Failure; PluginUpdatePhase3Error; ManualRecoveryError; errorWithManualRecovery; findManualRecoveryError; manualRecoveryLeaks; PluginShapeErrorShape; PluginShapeErrorKind; PluginShapeError; ResourcesDiscoverFailure; AggregateResourcesDiscoverError without changing the current public surface (D-01)."
    - "Resolve the cause-chain boundary at zero links, one link, the exact maximum depth, one link beyond, and self/cyclic causes; cover empty, duplicate, single, and several leaks/failures; preserve first-seen order for equal/duplicate leak text; cover every adjacent discriminator arm; and mark numeric precision as not applicable outside the exact depth count."
    - "Every runtime case created or changed uses separate lowercase phase markers and case-owned mutable state; type-only evidence follows D-03 (D-02, D-03, D-04)."
    - "The owner alone reaches 100 percent direct function, line, and branch coverage for extensions/pi-claude-marketplace/shared/errors.ts."
  artifacts:
    - path: "tests/shared/errors.test.ts"
      provides: "Canonical mirrored owner for P109-07"
    - path: "extensions/pi-claude-marketplace/shared/errors.ts"
      provides: "Existing public contract under direct owner coverage; no new public symbols"
  key_links:
    - from: "tests/shared/errors.test.ts"
      to: "extensions/pi-claude-marketplace/shared/errors.ts"
      via: "direct relative import of the paired source"
      pattern: "from .*errors\\.ts"
    - from: "tests/shared/errors.test.ts"
      to: "scripts/test-coverage-direct.mjs"
      via: "focused source-to-owner mapping"
      pattern: "test:coverage:direct"
---

<objective>
Complete P109-07 as one pair-atomic owner plan per D-01 through D-04: Lock all shared error helpers, classes, and discriminants.

Purpose: Preserve the current caller-visible contract while replacing brownfield or missing evidence with exact public-value and side-effect proof.
Output: tests/shared/errors.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shared-contracts/109-CONTEXT.md
@.planning/phases/109-shared-contracts/109-RESEARCH.md
@.planning/phases/109-shared-contracts/109-PATTERNS.md
@.planning/phases/109-shared-contracts/109-VALIDATION.md
@extensions/pi-claude-marketplace/shared/errors.ts

</context>

<interfaces>
From `extensions/pi-claude-marketplace/shared/errors.ts`:
- Public exports in scope: errorMessage; isErrnoException; assertNever; causeChainTrailer; composeErrorWithCauseChain; appendLeakToError; appendLeaks; StaleSourceCloneError; MarketplaceDuplicateNameError; MarketplaceNotFoundError; MarketplaceUpdateError; InvalidMarketplaceManifestError; UnsupportedSourceError; CrossPluginConflictError; ConcurrentInstallError; ConcurrentUninstallError; StateLockHeldError; Phase3Failure; PluginUpdatePhase3Error; ManualRecoveryError; errorWithManualRecovery; findManualRecoveryError; manualRecoveryLeaks; PluginShapeErrorShape; PluginShapeErrorKind; PluginShapeError; ResourcesDiscoverFailure; AggregateResourcesDiscoverError
- Public behavior to preserve: Normalize the owner and enumerate every exported helper, error class, and `PluginShapeErrorShape` arm before implementation. Compare exact names, complete messages, stable fields, causes, trailers, discriminants, frozen copies, and defensive aliasing. Use module-scope type evidence for closed discriminants where it strengthens runtime rows.
- Production callers must continue to compile and observe identical values and side effects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trace callers and establish the canonical P109-07 owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/errors.ts - paired production source and public exports: errorMessage; isErrnoException; assertNever; causeChainTrailer; composeErrorWithCauseChain; appendLeakToError; appendLeaks; StaleSourceCloneError; MarketplaceDuplicateNameError; MarketplaceNotFoundError; MarketplaceUpdateError; InvalidMarketplaceManifestError; UnsupportedSourceError; CrossPluginConflictError; ConcurrentInstallError; ConcurrentUninstallError; StateLockHeldError; Phase3Failure; PluginUpdatePhase3Error; ManualRecoveryError; errorWithManualRecovery; findManualRecoveryError; manualRecoveryLeaks; PluginShapeErrorShape; PluginShapeErrorKind; PluginShapeError; ResourcesDiscoverFailure; AggregateResourcesDiscoverError
    - tests/shared/errors.test.ts - current owner if present; otherwise confirm it is absent before creating it
    - tests/shared/errors-bridges.test.ts:20-83 - mapped PATTERNS.md analog for complete independently authored error values, fields, and messages
    - .planning/phases/109-shared-contracts/109-CONTEXT.md - D-01 through D-04 and notification decisions when applicable
    - .planning/phases/109-shared-contracts/109-RESEARCH.md - pair matrix and edge evidence for P109-07
    - .planning/phases/109-shared-contracts/109-PATTERNS.md - assigned analog and pair boundary
    - .claude/rules/typescript-unit-testing.md - executable owner-test contract
    - docs/messaging-style-guide.md
  </read_first>
  <files>tests/shared/errors.test.ts</files>
  <action>Before any edit, run CodeGraph for `extensions/pi-claude-marketplace/shared/errors.ts` and the listed exports, trace production callers, and record the caller-facing invariants in the execution notes and final summary. Run the current owner and focused coverage when the owner exists. Directly import the paired source from `tests/shared/errors.test.ts`. Apply the mapped `tests/shared/errors-bridges.test.ts:20-83` pattern: construct each error through its public API, project every stable public field into one whole value, and compare it with an independently authored complete expected value and message. Normalize the owner and enumerate every exported helper, error class, and `PluginShapeErrorShape` arm before implementation. Compare exact names, complete messages, stable fields, causes, trailers, discriminants, frozen copies, and defensive aliasing. Use module-scope type evidence for closed discriminants where it strengthens runtime rows. Keep the production source byte-identical; all evidence must use its current public exports. Per D-02, every runtime case added or changed uses exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; only one throwing or rejection expression may use lowercase `// act & assert`, and data rows always keep separate phases. For compile-time evidence, use only module-scope `satisfies` expressions and targeted `@ts-expect-error` negatives with no runtime wrapper or phase comments per D-03. Per D-04, create fresh state and register cleanup before mutation. Do not add a public export, reset hook, state reader, test mode, coverage exemption, generic helper, or unrelated source edit.</action>
  <verify>
    <automated>node --test tests/shared/errors.test.ts
npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The canonical owner directly imports `extensions/pi-claude-marketplace/shared/errors.ts`, its core public behavior passes alone, the caller trace is recorded, and no unrelated file changed.</acceptance_criteria>
  <done>P109-07 has a production-quality owner path through the paired source and its core contract.</done>
</task>

<task type="auto">
  <name>Task 2: Complete exact edge coverage and pair-local quality gates</name>
  <read_first>
    - tests/shared/errors.test.ts - Task 1 owner state and case names
    - extensions/pi-claude-marketplace/shared/errors.ts - branch and export inventory
    - .planning/phases/109-shared-contracts/109-VALIDATION.md - P109-07 command and secure behavior
    - .claude/rules/typescript-comments.md - durable source and test-title wording
  </read_first>
  <files>tests/shared/errors.test.ts</files>
  <action>Expand the owner only where a distinct public contract remains. Resolve the cause-chain boundary at zero links, one link, the exact maximum depth, one link beyond, and self/cyclic causes; cover empty, duplicate, single, and several leaks/failures; preserve first-seen order for equal/duplicate leak text; cover every adjacent discriminator arm; and mark numeric precision as not applicable outside the exact depth count. Compare whole public values, structured errors, arrays, side effects, or exact bytes to expectations authored independently of production constants and formatters. Keep data rows as sibling `test()` cases with no conditional branch in the loop body. Remove migration or work-session commentary from the changed owner. Keep the production source byte-identical; all evidence must use its current public exports. Confirm the public export surface is unchanged and that all callers identified in Task 1 still typecheck.</action>
  <verify>
    <automated>node --test tests/shared/errors.test.ts
npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/errors.ts
npm run typecheck
npx eslint extensions/pi-claude-marketplace/shared/errors.ts tests/shared/errors.test.ts
npx prettier --check extensions/pi-claude-marketplace/shared/errors.ts tests/shared/errors.test.ts
git diff --check
git diff --exit-code -- extensions/pi-claude-marketplace/shared/errors.ts</automated>
  </verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision rows are explicitly resolved; every applicable edge has an independently discriminating case; the owner reaches 100 percent direct functions, lines, and branches; lint, formatting, type checks when relevant, and diff checks pass.</acceptance_criteria>
  <done>Lock all shared error helpers, classes, and discriminants; public behavior and surface remain unchanged, and P109-07 is ready to commit as one source-owner pair.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                           | Description                                                                                                                 |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Production caller to shared export | Caller input crosses into `extensions/pi-claude-marketplace/shared/errors.ts`; tests preserve the accepted public contract. |
| Owner test to runtime boundary     | The test controls any filesystem, environment, cache, console, notification, or type-checking state it exercises per D-04.  |
| External API or package install    | N/A. P109-07 adds no SDK, service integration, dependency, or package-manager operation.                                    |

## STRIDE threat register

**Security policy:** ASVS L1; block completion on any unmitigated high-severity threat.

| Threat ID       | Category                                   | Component                               | Severity | Disposition | Mitigation plan                                                                                                                              |
| --------------- | ------------------------------------------ | --------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-109-07        | Information disclosure / Denial of service | shared error and cause-chain projection | medium   | mitigate    | Pin bounded cause traversal, cycle termination, complete stable fields, exact discriminants, and leak de-duplication through public exports. |
| T-109-07-NA     | Spoofing                                   | External identity boundary              | low      | accept      | N/A for this pair because it performs no authentication or external identity decision.                                                       |
| </threat_model> |

<verification>
- Run `node --test tests/shared/errors.test.ts`.
- Run `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/errors.ts` and require 100 percent functions, lines, and branches.
- Run pair-local ESLint, formatting, type checks when relevant, and `git diff --check`.
- Confirm no test-only public surface, generic helper, coverage ignore, or unrelated legacy-suite edit was introduced.
</verification>

<success_criteria>

1. P109-07 satisfies MOD-02 for its one mirrored source-owner pair.
2. Its exact public values and side effects include defensible boundary, adjacency, empty, ordering, and precision outcomes.
3. D-01 through D-04 are visible in the implementation and automated evidence.
4. The owner and focused direct coverage pass without external services, developer state, or live network access.
   </success_criteria>

## Artifacts this phase produces

- `tests/shared/errors.test.ts`: canonical mirrored owner for `extensions/pi-claude-marketplace/shared/errors.ts`.
- Production symbols created: none. The production source remains byte-identical.
- `.planning/phases/109-shared-contracts/109-07-SUMMARY.md`: execution evidence, caller trace, exact commands, and pair commit record.

<output>
Create `.planning/phases/109-shared-contracts/109-07-SUMMARY.md` when done.
</output>
