---
phase: 109-shared-contracts
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/shared/session-env.test.ts"
autonomous: true
requirements:
  - MOD-02
estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P109-17 directly imports and proves claudeSessionEnvFor; applySessionEnv; PATH_LEDGER_ENV; applyPathLedger without changing the current public surface (D-01)."
    - "Cover empty PATH/ledger/fresh lists, one and several entries, duplicate equal entries, relative and absolute ledger adjacency, platform delimiter handling, repeated fresh entries, stable non-owned order, and exact string behavior with no numeric precision concerns."
    - "Every runtime case created or changed uses separate lowercase phase markers and case-owned mutable state; type-only evidence follows D-03 (D-02, D-03, D-04)."
    - "The owner alone reaches 100 percent direct function, line, and branch coverage for extensions/pi-claude-marketplace/shared/session-env.ts."
  artifacts:
    - path: "tests/shared/session-env.test.ts"
      provides: "Canonical mirrored owner for P109-17"
    - path: "extensions/pi-claude-marketplace/shared/session-env.ts"
      provides: "Existing public contract under direct owner coverage; no new public symbols"
  key_links:
    - from: "tests/shared/session-env.test.ts"
      to: "extensions/pi-claude-marketplace/shared/session-env.ts"
      via: "direct relative import of the paired source"
      pattern: "from .*session-env\\.ts"
    - from: "tests/shared/session-env.test.ts"
      to: "scripts/test-coverage-direct.mjs"
      via: "focused source-to-owner mapping"
      pattern: "test:coverage:direct"
---

<objective>
Complete P109-17 as one pair-atomic owner plan per D-01 through D-04: Lock session projection and PATH ledger ownership.

Purpose: Preserve the current caller-visible contract while replacing brownfield or missing evidence with exact public-value and side-effect proof.
Output: tests/shared/session-env.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shared-contracts/109-CONTEXT.md
@.planning/phases/109-shared-contracts/109-RESEARCH.md
@.planning/phases/109-shared-contracts/109-PATTERNS.md
@.planning/phases/109-shared-contracts/109-VALIDATION.md
@extensions/pi-claude-marketplace/shared/session-env.ts

</context>

<interfaces>
From `extensions/pi-claude-marketplace/shared/session-env.ts`:
- Public exports in scope: claudeSessionEnvFor; applySessionEnv; PATH_LEDGER_ENV; applyPathLedger
- Public behavior to preserve: Normalize existing session cases and add complete `applyPathLedger` matrices. Compare the exact session triple, unconditional refresh, non-interference with unrelated environment keys, the exact ledger key, owned-entry removal, fresh-entry append, and tamper resistance. Any live `process.env` case registers restoration for every touched key before mutation.
- Production callers must continue to compile and observe identical values and side effects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trace callers and establish the canonical P109-17 owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/session-env.ts - paired production source and public exports: claudeSessionEnvFor; applySessionEnv; PATH_LEDGER_ENV; applyPathLedger
    - tests/shared/session-env.test.ts - current owner if present; otherwise confirm it is absent before creating it
    - tests/shared/debug-log.test.ts:19-52 - mapped PATTERNS.md analog for saving and restoring process state in the same case before mutation
    - .planning/phases/109-shared-contracts/109-CONTEXT.md - D-01 through D-04 and notification decisions when applicable
    - .planning/phases/109-shared-contracts/109-RESEARCH.md - pair matrix and edge evidence for P109-17
    - .planning/phases/109-shared-contracts/109-PATTERNS.md - assigned analog and pair boundary
    - .claude/rules/typescript-unit-testing.md - executable owner-test contract
    - docs/env-vars.md
    - extensions/pi-claude-marketplace/shared/debug-log.ts
  </read_first>
  <files>tests/shared/session-env.test.ts</files>
  <action>Before any edit, run CodeGraph for `extensions/pi-claude-marketplace/shared/session-env.ts` and the listed exports, trace production callers, and record the caller-facing invariants in the execution notes and final summary. Run the current owner and focused coverage when the owner exists. Directly import the paired source from `tests/shared/session-env.test.ts`. Apply the state-lifecycle pattern from `tests/shared/debug-log.test.ts:19-52`: capture every touched environment value and register same-case restoration before the first mutation. Normalize existing session cases and add complete `applyPathLedger` matrices. Compare the exact session triple, unconditional refresh, non-interference with unrelated environment keys, the exact ledger key, owned-entry removal, fresh-entry append, and tamper resistance. Any live `process.env` case registers restoration for every touched key before mutation. Keep the production source byte-identical; all evidence must use its current public exports. Per D-02, every runtime case added or changed uses exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; only one throwing or rejection expression may use lowercase `// act & assert`, and data rows always keep separate phases. This pair needs runtime evidence only; do not invent type-only ceremony. Per D-04, create fresh state and register cleanup before mutation. Do not add a public export, reset hook, state reader, test mode, coverage exemption, generic helper, or unrelated source edit.</action>
  <verify>
    <automated>node --test tests/shared/session-env.test.ts</automated>
  </verify>
  <acceptance_criteria>The canonical owner directly imports `extensions/pi-claude-marketplace/shared/session-env.ts`, its core public behavior passes alone, the caller trace is recorded, and no unrelated file changed.</acceptance_criteria>
  <done>P109-17 has a production-quality owner path through the paired source and its core contract.</done>
</task>

<task type="auto">
  <name>Task 2: Complete exact edge coverage and pair-local quality gates</name>
  <read_first>
    - tests/shared/session-env.test.ts - Task 1 owner state and case names
    - extensions/pi-claude-marketplace/shared/session-env.ts - branch and export inventory
    - .planning/phases/109-shared-contracts/109-VALIDATION.md - P109-17 command and secure behavior
    - .claude/rules/typescript-comments.md - durable source and test-title wording
  </read_first>
  <files>tests/shared/session-env.test.ts</files>
  <action>Expand the owner only where a distinct public contract remains. Cover empty PATH/ledger/fresh lists, one and several entries, duplicate equal entries, relative and absolute ledger adjacency, platform delimiter handling, repeated fresh entries, stable non-owned order, and exact string behavior with no numeric precision concerns. Compare whole public values, structured errors, arrays, side effects, or exact bytes to expectations authored independently of production constants and formatters. Keep data rows as sibling `test()` cases with no conditional branch in the loop body. Remove migration or work-session commentary from the changed owner. Keep the production source byte-identical; all evidence must use its current public exports. Confirm the public export surface is unchanged and that all callers identified in Task 1 still typecheck.</action>
  <verify>
    <automated>node --test tests/shared/session-env.test.ts
npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/session-env.ts
npx eslint extensions/pi-claude-marketplace/shared/session-env.ts tests/shared/session-env.test.ts
npx prettier --check extensions/pi-claude-marketplace/shared/session-env.ts tests/shared/session-env.test.ts
git diff --check
git diff --exit-code -- extensions/pi-claude-marketplace/shared/session-env.ts</automated>
  </verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision rows are explicitly resolved; every applicable edge has an independently discriminating case; the exact owned session keys, unrelated-key preservation, absolute-ledger filtering, fresh-entry de-duplication, and same-case process restoration all pass; the owner reaches 100 percent direct functions, lines, and branches; lint, formatting, type checks when relevant, and diff checks pass. Under ASVS L1, any failed high-severity environment mitigation blocks completion.</acceptance_criteria>
  <done>Lock session projection and PATH ledger ownership; public behavior and surface remain unchanged, and P109-17 is ready to commit as one source-owner pair.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                           | Description                                                                                                                      |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Production caller to shared export | Caller input crosses into `extensions/pi-claude-marketplace/shared/session-env.ts`; tests preserve the accepted public contract. |
| Owner test to runtime boundary     | The test controls any filesystem, environment, cache, console, notification, or type-checking state it exercises per D-04.       |
| External API or package install    | N/A. P109-17 adds no SDK, service integration, dependency, or package-manager operation.                                         |

## STRIDE threat register

**Security policy:** ASVS L1; block completion on any unmitigated high-severity threat.

| Threat ID       | Category                           | Component                                    | Severity | Disposition | Mitigation plan                                                                                                                                                 |
| --------------- | ---------------------------------- | -------------------------------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-109-17        | Tampering / Information disclosure | process environment and PATH ledger boundary | high     | mitigate    | Pin the three owned session keys, preserve unrelated keys, accept only absolute ledger entries, de-duplicate fresh entries, and restore process state per case. |
| T-109-17-NA     | Spoofing                           | External identity boundary                   | low      | accept      | N/A for this pair because it performs no authentication or external identity decision.                                                                          |
| </threat_model> |

<verification>
- Run `node --test tests/shared/session-env.test.ts`.
- Run `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/session-env.ts` and require 100 percent functions, lines, and branches.
- Under ASVS L1, treat any failure in owned-key isolation, absolute-ledger filtering, de-duplication, or process restoration evidence as a completion blocker.
- Run pair-local ESLint, formatting, type checks when relevant, and `git diff --check`.
- Confirm no test-only public surface, generic helper, coverage ignore, or unrelated legacy-suite edit was introduced.
</verification>

<success_criteria>

1. P109-17 satisfies MOD-02 for its one mirrored source-owner pair.
2. Its exact public values and side effects include defensible boundary, adjacency, empty, ordering, and precision outcomes.
3. D-01 through D-04 are visible in the implementation and automated evidence.
4. The owner and focused direct coverage pass without external services, developer state, or live network access.
   </success_criteria>

## Artifacts this phase produces

- `tests/shared/session-env.test.ts`: canonical mirrored owner for `extensions/pi-claude-marketplace/shared/session-env.ts`.
- Production symbols created: none. The production source remains byte-identical.
- `.planning/phases/109-shared-contracts/109-17-SUMMARY.md`: execution evidence, caller trace, exact commands, and pair commit record.

<output>
Create `.planning/phases/109-shared-contracts/109-17-SUMMARY.md` when done.
</output>
