---
phase: 109-shared-contracts
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/shared/debug-log.test.ts"
autonomous: true
requirements:
  - MOD-02
estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P109-05 directly imports and proves hookDebugLog without changing the current public surface (D-01)."
    - "Resolve adjacency around the exact gate with undefined, empty, `0`, `true`, whitespace-padded `1`, and exact `1`; cover an empty detail string and a one-character detail; preserve tag-before-detail output order; and mark numeric precision as not applicable."
    - "Every runtime case created or changed uses separate lowercase phase markers and case-owned mutable state; type-only evidence follows D-03 (D-02, D-03, D-04)."
    - "The owner alone reaches 100 percent direct function, line, and branch coverage for extensions/pi-claude-marketplace/shared/debug-log.ts."
  artifacts:
    - path: "tests/shared/debug-log.test.ts"
      provides: "Canonical mirrored owner for P109-05"
    - path: "extensions/pi-claude-marketplace/shared/debug-log.ts"
      provides: "Existing public contract under direct owner coverage; no new public symbols"
  key_links:
    - from: "tests/shared/debug-log.test.ts"
      to: "extensions/pi-claude-marketplace/shared/debug-log.ts"
      via: "direct relative import of the paired source"
      pattern: "from .*debug-log\\.ts"
    - from: "tests/shared/debug-log.test.ts"
      to: "scripts/test-coverage-direct.mjs"
      via: "focused source-to-owner mapping"
      pattern: "test:coverage:direct"
---

<objective>
Complete P109-05 as one pair-atomic owner plan per D-01 through D-04: Lock the exact debug gate and console bytes.

Purpose: Preserve the current caller-visible contract while replacing brownfield or missing evidence with exact public-value and side-effect proof.
Output: tests/shared/debug-log.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shared-contracts/109-CONTEXT.md
@.planning/phases/109-shared-contracts/109-RESEARCH.md
@.planning/phases/109-shared-contracts/109-PATTERNS.md
@.planning/phases/109-shared-contracts/109-VALIDATION.md
@extensions/pi-claude-marketplace/shared/debug-log.ts

</context>

<interfaces>
From `extensions/pi-claude-marketplace/shared/debug-log.ts`:
- Public exports in scope: hookDebugLog
- Public behavior to preserve: Normalize the accepted owner per D-01. For each case, save and register restoration of `PI_CLAUDE_MARKETPLACE_DEBUG` before mutation, use the current test context's `t.mock.method(console, "error")`, and prove the default `hooks` tag, a custom tag, and exact emitted argument bytes.
- Production callers must continue to compile and observe identical values and side effects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trace callers and establish the canonical P109-05 owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/debug-log.ts - paired production source and public exports: hookDebugLog
    - tests/shared/debug-log.test.ts - current owner if present; otherwise confirm it is absent before creating it
    - .planning/phases/109-shared-contracts/109-CONTEXT.md - D-01 through D-04 and notification decisions when applicable
    - .planning/phases/109-shared-contracts/109-RESEARCH.md - pair matrix and edge evidence for P109-05
    - .planning/phases/109-shared-contracts/109-PATTERNS.md - assigned analog and pair boundary
    - .claude/rules/typescript-unit-testing.md - executable owner-test contract
    - docs/env-vars.md
  </read_first>
  <files>tests/shared/debug-log.test.ts</files>
  <action>Before any edit, run CodeGraph for `extensions/pi-claude-marketplace/shared/debug-log.ts` and the listed exports, trace production callers, and record the caller-facing invariants in the execution notes and final summary. Run the current owner and focused coverage when the owner exists. Directly import the paired source from `tests/shared/debug-log.test.ts`. Normalize the accepted owner per D-01. For each case, save and register restoration of `PI_CLAUDE_MARKETPLACE_DEBUG` before mutation, use the current test context's `t.mock.method(console, "error")`, and prove the default `hooks` tag, a custom tag, and exact emitted argument bytes. Keep the production source byte-identical; all evidence must use its current public exports. Per D-02, every runtime case added or changed uses exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; only one throwing or rejection expression may use lowercase `// act & assert`, and data rows always keep separate phases. This pair needs runtime evidence only; do not invent type-only ceremony. Per D-04, create fresh state and register cleanup before mutation. Do not add a public export, reset hook, state reader, test mode, coverage exemption, generic helper, or unrelated source edit.</action>
  <verify>
    <automated>node --test tests/shared/debug-log.test.ts</automated>
  </verify>
  <acceptance_criteria>The canonical owner directly imports `extensions/pi-claude-marketplace/shared/debug-log.ts`, its core public behavior passes alone, the caller trace is recorded, and no unrelated file changed.</acceptance_criteria>
  <done>P109-05 has a production-quality owner path through the paired source and its core contract.</done>
</task>

<task type="auto">
  <name>Task 2: Complete exact edge coverage and pair-local quality gates</name>
  <read_first>
    - tests/shared/debug-log.test.ts - Task 1 owner state and case names
    - extensions/pi-claude-marketplace/shared/debug-log.ts - branch and export inventory
    - .planning/phases/109-shared-contracts/109-VALIDATION.md - P109-05 command and secure behavior
    - .claude/rules/typescript-comments.md - durable source and test-title wording
  </read_first>
  <files>tests/shared/debug-log.test.ts</files>
  <action>Expand the owner only where a distinct public contract remains. Resolve adjacency around the exact gate with undefined, empty, `0`, `true`, whitespace-padded `1`, and exact `1`; cover an empty detail string and a one-character detail; preserve tag-before-detail output order; and mark numeric precision as not applicable. Compare whole public values, structured errors, arrays, side effects, or exact bytes to expectations authored independently of production constants and formatters. Keep data rows as sibling `test()` cases with no conditional branch in the loop body. Remove migration or work-session commentary from the changed owner. Keep the production source byte-identical; all evidence must use its current public exports. Confirm the public export surface is unchanged and that all callers identified in Task 1 still typecheck.</action>
  <verify>
    <automated>node --test tests/shared/debug-log.test.ts
npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/debug-log.ts
npx eslint extensions/pi-claude-marketplace/shared/debug-log.ts tests/shared/debug-log.test.ts
npx prettier --check extensions/pi-claude-marketplace/shared/debug-log.ts tests/shared/debug-log.test.ts
git diff --check
git diff --exit-code -- extensions/pi-claude-marketplace/shared/debug-log.ts</automated>
  </verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision rows are explicitly resolved; every applicable edge has an independently discriminating case; the owner reaches 100 percent direct functions, lines, and branches; lint, formatting, type checks when relevant, and diff checks pass.</acceptance_criteria>
  <done>Lock the exact debug gate and console bytes; public behavior and surface remain unchanged, and P109-05 is ready to commit as one source-owner pair.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                           | Description                                                                                                                    |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Production caller to shared export | Caller input crosses into `extensions/pi-claude-marketplace/shared/debug-log.ts`; tests preserve the accepted public contract. |
| Owner test to runtime boundary     | The test controls any filesystem, environment, cache, console, notification, or type-checking state it exercises per D-04.     |
| External API or package install    | N/A. P109-05 adds no SDK, service integration, dependency, or package-manager operation.                                       |

## STRIDE threat register

**Security policy:** ASVS L1; block completion on any unmitigated high-severity threat.

| Threat ID       | Category               | Component                        | Severity | Disposition | Mitigation plan                                                                                                              |
| --------------- | ---------------------- | -------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-109-05        | Information disclosure | environment-gated console output | medium   | mitigate    | Prove every near-match gate is silent, the exact gate emits once, and each case restores the environment and console method. |
| T-109-05-NA     | Spoofing               | External identity boundary       | low      | accept      | N/A for this pair because it performs no authentication or external identity decision.                                       |
| </threat_model> |

<verification>
- Run `node --test tests/shared/debug-log.test.ts`.
- Run `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/debug-log.ts` and require 100 percent functions, lines, and branches.
- Run pair-local ESLint, formatting, type checks when relevant, and `git diff --check`.
- Confirm no test-only public surface, generic helper, coverage ignore, or unrelated legacy-suite edit was introduced.
</verification>

<success_criteria>

1. P109-05 satisfies MOD-02 for its one mirrored source-owner pair.
2. Its exact public values and side effects include defensible boundary, adjacency, empty, ordering, and precision outcomes.
3. D-01 through D-04 are visible in the implementation and automated evidence.
4. The owner and focused direct coverage pass without external services, developer state, or live network access.
   </success_criteria>

## Artifacts this phase produces

- `tests/shared/debug-log.test.ts`: canonical mirrored owner for `extensions/pi-claude-marketplace/shared/debug-log.ts`.
- Production symbols created: none. The production source remains byte-identical.
- `.planning/phases/109-shared-contracts/109-05-SUMMARY.md`: execution evidence, caller trace, exact commands, and pair commit record.

<output>
Create `.planning/phases/109-shared-contracts/109-05-SUMMARY.md` when done.
</output>
