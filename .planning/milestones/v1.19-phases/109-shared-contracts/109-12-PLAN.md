---
phase: 109-shared-contracts
plan: 12
type: execute
wave: 2
depends_on:
  - "109-03"
  - "109-04"
  - "109-13"
files_modified:
  - "tests/shared/notify-context.test.ts"
autonomous: true
requirements:
  - MOD-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "P109-12 directly imports and proves RenderFn; CommandContext; Single; Plural; WithPlugins; MarketplaceRows; notifyWithContext; notifyUpdateWithContext; notifyUpdateNoOpWithContext; notifyReconcileAppliedWithContext without changing the current public surface (D-01)."
    - "Cover empty, one-row, and plural marketplace inputs; exact single/plural cardinality; zero, one, and several tally counts; equal status rows that dispatch separately in input order; missing render arms adjacent to present arms; and exact integer counts without rounding."
    - "Every runtime case created or changed uses separate lowercase phase markers and case-owned mutable state; type-only evidence follows D-03 (D-02, D-03, D-04)."
    - "The owner alone reaches 100 percent direct function, line, and branch coverage for extensions/pi-claude-marketplace/shared/notify-context.ts."
  artifacts:
    - path: "tests/shared/notify-context.test.ts"
      provides: "Canonical mirrored owner for P109-12"
    - path: "extensions/pi-claude-marketplace/shared/notify-context.ts"
      provides: "Existing public contract under direct owner coverage; no new public symbols"
  key_links:
    - from: "tests/shared/notify-context.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notify-context.ts"
      via: "direct relative import of the paired source"
      pattern: "from .*notify-context\\.ts"
    - from: "tests/shared/notify-context.test.ts"
      to: "scripts/test-coverage-direct.mjs"
      via: "focused source-to-owner mapping"
      pattern: "test:coverage:direct"
---

<objective>
Complete P109-12 as one pair-atomic owner plan per D-01 through D-04: Own controlled notification context dispatch.

Purpose: Preserve the current caller-visible contract while replacing brownfield or missing evidence with exact public-value and side-effect proof.
Output: tests/shared/notify-context.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shared-contracts/109-CONTEXT.md
@.planning/phases/109-shared-contracts/109-RESEARCH.md
@.planning/phases/109-shared-contracts/109-PATTERNS.md
@.planning/phases/109-shared-contracts/109-VALIDATION.md
@extensions/pi-claude-marketplace/shared/notify-context.ts

</context>

<interfaces>
From `extensions/pi-claude-marketplace/shared/notify-context.ts`:
- Public exports in scope: RenderFn; CommandContext; Single; Plural; WithPlugins; MarketplaceRows; notifyWithContext; notifyUpdateWithContext; notifyUpdateNoOpWithContext; notifyReconcileAppliedWithContext
- Public behavior to preserve: Create the missing owner after P109-03, P109-04, and P109-13. Build local typed `CommandContext` values with controlled renderer functions; do not import production command render maps. Absorb dispatch behavior from `tests/shared/notify-context-dispatch-guard.test.ts` and the dispatch portions of the disabled/not-installed reason suites. Prove all four wrappers, label and cardinality projection, renderer selection, exact arguments, call count, tally, no-op, reconcile dispatch, and deterministic missing-arm fallback. Keep central notification byte expectations out of this owner per D-06.
- Production callers must continue to compile and observe identical values and side effects.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trace callers and establish the canonical P109-12 owner</name>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify-context.ts - paired production source and public exports: RenderFn; CommandContext; Single; Plural; WithPlugins; MarketplaceRows; notifyWithContext; notifyUpdateWithContext; notifyUpdateNoOpWithContext; notifyReconcileAppliedWithContext
    - tests/shared/notify-context.test.ts - current owner if present; otherwise confirm it is absent before creating it
    - .planning/phases/109-shared-contracts/109-CONTEXT.md - D-01 through D-04 and notification decisions when applicable
    - .planning/phases/109-shared-contracts/109-RESEARCH.md - pair matrix and edge evidence for P109-12
    - .planning/phases/109-shared-contracts/109-PATTERNS.md - assigned analog and pair boundary
    - .claude/rules/typescript-unit-testing.md - executable owner-test contract
    - tests/shared/notify-context-dispatch-guard.test.ts
    - tests/shared/notify-disabled-reasons.test.ts
    - tests/shared/notify-not-installed-reasons.test.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
  </read_first>
  <files>tests/shared/notify-context.test.ts</files>
  <action>Before any edit, run CodeGraph for `extensions/pi-claude-marketplace/shared/notify-context.ts` and the listed exports, trace production callers, and record the caller-facing invariants in the execution notes and final summary. Run the current owner and focused coverage when the owner exists. Directly import the paired source from `tests/shared/notify-context.test.ts`. Create the missing owner after P109-03, P109-04, and P109-13. Build local typed `CommandContext` values with controlled renderer functions; do not import production command render maps. Absorb dispatch behavior from `tests/shared/notify-context-dispatch-guard.test.ts` and the dispatch portions of the disabled/not-installed reason suites. Prove all four wrappers, label and cardinality projection, renderer selection, exact arguments, call count, tally, no-op, reconcile dispatch, and deterministic missing-arm fallback. Keep central notification byte expectations out of this owner per D-06. Keep the production source byte-identical; all evidence must use its current public exports. Per D-02, every runtime case added or changed uses exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; only one throwing or rejection expression may use lowercase `// act & assert`, and data rows always keep separate phases. For compile-time evidence, use only module-scope `satisfies` expressions and targeted `@ts-expect-error` negatives with no runtime wrapper or phase comments per D-03. Per D-04, create fresh state and register cleanup before mutation. Do not add a public export, reset hook, state reader, test mode, coverage exemption, generic helper, or unrelated source edit.</action>
  <verify>
    <automated>node --test tests/shared/notify-context.test.ts
npm run typecheck</automated>
  </verify>
  <acceptance_criteria>The canonical owner directly imports `extensions/pi-claude-marketplace/shared/notify-context.ts`, its core public behavior passes alone, the caller trace is recorded, and no unrelated file changed.</acceptance_criteria>
  <done>P109-12 has a production-quality owner path through the paired source and its core contract.</done>
</task>

<task type="auto">
  <name>Task 2: Complete exact edge coverage and pair-local quality gates</name>
  <read_first>
    - tests/shared/notify-context.test.ts - Task 1 owner state and case names
    - extensions/pi-claude-marketplace/shared/notify-context.ts - branch and export inventory
    - .planning/phases/109-shared-contracts/109-VALIDATION.md - P109-12 command and secure behavior
    - .claude/rules/typescript-comments.md - durable source and test-title wording
  </read_first>
  <files>tests/shared/notify-context.test.ts</files>
  <action>Expand the owner only where a distinct public contract remains. Cover empty, one-row, and plural marketplace inputs; exact single/plural cardinality; zero, one, and several tally counts; equal status rows that dispatch separately in input order; missing render arms adjacent to present arms; and exact integer counts without rounding. Compare whole public values, structured errors, arrays, side effects, or exact bytes to expectations authored independently of production constants and formatters. Keep data rows as sibling `test()` cases with no conditional branch in the loop body. Remove migration or work-session commentary from the changed owner. Keep the production source byte-identical; all evidence must use its current public exports. Confirm the public export surface is unchanged and that all callers identified in Task 1 still typecheck.</action>
  <verify>
    <automated>node --test tests/shared/notify-context.test.ts
npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/notify-context.ts
npm run typecheck
npx eslint extensions/pi-claude-marketplace/shared/notify-context.ts tests/shared/notify-context.test.ts
npx prettier --check extensions/pi-claude-marketplace/shared/notify-context.ts tests/shared/notify-context.test.ts
git diff --check
git diff --exit-code -- extensions/pi-claude-marketplace/shared/notify-context.ts</automated>
  </verify>
  <acceptance_criteria>Boundary, adjacency, empty, ordering, and precision rows are explicitly resolved; every applicable edge has an independently discriminating case; the owner reaches 100 percent direct functions, lines, and branches; lint, formatting, type checks when relevant, and diff checks pass.</acceptance_criteria>
  <done>Own controlled notification context dispatch; public behavior and surface remain unchanged, and P109-12 is ready to commit as one source-owner pair.</done>
</task>

</tasks>

<threat_model>

## Trust boundaries

| Boundary                           | Description                                                                                                                         |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Production caller to shared export | Caller input crosses into `extensions/pi-claude-marketplace/shared/notify-context.ts`; tests preserve the accepted public contract. |
| Owner test to runtime boundary     | The test controls any filesystem, environment, cache, console, notification, or type-checking state it exercises per D-04.          |
| External API or package install    | N/A. P109-12 adds no SDK, service integration, dependency, or package-manager operation.                                            |

## STRIDE threat register

**Security policy:** ASVS L1; block completion on any unmitigated high-severity threat.

| Threat ID       | Category                | Component                                         | Severity | Disposition | Mitigation plan                                                                                                                                       |
| --------------- | ----------------------- | ------------------------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-109-12        | Tampering / Repudiation | command context to notification dispatch boundary | medium   | mitigate    | Use strict controlled renderers and complete interaction verification so every wrapper dispatches the intended row once with exact projection fields. |
| T-109-12-NA     | Spoofing                | External identity boundary                        | low      | accept      | N/A for this pair because it performs no authentication or external identity decision.                                                                |
| </threat_model> |

<verification>
- Run `node --test tests/shared/notify-context.test.ts`.
- Run `npm run test:coverage:direct -- extensions/pi-claude-marketplace/shared/notify-context.ts` and require 100 percent functions, lines, and branches.
- Run pair-local ESLint, formatting, type checks when relevant, and `git diff --check`.
- Confirm no test-only public surface, generic helper, coverage ignore, or unrelated legacy-suite edit was introduced.
</verification>

<success_criteria>

1. P109-12 satisfies MOD-02 for its one mirrored source-owner pair.
2. Its exact public values and side effects include defensible boundary, adjacency, empty, ordering, and precision outcomes.
3. D-01 through D-04 are visible in the implementation and automated evidence.
4. The owner and focused direct coverage pass without external services, developer state, or live network access.
   </success_criteria>

## Artifacts this phase produces

- `tests/shared/notify-context.test.ts`: canonical mirrored owner for `extensions/pi-claude-marketplace/shared/notify-context.ts`.
- Production symbols created: none. The production source remains byte-identical.
- `.planning/phases/109-shared-contracts/109-12-SUMMARY.md`: execution evidence, caller trace, exact commands, and pair commit record.

<output>
Create `.planning/phases/109-shared-contracts/109-12-SUMMARY.md` when done.
</output>
