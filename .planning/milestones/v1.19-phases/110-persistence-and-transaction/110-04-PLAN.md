---
phase: 110-persistence-and-transaction
plan: 04
type: execute
wave: 3
depends_on: [110-03]
files_modified:
  - tests/persistence/config-merge.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "When base and local entries collide, the complete local entry replaces the complete base entry; fields never deep-merge, and provenance is local."
    - "Empty, base-only, local-only, dangling-plugin, and equal-key inputs produce complete deterministic maps with stable insertion order (D-04)."
    - "loadMergedScopeConfig preserves the complete base and local absent, invalid, and valid results while deriving the merged view."
    - "The baseline PASS owner is normalized and re-proved at 100 percent direct coverage with case-owned loader directories (D-01, D-14, D-15)."
  artifacts:
    - path: tests/persistence/config-merge.test.ts
      provides: "Canonical direct owner for whole-entry merge, provenance, and per-file load outcomes"
  key_links:
    - from: tests/persistence/config-merge.test.ts
      to: extensions/pi-claude-marketplace/persistence/config-merge.ts
      via: "direct calls to mergeScopeConfigs and loadMergedScopeConfig"
      pattern: "mergeScopeConfigs|loadMergedScopeConfig"
    - from: extensions/pi-claude-marketplace/persistence/config-merge.ts
      to: extensions/pi-claude-marketplace/persistence/config-io.ts
      via: "base and local ConfigLoadResult arms proved by 110-03"
      pattern: "loadConfig"
---

<objective>
Pin config merge collisions, provenance, ordering, and paired-file load outcomes as complete values.

Purpose: Local overrides remain whole-entry replacements instead of silently becoming a field-level deep merge.
Output: A normalized baseline-PASS owner with complete direct coverage and no production change per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Prove one complete base/local collision through the public reducer</name>
  <files>tests/persistence/config-merge.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/config-merge.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/config-merge.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/domain/source.test.ts
    - tests/shared/completion-cache.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the baseline PASS owner around a direct import of mergeScopeConfigs. Start with one independently authored base/local marketplace and plugin collision whose complete expected MergedConfig proves whole-entry local replacement, exact `source: "local"` provenance, and no leaked base fields. Use literal input and output records per D-02 and D-14. Every runtime case must use exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows keep those separate phases and type-only `satisfies` evidence stays at module scope per D-16. Keep the production source and exports unchanged per D-13.</action>
  <verify>
    <automated>node --test tests/persistence/config-merge.test.ts</automated>
  </verify>
  <acceptance_criteria>The collision case fails if any base field survives, if provenance changes, or if the whole expected marketplaces/plugins maps differ.</acceptance_criteria>
  <done>The adjacency/collision fallback probe is resolved explicitly through the public reducer.</done>
</task>

<task type="auto">
  <name>Task 2: Expand empty, ordering, dangling, and loader outcomes</name>
  <files>tests/persistence/config-merge.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/config-merge.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/config-merge.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/domain/source.test.ts
    - tests/shared/atomic-json.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add named literal rows for empty, base-only, local-only, repeated/equal keys, and a plugin whose marketplace entry is absent; compare the complete merged maps and provenance in deterministic key order per D-02 and D-04. Add loadMergedScopeConfig cases for both valid arms and for absent or invalid base/local arms, preserving the full base and local ConfigLoadResult values beside the derived merge. Each loader case creates and cleans its own temporary directory per D-01 and D-03. Keep runtime phases exact and lowercase per D-16, with no cleanup sleeps, shared root, generic helper, deep-merge helper, or snapshots. Re-prove the PASS owner rather than accepting its prior coverage per D-14 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/config-merge.test.ts</automated>
  </verify>
  <acceptance_criteria>Empty and all load arms are complete values, collisions are whole-entry replacements, equal-key ordering is stable, loader directories are case-owned, and direct coverage is 100 percent.</acceptance_criteria>
  <done>The complete merge and paired-load public contract is coverage-complete.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary                                           | Description                                                                                 |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| base/local config documents → merged desired state | A local collision must replace one complete entry without inheriting untrusted base fields. |

## STRIDE Threat Register

| Threat ID   | Category    | Component             | Severity | Disposition | Mitigation Plan                                                                              |
| ----------- | ----------- | --------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------- |
| T-110-04-01 | Tampering   | mergeScopeConfigs     | high     | mitigate    | Pin whole-entry replacement, provenance, dangling rows, and complete output ordering.        |
| T-110-04-02 | Repudiation | loadMergedScopeConfig | medium   | mitigate    | Preserve exact per-file load outcomes beside the merged view so invalid input is not hidden. |

Canon-security breadcrumb: config collision integrity is ordinary ASVS business-logic validation; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `tests/persistence/config-merge.test.ts` as the canonical owner of `mergeScopeConfigs`, `loadMergedScopeConfig`, `MergedConfig`, and `ScopeLoadOutcome`.
- Creates no new public production symbol and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/config-merge.test.ts`; it must pass with 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Collision, empty, dangling, ordering, and all paired-file outcomes are independently pinned.
- No deep merge or production-surface change is introduced.
- Baseline PASS evidence is normalized under D-14 and D-16.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-04-SUMMARY.md` when done.
</output>
