---
phase: 110-persistence-and-transaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/persistence/agents-index-schema.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The published schema accepts exactly schemaVersion 1 with an agents array and rejects adjacent version 2 and an entries alias (D-04)."
    - "Every required row field and array plus optional originalModel has independent positive and negative validator evidence (D-02)."
    - "The baseline PASS owner is normalized and re-proved through a direct paired import at 100 percent direct coverage (D-14, D-15)."
  artifacts:
    - path: tests/persistence/agents-index-schema.test.ts
      provides: "Canonical runtime and compile-time owner for agents-index schemas, types, and validators"
  key_links:
    - from: tests/persistence/agents-index-schema.test.ts
      to: extensions/pi-claude-marketplace/persistence/agents-index-schema.ts
      via: "direct imports of both validators and schema-derived types"
      pattern: "AGENTS_INDEX_(ENTRY_)?VALIDATOR|AgentsIndex"
---

<objective>
Normalize and re-prove the agents-index schema and validator contract before I/O owners consume it.

Purpose: A durable version-1 index cannot drift to a renamed envelope or incomplete row unnoticed.
Output: A complete direct schema owner with runtime validation and module-scope type evidence; no production export changes per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Publish the complete version-1 schema contract</name>
  <files>tests/persistence/agents-index-schema.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/agents-index-schema.ts
    - tests/persistence/agents-index-schema.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/completion-cache.test.ts
    - tests/domain/components/plugin.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the baseline PASS owner around direct imports of AGENTS_INDEX_VALIDATOR and AGENTS_INDEX_ENTRY_VALIDATOR. Use independent literal complete rows to prove every required string and array, the optional originalModel arm, version 1, an empty agents array, and several agents per D-02, D-04, and D-14. Compare complete validation outcomes and keep positive `satisfies AgentsIndexEntry` and `satisfies AgentsIndex` evidence at module scope. Every runtime case must use exact lowercase `// arrange`, a blank line, `// act`, a blank line, and `// assert`; no fake runtime phases around type evidence per D-16. Keep the production schema and exports unchanged per D-13.</action>
  <verify>
    <automated>node --test tests/persistence/agents-index-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>Both compiled validators are exercised through direct imports with complete independent version-1 and row values; module-scope type evidence compiles.</acceptance_criteria>
  <done>The accepted agents-index wire shape is visible as one complete runnable contract.</done>
</task>

<task type="auto">
  <name>Task 2: Prove adjacent versions and incomplete-row rejection</name>
  <files>tests/persistence/agents-index-schema.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/agents-index-schema.ts
    - tests/persistence/agents-index-schema.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/completion-cache.test.ts
    - tests/domain/components/hooks/schema.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add independent negative values for schemaVersion 2, `entries` in place of `agents`, null and primitive envelopes, a missing required row field, a non-array droppedFields/droppedTools/warnings field, and an invalid originalModel. Keep each generated callback's separate lowercase arrange, act, and assert phases per D-16; add targeted module-scope `@ts-expect-error` evidence only where it proves the exported static type rather than duplicating runtime validation. Assert whole validator results, not production-derived expectations, per D-02 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/agents-index-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>Adjacent version and envelope-name collisions reject, every required row category is pinned, no runtime skip/todo exists, and direct coverage is 100 percent.</acceptance_criteria>
  <done>The baseline PASS pair is fully normalized and re-proved per D-14 and D-15.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary                                   | Description                                                           |
| ------------------------------------------ | --------------------------------------------------------------------- |
| unknown parsed value → compiled validators | Untrusted persisted shapes must not be admitted as a version-1 index. |

## STRIDE Threat Register

| Threat ID   | Category          | Component                    | Severity | Disposition | Mitigation Plan                                                                                                                        |
| ----------- | ----------------- | ---------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| T-110-02-01 | Tampering         | AGENTS_INDEX_VALIDATOR       | high     | mitigate    | Pin exact version, envelope key, row cardinality, and required field types with independent values.                                    |
| T-110-02-02 | Denial of service | AGENTS_INDEX_ENTRY_VALIDATOR | low      | accept      | Validator compilation remains module-scoped and unchanged; bounded literal invalid cases prove rejection without new processing paths. |

Canon-security breadcrumb: schema-validation of persisted JSON is an ASVS validation control; no bespoke prohibition is minted for this pure schema pair.
</threat_model>

## Artifacts this phase produces

- Refines `tests/persistence/agents-index-schema.test.ts` as the canonical owner for `AgentsIndexEntry`, `AgentsIndex`, `AGENTS_INDEX_ENTRY_VALIDATOR`, and `AGENTS_INDEX_VALIDATOR`.
- Creates no new public production symbol and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/agents-index-schema.test.ts`; the owner must pass at 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Version 1 and `agents` are the only accepted envelope contract.
- Complete row fields, arrays, and optional model evidence are explicit.
- Lowercase runtime phases and module-scope type-only evidence follow D-16 exactly.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-02-SUMMARY.md` when done.
</output>
