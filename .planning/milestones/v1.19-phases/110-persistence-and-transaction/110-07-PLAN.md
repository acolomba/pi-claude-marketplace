---
phase: 110-persistence-and-transaction
plan: 07
type: execute
wave: 3
depends_on: [110-03]
files_modified:
  - extensions/pi-claude-marketplace/persistence/migrate-config.ts
  - tests/persistence/migrate-config.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "buildConfigFromState projects complete marketplace/plugin records from independent state values, including nullish stored source as the exact string `null` (D-02, D-05)."
    - "migrateFirstRunConfig writes only for absent nonempty state, preserves valid/invalid existing files, and returns exact empty-state behavior for zero entries (D-04, D-07)."
    - "Replaying migration against the created file is an exact no-op with identical bytes and no rewrite effect (D-06, D-08)."
    - "The return-type narrowing removes only unreachable nullish entry-count branches and preserves all public exports and runtime behavior (D-13)."
  artifacts:
    - path: extensions/pi-claude-marketplace/persistence/migrate-config.ts
      provides: "Behavior-preserving non-null record return type for buildConfigFromState"
    - path: tests/persistence/migrate-config.test.ts
      provides: "Canonical direct owner for first-run config projection, suppression, persistence, and replay"
  key_links:
    - from: tests/persistence/migrate-config.test.ts
      to: extensions/pi-claude-marketplace/persistence/migrate-config.ts
      via: "direct pure-transform and public migration calls"
      pattern: "buildConfigFromState|migrateFirstRunConfig"
    - from: extensions/pi-claude-marketplace/persistence/migrate-config.ts
      to: extensions/pi-claude-marketplace/persistence/config-io.ts
      via: "load trichotomy and contained atomic save proved by 110-03"
      pattern: "loadConfig|saveConfig"
---

<objective>
Prove first-run config projection, exact suppression arms, durable bytes, and idempotent replay while narrowing an already-present invariant.

Purpose: First-run capture remains a fixed point and never creates an empty or invalid desired-state file.
Output: Pair-local behavior-preserving type refinement plus a complete direct migration owner.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Refine and prove the complete state-to-config projection</name>
  <files>extensions/pi-claude-marketplace/persistence/migrate-config.ts, tests/persistence/migrate-config.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/migrate-config.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/persistence/migrate-config.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/completion-cache.test.ts
    - tests/domain/source.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Give buildConfigFromState an explicit behavior-preserving return type that keeps `schemaVersion: 1` and makes `marketplaces` and `plugins` present non-null records. Remove only the now-redundant `?? {}` operands from migrateFirstRunConfig's entry count; add no new export or test seam per D-13. Rebuild the owner around direct imports and independent literal ExtensionState inputs for empty state, several marketplaces/plugins, exact true/false legacy autoupdate projection, soft-degraded plugin inclusion, raw source preservation, an unknown object source, and nullish source producing the exact string `null` per D-02, D-04, and D-05. Compare complete ScopeConfig results. Every runtime case uses exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; type-only `satisfies` evidence stays module-scoped per D-16.</action>
  <reversibility rating="reversible">The return type narrows to records already constructed unconditionally; undo is pair-local and changes no stored bytes.</reversibility>
  <verify>
    <automated>npm run typecheck &amp;&amp; node --test tests/persistence/migrate-config.test.ts</automated>
  </verify>
  <acceptance_criteria>buildConfigFromState exposes present marketplace/plugin records, the nullish source is exactly `null`, all projection values are complete and independent, and all callers typecheck.</acceptance_criteria>
  <done>The public pure projection establishes the invariant later replay logic consumes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove suppression, exact persistence, failure, and replay</name>
  <files>tests/persistence/migrate-config.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/migrate-config.ts
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/migrate-config.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/atomic-json.test.ts
    - tests/shared/completion-cache.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add fresh case-owned filesystem cases for existing valid, existing invalid, absent plus empty state, absent plus populated state, and propagated save/containment failure. Assert every complete MigrateFirstRunResult arm, stable error text/cause where public, retained in-memory input, and exact disk effects per D-01, D-04, and D-07. For the migrated case, read and compare exact complete JSON bytes, then call migrateFirstRunConfig a second time through the same public path and require `{ migrated: false, reason: "existing-valid", filePath }`, byte identity, and no rewrite observable through unchanged file metadata or a case-local method observation per D-06 and D-08. Do not use sleeps, polling, shared fixture roots, snapshots, or production builders for expectations. Keep exact lowercase phases in every runtime callback per D-16 and re-prove the owner at direct 100 percent coverage per D-14 and D-15.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/migrate-config.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>All four migration result families are complete, empty state writes nothing, first migration bytes are exact, replay is a byte-identical no-op, failure effects are exact, and direct coverage/typecheck pass.</acceptance_criteria>
  <done>The first-run config migration is idempotent, failure-explicit, and coverage-complete.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary                                      | Description                                                                                     |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| legacy in-memory state → desired-state config | Unknown/null sources and legacy fields must project to valid deterministic strings and records. |
| absent config path → first durable config     | Repeated migration or save failure must not create divergent or partial desired state.          |

## STRIDE Threat Register

| Threat ID   | Category    | Component                    | Severity | Disposition | Mitigation Plan                                                                      |
| ----------- | ----------- | ---------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------ |
| T-110-07-01 | Tampering   | buildConfigFromState         | high     | mitigate    | Compare complete literal projections including unknown/null sources and every entry. |
| T-110-07-02 | Repudiation | migrateFirstRunConfig replay | medium   | mitigate    | Pin every result arm, propagated failure, exact bytes, and second-call no-op.        |

Canon-security breadcrumb: migration validation and atomic persistence are canon ASVS controls; no bespoke prohibition applies.
</threat_model>

## Artifacts this phase produces

- Refines `buildConfigFromState`'s existing return contract so `marketplaces` and `plugins` are statically present; no new public symbol is created.
- Refines `tests/persistence/migrate-config.test.ts` as the canonical owner of `buildConfigFromState`, `migrateFirstRunConfig`, and `MigrateFirstRunResult`.
- Creates no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/migrate-config.test.ts` and `npm run typecheck`; both must pass.
</verification>

<success_criteria>

- Projection values, empty/null behavior, suppression results, save failures, exact bytes, and replay are complete.
- Production change is a behavior-preserving type narrowing only.
- D-01, D-02, D-05, D-06, D-07, D-08, D-13, D-14, D-15, and D-16 are satisfied.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-07-SUMMARY.md` when done.
</output>
