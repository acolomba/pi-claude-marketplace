---
phase: 110-persistence-and-transaction
plan: 03
type: execute
wave: 2
depends_on: [110-06]
files_modified:
  - tests/persistence/config-io.test.ts
autonomous: true
requirements: [MOD-03]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "loadConfig keeps exact absent, invalid, and valid outcomes for missing, zero-byte, malformed, schema-invalid, unknown-field, and ordinary read inputs (D-04, D-07)."
    - "isDeclaredEnabled returns true for an absent or true flag and false only for literal false."
    - "saveConfig validates before containment and atomic replacement, and successful writes have complete exact JSON bytes (D-08)."
    - "Every filesystem case owns its fresh directory and cleanup, and the owner directly reaches 100 percent function, line, and branch coverage (D-01, D-15)."
  artifacts:
    - path: tests/persistence/config-io.test.ts
      provides: "Canonical owner for config load trichotomy, tri-state enabled predicate, validation, containment, and save bytes"
  key_links:
    - from: tests/persistence/config-io.test.ts
      to: extensions/pi-claude-marketplace/persistence/config-io.ts
      via: "direct public calls to isDeclaredEnabled, loadConfig, and saveConfig"
      pattern: "isDeclaredEnabled|loadConfig|saveConfig"
    - from: extensions/pi-claude-marketplace/persistence/config-io.ts
      to: extensions/pi-claude-marketplace/shared/path-safety.ts
      via: "containment before atomic write"
      pattern: "assertPathInside"
---

<objective>
Make config I/O behavior explicit across absent, invalid, lenient-valid, containment, and exact-save paths.

Purpose: Maintainers can evolve desired-state I/O without collapsing distinct failure states or writing outside the scope root.
Output: A normalized direct owner at complete coverage with production behavior and exports preserved per D-13.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-persistence-and-transaction/110-CONTEXT.md
@.planning/phases/110-persistence-and-transaction/110-RESEARCH.md
@.planning/phases/110-persistence-and-transaction/110-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Establish the config load trichotomy and exact save path</name>
  <files>tests/persistence/config-io.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/config-io.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/atomic-json.test.ts
    - tests/shared/completion-cache.test.ts
    - tests/shared/path-safety.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Rebuild the owner around direct imports of loadConfig, saveConfig, CONFIG_VALIDATOR, and isDeclaredEnabled. Use fresh case-owned directories and cleanup for missing, valid, unknown-field, and successful save cases per D-01 and D-03. Author literal config JSON and separate complete ConfigLoadResult expectations; keep unknown forward-compatible keys visible, and compare exact two-space JSON plus trailing newline after save per D-02, D-04, and D-08. Add direct absent, true, and false isDeclaredEnabled cases. Every runtime case must use exact lowercase `// arrange`, blank line, `// act`, blank line, `// assert`; generated rows repeat those phases and type-only evidence stays module-scoped per D-16. Preserve production exports and behavior per D-13.</action>
  <verify>
    <automated>node --test tests/persistence/config-io.test.ts</automated>
  </verify>
  <acceptance_criteria>Absent and valid load arms plus enabled tri-state and exact save bytes are asserted as complete independent values in case-owned directories.</acceptance_criteria>
  <done>The public desired-state happy path is runnable from literal file through load and contained atomic save.</done>
</task>

<task type="auto">
  <name>Task 2: Close invalid-input, fallback-detail, and containment branches</name>
  <files>tests/persistence/config-io.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/persistence/config-io.ts
    - tests/persistence/config-io.test.ts
    - .planning/phases/110-persistence-and-transaction/110-CONTEXT.md
    - .planning/phases/110-persistence-and-transaction/110-RESEARCH.md
    - .planning/phases/110-persistence-and-transaction/110-PATTERNS.md
    - tests/shared/completion-cache.test.ts
    - tests/shared/fs-utils.test.ts
    - tests/shared/path-safety.test.ts
    - .claude/rules/typescript-unit-testing.md
    - scripts/test-coverage-direct.mjs
  </read_first>
  <action>Add independent cases for a zero-byte file, malformed JSON, root-level invalid document, wrong adjacent schema version, ordinary non-ENOENT read failure, and invalid save. Reach the measured empty CONFIG_VALIDATOR.Errors fallback through a public load or save with case-local t.mock.method and assert the complete invalid result/error text per D-04 and D-07. Prove containment rejection before replacement and unchanged disk state on validation or containment failure; keep every corrupt/failure directory case-owned per D-01. Assert complete errors, causes where public, values, and bytes per D-07 and D-08. Retain exact lowercase runtime phases per D-16 and do not add snapshots, platform skips, generic helpers, or test-only production seams per D-03 and D-13.</action>
  <verify>
    <automated>npm run test:coverage:direct -- tests/persistence/config-io.test.ts</automated>
  </verify>
  <acceptance_criteria>All invalid categories remain distinguishable, the validator empty-detail fallback and containment branches execute publicly, failed saves leave bytes unchanged, and direct coverage is 100 percent.</acceptance_criteria>
  <done>The full config I/O owner is normalized and re-proved per D-14 and D-15.</done>
</task>

</tasks>

<threat_model>

Security policy: OWASP ASVS Level 1; any unmitigated high-severity threat blocks plan completion.

## Trust Boundaries

| Boundary                                     | Description                                                                          |
| -------------------------------------------- | ------------------------------------------------------------------------------------ |
| user-authored config bytes → loadConfig      | Malformed or schema-invalid local desired state must not become valid configuration. |
| validated ScopeConfig → scope-contained file | A save must not escape its root or partially replace durable state.                  |

## STRIDE Threat Register

| Threat ID   | Category               | Component                     | Severity | Disposition | Mitigation Plan                                                                        |
| ----------- | ---------------------- | ----------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------- |
| T-110-03-01 | Tampering              | loadConfig / CONFIG_VALIDATOR | high     | mitigate    | Prove parse, root-schema, version, empty-detail, and lenient unknown-field boundaries. |
| T-110-03-02 | Elevation of privilege | saveConfig containment        | high     | mitigate    | Prove validation and assertPathInside happen before exact atomic replacement.          |

Canon-security breadcrumb: persisted-input validation and path containment are ASVS controls routed to secure-phase; this utility pair has no bespoke values/safety/ethics prohibition.
</threat_model>

## Artifacts this phase produces

- Refines `tests/persistence/config-io.test.ts` as the canonical owner of `ScopeConfig`, `CONFIG_VALIDATOR`, `isDeclaredEnabled`, `loadConfig`, and `saveConfig`.
- Creates no new public production symbol and no external-API coverage artifact.

<verification>
Run `npm run test:coverage:direct -- tests/persistence/config-io.test.ts`; the focused process must pass with 100 percent direct function, line, and branch coverage.
</verification>

<success_criteria>

- Absent, invalid, valid, containment, and write outcomes remain exact and distinct.
- Empty/null input behavior and exact JSON bytes are pinned without shared fixtures.
- Every changed runtime case satisfies D-16 and every filesystem case satisfies D-01.
  </success_criteria>

<output>
Create `.planning/phases/110-persistence-and-transaction/110-03-SUMMARY.md` when done.
</output>
