---
phase: 115-composition-orchestrators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/orchestrators/edge-deps.test.ts
autonomous: true
requirements: [MOD-08]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: medium
must_haves:
  truths:
    - "makeLocationsResolver returns the two cache paths, the projected scope state, and the complete manifest row list for every status partition, each asserted as one whole value against a hand-authored literal."
    - "No expected value in the suite is produced by calling loadMarketplaceManifest, resolveStrict, classifyInstalledRecord, classifyManifestEntry, or any other production function; the status oracle stays with tests/orchestrators/plugin/plugin-state-classifier.test.ts."
    - "The suite imports no symbol from orchestrators/plugin/list.ts; edge-deps row shapes are asserted as literals owned by this pair."
    - "Every case owns one temporary tree and registers its removal plus the HOME and agent-directory restore through t.after(), so no temporary root and no process environment change survives the case (D-115-03: no production seam is added to a read-only wiring module)."
    - "Every case installs a fail-fast external boundary and asserts it was never reached, and the pair keeps 100 percent direct functions, lines, and branches with no coverage exception."
  artifacts:
    - path: tests/orchestrators/edge-deps.test.ts
      provides: "Sole mirrored direct owner for orchestrators/edge-deps.ts, contract-compliant"
  key_links:
    - from: tests/orchestrators/edge-deps.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/edge-deps.ts
      via: "direct makeLocationsResolver import and exported-resolver proof"
      pattern: "makeLocationsResolver"
---

<objective>
Re-prove the completion-resolver wiring module through a contract-compliant owner.

Purpose: `edge-deps.ts` already passes direct coverage at 26/26 branches, 8/8 functions, and
242/242 lines, so the debt here is contract shape, not reach. The suite currently derives one
expectation by calling the same production classifiers the module calls, reaches into a second
pair's production surface for a row string, restores the environment only in `finally`, leaks
two temporary roots per case, and asserts 40 single values plus 13 existence checks where the
whole row list is the promise. A wrong classifier passes that suite today.

This is the wave-1 proving slice for the phase. It exercises the whole delivery path — the
S1 case-owned scope, the S2 strict-boundary doubles, the S4 literal row table, the direct
coverage gate, `fallow` (including the `dupes` threshold the shared factory recipe stresses),
and the pair-atomic commit — on the cheapest pair, so a defect in the recipe surfaces before
seven more suites adopt it. D-115-03 governs the double strategy: this module declares no
collaborator parameter, so the contract is the value it reads back off a real tree, and no
seam is added for the test.

Output: one exhaustive, hermetic, independently-expected owner for `orchestrators/edge-deps.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-composition-orchestrators/115-CONTEXT.md
@.planning/phases/115-composition-orchestrators/115-RESEARCH.md
@.planning/phases/115-composition-orchestrators/115-PATTERNS.md
@.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the edge-deps owner with independent expectations and a hermetic per-case scope</name>
  <files>tests/orchestrators/edge-deps.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/edge-deps.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts
    - extensions/pi-claude-marketplace/shared/completion-cache.ts
    - tests/orchestrators/edge-deps.test.ts
    - tests/orchestrators/plugin/plugin-state-classifier.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - tests/orchestrators/marketplace/add.test.ts
    - tests/orchestrators/auth-host.test.ts
    - tests/platform/git-ops-fake.ts
    - .planning/phases/115-composition-orchestrators/115-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/orchestrators/edge-deps.test.ts` so every case proves `makeLocationsResolver`
through its own exported surface with a hand-authored expectation.

Structure. Keep one top-level `describe()` per public operation reached through the export:
`marketplaceNamesCachePath`, `pluginCachePath`, `loadStateForScope`,
`loadManifestForMarketplace`. Do not nest. Every runtime case carries separate lowercase
`// arrange`, `// act`, `// assert` phases in that order, blank-line separated. Reserve the
combined phase marker for a single throwing or rejecting expression. Add no other comments
unless the setup is genuinely not obvious.

Kill the derived expectation. The bucketizer parity case near line 573 builds `expectedByName`
by calling `loadMarketplaceManifest`, `resolveStrict`, `classifyInstalledRecord`, and
`classifyManifestEntry` — the same functions the module under test calls — then compares the
two maps. Replace it with the row-table shape from
`tests/orchestrators/plugin/plugin-state-classifier.test.ts:95-201`: a typed
`readonly {...}[]` array whose status column is a written-out literal, `() =>` thunks for the
fixture factories, a `satisfies` tail on the array so a missing or misspelled field is a
compile error, and a `for` loop that wraps `test()` to emit one sibling case per row. Never a
loop inside one case. That classifier oracle belongs to
`plugin-state-classifier.test.ts`; this pair asserts only the rows `edge-deps.ts` itself
returns.

Cut the cross-owner reach. Delete the import of `availableRowMessage` from
`orchestrators/plugin/list.ts` near line 20 and its call site near line 749. The rendered row
text belongs to `tests/orchestrators/plugin/list.test.ts`. Assert this pair's returned
`PluginIndexRow` values as literals instead.

Whole-value assertions. Replace the 40 single-property and 13 existence assertions with one
`assert.deepStrictEqual()` per public surface: the complete `PluginIndexRow[]` in declaration
order, the complete projected `{ marketplaces: {...} }` record, and the complete path strings.
Do not probe a map one key at a time.

Hermetic per-case scope. Replace the file-local `withHermeticProjectScope` (which restores
`HOME` only in a `finally`, and whose `cleanup` near line 52 removes neither temporary root)
with the pattern at `tests/orchestrators/marketplace/shared.test.ts:188-227`: one
`mkdtemp(join(tmpdir(), "edge-deps-<label>-"))` per case, and one `t.after()` registered
before the module is called that removes every temporary root, restores `HOME` to its prior
presence and value, and restores or deletes `PI_CODING_AGENT_DIR`. Keep the agent-directory
clear — `getAgentDir()` reads that variable before `homedir()`, so a CI environment that sets
it defeats a hermetic `HOME`.

Fail-fast external boundary. `makeLocationsResolver(cwd)` takes no git, credential, or HTTP
port, and `probeManifestEntry`/`probeUpgradeCandidate` are presence-derived and file-system
only, so the honest boundary proof is a context-owned replacement that throws on any call.
Register `t.mock.method(globalThis, "fetch", ...)` with an implementation that throws, and
assert its call count is zero after the act phase, in every case that reaches
`loadManifestForMarketplace` or `loadStateForScope`. If while reading the module you find an
external port this description missed, install the matching in-repo fake
(`createGitOpsFake` from `tests/platform/git-ops-fake.ts`, or
`tests/platform/credential-ops-fake.ts`) with an empty allow-list instead, and say so in the
summary. Do not add a parameter to production to make the boundary injectable.

Types and naming. Remove every double assertion through `unknown` and every broad partial
cast; build typed literals checked with `satisfies`, and use
`mock<ExtensionAPI>({ exactParams: true, name: "extension API" })` if any case needs a Pi
handle. Name values after production roles — `indexRows`, `scopeState`, `cachePath`,
`expectedRows` — never the forbidden placeholder names. Case titles state public behavior and
may cite a durable requirement or decision ID; they may not cite a plan, phase, wave, or
ticket.

<!-- planner-discipline-allow: classifyInstalledRecord -->
<!-- planner-discipline-allow: classifyManifestEntry -->
<!-- planner-discipline-allow: resolveStrict -->
<!-- planner-discipline-allow: loadMarketplaceManifest -->
<!-- planner-discipline-allow: plugin/list.ts -->

Coverage. `edge-deps.ts` is at 100 percent today; re-verify it after the rewrite, since
deleting the derived case removes real execution. Every branch must be closed by a
behavioral case, never by a coverage exception. Change no production file in this plan.
  </action>
  <verify><automated>node --test tests/orchestrators/edge-deps.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/edge-deps.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/edge-deps.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/edge-deps.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)|plugin/list\.ts|classifyInstalledRecord|classifyManifestEntry|resolveStrict|loadMarketplaceManifest' tests/orchestrators/edge-deps.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/orchestrators/edge-deps.test.ts &amp;&amp; git diff --check -- tests/orchestrators/edge-deps.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/edge-deps.ts extensions/pi-claude-marketplace/orchestrators/plugin/git-source-probe.ts extensions/pi-claude-marketplace/orchestrators/plugin/plugin-state-classifier.ts</automated></verify>
  <acceptance_criteria>
`node --test tests/orchestrators/edge-deps.test.ts` passes alone and
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/edge-deps.ts`
reports 100 percent functions, lines, and branches with no coverage exception added. Every
runtime case has separate lowercase arrange, act, and assertion phases, and the count of
arrange markers equals the count of case bodies (a row table emits several
runtime cases from one marked body; each row still runs all three phases). No expected value is computed by
production: the suite contains no reference to `classifyInstalledRecord`,
`classifyManifestEntry`, `resolveStrict`, `loadMarketplaceManifest`, or
`orchestrators/plugin/list.ts`. Every public surface is compared as one whole value. Every
case owns one temporary tree, registers removal and environment restore through `t.after()`,
installs a fail-fast external boundary, and asserts it was never called. No double assertion
through `unknown`, no wildcard matcher, no unbounded-count expectation, and no `only`, `skip`,
or `todo` marker remains. `git diff` shows no change under `extensions/`. `npm run check` is
green end to end, allowing only the pre-existing local `format:check` noise from untracked
`.mcp.json` and `.planning/research/.cache/*.json` files, which this plan must not touch.
  </acceptance_criteria>
  <done>P115-01 is contract-compliant, hermetic, independently expected, and still at 100 percent direct coverage.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded by this plan, so the package-legitimacy gate does not apply.

| Threat ID  | Category               | Component                          | Severity | Disposition | Mitigation Plan                                                                                                                             |
| ---------- | ---------------------- | ---------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| T-115-01-A | Information Disclosure | external transport reachable from the resolver | high     | mitigate    | Context-owned fail-fast boundary that throws on any call, asserted uncalled in every filesystem case; no live remote and no developer credential. |
| T-115-01-B | Tampering              | temporary trees, `HOME`, `PI_CODING_AGENT_DIR` | high     | mitigate    | One `mkdtemp` root per case; removal and environment restore registered through `t.after()` before the act phase; no write into the repository or the real home directory. |
| T-115-01-C | Repudiation            | coverage evidence                  | medium   | mitigate    | Direct per-pair coverage gate run alone; no coverage exception, pragma, or production seam introduced.                                        |

</threat_model>

<verification>
Run the focused owner alone, the exact direct-coverage gate for the paired source, repository
typecheck, scoped lint and format checks, the whole-repository `fallow` chain (dead-code,
health, dupes — the `dupes` threshold is what the shared factory recipe stresses), the
prohibited-pattern scan, and a diff check proving no production file changed. Before
committing, run `pre-commit run --all-files` and `npm run check`; the only tolerated failure
is the pre-existing untracked-file `format:check` noise described in the acceptance criteria.
</verification>

<success_criteria>

- `orchestrators/edge-deps.ts` keeps 100 percent direct functions, lines, and branches with its
  owner run alone.
- Every expectation in the suite is authored by hand and compared as a complete value.
- The suite reaches no second pair's production surface and computes no expected value with
  production code.
- Every case is hermetic: one owned temporary tree, environment restore through the test
  context, a fail-fast external boundary proved uncalled.
- No production file changes.

</success_criteria>

<output>
Create .planning/phases/115-composition-orchestrators/115-01-SUMMARY.md when done.
</output>
