---
phase: 115-composition-orchestrators
plan: 02
type: execute
wave: 2
depends_on: ["115-03"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
  - tests/orchestrators/import/execute.test.ts
autonomous: true
requirements: [MOD-08]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-115-02: the owner imports its paired source directly and no longer reaches importClaudeSettings through the barrel, clearing the recorded wrong-import violation."
    - "D-115-04: the coverage-exception pragma is gone from execute.ts, and at least one case calls importClaudeSettings with no dependency bundle at all so every default collaborator resolver executes for real."
    - "D-115-07 and D-115-08: every one of the eight public outcome types is produced through every one of its producer routes, and every matrix cell asserts the complete aggregated import result rather than only its own row."
    - "D-115-09: a failing FIRST entry proves the batch is not aborted and a failing MIDDLE entry proves the remainder is still processed and earlier commits stay intact, for both the marketplace pass and the plugin pass."
    - "D-05: the two structurally unreachable defensive arms in execute.ts are removed after a caller trace, the private block-status type is narrowed so a bad token is a compile error, and no seam, export, pragma, or coverage exception is added."
  artifacts:
    - path: tests/orchestrators/import/execute.test.ts
      provides: "Sole mirrored direct owner for orchestrators/import/execute.ts at 100 percent direct coverage"
    - path: extensions/pi-claude-marketplace/orchestrators/import/execute.ts
      provides: "Pragma-free cascade with its private block-status type narrowed and two unreachable arms removed"
  key_links:
    - from: tests/orchestrators/import/execute.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/import/execute.ts
      via: "direct importClaudeSettings import driving the injected ImportDeps bundle"
      pattern: "importClaudeSettings"
---

<objective>
Prove the import cascade exhaustively through its own source, and delete the last coverage
exception in the extension.

Purpose: `import/execute.ts` sits 21 branches, 2 functions, and 59 lines short of full direct
coverage, and its owner test is the recorded wrong-import violation — it reaches
`importClaudeSettings` through the barrel instead of through its own paired module. The file
also carries the only coverage-exception pragma in the whole `extensions/` tree, on the default
state-loader that no case ever reaches because every case injects a dependency bundle. D-115-04
closes that by proving the production default rather than keeping the exception.

D-115-03 settles the double strategy: this module already exposes `ImportDeps` as explicit
parameter-level dependency injection, and its contract is aggregation, so the collaborators are
injected. That makes the D-115-07 matrix cheap — almost none of it needs a filesystem.
D-115-08 draws the boundary: the matrix varies which fault each entry hits and which outcome
each collaborator returns, then asserts the composition's aggregated public reporting. It does
not re-derive why a lifecycle workflow failed internally; Phase 114 owns that directly and
duplicating a single-module oracle is forbidden.

Sizing note. Research put the honest matrix at 46 to 49 cells against 25 existing cases, so
roughly 22 to 25 net-new cases plus 25 rewrites, landing near 2,000 to 2,400 lines. That is
larger than one comfortable executor pass, so this plan is split into three ordered tasks
within the single pair rather than into a second plan — the pair contract permits exactly one
production pair per plan and per commit, and the roadmap fixes the phase at eight plans. Do not
shrink the matrix to fit; if a task will not fit, stop after it and report progress rather than
sampling representative cells.

Output: a pragma-free source with two unreachable arms removed, and an exhaustive,
contract-compliant owner at 100 percent direct coverage.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-composition-orchestrators/115-CONTEXT.md
@.planning/phases/115-composition-orchestrators/115-RESEARCH.md
@.planning/phases/115-composition-orchestrators/115-PATTERNS.md
@.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md
@.planning/inputs/unit-test-refactor-handoff/BEHAVIOR-CONTRACTS.yaml
@.planning/inputs/unit-test-refactor-handoff/PUBLIC-SURFACE.yaml
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the owner, remove the pragma and the unreachable arms, and fix the file-level hygiene</name>
  <precondition>P115-03 has landed: extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts imports from ../../../orchestrators/import/index.ts, so removing this suite's barrel import leaves the barrel with a production consumer and the dead-code gate clean.</precondition>
  <files>extensions/pi-claude-marketplace/orchestrators/import/execute.ts, tests/orchestrators/import/execute.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/import/index.ts
    - extensions/pi-claude-marketplace/orchestrators/types.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
    - extensions/pi-claude-marketplace/persistence/state-io.ts
    - tests/orchestrators/import/execute.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - scripts/check-corresponding-tests.mjs
    - .planning/phases/115-composition-orchestrators/115-RESEARCH.md
  </read_first>
  <action>
<!-- planner-discipline-allow: orchestrators/import/index.ts -->

Confirm the precondition first: `extensions/pi-claude-marketplace/edge/handlers/plugin/import.ts`
must already import from the barrel. If it does not, stop and report — removing this suite's
barrel import before that lands would leave the barrel with no consumer and break the
dead-code gate.

D-115-02 repoint. Change the owner's import of `importClaudeSettings` from
`../../../extensions/pi-claude-marketplace/orchestrators/import/index.ts` to
`../../../extensions/pi-claude-marketplace/orchestrators/import/execute.ts`. An owner test
imports its own source; the barrel is proved by its own owner. Confirm the correspondence gate
stops reporting the wrong-import violation for this file.

File-level hygiene, both of which are structural defects in this suite:

- Delete the module-scope environment mutation that runs at import time between the node
  builtins and the production imports — the line that assigns a freshly created temporary
  directory to the agent-directory variable near line 15. It is a process global mutated at
  module load, restored nowhere, whose temporary root is never removed, and it splits the
  import block in a way the import-ordering rule forbids. The need behind it is real: the
  cascade's configuration write-back would otherwise write fixture entries into the developer's
  real user-scope configuration. Satisfy that need per case instead, with the hermetic-scope
  pattern at `tests/orchestrators/marketplace/shared.test.ts:188-227` — one temporary root per
  case and one `t.after()` registered before the act phase that removes every root, restores
  `HOME`, and restores or deletes the agent-directory variable.
- Delete the blanket file-wide lint suppression on the first line. It is unexplained and
  whole-file, unlike every other suppression in this directory, which is a single next-line
  form carrying an inline justification. Async collaborator stubs that never await should
  return a resolved promise directly instead; if one narrow site still needs the suppression,
  use a justified next-line form.

D-115-04, the coverage exception. Delete the coverage-exception pragma on the default
state-loader near line 214. It is the only such pragma in the entire `extensions/` tree and the
milestone contract forbids coverage exceptions. Replace it with proof: add at least one case
that calls `importClaudeSettings` with no dependency bundle at all, against a real hermetic
tree, so all four default resolvers — state loader, settings loader, marketplace adder, and
plugin installer — execute for real in one act. Add per-resolver isolation cases if that single
case leaves any of the four right-hand operands uncovered. Prefer inputs that make the default
path terminate quickly and deterministically without a network reach; the git port is optional
on the options object, so pass the in-repo git fake with an explicit remote allow-list where a
marketplace add would otherwise be attempted.

D-05, the two unreachable defensive arms. Trace callers with `codegraph explore` (a
`.codegraph/` index exists at the repository root) and record the evidence in the summary
before editing.

- The marketplace-block message builder's trailing `default:` that throws on an unexpected
  block status. The private block type declares its status with a wide union, but the module
  assigns only three tokens to it and the switch lists all three plus the undefined case.
  Narrow that private type to exactly the three assigned tokens using an extraction over the
  wide union, then delete the `default:`. The type is declared privately inside this module, so
  nothing outside sees the change — this is one file, one pair, no seam, no export change.
- The exhaustiveness `default:` on the two-arm install outcome union. Both arms are explicit
  cases already; delete it.

If narrowing makes the compiler complain that a path returns no value, restructure so the final
legal case supplies the return; do not reintroduce a default and do not add a non-null
assertion. Preserve every public outcome: the returned result shape and the rendered rows must
not change.

Do not start the case normalization or the matrix in this task. Leave the suite passing.
  </action>
  <verify><automated>rg -q 'orchestrators/import/execute\.ts' tests/orchestrators/import/execute.test.ts &amp;&amp; ! rg -q 'orchestrators/import/index\.ts' tests/orchestrators/import/execute.test.ts &amp;&amp; ! rg -n 'c8 ignore|node:coverage ignore' extensions/pi-claude-marketplace/orchestrators/import/execute.ts &amp;&amp; ! rg -n '^/\* eslint-disable' tests/orchestrators/import/execute.test.ts &amp;&amp; ! rg -n 'mkdtempSync' tests/orchestrators/import/execute.test.ts &amp;&amp; node --test tests/orchestrators/import/execute.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint extensions/pi-claude-marketplace/orchestrators/import/execute.ts tests/orchestrators/import/execute.test.ts &amp;&amp; npm exec -- prettier --check extensions/pi-claude-marketplace/orchestrators/import/execute.ts tests/orchestrators/import/execute.test.ts &amp;&amp; npm run fallow &amp;&amp; node --test tests/orchestrators/import/index.test.ts tests/edge/handlers/import.test.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -q 'wrong-import: tests/orchestrators/import/execute\.test\.ts' &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/import/index.ts extensions/pi-claude-marketplace/edge/</automated></verify>
  <acceptance_criteria>
The owner imports `execute.ts` directly and no longer imports the barrel; the correspondence
gate stops reporting the wrong-import violation for it. The coverage-exception pragma is gone
from `execute.ts`, and at least one case calls `importClaudeSettings` with no dependency bundle
so the default resolvers run for real. The module-scope environment mutation and its unremoved
temporary root are gone, replaced by per-case hermetic scopes torn down through `t.after()`.
The file-wide lint suppression is gone. The two unreachable defensive arms are removed with a
recorded caller trace, and the private block-status type is narrowed so an unexpected token is
a compile error rather than a runtime throw. No seam, export, pragma, coverage exception, or
non-null assertion was added. `npm run typecheck`, `npm run fallow`, the barrel owner, and the
edge handler suite are all green, and neither the barrel nor anything under `edge/` changed.
  </acceptance_criteria>
  <done>The cascade source is pragma-free and arm-clean, and its owner imports its own module through a hermetic per-case scope.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize the twenty-five existing cascade cases</name>
  <files>tests/orchestrators/import/execute.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/types.ts
    - tests/orchestrators/import/execute.test.ts
    - tests/orchestrators/marketplace/add.test.ts
    - tests/orchestrators/auth-host.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - .claude/rules/typescript-unit-testing.md
    - docs/messaging-style-guide.md
  </read_first>
  <action>
Rewrite the twenty-five existing cases to the contract while preserving every behavior they
prove: the happy cascade, blocked-marketplace continuation, all four combinations of the agent
and MCP declaration propagation, the reload trailer, the plan's skipped-plugin warnings, the
post-commit diagnostics, cross-scope independence, and the five batched write-back cases.

Structure. One top-level `describe()` per exported entrypoint; do not nest. Every runtime case
carries separate lowercase `// arrange`, `// act`, `// assert` phases in that order,
blank-line separated, with the combined marker reserved for one throwing or rejecting
expression. Case titles state public behavior, may cite a durable requirement or decision ID,
and cite no plan, phase, wave, or ticket.

Assertions. One `assert.deepStrictEqual` on the complete returned execution result per case,
plus one on the complete captured notification array, plus complete bytes for any configuration
file the case writes back. Replace the piecemeal single-value and existence assertions. Build
every expected value by hand from the shipped grammar in `docs/messaging-style-guide.md` and
`docs/output-catalog.md`; never derive an expectation by calling a production builder,
projector, or formatter. Presentation-only inventories are alphabetical; behavior ordering is
contractual and must not be alphabetized. Assert typed errors by class and stable fields, not
by message substring.

Doubles, chosen per case rather than per file. This is the axis the rules care about most here:

- When the case's promise is only what the composition reports, the collaborator is a stub — a
  plain typed object checked with `satisfies ImportDeps` returning a canned outcome. Do not
  assert its call count.
- When the case's promise is which collaborator ran, with which scope, and in which order, it
  is a mock: `mock<ImportDeps["addMarketplace"]>({ exactParams: true, name: "add marketplace" })`
  and the equivalent for the installer, with every allowed call stated by exact arguments and
  an explicit verification at the end of the case, after the result and state assertions. No
  wildcard matcher, no unbounded count.
- Ordering across two collaborators cannot be checked by the mocking library, so when order is
  the promise, replace each promised method with a recorder that pushes the meaningful
  arguments into one shared log and compare the whole log as an array, then still verify every
  mock. The in-repo precedent is `tests/orchestrators/marketplace/shared.test.ts:585-591`.
- Replace the two doubles built by casting through `unknown` with the strict context and Pi
  mocks from `tests/orchestrators/marketplace/add.test.ts:173-201`, sized to the case's exact
  notification count. Sizing to zero is how a silent case proves silence.

No completed owner in this repository injects a dependency bundle through a strict mock, so
there is no in-repo analog for that half — follow the reference case in
`.claude/rules/typescript-unit-testing.md` and the mock discipline demonstrated at
`tests/orchestrators/auth-host.test.ts:159-170`, and say so in the summary rather than citing a
precedent that does not exist.

Data. Short self-describing literals, typed and checked with `satisfies`, built from seed
functions that return fresh values. Convert any row loop inside a case into a `for` loop that
wraps `test()`, with a `satisfies readonly {...}[]` tail on the row array.

Do not add the matrix cells in this task.
  </action>
  <verify><automated>node --test tests/orchestrators/import/execute.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/import/execute.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/import/execute.test.ts &amp;&amp; npm run fallow &amp;&amp; rg -q 't\.after\(' tests/orchestrators/import/execute.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/import/execute.test.ts &amp;&amp; git diff --check -- tests/orchestrators/import/execute.test.ts</automated></verify>
  <acceptance_criteria>
All twenty-five prior behaviors are still proved, now with separate lowercase arrange, act, and
assertion phases in every runtime case, one whole-value comparison per public surface, and
production-role value names. Every collaborator double matches its role: stubs where the
composition's report is the promise, strict mocks with exact parameters and explicit
verification where the interaction is the promise, and one shared ordered log where order is
the promise. No double assertion through `unknown`, wildcard matcher, or unbounded-count
expectation remains. No expected value is produced by production code. Every case owns its
temporary state and tears it down through `t.after()`.
  </acceptance_criteria>
  <done>The cascade owner is contract-shaped with role-correct, verified doubles.</done>
</task>

<task type="auto">
  <name>Task 3: Build the exhaustive outcome matrix and close every remaining branch</name>
  <files>tests/orchestrators/import/execute.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
    - extensions/pi-claude-marketplace/orchestrators/import/types.ts
    - extensions/pi-claude-marketplace/orchestrators/types.ts
    - extensions/pi-claude-marketplace/shared/errors.ts
    - extensions/pi-claude-marketplace/persistence/config-write-back.ts
    - tests/orchestrators/import/execute.test.ts
    - .planning/phases/115-composition-orchestrators/115-RESEARCH.md
    - docs/messaging-style-guide.md
  </read_first>
  <action>
Build the D-115-07 matrix. Re-run the direct coverage gate first and work from the current
zero-hit records; research pinned its numbers to an earlier tree and the two earlier tasks
changed both files.

Every one of the eight public outcome types must be produced, and four of them have more than
one producer route — read that requirement as every route, since a single route leaves its
sibling branch uncovered anyway. Enumerate at least:

- marketplace not yet recorded: added; a typed non-added outcome whose cause propagates; an
  undefined outcome, which the composition reports as having returned no outcome in orchestrated
  mode; and a throw;
- marketplace already recorded: the same-source skip, the different-source mismatch reported per
  dependent plugin, and the unknown-stored source that produces a block plus a diagnostic;
- the plan's skipped plugins, producing the unmappable-marketplace-source warning;
- a plugin under a blocked marketplace, which is silently skipped with no outcome;
- a plugin already recorded, producing the already-installed skip;
- real installs: installed; installed carrying post-commit warnings; each typed shape error the
  dispatcher discriminates, including already-installed, not-in-manifest, not-installable, and
  no-longer-installable; the concurrent-install error, which the current suite has never
  produced because its race case injects a shape error instead; another typed error; and an
  installer that throws;
- scope level: a rejecting state loader producing the settings-read diagnostic and aborting only
  that scope; settings diagnostics passing through; two scopes proved independent;
- the configuration write-back post-pass: a batched happy patch; an empty patch that is
  skipped; a mixed pass where only successes land; a per-scope invalid configuration aborting
  only that scope; a write that throws, producing a diagnostic; both defensive continues where a
  recorded marketplace name is absent from the ensure list, which is the case where the recorded
  name differs from the declared configuration key; the ensure-and-repair merge across its
  present, absent, and neither combinations; and both operands of the empty-patch predicate,
  which needs patches that are empty in one dimension only;
- the notification projection: block status added, updated, failed, and the undefined status
  that a skipped-existing marketplace carrying a plugin row produces; the four warning reasons
  folding to their content reasons, including the two reasons that are suppressed from the
  notification but still ride the returned result; the reload trailer; and the plural tally;
- the optional git port present and absent, which is a conditional spread.

D-115-08 boundary. Each cell provokes or injects one cause per outcome kind and then proves the
composition's continuation, ordering, tally, and notification effect. Do not re-derive why a
lifecycle workflow failed internally — Phase 114 owns those failure modes directly and
duplicating a single-module oracle is forbidden. Every cell asserts the complete aggregated
result, not only its own row.

D-115-09 continuation. Include a failing FIRST entry, proving the batch is not aborted, and a
failing MIDDLE entry, proving the remainder is still processed and earlier commits stay intact,
for the marketplace pass and for the plugin pass. Because every cell already asserts the whole
aggregated result, fold continuation into the failure cells by authoring each as a three-entry
batch with the fault in position one or two, rather than adding a separate block of cells.

Use the data-row idiom from `tests/orchestrators/marketplace/shared.test.ts:560-592` where a
family of cells differs only by input and expectation: a typed row array with a
`satisfies readonly {...}[]` tail, and a `for` loop that wraps `test()` so each row is its own
case with the row interpolated into the title. Never a loop inside one case, and never a
conditional in the loop body — different branches or expectations are separate named cases.

Finish by closing every remaining branch with a behavioral case. Do not add a coverage
exception, do not cast an impossible value into an entrypoint, and do not add a production
seam. If a branch will not close, report the exact branch, the reason, and the caller trace.
  </action>
  <verify><automated>node --test tests/orchestrators/import/execute.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/import/execute.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/import/execute.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/import/execute.test.ts &amp;&amp; npm run fallow &amp;&amp; rg -q 'ConcurrentInstallError' tests/orchestrators/import/execute.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/import/execute.test.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -q 'tests/orchestrators/import/' &amp;&amp; git diff --check -- tests/orchestrators/import/execute.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/marketplace extensions/pi-claude-marketplace/orchestrators/plugin extensions/pi-claude-marketplace/orchestrators/reconcile</automated></verify>
  <acceptance_criteria>
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/import/execute.ts`
reports 100 percent functions, lines, and branches with the owner run alone, and no coverage
exception was added. All eight public outcome types are produced through every producer route,
including the concurrent-install skip route the suite never produced before. Every matrix cell
asserts the complete aggregated execution result and the complete notification array. Failure
cells are authored as multi-entry batches with the fault in first and in middle position, so
continuation, ordering, and tally are proved together. No case re-derives a lifecycle
workflow's internal failure reason. No production module outside `import/execute.ts` changed.
The correspondence gate reports nothing under `tests/orchestrators/import/`. `npm run check` is
green end to end, allowing only the pre-existing local `format:check` noise from untracked
`.mcp.json` and `.planning/research/.cache/*.json` files, which this plan must not touch.
  </acceptance_criteria>
  <done>P115-02 is exhaustive, continuation-proving, and at 100 percent direct coverage.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded by this plan, so the package-legitimacy gate does not apply.

| Threat ID  | Category                          | Component                                | Severity | Disposition | Mitigation Plan                                                                                                                                    |
| ---------- | --------------------------------- | ---------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-115-02-A | Tampering                         | developer user-scope configuration       | high     | mitigate    | The import-time environment mutation is replaced by per-case hermetic scopes; the write-back cascade can only reach a temporary tree, and the restore is registered through `t.after()` before the act phase. |
| T-115-02-B | Spoofing / Information Disclosure | git remote reached by the default adder  | high     | mitigate    | The no-dependency-bundle cases pass the in-repo git fake with an explicit remote allow-list; injected-collaborator cases reach no transport at all.  |
| T-115-02-C | Repudiation                       | the last coverage exception in the tree  | high     | mitigate    | The pragma is deleted and its path proved by a real case; the prohibited-pattern scan fails the task if any pragma returns.                          |
| T-115-02-D | Input Validation                  | malformed settings and configuration     | medium   | mitigate    | Matrix cells feed malformed configuration and settings on purpose, exercising the existing schema validators rather than bypassing them.             |
| T-115-02-E | Tampering                         | removal of defensive production arms     | medium   | mitigate    | Each removal is preceded by a recorded caller trace; the private status type is narrowed so a bad token becomes a compile error instead of a runtime throw. |

</threat_model>

<verification>
Task 1 gates the repoint, the pragma deletion, and the arm removals on the correspondence gate,
typecheck, `fallow`, and the unchanged barrel and edge suites. Task 2 gates the normalization on
the focused run and the prohibited-pattern scan. Task 3 gates the matrix on the direct per-pair
coverage gate and a clean correspondence report for the whole import directory. Before
committing, run `pre-commit run --all-files` and `npm run check`; the only tolerated failure is
the pre-existing untracked-file `format:check` noise described in the acceptance criteria.
</verification>

<success_criteria>

- `orchestrators/import/execute.ts` reaches 100 percent direct functions, lines, and branches
  with its owner run alone, and carries no coverage exception.
- The owner imports its own paired source; the wrong-import violation is closed.
- All eight public outcome types are produced through every producer route, each cell asserting
  the complete aggregated result.
- Continuation holds with the failing entry in first and in middle position, for marketplaces
  and for plugins.
- Two unreachable defensive arms are removed with recorded caller traces and no public behavior
  change; no production module outside the pair is touched.

</success_criteria>

<output>
Create .planning/phases/115-composition-orchestrators/115-02-SUMMARY.md when done.
</output>
