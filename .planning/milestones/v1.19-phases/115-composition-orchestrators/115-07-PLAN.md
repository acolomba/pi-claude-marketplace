---
phase: 115-composition-orchestrators
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
  - tests/orchestrators/reconcile/notify.test.ts
  - tests/orchestrators/reconcile/notify-projection-edge.test.ts
  - tests/orchestrators/reconcile/plan-convergence.test.ts
  - tests/integration/reconcile-plan-convergence.test.ts
autonomous: true
requirements: [MOD-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "buildReconcileAppliedCascade projects every marketplace-subject and plugin-subject outcome arm, including the two arms that fold a reason into content, the bare partial-remove header, and a two-block cascade whose blocks come back sorted."
    - "buildReconcilePendingNotification and the plan-emptiness predicate are proved for a plan whose removal list carries plugin children, and the force-install resolve failure path is proved with a real fixture."
    - "D-05: the four defensive default arms in notify.ts whose scrutinee narrows to never are removed after a caller trace proves no legal value reaches them; no impossible value is smuggled past a type to keep them."
    - "D-115-05: the two supplemental projection cases live in the owner and their file is deleted in the same change, so no unique evidence is lost."
    - "D-115-06: the cross-layer fixed-point identity moves to tests/integration/ intact, keeping its four-module composition, and the correspondence gate stops reporting both reconcile supplementals."
  artifacts:
    - path: tests/orchestrators/reconcile/notify.test.ts
      provides: "Sole mirrored direct owner for orchestrators/reconcile/notify.ts at 100 percent direct coverage"
    - path: tests/integration/reconcile-plan-convergence.test.ts
      provides: "Relocated cross-layer plan/merge/config/source fixed-point identity, unchanged in substance"
    - path: tests/orchestrators/reconcile/notify-projection-edge.test.ts
      provides: "Removed after both cases are present in the owner"
  key_links:
    - from: tests/orchestrators/reconcile/notify.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
      via: "direct projection-entrypoint imports asserted as whole returned messages"
      pattern: "buildReconcileAppliedCascade|buildReconcilePendingNotification"
    - from: tests/integration/reconcile-plan-convergence.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
      via: "cross-layer fixed-point composition, excluded from the correspondence gate by root"
      pattern: "planReconcile"
---

<objective>
Complete the reconcile projection owner and clear both reconcile correspondence violations.

Purpose: `notify.ts` is the cheapest large gap in the phase and the one every other reconcile
pair depends on. Its pending (preview) projection is thoroughly proved; its applied-cascade
projection is barely proved. The 23 uncovered branches and three uncovered functions are almost
exactly the marketplace-subject half of the marketplace-outcome projector plus the
plugin-failure half of the plugin-outcome projector — all of which are pure functions of an
outcome array with no filesystem, no orchestrator, and no fake required.

Sequencing matters: `apply.ts:67` imports `buildReconcileAppliedCascade`, so the outcome
vocabulary P115-05 must produce is exactly the vocabulary this pair must project. Pinning the
projection first lets P115-05 assert against a settled row shape.

This plan also carries the two reconcile supplementals to their homes. D-115-05 absorbs the
projection-edge file into this owner — it is the only current prover of the reason-to-content
fallback and of the bare partial-remove header, so absorption and deletion must be one change.
D-115-06 relocates the cross-layer fixed-point identity into `tests/integration/`, where the
correspondence gate excludes it by root, with no allow-list entry and no flattening.

Output: a 100-percent projection owner, four fewer unreachable arms in production, one deleted
supplemental, and one relocated integration.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-composition-orchestrators/115-CONTEXT.md
@.planning/phases/115-composition-orchestrators/115-RESEARCH.md
@.planning/phases/115-composition-orchestrators/115-PATTERNS.md
@.planning/phases/114-plugin-and-marketplace-lifecycle/114-CONTEXT.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
@docs/messaging-style-guide.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the cross-layer fixed-point identity into the integration suite</name>
  <files>tests/orchestrators/reconcile/plan-convergence.test.ts, tests/integration/reconcile-plan-convergence.test.ts</files>
  <read_first>
    - tests/orchestrators/reconcile/plan-convergence.test.ts
    - scripts/check-corresponding-tests.mjs
    - .planning/phases/115-composition-orchestrators/115-CONTEXT.md
  </read_first>
  <action>
D-115-06 carrier work. `git mv tests/orchestrators/reconcile/plan-convergence.test.ts
tests/integration/reconcile-plan-convergence.test.ts` so history follows the file. Use
`git mv`; do not create and delete.

The file composes `planReconcile`, `mergeScopeConfigs`, `buildConfigFromState`, and
`domain/source.ts` into a fixed-point identity that no single owner honestly owns. Keep that
identity: do not flatten it into an owner, do not split it, do not add a correspondence-gate
exception, and preserve its assertions unless a shipped contract changed.

The only edits are mechanical. The relative depth shortens by one segment, so each of the
production import specifiers loses one leading parent segment. Confirm every import resolves
and the file type-checks. Leave the case titles and bodies otherwise untouched — this task is a
relocation, not a rewrite; the contract normalization of an integration file is not Phase 115
work.

Confirm the relocated file is picked up by the integration runner (its glob is
`tests/integration/**/*.test.ts`) and that the correspondence gate no longer reports it, since
the gate excludes the `integration` root by name.
  </action>
  <verify><automated>test ! -e tests/orchestrators/reconcile/plan-convergence.test.ts &amp;&amp; test -e tests/integration/reconcile-plan-convergence.test.ts &amp;&amp; npm run typecheck &amp;&amp; node --test tests/integration/reconcile-plan-convergence.test.ts &amp;&amp; npm run test:integration &amp;&amp; npm exec -- eslint tests/integration/reconcile-plan-convergence.test.ts &amp;&amp; npm exec -- prettier --check tests/integration/reconcile-plan-convergence.test.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -q 'plan-convergence' &amp;&amp; git log --follow --oneline -1 -- tests/integration/reconcile-plan-convergence.test.ts</automated></verify>
  <acceptance_criteria>
The old path is absent, `tests/integration/reconcile-plan-convergence.test.ts` exists and was
produced by `git mv`, `npm run typecheck` is green, the file passes under
`npm run test:integration`, and the correspondence gate no longer names it. Its case count and
assertions are unchanged apart from the import-depth edit.
  </acceptance_criteria>
  <done>The cross-layer identity lives under the integration root with its history and substance intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the unreachable projection defaults and normalize the existing owner</name>
  <files>extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts, tests/orchestrators/reconcile/notify.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/reconcile.messaging.ts
    - tests/orchestrators/reconcile/notify.test.ts
    - tests/orchestrators/reconcile/plan.test.ts
    - .planning/phases/115-composition-orchestrators/115-RESEARCH.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
First, the production disposition, under D-05 unreachable-code removal.

Four defensive `default:` arms in `notify.ts` cannot be reached by any legal value: the
marketplace-block message builder's arm near line 162, whose scrutinee is a closed three-token
status union plus an explicit undefined case; the marketplace-outcome projector's arm near line
726, whose parameter is an extraction over exactly the seven kinds the switch lists; the
plugin-outcome projector's arm near line 835, built the same way over the nine plugin kinds;
and the dispatching projector's arm near line 868, which a test could only reach by casting an
invalid discriminant into the entrypoint — which the type-discipline rule forbids, and which
the repository's two precedents for exercising an exhaustiveness helper deliberately avoid by
calling that helper from its own owner instead.

Before deleting anything, trace callers with `codegraph explore` (a `.codegraph/` index exists
at the repository root) and record in the summary, per arm: the scrutinee's declared type, the
complete set of cases the switch already lists, and every call site that reaches the function.
Then delete the four arms. Preserve every public outcome; the module's returned messages must
not change. This is the same recipe the file already documents for a previously deleted
defensive throw over the same block status.

If deleting the marketplace-block arm makes the compiler complain that a code path returns no
value, do not reintroduce a default: restructure so the final legal case supplies the return,
keeping the switch exhaustive over the closed union. Add no export, no seam, no pragma, and no
coverage exception. Change no file other than `notify.ts` under `extensions/`.

Second, normalize the owner. Rewrite the existing cases in
`tests/orchestrators/reconcile/notify.test.ts` to the contract, using
`tests/orchestrators/reconcile/plan.test.ts` as the exact analog — same directory, same layer,
same regime: a pure function of literal inputs asserted with one whole-value comparison.

- One top-level `describe()` per exported entrypoint. Do not nest.
- Every runtime case carries separate lowercase `// arrange`, `// act`, `// assert` phases in
  that order, blank-line separated; the combined marker is reserved for a single throwing or
  rejecting expression.
- Replace the piecemeal single-value and existence assertions with one
  `assert.deepStrictEqual` on the complete returned message. Prefer the strict spelling for
  explicitness; an existing loose deep comparison is not itself a defect but should be
  upgraded as cases are rewritten.
- Rename the 41 placeholder-named values to production roles: `cascade`, `message`, `plan`,
  `blocks`. The forbidden placeholder names must not remain.
- Convert any row loop that lives inside a single case into a `for` loop that wraps `test()`,
  emitting one sibling case per row with the row interpolated into the title, and put a
  `satisfies readonly {...}[]` tail on the row array so a missing or misspelled field is a
  compile error.
- Seed factories are functions that return fresh values and spread-copy their inputs, so no
  case shares mutable state. Take that shape from `plan.test.ts:26-93`.
- Do not build an expected value by calling a production projector or formatter. An input
  producer may be a production function; an expectation may not.
- Case titles state public behavior and cite no plan, phase, wave, or ticket.
- The projection entrypoints take plain data, so most cases need no double, no filesystem, and
  no context handle. Where a case does need a context handle, use a strict mock with exact
  parameters created inside the case and verified at the end.

Do not delete the supplemental file in this task and do not add the new projection cells yet —
both belong to Task 3.
  </action>
  <verify><automated>node --test tests/orchestrators/reconcile/notify.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts tests/orchestrators/reconcile/notify.test.ts &amp;&amp; npm exec -- prettier --check extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts tests/orchestrators/reconcile/notify.test.ts &amp;&amp; npm run fallow &amp;&amp; node --test tests/orchestrators/reconcile/pending.test.ts tests/orchestrators/reconcile/apply.test.ts tests/orchestrators/reconcile/notify-projection-edge.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo\()|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)|const (msg|result|data|value|subject|sut) ' tests/orchestrators/reconcile/notify.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts extensions/pi-claude-marketplace/orchestrators/reconcile/pending.ts</automated></verify>
  <acceptance_criteria>
The four unreachable defensive default arms are gone from `notify.ts`, each backed by a
recorded caller trace naming the scrutinee type, the listed cases, and the call sites; no
impossible value was fabricated to keep any of them, and no seam, export, pragma, or coverage
exception was added. `npm run typecheck` and `npm run fallow` are green, and the module's
public messages are unchanged — the sibling reconcile suites (`pending`, `apply`, and the
projection-edge supplemental, which is still present at the end of this task) all still pass.
Every runtime case in the owner has separate lowercase arrange, act, and assertion phases, one
whole-value comparison per public surface, production-role value names, and row loops that wrap
`test()` rather than looping inside a case. No expected value is produced by a production
projector. Only `notify.ts` changed under `extensions/`.
  </acceptance_criteria>
  <done>The projection module has no unreachable arms and its owner is contract-shaped.</done>
</task>

<task type="auto">
  <name>Task 3: Absorb the supplemental, close the applied-cascade projection, and reach full coverage</name>
  <files>tests/orchestrators/reconcile/notify.test.ts, tests/orchestrators/reconcile/notify-projection-edge.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts
    - extensions/pi-claude-marketplace/orchestrators/reconcile/types.ts
    - tests/orchestrators/reconcile/notify-projection-edge.test.ts
    - tests/orchestrators/reconcile/notify.test.ts
    - docs/messaging-style-guide.md
    - docs/output-catalog.md
    - .planning/phases/115-composition-orchestrators/115-RESEARCH.md
  </read_first>
  <action>
D-115-05 absorption and coverage closure, delivered as one change so no unique evidence is
lost between steps.

Absorb both cases from `tests/orchestrators/reconcile/notify-projection-edge.test.ts` into the
owner — the bare partial-remove marketplace header with no marketplace-level reasons brace, and
the reason-to-content fallback that maps the structural marketplace-absent marker onto the
"not found" content reason. Both are already in compliant phase-marked form; transplant them,
upgrade the loose deep comparison to the strict spelling, retitle them to state public behavior
without a ticket reference, and place them under the applied-cascade entrypoint's `describe()`.
Then delete `tests/orchestrators/reconcile/notify-projection-edge.test.ts` in the same change.
Do not delete it first and re-add the evidence afterwards.

Close the remaining coverage. Add cases that route each of these through the public projection
entrypoints, asserting the complete returned message every time:

- the marketplace-block message builder's added and removed arms;
- the marketplace-outcome projector's added, removed, add-failed, remove-failed, and
  remove-partial arms — the two failed arms are what exercise the reason-to-content fold;
- the plugin-outcome projector's uninstalled arm, disabled arm, and the four failure arms
  (install-failed, uninstall-failed, enable-failed, disable-failed);
- the dispatching projector's five marketplace-subject cases;
- an applied cascade carrying two marketplaces, so the block sort comparator actually runs and
  the returned block order is asserted as a whole ordered array — a single-block cascade never
  calls it;
- a reconcile plan whose removal list carries a marketplace with plugin children, which is what
  makes the plan-emptiness predicate evaluate its inner per-marketplace test;
- the force-install preview path where the strict resolve throws, so the surrounding catch
  runs. That is the one case in this pair that needs a real fixture: build a case-owned
  temporary tree, register removal and any environment restore through `t.after()`, and shape
  the fixture so the resolve genuinely fails. Do not fake the resolver and do not add a seam.

Every case supplies complete, case-local, independently authored inputs and one complete
expected message. Take the exact expected bytes and row grammar from `docs/messaging-style-guide.md`
and `docs/output-catalog.md`; the shipped grammar outranks any stale expectation in the current
suite. Presentation-only inventories are alphabetical; behavior ordering is contractual and
must not be alphabetized.

Finish by running the direct coverage gate for the pair and closing every remaining branch with
a behavioral case. If a branch will not close, do not add an exception and do not cast an
invalid value into an entrypoint: report the exact branch and the reason.
  </action>
  <verify><automated>test ! -e tests/orchestrators/reconcile/notify-projection-edge.test.ts &amp;&amp; node --test tests/orchestrators/reconcile/notify.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/reconcile/notify.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/reconcile/notify.test.ts &amp;&amp; npm run fallow &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -q 'notify-projection-edge|plan-convergence' &amp;&amp; rg -q 'mp-remove-partial' tests/orchestrators/reconcile/notify.test.ts &amp;&amp; rg -q 'not found' tests/orchestrators/reconcile/notify.test.ts &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|//[[:space:]]+(Arrange|Act|Assert)' tests/orchestrators/reconcile/notify.test.ts &amp;&amp; git diff --check -- tests/orchestrators/reconcile/notify.test.ts</automated></verify>
  <acceptance_criteria>
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts`
reports 100 percent functions, lines, and branches with the owner run alone, and no coverage
exception was added. Both supplemental cases are present in the owner and
`tests/orchestrators/reconcile/notify-projection-edge.test.ts` is deleted in the same change.
The correspondence gate reports neither reconcile supplemental; the remaining violations are
only the ones deferred to Phases 116 and 117. A two-block applied cascade asserts the complete
ordered block array, a removal-with-children plan exercises the emptiness predicate's inner
test, and a real fixture drives the force-install resolve failure. Every case asserts one
complete message built from the shipped grammar. `npm run check` is green end to end, allowing
only the pre-existing local `format:check` noise from untracked `.mcp.json` and
`.planning/research/.cache/*.json` files, which this plan must not touch.
  </acceptance_criteria>
  <done>P115-07 is at 100 percent direct coverage, owns its absorbed evidence, and both reconcile correspondence violations are closed.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded by this plan, so the package-legitimacy gate does not apply.

| Threat ID  | Category               | Component                             | Severity | Disposition | Mitigation Plan                                                                                                                              |
| ---------- | ---------------------- | ------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-115-07-A | Repudiation            | deleted supplemental evidence         | high     | mitigate    | Absorption and deletion land as one change; the gate scans the owner for both absorbed behaviors before the file is removed.                    |
| T-115-07-B | Tampering              | removal of defensive production arms  | high     | mitigate    | Each removal is preceded by a recorded caller trace proving the scrutinee narrows to no legal value; sibling reconcile suites must pass unchanged. |
| T-115-07-C | Information Disclosure | the one fixture-backed case           | medium   | mitigate    | Case-owned temporary tree with removal and environment restore through `t.after()`; the projection entrypoints reach no network, git, or credential port. |
| T-115-07-D | Tampering              | relocated integration identity        | medium   | mitigate    | `git mv` preserves history; imports change depth only; assertions and case count are unchanged and re-run under the integration runner.          |

</threat_model>

<verification>
Task 1 gates the relocation on the absent old path, the present new path, typecheck, the
integration runner, and the correspondence gate. Task 2 gates the production removals on
typecheck, `fallow`, and the unchanged sibling reconcile suites. Task 3 gates the owner on the
focused run, the direct per-pair coverage gate, the absorbed-evidence scan, and both
correspondence lines disappearing. Before committing, run `pre-commit run --all-files` and
`npm run check`; the only tolerated failure is the pre-existing untracked-file `format:check`
noise described in the acceptance criteria.
</verification>

<success_criteria>

- `orchestrators/reconcile/notify.ts` reaches 100 percent direct functions, lines, and branches
  with its owner run alone.
- Every applied-cascade outcome arm and both projection entrypoints are proved with complete,
  independently authored messages.
- Four unreachable defensive arms are gone from production with recorded caller-trace evidence
  and no public behavior change.
- The projection-edge supplemental is absorbed and deleted in one change, and the fixed-point
  identity lives under `tests/integration/` intact.
- The correspondence gate reports neither reconcile supplemental.

</success_criteria>

<output>
Create .planning/phases/115-composition-orchestrators/115-07-SUMMARY.md when done.
</output>
