---
phase: 106-test-architecture-foundation
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - scripts/check-corresponding-tests.mjs
  - package.json
autonomous: true
requirements: [PAIR-01, PAIR-02, PAIR-03, PAIR-04]
must_haves:
  truths:
    - The gate derives every expected test path from the production tree.
    - The gate reports the complete missing-pair set.
    - Type-only modules and barrels receive no exemptions.
    - A corresponding test directly references its paired production module.
  prohibitions:
    - Do not add an exemption list.
    - Do not classify old test moves as final ownership decisions.
    - Do not make the migration gate part of `npm run check` before the tree passes.
---

<objective>
Add one structural corresponding-test gate for the migration.

The gate is available during the migration. The completed phase wires it into
the quality command only after the tree passes.
</objective>

<tasks>

<task type="auto">
  <name>Implement the corresponding-test gate</name>
  <files>scripts/check-corresponding-tests.mjs, package.json</files>
  <action>
Derive the mirrored test path for every production TypeScript module. Report
missing tests, duplicate ownership, and a corresponding test that does not
reference its paired source module.
  </action>
  <verify>
Run the gate on the current tree. It must report the known migration gaps and
return a nonzero status.
  </verify>
</task>

<task type="auto">
  <name>Prove the clean and failing structural paths</name>
  <files>none</files>
  <action>
Run the gate against a temporary minimal tree. Plant one missing test and one
wrong import. Make sure that each violation fails. Remove each violation and
make sure that the clean tree passes.
  </action>
  <verify>Record each command and status in the phase summary.</verify>
</task>

</tasks>
