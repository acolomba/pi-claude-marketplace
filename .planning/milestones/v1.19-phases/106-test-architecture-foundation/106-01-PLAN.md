---
phase: 106-test-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/test-coverage-direct.mjs
autonomous: true
requirements: [COV-02, COV-03]
must_haves:
  truths:
    - A source path and its mirrored test path resolve to the same pair.
    - A missing, ambiguous, or unsupported path returns an error.
    - The runner reads only the paired source record from LCOV.
    - A type-only module passes without a fabricated LCOV record.
  prohibitions:
    - Do not add a coverage exclusion or baseline count.
    - Do not restore the abandoned sharded runner.
    - Do not hide a missing pair during all-pair selection.
---

<objective>
Add one direct-coverage runner and the strict interaction-mock dependency.

The runner supports focused, changed-pair, and all-pair modes. All modes use one
path mapper and one LCOV evaluator.
</objective>

<tasks>

<task type="auto">
  <name>Add the strict mock dependency and direct-coverage commands</name>
  <files>package.json, package-lock.json</files>
  <action>
Add `strong-mock` as a development dependency. Add the focused and all-pair
commands from the TypeScript unit testing guidelines.
  </action>
  <verify>npm install completes without an audit error.</verify>
</task>

<task type="auto">
  <name>Implement the fail-closed direct-coverage runner</name>
  <files>scripts/test-coverage-direct.mjs</files>
  <action>
Map `extensions/pi-claude-marketplace/<path>.ts` to
`tests/<path>.test.ts`. Accept either path as focused input. Select changed
pairs when no path is present. Select every pair with `--all`.

Run each test alone with Node coverage and an LCOV reporter. Read the exact
paired source record. Require complete functions, lines, and branches. Use the
TypeScript compiler to identify a module with no emitted runtime statements.
  </action>
  <verify>
`npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/name.ts`
reports the current uncovered lines and branches.
  </verify>
</task>

</tasks>
