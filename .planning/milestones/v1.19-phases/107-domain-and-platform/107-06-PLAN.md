---
phase: 107-domain-and-platform
plan: 06
type: execute
wave: 1
depends_on: [107-05]
files_modified:
  - tests/domain/components/hook-if-targets.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - The table exposes exactly Bash, Read, Edit, and Write in precedence order.
    - Each prefix has its complete Pi event set and target field.
  prohibitions:
    - Do not reproduce parser or runtime predicate behavior in this data test.
    - Do not preserve architecture-move comments.
---

<objective>
Add the missing mirror for `domain/components/hook-if-targets.ts`.

Own the complete exported mapping and its public key order. Leave parser and
predicate behavior with their production modules.
</objective>

<tasks>

<task type="auto">
  <name>Add the hook if-target table test</name>
  <files>tests/domain/components/hook-if-targets.test.ts</files>
  <action>
Compare the complete table with independent sets and fields. Compare the
ordered public keys in a separate case.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
