---
phase: 107-domain-and-platform
plan: 13
type: execute
wave: 1
depends_on: [107-12]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hook-events.ts
  - tests/domain/components/hook-events.test.ts
  - tests/architecture/hooks-supportability.test.ts
  - tests/architecture/hooks-translators.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - The admitted event tuple contains the complete ordered public set.
    - The public tool-event tuple contains only the three tool events.
    - Dispatchability accepts each admitted event and rejects other names.
    - Non-tool matcher fields and closed sets match the complete contract.
    - The StopFailure type contains the complete error vocabulary.
  prohibitions:
    - Do not expose internal event tuples or helper types for tests.
    - Do not keep a second architecture test for this module.
    - Do not derive expected tables from production data.
    - Do not keep design-history comments in the test.
---

<objective>
Give the hook event metadata module one direct mirror.

Test each public table, type boundary, and guard with independent expected
values. Keep translator totality independent from private domain metadata.
</objective>

<tasks>

<task type="auto">
  <name>Replace the hook event metadata test</name>
  <files>
extensions/pi-claude-marketplace/domain/components/hook-events.ts,
tests/domain/components/hook-events.test.ts,
tests/architecture/hooks-supportability.test.ts,
tests/architecture/hooks-translators.test.ts
  </files>
  <action>
Make the dispatch tuple and non-tool helper type private. Replace the
architecture test with a corresponding mirror. Use the translator test's own
event list for its cross-module totality check.
  </action>
  <verify>
Run the focused test, affected architecture test, ESLint, TypeScript, and
direct coverage for the pair.
  </verify>
</task>

</tasks>
