---
phase: 107-domain-and-platform
plan: 17
type: execute
wave: 1
depends_on: [107-16]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hooks.ts
  - extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts
  - tests/domain/components/hooks.test.ts
  - tests/domain/components/hooks/matcher.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Match-all sentinels remain distinct from literal tool matchers.
    - Every supported Claude tool name maps to its Pi tool name.
    - Valid MCP literals remain opaque matcher values.
    - Regex syntax, malformed alternation, and unmapped tools fail closed.
    - Existing imports from hooks.ts remain valid.
  prohibitions:
    - Do not change matcher grammar or dispatch behavior.
    - Do not duplicate matcher cases in the hooks parser test.
    - Do not add comments about the extraction history.
---

<objective>
Extract and refactor the hook matcher parser source-test pair.

Keep the existing public import path as a compatibility re-export. Give the
matcher implementation a direct corresponding test with complete coverage.
</objective>

<tasks>

<task type="auto">
  <name>Extract and test hook matcher parsing</name>
  <files>
extensions/pi-claude-marketplace/domain/components/hooks.ts,
extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts,
tests/domain/components/hooks.test.ts,
tests/domain/components/hooks/matcher.test.ts
  </files>
  <action>
Move the matcher grammar and parser to its own production module. Re-export
the public parser and type from hooks.ts. Replace the old mixed-suite matcher
block with focused, case-isolated tests beside the new module.
  </action>
  <verify>
Run the focused tests, affected hook tests, ESLint, TypeScript, and direct
coverage for the matcher module.
  </verify>
</task>

</tasks>
