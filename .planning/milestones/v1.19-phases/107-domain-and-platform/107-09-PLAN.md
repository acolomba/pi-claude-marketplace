---
phase: 107-domain-and-platform
plan: 09
type: execute
wave: 1
depends_on: [107-08]
files_modified:
  - tests/domain/auth-registry.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Each provider exposes its complete stable descriptor data.
    - Host matching accepts only the exact SaaS hostname.
    - Credential mapping returns the complete provider-specific credentials.
    - Registry lookup returns the exact provider binding or undefined.
    - Compile-time checks own the provider port.
  prohibitions:
    - Do not run the device-flow engine in the registry test.
    - Do not use generic credential or HTTP helpers.
    - Do not preserve provider-move history in comments.
---

<objective>
Refactor the existing `domain/auth-registry.ts` mirror.

Keep descriptor data, host matching, credential mapping, and lookup here. Leave
device-flow behavior with `domain/github-auth.ts`.
</objective>

<tasks>

<task type="auto">
  <name>Replace the auth registry test</name>
  <files>tests/domain/auth-registry.test.ts</files>
  <action>
Group cases by the two provider exports and the lookup export. Compare complete
descriptor data and credentials. Add compile-time provider checks.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
