---
phase: 107-domain-and-platform
plan: 12
type: execute
wave: 1
depends_on: [107-11]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts
  - tests/domain/components/hook-tool-names.test.ts
  - tests/architecture/hooks-tool-name-map.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Every built-in Pi tool name maps to its Claude spelling.
    - Unknown and Claude-form names pass through unchanged.
    - The public reverse map contains every supported Claude-to-Pi entry.
    - The Pi tool-name type excludes open-ended custom names.
  prohibitions:
    - Do not expose the forward table or its conditional helper type for tests.
    - Do not keep a second architecture test for this module.
    - Do not derive expected mappings from production data.
    - Do not keep design-history comments in the test.
---

<objective>
Give the hook tool-name module one direct mirror.

Test its public table, translation function, and type contract. Remove exports
that existed only for the old architecture test.
</objective>

<tasks>

<task type="auto">
  <name>Replace the hook tool-name test</name>
  <files>
extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts,
tests/domain/components/hook-tool-names.test.ts,
tests/architecture/hooks-tool-name-map.test.ts
  </files>
  <action>
Make the forward map and conditional type private. Replace the architecture
test with a corresponding mirror that uses independent expected values and the
required arrange, act, and assert structure.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
