---
phase: 107-domain-and-platform
plan: 18
type: execute
wave: 1
depends_on: [107-17]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hooks.ts
  - extensions/pi-claude-marketplace/domain/components/hooks/schema.ts
  - tests/domain/components/hooks.test.ts
  - tests/domain/components/hooks/schema.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - The hook configuration remains an open record of event arrays.
    - Command handlers still require a string command.
    - Other handler types remain forward-compatible.
    - Optional hook fields retain their current runtime validation.
    - Existing imports from hooks.ts remain valid.
  prohibitions:
    - Do not make any schema level strict against unknown fields.
    - Do not inspect production source text from the corresponding test.
    - Do not duplicate schema cases in the hooks parser test.
    - Do not add comments about the extraction history.
---

<objective>
Extract and refactor the hook configuration schema source-test pair.

Keep the existing public import path as a compatibility re-export. Test the
runtime wire contract through the compiled validator instead of source text.
</objective>

<tasks>

<task type="auto">
  <name>Extract and test the hook schema</name>
  <files>
extensions/pi-claude-marketplace/domain/components/hooks.ts,
extensions/pi-claude-marketplace/domain/components/hooks/schema.ts,
tests/domain/components/hooks.test.ts,
tests/domain/components/hooks/schema.test.ts
  </files>
  <action>
Move the hook handler type, TypeBox schema, compiled validator, and config type
to one production module. Re-export its public API from hooks.ts. Replace the
mixed-suite schema block with direct validator cases.
  </action>
  <verify>
Run the focused tests, affected hook tests, ESLint, TypeScript, and direct
coverage for the schema module.
  </verify>
</task>

</tasks>
