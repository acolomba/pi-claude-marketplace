---
phase: 107-domain-and-platform
plan: 19
type: execute
wave: 1
depends_on: [107-18]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hooks.ts
  - extensions/pi-claude-marketplace/domain/components/hooks/partition.ts
  - tests/domain/components/hooks.test.ts
  - tests/domain/components/hooks/partition.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Unknown events are reported and removed.
    - Unsupported matcher groups are reported and removed.
    - Non-command handlers are reported without removing sibling commands.
    - Supported groups retain their event, group, and handler order.
    - Existing imports from hooks.ts remain valid.
  prohibitions:
    - Do not expose dependency injection only to reach dead branches.
    - Do not change the supported hook subset.
    - Do not duplicate partition cases in the hooks parser test.
    - Do not add comments about the extraction history.
---

<objective>
Extract and refactor the hook partition source-test pair.

Keep the existing public import path as a compatibility re-export. Remove the
unreachable fallback for immutable event tables and test every authored input
path directly.
</objective>

<tasks>

<task type="auto">
  <name>Extract and test hook partitioning</name>
  <files>
extensions/pi-claude-marketplace/domain/components/hooks.ts,
extensions/pi-claude-marketplace/domain/components/hooks/partition.ts,
tests/domain/components/hooks.test.ts,
tests/domain/components/hooks/partition.test.ts
  </files>
  <action>
Move partition types and behavior to one production module. Re-export its
public API from hooks.ts. Replace the mixed-suite partition block with focused
cases that pin supported output, dropped entries, and stable order.
  </action>
  <verify>
Run the focused tests, affected hook tests, ESLint, TypeScript, and direct
coverage for the partition module.
  </verify>
</task>

</tasks>
