---
phase: 107-domain-and-platform
plan: 10
type: execute
wave: 1
depends_on: [107-09]
files_modified:
  - extensions/pi-claude-marketplace/domain/version.ts
  - tests/domain/version.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Hash versions use a fixed 12-character lowercase SHA-256 prefix.
    - Hashing is deterministic across entry order and nested paths.
    - Hashing ignores .git, node_modules, .DS_Store, and symlink target bytes.
    - UTF-8 BOM and CRLF normalization preserve the documented hash.
    - Standalone carriage returns remain significant.
    - SHA versions use the first 12 commit characters.
  prohibitions:
    - Do not write fixtures into the repository.
    - Do not keep exports used only by tests.
    - Do not test shared version rendering here.
    - Do not keep an unreachable buffer-index guard.
---

<objective>
Refactor the existing `domain/version.ts` pair.

Replace repository fixtures with private temporary trees. Prove private rules
through exported hash behavior. Remove the handoff's identified test-only
surface and one unreachable guard.
</objective>

<tasks>

<task type="auto">
  <name>Narrow the version module surface</name>
  <files>extensions/pi-claude-marketplace/domain/version.ts</files>
  <action>
Make the walk filter private. Remove the unused SHA predicate and regular
expression exports. Use an in-range Buffer read without an unreachable guard.
  </action>
  <verify>npm run typecheck</verify>
</task>

<task type="auto">
  <name>Replace the version test</name>
  <files>tests/domain/version.test.ts</files>
  <action>
Give every filesystem case a fresh temporary directory. Compare fixed complete
hashes for empty, nested, ignored, normalized, carriage-return, and symlink
trees. Keep SHA version cases with the SHA export.
  </action>
  <verify>npm run test:coverage:direct -- tests/domain/version.test.ts</verify>
</task>

</tasks>
