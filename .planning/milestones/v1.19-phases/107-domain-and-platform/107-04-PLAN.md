---
phase: 107-domain-and-platform
plan: 04
type: execute
wave: 1
depends_on: [107-03]
files_modified:
  - tests/domain/plugin-root.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - A valid absolute path is returned unchanged and receives the brand.
    - Rebranding an already branded root is idempotent.
    - Empty, null-byte, delimiter-bearing, and relative roots fail completely.
    - Absolute paths with parent segments preserve their input spelling.
  prohibitions:
    - Do not preserve coverage-suite or implementation-history comments.
    - Do not infer a normalized return value that the export does not promise.
---

<objective>
Refactor the existing `domain/plugin-root.ts` mirror.

Keep its security boundary cases while making the test cross-platform and
compliant with the case-layout and assertion rules.
</objective>

<tasks>

<task type="auto">
  <name>Replace the plugin root test</name>
  <files>tests/domain/plugin-root.test.ts</files>
  <action>
Build native absolute paths with `node:path`. Compare every returned path and
every native error completely. Add compile-time ownership of the brand.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
