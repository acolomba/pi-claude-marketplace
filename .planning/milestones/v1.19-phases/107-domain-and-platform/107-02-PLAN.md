---
phase: 107-domain-and-platform
plan: 02
type: execute
wave: 1
depends_on: [107-01]
files_modified:
  - tests/domain/components/mcp.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - The exported schema describes a string-keyed object with unknown values.
    - The compiled validator accepts empty and populated server maps.
    - The compiled validator rejects values that are not records.
  prohibitions:
    - Do not test TypeBox internals.
    - Do not leave MCP schema ownership in another module's corresponding test.
---

<objective>
Add the missing corresponding test for `domain/components/mcp.ts`.

Test both runtime exports through their public JSON-schema and validation
contracts.
</objective>

<tasks>

<task type="auto">
  <name>Add the MCP component test</name>
  <files>tests/domain/components/mcp.test.ts</files>
  <action>
Use one top-level group per export. Compare the schema as a complete JSON-schema
value. Use sibling cases for accepted and rejected validator inputs.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
