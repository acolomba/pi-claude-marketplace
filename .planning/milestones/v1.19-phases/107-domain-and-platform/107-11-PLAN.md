---
phase: 107-domain-and-platform
plan: 11
type: execute
wave: 1
depends_on: [107-10]
files_modified:
  - tests/domain/manifest-cache.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - A fresh cache loads once and unchanged hits return the same value reference.
    - Size and modification-time changes invalidate positive and negative entries.
    - Negative hits rethrow the exact stored value without a second load.
    - Initial stat failures are pure misses and preserve loader failures.
    - A file that disappears during loading is not cached.
    - The cache writes no sidecar file.
  prohibitions:
    - Do not import shared error rendering or a concrete manifest error.
    - Do not share temporary directories across cases.
    - Do not use manual counters when the test context can observe the loader.
    - Do not keep design-history essays in the test.
---

<objective>
Refactor the existing `domain/manifest-cache.ts` mirror.

Test the generic cache contract with private files and context-owned loader
doubles. Cover both post-load stat-failure outcomes that the old suite missed.
</objective>

<tasks>

<task type="auto">
  <name>Replace the manifest cache test</name>
  <files>tests/domain/manifest-cache.test.ts</files>
  <action>
Create a fresh cache, loader, and temporary directory in each case. Assert
public values and failures before loader observations. Cover success and failure
invalidation in both directions and both post-load disappearance outcomes.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
