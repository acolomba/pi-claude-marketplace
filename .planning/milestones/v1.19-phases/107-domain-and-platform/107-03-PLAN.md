---
phase: 107-domain-and-platform
plan: 03
type: execute
wave: 1
depends_on: [107-02]
files_modified:
  - tests/domain/manifest-lookup.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - An exact plugin name returns the complete declared entry.
    - A missing plugin name returns the absent state.
    - Membership uses case-sensitive, normalization-sensitive string identity.
    - Compile-time checks own all three lookup states.
  prohibitions:
    - Do not duplicate surface-specific read-failure behavior here.
    - Do not test the private array lookup mechanism.
---

<objective>
Add the missing corresponding test for `domain/manifest-lookup.ts`.

Own the pure membership rule and its exported discriminated types without
copying surface-specific rendering behavior into this test.
</objective>

<tasks>

<task type="auto">
  <name>Add the manifest lookup test</name>
  <files>tests/domain/manifest-lookup.test.ts</files>
  <action>
Add compile-time checks for the entry and lookup types. Add independent runtime
cases for declared, absent, case-sensitive, and normalization-sensitive names.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
