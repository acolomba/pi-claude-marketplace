---
phase: 107-domain-and-platform
plan: 15
type: execute
wave: 1
depends_on: [107-14]
files_modified:
  - tests/platform/pi-api.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Runtime peer exports retain binding identity.
    - Type peer exports retain type identity.
    - Local structural event and content types accept their promised shapes.
    - Soft-dependency probes recognize their tools and degrade safely on faults.
    - The combined status reports both probes independently.
  prohibitions:
    - Do not use shared mutable tool lists.
    - Do not assert only a list length or one property when the full status is promised.
    - Do not keep coverage-line or design-history comments in the test.
---

<objective>
Refactor the Pi API boundary mirror.

Pin peer re-exports by identity, compile-check structural contracts, and cover
the three soft-dependency entrypoints with fresh typed stubs.
</objective>

<tasks>

<task type="auto">
  <name>Replace the Pi API boundary test</name>
  <files>tests/platform/pi-api.test.ts</files>
  <action>
Use top-level entrypoint describes, whole-value assertions, role-based names,
and independent branch-driving fixtures. Keep the frontmatter semantics that
the boundary promises to staging consumers.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
