---
phase: 107-domain-and-platform
plan: 14
type: execute
wave: 1
depends_on: [107-13]
files_modified:
  - extensions/pi-claude-marketplace/domain/github-auth.ts
  - tests/domain/github-auth.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Device Flow initializes, notifies, polls, persists credentials, and returns them.
    - Pending polls keep the interval and slow-down polls add five seconds cumulatively.
    - Every terminal, cancellation, timeout, transport, and malformed-response path returns its exact outcome.
    - The default HTTP adapter sends the required endpoints, headers, and form bodies.
    - Tokens and device codes do not appear in the notification.
  prohibitions:
    - Do not use generic test helpers or manual call logs.
    - Do not replace global fetch without the current test context.
    - Do not wait for real polling intervals.
    - Do not export the default HTTP adapter only so tests can call internals.
    - Do not keep coverage-line or design-history comments in the test.
---

<objective>
Refactor the GitHub Device Flow source-test pair.

Use strict interaction mocks for the public workflow and context-owned fetch
stubs for the private HTTP adapter. Cover every direct branch without network
access or real polling delays.
</objective>

<tasks>

<task type="auto">
  <name>Replace the GitHub authentication test</name>
  <files>
extensions/pi-claude-marketplace/domain/github-auth.ts,
tests/domain/github-auth.test.ts
  </files>
  <action>
Remove the test-only default-adapter export. Replace helper-backed and
process-global tests with fresh strict mocks and test-context fetch stubs.
Assert complete outcomes before promised interactions.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
