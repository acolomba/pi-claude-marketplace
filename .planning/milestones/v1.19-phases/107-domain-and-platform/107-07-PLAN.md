---
phase: 107-domain-and-platform
plan: 07
type: execute
wave: 1
depends_on: [107-06]
files_modified:
  - tests/domain/components/plugin.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - The entry schema exposes its complete required and optional field contract.
    - Entry validation accepts opaque component fields and both MCP forms.
    - Entry validation rejects missing required fields and typed-field defects.
    - Standalone manifest validation keeps every field optional and typed fields strict.
    - Compile-time checks own the exported PluginEntry type.
  prohibitions:
    - Do not test filesystem loading or whole-marketplace failure behavior here.
    - Do not derive expected schema values from the exported schema.
    - Do not keep comments about where cases used to live.
---

<objective>
Add the missing mirror for `domain/components/plugin.ts`.

Move ownership of plugin entry and standalone manifest schemas out of the
marketplace manifest test. Keep only the component module's direct contract.
</objective>

<tasks>

<task type="auto">
  <name>Add the plugin component test</name>
  <files>tests/domain/components/plugin.test.ts</files>
  <action>
Compare the complete exported entry schema. Add compile-time `PluginEntry`
checks. Group validation cases by the two exported compiled validators.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
