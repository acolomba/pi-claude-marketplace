---
phase: 107-domain-and-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/domain/name.ts
  - tests/domain/name.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Safe names accept the documented character set.
    - Unsafe names fail with the documented error type and complete message.
    - Skill, command, and agent names preserve their public formats and elision rules.
    - Nested command names preserve segment order and validate each segment.
  prohibitions:
    - Do not keep milestone, migration, or relocation comments in the test.
    - Do not test private implementation details.
    - Do not keep an unreachable fallback only to create a coverage branch.
---

<objective>
Refactor the `domain/name.ts` pair through its four exported functions.

The test must read as a clean first-author suite. Remove two unreachable
fallbacks from the production module so direct branch coverage measures real
behavior only.
</objective>

<tasks>

<task type="auto">
  <name>Replace the name tests</name>
  <files>tests/domain/name.test.ts</files>
  <action>
Use one top-level group for each exported function. Give every case the
required phase comments. Compare generated names and native errors completely.
Use sibling data-driven cases only when their public behavior is the same.
  </action>
  <verify>node --test tests/domain/name.test.ts</verify>
</task>

<task type="auto">
  <name>Remove unreachable name fallbacks</name>
  <files>extensions/pi-claude-marketplace/domain/name.ts</files>
  <action>
Remove the nullish fallbacks after an in-range string lookup and after
`String.split()`. Keep the same runtime behavior for every reachable input.
  </action>
  <verify>npm run test:coverage:direct -- tests/domain/name.test.ts</verify>
</task>

</tasks>
