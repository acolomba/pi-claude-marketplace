---
phase: 107-domain-and-platform
plan: 05
type: execute
wave: 1
depends_on: [107-04]
files_modified:
  - tests/domain/clone-key.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Clone keys contain the independent URL hash and the first 12 SHA characters.
    - Mirror keys contain the independent URL hash without a SHA suffix.
    - Key functions hash the caller's URL verbatim.
    - Each git source kind produces its documented canonical clone URL.
  prohibitions:
    - Do not calculate expected hashes with the production algorithm.
    - Do not compare one production call only with another production call.
    - Do not preserve decision-history comments in the test.
---

<objective>
Refactor the existing `domain/clone-key.ts` mirror.

Replace implementation-derived expectations with fixed, independently known
hash values. Keep source-kind URL reconstruction in the same module test.
</objective>

<tasks>

<task type="auto">
  <name>Replace the clone key test</name>
  <files>tests/domain/clone-key.test.ts</files>
  <action>
Group cases by the three exported functions. Compare each complete key or URL
with an independently fixed expected string. Include verbatim URL sensitivity.
  </action>
  <verify>
Run the focused test, ESLint, TypeScript, and direct coverage for the pair.
  </verify>
</task>

</tasks>
