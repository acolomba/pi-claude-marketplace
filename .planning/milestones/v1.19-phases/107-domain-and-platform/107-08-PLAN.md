---
phase: 107-domain-and-platform
plan: 08
type: execute
wave: 1
depends_on: [107-07]
files_modified:
  - extensions/pi-claude-marketplace/domain/manifest.ts
  - tests/domain/manifest.test.ts
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - Marketplace validation owns only the top-level composition contract.
    - Loading returns the complete raw parsed object with unknown fields intact.
    - Invalid JSON preserves a typed error with its SyntaxError cause.
    - Root and nested schema defects report complete typed errors.
    - Missing files preserve their filesystem error fields.
  prohibitions:
    - Do not retain plugin component or MCP component cases in this mirror.
    - Do not add a cache reset hook.
    - Do not keep an unreachable no-validation-error fallback.
    - Do not preserve relocation or decision-history comments.
---

<objective>
Refactor the existing `domain/manifest.ts` mirror.

Keep top-level schema composition and filesystem loading here. Remove the
component cases now owned by their direct mirrors. Remove one unreachable
validator-error fallback so direct coverage reflects possible behavior.
</objective>

<tasks>

<task type="auto">
  <name>Replace the marketplace manifest test</name>
  <files>tests/domain/manifest.test.ts</files>
  <action>
Group cases by the validator and loader exports. Give each filesystem case a
fresh temporary directory. Compare complete returned manifests and stable error
fields.
  </action>
  <verify>node --test tests/domain/manifest.test.ts</verify>
</task>

<task type="auto">
  <name>Remove the unreachable validator fallback</name>
  <files>extensions/pi-claude-marketplace/domain/manifest.ts</files>
  <action>
Use the validator invariant that a failed check supplies at least one error.
Keep root and nested location formatting unchanged.
  </action>
  <verify>npm run test:coverage:direct -- tests/domain/manifest.test.ts</verify>
</task>

</tasks>
