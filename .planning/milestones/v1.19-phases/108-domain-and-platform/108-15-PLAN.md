---
phase: 108-domain-and-platform
plan: 15
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/manifest.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Manifest loading preserves parsed values by reference, including key order and extra fields, and emits typed errors with causes (D-01)."
    - "All modified runtime cases satisfy D-02 through D-04 without documentation edits (D-05)."
  artifacts:
    - path: tests/domain/manifest.test.ts
      provides: Canonical mirrored owner for manifest.ts
  key_links:
    - from: tests/domain/manifest.test.ts
      to: extensions/pi-claude-marketplace/domain/manifest.ts
      via: direct validator/loader/error imports
  prohibitions:
    - "No expected manifest may be reconstructed in a way that hides reference identity or extra fields."
---

<objective>
Normalize manifest validation/loading tests and preserve value identity and typed failure contracts (D-01 through D-05).

Purpose: Ensure parsed metadata is neither silently rewritten nor weakly error-checked.
Output: A compliant owner for manifest validators, loader, and error type.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/manifest.test.ts` — modified owner for validation/loading/error behavior.

<tasks>
<task type="auto">
  <name>Task 1: Normalize valid manifests and parsed-value identity</name>
  <files>tests/domain/manifest.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/manifest.ts` — public validators, loader, and return semantics.
    - `tests/domain/manifest.test.ts` — current owner.
    - `tests/domain/components/plugin.test.ts` — closest schema analog.
    - `.claude/rules/typescript-unit-testing.md` — runtime structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — identity and whole values.
  </read_first>
  <action>Normalize all accepted cases per D-01, D-02, and D-04. Load a literal manifest with intentionally ordered keys and extra fields, assert the complete value, and assert the public loader returns the parsed object by reference where the source promises identity. Cover the smallest valid manifest and the complete representative manifest. Use D-03 only for a single throwing/rejection expression. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/manifest.test.ts</automated></verify>
  <acceptance_criteria>
    - Minimum and representative valid manifests are whole-value asserted.
    - Parsed-object identity, extra fields, and key order are observable in the case.
    - Exact lowercase phase comments appear in all touched runtime cases.
  </acceptance_criteria>
  <done>Valid manifest behavior and identity are independently preserved.</done>
</task>
<task type="auto">
  <name>Task 2: Lock malformed JSON and schema-error cause chains</name>
  <files>tests/domain/manifest.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/manifest.ts` — `InvalidMarketplaceManifestError` construction and causes.
    - `tests/domain/manifest.test.ts` — normalized valid cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Normalize/add malformed JSON and parsed-but-invalid schema cases. Assert each rejects with `InvalidMarketplaceManifestError`, the exact public message/path data, and the correct underlying parse or validation cause type; compare complete error-visible values instead of only checking that something throws. Leave docs unchanged per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/manifest.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest.ts</automated></verify>
  <acceptance_criteria>
    - Parse and schema failures are distinguishable through their cause chains.
    - The exported typed error is asserted directly.
    - Direct coverage accepts the owner.
  </acceptance_criteria>
  <done>Success identity and typed failure behavior are complete and directly covered.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| file bytes → parsed trusted manifest | Untrusted JSON crosses parsing and schema validation. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-15-01 | Tampering | manifest parser/validator | high | mitigate | Valid minimum/full values plus malformed and schema-invalid cases lock both gates. |
| T-108-15-02 | Repudiation | error cause chain | medium | mitigate | Typed error, public message/path, and distinct cause assertions preserve diagnosability. |
</threat_model>
<verification>
- `node --test tests/domain/manifest.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/manifest.ts`
- `npm run check`
</verification>
<success_criteria>
- Parsed values, reference identity, and extra fields remain preserved.
- Error type/message/cause behavior remains exact.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-15-SUMMARY.md` when done.</output>
