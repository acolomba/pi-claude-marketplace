---
phase: 108-domain-and-platform
plan: 19
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/source.test.ts
  - tests/bridges/integration.test.ts
  - tests/bridges/skills/discover.test.ts
  - tests/bridges/skills/stage.test.ts
  - tests/orchestrators/plugin/plugin-state-classifier.test.ts
autonomous: true
requirements: [MOD-01, RES-01]
estimate: {tokens: 52000, raw_tokens: 52000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Source parsing rejects non-string primitives and arrays, and path equality distinguishes unequal logical values (D-01)."
    - "All existing source kinds retain complete public results/errors under D-02 through D-04 normalization."
    - "Twelve integration/skills/classifier fixture constructions carry exact literal materializability booleans before 108-18, including false only for the unavailable arm (RES-01)."
  artifacts:
    - path: tests/domain/source.test.ts
      provides: Complete canonical mirrored owner for source.ts
  key_links:
    - from: tests/domain/source.test.ts
      to: extensions/pi-claude-marketplace/domain/source.ts
      via: direct imports of every public parser/normalizer/equality export
  prohibitions:
    - "No weak field-by-field assertion may conceal extra or missing source fields."
    - "No resolver discriminant may be optional, cast away, or inferred from `state` at the fixture boundary."
---

<objective>
Repair the final source.ts coverage gaps while normalizing the broad existing owner, then land the second typecheck-safe resolver fixture-preflight batch (D-01 through D-05, RES-01).

Purpose: Preserve source parsing, containment, and equality across every public source shape.
Output: A compliant owner at 100% direct coverage plus the remaining twelve strict resolver fixture literals required before 108-18.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-VALIDATION.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/source.test.ts` — modified canonical owner adding the two measured branch repairs and normalizing all touched source tables.
- `tests/bridges/integration.test.ts`, `tests/bridges/skills/discover.test.ts`, `tests/bridges/skills/stage.test.ts`, and `tests/orchestrators/plugin/plugin-state-classifier.test.ts` — twelve supporting fixture constructions staged for the strict boolean union; none completes its later production pair here.

<tasks>
<task type="auto">
  <name>Task 1: Normalize source tables and complete public values</name>
  <files>tests/domain/source.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/source.ts` — complete public source parsing/normalization surface.
    - `tests/domain/source.test.ts` — broad legacy owner and weak table assertions.
    - `.claude/rules/typescript-unit-testing.md` — D-02/D-04 structure.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value and negative-table guidance.
  </read_first>
  <action>Normalize every touched runtime table/case per D-01, D-02, and D-04. Replace weak field-by-field assertions with complete `deepStrictEqual` source/result/error values across every current public source kind, preserving exact normalization, containment, and validation behavior. Use literal fixtures, and use D-03 only where a case contains one throwing/rejection expression. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/source.test.ts</automated></verify>
  <acceptance_criteria>
    - Every touched success case compares the complete public source value.
    - Every touched failure row asserts the complete exported failure behavior.
    - Exact lowercase phase comments appear in all modified runtime cases.
  </acceptance_criteria>
  <done>The existing broad source contract is normalized without behavior loss.</done>
</task>
<task type="auto">
  <name>Task 2: Cover non-string inputs and unequal logical path values</name>
  <files>tests/domain/source.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/source.ts` — uncovered input arm formerly at lines 376-378 and path comparison false arm formerly at line 594.
    - `tests/domain/source.test.ts` — normalized owner and sibling public-behavior cases.
    - `.planning/phases/108-domain-and-platform/108-RESEARCH.md` — exact measured coverage gaps.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 scope.
  </read_first>
  <action>Add a literal invalid-input matrix containing null, boolean, number, object, empty array, and non-empty array at the exported parse boundary, asserting exact rejections. Add two complete path-source values that are identical except for one logical path field and assert inequality, alongside equal clones and adjacent path text. This resolves MOD-01 empty/null, adjacency, and equality behavior and reaches both measured false arms. Do not add coverage ignores or documentation changes per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/source.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/source.ts</automated></verify>
  <acceptance_criteria>
    - Non-string primitive, object, null, and array inputs reach exact public rejection.
    - Equal path sources compare equal; a one-field logical-path difference compares unequal.
    - Direct coverage reaches 100% functions, lines, and branches without ignores.
  </acceptance_criteria>
  <done>The two measured coverage gaps and the source boundary probes are closed.</done>
</task>
<task type="auto">
  <name>Task 3: Stage the integration, skills, and classifier resolver fixtures</name>
  <files>tests/bridges/integration.test.ts, tests/bridges/skills/discover.test.ts, tests/bridges/skills/stage.test.ts, tests/orchestrators/plugin/plugin-state-classifier.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/resolver.ts` — the pre-108-18 result union and exact three state arms.
    - Each listed supporting test — twelve existing materializable/unavailable fixture construction sites and their runtime uses.
    - `.planning/REQUIREMENTS.md` — RES-01's true/false literal contract and pair ownership boundary.
    - `.planning/phases/108-domain-and-platform/108-PATTERNS.md` — staged fixture-field spread pattern.
    - `.claude/rules/typescript-unit-testing.md` — lowercase runtime phases and type-only exception.
  </read_first>
  <action>Per RES-01, add module-scope exact-literal field bags equivalent to `{ installable: true } as const` and, where needed, `{ installable: false } as const`, then spread the correct bag into all twelve fixture constructions: one in bridges/integration, seven in skills/discover, one in skills/stage, and the installable, partially-available, and unavailable constructors in plugin-state-classifier. Both materializable states receive true and only the unavailable state receives false. The spread is required so the fixtures typecheck before the resolver declaration changes and satisfy the required field afterward; do not use `any`, a result-type cast, an optional property, compatibility code, or a production consumer edit. Preserve every scenario and other field. Every runtime case body modified in these supporting tests must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; lowercase `// act &amp; assert` is allowed only for one throwing/rejection expression. Legitimate type-only `satisfies`/`@ts-expect-error` checks use no artificial runtime phases, per D-02 through D-04.</action>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/bridges/integration.test.ts tests/bridges/skills/discover.test.ts tests/bridges/skills/stage.test.ts tests/orchestrators/plugin/plugin-state-classifier.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/source.ts</automated></verify>
  <acceptance_criteria>
    - All twelve named fixtures expose the correct exact literal; partial is true and unavailable is false.
    - Repository typecheck passes against the pre-change union and all four supporting suites preserve their existing scenarios.
    - Every modified runtime case body follows exact lowercase D-02/D-03 structure; type-only evidence receives no fake phases.
    - This plan still owns exactly the source.ts/source.test.ts pair and claims no bridge or classifier production pair complete.
  </acceptance_criteria>
  <done>The remaining twelve construction sites are forward-compatible and green before 108-18 makes the boolean required.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| raw source value → normalized source union | Untrusted manifest data selects local/remote source behavior. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-19-01 | Tampering | source parser | high | mitigate | Null/object/primitive/array rejection and complete accepted values lock the union boundary. |
| T-108-19-02 | Spoofing | logical path equality | high | mitigate | Equal clones and one-field-different paths explicitly exercise both equality outcomes. |
| T-108-19-03 | Information Disclosure | source fixtures | low | accept | Fixtures contain only synthetic paths/URLs and no credentials. |
| T-108-19-04 | Tampering | pre-resolver result fixtures | high | mitigate | Exact true/false field bags, pre-change typecheck, and four focused suites prevent a cast/optional RES-01 bypass. |
</threat_model>
<verification>
- `node --test tests/domain/source.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/source.ts`
- `npm run typecheck`
- `node --test tests/bridges/integration.test.ts tests/bridges/skills/discover.test.ts tests/bridges/skills/stage.test.ts tests/orchestrators/plugin/plugin-state-classifier.test.ts`
- `npm run check`
</verification>
<success_criteria>
- Every source kind and invalid top-level input retains exact public behavior.
- Path logical equality covers true and false outcomes.
- Twelve integration/skills/classifier fixtures are typecheck-safe before and after the strict RES-01 union change.
- The owner reaches 100% direct coverage under D-01 through D-05.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-19-SUMMARY.md` when done.</output>
