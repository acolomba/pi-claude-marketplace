---
phase: 108-domain-and-platform
plan: 18
type: execute
wave: 3
depends_on: [108-06, 108-19]
files_modified:
  - extensions/pi-claude-marketplace/domain/resolver.ts
  - tests/domain/resolver.test.ts
  - tests/domain/resolver-comp01.test.ts
  - tests/domain/resolver-default-enabled.test.ts
  - tests/domain/resolver-loose.test.ts
  - tests/domain/resolver-strict.test.ts
  - tests/domain/resolver.types.test.ts
autonomous: true
requirements: [MOD-01, RES-01]
estimate: {tokens: 54000, raw_tokens: 54000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Every resolver result exposes literal `installable`; both materializable arms are true, the unavailable arm is false and cannot expose `pluginRoot`, and all three states remain distinct."
    - "`requirePartialInstallable` and `requireInstallable` narrow first on the boolean, with `state` used only for secondary detail."
    - "One canonical resolver owner consolidates all legacy runtime/type evidence and passes 100% direct coverage (D-01 through D-05)."
    - "All 19 repository fixture constructions already expose exact literal booleans through the typecheck-safe 108-06/108-19 preflight batches before this plan requires the field."
    - "Every production resolver consumer is inventoried below; Phase 108 changes no second production pair, and each lexical boolean-first migration is assigned to its later owning pair."
  artifacts:
    - path: extensions/pi-claude-marketplace/domain/resolver.ts
      provides: Boolean-discriminated `ResolvedPlugin` union and updated narrowers
    - path: tests/domain/resolver.test.ts
      provides: Sole canonical runtime/type owner for resolver.ts
  key_links:
    - from: tests/domain/resolver.test.ts
      to: extensions/pi-claude-marketplace/domain/resolver.ts
      via: direct runtime imports plus module-scope type narrowing assignments
    - from: .planning/phases/108-domain-and-platform/108-06-SUMMARY.md
      to: extensions/pi-claude-marketplace/domain/resolver.ts
      via: seven true-literal fixture constructions pass pre-change typecheck before the field becomes required
    - from: .planning/phases/108-domain-and-platform/108-19-SUMMARY.md
      to: extensions/pi-claude-marketplace/domain/resolver.ts
      via: twelve true/false fixture constructions pass pre-change typecheck before the field becomes required
    - from: .planning/phases/108-domain-and-platform/108-RESEARCH.md
      to: .planning/ROADMAP.md
      via: resolved caller inventory assigns each later production edit to its exact owning pair
  prohibitions:
    - "No false resolver arm may contain or type-expose `pluginRoot`."
    - "No legacy resolver suite may remain as an unclassified proxy owner."
    - "No later orchestrator pair may be claimed complete by the carrier support edit."
---

<objective>
Land the resolver's boolean materializability contract and its sole mirrored owner after the two authorized fixture-preflight owner plans, while preserving pair-atomic ownership (D-01 through D-05, RES-01).

Purpose: Make `pluginRoot` access structurally safe while retaining the three-way state detail.
Output: Updated resolver schema/constructors/narrowers, canonical runtime/type owner, removed proxy owners, a complete 19-site fixture-precondition ledger, and a caller-to-owning-pair production migration ledger.

Resolved ruling on the research question: DEL-01 and DEL-03 keep P108-18 strictly pair-atomic. Plans 108-06 and 108-19 own the 19 supporting fixture migrations and must complete first; this plan changes only `resolver.ts` and its canonical/legacy owner evidence. Lexical boolean-first edits in later production modules remain deferred to their exact P113/P114/P115 owning pairs below; Phase 108 proves the new union and compile-time narrowing but does not claim repository-wide caller migration complete.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-VALIDATION.md
@.planning/phases/108-domain-and-platform/108-06-SUMMARY.md
@.planning/phases/108-domain-and-platform/108-19-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `extensions/pi-claude-marketplace/domain/resolver.ts` — modified `MATERIALIZABLE_FIELDS`, unavailable schema, constructors, `requirePartialInstallable`, and `requireInstallable`.
- `tests/domain/resolver.test.ts` — new sole owner containing consolidated runtime cases and module-scope type contract cases.
- `tests/domain/resolver-comp01.test.ts`, `resolver-default-enabled.test.ts`, `resolver-loose.test.ts`, `resolver-strict.test.ts`, and `resolver.types.test.ts` — removed after complete migration.

<tasks>
<task type="auto">
  <name>Task 1: Add the installable discriminant and update resolver-owned narrowers</name>
  <files>extensions/pi-claude-marketplace/domain/resolver.ts, tests/domain/resolver.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/resolver.ts` — TypeBox schemas, all result constructors, and narrowing helpers.
    - `tests/domain/resolver-comp01.test.ts` — constructor/runtime fixtures to migrate.
    - `tests/domain/resolver-default-enabled.test.ts` — result/default fixtures to migrate.
    - `tests/domain/resolver-loose.test.ts` — loose-mode public behavior.
    - `tests/domain/resolver-strict.test.ts` — strict-mode public behavior.
    - `tests/domain/resolver.types.test.ts` — current type evidence.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — integration and pair-atomic constraints.
    - `108-06-SUMMARY.md` and `108-19-SUMMARY.md` — evidence that all 19 external fixture constructions already carry exact literal booleans and pass pre-change typecheck.
  </read_first>
  <action>First confirm the 108-06 and 108-19 summaries account for exactly all 19 external construction sites in the fixture-precondition ledger below and that `npm run typecheck` is green on those merged preflight edits. Then bootstrap and create `tests/domain/resolver.test.ts` as the canonical owner with focused runtime fixtures for every resolver result constructor and both resolver-owned narrowers changed here. Add required `installable: Type.Literal(true)` to `MATERIALIZABLE_FIELDS` so both `installable` and `partially-available` arms share it, and add required `installable: Type.Literal(false)` only to the unavailable object; keep `pluginRoot` solely in the materializable bag. Update every resolver result constructor and the new canonical owner's exact fixtures to return the correct literal. Change `requirePartialInstallable` to accept after `result.installable`; change `requireInstallable` to reject `!result.installable` before checking `state === "installable"` for the secondary partial distinction. Do not make the field optional, add a compatibility shim, edit the 19 supporting fixtures again, or edit any caller production module: 108-06/108-19 own the fixture edits and the production inventory below assigns lexical migrations to later pairs under DEL-01/DEL-03. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed only when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/domain/resolver.test.ts</automated></verify>
  <acceptance_criteria>
    - All resolver constructors emit the correct literal boolean.
    - `pluginRoot` exists only in true-arm schemas and narrowers check the boolean first.
    - Typecheck accepts the new required union with all 19 preflight fixtures, and no supporting fixture or caller production file is edited here.
  </acceptance_criteria>
  <done>The resolver-owned public union and narrowing helpers use boolean-first materializability without changing secondary states.</done>
</task>
<task type="auto">
  <name>Task 2: Consolidate all resolver behavior into one compliant owner</name>
  <files>tests/domain/resolver.test.ts, tests/domain/resolver-comp01.test.ts, tests/domain/resolver-default-enabled.test.ts, tests/domain/resolver-loose.test.ts, tests/domain/resolver-strict.test.ts, tests/domain/resolver.types.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/resolver.ts` — complete current public export surface after Task 1.
    - Each listed legacy resolver suite — behavior and fixtures that must migrate without loss.
    - `tests/domain/manifest.test.ts` — canonical owner organization analog.
    - `.claude/rules/typescript-unit-testing.md` — owner, AAA, type-only, and direct-coverage rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-value/negative-path standards.
  </read_first>
  <action>Extend and use the canonical `tests/domain/resolver.test.ts` owner created by Task 1, migrating every remaining exported runtime behavior from the five legacy suites, including component/default-enabled, loose, strict, success, partial, unavailable, error, ordering, validation, and aliasing/mutation cases. Normalize all runtime cases per D-01, D-02, and D-04; assert complete result objects including `installable`. Preserve compile-time cases at module scope with `satisfies` and `@ts-expect-error` per D-03. After parity is demonstrated, delete all five legacy files so no proxy owner remains; do not edit rule docs per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/resolver.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run test:corresponding</automated></verify>
  <acceptance_criteria>
    - The canonical owner contains every distinct legacy scenario and directly imports resolver.ts.
    - Each runtime case uses exact lowercase phases; type-only assignments use no fake runtime wrapper.
    - All five legacy owners are removed and correspondence passes.
  </acceptance_criteria>
  <done>Resolver behavior and type evidence live in one compliant mirrored owner.</done>
</task>
<task type="auto">
  <name>Task 3: Prove boolean, empty, ordering, and compile-time union boundaries</name>
  <files>tests/domain/resolver.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/resolver.ts` — final result schemas, constructors, and narrowers.
    - `tests/domain/resolver.test.ts` — consolidated owner.
    - `.planning/REQUIREMENTS.md` — exact RES-01 literals and MOD-01 pair gate.
    - `.planning/phases/108-domain-and-platform/108-RESEARCH.md` — caller trace and edge constraints.
  </read_first>
  <action>Add explicit whole-result cases for the equality-adjacent true/false arms, a minimal empty-component/input scenario yielding the exact unavailable result, installable versus partially-available true-arm distinctions, declared-first/implicit-last ordering, and first-wins deduplication where exposed. Add module-scope narrowing proof that `if (result.installable)` admits `pluginRoot`, `if (!result.installable)` rejects it with `@ts-expect-error`, and all three `state` literals remain legal. These concrete cases resolve RES-01 adjacency, empty, and ordering probes. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/resolver.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/resolver.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>
    - True/false arms, minimal unavailable input, all three states, and ordering/deduplication are explicit.
    - False-arm `pluginRoot` access is a consumed compile error; true-arm access compiles.
    - Focused direct coverage reaches 100% functions, lines, and branches.
  </acceptance_criteria>
  <done>RES-01 and the resolver pair contract are proven at runtime, compile time, and direct coverage.</done>
</task>
</tasks>

<resolver_fixture_precondition_inventory>
## Resolver fixture precondition ledger

These are supporting test fixtures, not second production-owner pairs. The owning Phase 108 plans add exact literals through field-bag spreads that typecheck against both the old and new union. P108-18 edits none of these files and cannot execute until both prerequisite summaries are present.

| Supporting fixture file | Construction sites | Literal assignment | Authorized owner plan |
|---|---:|---|---|
| `tests/bridges/agents/stage.test.ts` | 1 | true | 108-06 Task 3 |
| `tests/bridges/commands/discover.test.ts` | 3 | true | 108-06 Task 3 |
| `tests/bridges/commands/stage.test.ts` | 1 | true | 108-06 Task 3 |
| `tests/bridges/integration-foreign-content.test.ts` | 1 | true | 108-06 Task 3 |
| `tests/bridges/integration-materialization-gate.test.ts` | 1 | true | 108-06 Task 3 |
| `tests/bridges/integration.test.ts` | 1 | true | 108-19 Task 3 |
| `tests/bridges/skills/discover.test.ts` | 7 | true | 108-19 Task 3 |
| `tests/bridges/skills/stage.test.ts` | 1 | true | 108-19 Task 3 |
| `tests/orchestrators/plugin/plugin-state-classifier.test.ts` | 3 | installable/partially-available true; unavailable false | 108-19 Task 3 |

Total: 19 construction sites. Every runtime case body modified by either prerequisite uses exact lowercase `// arrange`, `// act`, `// assert` with blank-line separation; lowercase `// act &amp; assert` is limited to one throwing/rejection expression, and legitimate type-only `satisfies`/`@ts-expect-error` evidence receives no fake phases (D-02 through D-04).
</resolver_fixture_precondition_inventory>

<resolver_consumer_inventory>
## Resolver consumer ownership ledger

CodeGraph plus a static import/dynamic-import inventory found every direct production consumer of `resolver.ts` and the one transitive `enable-disable.ts` consumer. The following sites still make or project a materializability decision and therefore receive an explicit later owning-pair assignment. P108-18 edits none of them.

| Consumer | Current decision sites | Owning pair | Required owner-plan action |
|---|---|---|---|
| `orchestrators/plugin/plugin-state-classifier.ts` | `classifyInstalledRecord` candidate checks near current lines 150/160 | P113-24 / `113-24-PLAN.md` | Narrow candidate presence on `resolved.installable` before using `state` for partial/unavailable classification. |
| `orchestrators/plugin/list.ts` | `probeUpgradeCandidate`, `installedRowMessage`, and `resolvedCandidateRow` near current lines 571/846 | P114-11 / `114-11-PLAN.md` | Use the boolean for materializable/unavailable branching; retain `state` only for displayed secondary status. |
| `orchestrators/plugin/info.ts` | installed/not-installed row builders near current lines 1568, 1660, 1861, and 2012 | P114-09 / `114-09-PLAN.md` | Put `installable` first at every root-access/materialization gate, then distinguish installable vs partially-available with `state`. |
| `orchestrators/plugin/fetch.ts` | `reasonedRow` near current lines 479/488 | P114-08 / `114-08-PLAN.md` | Branch false vs true on `installable`; use `state` only to label the true partial arm. |
| `orchestrators/plugin/install.ts` | `preflightInstallResolve` and install row/outcome projections near current lines 1197, 1600, 1685, and 1749 | P114-10 / `114-10-PLAN.md` | Preserve resolver gate calls and make any materializability branch boolean-first; keep later `state` checks as secondary detail after the true arm is established. |
| `orchestrators/plugin/update.ts` | `resolveUpdateCandidate`, disabled-pin/result projections near current lines 1545/2385 | P114-14 / `114-14-PLAN.md` | Establish the boolean true arm before partial-state projections and root-bearing update work. |
| `orchestrators/plugin/reinstall.ts` | `resolveInstallable` and state-record projection near current line 1459 | P114-12 / `114-12-PLAN.md` | Establish the boolean true arm before secondary installable/partial state projection. |
| `orchestrators/plugin/enable-disable.ts` | transitive `InstallLedgerSummary.resolved` projection in `runEnableBranch` near current line 301 | P114-07 / `114-07-PLAN.md` | Audit the transitive resolver result and establish boolean materializability before partial-state fields; do not treat the current MaterializablePlugin type alone as lexical completion. |
| `orchestrators/reconcile/backfill.ts` | `maybeBackfillPlugin` and backfill result projection near current lines 332, 402, and 407 | P115-06 / `115-06-PLAN.md` | Reject on `!resolved.installable` first, then derive the secondary state fields. |
| `orchestrators/reconcile/notify.ts` | `resolvePendingForceInstalls` near current line 299 | P115-07 / `115-07-PLAN.md` | Establish the boolean true arm before inspecting partial availability for notification reasons. |

The remaining direct imports are already narrowed ports rather than materializability decisions: P111-08 `bridges/agents/types.ts`, P111-10 `bridges/commands/discover.ts`, P111-13 `bridges/commands/types.ts`, P111-24 `bridges/skills/discover.ts`, P111-30 `bridges/skills/types.ts`, P113-15 `plugin/clone-cache.ts`, P113-17 `plugin/discover-names.ts`, and P113-26 `plugin/shared.ts` consume `MaterializablePlugin`/`GitPluginRootResult`; P113-20 `plugin/git-source-probe.ts` returns/delegates the full union but makes no root-access materializability gate. Their owning plans must preserve those typed boundaries, but no lexical boolean-first rewrite is required. Comment-only references in `domain/components/hooks.ts`, `bridges/mcp/parse.ts`, and `shared/notify.ts` are not consumers.

This is a milestone boundary, not a completion claim: Phase 108 closes the resolver pair and leaves the ten caller rows open until their named owning pairs execute.
</resolver_consumer_inventory>

<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| untrusted manifest/source state → resolved plugin union | Parsed configuration determines whether a filesystem root may be used. |
| resolver union → orchestrator consumer | Type narrowing authorizes materialization behavior. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-18-01 | Elevation of Privilege | false resolver arm | high | mitigate | Schema exclusion plus consumed `@ts-expect-error` proves `pluginRoot` is inaccessible. |
| T-108-18-02 | Tampering | result constructors | high | mitigate | Whole-result cases assert `installable` on every state and direct coverage reaches all constructors. |
| T-108-18-03 | Tampering | fixture and consumer rollout | high | mitigate | Exact 19-site precondition ledger plus complete production consumer inventory assigns every edit to its owner; P108-18 changes no second pair and does not overclaim rollout completion. |
| T-108-18-04 | Repudiation | legacy/proxy owners | medium | mitigate | Canonical consolidation and correspondence gate remove ambiguous ownership. |
</threat_model>
<verification>
- `node --test tests/domain/resolver.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/resolver.ts`
- `npm run typecheck`
- `npm run test:corresponding`
- `npm run check`
</verification>
<success_criteria>
- The boolean is the primary materializability discriminator and state remains secondary.
- False arms cannot expose pluginRoot at runtime/schema/type levels.
- One canonical owner passes 100% direct coverage and all legacy proxy owners are gone.
- All 19 supporting fixtures were migrated by 108-06/108-19, and this plan makes no fixture-only edit.
- The caller migration ruling is pair-atomic, complete as an ownership ledger, and explicitly open until the named later pairs execute.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-18-SUMMARY.md` when done.</output>
