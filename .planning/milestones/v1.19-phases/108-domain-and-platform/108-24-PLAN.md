---
phase: 108-domain-and-platform
plan: 24
type: execute
wave: 9
depends_on: [108-10, 108-16, 108-19, 108-23]
files_modified:
  - tests/domain/components/mcp.test.ts
  - tests/domain/name.test.ts
  - tests/domain/source.test.ts
  - extensions/pi-claude-marketplace/domain/github-auth.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
autonomous: true
gap_closure: true
requirements: [MOD-01]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "All ten failed data-row definitions use separate lowercase `// arrange`, blank line, `// act`, blank line, and `// assert` phases per D-02 through D-04."
    - "The two accepted-name data rows execute `assertSafeName` in the act phase and assert its void result separately; no `assert.doesNotThrow()` assertion is weakened or hidden in a combined phase."
    - "No source or test comment names a deleted generic Git, credential, or device-flow helper path after the D-07 migration."
    - "The repair changes no runtime behavior and creates no source-test owner; the original 23 mirrored pairs remain the complete Phase 108 accounting per D-10."
  artifacts:
    - path: tests/domain/components/mcp.test.ts
      provides: One repaired MCP rejection data-row definition
    - path: tests/domain/name.test.ts
      provides: Seven repaired name data-row definitions, including two separate void-result assertions
    - path: tests/domain/source.test.ts
      provides: Two repaired source rejection data-row definitions
    - path: extensions/pi-claude-marketplace/domain/github-auth.ts
      provides: Current concern-local device-flow fake guidance
    - path: extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
      provides: Current concern-local credential fake guidance
    - path: extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
      provides: Current concern-local credential fake guidance in both option interfaces
  key_links:
    - from: tests/domain/components/mcp.test.ts
      to: extensions/pi-claude-marketplace/domain/components/mcp.ts
      via: unchanged direct import and rejection assertions
    - from: tests/domain/name.test.ts
      to: extensions/pi-claude-marketplace/domain/name.ts
      via: unchanged direct import, data tables, and exact boundary assertions
    - from: tests/domain/source.test.ts
      to: extensions/pi-claude-marketplace/domain/source.ts
      via: unchanged direct import and exact thrown-error assertions
    - from: extensions/pi-claude-marketplace/domain/github-auth.ts
      to: tests/domain/device-flow-fake.ts
      via: comment names the surviving concern-local fake rather than deleted support
    - from: extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts
      to: tests/platform/credential-ops-fake.ts
      via: comment names the surviving concern-local fake rather than deleted support
    - from: extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
      to: tests/platform/credential-ops-fake.ts
      via: both option-interface comments name the surviving concern-local fake
  prohibitions:
    - "Do not flatten or remove data rows to satisfy the phase-layout audit."
    - "Do not restore, re-export, or recreate a generic adapter helper."
    - "Do not alter production statements, signatures, imports, or runtime behavior while repairing source comments."
    - "Do not count this verification repair as a twenty-fourth source-test pair."
---

<objective>
Close the two bounded Phase 108 verification gaps while preserving all previously verified behavior (D-02, D-03, D-04, D-05, D-07, D-10).

Purpose: Make the executed phase conform exactly to its lowercase data-row contract and remove misleading references to deleted generic support.
Output: Ten corrected data-row definitions and four corrected source comments across exactly six files; no new owner pair and no runtime change.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-VERIFICATION.md
@.planning/phases/108-domain-and-platform/108-10-SUMMARY.md
@.planning/phases/108-domain-and-platform/108-16-SUMMARY.md
@.planning/phases/108-domain-and-platform/108-19-SUMMARY.md
@.planning/phases/108-domain-and-platform/108-23-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Gap source audit

| Source | ID | Gap requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Every domain/platform owner remains a compliant mirrored test with preserved public behavior | 108-24 | COVERED | Repairs compliance only; the original 23 pair owners remain unchanged. |
| REQ | MOD-01 | All 23 domain/platform pairs complete the pair contract | 108-24 | COVERED | Closes failed truths in P108-10, P108-16, P108-19, and P108-23 without adding a pair. |
| REQ | RES-01 | Resolver discrimination contract | existing 108-18/108-19 | COVERED | Verification already passed; this gap plan does not reopen it. |
| REQ | PRES-03 | Shared production/fake adapter cases | existing 108-12/108-21/108-22 | COVERED | Verification already passed; comment repairs do not change participants. |
| REQ | PRES-04 | Independent adapter negative controls | existing 108-12/108-21/108-22 | COVERED | Verification already passed; no control changes are permitted. |
| RESEARCH | — | Data rows use separate lowercase phases; complete direct coverage remains green | 108-24 Task 1 | COVERED | Exact bounded normalization plus focused and direct gates. |
| RESEARCH | — | Generic support is absent after concern-local relocation | 108-24 Task 2 | COVERED | Correct surviving references, prove deleted paths absent, and do not restore helpers. |
| CONTEXT | D-02/D-03/D-04 | Exact lowercase phase rules for modified runtime cases | 108-24 Task 1 | COVERED | Includes the stricter data-row rule and the throwing/rejection exception boundary. |
| CONTEXT | D-05 | Avoid canonical-rule and preserved-input churn | 108-24 Task 1 | COVERED | Rule/guideline files are read-only inputs. |
| CONTEXT | D-07/D-10 | Concern-local support and supplemental—not owner—classification | 108-24 Task 2 | COVERED | Only comments change; the 23-pair ledger does not change. |

<tasks>

<task type="auto">
  <name>Task 1: Separate all ten data-row runtime phases</name>
  <files>tests/domain/components/mcp.test.ts, tests/domain/name.test.ts, tests/domain/source.test.ts</files>
  <read_first>
    - `.planning/phases/108-domain-and-platform/108-VERIFICATION.md` — the exact ten failed definitions and two `assert.doesNotThrow()` cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — locked D-02 through D-05 rules.
    - `.claude/rules/typescript-unit-testing.md` — executable lowercase phase contract.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — canonical data-row and throwing/rejection examples.
    - `tests/domain/components/mcp.test.ts` — one rejection-table callback to repair without changing its detailed `ParseError` assertions.
    - `tests/domain/name.test.ts` — seven table callbacks to repair; the accepted-name callbacks are the two void-return cases.
    - `tests/domain/source.test.ts` — two rejection-table callbacks to repair without changing expected error objects.
  </read_first>
  <action>Repair exactly the ten data-driven callback definitions identified by the verifier: one MCP rejection definition, seven name definitions, and two source rejection definitions. Keep each table and every generated test case intact. In every callback, keep lowercase arrange first, add a blank line before lowercase act, and add a blank line before lowercase assert. For the eight rejection definitions, bind the operation as a callable in the act phase and pass that callable to the existing `assert.throws` assertion in the assert phase; preserve every validator, error constructor, name, message, and return-true check. For the two accepted-name definitions, invoke `assertSafeName` directly in the act phase, bind its void result, and compare that result with `undefined` in the assert phase; an unexpected throw must still fail the case. Remove the combined phase comment only from these ten data rows. Preserve the one legitimate non-data-row combined throwing expression in `name.test.ts`, and do not normalize unrelated tests. Keep the rule, guideline, handoff inputs, production modules, and pair inventory unchanged per D-05 and D-10.</action>
  <verify>
    <automated>node --test tests/domain/components/mcp.test.ts tests/domain/name.test.ts tests/domain/source.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/mcp.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/name.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/source.ts &amp;&amp; test "$(rg -n '// act &amp; assert' tests/domain/components/mcp.test.ts tests/domain/source.test.ts | wc -l)" -eq 0 &amp;&amp; test "$(rg -n '// act &amp; assert' tests/domain/name.test.ts | wc -l)" -eq 1 &amp;&amp; rg -n -A8 -B2 '// act &amp; assert' tests/domain/name.test.ts | rg 'assert\.throws' &amp;&amp; ! rg -n '// (Arrange|Act|Assert)( &amp; Assert)?' tests/domain/components/mcp.test.ts tests/domain/name.test.ts tests/domain/source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All ten verifier-identified data-row definitions have separate lowercase arrange, act, and assert markers with blank-line separation.
    - The MCP and source data rows retain their full thrown-error observations.
    - The two accepted-name rows execute in act and assert an exact void result; unexpected throws still fail.
    - The only combined marker remaining across the three files belongs to the existing single non-data-row `assert.throws()` expression.
    - Focused tests and all three direct-coverage commands pass at 100 percent functions, lines, and branches.
  </acceptance_criteria>
  <done>P108-10, P108-16, and P108-19 satisfy the exact D-02/D-03 data-row phase contract without behavior or assertion loss.</done>
</task>

<task type="auto">
  <name>Task 2: Replace four stale helper comments and hand off clean re-verification</name>
  <files>extensions/pi-claude-marketplace/domain/github-auth.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts</files>
  <read_first>
    - `.planning/phases/108-domain-and-platform/108-VERIFICATION.md` — the exact four stale comment locations.
    - `.planning/phases/108-domain-and-platform/108-23-SUMMARY.md` — deletion evidence and the original 23-pair accounting.
    - `tests/domain/device-flow-fake.ts` — surviving concern-local device-flow fake name.
    - `tests/platform/credential-ops-fake.ts` — surviving concern-local credential fake name.
    - `extensions/pi-claude-marketplace/domain/github-auth.ts` — fetch-backed seam comment only.
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts` — credential injection comment only.
    - `extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts` — both credential injection comments only.
  </read_first>
  <action>Update exactly four comments: make the device-flow seam comment name `tests/domain/device-flow-fake.ts`, and make the three credential injection comments name `tests/platform/credential-ops-fake.ts`. Keep the surrounding descriptions of production defaults, subprocess avoidance, and test injection accurate. Do not change a TypeScript token outside comment trivia, restore a deleted generic helper, add a compatibility export, or touch imports/signatures/runtime statements. Run a repository source/test/script reference scan for all three former generic helper names and require zero matches plus physical absence of the deleted files. Then run tracked-file formatting, typecheck, ESLint, Fallow, unit, and integration gates. Record the focused tests, direct coverage, lexical scans, and full quality results in `108-24-SUMMARY.md`, and hand control back for Phase 108 re-verification without editing `108-VERIFICATION.md`, ROADMAP pair counts, or STATE plan counts.</action>
  <verify>
    <automated>test ! -e tests/helpers/git-mock.ts &amp;&amp; test ! -e tests/helpers/credential-mock.ts &amp;&amp; test ! -e tests/helpers/device-flow-mock.ts &amp;&amp; ! rg -n 'tests/helpers/(git-mock|credential-mock|device-flow-mock)\.ts' extensions tests scripts &amp;&amp; ./node_modules/.bin/prettier --check tests/domain/components/mcp.test.ts tests/domain/name.test.ts tests/domain/source.test.ts extensions/pi-claude-marketplace/domain/github-auth.ts extensions/pi-claude-marketplace/orchestrators/marketplace/add.ts extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The four verifier-identified comments point to the surviving concern-local fake paths.
    - The three deleted generic helper files remain absent and no source, test, or script names their former path.
    - The source changes are comment-only; runtime statements, signatures, and imports are byte-preserved.
    - Formatting on all six tracked files, typecheck, ESLint, Fallow, unit tests, integration tests, and diff checks pass.
    - The summary preserves 23/23 mirrored owner-pair accounting and explicitly requests Phase 108 re-verification.
  </acceptance_criteria>
  <done>P108-23's zero-reference truth is closed without runtime change or helper restoration, and the plan leaves complete evidence for re-verification.</done>
</task>

</tasks>

<threat_model>
ASVS L1 policy applies. This plan crosses no production runtime trust boundary; its relevant risks are verification and documentation integrity.

## Trust Boundaries

| Boundary | Description |
|---|---|
| data-row inputs → assertions | Table values drive shared callbacks; a layout-only repair must not weaken any generated case. |
| source comments → maintainer guidance | Comments must identify support that exists and must not direct contributors to deleted generic helpers. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-24-01 | Tampering | three data-driven owner tests | medium | mitigate | Preserve tables and exact assertions; run focused tests, direct coverage, and the lowercase phase scan. |
| T-108-24-02 | Spoofing | three production-source comments | low | mitigate | Point comments to the existing concern-local fakes and require a zero-reference scan for former paths. |
| T-108-24-03 | Tampering | Phase 108 pair ledger | medium | mitigate | Add no owner mapping and leave ROADMAP/STATE counts untouched; summary records this as a gap repair only. |
</threat_model>

<verification>
- Run all three focused owner tests together.
- Run direct coverage separately for `domain/components/mcp.ts`, `domain/name.ts`, and `domain/source.ts`; each must report complete functions, lines, and branches.
- Run the targeted lowercase phase scan and require the ten data-row violations to be absent while the one legitimate non-data-row combined throw remains.
- Require all three generic helper files and all source/test/script references to remain absent.
- Run Prettier on the six tracked files, then typecheck, ESLint, Fallow, unit tests, integration tests, and `git diff --check`.
- Create the summary and return control so the orchestrator can rerun the Phase 108 verifier; do not edit the verification report directly.
</verification>

<success_criteria>
- Both actionable entries in `108-VERIFICATION.md:gaps` are closed exactly.
- No already-verified runtime behavior, assertion strength, adapter contract, resolver contract, or hermetic boundary regresses.
- Phase 108 still represents 23 mirrored source-test pairs; Plan 108-24 is verification repair metadata only.
- Re-verification can change Phase 108 from `gaps_found` to passed using automated evidence alone.
</success_criteria>

<output>Create `.planning/phases/108-domain-and-platform/108-24-SUMMARY.md` when done, then hand off for Phase 108 re-verification.</output>
