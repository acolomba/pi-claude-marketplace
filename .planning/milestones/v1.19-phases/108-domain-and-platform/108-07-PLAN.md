---
phase: 108-domain-and-platform
plan: 07
type: execute
wave: 7
depends_on: [108-22]
files_modified:
  - tests/domain/components/hooks/matcher.test.ts
  - tests/orchestrators/plugin/install-auth.test.ts
  - tests/orchestrators/plugin/install.test.ts
  - tests/orchestrators/plugin/reinstall.test.ts
  - tests/orchestrators/plugin/update-reinstall-auth.test.ts
  - tests/orchestrators/plugin/update.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/shared/device-flow-prompt.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 44000, raw_tokens: 44000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Matcher parsing preserves match-all, literal MCP, mapped-token, deduplication, and rejection behavior (D-01)."
    - "The owner uses exact D-02 runtime phases, D-03 exceptions, and D-04 normalization scope."
    - "The assigned lifecycle/reconcile/prompt consumers use concern-local guarded support with case-owned state (D-07, D-18, D-19)."
  artifacts:
    - path: tests/domain/components/hooks/matcher.test.ts
      provides: Canonical mirrored owner for matcher.ts
  key_links:
    - from: tests/domain/components/hooks/matcher.test.ts
      to: extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts
      via: direct `parseMatcher` import
  prohibitions:
    - "No expected matcher set may be built by calling production mapping helpers."
---

<objective>
Normalize and complete the matcher owner while preserving its parsing and validation boundaries (D-01 through D-05).

Purpose: Make matcher acceptance, deduplication, and ordering deterministic.
Output: A compliant direct owner for `parseMatcher`.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.planning/phases/108-domain-and-platform/108-22-SUMMARY.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/components/hooks/matcher.test.ts` — modified owner for `parseMatcher`.
- The seven listed lifecycle/reconcile/prompt tests — bounded concern-local adapter import/call migration only; their production pairs remain open.

<tasks>
<task type="auto">
  <name>Task 1: Normalize accepted matcher forms and whole outputs</name>
  <files>tests/domain/components/hooks/matcher.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts` — parsing branches, safe character rules, and mapping behavior.
    - `tests/domain/components/hooks/matcher.test.ts` — current owner.
    - `extensions/pi-claude-marketplace/domain/components/hook-tool-names.ts` — public token mapping used by the parser.
    - `.claude/rules/typescript-unit-testing.md` — phase-comment contract.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — whole-result guidance.
  </read_first>
  <action>Normalize every accepted-form case per D-01, D-02, and D-04. Assert complete matcher results for empty and `*` match-all inputs, a valid MCP literal, mapped tool tokens, repeated tokens, and mixed mapped/unmapped tokens. Use literal expected sets/arrays in source-defined insertion order, and use the D-03 combined phase only for a single throwing/rejection expression. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hooks/matcher.test.ts</automated></verify>
  <acceptance_criteria>
    - Empty and `*` inputs produce the exact match-all representation.
    - Repeated tokens deduplicate without changing first-occurrence order.
    - MCP and mapped-token outputs are whole-value asserted.
  </acceptance_criteria>
  <done>Every accepted matcher form has exact compliant evidence.</done>
</task>
<task type="auto">
  <name>Task 2: Lock unsafe, empty-alternative, and unmapped boundaries</name>
  <files>tests/domain/components/hooks/matcher.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts` — invalid-character and token rejection branches.
    - `tests/domain/components/hooks/matcher.test.ts` — normalized accepted cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 documentation boundary.
  </read_first>
  <action>Add explicit failures for regex-significant unsafe characters, a leading/trailing/doubled pipe that creates an empty alternative, and an unmapped token that the public contract rejects. Assert exact error type/message or exact failure result as exported. Include equality-adjacent valid/invalid strings differing by one character. Keep docs unchanged per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/components/hooks/matcher.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts</automated></verify>
  <acceptance_criteria>
    - Each unsafe and empty-alternative form reaches its public rejection path.
    - One-character valid/invalid boundaries are explicit.
    - Direct coverage accepts the canonical owner.
  </acceptance_criteria>
  <done>Matcher acceptance and rejection boundaries are complete and deterministic.</done>
</task>
<task type="auto">
  <name>Task 3: Migrate install, update, reconcile, and prompt consumers</name>
  <files>tests/orchestrators/plugin/install-auth.test.ts, tests/orchestrators/plugin/install.test.ts, tests/orchestrators/plugin/reinstall.test.ts, tests/orchestrators/plugin/update-reinstall-auth.test.ts, tests/orchestrators/plugin/update.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/shared/device-flow-prompt.test.ts</files>
  <read_first>
    - `tests/platform/credential-ops-fake.ts`, `tests/domain/device-flow-fake.ts`, and `tests/platform/git-ops-fake.ts` — stabilized guarded replacements.
    - Each listed consumer — generic imports, fake controls, and cleanup ownership.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-02 through D-04, D-07, D-18, and D-19.
  </read_first>
  <action>Update this non-overlapping seven-file batch to concern-local adapter imports and explicit guarded fake/local boundaries, preserving every install/reinstall/update/reconcile/prompt scenario. Do not claim the consumer production pairs complete. Import-only edits do not rewrite cases; if a factory call changes a runtime case body, normalize the whole case with exact lowercase `// arrange`, `// act`, and `// assert` comments in order with blank-line separation, or exact lowercase `// act &amp; assert` only for one throwing/rejection expression. Preserve D-03 type-only exceptions and case-owned timers/globals/temp cleanup; do not delete generic helpers here.</action>
  <verify><automated>node --test tests/orchestrators/plugin/install-auth.test.ts tests/orchestrators/plugin/install.test.ts tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/update-reinstall-auth.test.ts tests/orchestrators/plugin/update.test.ts tests/orchestrators/reconcile/apply.test.ts tests/shared/device-flow-prompt.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts</automated></verify>
  <acceptance_criteria>
    - All seven consumers use concern-local support and preserve current behavior.
    - Every changed runtime case follows exact lowercase D-02/D-03 structure and owns cleanup.
    - No consumer production source or generic helper is edited here.
  </acceptance_criteria>
  <done>The final consumer-migration batch is bounded and green before 108-23 deletes generic helpers.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| matcher text → hook selection | Manifest-controlled matcher syntax selects callable hooks. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-07-01 | Tampering | `parseMatcher` | high | mitigate | Unsafe regex characters and empty alternatives have explicit rejection cases. |
| T-108-07-02 | Tampering | matcher token set | medium | mitigate | Literal whole outputs cover mapping, deduplication, and insertion order. |
| T-108-07-03 | Tampering | lifecycle/reconcile support imports | medium | mitigate | Run the exact seven consumers after guarded concern-local migration and preserve per-case cleanup. |
</threat_model>
<verification>
- `node --test tests/domain/components/hooks/matcher.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/components/hooks/matcher.ts`
- `npm run check`
</verification>
<success_criteria>
- All parser branches remain preserved.
- Unsafe syntax cannot become a matcher.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-07-SUMMARY.md` when done.</output>
