---
phase: 108-domain-and-platform
plan: 17
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - tests/domain/plugin-root.test.ts
autonomous: true
requirements: [MOD-01]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Plugin-root resolution remains absolute, idempotent, source-compatible, and preserves explicitly supported parent segments (D-01)."
    - "Invalid empty/NUL/delimiter inputs preserve their exact public failures under D-02 through D-04 structure."
  artifacts:
    - path: tests/domain/plugin-root.test.ts
      provides: Canonical mirrored owner for plugin-root.ts
  key_links:
    - from: tests/domain/plugin-root.test.ts
      to: extensions/pi-claude-marketplace/domain/plugin-root.ts
      via: direct plugin-root resolver import
  prohibitions:
    - "No test path may escape its case-owned temporary root or leave residue."
---

<objective>
Normalize plugin-root tests and preserve absolute/idempotent path resolution plus input containment (D-01 through D-05).

Purpose: Make supported path semantics explicit without weakening invalid-name guards.
Output: A hermetic canonical owner for plugin-root resolution.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/108-domain-and-platform/108-CONTEXT.md
@.planning/phases/108-domain-and-platform/108-RESEARCH.md
@.planning/phases/108-domain-and-platform/108-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

## Artifacts this phase produces

- `tests/domain/plugin-root.test.ts` — modified path-resolution owner with case-owned temporary roots.

<tasks>
<task type="auto">
  <name>Task 1: Normalize valid absolute, relative, parent, and idempotent roots</name>
  <files>tests/domain/plugin-root.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/plugin-root.ts` — public path contract and guards.
    - `tests/domain/plugin-root.test.ts` — current owner and temp-path fixtures.
    - `.claude/rules/typescript-unit-testing.md` — structure/cleanup rules.
    - `docs/guidelines/typescript-unit-testing-guidelines.md` — exact path assertions.
  </read_first>
  <action>Normalize accepted cases per D-01, D-02, and D-04 using case-owned temporary roots. Assert complete absolute outputs for relative input, already-absolute input, repeated/idempotent resolution, and the exact parent-segment form the source intentionally preserves. Use platform path utilities only to construct literal expected paths, and apply D-03 only to a single throwing/rejection expression. Register recursive cleanup in each case. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/plugin-root.test.ts</automated></verify>
  <acceptance_criteria>
    - Relative, absolute, parent-segment, and idempotent cases match exact paths.
    - Each temporary directory is scoped and removed by its case.
    - All modified runtime cases use exact lowercase phase comments.
  </acceptance_criteria>
  <done>Supported plugin-root behavior is hermetic and normalized.</done>
</task>
<task type="auto">
  <name>Task 2: Lock invalid inputs and temporary-path containment</name>
  <files>tests/domain/plugin-root.test.ts</files>
  <read_first>
    - `extensions/pi-claude-marketplace/domain/plugin-root.ts` — empty/NUL/delimiter guard branches.
    - `tests/domain/plugin-root.test.ts` — normalized success cases.
    - `.planning/phases/108-domain-and-platform/108-CONTEXT.md` — D-05 and cleanup scope.
  </read_first>
  <action>Normalize/add literal failures for empty text, NUL, and every platform delimiter shape rejected by the source. Assert exact errors and verify no rejected case creates a path outside or leaves content under the case-owned temporary root. Do not reinterpret the source-supported parent-segment behavior as a rejection, and leave docs unchanged per D-05. Runtime test cases must use exact lowercase `// arrange`, then a blank line, `// act`, then a blank line, `// assert`; combined lowercase `// act &amp; assert` is allowed when act and assertion are a single throwing/rejection expression; legitimate type-only checks use `satisfies`/`@ts-expect-error` without fake runtime phases.</action>
  <verify><automated>node --test tests/domain/plugin-root.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/plugin-root.ts</automated></verify>
  <acceptance_criteria>
    - Empty, NUL, and delimiter guards are explicit and exact.
    - Supported parent segments remain supported.
    - Direct coverage passes and case-owned temp roots are removed.
  </acceptance_criteria>
  <done>The plugin-root public boundary is preserved without path escape or residue.</done>
</task>
</tasks>
<threat_model>
ASVS L1 policy: any high-severity mitigation failure blocks plan completion.

## Trust Boundaries
| Boundary | Description |
|---|---|
| plugin root text → filesystem path | Caller-controlled text selects a filesystem location. |
| test temp root → host filesystem | Test-created paths must remain contained and cleaned. |
## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-108-17-01 | Tampering | plugin-root validation | high | mitigate | Exact empty/NUL/delimiter guard cases and complete expected paths lock containment. |
| T-108-17-02 | Information Disclosure | temporary filesystem | high | mitigate | Case-owned roots, explicit within-root assertions, and recursive cleanup prevent path escape/residue. |
</threat_model>
<verification>
- `node --test tests/domain/plugin-root.test.ts`
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/plugin-root.ts`
- `npm run check`
</verification>
<success_criteria>
- Supported absolute/idempotent/parent behavior remains exact.
- Rejected inputs cannot escape or dirty the host filesystem.
- D-01 through D-05 are satisfied.
</success_criteria>
<output>Create `.planning/phases/108-domain-and-platform/108-17-SUMMARY.md` when done.</output>
