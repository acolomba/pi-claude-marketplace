---
phase: 116-edge-surface
plan: "30"
type: execute
wave: 2
depends_on: ["116-00"]
files_modified:
  - tests/edge/types.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "EdgeDeps is satisfied by an object carrying only gitOps and pluginUpdate, which is the positive proof that importClaudeSettings is optional."
    - "Omitting gitOps, and separately omitting pluginUpdate, each fails to satisfy EdgeDeps, proven by a type-level negative placed on the line the diagnostic actually lands on."
    - "All three members are readonly, proven by an assignment negative inside a function that takes an EdgeDeps parameter."
    - "importClaudeSettings, when supplied, must accept the orchestrator options shape and return the orchestrator result promise; a function with an incompatible parameter or return type fails to satisfy the member."
    - "The suite holds no runtime case and imports nothing from node:test, matching the six type-only owners that already exist."
    - "The suite does not enumerate the EdgeDeps member set, does not assert the module export surface, and does not re-pin GitOps or PluginUpdateFn, each of which is owned elsewhere."
    - "This pair carries no exhaustiveness claim: types.ts declares one interface and contains no switch, so no missing-arm plant has a target here."
  artifacts:
    - path: tests/edge/types.test.ts
      provides: "New sole mirrored direct owner for edge/types.ts, closing one correspondence-gate violation"
  key_links:
    - from: tests/edge/types.test.ts
      to: extensions/pi-claude-marketplace/edge/types.ts
      via: "type-only import of EdgeDeps with satisfies bindings and ts-expect-error negatives"
      pattern: "EdgeDeps"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Write the missing type-only owner for the orchestrator-side injection surface.

Purpose: `edge/types.ts` declares one three-member interface, `EdgeDeps`, and has no test at its
mirrored path (D-116-08). Six type-only owners already exist and set the exact form D-116-11
requires. The trap this owner must avoid twice over: a marker attached to nothing, and a case that
pins usage rather than shape. A multi-line `satisfies` reports its diagnostic on the **closing**
line, so a suppression comment above the opening brace attaches to nothing and passes silently —
that was one of the six proofs-that-cannot-fail caught in the two preceding phases (D-116-13). And
enumerating the member set to catch an added member cannot work, because a test observes shape while
usage belongs to the call graph (D-116-12).

Output: one new type-only owner for `edge/types.ts`, with every negative proven to fire.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the type-only owner for EdgeDeps with every negative placed where its diagnostic lands</name>
  <files>tests/edge/types.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/types.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
    - extensions/pi-claude-marketplace/orchestrators/types.ts
    - tests/orchestrators/import/types.test.ts
    - tests/orchestrators/types.test.ts
    - tests/bridges/skills/types.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Create `tests/edge/types.test.ts` as a type-only owner. Match the shape of
`tests/orchestrators/import/types.test.ts` for scale and file layout, and
`tests/orchestrators/types.test.ts` for the two suppression-comment placements. The file has a
type-only import block, module-scope `satisfies` bindings, the negatives, and **no runtime case, no
`test()`, and no `node:test` import at all**. Do not add a runtime assertion to make the file look
active.

Build the two required members once, at module scope, as typed stubs: a `gitOps` value checked with
`satisfies` against the imported `GitOps` type, and a `pluginUpdate` value checked against
`PluginUpdateFn`. Derive both types from `edge/types.ts`'s own imports so a change to either seam is
a compile error here rather than a silently stale hand-copy.

Positive bindings:

- `{ gitOps, pluginUpdate }` satisfies `EdgeDeps`. This is the positive proof that
  `importClaudeSettings` is optional; it is the whole point of the owner.
- `{ gitOps, pluginUpdate, importClaudeSettings }` satisfies `EdgeDeps` with a stub whose parameter
  is the orchestrator options type and whose return is the orchestrator result promise.

Negatives, each placed on the line its diagnostic actually lands on:

- Omitting `gitOps`. Write it as a multi-line `satisfies` and put the suppression comment on the
  **last property line**, immediately before the closing brace — that is where the diagnostic lands.
- Omitting `pluginUpdate`, same placement.
- An `importClaudeSettings` whose parameter type is incompatible with the orchestrator options shape.
- An `importClaudeSettings` whose return type is not the orchestrator result promise.
- A single-line `satisfies` negative, with the suppression comment on the line **above** it, so the
  file carries both placements the analog demonstrates.

Readonly negatives: declare a function taking an `EdgeDeps` parameter and, inside it, assign to
`gitOps`, to `pluginUpdate`, and to `importClaudeSettings`, each with its own suppression comment.
Reference the function with a `void` statement so `noUnusedLocals` is satisfied without an export.

Boundaries, all of them hard:

- Pin only the required-versus-optional split of `EdgeDeps` itself. Do **not** re-pin `GitOps` or
  `PluginUpdateFn`; `PluginUpdateFn` is already owned by `tests/orchestrators/types.test.ts` and
  `GitOps` by its own pair.
- Do **not** enumerate the member set — no binding of the form "these three members and no others",
  and nothing built on `Required<EdgeDeps>` to make a fourth member impossible to add silently. A
  test observes shape; whether a member is read belongs to the call graph, and coverage cannot see
  an unread member because it has no read site.
- Do **not** assert the module export surface; `fallow dead-code` already reports an unused export.

Comments cite decision or requirement IDs only, never a plan, phase, wave, or task.

Plants (D-116-04), both test-side, no production edit:

1. Move one of the multi-line suppression comments from the last property line to the line above the
   opening brace. Run `npm run typecheck` and confirm it now reports an unused-suppression
   diagnostic on that line — that is the proof the placement is load-bearing rather than decorative.
   Revert.
2. Delete `pluginUpdate` from the positive `{ gitOps, pluginUpdate }` binding, confirm `npm run
   typecheck` goes RED because the member is required, and revert.

Record both verbatim compiler outputs. A plant that stays GREEN is a finding — the negative is
attached to nothing and must be moved, not papered over.

This pair makes no exhaustiveness claim; `types.ts` declares one interface and has no `switch`.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/types.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/types.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/types.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/types.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/types.test.ts &amp;&amp; ! rg -n 'node:test' tests/edge/types.test.ts &amp;&amp; git diff --check -- tests/edge/types.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/types.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), the type-only scan matching a `node:test` import (which makes that negated link exit 1), `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/types.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. The suite holds no runtime case: it imports nothing from `node:test` and `node --test` runs it with zero cases. Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The direct-coverage gate reports the pair as type-only and exits 0. The file contains no `test()` and no `node:test` import. No binding enumerates the `EdgeDeps` member set, nothing is built on `Required<EdgeDeps>`, the module export surface is not asserted, and neither `GitOps` nor `PluginUpdateFn` is re-pinned. The summary records the moved-suppression plant with its verbatim unused-suppression diagnostic, and the deleted-required-member plant with its verbatim compiler error.
  </acceptance_criteria>
  <done>P116-30 exists as a type-only owner, every negative is placed where its diagnostic lands, and both placements are proven to fire.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-30-A | Tampering | the orchestrator-side injection surface the whole command tier is wired from | high | mitigate | The required-versus-optional split is proven positively and negatively, and each negative is planted to prove it fires; a suppression comment attached to nothing would otherwise let a required member silently become optional. |
| T-116-30-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-30-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/types.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/types.test.ts` and
`npm exec -- prettier --check tests/edge/types.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-30-SUMMARY.md when done.
</output>
