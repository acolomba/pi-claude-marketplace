---
phase: 116-edge-surface
plan: "05"
type: execute
wave: 7
depends_on: ["116-00", "116-03", "116-06", "116-29"]
files_modified:
  - tests/edge/completions/provider.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "getArgumentCompletions offers the top-level subcommand vocabulary at an empty prefix, the marketplace subcommand vocabulary after the marketplace token, and the per-verb flag entries after a long-flag prefix."
    - "A verb that carries a plugin reference argument offers plugin reference candidates in the completion mode that verb maps to, and a verb that does not offers none."
    - "The scope flag value list is offered after the scope flag and nowhere else, and an exact subcommand token followed by a space promotes to the next argument position rather than re-offering the subcommand."
    - "Every candidate list preserves the order its source produced and is compared unsorted and as one whole array."
    - "This read-only surface never reaches the network: a context-owned fail-fast network entry point records zero calls across every case."
    - "This pair carries no exhaustiveness claim: the branch selector switches on an open string peeled from raw user input and ends with a default arm, so a deleted case is a behavior change rather than a compiler diagnostic; a missing-arm plant has no target here and must not be attempted."
    - "D-116-01a claim: the empty-object arm of the conditional at extensions/pi-claude-marketplace/edge/completions/provider.ts:125 -- the `description === undefined` side of `optionalDescription` -- is unreachable at runtime through the module's single export, so this pair lands exactly ONE branch short of complete while functions and lines are complete. The reason is structural, not a compiler setting: the only two producers of the entry list in `flagCompletions` are a written-out literal that carries a description and `completionFlagEntries`, whose every element derives from a `FlagEntry` whose `description` field is required; the declared element type keeps the field optional, so the guard must exist and nothing reachable can supply an entry without one. Measured, not inspected: a plant replacing the empty-object arm with a distinguishable description left every case green, and an independent route drove 406 long-flag cursor prefixes spanning every top-level head, every marketplace subcommand, unknown heads and scope-, partial- and reference-bearing prefixes, emitting 169 items of which zero lacked a description and zero carried the plant marker. Pinned by IDENTITY -- an `Incomplete direct coverage for <source>: branches N/M` verdict with the branch numbers read loosely, denominator minus numerator exactly 1, and the `$` anchor that makes a `lines` or `functions` clause a failure -- never by an absolute branch pair. Measured 79/80 at authoring time; that pair is an observation, not a gate. No coverage-exception pragma is added and no production file is changed."
  artifacts:
    - path: tests/edge/completions/provider.test.ts
      provides: "Sole mirrored direct owner for edge/completions/provider.ts, contract-compliant"
  key_links:
    - from: tests/edge/completions/provider.test.ts
      to: extensions/pi-claude-marketplace/edge/completions/provider.ts
      via: "injected locations resolver plus a hermetic tree and an offline assertion"
      pattern: "getArgumentCompletions"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the completion provider — one export, 335 lines, replacing the largest
non-compliant file in the phase.

Purpose: `edge/completions/provider.ts` exposes a single function and sits at 71/78 branches and
319/335 lines; the uncovered region is `provider.ts:238-254`, the two enable and disable branches of
the verb-to-completion-mode selector. The existing suite is 1,670 lines across sixty-seven cases and
carries none of the current contract's markers.

The module composes three settled surfaces: the subcommand vocabularies from the router pair, the
per-verb flag entries from the catalog pair, and the candidate maps from the completion data pair.
This owner asserts the composition — which vocabulary appears at which cursor position — and leaves
each source's own contents to its own pair.

Output: one exhaustive, hermetic, offline-proven owner for `edge/completions/provider.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the completion provider owner as a composition proof across cursor positions</name>
  <files>tests/edge/completions/provider.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/completions/provider.ts
    - extensions/pi-claude-marketplace/edge/completions/data.ts
    - extensions/pi-claude-marketplace/edge/flag-catalog.ts
    - extensions/pi-claude-marketplace/edge/router.ts
    - extensions/pi-claude-marketplace/shared/types.ts
    - tests/edge/completions/provider.test.ts
    - tests/edge/completions/data.test.ts
    - tests/architecture/flag-catalog-drift.test.ts
    - tests/architecture/partial-vocabulary-guard.test.ts
    - tests/architecture/scope-order-drift.test.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/completions/provider.test.ts`. One export, so a flat sequence of `test()` bodies
with no `describe()` — or one `describe()` per cursor position if that reads better; do not nest.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts`: two `mkdtemp`
roots per case, the home and agent-directory environment variables saved with an own-property check
and restored in a `t.after()` registered **before** the act with the agent-directory variable deleted
rather than overwritten, and a context-owned replacement of the global network entry point that
throws on any call and exposes its call count. No `finally` block. Assert the call count is zero in
every case, by count and never by an error message.

Build the injected resolver as a typed fake per case — a plain object checked with `satisfies`
against the interface the data module declares, with arrow properties rather than methods so
destructuring does not trip the unbound-method rule. Seed its state and manifest data per case.

**Cursor positions.** Compare every returned candidate list as one whole array, unsorted, against a
hand-authored literal. Cover:

- Empty prefix: the top-level subcommand vocabulary, in the order the exported list declares it.
- A partial top-level token: the vocabulary narrowed by a case-sensitive prefix match. Add a
  differing-case partial that matches nothing, which is the discriminating proof that no case folding
  is applied.
- An exact top-level token with no trailing space: read the module and state whether the token is
  re-offered or the position promotes; assert the observed outcome.
- An exact top-level token followed by a space: the position promotes to the next argument, so the
  subcommand vocabulary is **not** re-offered. This pair of cases is the promotion proof.
- The marketplace token followed by a space: the marketplace subcommand vocabulary.
- A partial marketplace subcommand: that vocabulary narrowed.
- A marketplace verb that takes a name argument, followed by a space: marketplace name candidates
  from the seeded resolver.
- A marketplace verb that takes no name argument: no name candidates.
- Each verb that takes a plugin reference, followed by a space: plugin reference candidates. Drive
  every such verb as a row table, each row stating the completion mode that verb maps to by hand, and
  seed candidates whose status is eligible for that mode plus at least one that is not, so the
  returned list discriminates between modes. **The enable and disable verbs are the region uncovered
  today** — include them explicitly.
- A verb that takes no plugin reference: no reference candidates.
- A long-flag prefix after a catalog verb: the verb's completable flag entries, in catalog
  declaration order, plus the global scope flag entry the module prepends. Assert the whole list
  including that prepended entry — its position is part of the promise.
- A long-flag prefix after a verb the catalog does not know: read the module and state the outcome;
  assert it.
- The scope flag followed by a space: the scope value list, taken from the shared scope declaration
  rather than a hand-written pair literal, because `tests/architecture/scope-order-drift.test.ts`
  forbids a hand-rolled scope-order literal. Then the same scope value list after a partial value.
- The scope flag value list is offered **nowhere else**: assert it is absent at the top-level and
  plugin-reference positions.
- An unknown leading token: read the module's default arm and assert the observed outcome.

Case titles state public behavior and may cite a durable requirement or decision ID — `TC-6`,
`D-54-01`, `ENBL-01`, and `ENBL-02` are the right anchors — and may not cite a plan, phase, wave, or
task. Every runtime case carries separate lowercase `// arrange`, `// act`, `// assert` phases in
that order, blank-line separated. Name values after their production role. Use no double assertion
through `unknown`, no broad partial cast, and no module replacement or loader hook.

Plant (D-116-04): change the enable branch of the verb-to-mode selector to the install mode, confirm
the enable row goes RED because a candidate eligible for one mode but not the other appears or
disappears, and revert. Then remove the prepended global scope flag entry from the flag completion
list, confirm the flag-prefix case goes RED, and revert. Record both verbatim failure outputs. If the
first plant stays GREEN, the seeded candidates do not discriminate between the two modes — fix the
seed rather than papering over it.

**Do not attempt an exhaustiveness plant.** The verb-to-mode selector switches on an open string and
ends with a default arm, so a deleted case is a behavior change rather than a compiler diagnostic.
State that absence in the summary.

Do not restate what a gate already owns: no case asserts the absence of direct process output, no
case re-pins the per-verb completion labels that `tests/architecture/flag-catalog-drift.test.ts`
already pins exactly, no case restates the retired vocabulary that
`tests/architecture/partial-vocabulary-guard.test.ts` forbids, no case re-proves the candidate-map
behavior owned by `tests/edge/completions/data.test.ts`, and no case hand-rolls a scope-order
literal.

If this plan runs out of room, stop and report exactly which cursor positions are complete and which
remain. Do not sample and do not shrink a matrix to fit.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/completions/provider.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/completions/provider.ts 2&gt;&amp;1 | node -e 'let s="";process.stdin.on("data",function(d){s+=d}).on("end",function(){var v=/^Incomplete direct coverage for extensions\/pi-claude-marketplace\/edge\/completions\/provider\.ts: branches (\d+)\/(\d+)$/m.exec(s);if(!v)throw new Error("no documented incomplete verdict, or lines/functions are no longer complete");if(v[2]-v[1]!==1)throw new Error("expected exactly 1 uncovered branch, saw "+(v[2]-v[1]));});' &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/completions/provider.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/completions/provider.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/completions/provider.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/completions/provider.test.ts &amp;&amp; git diff --check -- tests/edge/completions/provider.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/completions/provider.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, the coverage gate not printing an `Incomplete direct coverage for .../edge/completions/provider.ts:` line; that line carrying a `lines` or `functions` clause, which would mean lines or functions are no longer complete; more or fewer than exactly one uncovered branch (denominator minus numerator other than 1); or a passing verdict, which means the documented shortfall no longer holds and must be reported rather than edited away, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/completions/provider.test.ts` passes alone and the direct-coverage gate reports 100 percent functions
and lines for the paired source, with exactly ONE uncovered branch — the D-116-01a claim recorded in `must_haves` — and no
coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. Every verb that takes a plugin reference is driven with candidates seeded so the returned list discriminates between completion modes; the enable and disable rows are present. Every candidate list is compared unsorted and as one whole array. The summary states that the verb-to-mode selector carries no exhaustiveness guarantee and that no missing-arm plant was attempted, and it records the D-116-01a claim with its measured plant and its independent route. Every case asserts the fail-fast network entry point recorded zero calls.
  </acceptance_criteria>
  <done>P116-05 is contract-compliant, covers every cursor position including the enable and disable branches, stays offline, and reaches complete direct functions and lines with the single pinned D-116-01a branch outstanding.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-05-A | Tampering | a completion surface that reads scoped state and manifest data on every keystroke and must never reach the network | high | mitigate | A context-owned fail-fast network entry point is asserted uncalled by call count in every case, and the verb-to-mode mapping is planted to prove the per-mode candidate assertions discriminate rather than passing for any mode. |
| T-116-05-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-05-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/completions/provider.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/completions/provider.test.ts` and
`npm exec -- prettier --check tests/edge/completions/provider.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions and lines with its owner run alone, and the
  branch shortfall is exactly the one pinned D-116-01a claim.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-05-SUMMARY.md when done.
</output>
