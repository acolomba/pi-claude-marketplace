---
phase: 116-edge-surface
plan: "22"
type: execute
wave: 5
depends_on: ["116-00", "116-02", "116-08", "116-23", "116-26"]
files_modified:
  - tests/edge/handlers/plugin/reinstall.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "makeReinstallHandler maps the three accepted positional shapes onto the three target forms: no positional yields the all form, a bare marketplace reference yields the marketplace form, and a plugin reference yields the plugin form."
    - "The handler passes an empty pass-through list to the shared flag scan, so every long flag other than the scope and scope-target flags is rejected there; the retired overwrite flag now fails as an unknown flag."
    - "A long-flag-shaped token that survives the shared scan is rejected by the handler own positional loop with the unknown-option sentence — the region uncovered at the start of this plan."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This owner carries the exact-argument gap D-116-05 scopes to the thirteen seamless handlers: the reinstall workflow is reached by direct import with no injection point, so the delegation is observed through one minimal sized effect rather than as a stated argument list. The negative half of D-116-06 is proven in full."
    - "This pair carries no exhaustiveness claim: plugin/reinstall.ts selects its target form with if statements over string shapes, not a switch over a closed union, so no missing-arm plant has a target here."
  artifacts:
    - path: tests/edge/handlers/plugin/reinstall.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/plugin/reinstall.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/plugin/reinstall.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts
      via: "three target forms plus the two-stage flag rejection and boundary sizing"
      pattern: "makeReinstallHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the reinstall shim, whose interesting property is a two-stage flag rejection.

Purpose: `edge/handlers/plugin/reinstall.ts` is 100 lines and sits at 24/25 branches and 98/100
lines. The uncovered region is `reinstall.ts:50-51`, the unknown-option rejection inside the
handler's own positional loop. It looks unreachable at first glance: the handler passes an **empty**
pass-through list to the shared flag scan, so that scan rejects every long flag before the loop ever
runs. It is reachable through quoting — a quoted run that the shared scan splits on whitespace into
tokens that do not begin with the long-flag prefix, but which the tokenizer later reassembles into
one token that does. Read both scanners before choosing the input; if no such input exists, report
the branch as unreachable rather than inventing a case that cannot fail.

Output: one exhaustive, hermetic owner for `edge/handlers/plugin/reinstall.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the reinstall owner covering three target forms and both flag-rejection stages</name>
  <files>tests/edge/handlers/plugin/reinstall.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/edge/handlers/shared.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
    - extensions/pi-claude-marketplace/edge/args.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/plugin/reinstall.test.ts
    - tests/edge/handlers/marketplace/autoupdate.test.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/plugin/reinstall.test.ts`. One export, so a flat sequence of `test()`
bodies with no `describe()`.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts` — two `mkdtemp`
roots per case, the home and agent-directory environment variables saved with an own-property check
and restored in a `t.after()` registered before the act with the agent-directory variable deleted
rather than overwritten, and a context-owned fail-fast network entry point exposing its call count.
No `finally` block. Build the Pi boundary with `createNotificationBoundary`, never a hand-rolled
context object; copy the no-seam negative-delegation case verbatim from
`tests/edge/handlers/marketplace/autoupdate.test.ts`. Rejection cases size the boundary at one
emission and zero probes; delegating cases size it at the counts the workflow spends, read from the
module, and state `ctx.cwd` with the read count the handler performs. Seed installed plugins from a
path-source marketplace with `tests/helpers/marketplace-seed.ts` so the delegating cases reinstall
without touching the network.

Cover:

- Accepted arity of zero positionals: the all form reaches the workflow. Seed two installed plugins
  and assert both were re-materialised, so a narrower form would fail this case.
- One positional, a bare marketplace reference — a leading separator followed by a name: the
  marketplace form. Seed two marketplaces and assert only the named one was touched.
- One positional, a plugin reference: the plugin form. Seed two plugins in the same marketplace and
  assert only the named one was touched.
- A lone separator with nothing after it: read the module guard, which requires length beyond the
  separator, and assert the malformed-reference rejection rather than the marketplace form.
- One above the accepted arity — two positionals: exactly one notification whose whole value is the
  too-many-arguments sentence, a blank line, and **the reinstall usage block written out by hand**,
  then `verifyBoundary()` and nothing re-materialised. Add a three-positional row.
- A malformed plugin reference: exactly one notification whose sentence names the offending token
  verbatim. Drive all three malformed shapes as a row table.
- **First-stage flag rejection**: an ordinary unknown long flag is rejected by the shared scan with
  its unknown-flag sentence, because the handler supplies an empty pass-through list. Include the
  retired overwrite flag as one row — it now fails as an unknown flag, and that is a product
  decision worth its own case title.
- **Second-stage flag rejection**: a quoted token that survives the shared scan and reaches the
  handler's own positional loop, rejected with the **unknown-option** sentence — different wording
  from the first stage, so write both out by hand and assert the difference. This is the uncovered
  region. If no input reaches it, state that in the summary with the reasoning from both scanners and
  report the branch as unreachable rather than writing a case that cannot fail.
- Each scope value supplied, as a row table, and scope omitted; and the scope-target flag supplied
  and omitted, driven before and after the positional, asserting the identical outcome.
- Mutually exclusive scope selectors supplied together: read the module, the flag scan, and the
  workflow first, then state the observed outcome and assert it. Do not guess.
- An unrecognised scope value reaching the tokenizer **after** the shared scan: exactly one
  notification carrying the thrown error's own message with the usage block appended.
- Offline: assert the network entry point's call count is zero in every case.

Write every expected notification out by hand as one whole value including the blank line. Do not
read the usage string back off the handler and do not build an expected message by calling the
production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID — `RINST-01` and
`D-67-03` are the right anchors — and may not cite a plan, phase, wave, or task. Every runtime case
carries separate lowercase `// arrange`, `// act`, `// assert` phases in that order, blank-line
separated. Name values after their production role. Use no double assertion through `unknown`, no
broad partial cast, and no module replacement or loader hook.

Plant (D-116-04): remove the leading-separator branch so a bare marketplace reference falls through
to the plugin split, confirm the marketplace-form case goes RED, and revert. Then change the
handler's pass-through list from empty to include an arbitrary flag name, confirm the first-stage
rejection row for that flag goes RED, and revert. Record both verbatim failure outputs.

This pair makes no exhaustiveness claim. The target selection is a chain of `if` statements over
string shapes, not a `switch` over a closed union, so no missing-arm plant has a target here.

Do not restate what a gate already owns: no case asserts the absence of direct process output, no
case re-proves the flag scan owned by `tests/edge/handlers/shared.test.ts` or the reference split
owned by `tests/edge/handlers/plugin/shared.test.ts`, no case restates the retired-vocabulary guard
owned by `tests/architecture/partial-vocabulary-guard.test.ts`, and no case re-derives the reinstall
workflow outcome owned by its own orchestrator pair.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/reinstall.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/reinstall.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/reinstall.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/reinstall.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/reinstall.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/reinstall.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/plugin/reinstall.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/reinstall.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The two flag-rejection stages are proven to emit different wording, each written out by hand. The second-stage rejection is either covered by a real input or reported as unreachable with the reasoning from both scanners stated. The exact-argument gap for this seamless handler is stated in `must_haves` so a verifier reads it as scoped rather than missed.
  </acceptance_criteria>
  <done>P116-22 is contract-compliant, distinguishes both flag-rejection stages, and is at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-22-A | Tampering | a state-changing reinstall whose overwrite of collisions and foreign content is unconditional | high | mitigate | Every target form is proven to touch only what it names, so a wrong form cannot overwrite more than the user asked for; both flag-rejection stages are proven to leave nothing re-materialised. |
| T-116-22-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-22-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/reinstall.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/reinstall.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/reinstall.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-22-SUMMARY.md when done.
</output>
