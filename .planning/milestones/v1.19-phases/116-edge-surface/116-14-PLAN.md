---
phase: 116-edge-surface
plan: "14"
type: execute
wave: 5
depends_on: ["116-00", "116-02", "116-07", "116-12", "116-23", "116-26"]
files_modified:
  - tests/edge/handlers/plugin/bootstrap.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "makeBootstrapHandler accepts zero positionals and rejects the scope flag outright with its own sentence, because bootstrap always targets the user scope."
    - "It forwards the identical gitOps port it was constructed with into the bootstrap workflow, proven by that port recording the clone the workflow performs."
    - "A throw escaping the bootstrap workflow is converted into one failed marketplace row at error severity carrying the bootstrap marketplace name, the user scope, and an empty plugin list; no raw stack trace reaches the user."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This pair carries no exhaustiveness claim: plugin/bootstrap.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
  artifacts:
    - path: tests/edge/handlers/plugin/bootstrap.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/plugin/bootstrap.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/plugin/bootstrap.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts
      via: "injected gitOps port whose call recorder proves the forward, plus the failure-conversion row"
      pattern: "makeBootstrapHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the bootstrap shim, the one handler that rejects a scope flag rather than
forwarding it, and one of the two that emit through the probing notification path.

Purpose: `edge/handlers/plugin/bootstrap.ts` is 88 lines and passes direct coverage today at 11/11
branches, 2/2 functions, and 88/88 lines, so the debt is contract shape — and a thinner rewrite can
drop a branch the old suite covered incidentally, so re-measure after. It carries three distinct
rejection sentences of its own and one failure-conversion path that exists so a raw stack trace can
never reach the user channel.

Two things make it unlike its siblings. It calls the **probing** notification path rather than the
usage-error path on the failure branch, so that case keeps the derived probe count instead of zero.
And it carries an injected `deps.gitOps` port, so the port-forward proof from the marketplace add
owner applies directly here (locked decision D-116-05 / option O3).

Output: one exhaustive, hermetic owner for `edge/handlers/plugin/bootstrap.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the bootstrap owner covering three rejection sentences, the port forward, and the failure conversion</name>
  <files>tests/edge/handlers/plugin/bootstrap.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts
    - extensions/pi-claude-marketplace/edge/args.ts
    - extensions/pi-claude-marketplace/edge/types.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/bootstrap.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/plugin/bootstrap.test.ts
    - tests/platform/git-ops-fake.ts
    - tests/helpers/notification-boundary.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/plugin/bootstrap.test.ts`. One export, so a flat sequence of `test()`
bodies with no `describe()`.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts`: one `mkdtemp`
working root and one `mkdtemp` home root per case; the home and agent-directory environment
variables saved with an own-property check and restored in a `t.after()` registered **before** the
act, with the agent-directory variable deleted rather than overwritten; and a context-owned
replacement of the global network entry point that throws on any call and exposes its call count. No
`finally` block.

Build the Pi boundary with `createNotificationBoundary`, never a hand-rolled context object. The
three rejection cases reach the usage-error path, which runs **no** soft-dependency probe, so they
size the boundary at one emission and **zero** probes. The failure-conversion case reaches the
probing notification path, which runs one probe per emission and reads the tool list twice per
probe, so it keeps the derived count — read the module and the boundary helper and state the number
explicitly rather than inheriting a default. Delegating cases state `ctx.cwd` through the boundary
`cwd` option with the read count the handler performs; rejecting cases omit it.

Build the git port with `createGitOpsFake({ boundary: "memory", ... })` from
`tests/platform/git-ops-fake.ts` — never a hand-rolled literal of git functions.

Cover:

- Accepted arity of zero positionals with no flags: the bootstrap workflow runs and the git port
  recorder holds the clone the workflow performed, compared as one whole value against a
  hand-authored literal. That recording is only possible if the identical injected port reached the
  workflow — it is the exact-argument proof this tier can give. Do not re-derive the workflow's
  outcome or its notification body; its own pair owns those at 100 percent direct coverage.
- One above the accepted arity — one positional: exactly one notification whose whole value is the
  no-arguments sentence, a blank line, and **the bootstrap usage block written out by hand**, then
  `verifyBoundary()` and an empty git port recorder. Add a second row with two positionals.
- The scope flag supplied: exactly one notification whose whole value is the
  scope-not-accepted sentence with the same usage block. Drive both scope values as a row table.
  This is the only handler in the phase that rejects the scope flag rather than forwarding it, so
  the sentence must be written out verbatim rather than matched loosely.
- Mutually exclusive scope selectors supplied together — a scope flag plus the scope-target flag:
  read the module first. This handler parses raw arguments directly and does not run the shared flag
  scan, so state which rejection fires and assert that whole notification. Do not guess.
- An unrecognised scope value: the tokenizer throws, and exactly one notification carries the
  thrown error's own message with the usage block appended. Assert this **before** the
  no-arguments rejection can apply by supplying no positional, so the ordering of the three guards
  is pinned rather than assumed.
- The scope-target flag alone: this handler has no flag scan, so read the module and state whether
  it lands as a positional and triggers the no-arguments rejection. Assert the observed outcome.
- Failure conversion: make the injected git port fail so the bootstrap workflow throws. Assert the
  boundary recorded exactly one notification and that its payload is one failed marketplace row
  carrying the bootstrap marketplace name, the user scope, the failed status, error severity, and an
  empty plugin list. Import the bootstrap marketplace name constant from the orchestrator module
  rather than restating the literal, since it is that module's contract. Assert no raw error text
  appears in the emitted message.

Write every expected notification out by hand as one whole value including the blank line before the
usage block. Do not read the usage string back off the handler and do not build an expected message
by calling the production formatter or by interpolating the caught error.

Case titles state public behavior and may cite a durable requirement or decision ID; they may not
cite a plan, phase, wave, or task. Every runtime case carries separate lowercase `// arrange`,
`// act`, `// assert` phases in that order, blank-line separated. Name values after their production
role. Use no double assertion through `unknown`, no broad partial cast, and no module replacement or
loader hook.

Plant (D-116-04): remove the try/catch around the bootstrap workflow call, confirm the
failure-conversion case goes RED with the raw throw escaping, and revert. Then change the handler to
construct a fresh git port instead of forwarding the injected one, confirm the recorder assertion
goes RED, and revert. Record both verbatim failure outputs. A plant that stays GREEN is a finding.

This pair makes no exhaustiveness claim; `plugin/bootstrap.ts` has no `switch`. Do not restate what a
gate already owns: no case asserts the absence of direct process output, no case re-proves the
tokenizer behavior owned by `tests/edge/args.test.ts`, and no case re-derives the bootstrap workflow
outcome owned by its own orchestrator pair.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/bootstrap.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/bootstrap.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/bootstrap.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/bootstrap.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/bootstrap.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/bootstrap.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/plugin/bootstrap.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/bootstrap.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The failure-conversion case sizes the boundary at the probing count read from the module rather than inheriting a default, and asserts no raw error text reaches the emitted message. The three rejection sentences are each written out verbatim. The git port is `createGitOpsFake`, never a hand-rolled literal.
  </acceptance_criteria>
  <done>P116-14 is contract-compliant, proves the port forward and the failure conversion, and is still at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-14-A | Tampering | a bootstrap command that clones a remote marketplace on a first run and must never surface a raw stack trace | high | mitigate | The failure path is proven to convert a thrown error into one structured failed row with no raw error text, and the conversion is planted by removing the guard; every argument rejection is proven to leave the workflow unreached. |
| T-116-14-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-14-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/bootstrap.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/bootstrap.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/bootstrap.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-14-SUMMARY.md when done.
</output>
