---
phase: 116-edge-surface
plan: "15"
type: execute
wave: 5
depends_on: ["116-00", "116-08", "116-23", "116-26"]
files_modified:
  - tests/edge/handlers/plugin/enable-disable.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "One factory serves both subcommands: the enabling and disabling forms differ only in the boolean the factory was built with and the usage block that boolean selects, and both arms reach the workflow with opposite intent."
    - "The scope-target flag is extracted before the plugin reference is parsed, so its position cannot change the outcome, and it reaches the workflow as a present member only when supplied."
    - "A throw escaping the enable-disable workflow is converted into one failed plugin row at error severity carrying the parsed marketplace, the parsed plugin, the unreadable reason, and the caught cause; the scope defaults to user when none was supplied."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This owner carries the exact-argument gap D-116-05 scopes to the thirteen seamless handlers: the enable-disable workflow is reached by direct import with no injection point, so the delegation is observed through one minimal sized effect rather than as a stated argument list. The negative half of D-116-06 is proven in full."
    - "This pair carries no exhaustiveness claim: plugin/enable-disable.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
  artifacts:
    - path: tests/edge/handlers/plugin/enable-disable.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/plugin/enable-disable.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/plugin/enable-disable.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts
      via: "boundary sized at one emission for rejections, at the probing count for the failure conversion"
      pattern: "makeEnableDisableHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the dual-form enable and disable shim.

Purpose: `edge/handlers/plugin/enable-disable.ts` is 87 lines and sits at 14/16 branches with every
line covered, so the two missing branches are conditional spreads and defaults rather than whole
statements: read the module and identify each one before writing, rather than sampling. It is the
second of the two handlers that emit through the **probing** notification path, on its
failure-conversion branch, so that case keeps the derived probe count while the rejection cases use
zero.

Its ordering rule is the one the shared flag scan exists for: the scope-target flag is extracted
first, so the reference parse never sees it and the flag can appear anywhere.

Output: one exhaustive, hermetic owner for `edge/handlers/plugin/enable-disable.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the enable-disable owner covering both factory arms and the failure conversion</name>
  <files>tests/edge/handlers/plugin/enable-disable.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts
    - extensions/pi-claude-marketplace/edge/handlers/shared.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/plugin/enable-disable.test.ts
    - tests/edge/handlers/marketplace/autoupdate.test.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/plugin/enable-disable.test.ts`. One export, so a flat sequence of
`test()` bodies with no `describe()`.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts` — one `mkdtemp`
working root and one `mkdtemp` home root per case, the home and agent-directory environment
variables saved with an own-property check and restored in a `t.after()` registered before the act
with the agent-directory variable deleted rather than overwritten, and a context-owned fail-fast
network entry point exposing its call count. No `finally` block. Build the Pi boundary with
`createNotificationBoundary`, never a hand-rolled context object. Copy the no-seam
negative-delegation case verbatim from `tests/edge/handlers/marketplace/autoupdate.test.ts`: a
boundary sized at one emission and zero probes plus a verification call is what proves the workflow
never ran. The failure-conversion case reaches the probing path, so read the module and state its
count explicitly rather than inheriting a default. Delegating cases state `ctx.cwd` with the read
count the handler performs; rejecting cases omit it. Seed installed plugin records with
`tests/helpers/marketplace-seed.ts`.

Drive every case through both factory arms as a row table wherever the outcome differs only by the
usage block or the recorded intent, so both arms are exercised without duplicated bodies.

Cover:

- Accepted arity of exactly one plugin reference: the workflow runs and the seeded record's enabled
  state matches the arm. Assert that recorded state as the one minimal effect; do not re-derive the
  workflow's outcome or notification body, which its own pair owns at 100 percent direct coverage.
- The two arms leave **opposite** recorded states for the same input — the discriminating proof that
  the boolean reaches the workflow rather than merely selecting a usage block.
- The boolean also selects the usage block: drive the same invalid input through both arms and
  assert the two notifications differ in exactly the usage block, both written out by hand.
- One below the accepted arity — no positional: exactly one notification carrying the collapsed
  missing-argument sentence with the arm's usage block, then `verifyBoundary()`, plus the seeded
  record's enabled state unchanged.
- One above the accepted arity — two positionals: the single-positional shape takes the first token,
  so read the module and helper and assert what actually reached the workflow rather than assuming a
  rejection.
- A malformed plugin reference: exactly one notification whose sentence names the offending token
  verbatim, the seeded record unchanged, and `verifyBoundary()`.
- Each scope value supplied, as a row table, and scope omitted: the record touched is the one in the
  scope the flags selected. Seed the same plugin in both scopes so a wrong scope is visible.
- Scope-target flag supplied and omitted, driven **before** the reference and **after** it, both
  asserting the identical outcome. This is the ordering proof: were the reference parsed first, the
  flag token would be read as the reference and the command would be rejected.
- Mutually exclusive scope selectors supplied together: read the module, the flag scan, and the
  workflow first, then state the observed outcome and assert it. Do not guess.
- An unknown long flag, and separately an unrecognised scope value: each is exactly one notification
  and leaves the seeded record unchanged.
- Failure conversion: drive the workflow to throw — seed a state the workflow cannot read, or make
  the state file unreadable in the hermetic tree — and assert the boundary recorded exactly one
  notification whose payload is one failed plugin row carrying the parsed marketplace, the parsed
  plugin, error severity, the unreadable reason, and a cause. Assert the row's scope is the user
  default when no scope flag was supplied, and the supplied scope otherwise.

Write every expected notification out by hand as one whole value including the blank line before the
usage block. Do not read the usage string back off the handler, do not build an expected message by
calling the production formatter, and do not interpolate the caught error into the expectation.

Case titles state public behavior and may cite a durable requirement or decision ID — `ENBL-01`
through `ENBL-04` and `WR-02` are the right anchors — and may not cite a plan, phase, wave, or task.
Every runtime case carries separate lowercase `// arrange`, `// act`, `// assert` phases in that
order, blank-line separated. Name values after their production role. Use no double assertion through
`unknown`, no broad partial cast, and no module replacement or loader hook.

Plant (D-116-04): swap the flag scan and the reference parse so the reference is parsed first,
confirm the flag-before-reference row goes RED, and revert. Then change the scope default on the
failure row from the user scope to the project scope, confirm the failure-conversion scope case goes
RED, and revert. Record both verbatim failure outputs. A plant that stays GREEN is a finding.

This pair makes no exhaustiveness claim; `plugin/enable-disable.ts` has no `switch`. Do not restate
what a gate already owns: no case asserts the absence of direct process output, no case re-proves the
flag scan owned by `tests/edge/handlers/shared.test.ts` or the reference parse owned by
`tests/edge/handlers/plugin/shared.test.ts`, and no case re-derives the workflow outcome owned by its
own orchestrator pair.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/enable-disable.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/enable-disable.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/enable-disable.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/enable-disable.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/enable-disable.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/enable-disable.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/plugin/enable-disable.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/enable-disable.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The two factory arms are proven to leave opposite recorded states for the same input, not merely different usage blocks. The failure-conversion case sizes the boundary at the probing count read from the module rather than inheriting a default. The exact-argument gap for this seamless handler is stated in `must_haves` so a verifier reads it as scoped rather than missed.
  </acceptance_criteria>
  <done>P116-15 is contract-compliant, proves both arms and the failure conversion, and is at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-15-A | Tampering | a state-changing enable or disable driven by a user-supplied plugin reference | high | mitigate | Every rejection is proven to leave the seeded record state unchanged and the workflow unreached; the flag-before-reference ordering is planted to prove the position-independence case is discriminating. |
| T-116-15-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-15-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/enable-disable.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/enable-disable.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/enable-disable.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-15-SUMMARY.md when done.
</output>
