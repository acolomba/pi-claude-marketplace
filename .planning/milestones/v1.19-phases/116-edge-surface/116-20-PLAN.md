---
phase: 116-edge-surface
plan: "20"
type: execute
wave: 5
depends_on: ["116-00", "116-02", "116-06", "116-08", "116-23"]
files_modified:
  - tests/edge/handlers/plugin/list.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "makeListHandler accepts zero or one marketplace positional plus the catalog-derived filter flags, and each supplied filter flag reaches the list workflow as its own present member while the unsupplied ones are absent."
    - "An unrecognised long flag is rejected with the list unknown-option sentence, and more than one positional is rejected with the too-many-arguments sentence — the region uncovered at the start of this plan."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This read-only path never reaches the network: a context-owned fail-fast network entry point records zero calls across every case."
    - "This owner carries the exact-argument gap D-116-05 scopes to the thirteen seamless handlers: the list workflow is reached by direct import with no injection point, so the delegation is observed through one minimal sized effect rather than as a stated argument list. The negative half of D-116-06 is proven in full."
    - "This pair carries no exhaustiveness claim: plugin/list.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
    - "The module re-exports its boolean flag set for the architecture drift gate. That is a pair-rule exception the operator has ruled on: it stays, this owner takes no action on it, and it is recorded as an observation for the phase that owns the repository-wide gates."
  artifacts:
    - path: tests/edge/handlers/plugin/list.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/plugin/list.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/plugin/list.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
      via: "catalog-derived filter flag set plus boundary sizing over the shared prelude"
      pattern: "makeListHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the plugin list shim, which carries the widest filter-flag family in the tier.

Purpose: `edge/handlers/plugin/list.ts` is 82 lines and sits at 15/17 branches and 79/82 lines. The
uncovered region is `list.ts:62-64`, the too-many-arguments rejection, which the existing nine-case
suite never drives. Five filter flags reach the workflow as five independent members, each present
only when its flag was supplied.

One standing observation belongs here and nowhere else. The module re-exports its boolean flag set,
and the only consumer outside the file is the architecture drift gate. That violates the pair rule's
ban on exporting a symbol for a test, but removing it would break a passing gate, and the operator
has ruled that it stays. This owner records it and takes no action.

Output: one exhaustive, hermetic, offline-proven owner for `edge/handlers/plugin/list.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the plugin list owner with the full filter-flag family and the uncovered arity rejection</name>
  <files>tests/edge/handlers/plugin/list.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
    - extensions/pi-claude-marketplace/edge/flag-catalog.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/plugin/list.test.ts
    - tests/architecture/flag-catalog-drift.test.ts
    - tests/architecture/scope-fences-63.test.ts
    - tests/edge/handlers/marketplace/autoupdate.test.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/plugin/list.test.ts`. One runtime export plus the re-exported flag set,
so a flat sequence of `test()` bodies with no `describe()`.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts` — two `mkdtemp`
roots per case, the home and agent-directory environment variables saved with an own-property check
and restored in a `t.after()` registered before the act with the agent-directory variable deleted
rather than overwritten, and a context-owned fail-fast network entry point exposing its call count.
No `finally` block. Build the Pi boundary with `createNotificationBoundary`, never a hand-rolled
context object; copy the no-seam negative-delegation case verbatim from
`tests/edge/handlers/marketplace/autoupdate.test.ts`. Rejection cases size the boundary at one
emission and zero probes; delegating cases size it at the counts the workflow spends, read from the
module, and state `ctx.cwd` with the read count the handler performs. Seed plugins covering several
statuses with `tests/helpers/marketplace-seed.ts`.

Cover:

- Accepted arity of zero positionals with no flags: the list workflow runs and every seeded plugin
  appears. That row set is the one minimal effect; do not re-derive the workflow's outcome or full
  notification body, which its own pair owns at 100 percent direct coverage.
- Accepted arity of one marketplace positional: only that marketplace's plugins appear. Seed two
  marketplaces so a missing filter is visible.
- One above the accepted arity — two positionals: this is the uncovered region. Exactly one
  notification whose whole value is the too-many-arguments sentence, a blank line, and **the list
  usage block written out by hand**, then `verifyBoundary()`. Add a three-positional row.
- Each filter flag alone, as a row table driven from the catalog parse-set derivation for this verb
  rather than a restated literal list: the emitted rows narrow to the matching status, and the other
  filter members are absent from the workflow options. Assert the narrowed row set as one whole
  value; a flag that reached the workflow under the wrong member name would produce a different set.
- Two filter flags together: the union, proving the members are independent rather than mutually
  exclusive. Then all five together.
- A filter flag combined with the marketplace positional: both constraints apply.
- Filter flags before and after the positional yield the identical outcome.
- An unrecognised long flag: exactly one notification whose sentence names the offending token
  verbatim — note this handler uses the unknown-**option** wording, not the unknown-flag wording its
  siblings use, so write the exact sentence out by hand rather than copying a sibling's. Drive it
  alone and after a recognised filter flag, so the scan is proven not to stop early.
- Each scope value supplied, as a row table, and scope omitted: the rows come from the scope the
  flags selected. Seed the same plugin in both scopes so a wrong scope is visible.
- Mutually exclusive scope selectors supplied together: this handler does not consume the
  scope-target flag, so read the module and state where that token lands. Assert the observed
  outcome; do not guess.
- An unrecognised scope value: the shared prelude notifies once with the thrown error's own message
  and the usage block, and the workflow never runs.
- Offline: assert the network entry point's call count is zero in every case, by call count and never
  by an error message.

Write every expected notification out by hand as one whole value including the blank line. Do not
read the usage string back off the handler and do not build an expected message by calling the
production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID — `LIST-01`,
`D-67-01`, `RSTA-07`, and `D-80-07` are the right anchors — and may not cite a plan, phase, wave, or
task. Every runtime case carries separate lowercase `// arrange`, `// act`, `// assert` phases in
that order, blank-line separated. Name values after their production role. Use no double assertion
through `unknown`, no broad partial cast, and no module replacement or loader hook.

Plant (D-116-04): remove one filter flag's conditional spread from the workflow options, confirm that
flag's row goes RED, and revert. Then delete the too-many-arguments guard, confirm that case goes
RED, and revert. Record both verbatim failure outputs.

**Take no action on the re-exported flag set.** Record in the summary that
`edge/handlers/plugin/list.ts` re-exports its boolean flag set, that the only consumer outside the
file is `tests/architecture/flag-catalog-drift.test.ts`, that this conflicts with the pair rule's ban
on exporting a symbol for a test, and that the operator has ruled it stays because removing it would
break a passing gate — carrying it forward as an observation for the phase that owns the
repository-wide gates. Do not remove the export, do not restructure the drift gate, and do not import
the re-exported set into this owner.

This pair makes no exhaustiveness claim; `plugin/list.ts` has no `switch`. Do not restate what a gate
already owns: no case asserts the absence of direct process output, no case re-pins the catalog flag
sets or per-verb completion labels, no case restates the hook-column and hook-handler guards owned by
`tests/architecture/scope-fences-63.test.ts`, and no case re-derives the list workflow outcome owned
by its own orchestrator pair.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/list.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/list.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/list.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/list.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/list.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/list.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/list.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. Each filter flag is proven independently and in combination, with the narrowed row set asserted as one whole value. The re-exported boolean flag set is untouched and is recorded in the summary as an observation for the phase that owns the repository-wide gates. Every case asserts the fail-fast network entry point recorded zero calls, by call count.
  </acceptance_criteria>
  <done>P116-20 is contract-compliant, covers the full filter-flag family and the arity rejection, stays offline, and is at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-20-A | Tampering | a read-only plugin listing whose filter flags decide which records a user sees | high | mitigate | Each filter flag is proven to narrow the row set independently so a flag cannot silently widen or drop a bucket, every rejection is proven to leave the workflow unreached, and the network entry point is asserted uncalled by call count. |
| T-116-20-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-20-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/list.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/list.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/list.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-20-SUMMARY.md when done.
</output>
