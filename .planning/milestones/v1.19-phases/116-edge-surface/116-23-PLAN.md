---
phase: 116-edge-surface
plan: "23"
type: execute
wave: 3
depends_on: ["116-00", "116-01", "116-02", "116-06"]
files_modified:
  - tests/edge/handlers/plugin/shared.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "splitPluginMarketplaceRef accepts exactly one interior separator and returns the plugin and marketplace halves; a ref with no separator, a leading separator, or a trailing separator is rejected."
    - "parseMapModelArgs separates the two catalog-derived downstream boolean flags from non-flag positionals, carries scope only when supplied, and rejects any other long flag before returning."
    - "A tokenizer throw inside parseMapModelArgs is notified with the error's own message and yields undefined, with no positional scan performed."
    - "parseRequiredPluginMarketplaceRef collapses the duplicated usage block to the single missing-argument sentence, passes any other diagnostic through verbatim, and rejects a malformed ref with its own sentence naming the offending token."
    - "withParsedArgs calls its injected run delegate exactly once with the parsed value and the context when parsing succeeds, and on a parse throw notifies once and never calls the delegate."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "This pair carries no exhaustiveness claim: plugin/shared.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
    - "The module-private positional scanner is reached only through its exported callers; no symbol is exported from production to make it testable."
    - "The nine plugin handler owners that import this helper assert only that they supplied the right arguments and that the parsed result reached the next stage; they do not restate the parsing proved here."
  artifacts:
    - path: tests/edge/handlers/plugin/shared.test.ts
      provides: "New sole mirrored direct owner for edge/handlers/plugin/shared.ts, closing one correspondence-gate violation"
  key_links:
    - from: tests/edge/handlers/plugin/shared.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
      via: "exact-argument strong mock on the withParsedArgs run delegate"
      pattern: "withParsedArgs"
    - from: extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
      to: extensions/pi-claude-marketplace/edge/flag-catalog.ts
      via: "the downstream boolean flag set is the union of two catalog pass-through sets"
      pattern: "passThroughFlagNames"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Write the missing owner for the plugin-tier argument helper — the largest new file in the phase and
the module nine plugin handlers depend on.

Purpose: `edge/handlers/plugin/shared.ts` has no test at its mirrored path (D-116-08). It is 201
lines with seven exports, four of them runtime, and it holds the ref-splitting, flag-scanning, and
prelude logic that nine handler owners must be able to delegate to rather than re-prove (D-116-07).
Two of its exports carry injected collaborators — `withParsedArgs` takes `run`, and `parseMapModelArgs`
takes the context whose notification channel is the observable — so the exact-argument proof D-116-05
asks for is available here at full strength.

One trap is specific to this module: `parsePositionalsWithFlags` is module-private and must stay
that way. The pair rule forbids exporting a symbol for a test, so reach it only through
`parseMapModelArgs`.

Output: one new owner for `edge/handlers/plugin/shared.ts` covering all four runtime exports.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/guidelines/typescript-unit-testing-guidelines.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the plugin shared-helper owner covering all four runtime exports</name>
  <files>tests/edge/handlers/plugin/shared.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts
    - extensions/pi-claude-marketplace/edge/args.ts
    - extensions/pi-claude-marketplace/edge/args-schema.ts
    - extensions/pi-claude-marketplace/edge/flag-catalog.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
    - extensions/pi-claude-marketplace/edge/handlers/plugin/list.ts
    - tests/helpers/notification-boundary.ts
    - tests/orchestrators/plugin/shared.test.ts
    - tests/orchestrators/import/execute.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Create `tests/edge/handlers/plugin/shared.test.ts` in the G3 helper shape: one top-level `describe()`
per exported entrypoint — `splitPluginMarketplaceRef`, `parseMapModelArgs`,
`parseRequiredPluginMarketplaceRef`, `withParsedArgs` — and no nesting. `PluginMarketplaceRef`,
`ParsedPluginMarketplaceRef`, and `ParsedMapModelArgs` are types.

Build the Pi boundary with `createNotificationBoundary` — never a hand-rolled context object. Every
user-visible path in this module goes through `notifyUsageError`, which runs no soft-dependency
probe, so the probe count is zero everywhere. A case that expects no diagnostic sizes the boundary at
zero emissions, which is itself the proof that a successful parse is silent. Nothing in this module
reads `ctx.cwd`, so no case states it.

`describe("splitPluginMarketplaceRef")` — a pure function, no context. Cover with a row table plus
named cases, asserting the whole returned object or `undefined`:

- One interior separator yields both halves.
- A separator inside the marketplace half: the split takes the **first** separator, so the remainder
  including further separators stays in the marketplace half. State that outcome explicitly.
- No separator, a leading separator, and a trailing separator each yield `undefined`. These are the
  three rejection branches named in the source guard.
- A ref that is only the separator yields `undefined`.
- The empty string yields `undefined`.

`describe("parseMapModelArgs")` — cover:

- Both downstream boolean flags absent: the whole result is
  `{ nonFlagPositionals: [...], mapModel: false, partial: false }` with no `scope` key.
- Each downstream boolean flag alone, and both together, each setting only its own member. Drive the
  flag names from the catalog pass-through sets rather than restating them, but do not assert the
  catalog contents.
- A downstream flag placed before, between, and after the positionals yields the identical whole
  value — the position-independence proof for this scanner.
- Scope supplied: the result carries `scope`; scope absent: the key is omitted, not set to
  `undefined`.
- An unrecognised long flag: exactly one notification whose whole value is the unknown-flag sentence
  followed by a blank line and the usage block, and the result is `undefined`. This is the only route
  to the module-private positional scanner's rejection arm — do not export it to reach it directly.
- A tokenizer throw, driven by an unrecognised scope value: exactly one notification carrying the
  thrown error's own message, and the result is `undefined`. Prove the positional scan did **not**
  run by supplying an unrecognised long flag in the same input and asserting the notification is the
  scope diagnostic, not the unknown-flag sentence.
- Zero positionals: an empty `nonFlagPositionals` list with both booleans false.

`describe("parseRequiredPluginMarketplaceRef")` — cover:

- Accepted arity of exactly one positional ref: the whole result is `{ plugin, marketplace }`, plus
  `scope` when supplied.
- One below the accepted arity — no positional: exactly one notification carrying the collapsed
  missing-argument sentence, and `undefined` returned.
- One above the accepted arity — two positionals: state the observed outcome from the module source.
  The single-positional schema takes the first token; assert the whole result rather than assuming a
  rejection.
- A malformed ref: exactly one notification whose sentence names the offending token verbatim, and
  `undefined` returned. Drive all three malformed shapes as a row table.
- A diagnostic that is not the usage string reaches the user verbatim rather than being collapsed —
  the discriminating case for the collapse rule.

`describe("withParsedArgs")` — this is the exact-argument seam. Cover:

- Successful parse: the injected `run` delegate is called exactly once with the parsed value and the
  context. Build it as `mock&lt;…&gt;({ exactParams: true, name: "run" })` created inside the case,
  derive its type from the module's own parameter list, state the complete call in one `when()`, and
  call `verify(run)` as the last line after the result assertions. The boundary recorded zero
  notifications.
- The parsed value reaching the delegate is exactly what the supplied `parse` function returned:
  supply a `parse` that returns a distinctive literal and state that literal in the `when()`.
- Parse throws: exactly one notification carrying the thrown error's own message with the usage block
  appended, and the delegate is **never called** — prove it with a mock that has no stated
  expectation plus `verify(run)`, since such a mock throws on any call.
- A throw of a non-`Error` value still notifies with a message rather than reaching the delegate, if
  the module's error formatter accepts one; if the supplied `parse` cannot throw a non-`Error` in
  practice, say so in the summary rather than writing a case that cannot fail.
- The delegate rejecting propagates to the caller rather than being swallowed.

Write every expected notification out by hand as one whole value, including the blank line before
the usage block. Do not interpolate the usage string the case passed in, and do not build an expected
message by calling the production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID — `MSG-NC-2`,
`PI-1`, and `D-65-05` are the right anchors — and may not cite a plan, phase, wave, or task. Every
runtime case carries separate lowercase `// arrange`, `// act`, `// assert` phases in that order,
blank-line separated; reserve the combined marker for a single rejecting expression. Name values
after their production role. Use no double assertion through `unknown`, no broad partial cast, and no
module replacement or loader hook.

Plant (D-116-04): change the ref-split guard from a strict interior-separator test to accept a
leading separator, confirm the leading-separator rejection row goes RED, and revert. Then move the
downstream-flag membership test **after** the unknown-flag rejection in the private scanner, confirm
the recognised-flag cases go RED with the unknown-flag sentence, and revert. Record both verbatim
failure outputs. A plant that stays GREEN is a finding.

This pair makes no exhaustiveness claim; `plugin/shared.ts` has no `switch`. Do not restate what a
gate already owns: no case asserts the absence of direct process output, no case re-proves the
tokenizer behavior that `tests/edge/args.test.ts` owns or the positional-schema behavior that
`tests/edge/args-schema.test.ts` owns, and no case re-pins the catalog's per-verb flag sets.

If this plan runs long, stop and report exactly which `describe()` blocks are complete and which
cases remain. Do not sample and do not shrink a matrix to fit.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/plugin/shared.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/plugin/shared.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/plugin/shared.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/plugin/shared.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/plugin/shared.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/plugin/shared.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/plugin/shared.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. The module-private positional scanner is reached only through `parseMapModelArgs`; no production symbol was exported to make it testable. Every mock is a strict interaction mock created inside its case with exact-parameter matching, states its complete promised call, and is verified as the last line. `node scripts/check-corresponding-tests.mjs` reports one fewer missing-test violation than it did before this plan.
  </acceptance_criteria>
  <done>P116-23 exists, owns all four runtime exports, states the run delegate interaction with exact arguments, and is at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-23-A | Tampering | the plugin-tier argument helper nine state-changing handlers parse through | high | mitigate | Every malformed-ref and unknown-flag rejection is proven to emit exactly one notification and return undefined, and the prelude delegate is proven uncalled on a parse throw through a mock with no stated expectation. |
| T-116-23-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-23-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/plugin/shared.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/plugin/shared.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/plugin/shared.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-23-SUMMARY.md when done.
</output>
