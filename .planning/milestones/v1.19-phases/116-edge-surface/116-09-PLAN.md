---
phase: 116-edge-surface
plan: "09"
type: execute
wave: 4
depends_on: ["116-00", "116-12"]
files_modified:
  - tests/edge/handlers/marketplace/info.test.ts
autonomous: true
requirements: [MOD-09]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "makeMarketplaceInfoHandler builds a single-name marketplace handler from the info usage block and the info workflow, and forwards its Pi handle unchanged."
    - "The usage block this handler supplies is the info form and no other, proven by driving a rejection and comparing the whole emitted text."
    - "Each handler owner proves the accepted positional arity, one below it, and one above it. The accepted count reaches the orchestrator with exact arguments; both out-of-range counts are rejected with a usage error before any orchestrator call, proven by sizing the notification boundary at one emission and zero probes and verifying it."
    - "The router owner proves every alias resolves to the same handler identity as its canonical subcommand and that no alias shadows a canonical name. Each handler owner proves that mutually exclusive scope flags supplied together are rejected before any orchestrator call."
    - "This read-only path never reaches the network: a context-owned fail-fast network entry point records zero calls across every case."
    - "This owner carries the exact-argument gap D-116-05 scopes to the thirteen seamless handlers: the info workflow is reached by direct import at the factory call site with no injection point, so the delegate identity is observed through one minimal sized effect rather than as a stated argument list. The negative half of D-116-06 is proven in full."
    - "This pair carries no exhaustiveness claim: marketplace/info.ts contains no switch and no closed-union dispatch, so no missing-arm plant has a target here."
  artifacts:
    - path: tests/edge/handlers/marketplace/info.test.ts
      provides: "Sole mirrored direct owner for edge/handlers/marketplace/info.ts, contract-compliant"
  key_links:
    - from: tests/edge/handlers/marketplace/info.test.ts
      to: extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts
      via: "usage-block identity and boundary sizing over the shared single-name helper"
      pattern: "makeMarketplaceInfoHandler"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When a plan runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
---

<objective>
Rewrite the owner for the thinnest shim in the phase, and prove the read-only path stays offline.

Purpose: `edge/handlers/marketplace/info.ts` is 22 lines: one factory that returns the shared
single-name marketplace handler wired to the info usage block and the info workflow. It passes direct
coverage today at 2/2 branches, 1/1 functions, and 22/22 lines, so the debt is contract shape — and
a thinner rewrite can drop a branch the old suite covered incidentally, so re-measure after.

Its own promise is narrow and exactly three things: the usage block it supplies, the delegate it
supplies, and the Pi handle it forwards. Everything else — the positional parse, the collapse of the
duplicated usage block, the options object shape — belongs to
`tests/edge/handlers/marketplace/shared.test.ts` and must not be restated here (D-116-07).

The offline half of the phase success criteria **is** per-owner work: the architecture suite that
gates network reach names orchestrator files only and says nothing about the edge tier.

Output: one thin, hermetic, offline-proven owner for `edge/handlers/marketplace/info.ts`.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/116-edge-surface/116-CONTEXT.md
@.planning/phases/116-edge-surface/116-RESEARCH.md
@.planning/phases/116-edge-surface/116-PATTERNS.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/messaging-style-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the marketplace info owner as a thin shim proof with an offline assertion</name>
  <files>tests/edge/handlers/marketplace/info.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts
    - extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
    - extensions/pi-claude-marketplace/shared/notify.ts
    - tests/edge/handlers/marketplace/info.test.ts
    - tests/helpers/notification-boundary.ts
    - tests/helpers/marketplace-seed.ts
    - tests/orchestrators/edge-deps.test.ts
    - .planning/phases/116-edge-surface/116-PATTERNS.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Rewrite `tests/edge/handlers/marketplace/info.test.ts`. One export, so a flat sequence of `test()`
bodies with no `describe()`.

**Harness.** Copy the hermetic scope from `tests/orchestrators/edge-deps.test.ts`: one `mkdtemp`
working root and one `mkdtemp` home root per case; the home and agent-directory environment
variables saved with an own-property check and restored in a `t.after()` registered **before** the
act, with the agent-directory variable deleted rather than overwritten; and a context-owned
replacement of the global network entry point that throws on any call and exposes its call count. No
`finally` block.

Build the Pi boundary with `createNotificationBoundary`, never a hand-rolled context object.
Rejecting cases size it at one emission and zero probes; the delegating case reaches the info
workflow, which emits and probes — read the module and state both counts. The delegating case states
`ctx.cwd` through the boundary `cwd` option with the read count the shared helper performs;
rejecting cases omit it.

Cover exactly this owner's own promise, and nothing the shared helper owns:

- Accepted arity of exactly one name positional: the info workflow runs against a seeded tree, and
  the boundary recorded the workflow's emission count. Assert one minimal effect that only the info
  workflow produces — the emitted row naming the seeded marketplace — rather than re-deriving the
  workflow's outcome value or its full notification body, which its own pair owns at 100 percent
  direct coverage.
- One below the accepted arity — no positional: exactly one notification whose whole value is the
  collapsed missing-argument sentence, a blank line, and **the info usage block written out by
  hand**. This is the usage-block identity proof; a wrong usage block would make the shim
  indistinguishable from the remove shim. Then `verifyBoundary()`, which proves the workflow never
  ran.
- One above the accepted arity — two positionals: state the observed outcome from the module and
  helper sources; the single-positional shape takes the first token. Assert what actually reaches
  the workflow rather than assuming a rejection.
- Each scope value supplied, as a row table, and scope omitted: the scope the flags selected is the
  scope the workflow reports. Observe it through the emitted row's scope bracket.
- Mutually exclusive scope selectors supplied together: read the module and helper first, then state
  the observed outcome. This shim does not consume the scope-target flag, so say explicitly what
  happens to it and assert that outcome; do not guess.
- An unrecognised scope value: exactly one notification carrying the parse diagnostic verbatim,
  which is the discriminating case for the collapse rule, and `verifyBoundary()`.
- Offline: assert the network entry point's call count is zero in **every** case, including the
  delegating one. Assert the count, never an error message — an error-message assertion passes for
  the wrong error.

Write every expected notification out by hand as one whole value including the blank line before the
usage block. Do not read the usage string back off the handler and do not build an expected message
by calling the production formatter.

Case titles state public behavior and may cite a durable requirement or decision ID; they may not
cite a plan, phase, wave, or task. Every runtime case carries separate lowercase `// arrange`,
`// act`, `// assert` phases in that order, blank-line separated. Name values after their production
role. Use no double assertion through `unknown`, no broad partial cast, and no module replacement or
loader hook.

Plant (D-116-04): change the usage constant in the handler to the remove form, confirm the
usage-block identity case goes RED, and revert. Then change the factory to pass the remove workflow
as its delegate, confirm the delegating case goes RED, and revert. Record both verbatim failure
outputs. If the second plant stays GREEN, the delegating case is not discriminating — add the case
that distinguishes the two workflows rather than papering over it.

This pair makes no exhaustiveness claim; `marketplace/info.ts` has no `switch`. Do not restate what a
gate already owns: no case asserts the absence of direct process output, no case re-proves the
single-name helper parsing owned by `tests/edge/handlers/marketplace/shared.test.ts`, and no case
re-derives the info workflow outcome owned by its own orchestrator pair.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/edge/handlers/marketplace/info.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/edge/handlers/marketplace/info.test.ts &amp;&amp; npm exec -- prettier --check tests/edge/handlers/marketplace/info.test.ts &amp;&amp; npm run fallow &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|as unknown as|as any|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|//[[:space:]]+(Arrange|Act|Assert)' tests/edge/handlers/marketplace/info.test.ts &amp;&amp; rg -c '^\s+// arrange$' tests/edge/handlers/marketplace/info.test.ts &amp;&amp; git diff --check -- tests/edge/handlers/marketplace/info.test.ts &amp;&amp; git diff --quiet -- extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts extensions/pi-claude-marketplace/edge/handlers/shared.ts extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts extensions/pi-claude-marketplace/edge/handlers/plugin/shared.ts extensions/pi-claude-marketplace/edge/flag-catalog.ts tests/helpers/notification-boundary.ts</automated>
    <fails_when>non-zero exit from any link: a `# fail` count greater than 0 in the `node --test` TAP summary, a line beginning `Incomplete direct coverage for` from the coverage gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit, any match printed by the anti-pattern scan (which makes the negated link exit 1), `rg -c` finding zero arrange markers, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a pinned production file or the shared boundary helper changed.</fails_when>
  </verify>
  <acceptance_criteria>
`node --test tests/edge/handlers/marketplace/info.test.ts` passes alone and the direct-coverage gate reports 100 percent functions,
lines, and branches for the paired source with no coverage exception added. Every runtime case carries separate lowercase arrange, act, and assert phases, and the arrange-marker count equals the case-body count (a row table emits several runtime cases from one marked body). Every
expected value is a hand-authored literal: no expectation is produced by calling the code under
test, by re-reading a constant off the module under test, or by transforming an actual result.
Every public result is compared as one whole value with `assert.deepStrictEqual`. The suite
contains no double assertion through `unknown`, no wildcard argument matcher, no unbounded-count
expectation, no process-wide verify-all, no module replacement or loader hook, and no `only`,
`skip`, or `todo` marker. No case restates a fact ESLint, `fallow`, the compiler, or an
existing architecture suite already enforces. `git diff` shows no change under `extensions/` and none to `tests/helpers/notification-boundary.ts`. The summary records every plant this plan names,
its verbatim RED output, and the revert. Every case asserts the fail-fast network entry point recorded zero calls, by call count rather than by error message. The usage block is written out by hand and is proven to be the info form by a plant that swaps it. The exact-argument gap for this seamless handler is stated in `must_haves` so a verifier reads it as scoped rather than missed.
  </acceptance_criteria>
  <done>P116-09 is contract-compliant, proves its usage block and delegate identity, stays offline, and is still at 100 percent direct coverage.</done>
</task>

</tasks>

## Artifacts this phase produces

- 5 new test files: `tests/edge/flag-catalog.test.ts` (116-06),
  `tests/edge/handlers/marketplace/shared.test.ts` (116-12),
  `tests/edge/handlers/plugin/shared.test.ts` (116-23),
  `tests/edge/handlers/plugin/import.test.ts` (116-17, via `git mv`),
  `tests/edge/types.test.ts` (116-30).
- 25 rewritten test files: the remaining mirrored pairs under `tests/edge/`.
- 1 deleted file: `tests/edge/handlers/import.test.ts` (moved by 116-17).
- 1 changed exported test helper, in 116-00 only:
  `createNotificationBoundary(emissions, toolProbes, cwd?)` in
  `tests/helpers/notification-boundary.ts` — new required parameter `toolProbes: number`, new
  optional parameter `cwd?: { readonly value: string; readonly reads: number }`, widened return
  member `ctx: ExtensionCommandContext`.
- No production symbol is created, renamed, or exported for a test. Five plans (116-02, 116-17, 116-21, 116-26, 116-27) additionally delete a branch that is unreachable through the module exports, which the pair rule requires instead of a coverage exception; no exported signature changes anywhere in the phase.

<threat_model>
Security policy: OWASP ASVS Level 1; high-severity threats block completion. No package is
installed, removed, or upgraded, so the package-legitimacy gate does not apply.

Trust boundary in scope: raw `/claude:plugin` command text crossing from the user into the edge
tier. ASVS V5 (input validation) is the category this phase's owners exist to prove; V12 (file
operations) is owned one tier down and is not restated here.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --------- | -------- | --------- | -------- | ----------- | --------------- |
| T-116-09-A | Tampering | a read-only marketplace query that must never reach the network or mutate state | high | mitigate | A context-owned fail-fast network entry point is asserted uncalled by call count in every case, and every rejection is proven to leave the workflow unreached by boundary sizing plus verification. |
| T-116-09-B | Repudiation | direct coverage evidence for this pair | medium | mitigate | Re-measure with the per-pair direct-coverage gate after the rewrite; an outcome-thin rewrite can drop a branch the old suite covered incidentally. |
| T-116-09-C | Tampering | pinned production sources and the shared boundary helper | high | mitigate | `git diff --quiet` pins the pair own source, all three `shared.ts` helpers, `flag-catalog.ts`, and `tests/helpers/notification-boundary.ts`, so this plan cannot ease its own proof by editing a shared file. |
</threat_model>

<verification>
Run each gate separately and check its exit code. Do not use `npm run check`: its `format:check`
link fails on pre-existing untracked operator files and short-circuits before the tests run, so a
green result would mean nothing. Do not touch those files and do not edit `.prettierignore`.

Before committing, scan the changed files for secrets by the filesystem route, because this checkout
is a linked worktree and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/edge/handlers/marketplace/info.test.ts --results=verified,unknown --fail`.

Then run `SKIP=trufflehog,npm-format-check pre-commit run --files tests/edge/handlers/marketplace/info.test.ts` and
`npm exec -- prettier --check tests/edge/handlers/marketplace/info.test.ts`. Never `--all-files`, never with hooks disabled. Stage only
the paths listed in `files_modified`.
</verification>

<success_criteria>

- The paired source keeps 100 percent direct functions, lines, and branches with its owner run
  alone.
- Every expectation is hand-authored and compared as a complete value.
- Every plant this plan names went RED and is recorded verbatim in the summary.
- No production file changes and no change to the shared boundary helper.

</success_criteria>

<output>
Create .planning/phases/116-edge-surface/116-09-SUMMARY.md when done.
</output>
