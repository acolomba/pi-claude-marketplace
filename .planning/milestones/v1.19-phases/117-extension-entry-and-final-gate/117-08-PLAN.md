---
phase: 117-extension-entry-and-final-gate
plan: "08"
type: execute
wave: 3
depends_on: ["117-01", "117-02", "117-03", "117-04", "117-05", "117-06", "117-07"]
files_modified:
  - tests/index.test.ts
  - package.json
files_deleted:
  - tests/shared/index-smoke.test.ts
  - tests/edge/index-handler.test.ts
autonomous: true
requirements:
  [
    MOD-10,
    OWN-01,
    OWN-03,
    COV-01,
    CASE-01,
    CASE-02,
    CASE-03,
    CASE-04,
    TEST-01,
    TEST-02,
    TEST-03,
    TEST-04,
    TEST-05,
    DES-01,
    DES-02,
    DES-03,
    DEL-01,
    DEL-02,
    SUITE-01,
    SUITE-03,
  ]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "`extensions/pi-claude-marketplace/index.ts` has one mirrored owner at `tests/index.test.ts` that imports it directly, taking the pair total to 204 of 204. RED when: the owner is absent or imports the module through anything else — the correspondence gate reports a missing or wrongly-imported mirror."
    - "The gate reports the direct-coverage verdict `Direct coverage passed` for the pair with the owner run ALONE, with hit equal to found on functions, lines and branches and no coverage exception anywhere. The numbers are quoted from the gate; no absolute branch pair is written into this plan, because a branch range is emitted only when its count diverges from its enclosing block, so strengthening a suite raises numerator and denominator together."
    - "The factory's registration table is proven by hand-authored EXACT arguments, one expectation per registration with a literal event or command name, and only the callback captured beside it. RED when: a name is wrong — the mock reports the call as unexpected at the call site. Coverage cannot see a wrong wiring, because every name a registration carries is a value no branch selects."
    - "Both deferred failure paths that no existing suite reaches are reached: the hydrate catch and the plugin-PATH recompute catch. Both are driven cast-free by a proxy over the discover EVENT that refuses a chosen working-directory read, so the rest of the handler still runs. RED when: either case is removed — measured twice during research, and each removal moved the branch DENOMINATOR, not just the numerator."
    - "The last-ditch notify path and its inner catch are reached by a proxy over the boundary's own context whose notification member throws, seeded against a workspace whose declared configuration does not parse."
    - "The owner runs under `npm test`, not only under the focused pair command. RED when: either glob is left unamended — the glob-completeness control landed by 117-01 fails, and this plan RECORDS that failure before fixing it, as its free plant."
    - "The suite total rises when the amendment lands. Every figure is read from the runner's own `ℹ tests` line and never computed from a delta, including the owner's own case count read from a focused run."
    - "The two legacy proxies are deleted and every claim of theirs that survives is carried, including the on-disk negative that neither the project nor the home scope root is created by a clean reconcile. RED when: that negative is dropped — a production comment in the hooks event router gates a directory creation on it by name."
    - "The owner contains no double assertion through unknown, no any-cast and no never-cast, per D-117-02. Research proved a cast-free owner reaching complete coverage, so a cast surviving the fold is a finding to report, not a cast to carry."
    - "Per D-117-03 the merged owner reaches complete direct coverage when run ALONE, and it follows the house registration-table pattern rather than inheriting either legacy suite's hand-rolled Pi double — one of which ends in a double assertion and records into plain maps with no exact-parameter checking, the other of which uses a spy rather than a strict mock."
    - "The headline verdict is taken from the focused per-pair run and never from the merged report, per D-117-17: measured on this very pair, the merged report emits no branch range at all for a line one suite leaves uncovered and reports its body as hit."
    - "Every case owns its own temporary working directory and home, deletes the agent-directory environment variable, and restores all of it through the test context — the factory hydrates the user scope off disk at load time, so a non-hermetic case would touch the operator's real home."
    - "A stale production comment is created by this plan and is REPORTED, not fixed: the hooks event router names one of the deleted suites as the pin for its clean-reconcile invariant. D-117-13 opens no production licence, so it is filed in the broken-windows ledger."
  artifacts:
    - path: tests/index.test.ts
      provides: "Sole mirrored direct owner for the extension entry point — the 204th and final pair"
  key_links:
    - from: tests/index.test.ts
      to: extensions/pi-claude-marketplace/index.ts
      via: "the default-exported async factory invoked against a strict Pi mock, with every registered callback captured and invoked"
      pattern: "claudeMarketplaceExtension"
    - from: package.json
      to: tests/index.test.ts
      via: "a root-level pattern added to the `test` and `test:coverage:unit` globs, whose brace alternatives all name directories"
      pattern: "tests/index.test.ts"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run. When it runs out of room it stops and reports exactly which cases are done and which remain; it never samples a matrix and never shrinks one to fit."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit that eases the plan's own proof, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not write an absolute branch pair into any acceptance criterion or truth. It states completeness — hit equals found — and quotes the gate's own numbers in the summary."
    - "This plan must not carry a cast forward from either legacy suite. If a proof cannot be restated without one, that is a finding to report."
    - "This plan must not edit any production file, including the stale comment its own deletion creates."
    - "This plan must not copy the prototype preserved in the research scratchpad as the deliverable. That file is a COVERAGE ROUTE reference: it proves which inputs reach which branch. Its assertions are deliberately thin and do not meet the assertion contract."
---

<objective>
Close the pair contract: give the extension entry point its mirrored owner, and make the suite
actually run it.

Purpose: `extensions/pi-claude-marketplace/index.ts` is the last of 204 production modules without a
mirrored owner. Two legacy suites drive it today from the wrong path — one from the shared root, one
from the edge root — and between them they hold every cast that is absent from all thirty of the
previous phase's pairs. Neither reaches the two deferred failure catches, and merging their coverage
reports actively HIDES a third uncovered branch: run alone one of them emits an uncovered branch
range for the inner notify catch, and merged the runtime emits no range for that line at all and
reports the body as hit. That is the strongest available argument for taking the headline claim from
the focused per-pair run and never from the aggregate.

And a defect that would have made the whole deliverable invisible: every alternative in the unit-suite
globs names a DIRECTORY, so a file at the tests root matches none of them. Without the amendment the
pair gate would report this pair green while its owner never ran a single case under `npm test`. The
control for that landed first, in 117-01, precisely so this plan has to turn it RED before it can turn
it green — a measured plant obtained in the natural order of the work.

Output: the 204th pair, at complete direct coverage, running in the suite, with both legacy proxies
gone and the correspondence gate at zero violations.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/117-extension-entry-and-final-gate/117-VALIDATION.md
@.planning/phases/116-edge-surface/.continue-here.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the entry owner and make the unit suite run it</name>
  <files>tests/index.test.ts, package.json</files>
  <read_first>
    - extensions/pi-claude-marketplace/index.ts
    - extensions/pi-claude-marketplace/edge/register.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin-path.ts
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - extensions/pi-claude-marketplace/shared/session-env.ts
    - extensions/pi-claude-marketplace/platform/pi-api.ts
    - tests/shared/index-smoke.test.ts
    - tests/edge/index-handler.test.ts
    - tests/edge/register.test.ts
    - tests/edge/notification-boundary.ts
    - tests/edge/handlers/plugin/enable-disable.test.ts
    - .planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <behavior>
    - The default-exported async factory awaits the hooks-bridge registration before registering anything of its own, so every callback the bridge installs is present when the factory resolves.
    - It registers a resources-discover handler, a session-start handler that resets the session environment, the slash command, and exactly two read-only tools — and nothing else.
    - The discover handler resolves the project working directory from the EVENT at invocation time, not from the process.
    - A hydrate failure, a reconcile failure, a last-ditch notification failure, a plugin-PATH recompute failure, a plugin-PATH warning notification failure, and a session-id failure are each swallowed, and the handler still answers with a discovered-resource result.
    - A scope whose recorded install state does not parse produces exactly one warning notification naming that scope, and the plugin PATH for the other scope still applies.
    - A clean reconcile over a pristine working directory creates no directory under either scope root.
  </behavior>
  <action>
Read `extensions/pi-claude-marketplace/index.ts` and both legacy suites in full before writing
anything. Then read the research document's sections A, B and C: they carry the measured registration
order, the measured probe counts per fixture, the measured route to each unreached branch, and a
case-by-case disposition of both legacy suites. Do not re-derive any of it by experiment before you
have read it.

A prototype is preserved at
`/tmp/claude-1000/-home-acolomba-pi-claude-marketplace-unit-test-refactor/4b89fec9-1c29-4a8a-88ef-c3496865a093/scratchpad/proto-index.test.ts.txt`.
Read it. It is a COVERAGE ROUTE reference and nothing more: it proves which input reaches which
branch, cast-free, at complete coverage. Its assertions are deliberately thin — several assert only
that an array came back — and it drops the behavioural cases the legacy suites carry. Do NOT ship it.
Take its routes; write the assertions to the contract.

**Harness.** Follow `tests/edge/register.test.ts`, which is the nearest analog and the phase's
reference for a registration table. Per case: two temporary directories for the working directory and
the home, an own-property check saved for the home and agent-directory environment variables with the
agent-directory one DELETED rather than overwritten, restoration registered in the test context
BEFORE anything runs, and a fail-fast replacement of the network entry point the git transport
actually opens. That entry point is the HTTPS request function, not the global fetch — the fetch door
has exactly one production caller in this repository and the transport does not use it. The factory
hydrates the user scope off disk at load time, so a case without its own home touches the operator's
real one.

**The boundary.** Use `createNotificationBoundary(emissions, toolProbes)` and its returned Pi handle
and context. Research measured that the helper is NOT broken by the factory reading its registration
method as a value and binding it — that premise was false, and the resulting handle records the bound
call exactly as a direct one. From the tests root the helper is now a cross-tier import into the edge
root; this owner is a SEVENTH accepted cross-tier consumer beyond the six D-117-05 names, and the
summary must say so rather than leave it to be rediscovered.

Size `toolProbes` PER CASE by measuring it, never by deriving it from the emission count. Research
measured three different pairings on this module, including a fixture that emits nothing through the
boundary and still takes two probes. A wrong count fails naming the probe instead of the mistake.

**The registration table.** State one expectation per registration, with the event or command name
written out by hand as an exact argument and only the callback captured beside it, typed. Do not loop
over a union-typed array of event names — the platform's registration method is a large overload set
and a union-typed argument resolves none of them. Do not use a wildcard argument matcher: it costs
five unsafe-argument diagnostics and the house rules forbid it anyway. Type each capture by naming the
handler type or by instantiating the parameter type of the registration method.

Rebuild the legacy sorted-name comparison rather than carrying it: a sorted list cannot see
registration ORDER and cannot see a swapped handler, and it compares against a hand-copied array,
which is the weakest half of what the register owner measured. Per-registration expectations with
literal names replace it. Drop the legacy case that asserts the default export is a function and the
one that asserts the handler takes two parameters — the first restates a compiler fact and the second
is satisfied by any two-parameter arrow whatever it does with them.

**Cases to carry from the legacy suites**, each restated cast-free:

- The discover handler resolves the project working directory at INVOCATION time: a prompt seeded
  under the event's directory is discovered and one seeded under the process directory is not. This is
  a behavioural discriminator no coverage number can see.
- The plugin PATH is applied from a valid recorded install state, observed as the process environment
  footprint.
- A scope whose recorded install state does not parse produces exactly one warning notification naming
  that scope, compared as a whole value.
- The session-start handler applies the three session environment variables after reading the session
  id.
- The session-start handler swallows both a throwing session-id read and an absent session manager.
  Do NOT write these as two inputs asserted to produce the identical outcome — that phrasing is a
  tautology template. Either find an input where the two disagree, or keep ONE case and state plainly
  in the summary that both are the same defence-in-depth path with no discriminating observable.
- A clean reconcile over a pristine working directory answers with a discovered-resource result,
  emits nothing, and creates NEITHER scope root on disk. Keep the on-disk half: it is the invariant a
  production comment in the hooks event router gates a directory creation on.
- A reconcile failure surfaces as a single raw error-severity line beginning with the abort prefix,
  and a last-ditch notification failure on top of it is swallowed too.

**Cases that are new**, from the measured routes:

- The hydrate catch and the plugin-PATH recompute catch, each reached by a proxy over the discover
  EVENT that counts working-directory reads and refuses a chosen one. The proxy preserves the event
  type, so no assertion is needed; annotate the trap's return explicitly or the reflective read widens
  to a type the unsafe-return rule rejects. An existing edge suite already writes this form — copy it.
- The inner notification catch on the plugin-PATH warning path, reached by a workspace whose recorded
  install state does not parse together with a context whose notification member always throws.

**Assertions.** Every expected value is hand-authored. No expectation is produced by calling the code
under test, by re-reading a constant off the module under test, or by transforming an actual result.
Compare every public result and every notification list as one whole value. Assert public results and
state BEFORE promised interactions, and call the boundary verification last in every case. Titles
state public behaviour and may cite a durable requirement or decision anchor; they may not cite a
plan, phase, wave or task. Every runtime case carries separate lowercase arrange, act and assert phase
markers in that order, blank-line separated. Independent top-level cases only, no marker that
suppresses one.

**The offline question, asked as TWO questions.** The factory statically imports the module that
re-exports the git operations, so the transport IS in this module's import graph. That is not the same
as a reachable route to it. Ask separately: can any fixture this suite builds reach the transport, and
does any input through the captured handler turn it on. Measure the answer — do not assume it.
Whatever you find, install the fail-fast replacement and then label the disposition honestly: a
measurement if an input moves the counter, a labelled regression guard with no positive control if
none does. Do not present a vacuous zero as a measurement.

**The glob amendment, and the plant it hands you for free.** After the owner exists and passes the
focused pair command, run
`node --test tests/architecture/unit-suite-glob-completeness.test.ts` BEFORE touching the manifest and
RECORD VERBATIM what it printed. That is this plan's first plant and it costs nothing. Then re-read
`package.json` from disk — three workstreams touch these two script lines — and add the root-level
owner as a second quoted pattern to BOTH the `test` and the `test:coverage:unit` scripts, changing
nothing else in either string. Re-run the control; it must pass.

Then read three totals from runners, never computing any of them: the `ℹ tests` line from
`node --test tests/index.test.ts` alone, the `ℹ tests` line from `npm test` immediately BEFORE the
amendment, and the `ℹ tests` line from `npm test` immediately AFTER it. Report all three. The suite
count reported beside them counts describe blocks rather than files, so it may not move at all — do
not treat it as evidence the file ran.

Commit the owner and the manifest amendment together: the owner existing without the amendment is the
defect this plan exists to close.

**Plants (D-116-04).** Beyond the free glob plant, remove the hydrate-refusal case, run the pair gate,
record verbatim, restore. Then remove the plugin-PATH-recompute-refusal case, run the pair gate,
record verbatim, restore. Research measured both of these moving the branch DENOMINATOR rather than
only the numerator, so write no predicted numbers — run each and write down what it said. Then plant
the registration table: change one literal event name, record what the mock reported and at which call
site, revert. A plant that stays GREEN is a finding: narrow or strengthen the claim and report it.

If this plan runs out of room, STOP and report exactly which cases are written and which remain, and
which plants ran. Do not sample a matrix and do not shrink one to fit.

Change no production file.
  </action>
  <verify>
    <automated>node --test tests/index.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; node --test tests/architecture/unit-suite-glob-completeness.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/index.test.ts &amp;&amp; npm exec -- prettier --check tests/index.test.ts package.json &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|v8 ignore|istanbul ignore|as unknown as|as any|as never|anyTimes\(\)|It\.isAny\(\)|verifyAll\(|mock\.module\(|experimental-test-module-mocks|Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]' tests/index.test.ts &amp;&amp; rg -c '^\s*// arrange$' tests/index.test.ts &amp;&amp; rg -q 'tests/index.test.ts' package.json &amp;&amp; git diff --check -- tests/index.test.ts &amp;&amp; git diff --quiet -- extensions/</automated>
    <fails_when>Non-zero exit from any link: a `# fail` count above 0 in the focused run, a line beginning `Incomplete direct coverage for` from the pair gate, a failing glob-completeness control (which means an unamended glob), an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` or `npm test` exit, any match printed by the anti-pattern scan, `rg -c` finding zero arrange markers, the manifest scan not finding the root pattern, `git diff --check` reporting whitespace damage, or `git diff --quiet` exiting 1 because a production file changed.</fails_when>
  </verify>
  <acceptance_criteria>
`tests/index.test.ts` exists, imports the entry module directly, and passes alone. The pair gate prints
its passing verdict with hit equal to found on functions, lines and branches, with no coverage
exception anywhere in the repository — and the summary QUOTES the gate's own numbers rather than any
number written in this plan. Every registration is a hand-authored exact name with a typed capture
beside it; no sorted-name comparison and no wildcard matcher survives. The suite contains no double
assertion through unknown, no any-cast and no never-cast. Every case owns its own working directory
and home and restores the environment through the test context. Every case calls the boundary
verification last. Both unit-suite globs name the root-level owner and nothing else in either script
string changed. The pre-amendment run of the glob-completeness control is recorded verbatim as the
plan's first plant. Three totals are reported, each read from a runner's own `ℹ tests` line: the
owner alone, `npm test` before the amendment, and `npm test` after it. Every other plant this task
names was run and its verbatim output recorded. `git diff` shows no change under `extensions/`.
  </acceptance_criteria>
  <done>The extension entry point has its mirrored owner at complete direct coverage, and the unit suite actually runs it.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two legacy proxies and take the correspondence gate to zero</name>
  <files>tests/shared/index-smoke.test.ts, tests/edge/index-handler.test.ts</files>
  <read_first>
    - tests/index.test.ts
    - tests/shared/index-smoke.test.ts
    - tests/edge/index-handler.test.ts
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
    - scripts/check-corresponding-tests.mjs
    - .planning/WINDOWS.md
  </read_first>
  <action>
Before deleting either file, walk its case list one final time against the owner written in Task 1 and
confirm every surviving claim has a home. Name in the summary, case by case, where each went: carried,
merged into another case, or deliberately dropped with the reason. A claim that quietly disappears in
a deletion is indistinguishable from one that was never proven.

Then delete both files in one commit. This is not a rename and must not be presented as one: git
detects a rename by content similarity and a two-into-one rewrite shares nothing structural with
either original, so the history shows a delete plus a create and `git log --follow` will not bridge
it. Say so plainly rather than writing an acceptance criterion that cannot be met.

Run the correspondence gate. With the six earlier plans landed and these two files gone it must exit
0 and print its passing line. If it does not, STOP and report which rows remain and which plan owns
each — do not resolve someone else's row here.

**The finding this deletion creates.** `extensions/pi-claude-marketplace/bridges/hooks/event-router.ts`
names one of the deleted suites, by path, in the comment that justifies gating a directory creation on
the clean-reconcile invariant. Re-grep for it — do not trust a line number — and confirm the invariant
itself survives in the new owner. Then report the stale reference as a finding and file it in
`.planning/WINDOWS.md` as an open entry against that production file, with the kind used for a
comment asserting something the tree no longer supports. D-117-13 opens no production licence: this
is recorded, not fixed. Use the ledger's own tooling if it works and hand-edit if it does not,
checking the frontmatter counters afterwards either way.

Read the `npm test` total from the runner's own line after the deletion and report it beside the
three totals Task 1 recorded. It falls by the two deleted suites' case counts and rises by the
owner's; report the measured number and do not reconstruct it by arithmetic.

Change no production file, including the stale comment.
  </action>
  <verify>
    <automated>node scripts/check-corresponding-tests.mjs &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; npm run test:corresponding:negative &amp;&amp; npm run test:coverage:direct:negative &amp;&amp; test ! -e tests/shared/index-smoke.test.ts &amp;&amp; test ! -e tests/edge/index-handler.test.ts &amp;&amp; ! rg -n 'index-smoke' tests scripts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/index.ts &amp;&amp; git diff --quiet -- extensions/</automated>
    <fails_when>Non-zero exit from any link: the correspondence gate reporting any violation at all, an `error TS` line from typecheck, any ESLint problem line, a non-zero `fallow`, `npm test` or integration exit, either existing negative control failing, either deleted file still existing, a surviving reference to the deleted smoke suite under `tests` or `scripts`, a line beginning `Incomplete direct coverage for` from the pair gate, or `git diff --quiet` exiting 1 because a production file changed.</fails_when>
  </verify>
  <acceptance_criteria>
Both legacy suites are deleted and the correspondence gate exits 0 with its passing line, which is the
first time in this milestone it has. The summary maps every case of both deleted suites to its
disposition — carried, merged, or dropped with a reason. The stale production comment in the hooks
event router is reported as a finding and filed as an open entry in `.planning/WINDOWS.md`, and the
production file is unchanged. The clean-reconcile on-disk invariant that comment refers to is proven
to survive in the new owner. The summary states plainly that git records no rename for a two-into-one
rewrite. The post-deletion `npm test` total is read from the runner and reported beside Task 1's three
totals. Both pre-existing negative controls still pass. `git diff` shows no change under `extensions/`.
  </acceptance_criteria>
  <done>The correspondence gate reports zero violations, and the milestone's pair contract is complete at 204 of 204.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. Nothing under `extensions/` is modified.

Trust boundary in scope: the Pi extension host loading this factory. Every session-lifecycle event the
host emits crosses into this module, and the module's contract is that no failure ever propagates back
out.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-08-A | Denial of Service | the extension factory and its two lifecycle handlers, whose contract is that a throw never escapes and blocks the host load | high | mitigate | Every failure path is driven and asserted to be swallowed, including the three no existing suite reaches, and each is planted so its case is proven load-bearing rather than incidentally green. |
| T-117-08-B | Tampering | the registration table, which is almost entirely data no branch selects | high | mitigate | Every event, command and tool name is a hand-authored exact argument, and a name plant records what the mock reports at the call site. Coverage alone cannot see a wrong wiring. |
| T-117-08-C | Repudiation | the evidence that this pair is covered at all | high | mitigate | The verdict comes from the focused per-pair run; the merged report was measured to HIDE an uncovered branch on this very pair, so the aggregate is not merely weaker but wrong in the safe direction. |
| T-117-08-D | Information Disclosure | the operator's real home directory and recorded install state | medium | mitigate | Every case owns a temporary home, deletes the agent-directory variable, and restores through the test context, because the factory hydrates the user scope off disk at load time. |
| T-117-08-E | Spoofing | the network, reachable through the transport this module statically imports | medium | mitigate | A fail-fast replacement of the HTTPS request entry point is installed per case, and its disposition is labelled a measurement or a regression guard according to what was measured, not assumed. |
</threat_model>

<verification>
Run each gate separately and read its exit code. Do not use `npm run check`: its `format:check` link
fails on the operator's pre-existing untracked files (`.mcp.json`, seven
`.planning/research/.cache/*.json`) and short-circuits before the tests run, so a green result would
mean nothing. Do not touch those files and do not edit `.prettierignore`. SUITE-05 names the aggregate
gate; report it as the five separate commands plus the reason the aggregate cannot speak here.

Two commits: the owner together with the manifest amendment, then the two deletions. Before each,
scan the changed paths for secrets by the filesystem route with explicit literal paths — this shell
does not word-split and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem <the literal paths> --results=verified,unknown --fail`, confirming non-zero
chunk and byte counts. Then `npm exec -- prettier --check <the literal paths>` and
`SKIP=trufflehog,npm-format-check pre-commit run --files <the literal paths>`. Git hooks are not
installed here, so a clean `git commit` is not evidence hooks passed. Never `--all-files`, never
`--no-verify`. Use `git commit -F`. Stage only the literal paths this plan owns; never `git add -A`.

Confirm each commit with `git status --short` and `git log -1 --stat`.
</verification>

<success_criteria>

- The extension entry point has one mirrored owner at the tests root that imports it directly.
- The focused pair gate reports complete function, line and branch coverage, quoted from the gate.
- Both unit-suite globs name the root-level owner, and the glob-completeness control was recorded RED
  before the amendment and green after.
- The correspondence gate exits 0.
- Both legacy proxies are gone, every claim of theirs is accounted for, and no cast survives.
- The stale production comment the deletion creates is filed in the broken-windows ledger, not fixed.
- Nothing under `extensions/` changed.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-08-SUMMARY.md` when done.
</output>
