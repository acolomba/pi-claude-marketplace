---
phase: 117-extension-entry-and-final-gate
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/domain/github-auth.test.ts
files_deleted:
  - tests/shared/device-flow-prompt.test.ts
autonomous: true
requirements: [OWN-06, CASE-01, CASE-04, DEL-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The device-flow prompt evidence lives in the mirrored owner of the module it measures. The supplement drives `initiateDeviceFlow`, which `domain/github-auth.ts` exports and `tests/domain/github-auth.test.ts` owns, so D-117-01's fold branch applies and its relocate branch does not."
    - "The prompt's exact byte form and severity stay locked against the published output catalog, and the lock stays lockstep: changing the emission string in the production module without changing both the catalog and this case turns the case RED."
    - "The prompt is emitted before any token exists, so the asserted string carries no credential field. RED when: the emission is moved after the poll loop, which the ordering case detects."
    - "The correspondence gate stops reporting the supplement as an unexpected test and reports nothing new, because its cases now live in an owner the gate already expects."
    - "The pair keeps complete direct coverage of `domain/github-auth.ts` with the owner run alone. Measured after the fold, never assumed: a fold adds cases, and the branch denominator is a property of the suite rather than of the source."
    - "Any folded case that restates one the owner already carries is dropped and named in the summary with the case it duplicates. One case run twice is not evidence."
    - "The supplement's header claims it parallels an AUTH-03 case in the fold target. Grep finds no such case there today, so that claim is stale and must not be carried across — re-check it and report what you found."
  artifacts:
    - path: tests/domain/github-auth.test.ts
      provides: "Sole mirrored owner for domain/github-auth.ts, now carrying the device-flow prompt byte-form lock"
  key_links:
    - from: tests/domain/github-auth.test.ts
      to: docs/output-catalog.md
      via: "a hand-authored expected string compared byte for byte against the published prompt form"
      pattern: "device-flow-prompt"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not weaken the byte-form lock into a substring check. The supplement exists precisely because a substring assertion was too weak."
    - "This plan must not promise git a rename. A merge into an existing file shares nothing structural with the deleted one and git detects renames by similarity, so this is a delete plus an edit and the summary says so."
---

<objective>
Fold the device-flow prompt lock into the owner of the module it measures.

Purpose: the correspondence gate reports the prompt suite as an unexpected test. It is not spurious
evidence — it locks the exact bytes and severity of the out-of-band prompt against the published
output catalog, which a substring assertion cannot do. But it measures exactly one production module,
`domain/github-auth.ts`, and that module already has a mirrored owner. D-117-01's rule is that a
supplement measuring one module folds into that module's owner; only a supplement spanning modules
relocates.

Output: one fewer correspondence violation, and a github-auth owner that carries the byte-form lock it
should always have carried.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/116-edge-surface/.continue-here.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
@docs/output-catalog.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold the device-flow prompt byte-form lock into the github-auth owner</name>
  <files>tests/domain/github-auth.test.ts</files>
  <read_first>
    - tests/shared/device-flow-prompt.test.ts
    - tests/domain/github-auth.test.ts
    - tests/domain/device-flow-fake.ts
    - tests/domain/device-flow-contract.ts
    - tests/platform/credential-ops-fake.ts
    - extensions/pi-claude-marketplace/domain/github-auth.ts
    - docs/output-catalog.md
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Read both files in full before writing anything. The supplement is 153 lines carrying two cases; the
owner is roughly 1200 lines. This is a merge into an existing file, so it is a delete plus an edit —
git will not record a rename and must not be asked to. Do the fold and the deletion in ONE commit:
the tree does not have a coherent intermediate state where the cases exist in two places, and DEL-01's
one-concern rule is satisfied by "give the device-flow prompt lock its owner".

What to carry across:

- The byte-form case, which drives the initiation entrypoint through a fully controlled transport
  fake and credential fake plus a recording notify callback, and compares the emitted message and
  severity against a hand-authored expected value taken from the published output catalog. Keep the
  comparison as one whole value. Do not soften it to a substring check — the substring form is what
  this suite exists to replace.
- The ordering case, which proves the prompt is emitted before the poll loop and therefore before any
  token exists, so the expected string carries no credential field.

What to reconcile:

- Imports. The supplement's two support imports resolve differently from the owner's directory: the
  device-flow fake becomes a sibling and the credential fake keeps its cross-directory climb. The
  owner already imports the initiation entrypoint, so do not import it twice. Let ESLint settle the
  ordering; the house rule puts type-only imports last.
- Local helpers. The supplement declares its own notify-recorder type and factory. Check whether the
  owner already has an equivalent before copying one in — `fallow dupes` runs at a three-line
  threshold and a near-copy of an existing helper is a real failure, not a nuisance.
- Redundancy. Read the owner's existing cases before deciding what survives. If a folded case
  restates one the owner already carries, drop it and name in the summary which case it duplicated.
  Do not keep both: one case run twice is not evidence.

What to correct rather than carry:

- The supplement's header asserts that it parallels an existing case in the fold target. Grep the
  target for that anchor before you rely on the claim; it does not appear there today, so the sentence
  is stale. Re-run the grep, report what you actually found, and either drop the sentence or restate
  it against what is really there. Do not copy a claim you have not checked — a stale comment
  asserting a property the code does not have has already cost this milestone one wrong plan.
- The supplement's path header comment. Neither its old path nor a note about having moved belongs in
  the merged file; SUITE-03 forbids relocation history and git already holds it.

Every carried case keeps separate lowercase arrange, act and assert phase markers in that order. No
case is marked to be suppressed. Case titles state public behaviour and may cite a durable
requirement or decision anchor; they may not cite a plan, phase, wave or task.

Plant (D-116-04): change one character of the expected prompt string, run the owner, record verbatim
what it printed, and revert. That proves the lock is a byte comparison rather than a shape check. Then
run the second plant against the ordering claim: move the emission to after the poll completes in a
scratch copy of the production module, record what the ordering case said, and revert the production
file so `git diff --quiet -- extensions/` is clean. Write no predicted diagnostic; run each plant and
write down what it actually said. A plant that stays GREEN is a finding — narrow or strengthen the
claim and report it.

Re-measure the pair's direct coverage after the fold and report the numbers read from the gate. Do not
assume adding cases cannot move them: the branch denominator is a property of the suite, not of the
source, and this milestone has measured it move in both directions.

Change no production file. Any production plant is reverted before the commit and the verify block
proves it.
  </action>
  <verify>
    <automated>node --test tests/domain/github-auth.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/domain/github-auth.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/domain/github-auth.test.ts &amp;&amp; npm exec -- prettier --check tests/domain/github-auth.test.ts &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|v8 ignore|istanbul ignore|anyTimes\(\)|mock\.module\(|Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]|moved from|relocated|tests/shared/device-flow-prompt' tests/domain/github-auth.test.ts &amp;&amp; rg -c '^\s*// arrange$' tests/domain/github-auth.test.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'device-flow-prompt' &amp;&amp; test ! -e tests/shared/device-flow-prompt.test.ts &amp;&amp; git diff --quiet -- extensions/ package.json docs/output-catalog.md</automated>
    <fails_when>Non-zero exit from any link: a `# fail` count above 0 in the focused run, a line beginning `Incomplete direct coverage for` from the pair gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit including a new duplicate clone from a copied helper, a non-zero `npm test` exit, any match printed by the anti-pattern scan, `rg -c` finding zero arrange markers, the correspondence gate still naming the supplement, the supplement still existing, or `git diff --quiet` exiting 1 because a production file, `package.json` or the output catalog changed.</fails_when>
  </verify>
  <acceptance_criteria>
The supplement is deleted and its surviving evidence lives in the github-auth owner, in one commit.
The prompt comparison is a whole-value byte comparison against a hand-authored expected string, not a
substring check. The pair reports complete direct function, line and branch coverage with the owner
run alone, and the summary quotes the gate's own numbers rather than restating the previous ones. Both
plants were run, both verbatim outputs are recorded, and any production plant is reverted so
`git diff` shows no change under `extensions/`. Any dropped folded case is named in the summary
alongside the owner case that already carried its claim. The stale header claim about a paralleling
case is re-checked against the target and the finding is reported. The correspondence gate names the
supplement in no verdict. The summary states plainly that this is a delete plus an edit and that git
records no rename, because a merge into an existing file cannot show one.
  </acceptance_criteria>
  <done>The device-flow prompt lock lives in its module's mirrored owner, the pair is at complete direct coverage, and one more correspondence violation is resolved.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. The folded cases touch the GitHub device-flow entrypoint, so
ASVS V2 is in scope for what the evidence asserts, not for a change to the flow itself — nothing under
`extensions/` is modified.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-05-A | Information Disclosure | the out-of-band device-flow prompt, which is emitted before any token exists | high | mitigate | The ordering case proves the emission precedes the poll loop and the byte-form case compares the whole message, so a credential field appearing in the prompt fails both. Both are planted. |
| T-117-05-B | Repudiation | the published output-catalog byte form | high | mitigate | The expected string is hand-authored from the catalog and compared as one whole value, and the single-character plant proves the comparison is byte-level. The catalog itself is pinned by `git diff --quiet`. |
| T-117-05-C | Tampering | production sources, pinned by the plant that moves the emission | high | mitigate | The production plant is reverted before the commit and the verify block ends with `git diff --quiet -- extensions/ package.json docs/output-catalog.md`. |
</threat_model>

<verification>
Run each gate separately and read its exit code. Do not use `npm run check`: its `format:check` link
fails on the operator's pre-existing untracked files and short-circuits before the tests run. Do not
touch those files and do not edit `.prettierignore`.

One commit, containing both the merged owner and the deleted supplement. Before it, scan the changed
paths for secrets by the filesystem route with explicit literal paths — this shell does not word-split
and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/domain/github-auth.test.ts --results=verified,unknown --fail`, confirming
non-zero chunk and byte counts. Then `npm exec -- prettier --check tests/domain/github-auth.test.ts`
and `SKIP=trufflehog,npm-format-check pre-commit run --files tests/domain/github-auth.test.ts`. Git
hooks are not installed here, so a clean `git commit` is not evidence hooks passed. Never
`--all-files`, never `--no-verify`. Use `git commit -F`. Stage only the two literal paths this plan
owns; never `git add -A`.

Confirm with `git status --short` and `git log -1 --stat`.
</verification>

<success_criteria>

- The device-flow prompt supplement is gone and its surviving evidence is in the github-auth owner.
- The pair reports complete direct coverage with the owner run alone, quoted from the gate.
- Both plants went RED with their outputs recorded verbatim, and any production plant is reverted.
- The correspondence gate reports one fewer violation.
- Nothing under `extensions/`, `package.json` or the output catalog changed.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-05-SUMMARY.md` when done.
</output>
