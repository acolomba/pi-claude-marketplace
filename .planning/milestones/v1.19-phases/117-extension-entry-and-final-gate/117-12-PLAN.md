---
phase: 117-extension-entry-and-final-gate
plan: "12"
type: execute
wave: 6
depends_on: ["117-11"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [OWN-05, SUITE-06, MOD-10, PRES-01, PRES-02, DEL-04, SUITE-05]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The recorded pair total matches the measured one, per D-117-12. RED when: any document still reads 203 of 204 — the total appears in the roadmap progress table and twice in the state narrative, and the three drift independently."
    - "Every one of the 204 inventory rows carries a closed status, and none reads open. Measured before this plan: 154 rows read open, across phases 110 through 114 and 116 and the entry row — not the roughly 115 across 110 through 114 that the earlier estimate claimed, because the previous phase's own 30 rows were missed entirely by that count."
    - "The two module-completion requirements this milestone still leaves open are closed in BOTH places each appears: the checklist entry and the requirement-to-phase mapping row. RED when: only one is edited — that is exactly how one of them came to read pending while its phase was verified complete."
    - "The roadmap's phase-117 plan count agrees with the plans that actually exist, in all four places it appears: the prose count, the plan checklist, the progress-table row, and the milestone checkbox. The roadmap tooling verb mangles this file on every run measured, so every edit is made by hand and diffed."
    - "The previous phase's milestone checkbox is unchecked while its progress-table row reads complete. Found during planning, not inherited — fix both and report the drift."
    - "The broken-windows ledger's frontmatter counters agree with its rows after the entry filed by the entry-pair plan. RED when: a counter is left stale — the ship gate reads the open count."
    - "The entry pair adds NO coverage-shortfall entry to the ledger, because it reaches complete direct coverage. That is a measurement from the pair gate, not an assumption, and the summary quotes it."
    - "The consequences D-117-13 chose to record rather than fix are named in one place, still open: the two working-directory comments in the registration module, the two stale completion-data comments, and the flag re-export kept alive only for an architecture suite. Plus the stale test-path reference the entry-pair plan created."
    - "The tool parameter-description question stays OPEN for the operator per D-117-14 and is recorded as such. RED when: this plan decides it — the descriptions are part of a published tool contract and a pinned registration schema, so changing them is one-way."
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "The inventory, truthful: 204 closed rows and every phase-117 requirement dispositioned"
    - path: .planning/ROADMAP.md
      provides: "The roadmap, truthful: the pair total, the phase row, the plan count in all four places"
    - path: .planning/STATE.md
      provides: "The narrative, truthful: the closed pair total and the closed-phase position"
    - path: .planning/WINDOWS.md
      provides: "The ledger, with counters agreeing with its rows"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/117-extension-entry-and-final-gate/117-ALL-PAIR-RESULT.md
      via: "the measured all-pair result, which is the evidence every closed inventory row rests on"
      pattern: "204"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not touch any file outside the planning tree. It edits no source, no test, no script and no manifest."
    - "This plan must not use the roadmap plan-progress tooling verb. It mangled the roadmap on every run measured in the previous phase — injecting a duplicate plan list and collapsing the progress-table alignment. Hand-edit and diff."
    - "This plan must not resolve the tool parameter-description question, open a production licence, or fix any of the recorded findings."
    - "This plan must not close an inventory row whose phase is not verified complete. OWN-05 keeps a row open until its pair plan records complete guideline evidence."
---

<objective>
Make the inventory tell the truth, last, so it records measured outcomes rather than intended ones.

Purpose: SUITE-06 asks that the final inventory contain exactly the production modules at the accepted
baseline with no missing or unexpected owner test. The tree now satisfies that; the documents do not.
Measured before this plan: the roadmap still reports 203 of 204 and marks the phase not started, the
state narrative says one pair remains in two places, 154 of the 204 inventory rows still read open
across six phases, and two module-completion requirements read pending in one place each while being
complete in another.

This is carrier work for the pair MOD-10 owns, not a verification-only plan: it changes four
artifacts to match measurements taken by earlier plans, and MOD-10 cannot close until it does.

The tooling here is known-broken in specific ways and every one of them was measured in the previous
phase. Hand-edit, diff, and check the counters afterwards.

Output: four planning documents that agree with the tree and with each other.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/116-edge-surface/.continue-here.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close the inventory rows and the requirement dispositions</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/117-extension-entry-and-final-gate/117-ALL-PAIR-RESULT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Re-derive the counts from the file before editing it. The operator edits this checkout concurrently
and the figures below are a measurement taken during planning, not a promise:

- the per-pair status column carries 154 rows reading open, spread across phases 110, 111, 112, 113,
  114, 116 and the single entry row. The drift is NOT a contiguous run — phase 115's rows were swept
  and phase 116's were not, which the earlier estimate of roughly 115 rows across 110 to 114 missed
  entirely. Count them yourself and report the number you found beside the number here.

Close every one of those rows. OWN-05 keeps a row open until its pair plan records complete guideline
evidence, so state in the summary what the evidence is: each of those phases is verified complete with
per-plan summaries on disk, and the all-pair result this phase produced carries a verdict for every
row. Do not close a row belonging to a phase that is not verified complete — check the roadmap's
progress table before you sweep a block.

Then close the two module-completion requirements, each in BOTH places it appears: the checklist entry
near the top and the requirement-to-phase mapping row near the bottom. One of them already reads
complete in one place and pending in the other, which is precisely the failure mode of editing only
one.

Then disposition every requirement this phase owns in the mapping table. For each, mark it complete
only if a plan in this phase actually satisfied it, and read the plan summaries rather than assuming.
Two need care:

- the requirement about the aggregate repository gate cannot be discharged by that aggregate command
  in this checkout — its formatting link fails on the operator's untracked files and short-circuits
  before the tests. It is satisfied by the five separate gates plus the two negative controls and the
  correspondence gate, as recorded in the all-pair result. Say so in the summary rather than marking it
  complete silently.
- the requirement about the all-pair record is met under the reading D-117-20 fixed: numeric records
  for the emitting modules and named type-only verdicts for the rest. Cite the artifact.

Change nothing outside the planning tree.
  </action>
  <verify>
    <automated>! rg -n '\| Open +\|' .planning/REQUIREMENTS.md &amp;&amp; ! rg -n '\| MOD-07 +\| Phase 114 +\| Pending' .planning/REQUIREMENTS.md &amp;&amp; ! rg -n '\| MOD-10 +\| Phase 117 +\| Pending' .planning/REQUIREMENTS.md &amp;&amp; ! rg -n '^- \[ \] \*\*MOD-(07|10)\*\*' .planning/REQUIREMENTS.md &amp;&amp; test "$(rg -c '^\| P1[0-9][0-9]-[0-9]' .planning/REQUIREMENTS.md)" = "204" &amp;&amp; git diff --quiet -- extensions/ tests/ scripts/ package.json</automated>
    <fails_when>Non-zero exit from any link: any inventory row still reading open, either module-completion requirement still reading pending in its mapping row or unchecked in its checklist entry, an inventory row count other than 204, or `git diff --quiet` exiting 1 because something outside the planning tree changed.</fails_when>
  </verify>
  <acceptance_criteria>
No inventory row reads open, the row count is 204, and both module-completion requirements are closed
in both of their places. Every phase-117 requirement in the mapping table carries a disposition
supported by a plan summary that was actually read. The summary reports the open-row count measured
before the sweep beside the 154 recorded here, and states the evidence each closed block rests on.
The aggregate-gate requirement's disposition is explained rather than asserted. Nothing outside the
planning tree changed.
  </acceptance_criteria>
  <done>The inventory carries 204 closed rows and every phase-117 requirement is dispositioned against read evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Bring the roadmap and the state narrative to the measured totals</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/117-extension-entry-and-final-gate/117-ALL-PAIR-RESULT.md
  </read_first>
  <action>
Hand-edit both files and diff every change. Do NOT use the roadmap plan-progress tooling verb: it
mangled this file on every run measured in the previous phase, injecting a duplicate bare plan list
beside the descriptive one and collapsing the progress-table row alignment.

In the roadmap, five things move and they drift independently, so find each by reading rather than by
line number:

1. the progress table's total row, which still reports 203 of 204 and a status of in progress;
2. the progress-table row for this phase, which reports zero of one and not started;
3. the prose plan count for this phase, which reports one plan;
4. the plan checklist under this phase, which lists one plan — replace it with the plans that actually
   exist, each with its one-line objective;
5. the milestone checkbox for this phase.

And a sixth, found during planning and not inherited: the milestone checkbox for the PREVIOUS phase is
unchecked while its progress-table row reads complete with a date. Fix both, and report the drift in
the summary as a finding rather than folding it in silently.

Guard against a known false positive while you write: prose containing a shipped-past-tense keyword
anywhere in the roadmap makes the milestone tooling report the milestone complete regardless of the
state file. Grep for it after editing and reword if you introduced one.

In the state narrative, two places still say one pair remains. One is inside the previous phase's
history block and is legitimately history — leave it, or restate it as history explicitly. The other
two are current-position prose and must read the closed total. Rewrite the inherited-work section for
the closed state: what this phase delivered, what it recorded rather than fixed, and what remains open
for the operator.

Keep the current-position line naming the plans that completed, in the shape the file already uses.
The position tooling increments blindly and sometimes reverts to a bare ordinal that reads as though
plans finished in roadmap order; check the line after any tooling call and repair it by hand. The
metric verb double-increments the completed-plan count, so check that value too and correct it. The
phase-completion verb cannot write the root planning files while workstream mode is active and neither
workstream holds this milestone — every phase transition since two phases ago was hand-applied and
this one will be too. It also truncates a multi-line frontmatter field to its first line and leaks the
remainder into the position prose, so keep any such field on one line and re-read the block afterwards.

Change nothing outside the planning tree.
  </action>
  <verify>
    <automated>! rg -n '203/204|Two hundred three of 204|One pair remains' .planning/ROADMAP.md .planning/STATE.md &amp;&amp; rg -q '204/204' .planning/ROADMAP.md &amp;&amp; ! rg -n '^- \[ \] \*\*Phase 11[67]:' .planning/ROADMAP.md &amp;&amp; ! rg -n '\*\*Plans\*\*: 1 plan' .planning/ROADMAP.md &amp;&amp; ! rg -ni 'shipped' .planning/ROADMAP.md &amp;&amp; git diff --quiet -- extensions/ tests/ scripts/ package.json</automated>
    <fails_when>Non-zero exit from any link: any surviving 203-of-204 total or one-pair-remains prose in either file, the closed total missing from the roadmap, either recent phase's milestone checkbox still unchecked, the stale single-plan prose count surviving, a shipped-keyword false positive introduced anywhere in the roadmap, or `git diff --quiet` exiting 1 because something outside the planning tree changed.</fails_when>
  </verify>
  <acceptance_criteria>
The roadmap reports the closed pair total, a complete status for this phase with its real plan count
in all four places, and checked milestone boxes for both recent phases. The previous phase's checkbox
drift is reported as a finding. The state narrative reads the closed total in both current-position
places, the history block is either left as history or explicitly restated as such, and the
inherited-work section is rewritten for the closed state. No shipped-keyword false positive exists in
the roadmap. Every tooling verb used had its output diffed and repaired by hand, and the summary names
which verbs were used and what each damaged. Nothing outside the planning tree changed.
  </acceptance_criteria>
  <done>The roadmap and the state narrative report the measured totals and the real plan count.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the broken-windows ledger and record what stays open</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - .planning/WINDOWS.md
    - .planning/phases/117-extension-entry-and-final-gate/117-08-SUMMARY.md
    - .planning/phases/117-extension-entry-and-final-gate/117-ALL-PAIR-RESULT.md
  </read_first>
  <action>
Count the ledger's rows and its status values yourself. Measured during planning at 22 rows with 17
open and 5 fixed, with the frontmatter counters agreeing — plus whatever the entry-pair plan filed for
the stale test-path reference it created in the hooks event router. Re-count and make the frontmatter
counters agree with the rows either way. The ship gate reads the open count.

Confirm from the all-pair result that the entry pair added NO coverage-shortfall entry: it reaches
complete direct coverage, so there is no shortfall identity to pin. Quote the gate's verdict for that
pair rather than asserting it.

Then write, in one place in the phase summary rather than as new ledger rows, the set of things
D-117-13 chose to record rather than fix, so a later reader finds them together:

- the two comments in the registration module asserting a working-directory capture the code does not
  make — already an open ledger entry, so cite it rather than duplicating it;
- the two stale comments in the completion-data module: one advertising an export that does not exist,
  one describing a flag as widening a set it in fact shifts;
- the flag constant re-exported from a handler solely to keep an architecture suite compiling;
- the stale test-path reference the entry-pair plan created in the hooks event router, which should
  already be a ledger entry filed by that plan — confirm it is, and file it if it is not.

Record separately, and explicitly as still OPEN for the operator, the tool parameter-description
question D-117-14 holds back. Do not decide it: the descriptions are part of a published tool contract
that a language model consumes and of a pinned registration schema, so a later revert would be a
second contract change rather than an undo.

Note also the one documentation drift found during research, outside the source tree: an architecture
document describes an aggregate bridges barrel that does not exist. It is a planning document, not
production, and it is out of this phase's scope — record it, do not fix it.

Change nothing outside the planning tree.
  </action>
  <verify>
    <automated>awk -F'|' '/^total_count: /{split($0,a,": "); T=a[2]} /^open_count: /{split($0,a,": "); O=a[2]} /^fixed_count: /{split($0,a,": "); F=a[2]} /^\| [0-9]+ \|/{n++; s=$8; gsub(/ /,"",s); c[s]++} END {printf "rows=%d open=%d fixed=%d declared=%d/%d/%d\n", n, c["open"], c["fixed"], T, O, F; if (n!=T || c["open"]!=O || c["fixed"]!=F) exit 1}' .planning/WINDOWS.md &amp;&amp; rg -q 'event-router' .planning/WINDOWS.md &amp;&amp; git diff --quiet -- extensions/ tests/ scripts/ package.json</automated>
    <fails_when>Non-zero exit from any link: the counter check throwing because a frontmatter counter disagrees with the rows it summarises, the ledger carrying no entry for the stale test-path reference in the hooks event router, or `git diff --quiet` exiting 1 because something outside the planning tree changed.</fails_when>
  </verify>
  <acceptance_criteria>
The ledger's frontmatter counters agree with its rows, verified by a script that counts them rather
than by reading the header. The entry-pair plan's finding about the stale test-path reference is
present as an entry. The summary quotes the entry pair's own coverage verdict as the evidence that it
files no shortfall entry. The five recorded-not-fixed items are listed together in one place, the tool
parameter-description question is recorded as still open for the operator and is NOT decided, and the
architecture-document drift is recorded and not fixed. Nothing outside the planning tree changed.
  </acceptance_criteria>
  <done>The ledger is internally consistent and everything this phase chose to record rather than fix is written down in one place.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. This plan edits four planning documents and touches no source,
test, script or manifest; it changes no runtime surface and no trust boundary.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-12-A | Repudiation | the inventory, which is the milestone's own record of what was proven | high | mitigate | Every closed row rests on a phase verified complete plus the retained all-pair result, and the pre-sweep open count is re-measured and reported beside the planned figure rather than assumed. |
| T-117-12-B | Tampering | the roadmap, which the plan-progress tooling verb corrupts on every run measured | high | mitigate | The verb is prohibited outright; every edit is made by hand, diffed, and checked against a shipped-keyword false positive that would otherwise make the milestone tooling report completion regardless of the state file. |
| T-117-12-C | Repudiation | the ship gate, which reads the ledger's open count | medium | mitigate | A script counts the rows by status and compares them against the frontmatter counters, so a stale counter fails rather than silently unblocking the gate. |
| T-117-12-D | Elevation of Privilege | a published tool contract a language model consumes | high | accept | The parameter-description question is one-way and is deliberately left open for the operator per D-117-14; this plan records it and does not decide it. |
</threat_model>

<verification>
This plan runs no test gate of its own beyond confirming that nothing outside the planning tree
changed — it edits documents, and its correctness is checked by the scans in each task's verify block
plus the diff of every hand-edit.

Expect the tooling to misbehave, and repair it by hand: the roadmap plan-progress verb corrupts the
file every time, the position verb increments blindly and sometimes reverts to a bare ordinal, the
metric verb double-increments the completed-plan count, the progress verb writes nothing, the
phase-completion verb cannot write the root planning files while workstream mode is active and
truncates a multi-line frontmatter field to its first line, and the decision verb rejects a summary
path outside the repository root. Check the file after every call.

Commit three times, one per task. Before each, scan the changed paths for secrets by the filesystem
route with explicit literal paths — this shell does not word-split and trufflehog's git-mode scan
aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem <the literal paths> --results=verified,unknown --fail`, confirming non-zero
chunk and byte counts. Then
`SKIP=trufflehog,npm-format-check pre-commit run --files <the literal paths>`. The planning tree is
excluded from the markdown formatters, so expect those hooks to skip. Git hooks are not installed
here, so a clean `git commit` is not evidence hooks passed. Never `--all-files`, never `--no-verify`.
Use `git commit -F`. Stage only the literal paths this plan owns; never `git add -A`.

Confirm each commit with `git status --short` and `git log -1 --stat`.
</verification>

<success_criteria>

- No inventory row reads open, and the row count is 204.
- Both module-completion requirements are closed in both of their places, and every phase-117
  requirement carries a disposition read from a plan summary.
- The roadmap reports the closed pair total and this phase's real plan count in all four places, with
  both recent milestone checkboxes checked.
- The state narrative reads the closed total in every current-position place.
- The ledger's counters agree with its rows, verified by counting.
- Everything recorded rather than fixed is written down in one place, and the tool
  parameter-description question is still open.
- Nothing outside the planning tree changed.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-12-SUMMARY.md` when done.
</output>
