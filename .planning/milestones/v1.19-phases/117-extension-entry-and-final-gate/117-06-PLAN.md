---
phase: 117-extension-entry-and-final-gate
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/orchestrators/marketplace/shared.test.ts
files_deleted:
  - tests/orchestrators/marketplace/cascade.test.ts
autonomous: true
requirements: [OWN-06, CASE-02, CASE-03, DEL-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The cross-bridge cascade evidence lives in the mirrored owner of the module it measures. The supplement drives `cascadeUnstagePlugin`, which `orchestrators/marketplace/shared.ts` exports and `tests/orchestrators/marketplace/shared.test.ts` already owns and already imports, so D-117-01's fold branch applies."
    - "The on-disk half of the cascade stays proven: the hooks subtree is really removed from the scope root, and removal is idempotent when the subtree is absent. RED when: the removal is reduced to an outcome-shape assertion, which cannot see whether anything left the disk."
    - "The fold reuses the owner's existing plugin-record factory rather than copying the supplement's. RED when: a near-copy is carried across — `fallow dupes` runs at a three-line threshold and reports it."
    - "Every folded case that restates one of the owner's five existing cascade cases is dropped and named in the summary alongside the case that already carried its claim."
    - "The pair keeps complete direct coverage of `orchestrators/marketplace/shared.ts` with the owner run alone. Measured after the fold, never assumed."
    - "The fold needs no specifier change: both files sit in the same directory and the owner already imports the same two production modules by the same paths. Confirmed by reading both, not inferred."
  artifacts:
    - path: tests/orchestrators/marketplace/shared.test.ts
      provides: "Sole mirrored owner for orchestrators/marketplace/shared.ts, now carrying the on-disk cross-bridge cascade evidence"
  key_links:
    - from: tests/orchestrators/marketplace/shared.test.ts
      to: extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
      via: "the cascade entrypoint driven against a real temporary scope root, with the surviving tree read back"
      pattern: "cascadeUnstagePlugin"
  prohibitions:
    - "A plan must not report a proof, a plant, or a coverage result it did not actually run."
    - "A plan must not buy a green gate by weakening the evidence: no coverage exception or ignore pragma, no production edit, and no symbol exported from production for a test."
    - "A plan must not damage work it does not own. It stages only the explicit paths it wrote, never every file, never with hooks disabled, and it does not edit, revert, or clean the operator's untracked or unrelated modified files."
    - "This plan must not replace an on-disk read with an assertion about the returned outcome. A destructive verb is owned through its footprint; a returned shape can be right while nothing left the disk."
    - "This plan must not promise git a rename. A merge into an existing file is a delete plus an edit and the summary says so."
---

<objective>
Fold the cross-bridge cascade supplement into the owner of the module it measures.

Purpose: the correspondence gate reports the cascade suite as an unexpected test. Its two cases are
real evidence — they drive the unstage cascade against a real temporary scope root and read the
surviving tree back, which is the footprint half no outcome-shape assertion can supply. But they
measure exactly one production module, `orchestrators/marketplace/shared.ts`, whose mirrored owner
already exists, already imports the same entrypoint from the same path, and already carries five
cascade cases of its own. D-117-01 folds a single-module supplement into that owner.

Both files sit in the same directory, so this fold costs no specifier change. What it does cost is
judgement: the owner has its own plugin-record factory and its own cascade cases, and carrying a
near-duplicate of either would trade one violation for a duplicate clone.

Output: one fewer correspondence violation, and the on-disk cascade evidence in the owner that should
always have held it.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-unit-test-refactor/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-extension-entry-and-final-gate/117-CONTEXT.md
@.planning/phases/117-extension-entry-and-final-gate/117-RESEARCH.md
@.planning/phases/116-edge-surface/.continue-here.md
@.claude/rules/typescript-unit-testing.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold the cross-bridge cascade evidence into the marketplace shared owner</name>
  <files>tests/orchestrators/marketplace/shared.test.ts</files>
  <read_first>
    - tests/orchestrators/marketplace/cascade.test.ts
    - tests/orchestrators/marketplace/shared.test.ts
    - extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts
    - extensions/pi-claude-marketplace/persistence/locations.ts
    - .claude/rules/typescript-unit-testing.md
  </read_first>
  <action>
Read both files in full before writing anything. The supplement is 127 lines carrying two cases; the
owner is roughly 1200 lines carrying about thirty, five of which already drive the same cascade
entrypoint. This is a merge into an existing file, so it is a delete plus an edit — git will not
record a rename and must not be asked to. Do the fold and the deletion in ONE commit; there is no
coherent intermediate state where the cases live in two places, and DEL-01's one-concern rule is
satisfied by "give the cross-bridge cascade evidence its owner".

Before carrying anything, answer three questions from the two files and record the answers in the
summary:

1. Which of the owner's five existing cascade cases, if any, already proves what a folded case
   proves? Read them. If one does, drop the folded case and name the pair. Keeping both is one case
   run twice, which is not evidence.
2. What does each folded case observe that an outcome-shape assertion cannot? Both read the surviving
   tree under a real temporary scope root. That footprint half is the reason they are worth keeping —
   a destructive verb is owned through its footprint, and a returned outcome can be correct while
   nothing left the disk. Whatever survives the fold keeps its on-disk read.
3. Does the owner's existing plugin-record factory produce a record the folded cases can use? If it
   does, use it and delete the supplement's own factory rather than carrying it across. If it does
   not, extend the owner's factory rather than adding a second one — `fallow dupes` runs at a
   three-line threshold and two near-identical factories in one file is exactly what it reports.

Reconciliation notes measured on the current tree, to be re-derived rather than trusted:

- Both files import the cascade entrypoint and the scope-locations bundle from the same production
  paths, because they sit in the same directory. No specifier changes.
- The owner uses `strong-mock` and a test context; the supplement uses plain temporary directories and
  no mock. Neither mechanism is wrong: the doubles must match their role in the case, and a cascade
  case that reads a real tree needs no interaction mock. Do not convert a footprint case into a mock
  case to make the file look uniform.
- Each carried case owns its own temporary directory, created per case and removed through the test
  context. Do not share one across cases.

Every carried case keeps separate lowercase arrange, act and assert phase markers in that order,
blank-line separated. No case is marked to be suppressed. Case titles state public behaviour and name
values after their production roles; they may cite a durable requirement or decision anchor and may
not cite a plan, phase, wave or task. Add no note about the file having absorbed another — SUITE-03
forbids relocation history and git already holds it.

Plant (D-116-04): delete the hooks-subtree removal from the production cascade, run the owner, record
verbatim what it printed, and revert so `git diff --quiet -- extensions/` is clean. That is the plant
that separates a real footprint read from an assertion about a returned outcome — if the case stays
GREEN, the on-disk half is not load-bearing and that is a finding to report and fix, not to paper
over. Write no predicted diagnostic; run the plant and write down what it actually said.

Re-measure the pair's direct coverage after the fold and report the numbers read from the gate. Do not
assume adding cases cannot move them.

Change no production file. The plant is reverted before the commit and the verify block proves it.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/shared.test.ts &amp;&amp; npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/marketplace/shared.ts &amp;&amp; npm run typecheck &amp;&amp; npm exec -- eslint tests/orchestrators/marketplace/shared.test.ts &amp;&amp; npm exec -- prettier --check tests/orchestrators/marketplace/shared.test.ts &amp;&amp; npm run fallow &amp;&amp; npm test &amp;&amp; ! rg -n '(?:test|t)\.(?:only|skip|todo)\(|node:coverage ignore|c8 ignore|v8 ignore|istanbul ignore|anyTimes\(\)|mock\.module\(|Phase [0-9]|Plan [0-9]|Wave [0-9]|Pitfall [0-9]|moved from|relocated|cascade\.test\.ts' tests/orchestrators/marketplace/shared.test.ts &amp;&amp; rg -c '^\s*// arrange$' tests/orchestrators/marketplace/shared.test.ts &amp;&amp; ! node scripts/check-corresponding-tests.mjs 2>&amp;1 | rg -n 'marketplace/cascade' &amp;&amp; test ! -e tests/orchestrators/marketplace/cascade.test.ts &amp;&amp; git diff --quiet -- extensions/ package.json</automated>
    <fails_when>Non-zero exit from any link: a `# fail` count above 0 in the focused run, a line beginning `Incomplete direct coverage for` from the pair gate, an `error TS` line from typecheck, any ESLint problem line, a `[warn]` path from the format check, a non-zero `fallow` exit including a duplicate clone from a copied record factory, a non-zero `npm test` exit, any match printed by the anti-pattern scan, `rg -c` finding zero arrange markers, the correspondence gate still naming the supplement, the supplement still existing, or `git diff --quiet` exiting 1 because a production file or `package.json` changed.</fails_when>
  </verify>
  <acceptance_criteria>
The supplement is deleted and its surviving evidence lives in the marketplace shared owner, in one
commit. Every surviving folded case still reads the on-disk tree rather than only the returned
outcome. Exactly one plugin-record factory exists in the merged file and `fallow dupes` is silent.
The pair reports complete direct function, line and branch coverage with the owner run alone, quoted
from the gate rather than restated from before. The plant was run, its verbatim output is recorded,
and the production file is reverted so `git diff` shows no change under `extensions/`. Each dropped
folded case is named alongside the owner case that already carried its claim, and the summary answers
all three pre-fold questions. The correspondence gate names the supplement in no verdict. The summary
states plainly that git records no rename because a merge into an existing file cannot show one.
  </acceptance_criteria>
  <done>The cross-bridge cascade evidence lives in its module's mirrored owner, the pair is at complete direct coverage, and one more correspondence violation is resolved.</done>
</task>

</tasks>

<threat_model>
Security policy: OWASP ASVS Level 1. No package is installed, removed or upgraded, so the
package-legitimacy gate does not apply. Nothing under `extensions/` is modified.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-117-06-A | Tampering | the unstage cascade, a destructive verb that removes subtrees under a scope root | high | mitigate | The surviving cases read the real tree back after the cascade rather than trusting the returned outcome, and the plant that deletes the hooks-subtree removal proves that read is load-bearing. |
| T-117-06-B | Repudiation | evidence that removal is idempotent when the subtree is already absent | medium | mitigate | The idempotence case is carried across or explicitly named as duplicated by an existing owner case; it is not silently dropped. |
| T-117-06-C | Tampering | production sources, pinned by the plant | high | mitigate | The plant is reverted before the commit and the verify block ends with `git diff --quiet -- extensions/ package.json`. |
</threat_model>

<verification>
Run each gate separately and read its exit code. Do not use `npm run check`: its `format:check` link
fails on the operator's pre-existing untracked files and short-circuits before the tests run. Do not
touch those files and do not edit `.prettierignore`.

One commit, containing both the merged owner and the deleted supplement. Before it, scan the changed
path for secrets by the filesystem route with an explicit literal path — this shell does not
word-split and trufflehog's git-mode scan aborts structurally here:
`TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)`
then `"$TH" filesystem tests/orchestrators/marketplace/shared.test.ts --results=verified,unknown --fail`,
confirming non-zero chunk and byte counts. Then
`npm exec -- prettier --check tests/orchestrators/marketplace/shared.test.ts` and
`SKIP=trufflehog,npm-format-check pre-commit run --files tests/orchestrators/marketplace/shared.test.ts`.
Git hooks are not installed here, so a clean `git commit` is not evidence hooks passed. Never
`--all-files`, never `--no-verify`. Use `git commit -F`. Stage only the two literal paths this plan
owns; never `git add -A`.

Confirm with `git status --short` and `git log -1 --stat`.
</verification>

<success_criteria>

- The cascade supplement is gone and its surviving evidence is in the marketplace shared owner.
- The pair reports complete direct coverage with the owner run alone, quoted from the gate.
- The plant went RED with its output recorded verbatim, and the production file is reverted.
- Exactly one plugin-record factory survives in the merged file.
- The correspondence gate reports one fewer violation.
- Nothing under `extensions/` and nothing in `package.json` changed.
</success_criteria>

<output>
Create `.planning/phases/117-extension-entry-and-final-gate/117-06-SUMMARY.md` when done.
</output>
