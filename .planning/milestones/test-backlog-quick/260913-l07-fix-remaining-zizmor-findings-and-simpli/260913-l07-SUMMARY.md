---
quick_id: 260913-l07
slug: fix-remaining-zizmor-findings-and-simpli
date: 2026-09-13
status: complete
tasks: 3
commits: 1
plan_head_before: c4571c73329eb511641968bd019feb6f073455aa
---

# Quick Task 260913-l07 Summary

Removed zizmor's `--min-severity high` floor, fixed all 14 findings it then
reported, replaced the archived prettier mirror with a local hook, and rewrote
the seven affected comments in plain language with the Sonar rule IDs stripped.

## What changed

**Commit `729348b4`** — `ci: fix all zizmor findings and drop the severity floor`
(10 files, +41/-41).

| Task | What | Files |
| ---- | ---- | ----- |
| 1 | Dropped the severity floor, applied `zizmor --fix=all` | `.pre-commit-config.yaml`, `.github/dependabot.yml`, `ci.yml`, `e2e-nightly.yml`, `lint.yml`, `publish.yml`, `sonarcloud.yml` |
| 2 | Replaced the archived `mirrors-prettier` repo with a local hook | `.pre-commit-config.yaml` |
| 3 | Rewrote five comment sites, dropped every rule ID | `zizmor.yml`, `.pre-commit-config.yaml`, `scripts/check-workflow-install-scripts.mjs`, `scripts/check-workflow-install-scripts.negative.mjs` |

The auto-fixer's output matched the plan's prediction exactly — 13 hunks across
6 files, `.pre-commit-config.yaml` untouched by the fixer:

- `persist-credentials: false` on all 10 `actions/checkout` steps: 6 new `with:`
  blocks, 4 keys appended to existing ones. The `ci.yml` `fetch-depth: 0` step
  kept its `D-08-15` comment.
- `publish.yml:26` `uses: ./.github/workflows/ci.yml` → `$/.github/workflows/ci.yml`.
- `cooldown: default-days: 7` on both `.github/dependabot.yml` ecosystem entries.

Nothing landed outside that set, so no stop-and-report was needed.

## Behavior change

**Dependabot now delays a pull request until the release is 7 days old**
(`cooldown: default-days: 7`, both ecosystems). Recorded in the commit body.

## Verification

| Check | Result |
| ----- | ------ |
| `zizmor` over workflows + dependabot + pre-commit config, **no severity floor** | exit **0**, `No findings to report` (9 suppressed) |
| `git diff --stat` after `--fix=all` | exactly the 6 predicted files + the manual `.pre-commit-config.yaml` edit |
| `grep -c 'persist-credentials: false' .github/workflows/*.yml` | 10 total — ci 5, e2e-nightly 1, lint 2, publish 1, sonarcloud 1 |
| `npm run lint:workflows` | exit 0 |
| `npm run lint:workflows:negative` | exit 0 — gate still fires on the planted violation |
| `npm run format:check` | exit 0 |
| `SKIP=trufflehog pre-commit run --all-files` | exit 0, every hook `Passed` |
| new local `prettier` hook visible in that output | **`prettier ... Passed`** — ran, did not report `(no files to check)Skipped` |
| `grep -rn "S6505\|S7637\|githubactions:" zizmor.yml .pre-commit-config.yaml scripts/` | no matches |

Baseline before the change was re-measured and matched the plan: 23 raw findings,
9 suppressed, 14 reported — 11 low, 3 medium, 0 high.

## Deviations from plan

**None in substance.** One structural note:

**Single commit for all three tasks, as the plan specifies.** The plan carries
exactly one commit instruction, at the end of Task 3, and this is not merely
stylistic: Task 1 alone cannot be committed. It stages `.pre-commit-config.yaml`,
which still holds the archived `mirrors-prettier` repo — the one `archived-uses`
finding zizmor cannot auto-fix. With the floor removed, the zizmor hook fails on
that staged file, and `--no-verify` is forbidden. Task 2 is what closes it, so
Tasks 1 and 2 are inseparable at the commit boundary.

The plan's three corrections to the handed-down brief were all honored:

1. The prettier hook's `files:` is `'\.(js|json|ts)$|^scripts/.*\.mjs$'`, not a
   bare `\.mjs$`. `.planning/inputs/` never entered a staged diff.
2. `- id: prettier` is the **first** hook in the `- repo: local` block, ahead of
   `npm-format-check`.
3. Entry is `node_modules/.bin/prettier --write`, not `npx prettier`.

The `$/` change in `publish.yml` was left in place. `actionlint` is not in this
repository's pipeline and was not run.

## Notes

- **No pre-commit git hook is installed in this checkout** (the hooks directory
  holds only `post-*` and `pre-push`), so `git commit` fired nothing. The gate
  was satisfied by running `SKIP=trufflehog pre-commit run --all-files` by hand
  immediately before committing, per CLAUDE.md.
- No writing hook (`prettier`, `yamlfmt`, `mdformat`, `texthooks`) rewrote a
  file, so no follow-up commit was needed. `git status` after the commit is clean
  apart from this untracked planning directory.
- TruffleHog was skipped throughout. It fails in this checkout with
  `failed to read index file: .git/index: not a directory` because this is a git
  worktree — environmental and pre-existing.
- `npm run check` was deliberately not run. Nothing here touches `extensions/`
  or `tests/`.

## Watch item, not a gate

With the floor gone, a future zizmor release that adds a new low- or
medium-severity audit will fail the hook where it previously would not have.
That is the consequence the operator accepted. Triage any such finding on its
merits rather than reinstating the floor.

## Known Stubs

None.

## Self-Check: PASSED

- `zizmor.yml` — FOUND
- `.pre-commit-config.yaml` — FOUND
- `scripts/check-workflow-install-scripts.mjs` — FOUND
- `scripts/check-workflow-install-scripts.negative.mjs` — FOUND
- Commit `729348b4` — FOUND in `git log --all`
- Measured commit count from `c4571c73..HEAD`: **1**
