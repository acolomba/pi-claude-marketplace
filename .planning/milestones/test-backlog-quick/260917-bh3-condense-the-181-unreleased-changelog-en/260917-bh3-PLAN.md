---
quick_id: 260917-bh3
slug: condense-the-181-unreleased-changelog-en
date: 2026-09-17
status: planned
---

# Quick Task 260917-bh3

Condense the ten `[Unreleased]` CHANGELOG.md bullets that #181 added so they follow
`.claude/rules/changelog.md`.

## Problem

Commit 553513a5 (#181) added ten bullets under `[Unreleased]`, one per repaired defect. Several are
internal (a message-substring classifier replaced by typed classification) or share one symptom
class (a failure partway through a command leaving half-applied state). The rule file targets one
user-visible change per bullet, 40 words or fewer, 25 words per sentence.

## Tasks

### Task 1: Replace the ten bullets with five

**Files:** `CHANGELOG.md`

**Action:** Replace the ten consecutive bullets from "Plugins that keep agents in more than one
directory..." through "A discovery failure in one scope no longer discards..." with five:

1. Agents in more than one directory all install (kept, trimmed to two sentences).
2. Fail-clean group: ambiguous marketplace alias, malformed `mcpServers` entry, cleanup after a
   failed update, discovery failure in one scope.
3. Hardening group: path escape via normalization or symlink, hook `if` inherited keys.
4. `PreCompact`/`PostCompact` `trigger` value (kept, reworded to name the values).
5. Notification plugin count and bulk-zero report (kept, split into two sentences).

Drop the "classified by type rather than by matching words" bullet: it describes an internal
refactor with no user-visible symptom.

**Verify:** `git diff --stat CHANGELOG.md` shows one file, 10 deletions, 5 insertions; every new
bullet is 40 words or fewer; no other line of the file changes.

**Done:** CHANGELOG.md committed alone under `docs(changelog): ...`.
