---
phase: 10-constraint-aware-update
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - docs/dependency-resolution.md
  - CHANGELOG.md
  - tests/architecture/dependency-doc-agreement.test.ts
  - tests/orchestrators/plugin/update-flow.test.ts
  - tests/orchestrators/plugin/update-cascade.test.ts
  - tests/orchestrators/marketplace/update.messaging.test.ts
  - scripts/check-unused-type-members.contracts.json
autonomous: true
requirements: [UPDT-01, UPDT-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs/dependency-resolution.md` states what `update` and `autoupdate` now do to a constrained plugin -- the two stages, who constrains, what the held row says, and the current-copy fallback -- and the sentences the phase made untrue are amended rather than left standing."
    - "The prose cannot drift back: an architecture case drives the real composers and asserts the document names the held token, so removing the prose or renaming the token fails a test rather than passing silently."
    - "An unconstrained plugin updates exactly as before on BOTH cascades -- proven by running one fixture through the full cascade composition with the gate in the path and asserting the rendered rows equal the rows the same fixture produced before this phase, not by comparing a value to itself (success criterion 3)."
    - "`update-flow.ts` and `update-preflight.ts` remain the only orchestrator git consumers, and the constraint gate leaf is absent from `NETWORK_FREE_TARGETS` while every gated update owner stays in it (D-10-19, success criterion 3)."
    - "The whole gate is green on the final tree: `npm run check` passes, and `npm run test:coverage:direct` reports no module below the threshold with the exemption pin file still holding an empty row list."
    - "Every `file:line:col` anchor in the type-member contract file resolves to the member it names after four plans of edits to `update-preflight.ts`, `update-swap.ts` and `types.ts`."
    - "UPDT-01 and UPDT-02 are checked off in `.planning/REQUIREMENTS.md`."
  artifacts:
    - path: "docs/dependency-resolution.md"
      provides: "the constraint-aware update section and the amended promotion / update sentences"
      contains: "dependents constrain"
    - path: "tests/architecture/dependency-doc-agreement.test.ts"
      provides: "the update-side half of the prose-versus-code gate"
      contains: "dependents constrain"
    - path: "CHANGELOG.md"
      provides: "the Unreleased entry for constraint-aware update"
      contains: "update"
  key_links:
    - from: "tests/architecture/dependency-doc-agreement.test.ts"
      to: "docs/dependency-resolution.md"
      via: "the case reads the document and asserts it names the token the held-row composer actually stamps"
      pattern: "dependency-resolution.md"
    - from: "tests/orchestrators/plugin/update-cascade.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts"
      via: "the regression case composes a full cascade from unconstrained outcomes with the gate in the preflight path and compares rendered rows"
      pattern: "composeUpdateCascade"
  prohibitions:
    - statement: "The regression proof MUST NOT be a byte-equality that compares a value to itself. The fixture must actually exercise the gate -- an unconstrained target evaluated by the real gate over a real state document -- and the expected bytes must be written down, not captured from the same run."
      status: unresolved
      verification: null
    - statement: "The docs amendment MUST NOT rewrite the update section wholesale; only the sentences this phase made untrue, plus the new constraint prose, are in scope (CONTEXT.md Deferred Ideas)."
      status: unresolved
      verification: null
    - statement: "The docs MUST NOT describe the load-time check's out-of-range arm as something update now does; that check stays where it is and this phase does not duplicate it."
      status: unresolved
      verification: null
    - statement: "No gate may be disarmed, skipped or narrowed to make the final run green; a red gate is reported, not silenced."
      status: unresolved
      verification: null
---

<objective>
Write down what changed, make the writing non-drifting, and prove the parts of
the tree this phase did NOT change really did not change.

`docs/dependency-resolution.md` currently tells a reader that `update` replaces
files and versions and reads no constraint. That is the prose this phase makes
untrue. The same document already carries a fail-closed paragraph that is the
model for the held row's unreadable-declarer arm, so the amendment belongs
beside it rather than in a new document.

The second half is the phase's own regression obligation: an unconstrained
plugin updates exactly as before, and the update family stays inside its network
policy. Both are stated as ROADMAP success criterion 3 and neither is proven by
any single earlier task on its own.

Purpose: close the phase with a green whole-tree gate, prose that a test holds
to the code, and a requirements ledger that matches what shipped.

Output: the amended dependency-resolution prose and its drift gate, the
cross-cascade regression proof, the network-policy proof, the remapped type
contracts, and a green `npm run check`.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-constraint-aware-update/10-CONTEXT.md
@.planning/phases/10-constraint-aware-update/10-01-SUMMARY.md
@.planning/phases/10-constraint-aware-update/10-02-SUMMARY.md
@.planning/phases/10-constraint-aware-update/10-03-SUMMARY.md
@.claude/rules/typescript-style.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_this_phase_produces>
This plan adds no production symbol. It produces prose, one architecture case,
two regression cases, and the ledger updates.

- A new subsection in `docs/dependency-resolution.md` describing constraint-aware
  update, placed after the promotion prose that currently says `update` replaces
  versions and before the load-time-check section.
- Amended sentences: the one stating what `reinstall` and `update` do to a
  dependency record, and the one directing a reader to `update` or `reinstall` to
  move a dependency's version -- both stay true but now carry the constraint
  qualifier.
- A `CHANGELOG.md` `[Unreleased]` entry.
- New architecture case in `tests/architecture/dependency-doc-agreement.test.ts`
  holding the update prose to the token the composer stamps.

**New test case names (verbatim titles the verifier can grep for):**
`UPDT-02: the document names the token the held update row stamps`,
`SC3: an unconstrained plugin renders the same manual cascade rows as before`,
`SC3: an unconstrained plugin renders the same autoupdate cascade rows as before`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Amend the dependency documentation, and hold it to the code with a gate</name>
  <files>
    docs/dependency-resolution.md,
    CHANGELOG.md,
    tests/architecture/dependency-doc-agreement.test.ts
  </files>
  <read_first>
    - docs/dependency-resolution.md (the whole promotion / update region around line 126-128; the fail-closed paragraph around line 146, which is the model for the unreadable-declarer arm's wording; the load-time-check section around line 198-206, which this phase must not duplicate or claim; and the tag-pin sentence stating that a tag-pinned dependency records the version the tag names)
    - tests/architecture/dependency-doc-agreement.test.ts (the whole file -- how the first half drives `composeCascadeFailureMessage` / `composeCascadeMemberRows` and collects the reasons they actually emit, and how the DIVG-01 half reads named document sections)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-cascade.ts (the state after plan 10-03 -- `projectSkippedOutcome` is the composer the new case drives)
    - extensions/pi-claude-marketplace/orchestrators/plugin/update-constraint-gate.ts (the state after plan 10-03 -- `describeConstraint`'s arm clauses, which the prose must describe without quoting byte-for-byte)
    - docs/messaging-style-guide.md (the row-grammar rules the prose must not contradict)
    - CHANGELOG.md (lines 1-40 -- the `[Unreleased]` section's existing entry shape)
    - .planning/phases/10-constraint-aware-update/10-CONTEXT.md (the Deferred Ideas block -- a broader rewrite of the update section is explicitly out of scope)
  </read_first>
  <action>
Write the prose, then make a test hold it.

**The new subsection (`docs/dependency-resolution.md`).** Place it after the
promotion prose and before the load-time-check section. Write it in the
document's existing voice -- second person, no decision identifiers in the body,
plain sentences. Cover, in this order:

  - What the rule is. An update never moves a plugin outside the range the
    installed plugins that declare it hold it to. When those ranges leave room,
    the update takes the highest version inside the room; when they leave none,
    that plugin's update is skipped and its row names who is holding it, and
    every other plugin in the same run updates as before.
  - Who constrains. Every installed plugin in the same scope, enabled or
    disabled, whatever its origin. A disabled plugin keeps its declarations
    exactly as it keeps its files, so a plugin briefly turned off still holds the
    things it needs. Constraints are scope-local: a project-scope update reads
    project-scope declarations, and a bare `update` across both scopes evaluates
    each independently.
  - How a version is chosen. First the plugin's release tags are searched for
    the highest one inside the combined range -- the source repository's tags for
    a git-source plugin, the marketplace repository's own `<name>--v<version>`
    tags for a plugin whose entry is a relative path. When a tag is found the
    update takes it, and the record stores the version that tag names. When no
    tag is found -- including a repository with no release tags at all, and a
    marketplace whose tag list cannot be read -- the update resolves as it
    otherwise would and the version that comes out is then measured against the
    same range. In range it proceeds; out of range it is skipped.
  - What the skipped row says. The brace carries the held token; the `cause:`
    line says which of the situations it is (the declared ranges admit nothing in
    common, no release tag satisfies them, or the version that resolved falls
    outside them) and names the plugins holding it, marking any that are
    currently disabled. On the out-of-range situation only the plugins whose own
    range rejects that version are named.
  - The current-copy fallback. A plugin whose entry is a relative path and whose
    marketplace has no satisfying tag falls back to the marketplace's current
    copy, and that copy is then measured like any other. When it is in range the
    update goes through and the row reads `{dependency current copy}` -- the same
    phrase an install uses for the same fact. When it is out of range the update
    is skipped. Say plainly that this differs from the install side, and why: on
    an install the alternative is having nothing, while on an update the
    alternative is keeping a copy that is already in range.
  - Already at the ceiling. When the plugin is already at the highest version the
    ranges admit, the row still reads `{up-to-date}` and the `cause:` line names
    the range and its holders, so `nothing newer exists` is distinguishable from
    `nothing newer is allowed`.
  - An unreadable declarer. When some other installed plugin's declarations
    cannot be established, this plugin's update is skipped and the cause line
    names that plugin and says why -- the same fail-closed rule the uninstall
    section already describes, differing only in that an update is skipped rather
    than refused. Cross-reference that paragraph rather than restating it.
  - Severity. The skipped row is reported at warning on every surface, including
    a background autoupdate, because the condition persists until a declaration
    changes.

**Amend, do not rewrite.** Two existing sentences need a qualifier: the one
stating that `reinstall` and `update` replace files and versions and leave the
record's origin alone (still true, but add that an update's version choice is now
bounded by what depends on the plugin), and the one directing a reader to
`update` or `reinstall` to move a dependency's version (add that the move is
bounded by the same ranges). Change nothing else in the section. A broader
rewrite is out of scope.

**Do not claim the load-time check.** The section must not say that update now
performs the load-time satisfaction check or that it disables anything. It
selects and it skips; the check that disables a dependent stays where it is.

**The drift gate (`dependency-doc-agreement.test.ts`).** Add a case in the
existing file's style: drive the REAL held-row composer -- build a
`PluginUpdateSkippedOutcome` carrying the held token and a cause, run it through
`projectSkippedOutcome`, and collect the reason token the message actually
carries -- then read `docs/dependency-resolution.md` and assert the document
contains that exact token string. Do not read a token list from anywhere and
compare it to itself; the token must come out of the composer. Title it
`UPDT-02: the document names the token the held update row stamps`.

**CHANGELOG.** Add one `[Unreleased]` entry naming the user-visible change:
`update` and `autoupdate` now keep a plugin inside the version ranges the plugins
that depend on it declare, choosing the highest allowed version and skipping
with a row that names the holders when none is allowed.
  </action>
  <verify>
    <automated>node --test tests/architecture/dependency-doc-agreement.test.ts tests/architecture/partial-vocabulary-guard.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports `# fail` greater than 0, or `# pass 0`</fails_when>
    <automated>pre-commit run --files CHANGELOG.md docs/dependency-resolution.md</automated>
    <fails_when>non-zero exit, or any hook reports `Failed`, or `git status --porcelain` afterwards shows a hook rewrote a file that was not then committed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dependents constrain' docs/dependency-resolution.md` prints 1 or more.
    - `grep -c 'dependency current copy' docs/dependency-resolution.md` prints 2 or more (the install-side mention and the new update-side one).
    - `grep -c 'up-to-date' docs/dependency-resolution.md` prints 1 or more, in the new ceiling paragraph.
    - The new subsection sits between the promotion prose and the load-time-check section: `grep -n 'dependents constrain' docs/dependency-resolution.md` reports a line number greater than the line matching `never promote` and less than the line matching the load-time check's own heading.
    - A `dependency-doc-agreement.test.ts` case titled `UPDT-02: the document names the token the held update row stamps` obtains the token from `projectSkippedOutcome`'s returned message and asserts the document contains it.
    - `grep -cE 'D-10-[0-9]{2}' docs/dependency-resolution.md` prints 0 (the document's body names no decision identifier from this phase; the file carries no such identifier today, so any hit is prose the amendment introduced).
    - `CHANGELOG.md` has one new bullet under `[Unreleased]` naming the constraint-aware update behaviour.
  </acceptance_criteria>
  <done>The dependency documentation states what a constrained update does on both source kinds and in every reported arm, the two sentences this phase made incomplete carry their qualifier, and a test drives the real composer so the prose cannot drift away from the token again.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the untouched paths are untouched, and close the phase on a green whole-tree gate</name>
  <files>
    tests/orchestrators/plugin/update-cascade.test.ts,
    tests/orchestrators/marketplace/update.messaging.test.ts,
    tests/orchestrators/plugin/update-flow.test.ts,
    scripts/check-unused-type-members.contracts.json
  </files>
  <read_first>
    - tests/architecture/no-orchestrator-network.test.ts (the whole file -- what it scans for and how a target's source is read)
    - tests/architecture/gate-targets.ts (lines 38-110 `NETWORK_FREE_TARGETS`, and `NETWORK_FREE_CONTROL_TARGET` -- the membership annotation that cannot drift)
    - tests/orchestrators/plugin/update-cascade.test.ts (the state after plan 10-03 -- how a cascade is composed and how rendered rows are captured)
    - tests/orchestrators/marketplace/update.messaging.test.ts (the state after plan 10-03)
    - tests/orchestrators/plugin/update-flow.test.ts (the state after plan 10-02 -- how a bulk run is driven end to end)
    - scripts/check-unused-type-members.contracts.json (every `update-preflight.ts`, `update-swap.ts` and `types.ts` anchor, after three plans of edits)
    - docs/unused-type-member-gate.md (the audit and remap procedure)
    - scripts/test-coverage-direct.pin.json (confirm the row list is still empty -- no exemption was added to pass a gate)
    - .planning/REQUIREMENTS.md (the UPDT section)
  </read_first>
  <action>
Prove what did not change, then run everything.

**The cross-cascade regression (success criterion 3).** Add two cases, one per
cascade, that each: build a state document with an installed target that NO
record declares, plus a second installed plugin that declares something else
entirely (so the declaration walk really runs and really returns an empty holder
set for the target); drive the target through the full path -- the real gate,
the real preflight, the real cascade composer -- and assert the rendered rows
equal a WRITTEN-DOWN expected value. The expected rows are authored from the
pre-phase behaviour, not captured from the run under test; a comparison of a
value to itself proves nothing and is prohibited here. Title them
`SC3: an unconstrained plugin renders the same manual cascade rows as before`
and its autoupdate sibling. If an existing case already asserts those exact rows
for the same fixture, extend that case rather than adding a parallel one, and
say in a comment that the gate is now in the path.

**The network-policy proof.** Confirm by running `no-orchestrator-network` that
`update-flow.ts` and `update-preflight.ts` remain the only permitted git
consumers in the update family and that every other update owner is still a
gated target. Confirm the constraint gate leaf is absent from
`NETWORK_FREE_TARGETS`. These are assertions the existing gate already makes --
run it, do not weaken it, and do not add the leaf to the list to make something
pass.

**Type-member contracts.** Run `npm run lint:type-members:audit --check` and
remap every anchor that three plans of edits shifted, across `update-preflight.ts`,
`update-swap.ts` and `orchestrators/types.ts`. Remap; do not delete an entry to
silence it.

**Coverage.** Run `npm run test:coverage:direct` over the whole tree (not just
the commit range) and confirm no module is below threshold and that
`scripts/test-coverage-direct.pin.json` still carries an empty row list. If a
module is short, cover it -- an exemption entry is not an acceptable outcome of
this phase.

**The whole gate.** Run `npm run check` on the final tree. Report the exact
counts it prints. If anything is red, fix the cause; do not skip, narrow or
disarm a gate to reach green, and do not repair a byte expectation from actual
output.

**Requirements ledger.** Tick UPDT-01 and UPDT-02 in `.planning/REQUIREMENTS.md`
only after `npm run check` is green.
  </action>
  <verify>
    <automated>node --test tests/orchestrators/plugin/update-cascade.test.ts tests/orchestrators/marketplace/update.messaging.test.ts tests/orchestrators/plugin/update-flow.test.ts</automated>
    <fails_when>non-zero exit, or the TAP summary reports `# fail` greater than 0, or `# pass 0`</fails_when>
    <automated>node --test tests/architecture/no-orchestrator-network.test.ts tests/architecture/marketplace-tag-probe-offline.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts</automated>
    <fails_when>non-zero exit, or the network gate names any update-family module other than `update-flow.ts` and `update-preflight.ts` as a git consumer</fails_when>
    <automated>npm run lint:type-members &amp;&amp; npm run test:coverage:direct</automated>
    <fails_when>non-zero exit, or the coverage report names any module below threshold, or `scripts/test-coverage-direct.pin.json` no longer carries an empty row list</fails_when>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or any sub-command in the chain reports a failure count above zero</fails_when>
  </verify>
  <acceptance_criteria>
    - A case titled `SC3: an unconstrained plugin renders the same manual cascade rows as before` exists and its expected rows are a literal in the test file, not a value derived from the same invocation.
    - A case titled `SC3: an unconstrained plugin renders the same autoupdate cascade rows as before` exists with the same property.
    - Both SC3 fixtures include a second installed plugin whose declarations are non-empty, so the declaration walk executes rather than short-circuiting on an empty state.
    - `grep -c 'update-constraint-gate' tests/architecture/gate-targets.ts` prints 0.
    - `node -e "const p=require('./scripts/test-coverage-direct.pin.json'); process.exit(p.rows.length === 0 ? 0 : 1)"` exits 0.
    - `npm run lint:type-members` exits 0 with the remapped anchors committed, and `git diff --stat scripts/check-unused-type-members.contracts.json` shows modified lines rather than removed entries.
    - `npm run check` exits 0 on the final tree, and the SUMMARY records the unit and integration counts it printed.
    - `.planning/REQUIREMENTS.md` shows UPDT-01 and UPDT-02 ticked.
  </acceptance_criteria>
  <done>An unconstrained plugin's rendered rows are proven unchanged on both cascades against written-down expectations, the update family's git-consumer set is unchanged, the coverage exemption list is still empty, and `npm run check` is green on the final tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator's mental model | Prose is what an operator acts on when a row says a plugin was held; wrong prose sends them to the wrong remedy. |
| test suite -> the claim that nothing else changed | The regression cases are the only evidence that surfaces this phase did not intend to touch are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-16 | Tampering | the phase's own gates | high | mitigate | Three named prohibitions bound the closing run: no gate disarmed, no byte expectation repaired from actual output, no coverage exemption added. The pin file's empty row list is asserted programmatically, and the contract-file diff is required to show modifications rather than removals. |
| T-10-17 | Repudiation | the regression proof | high | mitigate | A vacuous self-comparison would let a real change in the unconstrained path pass unnoticed. The expected rows are required to be literals in the test file, and each fixture is required to contain a second declaring plugin so the walk genuinely runs. |
| T-10-18 | Information disclosure | documentation | low | mitigate | The prose names tokens and phrases the composer stamps and no absolute path, credential or internal identifier; an acceptance criterion pins that no decision identifier reaches the document body. |
| T-10-19 | Tampering | network policy | high | mitigate | The gate leaf's absence from the network-free target list is asserted by grep, and `no-orchestrator-network` is run rather than trusted; adding the leaf to the list to pass something is named as a prohibited remedy. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No runtime or dev dependency is added in this phase; the Package Legitimacy Gate is recorded as not applicable in RESEARCH.md, and no task runs a package-manager install. |
</threat_model>

<verification>
- `node --test tests/architecture/dependency-doc-agreement.test.ts tests/architecture/partial-vocabulary-guard.test.ts`
- `pre-commit run --files CHANGELOG.md docs/dependency-resolution.md`
- `node --test tests/orchestrators/plugin/update-cascade.test.ts tests/orchestrators/marketplace/update.messaging.test.ts tests/orchestrators/plugin/update-flow.test.ts`
- `node --test tests/architecture/no-orchestrator-network.test.ts tests/architecture/marketplace-tag-probe-offline.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts`
- `npm run lint:type-members && npm run test:coverage:direct`
- `npm run check`
</verification>

<success_criteria>
- The dependency documentation describes constraint-aware update on both source kinds and in every reported arm, and a test holds it to the composer's token.
- Success criterion 3 is proven non-vacuously on both cascades against written-down expectations.
- The update family's permitted git-consumer set is unchanged and the gate leaf is not in the network-free list.
- The coverage exemption list is still empty and every type-member anchor resolves.
- `npm run check` is green on the final tree, and UPDT-01 / UPDT-02 are ticked.
</success_criteria>

<flagged_assumptions>
- The pre-commit environment is assumed available for the docs lint. The repository has no installed commit hook, so the run is manual and it WRITES: `git status --porcelain` must be checked after it and any rewrite committed as a follow-up rather than amended.
- `docs/dependency-resolution.md` is assumed not to be covered by the `format:check` prettier glob (which names js, json and ts). If the pre-commit run rewrites it, that rewrite is the formatter of record and is committed; do not run a formatter against it by hand.
</flagged_assumptions>

<output>
Create `.planning/phases/10-constraint-aware-update/10-04-SUMMARY.md` when done
</output>
