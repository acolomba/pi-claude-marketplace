---
phase: 11-cross-marketplace-dependency-allowlist
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
  - extensions/pi-claude-marketplace/shared/notification-types.ts
  - extensions/pi-claude-marketplace/shared/notification-grammar.ts
  - tests/orchestrators/marketplace/info.test.ts
  - tests/shared/notification-grammar.test.ts
  - tests/architecture/catalog-uat/fixtures/marketplace-info.ts
  - tests/architecture/catalog-uat/catalog-contract.test.ts
  - docs/output-catalog.md
  - scripts/check-unused-type-members.contracts.json
autonomous: true
requirements: [XMKT-01]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "marketplace info displays allowed_marketplaces only for a nonempty parsed array, in source order, independently for each scope."
    - "Absent and empty lists omit the line; duplicates and empty string entries remain visible in a nonempty list."
    - "The info representation escapes controls without changing the parsed policy values."
    - "Malformed present policies use the existing typed info failure path; info remains read-only and offline."
  artifacts:
    - path: extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
      provides: "Projection from the cached MarketplaceManifest into the typed info message."
    - path: extensions/pi-claude-marketplace/shared/notification-types.ts
      provides: "Optional allowedMarketplaces input to the info renderer."
    - path: extensions/pi-claude-marketplace/shared/notification-grammar.ts
      provides: "Omission-aware, single-line allowed_marketplaces rendering."
    - path: tests/architecture/catalog-uat/fixtures/marketplace-info.ts
      provides: "Exact public nonempty-policy output example."
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts
      to: extensions/pi-claude-marketplace/domain/manifest.ts
      via: "buildBlock calls loadMarketplaceManifest."
      pattern: loadMarketplaceManifest
    - from: extensions/pi-claude-marketplace/shared/notification-grammar.ts
      to: extensions/pi-claude-marketplace/shared/notification-types.ts
      via: "renderMarketplaceInfo consumes allowedMarketplaces."
      pattern: allowedMarketplaces
---

<objective>
Expose the validated marketplace allowlist through info per D-11-04, preserving D-11-05's exact parsed values.

Purpose: Users can inspect the policy the install path will enforce.
Output: Typed info field, escaped info line, paired tests and catalog example.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-CONTEXT.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-RESEARCH.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-PATTERNS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-VALIDATION.md
@.planning/HANDOFF-upstream-dependency-parity.md
@.planning/phases/03-dependency-resolution/03-CONTEXT.md
@docs/dependency-resolution.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@skills/typescript-comments/SKILL.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-01-SUMMARY.md
</context>

## Execution discipline

Read each edited file before changing it and use CodeGraph to trace the named owners and callers. Source and tests follow the four project TypeScript skills. Use real temporary files, public exports, independent expected values, and the existing notification boundary; no new package is needed. All commands below run from the repository root. Preserve the five operator-owned local changes listed in RESEARCH.md. Keep contract-anchor edits limited to coordinates for unchanged owner/member identities.

## Artifacts this phase produces

- MarketplaceInfoMessage.allowedMarketplaces?: readonly string[].
- Module-private renderAllowedMarketplaces in notification-grammar.ts, if extracted to keep rendering within the existing complexity cap.
- Catalog state marketplace-info-allowed-marketplaces in the existing info fixture and output catalog.
- No new production or test files.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Show the validated policy on marketplace info</name>
  <reversibility rating="costly">The allowed_marketplaces label and omission behavior are published bytes fixed by D-11-04; reversal needs coordinated output and catalog changes.</reversibility>
  <files>extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts, extensions/pi-claude-marketplace/shared/notification-types.ts, extensions/pi-claude-marketplace/shared/notification-grammar.ts, tests/orchestrators/marketplace/info.test.ts, tests/shared/notification-grammar.test.ts</files>
  <read_first>
    Read all five edited files, domain/manifest.ts, tests/architecture/catalog-uat/fixtures/marketplace-info.ts, and the canonical context/output references. Trace buildBlock and renderMarketplaceInfo callers with CodeGraph.
  </read_first>
  <behavior>
    - Absent and empty lists produce no allowed_marketplaces line.
    - ["tools","team","tools"] prints exactly allowed_marketplaces: ["tools","team","tools"] after the optional description line.
    - Both-scope info prints each scope's own list, with no state, configuration, or network mutation.
    - Controls, quotes, backslashes, line separators, and bidi controls remain visible as escaped data on one physical line.
  </behavior>
  <action>
    Per D-11-04, project the loader's typed allowCrossMarketplaceDependenciesOn array into MarketplaceInfoMessage.allowedMarketplaces. Extend the renderer with one line after description, or after the current last detail when description is absent, only when length is positive. Choose compact JSON-array notation in source order; duplicate and empty string entries remain visible.
    Encode JSON controls through JSON string encoding and explicitly escape remaining C1 controls U+007F-U+009F, U+2028/U+2029, and bidi controls U+061C, U+200E/U+200F, U+202A-U+202E, U+2066-U+2069 as lowercase four-digit Unicode escapes. Preserve ordinary printable Unicode. Keep escaping local to display; matching must consume the unmodified parsed strings. A small private renderAllowedMarketplaces helper may own this concern. Use the existing central notify path and assert exact whole messages for single- and both-scope reads.
  </action>
  <acceptance_criteria>
    - The literal label is allowed_marketplaces: and omitted for absence and [].
    - One scope cannot borrow another scope's displayed policy.
    - Hostile strings create one labeled line with escaped values, and the manifest array is unchanged after rendering.
    - A malformed present field follows the existing info failure path rather than displaying an empty policy.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/marketplace/info.test.ts tests/shared/notification-grammar.test.ts</automated>
    <fails_when>Non-zero exit, a nonzero fail count, or a zero-test summary.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/marketplace/info.ts extensions/pi-claude-marketplace/shared/notification-grammar.ts</automated>
    <fails_when>Non-zero exit or either requested pair is absent from the coverage report.</fails_when>
  </verify>
  <done>Info displays precisely the nonempty parsed list through its typed renderer and stays offline and read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the public info example and retain contract gates</name>
  <files>tests/architecture/catalog-uat/fixtures/marketplace-info.ts, tests/architecture/catalog-uat/catalog-contract.test.ts, docs/output-catalog.md, scripts/check-unused-type-members.contracts.json</files>
  <read_first>
    Read all four files, tests/architecture/catalog-uat/catalog-parser.test.ts, and tests/shared/notification-types.test.ts. Use the info fixture and catalog-state pattern mapped in 11-PATTERNS.md.
  </read_first>
  <action>
    Add marketplace-info-allowed-marketplaces with ["tools","team"] to the typed fixture and a matching independently reviewed rendered block in the catalog. Keep existing absent-policy examples byte-identical and explain the empty-array omission beside the new state per D-11-04. The measured starting catalog count is 227; this plan adds one state, so set the total to 228 while leaving module/section counts unchanged. If a gate coordinate moves after the type addition, re-derive its exact owner/member location with the existing type-member tool and update only the relevant contract coordinates.
  </action>
  <acceptance_criteria>
    - Catalog parser and runtime fixture driver recognize exactly one new state and total 228 states.
    - The new example contains exactly one allowed_marketplaces line.
    - The type-member gate has no new suppression or exception and every adjusted anchor resolves.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts</automated>
    <fails_when>Non-zero exit, fixture/catalog byte mismatch, or a zero-test summary.</fails_when>
    <automated>npm run lint:type-members &amp;&amp; npm run lint:type-members:negative</automated>
    <fails_when>Non-zero exit or a missing negative-control completion report.</fails_when>
  </verify>
  <done>The info line has a catalog contract and the shared type contract remains checked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Cached manifest strings to terminal notification | String contents remain data on one physical line. |
| Scope-selected manifest to info block | Each block must use its own marketplace record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-11-02 | Spoofing | shared/notification-grammar.ts | medium | mitigate | Task 1 encodes control and bidi characters and compares exact single-line output. |
| T-11-03 | Information Disclosure | marketplace/info.ts | low | accept | Policy names are already public marketplace metadata and requested explicitly by info. |

ASVS L1; unresolved high or critical threats block completion. No package install or external setup.
</threat_model>

<verification>
Run the focused task and direct coverage gates, then npm run check after this wave. Preserve and report operator-owned formatting failures and run later scripts independently when needed.
</verification>

<success_criteria>
D-11-04 uses the one cached typed value from 11-01, has safe exact output for both scopes, and is pinned at 228 catalog states.
</success_criteria>

<output>
Create .planning/phases/11-cross-marketplace-dependency-allowlist/11-02-SUMMARY.md with test evidence and exact limitations.
</output>
