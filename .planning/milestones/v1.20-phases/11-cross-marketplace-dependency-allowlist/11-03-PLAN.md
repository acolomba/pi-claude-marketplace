---
phase: 11-cross-marketplace-dependency-allowlist
plan: "03"
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - extensions/pi-claude-marketplace/shared/notification-types.ts
  - extensions/pi-claude-marketplace/shared/notify-reasons.ts
  - tests/architecture/catalog-uat/fixtures/plugin-install.ts
  - tests/architecture/catalog-uat/catalog-contract.test.ts
  - docs/output-catalog.md
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/architecture/compat-01-no-expansion.test.ts
  - tests/shared/notification-types.test.ts
  - scripts/check-unused-type-members.contracts.json
autonomous: true
requirements: [XMKT-01]
estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A typed failed dependency row can report the closed-set cross-marketplace reason with error severity and both remedies."
    - "The refusal names the blocked dependency, its declarer, the target marketplace, and the marketplace whose policy must change."
    - "The closed reason vocabulary remains append-only; cross-marketplace is the 63rd reason."
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/notification-types.ts
      provides: "cross-marketplace as a Reason and ContentReason."
      contains: cross-marketplace
    - path: extensions/pi-claude-marketplace/shared/notify-reasons.ts
      provides: "Complete classification of the new dependency reason."
    - path: tests/architecture/catalog-uat/fixtures/plugin-install.ts
      provides: "Exact failed dependency/root-row notification contract."
    - path: docs/output-catalog.md
      provides: "Human-readable contract for the same rendered refusal."
  key_links:
    - from: tests/architecture/catalog-uat/fixtures/plugin-install.ts
      to: docs/output-catalog.md
      via: "catalog-contract drives the fixture through real notify and compares complete rendered bytes."
      pattern: dependency-cross-marketplace
  prohibitions:
    - statement: "MUST NOT describe an added marketplace as permission to install its plugins automatically; the refusal must explain the actual trust decision."
      status: unresolved
      reason: "Flagged-unverified spec-less recall: preserve informed user control over automatic cross-marketplace installation."
    - statement: "MUST NOT present editing a marketplace policy as the only remedy or hide the option to install the named dependency manually first."
      status: unresolved
      reason: "Flagged-unverified spec-less recall: preserve a narrow manual remedy alongside changing marketplace policy."
---

<objective>
Establish the cross-marketplace refusal's complete typed notification contract per D-11-06, including both remedies and every vocabulary gate.

Purpose: The install and reload producers can carry one truthful reason and cause through existing notification machinery.
Output: Closed-set reason, rendered catalog tracer, and synchronized membership/order/count proofs.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-CONTEXT.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-RESEARCH.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-PATTERNS.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-VALIDATION.md
@.planning/HANDOFF-upstream-dependency-parity.md
@.planning/phases/03-dependency-resolution/03-CONTEXT.md
@docs/dependency-resolution.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@skills/typescript-comments/SKILL.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
@.planning/phases/11-cross-marketplace-dependency-allowlist/11-02-SUMMARY.md
</context>

## Execution discipline

This plan is one closed-set contract amendment. Its nine owned files include every required enum, classification, membership, order, catalog, and anchor change so the wave boundary remains type-correct. Complete both tasks before committing the amendment; intermediate enum and membership changes are intentionally not independent commits. Keep every existing token's relative order. Read all edited files, trace consumers with CodeGraph, and follow the project source/test skills. Shared type/catalog ownership forces this plan after 11-02. Preserve operator-owned local files.

## Cause contract

For the fixture helper@official requiring formatter@tools, use this complete cause:

Dependency "formatter@tools", declared by "helper@official", is from marketplace "tools", which root marketplace "official" does not allow. Install "formatter@tools" manually first, or add "tools" to allowCrossMarketplaceDependenciesOn in the marketplace.json for root marketplace "official".

The implementation in 11-04 parameterizes dependency key, declarer key, target marketplace, and policy-root marketplace. On reload's original-edge refusal, the policy-root marketplace is the first eligible declarer's marketplace; on a failure inside B's cascade, it is B's marketplace. D-11-06's facts and remedies stay identical.

## Artifacts this phase produces

- Appended REASONS member "cross-marketplace", inherited Reason/ContentReason member and its CommandPrivateReason classification.
- Catalog state dependency-cross-marketplace in the existing plugin-install fixture and output catalog.
- No new production function, CLI flag, module, or test file.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render the new refusal through the real notify dispatcher</name>
  <reversibility rating="costly">The published reason and refusal facts are fixed by D-11-06; reversal needs coordinated reason, output, and catalog changes.</reversibility>
  <files>extensions/pi-claude-marketplace/shared/notification-types.ts, extensions/pi-claude-marketplace/shared/notify-reasons.ts, tests/architecture/catalog-uat/fixtures/plugin-install.ts, tests/architecture/catalog-uat/catalog-contract.test.ts, docs/output-catalog.md</files>
  <read_first>
    Read all five edited files, tests/architecture/catalog-uat/catalog-parser.test.ts, install-cascade.messaging.ts, docs/messaging-style-guide.md, the phase context, and the skills in context.
  </read_first>
  <behavior>
    - A dependency-cross-marketplace fixture renders a formatter@tools failed row with cross-marketplace and the full cause above, plus helper's dependency failed row.
    - The real notify dispatcher emits error severity, with no reload hint for a pre-materialization refusal.
    - All previous catalog states retain their bytes and order.
  </behavior>
  <action>
    Per D-11-06, append cross-marketplace to the REASONS tail after dependents constrain. Classify it with the other command-private dependency-cascade reasons in notify-reasons.ts so the completeness proof includes it. It is ContentReason, not a structural marketplace-absence token.
    Add dependency-cross-marketplace immediately beside the existing dependency-marketplace-not-added catalog state and fixture, using the exact cause contract above. Keep Pi's existing failed-row grammar, root dependency failed row, summary, severity, sorting, and needsReload:false semantics. Drive the fixture through the current catalog test's real notify path; compare the complete output, with expected bytes independently written.
    Update the catalog's reason count to 63 and the catalog-state count from 228 after 11-02 to 229. Revise the old prose calling marketplace absence the only trust rule: availability and root permission are separate facts. Do not copy upstream's complete output format.
  </action>
  <acceptance_criteria>
    - cross-marketplace occurs once at the REASONS tail and has one classification in the existing proof.
    - The real notification fixture passes byte equality with both remedies and all four names.
    - The catalog recognizes 229 states; prior examples are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts</automated>
    <fails_when>Non-zero exit, catalog count/byte mismatch, or a zero-test summary.</fails_when>
    <automated>npx prettier --check docs/output-catalog.md</automated>
    <fails_when>Non-zero exit or a formatting warning naming docs/output-catalog.md.</fails_when>
  </verify>
  <done>The existing notification boundary renders the new policy refusal with the agreed meaning and grammar.</done>
</task>

<task type="auto">
  <name>Task 2: Lock reason membership, order, and type consumers</name>
  <files>tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/compat-01-no-expansion.test.ts, tests/shared/notification-types.test.ts, scripts/check-unused-type-members.contracts.json</files>
  <read_first>
    Read all four edited files, shared/notification-types.ts, shared/notify-reasons.ts, scripts/check-unused-type-members.mjs, and the phase output contract.
  </read_first>
  <action>
    Add cross-marketplace to every applicable reason enrollment, ordered expected-reason tuple, and compile-time membership case. Change the exact count lock from 62 to 63. Preserve old ordering and keep the unsupported/structural exclusions intact. Extend existing type-only cases only where they assert real permitted or forbidden shapes; do not add runtime member-enumeration tests duplicating TypeScript.
    Re-derive any contract coordinates shifted by the tuple addition from the type-member tool's actual owner/member identity. Keep all exemptions and coverage policies unchanged. Run the known Phase 10 vocabulary/catalog command verbatim below and close both tasks as one contract change. The additional paired notify-reasons assertions are assigned to 11-05 alongside the production refusal composer; existing classification completeness already incorporates the new member here.
  </action>
  <acceptance_criteria>
    - Both independent vocabulary locks accept exactly the prior 62 members plus the new final member.
    - ContentReason accepts cross-marketplace and existing structural exclusions still fail their compile-time cases.
    - Typecheck and the type-member positive and negative gates pass without new exceptions.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts tests/architecture/compat-01-no-expansion.test.ts tests/shared/notification-types.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/no-orchestrator-network.test.ts</automated>
    <fails_when>Non-zero exit, a nonzero fail count, or a zero-test summary.</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run lint:type-members &amp;&amp; npm run lint:type-members:negative</automated>
    <fails_when>Non-zero exit or an invalid/unresolved contract anchor in output.</fails_when>
  </verify>
  <done>The notification contract has no missing classification, stale count, changed old ordering, or stale type anchor.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Structured install failure to notification | The policy root and remedy must identify the actual authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-11-04 | Spoofing | Refusal cause and root row | medium | mitigate | Task 1 pins dependency, declarer, target, policy root, manual remedy, and field-edit remedy in full rendered bytes. |
| T-11-05 | Repudiation | Reason classification | low | mitigate | Task 2 keeps closed-set enrollment, append-only ordering, and compile-time completeness checked. |

ASVS L1; high/critical findings block completion. No new package or transport.
</threat_model>

## Spec-less prohibition audit

No SPEC supplies prohibitions. The two items in must_haves.prohibitions are the exact descriptor-less projectProhibitions projection and remain flagged-unverified; they have no check_kind, check_target, or fabricated wired check. They are review intent, not new pending product decisions. The complete recall/drop ledger is in 11-07-PLAN.md.

<verification>
Run the task gates and npm run check after the wave. Preserve and explicitly report the known operator configuration formatting issue if it remains the only failing check; execute later scripts independently in that case.
</verification>

<success_criteria>
D-11-06 has one closed-set notification contract ready for the install producers, without changing existing reasons or row grammar.
</success_criteria>

<output>
Create .planning/phases/11-cross-marketplace-dependency-allowlist/11-03-SUMMARY.md.
</output>
