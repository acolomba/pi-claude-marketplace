---
phase: 12-standalone-prune-with-dry-run
plan: "04"
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - extensions/pi-claude-marketplace/persistence/state-io.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
  - extensions/pi-claude-marketplace/edge/flag-catalog.ts
  - extensions/pi-claude-marketplace/shared/notification-types.ts
  - extensions/pi-claude-marketplace/shared/notification-grammar.ts
  - tests/persistence/state-io.test.ts
  - tests/integration/standalone-prune.test.ts
autonomous: true
requirements: [PRUNE-07, FLAG-02]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "For an unchanged snapshot and successful removals, prune --dry-run lists the same selected members in the same fixpoint order as actual prune, using (will uninstall) {dependency pruned} at info severity (D-12-01, PRUNE-07); an actual member failure can retain later held candidates."
    - "Preview reads exactly one scope without a write lock, directory creation, state migration persistence, config write, artifact removal, or reload hint (PRUNE-07)."
    - "A fresh actual prune recomputes its selector inside its lock after any earlier preview; preview is a snapshot description, not a reservation."
    - "The only added per-verb flag is --dry-run, and no confirmation prompt or -y path is introduced (FLAG-02, D-02-05)."
  prohibitions: []
  artifacts:
    - path: extensions/pi-claude-marketplace/persistence/state-io.ts
      provides: "Explicit nonpersisting loadState mode while retaining the existing default."
      contains: persistMigration
    - path: extensions/pi-claude-marketplace/shared/notification-types.ts
      provides: "Pending prune reason while preserving plain pending rows."
      contains: PluginWillUninstallMessage
    - path: tests/integration/standalone-prune.test.ts
      provides: "Snapshot parity and byte/tree/lock no-write proof."
  key_links:
    - from: extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
      via: "Consumed --dry-run flag forwarded to the one prune operation."
      pattern: dryRun
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
      to: extensions/pi-claude-marketplace/persistence/state-io.ts
      via: "Preview calls loadState with migration persistence disabled before reading declarations."
      pattern: persistMigration
---

<objective>
Add a genuinely read-only prune preview to the working standalone command.

Purpose: Users can inspect the current orphan sweep safely, including on an old or missing state document.
Output: Preview selector path, nonpersisting state load, pending reason row, and disk-preservation regression tests. Plan 05 adds the normal empty result.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-PATTERNS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-VALIDATION.md
@.planning/phases/12-standalone-prune-with-dry-run/12-02-SUMMARY.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-comments/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Execution discipline

Read every edited file and trace its callers with CodeGraph before changes. Begin each behavior change with a failing case. Keep preview completely outside withLockedStateTransaction and every other mutating helper. The command integration test exercises the preview path here; Plans 06 and 07 pin the grammar and edge source/test pairs directly. Commands run at repository root.

## Artifacts this phase produces

- New option: PrunePluginOptions.dryRun and catalog-owned --dry-run, accepted only by the prune handler.
- PluginWillUninstallMessage gains an optional reason set without changing plain pending rows. Plan 05 adds PruneEmptyMessage.
- New state-load option: persistMigration false suppresses the existing background migration save only for read-only callers.
- Existing new files from Plan 01 gain preview behavior and snapshot parity tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preview the real orphan selector without taking a write lock</name>
  <files>extensions/pi-claude-marketplace/persistence/state-io.ts, extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/prune.ts, extensions/pi-claude-marketplace/edge/flag-catalog.ts, extensions/pi-claude-marketplace/shared/notification-types.ts, extensions/pi-claude-marketplace/shared/notification-grammar.ts, tests/integration/standalone-prune.test.ts, tests/persistence/state-io.test.ts</files>
  <read_first>Read all edited files, tests/orchestrators/plugin/prune.test.ts, transaction/with-state-guard.ts, domain/dependency-orphans.ts, shared/notification-dispatch.ts, 12-CONTEXT.md D-12-01 and FLAG-02, and RESEARCH.md Architecture Patterns 1-4. Trace loadState and prune operation callers with CodeGraph.</read_first>
  <behavior>
    - Given an orphan A that declares B, preview displays A then B in pending removal tense; actual prune from the same unchanged snapshot displays A then B in completed removal tense.
    - Running preview on a current-state scope leaves state bytes, mtime, extension-root tree, config, artifacts, and lock path identical.
  </behavior>
  <action>Per PRUNE-07 and D-12-01, add PrunePluginOptions.dryRun. Give loadState an explicit options argument with persistMigration false; the default must preserve every existing caller's background migration behavior. In the preview branch, resolve the selected scope, call the nonpersisting load directly, build the same full-scope offline declaration index as actual prune, and call pruneOrphans with an empty initially-gone set. Map ordered keys to the indexed records and emit versionless PluginWillUninstallMessage rows with reasons containing dependency pruned, severity info, and needsReload false. Preserve global selector order with contiguous singleton marketplace blocks, including alternating marketplace names. Extend the pending type and renderer to append that one brace while retaining bare pending rows byte-for-byte. Add --dry-run as the prune catalog entry with parse and complete true, export its name for handler mapping, consume it in the handler, and pass dryRun true only when present. Reject other flags and operands before the operation. The preview branch must never call the transaction, cascade, cleanup, atomic write, or network path. Start with a failing end-to-end no-write case for current state that records state bytes, mtime, scope tree, and lock-file absence before calling the real command.</action>
  <acceptance_criteria>
    - Same-snapshot preview and actual select identical ordered names, while their statuses differ exactly as D-12-01 specifies.
    - Current-state preview leaves every disk observation unchanged, including no lock creation.
    - Existing loadState callers still persist migration by default; plain pending rows still omit reason braces.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/integration/standalone-prune.test.ts tests/persistence/state-io.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, mismatched preview order, or any bytes, mtime, tree, or lock change on preview.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/state-io.ts</automated>
    <fails_when>Non-zero exit or the state I/O source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>A user can inspect the real orphan selection through --dry-run without a filesystem write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove legacy and missing-state previews never write</name>
  <files>extensions/pi-claude-marketplace/persistence/state-io.ts, extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts, tests/persistence/state-io.test.ts, tests/integration/standalone-prune.test.ts</files>
  <read_first>Read all edited files, transaction/with-state-guard.ts, persistence/locations.ts, 12-CONTEXT.md D-12-01, and the migration-persistence discussion in 12-RESEARCH.md. Trace loadState's migration-save callers with CodeGraph.</read_first>
  <behavior>
    - A legacy state document is normalized for preview in memory while its bytes, mtime, config, artifacts, extension-root tree, and lock path remain identical.
    - Preview of a missing state or extension root does not create the root, lock, state file, or config.
    - Existing loadState callers still persist migration by default; only explicit preview reads suppress it.
  </behavior>
  <action>Per PRUNE-07, expand the state I/O and real-command integration pairs around the nonpersisting load introduced in Task 1. Use a legacy document that would cause the default load path to save; compare bytes, mtime, complete extension-root tree, config, artifacts, and lock path before and after repeated previews. Also start with an absent extension root and prove it stays absent after preview. Keep the preview entirely outside withLockedStateTransaction and any cleanup or atomic-write helper. Test the unchanged default migration-persistence behavior for another loadState caller, so the new option does not silently alter existing operations. Plan 05 adds D-12-02's normal empty notification to the now read-only path.</action>
  <acceptance_criteria>
    - Legacy and missing-state preview calls leave every disk observation unchanged.
    - The default loadState call still persists migrations as it did before this phase.
    - Repeated previews are byte-identical and carry no reload hint.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/integration/standalone-prune.test.ts tests/persistence/state-io.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, or any bytes, mtime, tree, config, or lock path changes during preview.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/state-io.ts</automated>
    <fails_when>Non-zero exit or the state I/O source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>The preview remains read-only on current, legacy, and absent scope trees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Preview command to state persistence | Even a read may trigger a legacy migration write unless explicitly suppressed. |
| Snapshot to user-visible row | A pending action must not be presented as a completed removal. |
| Concurrent state to actual deletion | Actual prune must select again inside its lock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-04 | Tampering | state-io.ts legacy load | high | mitigate | Task 2 verifies bytes, mtime, tree, config, and lock on legacy and missing state. |
| T-12-05 | Tampering | prune.ts preview/actual split | high | mitigate | Task 1 selects from the same pure fixpoint; actual always rebuilds inside its own lock, and Plan 08 tests an intervening change. |
| T-12-06 | Repudiation | pending prune row | medium | mitigate | Task 1 uses pending tense, info severity, and no reload hint; Plan 06 pins exact row bytes. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run all focused commands and direct coverage above. Run node --test tests/architecture/no-orchestrator-network.test.ts and npm run typecheck after the wave. The preview must remain offline and read-only even on legacy state.
</verification>

<success_criteria>
PRUNE-07 has a command-level preview that lists the same current-snapshot selection as successful actual prune and changes no disk object, including on legacy and missing state. Failed actual members remain installed and can reduce later removals. Plan 05 adds D-12-02's normal scoped empty result; Plan 07 pins FLAG-02's exact flag set.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-04-SUMMARY.md when done.
</output>
