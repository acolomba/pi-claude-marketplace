---
phase: 12-standalone-prune-with-dry-run
plan: "06"
type: execute
wave: 5
depends_on: [12-05]
files_modified:
  - docs/output-catalog.md
  - docs/messaging-style-guide.md
  - tests/architecture/catalog-uat/fixtures/plugin-prune.ts
  - tests/architecture/catalog-uat/catalog-contract.test.ts
  - tests/architecture/catalog-uat/catalog-parser.test.ts
  - tests/architecture/notify-closed-set-locks.test.ts
  - tests/shared/notification-grammar.test.ts
autonomous: true
requirements: [PRUNE-06, PRUNE-07]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The published output catalog shows exact actual, pending, empty, and failure states for the standalone prune command (D-12-01, D-12-02)."
    - "The catalog and independently authored fixtures agree on every new byte, section order, and state count."
    - "Existing plain pending uninstall and uninstall --prune catalog states retain their prior bytes and meaning."
  prohibitions: []
  artifacts:
    - path: docs/output-catalog.md
      provides: "New /claude:plugin prune command section and amended pending-reason grammar."
      contains: "/claude:plugin prune"
    - path: tests/architecture/catalog-uat/fixtures/plugin-prune.ts
      provides: "Independent catalog fixtures for standalone prune states."
      exports: [PLUGIN_PRUNE_FIXTURES]
  key_links:
    - from: tests/architecture/catalog-uat/catalog-contract.test.ts
      to: tests/architecture/catalog-uat/fixtures/plugin-prune.ts
      via: "Fixture map executes the real shared notification dispatcher against literal catalog bytes."
      pattern: PLUGIN_PRUNE_FIXTURES
---

<objective>
Lock the new prune output as a published, byte-exact command contract.

Purpose: Pending and completed removals, ordinary emptiness, and failures must remain honest as the notification model evolves.
Output: New catalog section, fixture module, count pins, and messaging guide amendment.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-05-SUMMARY.md
@docs/output-catalog.md
@docs/messaging-style-guide.md
@tests/architecture/catalog-uat/catalog-contract.test.ts
@tests/architecture/catalog-uat/catalog-parser.test.ts
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Artifacts this phase produces

- New fixture file tests/architecture/catalog-uat/fixtures/plugin-prune.ts and exported PLUGIN_PRUNE_FIXTURES.
- New output-catalog section for /claude:plugin prune. No new notification reason token is created; dependency pruned is reused.
- Deliberate catalog pin changes: fixture modules and sections increase from 20 to 21; seven new command states increase the 230-state baseline to 237.

<tasks>

<task type="auto">
  <name>Task 1: Publish seven independently pinned prune output states</name>
  <files>docs/output-catalog.md, tests/architecture/catalog-uat/fixtures/plugin-prune.ts, tests/architecture/catalog-uat/catalog-contract.test.ts, tests/architecture/catalog-uat/catalog-parser.test.ts</files>
  <read_first>Read every edited file before changing it, plus plugin-uninstall.ts and plugin-pending.ts catalog fixtures, shared/notification-types.ts, shared/notification-grammar.ts, shared/notification-dispatch.ts, 12-CONTEXT.md D-12-01/02, and the output-catalog conventions. Trace catalog fixture imports and parser callers with CodeGraph.</read_first>
  <action>Insert a /claude:plugin prune section in command order and author seven catalog states: one actual orphan; an actual interleaved-marketplace fixpoint chain; one pending orphan; a pending interleaved chain; the shared scoped Nothing to prune outcome; a member failure alongside an independent success; and unreadable-declarer refusal. State that actual rows use (uninstalled) {dependency pruned} with the existing reload hint, while preview rows use (will uninstall) {dependency pruned} with no version or reload hint, per D-12-01. State that no-orphans is information rather than failure per D-12-02. Make PLUGIN_PRUNE_FIXTURES produce the corresponding typed messages with literal expected bytes independent of the renderer. Add the fixture to catalog-contract.test.ts and update module/section/state counts from the measured 20/20/230 baseline to 21/21/237; update catalog-parser.test.ts to 237 states and 21 sections. If implementation produces a distinct extra state, add a named fixture and adjust counts using the parser's measured output, never hide it. Preserve every existing catalog state and its exact bytes, especially uninstall --prune and plain pending uninstall.</action>
  <acceptance_criteria>
    - Seven new catalog-state markers and seven fixture states correspond one-to-one.
    - Both catalog tests compare full UTF-8 outputs; the existing 230 states remain unchanged.
    - The new prose explains when preview and actual may differ after concurrent change or member failure, without claiming a reservation.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a state-count mismatch, or a byte mismatch in any old or new catalog tuple.</fails_when>
  </verify>
  <done>The output catalog has a complete new prune section with 237 independently pinned states across 21 sections.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify pending grammar and retain closed-set locks</name>
  <files>docs/messaging-style-guide.md, tests/architecture/notify-closed-set-locks.test.ts, tests/shared/notification-grammar.test.ts</files>
  <read_first>Read both edited files, docs/output-catalog.md's new prune section, shared/notification-types.ts and its paired test, shared/notification-grammar.ts and its paired test, and 12-CONTEXT.md D-12-01/02. Trace Reason and NotificationMessage consumers with CodeGraph.</read_first>
  <action>Amend the guide's pending-row rule to admit the existing dependency pruned reason only when the prune preview supplies it, while ordinary pending uninstall stays bare and versionless. Document the normal scoped empty message and the fact that no reload trailer belongs on a preview. Amend the closed-set architecture test deliberately for the new PruneEmptyMessage kind and the optional pending reason member; retain the 63-member Reason vocabulary and all prior status literals without weakening exact-set assertions. Extend the direct grammar pair with byte-exact pending prune, bare pending uninstall, and no-reload cases, then run it and the dispatch pair alongside the architecture lock so the written rule matches the actual renderer.</action>
  <acceptance_criteria>
    - Messaging guidance distinguishes pending prune from completed prune and from existing pending reconciliation.
    - Closed-set tests still reject an unplanned reason or status expansion.
    - No new reason token or list/info orphan marker is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/architecture/notify-closed-set-locks.test.ts tests/shared/notification-grammar.test.ts tests/shared/notification-dispatch.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, drift in the existing closed vocabularies, or wrong pending-row bytes.</fails_when>
    <automated>npx prettier --check docs/output-catalog.md docs/messaging-style-guide.md</automated>
    <fails_when>Non-zero exit or either changed document fails formatting.</fails_when>
  </verify>
  <done>The messaging guide and closed-set checks describe the same preview and actual rows as the catalog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Structured notification to terminal text | Users rely on tense and reason to decide whether a plugin has actually been removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-07 | Repudiation | output-catalog and fixture map | medium | mitigate | Task 1 pins exact pending and actual bytes plus the normal scoped empty and failure states. |
| T-12-08 | Information disclosure | unreadable-declarer catalog state | medium | mitigate | Task 1 gives the failure a key and redacted cause, and compares full emitted bytes. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run the catalog contract/parser, closed-set lock, shared notification pairs, and Prettier check. Inspect the catalog diff to confirm existing examples remain byte-identical.
</verification>

<success_criteria>
The catalog, fixtures, and messaging guide independently enforce D-12-01 and D-12-02 for actual, preview, empty, and failure output.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-06-SUMMARY.md when done.
</output>
