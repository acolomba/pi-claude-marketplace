---
phase: 12-standalone-prune-with-dry-run
plan: "02"
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
  - tests/orchestrators/plugin/dependency-index.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/prune.test.ts
autonomous: true
requirements: [PRUNE-06]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A standalone actual prune reaches the whole-scope fixpoint in dependency-safe localeCompare order; explicit, disabled-declarer-held, and other-scope records survive (D-05-01/02/04/05/06)."
    - "An unreadable declarer stops the whole sweep, while a failed member remains installed and holds its dependencies; independent members still report and commit their outcomes (D-05-07/11/13)."
    - "Named uninstall still removes a depended-on plugin when Phase 6 allows it, then prunes only after the primary removal succeeds (D-05-03)."
  prohibitions: []
  artifacts:
    - path: tests/orchestrators/plugin/prune.test.ts
      provides: "Direct actual-prune fixpoint, cleanup, failure, and scope tests."
    - path: tests/orchestrators/plugin/dependency-index.test.ts
      provides: "Full-scope declaration indexing and fail-closed direct tests."
    - path: tests/orchestrators/plugin/uninstall.test.ts
      provides: "Shared sweep and allowed-uninstall regression tests."
  key_links:
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      via: "The standalone operation calls the shared guarded sweep and post-commit finalizer."
      pattern: sweepOrphans
    - from: extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts
      to: extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts
      via: "Named uninstall excludes its removed primary; standalone indexing includes every scope record."
      pattern: buildScopeDeclarationIndex
---

<objective>
Expand the working actual-prune tracer into a fully protected same-scope fixpoint and retain named-uninstall behavior.

Purpose: The command must remove every eligible dependency while preserving held records after errors.
Output: Direct prune, declaration-index, and uninstall source/test pairs with exact state and row assertions.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-CONTEXT.md
@.planning/phases/12-standalone-prune-with-dry-run/12-RESEARCH.md
@.planning/phases/12-standalone-prune-with-dry-run/12-PATTERNS.md
@.planning/phases/12-standalone-prune-with-dry-run/12-01-SUMMARY.md
@.planning/phases/05-prune-on-uninstall/05-CONTEXT.md
@.planning/phases/06-load-time-dependency-check-and-allowed-uninstall/06-04-SUMMARY.md
@skills/typescript-google-style-review/SKILL.md
@skills/typescript-comments/SKILL.md
@skills/typescript-unit-testing/SKILL.md
@skills/typescript-unit-testing-review/SKILL.md
</context>

## Execution discipline

Read every edited source and test before changing it. Trace buildScopeDeclarationIndex, sweepOrphans, createPrunePlugin, and their callers with CodeGraph first. Start behavior cases red and use independent literal order and state expectations. No new package, persisted field, network operation, or list/info marker belongs here.

## Artifacts this phase produces

- New direct pair tests/orchestrators/plugin/prune.test.ts for the operation introduced in Plan 01.
- Expanded declaration-index and uninstall direct pairs for the extracted shared sweep.
- A passing direct-coverage gate for all three changed production modules before preview work.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove every eligible orphan and contain failed members</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/orchestrators/plugin/prune.test.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <read_first>Read every existing file in this task, domain/dependency-orphans.ts and its paired test, orchestrators/plugin/uninstall.messaging.ts, 12-CONTEXT.md D-05-01/02/04/11/13, and 12-RESEARCH.md Common Pitfalls. Trace sweepOrphans and createPrunePlugin callers with CodeGraph.</read_first>
  <behavior>
    - Orphans left by a prior plain uninstall are removed dependents before dependencies in localeCompare fixpoint batches, including interleaved marketplaces.
    - Explicit installs, same-scope held dependencies, and dependencies of disabled declarers survive; a separate scope is never scanned or mutated.
    - A failed member remains a holder so its descendants survive, while an independent candidate continues and reports its ordinary row.
    - Repeating successful actual prune changes no remaining record.
  </behavior>
  <action>Per D-05-01/02/04/05/11/13, write the prune direct pair against the Plan 01 operation and strengthen the shared sweep if the red tests expose a gap. Assert the full state and filesystem after each call, the exact ordered rows, at most one locked save, and post-commit hooks/cache/data cleanup. Assert independent literal order for an alternating-marketplace chain. Inject a member unstage failure: its record and descendants remain, an independent member still removes, and each attempt gets its existing success or failure row. Keep the isHeldBy recheck immediately before each removal and add only successful members to the gone set. Use the existing contained cascade and offline declaration path; no separate per-member transaction.</action>
  <acceptance_criteria>
    - The direct pair proves the whole-scope fixpoint and literal order with multiple marketplaces.
    - A failed member cannot unlock a dependency only it declared; independent removals commit once.
    - An explicit or disabled-declarer-held record remains installed and staged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/prune.test.ts tests/orchestrators/plugin/uninstall.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a selection-order mismatch, or a failure case that mutates a held record.</fails_when>
  </verify>
  <done>The actual operation removes all eligible members and folds partial failures safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep declaration refusal and allowed named uninstall intact</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts, extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts, tests/orchestrators/plugin/dependency-index.test.ts, tests/orchestrators/plugin/uninstall.test.ts</files>
  <read_first>Read all files in this task, orchestrators/plugin/enable-disable.ts, the Phase 5 decision file, 12-CONTEXT.md D-05-03/05/06/07, and Phase 6's allowed-uninstall summary. Trace all buildScopeDeclarationIndex callers and the primary-removal path in CodeGraph.</read_first>
  <behavior>
    - Full-scope standalone indexing includes all installed declarers, disabled records, and candidates; named uninstall still excludes only its successfully removed primary.
    - A corrupt declarer returns its name and redacted cause before any removal or save, with no network lookup.
    - Allowed named uninstall prunes after the primary leaves; refusal or failure of the primary never runs its sweep, and reconcile gains no pruning.
  </behavior>
  <action>Per D-05-03/05/06/07, add direct index cases for optional exclusion, offline own-manifest and marketplace fallback precedence, same-scope declarers, and a corrupt declaration. Pin the failure's declarer and redacted cause without leaking absolute paths. Add the Phase 6 uninstall regression where a depended-on named plugin is allowed to leave, then the shared sweep evaluates the post-primary scope; also assert no sweep when the primary does not leave. Preserve the existing enable/disable and reconcile callers' index contracts. Complete the direct source/test-pair coverage for dependency-index.ts, uninstall.ts, and prune.ts before Plan 04 uses these seams for preview.</action>
  <acceptance_criteria>
    - The index refuses unreadable declarations before effects and its cause contains no absolute path.
    - Existing named uninstall, including the Phase 6 allowed-removal outcome, remains green.
    - All three changed production modules pass direct line, branch, and function coverage.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/orchestrators/plugin/dependency-index.test.ts tests/orchestrators/plugin/uninstall.test.ts tests/orchestrators/plugin/prune.test.ts</automated>
    <fails_when>Non-zero exit, zero tests, a missing declarer refusal, or a named-uninstall regression.</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/orchestrators/plugin/prune.ts extensions/pi-claude-marketplace/orchestrators/plugin/uninstall.ts extensions/pi-claude-marketplace/orchestrators/plugin/dependency-index.ts</automated>
    <fails_when>Non-zero exit or any changed source/test pair lacks full direct line, branch, and function coverage.</fails_when>
  </verify>
  <done>The same-scope declaration and shared-removal contracts are covered directly without changing named uninstall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Installed declarations to deletion selector | A damaged declaration must not be treated as an empty set of dependents. |
| Member removal to remaining holders | A failed member remains installed and must continue to hold its dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-12-02 | Tampering | declaration index and member sweep | high | mitigate | Tasks 1 and 2 pin full-scope indexing and failed-member rechecks through direct state assertions. |
| T-12-03 | Information disclosure | unreadable declarer row | medium | mitigate | Task 2 checks the named declarer and redacted cause against an absolute-path leak. |

ASVS level 1. Any unresolved high or critical threat blocks completion. No package install occurs.
</threat_model>

<verification>
Run focused direct pairs, direct coverage, node --test tests/architecture/no-orchestrator-network.test.ts, and npm run typecheck before preview work.
</verification>

<success_criteria>
PRUNE-06 reaches the complete safe fixpoint and keeps Phase 5 and Phase 6 uninstall behavior intact; all three operation seams have direct tests.
</success_criteria>

<output>
Create .planning/phases/12-standalone-prune-with-dry-run/12-02-SUMMARY.md when done.
</output>
