---
phase: 09-reload-installs-missing-dependencies
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - docs/output-catalog.md
  - docs/dependency-resolution.md
  - docs/plugin-enablement.md
  - CHANGELOG.md
  - .planning/BACKLOG.md
  - scripts/check-unused-type-members.contracts.json
  - tests/architecture/catalog-uat/catalog-contract.test.ts
  - tests/architecture/catalog-uat/catalog-parser.test.ts
  - tests/architecture/catalog-uat/fixtures/reconcile-applied.ts
autonomous: true
requirements: [MISS-01, MISS-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/output-catalog.md` documents the two-row failure form as the `reconcile-dependency-install-failed` state: the dependency's own `(failed) {dependency failed}` row with its cause line, then the dependent's `(disabled) {dependency unsatisfied}` row with the install remedy, and its fenced bytes are the bytes the real dispatcher renders for the fixture and the bytes the apply test pins (MISS-02, D-09-10)."
    - "`docs/dependency-resolution.md` § The load-time check states what a `/reload` installs, that session start installs nothing, that a failure reports on the dependency's row and leaves the dependent to the check, and that the install is retried on every `/reload` while the declaration stays unsatisfied (D-09-13, D-09-14)."
    - "`docs/plugin-enablement.md` states that the reconcile lift reaches a dependency-provenance record the check held down, and that a disabled dependency is still never re-enabled by a reload (D-09-08, D-09-04)."
    - "The `[Unreleased]` changelog entry for PR #198 describes what the PR now ships: a `/reload` installs a missing declared dependency, and `uninstall` proceeds while naming the dependents (LOAD-03) rather than refusing."
    - "`.planning/BACKLOG.md` carries the two deferred ideas 09-CONTEXT.md asked to record: `marketplace add` / post-install activation running the step (D-09-15) and re-planning the toggles after any mutating bucket (D-09-07)."
    - "`scripts/check-unused-type-members.contracts.json`'s line-pinned entries for every file this phase shifted are remapped once, and `npm run check` is green on the final tree."
  artifacts:
    - path: "docs/output-catalog.md"
      provides: "the reconcile-dependency-install-failed state"
      contains: "catalog-state: reconcile-dependency-install-failed"
    - path: "docs/dependency-resolution.md"
      provides: "the reload paragraph under The load-time check"
      contains: "/reload"
    - path: "CHANGELOG.md"
      provides: "the reload sub-bullet under the PR #198 entry"
      contains: "/reload"
    - path: ".planning/BACKLOG.md"
      provides: "MISS-MPADD-01 and RECON-REPLAN-01"
      contains: "MISS-MPADD-01"
  key_links:
    - from: "tests/architecture/catalog-uat/fixtures/reconcile-applied.ts"
      to: "docs/output-catalog.md"
      via: "catalog-contract renders the fixture through the real dispatcher and compares bytes with the fenced block"
      pattern: "reconcile-dependency-install-failed"
    - from: "tests/architecture/dependency-doc-agreement.test.ts"
      to: "docs/dependency-resolution.md"
      via: "the failure table and the already-installed section are parsed by heading; the new paragraph must not break either"
      pattern: "Why a dependency can fail"
  prohibitions:
    - statement: "The catalog's fenced bytes MUST NOT be hand-typed; they are produced by running the real `notify()` dispatcher over the fixture message (08-03 precedent)."
      status: unresolved
      verification: null
    - statement: "The changelog entry MUST NOT explain the mechanism, list what remains unimplemented, or split one PR across entries (`.claude/rules/changelog.md`)."
      status: unresolved
      verification: null
    - statement: "No production source file may change in this plan except through the contracts remap, which edits only line:col pins."
      status: unresolved
      verification: null
---

<objective>
Say what the reload now does, everywhere a reader looks for it, and prove the
whole tree green. The catalog gains the two-row failure form whose bytes plan
09-03's apply test already pinned; the dependency-resolution doc's load-time
section gains the reload paragraph; the enablement doc's lift paragraph gains
the provenance-independent fact; the changelog entry for PR #198 says what the
PR ships; the BACKLOG records the two deferred ideas; the type-member pins are
remapped once; and `npm run check` runs on the final tree.

Purpose: MISS-02's documented form and the phase gate. Every docs change here
describes behavior plans 09-01..09-03 already proved by test.

Output: the `reconcile-dependency-install-failed` state and fixture, three doc
edits, the changelog sub-bullet, two backlog entries, the pin remap, and a
green `npm run check`.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-reload-installs-missing-dependencies/09-CONTEXT.md
@.planning/phases/09-reload-installs-missing-dependencies/09-RESEARCH.md
@.planning/phases/09-reload-installs-missing-dependencies/09-01-SUMMARY.md
@.planning/phases/09-reload-installs-missing-dependencies/09-03-SUMMARY.md
@.claude/rules/changelog.md
</context>

<artifacts_this_phase_produces>
- `reconcile-dependency-install-failed` -- new `<!-- catalog-state: ... -->`
  anchor and section in `docs/output-catalog.md`, with its fixture entry in
  `tests/architecture/catalog-uat/fixtures/reconcile-applied.ts`.
- `MISS-MPADD-01`, `RECON-REPLAN-01` -- new headings in `.planning/BACKLOG.md`.
- The reload paragraph in `docs/dependency-resolution.md` § "The load-time
  check" and the lift sentence in `docs/plugin-enablement.md`.
- The `/reload` sub-bullet and the corrected `uninstall` sentence under the
  PR #198 entry in `CHANGELOG.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: The failure form, end to end from the fixture to the catalog bytes</name>
  <files>
    docs/output-catalog.md,
    tests/architecture/catalog-uat/fixtures/reconcile-applied.ts,
    tests/architecture/catalog-uat/catalog-contract.test.ts,
    tests/architecture/catalog-uat/catalog-parser.test.ts
  </files>
  <read_first>
    - tests/orchestrators/reconcile/apply.test.ts (the plan 09-03 case `MISS-02: a dependency whose closure fails reports on its own row and the dependent is held down` -- its notification bytes are the source of truth for this state)
    - docs/output-catalog.md (the `reconcile-dependency-installed` state plan 09-01 added, and the `reconcile-install-dependency-failed` state at ~2787-2800 whose prose distinguishes the config-install fold from this two-row form)
    - tests/architecture/catalog-uat/fixtures/reconcile-applied.ts (the `reconcile-dependency-installed` and `reconcile-install-dependency-failed` fixtures)
    - tests/architecture/catalog-uat/catalog-contract.test.ts (the count ledger, `EXPECTED_STATE_COUNT = 221`, `EXPECTED_UTF8_BYTES`)
    - tests/architecture/catalog-uat/catalog-parser.test.ts (the `parses all 221` title)
    - .planning/phases/08-enablement-parity-for-dependencies/08-03-SUMMARY.md (key-decisions: fenced bytes come from running the real dispatcher over the fixture)
  </read_first>
  <action>
Add the `reconcile-dependency-install-failed` state under the
`reconcile-applied-cascade` section directly after `reconcile-dependency-installed`,
titled `Reload could not install a missing declared dependency (MISS-02)`.
The prose states: the failing dependency reports on its OWN `(failed)` row
through the same `plugin-install-failed` outcome and `classifyOrchestratorThrow`
path the config-install row uses (D-09-10), so a closure or constraint failure
the cascade wraps as `DependencyCascadeError` renders `{dependency failed}` with
the redacted specific reason on the cause line (`is not declared by its
marketplace`, `requires marketplace "mp", which is not added`, `has no release
tag satisfying`), and only a failure of the dependency's own ledger keeps its
specific token (`{not in manifest}`, `{network unreachable}`); no marketplace
is added or cloned to satisfy a declaration (D-03-08); the dependent gets the
LOAD-01 `(disabled) {dependency unsatisfied}` row with the install remedy from
the check's verdict, or nothing when it was already down; nothing is
half-materialized (D-03-07, NFR-3); the reload completes; and the install is
retried on the next `/reload` while the declaration stays unsatisfied, each
attempt reporting the failure row alone once the dependent is down (D-09-14).
Severity `error`, tally `Reconcile: 1 failure, 1 warning`, no reload-hint.

The fixture (`reconcile-dependency-install-failed` in `reconcile-applied.ts`,
`expectedSeverity: "error"`) is one `mp [project]` block carrying, in this
order, a `failed` row for `secrets-vault` with `reasons: ["dependency failed"]`
and `cause: new Error('Dependency "crypto-core@mp" is not declared by its marketplace.')`,
then a `disabled` row for `deploy-kit` at `1.0.0` with
`reasons: ["dependency unsatisfied"]`, `cause` the Install remedy,
`severity: "warning"`, `needsReload: true` -- the shape the LOAD-01 fixture
already uses for the second row. Produce the fenced bytes by running the real
`notify()` dispatcher over that message and confirm they equal the bytes the
plan 09-03 apply case asserts; if the two disagree, the apply test is right and
the fixture is wrong. Bump `EXPECTED_STATE_COUNT` 221 -> 222 with a MISS-02
ledger line, recompute `EXPECTED_UTF8_BYTES` from the committed file, and bump
the parser title and assertion to 222. Run prettier on the doc.
  </action>
  <verify>
    <automated>node --test tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/notify-closed-set-locks.test.ts</automated>
    <fails_when>non-zero exit, or any assertion names a count or byte mismatch</fails_when>
    <automated>npx prettier --check docs/output-catalog.md</automated>
    <fails_when>non-zero exit, or the output names `docs/output-catalog.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'catalog-state: reconcile-dependency-install-failed' docs/output-catalog.md` prints one line.
    - `grep -n '"reconcile-dependency-install-failed"' tests/architecture/catalog-uat/fixtures/reconcile-applied.ts` prints one line.
    - `grep -n 'EXPECTED_STATE_COUNT' tests/architecture/catalog-uat/catalog-contract.test.ts` shows the pin set to 222, and `grep -n 'parses all' tests/architecture/catalog-uat/catalog-parser.test.ts` shows the title naming 222.
    - The fenced block under the new anchor contains the line `  ⊘ secrets-vault (failed) {dependency failed}` followed by `    cause: Dependency "crypto-core@mp" is not declared by its marketplace.` and later `  ◍ deploy-kit v1.0.0 (disabled) {dependency unsatisfied}`, and ends with `Reconcile: 1 failure, 1 warning`.
    - The fenced block's lines are byte-equal to the notification the apply.test.ts closure-failure case asserts (diff the two by hand once; record the check in the SUMMARY).
  </acceptance_criteria>
  <done>The catalog documents the two-row failure form with bytes the real dispatcher produced and the apply test already pinned, and every catalog pin agrees at 222 states.</done>
</task>

<task type="auto">
  <name>Task 2: The docs, the changelog and the backlog say what the reload does</name>
  <files>
    docs/dependency-resolution.md,
    docs/plugin-enablement.md,
    CHANGELOG.md,
    .planning/BACKLOG.md
  </files>
  <read_first>
    - docs/dependency-resolution.md (lines 170-215 "The load-time check" -- the paragraph order: the three cases, the marker, no oscillation, the lift, the chain; lines 216-250 "Why a dependency can fail" is parsed by `tests/architecture/dependency-doc-agreement.test.ts` and must keep its table)
    - docs/plugin-enablement.md (line 34 the `dependencyDisabled` paragraph; line 52 the "only ever holds a record DOWN; nothing on that path lifts one" sentence, which is now false for the marker-held lift and stays true for a disabled dependency)
    - CHANGELOG.md (lines 1-12 the `[Unreleased]` PR #198 entry and its sub-bullets; line 8 says `uninstall` refuses while other plugins need the plugin, which LOAD-03 changed)
    - .claude/rules/changelog.md (the shape and wording rules; run the `simple-english` Plain and `humanizer` self-checks on every line added or changed)
    - .planning/BACKLOG.md (the `PENDING-VERDICT-01` entry at ~3195 for the heading and paragraph shape)
    - .planning/phases/09-reload-installs-missing-dependencies/09-CONTEXT.md (Deferred Ideas: the two items to record)
  </read_first>
  <action>
**`docs/dependency-resolution.md`.** Under "The load-time check", after the
chain paragraph ("One broken dependency reaches every plugin above it..."),
add a short section or paragraph run titled for the reload. Say, in plain
sentences: a `/reload` installs a declared dependency the scope does not have,
through the same install cascade `install` uses, from the marketplace the
declaration names, at a version inside every declaring plugin's range, and
records it as installed for a dependency, not by name; the dependent then
loads in that same reload; a session start does not install anything -- it
reports the missing dependency and waits for a `/reload`; a dependency that
is installed but disabled is not enabled by a reload (the row still says
enable it or uninstall the dependent); when the install fails, the dependency
gets its own row with the reason, the dependent stays disabled with the
install remedy, nothing is left half installed, and the next `/reload` tries
again. Include the two-row example block byte-copied from the catalog's
`reconcile-dependency-install-failed` fenced text and the one-row success
example from `reconcile-dependency-installed`. Do not touch the failure table
under "Why a dependency can fail".

**`docs/plugin-enablement.md`.** Amend the line-52 sentence so it stays true:
the reconcile pass lifts the disable it applied itself once the dependency is
satisfied -- for a plugin the configuration names and for a dependency record
it does not name alike (D-09-08) -- and still never enables a dependency the
user disabled, and a `/reload` installs a MISSING dependency (link to
`dependency-resolution.md`'s load-time section) while a DISABLED one stays
disabled until `enable <plugin>` or `install <plugin>` (D-09-04).

**`CHANGELOG.md`.** Under the PR #198 top-level bullet add one sub-bullet:
`/reload` now installs a declared dependency that an installed plugin lacks,
and enables the plugin that needed it; a dependency that cannot be installed
gets its own row with the reason. Then correct the existing sub-bullet on line
8 so it describes what the PR ships after LOAD-03: `uninstall` proceeds while
other installed plugins still need the plugin, names them, and the next
`/reload` disables them with a remedy. Keep every bullet at 40 words or fewer,
one sentence, active voice, no `-ing` clause after a comma, no `should`,
`would`, `could`, `may`, no semicolon, no dash between statements. Run
`pre-commit run --files CHANGELOG.md` before committing.

**`.planning/BACKLOG.md`.** Append two entries in the file's heading style:
`## MISS-MPADD-01: marketplace add and post-install activation do not resolve missing dependencies`
(upstream runs its resolver on `marketplace add` and after an install; here
neither `marketplace add`, `bootstrap.ts` nor `autoupdate.ts` reaches
`applyReconcile`, so the next `/reload` completes the closure; D-09-15; scope
when picked up: run the verdict and the dependency-install step for one scope
from those call sites), and
`## RECON-REPLAN-01: the toggles are re-planned only after a dependency install`
(a config-driven uninstall of a dependency in the same pass is invisible to the
read-pass verdict, so its dependent is held down one reload late; D-09-07
re-plans only when the dependency step installed or found something; scope
when picked up: re-plan after any mutating bucket, and measure the cost on a
steady-state reload).
  </action>
  <verify>
    <automated>node --test tests/architecture/dependency-doc-agreement.test.ts tests/architecture/partial-vocabulary-guard.test.ts</automated>
    <fails_when>non-zero exit (the failure table or a guarded vocabulary drifted)</fails_when>
    <automated>pre-commit run --files CHANGELOG.md docs/dependency-resolution.md docs/plugin-enablement.md .planning/BACKLOG.md</automated>
    <fails_when>non-zero exit, or the output reports `Failed` for mdformat or markdownlint, or reports `files were modified by this hook` (re-run until clean, then stage the rewritten files)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '/reload' docs/dependency-resolution.md` prints 1 or more within the "The load-time check" section (check by reading the section).
    - `grep -c 'reconcile-dependency-install-failed\|is not declared by its marketplace' docs/dependency-resolution.md` prints 1 or more.
    - `grep -c 'D-09-08\|dependency record it does not name\|does not name' docs/plugin-enablement.md` prints 1 or more, and the sentence `nothing on that path lifts one` is gone.
    - `grep -c 'refuses to remove a plugin that another installed plugin still needs' CHANGELOG.md` prints 0.
    - `grep -c '/reload' CHANGELOG.md` prints at least one more than before this task (the new sub-bullet).
    - `grep -n '^## MISS-MPADD-01\|^## RECON-REPLAN-01' .planning/BACKLOG.md` prints two lines, one per heading.
    - Every added or changed CHANGELOG line is 40 words or fewer (count by hand; record the counts in the SUMMARY).
  </acceptance_criteria>
  <done>A reader of the dependency doc, the enablement doc, the changelog and the backlog finds the reload's behavior, its limits and its deferred follow-ups stated in plain English that matches the shipped tests.</done>
</task>

<task type="auto">
  <name>Task 3: Remap the type-member pins once and run the full gate</name>
  <files>
    scripts/check-unused-type-members.contracts.json
  </files>
  <read_first>
    - scripts/check-unused-type-members.contracts.json (the entries whose `id` / `filter` name `orchestrators/reconcile/apply.ts`, `orchestrators/reconcile/notify.ts`, `shared/notification-types.ts`, `orchestrators/plugin/install-cascade.ts`, `orchestrators/plugin/install-flow.ts` -- each pins a line:col that this phase's edits shifted)
    - scripts/check-unused-type-members.mjs (lines 1-60 the exit-status contract: 2 means a setup failure such as a drifted pin, never a clean tree)
    - .planning/phases/08-enablement-parity-for-dependencies/08-01-SUMMARY.md (the chore commit that remapped the pins after the content commit)
  </read_first>
  <action>
Run `npm run lint:type-members`. For every contract whose pinned `id` or
`filter` no longer sits on the member it names (the gate reports the drifted
coordinate, or exits 2 with an empty report), find the member's CURRENT
line:col in the named file and rewrite ONLY the `id` and `filter` coordinates
of that entry; change no `owner`, `key`, `category` or `purpose`, and add no
new entry. Re-run until the gate exits 0. Then run the full gate,
`npm run check`, and fix nothing else in this task: a red step here is a
defect of an earlier plan and goes back to it, never a pin or an exception
added here. Record the list of remapped ids in the SUMMARY.
  </action>
  <verify>
    <automated>npm run lint:type-members</automated>
    <fails_when>non-zero exit, or an empty report with exit status 2 (a drifted pin)</fails_when>
    <automated>npm run check</automated>
    <fails_when>non-zero exit, or any step's output reports a failure, a coverage percentage below 100, or a fallow finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run lint:type-members` exits 0.
    - `git diff --stat scripts/check-unused-type-members.contracts.json` shows only line changes inside existing entries; `grep -c '"id"' scripts/check-unused-type-members.contracts.json` prints the same count before and after (record both in the SUMMARY).
    - `npm run check` exits 0 on the final tree.
    - `git status --short` after the commit shows no unstaged rewrite of a committed file (the prettier hook writes; check and commit a follow-up if it did, never amend).
  </acceptance_criteria>
  <done>Every line-pinned contract names the member it always named, and the full gate is green on the tree that ships this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture message -> published catalog bytes | The catalog is a published contract; its bytes must be what the renderer produces, not what an author typed. |
| docs -> reader | A doc claiming behavior the tests do not prove teaches the user a false remedy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-16 | Repudiation | `docs/output-catalog.md` fenced bytes | medium | mitigate | The bytes are produced by the real dispatcher over the fixture and cross-checked against the apply test's assertion; `catalog-contract.test.ts` re-renders them on every run. |
| T-09-17 | Information disclosure | catalog and doc cause-line examples | low | mitigate | Every example cause line names keys and marketplace names only, never a path; the same `redactedDependencyCascadeError` the apply step applies is what produced them. |
| T-09-18 | Tampering | `check-unused-type-members.contracts.json` remap | low | mitigate | Only coordinates change; owner / key / purpose are untouched and the entry count is asserted unchanged, so no member gains an allowance it did not have. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. |
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/dependency-doc-agreement.test.ts`
- `pre-commit run --files CHANGELOG.md docs/dependency-resolution.md docs/plugin-enablement.md docs/output-catalog.md .planning/BACKLOG.md`
- `npm run lint:type-members`
- `npm run check`
</verification>

<success_criteria>
- The catalog documents both D-09-09's success row and D-09-10's two-row failure form, each matched by a fixture the real dispatcher renders byte-for-byte.
- `docs/dependency-resolution.md`, `docs/plugin-enablement.md` and `CHANGELOG.md` describe the reload behavior the tests prove, including its startup, disabled-dependency and retry limits.
- The two deferred ideas have BACKLOG carriers.
- `npm run check` is green on the final tree with every type-member pin remapped.
</success_criteria>

<output>
Create `.planning/phases/09-reload-installs-missing-dependencies/09-04-SUMMARY.md` when done
</output>
