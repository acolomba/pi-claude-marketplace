---
phase: 04-install-provenance
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/architecture/compat-01-no-expansion.test.ts
  - tests/architecture/hooks-foundation.test.ts
  - tests/persistence/state-io.test.ts
  - tests/persistence/migrate.test.ts
autonomous: true
requirements: [PROV-01, PROV-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04-03: the four architecture pins that hold the record's key set and the schemaVersion union are AMENDED by equality, never loosened into a subset or range check, and each amended failure message records the new precedent it now sanctions."
    - "D-04-06: PROV-04 is satisfied by a truthful silent upgrade and nothing else — no stale-state report, no recovery-command wording, no notification. Nothing in this plan borrows from MIGR-01, whose staleness gate stays in the backlog intact."
    - "PROV-04: a pre-milestone document loads, every record reads the direct-install value, and no record is misreported as a dependency — proven over a multi-record, multi-marketplace document, not a single record."
    - "PROV-04 (edge: unclassified) — FLAGGED PLANNER ASSUMPTION, deliberately left unresolved: the probe returned `unclassified` for PROV-04 and no defensible acceptance criterion was minted for it. The assumption the executor is carrying is that `loadState` is the ONLY reader of an on-disk document, so the fill is the complete answer and no second path can bypass it. 04-RESEARCH.md § Runtime State Inventory asserts this from the two-writer seam gate; it was not independently re-probed. If a second reader exists, PROV-04 is under-covered and this must be surfaced at verification rather than silently accepted."
  artifacts:
    - tests/architecture/compat-01-no-expansion.test.ts
    - tests/architecture/hooks-foundation.test.ts
    - tests/persistence/state-io.test.ts
    - tests/persistence/migrate.test.ts
  key_links:
    - "`compat-01-no-expansion.test.ts` reads the record's key set off `PLUGIN_INSTALL_RECORD_SCHEMA` itself, so the pin moves only when the schema moves — the equality is the whole point."
    - "`hooks-foundation.test.ts`'s schemaVersion-union case is a SECOND, independent pin on the same union; its own comment nominates this moment as the one that must update it."
  prohibitions:
    - "No gate may be disarmed to make it pass. The key-set equality must not become a subset check, the schemaVersion union must not become a range or a length-only check, and a byte-exact expectation must never be repaired by pasting the new actual bytes back into it. An expectation is rebuilt independently from what the contract now says, or the test has verified nothing."
---

<objective>
Amend the four pinned gates that this phase trips by design, and re-establish the persistence layer's byte-exact and behavioral contracts at schemaVersion 3.

This is the deliberate act the COMPAT-01 gate exists to force. The ROADMAP calls tripping it "the point of having it": the record's key set grows only on purpose, and a required field plus a schemaVersion bump plus a migrate fill is a NEW precedent the gate's own failure message must now record.

Purpose: after this plan the two architecture files and the two persistence test files are green, which isolates the remaining red to fixture shape — the thing plan 04-03 sweeps.
Output: four amended pins with rewritten failure messages, and the PROV-04 contract proven over a realistic multi-record legacy document.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-install-provenance/04-CONTEXT.md
@.planning/phases/04-install-provenance/04-PATTERNS.md
@.planning/phases/04-install-provenance/04-VALIDATION.md
@.planning/phases/04-install-provenance/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend the four architecture pins by equality, and rewrite what each now sanctions</name>
  <files>tests/architecture/compat-01-no-expansion.test.ts, tests/architecture/hooks-foundation.test.ts</files>
  <read_first>
    - tests/architecture/compat-01-no-expansion.test.ts — the whole file, but in particular the three cases this phase trips: the install-record key-set enumeration equality, the schemaVersion union case, and the `DEFAULT_STATE.schemaVersion` case. Read each case's failure message in full; the message is part of the contract, not decoration.
    - tests/architecture/hooks-foundation.test.ts — the `ENBL-02: STATE_SCHEMA.schemaVersion` case, including its arity assertion and its own comment nominating a future widening to v3 as the moment it must be updated.
    - extensions/pi-claude-marketplace/persistence/state-io.ts — the amended schema, so the expected key set and union are read from what the schema now says rather than from memory.
    - .planning/codebase/CONVENTIONS.md — the "plant the violation, don't read the config" rule.
  </read_first>
  <action>
    Amend all four pins, in one commit, by EQUALITY.

    Pin 1 — the install-record key-set enumeration. Add the new field name to the
    expected array in its sorted position. Then rewrite the failure message. Today
    it sanctions exactly one way for the key set to grow: an OPTIONAL additive field
    that needs no schemaVersion bump and no migrate fill, citing the `resolvedSha` /
    `hookEntries` precedent. That is no longer the only sanctioned way. The rewritten
    message must state the second, now-established route — a REQUIRED field that
    arrives WITH a schemaVersion bump and a migrate fill that runs before validation
    — and cite `D-04-03` and `ENBL-02` as the two instances of it. Keep the
    `assert.deepStrictEqual` shape. Do not relax it into a subset or superset check
    and do not sort-then-compare a partial list.

    Pin 2 — the schemaVersion union. Extend the expected array to three members.
    Its message today asserts that no state-schema migration was introduced and that
    a third version would mean an on-disk migration the work promised not to require.
    That sentence is now false. Rewrite it to state that the migration WAS introduced,
    what it is (an absent required field filled with a truthful default before
    validation), and why it needed no user-visible step. Cite `D-04-03`.

    Pin 3 — `DEFAULT_STATE.schemaVersion`. Move the expected value to 3.

    Pin 4 — the second, independent union pin in `hooks-foundation.test.ts`. Its
    title names the two-member union, its arity assertion pins the length at two, and
    it asserts both literal memberships. Update the title, the arity, and the
    membership assertions, and amend its "any future widening to v3 requires this test
    to be updated" comment to record that this is that widening. Do not delete the
    comment — it is the tripwire's own documentation and it should now name what
    happened.

    Before committing, PLANT one violation and observe each amended pin go red:
    temporarily rename the new schema field and confirm pin 1 fails; temporarily set
    `DEFAULT_STATE.schemaVersion` back to 2 and confirm pin 3 fails. Restore both.
    A pin that was never seen red after amendment proves nothing.
  </action>
  <verify>
    <automated>node --test tests/architecture/compat-01-no-expansion.test.ts tests/architecture/hooks-foundation.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any `fail` count above 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Both files report `fail 0` and a non-zero `pass` count.
    - `grep -c 'deepStrictEqual' tests/architecture/compat-01-no-expansion.test.ts` is unchanged from before this task — no equality assertion was converted to a weaker form.
    - The key-set case's failure message names both sanctioned growth routes (optional-additive-no-bump, and required-with-bump-and-fill) and cites `D-04-03`.
    - The schemaVersion union case's message no longer claims that no migration was introduced.
    - `hooks-foundation.test.ts`'s union case asserts an arity of 3 and its comment records that the widening happened.
    - Reverting `DEFAULT_STATE.schemaVersion` to 2 makes pin 3 fail; the executor observed this and restored the file.
  </acceptance_criteria>
  <done>Both architecture files are green, every amendment is by equality, and each amended failure message records the new precedent rather than the retired claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-establish the persistence contracts at schemaVersion 3, and prove PROV-04</name>
  <files>tests/persistence/state-io.test.ts, tests/persistence/migrate.test.ts</files>
  <read_first>
    - tests/persistence/state-io.test.ts — the whole file. In particular the negative case asserting that schema version 3 is currently REJECTED (it inverts), the frozen-default-state byte case, the exact-bytes save/load round trip, the normalized-document case that preserves its stamp, and the two `clonePluginRecord` field-census cases.
    - tests/persistence/migrate.test.ts — the whole file, in particular the fixed-point replay case that asserts exact migrated bytes.
    - extensions/pi-claude-marketplace/persistence/state-io.ts — the amended schema, `DEFAULT_STATE`, `loadState`'s version guard, `clonePluginRecord`.
    - extensions/pi-claude-marketplace/persistence/migrate.ts — `ensurePluginProvenance` and its call site.
    - .agents/skills/typescript-unit-testing-review/SKILL.md — the assertion and hermeticity rules these amendments must satisfy.
  </read_first>
  <behavior>
    - `loadState` accepts a document declaring schemaVersion 1, 2 or 3 and rejects 4 with the unsupported-version error.
    - The frozen default state's exact bytes declare schemaVersion 3 and are asserted against an independently written literal, not against what the code produced.
    - A save/load round trip of a complete record preserves every field including provenance, byte for byte.
    - `clonePluginRecord` copies provenance; a legacy record clone still invents no optional field.
    - A multi-marketplace, multi-plugin v2 document with no provenance anywhere migrates so that EVERY record reads the direct-install value and NONE reads the dependency value.
    - Migrating the same document twice is a fixed point — the second pass reports no mutation and writes identical bytes.
  </behavior>
  <action>
    Amend the roughly 18 behavioral and byte-exact cases in
    `tests/persistence/state-io.test.ts` and the roughly 5 in
    `tests/persistence/migrate.test.ts` that this phase moves.

    Invert the negative case that currently asserts schema version 3 is rejected:
    it becomes an acceptance case, and the rejection case moves to version 4 so the
    guard's upper bound stays pinned. Both directions must be present; an acceptance
    case alone would pass against a guard that accepts everything.

    For every byte-exact expectation, REBUILD the expected literal from what the
    contract now says — the record's declared field order and the new schemaVersion
    — and write it out by hand. Do NOT run the code, copy its output and paste it
    into the expectation. A byte assertion repaired that way has verified nothing,
    and the suite's own convention is that expected values are built independently.

    Add the three PROV-04 cases named in 04-VALIDATION.md's test map, each titled
    with its `D-04-03` anchor:
    - a v2 document with no provenance loads, fills the direct-install value, and
      is persisted;
    - a record whose provenance is present but not one of the two literals reaches
      `STATE_VALIDATOR.Check` unchanged and is rejected with a message containing
      that record's JSON pointer path;
    - a document with at least two marketplaces and at least two plugins each
      migrates so that every record reads the direct-install value — the "no record
      is misreported as a dependency" half of PROV-04, which a single-record case
      cannot show.

    For the second of those, observe it failing against a fill that overwrites
    rather than only filling absence. For the third, observe it failing against a
    fill whose default is the dependency literal. Plant each, watch it go red,
    restore. These two planted runs are what make the pair falsifiable; without them
    the cases pass against the wrong implementation.

    Anchor every new test title on `D-04-03` or `D-04-06`. Never a bare `PROV-NN`.

    On PROV-04's unresolved probe edge: this plan carries the assumption that
    `loadState` is the only reader of an on-disk state document, so the fill is the
    complete answer. Do not add a defensive second fill anywhere; if the executor
    finds a second reader while working, stop and surface it rather than patching
    around it.
  </action>
  <verify>
    <automated>node --test tests/persistence/state-io.test.ts tests/persistence/migrate.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports any `fail` count above 0</fails_when>
    <automated>npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/migrate.ts</automated>
    <fails_when>non-zero exit, or the report shows any function, line or branch in `migrate.ts` below 100% when the pair is run alone</fails_when>
  </verify>
  <acceptance_criteria>
    - Both persistence test files report `fail 0`.
    - A case asserts schemaVersion 3 is ACCEPTED and a sibling case asserts 4 is REJECTED with the unsupported-version error text.
    - A case migrates a document with at least two marketplaces and at least two plugins each and asserts every resulting record carries the direct-install value.
    - A case asserts a present-but-invalid provenance survives the migrate untouched and is rejected by the validator with its JSON pointer path in the message.
    - The executor recorded, in the summary, having observed the overwrite-instead-of-fill and dependency-default plants going red.
    - `npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/migrate.ts` reaches 100% on all three axes.
  </acceptance_criteria>
  <done>
    `tests/persistence/**` is green at schemaVersion 3, PROV-04's three cases exist
    and were each observed red against a specific wrong implementation, and
    `migrate.ts` holds full run-alone coverage.
  </done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

See `04-01-PLAN.md` § Artifacts for the full phase table. This plan contributes:

| Symbol / path | Kind |
|---|---|
| Amended key-set failure message recording the required-with-bump-and-fill precedent | gate prose |
| Amended schemaVersion-union failure message (both pins) | gate prose |
| `D-04-03` acceptance cases for schemaVersion 3 and rejection of 4 | tests |
| `D-04-03` multi-record no-misreport case | test |
| `D-04-03` present-but-invalid-value non-coercion case | test |

No new production module. No new source-test pair.
</artifacts>

<assumption_delta_decision>
**Noun that is now primary:** the install record's `provenance` — a record states how
the plugin got here, and "the user asked for it by name" is one variant.

**Decision: promote.** A single REQUIRED field on the single existing record schema,
with the old carrier of the same fact (the desired-state config entry for a
dependency) retired in wave 5 rather than kept beside it. This plan's contribution to
the promote is that the four gates pinning the OLD singular shape are amended by
equality — the mechanism that forces any future re-narrowing to be equally deliberate.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-edited `state.json` -> `loadState` | an untrusted document crosses into the extension's own model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Denial of Service | `STATE_VALIDATOR.Check` / `loadState` | low | mitigate | This plan's non-coercion case is the proof: a present-but-invalid value reaches the validator and is rejected with a JSON pointer naming the record, rather than being silently coerced into a value reconcile would then act on. |
| T-04-06 | Tampering | the four amended architecture pins | medium | mitigate | Every amendment stays an equality assertion and each rewritten message records what it now sanctions. Loosening any of them into a subset check would disarm the tripwire for every future field, which is why it is a named prohibition here rather than a review note. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; `04-RESEARCH.md` § Package Legitimacy Audit records the not-applicable verdict. |
</threat_model>

<verification>
- `node --test tests/architecture/compat-01-no-expansion.test.ts tests/architecture/hooks-foundation.test.ts` reports `fail 0`.
- `node --test tests/persistence/state-io.test.ts tests/persistence/migrate.test.ts` reports `fail 0`.
- `npm run test:coverage:direct -- extensions/pi-claude-marketplace/persistence/migrate.ts` reaches 100%.
- `npm test` as a whole is still EXPECTED red; plan 04-03 drives it to zero.
</verification>

<success_criteria>
- All four architecture pins are amended by equality and green, with rewritten messages recording the new precedent.
- `loadState` accepts 1, 2 and 3 and rejects 4, each direction proven by its own case.
- A multi-record legacy document migrates with every record reading the direct-install value.
- A present-but-invalid value is never coerced.
- Each new case was observed failing against a named wrong implementation.
</success_criteria>

<output>
Create `.planning/phases/04-install-provenance/04-02-SUMMARY.md` when done.
Record the observed-red evidence for each planted violation and the remaining
`npm test` failure count, so plan 04-03 starts from a measured number rather than
the research estimate.
</output>
