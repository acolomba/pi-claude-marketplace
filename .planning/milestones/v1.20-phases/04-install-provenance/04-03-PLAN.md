---
phase: 04-install-provenance
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/orchestrators/plugin/update-flow.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/orchestrators/plugin/uninstall.test.ts
  - tests/orchestrators/plugin/list-flow.test.ts
  - tests/orchestrators/plugin/update-preflight.test.ts
  - tests/orchestrators/plugin/install-cascade.test.ts
  - tests/orchestrators/plugin/install-flow.test.ts
  - tests/orchestrators/plugin/install-outcome.test.ts
  - tests/orchestrators/plugin/reinstall-targets.test.ts
  - tests/orchestrators/plugin/reinstall-flow.test.ts
  - tests/orchestrators/plugin/shared.test.ts
  - tests/orchestrators/plugin/clone-gc.test.ts
  - tests/orchestrators/plugin/bootstrap.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - tests/orchestrators/plugin/update-swap.test.ts
  - tests/orchestrators/marketplace/update.test.ts
  - tests/orchestrators/marketplace/remove.test.ts
  - tests/orchestrators/marketplace/add.test.ts
  - tests/orchestrators/marketplace/autoupdate.test.ts
  - tests/orchestrators/reconcile/backfill.test.ts
  - tests/orchestrators/reconcile/apply.test.ts
  - tests/orchestrators/reconcile/pending.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - tests/orchestrators/edge-deps.test.ts
  - tests/orchestrators/plugin-path.test.ts
  - tests/edge/handlers/plugin/uninstall.test.ts
  - tests/edge/handlers/plugin/reinstall.test.ts
  - tests/edge/handlers/plugin/list.test.ts
  - tests/edge/handlers/plugin/update.test.ts
  - tests/edge/handlers/plugin/enable-disable.test.ts
  - tests/edge/handlers/marketplace/update.test.ts
  - tests/edge/handlers/tools.test.ts
  - tests/edge/register.test.ts
  - tests/transaction/with-state-guard.test.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/index.test.ts
autonomous: true
requirements: [PROV-01, PROV-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The whole gate is green: `npm run check` exits 0, which includes typecheck, lint, both workflow linters, fallow, prettier, both corresponding-test gates, the direct-coverage negative gate, the unit suite and the integration suite."
    - "PROV-02: a plugin the user installed directly keeps its ENTIRE record byte-identical after a later install declares it as a dependency — not just its provenance, every field — proven by snapshotting with `clonePluginRecord` before the second install and comparing the whole record after."
    - "PROV-02 (edge: unclassified) — FLAGGED PLANNER ASSUMPTION, deliberately left unresolved: the probe returned `unclassified` for PROV-02 and no defensible acceptance criterion was minted for it. The assumption carried is that RESV-05's already-installed branch is the ONLY path on which an existing record meets a cascade, because the closure walk's root guard excludes already-installed non-root members before any ledger phase exists. This was read from the closure walk's source, not exhaustively probed. If a second path exists, PROV-02 is under-covered and must be surfaced at verification, not silently accepted."
    - "D-04-08: no test title or source comment added by this plan cites a bare `PROV-NN`; every anchor is `D-04-01`, `D-04-02` (the phase's decision IDs) or an existing in-tree anchor."
  artifacts:
    - tests/orchestrators/plugin/install-flow.test.ts
    - tests/orchestrators/plugin/update-flow.test.ts
    - tests/orchestrators/reconcile/apply.test.ts
    - tests/transaction/with-state-guard.test.ts
  key_links:
    - "Most files have ONE shared per-file record builder; fixing that builder collapses dozens of failures at once (the probe measured 276 failures in `update-flow.test.ts` arising from 2 compiler diagnostics)."
    - "`saveState` revalidates before every write, so a loosely-typed fixture the compiler never sees still fails at runtime with a `must have required property` refusal — which is why the exit criterion is the suite, not the typechecker."
  prohibitions:
    - "No gate may be disarmed to make it pass. A byte-exact expectation is never repaired by pasting the run's actual output back into it, a `deepStrictEqual` is never weakened to a partial or subset comparison, and a case is never deleted because amending it is tedious. If a case genuinely no longer describes a real contract, say so in the summary and delete it deliberately with the reason — do not let it quietly become vacuous."
    - "A fixture builder must not be given the dependency value as its default. Every existing case was written about a directly-installed plugin; silently relabelling them all would make the reconcile exemption in wave 4 look correct while proving nothing."
---

<objective>
Drive the tree from roughly 861 failing cases to zero, and prove PROV-02 with a test that cannot pass vacuously.

The required field invalidates every hand-built install record in the suite. Around 765 of the failures are one runtime message from loosely-typed fixtures and collapse per-file when each file's shared record builder gains the field. The remaining ~96 across 20 files are genuine byte-exact and behavioral contracts; each needs reading and amending, not editing.

Purpose: this is the wave where `npm run check` returns to green. Until it does, every later wave is building on an unmeasurable tree.
Output: a green gate, and PROV-02's whole-record proof observed failing against a planted cascade that touches skipped members.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-install-provenance/04-CONTEXT.md
@.planning/phases/04-install-provenance/04-PATTERNS.md
@.planning/phases/04-install-provenance/04-VALIDATION.md
@.planning/phases/04-install-provenance/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the orchestrator suites — builders first, contracts second</name>
  <files>tests/orchestrators/plugin/update-flow.test.ts, tests/orchestrators/plugin/info.test.ts, tests/orchestrators/plugin/uninstall.test.ts, tests/orchestrators/plugin/list-flow.test.ts, tests/orchestrators/plugin/update-preflight.test.ts, tests/orchestrators/plugin/install-cascade.test.ts, tests/orchestrators/plugin/install-flow.test.ts, tests/orchestrators/plugin/install-outcome.test.ts, tests/orchestrators/plugin/reinstall-targets.test.ts, tests/orchestrators/plugin/reinstall-flow.test.ts, tests/orchestrators/plugin/shared.test.ts, tests/orchestrators/plugin/clone-gc.test.ts, tests/orchestrators/plugin/bootstrap.test.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/orchestrators/plugin/update-swap.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/marketplace/remove.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/autoupdate.test.ts, tests/orchestrators/reconcile/backfill.test.ts, tests/orchestrators/reconcile/apply.test.ts, tests/orchestrators/reconcile/pending.test.ts, tests/orchestrators/reconcile/plan.test.ts, tests/orchestrators/edge-deps.test.ts, tests/orchestrators/plugin-path.test.ts</files>
  <read_first>
    - The per-file shared record builder in each file before touching any case in it. In most files it is a single local function returning a `PluginInstallRecord`-shaped literal, near the top of the file. `tests/orchestrators/reconcile/plan.test.ts`'s `pluginRecord` is the canonical shape even though that file is not in this task's list.
    - extensions/pi-claude-marketplace/persistence/state-io.ts — the amended record schema, so each builder is given the field in its declared position.
    - .agents/skills/typescript-unit-testing-review/SKILL.md — the assertion, hermeticity and fixture rules; a builder that grows an option must not become a shared cross-file fixture.
    - The nine behavioral cases in `tests/orchestrators/marketplace/add.test.ts`, the eleven in `tests/orchestrators/marketplace/remove.test.ts`, the seven in `tests/orchestrators/reconcile/backfill.test.ts` and the six in `tests/orchestrators/reconcile/apply.test.ts` in full before amending any of them — these are contracts, not fixtures.
  </read_first>
  <action>
    Work file by file, builders first. For each file, find its shared record builder
    and give it the new field with the direct-install value as the default, plus an
    option so a case that needs the dependency value can ask for it. This one edit is
    what collapses the bulk of the failures — the probe measured 276 failures in
    `update-flow.test.ts` arising from 2 compiler diagnostics, because that file has
    such a builder.

    After the builder pass, run the file and read what is still red. Those are the
    behavioral and byte-exact contracts. Amend each deliberately: rebuild the
    expected value from what the contract now says and write it out, rather than
    running the code and pasting its output back. Roughly 96 cases across 20 files
    fall into this group; the rest are mechanical.

    Three groups in this task deserve reading rather than editing:
    `tests/orchestrators/marketplace/add.test.ts` and `remove.test.ts` (20 cases
    between them) assert whole persisted-state shapes; `tests/orchestrators/reconcile/
    backfill.test.ts` and `apply.test.ts` (13 cases) assert reconcile outcomes over
    hand-built state; and `tests/orchestrators/plugin/shared.test.ts` (3 cases)
    asserts config-write bytes. Each of these asserts something this phase is
    deliberately changing the inputs of but NOT the outputs of — if an expected
    OUTPUT moves in one of them, stop and record why in the summary, because that is
    a behavior change this plan did not intend.

    `tests/orchestrators/reconcile/plan.test.ts` is a special case. Its
    `pluginRecord` builder types its return as the real record type, so it carries a
    compiler diagnostic that `npm run check` will not pass without. Touch that
    builder and ONLY that builder: give it the field with the direct-install default
    and a provenance option in its existing options bag, exactly as the other
    builders get. Add no case and amend no assertion in that file — plan 04-04 owns
    its cases and needs the option this task leaves behind.

    Anchor any new or amended comment on `D-04-01` or `D-04-03`. Never a bare
    `PROV-NN`.
  </action>
  <verify>
    <automated>node --test "tests/orchestrators/**/*.test.ts"</automated>
    <fails_when>non-zero exit, or the summary reports any `fail` count above 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/orchestrators/**/*.test.ts"` reports `fail 0` with a `pass` count at or above the pre-phase count for that glob.
    - No `deepStrictEqual` in the touched files was converted to a partial or subset comparison; the executor states the count of such assertions before and after in the summary.
    - Every builder that gained the field defaults to the direct-install value; `grep` across the touched files finds no builder whose default is the dependency value.
    - Any expected OUTPUT that moved (as opposed to an expected INPUT) is enumerated in the summary with its reason.
  </acceptance_criteria>
  <done>The orchestrator suites are green and every non-mechanical amendment is accounted for in the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the edge, transaction, bridge and entry suites, and close the integration suite</name>
  <files>tests/edge/handlers/plugin/uninstall.test.ts, tests/edge/handlers/plugin/reinstall.test.ts, tests/edge/handlers/plugin/list.test.ts, tests/edge/handlers/plugin/update.test.ts, tests/edge/handlers/plugin/enable-disable.test.ts, tests/edge/handlers/marketplace/update.test.ts, tests/edge/handlers/tools.test.ts, tests/edge/register.test.ts, tests/transaction/with-state-guard.test.ts, tests/bridges/hooks/event-router.test.ts, tests/index.test.ts</files>
  <read_first>
    - tests/transaction/with-state-guard.test.ts — the six cases asserting literal `state.json` bytes. Read all six before editing any; they pin the exact serialized document and each must be rebuilt from the contract, not from output.
    - tests/edge/handlers/tools.test.ts — the three behavioral cases among its failures.
    - tests/edge/register.test.ts and tests/index.test.ts — two and one behavioral cases respectively.
    - The per-file record builder in each of the edge handler suites.
    - extensions/pi-claude-marketplace/persistence/state-io.ts — the amended schema and `DEFAULT_STATE`.
  </read_first>
  <action>
    Same discipline as task 1: builder first, then read what is still red.

    `tests/transaction/with-state-guard.test.ts` is the sharpest case in this task —
    its six failures assert a literal serialized document including the schemaVersion
    number. Rewrite each expected string by hand from the contract. If two of them
    can be satisfied by the same string, that is a signal one of them is not actually
    asserting what its title claims; note it rather than collapsing them.

    Then run the integration suite. The probe only ran the unit glob, so the
    integration suite's failure count is unmeasured — treat whatever it reports as
    part of this task and drive it to zero with the same builder-first discipline.
    The integration suite writes real state documents through real `saveState` calls,
    so its failures will be the same required-property refusal.

    Finally run the full gate. `npm run check` chains typecheck, lint, both workflow
    linters, fallow, prettier, both corresponding-test gates, the direct-coverage
    negative gate, the unit suite and the integration suite; this is the first point
    in the phase where all of it can be green, and it is this plan's real exit
    criterion.
  </action>
  <verify>
    <automated>node --test "tests/{edge,transaction,bridges}/**/*.test.ts" "tests/index.test.ts"</automated>
    <fails_when>non-zero exit, or the summary reports any `fail` count above 0</fails_when>
    <automated>npm run check</automated>
    <fails_when>non-zero exit at any link of the chain — typecheck diagnostics, an ESLint error, a fallow dead-code/health/dupes finding, a prettier diff, a corresponding-test violation, or any `fail` count above 0 in either suite</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0.
    - `npm test` reports `fail 0`.
    - `npm run test:integration` reports `fail 0`.
    - The six byte-literal cases in `tests/transaction/with-state-guard.test.ts` each assert a distinct expected string, hand-written from the contract.
    - The executor states in the summary the integration suite's failure count BEFORE this task, which the research probe never measured.
  </acceptance_criteria>
  <done>The whole gate chain is green for the first time since plan 04-01's tracer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PROV-02's whole-record proof, observed failing against a planted cascade</name>
  <files>tests/orchestrators/plugin/install-flow.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/orchestrators/plugin/install-cascade.ts — `checkInstalledMember` and its doc comment, which states that an already-installed dependency is checked and never touched, is not in the closure, never becomes a ledger phase, and that no path below can reinstall, re-pin or re-declare it.
    - extensions/pi-claude-marketplace/domain/dependency-closure.ts — the walk's root guard and the `installedKeys` membership test that partitions already-installed members out of the closure. This is the line the plant relaxes.
    - extensions/pi-claude-marketplace/persistence/state-io.ts — `clonePluginRecord`, used to take the before-snapshot.
    - tests/orchestrators/plugin/install-flow.test.ts — the cascade cases near the RESV-05 skip cases, for the seeding helpers and the two-install harness.
  </read_first>
  <behavior>
    - Install `some-other-plugin@mp` directly. Snapshot its whole persisted record.
    - Install `hello@mp`, which declares `some-other-plugin@mp` as a dependency.
    - `some-other-plugin`'s record after the second install is deepStrictEqual to the snapshot — every field, not only provenance.
    - The same case fails when the closure walk's already-installed partition is relaxed so that skipped members re-enter the closure.
  </behavior>
  <action>
    Add one case to `tests/orchestrators/plugin/install-flow.test.ts`, titled with a
    `D-04-01` anchor and naming the ratchet it proves (a direct install stays a direct
    install when a later plugin declares it).

    Snapshot the first install's record with `clonePluginRecord` before the second
    install runs, then `assert.deepStrictEqual` the WHOLE post-install record against
    that snapshot. Do not assert only that provenance reads the direct-install value —
    that assertion would pass against a cascade that rewrote every other field on the
    record, which is exactly the vacuous-pass failure mode this project has already
    shipped once.

    Before the case is committed, PLANT the violation: relax the already-installed
    partition in `domain/dependency-closure.ts` so a skipped member re-enters the
    closure and becomes a ledger phase. Run the case, observe it red, then restore the
    guard byte for byte and confirm `node --test tests/domain/dependency-closure.test.ts`
    and the new case are both green again. Record the red output in the summary —
    PROV-02 is satisfied by an ABSENCE of production code, so the planted red is the
    only evidence that the case can fail at all.

    This task adds NO production code. If it seems to need any, stop: something other
    than the closure partition is touching skipped members, and that is a finding, not
    a fix.
  </action>
  <verify>
    <automated>node --test --test-name-pattern "D-04-01" tests/orchestrators/plugin/install-flow.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports `pass 0` (the pattern matched nothing), or any `fail` count above 0</fails_when>
    <automated>git diff --quiet -- extensions/; echo "extensions-dirty=$?"</automated>
    <fails_when>the printed line is anything other than `extensions-dirty=0` — this task must leave `extensions/` untouched, and a dirty tree means the planted closure-walk change was not restored</fails_when>
  </verify>
  <acceptance_criteria>
    - A case exists whose assertion is a `deepStrictEqual` of the whole record against a `clonePluginRecord` snapshot taken before the second install.
    - The summary quotes the red output observed with the closure partition relaxed.
    - `git diff --quiet -- extensions/` exits 0 after the task, proving the plant was restored.
    - `npm test` reports `fail 0`.
  </acceptance_criteria>
  <done>PROV-02 has a falsifiable whole-record proof, the plant was observed red and restored, and no production code was added.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

See `04-01-PLAN.md` § Artifacts for the full phase table. This plan contributes:

| Symbol / path | Kind |
|---|---|
| A provenance option on each per-file record builder across ~35 test files | test fixtures |
| `D-04-01` whole-record ratchet case in `install-flow.test.ts` | test |

No new production module. No production code at all in task 3.
</artifacts>

<assumption_delta_decision>
**Noun that is now primary:** the install record's `provenance` — a record states how
the plugin got here, and "the user asked for it by name" is one variant.

**Decision: promote.** The fixture sweep is where the promote becomes visible across
the whole suite: every existing case was written about the old implicit singular kind,
and each one now has to name which variant it is about. Defaulting the builders to the
direct-install value is what keeps the demotion truthful — those cases genuinely were
about direct installs — rather than relabelling them wholesale.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> `saveState` revalidation | a hand-built document crosses the same validator a real one does |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | the suite's own assertions | high | mitigate | A sweep of 861 failures is the largest single opportunity in this phase to disarm a gate for convenience. Three named prohibitions bound it: no byte expectation repaired from actual output, no `deepStrictEqual` weakened, no case deleted without a stated reason. The executor reports the before/after count of strict-equality assertions, which makes a silent weakening visible. |
| T-04-08 | Spoofing | PROV-02's proof | high | mitigate | PROV-02 is satisfied by an absence of code, so a test that never fails would read as satisfied while proving nothing — the exact shape of the CR-01 defect this milestone already shipped once. The planted-red requirement plus the `git diff --stat` check on `extensions/` are the controls. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; `04-RESEARCH.md` § Package Legitimacy Audit records the not-applicable verdict. |
</threat_model>

<verification>
- `npm run check` exits 0 — the full chain, which is the phase's standing gate.
- `npm test` and `npm run test:integration` both report `fail 0`.
- `node --test --test-name-pattern "D-04-01" tests/orchestrators/plugin/install-flow.test.ts` passes.
- `git diff --stat -- extensions/` is empty after task 3.
- `pre-commit run --all-files` before the final commit of this plan (no pre-commit hook is installed in this checkout, so it must be run by hand; budget roughly 9 minutes).
</verification>

<success_criteria>
- The tree is green end to end for the first time since the tracer.
- PROV-02's proof is a whole-record comparison and was observed failing against a named plant.
- No gate was weakened, and every non-mechanical amendment is enumerated in the summary.
- The integration suite's pre-task failure count is recorded, closing the one number the research probe never measured.
</success_criteria>

<output>
Create `.planning/phases/04-install-provenance/04-03-SUMMARY.md` when done.
Record: the measured before/after failure counts for each suite, the integration
suite's pre-task count, the strict-equality assertion counts before and after, every
expected OUTPUT that moved with its reason, and the red output from the PROV-02 plant.
</output>
