---
phase: 45-manifest-in-memory-cache
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/domain/manifest-cache.test.ts
autonomous: true
requirements: [CACHE-01, CACHE-02, CACHE-03, CACHE-05]
must_haves:
  truths:
    - "A repeated read of an unchanged manifest runs the injected loader exactly once across N sequential reads (CACHE-01)"
    - "Hits return the loaded value by reference (r1 === r2 === r3) (CACHE-01 / D-03)"
    - "A size change between reads triggers a reload and returns the new value (CACHE-02 success->success)"
    - "A prior negative (failure) entry is discarded on a (mtimeMs,size) change; the next read re-attempts and succeeds (CACHE-02 / CACHE-05 failure->success)"
    - "Repeated reads of an unchanged bad manifest re-throw the SAME Error instance with no second loader call; .message is stable (CACHE-05 / D-03)"
    - "A stat() failure (nonexistent path) is a pure miss: the loader runs on EVERY read (not negative-cached) and the original error code (ENOENT) propagates (D-02)"
    - "A freshly constructed createManifestCache() starts empty: the first load is a miss that runs the loader (CACHE-03)"
  artifacts:
    - path: "tests/domain/manifest-cache.test.ts"
      provides: "Wave 0 behavioral test suite for the manifest cache, using an injected counting loader + mkdtemp/writeFile harness"
      contains: "createManifestCache"
      min_lines: 90
  key_links:
    - from: "tests/domain/manifest-cache.test.ts"
      to: "extensions/pi-claude-marketplace/domain/manifest-cache.ts"
      via: "import { createManifestCache }"
      pattern: "createManifestCache"
---

<objective>
Author the Wave 0 behavioral test file `tests/domain/manifest-cache.test.ts` that
encodes the full locked cache contract (CACHE-01, CACHE-02 both arms, CACHE-03,
CACHE-05, and the D-02 stat-fail path) BEFORE the implementation exists. Each test
constructs a FRESH `createManifestCache(loader)` instance with an INJECTED COUNTING
LOADER (never the module singleton, never a `readFile` mock) and asserts loader-call
counts + by-reference identity + same-Error re-throw.

This is the RED phase of the TDD cycle. The file imports `createManifestCache` from
`extensions/pi-claude-marketplace/domain/manifest-cache.ts`, which does NOT yet exist
(landed in Plan 45-02). The suite is EXPECTED to fail at import resolution until
Plan 45-02 creates the module -- that failing state is the Nyquist scaffold, not a
defect.

Purpose: Make every cache behavior observable through a single clean seam (the
injected counting loader) so Plan 45-02's implementation has a binding, executable
specification to satisfy.
Output: `tests/domain/manifest-cache.test.ts` (new), 7 behavioral tests.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-manifest-in-memory-cache/45-CONTEXT.md
@.planning/phases/45-manifest-in-memory-cache/45-RESEARCH.md
@.planning/phases/45-manifest-in-memory-cache/45-PATTERNS.md
@.planning/phases/45-manifest-in-memory-cache/45-VALIDATION.md

# The harness to mirror (mkdtemp + writeFile + try/finally rm; per-test isolation):
@tests/domain/manifest.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Wave 0 manifest-cache behavioral test suite</name>
  <files>tests/domain/manifest-cache.test.ts</files>
  <read_first>
    - tests/domain/manifest.test.ts (MANDATORY -- copy its `mkdtemp`/`writeFile`/`try/finally rm` per-test isolation harness and the `assert.rejects` convention; the new tests mirror lines 1-15 imports and the :74-91 tmpdir harness)
    - .planning/phases/45-manifest-in-memory-cache/45-PATTERNS.md (the `tests/domain/manifest-cache.test.ts` section -- copy-pasteable CACHE-01 + CACHE-05 test bodies, the injected-counting-loader shape, and the "Additional tests to author" list)
    - .planning/phases/45-manifest-in-memory-cache/45-RESEARCH.md (Code Examples + Sampling/Observability Seam per requirement + Common Pitfalls 1/3/4/5/6 -- the test landmines)
    - .planning/phases/45-manifest-in-memory-cache/45-VALIDATION.md (Per-Task Verification Map + Test Landmines -- the binding requirement→test contract)
  </read_first>
  <behavior>
    - CACHE-01: construct `createManifestCache(async () => { calls++; return value; })`; `await` three sequential `load(p)` calls; assert `calls === 1` and `r1 === r2 === r3` (by-reference identity, D-03). MUST be sequential awaits, NOT `Promise.all` (Pitfall 5 -- no in-flight de-dup by design).
    - CACHE-02 success->success: write a manifest, read it (loader runs once), then rewrite the SAME path with a DIFFERENT byte length (add/remove a plugin entry or pad a field -- a SIZE change, never a same-size rewrite per Pitfall 3/4), read again; assert the loader ran a second time (`calls === 2`) and the returned value reflects the new content.
    - CACHE-02 + CACHE-05 failure->success: first read with a loader that throws stores a negative entry; change `(mtimeMs,size)` via a SIZE change; the next read re-attempts the loader and succeeds (negative entry discarded), returning a valid value.
    - CACHE-05 negative caching: bad file read twice with NO size change; assert loader `calls === 1` (no re-parse on the second read), the two caught errors are the SAME instance (`assert.equal(e1, e2)`), and `(e1).message === (e2).message`. Optionally cross-check `errorMessage(e1) === errorMessage(e2)` to mirror the soft-load consumer.
    - D-02 stat-fail: `load` a NONEXISTENT path twice; assert the injected loader ran on EVERY read (`calls === 2` for two reads -- proving stat-fail is a pure miss, NOT negative-cached) and the propagated error carries the original `code` (e.g. `ENOENT`).
    - CACHE-03 cold start: a freshly constructed `createManifestCache(...)`'s very first `load` is a miss (loader runs once); no cache file or sidecar is written under the tmpdir.
  </behavior>
  <action>
    Create `tests/domain/manifest-cache.test.ts`. Import `assert from "node:assert/strict"`, `{ mkdtemp, rm, writeFile } from "node:fs/promises"`, `os from "node:os"`, `path from "node:path"`, `test from "node:test"`, and `{ createManifestCache } from "../../extensions/pi-claude-marketplace/domain/manifest-cache.ts"`. For the CACHE-05 `.message` cross-check, import `{ errorMessage } from "../../extensions/pi-claude-marketplace/shared/errors.ts"` (matches the soft-load consumer in list.ts). Each test wraps its body in the `mkdtemp(path.join(os.tmpdir(), "pi-cm-..."))` + `try { ... } finally { await rm(tmp, { recursive: true, force: true }); }` harness from `tests/domain/manifest.test.ts`. The observability seam is the INJECTED COUNTING LOADER passed to `createManifestCache` -- a `let calls = 0; const cache = createManifestCache(async (p) => { calls++; ... });` closure. Do NOT mock `readFile`/`JSON.parse`/`MARKETPLACE_VALIDATOR` (Pitfall 1 -- `readFile` is unmockable on the ESM namespace). Do NOT route any test through the module singleton `loadMarketplaceManifest` (Pitfall 6 -- singleton leakage); every test constructs its own fresh instance. Author the 7 tests enumerated in <behavior>, keyed to CACHE-01, CACHE-02 (×2 arms), CACHE-05, D-02 stat-fail, and CACHE-03. Drive every invalidation off a SIZE change (different byte length), never a same-size rewrite (Pitfall 3/4 mtime-resolution flakiness). Keep all `load` calls sequential `await`s.
  </action>
  <verify>
    <automated>node --check tests/domain/manifest-cache.test.ts 2>/dev/null; test -f tests/domain/manifest-cache.test.ts && grep -q "createManifestCache" tests/domain/manifest-cache.test.ts && echo SCAFFOLD_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `tests/domain/manifest-cache.test.ts` exists and imports `createManifestCache` from `../../extensions/pi-claude-marketplace/domain/manifest-cache.ts`.
    - The file defines 7 `test(...)` blocks covering: CACHE-01 (N reads -> 1 loader call + by-reference identity), CACHE-02 success->success (size change -> reload), CACHE-02/CACHE-05 failure->success (negative entry discarded on size change), CACHE-05 (same-instance re-throw + `.message` stable, no re-parse), D-02 stat-fail (loader runs every read, `code` preserved), CACHE-03 (fresh cache cold first read).
    - No test mocks `readFile`, `JSON.parse`, or `MARKETPLACE_VALIDATOR` (grep finds no `t.mock.method` against any of them); the only observability seam is the injected counting loader.
    - No test imports or calls the module singleton `loadMarketplaceManifest` (grep: the only cache import is `createManifestCache`).
    - All `load` calls in the CACHE-01 test are sequential `await`s; no `Promise.all` in that test.
    - Every CACHE-02 invalidation is driven by a different-byte-length rewrite (no assertion that a same-size rewrite changes mtime).
    - EXPECTED at end of this plan: `node --test tests/domain/manifest-cache.test.ts` FAILS at import resolution (module `manifest-cache.ts` does not exist yet) -- this is the intended RED scaffold; Plan 45-02 turns it GREEN.
  </acceptance_criteria>
  <done>
    The test file exists with the 7 enumerated behavioral tests, uses the injected counting loader as its sole observability seam, mirrors the tmpdir harness from `tests/domain/manifest.test.ts`, and is syntactically valid (`node --check` passes). The suite fails only because `domain/manifest-cache.ts` is not yet implemented (Plan 45-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> seam loader | `marketplace.json` bytes cross into the parse/validate path; this phase only adds a `stat` before that boundary -- no new untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | stale-manifest serving after external edit (same-tick, same-size rewrite) | accept | `(mtimeMs,size)` per-read `stat` covers all realistic human-driven edits; the sub-resolution same-size collision is a documented accepted residual risk (hashing is a Non-Goal). This plan's CACHE-02 tests are deliberately driven off a SIZE change to avoid asserting the flaky same-size arm. |
| T-45-02 | Tampering | serving unvalidated content on a hit | mitigate | The negative-cache test (CACHE-05) proves a parse/validate failure is memoized as a thrown Error, never a partial object; the cache memoizes only the post-validate value. Tests lock that a negative entry re-throws (never returns) on hit. |
| T-45-03 | Information disclosure / staleness | stat-failure (ENOENT) negative-caching a stale error | mitigate | The D-02 stat-fail test asserts the loader runs on EVERY read for a nonexistent path (not negative-cached), so a file reappearing is never masked by a cached error. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase -- `node:test`, `node:fs/promises` are Node built-ins; `package.json` is untouched (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `node --check tests/domain/manifest-cache.test.ts` passes (syntactically valid).
- `grep -c "test(" tests/domain/manifest-cache.test.ts` >= 7.
- The suite is RED (fails at `createManifestCache` import) until Plan 45-02 lands -- this is the intended Wave 0 state.
</verification>

<success_criteria>
- `tests/domain/manifest-cache.test.ts` encodes CACHE-01, CACHE-02 (both arms), CACHE-03, CACHE-05, and the D-02 stat-fail contract as 7 sequential, injected-counting-loader-driven tests.
- No `readFile`/`JSON.parse`/`MARKETPLACE_VALIDATOR` mocking; no singleton routing; no same-size-rewrite invalidation assertion.
- The file is syntactically valid and ready to turn GREEN once Plan 45-02 implements `domain/manifest-cache.ts`.
</success_criteria>

<output>
Create `.planning/phases/45-manifest-in-memory-cache/45-01-SUMMARY.md` when done.
</output>
