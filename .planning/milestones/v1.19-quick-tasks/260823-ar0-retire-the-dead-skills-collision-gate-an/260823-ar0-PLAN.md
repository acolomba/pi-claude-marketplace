---
id: 260823-ar0
slug: retire-the-dead-skills-collision-gate-an
description: Retire the dead skills collision gate and deliver bridge warnings on update and reinstall
created: 2026-08-23
mode: quick
local_branch: pr-141
worktree: .worktrees/pr-141
pr: 141
tasks: 4
---

# Quick Task 260823-ar0: retire the dead skills gate, deliver the warnings on update and reinstall

Two independent items, both named in `260821-eln-SUMMARY.md` under "Left open,
deliberately". Both decisions are now settled by the operator and are LOCKED.

1. `assertNoSkillCollisions` cannot fire. Retire it, correct every comment that
   promises a hard error, and amend PRD RN-6 to state the real skills policy.
2. `orchestrators/plugin/update.ts` reads no bridge `result.warnings` at all, so
   every staging and discovery warning is dropped on the update path.
   `reinstall.ts` folds all four bridges flat into one orchestrated-only array.
   Give both the D-141-03 split `install.ts` already runs, so the three
   orchestrators carry ONE policy.

## Locked decisions -- do not revisit, do not offer alternatives

- **Retire the skills gate. Do not make it fire.** Moving it upstream of the
  dedup is the only place it would fire, and firing contradicts first-wins.
  Behavior must not change: this is dead-code removal plus documentation
  catching up to code. Exactly what 260821-eln did for
  `assertNoCommandCollisions`.
- **Fold with the D-141-03 split, not a flat fold.** Skills and commands
  warnings reach the standalone-visible discovery channel; agents and mcp
  warnings ride the orchestrated-only channel.
- **No version bump.** `package.json`, `package-lock.json`, `EXTENSION_VERSION`
  and `sonar.projectVersion` all stay at `0.17.0`. New CHANGELOG copy goes under
  the existing `## [Unreleased]` heading.
- **Do not rewrite history.** No rebase, no amend, no force-push, no branch
  switch. The contributor commit `6bd79fea` stays untouched.

## The mechanism, already measured -- do not re-derive it

**Item 1.** `discoverPluginSkills`
(`extensions/pi-claude-marketplace/bridges/skills/discover.ts`) returns
`Object.freeze([...seenByGenerated.values()])` from a `Map` keyed on
`generatedName`, so the array reaching `assertNoSkillCollisions`
(`bridges/skills/stage.ts:71`, called at `:211`) is duplicate-free by
construction in every case, within-dir included. A within-dir collision already
took the first-wins-plus-warning branch at `discover.ts:99` and `:172`. The gate
cannot fire; the live skills policy is already first-wins plus a warning,
identical to the commands policy D-141-01 locked. The two existing tests of the
gate hand-build the colliding array and call it directly.

**Item 2.** `update.ts` holds the same `handles.{skills,commands,agents,mcp}.result`
shape `reinstall.ts` uses (four `prepareStage*` calls in `prepareUpdateHandles`,
`update.ts:1223-1268`; `applyPerBridgeResources` at `:1705` already reads
`handles.<bridge>.result.recorded`). It never reads `.warnings`.
`reinstall.ts::collectStagingWarnings` (`:1468`, used at `:999`) concatenates all
four `.warnings` arrays into `bridgeWarnings`, which the `render === "none"` arm
turns into outcome `notes` and the standalone arm drops entirely.

`install.ts` is the reference: the skills and commands phases push
`prep.result.warnings` into `c.discoveryWarnings` (`:940`, `:983`); the agents and
mcp phases push into `c.bridgeWarnings` (`:1029`, `:1129`).
`collectPostCommitWarnings` (`:1504`) pushes `discoveryWarnings` unconditionally
and gates every hygiene warning on `orchestrated`, so in standalone mode the
returned array is discovery warnings and nothing else;
`surfaceStandaloneDiscoveryWarnings` (`:1583`) then renders that whole array
through `notifyDiagnostic` after the install row (`:2418`), with
`redactAbsolutePaths` applied per NFR-9.

The D-141-03 rationale in `.planning/PROJECT.md:478` is why the split falls where
it does: the agents bridge aggregates index corruptions, per-agent conversion
notes and D-07 skips onto one field that cannot be split at the call site, so its
whole array joins the hygiene channel -- folded rather than dropped, but
orchestrated-only.

## Ground rules for every task

All work happens in the worktree at
`/home/acolomba/pi-claude-marketplace/.worktrees/pr-141` on branch `pr-141`,
which is checked out and clean. Never commit to `main`. Every path below is
relative to that worktree.

**The shell is fish and does NOT word-split variables.** Any multi-path command
must list its paths explicitly or run under `bash -c`.

**Commit protocol, per task, in this order:**

1. `pre-commit run --all-files` and get it clean BEFORE attempting `git commit`.
   CI runs `--all-files`; a scoped `--files` run hides pre-existing violations.
   If a hook rewrites a file, restage and re-run until clean, then fold the
   rewrite into THIS task's commit. Never recover after the fact with `--amend`.
2. trufflehog's git-mode hook is structurally broken in a linked worktree
   (`.git` is a file, not a directory). Run the filesystem scan over exactly the
   paths being committed:
   ```bash
   TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)
   "$TH" filesystem <the paths of this commit> --results=verified,unknown --fail
   ```
   Confirm `verified_secrets: 0` and `unverified_secrets: 0`.
3. Only then: `SKIP=trufflehog git commit -F <message file>`. Never
   `--no-verify`. Never extend `SKIP=` to another hook. Backticks inside a
   `-m` string EXECUTE in fish -- write the message to a file and use `-F`.
4. `git status` after the commit; a prettier or mdformat hook can rewrite a file
   and still let the commit succeed. Fix any rewrite in a FOLLOW-UP commit.

**Comment policy** (`.claude/rules/typescript-comments.md`): keep decision and
requirement IDs (`D-141-04`, `RN-6`, `NFR-9`), never write phase/plan/wave
references.

**The gate is `npm run check`** (typecheck, lint, fallow, format:check, test,
test:integration) and it MUST end exit 0. NEVER pipe it -- a piped run reports
the pipe's status and has verified nothing. Redirect to a file and read the real
exit code:
```bash
npm run check > /tmp/check.log 2>&1; echo "EXIT=$status"   # fish
```
`node_modules` is present in the worktree as a symlink to `../../node_modules`.
Leave it in place.

Both ESLint (`sonarjs/cognitive-complexity: 15`) and fallow
(`maxCognitive: 15`, `maxCyclomatic: 20`, `maxUnitSize: 60`) must pass
independently; they disagree on a given function's score. `fallow dupes` has
`threshold: 3` and `sonarjs/no-identical-functions` is an error, which is why
Task 2 hoists a shared helper instead of copying one into two orchestrators.

---

## Task 1 -- retire the skills collision gate

**Files:**
`extensions/pi-claude-marketplace/bridges/skills/stage.ts`,
`extensions/pi-claude-marketplace/bridges/skills/discover.ts`,
`tests/bridges/skills/stage.test.ts`,
`tests/bridges/skills/discover.test.ts`,
`docs/prd/pi-claude-marketplace-prd.md`,
`.planning/PROJECT.md`

**Do:**

1. Delete `assertNoSkillCollisions` (`stage.ts:65-92`, including its doc
   comment) and its call site (`stage.ts:211`). It is NOT re-exported from
   `bridges/skills/index.ts`, so no barrel change is needed -- confirm that with
   a grep rather than assuming it.

2. Delete the hand-built-array test at `tests/bridges/skills/stage.test.ts:304`
   ("RN-6 assertNoSkillCollisions throws with both source names when two skills
   elide to same generated") and drop `assertNoSkillCollisions` from that file's
   import block (`stage.test.ts:10`). Drop the `DiscoveredSkill` type import too
   if that test was its only consumer -- `noUnusedLocals` will say.

3. Correct EVERY comment that promises a hard error that cannot happen. Verify
   the site list with
   `grep -rn "assertNoSkillCollisions" extensions/ tests/ docs/` and
   `grep -rni "hard error" extensions/pi-claude-marketplace/bridges/skills/ tests/bridges/skills/`
   before editing, and fix whatever those two greps return. Known sites:
   - `stage.ts:13-14` (the `RN-6:` bullet in the file header)
   - `stage.ts:203-206` (the `D-07:` comment above the `discoverPluginSkills`
     call)
   - `discover.ts:11-14` (the `D-07 (COMP-01)` bullet in the file header)
   - `discover.ts:132-135` (the comment above `seenByGenerated`)
   - `discover.ts:170-172` (the comment above the `seenByGenerated.has` branch)
   - `tests/bridges/skills/discover.test.ts:209` (the "those are HARD errors"
     line inside the D-07 dedup test)

   Each replacement says the same true thing: a generated-name collision is
   first-wins plus a warning, and it does not matter whether the two sources sit
   in one declared `componentPaths.skills` entry or in two. Cite `D-141-04`.
   `assertSafeName` still guards the source name and that claim stays.

4. Add the regression test that pins the retired behavior, in
   `tests/bridges/skills/discover.test.ts` next to the existing
   "D-07 discoverPluginSkills first-wins dedup across array elements" test and
   built the same way (`mkdtemp`, a `ResolvedPluginInstallable` literal,
   `cleanupStaging` in `finally`). ONE declared `componentPaths.skills` entry
   holding two skill dirs that elide to the same generated name: for plugin
   `acme`, dirs `acme-foo/` and `foo/` both generate `acme-foo`
   (`domain/name.ts::generatedSkillName` elides the `acme-` prefix then
   re-prefixes). Discovery sorts by `name.localeCompare`, so `acme-foo` wins.
   Assert: exactly one discovered skill, its `sourceName` is `acme-foo`, exactly
   one warning, the warning names `"acme-foo"` and contains `ignoring
   duplicate`, and the call does NOT throw. Title it with `D-141-04`.

   Do NOT assert on the warning's `already produced by an earlier
   componentPaths.skills entry` clause. That sentence is inaccurate for a
   within-entry collision, it is pre-existing on both the skills and commands
   bridges, 260821-eln recorded it as out of scope, and it stays out of scope
   here -- asserting on it would lock it in.

5. Amend PRD RN-6 (`docs/prd/pi-claude-marketplace-prd.md:582`) so it states the
   real skills policy. Read what 260821-eln's amendment did for commands in that
   same row and match its shape and voice: the row currently opens by requiring
   skills to throw, then appends "Commands do NOT throw: ..." as the exception.
   After this task neither kind throws, so restate the row with first-wins plus
   a warning as the rule, cite `D-141-04` for skills the way the row already
   cites `D-141-01` for commands, and keep both the "A source name equal to the
   plugin name becomes skill `foo` and command `foo:foo`" sentence and the
   "(For agents, see AG-12.)" pointer. Agents are out of RN-6's scope and
   `assertNoAgentCollisions` is NOT touched by this task -- say nothing about
   agents beyond that existing pointer.
   Grep the PRD for any other RN-6 or skills-collision claim (Appendix B carries
   parallel stripping-rule rows) and fix whatever contradicts the new text.
   mdformat will re-pad the table; let it, and fold its rewrite into this
   commit.

6. Add the `D-141-04` row to `## Key Decisions` in `.planning/PROJECT.md`
   (line 418 onward), directly after the `D-141-03` row at line 478 and in the
   same three-column format (`| **D-NNN: statement** (PR #141, 2026-08-23): ... | rationale | -- Locked |`).
   The decision: the within-plugin skills collision gate is retired rather than
   moved upstream, and first-wins plus a warning is the skills policy, as the
   code has always run it. The rationale must carry why this gets an ID at all
   when the commands retirement did not: commands had D-141-01 to point at, and
   skills has nothing, so a future reader asking why the PRD's documented hard
   error disappeared needs a rationale surface that a one-sentence requirement
   row cannot be. Also record that moving the gate upstream of the dedup is the
   only place it would fire and that firing contradicts first-wins.

**Verify:** `npx tsc --noEmit` clean, then
`node --test tests/bridges/skills/discover.test.ts tests/bridges/skills/stage.test.ts`
passes with the new case present and the deleted case gone.

Plant the mutation before you trust the new test: revert the `continue` in
`discover.ts`'s `seenByGenerated.has(generatedName)` branch so the second source
overwrites the first, confirm the new case fails, then restore it. A test that
cannot fail has verified nothing.

**Done:** `grep -rn "assertNoSkillCollisions" extensions/ tests/ docs/` returns
nothing. No comment or test title in `bridges/skills/` or `tests/bridges/skills/`
claims a hard error for a generated-name collision. PRD RN-6 and PROJECT.md
D-141-04 both state first-wins plus a warning. Committed as one
`docs`-or-`refactor`-typed Conventional Commit under the protocol above.

---

## Task 2 -- one warning policy: the shared surface, and reinstall

**Files:**
`extensions/pi-claude-marketplace/orchestrators/plugin/shared.ts`,
`extensions/pi-claude-marketplace/orchestrators/plugin/install.ts`,
`extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts`,
`tests/orchestrators/plugin/reinstall.test.ts`,
`.planning/PROJECT.md`

**Do:**

1. Hoist two helpers into `orchestrators/plugin/shared.ts`. That file is the
   documented home for cross-verb plugin-orchestrator helpers, it already
   imports `notify` from `shared/notify.ts` and already emits a notification
   (`emitMarketplaceNotAdded`, `:1142`), and it is listed in
   `sonar.cpd.exclusions`. Its header forbids imports from `bridges/`, so both
   helpers take plain `readonly string[]` and never name a bridge type.

   - `splitStagingWarnings({ skills, commands, agents, mcp })` returning
     `{ discovery, bridge }`: skills and commands concatenate into `discovery`,
     agents and mcp into `bridge`. This is the D-141-03 policy in one place, so
     update and reinstall cannot drift apart and `sonarjs/no-identical-functions`
     plus `fallow dupes` stay quiet.
   - `surfaceDiscoveryWarnings(ctx, { plugin, verb, warnings })` where `verb` is
     the closed union `"installed" | "updated" | "reinstalled"`. It returns
     early on an empty array, maps each warning through `redactAbsolutePaths`
     (NFR-9 -- a discovery warning embeds the absolute component directory it
     walked), builds the header, and calls `notifyDiagnostic`.

     The header MUST reproduce `install.ts:1583`'s existing text byte-identically
     for the `installed` verb, singular and plural both:
     `Plugin "<name>" installed; 1 declared component was skipped.` and
     `Plugin "<name>" installed; <N> declared components were skipped.`
     The existing install tests assert on that string and must stay green
     without being edited.

2. Delete the private `surfaceStandaloneDiscoveryWarnings` from `install.ts` and
   point its one call site (`:2418`) at the shared helper with
   `verb: "installed"`. Behavior unchanged. Do NOT restructure install's ledger
   phases -- each phase pushes its own warnings inline, which is already the
   split, so `splitStagingWarnings` has no call site in `install.ts`. Keep the
   doc comment on `collectPostCommitWarnings` (`:1485-1503`) accurate: it now
   points at the shared helper by name.

3. Give `reinstall.ts` the split. It currently folds all four bridges flat
   through `collectStagingWarnings` (`:1468`, used at `:999`).
   - Replace `collectStagingWarnings` with a call to `splitStagingWarnings` over
     the four `handles.<bridge>.result.warnings` arrays.
   - Add `discoveryWarnings: readonly string[]` alongside the existing
     `bridgeWarnings` on the `LockedSuccess` interface (`:256`), mirroring
     install's two-array `InstallCtx` shape. `bridgeWarnings` keeps the agents
     and mcp halves plus the existing `finalizeReplacements` leak strings.
   - Orchestrated arm (`render === "none"`, `:325-328`): notes become
     `[...discoveryWarnings, ...bridgeWarnings, ...maintenanceWarnings]`, all
     mapped through the existing `warning: ` prefix. Discovery warnings reach
     BOTH modes, exactly as install's `collectPostCommitWarnings` pushes them
     unconditionally.
   - Standalone arm: after the success row's `notifyWithContext` call, call
     `surfaceDiscoveryWarnings(ctx, { plugin, verb: "reinstalled", warnings: discoveryWarnings })`.
     After the row, never before -- the user reads the row, then the detail.
     `bridgeWarnings` and `maintenanceWarnings` stay dropped in standalone mode;
     that is D-19-01 and it is unchanged.
   - Correct the file header (`:17-22`), which currently states flatly that
     post-success soft warnings are not surfaced. Discovery warnings now are.
     Cite `D-141-03` and `D-141-05`.

4. Add two tests to `tests/orchestrators/plugin/reinstall.test.ts`, modelled on
   the pair `f4db2b0c` added at `tests/orchestrators/plugin/install.test.ts:6773`
   and `:6808`. Reuse that file's own `makeCtx` / `withHermeticHome` /
   `seedMarketplace` / `writePluginTree` helpers -- do not import test helpers
   across orchestrator test files.
   - **Standalone surfaces a skills or commands discovery warning.** Seed a
     plugin whose skills directory holds `acme-foo/SKILL.md` and `foo/SKILL.md`
     (both generate `acme-foo`; see Task 1), reinstall it on the default
     standalone path, and assert the notification count is the reinstall row plus
     one `warning`-severity diagnostic whose message matches
     `/1 declared component was skipped/` and names `"acme-foo"`. Assert the
     absolute plugin root does NOT appear in the message (NFR-9).
   - **Agents and mcp warnings stay on the orchestrated channel.** The cheapest
     agents warning to seed is an agent `.md` whose frontmatter carries no
     `description`, which `bridges/agents/convert.ts:473` reports as
     `source description was missing or empty -- using fallback`. Assert that
     with `render: "none"` the returned outcome's `notes` carries it, and that
     on the standalone path it appears in NO notification.

5. Add the `D-141-05` row to `## Key Decisions` in `.planning/PROJECT.md`,
   directly after the new `D-141-04` row, in the same format. The decision: the
   discovery-versus-hygiene split is the policy for every verb that stages
   bridges, not an install-local arrangement. The rationale must say what the
   ID is doing that D-141-03 does not already do -- D-141-03 states what the
   split is and is written about `install.ts`; this one states that install,
   update and reinstall run ONE policy, which they did not before (update read
   no `.warnings` at all, reinstall folded all four flat into an
   orchestrated-only array), so the comments in `update.ts` and `reinstall.ts`
   have an anchor that is not scoped to a verb they are not.

**Verify:** `npx tsc --noEmit` clean, then
`node --test tests/orchestrators/plugin/reinstall.test.ts tests/orchestrators/plugin/install.test.ts`
passes -- install unedited and still green is the proof the hoist changed no
behavior.

**Done:** `splitStagingWarnings` and `surfaceDiscoveryWarnings` live in
`shared.ts` and are the only copies. `install.ts` has no private surface helper.
A standalone reinstall emits the row plus one diagnostic; an orchestrated one
emits none and carries every warning on `notes`. Committed under the protocol
above.

---

## Task 3 -- deliver the warnings on the update path

**Files:**
`extensions/pi-claude-marketplace/orchestrators/types.ts`,
`extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`,
`tests/orchestrators/plugin/update.test.ts`

**Do:**

1. Read `orchestrators/types.ts` first and confirm, do not assume, that no
   consumer of the `updated` partition reads `notes`. `notes` is required on
   `PluginUpdateSkippedOutcome` (`:76`) and `PluginUpdateFailedOutcome` (`:81`)
   and is narrowed by `narrowSkipReasons` (`update.ts:2410`) and
   `narrowFailReasons` (`:2511`) -- both on their own partition's arm. If a
   third reader exists, stop and report it rather than widening the type.

   Then add `readonly notes?: readonly string[]` to
   `PluginUpdateUpdatedOutcome`, documented as the warning carrier and mirroring
   the optional `notes?` that `ReinstallOutcomeBase` (`:71`) already carries.
   The `updated` partition needs it because the warnings originate inside the
   three-phase runner while the direct path's rows are not rendered until
   `renderUpdateCascadeAndNotify` (`update.ts:451`) at the very end of
   `updatePlugins`; firing a diagnostic from inside the runner would print the
   detail BEFORE the row it explains. The outcome is the only carrier that
   crosses that boundary. Do not add a second field.

2. In `update.ts`, read the four `handles.<bridge>.result.warnings` arrays after
   `prepareUpdateHandles` succeeds and run them through
   `splitStagingWarnings` from `./shared.ts`. Put the result on the `updated`
   outcome as

   ```
   notes = [...discovery, ...(args.cascade ? bridge : [])]
   ```

   which is install's rule exactly: discovery warnings reach both modes, the
   hygiene half is orchestrated-only. Omit `notes` entirely when the array is
   empty, so a clean update's outcome shape is unchanged.

   `ThreePhaseArgs` already carries `cascade: boolean` (`:674`) plus the
   direct-path-only `ctx` (`:682`) and `pi` (`:689`) -- that is the seam, no new
   plumbing flag. Keep whatever function you add small; `prepareUpdateHandles`
   and the finalize window are already near the fallow `maxCognitive: 15` and
   `maxUnitSize: 60` ceilings, so put the fold in its own named helper rather
   than growing an existing one.

3. In `updatePlugins`, after `renderUpdateCascadeAndNotify` (`:451`), walk the
   collected `outcomes[]`, and for each `partition === "updated"` outcome with a
   non-empty `notes`, call
   `surfaceDiscoveryWarnings(ctx, { plugin: outcome.name, verb: "updated", warnings: outcome.notes })`.
   On the direct path `cascade` is `false`, so `notes` holds discovery warnings
   and nothing else -- the same property install's standalone arm relies on.
   Do not call it from the `renderUpdateCascadeIfAny` early-abort path at `:444`;
   an aborted batch has a failure to explain and a skipped-component note is
   noise against it.

   `updateSinglePlugin` (`:557`) is the cascade entrypoint and emits nothing of
   its own -- leave its notification behavior alone; its outcome carries both
   halves on `notes` for the marketplace orchestrator.

4. Correct the `update.ts` file header and the `prepareUpdateHandles` comment to
   say the warnings are now read and where each half goes. Cite `D-141-03` and
   `D-141-05`.

5. Add two tests to `tests/orchestrators/plugin/update.test.ts`, mirroring the
   pair from Task 2 and reusing that file's own `makeCtx` / `withHermeticHome` /
   `seedPathMarketplace` / `rewriteManifest` helpers. An update needs a version
   change to reach the swap, so seed the colliding skill dirs (`acme-foo/` and
   `foo/`, per Task 1) into the NEW version's plugin tree.
   - **A standalone `updatePlugins` run surfaces the skills discovery warning**
     as a `warning`-severity notification AFTER the update row, matching
     `/1 declared component was skipped/` and naming `"acme-foo"`, with no
     absolute path in the message.
   - **An `updateSinglePlugin` cascade run emits no notification of its own and
     carries the warning on the returned outcome's `notes`**, together with an
     agents warning (an agent `.md` with no `description`) that must NOT reach
     any standalone notification.

**Verify:** `npx tsc --noEmit` clean, then
`node --test tests/orchestrators/plugin/update.test.ts` passes.

Plant the mutation: drop the `notes` assignment from the updated outcome and
confirm both new cases fail, then restore it.

**Done:** `grep -n "result.warnings" extensions/pi-claude-marketplace/orchestrators/plugin/update.ts`
returns the fold. A standalone update prints the skills and commands warnings; a
cascade update carries all four halves on `notes` and prints nothing. Committed
under the protocol above.

---

## Task 4 -- CHANGELOG and the full gate

**Files:** `CHANGELOG.md`

**Do:**

1. **Amend the existing `## [Unreleased]` bullet, do not add a new one.** The
   bullet that begins "Discovery warnings now reach you." already describes this
   exact change and ends "A standalone `install` now prints the command and
   skill warnings under the install row; the agent warnings reach the cascade
   channel that `/reload` and `import` already use." Widen the standalone clause
   so it names `install`, `update` and `reinstall`. The changelog rule forbids
   spreading one change across several bullets, and until `0.17.0` ships this is
   one unreleased change.

   Keep the amended bullet at 40 words or fewer, one sentence per idea, 25 words
   or fewer per sentence, active voice, simple tense, no internal function names.
   Apply the `simple-english` and `humanizer` skills to whatever you rewrite.

2. **Add NO entry for the retired skills gate.** It changes no user-visible
   behavior -- the gate never fired. A dead-code removal and a documentation
   correction are not changelog events, and the rule forbids explaining what the
   old code did internally. If you find yourself writing one, that is the signal
   to stop.

3. Confirm no version bump crept in:
   `git diff main...HEAD -- package.json package-lock.json sonar-project.properties`
   shows no version change, and `EXTENSION_VERSION` is untouched.

4. Run the full gate and read the real exit code:
   ```bash
   npm run check > /tmp/260823-ar0-check.log 2>&1; echo "EXIT=$status"   # fish
   ```
   Never pipe it. If it is not 0, fix the cause and re-run; do not report a
   number you read off a truncated tail.

**Verify:** `npm run check` exit 0, with the unit and integration test counts
read out of the log. `git status` clean after the commit.

**Done:** one amended CHANGELOG bullet, gate green, working tree clean, five or
six commits on `pr-141` with `6bd79fea` unchanged at the base.

---

## Out of scope -- do not drift into these

- **`assertNoAgentCollisions`.** Not measured, not settled by a decision, and
  RN-6 explicitly points agents at AG-12. Leave the agents bridge alone except
  for reading its `result.warnings`.
- **The `duplicateWarning` "already produced by an earlier componentPaths entry"
  sentence.** Inaccurate for a within-entry collision on both the skills and the
  commands bridge, pre-existing, recorded by 260821-eln as a later pass.
- **`discover-names.ts`.** It still discards its copy of the discovery warnings
  on purpose: every caller walks the same directories again during staging, and
  that pass is the one that reports. Folding here too would print each warning
  twice.
- **Any version bump, any history rewrite, any branch switch, any push.**

## Verification summary

| Claim | How it is proven |
| --- | --- |
| The skills gate is gone and nothing claims otherwise | `grep -rn "assertNoSkillCollisions" extensions/ tests/ docs/` returns nothing |
| Retiring it changed no behavior | the new `D-141-04` discover test passes, and it fails under the planted first-wins mutation |
| The hoist changed no install behavior | `tests/orchestrators/plugin/install.test.ts` passes unedited |
| A standalone reinstall surfaces a skills or commands warning | new reinstall test: row plus one `warning` diagnostic |
| A standalone update surfaces one | new update test: row plus one `warning` diagnostic |
| Agents and mcp warnings stay orchestrated-only | both new orchestrated tests: present on `notes`, absent from every notification |
| No absolute path leaks | each standalone test asserts the plugin root is not in the message (NFR-9) |
| The whole thing is green | `npm run check` exit 0, redirected to a file, not piped |
