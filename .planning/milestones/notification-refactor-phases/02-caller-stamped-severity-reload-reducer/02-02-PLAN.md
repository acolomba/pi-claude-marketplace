---
phase: 02-caller-stamped-severity-reload-reducer
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-context.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - tests/architecture/catalog-uat.test.ts
  - tests/shared/notify-v2.test.ts
  - tests/shared/notify-inert-fields.test.ts
autonomous: true
requirements: [RLD-04, RLD-05]
must_haves:
  truths:
    - "catalog-uat EXPECTED output blocks stay BYTE-IDENTICAL to docs/output-catalog.md (D-01 cross-cutting invariant — only test INPUT literals change, never the byte-compared expected blocks)"
    - "Omitting severity OR needsReload on a transition message literal remains a tsc compile error TS2741 (GATE-01/D-04 cross-cutting invariant)"
    - "The `present` plugin status no longer exists; the list-surface installed inventory row stamps needsReload:false"
    - "The `disable-cascade` cascade kind no longer exists; the disable command stamps needsReload:true on its realized rows directly"
    - "npm run check is green at every task commit"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "PluginPresentMessage + present status token + disable-cascade kind removed"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/list.ts"
      provides: "installedRowMessage emits status:installed + needsReload:false (was present)"
      contains: "needsReload: false"
  key_links:
    - from: "orchestrators/plugin/list.ts installedRowMessage"
      to: "PluginInstalledMessage (status:installed, needsReload:false)"
      via: "status-token collapse + inventory needsReload suppression"
      pattern: "status: \"installed\""
    - from: "orchestrators/plugin/enable-disable.ts disable dispatch"
      to: "notifyWithContext without the disable-cascade 5th arg"
      via: "needsReload:true stamped on the realized disabled row"
      pattern: "needsReload: true"
---

<objective>
Collapse the `present` plugin status into `installed` (RLD-04) — its only role was
list-surface reload suppression, now carried by `needsReload: false` on the
inventory `installed` row — and remove the `disable-cascade` cascade kind (RLD-05),
since Plan 02-01 made the disable command stamp `needsReload: true` on its realized
rows directly. The test fixtures that reference the deleted `present` type and
`disable-cascade` kind are migrated in lockstep so `npm run check` stays green;
EXPECTED rendered bytes stay byte-identical (D-01). The catalog markdown's
`present`→`installed` grammar collapse (the prose in docs/output-catalog.md) is
Phase 3 (OUT-08), not here.

Purpose: eliminate the two structural straddles (`present` status, `disable-cascade`
kind) now that caller-stamped `needsReload` makes them redundant (RLD-04/RLD-05).
Output: one fewer plugin status token, one fewer cascade kind, all consumers + test
input fixtures updated, EXPECTED output byte-identical.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-PATTERNS.md
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-01-PLAN.md
@.claude/rules/typescript-comments.md
</context>

<ordering_note>
catalog-uat reads `docs/output-catalog.md` for the EXPECTED fenced blocks and pairs
them with INLINE programmatic `NotificationMessage` fixtures (the FIXTURES map). The
inline input fixtures use `status: "present"` (~16 entries, catalog-uat ~:265-490)
and `kind: "disable-cascade"` (~:2292). Deleting the `present` type / `disable-
cascade` kind in source makes those FIXTURE literals fail to typecheck — so each
task migrates its fixtures (input `status`/`kind` literals + add the stamps) IN THE
SAME TASK as the source deletion, while leaving the byte-compared `expected` blocks
in docs/output-catalog.md untouched (RESEARCH Q2 — input-literal edits are in-scope
and are NOT a D-01 violation; the catalog markdown grammar collapse is Phase 3).
</ordering_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Collapse `present` into `installed` + migrate present fixtures (D-08 / RLD-04)</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/list.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts,
    extensions/pi-claude-marketplace/edge/handlers/tools.ts,
    tests/architecture/catalog-uat.test.ts,
    tests/shared/notify-v2.test.ts,
    tests/shared/notify-inert-fields.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (PLUGIN_STATUSES ~:461-478 incl. "present" ~:472, PluginPresentMessage ~:764-771, the PluginNotificationMessage union ~:890, the installed renderer arm ~:757-759 confirming byte-identity, the present-render arm)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts (installedRowMessage ~:234-294 returning status:"present" ~:288, its return type ~:240, the UAT G-21-01 note ~:665)
    - extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts (PluginPresentMessage import ~:15 + render-map arm ~:54)
    - extensions/pi-claude-marketplace/edge/handlers/tools.ts (the 3 `case "present":` arms ~:164/314/364 — UAT G-21-01)
    - tests/architecture/catalog-uat.test.ts (the ~16 inline `status: "present"` fixture literals ~:265-490 — INPUT, not the byte-compared expected blocks)
    - tests/shared/notify-v2.test.ts (the `present` fixtures ~:897-981) and tests/shared/notify-inert-fields.test.ts (the `present` fixture ~:102)
    - .planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-PATTERNS.md (present-collapse section + Pitfall 2 orphan-rewake brace + closed-set tuple idiom)
    - .planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-RESEARCH.md (RLD-04 row, Runtime State Inventory "present" 50+ refs, Pitfall 2, Q2 fixture-edit scope)
  </read_first>
  <action>
    Remove the `present` plugin status entirely and route its list-surface role
    through `installed` + `needsReload: false`:
    - In `notify.ts`: delete `"present"` from `PLUGIN_STATUSES` (~:472), delete the
      `PluginPresentMessage` interface (~:764-771), remove it from the
      `PluginNotificationMessage` union (~:890). Keep the `installed` renderer arm
      (already byte-identical to the present arm per ~:757-759); fold the present
      render arm into it (produces `ICON_INSTALLED + "(installed)"`, no reasons
      brace). Keep every `assertNever` exhaustiveness tail intact.
    - In `list.ts` `installedRowMessage` (~:234-294): change `status: "present"` →
      `status: "installed"`, add `severity: "info", needsReload: false` (the
      `needsReload:false` IS the old present reload-suppression), update the return
      type (~:240) `PluginPresentMessage | ...` → `PluginInstalledMessage | ...`. Keep
      `reasons` OMITTED (Pitfall 2 — an `installed` row carrying reasons leaks the
      orphan-rewake brace onto a steady-state inventory row).
    - In `list.messaging.ts`: drop the `PluginPresentMessage` import (~:15) and its
      render-map arm (~:54); the command's status set narrows (Phase-1 missing-arm
      compile check confirms totality).
    - In `edge/handlers/tools.ts`: merge each `case "present":` arm (~:164/314/364)
      into the matching `case "installed":` arm (they already share behavior per the
      UAT G-21-01 notes); remove the dead `present` arms.
    - MIGRATE the test INPUT fixtures (so the test files typecheck against the deleted
      type — leave EXPECTED byte blocks untouched): in `catalog-uat.test.ts` change
      every inline `status: "present"` fixture literal (~:265-490) to `status:
      "installed"` + `severity: "info", needsReload: false`; in `notify-v2.test.ts`
      (~:897-981) and `notify-inert-fields.test.ts` (~:102) migrate the `present`
      fixtures the same way and update their assertions/comments to the
      `installed`+`needsReload:false` model (titles anchored `RLD-04`/`D-08`).

    Anchor edited comments with `RLD-04`/`D-08` (never `Phase 2`/`Wave N`). Rendered
    bytes preserved (identical renderer arm + `reasons` omitted) — confirm catalog-uat
    EXPECTED blocks stay byte-identical and `git diff docs/output-catalog.md` is empty.
  </action>
  <verify>
    <automated>cd extensions/pi-claude-marketplace && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `node --test tests/architecture/catalog-uat.test.ts` passes (EXPECTED output byte-identical — D-01); `git diff docs/output-catalog.md` empty
    - `grep -rn '"present"\|PluginPresentMessage' extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/orchestrators/plugin/list.ts extensions/pi-claude-marketplace/orchestrators/plugin/list.messaging.ts extensions/pi-claude-marketplace/edge/handlers/tools.ts | grep -v '^[^:]*: *[0-9]*: *[*/]'` returns no code lines (the status token is gone from source; comments may remain)
    - `grep -rn '"present"' tests/architecture/catalog-uat.test.ts` returns no fixture input lines (all migrated to `installed`)
    - `list.ts` installedRowMessage returns `status: "installed"` with `needsReload: false` and NO `reasons` field (verify by reading)
  </acceptance_criteria>
  <done>The `present` status token is gone from source and test fixtures; the list inventory row is `installed` + `needsReload:false`; EXPECTED bytes byte-identical.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the `disable-cascade` cascade kind + migrate UAT-03 fixtures (D-07 / RLD-05)</name>
  <files>
    extensions/pi-claude-marketplace/shared/notify.ts,
    extensions/pi-claude-marketplace/shared/notify-context.ts,
    extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts,
    tests/architecture/catalog-uat.test.ts,
    tests/shared/notify-v2.test.ts
  </files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (the `kind?` union ~:1057 incl. "disable-cascade", the `case "disable-cascade":` switch arm ~:3147-3148; confirm Plan 02-01 already removed the `disabledIsTransition` straddle from shouldEmitReloadHint)
    - extensions/pi-claude-marketplace/shared/notify-context.ts (notifyWithContext `kind?: "cascade" | "disable-cascade"` ~:137; widening cast ~:145 stays)
    - extensions/pi-claude-marketplace/orchestrators/plugin/enable-disable.ts (the disable dispatch passing 5th arg "disable-cascade" ~:863, realized disabled row ~:967 stamped needsReload:true in Plan 02-01)
    - tests/architecture/catalog-uat.test.ts (the disable fixture with `kind: "disable-cascade"` ~:2292) and tests/shared/notify-v2.test.ts (the 2 UAT-03 tests ~:3850/3884 with `kind:"disable-cascade"`)
    - .planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-PATTERNS.md (disable-cascade kind removal section + assertNever tail rule)
    - .planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-RESEARCH.md (RLD-05 row, Runtime State Inventory disable-cascade ~25 refs, Pitfall 4)
  </read_first>
  <action>
    Remove the `disable-cascade` cascade kind now that disable stamps `needsReload:
    true` on its realized rows directly (Plan 02-01):
    - In `notify.ts`: drop `"disable-cascade"` from the cascade `kind?` union (~:1057)
      → `kind?: "cascade"`; delete the `case "disable-cascade":` arm in the `notify()`
      exhaustiveness switch (~:3147-3148) — its body fell through to the cascade path
      handled by the remaining `"cascade"`/`undefined` arms. Keep `default:
      assertNever`.
    - In `notify-context.ts`: change the `notifyWithContext` `kind?` param union
      (~:137) `"cascade" | "disable-cascade"` → `"cascade"`. Leave the post-check
      widening cast (~:145).
    - In `enable-disable.ts`: drop the 5th `"disable-cascade"` argument from the
      disable `notifyWithContext` call (~:863). The realized `disabled` row already
      stamps `needsReload: true` (Plan 02-01), so the OR-reduce trailer still fires for
      a fresh disable; list/info `disabled` inventory stamps `false`.
    - MIGRATE the UAT-03 test fixtures: in `catalog-uat.test.ts` drop `kind:
      "disable-cascade"` from the disable fixture (~:2292) and ensure the disabled row
      carries `needsReload: true` (the trailer now comes from the stamp); in
      `notify-v2.test.ts` update the 2 UAT-03 tests (~:3850/3884) to drop the kind and
      assert the trailer is driven by `needsReload` (the second test — kind-without-
      trigger-row — becomes "a `disabled` inventory row with `needsReload:false` stays
      trailer-free"). Anchor titles `RLD-05`/`UAT-03`. Leave EXPECTED byte blocks in
      docs/output-catalog.md untouched.

    Anchor edited comments with `RLD-05`/`D-07`/`UAT-03` (never `Phase 2`/`Wave N`).
    Bytes preserved — confirm catalog-uat EXPECTED blocks byte-identical, `git diff
    docs/output-catalog.md` empty.
  </action>
  <verify>
    <automated>cd extensions/pi-claude-marketplace && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0
    - `grep -rn "disable-cascade" extensions/pi-claude-marketplace/shared extensions/pi-claude-marketplace/orchestrators tests | grep -v '^[^:]*: *[0-9]*: *[*/]'` returns no code/fixture lines (the kind literal is absent from unions, switch arms, call args, and test fixtures; only historical-rationale comments may remain)
    - `notifyWithContext` `kind?` param is `"cascade"` only (verify by reading notify-context.ts ~:137)
    - `node --test tests/architecture/catalog-uat.test.ts` passes (byte-identical — D-01); `node --test tests/shared/notify-v2.test.ts` passes; `git diff docs/output-catalog.md` empty
    - A fresh-disable cascade still emits the `/reload to pick up changes` trailer (driven by needsReload:true) — confirmed by the catalog-uat disable fixture staying byte-identical
  </acceptance_criteria>
  <done>The `disable-cascade` kind is gone from unions, switch arms, the call site, and test fixtures; the disable trailer is driven by needsReload:true; EXPECTED bytes byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | No new trust boundary. Removing a status token and a cascade kind from internally-constructed, closed-union notification rows. No network, file writes, input parsing, or crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering (correctness regression, NOT a security threat) | present→installed collapse + disable-cascade removal | mitigate | Risk is output drift (a stray reasons brace on the inventory installed row, or a lost reload trailer on disable). Gated by `tests/architecture/catalog-uat.test.ts` byte-equality (D-01) + `npm run check` on every task; Pitfall 2 (omit reasons) explicitly handled. |
| T-02-I | Information disclosure | rendered output (path leakage) | accept | UNCHANGED — redaction seam untouched (NFR-9); byte-gated. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (in-repo refactor). |

**Honest conclusion (block-on severity: high):** no new security attack surface.
The single high-severity concern is a CORRECTNESS regression (rendered-output
drift), fully guarded by the byte gate + type gates — not a security threat.
</threat_model>

<verification>
- `npm run check` green — GATE-03.
- `node --test tests/architecture/catalog-uat.test.ts` EXPECTED blocks byte-identical — GATE-02 / D-01.
- `grep -rn "disable-cascade"` and `grep -n '"present"\|PluginPresentMessage'` return no source/fixture lines.
- `git diff docs/output-catalog.md` empty.
</verification>

<success_criteria>
- `present` status collapsed into `installed`; list inventory row stamps needsReload:false (RLD-04).
- `disable-cascade` kind removed; disable stamps needsReload:true on realized rows; inventory stamps false (RLD-05).
- Test input fixtures migrated; EXPECTED output byte-identical; catalog-uat + npm run check green (D-01).
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-02-SUMMARY.md` when done.
</output>
