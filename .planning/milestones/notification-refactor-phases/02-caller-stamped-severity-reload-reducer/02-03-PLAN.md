---
phase: 02-caller-stamped-severity-reload-reducer
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - tests/architecture/notify-stamp-coverage.test.ts
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "The reconcile-applied/pending projection (the one producer family the type system cannot reach) stamps both severity and needsReload on every state-change row — proven at runtime (GATE-01/D-05)"
    - "The arch test FAILS if any transition row from buildReconcileAppliedCascade / buildReconcilePendingNotification omits severity or needsReload"
    - "npm run check is green with the new test included"
  artifacts:
    - path: "tests/architecture/notify-stamp-coverage.test.ts"
      provides: "D-05 runtime-introspection backstop driving the reconcile projection, asserting transition rows stamp both fields"
      contains: "buildReconcileAppliedCascade"
  key_links:
    - from: "tests/architecture/notify-stamp-coverage.test.ts"
      to: "orchestrators/reconcile/notify.ts buildReconcileAppliedCascade / buildReconcilePendingNotification"
      via: "import + drive the projection, walk rows, assert stamped fields on transition statuses"
      pattern: "buildReconcileAppliedCascade|buildReconcilePendingNotification"
---

<objective>
Add the D-05 architecture-test backstop that closes GATE-01 for the one producer
family the type system cannot reach: the reconcile projection
(`orchestrators/reconcile/notify.ts`), which builds rows directly via
`block.plugins.push({...})` into a widening `MarketplaceBlock` accumulator rather
than through a command render map. The test drives `buildReconcileAppliedCascade`
and `buildReconcilePendingNotification` with sample outcomes and asserts every
state-change (transition) row carries both `severity` and `needsReload`.

Purpose: GATE-01's belt-and-suspenders — the type narrowing (Plan 02-01/D-04) covers
literal construction sites; this runtime test covers the dynamic projection
(D-05), so the relocation to ~18 producers is fully gated.
Output: a new `tests/architecture/notify-stamp-coverage.test.ts` that fails loudly
on any unstamped transition row in the projection.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-PATTERNS.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the D-05 runtime-introspection backstop (GATE-01)</name>
  <files>tests/architecture/notify-stamp-coverage.test.ts</files>
  <read_first>
    - tests/architecture/notify-grammar-invariant.test.ts (the runtime-introspection arch-test idiom to mirror — header doc-comment + requirement-ID anchors, `import assert from "node:assert/strict"`, `import test from "node:test"`, import the production symbol, walk real notify shapes)
    - extensions/pi-claude-marketplace/orchestrators/reconcile/notify.ts (the exported projections buildReconcilePendingNotification ~:197 + buildReconcileAppliedCascade ~:471; applyOutcomeToBlock ~:311-419; the PerEntryOutcome input type imported from ./apply-outcomes.ts; the transition vs failed push arms ~:332-407)
    - extensions/pi-claude-marketplace/shared/notify.ts (PLUGIN_STATUSES + PluginStatus type ~:461-516 — to pin TRANSITION_STATUSES via `satisfies readonly PluginStatus[]`)
    - .planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-PATTERNS.md (the notify-stamp-coverage.test.ts section — exact structure + the TRANSITION_STATUSES drift-proof set)
    - .planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-RESEARCH.md (D-05 Code Example — the runtime-introspection mechanism; the recommendation to prefer it over an AST walk)
  </read_first>
  <action>
    Create `tests/architecture/notify-stamp-coverage.test.ts` modeled on
    `notify-grammar-invariant.test.ts`. Define a drift-proof `const
    TRANSITION_STATUSES = new Set([...]) ` covering exactly the realized-transition
    plugin statuses (`installed`, `updated`, `reinstalled`, `uninstalled`,
    `disabled`), pinned via a `satisfies readonly PluginStatus[]`-style assertion so
    adding/removing a status keeps the set honest. Build a small set of representative
    `PerEntryOutcome[]` sample inputs covering each transition outcome kind the
    projection emits (plugin-installed, plugin-uninstalled, plugin-enabled→installed,
    plugin-disabled, and the *-failed arms) plus at least one inventory/non-transition
    outcome. Drive BOTH exported projections (`buildReconcileAppliedCascade` and
    `buildReconcilePendingNotification`) with the samples, then walk
    `msg.marketplaces[].plugins[]`: for every row whose `status` is in
    `TRANSITION_STATUSES`, assert `row.severity !== undefined` and `typeof
    row.needsReload === "boolean"` with a row-level diagnostic message naming the
    status. Add a second assertion that the failed rows stamp `severity:"error",
    needsReload:false` and the realized-transition rows stamp `needsReload:true`, so
    the test pins the D-06 reload semantics for the projection, not just presence.

    Anchor the test titles and the header doc-comment with `GATE-01`/`D-05`/`RECON-04`
    (never `Phase 2`/`Wave N`/`Plan NN`), per `.claude/rules/typescript-comments.md`.
    The projections are pure (no ctx/pi needed) — do NOT add mock ctx/pi unless an
    export signature requires it; if it does, copy the `makeCtx`/`piWithBothLoaded`
    helpers verbatim from notify-grammar-invariant.test.ts.
  </action>
  <verify>
    <automated>cd extensions/pi-claude-marketplace && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check` exits 0 with the new test included in the suite
    - `node --test tests/architecture/notify-stamp-coverage.test.ts` passes
    - The test imports and drives both `buildReconcileAppliedCascade` and `buildReconcilePendingNotification` (verify by reading; `grep -c "buildReconcileAppliedCascade\|buildReconcilePendingNotification" tests/architecture/notify-stamp-coverage.test.ts` returns >= 2)
    - Negative proof: temporarily strip `needsReload` from one transition push in `orchestrators/reconcile/notify.ts`, run `node --test tests/architecture/notify-stamp-coverage.test.ts`, confirm it FAILS with the row-level diagnostic; restore the field and confirm green
    - Test titles anchored with `GATE-01`/`D-05` (not `Phase 2`/`Wave N`); `grep -c "Phase 2\|Wave " tests/architecture/notify-stamp-coverage.test.ts` returns 0
  </acceptance_criteria>
  <done>The D-05 backstop drives both reconcile projections, asserts transition rows stamp both fields with correct D-06 reload semantics, fails on a stripped stamp, and npm run check is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A test-only addition. Drives in-repo projections over internally-constructed sample data. No network, file writes, input parsing, or crypto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering (correctness regression, NOT a security threat) | reconcile projection stamp coverage | mitigate | This task ADDS a gate (the D-05 backstop) that mitigates the relocation's regression risk; it is itself proven by a deliberate strip-the-stamp negative test. No new attack surface. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (test uses node:test built-in). |

**Honest conclusion (block-on severity: high):** no new security attack surface.
This plan strengthens the correctness-regression mitigation (GATE-01). No security
threats here.
</threat_model>

<verification>
- `npm run check` green — GATE-03.
- `node --test tests/architecture/notify-stamp-coverage.test.ts` passes; fails on a deliberately stripped projection stamp — GATE-01 / D-05 proven live.
- catalog-uat remains byte-identical (unchanged by this test-only addition).
</verification>

<success_criteria>
- A runtime arch test asserts every transition row from the reconcile projection stamps both fields (GATE-01/D-05).
- The test fails loudly on an unstamped transition row; npm run check green.
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/02-caller-stamped-severity-reload-reducer/02-03-SUMMARY.md` when done.
</output>
