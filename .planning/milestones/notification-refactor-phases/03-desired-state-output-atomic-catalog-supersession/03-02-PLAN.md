---
phase: 03-desired-state-output-atomic-catalog-supersession
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/shared/notify-context.ts
  - docs/output-catalog.md
  - tests/architecture/catalog-uat.test.ts
autonomous: true
requirements: [OUT-03, OUT-04, OUT-06, OUT-08, GATE-02]
must_haves:
  truths:
    - "OUT-03/D-04: plural (bulk) operations carry a trailing tally line `<Operation>: <n> failure(s), <n> warning(s), <n> success(es)` — pluralized by count, zero-count categories omitted, NO terminal period."
    - "OUT-04/D-04: the tally `<Operation>` is the command's `CommandContext.Messaging.label` (e.g. `Plugin install`, `Marketplace add`), threaded through emitContextCascade/emitReconcileAppliedContextCascade into emitWithSummary."
    - "D-04: the tally appears IFF the operation is plural, bound to Phase 1's STRUCTURAL tuple-vs-array cardinality (OUT-07) — NOT a render-time row-count heuristic; single-target (1-tuple) operations OMIT the tally but still show the leading severity sentence for error/warning."
    - "OUT-06/D-03: mixed-subject cascades (load-time reconcile, import) use the operation name in the tally, counting all rows uniformly."
    - "the success/info count for `<n> success(es)` comes from widening the existing countRowsBySeverity `target` union to include `\"info\"` (do not build a new counter — RESEARCH Don't-Hand-Roll)."
    - "OUT-08/GATE-02/D-06: every PLURAL catalog fenced block gains its tally line + the catalog-uat fixtures are updated IN LOCKSTEP; catalog-uat is GREEN at every commit boundary."
    - "the single emitWithSummary seam is preserved — the tally folds into the one composed string (no second ctx.ui.notify), placed after the body and before the RELOAD_HINT_TRAILER."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "trailing tally composer + widened countRowsBySeverity + label threading into emitWithSummary"
      contains: "failure(s)"
    - path: "extensions/pi-claude-marketplace/shared/notify-context.ts"
      provides: "Messaging.label threaded through notifyWithContext → emitContextCascade"
    - path: "docs/output-catalog.md"
      provides: "tally lines added to plural catalog blocks"
    - path: "tests/architecture/catalog-uat.test.ts"
      provides: "fixtures updated for the tally-bearing plural blocks"
  key_links:
    - from: "notifyWithContext (notify-context.ts:137)"
      to: "emitContextCascade (notify.ts:3091) → emitWithSummary (notify.ts:2938)"
      via: "context.Messaging.label passed as a sibling param to renderPluginRowBody"
      pattern: "Messaging.label"
    - from: "tally composer"
      to: "countRowsBySeverity (notify.ts:2245)"
      via: "info/warning/error counts"
      pattern: "countRowsBySeverity"
---

<objective>
Add the OUT-03/OUT-04 trailing per-operation tally (`<Operation>: <n> failure(s),
<n> warning(s), <n> success(es)`), bound to Phase 1's STRUCTURAL tuple-vs-array
cardinality (D-04 — plural emits, single omits), using the command's
`Messaging.label` as `<Operation>` (threaded through the cascade seams), with the
mixed-subject tally (D-03/OUT-06) for reconcile/import — all with atomic catalog
supersession (OUT-08 / GATE-02).

This builds on Plan 01 (the D-02 leading-sentence grammar must already be live so the
catalog blocks carry the new summary line; the tally is additive on top). The tally is
additive per cascade section, so it can land cleanly without re-touching the leading
sentence.

Purpose: Bulk operations get a per-operation outcome tally so the user sees the
aggregate result; single-target operations stay terse (row embeds the outcome).
Output: Tally composer + label plumbing + widened counter in notify.ts; tally lines
in every plural catalog block + fixtures.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-PATTERNS.md
@.planning/workstreams/notification-refactor/phases/01-localized-type-model-command-context-spine/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread Messaging.label + widen countRowsBySeverity + resolve the structural cardinality signal</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/shared/notify-context.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts — `countRowsBySeverity` (notify.ts:2245-2261, the counter to widen — its `target` union is `"warning" | "error"` today), `countFailedRows`/`countSkippedRows` (notify.ts:2218/2236, thin wrappers), `emitContextCascade` (notify.ts:3091) + `emitReconcileAppliedContextCascade` (notify.ts:3137) (the seams that already thread `renderPluginRowBody` — add `label` the same way), `emitWithSummary` (notify.ts:2938, the single emission seam), `RELOAD_HINT_TRAILER` (notify.ts:2088) + the body→hint fold (notify.ts:3058/3113).
    - extensions/pi-claude-marketplace/shared/notify-context.ts — `CommandContext`/`Messaging.label` (notify-context.ts:62-65), `notifyWithContext` (notify-context.ts:137-157, HAS the context, currently passes only the render closure to emitContextCascade).
    - 03-PATTERNS.md §"shared/notify.ts — thread Messaging.label" (the `renderPluginRowBody` param analog — add `label` the same way; keep it INERT until the tally consumes it so no byte shifts) and §"shared/notify.ts — NEW trailing tally" (widen the `target` union to include `"info"`; the `(x.severity ?? "info") === target` predicate already works for `"info"`).
    - 03-RESEARCH.md Open Question #1 (the structural cardinality signal — carry a `cardinality: "single" | "plural"` discriminant on the message or thread it via CommandContext; NO row-count heuristic, D-04/Pitfall 5), §"How the label reaches the seam".
    - 03-CONTEXT.md D-04 (tally bound to structural cardinality; single 1-tuple omits, plural array emits), OUT-07 (cardinality is structural).
  </read_first>
  <behavior>
    - countRowsBySeverity with target `"info"` returns the success/info count over mp + plugin rows.
    - emitContextCascade / emitReconcileAppliedContextCascade receive the command's `Messaging.label` and pass it through to emitWithSummary.
    - Threading the label causes NO byte change (it is unconsumed until Task 2's tally) — catalog-uat stays green with zero fixture edits in this task.
    - a runtime structural cardinality signal (`"single" | "plural"`) is available at the emitWithSummary seam for Task 2's tally gate, derived from the call-site structural shape (NOT a render-time row count).
  </behavior>
  <action>
Widen `countRowsBySeverity` (notify.ts:2245) `target` union from `"warning" | "error"`
to `"info" | "warning" | "error"` — the existing `(x.severity ?? "info") === target`
predicate already handles `"info"` with no further change (RESEARCH Don't-Hand-Roll).
Thread `context.Messaging.label` through the cascade seams: add a `label: string`
parameter to `emitContextCascade` (notify.ts:3091) and
`emitReconcileAppliedContextCascade` (notify.ts:3137) — mirror exactly how
`renderPluginRowBody` is already threaded — and pass it from `notifyWithContext` /
`notifyReconcileAppliedWithContext` (notify-context.ts:137) which already hold the
`context`. Resolve Open Question #1 (the structural cardinality signal): carry a
`cardinality: "single" | "plural"` discriminant from the call site into the seam (the
command knows whether it is single-target — single-target plugin commands construct the
1-tuple shape, bulk/@marketplace/import/reconcile construct the array). Pick the
cleanest mechanism — either a field on the message/CommandContext or a parameter
alongside `label` — and make it concrete; do NOT infer plural by counting rows at
render time (D-04 / Pitfall 5: a bulk op affecting one plugin must still be plural).
Keep the threaded label + cardinality INERT this task (unconsumed by emitWithSummary)
so NO rendered byte shifts and catalog-uat stays green with zero fixture edits — the
tally that consumes them lands in Task 2. Preserve the single `ctx.ui.notify` seam
(IL-2). Use decision-ID comment anchors (`OUT-04`, `D-04`), never `Phase N`.
  </action>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 with NO fixture edits in this task (the label/cardinality threading is inert — proves zero byte shift).
    - `grep -n '"info" | "warning" | "error"' extensions/pi-claude-marketplace/shared/notify.ts` shows the widened countRowsBySeverity target union.
    - `grep -n 'label' extensions/pi-claude-marketplace/shared/notify.ts` shows `label` threaded into emitContextCascade and emitReconcileAppliedContextCascade signatures.
    - A runtime `cardinality` signal (`"single" | "plural"`) is reachable at the emitWithSummary seam without counting rows: `grep -n 'cardinality' extensions/pi-claude-marketplace/shared/notify.ts extensions/pi-claude-marketplace/shared/notify-context.ts` shows the discriminant carried structurally.
    - Single `ctx.ui.notify` per notify invocation preserved (no new emission call).
    - `npm run check` green.
  </acceptance_criteria>
  <done>countRowsBySeverity counts info; Messaging.label + a structural cardinality signal are threaded to the emitWithSummary seam, inert (no byte shift); catalog-uat and npm run check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trailing tally composer (OUT-03/04) gated on cardinality + mixed-subject tally (D-03) + all plural catalog blocks/fixtures (atomic)</name>
  <files>extensions/pi-claude-marketplace/shared/notify.ts, docs/output-catalog.md, tests/architecture/catalog-uat.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts — `emitWithSummary` (notify.ts:2938, where the tally folds into the body string), `countRowsBySeverity` (widened in Task 1), `operationPhrase` (notify.ts:2294, the `count === 1 ? singular : plural` ternary to mirror for `failure(s)`/`warning(s)`/`success(es)`), `RELOAD_HINT_TRAILER` (notify.ts:2088) + the `{body}\n\n{hint}` fold (notify.ts:3058) — the tally inserts between body and hint.
    - 03-PATTERNS.md §"shared/notify.ts — NEW trailing tally" (the tally format `<Operation>: <n> failure(s), <n> warning(s), <n> success(es)`, pluralized, zero-count omitted, no terminal period; placement `{body}\n\n{tally}\n\n{hint}`; the byte fence is the contract).
    - 03-RESEARCH.md §"The Summary Composition Surface" (the tally is part of `body`, one notify call), Open Question #3 (tally placement: after body, before reload trailer; tally shown whenever plural regardless of severity, so a successful bulk import shows `Plugin import: 3 success(es)` — the catalog fence pins the exact contract), §"Atomic-Supersession Sequencing Strategy" item 4.
    - 03-CONTEXT.md D-04 (tally format, plural-only, label = Messaging.label), D-03 (mixed-subject tally uses operation name, counts all rows uniformly), OUT-03/OUT-04/OUT-06, D-06.
    - docs/output-catalog.md — the plural cascade sections (reinstall, update cascade, import, reconcile-applied, marketplace update cascade) that gain a tally line; tests/architecture/catalog-uat.test.ts — the fixtures for those plural states.
  </read_first>
  <behavior>
    - Plural error+warning+success mix, label `Plugin reinstall`: tally `Plugin reinstall: 2 failure(s), 1 warning(s), 3 success(es)` (zero-count categories omitted; no terminal period).
    - Plural all-success import, label `Plugin import`: tally `Plugin import: 3 success(es)` (regardless of info severity — RESEARCH OQ#3 resolution).
    - Single-target (1-tuple) operation: NO tally line (cardinality === "single"), but leading severity sentence still present for error/warning.
    - Mixed-subject reconcile/import (D-03): tally uses the operation label, counting all rows uniformly across plugin + marketplace subjects.
    - Placement: tally sits after the body, before the `/reload to pick up changes` trailer (`{body}\n\n{tally}\n\n{hint}`).
    - byte-equality: every plural catalog fenced block carries its tally line; catalog-uat forward + inverse walks green.
  </behavior>
  <action>
Add a tally composer in notify.ts that, when `cardinality === "plural"` (the structural
signal from Task 1), produces `<Operation>: <n> failure(s), <n> warning(s), <n>
success(es)` where `<Operation>` is the threaded `Messaging.label`, counts come from
`countRowsBySeverity(marketplaces, "error"|"warning"|"info")`, each category pluralized
via the `operationPhrase` `count === 1 ? singular : plural` ternary idiom
(`failure`/`failures`, `warning`/`warnings`, `success`/`successes`), ZERO-count
categories OMITTED, NO terminal period (D-04/OUT-03). Gate strictly on the structural
cardinality discriminant — single-target (`"single"`) OMITS the tally (Pitfall 5: never
count rows to decide plural). For mixed-subject cascades (D-03/OUT-06) the tally uses
the operation label and counts all rows uniformly (no per-subject split) — this falls
out of counting over all marketplaces + nested plugin rows. Fold the tally into the
single composed string inside/just-before `emitWithSummary` (notify.ts:2938) — place it
AFTER the body and BEFORE the `RELOAD_HINT_TRAILER` (`{body}\n\n{tally}\n\n{hint}`,
mirroring the existing body→hint fold at notify.ts:3058). Preserve the single
`ctx.ui.notify` seam (IL-2 — exactly one emission). Per RESEARCH OQ#3, the tally appears
on plural ops regardless of severity (a successful bulk import shows
`Plugin import: 3 success(es)`); the byte-compared catalog fence is the binding contract
for the exact line + placement. IN THE SAME COMMIT add the tally line to EVERY plural
catalog fenced block in docs/output-catalog.md (reinstall, update cascade, import,
reconcile-applied, marketplace update cascade, and any other array-shaped section) and
update the matching catalog-uat.test.ts fixtures so the forward + inverse walks stay
green. Use decision-ID comment anchors (`OUT-03`, `OUT-04`, `D-04`), never `Phase N`.
  </action>
  <verify>
    <automated>node --test "tests/architecture/catalog-uat.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 (every plural block carries its tally; single blocks have none; forward + inverse walks green).
    - `grep -n 'failure(s)\|success(es)\|failures\|successes' extensions/pi-claude-marketplace/shared/notify.ts` shows the tally pluralizers.
    - A plural mixed-severity catalog fixture renders `<Label>: <n> failure(s), <n> warning(s), <n> success(es)` with zero-count categories omitted and no terminal period (verified via its byte-block).
    - A single-target (1-tuple) catalog fixture has NO tally line (cardinality gate proven).
    - A successful bulk-import catalog fixture renders `Plugin import: <n> success(es)` (info-severity plural shows the tally per OQ#3).
    - Single `ctx.ui.notify` per notify invocation preserved.
    - `npm run check` green.
  </acceptance_criteria>
  <done>The OUT-03/04 tally renders on plural ops gated by structural cardinality, uses Messaging.label, drops to the operation name for mixed-subject (D-03), and every plural catalog block + fixture is updated in lockstep; catalog-uat and npm run check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tally composition → user-visible notification | The tally is renderer-derived from stamped severity counts + the command label; no untrusted input, no path, no secret crosses this seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | tally line (counts + Messaging.label) | accept | The tally is integer counts + a static English command label (`Plugin install` etc.) from CommandContext — no path, PII, or secret. No new cause-bearing row is introduced in this plan; the redaction seam (notify.ts:223) on existing cause trailers is untouched. Low-value, no leak surface. |
| T-03-04 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (in-repo TypeScript/markdown only). No legitimacy gate required. |
</threat_model>

<verification>
- `node --test "tests/architecture/catalog-uat.test.ts"` exits 0 after each task (GATE-02 atomic supersession — green at every commit boundary).
- `npm run check` green at the plan/wave boundary (GATE-03).
- The single emitWithSummary seam (one ctx.ui.notify) is preserved (IL-2).
</verification>

<success_criteria>
- OUT-03/OUT-04/D-04 trailing tally renders on plural operations, gated by structural cardinality, using Messaging.label; single-target omits it.
- OUT-06/D-03 mixed-subject tally uses the operation name, counting all rows uniformly.
- OUT-08/GATE-02 every plural catalog block + fixture updated in lockstep; catalog-uat green at every commit.
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/03-desired-state-output-atomic-catalog-supersession/03-02-SUMMARY.md` when done.
</output>
