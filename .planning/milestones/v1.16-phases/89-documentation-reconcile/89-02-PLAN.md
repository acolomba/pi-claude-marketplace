---
phase: 89-documentation-reconcile
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hooks-compatibility.md
autonomous: true
requirements: [DOC-04]
must_haves:
  truths:
    - "In docs/hooks-compatibility.md the `Stop` and `StopFailure` event-table rows read `✓` (supported), not `✗`; the `Stop` row note references the timing shift and the `StopFailure` row note marks it observation-only."
    - "A turn-boundary timing-shift subsection near the events table documents the one irreducible divergence (upstream folds a blocked stop into the same turn; Pi re-enters as a new turn; invisible to hook scripts — same payload, flag cadence, and 8-block cap; transcript shows an extra turn boundary) and points to docs/research/issue-103-stop-stopfailure-promotion.md."
    - "The matcher table contains a StopFailure error-type matcher row listing EXACTLY these 10 closed-set values and no others: rate_limit, overloaded, authentication_failed, oauth_org_not_allowed, billing_error, invalid_request, model_not_found, server_error, max_output_tokens, unknown — exact-match charset (letters, digits, `_`, `|`), no pipe-OR splitting; Stop's no-matcher disposition (non-empty matcher = reported no-matcher-support drop, UserPromptSubmit precedent) is noted."
    - "Exactly the `Stop` and `StopFailure` event rows flip from `✗` to `✓`; no other event row's support glyph changes (any other glyph change must trace to a research-inventory finding)."
    - "The 'Install-time disposition' section describes three DISTINCT arms: a parseable-but-unsupportable event/matcher/handler drops per-entry and the plugin resolves `(partially-available)` with a SINGLE aggregate `{unsupported hooks}` brace (per-handler breakdown on `plugin info`); a STRUCTURAL malformed `hooks.json` resolves `(unavailable)`; the silent fall-open and silent-drop arms are verified and kept — the arms are never conflated."
    - "No milestone-version string (v1.13 / any v1.1x) appears anywhere in the reconciled doc; the third table-column headers read `Pi` (version-neutral)."
  artifacts:
    - docs/hooks-compatibility.md
  key_links:
    - "hooks-compatibility.md timing-shift subsection -> pointer to docs/research/issue-103-stop-stopfailure-promotion.md."
    - "hooks-compatibility.md Install-time disposition rewrite <- authority prose in docs/output-catalog.md:390,1489 (partial-partition model; D-71-04 aggregate brace, D-71-05 info breakdown)."
    - "hooks-compatibility.md `additionalContext (Stop)` row <- STOP-05 additionalContext-without-block re-entry (settle.ts:269-275)."
  prohibitions:
    - "No milestone-version string (v1.13 or any v1.1x) appears anywhere in the reconciled doc — do NOT swap v1.13 for v1.16; strip milestone framing entirely (D-89-01)."
    - "No per-event peer-floor version note (for example '>= 0.80.5') appears in the doc — the floor is a package-level constraint, not per-event (D-89-03)."
    - "The three install-time disposition arms are never conflated — a structural malformed `hooks.json` stays `(unavailable)` and is never described as `(partially-available)` (Pitfall 2)."
---

<objective>
Reconcile `docs/hooks-compatibility.md` with the shipped v1.16 Stop/StopFailure behavior via the
full-doc audit D-89-04 mandates: flip the `Stop`/`StopFailure` event rows to supported, document
the one irreducible timing-shift divergence, add the StopFailure error-type matcher row, rewrite
the stale v1.13 hard-trip "Install-time disposition" section for the force-install
partial-partition model, and strip all milestone-version framing (D-89-01). Every remaining table
row is walked against current code and corrected only where the audit finds drift (the load-bearing
edits are the Stop/StopFailure promotion and the disposition rewrite; the promotion touched only
turn-boundary events and the disposition model, so the rest is expected accurate).

This doc is read by no test (grep: NONE) — edits carry zero byte-test risk; `format:check` does
not gate markdown. Correctness is proven structurally by grep assertions here and confirmed by
human review against the research Stale-Claim Inventory (§A) at /gsd-verify-work.

Purpose: a reader of the compatibility reference sees `Stop`/`StopFailure` as first-class supported
events, the exact matcher vocabulary, the honest three-arm install-time disposition, and no
misleading milestone-version narration.
Output: one reconciled markdown file describing the current bridge.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-documentation-reconcile/89-CONTEXT.md
@.planning/phases/89-documentation-reconcile/89-RESEARCH.md
</context>

<artifacts_produced>
This plan modifies (documentation only; no code symbols):
- `docs/hooks-compatibility.md` — full-doc reconcile (rows A1-A14 of the research Stale-Claim Inventory §A).

Phase-wide artifact set (across all three plans): `docs/hooks-compatibility.md`,
`docs/research/claude-hooks-vs-pi-events.md`, `docs/research/issue-103-stop-stopfailure-promotion.md`,
`docs/output-catalog.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Stop/StopFailure promotion — version-neutral reframe, event-row flips, timing-shift subsection, matcher row</name>
  <files>docs/hooks-compatibility.md</files>
  <read_first>
    - docs/hooks-compatibility.md (full read — the Events table 11-37, the "Event status classification" buckets 39-68, the Matcher-syntax table 70-84, and every table's third-column header)
    - .planning/phases/89-documentation-reconcile/89-RESEARCH.md (Stale-Claim Inventory §A rows A1-A6, and the "Reusable Wording" section)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ "The one irreducible divergence" lines 75-77 and executive summary line 11 — reuse this near-final timing-shift wording; § "StopFailure arm" line 73 for the 10-value set and charset)
    - .planning/phases/89-documentation-reconcile/89-CONTEXT.md (D-89-01 version-neutral, D-89-02 flip rationale and matcher/no-matcher disposition)
  </read_first>
  <action>
    Apply the A1-A6 promotion edits from the research inventory §A:
    A1 (D-89-01): strip milestone-version framing. Remove the version phrase from the intro
    (line 3 "in v1.13") and the sources line (line 7 "the v1.13 bridge sources" -> "the Pi bridge
    sources"); rename the third column header of EVERY table from `Pi v1.13` to `Pi`
    (version-neutral) — there are nine such headers; and remove any remaining "v1.13 bridge"
    narration in body prose (e.g. the deferred-bucket bullet at line 51). Do not substitute v1.16
    or any other milestone version; describe the current bridge — git carries lineage.
    A2 (line 21, `Stop` row): change the support glyph from `✗` to `✓`; replace the note
    ("Pi end-of-turn is observation-only; cannot honor block-to-continue") with a note that Stop is
    supported with full decision control and a reference to the timing-shift subsection below
    (see A5).
    A3 (line 22, `StopFailure` row): change `✗` to `✓`; note it fires on `error`/`length` endings,
    observation-only (hook output and exit code ignored).
    A4 (lines 43-51): in the "Deferred for engineering reasons" bucket, remove the `Stop` bullet
    (line 46) and the `StopFailure` bullet (line 50); leave `SubagentStart`/`SubagentStop`
    (line 51) and the other deferred entries.
    A5 (new subsection near the events table): add a short subsection (heading is Claude's
    discretion, e.g. "Turn-boundary timing shift") documenting the one irreducible divergence —
    adapt the issue-103 § "The one irreducible divergence" wording (read_first), keeping the
    CONTEXT D-89-02 framing: upstream folds a blocked stop into the same turn; under Pi the agent
    has settled and re-entry starts a new turn; hook scripts cannot observe the difference (same
    payload, flag cadence, and 8-block cap); the transcript shows an extra turn boundary — with a
    pointer to `docs/research/issue-103-stop-stopfailure-promotion.md`. Because the shift is not
    hook-observable, this is why the rows are `✓` and not `⚠` (D-89-02).
    A6 (matcher-syntax table, near the `SessionEnd reason matcher` row ~line 83): add a
    `StopFailure error-type matcher` row listing exactly the ten closed-set values (rate_limit,
    overloaded, authentication_failed, oauth_org_not_allowed, billing_error, invalid_request,
    model_not_found, server_error, max_output_tokens, unknown) with a note that the charset is
    exact-match (letters, digits, `_`, `|`) with no pipe-OR splitting. Note Stop's no-matcher
    disposition — a non-empty `Stop` matcher is reported as a `no-matcher-support` group drop
    (UserPromptSubmit precedent), never silently ignored — either as its own row or a note on the
    events row, matching the table's existing shape (Claude's discretion).
    <reversibility rating="reversible">Git-tracked markdown edit; `git checkout` restores prior state.</reversibility>
  </action>
  <verify>
    <automated>test "$(grep -Ec 'v1\.1[0-9]' docs/hooks-compatibility.md)" = "0" && grep -q 'oauth_org_not_allowed' docs/hooks-compatibility.md && grep -q 'max_output_tokens' docs/hooks-compatibility.md && grep -qi 'timing shift' docs/hooks-compatibility.md && grep -q 'issue-103-stop-stopfailure-promotion.md' docs/hooks-compatibility.md</automated>
    <human-check>The `Stop` and `StopFailure` event rows show `✓`; the StopFailure matcher row lists all ten values and ONLY those ten; the timing-shift subsection reads correctly and the rows are `✓` not `⚠`.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -Ec 'v1\.1[0-9]' docs/hooks-compatibility.md` returns 0 (no milestone-version string remains; the nine third-column headers are version-neutral `Pi`).
    - The `Stop` and `StopFailure` rows in the Events table show `✓`; neither appears in the "Deferred for engineering reasons" bucket.
    - A timing-shift subsection exists near the events table and contains the string `issue-103-stop-stopfailure-promotion.md`.
    - The matcher table's StopFailure row contains all ten tokens rate_limit, overloaded, authentication_failed, oauth_org_not_allowed, billing_error, invalid_request, model_not_found, server_error, max_output_tokens, unknown — and no eleventh error-type token.
  </acceptance_criteria>
  <done>Stop/StopFailure read supported with correct notes; the timing-shift subsection and StopFailure matcher row exist; no milestone-version string remains.</done>
</task>

<task type="auto">
  <name>Task 2: Install-time disposition rewrite, stdin/stdout Stop rows, and remaining-row audit</name>
  <files>docs/hooks-compatibility.md</files>
  <read_first>
    - docs/hooks-compatibility.md (the Matcher-syntax regex-row note line 77, the Tool-name-mapping prose line 100, the Handler-types table 128-135, the stdin/stdout table 150-171, the Async/lifecycle rows 199-207, the "Install-time disposition" section 209-232, and the Env-var/Handler-fields/Config-surfaces tables 174-197)
    - .planning/phases/89-documentation-reconcile/89-RESEARCH.md (Stale-Claim Inventory §A rows A7-A14, the "Audit stance" note, and Common Pitfall 2 — the three-arm distinction)
    - docs/output-catalog.md (the partial-hook entry at line 390 and the disposition prose at line 1489 — the authority for the partial-partition model: D-71-04 single aggregate `{unsupported hooks}` brace, D-71-05 per-handler `event(matcher) (unsupported)` breakdown on `info`, and the distinct structural-malformed `(unavailable)` arm via narrowResolverNotes)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ "Stop decision control" and § "Stop payload" — the stop_hook_active flag, last_assistant_message field, and the block / exit-2 / additionalContext / continue:false arms)
  </read_first>
  <action>
    Apply the A7-A14 disposition + audit edits from the research inventory §A:
    A7 (matcher-syntax regex row note, line 77): the note "any character outside the safe charset
    trips `(unavailable) {unsupported hooks}`" is stale — reconcile it to the partial-partition
    model: a parseable-but-unsupportable matcher group drops per-entry and the plugin resolves
    `(partially-available)`; only a STRUCTURALLY malformed `hooks.json` resolves `(unavailable)`.
    A8 (tool-name mapping prose, line 100): "the plugin will install with `(unavailable)
    {unsupported hooks}`" -> the affected hook entry drops per-entry and the plugin resolves
    `(partially-available)` with the single aggregate `{unsupported hooks}` brace.
    A9 (Handler types rows, lines 132-135): the `http` note "plugin trips `(unavailable)
    {unsupported hooks}`" and the `mcp_tool`/`prompt`/`agent` "unsupported" notes -> reconcile so
    an unsupportable handler alongside supported ones drops per-entry and the plugin resolves
    `(partially-available)`, not a whole-plugin `(unavailable)` trip.
    A10 (stdin/stdout rows 167-168): Stop now supports `additionalContext`-without-block re-entry
    (STOP-05). Add an `additionalContext (Stop)` row marked `✓` (re-enters the agent loop without a
    block — feedback labeling), and narrow the `additionalContext (other events)` `✗` row to
    events other than SessionStart and Stop; leave the SessionStart capture path unchanged.
    A11 (new rows/notes, per D-89-04): reflect Stop's decision-control arms and payload fields.
    Update the existing generic `decision: "block"` (line 159), `continue: false` (line 158), and
    exit-2 (line 156) row notes so they acknowledge Stop now exercises these per-event arms
    (block re-entry via followUp+triggerTurn; exit-2 blocks with stderr; top-level continue:false
    takes precedence over block). Add coverage for the `stop_hook_active` and
    `last_assistant_message` Stop stdin payload fields where the table shape best fits (a payload
    row/note or folded into the timing-shift/Stop subsection from Task 1) — Claude's discretion,
    keep tables consistent with their column shapes.
    A12 (Install-time disposition section, lines 209-232): REWRITE the "Hard install-time trip"
    model. Replace the whole-plugin `(unavailable)` "strict supportability over partial support"
    framing with the three DISTINCT arms — do NOT conflate them: (1) a parseable-but-unsupportable
    event / matcher group / handler type drops per-entry; the plugin resolves `(partially-available)`
    with a SINGLE aggregate `{unsupported hooks}` brace (D-71-04) and a per-handler
    `event(matcher) (unsupported)` breakdown on the `plugin info` surface (D-71-05). (2) a
    STRUCTURALLY malformed `hooks.json` (invalid JSON / schema failure) resolves `(unavailable)`
    (distinct arm, sourced through narrowResolverNotes). (3) the existing "Silent fall-open"
    (lines 221-227) and "Silent drop" (lines 228-232) arms — verify against code and keep where
    still accurate. Authority prose: docs/output-catalog.md:390,1489.
    A13 (Async/lifecycle, lines 205-206) and A14 (Env-var / Handler-fields / Config-surfaces
    tables, lines 174-197): walk each row against current code; these are expected still accurate
    (the promotion touched only turn-boundary events and the disposition model). Correct ONLY a row
    the audit finds actually drifted; make no cosmetic changes to accurate rows. In the SUMMARY,
    record which rows were audited-and-unchanged versus corrected.
    <reversibility rating="reversible">Git-tracked markdown edit; `git checkout` restores prior state.</reversibility>
  </action>
  <verify>
    <automated>test "$(grep -Ec '\(unavailable\) \{unsupported hooks\}' docs/hooks-compatibility.md)" = "0" && grep -q '(partially-available)' docs/hooks-compatibility.md && grep -qi 'malformed' docs/hooks-compatibility.md && test "$(grep -Ec 'v1\.1[0-9]' docs/hooks-compatibility.md)" = "0"</automated>
    <human-check>The Install-time disposition section presents three distinct arms (structural malformed stays `(unavailable)`); the additionalContext (Stop) row is `✓`; A13/A14 rows are verified against code with only genuine drift corrected.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '\(unavailable\) \{unsupported hooks\}' docs/hooks-compatibility.md` returns 0 — the stale hard-trip phrasing is gone from the matcher note (A7), tool-name prose (A8), handler-types (A9), and the disposition section (A12).
    - The doc contains `(partially-available)` and describes a per-entry drop with a single aggregate `{unsupported hooks}` brace.
    - The Install-time disposition section still describes a structurally malformed `hooks.json` resolving `(unavailable)` as a DISTINCT arm (the three arms are not collapsed into one).
    - An `additionalContext (Stop)` row exists marked `✓`; the `additionalContext (other events)` row no longer implies Stop is unsupported.
    - `grep -Ec 'v1\.1[0-9]' docs/hooks-compatibility.md` still returns 0 (Task 1's version strip is not regressed).
  </acceptance_criteria>
  <done>The disposition section reads as the three-arm partial-partition model; Stop's additionalContext/decision-control coverage is present; remaining rows are audited with only genuine drift corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human reader -> docs | Static markdown consumed by humans; no runtime code path executes this file. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-03 | Tampering | hooks-compatibility.md content accuracy | low | mitigate | Grep assertions gate the structural edits (version strip, glyph flips, matcher tokens, disposition phrasing); human review at /gsd-verify-work confirms prose against the research Stale-Claim Inventory §A. |
| T-89-04 | Repudiation | milestone-version narration | low | accept | D-89-01 removes version narration; git history carries lineage (matches house source-comment policy). |
| T-89-SC | Tampering (supply chain) | package installs | n/a | accept | No npm/pip/cargo install occurs in this docs-only phase; supply-chain vector not present. |
</threat_model>

<verification>
- `grep -Ec 'v1\.1[0-9]' docs/hooks-compatibility.md` returns 0.
- `grep -Ec '\(unavailable\) \{unsupported hooks\}' docs/hooks-compatibility.md` returns 0.
- `npm run check` stays green (NFR-6) — this doc is not test-coupled, but the full gate confirms nothing else regressed.
- Human review (at /gsd-verify-work): the reconciled doc matches the research Stale-Claim Inventory §A row-by-row.
</verification>

<success_criteria>
- Stop/StopFailure read supported (`✓`) with correct notes and the timing-shift subsection.
- The StopFailure matcher row lists exactly the ten closed-set values; Stop's no-matcher disposition noted.
- The Install-time disposition section is the three-arm partial-partition model with the structural-malformed arm distinct.
- No milestone-version string and no per-event peer-floor note appear anywhere in the doc.
</success_criteria>

<output>
Create `.planning/phases/89-documentation-reconcile/89-02-SUMMARY.md` when done.
</output>
