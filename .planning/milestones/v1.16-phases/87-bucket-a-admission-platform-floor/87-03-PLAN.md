---
phase: 87-bucket-a-admission-platform-floor
plan: 03
type: execute
wave: 3
depends_on: ["87-02"]
files_modified:
  - tests/fixtures/hookify-hooks.json
  - tests/fixtures/ralph-wiggum-hooks.json
  - tests/domain/resolver-strict.test.ts
  - tests/orchestrators/plugin/info.test.ts
autonomous: true
requirements: [ADMIT-02]

must_haves:
  truths:
    - "A plugin declaring Stop/StopFailure ALONGSIDE already-supported bucket-A events (hookify) resolves `available` — no `{unsupported hooks}` partition drop for these two events; they sit in the same supported partition as the others (ADMIT-02, edge: adjacency)"
    - "A Stop-only plugin (ralph-wiggum, no other bucket-A event) resolves `available` with a NON-empty supported subset — NOT the empty-subset→unavailable edge a genuinely non-bucket-A-only plugin hits (ADMIT-02, edge: empty)"
    - "`plugin info` lists Stop and StopFailure as supported (bare `<event>` non-tool entries) in deterministic hook-summary order for both fixtures; entries render stably (ADMIT-02, edge: ordering)"
    - "The hookify fixture Stop arm and the ralph-wiggum fixture are derived from real claude-plugins-official wire bytes, with provenance recorded in each fixture `description` (D-87-03)"
  artifacts:
    - tests/fixtures/hookify-hooks.json
    - tests/fixtures/ralph-wiggum-hooks.json
  key_links:
    - "fixtures → parseHooksConfig → partitionHooks → resolver verdict — with BUCKET_A_MEMBERS now containing Stop/StopFailure, the drop path no longer emits `{kind:\"event\"}` for them"
    - "info projectHookSummaryEntries (info.ts:376) non-tool arm — lists Stop/StopFailure as bare `<event>` supported entries once the union is widened (Plan 02)"
  prohibitions:
    - statement: "Fixtures MUST be derived from real claude-plugins-official wire bytes (hookify precedent), NOT invented — record provenance in the fixture description (D-87-03)"
      status: flagged-unverified
    - statement: "Do NOT touch the network — all assertions are offline unit fixtures under `npm run check` (D-87-03, NFR-5)"
      status: flagged-unverified
    - statement: "Do NOT add dispatch, translators, or an agent_settled subscription — this phase only proves the admission/info verdict; dispatch is Phase 88"
      status: flagged-unverified
    - statement: "Do NOT record phase/milestone/plan/wave tokens in comments or test titles; decision (D-87-03) and requirement (ADMIT-02) IDs are the allowed anchors"
      status: flagged-unverified
---

<objective>
Prove the observable outcome of the phase (ADMIT-02): a plugin declaring
Stop/StopFailure resolves `available` and `plugin info` lists both as supported.
Restore the Stop arm that was slimmed out of the hookify fixture and add a new
Stop-only ralph-wiggum fixture — both from real claude-plugins-official wire
bytes (D-87-03) — then assert the resolver partition and the `plugin info`
supported listing flip to fully available. Everything runs offline in
`npm run check`; live resolution against the real marketplace is a
milestone-audit/UAT concern, not a Phase 87 test.

Purpose: Plan 02 made Stop/StopFailure admissible on the machinery; this plan is
the fixture-backed, user-facing proof that the resolver verdict and the info
surface actually flip — the emergent behavior of growing BUCKET_A_MEMBERS with
zero new admission code.

Output: hookify Stop arm restored; `ralph-wiggum-hooks.json` added; resolver
available-flip cases for both fixtures; `plugin info` supported-listing cases for
both events.
</objective>

<edge_probe_accounting>
This plan authors the 3 ADMIT-02 edge-probe items (adjacency, empty, ordering)
into `must_haves.truths` above — the remaining covered probes from the phase's
8-item set (ADMIT-01 × 4 landed in Plan 02; FLOOR-01 × 1 flagged in Plan 02).
No probe item silently dropped.
</edge_probe_accounting>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-CONTEXT.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-RESEARCH.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-PATTERNS.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-02-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore hookify Stop arm and add the ralph-wiggum fixture (real wire bytes)</name>
  <files>tests/fixtures/hookify-hooks.json, tests/fixtures/ralph-wiggum-hooks.json</files>
  <read_first>
    - tests/fixtures/hookify-hooks.json (current top-level `hooks` object with PreToolUse / PostToolUse / UserPromptSubmit command-handler arms — the exact shape to match for the restored Stop arm)
    - 87-PATTERNS.md "restore Stop arm" and "ralph-wiggum" sections (handler shape + provenance guidance)
    - 87-CONTEXT.md D-87-03 (fixtures offline, derived from real wire bytes, provenance in description)
  </read_first>
  <action>
    Add a `Stop` arm to the hookify fixture's top-level `hooks` object matching
    the existing command-handler shape (a group with a `hooks` array of
    `{ type: "command", command: "...", timeout: ... }`). Derive the exact
    command/path from the real hookify `hooks.json` in claude-plugins-official
    (match how the existing PreToolUse/PostToolUse/UserPromptSubmit arms were
    produced — `${CLAUDE_PLUGIN_ROOT}`-interpolated command). Keep the existing
    arms byte-unchanged.

    Create `tests/fixtures/ralph-wiggum-hooks.json` mirroring the hookify fixture
    structure: a top-level `description` recording provenance (real
    claude-plugins-official ralph-wiggum wire bytes; match the hookify fixture's
    provenance-in-description precedent) and a top-level `hooks` object declaring
    a Stop-only arm (no other bucket-A event) in the same command-handler shape.
    Use a match-all (empty or `*`) Stop matcher so it is admissible.
  </action>
  <verify>
    <automated>node -e "for (const f of ['tests/fixtures/hookify-hooks.json','tests/fixtures/ralph-wiggum-hooks.json']) { const j = require('node:fs').readFileSync(f,'utf8'); JSON.parse(j); } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(Object.keys(JSON.parse(require('node:fs').readFileSync('tests/fixtures/hookify-hooks.json','utf8')).hooks))"` includes `Stop` alongside the pre-existing `PreToolUse`, `PostToolUse`, `UserPromptSubmit`.
    - `tests/fixtures/ralph-wiggum-hooks.json` parses as JSON, has a `description` naming its real-wire-bytes provenance, and a `hooks` object whose ONLY key is `Stop`.
    - `npx prettier --check tests/fixtures/hookify-hooks.json tests/fixtures/ralph-wiggum-hooks.json` passes.
    - The two pre-existing hookify arms are byte-unchanged.
  </acceptance_criteria>
  <done>hookify has a restored real-wire-bytes Stop arm; ralph-wiggum-hooks.json exists as a Stop-only fixture with recorded provenance; both parse and pass format:check.</done>
</task>

<task type="auto">
  <name>Task 2: Assert resolver available-flip and plugin info supported-listing for both fixtures</name>
  <files>tests/domain/resolver-strict.test.ts, tests/orchestrators/plugin/info.test.ts</files>
  <read_first>
    - tests/domain/resolver-strict.test.ts (existing `available` and `partially-available` cases as analogs; the fixture-loading `fixture(...)` helper; the empty-subset→unavailable case re-pointed to Notification in Plan 01)
    - tests/orchestrators/plugin/info.test.ts (existing supported-listing cases; how non-tool events render as bare `<event>` on the info surface; INFO-05 lenient reader case re-pointed to Notification in Plan 01)
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts (projectHookSummaryEntries ~376 non-tool arm)
    - tests/fixtures/hookify-hooks.json and tests/fixtures/ralph-wiggum-hooks.json (from Task 1)
  </read_first>
  <action>
    Add resolver cases asserting: the hookify fixture (Stop + bucket-A events)
    resolves `available` with no `{unsupported hooks}` partition drop for Stop or
    StopFailure — the supported subset retains all its arms; and the ralph-wiggum
    fixture (Stop-only) resolves `available` with a non-empty supported subset
    (NOT the empty-subset→unavailable edge). Model these on the existing
    `available` cases; assert the resolver state and the absence of an
    unsupported-hooks note for these events.

    Add `plugin info` cases asserting the supported hook listing includes `Stop`
    (and, for a fixture that declares it, `StopFailure`) as bare `<event>`
    supported entries — no ` (unsupported)` suffix — for both fixtures, in the
    deterministic hook-summary order. Model these on the existing supported
    listing cases. Test titles anchor on ADMIT-02 / the existing info-surface IDs;
    no phase or Pitfall tokens (comment policy).
  </action>
  <verify>
    <automated>node --test tests/domain/resolver-strict.test.ts tests/orchestrators/plugin/info.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A resolver case proves hookify resolves `available` and its supported hooks include the Stop arm (no `{unsupported hooks}` note referencing Stop/StopFailure).
    - A resolver case proves ralph-wiggum (Stop-only) resolves `available` with a non-empty supported subset (distinct from the empty-subset→unavailable Notification case).
    - A `plugin info` case shows `Stop` listed as a supported (non-suffixed) entry for the ralph-wiggum fixture and Stop listed supported for hookify; `grep -rn "Stop (unsupported)" tests/orchestrators/plugin/info.test.ts` returns nothing.
    - Both listed suites pass; `npm run check` is green end-to-end.
  </acceptance_criteria>
  <done>Both fixtures resolve available with Stop/StopFailure in the supported partition and listed as supported by `plugin info`; the resolver and info suites are green; `npm run check` passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols/files this plan creates (excluded from cross-plan drift verification):
- New fixture `tests/fixtures/ralph-wiggum-hooks.json` (Stop-only, real wire bytes).
- Restored `Stop` arm in `tests/fixtures/hookify-hooks.json`.
- New resolver available-flip and info supported-listing test cases (no new
  public symbols).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture `hooks.json` bytes → offline parser under test | Test-only fixtures; no runtime/network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-04 | Tampering | test fixtures | low | accept | Fixtures are offline, derived from real published marketplace wire bytes (D-87-03); no production code, no network (NFR-5). Provenance recorded in each fixture description. |

No package installs, no network, no auth/crypto/secrets — no `T-87-SC`
supply-chain checkpoint applies.
</threat_model>

<verification>
- `node --test tests/domain/resolver-strict.test.ts tests/orchestrators/plugin/info.test.ts` green.
- `npm run check` green end-to-end (offline; NFR-5 — no network for these
  assertions, D-87-03).
- `grep -rn "Stop (unsupported)" tests/` returns nothing (Stop is now supported
  everywhere except where Notification is the deliberate unsupported example).
</verification>

<success_criteria>
The hookify (Stop + bucket-A) and ralph-wiggum (Stop-only) fixtures resolve
`available` with Stop/StopFailure in the supported partition, and `plugin info`
lists both events as supported — the emergent, fixture-backed proof of ADMIT-02
with zero new admission code. All offline; `npm run check` green.
</success_criteria>

<output>
Create `.planning/phases/87-bucket-a-admission-platform-floor/87-03-SUMMARY.md` when done.
</output>
