---
phase: 87-bucket-a-admission-platform-floor
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hook-events.ts
  - extensions/pi-claude-marketplace/shared/concerns/hooks.ts
  - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
  - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  - tests/architecture/hooks-supportability.test.ts
  - tests/architecture/hooks-translators.test.ts
  - tests/domain/components/hooks.test.ts
  - tests/domain/resolver-strict.test.ts
  - package.json
autonomous: true
requirements: [ADMIT-01, FLOOR-01]

must_haves:
  truths:
    - "BUCKET_A_EVENTS contains exactly 10 events; Stop then StopFailure appended after SessionEnd in deterministic lifecycle-tail registration order (ADMIT-01)"
    - "An empty or `*` Stop matcher (match-all) is admissible; any non-empty Stop matcher drops as `no-matcher-support`, never silently ignored (ADMIT-01, edge: empty)"
    - "StopFailure closed-set membership is exact whole-string byte-equality against the 10-value vocabulary — no case-folding, no normalization, no pipe-OR splitting; a compound `rate_limit|server_error` matcher drops as `closed-set` (ADMIT-01, edges: encoding + adjacency)"
    - "A StopFailure matcher exactly equal to a vocabulary value (e.g. `rate_limit`) is admitted; any value outside the 10-value set drops as `closed-set` (ADMIT-01, edge: adjacency)"
    - "StopFailure lands BOTH `NON_TOOL_EVENT_FIELDS` (non-null field label) and `NON_TOOL_EVENT_CLOSED_SETS` (10-value set) in the same edit; Stop lands ONLY the `null` sentinel and is omitted from the closed-set table like UserPromptSubmit (WR-04)"
    - "ClaudeHookEvent widens to 10 in lockstep with BUCKET_A_EVENTS via the `as const satisfies readonly ClaudeHookEvent[]` pin (ADMIT-01)"
    - "package.json peerDependencies `@earendil-works/pi-coding-agent` floor is `>=0.80.5` — declarative only, no runtime version detection (FLOOR-01, D-87-01, D-87-05)"
    - { statement: "The dispatch-key narrowing guard converts a would-be typecheck break at the two `TRANSLATORS`/`REQUIRED_EVENT_FIELDS` index sites (now that BucketAEvent ⊋ DispatchableEvent) into an observable debug-log + noop on the currently-unreachable non-dispatchable arm", verification: backstop }
  artifacts:
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts
    - extensions/pi-claude-marketplace/shared/concerns/hooks.ts
    - package.json
  key_links:
    - "`BUCKET_A_MEMBERS = new Set(BUCKET_A_EVENTS)` (hooks.ts:759) — admission auto-follows the tuple; no new admission code is needed for Stop/StopFailure to resolve available"
    - "`as const satisfies readonly ClaudeHookEvent[]` pin at hook-events.ts — BUCKET_A_EVENTS and ClaudeHookEvent must widen together or tsc breaks at the assertion site"
    - "WR-04 desync guard in hooks.ts (`tryNonToolEventTrip`) — a non-null StopFailure field with no closed-set entry throws HooksTableDesyncError; both tables land together"
  prohibitions:
    - statement: "Do NOT add pipe-OR (`|`) tokenization to the non-tool closed-set gate — it would silently change SessionStart behavior; StopFailure stays exact whole-string membership like SessionStart"
      status: flagged-unverified
    - statement: "Do NOT add a `Stop` entry to `NON_TOOL_EVENT_CLOSED_SETS` — Stop is the `null` sentinel only, omitted like UserPromptSubmit; a Stop closed-set entry is wrong"
      status: flagged-unverified
    - statement: "Do NOT build runtime version/capability detection for the peer floor — declarative peerDependencies range only (FLOOR-01, D-87-01)"
      status: flagged-unverified
    - statement: "Do NOT edit README.md, docs/hooks-compatibility.md, or docs/output-catalog.md — doc reconcile is Phase 89 (D-87-02); output-catalog defers unless catalog-uat breaks (D-87-06)"
      status: flagged-unverified
    - statement: "Do NOT add Stop/StopFailure translators, payload modules, dispatch wiring, or an agent_settled subscription — Phase 88; the dispatch tables stay keyed on DispatchableEvent (8) (D-87-04)"
      status: flagged-unverified
    - statement: "Do NOT record phase/milestone/plan/wave tokens in comments or test titles; decision (D-87-NN) and requirement (ADMIT-01, FLOOR-01, WR-04) IDs are the allowed anchors"
      status: flagged-unverified
---

<objective>
Grow the bucket-A admission set from 8 to 10 (ADMIT-01) and raise the peer floor
(FLOOR-01) — the coherent, observable admission boundary of this phase. `Stop`
takes the `null` no-matcher sentinel (any non-empty matcher reported as a
`no-matcher-support` group drop, never silently ignored); `StopFailure` takes
the closed 10-value error-type set through the existing
`NON_TOOL_EVENT_CLOSED_SETS` machinery (exact whole-string membership, same shape
as SessionStart). `ClaudeHookEvent` widens in lockstep. The
`@earendil-works/pi-coding-agent` peer floor rises `>=0.74.0` → `>=0.80.5`
(D-87-05: 0.80.4 was never published; `agent_settled` first ships in 0.80.5),
declarative only (D-87-01).

Purpose: This is the tracer slice — proving end-to-end that a plugin declaring
Stop/StopFailure alongside supported events resolves available, on production
machinery that already models both dispositions (null-sentinel = UserPromptSubmit
precedent; closed-set = SessionStart precedent). Because Plan 01 decoupled the
dispatch key domain and re-pointed the Stop-as-unsupported examples, the widen
lands cleanly.

Output: BUCKET_A_EVENTS = 10 with both matcher dispositions; the two index
sites carry the `DispatchableEvent` narrowing guard; drift-guards updated;
Stop/StopFailure unit dispositions + one inline resolver "available" proof; the
peer floor bumped.
</objective>

<edge_probe_accounting>
Spec-less probe fallback (no SPEC.md). The 8 deterministic edge-probe items
resolve as: 7 authored into `must_haves.truths` (ADMIT-01 × 4 here; ADMIT-02 × 3
in Plan 03) + 1 flagged assumption. No `backstop`-only edges. The FLOOR-01
`unclassified` row stays a flagged assumption (below). Additionally, one
non-probe backstop truth records the dispatch-key guard's whole-suite green
outcome. No probe item is silently dropped: 8 = 7 authored + 1 flagged.

Flagged assumptions (surfaced, not dismissed):
- FLOOR-01 (unclassified probe): assumes npm semver range semantics for
  `>=0.80.5`; there is NO runtime enforcement below the floor — "stay
  unsupported rather than degrading" is a declarative design choice (D-87-01),
  not capability detection. Below-floor behavior is off-contract exactly like
  every prior floor bump.
- [ASSUMED — field-name label] `StopFailure`'s Claude-side matcher target field
  is labelled `"error"` in `NON_TOOL_EVENT_FIELDS`. Per 87-RESEARCH Assumptions
  A2, the label is non-load-bearing: the gate compares the raw matcher string to
  the closed set regardless of the field-name label. Field name confirmation
  against the upstream hooks contract is deferred to Phase 89's doc reconcile.
</edge_probe_accounting>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-CONTEXT.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-RESEARCH.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-PATTERNS.md
@.planning/phases/87-bucket-a-admission-platform-floor/87-01-SUMMARY.md
@.claude/rules/typescript-comments.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Admission cutover — widen BUCKET_A_EVENTS 8→10 with both dispositions, wired end-to-end</name>
  <files>extensions/pi-claude-marketplace/domain/components/hook-events.ts, extensions/pi-claude-marketplace/shared/concerns/hooks.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, tests/architecture/hooks-supportability.test.ts, tests/architecture/hooks-translators.test.ts, tests/domain/resolver-strict.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (BUCKET_A_EVENTS tuple 36-45; NON_TOOL_EVENT_FIELDS Record<NonToolEvent> 109-115 with UserPromptSubmit:null; NON_TOOL_EVENT_CLOSED_SETS Partial<Record<BucketAEvent>> 151-168 with SessionStart:new Set(["startup","resume"]) as the analog and UserPromptSubmit omitted; the DISPATCHABLE_EVENTS subset added in Plan 01)
    - extensions/pi-claude-marketplace/shared/concerns/hooks.ts (ClaudeHookEvent union 58-66; _ToolEvent 68; the non-tool arm of HookSummaryEntry renders bare `<event>` automatically)
    - extensions/pi-claude-marketplace/domain/components/hooks.ts (tryNonToolEventTrip 803-830: null-sentinel → no-matcher-support drop; closed-set exact `.has()` → closed-set drop; WR-04 desync throw when field non-null but closed-set entry missing; BUCKET_A_MEMBERS at 759; the match-all short-circuit at tryMatcherTrip ~849)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts (buildPayload 232-267 indexing TRANSLATORS[claudeEvent] and REQUIRED_EVENT_FIELDS[claudeEvent]; the dispatchHookExec callsite at 196)
    - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts (TRANSLATORS[entry.claudeEvent] at 234)
    - tests/architecture/hooks-supportability.test.ts (the exact 8-tuple/order deepEqual at 42-59; SessionStart/SessionEnd disposition pins around 95-165)
    - tests/architecture/hooks-translators.test.ts (the `assert.equal(BUCKET_A_EVENTS.length, 8, ...)` at 176)
    - tests/domain/resolver-strict.test.ts (existing available / partial cases as the analog for the new inline Stop-available case)
  </read_first>
  <action>
    Append `"Stop"` then `"StopFailure"` to BUCKET_A_EVENTS after `"SessionEnd"`
    (deterministic lifecycle-tail order — turn-boundary events last; document the
    order choice in the tuple doc-comment per Claude's Discretion). Widen
    `ClaudeHookEvent` in shared/concerns/hooks.ts with the same two literals so
    the `as const satisfies readonly ClaudeHookEvent[]` pin holds.

    Add the two matcher dispositions in hook-events.ts, copying the in-file
    analogs. `Stop`: `null` in NON_TOOL_EVENT_FIELDS (copy the UserPromptSubmit
    row) and OMITTED from NON_TOOL_EVENT_CLOSED_SETS. `StopFailure`: field label
    `"error"` in NON_TOOL_EVENT_FIELDS (non-null) AND a
    `NON_TOOL_EVENT_CLOSED_SETS.StopFailure = new Set([...])` holding exactly the
    10 vocabulary values in this order: rate_limit, overloaded,
    authentication_failed, oauth_org_not_allowed, billing_error, invalid_request,
    model_not_found, server_error, max_output_tokens, unknown. Both StopFailure
    table entries MUST land in this same edit (WR-04 desync guard). Add derivation
    doc-comments citing D-58-06 / ADMIT-01 / SFAIL-03 and an [ASSUMED — field-name
    label] note on the `"error"` label; do NOT add pipe-OR handling and do NOT add
    version-history narration. Rewrite the touched doc-comments in hook-events.ts
    to drop the "eight v1.13-supported" / "v1.13" narration (comment policy),
    keeping the D-/requirement-ID anchors.

    Add a narrowing guard for the two dispatch index sites, now that
    `BucketAEvent ⊋ DispatchableEvent`. Introduce a `DISPATCHABLE_MEMBERS` Set +
    an `isDispatchableEvent(event: BucketAEvent): event is DispatchableEvent`
    type predicate in hook-events.ts (mirror `BUCKET_A_MEMBERS` / `TOOL_EVENT_MEMBERS`
    at hooks.ts:759-760). In dispatch-exec.ts `buildPayload` and
    async-rewake/registry.ts, guard the table index with `isDispatchableEvent`
    before use; on the (currently-unreachable) non-dispatchable arm, `hookDebugLog`
    and return the same noop shape the surrounding never-throws contract already
    uses (buildPayload's caller expects a payload — return early via the caller's
    noop path; match the existing `{ kind: "noop" }` / debug-log idiom). No Pi
    event routes Stop/StopFailure to dispatch in this phase, so this arm is a
    defensive belt, not live behavior.

    Update the two drift guards: in hooks-supportability.test.ts change the
    locked-order deepEqual to the 10-event tuple (append Stop, StopFailure); in
    hooks-translators.test.ts change `BUCKET_A_EVENTS.length` from 8 to 10 and add
    a companion `DISPATCHABLE_EVENTS.length === 8` assertion so the translator
    count and the admission-tuple length are pinned separately.

    Add ONE inline end-to-end proof in resolver-strict.test.ts: a hooks config
    declaring a `Stop` group (match-all matcher) alongside a supported bucket-A
    event resolves `available` with no `{unsupported hooks}` drop for Stop. This
    is the tracer's real end-to-end check (config → partitionHooks → resolver
    verdict).
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/architecture/hooks-supportability.test.ts tests/architecture/hooks-translators.test.ts tests/domain/resolver-strict.test.ts tests/bridges/hooks/dispatch-exec.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./extensions/pi-claude-marketplace/domain/components/hook-events.ts').then(m=>{console.log(m.BUCKET_A_EVENTS.length)})"` prints 10 (or the supportability test's locked-order deepEqual asserts the 10-tuple ending in Stop, StopFailure).
    - `grep -n "Stop\|StopFailure" extensions/pi-claude-marketplace/domain/components/hook-events.ts` shows `Stop: null` in NON_TOOL_EVENT_FIELDS, `StopFailure: "error"` in NON_TOOL_EVENT_FIELDS, a `StopFailure` closed set of 10 values, and NO `Stop:` key inside NON_TOOL_EVENT_CLOSED_SETS.
    - `grep -c "Stop\|StopFailure" extensions/pi-claude-marketplace/shared/concerns/hooks.ts` shows both literals added to ClaudeHookEvent.
    - A unit/inline assertion proves: a non-empty Stop matcher yields a `no-matcher-support` drop; an empty/`*` Stop matcher is admissible; a `StopFailure` matcher of `rate_limit` is admitted while `rate_limit|server_error` and `bogus_value` drop as `closed-set`. (These may live in this task's inline resolver proof or be deferred to Task 2's unit cases — at minimum the resolver-available inline case passes here.)
    - `grep -rn "isDispatchableEvent" extensions/pi-claude-marketplace/bridges/hooks/` shows the guard applied at both index sites; the dispatch-exec suite passes with unchanged translator behavior for the 8 dispatchable events.
    - `npm run typecheck` exits 0; no `Record<BucketAEvent>` totality site demands a Stop/StopFailure translator.
    - The 8-entry `pi.on` subscription assertion in hooks-dispatch tests is untouched (no agent_settled subscription added).
  </acceptance_criteria>
  <reversibility rating="reversible">D-87-04 keeps dispatch decoupled; Phase 88 folds DispatchableEvent back into BucketAEvent. The tuple widen and floor bump are plain reversible edits.</reversibility>
  <done>BUCKET_A_EVENTS = 10 with Stop null-sentinel + StopFailure 10-value closed set (both tables, WR-04); ClaudeHookEvent widened in lockstep; the two dispatch index sites carry the DispatchableEvent guard; drift guards updated; an inline Stop-config-resolves-available proof passes; typecheck and the touched arch/resolver suites are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Matcher-disposition unit pins + peer-floor bump</name>
  <files>tests/domain/components/hooks.test.ts, tests/architecture/hooks-supportability.test.ts, package.json</files>
  <behavior>
    - Stop with a non-empty matcher → partition drop `{ cond: "no-matcher-support" }` (group-level), never silently ignored.
    - Stop with `""` / `"*"` matcher → admissible (match-all short-circuit), no drop.
    - StopFailure matcher exactly `rate_limit` (any single vocabulary value) → admitted.
    - StopFailure matcher `bogus_value` (outside the 10-value set) → drop `{ cond: "closed-set" }`.
    - StopFailure matcher `rate_limit|server_error` (pipe compound) → drop `{ cond: "closed-set" }` (exact whole-string membership; no pipe splitting).
    - Architecture pins: NON_TOOL_EVENT_FIELDS.Stop === null; NON_TOOL_EVENT_FIELDS.StopFailure is a non-null label; NON_TOOL_EVENT_CLOSED_SETS.StopFailure holds exactly the 10 values; NON_TOOL_EVENT_CLOSED_SETS has no Stop entry; the two tables are in sync for StopFailure (WR-04).
  </behavior>
  <read_first>
    - tests/domain/components/hooks.test.ts (SessionStart closed-set cases + UserPromptSubmit no-matcher-support cases as the exact analogs for the new Stop/StopFailure cases; the drop-shape assertions the suite already uses)
    - tests/architecture/hooks-supportability.test.ts (the SessionStart / UserPromptSubmit disposition pins around 95-165 as the analog for the new Stop/StopFailure pins)
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (the dispositions landed in Task 1)
    - package.json (peerDependencies at line 55-60; the `@earendil-works/pi-coding-agent` range at line 56)
    - 87-RESEARCH.md Pitfall 3 (no pipe-OR) and Pitfall 4 (WR-04 both tables)
  </read_first>
  <action>
    Author the disposition unit cases in hooks.test.ts covering the Behavior
    block above (Stop no-matcher-support drop for a non-empty matcher; Stop
    admissible for match-all; StopFailure admit on an in-vocabulary value;
    StopFailure closed-set drop on an out-of-vocabulary value AND on a pipe
    compound). Copy the assertion shapes from the neighboring SessionStart /
    UserPromptSubmit cases so the drop `cond` values and structural shapes match
    the suite's conventions. Add the architecture disposition pins in
    hooks-supportability.test.ts for Stop (field is null; omitted from closed
    sets) and StopFailure (non-null field; closed set is exactly the 10 values;
    tables in sync). Test titles follow comment policy — anchor on ADMIT-01 /
    SFAIL-03 / WR-04 / D-58-06, no phase or Pitfall tokens.

    Bump the peer floor in package.json: change
    `"@earendil-works/pi-coding-agent": ">=0.74.0"` to `">=0.80.5"` in
    `peerDependencies` (FLOOR-01, D-87-05 — 0.80.4 was never published,
    `agent_settled` first ships in 0.80.5). Declarative only (D-87-01): no
    runtime detection, no other file. The devDependency (`^0.82.1`) already
    satisfies the new floor, so no lockfile churn is expected; if `npm run check`
    reports a stale local dev tree, note it (environment, not a regression) but
    do not downgrade the floor.
  </action>
  <verify>
    <automated>node --test tests/domain/components/hooks.test.ts tests/architecture/hooks-supportability.test.ts && grep -n "0.80.5" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "@earendil-works/pi-coding-agent" package.json` shows the `peerDependencies` entry as `">=0.80.5"` and the `devDependencies` entry unchanged; `grep -c "0.74.0" package.json` returns 0.
    - hooks.test.ts contains cases asserting: Stop non-empty matcher → `no-matcher-support`; Stop match-all → admissible; StopFailure in-vocabulary → admitted; StopFailure out-of-vocabulary and pipe-compound → `closed-set`.
    - hooks-supportability.test.ts pins: NON_TOOL_EVENT_FIELDS.Stop === null, NON_TOOL_EVENT_FIELDS.StopFailure non-null, NON_TOOL_EVENT_CLOSED_SETS.StopFailure has size 10 and no Stop key.
    - `npx prettier --check package.json` passes (formatting preserved).
    - Both listed suites are green.
  </acceptance_criteria>
  <done>The Stop null-sentinel and StopFailure closed-set dispositions are pinned at both unit and architecture level; the peer floor reads `>=0.80.5` declaratively; the listed suites and formatting checks are green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan creates (excluded from cross-plan drift verification):
- Widened `BUCKET_A_EVENTS` (now 10 members: adds `Stop`, `StopFailure`) and the
  new table entries `NON_TOOL_EVENT_FIELDS.Stop`, `NON_TOOL_EVENT_FIELDS.StopFailure`,
  `NON_TOOL_EVENT_CLOSED_SETS.StopFailure` in `hook-events.ts`.
- Widened `ClaudeHookEvent` union (adds `Stop`, `StopFailure`) in
  `shared/concerns/hooks.ts`.
- `DISPATCHABLE_MEMBERS` set + `isDispatchableEvent` type-guard in `hook-events.ts`.
- New test cases (Stop null-sentinel drop, StopFailure closed-set admit/drop,
  disposition pins, inline Stop-resolves-available) — no new public symbols.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin `hooks.json` matcher strings → `partitionHooks` / `tryNonToolEventTrip` | Untrusted plugin-authored matcher values for the two newly-admitted events cross here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-02 | Tampering | StopFailure closed-set gate | low | mitigate | Exact whole-string membership against a 10-value closed set (D-58-06 strict-supportability); out-of-vocabulary and pipe-compound matchers drop rather than translate to a no-op filter. No new parse surface — hooks.json was already schema-validated. |
| T-87-03 | Denial of Service | matcher validation | low | accept | The widened closed set is more restrictive than accepting arbitrary matchers; the existing regex path already sidesteps catastrophic backtracking (S5852). No change. |

No package installs (the floor bump is a semver-range edit, not an install), no
network, no auth/crypto/secrets — no `T-87-SC` supply-chain checkpoint applies.
</threat_model>

<verification>
- `npm run typecheck` green — BUCKET_A_EVENTS = 10 with no `Record<BucketAEvent>`
  totality site demanding Stop/StopFailure translators.
- `npm run check` green (typecheck + lint + format + unit + integration),
  including `tests/architecture/catalog-uat.test.ts` passing WITHOUT any edit to
  `docs/output-catalog.md` (D-87-06 — the doc's Stop example defers to Phase 89
  unless this test breaks; confirm it does not).
- The 8-entry `pi.on` subscription assertion stays green and untouched (no
  agent_settled subscription in this phase).
- `grep -c "0.74.0" package.json` returns 0; `grep -c ">=0.80.5" package.json`
  returns 1.
</verification>

<success_criteria>
BUCKET_A_EVENTS is exactly 10 in locked order; Stop drops any non-empty matcher
as `no-matcher-support` and admits match-all; StopFailure admits its 10-value
vocabulary by exact whole-string membership and drops everything else (including
pipe compounds) as `closed-set`, with both StopFailure tables in sync (WR-04);
ClaudeHookEvent is widened in lockstep; the peer floor is `>=0.80.5` declaratively
(D-87-01/D-87-05); the dispatch tables remain keyed on the 8-event
DispatchableEvent subset; `npm run check` is green.
</success_criteria>

<output>
Create `.planning/phases/87-bucket-a-admission-platform-floor/87-02-SUMMARY.md` when done.
</output>
