---
phase: 88-agent-settled-dispatcher-stop-contract-stopfailure
plan: 04
type: execute
wave: 4
depends_on: ["88-03"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
  - extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts
  - tests/bridges/hooks/settle.test.ts
  - tests/bridges/hooks/payloads/stop-failure.test.ts
autonomous: true
requirements: [SFAIL-01, SFAIL-02, SFAIL-03]
must_haves:
  truths:
    - "StopFailure fires observation-only at settle on `stopReason` `error` and `length`: the settle handler runs the StopFailure bucket and DISCARDS the result entirely — no decision machinery, no re-entry, no loop guard (SFAIL-01)."
    - "The StopFailure stdin envelope carries `error` (the classified type), optional `error_details`, and `last_assistant_message` = the rendered error text from Pi's `errorMessage` (SFAIL-02)."
    - "Classification (D-88-02) is errorMessage-only: a substring table over Pi's rendered `errorMessage` maps into the closed 10-value vocabulary with `unknown` as the in-vocabulary fallback; `stopReason:\"length\"` maps deterministically to `max_output_tokens` (NOT from errorMessage). No `after_provider_response` subscription, no HTTP-status cell (SFAIL-03)."
    - "The classifier output is always a member of the closed 10-value set {rate_limit, overloaded, authentication_failed, oauth_org_not_allowed, billing_error, invalid_request, model_not_found, server_error, max_output_tokens, unknown}."
    - "Edge SFAIL-01 empty: an empty StopFailure bucket -> no dispatch; observation-only means even when hooks run their output/exit code is discarded (no re-entry ever)."
    - "Edge SFAIL-01 encoding: StopFailure fires on exact `stopReason` equality (`error` or `length`) only — never on `stop`/`aborted`/`toolUse`."
    - "Edge SFAIL-02 empty: a missing/empty `errorMessage` yields `last_assistant_message: \"\"`, `error: \"unknown\"` classification, and `error_details` omitted when absent."
    - "Edge SFAIL-02 encoding: `error` is the classified closed-set token (equality against the vocab); `last_assistant_message` is the verbatim `errorMessage` string."
    - "Edge SFAIL-03 empty: an empty `errorMessage` classifies `unknown`; `stopReason:\"length\"` classifies `max_output_tokens` regardless of `errorMessage` content."
    - "Edge SFAIL-03 encoding: substring matching is case-INSENSITIVE (grounded in Pi's own `/.../i` classifier regexes); the closed-set matcher membership itself is exact byte-equality (Phase 87 NON_TOOL_EVENT_CLOSED_SETS)."
  artifacts:
    - "Filled StopFailure arm in settle.ts (runStopFailure: reduceBucket + discard)"
    - "classifyStopFailure(errorMessage, stopReason) in payloads/stop-failure.ts"
    - "Finalized StopFailureStdin envelope + translate"
    - "tests/bridges/hooks/payloads/stop-failure.test.ts (classifier fixtures + envelope shape)"
  key_links:
    - "stopReason error/length -> runStopFailure -> reduceBucket('StopFailure') with the result discarded (observation-only)."
    - "errorMessage substring table + length-deterministic map -> classified error token -> StopFailureStdin.error + closed-set membership."
    - "classifier fallback -> unknown (in-vocabulary), so an unmatched errorMessage never escapes the closed set."
---

<objective>
Fill the StopFailure arm the tracer stubbed: an observation-only dispatch on
`error`/`length` settle endings (result discarded — no re-entry, no loop
guard), the StopFailure stdin envelope, and the errorMessage-only classifier
that maps into the closed 10-value error-type vocabulary with `unknown` as the
in-vocabulary fallback and a deterministic `length -> max_output_tokens` map.

Purpose: without StopFailure, `error`/`length` endings are invisible to
plugins — matching neither Claude nor a defensible partial. This closes the
contract at the same fidelity bar as Stop, observation-only per upstream.
Output: `runStopFailure` in settle.ts, `classifyStopFailure` + the envelope in
payloads/stop-failure.ts, fixture-tested.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-CONTEXT.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-PATTERNS.md
@docs/research/issue-103-stop-stopfailure-promotion.md
@.claude/rules/typescript-comments.md
</context>

<artifacts_produced>
- Fills `runStopFailure` in `bridges/hooks/settle.ts` (reduceBucket + discard).
- `classifyStopFailure(errorMessage: string, stopReason: StopReason)` + finalized `StopFailureStdin` + `translate` in `bridges/hooks/payloads/stop-failure.ts`.
- New test `tests/bridges/hooks/payloads/stop-failure.test.ts`; extends `settle.test.ts`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StopFailure observation-only arm + envelope (SFAIL-01, SFAIL-02)</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/settle.ts, extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts, tests/bridges/hooks/settle.test.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts (the `runStopFailure` stub from plan 01 the gate routes error/length into; the reduceBucket import)
    - extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts (reduceBucket 177-229 — the reduced result is DISCARDED for StopFailure)
    - extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts (the thin plan-01 draft)
    - extensions/pi-claude-marketplace/bridges/hooks/payloads/session-end.ts (common-fields template)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ StopFailure arm — observation-only; payload = error + error_details + last_assistant_message)
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md (Pattern 3 discard-result; SFAIL-02 envelope)
  </read_first>
  <behavior>
    - stopReason "error" -> runStopFailure runs the StopFailure bucket and returns void; NO sendMessage, NO stop_hook_active change, NO counter change.
    - stopReason "length" -> same observation-only path.
    - even when a StopFailure hook returns {decision:"block"} or exits 2, there is NO re-entry (result discarded).
    - empty StopFailure bucket -> no dispatch.
    - envelope: translate({error:"rate_limit", error_details:"...", last_assistant_message:"boom"}, ctx) -> {session_id, transcript_path, cwd, hook_event_name:"StopFailure", error:"rate_limit", error_details:"...", last_assistant_message:"boom"}.
    - missing error_details -> key omitted; empty errorMessage -> last_assistant_message "".
  </behavior>
  <action>
    Fill `runStopFailure(cachedMessage, classifiedError, ctx, pi)` in settle.ts:
    build the synthetic StopFailure event ({error, error_details?,
    last_assistant_message}), call `reduceBucket("StopFailure", syntheticEvent,
    ctx, pi, () => true)`, and DISCARD the return (observation-only — SFAIL-01).
    Do not touch stop_hook_active, the counter, or sendMessage. The gate (plan
    01) already routes `error` -> `runStopFailure(msg, classify(...), ...)` and
    `length` -> `runStopFailure(msg, "max_output_tokens", ...)`; wire task 2's
    classifier into the `error` arm.

    Finalize `payloads/stop-failure.ts`: `StopFailureStdin` = `session_id`,
    `transcript_path`, `cwd`, `hook_event_name: "StopFailure"`, `error: string`,
    `error_details?: string`, `last_assistant_message: string`. `translate`
    reads the common three from `TranslationContext` and the StopFailure fields
    from the synthetic event; omit `error_details` when absent. `last_assistant_message`
    is the verbatim `errorMessage` (or "" when absent).

    Extend settle.test.ts: assert error/length route to the observation arm with
    zero sendMessage / zero loop-state change even when the StopFailure hook
    returns a block or exits 2; empty bucket -> no dispatch.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/hooks/settle.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `error`/`length` dispatch StopFailure with the result discarded; a blocking/exit-2 StopFailure hook produces NO re-entry and NO loop-state mutation.
    - StopFailureStdin carries exactly the seven fields (error_details omitted when absent); last_assistant_message is the verbatim errorMessage.
    - No sendMessage on any StopFailure path.
  </acceptance_criteria>
  <done>StopFailure rides the settle dispatcher observation-only; the envelope is final and shape-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: errorMessage-only classifier into the closed 10-value vocab (SFAIL-03, D-88-02)</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/payloads/stop-failure.ts, tests/bridges/hooks/payloads/stop-failure.test.ts</files>
  <read_first>
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md ("StopFailure classifier grounding" — the evidence-based substring table + Pi's own /i regex sources; keep the table SMALL and fixture-tested)
    - extensions/pi-claude-marketplace/domain/components/hook-events.ts (NON_TOOL_EVENT_CLOSED_SETS.StopFailure at 253-264 — the authoritative closed 10-value set the classifier output MUST be a member of; exact whole-string membership, no case-folding on the MATCHER side)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ StopFailure arm — length -> max_output_tokens; error best-effort; unknown fallback; no after_provider_response)
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-CONTEXT.md (D-88-02 — errorMessage-only; declined the firming variant)
  </read_first>
  <behavior>
    - classifyStopFailure("", "error") -> "unknown".
    - classifyStopFailure(anything, "length") -> "max_output_tokens" (deterministic; errorMessage ignored).
    - "Rate limit exceeded (429)" -> "rate_limit" (case-insensitive substring).
    - "Overloaded" / "...529..." -> "overloaded".
    - "Authentication failed for ..." / "401" -> "authentication_failed".
    - "billing" / "quota exceeded" / "insufficient_quota" / "usage limit" -> "billing_error".
    - "503 service unavailable" / "internal error" -> "server_error".
    - "model not found" -> "model_not_found".
    - "invalid request" / "400" -> "invalid_request".
    - an org-policy string with no observed substring -> "unknown" (oauth_org_not_allowed stays in the vocab but is not synthesized from a guessed substring — A2).
    - every return value is a member of the closed 10-value set.
  </behavior>
  <action>
    Add `classifyStopFailure(errorMessage: string, stopReason: StopReason):
    string` to payloads/stop-failure.ts. If `stopReason === "length"`, return
    `"max_output_tokens"` immediately (deterministic; do NOT consult
    errorMessage). Otherwise run a SMALL, ordered, case-INSENSITIVE substring
    table over `errorMessage` grounded in Pi's own dist regexes (88-RESEARCH.md
    "StopFailure classifier grounding"), mapping into the closed vocab; fall
    back to `"unknown"`. Keep entries evidence-based (billing/quota/usage-limit
    -> billing_error; rate limit/429/too many requests -> rate_limit;
    overloaded/529 -> overloaded; authentication failed/401/403 ->
    authentication_failed; 5xx/server error/internal error/service unavailable
    -> server_error; model not found -> model_not_found; invalid request/400 ->
    invalid_request). Do NOT invent a substring for `oauth_org_not_allowed`
    (A2 — org-policy errors classify `unknown`). Do NOT subscribe to
    `after_provider_response` or add an HTTP-status cell (D-88-02). Add a test
    assertion (or a runtime narrow) that the return type is a member of the
    closed set imported from hook-events.ts so the classifier can never escape
    the vocab.

    Write `tests/bridges/hooks/payloads/stop-failure.test.ts`: the classifier
    fixtures above PLUS the envelope shape from task 1, and a membership
    assertion over `NON_TOOL_EVENT_CLOSED_SETS.StopFailure` for every fixture
    output.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/bridges/hooks/payloads/stop-failure.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && node --test tests/architecture/hooks-dispatch.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `length` -> `max_output_tokens` regardless of errorMessage; empty/unmatched errorMessage -> `unknown`.
    - Every classifier fixture output is a member of `NON_TOOL_EVENT_CLOSED_SETS.StopFailure` (asserted).
    - No new `pi.on` subscription is added this plan — the DISP-01 subscription count stays at 11 from plan 03, and the classifier consumes only its two params (errorMessage, stopReason); the declined provider-response firming variant is not resurrected (D-88-02).
    - The substring table is small and each entry is fixture-covered.
  </acceptance_criteria>
  <done>StopFailure error-type classification maps errorMessage into the closed 10-value vocab with unknown fallback and a deterministic length map, fixture-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi `errorMessage` string -> classifier -> `error` matcher token | untrusted/provider-rendered error text drives classification |
| StopFailure hook stdout/exit -> (discarded) | observation-only; output cannot influence control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-03 | Tampering | errorMessage substring spoofing driving misclassification | low | accept | Non-load-bearing — no first-party plugin consumes StopFailure; the closed vocab + `unknown` fallback bounds impact to a matcher-filter mislabel with no control-flow effect (observation-only). |
| T-88-07 | (correctness) | classifier escaping the closed 10-value vocabulary | low | mitigate | The return is asserted to be a member of `NON_TOOL_EVENT_CLOSED_SETS.StopFailure`; `unknown` is the total fallback so no input escapes the set. |
</threat_model>

<verification>
- `node --test tests/bridges/hooks/settle.test.ts tests/bridges/hooks/payloads/stop-failure.test.ts` green.
- `node --test tests/architecture/hooks-dispatch.test.ts` green with the subscription count unchanged at 11 (no new provider-response subscription — D-88-02).
- `npm run typecheck` green.
</verification>

<success_criteria>
- SFAIL-01..03 delivered: observation-only StopFailure arm, envelope, and errorMessage-only classifier into the closed vocab with `unknown` fallback + deterministic `length` map.
- Six SFAIL edge criteria pinned; classifier output provably in-vocabulary.
</success_criteria>

<output>
Create `.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-04-SUMMARY.md` when done.
</output>
