---
phase: 88-agent-settled-dispatcher-stop-contract-stopfailure
plan: 05
type: execute
wave: 5
depends_on: ["88-04"]
files_modified:
  - tests/live-uat/stop-canary.mjs
  - tests/live-uat/README.md
autonomous: true
user_setup:
  - service: pi-cli
    why: "Live runtime UAT drives a real Pi >= 0.80.5 session to answer the four implementation-time verification questions (D-88-03b)."
    env_vars:
      - name: PI_CODING_AGENT_DIR
        source: "Set to $(pwd)/tmp/pi-uat/agent (existing sandbox) so the UAT session does not touch the developer's real Pi state."
requirements: [STOP-01, STOP-07]
must_haves:
  truths:
    - "A scripted canary harness drives a real Pi >= 0.80.5 session with a ralph-wiggum-shaped Stop-only plugin installed and asserts the block re-entry path fires and the 8-consecutive-block cap trips exactly once (D-88-03b item 4)."
    - "The scriptable checks (block re-entry + 8-block cap on a single turn) run automated; the unscriptable residue (abort mid-tool-call, queued-message settle timing, sendMessage-re-entry-does-not-fire-input) is surfaced as explicit human_verification items so the verifier routes human_needed, never a silent pass (D-88-03b)."
    - "Verification item 1 (STOP-01): whether `agent_settled` fires after a user abort mid-tool-call and whether the final assistant message reliably carries `stopReason:\"aborted\"` on every interrupt path — recorded as a human_verification item with exact repro steps."
    - "Verification item 2 (STOP-01): settle timing with queued user messages (upstream fires Stop per response; settle fires after queue drain) — any divergence documented as a human_verification item."
    - "Verification item 3 (STOP-07): a bridge `sendMessage` custom-message re-entry does NOT itself fire `input` (the stop_hook_active reset must not self-clear) — recorded as a human_verification item."
  artifacts:
    - "tests/live-uat/stop-canary.mjs (scripted canary driver + assertions)"
    - "tests/live-uat/README.md (repro steps + the human_verification checklist for the unscriptable items)"
  key_links:
    - "ralph-wiggum-shaped Stop plugin -> real Pi session -> observable block re-entry + 8-block cap (the end-to-end proof the mocked settle tests approximate)."
    - "unscriptable runtime timing questions -> explicit human_verification items -> verifier routes human_needed (not silently passed)."
---

<objective>
D-88-03: the verification harness is BOTH mocked and live in-phase — plans
01-04 carry the mocked-Pi event-sequence half; this plan is the live half.
Answer the four implementation-time verification questions (D-88-03b) against a
real Pi >= 0.80.5 runtime: the scriptable ones (ralph-wiggum canary exercising
the block path + 8-block cap) as an autonomous harness with assertions, and the
unscriptable timing/interrupt questions as explicit human_verification items so
the verifier routes `human_needed` rather than silently passing.

Purpose: the mocked settle tests (plans 01-04) prove the dispatcher logic
offline; this plan proves the settle FIRE-POINT behaves as designed on real Pi
(agent_settled cadence on abort/queue, input-reset not self-clearing), which no
fake `pi` can establish.
Output: a scripted canary harness + a README recording the human_verification
checklist.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-CONTEXT.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md
@.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-VALIDATION.md
@docs/research/issue-103-stop-stopfailure-promotion.md
</context>

<artifacts_produced>
- `tests/live-uat/stop-canary.mjs` — a node driver that installs this extension + a ralph-wiggum-shaped Stop-only plugin into the `PI_CODING_AGENT_DIR` sandbox, drives a Pi session, and asserts the block re-entry + 8-block-cap observables.
- `tests/live-uat/README.md` — repro steps + the human_verification checklist for the three unscriptable items.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Scripted ralph-wiggum canary — block re-entry + 8-block cap (STOP-07, D-88-03b item 4)</name>
  <files>tests/live-uat/stop-canary.mjs, tests/live-uat/README.md</files>
  <precondition>A live `pi` CLI >= 0.80.5 is on PATH (verified 0.80.10) and the `tmp/pi-uat/agent/` PI_CODING_AGENT_DIR sandbox exists; `tests/fixtures/ralph-wiggum-hooks.json` (a Stop-only always-block manifest) is present.</precondition>
  <read_first>
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md ("Live UAT" section + "Environment Availability" — the sandbox, the live pi version, the ralph-wiggum fixture)
    - tests/fixtures/ralph-wiggum-hooks.json (the canary manifest shape)
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ Implementation-time verifications — the four items; § The one irreducible divergence — the extra turn boundary is expected, NOT a failure)
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts (the block re-entry + cap behavior being exercised)
  </read_first>
  <action>
    Author `tests/live-uat/stop-canary.mjs`: set
    `PI_CODING_AGENT_DIR=$(pwd)/tmp/pi-uat/agent`, install this extension + a
    ralph-wiggum-shaped Stop-only plugin (a Stop hook that always returns
    `{"decision":"block","reason":"keep going"}`, and writes a per-invocation
    marker — e.g. appends a line to a temp file — so re-entry is observable),
    drive a single Pi turn to completion, and assert: (a) the Stop hook fired
    and the agent re-entered at least once (block re-entry works end-to-end);
    (b) after 8 consecutive blocks the override cap trips — the marker file
    shows the block invocations bounded and the run terminates rather than
    livelocking, and the cap-trip warning is emitted. The one irreducible
    divergence (re-entry starts a NEW turn -> an extra transcript turn boundary)
    is EXPECTED — assert on the block count + cap, NOT on same-turn folding.
    Keep the harness runnable standalone (`node tests/live-uat/stop-canary.mjs`)
    and NOT wired into `npm run check` (it needs the live runtime + sandbox).
    Write `tests/live-uat/README.md` with the exact repro command, the sandbox
    setup, and the pass criteria.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace && PI_CODING_AGENT_DIR=$(pwd)/tmp/pi-uat/agent node tests/live-uat/stop-canary.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The harness exits 0 with the ralph-wiggum canary showing block re-entry AND the 8-block cap tripping exactly once (bounded block count, run terminates, cap warning observed).
    - The harness is standalone and documented in tests/live-uat/README.md; it is NOT added to `npm run check`.
    - If the live `pi`/sandbox precondition is unmet, the harness exits non-zero with a clear "live runtime required" message (so the verifier routes human_needed rather than a false pass).
  </acceptance_criteria>
  <done>The ralph-wiggum canary proves block re-entry + the 8-block cap on real Pi, standalone and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the three unscriptable verification items as human_needed (STOP-01, STOP-07, D-88-03b items 1-3)</name>
  <files>tests/live-uat/README.md</files>
  <read_first>
    - docs/research/issue-103-stop-stopfailure-promotion.md (§ Implementation-time verifications items 1-3)
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-RESEARCH.md (Pitfall 4 self-clear; A4 agent_end precedes agent_settled; Assumptions Log)
    - .planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-VALIDATION.md (Manual-Only Verifications row)
  </read_first>
  <action>
    Append a "Human verification checklist" section to tests/live-uat/README.md
    with exact, repeatable manual steps + expected observations for the three
    items that cannot be fully scripted, so a human driver can confirm each and
    the verifier routes `human_needed` instead of silently passing: (1) abort a
    turn mid-tool-call and observe whether `agent_settled` fires and whether the
    final assistant message carries `stopReason:"aborted"` (Stop must NOT fire —
    STOP-01); (2) queue two user messages and observe settle timing (upstream
    fires Stop per response; settle fires after the queue drains — document any
    divergence); (3) trigger a Stop block re-entry and confirm the bridge's
    `sendMessage` custom message does NOT fire an `input` event (the
    stop_hook_active flag must survive re-entry and clear only on a genuine user
    input — STOP-07). Each item states the exact repro, the expected result, and
    the failure signature.
  </action>
  <verify>
    <human-check>
      Item 1 (abort mid-tool-call): with a real Pi >= 0.80.5 session driving this extension, start a turn that calls a slow tool, interrupt it mid-call, and confirm the final message carries stopReason "aborted" and NO Stop hook fired. Expected: Stop suppressed on user interrupt.
      Item 2 (queued messages): queue two user messages during an active turn; confirm settle fires once after the queue drains (document any per-response divergence). Expected: one settle-time Stop dispatch after drain.
      Item 3 (input self-clear): cause a Stop block re-entry; confirm the injected sendMessage does NOT clear stop_hook_active (no `input` fired by the bridge message) and that a subsequent genuine user prompt DOES clear it. Expected: flag survives re-entry, clears on real input.
    </human-check>
  </verify>
  <acceptance_criteria>
    - tests/live-uat/README.md carries a human verification checklist with exact repro + expected + failure signature for all three items.
    - The three items are framed so the verifier records them as human_needed (unscripted runtime observations), never silently passed.
  </acceptance_criteria>
  <done>The three unscriptable runtime questions are explicit human_verification items with repro steps; the verifier routes human_needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Pi session -> sandbox `PI_CODING_AGENT_DIR` | UAT must not touch the developer's real Pi state |
| ralph-wiggum canary (always-block hook) -> agent loop | the canary is the DoS repro; the cap is what must contain it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-02 | Denial of Service | the ralph-wiggum always-block canary driving unbounded re-entry | high | mitigate | This plan is the live PROOF that the 8-block cap (T-88-02 mitigation from plan 03) actually contains the livelock on real Pi, not just in mocks. |
| T-88-08 | Tampering | UAT writing to the developer's real Pi agent dir | low | mitigate | Harness pins `PI_CODING_AGENT_DIR` to the `tmp/pi-uat/agent` sandbox; refuses to run if unset. |
</threat_model>

<verification>
- `PI_CODING_AGENT_DIR=$(pwd)/tmp/pi-uat/agent node tests/live-uat/stop-canary.mjs` exits 0 (block re-entry + cap proven) when the live-pi precondition holds; exits non-zero with a "live runtime required" message otherwise.
- The three human_verification items are recorded in tests/live-uat/README.md for the verifier to route human_needed.
</verification>

<success_criteria>
- The ralph-wiggum canary proves block re-entry + the 8-block cap on real Pi (D-88-03b item 4).
- The three unscriptable items (abort, queue timing, input self-clear) are explicit human_verification items so the verifier routes human_needed.
</success_criteria>

<output>
Create `.planning/phases/88-agent-settled-dispatcher-stop-contract-stopfailure/88-05-SUMMARY.md` when done.
</output>
