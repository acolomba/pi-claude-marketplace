---
phase: 260815-p25-implement-hooks-cycle-removal-from-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
  - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
  - extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts
  - extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts
  - extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts
  - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
  - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  - package.json
  - .planning/codebase/ARCHITECTURE.md
  - .planning/BACKLOG.md
  - .planning/STATE.md
autonomous: true
requirements: [FLOW-02, NFR-6]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npx fallow dead-code --circular-deps` reports 0 circular dependencies"
    - "`npm run check` is green, including the widened fallow gate"
    - "No file under `tests/` is modified by this change"
    - "`event-router.ts` still exports `currentEpoch`, `getRoutingBucket`, `appendPendingSessionStartContext`, `RoutingEntry`, `PendingSessionStartContext` so the 17 test files reaching its seams need no edit"
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts
  key_links:
    - "`routing-state.ts` imports only `domain/`, `shared/`, and same-zone `if-field/` -- it must never import a knot module, or the cycle returns"
    - "The `fallow` script flag addition and the cycle removal must land in the SAME commit, or the first commit fails on 8 inherited cycles"
---

<objective>
Ship the proven `bridges/hooks/` circular-dependency removal from spikes 018/019a/020, and close the local cycle gate in the same commit.

Purpose: 8 import cycles in `bridges/hooks/` are why `import-x/no-cycle` stops at `orchestrators/` and why `npm run fallow` runs without `--circular-deps`. A developer can introduce a cycle today and see a green local `npm run check` until the pull request opens. This closes BACKLOG FLOW-02.

Output: a new leaf module `bridges/hooks/routing-state.ts`, five repointed importers, a widened `fallow` gate, and updated ARCHITECTURE.md / BACKLOG.md.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/skills/spike-findings-pi-claude-marketplace/references/hooks-cycle-removal.md
@.claude/rules/typescript-comments.md
@CLAUDE.md
</context>

<preflight_findings>
Verified before planning. Read this before starting -- two findings change the shape of the work versus the blueprint's prose.

1. **`full-candidate.diff` applies cleanly.** `git apply --check .planning/spikes/020-hooks-cycle-gate-closure/full-candidate.diff` exits 0 against the current tree (branch `features/hooks-cycle-removal`, clean worktree). Diffstat: 7 files, +39/-97.

2. **The diff does NOT create `routing-state.ts`.** It only touches the 6 knot files plus `package.json`, and every one of its hunks assumes `./routing-state.ts` already resolves. The leaf module must be created FIRST (Task 1) or the diff applies and then typecheck fails on 5 unresolved specifiers. Source: `.planning/spikes/019-a-hooks-cycle-leaf-extraction/routing-state.ts` (3.5K, identical copy also under the skill's `sources/`).

3. **The diff leaves 6 orphaned doc-comment blocks in `event-router.ts`.** It deletes declarations while keeping the block comments that documented them. Nothing in `npm run check` catches a dangling block comment -- it is valid TypeScript, Prettier accepts it, and no ESLint rule fires. The measured "+39/-97" does not account for this. Task 2 fixes it explicitly.

4. **The spike's `routing-state.ts` drops per-field documentation.** Its `RoutingEntry` / `CacheEntry` fields are bare, so the D-60-01, D-60-04, MATCH-03, and D-61-02 traceability anchors that live on those fields today would be destroyed by the move. `.claude/rules/typescript-comments.md` names these IDs as encouraged anchors. Task 1 carries them across.

5. **The re-export question is settled by the artifact, not re-opened here.** The candidate diff repoints the five *knot-internal* importers at the leaf (that is what breaks the cycle) AND appends a re-export block to `event-router.ts` for the external surface. That is why zero test files needed modification. Apply the diff as written; do not add or remove names from that block.

6. **No `.fallowrc.json` change is needed.** `bridges-hooks` already zones `bridges/hooks/**`, and the new leaf's imports all sit inside that zone's `allow` list.

7. **No `consistent-type-imports` ESLint rule exists**, so the leaf module's inline `import { type X } from` style passes lint as written.
</preflight_findings>

<tasks>

<task type="auto">
  <name>Task 1: Create the routing-state leaf module</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts</files>
  <read_first>
    - `.planning/spikes/019-a-hooks-cycle-leaf-extraction/routing-state.ts` (the module body to copy)
    - `extensions/pi-claude-marketplace/bridges/hooks/event-router.ts` lines 80-200 and 296-330 and 695-710 (the doc comments to carry across)
    - `.claude/rules/typescript-comments.md`
  </read_first>
  <action>
Copy the spike module to `extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts`, keeping its declarations and their order exactly: `RoutingEntry`, `CacheEntry`, `PendingSessionStartContext`, `parsedConfigCache`, `routingTable`, the two private `let` cells, and the seven accessors (`currentEpoch`, `bumpEpoch`, `resetEpoch`, `appendPendingSessionStartContext`, `pendingSessionStartContextEntries`, `clearPendingSessionStartContext`, `getRoutingBucket`).

Then make it production code rather than a copied experiment:

Rewrite the file header. The copied header states the file is an experiment that was reverted after measurement, which is false once this lands. Replace it with a file-level block comment that says what the module is: the leaf-ward home for the hooks bridge's routing table, parsed-config cache, and epoch/pending-context cells (D-59-02 / D-59-03), extracted so the knot modules can read shared state without importing `event-router.ts`. State the invariant that keeps the cycle gone: this module imports only `domain/`, `shared/`, and same-zone `if-field/`, and must never import a sibling in the hooks dispatch chain. Cite no GSD planning artifact per `.claude/rules/typescript-comments.md`.

Carry across the documentation the candidate diff is about to delete from `event-router.ts`, moving it onto the declarations it describes:
- The block above `event-router.ts:95` (`export interface RoutingEntry`) becomes the block above `RoutingEntry` here, ending with the `declarationIndex` / DISP-04 paragraph.
- The three per-field blocks inside `RoutingEntry` (on `resolvedSource`, on `claudeEvent` citing D-60-01 / D-60-04, on `ifPredicate` citing MATCH-03 / D-61-02) move onto the same fields here.
- The two per-field blocks inside `CacheEntry` (on `resolvedSource`, on `ifPredicates` citing MATCH-03) move onto the same fields here.
- The `SessionStart additionalContext capture buffer` block at `event-router.ts:162-185` becomes the block above `PendingSessionStartContext` here.
- The D-59-03 block above `currentEpoch` and the block above `appendPendingSessionStartContext` move onto those functions here, verbatim.
- The block above `getRoutingBucket` moves here, but edit its final clause: it currently asserts the routing-table cell stays module-private to `event-router.ts`, which stops being true. Say instead that the cell is private to this module and reached only through this accessor.

Give the four new accessors that have no existing prose (`bumpEpoch`, `resetEpoch`, `pendingSessionStartContextEntries`, `clearPendingSessionStartContext`) one short block each explaining why they exist: ESM imported bindings are read-only, so a reassigned cell can only live here if its writes live here as named mutators.

Do not change any declaration's name, signature, or exported-ness. `CacheEntry` is exported here even though it was module-private in `event-router.ts`, because `_parsedConfigCacheForTest` still names it across the boundary.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; test 0 -eq "$(grep -c 'PROBE' extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts)" &amp;&amp; npx prettier --check extensions/pi-claude-marketplace/bridges/hooks/routing-state.ts</automated>
  </verify>
  <done>`routing-state.ts` exists, typechecks, is Prettier-clean, carries no experiment-provenance header, and its `RoutingEntry` / `CacheEntry` fields still cite D-60-01, D-60-04, and MATCH-03.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the candidate diff and clear the orphaned comments</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch.ts, extensions/pi-claude-marketplace/bridges/hooks/dispatch-exec.ts, extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts, extensions/pi-claude-marketplace/bridges/hooks/settle.ts, extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts, package.json</files>
  <action>
Apply the candidate diff:

`git apply .planning/spikes/020-hooks-cycle-gate-closure/full-candidate.diff`

It repoints five importers at the leaf, strips the moved declarations out of `event-router.ts`, appends the re-export block that preserves the external surface, and widens the `fallow` script to `fallow dead-code --boundary-violations --circular-deps --re-export-cycles --fail-on-issues --format human`. Take the `package.json` hunk with the rest -- the gate flags MUST ship in the same commit, because added alone they fail on the 8 inherited cycles.

If `git apply` fails, stop and report rather than hand-editing around it; the preflight confirmed it applies, so a failure means the tree moved.

Then delete the six comment blocks the diff strands in `event-router.ts`. Each documented a declaration that is now gone, and Task 1 already rehomed the content. Locate them by content (line numbers below are pre-apply and will have shifted):

1. The block at 80-94 that described the routing-table entry record and ended on the DISP-04 declaration-order paragraph. It now floats above the module-state banner.
2. The banner at 152-154 naming the module-state cells and citing D-59-02 / D-59-03. All three cells beneath it moved; delete the banner and its surrounding blank lines.
3. The block at 162-185 describing the SessionStart additionalContext capture buffer.
4. The block at 300-305 that described the read-only epoch accessor.
5. The block at 314-327 that described appending a SessionStart payload to the pending buffer.
6. The block at 699-704 that described the routing-bucket accessor.

Leave the banner above `beforeAgentStartHandlerFor` alone -- `beforeAgentStartHandlerFor` still sits under it, so it is not stranded. Collapse any doubled blank lines the deletions leave behind, and confirm no import in `event-router.ts` is left unused (`noUnusedLocals` will catch it, which is the compile-time failure mode the blueprint predicts).

Do not touch any file under `tests/`. The re-export block is what makes that possible; if a test fails to resolve a symbol, the fix is in the re-export block, not in the test.
  </action>
  <verify>
    <automated>npm run check &amp;&amp; npx fallow dead-code --circular-deps --format human &amp;&amp; test 0 -eq "$(git status --porcelain tests/ | wc -l | tr -d ' ')"</automated>
  </verify>
  <done>`npm run check` exits 0 with the widened gate in place, `fallow dead-code --circular-deps` reports 0 circular dependencies, `git status --porcelain tests/` is empty, and `event-router.ts` contains no block comment whose subject was moved out.</done>
</task>

<task type="auto">
  <name>Task 3: Update the docs, close FLOW-02, and commit</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/BACKLOG.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/codebase/ARCHITECTURE.md` "Architectural Constraints" -> "Circular imports" bullet
    - `.planning/BACKLOG.md` around line 106 (FLOW-02)
  </read_first>
  <action>
**ARCHITECTURE.md.** The third sub-bullet under "Circular imports" currently reads that `bridges/hooks/` carries a pre-existing cycle knot between `event-router.ts`, `dispatch.ts`, and `async-rewake/registry.ts`, and cites that knot as the reason the `no-cycle` glob stops at `orchestrators/`. Both halves are now stale. Rewrite that clause to say the knot was removed by extracting shared module state into the `bridges/hooks/routing-state.ts` leaf, and that whole-repo cycle coverage now comes from `npm run fallow`'s `--circular-deps` flag rather than from the ESLint glob. Keep the `orchestrators/plugin/bootstrap.ts` composer exemption in the same sub-bullet -- that half is still true.

Add one sentence recording the open follow-up: now that the knot is gone, whether the `import-x/no-cycle` glob should widen past `orchestrators/` is an open question, deliberately out of scope here because widening it has its own blast radius across the extension. Do NOT widen the glob in this change.

In the `bridges/` layer bullet's "Contains" line, add `hooks/routing-state.ts` alongside the other named bridge-local helpers, described as the leaf holding the hooks routing table and epoch cells.

**BACKLOG.md.** Mark FLOW-02 closed, dated today, with the resolution in one line: `npm run fallow` now runs `--boundary-violations --circular-deps --re-export-cycles`, so the local gate and the pull-request gate see the same finding classes. Note that the 8 inherited cycles it described are gone. Follow whatever closure convention the surrounding items in the file already use rather than inventing a new one.

Then file the ESLint-glob follow-up as a new backlog item in the same file, matching the file's existing item format: the question is whether `import-x/no-cycle` should now cover `extensions/pi-claude-marketplace/**` instead of stopping at `orchestrators/`, noting that fallow already covers the whole repo so the ESLint rule would be defense in depth, and that `tests/architecture/import-boundaries.test.ts` pins the current glob and would need updating with it.

**STATE.md.** Update `last_activity`, `last_activity_desc`, and the "Last activity" line in Current Position to record this quick task. Leave the milestone fields alone.

**Commit.** One commit carrying everything from all three tasks -- source, gate flags, and docs together.

Run a filesystem trufflehog scan over the changed paths first, because this is a linked worktree where the git-mode hook structurally cannot run:

```
TH=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1)
"$TH" filesystem <changed paths> --results=verified,unknown --fail
```

Exit 0 with zero verified and zero unverified secrets is the clean result. Then run `pre-commit run --files <changed files>`, fix and restage until clean, and only then commit. Never pass `--no-verify`. Prefix the commit with `SKIP=trufflehog` and extend `SKIP=` to nothing else.

Commit message, ASCII only, no em dashes, title at most 72 characters, body lines at most 80:

```
refactor(hooks): remove circular-dependency knot and gate cycles

Extract the hooks bridge routing table, parsed-config cache, and epoch
cells into a new leaf module, bridges/hooks/routing-state.ts, and
repoint the five knot importers at it. event-router.ts re-exports the
moved names so consumers outside the knot are unchanged.

Widen the local fallow gate to --circular-deps and --re-export-cycles
in the same commit, since the flags fail on the inherited cycles until
those cycles are gone. Closes FLOW-02.
```
  </action>
  <verify>
    <automated>test 0 -eq "$(grep -c 'pre-existing cycle knot' .planning/codebase/ARCHITECTURE.md)" &amp;&amp; git log -1 --format=%s | grep -q '^refactor(hooks)' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>ARCHITECTURE.md no longer describes the knot as present, names `routing-state.ts`, and records the glob-widening follow-up; FLOW-02 is marked closed and the follow-up item is filed; one commit exists on `features/hooks-cycle-removal` with a clean worktree behind it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This is a pure intra-module refactor. No new input crosses any boundary; the hook subprocess boundary in `dispatch-exec.ts` is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p25-01 | Tampering | `bridges/hooks/routing-state.ts` | low | mitigate | The moved `RoutingEntry.resolvedSource` keeps its `AbsolutePluginRoot` brand, so the type system still blocks unvalidated strings reaching the subprocess env as `CLAUDE_PLUGIN_ROOT`. Task 1 forbids signature changes; `npm run check` proves the brand survived. |
| T-p25-02 | Elevation of Privilege | epoch cell relocation | low | mitigate | The D-59-03 epoch guard is what makes a stale post-`/reload` handler a no-op. Moving `liveEpoch` behind `bumpEpoch`/`resetEpoch` preserves single-writer semantics; the 17 test files pinning the no-op-on-mismatch contract run unmodified under `npm run check`. |
| T-p25-SC | Tampering | package-manager installs | n/a | accept | No dependency is added, removed, or upgraded. `package.json` changes only the `fallow` script's CLI flags, so no legitimacy audit applies. |
</threat_model>

<verification>
1. `npm run check` exits 0 (typecheck, ESLint, the widened fallow gate, Prettier, unit tests, integration tests).
2. `npx fallow dead-code --circular-deps --format human` reports 0 circular dependencies, down from 8.
3. `git status --porcelain tests/` is empty -- zero test files modified. If any test needed editing, something went wrong; stop and report rather than editing the test.
4. `grep 'fallow dead-code' package.json` shows all three flags: `--boundary-violations --circular-deps --re-export-cycles`.
5. `git show --stat HEAD` shows the source change, the `package.json` flag change, and the doc updates in one commit.
</verification>

<success_criteria>
- 0 circular dependencies in the extension, verified by fallow.
- `npm run check` green with the cycle and re-export-cycle gates active locally, so a green local run now implies a green pull-request gate.
- Zero test files modified.
- ARCHITECTURE.md and BACKLOG.md tell the truth about the current tree, and the ESLint-glob widening is recorded as an explicit follow-up rather than silently dropped.
</success_criteria>

<output>
No SUMMARY file. Report completion inline with the fallow cycle count before and after, the `npm run check` result, and the commit SHA.
</output>
