---
phase: 260912-fp3
plan: 01
type: execute
wave: 2
depends_on: ["260912-fp0", "260912-fp1", "260912-fp2"]
files_modified:
  - .planning/WINDOWS.md
autonomous: true

must_haves:
  truths:
    - "`windows status` reports `open_count: 0` — no entry in the ledger reads `open`."
    - "Ids 1, 2, 3, 7, 8, 9, 10, 13, 14, 15, 16, 17 and 18 read `waived`, each carrying the measured reason recorded for it in section B of the disposition spec, byte for byte."
    - "Ids 20, 23, 24, 25, 26 and 29 read `fixed`."
    - "The rendered markdown table and the fenced JSON still describe the same entry set, so the next `windows` verb is admitted rather than refused."
    - "`total_count` is still 31 and entry 30 is untouched — still `fixed`, `resolved_at` still null."
    - "Every disposition was written by a `windows` verb. The rendered table carries no hand edit."
  artifacts:
    - .planning/WINDOWS.md
  key_links:
    - "Reason text travels spec -> derived JSON -> argv -> ledger without shell mangling. Twelve of the thirteen reasons carry backticks, which a double-quoted shell literal would run as command substitution; four carry an apostrophe, which breaks single-quoting. A node read into a double-quoted parameter expansion is the one path that survives both, because bash does not re-scan an expansion for command substitution."
    - "The frontmatter counts, the rendered table and the fenced JSON are three views of ONE entry set. `parseLedger` refuses when the counts disagree with the entries and `writeLedgerAtomic` refuses when the table disagrees with the JSON, so a single hand edit bricks every later verb."
---

<objective>
Dispose all 19 remaining `open` entries in `.planning/WINDOWS.md` so the ledger holds no open
entry and the `/gsd-ship` window gate stops blocking: six marked `fixed` because their
corrections landed in the sibling items, thirteen `waived` with the measured reason recorded
for each.

Purpose: `/gsd-ship` blocks while `open_count > 0`. The milestone left these 19 deliberately
un-closed rather than close them on narrative, and the operator approved the full sweep on
2026-09-12. This item is the disposition pass — it records verdicts, it does not fix code.

Output: one ledger file mutated exclusively through the `windows fixed` and `windows waive`
verbs, plus one Conventional Commits commit.

Ground truth measured while planning, at ledger `last_updated 2026-09-11T21:38:25.338Z`:
the ledger parses, the table matches a re-render of its own JSON, counts read
`19/0/12/31` (open/waived/fixed/total), and the open ids are exactly
`1, 2, 3, 7, 8, 9, 10, 13, 14, 15, 16, 17, 18, 20, 23, 24, 25, 26, 29` — the spec's 13 + 6
with nothing left over.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@/tmp/claude-1000/-home-acolomba-pi-claude-marketplace-refine-unit-tests/18969798-5830-434f-9201-d9cd7ed141d0/scratchpad/window-pass-spec.md
@.planning/HANDOFF-refine-unit-tests-open-items.md
@.planning/WINDOWS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Derive the disposition inputs and dispose one entry end-to-end</name>
  <files>
    $SCRATCH/waive-reasons.json (scratchpad, not the repo),
    $SCRATCH/ledger-check.mjs (scratchpad, not the repo),
    .planning/WINDOWS.md
  </files>
  <read_first>
    /tmp/claude-1000/-home-acolomba-pi-claude-marketplace-refine-unit-tests/18969798-5830-434f-9201-d9cd7ed141d0/scratchpad/window-pass-spec.md — sections "Mechanics", A and B. Section B is the authoritative text of all 13 waive reasons.
    .claude/gsd-core/bin/lib/broken-windows.cjs — `markWaived`/`markFixed` (the `assertOpen` guard), `renderTable` (the `cell()` escaper), `extractTableRegion` and `parseLedger`. Read these before writing the checker; the plan's assertions name these exact exports.
  </read_first>
  <precondition>The disposition spec is readable at the scratchpad path above. It is the sole source of the 13 waive reasons and there is no fallback — if it is missing, halt and ask rather than paraphrasing a reason from memory.</precondition>
  <action>
Set `SCRATCH` to the scratchpad directory for this session and work from the repository root on
branch `features/refine-unit-tests`.

First, derive the waive reasons MECHANICALLY from section B of the spec — do not hand-transcribe
13 long prose strings, because a transcription pass is exactly how a measured record turns into a
paraphrase. Write a short node script that reads the spec, takes the text after the `## B.`
heading and before the next horizontal rule, and matches each table row against a row pattern
that captures the leading integer as the id and everything between the second and the final pipe
as the reason. Emit `$SCRATCH/waive-reasons.json` as one flat object keyed by id string.

Section B applies ONE escape of its own that must be undone: section B is itself a markdown
table, so id 16's reason renders its pipe character with a leading backslash to stop it splitting
the column. The ledger wants the bare pipe. Undo that one escape during derivation — replace the
backslash-plus-pipe pair with a lone pipe — and undo nothing else. `renderTable`'s `cell()`
re-escapes pipes and backslashes on its own when it draws the table, so the reason stored in the
JSON must carry the bare character. None of the 13 reasons legitimately contains a backslash,
which makes "no reason contains a backslash" a usable invariant.

Two things about section B that have to be handled deliberately. Its rows are NOT in numeric
order — they run 9, 7, 8, 10, 13, 14, 15, 16, 17, 18, 1, 2, 3 — so pair each reason with the id
in its own first column, never with its row position. And the derived object must come out with
exactly 13 keys matching the spec's 13 ids; a parser that silently drops a row would waive an
entry with the wrong text.

Second, write `$SCRATCH/ledger-check.mjs`, the gate every task in this plan verifies through. It
requires `.claude/gsd-core/bin/lib/broken-windows.cjs`, reads `.planning/WINDOWS.md`, and on
every invocation asserts: `parseLedger` accepts the file (which is itself the frontmatter-counts
check — it throws when the counts disagree with the entries); `extractTableRegion(raw,
total_count)` is non-null and byte-equal to `renderTable(entries)`, which is the same predicate
`writeLedgerAtomic` enforces on write and therefore the early warning that the next verb would be
refused; no entry whose status is `waived` has an empty reason; and no waived reason contains a
backslash. It then takes expectations as flags — the four frontmatter counts, the exact set of
waived ids, the set of ids required to be `fixed`, and a path to the reasons JSON against which
every waived entry's stored reason must compare equal. It prints the status distribution and
exits 0, or names the single failing assertion and exits non-zero. Keep it dependency-free.

Third, the tracer itself: dispose exactly ONE entry, id 16, and stop. Id 16 is the right probe
because its reason is the only one carrying a raw pipe, so a single write exercises the whole
mechanic at once — reason authoring, the argv crossing, `cell()`'s table escaping, the atomic
write, and the read-back.

Read the reason out of the JSON with a node one-liner into a shell variable, then pass it as
`"$REASON"`. Never paste reason prose into a shell command literal: twelve of the thirteen carry
backticks, which bash runs as command substitution inside double quotes, and four carry an
apostrophe, which terminates a single-quoted string. A double-quoted parameter expansion is safe
because bash does not re-scan an expansion for command substitution — measured during planning
against a string carrying backticks, an apostrophe, a pipe and an em dash, all of which reached
argv byte-identical.

Then run `node .claude/gsd-core/bin/gsd-tools.cjs windows waive 16 "$REASON"`.

One property of the verbs governs everything that follows: `markWaived` and `markFixed` both call
`assertOpen`, so a verb run twice against the same id exits non-zero with
`windows_already_resolved`. The verbs are NOT idempotent. Never re-run the whole batch to recover
from a partial failure, and never repair a refusal by editing the file.
  </action>
  <verify>
    <automated>node "$SCRATCH/ledger-check.mjs" --open 18 --waived 1 --fixed 12 --total 31 --expect-waived 16 --reasons "$SCRATCH/waive-reasons.json"</automated>
    <automated>node -e 'const fs=require("node:fs");const spec=fs.readFileSync(process.argv[1],"utf8");const b=spec.split(/^## B\./m)[1].split(/^---$/m)[0];const fromSpec={};for(const line of b.split("\n")){const m=/^\|\s*(\d+)\s*\|\s*(.*\S)\s*\|\s*$/.exec(line);if(m)fromSpec[m[1]]=m[2].replace(/\\\|/g,"|");}const got=JSON.parse(fs.readFileSync(process.argv[2],"utf8"));const want=["1","2","3","7","8","9","10","13","14","15","16","17","18"].sort();const have=Object.keys(got).sort();if(JSON.stringify(have)!==JSON.stringify(want))throw new Error("derived id set is "+JSON.stringify(have)+", want "+JSON.stringify(want));for(const id of have){if(got[id]!==fromSpec[id])throw new Error("id "+id+" does not match spec section B");if(got[id].includes("\\"))throw new Error("id "+id+" retains a backslash");}if(!got["16"].includes("`T | undefined`"))throw new Error("id 16 lost its bare pipe");console.log("13 reasons match spec section B; no backslashes; id 16 pipe is bare");' "$SCRATCH/window-pass-spec.md" "$SCRATCH/waive-reasons.json"</automated>
  </verify>
  <done>
`$SCRATCH/waive-reasons.json` holds exactly 13 reasons, each byte-equal to its section B row with
id 16's pipe un-escaped and no backslash anywhere. `$SCRATCH/ledger-check.mjs` exists and gates on
the four invariants plus its flag expectations. Entry 16 reads `waived` in the ledger with that
exact reason stored, counts read 18/1/12/31, and the table still matches a re-render of the JSON —
so the next verb will be admitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dispose the remaining 18 entries — 12 waives and 6 fixed</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    .planning/quick/260912-fp0-*/260912-fp0-SUMMARY.md, .planning/quick/260912-fp1-*/260912-fp1-SUMMARY.md, .planning/quick/260912-fp2-*/260912-fp2-SUMMARY.md — read all three before marking anything `fixed`. Each names which ledger entries it corrected by an edit and which it found already correct.
  </read_first>
  <precondition>The three sibling items' corrections are present in this working tree — their SUMMARY files exist under `.planning/quick/260912-fp{0,1,2}-*/` and their commits are in the branch. The six `fixed` verdicts are only truthful once those corrections have landed; `depends_on` orders the items but does not by itself prove the merge brought the edits into this tree.</precondition>
  <action>
Run the remaining 12 waives, then the 6 fixed verbs, then verify once.

For the waives, loop over ids 1, 2, 3, 7, 8, 9, 10, 13, 14, 15, 17 and 18 — id 16 is already done
by Task 1 and re-running it would exit non-zero. Read each reason out of
`$SCRATCH/waive-reasons.json` into a shell variable with a node one-liner and pass it as
`"$REASON"`, exactly as Task 1 established. Run the verbs one id at a time, echoing the id before
each, and stop the loop on the first non-zero exit rather than pressing on — because the verbs are
not idempotent, a mid-batch failure has to be resumed from the id that failed, and a blind re-run
of the whole list would refuse on every id already disposed.

Before running the `fixed` verbs, read the three sibling SUMMARYs. The spec anticipates that some
entries describe prose that already reads correctly, in which case the sibling reported the entry
as stale rather than corrected and made no edit. Those entries still get marked `fixed` — the
ledger was wrong, not the source — but record which ones they were, because the commit body and
the SUMMARY have to say so. Measured while planning: entry 29's target sentence in
`.planning/codebase/CONVENTIONS.md` already reads correctly (it names the aggregate barrel only to
state it was removed as unreachable), so entry 29 is the likely stale case. Do not treat that as
licence to edit anything — this item edits no source, and a negative grep for the aggregate
barrel's path against that file would be a false alarm, since the corrected sentence legitimately
still contains the token.

Then run `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed <id>` for ids 20, 23, 24, 25, 26
and 29, one at a time, same fail-fast discipline. `windows fixed` takes the id alone and no reason.

Do not touch entry 30. It already reads `fixed` and its `resolved_at` is null by decision — it is
a rendered column and a hand-supplied timestamp re-creates the very table/JSON drift that was
repaired earlier in this milestone.

Finally read back with `node .claude/gsd-core/bin/gsd-tools.cjs windows status` and confirm from
the verb's own output — not from the rendered table — that `open_count` is 0. Record the final
distribution: the expected end state is `open 0 / waived 13 / fixed 18 / total 31`, the 18 being
the 12 already `fixed` before this pass plus the 6 marked here.
  </action>
  <verify>
    <automated>node "$SCRATCH/ledger-check.mjs" --open 0 --waived 13 --fixed 18 --total 31 --expect-waived 1,2,3,7,8,9,10,13,14,15,16,17,18 --expect-fixed 20,23,24,25,26,29 --reasons "$SCRATCH/waive-reasons.json"</automated>
    <automated>ST=$(node .claude/gsd-core/bin/gsd-tools.cjs windows status) || { echo "windows status exited non-zero"; exit 1; }; node -e 'const l=JSON.parse(process.argv[1]).ledger;if(l.open_count!==0)throw new Error("open_count is "+l.open_count+", want 0");const e30=l.entries.find(x=>x.id===30);if(!e30||e30.status!=="fixed"||e30.resolved_at!==null)throw new Error("entry 30 was mutated");console.log("verb reports open=0 waived="+l.waived_count+" fixed="+l.fixed_count+" total="+l.total_count+"; entry 30 untouched");' "$ST"</automated>
  </verify>
  <done>
All 19 dispositions are written by the verbs. `windows status` reports `open_count: 0` with
waived 13, fixed 18, total 31. The 13 waived ids carry their spec reasons byte for byte, the 6
named ids read `fixed`, entry 30 is unchanged, and the table still matches a re-render of the
JSON. Which entries were fixed with no source edit is recorded for the commit body.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate and commit the ledger change</name>
  <files>.planning/WINDOWS.md</files>
  <action>
Stage the one explicit path — `git add .planning/WINDOWS.md`, never `git add -A`, because the
operator edits files and switches branches in this same checkout.

Run `pre-commit run --files .planning/WINDOWS.md` BEFORE `git commit`. If a hook fails or rewrites
the file, fix it, restage, and re-run until clean. Never `--no-verify`. Never `--amend` to recover
from a hook failure — a failed hook means the commit did not happen, so amending would rewrite the
previous commit instead.

Measured while planning, so expectations are concrete rather than hopeful: `mdformat`,
`markdownlint-cli2`, `fix-smartquotes`, `fix-unicode-dashes` and `fix-ligatures` all carry an
`exclude` for `^\.planning/` in `.pre-commit-config.yaml`, so none of them touch this file and the
em dashes throughout the ledger are safe. The hooks that DO fire on it are `trailing-whitespace`,
`end-of-file-fixer`, `fix-spaces`, `forbid-bidi-controls`, `check-merge-conflict` and
`trufflehog`. `renderLedger` ends every table row with a pipe and terminates the file with a
newline, so the first two should have nothing to do — but verify rather than assume.

If any hook does rewrite the file, re-run the ledger checker afterwards before committing. A hook
that touched the rendered table without touching the fenced JSON would desync the two and refuse
every future `windows` verb, which is the failure this milestone already had to repair once.

Commit with a Conventional Commits message. Title at most 72 characters, body lines at most 80,
and no milestone, phase, plan or wave reference anywhere in it — `docs(windows):` is the right
scope. The body should say what was disposed: the six ids marked `fixed` now their corrections
have landed, the thirteen waived with the measured reason recorded for each, that every
disposition went through the `windows` verbs and the rendered table was not hand-edited, and
which entries were marked `fixed` with no source edit because the ledger was stale rather than the
file. Write the message with `git commit -F <file>` rather than `-m`, since the body names
identifiers and a backtick in a `-m` argument can be executed by the shell.

After committing, check `git status` for `.planning/WINDOWS.md`. The prettier and whitespace hooks
can rewrite a file mid-commit while the commit still succeeds, which leaves the rewrite unstaged.
If that happened, fix it with a FOLLOW-UP commit, never an amend, and re-run the ledger checker
after it.

Do not run `npm run check` here. The batch coordinator runs it once after every item lands. Do not
open the PR — that is a separate, later step.
  </action>
  <verify>
    <automated>RES=$(git status --porcelain .planning/WINDOWS.md) || { echo "git status exited non-zero"; exit 1; }; test -z "$RES" || { echo "uncommitted residue after commit: $RES"; exit 1; }; echo "no residue for .planning/WINDOWS.md"</automated>
    <automated>node "$SCRATCH/ledger-check.mjs" --open 0 --waived 13 --fixed 18 --total 31 --expect-waived 1,2,3,7,8,9,10,13,14,15,16,17,18 --expect-fixed 20,23,24,25,26,29 --reasons "$SCRATCH/waive-reasons.json"</automated>
    <automated>FILES=$(git diff-tree --no-commit-id --name-only -r HEAD) || { echo "git diff-tree exited non-zero"; exit 1; }; test "$FILES" = ".planning/WINDOWS.md" || { echo "HEAD touches: $FILES"; exit 1; }; echo "HEAD touches only .planning/WINDOWS.md"</automated>
    <automated>SUBJ=$(git log -1 --pretty=%s) || { echo "git log exited non-zero"; exit 1; }; node -e 'const t=process.argv[1].trim();if(t.length>72)throw new Error("subject is "+t.length+" chars, max 72");if(!/^[a-z]+(\([a-z0-9.-]+\))?: .{5,}/.test(t))throw new Error("subject is not Conventional Commits: "+t);if(/\b(phase|plan|wave|milestone)\b/i.test(t))throw new Error("subject names a planning artifact: "+t);console.log("subject ok ("+t.length+" chars): "+t);' "$SUBJ"</automated>
  </verify>
  <done>
`pre-commit run --files .planning/WINDOWS.md` is clean. One commit exists whose only file is
`.planning/WINDOWS.md`, with a Conventional Commits subject of at most 72 characters naming no
planning artifact, and a body recording the six `fixed`, the thirteen waived, that the verbs did
every write, and which entries were fixed with no source edit. `git status` shows no residue for
the ledger, and the checker is still green after the hooks ran.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec prose -> shell argv | 13 reason strings cross into a command line. Twelve carry backticks, which bash executes as command substitution inside double quotes; four carry an apostrophe, which terminates a single-quoted string. Content that is merely descriptive on one side of this boundary is executable on the other. |
| `windows` verb -> ledger file | `writeLedgerAtomic` reconstructs frontmatter, rendered table and fenced JSON from one entry set. Any edit arriving from outside that write path desyncs the three views. |
| pre-commit hooks -> ledger file | The hooks rewrite files in place, mid-commit, while the commit still succeeds. A rewrite that reached the table but not the JSON would be indistinguishable from a hand edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fp3-01 | Tampering | `windows waive` argv | high | mitigate | Never inline reason prose in a shell literal. Read each reason from the derived JSON with node and pass it as a double-quoted parameter expansion, which bash does not re-scan for command substitution. Measured during planning against a probe carrying a backtick, an apostrophe, a pipe and an em dash — all reached argv byte-identical. The checker's "no waived reason contains a backslash" and reason-equality assertions detect any mangling that slipped through. |
| T-fp3-02 | Tampering | `.planning/WINDOWS.md` rendered table | high | mitigate | Write only through the `windows` verbs. `ledger-check.mjs` computes `extractTableRegion(raw, total_count) === renderTable(entries)` — the same predicate `writeLedgerAtomic` enforces — and runs after every batch, so a desync surfaces as a failed gate instead of as a refusal that bricks the next verb. |
| T-fp3-03 | Repudiation | waived `reason` field | medium | mitigate | The reasons are the measured audit record, not narrative closures. Derive all 13 mechanically from spec section B and assert equality against an independent re-parse of that same section, so a paraphrase cannot reach the ledger. |
| T-fp3-04 | Tampering | pre-commit in-place rewrite | medium | mitigate | Re-run the ledger checker after any hook rewrite and before commit; check `git status` after commit and repair with a follow-up commit, never an amend. |
| T-fp3-05 | Information disclosure | ledger prose, commit body | low | accept | The reasons are project-internal engineering notes whose substance is already committed in the entry descriptions. No credential or secret material is introduced, and `trufflehog` runs over the staged file. |
| T-fp3-SC | Tampering | npm/pip/cargo installs | low | accept | This item installs no packages and invokes no package manager, so the package-legitimacy gate has no target. The checker script is dependency-free and requires only a repository-local module and node builtins. |
</threat_model>

<verification>
1. `node .claude/gsd-core/bin/gsd-tools.cjs windows status` reports `open_count: 0`, and the
   distribution reads waived 13 / fixed 18 / total 31. Read this from the verb's output, never
   from the rendered table.
2. The 13 waived ids are exactly 1, 2, 3, 7, 8, 9, 10, 13, 14, 15, 16, 17, 18 and each stored
   reason is byte-equal to its spec section B row, with id 16's pipe bare and no backslash in any
   reason.
3. Ids 20, 23, 24, 25, 26 and 29 read `fixed`.
4. Entry 30 is untouched: `fixed`, `resolved_at` null.
5. `extractTableRegion` of the on-disk file equals `renderTable` of its own fenced JSON, so the
   next `windows` verb is admitted.
6. `pre-commit run --files .planning/WINDOWS.md` is clean, `git status` shows no residue, and
   `HEAD` touches `.planning/WINDOWS.md` and nothing else.
</verification>

<success_criteria>
- No entry in `.planning/WINDOWS.md` reads `open`; `open_count` is 0 and `total_count` is still 31.
- All 19 dispositions were written by `windows fixed` / `windows waive`. The rendered table carries
  no hand edit and the ledger still parses and still re-renders identically.
- Each of the 13 waives carries its measured reason from spec section B verbatim.
- One commit, one file, Conventional Commits, no planning-artifact reference in the message.
- The SUMMARY reports the final status distribution and names any entry marked `fixed` with no
  source edit, because in that case the ledger was stale rather than the file.
</success_criteria>

<output>
Create `.planning/quick/260912-fp3-dispose-every-remaining-open-entry-in-th/260912-fp3-SUMMARY.md`
when done.
</output>
