---
phase: 260912-fp1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
  - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
autonomous: true

must_haves:
  truths:
    - "No comment in either touched file cites a test path that is absent from `git ls-files`."
    - "The `isHooksResolverNote` doc comment in `install.messaging.ts` names the parity suite at its real current location, `tests/architecture/cross-surface-reason-parity.test.ts`."
    - "The `ensureSharedDataDir` SessionStart-gate comment in `event-router.ts` names `tests/index.test.ts` as the pin for the clean-reconcile invariant, and the `WR-05` anchor survives verbatim."
    - "Every other traceability anchor in the two touched comment blocks survives: `SURF-01`, `HOOK-03`, `LIFE-01` in `install.messaging.ts`; `WR-05`, `D-60-06` in `event-router.ts`."
    - "Executable behavior is byte-unchanged: every added and removed line in the diff is a comment line."
  artifacts:
    - extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
    - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
  key_links:
    - "Both repointed comments carry a phrase from the cited test's own title, so the citation stays checkable by grep if a suite relocates again: `install.messaging.ts` keeps the `SURF-01` cross-surface framing the parity suite asserts; `event-router.ts` carries `both scope roots untouched`, lifted from the surviving test title."
---

<objective>
Repoint two stale test-path references in production comments: the `isHooksResolverNote` doc
comment in `orchestrators/plugin/install.messaging.ts` and the `ensureSharedDataDir`
SessionStart-gate comment in `bridges/hooks/event-router.ts`. WINDOWS ledger entries 23 and 26.

Purpose: both comments cite a pin by path, and both paths moved out from under them. The
parity suite relocated to `tests/architecture/`; the `index-handler` suite was deleted
outright. A comment that names a nonexistent file is worse than no citation at all — a reader
checking the claim finds nothing and cannot tell whether the invariant lapsed or the pin just
moved. For entry 26 the invariant itself is alive and green, so the fix is a repoint, not a
retraction.

Output: a comment-only diff across two production files, plus one Conventional Commits commit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts
@extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
</context>

<pre_read_findings>
Measured by the planner against the live tree at `6bc71dc4`. **Both ledger entries are real —
neither is a stale ledger row.** Both comments need an edit. Line numbers have drifted from the
ledger, so locate by content, not by number.

**Entry 23 — `install.messaging.ts`, ledger says `:476`, actually `:478`.**

- `git ls-files` places the suite at `tests/architecture/cross-surface-reason-parity.test.ts`
  (one hit, single location). The path named in the comment has no entry in `git ls-files`.
- The citation is the last line of the JSDoc block above `isHooksResolverNote`, reading
  `* <stale path>).` and closing a parenthetical opened on the previous line with `(pinned by`.
  Only the path segment is wrong; the sentence around it is accurate.
- The block's other anchors are `SURF-01` (11 occurrences file-wide), `HOOK-03` and `LIFE-01`
  (2 each). Keep all of them.
- `install.messaging.ts` contains exactly ONE `tests/` citation — this one. It must still be
  exactly one after the fix.
- The word `orchestrators` appears twice in the file: `:326` cites
  `orchestrators/import/execute.ts` and is accurate and unrelated — **DO NOT touch `:326`.**

**Entry 26 — `event-router.ts`, ledger says `:741`, actually `:843`.**

- `tests/edge/index-handler.test.ts` is absent from `git ls-files` — deleted, confirmed.
- The citation sits mid-sentence in a twelve-line `//` block above
  `if (routingState.getRoutingBucket("SessionStart").length > 0) { await ensureSharedDataDir(loc); }`.
  The block opens `D-60-06:` and the citation closes the parenthetical `... violate WR-05 (the
  "no files on a clean reconcile" invariant pinned by <stale path>).`
- **The invariant survives and is green.** `tests/index.test.ts:870`:
  `test("leaves both scope roots untouched and emits nothing when a pristine workspace reconciles (WR-05)", ...)`
  seeds a hermetic pristine scope, runs `discover`, and asserts `EMPTY_DISCOVERY`,
  `existsSync(<cwd>/.pi) === false`, `existsSync(<home>/.pi) === false`, and `notifications === []`.
  Planner ran it: `node --test --test-name-pattern "pristine workspace reconciles" tests/index.test.ts`
  → 1/1 pass, 2.9s.
- `WR-05` occurs exactly ONCE in `event-router.ts` (`:841`, the line above the citation, which
  this edit does not touch). `D-60-06` occurs twice. `event-router.ts` contains exactly ONE
  `tests/` citation — the stale one. Both counts must be preserved.
- **`WR-05` is a reused ID in this repo** (~25 unrelated occurrences across `extensions/` and
  `tests/`, each scoped to its own local finding). The one that matters here is the
  clean-reconcile invariant. Keep the token — `.claude/rules/typescript-comments.md` lists
  `WR-NN` as an encouraged traceability anchor.

**Gate exposure — measured, and the edits are safe.**

- **No gate anywhere pins either stale path string.** Grepped `tests/`, `scripts/`, `docs/`,
  `.pre-commit-config.yaml`, `eslint.config.js`, `.fallowrc.json`.
- Two architecture gates DO source-scan these files, and both match **code** tokens, never
  comment prose: `tests/architecture/scope-fences-63.test.ts` reads `install.messaging.ts` and
  asserts only on lines containing `MANIFEST_FIELD_REASONS` (`new Set(`, `"lspServers"`);
  `tests/architecture/hooks-lifecycle.test.ts` strips full-line `//` comments via
  `readNonCommentLines` and matches `addPluginConfigToCache(` / `rebuildRoutingTables(`.
- `cross-surface-reason-parity.test.ts` and `notify-producer-wire-coverage.test.ts` import
  symbols and assert behavior; they read no source text.
- No count-based scan targets either file. The `SessionStart` token count in `event-router.ts`
  (23) is preserved by the reflow below; `tests/index.test.ts` asserts construction source
  strings but names neither of these files.

**Pre-commit cost — expect a slow run, this is normal.** Both paths match all five local npm
hooks' `files:` gates (`npm-lint`, `npm-format-check`, `npm-typecheck`, `npm-fallow`,
`npm-coverage-direct`), so one `pre-commit run --files` on them runs lint, prettier, `tsc`,
the full `fallow` chain, and `test:coverage:direct:commit`. That is the batch's typecheck and
lint coverage for this item — it is why the plan does not run them separately.
`npm-coverage-direct` runs `--base HEAD` and measures the changed pairs. Neither touched file
appears in `scripts/test-coverage-direct.pin.json` (only `bridges/commands/discover.ts` and
`orchestrators/plugin/install-outcome.ts` are pinned), so both pairs must measure complete.
Comment lines are not executable, so the readings should not move; if that hook fails on a
shifted count, STOP and report rather than editing the pin.
</pre_read_findings>

<!-- planner-discipline-allow: tests/orchestrators/plugin/cross-surface-reason-parity.test.ts -->
<!-- planner-discipline-allow: tests/edge/index-handler.test.ts -->

<tasks>

<task type="auto">
  <name>Task 1: Repoint the isHooksResolverNote parity-suite citation</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts</files>
  <action>
Edit COMMENT TEXT ONLY in
`extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts`. Do not touch a
single executable line.

One site: the final line of the JSDoc block above the `isHooksResolverNote` function
declaration (currently `:478`, located by content — it is the only `tests/` citation in the
file). It currently reads the stale path
`tests/orchestrators/plugin/cross-surface-reason-parity.test.ts` inside a parenthetical that
the previous line opens with `(pinned by`.

Change the directory segment only: the cited path becomes
`tests/architecture/cross-surface-reason-parity.test.ts`. The basename, the surrounding
sentence, the closing `).`, and the whole rest of the block are unchanged. This is a
one-line, one-substring edit.

Do NOT touch `:326`, which cites `orchestrators/import/execute.ts` — that reference is
accurate and unrelated to this fix.

Preserve every anchor in the block: `SURF-01`, `HOOK-03`, `LIFE-01`. Per
`.claude/rules/typescript-comments.md`, introduce no `Phase NN` / `Plan NN` / `Wave N` /
`milestone vX.Y` reference, and do not narrate the move — the comment states where the pin IS,
never that it relocated or where it used to live. Keep the ` * ` JSDoc continuation style and
the file's existing wrap column.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && F=extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts && grep -q 'tests/architecture/cross-surface-reason-parity\.test\.ts' "$F" && ! grep -q 'tests/orchestrators/plugin/cross-surface-reason-parity\.test\.ts' "$F" && grep -q 'orchestrators/import/execute\.ts' "$F"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && F=extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts && grep -q 'SURF-01' "$F" && grep -q 'HOOK-03' "$F" && grep -q 'LIFE-01' "$F" && ! grep -qiE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|milestone v[0-9]' "$F"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && D=$(git diff -U0 -- extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts) && N=$(printf '%s\n' "$D" | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*(//|\*)' | wc -l | tr -d ' ') && test "$N" -eq 0</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && O=$(node --test tests/architecture/cross-surface-reason-parity.test.ts 2>&1) && printf '%s\n' "$O" | grep -qE 'pass 17$' && printf '%s\n' "$O" | grep -qE 'fail 0$'</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && npx prettier --check extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts</automated>
  </verify>
  <done>
The `isHooksResolverNote` JSDoc cites `tests/architecture/cross-surface-reason-parity.test.ts`;
the old path is gone; the unrelated `orchestrators/import/execute.ts` citation still stands.
`SURF-01`, `HOOK-03` and `LIFE-01` all survive and no planning reference was introduced. The
`git diff` for the file contains ZERO changed non-comment lines. The parity suite reports
17 pass / 0 fail and prettier reports the file formatted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repoint the WR-05 clean-reconcile citation on the SessionStart gate</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts</files>
  <action>
Edit COMMENT TEXT ONLY in `extensions/pi-claude-marketplace/bridges/hooks/event-router.ts`. Do
not touch a single executable line — in particular leave the
`if (routingState.getRoutingBucket("SessionStart").length > 0)` guard and the
`await ensureSharedDataDir(loc)` call exactly as they are. The gate is correct; only its
justifying comment is wrong.

One site: the `//` block opening `D-60-06:` directly above that guard (currently `:835-846`,
located by content — it is the only `tests/` citation in the file). Its stale segment is the
tail of the parenthetical `... violate WR-05 (the "no files on a clean reconcile" invariant
pinned by tests/edge/index-handler.test.ts).`

Rewrite that tail, and reflow the rest of the block's prose to absorb the length change, so
the comment reads as a continuous paragraph. Requirements on the new text:

  - The cited pin is `tests/index.test.ts`. Keep the `WR-05` token exactly once and keep it on
    the line where it already sits — that line is not part of the edit.
  - Carry the phrase `both scope roots untouched`, lifted from the surviving test's own title,
    so the citation is greppable against the test if the suite ever moves again. State the
    assertion in substance: a pristine workspace reconcile leaves both scope roots untouched
    and emits nothing.
  - Every sentence already in the block that is still true survives with its meaning intact:
    the `D-60-06` rationale for the dir, the `CLAUDE_ENV_FILE` path it serves, the reason the
    gate is needed (an unsolicited mkdir on a pristine scope would create
    `<scopeRoot>/pi-claude-marketplace/...`), the harmlessness of the dir's absence when no
    plugin declares SessionStart hooks, the `mkdir { recursive }` idempotence across
    `/reload`, and that failures route through `hookDebugLog`.
  - The token `SessionStart` must still appear exactly three times inside this block (and 23
    times file-wide). The reflow moves words between lines; it must not drop or add one.

Per `.claude/rules/typescript-comments.md`: present-tense facts only. Do not write that the
pin moved, was deleted, or previously lived elsewhere — name where it IS. Introduce no
`Phase NN` / `Plan NN` / `Wave N` / `milestone vX.Y` reference. Keep both `D-60-06` mentions.
Match the block's existing 4-space + `// ` style and keep lines at or under the roughly
76-column wrap the surrounding block already uses — well inside prettier's 100.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && F=extensions/pi-claude-marketplace/bridges/hooks/event-router.ts && grep -q 'tests/index\.test\.ts' "$F" && ! grep -q 'tests/edge/index-handler\.test\.ts' "$F" && grep -q 'both scope roots untouched' "$F"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && F=extensions/pi-claude-marketplace/bridges/hooks/event-router.ts && grep -q 'WR-05' "$F" && grep -q 'D-60-06' "$F" && grep -q 'SessionStart' "$F" && ! grep -qiE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|milestone v[0-9]' "$F"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && D=$(git diff -U0 -- extensions/pi-claude-marketplace/bridges/hooks/event-router.ts) && N=$(printf '%s\n' "$D" | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*//' | wc -l | tr -d ' ') && test "$N" -eq 0</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && O=$(node --test --test-name-pattern "pristine workspace reconciles" tests/index.test.ts 2>&1) && printf '%s\n' "$O" | grep -qE 'pass 1$' && printf '%s\n' "$O" | grep -qE 'fail 0$'</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && npx prettier --check extensions/pi-claude-marketplace/bridges/hooks/event-router.ts</automated>
  </verify>
  <done>
The `D-60-06` block cites `tests/index.test.ts` and carries `both scope roots untouched`; the
deleted suite's path is gone. `WR-05`, `D-60-06` and `SessionStart` all survive and no planning
reference was introduced. The `git diff` for the file contains ZERO changed non-comment lines,
so the SessionStart guard and the `ensureSharedDataDir` call are untouched. The cited `WR-05`
test reports 1 pass / 0 fail — a non-vacuous run, so a renamed test cannot green this gate —
and prettier reports the file formatted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate and commit both comment repoints as one change</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts, extensions/pi-claude-marketplace/bridges/hooks/event-router.ts</files>
  <action>
Confirm the branch FIRST: `git rev-parse --abbrev-ref HEAD` must read `features/refine-unit-tests`.
If it reads `main`, STOP and report — never commit to main.

Stage the two explicit paths, never `git add -A`:
`git add extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts extensions/pi-claude-marketplace/bridges/hooks/event-router.ts`

Run `pre-commit run --files` over exactly those two paths BEFORE committing. Expect several
minutes: both paths trigger `npm-lint`, `npm-format-check`, `npm-typecheck`, `npm-fallow` and
`npm-coverage-direct` (see the pre-read findings above). If a hook rewrites a file or reports a
failure, inspect the change, restage the same explicit paths, and re-run until the run is
clean. Never pass `--no-verify`. Never use `git commit --amend` to recover from a hook failure
— a failed hook means no commit happened, so amending would rewrite the PREVIOUS commit.

If `npm-coverage-direct` fails, STOP and report. Do not edit
`scripts/test-coverage-direct.pin.json` to make it pass — a coverage move from a comment-only
diff is a finding, not a pin update.

Commit both files together in one commit: they are one change, repointing citations at moved
and deleted test files. Conventional Commits, title <=72 chars, body lines <=80 chars, and NO
phase/plan/wave/milestone reference. Suggested:

  title: `docs(extensions): repoint stale test paths in source comments`
  body:  Name each site and its new target — the `isHooksResolverNote` parity
         citation now points at `tests/architecture/`, and the SessionStart
         `ensureSharedDataDir` gate now cites `tests/index.test.ts` for the
         `WR-05` clean-reconcile invariant, whose assertion is live and green.
         Say that the `WR-05` anchor is kept as traceability and that the diff
         is comment-only, with no behavior change.

After the commit, run `git status --short` and `git diff --stat`. The prettier and whitespace
hooks can rewrite a file mid-commit while the commit still succeeds, leaving a dirty tree. If
anything is dirty, land it as a FOLLOW-UP commit — never an amend.

Do NOT run `npm run check`: the batch coordinator runs the suite once after every item lands.
Do NOT touch `.planning/WINDOWS.md` — the ledger disposition for entries 23 and 26 belongs to
sibling item `260912-fp3`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && BR=$(git rev-parse --abbrev-ref HEAD) && test "$BR" != main && S=$(git status --porcelain -- extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts extensions/pi-claude-marketplace/bridges/hooks/event-router.ts) && test -z "$S"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && T=$(git log -1 --format=%s) && printf '%s\n' "$T" | grep -qE '^[a-z]+(\([a-z0-9./-]+\))?!?: .{4,}' && test "${#T}" -le 72 && B=$(git log -1 --format=%B) && ! printf '%s\n' "$B" | grep -qiE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|milestone v[0-9]' && N=$(printf '%s\n' "$B" | awk '{ if (length($0) > 80) c++ } END { print c+0 }') && test "$N" -eq 0</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && NS=$(git show --numstat --format= HEAD) && printf '%s\n' "$NS" | grep -q 'orchestrators/plugin/install.messaging.ts' && printf '%s\n' "$NS" | grep -q 'bridges/hooks/event-router.ts' && W=$(git status --porcelain -- .planning/WINDOWS.md) && test -z "$W"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && P=$(grep -ho 'tests/[A-Za-z0-9._/-]*\.test\.ts' extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts extensions/pi-claude-marketplace/bridges/hooks/event-router.ts) && test -n "$P" && for p in $P; do git ls-files --error-unmatch "$p" >/dev/null; done</automated>
  </verify>
  <done>
HEAD is on `features/refine-unit-tests` (not `main`) and carries one commit touching both
files, with a Conventional Commits subject at or under 72 chars, no body line over 80 chars,
and no planning reference. `pre-commit` ran clean over both paths before the commit. The
working tree is clean for both files and `.planning/WINDOWS.md` is untouched. Every `tests/`
path cited in either file resolves in `git ls-files` — the closing gate for this item, and the
one that is RED at HEAD before the edits, so it cannot pass vacuously.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan changes comment text in two TypeScript files. No input parsing, no I/O surface, no new call site, no dependency change. The `ensureSharedDataDir` filesystem write and its `SessionStart` guard — the only security-relevant construct near either edit — are untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fp1-01 | Tampering | `event-router.ts` SessionStart gate on `ensureSharedDataDir` | high | mitigate | The gate is what keeps the bridge from creating `<scopeRoot>/pi-claude-marketplace/...` in a pristine user workspace (NFR-10 containment, `WR-05`). Task 2's third automated gate fails on ANY changed non-comment line, so the guard or the call cannot be altered under cover of the comment edit; its fourth gate re-runs the `WR-05` assertion that observes the invariant end-to-end. |
| T-fp1-02 | Tampering | `install.messaging.ts` reason classifiers | medium | mitigate | `isHooksResolverNote` feeds the closed-set reason vocabulary users see. Task 1's comment-only diff gate plus a run of `tests/architecture/cross-surface-reason-parity.test.ts` (17 cases, cross-checks install-side against probe-side tokens) prove the classifier's behavior is unchanged. |
| T-fp1-03 | Information Disclosure | comment prose in both files | low | mitigate | The new text names only repo-relative test paths, a test-title phrase, and existing requirement IDs. No absolute path, host name, token, or operator directory may appear — `shared/redact-absolute-paths.ts` exists because absolute paths are treated as leakable. |
| T-fp1-04 | Repudiation | git history | low | accept | One commit on `features/refine-unit-tests` with a Conventional Commits subject; no amend and no history rewrite, so provenance stays intact. A mid-commit hook rewrite is landed as a follow-up commit rather than folded in. |
| T-fp1-05 | Tampering | supply chain | low | accept | NO package-manager install task exists in this plan (no npm/pip/cargo add or install). `npx prettier`, `node --test` and `pre-commit` execute only already-installed, lockfile-pinned dependencies, so the package-legitimacy gate has no target here. |
</threat_model>

<verification>
- `! grep -q 'tests/orchestrators/plugin/cross-surface-reason-parity\.test\.ts' extensions/pi-claude-marketplace/orchestrators/plugin/install.messaging.ts` — the moved suite's old path is gone.
- `! grep -q 'tests/edge/index-handler\.test\.ts' extensions/pi-claude-marketplace/bridges/hooks/event-router.ts` — the deleted suite's path is gone.
- **The closing gate, and the only one that is RED at HEAD** — every `tests/` path cited in
  either file resolves in `git ls-files`:
  `P=$(grep -ho 'tests/[A-Za-z0-9._/-]*\.test\.ts' <both files>) && test -n "$P" && for p in $P; do git ls-files --error-unmatch "$p" >/dev/null; done`
  The planner ran this at `6bc71dc4`: it reports both cited paths UNRESOLVED and exits
  non-zero, so it is not vacuous and it goes green only when both repoints land. The
  `test -n "$P"` arm guards the other failure mode — a run that finds no citation at all and
  would otherwise pass over an empty loop.
- `grep -q 'WR-05' extensions/pi-claude-marketplace/bridges/hooks/event-router.ts` — the
  traceability anchor survives.
- Comment-only across BOTH files in the committed change:
  `D=$(git show --format= -U0 HEAD -- extensions/pi-claude-marketplace) && N=$(printf '%s\n' "$D" | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-][[:space:]]*(//|\*)' | wc -l | tr -d ' ') && test "$N" -eq 0`
  The planner negative-controlled this filter against a planted `+  const x = 1;` line and it
  correctly reports 1, so it fires on a real code change rather than passing by construction.
- `node --test tests/architecture/cross-surface-reason-parity.test.ts` passes (17/17 measured green by the planner pre-edit).
- `node --test --test-name-pattern "pristine workspace reconciles" tests/index.test.ts` passes (1/1 measured green by the planner pre-edit).
- `pre-commit run --files <both paths>` exits clean, which covers lint, prettier, `tsc`, `fallow` and the changed-pair direct-coverage gate for this item.
- `.planning/WINDOWS.md` is unmodified — entries 23 and 26 are dispositioned by sibling item `260912-fp3`.
</verification>

<success_criteria>
- Both stale test-path citations are repointed at paths that exist in `git ls-files`; both
  ledger entries closed by a real edit, not by a stale-ledger finding.
- The `WR-05` anchor is retained in `event-router.ts` and its comment names the surviving
  assertion in `tests/index.test.ts` with a greppable phrase from that test's title.
- Zero non-comment lines changed in either file; no behavior change.
- One Conventional Commits commit on `features/refine-unit-tests`, pre-commit clean before the
  commit, tree clean after it, `.planning/WINDOWS.md` untouched, `npm run check` not run.
</success_criteria>

<output>
Create `.planning/quick/260912-fp1-repoint-two-stale-test-path-references-i/260912-fp1-SUMMARY.md` when done.

Report explicitly: that BOTH ledger entries (23, 26) were closed by a real source edit rather
than found stale, the new path cited at each site, confirmation that the `WR-05` anchor was
kept, and the measured non-comment-line count of the diff (which must be 0).
</output>
