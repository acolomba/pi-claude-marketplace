---
phase: 260912-fp0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/edge/register.ts
autonomous: true

must_haves:
  truths:
    - "No comment in edge/register.ts asserts that the working directory is read once when the command is registered, or that a resolver is captured once and reused."
    - "Both cwd comment sites state the measured behavior: `process.cwd()` is evaluated inside the `getArgumentCompletions` arrow, so it is read on every completion lookup."
    - "The `D-04` anchor on the file header survives, and no phase/plan/wave/milestone reference is introduced."
    - "Executable behavior is byte-unchanged: the only diff hunks are comment lines."
  artifacts:
    - extensions/pi-claude-marketplace/edge/register.ts
  key_links:
    - "The corrected inline comment cites the surviving pin by path AND title (`tests/edge/register.test.ts`, \"resolves argument completions against the working directory the callback runs in\"), so the claim stays checkable if the suite moves."
---

<objective>
Correct the two cwd-lifetime comments in `extensions/pi-claude-marketplace/edge/register.ts`
so they describe what the code does. WINDOWS ledger entry 20.

Purpose: two production comments assert a property the module does not have. The file header
(around `:18-20`) and the inline comment above `getArgumentCompletions` (around `:117-119`)
both say the working directory is captured when the command is registered and reused for
every later lookup. It is not. `process.cwd()` is evaluated INSIDE the
`getArgumentCompletions` arrow body, so it is read on every completion lookup and nothing is
closed over. The runtime behavior is the correct one (completions resolve against the
directory the user is actually in) and is pinned by a live two-root test case. The defect is
the comment text.

Output: a comment-only diff on one production file, plus one Conventional Commits commit.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/edge/register.ts
</context>

<pre_read_findings>
Verified by the planner against the live file at `6bc71dc4`. Line numbers HAVE DRIFTED from
the ledger — locate by content, not by number:

- File header, currently `:18-20` — a three-line paragraph that sanctions `process.cwd()` at
  the registration glue layer and then closes with a sentence asserting the cwd's lifetime is
  tied to command registration. The sanction sentence is TRUE and must survive; the
  lifetime sentence is the false claim.
- Inline comment, currently `:117-119` (the ledger says `:104-106`) — three lines directly
  above the `getArgumentCompletions` property. Line 1 restates the sanction (true, keep).
  Lines 2-3 assert a registration-time capture reused by a resolver that outlives the
  registration call (false).
- The code under it, currently `:120-121`:
  `getArgumentCompletions: (prefix) => getArgumentCompletions(prefix, makeLocationsResolver(process.cwd()), deps.completionCache)`
  — `process.cwd()` sits inside the arrow body, so it re-evaluates per call and
  `makeLocationsResolver` is rebuilt per call. Nothing is captured at registration.
- Header `:9-10` also names `makeLocationsResolver(process.cwd())` in the completion flow.
  That line makes no lifetime claim and is accurate — DO NOT touch it.
- `D-04` on header `:3` is a traceability anchor — keep it.
- The surviving pin is `tests/edge/register.test.ts`, test
  `"resolves argument completions against the working directory the callback runs in (D-04)"`:
  it seeds one marketplace under the registration root, `process.chdir`s to a second root
  seeded with a different marketplace, drives the captured callback, and asserts the SECOND
  root's name comes back.
- No gate asserts source strings against `register.ts`: it is absent from `tests/index.test.ts`
  and from every `tests/architecture/*` source-scan target list. Citing a `tests/...` path in
  a production comment is established house style (see `bridges/hooks/dispatch-exec.ts:47`,
  `edge/flag-catalog.ts:16`).
</pre_read_findings>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite both cwd comment sites to the measured behavior</name>
  <files>extensions/pi-claude-marketplace/edge/register.ts</files>
  <action>
Edit COMMENT TEXT ONLY in `extensions/pi-claude-marketplace/edge/register.ts`. Do not touch a
single executable line. Two sites, both located by content per `<pre_read_findings>`.

Site 1 — the file-header paragraph (currently `:18-20`). Keep the first two lines that
sanction `process.cwd()` at the registration glue layer verbatim. Replace the closing
lifetime sentence with a statement of the real semantics: the read happens inside the
`getArgumentCompletions` callback, once per completion lookup, so completions resolve against
the directory the process is in at the moment the user types, and no directory value is
retained between lookups.

Site 2 — the three-line comment directly above the `getArgumentCompletions` property
(currently `:117-119`). Keep its first line (the single-sanctioned-site statement). Replace
the two lines that follow with: the arrow body runs per completion lookup and reads
`process.cwd()` there, so a fresh `makeLocationsResolver` is built from the current directory
on each lookup. Then cite the pin by BOTH path and title so it stays findable if the suite
relocates: `tests/edge/register.test.ts`, test "resolves argument completions against the
working directory the callback runs in".

One clarifying negative is allowed and wanted at site 2 — a reader's default assumption is a
registration-time capture, and dispelling it is the entire point of this fix. Phrase it as a
property of the code as it stands ("the read happens per lookup, not once when the command is
registered"), never as narration of a shape that was removed.

Constraints on the prose, from `.claude/rules/typescript-comments.md`:
  - Keep the `D-04` anchor on header `:3` and every other requirement/decision ID untouched.
  - Introduce NO `Phase NN` / `Plan NN` / `Wave N` / `milestone vX.Y` reference, and no
    `(reviewed …)` parenthetical.
  - Write present-tense facts about the current code. Do not write that a comment or behavior
    "used to" say anything, and do not describe a prior shape.
  - Match the file's existing `//` block style and keep lines inside the surrounding ~72-column
    wrap of the file's other comment blocks.

Leave header `:9-10`, header BLOCK C, header BLOCK A, both JSDoc blocks, and the
`session_start` / TC-7 comment exactly as they are.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && ! grep -qE 'per-command-registration|Captured at registration time|closed-over resolver' extensions/pi-claude-marketplace/edge/register.ts && grep -q 'completion lookup' extensions/pi-claude-marketplace/edge/register.ts && grep -q 'tests/edge/register.test.ts' extensions/pi-claude-marketplace/edge/register.ts && grep -q 'D-04' extensions/pi-claude-marketplace/edge/register.ts && ! grep -qiE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|milestone v[0-9]' extensions/pi-claude-marketplace/edge/register.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && d=$(git diff HEAD -U0 -- extensions/pi-claude-marketplace/edge/register.ts) && test -n "$d" && n=$(printf '%s\n' "$d" | awk '/^(\+\+\+|---)/ {next} /^[+-][[:space:]]*\/\// {next} /^[+-]/ {c++} END {print c+0}') && test "$n" -eq 0</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && node --test tests/edge/register.test.ts</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && npx prettier --check extensions/pi-claude-marketplace/edge/register.ts</automated>
  </verify>
  <done>
Neither cwd comment site claims a registration-time capture or a resolver reused across
lookups; both state the per-lookup read. The inline site cites `tests/edge/register.test.ts`
by path and by test title. `D-04` survives. The `git diff` of the file contains ZERO changed non-comment lines.
`tests/edge/register.test.ts` passes and prettier reports the file formatted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate and commit the comment fix</name>
  <files>extensions/pi-claude-marketplace/edge/register.ts</files>
  <action>
Stage the explicit path (never `git add -A`):
`git add extensions/pi-claude-marketplace/edge/register.ts`.

Run `pre-commit run --files extensions/pi-claude-marketplace/edge/register.ts` BEFORE
committing. If a hook rewrites the file or reports a failure, inspect the change, restage the
same explicit path, and re-run until the run is clean. Never pass `--no-verify`. Never use
`git commit --amend` to recover from a hook failure — a failed hook means no commit happened,
so amending would rewrite the PREVIOUS commit.

Commit on the current branch `features/refine-unit-tests`. Confirm with
`git rev-parse --abbrev-ref HEAD` before committing; if it reads `main`, STOP and report — do
not commit.

Commit with a Conventional Commits message, title <=72 chars, body lines <=80 chars, and NO
phase/plan/wave/milestone reference. Suggested:

  title: `docs(edge): fix cwd-lifetime comments on argument completions`
  body:  state that `process.cwd()` is evaluated inside the
         `getArgumentCompletions` arrow, so it is read per completion lookup
         and nothing is closed over; the behavior is unchanged and correct,
         and the two comments now match it. Name the pinning test.

After the commit, run `git status --short` and `git diff --stat`. The prettier/whitespace
hooks can rewrite a file mid-commit while the commit still succeeds, leaving a dirty tree. If
anything is dirty, land it as a FOLLOW-UP commit — never an amend.

Do NOT run `npm run check` and do NOT touch `.planning/WINDOWS.md`. The batch coordinator runs
the suite once at the end, and the ledger disposition for entry 20 belongs to sibling item
`260912-fp3`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && br=$(git rev-parse --abbrev-ref HEAD) && test -n "$br" && test "$br" != main && st=$(git status --short -- extensions/pi-claude-marketplace/edge/register.ts) && test -z "$st"</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && subj=$(git log -1 --format=%s) && body=$(git log -1 --format=%B) && printf '%s\n' "$subj" | grep -qE '^[a-z]+(\([a-z0-9./-]+\))?!?: .{4,}' && test "${#subj}" -le 72 && ! printf '%s\n' "$body" | grep -qiE 'Phase [0-9]+|Plan [0-9]+|Wave [0-9]+|milestone v[0-9]'</automated>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && files=$(git show --numstat --format= HEAD) && printf '%s\n' "$files" | grep -q 'edge/register.ts' && n=$(printf '%s\n' "$files" | awk 'NF {c++} END {print c+0}') && test "$n" -le 2</automated>
  </verify>
  <done>
HEAD is on `features/refine-unit-tests` (not `main`), carries a Conventional Commits subject
under 72 chars with no planning references, touches `edge/register.ts` (and at most one
follow-up path), and the working tree is clean for that file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed) | This plan changes comment text in one TypeScript file. No input parsing, no I/O surface, no new call site, no dependency. `process.cwd()` already reads a process-owned value and is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fp0-01 | Information Disclosure | `edge/register.ts` comment prose | low | mitigate | The corrected comments name only a repo-relative test path and a test title. No absolute path, host name, token, or operator directory may appear (`shared/redact-absolute-paths.ts` exists precisely because absolute paths are treated as leakable). |
| T-fp0-02 | Tampering | `edge/register.ts` executable lines | medium | mitigate | Task 1's second `<automated>` gate fails if the diff contains ANY changed non-comment line, so a behavior edit smuggled in beside the comment fix cannot pass. |
| T-fp0-03 | Repudiation | git history | low | accept | Single commit on `features/refine-unit-tests` with a Conventional Commits subject; no amend and no history rewrite, so provenance stays intact. |
| T-fp0-04 | Tampering | supply chain | low | accept | NO package-manager install task exists in this plan (no npm/pip/cargo add or install). `npx prettier` and `node --test` execute only already-installed, lockfile-pinned devDependencies, so the package-legitimacy gate has no target here. |
</threat_model>

<verification>
- `! grep -qE 'per-command-registration|Captured at registration time|closed-over resolver' extensions/pi-claude-marketplace/edge/register.ts` — every stale lifetime claim is gone.
- `git diff` of `edge/register.ts` against the pre-task tree shows comment lines only.
- `node --test tests/edge/register.test.ts` exits 0 (the module still parses and the two-root
  completion case still holds).
- `npx prettier --check extensions/pi-claude-marketplace/edge/register.ts` exits 0.
- `git log -1` subject is Conventional Commits, <=72 chars, and free of planning references.
</verification>

<success_criteria>
Both cwd comments in `extensions/pi-claude-marketplace/edge/register.ts` describe the
per-completion-lookup read and cite the pinning test; no executable line changed; the change is
committed on `features/refine-unit-tests` with a clean `pre-commit` run and a clean tree
afterwards. `.planning/WINDOWS.md` is untouched by this plan.
</success_criteria>

<output>
Create `.planning/quick/260912-fp0-correct-the-cwd-lifetime-comments-in-ext/260912-fp0-SUMMARY.md` when done.

The summary MUST state, for sibling item `260912-fp3` to dispose WINDOWS entry 20:
whether the fix landed as a real edit (it should — the planner confirmed both stale claims are
live in the file at `6bc71dc4`) or whether the ledger turned out to be stale, plus the commit
SHA.
</output>
