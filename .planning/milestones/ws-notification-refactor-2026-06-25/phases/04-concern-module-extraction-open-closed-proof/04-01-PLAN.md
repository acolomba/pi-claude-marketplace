---
phase: 04-concern-module-extraction-open-closed-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts
  - extensions/pi-claude-marketplace/shared/notify.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/install.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/list.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/update.ts
  - extensions/pi-claude-marketplace/orchestrators/import/execute.ts
autonomous: true
requirements: [MOD-04]

must_haves:
  truths:
    - "Adding a command requires zero edits to the soft-dep marker decision (it owns its data in shared/concerns/soft-dep.ts)"
    - "Rendered notification output is byte-identical before and after the extraction (soft-dep markers render in the same agents-before-mcp order)"
    - "composeReasons keeps its exact 4-arg signature so the 13+ orchestrator render-map call sites are untouched"
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts"
      provides: "Soft-dep concern: DEPENDENCIES, Dependency, marker constants, pure softDepMarkers() helper"
      exports: ["DEPENDENCIES", "Dependency", "softDepMarkers"]
      contains: "export function softDepMarkers"
    - path: "extensions/pi-claude-marketplace/shared/notify.ts"
      provides: "composeReasons delegates its soft-dep branch to softDepMarkers; no longer declares DEPENDENCIES/Dependency/markers"
      contains: "softDepMarkers(declaresAgents, declaresMcp, probe)"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/notify.ts"
      to: "extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts"
      via: "composeReasons calls softDepMarkers"
      pattern: "softDepMarkers\\("
    - from: "extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts"
      to: "extensions/pi-claude-marketplace/shared/notify.ts"
      via: "import type { Reason } (type-only — the cycle safeguard)"
      pattern: "import type \\{ Reason \\}"
---

<objective>
Extract the soft-dep marker injection concern out of `shared/notify.ts` into a new
`shared/concerns/soft-dep.ts` module (MOD-04, per D-01 direct-function-call wiring —
NO registry, NO `Concern` interface). The concern owns `DEPENDENCIES`, the
`Dependency` type, the two marker constants, and a pure `softDepMarkers(declaresAgents,
declaresMcp, probe)` helper. Central `composeReasons` STAYS in notify.ts and delegates
its soft-dep branch to the concern by calling `softDepMarkers`.

This is an OUTPUT-NEUTRAL move-and-rewire, not a rewrite. The soft-dep marker logic is
lifted verbatim (agents-before-mcp push order preserved). `composeReasons` keeps its
exact 4-arg signature, so the 13+ orchestrator `*.messaging.ts` render-map call sites
are invisible to this change.

Purpose: Closes the soft-dep half of MOD-04 — one of the two cross-cutting concerns
that must leave the monolith so `notify.ts` slims to envelope + reducer + shared
vocabulary, and so adding a command requires 0 `notify.ts` edits.
Output: New `shared/concerns/soft-dep.ts`; slimmed `composeReasons` in `notify.ts`;
the `Dependency` importers repointed to the concern.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/notification-refactor/STATE.md
@.planning/workstreams/notification-refactor/ROADMAP.md
@.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-CONTEXT.md
@.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-RESEARCH.md
@.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract soft-dep concern to shared/concerns/soft-dep.ts and delegate composeReasons</name>
  <files>extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts, extensions/pi-claude-marketplace/shared/notify.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply-outcomes.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, extensions/pi-claude-marketplace/orchestrators/marketplace/update.ts, extensions/pi-claude-marketplace/orchestrators/plugin/install.ts, extensions/pi-claude-marketplace/orchestrators/plugin/reinstall.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list.ts, extensions/pi-claude-marketplace/orchestrators/plugin/update.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts</files>
  <read_first>
    - extensions/pi-claude-marketplace/shared/notify.ts (the source spans: DEPENDENCIES :476, Dependency :495, SOFT_DEP_MARKER_AGENTS/_MCP :1620-1621, composeReasons :1749 with its soft-dep branch :1755-1763)
    - extensions/pi-claude-marketplace/shared/notify-reasons.ts (the intra-shared module-shape + `import type { Reason } from "./notify.ts"` precedent — copy this header shape)
    - extensions/pi-claude-marketplace/platform/pi-api.ts (lines 83-126: the SoftDepStatus interface — imported type-only by the concern)
    - .planning/.../04-RESEARCH.md §2 (soft-dep seam: exact spans, signature, threading) + §4 (import fence) + §7 Risk 1/5 (type-only discipline, agents-before-mcp byte order)
    - .planning/.../04-PATTERNS.md "shared/concerns/soft-dep.ts" section (verbatim lift excerpts) + the "import type discipline" and "import-order" shared patterns
    - .claude/rules/typescript-comments.md (no GSD phase/plan references in comments; D-NN anchors allowed)
  </read_first>
  <action>
    Create `extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts`. Move these
    symbols out of notify.ts into it (per D-01, the concern owns its data + logic):
    `DEPENDENCIES` (the `["agents", "mcp"] as const` tuple, currently notify.ts:476),
    `Dependency` (the `(typeof DEPENDENCIES)[number]` literal-union, currently :495),
    `SOFT_DEP_MARKER_AGENTS` and `SOFT_DEP_MARKER_MCP` (the two `Reason` marker
    constants, currently :1620-1621 — keep them file-private to the concern), and the
    soft-dep marker decision currently living inside `composeReasons` at notify.ts:1755-1763.
    Export that decision as a PURE helper `softDepMarkers(declaresAgents: boolean,
    declaresMcp: boolean, probe: SoftDepStatus): readonly Reason[]` that pushes
    `SOFT_DEP_MARKER_AGENTS` iff `declaresAgents && !probe.piSubagentsLoaded`, then
    `SOFT_DEP_MARKER_MCP` iff `declaresMcp && !probe.piMcpAdapterLoaded` — agents BEFORE
    mcp (byte-critical order; any reorder fails catalog-uat per RESEARCH §7 Risk 5).
    `DEPENDENCIES` and `Dependency` are exported; `softDepMarkers` is exported.

    The concern imports ONLY types from outside (RESEARCH §7 Risk 1 — type-only is the
    real cycle safeguard since no `import-x/no-cycle` rule exists):
    `import type { Reason } from "../notify.ts"` (intra-shared, MUST be `import type`)
    and `import type { SoftDepStatus } from "../../platform/pi-api.ts"` (shared→platform,
    the one sanctioned sibling; Pi types come through pi-api.ts per the ESLint Block E
    chokepoint). Note the depth: `../notify.ts` and `../../platform/pi-api.ts` (one level
    deeper than the notify.ts originals). Give the file a leading block comment in the
    `notify-reasons.ts` style (relative module path + what it owns + what stays in
    notify.ts; D-01 anchor allowed, NO GSD phase/plan references).

    In `shared/notify.ts`: DELETE the moved declarations (`DEPENDENCIES`, `Dependency`,
    `SOFT_DEP_MARKER_AGENTS`, `SOFT_DEP_MARKER_MCP`). Add a VALUE import
    `import { softDepMarkers } from "./concerns/soft-dep.ts"` and, because notify.ts
    message interfaces still declare `dependencies: readonly Dependency[]`, re-import the
    relocated types/values it still references: `import { DEPENDENCIES } from "./concerns/soft-dep.ts"`
    (value, if notify.ts still references the tuple) and `import type { Dependency } from
    "./concerns/soft-dep.ts"`. Replace the soft-dep branch inside `composeReasons`
    (:1755-1763) with the single delegating line `composed.push(...softDepMarkers(
    declaresAgents, declaresMcp, probe))` after `const composed: Reason[] = reasons ===
    undefined ? [] : [...reasons];`. `composeReasons` KEEPS its exact 4-arg signature
    and stays exported — do NOT change its parameters or move it (RESEARCH §7 Non-risk:
    13+ orchestrator callers must stay untouched). Do NOT touch `redactAbsolutePaths`
    (:223, security primitive per NFR-9), the reducer spine, the envelope, the probe
    threading (`softDepStatus(pi)` at :3152/:3244/:3294 STAYS in the renderer), or any
    other shared vocabulary.

    Repoint every `Dependency` importer from `shared/notify.ts` to
    `shared/concerns/soft-dep.ts` (all are orchestrators→shared, allowed; type-only
    imports). Grep first to confirm the full set, then update: `apply-outcomes.ts:26`,
    `apply.ts:68`, `marketplace/update.ts`, `plugin/install.ts:145`, `plugin/reinstall.ts:115`,
    `plugin/list.ts:77`, `plugin/update.ts:135`, `import/execute.ts:41`. Run
    `grep -rn "\bDependency\b\|DEPENDENCIES" extensions/pi-claude-marketplace --include="*.ts"`
    to verify no importer is missed (a missed repoint is a typecheck error, not silent).
    No test imports `DEPENDENCIES`/markers directly (verified), so no test edits for the
    soft-dep move. Run `eslint --fix` + `prettier --write` on every new/edited file to
    settle import-order and formatting before committing.
  </action>
  <verify>
    <automated>cd extensions/pi-claude-marketplace 2>/dev/null; cd "$(git rev-parse --show-toplevel)" && node --test tests/architecture/catalog-uat.test.ts && git diff --exit-code docs/output-catalog.md && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture/catalog-uat.test.ts` exits 0 (rendered bytes unchanged)
    - `git diff --exit-code docs/output-catalog.md` produces no output (catalog byte-frozen)
    - `npm run typecheck` exits 0 (all `Dependency` importers repointed; no dangling reference)
    - `npm run lint` exits 0 (import-fence `import-x/no-restricted-paths` + `import-x/order` clean)
    - `shared/concerns/soft-dep.ts` exists and contains `export function softDepMarkers` and `export const DEPENDENCIES`
    - `grep -c "import type { Reason }" extensions/pi-claude-marketplace/shared/concerns/soft-dep.ts` is 1 (type-only cycle safeguard)
    - `shared/notify.ts` no longer declares `const DEPENDENCIES` nor `const SOFT_DEP_MARKER_AGENTS` (moved): `grep -vE '^\s*[*/]' extensions/pi-claude-marketplace/shared/notify.ts | grep -c "const SOFT_DEP_MARKER_AGENTS" ` is 0
    - `shared/notify.ts` `composeReasons` calls the concern: `grep -c "softDepMarkers(declaresAgents, declaresMcp, probe)" extensions/pi-claude-marketplace/shared/notify.ts` is >= 1
    - `composeReasons` signature unchanged: it still takes `(reasons, declaresAgents, declaresMcp, probe)` and stays exported (the 13+ orchestrator callers do not change)
  </acceptance_criteria>
  <done>
    The soft-dep concern is a standalone `shared/concerns/soft-dep.ts` module that owns
    DEPENDENCIES/Dependency/markers/softDepMarkers; notify.ts's composeReasons delegates
    to it with an unchanged signature; all Dependency importers are repointed; rendered
    output is byte-identical (catalog-uat green, output-catalog.md diff empty); typecheck
    and lint (fence) pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentation refactor — moves string-building code; no new input, auth, crypto, network, or persistence surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | rendered notification bytes | mitigate | `catalog-uat` byte-equality + `git diff --exit-code docs/output-catalog.md` gate every commit; soft-dep marker order (agents-before-mcp) preserved verbatim. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No packages added/removed/upgraded this phase (RESEARCH Package Legitimacy Audit: N/A). No install tasks → no legitimacy checkpoint needed. |

No new threats — pure presentation refactor. `redactAbsolutePaths` (notify.ts:223) is
the cross-cutting path-redaction security primitive and MUST stay in notify.ts (NFR-9);
it is NOT a concern to extract.
</threat_model>

<verification>
- `node --test tests/architecture/catalog-uat.test.ts` green (byte-equality oracle)
- `git diff --exit-code docs/output-catalog.md` empty (output-neutral)
- `npm run typecheck` + `npm run lint` green (types resolve, fence intact)
</verification>

<success_criteria>
- `shared/concerns/soft-dep.ts` owns DEPENDENCIES, Dependency, the markers, and the pure `softDepMarkers` helper
- `composeReasons` stays central in notify.ts with its exact 4-arg signature, delegating the soft-dep branch
- All `Dependency` importers repointed; no notify.ts soft-dep declarations remain
- Rendered output byte-identical; typecheck + lint green
</success_criteria>

<output>
Create `.planning/workstreams/notification-refactor/phases/04-concern-module-extraction-open-closed-proof/04-01-SUMMARY.md` when done
</output>
