---
phase: 07-reliable-coverage-metrics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/coverage-capture.mjs
  - scripts/coverage-capture.runtime.mjs
  - scripts/coverage-capture.manifest.mjs
  - tests/scripts/coverage-capture.test.ts
  - package.json
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 31000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One successful native unit execution produces both LCOV and raw V8 tied to the exact executed source."
    - "Source/test/tool drift or incomplete workers cannot publish a successful metric run."
    - "Native unit behavior and coverage dimensions remain unchanged."
  artifacts:
    - path: scripts/coverage-capture.mjs
      provides: "Immutable same-run native coverage capture behavior and acceptance evidence"
    - path: scripts/coverage-capture.runtime.mjs
      provides: "Immutable same-run native coverage capture behavior and acceptance evidence"
    - path: scripts/coverage-capture.manifest.mjs
      provides: "Immutable same-run native coverage capture behavior and acceptance evidence"
  key_links:
    - from: scripts/coverage-capture.mjs
      to: tests/scripts/coverage-capture.test.ts
      via: Independently authored controls exercise the real capture/converter/validator/consumer operation.
---



<objective>
Immutable same-run native coverage capture.
Purpose: One successful native unit execution produces both LCOV and raw V8 tied to the exact executed source.
Output: scripts/coverage-capture.mjs, scripts/coverage-capture.runtime.mjs, scripts/coverage-capture.manifest.mjs, tests/scripts/coverage-capture.test.ts, package.json.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md

</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Capture one native unit fixture and its exact executed source</name>
  <files>scripts/coverage-capture.mjs, scripts/coverage-capture.runtime.mjs, scripts/coverage-capture.manifest.mjs, tests/scripts/coverage-capture.test.ts, package.json</files>
  <behavior>The same tiny unit selection produces native LCOV, raw V8 and immutable executed-source records with one run identity; native outcomes and production LCOV counts equal the ordinary runner.</behavior>
  <action>Per D-01, D-02 and D-04, write a real child-process fixture test first. Add a capture CLI and synchronous runtime source-capture hook using Node registerHooks/stripTypeScriptTypes with the installed runtime, preloaded before application modules and inherited by test workers. Capture the immutable source bytes and the exact JavaScript supplied for evaluation under the original module URL; native type stripping is the only permitted transformation. For TypeScript, supply the recorded Node-stripped JavaScript as the actual module input so reconstruction is not an assumption. Preserve module resolution, exports, line boundaries and native test coverage flags; refuse unsupported formats/transforms instead of approximating them. Disable the compile cache and set a unique NODE_V8_COVERAGE directory explicitly. Establish the manifest interface for run ID, selected test paths/argv, input inventory/digests, executed-source digests, runtime/tool identities, worker captures and artifact digests. Compare this tracer with ordinary native execution: identical independent outcomes and LCOV production line/function/branch counts are required before expanding. Add coverage:capture as a real package-script entry, leaving the normal unit/check path unchanged until 07-08. The CLI accepts --root for isolated fixture roots; fixture roots use the same production/test directory and selection rules, not a weaker coverage configuration. Set NODE_DISABLE_COMPILE_CACHE=1 explicitly. Hash the exact recorded Node invocation and reject unqualified loaders/transforms rather than mixing their output with the native-strip identity contract.</action>
  <fails_when>Fails if conversion later rereads mutable source as execution evidence, the hook changes native outcomes/coverage, a second unit run supplies raw data, or unsupported transforms are silently accepted.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-capture.test.ts</automated></verify>
  <done>The real fixture has one successful same-run capture with proven executed-source identity and native equivalence.</done>
</task>

<task type="auto" tdd="true">
  <name>Bind the full authoritative unit selection and worker population</name>
  <files>scripts/coverage-capture.mjs, scripts/coverage-capture.runtime.mjs, scripts/coverage-capture.manifest.mjs, tests/scripts/coverage-capture.test.ts</files>
  <behavior>Every selected test/worker and relevant input is represented; failed/interrupted workers, missing raw captures and empty production inventories reject the run.</behavior>
  <action>Per D-01, D-02 and D-10, select exactly the existing unit glob population, including script/architecture tests and tests/index.test.ts, with one authoritative selection used by ordinary and captured execution. Preserve TEST_CONCURRENCY handling and native reporter arguments. Enumerate every production .ts source and the selected tests/support modules, repository resources consumed by those tests, configs, lockfile and tooling inputs; record sorted sets plus SHA-256 bytes. Capture source modules as immutable per-run content before execution, and reject any loaded bytes differing from that inventory. Include worker registration/completion evidence and reconcile raw V8 records against it; launch error, signal, nonzero tests, interrupted worker, missing capture and unexpected reused run data are separate failures. Capture nested subprocess evidence without allowing a child fixture's environment override to replace the parent run's destination. Do not equate native LCOV function counts with future AST counts. The runtime contract is the exact conformance-tested Node version; app package engines remain unchanged.</action>
  <fails_when>Fails if selection differs from npm test, an unregistered/incomplete production capture passes, native and raw artifacts come from different workers/runs, or a missing module is omitted.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-capture.test.ts</automated></verify>
  <done>The complete existing unit population can be captured once with explicit process/input completeness evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Reject drift and publish only complete immutable capture bundles</name>
  <files>scripts/coverage-capture.mjs, scripts/coverage-capture.manifest.mjs, tests/scripts/coverage-capture.test.ts</files>
  <behavior>Source edits/additions/deletions, tool changes, stale reports and failed capture invalidate success; successful bundles are published atomically after final verification.</behavior>
  <action>Per D-02 and D-10, check complete input sets and content before execution, after execution, before conversion handoff and before final publication. The loader's recorded actual bytes must match the immutable pre-run source, catching edits occurring during loading even if files later revert. Write raw records/source captures/LCOV into the unique owned run directory; retain failed evidence as failed, never as a reusable success. Manifest state distinguishes captured from accepted-converted so a successful test process alone cannot certify Istanbul. Remove/invalidate the previous public success pointer when starting a replacement run, publish a success manifest last by atomic rename and verify artifacts again when consumed. Reject missing manifest/LCOV/raw/source capture, foreign paths, duplicate canonical records, stale versions and swapped integration/e2e artifacts. Use contained paths and argv arrays, and scope cleanup to owned run directories.</action>
  <fails_when>Fails if mtime or a filename proves freshness, a failed run leaves an accepted pointer, source changes are repaired by offset shifts, or cleanup touches unrelated files.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-capture.test.ts</automated></verify>
  <done>Immutable capture bundles survive readback validation; every stale/failed/incomplete counterpart is refused.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion evidence | Coverage evidence |
| --- | --- | --- |
| 07-01-T1 | The same tiny unit selection produces native LCOV, raw V8 and immutable executed-source records with one run identity; native outcomes and production LCOV counts equal the ordinary runner. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-01-T2 | Every selected test/worker and relevant input is represented; failed/interrupted workers, missing raw captures and empty production inventories reject the run. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-01-T3 | Source edits/additions/deletions, tool changes, stale reports and failed capture invalidate success; successful bundles are published atomically after final verification. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-01-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-01-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-01-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-01-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-01-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. Fixture acceptance is not a certified production baseline: the complete current measurement belongs to 07-07 after Phase 6 stability. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- One successful native unit execution produces both LCOV and raw V8 tied to the exact executed source.
- Source/test/tool drift or incomplete workers cannot publish a successful metric run.
- Native unit behavior and coverage dimensions remain unchanged.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-01-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
