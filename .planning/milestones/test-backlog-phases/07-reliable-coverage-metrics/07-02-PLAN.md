---
phase: 07-reliable-coverage-metrics
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/scripts/coverage-producer.test.ts
  - tests/scripts/coverage-producer-fixtures.ts
  - scripts/coverage-producer.mjs
  - package.json
  - package-lock.json
  - scripts/build-coverage-producer.mjs
  - vendor/coverage/ast-v8-to-istanbul-1.0.6.patch
  - vendor/coverage/PROVENANCE.md
  - vendor/coverage/LICENSE
  - vendor/coverage/ast-v8-to-istanbul-1.0.6-project.1.tgz
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Only a pinned producer that preserves nested logical descendants is eligible for conversion."
    - "A maintained patch is reproducible from identified upstream bytes with original license and visible provenance."
    - "Ordinary tests and gates never mutate installed dependencies."
  artifacts:
    - path: scripts/coverage-producer.mjs
      provides: "Conformance-qualified producer delivery and licensed repair behavior and acceptance evidence"
    - path: scripts/build-coverage-producer.mjs
      provides: "Conformance-qualified producer delivery and licensed repair behavior and acceptance evidence"
  key_links:
    - from: tests/scripts/coverage-producer.test.ts
      to: scripts/coverage-producer.mjs
      via: Independently authored conformance controls qualify the exact installed producer payload bound by the delivery receipt.
---



<objective>
Conformance-qualified producer delivery and licensed repair.
Purpose: Only a pinned producer that preserves nested logical descendants is eligible for conversion.
Output: tests/scripts/coverage-producer.test.ts, tests/scripts/coverage-producer-fixtures.ts, scripts/coverage-producer.mjs, package.json, package-lock.json, scripts/build-coverage-producer.mjs, vendor/coverage/ast-v8-to-istanbul-1.0.6.patch, vendor/coverage/PROVENANCE.md, vendor/coverage/LICENSE, vendor/coverage/ast-v8-to-istanbul-1.0.6-project.1.tgz.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/07-reliable-coverage-metrics/07-01-SUMMARY.md
</context>

<scope_budget>
This plan owns one claim: the exact installed producer bytes correctly preserve the independently specified nested function/statement behavior. Reproduction, a bounded upstream repair and installed-byte qualification form that claim's three ordered tasks. Keep them together so the original counterexample, patch and final tested payload remain bound to one evidence chain.

The ten-path count includes four active code/test files, one targeted upstream code patch, two dependency records and three delivery/evidence artifacts. It does not represent ten independently designed modules:

| Responsibility | Exact paths | Context work |
| --- | --- | --- |
| Adapter and conformance | scripts/coverage-producer.mjs; tests/scripts/coverage-producer.test.ts; tests/scripts/coverage-producer-fixtures.ts | One adapter and its independently authored syntax corpus, reused in Tasks 1 and 3 |
| Maintained delivery | scripts/build-coverage-producer.mjs; vendor/coverage/ast-v8-to-istanbul-1.0.6.patch | One deterministic build/verifier and the specific logical-descendant traversal correction |
| Dependency resolution | package.json; package-lock.json | Selected package aliases and exact dependency resolution; inspect the changed lockfile records |
| Evidence and packaged output | vendor/coverage/PROVENANCE.md; vendor/coverage/LICENSE; vendor/coverage/ast-v8-to-istanbul-1.0.6-project.1.tgz | Bounded provenance, verbatim original license and generated archive; inspect manifest/payload and verify digests rather than loading binary/archive contents as prose |

The existing 35,000-token estimate is unchanged: Task 1 approximately 11,000, Task 2 10,000 and Task 3 14,000 tokens, with calibration factor 1 and low confidence from zero completed samples. This is below the 100,000-token planning budget. Each task still touches five files. No capture, source-map, general correspondence, consumer policy or live-production implementation belongs to this plan; those have separate owners in 07-01 and 07-03 through 07-08.

Read only the upstream walker and directly implicated registration paths needed to prove the targeted correction. If observed repair scope requires additional upstream subsystems, files or a materially larger context than projected, record the new evidence and obtain a bounded Phase 7 repair plan before expanding implementation. The current task cannot silently widen or qualify incomplete behavior. A corrected official release follows the same provenance/conformance chain and avoids unnecessary patch/archive outputs.
</scope_budget>

<tasks>

<task type="tracer" tdd="true">
  <name>Reproduce the producer omission through an independent conformance tracer</name>
  <files>tests/scripts/coverage-producer.test.ts, tests/scripts/coverage-producer-fixtures.ts, scripts/coverage-producer.mjs, package.json, package-lock.json</files>
  <behavior>The unmodified researched 1.0.6 producer is rejected for its missing nested logical callback and body statements; the source fixture's expected identities and hits are independent.</behavior>
  <action>Per D-03, D-05 and D-09, review the resolved package-provenance audit in 07-RESEARCH.md, verify selected package identities, exact registry integrity and license before installation, and use npm with --ignore-scripts. Pin Acorn, Istanbul merger and source-map codec to the reviewed/tested versions; verify current metadata for a corrected official producer release. Research establishes only 1.0.6 and a live registry refresh failed EAI_AGAIN, so do not invent a newer verified release. Start with a producer adapter and independently authored runtime fixtures: three source functions with one callback inside the left operand of a chained logical expression, and a nested callback with multiple partially executed statements. Capture with 07-01, compare exact function/body/statement spans and counters, and assert that unmodified 1.0.6 is not accepted. Add coverage:producer as the producer adapter's real CLI entry for converting a supplied captured fixture/bundle, and coverage:producer:check for the conformance suite; keep conversion failures explicit and the module inert on import. This task proves rejection of the known defect; it does not certify a candidate or enable CRAP. No new production exports, ignores or coordinate repair are allowed.</action>
  <acceptance_criteria>
    Per D-03, D-05 and D-09, record the original package/version, resolved archive integrity, SHA-256 of the actual producer payload and original license, and independently authored fixture source/expected-location hashes in the task ledger. The actual runtime control must reject the original missing logical callback and partially executed body statements at their exact expected identities. Task 2 may begin only with this recorded baseline and counterexample; a release number or function count alone is insufficient.
  </acceptance_criteria>
  <fails_when>Fails if success means only three functions were counted, Fallow match percentage is used as proof, the expected corpus is generated from producer output, or unmodified 1.0.6 is promoted.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-producer.test.ts</automated></verify>
  <done>The known omission is a durable failing-producer control using actual raw execution and exact independently specified expected syntax.</done>
</task>

<task type="auto" tdd="true">
  <name>Deliver a version-specific repair as a reproducible dependency artifact</name>
  <files>scripts/build-coverage-producer.mjs, vendor/coverage/ast-v8-to-istanbul-1.0.6.patch, vendor/coverage/PROVENANCE.md, vendor/coverage/LICENSE, vendor/coverage/ast-v8-to-istanbul-1.0.6-project.1.tgz</files>
  <behavior>The maintained artifact reproduces exactly from verified upstream bytes and a reviewed patch; changed upstream hashes, patch context, output hash or missing license fail.</behavior>
  <action>Per D-05, prefer a corrected upstream release only if the complete corpus can validate it. Otherwise produce the explicit 1.0.6-project.1 npm tarball from the verified 1.0.6 distribution in a temporary build directory. Record source URL/version/integrity, original author/MIT license, exact patch rationale and output hash. Fix the walker's skipped-logical-node handling so suppressing duplicate branch registration does not suppress descendant traversal; review statement and branch consequences rather than copying the one-line experiment blindly. Build tooling must verify the exact original digest/context, apply the visible patch in the temporary copy, retain original license, generate deterministic archive metadata and refuse drift. The packaged artifact is the actual dependency used by npm ci; no postinstall or ordinary test/gate command edits node_modules. If a corrected official release passes instead, the same build tool's --verify command checks its exact official bytes/integrity/license and records provenance; omit unnecessary patch/tarball files rather than retaining an unused fork. Retain the verification tool, provenance and license outputs in either route. Do not publish a fork remotely. The build script is a maintainer operation with a real package alias added in Task 3.</action>
  <acceptance_criteria>
    Per D-05, bind the baseline payload hash from Task 1 to the exact patch hash, retained original license hash and output archive/payload hashes in PROVENANCE.md. For the patch route, build in two separately created temporary directories and require byte-identical deterministic archives and payloads; --verify rejects an altered baseline, patch context, output digest or missing license. For a corrected official release, require the same exact official archive/payload/license identity checks without inventing a patch. Record these results before Task 3 installs and qualifies that delivery.
  </acceptance_criteria>
  <fails_when>Fails if the patch changes installed dependency files during checks, lacks original license/provenance, applies to unexpected upstream bytes, or depends on an unreproducible hand-edited tarball.</fails_when>
  <verify><automated>node scripts/build-coverage-producer.mjs --verify</automated></verify>
  <done>A reviewed licensed reproducible producer artifact exists, or a specifically verified corrected official release replaces it.</done>
</task>

<task type="auto" tdd="true">
  <name>Qualify the installed delivery against the full syntax corpus</name>
  <files>scripts/coverage-producer.mjs, tests/scripts/coverage-producer.test.ts, tests/scripts/coverage-producer-fixtures.ts, package.json, package-lock.json</files>
  <behavior>Installed delivered bytes pass nested/same-line/Unicode/method/partial/zero/worker controls; removing the repair or changing its digest invalidates acceptance.</behavior>
  <action>Per D-03, D-05 and D-09, install the selected exact corrected release or local licensed tarball via the lockfile with scripts disabled. Add coverage:producer:build for the maintained delivery/verification tool, using its --verify mode for an unpatched official release. Exercise changed upstream digest, patch context, output hash and license mutants in the producer suite so the maintainer tool's rejection claims are tested. Run the full conformance corpus against installed bytes: nested logical descendants and partially executed callback body statements; same-line/opposite-coverage neighbors; repeated methods and real numeric-suffix identifiers; arrows, methods, constructors, getters/setters, async/generator and empty functions; ternaries/default arguments/implicit else; Unicode UTF-16/CRLF/endpoints; split/overlapping worker reports with fresh AST instances. Preserve documented V8 observation limits after throwing calls and default arguments as explicit limits, not invented exact-execution claims. Record a producer identity receipt bound to exact package/artifact hashes and Node/parser/merger/codec versions; the later exact-map/schema validators must also pass before production acceptance. Rerunning the known-broken producer and corrupting the delivery identity must fail. No claim is based on the temporary experiment or release age. This producer-level corpus supplies independently constructed exact source maps as input; 07-03 implements and verifies the production mapping adapter. Qualification here proves the delivered walker/syntax behavior under valid inputs. Full pipeline promotion still requires 07-03 source-name/mapping checks and 07-04 independent schema/correspondence checks, rerunning this corpus whenever adapter bytes change.</action>
  <acceptance_criteria>
    Per D-03, D-05 and D-09, resolve the installed producer entry actually imported by coverage-producer.mjs and require its payload digest to match the verified delivery, package-lock resolution and Task 2 provenance. Record the exact Node/parser/merger/codec versions, adapter and independent fixture hashes, full conformance result and original-defect/delivery-drift rejection results in the qualification receipt. A changed installed payload, fixture, adapter or tool identity invalidates that receipt. All syntax/body-statement controls must pass for these exact bytes before 07-03/07-04 integration; final pipeline and stable production certification remain their scheduled later gates.
  </acceptance_criteria>
  <fails_when>Fails if function-entry recovery masks omitted body statements, a package/version change reuses qualification, or documented V8 limits are asserted as exact coverage.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-producer.test.ts</automated></verify>
  <done>The installed delivery is qualified by the full independent corpus and immutable provenance; known omission mutants fail.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion checkpoint | Hash/provenance checkpoint | Coverage evidence |
| --- | --- | --- | --- |
| 07-02-T1 | Original producer rejected for exact missing logical callback and partially hit body statements; oracle independently authored | Original archive integrity, payload/license SHA-256, fixture source and expected-location hashes | Record actual runtime positive/partial/zero cases and defect rejection; production/native/direct coverage settings unchanged |
| 07-02-T2 | Deterministic rebuild equality and altered-source/patch/output/license refusals | Task 1 baseline -> exact patch/license -> two equal archive/payload digests, or exact corrected official release provenance | Build/verifier success/error paths recorded; no application or test-assertion change |
| 07-02-T3 | Complete installed-byte syntax corpus plus original-defect and delivery-drift mutants | Imported installed payload equals verified delivery and lock resolution; receipt binds tools, adapter and fixture hashes | Full nested/body-statement/Unicode/method/partial/zero/worker controls; final native100/direct validation remains at stable integration boundaries |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-02-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-02-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-02-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-02-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-02-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. Fixture acceptance is not a certified production baseline: the complete current measurement belongs to 07-07 after Phase 6 stability. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- Only a pinned producer that preserves nested logical descendants is eligible for conversion.
- A maintained patch is reproducible from identified upstream bytes with original license and visible provenance.
- Ordinary tests and gates never mutate installed dependencies.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-02-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
