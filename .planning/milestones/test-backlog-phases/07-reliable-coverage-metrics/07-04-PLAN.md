---
phase: 07-reliable-coverage-metrics
plan: "04"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - scripts/coverage-correspondence.mjs
  - tests/scripts/coverage-correspondence.test.ts
  - scripts/coverage-schema.mjs
  - tests/scripts/coverage-schema.test.ts
  - scripts/coverage-validate.mjs
  - tests/scripts/coverage-validation.test.ts
  - package.json
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every executable production function and covered-statement construct maps exactly to the declared Istanbul model."
    - "Missing nested records cannot pass through a consumer fallback."
    - "Implicit absent branch syntax is admitted only by an exact AST-proven convention."
  artifacts:
    - path: scripts/coverage-correspondence.mjs
      provides: "Independent function/statement correspondence and strict schemas behavior and acceptance evidence"
    - path: scripts/coverage-schema.mjs
      provides: "Independent function/statement correspondence and strict schemas behavior and acceptance evidence"
    - path: scripts/coverage-validate.mjs
      provides: "Independent function/statement correspondence and strict schemas behavior and acceptance evidence"
  key_links:
    - from: scripts/coverage-correspondence.mjs
      to: tests/scripts/coverage-correspondence.test.ts
      via: Independently authored controls exercise the real capture/converter/validator/consumer operation.
---



<objective>
Independent function/statement correspondence and strict schemas.
Purpose: Every executable production function and covered-statement construct maps exactly to the declared Istanbul model.
Output: scripts/coverage-correspondence.mjs, tests/scripts/coverage-correspondence.test.ts, scripts/coverage-schema.mjs, tests/scripts/coverage-schema.test.ts, scripts/coverage-validate.mjs, tests/scripts/coverage-validation.test.ts, package.json.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/07-reliable-coverage-metrics/07-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Reject a missing nested function and its body statements independently</name>
  <files>scripts/coverage-correspondence.mjs, tests/scripts/coverage-correspondence.test.ts</files>
  <behavior>A complete manually authored exact map passes; deleting a nested callback or one of its partially hit statements fails even if another nearby function has the same name/hit percentage.</behavior>
  <action>Per D-03, D-04 and D-09, implement an independent executed-source AST inventory and validator. Define the versioned supported coverage-syntax relationship explicitly, including function declarations/expressions/arrows/methods and expression bodies, variable/statement constructs and branch constructs used by the producer. Each explicit function must correspond to exactly one Istanbul declaration/body record and vice versa. Each executable statement relationship in the adopted Istanbul model must be present at its exact source span, including statements nested inside logical-expression callbacks; do not limit traversal to nodes the producer happened to emit. Preserve evidence for genuinely runtime-generated V8 roots without pretending they are explicit AST functions. Use independent manually specified spans and known runtime executions for conformance oracles, not producer count equality. Expose the validator's interface for this plan's CLI in Task 3 and the publication pipeline in 07-05, keeping same-wave source-map/producer ownership disjoint.</action>
  <fails_when>Fails if equal counts, 100% AST hits, nearest-function lookup or function-entry recovery suffice when a nested statement/function is absent.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-correspondence.test.ts</automated></verify>
  <done>Independent exact function/statement correspondence rejects both known omission classes.</done>
</task>

<task type="auto" tdd="true">
  <name>Validate report schema, paths, counters and exact implicit-else semantics</name>
  <files>scripts/coverage-correspondence.mjs, scripts/coverage-schema.mjs, tests/scripts/coverage-correspondence.test.ts, tests/scripts/coverage-schema.test.ts</files>
  <behavior>Malformed coordinates/counter sets/paths and unexpected absent positions fail; an exact AST-proven implicit else retains its absent-source convention without fabricated zero coordinates.</behavior>
  <action>Per D-03, D-04 and D-09, implement strict structural and cross-field validation before JSON serialization and after readback: exact map/counter key sets; finite nonnegative integer hits; branch cardinality; canonical contained source paths; no duplicate canonical files; concrete positions finite integer, nonnegative, in bounds and ordered with valid one-based lines/zero-based columns. Reject null/Infinity/negative/fractional/reversed coordinates. Resolve the implicit-else policy by an actual producer plus installed Fallow fixture: an absent coordinate is allowed only for the exact empty location object associated with an AST if lacking else in the pinned producer schema. Tag/prove that relationship in evidence, preserve absent syntax, and reject missing coordinates everywhere else. Require exact schema/tool versions and unknown-key policy; source/statement/function additions or removals invalidate correspondence.</action>
  <fails_when>Fails if missing positions are filled with zero, Fallow's tolerant parser certifies invalid input, map/counter keys differ, or an ordinary malformed branch resembles an accepted implicit else.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-schema.test.ts tests/scripts/coverage-correspondence.test.ts</automated></verify>
  <done>Strict syntax/counter/path/schema checks distinguish proven implicit syntax from malformed data.</done>
</task>

<task type="auto" tdd="true">
  <name>Make the validator a real fail-closed CLI operation</name>
  <files>scripts/coverage-validate.mjs, scripts/coverage-correspondence.mjs, scripts/coverage-schema.mjs, tests/scripts/coverage-validation.test.ts, package.json</files>
  <behavior>The CLI accepts the complete independently authored fixture and rejects missing/malformed/stale identity, including a deliberately incomplete map that Fallow would approximately match.</behavior>
  <action>Per D-02, D-03 and D-09, add coverage:validate as a real package alias and CLI that reads the capture manifest, immutable sources and candidate map, invokes strict schema and complete correspondence validation, and writes an acceptance receipt only for that exact input fingerprint. Keep the validator modules reachable from this CLI immediately without a test-only script export or broad dead-code suppression. Preserve same-wave ownership: this plan owns the validator CLI/package alias; 07-03 owns source-map/producer changes. Exercise actual process status plus precise diagnostic for deleted nested function/statement, foreign path, duplicate file, changed schema/tool/source hashes, incomplete production inventory and malformed JSON. Invalid artifact status must differ from launch/setup failure. The accepted receipt is an input to 07-05, not authorization to publish a changed report.</action>
  <fails_when>Fails if malformed input yields success, receipt identity does not cover source/map/tool bytes, or process failure alone counts as correct detection.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-validation.test.ts tests/scripts/coverage-correspondence.test.ts tests/scripts/coverage-schema.test.ts</automated></verify>
  <done>The shipping validator provides a discriminating exact acceptance operation and refuses all misleading counterparts.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion evidence | Coverage evidence |
| --- | --- | --- |
| 07-04-T1 | A complete manually authored exact map passes; deleting a nested callback or one of its partially hit statements fails even if another nearby function has the same name/hit percentage. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-04-T2 | Malformed coordinates/counter sets/paths and unexpected absent positions fail; an exact AST-proven implicit else retains its absent-source convention without fabricated zero coordinates. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-04-T3 | The CLI accepts the complete independently authored fixture and rejects missing/malformed/stale identity, including a deliberately incomplete map that Fallow would approximately match. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-04-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-04-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-04-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-04-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-04-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. Fixture acceptance is not a certified production baseline: the complete current measurement belongs to 07-07 after Phase 6 stability. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- Every executable production function and covered-statement construct maps exactly to the declared Istanbul model.
- Missing nested records cannot pass through a consumer fallback.
- Implicit absent branch syntax is admitted only by an exact AST-proven convention.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-04-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
