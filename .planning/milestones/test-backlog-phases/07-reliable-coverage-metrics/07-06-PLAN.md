---
phase: 07-reliable-coverage-metrics
plan: "06"
type: execute
wave: 5
depends_on: ["07-05"]
files_modified:
  - scripts/check-coverage-risk.mjs
  - scripts/coverage-risk-policy.json
  - tests/scripts/check-coverage-risk.test.ts
  - package.json
  - scripts/coverage-correspondence.mjs
  - scripts/check-coverage-risk.negative.mjs
  - tests/scripts/check-coverage-risk.negative.test.ts
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every production health row has an exact validated source-function counterpart and measured statement coverage."
    - "CRAP 30 rejects scores meeting or exceeding 30 while all existing whole-tree complexity gates remain intact."
    - "Missing/estimated/approximately matched production coverage cannot pass the shipping wrapper."
  artifacts:
    - path: scripts/check-coverage-risk.mjs
      provides: "Measured production CRAP policy and exact Fallow correspondence behavior and acceptance evidence"
    - path: scripts/coverage-correspondence.mjs
      provides: "Measured production CRAP policy and exact Fallow correspondence behavior and acceptance evidence"
    - path: scripts/check-coverage-risk.negative.mjs
      provides: "Measured production CRAP policy and exact Fallow correspondence behavior and acceptance evidence"
  key_links:
    - from: scripts/check-coverage-risk.mjs
      to: tests/scripts/check-coverage-risk.test.ts
      via: Independently authored controls exercise the real capture/converter/validator/consumer operation.
---



<objective>
Measured production CRAP policy and exact Fallow correspondence.
Purpose: Every production health row has an exact validated source-function counterpart and measured statement coverage.
Output: scripts/check-coverage-risk.mjs, scripts/coverage-risk-policy.json, tests/scripts/check-coverage-risk.test.ts, package.json, scripts/coverage-correspondence.mjs, scripts/check-coverage-risk.negative.mjs, tests/scripts/check-coverage-risk.negative.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/07-reliable-coverage-metrics/07-05-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Trace the real covered/uncovered CC6 fixtures through Fallow</name>
  <files>scripts/check-coverage-risk.mjs, scripts/coverage-risk-policy.json, tests/scripts/check-coverage-risk.test.ts, package.json</files>
  <behavior>With the shipped policy 30, an uncovered CC6 function scores 42 and fails; the same fully exercised function scores 6 and passes with exact measured provenance.</behavior>
  <action>Per D-06, D-08 and D-09, create an additional production-coverage risk wrapper with an explicit policy file threshold 30 and a real coverage:risk package alias. Keep .fallowrc.json's existing whole-tree cyclomatic 20, cognitive 15, unit size 60 and maxCrap 0 unchanged: the separate wrapper applies the additional production CRAP policy without inventing test/script coverage or excluding extension analysis. It must validate the current accepted run through 07-05 before passing an explicit coverage path to the installed Fallow binary, with inherited FALLOW_COVERAGE/coverage-root overrides neutralized. Capture the complete health function population using a documented diagnostic invocation that emits every row (for 3.22.0, --max-crap 1 --format json --no-cache), treating its expected finding status as data only after strict schema validation; the wrapper's actual gate still applies the shipping threshold 30. Use full output without top/changed/baseline filters. Verify the CC6 offender and benign case through this actual wrapper and compare independently specified CC, statement coverage, score, source identity and exit status. Mandatory npm check activation waits for 07-07 certification.</action>
  <fails_when>Fails if Fallow estimates missing coverage, fixtures bypass the shipping policy, a reporting-only subprocess is mistaken for the gate verdict, or whole-tree limits change.</fails_when>
  <verify><automated>node --test tests/scripts/check-coverage-risk.test.ts</automated></verify>
  <done>The real consumer path enforces policy 30 and requires fresh measured production evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Prove exact consumer joins and the threshold boundary</name>
  <files>scripts/check-coverage-risk.mjs, scripts/coverage-correspondence.mjs, tests/scripts/check-coverage-risk.test.ts</files>
  <behavior>Same-line/repeated-name/opposite-coverage functions match their exact AST identities; deleting a nested record or swapping counters fails. A score exactly 30 fails and a true value just below 30 passes regardless of display rounding.</behavior>
  <action>Per D-03, D-04, D-06 and D-08, validate Fallow's exact version/schema and enumerate every production row from the complete all-tree report. Join each row to an exact original AST declaration/function anchor and validated Istanbul body, requiring one-to-one coverage of the production function population in both directions; names/proximity or a reported match percentage are insufficient. Require coverage_source istanbul and compare coverage_pct to the independently computed proportion of hit statements contained in that exact body, including nested-function statements. Only genuinely statement-empty bodies use the function-entry rule, proven by the independent statement inventory. Verify the consumer's CC-based formula using unrounded coverage and CRAP arithmetic; the consumer may round display scores, so boundary enforcement cannot compare rounded labels. Use an uncovered CC5 fixture yielding exactly 30, a genuinely subthreshold partial fixture and direct arithmetic boundary controls; run Fallow's own --max-crap 30 counterpart to establish >= behavior. Preserve separate all-tree cyclomatic/cognitive/unit-size enforcement and report production risk denominator separately from tests/scripts. Establish the installed Fallow report's coordinate convention with Unicode/surrogate fixtures. If its anchor unit differs from Istanbul UTF-16 columns, convert anchor units only through exact original-source byte/UTF-16 boundary tables before joining; never shift an Istanbul span or use proximity. A consumer result still must agree with the exact source body's independently computed coverage.</action>
  <fails_when>Fails if nearest-function fallback supplies a neighbor's coverage, equal percentages hide wrong identity, a missing statement invokes binary fallback, or display rounding decides the threshold.</fails_when>
  <verify><automated>node --test tests/scripts/check-coverage-risk.test.ts</automated></verify>
  <done>Exact production joins and unrounded threshold behavior are independently proven against the installed consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Exercise the shipping metric gate against misleading consumer inputs</name>
  <files>scripts/check-coverage-risk.negative.mjs, tests/scripts/check-coverage-risk.negative.test.ts, scripts/check-coverage-risk.mjs, package.json</files>
  <behavior>Missing/stale artifacts, estimated provenance, duplicate/missing production rows and wrong function joins fail the actual risk wrapper; the correct current counterpart passes.</behavior>
  <action>Per D-08 and D-09, add coverage:risk:negative with executable controls against the actual wrapper and policy file. Cover missing accepted report, altered source/tool/schema, malformed map despite Fallow clamping tolerance, a deleted nested function, repeated-name counter swap, estimated coverage_source, omitted/duplicate production row, all-tree denominator confusion, a production violation alongside unrelated test rows, and the exact threshold offender/benign pair. Verify always-pass/wrong-error/launch-failure harness mutants. Assert that a test/script-only estimated row does not demand invented production coverage while every production row remains mandatory; the separate existing whole-tree health gate still rejects its ordinary complexity violation. No --min-score 0, broad ignore, threshold change or filtered fixture config may supply the final acceptance result.</action>
  <fails_when>Fails if an estimated/missing production row passes, nonproduction rows weaken production completeness, existing complexity findings are ignored, or a bypass configuration produces the control verdict.</fails_when>
  <verify><automated>node --test tests/scripts/check-coverage-risk.negative.test.ts && node scripts/check-coverage-risk.negative.mjs</automated></verify>
  <done>The shipping wrapper and negative harness reject misleading input without narrowing whole-tree quality policy.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion evidence | Coverage evidence |
| --- | --- | --- |
| 07-06-T1 | With the shipped policy 30, an uncovered CC6 function scores 42 and fails; the same fully exercised function scores 6 and passes with exact measured provenance. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-06-T2 | Same-line/repeated-name/opposite-coverage functions match their exact AST identities; deleting a nested record or swapping counters fails. A score exactly 30 fails and a true value just below 30 passes regardless of display rounding. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-06-T3 | Missing/stale artifacts, estimated provenance, duplicate/missing production rows and wrong function joins fail the actual risk wrapper; the correct current counterpart passes. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-06-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-06-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-06-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-06-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-06-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. Fixture acceptance is not a certified production baseline: the complete current measurement belongs to 07-07 after Phase 6 stability. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- Every production health row has an exact validated source-function counterpart and measured statement coverage.
- CRAP 30 rejects scores meeting or exceeding 30 while all existing whole-tree complexity gates remain intact.
- Missing/estimated/approximately matched production coverage cannot pass the shipping wrapper.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-06-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
