---
phase: 07-reliable-coverage-metrics
plan: "05"
type: execute
wave: 4
depends_on: ["07-03","07-04"]
files_modified:
  - scripts/coverage-unit.mjs
  - scripts/coverage-capture.manifest.mjs
  - scripts/coverage-producer.mjs
  - tests/scripts/coverage-unit.test.ts
  - package.json
  - scripts/coverage-correspondence.mjs
  - tests/scripts/coverage-producer.test.ts
  - scripts/coverage-unit.negative.mjs
  - tests/scripts/coverage-unit.negative.test.ts
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every production source is represented, including unloaded runtime files and proven type-only modules."
    - "Worker merging preserves exact map identities and measured partial/zero coverage."
    - "Only complete accepted same-run artifacts can reach the metric consumer."
  artifacts:
    - path: scripts/coverage-unit.mjs
      provides: "Complete same-run conversion and atomic accepted artifacts behavior and acceptance evidence"
    - path: scripts/coverage-capture.manifest.mjs
      provides: "Complete same-run conversion and atomic accepted artifacts behavior and acceptance evidence"
    - path: scripts/coverage-producer.mjs
      provides: "Complete same-run conversion and atomic accepted artifacts behavior and acceptance evidence"
  key_links:
    - from: scripts/coverage-unit.mjs
      to: tests/scripts/coverage-unit.test.ts
      via: Independently authored controls exercise the real capture/converter/validator/consumer operation.
---



<objective>
Complete same-run conversion and atomic accepted artifacts.
Purpose: Every production source is represented, including unloaded runtime files and proven type-only modules.
Output: scripts/coverage-unit.mjs, scripts/coverage-capture.manifest.mjs, scripts/coverage-producer.mjs, tests/scripts/coverage-unit.test.ts, package.json, scripts/coverage-correspondence.mjs, tests/scripts/coverage-producer.test.ts, scripts/coverage-unit.negative.mjs, tests/scripts/coverage-unit.negative.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/07-reliable-coverage-metrics/07-03-SUMMARY.md
@.planning/phases/07-reliable-coverage-metrics/07-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Publish one captured run through conversion, validation and readback</name>
  <files>scripts/coverage-unit.mjs, scripts/coverage-capture.manifest.mjs, scripts/coverage-producer.mjs, tests/scripts/coverage-unit.test.ts, package.json</files>
  <behavior>A real fixture unit run yields native LCOV and validated Istanbul under one immutable accepted manifest; deleting/changing either artifact makes consumption fail.</behavior>
  <action>Per D-01 through D-04 and D-10, implement coverage:unit:verified orchestration over 07-01 capture, qualified producer, exact mapping and 07-04 validators. Publish coverage/unit.lcov unchanged for Sonar, coverage/unit.istanbul.json and coverage/unit.manifest.json only after source/test/tool sets, raw inputs, producer receipt and all schema/correspondence checks agree. Keep a unique immutable run bundle and publish its manifest last; consumers recheck the public artifacts against it and invalidate old success on any failed replacement. Bind exact tool versions/patch hash, executed command/selection, raw/source inventories, native LCOV and Istanbul hashes plus validation schema. A map produced after the test source changed must fail rather than relocate. Do not run the unit suite twice to obtain two formats. Define coverage:validate without positional arguments to validate the current published manifest/map and require its captured/converted identity; fixture tests supply an explicit contained --root.</action>
  <fails_when>Fails if native and Istanbul reports have different run provenance, conversion success bypasses strict validation, or publication leaves a partially accepted bundle.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-unit.test.ts</automated></verify>
  <done>The tracer publishes one complete accepted run and rejects stale or swapped artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Merge workers and account for every production source</name>
  <files>scripts/coverage-unit.mjs, scripts/coverage-producer.mjs, scripts/coverage-correspondence.mjs, tests/scripts/coverage-unit.test.ts, tests/scripts/coverage-producer.test.ts</files>
  <behavior>Split/reordered/overlapping worker captures produce stable exact identities and faithful counters; never-loaded runtime files have explicit zero coverage and type-only files have source-proven empty maps.</behavior>
  <action>Per D-04, D-07 and D-08, enumerate the complete production source set independently from loaded raw reports. For every source classify executable versus genuinely type-only from recorded executed AST; unloaded executable files receive the producer's complete zero-execution model, while type-only empty maps require AST proof. No extension file may vanish from the denominator. Convert each worker using a fresh AST and stable source identities, reject mismatched maps, merge with the reviewed Istanbul merger, and revalidate full correspondence after merge. Define overlapping raw capture semantics by process/isolate/run identity so duplicate snapshots cannot be summed as independent executions. Exercise reversed order, disjoint workers, duplicated raw record, imported-but-uncalled functions, unimported executable module and type-only module. Keep native and AST denominators separately labeled and preserve every genuine AST deficit.</action>
  <fails_when>Fails if unloaded runtime code becomes empty/type-only, duplicate snapshots inflate hits, fresh ASTs are not used, or native/AST denominator equality is imposed.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-unit.test.ts tests/scripts/coverage-producer.test.ts</automated></verify>
  <done>All production modules and exact function/statement identities are accounted for with deterministic faithful merged coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Reject stale and substituted artifacts through executable controls</name>
  <files>scripts/coverage-unit.negative.mjs, tests/scripts/coverage-unit.negative.test.ts, scripts/coverage-unit.mjs, package.json</files>
  <behavior>Missing/raw/native/Istanbul/manifest components, changed inputs/tooling, failed tests and foreign-run substitution make the actual pipeline fail with specific diagnostics; benign restoration passes.</behavior>
  <action>Per D-02 and D-09, add coverage:unit:negative and a negative runner that executes the shipping capture/conversion/validation path on tiny fixtures. Mutate one property at a time: source add/remove/change, test/support/config/lock/tool change, missing/empty raw/native/Istanbul/manifest, worker interruption, malformed coordinates, missing nested function/statement, changed artifact hash, same-name report from another run, integration/e2e substitution and inherited coverage environment override. Require the precise expected failure and clean counterpart; an always-pass executable, wrong diagnostic, malformed JSON, launch failure or signal must make the runner itself fail. Guarantee contained temporary cleanup and preserve failed evidence without a success pointer. This is artifact validation, not a special permissive fixture configuration.</action>
  <fails_when>Fails if arbitrary nonzero status satisfies the offender, a benign case is missing, or any inherited path/env override selects an unvalidated old report.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-unit.negative.test.ts && node scripts/coverage-unit.negative.mjs</automated></verify>
  <done>The actual artifact pipeline and its negative harness discriminate all freshness/completeness failures.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion evidence | Coverage evidence |
| --- | --- | --- |
| 07-05-T1 | A real fixture unit run yields native LCOV and validated Istanbul under one immutable accepted manifest; deleting/changing either artifact makes consumption fail. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-05-T2 | Split/reordered/overlapping worker captures produce stable exact identities and faithful counters; never-loaded runtime files have explicit zero coverage and type-only files have source-proven empty maps. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-05-T3 | Missing/raw/native/Istanbul/manifest components, changed inputs/tooling, failed tests and foreign-run substitution make the actual pipeline fail with specific diagnostics; benign restoration passes. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-05-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-05-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-05-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-05-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-05-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. Fixture acceptance is not a certified production baseline: the complete current measurement belongs to 07-07 after Phase 6 stability. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- Every production source is represented, including unloaded runtime files and proven type-only modules.
- Worker merging preserves exact map identities and measured partial/zero coverage.
- Only complete accepted same-run artifacts can reach the metric consumer.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-05-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
