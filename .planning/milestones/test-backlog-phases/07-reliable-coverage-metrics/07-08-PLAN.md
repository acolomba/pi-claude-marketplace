---
phase: 07-reliable-coverage-metrics
plan: "08"
type: execute
wave: 7
depends_on: ["07-07"]
files_modified:
  - package.json
  - .pre-commit-config.yaml
  - scripts/coverage-unit.mjs
  - tests/architecture/coverage-metrics-pipeline.test.ts
  - .github/workflows/ci.yml
  - .github/workflows/lint.yml
  - .github/workflows/sonarcloud.yml
  - sonar-project.properties
  - docs/coverage-metrics.md
  - .planning/phases/07-reliable-coverage-metrics/07-MEASUREMENT.md
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Normal checks produce or validate one current unit run before consuming its risk metrics."
    - "Local pre-commit, CI and Sonar preserve their scopes and use content-validated artifacts."
    - "All existing complexity, duplication, native aggregate and direct-pair gates remain unchanged in strength."
  artifacts:
    - path: scripts/coverage-unit.mjs
      provides: "Activate the ordered local and CI coverage-risk pipeline behavior and acceptance evidence"
  key_links:
    - from: package.json
      to: tests/architecture/coverage-metrics-pipeline.test.ts
      via: Independently authored controls exercise the real capture/converter/validator/consumer operation.
---



<objective>
Activate the ordered local and CI coverage-risk pipeline.
Purpose: Normal checks produce or validate one current unit run before consuming its risk metrics.
Output: package.json, .pre-commit-config.yaml, scripts/coverage-unit.mjs, tests/architecture/coverage-metrics-pipeline.test.ts, .github/workflows/ci.yml, .github/workflows/lint.yml, .github/workflows/sonarcloud.yml, sonar-project.properties, docs/coverage-metrics.md, .planning/phases/07-reliable-coverage-metrics/07-MEASUREMENT.md.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/07-reliable-coverage-metrics/07-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Integrate one authoritative unit capture into the local check order</name>
  <files>package.json, .pre-commit-config.yaml, scripts/coverage-unit.mjs, tests/architecture/coverage-metrics-pipeline.test.ts</files>
  <behavior>Normal npm check invokes capture, conversion, strict validation and production risk in order without a second unit execution; a missing/stale report cannot make a direct metric consumer succeed.</behavior>
  <action>Per D-01, D-06, D-08 and D-10, assert 07-07's complete current acceptance record before activation. Make test:coverage:unit use the accepted same-run pipeline while preserving native coverage/unit.lcov, native coverage flags and the exact unit selection; keep npm test available for ordinary focused/full testing. Reorder npm check so its existing unit execution is fulfilled once by the verified capture before coverage:risk consumption; preserve typecheck/lint/format, all member/export gates, existing whole-tree health/dupes and integration checks. Keep the direct-pair commands/pin untouched. For pre-commit ensure relevant changes (production/tests/support resources, tools/patches/config/package/lock/workflows) trigger the same quality path with whole-project analysis; the producer may reuse a content-validated same-source successful bundle, while a direct consumer always fails missing/stale input. A needed fresh capture is an explicit orchestration step, never a consumer fallback. Test exact command ordering, one unit child launch, invalidation after source/config/tool changes and no read of old inherited coverage paths. Make ordinary npm test and captured execution consume one authoritative unit selection, for example through coverage-capture.mjs --plain versus capture mode, forwarding supported Node test arguments explicitly. Preserve selected tests and outcomes; avoid duplicated glob definitions that can drift.</action>
  <fails_when>Fails if risk executes before validation, npm check runs the unit suite twice, a stale artifact is silently consumed/regenerated by the consumer, or a prior quality gate disappears.</fails_when>
  <verify><automated>node --test tests/architecture/coverage-metrics-pipeline.test.ts && npm run coverage:unit:negative && npm run coverage:risk:negative</automated></verify>
  <done>The actual local check path activates policy30 after one accepted unit run and retains all existing gates.</done>
</task>

<task type="auto">
  <name>Use the same tested pipeline in CI and preserve Sonar's native input</name>
  <files>.github/workflows/ci.yml, .github/workflows/lint.yml, .github/workflows/sonarcloud.yml, tests/architecture/coverage-metrics-pipeline.test.ts, sonar-project.properties</files>
  <behavior>CI/pre-commit jobs prepare the correct tested tooling and run the same ordered pipeline; Sonar continues reading only coverage/unit.lcov.</behavior>
  <action>Per D-01, D-08 and D-10, inspect the current workflows after earlier phases and wire the same captured unit/validation/risk commands into their actual execution paths. Keep the existing Node24 CI tooling family and prove its concrete version through the same conformance corpus before accepting artifacts; local Node26.8.2 evidence does not certify Node24 automatically. Artifacts bind exact runtime versions and cannot be reused across them. Retain application engines, branch-scoped/whole-tree direct coverage jobs, integration/E2E separation, existing whole-tree Fallow audit behavior and pinned workflow action identities. The ordinary npm ci --ignore-scripts must install the already-corrected producer artifact without implicit patch hooks. Preserve sonar.javascript.lcov.reportPaths=coverage/unit.lcov and avoid feeding integration/E2E/Istanbul reports into that setting. Ensure each isolated job captures its own current run or uses a verified same-content artifact with exact identity; never assume files exist because another job ran tests. Add structural/executable workflow controls for missing capture, wrong ordering and altered Sonar input.</action>
  <fails_when>Fails if CI uses unqualified producer/runtime bytes, a job consumes an unverified other-job report, Sonar mixes measurements, or scripts-disabled installation cannot reproduce the selected producer.</fails_when>
  <verify><automated>node --test tests/architecture/coverage-metrics-pipeline.test.ts && npm run lint:workflows && npm run lint:workflows:negative</automated></verify>
  <done>Actual CI/local paths run the same accepted process, and Sonar remains native unit-only.</done>
</task>

<task type="auto">
  <name>Verify final gates and publish the supported measurement contract</name>
  <files>docs/coverage-metrics.md, .planning/phases/07-reliable-coverage-metrics/07-MEASUREMENT.md</files>
  <behavior>The final stable integrated tree passes normal checks, producer/artifact/risk controls and unchanged direct pins; native production coverage is exactly 100 percent.</behavior>
  <action>Per D-01 through D-10, run npm run check on one stable final input snapshot and reuse its validated same-run LCOV/Istanbul/manifest for the final audit; run unchanged direct-pair verification separately. Confirm native production line/function/branch hit/found totals are exactly equal, not rounded display100 or integration/E2E substitutes. Confirm all production AST/function/statement and Fallow relationships independently, no coordinate clamping, no unexplained absence and actual >=30 policy behavior. Record exact source/tool/producer hashes, separate denominators, real measured distribution, controls, runtime versions and known V8 limits in 07-MEASUREMENT.md and the phase summary. Document commands, freshness failures, explicit producer update/rebuild procedure, license/provenance, and the distinction between native100 and faithful AST deficits. Run final pre-commit checks when executing the phase. Do not repeat the full unit run after documentation-only edits unless those exact documents are tested inputs.</action>
  <fails_when>Fails if evidence predates tested-input changes, a denominator is mislabeled, direct pins or complexity limits move, or unsupported conversion is represented as certified.</fails_when>
  <verify><automated>npm run check && npm run test:coverage:direct:all</automated></verify>
  <done>All final evidence is current and preserves native100, direct pins, assertion strength and whole-tree complexity/duplication policy.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion evidence | Coverage evidence |
| --- | --- | --- |
| 07-08-T1 | Normal npm check invokes capture, conversion, strict validation and production risk in order without a second unit execution; a missing/stale report cannot make a direct metric consumer succeed. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-08-T2 | CI/pre-commit jobs prepare the correct tested tooling and run the same ordered pipeline; Sonar continues reading only coverage/unit.lcov. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-08-T3 | The final stable integrated tree passes normal checks, producer/artifact/risk controls and unchanged direct pins; native production coverage is exactly 100 percent. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-08-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-08-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-08-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-08-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-08-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. The final acceptance record must cover the stable current whole production population, exact correspondence, faithful AST differences and the actual policy30 verdict. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- Normal checks produce or validate one current unit run before consuming its risk metrics.
- Local pre-commit, CI and Sonar preserve their scopes and use content-validated artifacts.
- All existing complexity, duplication, native aggregate and direct-pair gates remain unchanged in strength.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-08-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
