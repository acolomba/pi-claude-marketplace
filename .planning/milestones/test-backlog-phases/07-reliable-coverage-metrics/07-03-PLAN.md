---
phase: 07-reliable-coverage-metrics
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - scripts/coverage-source-map.mjs
  - scripts/coverage-producer.mjs
  - tests/scripts/coverage-source-map.test.ts
autonomous: true
requirements: [METRIC-01, METRIC-02]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every concrete location uses exact UTF-16 source coordinates including line endpoints."
    - "Repeated methods are restored only by original AST declaration identity."
    - "Invalid transformations or positions are rejected rather than repaired."
  artifacts:
    - path: scripts/coverage-source-map.mjs
      provides: "Exact executed-source mapping and AST-proven identities behavior and acceptance evidence"
    - path: scripts/coverage-producer.mjs
      provides: "Exact executed-source mapping and AST-proven identities behavior and acceptance evidence"
  key_links:
    - from: scripts/coverage-source-map.mjs
      to: tests/scripts/coverage-source-map.test.ts
      via: Independently authored controls exercise the real capture/converter/validator/consumer operation.
---



<objective>
Exact executed-source mapping and AST-proven identities.
Purpose: Every concrete location uses exact UTF-16 source coordinates including line endpoints.
Output: scripts/coverage-source-map.mjs, scripts/coverage-producer.mjs, tests/scripts/coverage-source-map.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reliable-coverage-metrics/07-CONTEXT.md
@.planning/phases/07-reliable-coverage-metrics/07-RESEARCH.md
@.planning/phases/07-reliable-coverage-metrics/07-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/07-reliable-coverage-metrics/07-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Convert one captured Unicode same-line function pair exactly</name>
  <files>scripts/coverage-source-map.mjs, scripts/coverage-producer.mjs, tests/scripts/coverage-source-map.test.ts</files>
  <behavior>Opposite-coverage functions on the same line after surrogate-pair text retain different exact columns, bodies and hit counts, including an end-of-line endpoint.</behavior>
  <action>Per D-03 and D-04, build an explicit identity source map from the immutable original source and exact recorded executed JavaScript, using the official codec. Require equal UTF-16 length, every line boundary and supported position-preserving Node stripping semantics; a source transform that violates this relationship is a refusal. Emit a segment for every UTF-16 column including the line-length endpoint. Parse a fresh executed-code AST for every worker conversion because the producer mutates nodes/visited state. Wire this into the producer adapter and trace one actual raw fixture to finite exact Istanbul locations, preserving zero and partial execution. Never clamp, shift, replace Infinity/null, or infer function spans from LCOV.</action>
  <fails_when>Fails if same-line functions collapse, surrogate pairs are counted as code points/bytes, endpoints become Infinity/null, or a transform is silently normalized.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-source-map.test.ts tests/scripts/coverage-producer.test.ts</automated></verify>
  <done>The real captured pair converts with exact finite source/body spans and independently expected coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Restore original names through exact source declarations and reject drift</name>
  <files>scripts/coverage-source-map.mjs, scripts/coverage-producer.mjs, tests/scripts/coverage-source-map.test.ts</files>
  <behavior>Repeated constructors/object methods keep separate identities and counters; a genuine source name ending _2 remains unchanged. CRLF/blank lines and transformed-source mismatch are discriminating controls.</behavior>
  <action>Per D-04 and D-09, resolve each producer-generated function name to the original source AST using exact declaration identifier and body spans. Only then restore the original identifier name; never strip suffixes heuristically or match nearby lines. Preserve anonymous functions as exact distinct locations without inventing identifiers. Add repeated same-spelling methods on one line with opposite coverage, getters/setters, constructors, nested scopes, named functions with numeric suffixes, one-line expression bodies, CRLF and blank-line controls. Validate position ranges in memory before serialization and again on JSON readback; no invalid sentinel may become JSON null unnoticed. Compare the run's source/tool identities before each mapping operation and reject obsolete captures.</action>
  <fails_when>Fails if a real numeric suffix is removed, nearest-name matching chooses another method, or stale captures/invalid positions are accepted.</fails_when>
  <verify><automated>node --test tests/scripts/coverage-source-map.test.ts</automated></verify>
  <done>Every restored name is source-proven and every mapping survives strict coordinate/readback checks.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task record its independent expected result, concrete weakening counterexample and source-coverage impact; afterward record exact commands/results in this plan's SUMMARY. Do not build expected syntax lists or coverage percentages from the producer output under test. New tooling behavior needs meaningful partial/zero/error controls; no task may change production exclusions, native aggregate thresholds, direct pins or existing assertion contracts. Reuse one stable integration snapshot for aggregate measurements instead of running the whole suite after each small change.

| Task | Independent assertion evidence | Coverage evidence |
| --- | --- | --- |
| 07-03-T1 | Opposite-coverage functions on the same line after surrogate-pair text retain different exact columns, bodies and hit counts, including an end-of-line endpoint. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
| 07-03-T2 | Repeated constructors/object methods keep separate identities and counters; a genuine source name ending _2 remains unchanged. CRLF/blank lines and transformed-source mismatch are discriminating controls. | Record focused positive/negative/partial/zero/error paths, preserved assertions and unchanged production/native/direct impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Source/test/tool inputs to capture | Actual executed bytes must match the immutable run identity. |
| Raw/converted artifacts to metric consumer | Coordinates, syntax identities, counters and freshness must be independently validated. |
| Dependency artifact to execution | Only the reviewed exact producer and tested tooling may supply conversion behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-03-01 | Tampering | Run/coverage identity | high | mitigate | Content hashes, complete sets, exact worker/source records and atomic accepted-manifest publication reject stale/substituted results. |
| T-07-03-02 | Tampering | Producer omissions and consumer fallback | high | mitigate | Exact independent AST function/statement correspondence, source-proven names and opposite-coverage/deleted-record controls before any metric verdict. |
| T-07-03-03 | Information disclosure | Artifact paths and cleanup | medium | mitigate | Canonical contained paths, explicit destinations and cleanup limited to owned temporary runs. |
| T-07-03-04 | Repudiation | Child process outcomes | medium | mitigate | Distinguish launch/signal/setup failures from exact expected finding statuses; always-pass and wrong-diagnostic harness controls. |
| T-07-03-SC | Tampering | npm development tooling | high | mitigate | Use the resolved 07-RESEARCH provenance audit, exact integrity/version/license, scripts-disabled installation and reproducible licensed patch artifact; ordinary checks never mutate installed dependencies. |

ASVS scope is input consistency and local file handling; no new network service or authentication mechanism is introduced.
</threat_model>

<verification>
Run each task's automated checks and the existing typecheck/lint/format gates for the completed wave. Preserve native aggregate production unit line/function/branch coverage at exactly 100 percent and unchanged direct-pair floors on stable integration snapshots. Fixture acceptance is not a certified production baseline: the complete current measurement belongs to 07-07 after Phase 6 stability. Never accept clamped coordinates, omitted functions/statements, stale reports or approximate Fallow matching as evidence.
</verification>

<success_criteria>
- Every concrete location uses exact UTF-16 source coordinates including line endpoints.
- Repeated methods are restored only by original AST declaration identity.
- Invalid transformations or positions are rejected rather than repaired.
- Every task's fails_when is tested and its assertion/coverage ledger completed.
- No native/direct coverage, assertion, whole-tree complexity or duplication policy is weakened.
</success_criteria>

<output>
Create .planning/phases/07-reliable-coverage-metrics/07-03-SUMMARY.md with exact commands/results, independent acceptance evidence, tool/source identities, known limits and any necessary bounded repair-plan IDs. Do not claim producer or production certification before the corresponding acceptance task passes.
</output>
