---
phase: 08-final-verification-and-reconciliation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md
autonomous: true
requirements: [FINAL-01]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "FINAL-01: every one of the sixteen `npm run check` members ran as its own process at the phase HEAD with its exit captured by `rc=$?` on the command itself (never through a pipe), and 08-MEASUREMENT.md section 2 holds one row per member with exit 0, its seconds, and its quoted count line."
    - "FINAL-01: 08-MEASUREMENT.md section 3 records `npm run test:e2e`, `npm run test:coverage:direct:all` and `pre-commit run --all-files` with their exits; the TruffleHog worktree red is classified environment in section 6 with the verbatim error and the `SKIP=trufflehog` exit-0 control; a blocked e2e launch, if it happens, is quoted verbatim as an environment debt and never counted as a pass or a skip."
    - "FINAL-01: the aggregate unit claim in section 4 comes from the run's own validated bundle (`npm run coverage:validate` exit 0) recounted independently from `coverage/unit.lcov`: records 230, LF == LH, FNF == FNH, BRF == BRH, zero `DA:...,0` / `BRDA:...,0` / `FNDA:0,...` entries; the native and syntax denominators are reported in separate rows and never equated."
    - "FINAL-01 adjacency: each fresh denominator is compared with the Phase 7 certified value (native 63825/63825, 1890/1890, 9234/9234; syntax 1865/1865, 10392/10393, 6633/6653) and the comparison verdict is written down; any inequality is recorded in section 6 as a finding with its cause, never replaced by the Phase 7 value."
    - "FINAL-01 empty input: a member whose log ends without its `cmd='npm run <member>' exit=<n> seconds=<s>` summary line is re-run, never recorded; the LCOV recount requires records == 230 and LF > 0, so an empty or truncated `coverage/unit.lcov` cannot satisfy LF == LH."
    - "FINAL-01 ordering: the sixteen chain rows are numbered 1 to 16 in `package.json` `check` order, `npm run typecheck` first and `npm run lint:type-members:negative` last."
    - "FINAL-01: section 7 states that nothing was loosened and proves it with `git diff --quiet 0844c2a7 HEAD -- extensions tests scripts package.json package-lock.json .fallowrc.json eslint.config.js sonar-project.properties .pre-commit-config.yaml` exiting 0 (the last code commit and every gate configuration are byte-identical at the measured HEAD)."
  prohibitions:
    - statement: "No threshold, exclusion, suppression, census pin, direct-coverage pin, `fallow-ignore` marker, exception entry or pre-commit hook configuration may be changed to turn a gate green; a red is classified (regression or environment) and recorded, and a regression is fixed through reachable public behavior only."
      status: resolved
      verification: judgment
    - statement: "No figure in 08-MEASUREMENT.md may be copied from 08-RESEARCH.md or 07-MEASUREMENT.md as if it were this phase's run; every count, exit, second, runId and digest comes from the logs and artifacts of the runs this plan makes (the research runId `20260918T225947291Z-f055e502` and manifest digest `39a577cc3a087653` are the values the fresh run must differ from)."
      status: resolved
      verification: judgment
    - statement: "A launch error, a killed process or a log without its summary line must never be recorded as a passing or a skipped gate."
      status: resolved
      verification: judgment
    - statement: "The four uncommitted local edits (`.claude/settings.json`, `.codex/config.toml`, `.planning/state.json`, `.mcp.json`) must never be staged, stashed or reverted; `git add` takes explicit paths only."
      status: resolved
      verification: judgment
    - statement: "No commit may use `--no-verify`, `--amend`, `gsd-tools commit`, or a rebase; every commit is preceded by `pre-commit run --files <changed files>` exiting 0 and made with the `SKIP=trufflehog` prefix this worktree requires."
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md"
      provides: "The phase-8 measurement record: preconditions, the sixteen chain rows, the three additional gates, the aggregate unit recount, deficits and CRAP distribution, red classification, and the nothing-loosened statement"
      contains: "## 7. What did not change"
  key_links:
    - from: ".planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md"
      to: "package.json"
      via: "section 2 lists the sixteen `check` members by their exact script names in chain order"
      pattern: "npm run lint:type-members:negative"
    - from: ".planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md"
      to: "coverage/unit.manifest.json"
      via: "section 4 quotes the fresh runId, acceptedAt, `acceptance.population`, `acceptance.denominators` and the sha256 first-16 digests of the four public artifacts"
      pattern: "acceptance.denominators"
---

<objective>
Run every required gate fresh at the phase HEAD, each as its own process with its exit captured directly, and record the result in `08-MEASUREMENT.md` in the shape of `07-MEASUREMENT.md`: the sixteen `check` members, the three additional gates, the independently recounted aggregate unit coverage, the deficit list and CRAP distribution, the classification of every red, and the explicit statement that nothing was loosened.

Purpose: FINAL-01 requires the 100% aggregate unit production coverage, assertion strength, direct-pair requirements and every quality check to be proven at the final tree, not inherited from Phase 7. The Phase 7 figures are the expected values; a mismatch is a finding to investigate, never a number to overwrite.

Output: `.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md` sections 1 through 7 (section 8, the item reconciliation, is written by plan 08-02).
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-final-verification-and-reconciliation/08-CONTEXT.md
@.planning/phases/08-final-verification-and-reconciliation/08-RESEARCH.md
@.planning/phases/08-final-verification-and-reconciliation/08-PATTERNS.md
@.planning/phases/08-final-verification-and-reconciliation/08-VALIDATION.md
@.planning/phases/07-reliable-coverage-metrics/07-MEASUREMENT.md
@.agents/skills/simple-english/SKILL.md
@.agents/skills/humanizer/SKILL.md
</context>

<execution_rules>
- This phase changes no source, test, script or configuration file. The only file this plan writes is `08-MEASUREMENT.md`. If a gate is red for a reason that is not environment, stop and report it; do not edit anything outside `.planning/` in this plan.
- Run from `/home/acolomba/src/pi-claude-marketplace-test-backlog`, the milestone worktree that holds `node_modules/`, `coverage/` and the vendored producer. A nested per-plan worktree has none of these and cannot run the gates.
- The Bash tool ceiling is 10 minutes and `npm run check` takes about 22 minutes. Never run `npm run check` as one command. Run each member as its own `npm run <member>` process through a sequential runner script launched detached (`setsid nohup bash <runner> <members...> > <runner.out> 2>&1 &`), and wait by polling its summary file with a background `until grep -q ...; do sleep 30; done` loop (the harness Monitor pattern), never a foreground sleep. The three additional gates that exceed or approach the ceiling (`npm run test:coverage:direct:all` about 509 s, `pre-commit run --all-files` about 838 s) are launched the same way.
- Exit capture is `rc=$?` on the command itself, redirecting output to a log file. Never `cmd | tee` and never `cmd | tail`: the pipe's exit is the last command's.
- Logs live under the executor's scratchpad directory (the `Scratchpad directory` named in the environment), subdirectory `gates/`, referred to below as `$OUT`. They are evidence to quote into the measurement document, never a file to commit. `coverage/` is gitignored for the same reason.
- Never run two coverage-producing commands at once, and read `coverage/unit.manifest.json` only after `npm run test:coverage:unit` has exited: the public artifacts are removed at capture start and rewritten at acceptance.
- Commit discipline (CLAUDE.md, worktree): `pre-commit run --files <paths>; echo "exit=$?"` must print `exit=0` before `git commit`; stage explicit paths only; commit with `SKIP=trufflehog git commit -F <message file>`; never `--no-verify`, never `--amend`, never a rebase, never `gsd-tools commit`. After every commit `git status --short` must show exactly ` M .claude/settings.json`, ` M .codex/config.toml`, ` M .planning/state.json` and `?? .mcp.json` and nothing else. Commit titles follow Conventional Commits with no phase or milestone mention, for example `docs: measure every quality gate at the final tree`.
- Prose in the measurement document follows the `simple-english` (Plain mode) and `humanizer` skills: short sentences, exact numbers, no inflated claims. Planning documents may name phases; source comments are not touched.
</execution_rules>

<tasks>

<task type="tracer">
  <name>Task 1: One gate member end to end: `npm run typecheck` run, exit captured, row written into 08-MEASUREMENT.md</name>
  <precondition>The cwd is `/home/acolomba/src/pi-claude-marketplace-test-backlog` with `node_modules/` present (`npx fallow --version` prints `3.23.0`) and `grep -l '^status: passed' .planning/phases/0[1-7]-*/0[1-7]-VERIFICATION.md` lists exactly seven files.</precondition>
  <reversibility rating="reversible">Adds one planning document; `git revert` of the commit removes it with no other effect.</reversibility>
  <files>.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/07-reliable-coverage-metrics/07-MEASUREMENT.md (sections 1 and 9.2: the preconditions table and the command / exit / seconds / output row shape to imitate)
    - .planning/phases/08-final-verification-and-reconciliation/08-RESEARCH.md (sections "Live Gate Measurements", "Architecture Patterns" Pattern 1 and Pattern 2, "Common Pitfalls" 1 and 3, "Code Examples")
    - .planning/phases/08-final-verification-and-reconciliation/08-PATTERNS.md (the `08-MEASUREMENT.md` section: skeleton, opening paragraph, preconditions table, row pattern)
    - .planning/phases/08-final-verification-and-reconciliation/08-CONTEXT.md (Gate execution decisions)
    - .planning/phases/01-reliable-negative-controls/01-VERIFICATION.md, .planning/phases/02-sonar-rules-for-tests/02-VERIFICATION.md, .planning/phases/03-reachable-agent-collision-contract/03-VERIFICATION.md, .planning/phases/04-strict-command-arguments/04-VERIFICATION.md, .planning/phases/05-production-export-ownership/05-VERIFICATION.md, .planning/phases/06-unused-type-member-gate/06-VERIFICATION.md, .planning/phases/07-reliable-coverage-metrics/07-VERIFICATION.md (frontmatter only: `verified`, `status`, `score`)
    - package.json (the `check` script: the sixteen member names in order)
  </read_first>
  <action>
    Per the CONTEXT.md "Gate execution (FINAL-01)" decisions, prove the whole path once before the sweep: one `check` member run as its own process, its exit captured directly, its row written into the measurement document.

    1. Create the runner `$OUT/run-gates.sh` (in the scratchpad, never committed). It takes npm script names as arguments and, for each name `s` in order: records a start time, runs `npm run "$s" > "$OUT/${s//:/_}.log" 2>&1`, captures `rc=$?` on that command, and appends one line `cmd='npm run <s>' exit=<rc> seconds=<elapsed>` to `$OUT/SUMMARY.txt`. No pipe anywhere in the runner; the log is the command's stdout and stderr together. Record the runner's own start and end timestamps in `$OUT/runner.out` (ISO-8601 UTC) so section 1 can name the run window.

    2. Run the runner for member 1 only: `bash "$OUT/run-gates.sh" typecheck` in the foreground (about 37 s). Confirm `$OUT/SUMMARY.txt` holds exactly one line and that line reads `cmd='npm run typecheck' exit=0 seconds=<n>`. Confirm `$OUT/typecheck.log` ends with the `tsc --noEmit` invocation and no diagnostic. If the exit is not 0, stop: do not write the document; classify the failure per CONTEXT.md before anything else.

    3. Create `.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md` with the title `# Phase 8 -- Final gate measurement at the phase HEAD`, an opening paragraph in the shape of `07-MEASUREMENT.md` line 3 stating that every figure below was measured by the runs this document names and that no figure is copied from 08-RESEARCH.md or from Phase 7, then:

    Section `## 1. Preconditions` with a two-column table `| Precondition | Evidence |` holding, in this order: one row per prior phase (seven rows, `Phase N verification complete`) whose evidence quotes the frontmatter of that phase's VERIFICATION.md exactly as `\`0N-VERIFICATION.md\`: \`status: passed\`, \`score: <score>\`, verified <timestamp>` (the Phase 7 row adds "the report body counts 10/10; the frontmatter value is cited as canonical"); one row `Code tree identity` whose evidence names the last commit touching `extensions/` (`git log -1 --format='%h %ad' --date=iso -- extensions`, expected `31ed3c72`), the last commit touching `tests/` and `scripts/` (expected `0844c2a7`), and the output of `git diff --quiet 0844c2a7 HEAD -- extensions tests scripts package.json package-lock.json .fallowrc.json eslint.config.js sonar-project.properties .pre-commit-config.yaml; echo "exit=$?"` (expected `exit=0`), so a docs-only commit between the runs of this plan does not change what was measured; one row `Run identity` with the full HEAD sha of this run (`git rev-parse HEAD`), the branch, `git rev-list --left-right --count origin/main...HEAD`, the run window from `$OUT/runner.out`, and `node --version`, `npm --version`, `npx fallow --version`, `pre-commit --version` as printed; one row `Local edits present, never staged` listing the four `git status --short` lines that exist before the run.

    Section `## 2. The check chain member by member` with one sentence stating the runner shape (one process per member, `rc=$?` on the command, never a pipe, logs under the scratchpad and not committed), then the table `| # | Command | Exit | Seconds | Counts / output line |` with the header and row 1: `| 1 | \`npm run typecheck\` | 0 | <seconds from SUMMARY.txt> | \`tsc --noEmit\`, no diagnostics |`. Rows 2 to 16 are written by Task 2; do not write them from research.

    Keep the whole document under `simple-english` Plain mode. Then `pre-commit run --files .planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md; echo "exit=$?"` (expected `exit=0`; if a hook rewrote the file, re-run until it prints `exit=0` with no rewrite), stage that one path, and commit with `SKIP=trufflehog git commit -F <message file>`; confirm `git status --short` shows only the four local edits.
  </action>
  <verify>
    <automated>npm run typecheck > /dev/null 2>&1; echo "typecheck exit=$?"; grep -cE '^\| 1 \| `npm run typecheck` \| 0 \| [0-9]+ \|' .planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md</automated>
    <fails_when>output lacks the line `typecheck exit=0`, or the final line printed by grep is not `1` (the member-1 row with exit 0 is absent or duplicated)</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md` exists and `git ls-files` lists it after the commit.
    - `grep -c '^## 1\. Preconditions' 08-MEASUREMENT.md` prints `1` and `grep -c '^## 2\. The check chain member by member' 08-MEASUREMENT.md` prints `1`.
    - Section 1 holds seven rows matching `^\| Phase [1-7] verification complete \|` and each row contains the literal `status: passed`; the Phase 7 row contains `score: 8/8`.
    - Section 1 contains a row beginning `| Code tree identity |` whose evidence cell contains `31ed3c72`, `0844c2a7` and `exit=0`.
    - Section 1 contains a row beginning `| Run identity |` whose evidence cell contains a 40-hex sha, `v26.8.2`, `11.19.1` and `3.23.0` as printed by the tools in this run (a different printed version is recorded as printed, not corrected to these).
    - `grep -cE '^\| [0-9]+ \| `npm run ' 08-MEASUREMENT.md` prints `1` (only row 1 exists after this task).
    - `$OUT/SUMMARY.txt` holds exactly one line and it starts with `cmd='npm run typecheck' exit=0`.
    - `git status --short` after the commit prints exactly four lines: ` M .claude/settings.json`, ` M .codex/config.toml`, ` M .planning/state.json`, `?? .mcp.json`.
  </acceptance_criteria>
  <done>Member 1 ran as its own process with exit 0, the measurement document exists with sections 1 and 2 (row 1), and the commit leaves the four local edits untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run members 2 to 16 and the three additional gates, then write sections 2 and 3</name>
  <reversibility rating="reversible">Extends the planning document; `git revert` restores the Task 1 state.</reversibility>
  <files>.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md (the document as committed by Task 1)
    - .planning/phases/08-final-verification-and-reconciliation/08-RESEARCH.md (sections "Live Gate Measurements": the two tables, the red classification summary, the post-run readbacks; "Common Pitfalls" 1, 2, 3, 4, 6; "Code Examples": exit status without a pipe, pre-commit with before/after status)
    - .planning/phases/08-final-verification-and-reconciliation/08-CONTEXT.md (Gate execution: e2e network rule, failing-gate classification rule)
    - tests/e2e/_helpers.ts and tests/e2e/_pinned-sha.ts (what the e2e launch fetches, so a launch error can be recognized and quoted)
    - CLAUDE.md line 17 (the `SKIP=trufflehog` worktree rule)
  </read_first>
  <action>
    Per CONTEXT.md "Gate execution (FINAL-01)", run the remaining fifteen chain members and the three additional gates fresh, each as its own process, and record every exit.

    1. Launch the Task 1 runner detached for members 2 to 16 in `package.json` `check` order: `lint lint:workflows lint:workflows:negative fallow format:check test:corresponding test:corresponding:negative test:coverage:direct:negative test:coverage:unit coverage:unit:negative coverage:risk coverage:risk:negative test:integration lint:type-members lint:type-members:negative`. Use `setsid nohup bash "$OUT/run-gates.sh" <those fifteen names> > "$OUT/runner-2.out" 2>&1 &` and wait with a background polling loop until `grep -c "^cmd=" "$OUT/SUMMARY.txt"` prints `16`. Expected wall time about 21 minutes. If a log ends without its summary line (a killed run), find and kill the orphan `node` children by pid, delete that member's log, and re-run that member alone through the runner; never write a row for a member that has no summary line.

    2. Run `npm run test:e2e > "$OUT/test_e2e.log" 2>&1; echo "exit=$?"` in the foreground (about 10 s; before it, record `git ls-remote https://github.com/anthropics/claude-plugins-official HEAD > /dev/null 2>&1; echo "network exit=$?"` as the reachability control). If the exit is not 0 and the log shows a fetch or launch error, quote the error verbatim into section 6 as an environment debt; it is not a pass and not a skip.

    3. Launch `npm run test:coverage:direct:all > "$OUT/direct_all.log" 2>&1` detached with the same `cmd=... exit=... seconds=...` line appended to `$OUT/EXTRA-SUMMARY.txt` (about 509 s); poll until its line exists.

    4. Capture `git status --short > "$OUT/status-before.txt"`, then launch `pre-commit run --all-files > "$OUT/pre-commit.log" 2>&1` detached with its exit line appended to `$OUT/EXTRA-SUMMARY.txt` (about 838 s); poll until its line exists; then `git status --short > "$OUT/status-after.txt"` and `diff "$OUT/status-before.txt" "$OUT/status-after.txt"; echo "status diff exit=$?"` (expected `exit=0`: no file rewritten). Then run the control `SKIP=trufflehog pre-commit run trufflehog --all-files; echo "exit=$?"` (expected the hook line reads `Skipped` and `exit=0`). Count the hook verdicts in `$OUT/pre-commit.log` with `grep -cE '\.{3,}Passed$'`, `grep -cE '\.{3,}Skipped$'` and `grep -cE '\.{3,}Failed$'` and name every `Failed` hook.

    5. Post-run readbacks, exits captured directly: `npm run coverage:validate > "$OUT/validate.log" 2>&1; echo "exit=$?"` and `npm run coverage:risk -- --report coverage/unit.risk.json > "$OUT/risk-report.log" 2>&1; echo "exit=$?"` (both expected `exit=0`). Confirm with `node -e` over `coverage/unit.manifest.json` that `runId` and `acceptance.acceptedAt` are the ones the chain's `test:coverage:unit` log printed, so `pre-commit`'s `npm-coverage-unit` hook reused the bundle rather than replacing it.

    6. Write into `08-MEASUREMENT.md`: rows 2 to 16 of the section 2 table, each `| <n> | \`npm run <member>\` | <exit> | <seconds> | <quoted count line> |` using the exact figures from `$OUT/SUMMARY.txt` and the member's log (the lines to quote per member are the ones 08-RESEARCH.md's table quotes: the `tests N / suites / pass / fail` line and the `Coverage unit verified:` line for member 10, the `Coverage risk verified:` line for member 12, `Unused type member gate passed with N recorded exception(s).` for member 15, and so on; for member 5 quote fallow's four result lines and state that the health and dupes lines print a red glyph while exiting 0). Below the table add the chain total in seconds and minutes and the Phase 7 comparison (`1287 s`).

    Then `## 3. Additional gates`: the table `| Command | Exit | Seconds | Counts / output line | Classification |` with the rows for `npm run test:e2e` (quote the `tests 14 / pass 14` line and the network control exit), `npm run test:coverage:direct:all` (quote the `All-pair run complete: 239 pairs` line and the `2 pinned shortfall(s) matched scripts/test-coverage-direct.pin.json exactly` line with the two pinned files and their figures), and `pre-commit run --all-files` (exit as captured, the Passed / Skipped / Failed counts, the name of each Failed hook, the `status diff exit=0` verdict, and in the classification cell the word `environment` or `regression` for each red); then a paragraph `Post-run readbacks` quoting the `Coverage bundle verified:` line, the `Coverage risk verified:` line, and the `SKIP=trufflehog` control result.

    A red that is not the TruffleHog worktree failure is a finding: stop, classify it per CONTEXT.md (regression versus environment) and report it in the SUMMARY before any fix is attempted; this plan changes no code.

    Commit as in Task 1 (`pre-commit run --files` on the document, explicit path, `SKIP=trufflehog`).
  </action>
  <verify>
    <automated>D=.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md; echo "chain-rows=$(grep -cE '^\| ([1-9]|1[0-6]) \| `npm run [a-z:-]+` \| 0 \| [0-9]+ \|' "$D")"; echo "e2e-rows=$(grep -cE '^\| `npm run test:e2e` \| [0-9]+ \|' "$D")"; echo "direct-rows=$(grep -cE '^\| `npm run test:coverage:direct:all` \| 0 \| [0-9]+ \|' "$D")"; echo "precommit-rows=$(grep -cE '^\| `pre-commit run --all-files` \| [0-9]+ \|' "$D")"; npm run coverage:validate; echo "validate exit=$?"</automated>
    <fails_when>`chain-rows=` is not 16, `e2e-rows=` is not 1, `direct-rows=` is not 1, `precommit-rows=` is not 1, the output lacks a `Coverage bundle verified:` line, or `validate exit=` is not 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `$OUT/SUMMARY.txt` holds exactly sixteen `cmd='npm run ...' exit=` lines, in `check` order, and `grep -c "exit=0" $OUT/SUMMARY.txt` prints `16`; any other count means a red that section 6 must classify and the SUMMARY must report.
    - Section 2 of `08-MEASUREMENT.md` holds sixteen rows numbered 1 to 16 whose command cells read, in order: `npm run typecheck`, `npm run lint`, `npm run lint:workflows`, `npm run lint:workflows:negative`, `npm run fallow`, `npm run format:check`, `npm run test:corresponding`, `npm run test:corresponding:negative`, `npm run test:coverage:direct:negative`, `npm run test:coverage:unit`, `npm run coverage:unit:negative`, `npm run coverage:risk`, `npm run coverage:risk:negative`, `npm run test:integration`, `npm run lint:type-members`, `npm run lint:type-members:negative`.
    - Row 10's count cell contains `pass 6900` (or the count the run printed, with `fail 0`) and a `Coverage unit verified:` line naming a runId that is not `20260918T225947291Z-f055e502`; row 12's cell contains `Coverage risk verified:` and `policy < 30`; row 14's cell contains `pass 32`; row 15's cell contains `Unused type member gate passed with 5 recorded exception(s).`
    - Section 3 holds the `npm run test:e2e` row with either `pass 14` and exit 0, or a non-zero exit whose classification cell reads `environment` and whose verbatim launch error also appears in section 6.
    - Section 3's `npm run test:coverage:direct:all` row contains `239 pairs` and `2 pinned shortfall(s) matched scripts/test-coverage-direct.pin.json exactly`.
    - Section 3's `pre-commit run --all-files` row names every Failed hook; if TruffleHog failed, its cell contains `not a directory`, the classification cell reads `environment`, and the `Post-run readbacks` paragraph contains `Skipped` for the `SKIP=trufflehog` control with `exit=0`; the row also states `status diff exit=0` (no file rewritten).
    - The `Post-run readbacks` paragraph quotes a `Coverage bundle verified:` line and a `Coverage risk verified:` line whose runId equals the one in row 10.
    - `git status --short` after the commit prints exactly the four local-edit lines.
  </acceptance_criteria>
  <done>All sixteen chain members and the three additional gates ran fresh with exits captured directly, sections 2 and 3 record every command, exit, duration and count line, and each red carries a classification.</done>
</task>

<task type="auto">
  <name>Task 3: Recount the aggregate unit coverage independently and write sections 4 to 7</name>
  <reversibility rating="reversible">Extends the planning document; `git revert` restores the Task 2 state.</reversibility>
  <files>.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md (as committed by Task 2)
    - .planning/phases/07-reliable-coverage-metrics/07-MEASUREMENT.md (sections 4, 5, 6, 7 and 9.2 to 9.3: denominators table, independent-recount rows, the 21-deficit list, the CRAP histogram, the fingerprint table, the "nothing loosened" sentence at line 218)
    - .planning/phases/08-final-verification-and-reconciliation/08-RESEARCH.md (sections "Aggregate Unit Coverage", "Code Examples: Independent recount of the published bundle", "Security Domain", "Common Pitfalls" 7 and 8)
    - .planning/phases/08-final-verification-and-reconciliation/08-PATTERNS.md (denominators table, independent-recount row pattern, CRAP histogram, "nothing loosened" statement, classification-table pattern)
    - scripts/coverage-risk-policy.json (the threshold and Fallow version the policy pins)
    - scripts/test-coverage-direct.pin.json (the two pinned shortfalls)
  </read_first>
  <action>
    Per CONTEXT.md "Gate execution (FINAL-01)" (the 100% claim comes from the validated same-run aggregate capture, in all three dimensions, with the direct-pair pin and the integration/e2e reports reported separately), write the four remaining sections from the run's own artifacts.

    Section `## 4. Aggregate unit coverage`: a paragraph naming the published bundle (`runId`, started / completed / accepted UTC times, `runtime` object) read from `coverage/unit.manifest.json`; a table of sha256 first-16 digests for `coverage/unit.lcov`, `coverage/unit.istanbul.json`, `coverage/unit.manifest.json` and `coverage/unit.validation.json` (`sha256sum <file> | cut -c1-16`); the manifest's `acceptance.population` and `acceptance.denominators` objects quoted verbatim with the nine unloaded production paths listed; the denominators table `| Model | Files | Functions | Statements or lines | Branches |` with the native row (LCOV records) and the syntax row (Istanbul map) kept apart and never derived from each other; the independent recount rows in the `07-MEASUREMENT.md` section 5 shape, computed by the recount used in this task's verify command (records, LF, LH, FNF, FNH, BRF, BRH, zero-count entries) and by a map recount over `f`, `s`, `b` of `coverage/unit.istanbul.json`; and a `Comparison with Phase 7` table that lists each of the six denominators beside its Phase 7 certified value (native 63825/63825, 1890/1890, 9234/9234; syntax 1865/1865, 10392/10393, 6633/6653) with an `equal` or `differs` verdict per row. A `differs` row is a finding: record it in section 6 with the cause investigated (which files, which counters, whether any zero / nonzero flip occurred) and keep the fresh number.

    Section `## 5. Deficits and CRAP distribution`: the list of syntax counters at zero from the fresh map (expected the one statement `bridges/commands/discover.ts:289` and the twenty arms listed in 08-RESEARCH.md "Aggregate Unit Coverage"), stated as equal to `07-MEASUREMENT.md` section 6 or with the delta named; then the CRAP histogram computed from `coverage/unit.risk.json` (`| Score | Functions |` with rows `below 5`, `5 to 9.99`, `10 to 19.99`, `20 to 29.99`, `30 or more`), the maximum with its anchor, the partial function and its ratio, and `failures` empty, each compared with `07-MEASUREMENT.md` sections 7 and 9.3.

    Section `## 6. Red classification`: the table `| Command | Verbatim error | Classification | Cause | Debt or fix |` with one row per non-zero exit recorded anywhere in sections 2 and 3. Expected content: exactly one row, `pre-commit run --all-files`, the TruffleHog error quoted verbatim from `$OUT/pre-commit.log`, `environment`, the cause (this checkout is a git worktree: `.git` is a file containing `gitdir: ...`, and TruffleHog opens `.git/index` as if `.git` were a directory), and the debt statement (CLAUDE.md line 17 already prescribes `SKIP=trufflehog` inside worktrees; `detect-private-key` passed; CI's `lint.yml` runs the same hook on a normal checkout; the hook configuration is not changed). If there is no red, the section says so in one sentence and keeps the empty table header. A `regression` row must also name the reachable public behavior the fix would go through and state that no fix was attempted in this plan.

    Section `## 7. What did not change`: the sentence in the shape of `07-MEASUREMENT.md` line 218 adapted to this phase (no production source under `extensions/`, no test, no script, no coverage configuration, census pin, threshold, suppression, direct pin, `.fallowrc.json` value, ESLint rule, Sonar property, pre-commit hook or `package.json` script changed; `maxCrap: 0` stays disabled and the policy stays 30), followed by the command and its output: `git diff --quiet 0844c2a7 HEAD -- extensions tests scripts package.json package-lock.json .fallowrc.json eslint.config.js sonar-project.properties .pre-commit-config.yaml; echo "exit=$?"` printing `exit=0`, the `git diff --stat 0844c2a7 HEAD` list of files that did change (expected: `.planning/**` only), the two pinned shortfalls from `scripts/test-coverage-direct.pin.json` (`bridges/commands/discover.ts` branches 55/56, lines 412/414; `orchestrators/plugin/install-outcome.ts` branches 109/111, lines 1039/1045) matched by the section 3 row, and the count of `fallow-ignore` markers (`rg -c "fallow-ignore" extensions tests scripts` summed) compared with the 11 recorded in `.planning/codebase/CONVENTIONS.md`.

    Commit as in Task 1.
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const t=fs.readFileSync("coverage/unit.lcov","utf8").split("\n");let rec=0,LF=0,LH=0,FNF=0,FNH=0,BRF=0,BRH=0,z=0,p=false;for(const l of t){if(l.startsWith("SF:")){p=l.includes("extensions/pi-claude-marketplace/");if(p)rec++;continue;}if(!p)continue;const i=l.indexOf(":");if(i<0)continue;const k=l.slice(0,i),v=l.slice(i+1);if(k==="LF")LF+=+v;else if(k==="LH")LH+=+v;else if(k==="FNF")FNF+=+v;else if(k==="FNH")FNH+=+v;else if(k==="BRF")BRF+=+v;else if(k==="BRH")BRH+=+v;else if((k==="DA"&&v.endsWith(",0"))||(k==="BRDA"&&(v.endsWith(",0")||v.endsWith(",-")))||(k==="FNDA"&&v.startsWith("0,")))z++;}const ok=rec===230&&LF>0&&LF===LH&&FNF===FNH&&BRF===BRH&&z===0;console.log(JSON.stringify({rec,LF,LH,FNF,FNH,BRF,BRH,zeroEntries:z}));console.log(ok?"RECOUNT PASS":"RECOUNT FAIL");process.exit(ok?0:1)'; git diff --quiet 0844c2a7 HEAD -- extensions tests scripts package.json package-lock.json .fallowrc.json eslint.config.js sonar-project.properties .pre-commit-config.yaml; echo "unchanged exit=$?"; D=.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md; for h in '^## 4\. Aggregate unit coverage' '^## 5\. Deficits and CRAP distribution' '^## 6\. Red classification' '^## 7\. What did not change'; do echo "$h=$(grep -cE "$h" "$D")"; done</automated>
    <fails_when>output lacks `RECOUNT PASS` (records not 230, LF != LH, FNF != FNH, BRF != BRH, or zeroEntries not 0), `unchanged exit=` is not 0, or any of the four heading counts is not 1</fails_when>
  </verify>
  <acceptance_criteria>
    - Section 4 contains the literal `acceptance.denominators` and a `runId` that is not `20260918T225947291Z-f055e502`; the manifest digest it records is not `39a577cc3a087653`.
    - Section 4's denominators table has a row beginning `| Native` and a row beginning `| Syntax`, and the native row's three ratios each have equal numerator and denominator (`<n>/<n>`).
    - Section 4's `Comparison with Phase 7` table has six rows, each ending in `equal` or `differs`; every `differs` row has a matching row in section 6.
    - Section 4 states the recount result with the literal `zero` for `DA:...,0`, `BRDA:...,0` and `FNDA:0,...` entries, or names the count and the file if any exists (which is a section 6 finding).
    - Section 5's histogram has the five score rows and a `30 or more` row whose count is `0`, or a non-zero count that section 6 classifies; the maximum line names its anchor (`plugin-browser.ts:108` `statusTag` expected).
    - Section 6's table has exactly one row per non-zero exit in sections 2 and 3, each with a classification cell reading `environment` or `regression`; the TruffleHog row, if present, contains `not a directory` and `SKIP=trufflehog`.
    - Section 7 contains `git diff --quiet 0844c2a7 HEAD` and `exit=0`, the two pinned shortfall paths `bridges/commands/discover.ts` and `orchestrators/plugin/install-outcome.ts`, and the phrase `maxCrap: 0`.
    - `git diff --name-only 0844c2a7 HEAD` lists only paths under `.planning/` at the commit of this task.
    - `git status --short` after the commit prints exactly the four local-edit lines.
  </acceptance_criteria>
  <done>Sections 4 to 7 record the fresh bundle identity, an independent recount equal to the manifest, the deficit list and CRAP distribution compared with Phase 7, one classified row per red, and the diff-backed statement that nothing was loosened.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `.planning/phases/08-final-verification-and-reconciliation/08-MEASUREMENT.md` (new; sections 1 to 7 by this plan, section 8 by plan 08-02): the phase-8 gate measurement record.
- Transient evidence under the executor scratchpad `gates/` directory (`run-gates.sh`, `SUMMARY.txt`, `EXTRA-SUMMARY.txt`, one log per gate, `status-before.txt`, `status-after.txt`): quoted into the document, never committed.
- Regenerated gitignored artifacts under `coverage/` (`unit.lcov`, `unit.istanbul.json`, `unit.manifest.json`, `unit.validation.json`, `unit.risk.json`, `all-pairs.jsonl`, `runs/<runId>/`): the run this document certifies.
- No code symbol, test, script or configuration file is created or modified.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Gate output to measurement record | Log lines and exit codes are copied by hand into a committed document; a mis-copied or research-copied figure would certify a run that did not happen. |
| Network egress (e2e only) | `npm run test:e2e` fetches one pinned upstream SHA (`git fetch --depth 1`) from `https://github.com/anthropics/claude-plugins-official`, anonymously (`GITHUB_TOKEN` unset). |
| Working tree to git index | `git add` runs beside four uncommitted per-machine files (`.claude/settings.json`, `.codex/config.toml`, `.planning/state.json`, `.mcp.json`) that must never enter a commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-08-01 | Tampering | gate configuration (`.fallowrc.json`, `eslint.config.js`, `scripts/coverage-risk-policy.json`, `scripts/test-coverage-direct.pin.json`, `scripts/check-unused-type-members.exceptions.json`, `sonar-project.properties`, `.pre-commit-config.yaml`, `package.json`) | high | mitigate | Section 7 records `git diff --quiet 0844c2a7 HEAD -- <those files>` exiting 0 at the measured HEAD; the prohibition forbids loosening anything to clear a red; Task 3's verify fails on a non-zero diff exit. |
| T-08-02 | Repudiation | exit capture in the runner | medium | mitigate | `rc=$?` on the command itself with output redirected to a log; no `tee`, no `tail`; a member without a summary line is re-run, never recorded. |
| T-08-03 | Spoofing | figures copied from 08-RESEARCH.md or Phase 7 instead of the run | medium | mitigate | The fresh `runId` and manifest digest must differ from the research run (`20260918T225947291Z-f055e502`, `39a577cc3a087653`); every count is quoted from `$OUT/*.log`; the Phase 7 numbers appear only in the labelled comparison table. |
| T-08-04 | Information disclosure | absolute worktree paths inside quoted error lines (the TruffleHog error names `/home/acolomba/...`) | low | accept | The measurement record is a planning document on a private branch; the same path already appears in committed planning files (STATE.md, 08-RESEARCH.md); no secret is present. |
| T-08-05 | Information disclosure | per-machine config files (`.mcp.json`, `.claude/settings.json`, `.codex/config.toml`) staged by accident | medium | mitigate | `git add` takes one explicit path per commit; `git status --short` after each commit must list exactly the four local edits. |
| T-08-06 | Tampering | e2e depth-1 fetch of the pinned SHA | low | accept | Read-only fetch of a content-addressed commit; the test asserts the pinned SHA (`tests/e2e/_pinned-sha.ts`); no credential is used. |
| T-08-07 | Denial of service | orphan `node` children after a killed member, or two coverage producers running at once | low | mitigate | One sequential detached runner; members re-run singly after a kill; `coverage/unit.manifest.json` is read only after `test:coverage:unit` exits. |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan (`package.json` and `package-lock.json` are inside the T-08-01 diff, expected unchanged); the package-legitimacy gate has no target here. |
</threat_model>

<verification>
- Sixteen chain rows with exit 0 in `package.json` `check` order (Task 2 verify counts them).
- `npm run coverage:validate` exits 0 after every write, proving the certified bundle is the run's own and no code drifted.
- The LCOV recount prints `RECOUNT PASS` (records 230, LF == LH, FNF == FNH, BRF == BRH, zero-count entries 0).
- `git diff --quiet 0844c2a7 HEAD -- <code and gate configuration>` exits 0 at the final commit of this plan.
- Every non-zero exit in sections 2 and 3 has one classified row in section 6; the expected set is the single TruffleHog worktree row.
- After each of the three commits `git status --short` prints exactly the four local-edit lines.
</verification>

<success_criteria>
- FINAL-01: every required gate ran fresh at the phase HEAD as its own process with its exit captured directly, all sixteen chain members exit 0, and the three additional gates are recorded with their exits and classifications.
- FINAL-01: aggregate unit production coverage is exactly 100% in lines, functions and branches on the run's own validated bundle, recounted independently, with the syntax model reported apart.
- FINAL-01: the direct-pair pin matched exactly (239 pairs, the two pinned shortfalls), assertion strength and every threshold are unchanged, and section 7 proves it with an empty diff over the code tree and gate configuration.
- The measurement document is committed, its prose is plain, and no file outside `.planning/` changed.
</success_criteria>

<output>
Create `.planning/phases/08-final-verification-and-reconciliation/08-01-SUMMARY.md` when done, recording each commit, the `$OUT/SUMMARY.txt` and `$OUT/EXTRA-SUMMARY.txt` lines verbatim, the fresh runId, the recount output, and any red with its classification.
</output>
