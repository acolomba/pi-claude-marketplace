---
phase: 05-production-export-ownership
plan: "15"
type: execute
wave: 6
depends_on: ["05-02","05-07","05-12"]
files_modified:
  - "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
  - "tests/orchestrators/plugin/operations.test.ts"
  - "extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts"
  - "extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts"
  - "tests/orchestrators/plugin/install-flow.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
  - "extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts"
  - "tests/integration/transaction-lifecycle-cascade.test.ts"
  - "tests/integration/fold-adoption.test.ts"
  - "tests/integration/concurrent-install-child.ts"
  - "tests/architecture/config-state-consistency.test.ts"
  - "tests/architecture/cross-op-convergence.test.ts"
  - "tests/orchestrators/plugin/update-flow.test.ts"
  - "tests/orchestrators/plugin/reinstall-flow.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Give install its production composition owner preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/import/execute.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/orchestrators/plugin/operations.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Give install its production composition owner.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.
operations.ts is shared by 05-16 through 05-18; these plans execute sequentially.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts
@extensions/pi-claude-marketplace/orchestrators/import/execute.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Compose install at the command boundary</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts, tests/orchestrators/plugin/operations.test.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts, tests/integration/transaction-lifecycle-cascade.test.ts, tests/integration/fold-adoption.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/edge/handlers/plugin/install.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, create the single plugin operations composition module. Add createInstallOperation(hooksRouting, completionCache), importing createInstallPlugin and binding the same real runPhases/withLockedStateTransaction implementation currently used by createNodeInstallPlugin. Move the concrete transaction binding into this owner; retain semantic InstallTransaction in install-flow.ts. Switch the install handler to the new composition operation. The new mirrored owner tests exercise the actual composed install against a hermetic local plugin and assert result/state/notifications, including a no-side-effects-on-construction check. No external connection, disk read or timer may start while constructing the operation. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan. Also migrate these two integration callers from createNodeInstallPlugin to createInstallOperation, preserving their complete lifecycle and fold assertions.</action>
  <verify><automated>node --test tests/orchestrators/plugin/operations.test.ts tests/edge/handlers/plugin/install.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Migrate cascading install callers and retire the redundant wrapper</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts, tests/orchestrators/plugin/install-flow.test.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts, tests/integration/concurrent-install-child.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/install-flow.ts, tests/orchestrators/plugin/install-flow.test.ts, extensions/pi-claude-marketplace/orchestrators/import/execute.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, switch the import executor and reconcile apply to createInstallOperation, then remove createNodeInstallPlugin and its old concrete transaction binding from install-flow.ts. Preserve createInstallPlugin as the now production-consumed semantic factory. Owner tests use that public factory with the existing transaction contract; preserve all typed outcomes, routing/cache lifetimes, rollback order and complete notifications. Move any concrete-adapter assertions to operations.test.ts as part of the earlier composition task's owner work. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan. Repoint the concurrent-install child helper to createInstallOperation without changing its protocol or diagnostics.</action>
  <verify><automated>node --test tests/orchestrators/plugin/install-flow.test.ts tests/orchestrators/import/execute.test.ts tests/orchestrators/reconcile/apply.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto">
  <name>Migrate the remaining install composition test consumers</name>
  <files>tests/architecture/config-state-consistency.test.ts, tests/architecture/cross-op-convergence.test.ts, tests/orchestrators/plugin/update-flow.test.ts, tests/orchestrators/plugin/reinstall-flow.test.ts</files>
  <read_first>tests/architecture/config-state-consistency.test.ts, tests/architecture/cross-op-convergence.test.ts, tests/orchestrators/plugin/update-flow.test.ts, tests/orchestrators/plugin/reinstall-flow.test.ts</read_first>
  <action>Per D-01, D-03 and D-04, repoint all remaining createNodeInstallPlugin test imports to createInstallOperation from operations.ts. Preserve the exact configured routing/cache instances, saved-state and cross-operation convergence results, and all update/reinstall assertions. Update ReturnType references with the same public function contract. Do not re-export the retired convenience merely to keep these imports working. Record the reviewed assertion migration for the parent-owned wave reconciliation.</action>
  <verify><automated>node --test tests/architecture/config-state-consistency.test.ts tests/architecture/cross-op-convergence.test.ts tests/orchestrators/plugin/update-flow.test.ts tests/orchestrators/plugin/reinstall-flow.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>Every named import uses the real composition owner and all existing public-contract assertions pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-15-01 | Tampering | extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-15-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-15-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-15-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
