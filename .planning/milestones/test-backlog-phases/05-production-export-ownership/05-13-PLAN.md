---
phase: 05-production-export-ownership
plan: "13"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "extensions/pi-claude-marketplace/edge/completions/data.ts"
  - "tests/edge/completions/data.test.ts"
  - "extensions/pi-claude-marketplace/edge/flag-catalog.ts"
  - "tests/edge/flag-catalog.test.ts"
  - "tests/architecture/flag-catalog-drift.test.ts"
  - "extensions/pi-claude-marketplace/edge/router.ts"
  - "tests/edge/router.test.ts"
  - "tests/edge/register.test.ts"
  - "extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts"
  - "tests/edge/handlers/plugin/fetch.test.ts"
  - "extensions/pi-claude-marketplace/edge/handlers/tools.ts"
  - "tests/edge/handlers/tools.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/plugin/list-flow.ts"
  - "tests/orchestrators/plugin/list-flow.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/plugin/list-orphan-fold.ts"
  - "tests/orchestrators/plugin/list-orphan-fold.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Own command completion and dispatch projections preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/edge/flag-catalog.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/edge/router.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/edge/completions/data.test.ts"
      to: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own command completion and dispatch projections.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/edge/completions/data.ts
@extensions/pi-claude-marketplace/edge/flag-catalog.ts
@extensions/pi-claude-marketplace/edge/router.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Observe catalog policy through completion behavior</name>
  <files>extensions/pi-claude-marketplace/edge/completions/data.ts, tests/edge/completions/data.test.ts, extensions/pi-claude-marketplace/edge/flag-catalog.ts, tests/edge/flag-catalog.test.ts, tests/architecture/flag-catalog-drift.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/edge/completions/data.ts, tests/edge/completions/data.test.ts, extensions/pi-claude-marketplace/edge/flag-catalog.ts, tests/edge/flag-catalog.test.ts, tests/architecture/flag-catalog-drift.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, privatize buildItem and getPluginToMarketplacesMap and exercise them through the public completion-data operation, retaining ordering, duplicate names, scope, unavailable entries and complete replacement texts. Remeasure CATALOG_VERBS after Phase 4: retain it only if a real production reader now uses it; otherwise retire its unconsumed derived constant and make the drift check use an independently declared exact 19-verb inventory plus the public catalog lookup. Keep Phase 4's offender and benign drift controls and argument semantics; a catalog cannot supply its own expected list. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/edge/completions/data.test.ts && node --test tests/edge/flag-catalog.test.ts tests/edge/completions/provider.test.ts tests/architecture/flag-catalog-drift.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Observe usage strings through dispatch</name>
  <files>extensions/pi-claude-marketplace/edge/router.ts, tests/edge/router.test.ts, tests/edge/register.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/edge/router.ts, tests/edge/router.test.ts, tests/edge/register.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01 and D-03, make TOP_LEVEL_USAGE and MARKETPLACE_USAGE private. Preserve full text, severity, and zero-dispatch behavior for unknown/malformed commands through the public router and registered command. Replace register.test.ts's import of the expected usage constant with independently written expected bytes. Retain all 19-command unknown-flag/surplus-positional assertions. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/edge/router.test.ts tests/edge/register.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto">
  <name>Narrow the list payload producer to its actual row contract</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/list-flow.ts, tests/orchestrators/plugin/list-flow.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/list-orphan-fold.ts, tests/orchestrators/plugin/list-orphan-fold.test.ts</files>
  <read_first>Trace loadPluginListPayload, enumerateMarketplacePlugins, OrphanFold, foldOrphanListRows and orderPluginListBlocks with CodeGraph. Keep list.messaging.ts unchanged because the notification path legitimately owns failed rows.</read_first>
  <action>Narrow loader output and intermediate payload collections to the nine real list-row variants, excluding the failed row constructed only by listPlugins after a loader rejection. Preserve all runtime operations and failure notifications. Make orphan folding output describe the five installed variants its filter actually returns and make pure ordering preserve its caller's row type. Add positive exact-union and negative impossible-status compile-time controls in the corresponding owners. Keep all existing row/result/error/order/mutation assertions. This task is a prerequisite to removing the tool projection's unreachable broad-status refusal branches.</action>
  <verify><automated>node --test tests/orchestrators/plugin/list-flow.test.ts tests/orchestrators/plugin/list-orphan-fold.test.ts &amp;&amp; npm run typecheck</automated><fails_when>Any existing runtime contract fails, an impossible loader row type compiles, or direct owner coverage regresses.</fails_when></verify>
  <done>The real producer exposes only its actual rows, ordering retains its input type, and the separate failed notification contract remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Observe fetch targets and tool statuses through handlers</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts, tests/edge/handlers/plugin/fetch.test.ts, extensions/pi-claude-marketplace/edge/handlers/tools.ts, tests/edge/handlers/tools.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts, tests/edge/handlers/plugin/fetch.test.ts, extensions/pi-claude-marketplace/edge/handlers/tools.ts, tests/edge/handlers/tools.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-10, make parseFetchTarget and its exclusive ParsedFetchTarget interface private. After narrowing the real payload producer in the added task, make projectRowStatus private and exhaustive over that nine-status row contract. Preserve target parsing failures and complete tool row/status values through public handlers. Retire only unreachable non-list helper refusal branches with type-negative evidence; retain load errors and listPlugins failure notifications. Keep no-work-before-validation and all flags from Phase 4. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/edge/handlers/plugin/fetch.test.ts tests/edge/handlers/tools.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-13-01 | Tampering | extensions/pi-claude-marketplace/edge/completions/data.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-13-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-13-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-13-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
