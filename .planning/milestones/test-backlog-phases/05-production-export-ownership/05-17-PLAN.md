---
phase: 05-production-export-ownership
plan: "17"
type: execute
wave: 8
depends_on: ["05-02","05-15","05-16"]
files_modified:
  - "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
  - "tests/orchestrators/plugin/operations.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts"
  - "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-flow.ts"
  - "tests/orchestrators/plugin/reinstall-flow.test.ts"
  - "tests/orchestrators/plugin/enable-disable.test.ts"
  - "tests/integration/transaction-lifecycle-cascade.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Give reinstall backfill a real factory consumer preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-flow.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/orchestrators/plugin/operations.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Give reinstall backfill a real factory consumer.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.planning/phases/05-production-export-ownership/05-15-SUMMARY.md
@extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts
@extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-flow.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Compose backfill reinstall through the semantic factory</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts, tests/orchestrators/plugin/operations.test.ts, extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts, tests/orchestrators/plugin/enable-disable.test.ts, tests/integration/transaction-lifecycle-cascade.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/reconcile/backfill.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, add createReinstallOperation(hooksRouting, completionCache) to operations.ts using createReinstallPlugin and the already-public REAL_REINSTALL_TRANSACTION from reinstall-replace.ts. Switch backfill's createNodeReinstallPlugin import to that operation. Preserve the supplied routing/cache lifecycle and direct/orchestrated result contract; add a concrete adapter case to the operations owner. Leave the bulk/direct command's existing createNodeReinstallPlugins contract intact. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan. Migrate the two outside single-reinstall convenience imports in these tests to createReinstallOperation with the same lifecycle owners and assertions. The bulk createNodeReinstallPlugins imports remain live and unchanged.</action>
  <verify><automated>node --test tests/orchestrators/plugin/operations.test.ts tests/orchestrators/reconcile/backfill.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep bulk composition internal and preserve reinstall behavior</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-flow.ts, tests/orchestrators/plugin/reinstall-flow.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/reinstall-flow.ts, tests/orchestrators/plugin/reinstall-flow.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-10, make the now same-file-only createNodeReinstallPlugin private while createNodeReinstallPlugins continues consuming it. createReinstallPlugin now has the real external production reader introduced above and keeps its semantic transaction tests. Replace direct imports of the private convenience wrapper in the owner with the public semantic factory and explicit real transaction where the concrete adapter is the promise. Preserve rollback, direct/bulk ordering and notifications. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/orchestrators/plugin/reinstall-flow.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-17-01 | Tampering | extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-17-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-17-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-17-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
