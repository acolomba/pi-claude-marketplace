---
phase: 05-production-export-ownership
plan: "23"
type: execute
wave: 6
depends_on: ["05-10","05-11","05-12"]
files_modified:
  - "extensions/pi-claude-marketplace/shared/notification-types.ts"
  - "tests/shared/notification-types.test.ts"
  - "tests/architecture/closed-set-enrollment.test.ts"
  - "tests/architecture/notify-closed-set-locks.test.ts"
  - "tests/architecture/compat-01-no-expansion.test.ts"
  - "extensions/pi-claude-marketplace/shared/notification-grammar.ts"
  - "tests/shared/notification-grammar.test.ts"
  - "extensions/pi-claude-marketplace/shared/notification-dispatch.ts"
  - "tests/shared/notification-dispatch.test.ts"
  - "extensions/pi-claude-marketplace/shared/notify-reasons.ts"
  - "tests/shared/notify-reasons.test.ts"
  - "tests/shared/notification-summary.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Own notification vocabularies, rendering, and delivery preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/shared/notification-types.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/shared/notification-grammar.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/shared/notification-dispatch.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/shared/notify-reasons.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/shared/notification-types.test.ts"
      to: "extensions/pi-claude-marketplace/shared/notification-types.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own notification vocabularies, rendering, and delivery.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/shared/notification-types.ts
@extensions/pi-claude-marketplace/shared/notification-grammar.ts
@extensions/pi-claude-marketplace/shared/notification-dispatch.ts
@extensions/pi-claude-marketplace/shared/notify-reasons.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Keep closed vocabularies private while preserving exact type contracts</name>
  <files>extensions/pi-claude-marketplace/shared/notification-types.ts, tests/shared/notification-types.test.ts, tests/architecture/closed-set-enrollment.test.ts, tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/compat-01-no-expansion.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/notification-types.ts, tests/shared/notification-types.test.ts, tests/architecture/closed-set-enrollment.test.ts, tests/architecture/notify-closed-set-locks.test.ts, tests/architecture/compat-01-no-expansion.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-07, remove export visibility from REASONS, STATUS_TOKENS, PLUGIN_STATUSES and MARKETPLACE_STATUSES while preserving their canonical closed contracts. Replace outside runtime imports with independent exact literal vocabularies checked bidirectionally against public Reason/StatusToken/PluginStatus/MarketplaceStatus types; a one-sided satisfies check cannot prove no expansion. Keep the enrollment and no-expansion gates discriminating against an added or missing union member. Preserve all corresponding real renderer cases. Where the tuples serve only type derivation, retain private canonical declarations or encode the identical closed type contracts without runtime-only artifacts; prove the same union equality and negative compiler cases. Do not grant exceptions merely to keep tests importing constants. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>npm run typecheck</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Observe private icons and summary delivery through public outputs</name>
  <files>extensions/pi-claude-marketplace/shared/notification-grammar.ts, tests/shared/notification-grammar.test.ts, extensions/pi-claude-marketplace/shared/notification-dispatch.ts, tests/shared/notification-dispatch.test.ts, tests/architecture/compat-01-no-expansion.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/notification-grammar.ts, tests/shared/notification-grammar.test.ts, extensions/pi-claude-marketplace/shared/notification-dispatch.ts, tests/shared/notification-dispatch.test.ts, tests/architecture/compat-01-no-expansion.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01 and D-03, make ICON_REMOTE and ICON_PARTIALLY_AVAILABLE private, retaining exact glyph/spacing bytes through renderRemoteRow and renderPartiallyAvailableRow. Make emitWithSummary private and relocate its cases to notify, notifyDiagnostic and the other public delivery paths that consume it, preserving complete output and exactly-once append/notify ordering. Update the compatibility test's icon imports to independent expected bytes and public rendering assertions. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/shared/notification-grammar.test.ts tests/shared/notification-dispatch.test.ts tests/architecture/compat-01-no-expansion.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Consume the reason proof and preserve summary ownership</name>
  <files>extensions/pi-claude-marketplace/shared/notify-reasons.ts, tests/shared/notify-reasons.test.ts, tests/shared/notification-summary.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/shared/notify-reasons.ts, tests/shared/notify-reasons.test.ts, tests/shared/notification-summary.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-07, fold _ReasonsCoverageProof into the actual public reason-mapping contract so the proof is private but still compiler-enforced. Keep exact reason coverage and a missing-reason offender/benign compiler pair. Remove notification-summary.test.ts's import of private emitWithSummary: keep its own summary API cases there and move delivery assertions into the dispatch owner touched by Task 2, preserving exact messages, summary state and ordering. No assertion is discarded merely because its original helper is private. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/shared/notify-reasons.test.ts tests/shared/notification-summary.test.ts tests/shared/notification-dispatch.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-23-01 | Tampering | extensions/pi-claude-marketplace/shared/notification-types.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-23-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-23-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-23-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
