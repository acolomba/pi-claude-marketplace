---
phase: 05-production-export-ownership
plan: "10"
type: execute
wave: 3
depends_on: ["05-08"]
files_modified:
  - "extensions/pi-claude-marketplace/domain/plugin-resolver.ts"
  - "tests/domain/plugin-resolver.test.ts"
  - "extensions/pi-claude-marketplace/domain/mcp-resolution.ts"
  - "tests/domain/mcp-resolution.test.ts"
  - "tests/architecture/hooks-foundation.test.ts"
  - "extensions/pi-claude-marketplace/domain/auth-registry.ts"
  - "tests/domain/auth-registry.test.ts"
  - "extensions/pi-claude-marketplace/domain/unsupported-components.ts"
  - "tests/domain/unsupported-components.test.ts"
  - "extensions/pi-claude-marketplace/domain/resolver-types.ts"
  - "tests/domain/resolver-types.test.ts"
  - "extensions/pi-claude-marketplace/domain/component-paths.ts"
  - "tests/domain/component-paths.test.ts"
  - "eslint.config.js"
  - "tests/architecture/sonar-test-rules.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 5
  confidence: low
must_haves:
  truths:
    - "Own resolver, provider, and component contracts preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/plugin-resolver.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/domain/mcp-resolution.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/domain/auth-registry.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/domain/unsupported-components.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/domain/plugin-resolver.test.ts"
      to: "extensions/pi-claude-marketplace/domain/plugin-resolver.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own resolver, provider, and component contracts.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/domain/plugin-resolver.ts
@extensions/pi-claude-marketplace/domain/mcp-resolution.ts
@extensions/pi-claude-marketplace/domain/auth-registry.ts
@extensions/pi-claude-marketplace/domain/unsupported-components.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Retire the uncalled loose resolver branch</name>
  <files>extensions/pi-claude-marketplace/domain/plugin-resolver.ts, tests/domain/plugin-resolver.test.ts, extensions/pi-claude-marketplace/domain/mcp-resolution.ts, tests/domain/mcp-resolution.test.ts, tests/architecture/hooks-foundation.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/domain/plugin-resolver.ts, tests/domain/plugin-resolver.test.ts, extensions/pi-claude-marketplace/domain/mcp-resolution.ts, tests/domain/mcp-resolution.test.ts, tests/architecture/hooks-foundation.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-02 and D-03, reconfirm that resolveLoose has no production caller and that resolveLooseMcp is reachable only from that branch. Retire both implementations and their exclusively dead API tests. Map every shared validation/materialization/error assertion to the live resolveStrict or resolveStrictMcp tests before deleting a duplicate; retain all strict public outcomes and installable discrimination. Migrate hooks-foundation's loose-resolver fixture to the actual strict production path while preserving its hooks/schema assertion. Remove parameters/imports/private branches made unreachable by the retirement in these owners; do not leave a newly orphaned export in the census. Report the reviewed finding delta in the summary; the parent updates the shared exact census only at the stable wave boundary. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/domain/plugin-resolver.test.ts tests/domain/mcp-resolution.test.ts tests/architecture/hooks-foundation.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Observe providers and supported kinds through production queries</name>
  <files>extensions/pi-claude-marketplace/domain/auth-registry.ts, tests/domain/auth-registry.test.ts, extensions/pi-claude-marketplace/domain/unsupported-components.ts, tests/domain/unsupported-components.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/domain/auth-registry.ts, tests/domain/auth-registry.test.ts, extensions/pi-claude-marketplace/domain/unsupported-components.ts, tests/domain/unsupported-components.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01 and D-03, privatize GITLAB_PROVIDER and UNSUPPORTED_COMPONENT_KINDS. Retire SUPPORTED_COMPONENT_KINDS after confirming it has no production reader; retain actual supported-component coverage through resolveStrict. Keep GitLab registered in the real provider lookup, and assert all existing exact descriptors/host matching behavior through that lookup. Assert supported/unsupported classification and complete notification reasons through the public component predicates/classifiers with independent expected values. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/domain/auth-registry.test.ts tests/domain/unsupported-components.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Make the resolver schema private without losing its proofs</name>
  <files>extensions/pi-claude-marketplace/domain/resolver-types.ts, tests/domain/resolver-types.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/domain/resolver-types.ts, tests/domain/resolver-types.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-07 and D-10, retire unconsumed runtime schema construction, represent the same TypeBox schema as private type aliases, and derive all nine unchanged public contracts from the canonical union. Prove exact before/after type equality and preserve all positive and negative contract checks. The isolated compiler/analyzer probes justify one immediately adjacent private-type-leak annotation on ResolvedPlugin's schema reference, replacing three old proof annotations; retain no general exemption. The parent must add the exact type-only resolver owner to the existing Sonar policy and independent control list. Fold DroppedHookDriftCheck and DroppedHookArmKeysCheck into the public types they protect so the private proof aliases remain genuinely consumed and enforce the same bidirectional arm/key equality. Preserve exact positive and @ts-expect-error negative owner cases. Probe an added hook arm and an extra arm field in an isolated compiler fixture to show both drift checks fail; the unchanged fixture must compile. Only a demonstrated compiler limitation can justify an exact local proof exception. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan. Caller and gate revalidation found no resolver schema scan in closed-set-enrollment.test.ts; leave that unrelated file unchanged. Preserve resolver proofs in the actual public type owner.</action>
  <verify><automated>npm run typecheck</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Retire the exclusive loose component-path dependency</name>
  <files>extensions/pi-claude-marketplace/domain/component-paths.ts, tests/domain/component-paths.test.ts</files>
  <read_first>Trace collectLooseComponentPaths and collectLooseComponentKind with CodeGraph before editing.</read_first>
  <action>Retire both loose collectors after confirming their only production caller is the removed resolveLoose. Migrate complete entry-path deduplication and absent/null results to collectStrictComponentPaths with no matching convention. Retire only the exclusively loose manifest-conflict error. Preserve every existing strict containment, ordering, malformed-input, and propagation assertion.</action>
  <verify><automated>node --test tests/domain/component-paths.test.ts</automated></verify>
  <done>The exclusive dependency is gone without a newly orphaned export; the strict owner remains exactly covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Enroll the type-only resolver owner in the exact Sonar policy</name>
  <files>eslint.config.js, tests/architecture/sonar-test-rules.test.ts</files>
  <read_first>Read the exact type-only owner exception list and its independent Sonar control inventory. This task is parent-owned; the resolver executor must not edit these files.</read_first>
  <action>The parent adds tests/domain/resolver-types.test.ts as the ninth exact no-empty-test-file exception because its production owner now has no runtime emit. Add the same independently declared owner to the Sonar control inventory. Preserve all other assertion rules and ordinary runtime-owner empty-test refusals. Add no fake runtime assertion or blanket types-file exemption.</action>
  <verify><automated>node --test tests/architecture/sonar-test-rules.test.ts</automated></verify>
  <done>The exact type-only owner is accepted and all fifteen positive/negative Sonar controls remain discriminating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-10-01 | Tampering | extensions/pi-claude-marketplace/domain/plugin-resolver.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-10-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-10-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-10-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
