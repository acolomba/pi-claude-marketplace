---
phase: 05-production-export-ownership
plan: "21"
type: execute
wave: 10
depends_on: ["05-18"]
files_modified:
  - "extensions/pi-claude-marketplace/platform/git.ts"
  - "tests/platform/git.test.ts"
  - "extensions/pi-claude-marketplace/platform/git-auth-callbacks.ts"
  - "tests/platform/git-auth-callbacks.test.ts"
  - "tests/integration/auth-e2e.test.ts"
  - "tests/orchestrators/marketplace/add.test.ts"
  - "tests/orchestrators/marketplace/update.test.ts"
  - "tests/orchestrators/plugin/fetch.test.ts"
  - "extensions/pi-claude-marketplace/platform/git-credential.ts"
  - "tests/platform/git-credential.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/auth-host.ts"
  - "tests/orchestrators/auth-host.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Own git authentication callbacks and credentials preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/platform/git.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/platform/git-auth-callbacks.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/platform/git-credential.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/auth-host.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/platform/git.test.ts"
      to: "extensions/pi-claude-marketplace/platform/git.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own git authentication callbacks and credentials.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/platform/git.ts
@extensions/pi-claude-marketplace/platform/git-auth-callbacks.ts
@extensions/pi-claude-marketplace/platform/git-credential.ts
@extensions/pi-claude-marketplace/orchestrators/auth-host.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Move the complete authentication callback protocol to its owner</name>
  <files>extensions/pi-claude-marketplace/platform/git.ts, tests/platform/git.test.ts, extensions/pi-claude-marketplace/platform/git-auth-callbacks.ts, tests/platform/git-auth-callbacks.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/platform/git.ts, tests/platform/git.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01 and D-03, move buildAuthCallbacks, its callback option contract and complete fill/device-flow/reject/cancel behavior into platform/git-auth-callbacks.ts. git.ts must import and use it for clone, fetch and resolveRemoteRef; relocate direct callback cases to the new mirrored owner and retain git's transport tests. The new module is the complete protocol, not one file for a private helper. Reconfirm that listBranches/listRemotes have no production callers and retire those wrappers and their exclusive option type/tests; preserve every live transport assertion. Retain complete credential cancellation, rejection, error redaction and notification checks. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/platform/git.test.ts tests/platform/git-auth-callbacks.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto">
  <name>Migrate callback contract consumers</name>
  <files>tests/integration/auth-e2e.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/fetch.test.ts</files>
  <read_first>tests/integration/auth-e2e.test.ts, tests/orchestrators/marketplace/add.test.ts, tests/orchestrators/marketplace/update.test.ts, tests/orchestrators/plugin/fetch.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-01 and D-03, repoint buildAuthCallbacks imports to its new production-owned platform module. Keep every callback invocation, credentials result, retry/rejection sequence and redaction assertion unchanged. Do not replace these hermetic callback tests with a live auth service. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/integration/auth-e2e.test.ts tests/orchestrators/marketplace/add.test.ts tests/orchestrators/marketplace/update.test.ts tests/orchestrators/plugin/fetch.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Compose credential operations in auth-host</name>
  <files>extensions/pi-claude-marketplace/platform/git-credential.ts, tests/platform/git-credential.test.ts, extensions/pi-claude-marketplace/orchestrators/auth-host.ts, tests/orchestrators/auth-host.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/platform/git-credential.ts, tests/platform/git-credential.test.ts, extensions/pi-claude-marketplace/orchestrators/auth-host.ts, tests/orchestrators/auth-host.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, make auth-host construct DEFAULT_CREDENTIAL_OPS by importing createCredentialOps and supplying the same Node process launcher and 5000ms timeout explicitly. Remove the unconsumed default convenience from git-credential.ts; keep the entire credential subprocess/protocol implementation and narrow injected process contract in that platform module. Update auth-host's callback imports to the new owner and preserve its credential public binding tests. Preserve the credential owner tests for protocol bytes, status/launch discrimination, timeout, fill miss, approve/reject and redaction. Construction must not spawn a process. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/platform/git-credential.test.ts tests/orchestrators/auth-host.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-21-01 | Tampering | extensions/pi-claude-marketplace/platform/git.ts | high | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-21-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-21-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-21-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
