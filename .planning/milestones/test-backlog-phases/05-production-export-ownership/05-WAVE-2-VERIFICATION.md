---
phase: 05-production-export-ownership
wave: 2
status: passed
plans: ["05-02", "05-06", "05-08", "05-09", "05-13"]
---

# Production export ownership: wave 2 verification

The stable source/test tree passed final integration. This wave retires 25 unused value exports and the duplicate assertNever group: the complete production census moves from 111 to 85, with no additions. The remaining 68 unused values, 12 unused types, one unused file, one unused class member, and three duplicate groups remain open for later approved owner plans.

## Reviewed scope and assertion strength

The five plan summaries contain complete assertion ledgers. Marker tests preserve independent current/legacy bytes, ownership refusals and lifecycle filesystem state. Hook tests preserve the complete predicate union and both exhaustiveness diagnostics; the now type-only outcome owner retains all positive and negative compiler proofs. MCP cleanup removes the unreachable legacy parser and its exclusive types, while retaining the live malformed-field error privately with exact public failure fields and unchanged filesystem state. Collision, substitution and tool inventory checks move through real public operations.

The command projection cleanup preserves all nineteen canonical verbs and twenty-two accepted spellings, exact completion order/text, fetch target selection, and validation-before-work behavior. The loader type now declares only its nine producible statuses; the tool no longer exposes or tests ten impossible helper inputs. Independent compile controls reject those statuses, while the list command's separate failure notification and every public tool status case remain covered.

No runtime implementation is retained merely for a test export. No threshold, production exclusion, skipped case, or fake production caller was added. The exact eighth Sonar type-only exception is independently controlled; all earlier assertion rule controls remain.

## Integration findings and corrections

1. The complete census parser rejected a new private-type-leak category instead of ignoring it. The narrowed loader signature now exposes its actual Exclude contract directly. Two helper-only types orphaned by privacy became private, and their tests derive the public options or use independent expected status literals. All three temporary findings disappear; they are not counted as initial-census removals.
2. The shipping-mode control found the frontmatter marker facade after its last external test consumer moved. Its planned retirement moved forward from 05-03 into 05-02. Only the re-export and its comment were removed; the writer's defining import and all generated bytes remain. The initial controls failed 52/53 and the initial aggregate run failed the same shipping census case. The ongoing initial precommit and all-pairs runs were stopped before repair. Their results do not certify the corrected tree.

## Final evidence

- Complete census: 111 -> 85, exactly 26 reviewed removals, zero additions. `/tmp/test-backlog-wave2-census.json` records normalized identities and the complete delta.
- Census, real Fallow controls, registry and explicit-seam gate: **53/53 passed**, no skipped/cancelled/todo cases. `/tmp/test-backlog-wave2-controls-final.log`.
- Frontmatter direct check after early facade retirement: **639/639 lines, 19/19 functions, 104/104 branches**. `/tmp/test-backlog-wave2-frontmatter-direct.log`.
- Other plan-focused and direct checks are recorded in the five summaries; all affected direct pairs pass at their unchanged strictness.
- Full native aggregate production unit coverage: **6,230/6,230 tests passed**, zero failures/skips/cancellations/todo, 84.876 seconds. All 225 emitted production modules have exact **63,120/63,120 lines, 1,848/1,848 functions, and 9,099/9,099 branches**. `/tmp/test-backlog-wave2-unit-final.log` and `/tmp/test-backlog-wave2-coverage-final.json`. The count falls from 227 because the unreachable MCP parser was removed and hook exec-result now emits no JavaScript; neither module is excluded from correspondence or its applicable type proofs.
- All production source/test pairs: **233 unique pairs passed** in 490.2 seconds on Node v26.8.2, including eight type-only owners. Both pre-existing direct shortfalls match the unchanged pin exactly. `/tmp/test-backlog-wave2-direct-all-final.log`; machine-readable report `coverage/all-pairs.jsonl`.
- Mandatory full precommit: **passed**, including lint, formatting, typecheck, Fallow and changed-pair coverage. `/tmp/test-backlog-wave2-precommit-clean.log`. An earlier run found one missing blank line in collision-slots.test.ts; the correction preserves all 2,638 non-comment tokens, assertions and production source. The clean full rerun certifies the corrected tree; `/tmp/test-backlog-wave2-format-equivalence.json` records token equivalence.
- Independent code review: all 54 source/test/config paths reviewed, including the final facade delta and exact census; no substantiated findings. See 05-WAVE-2-REVIEW.md.
- Commit: `080d395e`, 54 explicitly staged source/test/config paths; unrelated settings and setup edits remain uncommitted.

Aggregate unit/Sonar coverage, direct-pair measurements, and integration/E2E reports remain separate. This wave report is not phase completion: EXPORT-01 and EXPORT-02 remain open until every remaining finding is dispositioned and production-mode activation passes its controls.
