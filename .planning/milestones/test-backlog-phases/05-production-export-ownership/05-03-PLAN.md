---
phase: 05-production-export-ownership
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
  - "tests/bridges/agents/convert.test.ts"
  - "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
  - "tests/bridges/agents/frontmatter.test.ts"
  - "extensions/pi-claude-marketplace/bridges/agents/marker.ts"
  - "tests/bridges/agents/marker.test.ts"
  - "extensions/pi-claude-marketplace/bridges/agents/index.ts"
  - "tests/bridges/agents/index.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Own agent conversion and frontmatter exports preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/bridges/agents/marker.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/bridges/agents/index.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/bridges/agents/convert.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/agents/convert.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own agent conversion and frontmatter exports.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/bridges/agents/convert.ts
@extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts
@extensions/pi-claude-marketplace/bridges/agents/marker.ts
@extensions/pi-claude-marketplace/bridges/agents/index.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Test conversion tables and scalar handling through emitted agents</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, tests/bridges/agents/convert.test.ts, extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/agents/convert.ts, tests/bridges/agents/convert.test.ts, extensions/pi-claude-marketplace/bridges/agents/frontmatter.ts, tests/bridges/agents/frontmatter.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01, D-03 and D-04, privatize MODEL_MAP, TOOL_MAP, THINKING_VALUES, emitYamlScalar and sanitizeProvenanceValue; confirm frontmatter's marker convenience re-export already retired in 05-02 to keep the intermediate shipping gate clean. Drive all existing mapping, unknown-value, quoting, newline/control-character and provenance cases through convertAgent and the public frontmatter writer/parser. Preserve exact generated bytes and complete warning arrays. Use the current Phase 3 conversion code; do not restore its removed collision helper or the old lossy naming behavior. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/frontmatter.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep marker detection public and prune the bridge facade</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/marker.ts, tests/bridges/agents/marker.test.ts, extensions/pi-claude-marketplace/bridges/agents/index.ts, tests/bridges/agents/index.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/agents/marker.ts, tests/bridges/agents/marker.test.ts, extensions/pi-claude-marketplace/bridges/agents/index.ts, tests/bridges/agents/index.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, make GENERATED_AGENT_PREFIX and GENERATED_AGENT_MARKER_LEGACY private. Keep the real marker detector and any marker binding with actual production readers public. Remove marker convenience exports from the agents barrel and update its exact binding assertions. Retain current/legacy/foreign/truncated marker recognition through the public detector using independent fixture bytes; prior plan 05-02 already migrated external test consumers. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/bridges/agents/marker.test.ts tests/bridges/agents/index.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-03-01 | Tampering | extensions/pi-claude-marketplace/bridges/agents/convert.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-03-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-03-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-03-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
