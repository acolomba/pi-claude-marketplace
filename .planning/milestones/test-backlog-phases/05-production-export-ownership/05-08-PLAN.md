---
phase: 05-production-export-ownership
plan: "08"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - "extensions/pi-claude-marketplace/bridges/mcp/parse.ts"
  - "tests/bridges/mcp/parse.test.ts"
  - "extensions/pi-claude-marketplace/bridges/mcp/index.ts"
  - "tests/bridges/mcp/index.test.ts"
  - "tests/architecture/integration-materialization-gate.test.ts"
  - "extensions/pi-claude-marketplace/bridges/mcp/substitute.ts"
  - "tests/bridges/mcp/substitute.test.ts"
  - "extensions/pi-claude-marketplace/bridges/mcp/stage.ts"
  - "tests/bridges/mcp/stage.test.ts"
  - "tests/bridges/mcp/unstage.test.ts"
  - "extensions/pi-claude-marketplace/bridges/mcp/types.ts"
  - "tests/bridges/mcp/types.test.ts"
files_deleted:
  - "extensions/pi-claude-marketplace/bridges/mcp/parse.ts"
  - "tests/bridges/mcp/parse.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Own MCP parsing, substitution, and stage errors preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/bridges/mcp/index.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/bridges/mcp/substitute.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/bridges/mcp/stage.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/bridges/mcp/index.test.ts"
      to: "extensions/pi-claude-marketplace/bridges/mcp/index.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Own MCP parsing, substitution, and stage errors.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/bridges/mcp/parse.ts
@extensions/pi-claude-marketplace/bridges/mcp/index.ts
@extensions/pi-claude-marketplace/bridges/mcp/substitute.ts
@extensions/pi-claude-marketplace/bridges/mcp/stage.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Retire the unreachable bridge MCP parser</name>
  <files>extensions/pi-claude-marketplace/bridges/mcp/parse.ts, tests/bridges/mcp/parse.test.ts, extensions/pi-claude-marketplace/bridges/mcp/index.ts, tests/bridges/mcp/index.test.ts, tests/architecture/integration-materialization-gate.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/mcp/parse.ts, tests/bridges/mcp/parse.test.ts, extensions/pi-claude-marketplace/bridges/mcp/index.ts, tests/bridges/mcp/index.test.ts, tests/architecture/integration-materialization-gate.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-02 and D-03, confirm with CodeGraph that bridge parse.ts has no production consumer except its unused index re-export: it contains only parseMcpServers and resolvePluginMcpServers. Retire that module and its paired tests together, remove the facade re-export, and update the barrel's remaining exact binding assertions. Record each retired legacy-only contract; retain shared validation/materialization assertions on the current domain resolution and staging path. Rewrite the architecture materialization fixture to use domain resolveStrict and the existing bridge stage operation, preserving exact resolved config, warnings and materialization outcomes. Do not create a replacement wrapper to keep the obsolete parser artificially live. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/bridges/mcp/index.test.ts tests/architecture/integration-materialization-gate.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Own substitution and the live MCP staging failure contract</name>
  <files>extensions/pi-claude-marketplace/bridges/mcp/substitute.ts, tests/bridges/mcp/substitute.test.ts, extensions/pi-claude-marketplace/bridges/mcp/stage.ts, tests/bridges/mcp/stage.test.ts, tests/bridges/mcp/unstage.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/bridges/mcp/substitute.ts, tests/bridges/mcp/substitute.test.ts, extensions/pi-claude-marketplace/bridges/mcp/stage.ts, tests/bridges/mcp/stage.test.ts, tests/bridges/mcp/unstage.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-01 and D-03, make deepSubstitute private and drive its nested object/array, escaped/missing-variable and environment-injection cases through substituteAndInject. The refreshed caller trace confirms MalformedMcpServersError remains live through classifyMcpServers in both staging and unstaging. Keep its implementation private. Replace owner tests' private-constructor imports with public Error-instance and exact constructor-name, name, message, mcpJsonPath, valueKind, retained-byte and unchanged-metadata assertions. Preserve all live prepare/replace/rollback behavior and assertions. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/bridges/mcp/substitute.test.ts tests/bridges/mcp/stage.test.ts tests/bridges/mcp/unstage.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto">
  <name>Retire the legacy parser's orphan types and compile-time proofs</name>
  <files>extensions/pi-claude-marketplace/bridges/mcp/types.ts, tests/bridges/mcp/types.test.ts</files>
  <read_first>Trace McpServersSource, ResolvedMcpServers and ResolvePluginMcpServersInput with CodeGraph; inspect their mirrored type-owner assertions.</read_first>
  <action>Remove only the three types whose sole production consumer was the retired parser. Retire their positive literals, complete object shapes, required-field negatives and readonly assignments together. Preserve every unrelated RawMcpDoc, staging, commit, replacement and unstage compile-time proof. Update the StageMcpInput servers documentation to identify the live domain resolver. Record the exact retired proof ledger and the absence of production readers; do not add a fake caller or suppression to retain these types.</action>
  <verify><automated>node --test tests/bridges/mcp/types.test.ts &amp;&amp; npm run typecheck</automated><fails_when>The type owner or compiler fails, or an unrelated existing compile-time proof is removed or weakened.</fails_when></verify>
  <done>The orphan parser types and only their corresponding proofs are retired; all live MCP type contracts remain checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-08-01 | Tampering | extensions/pi-claude-marketplace/bridges/mcp/index.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-08-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-08-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-08-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
