---
phase: 05-production-export-ownership
plan: "18"
type: execute
wave: 9
depends_on: ["05-13","05-15","05-16","05-17"]
files_modified:
  - "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
  - "tests/orchestrators/plugin/operations.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts"
  - "tests/orchestrators/plugin/fetch.test.ts"
  - "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
  - "tests/orchestrators/plugin/info.test.ts"
  - "extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts"
  - "extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts"
  - "tests/architecture/cross-op-convergence.test.ts"
autonomous: true
requirements: ["EXPORT-01"]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Compose fetch and info with their real read capabilities preserves the existing observable production results, failures, and promised interactions."
    - "Every changed export has a real production consumer, becomes private behind its public contract, or is retired with no-caller evidence."
    - "Owner tests preserve exact assertions and the integrated wave retains the 100% aggregate production unit baseline."
  artifacts:
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts"
      provides: "Production-owned contract for this concern"
    - path: "extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/orchestrators/plugin/operations.test.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Compose fetch and info with their real read capabilities.

Purpose: fulfill EXPORT-01 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.planning/phases/05-production-export-ownership/05-15-SUMMARY.md
@extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts
@extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
@extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer" tdd="true">
  <name>Bind both read commands in the shared composition module</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts, tests/orchestrators/plugin/operations.test.ts</files>
  <read_first>package.json, .fallowrc.json, tests/architecture/unowned-exports-census.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03, compose fetchPlugins and getPluginInfo in operations.ts by importing createFetchPlugins/createGetPluginInfo and the concrete filesystem/status capabilities already used in their owners. Use explicit readTextFile/listDirectory and makePresenceProbe/probeManifestEntry bindings. Extend the operations owner with local read-command behavior, including no I/O during construction and complete emitted results. This is the same plugin composition concern, not two new helper modules. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/orchestrators/plugin/operations.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto" tdd="true">
  <name>Keep fetch/info logic injectable through production-owned factories</name>
  <files>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, tests/orchestrators/plugin/fetch.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/orchestrators/plugin/fetch.ts, tests/orchestrators/plugin/fetch.test.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, tests/orchestrators/plugin/info.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <behavior>
    - The named public path retains the complete successful result and resulting state asserted by its owner.
    - Every existing malformed-input, failure, and rollback case retains the exact error contract, emitted bytes, and promised interaction sequence.
    - The changed helper is unreachable as a test-only export; the named real consumer or public owner test exercises its live behavior.
  </behavior>
  <action>Per D-03 and D-04, remove the old concrete fetchPlugins/getPluginInfo values and Node binding objects from the flow owners. Retain createFetchPlugins and createGetPluginInfo with their current narrow interfaces. Migrate owner tests from the convenience values to those factories using the existing stateful reader/status collaborators; retain complete outputs, cold/warm cache decisions, auth memo use and all no-network assertions. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan.</action>
  <verify><automated>node --test tests/orchestrators/plugin/fetch.test.ts tests/orchestrators/plugin/info.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

<task type="auto">
  <name>Route live read commands to the production bindings</name>
  <files>extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts, tests/architecture/cross-op-convergence.test.ts</files>
  <read_first>extensions/pi-claude-marketplace/edge/handlers/plugin/fetch.ts, extensions/pi-claude-marketplace/edge/handlers/plugin/info.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-03 and D-10, switch the two handler imports to operations.ts, preserving argument validation and notification behavior. Keep type imports at their defining flow owners. Verify both registered paths still execute the same command contract; reconcile the exact census and account for the two newly production-owned factories. Report the reviewed finding delta in the summary; the parent updates the shared exact census only at the stable wave boundary. Per D-01 and D-04, preserve concurrent work and the completed lifecycle/seam protections. Record this task's assertion and finding dispositions for the parent-owned wave reconciliation; do not edit the shared census pin from this plan. Migrate cross-op-convergence's getPluginInfo import to the real composed value in operations.ts and preserve every exact convergence assertion.</action>
  <verify><automated>node --test tests/edge/handlers/plugin/fetch.test.ts tests/edge/handlers/plugin/info.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above. The summary identifies the reviewed finding delta for the wave coordinator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-18-01 | Tampering | extensions/pi-claude-marketplace/orchestrators/plugin/operations.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-18-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-18-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. The parent performs the 05-VALIDATION.md wave reconciliation after all plans in this wave complete: review source/finding deltas, update the exact pin once, run the full census/control gates, and measure aggregate production unit coverage on the stable wave snapshot. Do not run or update a live whole-tree census concurrently with another source writer.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-18-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
