---
phase: 05-production-export-ownership
plan: "28"
type: execute
wave: 11
depends_on: ["05-01","05-02","05-03","05-04","05-05","05-06","05-07","05-08","05-09","05-10","05-11","05-12","05-13","05-14","05-15","05-16","05-17","05-18","05-19","05-20","05-21","05-22","05-23","05-24","05-25","05-26","05-27"]
files_modified:
  - "scripts/check-phase-06-hub-ledger.mjs"
  - "tests/scripts/check-phase-06-hub-ledger.test.ts"
  - ".planning/BACKLOG.md"
  - ".fallowrc.json"
  - "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts"
  - "extensions/pi-claude-marketplace/index.ts"
  - "tests/index.test.ts"
  - "tests/architecture/fallow-production-mode.test.ts"
  - "tests/architecture/unowned-exports-census.test.ts"
  - "tests/architecture/gate-targets.ts"
  - "eslint.config.js"
  - "tests/architecture/sonar-test-rules.test.ts"
files_deleted:
  - "scripts/check-phase-06-hub-ledger.mjs"
  - "tests/scripts/check-phase-06-hub-ledger.test.ts"
autonomous: true
requirements: ["EXPORT-01","EXPORT-02"]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The shipping dead-code command uses production reachability and returns an empty complete report."
    - "Health and duplication retain their existing test scope, and entry-export detection remains enabled."
    - "All live controls and quality checks pass with 100% aggregate production unit coverage and unchanged direct-pair requirements."
  artifacts:
    - path: "extensions/pi-claude-marketplace/index.ts"
      provides: "Production-owned contract for this concern"
  key_links:
    - from: "tests/index.test.ts"
      to: "extensions/pi-claude-marketplace/index.ts"
      via: "The public operation or type contract named in the tasks"
---

<objective>
Activate production enforcement and close the complete census.

Purpose: fulfill EXPORT-01 and EXPORT-02 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.

</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@extensions/pi-claude-marketplace/index.ts
</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer">
  <name>Retire the historical hub-ledger command and reconcile FLOW-09</name>
  <files>scripts/check-phase-06-hub-ledger.mjs, tests/scripts/check-phase-06-hub-ledger.test.ts, .planning/BACKLOG.md, eslint.config.js, tests/architecture/sonar-test-rules.test.ts</files>
  <read_first>scripts/check-phase-06-hub-ledger.mjs, tests/scripts/check-phase-06-hub-ledger.test.ts, .planning/BACKLOG.md; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-02 and D-09, run the existing hub-ledger paired test before deletion and reconfirm no package/workflow/production caller exists; remaining archive references document completed historical work. Retire the script and its exclusive paired test together, preserving every archive. Record the script's factual disposition and the complete phase finding/assertion ledger in the summary. Update the active FLOW-09 status with the completed ordinary-helper and production-mode work, clearly preserving the earlier explicit-seam closure. Mark it fully closed only after this plan's remaining verification succeeds. Reconcile any newly type-only paired tests, including exec-result, with Phase 2's exact justified type-only handling in ESLint. Keep all three assertion rules active for runtime tests and rerun the existing offender, benign, helper, and type-only controls. Do not broaden a file pattern or exempt production.</action>
  <verify><automated>npm run test:corresponding && npm run typecheck</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above.</done>
</task>

<task type="auto">
  <name>Activate the shipping production mode with only proven local exceptions</name>
  <files>.fallowrc.json, extensions/pi-claude-marketplace/index.ts, tests/index.test.ts, tests/architecture/fallow-production-mode.test.ts, extensions/pi-claude-marketplace/bridges/hooks/async-rewake/ring-buffer.ts</files>
  <read_first>.fallowrc.json, extensions/pi-claude-marketplace/index.ts, tests/index.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-05, D-06 and D-08, set only production to {deadCode:true,health:false,dupes:false}, keeping includeEntryExports:true and every existing boundary/rule/health/duplication setting. Put the single adjacent unused-export annotation above the manifest-loaded default entry function with its concrete loader reason. Add the single adjacent RingBuffer.read unused-class-member annotation proven in 05-05, citing actual registry stderrBuffer.read and stdoutBuffer.read callers. Its activation was deferred because test-inclusive shipping mode reports it as stale; apply it atomically with production mode. Add no other unproven exceptions. Change the control harness to consume shipping production/rule settings verbatim, overriding only fixture paths; invoke the same no-production-flag dead-code command as npm run fallow so config regressions cannot be hidden. Assert the actual entry exports exactly default, and prove a same-entry stray named export, a test-only helper, and unrelated default/read offenders still fail. The benign companions must pass with the same effective settings. Preserve health/dupes test discovery by checking their unchanged per-analysis scope and actual report evidence.</action>
  <verify><automated>node --test tests/index.test.ts tests/architecture/fallow-production-mode.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above.</done>
</task>

<task type="auto">
  <name>Require an empty complete report and all retained quality evidence</name>
  <files>tests/architecture/unowned-exports-census.test.ts, tests/architecture/gate-targets.ts</files>
  <read_first>tests/architecture/unowned-exports-census.test.ts, tests/architecture/gate-targets.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-01, D-02, D-04, D-08 and D-10, replace the transitional shipping-versus-production blind-spot assertion with equality of the shipping report and explicit production report, both fully clean. Require correct report kind/schema, positive production entry discovery, zero total issues and empty finding collections across all categories. Set the exact complete census to empty only after the reviewed dispositions and controls prove the instrument. Preserve the historical routed-disposition check and the no-test-only-production-surface gate. Reconcile every initial finding and newly exposed transitive finding with a completed plan, retired implementation, private declaration, real production consumer or exact proven annotation. Run the complete stable-snapshot verification below and retain measured production-unit 100% coverage and assertion review evidence before closing FLOW-09.</action>
  <verify><automated>node --test tests/architecture/unowned-exports-census.test.ts tests/architecture/no-test-only-production-surface.test.ts tests/architecture/fallow-production-mode.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-28-01 | Tampering | extensions/pi-claude-marketplace/index.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-28-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-28-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run `npm run test:coverage:direct:all`, `npm run test:coverage:unit`, and `npm run check` on the same final snapshot. Read coverage/unit.lcov for the production extension records and require LF=LH, FNF=FNH, and BRF=BRH in the aggregate; additionally confirm the production file inventory is fully represented according to the established coverage measurement (type-only files remain accounted for). Do not substitute the direct-pair pin or an integration/e2e union. Record command exits and exact production totals. Run all public owner cases and required architecture controls after the last edit; final quality and census must both be green.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The complete report is empty; all controls and required quality gates pass; FLOW-09 has current final evidence.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-28-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
