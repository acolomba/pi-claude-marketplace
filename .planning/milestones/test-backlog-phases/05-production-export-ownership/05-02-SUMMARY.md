---
phase: 05-production-export-ownership
plan: "02"
status: complete
requirements-completed: []
plan_head_before: 7615c965
actuals:
  tasks: 2
---

# Independent marker contract tests

Implementation and focused verification are complete. Stable-wave census reconciliation, full quality gates, aggregate native coverage and parent commits remain required.

# Plan 05-02 assertion ledger

Five lifecycle owner files replace only GENERATED_AGENT_PREFIX imports/interpolations with independently authored identical prefix bytes. No assertions are removed or weakened: state, warnings, full generated bytes, exact filesystem inventory, foreign preservation, rollback and retry remain. Parent-reviewed Phase3 names stay identical. Assertions per file are not proof by themselves; the diff and focused owners must pass.

Provenance integration imports GENERATED_AGENT_MARKER from its real marker owner; every existing assertion remains unchanged.

Architecture current marker exact-byte assertion remains. Legacy marker and basename constant equality become real reader checks over independent current/legacy file bytes, wrong basename and near-match marker refusals. Every reader result is compared whole, and full fixture bytes must remain unchanged. The unrelated state-lock path relationship remains identical.

No production file changes; this prepares later marker privatization. No Fallow finding is closed until its export owner changes in the dependent wave. Focused verification passed: 538/538 cases, including the provenance integration owner.

## Verification

The seven planned owners passed 538/538 cases, with zero failures or skips, including the actual installed pi-subagents provenance parser. `/tmp/test-backlog-owner2-final.log`. Production files did not change in this plan. No assertion was removed from the five lifecycle owners or provenance integration owner. The architecture test now has independent current/legacy reader and refusal controls alongside the unchanged current marker and state-lock contracts.

## Parent integration: retire the final unused facade

The shipping Fallow check found one newly unused export after the provenance test moved: frontmatter.ts's GENERATED_AGENT_MARKER convenience binding. The canonical marker is still used by the writer and other production modules. Parent retired only that redundant re-export and its explanatory comment here, bringing forward the planned 05-03 removal. No owner test imported this binding, and no assertion was removed. Existing full writer bytes and provenance invisibility cases remain. Initial controls failed52/53 solely on this shipping finding; the corresponding initial aggregate run failed its same census case, and ongoing direct/precommit runs were stopped before source repair. Fresh stable-wave validation supersedes those runs.


## Final parent acceptance

Completed in `080d395e` with the five-plan stable wave. The earlier pending integration statements record executor handoff status and are superseded by this acceptance. The final complete census is 85 findings: exactly 26 reviewed initial findings removed, with no additions. The early frontmatter facade retirement accounts for the change from the intermediate 86-finding snapshot. All 53 analyzer/census controls pass. Native unit tests pass 6,230/6,230, with production coverage exactly 63,120/63,120 lines, 1,848/1,848 functions and 9,099/9,099 branches across 225 emitted modules. All 233 direct pairs pass; the two pre-existing shortfalls match their unchanged pins. The complete pre-commit gate passes and independent review reports no findings across all 54 changed paths. See [wave verification](05-WAVE-2-VERIFICATION.md) for logs, limits and assertion preservation. EXPORT-01 and EXPORT-02 remain open for later plans.
