---
phase: 05-production-export-ownership
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "tests/architecture/fallow-production-mode.test.ts"
  - "tests/architecture/fallow-report.ts"
  - "tests/architecture/unowned-exports-census.test.ts"
  - "tests/architecture/gate-targets.ts"
autonomous: true
requirements: ["EXPORT-01","EXPORT-02"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The analyzer rejects an export read only by a test and accepts the same operation when a production caller reads it."
    - "The entry exception leaves unrelated named entry exports, default exports, and read members detectable."
    - "Every current finding category is measured by an exact identity pin with launch and report-shape checks."
  artifacts:
    - path: "tests/architecture/fallow-production-mode.test.ts"
      provides: "Executable public-contract or analyzer control assertions"
    - path: "tests/architecture/unowned-exports-census.test.ts"
      provides: "Executable public-contract or analyzer control assertions"
    - path: "tests/architecture/gate-targets.ts"
      provides: "Executable public-contract or analyzer control assertions"
  key_links:
    - from: "tests/architecture/fallow-production-mode.test.ts"
      to: "node_modules/fallow/bin/fallow"
      via: "Real analyzer invocation or public operation assertion; no test-only production seam"
---

<objective>
Calibrate the complete production finding gate.

Purpose: fulfill EXPORT-01 and EXPORT-02 under D-01 through D-10, with public-contract assertions and no test-only production surface.
Output: the exact source/test artifacts listed above and an evidence-bearing plan summary.
Phase 4 must be complete before taking this baseline; Phase 3 conversion and Phase 4 catalog changes are inputs.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-production-export-ownership/05-CONTEXT.md
@.planning/phases/05-production-export-ownership/05-RESEARCH.md
@.planning/phases/05-production-export-ownership/05-VALIDATION.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md

</context>

<tasks>

For each task, record an assertion ledger in SUMMARY.md: original test/contract, replacement public result/error/state/byte assertion, and focused/direct coverage result. Account for every removed assertion; absence of this mapping blocks completion. Larger coherent owner batches must not rely on one final aggregate percentage.

<task type="tracer">
  <name>Prove production reachability with real Fallow controls</name>
  <precondition>.planning/phases/04-strict-command-arguments/04-VERIFICATION.md exists with passed status; its 19-verb catalog and Phase 3 naming work are present in the checkout.</precondition>
  <files>tests/architecture/fallow-production-mode.test.ts, tests/architecture/fallow-report.ts</files>
  <read_first>package.json, .fallowrc.json, tests/architecture/unowned-exports-census.test.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-05, D-06 and D-08, create isolated temporary projects and invoke the installed Fallow launcher with fixed argument arrays, no shell, --no-cache, JSON output and --fail-on-issues. Start with the repository config cloned into the fixture, overriding only its fixture entry path and production deadCode mode until 05-28 activates the shipping setting. Add paired cases for a helper imported only by its test versus a real production caller, and for a stray named entry export versus the externally loaded default carrying the single adjacent unused-export annotation. Assert exact rule/path/symbol identities, status 1 for offenders and 0 for benign controls, null signal and absent launch error, report kind/schema and positive entry discovery. Capture child stdout/stderr with the proven file-descriptor approach from Phase 1 if nested Node 26 pipes lose output. Clean temporary roots on every path. Extend the same instrument with one isolated unused type, unused file, unused class member, and pair of same-named live exports; repair only each fixture's cause in its benign companion. Detect a malformed/empty report as instrument failure. Add an unrelated read member and unrelated default export control to prove local annotations do not grant global exceptions.</action>
  <verify><automated>node --test tests/architecture/fallow-production-mode.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above.</done>
</task>

<task type="auto">
  <name>Pin every live finding category without weakening drift checks</name>
  <files>tests/architecture/unowned-exports-census.test.ts, tests/architecture/gate-targets.ts</files>
  <read_first>tests/architecture/unowned-exports-census.test.ts, tests/architecture/gate-targets.ts; trace the named symbols and callers with CodeGraph before editing.</read_first>
  <action>Per D-02, D-04, D-08 and D-10, capture the post-Phase-4 complete production report. Extend the existing exact keyed census to normalize category, publishing path, symbol/member, and sorted duplicate locations across all report issue categories; retain the existing value census compatibility if other gates read it. Pin measured identities explicitly in gate-targets.ts, never only a count. Preserve addition/removal/swap failures and the routed historical-disposition record assertion. Replace the assumption that a nonempty finding set proves analyzer operation with the live controls from Task 1. Keep the current shipping-versus-production observation as a measured comparison until the final config transition; do not assert that shipping production must remain disabled permanently. Record every initial identity and any changes from the 111-finding research snapshot in this plan's summary.</action>
  <verify><automated>node tests/architecture/unowned-exports-census.test.ts</automated><fails_when>The command exits nonzero, reports any failed or missing intended case, or emits an unexpected analyzer identity/report shape. For coverage commands, any hit/found mismatch or existing pin regression fails.</fails_when></verify>
  <done>The named source/test changes are complete, the listed automated check passes, and each changed or retired contract has the explicit result/error/state/byte evidence described above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Production operation to injected or filesystem boundary | Export cleanup must preserve validation, ownership checks, and lifecycle state. |
| Source/configuration to analyzer and test runner | A missing caller, malformed report, or relaxed scope must not appear as a successful gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-01-01 | Tampering | tests/architecture/fallow-production-mode.test.ts | medium | mitigate | Preserve the named validation/error/state/byte assertions and trace callers before removing exports or implementations. |
| T-05-01-02 | Repudiation | Finding census and public contract evidence | medium | mitigate | Record exact reviewed identities; parent reconciles only the stable wave snapshot and proves offender/benign controls. |
| T-05-01-03 | Information disclosure | Test filesystem and subprocess boundaries | low | mitigate | Use isolated temporary roots, fixed argv, guaranteed cleanup, and no live credentials or external services. |

No package installation or new external service is required.
</threat_model>

<verification>
Run each task command and `npm run typecheck` after interface changes. For each changed production module, run `npm run test:coverage:direct -- &lt;that exact source path&gt;` using the paths in files_modified; preserve the current pin and repair any regression. Run both architecture files together and record the complete initial finding identity set.
</verification>

<success_criteria>
- The observable truths above hold with the task-specific public assertions.
- All source changes stay inside the declared owner set; each removed finding and migrated assertion is explained.
- The parent accepts the reviewed wave census and confirms unchanged aggregate production coverage before the next dependent wave.
</success_criteria>

<output>
Create `.planning/phases/05-production-export-ownership/05-01-SUMMARY.md`. Include the old finding identity, disposition, production caller or retirement evidence, old-to-public assertion mapping, exact verification commands/results, and any remaining blocker. Do not label a finding closed from a clean count alone.
</output>
