---
phase: 04-strict-command-arguments
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/shared.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/info.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/list.ts
  - extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
  - tests/edge/handlers/marketplace/shared.test.ts
  - tests/edge/handlers/marketplace/info.test.ts
  - tests/edge/handlers/marketplace/list.test.ts
  - tests/edge/handlers/marketplace/update.test.ts
  - docs/prd/pi-claude-marketplace-prd.md
  - README.md
autonomous: true
requirements: [ARGS-02]
must_haves:
  truths:
    - Marketplace info/list/update accept documented --local without narrowing merged reads or introducing config writes.
    - Both named and bulk update retain their existing cache/state refresh and cascade behavior.
  artifacts:
    - path: extensions/pi-claude-marketplace/edge/handlers/marketplace/update.ts
      provides: Accepted local modifier with unchanged merged refresh behavior
  key_links:
    - from: makeMarketplaceInfoHandler / makeMarketplaceListHandler / makeMarketplaceUpdateHandler
      to: extractLocalFlag
      via: Validate and consume the accepted config-target modifier
---

<objective>Apply the user's merged-read/local-write policy to marketplace handlers and documentation.</objective>
<tasks>
<task type="auto">
<name>Accept --local while retaining merged read and refresh behavior</name>
<files>extensions/pi-claude-marketplace/edge/handlers/marketplace/{shared,info,list,update}.ts, tests/edge/handlers/marketplace/{shared,info,list,update}.test.ts, README.md, docs/prd/pi-claude-marketplace-prd.md</files>
<action>Validate/consume --local in the three handlers and document it in usage. Reads remain merged. Current marketplace update writes state/cache only; its plugin cascade's finalizeUpdateRecord explicitly skips config write-back. Therefore accepting --local must not introduce config mutation, pass an unread option through orchestrator types, or filter marketplace selection. Exercise shared-only, local-only and overlapping config declarations using existing owner fixtures; compare complete info/list outputs and named/bulk update effects with and without --local. Check both physical config files remain byte-identical, including when a plugin cascade runs. Preserve scope selection. Explain in user docs that --local selects config writes where a command performs them; these three operations retain merged reads and do not write those config files. Existing add/remove/autoupdate behavior remains covered by its real owner tests.</action>
<verify><automated>node --test tests/edge/handlers/marketplace/*.test.ts</automated></verify>
<done>Flag accepted at any position, usage accurate, merged results and update effects unchanged, and config bytes preserved.</done>
</task>
</tasks>
<verification>Run affected direct-pair gate, complete unit coverage, typecheck, lint, Fallow and formatting. Exact aggregate production coverage stays 100%; preserve thresholds and assertion strength. Complete GSD review and goal verification before closing ARGS-01.</verification>
<output>Write 04-02-SUMMARY.md. Phase review and verification follow Plan 04-03.</output>
