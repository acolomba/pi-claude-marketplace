---
phase: 04-strict-command-arguments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/edge/args.ts
  - extensions/pi-claude-marketplace/edge/args-schema.ts
  - extensions/pi-claude-marketplace/edge/handlers/shared.ts
  - tests/edge/args.test.ts
  - tests/edge/args-schema.test.ts
  - tests/edge/handlers/shared.test.ts
  - tests/edge/register.test.ts
autonomous: true
requirements: [ARGS-01]
must_haves:
  truths:
    - Schema parsing rejects unknown long flags and surplus positional tokens before returning a partial result.
    - Local-flag extraction preserves quoted positional text and never removes a scope value as a boolean flag.
    - Every live public command rejects surplus arguments and unknown flags before doing work.
  artifacts:
    - path: extensions/pi-claude-marketplace/edge/args-schema.ts
      provides: Shared complete argument validation
    - path: tests/edge/register.test.ts
      provides: Real registered-command rejection matrix
  key_links:
    - from: extractLocalFlag
      to: edge/args.ts
      via: Shared tokenizer with original source spans
    - from: tests/edge/register.test.ts
      to: registerClaudePluginCommand
      via: Captured real command handler and strict boundaries
---

<objective>Close the shared arity gap and verify the real command inventory without changing valid argument meaning.</objective>
<tasks>
<task type="auto">
<name>Validate the full schema input and preserve quoted text</name>
<files>extensions/pi-claude-marketplace/edge/args.ts, extensions/pi-claude-marketplace/edge/args-schema.ts, extensions/pi-claude-marketplace/edge/handlers/shared.ts, tests/edge/args.test.ts, tests/edge/args-schema.test.ts, tests/edge/handlers/shared.test.ts</files>
<action>Add explicit unknown-long-flag and maximum positional count validation to parseCommandArgs. Send a specific Too many arguments. error instead of the missing-argument usage sentinel. Keep current required/optional/scope behavior. Existing owner tests deliberately pin ignored surplus input: replace that assertion with the corrected complete rejection, retaining benign required/optional controls. Share the existing lexical implementation between parseArgs and extractLocalFlag: preserve original token spans so removing standalone --local never changes spaces or flag-like text inside a quoted positional. Do not stringify and reparse decoded token values. Preserve --scope's value token even when it spells --local. A tokenizer export is justified only by this real production consumer, in the existing args module. Preserve the documented no-escape grammar; do not add a second independent tokenizer. Keep parseArgs's downstream boolean-token contract for existing plugin handlers.</action>
<verify><automated>node --test tests/edge/args.test.ts tests/edge/args-schema.test.ts tests/edge/handlers/shared.test.ts</automated></verify>
<done>Required/optional/zero-argument schemas reject excess input and unknown flags; quoted benign values survive extraction byte-for-byte in the residual spelling.</done>
</task>
<task type="auto">
<name>Exercise every public command's rejection boundary</name>
<files>tests/edge/register.test.ts</files>
<action>Enumerate the live router verbs and aliases, not the backlog's old count. Use the existing registerCommandWithCache/createHermeticScope harness to call the actual registered command. For each valid command shape, plant an unknown long flag and one surplus positional; require the complete usage notification and strict boundary verification. Use memory Git operations, the existing network trap, and local fixture roots so a regression cannot touch real user state or network. Assert the case inventory covers router commands, including bootstrap and marketplace aliases. Reuse existing positive owner cases and add a benign matched control where a rejection case could otherwise pass because its invocation was already invalid. If a real verb still drops extras outside parseCommandArgs, fix its owning parser in the same plan and document the newly discovered file.</action>
<verify><automated>node --test tests/edge/register.test.ts</automated></verify>
<done>The real dispatcher rejects bad input across the current inventory before orchestrator side effects.</done>
</task>
</tasks>
<verification>Typecheck, lint, focused owners and direct pair measurements must pass. Do not weaken full-result assertions or coverage gates.</verification>
<output>Write 04-01-SUMMARY.md.</output>
