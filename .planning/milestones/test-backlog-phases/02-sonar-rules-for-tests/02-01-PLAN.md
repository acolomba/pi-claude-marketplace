---
phase: 02-sonar-rules-for-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eslint.config.js
  - tests/architecture/sonar-test-rules.test.ts
  - tests/domain/device-flow-contract.ts
  - tests/platform/credential-ops-contract.ts
  - tests/orchestrators/plugin/shared.test.ts
  - tests/orchestrators/plugin/enable-disable.test.ts
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements: [SONAR-01, SONAR-02]
must_haves:
  truths:
    - Empty runtime tests, missing assertions, and trivial assertions are rejected.
    - Seven legitimate type-only owners keep compiler proofs without dummy runtime cases.
    - Strict mocks and dynamic contract assertions remain effective.
  artifacts:
    - path: tests/architecture/sonar-test-rules.test.ts
      provides: Offender and benign controls using the real ESLint configuration
    - path: eslint.config.js
      provides: Deliberate test assertion policy
  key_links:
    - from: tests/architecture/sonar-test-rules.test.ts
      to: eslint.config.js
      via: ESLint effective config and lintText
---
<objective>Adopt three Sonar assertion rules with measured exceptions and controls.</objective>
<tasks>
<task type="auto">
<name>Adopt assertion rules and preserve meaningful assertions</name>
<files>eslint.config.js, tests/domain/device-flow-contract.ts, tests/platform/credential-ops-contract.ts, tests/orchestrators/plugin/shared.test.ts, tests/orchestrators/plugin/enable-disable.test.ts</files>
<action>Enable the three named rules for tests. Use a precisely named seven-file override for no-empty-test-file on type-only owners. Inspect all six assertion flags and preserve strong-mock and dynamic contract assertions with narrow documented analyzer exceptions where necessary. Strengthen the real standalone overload call with complete outcome/notification expectations. No dummy assertions, helper renames merely to satisfy the analyzer, or broad test-tree suppressions.</action>
<verify><automated>npm run lint</automated></verify>
<done>All existing tests comply without reduced assertion strength or production exclusions.</done>
</task>
<task type="auto">
<name>Prove effective rules and record remaining cluster dispositions</name>
<files>tests/architecture/sonar-test-rules.test.ts, .planning/codebase/CONVENTIONS.md</files>
<action>Use the actual ESLint config through its API and lint planted violating/benign snippets. Check each rule is active for runtime tests and that disabling it breaks its offender control. Validate exact type-owner exemptions separately from runtime neighbors. Measure full recommended-rule scan (/tmp/test-backlog-sonar-all.json if available), inspect clusters, and document why each is adopted, already enforced, or deliberately outside this policy. Do not blindly enable fixture-security or compile-time-idiom rules. Record unresolved substantive defects for current user discussion, not silent closure.</action>
<verify><automated>node --test tests/architecture/sonar-test-rules.test.ts</automated></verify>
<done>Discriminating controls pass and every SWTEST-01 cluster has current evidence.</done>
</task>
</tasks>
<verification>Focused controls, affected owner tests/direct-pair gate, typecheck and lint pass.</verification>
<output>Write 02-01-SUMMARY.md, 02-REVIEW.md, and 02-VERIFICATION.md after checks.</output>
