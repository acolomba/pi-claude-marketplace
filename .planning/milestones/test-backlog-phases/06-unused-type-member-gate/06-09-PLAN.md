---
phase: 06-unused-type-member-gate
plan: "09"
type: execute
wave: 7
depends_on: ["06-07"]
files_modified:
  - extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
  - extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
  - extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts
  - extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts
  - extensions/pi-claude-marketplace/bridges/hooks/stage.ts
  - extensions/pi-claude-marketplace/index.ts
  - tests/bridges/hooks/event-router.test.ts
  - tests/architecture/hooks-lifecycle.test.ts
  - tests/integration/hooks-additionalcontext-end-to-end.test.ts
  - tests/integration/hooks-cross-scope-reconcile.test.ts
  - tests/integration/hooks-dispatch-end-to-end.test.ts
  - tests/integration/hooks-spawn-end-to-end.test.ts
  - .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 145000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The 15 AsyncRewakeEntry rows leave the population because the published name declares no members of its own, and the one value it annotates still reaches runtime.registerChild unchanged."
    - "The register-bridge options bag has exactly one declaration whose cwd and executor are read through it, and the never-read ctx field is gone from the bag and from every call site including the production factory's placeholder cast."
    - "The bridges/hooks owner group reports zero unread members, or every remaining row carries a recorded reason a reader can check against source rather than an exemption."
    - "No row is cleared by adding a read, a test or a contract entry; each is cleared by removing or aliasing the declaration, and the count is measured by the real CLI (D-02, D-05, D-09)."
    - "npm run check is exit 0 and native aggregate production unit coverage is still 100 percent with no module below 100 percent (D-07)."
    - "audit --check reconciles against a freshly recomputed digest with zero stale-source, stale-record, missing, duplicate, incomplete or invalid rows."
  artifacts:
    - path: extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts
      provides: "AsyncRewakeEntry published as an alias of the runtime row, declaring no members of its own"
    - path: extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
      provides: "One named register-bridge options declaration, no dead ctx field, no dead HydratedScope.state field"
    - path: extensions/pi-claude-marketplace/index.ts
      provides: "Factory call site without the placeholder ExtensionContext cast the bag no longer requires"
    - path: .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
      provides: "Record regenerated against a fresh digest so --check reports no stale-source after the repairs"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts"
      to: "extensions/pi-claude-marketplace/bridges/hooks/runtime.ts"
      via: "The published alias resolves every member read to the runtime row's own declaration, which already carries the witnesses."
    - from: "extensions/pi-claude-marketplace/bridges/hooks/event-router.ts"
      to: "extensions/pi-claude-marketplace/index.ts"
      via: "The single options declaration drops ctx, so the factory's placeholder cast is removed with it rather than left orphaned."
    - from: "extensions/pi-claude-marketplace/bridges/hooks/stage.ts"
      to: ".planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md"
      via: "audit --inventory recomputes the source digest after the last edit so the record stays self-consistent."
---
<objective>
Repair the 26 unread type members the live triage assigns to the `bridges/hooks`
owner, by source repair only: a duplicate declaration becomes an alias, four
mirrored options bags become one declaration, and four genuinely dead slots are
removed.

Purpose: 06-08 cannot activate the mandatory gate while any owner group carries
an unexplained finding, and the honest way to drain this group is to fix the
source, never to widen a proof or add a reader (D-05, D-09).
Output: five `bridges/hooks` source files, the extension factory, the hooks test
call sites, and a regenerated `06-LIVE-TRIAGE.md`.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-06-SUMMARY.md

`06-LIVE-TRIAGE.md` is deliberately NOT referenced here: it is 500 KB over 4,908
lines and would consume most of an executor's context. Read only this plan's own
slice of it, with
`sed -n '/^### bridges\/hooks$/,/^### bridges\/skills$/p' .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md`
under `## Unresolved members` (the first of the two `### bridges/hooks`
subsections, around line 67). That subsection is the authoritative row list for
this plan and it is 26 rows.
</context>

<!-- The acceptance greps below run over the analyzer CLI's stderr finding lines
     (`status: path:line:col owner.key`), never over this plan or over project
     source, so a path literal appearing in an action body cannot satisfy or
     invalidate them. -->
<!-- planner-discipline-allow: bridges/hooks -->
<!-- planner-discipline-allow: async-rewake/registry.ts -->
<!-- planner-discipline-allow: bridges/hooks/event-router.ts -->

<tasks>

<task type="tracer">
  <name>Publish AsyncRewakeEntry as an alias of the runtime row it duplicates</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/async-rewake/registry.ts</files>
  <precondition>`npm run check` is exit 0 on the inherited tree before any edit; a red baseline is a blocking finding to record and repair first, not to work around (06-06 found 06-05 had left one).</precondition>
  <behavior>
    Before: the analyzer reports 15 unread members on `AsyncRewakeEntry`
    (`async-rewake/registry.ts:129:3` through `143:3`). After: it reports zero
    for that file, the count for `bridges/hooks/runtime.ts` does not rise, and no
    other owner's count moves.
  </behavior>
  <action>
    `AsyncRewakeEntry` (registry.ts:128) declares the same fifteen members as
    `HooksRuntimeChildEntry` (runtime.ts:11). The one value annotated with it
    (registry.ts:293) is handed straight to `runtime.registerChild`, whose
    parameter is the runtime row, so every read resolves to the runtime
    declaration. Per D-03 that is correct: structural compatibility is not a
    read. Replace the fifteen-member interface body with a single alias of
    `HooksRuntimeChildEntry`, so the published name declares no members of its
    own and stops being a candidate at all.
    Add `HooksRuntimeChildEntry` to the existing type-only import of
    `HooksRuntime` from `../runtime.ts`; that edge already exists, and
    `runtime.ts` imports only the `./async-rewake/ring-buffer.ts` leaf, so no
    cycle forms and neither fallow cycle run changes.
    Keep the `fallow-ignore-next-line unused-type` marker and its WR-01
    justification on the alias, and rewrite the doc comment to say what the name
    now is. Do not delete the marker on suspicion: if fallow reports it stale,
    stop and record that as a separate finding about WR-01, because a stale
    suppression is evidence the published name is dead and that is an owner
    decision, not a cleanup.
    Note the one real type change: the alias gives the literal at line 293 a
    `child` slot typed `ChildLike` instead of `ChildProcess`. The value assigned
    is a real `ChildProcess`, and the rest of `spawnAndRegister` reads the local
    `child` binding rather than `asyncEntry.child`, so nothing downstream loses a
    `ChildProcess` member; prove it with `npm run typecheck` rather than by
    reasoning. Remove only the imports this edit itself orphaned, per CLAUDE.md.
    Report the measured before and after counts for this file in the SUMMARY.
  </action>
  <fails_when>Fails if the alias is achieved by deleting the published name instead of aliasing it, if any read is redirected by adding a reader or a contract entry rather than by removing the duplicate declaration, or if the 15 rows disappear from the report while the runtime row's own count silently rises.</fails_when>
  <verify><automated>npm run typecheck &amp;&amp; node --test tests/bridges/hooks/async-rewake/registry.test.ts tests/bridges/hooks/runtime.test.ts tests/architecture/hooks-async-rewake.test.ts &amp;&amp; test "$(node scripts/check-unused-type-members.mjs 2>&amp;1 >/dev/null | grep -c 'async-rewake/registry.ts')" = 0</automated></verify>
  <done>The published name is an alias, the analyzer reports zero unread members for `async-rewake/registry.ts`, and the hooks runtime and async-rewake suites pass unchanged.</done>
</task>

<task type="auto">
  <name>Collapse the mirrored hooks options bags and drop two dead slots</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-router.ts, extensions/pi-claude-marketplace/index.ts, tests/bridges/hooks/event-router.test.ts, tests/architecture/hooks-lifecycle.test.ts, tests/integration/hooks-additionalcontext-end-to-end.test.ts, tests/integration/hooks-cross-scope-reconcile.test.ts, tests/integration/hooks-dispatch-end-to-end.test.ts, tests/integration/hooks-spawn-end-to-end.test.ts</files>
  <reversibility rating="costly">Removing a field from the exported `HooksHydration.registerHooksBridge` signature rewrites roughly thirty call sites across production and tests; reverting means re-threading all of them.</reversibility>
  <behavior>
    Before: the analyzer reports 7 unread members in `event-router.ts`
    (`411:3`, `457:13`, `457:36`, `457:49`, `497:5`, `807:11`, `1002:15`).
    After: it reports zero for that file, and `hydrateProjectScopeForCwd`,
    `registerHooksBridge` and the whole hooks dispatch path behave identically.
  </behavior>
  <action>
    Four inline object types spell the same options bag: the
    `HooksHydration.registerHooksBridge` delegate (457), `hydrateCacheFromDisk`
    (497), `registerHooksBridgeWith` (807) and the object literal method
    `createHooksHydration` returns (1002). Reads of `cwd` (507, 526, 148) and
    `executor` (899, 929-972) land on the implementation's own declaration, so
    the other spellings are mirrors. Declare the bag once as a named exported
    interface in `event-router.ts` and point all four sites at it, so `cwd` and
    `executor` have exactly one declaration and it is read.
    Then remove `ctx` from that declaration. `opts.ctx` is read at no site in the
    tree: `index.ts`'s own comment already records that "the bridge's hydrate
    path does not consume `opts.ctx` (only `opts.cwd`); the field is structurally
    required by the signature but functionally unused at factory time." Removing
    it deletes the `{} as unknown as ExtensionContext` placeholder at the sole
    production call site along with the `ctx:` key, and corrects the step-2
    paragraph of that comment block, which stops being true. Update every test
    call site that passes `ctx:` into `registerHooksBridge`; the integration
    suites keep their own `placeholderCtx` bindings where they also pass one to a
    Pi handler, so drop only the `ctx:` key from the options literal there.
    Separately, drop `state` from the module-private `HydratedScope` (411) and
    from the literal at 527. Its only consumer destructures `{ loc }` at 837 and
    never touches `state`; the doc comment at 405-408 claims registration needs
    both and is stale, so rewrite it to match. Record in the SUMMARY that this
    row's recorded disposition ("delegate parameter shape", "keep the mirror and
    accept the finding") is the cluster note applied to the whole file and is
    mis-assigned for this row -- `HydratedScope` is a private return shape, not a
    delegate parameter -- and that the evidence, not the note, was followed. The
    same correction applies to `hydrateCacheFromDisk.opts.ctx` (497), which is a
    concrete local function rather than a delegate. Per D-09 a recorded
    disposition is evidence to check, not an instruction to obey; if inspection
    contradicts one, say so and repair honestly.
    Behaviour must not change. These are a type rename, a dead parameter field
    and a dead return field; if any of the three turns out to alter a runtime
    path, stop, leave that row, and record why (D-06).
  </action>
  <fails_when>Fails if `ctx` is removed from the bag while the placeholder cast or the stale comment is left behind, if a test call site is made to compile by widening the options type rather than by dropping the key, or if any row clears because a test was added to observe it (D-02).</fails_when>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node --test tests/bridges/hooks/event-router.test.ts tests/bridges/hooks/index.test.ts tests/architecture/hooks-lifecycle.test.ts tests/index.test.ts &amp;&amp; npm run test:integration &amp;&amp; test "$(node scripts/check-unused-type-members.mjs 2>&amp;1 >/dev/null | grep -c 'bridges/hooks/event-router.ts')" = 0</automated></verify>
  <done>One named options declaration serves all four sites, `ctx` and `HydratedScope.state` are gone from source and from every call site, the analyzer reports zero unread members for `event-router.ts`, and the hooks unit and integration suites pass.</done>
</task>

<task type="auto">
  <name>Clear the last four rows, reconcile the record and measure the delta</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/event-adapters.ts, extensions/pi-claude-marketplace/bridges/hooks/exec-timer.ts, extensions/pi-claude-marketplace/bridges/hooks/stage.ts, .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md</files>
  <behavior>
    After this task the analyzer reports zero unread members for the whole
    `bridges/hooks` owner group -- 26 of the inherited 138 cleared, leaving 112
    -- or each row still standing carries a recorded reason. `audit --check`
    reports that measured number with zero stale-source, stale-record, missing,
    duplicate, incomplete or invalid rows.
  </behavior>
  <action>
    Three independent repairs, then the measurement.
    First, `applyToolResultPatch` (event-adapters.ts:126-139) mints two
    single-member cast literals whose only syntax writes to them, and a write is
    not a read (D-04). The installed peer declares `ToolResultEventBase.content`
    and `.isError` as ordinary mutable slots, so write through the event's own
    slots and delete both casts; CR-01's whitelist is enforced by the two
    guarded `if` statements, not by the cast, so the security property is
    untouched. If the union write does not type-check, fall back to one named
    writable view used by both statements -- a mapped type declares no members of
    its own -- and record which form was used and why.
    Second, drop `pid` from `ChildLike` (exec-timer.ts:50). Nothing reads it: the
    ladder guards on exit state, the entry carries its own top-level `pid`, and
    both `satisfies ChildLike` literals in `tests/bridges/hooks/runtime.test.ts`
    spell only `kill`. This is the one row in the slice where removal is a real
    owner judgment rather than bookkeeping: the slot documents the adapter's
    correspondence to `ChildProcess`, and the paired spy carries an explicit
    comment about keeping a field for exactly that reason. Remove it if the
    correspondence survives; if it does not, leave the row and record the reason
    against the evidence, which is a better artifact than a laundered clean.
    Third, drop `written: true` from `WriteHookConfigResult` (stage.ts:227) and
    from the literal at 256. No production caller keeps the result and no test
    reads the field. Before deleting, check `tests/bridges/hooks/stage.test.ts`
    and `tests/bridges/hooks/index.test.ts` for a deep comparison of the whole
    result object: a deep-equal would be a whole-object read and would
    contradict the recorded `unread` status, so if one exists, stop and record
    the contradiction rather than deleting the field.
    Then reconcile and measure. Contract drift is the intended coupling (D-05):
    the only validated contract entry in any file this plan touches is
    `event-adapters.ts:87:37` with its filter at `87:11`, and every edit here is
    below line 87, so that coordinate must not move -- confirm it rather than
    assume it, and if any repair makes a contracted member readable, retire that
    entry by name, never widen a proof to keep it. Regenerate the record with
    `node scripts/check-unused-type-members.audit.mjs --inventory`, which carries
    every note whose row did not move, then run `--check`: it exits 1 by design
    while other owners' rows remain, so read its problem breakdown, not its
    status. Record the exact before and after counts per owner, the fresh digest
    and revision, and the transfer counters; adding or editing tests moves those
    counters because `tests/**/*.ts` is analysed input, which is not drift.
    Coverage is this task's obligation, not the orchestrator's (D-07). Every
    earlier task edits `extensions/`, and an aggregate run is only valid for the
    tree it measured, so run it once here on the final snapshot: inspect the
    unmodified native `coverage/unit.lcov` and require the full 100 percent
    aggregate production result with no module below 100 percent, against the
    inherited 1,834 functions and 9,050 branches, and keep the direct pin floors
    unchanged. Investigate any shortfall rather than adjusting a pin, an
    exclusion or a threshold.
  </action>
  <fails_when>Fails if a row is cleared by a contract entry or a new test instead of a source repair, if the `event-adapters.ts:87` contract coordinate moves without being retired by name, if the record is left reporting stale-source, if coverage evidence predates the last source edit, or if the cleared-row count is asserted rather than read out of the real CLI.</fails_when>
  <verify><automated>npm run check &amp;&amp; test "$(node scripts/check-unused-type-members.mjs 2>&amp;1 >/dev/null | grep -c 'bridges/hooks')" = 0 &amp;&amp; npm run test:coverage:unit &amp;&amp; npm run test:coverage:direct:all</automated></verify>
  <done>The `bridges/hooks` owner group reports zero unread members or a recorded reason per remaining row, `npm run check` is exit 0, aggregate production unit coverage is still 100 percent with unchanged direct pins, and the regenerated record reconciles against a fresh digest.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record the exact measured count for the files it touches, read
out of the real CLI's stderr and not out of the record. On completion record
exact commands, statuses and counts in this plan's SUMMARY using the IDs below.
A cleared row must be traceable to a removed or aliased declaration; no row may
be cleared by a contract entry, a new test or a widened proof (D-02, D-05).
Production coverage scripts, exclusions, direct pins and assertion strength are
preserved; the aggregate run happens once, on the final snapshot, because an
earlier task's edit invalidates any aggregate measured before it.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-09-T1 | 15 rows on `async-rewake/registry.ts` before, 0 after; the runtime row's own count does not rise; the async-rewake, runtime and hooks-architecture suites pass unmodified; typecheck proves the `ChildProcess` to `ChildLike` slot change costs nothing downstream. | Focused verify command above; type-only edit, so no emitted JS and no production coverage number can move. Record the direct-pin result for the changed file. |
| 06-09-T2 | 7 rows on `event-router.ts` before, 0 after; `registerHooksBridge`, `hydrateProjectScopeForCwd` and the dispatch path behave identically across the unit and integration suites; the placeholder cast and both stale comments are gone with the fields they described. | Focused verify command above; two one-property literal changes and one removed const. Record covered positive, negative and failure paths and the direct-pin result for `event-router.ts` and `index.ts`. |
| 06-09-T3 | 4 rows before, 0 after across three files; the `event-adapters.ts:87:37` contract coordinate is unmoved and the engine still accepts 81 entries; the record reconciles against a fresh digest with zero stale, missing, duplicate, incomplete or invalid rows; the owner-group total is read out of the CLI as 26 cleared. | Full aggregate on the final snapshot: 100 percent native production unit coverage, no module below 100 percent, unchanged direct pin floors, recorded with the exact revision and digest. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source and contract text is parsed as data and must never execute. |
| Evidence to clean verdict | A repair must clear a row by removing the declaration, never by manufacturing an observation. |
| Hook patch to Pi event | A hook-supplied `tool_result` patch crosses into a live Pi event object; only the whitelisted slots may be written. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch and configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-09-01 | Tampering | Source repair that launders a finding | high | mitigate | Every cleared row is cleared by removing or aliasing a declaration; each task's `fails_when` rejects a clear achieved by a contract entry, a new reader or a new test, and the count is read out of the real CLI. |
| T-06-09-02 | Tampering | CR-01 `tool_result` mutation whitelist | high | mitigate | The two guarded `if` statements that whitelist `content` and `isError` are preserved verbatim; only the cast around the write is removed, and the adapter suite is re-run to prove the discriminator and `toolName` still cannot be rewritten. |
| T-06-09-03 | Tampering | Validated contract coordinates | medium | mitigate | Confirm the single in-scope entry at `event-adapters.ts:87:37` still resolves after the edits; a member made readable retires its entry by name rather than widening the proof. |
| T-06-09-04 | Denial of service | Hooks child lifecycle after the `ChildLike` narrowing | medium | mitigate | The SIGTERM-to-SIGKILL ladder guards on exit state, not on `pid`; the exec-timer and runtime suites are re-run, and the row is left standing if the `ChildProcess` correspondence proves load-bearing. |
| T-06-09-05 | Repudiation | Gate acceptance after a source repair | medium | mitigate | Deterministic member-level counts before and after each task, a record regenerated against a freshly recomputed digest, and the per-task assertion and coverage ledger above. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks in the foreground with its exit status
captured. The analyzer costs roughly 80 seconds and 2 GiB per run; that is the
price of measuring the record against a fresh analysis rather than against
itself. Measure this plan's delta per owner group, not against the global total,
so a sibling repair plan landing in the same wave cannot invalidate it. The
regenerated `06-LIVE-TRIAGE.md` is shared with the other repair plans in this
wave and with 06-08; the last plan to land is authoritative, and `--check` is
what proves it. Run the final full check and the aggregate and direct coverage
gates on one stable snapshot taken after the last source edit. Commits are plain
Conventional Commits with no milestone or phase identifiers, and
`SKIP=trufflehog pre-commit run --files <changed files>` passes before each one.
</verification>

<success_criteria>
- All 26 `bridges/hooks` rows are resolved by source repair, taking the live population from 138 to a measured 112, or every row still standing carries a recorded reason a reader can check against source.
- The measured post-plan count for this owner group is read out of the real CLI and reported, not assumed.
- No production behaviour changes: every repair is a type-level change, a dead parameter field or a dead return field.
- `npm run check` is exit 0, native aggregate production unit coverage is 100 percent with no module below 100 percent, and every direct pin floor is unchanged.
- No contract entry, suppression, threshold, exclusion or assertion is weakened; a contracted member made readable retires its entry by name.
- The regenerated record reconciles against a fresh digest with zero stale-source, stale-record, missing, duplicate, incomplete or invalid rows.
- Every task's `fails_when` counterexample is exercised and its assertion and coverage ledger row is complete.
</success_criteria>

<output>
Create `.planning/phases/06-unused-type-member-gate/06-09-SUMMARY.md` when done.
Record the per-file before and after counts, the exact verification results, the
fresh digest and revision, any recorded disposition this plan judged
mis-assigned, and any row deliberately left standing with its reason.
</output>
