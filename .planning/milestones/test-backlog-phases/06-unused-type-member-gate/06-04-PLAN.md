---
phase: 06-unused-type-member-gate
plan: "04"
type: execute
wave: 3
depends_on: ["06-02","06-03"]
files_modified:
  - scripts/check-unused-type-members.operations.mjs
  - scripts/check-unused-type-members.analysis.mjs
  - tests/scripts/check-unused-type-members.operations.test.ts
  - scripts/check-unused-type-members.flow.mjs
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Whole-object observations identify the actual resolved operation and production-derived operands."
    - "Serialization, shallow copies and deep test comparisons follow their real eligible-key semantics."
    - "Changing a summarized wrapper into a non-reader invalidates the witness."
  artifacts:
    - path: scripts/check-unused-type-members.operations.mjs
      provides: "Whole-object operations and validated wrapper summaries implementation/evidence"
    - path: scripts/check-unused-type-members.analysis.mjs
      provides: "Whole-object operations and validated wrapper summaries implementation/evidence"
    - path: scripts/check-unused-type-members.flow.mjs
      provides: "Whole-object operations and validated wrapper summaries implementation/evidence"
  key_links:
    - from: "scripts/check-unused-type-members.operations.mjs"
      to: "tests/scripts/check-unused-type-members.operations.test.ts"
      via: "Independently authored controls execute the real operation and assert exact observations and failures."
---
<objective>
Whole-object operations and validated wrapper summaries.

Purpose: Whole-object observations identify the actual resolved operation and production-derived operands.
Output: scripts/check-unused-type-members.operations.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.operations.test.ts, scripts/check-unused-type-members.flow.mjs.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-02-SUMMARY.md
@.planning/phases/06-unused-type-member-gate/06-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Trace a produced record through a serializer wrapper to a CLI witness</name>
  <files>scripts/check-unused-type-members.operations.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.operations.test.ts</files>
  <behavior>
    A produced nested record passed through an unknown-typed wrapper to built-in JSON.stringify is observed; a same-named local function and a non-reading wrapper are not.
  </behavior>
  <action>
    Per D-03, D-04 and D-05, derive or validate operation summaries by resolved compiler symbol, operand positions, nested-path policy and actual body. Connect the flow engine and contract engine into the analysis pipeline. Start with a source record passed through a local payload: unknown wrapper to the actual built-in JSON.stringify and prove the final CLI witness retains its originating production declaration. Local wrappers such as serializeWithTruncation and atomicWriteJson must preserve caller origins through the serialized argument, even where the declared parameter is unknown. A wrapper body changed to discard/identity-return its input must invalidate its summary. Never accept a summary from a function-name match. Keep operation summaries in operations.mjs rather than scattering sink special cases across the inventory.
  </action>
  <fails_when>Fails if any function named stringify is trusted, an unknown parameter erases its actual caller origin, or stale summary evidence remains valid.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.operations.test.ts tests/scripts/check-unused-type-members.contracts.test.ts</automated></verify>
  <done>The serializer path passes end to end while shadowed, disconnected and changed-body controls fail precisely.</done>
</task>

<task type="auto" tdd="true">
  <name>Implement own-property and shallow-copy observation semantics</name>
  <files>scripts/check-unused-type-members.operations.mjs, scripts/check-unused-type-members.flow.mjs, tests/scripts/check-unused-type-members.operations.test.ts</files>
  <behavior>
    Spread/rest and Object values/entries/assign sources observe only proven own enumerable source values; rest excludes separately extracted keys and copies stay shallow.
  </behavior>
  <action>
    Per D-04 and D-06, model object spread/rest, Object.values/entries and Object.assign with symbol identity and concrete argument roles. Copy operations read their eligible source values even if the copied result is unused; preserve that observed-read fact when a later property overwrites the copied value. Named rest exclusions are observed by the named destructuring operation, while the rest copy omits them. Assign target identity is not equivalent to reading all its values. Distinguish shallow member observation from recursively reading nested child members. Prove own/enumerable eligibility from actual local constructions or validated contract evidence; an interface alone does not prove it. Class/accessor/proxy/unknown-object cases receive targeted unsupported diagnostics where no independent witness exists. Key-only enumeration, prototype properties and unrelated same-shaped objects must not produce bulk value credit.
  </action>
  <fails_when>Fails if a spread recursively credits nested fields, Object.assign targets are credited as sources, key enumeration reads every value, or unknown enumerability is silently accepted.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.operations.test.ts tests/scripts/check-unused-type-members.flow.test.ts</automated></verify>
  <done>Copy/key/own-property controls match operation semantics and preserve precise origins.</done>
</task>

<task type="auto" tdd="true">
  <name>Distinguish deep test observations and serialization options</name>
  <files>scripts/check-unused-type-members.operations.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.operations.test.ts</files>
  <behavior>
    Node deep comparisons of actual production results supply test-only observations; identity comparisons and constructed typed expected literals do not. JSON replacers/toJSON/symbol keys are respected.
  </behavior>
  <action>
    Per D-02, D-04 and D-08, model resolved node:assert strict deep comparison operations with their actual recursive/property semantics and actual production-derived operand lineage. Preserve independent expected fixtures; a freshly constructed typed expected literal cannot itself prove that production output was consumed. Identity assertions do not bulk-read members. Support JSON.stringify's eligible string keys, omitted symbol/function/undefined values, replacer arrays/functions and toJSON transformations only where actual value-flow and key semantics can be proven; emit targeted unsupported diagnostics for candidate paths in unmodeled dynamic options rather than granting recursive credit. Include local shadowed deepStrictEqual, wrapper aliases, cyclic/relevant unknown flows, returned nested arrays, a genuine production metadata read only in tests, declaration/type-enumeration non-witnesses and mutations of summarized wrapper bodies. Verify the complete fixture suite still explains accepted results and fails all relevant unknowns.
  </action>
  <fails_when>Fails if an expected literal counts as production consumption, identity assertions become deep reads, JSON symbol/replacer semantics are ignored, or an unknown sink passes.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.test.ts tests/scripts/check-unused-type-members.model.test.ts tests/scripts/check-unused-type-members.flow.test.ts tests/scripts/check-unused-type-members.contracts.test.ts tests/scripts/check-unused-type-members.operations.test.ts</automated></verify>
  <done>Deep-operation controls preserve independently specified assertions and classify production/test observations correctly.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands/results and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not generate expected observations from the analyzer's declaration enumeration. These tasks add analyzer/configuration/control behavior without production .ts edits. Record focused analyzer case/branch coverage and assertion strength; preserve all production coverage scripts, exclusions and direct pins. The orchestrator runs the native aggregate unit and direct gates once per stable integration wave, not once per minor task.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-04-T1 | A produced nested record passed through an unknown-typed wrapper to built-in JSON.stringify is observed; a same-named local function and a non-reading wrapper are not. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-04-T2 | Spread/rest and Object values/entries/assign sources observe only proven own enumerable source values; rest excludes separately extracted keys and copies stay shallow. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-04-T3 | Node deep comparisons of actual production results supply test-only observations; identity comparisons and constructed typed expected literals do not. JSON replacers/toJSON/symbol keys are respected. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source/configuration/overlay text is parsed as data and must never execute. |
| Evidence to clean verdict | Symbol, flow and contract records must not manufacture observations or hide unexplained members. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch/configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-04-01 | Tampering | Candidate provenance and contract evidence | high | mitigate | Require exact declaration identity, directed origin/path witnesses and sibling/stale-evidence controls in the task suite. |
| T-06-04-02 | Elevation of privilege | Compiler inputs and subprocess controls | high | mitigate | Parse analyzed modules without importing/executing them; use argv arrays and contained temporary overlays, then verify cleanup. |
| T-06-04-03 | Denial of service | Recursive graph and malformed compiler input | medium | mitigate | Cycle-aware bounded worklists and explicit resource/setup diagnostics; a cutoff cannot return clean. |
| T-06-04-04 | Repudiation | Gate acceptance | medium | mitigate | Deterministic member-level reports, task assertion/coverage ledger and exact positive/negative process results. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run typecheck, lint and format checks on the completed wave; the orchestrator reuses one stable wave snapshot for unchanged native aggregate production unit coverage and direct pins. Tests must assert exact independently specified category, declaration/member and observation semantics, not successful enumeration. Track unsupported analysis explicitly. Live repository acceptance is deferred only to the scheduled post-Phase-5 reconciliation in 06-06; no intermediate fixture success claims the current production tree is clean.
</verification>

<success_criteria>
- Whole-object observations identify the actual resolved operation and production-derived operands.
- Serialization, shallow copies and deep test comparisons follow their real eligible-key semantics.
- Changing a summarized wrapper into a non-reader invalidates the witness.
- Every task's fails_when counterexample is exercised, and its assertion/coverage ledger is complete.
- No production coverage threshold, direct pin or assertion contract is weakened.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-04-SUMMARY.md when done. Record implementation evidence, exact verification results, observed limits and any newly required bounded live-remediation plan IDs.
</output>
