---
phase: 06-unused-type-member-gate
plan: "08"
type: execute
wave: 9
depends_on: ["06-15"]
files_modified:
  - package.json
  - .pre-commit-config.yaml
  - tests/architecture/unused-type-member-gate.test.ts
  - docs/unused-type-member-gate.md
  - .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Normal npm check and pre-commit run the same whole-project member gate and its negative controls."
    - "Removing a reader in any production/test file triggers the gate; configuration and contract changes cannot bypass it."
    - "Final native aggregate production unit coverage remains 100 percent and all direct-pair pins/assertions remain intact."
  artifacts:
    - path: package.json
      provides: "Enable the zero-unexplained gate in local and CI checks implementation/evidence"
    - path: .pre-commit-config.yaml
      provides: "Enable the zero-unexplained gate in local and CI checks implementation/evidence"
  key_links:
    - from: "package.json"
      to: "tests/architecture/unused-type-member-gate.test.ts"
      via: "Independently authored controls execute the real operation and assert exact observations and failures."
---
<objective>
Enable the zero-unexplained gate in local and CI checks.

Purpose: Normal npm check and pre-commit run the same whole-project member gate and its negative controls.
Output: package.json, .pre-commit-config.yaml, tests/architecture/unused-type-member-gate.test.ts, docs/unused-type-member-gate.md, .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-07-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Wire the verified detector into the normal quality path</name>
  <files>package.json, .pre-commit-config.yaml, tests/architecture/unused-type-member-gate.test.ts</files>
  <behavior>
    The package check path invokes both the detector and its negative runner; pre-commit triggers for reader, analyzer, contract and compiler-input changes with pass_filenames false.
  </behavior>
  <action>
    Per D-01, D-07 and D-09, assert 06-06's fresh population closure and all additional live-remediation plans are complete before activation. Add lint:type-members and lint:type-members:negative package scripts invoking the actual CLI and negative runner; include both in the current npm check chain without replacing or weakening existing checks. Read the current package scripts because Phase 5 may have added gates. Add a local pre-commit hook that invokes the same pair and analyzes the whole project with pass_filenames false. Its trigger expression includes production/test .ts, analyzer/negative .mjs, exact contracts .json, tsconfig, package/lock and the hook configuration itself; removing the sole reader in a different file must trigger. Confirm CI's existing npm run check path includes the gate, editing no workflow unless its current call path is absent; an absent CI path requires a bounded follow-up before completion. Extend the architecture test to parse exact package/hook configuration and exercise trigger controls for a reader removal, contract change, unrelated non-input and setup errors. Preserve all current lint/format/Fallow modes, exports, coverage scripts and thresholds.
    Verify 06-06's measured budget rationale, deliberate exhaustion failure and fresh installed-boundary/contract-drift evidence before enabling the gate. Missing acceptance evidence is a failed activation prerequisite even if today's unread count is zero.
  </action>
  <fails_when>Fails if activation precedes closure, CI uses a weaker invocation, deleting a reader misses the hook, or any existing quality gate is dropped. Closure means every finding is either absent or carried by a named per-row exception stating the member, its measured mechanism, and why no category reaches it -- never a count, a threshold, a path glob, or an unexplained allowance. Fails if the exception list is expressed as anything other than exact member coordinates, if it may grow without a recorded decision, or if a listed row loses its mechanism.</fails_when>
  <verify><automated>node --test tests/architecture/unused-type-member-gate.test.ts && npm run lint:type-members && npm run lint:type-members:negative</automated></verify>
  <done>The normal local/CI path and whole-project hook both execute the verified gate and controls on all relevant changes.</done>
</task>

<task type="auto">
  <name>Verify the final integration and publish its supported-scope evidence</name>
  <files>docs/unused-type-member-gate.md, .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md</files>
  <behavior>
    The actual post-integration tree passes the member gate/negative controls, normal check, native aggregate unit coverage and unchanged direct-pair gates.
  </behavior>
  <action>
    Per D-06, D-07 and D-09, document invocation, supported operations, separate production/test observations, precise contract rationale, actionable unsupported diagnostics and the may-observe limitation. State that a clean result does not establish execution/usefulness and assertion review remains required. Include the real EdgeDeps plant procedure and contract drift controls; the document must not instruct broad exclusions, count thresholds, path globs or unexplained baseline allowances; the only sanctioned residual form is a per-row exception naming the exact member coordinate and its measured mechanism, and the document must state that adding a row to it is a decision, not maintenance. Run npm run check, npm run test:coverage:unit and npm run test:coverage:direct:all on one stable final integration snapshot, reusing that snapshot for the recorded evidence. Inspect unmodified native coverage/unit.lcov for every production file/function/branch/line: require the user's full 100 percent aggregate production unit result, not integration/E2E coverage or rounded partial totals. Preserve the direct pin file/floors and investigate any failure rather than updating pins, excluding sources or lowering thresholds. Record command results, exact revision/fingerprint, analyzer work counts/timing and the completed per-task assertion/coverage ledger in the phase summary and live ledger. Run the normal pre-commit checks on the final touched tracked files when executing the phase; no repeated aggregate run is needed after documentation-only edits.
  </action>
  <fails_when>Fails if coverage comes from integration/E2E, any production metric falls below 100 percent, direct floors change, a new allowance masks findings, or evidence predates a source change.</fails_when>
  <verify><automated>npm run check && npm run test:coverage:unit && npm run test:coverage:direct:all</automated></verify>
  <done>Final evidence shows zero unexplained analysis findings, all negative controls, unchanged strong assertions, 100 percent native aggregate production unit coverage and passing direct pins.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands/results and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not generate expected observations from the analyzer's declaration enumeration. Final snapshot must show 100 percent native aggregate production unit coverage and unchanged direct-pair floors; attach exact results.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-08-T1 | The package check path invokes both the detector and its negative runner; pre-commit triggers for reader, analyzer, contract and compiler-input changes with pass_filenames false. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-08-T2 | The actual post-integration tree passes the member gate/negative controls, normal check, native aggregate unit coverage and unchanged direct-pair gates. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source/configuration/overlay text is parsed as data and must never execute. |
| Evidence to clean verdict | Symbol, flow and contract records must not manufacture observations or hide unexplained members. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch/configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-08-01 | Tampering | Candidate provenance and contract evidence | high | mitigate | Require exact declaration identity, directed origin/path witnesses and sibling/stale-evidence controls in the task suite. |
| T-06-08-02 | Elevation of privilege | Compiler inputs and subprocess controls | high | mitigate | Parse analyzed modules without importing/executing them; use argv arrays and contained temporary overlays, then verify cleanup. |
| T-06-08-03 | Denial of service | Recursive graph and malformed compiler input | medium | mitigate | Cycle-aware bounded worklists and explicit resource/setup diagnostics; a cutoff cannot return clean. |
| T-06-08-04 | Repudiation | Gate acceptance | medium | mitigate | Deterministic member-level reports, task assertion/coverage ledger and exact positive/negative process results. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run the final full check and unchanged aggregate/direct coverage gates on the same stable source snapshot. Tests must assert exact independently specified category, declaration/member and observation semantics, not successful enumeration. Track unsupported analysis explicitly. Live closure requires zero unsupported findings and zero unread findings other than the named per-row exceptions, each carrying its measured mechanism, after all bounded source and analyzer repairs. A new unread member outside that list must fail the gate.
</verification>

<success_criteria>
- Normal npm check and pre-commit run the same whole-project member gate and its negative controls.
- Removing a reader in any production/test file triggers the gate; configuration and contract changes cannot bypass it.
- Final native aggregate production unit coverage remains 100 percent and all direct-pair pins/assertions remain intact.
- Every task's fails_when counterexample is exercised, and its assertion/coverage ledger is complete.
- No production coverage threshold, direct pin or assertion contract is weakened.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-08-SUMMARY.md when done. Record implementation evidence, exact verification results, observed limits and any newly required bounded live-remediation plan IDs.
</output>
