---
phase: 06-unused-type-member-gate
plan: "07"
type: execute
wave: 6
depends_on: ["06-06"]
files_modified:
  - scripts/check-unused-type-members.negative.mjs
  - tests/scripts/check-unused-type-members.negative.test.ts
  - tests/architecture/unused-type-member-gate.test.ts
  - package.json
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The actual CLI rejects a temporary unread optional member added to the real EdgeDeps declaration."
    - "Removing the offender or adding a genuine receiver read passes while unrelated reads do not."
    - "Negative controls reject an always-passing gate and distinguish setup/compiler failures from successful detection."
  artifacts:
    - path: scripts/check-unused-type-members.negative.mjs
      provides: "Real CLI offender and benign controls including live EdgeDeps implementation/evidence"
    - path: tests/scripts/check-unused-type-members.negative.test.ts
      provides: "Real CLI offender and benign controls including live EdgeDeps implementation/evidence"
  key_links:
    - from: "scripts/check-unused-type-members.negative.mjs"
      to: "tests/scripts/check-unused-type-members.negative.test.ts"
      via: "Independently authored controls execute the real operation and assert exact observations and failures."
---
<objective>
Real CLI offender and benign controls including live EdgeDeps.

Purpose: The actual CLI rejects a temporary unread optional member added to the real EdgeDeps declaration.
Output: scripts/check-unused-type-members.negative.mjs, tests/scripts/check-unused-type-members.negative.test.ts, tests/architecture/unused-type-member-gate.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-06-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Plant an unread optional member in the actual EdgeDeps compiler input</name>
  <files>scripts/check-unused-type-members.negative.mjs, tests/scripts/check-unused-type-members.negative.test.ts, tests/architecture/unused-type-member-gate.test.ts, package.json</files>
  <behavior>
    Clean live analysis passes; the real source overlay adds precisely EdgeDeps.neverReadAnywhere and yields status 1 with that exact unread diagnostic; a real receiver read removes it.
  </behavior>
  <action>
    Per D-01 and D-08, implement the negative runner around spawnSync/process.execPath with argv arrays, bounded output and guaranteed temporary cleanup. Analyze the clean live project and then a CompilerHost overlay that inserts readonly neverReadAnywhere?: string into the actual interface declaration resolved in extensions/pi-claude-marketplace/edge/types.ts. Compute insertion from AST structure and independently assert the resulting expected owner/key/category/span; no production file is written. Require the offender report to contain exactly the new member failure relative to the clean baseline and no setup/internal diagnostics. A benign overlay appends a well-typed exported probe with an actual value read from an EdgeDeps receiver to the existing tests/edge/types.test.ts compiler input; removing the plant also restores the clean report. A same-spelling property read on an unrelated object must leave the offender. Assert original file bytes and tracked source diff remain unchanged after success or failure. Expose real CLI invocation so the runner can be tested against controlled defective gate executables.
    Add the lint:type-members:negative package alias for the completed runner now, preserving the aliases already added by 06-01 and 06-06. Plan 06-08 activates the detector and runner in the mandatory check chain.
  </action>
  <fails_when>Fails if only a synthetic lookalike interface is tested, the offender passes on unrelated reads, the baseline itself fails, or any arbitrary nonzero process exit counts as detection.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.negative.test.ts tests/architecture/unused-type-member-gate.test.ts</automated></verify>
  <done>The runner proves real live EdgeDeps detection, benign recovery and zero production mutation using exact reports and statuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Prove negative-runner discrimination and the full observation boundary</name>
  <files>scripts/check-unused-type-members.negative.mjs, tests/scripts/check-unused-type-members.negative.test.ts</files>
  <behavior>
    Always-pass, always-fail, malformed JSON, wrong-member failure, compiler/setup failure and launch failure all make the negative runner reject its result.
  </behavior>
  <action>
    Per D-02, D-04, D-05 and D-08, extend independently authored offender/benign controls around the real CLI for declaration/initializer/write/delete/type-only and same-name negatives; optional/literal/finite-computed access and binding/assignment destructuring positives; aliases/structural transfers/callbacks/container directions; whole-object source/target, expected-literal/actual-result, serialization/shadowing, and precise external-contract sibling/drift cases. Reuse fixture infrastructure where it reduces duplication, but keep expected report facts independent of analyzer output. Couple this runner to core fixture-suite cases without replaying every expensive live scan. Prove the runner fails when handed a process that always reports clean, always exits failure, emits the wrong diagnostic or malformed report, or cannot launch. Keep compiler/setup failures status 2 distinct from intended member findings status 1 and require the benign case to return 0. Inspect cleanup after failure too. Record which adversarial controls prove each supported analysis claim; no declaration enumeration test may serve as a runtime witness.
  </action>
  <fails_when>Fails if assertions check exit status alone, the runner trusts a report it generated itself, benign controls are omitted, or malformed/setup outcomes satisfy the offender.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.negative.test.ts && node scripts/check-unused-type-members.negative.mjs</automated></verify>
  <done>All detector and harness mutants are rejected and every claimed supported observation category has a discriminating control.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands/results and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not generate expected observations from the analyzer's declaration enumeration. These tasks add analyzer/configuration/control behavior without production .ts edits. Record focused analyzer case/branch coverage and assertion strength; preserve all production coverage scripts, exclusions and direct pins. The orchestrator runs the native aggregate unit and direct gates once per stable integration wave, not once per minor task.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-07-T1 | Clean live analysis passes; the real source overlay adds precisely EdgeDeps.neverReadAnywhere and yields status 1 with that exact unread diagnostic; a real receiver read removes it. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-07-T2 | Always-pass, always-fail, malformed JSON, wrong-member failure, compiler/setup failure and launch failure all make the negative runner reject its result. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source/configuration/overlay text is parsed as data and must never execute. |
| Evidence to clean verdict | Symbol, flow and contract records must not manufacture observations or hide unexplained members. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch/configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-07-01 | Tampering | Candidate provenance and contract evidence | high | mitigate | Require exact declaration identity, directed origin/path witnesses and sibling/stale-evidence controls in the task suite. |
| T-06-07-02 | Elevation of privilege | Compiler inputs and subprocess controls | high | mitigate | Parse analyzed modules without importing/executing them; use argv arrays and contained temporary overlays, then verify cleanup. |
| T-06-07-03 | Denial of service | Recursive graph and malformed compiler input | medium | mitigate | Cycle-aware bounded worklists and explicit resource/setup diagnostics; a cutoff cannot return clean. |
| T-06-07-04 | Repudiation | Gate acceptance | medium | mitigate | Deterministic member-level reports, task assertion/coverage ledger and exact positive/negative process results. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run typecheck, lint and format checks on the completed wave; the orchestrator reuses one stable wave snapshot for unchanged native aggregate production unit coverage and direct pins. Tests must assert exact independently specified category, declaration/member and observation semantics, not successful enumeration. Track unsupported analysis explicitly. Live closure requires zero unread and unsupported findings after all bounded source repairs.
</verification>

<success_criteria>
- The actual CLI rejects a temporary unread optional member added to the real EdgeDeps declaration.
- Removing the offender or adding a genuine receiver read passes while unrelated reads do not.
- Negative controls reject an always-passing gate and distinguish setup/compiler failures from successful detection.
- Every task's fails_when counterexample is exercised, and its assertion/coverage ledger is complete.
- No production coverage threshold, direct pin or assertion contract is weakened.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-07-SUMMARY.md when done. Record implementation evidence, exact verification results, observed limits and any newly required bounded live-remediation plan IDs.
</output>
