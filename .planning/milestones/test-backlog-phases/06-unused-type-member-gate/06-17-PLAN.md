---
phase: 06-unused-type-member-gate
plan: "17"
type: execute
wave: 8
depends_on: ["06-15"]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/hooks.ts
  - extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts
  - extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
  - extensions/pi-claude-marketplace/orchestrators/edge-deps.ts
  - extensions/pi-claude-marketplace/edge/completions/data.ts
  - .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 95000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The anchor triple consumed by the `if`-field compile path has exactly ONE declaration, the domain one, and the production reads of `homedir` and `projectRoot` land on it (D-03)."
    - "The completions seam has exactly ONE declaration of the resolver shape and of the state-record shape, both on the orchestrator side, and the edge reads land on them (D-03)."
    - "Every cleared row is cleared by collapsing a duplicate declaration -- no contract entry, no added reader, no added test, no widened proof (D-02, D-05)."
    - "No production behaviour changes: every edit is a type-level collapse or a comment the collapse falsified. The resolver still returns the projected shape its paired test deep-compares (D-06)."
    - "Each collapse runs a direction the architecture already permits, and all four fallow sub-gates pass unchanged; no zone allow-list, ESLint boundary or suppression is edited (D-07)."
    - "The delta is measured as a `(path, owner, key)` set difference against a pre-plan baseline and ZERO findings are gained; a row that will not clear is left standing with a measured reason rather than laundered (D-06, D-09)."
    - "`npm run check` is exit 0, the seven negative controls still pass, and aggregate production unit coverage is still 100 percent with no module below 100 percent and both direct pin rows unchanged (D-07)."
    - "`audit --check` reconciles against a freshly recomputed digest with zero stale-source, stale-record, missing, duplicate, incomplete or invalid rows, and every re-keyed note was DERIVED from the fresh report."
  artifacts:
    - path: extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts
      provides: "`CompileIfPredicateContext` published as an alias of the domain anchor context, declaring no members of its own"
    - path: extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts
      provides: "`PathAnchorContext` collapsed onto the same declaration if and only if the alias alone leaves a row standing"
    - path: extensions/pi-claude-marketplace/domain/components/hooks.ts
      provides: "`ResolveHookIfContext` as the sole declaration of the anchor triple, with the mirror language the collapse falsified corrected"
    - path: extensions/pi-claude-marketplace/edge/completions/data.ts
      provides: "`LocationsResolver` and `MarketplaceStateRecord` published under the same spellings as aliases of the orchestrator declarations"
    - path: extensions/pi-claude-marketplace/orchestrators/edge-deps.ts
      provides: "The canonical seam declarations, with the hand-sync comment the collapse made obsolete removed rather than left standing"
    - path: .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
      provides: "Record regenerated against a fresh digest so `--check` reports no stale-source after the collapses"
  key_links:
    - from: "extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts"
      to: "extensions/pi-claude-marketplace/domain/components/hooks.ts"
      via: "`bridges/` may import `domain/` and `domain/` may not import `bridges/`, so the one surviving declaration of the anchor triple has to be the domain one."
    - from: "extensions/pi-claude-marketplace/edge/completions/data.ts"
      to: "extensions/pi-claude-marketplace/orchestrators/edge-deps.ts"
      via: "`edge/` may import `orchestrators/` and `orchestrators/` may not import `edge/`, so the one surviving declaration of the seam has to be the orchestrator one."
    - from: "the source edits"
      to: "scripts/check-unused-type-members.contracts.json and 06-LIVE-TRIAGE.md"
      via: "Both records are coordinate-addressed, so any line-shifting edit re-keys entries and disposition rows in the same change or the engine exits 2 before any member verdict."
---

<objective>
Close four of the sixteen remaining unread type members. All four are the same
defect twice over: ONE shape declared in two places, with every production read
landing on the copy that is not the one the analyzer reports.

Pair 1 is the `if`-field anchor triple, duplicated into `domain/` so the parser
does not type-depend on the bridge surface (D-11 import direction). Pair 2 is
the completions seam, duplicated into `orchestrators/` because the architecture
boundary forbids `orchestrators/` from importing `edge/`. In both cases the
boundary that created the duplicate permits the collapse to run the OTHER way,
so each mirror can become a published alias of a single declaration.

Purpose: 06-08 cannot activate the mandatory gate while rows stand unexplained,
and the honest way to drain a duplicate-declaration row is to remove the
duplicate, never to widen a proof or add a reader (D-02, D-05, D-09).
Output: five collapsed or corrected source files, a measured `(path, owner, key)`
delta with zero findings gained, and a regenerated `06-LIVE-TRIAGE.md`.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-09-SUMMARY.md
@.planning/phases/06-unused-type-member-gate/06-11-SUMMARY.md
@.planning/phases/06-unused-type-member-gate/06-12-SUMMARY.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.planning/codebase/ARCHITECTURE.md
@extensions/pi-claude-marketplace/domain/components/hooks.ts
@extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts
@extensions/pi-claude-marketplace/orchestrators/edge-deps.ts
@extensions/pi-claude-marketplace/edge/completions/data.ts

`06-LIVE-TRIAGE.md` is deliberately NOT referenced above: it is 394 KB over
3,903 lines and would consume most of an executor's context. Read only this
plan's own four rows, with
`sed -n '/^## Unresolved members$/,/^## Members only tests read$/p' .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md`
(around lines 47-84). Every row's `Disposition` cell is the measured evidence
that produced this plan; per D-09 it is evidence to check, not an instruction
to obey.
</context>

<owned_rows>
These four rows, and no others, are this plan's scope. The record was written at
revision `18b9b3e5`; `git log 18b9b3e5..HEAD` since then is docs-only, so
`extensions/` is byte-identical to the tree the record measured. Confirm each id
against a FRESH report before editing anyway -- a row that has already moved is a
signal the record is stale, not a licence to proceed.

**Pair 1 -- the `if`-field anchor triple (2 rows, one collapse):**

| Row id | Declaration |
| --- | --- |
| `domain/components/hooks.ts:77:3` | `ResolveHookIfContext.homedir` |
| `domain/components/hooks.ts:79:3` | `ResolveHookIfContext.projectRoot` |

**Pair 2 -- the completions seam (2 rows, one collapse):**

| Row id | Declaration |
| --- | --- |
| `orchestrators/edge-deps.ts:58:3` | `MarketplaceStateRecordLike.plugins` |
| `orchestrators/edge-deps.ts:64:5` | `LocationsResolverLike.loadStateForScope` return, `marketplaces` |

**The other twelve rows are OUT OF SCOPE and must not be touched.** Seven belong
to engine plan 06-16 (the destructuring-transfer and constraint-proof
categories). Five are recorded exceptions with owners elsewhere:
`enableRowDependencies.signals.partition`, `AuthAttemptResult.authAttempted`
twice (recorded decision D-32-05, which this plan has no authority to revoke),
`UpdatePhaseFailure.msg`, and `PLUGIN_INFO_RENDER.status`. If a repair here
moves any of those twelve, that is a gained or lost finding to investigate and
report, not a bonus to claim.
</owned_rows>

<standing_constraints>
**1. Production behaviour must not change.** Every edit in this plan is a
type-level collapse or a comment the collapse falsified. If any collapse turns
out to alter what a function returns or accepts at runtime, stop, leave that
row, and record why (D-06). 06-09's precedent is the standard: it named the one
real type change its alias introduced and proved it cost nothing downstream
with the compiler rather than with reasoning.

**2. The architecture boundary is what CREATED these duplicates. Do not violate
it to remove them.** `.fallowrc.json` gives each zone an explicit allow-list and
`eslint.config.js` carries a coarser `import-x/no-restricted-paths` mirror of
the same rules. Each task names the direction its collapse runs and why that
direction is legal. `npm run fallow` -- all four sub-gates -- is the check. No
zone, allow-list, restricted-path entry or suppression may be edited to make a
collapse fit.

**3. Contract coordinates are pinned and fragile.**
`scripts/check-unused-type-members.contracts.json` addresses each entry by exact
`path:line:column`. Measured at revision `18b9b3e5`: the file holds 101 entries
and NONE of them names any file this plan touches; the only two entries under
`bridges/hooks` sit in `event-adapters.ts` and `wire-protocol.ts`, neither of
which is in scope here. **Re-derive that against the current file rather than
trusting this plan** -- if any entry does land in a touched file, re-anchor it
from the EDITED source, never by arithmetic on the old value. 06-10 measured the
failure mode: a detached entry makes the engine exit 2 before any member verdict
with `names no declaration in this program`. Exit 2 is a blocking stop, never an
allowance. The entry count stays 101 and no `category`, `owner`, `key` or
`purpose` changes.

**4. Measure the delta by `(path, owner, key)` diff, not by count.** Capture
`node scripts/check-unused-type-members.mjs --json > tmp/members-before.json`
before the first edit and diff against it after the last. Fail if ANY finding
was gained, even on a flat or falling total. Seven plans have met this standard
without exception.

**5. Derive every re-keyed triage note; NEVER transcribe one.** That hazard
produced twelve wrong coordinates across three plans. `--check` does not compare
note text against the report, so a wrong coordinate is certified `explained`
while pointing at nothing.

**6. Coverage is this plan's obligation, not the orchestrator's (D-07).**
Aggregate production unit coverage is exactly 100 percent -- 1833/1833
functions, 9049/9049 branches, zero modules below 100 percent. Every edit here
is type-level and emits no JavaScript, so any movement at all is a signal to
investigate. Never lower a threshold, exclude a source or re-pin a floor to
pass. Neither row in `scripts/test-coverage-direct.pin.json` names a file this
plan touches.
</standing_constraints>

<!-- The acceptance greps below run over the analyzer CLI's stderr finding lines
     (`status: path:line:col owner.key`), never over this plan or over project
     source, so a path literal appearing in an action body cannot satisfy or
     invalidate them. -->
<!-- planner-discipline-allow: domain/components/hooks.ts -->
<!-- planner-discipline-allow: orchestrators/edge-deps.ts -->

<tasks>

<task type="tracer">
  <name>Collapse the if-field anchor triple onto its single domain declaration</name>
  <files>extensions/pi-claude-marketplace/bridges/hooks/if-field/index.ts, extensions/pi-claude-marketplace/bridges/hooks/if-field/glob.ts, extensions/pi-claude-marketplace/domain/components/hooks.ts</files>
  <precondition>`npm run check` is exit 0 on the inherited tree before any edit; a red baseline is a blocking finding to record and repair first, not to work around (06-06 found 06-05 had left one).</precondition>
  <behavior>
    Before: the analyzer reports 2 unread members on `ResolveHookIfContext`
    (`domain/components/hooks.ts:77:3` and `:79:3`), and its third slot
    (`:78:3`, `cwd`) is `test-only-observed` with one witness. After: it reports
    zero unread members for that file, no count anywhere else rises, and the
    `if`-field compile and dispatch paths behave identically.
  </behavior>
  <action>
    THE SAME THREE FIELDS ARE DECLARED THREE TIMES. `ResolveHookIfContext`
    (`domain/components/hooks.ts:76`), `CompileIfPredicateContext`
    (`bridges/hooks/if-field/index.ts:151`) and `PathAnchorContext`
    (`bridges/hooks/if-field/glob.ts:92`) each declare `readonly homedir`,
    `readonly cwd`, `readonly projectRoot` and nothing else. Two of the three say
    so in their own doc comments.

    WHY THE READS MISS THE DOMAIN COPY, measured rather than assumed. The value
    is built at `event-router.ts:149` and `:670`, handed to `parseHooksConfig`
    whose parameter is the domain type, and passed from there into the
    `compileIf` CALLBACK parameter. In production that callback is
    `compileIfPredicate`, whose own parameter is the bridge type, which forwards
    to `compilePathGlob`, whose parameter is the glob type -- and the five
    property accesses that actually read the fields are in `glob.ts` at
    `resolveAnchor` (`:437`, `:445`, `:453`, `:461`, `:468`). So every production
    read resolves to a declaration downstream of the domain one. The proof that
    this is the mechanism and not a guess: `ResolveHookIfContext.cwd` is credited
    at all, and its ONE witness is `tests/domain/components/hooks.test.ts:141:14`
    -- a TEST that supplies its own inline `compileIf` callback and reads
    `ctx.cwd` off the domain-typed parameter directly (`:138`). Production has no
    such site.

    THE DIRECTION IS FORCED. `.fallowrc.json` allows `bridges-hooks -> domain`
    and allows `domain -> shared, platform` only; `eslint.config.js` mirrors it
    ("bridges/ may only import from domain/, persistence/, shared/, platform/",
    and files under `domain/` may not import from `edge/`, `orchestrators/` or
    `bridges/`). So the one surviving declaration MUST be the domain one and the
    bridge names must alias it. This is the shape 06-06 named for the
    async-rewake duplicate and 06-09 executed: a published duplicate becomes an
    alias of the declaration its reads already resolve to, so it stops being a
    candidate without the published name being deleted.

    STEP A, then measure. In `if-field/index.ts`, replace the
    `CompileIfPredicateContext` interface body with an alias of
    `ResolveHookIfContext`, imported type-only from
    `../../../domain/components/hooks.ts`. That module edge is new for this file
    but the zone edge is not -- it already imports `hook-events.ts` and
    `hook-if-targets.ts` from the same zone -- and `domain/components/hooks.ts`
    imports nothing from any bridge, so no cycle forms and neither fallow cycle
    run changes. Keep the name exported with the same spelling. Then re-run the
    analyzer and read the two row ids.

    STEP B, only if step A leaves either row standing. Collapse
    `PathAnchorContext` the same way, which puts the five property accesses in
    `glob.ts` DIRECTLY on the domain declaration instead of depending on a
    transfer hop through two parameters. Re-measure. Record which step cleared
    the rows; if step A alone was enough, do not take step B -- the phase does
    not churn source without a measured row to show for it.

    IF BOTH ROWS STILL STAND AFTER STEP B, restore both files byte-for-byte and
    record the measured mechanism instead. That is 06-12's `update-swap.ts`
    precedent and it is an acceptable outcome for this pair. Do NOT reshape the
    read sites, restate the triple, or add a reader to manufacture credit.

    EXPECT `cwd` TO MOVE TOO. `:78:3` should stop being `test-only-observed` and
    become runtime-observed once the production reads land on the domain
    declaration. That is the same collapse working, not a gained finding -- but
    it removes a recorded disposition row from the "Members only tests read"
    section, so report it explicitly.

    COMMENTS THAT STOP BEING TRUE GO WITH THE MIRROR. The `ResolveHookIfContext`
    doc block (`domain/components/hooks.ts:70-75`) says the shape "Mirrors ...
    structurally -- duplicated here so the parser does not depend on the bridge
    surface". After the collapse there is no duplicate: state that this is the
    sole declaration and that the bridge publishes an alias of it, and keep the
    D-11 rationale, which is what makes the domain side the canonical one. Fix
    the matching claim in the `if-field/index.ts` header block and in the
    `CompileIfPredicateContext` doc block. Do not touch the D-61-02 / D-61-03 /
    A1 behaviour contracts either block records.

    DO NOT EDIT ANY TEST IN THIS TASK.
    `tests/bridges/hooks/if-field/index.test.ts:50` pins the shape
    independently with `Same<CompileIfPredicateContext,
    ExpectedCompileIfPredicateContext>` against a hand-written expectation, and
    `tests/domain/components/hooks.test.ts` carries the domain-side positive and
    negative `satisfies` proofs. Those are the guards that prove the alias is
    shape-preserving. They must pass unmodified.
  </action>
  <fails_when>Fails if the collapse runs `domain -> bridges`; if a published name is deleted rather than aliased; if either structural pin test is edited to accommodate the alias; if step B is taken when step A already cleared both rows; if a row clears because a reader, a test or a contract entry was added rather than because a declaration was removed (D-02); or if a comment claiming the shape is duplicated survives the collapse that removed the duplicate.</fails_when>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; node --test tests/domain/components/hooks.test.ts tests/bridges/hooks/if-field/index.test.ts tests/bridges/hooks/if-field/glob.test.ts tests/architecture/hooks-if-field.test.ts tests/bridges/hooks/event-router.test.ts &amp;&amp; test "$(node scripts/check-unused-type-members.mjs 2>&amp;1 >/dev/null | grep -c 'domain/components/hooks.ts')" = 0</automated></verify>
  <done>The anchor triple has one declaration, the analyzer reports zero unread members for `domain/components/hooks.ts`, all four fallow sub-gates pass, and the `if`-field, glob and event-router suites pass unmodified.</done>
</task>

<task type="auto">
  <name>Collapse the completions seam onto its single orchestrator declaration</name>
  <files>extensions/pi-claude-marketplace/edge/completions/data.ts, extensions/pi-claude-marketplace/orchestrators/edge-deps.ts</files>
  <reversibility rating="reversible">A type-only import edge from `edge/completions/` to `orchestrators/edge-deps.ts`; reverting means restoring two interface bodies in one file. It is recorded here because it is a deliberate architectural choice about which side owns the seam shape, not because it is hard to undo.</reversibility>
  <behavior>
    Before: the analyzer reports 2 unread members in `orchestrators/edge-deps.ts`
    (`:58:3` and `:64:5`). After: it reports zero for that file, `edge/register.ts`
    still hands `makeLocationsResolver`'s result to `getArgumentCompletions`
    without a cast, and every completion path returns the same items.
  </behavior>
  <action>
    THE SAME SHAPE IS DECLARED ON BOTH SIDES OF ONE BOUNDARY.
    `MarketplaceStateRecordLike` / `LocationsResolverLike`
    (`orchestrators/edge-deps.ts:57` and `:61`) restate
    `MarketplaceStateRecord` / `LocationsResolver`
    (`edge/completions/data.ts:145` and `:130`). The edge reads go through the
    edge declarations -- `state.marketplaces` at `data.ts:261` and `:351`,
    `?.plugins` at `:352` -- so the orchestrator copies have no reader BY
    CONSTRUCTION, which is exactly what the two rows report.

    THE DIRECTION IS FORCED, AND IT IS THE OPPOSITE OF PAIR 1'S. `.fallowrc.json`
    allows `edge -> orchestrators` and does NOT allow `orchestrators -> edge`;
    `eslint.config.js` says the same twice ("edge/ may only import from
    orchestrators/, domain/, shared/, platform/" and "orchestrators/ MUST NOT
    import from edge/"). `edge/register.ts` already imports this very module. So
    the one surviving declaration MUST be the orchestrator one, and the edge
    names become aliases of it. That reaches into an edge-owned file, which is
    the ONLY reason 06-12 recorded these rows instead of closing them -- a file
    ownership constraint, not a technical one, and it no longer applies because
    this plan holds both files.

    THE EDIT. In `edge/completions/data.ts`, replace the two interface bodies
    with published aliases of the orchestrator declarations -- either
    `export type { LocationsResolverLike as LocationsResolver,
    MarketplaceStateRecordLike as MarketplaceStateRecord } from
    "../../orchestrators/edge-deps.ts";` or the equivalent type-only import plus
    two `export type` aliases, whichever satisfies `import-x/order` with the
    least churn. Both names MUST keep their current spelling and stay exported:
    `edge/completions/provider.ts` and four test files
    (`tests/edge/completions/data.test.ts`, `tests/edge/completions/provider.test.ts`,
    `tests/architecture/flag-catalog-drift.test.ts`, `tests/edge/register.test.ts`)
    import them, and none of those files may be edited by this task. The import
    is type-only and therefore erased, so `edge/completions/` gains no runtime
    dependency; the seam's whole point -- that `edge/` never imports
    `persistence/` or `domain/` -- is untouched, because the orchestrator
    declarations reference only `Scope` and `PluginIndexRow`, which `data.ts`
    already imports from `shared/`.

    FORBIDDEN, EXPLICITLY. Do not make the resolver return the persistence state
    unprojected. It would resolve `:64:5` by deleting the projection loop at
    `edge-deps.ts:158-163`, but it changes what `loadStateForScope` returns, and
    `tests/orchestrators/edge-deps.test.ts:267-291` deep-compares that exact
    value. That is behaviour, not a type repair, and it is out of scope (D-06).

    ALSO FORBIDDEN: renaming the orchestrator declarations to drop the now-
    misleading `Like` suffix. It would churn the implementation and its paired
    test for zero additional rows. Record in the SUMMARY that the suffix now
    records history rather than structure, and that a rename is available to a
    later owner -- do not silently drop the observation.

    THE COMMENTS THE COLLAPSE FALSIFIES MUST GO WITH IT. `edge-deps.ts:45-55`
    calls these declarations "a structural alias ... Re-declared here -- NOT
    imported", says "The fields MUST stay in sync with edge/completions/data.ts",
    and claims "a future rename would be caught by the edge-side TypeScript
    compile". The last claim is measured FALSE in
    `.planning/reviews/unit-test-adversarial/adversarial/orchestrators-root.md:36-37`:
    both mirrors declare every member optional, so TypeScript's weak-type
    detection fires only when the two share NO property, and a single-field
    rename compiles clean -- the repo's own recorded optional-field
    silent-omission class. After the collapse the sync is true by construction,
    because there is only one declaration left to be in sync with. Rewrite that
    block to say so, and fix the matching line in the file header
    (`edge-deps.ts:14-15`) which names the edge file as the one that declares the
    interface. Leave the BLOCK C rationale and the error contracts intact -- they
    are still exactly right and they are why the orchestrator side is canonical.

    `edge/completions/data.ts` is a scanned target of the completion-description
    vocabulary guard (`tests/architecture/gate-targets.ts:509`,
    `tests/architecture/partial-vocabulary-guard.test.ts:615`). That guard reads
    double-quoted `description:` string VALUES only, so a type edit is outside
    its reach -- but do not introduce one.
  </action>
  <fails_when>Fails if the collapse runs `orchestrators -> edge`; if the projection loop is removed or the resolver's return value otherwise changes; if either published edge name changes spelling or stops being exported; if any of the four consuming test files is edited; if the sync-comment or the disproved compile-catches-it claim survives the mirror it described; or if either row clears because a reader or a contract entry was added rather than because a declaration was removed (D-02).</fails_when>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run fallow &amp;&amp; node --test tests/orchestrators/edge-deps.test.ts tests/edge/completions/data.test.ts tests/edge/completions/provider.test.ts tests/edge/register.test.ts tests/architecture/flag-catalog-drift.test.ts tests/architecture/import-boundaries.test.ts &amp;&amp; test "$(node scripts/check-unused-type-members.mjs 2>&amp;1 >/dev/null | grep -c 'orchestrators/edge-deps.ts')" = 0</automated></verify>
  <done>The seam has one declaration per shape, the analyzer reports zero unread members for `orchestrators/edge-deps.ts`, all four fallow sub-gates pass, the resolver still returns the projected shape its paired test deep-compares, and the edge and register suites pass unmodified.</done>
</task>

<task type="auto">
  <name>Measure the delta, reconcile the record and run the full gate chain</name>
  <files>.planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md</files>
  <behavior>
    After this task the live population is a measured 16 to 12 with ZERO
    findings gained, `audit --check` reports that measured number with no
    stale-source, stale-record, missing, duplicate, incomplete or invalid rows,
    `npm run check` is exit 0 including all four fallow sub-gates, the seven
    negative controls still pass, and aggregate production unit coverage is
    unchanged at 100 percent.
  </behavior>
  <action>
    THE DELTA, AS A SET DIFFERENCE. Diff the post-edit finding set against
    `tmp/members-before.json` by `(path, owner, key)`, not by total:

      comm -13 (sorted before) (sorted after)   -- gained: MUST be empty
      comm -23 (sorted before) (sorted after)   -- lost:   the four rows

    A repair that silently pushed some other member into `unread` shows up here
    even on a flat total. Any gain is a blocking failure to investigate, not a
    number to net off.

    THE PREDICTION, TO CHECK RATHER THAN TO RESTATE. Unread 16 to 12. Candidates
    3352 falling by roughly eleven: three declarations leave with the bridge
    anchor alias, three more if step B was taken, five with the edge seam
    aliases. Test-only-observed 240 to 239, because `ResolveHookIfContext.cwd`
    gains production witnesses. Contracts stay at 101 and unsupported stays at 0.
    Report the measured figures against these; a mismatch is a finding to explain
    in the SUMMARY, never a prediction to quietly rewrite.

    CONTRACT COORDINATES. Re-derive which entries sit in the five touched files
    against the CURRENT contracts file, before and after. The measured answer at
    `18b9b3e5` is none, which would make this a confirmation rather than a
    re-anchoring; confirm it, do not assume it. If an entry does sit in a touched
    file, re-derive its coordinate from the edited source. The engine exiting 2
    at any point is a blocking stop.

    THE RECORD. Regenerate with
    `node scripts/check-unused-type-members.audit.mjs --inventory`, which carries
    forward every note whose row did not move, then run `--check`: it exits 1 by
    design while the other twelve rows stand, so read its problem breakdown, not
    its status. Every note you re-key must be DERIVED from the fresh report --
    `--check` compares coordinates, not prose, so a transcribed coordinate is
    certified `explained` while pointing at nothing. Two dispositions disappear
    rather than move: the two `ResolveHookIfContext` unread notes, and the
    test-only note on `:78:3`. Confirm the regenerated record no longer carries
    them and that its totals close.

    A RECORDED DISPOSITION THIS PLAN JUDGES MIS-ASSIGNED (D-09). 06-15's
    "Next Phase Readiness" groups the two `hooks.ts` rows with
    `hook-if-targets.ts` as "6 rows want two new evidence categories", naming a
    `satisfies`-constraint category as their owner. The triage rows themselves
    say otherwise, and so does the source: these two are a duplicate declaration
    with a legal collapse direction, not a `satisfies` constraint, and no engine
    change was needed to clear them. Record that correction with the evidence, as
    06-09 did for its three mis-assigned rows. Leave the `hook-if-targets.ts`
    rows alone -- those really are `satisfies`-constrained and belong to 06-16.

    THE GATE CHAIN, ONCE, ON THE FINAL SNAPSHOT. An aggregate run is only valid
    for the tree it measured, so run these after the last source edit:
    `npm run check` (exit 0, including all four fallow sub-gates),
    `node scripts/check-unused-type-members.negative.mjs` (7 of 7, roughly seven
    minutes and 2.1 GiB), `npm run test:coverage:unit` read out of the unmodified
    native `coverage/unit.lcov` (1833/1833 functions, 9049/9049 branches, zero
    modules below 100 percent), and `npm run test:coverage:direct:all` with both
    pin rows unchanged. Every edit in this plan is type-level and emits no
    JavaScript, so a moved coverage number means something other than a type
    collapse happened -- investigate it rather than re-pinning.

    Report in the SUMMARY: the four row ids with the collapse that cleared each,
    the direction each collapse ran and the allow-list line that permits it, the
    measured before and after counts per file read out of the real CLI, the set
    difference in both directions, whether step B was needed, the population
    movements for `cwd`, the contract confirmation, the exact commands with their
    exit statuses, and every `fails_when` counterexample exercised.
  </action>
  <fails_when>Fails if any finding was gained; if the delta is reported as a subtraction of totals rather than a set difference; if a triage note is transcribed rather than derived; if the analyzer exits 2 at any point and the run continues; if the coverage or negative-control evidence predates the last source edit; if a threshold, exclusion, pin or suppression is adjusted to pass; or if a measured figure is restated from this plan's prediction instead of read out of the real CLI.</fails_when>
  <verify><automated>npm run check &amp;&amp; node scripts/check-unused-type-members.audit.mjs --inventory &amp;&amp; npm run test:coverage:unit &amp;&amp; npm run test:coverage:direct:all &amp;&amp; node scripts/check-unused-type-members.negative.mjs</automated></verify>
  <done>Four rows cleared and zero gained, measured by `(path, owner, key)` set difference; the record reconciles against a fresh digest; `npm run check` is exit 0; the seven negative controls pass; aggregate production unit coverage is still 100 percent with both direct pins unchanged.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record the exact measured count for the files it touches, read
out of the real CLI's stderr and not out of the record. On completion record
exact commands, statuses and counts in this plan's SUMMARY using the IDs below.
A cleared row must be traceable to a collapsed duplicate declaration; no row may
be cleared by a contract entry, a new test or a widened proof (D-02, D-05).
Production coverage scripts, exclusions, direct pins and assertion strength are
preserved; the aggregate run happens once, on the final snapshot, because an
earlier task's edit invalidates any aggregate measured before it.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-17-T1 | 2 rows on `domain/components/hooks.ts` before, 0 after; no other file's count rises; both structural pin tests (`Same<...>` on the bridge side, the positive and negative `satisfies` proofs on the domain side) pass UNMODIFIED; the `if`-field, glob, hooks-architecture and event-router suites pass; all four fallow sub-gates pass with the new module edge. | Focused verify command above; a type-only edit emits no JavaScript, so no production coverage number can move. Record the direct-pin result for each changed file. |
| 06-17-T2 | 2 rows on `orchestrators/edge-deps.ts` before, 0 after; the resolver's projected return value is byte-identical under its paired deep comparison; `edge/register.ts`'s hand-off still type-checks with no cast; the four consuming test files appear in no commit; the boundary sub-gate passes with the new `edge -> orchestrators` edge. | Focused verify command above; two interface bodies become aliases, so no emitted line changes. Record the direct-pin result for both changed files. |
| 06-17-T3 | The gained set is empty and the lost set is exactly the four owned rows; the record reconciles against a fresh digest with zero stale, missing, duplicate, incomplete or invalid rows; contracts stay at 101 with every coordinate resolving; the `cwd` reclassification is reported; the mis-assigned 06-15 grouping is corrected with evidence. | Full aggregate on the final snapshot: 1833/1833 functions, 9049/9049 branches, zero modules below 100 percent, both direct pin rows unchanged, recorded with the exact revision and digest. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source and contract text is parsed as data and must never execute. |
| Evidence to clean verdict | A repair must clear a row by removing a declaration, never by manufacturing an observation. |
| Architecture zone edges | `domain/` may not reach `bridges/`; `orchestrators/` may not reach `edge/`. A collapse that inverts either direction trades a reported finding for an unreported structural defect. |
| Completions seam to persistence | `edge/` reaches persistence and domain surfaces only through the injected resolver; the seam's shape must not become a channel that leaks those imports into `edge/`. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch and configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-17-01 | Tampering | Source repair that launders a finding | high | mitigate | Every cleared row is cleared by collapsing a duplicate declaration; each task's `fails_when` rejects a clear achieved by a contract entry, a new reader or a new test; the delta is a `(path, owner, key)` set difference against a pre-edit baseline, so a gained finding cannot hide behind a falling total. |
| T-06-17-02 | Elevation of privilege | Architecture zone allow-lists | high | mitigate | Each collapse runs the direction the allow-list already permits and the task names that line; `npm run fallow` (all four sub-gates) plus `tests/architecture/import-boundaries.test.ts` run in both source tasks; no zone, restricted-path entry or suppression may be edited to make a collapse fit. |
| T-06-17-03 | Tampering | Completions-seam runtime contract | high | mitigate | Returning the persistence state unprojected is forbidden by name; `tests/orchestrators/edge-deps.test.ts` deep-compares the projected value and runs in the task's verify; the seam import is type-only, so `edge/` gains no runtime dependency and no persistence or domain type crosses into it. |
| T-06-17-04 | Tampering | Structural pins that prove the aliases are shape-preserving | medium | mitigate | `Same<CompileIfPredicateContext, ExpectedCompileIfPredicateContext>`, the domain-side `satisfies` proofs and the four edge-side consumers must pass UNMODIFIED; editing any of them to accommodate a collapse is an explicit `fails_when`. |
| T-06-17-05 | Tampering | Coordinate-addressed contract entries and disposition notes | medium | mitigate | Which entries sit in the touched files is re-derived from the current contracts file rather than assumed; any moved coordinate is re-derived from the edited source, never by arithmetic; a detached entry surfaces as exit 2, which is a blocking stop; every re-keyed note is derived from the fresh report. |
| T-06-17-06 | Repudiation | Gate acceptance after a source repair | medium | mitigate | Deterministic member-level counts before and after each task read out of the real CLI, the seven negative controls and the full check chain run once on the final snapshot, a record regenerated against a freshly recomputed digest, and the per-task assertion and coverage ledger above. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks in the foreground with its exit status captured
directly, never through a pipe whose status belongs to `tail`. The analyzer costs
roughly 80 seconds and 2 GiB per run and the negative-control runner roughly
seven minutes and 2.1 GiB; that is the price of measuring the record against a
fresh analysis rather than against itself. The analyzer must exit 1 with member
findings after every task -- exit 2 is a stale or invalid contract or an
exhausted budget and is a blocking failure, never an allowance.

Measure this plan's delta against its own four rows, not against the global
total, so a sibling plan landing in the same wave cannot invalidate it. The
regenerated `06-LIVE-TRIAGE.md` is shared with 06-16 and with 06-08; the last
plan to land is authoritative, and `--check` is what proves it. Run
`npm run check`, the coverage gates and the negative controls once, on one stable
snapshot taken after the last source edit.

Commits are plain Conventional Commits with no milestone or phase identifiers,
and `SKIP=trufflehog pre-commit run --files <changed files>` passes before each
one. Comments and test titles cite durable spec and decision IDs only.
</verification>

<success_criteria>
- All four owned rows are cleared by collapsing a duplicate declaration, taking the live population from 16 to a measured 12, OR any row still standing carries a recorded reason a reader can check against source.
- The measured post-plan counts are read out of the real CLI and reported against this plan's predictions, not assumed from them.
- Zero findings gained, proved by a `(path, owner, key)` set difference in both directions against a pre-edit baseline.
- Each collapse ran a direction the architecture already permits, the permitting allow-list line is named, and all four fallow sub-gates pass with no zone, boundary rule or suppression edited.
- No production behaviour changes: every edit is a type-level collapse or a comment the collapse falsified, and the resolver still returns the projected shape its paired test deep-compares.
- `npm run check` is exit 0, the seven negative controls pass, native aggregate production unit coverage is 100 percent with no module below 100 percent, and both direct pin rows are unchanged.
- No contract entry, suppression, threshold, exclusion or assertion is weakened; the entry count stays 101 and every coordinate resolves.
- The regenerated record reconciles against a fresh digest with zero stale-source, stale-record, missing, duplicate, incomplete or invalid rows, and every re-keyed note was derived from the fresh report.
- Every task's `fails_when` counterexample is exercised and its assertion and coverage ledger row is complete.
</success_criteria>

<output>
Create `.planning/phases/06-unused-type-member-gate/06-17-SUMMARY.md` when done.
Record the four row ids with the collapse that cleared each, the direction each
collapse ran and why it is legal, the per-file before and after counts, the set
difference in both directions, whether the second anchor collapse was needed,
the `ResolveHookIfContext.cwd` reclassification, the contract confirmation, the
fresh digest and revision, the recorded disposition this plan judged
mis-assigned, the naming observation deferred on the `Like` suffix, and any row
deliberately left standing with its measured reason.
</output>
