---
phase: 06-unused-type-member-gate
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/check-unused-type-members.mjs
  - scripts/check-unused-type-members.contracts.mjs
  - scripts/check-unused-type-members.contracts.json
  - tests/scripts/check-unused-type-members.contracts.test.ts
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 29000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every exceptional member identifies its exact declaration and necessary contract purpose."
    - "External output acceptance proves an actual boundary transfer; unused external input mirrors do not inherit that acceptance."
    - "Stale, broad or redundant contracts fail and unread siblings remain detectable."
  artifacts:
    - path: scripts/check-unused-type-members.contracts.mjs
      provides: "Validated external and type-system contract engine implementation/evidence"
  key_links:
    - from: "scripts/check-unused-type-members.contracts.mjs"
      to: "tests/scripts/check-unused-type-members.contracts.test.ts"
      via: "Independently authored controls execute the real operation and assert exact observations and failures."
---
<objective>
Validated external and type-system contract engine.

Purpose: Every exceptional member identifies its exact declaration and necessary contract purpose.
Output: scripts/check-unused-type-members.contracts.mjs, scripts/check-unused-type-members.contracts.json, tests/scripts/check-unused-type-members.contracts.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Validate one exact external-output contract against a real transfer</name>
  <files>scripts/check-unused-type-members.contracts.mjs, scripts/check-unused-type-members.contracts.json, tests/scripts/check-unused-type-members.contracts.test.ts, scripts/check-unused-type-members.mjs</files>
  <behavior>
    A fixture output member transferred to a resolved external callback return is accepted; its unread sibling and the same type without a boundary path fail.
  </behavior>
  <action>
    Per D-05 and D-06, create a strict versioned contract loader/validator as a coherent independent module consuming the 06-01 model and directed evidence supplied by the analyzer. Use the contractEvaluator interface established by 06-01 with actual compiler symbols and directed edges, and exercise it through the real CLI during this task. Contract entries name exact project-relative declaration owner/member, purpose, category, boundary source site and resolved target, and upstream declaration or type-system proof. Start the repository contract file with no speculative allowances; live justified entries are added after Phase 5 in 06-06. Resolve identity from compiler declarations, not line strings alone; check path containment, keys, duplicates, missing/stale targets, wrong directions and redundant ordinary-read exceptions. Validate an external-output entry only when the member origin reaches the evidenced external return/output slot, including nested paths. Exact output contract plus actual transfer must pass the fixture report; sibling or disconnected declaration must fail.
    Wire the real contract validator from the CLI into the contractEvaluator interface established by 06-01. This makes the module a real production caller dependency immediately, keeps same-wave flow ownership disjoint, and runs the tracer through the real CLI as well as the contract API. Do not wait until 06-04 to make this module reachable; 06-04 adds whole-operation evidence to the existing pipeline.
  </action>
  <fails_when>Fails if upstream field existence alone accepts a local member, a contract applies by name/glob, or a missing boundary still passes.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.contracts.test.ts</automated></verify>
  <done>The first external-output fixture has a validated boundary witness, and all invalid/sibling variants reject.</done>
</task>

<task type="auto" tdd="true">
  <name>Preserve precise brands and type filters while requiring input purpose</name>
  <files>scripts/check-unused-type-members.contracts.mjs, tests/scripts/check-unused-type-members.contracts.test.ts</files>
  <behavior>
    An exact primitive/object unique-symbol brand and a genuine Extract selection literal retain their type-system role; ordinary siblings and superficial lookalikes fail. Unused external inputs need proven local necessity.
  </behavior>
  <action>
    Per D-05 and D-09, implement contract categories that distinguish output transfer, externally supplied input, nominal brand and type selection. External input must identify a local contract necessity such as an indispensable signature relationship proven against installed upstream declarations; mere upstream presence or an asserted callback cast is insufficient. Preserve the actual primitive string intersection with ambient unique symbol and exact object-scoping symbol, verifying their declaration/type shape and compile-time incompatibility proof without exempting ordinary properties. For Extract/conditional filter literals prove that checker-resolved selection/refinement affects the selected variant/type relationship; this is a type-system contract, never a runtime read of the filter property. Add controls for changed upstream member/signature, removed boundary, fake brand ordinary property, altered filter with no narrowing, stale/duplicate/wildcard/unknown-key entries, and an entry rendered redundant by a genuine read. Use strict AST/compiler evidence and independently specified expectations, with no generated member enumeration assertion.
  </action>
  <fails_when>Fails if every unique-symbol-looking property or Extract-shaped literal is accepted, an input mirror lacks local purpose, or a new unread sibling is covered.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.contracts.test.ts</automated></verify>
  <done>Type-system contracts retain the researched brands/refinements and reject unsupported external-input allowances or stale proofs.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands/results and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not generate expected observations from the analyzer's declaration enumeration. These tasks add analyzer/configuration/control behavior without production .ts edits. Record focused analyzer case/branch coverage and assertion strength; preserve all production coverage scripts, exclusions and direct pins. The orchestrator runs the native aggregate unit and direct gates once per stable integration wave, not once per minor task.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-03-T1 | A fixture output member transferred to a resolved external callback return is accepted; its unread sibling and the same type without a boundary path fail. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-03-T2 | An exact primitive/object unique-symbol brand and a genuine Extract selection literal retain their type-system role; ordinary siblings and superficial lookalikes fail. Unused external inputs need proven local necessity. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source/configuration/overlay text is parsed as data and must never execute. |
| Evidence to clean verdict | Symbol, flow and contract records must not manufacture observations or hide unexplained members. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch/configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-03-01 | Tampering | Candidate provenance and contract evidence | high | mitigate | Require exact declaration identity, directed origin/path witnesses and sibling/stale-evidence controls in the task suite. |
| T-06-03-02 | Elevation of privilege | Compiler inputs and subprocess controls | high | mitigate | Parse analyzed modules without importing/executing them; use argv arrays and contained temporary overlays, then verify cleanup. |
| T-06-03-03 | Denial of service | Recursive graph and malformed compiler input | medium | mitigate | Cycle-aware bounded worklists and explicit resource/setup diagnostics; a cutoff cannot return clean. |
| T-06-03-04 | Repudiation | Gate acceptance | medium | mitigate | Deterministic member-level reports, task assertion/coverage ledger and exact positive/negative process results. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run typecheck, lint and format checks on the completed wave; the orchestrator reuses one stable wave snapshot for unchanged native aggregate production unit coverage and direct pins. Tests must assert exact independently specified category, declaration/member and observation semantics, not successful enumeration. Track unsupported analysis explicitly. Live repository acceptance is deferred only to the scheduled post-Phase-5 reconciliation in 06-06; no intermediate fixture success claims the current production tree is clean.
</verification>

<success_criteria>
- Every exceptional member identifies its exact declaration and necessary contract purpose.
- External output acceptance proves an actual boundary transfer; unused external input mirrors do not inherit that acceptance.
- Stale, broad or redundant contracts fail and unread siblings remain detectable.
- Every task's fails_when counterexample is exercised, and its assertion/coverage ledger is complete.
- No production coverage threshold, direct pin or assertion contract is weakened.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-03-SUMMARY.md when done. Record implementation evidence, exact verification results, observed limits and any newly required bounded live-remediation plan IDs.
</output>
