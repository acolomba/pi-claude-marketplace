---
phase: 06-unused-type-member-gate
plan: "14"
type: execute
wave: 7
depends_on: ["06-07"]
files_modified:
  - extensions/pi-claude-marketplace/shared/errors-bridges.ts
  - extensions/pi-claude-marketplace/shared/errors.ts
  - extensions/pi-claude-marketplace/shared/notify-context.ts
  - extensions/pi-claude-marketplace/shared/notification-grammar.ts
  - tests/shared/errors-bridges.test.ts
  - tests/shared/errors.test.ts
  - tests/shared/notify-context.test.ts
  - tests/shared/notification-grammar.test.ts
  - scripts/check-unused-type-members.contracts.json
  - .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "The `shared` owner group reports zero unread members; the one remaining unread row under `shared/` is `shared/fs-utils.ts:225:32`, which belongs to 06-10."
    - "Every rendered message, error name, public field, frozen copy and cause chain is byte-identical before and after; no throw site changes what a user sees."
    - "No contract proof is widened and no contract entry is written that the real engine refused against this tree (D-05)."
    - "The regenerated triage reconciles against a freshly recomputed digest: zero stale-source, stale-record, missing, duplicate, incomplete or invalid rows for this owner's repairs."
    - "Aggregate production unit coverage is still 100 percent and every direct pin still holds (D-07)."
  artifacts:
    - path: extensions/pi-claude-marketplace/shared/errors-bridges.ts
      provides: "House-pattern ErrorOptions annotation and a single named coordinate shape, replacing four locally mirrored member declarations"
    - path: extensions/pi-claude-marketplace/shared/errors.ts
      provides: "The surplus Phase3Failure slot removed after its full call-site consequence is proved"
    - path: extensions/pi-claude-marketplace/shared/notify-context.ts
      provides: "Bounded type parameters the existing bounded-selection proof reaches, and a declared home for the single localized severity write"
    - path: extensions/pi-claude-marketplace/shared/notification-grammar.ts
      provides: "A presence filter re-expressed over the status discriminant the runtime map already keys on"
    - path: scripts/check-unused-type-members.contracts.json
      provides: "Only entries the real engine accepted against this tree for the repaired declarations"
    - path: .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md
      provides: "A freshly recomputed record in which this owner's ten rows are gone and the eleventh is still attributed to 06-10"
  key_links:
    - from: "extensions/pi-claude-marketplace/shared/errors-bridges.ts"
      to: "extensions/pi-claude-marketplace/bridges/commands/stage.ts"
      via: "Three BridgeStagingError construction sites whose observable behaviour must not move."
    - from: "extensions/pi-claude-marketplace/shared/errors-bridges.ts"
      to: "extensions/pi-claude-marketplace/bridges/commands/discover.ts"
      via: "Four CommandNameError sites, including the instanceof narrowing and the warning composer."
    - from: "extensions/pi-claude-marketplace/shared/notify-context.ts"
      to: "extensions/pi-claude-marketplace/orchestrators"
      via: "Twenty-one CommandContext instantiations in sixteen orchestrator files that must keep type-checking with no edit outside this owner."
    - from: "scripts/check-unused-type-members.contracts.json"
      to: ".planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md"
      via: "Every newly contracted row is accepted by the engine first and only then reconciled by a fresh inventory."
---
<objective>
Repair the ten unread type members the live triage attributes to the `shared` owner, taking that owner group from ten unread to zero without changing one byte of observable behaviour and without widening one proof.

Purpose: 06-08 cannot activate the mandatory gate while any member is unread. This is one of the six bounded owner-scoped repair plans 06-06 named; it owns `shared` and nothing else.
Output: four repaired `shared/` modules, their four paired suites, any contract entries the real engine accepted, and a regenerated triage record.

The eleven unread rows the triage lists under `### shared` are not eleven rows of this plan's work. `extensions/pi-claude-marketplace/shared/fs-utils.ts:225:32` (`RollbackReplacementInput.renamed.from`) is explicitly attributed to **owner plan 06-10**, because it is one dead field across four declaration sites that have to move together. It is OUT OF SCOPE here and `shared/fs-utils.ts` must not be edited by this plan.

The ten rows this plan owns, and the task that clears each:

| Row | Declaration | Member | Recorded disposition | Task |
| --- | --- | --- | --- | --- |
| R1 | `shared/errors-bridges.ts:27:26` | `AgentOwnershipConflictError.stagingFor` . `marketplace` | Public error field nobody reads; drop the frozen copy or name the consumer | T2 |
| R2 | `shared/errors-bridges.ts:27:56` | `AgentOwnershipConflictError.stagingFor` . `plugin` | Same row, second member | T2 |
| R3 | `shared/errors-bridges.ts:65:44` | `BridgeStagingError.options` . `cause` | Local mirror of the ambient options bag; annotate the parameter as the ambient type, as the house precedent already does | T1 |
| R4 | `shared/errors-bridges.ts:85:68` | `CommandNameError.options` . `cause` | Same mirror, second class | T1 |
| R5 | `shared/errors.ts:501:3` | `Phase3Failure` . `cause` | Unread slot on a locally declared shape; confirm surplus and remove, or name the missing reader | T2 |
| R6 | `shared/notification-grammar.ts:1534:46` | `isDescriptionBearingRow` . `description` | Selection by member presence, not by a discriminant; keep and accept, or narrow by a discriminant the proof can read | T4 |
| R7 | `shared/notify-context.ts:64:61` | `CommandContext.render` . `status` | Selection over an open type parameter; constrain it so the existing selection proof reaches it | T3 |
| R8 | `shared/notify-context.ts:269:45` | `notifyReconcileAppliedWithContext` . `status` | Same cluster note | T3 |
| R9 | `shared/notify-context.ts:323:29` | `arm` . `status` | Same cluster note | T3 |
| R10 | `shared/notify-context.ts:332:15` | `dispatchRow` . `severity` | Assignment target inside a cast literal; the write needs a declared home the compiler checks | T3 |

**This group is not uniformly mechanical, and the plan does not pretend it is.** 06-06 called *the two `ErrorOptions` mirrors* behaviour-free and already the house pattern, and that is exactly true of R3 and R4 — the ambient options type is structurally identical to the hand-written mirror, so T1 is a pure annotation swap. The other eight rows carry owner decisions: R1/R2 and R5 delete or re-home declarations that real tests assert on, and R6 through R10 change type-level shapes whose repair is only valid if the real contract engine accepts the resulting proof. T1 is therefore the tracer: it proves the whole repair-measure-record loop on the one row pair that cannot be wrong, before the eight that can.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.planning/phases/06-unused-type-member-gate/06-06-SUMMARY.md
@.planning/phases/06-unused-type-member-gate/06-07-SUMMARY.md
@extensions/pi-claude-marketplace/shared/errors-bridges.ts
@extensions/pi-claude-marketplace/shared/notify-context.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Conform the two locally mirrored options bags to the ambient house pattern, end to end</name>
  <files>extensions/pi-claude-marketplace/shared/errors-bridges.ts, tests/shared/errors-bridges.test.ts</files>
  <precondition>The working tree is clean and `node scripts/check-unused-type-members.mjs --json` reproduces the inherited baseline of 138 findings before any edit, 11 of them under `shared/`.</precondition>
  <behavior>
    - Constructing either error with an options bag carrying a cause still chains that cause; `causeChainTrailer` renders it exactly once.
    - Constructing either error with no options bag still yields an instance whose cause is absent, not null and not an own enumerable key.
    - `error.name`, `error.message` and every public readonly field are unchanged for every existing construction site.
    - The two member rows at `errors-bridges.ts:65:44` and `85:68` leave the unread set; no sibling row appears in their place.
  </behavior>
  <action>
    Clears R3 and R4. Per D-05 and the CONVENTIONS rule that a cause travels through the standard second argument of the base constructor, replace the hand-written options annotation on `BridgeStagingError` and on `CommandNameError` with the ambient options type the platform library already declares -- the annotation `PluginUpdatePhase3Error`, `ManualRecoveryError`, `StateLockHeldError`, `InvalidMarketplaceManifestError` and `PluginShapeError` in `shared/errors.ts` already use. The ambient type declares exactly one optional member of exactly the same type as the mirror, so this is an annotation swap with no widening, no narrowing and no runtime effect.

    Do not stop at the declaration. Read every construction and catch site before editing and confirm each still type-checks and still behaves identically: the three `BridgeStagingError` sites reached through `contextualStagingError` in `bridges/commands/stage.ts`, and the four `CommandNameError` sites in `bridges/commands/discover.ts` including the `instanceof` narrowing and the warning composer that reads the error's own typed fields. Those two bridge files belong to other owners and MUST NOT be edited; if either needs an edit to keep type-checking, the annotation swap was not behaviour-free -- stop and report rather than editing across the owner boundary.

    Strengthen the paired suite rather than adjusting it: the existing cases already pin name, message, frozen fields and the with-cause and without-cause shapes. Add the control that would have caught a widening -- a construction whose options bag carries an unexpected extra key must remain a compile error, expressed as a compile-time negative in the paired suite.

    This is the tracer, so carry the one repair through the whole loop before expanding: edit, run the paired and bridge suites, run the typecheck, re-run the member analysis, confirm exactly two rows left the unread set and nothing else moved, confirm no validated contract entry became readable (a contracted member turning readable fails the gate at exit 2 by name and means the entry must be retired, never that the proof should be widened), then commit. Commit messages follow Conventional Commits with no milestone or phase identifier, per the project's own contribution rules, and `SKIP=trufflehog pre-commit run --files <changed files>` must pass before the commit is attempted.
  </action>
  <fails_when>Fails if the mirror is deleted without an equivalent annotation, if any bridge file outside this owner is edited, if a construction site's rendered output or error name moves, if the paired suite is relaxed to accommodate the new annotation, or if the analysis is re-read from the previous report instead of re-run.</fails_when>
  <verify><automated>npm run typecheck && node --test tests/shared/errors-bridges.test.ts tests/bridges/commands/stage.test.ts tests/bridges/commands/discover.test.ts</automated></verify>
  <done>Both options bags carry the ambient annotation, all seven construction and catch sites are unchanged and still green, and a fresh analysis shows exactly R3 and R4 gone with no new finding and no contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Retire the two surplus declarations in the error modules, each only after its consequence is proved</name>
  <files>extensions/pi-claude-marketplace/shared/errors-bridges.ts, extensions/pi-claude-marketplace/shared/errors.ts, tests/shared/errors-bridges.test.ts, tests/shared/errors.test.ts</files>
  <behavior>
    - The ownership-conflict error still exposes a frozen coordinate whose members are unchanged, and mutating the caller's object after construction still cannot change the error's copy.
    - The refusal message still names the marketplace and plugin in the same order and the same bytes.
    - The aggregate phase-3 error still carries the same entries at runtime and the update family still reads each entry's originating error through its own narrower declaration.
    - Rows R1, R2 and R5 leave the unread set, and no member of the surviving declarations enters it.
  </behavior>
  <action>
    Clears R1, R2 and R5. Two different repairs, both governed by the standing instruction to remove a declaration only after proving its full contract and call-site consequence.

    R1 and R2, the ownership-conflict coordinate: the triage offers dropping the frozen copy, but the paired suite genuinely asserts that copy and genuinely asserts it is frozen against post-construction mutation of the caller's object, and `tests/bridges/agents/stage.test.ts` asserts it too. Deleting it would delete tested defensive behaviour, so do not delete it. The real defect is duplication: the very same marketplace-and-plugin shape is declared three times in this one file -- on the conflict interface's owner slot, on the error's public field, and again on the constructor parameter -- and only the conflict interface's copy is ever read, at the message-composing map in the constructor. Collapse all three onto one named readonly interface declared once in this module, so there is exactly one declaration of those two members and it is the one production already reads. This is the same repair shape 06-06 recorded for 06-09, where a published type becomes an alias of the row it duplicates, and it is behaviour-free: the frozen copy, the freeze assertion, the message bytes and every field stay exactly as they are. Confirm the constructor parameter becoming readonly breaks no caller, including the suite case that deliberately mutates its own object after handing it over.

    R5, the aggregate phase-3 failure slot: prove the absence of a reader before removing anything. The evidence to confirm is that the update family declares its own narrower entry shape by omitting this slot and restating it, that the only production read of an entry's originating error resolves to that narrower declaration, and that the shared declaration's own slot therefore has no reader anywhere in production. If that holds, remove the slot from the shared entry shape and update the compile-time proofs in `tests/shared/errors.test.ts` that spell it, keeping every one of those proofs at its current strength -- they exist to pin the closed phase vocabulary, and that is what they must still pin afterwards. If the evidence does not hold, do not remove it: record the reader the analysis is missing and report the row instead.

    Removing the slot leaves the update family's omit-and-restate expression vestigial. That expression lives in `orchestrators/plugin/update-swap.ts`, which carries two unread rows of its own and is already claimed in this same wave by owner plan 06-10, with 06-12 owning the rows in it. DO NOT edit it here: a same-wave edit to another plan's file is a collision, and the expression stays correct and still compiles once the slot is gone. Record it in this plan's SUMMARY as an explicit handoff to whichever of those two plans lands in that file, so the redundancy is simplified by its owner rather than left unnoticed.
  </action>
  <fails_when>Fails if the frozen copy or its freeze assertion is deleted, if the refusal message bytes move, if the surplus slot is removed without first proving no production reader resolves to it, if any compile-time phase-vocabulary proof is weakened rather than updated, or if any file under `orchestrators/` is edited.</fails_when>
  <verify><automated>npm run typecheck && node --test tests/shared/errors-bridges.test.ts tests/shared/errors.test.ts tests/bridges/agents/stage.test.ts tests/orchestrators/plugin/update-flow.test.ts</automated></verify>
  <done>One named coordinate shape is declared once and read by production, the surplus aggregate slot is gone with its consequence proved and recorded, and a fresh analysis shows R1, R2 and R5 gone with no new finding and no contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Give the command-context filters a bound the proof can read and the localized write a declared home</name>
  <files>extensions/pi-claude-marketplace/shared/notify-context.ts, tests/shared/notify-context.test.ts, scripts/check-unused-type-members.contracts.json</files>
  <behavior>
    - Every existing command context still pins against the shared shape unchanged, and a context missing a render arm for one of its own statuses is still a compile error at its pin site.
    - A producer pushing a row whose status the render map omits is still a compile error at the producer, in both directions of the stated exhaustiveness guarantee.
    - The out-of-band fallback row still stamps its severity, still degrades silently on a frozen row, and still renders the same self-describing diagnostic bytes.
    - Rows R7, R8, R9 and R10 leave the unread set, each either by having no declaration left to be unread or by an entry the real engine accepted.
  </behavior>
  <action>
    Clears R7, R8, R9 and R10. All four are type-level declarations with no runtime reader, so each repair is valid only if it is either a genuine removal of a redundant declaration or a proof the real contract engine accepts against this tree. Draft, run the engine, keep only what it accepts -- the method 06-06 used for all 81 live entries. An entry the engine refuses stays a finding and gets reported; it never gets accommodated by relaxing the proof (D-05).

    R7 and R9, the two selection filters over an open type parameter: the shared command-context shape and the internal row dispatcher both leave their message parameter unconstrained, so at the declaration nothing tells the compiler the filter discriminates. Constrain the message parameter to the plugin notification union the three public emitters already require it to extend. That makes the filter a selection over a bounded type parameter, which is precisely the proof 06-06 added, so draft the selection entries and keep them only if the engine accepts them. Sixteen orchestrator files instantiate the shared shape at twenty-one sites, two of them with the empty type; read every one before editing and confirm the bound holds at all of them. Those files belong to owner plan 06-12 in this same wave: if any instantiation needs an edit, the bound is wrong for this codebase -- stop, report, and leave the rows rather than editing across the owner boundary.

    R8, the status key inside the reconcile emitter's own parameter bound: this one differs from its three siblings, which are not findings. The three siblings each additionally thread rows through the distributive marketplace row type, so a real read transfers back to their bound; the reconcile emitter takes a prebuilt envelope instead and threads no rows, so nothing reaches its bound. Two repairs are available: bound the status parameter by the union's own status key so the intersection strictly narrows a slot the rest of it already declares and the refinement proof can reach it, or drop the intersection from that one bound if it is provably not load-bearing. Prefer the first: it adds evidence rather than removing a compile-time constraint. Take the second only with a compile-time negative control proving the dropped constraint guarded nothing, and never on the strength of the suite merely staying green.

    R10, the severity write: the only syntax naming this member writes to it, and a write is not a read (D-04). The cast literal is minted at the write site, so the compiler checks the write against a hand-written shape rather than against the declared field. Give the write a declared home by casting through a mutable view derived from the real declared row field instead of a freshly declared literal, so the write is type-checked against the actual declaration and no new member declaration is introduced. Preserve the surrounding contract exactly: the write stays inside its guard, the guard still swallows the rejection a frozen row raises, and the fallback still returns the same bytes. Confirm no new candidate appears at the new expression.

    Contract entries go in the repository contract file, which every repair plan in this wave may also touch. Append only; never reorder, reformat or remove an entry you did not add. If the file has moved under you, re-read it before writing.
  </action>
  <fails_when>Fails if a contract entry the engine refused is written anyway, if a proof is widened to absorb a refused draft, if any orchestrator file is edited, if a compile-time constraint is dropped without a negative control proving it guarded nothing, or if the fallback row's bytes or its frozen-row degradation change.</fails_when>
  <verify><automated>npm run typecheck && node --test tests/shared/notify-context.test.ts tests/shared/notification-dispatch.test.ts tests/orchestrators/reconcile/reconcile.messaging.test.ts tests/orchestrators/plugin/shared.test.ts && node --test tests/scripts/check-unused-type-members.contracts.test.ts</automated></verify>
  <done>The two selection filters carry a bound the engine proves, the reconcile bound is resolved by evidence or reported as refused, the severity write is checked against the real declaration, and a fresh analysis shows R7 through R10 gone with no new finding and no contract drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Re-express the presence filter over its real discriminant, then measure and record the delta</name>
  <files>extensions/pi-claude-marketplace/shared/notification-grammar.ts, tests/shared/notification-grammar.test.ts, scripts/check-unused-type-members.contracts.json, .planning/phases/06-unused-type-member-gate/06-LIVE-TRIAGE.md</files>
  <behavior>
    - Every rendered row is byte-identical: the description trailer appears on exactly the same statuses as before and on no others.
    - The totality guarantee survives: a new status variant still fails the check rather than silently losing its description line.
    - Row R6 leaves the unread set.
    - The `shared` owner group reports zero unread members; exactly one unread row remains under `shared/`, and it is the one attributed to 06-10.
  </behavior>
  <action>
    Clears R6 and closes the plan.

    R6: the description-bearing predicate narrows by member presence. An optional member is not a unit type, so it cannot tell the variants apart the way the selection proof requires, and the engine refuses it by name. The runtime side of the same predicate already keys on the status discriminant through a total map over the status union. Re-express the type-level side over that same discriminant so the predicate's narrowing and its runtime decision are derived from one source. Prove the replacement narrows to exactly the same set of variants as today, in both directions, with a compile-time assignability control rather than by observing that the suite stayed green -- an equivalence claim about a type is not evidence unless something would fail if it were false. Keep the map total over the status union; that totality is why a new variant fails the check today and it must still do so. Then draft the selection entry, run the real engine, and keep it only if accepted.

    Closure, after all four repairs are committed. Any edit under `extensions/` or `tests/` moves the source digest, so the recorded triage reads stale until it is rebuilt. Regenerate it with the audit's inventory mode, which rewrites the prose and carries every recorded disposition forward for every row that did not move, then run the audit's check mode and read its problem categories. The check exits non-zero by design while any member anywhere is unread -- the other five repair plans still owe theirs -- so the verdict this plan is accountable for is narrower and must be measured, not asserted:

    - the `shared` owner row of the population table reports unread 1, not 11;
    - the single remaining unread finding whose path is under `shared/` is `shared/fs-utils.ts:225:32`, owned by 06-10;
    - zero problems in the check output are stale-record, stale-source, missing, duplicate, incomplete or invalid;
    - the contract count moved by exactly the number of entries the engine accepted in T3 and T4, and no previously validated entry was removed or altered.

    Production source changed in this plan, so the coverage obligation is live rather than inherited: re-run the native aggregate unit coverage and the direct-pair pins, and record the measured function and branch totals against the inherited 100 percent baseline of 1,834 functions and 9,050 branches with no module below 100 percent. A pin that moves is repaired by covering the new path, never by lowering the pin (D-07). Record the before and after unread counts, per row, in the SUMMARY.
  </action>
  <fails_when>Fails if the rendered description trailer appears on a different set of statuses, if the map stops being total over the status union, if type equivalence is claimed without a control that would fail when it is false, if the triage is hand-edited instead of regenerated, if the closing counts are asserted rather than measured, or if a coverage pin is lowered.</fails_when>
  <verify><automated>npm run typecheck && node --test tests/shared/notification-grammar.test.ts tests/shared/notification-types.test.ts && node scripts/check-unused-type-members.audit.mjs --inventory && mkdir -p coverage && node scripts/check-unused-type-members.mjs --json > coverage/type-members.json; node --input-type=module -e "import {readFileSync} from 'node:fs'; const r = JSON.parse(readFileSync('coverage/type-members.json','utf8')); const s = r.findings.filter((f) => f.path.includes('/shared/')); if (s.length !== 1 || !s[0].path.endsWith('shared/fs-utils.ts')) { throw new Error(JSON.stringify(s)); }"</automated></verify>
  <done>The presence filter narrows by a real discriminant with an equivalence control, the regenerated record reconciles with zero stale or unexplained rows for this owner, and the measured `shared` unread count is 1 -- the 06-10 row -- down from 11.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands and results, the measured unread delta per row, and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not derive an expected observation from the analyzer's own report -- read the source. Unlike 06-06 and 06-07 this plan DOES edit production `.ts` files, so the aggregate production unit coverage and the direct-pair pins are live obligations here and must be re-measured and reported, not inherited from the stable wave boundary.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-14-T1 | Positive: with-cause and without-cause construction for both classes, name, message and public fields unchanged at all seven construction and catch sites. Negative: an unexpected extra key in the options bag stays a compile error. Absence: no bridge file outside this owner is edited, and the rendered cause trailer still appears exactly once. | Focused verify command above; record the exact rows that left the unread set, the unchanged contract count, and any direct-pin movement for `shared/errors-bridges.ts`. |
| 06-14-T2 | Positive: the frozen coordinate, its freeze against post-construction mutation, and the refusal message bytes all hold with one named shape. Negative: the surplus slot is removed only after the missing-reader evidence is recorded; the closed phase vocabulary proof still refuses an unknown phase. Absence: no `orchestrators/` file is edited and the vestigial expression is recorded as a handoff. | Focused verify command above; record rows cleared, and direct-pin movement for `shared/errors.ts` and `shared/errors-bridges.ts`. |
| 06-14-T3 | Positive: all twenty-one instantiations still pin unchanged; the fallback row still stamps severity and still degrades on a frozen row. Negative: a context missing a render arm and a producer pushing an undeclared status are both still compile errors. Absence: every contract entry written was accepted by the real engine; refused drafts stayed findings and were reported. | Focused verify command above plus the contract-engine suite; record accepted-versus-refused draft counts and direct-pin movement for `shared/notify-context.ts`. |
| 06-14-T4 | Positive: the description trailer renders on exactly the same statuses; the map stays total. Negative: a compile-time control fails if the re-expressed narrowing is not equivalent to today's. Absence: the closing counts come from a regenerated record and a fresh analysis, not from the previous report. | Full aggregate unit coverage and direct pins re-run and reported against the 1,834-function, 9,050-branch, no-module-below-100-percent baseline. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repair to observable behaviour | A type-level repair must not move any rendered byte, error identity, frozen copy or cause chain a user or a caller can observe. |
| Repair to evidence | A member must leave the unread set because it is genuinely gone or genuinely proved, never because a proof was widened or a record hand-edited. |
| Owner scope to the rest of the tree | Five sibling repair plans run in the same wave; an edit outside this owner's files is a collision as well as a scope breach. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14-01 | Tampering | Contract file and triage record | high | mitigate | Every entry must be accepted by the real engine against this tree before it is written; the record is regenerated by the audit, never hand-edited; append-only discipline on the shared contract file. |
| T-06-14-02 | Repudiation | The claimed ten-to-zero delta | high | mitigate | The closing counts are measured from a fresh analysis and a regenerated record, asserted by a verify command that fails unless exactly one `shared/` finding remains and it is the 06-10 row. |
| T-06-14-03 | Denial of service | Rendered output and the single notify seam | medium | mitigate | The severity write keeps its guard and its frozen-row degradation; a throw must never escape the single output seam. Byte-level controls on the description trailer and the fallback row. |
| T-06-14-04 | Tampering | Cross-owner files in the same wave | medium | mitigate | Named prohibitions on editing `shared/fs-utils.ts`, `bridges/commands/*`, `bridges/agents/*` and `orchestrators/*`; each task stops and reports instead of reaching across an owner boundary. |
| T-06-14-05 | Information disclosure | Error messages and public error fields | low | accept | The repairs touch annotations and declaration homes only; no message, field or cause value is added, removed or re-worded, so no new information can reach a user. |

No package installation is planned; use the installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run `npm run check` at plan end and require exit 0 -- typecheck, lint, all four fallow sub-gates, format check, both corresponding-test gates, the direct-coverage negative gate, the unit suite and the integration suite. Run `SKIP=trufflehog pre-commit run --files <changed files>` before every commit attempt and fix, restage and re-run until clean; a failed hook means the commit did not happen, so never recover with an amend. Commit messages follow Conventional Commits with no milestone or phase identifier.

Because production `.ts` files change here, also re-run the native aggregate unit coverage and the direct-pair pins and report the measured totals. Live closure of this owner requires the regenerated record to show zero unread members for `shared` other than the single `shared/fs-utils.ts` row owned by 06-10, and zero stale, missing, duplicate, incomplete or invalid rows arising from these repairs. Whole-phase closure remains 06-08's, after all six repair plans land.

Two files in `files_modified` are shared with the other repair plans in this wave: the contract file and the triage record. Treat both as append-or-regenerate only, re-read them immediately before writing, and never reorder or remove an entry or a disposition this plan did not create.
</verification>

<success_criteria>
- The `shared` owner group reports zero unread members; exactly one unread row remains under `shared/` and it is `shared/fs-utils.ts:225:32`, attributed to 06-10. The delta is 11 to 1 for the path prefix, 10 to 0 for this plan's ownership, and it is measured from a fresh analysis rather than asserted.
- Every rendered byte, error name, public field, frozen copy and cause chain is unchanged; no construction site, catch site or context pin outside this owner needed an edit.
- No contract proof was widened, no refused draft was written, and no previously validated entry was removed or altered; any row the engine refused is reported as a finding with its refusal reason rather than laundered.
- The regenerated record reconciles against a freshly recomputed digest with zero stale, missing, duplicate, incomplete or invalid rows arising from these repairs.
- Aggregate production unit coverage is re-measured at 100 percent with no module below 100 percent, and every direct pin holds without being lowered.
- Every task's fails_when counterexample is exercised, and its assertion and coverage ledger row is complete.
- `npm run check` exits 0 on the final tree.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-14-SUMMARY.md when done. Record the per-row before and after status, the exact verification results, the accepted-versus-refused contract draft counts, the re-measured coverage totals, the handoff for the vestigial omit expression in `orchestrators/plugin/update-swap.ts` (claimed this wave by 06-10, rows owned by 06-12), and any row whose recorded disposition proved unsafe to follow, with the evidence for leaving it.
</output>
