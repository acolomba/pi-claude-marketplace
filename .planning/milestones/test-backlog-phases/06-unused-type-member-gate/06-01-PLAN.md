---
phase: 06-unused-type-member-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/check-unused-type-members.mjs
  - scripts/check-unused-type-members.analysis.mjs
  - tests/scripts/check-unused-type-members.test.ts
  - scripts/check-unused-type-members.model.mjs
  - tests/scripts/check-unused-type-members.model.test.ts
autonomous: true
requirements: [MEMBER-01, MEMBER-02]
estimate:
  tokens: 31000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The CLI rejects an unread optional member using its exact declaration identity."
    - "Production and test observations are separate and declaration-only syntax supplies no read."
    - "Compiler setup failures and unsupported candidate operations cannot return a clean verdict."
  artifacts:
    - path: scripts/check-unused-type-members.mjs
      provides: "Compiler inventory and runtime observation classifier implementation/evidence"
    - path: scripts/check-unused-type-members.analysis.mjs
      provides: "Compiler inventory and runtime observation classifier implementation/evidence"
    - path: scripts/check-unused-type-members.model.mjs
      provides: "Compiler inventory and runtime observation classifier implementation/evidence"
  key_links:
    - from: "scripts/check-unused-type-members.mjs"
      to: "tests/scripts/check-unused-type-members.test.ts"
      via: "Independently authored controls execute the real operation and assert exact observations and failures."
---
<objective>
Compiler inventory and runtime observation classifier.

Purpose: The CLI rejects an unread optional member using its exact declaration identity.
Output: scripts/check-unused-type-members.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.test.ts, scripts/check-unused-type-members.model.mjs, tests/scripts/check-unused-type-members.model.test.ts.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-test-backlog/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-unused-type-member-gate/06-CONTEXT.md
@.planning/phases/06-unused-type-member-gate/06-RESEARCH.md
@.planning/phases/06-unused-type-member-gate/06-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.agents/skills/typescript-google-style-review/SKILL.md

</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Trace one optional member from compiler input to CLI verdict</name>
  <files>scripts/check-unused-type-members.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.test.ts, package.json</files>
  <behavior>
    An interface member without a read yields status 1 and its exact unread diagnostic; adding a real dot read yields status 0. A missing tsconfig yields status 2, not a member violation.
  </behavior>
  <action>
    Per D-01, D-06 and D-07, begin with independently authored temporary-project fixtures and real child-process CLI tests, then implement a parser-only entry point and analyzeProject pipeline using installed TypeScript and the project's resolved tsconfig. Candidate roots are non-declaration production .ts files under extensions/pi-claude-marketplace; tests contribute observations and never candidate declarations. CLI accepts --root, --json and an optional --overlay JSON file mapping existing project-relative TypeScript paths to replacement source text. Validate paths remain inside the project and reject unknown options, malformed overlay/config, unresolved compiler inputs and empty production/candidate inventories. The overlay is a CompilerHost read override, never an on-disk production edit. Reports include versioned schema, status, relative paths, owner/key/span, observation provenance and diagnostics. Choose exit 0 clean, 1 unread/unsupported member, 2 setup/internal analysis failure. Imports remain inert: importing the analyzer never invokes main, and analyzed source never runs. This slice handles one real direct-read path through compiler, classifier, report and CLI; expansion adds the rest without returning success for encountered unsupported candidate syntax. Follow the existing tests/scripts/check-phase-06-hub-ledger.test.ts convention for a documented .mjs import typing boundary and explicit test-facing interface if needed; do not loosen tsconfig or ESLint. Keep analyzer exports limited to real module composition and tested public operations.
    Add the lint:type-members package alias now so the executable has a real package-script entry; mandatory npm check activation remains 06-08 after live closure. Establish analyzeProject's contractEvaluator injection interface now: it receives the compiled program, exact model and directed transfer graph and returns validated contract decisions/diagnostics; absent evaluator grants no exceptions. The CLI will supply the real validator in 06-03 without changing this interface or flow ownership.
  </action>
  <fails_when>Fails if declarations or type references satisfy the offender, the CLI always passes/fails, invalid setup looks like a valid finding, or any analyzed top-level source executes.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.test.ts</automated></verify>
  <done>The same well-typed optional-member fixture fails before a real read and passes after; malformed setup is distinguishable.</done>
</task>

<task type="auto" tdd="true">
  <name>Inventory exact symbols and classify complete read/write syntax</name>
  <files>scripts/check-unused-type-members.model.mjs, scripts/check-unused-type-members.analysis.mjs, tests/scripts/check-unused-type-members.model.test.ts</files>
  <behavior>
    Distinct same-spelling members remain distinct; indexed access/keyof/type queries do not read. Binding and assignment destructuring read sources; simple writes/delete do not, while compound/update expressions do.
  </behavior>
  <action>
    Per D-02, D-03 and D-04, move coherent compiler identity/inventory and AST observation logic into model.mjs. Inventory interface and type-literal members, methods, nested records/arrays and anonymous parameter/return shapes with declaration span, owner, literal/unique-symbol key and optionality. Normalize alias/root/merged/instantiated symbols through checker APIs and retain original candidate provenance; never join by spelling or structural assignability. Classify AST context before reference flags. Cover dot/optional/literal and finite-union computed access, renamed/default/nested binding and assignment destructuring, compound/logical/update read-write operations, assignment/delete/loop targets and receiver/key reads, object initializers, shorthand source expressions, and value operands of as/satisfies. Type nodes, import types and declarations contribute no runtime witness. Exact in/own-property observations may satisfy the member-existence predicate, recorded as presence observations distinct from value reads; key enumeration alone supplies neither per-member value credit nor an all-member exemption. Preserve type-filter literal and nominal-brand candidates for precise contract validation in 06-03 rather than dropping their syntax. Emit candidate-local unsupported reasons for unresolved keys or identity-erasing casts.
  </action>
  <fails_when>Fails if same-name or declaration-only syntax creates a witness, nested write receiver reads disappear, or an unsupported computed access credits every member.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.model.test.ts tests/scripts/check-unused-type-members.test.ts</automated></verify>
  <done>Every classifier case has exact independent positive and negative outcomes with production/test attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Make reports deterministic, bounded and fail-closed</name>
  <files>scripts/check-unused-type-members.analysis.mjs, scripts/check-unused-type-members.model.mjs, tests/scripts/check-unused-type-members.test.ts, tests/scripts/check-unused-type-members.model.test.ts</files>
  <behavior>
    Removing the sole test read changes test-only-observed to unread; adding an independent production read yields runtime-observed. Budget exhaustion and unknown relevant provenance produce actionable failures.
  </behavior>
  <action>
    Per D-06 and D-07, finalize the shared analysis contract used by later modules: candidate records keyed by stable declaration identity; origin/path-bearing value nodes; directed transfers; observation records carrying source site and production/test origin; validated-contract decisions; unsupported reasons. Classify final results as runtime-observed, test-only-observed, explicit-contract, unread or unsupported-analysis, retaining evidence for each. Independent proven observation settles that candidate's may-observe question; opaque paths only block candidates lacking a separate witness. Cache compiler identities and use explicit worklist/resource budgets, deterministic ordering and cycle handling; budget exhaustion reports affected provenance or analysis failure and never returns a partial clean result. Add a fixture with a top-level throwing/write-producing expression and prove analysis does not execute it. Document the bounded may-observe claim and supported syntax in CLI help; do not claim branch execution or useful assertions from a property read.
  </action>
  <fails_when>Fails if test-only provenance is promoted to production, cutoff is clean, an unrelated dynamic expression poisons all candidates, or reports omit witness locations.</fails_when>
  <verify><automated>node --test tests/scripts/check-unused-type-members.test.ts tests/scripts/check-unused-type-members.model.test.ts</automated></verify>
  <done>Deterministic reports explain each accepted member and fail for unresolved relevant analysis or setup gaps.</done>
</task>

</tasks>

<assertion_coverage_ledger>
Before each task, record its independently authored expectations and the behavior that would fail if weakened. On completion record exact commands/results and any coverage delta in this plan's SUMMARY; use the same IDs below. Do not generate expected observations from the analyzer's declaration enumeration. These tasks add analyzer/configuration/control behavior without production .ts edits. Record focused analyzer case/branch coverage and assertion strength; preserve all production coverage scripts, exclusions and direct pins. The orchestrator runs the native aggregate unit and direct gates once per stable integration wave, not once per minor task.

| Task | Assertion and regression evidence | Coverage evidence |
| --- | --- | --- |
| 06-01-T1 | An interface member without a read yields status 1 and its exact unread diagnostic; adding a real dot read yields status 0. A missing tsconfig yields status 2, not a member violation. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-01-T2 | Distinct same-spelling members remain distinct; indexed access/keyof/type queries do not read. Binding and assignment destructuring read sources; simple writes/delete do not, while compound/update expressions do. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
| 06-01-T3 | Removing the sole test read changes test-only-observed to unread; adding an independent production read yields runtime-observed. Budget exhaustion and unknown relevant provenance produce actionable failures. | Focused verify command above; record covered positive, negative, unsupported and failure paths, preserved assertions, and source coverage impact. |
</assertion_coverage_ledger>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Compiler input to analyzer | Source/configuration/overlay text is parsed as data and must never execute. |
| Evidence to clean verdict | Symbol, flow and contract records must not manufacture observations or hide unexplained members. |
| Gate process to quality checks | Intended findings must remain distinguishable from launch/configuration failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-01-01 | Tampering | Candidate provenance and contract evidence | high | mitigate | Require exact declaration identity, directed origin/path witnesses and sibling/stale-evidence controls in the task suite. |
| T-06-01-02 | Elevation of privilege | Compiler inputs and subprocess controls | high | mitigate | Parse analyzed modules without importing/executing them; use argv arrays and contained temporary overlays, then verify cleanup. |
| T-06-01-03 | Denial of service | Recursive graph and malformed compiler input | medium | mitigate | Cycle-aware bounded worklists and explicit resource/setup diagnostics; a cutoff cannot return clean. |
| T-06-01-04 | Repudiation | Gate acceptance | medium | mitigate | Deterministic member-level reports, task assertion/coverage ledger and exact positive/negative process results. |

No package installation is planned; use installed TypeScript and Node.
</threat_model>

<verification>
Run each task's automated checks. Run typecheck, lint and format checks on the completed wave; the orchestrator reuses one stable wave snapshot for unchanged native aggregate production unit coverage and direct pins. Tests must assert exact independently specified category, declaration/member and observation semantics, not successful enumeration. Track unsupported analysis explicitly. Live repository acceptance is deferred only to the scheduled post-Phase-5 reconciliation in 06-06; no intermediate fixture success claims the current production tree is clean.
</verification>

<success_criteria>
- The CLI rejects an unread optional member using its exact declaration identity.
- Production and test observations are separate and declaration-only syntax supplies no read.
- Compiler setup failures and unsupported candidate operations cannot return a clean verdict.
- Every task's fails_when counterexample is exercised, and its assertion/coverage ledger is complete.
- No production coverage threshold, direct pin or assertion contract is weakened.
</success_criteria>

<output>
Create .planning/phases/06-unused-type-member-gate/06-01-SUMMARY.md when done. Record implementation evidence, exact verification results, observed limits and any newly required bounded live-remediation plan IDs.
</output>
