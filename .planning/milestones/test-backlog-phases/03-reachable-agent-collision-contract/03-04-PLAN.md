---
phase: 03-reachable-agent-collision-contract
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - tests/orchestrators/plugin/update-swap.test.ts
  - tests/orchestrators/plugin/reinstall-flow.test.ts
  - tests/bridges/agents/stage.test.ts
autonomous: true
requirements: [AGENT-01, AGENT-02]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Updating an installed prefixed agent replaces its old indexed filename and state resource name with the full-source name.
    - Reinstalling the same plugin version performs the same migration and remains safe to repeat.
    - When both source identities exist, migration installs both and assigns each target the correct source contents.
    - Other owners and unrelated content at newly named targets survive failed migrations.
    - Reinstall rollback restores the previous file bytes, index, and state after a later failure.
  artifacts:
    - path: tests/orchestrators/plugin/update-swap.test.ts
      provides: Real update migration and commit failure recovery evidence
    - path: tests/orchestrators/plugin/reinstall-flow.test.ts
      provides: Same-version migration, rollback, and unrelated-target preservation evidence
    - path: tests/bridges/agents/stage.test.ts
      provides: Direct replacement migration and rollback ownership assertions
  key_links:
    - from: swapPluginUpdate
      to: commitPreparedAgents
      via: old index paths reconciled with staged full-source names and recorded resource names
    - from: reinstallPlugin
      to: replacePreparedAgents and rollbackAgentsReplacement
      via: replacement backup transaction and state-save failure rollback
---

<objective>
Prove the approved owned-name migration through real update and reinstall, including coexistence, rollback, and occupied destinations, per D-01, D-02, and D-04. Exercise existing public lifecycle paths with the naming and safety implementation from 03-02.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/03-reachable-agent-collision-contract/03-CONTEXT.md
@.planning/phases/03-reachable-agent-collision-contract/03-RESEARCH.md
@.planning/phases/03-reachable-agent-collision-contract/03-VALIDATION.md
@.agents/skills/typescript-unit-testing-review/SKILL.md

CodeGraph-grounded seams: update-swap.test.ts invokes preparePluginUpdate and swapPluginUpdate and uses the existing update-flow.test.ts seedPathMarketplace/withHermeticHome fixtures. seedPathMarketplace already accepts agents with frontmatterName and agentDirectories. reinstall-flow.test.ts already seeds full installed resources and has PDEF-01 rollback and PRL-10 state-save failure cases. Extend those concern-local fixtures in place only if needed. agents/stage.test.ts owns the public prepare/commit/replace/rollback bridge functions. No new source export, index schema, or lifecycle API is required.
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Migrate a seeded old agent installation through the real update swap</name>
  <files>tests/orchestrators/plugin/update-swap.test.ts</files>
  <behavior>
    - A version-changing update of hello-reviewer replaces pi-claude-marketplace-hello-reviewer.md with pi-claude-marketplace-hello-hello-reviewer.md and records the full new index/state.
    - If reviewer is also present in the new plugin, both new source identities exist; the old short target is correctly reused for reviewer with its own contents.
    - An unrelated occupied new target fails the agents commit before old agent deletion; the old agent index/resource record and foreign bytes remain intact.
    - A subsequent valid update retries successfully after the external obstacle is removed by the test owner.
  </behavior>
  <action>Per D-01, D-02, and D-04, seed a genuine legacy state and agents-index row with the old generated name, complete provenance-bearing target bytes, and a prefixed sourceAgent value. Use literal legacy names rather than calling the new naming helper to generate old expectations. Drive preparePluginUpdate then swapPluginUpdate against a local plugin with a changed version so the update cannot short-circuit as up-to-date. Add AGENT-01 cases for prefixed-only migration, coexistence with the unprefixed source, a foreign file at the new name, and retry after the test removes that obstacle. Assert complete outcomes, complete index documents, full affected state records/resources, complete generated file bytes, old target removal when no new source reuses it, and unchanged other-owner sentinel files/rows. For the failure case assert update's actual phaseFailures and failed-bridge resource retention; update commits components independently and is not a full reinstall backup transaction. Preserve its existing operation-ledger and warning/error shape. Do not declare an absent old path when it is legitimately reused by the unprefixed source. All expectations are written independently of production serializers.</action>
  <verify><automated>node --test --test-name-pattern='AGENT-01' tests/orchestrators/plugin/update-swap.test.ts</automated></verify>
  <done>The public update swap proves complete legacy-name migration, correct coexistence, preservation on occupied targets, and successful retry.</done>
</task>
<task type="auto" tdd="true">
  <name>Prove same-version reinstall migration and exact rollback</name>
  <files>tests/orchestrators/plugin/reinstall-flow.test.ts, tests/bridges/agents/stage.test.ts</files>
  <behavior>
    - Same-version reinstall migrates a legacy prefixed agent and repeating reinstall leaves the same complete agent identity/index state.
    - Coexisting prefixed and unprefixed sources retain both installed identities with correct body/provenance ownership.
    - A foreign file or another indexed owner at a newly named target blocks replacement and preserves old files, other owners, index, and state.
    - A failure after agent replacement restores exact old target bytes, old index text, and state, and removes newly introduced targets.
    - Reinstall's existing forced replacement of previous indexed plugin content still behaves as specified, including rollback restoration of those bytes.
  </behavior>
  <action>Per D-01, D-02, D-04, and D-05, add AGENT-01 cases to the existing reinstall owner using its real seeded installation and existing post-replacement/state-save failure fixtures. Seed old names literally and keep the current version unchanged to prove reinstall performs migration without an update. Compare complete results, source provenance, generated bytes, full index rows, and state resource names; repeat the operation and assert stable contents and resource identity. Add separate occupied-new-target and cross-index-owner cases; force:true applies to previous indexed targets and must not authorize a newly claimed foreign path. Retain the existing RINST-01 overwrite expectations. In stage.test.ts add direct real prepare→replace→rollback migration cases, including the coexistence case where one new source reuses the old target; compare complete prior index text and bytes after rollback and absence of doubled-prefix targets. Invoke public replacement operations rather than fabricated duplicate or prepared arrays. Use existing public failure seams and real temporary files; do not introduce test-only exports, object-accessor traps, global mocks, or extracted one-function production helpers. These tests validate the existing replacement implementation; if a test exposes an actual lifecycle defect, fix it narrowly in the owning production module with its direct pair, report the concrete deviation, and preserve all unrelated transaction behavior.</action>
  <verify><automated>node --test --test-name-pattern='AGENT-01|RINST-01' tests/orchestrators/plugin/reinstall-flow.test.ts tests/bridges/agents/stage.test.ts</automated></verify>
  <done>Same-version and repeated reinstall migrate correctly, unrelated new-target content is protected, and later failures restore the exact previous installation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Legacy ownership data to newly generated names | A new target may be unrelated content or another owner's artifact. |
| Filesystem replacement to state persistence | A failure can occur after files have moved but before state is saved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-03-04-01 | Tampering | update/reinstall migration | high | mitigate | Real lifecycle assertions cover newly occupied targets, indexed foreign owners, and unchanged full bytes/index rows. |
| T-03-04-02 | Denial of service | replacement rollback | high | mitigate | Direct replacement and state-save failure cases prove restoration of old targets/index/state and removal of new names. |
| T-03-04-03 | Spoofing | reused old basename | medium | mitigate | Coexistence tests assert complete source provenance and bytes so a swapped identity cannot pass. |
</threat_model>
<verification>
Run full owners without name filters after focused cases. Run npm run check and direct coverage for every changed production owner from 03-02/03-03, then env -u PI_CODING_AGENT_DIR npm run test:coverage:unit. Confirm exactly covered production lines/functions/branches from coverage/unit.lcov, all full assertions preserved, and the existing two unrelated direct pins unchanged. New AGENT-01 cases must actually execute in each focused command. Record every failure honestly; distinguish environment failures from product regressions using the established file-descriptor capture if process-pipe output is unreliable.
</verification>
<success_criteria>All migration, coexistence, occupied-target, retry, and rollback cases pass; complete quality checks pass; aggregate unit production coverage remains exactly 100%.</success_criteria>
<output>Create .planning/phases/03-reachable-agent-collision-contract/03-04-SUMMARY.md and record final phase evidence against 03-VALIDATION.md.</output>
