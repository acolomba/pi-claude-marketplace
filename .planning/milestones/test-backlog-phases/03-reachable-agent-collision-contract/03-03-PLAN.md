---
phase: 03-reachable-agent-collision-contract
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - extensions/pi-claude-marketplace/bridges/agents/discover.ts
  - extensions/pi-claude-marketplace/bridges/agents/types.ts
  - tests/bridges/agents/discover.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/orchestrators/plugin/install-flow.test.ts
  - tests/orchestrators/plugin/update-flow.test.ts
  - tests/orchestrators/plugin/reinstall-flow.test.ts
  - docs/prd/pi-claude-marketplace-prd.md
autonomous: true
requirements: [AGENT-01, AGENT-02]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - The first discovered exact source name remains active within and across the supported agent directories.
    - Every duplicate warning identifies both full source paths and the retained source.
    - Install, update, and reinstall preserve the complete duplicate diagnostic at their public outcome surfaces.
    - AG-12, RN-1, RN-6, and live source comments describe the implemented exact-name contract.
  artifacts:
    - path: extensions/pi-claude-marketplace/bridges/agents/discover.ts
      provides: Single exact-name duplicate decision and complete warning
    - path: tests/bridges/agents/discover.test.ts
      provides: Real same-directory and cross-directory exact duplicate cases
    - path: docs/prd/pi-claude-marketplace-prd.md
      provides: Reconciled agent identity and collision contract
  key_links:
    - from: duplicateWarning
      to: discoverPluginAgents
      via: retained DiscoveredAgent and incoming source path
    - from: discovery warnings
      to: prepareStagePluginAgents and lifecycle outcomes
      via: existing ordered warning propagation
---

<objective>
Make the live duplicate outcome and its complete diagnostic agree across discovery, staging, lifecycle operations, and the PRD, per D-03 and D-05. Preserve distinct source identities delivered by 03-02.
</objective>
<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@AGENTS.md
@CLAUDE.md
@.planning/phases/03-reachable-agent-collision-contract/03-CONTEXT.md
@.planning/phases/03-reachable-agent-collision-contract/03-RESEARCH.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md
@.claude/rules/typescript-comments.md

Discovery remains directory-based: agentsDirs in resolver order and filenames sorted by the existing localeCompare comparator. The researched upstream incumbent behavior supports exact-name duplicate tolerance; Claude's custom-file array has a different winner and is not implemented here.
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Expose a same-directory duplicate winner from real discovery through staging</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/discover.ts, tests/bridges/agents/discover.test.ts, tests/bridges/agents/stage.test.ts</files>
  <behavior>
    - Two real markdown files with the same frontmatter name and different bodies return the first discovered row, one complete warning, and the winner's staged bytes.
    - Cross-directory duplicates retain the directory-order incumbent; reversing directory input changes the winner and the complete warning consistently.
    - Distinct prefixed/unprefixed source names produce both records and no duplicate warning.
    - Filename fallback participates in the same exact-name duplicate rule.
  </behavior>
  <action>Per D-03 and D-05, retain the existing traversal and first-discovered skip but retrieve the incumbent DiscoveredAgent from the map when a duplicate is found. Change the private warning formatter to accept the incumbent and incoming source name/path. Use the complete format: agent source "INCOMING_NAME" at "INCOMING_PATH" duplicates generated name "GENERATED_NAME" already produced by agent source "WINNING_NAME" at "WINNING_PATH"; keeping first discovered source. Substitute real full paths and names. Update discovery's header, public contract, and inline decision comments to describe exact-name duplicates within and across directory inputs, with discovery as the policy owner. A map keyed by the generated name remains valid because full-source generation is injective within this plugin. Add independent full-result expectations for same-directory duplicates, directory-order reversal, and filename fallback. Extend the stage owner to assert complete results, winner body/provenance, and warning order; update the existing multi-warning case without weakening any fields. Keep symlink, dotfile, raw-byte hash, invalid-name, and tolerant missing-directory controls unchanged.</action>
  <verify><automated>node --test tests/bridges/agents/discover.test.ts tests/bridges/agents/stage.test.ts</automated></verify>
  <done>The complete same-directory duplicate behavior is proven through real files and staging, and no diagnostic falsely describes the duplicate as a normalization collision.</done>
</task>
<task type="auto" tdd="true">
  <name>Carry the complete diagnostic through install, update, and reinstall</name>
  <files>tests/orchestrators/plugin/install-flow.test.ts, tests/orchestrators/plugin/update-flow.test.ts, tests/orchestrators/plugin/reinstall-flow.test.ts</files>
  <behavior>
    - Existing declared-directory lifecycle cases preserve the full winning and losing source paths in their expected outcome notes or notifications.
    - A fresh install with prefixed and unprefixed agent names produces both complete resource entries and generated files.
    - Duplicate selection still uses the adapter's current directory order and preserves warning ordering with other component warnings.
  </behavior>
  <action>Per D-01, D-03, D-04, and D-05, update the existing PDEF-01 agent duplicate scenarios in all three flow owners to assert the new full warning strings and keep their existing complete outcomes and filesystem checks. Add a real fresh-install case for both exact source identities in install-flow.test.ts using its concern-local fixtures. Assert both installed contents/provenance, complete agents-index rows, and state resource names with independent expected values. Match source fixtures to their actual resolved order; do not equate directory precedence with upstream custom-file semantics. Do not introduce shared helpers or test-only production exports. Keep notifications through the existing UI boundary and preserve severity/ordering expectations.</action>
  <verify><automated>node --test --test-name-pattern='PDEF-01|AGENT-01' tests/orchestrators/plugin/install-flow.test.ts tests/orchestrators/plugin/update-flow.test.ts tests/orchestrators/plugin/reinstall-flow.test.ts</automated></verify>
  <done>All existing agent directory scenarios and the new AGENT-01 fresh-install case assert complete diagnostics and installed identities.</done>
</task>
<task type="auto">
  <name>Reconcile the agent naming and duplicate contracts</name>
  <files>docs/prd/pi-claude-marketplace-prd.md, extensions/pi-claude-marketplace/bridges/agents/types.ts</files>
  <action>Per D-01 through D-05, update AG-12 to retain the first discovered exact source name in the supported directory adapter, skip later exact duplicates, and warn with both full paths and the winner. State the adapter's actual directory-order and deterministic filename traversal without promising Claude's lexical ordering or custom-file behavior. Update RN-1's agent clause to append the complete source name, retaining the current skill/command clauses. Update RN-6's agent cross-reference to distinguish exact duplicates from distinct full source names. Add the update/reinstall indexed-name migration behavior and newly claimed target preservation to the agent contract without changing schemaVersion. Preserve the existing reinstall policy permitting overwrite of previous indexed plugin targets. Replace the stale install-collision hard-error entry in the PRD testing inventory with the reachable duplicate and distinct-name cases. Update DiscoveredAgent.generatedName's comment to describe the complete source. Scope edits to these contracts and current comments; do not alter historical archives or unrelated requirements. Read existing files first and retain product terminology.</action>
  <verify><automated>npm run typecheck && npm run lint && npx prettier --check docs/prd/pi-claude-marketplace-prd.md extensions/pi-claude-marketplace/bridges/agents/types.ts</automated></verify>
  <done>AG-12, RN-1, RN-6, the testing inventory, and live agent type comments agree with the real tested behavior.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Plugin markdown to discovery | Source files supply identities and paths reported in diagnostics. |
| Bridge warnings to lifecycle output | Users rely on the diagnostic to identify the retained file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-03-03-01 | Tampering | discoverPluginAgents | high | mitigate | Preserve plain-markdown, symlink, safe-name, and raw-byte-hash checks while changing duplicate warnings. |
| T-03-03-02 | Repudiation | duplicateWarning | medium | mitigate | Assert complete incumbent and incoming source paths and retained identity through discovery, stage, and lifecycle output. |
| T-03-03-03 | Information disclosure | local diagnostic paths | low | accept | Full paths are the approved local-user diagnostic; no remote transmission is introduced. |
</threat_model>
<verification>
Run the full affected flow owners after focused checks, direct coverage for agents/discover.ts and agents/stage.ts, npm run typecheck, npm run lint, and env -u PI_CODING_AGENT_DIR npm run test:coverage:unit. Compare all production LCOV totals and complete assertions. Inspect the scoped documentation diff for all three contract IDs and the stale test-inventory correction. No new packages are needed.
</verification>
<success_criteria>First-discovered exact duplicate behavior, complete warning propagation, preserved distinct names, and matching live documentation with exact aggregate coverage.</success_criteria>
<output>Create .planning/phases/03-reachable-agent-collision-contract/03-03-SUMMARY.md with actual focused/full commands and coverage counts.</output>
