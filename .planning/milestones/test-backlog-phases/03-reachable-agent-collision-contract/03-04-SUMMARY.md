---
phase: 03-reachable-agent-collision-contract
plan: 04
subsystem: agents
tags: [migration, update, reinstall, rollback]
status: complete
plan_head_before: 08fe8e65a52eaf9416387eb1de316fc8c2f37708
actuals:
  tasks: 2
  commits: 1
requires:
  - phase: 03-03
    provides: Full-source identities and complete duplicate diagnostics
provides:
  - Real update migration, coexistence and occupied-target retry proofs
  - Same-version repeated reinstall migration and exact rollback proofs
affects: [FINAL-01]
tech-stack:
  added: []
  patterns: [legacy index fixtures through public lifecycle operations]
key-files:
  created: []
  modified:
    - tests/orchestrators/plugin/update-swap.test.ts
    - tests/orchestrators/plugin/reinstall-flow.test.ts
    - tests/bridges/agents/stage.test.ts
key-decisions:
  - Test update's stale record and retry behavior without claiming full backup rollback.
  - Reinstall retains previous indexed target overwrite semantics and restores exact backups on later failure.
requirements-completed: [AGENT-01, AGENT-02]
completed: 2026-09-14
---

# Phase 3 Plan 4: Owned-name migration Summary

Update and same-version reinstall migrate legacy prefixed agents, preserve distinct source contents, and protect unrelated destinations.

## Accomplishments

1. Added real `preparePluginUpdate` → `swapPluginUpdate` scenarios for prefixed-only migration, coexistence and an occupied new target followed by retry. Assertions cover complete outcomes, index documents, affected state records, generated bytes, old-target removal or reuse, and other-owner sentinel bytes/rows.
2. Added same-version reinstall migration with and without coexistence. Repeating reinstall preserves complete files, names, index and state. Its `resourcesChanged` remains true because the established outcome signals replacement work, including repeated reinstall.
3. Added reinstall foreign-new-target, cross-index-owner and post-replacement state-save failure cases. Complete diagnostics, original state/index bytes and old target bytes are asserted. The state-save failure removes the introduced doubled-prefix target.
4. Added direct real prepare/replace/rollback cases for prefixed-only migration and coexistence that reuses the old target. Both restore exact prior bytes/index and remove the introduced target. Existing RINST-01 forced replacement tests remain intact.

## Verification

- `node --test --test-name-pattern='AGENT-01' tests/orchestrators/plugin/update-swap.test.ts tests/orchestrators/plugin/reinstall-flow.test.ts`: 8/8 passed (`/tmp/agents-migrations.log`).
- Direct migration/rollback stage focus: 2/2 passed (`/tmp/agents-migration-stage.log`).
- Full affected owner run exercised 578 cases. All update/reinstall/migration cases passed; two install soft-preservation fixtures required the contract adjustment documented in 03-03.
- Stage direct pair: 639/639 lines, 26/26 functions, 109/109 branches. All other changed production pairs also remain exactly covered, recorded in 03-02.
- Parent owns the stable integrated `npm run check`, aggregate coverage, final lint and final typecheck. No aggregate counts are asserted yet.

These are characterization tests of the production migration supplied by 03-02. No deliberately broken production implementation was introduced to manufacture a RED result. Initial expectation corrections preserved the established update recovery text and reinstall cause-chain/result contracts.

## Deviations from Plan

Independent review found that failure to persist the new index left unrecorded newly named files behind, blocking retry through the new occupancy guard. The narrow production repair places index persistence inside commit's existing rename rollback boundary; it does not add a full backup transaction. Four real read-only-root regression cases prove prefixed-only/coexistence failure cleanup followed by commit or forced replacement recovery. Full stage owner: 46/46; direct coverage: 638/638 lines, 26/26 functions, 113/113 branches. Focused lifecycle contracts: 16/16 passed. Logs and detailed repair evidence are recorded in 03-02-SUMMARY.md.

Update failure retains its old resource record with `update-in-progress`; it does not restore deleted files from backups. Reinstall uses its existing backup rollback and previous-index force policy.

## Commit and Finalization Status

Committed in `b663bc68` with the other coordinated Phase 3 plans. Independent goal verification passed 18/18.

Final verification passed on 2026-09-14: 6,267/6,267 unit tests, 32/32 integration tests, and clean pre-commit checks including lint, typecheck, formatting, Fallow and changed direct pairs. All 227 production LCOV records retain exact coverage: 63,374/63,374 lines, 1,851/1,851 functions and 9,145/9,145 branches. Workflow, correspondence and direct-coverage negative controls also pass. The two unrelated direct shortfall pins remain unchanged.

Evidence: `/tmp/test-backlog-phase34-unit-final.log`, `/tmp/test-backlog-phase34-integration.log`, `/tmp/test-backlog-phase34-precommit-final.log`, and the phase VERIFICATION.md report. Earlier checkpoints above remain historical measurements.

## Self-Check: PASSED

All three test owner files and the recorded base commit exist. No production APIs, schemas, test-only exports, skipped tests or coverage exclusions were introduced.
