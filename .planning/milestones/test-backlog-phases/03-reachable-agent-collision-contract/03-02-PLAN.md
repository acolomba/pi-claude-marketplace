---
phase: 03-reachable-agent-collision-contract
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - extensions/pi-claude-marketplace/domain/name.ts
  - extensions/pi-claude-marketplace/bridges/agents/stage.ts
  - extensions/pi-claude-marketplace/bridges/agents/convert.ts
  - tests/domain/name.test.ts
  - tests/bridges/agents/discover.test.ts
  - tests/bridges/agents/stage.test.ts
  - tests/bridges/agents/convert.test.ts
autonomous: true
requirements: [AGENT-01, AGENT-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Existing content at a newly claimed agent target is rejected before old owned files are removed.
    - Both reviewer and acme-reviewer survive discovery and staging in plugin acme with different generated names.
    - Exact duplicates are resolved by discovery; conversion does not contain a second unreachable collision policy.
    - The existing validated nonempty tools contract and all reachable safety errors remain enforced.
  artifacts:
    - path: extensions/pi-claude-marketplace/domain/name.ts
      provides: Complete source-name agent generation
    - path: extensions/pi-claude-marketplace/bridges/agents/stage.ts
      provides: Safe commit preflight and canonical discovery consumption
    - path: tests/bridges/agents/stage.test.ts
      provides: Real filesystem discovery, commit protection, and two-agent staging proofs
  key_links:
    - from: generatedAgentName
      to: discoverPluginAgents
      via: full source-name identity
    - from: prepareStagePluginAgents
      to: commitPreparedAgents
      via: new index entries compared with safe previous target paths before deletion
---

<objective>
Preserve both exact Claude source identities and make newly claimed installation paths safe before enabling name migration, per D-01, D-02, D-04, and D-05.
Output: complete-source agent naming, reachable staging tests, and removal of the dead converter gate.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-reachable-agent-collision-contract/03-CONTEXT.md
@.planning/phases/03-reachable-agent-collision-contract/03-RESEARCH.md
@.planning/phases/03-reachable-agent-collision-contract/03-01-SUMMARY.md
@.agents/skills/typescript-google-style-review/SKILL.md
@.agents/skills/typescript-unit-testing-review/SKILL.md

Discovery level 0: existing TypeScript, node:test, pathExists, index ownership, and staging facilities suffice; no package or external service changes.
Use CodeGraph before source exploration. commitPreparedAgents is called by install-outcome.ts and update-swap.ts; replacePreparedAgents is used by reinstall-replace.ts with force:true. The former deletes previous targets and commits sequential renames; the latter backs up and rejects occupied new targets. Preserve that distinction.
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Protect a newly claimed agent target through real prepare and commit</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/stage.ts, tests/bridges/agents/stage.test.ts</files>
  <behavior>
    - Seed an old indexed, marker-owned target and a newly named source. A foreign file at the new target survives commit unchanged; the old file and complete index also remain unchanged.
    - A target created after prepare but before commit receives the same protection.
    - Missing new targets and safe previous targets still commit; other-owner indexed conflicts retain their existing structured error.
    - Occupied directories and dangling symlinks count as occupied; no ownership is inferred from the generated-name prefix alone.
  </behavior>
  <action>Per D-02 and D-04, add a failing real-filesystem test through prepareStagePluginAgents then commitPreparedAgents before implementing the guard. At the beginning of the staged commit, before the Promise.all previous-target removal, build membership from the safe _previousEntries target paths and inspect _newEntries targetPath values with existing lstat-based pathExists. Reject any occupied target outside that safe previous set with a complete path-bearing error, using the existing cleanup-and-append-cause convention. Reuse the replacement error's terminology: Cannot replace agent target with non-previous content at the target path. This includes unindexed content and foreign-preserved previous rows when a newly staged file would overwrite them; retain AG-5 soft preservation when there is no conflicting new write. Iterate _newEntries for the preflight so the sequential staged-rename bookkeeping remains separate. Do not add a second equivalent guard, exported helper, new filesystem module, or new ownership format. Preserve the replacement force behavior for previous indexed targets. The check protects occupancy observed at commit entry; retain existing transaction serialization and make no new concurrency guarantee about external writers after that check. Update the existing blocked-target rollback case to remove a later staged file after real prepare, so it continues to prove rollback after an earlier rename instead of merely exercising the new preflight. Retain complete diagnostics, rollback-leak, and cleanup assertions.</action>
  <verify><automated>node --test tests/bridges/agents/stage.test.ts</automated></verify>
  <done>The public prepare/commit path refuses occupied new destinations before deleting old owned bytes, successful commits remain valid, and partial rename rollback is still exercised.</done>
</task>

<task type="auto" tdd="true">
  <name>Preserve full source names from generation through staging</name>
  <reversibility rating="one-way">Generated filenames change for prefixed sources; the user explicitly approved lifecycle migration in D-01 and D-02.</reversibility>
  <files>extensions/pi-claude-marketplace/domain/name.ts, tests/domain/name.test.ts, tests/bridges/agents/discover.test.ts, tests/bridges/agents/stage.test.ts</files>
  <behavior>
    - acme plus reviewer produces pi-claude-marketplace-acme-reviewer; acme plus acme-reviewer produces pi-claude-marketplace-acme-acme-reviewer.
    - Repeated prefixes, case, punctuation permitted by assertSafeName, source equal to plugin, and a valid source ending with a hyphen remain intact.
    - Unsafe plugin, source, and generated values retain their applicable validation errors.
    - Two real source files with distinct complete names and different bodies produce two complete staged records and files without duplicate warnings.
  </behavior>
  <action>Per D-01 and D-04, replace only generatedAgentName's prefix-removal logic with the full validated source appended to the existing installation namespace. Retain validation of plugin, source, and generated output; remove only validation of the no-longer-produced shortened intermediate value. Update the existing domain table and former manufactured-collision discovery case to expect both full identities and complete records. The formerly rejected acme- source is valid under the full-source rule: change that specific test to the complete successful result while retaining actual empty/unsafe-name negatives. Add an end-to-end staging case with independent expected bytes, provenance, target names, complete warnings, and recorded rows. Do not compute expected names or emitted contents using production generators/serializers. Leave skill and command naming untouched.</action>
  <verify><automated>node --test tests/domain/name.test.ts tests/bridges/agents/discover.test.ts tests/bridges/agents/stage.test.ts</automated></verify>
  <done>Both exact source identities are discoverable and stageable, all naming boundaries are covered, and the safety check from Task 1 protects the resulting new target.</done>
</task>

<task type="auto">
  <name>Remove the unreachable converter collision policy</name>
  <files>extensions/pi-claude-marketplace/bridges/agents/convert.ts, extensions/pi-claude-marketplace/bridges/agents/stage.ts, tests/bridges/agents/convert.test.ts</files>
  <action>Per D-05, remove assertNoAgentCollisions, its stage import/call, and the direct tests that construct duplicate arrays for that export. Update stage's overview, throws contract, and numbered preparation comments to describe discovery as the sole within-plugin deduplication owner; keep cross-owner conflicts, empty mapped-tool errors, index corruption, and IO failures explicit. Use the real filesystem collision and distinct-name tests as the behavioral evidence. Per D-04, preserve 03-01's assertion-narrowed nonempty mapping and all other converter assertions. Trace exports/callers before removal, including any actual barrel binding; the current agents barrel does not export this assertion. Do not extract a replacement module or expose a test seam.</action>
  <verify><automated>node --test tests/bridges/agents/convert.test.ts tests/bridges/agents/stage.test.ts</automated></verify>
  <done>Staging consumes discovery's canonical result directly and converter tests retain every reachable conversion contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Plugin source to generated filename | Local plugin names control scoped target names. |
| Ownership index to filesystem commit | New names may already identify unrelated local content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-03-02-01 | Tampering | generatedAgentName | high | mitigate | Retain assertSafeName on plugin, exact source, and generated output; retain containment at staging. |
| T-03-02-02 | Tampering | commitPreparedAgents | high | mitigate | Check new destinations against safe prior ownership before deleting previous targets; test foreign files, directories, and symlinks. |
| T-03-02-03 | Denial of service | commit failure | medium | mitigate | Keep sequential rollback and cleanup cause chains, with real partial-failure assertions. |
</threat_model>

<verification>
Run the task commands, npm run typecheck, npm run lint, and direct coverage for domain/name.ts and agents stage.ts/convert.ts/discover.ts using npm run test:coverage:direct -- followed by each full production path. Measure env -u PI_CODING_AGENT_DIR npm run test:coverage:unit and confirm production line/function/branch totals are each exactly covered. Preserve unrelated direct-pin rows and all thresholds. See 03-VALIDATION.md for the aggregate interpretation.
</verification>
<success_criteria>Safe new-name commits, two retained source identities, one reachable deduplication owner, and exact aggregate coverage with complete assertions.</success_criteria>
<output>Create .planning/phases/03-reachable-agent-collision-contract/03-02-SUMMARY.md with actual commands and coverage counts.</output>
