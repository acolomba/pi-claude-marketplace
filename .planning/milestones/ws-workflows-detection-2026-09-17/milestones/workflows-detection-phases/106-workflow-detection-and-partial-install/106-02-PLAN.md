---
phase: 106-workflow-detection-and-partial-install
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - extensions/pi-claude-marketplace/domain/components/plugin.ts
  - tests/domain/manifest.test.ts
  - tests/domain/resolver-strict.test.ts
  - tests/domain/resolver-loose.test.ts
autonomous: true
requirements: [WDET-01, WDET-02, WDET-03]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "EDGE-01: Schema admission is read-only and deterministic. Concurrent marketplace and plugin-manifest loads accept the same defined workflows field without shared mutation (WDET-01, D-106-01)."
    - "EDGE-02: Strict and loose resolution detect only the literal workflows directory. Synthetic claude-security and code-modernization layouts produce the same kind without network access (WDET-02, D-106-02, D-106-03)."
    - "Top-level entry and plugin-manifest declarations are opaque presence signals. A non-path value is accepted and never interpreted (WDET-01, D-106-01)."
    - "A declaration and the conventional directory merge into one ordered `workflows` kind in both resolver modes (WDET-03, D-106-02, D-106-03)."
  artifacts:
    - path: "extensions/pi-claude-marketplace/domain/components/plugin.ts"
      provides: "one shared opaque `workflows` field for marketplace entries and plugin manifests"
      contains: "workflows"
    - path: "tests/domain/manifest.test.ts"
      provides: "opaque and deterministic schema-admission regressions"
      contains: "workflows"
    - path: "tests/domain/resolver-strict.test.ts"
      provides: "strict declaration, convention, dedupe, named-layout, and precedence regressions"
      contains: "workflows"
    - path: "tests/domain/resolver-loose.test.ts"
      provides: "loose-mode parity for every workflow signal route"
      contains: "workflows"
  key_links:
    - from: "extensions/pi-claude-marketplace/domain/components/plugin.ts"
      to: "extensions/pi-claude-marketplace/domain/resolver.ts"
      via: "both schema consumers expose the same top-level field to `declaresUnsupportedKind`"
      pattern: "UNSUPPORTED_COMPONENT_FIELDS"
    - from: "tests/domain/resolver-strict.test.ts"
      to: "tests/domain/resolver-loose.test.ts"
      via: "the same signal matrix asserts a shared collector result in both modes"
      pattern: "workflows"
---

<objective>
Add every declaration route and resolver-mode edge to the tracer from Plan 01.

D-106-01 requires one opaque top-level field shared by both schemas. D-106-02
and D-106-03 require declaration and fixed-directory parity in strict and loose
resolution. No task parses a value or adds an experimental namespace.

Purpose: make every supported input route reach the same typed result while the
tracer remains the only production classification path.

Output: one schema field and focused schema plus resolver regressions. This plan
creates no production file.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/workflows-detection/ROADMAP.md
@.planning/workstreams/workflows-detection/REQUIREMENTS.md
@.planning/workstreams/workflows-detection/STATE.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-CONTEXT.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-RESEARCH.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-PATTERNS.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-VALIDATION.md
@.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-01-SUMMARY.md

<interfaces>
- `UNSUPPORTED_COMPONENT_FIELDS` is spread into `PLUGIN_ENTRY_SCHEMA` and `PLUGIN_MANIFEST_SCHEMA`.
- `declaresUnsupportedKind(kind, entry, manifest)` treats a defined top-level value as presence.
- Both `resolveStrict` and `resolveLoose` call the shared unsupported-kind collector.
- Non-materializable resolver arms do not expose `pluginRoot`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admit opaque workflow declarations through both schemas</name>
  <files>extensions/pi-claude-marketplace/domain/components/plugin.ts, tests/domain/manifest.test.ts</files>

  <read_first>
    - Read both listed files before editing them.
    - In `plugin.ts`, read `UNSUPPORTED_COMPONENT_FIELDS`, `PLUGIN_ENTRY_SCHEMA`, `PLUGIN_MANIFEST_SCHEMA`, and both compiled validator exports.
    - Trace the two schema validators with `rg -n 'PLUGIN_ENTRY_VALIDATOR|PLUGIN_MANIFEST_VALIDATOR' extensions tests` before modification.
    - In `manifest.test.ts`, read the existing opaque unsupported-field admission cases for both validators.
  </read_first>

  <behavior>
    - A marketplace entry with a defined `workflows` field passes schema validation.
    - A standalone plugin manifest with a defined `workflows` field passes schema validation.
    - Non-path objects and numbers pass because the field is opaque.
    - Repeated and concurrent validation returns the same result without shared mutation.
  </behavior>

  <action>
Write the schema tests first. Cover both validators with defined values that
cannot be mistaken for paths. Add a repeated concurrent admission case over
the same frozen inputs. Assert that validation does not mutate those inputs.

Append `workflows: Type.Optional(Type.Unknown())` to
`UNSUPPORTED_COMPONENT_FIELDS` per D-106-01. Rely on the two existing object
spreads. Do not add the field twice, define its payload, or add an
`experimental` workflow key.

Commit both files with `feat: accept workflow declarations` after the focused
test, secret scan, and pre-commit gates pass.
  </action>

  <verify>
    <automated>node --test tests/domain/manifest.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem extensions/pi-claude-marketplace/domain/components/plugin.ts tests/domain/manifest.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files extensions/pi-claude-marketplace/domain/components/plugin.ts tests/domain/manifest.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Both compiled validators accept a defined `workflows` field.
    - At least one accepted value is an object or number, not a path string.
    - The concurrent case returns stable true results and leaves frozen inputs unchanged.
    - The production field uses exactly `Type.Optional(Type.Unknown())` in the shared field object.
    - No new declaration namespace, payload schema, parser, or path reader exists.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>Both schema consumers admit the same opaque workflow declaration by one shared field.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove strict and loose resolver parity for every workflow signal</name>
  <files>tests/domain/resolver-strict.test.ts, tests/domain/resolver-loose.test.ts</files>

  <read_first>
    - Read both listed files before editing them.
    - Read their `basicEntry`, `mockCtx`, manifest-fixture, and fixed-convention helpers.
    - In the strict suite, read the current table-driven convention case and the structural-precedence case.
    - In the loose suite, read the existing declared-theme and partial-arm cases.
    - Read the committed `collectUnsupportedKinds` loop from Plan 01. Trace `resolveStrict` and `resolveLoose` callers before adding cases.
  </read_first>

  <behavior>
    - Entry declaration, plugin-manifest declaration, conventional directory, and declaration plus directory return the same exact ordered kind in both modes.
    - Missing `plugin.json` does not change directory detection.
    - Synthetic claude-security and code-modernization roots classify locally with no network call.
    - Parallel classifications return the same result and add no state.
  </behavior>

  <action>
Add an explicit workflow matrix to each resolver suite. Use top-level entry and
plugin-manifest declarations, the literal directory, and the combined signal.
Assert the exact state and exact `unsupported` array. Use a non-path declaration
value in at least one route per D-106-01.

Add synthetic roots named `claude-security` and `code-modernization`. Create
only local fixture directories. Assert that each mode finds `workflows` without
`plugin.json` and without a network seam call per WDET-02.

Run a small `Promise.all` classification set over independent contexts. Assert
identical results and no shared mutation. Add a structural-defect counter-case
that stays unavailable and exposes no `pluginRoot` arm per D-106-06.

Keep the shared collector unchanged. Commit both tests with
`test: cover workflow resolver modes` after all task gates pass.
  </action>

  <verify>
    <automated>node --test tests/domain/resolver-strict.test.ts tests/domain/resolver-loose.test.ts</automated>
    <automated>TASK_TRUFFLEHOG=$(find "${PRE_COMMIT_HOME:-$HOME/.cache/pre-commit}" -type f -name trufflehog -perm -u+x | head -1) &amp;&amp; test -n "$TASK_TRUFFLEHOG" &amp;&amp; "$TASK_TRUFFLEHOG" filesystem tests/domain/resolver-strict.test.ts tests/domain/resolver-loose.test.ts --results=verified,unknown --fail &amp;&amp; SKIP=trufflehog pre-commit run --files tests/domain/resolver-strict.test.ts tests/domain/resolver-loose.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Strict and loose modes produce the exact same workflow classification for every signal route.
    - Declaration plus directory produces exactly one `workflows` entry.
    - Both named synthetic layouts classify without network access.
    - Missing `plugin.json` leaves literal-directory detection unchanged.
    - Parallel read-only classification is deterministic and creates no shared state.
    - The structural counter-case stays unavailable and does not expose a materializable root.
    - The filesystem scan and all applicable pre-commit hooks pass before the task commit.
  </acceptance_criteria>

  <done>All declaration and convention routes agree in strict and loose mode without network access or structural-precedence drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest JSON to TypeBox schema | Untrusted values enter the domain as opaque presence signals. |
| Schema output to resolver | Both strict and loose routes must use the same closed collector. |
| Plugin root to filesystem stat | The plugin controls directory contents but not the literal path selected by the resolver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-106-01 | Tampering | plugin schemas and resolver collector | high | mitigate | Admit values as unknown, inspect presence only, and stat the one fixed directory. |
| T-106-03 | Tampering | strict and loose resolution | high | mitigate | Pin both modes to the same exact state and preserve structural precedence. |
| T-106-06 | Denial of service | concurrent schema and resolver reads | low | accept | Reads have no shared mutable state. Deterministic concurrency tests document the bounded risk. |
</threat_model>

## Artifacts this phase produces

This phase creates no production file, bridge, resource field, discovery field,
or executor. It produces these symbols and contract IDs:

- `UNSUPPORTED_COMPONENT_FIELDS.workflows`.
- The `UNSUPPORTED_COMPONENT_KINDS` tail member and fixed workflow directory convention.
- The `UnsupportedReason` workflow member and shared mapping.
- The `REASONS` workflow tail member and unsupported topic-group member.
- Fixture-only workflow seed support in `install.test.ts`.
- Catalog states `workflow-partially-available-inventory`, `workflow-install-rejection`, and `workflow-partial-install-success`.

<verification>
- Run each focused command after its task.
- Run `npm test` and `npm run test:integration` after Wave 2 plans complete.
- Confirm that no test fixture performs a fetch or names an external service.
</verification>

<success_criteria>
- Both declaration schemas admit opaque workflow values.
- Strict and loose resolution agree for declarations and the fixed directory.
- Named plugin layouts use synthetic local roots only.
- Duplicate signals collapse to one kind.
- Structural invalidity remains unavailable.
</success_criteria>

<output>
Create `.planning/workstreams/workflows-detection/phases/106-workflow-detection-and-partial-install/106-02-SUMMARY.md` when done.
</output>
