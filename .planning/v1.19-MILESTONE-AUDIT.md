---
milestone: v1.19
milestone_name: Unit Test Refactor
audited: 2026-09-04T03:00:00Z
status: tech_debt
scores:
  requirements: 48/48
  phases: 10/10
  integration: 5/5
  flows: 204/204
gaps:
  requirements: []
  integration: []
  flows: []
requirements_bookkeeping:
  - id: COV-03
    status: satisfied
    phase: Phase 117
    verification_status: passed
    note: >
      Checked in REQUIREMENTS.md and carries an evidence row in 117-VERIFICATION.md
      ("Changed-pair and all-pair commands both route through assertCompleteCoverage/pairForPath"),
      but is named by no SUMMARY.md `requirements_completed` frontmatter. Source-3 bookkeeping
      gap only — coverage is evidenced by sources 1 and 2.
  - id: COV-05
    status: satisfied
    phase: Phase 117
    verification_status: passed
    note: >
      Same shape. Evidenced in 117-VERIFICATION.md as satisfied under the operator-amended
      D-117-20 (190 complete + 7 accepted-shortfall + 7 type-only), independently reproduced
      from the retained artifact. Absent from SUMMARY frontmatter because the requirements
      sweep (117-12) recorded only the IDs that plan itself closed.
nyquist:
  compliant_phases: [109, 110, 111, 112, 113, 114]
  partial_phases: []
  not_validated_phases: [108, 116, 117]
  missing_phases: [115]
  overall: partial-coverage
tech_debt:
  - phase: 108-domain-and-platform
    items:
      - "VALIDATION.md is `status: draft` — validate-phase never reconciled it, so nyquist_compliant is not authoritative (#2117). Run /gsd-validate-phase 108."
  - phase: 112-hook-runtime
    items:
      - "WINDOWS 86 (unrun-verify): bridges/skills/stage.ts"
      - "WINDOWS 88 (stub): bridges/hooks/settle.ts and bridges/hooks/payloads/stop-failure.ts"
  - phase: 115-composition-orchestrators
    items:
      - "No VALIDATION.md at all — nyquist coverage MISSING. Run /gsd-validate-phase 115."
      - "WINDOWS 115 (deviation) x6: reconcile/backfill.test.ts, reconcile/pending.test.ts, reconcile/apply.ts (x2), marketplace/add.ts, import/execute.ts"
  - phase: 116-edge-surface
    items:
      - "WINDOWS 15-19, 21, 22 (unmet-truth) x7: the accepted D-116-01a single-branch shortfalls. DELIBERATELY OPEN — each is pinned by identity in its own pair and closes only by a production rewrite. Not debt to pay down; a recorded, measured acceptance."
      - "WINDOWS (deviation): edge/register.ts:18"
      - "VALIDATION.md is `status: draft`. Run /gsd-validate-phase 116."
  - phase: 117-extension-entry-and-final-gate
    items:
      - "WINDOWS (deviation): orchestrators/plugin/install.messaging.ts:476 — doc comment names a test path 117-04 vacated"
      - "WINDOWS (stub): docs/output-catalog.md:2729 — names the suite 117-05 deleted"
      - "WINDOWS (deviation): .planning/codebase/TESTING.md:124 — describes tests/helpers/ as live in ten places, quotes both pre-amendment globs"
      - "WINDOWS (deviation): bridges/hooks/event-router.ts:741 — comment names a deleted suite; the WR-05 invariant it cites survives at tests/index.test.ts:525-526"
      - "WINDOWS (deviation): .planning/codebase/CONVENTIONS.md:151 — claims an aggregate bridges/index.ts that does not exist"
      - "VALIDATION.md is `status: draft`. Run /gsd-validate-phase 117."
accepted_overrides:
  - phase: 117
    must_have: "The clean tree passes all-pair coverage (Success Criterion 4 / SUITE-05)"
    decision_ref: "D-117-20 (amended 2026-09-03)"
    accepted_by: operator
    accepted_at: 2026-09-04
---

# Milestone v1.19: Unit Test Refactor — Audit

**Status:** `tech_debt` — no blockers, no unsatisfied requirements, no integration gaps.
Accumulated deferred items and Nyquist coverage gaps need a decision before close.

## Definition of Done

> Every production module under `extensions/pi-claude-marketplace/` has exactly one mirrored
> owner test that imports it directly, and each pair reaches complete direct coverage measured
> in isolation.

**Met.** Verified two independent ways (see Integration below).

## Scores

| Dimension | Score | Basis |
|---|---|---|
| Requirements | 48/48 | All checked in REQUIREMENTS.md; all evidenced in a phase VERIFICATION.md |
| Phases | 10/10 | Every phase 108-117 has a VERIFICATION.md with `status: passed` |
| Integration | 5/5 | Five targeted seam questions, each measured, all WIRED |
| Pair invariant | 204/204 | Correspondence gate exit 0, zero violations |

## Phase Verifications

| Phase | Name | Status | Overrides |
|---|---|---|---|
| 108 | Domain and Platform | passed | 0 |
| 109 | Shared Contracts | passed | 0 |
| 110 | Persistence and Transaction | passed | 0 |
| 111 | Non-Hook Component Bridges | passed | 0 |
| 112 | Hook Runtime | passed | 0 |
| 113 | Orchestrator Support and Presenters | passed | 0 |
| 114 | Plugin and Marketplace Lifecycle | passed | 0 |
| 115 | Composition Orchestrators | passed | 0 |
| 116 | Edge Surface | passed | — |
| 117 | Extension Entry and Final Gate | passed | 1 (accepted) |

No phase is missing a VERIFICATION.md.

## Requirements Coverage — three-source cross-reference

All 48 milestone REQ-IDs are `[x]` in `.planning/REQUIREMENTS.md` (source 1) and carry an
evidence row in a phase VERIFICATION.md (source 2). 46 of 48 are also named in a SUMMARY.md
`requirements_completed` frontmatter (source 3).

The two exceptions — **COV-03** and **COV-05** — are **satisfied**, not orphaned. Both are
evidenced in `117-VERIFICATION.md`; they are simply absent from SUMMARY frontmatter, because
the requirements sweep (117-12) recorded only the IDs that plan itself closed, while these two
were evidenced during the verification pass. This is a bookkeeping gap in source 3, not a
coverage gap. No requirement is unsatisfied and none is orphaned, so the FAIL gate does not fire.

## Integration — measured, not restated

This milestone shipped no production feature and no API routes, so "integration" means the
seams a per-phase verification cannot see. Five questions, each measured:

| # | Question | Result |
|---|---|---|
| 1 | Does any test still import a path a later phase vacated? | **No.** `tests/helpers/` is gone; all four relocated modules resolve to their new owning tiers. The dissolution spanned phases 117-02/03/07 and left no dangling import. |
| 2 | Do the gate's 204 pairs match the artifact's 204 rows? | **Yes, set-equal in both directions.** Two independently-computed mechanisms agree exactly, source path for source path. |
| 3 | Is every `*.test.ts` reached by a CI script? | **Yes.** 266 files = 247 unit + 13 integration + 6 e2e; each reached by exactly one script. `test:coverage:unit`'s glob is byte-identical to `test`'s, so run-suite and coverage-suite cannot drift. |
| 4 | Do MOD-01..MOD-10 partition the production tree? | **Yes.** Per-area counts sum 23+19+12+31+31+35+14+8+30+1 = 204, and Q2 established set-equality rather than a count coincidence. One area per phase, 1:1. |
| 5 | Is the integration/e2e exemption coherent? | **Yes.** Both trees are structurally excluded via `nonCorrespondingRoots`; since the unit tiers alone give all 204 modules an owner, nothing there can be a module's only owner. |

**No blockers. No orphaned relocation, no glob drift, no MOD-area gap.**

## Accepted Override

`npm run test:coverage:direct:all` exits 1 on a clean tree, stopping at row 84 (`edge/args.ts`)
because the gate refuses the first of seven accepted D-116-01a shortfalls. This is the amended
**D-117-20** working as designed. ROADMAP Success Criterion 4 and requirement SUITE-05 were
worded before that amendment, so their literal "passes" clause is accepted as superseded and
recorded as an `overrides:` entry on `117-VERIFICATION.md`.

The reproducibility half of that gap was **fixed rather than waived**:
`npm run test:coverage:direct:report` regenerates all 204 rows from the gate's own enumeration,
matching the retained artifact row-by-row at exit 0. Its diff to the gate itself is three
`export` keywords.

## Tech Debt

22 open entries in `.planning/WINDOWS.md`, 3 open deferred items in phase 117, and 4 Nyquist
coverage gaps.

**Seven of the 22 are not debt.** WINDOWS 15-19, 21 and 22 are the accepted D-116-01a
single-branch shortfalls: each is pinned by identity in its own pair, each closes only by a
production rewrite, and the operator has twice declined to paper over them. They are a recorded
acceptance, and they stay open by design.

The remaining 15, plus the 3 deferred items, break down as:

- **Stale documentation references (6)** — the largest group, all created by this milestone's own
  relocations. Comments and docs naming test paths that phases 117-02/04/05 vacated, plus two
  `.planning/codebase/` documents describing the pre-refactor tree. None affects a gate; all are
  cheap. Two live under `extensions/`, which phase 117 had no licence to touch.
- **Phase 115 deviations (6)** — recorded during composition-orchestrator work.
- **Phase 112 items (3)** — one unrun-verify on `bridges/skills/stage.ts`, two stubs under
  `bridges/hooks/`.

### Nyquist coverage

| Phase | VALIDATION.md | Status | Action |
|---|---|---|---|
| 108 | exists | `draft` | `/gsd-validate-phase 108` |
| 109-114 | exists | `validated`, compliant | — |
| 115 | **missing** | — | `/gsd-validate-phase 115` |
| 116 | exists | `draft` | `/gsd-validate-phase 116` |
| 117 | exists | `draft` | `/gsd-validate-phase 117` |

Per #2117, `status: draft` means validate-phase never reconciled the file, so `nyquist_compliant`
is not authoritative there. These are coverage TODOs, not compliance failures — none is a
`validated` + `nyquist_compliant: false`, which would be a genuine PARTIAL.

## Closing Gates

Measured directly by the orchestrator, each run unpiped with its own exit code read:

| Gate | Result |
|---|---|
| `npm test` | 5144 tests / 295 suites / 0 fail — on **both** Node v22.22.2 and v26.8.1 |
| `npm run test:integration` | 31/31 |
| `npm run typecheck` | 0 |
| `npm run lint` | 0 |
| `npm run fallow` | 0 |
| `npm run test:corresponding` | 0, zero violations, 204 pairs |
| `npm run test:corresponding:negative` | 0 |
| `npm run test:coverage:direct:negative` | 0 |
| `npm run check` | 1 at `format:check` only, on eight **untracked** operator files — CI never sees them; not a milestone defect |

---

_Audited: 2026-09-04_
