---
phase: quick-260802-v2z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [PENV-01, BINP-01, MENV-01, MENV-03, DOC-06]
estimate:
  tokens: 42000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - All four docs describe getShellEnv() as spreading the full live process.env (only mutation prepends the managed bin dir to PATH) with resolveSpawnContext() re-deriving exactly five named keys — no PI_* prefix scrub
    - PENV-01 exists as a Phase 90 v1 requirement and the REQUIREMENTS traceability shows 14/14 coverage
    - BINP-01 exists as a v2 deferral plus an Out of Scope row in REQUIREMENTS
    - The MENV-01 stage-time substitution set includes project-scope ${CLAUDE_PROJECT_DIR} (user-scope a documented absence) across the docs that carry MENV-01
    - The pi-mcp-adapter open question is replaced everywhere by the verified resolveEnv finding (spread-then-override, live process.env inheritance)
    - Phase 93 states four call sites across the three bridges and the invoke-time user-scope divergence justification
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/PROJECT.md
  key_links:
    - Traceability count consistency — REQUIREMENTS coverage 14/14 matches STATE roadmap-summary "14 v1 requirements"
    - The "### In progress v1.17 env-parity" ROADMAP heading is preserved exactly (GSD tooling depends on it)
---

<objective>
Amend the four v1.17 env-parity planning docs (REQUIREMENTS.md, ROADMAP.md, STATE.md, PROJECT.md) to carry the LOCKED validation-pass findings (verified 2026-08-02 against Claude Code 2.1.212, pi-coding-agent 0.82.1, pi-mcp-adapter 2.10.0). Doc-only: correct the getShellEnv mechanism description, extend MENV-01's substitution set, resolve the pi-mcp-adapter open question, fix the Phase 93 call-site count and user-scope justification, add the new PENV-01 requirement (Phase 90) and BINP-01 v2 deferral, add Out of Scope rows, and update provenance + bookkeeping.

Purpose: Keep the planning corpus faithful to the re-verified ground truth before any phase is planned or executed.
Output: Four amended planning docs, committed.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT.md
@CLAUDE.md

Doc-only task. No code, no tests. Match each file's existing structure precisely (tables, checkbox lists, heading levels). Use scoped Edit calls — never Write — on these existing files. Preserve every unrelated line. All quoted OLD/NEW text below is verbatim from the LOCKED validation spec; embed it exactly, do not paraphrase away technical specifics.
</context>

<tasks>

<!-- planner-discipline-allow: scrubbing only -->

<task type="auto">
  <name>Task 1: Amend .planning/REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Apply these scoped edits with Edit (each OLD is unique in the file):

(1) getShellEnv description + provenance — REPLACE the ground-truth paragraph clause. OLD contains: "Pi's bash tool builds every child env from live `process.env` (`getShellEnv()` spreads it, scrubbing only `PI_*` keys); plugin MCP entries are currently written to `mcp.json` verbatim, so `${CLAUDE_PLUGIN_ROOT}` references reach pi-mcp-adapter unexpanded." — REPLACE that clause WITH: "Pi's bash tool builds every child env fresh at each spawn — `getShellEnv()` spreads the full live `process.env` (its only mutation: prepends Pi's managed bin dir to `PATH`), then the bash tool's `resolveSpawnContext()` deletes and re-derives exactly five named keys from the live session context (`PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, `PI_REASONING_LEVEL`); there is no `PI_*`-prefix scrub, so extension mutations of `process.env` (other than those five keys) reach every later bash child; plugin MCP entries are currently written to `mcp.json` verbatim, so `${CLAUDE_PLUGIN_ROOT}` references reach pi-mcp-adapter unexpanded. Re-verified 2026-08-02: live Claude Code 2.1.212 session env; binary string extraction (child-env builder, MCP stdio spawn env, invoke-time content-substitution set); pi-mcp-adapter 2.10.0 source; pi-coding-agent 0.82.1 source."

(2) NEW subsection PENV-01 — INSERT a new subsection immediately after the "### Session Environment" block (after the SENV-03 bullet, before "### Hook Environment"). ADD verbatim:
"### Plugin PATH

- [ ] **PENV-01**: At session start, each installed enabled plugin's `<pluginRoot>/bin` directory is appended to `process.env.PATH` — appended, never prepended; deduplicated and idempotent across repeated session-start events; recomputed from install state so installs/uninstalls are reflected after reload; the entry is added even if the directory does not exist (verified live: Claude Code 2.1.212 appends `<pluginRoot>/bin` for enabled plugins unconditionally)"

(3) MENV-01 extended — REPLACE the MENV-01 bullet. OLD: "- [ ] **MENV-01**: A plugin MCP server whose `command`/`args`/`env` values contain `${CLAUDE_PLUGIN_ROOT}` or `${CLAUDE_PLUGIN_DATA}` is written to `mcp.json` with real install paths substituted" — WITH: "- [ ] **MENV-01**: A plugin MCP server whose `command`/`args`/`env` values contain a stage-time substitution variable is written to `mcp.json` with real install paths substituted. The substitution set is `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, and — project-scope installs only — `${CLAUDE_PROJECT_DIR}`; user-scope `${CLAUDE_PROJECT_DIR}` is a documented absence (unknowable at install time). Rationale: Claude Code substitutes all three (plus `${user_config.*}` and generic `${ENV_VAR}`) at config load; pi-mcp-adapter does NOT interpolate `command`/`args` at all, and interpolates env values against `process.env` replacing unknown `${VAR}` with the empty string — stage-time substitution is therefore the only delivery path for `command`/`args` and the only correct one for per-plugin values in `env`."

(4) MENV-03 ground-truth note — REPLACE the MENV-03 bullet. OLD: "- [ ] **MENV-03**: Project-scope installs additionally inject `CLAUDE_PROJECT_DIR` into each server's `env`; user-scope absence is documented" — WITH: "- [ ] **MENV-03**: Project-scope installs additionally inject `CLAUDE_PROJECT_DIR` into each server's `env`; user-scope absence is documented. Ground truth: upstream injects `CLAUDE_PROJECT_DIR` into ALL MCP stdio spawns (any scope, value resolved at spawn); Pi's project-scope-only bake is the correct install-time approximation, and user-scope absence stays documented."

(5) DOC-06 resolved finding — REPLACE the DOC-06 bullet. OLD ends with: "documented absences; includes the resolved answer on whether pi-mcp-adapter spawns servers inheriting Pi's `process.env`" — REPLACE the whole DOC-06 bullet WITH: "- [ ] **DOC-06**: New `docs/env-vars.md` — per-variable × per-surface matrix (Claude Code ground truth vs Pi delivery), the two-mechanism model (install-time textual substitution for install-stable per-plugin values vs runtime env injection for session-scoped values), documented absences; records the verified finding that pi-mcp-adapter inherits Pi's `process.env` (spread-then-override) and its runtime-coverage consequences — pi-mcp-adapter 2.10.0 `server-manager.ts::resolveEnv` spawns stdio servers with `{...process.env, ...interpolated(config.env)}` (full live `process.env` inheritance, config keys winning; `${VAR}`/`$env:VAR` interpolation applies to env values, cwd, headers, bearerToken with unknown var → empty string, NOT to command/args), so session vars set by Phase 90 reach MCP servers spawned afterward (matching Claude Code, whose stdio MCP spawn env injects `CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_PROJECT_DIR`); documents the spawn-order caveat (servers spawned before the extension's session-start handler miss the session vars) and session-switch staleness (a running server keeps spawn-time env)"

(6) BINP-01 v2 deferral — under "## v2 Requirements", after the EFRT-01 "### Effort Mapping" block, ADD a new subsection verbatim:
"### Plugin Binaries

- **BINP-01**: Plugin binaries provisioning — a `plugin.json` `binaries` map of sha256-pinned files fetched into `<pluginRoot>/bin` at install time (upstream implements an asset cache with hash verification, containment checks, per-pass caps). Deferred: needs its own design pass AND an NFR-5 decision (install-time network is currently forbidden). PENV-01's PATH wiring still serves plugins that commit executables into `bin/`."

(7) Out of Scope rows — in the "## Out of Scope" table, ADD three rows at the end of the table body:
"| `AI_AGENT` | Identity of a different host (e.g. `claude-code_2-1-212_agent`); setting it would misrepresent Pi — same class as `CLAUDE_CODE_ENTRYPOINT` |
| `CLAUDE_CODE_BRIDGE_SESSION_ID` / `CLAUDE_CODE_REMOTE_SESSION_ID` | Remote Control / cloud-session features Pi lacks |
| Plugin binaries provisioning (`plugin.json` `binaries` map) | Deferred to v2 as BINP-01 — needs its own design pass and an NFR-5 install-time-network decision; PENV-01 still wires `<pluginRoot>/bin` onto PATH |"

(8) Traceability + coverage — in the Traceability table, ADD a row after the SENV-03 row: "| PENV-01 | Phase 90 | Pending |". Then in the "**Coverage:**" list, REPLACE "- v1 requirements: 13 total" WITH "- v1 requirements: 14 total" and REPLACE "- Mapped to phases: 13 ✓" WITH "- Mapped to phases: 14 ✓".

(9) Footer — REPLACE "*Last updated: 2026-08-01 after roadmap creation (Phases 90-94 mapped)*" WITH "*Last updated: 2026-08-02 after validation pass (PENV-01 added; MENV-01 extended; pi-mcp-adapter question resolved)*".
  </action>
  <verify>
    <automated>F=.planning/REQUIREMENTS.md; ! grep -q "scrubbing only" "$F" && grep -q "resolveSpawnContext" "$F" && grep -q "PENV-01" "$F" && grep -q "BINP-01" "$F" && grep -q "resolveEnv" "$F" && grep -q "AI_AGENT" "$F" && grep -q "CLAUDE_CODE_BRIDGE_SESSION_ID" "$F" && grep -q "v1 requirements: 14 total" "$F" && grep -q "Mapped to phases: 14" "$F"</automated>
  </verify>
  <done>REQUIREMENTS.md carries the corrected getShellEnv/resolveSpawnContext description (no "scrubbing only" phrase remains), the PENV-01 requirement + traceability row, BINP-01 v2 deferral, both new Out of Scope identity/cloud rows, the extended MENV-01/MENV-03 and resolved DOC-06 text, and coverage reads 14/14.</done>
</task>

<!-- planner-discipline-allow: three call sites -->

<task type="auto">
  <name>Task 2: Amend .planning/ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Apply these scoped edits with Edit. Do NOT alter the "### In progress v1.17 env-parity" heading.

(1) Phase 90 summary bullet — REPLACE the OLD bullet: "- [ ] **Phase 90: Session environment initialization** — at session start the extension sets `CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID=<Pi session id>`, and the pi-only `CLAUDE_SESSION_ID` alias on Pi's live `process.env`, so a skill/command script launched through Pi's bash tool (which builds every child env from `process.env` via `getShellEnv()`, scrubbing only `PI_*`) inherits them exactly as under Claude Code. (SENV-01, SENV-02, SENV-03)" — WITH: "- [ ] **Phase 90: Session environment initialization** — at session start the extension sets `CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID=<Pi session id>`, and the pi-only `CLAUDE_SESSION_ID` alias on Pi's live `process.env`, and appends each installed enabled plugin's `<pluginRoot>/bin` to `process.env.PATH`, so a skill/command script launched through Pi's bash tool inherits them exactly as under Claude Code. Pi's bash tool builds every child env fresh at each spawn: `getShellEnv()` spreads the full live `process.env` (its only mutation: prepends Pi's managed bin dir to `PATH`), then `resolveSpawnContext()` deletes and re-derives exactly five named keys (`PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, `PI_REASONING_LEVEL`) from the live session context — there is no `PI_*`-prefix scrub, so extension mutations of `process.env` (other than those five keys) reach every later bash child. (SENV-01, SENV-02, SENV-03, PENV-01)"

(2) Phase 93 summary bullet — REPLACE "extending `shared/vars.ts::substituteClaudeVars` and its three call sites; user-scope" WITH "extending `shared/vars.ts::substituteClaudeVars` and its four call sites across the three bridges (skills stage ×2 — description augmentation and whole-file; commands stage; agents convert); user-scope".

(3) Phase 94 summary bullet — REPLACE "and the resolved pi-mcp-adapter `process.env`-inheritance answer" WITH "and the verified pi-mcp-adapter `process.env`-inheritance finding".

(4) Phase 90 Goal — REPLACE the OLD Goal: "**Goal**: A skill or command script launched through Pi's bash tool sees the Claude Code session environment variables — exactly as it would under Claude Code — because the extension sets them on Pi's live `process.env` at session start and Pi's bash tool builds every child env from `process.env` (`getShellEnv()` spreads it, scrubbing only `PI_*` keys). This is the shared env-injection groundwork the hook lane leans on: it establishes the two session-scoped values (`CLAUDECODE`, `CLAUDE_CODE_SESSION_ID`) and the pi-only `CLAUDE_SESSION_ID` shim as the milestone's runtime-injection foundation." — WITH: "**Goal**: A skill or command script launched through Pi's bash tool sees the Claude Code session environment variables — exactly as it would under Claude Code — because the extension sets them on Pi's live `process.env` at session start and Pi's bash tool builds every child env fresh at each spawn: `getShellEnv()` spreads the full live `process.env` (its only mutation: prepends Pi's managed bin dir to `PATH`), then `resolveSpawnContext()` deletes and re-derives exactly five named keys (`PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, `PI_REASONING_LEVEL`) from the live session context — there is no `PI_*`-prefix scrub, so extension mutations of `process.env` reach every later bash child. This is the shared env-injection groundwork the hook lane leans on: it establishes the two session-scoped values (`CLAUDECODE`, `CLAUDE_CODE_SESSION_ID`) and the pi-only `CLAUDE_SESSION_ID` shim as the milestone's runtime-injection foundation, and additionally appends each installed enabled plugin's `<pluginRoot>/bin` to `process.env.PATH` (PENV-01). Mechanism note: Pi's `session_start` payload carries no session id — the handler reads `ctx.sessionManager.getSessionId()`; the event fires for startup/reload/new/resume/fork."

(5) Phase 90 Requirements line — REPLACE "**Requirements**: SENV-01, SENV-02, SENV-03" WITH "**Requirements**: SENV-01, SENV-02, SENV-03, PENV-01".

(6) Phase 90 new success criterion — after criterion 4 ("(non-interference). (SENV-01, SENV-02, SENV-03)"), ADD a new criterion 5: "  5. At session start, each installed enabled plugin's `<pluginRoot>/bin` is appended to `process.env.PATH` — appended not prepended, deduplicated and idempotent across repeated session-start events, recomputed from install state (installs/uninstalls reflected after reload), and added even if the directory does not exist (verified live: Claude Code 2.1.212 appends `<pluginRoot>/bin` for enabled plugins unconditionally). (PENV-01)"

(7) Phase 92 Goal — REPLACE the OLD sentence "At stage time, `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}` references in each server's `command`/`args`/`env` are substituted with real install paths, and `CLAUDE_PLUGIN_ROOT`/`CLAUDE_PLUGIN_DATA` (plus `CLAUDE_PROJECT_DIR` for project-scope installs) are injected into each server's `env` map — mirroring the injection Claude Code performs at spawn time — with the substitution re-derived on every re-stage so a plugin-root change never leaves stale paths." — WITH: "At stage time, the substitution set `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, and — project-scope installs only — `${CLAUDE_PROJECT_DIR}` is substituted with real install paths in each server's `command`/`args`/`env` (user-scope `${CLAUDE_PROJECT_DIR}` is a documented absence, unknowable at install time), and `CLAUDE_PLUGIN_ROOT`/`CLAUDE_PLUGIN_DATA` (plus `CLAUDE_PROJECT_DIR` for project-scope installs) are injected into each server's `env` map — mirroring the injection Claude Code performs at spawn time — with the substitution re-derived on every re-stage so a plugin-root change never leaves stale paths. Rationale: Claude Code substitutes all three (plus `${user_config.*}` and generic `${ENV_VAR}`) at config load; pi-mcp-adapter does NOT interpolate `command`/`args` at all, and interpolates env values against `process.env` replacing unknown `${VAR}` with the empty string — stage-time substitution is therefore the only delivery path for `command`/`args` and the only correct one for per-plugin values in `env`."

(8) Phase 92 SC1 — REPLACE "  1. An MCP server whose `command`/`args`/`env` values contain `${CLAUDE_PLUGIN_ROOT}` or `${CLAUDE_PLUGIN_DATA}` is written to `mcp.json` with the real installed paths substituted in place of the literals. (MENV-01)" WITH "  1. An MCP server whose `command`/`args`/`env` values contain `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, or — for a project-scope install — `${CLAUDE_PROJECT_DIR}` is written to `mcp.json` with the real installed paths substituted in place of the literals; a user-scope `${CLAUDE_PROJECT_DIR}` is a documented absence (unknowable at install time). (MENV-01)"

(9) Phase 92 SC3 — REPLACE "  3. A project-scope install additionally injects `CLAUDE_PROJECT_DIR` into each server's `env`; a user-scope install omits it (the user-scope value varies per session — documented absence). (MENV-03)" WITH "  3. A project-scope install additionally injects `CLAUDE_PROJECT_DIR` into each server's `env`; a user-scope install omits it (the user-scope value varies per session — documented absence). Ground truth: upstream injects `CLAUDE_PROJECT_DIR` into ALL MCP stdio spawns (any scope, value resolved at spawn); Pi's project-scope-only bake is the correct install-time approximation, user-scope absence stays documented. (MENV-03)"

(10) Phase 92 Implementation-time verification block — REPLACE the OLD block: "**Implementation-time verification**: Resolve whether pi-mcp-adapter spawns MCP servers inheriting Pi's `process.env` — this determines the runtime coverage for user-scope `CLAUDE_PROJECT_DIR` (deliberately not baked into `env` here) and for the session vars in MCP server processes. The finding feeds `docs/env-vars.md` (DOC-06) in Phase 94." — WITH: "**Verified finding (2026-08-02)**: pi-mcp-adapter 2.10.0 `server-manager.ts::resolveEnv` spawns stdio servers with `{...process.env, ...interpolated(config.env)}` — full live `process.env` inheritance, config keys winning; `${VAR}`/`$env:VAR` interpolation applies to env values, cwd, headers, and bearerToken (unknown var → empty string), NOT to command/args. Consequences: session vars set by Phase 90 reach MCP servers spawned afterward, matching Claude Code (whose stdio MCP spawn env injects `CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_PROJECT_DIR`); user-scope `CLAUDE_PROJECT_DIR` stays absent for Pi MCP servers (documented); the spawn-order caveat (servers spawned before the extension's session-start handler miss the session vars) and session-switch staleness (a running server keeps spawn-time env) are documented in `docs/env-vars.md` (DOC-06) in Phase 94."

(11) Phase 93 Goal — REPLACE "Extends `shared/vars.ts::substituteClaudeVars` (today only `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}`) and its three call sites — skills stage, commands stage, agents convert." WITH "Extends `shared/vars.ts::substituteClaudeVars` (today only `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}`) and its four call sites across the three bridges — skills stage ×2 (description augmentation and whole-file), commands stage, agents convert."

(12) Phase 93 SC3 — REPLACE "  3. A user-scope occurrence of `${CLAUDE_PROJECT_DIR}` passes through untouched (documented — SENV-03's shell expansion / env inheritance covers it), and the already-substituted `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}` remain correctly substituted. (SUB-02)" WITH "  3. A user-scope occurrence of `${CLAUDE_PROJECT_DIR}` passes through untouched (documented divergence — Claude Code substitutes `${CLAUDE_PROJECT_DIR}` at invoke time even for user-scope artifacts, so a user-scope artifact referencing it works under Claude Code but stays literal under Pi; no env var rescues it — Claude Code's own bash children do not carry `CLAUDE_PROJECT_DIR`, so Pi deliberately sets none; DOC-06 states this gap explicitly), and the already-substituted `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}` remain correctly substituted. (SUB-02)"

(13) Phase 94 SC2 — REPLACE "  2. `docs/env-vars.md` records the resolved answer to whether pi-mcp-adapter spawns servers inheriting Pi's `process.env` (the Phase 92 verification), stating the runtime coverage that follows for user-scope `CLAUDE_PROJECT_DIR` and the session vars in MCP server processes. (DOC-06)" WITH "  2. `docs/env-vars.md` records the verified finding that pi-mcp-adapter inherits Pi's `process.env` (spread-then-override — `server-manager.ts::resolveEnv` spawns stdio servers with `{...process.env, ...interpolated(config.env)}`, config keys winning; interpolation applies to env/cwd/headers/bearerToken with unknown var → empty string, NOT to command/args) and its runtime-coverage consequences: session vars set by Phase 90 reach MCP servers spawned afterward, user-scope `CLAUDE_PROJECT_DIR` stays absent, plus the spawn-order caveat and session-switch staleness. (DOC-06)"
  </action>
  <verify>
    <automated>F=.planning/ROADMAP.md; ! grep -q "scrubbing only" "$F" && ! grep -q "three call sites" "$F" && grep -q "four call sites" "$F" && grep -q "resolveSpawnContext" "$F" && grep -q "PENV-01" "$F" && grep -q "resolveEnv" "$F" && grep -q "documented divergence" "$F" && [ "$(grep -c '### In progress v1.17 env-parity' "$F")" = "1" ]</automated>
  </verify>
  <done>ROADMAP.md Phase 90 carries the corrected getShellEnv/resolveSpawnContext description, PENV-01 in requirements + new SC5; Phase 92 carries the extended MENV-01 substitution set, MENV-03 ground truth, and the verified resolveEnv finding; Phase 93 states four call sites + the invoke-time divergence justification; Phase 94 SC2 records the verified finding. The "### In progress v1.17 env-parity" heading is unchanged (exactly one occurrence) and no "scrubbing only" / "three call sites" phrasing remains.</done>
</task>

<!-- planner-discipline-allow: scrubbing only -->
<!-- planner-discipline-allow: three call sites -->

<task type="auto">
  <name>Task 3: Amend .planning/STATE.md and .planning/PROJECT.md</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <action>
STATE.md edits (mirror the amended Roadmap Summary + Current Position bookkeeping):

(S1) Current Position — after the "Last activity: 2026-08-01 — v1.17 env-parity roadmap created" line, ADD a new line: "Amended: Requirements/roadmap amended 2026-08-02 after validation pass (PENV-01 added; MENV-01 extended; pi-mcp-adapter question resolved)".

(S2) Roadmap Summary first bullet — REPLACE "All 13 v1 requirements mapped, no orphans." WITH "All 14 v1 requirements mapped, no orphans.".

(S3) Phase 90 summary bullet — REPLACE the OLD bullet (from "- **Phase 90 — Session environment initialization** (SENV-01, SENV-02, SENV-03):" through "The session-id value must track the active session (fresh after switch / `/reload`).") WITH: "- **Phase 90 — Session environment initialization** (SENV-01, SENV-02, SENV-03, PENV-01): the shared runtime-injection groundwork. At session start the extension sets `CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID=<Pi session id>`, and the pi-only `CLAUDE_SESSION_ID` alias on Pi's live `process.env`, and appends each installed enabled plugin's `<pluginRoot>/bin` to `process.env.PATH` (appended not prepended, deduplicated/idempotent, recomputed from install state, added even if absent — PENV-01). Pi's bash tool builds every child env fresh at each spawn: `getShellEnv()` spreads the full live `process.env` (its only mutation: prepends Pi's managed bin dir to `PATH`), then `resolveSpawnContext()` deletes and re-derives exactly five named keys (`PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, `PI_REASONING_LEVEL`) — there is no `PI_*`-prefix scrub, so extension mutations of `process.env` reach every later bash child. The session-id value must track the active session (fresh after switch / `/reload`)."

(S4) Phase 92 summary bullet — REPLACE the OLD "Substitute `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}` in each server's `command`/`args`/`env`, and inject" WITH "Substitute the set `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, and — project-scope installs only — `${CLAUDE_PROJECT_DIR}` (user-scope `${CLAUDE_PROJECT_DIR}` a documented absence, unknowable at install time) in each server's `command`/`args`/`env`, and inject". THEN, in the same bullet, REPLACE "(Claude's spread order). The `update`/`reinstall` re-stage paths re-derive" WITH "(Claude's spread order). Rationale: Claude Code substitutes all three at config load; pi-mcp-adapter does NOT interpolate `command`/`args` at all and replaces unknown `${VAR}` in env with the empty string — so stage-time substitution is the only delivery path for `command`/`args` and the only correct one for per-plugin `env` values. The `update`/`reinstall` re-stage paths re-derive".

(S5) Phase 93 summary bullet — REPLACE the OLD (from "and its three call sites (skills stage, commands stage," through "expansion / env inheritance covers it).") WITH: "and its four call sites across the three bridges (skills stage ×2 — description augmentation and whole-file; commands stage; agents convert) so `${CLAUDE_SKILL_DIR}` resolves to the skill's installed dir and project-scope `${CLAUDE_PROJECT_DIR}` resolves to the project root; user-scope `${CLAUDE_PROJECT_DIR}` passes through untouched (documented divergence — Claude Code substitutes it at invoke time even for user-scope artifacts, so such an artifact works under Claude Code but stays literal under Pi; no env var rescues it, and Claude Code's own bash children carry no `CLAUDE_PROJECT_DIR` so Pi deliberately sets none; DOC-06 states the gap)."

(S6) Implementation-time verification bullet — REPLACE the OLD bullet (from "- **Implementation-time verification** (lands in Phase 92, documented in Phase 94):" through "The finding feeds `docs/env-vars.md` (DOC-06).") WITH: "- **Verified finding (2026-08-02, documented in Phase 94)**: pi-mcp-adapter 2.10.0 `server-manager.ts::resolveEnv` spawns stdio servers with `{...process.env, ...interpolated(config.env)}` — full live `process.env` inheritance, config keys winning; `${VAR}`/`$env:VAR` interpolation applies to env values, cwd, headers, bearerToken (unknown var → empty string), NOT to command/args. Session vars set by Phase 90 reach MCP servers spawned afterward (matching Claude Code, whose stdio MCP spawn injects `CLAUDECODE=1`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_PROJECT_DIR`); user-scope `CLAUDE_PROJECT_DIR` stays absent for Pi MCP servers (documented); DOC-06 records the spawn-order caveat (servers spawned before the session-start handler miss the session vars) and session-switch staleness (a running server keeps spawn-time env)."

PROJECT.md edits (Current Milestone: v1.17 section + Active requirements checklist):

(P1) Session env init bullet — REPLACE "Pi's bash tool builds every child env from live `process.env` (`getShellEnv()` spreads it, scrubbing only `PI_*`), so skill/command scripts inherit them exactly as under Claude Code." WITH "Pi's bash tool builds every child env fresh at each spawn — `getShellEnv()` spreads the full live `process.env` (its only mutation: prepends Pi's managed bin dir to `PATH`), then `resolveSpawnContext()` deletes and re-derives exactly five named keys (`PI_SESSION_ID`, `PI_SESSION_FILE`, `PI_PROVIDER`, `PI_MODEL`, `PI_REASONING_LEVEL`) from the live session context — there is no `PI_*`-prefix scrub, so extension mutations of `process.env` reach every later bash child, and skill/command scripts inherit the session vars exactly as under Claude Code."

(P2) NEW Plugin PATH target feature — immediately after the "Session env init" bullet, ADD a new list item: "- **Plugin PATH (PENV-01)** — at session start each installed enabled plugin's `<pluginRoot>/bin` is appended to `process.env.PATH` (appended not prepended; deduplicated and idempotent across repeated session-start events; recomputed from install state so installs/uninstalls reflect after reload; added even if the directory does not exist — verified live: Claude Code 2.1.212 appends `<pluginRoot>/bin` for enabled plugins unconditionally). Plugin `binaries`-map provisioning (sha256-pinned fetch into `bin/`) is deferred to v2 as BINP-01 (needs its own design pass + an NFR-5 install-time-network decision)."

(P3) MCP staging parity bullet — REPLACE "at stage time (`bridges/mcp/stage.ts::stampServers`) substitute `${CLAUDE_PLUGIN_ROOT}`/`${CLAUDE_PLUGIN_DATA}` in `command`/`args`/`env` values and inject `CLAUDE_PLUGIN_ROOT`/`CLAUDE_PLUGIN_DATA` into each server's `env` map — mirroring the injection Claude Code performs at spawn time. `CLAUDE_PROJECT_DIR` baked for project-scope installs only (user-scope value varies per session)." WITH "at stage time (`bridges/mcp/stage.ts::stampServers`) substitute the set `${CLAUDE_PLUGIN_ROOT}`, `${CLAUDE_PLUGIN_DATA}`, and — project-scope installs only — `${CLAUDE_PROJECT_DIR}` (user-scope `${CLAUDE_PROJECT_DIR}` a documented absence, unknowable at install time) in `command`/`args`/`env` values, and inject `CLAUDE_PLUGIN_ROOT`/`CLAUDE_PLUGIN_DATA` into each server's `env` map — mirroring the injection Claude Code performs at spawn time. `CLAUDE_PROJECT_DIR` baked into `env` for project-scope installs only (user-scope value varies per session). Rationale: Claude Code substitutes all three at config load; pi-mcp-adapter does NOT interpolate `command`/`args` at all and replaces unknown `${VAR}` in env with the empty string, so stage-time substitution is the only delivery path for `command`/`args` and the only correct one for per-plugin `env` values."

(P4) Substitution completion bullet — REPLACE "Extends `shared/vars.ts::substituteClaudeVars` + its three call sites (skills/commands stage, agents convert)." WITH "Extends `shared/vars.ts::substituteClaudeVars` + its four call sites across the three bridges (skills stage ×2 — description augmentation and whole-file; commands stage; agents convert)."

(P5) Key context — REPLACE the trailing sentence "Open verification item: whether pi-mcp-adapter spawns servers inheriting Pi's `process.env` (determines runtime coverage for user-scope `CLAUDE_PROJECT_DIR` and session vars in MCP server processes)." WITH "Verified finding (2026-08-02): pi-mcp-adapter 2.10.0 `server-manager.ts::resolveEnv` spawns stdio servers with `{...process.env, ...interpolated(config.env)}` — full live `process.env` inheritance (config keys win; `${VAR}`/`$env:VAR` interpolation on env/cwd/headers/bearerToken, unknown var → empty string, NOT command/args), so session vars set by Phase 90 reach MCP servers spawned afterward (matching Claude Code); user-scope `CLAUDE_PROJECT_DIR` stays absent for Pi MCP servers (documented), with a spawn-order caveat and session-switch staleness recorded in DOC-06. Re-verified 2026-08-02: live Claude Code 2.1.212 session env; binary string extraction (child-env builder, MCP stdio spawn env, invoke-time content-substitution set); pi-mcp-adapter 2.10.0 source; pi-coding-agent 0.82.1 source."

(P6) Active requirements checklist — in the "### Active" section (the v1.17 checklist), after the item beginning "- [ ] Skill/command scripts run via Pi's bash tool see `CLAUDECODE=1`", ADD a new checklist item: "- [ ] Each installed enabled plugin's `<pluginRoot>/bin` is appended to `process.env.PATH` at session start (appended, deduplicated/idempotent, recomputed from install state, added even if absent) — matching Claude Code 2.1.212 (PENV-01)".
  </action>
  <verify>
    <automated>for F in .planning/STATE.md .planning/PROJECT.md; do grep -q "resolveEnv" "$F" && grep -q "resolveSpawnContext" "$F" || exit 1; grep -q "scrubbing only" "$F" && exit 1; grep -q "three call sites" "$F" && exit 1; done; grep -q "PENV-01" .planning/STATE.md && grep -q "PENV-01" .planning/PROJECT.md && grep -q "amended 2026-08-02" .planning/STATE.md && grep -q "14 v1 requirements mapped" .planning/STATE.md && grep -q "BINP-01" .planning/PROJECT.md && grep -q "four call sites" .planning/PROJECT.md</automated>
  </verify>
  <done>STATE.md mirrors the amended Phase 90/92/93 + verified-finding bullets, notes the 2026-08-02 amendment in Current Position, and reads "14 v1 requirements mapped". PROJECT.md carries the corrected getShellEnv description, the PENV-01 target feature + checklist item, BINP-01 deferral note, extended MENV-01 substitution set, four call sites, and the verified resolveEnv finding + provenance. No "scrubbing only" / "three call sites" phrasing remains in either file.</done>
</task>

</tasks>

<threat_model>
Doc-only planning-corpus edit — no code, no network, no package installs, no runtime surface. No trust boundary is crossed and no STRIDE threat applies. The only integrity risk is a whole-file overwrite corrupting sibling content; mitigated by using scoped `Edit` calls (never `Write`) with unique OLD anchors and a heading-preservation grep gate on `### In progress v1.17 env-parity`.
</threat_model>

<verification>
Run each task's automated grep gate. Additionally confirm no unintended structural drift: `git diff --stat .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md` shows only the four target files, and `grep -rn "conflict\|<<<<<<<\|>>>>>>>" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md` returns nothing.
</verification>

<success_criteria>
- getShellEnv "scrubbing only PI_*" wording is gone from all four docs, replaced by the resolveSpawnContext five-key description
- PENV-01 present in REQUIREMENTS (requirement + traceability), ROADMAP (Phase 90), STATE, PROJECT; coverage/summary read 14
- BINP-01 present as a v2 deferral + Out of Scope row (REQUIREMENTS) and a PROJECT deferral note
- MENV-01 substitution set extended to include project-scope ${CLAUDE_PROJECT_DIR} across REQUIREMENTS/ROADMAP/STATE/PROJECT
- The pi-mcp-adapter open question replaced by the verified resolveEnv finding in all four docs
- Phase 93 shows four call sites + invoke-time divergence justification (ROADMAP + STATE + PROJECT)
- "### In progress v1.17 env-parity" heading unchanged
</success_criteria>

<output>
Create `.planning/quick/260802-v2z-amend-v1-17-env-parity-planning-docs-per/260802-v2z-SUMMARY.md` when done.
</output>
