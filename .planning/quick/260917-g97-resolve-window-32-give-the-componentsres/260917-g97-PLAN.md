---
quick_id: 260917-g97
phase: quick-260917-g97
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEPS-01, DEPS-02]
files_modified:
  - extensions/pi-claude-marketplace/shared/notification-types.ts
  - extensions/pi-claude-marketplace/shared/notification-grammar.ts
  - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
  - tests/shared/notification-grammar.test.ts
  - tests/orchestrators/plugin/info.test.ts
  - tests/architecture/catalog-uat/fixtures/plugin-info.ts
  - tests/architecture/catalog-uat/catalog-contract.test.ts
  - tests/architecture/catalog-uat/catalog-parser.test.ts
  - docs/output-catalog.md
  - CHANGELOG.md
  - .planning/WINDOWS.md

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`info <plugin>@<mp>` on a not-installed git-source plugin whose clone is COLD and whose marketplace entry declares `dependencies` renders `    dependencies: <list>` as the LAST line, after `    components: not resolved` (D-01-32 entry fallback, INFO-02 line-last rule), with no clone created and no network call (NFR-5)"
    - "Every `componentsResolved: false` row that carries no `dependencies` renders exactly as it does today: the `(remote)` fetch-failure and warm-unreadable arms, every `(unavailable)` / `(partially-available)` arm, and the `npm`/`unknown` marker rows are byte-unchanged"
    - "`docs/output-catalog.md` gains one new catalog state, `remote-single-scope-with-dependencies`, bound to a fixture; the existing `remote-single-scope` bytes are untouched; the catalog UAT contract counts 213 states and 28,729 example bytes"
    - "Broken-windows ledger entry 32 reads `status: fixed` in the fenced JSON and the regenerated table, with `open_count: 0`, written by `gsd-tools windows fixed 32` and not by hand"
  artifacts:
    - "extensions/pi-claude-marketplace/shared/notification-types.ts — `PluginInfoComponentsUnresolved` gains `readonly dependencies?: readonly string[]` (D-01-32 doc comment)"
    - "extensions/pi-claude-marketplace/shared/notification-grammar.ts — file-private `appendDependenciesLine` shared by both `renderPluginInfo` arms; `case false:` emits the marker then the line"
    - "extensions/pi-claude-marketplace/orchestrators/plugin/info.ts — `buildRemoteNotInstalledRow` doc comment states the row carries the entry-declared list (D-01-32); no code change"
    - "tests/shared/notification-grammar.test.ts — unresolved arm with dependencies renders the line after the marker; empty list renders no line"
    - "tests/orchestrators/plugin/info.test.ts — the D-01-32 COLD git case expects the `dependencies: from-entry@mp` line"
    - "tests/architecture/catalog-uat/fixtures/plugin-info.ts — `remote-single-scope-with-dependencies` fixture"
    - "docs/output-catalog.md — `### Success -- remote single scope with dependencies (D-01-32)` section after `remote-single-scope`"
    - "CHANGELOG.md — one `[Unreleased]` bullet for the user-visible change"
    - ".planning/WINDOWS.md — entry 32 fixed via the verb"
  key_links:
    - "info.ts::buildGitNotInstalledRow (presence.kind !== \"materialized\") -> buildRemoteNotInstalledRow(pluginName, version, description, dependencies) -> row `{ status: \"remote\", componentsResolved: false, dependencies }` (already spread today; the type now admits it)"
    - "notification-grammar.ts::renderPluginInfo `case false:` -> push marker -> appendDependenciesLine(lines, plugin.dependencies)"
    - "catalog-contract.test.ts -> notify(fixture.message) -> byte-equal to the `remote-single-scope-with-dependencies` block"
---

<objective>
Resolve broken-windows ledger entry 32: the D-01-32 cold-git fallback computes the marketplace entry's `dependencies` for a not-installed git-source plugin, `buildRemoteNotInstalledRow` spreads it into the `(remote)` row, and the renderer drops it because `PluginInfoComponentsUnresolved` declares no `dependencies` field and `renderPluginInfo`'s `case false:` arm never reads one. TypeScript does not excess-property-check object spreads, so the drop is silent.

The fix is a closed-set catalog amendment: the unresolved row type admits `dependencies`, the renderer emits `    dependencies: <list>` AFTER `    components: not resolved` so the INFO-02 "dependencies line LAST" rule holds on both arms, and `docs/output-catalog.md` gains the one new state with its fixture. Scope stays on the ONE call site the ledger names (the cold-clone path in `buildGitNotInstalledRow`). The two other `status: "remote"` returns in that function (fetch-throw arm, warm-tree-unreadable arm) and every `(unavailable)` / `(partially-available)` arm do not receive the value and stay dependency-less, as the originating discovery already records.

Type decision (locked): add `readonly dependencies?: readonly string[]` to `PluginInfoComponentsUnresolved`, mirroring the resolved arm. Hoisting the field to `PluginInfoRowBase` is rejected: it would make every builder able to set it silently and would imply the other unresolved builders now render it, which they do not; the arm-local optional keeps the amendment as narrow as the ledger entry.

Purpose: `info` reads the plugin's own manifest when readable and the entry when not (D-01-32). The cold-git row is the one case where the entry is the only offline source, and the row showed nothing.
Output: two production edits plus one doc-comment edit, four test edits, one catalog section, one changelog line, one ledger update; two commits.
</objective>

<execution_context>
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/acolomba/src/pi-claude-marketplace-manifest/CLAUDE.md
@/home/acolomba/src/pi-claude-marketplace-manifest/skills/typescript-comments/SKILL.md
@/home/acolomba/src/pi-claude-marketplace-manifest/skills/typescript-unit-testing/SKILL.md
@/home/acolomba/src/pi-claude-marketplace-manifest/skills/typescript-google-style-review/SKILL.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.claude/rules/changelog.md
@/home/acolomba/src/pi-claude-marketplace-manifest/.planning/phases/01-manifest-read-fidelity/deferred-items.md
@/home/acolomba/src/pi-claude-marketplace-manifest/extensions/pi-claude-marketplace/shared/notification-types.ts
@/home/acolomba/src/pi-claude-marketplace-manifest/extensions/pi-claude-marketplace/shared/notification-grammar.ts
@/home/acolomba/src/pi-claude-marketplace-manifest/extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
@/home/acolomba/src/pi-claude-marketplace-manifest/tests/architecture/catalog-uat/catalog-contract.test.ts
@/home/acolomba/src/pi-claude-marketplace-manifest/tests/architecture/catalog-uat/fixtures/plugin-info.ts
@/home/acolomba/src/pi-claude-marketplace-manifest/docs/output-catalog.md

**Execution environment (observed at planning time, HEAD `aee128c9` on `features/manifest`):**
- Run everything in THIS checkout, `/home/acolomba/src/pi-claude-marketplace-manifest`. It is itself a git worktree with `node_modules` present (Node v26.8.2). Do not create a nested worktree.
- No pre-commit git hook is installed: nothing runs on `git commit`. Run `SKIP=trufflehog pre-commit run --files <every changed path>` by hand before each commit; the hooks rewrite files (mdformat, prettier, whitespace fixers), so restage and re-run until it exits 0 with nothing modified. Never `--no-verify`, never `--amend`, never rebase.
- The operator has UNCOMMITTED edits in `.claude/settings.json`, `.codex/config.toml`, `.planning/config.json`, `.planning/state.json` and untracked `.claude/CLAUDE.md`, `.mcp.json`, `AGENTS.md`. Never stage those. Never `git add -A` or `git add -u`. Stage explicit paths only.
- Commit messages: Conventional Commits; title <= 72 chars with a code scope (`fix(info):`, `docs(windows):`), no phase/milestone/window mention in the title; body lines <= 80 chars. Write the message to a scratchpad file and commit with `git commit -F` (a backticked token in `-m` is executed by the shell). Every commit ends with the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01XCXpZ2LFkzGt8mXUcCvfu3`.
- Comment and test-title policy (`skills/typescript-comments`): present-tense facts about the code as it stands; no "the former X", "no longer", "used to", "byte-identical to before"; cite D-01-32 / INFO-02 / RSTA-01 / D-80-04 / NFR-5, never a phase, plan, wave, ledger id or quick-task id.
- `tests/architecture/partial-vocabulary-guard.test.ts` scans `docs/output-catalog.md`: do not write the tokens `force`-family or `(unsupported)` in the new catalog prose.
- `tests/architecture/catalog-uat/catalog-parser.test.ts` ALSO pins the state count (212 at lines 69 and 74), not only `catalog-contract.test.ts` (`EXPECTED_STATE_COUNT = 212`, `EXPECTED_UTF8_BYTES = 28_543`, title at line 353). Both files change.
- `.planning/WINDOWS.md`: the fenced JSON block is the sole source of truth and the rendered table is regenerated from it. At planning time the table is BEHIND the JSON (row 30's table cell lacks its `resolved_at`; row 32 has no table row at all), so `gsd-tools windows fixed 32` refuses with `WINDOWS_LEDGER_TABLE_DRIFT` naming rows 30 and 32. The JSON is the richer side in both rows, so regenerating the table from the JSON loses no prose. The regenerate-then-verb sequence in Task 3 was run on a scratch copy and succeeds. `windows fixed` takes no reason argument.
- No precedent exists for a resolution note in a `deferred-items.md` file (this phase's is the only such file in the tree). Leave `.planning/phases/01-manifest-read-fidelity/deferred-items.md` untouched; the ledger is the register of record.

**Mechanism as it stands:** `info.ts` lines 1158-1189 compute `dependencies` per D-01-32 (own `plugin.json` when readable offline, else the entry; `undefined` when the rendered list is empty). `buildGitNotInstalledRow` (line 2193) probes presence fs-only; on `presence.kind !== "materialized"` it returns `buildRemoteNotInstalledRow(pluginName, version, description, dependencies)` (line 2163), which spreads `...(dependencies !== undefined && { dependencies })` after `componentsResolved: false`. `notification-types.ts` line 684 declares `PluginInfoComponentsUnresolved { readonly componentsResolved: false }`. `notification-grammar.ts` `renderPluginInfo` (line 1417) switches on `plugin.componentsResolved`; `case true:` calls `appendResolvedComponentLines(lines, plugin.components, plugin.dependencies)` whose tail pushes `    dependencies: ${dependencies.join(", ")}` when defined and non-empty; `case false:` pushes only `    components: not resolved`.

**Tests already covering the neighbours (do not duplicate):** `tests/shared/notification-grammar.test.ts` line 437 loops every status through the bare unresolved row and pins its exact bytes (this is the no-dependencies negative control for the type change; it stays as is). `tests/orchestrators/plugin/info.test.ts` line 4827 (`RSTA-01` cold url-source, entry declares `dep@mp`) asserts with regexes and is unaffected. Line 7859 (`D-01-32 / NFR-5: a COLD git source renders the entry's list and creates no clone directory`) is the ONE exact-bytes case that asserts the line is absent; its title is already correct for the fixed behaviour. `tests/edge/handlers/plugin/info.test.ts` and `tests/shared/notification-dispatch.test.ts` render `(remote)` rows whose entries declare no dependencies: unaffected. `tests/architecture/compat-01-no-expansion.test.ts` does not lock `PluginInfoComponentsUnresolved`. No integration or e2e test renders a `(remote)` row.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add the renderer case, flip the cold-git info expectation, add the catalog state and its fixture; observe the three failures</name>
  <files>tests/shared/notification-grammar.test.ts, tests/orchestrators/plugin/info.test.ts, tests/architecture/catalog-uat/fixtures/plugin-info.ts, tests/architecture/catalog-uat/catalog-contract.test.ts, tests/architecture/catalog-uat/catalog-parser.test.ts, docs/output-catalog.md</files>
  <behavior>
    - notification-grammar: a `remote` row with `componentsResolved: false` and `dependencies: ["helper@mp"]` renders `● official [user] <no autoupdate>\n  ◌ alpha (remote)\n    components: not resolved\n    dependencies: helper@mp` (line LAST, after the marker) — D-01-32 / INFO-02
    - notification-grammar: the same row with `dependencies: []` renders the three-line bare form with no dependencies line (negative control; passes before and after)
    - info (D-01-32 COLD git case at line 7859): the notification is `● mp [user] <no autoupdate>\n  ◌ gplug v1.0.0 (remote)\n    components: not resolved\n    dependencies: from-entry@mp` and `plugin-clones/` still does not exist (NFR-5)
    - catalog-uat: the `remote-single-scope-with-dependencies` fixture renders byte-equal to its catalog block; 213 states; 28,729 example bytes
  </behavior>
  <action>
Write the tests and the catalog amendment FIRST, then run them against the unchanged production code. All three test files use top-level `test()`; keep the new cases top-level so the `tdd-red-evidence` checker can read the TAP names.

1. `tests/shared/notification-grammar.test.ts`. After the `renders exact unresolved ${status} plugin info` loop (line 437), add two top-level cases with `// arrange` / `// act` / `// assert` markers and the same `message as never` + `bothLoadedProbe()` shape the loop uses:
   - `renders the entry-declared dependencies line after the unresolved marker (D-01-32)`: plugin `{ status: "remote", name: "alpha", componentsResolved: false, dependencies: ["helper@mp"] }`, marketplace `official` / `user` / `{ autoupdate: false }`; `assert.equal` against the four-line string in `<behavior>`.
   - `renders no dependencies line for an unresolved row whose list is empty`: same plugin with `dependencies: []`; expected is the three-line bare string the loop pins for `remote`. This row passes before and after the change; that is what makes it the negative control.
   Name the rendered value `rendered` (the loop's convention). Do not touch the loop.

2. `tests/orchestrators/plugin/info.test.ts`, the case at line 7859. Change the expected `message` to end with `"    components: not resolved\n" + "    dependencies: from-entry@mp"`. Rewrite the `// assert` comment so it states the current contract: the `(remote)` row carries the entry-declared list because the cold clone leaves no manifest readable without a fetch (D-01-32), and the load-bearing half is still that nothing was cloned to make the manifest readable (NFR-5). Keep the `clonesDir` precondition and postcondition assertions and the title unchanged.

3. `docs/output-catalog.md`. Directly after the `remote-single-scope` example fence (the block that ends at line 2306, before `### Disabled inventory row (D-54-01 / ENBL-04 / ENBL-17)`), insert a new section: heading `### Success -- remote single scope with dependencies (D-01-32)`; one prose paragraph in the catalog's register stating that the marketplace entry declares `dependencies`, that the clone is cold so the plugin's own `plugin.json` cannot be read without a fetch and NFR-5 forbids the fetch, so the entry's list is the source (D-01-32); that the `dependencies:` line renders LAST, after the `components: not resolved` marker, so its position matches the resolved arm; that only this cold-clone row carries the line (a `(remote)` row that reports a fetch or read failure, and every `(unavailable)` / `(partially-available)` row, renders no dependency line); severity `info`. Then the marker comment `<!-- catalog-state: remote-single-scope-with-dependencies -->`, a blank line, and a `text` fence whose body is the `remote-single-scope` block's four lines followed by one more line: four spaces then `dependencies: helper@community-mp`. Nothing else in the file changes; in particular the `remote-single-scope` block bytes and the section overview paragraph at line 2119 stay as they are except for this one wording change: in that overview sentence, after `OR the \`components: not resolved\` marker (INFO-05)`, append `, itself followed by the same optional \`dependencies:\` line on the cold git-source row (D-01-32)`.
   Byte accounting, measured at planning time through the real parser: the new example's `expected` is 186 UTF-8 bytes, so the catalog total goes 28,543 -> 28,729 and the state count 212 -> 213.

4. `tests/architecture/catalog-uat/fixtures/plugin-info.ts`. Directly after the `"remote-single-scope"` fixture (ends at line 464), add `"remote-single-scope-with-dependencies"`: same `pi`, `marketplaceName`, `marketplaceScope`, `marketplaceDetails`, and plugin fields as `remote-single-scope`, plus `dependencies: ["helper@community-mp"]` after `componentsResolved: false`. Add a one-line comment above the field: the list is pre-rendered from the marketplace entry, the only offline source for a cold clone (D-01-32).

5. `tests/architecture/catalog-uat/catalog-contract.test.ts`: append to the count-trail comment above `EXPECTED_STATE_COUNT` a line in its style — D-01-32: +1 state for the cold git-source `(remote)` row that carries the entry-declared `dependencies:` line after the unresolved marker (212 -> 213) — then set `EXPECTED_STATE_COUNT = 213`, `EXPECTED_UTF8_BYTES = 28_729`, and change the test title at line 353 to say `213 exact documented states`. `tests/architecture/catalog-uat/catalog-parser.test.ts`: change the title at line 69 and the `assert.equal(examples.length, 212)` at line 74 to 213.

6. Run the three verify commands and save each raw TAP to the scratchpad (`/tmp/claude-1000/-home-acolomba-src-pi-claude-marketplace-manifest/e4d03e39-b4c8-4058-8a19-a69d3e840eeb/scratchpad/red-grammar.tap`, `red-info.tap`, `red-catalog.tap`) so the SUMMARY can quote them. `tsc` is red at this point (the fixture and the grammar case set `dependencies` on a shape that does not declare it); Node strips types at run time, so `node --test` is the red evidence and `npm run typecheck` is not run in this task.
  </action>
  <verify>
    <automated>cd /home/acolomba/src/pi-claude-marketplace-manifest && node --test tests/shared/notification-grammar.test.ts; node --test tests/orchestrators/plugin/info.test.ts; node --test tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts</automated>
  </verify>
  <done>
Expected RED state, exactly: (a) grammar — `renders the entry-declared dependencies line after the unresolved marker (D-01-32)` is `not ok` with an `assert.equal` diff whose actual lacks the fourth line; `renders no dependencies line for an unresolved row whose list is empty` is `ok`; every existing case `ok`. (b) info — the line-7859 case is `not ok` with a `deepEqual` diff whose actual message lacks `    dependencies: from-entry@mp`; every other case `ok`. (c) catalog — `catalog contract matches all 20 fixture modules to 213 exact documented states` is `not ok` with a single `byte-mismatch` failure for state `remote-single-scope-with-dependencies` (counts and byte total already agree: 213 and 28,729); `loadCatalogExamples parses all 213 independent catalog tuples` is `ok`. Three `not ok` lines in total, raw TAP saved to the scratchpad.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — admit `dependencies` on the unresolved arm, render it after the marker, fix the doc comments, add the changelog line, gate, commit</name>
  <files>extensions/pi-claude-marketplace/shared/notification-types.ts, extensions/pi-claude-marketplace/shared/notification-grammar.ts, extensions/pi-claude-marketplace/orchestrators/plugin/info.ts, CHANGELOG.md</files>
  <behavior>
    - The three Task 1 `not ok` cases are `ok`; the negative control and every pre-existing case stay `ok`
    - `npm run typecheck`, `npm run lint`, `npm run fallow`, `npm run format:check`, `npm run test:coverage:direct:commit`, and `npm test` all exit 0
    - `git diff --stat` for the commit lists exactly the ten non-ledger paths in `files_modified`
  </behavior>
  <action>
Implement the locked design (D-01-32 rendering on the unresolved arm, INFO-02 line-last on both arms).

1. `extensions/pi-claude-marketplace/shared/notification-types.ts`, `PluginInfoComponentsUnresolved` (line 684). Add `readonly dependencies?: readonly string[];` after `componentsResolved: false`. Extend the interface's doc comment: the field is the pre-rendered, name-sorted list the cold git-source `(remote)` row carries from its marketplace entry (D-01-32), because the plugin's own `plugin.json` is not readable without a fetch and NFR-5 forbids one; the renderer emits it after the `components: not resolved` marker. Do not touch `PluginInfoRowBase` or `PluginInfoComponentsResolved`.

2. `extensions/pi-claude-marketplace/shared/notification-grammar.ts`. Add a file-private `appendDependenciesLine(lines: string[], dependencies: readonly string[] | undefined): void` next to `appendResolvedComponentLines` that pushes `    dependencies: ${dependencies.join(", ")}` when the list is defined and non-empty (the exact guard and template the resolved tail uses today). Its doc comment: both `renderPluginInfo` arms end with this line, so it is LAST after every per-kind line on the resolved arm and after the `components: not resolved` marker on the unresolved arm (INFO-02 / D-01-32). Replace the tail of `appendResolvedComponentLines` with a call to it. In `renderPluginInfo`'s `case false:` arm, after the marker push, call `appendDependenciesLine(lines, plugin.dependencies)` with a one-line comment: D-01-32 — the cold git-source `(remote)` row carries the entry-declared list. Update the `renderPluginInfo` doc comment sentence that describes the two arms so the unresolved arm reads: the single marker line `    components: not resolved` followed by the same optional `dependencies:` line (componentsResolved: false, D-01-32). Keep `appendResolvedComponentLines`'s signature.

3. `extensions/pi-claude-marketplace/orchestrators/plugin/info.ts`, `buildRemoteNotInstalledRow` doc comment (line 2154). Add a sentence: `dependencies` is the entry-declared list (D-01-32): the plugin's own `plugin.json` is not readable without a fetch, NFR-5 forbids one, so the entry is the only offline source, and the row carries the list so the `dependencies:` line renders after the marker. Optionally extend the file header sentence `A COLD clone renders \`(remote)\` + \`components: not resolved\`` with `plus the entry-declared \`dependencies:\` line when the entry declares one (D-01-32)`. No code change in this file: the spread at line 2175 already does the work once the type admits it. Do not widen the fetch-throw arm (line 2231), the warm-unreadable arm (line 2277), or any `(unavailable)` / `(partially-available)` builder.

4. `CHANGELOG.md`. Under `## [Unreleased]`, as the first bullet, add one line per `.claude/rules/changelog.md` (plain English, active voice, 40 words or fewer, no `should`/`would`, no semicolon): `info` on a git-source plugin that is not yet fetched now lists the dependencies its marketplace entry declares, after `components: not resolved`. This branch has no pull request yet, so the bullet ends at the period with no `(#NNN)`; the branch's ship step appends the number. Say so in the SUMMARY.

5. Gate. Run `node --test` on the four Task 1 files (all green), then `npm run typecheck && npm run lint && npm run fallow && npm run format:check && npm run test:coverage:direct:commit && npm test`. If `format:check` fails, run `npx prettier --write` on the named files and re-run. Direct coverage must stay 100% on `notification-grammar.ts` and `notification-types.ts` (neither is pinned in `scripts/test-coverage-direct.pin.json`; do not add a pin). `npm run check` is the full bar and may be run additionally; its `test:integration` leg is slow, and the two pi-subagents integration tests can fail locally on a stale global peer — that is environment, not a regression, and is not a reason to skip the unit gate above.

6. Commit. Run `SKIP=trufflehog pre-commit run --files` over the ten changed paths (the six Task 1 files, the three production files, `CHANGELOG.md`); if a hook rewrites a file, restage and re-run until clean. Stage exactly those ten paths. Title: `fix(info): show entry dependencies on the unfetched remote row` (or equivalent, <= 72 chars, `fix(info):` scope). Body: what changed and why in <= 80-column lines, citing D-01-32 / INFO-02 / NFR-5, then the two trailer lines. Use `git commit -F <scratchpad file>`.
  </action>
  <verify>
    <automated>cd /home/acolomba/src/pi-claude-marketplace-manifest && node --test tests/shared/notification-grammar.test.ts tests/orchestrators/plugin/info.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts && npm run typecheck && npm run lint && npm run fallow && npm run format:check && npm test && FIX_SHA=$(git rev-parse HEAD) && TITLE=$(git log -1 --format=%s "$FIX_SHA") && BODY=$(git log -1 --format=%B "$FIX_SHA") && NAMES=$(git diff --name-only "$FIX_SHA^..$FIX_SHA") && PATHS=$(printf '%s\n' "$NAMES" | LC_ALL=C sort | tr '\n' ' ') && printf '%s\n' "$TITLE" | grep -E '^fix\(info\): ' && printf '%s\n' "$BODY" | grep -q 'Co-Authored-By: Claude Opus 5' && printf '%s\n' "$BODY" | grep -q 'Claude-Session: ' && test "$PATHS" = "CHANGELOG.md docs/output-catalog.md extensions/pi-claude-marketplace/orchestrators/plugin/info.ts extensions/pi-claude-marketplace/shared/notification-grammar.ts extensions/pi-claude-marketplace/shared/notification-types.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/catalog-uat/fixtures/plugin-info.ts tests/orchestrators/plugin/info.test.ts tests/shared/notification-grammar.test.ts " && git diff --quiet "$FIX_SHA" -- extensions/pi-claude-marketplace/shared/notification-types.ts extensions/pi-claude-marketplace/shared/notification-grammar.ts CHANGELOG.md</automated>
  </verify>
  <done>
All three Task 1 cases are `ok`, the negative control and every pre-existing case stay `ok`; typecheck, lint, fallow, format:check, direct coverage (100% on both changed `shared/` modules, no pin edit), and `npm test` exit 0; pre-commit exits 0 with nothing modified; one commit exists whose stat lists exactly the ten non-ledger paths, whose title starts with `fix(info): `, and whose message ends with both trailer lines; `.planning/WINDOWS.md` and the operator's unrelated local edits are not in it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark ledger entry 32 fixed through the verb, commit</name>
  <files>.planning/WINDOWS.md</files>
  <action>
The ledger's rendered table is behind its fenced JSON (rows 30 and 32), and every `windows` verb refuses on that drift. Bring the table into agreement FROM the JSON (the sole source of truth; regenerating loses no prose because the JSON is the richer side of both rows — verified at planning time), then run the real verb so the tool writes `status` and `resolved_at` itself. Do not hand-edit the table and do not hand-edit entry 32's `status`.

1. From the repo root run: `node -e 'const fs=require("node:fs");const bw=require("./.claude/gsd-core/bin/lib/broken-windows.cjs");const p=".planning/WINDOWS.md";fs.writeFileSync(p,bw.renderLedger(bw.parseLedger(fs.readFileSync(p,"utf8"))),"utf8");'`. Confirm with `git diff .planning/WINDOWS.md` that the only changes are row 30's `resolved_at` cell now carrying `2026-09-09T16:14:31.361Z` and a new row 32 rendered from the JSON (status still `open`), plus frontmatter `last_updated`. If anything else changed, stop and report.

2. Run `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 32`. It prints the ledger JSON with `open_count: 0`, `fixed_count: 19`. Confirm with `node .claude/gsd-core/bin/gsd-tools.cjs windows status --pick ledger.open_count` (prints `0`) and by grepping the table for the row 32 line, which now carries `fixed` and a `resolved_at` timestamp.

3. Run `SKIP=trufflehog pre-commit run --files .planning/WINDOWS.md` (only the generic whitespace/EOF hooks apply under `.planning/`); if it rewrites the file, re-run the two steps above rather than hand-fixing, then re-run pre-commit. Stage ONLY `.planning/WINDOWS.md` and commit with title `docs(windows): mark the unfetched remote info row entry fixed` (or equivalent, `docs(windows):` scope, <= 72 chars, no phase or milestone in the title). Body: one or two lines naming ledger entry 32 and the Task 2 commit SHA (`git rev-parse --short HEAD` before this commit), then the two trailer lines. `git commit -F` from a scratchpad file.

`.planning/phases/01-manifest-read-fidelity/deferred-items.md` stays untouched (no precedent for resolution notes; the ledger is the register).
  </action>
  <verify>
    <automated>cd /home/acolomba/src/pi-claude-marketplace-manifest && test "$(node .claude/gsd-core/bin/gsd-tools.cjs windows status --pick ledger.open_count)" = "0" && ROW32=$(grep -E '^\| 32 \| 01 \| deviation \|' .planning/WINDOWS.md) && printf '%s\n' "$ROW32" | grep -q '| fixed |' && LEDGER_SHA=$(git rev-parse HEAD) && TITLE=$(git log -1 --format=%s "$LEDGER_SHA") && BODY=$(git log -1 --format=%B "$LEDGER_SHA") && NAMES=$(git diff --name-only "$LEDGER_SHA^..$LEDGER_SHA") && PATHS=$(printf '%s\n' "$NAMES" | tr '\n' ' ') && printf '%s\n' "$TITLE" | grep -E '^docs\(windows\): ' && printf '%s\n' "$BODY" | grep -q 'Co-Authored-By: Claude Opus 5' && test "$PATHS" = ".planning/WINDOWS.md " && git diff --quiet "$LEDGER_SHA" -- .planning/WINDOWS.md .planning/phases/01-manifest-read-fidelity/deferred-items.md</automated>
  </verify>
  <done>
`windows status` reports `open_count` 0 and entry 32 `fixed` with a `resolved_at` stamp in both the JSON and the regenerated table; the table also carries every other row exactly as the JSON renders it; a second commit contains only `.planning/WINDOWS.md`, its title starts with `docs(windows): `, and its body cites the Task 2 SHA and ends with both trailer lines; `deferred-items.md` has no diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace `marketplace.json` entry `dependencies` -> rendered `info` row | untrusted manifest text reaches a user-visible line on the `(remote)` row for the first time |
| `docs/output-catalog.md` -> catalog UAT contract | the catalog is the byte-exact output contract; a wrong block would pin wrong bytes |

## STRIDE Threat Register

ASVS level 1. No row reaches `high`; no package is installed.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q32-01 | Tampering | entry-declared `dependencies` strings on the `(remote)` row | low | mitigate | no new parse path: the list reaches the row only through the existing `renderDependencyList` -> `parseDeclaredDependencies` -> `withDeclaringMarketplace` -> `renderDependency` chain in `info.ts` (invalid shapes fold to the `invalid manifest` block before any row is built), the same chain the `(installed)` / `(available)` rows already render from |
| T-Q32-02 | Information disclosure | `dependencies:` line on a not-fetched plugin | low | accept | the line shows `name@marketplace` addresses plus version / short-sha constraints the marketplace already publishes; no path, no clone contents, no network reached (NFR-5 pinned by the flipped `info.test.ts` case asserting `plugin-clones/` stays absent) |
| T-Q32-03 | Denial of service | `info` on a cold git source | low | accept | unchanged: the fs-only presence probe runs as before; the change touches only the row type and the renderer |
| T-Q32-SC | Tampering | npm/pip/cargo installs | n/a | n/a | this task installs no package; the package-legitimacy gate is not triggered |
</threat_model>

<verification>
- Red evidence: Task 1 TAP shows exactly three `not ok` lines (1 grammar case, 1 info case, 1 catalog-contract case with a single `byte-mismatch` for `remote-single-scope-with-dependencies`) against HEAD `aee128c9`, with the empty-list negative control and the parser count case `ok`; the raw TAP is quoted in the SUMMARY.
- Green: the four test files pass; `npm run typecheck`, `npm run lint`, `npm run fallow`, `npm run format:check`, `npm run test:coverage:direct:commit`, and `npm test` exit 0; pre-commit exits 0 before each of the two commits.
- Scope: with `FIX_SHA` the Task 2 commit and `LEDGER_SHA` the Task 3 commit, `git diff --stat "$FIX_SHA^..$LEDGER_SHA"` lists exactly the eleven paths in `files_modified` and nothing else; the fetch-throw and warm-unreadable `(remote)` arms and the `(unavailable)` / `(partially-available)` builders in `info.ts` have no diff; the operator's local edits and untracked files are in neither commit.
- Catalog: the `remote-single-scope` block is byte-identical to HEAD; the new block is the same four lines plus `    dependencies: helper@community-mp`; counts are 213 states / 28,729 bytes in both catalog-uat test files.
- Ledger: `windows status` reports `open_count` 0; entry 32 is `fixed` in the JSON and the table; the resolution was written by the verb.
</verification>

<success_criteria>
- `info` on a not-installed, not-fetched git-source plugin whose entry declares dependencies renders the `dependencies:` line last, after `components: not resolved`, with no clone and no network (D-01-32, INFO-02, NFR-5).
- Every `componentsResolved: false` row without dependencies renders exactly as at HEAD, proven by the untouched `renders exact unresolved ${status} plugin info` loop and the empty-list negative control.
- One new catalog state bound to one fixture; the catalog UAT contract is green at 213 / 28,729.
- Two Conventional Commits with both trailer lines; ledger entry 32 fixed through `gsd-tools windows fixed 32`.
</success_criteria>

<output>
Write `/home/acolomba/src/pi-claude-marketplace-manifest/.planning/quick/260917-g97-resolve-window-32-give-the-componentsres/260917-g97-SUMMARY.md` when done. It must quote the raw Task 1 `not ok` TAP lines verbatim as the red evidence, name both commit SHAs, state the direct-coverage readings for `notification-grammar.ts` and `notification-types.ts`, record that the CHANGELOG bullet carries no PR number yet, and confirm `deferred-items.md` was left untouched.
</output>
