---
quick_id: 260917-g97
phase: quick-260917-g97
plan: 01
subsystem: notifications
tags: [info, dependencies, catalog, broken-windows]
status: complete
requires: []
provides:
  - "`PluginInfoComponentsUnresolved.dependencies?` (D-01-32)"
  - "catalog state `remote-single-scope-with-dependencies`"
affects: [info, notification-grammar, output-catalog]
tech-stack:
  added: []
  patterns: ["file-private `appendDependenciesLine` shared by both `renderPluginInfo` arms"]
key-files:
  created: []
  modified:
    - extensions/pi-claude-marketplace/shared/notification-types.ts
    - extensions/pi-claude-marketplace/shared/notification-grammar.ts
    - extensions/pi-claude-marketplace/orchestrators/plugin/info.ts
    - tests/shared/notification-grammar.test.ts
    - tests/orchestrators/plugin/info.test.ts
    - tests/architecture/catalog-uat/fixtures/plugin-info.ts
    - tests/architecture/catalog-uat/catalog-contract.test.ts
    - tests/architecture/catalog-uat/catalog-parser.test.ts
    - docs/output-catalog.md
    - CHANGELOG.md
    - .planning/WINDOWS.md
decisions:
  - "`dependencies?` lives on the unresolved arm only, not on `PluginInfoRowBase`, so only the cold-clone builder can carry it (plan-locked)"
metrics:
  duration: "13m"
  completed: "2026-09-17"
actuals:
  tokens: 5572
  tasks: 3
  commits: 2
plan_head_before: aee128c9fc5648b7c3595ef79c4268f930647d9e
---

# Quick 260917-g97: Resolve ledger entry 32 -- entry-declared dependencies on the unfetched `(remote)` info row Summary

The cold git-source `(remote)` info row now renders `    dependencies: <list>` after `    components: not resolved`, so the D-01-32 entry fallback that `info.ts` already computed reaches the screen instead of being dropped by a row type that never declared the field.

## Commits

| Task | Commit | Title |
| ---- | ---------- | ------------------------------------------------------------ |
| 1+2 | `975e6236` | `fix(info): show entry dependencies on the unfetched remote row` |
| 3 | `e01efa61` | `docs(windows): mark the unfetched remote info row entry fixed` |

`git diff --stat aee128c9..HEAD` lists exactly the eleven `files_modified` paths (131 insertions, 23 deletions). The operator's unrelated local edits and untracked files are in neither commit.

## RED evidence (Task 1, against HEAD `aee128c9`)

Three `not ok` lines in total, as the plan predicted. Raw TAP saved in the scratchpad (`red-grammar.tap`, `red-info.tap`, `red-catalog.tap`).

```text
not ok 55 - renders the entry-declared dependencies line after the unresolved marker (D-01-32)
# pass 95
# fail 1
not ok 168 - D-01-32 / NFR-5: a COLD git source renders the entry's list and creates no clone directory
# pass 167
# fail 1
not ok 4 - catalog contract matches all 20 fixture modules to 213 exact documented states
# pass 15
# fail 1
```

Diffs: the grammar case's actual lacked the fourth line (`-   '    dependencies: helper@mp'`); the info case's actual message lacked `    dependencies: from-entry@mp`; the catalog-contract case failed with a single `byte-mismatch: /claude:plugin info <plugin>@<marketplace>::remote-single-scope-with-dependencies`. The empty-list negative control (`ok 56`) and `loadCatalogExamples parses all 213 independent catalog tuples` (`ok 7`) were green before the fix, so the state count and byte total (213 / 28,729) already agreed at RED.

## GREEN gate (Task 2)

- Four Task 1 test files: 280 pass, 0 fail.
- `npm run typecheck` 0, `npm run lint` 0, `npm run fallow` 0 (the dupes summary prints its usual red glyph; exit code is 0), `npm run format:check` 0.
- `npm run test:coverage:direct:commit` 0. Direct coverage readings: `notification-grammar.ts` 100% (branches 210/210, functions 47/47, lines 1638/1638); `notification-types.ts` 100% (branches 26/26, functions 3/3, lines 738/738); `orchestrators/plugin/info.ts` 100% (branches 396/396, functions 79/79, lines 2835/2835). The three recorded shortfalls (`bridges/agents/convert.ts`, `bridges/commands/discover.ts`, `orchestrators/plugin/install-outcome.ts`) matched `scripts/test-coverage-direct.pin.json` exactly; no pin edit.
- `npm test`: 6528 pass, 0 fail, 0 skipped.
- `SKIP=trufflehog pre-commit run --files <ten paths>`: all hooks passed, nothing rewritten.

## Catalog state

- New state `remote-single-scope-with-dependencies` bound to its fixture; the `remote-single-scope` block bytes are untouched (the only removed line in `docs/output-catalog.md` is the reworded section-overview sentence).
- `EXPECTED_STATE_COUNT` 212 -> 213; `EXPECTED_UTF8_BYTES` 28,543 -> 28,729 (the new example's `expected` is 186 UTF-8 bytes, as measured at planning time). Both `catalog-contract.test.ts` and `catalog-parser.test.ts` carry 213.

## Ledger (Task 3)

1. Regenerated the table from the fenced JSON via `broken-windows.cjs` `renderLedger(parseLedger(...))`. The diff was exactly the two rows the plan predicted: row 30's `resolved_at` cell now carries `2026-09-09T16:14:31.361Z`, and row 32 gained its table row (`open`). Frontmatter `last_updated` did not change.
2. `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 32` exited 0 and printed the ledger with `"open_count": 0`, `"fixed_count": 19`, `"waived_count": 13`, `"total_count": 32`, `"last_updated": "2026-09-17T16:14:30.946Z"`. Entry 32 now reads `status: fixed`, `resolved_at: 2026-09-17T16:14:30.946Z` in the JSON and in the regenerated table row.
3. `windows status --pick ledger.open_count` fails with `command output was not JSON` (the verb pretty-prints; known `--pick` quirk), so the confirmation was done by parsing the full `windows status` output: `open_count 0`, `fixed_count 19`, `entry32 fixed`.
4. `.planning/phases/01-manifest-read-fidelity/deferred-items.md` is untouched (`git diff --quiet aee128c9 HEAD -- <path>` passes).

## Deviations from Plan

### Documented at planning time

**1. CHANGELOG bullet carries no PR number.** The `[Unreleased]` bullet ends at its period with no `(#NNN)` because this branch has no pull request yet. The ship step appends the number.

**2. `--pick` on `windows status`.** The plan's confirmation command `windows status --pick ledger.open_count` errors on this checkout's pretty-printed output; the same fact was confirmed by JSON-parsing the full output (see Ledger step 3). No ledger content differs from what the plan expected.

Otherwise the plan executed exactly as written. No Rule 1-4 deviations, no auth gates, no package installs.

## Known Stubs

None.

## Threat Flags

None. The list reaches the row only through the existing `renderDependencyList` chain in `info.ts` (T-Q32-01 mitigation holds: no new parse path); the flipped `info.test.ts` case still asserts `plugin-clones/` stays absent (NFR-5).

## Self-Check: PASSED

- Commits `975e6236` and `e01efa61` exist on `features/manifest` (`git log`).
- All eleven modified files exist and are in the two commits; `git diff --stat aee128c9..HEAD` lists nothing else.
- `commits: 2` measured from `plan_head_before` (`git rev-list --count aee128c9...HEAD` = 2).
