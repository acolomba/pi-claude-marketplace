---
phase: 260912-fp2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/output-catalog.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [WINDOW-24, WINDOW-25, WINDOW-29]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every test-file path named in `docs/output-catalog.md` resolves to a file that exists in the tree."
    - "The AUTH-03 byte-form lock reference in the catalog points at the suite that actually asserts the emission string, and that suite's own back-reference to the catalog entry still holds — the lockstep pair agrees in both directions."
    - "The AUTH-09 no-token-interpolatable sentence in the same catalog paragraph is byte-identical after the edit."
    - "`.planning/codebase/TESTING.md` names no `tests/helpers/` path and no retired test-double symbol; every test path it names resolves on disk."
    - "The two test-suite globs TESTING.md quotes match the `test` script in `package.json` member-for-member, and the file counts beside them match the tree."
    - "`.planning/codebase/CONVENTIONS.md` is NOT modified: its barrels passage was already corrected by `a64d00a4` (2026-09-09), so ledger entry 29 is stale — the ledger, not the document, was wrong."
  artifacts:
    - docs/output-catalog.md
    - .planning/codebase/TESTING.md
    - .planning/quick/260912-fp2-correct-three-stale-documentation-refere/260912-fp2-SUMMARY.md
  key_links:
    - "`docs/output-catalog.md` (catalog-state: device-flow-prompt) <-> `tests/domain/github-auth.test.ts` — the test's own comment names this catalog entry and declares the lockstep obligation, so a one-sided repoint re-creates the drift in the other direction."
    - "TESTING.md's quoted globs <-> `package.json` `scripts.test` — the glob is a quotation of a live script, not prose, so it must be copied from the script rather than composed."
    - "`docs/output-catalog.md` is read at test time by five architecture gates (`catalog-uat/catalog-parser`, `catalog-uat/catalog-contract`, `partial-vocabulary-guard`, `notify-closed-set-locks`, `hooks-cap-notify`) — any catalog edit must be re-measured against them, not assumed inert."
---

<objective>
Close WINDOWS.md ledger entries 24, 25 and 29 by correcting stale documentation
references — and by declining to correct the one that is already right.

All three claims were measured against the live tree during planning. Two are real
drift; one is ledger staleness. The dispositions below are findings, not hypotheses:

| entry | file | verdict |
|---|---|---|
| 24 | `docs/output-catalog.md` | **REAL** — names a deleted suite as the AUTH-03 byte-form lock. One-token repoint. |
| 25 | `.planning/codebase/TESTING.md` | **REAL, and wider than a path swap** — `tests/helpers/` is gone, two of the four named modules were retired outright (not moved), and the Mocking section's stated framework claim is now false. |
| 29 | `.planning/codebase/CONVENTIONS.md` | **LEDGER STALE — no edit.** The passage was corrected by `a64d00a4` (2026-09-09), six days after the entry was recorded. |

Purpose: the two `.planning/codebase/*.md` files are `@`-included from `CLAUDE.md`, so
their content loads into every session. A false path there is read as fact on every run.
Output: two corrected documents, one documented no-op, and a SUMMARY that records which
ledger entry was stale rather than which file needed changing.
</objective>

<execution_context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/workflows/execute-plan.md
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/acolomba/pi-claude-marketplace-refine-unit-tests/CLAUDE.md
@/tmp/claude-1000/-home-acolomba-pi-claude-marketplace-refine-unit-tests/18969798-5830-434f-9201-d9cd7ed141d0/scratchpad/window-pass-spec.md

Measured during planning — treat as established, do not re-derive:

**Entry 24.** `git ls-files | grep device-flow` returns no `tests/shared/` suite; the
deleted file is absent. The AUTH-03 lock is `tests/domain/github-auth.test.ts`, test
`"emits the documented AUTH-03 prompt before any token is acquired"` (line 191), which
asserts the exact emission string `Open https://github.com/login/device and enter: ABCD-1234`
and whose comment already cites this catalog entry by its `catalog-state` marker. The
stale reference is a single sentence in the prose paragraph under
`<!-- catalog-state: device-flow-prompt -->`. Pre-edit greps over `docs/output-catalog.md`:
stale-path hits 1, `tests/domain/github-auth.test.ts` hits 0, AUTH-09 sentence hits 1.

**Entry 25.** `tests/helpers/` does not exist. Of the four modules the ledger calls
"pre-move", only two moved:
- `credential-mock.ts`, `git-mock.ts` — **retired, not moved.** `MockCredentialState`,
  `makeMockCredentialOps`, `makeMockGitOps`, `makeMockDeviceFlowHttp` return zero hits
  repo-wide.
- `marketplace-seed.ts` -> `tests/edge/handlers/marketplace-seed.ts`
- `source-scan.ts` -> `tests/architecture/source-scan.ts`, with its own
  `tests/architecture/source-scan.test.ts`

What replaced them: `strong-mock@^9.2.2` is a devDependency, imported as
`{ mock, verify, when }` by 31 test files; reusable concern-owned doubles are `*-fake.ts`
modules co-located with the concern's own test directory, each paired with a `*-contract.ts`
shared contract suite —
`tests/platform/{credential-ops,credential-process,git-ops,removal-ops}-fake.ts`,
`tests/domain/device-flow-fake.ts`,
`tests/platform/{credential-ops,git-ops,removal-ops}-contract.ts`,
`tests/domain/device-flow-contract.ts`. Factories are `create*Fake(options)`; every options
bag declares `boundary: "memory"`; calls land in a readonly `calls` bag; failure injection
is per-operation `*Error?: Error` option fields (`cloneError`, `fetchError`, `checkoutError`,
`resolveRemoteRefError`, `fillError`, `approveError`, `rejectError`) — NOT the retired
`*Throws?: Error` shape.

`withHermeticHome` DOES now exist — exported from `tests/orchestrators/plugin/update-flow.test.ts:352`
and imported by sibling suites. It redirects HOME to a real temp directory; it is not an
in-memory filesystem layer, so the surrounding "never mock the filesystem" claim stays true
and must survive.

Live `scripts.test` glob:
`tests/{architecture,bridges,domain,edge,orchestrators,persistence,platform,scripts,shared,transaction}/**/*.test.ts`
plus `tests/index.test.ts`.
Live `scripts.check`:
`typecheck && lint && fallow && format:check && test:corresponding && test:corresponding:negative && test:coverage:direct:negative && test && test:integration`.
Live `tests/` children: architecture, bridges, domain, e2e, edge, fixtures, integration,
live-uat, orchestrators, persistence, platform, scripts, shared, transaction, and the
top-level file `index.test.ts`.
Measured counts (`find tests -name '*.test.ts' -type f`): 303 total; 284 under the `npm test`
glob (architecture 47, bridges 64, domain 26, edge 30, orchestrators 71, persistence 9,
platform 7, scripts 1, shared 25, transaction 3, plus `index.test.ts`); integration 13;
e2e 6. 284 + 13 + 6 = 303, so the partition is complete.

**Entry 29.** `.planning/codebase/CONVENTIONS.md` line 165 reads: barrels exist per bridge
kind "and at `orchestrators/import/index.ts`. The aggregate `bridges/index.ts` ... were all
removed as unreachable". The tree agrees: no `bridges/index.ts`; five per-kind barrels
present; `orchestrators/import/index.ts` present. `grep -n "bridges/index"` over the file
returns exactly one hit — that sentence. `git log -L165,165` shows `a64d00a4` (2026-09-09,
`docs(06): repoint architecture docs at post-split owners`) replaced the exact wording the
ledger quotes. The entry was recorded 2026-09-03. Nothing to fix.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Repoint the AUTH-03 byte-form lock in the output catalog, end to end</name>
  <files>docs/output-catalog.md</files>
  <precondition>The working tree has no uncommitted changes to `docs/output-catalog.md` (`git status --porcelain docs/output-catalog.md` prints nothing), so the commit in this task carries only this edit.</precondition>
  <action>
This is the thin end-to-end slice: one token through edit -> gate -> hook -> commit, proving
the whole path on the cheapest change before Task 2's larger rewrite.

<!-- planner-discipline-allow: device-flow-prompt -->
<!-- planner-discipline-allow: device-flow-prompt.test.ts -->

Locate the prose paragraph under the `catalog-state: device-flow-prompt` marker by content,
NOT by the line number the ledger records (2729) — the file has grown and the passage now
sits near line 3015. The negative grep in this task's verify is scoped to
`docs/output-catalog.md`, so naming the retired suite here cannot invalidate it.

In the single sentence beginning "The byte form is locked by", replace the deleted suite path
with `tests/domain/github-auth.test.ts`. Change nothing else in the paragraph. Specifically,
the two contract sentences that precede it — the AUTH-03 one-time-code sentence and the
AUTH-09 sentence ending "are NOT interpolatable into this message" — must come out
byte-identical; they are the security-relevant content of the entry and are verified below.

Do not reflow, re-wrap, or re-punctuate the paragraph. The file is read at test time by five
architecture gates and scanned token-wise by the D-75-01 vocabulary guard, so an
opportunistic prose tidy is a gate risk with no upside.

Then run the gate set and the lockstep pair (command in verify), run
`pre-commit run --files docs/output-catalog.md`, fix and restage until clean, and commit with
`docs(catalog): repoint the AUTH-03 byte-form lock reference`. Body: state that the named
suite was deleted and the lock now lives in `tests/domain/github-auth.test.ts`, keeping the
lockstep pair symmetric. No phase or milestone reference in the message. After committing,
run `git status --porcelain docs/output-catalog.md` — the markdown hooks rewrite files
mid-commit while the commit still succeeds; if it is dirty, fix with a follow-up commit,
never `--amend`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && test "$(grep -c 'device-flow-prompt\.test\.ts' docs/output-catalog.md)" = 0 && test "$(grep -c 'tests/domain/github-auth\.test\.ts' docs/output-catalog.md)" -ge 1 && test "$(grep -c 'are NOT interpolatable into this message' docs/output-catalog.md)" = 1 && node --test tests/architecture/catalog-uat/catalog-parser.test.ts tests/architecture/catalog-uat/catalog-contract.test.ts tests/architecture/partial-vocabulary-guard.test.ts tests/architecture/notify-closed-set-locks.test.ts tests/architecture/hooks-cap-notify.test.ts tests/domain/github-auth.test.ts</automated>
  </verify>
  <done>
`docs/output-catalog.md` names no deleted suite and names `tests/domain/github-auth.test.ts`
as the AUTH-03 byte-form lock. The AUTH-09 sentence is intact (exactly one occurrence). The
five catalog-reading gates plus the lock suite pass — 129 tests, measured green on the
pre-edit tree in 3.8s, so a failure here is caused by this edit and nothing else. The edit is
committed, and `git status --porcelain docs/output-catalog.md` is empty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the TESTING.md passages that name tests/helpers or a retired double</name>
  <files>.planning/codebase/TESTING.md</files>
  <action>
<!-- planner-discipline-allow: tests/helpers -->
<!-- planner-discipline-allow: credential-mock -->
<!-- planner-discipline-allow: git-mock -->
<!-- planner-discipline-allow: MockCredentialState -->
<!-- planner-discipline-allow: makeMock -->
<!-- planner-discipline-allow: Throws?: Error -->

Every negative grep in this task's verify is scoped to `.planning/codebase/TESTING.md`, so
naming the retired directory and symbols here cannot invalidate them — and the executor
cannot remove text it is forbidden to read.

Correct every passage in `.planning/codebase/TESTING.md` that names `tests/helpers/`, a
pre-move module path, or the retired hand-written-double API. `grep -n helpers` over the file
returns the full in-scope set: lines 16, 38, 51, 91, 93, 115, 124, 127, 144, 147. Locate each
by content. Use the measured facts recorded in `<context>` above; do not re-derive them and
do not introduce a path you have not confirmed exists.

The corrections, passage by passage:

1. Run Commands fence — quote the live `scripts.test` glob from `package.json` verbatim
   (`scripts` replaces `helpers`, and `tests/index.test.ts` is a second glob argument). While
   inside this same fence, also correct the `npm run check` chain line: it is four members
   short of the live script. That line is not a `helpers` passage, but it sits three lines
   below one being rewritten inside the same fence, and leaving a knowingly-false chain there
   would be indefensible. Disclose it in the commit body as a same-fence correction.
2. Directory listing — drop the `helpers/` row, add a `scripts/` row and the top-level
   `index.test.ts` file, so the block matches the live children of `tests/`.
3. Verified counts — replace the four counts with the measured ones (303 / 284 / 13 / 6) and
   replace the glob parenthetical's `helpers` with `scripts`. A corrected glob beside an
   uncorrected count is self-contradicting, so both move together. Re-stamp the "Verified
   counts" and "Verified directory listing" dates to today.
4. Mocking / Framework — the claim is false in both halves. State the current reality:
   `strong-mock` is a devDependency used via `mock`/`when`/`verify`, alongside reusable
   concern-owned `*-fake.ts` modules co-located with the concern's test directory, each
   paired with a `*-contract.ts` suite the real port and its fake both run.
5. Mocking / Patterns — retire the `MockCredentialState` code block and the `makeMock*`
   naming. Replace with the live shape: `create*Fake(options)` factories returning the
   production-shaped port plus a readonly `calls` bag; `boundary: "memory"` declared on every
   options bag; per-operation `*Error?: Error` failure injection. Name only the fake and
   contract modules listed in `<context>`. Keep the two substantive disciplines the old text
   recorded, because both still hold and are cited from CONVENTIONS.md: doubles are pure
   in-memory (no filesystem, no env mutation, no subprocess), and they import production
   contracts with `import type` only.
6. What to Mock — add `RemovalOps` to the boundary list; `tests/platform/removal-ops-fake.ts`
   exists and the port is injected.
7. What NOT to Mock — the parenthetical asserting no `withHermeticHome`-style helper exists
   is now false and must go. Replace it with the accurate statement: `withHermeticHome`
   exists, exported from `tests/orchestrators/plugin/update-flow.test.ts` and imported by
   sibling suites, and it redirects HOME to a real temp directory rather than interposing an
   in-memory filesystem. The bullet's own claim — that the filesystem is never mocked — is
   still true and must survive the edit; do not weaken it.
8. Fixtures and Factories — repoint the seed helper to `tests/edge/handlers/marketplace-seed.ts`.
9. Location — the convention is no longer a single shared directory. State that support
   modules sit beside the concern they serve (`tests/edge/handlers/marketplace-seed.ts`,
   `tests/architecture/source-scan.ts` with its own `tests/architecture/source-scan.test.ts`).
10. Test Types / Unit — drop `helpers` from the glob, add `scripts`. Replace "injected mocks"
    with wording covering both `strong-mock` doubles and the concern-owned fakes.
11. Test Types / Architecture — repoint `source-scan.ts` to `tests/architecture/source-scan.ts`.
12. Test Types / Integration — the file count is 13, not 10.
13. Error Testing — the `*Throws?: Error` field name is the retired API. Use the live
    `*Error?: Error` option fields, and note `strong-mock`'s rejection stubbing as the other
    route.

Out of scope — observe and record in the SUMMARY as a follow-up, do NOT fix here: the static
fixtures bullet names `tests/fixtures/bad-imports/`, which no longer exists. It is not a
`helpers` passage and not part of ledger entry 25; editing it would widen this item beyond
the approved disposition. Do not re-audit the rest of the file (the coverage caveat, the
fallow CRAP note, the e2e section) for unrelated drift.

Match the file's existing voice: measured statements with paths and symbol names, no hedging,
no promotional framing. Markdown here is formatted by **mdformat**; never run
`prettier --write` on it.

Then run `pre-commit run --files .planning/codebase/TESTING.md`, fix and restage until clean,
and commit with `docs(codebase): correct stale test-layout and mocking references`. Body:
name the retired directory, note that two of the four modules were retired rather than moved,
record the same-fence `npm run check` correction, and state that the counts were re-measured.
No phase or milestone reference. After committing, check
`git status --porcelain .planning/codebase/TESTING.md` and fix any hook rewrite with a
follow-up commit, never `--amend`.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && test "$(grep -c 'tests/helpers' .planning/codebase/TESTING.md)" = 0 && test "$(grep -cE 'credential-mock|git-mock|MockCredentialState|makeMock[A-Za-z]+|Throws\?: Error' .planning/codebase/TESTING.md)" = 0 && test "$(grep -c 'strong-mock' .planning/codebase/TESTING.md)" -ge 1 && for p in $(grep -oE 'tests/[A-Za-z0-9_./-]+\.(ts|json|mjs)' .planning/codebase/TESTING.md | sort -u); do test -e "$p" || { echo "MISSING: $p"; exit 1; }; done && test "$(grep -c 'test:corresponding' .planning/codebase/TESTING.md)" -ge 1 && node --test tests/architecture/partial-vocabulary-guard.test.ts</automated>
  </verify>
  <done>
`.planning/codebase/TESTING.md` contains zero `tests/helpers` references and zero retired
test-double symbols; it names `strong-mock`; every `tests/...` path it names resolves on
disk; the quoted `npm run check` chain includes the pairing gates. The D-75-01 vocabulary
guard still passes (it scans `tests/**/*.ts`, so it proves the rewrite named no retired
vocabulary that the guard polices). The edit is committed and the working tree is clean for
this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record entry 29 as ledger-stale — confirm, and make no edit</name>
  <files>.planning/quick/260912-fp2-correct-three-stale-documentation-refere/260912-fp2-SUMMARY.md</files>
  <action>
Re-confirm on the tree at hand, then write the disposition record. This task deliberately
produces no source diff — its deliverable is the finding that the ledger was wrong, which the
downstream `260912-fp3` item needs in order to mark entry 29 `fixed` with a truthful reason
rather than a fabricated one.

Confirm three things with the verify command below: `bridges/index.ts` is absent, the five
per-kind barrels are present, and `.planning/codebase/CONVENTIONS.md` already states that the
aggregate barrel was removed. Then confirm the file is untouched by this plan
(`git status --porcelain .planning/codebase/CONVENTIONS.md` prints nothing).

In the SUMMARY, record the three dispositions distinctly — this distinction is the point of
the item, so do not flatten it into "three files corrected":
- entry 24: fixed by an edit to `docs/output-catalog.md`
- entry 25: fixed by an edit to `.planning/codebase/TESTING.md`, and note that the ledger
  under-described it — two of the four named modules were retired outright rather than moved,
  and the Mocking section's framework claim had gone false
- entry 29: **no edit; the ledger was stale, not the document.** Cite `a64d00a4`
  (2026-09-09, `docs(06): repoint architecture docs at post-split owners`) as the commit that
  had already replaced the exact wording entry 29 quotes, six days after the entry was
  recorded 2026-09-03.

Also record the two out-of-scope observations for follow-up: `tests/fixtures/bad-imports/`
is named in TESTING.md but does not exist, and both ledger entries recorded line numbers
(24 -> 2729, 29 -> 151) that no longer locate their passages, which is why content-based
location was used throughout.

Do not run `npm run check` — the batch coordinator runs it once after every item lands.
  </action>
  <verify>
    <automated>cd /home/acolomba/pi-claude-marketplace-refine-unit-tests && test ! -e extensions/pi-claude-marketplace/bridges/index.ts && test "$(ls extensions/pi-claude-marketplace/bridges/*/index.ts | wc -l)" = 5 && grep -q 'The aggregate `bridges/index.ts`' .planning/codebase/CONVENTIONS.md && grep -q 'were all removed as unreachable' .planning/codebase/CONVENTIONS.md && test -z "$(git status --porcelain .planning/codebase/CONVENTIONS.md)" && test -e .planning/quick/260912-fp2-correct-three-stale-documentation-refere/260912-fp2-SUMMARY.md</automated>
  </verify>
  <done>
`.planning/codebase/CONVENTIONS.md` is unmodified and independently confirmed correct against
the tree. The SUMMARY records entry 29 as ledger-stale with the correcting commit cited,
keeps the three dispositions distinct, and lists the two out-of-scope observations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future agent sessions | `.planning/codebase/TESTING.md` and `CONVENTIONS.md` are `@`-included from `CLAUDE.md`, so their text is loaded as fact into every session. A wrong statement here is a supply chain into future decisions. |
| documentation -> architecture gates | `docs/output-catalog.md` is read at test time by five gates and token-scanned by the D-75-01 vocabulary guard. Prose in it is gate input, not inert text. |

No new code path, network call, credential handler, or package install is introduced by this
item, so no `T-fp2-SC` package-legitimacy row applies and no RESEARCH.md legitimacy audit is
required. All three tasks edit or read documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-fp2-01 | Tampering | `docs/output-catalog.md` AUTH-03 paragraph | medium | mitigate | The repoint sits in the same paragraph as the AUTH-09 guarantee that no access token is interpolatable into the prompt. Task 1 restricts the edit to the suite-path token and forbids reflowing; Task 1's verify asserts the AUTH-09 sentence occurs exactly once, so a drive-by weakening of that guarantee fails the gate. |
| T-fp2-02 | Tampering | five catalog-reading architecture gates | medium | mitigate | Catalog prose is gate input. Task 1 re-runs all five gates plus the lock suite against a baseline measured green pre-edit (129 tests, 3.8s), so any gate regression is attributed to this edit rather than discovered later in the batch-level `npm run check`. |
| T-fp2-03 | Information disclosure | `docs/output-catalog.md` device-flow example | low | accept | The catalog's example code `ABCD-1234` and URL `github.com/login/device` are synthetic and already committed; no real credential is introduced. trufflehog runs in pre-commit as backstop. |
| T-fp2-04 | Repudiation | WINDOWS.md ledger entry 29 | medium | mitigate | Marking a window `fixed` with no corresponding change is indistinguishable from silently skipping it unless the reason is recorded. Task 3 requires citing `a64d00a4` as the commit that already made the correction, so the downstream disposition is auditable rather than asserted. |
| T-fp2-05 | Tampering | `.planning/codebase/TESTING.md` | medium | mitigate | Replacing a known-stale path with an unverified one converts detected drift into undetected drift. Task 2's verify resolves every `tests/...` path the file names against the filesystem and fails on the first miss, so no invented path can survive. |
</threat_model>

<verification>
- `grep -c 'tests/helpers' .planning/codebase/TESTING.md` is 0.
- No retired double symbol (`credential-mock`, `git-mock`, `MockCredentialState`, `makeMock*`,
  `Throws?: Error`) appears in `.planning/codebase/TESTING.md`.
- Every `tests/...` path named in `.planning/codebase/TESTING.md` resolves on disk.
- `docs/output-catalog.md` names no deleted suite; the AUTH-09 sentence occurs exactly once.
- The five catalog-reading gates plus `tests/domain/github-auth.test.ts` pass.
- `git diff --stat` for this item touches exactly two files: `docs/output-catalog.md` and
  `.planning/codebase/TESTING.md`. `.planning/codebase/CONVENTIONS.md` is absent from the diff.
- Each commit message is Conventional Commits, title <=72 chars, body lines <=80 chars, and
  carries no phase or milestone reference.
- `npm run check` is NOT run here; the batch coordinator runs it once after all items land.
</verification>

<success_criteria>
Ledger entries 24 and 25 are closed by verified edits to two files, and entry 29 is closed
with no edit and a cited reason. The SUMMARY distinguishes the two cases, so `260912-fp3` can
dispose all three truthfully. No file outside `files_modified` is changed.
</success_criteria>

<output>
Create `.planning/quick/260912-fp2-correct-three-stale-documentation-refere/260912-fp2-SUMMARY.md` when done
</output>
