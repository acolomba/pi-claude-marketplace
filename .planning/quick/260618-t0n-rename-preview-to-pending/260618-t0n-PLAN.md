---
phase: quick
plan: 260618-t0n
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/edge/router.ts
  - extensions/pi-claude-marketplace/edge/register.ts
  - extensions/pi-claude-marketplace/edge/handlers/plugin/preview.ts (→ pending.ts)
  - extensions/pi-claude-marketplace/orchestrators/reconcile/preview.ts (→ pending.ts)
  - extensions/pi-claude-marketplace/edge/handlers/tools.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/apply.ts
  - extensions/pi-claude-marketplace/orchestrators/reconcile/plan.ts
  - extensions/pi-claude-marketplace/shared/notify.ts (notification kind)
  - tests/edge/handlers/plugin/preview.test.ts (→ pending.test.ts)
  - tests/orchestrators/reconcile/preview.test.ts (→ pending.test.ts)
  - tests/edge/router.test.ts
  - tests/edge/completions/provider.test.ts
  - tests/architecture/*.test.ts (notify-types, catalog-uat, etc.)
  - tests/orchestrators/reconcile/notify.test.ts
  - tests/orchestrators/reconcile/plan.test.ts
  - docs/messaging-style-guide.md
  - docs/output-catalog.md
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The user-facing CLI subcommand is `pending` (no longer `preview`). `/claude:plugin pending [--scope user|project]` is the documented invocation."
    - "All command-name literal strings `\"preview\"` have been renamed to `\"pending\"` in router dispatch arrays, switch cases, usage strings, and completion providers."
    - "Source files `edge/handlers/plugin/preview.ts` and `orchestrators/reconcile/preview.ts` are renamed to `pending.ts`. Test counterparts are renamed in lockstep."
    - "Symbols named after the command are renamed: `previewReconcile` → `pendingReconcile`, `makePreviewHandler` → `makePendingHandler`, `PreviewReconcileOptions` → `PendingReconcileOptions`."
    - "The notification kind `reconcile-preview-empty` is renamed to `reconcile-pending-empty`. Notification grammar tests and catalog UAT are updated accordingly."
    - "Documentation references to the command in `docs/messaging-style-guide.md` and `docs/output-catalog.md` are updated. A CHANGELOG entry under `[Unreleased]` records the rename as a breaking user-facing change."
    - "Incidental uses of the word `preview` that describe the OPERATION conceptually (e.g. 'dry-run preview pattern' in `enable-disable.ts`) are NOT renamed -- those are descriptions, not command-name references."
    - "Historical CHANGELOG entries that mention `preview` in the context of past versions are NOT rewritten (history stays history)."
    - "`npm run check` passes (typecheck + ESLint + Prettier + tests) at the end."
  artifacts: []
  key_links: []
---

<objective>
Rename the user-facing `/claude:plugin preview` command to `/claude:plugin pending`. Propagate the rename through:

1. CLI dispatch surface: router subcommand list, switch case, usage/help strings, completion provider.
2. File names: `edge/handlers/plugin/preview.ts` → `pending.ts`; `orchestrators/reconcile/preview.ts` → `pending.ts`. Test counterparts in lockstep.
3. Symbols named after the command: `previewReconcile`, `makePreviewHandler`, `PreviewReconcileOptions`, and the `reconcile-preview-empty` notification kind.
4. Documentation: `docs/messaging-style-guide.md`, `docs/output-catalog.md`, and a new CHANGELOG `[Unreleased]` entry recording the breaking rename.

Out of scope: incidental descriptive uses of the word "preview" (e.g. "dry-run preview pattern", "preview-empty notification" as a concept description) and historical CHANGELOG entries.

Purpose: align the command name with the operator's mental model -- `pending` better describes "the changes this scope would apply on the next reconcile" than `preview` does.
</objective>

<execution_context>
Mechanical rename. No discussion/research/validation phases. Execute in two atomic commits:

  - Commit 1: source + tests rename (the breaking change itself).
  - Commit 2: docs + CHANGELOG entry.

Each commit must leave `npm run check` green.
</execution_context>

<tasks>

## Task 1: Rename command + symbols + files in source

Touch points:
- `extensions/pi-claude-marketplace/edge/router.ts`: rename `"preview"` literal in `TOP_LEVEL_SUBCOMMANDS`, the `preview` field on the handler interface, the `case "preview":` dispatch, and the usage string.
- `extensions/pi-claude-marketplace/edge/register.ts`: rename `makePreviewHandler` import and the `preview:` field on the handler bundle.
- Rename file: `edge/handlers/plugin/preview.ts` → `pending.ts`. Inside: rename `makePreviewHandler` → `makePendingHandler`, update USAGE string, update file header comment.
- Rename file: `orchestrators/reconcile/preview.ts` → `pending.ts`. Inside: rename `previewReconcile` → `pendingReconcile`, `PreviewReconcileOptions` → `PendingReconcileOptions`. Keep any descriptive prose ("dry-run preview" → "dry-run pending"? No -- "preview" here describes what the operation IS conceptually; replace with "pending" only where it names the command. Specifically: rename "/claude:plugin preview" → "/claude:plugin pending" in docstrings; rename `reconcile-preview-empty` notification kind → `reconcile-pending-empty`).
- Update notification kind in `shared/notify.ts` and any rendering site: `reconcile-preview-empty` → `reconcile-pending-empty`.
- Update remaining importers: `apply.ts` references `preview.ts::previewReconcile` in a comment; update both the path and the symbol.

Run `npm run check` — fix any lint/format/test fallout.

## Task 2: Rename in tests

- Rename `tests/edge/handlers/plugin/preview.test.ts` → `pending.test.ts`. Update imports + test titles that name the command.
- Rename `tests/orchestrators/reconcile/preview.test.ts` → `pending.test.ts`. Update imports + test titles that name `previewReconcile` or the command.
- Update other test files that reference the command literal or the symbol: `tests/edge/router.test.ts`, `tests/edge/completions/provider.test.ts`, `tests/architecture/notify-grammar-invariant.test.ts`, `tests/architecture/notify-types.test.ts`, `tests/architecture/no-orchestrator-network.test.ts`, `tests/architecture/catalog-uat.test.ts`, `tests/orchestrators/reconcile/notify.test.ts`, `tests/orchestrators/reconcile/plan.test.ts`.

Run `npm run check` again.

Commit (1):

```
refactor: rename `preview` command to `pending`

BREAKING: the `/claude:plugin preview` subcommand is renamed to
`/claude:plugin pending`. The handler signature, the notification
kind `reconcile-preview-empty` → `reconcile-pending-empty`, the
`previewReconcile` orchestrator entrypoint, and the source/test
files are renamed accordingly.

Descriptive uses of the word `preview` in comments (where it names
the concept rather than the command) are preserved.
```

## Task 3: Docs + CHANGELOG

- Update `docs/messaging-style-guide.md`: rename command literals.
- Update `docs/output-catalog.md`: rename command literals and notification-kind references.
- Add an `[Unreleased]` entry to `CHANGELOG.md` recording the rename as a BREAKING change.

Run `npm run check`.

Commit (2):

```
docs: record `preview` → `pending` command rename

User-facing docs and CHANGELOG entry for the BREAKING rename of
`/claude:plugin preview` → `/claude:plugin pending`.
```

</tasks>
