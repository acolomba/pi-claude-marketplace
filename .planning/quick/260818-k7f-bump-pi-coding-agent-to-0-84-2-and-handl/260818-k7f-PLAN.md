---
phase: quick-260818-k7f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
  - tests/bridges/hooks/settle.test.ts
  - CHANGELOG.md
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/codebase/STACK.md
autonomous: true
requirements: [STOP-01, NFR-7]
estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run typecheck` is green with `@earendil-works/pi-coding-agent@^0.84.2` installed."
    - "A settle whose cached assistant message carries `stopReason` \"deferred\" dispatches no bucket and sends no re-entry message."
    - "The `default:` exhaustiveness pin in `settle.ts` still assigns `last.stopReason` to a `never`-typed binding."
    - "`npm run check` (typecheck + lint + fallow + format:check + unit + integration) exits 0."
    - "The four unrelated working-tree paths under `.claude/` are neither staged nor committed."
  artifacts:
    - extensions/pi-claude-marketplace/bridges/hooks/settle.ts
    - tests/bridges/hooks/settle.test.ts
    - package.json
    - package-lock.json
    - CHANGELOG.md
  key_links:
    - "`platform/pi-api.ts` derives `StopReason` structurally from `AssistantMessage[\"stopReason\"]`, so the installed peer package version is what widens the union that `settle.ts` switches on."
    - "The devDependency bump and the new `deferred` arm must land in the SAME commit: with the bump alone, the `never` pin at settle.ts:207 is a typecheck error, so an intermediate commit would be red."
---

<objective>
Bump the `@earendil-works/pi-coding-agent` devDependency from `^0.83.0` to `^0.84.2`
and handle the `"deferred"` member that 0.84.2 adds to the `StopReason` union.

Purpose: the compile-time exhaustiveness pin in the hooks settle dispatcher
(NFR-7) fired exactly as designed on Dependabot PR #136, producing one typecheck
error. `"deferred"` is an in-flight provider state carrying a `DeferredHandle`
to poll later, semantically like `"pending"` — it is not a turn ending, so it
must run neither the Stop bucket (STOP-01) nor the StopFailure arm.

Output: a single green commit on `features/pi-coding-agent-0-84-2` carrying the
dependency bump, the new defensive no-op arm, its mirroring unit test, the
refreshed dev-dependency figure in the three docs that state it, and a
`[Unreleased]` CHANGELOG entry.
</objective>

<execution_context>
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/workflows/execute-plan.md
@/Users/acolomba/src/pi-claude-marketplace/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/bridges/hooks/settle.ts
@tests/bridges/hooks/settle.test.ts

Verified facts — do NOT re-research these:

1. pi-ai 0.84.2 (`dist/types.d.ts:277`) widens the union to
   `"pending" | "stop" | "length" | "toolUse" | "error" | "aborted" | "deferred"`.
   `platform/pi-api.ts:118` derives our `StopReason` structurally from
   `AssistantMessage["stopReason"]`, so the widening reaches `settle.ts`
   automatically.
2. CI on PR #136 reported exactly ONE typecheck error:
   `bridges/hooks/settle.ts(197,15): error TS2322: Type '"deferred"' is not
   assignable to type 'never'.` No other site needs a change.
3. `FailureStopReason` (`bridges/hooks/payloads/stop-failure.ts:106`) is
   `Extract<StopReason, "error" | "length">` and is unaffected by the widening.
4. `tests/architecture/peer-floor.test.ts` asserts only the `peerDependencies`
   floor `>=0.80.5`. That floor does NOT change; only the devDependency moves.
5. The `default:` arm's `never` pin is deliberate and MUST be kept. Do not
   delete it, do not widen its type, do not replace it with a cast.
</context>

<source_coverage_audit>
| Source item | Covered by |
|-------------|------------|
| Bump devDependency to `^0.84.2`, regenerate lockfile via npm (no hand edit) | Task 1 |
| Add `deferred` to the defensive no-op case group with a short STOP-01 comment | Task 1 |
| Keep the `never` exhaustiveness pin intact | Task 1 (`<verify>` positive-greps for it) |
| Mirror the `STOP-01: stopReason toolUse is a defensive no-op` test for `deferred` | Task 1 |
| Update stale dev-dep figure in `CLAUDE.md` and `.planning/codebase/STACK.md` | Task 2 |
| Record the change in `CHANGELOG.md` under `[Unreleased]`; no version bump | Task 2 |
| `npm run check` green; pre-commit clean before commit; exclude the four `.claude/` paths | Task 3 |

Planner addition, flagged for the executor: `.planning/PROJECT.md:407` carries
the SAME constraint sentence as `CLAUDE.md:58` (CLAUDE.md's Constraints section
is generated from PROJECT.md). Editing only CLAUDE.md would let the stale figure
return on the next regeneration, so Task 2 updates both.

Deliberately NOT touched: `docs/competitive-analysis/*.md` (dated analysis
snapshots that record what the repo developed against at the time of writing)
and `.claude/skills/spike-findings-*/references/progress-messages.md` (a
generated spike artifact that a later spike wrap-up would rewrite anyway).
</source_coverage_audit>

<tasks>

<task type="tracer">
  <name>Task 1: Bump the devDependency and handle the deferred stop reason end to end</name>
  <precondition>The working tree is on branch `features/pi-coding-agent-0-84-2` in the main checkout (`git rev-parse --abbrev-ref HEAD`), and the npm registry is reachable for the install.</precondition>
  <files>package.json, package-lock.json, extensions/pi-claude-marketplace/bridges/hooks/settle.ts, tests/bridges/hooks/settle.test.ts</files>
  <behavior>
    - A cached assistant message whose stop reason is the new in-flight value must NOT dispatch the Stop bucket (the injected executor records nothing).
    - The same message must NOT produce a re-entry (`pi.sendMessage` is never called, `sent.length === 0`).
    - The existing test asserting that an unrecognized stop reason is debug-logged and dropped without throwing must keep passing unchanged.
  </behavior>
  <action>
    Do all three edits before running anything; the bump alone leaves the tree
    red, and none of this is committed until Task 3.

    1. Install the bump so package.json and the lockfile move together:
       `npm install --save-dev @earendil-works/pi-coding-agent@^0.84.2`.
       Do NOT hand-edit package-lock.json. Leave `@earendil-works/pi-tui` and
       the `peerDependencies` floor `>=0.80.5` untouched. Afterwards run
       `git diff --stat package.json package-lock.json` and confirm the churn is
       confined to the pi packages — if unrelated dependency entries moved,
       `git checkout -- package-lock.json package.json` and retry with the same
       targeted install rather than a bare `npm install`.

    2. In `extensions/pi-claude-marketplace/bridges/hooks/settle.ts`, extend the
       existing defensive no-op case group in `settleHandlerFor`'s
       `switch (last.stopReason)` — the group currently listing pending,
       aborted and toolUse ahead of a bare `return;` — with the new
       in-flight value, and put a SHORT comment above the group explaining why
       it is a no-op: the provider request was deferred to a batch/async lane
       and the message carries a `DeferredHandle` to poll for the real response
       later, so it is an in-flight state like pending rather than a turn
       ending; running the Stop bucket on it would fire turn-boundary hooks on
       a turn that has not ended. Keep the `default:` arm exactly as it is,
       including `const unknownStopReason: never = last.stopReason;` — that pin
       is what caught this widening and must keep catching the next one.

       Also refresh the two doc comments that enumerate the no-op set and are
       now stale: the module header block at the top of the file (the sentence
       ending "dispatch nothing") and the `settleHandlerFor` JSDoc sentence
       ending "are a defensive no-op". Both currently omit pending as well;
       state the full no-op set in each.

       Comment policy (`.claude/rules/typescript-comments.md`): cite the durable
       ID STOP-01 as the anchor. Never cite a GSD planning artifact — no phase,
       plan, wave or task references.

    3. In `tests/bridges/hooks/settle.test.ts`, add a test directly after
       `STOP-01: stopReason toolUse is a defensive no-op`, mirroring it exactly:
       same `_resetForTest()` / `resetSettleState()` preamble, same recording
       `injectedExecutor`, same `_setRoutingBucketForTest("Stop", [makeStopEntry("p1")])`,
       then `agentEndCacheHandler(epoch)(makeAgentEnd("deferred"))` and the two
       assertions on `fired` and `sent.length`. `makeAgentEnd` is typed
       `(stopReason: StopReason)`, so the literal only type-checks after step 1
       — that ordering is the point. Give the test name and BOTH assertion
       messages their own wording rather than copying the toolUse strings
       verbatim: `sonarjs/no-identical-functions` is an ESLint error in this
       repo and byte-identical callback bodies would trip it. Update the
       section banner comment above the aborted test, which currently names only
       two reasons, to cover the group as it now stands.
  </action>
  <verify>
    <automated>npm run typecheck && node --test tests/bridges/hooks/settle.test.ts && grep -c 'const unknownStopReason: never = last.stopReason;' extensions/pi-claude-marketplace/bridges/hooks/settle.ts && node -p "require('./package.json').devDependencies['@earendil-works/pi-coding-agent']"</automated>
  </verify>
  <done>`npm run typecheck` exits 0 with the 0.84.2 types installed; the settle suite passes including the new deferred test; the `never` pin grep returns 1; the devDependency prints `^0.84.2`.</done>
  <reversibility rating="reversible">A dependency range and one switch arm; revert is `git checkout` plus `npm install`.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Refresh the stale dev-dependency figure and record the change</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/codebase/STACK.md, CHANGELOG.md</files>
  <action>
    Still no commit — Task 3 owns the single commit.

    1. `CLAUDE.md:58` and `.planning/PROJECT.md:407` carry the same sentence:
       "peer dependency, pinned to `>=0.80.5` (dev `^0.83.0`)". Update the dev
       figure to `^0.84.2` in BOTH files. The peer floor `>=0.80.5` and the
       NFR-11 clause after it stay exactly as they are. PROJECT.md is the source
       the CLAUDE.md Constraints section is generated from, which is why both
       move together.

    2. `.planning/codebase/STACK.md:28` says "(peer dep `>=0.80.5`, dev dep
       `^0.83.0`)". Update the dev-dep figure only.

    3. Add a `[Unreleased]` section at the very top of `CHANGELOG.md`, above
       `## [0.16.0] - 2026-08-18`, following the file's existing style: an
       `## [...]` heading followed by `- ` bullets written as prose sentences,
       not terse fragments. One bullet is enough. Say what changed and why in
       user-facing terms: the development dependency on the Pi host API moved to
       0.84.2, whose stop-reason vocabulary gained a value for a provider
       request that was deferred to a batch or asynchronous lane, and the
       turn-boundary hook dispatcher treats that value as an in-flight state
       that fires no Stop hooks, the same as it already treats a pending
       request. Do NOT bump the version in `package.json`,
       `sonar-project.properties`, or the extension version constant —
       versioning is a separate step the developer decides on before the PR.
  </action>
  <verify>
    <automated>grep -c '0\.84\.2' CLAUDE.md .planning/PROJECT.md .planning/codebase/STACK.md && head -3 CHANGELOG.md | grep -c 'Unreleased' && node -p "require('./package.json').version"</automated>
  </verify>
  <done>All three docs report the dev dependency as `^0.84.2` while still stating the peer floor `>=0.80.5`; `CHANGELOG.md` opens with an `[Unreleased]` section describing the change; `package.json` `version` is unchanged at 0.16.0.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full gate, clear pre-commit, and land one commit</name>
  <files>(no new edits — verification and commit only)</files>
  <action>
    1. Run the full gate: `npm run check`. It chains typecheck, lint, fallow
       (dead-code + health + dupes), format:check, the unit suite and the
       integration suite, and every link is mandatory. Allow up to 10 minutes.
       Fix any failure at its source and re-run until it exits 0. If fallow
       reports the new test as a duplicate clone, do NOT add an `ignoredClones`
       entry — differentiate the test body instead (the assertion messages are
       the natural axis) or, if that is not enough, collapse the three
       defensive-no-op tests into one table-driven test over the reason list.

    2. Stage ONLY this task's files:
       `git add package.json package-lock.json extensions/pi-claude-marketplace/bridges/hooks/settle.ts tests/bridges/hooks/settle.test.ts CHANGELOG.md CLAUDE.md .planning/PROJECT.md .planning/codebase/STACK.md`.
       Never `git add -A` / `git add .`: `.claude/settings.json`,
       `.claude/commands/upstream-releases.md`, `.claude/jobs/` and
       `.claude/package.json` are unrelated pre-existing working-tree changes
       and must stay out of this commit.

    3. Run `pre-commit run --files <the eight staged paths>` BEFORE committing.
       Fix every failure, re-stage the fixed files, and re-run until the run is
       clean. This is the main checkout, not a worktree, so trufflehog runs
       normally — no `SKIP=` prefix. Never `--no-verify`. If a hook fails, the
       commit did not happen: fix and commit again, never `--amend`.

    4. Commit once, Conventional Commits, ASCII only — the fix-unicode-dashes
       hook rejects em dashes in COMMIT_EDITMSG, so use plain hyphens in the
       message. Title between 5 and 72 characters, body lines at most 80.
       Suggested title: `chore(deps-dev): bump pi-coding-agent to 0.84.2 and handle deferred`.
       Body: state that 0.84.2 widens the stop-reason union with a value for a
       deferred provider request, that the compile-time exhaustiveness pin
       (NFR-7) caught it, and that the new value joins the defensive no-op group
       because it is in-flight rather than a turn ending (STOP-01). Do not
       mention GSD phases, plans or waves.

    5. Confirm the four excluded paths survived untouched:
       `git status --short` must still show ` M .claude/settings.json` and the
       three untracked `.claude/` entries.
  </action>
  <verify>
    <automated>npm run check && git show --stat --name-only HEAD | grep -c 'settle' && git status --short | grep -c '.claude/'</automated>
  </verify>
  <done>`npm run check` exits 0; `pre-commit run --files ...` ran clean before the commit; exactly one new commit exists on `features/pi-coding-agent-0-84-2` containing the eight files; the four `.claude/` paths remain uncommitted in the working tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local dev tree | A third-party package version enters the build and the type surface the extension compiles against |
| Pi host -> hooks bridge | `AgentEndEvent.messages` is host-supplied data whose `stopReason` selects the dispatch arm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-K7F-SC | Tampering | `npm install --save-dev @earendil-works/pi-coding-agent@^0.84.2` | medium | mitigate | Not a new package: `@earendil-works/pi-coding-agent` is an existing pinned peer + dev dependency of this repo, already audited, so no `[ASSUMED]` legitimacy checkpoint applies. Task 1 constrains the install to that one name and requires `git diff --stat package-lock.json` to show pi-scoped churn only, so an unrelated transitive substitution is caught before it is staged. Pre-commit runs trufflehog over the staged files (Task 3). |
| T-K7F-01 | Denial of Service | `settleHandlerFor` deferred arm | low | mitigate | The new arm returns without dispatching, so a deferred (still in-flight) turn cannot fire Stop hooks, cannot call `pi.sendMessage`, and cannot increment the STOP-07 re-entry counter. Asserted by the new test's `sent.length === 0` and `fired` deep-equal checks. |
| T-K7F-02 | Tampering | `default:` exhaustiveness pin | medium | mitigate | The `never` pin is preserved verbatim and positive-grepped in Task 1's `<verify>`, so the next union widening fails typecheck instead of silently falling into the drop path. |
</threat_model>

<verification>
- `npm run typecheck` exits 0 against the installed 0.84.2 types (was one TS2322).
- `node --test tests/bridges/hooks/settle.test.ts` passes, including the new deferred no-op test and the unchanged unknown-stop-reason drop test.
- `npm run check` exits 0 end to end.
- `grep 'const unknownStopReason: never' extensions/pi-claude-marketplace/bridges/hooks/settle.ts` still matches.
- `tests/architecture/peer-floor.test.ts` still passes: `peerDependencies` remains `>=0.80.5`.
- `git log -1 --stat` shows one commit with exactly the eight intended files.
</verification>

<success_criteria>
- `@earendil-works/pi-coding-agent` devDependency is `^0.84.2` in `package.json` with a lockfile regenerated by npm.
- `settle.ts` treats the deferred stop reason as a defensive no-op alongside pending / aborted / toolUse, with a STOP-01-anchored comment and the `never` pin intact.
- A unit test pins that behavior.
- `CLAUDE.md`, `.planning/PROJECT.md` and `.planning/codebase/STACK.md` state the new dev figure; the peer floor is unchanged.
- `CHANGELOG.md` has an `[Unreleased]` entry; no version was bumped.
- One green commit on `features/pi-coding-agent-0-84-2`; the four unrelated `.claude/` paths are untouched.
</success_criteria>

<output>
Create `.planning/quick/260818-k7f-bump-pi-coding-agent-to-0-84-2-and-handl/260818-k7f-SUMMARY.md` when done
</output>
