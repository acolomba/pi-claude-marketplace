---
phase: quick-260814-q4h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/pi-claude-marketplace/index.ts
  - tests/integration/hooks-dispatch-end-to-end.test.ts
  - CHANGELOG.md
  - .planning/debug/resolved/project-scope-sessionstart-never-dispatches.md
  - .planning/debug/knowledge-base.md
  - .planning/BACKLOG.md
  - .planning/workstreams/hooks-sessionstart-hydrate/STATE.md
autonomous: true
requirements: [DISP-01, WR-05, NFR-2]
must_haves:
  truths:
    - "PR #127's contributor commit 41c60073 is preserved unrewritten (no rebase, no amend)"
    - "npm run check is fully green (typecheck + lint + format:check + unit + integration)"
    - "HOOK-E2E-03 fails if the session_start _shared mkdir gate stops being scope-aware"
    - "CHANGELOG records the fix under [Unreleased] with no version bump anywhere"
    - "No version field is touched: package.json, package-lock.json, EXTENSION_VERSION, sonar.projectVersion all unchanged"
    - "The planning-tree workstream migration (ROADMAP.md/STATE.md relocation) is NOT part of this change"
  artifacts:
    - path: ".planning/debug/resolved/project-scope-sessionstart-never-dispatches.md"
      provides: "Root-cause record with the verified pi event-order evidence"
    - path: ".planning/debug/knowledge-base.md"
      provides: "Digest entry so gsd-debugger surfaces this pattern on future hook investigations"
    - path: ".planning/BACKLOG.md"
      provides: "HKDIR-01 for the adjacent cross-scope mkdir gate this PR does not fix"
  key_links:
    - from: "session_start registration"
      to: "hydrateProjectScopeForCwd"
      via: "wrapper handler reading ctx.cwd before delegating"
      pattern: "hydrateProjectScopeForCwd\\(ctx\\.cwd\\)"
---

<objective>
Land external PR #127 (@rakesh-vs, "fix: hydrate project-scope SessionStart hooks at
session_start time") complete: verify the fix is correct, add the two review findings
it is missing, record the defect in the debug knowledge base, and file the adjacent
defect it surfaces -- without rewriting the contributor's commit and without bumping
the version.

The contributor's diagnosis and fix are correct as submitted. Verification performed
before accepting: the claimed pi event order is real (`agent-session.js:1761-1762`
emits session_start, then `extendResourcesFromExtensions` fires resources_discover;
same order on reload at 2070-2071); `ExtensionContext.cwd` is populated for every
event (`types.d.ts:216`, served from a runner getter at `runner.js:473-475`), which
refutes the v1.12 research assumption that opened the gap; the new test is a genuine
regression gate (reverting only event-router.ts makes HOOK-E2E-02 fail 0-vs-1); and
`npm run check` is green on the branch (3442 unit, 19 integration, 0 failures).

Two gaps remain, both raised in review:
1. `index.ts` still documents project-scope hydrate as deferred to `resources_discover`
   alone. There are now two deferral points and the order between them matters.
2. Nothing pins the WR-05 half of the new gate. The lazy hydrate creates the project
   `_shared` dir only when a project-scope SessionStart entry exists; if that gate
   regressed, the bridge would start creating `.pi/` inside users' projects on every
   session start. Verified correct by hand; not covered by a checked-in test.

Purpose: land a correct community contribution complete, and leave the durable
knowledge behind it in the repo rather than in a PR thread.
Output: commits on the contributor's branch `fix/session-start-project-scope-hydrate`,
pushed to rakesh-vs's fork so PR #127 carries the whole change.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.claude/rules/typescript-comments.md
@extensions/pi-claude-marketplace/bridges/hooks/event-router.ts
@extensions/pi-claude-marketplace/index.ts
@tests/integration/hooks-dispatch-end-to-end.test.ts
@CHANGELOG.md
</context>

<tasks>

### Task 1 — Worktree, workstream, and branch setup

- Fetch PR #127 head; confirm `main` is already an ancestor (no merge needed) and the
  branch is exactly main + contributor commit 41c60073.
- Create `.worktrees/pr-127` on that branch. GSD tooling is gitignored, so `cp -a`
  `.claude/gsd-core` and `.claude/scripts` into the worktree.
- Create the `hooks-sessionstart-hydrate` workstream.
- **Constraint:** `workstream create` migrates a single-stream `.planning/` to
  workstream mode, relocating `ROADMAP.md` and `STATE.md` under
  `.planning/workstreams/milestone/`. Revert that migration — a planning-tree
  restructure must not ride along inside a community bugfix PR, and main has an
  in-flight `workflows` workstream that it would collide with.

### Task 2 — Close the two review findings

- `index.ts`: rewrite the step-3 comment so it names both deferral points and states
  which runs first and why (`session_start` precedes `resources_discover`), rather
  than naming `resources_discover` alone.
- `tests/integration/hooks-dispatch-end-to-end.test.ts`: add `HOOK-E2E-03`. Seed a
  user-scope-only SessionStart plugin, boot the bridge against a third cwd so the
  factory's own mkdir cannot land in the project and mask the result, fire
  `session_start` with `ctx.cwd` = a pristine project, assert that cwd is still empty.

### Task 3 — Record the knowledge

- `.planning/debug/resolved/project-scope-sessionstart-never-dispatches.md`: root-cause
  record. State its provenance honestly — this came from PR review, not a `/gsd-debug`
  session — and carry the mechanically verified evidence, not the PR's claims.
- `.planning/debug/knowledge-base.md`: digest entry in the existing bullet shape.
- `.planning/BACKLOG.md`: `HKDIR-01` for the factory-time `_shared` mkdir gate, which
  tests the whole cross-scope routing table instead of the scope it is about to write.
- `CHANGELOG.md`: entry under a new `## [Unreleased]` heading, crediting @rakesh-vs
  and #127. No version bump — deliberately held.

### Task 4 — Verify and push

- `npm run check` green in the worktree.
- `pre-commit run --all-files` (CI runs it at that scope; a scoped run hides
  pre-existing violations until the Lint job fails).
- Commit with `SKIP=trufflehog` only after a filesystem trufflehog scan over the
  staged paths comes back clean — the git-mode hook cannot run in a linked worktree.
- Stage explicit paths only. Push to the contributor's fork.

</tasks>
